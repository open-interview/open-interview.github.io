[
  {
    "id": "test-databricks-data-engineer",
    "channelId": "databricks-data-engineer",
    "channelName": "Databricks Data Engineer",
    "title": "Databricks Data Engineer Knowledge Test",
    "description": "Test your understanding of Databricks Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "databricks-data-engineer-q-0",
        "questionId": "q-25795",
        "question": "Design a Databricks data pipeline for 4 tenants ingesting 100 GB/day of Parquet logs from S3. Build a Silver layer using Auto Loader with per-file provenance and automatic schema drift handling, then a streaming MERGE into a tenant-scoped Gold table partitioned by tenant_id and date. Implement a tenant-aware data quality gate, dynamic Unity Catalog masking, and a 1-day watermark for late data. Include a testing plan with synthetic late data and drift scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Auto Loader with per-file provenance and a drift-aware schema evolution policy, then stream a MERGE into a tenant-scoped Gold Delta table partitioned by tenant_id/date. Enforce RBAC + masking in U",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a 5 TB/day multi-tenant Kafka JSON DLTS design: Silver preserves per-record provenance (topic, partition, offset, ingest_ts) in AuditFiles via a lightweight UDF; Delta CDF enables per-tenant C",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Bronze from S3 JSON (Auto Loader) and Kafka; Silver computes per-user features (last_seen, total_events, avg_session_len) with a 3-minute watermark; Gold registers features in the Databricks Feature S",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Route ingestion errors to a dedicated ErrorEvents Delta table and quarantine failing files, while Bronze, Silver, Gold streams continue. Use Auto Loader with file-level error handling, capture tenant_",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design end-to-end multi-tenant pipelines with schema evolution, streaming upserts, and data governance in Databricks.\n\n## Key Concepts\n- Auto Loader with per-file provenance and schema drift handling\n- Streaming MERGE for upserts into tenant-scoped Gold\n- Unity Catalog RBAC and dynamic masking\n- Data quality gates and late data handling with watermarking\n\n## Code Example\n\n```python\n# skeleton: define Bronze->Silver streaming, then MERGE into Gold with watermark\nfrom pyspark.sql.functions import expr\n# read stream, apply schema drift checks, write to Gold with MERGE\n```\n\n## Follow-up Questions\n- How would you test schema drift without downtime?\n- How would you rollback a failed MERGE in production?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-1",
        "questionId": "q-4798",
        "question": "In a Databricks pipeline, you ingest 1 TB/day of Parquet logs into a Delta table via Auto Loader. The table has many small files causing frequent small-file problems, and downstream queries occasionally misreport counts due to late data and churn. Propose a safe, automated nightly compaction strategy that preserves CDC correctness, avoids breaking streaming readers, and uses ZORDER. Outline concrete steps, toggles, and a lightweight test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Load 3 GB/day Parquet logs from S3 into a Bronze Delta table using Auto Loader. Build a Silver layer that enriches each event by left joining with the small static dimension table customers (customer_id, country, segmen…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a two-tier store: offline features in Delta tables (Bronze-Silver-Gold) with feature_version and lineage, and an online store using Delta with a low-latency cache. Register features in a Unity Cat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Nightly two-phase compaction: Phase A creates a staging Delta table by reading the Bronze with read-consistency, rewrites small Parquet files into larger ones (target ~128 MB) using OPTIMIZE with ZORD",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Read Bronze with a permissive schema; map to a canonical Silver schema: tenant_id, event_id, event_ts, and payload (struct). Unknown fields go into payload to avoid drift. Use a MERGE into Silver on (",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical mastery of Delta Lake maintenance, streaming safety, and data correctness under schema/file churn. It probes operational thinking beyond basic pipeline design.\n\n## Key Concepts\n- Small-file problem and file size optimization\n- OPTIMIZE with ZORDER for read performance\n- CDC preservation via MERGE back into live table\n- Safety for streaming readers during maintenance\n- Rollback via Delta time travel and test verification\n\n## Code Example\n```sql\n-- Phase A: stage compacted data\nCREATE OR REPLACE TABLE bronze_staging AS\nSELECT * FROM delta.`/bronze/logs`;\nOPTIMIZE delta.`/bronze/logs` ZORDER BY ingestion_key;\n\n-- Phase B: merge back to preserve CDC\nMERGE INTO delta.`/bronze/logs` AS live\nUSING delta.`/bronze/logs_staging` AS stage\nON live.id = stage.id\nWHEN MATCHED THEN UPDATE SET *\nWHEN NOT MATCHED THEN INSERT *;\n```\n\n## Follow-up Questions\n- How would you monitor compaction impact on streaming queries?\n- What toggles would you expose to operators (e.g., enable/disable, dry-run)?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-2",
        "questionId": "q-18231",
        "question": "In a Databricks project ingesting 40 GB/day of JSON events from a multi-tenant S3 bucket into a Bronze Delta table, design a beginner Silver layer that (1) flattens nested JSON and normalizes fields into a tenant-scoped Silver table, (2) uses Auto Loader with per-file provenance (source_path, ingest_ts), (3) performs upserts via MERGE on event_id to simulate CDC, (4) enforces Unity Catalog RBAC so tenants see only their data, (5) implements a basic data-quality gate (NOT NULL on key fields, valid timestamps) and a 2-day late-data watermark; outline concrete steps, Spark SQL/UDFs used, and a lightweight test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Delta Live Tables pipeline with a bronze Kafka stream feeding a silver customer_dim SCD2. Implement CDC via a MERGE that closes the active row (set end_date, current=false) and inserts a new row with start_date…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Auto Loader to ingest JSON into Bronze; flatten nested fields into a tenant-scoped Silver table; implement MERGE on (tenant_id, event_id) to upsert; enforce Unity Catalog RBAC with per-tenant view",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage Auto Loader to ingest into Silver, retain per-file provenance in AuditFiles (source_path, file_timestamp, ingest_ts) and expose via a provenance view; implement a JSON drift policy with an ev",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Silver path that uses a central TenantSchema registry to normalize mixed JSON/Parquet per tenant, with a dynamic schema-merge UDF that evolves fields without breaking pipelines. Implement per",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical Silver-layer design for multi-tenant JSON data, focusing on isolation, CDC-like updates, and late-arrival handling with simple data-quality checks.\n\n## Key Concepts\n- Auto Loader with per-file provenance\n- MERGE-based CDC pattern on (tenant_id, event_id)\n- Flattening nested JSON via Spark SQL\n- Unity Catalog RBAC and per-tenant views\n- Data-quality gates and late-data watermark\n\n## Code Example\n```sql\nMERGE INTO silver AS s\nUSING bronze b\nON s.tenant_id = b.tenant_id AND s.event_id = b.event_id\nWHEN MATCHED THEN UPDATE SET ...\nWHEN NOT MATCHED THEN INSERT (...)\n```\n\n## Follow-up Questions\n- How would you test schema drift and RBAC changes?\n- How would you extend to thousands of tenants?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-3",
        "questionId": "q-1658",
        "question": "Design a secure external data sharing workflow in a Databricks environment using Unity Catalog and Delta Sharing to expose aggregated metrics derived from production Delta tables to external partners while maintaining tenant isolation and governance. Include data model, masking strategy, per partner quotas, refresh cadence, and how to monitor and revoke access?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: ingest with Auto Loader; store per-file provenance in AuditFiles (source_path, file_timestamp, ingest_ts); use Delta schema evolution for nested JSON; CDC-like upserts to Gold on (tenant_id,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Delta Sharing producer dataset based on a Silver view (tenant_id, metric, value) with masking on sensitive fields. Publish as a Delta Share to a consumer role with read access only. Enforce per",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a Silver layer that uses Auto Loader to read JSON and Parquet from a multi-tenant S3 path, infers schema with drift policy, and writes to Delta with a per-file provenance row in an AuditFiles t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose using DLT with per-tenant watermarks, deterministic MERGE keys, and row-level constraints. Bronze ingest from Kafka, then Silver with per-tenant SCD2 and a deterministic MERGE into tenant-scop",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests Delta Sharing, Unity Catalog governance, masking, quotas, and revocation in real-world partner data sharing.\n\n## Key Concepts\n- Delta Sharing producer/consumer model\n- Unity Catalog RBAC and quotas\n- Dynamic data masking policies\n- Data freshness and incremental refresh\n- Audit logs and compliance\n\n## Code Example\n```sql\n-- Create share and grant access to partner\nCREATE SHARE partner_agg_share;\nALTER SHARE partner_agg_share ADD CONSUMER 'partner_catalog' WITH 'us-east-1';\nGRANT SELECT ON database.shared_view TO SHARE partner_agg_share;\n```\n\n## Follow-up Questions\n- How would you handle schema drift in the shared view without breaking consumers?\n- What monitoring metrics indicate misuse or quota breaches?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-4",
        "questionId": "q-23880",
        "question": "In a Databricks platform ingesting 200 GB/day of regional mobile telemetry split across 4 regions and 120 apps, design a cross-tenant data governance pattern with a Silver layer that guarantees tenant isolation, a real-time per-tenant Gold layer, and Delta Sharing exports. Include a plan for per-tenant partitioning, schema evolution, CDC-like updates, masking via Unity Catalog, data-quality gates, late-data handling, testing, and a comparison of Delta Live Tables vs Structured Streaming for these layers?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-tenant Silver layer ingesting 100 GB/day of Parquet from S3 with Auto Loader provenance. Add a Contracts table describing per-tenant schemas, required fields, and ranges, plus a policy",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up a Bronze ingestion with Auto Loader for 20 GB/day CSVs. Silver layer: (1) normalize timestamps to UTC; (2) add ingest_ts and a 1h window_bucket; (3) deduplicate by record_id for the current day",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a tenant-aware data plane: Bronze layer uses Auto Loader to ingest JSON telemetry from regional S3 buckets with automatic schema inference. Silver layer implements tenant isolation through Delta tables partiti…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Auto Loader with per-file provenance to an Audit table; enable schema drift handling with non-destructive evolution for nested JSON; MERGE into Gold on (tenant_id, event_id) for deterministic upse",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates hands-on expertise in multi-tenant data governance across Bronze-Silver-Gold architectures with real-time requirements and secure data sharing—key production concerns for enterprise data platforms.\n\n## Key Concepts\n- Tenant isolation via partitioning and RBAC\n- Unity Catalog for fine-grained access control and column masking\n- Delta Lake schema evolution and constraints\n- CDC semantics with merge operations\n- Delta Sharing for secure cross-tenant exports\n- Late-data handling through watermarking\n- Data quality gates with validation and quarantine\n- Delta Live Tables vs Structured Streaming trade-offs\n\n## Code Example\n```python\n# Per-tenant Silver layer with partitioning and schema evolution\nsilver = (\n  spark.readStream.format(\"cloudFiles\")\n    .option(\"cloudFiles.format\", \"json\")\n    .option(\"cloudFiles.schemaHints\", \"tenant_id STRING, region STRING, timestamp STRING, payload STRING\")\n    .load(\"s3://region-*/tenant-*/\")\n    .withColumn(\"ingest_ts\", current_timestamp())\n    .withColumn(\"tenant_id\", col(\"metadata.tenant_id\"))\n    .withColumn(\"region\", col(\"metadata.region\"))\n)\n\n(silver.writeStream\n  .partitionBy(\"tenant_id\", \"region\")\n  .option(\"mergeSchema\", \"true\")\n  .option(\"checkpointLocation\", \"/mnt/checkpoints/silver\")\n  .trigger(availableNow=True)\n  .toTable(\"catalog.silver.telemetry\"))\n\n# CDC-like merge for Silver to Gold\nfrom delta.tables import DeltaTable\n\nsilver_table = DeltaTable.forName(spark, \"catalog.silver.telemetry\")\ngold_table = DeltaTable.forName(spark, \"catalog.gold.telemetry_by_tenant\")\n\n(gold_table.alias(\"target\")\n  .merge(silver_table.alias(\"source\"), \"target.tenant_id = source.tenant_id AND target.event_id = source.event_id\")\n  .whenMatchedUpdate(set={\n    \"payload\": \"source.payload\",\n    \"updated_at\": \"source.ingest_ts\",\n    \"_cdc_operation\": \"U\"\n  })\n  .whenNotMatchedInsert(values={\n    \"tenant_id\": \"source.tenant_id\",\n    \"event_id\": \"source.event_id\",\n    \"payload\": \"source.payload\",\n    \"created_at\": \"source.ingest_ts\",\n    \"_cdc_operation\": \"I\"\n  })\n  .execute())\n\n# Unity Catalog masking policy\nfrom pyspark.sql.functions import udf\n\n@udf\ndef mask_pii(ssn: str) -> str:\n    return \"***-**-\" + ssn[-4:] if ssn else None\n\nspark.sql(\"\"\"\nCREATE OR REPLACE MASKING POLICY catalog.security.mask_ssn\nAS (ssn STRING) RETURN\n  CASE WHEN CURRENT_USER() = 'admin' THEN ssn\n  ELSE mask_pii(ssn)\n  END\n\"\"\")\n```",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-5",
        "questionId": "q-7630",
        "question": "In a Databricks data platform, ingest 200 TB/week Parquet logs from S3 and 50 TB/week JSON telemetry from a REST API, multi-tenant. Build a Bronze-Silver-Gold lakehouse with a canonical_id dedup via MERGE CDC, and a 20-minute watermark for late data. Add per-file AuditDelta (path, ingest_ts, file_size, source_name) and enforce Unity Catalog RBAC on Silver/AuditDelta. Enable Delta Sharing for Gold with tenant-scoped access. Outline concrete steps, data-quality rules, and a lightweight synthetic-data test plan to verify CDC, watermark, RBAC, and sharing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement Silver with a governance policy table driving Delta Sharing exposure per tenant, plus Unity Catalog dynamic masking; persist per-file provenance in AuditFiles; handle schema drift with evolu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Delta Lake with a 3-layer Lakehouse; Bronze ingests Parquet/JSON from S3 and REST; Silver normalizes to a canonical schema and performs MERGE CDC on (canonical_id, event_time_bucket); late da",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Auto Loader to Bronze with source_path and ingest_ts; Silver MERGE on event_id; SchemaChangeLog versioning for drift; data_dict view with name, type, sample; RBAC and masking for PII; test plan with s",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a centralized data-contract registry with versioned schemas and field-level expectations; enforce via Delta Live Tables contract checks. On version bumps, trigger Bronze backfill, then casca",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end design skill for Databricks: multi-source ingestion, CDC semantics, watermarking for late data, per-file auditing, and robust governance via Unity Catalog and Delta Sharing.\n\n## Key Concepts\n- Delta Lake CDC with MERGE\n- Watermarking and late data handling\n- AuditDelta and file-level lineage\n- Unity Catalog RBAC and Delta Sharing\n- Synthetic data for validation\n\n## Code Example\n```javascript\n// No code required for the interview prompt\n```\n\n## Follow-up Questions\n- How would you handle schema evolution while preserving CDC guarantees?\n- How do you validate RBAC in automated tests?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-6",
        "questionId": "q-27306",
        "question": "Ingest 80 GB/day of Parquet logs from S3 for multiple tenants. Design a Databricks Silver layer that adds per-tenant, per-hour statistical sketches (HyperLogLog for distinct counts; Bloom filters for key existence) stored in a Stats table, triggers data-quality alerts when drift thresholds are exceeded, and performs a deterministic MERGE into a tenant-scoped Gold table. Outline ingestion, sketches, schema evolution, testing with synthetic late data, and TTL cleanup?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Three-layer lakehouse: Bronze (Kafka → Delta), Silver (per-tenant masking via Unity Catalog dynamic masking with tenant scoping), Gold (dashboards + features). Enforce RBAC, use DLT with schema evolut",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Silver layer using Auto Loader with schema inference for both JSON and Parquet sources. Preserve per-record provenance by appending metadata columns (ingestion_timestamp, source_file_path, source_partition) to…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Auto Loader for 200 GB/day; Silver with Delta Live Tables + CDC to incrementally refresh versioned features; AuditFiles for per-file provenance; JSON drift handled via Evolution with defaults",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Auto Loader; Silver adds per-tenant, per-hour sketches (HLL for uniques; Bloom filters for key existence) stored in a separate Stats table; update via MERGE; Gold upsert on (tenant_id, event_id).",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests ability to augment streaming Delta pipelines with lightweight statistical sketches to enable proactive data quality and cost controls in multi-tenant environments.\n\n## Key Concepts\n- Delta Lake MERGE and time-bucketed windows\n- HyperLogLog/approximate distinct counts\n- Bloom filters for presence checks\n- per-tenant RBAC via Unity Catalog and robust data quality gates\n- TTL/retention via VACUUM and data lifecycle\n\n## Code Example\n```scala\n// pseudo: compute hourly sketch stats per tenant and upsert\nval stats = df\n  .withWatermark(\"event_ts\", \"1 hour\")\n  .groupBy(\"tenant_id\", window($\"event_ts\", \"1 hour\").as(\"hour\"))\n  .agg(approx_count_distinct(\"entity_id\").as(\"hll_distinct\"))\n// MERGE into Stats and Gold tables as part of a single workflow\n```\n\n## Follow-up Questions\n- How would you test drift thresholds and alerting in CI/CD?\n- How would you backfillSketch data when late data arrives after the window closes?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-7",
        "questionId": "q-4330",
        "question": "In a Databricks data lake for a fintech app, ingest 8-12 TB/day JSON events from S3 via Auto Loader and a streaming CDC feed from Postgres. Build Bronze/Silver/Gold with strict per-record RBAC via Unity Catalog, implement field-level masking for SSN at query time, and introduce a progressive data quality gate that halts Silver if latency P95 exceeds threshold. Include schema evolution, partitioning, and a lightweight test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Auto Loader to ingest Parquet with per-file provenance, create a Silver table, compute and store a file checksum and per-file data-quality metrics for nulls/duplicates/schema drift, and MERGE into",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Silver table keyed by (device_id, sensor_type) storing the latest value per key, while preserving full history in a separate history mechanism. Upsert Bronze to Silver via MERGE with a 2-minute",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest from Kafka (stream) and REST API (batch) into Bronze with Auto Loader and schema evolution, then Silver masking PII and applying a 2-minute watermark. Use MERGE CDC to upsert into Gold where Cu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Auto Loader to ingest 8-12 TB/day JSON into Bronze; Silver deduplicates on event_id with MERGE CDC, and uses a 2-minute watermark. Mask SSN at query time via a masking VIEW over Silver. Enforce pe",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical data governance, schema evolution, and quality gates in a multi-tenant lakehouse. It emphasizes RBAC at the data layer, query-time masking, and production-safe progression from Bronze to Gold with latency guards.\n\n## Key Concepts\n- Delta Lake Bronze-Silver-Gold modeling\n- Unity Catalog RBAC and row/column masking\n- MERGE CDC and watermarking for late data\n- Delta Live Tables expectations for data quality\n\n## Code Example\n```javascript\n// Example placeholder snippet for masking view\n```\n\n## Follow-up Questions\n- How would you test masking performance under high cardinality SSN values?\n- How would you evolve the schema with minimal downtime while keeping downstream compatibility?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-8",
        "questionId": "q-20228",
        "question": "In a Databricks data mesh, 60 GB/day of multi-tenant JSON logs land in S3 with tenant_id in payload. Design a three-layer Delta Live Tables pipeline (Bronze/Silver/Gold): (1) preserve per-file provenance in a FilesAudit table, (2) enrich events with a reference Delta table (customer_meta) to add plan and region, (3) apply CDC-like MERGE on event_id into Silver, (4) expose a BI-ready Gold table partitioned by tenant_id/date, (5) enforce Unity Catalog RBAC with masking on PII, (6) implement a data-quality gate and a 5-minute late-data watermark, (7) craft a testing plan with synthetic late data and reference data drift; Additionally, describe how to keep reference data fresh without re-ingesting raw files and how to measure end-to-end latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant residency sandbox in Unity Catalog, encode provenance in AuditFiles during Silver-upsert, mask data at export time with tenant-scoped filters, expose Delta Sharing exports via p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Three-layer Delta Live Tables: Bronze ingests 60 GB/day from S3 with Auto Loader, preserving per-file provenance; Silver uses CDC-like MERGE on event_id and enriches events by joining a separate Delta",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Auto Loader with schema evolution, flatten nested fields, and a MERGE for idempotent upserts. ReadStream via cloudFiles, inferColumnTypes true, mergeSchema true. Flatten: user_id = user.id, email",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest Parquet from S3 via Auto Loader into Bronze; Silver adds per-file provenance using an Audit table and upserts to Silver on event_id; Gold aggregates to a BI-friendly table. Track ingestion_lag,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end pipeline design, multi-tenant governance, and realistic failure modes in Databricks.\n\n## Key Concepts\n- Delta Live Tables three layers (Bronze/Silver/Gold)\n- CDC-like MERGE on event_id\n- Join enrichment with reference data (customer_meta)\n- Per-tenant RBAC and masking via Unity Catalog\n- Data-quality gates and late-data watermark\n- Testing with synthetic late data and drift; refreshing reference data separately\n\n## Code Example\n```python\n# placeholder snippet showing a Silver table joining with reference data\nimport dlt\n@dlt.table\ndef silver():\n  events = dlt.read_stream(\"bronze\")\n  ref = dlt.read(\"customer_meta\")\n  return events.merge(ref, on=\"event_id\")\n```\n\n## Follow-up Questions\n- How would you handle drift in customer_meta and ensure idempotent MERGE semantics?\n- How would you scale this design across many tenants and regions?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-9",
        "questionId": "q-16761",
        "question": "In a Databricks pipeline ingesting 40 GB/day of Parquet logs from S3 for multiple tenants, design a beginner Silver layer that (1) writes per-file provenance to an AuditFiles table (path, ingest_ts), (2) uses MERGE on event_id to upsert into Silver, (3) exposes a masking view to redact PII (email, phone), and (4) enforces per-tenant isolation via Unity Catalog dynamic filters (tenant_id) so tenants see only their data. Include a lightweight data-quality gate (NOT NULL tenant_id and event_time, event_id unique within 5 minutes) and a test plan with synthetic data including a partially written file. Outline concrete steps and testing approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a Silver table that builds per-session features (session_id, user_id, event_ts, clicks, dwell, funnel_step) using 1h sliding windows, with a dynamic schema registry to absorb new event fields",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build Silver by Auto Loader into Bronze, then Silver with per-batch provenance (batch_id, source_path) and a simple schema-evolution rule; upsert into Gold using MERGE on event_id; publish via Delta S",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a two-branch flow: during Bronze ingestion, run a quick pre-check pass that lists files in the S3 prefix and validates Parquet footer/schema against a lightweight schema snapshot. If a file",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Auto Loader for incremental Bronze ingestion from S3. Implement per-file provenance by recording metadata in an AuditFiles table (path, ingest_ts) after each file completes. Use MERGE on event_id to upsert into Silv…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical knowledge of Silver layer design, combining data ingestion, provenance tracking, PII governance, and multi-tenant isolation with Unity Catalog.\n\n## Key Concepts\n- Auto Loader for incremental Bronze ingestion\n- MERGE for CDC upserts on event_id\n- AuditFiles table for per-file provenance\n- Masking view for PII redaction\n- Unity Catalog dynamic filters for tenant_id isolation\n- Data quality gates: NOT NULL constraints and uniqueness within time windows\n\n## Implementation Steps\n1. **Bronze Ingestion**: Auto Loader reads Parquet from S3 with schema inference and evolution support, landing raw data incrementally.\n2. **Provenance Tracking**: After each file loads, record its path and ingestion timestamp in the AuditFiles table.\n3. **Silver Transformation**: Apply the MERGE operation on event_id to upsert records into Silver, handling CDC deduplication.\n4. **PII Masking**: Create a view that redacts email and phone columns for downstream consumers.\n5. **Tenant Isolation**: Configure Unity Catalog dynamic filters on tenant_id to ensure each tenant accesses only their data.\n6. **Data Quality Gate**: Implement checks to reject records with NULL tenant_id or event_time, and identify duplicate event_ids within any 5-minute window.\n\n## Testing Approach\n- Generate synthetic tenant data with varied event_id patterns\n- Test NULL handling for tenant_id and event_time fields\n- Insert duplicate event_ids within 5-minute windows to verify detection\n- Simulate a partially written (in-flight) file to confirm fault tolerance and retry logic\n- Validate that dynamic filters correctly restrict data access per tenant",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-10",
        "questionId": "q-29160",
        "question": "In a Databricks pipeline ingesting 50 GB/day Parquet logs from S3 across 5 tenants, design a beginner Silver layer that (a) captures per-file provenance, (b) enforces a lightweight data-quality gate yielding a 0–1 score based on allowed event_type values and event_time within 90 days, (c) performs a deterministic MERGE upsert into Gold that aggregates counts by tenant_id and day, and (d) gracefully handles late data via a streaming watermark of 1 day with checkpointing. Provide a concrete test plan with synthetic late files?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a Silver layer as follows: 1) **Bronze → Silver with Auto Loader**: Use Auto Loader with `from_json` to ingest JSON logs and flatten nested fields into a structured table with columns (tenant_id, ts, e…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a DLT Silver pipeline that stores per-tenant provenance (source_path, ingest_ts, tenant_id), leverages Change Data Feed for CDC, applies a schema evolution policy for nested JSON using STRICT mode or ADD_COLUMNS…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design Silver with dedup by record_id, add processing_timestamp, and daily QA gate: if count falls outside baseline +/- 20%, stop downstream. Use Auto Loader for Bronze, MERGE to upsert Silver, mainta",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Auto Loader with `file_metadata` to preserve per-file provenance. Enforce a fixed schema upon ingestion. Implement a quality_score UDF that returns 1 if `event_type` in ('click', 'view', 'purchase') AND `event_time`…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical Databricks Silver-Gold architecture design with per-file provenance, data quality gating, and late-arriving data handling using streaming. It emphasizes correctness, testability, and simple CDC-like semantics suitable for a beginner-level candidate.\n\n## Key Concepts\n\n- **Auto Loader provenance**: Using `file_metadata` to capture source file path, size, and modification timestamp\n- **Data quality scoring**: Lightweight UDF-based gating with 0–1 score based on business rules\n- **Delta MERGE upserts**: Idempotent writes to Gold layer for daily tenant aggregations\n- **Streaming watermarking**: Handling late-arriving data with a 1-day watermark threshold\n- **Checkpointing**: Ensuring exactly-once processing and resumeability\n\n## Code Example\n\n```python\nfrom pyspark.sql import functions as F\nfrom pyspark.sql.window import Window\n\n# Bronze: Ingest with Auto Loader and provenance\nbronze_df = (spark.readStream\n    .format(\"cloudFiles\")\n    .option(\"cloudFiles.schemaLocation\", \"/mnt/bronze/schema\")\n    .option(\"cloudFiles.metadataColumn\", \"_metadata\")\n    .load(\"s3://logs/parquet/*.parquet\"))\n\n# Silver: Quality scoring UDF\n@F.udf\ndef quality_score(event_type: str, event_time: timestamp) -> int:\n    allowed_types = {\"click\", \"view\", \"purchase\"}\n    if event_type not in allowed_types:\n        return 0\n    days_90_ago = datetime.now() - timedelta(days=90)\n    if event_time < days_90_ago:\n        return 0\n    return 1\n\nsilver_df = (bronze_df\n    .withColumn(\"quality_score\", quality_score(F.col(\"event_type\"), F.col(\"event_time\")))\n    .withColumn(\"ingestion_timestamp\", F.current_timestamp()))\n\n# Route to quarantine vs pass\nquarantine_df = silver_df.filter(F.col(\"quality_score\") == 0)\nclean_df = silver_df.filter(F.col(\"quality_score\") == 1)\n\n# Write clean silver with watermark for late data\nquery = (clean_df\n    .withWatermark(\"event_time\", \"1 day\")\n    .writeStream\n    .format(\"delta\")\n    .outputMode(\"append\")\n    .option(\"checkpointLocation\", \"/mnt/silver/checkpoint\")\n    .table(\"silver_logs\"))\n\n# Gold: Daily aggregation with MERGE upsert\ngold_df = (spark.table(\"silver_logs\")\n    .groupBy(\"tenant_id\", F.col(\"event_time\").cast(\"date\").alias(\"event_date\"))\n    .agg(\n        F.count(\"*\").alias(\"event_count\"),\n        F.sum(F.when(F.col(\"event_type\") == \"click\", 1).otherwise(0)).alias(\"click_count\"),\n        F.sum(F.when(F.col(\"event_type\") == \"view\", 1).otherwise(0)).alias(\"view_count\"),\n        F.sum(F.when(F.col(\"event_type\") == \"purchase\", 1).otherwise(0)).alias(\"purchase_count\")\n    ))\n\n# MERGE into Gold (idempotent upsert)\n(gold_df.writeStream\n    .format(\"delta\")\n    .outputMode(\"append\")\n    .option(\"checkpointLocation\", \"/mnt/gold/checkpoint\")\n    .trigger(availableNow=True)\n    .toTable(\"gold_daily_tenant_summary\"))\n\n# For MERGE in batch mode:\ngold_df.createOrReplaceTempView(\"updates\")\nspark.sql(\"\"\"\n    MERGE INTO gold_daily_tenant_summary target\n    USING updates source\n    ON target.tenant_id = source.tenant_id AND target.event_date = source.event_date\n    WHEN MATCHED THEN UPDATE SET\n        event_count = target.event_count + source.event_count,\n        click_count = target.click_count + source.click_count,\n        view_count = target.view_count + source.view_count,\n        purchase_count = target.purchase_count + source.purchase_count\n    WHEN NOT MATCHED THEN INSERT *\n\"\"\")\n```\n\n## Test Plan with Synthetic Late Files\n\n1. **Setup**: Create a test Delta table with known `event_time` dates\n2. **On-time test**: Insert records with `event_time` = today → verify Gold count increments\n3. **Late data test**: Insert records with `event_time` = today - 3 days → verify watermark allows arrival within 1 day\n4. **Late data rejection**: Insert records with `event_time` = today - 5 days → verify dropped by watermark\n5. **Quality gate test**: Insert record with `event_type` = 'invalid' → verify `quality_score` = 0 and routed to quarantine\n6. **Old event test**: Insert record with `event_time` = today - 100 days → verify `quality_score` = 0\n7. **Idempotency test**: Re-run same batch → verify Gold counts don't double\n8. **Provenance test**: Query `_metadata` column → verify source file path captured accurately",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-11",
        "questionId": "q-22472",
        "question": "Ingest 40 GB/day of Parquet logs from S3 across multiple tenants. Design a beginner-friendly Silver layer that exposes a versioned Delta Sharing dataset while preserving per-tenant isolation and simple schema evolution. Provide concrete steps for: (a) per-batch provenance in an AuditFiles Delta table, (b) a deterministic MERGE-based upsert into a BI-ready Gold table, (c) Unity Catalog RBAC with column masking, (d) a lightweight data-quality gate, and (e) a test plan with synthetic late files and partially written data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Silver layer with an SCD Type 2 customer dimension table (customer_sk, customer_id, validity_start, validity_end, is_current) and a corresponding CustomerHistory audit table. Use MERGE to upsert from Bronze int…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Three-layer Delta Live Tables pipeline: Bronze layer ingests raw JSON/Parquet from S3 with per-file provenance tracking; Silver layer applies tenant-scoped schema registry with compatibility checks and automatic evoluti…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Auto Loader to ingest into Silver, retain per-file provenance in AuditFiles (source_path, file_timestamp, ingest_ts) and expose via a provenance view; implement a JSON drift policy with an ev",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build Silver by Auto Loader into Bronze, then Silver with per-batch provenance (batch_id, source_path) and a simple schema-evolution rule; upsert into Gold using MERGE on event_id; publish via Delta S",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of Databricks lineage, incremental updates, and multi-tenant governance using Delta Sharing. Evaluates a repeatable test plan and secure data exposure.\n\n## Key Concepts\n- Auto Loader Bronze/Silver/Gold workflow\n- per-batch provenance and simple schema evolution\n- MERGE-based upserts for deterministic updates\n- Delta Sharing versioning and policy-based exports\n- Unity Catalog RBAC with masking\n- lightweight data-quality gates and late-data handling\n- synthetic data tests for edge cases\n\n## Code Example\n```javascript\n// MERGE example (pseudo)\nMERGE INTO gold AS g\nUSING silver AS s\nON g.event_id = s.event_id\nWHEN MATCHED THEN UPDATE SET g.col = s.col\nWHEN NOT MATCHED THEN INSERT (col1, col2) VALUES (s.col1, s.col2)\n```\n\n## Follow-up Questions\n- How would you evolve Silver schema with new fields without breaking downstream jobs?\n- How would you validate late data scenarios and ensure idempotent restores?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-12",
        "questionId": "q-24351",
        "question": "In a multi-tenant Databricks lakehouse ingesting ~200 GB/day of Parquet/JSON logs into Bronze, design a per-tenant retention and archival policy that ages data, archives to a cost-efficient Delta table, and preserves point-in-time access via Delta Time Travel. Include (1) per-tenant retention metadata, (2) archival/cleanup workflow with VACUUM, (3) impact on Silver/Gold dashboards and query correctness, (4) testing with synthetic holds and late data, (5) Unity Catalog access controls. How would you implement and test this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Silver layer that ingests 20 GB/day multi-tenant JSON/Parquet from S3 via Auto Loader, preserves per-file provenance in AuditFiles (path, ingest_ts, file_size) and uses MERGE on event_id for",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use streaming Auto Loader to ingest JSON and Parquet from S3 into a Delta Silver table, preserving provenance via source_path, file_timestamp, and ingest_ts extracted from input_file_name() and current_timestamp(). Enab…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-tenant retention registry (tenant_id, retention_days, hold_flag) and partition Bronze/Silver by tenant_id/date. Archive data older than retention to a separate Delta archive table; prune wit",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement Auto Loader to ingest 12 GB/day of JSON from S3 into Bronze, then Silver adds per-file quality metrics into a Delta table with columns file_path, ingest_ts, row_count, nulls_per_column, unkn",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEvaluates lifecycle design for multi-tenant data, balancing cost, accessibility, and governance; tests whether the candidate can implement per-tenant policies, archival workflows, and PIT access without breaking dashboards.\n\n## Key Concepts\n\n- Per-tenant retention metadata\n- Archival vs. purging strategy\n- Delta Time Travel accessibility\n- VACUUM and data gravity considerations\n- Unity Catalog RBAC and masking implications\n\n## Code Example\n\n```sql\n-- Archive older per-tenant data\nINSERT INTO archive.bronze_archive\nSELECT * FROM bronze\nWHERE date < DATE_SUB(CURRENT_DATE(), INTERVAL 365 DAY);\n\n-- Purge from main Bronze table\nVACUUM bronze RETAIN 365 DAYS;\n```\n\n## Follow-up Questions\n\n- How would you handle legal holds that override retention windows?\n- How would you test performance and ensure dashboards reflect current data correctly?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-13",
        "questionId": "q-3914",
        "question": "In a beginner Databricks pipeline, ingest 5 GB/day JSON clickstream from S3 into Bronze via Auto Loader; flatten nested payloads into Silver, deduplicate by event_id with MERGE CDC, and apply a 2-minute watermark. Build Gold as daily active users by country. Partition by date; enforce Unity Catalog RBAC. Provide a lightweight test plan and RBAC considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest 5 GB/day JSON clickstream from S3 to Bronze via Auto Loader; flatten nested payloads into Silver, deduplicate by event_id with MERGE CDC, and apply a 2-minute watermark. Build Gold as daily act",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a streaming Silver with a Provenance table storing row_hash, source_path, file_timestamp, ingest_ts per record. Enable JSON drift handling via evolutionPolicy and explicit schema checks. Implement",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Auto Loader to land JSON into Bronze Delta, flatten into Silver with a date partition, enable mergeSchema for drift, and apply a lightweight quality guard for non-null fields and a unique key. Imp",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a comprehensive Delta Live Tables pipeline with multi-source ingestion, composite-key deduplication, late data handling, PII masking, and Unity Catalog governance. The pipeline ingests from S3 Auto Loader (JSON w…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to handle nested JSON, streaming ingestion with Auto Loader, deduplication via MERGE CDC, late data handling with a watermark, and governance via Unity Catalog RBAC. Also evaluates end-to-end testing mindset.\n\n## Key Concepts\n- Auto Loader streaming ingestion\n- Flattening nested JSON payloads\n- MERGE CDC for event_id dedup\n- 2-minute watermark for late data\n- Bronze/Silver/Gold data lake pattern\n- Unity Catalog RBAC and lineage\n\n## Code Example\n```python\n# PySpark flattening example\nraw = spark.readStream.format(\"cloudFiles\").option(\"cloudFiles.format\",\"json\").load(\"s3://bucket/clickstream/\")\nsilver = raw.selectExpr(\"event_id\", \"cast(ts as timestamp) as ts\", \"payload.user_id as user_id\", \"payload.country as country\", \"payload.actions as actions\")\n```\n\n## Follow-up Questions\n- How would you monitor late data and alert on stalls?\n- What are the trade-offs between MERGE CDC vs UPDATE-only approaches?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-14",
        "questionId": "q-27885",
        "question": "Design a Databricks pipeline ingesting 90 GB/day of multi-tenant Parquet/JSON logs from S3. Propose a Silver layer that uses a central per-tenant schema registry with versioned, backward-compatible contracts, enforces schema drift policies during Auto Loader ingestion, records per-file provenance, and writes a tenant-scoped Gold table for dashboards. Describe how data contracts, Unity Catalog RBAC, and a parallel, test-driven migration plan for schema upgrades across tenants would work, including rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an upsert into the Silver layer using MERGE on the composite key (tenant_id, event_id) to guarantee exact deduplication across pipeline replays and late-arriving data. Preserve per-file provenance in an audit…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Bronze layer using Auto Loader to ingest the 40 GB/day of multi-tenant Parquet logs from S3. Create a Delta lineage table with columns (source_path, ingest_ts, file_size, status) to capture per-file provenan…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a central, versioned schema registry keyed by tenant_id, enforcing backward-compatible contracts. Ingest with Auto Loader using schemaEvolutionMode='addNewColumns' and per-file provenance via the `handleExisti…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a Silver layer that computes per-minute anomaly scores with a 60-minute sliding window on event_time, writes to Delta table AnomalyScores, and exposes a read-only masking view; include a lightw",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates your ability to design governance and operational practices for multi-tenant data lakes on Databricks, with emphasis on schema contracts, data lineage, and safe migrations.\n\n## Key Concepts\n\n- **Central Schema Registry**: Versioned, per-tenant schemas stored in a dedicated metastore or external registry, enabling strict contract enforcement and auditability.\n- **Backward-Compatible Contracts**: New fields default to nullable or have safe defaults; removed fields are deprecated, never dropped, ensuring existing queries remain valid.\n- **Auto Loader with Schema Evolution**: `schemaEvolutionMode='addNewColumns'` auto-appends new columns; drift policies reject unexpected schema changes to protect downstream consumers.\n- **Per-File Provenance**: Every ingested file is tagged with tenant_id, ingestion timestamp, source path, and schema version, enabling full lineage and audit trails.\n- **Tenant-Scoped Gold Tables**: Partitioned or bucketed by tenant_id, these tables serve deterministic MERGE operations and feed dashboards with isolated, performant queries.\n- **Unity Catalog RBAC**: Column-level security and row filters enforce data isolation across tenants, preventing cross-tenant data leakage.\n- **Parallel, Test-Driven Migration**: Schema upgrades are validated in parallel branches per tenant using synthetic data, with gradual rollouts and automated rollback on failure.\n\n## Code Example\n\n```scala\nimport io.delta.tables.DeltaTable\nimport org.apache.spark.sql.functions._\nimport org.apache.spark.sql.types._\n\n// Define the versioned schema contract for a tenant\nval tenantSchema = StructType(Seq(\n  StructField(\"event_id\", StringType, nullable = false),\n  StructField(\"tenant_id\", StringType, nullable = false),\n  StructField(\"timestamp\", TimestampType, nullable = false),\n  StructField(\"payload\", StringType, nullable = true) // nullable for backward compat\n))\n\n// Auto Loader ingestion with provenance and drift policy\nspark.readStream\n  .format(\"cloudFiles\")\n  .option(\"cloudFiles.format\", \"json\")\n  .option(\"schemaEvolutionMode\", \"addNewColumns\")\n  .option(\"cloudFiles.schemaLocation\", s\"/mnt/provenance/$tenantId/_schemas\")\n  .schema(tenantSchema)\n  .load(s\"s3://logs-bucket/tenant=$tenantId/\")\n  .withColumn(\"ingested_at\", current_timestamp())\n  .withColumn(\"source_file\", input_file_name())\n  .writeStream\n  .format(\"delta\")\n  .option(\"mergeSchema\", \"true\")\n  .trigger(availableNow = true)\n  .toTable(s\"silver.raw_events\")\n\n// Deterministic MERGE into tenant-scoped Gold table\nDeltaTable.forName(spark, s\"gold.tenant_events\")\n  .as(\"target\")\n  .merge(\n    silverDF.as(\"source\"),\n    expr(\"target.tenant_id = source.tenant_id AND target.event_id = source.event_id\")\n  )\n  .whenMatched.updateAll()\n  .whenNotMatched.insertAll()\n  .execute()\n```",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-15",
        "questionId": "q-12286",
        "question": "In a Databricks Delta Live Tables pipeline ingesting 120–150 GB/day of Parquet for 3 tenants from S3, design a governance-and-quality framework that: (1) enforces per-tenant isolation with Unity Catalog RBAC and masking, (2) handles schema drift in Silver and records provenance, (3) performs CDC-like upserts into a tenant-aware Gold, (4) blocks downstream on data-quality gate failures and emits alerts, (5) exports data via Delta Sharing with time-bound access, (6) handles late data with watermark and reprocess tests. List concrete steps and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Unified Silver design ingests Kafka real-time events and nightly S3 batch logs into a canonical Events table. Implement per-row provenance via a lightweight UDF (source, source_timestamp, ingest_ts);",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a 2-region DLt plan: Silver preserves per-file provenance with an AuditFiles row and a file_hash dedupe; drift detected against a central schema registry with fallback evolution rules; per-t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Delta Live Tables pipeline with Bronze ingest from S3, Silver computed with dedup by event_id and a 2-minute watermark for late data, and a masking UDF for phone numbers. Expose Silver via a Uni",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Delta Live Tables pipeline that establishes per-tenant namespaces within Unity Catalog, applies RBAC controls and column-level masking for data isolation, utilizes a Silver layer that accommodates schema evo…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses expertise in designing enterprise-scale, multi-tenant data pipelines on Databricks that prioritize governance, quality assurance, and operational resilience. It evaluates knowledge of Unity Catalog security frameworks, Delta Live Tables architecture, change data capture methodologies, data quality validation mechanisms, secure data sharing protocols, and strategies for managing real-world challenges such as schema evolution and late-arriving data.\n\n## Key Concepts\n\n- Unity Catalog RBAC and column-level masking for tenant isolation\n- Delta Live Tables with per-tenant namespaces and environment-specific configurations\n- Schema evolution and provenance tracking in the Silver layer\n- CDC-style MERGE operations for incremental data processing\n- Data quality gates with downstream blocking and alerting mechanisms\n- Delta Sharing with time-bound access controls\n- Watermarking strategies and reprocess validation for late data handling",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-16",
        "questionId": "q-22413",
        "question": "In a Databricks data platform ingesting 70 GB/day of multi-tenant JSON and Parquet from S3, design a cost-aware retention and archival strategy that preserves nearline data for active tenants and archives cold data with deterministic restore. Outline Silver partitioning by tenant_id/date, a Gold layer for dashboards, automatic tiering to cold storage, Delta time travel, and per-tenant lifecycle policies. Include a test plan and rollback considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Delta Lake with a 3-layer Lakehouse; Bronze ingests Parquet/JSON from S3 and REST; Silver normalizes to a canonical schema and performs MERGE CDC on (canonical_id, event_time_bucket); late da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-tier lakehouse: Silver partitioned by tenant_id/date with per-tenant masking and Z-order for fast tenant-scoped queries; Gold materialized tables for dashboards. Archival: automatically move parti",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Adopt Silver with Auto Loader ingesting JSON and Parquet into per-file provenance AuditFiles; use Delta Lake schema evolution with a drift policy for nested JSON; CDC into Silver via Delta CDF and MER",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a comprehensive Databricks Feature Store pipeline that ingests Kafka real-time events and S3 batch catalog changes into Bronze tables via Delta Live Tables, computes versioned Silver layer features, publishes off…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates cost-aware data lifecycle, tenant isolation, and reliable restore in a real multi-tenant Databricks setup.\n\n## Key Concepts\n- Per-tenant lifecycle policies and TTL thresholds\n- Silver/Gold lakehouse design with partitioning and Z-order\n- Automatic archival to cold storage with Delta time travel\n- Unity Catalog RBAC and per-tenant masking\n- Rollback and restore drills for archival moves\n\n## Code Example\n```sql\n-- Pseudo archival policy (illustrative)\nALTER TABLE silver_tenant PARTITION (tenant_id=..., date < 'YYYY-MM-DD')\nRENAME TO silver_tenant_archive;\n```\n\n## Follow-up Questions\n- How would you monitor archival costs and restore latency?\n- How do you handle cross-tenant restores and data governance during outages?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-17",
        "questionId": "q-12390",
        "question": "Ingesting 40 GB/day of JSON clickstream logs from S3 into Bronze, design a beginner Silver layer that flattens a nested, evolving payload into a stable schema. How would you: (1) auto-evolve schema for new fields, (2) extract top-level columns and keep the rest as a map, (3) upsert via MERGE from a staging view, (4) build a BI-ready Gold with a users dim, (5) enforce Unity Catalog RBAC to restrict access, (6) implement a data-quality gate with tests including a newly added field?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Delta Live Tables with two inputs: Bronze (S3 Auto Loader) and Kafka. Silver dedupes on (tenant_id, event_id) via MERGE CDC; apply a 5-minute watermark on event_time for late data; enforce schema",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Auto Loader with per-file provenance and an explicit Spark StructType to ingest Parquet logs from S3. Enforce the schema via explicit CAST operations and validation checks to prevent schema drift. Implement a determ…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a Silver design that preserves per-event provenance via an AuditEvents table and a UDF-based provenance view, evolves nested JSON with a 2-stage merge policy, uses MERGE for CDC into Silver, a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would enable Auto Loader with schema evolution, flatten fields like user.id, session.id, event_time, page.url, device.type, geo.country into Silver, store remaining as map<string,string>, upsert via",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis checks practical handling of evolving JSON in Delta Lake, simple flattening, and governance.\n\n## Key Concepts\n- JSON schema evolution in Databricks Delta Lake\n- Flattening nested fields and preserving extras as a map\n- MERGE-based CDC in a beginner-friendly flow\n- Unity Catalog RBAC for data access\n- Lightweight data-quality gating and tests\n\n## Code Example\n```python\nfrom pyspark.sql.functions import col\n\ndf = spark.read.json('s3://bucket/logs/')\ndf_flat = df.select(\n  col('user.id').alias('user_id'),\n  col('session.id').alias('session_id'),\n  col('event_time').alias('event_time'),\n  col('page.url').alias('page'),\n  col('device.type').alias('device_type'),\n  col('geo.country').alias('geo_country'),\n  col('payload').cast('map<string,string>').alias('attributes')\n)\n```\n\n## Follow-up Questions\n- How would you validate schema evolution with a newly added field?\n- How would you test the MERGE CDC logic end-to-end?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-18",
        "questionId": "q-4086",
        "question": "In a Databricks project ingesting 5 GB/day of JSON event data from two APIs into Delta tables on S3, design a beginner-friendly pipeline that unifies the schemas, builds Bronze, Silver, and Gold layers, and handles 2-minute late data with a watermark. Implement simple per-field normalization, a PII masking approach at query time, and Unity Catalog RBAC restricting access to Silver and Gold. Outline concrete steps including partitioning, schema evolution, and a lightweight test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Delta Lake with a 3-layer Lakehouse; Bronze ingests Parquet/JSON from S3 and REST; Silver normalizes to a canonical schema and performs MERGE CDC on (canonical_id, event_time_bucket); late da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Bronze: Ingest both API feeds using Auto Loader into Delta Bronze tables with unified schema handling. Silver: Apply MERGE-CDC to deduplicate by event_id, normalize data types, and implement a 2-minute watermark for lat…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a beginner Silver: use Auto Loader to move Parquet to Bronze, flatten payloads, derive canonical_event_id (hash of id+ts), join country map to region, 1-hour watermark with a simple reprocess p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Silver layer partitioned by region and date while maintaining per-minute region counts in a RegionMinuteAgg table; implement event_id-based upserts from Bronze using MERGE operations; create a region-scoped mas…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical Databricks implementation skills including data ingestion, schema unification, late data handling, basic data privacy measures, and access control—all at a beginner-friendly level. It emphasizes actionable implementation steps over theoretical concepts.\n\n## Key Concepts\n\n- Auto Loader for Bronze layer ingestion\n- Silver layer deduplication using MERGE-CDC\n- Watermarking for late data management\n- PII masking through read views\n- Unity Catalog RBAC for access control\n\n## Code Example\n\n```javascript\n// PII masking view example\nCREATE VIEW masked_events AS\nSELECT \n    event_id,\n    event_timestamp,\n    event_type,\n    sha2(email, 256) as masked_email,\n    other_fields\nFROM silver_events;\n```",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-19",
        "questionId": "q-25690",
        "question": "In a Databricks Delta Live Tables pipeline ingesting ~60 GB/day of JSON and Parquet data from S3 across multiple tenants, design a Silver layer that performs near-real-time enrichment by joining a streaming enrichment feed (from a REST service) updated every minute with the main Bronze data, while preserving per-record provenance and supporting schema evolution. Include how you handle service outages, late events, and idempotent upserts into a Gold table, how you implement Unity Catalog RBAC masking for PII, and a practical test plan with synthetic enrichment outages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To implement: in Silver layer, materialize an enrichment stream from a REST service (minutely updated) as a separate streaming source; join with Bronze by a stable key; retain provenance fields (sourc",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Auto Loader to land per‑tenant Parquet files into Bronze, create Silver with schema enforcement, and MERGE into Gold on event_id/date. Add AuditFiles with path, ingest_ts, file_size, and checksum;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lightweight JSON schema registry stored as a Delta table to maintain schema versions and field definitions. Use Auto Loader with cloudFiles.schemaLocation to automatically detect and track schema drift. In t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a streaming DLT pipeline: Bronze landing per-tenant JSON from API gateway; Silver with event_id dedupe and CDC-style MERGE; Gold aggregates per-tenant. Enforce Unity Catalog RBAC and tenant_id",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design near-real-time enrichment, multi-source joins, and robust handling of outages in an established Databricks data platform.\n\n## Key Concepts\n- Delta Live Tables streaming joins between Bronze and a minute-delayed enrichment feed\n- Idempotent MERGE upserts into Gold for deterministic updates\n- Per-record provenance (source_path, file_timestamp, ingest_ts) preserved in Silver/ Audit\n- Schema evolution policy for nested JSON and Parquet\n- Unity Catalog RBAC masking for PII fields in downstream\n- Late data handling with watermarking and optional default enrichment\n- End-to-end test plan including enrichment outages and delayed events\n\n## Code Example\n```sql\n-- Pseudo MERGE into Gold after enrichment\nMERGE INTO gold AS g\nUSING silver_enriched AS s\nON g.key = s.key\nWHEN MATCHED THEN UPDATE SET g.value = s.value, g.enrichment_ts = s.enrichment_ts\nWHEN NOT MATCHED THEN INSERT (key, value, enrichment_ts) VALUES (s.key, s.value, s.enrichment_ts);\n```\n\n## Follow-up Questions\n- How would you monitor enrichment latency and automatically raise alerts on degradation?\n- How would you simulate enrichment service outages in CI/CD tests while preserving idempotency?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-20",
        "questionId": "q-14480",
        "question": "Design a Databricks Delta Live Tables Silver layer for a 50-70 GB/day multi-tenant pipeline ingesting mixed JSON/Parquet from S3. The design must (1) route tenant data to per-tenant streams while preserving per-file provenance, (2) use a central schema registry to handle evolution, (3) implement CDC via MERGE into Silver filtered by tenant_id, (4) expose a Gold table suitable for dashboards that is tenant-scoped, (5) enforce Unity Catalog RBAC with masking, (6) include a data-quality gate and late-data watermark, (7) capture end-to-end lineage with OpenLineage, and (8) specify a rollback/replay strategy when a schema error occurs. Provide concrete storage layouts and testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a Silver layer that materializes a versioned feature_store Delta table per tenant. Ingest JSON via Auto Loader with schema inference, and maintain an AuditFiles table for per-file provenance. Use Delta schema e…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a two-layer model: Silver stores per-file provenance (AuditFiles), evolves JSON schema with a versioned EvolutionPolicy, and CDC-like MERGE into TenantEvents. Gold computes tenant-scoped aggreg",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement Silver with per-tenant pipelines in Delta Live Tables, preserving per-file provenance in AuditFiles; use a central Schema Registry for evolution; apply CDC via MERGE into Silver filtered by",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Auto Loader with per-file provenance, enforce a fixed schema (reject new fields), write a lightweight DataQuality table logging daily row_count and null counts for key fields, upsert into Gold by",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of real-world multi-tenant governance, lineage, and schema evolution in Databricks.\n\n## Key Concepts\n- Delta Live Tables, Unity Catalog, masking policies, schema registry, OpenLineage, CDC with MERGE, data quality gates, late data handling.\n\n## Code Example\n```javascript\nMERGE INTO Silver.tenant_silver AS s\nUSING Bronze.tenant_bronze AS b\nON s.tenant_id = b.tenant_id AND s.event_id = b.event_id\nWHEN MATCHED THEN UPDATE SET ...\nWHEN NOT MATCHED THEN INSERT ...\n```\n\n## Follow-up Questions\n- How would you test per-tenant lineage end-to-end? What metrics signal a drift or masking failure?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-21",
        "questionId": "q-12961",
        "question": "In a Databricks pipeline ingesting 15 GB/day of Parquet logs from S3, design a beginner Silver layer that emphasizes data observability: (1) build a Delta DataQuality table recording date, table_name, row_count, nulls_per_col, and schema_digest; (2) expose a masked read view for PII; (3) implement a lightweight drift check that flags if row_count or schema_digest changes by more than 5% vs previous day; (4) enforce Unity Catalog RBAC for Silver and DataQuality; (5) outline a regression test plan using synthetic late data and partial files?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Silver layer that ingests 12 GB/day of JSON web-logs from S3 with Auto Loader. Enforce per-file provenance, flatten nested fields (user.id, session.id, event.time), and require non-null key f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Auto Loader for Bronze ingestion and a Silver layer that computes a Delta DataQuality table with fields date, table_name, row_count, nulls_by_col, schema_digest, and ingested_at; expose a masked v",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Explain a Silver-layer observability-first design for 60 GB/day multi-tenant ingestion. Include per-file/per-tenant latency metrics in Delta, a provenance view, drift alerts with thresholds, a backoff",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition Silver by tenant_id and date; enable Delta Change Data Feed for per-tenant CDC; use an idempotent MERGE into Silver on event_id; materialize a tenant-scoped Gold table (tenant_id, day); enfo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question targets practical data observability in a beginner Silver pipeline, emphasizing measurable quality signals and governance.\n\n## Key Concepts\n\n- DataQuality Delta table capturing row counts, null distributions, and schema_digest\n- Masked read views for PII to enforce data governance\n- Lightweight drift checks between consecutive days\n- Unity Catalog RBAC to control access to Silver and DataQuality\n- Regression tests using synthetic late data and partial files\n\n## Code Example\n\n```sql\n-- Create DataQuality table\nCREATE TABLE delta_dataquality (\n  date DATE,\n  table_name STRING,\n  row_count BIGINT,\n  nulls_by_col MAP&lt;STRING, BIGINT&gt;,\n  schema_digest STRING,\n  ingested_at TIMESTAMP\n);\n```\n\n## Follow-up Questions\n\n- How would you adapt drift checks for schema changes that are legitimate vs. breaking?\n- How would you extend RBAC to support multi-tenant isolation when adding more data sources?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-22",
        "questionId": "q-25041",
        "question": "Design a Databricks Silver layer for 200 GB/day of mixed CSV/JSON/Parquet data from 5 tenants. Implement a Canonical Schema Registry in Delta that records per-field metadata (name, type, source_schema_id); use a lightweight UDF to map each file's records to the canonical schema, preserving per-file provenance. Enable schema evolution, CDC-like MERGE into Gold, Unity Catalog masking, data-quality gates, and late-data handling with tests. Show observability plan and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Canonical Schema Registry in Delta Lake to maintain a unified schema with per-field metadata (name, type, source_schema_id). Ingest data using Auto Loader with per-file provenance tracking, and use a lightwe…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a Bronze→Silver→Gold pipeline using Auto Loader to ingest 200 GB/day of JSON events from two e-commerce APIs into Delta Lake on S3. The Bronze layer uses Auto Loader with schema inference and cloudFiles to land r…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Auto Loader to land JSON into Bronze; Silver uses Delta with mergeSchema=true and CDC upserts by (device_id, event_time) plus 1h watermark; Gold derives device metrics. enforce RBAC via Unity",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement the Silver layer using Auto Loader with schema inference and explicit file provenance metadata. Enforce a permissive-but-validated evolution policy for schema drift, derive tenant-scoped features using Spark w…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates the ability to unify multi-format data with evolving schemas, implement durable per-file provenance, and integrate with Delta Lake features such as MERGE and schema evolution, Unity Catalog masking, and robust late-data handling in production environments.\n\n## Key Concepts\n\n- Canonical Schema Registry for cross-format consistency\n- Per-file provenance tracking with lightweight UDF mapping\n- Schema evolution and backfilling strategy\n- CDC-like MERGE operations into Silver/Gold tables\n- Unity Catalog RBAC and column-level masking\n- Data-quality gates and late-arriving data handling with watermarking",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-23",
        "questionId": "q-13854",
        "question": "In a Databricks pipeline ingesting user activity data, design a Delta Lake-based per-row versioning and right-to-be-forgotten (RTBF) workflow. Describe how you store versions, apply RTBF requests, and maintain an auditable trail while preserving query performance and RBAC via Unity Catalog. Include data deletion tests and privacy checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "DLT Bronze from S3 JSON to Bronze_raw; Silver normalizes timestamps to UTC, flattens nested fields, and enforces non-null customer_id and session_id; Gold outputs per-event rows with a deterministic k",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I'll design a comprehensive Silver layer solution with these key components: **Core Architecture** - Ingest using Auto Loader with cloudFiles format for both JSON and Parquet - Partition by tenant_id and ingestion_date…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Add per-row versioning in Delta Lake: include valid_from, valid_to, and version_id columns; retain historical versions in the same table. RTBF uses tombstones: insert a tombstone and set valid_to to t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Route ingestion errors to a dedicated ErrorEvents Delta table and quarantine failing files, while Bronze, Silver, Gold streams continue. Use Auto Loader with file-level error handling, capture tenant_",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests per-row versioning, RTBF workflows, auditability, and Unity Catalog RBAC in a multi-tenant data platform. Evaluates how to balance privacy compliance with query performance at scale.\n\n## Key Concepts\n- Delta Lake time-bound versioning (valid_from, valid_to, version_id)\n- Right-to-be-forgotten (RTBF) tombstones and MERGE upserts\n- Audit trail and time travel for compliance\n- Unity Catalog RBAC and masking on sensitive fields\n- Testing with synthetic RTBF requests and privacy checks\n\n## Code Example\n```sql\n-- Query current, non-deleted rows for a given event_id\nSELECT * FROM events_versioned\nWHERE event_id = 'e123'\n  AND valid_from <= CURRENT_TIMESTAMP\n  AND (valid_to IS NULL OR valid_to > CURRENT_TIMESTAMP);\n```\n\n## Follow-up Questions\n- How would you backfill RTBF requests that arrive after historical gaps?\n- How do you control tombstone retention and storage costs without compromising auditability?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-24",
        "questionId": "q-13256",
        "question": "In a Databricks Delta Live Tables pipeline ingesting 80 GB/day of Parquet and JSON files from a multi-tenant S3 bucket, design a Silver layer that (1) uses Delta Change Data Feed to capture per-tenant CDC, (2) preserves per-file provenance (source_path, file_timestamp, ingest_ts) in an AuditFiles table, (3) handles schema drift for nested JSON fields with evolution policy, (4) exposes a Gold table suitable for dashboards with tenant_id and day granularity, (5) enforces Unity Catalog RBAC with masking and dynamic filters so tenants see only their data, (6) implements a data-quality gate and watermarking for late data, (7) provides a testing plan with synthetic late data and partial files; (8) outline fault-tolerance for Auto Loader and checkpointing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Delta Live Tables to build Silver from Bronze. Define Silver with event_id, event_time, payload, and a provenance column; populate a separate AuditFiles Delta table via a lightweight batch_id UDF",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: Use a DLT pipeline with Bronze ingest from JSON, then Silver derived from Bronze. Deduplicate on event_id, keeping the latest by ingest_time. Join to users_dim on user_id to validat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a comprehensive Delta Live Tables pipeline with Change Data Feed enabled at the Silver layer. Utilize Auto Loader to efficiently ingest Parquet and JSON files from the multi-tenant S3 bucket, maintaining detai…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Leverage Auto Loader for 200 GB/day; Silver with Delta Live Tables + CDC to incrementally refresh versioned features; AuditFiles for per-file provenance; JSON drift handled via Evolution with defaults",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical Databricks expertise in multi-tenant data architectures, specifically testing knowledge of CDC implementation with Change Data Feed, governance controls, and enterprise-grade data pipeline design. It assesses understanding of schema evolution strategies, late data handling patterns, and comprehensive provenance tracking requirements.\n\n## Key Concepts\n\n- **Delta Live Tables**: Declarative pipeline framework with built-in orchestration and dependency management\n- **Change Data Feed**: CDC mechanism for tracking row-level changes in Delta tables\n- **Auto Loader**: Scalable file ingestion solution with cloudFiles format and schema inference\n- **Unity Catalog**: Centralized governance platform for metadata, security, and lineage\n- **Schema Evolution**: Automatic handling of structural changes in source data\n- **Watermarking**: Technique for managing late-arriving data in streaming contexts\n- **Row-Level Security**: Fine-grained access control based on tenant identification\n- **Data Quality Gates**: Validation checkpoints ensuring data integrity before downstream consumption\n\n## Testing Strategy\n\nTesting should validate pipeline behavior under various failure scenarios, schema changes, and data quality conditions to ensure production readiness and reliability.",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-10T13:26:14.046Z",
    "version": 1
  }
]