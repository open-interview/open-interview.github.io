[
  {
    "id": "test-databricks-data-engineer",
    "channelId": "databricks-data-engineer",
    "channelName": "Databricks Data Engineer",
    "title": "Databricks Data Engineer Knowledge Test",
    "description": "Test your understanding of Databricks Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "databricks-data-engineer-q-0",
        "questionId": "q-1226",
        "question": "In a Databricks Delta Live Tables pipeline that ingests clickstream events from a cloud bucket into a Delta table, data quality checks are defined via expectations. A batch arrives with corrupted event_time values that would fail the run. Outline a concrete approach to quarantine bad data, feed downstream tables only from valid rows, and store invalids for audit, while still handling late data and deduplication?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Cross-region replication: maintain a centralized Gold table partitioned by region_tag, with Bronze and Silver layers replicated via near-real-time CDC using Databricks Auto Loader or Spark Structured Streaming. The mult…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a staging raw_events table, apply a light validation to tag rows as valid/invalid, store invalid rows in invalid_events, and feed downstream clean_events from valid rows only. Use a watermark o",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Architect Bronze→Silver with 200 GB/day nested JSON from REST into Delta. Silver deduplicates on canonical_id via MERGE CDC across tenants, enforces a 15-minute watermark for late data, and records pe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a Silver layer that tracks provenance per file, uses Delta schema evolution for nested JSON drift, applies a CDC-like MERGE into Silver, then writes a tenant-scoped offline feature table to th",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDemonstrates practical, production-ready handling of partial data quality failures in a Delta Live Tables pipeline, balancing correctness with availability.\n\n## Key Concepts\n- Staging vs downstream separation\n- Expectations-based quality checks\n- Quarantine and audit of invalid data\n- Late data handling with watermarking\n- Deduplication strategies across micro-batches\n\n## Code Example\n```javascript\nimport dlt\nimport pyspark.sql.functions as F\n\n@dlt.table\ndef raw_events():\n  return spark.read.format(\"cloudFiles\").option(\"cloudFiles.format\",\"json\").load(\"/mnt/events/raw\")\n\n@dlt.table\ndef valid_events():\n  df = dlt.read(\"raw_events\")\n  return df.filter(F.col(\"event_time\").isNotNull() & F.col(\"user_id\").isNotNull())\n\n@dlt.table\ndef invalid_events():\n  raw = dlt.read(\"raw_events\")\n  valid_ids = dlt.read(\"valid_events\").select(\"event_id\").distinct()\n  return raw.join(valid_ids, \"event_id\", \"left_anti\")\n\n@dlt.table\ndef clean_events():\n  return dlt.read(\"valid_events\")\n```\n\n## Follow-up Questions\n- How would you monitor and alert if invalid_events growth spikes?\n- What changes would you make to support reprocessing of invalid data after fixes?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-1",
        "questionId": "q-16121",
        "question": "Given a Databricks Delta Live Tables pipeline processing 120 GB/day of JSON/Parquet data from 200 tenants stored in S3, design a Silver and Gold architecture that (1) validates incoming records against a centralized, tenant-scoped data contract registry and writes per-record validation outcomes to a tenant audit table, (2) isolates tenants via Unity Catalog RBAC and per-tenant dynamic filters, (3) supports deterministic backfills of the Gold table using Delta Time Travel, (4) handles malformed or late data with quarantined records and retry logic, and (5) includes a practical testing plan with synthetic malformed data and backfill scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Silver layer with a tenant-scoped contract validation step that checks each row against a registry and writes per-record validation results to a per-tenant Audit table. Enforce Unity Catalog",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "DLT Silver plan: ingest Bronze Parquet with schema evolution (allow_unsafe_schema_evolution) to accept new fields; gate access via Unity Catalog RBAC and mask PII with masking policy/view; implement d",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lightweight JSON schema registry stored as a Delta table to maintain schema versions and field definitions. Use Auto Loader with cloudFiles.schemaLocation to automatically detect and track schema drift. In t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a cross-cloud Delta Live Tables plan: ingest 60 GB/day into AWS S3 Bronze; design Silver with per-tenant provenance and CDC via Delta CDF; publish Gold via Delta Sharing to Google Cloud; enfor",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAddresses data contracts, tenant isolation, and deterministic backfills in a multi-tenant Databricks setup.\n\n## Key Concepts\n- Tenant-scoped data contracts and per-record validation\n- Unity Catalog RBAC and per-tenant dynamic filters\n- Deterministic backfill using Delta Time Travel\n- Quarantine workflow for malformed data and retry policy\n- Synthetic testing plan and observability\n\n## Code Example\n```javascript\n// Pseudocode: validateRecord against registry, emit audit row on success/failure\nfunction validateRow(row, registry){ /* ... */ }\n```\n\n## Follow-up Questions\n- How would you design schema registry storage and versioning for contracts?\n- How would you test backfill consistency across tenants during DST changes?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-2",
        "questionId": "q-5346",
        "question": "In a Databricks ETL ingesting 15 TB/day of JSON clickstream from S3 and a streaming REST feed, design Bronze/Silver/Gold with event_id dedupe via MERGE CDC and a 5-minute watermark. Propose a rollback strategy to a known-good Delta version on data-quality failure, plus a lightweight test plan and Unity Catalog RBAC considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Load 3 GB/day Parquet logs from S3 into a Bronze Delta table using Auto Loader. Build a Silver layer that enriches each event by left joining with the small static dimension table customers (customer_id, country, segmen…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Configure a Delta Live Tables pipeline with three stages: Bronze (ingest Parquet from S3), Silver (per-tenant/date, schema drift tolerant), Gold (daily per-tenant metrics). Add dlt.expect constraints",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a MERGE CDC on Silver using event_id, enforce a 5-minute watermark, and track last_good_version in a small Delta table. On failure, time-travel Silver/Gold to that version and flip an alias to re-",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a Silver layer that ingests from the Bronze table using MERGE statements to deduplicate by event_id, implements a 2-minute watermark for late data handling, partitions by date for optimal performance, and creates…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical handling of mixed batch/streaming ingestion, late data, and real-world failure recovery without downtime. It also probes governance, test discipline, and rollback operational habits.\n\n## Key Concepts\n- Delta Time Travel for safe rollback\n- MERGE CDC in Silver layer\n- Watermarking for late data handling\n- Rollback orchestration with table aliases\n- Unity Catalog RBAC and lineage\n\n## Code Example\n```javascript\n-- Pseudocode: record last_good_version after a successful run\nINSERT INTO last_good_version (version, timestamp) VALUES (CURRENT_VERSION(), NOW());\n```\n\n## Follow-up Questions\n- How would you automate canary rollouts and automated rollback when a failure is detected?\n- How would you test rollback paths using synthetic faults and data skew injections?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-3",
        "questionId": "q-1264",
        "question": "In a Databricks job ingesting daily Parquet files from S3 into Delta Lake, a new field 'campaign_id' may appear in newer files while older ones do not. Describe a concrete, beginner-friendly approach to ingest without data loss, allowing downstream joins, and handling the schema change gracefully. Include how to enable Delta schema evolution (mergeSchema/autoMerge), define a compatible target schema, and implement a minimal validation to drop rows missing essential fields?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable Delta schema evolution by using mergeSchema on write and enabling auto-merge: spark.conf.set('spark.databricks.delta.schema.autoMerge.enabled','true'); df.write.format('delta').option('mergeSch",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a Silver layer with a formal data contract: event_id STRING NOT NULL, event_time TIMESTAMP NOT NULL, user_id STRING, and an optional extra_field STRING. Use Delta constraints and MERGE by ev",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Delta Live Tables with two inputs: Bronze (S3 Auto Loader) and Kafka. Silver dedupes on (tenant_id, event_id) via MERGE CDC; apply a 5-minute watermark on event_time for late data; enforce schema",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Unity Catalog RBAC to grant Silver access per tenant and implement a row-level policy that filters by tenant_id = current_tenant(). In Silver, perform MERGE CDC from Bronze on (tenant_id, event_id",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of practical schema evolution in Delta Lake during basic batch ingestion and how to keep data accessible for joins.\n\n## Key Concepts\n\n- Delta Lake schema evolution\n- mergeSchema and autoMerge\n- Nullability and backward compatibility\n- Data quality validation\n\n## Code Example\n\n```javascript\n// Ingestion example with schema evolution\ndf.write.format(\"delta\").option(\"mergeSchema\",\"true\").mode(\"append\").save(\"/delta/user_events\")\n```\n\n## Follow-up Questions\n\n- How would you validate that campaign_id is correctly populated in downstream queries?\n- How would you backfill the historical table to reflect a new campaigns dimension without downtime?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-4",
        "questionId": "q-30500",
        "question": "Design a Delta Live Tables streaming design with dual inputs: S3 Auto Loader for JSON and a Kafka CDC stream, to produce a Gold table. Outline per-record provenance, schema drift handling, CDC-like MERGE, RBAC masking, data-quality gates, late-data handling with watermarking, and a testing plan with synthetic late messages and partial files; include fault-tolerance for Auto Loader checkpointing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Silver layer that uses Auto Loader to read JSON and Parquet from a multi-tenant S3 path, infers schema with drift policy, and writes to Delta with a per-file provenance row in an AuditFiles t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Delta Live Tables streaming design with dual inputs: S3 Auto Loader for JSON and a Kafka CDC stream. Silver stores per-record provenance in AuditEvents (source_path, offset, ingest_ts) and appli",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a robust multi-tenant Databricks pipeline using Delta Live Tables with a Bronze-Silver-Gold medallion architecture. The Bronze layer ingests 100 GB/day of mixed JSON/Parquet logs from S3 and YAML configurations i…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use DLT with an AuditFiles table capturing source_path, file_timestamp, ingest_ts via a lightweight UDF; apply a schema-evolution policy for nested drift; implement CDC-like MERGE into Silver; derive",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a multi-source streaming pipeline with DLDT, including provenance, schema drift handling, CDC, and governance under RBAC.\n\n## Key Concepts\n- Delta Live Tables streaming\n- Auto Loader ingestion\n- Delta Change Data Feed (CDC)\n- MERGE upserts into Gold\n- Unity Catalog masking and RBAC\n- Data-quality gates and watermarking\n- Fault-tolerance and checkpointing\n\n## Code Example\n```sql\n-- Silver ingestion (illustrative)\nCREATE LIVE TABLE silver AS\nSELECT * FROM bronze;\n```\n\n## Follow-up Questions\n- How would you test late data reprocessing without re-ingesting from source?\n- Which metrics and alerts would you surface for quality gates and checkpoint health?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-5",
        "questionId": "q-20731",
        "question": "In a Databricks pipeline ingesting 40 GB/day of Parquet user-activity logs from S3 across 3 tenants, design a beginner-friendly Silver layer that (1) uses Auto Loader for per-file ingestion, (2) flattens nested payloads, (3) computes per-tenant daily active users into a Gold table partitioned by tenant_id/date, (4) masks PII via a masking view, (5) enforces Unity Catalog RBAC for read access, (6) includes a basic data-quality gate (non-null user_id, event_time within last 90 days), and (7) a test plan with synthetic late files. How would you approach this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Auto Loader for Bronze, then Silver with per-file provenance via AuditFiles using Delta metadata; implement schema drift handling with evolution; CDC via Delta CDF and MERGE into a tenant-sco",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Auto Loader to load Parquet into Bronze, then flatten nested payloads into Silver per-tenant; maintain a Gold table of daily_active_users partitioned by tenant_id/date. Implement a simple MERGE up",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Route ingestion errors to a dedicated ErrorEvents Delta table and quarantine failing files, while Bronze, Silver, Gold streams continue. Use Auto Loader with file-level error handling, capture tenant_",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a canonical Silver layer featuring a unified event schema with comprehensive provenance tracking (source_system, ingest_timestamp, trace_id, record_hash). Leverage Delta Lake's schema evolution capabilities to au…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical, beginner-friendly data modeling across Bronze-Silver-Gold with tenant isolation, simple CDC, and governance.\n\n## Key Concepts\n- Auto Loader ingestion\n- Flattening nested payloads\n- MERGE-based upsert for Silver/Gold\n- Per-tenant partitioning\n- Masking view for PII\n- Unity Catalog RBAC\n- Lightweight data-quality gates\n- Late-arrival test strategy\n\n## Code Example\n```javascript\n// Pseudo Spark-like snippet\nval bronzeDF = spark.read.format(\"parquet\").load(\"s3://bucket/bronze/\")\nval silverDF = bronzeDF.selectExpr(\"flatten(nested) as (tenant_id, user_id, event_time, email, ...)\" )\nsilverDF.write.format(\"delta\").mode(\"append\").saveAsTable(\"silver_table\")\n\nval goldDF = silverDF.groupBy(\"tenant_id\",\"date\").agg(countDistinct(\"user_id\").as(\"dau\"))\ngoldDF.write.format(\"delta\").partitionBy(\"tenant_id\",\"date\").saveAsTable(\"gold_table\")\n```\n\n## Follow-up Questions\n- How would you validate late-arriving data in this pipeline?\n- How would schema drift be detected and handled as fields evolve?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-6",
        "questionId": "q-26055",
        "question": "Scenario: Ingest 40 GB/day of JSON clickstream logs from S3 into Databricks. Build a Delta Live Tables pipeline with Bronze, Silver, and Gold tables. Bronze ingests raw JSON; Silver normalizes timestamps to UTC, flattens nested fields, and enforces non-null customer_id and session_id; Gold outputs per-event rows with a deterministic key (customer_id, session_id, event_time) and simple metrics. Include schema drift handling and a minimal test plan with synthetic late data. Outline concrete steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "DLT Bronze from S3 JSON to Bronze_raw; Silver normalizes timestamps to UTC, flattens nested fields, and enforces non-null customer_id and session_id; Gold outputs per-event rows with a deterministic k",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Auto Loader with per-file provenance to an Audit table; enable schema drift handling with non-destructive evolution for nested JSON; MERGE into Gold on (tenant_id, event_id) for deterministic upse",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Silver layer with per-tenant provenance tracking and CDC-like MERGE operations for upserts, implementing schema drift handling via explicit evolution rules. The Gold layer aggregates metrics into a BI-ready tab…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Auto Loader to Bronze with source_path and ingest_ts; Silver MERGE on event_id; SchemaChangeLog versioning for drift; data_dict view with name, type, sample; RBAC and masking for PII; test plan with s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nIntroduces Delta Live Tables and schema evolution with a practical end-to-end flow, testing ability to design multi-layer pipelines and validate data quality.\n\n## Key Concepts\n\n- Delta Live Tables Bronze/Silver/Gold pattern for structured streaming/batch logs\n- Schema drift and evolution controls in DLT\n- Deterministic keys for per-event records and lightweight metrics\n- Late-arriving data handling and end-to-end test strategy (synthetic data)\n- PyTest/Expectations-style checks for non-null keys, UTC normalization, and row counts\n\n## Code Example\n\n```javascript\nimport dlt\n\n@dlt.table\ndef Bronze_raw():\n  return spark.read.json(\"s3://bucket/logs/*/\")\n\n@dlt.table\ndef Silver_normalized(Bronze_raw):\n  df = Bronze_raw\n  df = df.withColumn(\"event_time_utc\", F.to_utc_timestamp(F.col(\"event_time\"), \"UTC\"))\n  # flatten nested fields as needed\n  return df.select(\"customer_id\", \"session_id\", \"event_type\", \"event_time_utc\", \"properties.*\")\n\n@dlt.table\ndef Gold_events(Silver_normalized):\n  df = Silver_normalized\n  df = df.withColumn(\"key\", F.concat_ws(\"|\", F.col(\"customer_id\"), F.col(\"session_id\"), F.col(\"event_time_utc\")))\n  return df\n```\n\n## Follow-up Questions\n\n- How would you adjust the test strategy for high-cardinality event_type values?\n- What monitoring would you add to detect drift or ingestion lags in production?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-7",
        "questionId": "q-23586",
        "question": "In a Databricks pipeline ingesting 40 GB/day of JSON logs from S3 for many tenants, design a beginner Silver layer that uses Auto Loader with file metadata, partitions by tenant_id/date, and performs a deterministic MERGE into a Gold table keyed by event_id; add a lightweight schema-drift audit (SchemaAudit) and simple Unity Catalog RBAC; outline concrete steps and a test plan with synthetic late events?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Auto Loader to ingest per-file, extract tenant_id, load tenant schema from contracts/tenant_id.json, cast fields to expected types, and compare against the contract. If mismatch, write file metada",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Auto Loader to ingest JSON from S3, create Bronze->Silver with tenant_id/date partitions, perform a MERGE into Gold on event_id, and write a SchemaAudit entry when new fields appear. Enforce",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Explain a Silver-layer observability-first design for 60 GB/day multi-tenant ingestion. Include per-file/per-tenant latency metrics in Delta, a provenance view, drift alerts with thresholds, a backoff",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a broadcast join and caching. Load the 10M-row fact table as df_big and the 10k-row segments as df_small, then join with F.broadcast(df_small) on customer_id. Cache df_big for the daily run. Write",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests practical data ingestion, partitioning, upserts, drift auditing, and access control in a single, beginner-friendly scenario.\n\n## Key Concepts\n\n- Auto Loader for JSON ingestion\n- Delta MERGE upserts\n- Schema drift auditing (SchemaAudit)\n- Unity Catalog RBAC\n- Late-data handling and test plans\n\n## Code Example\n\n```sql\nMERGE INTO gold AS g\nUSING silver AS s\nON g.event_id = s.event_id\nWHEN MATCHED THEN UPDATE SET g.* = s.*\nWHEN NOT MATCHED THEN INSERT *;\n```\n\n## Follow-up Questions\n\n- How would you implement drift thresholds and alerting?\n- How would you test with multiple tenants having different schemas?\n- What are the potential performance bottlenecks and how to mitigate them?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-8",
        "questionId": "q-14254",
        "question": "In a Databricks pipeline ingesting 20 GB/day of CSV logs from S3 into Bronze, design a beginner Silver layer that (1) normalizes timestamps to UTC and fills time zone inconsistencies, (2) adds an ingest_ts and a 1-hour window bucket, (3) deduplicates by record_id for the current day using MERGE, (4) applies conditional masking on phone_number via Unity Catalog policy, (5) enables basic schema evolution for changing headers, (6) includes a test plan with synthetic late files and a duplicate record. What concrete steps would you take to implement this end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I'll design a comprehensive Silver layer solution with these key components: **Core Architecture** - Ingest using Auto Loader with cloudFiles format for both JSON and Parquet - Partition by tenant_id and ingestion_date…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Delta Live Tables (DLT) pipeline where Auto Loader streams Avro logs from a multi-tenant lake into a Silver table with an Audit provenance. Enable schema evolution, CDC-like MERGE by event_id",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: Bronze→Silver with Auto Loader; a small UDF flattens nested JSON into a stable schema and writes per-file provenance (source_path, file_timestamp, ingest_ts) to an Audit table. Handle drift",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Set up a Bronze ingestion with Auto Loader for 20 GB/day CSVs. Silver layer: (1) normalize timestamps to UTC; (2) add ingest_ts and a 1h window_bucket; (3) deduplicate by record_id for the current day",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests a practical, beginner‑friendly Databricks Silver pattern focusing on normalization, dedup, masking, and schema evolution with a small test harness.\n\n## Key Concepts\n- Auto Loader Bronze ingestion\n- UTC timestamp normalization\n- Windowed dedup with MERGE\n- Unity Catalog masking policies\n- Simple schema evolution checks\n\n## Code Example\n```python\n# PySpark sketch: normalize UTC and set ingest_ts\nfrom pyspark.sql.functions import to_utc_timestamp, current_timestamp\n\n# illustrative placeholder\n```\n\n## Follow-up Questions\n- How would you test schema evolution when a CSV header adds a new column?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-9",
        "questionId": "q-26691",
        "question": "Ingest 40 GB/day of Parquet logs from S3 for multiple tenants. Build a beginner Silver layer using Auto Loader with per-file provenance and explicit, fixed schema enforcement. Implement a deterministic upsert into a Gold table partitioned by tenant_id/date. Expose a PII-masked view (not Unity Catalog masking) and add lightweight data-quality gates (non-null keys, date within last 7 days, no duplicates per tenant/date). Include a minimal test plan using synthetic late files and partially written files to verify recovery?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Auto Loader with per-file provenance and an explicit Spark StructType to ingest Parquet logs from S3. Enforce the schema via explicit CAST operations and validation checks to prevent schema drift. Implement a determ…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a deletion workflow with a DeletionLog stream; when a deletion request arrives, publish user_id and deletion_ts to a Delta table and a separate DeletionLog; on Bronze and Silver, use MERGE t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a scalable Silver layer that captures comprehensive per-record provenance (source_path, region, ingest_ts), handles schema evolution for mixed JSON and Parquet formats, implements cross-region CDC using MERGE wit…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Set up a Bronze ingestion with Auto Loader for 20 GB/day CSVs. Silver layer: (1) normalize timestamps to UTC; (2) add ingest_ts and a 1h window_bucket; (3) deduplicate by record_id for the current day",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your ability to design a practical, beginner-friendly Silver-to-Gold pipeline with explicit schema enforcement, data provenance, and basic governance controls.\n\n## Key Concepts\n- **Auto Loader with per-file provenance**: Track the source of each ingested file for auditability\n- **Explicit schema enforcement**: Use a fixed Spark StructType with CAST operations to prevent schema drift\n- **Deterministic MERGE upsert**: Ensure idempotent writes into a Gold table partitioned by tenant_id and date\n- **PII masking via view layer**: Apply masking transformations at the view level rather than catalog-level masking\n- **Lightweight data-quality gates**: Validate non-null keys, date ranges, and deduplication\n- **Late-arriving data handling**: Test recovery behavior with synthetic late-arriving and partially written files\n\n## Code Example\n```sql\nMERGE INTO gold AS g\nUSING silver_staging AS s\nON g.tenant_id = s.tenant_id AND g.event_date = s.event_date AND g.record_key = s.record_key\nWHEN MATCHED THEN UPDATE SET *\nWHEN NOT MATCHED THEN INSERT *\n```\n\n## Test Plan\n1. Generate synthetic Parquet files with valid and invalid data\n2. Simulate late-arriving files by backdating file timestamps\n3. Simulate partial writes by interrupting a running job\n4. Verify that data-quality gates correctly reject invalid records\n5. Confirm deterministic upsert behavior produces no duplicates after recovery",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-10",
        "questionId": "q-12390",
        "question": "Ingesting 40 GB/day of JSON clickstream logs from S3 into Bronze, design a beginner Silver layer that flattens a nested, evolving payload into a stable schema. How would you: (1) auto-evolve schema for new fields, (2) extract top-level columns and keep the rest as a map, (3) upsert via MERGE from a staging view, (4) build a BI-ready Gold with a users dim, (5) enforce Unity Catalog RBAC to restrict access, (6) implement a data-quality gate with tests including a newly added field?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Auto Loader for incremental Bronze ingestion from S3. Implement per-file provenance by recording metadata in an AuditFiles table (path, ingest_ts) after each file completes. Use MERGE on event_id to upsert into Silv…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would enable Auto Loader with schema evolution, flatten fields like user.id, session.id, event_time, page.url, device.type, geo.country into Silver, store remaining as map<string,string>, upsert via",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Ingest via Auto Loader and structured streaming, Bronze stores raw JSON, Silver uses MERGE on event_id and upserts latest per event, a 2-minute watermark for late data, Gold computes hourly device agg",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage Auto Loader to ingest JSON from S3, create Bronze->Silver with tenant_id/date partitions, perform a MERGE into Gold on event_id, and write a SchemaAudit entry when new fields appear. Enforce",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis checks practical handling of evolving JSON in Delta Lake, simple flattening, and governance.\n\n## Key Concepts\n- JSON schema evolution in Databricks Delta Lake\n- Flattening nested fields and preserving extras as a map\n- MERGE-based CDC in a beginner-friendly flow\n- Unity Catalog RBAC for data access\n- Lightweight data-quality gating and tests\n\n## Code Example\n```python\nfrom pyspark.sql.functions import col\n\ndf = spark.read.json('s3://bucket/logs/')\ndf_flat = df.select(\n  col('user.id').alias('user_id'),\n  col('session.id').alias('session_id'),\n  col('event_time').alias('event_time'),\n  col('page.url').alias('page'),\n  col('device.type').alias('device_type'),\n  col('geo.country').alias('geo_country'),\n  col('payload').cast('map<string,string>').alias('attributes')\n)\n```\n\n## Follow-up Questions\n- How would you validate schema evolution with a newly added field?\n- How would you test the MERGE CDC logic end-to-end?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-11",
        "questionId": "q-1853",
        "question": "In a Databricks job ingesting 50 GB/day of Avro logs from S3 into Delta Lake, design a beginner-friendly Delta Live Tables pipeline to produce Bronze (raw) and Silver (flattened) tables. Include how you flatten the nested field user (id and tier) into Silver, enforce a simple data quality gate (NOT NULL user_id, user_tier in {'free','standard','premium'}), and choose a partitioning strategy by file_date. Explain testing and how you'll measure improvements?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Auto Loader to ingest Parquet into Bronze with schema enforcement. Merge into Silver by event_id to deduplicate. In Silver, join with a small country_whitelist delta table on country; create a boo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline approach: per-tenant catalogs with Unity Catalog; a central feature registry with semantic versioning; offline features stored in Delta tables; an online cache backed by Redis for 200 ms reads",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Delta Live Tables pipeline with Bronze (raw Avro from S3) and Silver (flattened). Bronze loads from s3://bucket/logs/daily/, Silver computes user_id = user.id and user_tier = user.tier, plus ess",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Set up a Bronze ingestion with Auto Loader for 20 GB/day CSVs. Silver layer: (1) normalize timestamps to UTC; (2) add ingest_ts and a 1h window_bucket; (3) deduplicate by record_id for the current day",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical DLT usage, Bronze/Silver modeling, nested field flattening, basic data quality gates, and partitioning in a real-ish beginner scenario.\n\n## Key Concepts\n- Delta Live Tables (DLT)\n- Bronze/Silver data modeling\n- Flattening nested structs in Spark\n- Simple data quality gates\n- Partitioning strategy and schema evolution\n\n## Code Example\n```javascript\nimport dlt\n@dlt.table\ndef bronze_logs():\n  return spark.read.format(\"avro\").load(\"s3://bucket/logs/daily/\")\n\n@dlt.table\ndef silver_logs():\n  b = dlt.read(\"bronze_logs\")\n  s = b.selectExpr(\"user.id as user_id\",\"user.tier as user_tier\", \"*\")\n  return s.filter(\"user_id IS NOT NULL AND user_tier IN ('free','standard','premium')\")\n```\n\n## Follow-up Questions\n- How would you test drift between Bronze and Silver? \n- How would you handle an unseen user_tier value (e.g., 'enterprise')?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-12",
        "questionId": "q-1100",
        "question": "In a Databricks data pipeline, ingest JSON events from S3 into Delta Lake with Auto Loader. Each event has a nested user object (id, email) and an optional pages array. Some events miss user.email. How would you design the pipeline to safely flatten nested fields, handle missing values, and upsert the latest user state into a Delta Silver table using an idempotent MERGE?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a two-tier store: offline features in Delta tables (Bronze-Silver-Gold) with feature_version and lineage, and an online store using Delta with a low-latency cache. Register features in a Unity Cat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose using DLT with per-tenant watermarks, deterministic MERGE keys, and row-level constraints. Bronze ingest from Kafka, then Silver with per-tenant SCD2 and a deterministic MERGE into tenant-scop",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Auto Loader with schema evolution, flatten nested fields, and a MERGE for idempotent upserts. ReadStream via cloudFiles, inferColumnTypes true, mergeSchema true. Flatten: user_id = user.id, email",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Auto Loader with per-file provenance and a drift-aware schema evolution policy, then stream a MERGE into a tenant-scoped Gold Delta table partitioned by tenant_id/date. Enforce RBAC + masking in U",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical mastery of ingesting semi-structured data, flattening nested JSON, handling optional fields, and performing idempotent upserts in Delta Lake using Databricks primitives.\n\n## Key Concepts\n\n- Auto Loader with cloudFiles options for JSON with schema evolution\n- Flattening nested structs and handling missing fields safely\n- Upserts with MERGE to ensure idempotent state in Delta Lake\n- Null handling with COALESCE and robust array expansion (explode_outer)\n\n## Code Example\n\n```javascript\n// Databricks PySpark-like sketch (syntax-highlighted as javascript)\nval df = spark.readStream.format(\"cloudFiles\")\n  .option(\"cloudFiles.format\",\"json\")\n  .option(\"cloudFiles.inferColumnTypes\",\"true\")\n  .option(\"cloudFiles.mergeSchema\",\"true\")\n  .load(\"s3a://bucket/events/\")\n\nval flat = df.selectExpr(\"user.id as user_id\",\n                       \"coalesce(user.email, '') as email\",\n                       \"ts as event_ts\",\n                       \"explode_outer(pages) as page\")\n```\n\n## Follow-up Questions\n\n- How would you test this pipeline end-to-end, including schema evolution scenarios?\n- How would you handle new nested fields added to user in future deployments?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-13",
        "questionId": "q-29924",
        "question": "In a multi-tenant Databricks pipeline ingesting ~75 GB/day of Parquet/JSON from S3, design a Silver layer that (1) validates against a centralized DataContract registry with versioned tenant schemas, (2) records per-file provenance, (3) rejects incompatible schema evolution, (4) upserts into tenant-specific Gold via MERGE on (tenant_id, event_id), (5) plugs a pluggable data-quality policy engine that blocks downstream on contract failures, and (6) includes synthetic late-data tests and a lightweight observability dashboard?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Bronze: Auto Loader ingests S3 Parquet with tenant_id; Silver: CDC-like MERGE Bronze->Silver with 2-minute watermark; Gold: tenant-scoped aggregates. Enforce Unity Catalog RBAC on Silver and AuditDelt",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Bronze→Silver→Gold pipeline using Auto Loader to ingest 200 GB/day of JSON events from two e-commerce APIs into Delta Lake on S3. The Bronze layer uses Auto Loader with schema inference and cloudFiles to land r…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Silver layer to enforce versioned DataContracts per tenant, emit per-file provenance, block incompatible schema changes, MERGE into tenant Gold on (tenant_id, event_id), hook a policy engine",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement Silver using Delta tables; use Auto Loader for Bronze; join with users_dim with a broadcast join to enrich event data; write to Silver via MERGE on event_id; create a masking view masking em",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to build contract-driven pipelines at scale, with multi-tenant governance, strict schema evolution control, and observable quality signals. It evaluates integration of a policy engine, deterministic upserts, and robust late-data testing.\n\n## Key Concepts\n- Data contracts and versioning\n- Per-file provenance\n- Schema-evolution governance\n- MERGE upserts into tenant-specific Gold\n- Pluggable data-quality policy engine\n- Observability: latency, drift, late-arrival\n- Synthetic late-data testing\n\n## Code Example\n```python\n# pseudo: validate against contract and provenance push\nschema = registry.lookup(tenant_id, version)\nrecord_ok = validate(record, schema)\nprovenance_store.write(file_path, ingest_ts)\n```\n\n## Follow-up Questions\n- How to upgrade contracts without breaking downstream?\n- How to simulate adversarial late data in tests and verify dashboard signals?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-14",
        "questionId": "q-27513",
        "question": "Ingest 40 GB/day of Parquet logs from S3 across multiple tenants; design a beginner Silver layer that enforces tenant-level schema contracts loaded from contracts/ (tenant_id -> JSON schema). Validate each incoming file against its contract before writing to Silver, and quarantine any file that fails. Then perform a deterministic upsert into Gold on (tenant_id, event_id). Outline steps and a minimal test plan with synthetic data including a mismatch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant lifecycle using Delta Lake tiering: hot Delta tables partitioned by tenant_id/date retained for 30 days; migrate data from days 31–90 to a cost-optimized warm archive tier using Delta/Parquet; archiv…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a pipeline that emits OpenLineage events at each stage, stores lineage in a dedicated Lineage table, plugs Unity Catalog masking/rbac for tenant isolation, uses Delta Sharing to expose curated",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Auto Loader to ingest per-file, extract tenant_id, load tenant schema from contracts/tenant_id.json, cast fields to expected types, and compare against the contract. If mismatch, write file metada",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Auto Loader for incremental Bronze ingestion from S3. Implement per-file provenance by recording metadata in an AuditFiles table (path, ingest_ts) after each file completes. Use MERGE on event_id to upsert into Silv…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design per-tenant data contracts and gate ingestion with an auditable quarantine path. Demonstrates practical validation, lineage, and upsert logic suitable for beginner Databricks roles.\n\n## Key Concepts\n- Auto Loader per-file ingestion with provenance\n- Tenant-specific schema contracts\n- Data quality gate and quarantine path\n- Deterministic upsert into Gold on composite key\n- Lightweight test plan with synthetic batches\n\n## Code Example\n```python\n# Pseudocode: load contract, validate, route\ncontract = spark.read.json(\"contracts/<tenant_id>.json\"); df = spark.read.parquet(file)\nif df.schema != contract.schema:\n  quarantine.write(df)\nelse:\n  df.mergeInto(\"Gold\", on=[\"tenant_id\",\"event_id\"])\n```\n\n## Follow-up Questions\n- How would you evolve contracts without breaking existing data?\n- How would you test contract validation with unit tests?\n- How would you monitor quarantined files and retries?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-15",
        "questionId": "q-29160",
        "question": "In a Databricks pipeline ingesting 50 GB/day Parquet logs from S3 across 5 tenants, design a beginner Silver layer that (a) captures per-file provenance, (b) enforces a lightweight data-quality gate yielding a 0–1 score based on allowed event_type values and event_time within 90 days, (c) performs a deterministic MERGE upsert into Gold that aggregates counts by tenant_id and day, and (d) gracefully handles late data via a streaming watermark of 1 day with checkpointing. Provide a concrete test plan with synthetic late files?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Silver layer that implements per-file provenance using a lightweight UDF to emit audit metadata (source_path, file_timestamp, ingest_ts) into an AuditFiles table with an exposed view; handle schema drift throug…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Auto Loader with per-file provenance and an explicit Spark StructType to ingest Parquet logs from S3. Enforce the schema via explicit CAST operations and validation checks to prevent schema drift. Implement a determ…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Auto Loader with `file_metadata` to preserve per-file provenance. Enforce a fixed schema upon ingestion. Implement a quality_score UDF that returns 1 if `event_type` in ('click', 'view', 'purchase') AND `event_time`…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Delta Live Tables to build Silver from Bronze. Define Silver with event_id, event_time, payload, and a provenance column; populate a separate AuditFiles Delta table via a lightweight batch_id UDF",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical Databricks Silver-Gold architecture design with per-file provenance, data quality gating, and late-arriving data handling using streaming. It emphasizes correctness, testability, and simple CDC-like semantics suitable for a beginner-level candidate.\n\n## Key Concepts\n\n- **Auto Loader provenance**: Using `file_metadata` to capture source file path, size, and modification timestamp\n- **Data quality scoring**: Lightweight UDF-based gating with 0–1 score based on business rules\n- **Delta MERGE upserts**: Idempotent writes to Gold layer for daily tenant aggregations\n- **Streaming watermarking**: Handling late-arriving data with a 1-day watermark threshold\n- **Checkpointing**: Ensuring exactly-once processing and resumeability\n\n## Code Example\n\n```python\nfrom pyspark.sql import functions as F\nfrom pyspark.sql.window import Window\n\n# Bronze: Ingest with Auto Loader and provenance\nbronze_df = (spark.readStream\n    .format(\"cloudFiles\")\n    .option(\"cloudFiles.schemaLocation\", \"/mnt/bronze/schema\")\n    .option(\"cloudFiles.metadataColumn\", \"_metadata\")\n    .load(\"s3://logs/parquet/*.parquet\"))\n\n# Silver: Quality scoring UDF\n@F.udf\ndef quality_score(event_type: str, event_time: timestamp) -> int:\n    allowed_types = {\"click\", \"view\", \"purchase\"}\n    if event_type not in allowed_types:\n        return 0\n    days_90_ago = datetime.now() - timedelta(days=90)\n    if event_time < days_90_ago:\n        return 0\n    return 1\n\nsilver_df = (bronze_df\n    .withColumn(\"quality_score\", quality_score(F.col(\"event_type\"), F.col(\"event_time\")))\n    .withColumn(\"ingestion_timestamp\", F.current_timestamp()))\n\n# Route to quarantine vs pass\nquarantine_df = silver_df.filter(F.col(\"quality_score\") == 0)\nclean_df = silver_df.filter(F.col(\"quality_score\") == 1)\n\n# Write clean silver with watermark for late data\nquery = (clean_df\n    .withWatermark(\"event_time\", \"1 day\")\n    .writeStream\n    .format(\"delta\")\n    .outputMode(\"append\")\n    .option(\"checkpointLocation\", \"/mnt/silver/checkpoint\")\n    .table(\"silver_logs\"))\n\n# Gold: Daily aggregation with MERGE upsert\ngold_df = (spark.table(\"silver_logs\")\n    .groupBy(\"tenant_id\", F.col(\"event_time\").cast(\"date\").alias(\"event_date\"))\n    .agg(\n        F.count(\"*\").alias(\"event_count\"),\n        F.sum(F.when(F.col(\"event_type\") == \"click\", 1).otherwise(0)).alias(\"click_count\"),\n        F.sum(F.when(F.col(\"event_type\") == \"view\", 1).otherwise(0)).alias(\"view_count\"),\n        F.sum(F.when(F.col(\"event_type\") == \"purchase\", 1).otherwise(0)).alias(\"purchase_count\")\n    ))\n\n# MERGE into Gold (idempotent upsert)\n(gold_df.writeStream\n    .format(\"delta\")\n    .outputMode(\"append\")\n    .option(\"checkpointLocation\", \"/mnt/gold/checkpoint\")\n    .trigger(availableNow=True)\n    .toTable(\"gold_daily_tenant_summary\"))\n\n# For MERGE in batch mode:\ngold_df.createOrReplaceTempView(\"updates\")\nspark.sql(\"\"\"\n    MERGE INTO gold_daily_tenant_summary target\n    USING updates source\n    ON target.tenant_id = source.tenant_id AND target.event_date = source.event_date\n    WHEN MATCHED THEN UPDATE SET\n        event_count = target.event_count + source.event_count,\n        click_count = target.click_count + source.click_count,\n        view_count = target.view_count + source.view_count,\n        purchase_count = target.purchase_count + source.purchase_count\n    WHEN NOT MATCHED THEN INSERT *\n\"\"\")\n```\n\n## Test Plan with Synthetic Late Files\n\n1. **Setup**: Create a test Delta table with known `event_time` dates\n2. **On-time test**: Insert records with `event_time` = today → verify Gold count increments\n3. **Late data test**: Insert records with `event_time` = today - 3 days → verify watermark allows arrival within 1 day\n4. **Late data rejection**: Insert records with `event_time` = today - 5 days → verify dropped by watermark\n5. **Quality gate test**: Insert record with `event_type` = 'invalid' → verify `quality_score` = 0 and routed to quarantine\n6. **Old event test**: Insert record with `event_time` = today - 100 days → verify `quality_score` = 0\n7. **Idempotency test**: Re-run same batch → verify Gold counts don't double\n8. **Provenance test**: Query `_metadata` column → verify source file path captured accurately",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-16",
        "questionId": "q-5974",
        "question": "In a Databricks pipeline ingesting multi-tenant SaaS events from Kafka and S3 Parquet into Bronze, design a Silver and Gold layer with per-tenant isolation via Unity Catalog dynamic views (row-level predicates). Implement a dynamic predicate using current_user() to restrict rows by tenant_id in Silver/Gold, while preserving CDC via MERGE and handling schema drift. Provide concrete steps, a sample dynamic-view SQL, and a lightweight test plan with synthetic tenants?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Delta Live Tables pipeline with Bronze (raw Avro from S3) and Silver (flattened). Bronze loads from s3://bucket/logs/daily/, Silver computes user_id = user.id and user_tier = user.tier, plus ess",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement Silver using Delta tables; use Auto Loader for Bronze; join with users_dim with a broadcast join to enrich event data; write to Silver via MERGE on event_id; create a masking view masking em",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Unity Catalog Dynamic Views to enforce per-tenant access. Bronze→Silver deduplication by event_id with MERGE CDC; create Silver_Tenant as a dynamic view filtered by tenant_id using current_user(). Gold layer ag…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Set up a Delta Live Tables pipeline with Bronze ingest from S3 (Auto Loader) and Kafka, Silver deduped by event_id using MERGE CDC with a 2-minute watermark, and Gold with per-tenant aggregates. Enfor",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests per-tenant data isolation in a multi-source Delta pipeline, plus dynamic views and schema drift handling in Unity Catalog.\n\n## Key Concepts\n\n- Unity Catalog Dynamic Views\n- current_user() predicates\n- MERGE CDC with schema drift\n- RBAC for cross-tenant access\n\n## Code Example\n\n```sql\n-- SQL sample for Silver_Tenant dynamic view\nCREATE OR REPLACE VIEW Silver_Tenant AS\nSELECT * FROM Silver\nWHERE tenant_id = current_user();\n```\n\n## Follow-up Questions\n\n- How would you audit and test dynamic view predicates?\n- How do you onboard new tenants without redeploying views?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-17",
        "questionId": "q-17533",
        "question": "In a Databricks pipeline ingesting 40 GB/day of JSON logs from S3 into Bronze, design a beginner Silver layer that (1) flattens nested JSON into flat columns, (2) creates a tenant-scoped key and a simple user_id mapping table, (3) materializes a daily Gold table for BI dashboards, (4) enforces Unity Catalog RBAC with masking for PII, and (5) includes a lightweight test harness seeded with synthetic data to validate schema compatibility and query performance. Outline concrete steps and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Auto Loader with per-file provenance, enforce a fixed schema (reject new fields), write a lightweight DataQuality table logging daily row_count and null counts for key fields, upsert into Gold by",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To implement: in Silver layer, materialize an enrichment stream from a REST service (minutely updated) as a separate streaming source; join with Bronze by a stable key; retain provenance fields (sourc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enable Delta schema auto-merge for Silver and implement MERGE CDC from Bronze to Silver on event_id. Apply a 1-hour watermark; use schema evolution handling for new fields via ALTER TABLE ADD COLUMN.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner Silver layer for a Databricks pipeline ingesting 40 GB/day of JSON logs from S3 into Bronze, implementing: **Step 1: Flatten Nested JSON into Flat Columns** - Use Spark's `from_json` to parse JSON stri…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical data engineering fundamentals: JSON flattening, Silver/Gold medallion architecture modeling, Unity Catalog masking and RBAC implementation, and building lightweight test harnesses with synthetic data for schema and performance validation.\n\n## Key Concepts\n- Databricks Auto Loader for incremental data ingestion\n- Flattening nested JSON structures into flat columnar format\n- Delta Lake medallion architecture (Bronze/Silver/Gold)\n- Unity Catalog role-based access control and dynamic data masking\n- Lightweight test harness for schema drift and data quality validation",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-18",
        "questionId": "q-30058",
        "question": "In a Databricks pipeline ingesting 40 GB/day of JSON logs from S3 for 3 tenants, design a beginner Silver layer that handles JSON schema drift using a lightweight schema registry stored in Delta, applies per-tenant retention, uses Auto Loader with per-file provenance, and performs a deterministic MERGE into Gold on event_id. Include a minimal test plan with late data and drift scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a deletion workflow with a DeletionLog stream; when a deletion request arrives, publish user_id and deletion_ts to a Delta table and a separate DeletionLog; on Bronze and Silver, use MERGE t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a lightweight JSON schema registry stored as a Delta table to maintain schema versions and field definitions. Use Auto Loader with cloudFiles.schemaLocation to automatically detect and track schema drift. In t…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Deduplicate with composite key (user_id, event_ts) in Silver using MERGE to upsert latest per key, applying a 3-minute watermark for late data. Bronze ingests from S3 Parquet and REST JSON. Gold store",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition Silver by tenant_id and date; enable Delta Change Data Feed for per-tenant CDC; use an idempotent MERGE into Silver on event_id; materialize a tenant-scoped Gold table (tenant_id, day); enfo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to handle evolving JSON schemas with a lightweight registry, ensuring downstream stability without full pipeline rewrites. It tests knowledge of Auto Loader capabilities, tenant isolation patterns, and idempotent writes.\n\n## Key Concepts\n- JSON schema drift handling with Delta\n- Auto Loader schemaLocation for schema evolution\n- MERGE upserts into Gold on event_id\n- Tenant isolation and per-tenant retention\n- Deterministic data processing for exactly-once semantics\n\n## Code Example\n```python\n# Register schema in Delta registry\nspark.sql(\"\"\"\nCREATE TABLE IF NOT EXISTS schema_registry (\n    tenant_id STRING,\n    schema_version INT,\n    schema_json STRING,\n    created_at TIMESTAMP\n) USING DELTA\nPARTITIONED BY (tenant_id)\n\"\"\")\n\n# Auto Loader with schema evolution\ndf = (spark.readStream\n    .format(\"cloudFiles\")\n    .option(\"cloudFiles.format\", \"json\")\n    .option(\"cloudFiles.schemaLocation\", \"/mnt/schema_evolution/\")\n    .option(\"cloudFiles.schemaEvolutionMode\", \"addNewColumns\")\n    .load(\"s3://logs-bucket/raw/\"))\n\n# Validate and apply defaults\nfrom pyspark.sql.functions import col, when, lit\ndf_validated = df.select(\n    *[col(c) for c in df.columns],\n    when(col(\"tenant_id\").isNull(), lit(\"unknown\")).otherwise(col(\"tenant_id\")).alias(\"tenant_id\")\n)\n\n# Write to Silver with tenant partitioning\n(df_validated.writeStream\n    .format(\"delta\")\n    .partitionBy(\"tenant_id\")\n    .option(\"checkpointLocation\", \"/mnt/checkpoints/silver/\")\n    .trigger(availableNow=True)\n    .table(\"silver_logs\"))\n\n# MERGE into Gold on event_id\nspark.sql(\"\"\"\nMERGE INTO gold_logs AS target\nUSING silver_logs AS source\nON target.event_id = source.event_id\nWHEN MATCHED THEN UPDATE SET *\nWHEN NOT MATCHED THEN INSERT *\n\"\"\")\n\n# Per-tenant retention (e.g., 90 days per tenant)\nspark.sql(\"\"\"\nDELETE FROM silver_logs \nWHERE tenant_id = '{tenant_id}' \n  AND ingestion_time < current_timestamp() - interval 90 days\n\"\"\")\n```\n\n## Minimal Test Plan\n\n### Late Data Scenario\n| Step | Action | Expected Result |\n|------|--------|------------------|\n| 1 | Load batch with event_id=\"E1\", ts=\"T1\" into Gold | Record inserted |\n| 2 | Re-process batch containing event_id=\"E1\", ts=\"T1\" | No duplicate; record unchanged |\n| 3 | Process batch with event_id=\"E1\", ts=\"T2\" (updated) | Record updated with new timestamp |\n\n### Schema Drift Scenario\n| Step | Action | Expected Result |\n|------|--------|------------------|\n| 1 | Load JSON with fields {id, name, email} | Schema registered; data in Silver |\n| 2 | Load JSON with new field {phone} added | Schema evolved; column auto-added via cloudFiles.schemaLocation |\n| 3 | Verify MERGE to Gold includes new field | Gold table reflects schema change |\n| 4 | Load JSON missing optional field {name} | Default value applied; no null pointer errors |\n\n### Tenant Isolation Scenario\n| Step | Action | Expected Result |\n|------|--------|------------------|\n| 1 | Insert record for tenant=A and tenant=B | Both records exist in partitioned tables |\n| 2 | Apply 30-day retention for tenant=A only | Tenant=A records older than 30 days deleted; tenant=B unaffected |\n| 3 | Query per-file provenance for tenant=A event | Lineage shows source file path and timestamp |",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-19",
        "questionId": "q-20325",
        "question": "In a Databricks pipeline ingesting 80 GB/day of Parquet logs from S3 for 3 tenants, design a beginner-friendly Silver layer that (1) validates per-tenant data quality with a simple schema-drift detector and Delta schema evolution, (2) preserves history with automatic evolution, (3) performs a deterministic upsert into a per-tenant Gold table keyed by (tenant_id, event_id), (4) enforces Unity Catalog RBAC so each tenant reads only their Silver/Gold, and (5) includes a practical test plan for late-arriving data and a schema change?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Silver layer that partitions by tenant_id with per-tenant data masking, implements CDC-like MERGE operations using event_id as the natural key, applies schema evolution rules for drift handling, incorporates ev…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Configure Auto Loader to ingest per-tenant Parquet paths; implement a lightweight schema-drift detector that compares the incoming schema to a stored snapshot and triggers Delta schema evolution; use",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Bronze from S3 JSON (Auto Loader) and Kafka; Silver computes per-user features (last_seen, total_events, avg_session_len) with a 3-minute watermark; Gold registers features in the Databricks Feature S",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Silver layer using Delta Live Tables that preserves per-file provenance (source_path, file_timestamp, ingest_ts) and exposes a provenance view. Handle per-tenant schema drift through a central schema registr…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical application of Delta Lake schema evolution, per-tenant isolation, simple drift checks, and late data handling in a real Databricks workflow.\n\n## Key Concepts\n\n- Auto Loader per-tenant paths\n- Delta Lake schema evolution and drift detection\n- MERGE-based upserts for Gold\n- Unity Catalog RBAC for tenant isolation\n- Late data handling and lightweight tests\n\n## Code Example\n\n```python\n# PySpark sketch: drift check and evolution trigger\nfrom pyspark.sql.types import StructType\n\ndef drift_detect(new_schema: StructType, snapshot_schema: StructType) -> bool:\n    return new_schema != snapshot_schema\n```\n\n## Follow-up Questions\n\n- How would you handle frequent, non-breaking schema changes?\n- What tests would you add to catch regressions in RBAC policy changes?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-20",
        "questionId": "q-3914",
        "question": "In a beginner Databricks pipeline, ingest 5 GB/day JSON clickstream from S3 into Bronze via Auto Loader; flatten nested payloads into Silver, deduplicate by event_id with MERGE CDC, and apply a 2-minute watermark. Build Gold as daily active users by country. Partition by date; enforce Unity Catalog RBAC. Provide a lightweight test plan and RBAC considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Auto Loader for mixed Parquet/JSON into Bronze, with Delta schema evolution and a Silver MERGE by (tenant_id, event_id). Add a per-file provenance AuditFilesDelta via a simple UDF that return",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a DLT Silver pipeline that stores per-tenant provenance (source_path, ingest_ts, tenant_id), leverages Change Data Feed for CDC, applies a schema evolution policy for nested JSON using STRICT mode or ADD_COLUMNS…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a two-table CDC pattern: Bronze stream ingests events from Kafka, writes (tenant_id, event_id, ts, payload). Silver MERGEs into a partitioned table by tenant_id/date, upserting on (tenant_id, even",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest 5 GB/day JSON clickstream from S3 to Bronze via Auto Loader; flatten nested payloads into Silver, deduplicate by event_id with MERGE CDC, and apply a 2-minute watermark. Build Gold as daily act",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to handle nested JSON, streaming ingestion with Auto Loader, deduplication via MERGE CDC, late data handling with a watermark, and governance via Unity Catalog RBAC. Also evaluates end-to-end testing mindset.\n\n## Key Concepts\n- Auto Loader streaming ingestion\n- Flattening nested JSON payloads\n- MERGE CDC for event_id dedup\n- 2-minute watermark for late data\n- Bronze/Silver/Gold data lake pattern\n- Unity Catalog RBAC and lineage\n\n## Code Example\n```python\n# PySpark flattening example\nraw = spark.readStream.format(\"cloudFiles\").option(\"cloudFiles.format\",\"json\").load(\"s3://bucket/clickstream/\")\nsilver = raw.selectExpr(\"event_id\", \"cast(ts as timestamp) as ts\", \"payload.user_id as user_id\", \"payload.country as country\", \"payload.actions as actions\")\n```\n\n## Follow-up Questions\n- How would you monitor late data and alert on stalls?\n- What are the trade-offs between MERGE CDC vs UPDATE-only approaches?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-21",
        "questionId": "q-2033",
        "question": "In a Databricks workflow, ingest 50 GB/day of JSON web logs from S3 into a Bronze Delta table. Propose a beginner-friendly pipeline to populate a Silver table that upserts the latest event per session_id, handles 2-minute late data with a watermark, and validates data quality before write. Include partitioning strategy, a MERGE-based CDC, and Unity Catalog RBAC considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Add per-row versioning in Delta Lake: include valid_from, valid_to, and version_id columns; retain historical versions in the same table. RTBF uses tombstones: insert a tombstone and set valid_to to t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a pipeline using Autoloader to land JSON into Bronze, then upsert into Silver with MERGE on session_id using the latest timestamp. Apply a 2-minute watermark for late data, partition Silver by date for query perf…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a two-tier Silver and Gold architecture for a multi-tenant Databricks lakehouse handling 60-100 GB/day. **Ingestion and Lineage:** Use Auto Loader with `fileMetadata` mode to capture per-file lineage (source path…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement Bronze→Silver→Gold: dedupe by customer_id with max(event_time) in Silver; allowMissingFields for schema drift. In Gold, MERGE into customer_dim: on match with changes, set old row valid_to a",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\n- Tests practical Delta Lake upsert and late data handling with realistic data volume\n- Evaluates ability to translate business rules into robust, auditable pipelines\n- Assesses understanding of medallion architecture and enterprise data governance\n\nKey Concepts\n- Autoloader ingestion, Bronze to Silver MERGE CDC, watermarking for late data\n- Partitioning for query efficiency, schema quality gates, Unity Catalog RBAC\n- Practical validation without overengineering\n\nCode Example\n```javascript\n// Illustrative MERGE logic (conceptual)\nMERGE INTO silver_sessions AS s\nUSING (\n  SELECT session_id, MAX(ts) AS ts, MAX(event_type) AS event_type\n  FROM bronze_events\n  WHERE ts >= current_timestamp() - INTERVAL 2 MINUTES\n  GROUP BY session_id\n) AS b\nON s.session_id = b.session_id\nWHEN MATCHED THEN UPDATE SET *\nWHEN NOT MATCHED THEN INSERT *\n```",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-22",
        "questionId": "q-9089",
        "question": "In a Databricks pipeline ingesting 40 GB/day of mixed Parquet and JSON logs from S3 into Bronze, design a Silver layer that supports schema evolution, upserts by a composite key (tenant_id, event_id), records per-file provenance (path, ingest_ts, file_size, source_format) via a batch_id UDF, masks PII in a view, and enforces Unity Catalog RBAC for read to Silver and Gold. Include a robust test plan with synthetic data including partially written files and recovery?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Auto Loader for mixed Parquet/JSON into Bronze, with Delta schema evolution and a Silver MERGE by (tenant_id, event_id). Add a per-file provenance AuditFilesDelta via a simple UDF that return",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Set up a Delta Live Tables pipeline with Bronze ingest from S3 (Auto Loader) and Kafka, Silver deduped by event_id using MERGE CDC with a 2-minute watermark, and Gold with per-tenant aggregates. Enfor",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose using DLT with per-tenant watermarks, deterministic MERGE keys, and row-level constraints. Bronze ingest from Kafka, then Silver with per-tenant SCD2 and a deterministic MERGE into tenant-scop",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bronze: Ingest both API feeds using Auto Loader into Delta Bronze tables with unified schema handling. Silver: Apply MERGE-CDC to deduplicate by event_id, normalize data types, and implement a 2-minute watermark for lat…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests handling of schema evolution, CDC-like upserts, provenance, masking, and RBAC in a real Databricks flow; also evaluates a concrete test strategy for partial data and failures.\n\n## Key Concepts\n\n- Auto Loader with multi-format ingest\n- Delta Lake schema evolution and MERGE\n- per-file provenance with AuditFilesDelta\n- UDF to generate batch_id and attach source metadata\n- PII masking policy and masking view\n- Unity Catalog RBAC for Silver/Gold\n- robust test plan including partial writes and recovery\n\n## Code Example\n\n```javascript\n// Example: batch_id UDF pseudocode\nfunction batch_id(path, ingest_ts) {\n  // deterministic ID based on path and timestamp\n  return hash(path + ':' + ingest_ts);\n}\n```\n\n## Follow-up Questions\n\n- How would you handle evolving nested fields without breaking downstream queries?\n- How would you monitor and alert on data quality breaches across environments?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-23",
        "questionId": "q-15876",
        "question": "In a Databricks multi-tenant platform ingesting 200 GB/day of Parquet/JSON logs from S3 into a two‑layer lakehouse, design a Silver layer that enforces per‑tenant data residency, governance, and scalability. Provide concrete steps for per‑file provenance, JSON schema drift handling, CDC‑like MERGE into Silver, a BI‑ready Gold, Unity Catalog RBAC with masking and per‑tenant filters, a data‑quality gate, and per‑tenant late‑data handling with watermarking; include Delta Sharing exports with policy‑based access and a test plan with synthetic late data and cross‑tenant leakage checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Delta Sharing producer dataset based on a Silver view (tenant_id, metric, value) with masking on sensitive fields. Publish as a Delta Share to a consumer role with read access only. Enforce per",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement Delta Live Tables (DLT) for Bronze-Silver-Gold pipelines with a versioned Databricks Feature Store integrated with Unity Catalog RBAC. For the Bronze layer, create streaming tables for Kafka orders (5 MB/s) us…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Silver layer with per-tenant isolation using Auto Loader for incremental ingestion from S3. Implement per-file provenance via Delta Lake transaction logs and audit tables tracking source file paths, ingestion t…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a Silver layer that reads per-tenant Bronze, producing a single Silver table with surrogate_key as sha2(concat(tenant_id, event_id, ingest_ts), 256). Upsert into Silver via MERGE on event_id an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses the ability to design a scalable, tenant-aware lakehouse architecture with governance, provenance, and testing integrated into the data pipeline. It tests knowledge of Databricks lakehouse patterns, multi-tenant security models, and operational best practices for production data platforms.\n\n## Key Concepts\n- Tenant-scoped data residency and RBAC in Unity Catalog\n- Per-file provenance using Delta Lake transaction logs and audit tracking\n- Schema evolution and drift handling for semi-structured data\n- CDC-like MERGE operations for Silver with event_id as the merge key\n- Gold layer denormalization per tenant and day for BI access\n- Delta Sharing with policy-based access controls\n- Data-quality gates and validation frameworks\n- Per-tenant late data handling with watermarking\n\n## Code Example\n```sql\n-- MERGE into Silver per tenant (CDC-like pattern)\nMERGE INTO silver_logs AS s \nUSING staging_logs AS st \nON s.event_id = st.event_id AND s.tenant_id = st.tenant_id\nWHEN MATCHED THEN \n    UPDATE SET \n        s.payload = st.payload,\n        s.processed_at = current_timestamp(),\n        s._updated_at = current_timestamp()\nWHEN NOT MATCHED THEN \n    INSERT (tenant_id, event_id, payload, ingested_at, processed_at, _created_at, _updated_at)\n    VALUES (st.tenant_id, st.event_id, st.payload, st.ingested_at, current_timestamp(), current_timestamp(), current_timestamp());\n\n-- Per-tenant late data watermark query\nSELECT * FROM bronze_logs \nWHERE tenant_id = '{tenant_id}' \nAND ingestion_time > '{watermark}' \nAND ingestion_time >= '{tenant_lookback_start}';\n\n-- Cross-tenant leakage test\nSELECT tenant_id, COUNT(*) as record_count \nFROM gold_logs \nGROUP BY tenant_id \nHAVING COUNT(DISTINCT original_tenant_id) > 1;\n```",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-24",
        "questionId": "q-8700",
        "question": "Design an advanced Databricks Silver layer for a lakehouse ingesting 40 GB/day of Parquet logs from S3 into Bronze. The Silver layer must normalize to a canonical schema with optional fields that evolve, capture per-file provenance, enforce Delta check constraints (not null event_time; event_type in ['click','view','purchase']), apply Unity Catalog RBAC per-tenant, implement a 15-minute late-data watermark with a MERGE-based CDC to Silver, and expose a masking view for PII. Provide concrete steps and a lightweight test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Add per-row versioning in Delta Lake: include valid_from, valid_to, and version_id columns; retain historical versions in the same table. RTBF uses tombstones: insert a tombstone and set valid_to to t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Auto Loader to ingest S3 Parquet into Bronze. In Silver, cast event_time to UTC (from per-service TZ via to_timestamp(event_time, tz)). Upsert latest per customer_id with MERGE into Silver; partit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a Silver layer that normalizes to a canonical, evolvable schema with optional fields; include per-file provenance via a file_metadata table; enforce Delta CHECK constraints: not null event_tim",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design Bronze with Auto Loader; Silver uses MERGE by event_id to upsert into a tenant-scoped Silver; maintain a per-tenant schema_contracts Delta table to codify expected fields; implement drift_detec",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes ability to design a robust Silver layer that handles evolving schemas, strict data quality, per-tenant access, late data, and privacy masking in a production-grade Databricks environment.\n\n## Key Concepts\n- Schema evolution with optional fields\n- Delta Lake CHECK constraints\n- Unity Catalog RBAC and masking views\n- CDC with MERGE and late data watermark\n- Per-file provenance and synthetic-data testing\n\n## Code Example\n```sql\n-- Example skeleton for a MERGE CDC into Silver\nMERGE INTO silver_table AS s\nUSING bronze_table AS b\nON s.event_id = b.event_id\nWHEN MATCHED THEN UPDATE SET s.* = b.*\nWHEN NOT MATCHED THEN INSERT (*)\n```\n\n## Follow-up Questions\n- How would you backfill existing data after introducing a new optional field?\n- How would you validate that the masking view enforces tenant isolation across tenants?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-05-14T03:20:50.339Z",
    "version": 1
  }
]