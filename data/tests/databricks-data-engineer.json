[
  {
    "id": "test-databricks-data-engineer",
    "channelId": "databricks-data-engineer",
    "channelName": "Databricks Data Engineer",
    "title": "Databricks Data Engineer Knowledge Test",
    "description": "Test your understanding of Databricks Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "databricks-data-engineer-q-0",
        "questionId": "q-24700",
        "question": "In a Databricks pipeline ingesting 100 GB/day of multi-tenant Parquet and JSON from S3, design a Silver layer using Delta Live Tables that (1) captures per-tenant CDC into a tenant-scoped Silver table, (2) ensures tenant isolation via per-tenant compute pools and Unity Catalog RBAC, (3) enforces a schema evolution policy for nested fields, (4) preserves per-file provenance in an AuditFiles table, (5) performs deterministic upserts into Gold, (6) handles late data with per-tenant watermark, and (7) outlines a test plan with synthetic late files and tenant outages. Provide concrete steps and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a three-pronged Silver design: track per-file provenance in AuditFiles; implement schema drift policy with Delta Lake evolution; use CDC-like MERGE into Gold keyed by tenant_id and event_id; a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a beginner Silver layer for a Databricks pipeline ingesting 40 GB/day of JSON logs from S3 into Bronze, implementing: **Step 1: Flatten Nested JSON into Flat Columns** - Use Spark's `from_json` to parse JSON stri…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create Silver.Customer_SCD2 with surrogate_key, customer_id, name, email, begin_date, end_date, current_flag. On each event, MERGE into Silver: if same customer_id and attributes changed, end_date of",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Delta Live Tables Silver layer with tenant-scoped CDC, per-tenant compute pools for isolation, and Unity Catalog RBAC. Enforce a schema evolution policy for nested fields, preserve per-file p",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to implement tenant-aware, production-grade Delta Live Tables pipelines with CDC, provenance, and late-data handling, while balancing cost and isolation.\n\n## Key Concepts\n- Delta Live Tables (DLT) Silver/Gold flows\n- per-tenant isolation via compute pools and Unity Catalog RBAC\n- CDC and deterministic MERGE into Gold\n- schema evolution for nested fields\n- per-file provenance via AuditFiles\n- per-tenant watermark and late-data handling\n- synthetic testing and fault-tolerance\n\n## Code Example\n```javascript\n// DL T able setup (pseudo) - not executable code\n```\n\n## Follow-up Questions\n- How would you monitor per-tenant latency and automatically reallocate pools?\n- How would you test rollback and backfill scenarios across tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-1",
        "questionId": "q-6490",
        "question": "Ingest 3 GB/day of Parquet customer_events from S3 into Bronze with Auto Loader. Design a beginner-friendly Silver layer implementing an SCD Type 2 Customer dimension with a surrogate key, begin_date, end_date, and a current_flag. Use MERGE CDC to upsert on changes, support simple schema evolution for new fields, apply a 1-day late-data watermark, and enforce Unity Catalog RBAC on Silver. Outline concrete steps for partitioning, CDC, testing, and a lightweight backfill plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create Silver.Customer_SCD2 with surrogate_key, customer_id, name, email, begin_date, end_date, current_flag. On each event, MERGE into Silver: if same customer_id and attributes changed, end_date of",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement Silver with a governance policy table driving Delta Sharing exposure per tenant, plus Unity Catalog dynamic masking; persist per-file provenance in AuditFiles; handle schema drift with evolu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "DLT Silver plan: ingest Bronze Parquet with schema evolution (allow_unsafe_schema_evolution) to accept new fields; gate access via Unity Catalog RBAC and mask PII with masking policy/view; implement d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enable Delta Change Data Feed and schema auto-merge; configure Auto Loader with schemaEvolutionMode=enabled; set spark.databricks.delta.schema.autoMerge.enabled=true and spark.databricks.delta.propert",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to implement SCD Type 2 in a beginner-friendly way using MERGE CDC, while addressing late data, schema evolution, and RBAC.\n\n## Key Concepts\n- SCD Type 2 with surrogate keys\n- MERGE CDC pattern\n- Delta Lake schema evolution\n- Unity Catalog RBAC\n- Watermark for late data and a simple backfill plan\n\n## Code Example\n```sql\nMERGE INTO Silver.Customer_SCD2 AS tgt\nUSING Bronze.customer_events AS src\nON tgt.customer_id = src.customer_id AND tgt.end_date = DATE '9999-12-31'\nWHEN MATCHED AND (src.name <> tgt.name OR src.email <> tgt.email) THEN\n  UPDATE SET end_date = CURRENT_DATE(), current_flag = 'N'\nWHEN NOT MATCHED THEN\n  INSERT (surrogate_key, customer_id, name, email, begin_date, end_date, current_flag)\n  VALUES (uuid(), src.customer_id, src.name, src.email, CURRENT_DATE(), DATE '9999-12-31', 'Y');\n```\n\n## Follow-up Questions\n- How would you validate that historical rows are preserved while the latest view reflects current data?\n- How would you handle schema evolution when a new field is added to the source?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-2",
        "questionId": "q-13196",
        "question": "In a Databricks Delta Live Tables pipeline processing 200 GB/day of multi-tenant Parquet/Avro from Kinesis Firehose, design a Silver layer that partitions by tenant_id with masking, applies CDC-like MERGE on event_id, handles schema drift via evolution rules, uses event-time watermarking and late-data handling, and enforces per-tenant quality gates to block downstream on failures; include a testing plan with synthetic late data and tenant isolation. What concrete steps would you take?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Silver layer that partitions by tenant_id with per-tenant data masking, implements CDC-like MERGE operations using event_id as the natural key, applies schema evolution rules for drift handling, incorporates ev…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose Bronze from Kafka JSON and S3 Parquet, Silver with CDC MERGE on canonical_id, 15-minute watermark for late data, and per-field masking on PII via Unity Catalog column-level security. Include a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design and implement a Silver layer in Delta Live Tables to meet enterprise governance and data quality requirements. The implementation addresses seven key areas: 1. **Per-File Provenance**: Leverage Auto Loader with `…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a broadcast join and caching. Load the 10M-row fact table as df_big and the 10k-row segments as df_small, then join with F.broadcast(df_small) on customer_id. Cache df_big for the daily run. Write",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests comprehensive multi-tenant pipeline design capabilities, including governance, data quality, and robust event-time processing in Databricks Delta Live Tables.\n\n## Key Concepts\n- Delta Live Tables with CDC-like MERGE operations\n- Schema evolution and drift handling strategies\n- Event-time watermarking and late data processing\n- Unity Catalog data masking and per-tenant RBAC\n- Per-tenant quality gates and rollback mechanisms\n\n## Code Example\n```python\nimport dlt\nfrom pyspark.sql import functions as F\n\n@dlt.table(\n  partition_cols=[\"tenant_id\"],\n  schema_drift_mode=\"allow\"\n)\ndef silver():\n  df = dlt.read_stream(\"bronze\").select('*')\n  # Apply tenant-aware masking and business rules\n  return apply_masking_and_validations(df)\n```\n\n## Follow-up Questions\n- How would you monitor per-tenant SLA compliance and manage backfill operations when failures occur?\n- What approach would you take for tenant isolation during testing and canary deployments?\n- How would you optimize storage costs while maintaining per-tenant query performance?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-3",
        "questionId": "q-28007",
        "question": "In a Databricks pipeline ingesting 40 GB/day of Parquet logs from S3 across tenants, design a beginner Silver layer using Delta Live Tables to (1) enforce a rolling schema with a drift guard, (2) generate a per-record quality flag (non-null event_id, valid timestamp, non-decreasing event_time per tenant), (3) deterministically upsert into a tenant-scoped Gold table, and (4) handle late-arriving files by reprocessing affected partitions. Include a concise test plan with synthetic late data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Add per-row versioning in Delta Lake: include valid_from, valid_to, and version_id columns; retain historical versions in the same table. RTBF uses tombstones: insert a tombstone and set valid_to to t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Auto Loader to ingest JSON from S3 into Bronze, create Silver with per-user daily activity_score = sum(weighted_event) by user_id/date, apply a 1-day watermark on event_time, then MERGE into Gold",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a multi-tenant Silver layer ingesting 100 GB/day of Parquet from S3 with Auto Loader provenance. Add a Contracts table describing per-tenant schemas, required fields, and ranges, plus a policy",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Delta Live Tables pipeline with Bronze/Silver/Gold. Add a small schema_version drift guard table. Compute a per-record quality flag (non-null event_id, valid timestamp, timestamp monotonic pe",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis checks practical use of Delta Live Tables for a beginner level, ensuring schema discipline, data quality gates, idempotent upserts, and late-data handling. It also tests test planning and fault-tolerance.\n\n## Key Concepts\n- Delta Live Tables\n- schema drift guards\n- MERGE upserts\n- watermark/late data handling\n- synthetic data testing\n\n## Code Example\n\n```javascript\n// Placeholder DLT setup sketch\n```\n\n## Follow-up Questions\n- How would you scale the drift guard as schemas evolve?\n- How would you monitor data quality gates in production?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-4",
        "questionId": "q-6415",
        "question": "In a Databricks data pipeline ingesting 200 GB/day of JSON clickstream logs from S3 via Auto Loader, design a three-layer architecture (Bronze, Silver, Gold) that deduplicates by a composite key (user_id, event_id) using MERGE CDC, implements a 15-minute watermark to accommodate late data, and adds a per-file AuditDelta table with path, ingest_ts, file_size, and file_count. Specify Unity Catalog RBAC for Silver and AuditDelta, and implement a masking view for PII with role-based access. Outline a lightweight test plan using synthetic data to verify CDC, watermark, masking, and schema evolution handling without breaking existing workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Bronze layer ingests raw JSON clickstream from S3 using Auto Loader with schema inference; Silver layer performs MERGE CDC operations on composite key (user_id, event_id) with 15-minute watermark tolerance for late data…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a Silver layer partitioned by region and date while maintaining per-minute region counts in a RegionMinuteAgg table; implement event_id-based upserts from Bronze using MERGE operations; create a region-scoped mas…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Auto Loader with `file_metadata` to preserve per-file provenance. Enforce a fixed schema upon ingestion. Implement a quality_score UDF that returns 1 if `event_type` in ('click', 'view', 'purchase') AND `event_time`…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Provide a Silver layer that preserves per-file provenance in AuditFiles via a lightweight UDF; handles nested JSON drift with a schema-evolution policy; applies tenant-scoped MERGE into Gold for CDC;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end data engineering capabilities: late data handling strategies, CDC implementation in Delta Lake, governance frameworks via Unity Catalog, and PII masking across organizational roles.\n\n### Key Concepts\n- MERGE CDC operations using composite keys for deduplication\n- 15-minute watermark configuration for handling late-arriving data\n- AuditDelta implementation for comprehensive file-level lineage\n- Unity Catalog RBAC with granular privilege management\n- Dynamic masking views for PII protection\n- Schema evolution handling and comprehensive testing strategies\n\n### Code Example\n```python\n# Dynamic masking helper for PII protection\ndef mask_pii_field(value, field_type):\n    if not value:\n        return value\n    if field_type == 'email' and '@' in value:\n        local, domain = value.split('@', 1)\n        return f\"{local[0]}***@{domain}\"\n    elif field_type == 'phone':\n        return value[:3] + '***' + value[-4:] if len(value) > 7 else '***'\n    return value\n```\n\n### Follow-up Questions\n- How would you handle schema drift in the clickstream events?\n- What monitoring alerts would you configure for pipeline health?\n- How do you optimize Delta Lake file sizing for 200GB/day workload?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-5",
        "questionId": "q-28330",
        "question": "In a Databricks Bronze-Silver-Gold pipeline ingesting 60 GB/day of multi-tenant Parquet/JSON logs from S3, design a Silver layer with a Slowly Changing Dimension Type 2 for a customer dimension, including surrogate keys, an Audit table for SCD history, and a lineage query exposing per-file provenance. Ensure late-arriving events are correctly applied via watermarking, provide a MERGE-based upsert into Gold, enforce Unity Catalog RBAC, and present a minimal test plan with synthetic late updates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Silver layer with an SCD Type 2 customer dimension table (customer_sk, customer_id, validity_start, validity_end, is_current) and a corresponding CustomerHistory audit table. Use MERGE to upsert from Bronze int…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Outline approach: per-tenant catalogs with Unity Catalog; a central feature registry with semantic versioning; offline features stored in Delta tables; an online cache backed by Redis for 200 ms reads",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Auto Loader with per-file provenance and an explicit Spark StructType to ingest Parquet logs from S3. Enforce the schema via explicit CAST operations and validation checks to prevent schema drift. Implement a determ…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a multi-tenant Bronze-Silver-Gold pipeline: Bronze layer ingests from two tenant-specific S3 folders using Auto Loader with cloudFiles; Silver layer enforces per-tenant isolation through Unity Catalog RBAC combin…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical multi-tenant data warehousing skills, including SCD2 history management, data provenance, late data handling, and governance in Databricks and Delta Lake.\n\n## Key Concepts\n- Slowly Changing Dimensions Type 2 with surrogate keys preserves historical customer records while tracking current state.\n- MERGE upserts with event-time watermarking ensure accurate processing of out-of-order and late-arriving data.\n- Per-file provenance and an AuditFiles lineage table enable end-to-end traceability of data assets.\n- Delta Lake history and file-level metadata support compliance and debugging workflows.\n- Unity Catalog RBAC enforces fine-grained data access controls across workspaces and catalogs.\n\n## Code Example\n```sql\n-- SCD Type 2 MERGE into Silver\nMERGE INTO silver.customer_dim AS s\nUSING (\n  SELECT c.*, sha2(concat(customer_id, effective_date), 256) AS customer_sk\n  FROM bronze.customer_staging c\n) AS src\nON s.customer_id = src.customer_id AND s.is_current = true\nWHEN MATCHED AND s.record_hash != src.record_hash THEN\n  UPDATE SET s.is_current = false, s.validity_end = src.effective_date\nWHEN NOT MATCHED THEN\n  INSERT (customer_sk, customer_id, record_hash, name, email, effective_date, validity_start, validity_end, is_current)\n  VALUES (src.customer_sk, src.customer_id, src.record_hash, src.name, src.email, src.effective_date, src.effective_date, null, true);\n\n-- Late-arrival handling via watermark\nCREATE OR REPLACE TEMP VIEW late_arrivals AS\nSELECT * FROM bronze.customer_staging\nWHERE ingestion_time > (SELECT MAX(validity_end) FROM silver.customer_dim WHERE customer_id = staging.customer_id);\n\n-- Audit trail for lineage\nINSERT INTO silver.audit_files (file_path, record_count, ingestion_time, source_system)\nSELECT input_file_name(), count(*), current_timestamp(), 's3_log_source'\nFROM bronze.customer_staging\nGROUP BY input_file_name();\n\n-- Gold layer MERGE upsert\nMERGE INTO gold.customer_master AS g\nUSING silver.customer_dim AS s\nON g.customer_sk = s.customer_sk AND s.is_current = true\nWHEN NOT MATCHED THEN INSERT *;\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-6",
        "questionId": "q-2199",
        "question": "How would you implement a compliant, zero-duplication deletion workflow in a Databricks streaming pipeline that ingests user events from Kafka into Delta Lake? A deletions log triggers row-level removals across Bronze/Silver, handling late data with tombstones, ensuring idempotent MERGE, and preserving an auditable deletion history via a separate Delta table. Describe architecture, data flow, and validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a deletion workflow with a DeletionLog stream; when a deletion request arrives, publish user_id and deletion_ts to a Delta table and a separate DeletionLog; on Bronze and Silver, use MERGE t",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement the Silver layer using Auto Loader with schema inference and explicit file provenance metadata. Enforce a permissive-but-validated evolution policy for schema drift, derive tenant-scoped features using Spark w…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a multi-tenant DLT: Bronze via Auto Loader per tenant; Silver with per-tenant masking and Unity Catalog RBAC; schema drift handled via evolving schemas in DLTPipeline expectations; CDC-like ME",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bronze: Auto Loader ingests S3 Parquet with tenant_id; Silver: CDC-like MERGE Bronze->Silver with 2-minute watermark; Gold: tenant-scoped aggregates. Enforce Unity Catalog RBAC on Silver and AuditDelt",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design compliant, end-to-end data deletion in a streaming Delta Lake pipeline. It covers change data capture semantics, idempotency, auditability, and cross-layer consistency (Bronze to Silver), which are critical at scale.\n\n## Key Concepts\n\n- Delta Lake MERGE for row-level deletions\n- Deletion log and tombstone handling\n- Idempotent operations and deleted_at auditing\n- Data lineage, governance, and RBAC in Unity Catalog\n\n## Code Example\n\n```javascript\n// Pseudo-Delta MERGE for deletion\nconst stmt = `\nMERGE INTO BronzeDelta AS b\nUSING DeletionLog AS d\nON b.user_id = d.user_id\nWHEN MATCHED THEN DELETE\n`;\ndb.run(stmt);\n```\n\n## Follow-up Questions\n\n- How would you test end-to-end deletion latency and ensure no residual copies in backups? \n- How would you scale the deletion workflow when deletions are frequent and from multiple sources?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-7",
        "questionId": "q-13043",
        "question": "In a Databricks pipeline ingesting 10 GB/day of JSON events from S3 into Bronze, how would you implement a testable QA process that verifies schema validation, data quality, and lineage using unit/integration tests (e.g., PyTest) with synthetic data, and integrate tests in CI with automatic rollback if tests fail?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Auto Loader to ingest 40 GB/day of Parquet logs from S3 into Bronze, then Silver with tenant_id/date partitioning. Track per-file provenance in AuditFiles(path, ingest_ts) and upsert to Gold via M",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Auto Loader to ingest Parquet files into Bronze, then implement a lightweight Delta-based SchemaVersions registry. On each file batch, read the file's schema and compare to the latest registered version. If new…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a test harness that injects synthetic JSON into a dedicated test S3 bucket, run a Spark job to read the data, validate the schema (StructType), enforce non-null keys, and assert expected row co",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Silver: AuditFiles per-file provenance; schema evolution to tolerate JSON drift; Delta CDF for tenant-scoped CDC. Gold: tenant_id/date with masked, RBAC-controlled views. Gate: data-quality checks blo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReasoning about testing data pipelines, CI/CD, rollback, and maintainability without affecting prod.\n\n## Key Concepts\n- Test harness, Spark validation, PyTest integration, synthetic data, lineage, masking, CI.\n\n## Code Example\n```javascript\n// Pseudo-test harness outline\nfunction validateRecordSchema(record) {\n  const required = ['id','ts','payload'];\n  for (const k of required) {\n    if (!(k in record) || record[k] == null) return false;\n  }\n  return true;\n}\n```\n\n## Follow-up Questions\n- How would you scale tests for higher throughput?\n- How do you handle late data in tests?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-8",
        "questionId": "q-10716",
        "question": "Scenario: Ingest 50 GB/day of Parquet web logs from S3 into Bronze, design a beginner Silver layer that achieves cost-aware partitioning (date, user_id), tracks per-file provenance in an AuditFiles Delta table, upserts Silver by event_id via MERGE, handles schema drift on evolving fields, enforces Unity Catalog RBAC for Silver reads, and includes a lightweight data-quality gate (missing required fields <= 0.1%). Outline concrete steps and tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by flattening nested Parquet fields from the S3 Bronze data, then sessionize events by user_id using a 30-minute inactivity window to generate unique session identifiers. Create session summaries containing sessio…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe a beginner DLT Silver design for 40 GB/day JSON logs from S3: (1) record per-file provenance in AuditFiles (source_path, ingest_ts), (2) upsert by event_id into Silver, (3) builds Gold with t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Auto Loader with per-file provenance and an explicit Spark StructType to ingest Parquet logs from S3. Enforce the schema via explicit CAST operations and validation checks to prevent schema drift. Implement a determ…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Strategy: Use Auto Loader to ingest 50 GB/day of Parquet web logs from S3 into Bronze with cost-aware partitioning by date and user_id. Implement Silver layer with MERGE-based CDC upserts on event_id, maintain AuditFile…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical skills in building a robust Data Lake Silver layer with lineage, governance, and data quality in a beginner scenario.\n\n## Key Concepts\n\n- Auto Loader with partitioning and schema drift handling\n- MERGE-based CDC by event_id\n- Per-file provenance via AuditFiles Delta table\n- Unity Catalog RBAC for dataset access\n- Lightweight data-quality gates and test plans\n\n## Code Example\n\n```javascript\n// Pseudo-code: MERGE-based CDC into Silver\nval sourceDF = spark.readStream.format('parquet').load('s3://bucket/logs/')\nval silver = spark.table('silver_table')\nsilver.alias('target').merge(\n  sourceDF.alias('source'),\n  'target.event_id = source.event_id'\n).whenMatched().updateAll()\n .whenNotMatched().insertAll()\n .execute()\n```\n\n## Implementation Steps\n\n1. **Bronze Ingestion**: Configure Auto Loader with cloudFiles.format('parquet') and partitionBy('date', 'user_id')\n2. **Silver Upserts**: Implement MERGE logic on event_id with change data capture\n3. **Provenance Tracking**: Create AuditFiles Delta table with file metadata\n4. **Schema Evolution**: Enable schema drift handling with mergeSchema option\n5. **Access Control**: Apply Unity Catalog privileges for Silver table access\n6. **Data Quality**: Add quality checks with acceptable thresholds (≤ 0.1% missing required fields)\n\n## Testing Strategy\n\n- Unit tests for MERGE logic and schema evolution\n- Integration tests for end-to-end data flow\n- Quality gate validation with test datasets\n- Access control verification through RBAC testing",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-9",
        "questionId": "q-19817",
        "question": "Design a Databricks data pipeline for 200 GB/day consisting of JSON and Parquet logs arriving in S3 across four business units. Build a Silver layer that (1) captures per-file provenance (source_path, file_timestamp, ingest_ts) without heavy UDFs, (2) handles JSON schema drift via a registry-driven evolution policy, (3) propagates changes into a multi-tenant Gold table using MERGE-based CDC, (4) publishes the Gold table via Delta Sharing with region-scoped shares and masking policies, (5) enforces a data-quality gate and a late-data watermark, (6) ensures fault tolerance with Auto Loader checkpoints, and (7) includes a test plan with synthetic late and partial files. Explain trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a Silver layer that records per-file provenance in an AuditFiles table (source_path, file_timestamp, ingest_ts) through a lightweight metadata capture, uses a Schema Registry to evolve nested",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Auto Loader to ingest 8-12 TB/day JSON into Bronze; Silver deduplicates on event_id with MERGE CDC, and uses a 2-minute watermark. Mask SSN at query time via a masking VIEW over Silver. Enforce pe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest 200 GB/day from a MongoDB CDC feed (Debezium) into Delta Lake via Databricks. Build Silver to preserve per-tenant provenance (source_offset, collection, ingest_ts), support schema drift with ev",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument per-batch metrics: total events, duplicates, nulls, late data, latency; use Delta table stats for schema drift; apply KS-test on key fields vs baseline; publish to a central Databricks SQL",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDesigning a region-aware, governance-conscious Databricks pipeline tests practical skills in data provenance, schema evolution, and multi-tenant sharing. It also covers fault tolerance, late data handling, and test planning.\n\n## Key Concepts\n- Delta Lake, Delta CDF, MERGE, Auto Loader\n- Unity Catalog masking and RBAC; Delta Sharing region-scoped exports\n- Schema Registry for evolving JSON\n- Data quality gates and watermarking\n- AuditFiles-like provenance\n\n## Code Example\n```javascript\n// Pseudo Spark example: merge into Gold from Silver CDC\nGold.merge(silverCDC, on='tenant_id,date')\n```\n\n## Follow-up Questions\n- How would you test region-scoped shares and masking policies?\n- How would you handle extreme schema drift, large nested arrays, or binary payloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-10",
        "questionId": "q-11360",
        "question": "In Databricks, design a Bronze-Silver-Gold pipeline that ingests streaming clickstream from Kafka and batch logs from S3 into Delta Lake. Silver must preserve per-event provenance (source_topic, source_path, ingest_ts, event_ts), handle schema drift with Delta Lake schema evolution, and upsert into a deduplicated Silver table using MERGE (primary key event_id). Produce a time-windowed Gold table for dashboards, enforce Unity Catalog RBAC and data masking, and implement a data-quality gate that blocks downstream if duplicate event_id or null event_ts exceeds threshold. Include testing with synthetic late data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive Databricks Delta Live Tables ML feature store pipeline with intelligent data processing layers. The Silver layer maintains granular feature provenance tracking (name, version, source_path, ingest_…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Delta Live Tables/Auto Loader pipeline: Bronze ingests Kafka and S3, Silver preserves per-event provenance (source_topic, source_path, ingest_ts, event_ts) and evolves schema with Delta schema e",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Auto Loader to ingest 40 GB/day of Parquet logs from S3 into Bronze, then Silver with tenant_id/date partitioning. Track per-file provenance in AuditFiles(path, ingest_ts) and upsert to Gold via M",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition Silver by tenant_id; enforce Unity Catalog RBAC so tenants read only their data; apply a data masking policy (email, phone) at query time; implement CDC via MERGE into Silver on event_id; en",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end readiness for multi-source ingestion, provenance, schema drift handling, and secure access.\n\n## Key Concepts\n\n- Delta Lake schema evolution, MERGE upserts, CDC-like logic, late data handling\n- Unity Catalog RBAC and masking, data quality gates\n- Multi-source orchestration via Delta Live Tables/Auto Loader\n\n## Code Example\n\n```python\nspark.sql(\"\"\"\nMERGE INTO silver.user_events AS t\nUSING staged AS s\nON t.event_id = s.event_id\nWHEN MATCHED THEN UPDATE SET *\nWHEN NOT MATCHED THEN INSERT *\n\"\"\")\n```\n\n## Follow-up Questions\n\n- How would you test late data and deduplication at scale?\n- What partitioning strategy balances cost and latency for the Gold table?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-11",
        "questionId": "q-29758",
        "question": "In a multi-tenant Databricks Delta Live Tables pipeline ingesting ~40 GB/day of Parquet and JSON logs from S3, design a Silver layer that supports schema drift with per-tenant evolution, preserves per-file provenance and tenant isolation, and streams into a Gold table with a slowly changing dimension. Include Unity Catalog RBAC with masking, a data-quality gate that blocks downstream on failure, late-data handling, and a concise test plan with synthetic drift and late files. Be specific about the steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: load with Auto Loader using file provenance columns populated by a lightweight UDF; implement a Silver delta table with dedup by event_id and a 10-minute watermark for late data; cr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a cross-cloud Delta Live Tables plan: ingest 60 GB/day into AWS S3 Bronze; design Silver with per-tenant provenance and CDC via Delta CDF; publish Gold via Delta Sharing to Google Cloud; enfor",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end Databricks pipeline to ingest a daily JSON feed from an on-prem FTP drop into Delta Lake. Use Auto Loader with schema evolution for changing fields (user_id, event_type, event_ts, ip_address, device…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant drift policy in DLT: enable schema drift evolution, store per-file provenance in AuditFiles, and write to a tenant-scoped Silver. Use a streaming MERGE into a Gold SCD-2 table.",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a scalable, tenant-aware Silver-Gold workflow with schema drift, provenance, RBAC masking, and data-quality governance. It also probes testing rigor with drift and late data.\n\n## Key Concepts\n- Drift-aware schema evolution in Delta Live Tables\n- Per-tenant provenance and isolation\n- SCD-2 merge into Gold table\n- Unity Catalog masking and RBAC\n- Data-quality gates and late-data handling\n- Synthetic data drift tests\n\n## Code Example\n```python\nimport dlt\n@dlt.table\ndef silver():\n  bronze = dlt.read('bronze')\n  # apply per-tenant evolution rules here\n  silver_tbl = bronze\n  return silver_tbl\n```\n\n## Follow-up Questions\n- How would you monitor drift rates per tenant and auto-tune thresholds?\n- What failure modes trigger reprocessing and how would you alert on them?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-12",
        "questionId": "q-8771",
        "question": "Scenario: A Databricks pipeline ingests 6 GB/day of Parquet sensor logs from S3 into Bronze. Design a beginner Silver layer that (a) normalizes temperature readings using a lightweight UDF to convert Fahrenheit to Celsius, (b) enriches with a Dimension table (sensor metadata) via a simple join, (c) adds per-file provenance via a lightweight UDF, (d) exposes a masked view for PII fields, (e) enforces Unity Catalog RBAC so Silver readers see only the enriched silver dataset and the masked view, and (f) implements basic data quality: non-null timestamp and event_id unique per hour. Outline concrete steps including Autoloader, Delta tables, and a synthetic-data test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Fahrenheit-to-Celsius UDF; join Bronze with a sensor_dim for enrichment; add per-file provenance via a lightweight UDF capturing source_path and file_timestamp; create a masked view maskin",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement Silver with per-tenant pipelines in Delta Live Tables, preserving per-file provenance in AuditFiles; use a central Schema Registry for evolution; apply CDC via MERGE into Silver filtered by",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Delta Live Tables pipeline that establishes per-tenant namespaces within Unity Catalog, applies RBAC controls and column-level masking for data isolation, utilizes a Silver layer that accommodates schema evo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Silver layer that ingests 12 GB/day of JSON web-logs from S3 with Auto Loader. Enforce per-file provenance, flatten nested fields (user.id, session.id, event.time), and require non-null key f",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to implement a practical Silver layer with transformations, enrichment, governance, and a test plan in Databricks, using a small set of focused techniques.\n\n## Key Concepts\n- Lightweight UDFs for unit conversion and per-file provenance\n- Bronze-to-Silver pattern with schema normalization and simple joins\n- Data masking and Unity Catalog RBAC for restricted access\n- Basic data quality checks (non-null fields, per-hour uniqueness)\n\n## Code Example\n```python\ndef fahrenheit_to_celsius(f):\n    try:\n        return (f - 32.0) * 5.0/9.0\n    except Exception:\n        return None\n```\n\n## Follow-up Questions\n- How would you validate UDF correctness with edge cases like nulls and extreme temps?\n- How would you backfill provenance for historical files and ensure lineage is complete?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-13",
        "questionId": "q-25018",
        "question": "In a Databricks pipeline ingesting 60 GB/day of multi-tenant JSON logs from S3, design an incremental Silver layer that serves a versioned ML feature_store Delta table for downstream training. Requirements: per-file provenance in an AuditFiles table; handle schema drift with evolution policy; implement CDC-like upserts into feature_store using MERGE; isolate tenants with Unity Catalog RBAC and masking on features; enforce a data-quality gate and late-data watermark; provide a test harness with synthetic late data and schema drift?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deduplicate with composite key (user_id, event_ts) in Silver using MERGE to upsert latest per key, applying a 3-minute watermark for late data. Bronze ingests from S3 Parquet and REST JSON. Gold store",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Architecture Overview Build a multi-tenant DLT pipeline with strict tenant isolation at the Bronze layer, per-tenant CDC in Silver, and shared or tenant-specific Gold tables. ## 1. Bronze Layer — Per-Tenant Ingestion…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a multi-tenant Delta Live Tables pipeline: ingest from S3 Auto Loader and Kafka; Bronze raw with tenant_id, ts, event_id; Silver dedups by event_id via MERGE CDC and masks PII per tenant; Gold",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a Silver layer that materializes a versioned feature_store Delta table per tenant. Ingest JSON via Auto Loader with schema inference, and maintain an AuditFiles table for per-file provenance. Use Delta schema e…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design an ML feature store pipeline with full provenance, drift handling, CDC via MERGE, and secure multi-tenant data sharing. It goes beyond dashboard-focused queries by focusing on training-ready features and enterprise-grade governance.\n\n## Key Concepts\n- Delta Lake schema evolution and per-file provenance tracking\n- CDC via MERGE into a versioned feature_store for incremental updates\n- Tenant isolation with Unity Catalog RBAC and dynamic column masking\n- Delta Sharing for secure ML training data exposure\n- Late-data watermarking with allowed lateness thresholds\n- Data-quality gates using DLT expectations\n- End-to-end testing with synthetic late data and schema drift scenarios\n\n## Code Example\n```python\n# DLT Pipeline: Silver Layer with CDC and Quality Gates\nimport dlt\nfrom pyspark.sql.functions import col, when, current_timestamp\n\n# Configure Auto Loader for incremental JSON ingestion\n@dlt.table(name=\"raw_logs\")\ndef raw_logs():\n    return (\n        spark.readStream.format(\"cloudFiles\")\n        .option(\"cloudFiles.format\", \"json\")\n        .option(\"cloudFiles.schemaLocation\", \"/mnt/schemas/raw_logs\")\n        .load(\"s3://raw-logs/\")\n    )\n\n# Per-file provenance tracking\n@dlt.table(name=\"audit_files\")\ndef audit_files():\n    return (\n        spark.readStream.format(\"cloudFiles\")\n        .option(\"cloudFiles.format\", \"json\")\n        .load(\"s3://raw-logs/\")\n        .select(\n            col(\"_metadata\").getItem(\"file_name\").alias(\"file_name\"),\n            col(\"_metadata\").getItem(\"file_path\").alias(\"file_path\"),\n            col(\"_metadata\").getItem(\"file_modification_time\").alias(\"modification_time\"),\n            current_timestamp().alias(\"ingested_at\")\n        )\n    )\n\n# Silver layer with schema evolution and quality gates\n@dlt.table(\n    name=\"silver_logs\",\n    comment=\"Silver layer with quality gates and schema evolution\"\n)\n@dlt.expect_or_drop(\"valid_schema\", \"_rescued_data IS NULL\")\n@dlt.expect(\"valid_tenant_id\", \"tenant_id IS NOT NULL\")\ndef silver_logs():\n    return (\n        dlt.readStream(\"raw_logs\")\n        .withWatermark(\"event_time\", \"48 hours\")\n    )\n\n# Feature store with CDC-like MERGE and versioning\nfeature_store_table = \"catalog.ml.feature_store\"\n\ndef upsert_features(batch_df, batch_id):\n    batch_df.createOrReplaceTempView(\"batch_features\")\n    \n    spark.sql(f\"\"\"\n        MERGE INTO {feature_store_table} AS target\n        USING batch_features AS source\n        ON target.tenant_id = source.tenant_id \n           AND target.feature_key = source.feature_key\n        WHEN MATCHED THEN UPDATE SET *\n        WHEN NOT MATCHED THEN INSERT *\n    \"\"\")\n\n# Write with late-data watermark handling\n(batch_df.writeStream\n    .option(\"mergeSchema\", \"true\")\n    .option(\"checkpointLocation\", \"/mnt/checkpoints/features\")\n    .foreachBatch(upsert_features)\n    .trigger(availableNow=True)\n    .start())\n\n# Tenant isolation with RBAC and masking\n# Unity Catalog SECURE: Apply row-level and column masking\nspark.sql(f\"\"\"\n    CREATE OR REPLACE MASKING POLICY tenant_mask AS\n    CASE WHEN CURRENT_USER() IN (\n        SELECT member FROM catalog.db.grant_access_tenants\n        WHERE tenant_id = tenant_id\n    ) THEN value\n    ELSE '***REDACTED***' END\n\"\"\")\n\n# Test harness for synthetic late data and schema drift\ndef generate_synthetic_drift_test():\n    # Generate base dataset\n    base_data = [\n        {\"tenant_id\": \"t1\", \"event_time\": \"2024-01-01\", \"feature_a\": 1.0},\n        {\"tenant_id\": \"t2\", \"event_time\": \"2024-01-01\", \"feature_a\": 2.0}\n    ]\n    \n    # Inject late data (beyond watermark)\n    late_data = [\n        {\"tenant_id\": \"t1\", \"event_time\": \"2023-12-28\", \"feature_a\": 0.5}\n    ]\n    \n    # Inject schema drift (new column)\n    drift_data = [\n        {\"tenant_id\": \"t1\", \"event_time\": \"2024-01-02\", \"feature_a\": 3.0, \"new_feature\": \"drifted\"}\n    ]\n    \n    # Write to test input path\n    for batch in [base_data, late_data, drift_data]:\n        write_test_data(batch)\n        time.sleep(1)\n\n# Validate governance and audit\ndef validate_tenant_isolation():\n    result = spark.sql(\"SELECT * FROM catalog.ml.feature_store WHERE tenant_id = 't1'\")\n    assert result.count() == expected_t1_count\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-14",
        "questionId": "q-11894",
        "question": "In a Databricks pipeline ingesting 60 GB/day of Parquet logs from S3 for multiple tenants, design a Silver layer to export data via Delta Sharing. What concrete steps would you take to implement per-file provenance, schema drift evolution, CDC-like MERGE, a BI-ready Gold table, Unity Catalog RBAC with masking, policy-based Delta Sharing exports, a data-quality gate, and late-data handling with tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Bronze layer using Auto Loader to ingest the 40 GB/day of multi-tenant Parquet logs from S3. Create a Delta lineage table with columns (source_path, ingest_ts, file_size, status) to capture per-file provenan…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest 200 GB/day from a MongoDB CDC feed (Debezium) into Delta Lake via Databricks. Build Silver to preserve per-tenant provenance (source_offset, collection, ingest_ts), support schema drift with ev",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Silver layer that implements per-file provenance using a lightweight UDF to emit audit metadata (source_path, file_timestamp, ingest_ts) into an AuditFiles table with an exposed view; handle schema drift throug…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design uses Auto Loader for S3 and Kafka in Bronze, Silver performs CDC MERGE by event_id, Gold is the consumable layer. Add a drift-detection pass that scores per-field distribution changes and quara",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses your ability to design end-to-end data architecture that balances multi-tenant governance requirements with data sharing capabilities and fault tolerance in Databricks environments.\n\n## Key Concepts\n- Delta Sharing for secure data distribution\n- Unity Catalog RBAC and column masking for access control\n- Silver/Gold layer architecture patterns\n- Change Data Capture implementation via MERGE operations\n- Per-file provenance tracking and comprehensive audit trails\n- Schema drift management with evolution rules and validation\n- Late data handling strategies using watermarks and retention policies\n- Data quality gates with automated testing frameworks\n\n## Code Example\n```python\n# Provenance UDF implementation\nfrom pyspark.sql.functions import udf\nfrom pyspark.sql.types import StructType, StructField, StringType, TimestampType\n\n@udf(returnType=StructType([\n    StructField('source_path', StringType(), False),\n    StructField('file_timestamp', TimestampType(), False),\n    StructField('ingest_ts', TimestampType(), False)\n]))\ndef extract_provenance(file_metadata):\n    return parse_file_metadata(file_metadata)\n```\n\nThis approach demonstrates comprehensive understanding of enterprise data architecture patterns while addressing operational concerns around governance, quality, and reliability.",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-15",
        "questionId": "q-5651",
        "question": "In a Databricks pipeline consuming 10 TB/day of JSON logs from S3 via Auto Loader, design a per-tenant lineage graph: Bronze (raw), Silver (tenant_id dedupe by event_id using MERGE CDC), Gold (feature-ready). Tenants may evolve schemas independently. How would you implement per-tenant schema evolution, Unity Catalog RBAC, and a drift/test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deliver a per-tenant data graph: Bronze ingests S3 JSON logs, Silver partitions by tenant_id and deduplicates by event_id with MERGE CDC, and Gold yields feature-ready tables per tenant. Use Delta Lak",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a multi-tenant DLT: Bronze via Auto Loader per tenant; Silver with per-tenant masking and Unity Catalog RBAC; schema drift handled via evolving schemas in DLTPipeline expectations; CDC-like ME",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Silver layer using Delta Live Tables that preserves per-file provenance (source_path, file_timestamp, ingest_ts) and exposes a provenance view. Handle per-tenant schema drift through a central schema registr…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed approach: Use a DLT pipeline with Bronze ingest from JSON, then Silver derived from Bronze. Deduplicate on event_id, keeping the latest by ingest_time. Join to users_dim on user_id to validat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design tenant-isolated pipelines with schema drift handling and governance.\n\n## Key Concepts\n- Delta Lake 2.0 schema evolution\n- MERGE CDC and per-tenant isolation\n- Unity Catalog RBAC and tenancy\n- Drift testing with synthetic tenants\n\n## Code Example\n```javascript\n// Pseudo-logic showing a per-tenant Silver view creation\nCREATE OR REPLACE VIEW silver_tenant AS SELECT * FROM silver WHERE tenant_id = :tenant\n```\n\n## Follow-up Questions\n- How would you monitor cross-tenant data drift and auto-generate test cases?\n- How does Unity Catalog RBAC scale with N tenants, and what automation would you implement to provision policies?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-16",
        "questionId": "q-23486",
        "question": "In a Databricks Delta Live Tables pipeline ingesting 100 GB/day of JSON events from multi-tenant S3 folders, design a Silver layer that (1) preserves per-tenant provenance and file metadata, (2) handles schema drift for nested JSON with a defined evolution policy, (3) performs CDC-like MERGE into a tenant-scoped Gold table with deterministic keys, (4) enforces Unity Catalog RBAC with masking, (5) adds a data-quality gate and a late-data watermark, (6) ensures fault-tolerant checkpointing and retries; outline tests with synthetic late data and tenant churn?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Auto Loader to land JSON into Bronze Delta, flatten into Silver with a date partition, enable mergeSchema for drift, and apply a lightweight quality guard for non-null fields and a unique key. Imp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Delta Live Tables with a Silver table ingested via Auto Loader, preserving provenance in an AuditFiles table and applying a JSON schema evolution policy. Implement tenant-scoped CDC with MERGE int",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a DLT Silver pipeline that stores per-tenant provenance (source_path, ingest_ts, tenant_id), leverages Change Data Feed for CDC, applies a schema evolution policy for nested JSON using STRICT mode or ADD_COLUMNS…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a backfill-ready lineage: process 100 GB/day JSON/Parquet across 250 tenants. Store per-file provenance in AuditFiles; generate a fingerprint (schema + counts) to validate replays. Backfill",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design a robust, multi-tenant Delta Live Tables solution with real-time CDC, schema drift handling, provenance tracking, RBAC masking, and late-data resilience.\n\n## Key Concepts\n- Delta Live Tables CDC and MERGE semantics with deterministic keys\n- Schema drift handling for nested JSON using evolution policies\n- Unity Catalog RBAC and column-level masking for tenant isolation\n- Per-tenant provenance and audit trails for compliance\n- Late-data watermarking and fault-tolerant checkpointing with retries\n\n## Code Example\n```python\n# Silver layer configuration\n@dlt.table(\n    name=\"silver_events\",\n    comment=\"Silver layer with per-tenant provenance and schema evolution\"\n)\ndef silver_events():\n    return (\n        dlt.read_stream(\"bronze_events\")\n        .with_column(\"tenant_id\", col(\"source_path\").split(\"/\")[1])\n        .with_column(\"ingest_ts\", current_timestamp())\n        .withSchemaEvolutionPolicy(\"STRICT\")  # or \"ADD_COLUMNS\"\n    )\n\n# Gold layer with deterministic MERGE\n@dlt.table(\n    name=\"gold_events\",\n    comment=\"Gold layer with tenant-scoped partitioning\"\n)\ndef gold_events():\n    stream = dlt.read_stream(\"silver_events\")\n    dlt.create_streaming_live_table(\"gold_events\")\n    \n    dlt.applyChanges(\n        target=\"gold_events\",\n        source=\"silver_events\",\n        keys=[\"tenant_id\", \"event_id\"],\n        sequenceBy=\"ingest_ts\"\n    )\n```",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-17",
        "questionId": "q-7876",
        "question": "Design a DR strategy for a multi-region SaaS data platform (Airbnb/Stripe-scale) using Databricks Bronze-Silver-Gold with Delta Live Tables. Outline cross-region replication from Region A to Region B, per-tenant isolation via Unity Catalog RBAC, a global truth table for late-arriving data, and CDC-style reconciliation. Include testing with synthetic tenants and how to verify lineage during failover?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a two-layer model: Silver stores per-file provenance (AuditFiles), evolves JSON schema with a versioned EvolutionPolicy, and CDC-like MERGE into TenantEvents. Gold computes tenant-scoped aggreg",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an idempotent re-ingest: derive source_file_version from file_timestamp and a content hash; track provenance in AuditFiles (tenant_id, source_path, file_timestamp, source_file_version). Use ver",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt active DR with Region A as primary and Region B as secondary. Mirror Bronze in Region B using cross-region Auto Loader and a DR-enabled DLT pipeline; enforce per-tenant isolation via Unity Catal",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a Delta Live Tables Silver layer that ingests 100 GB/day of S3 Parquet per tenant via Auto Loader, attaches per-tenant provenance in an AuditFiles table, enforces a controlled schema-evolution",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes DR design, cross-region data consistency, tenant isolation, and lineage verification in a Databricks lakehouse.\n\n## Key Concepts\n- Cross-region replication with Auto Loader and DLT\n- Unity Catalog RBAC for per-tenant isolation\n- Global truth table for late-arriving data\n- CDC-style reconciliation with MERGE\n- Lineage verification and testing\n\n## Code Example\n```python\n# Pseudo-Delta Live Tables setup for DR\nimport dlt\n@dlt.table\ndef bronze_region_a():\n    ...\n\n@dlt.table\ndef bronze_region_b():\n    ...\n```\n\n## Follow-up Questions\n- How would you monitor replication latency and alerting?\n- How to test end-to-end failover with synthetic tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-18",
        "questionId": "q-25143",
        "question": "Ingest 15–20 GB/day of multi-tenant JSON logs from S3. Design a beginner Silver layer that (a) flattens nested JSON to a stable schema, (b) partitions Silver by tenant_id/date for fast dashboards, (c) performs a deterministic upsert into a per-tenant Gold table, (d) enriches with a Profile Delta table, and (e) applies per-tenant masking via Unity Catalog. Provide a concise test plan including late-data handling and synthetic skew?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a lineage-aware Silver layer that captures per-file provenance in an audit_files table (source_path, file_timestamp, ingest_ts, tenant_id, file_hash) with a provenance view for end-to-end tracking. Implement a sc…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Auto Loader for JSON landing, flatten to a stable schema, partition Silver by tenant_id/date, MERGE on (tenant_id, event_id) into Gold, enrich Silver with a Profile Delta table, and apply Uni",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Process overview: Ingest raw data via Auto Loader into Bronze layer; Silver layer enriches data with per-file provenance (source_path, file_timestamp, ingest_ts) using a lightweight UDF and maintains this information in…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an idempotent re-ingest: derive source_file_version from file_timestamp and a content hash; track provenance in AuditFiles (tenant_id, source_path, file_timestamp, source_file_version). Use ver",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design a beginner-friendly multi-tenant data path from landing to curated Gold with deterministic upserts, simple enrichment, and governance.\n\n## Key Concepts\n\n- Auto Loader for JSON landing\n- Flatten nested JSON to stable schema\n- Tenant/date partitioning for performance\n- MERGE-based upsert into per-tenant Gold\n- Lightweight enrichment join with Profile Delta table\n- Unity Catalog masking and RBAC\n- Late-data handling test plan\n\n## Code Example\n\n```javascript\n-- Example Spark SQL MERGE (Delta)\nMERGE INTO gold.events AS t\nUSING silver.events AS s\nON t.tenant_id = s.tenant_id AND t.event_id = s.event_id\nWHEN MATCHED THEN UPDATE SET t = s\nWHEN NOT MATCHED THEN INSERT ROW WITH s.*;\n```\n\n## Follow-up Questions\n\n- How would you test for schema drift and add a simple schema evolution strategy?\n- How would you validate tenant isolation in dashboards with RBAC?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-19",
        "questionId": "q-23880",
        "question": "In a Databricks platform ingesting 200 GB/day of regional mobile telemetry split across 4 regions and 120 apps, design a cross-tenant data governance pattern with a Silver layer that guarantees tenant isolation, a real-time per-tenant Gold layer, and Delta Sharing exports. Include a plan for per-tenant partitioning, schema evolution, CDC-like updates, masking via Unity Catalog, data-quality gates, late-data handling, testing, and a comparison of Delta Live Tables vs Structured Streaming for these layers?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a 5 TB/day multi-tenant Kafka JSON DLTS design: Silver preserves per-record provenance (topic, partition, offset, ingest_ts) in AuditFiles via a lightweight UDF; Delta CDF enables per-tenant C",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a tenant-aware data plane: Bronze layer uses Auto Loader to ingest JSON telemetry from regional S3 buckets with automatic schema inference. Silver layer implements tenant isolation through Delta tables partiti…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "DLT Bronze from S3 JSON to Bronze_raw; Silver normalizes timestamps to UTC, flattens nested fields, and enforces non-null customer_id and session_id; Gold outputs per-event rows with a deterministic k",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Silver layer that partitions by tenant_id with per-tenant data masking, implements CDC-like MERGE operations using event_id as the natural key, applies schema evolution rules for drift handling, incorporates ev…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates hands-on expertise in multi-tenant data governance across Bronze-Silver-Gold architectures with real-time requirements and secure data sharing—key production concerns for enterprise data platforms.\n\n## Key Concepts\n- Tenant isolation via partitioning and RBAC\n- Unity Catalog for fine-grained access control and column masking\n- Delta Lake schema evolution and constraints\n- CDC semantics with merge operations\n- Delta Sharing for secure cross-tenant exports\n- Late-data handling through watermarking\n- Data quality gates with validation and quarantine\n- Delta Live Tables vs Structured Streaming trade-offs\n\n## Code Example\n```python\n# Per-tenant Silver layer with partitioning and schema evolution\nsilver = (\n  spark.readStream.format(\"cloudFiles\")\n    .option(\"cloudFiles.format\", \"json\")\n    .option(\"cloudFiles.schemaHints\", \"tenant_id STRING, region STRING, timestamp STRING, payload STRING\")\n    .load(\"s3://region-*/tenant-*/\")\n    .withColumn(\"ingest_ts\", current_timestamp())\n    .withColumn(\"tenant_id\", col(\"metadata.tenant_id\"))\n    .withColumn(\"region\", col(\"metadata.region\"))\n)\n\n(silver.writeStream\n  .partitionBy(\"tenant_id\", \"region\")\n  .option(\"mergeSchema\", \"true\")\n  .option(\"checkpointLocation\", \"/mnt/checkpoints/silver\")\n  .trigger(availableNow=True)\n  .toTable(\"catalog.silver.telemetry\"))\n\n# CDC-like merge for Silver to Gold\nfrom delta.tables import DeltaTable\n\nsilver_table = DeltaTable.forName(spark, \"catalog.silver.telemetry\")\ngold_table = DeltaTable.forName(spark, \"catalog.gold.telemetry_by_tenant\")\n\n(gold_table.alias(\"target\")\n  .merge(silver_table.alias(\"source\"), \"target.tenant_id = source.tenant_id AND target.event_id = source.event_id\")\n  .whenMatchedUpdate(set={\n    \"payload\": \"source.payload\",\n    \"updated_at\": \"source.ingest_ts\",\n    \"_cdc_operation\": \"U\"\n  })\n  .whenNotMatchedInsert(values={\n    \"tenant_id\": \"source.tenant_id\",\n    \"event_id\": \"source.event_id\",\n    \"payload\": \"source.payload\",\n    \"created_at\": \"source.ingest_ts\",\n    \"_cdc_operation\": \"I\"\n  })\n  .execute())\n\n# Unity Catalog masking policy\nfrom pyspark.sql.functions import udf\n\n@udf\ndef mask_pii(ssn: str) -> str:\n    return \"***-**-\" + ssn[-4:] if ssn else None\n\nspark.sql(\"\"\"\nCREATE OR REPLACE MASKING POLICY catalog.security.mask_ssn\nAS (ssn STRING) RETURN\n  CASE WHEN CURRENT_USER() = 'admin' THEN ssn\n  ELSE mask_pii(ssn)\n  END\n\"\"\")\n```",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-20",
        "questionId": "q-28684",
        "question": "In a Databricks pipeline ingesting 40 GB/day of Parquet logs from S3 for multiple tenants, design a beginner Silver layer that (1) uses a record_hash to deduplicate and upsert into a single Gold table, (2) captures per-file provenance in an AuditFiles Delta table, and (3) implements a simple data-quality gate that flags out-of-range event_value (0 to 1,000,000) and quarantines offending records. Outline concrete steps, a minimal test plan using synthetic late-arriving files, and how you would verify end-to-end integrity?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Unity Catalog Dynamic Views to enforce per-tenant access. Bronze→Silver deduplication by event_id with MERGE CDC; create Silver_Tenant as a dynamic view filtered by tenant_id using current_user(). Gold layer ag…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Auto Loader with schema evolution, flatten nested fields, and a MERGE for idempotent upserts. ReadStream via cloudFiles, inferColumnTypes true, mergeSchema true. Flatten: user_id = user.id, email",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bi-temporal SCD2 model: customer_dim with columns customer_id, name, status, valid_from, valid_to; page_views as facts; silver layer updates via MERGE CDC from bronze. Maintain system_time_as_of",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Auto Loader with a deterministic record_hash = md5(concat(tenant_id, event_id, timestamp)). Upsert Silver into Gold via MERGE on record_hash. Maintain AuditFiles with path, ingest_ts, file_size. A",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nEvaluates practical data deduplication, provenance tracking, and basic data quality gating in a multi-tenant Databricks pipeline. Tests ensure idempotent upserts and late-arriving data handling.\n\n## Key Concepts\n\n- Idempotent upserts with Delta Lake MERGE\n- Per-file provenance auditing\n- Basic data quality gates and quarantine\n\n## Code Example\n\n```sql\n-- Example MERGE skeleton (pseudo)\nMERGE INTO gold AS g\nUSING silver AS s\nON g.record_hash = s.record_hash\nWHEN MATCHED THEN UPDATE SET ...\nWHEN NOT MATCHED THEN INSERT ...;\n```\n\n## Follow-up Questions\n\n- How would you scale the deduplication key if a single event can appear in multiple files? \n- How would you test the quarantine workflow under concurrent file arrivals?",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-21",
        "questionId": "q-29009",
        "question": "In a Databricks pipeline ingesting 100 GB/day Parquet logs from S3 and 200 GB/day JSON events from Kafka across 3 tenants, design a Silver layer that unifies streams, preserves per-record provenance, handles schema drift with evolution, performs deterministic MERGE upserts into a tenant-scoped Gold table, enforces Unity Catalog masking/RLS, and supports late data with watermarking; outline concrete steps and a minimal test strategy with synthetic late data and out-of-order events?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Auto Loader with per-file provenance written to an AuditFiles table; JSON drift handled via a schema-on-read to a dynamic union schema and Delta Lake evolution; implement CDC-like MERGE into a ten",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Delta Live Tables pipeline with Bronze (raw Parquet from S3) → Silver (per-tenant isolation partitioned by tenant_id/date) → Gold (BI-ready). Enforce schema via DLTable expectations, use a wa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use streaming Auto Loader to ingest JSON and Parquet from S3 into a Delta Silver table, preserving provenance via source_path, file_timestamp, and ingest_ts extracted from input_file_name() and current_timestamp(). Enab…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Silver layer that unifies Parquet (S3) and Kafka JSON streams for 3 tenants, preserving per-record provenance, supporting schema drift via Delta schema evolution, and performing deterministic",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nFocuses on multi-source unification, per-record lineage, and governance in a multi-tenant Databricks pipeline, with practical test coverage for drift and late data.\n\n## Key Concepts\n\n- Multi-source Silver design unifying Parquet and Kafka streams\n- Per-record provenance audit table (source, ingest_ts, event_ts, id)\n- Delta schema evolution to tolerate drift\n- Tenant-scoped Gold with deterministic MERGE upserts\n- Unity Catalog masking/RLS for data access control\n- Late-data handling with watermarking and monitoring\n- Lightweight test strategy using synthetic late and out-of-order data\n\n## Code Example\n\n```sql\n-- Pseudocode: merge Silver into Gold by tenant and id\nMERGE INTO gold g\nUSING silver s\nON g.tenant_id = s.tenant_id AND g.id = s.id\nWHEN MATCHED THEN UPDATE SET g.* = s.*\nWHEN NOT MATCHED THEN INSERT (columns...) VALUES (s.*);\n```\n\n## Follow-up Questions\n\n- How would you verify lineage correctness across reprocessing?\n- How would you adjust for changing tenant schemas without downtime?",
        "difficulty": "advanced"
      },
      {
        "id": "databricks-data-engineer-q-22",
        "questionId": "q-10273",
        "question": "Ingest 8 GB/day of Parquet web logs from S3 into Bronze; design a beginner Silver layer that (1) partitions Silver by region/date and maintains per-minute region counts in a RegionMinuteAgg table, (2) upserts events into Silver by event_id via MERGE, (3) exposes a region-scoped masking view that redacts IPs, (4) enforces Unity Catalog RBAC for region-scoped reads, (5) validates data quality: non-null event_id, region in allowed list, event_time within 30 days, (6) tests with synthetic data across regions including late arrivals and a partially written batch, (7) outline config for Auto Loader and a lightweight test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Delta Live Tables pipeline with Bronze (raw Parquet from S3) → Silver (per-tenant isolation partitioned by tenant_id/date) → Gold (BI-ready). Enforce schema via DLTable expectations, use a wa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement Silver with an AuditFiles table to capture per-file provenance, a schema-evolution policy for nested JSON, CDC via Delta CDF with MERGE into a tenant_id/date indexed Gold table, and a Delta",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement Auto Loader per tenant to ingest data into Bronze tables, then apply SCD Type 2 logic in Silver to create a cross-tenant conformed_dim table containing tenant_id, surrogate_key, effective_start_date, and end_d…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Silver layer partitioned by region and date while maintaining per-minute region counts in a RegionMinuteAgg table; implement event_id-based upserts from Bronze using MERGE operations; create a region-scoped mas…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical mastery of fundamental Databricks patterns including partitioned Silver tables, MERGE-based upserts, masking views, and Unity Catalog RBAC within a beginner-friendly context. It demonstrates understanding of a complete data pipeline with regional governance, late-arrival handling, and comprehensive quality validation.\n\n## Key Concepts\n\n- Delta Lake partitioning on region/date for query pruning\n- MERGE operations for event_id-based upserts\n- Masking views for PII/IP address redaction\n- Unity Catalog RBAC enforcement at table and view levels",
        "difficulty": "beginner"
      },
      {
        "id": "databricks-data-engineer-q-23",
        "questionId": "q-19006",
        "question": "In a multi-tenant data lake ingesting 60 GB/day of JSON/Parquet events from per-tenant S3 folders, design a Silver layer using Delta Live Tables that (1) preserves per-file provenance in AuditFiles with source_path and timestamps, (2) uses a central EvolutionPolicy to drive schema drift handling for nested JSON, (3) performs CDC-like upserts into Silver using event_id and a tombstone flag for deletes, (4) materializes a tenant-scoped Gold table for BI (tenant_id, date, metrics), (5) enforces Unity Catalog RBAC with masking and dynamic filters so tenants access only their data, (6) adds a data-quality gate and a bounded late-data watermark with fault-tolerant Auto Loader, and (7) includes a concrete testing plan with synthetic late and partial data. Provide concrete steps and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a pipeline using Autoloader to land JSON into Bronze, then upsert into Silver with MERGE on session_id using the latest timestamp. Apply a 2-minute watermark for late data, partition Silver by date for query perf…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a Drift-Detection and Auto-Recovery pattern: a Silver table CDC by (source, event_id); a lightweight drift detector on latency_ms compared to a stored histogram; on drift, quarantine the affec",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a per-tenant, Bronze→Silver→Gold pipeline: Bronze ingests 60–100 GB/day via Auto Loader from S3; Silver preserves per-file provenance (source_path, file_timestamp, ingest_ts) and enables schema evoluti…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Silver table keyed by event_id; maintain AuditFiles capturing source_path and ingest_ts; apply schema drift via EvolutionPolicy stored in a Delta table; implement MERGE into Silver on event_id w",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nNew angle on governance and drift control via a central EvolutionPolicy; tests and tenancy are practical at scale.\n\n## Key Concepts\n- Delta Live Tables, EvolutionPolicy, MERGE, Unity Catalog, masking, data quality gate, watermark, Auto Loader fault tolerance.\n\n## Code Example\n```python\nimport dlt\n@dlt.table\ndef silver():\n  raw = dlt.read_stream(\"bronze\")\n  evolved = evolve_with_policy(raw, policy_table)\n  return upsert_by_event_id(evolved)\n```\n\n## Follow-up Questions\n- How would you validate EvolutionPolicy across tenants?\n- How would you ensure cross-tenant dashboards cannot leak data?",
        "difficulty": "intermediate"
      },
      {
        "id": "databricks-data-engineer-q-24",
        "questionId": "q-24428",
        "question": "Design a Silver layer in a Databricks Delta Live Tables pipeline ingesting 200 GB/day of JSON and Parquet logs from S3 across 12 tenants. Specify a Silver design that (1) preserves per-file provenance in an AuditFiles table with source_path, file_timestamp, and ingest_ts via a lightweight UDF; (2) handles nested JSON drift with a schema-evolution policy; (3) performs tenant-scoped CDC with deterministic MERGE into a Gold table; (4) exposes a BI view optimized for tenant_id/date; (5) enforces Unity Catalog RBAC with masking; (6) implements a data-quality gate that blocks downstream on KPI drop; (7) tests with synthetic late data and partial files and outlines fault tolerance for Auto Loader?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Provide a Silver layer that preserves per-file provenance in AuditFiles via a lightweight UDF; handles nested JSON drift with a schema-evolution policy; applies tenant-scoped MERGE into Gold for CDC;",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create per-tenant delta tables for aggregates in Silver, partitioned by tenant_id. Ingest from Bronze via MERGE into Silver, then enable Delta Cache on those partitions. Implement a TTL-based refresh",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Auto Loader to ingest JSON from S3, create Bronze->Silver with tenant_id/date partitions, perform a MERGE into Gold on event_id, and write a SchemaAudit entry when new fields appear. Enforce",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design uses Auto Loader for S3 and Kafka in Bronze, Silver performs CDC MERGE by event_id, Gold is the consumable layer. Add a drift-detection pass that scores per-field distribution changes and quara",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEnd-to-end lineage, per-file provenance, and multi-tenant access control are critical in production Databricks pipelines. The question probes practical implementation of schema drift handling, CDC semantics, masking, and robust testing.\n\n## Key Concepts\n- Delta Live Tables Silver design for multi-tenant data\n- Per-file provenance with AuditFiles and UDF\n- Schema drift handling for nested JSON with evolution policy\n- Tenant-scoped CDC using MERGE into Gold\n- Unity Catalog RBAC with masking\n- Data-quality gates and fail-closed policy\n- Auto Loader fault tolerance with late data tests\n\n## Code Example\n```javascript\n// Pseudocode: provenance UDF for AuditFiles\nfunction provenance(source_path, file_timestamp, ingest_ts) {\n  return JSON.stringify({source_path, file_timestamp, ingest_ts})\n}\n```\n\n## Follow-up Questions\n- How would you test the data-quality gate under synthetic late-arriving data?\n- What monitoring would you add to detect drift and RBAC misconfigurations?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-24T06:07:43.064Z",
    "version": 1
  }
]