[
  {
    "id": "test-networking",
    "channelId": "networking",
    "channelName": "Networking",
    "title": "Networking Knowledge Test",
    "description": "Test your understanding of Networking with 25 questions.",
    "questions": [
      {
        "id": "networking-q-0",
        "questionId": "q-20517",
        "question": "In a four-region deployment (US-East, EU-West, APAC, US-West) where cross-region RPCs rely on a service mesh with external WAN links, a tail-latency spike is observed inter-region during peak hours while intra-region remains fast. No code changes. Design a concrete diagnostic plan to distinguish among (a) control-plane convergence delays at border routers, (b) data-plane ECMP hash collisions on cross-region paths, (c) MTU fragmentation across the overlay encapsulation, or (d) per-tenant QoS misclassification at egress devices, with exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Render a concrete diagnostic plan: instrument TLS handshake timings across regions, capture QUIC 0-RTT use and failures, monitor edge-cache TTLs and invalidations, check session-ticket cache size and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Collect cross‑region traces and DNS evidence: from EU and APAC run: traceroute -I <edge>, mtr -rwz 100 <edge>; dig +trace A/AAAA svc.example.com; dig @<LB> svc.example.com AAAA; capture TLS handshakes",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Record border-router BGP/EVPN adjacencies and convergence timers; run per-region traceroutes with timestamped hops; verify path MTU and cross-encapsulation header sizes; test ECMP hash stability with",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Start by comparing IPv6 vs IPv4 behavior: run tracepath -6 and tracepath -4, capture ICMPv6 PMTUD traffic with tcpdump, and collect per-flow latency stats across regions. If PMTUD is blocked, enable M",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAdvanced cross-region latency issues require concrete, instrumented approaches that differentiate control-plane, data-plane, MTU, and QoS factors. This question tests diagnostic rigor, tool usage, and actionable mitigations in a realistic WANed environment.\n\n## Key Concepts\n- Border-router convergence and EVPN/EVPN adjacencies\n- ECMP hashing behavior and flow distribution\n- Path MTU and overlay encapsulation overhead\n- QoS counters, queue depths, and per-tenant policies\n\n## Code Example\n```bash\n# Example commands (conceptual, not executable in this format):\n# 1) inspect BGP/EVPN state\nvtysh -c 'show ip bgp summary'\nvtysh -c 'show evpn all bindings'\n# 2) trace regional paths\ntraceroute -I <region-pair-address>\n# 3) MTU sanity\nping -M do -s 1472 <remote-host>\n```\n\n## Follow-up Questions\n- How would you validate and desensitize ECMP hashing to reduce tail latency without sacrificing throughput?\n- Which automation would you add to detect and alert on cross-region convergence issues in production?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-1",
        "questionId": "q-17852",
        "question": "In a global web API deployed in three regions (us-east, eu-west, apac) using a single Anycast DNS with TTL 60s, users in us-east report hitting eu-west endpoints during peak hours. No code changes. Provide a concrete diagnostic plan to differentiate among (a) DNS TTL and cache misconfiguration, (b) anycast route instability causing occasional redirection, (c) health-check based endpoint rotation misbehavior, or (d) client-side DNS resolver differences, with exact commands and data to collect, and mitigations to restore correct region routing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Synchronize clocks across regional collectors (PTP/NTP) and compare their inter-region RPC timestamps to captured pcap times. Collect end-to-end telemetry pcaps across all hops to look for reordering",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Diagnose by isolating DNS steering vs path and QoS issues. Collect per-region p95 latency with curl -v --resolve to force region endpoints, run traceroute/mtr, and capture QUIC logs and TLS handshakes",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Collect targeted packet and path data to test each hypothesis. Capture QUIC traces at region gateways (tcpdump -i any -w), run MTR/traceroute between region pairs, gather sFlow/NetFlow, and review NAT",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run DNS traces from multiple networks using dig +trace to reveal which A record is served at each DNS layer, then run traceroute to the returned regional endpoint to confirm path. Check root and TLD c",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical DNS troubleshooting skills, anycast path reasoning, and ability to design a reproducible diagnostic plan with concrete commands.\n\n## Key Concepts\n\n- DNS resolution and TTL behavior\n- Anycast routing and cache effects\n- Health checks and traffic steering\n- Reproducibility and data collection\n\n## Code Example\n\n```javascript\n// Example: parse a dns trace to extract served A record\nfunction extractRegion(traceLines) {\n  const line = traceLines.find(l => l.includes('A')) || traceLines[0];\n  const match = line.match(/([0-9]{1,3}\\.){3}[0-9]{1,3}/);\n  return match ? match[0] : null;\n}\n```\n\n## Follow-up Questions\n\n- How would you validate a rollback plan if regional DNS changes cause regression?\n- Which metrics signal a successful stabilization?",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-2",
        "questionId": "q-19255",
        "question": "In a four-region deployment (US-East, EU-West, APAC, US-West) using HTTP/3 over QUIC with a service mesh and per-tenant quotas, tail cross-region RPC latency spikes appear under peak load while intra-region remains stable. No code changes. Provide a concrete diagnostic plan to determine whether (a) per-tenant QUIC stream saturation on edge gateways, (b) cross-region TLS ticketing causing burst retransmits, (c) QUIC pacing misconfig, or (d) edge NAT timeout interactions are to blame, with exact commands and data to collect, and mitigations to restore inter-region latency and tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: isolate TLS handshake paths and CA sync. Collect: per-region TLS handshake timings, certificate serials and issuance timestamps, OCSP/CRL latency, and sidecar proxy metrics. Run commands: istioc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Timestamp skew across regional collectors is the most likely root cause. To validate this hypothesis, collect cross-region logs with precise timestamps and verify time synchronization health. Execute the following diagn…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Diagnostic Plan:** ### (a) Per-Tenant QUIC Stream Saturation on Edge Gateways **Data to collect:** - Deploy eBPF programs at edge gateways to capture per-tenant QUIC stream counts: ```bash sudo bpftrace -e 'kprobe:qui…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Diagnose tail latency by isolating edge TLS paths and HSM interactions. Collect: 1) per-region TLS handshake timings and HSM queue depths during peak; 2) rotation windows and key-cache stats; 3) DNS r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates advanced network debugging skills in distributed, multi-tenant systems. Candidates must demonstrate systematic root-cause analysis across interdependent subsystems—QUIC stack configuration, TLS session resumption, NAT gateway behavior, and edge gateway resource management—without code changes. The scenario tests whether an engineer can construct a diagnostic workflow that isolates variables, selects appropriate observability tools (eBPF, qlog, packet capture), and maps symptoms to concrete mitigations.\n\n## Key Concepts\n\n- **QUIC per-tenant stream management:** In multi-tenant environments, edge gateways must enforce fair sharing of QUIC streams across tenants. Stream saturation manifests as latency spikes exclusively on cross-region paths while intra-region traffic remains unaffected.\n- **TLS session resumption mechanics:** TLS tickets enable session resumption, reducing handshake overhead. When ticket key rotation occurs, clients may experience brief burst retransmits as new handshakes complete.\n- **QUIC pacing fundamentals:** Pacing prevents packet bursts that trigger buffer overflows or congestion window misbehavior. Misconfigured pacing rates can cause significant latency degradation.\n- **NAT timeout behavior:** QUIC relies on UDP, which requires explicit NAT mapping maintenance. Inconsistent timeout configurations between NAT gateways and QUIC endpoints cause premature mapping expiration, resulting in connection failures and retransmits.\n- **Diagnostic methodology:** The ability to correlate latency anomalies with specific subsystem behaviors using appropriate tooling separates senior network engineers from junior practitioners.",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-3",
        "questionId": "q-1089",
        "question": "In a multi-region Kubernetes deployment using VXLAN overlays for pod networking, you notice intermittent packet loss and high tail latency when pods in region A talk to pods in region B. The overlay adds headers that push MTU beyond 1500 on inter-region links, causing fragmentation in some paths. You can adjust MTU, MSS clamping, and overlay parameters but cannot modify application code. How would you diagnose end-to-end and implement a robust fix that preserves ECMP load balancing and minimizes fragmentation? Provide concrete steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect TLS handshake timing via tshark, curl timings, and host clocks with ntpq; measure MTU path (ping -M do -s 1472) and inter-hop RTT with traceroute; inspect NIC offloads (ethtool -k). If clocks",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by collecting DNS traces from all edge locations (dig +trace, dnsquery logs), plus client-side measurements (curl -v or browser timing). Capture TLS handshake data at the edge (openssl s_client",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Diagnose with path MTU discovery (tracepath, tracepath6), ping -M do -s, vxlan MTU, and ECMP hashing checks. Fix by tuning: set VXLAN MTU to 1450–1470, enable MSS clamping on border routers, ensure DF",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Root-cause triage plan: (a) compare handshake-failure rates with certificate-rotation windows in cert-manager/CA logs, (b) verify sidecar trust-store sync and reload cadence, (c) check clock synchroni",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe scenario mirrors real-world multi-region networks where overlay overhead and MTU issues hurt performance. It tests systematic troubleshooting and pragmatic fixes rather than theory.\n\n## Key Concepts\n- MTU/PMTU and how overlays add header overhead\n- VXLAN/VTEP MTU tuning and MSS clamping\n- ECMP hashing stability across overlays\n- DF bit handling and fragmentation avoidance\n- End-to-end diagnostics: path traces, packet captures\n\n## Code Example\n```bash\n# Example MTU validation commands\ntracepath -n dest.host\nping -c 20 -M do -s 1472 dest.host\n```\n\n## Follow-up Questions\n- How would you validate that ECMP hashing remains stable after MTU changes?\n- What are trade-offs of shrinking MTU vs enabling fragmentation in practice?\n- How would you automate regression checks for MTU-related path changes?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-4",
        "questionId": "q-24536",
        "question": "Four-region deployment US-East, EU-West, APAC, US-West; inter-region RPCs terminate TLS at regional edges. EU-West experiences morning tail latency spikes while intra-region remains normal. No code changes. Provide a concrete diagnostic plan to differentiate among: (a) TLS handshake renegotiation or 0-RTT stalls, (b) session-ticket cache misses at EU edge, (c) oversized certificate chain causing long handshakes, or (d) cross-region edge LB SNI routing misconfig. Include exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Provide a concrete diagnostic plan to determine whether (a) DNS resolver latency, (b) TTL-based caching behavior, or (c) anycast routing changes are to blame, with exact commands and data to collect,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Time TLS handshakes to EU edge vs US edge with openssl s_client -servername example.com -connect eu-edge:443 -tls1_3 and capture duration; collect TLS version, cipher, and cert chain length from logs;",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Diagnose with path MTU discovery (tracepath, tracepath6), ping -M do -s, vxlan MTU, and ECMP hashing checks. Fix by tuning: set VXLAN MTU to 1450–1470, enable MSS clamping on border routers, ensure DF",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a practical diagnostic plan: (1) run traceroute to EU and US edge POPs from both regions; (2) run dig +trace and dig @resolver to compare DNS timelines, TTLs, and EDNS usage; (3) capture UDP/5",
            "isCorrect": false
          }
        ],
        "explanation": "Why this is asked: highlights practical debugging of cross-region TLS handshakes, a common but high-variance latency point. Key concepts: TLS handshakes, 0-RTT, session tickets, certificate chains, SNI routing, and regional edge behavior. Code Example provides a concrete data-gathering approach. Follow-ups address trade-offs between security and latency, and how to validate mitigations with repeatable probes.",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-5",
        "questionId": "q-14578",
        "question": "In a three-region deployment (US-East, EU-Central, APAC) delivering a public REST API with DNS-based global traffic management that routes users to regional endpoints, tail latency spikes appear for cross-region calls during peak hours while intra-region calls stay fast. No code changes. Provide a concrete diagnostic plan to determine whether (a) DNS TTL caching leads to stale regional IPs, (b) GTM health checks are failing or misconfigured, (c) DNS anycast is delivering inconsistent paths, or (d) regional DNS resolvers are misconfigured, with exact commands and data to collect, and propose mitigations that restore stable cross-region routing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by mapping DNS flow with cross-region dig +trace and TTLs; validate GTM health checks and intervals; compare DNS responses from US/EU/APAC; test end-to-end by curling region endpoints and record",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Start by proving DNS steering is the culprit. Collect during peak: dig +trace for the steering domain, TTLs and EDNS; capture client and resolver caches; compare with authoritative zone; run MTR/trace",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run a side-by-side IPv6 and IPv4 test: measure inter-region latency with ping/traceroute6 and QUIC handshake timings; verify MTU with ping -6 -M do -s 1500 to peers; inspect IPv6 firewall/NAT rules (i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run dig +trace for the service across three public resolvers and an internal resolver; compare A records and TTLs. Capture DNS packets with tshark to observe retries, EDNS0 usage, and TCP fallback. Ch",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of DNS-based global traffic management and real-world diagnostics beyond basic networking concepts.\n\n## Key Concepts\n- DNS TTL and caching behavior\n- Global Traffic Management (GTM) health checks\n- DNS anycast and regional resolvers\n- Cross-region latency patterns\n\n## Code Example\n```bash\n# From each region, observe DNS flow\ndig +trace api.example.com\ndig +short api.example.com @<regional-dns-ip>\ncurl -sS -w \"%{time_total}\\n\" https://api.example.com/health\n```\n```\n\n## Follow-up Questions\n- How would you validate that a TTL change propagates globally and what metrics would you monitor?\n- What changes would you propose if GTM health checks were intermittently failing due to transient regional outages?",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-6",
        "questionId": "q-1031",
        "question": "In a beginner-friendly scenario, a REST API behind a global CDN shows sporadic 1–2s latency for some users while synthetic tests pass. Outline a practical, end-to-end diagnostic plan to isolate DNS, TLS, caching, and client-network factors, including concrete commands and data you would collect and an initial fix you would try?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable TLS session-ticket logs on EU and APAC proxies; collect handshake timing, cache-hit/miss rates, ticket lifetimes; compare latency with tickets vs full handshakes using openssl s_client and curl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Collect cross-network timings: DNS resolution with `dig +trace`, TCP connect and TLS handshake times and TTFB with `curl -w`. Inspect CDN edge cache status via response headers. Compare results from o",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Diagnose DNS latency by collecting trace data with dig +trace, resolve via a known external resolver (dig @1.1.1.1 example.com A), and compare timings for external vs internal clients. Check TTLs, cac",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: from 10.1.1, run dig +trace service-10-1-2.local @DNS; nslookup service-10-1-2.local @DNS; cat /etc/resolv.conf; ping DNS and 10.1.2.x; inspect DHCP leases and option 006/015; check ARP cache (a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical debugging chops for real-world network issues beyond theory.\n\n## Key Concepts\n- DNS resolution timing\n- TCP handshake and TLS behavior\n- CDN edge caching and cache-control\n- Time To First Byte and observability\n\n## Code Example\n```bash\n# Gather data across layers\n dig +trace example.com\n curl -w 'DNS=%{time_dns} CONNECT=%{time_connect} TTFB=%{time_starttransfer} TOTAL=%{time_total}\\n' -o /dev/null -s https://example.com/v1/items\n openssl s_client -servername example.com -connect example.com:443 -brief\n```\n\n## Follow-up Questions\n- How would you automate data collection across many clients?\n- How would you present findings to non-technical stakeholders?",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-7",
        "questionId": "q-17246",
        "question": "Globally distributed API (US-East, EU-West, APAC, US-West) using Istio mTLS between services. During a peak window, inter-region RPC tail latency spikes while intra-region remains fast. No code changes. Provide a concrete diagnostic plan to distinguish among (a) TLS session-ticket reuse contention across regional load balancers, (b) cross-region 0-RTT misconfig causing handshake stalls, (c) Istio sidecar CPU starvation during certificate validation, or (d) TLS record fragmentation due to MTU on inter-region paths. Include exact commands and data to collect and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect per-region TLS handshake timings and TLS ticket traces during peak. Use tshark to segment ClientHello/ServerHello across regions; compare ticket-cache usage and 0-RTT settings in Istio; confir",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "To isolate the root cause of cross-region tail-latency spikes in a three-region deployment (US-East, EU-Central, APAC) with UDP/IPv6 telemetry, P4-programmed NIC offload for VXLAN, and QUIC control channel, follow this…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by gathering traceroute and MTR data from each region to all other regions, then compare results against BGP looking-glass outputs to identify path changes and ECMP bias. Simultaneously, inspect DNS traces for sta…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: confirm time-sync uniformity across edge devices and tenants; compare token-refill timestamps to drop windows; collect per-tenant queue depths, pcap traces, and ECMP path hashes during bursts; r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests depth in cross-region TLS, service mesh, and observability.\n\n## Key Concepts\n- TLS handshake and 0-RTT behavior across regions\n- Istio mTLS and sidecar resource contention\n- Inter-region MTU discovery and fragmentation\n- Observability: pcap, tshark, and metrics\n\n## Code Example\n```javascript\n// Capture TLS handshakes across regions (illustrative)\nconst cmd1 = \"tcpdump -i any -w inter-region.cap 'tcp port 443'\";\nconst cmd2 = \"tshark -r inter-region.cap -Y 'ssl.handshake.type==1' -T fields -e frame.time -e ip.src -e ip.dst\";\n```\n\n## Follow-up Questions\n- How would you validate CA sync between regions? \n- What changes would you make to Istio configs to prevent handshake storms?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-8",
        "questionId": "q-10594",
        "question": "In a two-region deployment (US-East, EU-Central) delivering UDP telemetry behind a DNS-based global load balancer, cross-region packets sporadically drop during peak hours while intra-region traffic remains stable. No app changes. Provide a concrete diagnostic plan to determine whether (a) DNS-based path steering selects IPv6 paths with higher loss, (b) MTU mismatch on the IPv6 path, (c) NAT64 translation adding fragmentation, or (d) per-flow hashing on the load balancer causing path churn, with exact commands and data to collect, and propose mitigations preserving telemetry reliability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An ideal answer outlines a focused plan: 1) capture ESP and GRE traffic at tunnel endpoints with tcpdump, 2) inspect IPsec SA state and anti-replay windows via ip xfrm state, 3) verify MTU with ping -",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Collect DNS answers for the load balancer (dig A/AAAA telemetry.lb.example); test both IPv4 and IPv6 paths with traceroute6/tracepath6; verify MTU via ping -6 -M do -s 1400 to both endpoints; capture",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "## Diagnostic Plan **Step 1: Capture Per-Flow Path Data During Latency Spikes** ```bash # Ingress traffic capture with flow identifiers tcpdump -i any -w /tmp/trace_ingress.pcap 'ip6 and (tcp or udp)' -c 100000 # SRv6 h…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: compare UDP vs TCP DNS latency using dig @resolver +norecurse +bufsize=1232 example.com; dig @resolver +tcp +norecurse +bufsize=1232 example.com; check DNSSEC status with dig +dnssec example.com",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world networks often use DNS-driven path selection across IPv4/IPv6. This question checks practical diagnostic flow: observe DNS, test paths, validate MTU, and analyze LB behavior. It emphasizes concrete commands and data to collect, not theory.\n\n## Key Concepts\n- DNS-based traffic steering\n- IPv4 vs IPv6 path characteristics\n- MTU and fragmentation\n- Load balancer per-flow hashing\n\n## Code Example\n```bash\n# Example commands\ndig A telemetry.lb.example\ndig AAAA telemetry.lb.example\ntraceroute6 -I telemetry6.lb.example\ntracepath6 telemetry6.lb.example\n```\n```\n\n## Follow-up Questions\n- How would you automate this diagnostic plan?\n- What monitoring would you add to detect IPv6 path degradation proactively?",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-9",
        "questionId": "sd-1",
        "question": "Explain load balancing strategies and when to use Layer 4 vs Layer 7. How do round-robin, least connections, and IP hash algorithms compare?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Focus on VPN path behavior. Steps: collect OpenVPN logs and tunnel stats, run per-tunnel traffic captures with tcpdump on tun0/tun1, perform MTU tests with ping -M do -s 1472 between regional gateways",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Layer 4 uses TCP/UDP for faster performance with session persistence challenges. Layer 7 inspects HTTP headers for intelligent routing (path-based, host-based). Round-robin distributes evenly, least connections handles…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "**Diagnostic Plan: Inter-Region Tail Latency Spikes with DNS-Based Traffic Steering** ### Hypothesis 1: DNS Steering Misrouting - Toggle the feature flag OFF and compare inter-region tail latency histograms - Capture DN…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: Collect DNS history (dig + trace, resolver IPs, TTLs); measure TLS handshake times (curl -Iv --resolve); run traceroute/MTR from multiple edge locations to both regional endpoints during peak; m",
            "isCorrect": false
          }
        ],
        "explanation": "## Interview Context\nThis question evaluates system design skills, networking knowledge, and performance optimization capabilities essential for senior roles.\n\n## Key Technical Concepts\n### Load Balancing Layers\n- **Layer 4**: TCP/UDP level, faster (~1ms), no content inspection, session persistence challenges\n- **Layer 7**: Application level, intelligent routing, SSL termination, content-based rules (~5ms overhead)\n\n### Algorithms Comparison\n```yaml\n# Weighted Round Robin\nservers:\n  - id: server1\n    weight: 3  # 60% traffic\n    connections: 0\n  - id: server2\n    weight: 2  # 40% traffic\n    connections: 0\n\n# Least Connections\nactive_connections:\n  server1: 150\n  server2: 100  # Route new requests here\n\n# IP Hash\nhash_source: client_ip\nmodulus: server_count\nresult: consistent server mapping\n```\n\n### Architecture for 100K RPS\n```\nInternet → CDN → L7 Load Balancer (ALB) → L4 Load Balancer (NLB) → Microservices集群\n                                ↓\n                          WAF & Rate Limiting\n                                ↓\n                       Health Checks & Auto Scaling\n```\n\n## Performance Considerations\n- **Throughput**: NLB handles >100M TPS, ALB handles ~10M requests\n- **Latency**: Layer 4 = 1-2ms, Layer 7 = 3-8ms\n- **Scaling**: Horizontal load balancer deployment with DNS round-robin\n\n## Implementation Examples\n```javascript\n// Weighted Round Robin Implementation\nclass LoadBalancer {\n  constructor(servers) {\n    this.servers = servers.map(s => ({...s, currentWeight: 0}));\n  }\n  \n  getServer() {\n    const totalWeight = this.servers.reduce((sum, s) => sum + s.weight, 0);\n    let best = null;\n    let max = -1;\n    \n    this.servers.forEach(server => {\n      server.currentWeight += server.weight;\n      if (server.currentWeight > max) {\n        max = server.currentWeight;\n        best = server;\n      }\n    });\n    \n    if (best) best.currentWeight -= totalWeight;\n    return best;\n  }\n}\n```\n\n## Trade-offs\n- **Layer 4**: Max performance, minimal features\n- **Layer 7**: Rich features, higher latency\n- **Round Robin**: Simple, equal distribution\n- **Least Connections**: Better for varied response times\n- **IP Hash**: Session persistence, uneven distribution risk\n\n## Follow-up Questions\n1. How would you handle database connection pooling with this architecture?\n2. What monitoring metrics would you track for load balancer health?\n3. How does this design change for WebSocket connections?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-10",
        "questionId": "q-23396",
        "question": "In a four-region deployment (US-East, EU-West, APAC, US-West) running a service mesh (Istio) with mTLS across inter-region calls, inter-region tail latency spikes occur during peak hours while intra-region latency remains normal. No code changes. Provide a concrete diagnostic plan to distinguish among (a) inter-region certificate rotation delay, (b) Envoy connection pool exhaustion, (c) cross-region policy churn, or (d) inter-region NIC/underlay congestion, with exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect data from both Asia and NA edges simultaneously to isolate the region-specific issue: 1. **TLS/ALPN at edge vs origin**: Run from each edge location: ```bash openssl s_client -servername api.example.com -connect…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan to pinpoint whether inter-region tails come from mTLS handshake overhead, Envoy pool exhaustion, or cross-region policy churn. Collect: p95 handshake latencies, envoy stats, Istio traces, per-end",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "QUIC runs over UDP with independent streams, eliminating TCP-level blocking while implementing reliability at the application layer.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Diagnostic Plan ### Phase 1: Isolate the Root Cause **For (a) JWKS cache misses due to key rotation:** - Query gateway metrics for `jwks_fetch_duration_seconds` and `jwks_cache_miss_total` - Compare key rotation time…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes real-world debugging of cross-region service mesh latency, tying mTLS, Envoy, and policy behavior to observable tail latency. It requires concrete data collection steps and actionable mitigations.\n\n## Key Concepts\n- Istio/mTLS handshake latency in cross-region calls\n- Envoy connection pool sizing and pool exhaustion symptoms\n- Cross-region policy churn or policy cache invalidation effects\n- Inter-region underlay effects and diagnostics\n\n## Code Example\n```bash\n# Example diagnostic steps (illustrative)\nistioctl pc endpoints my-service --port 80\ncurl -v https://my-service.global --resolve my-service.global:443:<region_ip>\ntcptraceroute <region_ip>\ntcpdump -i eth0 -w /tmp/inter-region.pcap 'tcp'\n```\n\n## Follow-up Questions\n- How would you validate staggered certificate rotations across regions?\n- What metrics would you add to SRE dashboards to catch regressions early?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-11",
        "questionId": "q-20416",
        "question": "In a four-region cloud deployment (US-East, EU-West, APAC, US-West) hosting a web API with a simple HTTP health check, users report occasional 3-5 second delays during peak hours while normal traffic stays fast. No code changes. Provide a concrete diagnostic plan to differentiate among (a) DNS resolution latency, (b) TLS handshake caching, (c) NAT gateway port exhaustion, or (d) load balancer session affinity misconfig, with exact commands and data to collect and mitigations to restore latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Track DNS steering and latency by running dig +trace and recording TTLs per region; capture per-path traces with tcpdump on regional edges; measure queue depths with tc -s qdisc; run MTU tests (ping -",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Diagnostic Plan for Inter-Region Telemetry Latency Spikes ### Phase 1: MTU/PMTUD Failure Detection (ICMP Filtering) **Objective:** Determine if packet fragmentation due to ICMP filtering is causing latency spikes. **…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "TCP's congestion control operates at the transport layer and treats all HTTP/2 streams as a single connection. This creates a critical issue: when packet loss occurs on the underlying TCP connection, ALL HTTP/2 streams…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: 1) DNS latency: dig +trace api.example.com; record per-hop timings. 2) TLS: run openssl s_time -connect api.example.com:443 -servername api.example.com to measure handshake time. 3) NAT: review",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests hands-on debugging in realistic multi-region networks, focusing on root-cause isolation across DNS, TLS, NAT, and load balancer behavior.\n\n## Key Concepts\n\n- DNS latency and trace data\n- TLS handshake timing and session caching\n- NAT port exhaustion and translations\n- Load balancer session affinity and health checks\n\n## Code Example\n\n```javascript\n// DNS trace\ndig +trace api.example.com\n\n// TLS handshake timing\nopenssl s_time -connect api.example.com:443 -servername api.example.com\n```\n\n## Follow-up Questions\n\n- How would you instrument this plan for ongoing alerts?\n- What metrics would you collect and what thresholds would trigger alerts?",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-12",
        "questionId": "q-10154",
        "question": "In a global 6-region deployment (US-East, US-West, EU-Central, APAC, MEA, LATAM) using a service mesh with UDP telemetry and an eBPF-enabled dataplane, cross-region UDP telemetry drops occur for some tenants during peak hours while intra-region latency remains unaffected. No app changes. Provide a concrete diagnostic plan to determine whether (a) per-tenant telemetry rate-limiting at the centralized collector, (b) eBPF map eviction on dataplane, (c) cross-region MTU fragmentation, or (d) control-plane policy distribution delays are to blame, with exact commands and data to collect, and propose mitigations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Diagnostic Plan ### Phase 1: Immediate Data Collection (During Issue) **Cisco Commands:** ```bash # ARP/MAC stability checks show ip arp | include <SSH_SERVER_IP> show mac address-table dynamic | include <SSH_SERVER_…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: 1) path check: traceroute -6 -I vip; mtr -6 vip. 2) DNS delta: dig +trace AAAA domain; dig A domain. 3) capture: tcpdump -i eth0 ip6 and port 443 -w ipv6.pcap; tshark -r ipv6.pcap -Y 'tcp.analys",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin with end-to-end checks: ping and traceroute across subnets to confirm reachability, then validate routing with show ip route and show ip arp, inspect MAC tables, and verify trunk MTU with show i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Correlate collector quotas with tenant logs; inspect eBPF maps for per-tenant counters and eviction events; verify cross-region UDP MTU path. Commands: curl -I -H 'Tenant: t1' https://collector/health",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design concrete diagnostics for cross-region telemetry with eBPF dataplanes and per-tenant scaling.\n\n## Key Concepts\n- eBPF dataplanes and maps\n- per-tenant rate limiting\n- UDP telemetry and MTU path discovery\n- control-plane policy distribution\n\n## Code Example\n\n```bash\n# Example commands used in diagnosis\ncurl -I -H \"Tenant: t1\" https://collector/health\nbpftool map show /sys/fs/bpf/telemetry_map\ntcpdump -i eth0 -nn 'udp port 9000'\n```\n\n## Follow-up Questions\n- How would you reproduce in a staging environment?\n- What metrics indicate success for each hypothesis?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-13",
        "questionId": "q-14352",
        "question": "In a four-region deployment (US-East, EU-Central, APAC, US-West) where inter-service RPCs run over QUIC with mTLS inside a service mesh, a rolling upgrade in one region causes cross-region tail latency to spike during peak load while intra-region latency stays flat. No code changes. Design a concrete diagnostic plan to determine whether (a) QUIC congestion-control misbehavior, (b) TLS ticket/session resumption contention, (c) mesh sidecar resource starvation, or (d) inter-region MTU/path fragmentation are to blame, with exact data to collect and mitigations that preserve throughput?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect ARP entries (arp -a, ip neigh) and CAM table (show mac address-table), verify aging (show mac address-table aging-time), check STP state (show spanning-tree), and port-security counters (show",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Collect per-region QUIC RTT and congestion control statistics; enable TLS session ticket and 0-RTT connection logging on edge gateways; monitor service mesh sidecar CPU/memory utilization and per-connection metrics; cap…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "## Diagnostic Plan for Inter-Region Telemetry Latency Spikes ### Phase 1: MTU/PMTUD Failure Detection (ICMP Filtering) **Objective:** Determine if packet fragmentation due to ICMP filtering is causing latency spikes. **…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Collect webrtc-internals traces, ICE connection states, and STUN/TURN logs; capture coturn TURN statistics (Alloc, AllocError, remotes); run pcap on media ports to measure RTP/RTCP loss and jitter; te",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses the ability to perform systematic, data-driven diagnostics in a complex multi-region environment where QUIC, mTLS, and service mesh technologies intersect. It tests understanding of how these layers interact under load and the methodology to isolate performance bottlenecks.\n\n## Key Concepts\n- QUIC congestion control algorithms and packet pacing mechanisms\n- TLS session resumption, 0-RTT data, and ticket-based optimization\n- Service mesh sidecar resource constraints and connection pooling\n- Path MTU discovery, IP fragmentation, and cross-region network characteristics\n\n## Code Example\n```bash\n# Data collection commands\nkubectl top pods -n istio-system --sort-by=cpu\nss -tin state established '( dport = :443 or sport = :443 )'\ntc -s qdisc show dev eth0\ncurl -I https://gateway.us-east.example.com --max-time 5 -w '%{time_total}'\n```\n\n## Follow-up Questions\n- How would you validate the effectiveness of each proposed mitigation?\n- What automated monitoring would you implement to detect these issues proactively?\n- How might the diagnostic approach differ for IPv6 deployments?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-14",
        "questionId": "q-9754",
        "question": "In a three-region deployment (US-East, EU-Central, APAC) using a service mesh with UDP telemetry streaming across regions, cross-region UDP drops occur during peak hours while intra-region latency stays flat. No app changes. Design a concrete diagnostic plan to distinguish among (a) per-tenant UDP queue backlog at edge nodes, (b) cross-region path MTU discovery/fragmentation on UDP, (c) NAT64 state exhaustion, or (d) per-tenant ACL/PBR misconfiguration causing egress throttling, with exact commands and data to collect, and propose mitigations preserving telemetry reliability and tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: compare IPv6 vs IPv4 inter-region paths, collect per-path latency, MTU/PMTU, NAT64 state, QUIC handshake stats, and DNS data. Commands: traceroute -6 region; tracepath6 region; ping6 region -c 2",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a stepwise plan: capture BGP neighbor state, EVPN route history, MAC/IP table churn, VTEP replication stats, PBR rules. Collect: show bgp evpn summary, show evpn xcon, show mac, show arp, tcpd",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Collect NIC offload counters, TLS handshake timings, per-path MTU discovery, traceroutes per inter-region path, and BGP/ECMP churn metrics. Correlate spikes with peak hours and transit QoS marks. Miti",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Collect per-tenant UDP queue metrics on edge nodes (tc qdisc, ip -s xmit/rcv), pcap UDP 5040 ingress/egress, and dtrace/tcpdump across regions. Use tracepath/tracepath6 for MTU; inspect NAT64 associat",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAddresses an underexplored edge-path reliability issue with multi-region telemetry; focuses on queueing, path MTU, NAT state, and policy misconfiguration, not just transport protocols.\n\n## Key Concepts\n- UDP telemetry reliability across multi-region fabrics\n- Per-tenant QoS and edge queueing\n- MTU discovery and path fragmentation\n- NAT64 state management and ACL/policy effects\n\n## Code Example\n```javascript\n// Pseudo: parse collected stats and raise artifacts when backlog > threshold\nconst stats = fetchEdgeStats();\nif (stats.udpBacklog > 0.8*stats.queueCap) report('Backlog');\n```\n\n## Follow-up Questions\n- How would you design a tenant-isolated mitigation that preserves telemetry during NAT churn?\n- What observability would you add to prevent regression after changes?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-15",
        "questionId": "q-16159",
        "question": "Four-region deployment (US-East, EU-West, APAC, US-West) using QUIC over TLS for inter-region RPCs. Tail latency spikes appear inter-region during peak hours while intra-region RPCs stay fast. No code changes. Provide a concrete diagnostic plan to differentiate among (a) per-tenant QoS misconfiguration causing bursts on network devices, (b) NIC transmit queue depth saturation causing head-of-line blocking on inter-region hops, (c) QUIC pacing or path MTU issues triggering bursts, or (d) cross-region load balancer session affinity problems. Include exact commands and data to collect, and mitigations to preserve inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "QUIC runs over UDP with independent streams, eliminating TCP-level blocking while implementing reliability at the application layer.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: collect per-tenant BPF stats, program maps, and XDP stats; run bpftool map show; bpftool prog show; tc qdisc show; tcpdump -i eth0 'udp port 4242' -c 1000 -w /tmp/tele.pcap; ping -M do -s 1472 c",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: collect DNS response times, TTLs, and health-check logs; compare active routes with traceroute/mtr to detect stale steering; monitor TLS handshakes across regions and session tickets; review edg",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: collect per-tenant inter-region latency histograms, inspect NIC transmit queues (ethtool -S, sar) for drops, verify QUIC pacing stats, perform MTU discovery (ping -M do -s 1400), and correlate b",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes ability to diagnose cross-region latency with multiple interacting layers: network QoS, NIC buffering, QUIC behavior, and LB routing.\n\n## Key Concepts\n- Inter-region QoS and per-tenant isolation\n- NIC transmit queue depth and buffering\n- QUIC pacing and MTU behavior\n- LB session affinity and cross-region routing\n\n## Code Example\n```javascript\n// Example script outline to collect per-tenant latency histogram (pseudo)\nfunction collectTenantLatency(tenantId){\n  // query service mesh analytics for inter-region RPCs\n}\n```\n\n## Follow-up Questions\n- What metrics would you compare when bursts occur only inter-region?\n- How would you verify causality before applying mitigations?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-16",
        "questionId": "q-14942",
        "question": "In a four-region deployment (US-East, EU-Central, APAC, US-West) with a service mesh using mTLS and QUIC-based inter-region RPCs, a new edge gateway migration introduces jitter: 95th percentile cross-region latency spikes during peak hours while intra-region stays flat. The gateway now enforces per-tenant rate limits via token bucket. No code changes. Provide a concrete diagnostic plan to determine whether (a) per-tenant token bucket refills causing bursts, (b) cross-region path MTU discovery fragmentation, (c) QUIC stream multiplexing head-of-line blocking at the gateway, or (d) TLS session ticket caching causing stalls, with exact commands and data to collect, and propose mitigations preserving cross-region throughput and tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instrument per-path SRv6 telemetry across all four regions. Collect per-hop latency (traceroute6), MTU visibility, SID binding and steering counts, TE path churn, NAT64 translation table hits and time",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Diagnostic Plan:** During peak hours, collect per-peer data to distinguish among the four potential root causes: **(a) Per-peer BGP path asymmetry and ECMP churn:** - Collect BGP neighbor state: `show ip bgp neighbor…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose to collect per-subflow RTTs, loss, and reordering with ss -m and /proc/net/mptcp; capture per-path traffic with tcpdump; verify ECMP behavior via ip route get across destinations; test for opt",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Diagnostic Plan **Objective:** Determine root cause of 95th percentile cross-region latency spikes during peak hours while intra-region latency remains flat, with new edge gateway enforcing per-tenant rate limits via…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question tests the ability to diagnose gateway-induced tail latency in a multi-region service mesh environment with tenant-specific QoS controls. It evaluates skills in:\n\n- **Observability-driven debugging**: Using metrics, distributed tracing, and packet capture to isolate performance anomalies\n- **Understanding QUIC and mTLS internals**: Particularly around stream multiplexing, path MTU discovery, and session resumption\n- **Rate limiting architecture**: Per-tenant token bucket behavior under burst conditions\n- **Controlled experiments**: Systematically isolating variables to identify root causes\n\n## Key Concepts\n\n- **Per-tenant rate limiting**: Token bucket algorithms that enforce quotas per tenant can introduce bursty traffic patterns when refill rates are misconfigured\n- **Cross-region QUIC/RPC latency**: QUIC's stream multiplexing can experience head-of-line blocking at the gateway when streams share a single connection\n- **Path MTU discovery**: Cross-region paths may have varying MTUs; fragmentation increases latency and packet loss probability\n- **TLS session tickets**: Cached session tickets can cause stalls during rotation or when tickets become invalid\n\n## Code Example\n\n```javascript\n// Per-tenant latency statistics collection schema\nconst tenantLatencyStats = new Map();\n\nfunction recordLatency(tenantId, latencyMs, region) {\n  if (!tenantLatencyStats.has(tenantId)) {\n    tenantLatencyStats.set(tenantId, {\n      p50: [],\n      p95: [],\n      p99: [],\n      bucketRefills: 0,\n      regionLatencies: new Map()\n    });\n  }\n  \n  const stats = tenantLatencyStats.get(tenantId);\n  \n  // Record latency by percentile bucket\n  if (latencyMs < 50) stats.p50.push(performance.now());\n  else if (latencyMs < 200) stats.p95.push(performance.now());\n  else stats.p99.push(performance.now());\n  \n  // Track region-specific latencies\n  const regionStats = stats.regionLatencies.get(region) || [];\n  regionStats.push({ timestamp: Date.now(), latency: latencyMs });\n  stats.regionLatencies.set(region, regionStats);\n}\n\nfunction analyzeTokenBucketCorrelation() {\n  // Correlate latency spikes with token bucket exhaustion events\n  return tenantLatencyStats.entries().map(([tenant, stats]) => ({\n    tenant,\n    refillSpikeCount: stats.p99.length,\n    correlation: computeCorrelation(stats.bucketRefills, stats.p99.length)\n  }));\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-17",
        "questionId": "q-3870",
        "question": "Three-region deployment NA, EU, APAC uses edge TLS termination and a UDP telemetry overlay. Tail latency spikes appear EU→APAC during peak; suspect TLS session resumption misses on regional proxies, clock skew, or ticket rotation across regions. Without app changes, design a concrete diagnostic plan with commands and data to collect, and propose a mitigation that preserves ECMP while stabilizing latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A concrete diagnostic plan to distinguish among these failure modes: **1. Clock Drift / Sequence Skew** - Verify clock synchronization across all regions: `chronyc sources` and `ntpq -p` on each broker host - Compare `h…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Diagnostic Plan:** ### (a) Per-Tenant QUIC Stream Saturation on Edge Gateways **Data to collect:** - Deploy eBPF programs at edge gateways to capture per-tenant QUIC stream counts: ```bash sudo bpftrace -e 'kprobe:qui…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enable TLS session-ticket logs on EU and APAC proxies; collect handshake timing, cache-hit/miss rates, ticket lifetimes; compare latency with tickets vs full handshakes using openssl s_client and curl",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Start by capturing DNS TTLs, resolver cache, and LB routing data across all regions. Collect: dig +trace, traceroute to regional endpoints, tcpdump captures on edge devices, and LB health probe logs.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests diagnosing TLS-level cross-region latency in edge-terminated, ECMP-enabled networks, focusing on handshake costs, ticket management, and time synchronization.\n\n## Key Concepts\n- TLS session tickets and 0-RTT\n- Edge termination and cross-region flow symmetry\n- Clock synchronization (NTP/chrony)\n- ECMP and per-hop latency\n- Observability: handshake timings, ticket metrics, path tracing\n\n## Code Example\n```bash\n# Example data-gathering commands (illustrative)\nopenssl s_client -connect eu-edge.example.com:443 -servername eu-edge.example.com -tls1_3\ncurl -v https://eu-edge.example.com/health\nchronyc tracking\n```\n\n## Follow-up Questions\n- How would you rotate TLS tickets safely across regions without downtime?\n- How do TLS handshakes interact with ECMP hashing to affect tail latency, and what mitigations would you apply?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-18",
        "questionId": "q-5232",
        "question": "In a three-region VXLAN EVPN fabric with ECMP and IPsec inter-region tunnels, cross-region traffic between tenants experiences sporadic 200ms tail latency during peak hours, while intra-region is fine. No app changes. Provide a concrete diagnostic plan to determine whether (a) EVPN route convergence, (b) IPsec SA rekey/fragmentation, (c) NAT translation exhaustion, or (d) ECMP hashing on overlay/underlay is to blame, with exact commands and data to collect, and propose robust mitigations preserving isolation and telemetry reliability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: identify if (a) EVPN convergence delay, (b) IPsec SA rekey/fragmentation, (c) NAT port exhaustion, or (d) ECMP hashing is at fault. Include exact commands/data: show evpn route; show ip xfrm sta",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: run dig +trace and time measurements from each region; capture pcap of UDP DNS traffic to inspect EDNS0 payloads and fragmentation; collect resolver cache hit/miss stats and TTL distributions; r",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument edge-to-core paths during spikes and collect cross-region routing data and metrics. Correlate latency with BGP updates, ECMP churn, IXP congestion, and anycast steering/health-check churn.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Layer 4 uses TCP/UDP for faster performance with session persistence challenges. Layer 7 inspects HTTP headers for intelligent routing (path-based, host-based). Round-robin distributes evenly, least connections handles…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical triage across multi-region fabric, forcing candidates to distinguish between control-plane convergence, tunnel security lifecycles, NAT state pressure, and hashing behavior under load.\n\n## Key Concepts\n- EVPN route convergence and timing under churn\n- IPsec SA lifecycle, rekey bursts, and fragmentation\n- NAT translation handling and port exhaustion under concurrency\n- ECMP/hash behavior across overlay vs underlay paths\n\n## Code Example\n```javascript\n// Pseudo-commands flow (illustrative)\ncollectData();\nlogEVPN();\nlogIPsec();\nlogNAT();\ncalcPathHashes();\nproveMitigation();\n```\n\n## Follow-up Questions\n- How would you validate per-tenant isolation in telemetry after a mitigation?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-19",
        "questionId": "q-3472",
        "question": "In a three-region deployment (NA, EU, APAC) of a real-time service using a WireGuard-over-UDP overlay, UDP telemetry exhibits intermittent p95 latency spikes under load with no app changes. Outline a concrete diagnostic plan to determine whether overlay MTU fragmentation, WireGuard per-peer RTT, or NAT traversal is the culprit, including exact commands to run and data to collect, and propose a robust mitigation that preserves overlay security and ECMP stability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Focus on overlay MTU, per-peer RTT, and NAT traversal. Steps: 1) MTU discovery across WG peers with ping -M do -s 1472 <peer>; 2) tcpdump -i wg0 and -i <uplink> to capture UDP flows; 3) wg show to rea",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Diagnostic Plan **1. Baseline MPTCP Subflow Analysis** ```bash # Real-time per-subflow metrics watch -n 1 'ss -Mtni | grep -E \"(ESTAB|SYN-SENT)\"' # Detailed subflow state cat /proc/net/mptcp # Per-subflow RTT and cwn…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: instrument edge proxies and origin with time-aligned captures; compare handshake times, OCSP stapling status, and PMTUD results between peak and off-peak. Use: - tcpdump on port 443; - openssl s",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an L4 load balancer using epoll/kqueue for efficient I/O multiplexing. Leverage connection pooling with TCP keep-alive to minimize connection overhead, and implement both active and passive health checks with…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical, production-ready debugging of overlay networks under real-time load, focusing on IX routing, MTU, and NAT behavior.\n\n## Key Concepts\n\n- WireGuard MTU and fragmentation in overlays\n- Per-peer RTT measurement and path stability\n- NAT traversal interactions with overlays\n- ECMP compatibility during MTU adjustments\n\n## Code Example\n\n```bash\n#!/bin/bash\n# MTU discovery across peers\nfor p in peer1 peer2 peer3; do\n  ping -M do -s 1472 $p\ndone\n```\n\n## Follow-up Questions\n\n- How would you automate this telemetry into a dashboard?\n- What changes if a different transport (e.g., QUIC) is used over the overlay?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-20",
        "questionId": "q-23976",
        "question": "In a five-region deployment (us-east-1, eu-west-1, ap-south-1, us-west-2, sa-east-1) running a low-latency storage API with inter-region RPCs over QUIC/TLS and a regional in-cache layer, inter-region tail latency spikes occur only during burst traffic while intra-region latency stays normal. No code changes. Provide a concrete diagnostic plan to distinguish among (a) cross-region cache stampede causing backpressure, (b) QUIC path validation triggering frequent path changes, (c) underlay jitter from a transit provider, or (d) regional load balancer affinity causing cross-region retries, with exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would instrument per-path telemetry focusing on PMTU, RTT, and loss, plus NAT/overlay state. Commands: tracepath -n <peer>, ping -M do -s 1400, tc -s qdisc, tcpdump -i any and capture QUIC handshake",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: diagnose cross-region QUIC traffic by capturing cross-region traffic and per-tenant flows, collect QUIC metrics (cwnd, RTT, retransmits, pacing) from servers, map stream IDs to tenants, compare",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Focus on VPN path behavior. Steps: collect OpenVPN logs and tunnel stats, run per-tunnel traffic captures with tcpdump on tun0/tun1, perform MTU tests with ping -M do -s 1472 between regional gateways",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Collect: per-region RTTs, QUIC path-change events, ECMP churn, and cache stats during bursts. Commands: traceroute -I <peer>; tc qdisc show; ss -tulpenp; tcpdump -i any port 443; curl -sv https://peer",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to diagnose cross-region performance issues with multiple plausible culprits and limited signal.\n\n## Key Concepts\n- Cross-region latency, QUIC, ECMP, in-cache hot keys, path validation.\n- Observability: RTT, path changes, queueing, cache stats.\n- Mitigation: pacing, caching strategy, hashing stability.\n\n## Code Example\n```javascript\nfunction preventStampede(key) {\n  // simple mutex-like guard\n  if (cache.has(key)) return cache.get(key);\n  acquireLock(key);\n  try { /* refresh */ return value; } finally { releaseLock(key); }\n}\n```\n\n## Follow-up Questions\n- How would you validate cache-stampede vs QUIC path changes with data you collect?\n- What changes would you prioritize first in a live system?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-21",
        "questionId": "q-12488",
        "question": "In a two-region deployment (US-East, EU-Central) running AI training across Nvidia GPUs, inter-node RPC traffic over a programmable SDN fabric shows cross-tenant latency spikes during peak hours while intra-tenant latency stays flat. No code changes. Provide a concrete diagnostic plan to determine whether (a) QoS policy misconfiguration causes cross-tenant head-of-line blocking, (b) per-tenant queue depth bursts, (c) switch queueing issues on spine/leaf, or (d) routing oscillations are to blame, with exact commands and data to collect, and mitigations preserving throughput and isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Diagnose with path MTU discovery (tracepath, tracepath6), ping -M do -s, vxlan MTU, and ECMP hashing checks. Fix by tuning: set VXLAN MTU to 1450–1470, enable MSS clamping on border routers, ensure DF",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Diagnostic Plan ### Phase 1: Isolate the Root Cause **For (a) JWKS cache misses due to key rotation:** - Query gateway metrics for `jwks_fetch_duration_seconds` and `jwks_cache_miss_total` - Compare key rotation time…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan to diagnose intermittent cross-region multicast drops for telemetry in a four-region EVPN fabric. Check per-tenant IGMP/MLD snooping state and pruning, verify multicast group membership across re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: collect per-tenant QoS configs and queue depths (tc -s qdisc/class), switch egress queue metrics, and NetFlow logs. Capture traffic during peak with tcpdump/tshark across spine/leaf and correlat",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical diagnostic rigor for cross-tenant QoS issues in SDN fabrics, tying software configs to real-world latency behavior.\n\n## Key Concepts\n- QoS policy validation across switches and hosts\n- Linux tc/qdisc/class metrics and per-tenant queues\n- NetFlow/IPFIX and switch telemetry for path correlation\n- Path stability factors: MTU, hashing, and routing oscillations\n\n## Code Example\n```javascript\n// Pseudo: correlate latency with queue depth samples\nfunction correlate(latencyMs, queueDepth) {\n  return latencyMs / (queueDepth + 1);\n}\n```\n\n## Follow-up Questions\n- How would you validate isolation when adding a new tenant?\n- What metrics signal readiness to deploy a QoS change at scale?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-22",
        "questionId": "q-8169",
        "question": "In a multi-tenant Kubernetes cluster using Cilium with eBPF policy enforcement, a new UDP telemetry stream from tenant A to a central collector intermittently drops during scale-out. No app changes. Provide a concrete diagnostic plan to determine whether (a) per-tenant BPF policy maps are evicted due to memory pressure, (b) UDP load balancing across endpoints is skewed by policy traces, (c) MTU fragmentation or GRO offload in the Cilium datapath, or (d) host NIC ring/interrupt affinity causes TX starvation, with exact commands and data to collect, and propose mitigations preserving telemetry reliability and tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: inspect per-tenant BPF maps and eviction events, monitor policy traces (cilium monitor, hubble), verify UDP path using bpftool map and socket options, check MTU/GRO and NIC RSS, and review CPU a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Build a per-region delta dashboard comparing cross-region versus intra-region performance metrics. Collect QUIC qlog traces and TLS handshake telemetry for cross-region calls, monitoring congestion window (cwnd), RTT va…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: instrument in-edge NICs and underlay during peak to identify cross-region tail latency causes. Collect per-tenant NIC queue depth, RSS stats, inter-region path latency, and cross-region flow sam",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Diagnostic Plan 1. **Instrument per-edge TLS handshake timings** — Capture `tls_handshake_duration_seconds` metrics at each regional edge node during peak traffic. 2. **Monitor session-ticket cache hit/miss ratios**…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nUnderstand real-world data-path failures tied to policy enforcement in multi-tenant Kubernetes. Focus on eBPF maps, tracing, and NIC features under scale.\n\n## Key Concepts\n- eBPF policy maps, policy tracing, UDP load balancing, MTU/GRO, NIC RSS, CPU affinity.\n\n## Code Example\n```javascript\n// Pseudo diagnostic harness for policy eviction\nfunction checkEvictions(){ /* gather bpftool stats and cilium monitor output */ }\n```\n\n## Follow-up Questions\n- How would you automate policy-eviction detection and alerting?\n- Which metrics would you surface to on-call engineers during scale-out?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-23",
        "questionId": "q-25822",
        "question": "Four-region deployment US-East, EU-West, APAC, US-West. A gRPC/TLS service over QUIC shows inter-region tail latency spikes during peak hours while intra-region latency remains normal. No code changes. Provide a concrete diagnostic plan to distinguish among (a) APAC-edge CPU throttling, (b) TLS session resumption cache misses, (c) QUIC pacing under congestion, or (d) edge NAT queue saturation, with exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a stepwise plan: capture BGP neighbor state, EVPN route history, MAC/IP table churn, VTEP replication stats, PBR rules. Collect: show bgp evpn summary, show evpn xcon, show mac, show arp, tcpd",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Collect cross-region PCAPs at edge and regional egress, verify IPv6 PMTUD behavior and ICMPv6 PMTUD messages; check NAT64/DNS64 translation latency with per‑region logs; test QUIC MTU by probing varie",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: verify per-tenant multicast group membership and IGMP snooping configuration on each leaf switch; inspect EVPN multicast routes and PIM/BDR state specific to the affected tenant; confirm VXLAN MTU settings and fra…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: collect cross-region p99 latency and handshake timings plus APAC edge CPU/memory metrics; measure CPU steal, NIC queue depth, TLS ticket cache hits/misses, and QUIC pacing behavior. Use sar/mpst",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis probes deep diagnostic thinking for cross-region tail latency in QUIC/TLS contexts with no code changes.\n\n## Key Concepts\n\n- Cross-region latency vs intra-region latency\n- QUIC pacing and congestion control\n- TLS session resumption and ticket cache\n- Edge NAT/NIC queue dynamics, CPU steal, and path stability\n\n## Code Example\n\n```bash\n# data collection example\nmpstat -P ALL 1 60\nsar -n DEV 1 60\ntcpdump -i eth0 -w /tmp/apac.cap\n```\n\n## Follow-up Questions\n\n- How would you validate mitigations in 10-15 minutes without impacting intra-region performance?\n- Which metrics would you automate to detect recurrence and trigger alerts?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-24",
        "questionId": "q-13919",
        "question": "In a four-region deployment (US-East, EU-Central, APAC, US-West) delivering inter-region telemetry via a dual-stack WAN that uses NAT64 for IPv6-to-IPv4 translation, tail latency spikes appear during peak hours while intra-region latency stays flat. No app changes. Propose a concrete diagnostic plan to determine whether (a) NAT64 address pool exhaustion causing per-flow translation churn, (b) asymmetric MTU discovery causing fragmentation and reassembly delays, (c) NAT64 translation timeouts for large UDP payloads, or (d) per-tenant NAT table throttling are to blame, with exact commands and data to collect, and mitigations preserving telemetry reliability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect cross-network timings: DNS resolution with `dig +trace`, TCP connect and TLS handshake times and TTFB with `curl -w`. Inspect CDN edge cache status via response headers. Compare results from o",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: instrument cross-region endpoints with frequent traceroute/mtr during peaks; log QUIC handshake timings and 0-RTT usage per tenant; align spikes with cloud-accelerator path changes (Anycast) and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: compare NAT64 edge state across regions; collect NAT pool usage, per-translation counts, and PMTUD results. Run: tcpdump on NAT64 edges for UDP flows, show nat translations, ip6tables -L, ping -",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Provide a concrete diagnostic plan to distinguish among: (a) TLS rotation causing handshake stalls during inter-region QUIC/TLS setup, (b) QUIC 1-RTT handshake slowdown due to ticket refresh, (c) L7 L",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to diagnose cross-region NAT64 issues with real traffic patterns.\n\n## Key Concepts\n- NAT64 pool management\n- Path MTU discovery and fragmentation\n- UDP tail latency in overlays\n- Tenant isolation in shared NAT devices\n\n## Code Example\n```bash\n# Example: list NAT64 translations\nnstat -an\n```\n\n## Follow-up Questions\n- How would you validate PMTUD without dropping legitimate traffic?\n- What metrics indicate NAT pool exhaustion vs. MTU fragmentation?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-10T11:16:04.539Z",
    "version": 1
  }
]