[
  {
    "id": "test-networking",
    "channelId": "networking",
    "channelName": "Networking",
    "title": "Networking Knowledge Test",
    "description": "Test your understanding of Networking with 25 questions.",
    "questions": [
      {
        "id": "networking-q-0",
        "questionId": "q-11301",
        "question": "In a four-region deployment (US-East, EU-Central, APAC, US-West) with QUIC-based inter-region telemetry over UDP, tail latency spikes appear cross-region during peak hours while intra-region latency remains flat. No app changes. Provide a concrete diagnostic plan to determine whether (a) per-tenant egress policing at the host/NIC, (b) XDP-based load shedding, (c) QUIC TLS state/cache issues affecting 0-RTT, or (d) UDP firewall/NAT state exhaustion are to blame, with exact commands and data to collect, and mitigations preserving telemetry reliability and tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Map per-tenant egress policers and NIC queues (tc qdisc; tc -s qdisc; tc class show) and collect per-socket stats (ss -tupa; ss -u). Inspect XDP/BPF paths (bpftool prog show; cat /proc/net/bpf_*). Che",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Tail latency can arise when ECMP splits a connection across WAN paths with different RTTs, MTU fragmentation, and reordering that trigger retransmissions. Diagnose with per-flow RTT histograms (eBPF),",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run per-region DNS traces (dig +trace, TTL checks), traceroute/mtr to armstrong endpoints during peak, review BGP/ECMP churn on border routers, and log QUIC handshake timings and MTU behavior. Monitor",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a systematic diagnostic approach: First, instrument per-tenant edge queues using tc statistics (`tc -s qdisc`, `tc -s class`) on each regional edge router to capture queue depth patterns. Second, analyze cross…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests hands-on debugging of cross-region telemetry issues where QoS, data-plane tooling, and protocol state interact. It pushes a candidate to combine host-level QoS, kernel bypass (XDP/BPF), and QUIC behavior under load. The answer should show concrete commands, data collection, and pragmatic mitigations.\n\n## Key Concepts\n- Per-tenant QoS and egress policing\n- XDP/BPF impact on path latency\n- QUIC 0-RTT and TLS state interactions\n- UDP firewall/NAT state management under load\n\n## Code Example\n```javascript\n// Example snippet for collecting per-socket stats (pseudo)\nconst net = require('net')\nconst list = []\n// ... placeholder for illustrating collection logic\n```\n\n## Follow-up Questions\n- How would you validate that any mitigation preserves tenant isolation?\n- Which metrics would you automate collection for and how would you alert on regressions?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-1",
        "questionId": "q-26219",
        "question": "In a four-region deployment (US-East, EU-West, APAC, US-West) with inter-region RPCs over TLS, inter-region latency spikes appear during peak hours while intra-region latency remains normal. No code changes. Propose a concrete diagnostic plan to differentiate among (a) TLS handshake retries due to certificate validation delays, (b) system clock drift causing TLS renegotiation, (c) MTU path discovery fragmentation across VPNs, or (d) NIC offloads interacting with interconnect queueing. Include exact commands and data to collect, and mitigations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin with EU synthetic probes: dig +trace domain; curl -Iv https://domain; traceroute6 and mtr to the edge hostname; collect TLS handshake timing, DNS resolution latency, and CDN cache-hit/miss stats",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: query authoritative DNS from multiple vantage points with and without ECS to see which region is returned, capture the A/AAAA and TTL, then run traceroutes to regional endpoints and pull CDN edg",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Collect TLS handshake timing via tshark, curl timings, and host clocks with ntpq; measure MTU path (ping -M do -s 1472) and inter-hop RTT with traceroute; inspect NIC offloads (ethtool -k). If clocks",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: collect DNS TTLs and resolution logs across regions; run traceroutes and path MTU tests during spikes; monitor NAT state and edge queue depths; compare cross-region vs intra-region latency with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical debugging across regions focusing on TLS, timing, and network path interactions rather than theory.\n\n## Key Concepts\n- TLS handshake timings and renegotiation\n- Clock synchronization (NTP) and its effect on cert validation\n- MTU path discovery and VPN fragmentation\n- NIC offloads (TSO/GSO) and interconnect queues\n\n## Code Example\n```bash\n# TLS timing capture example (edge)\ntshark -i any -Y 'tls.handshake' -w tls_trace.pcap\ncurl -w 'time_namelookup=%{time_namelookup} time_connect=%{time_connect} time_starttransfer=%{time_starttransfer} total=%{time_total}\\n' -o /dev/null -s https://inter-region-endpoint\nntpq -p\n```\n\n## Follow-up Questions\n- How would you verify mitigations across all regions?\n- What changes would you propose to visibility/alerts for this class of issue?",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-2",
        "questionId": "q-19138",
        "question": "In a four-region deployment (US-East, EU-West, APAC, US-West) using QUIC/TLS inter-service RPCs with DNS-based service discovery and short TTLs, inter-region tail latency spikes appear only during business hours while intra-region remains stable. No code changes. Design a concrete diagnostic plan to distinguish whether (a) DNS round-robin returning rapidly changing region endpoints, (b) client DNS cache churn, (c) TLS handshake re-triggers across regions due to certificate checks, or (d) QUIC path validation oscillates across regional endpoints, with exact data and commands to collect and mitigations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Diagnostic Plan ### Step 1: Establish Baselines and Collect Per-Tunnel Metrics Collect simultaneously across all four regions: ```bash # WireGuard tunnel state and rekey counters sudo wg show all # Interface statisti…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Collect per-call destination IPs and DNS traces; from each region run `dig +trace <service>` during peak and capture A/AAAA, TTL, and cache-hit metrics. Run `traceroute`/`mtr` between regions; capture",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Collect comprehensive per-tenant telemetry across all network layers during peak hours to isolate the latency source. Execute in parallel: (1) Enable granular QUIC metrics on sidecars with `istioctl proxy-config routes`…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an L4 load balancer using epoll/kqueue for efficient I/O multiplexing. Leverage connection pooling with TCP keep-alive to minimize connection overhead, and implement both active and passive health checks with…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design pragmatic, DNS-aware diagnostics for multi-region networks, a common source of latency churn.\n\n## Key Concepts\n- DNS-based service discovery, TTL churn\n- QUIC/TLS inter-region paths, path validation\n- Traceroute/MTR, TLS handshake timing, DNS traces\n\n## Code Example\n```bash\n# DNS trace in one region\ndig +trace service.example.com\n```\n\n## Follow-up Questions\n- How would you automate data collection across regions?\n- What long-term mitigations reduce DNS churn without harming load distribution?",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-3",
        "questionId": "q-11428",
        "question": "In a four-region deployment (US-East, EU-Central, APAC, US-West) delivering UDP telemetry over a secure transport between regions. Tail latency spikes occur cross-region during peak hours; intra-region latency is flat. No app changes. Propose a concrete diagnostic plan to determine whether (a) per-tenant queueing at regional edge routers causes bursty retransmits, (b) cross-region ECMP hash collisions misload-balances flows, (c) NIC offload or RSS misconfig causing reordering, or (d) measurement plane jitter misinterpreting data, with exact commands and data to collect, and mitigations preserving telemetry reliability and tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: diagnose NAT64 per-tenant state exhaustion, IPv6 MTU/fragmentation, IPv6 ECMP hashing, and EVPN convergence. Collect: per-tenant NAT64 counters, IPv6 MTU probes, ECMP path distribution, EVPN rou",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a systematic diagnostic approach: First, instrument per-tenant edge queues using tc statistics (`tc -s qdisc`, `tc -s class`) on each regional edge router to capture queue depth patterns. Second, analyze cross…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Start by comparing IPv6 vs IPv4 behavior: run tracepath -6 and tracepath -4, capture ICMPv6 PMTUD traffic with tcpdump, and collect per-flow latency stats across regions. If PMTUD is blocked, enable M",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To diagnose, run multi-resolver DNS traces for service.example from EU, US, and APAC; capture A/AAAA records and TTLs. Inspect GSLB logs/config. Run traceroute/mtr from EU edge to regional origins; co",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to systematically diagnose complex cross-region performance issues involving queueing dynamics, load balancing, network hardware configuration, and measurement accuracy in distributed telemetry systems.\n\n## Key Concepts\n- Per-tenant queueing analysis at edge routers\n- ECMP hash collision detection and flow distribution\n- NIC offload and RSS configuration validation\n- Measurement plane accuracy and timing synchronization\n\n## Code Example\n```bash\n# Monitor per-tenant queue depths\ntc -s qdisc show dev eth0\ntc -s class show dev eth0\n\n# Analyze flow distribution\nip route show cache\nconntrack -L\nbpftrace -e 'tracepoint:net:net_dev_start_xmit { printf(\"%s %s\\n\", comm, args->name); }'\n\n# Validate NIC configuration\nethtool -k eth0\nethtool -x eth0\n\n# Capture UDP patterns with timestamps\ntcpdump -i eth0 -nn -vvv udp and port 12345 --time-stamp-precision=nano\n\n# Verify measurement plane accuracy\nntpq -p\nchronyc tracking\n```\n\n## Diagnostic Workflow\n1. **Queue Analysis**: Real-time monitoring of per-tenant queue depths during peak hours\n2. **Flow Distribution**: ECMP hash collision detection and load balancing validation\n3. **Hardware Validation**: NIC offload settings and RSS configuration verification\n4. **Measurement Accuracy**: Timing synchronization and hardware timestamp correlation\n\n## Targeted Mitigations\n- Queue discipline adjustments (FQ-CoDel, HTB)\n- ECMP hash seed modifications\n- NIC offload tuning\n- Measurement plane hardening",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-4",
        "questionId": "q-3405",
        "question": "In a global service using MPTCP over an overlay VPN across three regions, intermittent p95 latency spikes appear on UDP telemetry despite healthy synthetic tests. No app changes allowed. Provide a concrete diagnostic plan to determine if MPTCP subflow selection, per-path queueing, or VPN encapsulation is causing mid-flow path drift, including exact commands and data to collect and a robust mitigation that preserves MPTCP while stabilizing latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Diagnose DNS latency by collecting trace data with dig +trace, resolve via a known external resolver (dig @1.1.1.1 example.com A), and compare timings for external vs internal clients. Check TTLs, cac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: verify region-local routing by querying DNS from EU and US resolvers, using dig +trace and dig @resolver api.example.com; compare A-record results and TTLs; run traceroute/tracepath to EU endpoi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Diagnostic Plan **1. Baseline MPTCP Subflow Analysis** ```bash # Real-time per-subflow metrics watch -n 1 'ss -Mtni | grep -E \"(ESTAB|SYN-SENT)\"' # Detailed subflow state cat /proc/net/mptcp # Per-subflow RTT and cwn…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "During peak hours, inter-region tail latency spikes across IPsec tunnels with a global L7 load balancer. Diagnostic plan: measure path stability with mtr/traceroute between regions; collect BGP update",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nMulti-region MPTCP over VPN overlays can experience mid-flow path drift when ECMP rebalancing occurs, causing latency spikes that synthetic tests miss. A complete diagnostic plan with concrete commands and MPTCP-preserving mitigations demonstrates deep understanding of transport layer behavior.\n\n## Key Concepts\n\n- MPTCP subflow management and per-path metrics\n- Overlay VPN encapsulation overhead and queueing\n- ECMP path stability and flow hashing\n- Linux networking stack observability\n\n## Code Example\n\n```bash\n# Complete diagnostic workflow\nss -Mtni | awk '{print $4,$5,$6,$7}'\ntc -s qdisc show dev tun0 | grep -E '(bytes|drops|overruns)'\ntcpdump -i tun0 -c 1000 'udp and port 8080' -w capture.pcap\n```\n\n## Follow-up Questions\n\n- How would you validate that path pinning doesn't cause congestion hotspots?\n- What metrics would you use to detect subflow oscillation in real-time?\n- How would you extend this diagnostic approach to IPv6 overlays?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-5",
        "questionId": "q-20687",
        "question": "In a four-region deployment (us-east, eu-west, apac, us-west) of a high-throughput microservice mesh using HTTP/3 over QUIC with inter-region RPCs, cross-region tail latency spikes appear only during peak hours while intra-region latency stays low. No code changes. Provide a concrete diagnostic plan to distinguish among (a) per-region QUIC congestion-control pacing misconfiguration, (b) cross-region DNS-based traffic steering yielding bursty paths, (c) edge load balancer queue saturation, or (d) underlay fabric congestion with ECMP path contention. Include exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Diagnostic Plan ### Phase 1: Verify VXLAN Encapsulation Overhead (MTU/Fragmentation) - **Collect**: `ip -s link show vxlan0` and `show vxlan vxlan0` to verify configured MTU - **Collect**: `show interface | include M…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: collect per-tenant inter-region latency histograms, inspect NIC transmit queues (ethtool -S, sar) for drops, verify QUIC pacing stats, perform MTU discovery (ping -M do -s 1400), and correlate b",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Map cross-region RTT distributions during peak hours and collect edge QUIC metrics (pacing_rate, cwnd, handshake_time) from each region. Run tracepath/mtr between us-east, eu-west, apac, us-west; fetc",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Collect targeted packet and path data to test each hypothesis. Capture QUIC traces at region gateways (tcpdump -i any -w), run MTR/traceroute between region pairs, gather sFlow/NetFlow, and review NAT",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests diagnosing cross-region latency issues in QUIC-based inter-region RPCs with data-driven methods and clear mitigations.\n\n## Key Concepts\n- QUIC pacing and congestion control\n- DNS-based traffic steering\n- Edge load-balancer queueing\n- Underlay ECMP contention\n\n## Code Example\n```bash\n# collect cross-region RTTs\ndig +trace example.com\ntracepath -n us-east-edge\ntcpdump -i any -s 0 -w cross_region.pcap udp port 443\n```\n\n## Follow-up Questions\n- How would you validate pacing changes across regions?\n- Which metrics best distinguish DNS steering from edge queuing?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-6",
        "questionId": "q-27876",
        "question": "In a four-region deployment (US-East, US-West, EU-West, APAC) using a global API gateway with inter-region RPCs over TLS, inter-region tail latency spikes appear during peak hours while intra-region latency stays low. No code changes. Propose a concrete diagnostic plan to differentiate among: (a) BGP path changes causing ECMP path bias, (b) geo-DNS routing cache pollution, (c) TLS session resumption not being reused across regions, or (d) underlay link saturation at regional routers. Include exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run parallel traces and captures across all inter-region links. Collect VXLAN traffic via tcpdump 'udp port 4789' on edge gateways, plus 'ip route get' for multiple 5-tuples to observe ECMP paths. Gat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: capture edge and interconnect UDP telemetry with tcpdump on both sides; check firewall drop counters; verify MTU via ping -M do -s 1472; inspect ECMP/hash counters; run traceroute/mtr to map pat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: run side-by-side UDP probe captures from DC1 and DC2, test MTU, and inspect NAT/firewall state. Commands: - tcpdump -i any udp port 5000 -c 200 -w /tmp/dc1.pcap - tcpdump -i any udp port 5000 -c",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Begin by gathering traceroute and MTR data from each region to all other regions, then compare results against BGP looking-glass outputs to identify path changes and ECMP bias. Simultaneously, inspect DNS traces for sta…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates a candidate's practical debugging skills for multi-region networking issues that surface only under load. It focuses on diagnosing edge routing, DNS behavior, and TLS state management rather than application-level code fixes.\n\n## Key Concepts\n\n- **BGP and ECMP path selection** and churn\n- **Geo-DNS routing behavior** and TTL effects\n- **TLS session tickets** and resumption across regions\n- **Underlay link saturation** and regional router load\n\n## Diagnostic Commands\n\n```bash\n# Traceroute and MTR from each region to all other regions\ntraceroute -n <dest_region_ip>\nmtr -rwzbc 100 <dest_region_ip>\n\n# BGP looking-glass comparison across regions\n# Query looking-glass servers in each region for path changes:\nvtysh -c \"show ip bgp\"\nvvysh -c \"show ip bgp <dest_prefix>\"\n\n# DNS cache inspection and TTL analysis\ndig +noall +answer <service_fqdn> @<dns_resolver>\ndig +trace <service_fqdn>\n\n# TLS session ticket statistics\nopenssl s_client -connect <dest_region>:443 -sess_out /tmp/sess.pem 2>/dev/null\nopenssl s_client -connect <dest_region>:443 -sess_in /tmp/sess.pem 2>/dev/null | grep \"Reused\"\n\n# Router interface counters and NetFlow\nshow interfaces <interface>\nshow interfaces counter <interface>\nshow ip flow top-talkers\n\n# Underlay saturation detection\niperf3 -c <dest_region_ip> -P 16 -t 60\n```\n\n## Differentiating the Four Cases\n\n| Symptom | Likely Cause |\n|---------|-------------|\n| Inconsistent traceroute paths per run | BGP path change or ECMP bias |\n| DNS resolution returns wrong region | Geo-DNS cache pollution |\n| High latency on first request, low on subsequent | TLS resumption failure |\n| Consistent latency across all flows | Underlay link saturation |\n\n## Recommended Mitigations\n\n1. **BGP/ECMP**: Enable path flap dampening, configure route reflectors for stable ECMP, or implement traffic engineering via MPLS TE.\n2. **Geo-DNS**: Reduce TTL values before peak hours, flush caches proactively, and use anycast for critical endpoints.\n3. **TLS Session Tickets**: Deploy shared session ticket encryption keys across regions ( ticket rotation every 12-24h), or use a global session cache like Redis.\n4. **Underlay Saturation**: Implement QoS policies, activate traffic shaping, or provision additional bandwidth on saturated links.",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-7",
        "questionId": "q-275",
        "question": "How does QUIC solve HTTP/2's head-of-line blocking issue over TCP, and what are the implementation trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Correlate collector quotas with tenant logs; inspect eBPF maps for per-tenant counters and eviction events; verify cross-region UDP MTU path. Commands: curl -I -H 'Tenant: t1' https://collector/health",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: capture edge and interconnect UDP telemetry with tcpdump on both sides; check firewall drop counters; verify MTU via ping -M do -s 1472; inspect ECMP/hash counters; run traceroute/mtr to map pat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "QUIC runs over UDP with independent streams, eliminating TCP-level blocking while implementing reliability at the application layer.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Collect DNS response times from edge resolvers and authoritative servers, compare TTLs and cache lifetimes, use dig +trace and dig @auth to confirm stale mappings; capture traceroute/MTR and QUIC/TCP",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept\nHTTP/2 suffers from head-of-line blocking at the TCP level - a single lost packet blocks all streams. QUIC solves this by using UDP as the transport protocol and implementing reliability, congestion control, and stream multiplexing at the application layer.\n\n## Implementation\n```go\n// QUIC stream independence\nfor _, stream := range conn.Streams() {\n    go func(s quic.Stream) {\n        // Each stream processes independently\n        data, err := io.ReadAll(s)\n        // Lost packets only affect this stream\n        handleStreamData(s.ID(), data)\n    }(stream)\n}\n\n// Connection-level recovery\nconn.HandleLostPackets(func(packetID uint64) {\n    // Selective retransmission\n    if shouldRetransmit(packetID) {\n        conn.RetransmitPacket(packetID)\n    }\n})\n```\n\n## Trade-offs\n- **Pros**: Stream independence, faster recovery, reduced latency\n- **Cons**: Higher CPU usage, UDP firewall issues, complex implementation",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-8",
        "questionId": "q-13193",
        "question": "In a four-region deployment (US-East, EU-Central, APAC, MEA) with a service mesh using HTTP/3 over QUIC and mTLS at inter-region boundaries, cross-region RPCs experience rising tail latency under peak load while intra-region calls stay fast. No code changes. Provide a concrete diagnostic plan to determine whether (a) TLS session tickets are rotated inconsistently across edge proxies, (b) per-region session caches are oversized or undersized, (c) regional cert/key material differs causing handshake renegotiation storms, or (d) 0-RTT interactions with QUIC are causing replay pressure, with exact commands and data to collect, and propose mitigations to restore throughput and fairness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy comprehensive instrumentation across the control plane: capture TLS handshake metrics and CPU profiles during policy refresh storms, collect gRPC stream performance data, log policy fetch latency distributions an…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Layer 4 uses TCP/UDP for faster performance with session persistence challenges. Layer 7 inspects HTTP headers for intelligent routing (path-based, host-based). Round-robin distributes evenly, least connections handles…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: from 10.1.1, run dig +trace service-10-1-2.local @DNS; nslookup service-10-1-2.local @DNS; cat /etc/resolv.conf; ping DNS and 10.1.2.x; inspect DHCP leases and option 006/015; check ARP cache (a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Edge proxies issue TLS tickets per region; ticket cache storms can inflate tail latency. Collect per-region edge TLS session cache statistics, ticket lifetimes, handshake logs, QUIC connection IDs, and 0-RTT metrics. Co…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical debugging of cross-region TLS orchestration in a real-world multi-region service mesh, focusing on edge proxies, session tickets, and QUIC 0-RTT interactions under peak load conditions.\n\n## Key Concepts\n- TLS session tickets and cache mechanics across regional edge proxies\n- QUIC 0-RTT behavior and replay protection considerations\n- Inter-region mTLS implementation with HTTP/3 in service mesh architectures\n- Tail latency analysis in distributed systems\n\n## Code Example\n```bash\n# Example: gather TLS cache stats on an NGINX edge proxy\nnginx -V 2>&1 | grep --color 'OPENSSL'\nss -tpnl | grep -i tls\n```\n\n## Follow-up Questions\n- How would you verify consistent ticket sharing across regions without exposing security vulnerabilities?\n- What monitoring thresholds would you set for early detection of TLS-related latency degradation?\n- How do you balance 0-RTT performance benefits against replay attack risks in cross-region deployments?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-9",
        "questionId": "q-30444",
        "question": "In a four-region deployment (US-East, US-West, EU-West, APAC) with a programmable fabric handling inter-region RPCs, inter-region tail latency spikes occur during peak hours while intra-region latency remains normal. No code changes. Provide a concrete diagnostic plan to differentiate among: (a) in-kernel data-path stalls (XDP/ebpf) causing cross-region slow path, (b) per-flow hash collisions leading to cross-region imbalance, (c) SDN control-plane churn, or (d) NIC offload pacing interactions, with exact commands and data to collect, and mitigations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Diagnose DNS routing vs path issues. Collect: dig +trace, geo-record TTLs, and DNS cache states; measure inter- and intra-region RTT with ping/MTR; gather BGP/anycast state from edge routers; test reg",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan to separate (a) BGP path changes causing extra hops and jitter, (b) PMTUD fragmentation on peering links, (c) edge NAT rekey storms, or (d) TLS renegotiation delays in the service mesh. Collect:",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Collect and correlate: (i) inter-region latency histograms and per-flow hash distribution counters; (ii) XDP/eBPF map utilization, eviction rates, and per-tail latency; (iii) SDN controller policy-change event logs and…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: log per-tenant UDP drops at edge, pull firewall/IPS rate-limits, NAT state, and DNS responses; capture pcaps on inter-region links; compare bursts to IDS alerts and tenant IDs in headers; implem",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical debugging of a real-world programmable fabric where data-plane and control-plane interactions directly impact latency. Candidates must correlate low-level telemetry with high-level policy changes to isolate root causes and apply targeted mitigations.\n\n## Key Concepts\n\n- XDP/eBPF data-plane behavior under peak load\n- ECMP/hash-based load balancing and collision dynamics\n- SDN controller churn and policy stability requirements\n- NIC offload pacing and interrupt coalescence interactions\n\n## Code Example\n\n```javascript\n// Pseudo-diagnostic script outline\nasync function collectDiagnostics() {\n  const [bpfStats, hashStats, sdnLogs, nicStats] = await Promise.all([\n    getBpfMapStats(),\n    getFlowHashDistribution(),\n    getSdnPolicyChanges(),\n    getNicOffloadMetrics()\n  ]);\n\n  return correlateLatencyWithMetrics(bpfStats, hashStats, sdnLogs, nicStats);\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-10",
        "questionId": "q-25954",
        "question": "In a three-region deployment US-East, EU-West, APAC, a global gateway routes inter-region RPCs over TLS termination; inter-region tail latency spikes during peak hours while intra-region remains flat. No code changes. Provide a concrete diagnostic plan to distinguish among (a) TLS session cache saturation on gateways, (b) cross-region MTU discovery fragmentation, (c) per-path BGP churn on WAN, or (d) inter-region NAT queue saturation, with exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect per-call destination IPs and DNS traces; from each region run `dig +trace <service>` during peak and capture A/AAAA, TTL, and cache-hit metrics. Run `traceroute`/`mtr` between regions; capture",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument edge-to-core paths during spikes and collect cross-region routing data and metrics. Correlate latency with BGP updates, ECMP churn, IXP congestion, and anycast steering/health-check churn.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Diagnostic Plan 1. **Instrument per-edge TLS handshake timings** — Capture `tls_handshake_duration_seconds` metrics at each regional edge node during peak traffic. 2. **Monitor session-ticket cache hit/miss ratios**…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Diagnostic Plan for Inter-Region Latency Spikes** The symptom—inter-region RPC latency spikes during peak hours while intra-region remains flat, with no code changes—indicates a shared infrastructure bottleneck at the…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to design targeted diagnostics for production inter-region latency without code changes. It validates understanding of TLS session lifecycle, path MTU discovery, BGP/ECMP routing dynamics, and NAT translation performance—critical skills for operating global distributed systems.\n\n## Key Concepts\n\n- **TLS Session Caching**: Session tickets reduce handshake overhead; cache saturation forces full handshakes, spiking latency\n- **PMTUD and MTU Path Discovery**: Incorrect MTU causes fragmentation, retransmissions, and latency variance\n- **BGP/ECMP Churn**: Route flapping or ECMP rebalancing causes packet reordering and latency spikes\n- **NAT Translation**: Connection tracking table exhaustion or queue saturation introduces queuing delay\n\n## What Separates Good from Great Answers\n\nA strong answer demonstrates systematic elimination—ruling out each potential cause through targeted data collection rather than guesswork. Great candidates also recognize that these issues often compound: NAT saturation may trigger retries that fragment, or TLS cache misses may coincide with BGP route changes. The ability to correlate temporal patterns across multiple metric sources is what distinguishes senior-level diagnostic skills.",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-11",
        "questionId": "q-9996",
        "question": "Across four regions US-East, US-West, EU-Central, APAC, a service mesh uses eBPF/XDP-based load balancing for inter-region UDP telemetry. During peak hours cross-region latency spikes appear while intra-region stays stable. No app changes. Propose a concrete diagnostic plan to isolate whether (a) per-tenant BPF map bloat on edge gateways, (b) XDP redirect imbalance across NIC queues, (c) tc qdisc burst behavior, or (d) cross-region MTU/path fragmentation are to blame, with exact commands and data to collect, and propose mitigations preserving telemetry reliability and tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Isolate multicast path end-to-end. Verify EVPN IPv6 multicast routes, monitor per-edge replication latency, and inspect PIM join trees. Collect: tcpdump -i <edge> udp port 4789 for VXLAN, show evpn mu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: instrument in-edge NICs and underlay during peak to identify cross-region tail latency causes. Collect per-tenant NIC queue depth, RSS stats, inter-region path latency, and cross-region flow sam",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Troubleshoot by isolating DNS, NAT, ACLs, and routing. From a 10.2 host: 1) nslookup and dig public/internal domains; 2) traceroute to 8.8.8.8; 3) check ACLs between 10.1/10.2 and DNS/NAT rules; 4) in",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: collect per-tenant BPF stats, program maps, and XDP stats; run bpftool map show; bpftool prog show; tc qdisc show; tcpdump -i eth0 'udp port 4242' -c 1000 -w /tmp/tele.pcap; ping -M do -s 1472 c",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design targeted diagnostics for complex data-plane issues, focusing on BPF/XDP pathology, NIC queueing, and MTU. This angle is distinct from prior prompts which centered on TLS, NAT, MTU fragmentation, and path topology.\n\n## Key Concepts\n\n- eBPF/XDP per-tenant maps and isolation\n- XDP redirect and NIC multi-queue balancing\n- tc qdisc burst and pacing behavior\n- Cross-region MTU and fragmentation implications\n- Telemetry header correlation across tenants\n\n## Code Example\n\n```bash\nbpftool map show\nbpftool prog show\ntc qdisc show\ntcpdump -i eth0 'udp port 4242' -c 1000 -w /tmp/tele.pcap\n```\n\n## Follow-up Questions\n\n- How would you implement dynamic per-tenant map pruning without impacting other tenants?\n- Which additional instrumentation would you add to catch regression after mitigations?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-12",
        "questionId": "q-3756",
        "question": "In a global telemetry pipeline over UDP QUIC, edge collectors in three regions send to a central sink through cloud regions. During peak hours, tail latency spikes appear with no app changes. The path includes NIC offloads (GSO/TSO), IRQ coalescing, and hypervisor scheduling. Provide a concrete diagnostic plan to distinguish NIC offloads, interrupt coalescing, and scheduler jitter, with exact commands and data to collect, and a robust mitigation that preserves throughput and latency, without changing app code?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Diagnostic Plan for Inter-Region Latency Spikes** The symptom—inter-region RPC latency spikes during peak hours while intra-region remains flat, with no code changes—indicates a shared infrastructure bottleneck at the…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Diagnostic Plan for TLS 1.3 0-RTT Inter-Region Latency Spikes ### Phase 1: Baseline Collection 1. **Enable detailed logging on edge proxies**: Configure QUIC qlog output and TLS 1.3 0-RTT handshake event logging on a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: collect DNS TTLs and resolution logs across regions; run traceroutes and path MTU tests during spikes; monitor NAT state and edge queue depths; compare cross-region vs intra-region latency with",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Diagnose NIC offloads vs IRQ coalescing vs scheduler jitter. Collect: NIC offload settings (ethtool -k), per-queue stats (ethtool -S), interrupts (/proc/interrupts, /proc/net/softnet_stat), per-CPU pe",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical troubleshooting for cross-region telemetry with hidden path issues, focusing on NIC offloads, IRQ coalescing, and scheduler behavior rather than code fixes.\n\n## Key Concepts\n\n- NIC offloads (GSO/TSO/GRO) and their impact on latency\n- IRQ coalescing and interrupt affinity\n- Hypervisor scheduling jitter and CPU locality\n\n## Code Example\n\n```javascript\n// placeholder script showing metrics collection skeleton\nconst collect = () => ({ latencyHist: [], cpuStats: [] })\n```\n\n## Follow-up Questions\n\n- How would results change if MTU is fluctuating?\n- What production-safe mitigations balance latency and throughput best?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-13",
        "questionId": "q-9321",
        "question": "In a three-region deployment (US-East, EU-Central, APAC) using a dual-stack IPv4/IPv6 UDP telemetry path across a mesh, cross-region UDP streams show persistent loss on IPv6 during business hours while IPv4 remains healthy. No app changes. Provide a concrete diagnostic plan to determine whether (a) IPv6 MTU/path fragmentation in the tunnel, (b) NAT64 state exhaustion for IPv6 traffic, (c) per-tenant IPv6 firewall/PBR rules, or (d) IPv6 neighbor discovery issues in EVPN are to blame, with exact commands and data to collect, and propose mitigations preserving telemetry reliability and tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Focus on IPv6 path health diagnostics. Collect per-path MTU and fragmentation data, run Path MTU Discovery probes, establish IPv4 baseline metrics, inspect NAT64 state utilization and IPv6 neighbor caches, and validate…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "To isolate the root cause of cross-region tail-latency spikes in a three-region deployment (US-East, EU-Central, APAC) with UDP/IPv6 telemetry, P4-programmed NIC offload for VXLAN, and QUIC control channel, follow this…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: monitor inter-region TLS handshakes and connection churn with tcpdump/ss, capture per-region handshake counts, ALPN logs, and LB logs. Compare spike timing to tail latency and verify TLS session",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: inspect IPsec state and policies (ip xfrm state, ip xfrm policy) and dmesg for replay drops; verify MTU with ping -M do -s 1472 across regions and watch for fragmentation; confirm SA lifetimes/r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical IPv6 cross-region diagnostics capabilities, a common operational gap; requires concrete data collection methodologies and targeted mitigation strategies.\n\n## Key Concepts\n- IPv6 Path MTU Discovery and MTU handling\n- NAT64 state management for IPv6 traffic\n- EVPN Neighbor Discovery handling for IPv6\n- IPv6 firewall/PBR rule configuration and tenant isolation\n- Cross-region telemetry path constraints and optimization\n\n## Code Example\n```javascript\n// Pseudo-parser for IPv6 ping6/traceroute output\nfunction summarize(lines){\n  const drops = lines.filter(l=>/pac\"\n}",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-14",
        "questionId": "q-5432",
        "question": "In a dual-stack data center, a new UDP telemetry stream over **IPv6** intermittently experiences 200–400 ms tail latency, while **IPv4** telemetry remains fine. IPv6 uses a separate GRE tunnel; IPv4 uses standard VXLAN. No app changes. Provide a concrete **diagnostic plan** to identify whether (a) **IPv6 tunnel MTU/fragmentation**, (b) **GRE keepalive/session stability**, (c) **IPv6 neighbor discovery delays**, or (d) **ECMP hashing on the IPv6 path** is to blame, with exact commands and data to collect, and propose robust mitigations that preserve telemetry reliability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: instrument edge and region gateways to determine whether (a) edge SYN-rate limiter throttles cross-region handshakes, (b) VXLAN MTU/rekey bursts fragment packets, (c) PMTUD black holes result from ICMP blocks, or…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Diagnostic Plan for IPv6 Telemetry Tail Latency ### (a) IPv6 Tunnel MTU/Fragmentation ```bash # Trace path MTU to collector - shows where PMTUD fails tracepath6 -l 1400 <collector_ipv6> # Test at specific MTU boundar…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Procedure: collect traceroute6 output, path MTU, and TLS handshake timings from IPv6 paths; compare with IPv4; inspect firewall logs for ICMPv6 blocks; verify VPN MTU; test by lowering VPN MTU to 1280",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To diagnose: 1) verify DHCP relay config per tenant and scopes on the central server; 2) capture DHCP traffic to confirm proper server/relay via tcpdump -i <iface> port 67 or 68; 3) inspect ARP tables",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Answer Is Improved\n\nThe original answer was a surface-level checklist that lacked critical depth. This improved version addresses each gap:\n\n### Technical Accuracy Improvements\n\n1. **IPv6 MTU/Fragmentation:** Added `tracepath6 -l` for end-to-end MTU discovery across all hops. Clarified that IPv6 routers NEVER fragment (RFC 8200)—only the source can using fragmentation headers. Added packet capture filter for fragmentation header detection (`ip6[6] == 0x01`).\n\n2. **GRE Keepalive/Session Stability:** Added the critical syntax for Linux GRE keepalives (`keepalives 10 3 3` must be set on tunnel creation). Cisco uses different syntax (`keepalive` under interface). Added correlation between 200-400ms latency and typical GRE convergence time.\n\n3. **IPv6 Neighbor Discovery Delays:** Added NUD timer tuning commands (`nud reachable_time`, `base_reachable_time`). Explained the causal chain: ND delay → packet held → application timeout/retry → 200-400ms apparent latency. Added DAD failure detection via `dmesg`.\n\n4. **ECMP Hashing:** Added L4-aware hash policy (`fib_multipath_hash_policy=1`) as mitigation. Added Cisco IOS commands for verification. Added out-of-order packet analysis using `tshark` to quantify ECMP instability impact.\n\n### Practical Relevance\n\n- Every hypothesis now has **complete, copy-pasteable commands**\n- Added **key data to collect** sections explaining what to look for\n- Mitigations include **both Linux and Cisco IOS** commands where applicable\n- Added DSCP preservation and hardware timestamping as additional hardening steps\n- Correlation capture section ensures latency events are captured with enough context to distinguish between causes",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-15",
        "questionId": "q-21217",
        "question": "New angle: inter-region routing churn impact on inter-region QUIC/TLS RPC tail latency. In a four-region deployment US-East, EU-West, APAC, US-West, a regional control-plane upgrade triggers sudden inter-region path changes and spikes tail latency while intra-region remains stable. Provide a concrete diagnostic plan to distinguish among (a) BGP dampening causing path flaps, (b) ECMP hash collisions across inter-region links, (c) QUIC path migration and reordering, or (d) edge firewall or ISP rate-limiter-induced path instability, with exact commands and data to collect, and mitigations to restore latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Diagnose intermittent UDP telemetry loss across VPNs. Plan: review VPN SA/keepalive logs; run MTU/PMTUD tests between source, edge, and collector (e.g., ping -M do -s 1472, traceroute) to the telemetr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Diagnostic Plan for TLS 1.3 0-RTT Inter-Region Latency Spikes ### Phase 1: Baseline Collection 1. **Enable detailed logging on edge proxies**: Configure QUIC qlog output and TLS 1.3 0-RTT handshake event logging on a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Inter-region routing churn: collect BGP updates, per-peer routes, and ECMP state; run traceroute, tcptraceroute, and tcpdump across edge routers during the spike; inspect QUIC migration logs and path",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "To diagnose: 1) verify DHCP relay config per tenant and scopes on the central server; 2) capture DHCP traffic to confirm proper server/relay via tcpdump -i <iface> port 67 or 68; 3) inspect ARP tables",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle probes how control-plane dynamics at scale can become the dominant source of inter-region tail latency, a real-world failure mode.\n\n## Key Concepts\n- BGP dampening and path flaps\n- ECMP hashing stability across inter-region links\n- QUIC path migration and reordering\n- Edge firewall/NAT rate-limiting effects\n\n## Code Example\n```bash\n# Show BGP dampening state (adjust to routing platform)\nbirdc6 sh rib all\n```\n\n## Follow-up Questions\n- How would you automate correlation of routing events with latency spikes?\n- What mitigations would you deploy in production to prevent recurrence?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-16",
        "questionId": "q-23976",
        "question": "In a five-region deployment (us-east-1, eu-west-1, ap-south-1, us-west-2, sa-east-1) running a low-latency storage API with inter-region RPCs over QUIC/TLS and a regional in-cache layer, inter-region tail latency spikes occur only during burst traffic while intra-region latency stays normal. No code changes. Provide a concrete diagnostic plan to distinguish among (a) cross-region cache stampede causing backpressure, (b) QUIC path validation triggering frequent path changes, (c) underlay jitter from a transit provider, or (d) regional load balancer affinity causing cross-region retries, with exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: 1) capture DHCP on Site A gateway: tcpdump -i <iface> port 67 or 68; confirm gateway option matches intended edge. 2) verify DNS path: ip route get 10.0.0.53; traceroute -n 10.0.53.53. 3) inspec",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: mirror a subset of tenants with and without FEC, collect pcap and decoder stats to compare tail latency; inspect NIC offloads (ethtool -k), GRO/LRO behavior, and RX queue depths; monitor per-ten",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Collect: per-region RTTs, QUIC path-change events, ECMP churn, and cache stats during bursts. Commands: traceroute -I <peer>; tc qdisc show; ss -tulpenp; tcpdump -i any port 443; curl -sv https://peer",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Translates human-readable domain names into IP addresses, enabling users to access websites using memorable names instead of numeric addresses.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to diagnose cross-region performance issues with multiple plausible culprits and limited signal.\n\n## Key Concepts\n- Cross-region latency, QUIC, ECMP, in-cache hot keys, path validation.\n- Observability: RTT, path changes, queueing, cache stats.\n- Mitigation: pacing, caching strategy, hashing stability.\n\n## Code Example\n```javascript\nfunction preventStampede(key) {\n  // simple mutex-like guard\n  if (cache.has(key)) return cache.get(key);\n  acquireLock(key);\n  try { /* refresh */ return value; } finally { releaseLock(key); }\n}\n```\n\n## Follow-up Questions\n- How would you validate cache-stampede vs QUIC path changes with data you collect?\n- What changes would you prioritize first in a live system?",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-17",
        "questionId": "q-23833",
        "question": "Three-region deployment us-east, eu-west, apac behind a DNS-based global load balancer with weighted round-robin and short TTLs. Inter-region tail latency spikes during peak hours while intra-region calls are fast. No code changes. Provide a concrete diagnostic plan to distinguish among (a) DNS TTL caching causing stale IPs, (b) edge resolver churn at TTL expiry, (c) TLS session resumption across regions, or (d) LB health-check misconfig or backend saturation, with exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Start by proving DNS steering is the culprit. Collect during peak: dig +trace for the steering domain, TTLs and EDNS; capture client and resolver caches; compare with authoritative zone; run MTR/trace",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed answer: Collect DNS traces (dig +trace, +nocookie), TTLs and resolver behavior from multiple clients during peak; run traceroute/mtr to each regional endpoint; compare inter-region latency wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Concrete Diagnostic Plan for Inter-Region Latency Spike After Global Registry Rollout ### (a) Clock Skew in Regional Registries - **Command**: `chronyc sources -v` and `chronyc tracking` on each regional registry hos…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Diagnose the DNS path first with these steps: 1) Run `dig +trace` from multiple client networks across regions to inspect TTLs and identify stale A/AAAA records; 2) Collect edge resolver cache lifetimes and DNS error ra…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nDNS-driven global routing issues are common yet subtle in distributed systems. Diagnosing without code changes tests practical troubleshooting skills and understanding of the full request path—from DNS resolution through TLS handshake to load balancer health checks.\n\n## Key Concepts\n\n- DNS TTL behavior and resolver caching strategies\n- Edge resolver churn at TTL expiry\n- TLS handshake performance and session resumption mechanisms\n- Load balancer health-check configuration and backend saturation metrics\n\n## Diagnostic Approach\n\nA systematic, layered approach is essential: start at the bottom of the stack (DNS) and work upward, ruling out each potential cause before moving to the next. This minimizes wasted effort and ensures root causes are not overlooked.\n\n## Code Example\n\n```bash\n#!/bin/bash\n# Quick DNS TTL check across regions\nset -euo pipefail\n\nfor host in api.internal; do\n  echo \"=== $host ===\"\n  for region in us-east eu-west apac; do\n    echo \"Region: $region\"\n    dig +short +time=2 +tries=2 \"$host\" @\"resolver.$region.example.com\" | head -5\n    dig +ttl +short \"$host\" @\"resolver.$region.example.com\" | grep -E \"^$[0-9]+\"\n  done\ndone\n```",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-18",
        "questionId": "q-8633",
        "question": "In a three-region deployment (US-East, EU-Central, APAC) using DNS-based regional routing for UDP telemetry streams, tail latency spikes occur during peak hours as clients switch regions. Provide a concrete diagnostic plan to determine whether (a) DNS steering TTL/latency, (b) per-region NAT/firewall shaping, (c) ECMP churn across regional edges, or (d) VPN MTU fragmentation is to blame, with exact commands and data to collect, and propose mitigations that stabilize end-to-end telemetry?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Track DNS steering and latency by running dig +trace and recording TTLs per region; capture per-path traces with tcpdump on regional edges; measure queue depths with tc -s qdisc; run MTU tests (ping -",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: query authoritative DNS from multiple vantage points with and without ECS to see which region is returned, capture the A/AAAA and TTL, then run traceroutes to regional endpoints and pull CDN edg",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Diagnostic Plan: (a) QUIC congestion-control: Enable qlog on servers (--qlog-dir or QUIC_LOG_DIR env), capture cross-region pcap (tcpdump -i any -n 'udp port 443' -w cross.pcap), analyze in qvis for RTT spikes, packet l…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Map per-tenant egress policers and NIC queues (tc qdisc; tc -s qdisc; tc class show) and collect per-socket stats (ss -tupa; ss -u). Inspect XDP/BPF paths (bpftool prog show; cat /proc/net/bpf_*). Che",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of DNS-based path selection, telemetry reliability, and cross-region WAN debugging.\n\n## Key Concepts\n\n- DNS-based steering\n- UDP telemetry path characteristics\n- Edge NAT and firewall shaping\n- MTU and path MTU discovery\n\n## Code Example\n\n```bash\n# sample diagnostic commands\ndig +trace telemetry.example.com\n```\n\n## Follow-up Questions\n\n- How would you pin clients to a region while preserving DNS-based routing?\n- What are the trade-offs of longer TTLs for telemetry DNS responses?",
        "difficulty": "beginner"
      },
      {
        "id": "networking-q-19",
        "questionId": "q-28655",
        "question": "Four-region deployment uses a global TLS-terminating load balancer and TLS session tickets with cross-region caching. Inter-region tail latency spikes during peak hours; intra-region is fine. No code changes. Propose a concrete diagnostic plan to distinguish among: (a) TLS session ticket cache thrash across regions, (b) 0-RTT data rejections causing handshakes, (c) key-rotation misalignment, or (d) H2 multiplexing head-of-line blocking in the LB. Include exact commands and data to collect, and mitigations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect NIC offload counters, TLS handshake timings, per-path MTU discovery, traceroutes per inter-region path, and BGP/ECMP churn metrics. Correlate spikes with peak hours and transit QoS marks. Miti",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run cross-region handshake telemetry: enable QUIC/TLS debug logs on mesh proxies, collect TLS session-ticket counts and lifetimes, handshake retry counts, and per-edge MTU/discovery data for 3 regions",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Diagnostic Plan** ### (a) TLS Session Ticket Cache Thrash - **Collect**: Run `openssl s_client -connect <region-endpoint>:443 -reconnect -state -tls1_3 2>&1` across all four regions and compare `Session-ID` and `sessi…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "**Diagnostic Plan for Inter-Region Latency Spikes** **1. Enable End-to-End Distributed Tracing** Configure the OpenTelemetry collector to export traces to Jaeger: ```bash kubectl set env deployment/mesh-agent OTEL_EXPOR…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question assesses a candidate's ability to diagnose cross-region TLS-related latency issues without code changes. It shifts focus from underlay network problems to ticket caching behavior, 0-RTT handshake mechanics, and load balancer internals.\n\n## Key Concepts\n\n- **TLS 1.3 handshake mechanics**: Session tickets, 0-RTT, and 1-RTT resumption patterns\n- **Cross-region load balancer behavior**: Ticket synchronization, key rotation timing, and H2 multiplexing dynamics\n- **Diagnostic methodology**: Distinguishing between passive monitoring (SSLKEYLOGFILE, load balancer logs) and active probing (openssl s_client, httping)",
        "difficulty": "advanced"
      },
      {
        "id": "networking-q-20",
        "questionId": "q-17576",
        "question": "In a three-region deployment (US-East, EU-West, APAC) of a REST API served via a service mesh over QUIC/TLS, inter-region calls exhibit intermittent tail latency spikes only when TLS 1.3 early data (0-RTT) is enabled. No code changes. Provide a concrete diagnostic plan to differentiate among (a) 0-RTT replay/protection stalls, (b) per-tenant TLS cache eviction storms, (c) QUIC path validation races with 0-RTT, or (d) load balancer handling of early data. Include exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: Instrument per-region TLS handshake timings, 0-RTT acceptance/rejection rates, QUIC pacing metrics and connection IDs, plus edge proxy CPU load during rotation windows; capture edge-origin packet captures and DNS…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Diagnostic Plan for TLS 1.3 0-RTT Inter-Region Latency Spikes ### Phase 1: Baseline Collection 1. **Enable detailed logging on edge proxies**: Configure QUIC qlog output and TLS 1.3 0-RTT handshake event logging on a…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: instrument per-region cwnd/RTT, run MTU tests (ping -M do -s 1472 to each region), capture cross-region traffic with tcpdump, inspect NAT state (conntrack -L), and count TLS handshakes (openssl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument edge QUIC logs to map connection IDs to migration events and CDN revalidation signals. Run roaming-path traces and compare DNS A/AAAA under different networks to spot path changes. Distingu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design production-grade diagnostics for advanced TLS/QUIC edge cases that go beyond simple latency troubleshooting. It evaluates understanding of the interaction between early data, service mesh architecture, and multi-region networking.\n\n## Key Concepts\n- **TLS 1.3 0-RTT (Zero Round Trip Time)**: Allows clients to send data in the first flight before completing the full handshake, improving latency but introducing replay attack surface\n- **QUIC Tracing (qlog)**: Standardized logging format for QUIC protocol events including packet drops, path validation, and connection state changes\n- **Edge Proxy Telemetry**: Metrics from Envoy, NGINX, or similar proxies handling TLS termination and load balancing\n- **Per-Tenant TLS Caches**: Session resumption state that can be evicted under memory pressure, causing new handshakes\n- **Controlled Experiment Design**: A/B testing methodology to isolate variables and establish causality\n\n## Code Example\n```javascript\n// Example: QUIC qlog parser for 0-RTT analysis\nconst fs = require('fs');\nconst readline = require('readline');\n\nasync function analyzeQlog(filePath) {\n  const file = fs.createReadStream(filePath);\n  const rl = readline.createInterface({ input: file, crlfDelay: Infinity });\n  \n  let zeroRttEvents = [];\n  let pathValidationDelays = [];\n  \n  for await (const line of rl) {\n    const event = JSON.parse(line);\n    if (event.event_type === 'crypto' && event.data?.type === '0rtt') {\n      zeroRttEvents.push(event);\n    }\n    if (event.name === 'path_validation_failed') {\n      pathValidationDelays.push(event);\n    }\n  }\n  \n  console.log(`0-RTT events: ${zeroRttEvents.length}`);\n  console.log(`Path validation failures: ${pathValidationDelays.length}`);\n  \n  return { zeroRttEvents, pathValidationDelays };\n}\n\nanalyzeQlog('trace.qlog').then(console.log);\n```\n\n## Follow-up Questions\n\n**Q: How would you validate fixes with a canary rollout?**\n- Deploy the mitigation to a single region (e.g., EU-West) at 5% traffic\n- Monitor latency p50/p95/p99 alongside error rates for 24 hours\n- Gradually increase to 25%, then 50%, then 100% if metrics remain healthy\n- Implement automatic rollback via canary analysis: if p99 latency exceeds 2x baseline, revert automatically\n\n**Q: What metrics would you surface to SREs?**\n- **Primary**: `inter_region_latency_p99` (bucket by source-destination region pair)\n- **Supporting**: `tls_0rtt_rejection_total`, `tls_session_cache_hit_ratio`, `quic_path_validation_duration_seconds`\n- **Alerting**: `SLO: inter_region_latency_p99 > 500ms for > 5 minutes`\n- **Dashboard panels**: Latency heatmap by region pair, 0-RTT acceptance rate, cache eviction velocity",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-21",
        "questionId": "q-20853",
        "question": "In a production network spanning three data centers using MPLS/VPN with EVPN-VXLAN overlays and multi-tenant QoS, a single tenant's high-rate market data feed experiences intermittent inter-DC tail latency spikes during business hours while intra-DC latency is stable. No code changes. Develop a concrete diagnostic plan to distinguish among (a) underlay ECMP path contention and MTU fragmentation, (b) cross-DC VPN rekey storms, (c) VXLAN NIC offload GRO-induced reordering, or (d) BGP/EVPN convergence jitter or MAC flap. Include exact commands and data to collect, and mitigations to restore inter-DC latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement session affinity in HAProxy using either source IP hashing with 'balance source' or cookie-based stick tables, while maintaining high availability through health checks. The trade-offs include improved user ex…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Map tenant paths with traceroute/IPERF between DCs; MTU probing (ping -M do -s 1472); capture UDP feed via tcpdump; NIC checks with ethtool -K ethX gro off and -S; edge devices show bgp vpnv4 summary",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: instrument per-SRv6 tracing, gather SRv6 policy counters, verify MTU/fragments on the SR-encapsulated underlay, inspect EVPN/BGP convergence timings, and monitor UDP drop counters at edge/collec",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Diagnostic Plan:** ### (a) Per-Tenant QUIC Stream Saturation on Edge Gateways **Data to collect:** - Deploy eBPF programs at edge gateways to capture per-tenant QUIC stream counts: ```bash sudo bpftrace -e 'kprobe:qui…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end diagnosis for multi-DC overlay networks involving EVPN-VXLAN and MPLS VPNs, not just host latency. The plan must distinguish underlay vs overlay issues and provide actionable data.\n\n## Key Concepts\n- EVPN-VXLAN overlays across DCs\n- Underlay vs overlay causes of tail latency\n- ECMP hashing, MTU propagation, VPN rekey behavior, NIC offloads\n\n## Code Example\n```javascript\n// Example: show commands used in exploration\nconsole.log('Diagnostics commands executed');\n```\n\n## Follow-up Questions\n- How would you monitor for VPN rekey storms in production? \n- Which counters would you watch during failover to validate mitigation?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-22",
        "questionId": "q-19859",
        "question": "In a four-region MongoDB global cluster (US-East, EU-West, APAC, US-West) using DNS-based global load balancing for inter-region RPCs, tail latency spikes appear inter-region during peak hours while intra-region latency remains low. No code changes. Provide a concrete diagnostic plan to distinguish among (a) stale geo DNS causing misrouting, (b) anycast topology changes, (c) cross-region BGP path asymmetry, or (d) regional resolver cache poisoning, with exact commands and data to collect, and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Diagnose DNS routing vs path issues. Collect: dig +trace, geo-record TTLs, and DNS cache states; measure inter- and intra-region RTT with ping/MTR; gather BGP/anycast state from edge routers; test reg",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Collect targeted packet and path data to test each hypothesis. Capture QUIC traces at region gateways (tcpdump -i any -w), run MTR/traceroute between region pairs, gather sFlow/NetFlow, and review NAT",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: collect dns TTLs with dig +trace; capture QUIC handshakes on UDP/443; run per-region traceroutes/mtr; snapshot edge gateway sticky-session rules and ECMP state; correlate ramp timing with steeri",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: compare NAT64 edge state across regions; collect NAT pool usage, per-translation counts, and PMTUD results. Run: tcpdump on NAT64 edges for UDP flows, show nat translations, ip6tables -L, ping -",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to differentiate DNS-based routing issues from physical network problems in multi-region setups, and to propose practical, verifiable steps.\n\n## Key Concepts\n\n- DNS-based global load balancing, geo-DNS, anycast, TTL behavior\n- Inter-region vs intra-region latency diagnostics\n- BGP path stability and edge routing visibility\n- Endpoints discrimination and cache invalidation strategies\n\n## Code Example\n\n```bash\n#!/usr/bin/env bash\n# Collect DNS traces from multiple resolvers\nresolvers=(8.8.8.8 1.1.1.1 9.9.9.9)\nfor r in ${resolvers[@]}; do\n  dig +trace +short @${r} db.global.region.example\ndone\n```\n\n## Follow-up Questions\n\n- How would you validate that TTL changes actually propagate and reduce stale routes?\n- What monitoring would you add to detect DNS-induced misrouting in real time?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-23",
        "questionId": "q-16248",
        "question": "In a three-region deployment (us-east, eu-west, apac) of a web API with inter-region QUIC/TLS calls, tail latency spikes inter-region during peak hours while intra-region stays fast. No code changes. Provide a concrete diagnostic plan to distinguish among (a) BGP route dampening causing unstable paths, (b) cross-region MTU fragmentation from encapsulation, (c) NAT/session-table saturation at edge devices, or (d) load balancer affinity causing path bias, with exact commands to collect data and mitigations to restore inter-region latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Record border-router BGP/EVPN adjacencies and convergence timers; run per-region traceroutes with timestamped hops; verify path MTU and cross-encapsulation header sizes; test ECMP hash stability with",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Collect DNS data from multiple clients over time and from root and regional DNS servers. Run: dig +trace dep.example.com; dig A dep.example.com @<regional-dns>; dig +nocache A dep.example.com; tracero",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: 1) DNS latency: dig +trace api.example.com; record per-hop timings. 2) TLS: run openssl s_time -connect api.example.com:443 -servername api.example.com to measure handshake time. 3) NAT: review",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: 1) run mtr/traceroute between region pairs to pinpoint hops; 2) collect QUIC connection stats (handshake failures, RTT, loss) at edge proxies; 3) audit inter-region BGP events for path changes d",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests pragmatic diagnostic thinking for real-world tail latency across regions, focusing on observability, data collection, and actionable mitigations without code changes.\n\n## Key Concepts\n\n- QUIC/TLS behavior and PMTUD\n- Inter-region routing stability (BGP dampening, route changes)\n- Encapsulation overhead (VPN/IPsec)\n- NAT/session-state scale and edge QoS\n\n## Code Example\n\n```javascript\n// pseudo-collector example\nfunction logInterRegionStats(from,to){\n  const s = fetchStats(from,to);\n  console.log(s.rtt, s.handshakes, s.loss);\n}\n```\n\n## Follow-up Questions\n\n- What metrics would you alert on for inter-region health?\n- How would you validate a mitigation in a canary rollout?",
        "difficulty": "intermediate"
      },
      {
        "id": "networking-q-24",
        "questionId": "q-14675",
        "question": "In a three-region deployment (US-East, EU-Central, APAC) using QUIC/TLS with a service mesh, cross-region RPC tail latency spikes occur during certificate-rotation windows while intra-region latency stays flat. No code changes. Provide a concrete diagnostic plan to determine whether (a) TLS 0-RTT data replay/stall during rotation, (b) cross-region DNS routing churn, (c) edge proxy CPU contention during key scheduling, or (d) QUIC pacing mismatch across regions are to blame, with exact commands and data to collect, and propose mitigations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Measure UDP telemetry RTT/jitter per SRv6 path (iperf3 -6, ping6) and map paths with traceroute6. Inspect SRv6 state: SID maps and per-path policies via seg6 tooling or ip -6 route show. Compare MTU a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: Instrument per-region TLS handshake timings, 0-RTT acceptance/rejection rates, QUIC pacing metrics and connection IDs, plus edge proxy CPU load during rotation windows; capture edge-origin packet captures and DNS…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Begin by gathering traceroute and MTR data from each region to all other regions, then compare results against BGP looking-glass outputs to identify path changes and ECMP bias. Simultaneously, inspect DNS traces for sta…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Procedure: collect traceroute6 output, path MTU, and TLS handshake timings from IPv6 paths; compare with IPv4; inspect firewall logs for ICMPv6 blocks; verify VPN MTU; test by lowering VPN MTU to 1280",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of TLS 1.3 0-RTT interactions with QUIC in a multi-region, certificate-rotation-heavy environment and how that can affect cross-region RPC tail latency. It also probes practical instrumentation and mitigation strategies that scale.\n\n## Key Concepts\n\n- TLS 0-RTT, TLS session tickets, QUIC pacing, cross-region routing\n- Performance diagnostics: packet captures, handshake timings, DNS TTL impacts, edge CPU metrics\n- Mitigations: disable 0-RTT during rotation, stagger rotations, tune pacing and resource quotas\n\n## Code Example\n\n```bash\n# Example collection",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-21T05:21:18.893Z",
    "version": 1
  }
]