[
  {
    "id": "test-llm-ops",
    "channelId": "llm-ops",
    "channelName": "LLM OPS",
    "title": "LLM OPS Knowledge Test",
    "description": "Test your understanding of LLM OPS with 25 questions.",
    "questions": [
      {
        "id": "llm-ops-q-0",
        "questionId": "q-29863",
        "question": "Design a beginner-friendly llm-ops gateway feature that enables per-tenant 'shadow routing'—a non-blocking duplicate evaluation of prompts on a secondary model to collect latency and accuracy signals without affecting user response times. Include data model, routing logic, and a minimal test plan with two tenants: TenantA (prod 100 req/hr, shadow 25%); TenantB (prod 200 req/hr, shadow disabled)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Define per-tenant CanaryConfig: tenantId, baselineVersion, canaryVersion, sampleRate, rollbackVersion. Route requests probabilistically to canaryVersion according to sampleRate; monitor per-tenant lat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant budget, risk score, and a 200ms latency target drive routing. Implement a policy graph where prompts are evaluated for safety and relevance, then a decision engine assigns a variant: base,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant shadow routing feature: non-blocking duplicate evaluation of prompts on a secondary model to collect latency and accuracy signals without affecting user response times. Data model:",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant policy schema (tenant_id, retention_days, redaction_rules, provenance_scope, data_location). Implement policy-eval middleware that computes a deterministic prompt_hash, applies redaction, r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExplore a practical, low-risk method to measure model improvements without impacting user experience.\n\n## Key Concepts\n\n- Shadow routing: non-blocking parallel evaluation on a secondary model\n- Per-tenant config management: shadow_enabled, shadow_model_id, shadow_sample_rate\n- Observability: latency and accuracy signals, audit logging with privacy\n\n## Code Example\n\n```javascript\nfunction shouldShadow(tenant) {\n  return !!tenant.shadowEnabled && Math.random() < tenant.shadowSampleRate\n}\n```\n\n## Follow-up Questions\n\n- How would you handle shadow routing failures or skewed telemetry?\n- what metrics would you surface to evaluate shadow usefulness over time?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-1",
        "questionId": "q-5305",
        "question": "Design a production-ready per-tenant prompt caching and shard-aware routing layer for an LLM gateway serving NVIDIA, Lyft, and Square. The design must (a) respect data locality by tenant-region, (b) cache only non-PII prompts with keys tenantId|policyVersion|hash(prompt), (c) support per-tenant TTLs and memory budgets with policy-driven eviction, (d) handle bursts and maintain p95 latency <200 ms, and (e) include a minimal test plan with synthetic workloads and drift checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant latency budgets with model-slot routing. Data: TenantPolicy {tenantId,budgetMs,backlogLimit,quarantineUntil}; ModelVariants {fast,balanced,accurate} with latency estimates. Router selects f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-tenant policy engine (OPA with Rego) with region-bound policy bundles. Ingest prompts in the tenant's region, route embeddings and model calls there, and apply post-processing redaction (PII",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a regional, multi-tenant prompt cache with shard-aware routing. Cache keys use the format `tenantId|region|policyVersion|hash(prompt)` with xxHash3 for fast hashing. Only cache prompts that pass NLP-based PII dete…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a per-tenant policy plugin system with hot-swappable, versioned, and signed plugins using WASM sandbox isolation per tenant. Enforce strict isolation through separate WASM sandboxes with no shared memory between…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Matters\nThis evaluates production caching design for LLM gateways with strict compliance (data residency), multi-tenant isolation, and performance requirements (p95 <200ms latency).\n\n## Key Concepts\n\n### Regional Shard-Aware Routing\n- Use consistent hashing (Ketama algorithm) on tenantId\n- Route requests to region-specific cache shards\n- Ensures prompts stay in the tenant's designated region for compliance\n\n### Cache Key Design\n- Format: `tenantId|region|policyVersion|xxHash3(prompt)`\n- Provides uniqueness, locality awareness, and fast hashing\n\n### Multi-Tenant Isolation\n- Per-tenant TTLs control how long prompts stay cached\n- Per-tenant memory budgets prevent any tenant from dominating cache space\n- ARC eviction adapts to access patterns while respecting tenant boundaries\n\n### Performance & Reliability\n- Token bucket rate limiters handle burst traffic\n- Circuit breakers prevent cascade failures\n- Write-through caching ensures consistency between cache and backend\n\n## Test Plan\n- Generate synthetic workloads to simulate traffic patterns\n- Verify p95 latency stays under 200ms\n- Check drift between cached and actual policy versions",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-2",
        "questionId": "q-23578",
        "question": "Design a multi-tenant LLM-ops gateway that uses Retrieval-Augmented Generation (RAG) with per-tenant knowledge bases stored in tenant-scoped vector stores. The gateway must redact PII in retrieved documents, enforce data locality, prevent cross-tenant leakage through vector routing, and provide deterministic audit trails. Describe data flow, policy evaluation, latency targets, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a tenant-isolated prompt pipeline: prefix prompts with tenantId, route to per-tenant memory windows and caches, and apply per-tenant system prompts. Enforce strict data locality, redact outputs, and validate i…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Attach a per-tenant provenance blob to each prompt (tenant_id, prompt_id, model_id, timestamp, region) and emit a privacy-preserving telemetry event (latency_ms, status, redacted_input_size) stored pe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant RAG gateway where each tenant uses a separate vector-store namespace and policy. Route prompts to tenant-specific indices, redact PII from retrieved documents via a hybrid detec",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a per-tenant policy engine with tenant-scoped pools, prompt sanitization, and data isolation via separate worker processes and memory arenas. Route to a sanitized pipeline or full model based",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExplores RAG data governance, strict tenant isolation, and auditable trails in high-velocity, global deployments.\n\n## Key Concepts\n\n- RAG, per-tenant vector stores, policy_versioning, data locality\n- Cross-tenant leakage prevention, precise redaction\n- Latency targets, deterministic auditing\n\n## Code Example\n\n```javascript\n// Pseudo config sketch\nconst storeForTenant = (tenantId)=>`vstore://${tenantId}`;\nconst route = (tenantId, prompt)=>({store: storeForTenant(tenantId), prompt});\n```\n\n## Follow-up Questions\n\n- How would you handle policy drift and rollback across tenants?\n- How would you test for cross-tenant leakage in production?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-3",
        "questionId": "q-20757",
        "question": "Design a per-tenant provenance and audit layer for a multi-tenant LLM gateway used by finance-like tenants. Provide how you would collect, sign, and store prompt/response events to ensure tamper-evidence across regions, while maintaining data locality and minimal latency. Include API surface for querying provenance graphs, handling data deletion, and audit drift triggers?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a region-aware LLM gateway that enforces per-tenant data locality. The router consults a policy map (tenant → allowed regions) to route prompts exclusively to model workers deployed in permitted zones. During…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Tenant-scoped cache (Redis namespace) keyed by hash(tenantId+prompt) with TTL 60s and LRU eviction; per-tenant model pool with regional affinity; a dynamic policy layer enforcing regulatory rules to p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant quotas stored in Redis with fields: tenant_id, monthly_quota, used, burst_budget, last_refill. Token bucket refills at monthly rate; burst tokens allow short spikes. On exhaustion, throttle",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant provenance layer for a multi-tenant LLM gateway: use append-only, per-tenant encrypted logs with envelope encryption; sign events with tenant keys from KMS; compute Merkle roots fo",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\n- Fills a gap in auditability and data provenance for regulated tenants.\n- Tests ability to design tamper-evident, region-aware, privacy-preserving logging.\n\n## Key Concepts\n\n- Append-only provenance, per-tenant KMS keys, envelope encryption, Merkle trees, cross-region replication, provenance graph API, data deletion policy.\n\n## Code Example\n\n```javascript\n// Example: sign and emit a provenance event\nfunction signEvent(event, keyId){\n  const key = kms.getKey(keyId);\n  return hmacSHA256(key, JSON.stringify(event));\n}\n```\n\n## Follow-up Questions\n\n- How would you enforce deletion or data-retention across replicas?\n- How do you detect and respond to log tampering or key compromise?",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-4",
        "questionId": "q-12321",
        "question": "For a multi-tenant LLM gateway serving Oracle, Robinhood-like, and Google-like tenants, design a real-time prompt hygiene pipeline that (a) redacts PII/confidential data in prompts pre-routing, (b) enforces per-tenant policy versions on content filtering, (c) writes tamper-evident redaction logs, and (d) maintains p95 latency under 250 ms. Describe data models, eviction, drift checks, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build a multi-tenant audit-and-provenance layer using append-only storage with cryptographic chaining and hierarchical verification. **Core Data Model** Each audit record contains: tenant_id, model_version, prompt_hash…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a per-tenant compression and cache layer: define a CompressionConfig per tenant (strategy, maxTokens, qualityBudget). Use a deterministic paraphrase or template to shorten prompts while preser",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a streaming sanitizer stack before request routing: (i) per-tenant policy version selects the appropriate filter chain; (ii) redact PII and confidential data using regex patterns and NER models; (iii) emit tam…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a runtime prompt-injection and output-safety mechanism for a multi-tenant LLM-ops gateway that serves Airbnb, OpenAI, and Instacart. The system must detect and block jailbreak and exfiltration attempts that leak…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-time prompt hygiene, per-tenant policy versioning, and audit-log integrity under strict latency budgets.\n\n## Key Concepts\n\n- Streaming sanitization before request routing\n- Per-tenant policy version management\n- PII redaction via regex and NER\n- Tamper-evident, cryptographically signed audit logs\n- Latency budgeting and drift detection\n\n## Code Example\n\n```python\nimport re\n\nPII_PATTERNS = {\n  \"email\": re.compile(r\"[\\w.-]+@[\\w.-]+\\.[A-Za-z]{2,6}\")\n}\n\ndef redact(prompt, patterns):\n  out = prompt\n  for p in patterns.values():\n    out = p.sub(\"[REDACTED]\", out)\n  return out\n```\n\n## Follow-up Questions\n\n- How would you handle policy drift detection across multiple tenant versions?\n- What caching strategies would optimize performance while ensuring data consistency?\n- How do you balance between redaction accuracy and processing latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-5",
        "questionId": "q-8617",
        "question": "Design a beginner-friendly per-tenant redaction layer for a multi-tenant LLM gateway used by Microsoft and IBM internal apps. Tenants supply a PII schema and policy version; implement token-level redaction in prompts and model outputs while preserving intent. Outline data models, processing pipeline, and a minimal test plan with a concrete example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant budget tracking, SLA-aware routing, and deterministic fallbacks across three backends. Use a cost function c(model) and remaining budget to select models; apply rate limiting and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a per-tenant RedactionLayer that consumes a tenant schema of PII fields and a policy version. Ingest: redact fields in prompts using pattern rules, replace with tokens like <REDACTED>. Pass mas",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Build a multi-tenant audit-and-provenance layer using append-only storage with cryptographic chaining and hierarchical verification. **Core Data Model** Each audit record contains: tenant_id, model_version, prompt_hash…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a versioned model registry per-tenant contracts and a gateway controller that validates compatibility before routing. Checks include input/output schema, feature flags, and latency budgets.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes a practical privacy workflow for multi-tenant LLM ops, focusing on lightweight redaction and policy versioning rather than heavy governance.\n\n## Key Concepts\n\n- Per-tenant PII schemas\n- Token-level redaction and placeholder mapping\n- Maintained model intent with post-processing\n- Auditability and policy versioning\n\n## Code Example\n\n```javascript\nfunction redact(input, rules) {\n  // simple example: replace matches with <REDACTED>\n  for (const r of rules) input = input.replaceAll(r.pattern, '<REDACTED>');\n  return input;\n}\n```\n\n## Follow-up Questions\n\n- How would you test redaction under adversarial prompts?\n- How would you handle policy drift and schema evolution in production?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-6",
        "questionId": "q-18287",
        "question": "Design a beginner-friendly cost-aware routing policy for a multi-tenant LLM gateway serving Snowflake, Instacart, and Robinhood. Define per-tenant budgets, a simple model tier mapping (fast/balanced/accurate), and a routing flow that uses the cheapest model meeting latency targets, with a cache for repeated prompts. Include a concrete example, quota reset, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a policy schema with TenantBudget and PreferredModel, and a routing function that selects the cheapest model (fast, balanced, or accurate) that meets a latency SLO, then deducts usag",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Two-stage redaction: detect PII with a tenant-specific taxonomy and ML NER plus regex guards; redact prompts before routing and redact outputs after generation. Enforce per-tenant KMS keys with envelo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant budgets with a token bucket and a model-tier selector. On each request, deduct tokens; if the bucket falls below a low-water mark, route to a lighter model or trim context to meet",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant budgets with a real-time cost score: cost = (prompt_tokens * cost_per_token_prompt) + (response_tokens * cost_per_token_response) + compute; latency penalty = max(0, actual_latenc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests practical cost-aware routing for a real multi-tenant gateway, a common beginner task with clear trade-offs.\n\n## Key Concepts\n- Per-tenant budgets and quotas\n- Simple model tier mapping and latency targets\n- Atomic budget deduction and canary behavior\n- Caching for repeated prompts\n- Observability and test coverage\n\n## Code Example\n```javascript\nfunction selectModel(tenant, latencyTargetMs){\n  const models = {\n    fast: {cost:0.02, latency:80},\n    balanced: {cost:0.01, latency:200},\n    accurate: {cost:0.04, latency:400}\n  };\n  const tiers = ['fast','balanced','accurate'].filter(t => models[t].latency <= latencyTargetMs && tenant.budget >= models[t].cost);\n  return tiers.length? tiers[0] : 'fast';\n}\n```\n\n## Follow-up Questions\n- How would you test budget resets and multi-tenant isolation?\n- How would you extend to dynamic pricing or regional constraints?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-7",
        "questionId": "q-422",
        "question": "You're building a production LLM service handling 10K requests/second with transformer models experiencing memory spikes during multi-head attention. How would you optimize memory usage while maintaining throughput and latency requirements?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a production-ready streaming LLM gateway that routes requests across base, tuned, and safety model variants per tenant, with cryptographic attestation of all outputs. Implement tamper-evident append-only logs usi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Telemetry Schema**: tenant_id, model_id, metric, value, timestamp, sample_rate, policy_id. Redact prompts and outputs via deterministic hashing or tokenization; store only metadata per-tenant locally and encrypt telem…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant token-bucket with daily quota and burst capacity. Tokens refill continuously; daily quota sets total tokens per day, burstCapacity enables short spikes. Implement in Redis with keys like te",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement flash attention-2 with memory-efficient attention computation, use 8-bit quantization for weights and KV cache, apply dynamic batching with padding optimization, and implement gradient checkpointing. Configure…",
            "isCorrect": true
          }
        ],
        "explanation": "## Memory Optimization Techniques\n\n**Flash Attention-2** reduces memory from O(N²) to O(N) by computing attention in blocks without materializing the full attention matrix. For sequence length 4096, this cuts memory usage from ~67GB to ~2GB.\n\n**Quantization Strategy**: Use 8-bit quantization for model weights (4x memory reduction) and 4-bit for KV cache. Implement mixed-precision with FP16 for attention scores and INT8 for weights.\n\n## Architecture Patterns\n\n**Tensor Parallelism**: Split attention heads across GPUs. For 96-head model on 8 GPUs, each handles 12 heads, reducing per-GPU memory by 8x.\n\n**Paged Attention**: Implement KV cache with 2KB pages, allowing selective eviction and reducing fragmentation by 30-40%.\n\n## Performance Optimization\n\n**Dynamic Batching**: Group requests with similar sequence lengths. Use padding masks and implement continuous batching to achieve 85% GPU utilization.\n\n**Memory Pooling**: Pre-allocate memory pools for tensors, reducing allocation overhead by 60% and preventing memory spikes.\n\n## Real-world Implementation\n\n```python\n# Flash Attention with memory pooling\nclass OptimizedAttention(nn.Module):\n    def __init__(self, heads, dim):\n        self.flash_attn = FlashAttention2()\n        self.kv_cache = PagedKVCache(page_size=2048)\n    \n    def forward(self, x):\n        return self.flash_attn(\n            x, use_memory_pool=True,\n            kv_cache=self.kv_cache\n        )\n```\n\n**Monitoring**: Track GPU memory utilization, attention computation time, and cache hit rates. Set alerts for memory usage >80% to trigger auto-scaling.",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-8",
        "questionId": "q-7082",
        "question": "Design a privacy-first policy guardrail for a shared LLM gateway used by Microsoft, Snowflake, and MongoDB. Build a runtime policy engine that enforces per-tenant privacy settings, detects PII leakage in prompts and responses, supports versioned, hot-updatable policies, ensures data locality, and provides auditable logs with drift alerts. Include API, data model, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant sandboxed execution with versioned models; an append-only, cryptographically signed audit log; and a replayable prompt path for audits. Use deterministic decoding (seeded randomness, fixed",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant policy registry with versioned, hot-swappable rules and a runtime engine that (i) detects PII in prompts/responses (regex/ML), (ii) redacts sensitive fields, (iii) enforces tena",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Deploy a progressive canary deployment workflow that routes the new modelVersion to a small, privacy-isolated subset of tenants; continuously monitor per-tenant latency, error rate, policy violations, and potential PII…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a policy schema with TenantBudget and PreferredModel, and a routing function that selects the cheapest model (fast, balanced, or accurate) that meets a latency SLO, then deducts usag",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests privacy governance, multi-tenant isolation, and production readiness for llm-ops.\n\n## Key Concepts\n- Per-tenant policy versioning\n- PII detection and redaction\n- Data locality routing\n- Tamper-evident auditing\n- Drift alerts and minimal test plan\n\n## Code Example\n```javascript\n// Policy evaluation skeleton\nfunction evalPolicy(prompt, policy) {\n  // apply regex/ML checks, redact, route by region\n  return { allowed: true, redactions: [] };\n}\n```\n\n## Follow-up Questions\n- How would you test for policy drift across tenants?\n- How would you handle policy rollout vs. rollback in production?",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-9",
        "questionId": "q-28029",
        "question": "Design a runtime policy-driven prompt assembly pipeline for a multi-tenant LLM-ops gateway that must apply per-tenant redaction, support versioned, hot-swappable policies, ensure data locality, and provide auditable logs while allowing tenants to choose risk profiles (strict, balanced, permissive). Include data model, policy evaluation steps, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant policy schema: {tenantId, maxTokens, bannedTokens[], allowParaphrase}. Mutation steps: ban check, deterministic paraphrase when allowed, then truncation to maxTokens; provide a sa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant budgeted routing across models/providers, with latency caps and SLA guarantees. Maintain a policy store (tenant, max_cost, max_latency, preferred_models, regions). A routing engine uses rea",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant redaction policy engine with versioned JSONPath templates. Store policies in a regional, hot-swappable config service; at request arrival, evaluate tenantId, fetch policyVersion",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-tenant request cache using Redis with a namespace per tenant. Key: cache:{tenant}:{model}:{hash(input)}. TTL 120s; max 50k items; LRU eviction. Redact inputs and outputs before caching",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes runtime policy versioning, per-tenant privacy controls, data locality, and auditability under latency constraints.\n\n## Key Concepts\n- Runtime policy evaluation and hot-swapping\n- JSONPath-based redaction on nested structures\n- Data locality and in-region routing\n- Deterministic auditing and policy drift detection\n- Testing across tenants and profiles\n\n## Code Example\n```javascript\ntype Policy = { version: string; redactPaths: string[]; redactMask: string }\nfunction redact(obj, paths, mask) { /* apply per-path masks */ }\n```\n\n## Follow-up Questions\n- How would you detect policy drift and roll back safely?\n- How do you test redaction correctness and latency at scale?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-10",
        "questionId": "q-7194",
        "question": "Design a per-tenant routing policy that selects between a fast model (m1) and a higher-accuracy model (m2) using SLA class, latencyBudgetMs, and tokenBudget. Data: TenantPolicy(tenantId, slaClass, latencyBudgetMs, tokenBudget); RouteLog(requestId, tenantId, model, latencyMs, tokens). Rules: budgets tight -> m1; otherwise m2. Provide a concrete test plan and example mappings like DoorDash Bronze -> m1, Slack Gold -> m2?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a per-tenant RedactionLayer that consumes a tenant schema of PII fields and a policy version. Ingest: redact fields in prompts using pattern rules, replace with tokens like <REDACTED>. Pass mas",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant observability using OpenTelemetry traces with a tenant-scoped audit_id. Key log fields include: tenant_id, request_id, model_variant (fast/balanced/accurate), latency_ms, cost_units, endpoint, and t…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route per-tenant requests between m1 (fast) and m2 (high-accuracy) based on per-tenant SLA class, latencyBudgetMs, and tokenBudget. Data: TenantPolicy(tenantId, slaClass, latencyBudgetMs, tokenBudget)",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement request batching with dynamic batch sizes, use TensorRT-LLM with model parallelism across GPU clusters, employ two-level routing with fast-path for cached embeddings, and leverage KEDA autoscaling with GPU uti…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to translate business constraints into a simple policy with per-tenant state and verifiable tests.\n\n## Key Concepts\n- Per-tenant SLA classes\n- Budget-aware routing and simple policy rules\n- Lightweight telemetry for validation\n\n## Code Example\n```javascript\nfunction route(req, policy){\n  const tight = policy.latencyBudgetMs < req.latency && policy.tokenBudget < req.tokens;\n  return tight ? 'm1' : 'm2';\n}\n```\n\n## Follow-up Questions\n- How would you extend this with a caching layer for recent routes?\n- How would you test resilience under sudden traffic spikes?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-11",
        "questionId": "q-14652",
        "question": "Design a per-tenant drift-detection and governance service for a multi-tenant LLM gateway (Snowflake, Slack-like tenants). The service tracks per-tenant prompt/token distributions and output toxicity, compares to per-tenant baselines, and triggers guardrails or routes to safer model variants when drift crosses a threshold. Outline data collection, metrics, thresholds, data locality, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant drift-detection and governance service for a multi-tenant LLM gateway. For each tenant, collect per-request token distributions and outputs, compare against rolling baselines using statistical drift…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a per-tenant RAG policy engine with: 1) per-tenant data-source whitelist and fetch-window age, 2) per-tenant budget/cost cap that gates vector DB usage, 3) per-tenant retrieval stack selection",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant keys encrypt and sign prompts; prompts are attested in a trusted enclave before inference. Region-bound KMS keys enforce data locality. Maintain tamper-evident logs (append-only with HMAC).",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe how to enforce per-tenant monthly token quotas and cost caps in a multi-tenant LLM gateway, including data model, runtime checks, and safeguards when limits are hit (throttling, queuing, or a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design governance workflows for multi-tenant LLM gateways, including data locality and auditable drift handling.\n\n## Key Concepts\n- Drift metrics (Wasserstein/KL) per-tenant\n- Guardrail routing and audit trails\n- Per-tenant baselines, data locality, synthetic workloads\n\n## Code Example\n```javascript\n// Placeholder for integration mock\n```\n\n## Follow-up Questions\n- How would you handle concept drift vs data drift in prompts?\n- What would your alerting and SLA look like for high-drift tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-12",
        "questionId": "q-19610",
        "question": "Design a governance layer for an LLM-ops gateway to capture per-tenant prompt lineage, outputs, and model/version info, with tamper-evident audit logs, policy-driven retention and deletion, and data locality. Include data model, API surfaces, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a tenancy-aware policy engine with per-tenant model pools, routing, and prompt sanitization. Each request passes through: tenant policy lookup, prompt scrubber (PII/PHI), model selector with ve",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an immutable governance layer on the LLM gateway to capture per-tenant prompts, outputs, and model/version metadata with tamper-evident audit trails. Use event-sourced storage with cryptographic hash chaining…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a real-time, multi-tenant LLM inference pipeline that guarantees per-tenant latency budgets under burst traffic. Use per-tenant queues, dynamic request routing, and model warm pools. Describe the architecture, ro…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy a progressive canary deployment workflow that routes the new modelVersion to a small, privacy-isolated subset of tenants; continuously monitor per-tenant latency, error rate, policy violations, and potential PII…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design auditable, compliant systems at scale, combining knowledge of event sourcing, data governance, and multi-tenant architecture patterns.\n\n## Key Concepts\n- Event sourcing and immutable audit logs\n- Per-tenant data isolation and retention policies\n- Tamper-evident auditing using hash chaining (each entry includes the hash of the previous entry)\n- Policy-driven enforcement for retention and deletion\n- Data locality requirements for regulatory compliance\n- API surfaces for archival, retrieval, and deletion operations\n\n## Architecture Overview\n\n```python\n# Data Models\nfrom dataclasses import dataclass\nfrom datetime import datetime\nfrom typing import Optional\nimport hashlib\nimport json\n\n@dataclass\nclass AuditEntry:\n    entry_id: str\n    tenant_id: str\n    timestamp: datetime\n    prompt: str\n    output: str\n    model: str\n    version: str\n    previous_hash: str\n    current_hash: str\n    metadata: dict\n\nclass GovernanceLayer:\n    def __init__(self, retention_policy: dict, data_locality: dict):\n        self.retention_policy = retention_policy\n        self.data_locality = data_locality\n        self.chain = []\n        \n    def _compute_hash(self, entry_data: str, previous_hash: str) -> str:\n        return hashlib.sha256(f\"{entry_data}{previous_hash}\".encode()).hexdigest()\n    \n    def ingest(self, tenant_id: str, prompt: str, output: str, model: str, version: str) -> AuditEntry:\n        previous_hash = self.chain[-1].current_hash if self.chain else \"0\" * 64\n        entry_data = json.dumps({\n            \"tenant_id\": tenant_id,\n            \"prompt\": prompt,\n            \"output\": output,\n            \"model\": model,\n            \"version\": version\n        })\n        current_hash = self._compute_hash(entry_data, previous_hash)\n        \n        entry = AuditEntry(\n            entry_id=hashlib.sha256(f\"{tenant_id}{datetime.utcnow()}\".encode()).hexdigest()[:16],\n            tenant_id=tenant_id,\n            timestamp=datetime.utcnow(),\n            prompt=prompt,\n            output=output,\n            model=model,\n            version=version,\n            previous_hash=previous_hash,\n            current_hash=current_hash,\n            metadata={\"region\": self.data_locality.get(tenant_id)}\n        )\n        self.chain.append(entry)\n        return entry\n    \n    def verify_integrity(self) -> bool:\n        for i in range(1, len(self.chain)):\n            if self.chain[i].previous_hash != self.chain[i-1].current_hash:\n                return False\n        return True\n```\n\n## API Surfaces\n- `POST /api/v1/audit` - Ingest new prompt/output entry\n- `GET /api/v1/audit/{tenant_id}` - Retrieve tenant audit logs\n- `GET /api/v1/audit/{tenant_id}/{entry_id}` - Retrieve specific entry\n- `DELETE /api/v1/audit/{tenant_id}/{entry_id}` - Delete entry (append-only, creates deletion marker)\n- `POST /api/v1/audit/{tenant_id}/verify` - Verify tamper-evidence integrity\n- `GET /api/v1/audit/{tenant_id}/export` - Export tenant data for archival\n\n## Minimal Test Plan\n1. **Integrity Test**: Verify hash chaining maintains integrity after multiple writes\n2. **Tamper Test**: Attempt to modify historical entry, verify detection\n3. **Retention Test**: Verify entries older than retention period are flagged/deletable\n4. **Data Locality Test**: Confirm tenant data is stored in designated region\n5. **Isolation Test**: Ensure tenants cannot access other tenants' data\n6. **Deletion Test**: Verify soft-delete creates proper audit trail",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-13",
        "questionId": "q-13682",
        "question": "Design a beginner-friendly external-tool orchestration layer for a multi-tenant LLM gateway used by Airbnb and LinkedIn. The layer must route per-tenant tool calls (e.g., webSearch, calendar, data lookup), enforce per-tenant quotas, support simple caching, and ensure isolation between tenants. Include data models (TenantToolPolicy, ToolRequest, ToolResponse), a minimal API, and a concrete test plan with a sample tenant config. Target latency: 150 ms per tool call?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use OpenTelemetry with a W3C traceparent header to propagate a tenantId as a trace attribute across gateway, router, model service, and logger. Enforce PII redaction at each log point via a redact fun",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Establish per-tenant baselines using embedding vectors of prompts and outputs; detect drift within sub-200 ms p95 latency. Flag policy violations, leakage, or novel prompts via a lightweight detector.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant prompt redaction and privacy-preserving logging in an LLM-ops gateway. Use regex+lightweight ML to detect PII in inputs/outputs, apply deterministic token masking (e.g., [REDACTED-<id>]) to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant ToolPolicy model with tenantId, allowedTools, quotas{limit, windowMs}, and caching configuration. API: POST /tools/request {tenantId, toolName, params}, GET /tools/stats. Enforce quotas using sliding…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests designing a lightweight, auditable multi-tenant orchestration layer with simple latency guarantees and isolation.\n\n## Key Concepts\n- Tenant isolation and policy modeling\n- Rate limiting, sliding windows, and deterministic fallbacks\n- Caching for latency optimization\n- Auditing and observability of ToolRequests\n\n## Code Example\n```typescript\ntype TenantToolPolicy = {\n  tenantId: string\n  allowedTools: string[]\n  quotas: { limit: number; windowMs: number }\n  cacheTTL: number\n}\n```\n\n## Follow-up Questions\n- How would you test quota-window correctness under high concurrency?\n- What fallback strategies would you implement for tool unavailability?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-14",
        "questionId": "q-26138",
        "question": "Design a real-time governance layer for a multi-tenant LLM gateway that monitors model performance, safety, and content policy drift across tenants (e.g., Google/PayPal/Adobe). Specify online evaluation, drift detection (per-tenant thresholds, rolling windows), a per-tenant safety score with automated blocking/escalation, versioned policies, auditing, and rollback. Include data models, API contracts, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An append-only ledger with per-tenant keys and ED25519 signatures for each entry. Hash prompts and outputs with SHA-256; store tenant_id, prompt_hash, model_variant, latency, outputs_hash. Redact sens",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a real-time governance layer that computes per-tenant KPIs (latency, toxicity, policy-violation rate) and adjusts routing. Use rolling-window drift detectors (KL divergence/KS test) on outpu",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a per-tenant provenance layer with a tamper-evident, append-only audit trail. Each prompt processing request generates a signed, cryptographically chained log entry containing the prompt hash, filters applied, ro…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a versioned model registry per-tenant contracts and a gateway controller that validates compatibility before routing. Checks include input/output schema, feature flags, and latency budgets.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design production-grade governance across tenants, including drift detection, online evaluation, and rollback.\n\n## Key Concepts\n- Real-time evaluation pipeline per tenant\n- Drift detection (rolling windows, KL/KS)\n- Per-tenant safety scoring with automatic actions\n- Versioned policies and auditability\n- Minimal test plan and SLOs\n\n## Code Example\n```javascript\n// pseudo-implementation sketch\nfunction computeSafetyScore(outcome, policy) { /* ... */ }\n```\n\n## Follow-up Questions\n- How to choose drift thresholds and handle concept drift vs data drift?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-15",
        "questionId": "q-24275",
        "question": "Design a per-tenant prompt provenance layer for an LLM-ops gateway that guarantees cryptographic attestations of the processing path (prompt, filter set, routing, model version) without leaking tenant data. Include tamper-evident, append-only logs, replayable paths for audits, data locality, and a test plan ensuring audit verifiability under cross-tenant resource sharing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pre-infer redaction gate: apply per-tenant regex rules for emails, phone numbers, SSNs; replace with [REDACTED], preserving token structure. Store policies in a config service (tenant_id → rules). Red",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant policy DSL and a light, latency-aware prompt classifier that selects one of three model variants. Enforce pre/post filters in a sidecar, isolate data with tenant-scoped encrypti",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant provenance layer with a tamper-evident, append-only audit trail. Each prompt processing request generates a signed, cryptographically chained log entry containing the prompt hash, filters applied, ro…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Build a privacy-preserving LLM-ops proxy with per-tenant policy store. Before forwarding a prompt, apply dynamic redaction for PII, add DP noise within a per-tenant budget, and route to backends. Main",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates the candidate's understanding of cryptographic provenance, auditability, and secure multi-tenant architecture in LLM operations. It assesses the ability to design systems that maintain data integrity and enable compliance verification without compromising tenant isolation.\n\n## Key Concepts\n\n- **Per-tenant log chaining and signing**: Each tenant maintains isolated cryptographic keys in their dedicated KMS, ensuring complete isolation of audit trails while enabling cross-tenant verification when authorized.\n\n- **Tamper-evident append-only storage**: Utilizing technologies like Write-Ahead Logging (WAL) or blockchain-inspired structures ensures that once logged, entries cannot be modified without detection, providing cryptographic proof of data integrity.\n\n- **Replayable audit paths**: The system maintains sufficient state information to reconstruct any prompt's complete processing journey, enabling auditors to verify that specific filters, routing decisions, and model versions were applied in the correct sequence.\n\n- **Data locality and tenant isolation**: Geographic boundaries and tenant-specific storage partitions ensure sensitive data remains within designated regions while maintaining strict separation between tenants.\n\n## Evaluation Criteria\n\nStrong candidates should demonstrate understanding of: cryptographic hash chains, Merkle tree-based audit proofs, KMS integration patterns, zero-knowledge proofs for privacy-preserving audits, and compliance frameworks governing multi-tenant LLM deployments.",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-16",
        "questionId": "q-25042",
        "question": "Design a beginner-friendly llm-ops gateway failover policy: when a tenant's primary model becomes unhealthy, automatically route to a secondary model, preserving per-tenant SLA, redaction, and audit logs. Define the policy schema, health checks, failover criteria, and a minimal test plan with a concrete tenant scenario (TenantA in us-east) where primary fast fails and requests must land on secondary balanced?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant policy graph and a global resolver that evaluates rules in priority order, resolving conflicts deterministically (deny wins unless overridden by higher-priority allow). Isolate",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## LLM-Ops Gateway Failover Policy ### Policy Schema ```json { \"tenants\": { \"TenantA\": { \"region\": \"us-east\", \"primary\": \"fast\", \"secondary\": \"balanced\", \"sla\": { \"maxLatencyMs\": 2000, \"minAvailability\": 0.99 }, \"redact…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a per-request provenance graph: tenant_id, request_id, input_hash, timestamp, stages, model_id/version, transformation logs, data_residency tag. Append immutably to a per-tenant ledger with sig",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed data model: AuditLog {id, tenantId, requestId, modelVersion, timestamp, latencyMs, policiesHit[], piiRedacted, redactionStrategy, queryHash}. Ingest: apply regex-based PII redaction, then sto",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design robust, low-latency failover systems with per-tenant isolation, SLA guarantees, and auditable behavior. It demonstrates understanding of distributed systems patterns and operational rigor.\n\n## Key Concepts\n- **Per-tenant routing**: Isolated routing tables ensure tenant-specific primary/secondary assignments\n- **Health checks**: Continuous monitoring with configurable thresholds and timeouts\n- **SLA adherence**: Latency budgets and availability guarantees per tenant\n- **Failover determinism**: Predictable state transitions based on clear criteria\n- **Audit trails**: Complete logging of state changes for compliance\n- **Cooldown mechanics**: Prevent rapid flapping between primary and secondary\n\n## Code Example\n```javascript\n// Failover decision engine\nfunction route(tenant, healthStatus, config) {\n  const tenantConfig = config.tenants[tenant];\n  const primary = healthStatus[tenantConfig.primary];\n  const secondary = healthStatus[tenantConfig.secondary];\n\n  if (isHealthy(primary)) return tenantConfig.primary;\n  if (isHealthy(secondary)) return tenantConfig.secondary;\n  \n  throw new Error('No healthy models available');\n}\n\nfunction isHealthy(model) {\n  return model.consecutiveFailures < 2 && \n         model.latencyMs < 2000 && \n         model.status === 'UP';\n}\n```\n\n## Follow-up Questions\n- How would you simulate failures and measure p95 latency during switchover?\n- How to extend for tenants with multiple secondary models?\n- What cooldown period prevents rapid failover flapping?\n- How do you handle partial failures where only some requests fail?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-17",
        "questionId": "q-10008",
        "question": "Design a multi-tenant Retrieval-Augmented Generation (RAG) layer for an LLM-ops gateway serving NVIDIA and OpenAI tenants. The system must use per-tenant knowledge bases stored regionally, enforce per-tenant data governance (PII redaction, source filtering), support TTL-based cache eviction of retrieved contexts, and achieve p95 latency < 180 ms in bursts. Describe data models, cache layering (edge/regional/central), failure modes, and a concrete test plan with synthetic KB drift and prompt surges?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a geo-aware, fault-tolerant LLM-ops gateway routing prompts to tenant-region workers, using tenant KMS/HSM and envelope encryption. Ensure per-tenant data residency, tamper-evident deterministi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant latency budgets with model-slot routing. Data: TenantPolicy {tenantId,budgetMs,backlogLimit,quarantineUntil}; ModelVariants {fast,balanced,accurate} with latency estimates. Router selects f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implementation would separate a per-tenant KB catalog (tenantId, region, sourcePolicy, ttl) and a context cache layered at edge, regional, and central, with TTL eviction and provenance tagging. Enforc",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-tenant budgeting (tokens consumed per prompt) with a daily reset. Routing rules: if remainingBudget > 50% and latency < 80ms, route to fast; if remainingBudget > 20% and latency < 120ms,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical RAG integration, data locality, privacy controls, and latency under burst traffic. It also tests multi-tenant cache design, provenance, and failure handling.\n\n## Key Concepts\n\n- Retrieval-Augmented Generation (RAG) in multi-tenant contexts\n- Per-tenant governance, PII redaction, and source filtering\n- Multi-layered caching (edge/regional/central) with TTL eviction\n- Provenance tagging and drift/dataset update handling\n\n## Code Example\n\n```javascript\n// Pseudocode: gate retrieval by policy\nfunction shouldRetrieve(source, tenant) {\n  const policy = tenant.sourcePolicy\n  return policy.allows(source) && !source.isPII && !source.isConfidential\n}\n```\n\n## Follow-up Questions\n\n- How would you detect KB drift and trigger partial recomputation without harming latency?\n- What telemetry and rollback strategy would you implement for KB updates in a live gateway?",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-18",
        "questionId": "q-3930",
        "question": "Design a cost-aware, multi-tenant LLM routing system that assigns prompts to model variants (base, tuned, safety-guarded) based on per-tenant budgets and risk profiles. Explain the decision engine, data-minimization, and per-tenant policy enforcement. Ensure ≤200ms latency, deterministic fail-safe routing for violations, and describe a minimal synthetic tenant dataset for validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant budget, risk score, and a 200ms latency target drive routing. Implement a policy graph where prompts are evaluated for safety and relevance, then a decision engine assigns a variant: base,",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Adopt per-tenant HMAC keys stored in a KMS. Clients sign prompts with {tenantId, nonce, payload} using HMAC-SHA256; gateway verifies the signature and checks a nonce store to prevent replay. Rotate ke",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an end-to-end design that maps tenants to experiments, tracks per-request context, and isolates data. Include data models, a deterministic assignment method, simple APIs, and a lightweight tes",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant daily budget and per-request token cap with a two-model router (primary high-cost, fallback cheap). Track spend per tenant and reset daily; cost = tokens × modelRate. If budget exhaus",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design a cost-aware, policy-driven routing system at scale.\n\n## Key Concepts\n\n- Per-tenant budget/risk policy\n- Real-time routing decision engine\n- Latency guarantees and warm pools\n- Minimal provenance logging and data-minimization\n\n## Code Example\n\n```javascript\n// Pseudocode sketch for routing decision\nfunction routePrompt(p, tenant){\n  const policy = policyGraph[tenant];\n  if (tenant.budgetRemaining <= 0) return 'deny';\n  const v = chooseVariant(policy, p);\n  return v;\n}\n```\n\n## Follow-up Questions\n\n- How would you test drift in risk scoring across tenants?\n- What metrics would you instrument for SLA and cost adherence?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-19",
        "questionId": "q-12609",
        "question": "Design a beginner-friendly tenant-aware model routing policy for a multi-tenant LLM gateway used by HashiCorp and Uber. Given two models (fast-lightweight and accuracy-heavy), implement per-tenant SLAs to select model, and a simple in-memory cache to reduce latency. Describe data model, routing logic, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Latency-Aware Per-Tenant Routing Policy An LLM-ops gateway routes requests across four execution paths: (a) base hosted model, (b) tenant-fine-tuned model in a dedicated enclave, (c) prompt masking and post-processin…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Define per-tenant policy object with tenantId, slaMs, budgetMs. Routing: if current latency <= slaMs and budget remains, use m1 (fast); else fallback to m2 (accurate). Add an in-memory LRU cache keyed",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Each tenant has a sensitivity profile (PII/PHI level). Build screening with: 1) deterministic RGX/keyword checks, 2) a compact ML detector on edge for false positives, 3) redact or reject actions, wit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a per-tenant policy engine invoked before forwarding prompts. Each tenant provides a DSL (redact emails, strip SSNs, mask tokens). Implement a lightweight sanitizer (Go) with a tiny interpreter",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical, beginner-friendly routing in a multi-tenant LLM gateway and introduces per-tenant SLAs with a simple cache to meet latency budgets.\n\n## Key Concepts\n- Per-tenant SLA and budget-aware model routing\n- Lightweight in-memory caching (LRU) for prompts\n- Latency tracking, cost accounting, and observability\n- Data model design for TenantPolicy and CacheEntries\n\n## Code Example\n```javascript\n// Minimal routing example\nfunction selectModel(tenantPolicy, lastLatencyMs, budgetLeftMs) {\n  if (lastLatencyMs <= tenantPolicy.slaMs && budgetLeftMs > 0) {\n    return 'm1'; // fast\n  }\n  return 'm2'; // accurate\n}\n```\n\n## Follow-up Questions\n- How would you test cache coherence when SLA changes for a tenant?\n- How would you extend this to handle bursty traffic without blowing budget?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-20",
        "questionId": "q-11713",
        "question": "Design a lightweight per-tenant prompt sanitizer for a beginner-friendly multi-tenant LLM gateway. The system must detect and neutralize prompt injection attempts, enforce tenant-specific allowed patterns, preserve user intent, and audit sanitized prompts without exposing PII. Define API surface, data model, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant, hot-updatable policies in a Policy Engine; a Regional Router mapping tenants to a ModelVariant (base, privacy-guarded, locality-bound) by region; strict data locality checks to ensure prom",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant canary rollout: route a configurable percent to targetVersion and the rest to currentVersion. Persist rollout state per tenant (currentVersion, targetVersion, canaryPercent, sta",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lightweight per-tenant prompt sanitizer middleware that runs before the LLM. Use tenant-specific allowlists (regexes) and a blacklist of risky tokens; replace disallowed content with place",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-tenant redaction policy engine with versioned JSONPath templates. Store policies in a regional, hot-swappable config service; at request arrival, evaluate tenantId, fetch policyVersion",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nIntroduce a practical anti-prompt-injection exercise with tenant-scoped controls in a multi-tenant LLM gateway.\n\n## Key Concepts\n- prompt sanitization\n- per-tenant allowlists/regex\n- audit logging with redaction\n- sandboxed evaluation and timeouts\n\n## Code Example\n```javascript\nfunction sanitize(prompt, rules) {\n  // apply allowlist regexes per tenant\n  // replace disallowed tokens\n  return prompt;\n}\n```\n\n## Follow-up Questions\n- How would you test for injection patterns across tenants?\n- How would you handle false positives and user friction?\n- How would you scale the sanitizer as tenants grow and patterns evolve?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-21",
        "questionId": "q-26978",
        "question": "Design a streaming LLM-ops gateway for multi-tenant real-time chat. Each tenant has a distinct SLA (latency and tokens/sec) and messages must preserve order within a session, support per-message streaming tokens, and guarantee exactly-once delivery across retries. Provide data model, streaming protocol, backpressure strategy, and a minimal test plan with two tenants: TenantA: 500 ms SLA, 2000 tokens/sec; TenantB: 1000 ms SLA, 500 tokens/sec?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a dedicated WebSocket connection per session, enforce per-tenant rate limiting using token buckets calibrated to each tenant's SLA (500 ms latency / 2000 tokens/sec for TenantA; 1000 ms latency / 500 tokens/se…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a per-tenant policy firewall that sits between prompts, post-processing, and model variants. Policy-as-code (OPA/Rego) defines allowed content, data locality, and retention. Track end-to-end",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-tenant token bucket with fields tenantId, tokens, lastRefill, maxBurst; limit concurrentRequests. On each prompt, check bucket, decrement tokens, if tokens exhausted enqueue or route to fall",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a real-time governance layer that computes per-tenant KPIs (latency, toxicity, policy-violation rate) and adjusts routing. Use rolling-window drift detectors (KL divergence/KS test) on outpu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Matters\n\nThis question evaluates understanding of streaming LLM-ops infrastructure, per-tenant SLA enforcement, in-order delivery guarantees, and exactly-once semantics under retries. It also probes knowledge of backpressure mechanisms, pagination, and observability in a production multi-tenant gateway.\n\n## Core Concepts\n\n- Streaming protocols with per-session sequencing\n- Per-tenant rate limiting and backpressure strategies\n- Exactly-once delivery via deduplication and commit IDs\n- Durable logs with replay capability for audits\n\n## Data Model\n\n| Component | Description |\n|-----------|-------------|\n| `Tenant` | `id`, `sla_latency_ms`, `rate_limit_tokens_per_sec`, `web_socket_pool_size` |\n| `Session` | `id`, `tenant_id`, `created_at`, `status`, `sequence_counter` |\n| `Message` | `id`, `session_id`, `seq_num`, `idempotency_key`, `commit_id`, `content`, `status` |\n| `TokenBucket` | `tenant_id`, `capacity`, `refill_rate`, `last_refill_ts` |\n\n## Streaming Protocol\n\n1. Client establishes WebSocket with `tenant_id` and `session_id`.\n2. Server assigns monotonic `seq_num` on connect.\n3. Each message carries `idempotency_key` for deduplication and `commit_id` for exactly-once acknowledgment.\n4. Client retries include original `idempotency_key`; server skips duplicates within the deduplication window.\n5. Server streams tokens incrementally with `DONE` marker and `commit_id` in final response.\n\n## Backpressure Strategy\n\n- **Tenant-level**: Token bucket per tenant; excess requests queue with priority based on SLA tier.\n- **Session-level**: Output buffer with configurable high-water mark; emit `FLOW_CONTROL` frame when 80% full.\n- **Client-side**: Honor `RETRY-AFTER` headers; exponential backoff with jitter.\n- **Circuit breaker**: If downstream LLM API latency exceeds tenant SLA, route to fallback or return cached response.\n\n## Test Plan\n\n| Test | TenantA (500ms, 2000 tok/s) | TenantB (1000ms, 500 tok/s) | Expected Result |\n|------|---------------------------|------------------------------|------------------|\n| Latency under load | 100 concurrent sessions | 100 concurrent sessions | Both meet SLA within 5th-95th percentile |\n| Token rate adherence | Burst to 3000 tok/s | Burst to 800 tok/s | Excess tokens queued or rejected per bucket |\n| Message ordering | 1000 messages, 10% reorder injection | Same | 100% in-order delivery |\n| Exactly-once delivery | 5 retries per message with same idempotency key | Same | Single delivery confirmed |\n| Backpressure trigger | 50 sessions × 100 msg/s | Same | FLOW_CONTROL frames emitted, no drops |\n| Multi-tenant isolation | TenantA at max rate while TenantB idle | Same | TenantA throughput unaffected by TenantB load |\n\n## Implementation Example\n\n```javascript\n// Token bucket for per-tenant rate limiting\nclass TokenBucket {\n  constructor(capacity, refillRate) {\n    this.capacity = capacity;\n    this.tokens = capacity;\n    this.refillRate = refillRate;\n    this.lastRefill = Date.now();\n  }\n\n  tryConsume(tokens) {\n    this.refill();\n    if (this.tokens >= tokens) {\n      this.tokens -= tokens;\n      return true;\n    }\n    return false;\n  }\n\n  refill() {\n    const now = Date.now();\n    const elapsed = (now - this.lastRefill) / 1000;\n    this.tokens = Math.min(this.capacity, this.tokens + elapsed * this.refillRate);\n    this.lastRefill = now;\n  }\n}\n\n// Message with deduplication support\nconst processMessage = async (msg, tenantBuckets, deduplicationWindow) => {\n  const { sessionId, seqNum, idempotencyKey, content } = msg;\n\n  // Deduplication check\n  if (deduplicationWindow.has(idempotencyKey)) {\n    return { status: 'duplicate', commitId: deduplicationWindow.get(idempotencyKey) };\n  }\n\n  // Rate limit check\n  const bucket = tenantBuckets.get(msg.tenantId);\n  if (!bucket.tryConsume(msg.content.length)) {\n    return { status: 'rate_limited', retryAfter: 100 };\n  }\n\n  // Process and assign commit ID\n  const commitId = generateCommitId();\n  deduplicationWindow.set(idempotencyKey, commitId);\n\n  return { status: 'processed', commitId, seqNum };\n};\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-22",
        "questionId": "q-28861",
        "question": "Design a multi-tenant llm-ops gateway feature that dynamically routes prompts based on per-tenant privacy regimes and data locality requirements, with real-time policy evaluation and fallback to a default path. Include a policy language, a routing decision tree, and a minimal test plan with two tenants: FinanceCo (EU data stays local, strict minimization) and AdTechCo (US allowed, lower minimization)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant policy engine that evaluates data locality, minimization requirements, and regional constraints in real time, then routes prompts to either a tenant-specific sandbox, a masked-input shared model,…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a per-brand, hot-swappable steering layer between gateway and models. JSON-based policies enforce safety and tone per brand; compute a per-prompt risk score in under 20 ms; policy versions are",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a per-tenant data-provenance and privacy pipeline in a multi-cloud LLM gateway. Tag prompts/responses with tenantId and policyVersion, redact PII before any caching or streaming, enforce per",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy a progressive canary deployment workflow that routes the new modelVersion to a small, privacy-isolated subset of tenants; continuously monitor per-tenant latency, error rate, policy violations, and potential PII…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to reason about policy-driven routing, data locality, and cross-tenant isolation in real-time LLM operations.\n\n## Key Concepts\n\n- **Policy engines** for LLM-ops routing that evaluate per-tenant rules dynamically\n- **Data locality constraints** ensuring prompts stay within approved geographic boundaries\n- **Privacy regimes** handling varying minimization requirements across tenants\n- **Tenant isolation** maintaining strict separation between tenant data flows\n- **Auditability** logging all routing decisions for compliance verification\n- **Latency targets** with graceful fallback to default paths when policy evaluation fails\n\n## Policy Language (DSL)\n\n```javascript\n// Example policy DSL\ntenantPolicy = {\n  tenantId: \"FinanceCo\",\n  dataResidency: \"EU\",\n  minimizationLevel: \"strict\",\n  allowedRegions: [\"EU\"],\n  allowSharedModel: false,\n  fallbackPath: \"local-deployment\"\n}\n```\n\n## Routing Decision Tree\n\n```javascript\nfunction route(prompt, tenantPolicy, region) {\n  // Step 1: Check if policy engine is available\n  if (!tenantPolicy) {\n    return { target: 'default-path', reason: 'no-policy' };\n  }\n\n  // Step 2: Validate region compliance\n  if (!tenantPolicy.allowedRegions.includes(region)) {\n    return { target: 'default-path', reason: 'region-violation' };\n  }\n\n  // Step 3: Determine routing based on privacy posture\n  if (!tenantPolicy.allowSharedModel) {\n    return { target: 'tenant-sandbox', model: tenantPolicy.dedicatedModel };\n  }\n\n  // Step 4: Apply minimization level\n  if (tenantPolicy.minimizationLevel === 'strict') {\n    return { target: 'local-deployment', model: 'on-prem-llm' };\n  }\n\n  // Step 5: Default to shared model with input masking\n  return { target: 'shared-model', inputMasking: true };\n}\n```\n\n## Test Plan\n\n### Tenant 1: FinanceCo (EU, Strict Minimization)\n| Test Case | Input | Expected Output |\n|-----------|-------|-----------------|\n| Region compliance | Prompt, region=EU | Route to tenant-sandbox |\n| Region violation | Prompt, region=US | Fallback to default-path |\n| Shared model request | allowSharedModel=true | Reject, use dedicated model |\n\n### Tenant 2: AdTechCo (US, Lower Minimization)\n| Test Case | Input | Expected Output |\n|-----------|-------|-----------------|\n| Region compliance | Prompt, region=US | Route to shared-model with masking |\n| Shared model allowed | allowSharedModel=true | Use shared infrastructure |\n| Minimization relaxed | minimizationLevel=low | Allow PII in prompts |\n\n### Integration Tests\n- **Policy evaluation latency**: Must complete within 50ms\n- **Fallback behavior**: On policy engine failure, route to default path\n- **Audit logging**: All routing decisions logged with tenant ID, timestamp, and rationale",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-23",
        "questionId": "q-23714",
        "question": "Design a beginner-friendly llm-ops gateway feature for per-tenant cost-aware routing: tenants declare per-prompt budget and latency; gateway chooses among three model variants (fast, balanced, accurate) based on per-token costs and target latency, ensuring deterministic routing for identical prompts. Define policy schema, routing logic, budget-exhaustion handling, and a minimal test plan with a scenario (AcmeFin cap $0.50/prompt, 120ms)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant prompt governance layer that parses prompts into a tool-usage plan, consults a policy engine, and enforces per-tenant tool allowlists, data-scope, and request-rate quotas before",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Router design: per-tenant policy stores budget_per_prompt, max_latency_ms, and allowed_variants; each variant has token_cost and empirical latency. Pick the cheapest variant whose estimated cost <= bu",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Create a pre-routing sanitization module that uses per-tenant policyVersion to detect PII with lightweight classifiers, redacts or tokenizes sensitive fields, and emits a sanitized prompt for routing",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant RAG layer with region-bound embeddings, tenant-scoped vector stores, and isolated caches. Enforce policy-driven source selection (allowed sources, PII filters) and TTL eviction.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nGauges practical application of per-tenant constraints in llm-ops, focusing on cost and latency trade-offs in real routing decisions.\n\n## Key Concepts\n\n- Cost-aware routing and per-tenant budgets\n- Latency-aware model selection\n- Deterministic routing for identical prompts\n- Budget-exhaustion handling and fallbacks\n\n## Code Example\n\n```javascript\nfunction selectVariant(p, policy, costs, latencies) {\n  // p: prompt, policy: {budget, latency, allowed}, costs: {fast, balanced, accurate}, latencies: {...}\n  // returns chosen variant or null\n}\n```\n\n## Follow-up Questions\n\n- How would you test determinism across identical prompts?\n- How would you monitor and alert on per-tenant budget overruns and latency violations?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-24",
        "questionId": "q-13656",
        "question": "Design a beginner-friendly, per-tenant content-safety policy engine for a multi-tenant LLM gateway used by a chat app (Discord-like). The engine must allow per-tenant overrides (allowedKeywords, disallowedTerms, redactionRules), run at ingestion and again before model output within a 100 ms latency budget, redact PII in prompts and responses, and produce auditable logs. Provide data models, a minimal API, and a concrete test plan with a sample tenant configuration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant FIFO queues and a central dispatcher. Each tenant_id maps to its own asyncio.Queue; a single scheduler pulls from all tenants in a round-robin with prioritization by arrival time, dispatchi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant policy bundles stored in an encrypted policy store; a policy engine evaluates prompts with redaction rules, provenance checks, and leakage prevention. Route to tenant-specific poo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define a lightweight TenantPolicy (tenantId, allowedModels, redaction, latencyBudget) and a routing gate that reads the policy, selects ModelA or ModelB, applies per-tenant redaction rules before send",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly, per-tenant content-safety policy engine for a multi-tenant LLM gateway used by a chat application (Discord-like). The engine must support per-tenant overrides (allowedKeywords, disallowedTerm…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your ability to design a practical LLM Ops solution that balances security, performance, and multi-tenancy requirements. It tests understanding of policy enforcement, latency optimization, and compliance logging in real-world applications.\n\n## Key Concepts\n- Per-tenant policy registry ensuring tenant isolation\n- Dual-stage evaluation: ingestion filtering and pre-response validation\n- PII detection and redaction using configurable rules\n- Versioned policies with simple feature flag management\n- Efficient audit trails for compliance and monitoring\n- Sub-100ms latency through optimized string operations and caching\n\n## Code Example\n```javascript\nfunction evaluateContent(content, policy) {\n  // Apply allowed/disallowed keyword checks\n  // Execute PII redaction patterns\n  // Log violations with tenant context\n  // Return processed content and policy decisions\n}\n```\n\n## Follow-up Questions\n- How would you test latency under load while maintaining policy enforcement?\n- What strategies would you use to handle policy updates without impacting active requests?\n- How would you scale this engine across multiple gateway instances?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-08T06:44:03.941Z",
    "version": 1
  }
]