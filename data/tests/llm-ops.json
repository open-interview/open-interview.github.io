[
  {
    "id": "test-llm-ops",
    "channelId": "llm-ops",
    "channelName": "LLM OPS",
    "title": "LLM OPS Knowledge Test",
    "description": "Test your understanding of LLM OPS with 25 questions.",
    "questions": [
      {
        "id": "llm-ops-q-0",
        "questionId": "q-29437",
        "question": "Design a beginner-friendly llm-ops gateway feature that adds per-tenant provenance stamping: attach a cryptographic proof (HMAC) of the prompt, model version, and timestamp to the response logs, and expose a queryable audit API. Include data model, key management (rotation), latency budget (e.g., 50 ms extra p95), and a minimal test plan with two tenants (Slack, Salesforce) to demonstrate tamper-evidence and isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a latency-aware, per-tenant gateway that negotiates a policy contract at session start, then routes prompts to tenant backends (local model, vendor API, or ensemble) using latency budgets. I",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant provenance stamping using HMAC-SHA256 with dedicated keys ensures cryptographic integrity and tenant isolation. Each response log captures the tenant ID, prompt hash, model version, timestamp, and cryptograph…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a per-tenant append-only log store using S3 with Object Lock (WORM) for immutable storage, with DynamoDB for metadata indexing only. Each log entry includes: tenant_id, prompt_payload, model_variant, routing_deci…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement edge-first routing: if prompt <= 128 tokens and contains no PII, run on-device via TensorFlow Lite; else route to cloud. Key parts: client SDK, edge inference runner, policy module, cloud en",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design tamper-evident, tenant-isolated audit trails with minimal latency overhead. It tests understanding of cryptographic signing, key lifecycle management, and API design for compliance-focused systems.\n\n## Key Concepts\n- **Per-tenant HMAC keys**: Each tenant receives a unique cryptographic key, ensuring isolation and preventing cross-tenant signature forgery.\n- **Key rotation without downtime**: Implement key versioning with a configurable grace period—new logs use the latest key version while existing logs remain verifiable with their original key until the grace period expires.\n- **Tamper-evident verification**: The audit API recalculates HMACs on demand and compares them against stored signatures, instantly detecting any unauthorized log modifications.\n- **Latency budget management**: HMAC computation adds minimal overhead (~50 ms p95), which should be factored into overall response SLAs and monitored continuously.",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-1",
        "questionId": "q-4652",
        "question": "Design a cryptographic provenance and data locality framework for a multi-tenant LLM-ops gateway that ensures per-tenant prompt provenance, attestations, and tamper-evident auditing while meeting region-local data handling and latency targets. Outline the crypto stack, data-partitioning, tenant onboarding, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a lightweight per-tenant prompt-filtering service that evaluates and sanitizes prompts before LLM invocation. Data models: TenantPolicy(tenantId, allowedCategories[], redactRules, version) and PromptEvent(eventId…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant keys encrypt and sign prompts; prompts are attested in a trusted enclave before inference. Region-bound KMS keys enforce data locality. Maintain tamper-evident logs (append-only with HMAC).",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a runtime prompt-injection and output-safety mechanism for a multi-tenant LLM-ops gateway that serves Airbnb, OpenAI, and Instacart. The system must detect and block jailbreak and exfiltration attempts that leak…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant request queue with three priorities. The gateway should guarantee bounded latency for Gold, enforce fair scheduling within each tier (WFQ or weighted RR), and defer Bronze durin",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluate knowledge of cryptographic provenance, data locality, and auditable ML workflows in multi-tenant, cloud-agnostic environments. Tests ability to reason about key management, TEEs, and detection of data leakage under load.\n\n## Key Concepts\n- Verifiable provenance with per-tenant keys\n- Attested inference in a trusted enclave\n- Region-bound data locality and KMS\n- Tamper-evident audit logs and rotation\n\n## Code Example\n```javascript\n// Example: sign a payload with a per-tenant key (simplified)\nconst crypto = require('crypto');\nfunction signPayload(payload, key) {\n  const sign = crypto.createSign('SHA256');\n  sign.update(JSON.stringify(payload));\n  sign.end();\n  return sign.sign(key, 'base64');\n}\n```\n\n## Follow-up Questions\n- How would you rotate keys without downtime?\n- How would you measure and ensure latency impact and leakage risk?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-2",
        "questionId": "q-17267",
        "question": "Design a prompt provenance and policy-versioning layer for a multi-tenant LLM gateway used by Square and Snowflake-scale tenants. The system must (a) attach a cryptographic provenance stamp to every prompt and response, (b) support per-tenant policy versions with immutability and role-based access, (c) enable audit-ready drift detection between policy versions, and (d) maintain p95 latency under burst load while preserving data locality. Explain data models, signing scheme, audit/logging, and a minimal validation plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use OpenTelemetry with a W3C traceparent header to propagate a tenantId as a trace attribute across gateway, router, model service, and logger. Enforce PII redaction at each log point via a redact fun",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant budgeted routing across models/providers, with latency caps and SLA guarantees. Maintain a policy store (tenant, max_cost, max_latency, preferred_models, regions). A routing engine uses rea",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-tenant, versioned policy catalog (immutable after publish) and a cryptographic provenance stamp (JWS) on every prompt and response. The stamp includes tenantId, promptHash, policyVersion, ti",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a per-tenant shadow routing feature: non-blocking duplicate evaluation of prompts on a secondary model to collect latency and accuracy signals without affecting user response times. Data model:",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the candidate's ability to implement trustworthy, auditable prompts in a multi-tenant LLM gateway with strict latency and locality requirements. It also probes data governance, cryptographic provenance, and drift-detection strategies at scale.\n\n## Key Concepts\n- Per-tenant policy catalogs with immutable versions\n- Cryptographic provenance (JWS) for prompts/responses\n- KMS-backed signing and verification\n- Append-only audit logging and drift detection\n- Data locality-aware routing and latency targets\n\n## Code Example\n```javascript\n// Pseudocode: stamp creation on prompt\nconst stamp = signProvenance({ tenantId, promptHash, policyVersion, ts }, kmsKey);\nconst payload = { prompt, stamp };\n```\n\n## Follow-up Questions\n- How would you handle policy revocation and stamp revocation?\n- How would you test drift detection without impacting live traffic?",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-3",
        "questionId": "q-28708",
        "question": "Design a cost-aware, multi-tenant LLM-ops gateway that routes prompts to three model backends with different pricing and latency profiles. Tenants have budgets and SLAs (e.g., Robinhood-level, MongoDB-like, Snowflake-like). Implement per-tenant budget accounting, dynamic routing that favors cheaper models during bursts while preserving policy-compliant responses, fair queuing, and auditable cost logs. Include data model, routing algorithm, failover, and a minimal test plan with two tenants and budget scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant RAG policy engine with: 1) per-tenant data-source whitelist and fetch-window age, 2) per-tenant budget/cost cap that gates vector DB usage, 3) per-tenant retrieval stack selection",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant budget tracking, SLA-aware routing, and deterministic fallbacks across three backends. Use a cost function c(model) and remaining budget to select models; apply rate limiting and",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "## Data Plane (Request/Response Proxy Layer) **Tenant Tagging Middleware** - Intercept all LLM requests at proxy layer (envoy/istio sidecar or custom gateway) - Tag requests with: `tenant_id`, `model_version`, `request_…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant token-bucket with daily quota and burst capacity. Tokens refill continuously; daily quota sets total tokens per day, burstCapacity enables short spikes. Implement in Redis with keys like te",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to design cost-aware routing and per-tenant budgeting under SLA constraints, addressing a gap not covered by existing prompts.\n\n## Key Concepts\n\n- Per-tenant budgeting and spend tracking\n- SLA-aware routing and latency targets\n- Multi-backend cost modeling and fallback\n- Auditable, tamper-evident cost logs\n\n## Code Example\n\n```javascript\n// Pseudo route function illustrating core decision logic\nfunction routePrompt(state, models) {\n  // state.tenant has budget, sla, burst\n  // choose model by minimizing predicted cost while meeting SLA\n  // ... implementation details ...\n}\n```\n\n## Follow-up Questions\n\n- How would you test drift in pricing across backends over time?\n- How do you handle new tenants with zero history without starving others?",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-4",
        "questionId": "q-20217",
        "question": "Design a per-tenant prompt-embedding cache for a multi-tenant LLM-ops gateway used by Instacart, Netflix, and Nvidia. The cache should store per-tenant prompt embeddings and model outputs to meet sub-10 ms cache hits at peak, while guaranteeing tenant isolation, policy-driven eviction on policy updates, and data locality across regions. Describe the data model, cache key schema, eviction strategy, hot updates, audit logging, and a minimal test plan to verify latency, correctness, and drift handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Solution Overview ### Policy Schema Each tenant defines their routing preferences through a policy configuration: ```json { \"tenant_id\": \"airbnb\", \"daily_budget\": 100.00, \"routing_preference\": \"balanced\", \"fallback_v…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a privacy-preserving prompt augmentation service in an LLM-ops gateway that injects per-tenant, differential-privacy preserving context before routing prompts to model variants. Tenants include Robinhood, Apple,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant, namespaced cache keyed by tenant_id|prompt_digest, storing embedding, response, policy_version, and expiry. Enforce isolation via Redis namespaces or separate caches per tenant",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a streaming risk-scoring engine that ingests per-tenant telemetry, computes composite risk scores from behavioral signals, content classification, and policy drift indicators, and drives a policy engine to admit…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical caching at LLM-ops scale, focusing on per-tenant isolation, policy-driven eviction, and data locality.\n\n## Key Concepts\n\n- Tenant-scoped cache keys and namespaces\n- Versioned policies and hot updates\n- Data locality and region-aware caching\n- Security: encryption in transit/rest, KMS\n- Drift handling and test strategies\n\n## Code Example\n\n```javascript\n// Cache key example\nfunction makeCacheKey(tenantId, prompt) {\n  const hash = sha256(prompt);\n  return `${tenantId}:${hash}`;\n}\n```\n\n## Follow-up Questions\n\n- How would you validate eviction correctness under rapid policy updates?\n- How would you simulate burst traffic and measure tail latency?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-5",
        "questionId": "q-18662",
        "question": "Design a cost-aware, per-tenant adaptive inference planner for a multi-cloud LLM-ops gateway that routes prompts to model variants across NVIDIA, Discord, and Microsoft workloads, optimizing latency SLAs while minimizing cost. Include per-tenant SLAs, dynamic batching, canary routing, fallback, data locality, and a minimal test plan to validate performance and budget constraints?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant SLA-aware scheduler that scores each model variant by latency and cost, selects the fastest variant that satisfies the tenant's budget, and batches prompts across tenants when safe. Include",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a per-tenant risk-aware model-router that uses a policy engine (OPA) to match tenant risk appetite, latency budgets, and hallucination scores to model metadata (cost, reliability). Route to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant budget ledger (Redis) with monthly reset. Cost per request = (tokens / 1000) * rate(model). On each prompt, deduct from the tenant’s budget; if remaining budget falls below a sa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose audit_id = tenant_id|timestamp_ms|nonce. Propagate it through gateway, model call, and all logs; store in an immutable AuditLog with audit_id, tenant_id, model_version, timestamps, prompt_hash",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes ability to design cross-cloud, cost-aware LLM routing with strict SLAs and per-tenant budgets in a real-world, multi-tenant gateway.\n\n## Key Concepts\n\n- Per-tenant SLAs and budgets\n- Online latency and cost estimation per model variant\n- Dynamic batching across tenants\n- Canary rollout and safe fallback\n- Data locality and egress cost\n- Observability and rollback\n\n## Code Example\n\n```javascript\nfunction scoreVariant(latencyMs, costPerToken, sla) { \n  // naive score: prioritize SLA, then cost, then latency\n  const penalty = latencyMs/1000;\n  return sla ? (costPerToken * 0.6 + penalty * 0.4) : Number.MAX_SAFE_INTEGER;\n}\n```\n\n## Follow-up Questions\n\n- What metrics would you track for drift and cost overruns?\n- How would you validate canary rollouts at scale without affecting tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-6",
        "questionId": "q-4092",
        "question": "Design a beginner-friendly LLM-ops gateway feature for per-tenant prompt provenance and auditability. Implement a lightweight provenance store (append-only) recording tenantId, promptHash, modelVariant, timestamp, and responseId; expose an audit API with tenant-scoped access and an integrity check (hash chain). Include a minimal data model, a sample record, and a basic test plan to verify tamper-evidence and privacy constraints?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant provenance service: an append-only ledger keyed by tenantId that records promptHash, modelVariant, timestamp, and responseId. Build an audit API with tenant-scoped access and a hash chain for tamper-…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement horizontal scaling with a load balancer and GPU auto-scaling. Leverage vLLM's tensor parallelism across multiple GPUs, configure max_batch_size=32, enable continuous batching, and add Redis for request queuing.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant audit and provenance pipeline for an LLM gateway that signs every prompt/response and policy decision with tenant-specific keys, emits tamper-evident logs, and builds a data lin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Develop a per-tenant, cost-aware routing policy that maps each prompt to one of three model variants (base, tuned, safety-guarded) based on remaining monthly budget and latency targets (<200 ms p95).",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests auditability, data integrity, and privacy in multi-tenant LLM-ops. A practical, scalable provenance layer is essential for compliance and debugging.\n\n## Key Concepts\n- Append-only provenance ledger\n- Hash chaining for tamper-evidence\n- Tenant-scoped audit API\n- Privacy controls for prompts\n\n## Code Example\n```javascript\ntype ProvenanceRecord = {\n  tenantId: string;\n  promptHash: string;\n  modelVariant: string;\n  timestamp: string;\n  responseId: string;\n  // optional: dataRetentionId\n}\n```\n\n## Follow-up Questions\n- How would you enforce access control on the audit API?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-7",
        "questionId": "q-3317",
        "question": "In a multi-tenant LLM gateway serving tenants like Cloudflare and Airbnb, design a per-tenant prompt provenance and model-output provenance system with tamper-evident, append-only logs. Describe the data model, log routing, cryptographic hash chaining, and how you'd verify integrity at query time while keeping latency under 200 ms. Include tenant data isolation and retention considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a beginner-friendly, per-tenant content-safety policy engine for a multi-tenant LLM gateway used by a chat application (Discord-like). The engine must support per-tenant overrides (allowedKeywords, disallowedTerm…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a comprehensive per-tenant provenance system with these components: **Data Model**: Separate logical and physical schemas per tenant with immutable logs. Each record: tenant_id, request_id, prompt_hash, input_pro…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Route requests by pre-checking TenantBudget and Quota, then select ModelSpec to meet SLA and cost. Data models: TenantBudget{tenantId,balance,resetWindow}, ModelSpec{modelVersion,gpuTier,maxLatency},",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant policy engine (OPA) with a lightweight DSL for allowed_topics, require_disclaimer, and min_confidence. Gate at the inference router; disallowed prompts are routed to a safe fallback,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design tamper-evident, multi-tenant audit systems with cryptographic guarantees while meeting strict performance requirements.\n\n## Key Concepts\n- Per-tenant cryptographic isolation and signing\n- Append-only hash chains with Merkle trees\n- Real-time verification with caching strategies\n- Data locality and cross-region replication\n- Retention policies with legal hold capabilities\n- Performance budgeting under 200ms latency\n\n## Code Example\n```go\n// Hash chain verification with caching\nfunc verifyProvenance(tenantID string, fromSeq, toSeq uint64) (bool, error) {\n    // Check cache first for sub-50ms latency\n    if result := cache.Get(tenantID, fromSeq, toSeq); result != nil {\n        return result.valid, nil\n    }\n    \n    // Batch fetch from distributed log\n    records, err := logStore.FetchRange(tenantID, fromSeq, toSeq)\n    if err != nil {\n        return false, err\n    }\n    \n    // Verify hash chain sequentially\n    prevHash := records[0].PrevHash\n    for _, record := range records {\n        computed := sha256(prevHash + record.Payload)\n        if computed != record.Hash {\n            return false, nil\n        }\n        prevHash = record.Hash\n    }\n    \n    // Cache result for future queries\n    cache.Set(tenantID, fromSeq, toSeq, true)\n    return true, nil\n}\n```\n\n## Follow-up Questions\n- How would you handle tenant key rotation without breaking existing hash chains?\n- What's your strategy for proving correctness during partial network partitions?\n- How do you optimize Merkle tree updates for high-throughput scenarios?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-8",
        "questionId": "q-27212",
        "question": "Design a multi-tenant llm-ops gateway feature that enforces per-tenant data residency and cost budgets by dynamically routing prompts to region-local models and a shared global model, with strict isolation and auditable routing decisions. Include a policy DSL, data plane routing, latency targets, and a minimal test plan for two tenants: US residency 2k tokens/day and EU residency 3k tokens/day?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a versioned CapStore with tenantId, policyVersion, and features {text,code,image}. A sidecar router consults CapStore before routing; if image is disabled, route to a text/code backend only",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant token bucket in Redis, keyed by tenantId, with tokens = monthlyBudget/pricePerToken. Refill daily, enforce tokens per prompt, and grant burst credits for short spikes. Apply bac",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lightweight per-tenant routing policy: PolicyStore maps tenantId to budgetTier and dataLocality; Router uses routePrompt(tenantId, meta) to pick modelVersion (e.g., free -> v1, paid -> v2)",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a tenant-aware router enforcing data residency and budget via a policy DSL and per-tenant routing table. Route prompts to region-local models when residency requires; otherwise use a shared gl",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nInterview context explanation.\n\n## Key Concepts\n- Data residency enforcement\n- Multi-region routing\n- Per-tenant budgets and quotas\n- Auditable, deterministic routing\n- Latency targets and failover\n\n## Code Example\n```javascript\n// Example pseudo-DSL for policy\ntenantPolicy\n  id: \"tenant-us\"\n  residency: \"US\"\n  budget: 2000\n  route: \"region-local\"\n```\n\n## Follow-up Questions\n- How would you handle model versioning across regions for governance?\n- How to measure and enforce per-tenant budget accuracy under burst traffic?",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-9",
        "questionId": "q-28105",
        "question": "Design a beginner-friendly llm-ops feature: per-tenant prompt versioning and deterministic replay for audits. When a prompt is submitted, assign a per-tenant version, record a deterministic hash of input and output under that version, and enable replay without storing raw data. Include data schema, versioning rules, an audit API, and a minimal test plan with two tenants (ShopX, RegTech) and two versions each?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant keys encrypt and sign prompts; prompts are attested in a trusted enclave before inference. Region-bound KMS keys enforce data locality. Maintain tamper-evident logs (append-only with HMAC).",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage region-specific model pools with dual versions, progressive canaries (e.g., 5/20/75%), and a gate that halts on SLA breach. Pre-warm new shards, perform rolling upgrades, and roll back automa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement strict tenant isolation through per-tenant sandboxes and streaming boundaries; attach tamper-evident provenance metadata to every prompt (tenantId, policyVersion, timestamp, hash(prompt)); encrypt prompts at r…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a tenant-scoped prompt versioning system to enable deterministic audit replays. Each submission increments a per-tenant version; store a deterministic hash (HMAC-SHA256) of input and output",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests auditing, determinism, and privacy within a simple llm-ops workflow, ensuring a practical path to compliant replays without exposing raw data.\n\n## Key Concepts\n\n- Tenant-scoped versioning\n- Deterministic hashing with per-tenant salt\n- Audit replay API and sandboxed execution\n- Privacy: store hashes, not raw prompts/outputs\n- Version retention and storage strategy\n\n## Code Example\n\n```typescript\nimport crypto from 'crypto';\nfunction hashPrompt(input: string, salt: string): string {\n  const h = crypto.createHmac('sha256', salt);\n  h.update(input);\n  return h.digest('hex');\n}\n```\n\n## Follow-up Questions\n\n- How would you purge old versions?\n- How would you test for hash collisions and replay fidelity?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-10",
        "questionId": "q-17511",
        "question": "For a beginner-friendly multi-tenant LLM gateway used by Stripe and Two Sigma, design a per-tenant budget and quota system that tracks tokens consumed daily and enforces a hard limit. Describe data models (TenantAccount, UsageRecord), a simple API to push usage, and routing rules: under budget route normally, near budget switch to fast/cached, over budget refuse with safe fallback. Include a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a tenant-isolated prompt-protection layer between the gateway and model that includes: (a) real-time detection of prompt-injection and data exfiltration attacks, (b) PII redaction with per-tenant privacy polic…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Data Models** ```javascript // TenantAccount: Tracks budget and usage per tenant { tenantId: \"tenant_abc123\", dailyBudget: 1_000_000, // tokens per day tokensUsed: 750_000, // current day's consumption lastReset: \"202…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a per-tenant provenance service: an append-only ledger keyed by tenantId that records promptHash, modelVariant, timestamp, and responseId. Build an audit API with tenant-scoped access and a hash chain for tamper-…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant batch queues keyed by tenantId, with a global batch window (5–15 ms) to group prompts by urgency, then dispatch to base, tuned, or safety-guarded variants. Meter per-tenant budgets and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses your ability to translate budget enforcement into a concrete architecture with per-tenant isolation and auditable usage tracking—a common real-world constraint in multi-tenant LLM gateways.\n\n## Key Concepts Demonstrated\n\n- **Token accounting**: Accurate per-tenant tracking with atomic updates\n- **Daily reset logic**: Idempotent usage pushes with timestamp-based resets\n- **Budget-aware routing**: Dynamic routing decisions based on usage state\n- **Safe fallbacks**: Graceful degradation when limits are exceeded\n\n## Design Considerations\n\n- Use `UsageRecord` as an immutable append-only log for audits\n- Implement `TenantAccount` with optimistic locking for concurrent updates\n- Consider a buffer zone (10% over budget) for near-budget scenarios to allow cached responses\n- Store `lastReset` to handle timezone-aware daily rollovers",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-11",
        "questionId": "q-17238",
        "question": "Design a multi-tenant LLM-ops gateway that dynamically routes prompts to tenant-specific backends (local model, vendor API, or ensemble) while enforcing per-tenant policy negotiation (privacy, redaction, latency budgets). Describe data flow from ingestion to backends, policy evaluation, circuit breakers, canary rollouts, caching, and audit logging, plus latency targets and failure handling under burst traffic?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-tenant token bucket with fields tenantId, tokens, lastRefill, maxBurst; limit concurrentRequests. On each prompt, check bucket, decrement tokens, if tokens exhausted enqueue or route to fall",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "This design implements a per-tenant, region-scoped RAG pipeline with strict data isolation. Embeddings and retrieved documents reside in tenantId+region-specific vector stores; queries remain within those boundaries and…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant idempotency token system using Redis as the underlying cache with TTL-based eviction. The data model consists of IdempotencyRecord(tenantId, token, promptHash, modelVersion, timestamp, resultHash)…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a latency-aware, per-tenant gateway that negotiates a policy contract at session start, then routes prompts to tenant backends (local model, vendor API, or ensemble) using latency budgets. I",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a scalable, policy-driven routing gateway that handles multi-tenant privacy, performance, and reliability under burst traffic.\n\n## Key Concepts\n- Per-tenant policy contracts and versioning\n- Latency-aware routing and budget enforcement\n- Circuit breakers, backpressure, and burst handling\n- Canary rollouts and drift rollback strategies\n- Tamper-evident auditing and data locality\n\n## Code Example\n```python\n# Pseudo-code: select backend based on tenant policy and current load\ndef select_backend(tenant_id, policy, load):\n    backends = get_backends_for_tenant(tenant_id, policy)\n    # choose with latency budget and current load considered\n    return choose_best_backend(backends, policy.latency_budget, load)\n```\n\n## Follow-up Questions\n- How would you measure policy drift and trigger rollback?\n- What metrics define acceptable burst performance and SLA guarantees?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-12",
        "questionId": "q-1695",
        "question": "Design a compliant, auditable LLM inference pipeline for a regulated financial platform that must deliver per-transaction data lineage, prompt provenance, and immutable end-to-end logging with 24-month retention while keeping latency under 150 ms end-to-end. Describe data flows, encryption, storage, and how you test for data leakage and drift?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route through per-tenant gateway enforcing strict policy, attach lineage: tenant_id, txn_id, prompt_digest, model_version; stream logs to an append-only store with S3 Object Lock and KMS, plus a hash",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a tenant-scoped LRU cache with 2-5s TTL for short prompts, 30-60s for longer contexts, keyed by tenant_id, model_variant, and prompt_hash, plus a per-tenant budget token. Route to the nearest",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a cross-tenant orchestration with a model-version registry and per-tenant policy store. Route via an Envoy-based router that supports atomic swaps using feature flags; pin data locality to regi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a governance layer with a per-tenant policy registry (versioned), an evaluation pipeline that sanitizes prompts, enforces allow/deny rules, and gates retrieval augmentation. Isolate tenants via",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design auditable, low-latency LLM infra for regulated domains. It probes data lineage, provenance, immutability, retention, and leakage/drift controls.\n\n## Key Concepts\n\n- Per-tenant isolation and policy enforcement\n- Immutable, tamper-evident logging with provenance\n- End-to-end latency budgeting and micro-batching\n- Leakage testing and drift detection\n\n## Code Example\n\n```javascript\n// Pseudo log entry for audit\nconst log = {\n  tenantId: 'T123',\n  txnId: 'TXN456',\n  promptDigest: 'abc123',\n  modelVersion: 'v2.1',\n  timestamp: Date.now()\n};\n```\n\n## Follow-up Questions\n\n- How would you test retention using legal holds and egress audits?\n- How would you integrate this with your CI/CD pipelines?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-13",
        "questionId": "q-24275",
        "question": "Design a per-tenant prompt provenance layer for an LLM-ops gateway that guarantees cryptographic attestations of the processing path (prompt, filter set, routing, model version) without leaking tenant data. Include tamper-evident, append-only logs, replayable paths for audits, data locality, and a test plan ensuring audit verifiability under cross-tenant resource sharing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant provenance layer with a tamper-evident, append-only audit trail. Each prompt processing request generates a signed, cryptographically chained log entry containing the prompt hash, filters applied, ro…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a streaming sanitizer stack before request routing: (i) per-tenant policy version selects the appropriate filter chain; (ii) redact PII and confidential data using regex patterns and NER models; (iii) emit tam…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a SLA-based fairness gate: tenants declare latency_target_ms and max_tokens; gateway enforces per-tenant queues and uses a simple weighted round-robin scheduler with a cap on queue depth and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a per-tenant confidential compute gateway: prompts execute inside attested enclaves (SGX/SEV) with per-tenant KMS keys; inputs/outputs are envelope encrypted and sealed to the enclave; impleme",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates the candidate's understanding of cryptographic provenance, auditability, and secure multi-tenant architecture in LLM operations. It assesses the ability to design systems that maintain data integrity and enable compliance verification without compromising tenant isolation.\n\n## Key Concepts\n\n- **Per-tenant log chaining and signing**: Each tenant maintains isolated cryptographic keys in their dedicated KMS, ensuring complete isolation of audit trails while enabling cross-tenant verification when authorized.\n\n- **Tamper-evident append-only storage**: Utilizing technologies like Write-Ahead Logging (WAL) or blockchain-inspired structures ensures that once logged, entries cannot be modified without detection, providing cryptographic proof of data integrity.\n\n- **Replayable audit paths**: The system maintains sufficient state information to reconstruct any prompt's complete processing journey, enabling auditors to verify that specific filters, routing decisions, and model versions were applied in the correct sequence.\n\n- **Data locality and tenant isolation**: Geographic boundaries and tenant-specific storage partitions ensure sensitive data remains within designated regions while maintaining strict separation between tenants.\n\n## Evaluation Criteria\n\nStrong candidates should demonstrate understanding of: cryptographic hash chains, Merkle tree-based audit proofs, KMS integration patterns, zero-knowledge proofs for privacy-preserving audits, and compliance frameworks governing multi-tenant LLM deployments.",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-14",
        "questionId": "q-8822",
        "question": "Design a per-tenant prompt provenance and data-lineage system for a multi-tenant LLM gateway used by Netflix, Meta, and Microsoft-scale workloads. The system must capture tenantId, modelVersion, policyVersion, promptHash, timestamp, and outputs; provide cryptographic signing, immutable logs, per-tenant retention, and fast audit queries. Define data model, APIs, storage layout, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an end-to-end prompt routing and transformation system where tenants supply WebAssembly plugins for on-the-fly prompt shaping (e.g., redaction, normalization, or policy-enforced constraints). The router must enfo…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a provenance service with an append-only log, per-tenant namespace, and cryptographic signing. Record: tenantId, modelVersion, policyVersion, promptHash, timestamp, responseHash, and output",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a per-tenant router that (1) selects a model tier based on budget cap and SLA, (2) batches prompts with tenant-aware queues to maximize throughput, (3) enforces quotas via token-bucket budge",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant audit and provenance pipeline for an LLM gateway that signs every prompt/response and policy decision with tenant-specific keys, emits tamper-evident logs, and builds a data lin",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDesigning a robust provenance system tests auditability, security, and data locality under multi-tenant load.\n\n## Key Concepts\n- Append-only logs, per-tenant isolation, cryptographic signing\n- Immutable storage, retention, and efficient lineage queries\n- API design for lineage retrieval and compliance\n\n## Code Example\n```javascript\nfunction signEntry(entry, key){\n  // pseudo-signing for immutability\n  return HMACSHA256(key, JSON.stringify(entry));\n}\n```\n\n## Follow-up Questions\n- How would you handle key rotation for tenant keys without breaking lineage?\n- How would you scale to 100k tenants and retain latency for audit queries?",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-15",
        "questionId": "q-11800",
        "question": "Design a beginner-friendly request routing policy for a multi-tenant LLM gateway that, given per-tenant budgets and SLA targets, selects a model version and hardware tier for each request. Include data models (TenantBudget, ModelSpec, Quota), an API surface to update budgets and quotas, and a minimal test plan. Provide a concrete burst scenario where a tenant exceeds budget and explain the fallback behavior?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant telemetry with OpenTelemetry, tagging spans by tenant_id and region. Redact inputs/outputs before logging, store only hashed tokens or token-level counts. Use sampling (e.g., 5–10",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Define a lightweight TenantPolicy (tenantId, allowedModels, redaction, latencyBudget) and a routing gate that reads the policy, selects ModelA or ModelB, applies per-tenant redaction rules before send",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route requests by pre-checking TenantBudget and Quota, then select ModelSpec to meet SLA and cost. Data models: TenantBudget{tenantId,balance,resetWindow}, ModelSpec{modelVersion,gpuTier,maxLatency},",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement request batching with dynamic batch sizes, use TensorRT-LLM with model parallelism across GPU clusters, employ two-level routing with fast-path for cached embeddings, and leverage KEDA autoscaling with GPU uti…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical routing decisions using simple budget models and SLA constraints.\n\n## Key Concepts\n- Budget-aware routing\n- Per-tenant quotas\n- SLA-driven model selection\n- API for budgets and quotas\n\n## Code Example\n```javascript\nclass TenantBudget { constructor(tenantId, balance, resetWindow) { this.tenantId = tenantId; this.balance = balance; this.resetWindow = resetWindow; } }\nclass ModelSpec { constructor(modelVersion, gpuTier, maxLatency) { this.modelVersion = modelVersion; this.gpuTier = gpuTier; this.maxLatency = maxLatency; } }\nclass Quota { constructor(tokenLimit, window) { this.tokenLimit = tokenLimit; this.window = window; } }\n``` \n\n## Follow-up Questions\n- How would you test budget drift and SLA violations under load?\n- How would you extend to multi-region tenants with cross-region quotas?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-16",
        "questionId": "q-18779",
        "question": "Design a beginner-friendly 'prompt containment' feature for a multi-tenant LLM gateway (tenants include Hugging Face and Meta). It must filter disallowed content in prompts, apply per-tenant policy to decide whether to block, redact, or pass prompts, and route to a chosen model variant. Provide policy schema, data model, routing logic, and a minimal test plan with a concrete example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement a per-tenant policy store (TenantID → Policy{blocks, redacts, routeModel, maxLatency}), a lightweight preprocessor (regex-based redaction for PII, keyword blocks), and a router that enfo",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a per-request provenance graph: tenant_id, request_id, input_hash, timestamp, stages, model_id/version, transformation logs, data_residency tag. Append immutably to a per-tenant ledger with sig",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a tenant-aware prompt profiler in a multi-tenant LLM-ops gateway. It logs per-tenant latency, token usage, and output distributions; uses a sliding window to detect drift; enforces per-tenan",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly telemetry pipeline for a multi-tenant LLM gateway. Capture per-request latency, tokenCount, modelVersion, and outcome; log tenantId in an audit path while masking it in stan",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to specify per-tenant containment and safe routing, a core llm-ops skill.\n\n## Key Concepts\n- Per-tenant policy isolation\n- Prompt containment and redaction\n- Routing to model variants\n- Atomic policy updates and testability\n\n## Code Example\n```javascript\nfunction applyPolicy(tenantId, prompt) {\n  const policy = policyStore[tenantId];\n  if (policy?.blocks?.some(b => prompt.includes(b))) return {blocked: true};\n  let redacted = prompt;\n  policy?.redacts?.forEach(r => {\n    redacted = redacted.replace(new RegExp(r, 'g'), '[REDACTED]');\n  });\n  const routeModel = policy?.routeModel ?? 'balanced';\n  return {blocked: false, redactedPrompt: redacted, routeModel};\n}\n```\n\n## Follow-up Questions\n- How would you test race conditions during policy updates?\n- How would you extend the policy model to support multi-tenant auditing without leaking tenant data?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-17",
        "questionId": "q-5870",
        "question": "Design a real-time, tenant-aware prompt optimization and safety gating layer between a multi-tenant LLM gateway and three model variants (base, domain-tuned, privacy-preserving). The layer must select a model per-tenant per-prompt via a lightweight classifier, apply per-tenant pre- and post-inference filters, ensure strict data locality during model swaps with zero cross-tenant reads, and support canary rollouts. Provide architecture, data model, and a concrete validation plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A per-tenant drift engine that continuously compares model outputs to policy envelopes and redaction rules, and auto-suggests safe policy updates. Implement versioned rollouts with canaries, isolation",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant policy schema (tenant_id, retention_days, redaction_rules, provenance_scope, data_location). Implement policy-eval middleware that computes a deterministic prompt_hash, applies redaction, r",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant policy DSL and a light, latency-aware prompt classifier that selects one of three model variants. Enforce pre/post filters in a sidecar, isolate data with tenant-scoped encrypti",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a cost-aware routing engine for a multi-tenant LLM gateway (IBM/Stripe) that, per prompt, selects among models with different costs and latencies, plus a cache. Include per-tenant budgets, SLA",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design end-to-end tenant isolation in dynamic model routing and safety enforcement, including data locality and canary deployment, a real-world pain point.\n\n## Key Concepts\n\n- Per-tenant policy modeling\n- Lightweight per-prompt routing classifier\n- Pre/post-inference safety filters\n- Data locality and tenant isolation during upgrades\n- Canary rollout and rollback strategies\n\n## Code Example\n\n```javascript\n// Pseudo policy evaluation\nfunction selectModelForTenant(tenantId, prompt) {\n  const policy = loadPolicy(tenantId); // DSL to rules\n  const intent = classifyIntent(prompt);\n  return policy.chooseModel(intent, tenantId);\n}\n```\n\n## Follow-up Questions\n\n- How would you measure data-leak risk during model swap?\n- How would you implement drift detection for policy changes?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-18",
        "questionId": "q-6533",
        "question": "Design a lightweight per-tenant anomaly detector for a multi-tenant LLM gateway used by Salesforce and Tesla. The detector should flag unusual latency or token usage compared to per-tenant baselines using simple, rule-based thresholds (no ML). Describe the data model (TenantBaseline, AnomalyEvent), how baselines are initialized and updated, where metrics are stored, how alerts are delivered, and how you handle new tenants and data gaps. Provide a concrete example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Maintain a per-tenant routing table: tenantId, latencyBudgetMs, privacyConstraint, allowedModels. RouteChoice = model with shortest estimated latency that fits budget and privacy policy. Log routing e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a per-tenant policy engine routing prompts to base/tuned/safety-guarded models while enforcing data locality, per-tenant redaction rules, and auditable tamper-evident logs. Support streaming p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Store per-tenant baselines (tenantId, latencyMs, tokenCount, lastUpdated). On each request log latency and tokenCount, compare to the tenant baseline; raise AnomalyEvent if latency > 1.5x baseline or",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a privacy-preserving prompt augmentation service in an LLM-ops gateway that injects per-tenant, differential-privacy preserving context before routing prompts to model variants. Tenants include Robinhood, Apple,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical llm-ops monitoring: simple, per-tenant anomaly checks, alerting, and handling cold starts without ML.\n\n## Key Concepts\n- Per-tenant baselines\n- Rule-based anomaly detection\n- Telemetry storage and retention\n- Alerting and incident response\n- Cold-start handling\n\n## Code Example\n```python\ndef is_anomaly(current_latency, baseline_latency, current_tokens, baseline_tokens, lat_thresh=1.5, tok_thresh=1.8):\n    return current_latency > lat_thresh * baseline_latency or current_tokens > tok_thresh * baseline_tokens\n```\n\n## Follow-up Questions\n- How would you scale baselines to thousands of tenants with drift?\n- How would you test alerts using replayed traffic and synthetic tenants?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-19",
        "questionId": "q-9272",
        "question": "Design a beginner-friendly canary rollout workflow for per-tenant safety policy updates in a multi-tenant LLM gateway used by Tesla and Discord. Specify the API surface to publish updates, data models (PolicyVersion, TenantPolicyBinding), rollout steps (canary cohort sizes, monitoring metrics, thresholds, rollback), and a minimal test plan including privacy and latency checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "API surface: POST /policies/canary with {policyVersion, canaryTenantIds, rolloutPct, canaryWindowMs, forceRollout}. Data models: PolicyVersion(version, rules, createdAt); TenantPolicyBinding(tenantId,",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a tenant-scoped prompt versioning system to enable deterministic audit replays. Each submission increments a per-tenant version; store a deterministic hash (HMAC-SHA256) of input and output",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant token bucket in Redis, keyed by tenantId, with tokens = monthlyBudget/pricePerToken. Refill daily, enforce tokens per prompt, and grant burst credits for short spikes. Apply bac",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Cross-Tenant Prompt Deduplication and Response Caching Layer ### Data Model **Cache Key Structure:** ``` {tenant_id}:{model_version}:{policy_signature}:{prompt_semantic_hash} ``` **Redis Namespace per Tenant:** ``` c…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design safe, incremental policy updates in a multi-tenant LLM system, with concrete APIs, schemas, and governance. Emphasizes observable rollout, rollback criteria, privacy considerations, and a minimal validation path.\n\n## Key Concepts\n- Canary rollout cadence and escalation\n- Data models for versioned policies and tenant bindings\n- Telemetry and SLA monitoring during updates\n- Privacy in audit trails during migrations\n\n## Code Example\n```javascript\n// Pseudo-merge example\nfunction mergePolicy(current, next) {\n  // simple overlay with versioning\n  return { ...current, ...next, version: next.version };\n}\n```\n\n## Follow-up Questions\n- How would you ensure cross-region policy consistency during canary?\n- How do you determine safe thresholds for rollout increment and rollback triggers?",
        "difficulty": "beginner"
      },
      {
        "id": "llm-ops-q-20",
        "questionId": "q-25301",
        "question": "In a multi-tenant LLM-ops gateway servicing finance, social, and health tenants, design a prompt-sanitization and isolation scheme that prevents prompt injection and data leakage across tenants. Propose per-tenant sanitization rules, a deterministic, replayable execution path, cryptographically signed logs, and a test plan to prove no cross-tenant leakage and latency <200 ms p95?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant explainability microservice that selects technique by policy (token attribution, feature importance, or policy rationale). Run explainers in isolated containers, avoid cross-tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant policy DSL (safetyLevel, allowedTools, redTeamChecks) and a runtime policy engine that enforces rules at the entry point before routing to models. Add HMAC-signed audit logs, pe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant prompt sanitizer pipeline that strips or escapes injection vectors, enforces a bounded prompt alphabet, and uses token-level redaction for system prompts. Run prompts in sandbox",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-tenant Policy Engine at the router: tag requests with region, apply per-tenant redaction (regex + light-weight model) before routing, and enforce quotas. Use versioned JSON policies wi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nExplores security, isolation, and tracing in a real multi-tenant LLM-ops gateway.\n\n## Key Concepts\n- Prompt sanitization and policy enforcement\n- Tenant isolation and per-tenant cryptographic logs\n- Deterministic replayability and auditing\n- Latency and security trade-offs\n\n## Code Example\n```javascript\n// Pseudo: sanitizePrompt function outline\nfunction sanitizePrompt(prompt, policy){/*...*/}\n```\n\n## Follow-up Questions\n- How would you test for data leakage under gradual policy drift?\n- What metrics would you collect for auditability and latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-21",
        "questionId": "q-27668",
        "question": "Design a real-time, edge-deployed policy negotiation and enforcement subsystem for a multi-tenant LLM-ops gateway. It must support per-tenant privacy settings (PII redaction, data minimization), versioned hot-updatable policies, and drift alerts, all while meeting strict latency (p95 < 150 ms) and data locality constraints. Describe data flow, evaluation, auditing, and a minimal test plan across three tenants?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## LLM-Ops Gateway Failover Policy ### Policy Schema ```json { \"tenants\": { \"TenantA\": { \"region\": \"us-east\", \"primary\": \"fast\", \"secondary\": \"balanced\", \"sla\": { \"maxLatencyMs\": 2000, \"minAvailability\": 0.99 }, \"redact…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe an edge-resident policy engine that fetches per-tenant, versioned policy bundles, caches them locally, and enforces redaction and data-min rules at both request and response boundaries, with",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Tenant-scoped cache (Redis namespace) keyed by hash(tenantId+prompt) with TTL 60s and LRU eviction; per-tenant model pool with regional affinity; a dynamic policy layer enforcing regulatory rules to p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a streaming gate that sits between prompt ingestion and model inference, keyed by tenant, applying per-tenant policy for PII masking, log-minimization, and prompt lineage. Use a well-defined e",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to architect an edge, low-latency policy enforcement layer with per-tenant privacy controls, hot policy updates, and drift detection in a multi-tenant LLM-ops gateway. Emphasizes data locality, auditability, and resilience.\n\n## Key Concepts\n\n- Edge policy engine with per-tenant versioning\n- Privacy controls: PII redaction, data minimization\n- Hot-updatable policies and drift alerts\n- Latency budgets and data locality guarantees\n- Immutable audit logs\n\n## Code Example\n\n```javascript\n// Pseudo-implementation sketch\nfunction authorize(tenantId, payload) {\n  const policy = PolicyStore.get(tenantId).current;\n  const allowed = policy.allows(payload);\n  AuditLog.log(tenantId, payload, allowed);\n  return allowed;\n}\n```\n\n## Follow-up Questions\n\n- How would you validate policy drift across deployments?\n- What tests ensure <150 ms p95 for policy eval in edge nodes?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-22",
        "questionId": "q-22258",
        "question": "Design a per-tenant prompt validation gateway that runs every incoming prompt through a deterministic validator before routing to models. Enforce per-tenant privacy rules (PII redaction, data minimization), guardrails against prompt injection, and policy-based redaction. Describe architecture, validation rules, test plan for determinism, latency (<200 ms p95), and auditable logs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant validator service that normalizes prompts, applies tenant-specific privacy redactions (PII, sensitive fields), blocks injection patterns, and tags prompts for routing. Ensure de",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a prompt-injection resilient routing layer for a multi-tenant LLM-ops gateway serving Meta, Square, and Snap. **(a) Per-Tenant Detectors (Regex + ML):** Implement a two-stage detector per tenant. Stage one applie…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "An append-only ledger with per-tenant keys and ED25519 signatures for each entry. Hash prompts and outputs with SHA-256; store tenant_id, prompt_hash, model_variant, latency, outputs_hash. Redact sens",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a per-tenant data-provenance and privacy pipeline in a multi-cloud LLM gateway. Tag prompts/responses with tenantId and policyVersion, redact PII before any caching or streaming, enforce per",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical skills in enforcing privacy, security, and auditability in a multi-tenant LLM-ops gateway. It requires concrete architecture and measurable guarantees beyond high-level design.\n\n## Key Concepts\n\n- Per-tenant policy enforcement\n- Deterministic validation and replayability\n- PII detection and redaction\n- Prompt-injection guardrails\n- Latency budgeting and tamper-evident logging\n\n## Code Example\n\n```javascript\nfunction validatePrompt(prompt, policy) {\n  // normalize\n  let p = prompt.trim();\n  // redaction rules (pseudo)\n  policy.redactionPatterns.forEach(r => p = p.replace(r, 'REDACTED'));\n  // injection guard (very simple placeholder)\n  const blocked = policy.injectionPatterns.some(pat => new RegExp(pat, 'i').test(p));\n  return { valid: !blocked, redacted: p };\n}\n```\n\n## Follow-up Questions\n\n- How would you test policy coverage across tenants with different privacy rules?\n- How would you handle policy updates without affecting in-flight prompts?",
        "difficulty": "intermediate"
      },
      {
        "id": "llm-ops-q-23",
        "questionId": "q-10925",
        "question": "Architect a policy drift and auto-correction subsystem for a multi-tenant LLM-ops gateway: it continuously monitors per-tenant policy compliance, detects drift between allowed and observed outputs, and auto-generates safe policy updates with versioned rollouts, canary testing, isolation, and per-tenant audit trails. Describe API, data models, drift metrics, rollback strategy, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a per-tenant A/B rollout framework: tenants opt-in via a flag; route a small, deterministic fraction to a new model version (canary) using tenantId hashing; grow/shrink gradually. Collect per-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-tenant templating layer that composes prompt transformations from a DSL policy: redact or rewrite prompts, enforce regulatory constraints (data localization, licensing), cap tokens, and b",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A per-tenant drift engine that continuously compares model outputs to policy envelopes and redaction rules, and auto-suggests safe policy updates. Implement versioned rollouts with canaries, isolation",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Production optimization combines quantization (8-bit/4-bit), pruning, and distillation. Static quantization offers 2-4x speedup with minimal accuracy loss (<2%), while dynamic quantization provides better compatibility…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExplores how to automate governance at scale, handle drift, and safely deploy policy changes without cross-tenant leakage. It tests API design, data models, risk controls, and rollback discipline.\n\n## Key Concepts\n\n- Policy drift detection and auto-correction\n- Versioned policy rollouts and canaries\n- Tenant isolation and immutable audit trails\n\n## Code Example\n\n```javascript\n// Pseudo-implementation sketch for drift detector (not runnable)\nclass DriftDetector {\n  constructor(policyStore) { this.store = policyStore }\n  detect(output, policy) { /* compare and return drift score */ }\n}\n```\n\n## Follow-up Questions\n\n- How would you debounce drift signals to avoid flapping updates?\n- What metrics would you publish to SRE dashboards and how would you alert on them?",
        "difficulty": "advanced"
      },
      {
        "id": "llm-ops-q-24",
        "questionId": "q-25042",
        "question": "Design a beginner-friendly llm-ops gateway failover policy: when a tenant's primary model becomes unhealthy, automatically route to a secondary model, preserving per-tenant SLA, redaction, and audit logs. Define the policy schema, health checks, failover criteria, and a minimal test plan with a concrete tenant scenario (TenantA in us-east) where primary fast fails and requests must land on secondary balanced?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## LLM-Ops Gateway Failover Policy ### Policy Schema ```json { \"tenants\": { \"TenantA\": { \"region\": \"us-east\", \"primary\": \"fast\", \"secondary\": \"balanced\", \"sla\": { \"maxLatencyMs\": 2000, \"minAvailability\": 0.99 }, \"redact…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a runtime prompt provenance and leakage-resilience system for a multi-tenant LLM gateway. Requirements: per-prompt provenance with versioned policies and system messages, privacy-preserving met",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant policy schema: {tenantId, maxTokens, bannedTokens[], allowParaphrase}. Mutation steps: ban check, deterministic paraphrase when allowed, then truncation to maxTokens; provide a sa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To start, require every prompt carries tenantId, intent, and nonce via a lightweight per-tenant template engine. Data model: {tenantId, intent, template, nonce, ts}. Preprocess: load tenant template,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design robust, low-latency failover systems with per-tenant isolation, SLA guarantees, and auditable behavior. It demonstrates understanding of distributed systems patterns and operational rigor.\n\n## Key Concepts\n- **Per-tenant routing**: Isolated routing tables ensure tenant-specific primary/secondary assignments\n- **Health checks**: Continuous monitoring with configurable thresholds and timeouts\n- **SLA adherence**: Latency budgets and availability guarantees per tenant\n- **Failover determinism**: Predictable state transitions based on clear criteria\n- **Audit trails**: Complete logging of state changes for compliance\n- **Cooldown mechanics**: Prevent rapid flapping between primary and secondary\n\n## Code Example\n```javascript\n// Failover decision engine\nfunction route(tenant, healthStatus, config) {\n  const tenantConfig = config.tenants[tenant];\n  const primary = healthStatus[tenantConfig.primary];\n  const secondary = healthStatus[tenantConfig.secondary];\n\n  if (isHealthy(primary)) return tenantConfig.primary;\n  if (isHealthy(secondary)) return tenantConfig.secondary;\n  \n  throw new Error('No healthy models available');\n}\n\nfunction isHealthy(model) {\n  return model.consecutiveFailures < 2 && \n         model.latencyMs < 2000 && \n         model.status === 'UP';\n}\n```\n\n## Follow-up Questions\n- How would you simulate failures and measure p95 latency during switchover?\n- How to extend for tenants with multiple secondary models?\n- What cooldown period prevents rapid failover flapping?\n- How do you handle partial failures where only some requests fail?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-28T10:56:01.077Z",
    "version": 1
  }
]