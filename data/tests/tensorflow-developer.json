[
  {
    "id": "test-tensorflow-developer",
    "channelId": "tensorflow-developer",
    "channelName": "Tensorflow Developer",
    "title": "Tensorflow Developer Knowledge Test",
    "description": "Test your understanding of Tensorflow Developer with 25 questions.",
    "questions": [
      {
        "id": "tensorflow-developer-q-0",
        "questionId": "q-13424",
        "question": "On a 16-GPU Kubernetes cluster using tf.distribute.MultiWorkerMirroredStrategy to train a Transformer-based translator, how would you implement gradient accumulation to simulate a larger global batch? Describe the approach, provide a minimal code snippet for accumulating gradients over N steps, explain learning-rate scaling, and how you would measure and ensure efficient scaling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Compute per-feature medians offline. Load data with tf.data.experimental.make_csv_dataset(batch_size=128). In preprocessing, replace NaNs with medians, then feed into a tf.keras.layers.Normalization (",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route requests to two isolated queues (A and B), each configured with model-specific BatchConfig settings. For model A (image classifier, 224×224, FP16), use max_batch_size=64 with batch_timeout_micros=1000 to handle la…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement with tf.distribute.MultiWorkerMirroredStrategy and a gradient-accumulator. Per-replica batch size b, global batch = b * replicas. Accumulate grads for model.trainable_variables over N steps",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a single TF Serving signature for inputs (image: float32[batch, h, w, c], context: float32[batch, ctx_dim]). Use Envoy to enforce per-tenant quotas, routing A,B to CPU and burst to GPU via c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of distributed TF2 training, gradient accumulation, synchronization across workers, and practical trade-offs for large global batch sizes.\n\n## Key Concepts\n- tf.distribute strategies (MultiWorkerMirroredStrategy)\n- gradient accumulation across steps\n- per-replica vs global batch sizing\n- learning-rate and loss scaling for accumulation\n- monitoring scaling efficiency and convergence\n\n## Code Example\n```python\nimport tensorflow as tf\nstrategy = tf.distribute.MultiWorkerMirroredStrategy()\nper_replica_batch = 4\naccum_steps = 8\n\nwith strategy.scope():\n    model = ...  # Transformer model\n    optimizer = tf.keras.optimizers.Adam(learning_rate=0.001)\n    loss_fn = tf.keras.losses.SparseCategoricalCrossentropy(from_logits=True)\n    accum_grads = [tf.Variable(tf.zeros_like(v), trainable=False) for v in model.trainable_variables]\n    step = tf.Variable(0, trainable=False)\n\n    @tf.function\n    def train_step(batch):\n        x, y = batch\n        with tf.GradientTape() as tape:\n            y_pred = model(x, training=True)\n            loss = loss_fn(y, y_pred)\n        grads = tape.gradient(loss, model.trainable_variables)\n        for a, g in zip(accum_grads, grads):\n            a.assign_add(g)\n        if (step.assign_add(1) % accum_steps) == 0:\n            grads_to_apply = [g / accum_steps for g in accum_grads]\n            optimizer.apply_gradients(zip(grads_to_apply, model.trainable_variables))\n            for a in accum_grads:\n                a.assign(tf.zeros_like(a))\n```\n\n## Follow-up Questions\n- How would you integrate gradient clipping with accumulation?\n- How would you validate linear scalability across all workers?",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-1",
        "questionId": "q-27859",
        "question": "You have a dataset of plant leaf images stored as file paths in a CSV with columns: id,image_path,label. Design a beginner TensorFlow 2.x solution (Keras Functional API) that: 1) builds a tf.data pipeline to load and decode JPEGs, 2) applies on-the-fly data augmentation (random flip, rotation, brightness) and resizes to 128x128, 3) normalizes pixel values to [0,1], 4) uses a small Conv2D-based model built with the Functional API, 5) trains with model.fit using SparseCategoricalCrossentropy, 6) evaluates on a holdout test split. Provide a minimal code sketch and rationale?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a shared embedding encoder plus two heads: a sigmoid sentiment classifier and a softmax category classifier. Compile with losses={'sentiment':'binary_crossentropy','category':'sparse_categorica",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use tf.distribute.MirroredStrategy across GPUs; set global_batch_size = per_replica_batch_size * strategy.num_replicas_in_sync. Wrap dataset with strategy.experimental_distribute_dataset and run insid",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement two per-model queues feeding a shared batcher; tune A with max_batch_size 16 and batch_timeout_micros 1000, B with max_batch_size 8 and 1500. Apply input-shape padding to align batches. Isol",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a tf.data pipeline that maps each CSV row to (image, label). Decode JPEG, resize to 128x128, apply random flip, rotation, brightness, and normalize to [0,1]. Build a tiny Conv2D model with the",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests end-to-end TF2 image data handling: building robust tf.data pipelines, applying lightweight on-the-fly augmentation, normalization, and a simple Functional API CNN suitable for beginners. It mirrors real-world needs at PayPal/Salesforce for efficient preprocessing and compact models.\n\n## Key Concepts\n- tf.data.pipeline for loading image paths and labels from CSV\n- tf.image augmentation (flip, rotation, brightness)\n- image resizing and normalization to [0,1]\n- Keras Functional API model construction\n- SparseCategoricalCrossentropy with from_logits\n\n## Code Example\n```python\nimport tensorflow as tf\nimport pandas as pd\n\n# Minimal loader (pseudocode-level sketch)\ndf = pd.read_csv('data.csv')\nimage_paths = df['image_path'].values\nlabels = df['label'].values\n\ndef load_and_preprocess(path, label):\n    img = tf.io.read_file(path)\n    img = tf.image.decode_jpeg(img, channels=3)\n    img = tf.image.resize(img, [128, 128])\n    img = tf.image.random_flip_left_right(img)\n    img = tf.image.random_brightness(img, 0.1)\n    img = img / 255.0\n    return img, label\n\nds = tf.data.Dataset.from_tensor_slices((image_paths, labels))\nds = ds.map(load_and_preprocess).batch(32).prefetch(tf.data.AUTOTUNE)\n\nnum_classes = int(labels.max()) + 1\ninputs = tf.keras.Input(shape=(128,128,3))\nx = tf.keras.layers.Conv2D(16, 3, activation='relu')(inputs)\nx = tf.keras.layers.MaxPooling2D()(x)\nx = tf.keras.layers.Conv2D(32, 3, activation='relu')(x)\nx = tf.keras.layers.MaxPooling2D()(x)\nx = tf.keras.layers.Flatten()(x)\nx = tf.keras.layers.Dense(64, activation='relu')(x)\noutputs = tf.keras.layers.Dense(num_classes)(x)\nmodel = tf.keras.Model(inputs, outputs)\nmodel.compile(optimizer='adam', loss=tf.keras.losses.SparseCategoricalCrossentropy(from_logits=True), metrics=['accuracy'])\nmodel.fit(ds, epochs=5)\n```\n\n## Follow-up Questions\n- How would you handle significant class imbalance in this dataset?\n- How would you add early stopping and checkpointing for robustness?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-2",
        "questionId": "q-16393",
        "question": "You have images organized as data/train/{class} and data/val/{class}. In TensorFlow 2.x, build a beginner-friendly tf.data pipeline (or image_dataset_from_directory) to load images, apply on-the-fly augmentations (random flip, brightness), resize to 128x128, batch, and train a small CNN. Include a minimal code snippet for the pipeline and model, plus evaluation basics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Train a simple autoencoder: embed event IDs, pass through a small encoder (Dense/Conv1D), bottleneck, then decoder to reconstruct. Use tf.data to produce windows of 30, batch 64; compile with MSE, fit",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use tf.keras.image_dataset_from_directory to load train/val, map augmentations with tf.image.random_flip_left_right and tf.image.random_brightness, resize to 128x128, batch, prefetch, then a small CNN",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Load data with image_dataset_from_directory for train and val (180x180, batch 32). Add augmentation (RandomFlip, RandomBrightness). Build a tiny CNN: Conv2D(32,3), MaxPool, Conv2D(64,3), MaxPool, Flat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a TextVectorization layer with max_tokens=5000 and output_mode='int', adapt on the text column, then an Embedding(input_dim=5000, output_dim=64, mask_zero=True), followed by GlobalAveragePooling1D",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical tf.data usage, basic image augmentation, and a straightforward CNN workflow a developer would implement in entry roles.\n\n## Key Concepts\n- tf.data pipelines, image_dataset_from_directory\n- on-the-fly augmentation, resizing, batching, prefetch\n- simple CNN with Conv2D, pooling, dense head; compile with Adam\n- evaluation: accuracy, confusion matrix\n\n## Code Example\n```python\nimport tensorflow as tf\ntrain_ds = tf.keras.preprocessing.image_dataset_from_directory(\n    'data/train', image_size=(128,128), batch_size=32)\nval_ds = tf.keras.preprocessing.image_dataset_from_directory(\n    'data/val', image_size=(128,128), batch_size=32)\n\ndef aug(x, y):\n    x = tf.image.random_flip_left_right(x)\n    x = tf.image.random_brightness(x, max_delta=0.2)\n    return x/255.0, y\ntrain_ds = train_ds.map(aug).prefetch(tf.data.AUTOTUNE)\nval_ds = val_ds.map(lambda x,y: (x/255.0, y)).prefetch(tf.data.AUTOTUNE)\n\nmodel = tf.keras.Sequential([\n  tf.keras.layers.Conv2D(32, 3, activation='relu', input_shape=(128,128,3)),\n  tf.keras.layers.MaxPooling2D(),\n  tf.keras.layers.Conv2D(64, 3, activation='relu'),\n  tf.keras.layers.MaxPooling2D(),\n  tf.keras.layers.Flatten(),\n  tf.keras.layers.Dense(128, activation='relu'),\n  tf.keras.layers.Dense(len(train_ds.class_names), activation='softmax')\n])\nmodel.compile(optimizer='adam', loss='sparse_categorical_crossentropy', metrics=['accuracy'])\nmodel.fit(train_ds, validation_data=val_ds, epochs=5)\n```\n\n## Follow-up Questions\n- How would you handle class imbalance in this setup?\n- If the dataset grows, how would you optimize the pipeline for throughput?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-3",
        "questionId": "q-22216",
        "question": "Advanced: You deploy two variants of a text classifier on TensorFlow Serving in Kubernetes: A - FP32 baseline, B - int8 quantized via TF-TRT. Requests include a routing header to pick the variant. Design a safe, zero-downtime canary with per-model batching, separate queues, and isolation to prevent head-of-line blocking. Your answer should cover: (1) per-model BatchConfig and input specs, (2) a dispatcher that routes by gRPC metadata, (3) a monitoring plan with latency targets and rollback criteria, and (4) a minimal model.config snippet plus a canary upgrade workflow?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two SavedModels: textcls_A FP32 baseline and textcls_B int8 TF-TRT. Each has its own BatchConfig and concrete input_spec. A Dispatcher reads gRPC metadata X-Variant to route and uses per-model queues",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Shard the embedding by vocab axis; create N shard variables; implement a custom Keras layer that routes IDs to their appropriate shard with integer division, gathers results, and concatenates. With tf",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route requests via a custom header (X-Model-Version) to two TensorFlow Serving backends, each hosting its own SavedModel with unique preprocessing logic. Implement Envoy as the routing layer to split traffic 70/30 betwe…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Shard data per-tenant with a tenant-id lookup, interleave two streams (images and captions), and zip into per-tenant batches. Shuffle with a tenant-specific seed for deterministic replay, and use tf.d",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a canary rollout for multi-variant TensorFlow Serving deployments, including routing by metadata, per-model batching, and safe upgrades under bursty traffic.\n\n## Key Concepts\n\n- Per-model BatchConfig and explicit input specs\n- Model versioning and zero-downtime upgrades\n- gRPC metadata-based routing and per-model queues\n- Canary metrics and rollback criteria\n\n## Code Example\n\n```javascript\n{\n  \"model_config_list\": [\n    {\"config\": {\"name\": \"textcls_A\", \"base_path\": \"/models/textcls_A/1\", \"model_config\": {\"batching\": {\"max_batch_size\": 32, \"batch_timeout_micros\": 1000}}}},\n    {\"config\": {\"name\": \"textcls_B\", \"base_path\": \"/models/textcls_B/1\", \"model_config\": {\"batching\": {\"max_batch_size\": 32, \"batch_timeout_micros\": 1000}}}}\n  ]\n}\n```\n\n## Follow-up Questions\n\n- How would you simulate traffic to validate the canary before full rollout?\n- What metrics and thresholds trigger rollback, and how would you implement automatic rollback?\n- How would you ensure memory pressure on quantized models does not affect FP32 variants in the same cluster?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-4",
        "questionId": "q-10316",
        "question": "You're given a toy CSV with columns city (string), bedrooms (int), sqft (int), price (float). Build a beginner TensorFlow 2.x model to predict price using a tf.data pipeline that reads CSV, preprocesses city with StringLookup and CategoryEncoding, concatenates with numeric features, and trains a small dense network. Provide preprocessing, model, and a minimal model.fit snippet. How would you handle unseen cities at inference?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Load data with image_dataset_from_directory for train and val (180x180, batch 32). Add augmentation (RandomFlip, RandomBrightness). Build a tiny CNN: Conv2D(32,3), MaxPool, Conv2D(64,3), MaxPool, Flat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use tf.distribute.MirroredStrategy across 4 GPUs. Bucket inputs by length in tf.data, accumulate micro-batches to reach the target batch size, enable mixed precision with a LossScaleOptimizer, and fix",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use tf.data.experimental.make_csv_dataset to load the CSV and map features to tensors with price as the label. Build a city vocabulary from the data and apply tf.keras.layers.StringLookup (with a reserved token for unse…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Assign per-replica offsets and a fixed random seed in preprocessing to guarantee deterministic sharding. Cache on local SSD per worker, keep windowing deterministic, and use a global batch size with p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests end-to-end ability to build a small regression model with tabular data, including preprocessing and a clean pipeline in TensorFlow 2.x.\n\n## Key Concepts\n- tf.data CSV loading with a simple feature/label split\n- Preprocessing layers: StringLookup and CategoryEncoding for categorical features\n- Feature concatenation of numeric and encoded categorical features\n- Small dense network for regression; mean squared error loss\n\n## Code Example\n```python\nimport tensorflow as tf\n\n# Load data\ncsv_ds = tf.data.experimental.make_csv_dataset(\n    file_pattern='data.csv', batch_size=32, label_name='price',\n    num_epochs=1, shuffle=True)\n\n# Extract unique cities for vocabulary\ncities = set()\nfor batch, _ in csv_ds:\n    cities.update(batch['city'].numpy())\ncity_vocab = list(cities)\n\n# Preprocessing layers\ncity_lookup = tf.keras.layers.StringLookup(\n    vocabulary=city_vocab, mask_token=None, num_oov_indices=1)\ncity_encoding = tf.keras.layers.CategoryEncoding(\n    max_tokens=len(city_vocab) + 1, output_mode='one_hot')\n\n# Build model\ncity_input = tf.keras.Input(shape=(), dtype=tf.string, name='city')\nnumeric_input = tf.keras.Input(shape=(2,), dtype=tf.float32, name='numeric')\n\ncity_encoded = city_encoding(city_lookup(city_input))\nfeatures = tf.keras.layers.Concatenate()([city_encoded, numeric_input])\n\nx = tf.keras.layers.Dense(64, activation='relu')(features)\nx = tf.keras.layers.Dense(32, activation='relu')(x)\noutput = tf.keras.layers.Dense(1)(x)\n\nmodel = tf.keras.Model(\n    inputs=[city_input, numeric_input], outputs=output)\nmodel.compile(optimizer='adam', loss='mse')\n\n# Training function\ndef preprocess(features, label):\n    return {\n        'city': features['city'],\n        'numeric': tf.stack([features['bedrooms'], features['sqft']], axis=1)\n    }, label\n\ntrain_ds = csv_ds.map(preprocess)\nmodel.fit(train_ds, epochs=10)\n```",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-5",
        "questionId": "q-5551",
        "question": "You have a small CSV with two columns: text and label. Build a beginner TensorFlow 2.x Keras pipeline that uses tf.keras.layers.TextVectorization to tokenize and map to integers, configured with max_tokens and output_mode='int', and adapts on the training data. Then feed into an Embedding and a GlobalAveragePooling1D, ending with a Dense(1, sigmoid). Include minimal code for the vectorizer setup and model wiring, and discuss basic tradeoffs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a TextVectorization layer with max_tokens=5000 and output_mode='int', adapt on the text column, then an Embedding(input_dim=5000, output_dim=64, mask_zero=True), followed by GlobalAveragePooling1D",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a tf.data pipeline that interleaves CSVs, forward-fills NaNs, normalizes per-feature, builds 32-step windows, and feeds them to a small Conv1D classifier with a sigmoid output. Train with Bina",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To bound latency under burst traffic, implement dynamic batching with a fixed window and sequence-length bucketing at the server edge. Use tf.data.experimental.bucket_by_sequence_length (or a custom b",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Parse CSV with tf.data, convert timestamp to seconds, assemble features [temp,hum,press,timestamp], apply a Normalization layer, create 16-step sliding windows to predict the next temp, build a small",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end text preprocessing in TensorFlow 2.x using Keras layers, avoiding external vocab files, and confirms adaptability to training data.\n\n## Key Concepts\n- TextVectorization setup and adapt\n- Embedding + GlobalAveragePooling1D for text\n- End-to-end model preprocessing and inference parity\n\n## Code Example\n```python\n# Example wiring in Python\ntext_ds = tf.data.Dataset.from_tensor_slices(train_texts)\nvectorizer = tf.keras.layers.TextVectorization(max_tokens=5000, output_mode='int', split='whitespace', standardize=None)\nvectorizer.adapt(text_ds)\nmodel = tf.keras.Sequential([\n  vectorizer,\n  tf.keras.layers.Embedding(5000, 64, mask_zero=True),\n  tf.keras.layers.GlobalAveragePooling1D(),\n  tf.keras.layers.Dense(1, activation='sigmoid')\n])\nmodel.compile(optimizer='adam', loss='binary_crossentropy', metrics=['accuracy'])\n```\n\n## Follow-up Questions\n- How would you handle unseen tokens at inference time?\n- How would you switch to subword tokenization for better coverage?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-6",
        "questionId": "q-17013",
        "question": "In a production TensorFlow 2.x service deployed on Kubernetes, you need to support both 32-bit FP32 and int8 quantized variants of the same image classifier behind a single endpoint. Describe (with concrete steps) how you would implement a model routing layer that selects the appropriate SavedModel variant based on a request header, ensure one global graph is not reloaded on each request, and demonstrate how to expose a single signature that routes to the correct variant. Include a minimal code sketch for the routing wrapper and signatures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a single SavedModel with a language_id input; route to en/zh/es branches via tf.switch_case, each with its own embedding/encoder/decoder. Share surface tokens but keep per-language weights.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a tf.data service cluster (Dispatcher + multiple Workers) to parallelize preprocessing. Build a single dataset that yields (image, text); deterministically shard across workers: dataset = dataset.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Load both FP32 and int8 variants at startup and route per-request using a header like X-Model-Variant. Avoid reloading graphs by keeping a shared dispatcher that caches replicas and uses a single sign",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a gateway (Envoy) to route based on a tenant header to per-tenant TF Serving pods, each loading tenant-specific SavedModel versions (e.g., tenantA_v1, tenantB_v2). Run separate model servers or di",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world TF serving often needs multiple model variants (precision, architectures) behind one endpoint. This tests routing design, startup load strategy, thread-safety, and how to expose a flexible signature.\n\n## Key Concepts\n- tf.saved_model and signatures\n- multi-variant serving strategy and routing\n- startup preloading and thread-safe caching\n- warm-up, latency, and correctness\n\n## Code Example\n```python\n# Minimal routing sketch (conceptual)\nimport threading\nfrom tensorflow import saved_model\n\n_variants = {\n  'fp32': saved_model.load('models/fp32'),\n  'int8': saved_model.load('models/int8')\n}\n_lock = threading.Lock()\n\ndef route_predict(input_tensor, variant: str):\n  with _lock:\n    model = _variants.get(variant, _variants['fp32'])\n  # assume model has a callable signature 'serving_default'\n  return model.signatures['serving_default'](input_0=input_tensor)\n```\n\n## Follow-up Questions\n- How would you monitor per-variant latency and accuracy, and how would you handle one variant becoming unavailable?\n- How would you extend this to support dynamic reloading or A/B testing of variants without downtime?",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-7",
        "questionId": "q-12524",
        "question": "You're deploying a TensorFlow 2.x image classifier behind TensorFlow Serving on Kubernetes with autoscaling. Production traffic shows high tail latency due to cold starts and sporadic bursts. Outline a practical, end-to-end plan to bound the 95th percentile latency for inference, focusing on adaptive inference batching, startup/warmup strategies, and resource-aware tuning. Include (1) a sidecar batching design that buffers requests and emits variable-sized batches, (2) TF Serving batching configuration (max_batch_size, batch_timeout_millis) and compatibility notes, (3) a concrete measurement plan with latency metrics and a minimal test script to simulate bursts?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an adaptive batching sidecar to group requests into batches up to 64 items or until batch_timeout_millis elapses (2-5 ms peak, longer idle). Configure TF Serving with batching_parameters { b",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Expose two TF Serving signatures 'vision' and 'nlp' in one SavedModel; route by tenant_id using a header to select the signature. Enforce per-tenant quotas with a Redis token bucket and allocate GPU/m",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Shard data per-tenant with a tenant-id lookup, interleave two streams (images and captions), and zip into per-tenant batches. Shuffle with a tenant-specific seed for deterministic replay, and use tf.d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe a drift-aware routing design for TensorFlow Serving behind Envoy that monitors per-tenant input distributions, promotes canary versions on drift, and rolls back on failures. Include per-tenan",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design a practical, production-ready inference path focusing on tail latency, not just average performance.\n\n## Key Concepts\n\n- Inference tail latency and adaptive batching\n- TF Serving batching parameters and compatibility\n- Warmup, autoscaling, and resource awareness\n- Observability and synthetic burst testing\n\n## Code Example\n\n```yaml\n# TF Serving batching config example\nbatching_parameters {\n  batch_timeout_millis: 2000\n  max_batch_size: 64\n  preserve_ordering: true\n}\n```\n\n```python\n# Minimal burst test sketch (pseudo)\nimport time\nfor i in range(1000):\n  send_request()\n  if i % 10 == 0:\n    time.sleep(0.01)\n```\n\n## Follow-up Questions\n\n- How would you adapt this for GPU inference vs CPU?\n- What metrics and dashboards would you introduce to monitor p95 stability over time?",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-8",
        "questionId": "q-5888",
        "question": "Design a production-ready inference path for a TF 2.x image classifier with a large embedding layer deployed via TensorFlow Serving on Kubernetes. The embedding is updated via a streaming pipeline while inference runs 24/7. Explain sharding strategy for the embedding matrix, how online updates affect latency, consistency guarantees, and how you would roll out versioned models with zero downtime and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a SavedModel with two input paths: dense_features of shape [None,128] and sparse_features as a SparseTensor. Expose signatures 'predict' (dense+sparse) and 'prune_features' (tenant gating).",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Shard the embedding across replicas (parameter-server style) and route lookups through a per-version in-memory cache with atomic version swaps. A streaming updater feeds the cache while inference uses",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Configure per-GPU inference queues with TFServing, enable TF-TRT FP16, and tune batching per GPU: max_batch_size 16, batch_timeout_micros 1000, max_enqueued_batches 128, num_batch_threads 6. Pin each",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enable dynamic batching in TensorFlow Serving by configuring batching_parameters in the model config. Set max_batch_size to 128, batch_timeout_micros to 20000 (20 ms), and max_enqueued_batches to 100. This configuration…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests knowledge of production-grade model serving, stateful components, and zero-downtime updates under streaming data.\n\n## Key Concepts\n\n- Embedding sharding and stateful serving\n- Online updates without stale reads\n- Atomic model version swaps and canary deployment\n- Observability, SLAs, rollback strategies\n\n## Code Example\n\n```javascript\n// Pseudo-code: versioned embedding cache lookup\nconst version = process.env.MODEL_VERSION || 'v1';\nconst embedCache = loadEmbeddingCache(version);\n\nfunction predict(inputIds) {\n  const emb = embedCache.lookup(inputIds);\n  return model.apply(emb);\n}\n```\n\n## Follow-up Questions\n\n- How would you test zero-downtime swap guarantees?\n- What race conditions could arise and how would you mitigate them?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-9",
        "questionId": "q-14749",
        "question": "You're given a folder of CSVs with columns text,label (0/1) for binary sentiment. Using TensorFlow 2.x, implement a beginner pipeline that (1) reads all CSVs with tf.data, (2) applies a TextVectorization layer with a 5000-word vocab and output_mode='int', (3) builds a small classifier: Embedding(5000,64) -> GlobalAveragePooling1D -> Dense(1, sigmoid), (4) trains with model.fit on train batches, and evaluates on a held-out set. Provide code for dataset loading, vectorization, and training?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-branch graph: image encoder and text encoder feed a fusion head; cache image embeddings by image hash to reuse on text-only requests. In TensorFlow Serving, configure two dynamic_batching queues (",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a tf.data pipeline that accepts variable-size images, uses interleave to stream from storage, map with num_parallel_calls to resize/crop deterministically, and cache only the most expensive ops.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use tf.data to read all CSVs with tf.data.experimental.make_csv_dataset or tf.data.TextLineDataset and map to (text,label). Apply a TextVectorization layer with a 5000-word vocab and output_mode='int'",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use tf.data.Dataset.bucket_by_sequence_length with element_length_func returning the source length, boundaries [8,16,32,64], bucket_batch_sizes [64,64,32,16], and padded_shapes ([None], [None]). Shuff",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end text data handling in TF 2.x: data ingestion, text preprocessing with TextVectorization, a simple embedding-based classifier, and evaluation on a hold-out set.\n\n## Key Concepts\n- tf.data for CSV ingestion and batching\n- TextVectorization for learned tokenization with a fixed vocab\n- Embedding + GlobalAveragePooling for a compact text representation\n- Binary classification flow with model.compile and model.fit\n\n## Code Example\n```python\n# High-level outline (not full code)\n```\n\n## Follow-up Questions\n- How would you extend to multilingual data or handle unknown tokens?\n- How do you tune vocab size and embedding dims for small datasets?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-10",
        "questionId": "q-23261",
        "question": "Advanced: You deploy a TensorFlow 2.x Transformer-based anomaly detector in TensorFlow Serving on Kubernetes. Inputs are streaming JSON logs with variable-length sequences. A Redis-backed embedding table is refreshed every minute. Design an architecture that supports per-model batching, hot-embedding refresh without downtime, deterministic results across model versions, and canary rollout with versioned models. Include a concrete latency/throughput validation plan and a minimal config snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To bound latency under burst traffic, implement dynamic batching with a fixed window and sequence-length bucketing at the server edge. Use tf.data.experimental.bucket_by_sequence_length (or a custom b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-model BatchConfig queues in TF Serving for the transformer anomaly detector, each with its own batch_size and batch_timeout. Refresh a Redis-backed embedding table every minute asynchronously,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Isolate tenants with per-tenant models in TF Serving using a model_config_list, each with its own batching_parameters (max_batch_size, batch_timeout_micros). Pre‑process inputs to a fixed canonical si",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a shared feature schema and two signatures: 'lite' for on-device TF-Lite and 'full' for cloud. Route by device capability and connectivity; on-device runs a quantized encoder, cloud runs the full",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a streaming inference path with per-model batching, dynamic data refresh, and safe versioned rollouts in production-grade TF Serving. It also evaluates observability and risk mitigation for embeddings updates.\n\n## Key Concepts\n- TensorFlow Serving batching semantics and BatchConfig per model\n- Async in-place data updates (embedding refresh via Redis) with version gating\n- Canary deployments and header-based routing for V1/V2 models\n- End-to-end latency and throughput validation under bursts\n\n## Code Example\n```yaml\n# Minimal per-model batching config (pseudo)\nbatching_parameters {\n  batch_timeout_micros: 1000\n  max_batch_size: 64\n  max_enqueued_batches: 50\n}\n```\n\n```yaml\n# Embedding refresh (pseudo)\nembedding_refresh {\n  store: redis\n  interval_ms: 60000\n  canary_version_gate: true\n}\n```\n\n## Follow-up Questions\n- How would you test canary rollout latency and rollback?  \n- What failure modes occur if Redis refresh lags or fails for embedding vectors?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-11",
        "questionId": "q-17971",
        "question": "You're given a directory data/ with two subfolders cat/ and dog/, each containing PNGs of varying sizes. Build a beginner TensorFlow 2.x image classifier using a small CNN in Keras. Provide a tf.data pipeline that reads files, decodes PNG, resizes to 96x96, normalizes to [0,1], shuffles, batches, and prefetches. Include minimal data split and a toy training run with SparseCategoricalCrossentropy. How would you implement this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Assign per-replica offsets and a fixed random seed in preprocessing to guarantee deterministic sharding. Cache on local SSD per worker, keep windowing deterministic, and use a global batch size with p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Argo Rollouts with Istio to split 5-10% traffic to version v2; v1 handles the rest. Configure model_config_list so both versions are served on the same path. Add canaryAnalysis with latency (P95)",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a tf.data pipeline that reads file paths from the directory, decodes PNG images, resizes them to 96×96, normalizes pixel values to [0,1], shuffles the data, batches it, and prefetches for performance. The model ar…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement with tf.distribute.MultiWorkerMirroredStrategy and a gradient-accumulator. Per-replica batch size b, global batch = b * replicas. Accumulate grads for model.trainable_variables over N steps",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates your ability to build an end-to-end image classifier in TensorFlow 2.x, covering the complete pipeline from raw data loading to a trained model.\n\n## Key Concepts\n\n- **tf.data pipeline**: Production-ready, efficient data loading and preprocessing\n- **Image preprocessing**: Decoding, resizing, and normalization to [0,1] range for optimal model convergence\n- **CNN architecture**: Lightweight convolutional network suitable for small, low-resolution datasets\n- **Loss function**: Using from_logits=True with SparseCategoricalCrossentropy for numerical stability and faster training\n\n## Code Example\n\n```python\nimport tensorflow as tf\nimport os\n\ndata_dir = 'data/'\n\ndef load_image(path):\n    image = tf.io.read_file(path)\n    image = tf.image.decode_png(image, channels=3)\n    image = tf.image.resize(image, [96, 96])\n    image = image / 255.0\n    return image\n\ndef get_label(path):\n    parts = tf.strings.split(path, '/')\n    label = parts[-2]\n    return 0 if label == 'cat' else 1\n\nfilenames = tf.data.Dataset.list_files(data_dir + '*/*.png')\ndataset = filenames.map(lambda x: (load_image(x), get_label(x)))\ndataset = dataset.shuffle(buffer_size=1000).batch(32).prefetch(tf.data.AUTOTUNE)\n\nmodel = tf.keras.Sequential([\n    tf.keras.layers.Conv2D(32, 3, activation='relu'),\n    tf.keras.layers.MaxPooling2D(),\n    tf.keras.layers.Conv2D(64, 3, activation='relu'),\n    tf.keras.layers.MaxPooling2D(),\n    tf.keras.layers.Flatten(),\n    tf.keras.layers.Dense(128, activation='relu'),\n    tf.keras.layers.Dense(2)\n])\n\nmodel.compile(\n    optimizer='adam',\n    loss=tf.keras.losses.SparseCategoricalCrossentropy(from_logits=True),\n    metrics=['accuracy']\n)\n\nmodel.fit(dataset, epochs=5)\n```",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-12",
        "questionId": "q-7217",
        "question": "In a real-time fraud-detection pipeline, you deploy a TensorFlow 2.x ensemble behind TensorFlow Serving on Kubernetes. The ensemble includes a text encoder and a numeric feature subnet, with a gating head that produces the final score. Inference latency must stay under 40 ms at the 99th percentile during bursts. Describe your end-to-end design: model signatures for multi-input routing, per-request feature imputation, dynamic batching, and model versioning with canary deployments; plus observability. Include a minimal code sketch for the Serving signature and a small wrapper showing input preprocessing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Convert the Keras/MobileNetV2 to TensorFlow.js using the tfjs_converter with input_format=keras and 8-bit quantization (--quantization_bytes 1) for smaller size. Serve model.json plus weight shards fr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a salted per-input fingerprint cache for feature vectors with TTL eviction. Maintain per-model queues to drive independent batching timers and sizes. Use per-model BatchConfig to cap max_bat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Split the CSV chronologically (70/30) to prevent leakage. Build a tf.data.Dataset from x1,x2,x3 as features and y as target; normalize inputs with a train-fit Normalization layer. Use the Functional A",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single multi-input SavedModel signature that accepts text_ids: int32, nums: float32, and outputs a final score. Implement two subnets (text encoder and numeric path) feeding a gating head; prepr",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design multi-input, multi-path models in TF Serving, with practical concerns like per-request preprocessing, dynamic batching, and robust versioning.\n\n## Key Concepts\n- Multi-input SignedModel signatures for heterogeneous inputs\n- Separate preprocessing path for missing features\n- Dynamic batching and hardware acceleration (TF-TRT)\n- Versioned models with canary deployments and traffic splitting\n- Observability: latency/throughput metrics, dashboards\n\n## Code Example\n```javascript\n// Signature sketch: inputs: text_ids, nums; outputs: score\n```\n\n## Follow-up Questions\n- How would you calibrate canary launches to minimize risk?\n- How do you monitor model drift and decide when to roll back?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-13",
        "questionId": "q-25728",
        "question": "Develop an architecture where a shared preprocessing service feeds two TF Serving models: A is a 224x224x3 CNN and B is a 128-token text model. Use per-model queues, dynamic batching, and a shared embedding cache to reduce recomputation. Provide concrete BatchConfig per model, a memory isolation strategy, and a canary rollout plan with metrics to validate latency under burst?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Compile the custom op for CPU and GPU with Bazel, producing libcustom.so; link it via custom_op_library in the SavedModel. Expose a preprocessing signature that uses the op. Ship two TF Serving versio",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Parse CSV with tf.data TextLineDataset and tf.io.decode_csv to yield (label, pixels). Normalize by dividing by 255 and reshape to (28,28,1). Build a small CNN with the Keras Functional API: Input -> C",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a split-serve with a shared preprocessor feeding two TF Serving models: A (224x224x3 CNN) and B (128-token text). Use per-model queues and independent BatchConfig; add a shared embedding cac",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a single TF Serving signature for inputs (image: float32[batch, h, w, c], context: float32[batch, ctx_dim]). Use Envoy to enforce per-tenant quotas, routing A,B to CPU and burst to GPU via c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes multi-model inference orchestration, shared resources, and deployment safety under burst traffic. It tests batching policies, memory isolation, and rollout strategies in production-like constraints.\n\n## Key Concepts\n\n- Per-model batching with queues\n- Shared preprocessing and caching\n- Canary rollouts and metrics\n\n## Code Example\n\n```javascript\n// Example stub for BatchConfig\nconst batchA = { maxBatchSize: 32, batchTimeoutMicros: 1000 };\n```\n\n## Follow-up Questions\n\n- How would you measure head-of-line blocking in this setup?\n- How would you adapt to a third model with different input sizes?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-14",
        "questionId": "q-22384",
        "question": "In a Kubernetes TF Serving deployment, two models share a central feature extractor. Inputs include image bytes and text tokens, arriving in bursts. Design a streaming inference pipeline that caches feature vectors by input fingerprint, uses per-model batching, and guarantees isolation to avoid head-of-line blocking. Include TTL eviction, per-model BatchConfig interactions, SLA-driven monitoring, zero-downtime updates, plus minimal Serving YAML and a fingerprint sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two serving signatures: embeddings_only (cat_ids -> embeddings on CPU) and full_inference (cat_ids + dense_features -> predictions). Place Embedding under tf.device('/CPU:0'), run remaining layers on",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up drift detection and health checks for a live TF Serving stack. Use TF DV/TF Transform to collect feature statistics, stream them via Kafka into a drift analyzer, and trigger alerts when drift s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-input regression model: numeric features pass through Dense(16, relu); brand_id via Embedding(16,4) then Flatten; concatenate and pass through Dense(32, relu) and Dense(1). tf.data reads CSV, norm",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a salted per-input fingerprint cache for feature vectors with TTL eviction. Maintain per-model queues to drive independent batching timers and sizes. Use per-model BatchConfig to cap max_bat",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design a streaming, low-latency inference path with a shared preprocessor, tying together caching, batching, privacy, and production update patterns.\n\n## Key Concepts\n- Shared feature extractor\n- Fingerprint caching\n- TTL eviction\n- Per-model batch interplay\n- Isolation and zero-downtime updates\n\n## Code Example\n```python\ndef fingerprint(input_bytes, salt=b'secret'):\n    import hashlib\n    h = hashlib.blake2b(digest_size=16)\n    h.update(salt)\n    h.update(input_bytes)\n    return h.hexdigest()\n```\n\n## Follow-up Questions\n- How would you validate TTL eviction latency and cache size constraints under bursty load?\n- What metrics would you collect to alert on SLA degradation and cache saturation?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-15",
        "questionId": "q-12295",
        "question": "How would you implement dynamic batching in TensorFlow Serving to maximize throughput during bursty traffic for a model that serves a single signature with image and numeric inputs, while ensuring P95 latency remains under 200 ms? Include config settings, testing plan, and monitoring strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "This demonstrates an end-to-end TF pipeline for tabular data: read CSV with tf.data, split train/test, encode neighborhood via StringLookup + one-hot, concatenate numeric features, and pass through a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Configure dynamic_batching_parameters in the model config with max_batch_size around 128 and batch_timeout_micros around 10000, using a single signature to enable per-request batching. Validate with load testing using g…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a single TF Serving signature for inputs (image: float32[batch, h, w, c], context: float32[batch, ctx_dim]). Use Envoy to enforce per-tenant quotas, routing A,B to CPU and burst to GPU via c",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use tf.data.experimental.make_csv_dataset to load train.csv and test.csv, map to inputs [distance_km, traffic_index, weather_code, driver_rating] and label eta_seconds, batch and prefetch. Build a sin",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of TensorFlow Serving's dynamic batching, including configuration, testing, and observability under burst traffic.\n\n## Key Concepts\n- Dynamic batching configuration and trade-offs\n- Latency vs throughput under bursty loads\n- Observability and metrics\n\n## Code Example\n```protobuf\nmodel_config_list {\n  config {\n    name: \"my_model\"\n    base_path: \"/models/my_model/1\"\n    model_platform: \"tensorflow\"\n    dynamic_batching_parameters {\n      max_batch_size: 128\n      batch_timeout_micros: 10000\n      max_enqueued_batches: 100\n      num_batch_threads: 8\n    }\n  }\n}\n```\n\n## Testing Plan\n- Use ghz or k6 to simulate bursty traffic with varying QPS\n- Test with different batch sizes (32, 64, 128, 256)\n- Validate P95 latency stays under 200ms\n- Monitor GPU/CPU utilization during peak loads\n- Test failover and recovery scenarios\n\n## Monitoring Strategy\n- Track tensorflow_serving_request_count and latency metrics\n- Set up alerts for P95 latency > 180ms\n- Monitor batch efficiency (actual_batch_size/max_batch_size)\n- Track queue wait times and batch timeouts\n- Use custom metrics for per-signature performance",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-16",
        "questionId": "q-21114",
        "question": "Explain how you would monitor a live TensorFlow Serving deployment for data drift and model degradation. Outline using TFDV/TFT for feature statistics, a streaming stats collector (Kafka/Flink), alert thresholds, and a canary rollback plan. Describe validating a new version with a small traffic shard and key metrics: p99 latency, error rate, and drift score thresholds?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage TensorFlow Serving dynamic batching to coalesce requests, pad inputs to the batch's max length, and cache encoder outputs keyed by input signature to reduce recomputation during bursts. Run t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up drift detection and health checks for a live TF Serving stack. Use TF DV/TF Transform to collect feature statistics, stream them via Kafka into a drift analyzer, and trigger alerts when drift s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Answer: Implement a tenant-aware proxy in front of TensorFlow Serving that routes by gRPC metadata and maintains per-tenant queues. Each tenant/model pair gets its own BatchConfig and a dedicated batc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a tf.data pipeline to load the CSV, split features and label, apply a Normalization layer and adapt on the training data, then build a Keras Functional model: Input -> Dense(32, relu) -> Dense(16,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical drift monitoring and safe rollout in production TF workflows.\n\n## Key Concepts\n- Drift detection, TF DV, TFT\n- Canary releases, traffic shaping\n- Monitoring, alerting, rollback\n\n## Code Example\n```python\n# pseudo drift score calculation\ndef drift_score(current, baseline):\n    score = 0.0\n    for k in current:\n        score += abs(current[k]['mean'] - baseline[k]['mean'])\n    return score / max(1, len(current))\n```\n\n## Follow-up Questions\n- How would you scale to multi-tenant or model ensembles?\n- What metrics would you surface in a dashboard?",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-17",
        "questionId": "q-17137",
        "question": "You have a CSV with columns text,label (labels 0..3). Implement a beginner TensorFlow 2.x text classifier using character-level tokenization (TextVectorization with analyzer='char' and ngrams=3-5) and a small Conv1D head in Keras. Provide: a tf.data pipeline to read CSV, vocabulary setup, model construction, training with SparseCategoricalCrossentropy, and a tiny toy dataset for model.fit. Include a simple evaluation plan and discuss char-level vs word-level trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use tf.lite.TFLiteConverter.from_keras_model(model) with DEFAULT optimizations, optionally calibrate int8 with a representative_dataset. Convert to model.tflite, then run inference via tf.lite.Interpr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a beginner TensorFlow 2.x text classifier using character-level tokenization with TextVectorization (analyzer='char', ngrams=3-5) and a small Conv1D head in Keras. Include: a tf.data pipeline to read CSV data…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use per-tenant signatures in TF Serving, with a short preprocessor sidecar that maps tenant JSON to a fixed tensor order. Maintain a versioned feature schema registry; cache tenant features in Redis k",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Expect a concise plan showing tf.data CSV parsing, train/holdout split, a 2-feature input passed through a single Normalization layer, a small Dense-based Functional API model, BinaryCrossentropy, and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses end-to-end text modeling skills using an alternative tokenization approach (character-level) combined with a lightweight CNN architecture, along with practical tf.data handling for CSV inputs.\n\n## Key Concepts\n- Character-level TextVectorization\n- 1D CNN for text classification\n- tf.data CSV pipeline\n- Training with SparseCategoricalCrossentropy\n- Toy dataset scaffolding\n- Char-level vs word-level trade-offs\n\n## Code Example\n```python\nimport tensorflow as tf\nfrom tensorflow.keras.layers import TextVectorization, Embedding, Conv1D, Dense, GlobalMaxPooling1D\n\nCSV_PATH = \"data.csv\"\nMAX_VOCAB_SIZE = 10000\nMAX_SEQUENCE_LENGTH = 256\nNUM_CLASSES = 4\nEMBEDDING_DIM = 64\n\ndef get_dataset():\n    dataset = tf.data.experimental.make_csv_dataset(\n        CSV_PATH,\n        batch_size=32,\n        column_names=[\"text\", \"label\"],\n        label_name=\"label\",\n        num_epochs=1,\n        shuffle=True\n    )\n    return dataset.map(lambda x: (x[\"text\"], tf.cast(x[\"label\"], tf.int32)))\n\nvectorizer = TextVectorization(\n    max_tokens=MAX_VOCAB_SIZE,\n    output_mode=\"int\",\n    output_sequence_length=MAX_SEQUENCE_LENGTH,\n    analyzer=\"char\",\n    ngrams=(3, 5)\n)\n\nsample_texts = [\"hello world\", \"test example\", \"sample text\"]\nvectorizer.adapt(sample_texts)\n\nmodel = tf.keras.Sequential([\n    vectorizer,\n    Embedding(MAX_VOCAB_SIZE, EMBEDDING_DIM, input_length=MAX_SEQUENCE_LENGTH),\n    Conv1D(128, 5, activation=\"relu\"),\n    GlobalMaxPooling1D(),\n    Dense(64, activation=\"relu\"),\n    Dense(NUM_CLASSES, activation=\"softmax\")\n])\n\nmodel.compile(\n    optimizer=\"adam\",\n    loss=tf.keras.losses.SparseCategoricalCrossentropy(),\n    metrics=[\"accuracy\"]\n)\n\ntoy_data = tf.data.Dataset.from_tensor_slices((\n    [\"positive review\", \"negative feedback\", \"neutral opinion\", \"another positive\"] * 10,\n    [0, 1, 2, 0] * 10\n)).batch(4)\n\nmodel.fit(toy_data, epochs=5)\n\nloss, accuracy = model.evaluate(toy_data)\nprint(f\"Loss: {loss}, Accuracy: {accuracy}\")\n```\n\n## Char-Level vs Word-Level Trade-offs\n\n| Aspect | Character-Level | Word-Level |\n|--------|-----------------|------------|\n| **Vocabulary Size** | Smaller (chars + ngrams) | Larger (all unique words) |\n| **OOV Handling** | Naturally handles typos/misspellings | Requires special tokens |\n| **Sequence Length** | Longer (chars vs words) | Shorter |\n| **Training Speed** | Slower (longer sequences) | Faster |\n| **Context** | Limited local context | Full semantic context |\n| **Best For** | Morphologically rich languages, typo tolerance | Standard NLP tasks |\n\nCharacter-level models excel when training data is limited or contains noisy text (e.g., social media, user-generated content). Word-level models generally perform better with abundant training data and when semantic meaning matters most.",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-18",
        "questionId": "q-4763",
        "question": "In a Kubernetes deployment using TensorFlow Serving, you run a multi-tenant inference API where each tenant's models have separate versions and possibly different input shapes. Design an end-to-end solution that routes requests by tenantId to the correct model version, supports per-tenant latency targets, uses dynamic batching efficiently, handles model warmup/canary rollout, and provides monitoring/rollbacks. Be concrete about TF Serving config, gateway, and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a gateway to route tenantIds to per-tenant SavedModel directories (models/tenantX/versions/1,2) with TF Serving config per tenant. Run a canary rollout by shifting a subset of traffic to a new ver",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use tf.data.bucket_by_sequence_length to group by length, pad only to the bucket max, add cache and prefetch, and feed a fixed-batch-sized pipeline per bucket. Prefer RaggedTensor for internal ops whe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a combined QAT and PTQ strategy. Train with QAT using a representative calibration set, convert to int8 with tfmot and the TFLiteConverter, and enable NNAPI/Metal delegate. Deploy with on-device e",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build the custom op as a shared library compiled against the exact TF Serving version used, load it at startup via a bootstrap loader, and enforce ABI compatibility across Python/C++ boundaries. Packa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world multi-tenant inference requires precise routing, per-tenant SLAs, and safe rollouts. This tests design of TF Serving multi-model config, dynamic batching isolation, and observable canary deployments.\n\n## Key Concepts\n- TF Serving multi-model config with per-tenant base_path\n- Dynamic batching per model to respect latency targets\n- Canary rollouts and per-tenant rollback strategies\n- Observability with metrics and health checks\n\n## Code Example\n```yaml\nmodel_config_list:\n  config:\n  - name: \"tenantA_model\"\n    base_path: \"/models/tenantA/1\"\n    model_platform: \"tensorflow\"\n    dynamic_batching_parameters:\n      max_batch_size: 32\n      batch_timeout_millis: 100\n```\n\n```yaml\n# Istio-style gateway routing (illustrative)\nhttp:\n- match:\n  - uri: /v1/tenantA/*\n  route:\n  - destination:\n      host: tf-serving-tenantA\n```\n\n## Follow-up Questions\n- How would you handle input shape variability across tenants without frequent graph recompiles?\n- How would you implement per-tenant SLA monitoring and automatic rollback criteria?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-19",
        "questionId": "q-29305",
        "question": "Advanced: You deploy two TensorFlow models A and B on Kubernetes with TensorFlow Serving, sharing a dynamic batcher but separate input shapes: A 224x224x3, B 299x299x3. Traffic is bursty; SLA: 95th percentile latency for A <= 8 ms at 100 req/s, B <= 40 ms. Design per-model batching, separate queues to avoid head-of-line blocking, and input-shape aware padding. Include concrete BatchConfig, canary rollout plan, and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement two per-model queues feeding a shared batcher; tune A with max_batch_size 16 and batch_timeout_micros 1000, B with max_batch_size 8 and 1500. Apply input-shape padding to align batches. Isol",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-model batching: use batching_parameters_file per model; A: max_batch_size 16, batch_timeout_micros 1000, max_enqueued_batches 64, num_batch_threads 4; B: max_batch_size 32, batch_timeout_micros 20",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a TextVectorization layer with max_tokens=20000 and int outputs, feed into Embedding(64), then GlobalAveragePooling1D and Dense(1, sigmoid). Compile with binary_crossentropy and Adam, and fit on a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Answer: Implement a tenant-aware proxy in front of TensorFlow Serving that routes by gRPC metadata and maintains per-tenant queues. Each tenant/model pair gets its own BatchConfig and a dedicated batc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design per-model batching with isolation under bursty traffic, balancing latency vs throughput, plus rollout safety.\n\n## Key Concepts\n- Per-model batching with isolated queues\n- Head-of-line blocking avoidance\n- Input-shape aware padding for batching\n- Canary rollouts and zero-downtime updates\n- Observability of tail latency and SLA validation\n\n## Code Example\n```javascript\nconst batching = {\n  modelA: { max_batch_size: 16, batch_timeout_micros: 1000 },\n  modelB: { max_batch_size: 8, batch_timeout_micros: 1500 }\n}\n```\n\n## Follow-up Questions\n- How would you evolve this when adding a third model with 1x1 input? \n- How would you validate tail latency guarantees and monitor backpressure in production?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-20",
        "questionId": "q-1384",
        "question": "Describe how to deploy a TensorFlow model that accepts variable-length input sequences in production without a fixed max_length. Include input signatures, RaggedTensor usage, encoder compatibility, dynamic batching (bucketed), memory/latency considerations, and validation strategy with latency and throughput benchmarks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: use tf.data to read CSV, map features/label, impute missing values (column medians) inside map, apply a Normalization layer adapt on train features, build a small Functional API mod",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Export a SavedModel with an input signature that accepts a ragged tensor (e.g., tokens with shape [None]); implement the encoder to consume RaggedTensor via tf.RaggedTensor.to_tensor or masked ops; en",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use tf.data.bucket_by_sequence_length to group by length, pad only to the bucket max, add cache and prefetch, and feed a fixed-batch-sized pipeline per bucket. Prefer RaggedTensor for internal ops whe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use tf.distribute.MirroredStrategy across 4 GPUs. Bucket inputs by length in tf.data, accumulate micro-batches to reach the target batch size, enable mixed precision with a LossScaleOptimizer, and fix",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design production-ready serving for variable-length inputs, balancing correctness, latency, and memory. It probes familiarity with RaggedTensor flows, input signatures, and scalable batching strategies.\n\n## Key Concepts\n- RaggedTensor and dynamic shapes in TF models\n- SavedModel input signatures for non-uniform data\n- Dynamic/bucketed batching vs fixed padding\n- Memory budgeting and latency guarantees\n- Validation: latency percentiles and throughput benchmarks\n\n## Code Example\n```javascript\n# Python TensorFlow example (conceptual)\nimport tensorflow as tf\n\n@tf.function(input_signature=[tf.TensorSpec([None], tf.int32, name='tokens')])\ndef serve(inputs):\n    rt = tf.RaggedTensor.from_tensor(tf.expand_dims(inputs, -1))\n    x = rt.to_tensor()\n    # embedding + encoder would follow here\n    return x\n```\n\n## Follow-up Questions\n- How would you monitor cold-start latency and cache efficiency in this setup?\n- How would you orchestrate multiple models with varying max_lengths in a single inference service?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-21",
        "questionId": "q-10595",
        "question": "Design a production-ready TensorFlow Serving deployment for a SavedModel exposing two signatures, 'detect' and 'classify', sharing a backbone. Inputs arrive in bursts with varying batch sizes; latency target 15 ms per request at 1k RPS. Describe the model layout, serving architecture (single vs multi-model), dynamic batching, per-signature routing, zero-downtime updates, observation/metrics, and failure handling. Include concrete knobs (model_repository, model_config, signature routing, canary, monitoring)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a SavedModel with two signatures, 'detect' and 'classify', sharing a backbone. Deploy on TF Serving with a single model_config and route by signature_name in PredictRequest. Enable dynamic batchin",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a per-tenant, multi-model serving path on Kubernetes with Istio, using distinct TF Serving signatures and tenant-scoped routing. Explain how to enforce per-tenant quotas, resource isolation (cg",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant ServableConfigs with separate model versions, run tenant-scoped TF Serving instances behind Envoy that routes by a tenant header to tenant-specific endpoints, and apply per-tenant Batch",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Compare QAT vs PTQ for a vision model served behind TensorFlow Serving on Kubernetes. Propose an end-to-end plan: calibration data strategy and per-layer quantization choices, signature and batching c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests deep understanding of TF Serving features: signatures, routing, batching, and rolling updates in a real-world setting.\n\n## Key Concepts\n\n- SavedModel signatures and PredictRequest routing\n- Dynamic batching configuration per model\n- Deployment topology: single vs multi-model for isolation\n- Zero-downtime updates and canary / rolling updates\n- Monitoring TF Serving with Prometheus\n\n## Code Example\n\n```javascript\n// Deployment config placeholder\n```\n\n## Follow-up Questions\n\n- How would you implement per-tenant isolation with shared Backbone?\n- How would you measure tail latency under burst traffic?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-22",
        "questionId": "q-10855",
        "question": "Hybrid inference for a multilingual TTS transformer on Kubernetes: pre-processing (tokenization, feature extraction) runs in a sidecar; model runs in GPU via TensorFlow Serving. Tokens stream to the model with sub-40 ms latency per token under bursts. Design the end-to-end pipeline: data flow, IPC method, batching strategy, dynamic shapes, zero-downtime updates with versioned models, and a minimal wrapper sketch coordinating sidecar and model?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Prefetch preprocessor as a sidecar; IPC via Unix-domain socket with a fixed-size ring buffer; feed token chunks to a GPU TF Serving model using dynamic_batching; fix token stride to deterministic late",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Describe a production setup where two models share a GPU node with per-model batching and strict isolation. Outline per-model BatchConfig, a queuing layer to avoid head-of-line blocking, per-model quo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "This demonstrates an end-to-end TF pipeline for tabular data: read CSV with tf.data, split train/test, encode neighborhood via StringLookup + one-hot, concatenate numeric features, and pass through a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To handle streaming inference, design a tf.data pipeline with a 60-step causal window and online normalization (running mean/var). Use dataset.window(60, 1) and batch(1), plus prefetch and a compact i",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end production pipelines that separate CPU-bound pre-processing from GPU inference, manage latency budgets, and support zero-downtime updates.\n\n## Key Concepts\n- Hybrid inference architecture, IPC, shared memory, Unix sockets\n- Dynamic batching and deterministic latency\n- Model versioning and rolling updates, health checks\n- Observability: p95 latency, tail latency, queue depth\n\n## Code Example\n```javascript\n// High-level wrapper pseudo-code for coordinating sidecar preprocessor and TF Serving\nclass TTSWrapper {\n  constructor(socketPath) { /* ... */ }\n  async stream(inputText) { /* ... send tokens, receive audio */ }\n}\n```\n\n## Follow-up Questions\n- How would you test tail latency under burst traffic?\n- How would you handle backpressure if the sidecar gets backlogged?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-23",
        "questionId": "q-9641",
        "question": "In a TensorFlow Serving setup, you run three models (object detector, depth estimator, caption generator) that share a common backbone. The goal is to minimize memory and maximize throughput under bursty traffic, with per-model latency budgets (25 ms for detector, 30 ms for depth, 60 ms for caption). How would you architect the model loading, signature definitions, dynamic batching, and request routing to share the backbone, enforce per-model batching, and enable zero-downtime updates? Include an example config snippet and outline monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a single SavedModel with a shared backbone and three signatures (detector, depth, caption). Route by signature_name in Predict; share the backbone to reduce memory. Use per-signature batching",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Load a CSV with filenames and labels using tf.data, map to image tensors (read_file -> decode_png grayscale -> resize to 28x28) and normalize. Apply augmentation (random_flip_left_right, random_bright",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement two separate tf.data pipelines for images and text, then synchronize them using zip. Ensure determinism by seeding all map operations and fixing random seeds. Cache each stream to disk for repeated epochs. Use…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant model slots in TF Serving with dynamic_batching: define separate batching_config for each tenant, with max_batch_size, batch_timeout_mss, and per-tenant queue length. Route requests via",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design multi-head models with shared components and per-model routing, a common real-world pattern in production.\n\n## Key Concepts\n- Shared backbone\n- SignatureDefs and routing by signature_name\n- Per-signature batching and dynamic shapes\n- Model versioning for zero-downtime updates\n- Observability: metrics, tracing\n\n## Code Example\n```python\n# Python pseudo\nimport tensorflow as tf\ndef build_backbone(x): ...\ninputs = tf.keras.Input(shape=(None, None, 3), name='imgs')\nbackbone = build_backbone(inputs)\ndet_head = DetHead(backbone)\ndepth_head = DepthHead(backbone)\ncap_head = CaptionHead(backbone)\nmodel = tf.keras.Model(inputs=inputs, outputs={\n  'det': det_head.output,\n  'depth': depth_head.output,\n  'caption': cap_head.output\n})\n# Signatures would be registered for each head\n# tf.saved_model.save(model, 'model', signatures={\n#   'detector': det_signature, 'depth': depth_signature, 'caption': caption_signature\n# })\n```\n\n## Follow-up Questions\n- How would you test traffic shifting between signatures during upgrades?\n- What are pitfalls with mixing precision modes across heads?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-24",
        "questionId": "q-2081",
        "question": "In a distributed training setup with tf.distribute.MultiWorkerMirroredStrategy across 8 workers, how would you guarantee deterministic sharding and data order for a long-text Transformer training run? Include how to set global_batch_size, per_replica_batch_size, dataset sharding via input_context/experimental_distribute_dataset, and seeds/flags to ensure reproducibility; avoid hidden data leakage across workers?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set global_batch_size to 256, yielding per_replica_batch_size of 32 across 8 workers. Build the dataset using input_context for deterministic sharding: ds = ds.shard(input_context.num_input_pipelines, input_context.inpu…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Train with TensorFlow Federated (FedAvg) across 5 nonIID clients. Each client trains a Keras Embedding+LSTM text classifier locally; the server aggregates with tff.learning.build_federated_averaging_p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use tf.data: Dataset.from_tensor_slices(file_paths).map(load_and_label) where load_and_label reads the WAV, decodes with mono channel, pads/trims to 16000 samples, and scales to [-1,1]. Infer label fr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a custom TensorFlow Serving image bundling the CUDA op and compile TF with the op; structure model artifacts under /models/my_model/{1,2}; deploy two pods and route a fraction of traffic (e.g.,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses reproducible distributed data handling and exposure to tf.distribute nuances.\n\n## Key Concepts\n- Deterministic operations in TensorFlow\n- tf.data sharding with input_context\n- Global vs per-replica batch sizing\n- Seeding strategies across workers\n\n## Code Example\n```python\n# Python implementation showing the approach\nstrategy = tf.distribute.MultiWorkerMirroredStrategy()\nglobal_batch_size = 256\nper_replica_batch_size = global_batch_size // strategy.num_replicas_in_sync\n\ndef dataset_fn(input_context):\n    ds = create_dataset()\n    ds = ds.shard(input_context.num_input_pipelines, input_context.input_pipeline_id)\n    ds = ds.batch(per_replica_batch_size)\n    return ds\n\ndistributed_dataset = strategy.experimental_distribute_dataset(\n    dataset_fn, input_context=tf.distribute.InputContext()\n)\n```",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-16T10:34:13.654Z",
    "version": 1
  }
]