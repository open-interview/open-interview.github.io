[
  {
    "id": "test-tensorflow-developer",
    "channelId": "tensorflow-developer",
    "channelName": "Tensorflow Developer",
    "title": "Tensorflow Developer Knowledge Test",
    "description": "Test your understanding of Tensorflow Developer with 25 questions.",
    "questions": [
      {
        "id": "tensorflow-developer-q-0",
        "questionId": "q-6702",
        "question": "In TensorFlow Serving on Kubernetes you have a Transformer model too large for a single GPU. Describe how you would partition the model across multiple GPUs using tf.distribute.Strategy, enable per request cross device placement, and implement a batching strategy that minimizes inter device communication while preserving per request latency targets? Include a minimal config snippet and a compact code sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a single SavedModel with a shared backbone and two heads. Expose two ServingSignatures: 'classify' returning image logits, and 'detect' returning boxes and scores. Use input signature tf.Ten",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition with tf.distribute.MirroredStrategy across GPUs, place encoder on GPU:0 and decoder/heads on GPU:1; export as a single SavedModel. In TF Serving, add a lightweight wrapper that shards each b",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Proposed: a SavedModel with a shared trunk and two heads. Signature '/multimodal' accepts {'mode': string, 'image': float32[batch,224,224,3], 'text': string}. Use tf.switch_case on mode to route to im",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Baseline: two Conv2D layers (32,64 filters, 3x3), ReLU, MaxPool, Flatten, Dense(128), Softmax(10). Preprocess: Rescaling(1/255) with RandomFlip('horizontal') and RandomRotation(0.1) in a preprocessing",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical model-parallel inference with large models beyond a single GPU. \n\n## Key Concepts\n- Model parallelism across GPUs\n- tf.distribute.Strategy and device placement\n- Dynamic batching and latency budgeting\n- Stable SavedModel exports across versions\n\n## Code Example\n```javascript\n// placeholder sketch illustrating placement strategy\n```\n\n## Follow-up Questions\n- How would you monitor cross-GPU bandwidth impact and adjust batching?\n- What changes to TF Serving config optimize tail latency for bursts?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-1",
        "questionId": "q-15123",
        "question": "You have a small image classifier trained on a single GPU. You want to scale training to two GPUs with minimal changes. Using TensorFlow 2.x, implement a data-parallel workflow with tf.distribute.MirroredStrategy. Explain data pipeline sizing (per-replica vs global batch), model creation inside strategy.scope, compilation, fit, and a quick verification method to confirm both GPUs are utilized?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a single SavedModel with two signatures feeding distinct subgraphs. Implement per-model batching: A uses max_batch_size=32, batch_timeout_micros=1000; B uses 16 and 2000. Separate in-queue polic",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use tf.distribute.MirroredStrategy to do data-parallel training across GPUs. Compute global_batch_size = per_replica_batch_size * strategy.num_replicas_in_sync. Build the Keras model inside strategy.s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use `tf.data.experimental.make_csv_dataset` to create the dataset with batching, shuffling, and proper column defaults. Define `region_lookup = tf.keras.layers.StringLookup(vocabulary=['north','south','east','west'], nu…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would load data with image_dataset_from_directory('path/train', image_size=(64,64), batch_size=32, label_mode='binary'), add augmentation via a preprocessing.Sequential([RandomFlip('horizontal'), Ra",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of basic distribution strategies, parallel data processing, and practical verification in a beginner setting.\n\n## Key Concepts\n- tf.distribute.MirroredStrategy\n- global vs per-replica batch sizing\n- strategy.scope for model construction\n- tf.data pipeline with batching and prefetch\n- runtime verification of multi-GPU usage\n\n## Code Example\n```python\nimport tensorflow as tf\nstrategy = tf.distribute.MirroredStrategy()\nper_replica_batch_size = 16\nglobal_batch_size = per_replica_batch_size * strategy.num_replicas_in_sync\n\nwith strategy.scope():\n    model = tf.keras.Sequential([\n        tf.keras.layers.Conv2D(32, 3, activation='relu', input_shape=(128,128,3)),\n        tf.keras.layers.MaxPooling2D(),\n        tf.keras.layers.Flatten(),\n        tf.keras.layers.Dense(10, activation='softmax')\n    ])\n    model.compile(optimizer='adam', loss='sparse_categorical_crossentropy', metrics=['accuracy'])\n\n# toy data\nX = ...; y = ...\nds = tf.data.Dataset.from_tensor_slices((X, y)).batch(global_batch_size).prefetch(tf.data.AUTOTUNE)\n\nmodel.fit(ds, epochs=5)\n```\n\n## Follow-up Questions\n- How would you adjust for dynamic batch sizes or more GPUs?\n- What are potential bottlenecks and how would you monitor them?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-2",
        "questionId": "q-16592",
        "question": "Given a CSV with columns text,label (label in 0..2), implement a beginner-friendly TensorFlow 2.x text classifier using a word-level TextVectorization and a small Dense head in Keras. Include: data loading and train/test split, building the model, compilation with SparseCategoricalCrossentropy, a minimal model.fit on toy data, and a simple evaluation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Prepare a toy dataset; TextVectorization(max_tokens=5000, output_sequence_length=100, standardize='lower_and_strip_punctuation'); build a Sequential model: Embedding(5000,32) -> GlobalAveragePooling1D",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Proposed approach: implement a tf.data pipeline that slices each CSV into overlapping windows of 20 steps and corresponding next value. Normalize the window with tf.keras.layers.Normalization. Build a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set global_batch_size to 256, yielding per_replica_batch_size of 32 across 8 workers. Build the dataset using input_context for deterministic sharding: ds = ds.shard(input_context.num_input_pipelines, input_context.inpu…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt versioned TF Serving with a vocab service and embedding-extension. Base vocab stays fixed; new tokens are allocated in a small extension matrix loaded alongside the base at runtime. A new model version is deployed…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end basics of text preprocessing and a small neural head, including how to wire TextVectorization, embeddings, and a simple classifier, plus a toy data flow.\n\n## Key Concepts\n- TextVectorization and Embedding\n- Simple feedforward classifier\n- Mini dataset split with model.fit/evaluate\n- from_logits loss handling\n\n## Code Example\n```javascript\nimport tensorflow as tf\nfrom tensorflow.keras.layers import TextVectorization, Embedding, GlobalAveragePooling1D, Dense\nfrom tensorflow.keras import Sequential\n\ntexts = [\"good\", \"bad\", \"okay\"]\nlabels = [0, 1, 0]\n# Placeholder for building the actual pipeline in a full answer\n```\n\n## Follow-up Questions\n- How would you compare word-level vs. character-level vectorization?\n- How would you extend to 5 classes and more data to avoid overfitting?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-3",
        "questionId": "q-15643",
        "question": "Given a CSV with columns feature1 to feature20 and label (0/1), build a TensorFlow 2.x pipeline that reads the CSV with tf.data, imputes missing values using per-feature medians, standardizes features on the fly, and trains a two-layer MLP via the Keras Functional API. Include the data ingestion code, a preprocessing layer for normalization, training with model.fit, and an evaluation plan that prints a confusion matrix?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition with tf.distribute.MirroredStrategy across GPUs, place encoder on GPU:0 and decoder/heads on GPU:1; export as a single SavedModel. In TF Serving, add a lightweight wrapper that shards each b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a tf.data CSV pipeline using make_csv_dataset to efficiently parse the dataset, apply per-feature normalization via Normalization layers for numeric features, encode the categorical 'cat' feature using StringLo…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Compute per-feature medians offline. Load data with tf.data.experimental.make_csv_dataset(batch_size=128). In preprocessing, replace NaNs with medians, then feed into a tf.keras.layers.Normalization (",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Load data with image_dataset_from_directory for train and val (180x180, batch 32). Add augmentation (RandomFlip, RandomBrightness). Build a tiny CNN: Conv2D(32,3), MaxPool, Conv2D(64,3), MaxPool, Flat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical TensorFlow 2.x skills: data ingestion from CSV using tf.data, preprocessing with missing-value imputation and normalization, and building a compact neural network using the Keras Functional API. It also covers evaluation with a confusion matrix, a common real-world metric.\n\n## Key Concepts\n- tf.data CSV ingestion\n- Missing value imputation (per-feature medians)\n- On-the-fly feature normalization\n- Keras Functional API model construction\n- Model.fit and evaluation with confusion matrix\n\n## Code Example\n```python\nimport tensorflow as tf\nimport numpy as np\n\n# 1) Offline medians per feature (precomputed)\nmedians = np.array([0.0]*20)  # replace with real medians for feature1..feature20\n\ndef preprocess(features, label):\n    x = features  # dict of features\n    # convert to tensor of shape (20,)\n    arr = tf.stack([x[f'feature{i+1}'] for i in range(20)], axis=1)\n    arr = tf.where(tf.math.is_nan(arr), medians, arr)\n    return arr, label\n\n# 2) Data ingestion\nds = tf.data.experimental.make_csv_dataset(\n    'data.csv', batch_size=128, label_name='label', num_epochs=1\n)\nds = ds.map(preprocess)\n\n# 3) Preprocessing + model\ninputs = tf.keras.Input(shape=(20,), dtype=tf.float32)\nnormalizer = tf.keras.layers.Normalization()\nnormalizer.adapt(np.zeros((1,20)))  # example, replace with real adapt on training data\nx = normalizer(inputs)\nx = tf.keras.layers.Dense(64, activation='relu')(x)\noutputs = tf.keras.layers.Dense(1, activation='sigmoid')(x)\nmodel = tf.keras.Model(inputs=inputs, outputs=outputs)\nmodel.compile(optimizer='adam', loss='binary_crossentropy', metrics=['accuracy'])\n\n# 4) Train & evaluate\n# Note: you would typically call model.fit on a proper tf.data.Dataset; here is a simplified call\nmodel.fit(ds, epochs=5)\n# Evaluation and confusion matrix\n# y_true, y_pred = ...\n# cm = tf.math.confusion_matrix(y_true, tf.cast(y_pred>0.5, tf.int32))\n```\n\n## Follow-up Questions\n- How would you scale this to 100+ features with feature-wise normalization?\n- How would you handle class imbalance and monitor calibration?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-4",
        "questionId": "q-11405",
        "question": "Design a real-time inference stack for a multilingual Transformer (TF 2.x) served via TensorFlow Serving in a shared cluster supporting 1k tenants with per-tenant quotas and per-tenant model versions. Describe routing, dynamic batching, multi-tenant isolation, zero-downtime model updates, monitoring of p95 latency under bursts, and failure handling. Include a minimal TF Serving config and a Python sketch for tenant routing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan a chaos test with GPU memory pressure and latency spikes: inject faults via a Kubernetes chaos tool, enforce per-tenant budgets with queueing, monitor p95/p99 latency, tail error rate, and GPU me",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a per-tenant proxy routing to tenant-specific TF-Serving shards, enable dynamic_batching on each shard, and use versioned models with hot swapping for zero-downtime updates. Enforce per-tenant",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a shadow/canary rollout: deploy the new TFServing model under a separate name and route 5–10% of traffic to it via a service mesh (Istio/Linkerd) while the rest uses the baseline. Collect la",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Architecture Overview** **Embedding Sharding**: Partition the 100M user embeddings and 1M item embeddings using consistent hashing (e.g., Ring MD5 or MurmurHash) across 4 GPUs. Each GPU hosts approximately 25M user em…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design scalable, isolated multi-tenant serving with strict latency SLAs and zero-downtime updates. Encourages thinking about routing, batching, quotas, and observability in TensorFlow Serving at scale.\n\n## Key Concepts\n\n- Multi-tenant isolation and quota enforcement\n- Dynamic batching and per-tenant models\n- Zero-downtime updates via versioned models\n- Tail latency, monitoring, circuit breakers, graceful fallback\n\n## Code Example\n\n```python\n# tenant-aware routing mock\nimport os\n\ndef route(request):\n  tenant = request.headers.get('X-TENANT')\n  model = load_model_for(tenant)  # tenant-specific path/version\n  return model.predict(request.payload)\n```\n\n## Follow-up Questions\n\n- How would you test burst scenarios and validate tail latency under 99th percentile?\n- Which metrics and alerts would you deploy, and how would you auto-scale?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-5",
        "questionId": "q-21590",
        "question": "You have a single TensorFlow Serving SavedModel that handles both text and numeric features, and you want to share it across multiple tenants with strict data isolation. Propose a concrete plan to implement per-tenant shards inside the model (tenant_id selects an embedding shard), distinct input signatures for routing, tenant-aware quotas, and deterministic preprocessing for caching. Include (1) model structure and signature design, (2) in-process routing and out-of-band enforcement, (3) memory and cache isolation, (4) rollout/rollforward strategy with zero-downtime updates, (5) a practical test plan with tail-latency thresholds and cache hit/miss metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design the pipeline to keep data loading ahead of compute: use TFRecordDataset with num_parallel_reads set to the number of shards, map with num_parallel_calls, cache either locally or on GCS, shuffle, batch, and prefet…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a single SavedModel with a shared backbone and tenant_id selecting a dedicated embedding shard. Expose two signatures: route_signature (tenant routing) and predict_signature (inference). Use pe",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Set seeds for Python, NumPy, and TensorFlow to fixed values (42). Disable reshuffles and enable deterministic ops. Apply deterministic options to the tf.data pipeline and shard per replica using strat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a SavedModel with two signatures: /preprocess (tenant_id to normalize features) and /serve (inference). Enable dynamic_batching in TF Serving with per-tenant batch policies; add a lightweight",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical multi-tenant deployment: model-level routing, isolation, and rollout.\n\n## Key Concepts\n- TensorFlow Serving signatures\n- tenant routing via tenant_id\n- per-tenant cache/memory accounting\n- zero-downtime updates and traffic-splitting\n\n## Code Example\n```python\n# Routing sketch (pseudocode)\ndef route_by_tenant(input_batch, tenant_id):\n    shard = map_tenant_to_shard(tenant_id)\n    return model.predict(input_batch, shard=shard)\n```\n\n## Follow-up Questions\n- How to monitor isolation breaches and capacity? \n- How would you test for tenant-specific SLA violations?",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-6",
        "questionId": "q-7683",
        "question": "In TensorFlow Serving on Kubernetes, you must serve a multi-tenant image classifier where tenants require different input shapes and throughput. Propose a robust strategy to handle dynamic inputs, enable efficient batching, and prevent cross-tenant memory contention, including model config, pre-processing, and resource isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Stage the flow as a two-tier path: a lightweight preprocessor with a dynamic batcher max 16, CPU pinning via cpuset, and stable memory pools; apply static INT8 quantization with MKL-DNN kernels and pr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a reproducible, end-to-end tiny pipeline: load the CSV with tf.data, map features (age, gender, income) to tensors, normalize age and income with a Normalization layer, encode gender with StringLookup + CategoryEnco…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Isolate tenants with per-tenant models in TF Serving using a model_config_list, each with its own batching_parameters (max_batch_size, batch_timeout_micros). Pre‑process inputs to a fixed canonical si",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use dual vocab files with an atomic, read-only loader that flips active vocab version under a lock during updates. Maintain vocab A and B; switch writes to B while reads can continue on A, then swap.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design production-grade multi-tenant TF Serving with batching, resource isolation, input normalization, and latency guarantees across Kubernetes, TF Serving batching, and data-plane preprocessing.\n\n## Key Concepts\n- TensorFlow Serving model_config_list\n- dynamic_batching and batching_parameters\n- per-tenant isolation via separate models/pods\n- input normalization to fixed shapes for uniform batching\n\n## Code Example\n```yaml\nmodel_config_list {\n  config {\n    name: \"tenantA\"\n    base_path: \"/models/tenantA\"\n    model_platform: \"tensorflow\"\n  }\n  config {\n    name: \"tenantB\"\n    base_path: \"/models/tenantB\"\n    model_platform: \"tensorflow\"\n  }\n}\n```\n\n## Follow-up Questions\n- How would you measure cross-tenant cache/pool isolation?\n- How would you adapt batching if a tenant intermittently uses near-zero traffic?",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-7",
        "questionId": "q-27093",
        "question": "Scenario: An e-commerce dataset includes a numeric feature 'views' and 'price', a categorical 'brand_id' encoded as integers, and a target 'revenue'. Build a beginner TensorFlow 2.x model (Keras Functional API) that (1) constructs a small tf.data pipeline reading a CSV with columns views, price, brand_id, revenue, (2) uses an embedding for brand_id and merges with numeric features, (3) trains a regression model with MSE, (4) evaluates on a holdout, (5) provides a minimal code sketch and rationale?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a single SavedModel with inputs: input_ids and style_id; implement a StyleEmbedding layer that maps style_id to a per-style vector and concatenates with token embeddings before the decoder. Use a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a tf.data pipeline: normalize inputs to [0,1], then shuffle, batch, and prefetch. Create a small Keras CNN with 2 Conv2D layers, ReLU, MaxPool, Flatten, Dense(64), Dense(10) with softmax. Compil",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-input regression model: numeric features pass through Dense(16, relu); brand_id via Embedding(16,4) then Flatten; concatenate and pass through Dense(32, relu) and Dense(1). tf.data reads CSV, norm",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "In tf.distribute.MirroredStrategy across 4 GPUs, accumulate gradients on every micro-batch using a per-replica buffer. After N steps, call optimizer.apply_gradients with the averaged grads; scale the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to combine numeric and categorical features in a small TensorFlow 2.x model, and to wire a tf.data CSV pipeline with per-feature normalization and a train/holdout split.\n\n## Key Concepts\n\n- tf.data CSV parsing and per-feature normalization\n- Keras Functional API with multiple inputs\n- Embedding for categorical features\n- Simple regression with MSE and train/eval flow\n\n## Code Example\n\n```javascript\n// Implementation code here\n```\n\n## Follow-up Questions\n\n- How would you handle unseen brand_ids at inference time?\n- What changes would you make to support unbalanced data?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-8",
        "questionId": "q-20966",
        "question": "Context: Beginner TensorFlow 2.x regression on tabular data. You have a CSV with columns user_id (string), f1..f10 (float), target (float). Design a beginner workflow: (1) build a tf.data pipeline that parses CSV, imputes missing values with column means, hashes user_id to a discrete bucket and feeds an embedding alongside numeric features, normalizes numeric features, (2) implement a small Keras Functional API model (Input.concat Embedding) → Dense(64, relu) → Dense(32, relu) → Dense(1), (3) train with MSE on a held-out split, (4) report RMSE on hold-out. Provide a minimal code sketch and rationale?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Keras Functional API model with two inputs: user_id (integer) and numeric features [f1, f2]. Create an Embedding layer for user_id with input_dim=num_users+1 and output_dim=4, then flatten it. Concatenate the flat…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a tf.data pipeline that reads the CSV, parses numeric features and the user_id string, imputes missing values with column means, hashes user_id to ~1000 discrete buckets, and feeds the resulting integer IDs throug…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use tf.data.experimental.make_csv_dataset to read 20 features and a binary label, split 80/20, then standardize features in a map using training stats. Build a 2-hidden-layer dense network (20 -> 64 -",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Load a CSV with filenames and labels using tf.data, map to image tensors (read_file -> decode_png grayscale -> resize to 28x28) and normalize. Apply augmentation (random_flip_left_right, random_bright",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design a practical, beginner-friendly regression workflow for tabular data that includes a categorical feature. It combines preprocessing (imputation, hashing/embedding, normalization), a compact Keras model, and hold-out evaluation.\n\n## Key Concepts\n- tf.data CSV parsing and batch pipelines\n- Missing value imputation strategies\n- Categorical feature handling: hashing + embedding vs. one-hot encoding\n- Feature normalization and Functional API design\n- Model evaluation using RMSE\n\n## Code Example\n```python\nimport tensorflow as tf\nfrom tensorflow import keras\nfrom tensorflow.keras import layers\n\n# Hyperparameters\nNUM_BUCKETS = 1000\nEMBEDDING_DIM = 8\nBATCH_SIZE = 32\n\n# 1. Build tf.data pipeline\ndef parse_csv(row):\n    columns = [tf.io.decode_csv(row, [[0.0]] * 10 + [[0.0]])]\n    features = {'f1': columns[0][0], 'f2': columns[1][0], \n                'f3': columns[2][0], 'f4': columns[3][0],\n                'f5': columns[4][0], 'f6': columns[5][0],\n                'f7': columns[6][0], 'f8': columns[7][0],\n                'f9': columns[8][0], 'f10': columns[9][0],\n                'target': columns[10][0], 'user_id': columns[11][0]}\n    return features\n\ndataset = tf.data.TextLineDataset('data.csv').skip(1).map(parse_csv)\n\n# 2. Impute missing values with column means\ndef impute_and_normalize(features, mean_values, std_values):\n    numeric_features = tf.stack([features[f'f{i}'] for i in range(1, 11)], axis=1)\n    numeric_features = tf.where(tf.math.is_nan(numeric_features),\n                                 tf.gather(mean_values, tf.range(10)),\n                                 numeric_features)\n    normalized = (numeric_features - mean_values) / (std_values + 1e-7)\n    \n    user_id_hashed = tf.strings.to_hash_bucket_fast(features['user_id'], NUM_BUCKETS)\n    return {'numeric': normalized, 'user_id': user_id_hashed}, features['target']\n\n# Compute means/stds from training data (omitted for brevity)\nmean_values = tf.constant([0.5] * 10, dtype=tf.float32)\nstd_values = tf.constant([1.0] * 10, dtype=tf.float32)\n\ndataset = dataset.map(lambda row: impute_and_normalize(row, mean_values, std_values))\ndataset = dataset.batch(BATCH_SIZE).prefetch(tf.data.AUTOTUNE)\n\n# 3. Build Keras Functional API model\nuser_id_input = keras.Input(shape=(1,), dtype=tf.int32, name='user_id')\nnumeric_input = keras.Input(shape=(10,), name='numeric')\n\nembedding = layers.Embedding(NUM_BUCKETS, EMBEDDING_DIM)(user_id_input)\nembedding_flat = layers.Flatten()(embedding)\n\nconcatenated = layers.Concatenate()([embedding_flat, numeric_input])\nx = layers.Dense(64, activation='relu')(concatenated)\nx = layers.Dense(32, activation='relu')(x)\noutput = layers.Dense(1)(x)\n\nmodel = keras.Model(inputs=[user_id_input, numeric_input], outputs=output)\nmodel.compile(optimizer='adam', loss='mse')\n\n# 4. Train with MSE on held-out split\ntrain_ds, val_ds = dataset(0.take.8), dataset.skip(0.8)\nmodel.fit(train_ds, validation_data=val_ds, epochs=10)\n\n# 5. Report RMSE on hold-out\nimport numpy as np\nval_targets = []\npredictions = []\nfor batch in val_ds:\n    val_targets.extend(batch[1].numpy())\n    predictions.extend(model.predict(batch[0]).flatten())\nrmse = np.sqrt(np.mean((np.array(val_targets) - np.array(predictions)) ** 2))\nprint(f'Hold-out RMSE: {rmse:.4f}')\n```\n\n## Rationale\n- **Hashing + Embedding** avoids the cold-start problem of fixed vocabularies and is more memory-efficient than one-hot encoding for high-cardinality categoricals.\n- **Column mean imputation** is simple and effective for beginners; more advanced methods (e.g., KNN imputation) can be explored later.\n- **Normalization** ensures numeric features have comparable scales, accelerating training.\n- **Functional API** clearly demonstrates how to combine heterogeneous input types (embedding + numeric features).",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-9",
        "questionId": "q-19009",
        "question": "You need to serve a real-time recommendation model in TensorFlow Serving that blends dense and sparse features via embeddings; design per-tenant feature flags and embedding namespace isolation so tenants A,B,C can enable/disable features without redeploy, and embeddings can be updated independently. Outline the architecture and signing, embedding cache invalidation, safe hot-swaps with traffic splitting, and observability tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by reproducing the issue in a staging environment using deterministic inputs and systematic memory profiling. Instrument TF Serving to capture per-model peak GPU memory usage, Python allocation patterns, and batch…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use MirroredStrategy with per-replica batch size 64; target global batch size 1024, so accum_steps = 16. Within a tf.function under strategy.scope, accumulate gradients over 16 steps and apply once using optimizer.apply…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a gateway to route tenantIds to per-tenant SavedModel directories (models/tenantX/versions/1,2) with TF Serving config per tenant. Run a canary rollout by shifting a subset of traffic to a new ver",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe a TensorFlow Serving design where a single SavedModel exposes two signatures: 'infer' for runtime feature inputs and 'embed' for tenant-specific embeddings. Implement per-tenant feature flags",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes ability to design multi-tenant TF Serving with per-tenant feature flags, per-tenant embedding namespaces, and independent embedding updates without redeploy. It tests signing, routing, caching, and safe hot-swaps.\n\n## Key Concepts\n- TensorFlow Serving signatures\n- Tenant isolation with feature flags\n- Embedding caches and namespaces\n- Canary releases and hot-swaps\n- Observability: latency, cache metrics\n\n## Code Example\n```python\n# Placeholder example: define signatures for 'infer' and 'embed'\nimport tensorflow as tf\nclass MyModel(tf.Module):\n  @tf.function(input_signature=[tf.TensorSpec([None, 128], tf.float32)])\n  def infer(self, features):\n    pass\n  @tf.function(input_signature=[tf.TensorSpec([None], tf.int32)])\n  def embed(self, tenant_id):\n    pass\n```\n\n## Follow-up Questions\n- How would you test per-tenant isolation under burst traffic?\n- How do you validate embedding cache eviction policies for cold tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-10",
        "questionId": "q-17181",
        "question": "In a multi-GPU setup, you must train a 100M-parameter CNN on 256x256 images with TensorFlow 2.x. Describe how to implement data-parallel training with tf.distribute.MirroredStrategy, covering per-replica vs global batch sizes, input pipeline wrapping, mixed precision, and gradient synchronization. What are common pitfalls and how would you validate throughput and convergence?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Training loop uses tf.GradientTape to compute grads, then optimizer.apply_gradients(zip(grads, model.trainable_variables)); loss = loss_fn(y, preds). Log train_loss with tf.summary.scalar; track globa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed: a SavedModel with a shared trunk and two heads. Signature '/multimodal' accepts {'mode': string, 'image': float32[batch,224,224,3], 'text': string}. Use tf.switch_case on mode to route to im",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use tf.distribute.MirroredStrategy across GPUs; set global_batch_size = per_replica_batch_size * strategy.num_replicas_in_sync. Wrap dataset with strategy.experimental_distribute_dataset and run insid",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-model dynamic batching with separate queues and quotas on a single GPU node. Assign BatchConfig per model: A (max_batch_size=16, batch_timeout_micros=1000, max_enqueued_batches=64), B (8, 2000",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical knowledge of distributed training, batch sizing, precision, and debugging across GPUs, plus how to validate throughput and convergence.\n\n## Key Concepts\n- tf.distribute.MirroredStrategy\n- global vs per-replica batch size\n- strategy.experimental_distribute_dataset\n- mixed_precision\n- gradient synchronization\n- throughput and convergence validation\n\n## Code Example\n```python\nimport tensorflow as tf\nstrategy = tf.distribute.MirroredStrategy()\nwith strategy.scope():\n  model = ...  # CNN model\n  optimizer = tf.keras.optimizers.Adam()\n  model.compile(optimizer=optimizer,\n                loss='sparse_categorical_crossentropy',\n                metrics=['accuracy'])\n  per_replica_bs = 32\n  global_bs = per_replica_bs * strategy.num_replicas_in_sync\n  ds = ...  # tf.data pipeline\n  ds = ds.shuffle(1000).batch(global_bs).prefetch(tf.data.AUTOTUNE)\n  ds = strategy.experimental_distribute_dataset(ds)\n  model.fit(ds, epochs=..., steps_per_epoch=...)\n```\n\n## Follow-up Questions\n- How would you adapt this if only 1 GPU is available?\n- How do you handle gradient accumulation for larger effective batch sizes?",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-11",
        "questionId": "q-1752",
        "question": "In training a Transformer language model on 4 GPUs with mixed precision using tf.distribute.MirroredStrategy, how would you implement gradient accumulation to simulate a larger global batch while keeping updates stable? Provide a minimal code snippet showing an accumulation loop and the cadence for applying the optimizer?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a TextVectorization layer with max_tokens=20000 and int outputs, feed into Embedding(64), then GlobalAveragePooling1D and Dense(1, sigmoid). Compile with binary_crossentropy and Adam, and fit on a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by establishing an FP32 baseline for both accuracy and latency metrics. Apply post-training quantization to INT8 first; if accuracy degradation exceeds approximately 1%, transition to Quantization-Aware Training.…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a tenant-scoped schema registry (versioned JSON schemas) and a fast in-process cache. The preprocessor loads tenant/version, aligns features to a fixed tensor order, coerces types, and fills",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use gradient accumulation across micro-batches within a tf.distribute strategy, computing per-replica grads with strategy.run and summing them before a single optimizer.apply_gradients call. Scale the",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical understanding of distributed training with gradient accumulation and mixed-precision stability across devices.\n\n## Key Concepts\n\n- tf.distribute.MirroredStrategy for multi-GPU training\n- gradient accumulation to simulate larger batch sizes\n- per-replica vs global gradients synchronization\n- mixed-precision loss scaling and stable updates\n- cadence control for applying updates across replicas\n\n## Code Example\n\n```javascript\n# Pseudo-Python/TensorFlow code shown in a javascript fenced block\naccum_grads = [tf.zeros_like(v) for v in model.trainable_variables]\naccum_steps = K  # number of micro-batches to accumulate\n\ndef apply_update():\n    grads = [g / accum_steps for g in accum_grads]\n    optimizer.apply_gradients(zip(grads, model.trainable_variables))\n    for i in range(len(accum_grads)):\n        accum_grads[i] = tf.zeros_like(accum_grads[i])\n\nfor step, (x, y) in enumerate(dataset):\n    with tf.GradientTape() as tape:\n        preds = model(x, training=True)\n        loss = loss_fn(y, preds) / accum_steps  # scale loss\n    grads = tape.gradient(loss, model.trainable_variables)\n    accum_grads = [a + g for a, g in zip(accum_grads, grads)]\n    if (step + 1) % accum_steps == 0:\n        apply_update()\n```\n\n## Follow-up Questions\n\n- How would you verify no gradient leakage across devices during accumulation?\n- How would you adapt this approach for dynamic sequence lengths or heterogeneous hardware?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-12",
        "questionId": "q-9843",
        "question": "You're deploying a TensorFlow model for real-time fraud detection that must run on both CPU and GPU under mixed precision. Explain a robust inference deployment plan that supports dynamic batching, model versioning with canaries, deterministic CPU fallback, and end-to-end monitoring with Prometheus and OpenTelemetry. Include concrete steps, tooling choices, and a minimal code snippet to illustrate the setup?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy TensorFlow Serving with versioned model repositories and configurable canary traffic routing; enable GPU mixed-precision inference with automatic CPU fallback to maintain deterministic latency when GPU resources…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a single SavedModel with two signatures (predict_stream and predict_archive). Route by tenant via an X-Tenant header to one TF Serving instance. Implement tenant-aware dynamic batching with per-te",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a tf.data service cluster (Dispatcher + multiple Workers) to parallelize preprocessing. Build a single dataset that yields (image, text); deterministically shard across workers: dataset = dataset.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a TextVectorization layer with max_tokens=10000, output_mode='int', sequence_length=100, oov_token='[UNK]', and standardize='lower_and_strip_punctuation'. Adapt the layer on the text dataset, then build a smal…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEvaluates practical production ML deployment capabilities: canary releases, mixed precision optimization, dynamic batching, and observability across heterogeneous compute environments.\n\n## Key Concepts\n\n- TensorFlow Serving versioned deployments with canary traffic routing\n- Mixed precision inference on GPUs with deterministic CPU fallback\n- Dynamic batching to balance latency and throughput requirements\n- Prometheus metrics collection and OpenTelemetry distributed tracing\n- Real-time fraud detection performance constraints (<100ms latency)\n\n## Code Example\n\n```python\nimport tensorflow as tf\nfrom tensorflow_serving.apis import predict_pb2\nimport prometheus_client\nfrom opentelemetry import trace\n\n# Prometheus metrics\nLATENCY_HISTOGRAM = prometheus_client.Histogram(\n    'inference_latency_seconds', 'Inference latency by device', ['device']\n)\nBATCH_SIZE_GAUGE = prometheus_client.Gauge(\n    'dynamic_batch_size', 'Current batch size'\n)\nERROR_COUNTER = prometheus_client.Counter(\n    'inference_errors_total', 'Total inference errors', ['device', 'error_type']\n)\n\nclass FraudDetectionInference:\n    def __init__(self):\n        self.tracer = trace.get_tracer(__name__)\n        self.use_gpu = tf.config.list_physical_devices('GPU')\n        self.model = self._load_model()\n    \n    def _load_model(self):\n        return tf.keras.models.load_model('fraud_detection_model')\n    \n    @LATENCY_HISTOGRAM.time()\n    def predict(self, inputs):\n        with self.tracer.start_as_current_span('fraud_detection'):\n            try:\n                if self.use_gpu:\n                    return self._gpu_inference(inputs)\n                else:\n                    return self._cpu_inference(inputs)\n            except Exception as e:\n                ERROR_COUNTER.labels(\n                    device='gpu' if self.use_gpu else 'cpu',\n                    error_type=type(e).__name__\n                ).inc()\n                # Fallback to CPU on GPU failures\n                return self._cpu_inference(inputs)\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-13",
        "questionId": "q-12381",
        "question": "You have a small two-class image dataset stored in folders per class under a local directory. Build a beginner-friendly TensorFlow 2.x image classifier using the Keras Functional API. Load data with image_dataset_from_directory, apply lightweight augmentation via preprocessing layers, implement a small CNN, train with model.fit, and report validation accuracy. Provide a minimal code outline and training plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To support a live vocabulary without restarts, use a tf.lookup.MutableHashTable for string item_id keys, storing per-key embedding vectors. Preload current Redis entries, then run a lightweight backgr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use tf.data.experimental.make_csv_dataset to load train.csv and test.csv, map to inputs [distance_km, traffic_index, weather_code, driver_rating] and label eta_seconds, batch and prefetch. Build a sin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a TextVectorization layer with max_tokens=10000, output_mode='int', sequence_length=100, oov_token='[UNK]', and standardize='lower_and_strip_punctuation'. Adapt the layer on the text dataset, then build a smal…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would load data with image_dataset_from_directory('path/train', image_size=(64,64), batch_size=32, label_mode='binary'), add augmentation via a preprocessing.Sequential([RandomFlip('horizontal'), Ra",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nDemonstrates practical data loading from directories, lightweight augmentation, and a compact model suitable for beginners.\n\n## Key Concepts\n- image_dataset_from_directory for simple pipelines\n- preprocessing layers for augmentation\n- small CNN via Keras Functional API\n- binary_crossentropy, Adam, model.fit\n\n## Code Example\n```javascript\nimport tensorflow as tf\nfrom tensorflow.keras import layers, models\n\ntrain_ds = tf.keras.utils.image_dataset_from_directory(\n    'path/train', image_size=(64,64), batch_size=32, label_mode='binary')\nval_ds = tf.keras.utils.image_dataset_from_directory(\n    'path/val', image_size=(64,64), batch_size=32, label_mode='binary')\n\naugmentation = tf.keras.Sequential([\n  layers.RandomFlip('horizontal'),\n  layers.RandomRotation(0.1)\n])\n\ninputs = layers.Input(shape=(64,64,3))\nx = augmentation(inputs)\nx = layers.Conv2D(32, 3, activation='relu')(x)\nx = layers.MaxPooling2D()(x)\nx = layers.Conv2D(64, 3, activation='relu')(x)\nx = layers.MaxPooling2D()(x)\nx = layers.Flatten()(x)\nx = layers.Dense(64, activation='relu')(x)\noutputs = layers.Dense(1, activation='sigmoid')(x)\n\nmodel = models.Model(inputs, outputs)\nmodel.compile(optimizer='adam', loss='binary_crossentropy', metrics=['accuracy'])\nmodel.fit(train_ds, validation_data=val_ds, epochs=5)\n```\n\n## Follow-up Questions\n- How would you adapt for 3+ classes and use categorical_crossentropy?\n- What changes if you switch to tf.keras.applications with transfer learning?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-14",
        "questionId": "q-8413",
        "question": "In TensorFlow 2.x, deploy a single multi-tenant image classifier on Kubernetes where tenants share a model but have different latency budgets; outline a practical per-tenant dynamic batching strategy. Include (1) routing requests by tenant, (2) implementing per-tenant batching (shared pipeline vs custom layer), (3) measuring per-tenant latency/throughput and enforcing budgets. Provide a minimal per-tenant batcher sketch and discuss trade‑offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement tenant-aware batching on a shared TF Serving GPU: per-tenant queues and batchers, each with its own max_batch_size and batch_timeout_micros, enforce per-tenant quotas to prevent head-of-line",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route requests by tenant_id through a lightweight proxy that maintains per-tenant queues. Within each tenant queue, implement dynamic batching that groups requests up to a configurable max_batch_size with a short timeou…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a single multi-input SavedModel signature that accepts text_ids: int32, nums: float32, and outputs a final score. Implement two subnets (text encoder and numeric path) feeding a gating head; prepr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a compact CNN: Conv2D(32,3x3) ReLU MaxPool, Conv2D(64,3x3) ReLU MaxPool, Flatten, Dense(128) ReLU, Dense(10) Softmax. Train on CPU with Adam and sparse_categorical_crossentropy, save as Save",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nMulti-tenant inference systems are increasingly common in production environments, testing the ability to design solutions that balance resource efficiency with per-tenant performance guarantees.\n\n## Key Concepts\n- Tenant-aware request routing and isolation\n- Dynamic batching with latency constraints\n- SLA monitoring and budget enforcement\n- Shared inference pipeline with tenant-specific optimization\n\n## Code Example\n```python\n# minimal sketch\nfrom collections import defaultdict, deque\nimport asyncio\n\nqueues = defaultdict(asyncio.Queue)  # per-tenant queues\n\nasync def batch_processor(tenant_id, max_batch_size=32, timeout_ms=50):\n    while True:\n        batch = []\n        deadline = asyncio.get_event_loop().time() + timeout_ms/1000\n        \n        # Collect requests with timeout\n        while len(batch) < max_batch_size:\n            try:\n                remaining_time = deadline - asyncio.get_event_loop().time()\n                if remaining_time <= 0:\n                    break\n                request = await asyncio.wait_for(\n                    queues[tenant_id].get(), timeout=remaining_time\n                )\n                batch.append(request)\n            except asyncio.TimeoutError:\n                break\n        \n        if batch:\n            # Process batch through shared TF pipeline\n            results = await process_batch(batch, tenant_id)\n            # Send results back and update metrics\n            await send_results(results)\n            update_latency_metrics(tenant_id, batch)\n```\n\n## Trade-offs\n**Shared Pipeline Benefits:**\n- Higher GPU utilization through larger effective batches\n- Reduced model memory footprint\n- Simpler deployment and maintenance\n\n**Per-tenant Considerations:**\n- Need careful queue management to prevent head-of-line blocking\n- Must monitor per-tenant SLAs and implement backpressure\n- May require priority queuing for premium tenants\n\n**Implementation Complexity:**\n- Requires sophisticated batching logic with timeout handling\n- Need robust monitoring and alerting for SLA violations\n- Must handle tenant-specific preprocessing/postprocessing",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-15",
        "questionId": "q-10081",
        "question": "In production, you run a single TensorFlow Serving cluster behind a gateway handling 3 tenants. Each tenant has different latency budgets. Design a **tenant-aware** inference strategy using **dynamic_batching**, per-tenant routing, and isolated cache layers. Explain how you would configure **dynamic_batching**, model versions, and request routing, and include a minimal config snippet (dynamic_batching_config.json) and a sketch of a middleware to enforce per-tenant isolation. Also describe metrics to verify latency/throughput and how you’d perform tuning?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use tf.data.make_csv_dataset to stream rows, compute training-time imputations (mean for age and session_length; mode for country) via a small map step, then apply one-hot encoding with StringLookup +",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant model slots in TF Serving with dynamic_batching: define separate batching_config for each tenant, with max_batch_size, batch_timeout_mss, and per-tenant queue length. Route requests via",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Adopt a canary deployment with two model names (ETA-prod and ETA-canary) behind a service mesh; route ~10% of traffic to canary. Enable batching in TF Serving: max_batch_size: 64, batch_timeout_micros",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement TensorFlow Lite with a comprehensive quantization strategy to achieve sub-50ms inference on edge devices. Begin by comparing post-training quantization (PTQ) and quantization-aware training (QAT) approaches—PT…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world multi-tenant inference requires isolation and predictable latency. This question probes understanding of dynamic batching, routing, caching, and SLA assurance.\n\n## Key Concepts\n- TensorFlow Serving dynamic_batching per tenant\n- Tenant-aware routing and model versioning\n- Isolation of per-tenant caches and resources\n- Latency/throughput metrics (p50, p95, p99, tail latency)\n\n## Code Example\n```python\nconfig = {\n  'tenantA': {'max_batch_size': 32, 'batch_timeout_mss': 30},\n  'tenantB': {'max_batch_size': 16, 'batch_timeout_mss': 50}\n}\n```\n\n```python\n# High-level middleware sketch (Python):\ntenant = request.headers['X-Tenant']\nproxy_request_to_tenant_model(request, tenant)\n```\n\n## Follow-up Questions\n- How would you validate tenant isolation during a burst spike?\n- What tracing would you enable to diagnose per-tenant latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-16",
        "questionId": "q-14299",
        "question": "In a TensorFlow Serving deployment on Kubernetes handling a mix of requests with bursty traffic, how would you implement dynamic batching to reduce latency while preserving throughput? Specify how to tune max_batch_size, batch_timeout_micros, max_enqueued_batches, and num_batch_threads, and show a minimal batching config snippet and a metrics plan to validate latency targets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use tf.data to group records by user_id and generate sliding windows of length 15, mapping each window to (X, y) where y is the next target. Normalize features using a Normalization layer fitted to training statistics.…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use `tf.data.experimental.make_csv_dataset` to create the dataset with batching, shuffling, and proper column defaults. Define `region_lookup = tf.keras.layers.StringLookup(vocabulary=['north','south','east','west'], nu…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-input Keras model: numeric inputs normalized via a Normalization layer, city encoded with StringLookup and Embedding, concatenated, then a small Dense head to predict price. Build tf.data pipeline",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enable per-model dynamic batching in TensorFlow Serving and systematically tune batch parameters to optimize the latency-throughput trade-off under bursty traffic conditions. Begin with conservative baseline settings: m…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nDynamic batching under bursty traffic represents a critical production challenge in ML serving systems. This question assesses practical understanding of TensorFlow Serving's batching mechanics, the nuanced trade-offs between latency and throughput, and the ability to implement effective monitoring strategies for real-world deployments.\n\n## Key Concepts\n\n- TensorFlow Serving dynamic batching architecture and request aggregation\n- Parameter tuning fundamentals: max_batch_size, batch_timeout_micros, max_enqueued_batches, num_batch_threads\n- Tail latency optimization techniques using p95 and p99 metrics\n- Performance monitoring methodologies and validation frameworks\n- Resource utilization considerations and capacity planning",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-17",
        "questionId": "q-4483",
        "question": "You have a transformer-based text classifier served via TensorFlow Serving on Kubernetes. To handle bursts with latency under 20 ms, design an end-to-end serving approach using dynamic batching, on-the-fly padding, and a small cache for recent embeddings. Include how you'd configure the model server, enable zero-downtime updates with versioning, and how you'd monitor tail latency. What would be your concrete plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use tf.lite.TFLiteConverter.from_keras_model(model) with DEFAULT optimizations, optionally calibrate int8 with a representative_dataset. Convert to model.tflite, then run inference via tf.lite.Interpr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enforce determinism by combining fixed seeds, deterministic ops, and fixed parallelism. Set TF_DETERMINISTIC_OPS=1 and fix intra/inter op thread counts; use a global seed and per-replica seeds for dat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement: TextVectorization to produce token ids, an Embedding layer, global pooling, and a Dense head. Compute text_len as a numeric feature, pass it through a small Dense branch, then conca",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage TensorFlow Serving dynamic batching to coalesce requests, pad inputs to the batch's max length, and cache encoder outputs keyed by input signature to reduce recomputation during bursts. Run t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical skills in production ML systems: dynamic batching, padding strategies, caching, model versioning, and observability, focusing on latency tails.\n\n## Key Concepts\n\n- Dynamic batching in TensorFlow Serving\n- On-the-fly input padding; ragged vs padded inputs\n- Embedding cache to amortize repeated computations\n- Zero-downtime updates via model versioning and traffic shifting\n- Tail latency monitoring with Prometheus, TF Profiler, and tracing\n\n## Code Example\n\n```json\n{\n  \"model_config_list\": [\n    {\n      \"config\": {\n        \"name\": \"text_classifier\",\n        \"base_path\": \"/models/text_classifier/1\",\n        \"model_platform\": \"tensorflow\",\n        \"dynamic_batching\": {\n          \"max_batch_size\": 64,\n          \"batch_timeout_millis\": 5\n        }\n      }\n    }\n  ]\n}\n```\n\n```python\n# warmup for new version\ndef warm_up(serving_client, examples, reps=10):\n    for _ in range(reps):\n        _ = serving_client.predict(examples)\n```\n\n```mermaid\nflowchart TD\n  A[Client Requests] --> B[Dynamic Batching]\n  B --> C[Batch Collector]\n  C --> D[TF Serving]\n  D --> E[Response]\n  E --> F[Cache Hit?]\n```\n\n## Follow-up Questions\n\n- How would you verify canary traffic doesn't degrade user experience during rollout?\n- Which metrics and traces would you collect to pinpoint tail-latency causes?",
        "difficulty": "advanced"
      },
      {
        "id": "tensorflow-developer-q-18",
        "questionId": "q-5506",
        "question": "You're given a folder of CSV files containing daily metrics: timestamp, a, b, target. Build a tf.data pipeline that (1) reads all CSVs, (2) handles missing values by forward-filling within each file, (3) normalizes features to [0,1], (4) creates 12-day windows to predict the 13th value, and (5) trains a small feed-forward network with MSE loss. Provide a minimal, runnable outline of the dataset construction and a compact model?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a graph-batching layer that buckets graphs by node count, builds a RaggedTensor batch, and preserves per-graph masks so the GNN can operate on variable-sized inputs within a single batch. Attach a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a single SavedModel with a routing head that uses tenant_id and language_code to apply per-tenant calibration and a small embedding store fetched from Redis with a local cache. The head normali",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To handle streaming inference, design a tf.data pipeline with a 60-step causal window and online normalization (running mean/var). Use dataset.window(60, 1) and batch(1), plus prefetch and a compact i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use tf.data with make_csv_dataset to parse all CSVs, fill NaNs in a map with forward-fill logic, normalize features with a 0-1 scaler per-batch, create 12-step windows via dataset.window(12, 1, drop_remainder=True), and…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to assemble a reproducible tf.data pipeline for time-series CSVs with basic preprocessing, sequence windowing, and a lightweight model. Emphasizes practical steps over theory.\n\n## Key Concepts\n- tf.data and make_csv_dataset\n- NaN handling during preprocessing\n- Windowing for sequence-to-one prediction\n- Simple feed-forward model with MSE loss\n\n## Code Example\n```javascript\n// Implementation code here\n```\n\n## Follow-up Questions\n- How would you handle irregular time intervals or multiple targets?\n- What changes if the dataset is streamed from cloud storage?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-19",
        "questionId": "q-4621",
        "question": "You are building a beginner TensorFlow 2.x text classifier that reads sentences from a file and labels are provided separately. How would you implement a tf.data pipeline that tokenizes each line on whitespace, maps tokens to integer IDs using a StaticVocabularyTable built from the dataset, pads sequences to a fixed length, and batches for training? Include a minimal code snippet showing vocab creation, tokenization, and batching?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use two independent TF Serving models with per-model batching_config, enabling input-length bucketing and a shared routing proxy. Apply quantization/TF-TRT selectively on the encoder for speed, and us",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a fixed vocabulary via a StringLookup layer, tokenize with tf.strings.split, map tokens to IDs, and batch with padded_batch to a fixed length. Build vocab from the dataset with a <pad> token, then",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a tf.function with input_signature defined for a batch of images [None, 224, 224, 3], run model inference with training=False, apply tf.nn.softmax to convert logits to probabilities, and return a dictionary wi…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "```python import tensorflow as tf from tensorflow.keras import layers, models # Load images from directory - each subfolder is a class train_ds = tf.keras.utils.image_dataset_from_directory( 'data/train', image_size=(28…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical tf.data usage, tokenization, and vocabulary-based encoding for text models.\n\n## Key Concepts\n- tf.data pipeline: line-by-line streaming, tokenization, batching\n- tf.keras.layers.StringLookup for token -> ID mapping\n- Padding to fixed length with padded_batch\n- Embedding input expectations and reproducible vocab\n\n## Code Example\n```javascript\nimport tensorflow as tf\n\nvocab = [\"<pad>\",\"<unk>\",\"the\",\"cat\",\"sat\",\"on\",\"mat\"]\nlookup = tf.keras.layers.StringLookup(vocabulary=vocab, mask_token=None, oov_token=\"<unk>\")\n\ndef encode(line):\n    tokens = tf.strings.split(line)\n    ids = lookup(tokens)\n    return ids\n\ndataset = tf.data.TextLineDataset(\"data.txt\").map(encode).map(lambda ids: tf.cast(ids, tf.int32))\n\ndataloader = dataset.padded_batch(32, padding_values=0, padded_shapes=[None])\n```\n\n## Follow-up Questions\n- How would you handle OOV tokens beyond the vocabulary?\n- What are trade-offs of using FixedVocabulary vs subword tokenization for multilingual data?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-20",
        "questionId": "q-24210",
        "question": "You have a directory of grayscale images arranged by digit label (0-9) in subfolders named by digit, each image 28x28 PNG. Design a beginner TensorFlow 2.x CNN using the Keras Functional API that (1) loads data with tf.keras.utils.image_dataset_from_directory, (2) applies a simple data augmentation and normalization, (3) builds a small CNN with Conv2D, MaxPooling, and a Dense head, (4) compiles with SparseCategoricalCrossentropy, (5) trains with model.fit and evaluates on a held-out test set. Provide a minimal code sketch and rationale?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-model dynamic batching with separate queues and quotas on a single GPU node. Assign BatchConfig per model: A (max_batch_size=16, batch_timeout_micros=1000, max_enqueued_batches=64), B (8, 2000",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a tf.data pipeline: ds = tf.data.Dataset.list_files('train/*/*.jpg'); ds = ds.map(parse_fn, num_parallel_calls=tf.data.AUTOTUNE); ds = ds.shuffle(1000).repeat().batch(32).cache().prefetch(tf.data.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Load the dataset using `tf.keras.utils.image_dataset_from_directory`, applying `Rescaling(1./255)` for normalization and a data augmentation pipeline (e.g., RandomFlip and RandomRotation). Build a small CNN using the Ke…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Configure dynamic batching in TensorFlow Serving using batching_parameters_file. Choose max_batch_size 64–128 and batch_timeout_millis 50–200 ms; assign 4–8 batch_threads per replica. Validate with bu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical skills in data loading, preprocessing, and building compact CNNs using TensorFlow 2.x. It tests understanding of the Keras Functional API, proper normalization, data augmentation strategies, and end-to-end model training with hold-out evaluation.\n\n## Key Concepts\n- **`image_dataset_from_directory`**: Automatically loads labeled images from subdirectories named by digit (0-9), returning a `tf.data.Dataset`\n- **Grayscale handling**: Set `color_mode='grayscale'` to correctly load 28x28 single-channel images\n- **Train/validation split**: Use `validation_split` and `subset` parameters to create held-out sets\n- **Normalization**: `Rescaling(1./255)` converts pixel values from [0, 255] to [0, 1], improving convergence\n- **Data augmentation**: `RandomFlip` and `RandomRotation` layers expand dataset diversity during training\n- **Keras Functional API**: Allows flexible multi-input/output architectures with shared layers\n- **SparseCategoricalCrossentropy**: Appropriate for integer-encoded labels (0-9) without one-hot encoding\n- **Evaluation**: `model.evaluate()` computes loss and metrics on the test set",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-21",
        "questionId": "q-22310",
        "question": "Scenario: You have a small CSV dataset with columns user_id, event_time (ISO), event_type (A,B,C), and label (0/1). Build a beginner TensorFlow 2.x solution (Keras Functional API) that: 1) uses tf.data to parse CSVs and batch data; 2) derives time features hour_of_day and day_of_week from event_time; 3) one-hot encodes event_type and concatenates with numeric features; 4) trains a small Dense network using BinaryCrossentropy; 5) evaluates on a held-out set. Include a minimal code sketch and rationale?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant signatures in TF Serving, with a short preprocessor sidecar that maps tenant JSON to a fixed tensor order. Maintain a versioned feature schema registry; cache tenant features in Redis k",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use TF Serving batching with a max_batch_size of 64 and batch_delay_micros tuned to keep latency under 20 ms; implement client-side bucketing by input length to minimize padding and pad to fixed size",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed approach: Use tf.data to read CSV lines, map a parser that converts event_time to timestamp, computes hour_of_day and day_of_week, one-hot encodes event_type, and stacks features into a numer",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Configure TensorFlow Serving dynamic_batching with a sane max_batch_size (e.g., 32) and batch_timeout_msec (e.g., 100). Normalize input lengths via padding or bucketing, and use a fixed RNG seed for d",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to build an reproducible end-to-end pipeline: data parsing, feature engineering, and a simple model.\n\n## Key Concepts\n- tf.data CSV parsing, feature engineering from timestamps\n- One-hot encoding, feature concatenation\n- Keras Functional API, binary classification, BinaryCrossentropy\n\n## Code Example\n```javascript\n// Reference code would show: parser, feature derivation, model construction\n```\n\n## Follow-up Questions\n- How would you handle unseen event_type values in inference?\n- How would you add basic unit tests for the data pipeline?",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-22",
        "questionId": "q-29510",
        "question": "You're deploying a TensorFlow 2.x SavedModel behind TF Serving on Kubernetes. The model accepts two inputs: dense_features with shape [None, 128] and sparse_features as a SparseTensor. The system must support per-tenant feature gating and dynamic pruning with zero-downtime upgrades. Describe the end-to-end approach: 1) signature design (dense+sparse inputs, pruning path), 2) per-tenant routing and gate enforcement, 3) preprocessing/postprocessing in the Serving bundle, 4) canary rollout and traffic split strategy, and 5) a minimal test plan with latency, throughput, and feature-availability metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "PTQ is faster to deploy but may incur a small accuracy drop; QAT preserves accuracy but requires retraining. Plan: baseline FP32 SavedModel; PTQ path using representative calibration data to produce a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a dynamic batching loop: collect requests for a 20 ms window or up to 64 items, pad to the longest sequence, and run a single tf.function inference. Tag each input with an id, and reorder outputs",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a tf.data pipeline from CSV paths and labels, with a loader that reads images on the fly, decodes JPEG, converts to grayscale, resizes to 28x28, and scales pixels to [0,1]. Use a tf.lookup.Stati",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a SavedModel with two input paths: dense_features of shape [None,128] and sparse_features as a SparseTensor. Expose signatures 'predict' (dense+sparse) and 'prune_features' (tenant gating).",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\\nTests handling of multi-input serving graphs, sparse inputs, and tenant-specific feature control, all while preserving uptime. It also probes how preprocessing is isolated from model logic and how rollout and observability are planned.\\n\\n## Key Concepts\\n- TensorFlow Serving signatures with multiple inputs\\n- SparseTensor handling in serving graphs\\n- Per-tenant feature gating and config-driven routing\\n- Canary deployments and traffic split\\n- Observability: latency, throughput, feature availability per tenant\\n\\n## Code Example\\n```python\nimport tensorflow as tf\n\n# Define inputs\ndense = tf.keras.Input(shape=(128,), name=\"dense_features\")\nsparse = tf.keras.Input(type_spec=tf.SparseTensorSpec([None, None], tf.float32), name=\"sparse_features\")\n\n@tf.function(input_signature=[\n  tf.TensorSpec([None,128], tf.float32, name=\"dense_features\"),\n  tf.SparseTensorSpec([None, None], tf.float32, name=\"sparse_features\")\n])\ndef serving_predict(dense, sparse):\n    # simple placeholder fusion; real model combines both paths\n    x = dense\n    return {\"outputs\": tf.reduce_mean(x, axis=1, keepdims=True)}\n```\n\n## Follow-up Questions\\n- How would you validate tenant isolation during upgrades?\\n- How would you handle failures in the pruning path without affecting latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "tensorflow-developer-q-23",
        "questionId": "q-11897",
        "question": "You're given a small synthetic time-series CSV with columns t, f1, f2, f3, f4, label (0/1). Build a beginner TensorFlow 2.x model using the Keras Functional API to classify each 10-step window as normal or anomaly. Create a tf.data pipeline that parses CSV, forms sliding windows of length 10, pads if needed, batches, and feeds into a simple LSTM (64 units) followed by a dense output. Show architecture, loss, and a minimal model.fit script?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a tf.data pipeline that reads the CSV via `make_csv_dataset`, applies a `Normalization` layer to features f1–f4, builds sliding windows of length 10 with a 1-step shift, and batches. The model is a Keras Functional…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement with tf.distribute.MultiWorkerMirroredStrategy and a gradient-accumulator. Per-replica batch size b, global batch = b * replicas. Accumulate grads for model.trainable_variables over N steps",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a single SavedModel with a language_id input; route to en/zh/es branches via tf.switch_case, each with its own embedding/encoder/decoder. Share surface tokens but keep per-language weights.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a TensorFlow 2.x workflow: read CSV, create tf.data.Dataset of (path,label); map to read/decode image, resize to 32x32, scale to [0,1], and apply on-the-fly augmentations. Build a small CNN wit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical TensorFlow 2.x skills including tf.data pipelines, sliding windows for time-series data, feature normalization, and implementing a simple LSTM using the Functional API. It's beginner-friendly yet demonstrates handling of real data preprocessing, model architecture, and training workflows.\n\n## Key Concepts\n- tf.data CSV reading and normalization\n- Sliding window creation for sequential data\n- Padding, batching, and tensor shape management\n- Keras Functional API with LSTM and binary classification\n- Model compilation and basic training loop\n\n## Code Example\n```python\n# Complete example: dataset creation and model training\nimport tensorflow as tf\nimport pandas as pd\nimport numpy as np\n\ndef build_dataset(csv_path, window_size=10, batch_size=32):\n    # Create tf.data dataset from CSV\n    dataset = tf.data.experimental.make_csv_dataset(\n        csv_path,\n        batch_size=1,\n        column_names=['t', 'f1', 'f2', 'f3', 'f4', 'label'],\n        header=True,\n        shuffle=False\n    )\n    \n    # Extract features and labels\n    def parse_fn(features):\n        feature_vals = tf.stack([features['f1'], features['f2'], features['f3'], features['f4']], axis=1)\n        labels = features['label']\n        return feature_vals, labels\n    \n    dataset = dataset.map(parse_fn)\n    \n    # Create sliding windows\n    def window_fn(features, labels):\n        # Convert to sequences of length window_size\n        feature_windows = tf.signal.frame(features, window_size, 1, pad_end=True)\n        label_windows = tf.signal.frame(labels, window_size, 1, pad_end=True)\n        # Use the label of the last timestep as the window label\n        window_labels = label_windows[:, -1]\n        return feature_windows, window_labels\n    \n    dataset = dataset.map(window_fn)\n    dataset = dataset.batch(batch_size)\n    return dataset\n\ndef build_model(window_size=10, num_features=4):\n    inputs = tf.keras.Input(shape=(window_size, num_features))\n    x = tf.keras.layers.LSTM(64)(inputs)\n    outputs = tf.keras.layers.Dense(1, activation='sigmoid')(x)\n    \n    model = tf.keras.Model(inputs=inputs, outputs=outputs)\n    model.compile(\n        optimizer='adam',\n        loss='binary_crossentropy',\n        metrics=['accuracy']\n    )\n    return model\n\n# Usage\n# dataset = build_dataset('your_data.csv')\n# model = build_model()\n# model.fit(dataset, epochs=10)\n```",
        "difficulty": "beginner"
      },
      {
        "id": "tensorflow-developer-q-24",
        "questionId": "q-13424",
        "question": "On a 16-GPU Kubernetes cluster using tf.distribute.MultiWorkerMirroredStrategy to train a Transformer-based translator, how would you implement gradient accumulation to simulate a larger global batch? Describe the approach, provide a minimal code snippet for accumulating gradients over N steps, explain learning-rate scaling, and how you would measure and ensure efficient scaling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by reproducing the issue in a staging environment using deterministic inputs and systematic memory profiling. Instrument TF Serving to capture per-model peak GPU memory usage, Python allocation patterns, and batch…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a tf.data pipeline that reads file paths from the directory, decodes PNG images, resizes them to 96×96, normalizes pixel values to [0,1], shuffles the data, batches it, and prefetches for performance. The model ar…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage TF Serving batching with max_batch_size and batch_timeout_micros; surface RaggedTensor inputs by padding inside the graph to a fixed max_len and applying a mask. Fuse preprocessing (normaliza",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement with tf.distribute.MultiWorkerMirroredStrategy and a gradient-accumulator. Per-replica batch size b, global batch = b * replicas. Accumulate grads for model.trainable_variables over N steps",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of distributed TF2 training, gradient accumulation, synchronization across workers, and practical trade-offs for large global batch sizes.\n\n## Key Concepts\n- tf.distribute strategies (MultiWorkerMirroredStrategy)\n- gradient accumulation across steps\n- per-replica vs global batch sizing\n- learning-rate and loss scaling for accumulation\n- monitoring scaling efficiency and convergence\n\n## Code Example\n```python\nimport tensorflow as tf\nstrategy = tf.distribute.MultiWorkerMirroredStrategy()\nper_replica_batch = 4\naccum_steps = 8\n\nwith strategy.scope():\n    model = ...  # Transformer model\n    optimizer = tf.keras.optimizers.Adam(learning_rate=0.001)\n    loss_fn = tf.keras.losses.SparseCategoricalCrossentropy(from_logits=True)\n    accum_grads = [tf.Variable(tf.zeros_like(v), trainable=False) for v in model.trainable_variables]\n    step = tf.Variable(0, trainable=False)\n\n    @tf.function\n    def train_step(batch):\n        x, y = batch\n        with tf.GradientTape() as tape:\n            y_pred = model(x, training=True)\n            loss = loss_fn(y, y_pred)\n        grads = tape.gradient(loss, model.trainable_variables)\n        for a, g in zip(accum_grads, grads):\n            a.assign_add(g)\n        if (step.assign_add(1) % accum_steps) == 0:\n            grads_to_apply = [g / accum_steps for g in accum_grads]\n            optimizer.apply_gradients(zip(grads_to_apply, model.trainable_variables))\n            for a in accum_grads:\n                a.assign(tf.zeros_like(a))\n```\n\n## Follow-up Questions\n- How would you integrate gradient clipping with accumulation?\n- How would you validate linear scalability across all workers?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-01T11:46:35.318Z",
    "version": 1
  }
]