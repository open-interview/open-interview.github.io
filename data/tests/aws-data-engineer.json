[
  {
    "id": "test-aws-data-engineer",
    "channelId": "aws-data-engineer",
    "channelName": "AWS Data Engineer",
    "title": "AWS Data Engineer Knowledge Test",
    "description": "Test your understanding of AWS Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "aws-data-engineer-q-0",
        "questionId": "q-28843",
        "question": "Advanced: You operate a multi-region mobile-app data lake. Each region emits JSON event logs with evolving nested schemas. Design an end-to-end AWS pipeline that ingests, flattens, and stores as Parquet in s3://logs/mobile/YYYY/MM/DD/REGION/, partitions by date and region; registers/upgrades schemas in Glue Data Catalog with evolution; ensures idempotent upserts into a downstream Athena table representing a star schema; masks PII in transit; enforces fine-grained access via Lake Formation; and provides a robust end-to-end test plan including schema drift handling, data quality checks, and rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Advanced pipeline design: Regional JSON event streams are ingested through Kinesis Data Firehose or Kafka MSK, then processed by a Spark-based AWS Glue job that flattens nested JSON structures and writes Parquet files t…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a multi-region ingestion and cataloging pipeline: **Architecture Overview:** 1. **Ingestion Layer:** Configure S3 Event Notifications to trigger AWS Lambda on new JSON object creation in s3://lake/region-a/events…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Organize data by partner under a unified lake structure (s3://lake/partner/YYYY/MM/DD) and implement cross-account governance using Lake Formation. Ingest streaming data via Kinesis Firehose, converting to Parquet forma…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design idea: inbound via Kinesis Data Streams; a per-tenant schema registry (Glue Schema Registry) and a Lambda/Glue ETL stage validates JSON against tenant-specific schemas, masks PII, and writes Par",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates advanced AWS data engineering competency, covering multi-region ingestion, schema evolution, idempotent processing, data governance, and comprehensive testing strategies.\n\n## Key Concepts\n- Regional streaming ingestion with batch reconciliation\n- Flattening nested JSON to Parquet with partitioning by date and region\n- Glue Data Catalog schema evolution and backward compatibility handling\n- Idempotent upserts into downstream Athena tables (star schema)\n- PII masking in transit and Lake Formation-based access control\n- End-to-end testing, drift detection, and rollback plans\n\n## Code Example\n\n```python\n# Schema evolution and idempotent upsert example\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import from_json, col, explode\nfrom pyspark.sql.types import StructType, ArrayType\n\ndef flatten_events(df):\n    return df.select(\n        col(\"event_id\"),\n        col(\"event_time\"),\n        col(\"region\"),\n        col(\"user.id\").alias(\"user_id\"),\n        col(\"user.email\").alias(\"user_email\"),\n        col(\"device.type\").alias(\"device_type\"),\n        col(\"properties\")\n    )\n\ndef merge_to_athena(df, target_table, partition_cols):\n    df.createOrReplaceTempView(\"staging\")\n    merge_query = \"\"\"\n    MERGE INTO {target_table} AS target\n    USING staging AS source\n    ON target.event_id = source.event_id\n    WHEN MATCHED THEN UPDATE SET *\n    WHEN NOT MATCHED THEN INSERT *\n    \"\"\"\n    return spark.sql(merge_query)\n\n# Data quality check\ndef validate_data(df):\n    from pyspark.sql.functions import count, when, col\n    return df.select(\n        count(when(col(\"event_id\").isNull(), True)).alias(\"null_event_id\"),\n        count(when(col(\"region\").isNull(), True)).alias(\"null_region\")\n    )\n```\n\n## Rollback Strategy\n1. **S3 Versioning**: Enable versioning on source buckets to preserve historical data\n2. **Glue Job Bookmarks**: Use bookmarks to reprocess failed batches idempotently\n3. **Schema Versioning**: Store schema versions in Schema Registry; rollback to compatible version\n4. **Blue/Green Deployments**: Deploy new job versions alongside existing ones; switch traffic after validation\n5. **Automated Rollback Triggers**: CloudWatch alarms on data quality metrics trigger Lambda-initiated rollbacks",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-1",
        "questionId": "q-1521",
        "question": "You manage a global, multi-tenant data platform where streaming events bucket into S3 and downstream engines like Redshift and Athena rely on per-tenant schemas. Design a resilient end-to-end architecture that versions datasets, enforces per-tenant schema contracts, detects drift, quarantines bad records, and supports rollback without downtime. Specify services, data formats, governance, and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement SCD Type 2 on a customer_dim in S3 using a Glue Spark job. Consume CDC JSON from DMS, stage to a CDC bucket, compare with the latest Parquet row, and insert a new version with a new surrogat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an AWS Glue Studio Spark job to read s3://iot-raw/branch/date/*.json, flatten nested metrics, cast types, and write to Parquet at s3://iot-data/branch/date/, partitioned by date and device_id. Ena",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose using Firehose/Kinesis for streaming into S3 with per-tenant prefixes; Glue Schema Registry to enforce per-tenant contracts; Iceberg on S3 for versioned tables; Lake Formation for tenant isola",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design an incremental pipeline that ingests daily JSON.gz files from s3://ride-events/raw/date/*.json.gz, converts them to Parquet, and writes to an Iceberg table on S3 partitioned by date, registered in the Glue Data C…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-tenant data governance, schema contracts, drift detection, and rollback—critical for scalable data products.\n\n## Key Concepts\n- Multi-tenant governance with Glue Schema Registry and Lake Formation.\n- Versioned storage with Iceberg on S3.\n- Drift detection and quality checks via Glue jobs and Deequ.\n- Quarantine workflows and rollback with Step Functions.\n- Observability via CloudWatch and Athena-based lineage.\n\n## Code Example\n```javascript\n// Placeholder for a data quality check wiring example\n```\n\n## Follow-up Questions\n- How would you handle schema evolution while preserving historic queries?\n- What metrics and alerts signal a drift or ingestion failure?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-2",
        "questionId": "q-9453",
        "question": "Ingest streaming JSON telemetry from 1k devices into S3 as daily-partitioned Parquet with a Glue Data Catalog. New fields appear irregularly; design an upsertable lake layout using Apache Hudi or Apache Iceberg on AWS (Glue/EMR), covering partitioning, schema evolution, compaction, and data quality checks. Include testing plan and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a streaming-to-batch pipeline using Glue Streaming ETL to process Kinesis JSON events into date-partitioned Parquet files in S3, with Glue Schema Registry handling evolving schemas; maintain the users dimensio…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an upsert-enabled lake table (Apache Hudi or Iceberg) on S3 with daily partitions and a Glue Data Catalog. Ingest into a staging layer, apply upserts keyed by device_id, preserve schema evolution,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a streaming data lake ingestion pipeline using AWS services. Providers emit JSON events to Kinesis Data Streams, which serves as the buffering layer. Kinesis Data Firehose reads from the stream and writes Parquet…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Spark jobs (AWS Glue or Kinesis Data Analytics) to ingest streaming events and write to an Apache Hudi table at s3://lake/curated/tenant/date, while raw data lands at s3://lake/raw/tenant/date. Partition the table b…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design upsertable data lakes on AWS, choosing between Hudi/Iceberg, handling schema drift, and balancing cost with performance.\n\n## Key Concepts\n\n- Upserts on S3 with Parquet\n- Apache Hudi / Apache Iceberg\n- Glue Data Catalog integration\n- Partitioning strategies and compaction\n- Schema evolution and data quality checks\n\n## Code Example\n\n```python\n# PySpark example writing a Hudi upsert table\nfrom pyspark.sql import SparkSession\n\n# df = spark.read.json(\"s3://bucket/staging/**/*.json\")\ndf = spark.read.json('s3://bucket/staging/**/*.json')\ndf.write.format('hudi').options(\n  {\n    'hoodie.datasource.write.recordkey.field': 'device_id',\n    'hoodie.datasource.write.table_name': 'device_telemetry',\n    'hoodie.datasource.write.operation': 'upsert',\n    'hoodie.datasource.write.precombine.field': 'last_seen',\n  }\n).mode('append').save('s3://bucket/warehouse/device_telemetry')\n```\n\n## Follow-up Questions\n\n- How would you handle late-arriving data or reprocess failed upserts?\n- Which metrics would you monitor to optimize read performance and cost?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-3",
        "questionId": "q-7567",
        "question": "Design a beginner-friendly ingestion pipeline for a daily CSV feed arriving to s3://data-lake/raw/activities/YYYY/MM/DD/activities.csv. The schema includes user_id (string), event_type (string), event_ts (timestamp), metadata (string, nullable). Convert to Parquet, partition by date, and publish in s3://data-lake/warehouse/activities/YYYY/MM/DD/ and register in Glue Data Catalog. Include schema evolution plan, basic data quality checks, and a minimal end-to-end test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an AWS Glue Spark job to read the daily CSV with a defined schema (user_id string, event_type string, event_ts timestamp, metadata string nullable), write Parquet to a date-partitioned path, and u",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Apache Iceberg on S3 with Glue Data Catalog. Ingest JSON via Firehose/Spark, flatten, then upsert into an Iceberg table partitioned by tenant_id and date. Leverage Iceberg schema evolution for cha",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Streams for ingestion; land raw events in S3 as Parquet with daily partitions and Glue catalog. Run Apache Hudi on Glue Spark jobs to upsert into target Parquet, enabling dedup and la",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an AWS Glue Spark job to ingest NDJSON from s3://lab-raw/abtest/YYYY/MM/DD/, explode the users array, map fields to a unified schema, deduplicate by (user_id, event_id) for the day, and write Parquet files to s3://l…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical, beginner-friendly data ingestion with Glue, including partitioned Parquet output, catalog updates, and basic quality checks.\n\n## Key Concepts\n\n- AWS Glue Spark jobs\n- Parquet partitioning by date\n- Glue Data Catalog\n- Schema evolution (nullable columns)\n- Data quality checks (not-null, timestamp validity)\n\n## Code Example\n\n```javascript\n// Data quality check example (JS-like pseudocode for illustration)\nfunction validRecord(rec){\n  if (!rec.user_id || typeof rec.user_id !== 'string') return false;\n  if (!rec.event_ts || isNaN(Date.parse(rec.event_ts))) return false;\n  return true;\n}\n```\n\n## Follow-up Questions\n\n- How would you handle late-arriving files or reprocessing failed files without duplicating data?\n- How would you adapt the pipeline for multi-tenant inputs with evolving schemas?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-4",
        "questionId": "q-9674",
        "question": "A daily CSV log lands at s3://app-logs/raw/{YYYY}/{MM}/{DD}/app_logs.csv. The file has columns: user_id, event_time (UTC, may be missing), event_type, metadata (JSON string with nested keys). Design a beginner-level ingestion pipeline using AWS Glue to parse CSV, flatten metadata into columns, store Parquet in s3://app-logs/curated/{YYYY}/{MM}/{DD}/, and register in Glue Catalog. Explain how to handle missing event_time, implement schema evolution for new metadata keys, basic data quality checks, and an end-to-end test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Glue Spark job to read the CSV from raw, parse metadata JSON into flatten columns, write Parquet to curated/{YYYY}/{MM}/{DD} partitioned by date, and update Glue Catalog. If event_time is missin",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Land streaming JSON events to s3/raw/{tenant_id}/{YYYY}/{MM}/{DD}. Use a Glue Streaming ETL job to read from Kinesis or Kafka, leveraging Glue Schema Registry to handle schema evolution and capture schema drift. Write P…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Iceberg tables on S3 with a Glue catalog, landing JSON from Kinesis via a streaming job that MERGEs into Iceberg to support upserts/deletes; replicate CDC from multiple sources using DMS across regions with sch…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage Iceberg on S3 with Glue Data Catalog for ACID upserts and schema evolution. Ingest via Kinesis Data Streams into a Spark job (Glue) writing Parquet to s3://lake/events/date/, partitioned dail",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical ingestion with CSV input containing a JSON field, focusing on Flattening, schema evolution, and data quality in Glue.\n\n## Key Concepts\n- AWS Glue Spark ETL\n- CSV parsing and JSON flattening\n- Glue schema evolution handling\n- Data quality checks: nulls, types, duplicates\n- Date partitioning in S3 and Catalog\n\n## Code Example\n```javascript\n// Pseudo Glue Spark ETL steps\nimport com.amazonaws.services.glue.GlueContext\nimport org.apache.spark.sql.functions._\nval df = spark.read.option(\"header\", true).csv(\"s3://app-logs/raw/.../app_logs.csv\")\nval parsed = df.withColumn(\"metadata\", from_json(col(\"metadata\"), schema_of_json(col(\"metadata\"))))\n// flatten further columns from metadata here\nval out = parsed.drop(\"metadata\")\nout.write.mode(\"overwrite\").partitionBy(\"date\").parquet(\"s3://app-logs/curated/.../\")\n```\n\n## Follow-up Questions\n- How would you test schema evolution when a new metadata key appears?\n- How would you monitor for missing daily files and failed runs?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-5",
        "questionId": "q-21284",
        "question": "Design an intermediate-level, multi-source ingestion to a single analytics table in S3 using upserts. Data sources: mobile app events in JSON via Kinesis, web events in Parquet via Kinesis Firehose to S3, and daily CSV sales logs on S3. Explain how you would implement incremental upserts with Apache Hudi (or Iceberg) on Glue Spark, including idempotence, late-arriving data handling, schema evolution, daily partitioning, and access via Glue Catalog/Athena. Include testing and monitoring plan and trade-offs between Hudi and Iceberg?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Lake Formation with per-tenant databases and table permissions, and use IAM roles for cross-account access. Land data as Parquet in S3 with daily partitions, register in the Glue Data Catalog",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Apache Iceberg on S3 with Glue Data Catalog. Ingest JSON via Firehose/Spark, flatten, then upsert into an Iceberg table partitioned by tenant_id and date. Leverage Iceberg schema evolution for cha",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a multi-source ingestion pipeline to a single analytics table in S3 using upserts. Data sources include: mobile app events in JSON format via Kinesis, web events in Parquet format via Kinesis Firehose to S3, and…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use AWS Step Functions to orchestrate the workflow with retries and a manifest-based checkpoint",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates the ability to design an end-to-end data ingestion pipeline with upsert capabilities, handle late-arriving data, and manage schema evolution across multiple sources—skills essential for building production-grade data platforms at scale.\n\n## Key Concepts\n\n- Upsertable data lake formats using Hudi or Iceberg\n- AWS Glue Spark jobs and Data Catalog integration\n- Idempotent deduplication and late-arriving data handling\n- Partitioning strategy and cross-source schema normalization\n- Testing and monitoring for data quality and pipeline reliability\n\n## Code Example\n\n```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, when, to_timestamp\n\nspark = SparkSession.builder(\n    .appName(\"MultiSourceIngestion\")\n    .config(\"spark.serializer\", \"org.apache.spark.serializer.KryoSerializer\")\n    .config(\"spark.sql.extensions\", \"org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions\")\n    .getOrCreate()\n\n# Define the target table path\ntable_path = \"s3://analytics-bucket/warehouse/analytics_table\"\n\n# Read mobile events from Kinesis\nmobile_df = spark.readStream \\\n    .format(\"kinesis\") \\\n    .option(\"streamName\", \"mobile-events-stream\") \\\n    .option(\"initialPosition\", \"LATEST\") \\\n    .load()\n\n# Parse JSON and normalize schema\nmobile_parsed = mobile_df.select(col(\"data.*\")).withColumn(\"event_timestamp\", to_timestamp(col(\"timestamp\")))\n\n# Read web events from S3 (Parquet)\nweb_df = spark.read.parquet(\"s3://raw-bucket/web-events/\")\n\n# Read sales logs from S3 (CSV)\nsales_df = spark.read.csv(\"s3://raw-bucket/sales-logs/\", header=True)\n\n# Normalize schemas across all sources\ndef normalize_schema(df, source_name):\n    return df.select(\n        col(\"event_id\").alias(\"record_id\"),\n        col(\"event_timestamp\"),\n        col(\"event_type\"),\n        col(\"user_id\"),\n        col(\"properties\"),\n        col(\"event_timestamp\").cast(\"date\").alias(\"partition_date\"),\n        col(\"event_timestamp\").cast(\"timestamp\").alias(\"ingestion_timestamp\"),\n        col(\"source\")\n    )\n\nmobile_normalized = normalize_schema(mobile_parsed, \"mobile\").withColumn(\"source\", lit(\"mobile\"))\nweb_normalized = normalize_schema(web_df, \"web\").withColumn(\"source\", lit(\"web\"))\nsales_normalized = normalize_schema(sales_df, \"sales\").withColumn(\"source\", lit(\"sales\"))\n\n# Union all sources\nunified_df = mobile_normalized.unionByName(web_normalized, sales_normalized)\n\n# Write to Iceberg table with upsert using MERGE INTO\nunified_df.writeTo(table_path).partitionedBy(\"partition_date\").createOrReplace()\n\n# For incremental upserts with deduplication\nspark.sql(f\"\"\"\n    MERGE INTO {table_path} target\n    USING (\n        SELECT record_id, MAX(event_timestamp) as event_timestamp, MAX(event_type) as event_type,\n               MAX(user_id) as user_id, MAX(properties) as properties,\n               MAX(partition_date) as partition_date,\n               MAX(ingestion_timestamp) as ingestion_timestamp, MAX(source) as source\n        FROM source_table\n        GROUP BY record_id\n    ) source\n    ON target.record_id = source.record_id\n    WHEN MATCHED AND source.ingestion_timestamp > target.ingestion_timestamp THEN\n        UPDATE SET *\n    WHEN NOT MATCHED THEN\n        INSERT *\n\"\"\")\n```\n\n## Schema Evolution Handling\n\n```python\n# Handle schema evolution with Iceberg\nfrom pyspark.sql.types import StructType, StructField, StringType, IntegerType, TimestampType\n\n# Define evolved schema with new fields\nnew_schema = StructType([\n    StructField(\"record_id\", StringType(), False),\n    StructField(\"event_timestamp\", TimestampType(), True),\n    StructField(\"event_type\", StringType(), True),\n    StructField(\"user_id\", StringType(), True),\n    StructField(\"properties\", StringType(), True),\n    StructField(\"partition_date\", StringType(), True),\n    StructField(\"ingestion_timestamp\", TimestampType(), True),\n    StructField(\"source\", StringType(), True),\n    StructField(\"new_field\", StringType(), True)  # New field added\n])\n\n# Iceberg handles schema evolution automatically:\n# - ADD: New columns are added seamlessly\n# - DROP: Columns can be removed (data still readable)\n# - RENAME: Columns can be renamed\n# - UPDATE: Type promotions (int → long)\n```\n\n## Late-Arriving Data Handling\n\n```python\n# Configure late-data handling with watermarking\nfrom pyspark.sql.functions import window\n\n# Set watermark for late data (e.g., 7 days)\nwatermarked_df = unified_df \\\n    .withWatermark(\"event_timestamp\", \"7 days\") \\\n    .groupBy(window(\"event_timestamp\", \"1 hour\"), \"record_id\") \\\n    .agg(max(\"*\").alias(\"latest_record\"))\n\n# Upsert with late-data handling\nspark.sql(f\"\"\"\n    MERGE INTO {table_path} target\n    USING (\n        SELECT * FROM (\n            SELECT *, ROW_NUMBER() OVER (PARTITION BY record_id ORDER BY event_timestamp DESC) as rn\n            FROM source_with_watermark\n        ) WHERE rn = 1\n    ) source\n    ON target.record_id = source.record_id\n       AND target.event_timestamp >= source.event_timestamp - INTERVAL '7' DAYS\n    WHEN MATCHED AND source.event_timestamp > target.event_timestamp THEN\n        UPDATE SET *\n    WHEN NOT MATCHED THEN\n        INSERT *\n\"\"\")\n```\n\n## Testing Plan\n\n1. **Unit Tests**: Test schema normalization functions, deduplication logic\n2. **Integration Tests**: Verify end-to-end pipeline with mock data sources\n3. **Data Quality Tests**: Validate record counts, null checks, uniqueness constraints\n4. **Performance Tests**: Measure throughput and latency under load\n\n## Monitoring Plan\n\n- **CloudWatch Metrics**: Pipeline latency, record counts, error rates\n- **Data Quality Checks**: Deequ or Great Expectations for schema and value validation\n- **Table Metrics**: Iceberg provides built-in metadata tables for file sizes, commit history\n- **Alerting**: SNS notifications on pipeline failures or data quality issues\n\n## Hudi vs Iceberg Trade-offs\n\n| Aspect | Hudi | Iceberg |\n|--------|------|---------|\n| Schema Evolution | Limited (add only) | Full support (add, drop, rename, update) |\n| Time Travel | Via versioning | Native and efficient |\n| AWS Integration | Native (Glue, Athena) | Native (Glue, Athena) |\n| Performance | Optimized for upserts | Optimized for analytics |\n| Community | Active | Very active |\n| Hidden Partitioning | No | Yes |\n| Partition Evolution | Limited | Yes |",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-6",
        "questionId": "q-13802",
        "question": "Scenario: Two data sources emit daily JSON events to S3: sourceA at s3://lake/streamA/YYYY/MM/DD/events.json and sourceB at s3://lake/streamB/YYYY/MM/DD/events.json. Build a beginner-level ingestion using AWS Glue to land data as Parquet in s3://lake/warehouse/YYYY/MM/DD/source={A,B}/, partitioned by date and source, with Glue Data Catalog registration. Enforce idempotent writes, basic schema normalization for differing field sets, and a test plan that validates cross-source dedup and missing-field handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition by partner_id and date; store under s3://data-lake/partner_id/date/. Use a single Glue Data Catalog with per-partner databases and Lake Formation grants so each partner can only query its da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: Use a Glue Spark job to read both input paths, unify to a common schema, fill missing fields with nulls, and write Parquet to s3://lake/warehouse/YYYY/MM/DD/source=A or source=B/, p",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage AWS Glue Spark jobs with Apache Hudi to upsert JSON events from three SaaS vendors into a single Parquet dataset in S3, partitioned by date and vendor. Use Glue Data Catalog and Lake Formatio",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an AWS Glue Spark job to read events.jsonl from the raw bucket, enforce a schema (user_id, event_time, event_type), perform data quality checks, write partitioned Parquet to the processed path, and register the sche…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes multi-source ingestion, partitioning by multiple keys, and basic schema normalization. It also tests practical idempotency and end-to-end testing in a beginner context.\n\n## Key Concepts\n\n- Multi-source Glue ETL with unified schema\n- Parquet format and multi-key partitioning (date, source)\n- Job bookmarks for idempotent writes\n- Glue Data Catalog registration and lightweight data quality tests\n\n## Code Example\n\n```javascript\n// Pseudo Glue ETL skeleton (JS-style for illustration)\n// 1) read from two sources\nlet dfA = readJson('s3://lake/streamA/xxxx/yyyy/MM/dd/events.json');\nlet dfB = readJson('s3://lake/streamB/xxxx/yyyy/MM/dd/events.json');\n// 2) normalize schemas to common schema\nlet unified = dfA.unionByName(dfB, true);\n// 3) write as Parquet with partitions\nunified.write.parquet('s3://lake/warehouse/yyyy/mm/dd/source={A,B}', {partitionBy: ['date','source']});\n```\n\n## Follow-up Questions\n\n- How would you extend this to handle schema evolution across sources?\n- What monitoring would you add to detect failed runs or duplicates in near real time?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-7",
        "questionId": "q-14284",
        "question": "Advanced: Real-time clickstream events arrive as JSON with evolving fields in Kinesis Data Streams. Design an end-to-end ingestion that lands as Parquet in S3 (partitioned by date), registers in Glue Data Catalog, and uses Apache Iceberg on S3 for upserts to a users_dim with SCD2 history. Include schema evolution, idempotency, privacy in transit via KMS, and end-to-end testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a Glue Spark job that reads hourly JSON, flattens nested fields, and writes Parquet to a time-partitioned path. Deduplicate by event_id per hour, ensuring idempotence even if re-run. Use Step",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest JSON events regionally via Kinesis Data Streams, fan out to S3 Parquet with hourly partitions using Apache Hudi for upserts and schema evolution. Use Glue Schema Registry + Glue Data Catalog; q",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a SageMaker Feature Store pipeline with offline/online stores; ingest raw data from Kinesis and S3 into feature groups; define tenant-scoped access via Lake Formation; implement schema evolutio",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a streaming-to-batch pipeline using Glue Streaming ETL to process Kinesis JSON events into date-partitioned Parquet files in S3, with Glue Schema Registry handling evolving schemas; maintain the users dimensio…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests expertise in building enterprise-grade streaming data lakes with schema evolution, slowly changing dimensions, and comprehensive governance requirements at scale.\n\n## Key Concepts\n\n- Streaming ingestion, schema evolution, Iceberg upserts\n- Glue Data Catalog, Schema Registry, Lake Formation, IAM/KMS\n- Parquet partitioning, idempotent processing, end-to-end testing\n- Data governance and privacy\n\n## Code Example\n\n```javascript\n// Pseudo-ETL outline for Spark/Glue\n```\n\n## Follow-up Questions\n\n- How would you handle late-arriving data and schema drift?\n- What strategies would you implement for cost optimization?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-8",
        "questionId": "q-6795",
        "question": "Design an AWS-native data ingestion and processing pipeline for a multi-tenant SaaS that streams petabytes of telemetry from per-tenant devices to a centralized data lake on S3. The catch: you must support upserts and deletes, low-latency queries for dashboards, per-tenant data residency, and cost controls. Propose a data layout using Apache Hudi or Iceberg on S3, with Glue Catalog integration, Athena/Redshift Spectrum for querying, and a schema evolution strategy, along with testing, monitoring, and failure handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a pipeline that uses Glue Schema Registry for Avro evolution, a Spark-based Glue ETL to read from Kinesis Data Streams, and writes daily-partitioned Parquet to S3 with a Glue Data Catalog. Hand",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Apache Hudi on S3 to enable upserts and deletes with per-tenant partitions. Ingest telemetry via streaming into S3, process with Spark to write to Hudi tables, and run periodic compaction. Glue Da",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use AWS Step Functions to orchestrate the workflow with retries and a manifest-based checkpoint",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-level AWS data pipeline that ingests a daily JSON events file from s3://org-raw/events/YYYY/MM/DD/events.json, validates against a fixed required schema, and performs simple quality checks (non-null id…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a scalable, cost-aware data lake with upserts/deletes, multi-tenancy, and end-to-end testing in an AWS-heavy environment.\n\n## Key Concepts\n- Upserts/deletes on S3 using Apache Hudi or Iceberg\n- Per-tenant partitioning and residency controls\n- Glue Data Catalog integration; Athena/Redshift Spectrum for queries\n- Schema evolution strategy and data quality/testing\n\n## Code Example\n```scala\n// Spark Structured Streaming write to Hudi table\nimport org.apache.spark.sql.functions._\nval df = spark.readStream.format(\"json\").load(\"s3://bucket/raw/telemetry/\")\ndf.writeStream\n  .format(\"org.apache.hudi\")\n  .option(\"hoodie.datasource.write.storage.type\",\"MERGE_ON_READ\")\n  .option(\"hoodie.datasource.write.recordkey\",\"tenant_id,device_id,timestamp\")\n  .option(\"hoodie.datasource.write.partitionpath.field\",\"tenant_id\")\n  .option(\"hoodie.datasource.write.table_name\",\"telemetry\")\n  .option(\"hoodie.upsert.shuffle.parallelism\",\"200\")\n  .start(\"s3://bucket/warehouse/telemetry\")\n```\n\n## Follow-up Questions\n- How would you validate upsert/delete correctness with late-arriving events?\n- What strategies ensure tenant data residency during regional migrations?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-9",
        "questionId": "q-1981",
        "question": "Design a data lake ingestion pipeline on S3 for semi-structured SaaS data that must support upserts and deletes with time-travel queries; choose between Apache Iceberg, Hudi, or Delta Lake on AWS (Glue, EMR, Athena) and justify: schema evolution, compaction, CDC handling, partitioning, and data quality checks; include testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-source ingest using Kinesis Data Streams (or MSK) to collect JSON from each platform, de-duplicate by (platform,event_id), and land into S3 as Parquet with daily partitions. Use Glue Sc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Apache Iceberg on S3 (Parquet) with a Glue Data Catalog. Implement upserts/deletes via MERGE, with tombstones for deletes and partition pruning for cost. Ingest via CDC streams from SaaS sources,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Analytics to enrich JSON streams, then write Parquet to S3 with daily partitions and register in Glue Catalog. Leverage Glue Schema Registry for evolution; implement idempotent upsert",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a staging S3 bucket to receive CSV via SFTP. Use a Glue workflow with a bookmark-enabled Spark job to process only new files, output Parquet to daily partitions, and register a Glue Data Cata",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe candidate must reason about ACID on object storage and multi-source ingestion.\n\n## Key Concepts\n- Iceberg vs. alternatives for upserts\n- CDC, dedupe, tombstones\n- Schema evolution and time travel\n- Partitioning and compaction costs\n- Validation via Athena/QA dashboards\n\n## Code Example\n```javascript\n// placeholder example of MERGE operation with Iceberg\n```\n\n## Follow-up Questions\n- How would you instrument data quality checks and alerting?\n- How would you test schema evolution across deployments?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-10",
        "questionId": "q-30037",
        "question": "Design a data quality observability layer for a streaming data lake: JSON events land to S3 in daily partitions via Kinesis Firehose. Build a Glue ETL that computes per-batch metrics (nulls, inferred types, pattern checks), writes results to a separate Parquet dataset, and alerts via SNS on breaches. Ensure idempotent processing, Glue Data Catalog integration, and a testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a pipeline that uses Glue Schema Registry for Avro evolution, a Spark-based Glue ETL to read from Kinesis Data Streams, and writes daily-partitioned Parquet to S3 with a Glue Data Catalog. Hand",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Lake Formation with per-tenant databases and table permissions, and use IAM roles for cross-account access. Land data as Parquet in S3 with daily partitions, register in the Glue Data Catalog",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Pipeline design: ingest per-tenant JSON via Kinesis Data Streams; a Glue Streaming Spark job flattens, masks PII (hash emails, redact SSN) with a KMS key, and writes Parquet partitions by tenant/date",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Glue Spark job with job bookmarks to process each daily partition exactly once. Compute per-batch metrics: null counts, inferred types, and regex pattern checks for key fields. Write metrics to a separate Pa…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical data quality observability in a streaming data lake environment, testing knowledge of idempotency, catalog integration, and alerting under real-world constraints.\n\n## Key Concepts\n\n- Glue Job bookmarks for idempotent partition processing\n- Data quality checks: null counts, type validation, regex pattern validation\n- Separate Parquet metrics dataset and Glue Data Catalog integration\n- CloudWatch alarms and SNS for real-time alerting\n- End-to-end testing with synthetic data to validate metrics computation and alert triggers\n\n## Code Example\n\n```python\n# PySpark: compute quality metrics per batch\nfrom pyspark.sql.functions import col, count, when, lit\n\ndef compute_quality_metrics(df, schema):\n    metrics = {}\n    for field in schema.fields:\n        col_name = field.name\n        total = df.count()\n        null_count = df.filter(col(col_name).isNull()).count()\n        metrics[col_name] = {\n            'null_count': null_count,\n            'null_pct': null_count / total if total > 0 else 0,\n            'inferred_type': str(field.dataType),\n            'pattern_match_pct': compute_pattern_matches(df, col_name)\n        }\n    return metrics\n\n# Idempotent Glue job with bookmarks\njob = GlueContext().create_job(\n    'DataQualityJob',\n    script_location='s3://scripts/quality_job.py'\n)\njob.enable_job_bookmarks()\njob.enable_auto_categorization()\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-11",
        "questionId": "q-28154",
        "question": "Design a beginner-level AWS data pipeline for ingesting two sources (CSV and JSON) of sensor readings arriving daily under s3://data-lake/raw/sensors/csv/YYYY/MM/DD/sensors.csv and s3://data-lake/raw/sensors/json/YYYY/MM/DD/*.json. Use AWS Glue to unify into Parquet stored at s3://data-lake/warehouse/sensors/YYYY/MM/DD/, partitioned by date. Ensure idempotent processing with Glue Job Bookmarks and include a daily lineage manifest (manifest.json) recording source names, record counts, and a simple schema hash. Describe tests end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via KDS and a Lambda/Glue processor that applies column-level masking and tokenization (e.g., redact emails, tokenize 16-digit IDs) before writing to an Iceberg table on S3. Use Glue Data Catal",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage AWS Glue Spark jobs with Apache Hudi to upsert JSON events from three SaaS vendors into a single Parquet dataset in S3, partitioned by date and vendor. Use Glue Data Catalog and Lake Formatio",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Land JSON to S3, validate against Glue Schema Registry, deduplicate by event_id, and materialize Parquet per day. Expose features to SageMaker Feature Store; catalog schemas in Glue Data Catalog; enab",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single AWS Glue Spark job to read both inputs, infer a unified schema, cast types, and deduplicate by (sensor_id, ts) per day. Enable Job Bookmarks for idempotence, write Parquet partitions to s",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests handling multi-format ingestion, schema drift, idempotent processing, and lineage with beginner-appropriate Glue tooling.\n\n## Key Concepts\n- AWS Glue Spark jobs\n- Job Bookmarks\n- Schema unification across CSV/JSON\n- Parquet partitioning by date\n- Data lineage manifest with schema hash\n- End-to-end test strategy\n\n## Code Example\n```python\n# Pseudocode: unify, deduplicate, and write manifest (illustrative)\ninput_df = spark.read.format(\"csv\").option(\"header\", True).load(csv_path) \\\n  .union(spark.read.format(\"json\").load(json_path))\nnormalized = input_df.selectExpr(\"cast(device_id as string) as device_id\",\n                               \"cast(ts as timestamp) as ts\",\n                               \"cast(temp as double) as temp\",\n                               \"cast(humidity as int) as humidity\")\ndeduped = normalized.dropDuplicates([\"device_id\",\"ts\"])\nout_df = deduped.repartition(4)\nout_df.write.mode(\"append\").partitionBy(\"date\").parquet(out_path)\n\n# manifest\nschema_hash = hashlib.md5(str([(f.name, str(f.dataType)) for f in out_df.schema]).encode()).hexdigest()\nmanifest = {\"date\": date_str, \"source_count\": deduped.count(), \"schema_hash\": schema_hash}\ns3.put_object(Bucket=\"data-lake\", Key=f\"warehouse/sensors/{date_str}/manifest.json\", Body=json.dumps(manifest))\n```\n\n## Follow-up Questions\n- How would you extend to additional sources with minimal schema drift?\n- How would you monitor lineage quality and alert on mismatches?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-12",
        "questionId": "q-3912",
        "question": "Land daily CSVs via AWS Transfer Family to s3://reports/raw/YYYY/MM/DD/report.csv.gz. Glue Spark job reads, enforces types, writes Parquet to s3://reports/curated/YYYY/MM/DD/report.parquet partitioned by date. Use Glue Crawler for catalog; handle schema evolution for a new 'region' column. Data quality: row count >0 and non-null keys. End-to-end test with sample file and a quick Athena query?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Glue Spark ETL job that MERGEs incoming events into Iceberg on S3 using event_id as the primary key, enabling idempotent upserts and daily partitions. Enable Iceberg schema evolution, and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Land daily CSVs via AWS Transfer Family to S3 raw, then a Glue Spark job to cast types, write Parquet to curated with date partitions. Crawlers update the Data Catalog; enabled schema evolution to acc",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Set up an hourly ETL: decrypt ORC, normalize to Parquet, and write to S3 with hourly partitions. Track lastProcessedHour in DynamoDB; emit tombstones for deletes to propagate removals. Use Glue for ca",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a streaming+batch pipeline: raw JSON per tenant lands in S3; a Glue Spark job evolves the schema to Parquet (Iceberg) and partitions by tenant/date; apply PII masking in ETL; register in Glu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical ingestion from an SFTP-like source using AWS Transfer Family, plus ETL, partitioned Parquet, and catalog management with schema evolution. It also touches basic data quality and end-to-end testing.\n\n## Key Concepts\n- AWS Transfer Family for secure file landing\n- AWS Glue ETL (Spark) for transformation\n- Parquet with partitioning by date\n- Glue Data Catalog and schema evolution\n- Data quality checks and end-to-end testing\n\n## Code Example\n```python\nimport sys\nfrom awsglue.context import GlueContext\nfrom pyspark.context import SparkContext\nsc = SparkContext()\nglueContext = GlueContext(sc)\nspark = glueContext.spark_session\n\n# Read CSV from raw location\ndf = spark.read.option(\"header\", \"true\").csv(\"s3://reports/raw/YYYY/MM/DD/report.csv.gz\")\n# Simple type casting could be added here\n\n# Write to curated with date partitions\ndf.write.partitionBy(\"year\", \"month\", \"day\").parquet(\"s3://reports/curated/YYYY/MM/DD/report.parquet\")\n```\n\n## Follow-up Questions\n- How would you validate idempotency and handle re-ingestion of the same file?\n- What failure modes exist with Transfer Family and how would you monitor them?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-13",
        "questionId": "q-2875",
        "question": "In a regulated financial environment, design an end-to-end AWS data pipeline that ingests daily batch CSV exports and real-time credit-event streams into a centralized data lake. Explain how you would land data in S3 with partitions, implement idempotent upserts and schema evolution, enforce governance via Lake Formation and the Glue Data Catalog, implement data quality checks and tests, and observe and rollback on failures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-region ingestion and cataloging pipeline: **Architecture Overview:** 1. **Ingestion Layer:** Configure S3 Event Notifications to trigger AWS Lambda on new JSON object creation in s3://lake/region-a/events…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Land daily CSVs via AWS Transfer Family to S3 raw, then a Glue Spark job to cast types, write Parquet to curated with date partitions. Crawlers update the Data Catalog; enabled schema evolution to acc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Hybrid ingestion design: batch feeds land CSVs into S3 under date partitions; a real-time stream of events arrives on Kinesis Data Streams and is written to S3 as JSON via Firehose. Glue Spark jobs pe",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Glue Schema Registry with per-stream Avro/JSON schemas, versioned and backward/forward compatible. Producers send schemaId in headers; a validation step enforces the contract at ingest. Write to S",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes real-world data ingestion, transformation, governance, and rollback strategies under regulatory constraints.\n\n## Key Concepts\n- Batch + streaming ingestion patterns\n- Idempotent upserts and schema evolution\n- Lake Formation governance and Glue Catalog\n- Data quality checks and testing strategies\n- Observability and rollback plans\n\n## Code Example\n```javascript\n// Pseudo Glue job outline for Spark\nimport sys\nfrom awsglue.context import GlueContext\nfrom pyspark.context import SparkContext\n\nsc = SparkContext()\ngc = GlueContext(sc)\ndf = gc.create_dynamic_frame.from_options(\"s3\", {\"path\": \"s3://bucket/path\"}, \"csv\")\ndf = df.drop_fields([\"unneeded\"])\ndf = df.resolveChoice(\"cast\", {\"column\": \"int\"})\n# upsert logic would be implemented here using a sink with a manifest\n```\n\n## Follow-up Questions\n- How would you surface schema evolution without breaking downstream dashboards?\n- What tests validate idempotence and rollback behavior?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-14",
        "questionId": "q-27829",
        "question": "Scenario: A global mobile app writes events to s3://production-data/events/YYYY/MM/DD as Parquet. Near-real-time dashboards must read from us-west-2 while batch analytics run in us-east-1. Design a cross-region pipeline that streams incremental changes to us-west-2, keeps Glue Catalog in sync with drift detection, enforces per-tenant RBAC via Lake Formation, and guarantees idempotent processing for late-arriving data. Include a test plan and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a streaming-to-batch pattern: IoT devices feed Kinesis Data Firehose to S3 with daily partitioned Parquet. Glue Crawlers evolve the catalog; ETL converts JSON to Parquet by date, maintains schema",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use AWS Step Functions to orchestrate the workflow with retries and a manifest-based checkpoint",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use DMS for initial load and CDC to S3 Parquet, partitioned by date; trigger a Glue cataloging job to update the Data Catalog. Ensure idempotency by deduplicating on event_id and persisting the max LS",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Kinesis Data Streams with Global Streams to replicate real-time events from us-east-1 to us-west-2, with Firehose delivering to S3 Parquet in both regions. Glue Catalog stays in sync via a centralized drift detectio…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design cross-region data products with real-time requirements, governance controls, and cost optimization.\n\n## Key Concepts\n- **Cross-region replication**: Kinesis Global Streams for real-time event replication between us-east-1 and us-west-2\n- **Catalog synchronization**: Automated drift detection comparing Glue metastore schemas across regions\n- **Fine-grained access control**: Lake Formation RBAC with per-tenant isolation and column/row-level permissions\n- **Idempotent processing**: Deduplication strategies using event IDs and state stores\n- **Late-arriving data**: Watermark windows with configurable thresholds and batch fallback mechanisms\n\n## Code Example\n```python\n# Example deduplication by event_id in Spark/Glue\nfrom pyspark.sql import DataFrame\n\ndef dedupe(df: DataFrame, key: str = \"event_id\") -> DataFrame:\n    return df.dropDuplicates([key])\n```\n\n## Follow-up Questions\n- How would you monitor cross-region replication lag and set alerting thresholds?\n- What is your strategy for schema evolution when the Parquet schema changes?\n- How do you handle tenant isolation if one tenant exceeds their S3 write quota?\n- What cost optimization strategies apply when batch analytics can tolerate higher latency?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-15",
        "questionId": "q-22287",
        "question": "A supplier uploads daily CSVs to s3://supplier/inventory/raw/YYYY/MM/DD/inventory.csv. Design a beginner-level AWS data pipeline to ingest, flatten, and convert to Parquet partitioned by date at s3://supplier/inventory/processed/YYYY/MM/DD/, registered in Glue Data Catalog. Include enrichment via a small S3 mapping for category, simple data quality checks (required fields, amount >= 0), and idempotency with Glue Job Bookmarks. Outline end-to-end tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use AWS Step Functions to orchestrate the workflow with retries and a manifest-based checkpoint",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition by partner_id and date; store under s3://data-lake/partner_id/date/. Use a single Glue Data Catalog with per-partner databases and Lake Formation grants so each partner can only query its da",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest streams via Kinesis Firehose to S3 with TLS and SSE-S3. Partition Parquet by version/source, e.g., s3://ml-data/version=20260313/source=a/. Use Glue Data Catalog for schema evolution; Spark ETL",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest the daily CSV with a Glue Spark job, flatten, cast types, and write Parquet to the processed path partitioned by date. Enrich with a lightweight S3 mapping for category. Validate non-null keys",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical Glue-based ingestion, Parquet partitioning, simple enrichment, and idempotency for a real daily feed.\n\n## Key Concepts\n\n- Glue Spark ETL and job bookmarks\n- Parquet with date-based partitions\n- Data enrichment from S3 mapping\n- Basic data quality checks and schema drift handling\n\n## Code Example\n\n```python\nfrom pyspark.sql import functions as F\n\ndf = spark.read.csv('s3://supplier/inventory/raw/YYYY/MM/DD/inventory.csv', header=True, inferSchema=True)\ndf = df.withColumn('amount', F.col('amount').cast('double'))\n# enrichment join with category mapping stored in S3\n# write parquet with partitionBy date\n```\n\n## Follow-up Questions\n\n- How would you test idempotency if the same file is re-uploaded?\n- How would you handle new columns appearing over time?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-16",
        "questionId": "q-24150",
        "question": "Design an AWS data pipeline for a multi-tenant event stream where each tenant's JSON events arrive in S3 and must be transformed into Parquet, partitioned by tenant_id and ingestion_date. Use AWS Glue for ETL and cataloging, Lake Formation for per-tenant access, and implement a lineage strategy across Glue jobs. Include schema-evolution handling for nested JSON, late-arriving data, testing plan with canary rollout, and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an AWS Glue Spark job to read JSON logs from s3://logs/raw/daily/YYYY/MM/DD/events.json, enforce data quality by filtering out rows with null values in required fields, and write partitioned Parquet files to s3://lo…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a single AWS Glue Spark job to read both inputs, infer a unified schema, cast types, and deduplicate by (sensor_id, ts) per day. Enable Job Bookmarks for idempotence, write Parquet partitions to s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Spark streaming in AWS Glue to ingest events from Kinesis and write to an Iceberg table on S3 using the Glue Catalog. Implement upserts and deletes using Iceberg's MERGE INTO and DELETE FROM operations, partition by…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a multi-tenant pipeline: ingest JSON per tenant to S3, a Glue Spark job writes Parquet partitioned by tenant_id/date and registers in the Glue Catalog. Use Lake Formation to grant per-tenant",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practicality of multi-tenant data lakes, governance, and schema evolution in AWS.\n\n## Key Concepts\n- Multi-tenant partitioning and access control\n- Glue ETL + Data Catalog + Lake Formation\n- Schema evolution strategies and lineage\n- Late-arriving data handling and canary testing\n\n## Code Example\n```python\n# PySpark sketch for a Glue job\nimport sys\nfrom awsglue.context import GlueContext\nfrom pyspark.context import SparkContext\nsc = SparkContext()\nglueContext = GlueContext(sc)\nspark = glueContext.spark_session\n# Read tenant-scoped JSONs\ndf = spark.read.json(\"s3://raw-tenant-data/*/events/*.json\")\n# Write to Parquet with partitioning\ndf.write.partitionBy(\"tenant_id\", \"ingest_date\").parquet(\"s3://lake/tenant/parquet/\")\n```\n\n## Follow-up Questions\n- How would you test schema-evolution changes across tenants? \n- What metrics would you monitor for the Canary rollout and rollback plan?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-17",
        "questionId": "q-2244",
        "question": "You receive daily JSON event files from a mobile app stored in S3 with nested arrays. Design a beginner-friendly ingestion pipeline to flatten into Parquet with daily partitions, catalog in AWS Glue, and implement a basic data quality gate (required fields, non-null user_id, ISO8601 event_time). Include testing approach and handling of schema drift?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Glue to read the daily JSON, enable schema evolution, and write Parquet partitioned by vendor/date to S3; register tables in the Glue Data Catalog; enforce fine-grained access with Lake Formation;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Read daily events.json from the ingest path (s3://partners/ingest/YYYY/MM/DD/), validate against a provided JSON schema with support for backward-compatible schema evolution, cast types and handle missing fields with ap…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an AWS Glue Spark job (PySpark) to read events.json from s3://events/raw/, convert it to Parquet format, and write to s3://events/processed/YYYY/MM/DD/ with date-based partitioning. Catalog the data in the Glue D…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Glue Spark job that reads daily JSON files from s3://partner-logs/incoming/YYYY/MM/DD, flattens nested arrays with explode, and maps to a canonical schema (user_id, event_time, device, act",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical workflow design for beginner to apply Glue, S3, and Parquet with schema evolution and data quality.\n\n## Key Concepts\n- Flattening nested JSON, Parquet partitions, Glue Catalog\n- Simple data quality rules, alerting, basic deduplication\n- Testing with sample data and end-to-end validation\n\n## Code Example\n```javascript\n// Pseudo Glue job invocation using AWS SDK\nimport { GlueClient, StartJobRunCommand } from \"@aws-sdk/client-glue\";\nconst client = new GlueClient({ region: \"us-east-1\" });\nconst cmd = new StartJobRunCommand({ JobName: \"FlattenJsonToParquet\" });\nclient.send(cmd);\n```\n\n## Follow-up Questions\n- How would you extend this with schema drift handling across daily files?\n- How would you test idempotency for repeated runs",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-18",
        "questionId": "q-25011",
        "question": "You maintain an AWS data lake across two accounts receiving high-cardinality JSON events from mobile apps. The data lands in S3 as daily Parquet partitions, with evolving nested schemas. Design an ingestion and processing pipeline that handles late-arriving data, schema evolution, and cross-account access using Glue, Lake Formation, IAM roles, and Athena. Include data quality gates, idempotency strategy, and a testing plan for end-to-end validity before analytics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a two-bucket Glue pipeline: raw events land in s3://lake/raw/YYYY/MM/DD, and processed Parquet data flows to s3://lake/processed/YYYY/MM/DD. Ensure idempotency using Glue JobBookmarks combined with a staging-upse…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement Apache Iceberg on S3 to store multi-source data; partition by date and source; register with Glue Data Catalog. Use Iceberg upserts (MERGE) and schema evolution; ingest Kinesis and RDS feeds",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Glue Spark job to ingest daily partner CSVs from s3://partner-logs/raw/partnerA/YYYY/MM/DD/, flatten optional ip_address, and output Parquet to s3://partner-logs/curated/partnerA/YYYY/MM/D",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Glue Spark job with Job bookmarks to read new CSVs from s3://data-lake/sourceD/raw/YYYY/MM/DD/*.csv, convert to Parquet, and write to curated with partitions by date and producer. Register in Gl",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your ability to design production-grade data pipelines that handle real-world complexity: cross-account data ingestion, evolving schemas for nested data, late-arriving data handling, and governance at scale.\n\n## Key Concepts\n- **Glue ETL with JobBookmarks**: Enables incremental processing and exactly-once semantics\n- **Lake Formation cross-account access**: Provides fine-grained security across account boundaries\n- **Schema evolution**: Handles changing nested JSON structures without pipeline breakage\n- **Data quality gates**: Ensure data meets defined standards before reaching downstream consumers",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-19",
        "questionId": "q-18680",
        "question": "You operate a cross-region data lake ingesting clickstream JSON events from a mobile app into S3 every minute. The events have nested fields and occasional schema drift. Design a robust AWS ingestion and validation pipeline that surfaces schema changes, preserves idempotency, and masks PII before data lands in the analytics layer. Include services, data formats, drift-detection approach, testing strategy, and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Glue Schema Registry to enforce a canonical schema, run a Glue Streaming job (Spark) to flatten nested JSON and mask PII with a UDF, then write Parquet to s3://lake/ingest/date=YYYY-MM-DD/. Enable",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a SageMaker Feature Store pipeline with offline/online stores; ingest raw data from Kinesis and S3 into feature groups; define tenant-scoped access via Lake Formation; implement schema evolutio",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Read daily events.json from the ingest path (s3://partners/ingest/YYYY/MM/DD/), validate against a provided JSON schema with support for backward-compatible schema evolution, cast types and handle missing fields with ap…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Glue Spark ETL job that MERGEs incoming events into Iceberg on S3 using event_id as the primary key, enabling idempotent upserts and daily partitions. Enable Iceberg schema evolution, and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical skills for handling schema drift, data quality, and privacy in a real-time AWS data pipeline across regions.\n\n## Key Concepts\n\n- Glue Schema Registry for a canonical schema across regions\n- Spark streaming in Glue for near real-time processing\n- Nested JSON flattening and PII masking with custom UDFs\n- Idempotency via Glue Job bookmarks and atomic parquet writes\n- Schema drift detection with registry comparisons and SNS alerts\n\n## Code Example\n\n```python\n# example masking function (Python)\ndef mask_pii(value):\n    if value is None:\n        return None\n    return \"***REDACTED***\"\n```\n\n## Follow-up Questions\n\n- How would you test end-to-end drift handling with synthetic events and rollback scenarios?\n- How would you scale to 10x ingest rate while preserving strict schema governance?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-20",
        "questionId": "q-2653",
        "question": "Scenario: A global product analytics data lake ingests streaming clickstream events from mobile apps with evolving JSON schemas containing PII. You must mask PII in the shared dataset before analytics, support cross-account access, and keep raw data auditable for compliance. Design end-to-end ingestion using AWS (Kinesis or MSK, Glue, S3, Lake Formation/Macie, KMS). Address schema evolution, late data, data quality, testing, and rollout?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Glue Spark job (DynamicFrame) to map to the canonical schema via ApplyMapping, ResolveChoice, and drop unexpected columns. Read all CSVs, fill missing fields with defaults, cast types, and write",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan a Glue Spark job to validate each JSON event with a defined schema, route valid records to s3://iot-data/events/YYYY/MM/DD as Parquet, and malformed ones to s3://iot-raw/quarantine/YYYY/MM/DD. Us",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-branch streaming: ingest with Kinesis (or MSK); route to a masking Spark job in Glue that redacts PII but preserves IDs; emit masked Parquet to S3 in daily partitions (date, region, app). Use Glue",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement an idempotent pipeline: Glue ETL reads the daily CSV via dynamic frame, deduplicates by transaction_id, writes Parquet to s3://data-lake/transactions/YYYY/MM/DD/, overwriting the partition to ensure idempotenc…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\\nThis question probes practical privacy-preserving data ingestion at scale, cross-account access, and governance.\\n\\n## Key Concepts\\n- Streaming data pipelines (Kinesis/MSK)\\n- PII masking in ETL (Glue Spark UDFs)\\n- Data lake governance (Glue Catalog, Lake Formation)\\n- PII discovery (Macie) and KMS-based encryption\\n\\n## Code Example\\n```python\\n# Glue Spark masking example (conceptual)\\ndef mask(record):\\n  record['email'] = hash_value(record.get('email'))\\n  record['phone'] = mask_phone(record.get('phone'))\\n  return record\\n```\\n\\n## Follow-up Questions\\n- How would you test schema evolution and backward compatibility across partitions?\\n- How would you monitor data quality and alert on masking failures in production?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-21",
        "questionId": "q-14380",
        "question": "Design a beginner-level pipeline to capture daily changes from a MySQL source using AWS DMS CDC to S3 as Parquet, partitioned by date, and registered in Glue Data Catalog; ensure idempotent ingestion with event_id, add simple data quality checks (nonnulls, FK validity), and outline a test plan for end-to-end replay?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use DMS for initial load and CDC to S3 Parquet, partitioned by date; trigger a Glue cataloging job to update the Data Catalog. Ensure idempotency by deduplicating on event_id and persisting the max LS",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use an AWS Glue Spark job to read s3://data-logs/app-events/YYYY/MM/DD/events.json, flatten nested JSON fields, apply PII masking (hash emails, redact phone numbers), and write the processed data as Parquet files partit…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a hybrid real-time/batch data platform for multi-tenant fraud detection. Ingest via MSK, run KDA for sub-second signals into DynamoDB and land transformed events to S3 Parquet via Glue, with",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed solution: Ingest from KDS to S3 via a Glue streaming ETL or Firehose to Parquet, with daily partitions. Use Glue Schema Registry to version and evolve the nested JSON schema; keep tx_timestam",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExamines practical CDC ingestion using AWS DMS, Parquet storage, and Glue Cataloging, plus real-world concerns like deduplication, schema drift, and basic data quality.\n\n## Key Concepts\n\n- AWS DMS CDC for incremental replication\n- Parquet format in S3 and date-partitioning\n- Glue Data Catalog and crawler jobs\n- Idempotency with event_id/LSN and dedup logic\n- Simple FK and non-null data quality checks\n\n## Code Example\n\n```python\n# PySpark snippet (conceptual) to deduplicate by event_id after load\nfrom pyspark.sql import SparkSession\ns = SparkSession.builder.getOrCreate()\ndf = s.read.parquet('s3://bucket/raw/..')\ndf2 = df.dropDuplicates(['event_id'])\ndf2.write.mode('overwrite').parquet('s3://bucket/derived/..')\n```\n\n## Follow-up Questions\n\n- How would you handle a schema change in MySQL (new column) without breaking downstream analytics?\n- How would you monitor and alert on CDC lag and data quality failures?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-22",
        "questionId": "q-19718",
        "question": "Scenario: A regulated SaaS data lake spans two AWS accounts and two regions. Design an end-to-end pipeline that ingests streaming JSON and batch CSV, stores Parquet partitioned by date and tenant, catalogs via Glue, enforces read-time tenant masking and access control, and preserves data lineage. Outline services, formats, schema evolution, testing, and failure handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a multi-account, multi-region pipeline ingesting streaming JSON and batch CSV; land as Parquet partitioned by date and tenant; catalog with Glue Data Catalog; enforce tenant-scoped access and",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Read daily events.json from the ingest path (s3://partners/ingest/YYYY/MM/DD/), validate against a provided JSON schema with support for backward-compatible schema evolution, cast types and handle missing fields with ap…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Glue Spark job to read tenant-scoped events.json, deduplicate on tenant_id+event_id, then write Parquet to s3://saas-data/{tenant}/{YYYY}/{MM}/{DD}/ and register partitions in Glue. Enforce per-",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Spark Structured Streaming (Glue or EMR) to read from Kinesis Data Streams, write to an Iceberg table on S3 with daily partitions, register in Glue Catalog, enable schema evolution with MERGE operations for upserts,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests cross-account, cross-region pipeline design, tenant isolation, masking, and lineage at scale with practical trade-offs.\n\n## Key Concepts\n\n- Cross-account access\n- Read-time masking\n- Parquet partitioning (date/tenant)\n- Data lineage and auditing\n- Schema evolution\n\n## Code Example\n\n```python\n# PySpark masking example\nfrom pyspark.sql import functions as F\ndf = spark.read.parquet(\"s3://bucket/raw/\")\nmasked = df.withColumn(\"email\", F.when(F.col(\"email\").isNotNull(),\n    F.regexp_replace(F.col(\"email\"), r\"(^[^@]+)(.*)(@.*$)\", r\"\\1*****\\3\")))\nmasked.write.parquet(\"s3://bucket/curated/date=.../tenant=.../\")\n```\n\n## Follow-up Questions\n\n- How would you handle schema drift between streaming and batch sources?\n- How would you verify data lineage across accounts/regions, and what metrics would you monitor?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-23",
        "questionId": "q-26489",
        "question": "Scenario: A fintech data pipeline ingests JSON transactions from Kinesis Data Streams at high cardinality and nested structures. Design an intermediate ingestion and storage flow that lands data as Parquet in S3 partitioned by date, preserves exact transaction timestamps, and supports schema evolution. Include choice of service (Firehose vs Glue), how to handle late data, data quality checks, idempotency, and end-to-end testing. Also discuss governance and access controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-source ingestion pipeline to a single analytics table in S3 using upserts. Data sources include: mobile app events in JSON format via Kinesis, web events in Parquet format via Kinesis Firehose to S3, and…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed solution: Ingest from KDS to S3 via a Glue streaming ETL or Firehose to Parquet, with daily partitions. Use Glue Schema Registry to version and evolve the nested JSON schema; keep tx_timestam",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a Glue Spark job that reads daily JSON files from s3://partner-logs/incoming/YYYY/MM/DD, flattens nested arrays with explode, and maps to a canonical schema (user_id, event_time, device, act",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Lake Formation governance with per-tenant databases mapped to S3 prefixes (s3://lake/tenant-id/...). Expose cross-account access via resource links and tag-based ACLs, audited by Lake Formation an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time vs batch trade-offs, schema evolution, late data handling, idempotency, and end-to-end testing in a multi-account, cost-conscious environment.\n\n## Key Concepts\n- Streaming ETL vs batch processing, Glue vs Firehose\n- Parquet with date-based partitions and timestamp preservation\n- Glue Schema Registry for evolving nested JSON schemas\n- Late-arriving data strategies and watermarking\n- Idempotent upserts via txn_id/digest; end-to-end test plans\n\n## Code Example\n\n```javascript\n// Pseudo-configuration sketch (not runnable)\nconst pipeline = {\n  source: 'Kinesis Data Streams',\n  transform: 'Glue Streaming ETL',\n  sink: 'S3 Parquet with daily partitions',\n  catalog: 'Glue Data Catalog',\n  schema: 'Glue Schema Registry'\n}\n```\n\n## Follow-up Questions\n- How would you monitor data quality drift and schema evolution events?\n- What rollback or replay strategy would you implement for reprocessing?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-24",
        "questionId": "q-12061",
        "question": "Design a beginner-level AWS data ingestion pipeline for daily JSON events from multiple mobile apps arriving to s3://corp-lake/raw/appX/YYYY/MM/DD/events.json, ensuring idempotent ingestion, handling dynamic schemas, and masking PII before landing Parquet to s3://corp-lake/processed/appX/YYYY/MM/DD/. Describe services, partitioning, schema evolution strategy, and testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use AWS Glue Job with bookmarks to read s3://corp-lake/raw/appX/YYYY/MM/DD/events.json, flatten nested fields, coerce types, and mask PII (redact email local-part). Write Parquet to s3://corp-lake/pro",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use an AWS Glue Spark job: read CSVs from s3://partner-logs/raw/YYYY/MM/DD/*.csv with header inference and a fallback mapping, cast fields to the target types, and convert local timestamps to UTC with",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Iceberg-on-S3 for per-tenant, time-travel capable tables; stream data through Kinesis and batch via Glue; CDC via DMS from partners; catalog in Glue Data Catalog; enforce isolation with Lake Forma",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a serverless pipeline: raw CSV.gz lands in s3://cust-data/raw/saasBeta/YYYY/MM/DD/export.csv.gz. A Glue ETL job reads it, applies a defined schema (mergeSchema), and writes Parquet to s3://cust-da",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAims to assess practical, end-to-end data ingestion, basic ETL, and data privacy handling at the data-lake edge.\n\n## Key Concepts\n- Idempotent ingestion with Glue bookmarks or Dynamo-based dedupe\n- Dynamic schema handling, nested JSON flattening\n- Data masking for PII, Parquet writes, partitioning\n\n## Code Example\n```javascript\nfunction maskEmail(email){\n  if(!email) return null;\n  const [local, domain] = email.split('@');\n  return local ? local.charAt(0) + '***@' + domain : null;\n}\n```\n\n## Follow-up Questions\n- How would you validate end-to-end with schema drift in a daily job?\n- How would you monitor job failures and reprocess missed partitions?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-19T09:42:17.274Z",
    "version": 1
  }
]