[
  {
    "id": "test-aws-data-engineer",
    "channelId": "aws-data-engineer",
    "channelName": "AWS Data Engineer",
    "title": "AWS Data Engineer Knowledge Test",
    "description": "Test your understanding of AWS Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "aws-data-engineer-q-0",
        "questionId": "q-12216",
        "question": "Daily user activity CSVs arrive in s3://cust-lake/raw/users/YYYY/MM/DD/events.csv. Design a beginner-level ingestion to land as Parquet in s3://cust-lake/processed/users/YYYY/MM/DD/events.parquet, partitioned by date, with masking of PII (emails, phone) in the ETL, and a Glue Data Catalog table. Include data quality checks and a simple end-to-end test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Glue Spark job (Python) to read TSV, handle evolving headers via DynamicFrame.resolveChoice, and write Parquet partitioned by date and service_name to s3://saas-logs/processed/YYYY/MM/DD/service",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Step Functions workflow: Lambda fetches nightly API data and writes to the raw path; Glue ETL reads it, applies a stable Parquet schema, partitions by date, and updates the catalog; run a data q",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a beginner pipeline: trigger from new CSVs in s3://cust-lake/raw/users/YYYY/MM/DD/events.csv, run a Glue Spark job that masks PII (emails via regex, phone numbers) to tokens, writes Parquet",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Apache Iceberg on Spark (EMR or Glue) with a tiered storage strategy: a hot table for the last 14 days partitioned by day, with older data archived to cold Parquet storage. Ingest telemetry using MERGE INTO statemen…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests ability to design a compliant data pipeline with privacy in mind, plus Glue cataloging and simple tests.\n\n## Key Concepts\n- PII masking in ETL with Spark\n- Parquet partitioning and Glue Catalog\n- Lightweight data quality checks\n- End-to-end testing strategies\n\n## Code Example\n```javascript\n// Pseudo Spark masking in JS-like syntax\nconst df = readCsv('s3://cust-lake/raw/users/YYYY/MM/DD/events.csv');\nconst masked = df\n  .withColumn('email', regexReplace(col('email'), /\\b[\\w.%-]+@[\\w.-]+\\.[A-Za-z]{2,}\\b/, '<REDACTED>'))\n  .withColumn('phone', regexReplace(col('phone'), /\\d{3}-\\d{3}-\\d{4}/, '<REDACTED>'));\nmasked.writeParquet('s3://cust-lake/processed/users/YYYY/MM/DD/events.parquet');\n```\n\n## Follow-up Questions\n- How would you validate that masking did not alter non-PII fields?\n- How to ensure idempotent processing without duplicates?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-1",
        "questionId": "q-6990",
        "question": "Advanced: You run a multi-region data lake for telemetry from edge devices. Data arrives as JSON with evolving schema, occasional late data, and strict per-tenant access. Design an end-to-end pipeline using S3 with Apache Iceberg, Glue catalog, and Athena across accounts; include schema evolution, deduplication, data quality checks, cross-region replication, and auditing. How would you ensure low-latency analytics and governance under cost constraints?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a serverless ingest: trigger a Glue/Spark job from S3 event for each day, flatten nested JSON to Parquet, write to s3://data-lake/warehouse/ads/YYYY/MM/DD/, partition by date, and register the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a multi-tenant ingestion: land JSON events to s3://lake/ingest/{tenant}/{source}/{date}/, store as Iceberg-parquet in S3, and catalog with Glue. Enforce a data contract via Glue Schema Registry",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Apache Iceberg on S3 with a Glue catalog, ingested via Kinesis Data Streams and Spark on Glue/EMR. Partition by tenant and day; enable Iceberg schema evolution; deduplicate by (tenant,id,ts); hand",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a Glue Spark job that reads daily JSON files from s3://partner-logs/incoming/YYYY/MM/DD, flattens nested arrays with explode, and maps to a canonical schema (user_id, event_time, device, act",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nRigor: tests mastery of multi-region data lakes, Iceberg on S3, and governance at scale. It evaluates handling evolving schemas, late data, real-time ingestion, and cross-account access.\n\n## Key Concepts\n\n- Apache Iceberg on S3 with Glue catalog\n- Kinesis + Spark (Glue/EMR)\n- Lake Formation per-tenant access\n- Cross-region S3 replication\n- Data quality checks and deduplication\n\n## Code Example\n\n```scala\n// Pseudo-Spark dedupe\nimport org.apache.spark.sql.functions._\nval df = spark.readStream.format(\"json\").load(\"s3://...\")\nval deduped = df.withWatermark(\"timestamp\",\"1h\").dropDuplicates(\"tenant\",\"device_id\",\"event_id\")\n```\n\n## Follow-up Questions\n\n- How would you test late data handling?\n- How would you implement tombstones in Iceberg?\n- How to validate governance across accounts?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-2",
        "questionId": "q-27867",
        "question": "Beginner-level: Daily Glue ETL ingests JSON logs from s3://logs/raw/daily/YYYY/MM/DD/events.json and writes Parquet to s3://logs/warehouse/events/YYYY/MM/DD/, partitioned by date. Propose a minimal pipeline with a simple data quality check (required fields non-null), a practical schema evolution plan, and a lightweight monitoring setup using CloudWatch metrics and an SNS alert for failures; outline end-to-end tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an AWS Glue Spark job to read JSON logs from s3://logs/raw/daily/YYYY/MM/DD/events.json, enforce data quality by filtering out rows with null values in required fields, and write partitioned Parquet files to s3://lo…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a simple, idempotent pipeline: read both raw paths with Glue jobs, map fields to a canonical schema, coerce types (e.g., strings to ints where safe), union datasets, write Parquet to curated pa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Lake Formation to enforce per-tenant access; Glue Spark job reads per-tenant JSON from S3, detects drift by versioning tenant schemas under schemas/tenant, writes Parquet/Iceberg tables partitione",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Apache Iceberg on S3 to store a single logical table with per-tenant prefixes; stream with Kinesis Data Firehose and batch with Glue ETL; enforce Lake Formation permissions per tenant; implement i",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical understanding of an end-to-end data pipeline, covering ingestion, schema handling, partitioning, and operational reliability.\n\n## Key Concepts\n\n- AWS Glue Spark/DynamicFrame for ETL processing\n- Schema inference with controlled evolution strategy\n- Parquet output with date-based partitioning\n- Job bookmarks for idempotent, fault-tolerant processing\n- CloudWatch metrics and SNS alerts for monitoring and alerting\n- End-to-end validation and testing\n\n## Code Example\n\n```python\nfrom awsglue.context import GlueContext\nfrom pyspark.context import SparkContext\nfrom awsglue.dynamicframe import DynamicFrame\nfrom awsglue.job import Job\nimport boto3\n\nglue_context = GlueContext(SparkContext.get_or_create())\nsns_client = boto3.client('sns')\n\ndef check_required_fields(dyf):\n    required_fields = [\"event_id\", \"timestamp\", \"user_id\"]\n    return dyf.filter(f=lambda x: all(x.get(f) is not None for f in required_fields))\n\ndef send_alert(message):\n    sns_client.publish(TopicArn=\"arn:aws:sns:region:account:glue-alerts\", Message=message)\n\ntry:\n    source_path = \"s3://logs/raw/daily/2024/01/15/events.json\"\n    dyf = glue_context.create_dynamic_frame.from_options(\n        connection_type=\"s3\",\n        format=\"json\",\n        connection_options={\"path\": source_path}\n    )\n    \n    dyf_filtered = check_required_fields(dyf)\n    \n    output_path = \"s3://logs/warehouse/events/2024/01/15/\"\n    glue_context.write_dynamic_frame.from_options(\n        frame=dyf_filtered,\n        connection_type=\"s3\",\n        format=\"parquet\",\n        connection_options={\"path\": output_path, \"partitionBy\": [\"timestamp\"]}\n    )\nexcept Exception as e:\n    send_alert(f\"Glue job failed: {str(e)}\")\n    raise\n```",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-3",
        "questionId": "q-20912",
        "question": "Scenario: Daily JSON events arrive at s3://events/raw/YYYY/MM/DD/events.json. Design a beginner-level AWS data pipeline that converts to Parquet, writes to s3://events/processed/YYYY/MM/DD/ with date partitioning, catalogs in Glue, and implements schema drift alerts: if a field is added, removed, or its type changes, trigger a CloudWatch alarm and log the drift. Include a testing approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an AWS Glue Spark job (PySpark) to read events.json from s3://events/raw/, convert it to Parquet format, and write to s3://events/processed/YYYY/MM/DD/ with date-based partitioning. Catalog the data in the Glue D…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a beginner pipeline: let a Glue crawler infer the schema from logs.tsv, run an ETL job to flatten rows and write Parquet to s3://logs/warehouse/tsv/YYYY/MM/DD/, partitioned by date, and register a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Apache Iceberg on S3 (Parquet) with a Glue Data Catalog. Implement upserts/deletes via MERGE, with tombstones for deletes and partition pruning for cost. Ingest via CDC streams from SaaS sources,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a multi-source ingestion: Kinesis streams and S3 dumps fed into Spark-based Glue ETL writing Iceberg Parquet tables on S3, partitioned by date and registered in Glue Data Catalog. Ensure ide",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests knowledge of AWS Glue-based ETL, partitioning strategies, the Glue Data Catalog, and implementing data quality monitoring with schema drift alerts.\n\n## Key Concepts\n\n- AWS Glue Spark ETL (PySpark)\n- Parquet format with date-based partitioning\n- Glue Data Catalog for metadata management\n- Schema drift detection with CloudWatch alarms\n- Job bookmarks for idempotent, fault-tolerant runs\n\n## Code Example (PySpark)\n\n```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.types import StructType\nimport boto3\n\ndef detect_schema_drift(inferred_schema: StructType, catalog_schema: StructType) -> dict:\n    drift = {\"added\": [], \"removed\": [], \"type_changed\": []}\n    inferred_fields = {f.name: f.dataType for f in inferred_schema.fields}\n    catalog_fields = {f.name: f.dataType for f in catalog_schema.fields}\n    \n    for field in inferred_fields:\n        if field not in catalog_fields:\n            drift[\"added\"].append(field)\n        elif inferred_fields[field] != catalog_fields[field]:\n            drift[\"type_changed\"].append(field)\n    \n    for field in catalog_fields:\n        if field not in inferred_fields:\n            drift[\"removed\"].append(field)\n    \n    return drift\n\ndef handler(event, context):\n    spark = SparkSession.builder.getOrCreate()\n    \n    raw_path = \"s3://events/raw/\"\n    processed_path = \"s3://events/processed/\"\n    \n    df = spark.read.json(raw_path)\n    inferred_schema = df.schema\n    \n    glue = boto3.client('glue')\n    try:\n        table = glue.get_table(DatabaseName='events_db', Name='events')['Table']\n        catalog_schema = StructType.fromJson(table['StorageDescriptor']['Schema'])\n        \n        drift = detect_schema_drift(inferred_schema, catalog_schema)\n        \n        if any(drift.values()):\n            cloudwatch = boto3.client('cloudwatch')\n            cloudwatch.put_metric_data(\n                Namespace='SchemaDrift',\n                MetricData=[{\n                    'MetricName': 'SchemaDriftDetected',\n                    'Value': 1,\n                    'Unit': 'Count'\n                }]\n            )\n            print(f\"Schema drift detected: {drift}\")\n    except Exception as e:\n        print(f\"No catalog schema found: {e}\")\n    \n    df.write.partitionBy(\"year\", \"month\", \"day\").parquet(processed_path)\n    spark.stop()\n```\n\n## Testing Approach\n\n1. **Unit Test**: Mock the schema comparison logic with various drift scenarios (added, removed, type-changed fields)\n2. **Integration Test**: Create sample JSON files with schema changes, run the Glue job, and verify CloudWatch metrics are published\n3. **Data Quality Test**: Use AWS Glue Data Quality (DQX) rules to validate schema consistency post-ingestion",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-4",
        "questionId": "q-25969",
        "question": "Design a beginner-level AWS data pipeline that ingests a daily JSON events file landed at s3://org-raw/events/YYYY/MM/DD/events.json, validates a fixed required schema, and performs simple quality checks (non-null id and date, non-negative numeric fields). Write the clean data to Parquet partitioned by date at s3://org-curated/events/YYYY/MM/DD/ and emit failing records to s3://org-curated/events/anomalies/YYYY/MM/DD/ with a reason column. Implement with AWS Glue (Python Spark) and a Glue bookmark; outline tests and a lightweight schema evolution strategy for new optional fields?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route streams via Kinesis Data Streams into a Glue Streaming ETL (Spark) job that deduplicates by tenant+event_id using an in-flight state store (DynamoDB) and writes Parquet to S3 with daily partitio",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a beginner-level AWS data pipeline that ingests a daily JSON events file from s3://org-raw/events/YYYY/MM/DD/events.json, validates against a fixed required schema, and performs simple quality checks (non-null id…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage a streaming path with Kinesis Data Streams and Glue Schema Registry for validation, Kinesis Data Analytics for windowed transforms, and emit Parquet to a partitioned S3 layout. A batch path u",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Kinesis Data Streams with Global Streams to replicate real-time events from us-east-1 to us-west-2, with Firehose delivering to S3 Parquet in both regions. Glue Catalog stays in sync via a centralized drift detectio…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design a practical, end-to-end data ingestion pipeline with data quality enforcement and anomaly handling using AWS Glue at a beginner level. It also assesses understanding of idempotence, incremental processing, and lightweight schema evolution.\n\n## Key Concepts\n- AWS Glue Spark Jobs and DynamicFrames vs DataFrames\n- Fixed schema enforcement and quality control rules\n- Parquet partitioning by date for optimized queries\n- Anomaly handling with a reason column for debugging\n- Glue bookmarks for idempotent, incremental processing\n- Lightweight schema evolution strategy for new optional fields\n- End-to-end testing with clean and bad record scenarios\n\n## Code Example\n```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.types import StructType, StructField, StringType, LongType, DoubleType\nfrom pyspark.sql.functions import col, lit, to_json, struct\n\n# Define fixed target schema\nTARGET_SCHEMA = StructType([\n    StructField(\"id\", StringType(), False),\n    StructField(\"date\", StringType(), False),\n    StructField(\"event_type\", StringType(), True),\n    StructField(\"amount\", DoubleType(), True),\n    StructField(\"quantity\", LongType(), True)\n])\n\ndef run_glue_job():\n    glueContext = GlueContext(SparkSession.builder.getOrCreate())\n    spark = glueContext.sparkSession\n    \n    # Read raw JSON with bookmark enabled\n    datasource = glueContext.create_dynamic_frame.from_options(\n        connection_type=\"s3\",\n        connection_options={\n            \"path\": \"s3://org-raw/events/\",\n            \"recurse\": True,\n            \"jobBookmarkKeys\": [\"year\", \"month\", \"day\"],\n            \"jobBookmarkKeysSortOrder\": \"asc\"\n        },\n        format=\"json\"\n    )\n    \n    # Apply schema and QC transformations\n    mapped = ApplyMapping.apply(\n        frame=datasource,\n        mappings=TARGET_SCHEMA.mappings\n    )\n    \n    # Quality checks\n    def qc_check(dynamic_frame):\n        valid_records = []\n        invalid_records = []\n        \n        for record in dynamic_frame.toRDD().collect():\n            reasons = []\n            if not record.get(\"id\"):\n                reasons.append(\"NULL_ID\")\n            if not record.get(\"date\"):\n                reasons.append(\"NULL_DATE\")\n            amount = record.get(\"amount\")\n            quantity = record.get(\"quantity\")\n            if amount is not None and amount < 0:\n                reasons.append(\"NEGATIVE_AMOUNT\")\n            if quantity is not None and quantity < 0:\n                reasons.append(\"NEGATIVE_QUANTITY\")\n            \n            if reasons:\n                record[\"reason\"] = \"|\".join(reasons)\n                invalid_records.append(record)\n            else:\n                valid_records.append(record)\n        \n        return (\n            DynamicFrame.fromRDD(spark.sparkContext.parallelize(valid_records), \"valid\"),\n            DynamicFrame.fromRDD(spark.sparkContext.parallelize(invalid_records), \"invalid\")\n        )\n    \n    valid_df, invalid_df = qc_check(mapped)\n    \n    # Write valid data to Parquet partitioned by date\n    glueContext.write_dynamic_frame.from_options(\n        frame=valid_df,\n        connection_type=\"s3\",\n        connection_options={\"path\": \"s3://org-curated/events/\", \"partitionBy\": [\"year\", \"month\", \"day\"]},\n        format=\"parquet\"\n    )\n    \n    # Write anomalies with reason column\n    glueContext.write_dynamic_frame.from_options(\n        frame=invalid_df,\n        connection_type=\"s3\",\n        connection_options={\"path\": \"s3://org-curated/events/anomalies/\"},\n        format=\"json\"\n    )\n```\n\n## Testing Strategy\n1. **Unit Tests**: Test QC logic with mock records (valid, null id, null date, negative amounts)\n2. **Integration Tests**: Verify Parquet output schema and partitioning\n3. **End-to-End Tests**: Run job against sample data and validate both output paths\n\n## Lightweight Schema Evolution Strategy\n- Use schema of latest expected fields with all new optional fields set to nullable\n- Add new fields to TARGET_SCHEMA as nullable columns\n- For JSON sources, enable `withColumn` to handle missing fields gracefully\n- Log warnings for unexpected fields rather than failing the job",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-5",
        "questionId": "q-29920",
        "question": "Design an end-to-end AWS data ingestion for JSON events from mobile apps delivering 2 TB/day across regions. Implement a hybrid streaming/batch pipeline: streaming path via Kinesis Data Streams + AWS Glue Schema Registry + Kinesis Data Analytics to validate, transform, and emit Parquet to s3://data/events/dt=YYYY/MM/DD/ with daily partitions; batch path via Glue ETL to land raw Parquet in s3://data/events/raw/YYYY/MM/DD and register in the Data Catalog. Address late data, schema evolution, idempotency, cross-account access, testing plan, and data quality gates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build a cross-account ingestion using Glue Catalog + Lake Formation. Land Parquet partitions by date into S3; let a Glue Crawler update the catalog per region. Enforce per-team permissions via Lake Fo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a multi-account, multi-region pipeline ingesting streaming JSON and batch CSV; land as Parquet partitioned by date and tenant; catalog with Glue Data Catalog; enforce tenant-scoped access and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Kinesis Data Firehose for streamlined JSON event ingestion into S3 with dynamic partitioning by device_type and date. A Glue Spark job transforms data to Parquet format, updates the Data Catalog, and handles sc…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a streaming path with Kinesis Data Streams and Glue Schema Registry for validation, Kinesis Data Analytics for windowed transforms, and emit Parquet to a partitioned S3 layout. A batch path u",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design hybrid streaming/batch pipelines with schema evolution and data quality gates across regions. It also probes federation, idempotency, and testing.\n\n## Key Concepts\n- Streaming to Parquet with schema registry\n- Batch ingestion with Glue ETL\n- Late data handling, dedup, and watermarking\n- Cross-account access and Lake Formation\n- End-to-end testing and data quality gates\n\n## Code Example\n```javascript\n// Pseudo: ensure idempotent write\nconst df = readParquet('s3://data/events/raw/...');\nconst deduped = df.dropDuplicates(['record_id']);\nwriteParquet(deduped, 's3://data/events/streaming/partitioned/');\n```\n\n## Follow-up Questions\n- How would you monitor schema drift across regions?\n- Describe idempotent re-ingest strategies across streams and batches.",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-6",
        "questionId": "q-5538",
        "question": "In a daily multi-tenant JSON feed, design an end-to-end ingestion pipeline using AWS data services to land data into S3 as Parquet with daily partitions and per-tenant isolation. Include: partitioning strategy (date + tenant_id), Glue ETL to normalize and handle schema evolution, catalog registration, basic data quality checks, and a test plan. Also specify access controls (Lake Formation/IAM) per tenant?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Glue Job with bookmarks to process only new s3://iot-telemetry/raw/YYYY/MM/DD/ files, normalize to a fixed schema, and write Parquet to s3://iot-telemetry/processed/YYYY/MM/DD/ partitioned by da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an end-to-end pipeline for a daily multi-tenant JSON feed. Ingest to S3 with partitions by date and tenant_id, convert to Parquet, and register in the Glue Data Catalog. Use Glue ETL to normali",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Ingest with Kinesis to S3 Parquet (daily partitions). Use Glue ETL to enforce schema evolution and register schemas in Glue Data Catalog. Build offline features in Snowflake on top of Parquet tables;",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single source-of-truth data contract (Avro) in Glue Schema Registry, enforced by Glue Data Catalog. Streaming: Kinesis Data Streams to Firehose writes Parquet to S3 with daily partitions; Batch:",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical data ingestion for multi-tenant data lakes, with governance and basic quality checks.\n\n## Key Concepts\n- Partitioning strategy: date + tenant_id\n- Glue ETL: normalization, Parquet emission\n- Schema evolution: catalog versioning, add-field handling\n- Data quality: nulls, types, ranges\n- Access control: IAM/Lake Formation per tenant\n\n## Code Example\n```javascript\n// Pseudo-logic: not required in real environment\n```\n\n## Follow-up Questions\n- How would you automate schema drift detection across daily runs?\n- How would you test tenant isolation at scale?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-7",
        "questionId": "q-29664",
        "question": "Scenario: Two daily data sources arrive: s3://data-lake/raw/events/YYYY/MM/DD/events.csv and s3://data-lake/raw/products/YYYY/MM/DD/products.csv. Build a beginner-level AWS Glue pipeline that joins on product_id, flattens nested fields, handles missing values, outputs Parquet to s3://data-lake/warehouse/combined/YYYY/MM/DD/, partitions by date, and registers a Glue Data Catalog table. Describe schema evolution handling, idempotency, and a concrete end-to-end test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Glue Spark job that reads both CSV files via DynamicFrames, performs a left join on product_id, casts data types, fills missing fields with defaults, and writes Parquet output to the warehouse partitioned by date.…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Choose Apache Hudi (Copy-on-Write) to upsert Parquet datasets on S3 by event_id, partitioned by event_date. Ingest JSON in account A via Glue Spark job, upsert into a partitioned Parquet sink, catalog",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a solution using OpenLineage events emitted by Glue and Spark jobs, store lineage graph in a central catalog (Glue Data Catalog + OpenLineage backend) per account, propagate lineage across a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To support multi-geo data lakes, implement a hybrid ingestion architecture that combines Kinesis Data Streams for real-time events with AWS Glue streaming jobs for near-real-time processing, both landing as partitioned…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests your ability to integrate multiple data sources, perform joins, and manage schema evolution at a beginner level. It also validates your understanding of idempotent processing and end-to-end testing.\n\n## Key Concepts\n\n- AWS Glue ETL using DynamicFrames and Spark\n- Left join on product_id\n- Data type casting and default values for missing fields\n- Parquet output with date partitioning\n- Glue Data Catalog integration\n- Job bookmarks for idempotency\n- Schema evolution strategy with forward compatibility\n\n## Code Example\n\n```python\nimport sys\nfrom awsglue.context import GlueContext\nfrom awsglue.job import Job\nfrom awsglue.utils import getResolvedOptions\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, lit, when, to_date\nfrom awsglue.gluetypes import *\n\nspark = SparkSession.builder.getOrCreate()\nglueContext = GlueContext(spark)\njob = Job(glueContext)\n\nargs = getResolvedOptions(sys.argv, ['JOB_NAME'])\njob.init(args['JOB_NAME'], args)\n\n# Read events with job bookmark for idempotency\nevents_dyf = glueContext.create_dynamic_frame.from_options(\n    connection_type='s3',\n    connection_options={\n        'paths': ['s3://data-lake/raw/events/'],\n        'recurse': True\n    },\n    format='csv',\n    format_options={'withHeader': True}\n)\n\n# Read products\nproducts_dyf = glueContext.create_dynamic_frame.from_options(\n    connection_type='s3',\n    connection_options={\n        'paths': ['s3://data-lake/raw/products/'],\n        'recurse': True\n    },\n    format='csv',\n    format_options={'withHeader': True}\n)\n\n# Flatten nested fields and normalize schema\ndef flatten_and_cast(dyf, source):\n    df = dyf.toDF()\n    for col_name in df.columns:\n        df = df.withColumn(col_name, col(col_name).cast('string'))\n    return DynamicFrame.fromDF(df, glueContext, source)\n\nevents_flat = flatten_and_cast(events_dyf, 'events')\nproducts_flat = flatten_and_cast(products_dyf, 'products')\n\n# Handle missing values with defaults\nevents_filled = events_flat.make_missing_values_null().resolveChoice(\n    spec=[('product_id', 'cast:string')]\n)\nproducts_filled = products_flat.make_missing_values_null().resolveChoice(\n    spec=[('product_id', 'cast:string')]\n)\n\n# Left join on product_id\njoined_dyf = Join.apply(\n    frame1=events_filled,\n    frame2=products_filled,\n    keys1=['product_id'],\n    keys2=['product_id'],\n    transformation_ctx='joined'\n)\n\n# Apply defaults for null values\ndef apply_defaults(dyf):\n    df = dyf.toDF()\n    df = df.withColumn('created_at', when(col('created_at').isNull(), lit('1970-01-01')).otherwise(col('created_at')))\n    df = df.withColumn('category', when(col('category').isNull(), lit('unknown')).otherwise(col('category')))\n    return DynamicFrame.fromDF(df, glueContext, 'with_defaults')\n\nfinal_dyf = apply_defaults(joined_dyf)\n\n# Write Parquet with date partitioning\noutput_path = 's3://data-lake/warehouse/combined/'\nglueContext.write_dynamic_frame.from_options(\n    frame=final_dyf,\n    connection_type='s3',\n    connection_options={\n        'path': output_path,\n        'partitionBy': ['dt']\n    },\n    format='parquet'\n)\n\n# Register Glue Data Catalog table\ntarget_table = glueContext.create_table_from_catalog(\n    database='warehouse',\n    table_name='combined_events',\n    schema=final_dyf.schema(),\n    partition_keys=['dt'],\n    connection_options={\n        'path': output_path\n    }\n)\n\njob.commit()\n```\n\n## Schema Evolution\n\n1. **AddColumns (forward compatibility)**: New columns in source data are handled via `resolveChoice` with `make_missing_values_null`. Existing jobs continue reading without modification.\n2. **DropColumns**: Use explicit schema projection to ignore unexpected fields—filter columns explicitly before the join.\n3. **Type changes**: Cast all fields to String during ingestion, then cast to desired types downstream. This buffers against upstream type changes.\n4. **Version tracking**: Store schema version in the Glue Data Catalog table properties for auditing.\n\n## Idempotency\n\n1. **Job Bookmarks**: Enable `enableBookmarks` in the Glue job configuration. Glue tracks processed file names and last-modified timestamps in DynamoDB.\n2. **Overwrite mode**: Use `write` mode `overwrite` for Parquet writes to handle retries cleanly.\n3. **Unique output paths**: Partition by date ensures no data overlap between runs.\n4. **Upsert pattern**: For late-arriving data, use a merge/upsert strategy with a staging table instead of simple overwrite.\n\n## End-to-End Test Plan\n\n| Test Case | Input | Expected Output |\n|-----------|-------|------------------|\n| Happy path | Valid events.csv and products.csv with matching product_ids | Parquet with all joined records |\n| Missing product_id | events.csv with null product_id | Record retained with null join result |\n| No matching products | events.csv with unknown product_ids | Records kept via left join, null product fields |\n| Empty events file | Zero-row events.csv | Empty Parquet partition created |\n| New column added | products.csv with extra 'discount' column | Column added to output (forward compat) |\n| Duplicate product_ids | products.csv with duplicate product_ids | Cross-join behavior, all combinations returned |\n| Schema evolution | product_id type changed from int to string | Handled via String cast |\n\n**Test automation**:\n1. Generate test data using localstack S3 mock.\n2. Run Glue job in dev endpoint with `--additional-python-files` for test fixtures.\n3. Query Athena against the output table and assert row counts and sample values.\n4. Use AWS Step Functions to orchestrate test pipeline with cleanup teardown.",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-8",
        "questionId": "q-19940",
        "question": "You operate a global ecommerce platform with high-velocity events. Design an end-to-end real-time CDC pipeline to maintain an upsertable data lake on S3, including schema evolution, PII masking, governance, and multi-region replication. Specify data formats, engines, and services (Kinesis, Glue, Lake Formation, Apache Hudi/Iceberg), partitioning, idempotency, and test strategy. How would you optimize cost and latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a region-scoped ingestion pipeline using S3 landing zones in both us-east-1 and eu-west-1. Use AWS Glue Spark jobs with a unified schema registry to read JSON and CSV sources, transform and write partitioned Parq…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an AWS Glue Spark job to read s3://data-logs/app-events/YYYY/MM/DD/events.json, flatten nested JSON fields, apply PII masking (hash emails, redact phone numbers), and write the processed data as Parquet files partit…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a real-time CDC lake using Kinesis Data Streams ingested by Spark on Glue or EMR, writing to S3 as Apache Hudi upsertable tables with partitioning by date and region. Use Glue Schema Registr",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Glue Spark job that reads from Kinesis, writes Parquet to S3 with automatic partitioning, and runs Deequ checks for completeness, distinct counts, and type drift. Validate per-batch, append resu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical mastery of real-time data lakes, CDC semantics, and governance in AWS. It probes your ability to choose formats (Hudi/Iceberg), schemas, security, and cross-region cost trade-offs.\n\n## Key Concepts\n\n- Real-time CDC with Kinesis vs Kafka\n- Upserts in S3 via Apache Hudi/Iceberg\n- Schema evolution with Glue Schema Registry\n- PII masking in transit and at rest, encryption\n- Governance via Lake Formation and Data Catalog\n- Cross-region replication and cost considerations\n- Idempotency with dedupe and WAL\n\n## Code Example\n\n```python\nfrom pyspark.sql import SparkSession\nspark = SparkSession.builder.getOrCreate()\ndf = spark.read.json('s3://bucket/raw/events/')\ndf.write.format('hudi').options(**{\n  'hoodie.datasource.write.recordkey.field': 'event_id',\n  'hoodie.datasource.write.precombine.field': 'ts',\n  'hoodie.datasource.write.table.type': 'MERGE_ON_READ'\n}).mode('append').save('s3://bucket/hoodie/events')\n```\n\n## Follow-up Questions\n\n- How would you test schema evolution changes without downtime?\n- How would you monitor CDC lag and data quality at scale?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-9",
        "questionId": "q-13802",
        "question": "Scenario: Two data sources emit daily JSON events to S3: sourceA at s3://lake/streamA/YYYY/MM/DD/events.json and sourceB at s3://lake/streamB/YYYY/MM/DD/events.json. Build a beginner-level ingestion using AWS Glue to land data as Parquet in s3://lake/warehouse/YYYY/MM/DD/source={A,B}/, partitioned by date and source, with Glue Data Catalog registration. Enforce idempotent writes, basic schema normalization for differing field sets, and a test plan that validates cross-source dedup and missing-field handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an AWS Glue Spark job to read NDJSON files from daily S3 paths (s3://partner-clicks/YYYY/MM/DD/events.ndjson), unify the evolving schemas into a single target schema, and write Parquet partitioned output to s3://dat…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use AWS Glue with a Spark job to read logs.json, flatten nested fields, explode arrays, and normalize to a single schema. Write as Parquet to s3://corp-processed/logs/YYYY/MM/DD/, partitioned by date.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed approach: Use a Glue Spark job to read both input paths, unify to a common schema, fill missing fields with nulls, and write Parquet to s3://lake/warehouse/YYYY/MM/DD/source=A or source=B/, p",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest via Kinesis Data Streams and Firehose, redact PII in a preprocessing Lambda, write Parquet nightly with daily partitioning, enable SSE-KMS, enable S3 Object Lock on the raw bucket, use Apache H",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes multi-source ingestion, partitioning by multiple keys, and basic schema normalization. It also tests practical idempotency and end-to-end testing in a beginner context.\n\n## Key Concepts\n\n- Multi-source Glue ETL with unified schema\n- Parquet format and multi-key partitioning (date, source)\n- Job bookmarks for idempotent writes\n- Glue Data Catalog registration and lightweight data quality tests\n\n## Code Example\n\n```javascript\n// Pseudo Glue ETL skeleton (JS-style for illustration)\n// 1) read from two sources\nlet dfA = readJson('s3://lake/streamA/xxxx/yyyy/MM/dd/events.json');\nlet dfB = readJson('s3://lake/streamB/xxxx/yyyy/MM/dd/events.json');\n// 2) normalize schemas to common schema\nlet unified = dfA.unionByName(dfB, true);\n// 3) write as Parquet with partitions\nunified.write.parquet('s3://lake/warehouse/yyyy/mm/dd/source={A,B}', {partitionBy: ['date','source']});\n```\n\n## Follow-up Questions\n\n- How would you extend this to handle schema evolution across sources?\n- What monitoring would you add to detect failed runs or duplicates in near real time?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-10",
        "questionId": "q-1883",
        "question": "Design a streaming data contract and quality workflow for events arriving from multiple teams via MSK and Kinesis. Use AWS Glue Schema Registry to enforce evolving Avro/JSON schemas with versioning and compatibility (backward/forward). Describe publishing schemas, validating payloads at ingest, storing Parquet in S3 with daily partitions, and monitoring for drift and quality across regions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest from MSK with Spark on Glue/EMR, use Glue DynamicFrame for schema evolution; mask PII per tenant in the ETL via Spark UDFs; write Parquet partitioned by date and tenant to S3; register in Glue",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest telemetry data through Kinesis Data Streams, process per-tenant anomaly scores within 5-minute windows using either Kinesis Data Analytics or Spark on EMR, then persist results as Parquet files in S3 with partiti…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Glue-based pipeline that reads daily raw logs from s3://data-lake/raw/logs/YYYY/MM/DD/logs.json, maps three schemas into a canonical schema (user_id string, timestamp timestamp, event string,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Glue Schema Registry with per-stream Avro/JSON schemas, versioned and backward/forward compatible. Producers send schemaId in headers; a validation step enforces the contract at ingest. Write to S",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical use of Glue Schema Registry, schema evolution governance, and cross-region data quality workflows. It also covers integration with streaming sources (MSK/Kinesis) and storage in S3, with error handling and drift monitoring.\n\n## Key Concepts\n\n- Glue Schema Registry and Avro/JSON\n- Schema versioning and compatibility (backward/forward)\n- Ingest validation and header schemaId\n- Parquet ingestion with date-based partitions\n- Glue Data Quality and CloudWatch drift alerts\n\n## Code Example\n\n```javascript\n// Pseudo-code: register and validate a schema, then publish event with header schemaId\n```\n\n## Follow-up Questions\n\n- How would you test schema evolution changes across producers without downtime?\n- How do you handle breaking changes in existing pipelines?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-11",
        "questionId": "q-2204",
        "question": "Design a real-world, multi-tenant, multi-account ingestion pipeline: Ingest JSON events from microservices via Kinesis Data Streams, store as Parquet in S3 with daily partitioning by tenant_id, and catalog with AWS Glue. Enforce tenant isolation with Lake Formation across accounts, handle schema evolution, embed data quality checks, and outline end-to-end testing strategies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Glue Spark job to read both CSVs, align schemas, and union into a single dataframe. Add a source column, deduplicate on (transaction_id, source), and write Parquet to s3://brand-warehouse/transa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "In a multi-tenant, multi-account data lake, ingest JSON events from microservices via Kinesis Data Streams, land to S3 as Parquet partitioned by date and tenant_id. Use Glue for ETL and cataloging; en",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Stream events through Kinesis Data Streams into Kinesis Firehose, which writes Parquet partitions to S3, partitioned by date. Use an AWS Glue Streaming job with the Glue Schema Registry to manage schema evolution. Apply…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Glue Spark job to read the daily CSV from S3, cast/validate fields, fill firmware_version with 'unknown', and drop duplicates by vehicle_id and ts. Enrich with DynamoDB vehicle_metadata, then wr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-account data governance, real-world multi-tenant isolation, and evolving schemas in a streaming-to-batch pipeline.\n\n## Key Concepts\n- Multi-account data governance with Lake Formation\n- Parquet partitioning by date and tenant_id\n- Glue ETL + Data Catalog with schema evolution\n- Data quality checks in ETL and streaming\n- End-to-end testing with synthetic tenants\n\n## Code Example\n```javascript\n// Pseudocode: ETL outline for Glue-like job (conceptual)\nasync function runETL(inputStream){\n  const batch = await readJSONEvents(inputStream)\n  const transformed = batch.map(e => ({...e, tenant_id: e.tenantId, date: toDate(e.ts)}))\n  await writeParquet(transformed, `/s3/bucket/tenant/date`, {partitionBy: ['tenant_id','date']})\n  await updateGlueCatalog(transformed)\n}\n```\n\n## Follow-up Questions\n- How would you implement schema evolution without breaking downstream jobs?\n- What monitoring and alerting would you add for data-skew or partition churn?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-12",
        "questionId": "q-10061",
        "question": "You receive a nightly JSONL file of IoT device telemetry arriving at s3://iot-logs/raw/telemetry/YYYY/MM/DD/*.json. Create a beginner-level pipeline to land data as Parquet in s3://iot-logs/processed/YYYY/MM/DD/, partitioned by date and device_id, using AWS Glue for ETL and cataloging. Include a basic schema inference strategy, a plan for handling missing fields, and a test plan to validate end-to-end ingestion?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Apache Iceberg on S3 with Glue Data Catalog. Ingest JSON via Firehose/Spark, flatten, then upsert into an Iceberg table partitioned by tenant_id and date. Leverage Iceberg schema evolution for cha",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Configure a daily batch: logs at s3://logs/app_usage/YYYY/MM/DD/logs.csv; Glue ETL flattens, normalizes fields, and deduplicates within the day, output Parquet to s3://data/app_usage/YYYY/MM/DD/. Use",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Glue Schema Registry with per-stream Avro/JSON schemas, versioned and backward/forward compatible. Producers send schemaId in headers; a validation step enforces the contract at ingest. Write to S",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Glue Spark job (Python) to read JSONL from S3, safely cast fields (device_id string, ts timestamp, reading double), fill missing with defaults, and write Parquet to s3://iot-logs/processed/YYYY/",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical data ingestion basics: using Glue to transform JSONL, handle missing fields, partitioning, cataloging, and a simple QA/test plan, all from raw IoT telemetry.\n\n## Key Concepts\n- AWS Glue Spark jobs (Python)\n- Parquet partitioning by date and device_id\n- Schema inference and defaulting for missing fields\n- Glue Data Catalog and simple tests\n\n## Code Example\n```python\nimport sys\nfrom awsglue.transforms import *\nfrom awsglue.utils import getResolvedOptions\nfrom awsglue.context import GlueContext\nfrom pyspark.context import SparkContext\n\nsc = SparkContext()\nglueContext = GlueContext(sc)\ninput_path = \"s3://iot-logs/raw/telemetry/YYYY/MM/DD/*.json\"\ndf = glueContext.spark_session.read.json(input_path, multiLine=False)\n# Basic casting and defaults\ndf2 = df.selectExpr(\n  \"cast(device_id as string) as device_id\",\n  \"cast(ts as timestamp) as ts\",\n  \"coalesce(reading, 0.0) as reading\"\n)\n# Derive date partitions\ndf3 = df2.withColumn(\"year\", df2.ts.year).withColumn(\"month\", df2.ts.month).withColumn(\"day\", df2.ts.day)\nout_path = \"s3://iot-logs/processed/YYYY/MM/DD/\"\ndf3.write.partitionBy(\"year\", \"month\", \"day\", \"device_id\").parquet(out_path)\n```\n\n## Follow-up Questions\n- How would you detect schema drift across days and adjust the crawler?\n- How would you automate end-to-end tests for new partitions?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-13",
        "questionId": "q-14946",
        "question": "Scenario: IoT devices emit nested JSON with optional fields. Ingest via Kinesis Data Streams and build a near-real-time pipeline to flatten to Parquet in S3 with hourly partitions, cataloged in Glue Data Catalog. Design for schema evolution, deduplication, and late data handling; specify services, formats, testing, and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a beginner-level AWS data pipeline that ingests a daily JSON events file from s3://org-raw/events/YYYY/MM/DD/events.json, validates against a fixed required schema, and performs simple quality checks (non-null id…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Kinesis Data Streams with AWS Glue Streaming (Spark) to flatten nested JSON in real time, write Parquet to S3 with hourly partitions, and catalog in Glue Data Catalog. Deduplicate using a composite key of device_id…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Apache Iceberg tables on S3 with the AWS Glue Data Catalog. Ingest data via Spark jobs on AWS Glue or EMR that perform upserts and deletes, ensuring proper tombstone emission for delete operations. Partition the dat…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Lineage-aware pipeline: ingest raw JSON to S3, Glue Spark ETL to Parquet in partitioned folders with a provenance table (raw_id, event_ts, source, transforms, version). Publish engineered features to",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses end-to-end streaming ingestion design, including handling nested JSON, schema evolution, deduplication, and late data handling while ensuring testability and recoverability.\n\n## Key Concepts\n\n- **Streaming ETL**: AWS Glue Spark Streaming for real-time data transformation\n- **Storage Format**: Parquet in S3 with hourly partitions for optimized query performance\n- **Data Catalog**: Glue Data Catalog for schema management and schema evolution support\n- **Deduplication**: Composite key strategy (device_id + event_timestamp) with DynamoDB for state lookup\n- **Late Data Handling**: Watermarking with event time windows and configurable reprocessing window\n- **Testing Strategy**: Unit tests for transformation logic, integration tests for pipeline flow, and end-to-end tests with mock data\n- **Rollback Procedures**: Version-controlled Glue table schemas, S3 object versioning, and replay capability via Kinesis retention window",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-14",
        "questionId": "q-16117",
        "question": "You receive daily service usage logs in TSV format with a header that occasionally adds a column. The data lands in s3://saas-logs/raw/YYYY/MM/DD/logs.tsv. Design a beginner-level ingestion pipeline using AWS services to convert TSV to Parquet, partition by date and service_name, register in Glue Data Catalog, and implement schema evolution so new columns don't fail runs. Include simple data quality checks and a minimal end-to-end test plan; explain idempotency considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Step Functions to orchestrate per-tenant Glue Spark jobs that read batch CSVs and streaming JSON from S3, normalize to Parquet, and writes to Apache Iceberg tables on S3 partitioned by tenant/date",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Glue Spark job (Python) to read TSV, handle evolving headers via DynamicFrame.resolveChoice, and write Parquet partitioned by date and service_name to s3://saas-logs/processed/YYYY/MM/DD/service",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Land daily CSVs via AWS Transfer Family to S3 raw, then a Glue Spark job to cast types, write Parquet to curated with date partitions. Crawlers update the Data Catalog; enabled schema evolution to acc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Land streaming JSON events to s3/raw/{tenant_id}/{YYYY}/{MM}/{DD}. Use a Glue Streaming ETL job to read from Kinesis or Kafka, leveraging Glue Schema Registry to handle schema evolution and capture schema drift. Write P…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical, beginner-friendly exposure to AWS data ingestion workflows with schema evolution and idempotency.\n\n## Key Concepts\n- AWS Glue Spark jobs, DynamicFrame, schema evolution with resolveChoice\n- Parquet, partitioning by date and service_name\n- Glue Data Catalog, bookmarks for idempotency\n- Lightweight data quality checks and end-to-end testing\n\n## Code Example\n```python\nfrom awsglue.context import GlueContext\nfrom pyspark.context import SparkContext\nfrom awsglue.transforms import *\n\nsc = SparkContext()\nglueContext = GlueContext(sc)\nspark = glueContext.spark_session\n\ndf = spark.read.csv(\"s3://saas-logs/raw/YYYY/MM/DD/logs.tsv\", sep=\"\\t\", header=True, inferSchema=True)\ndf = df\n  .withColumnRenamed(\"date\", \"event_date\")\n# schema evolution handled by DynamicFrame when writing\n```\n\n## Follow-up Questions\n- How would you test idempotency for partial successes?\n- How would you modify to handle multiple tenants with per-tenant partitions?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-15",
        "questionId": "q-10687",
        "question": "Design an end-to-end, multi-region streaming ingestion pipeline for a new financial transactions feed arriving as JSON via 3 regional Kinesis Data Streams. Provide exactly-once semantics, deduplication with DynamoDB, landing in S3 as Parquet partitioned by date and region, Glue catalog with schema evolution, and data quality checks. Address late-arriving data, drift, testing, and governance implications. Include a diagram?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kinesis Data Streams for ingestion; land raw events in S3 as Parquet with daily partitions and Glue catalog. Run Apache Hudi on Glue Spark jobs to upsert into target Parquet, enabling dedup and la",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Spark Structured Streaming (Glue or EMR) to read from Kinesis Data Streams, write to an Iceberg table on S3 with daily partitions, register in Glue Catalog, enable schema evolution with MERGE operations for upserts,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a pipeline: IoT devices emit evolving JSON to S3. AWS Glue Spark ETL jobs flatten nested JSON to Parquet, partitioned by date and device_id. Implement idempotent upserts using a deduplication table with a commit…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-region Kinesis Data Streams feeding into Kinesis Firehose to S3 in Parquet, partitioned by date and region. Deduplicate with a DynamoDB registry and idempotent producers to achieve effectively",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests multi-region streaming design, exactly-once semantics, dedup, schema evolution, and data quality in a realistic data-lake context.\n\n## Key Concepts\n- Exactly-once / idempotent processing\n- Multi-region streaming and Parquet partitioning\n- Glue Data Catalog, schema evolution\n- Data quality checks and late-arriving data\n- Testing, drift handling, governance implications\n\n## Code Example\n\n```javascript\n// Pseudo dedup check using DynamoDB (high level)\nasync function isDuplicate(id) {\n  // put/get item in DynamoDB id registry; return true if exists\n}\n```\n\n## Follow-up Questions\n- How would you scale the dedup registry to millions of transactions per second?\n- How would you monitor late-arriving data and adjust partitions automatically?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-16",
        "questionId": "q-8887",
        "question": "Scenario: Weekly vendor CSVs arrive to s3://vendor-data/raw/ on Friday evenings with columns order_id, customer_id, amount, currency, ts; occasional files add a new column 'discount'. Design a beginner-level pipeline to convert to Parquet, store in s3://vendor-data/processed/YYYY/MM/DD/, partitioned by date, and register in Glue. Include schema evolution (adding discount), dedup on order_id, data quality checks (not null order_id, amount >= 0), and a minimal end-to-end test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would design a Spark-based AWS Glue job that reads the JSON events, flattens nested structures using Spark's built-in functions or lateral view Explode, deduplicates on a composite key (partner_event_id + ingest_ts),…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Streaming design: Kinesis Data Streams feeds a Glue Spark job (or Kinesis Data Analytics) that writes Parquet to S3 with daily partition by trade_date and tenant_id. Catalog via Glue Data Catalog. Imp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage AWS Glue Data Catalog with Lake Formation, store data in S3 using Apache Iceberg for upserts and time travel, orchestrate with Step Functions, transform in Glue Spark jobs, validate quality v",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Glue-based ETL pipeline: crawl raw CSVs to infer schema; a Spark job reads CSVs, deduplicates by order_id, fills missing discount values with 0, validates non-null order_id and amount >= 0, casts amount to d…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests the ability to implement a practical, beginner-friendly ingestion pipeline that handles schema evolution, deduplication, data quality, and catalog integration using AWS Glue.\n\n## Key Concepts\n- AWS Glue Crawlers and Jobs for schema inference and ETL\n- Parquet storage with date partitioning (YYYY/MM/DD)\n- Schema evolution: tolerate added columns (discount) without failing\n- Data quality checks: non-null keys, valid numeric ranges\n- Idempotent runs via job bookmarks and proper deduplication\n\n## Code Example\n```javascript\n// Pseudocode: deduplicate by order_id (illustrative)\ndf = spark.read.csv('s3://vendor-data/raw/')\ndf = df.dropDuplicates(['order_id'])\ndf = df.withColumn('discount', coalesce(col('discount'), lit(0)))\ndf = df.filter(col('order_id').isNotNull() && col('amount') >= 0)\n```\n\n## End-to-End Test Plan\n1. Upload sample CSV with and without discount column\n2. Verify Glue crawler detects schema correctly\n3. Run ETL job and confirm Parquet files in correct partition structure\n4. Validate data quality checks reject invalid records\n5. Confirm table registered in Glue with proper schema",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-17",
        "questionId": "q-26216",
        "question": "Design a streaming ingestion pipeline: JSON telemetry arrives in Kinesis Data Streams; a Glue streaming job validates against Glue Schema Registry, writes Parquet to S3 partitioned by day, and updates Iceberg tables in the Data Catalog. Enforce lineage, data quality gates, and late-data handling with watermarks and backfill; outline test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Kinesis Data Streams -> Glue streaming job validates against Glue Schema Registry, writes Parquet to S3 partitioned by day, and updates Iceberg tables in the Glue Data Catalog. Enforce lineage via the",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest via Kinesis Data Streams; Glue Spark parses JSON, masks PII, and writes Parquet to S3 partitioned by tenant_id/date. Catalog with Glue; enable cross-account Lake Formation grants for Snowflake/",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Firehose to stream events to S3 as Parquet partitioned by region/date. Use Glue ETL (Spark) to mask PII per column, and Lake Formation for cross-account access with column-level permi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a pipeline: IoT devices emit evolving JSON to S3. AWS Glue Spark ETL jobs flatten nested JSON to Parquet, partitioned by date and device_id. Implement idempotent upserts using a deduplication table with a commit…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical streaming ingestion design, schema evolution, and data governance across accounts in AWS.\n\n## Key Concepts\n- Kinesis Data Streams and Glue streaming ETL\n- Glue Schema Registry for evolving schemas\n- Parquet on S3 with daily partitions; Iceberg tables in Glue Catalog\n- End-to-end lineage, data quality gates, and late-data handling with watermarks\n\n## Code Example\n```python\n# Skeleton: Glue streaming job pseudocode\nimport sys\n# connect to KDS, register schema, read stream, validate, write Parquet to S3, update Iceberg\n```\n\n## Follow-up Questions\n- How would you implement schema evolution compatibility and backward-compat tests?\n- How would you monitor and alert on data skew or late-arriving partitions?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-18",
        "questionId": "q-8418",
        "question": "You have a daily CSV feed arriving at s3://data/raw/sales/YYYY/MM/DD/*.csv. Build a beginner-friendly ingestion pipeline that converts to Parquet, partitions by date, stores to s3://data/warehouse/sales/YYYY/MM/DD/, and registers in Glue Data Catalog. Then configure AWS Lake Formation to grant a read-only analytics role access to the dataset with minimal permissions, and add a simple audit log entry for each run. Outline services, formats, partitioning, cataloging, and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To satisfy these requirements: Use a two-account Lake Formation data lake. Ingest daily CSVs with a Glue ETL job (Python/DynamicFrame) that reads CSV, applies drift-tolerant schema evolution (merges new columns into exi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage AWS Glue Spark jobs with Apache Hudi to upsert JSON events from three SaaS vendors into a single Parquet dataset in S3, partitioned by date and vendor. Use Glue Data Catalog and Lake Formatio",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest from MSK to Spark Streaming, write to Iceberg tables on S3 with daily partitions; implement upserts/deletes using Iceberg MERGE; enable schema evolution via Iceberg schema migrations and Glue C",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Glue ETL pipeline that triggers daily via EventBridge to process CSV files from s3://data/raw/sales/YYYY/MM/DD/*.csv, converting them to Parquet format with date-based partitioning in s3://data/warehouse/sal…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical skills in end-to-end AWS data ingestion, cataloging, and access control, plus basic auditing. It ensures familiarity with Glue ETL, Parquet partitioning, Lake Formation permissions, and lightweight test plans.\n\n## Key Concepts\n- AWS Glue ETL jobs and data catalog\n- Parquet format and date partitioning\n- Glue Data Catalog table schemas\n- AWS Lake Formation least-privilege permissions\n- End-to-end test design and basic auditing\n\n## Code Example\n```javascript\n// Outline: Spark job structure for Glue (pseudocode)\nimport com.amazonaws.services.glue.GlueContext\nimport com.amazonaws.services.glue.util.Job\nimport com.amazonaws.services.glue.DynamicFrame\n\nval glueContext = new GlueContext(sparkSession)\nval datasource = glueContext.getCatalogSource(database = \"sales_db\", tableName = \"raw_sales\")\nval transformed = datasource.applyMapping(mappings)\nval sink = glueContext.getSinkWithFormat(connectionType = \"s3\", options = Map(\"path\" -> \"s3://data/warehouse/sales/\"), format = \"parquet\")\nsink.writeDynamicFrame(transformed)\n```",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-19",
        "questionId": "q-29545",
        "question": "You manage a two-account AWS data lake and ingest nightly clickstream JSON events from an external platform into S3 as daily Parquet partitions (YYYY/MM/DD). Design an end-to-end pipeline that (1) validates evolving JSON schema against Glue Schema Registry with backward/forward compatibility, (2) performs idempotent upserts into the partitioned Parquet data to accommodate late-arriving events, (3) preserves end-to-end lineage via Glue Data Catalog and S3 versioning, and (4) includes data quality gates, testing, and a rollback plan if a breaking schema arrives. Include concrete services, schemas, and testing steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an AWS Glue Spark job to read NDJSON files from daily S3 paths (s3://partner-clicks/YYYY/MM/DD/events.ndjson), unify the evolving schemas into a single target schema, and write Parquet partitioned output to s3://dat…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Debezium CDC to MSK, Glue Spark to write to Iceberg on S3 partitioned by date. Upserts/deletes use Iceberg tombstones and rewrite; evolving schemas via Iceberg schema evolution. Integrate Gre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end pipeline for a daily multi-tenant JSON feed. Ingest to S3 with partitions by date and tenant_id, convert to Parquet, and register in the Glue Data Catalog. Use Glue ETL to normali",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Glue Schema Registry to enforce backward/forward compatibility, Spark-based upserts in Glue ETL to merge late-arriving events into daily Parquet partitions using a version or sequence column for i",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical mastery of schema evolution, idempotent ingest, and cross-account governance in AWS data lakes. It ensures concrete use of Glue Schema Registry, Spark ETL patterns, and lineage controls.\n\n## Key Concepts\n- Glue Schema Registry for compatible evolution\n- Spark-based upsert/merge into Parquet per partition\n- Versioned keys for idempotence and late data handling\n- Glue Data Catalog + S3 object/versioning for lineage\n- Lake Formation for cross-account access and governance\n- Data quality gates and end-to-end testing\n\n## Code Example\n```javascript\n// Pseudocode: upsert in Glue ETL (PySpark-like)\nval newData = spark.read.json(\"s3://..../raw/date=\")\nval existing = spark.read.parquet(\"s3://..../data/date=\")\nval merged = existing.alias(\"e\").join(newData.alias(\"n\"), Seq(\"id\"), \"outer\")\n  .selectExpr(\"coalesce(n.id, e.id) as id\", \"coalesce(n.value, e.value) as value\", \"n.version as ver\")\nmerged.write.mode(\"overwrite\").partitionBy(\"date\").parquet(\"s3://..../data/date=\")\n```\n\n## Follow-up Questions\n- How would you handle schema evolution between producers and registry while minimizing downtime?\n- How would you validate rollback and ensure consumers see consistent results during a schema upgrade?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-20",
        "questionId": "aws-data-engineer-data-ingestion-1776520270890-2",
        "question": "You manage a multi-stage ingestion pipeline: collect logs into S3, run a Glue ETL job to normalize data, and load results into Redshift. You need idempotent loads, reliable retries, and a checkpoints mechanism to resume after failures. Which pattern best satisfies these requirements?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed solution: Ingest from KDS to S3 via a Glue streaming ETL or Firehose to Parquet, with daily partitions. Use Glue Schema Registry to version and evolve the nested JSON schema; keep tx_timestam",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an AWS Glue ETL job (PySpark) to read events.json from s3://ext-prod/events/raw/YYYY/MM/DD/, validate against a predefined JSON schema, flatten nested structures, and write partitioned Parquet files to s3://ext-p…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use AWS Step Functions to orchestrate the workflow with retries and a manifest-based checkpoint",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest regional JSON events into a staging S3 bucket via Kinesis/Glue, then upsert into Apache Iceberg tables on S3, partitioned by tenant/date. Enforce per-tenant isolation with Lake Formation or IAM",
            "isCorrect": false
          }
        ],
        "explanation": "## Correct Answer\nA: Use AWS Step Functions to orchestrate the workflow with retries and a manifest-based checkpoint\n\nStep Functions provide structured orchestration with explicit retry policies, catch blocks, and a persisted state. A manifest file can track already-processed batches, enabling idempotent loads and resumable progress after failures.\n\n## Why Other Options Are Wrong\n- B: A single Glue job lacks global orchestration, making retries across stages brittle and hard to guarantee idempotence.\n- C: Alarms alone do not automate recovery; manual interventions slow down recovery and risk human error.\n- D: A purely event-driven Lambda sequence without state leads to race conditions and duplicate processing without a reliable checkpoint.\n\n## Key Concepts\n- AWS Step Functions, Glue ETL, idempotent data loads, checkpointing\n\n## Real-World Application\n- Use when building reliable, auditable data pipelines that must recover gracefully from transient failures and avoid duplicate loads in analytics platforms like Redshift.\n",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-21",
        "questionId": "q-7959",
        "question": "Design an end-to-end pipeline to ingest high-volume IoT telemetry where devices emit protobuf payloads into AWS; propose a streaming path using Kinesis, a Spark/Glue ETL to Parquet in S3 partitioned by day, handle protobuf schema evolution via Glue Schema Registry, ensure idempotent processing and data-quality checks, and outline verification and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "This approach uses AWS DMS for CDC from on-prem Postgres to S3, then a Glue Spark job with Apache Hudi on S3 to upsert into a partitioned Parquet dataset. Include a staging layer for late data, manage",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Kinesis Data Streams to ingest protobuf payloads, decode them with Glue Schema Registry, and run a Glue Spark streaming job to parse and write Parquet to S3 partitioned by date. Manage protobuf schema evolution thro…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design an incremental pipeline that ingests daily JSON.gz files from s3://ride-events/raw/date/*.json.gz, converts them to Parquet, and writes to an Iceberg table on S3 partitioned by date, registered in the Glue Data C…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "MSK regional topics feed into a Glue Spark streaming ETL that flattens JSON, masks PII, and writes Parquet to s3://telemetry/{tenant}/{YYYY}/{MM}/{DD}/ with daily partitions. Apply Lake Formation per-",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of protobuf ingestion, schema evolution, and end-to-end testing in AWS data lakes.\n\n## Key Concepts\n- Streaming ingestion, protobuf decoding, Glue Schema Registry, Parquet partitioning\n- Idempotent processing, upserts, data quality checks\n- End-to-end testing, cost awareness\n\n## Code Example\n```python\n# Pseudocode: Spark Structured Streaming in Glue to decode protobuf and write Parquet\nfrom pyspark.sql.functions import col\ninput_df = spark.readStream.format(\"kinesis\").option(\"streamName\",\"telemetry\").load()\ndecoded = input_df.withColumn(\"payload\", decode_protobuf(col(\"data\")))\nquality_df = decoded.filter(validate_data_quality(col(\"payload\")))\nquality_df.writeStream.format(\"parquet\").partitionBy(\"date\").option(\"path\",\"s3://telemetry/\").start()\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-22",
        "questionId": "q-23751",
        "question": "Ingest a daily nested JSON log at s3://corp-logs/input/YYYY/MM/DD/logs.json. Design a beginner-level AWS pipeline to flatten to a single schema, store Parquet at s3://corp-processed/logs/YYYY/MM/DD/, partitioned by date, and register in Glue Data Catalog. Include a simple schema-evolution policy, basic data quality checks, and late-data handling/testing plan. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Spark Structured Streaming (Glue or EMR) to read from Kinesis Data Streams, write to an Iceberg table on S3 with daily partitions, register in Glue Catalog, enable schema evolution with MERGE operations for upserts,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use AWS Glue with a Spark job to read logs.json, flatten nested fields, explode arrays, and normalize to a single schema. Write as Parquet to s3://corp-processed/logs/YYYY/MM/DD/, partitioned by date.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a Glue ETL job to read JSON from s3://bucket/raw/app-logs, flatten device fields, and write Parquet to s3://bucket/curated/app-logs/yyyy/mm/dd with daily partitions. Enforce schema: userId string,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Glue Spark job triggered on new objects or a daily run. Read the gzip CSV from S3, normalize headers, cast types (order_id as string, amount as double, date as timestamp), drop bad rows, dedupli",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical data wrangling, schema evolution, and end-to-end testing in a beginner pipeline.\n\n## Key Concepts\n- Flattening nested JSON to a stable schema\n- Parquet partitioning and Glue catalog integration\n- Idempotent processing and late data handling\n- Basic data quality checks\n\n## Code Example\n```python\nfrom pyspark.sql.functions import explode, col\n\ndf = spark.read.json(\"s3://corp-logs/input/YYYY/MM/DD/logs.json\")\nflat = df.selectExpr(\"id\", \"timestamp\", \"payload.*\")\nflat = flat.withColumn(\"items\", explode(col(\"payload.items\")))\nflat.write.format(\"parquet\").mode(\"overwrite\").partitionBy(\"date\").save(\"s3://corp-processed/logs/YYYY/MM/DD/\")\n```\n\n## Follow-up Questions\n- How would you verify that late-arriving data does not violate partition integrity?\n- What minimal monitoring would you add to catch schema drift over time?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-23",
        "questionId": "q-28495",
        "question": "Scenario: A partner delivers daily CSV logs via SFTP into S3; path is s3://partners-logs/raw/YYYY/MM/DD/logs.csv. Design a beginner-level AWS data pipeline that uses AWS Transfer Family to ingest, deduplicates against a per-day manifest in S3, flattens headers and normalizes data types, converts to Parquet, partitions by date, and registers in Glue Data Catalog. Also emit a daily audit manifest with row counts. Describe your end-to-end testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kinesis Data Firehose to stream events to S3 as Parquet partitioned by region/date. Use Glue ETL (Spark) to mask PII per column, and Lake Formation for cross-account access with column-level permi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a multi-tenant ingestion: land JSON events to s3://lake/ingest/{tenant}/{source}/{date}/, store as Iceberg-parquet in S3, and catalog with Glue. Enforce a data contract via Glue Schema Registry",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest daily partner CSVs via AWS Transfer Family to s3://partners-logs/raw/YYYY/MM/DD/logs.csv, then run a Glue job that deduplicates against a per-day manifest in S3, normalizes headers and casts ty",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Architect a real-time CDC lake using Kinesis Data Streams ingested by Spark on Glue or EMR, writing to S3 as Apache Hudi upsertable tables with partitioning by date and region. Use Glue Schema Registr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests a beginner-friendly, end-to-end data ingestion pattern that involves external file landing, deduplication provenance, schema normalization, Parquet storage, cataloging, and basic observability.\n\n## Key Concepts\n\n- AWS Transfer Family for SFTP landings\n- Per-day manifests for deduplication and provenance\n- Header normalization and type casting in Glue\n- Parquet with date partitioning\n- Glue Data Catalog registration\n- Basic audit manifests for row counts\n\n## Code Example\n\n```python\n# Skeleton Glue PySpark for minimal ingest\nfrom pyspark.context import SparkContext\nfrom awsglue.context import GlueContext\nfrom awsglue.job import Job\n\nsc = SparkContext()\nglueContext = GlueContext(sc)\nspark = glueContext.spark_session\n# Placeholder: read CSVs from the landing path, apply schema, dedupe using per-day manifest, write Parquet to warehouse, and write manifest\n```\n\n## Follow-up Questions\n\n- How would you handle adding new columns (schema evolution) while preserving idempotence?\n- How would you monitor SLA and alert on ingest delays or failures with CloudWatch and Glue metrics?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-24",
        "questionId": "q-1785",
        "question": "Two-account AWS data lake (us-east-1 and eu-west-1) ingests partner JSON events via API into S3. Design an end-to-end pipeline using Glue for ETL and cataloging, store Parquet with daily partitions by country/date, enable Athena queries with Lake Formation access controls, and handle schema evolution, data quality, late-arrivals, and cross-region replication trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Apache Hudi on Glue Spark to enable upserts and deletes on a daily partitioned Parquet lake table in S3. Prefer MERGE_ON_READ for late-arriving data; partition by date and region to optimize query performance and pa…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a pipeline using Glue streaming for Kinesis JSON and a daily Glue/Spark batch for CSVs, writing Parquet to S3 with daily partitions. Use Apache Iceberg (or Glue table type) for upserts, enable",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Land partner JSON events into S3 landing bucket, run a Glue ETL to convert to Parquet, partition by country and date, and catalog with Glue. Use Lake Formation for fine‑grained access. Implement schem",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest with Kinesis to S3 Parquet (daily partitions). Use Glue ETL to enforce schema evolution and register schemas in Glue Data Catalog. Build offline features in Snowflake on top of Parquet tables;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a cross-account, cross-region data pipeline with end-to-end data quality and governance considerations. It emphasizes real-world friction points like schema drift, late-arrival data, and access controls.\n\n## Key Concepts\n\n- AWS Glue ETL and Data Catalog\n- Parquet, partition pruning (country/date)\n- Lake Formation access controls and sharing\n- Schema evolution and Glue Schema Registry\n- Data quality checks and late-arrival handling\n- Cross-region replication implications (latency, consistency, cost)\n\n## Code Example\n\n```python\nfrom awsglue.context import GlueContext\nfrom pyspark.context import SparkContext\nfrom pyspark.sql.functions import to_date, col\n\nsc = SparkContext.getOrCreate()\nglueContext = GlueContext(sc)\ndf = glueContext.read.json(\"s3://landing-bucket/partner-events/\")\ndf = df.withColumn(\"date\", to_date(col(\"event_time\"))).select(\"country\", \"date\", *df.columns)\ndf.write.partitionBy(\"country\", \"date\").mode(\"append\").parquet(\"s3://curated-bucket/parquet/partner-events/\")\n```\n\n## Follow-up Questions\n\n- How would you test end-to-end with backfills for the last 7 days?\n- How would you automate schema drift detection and backfill compatibility?\n- What Lake Formation grants would you set for analysts vs admins, across accounts?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-02T10:28:04.801Z",
    "version": 1
  }
]