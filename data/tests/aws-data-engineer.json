[
  {
    "id": "test-aws-data-engineer",
    "channelId": "aws-data-engineer",
    "channelName": "AWS Data Engineer",
    "title": "AWS Data Engineer Knowledge Test",
    "description": "Test your understanding of AWS Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "aws-data-engineer-q-0",
        "questionId": "q-17309",
        "question": "You ingest daily healthcare claims as NDJSON to S3. Build a beginner-level AWS Glue-based pipeline to flatten nested fields, enforce a fixed schema (claim_id, patient_id, service_code, amount, claim_date), and write Parquet to a partitioned path s3://health-processed/claims/YYYY/MM/DD/. Implement per-record validation: skip invalid records but log them to s3://health-logs/claims/YYYY/MM/DD/bad_records.json. Include a minimal end-to-end test that injects a bad line and verifies a failure-free run and a bad-record log?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Kinesis Data Streams and Firehose, redact PII in a preprocessing Lambda, write Parquet nightly with daily partitioning, enable SSE-KMS, enable S3 Object Lock on the raw bucket, use Apache H",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use DMS for initial load and CDC to S3 Parquet, partitioned by date; trigger a Glue cataloging job to update the Data Catalog. Ensure idempotency by deduplicating on event_id and persisting the max LS",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Read NDJSON from s3://health/raw/claims/YYYY/MM/DD/, flatten and enforce schema with Glue, write Parquet to s3://health-processed/claims/YYYY/MM/DD/ partitioned by date. For records missing required f",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Architect a solution using OpenLineage events emitted by Glue and Spark jobs, store lineage graph in a central catalog (Glue Data Catalog + OpenLineage backend) per account, propagate lineage across a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nIntroduce a practical NDJSON-to-Parquet pipeline with per-record validation for a real-world data source. It highlights schema enforcement, partitioning, and lightweight testing in a beginner-friendly way.\n\n## Key Concepts\n- NDJSON ingestion in Glue\n- DynamicFrame to DataFrame mapping and schema enforcement\n- Parquet partitioning by date\n- Per-record validation and bad-record logging\n- Lightweight end-to-end testing\n\n## Code Example\n```javascript\n// Glue job outline (pseudo)\nreadNDJSON = glueContext.create_dynamic_frame.from_options(\n  format_options={\"multiline\": False},\n  connection_type=\"s3\",\n  format=\"ndjson\",\n  connection_options={\"paths\": [\"s3://health/raw/claims/YYYY/MM/DD/\"]}\n)\nflattened = readNDJSON.flatMap(lambda r: flatten(r))\nschema = StructType([StructField(\"claim_id\", StringType(), False),\n                     StructField(\"patient_id\", StringType(), False),\n                     StructField(\"service_code\", StringType(), False),\n                     StructField(\"amount\", DecimalType(10,2), False),\n                     StructField(\"claim_date\", DateType(), False)])\nvalid = flattened.filter(lambda x: all([x.get(k) is not None for k in [\"claim_id\",\"patient_id\",\"service_code\",\"amount\",\"claim_date\"]]))\nbad = flattened.filter(lambda x: any([x.get(k) is None for k in [\"claim_id\",\"patient_id\",\"service_code\",\"amount\",\"claim_date\"]]))\nwriteParquet = valid.write.parquet(\"s3://health-processed/claims/YYYY/MM/DD/\", partitionBy=[\"claim_date\"])\nlogBad = bad.write.json(\"s3://health-logs/claims/YYYY/MM/DD/bad_records.json\")\n```\n\n## Follow-up Questions\n- How would you handle schema evolution for new fields?\n- How would you extend testing to cover partial failures and retries?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-1",
        "questionId": "q-1543",
        "question": "Design an event-driven data platform to ingest order and driver events from regional services into a centralized data lake for near-real-time analytics. Data arrives as evolving JSON schemas; must handle schema evolution, efficient partitioning, cross-account access, and cost. Which AWS services and data design patterns would you implement, and how would you validate data quality and enable fast ad-hoc queries?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kinesis Data Streams for event ingestion, a Glue streaming job with Glue Schema Registry to normalize evolving JSON schemas, and write Parquet to S3 partitioned by date/region/service. Employ Lake Formation for fine…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage Kinesis Data Streams with a Glue Structured Streaming (Glue 4.0) job to read JSON and write Parquet to S3, partitioned by tenant_id and date. Use Glue Schema Registry for per-tenant schema ev",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run a Glue Crawler on the S3 path to automatically update the Data Catalog with new fields",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage Kinesis Data Firehose for streamlined JSON event ingestion into S3 with dynamic partitioning by device_type and date. A Glue Spark job transforms data to Parquet format, updates the Data Catalog, and handles sc…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExamines real-time data ingestion, schema evolution, cross-account governance, and cost-aware storage patterns in a practical, production context.\n\n## Key Concepts\n\n- Streaming ingestion: Kinesis Data Streams + Glue Schema Registry\n- Real-time normalization: Glue Streaming or Kinesis Data Analytics\n- Storage design: Parquet on S3 with partitioning by date/region/service\n- Governance: Lake Formation fine-grained access, Glue Data Catalog, drift monitoring\n- Quality & cost: deduplication, compaction, managed small files\n\n## Code Example\n\n```python\n# Glue Spark job snippet for schema evolution and data quality\nfrom awsglue.context import GlueContext\nfrom awsglue.job import Job\nfrom pyspark.sql import functions as F\nfrom pyspark.sql.types import StructType\n\n# Initialize Glue context\nargs = getResolvedOptions(__SYS__, ['JOB_NAME'])\nsc = SparkContext()\nglue_context = GlueContext(sc)\nspark = glue_context.spark_session\n\n# Read from Kinesis with schema registry\ndynamic_frame = glue_context.create_dynamic_frame.from_options(\n    connection_type=\"kinesis\",\n    connection_options={\n        \"streamARN\": \"arn:aws:kinesis:region:account:stream/order-events\",\n        \"classification\": \"json\",\n        \"endpointUrl\": \"https://kinesis.region.amazonaws.com\"\n    },\n    transformation_ctx=\"kinesis_source\"\n)\n\n# Apply schema evolution and data quality checks\ndf = dynamic_frame.toDF()\n\n# Data quality validation\ndf = df.filter(\n    F.col(\"order_id\").isNotNull() &\n    F.col(\"timestamp\").isNotNull() &\n    F.col(\"region\").isNotNull()\n)\n\n# Write optimized Parquet to S3\ndf.write.mode(\"append\").partitionBy(\n    \"date\", \"region\", \"service_type\"\n).parquet(\"s3://data-lake/orders/\")\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-2",
        "questionId": "q-2060",
        "question": "Design a multi-region, streaming-plus-batch ingestion pipeline for a mobile app that emits JSON events at high volume. Ingest via Kinesis Data Streams to S3 Parquet using Apache Iceberg tables with a Glue catalog, supporting upserts, deletes, and schema evolution. Include multi-source CDC, data quality checks, governance via Lake Formation, and end-to-end testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Glue Spark job with Job bookmarks to read new CSVs from s3://data-lake/sourceD/raw/YYYY/MM/DD/*.csv, convert to Parquet, and write to curated with partitions by date and producer. Register in Gl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a multi-account, multi-region pipeline ingesting streaming JSON and batch CSV; land as Parquet partitioned by date and tenant; catalog with Glue Data Catalog; enforce tenant-scoped access and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Iceberg tables on S3 with a Glue catalog, landing JSON from Kinesis via a streaming job that MERGEs into Iceberg to support upserts/deletes; replicate CDC from multiple sources using DMS across regions with sch…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest daily partner CSVs via AWS Transfer Family to s3://partners-logs/raw/YYYY/MM/DD/logs.csv, then run a Glue job that deduplicates against a per-day manifest in S3, normalizes headers and casts ty",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes design for scalable, auditable data lakes across regions, balancing mutating events with schema changes and governance.\n\n## Key Concepts\n- Apache Iceberg on S3 with Glue catalog\n- Upserts/deletes via MERGE semantics\n- Multi-source CDC (DMS) across regions\n- Schema evolution and data quality checks (Deequ)\n- Lake Formation governance and end-to-end testing\n\n## Code Example\n```javascript\nMERGE INTO iceberg_db.events AS t\nUSING staging.events AS s\nON t.event_id = s.event_id\nWHEN MATCHED THEN UPDATE SET t.payload = s.payload, t.ts = s.ts\nWHEN NOT MATCHED THEN INSERT (event_id, payload, ts) VALUES (s.event_id, s.payload, s.ts)\n```",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-3",
        "questionId": "q-5191",
        "question": "Scenario: A new data source streams JSON logs into Kinesis Data Streams at high velocity. Design an end-to-end pipeline to land enriched data in S3 as Parquet with daily partitions, register in Glue Data Catalog, support schema evolution, and enable time-travel queries. Include enrichment, idempotency, data quality checks, testing plan, and governance controls (encryption, access)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Glue PySpark job that reads s3://lake/raw/events/YYYY/MM/DD/events.json, flattens nested JSON, fills missing with nulls, and writes Parquet to s3://lake/curated/events/YYYY/MM/DD/ partitio",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an AWS Glue Spark job to read NDJSON files from daily S3 paths (s3://partner-clicks/YYYY/MM/DD/events.ndjson), unify the evolving schemas into a single target schema, and write Parquet partitioned output to s3://dat…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an AWS Glue workflow: read LDJSON with a DynamicFrame, normalize to a fixed schema, and write Parquet partitioned by date to s3://ad-logs/parquet/YYYY/MM/DD. Enable Job Bookmarks for idempotence,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Kinesis Data Analytics to enrich JSON streams, then write Parquet to S3 with daily partitions and register in Glue Catalog. Leverage Glue Schema Registry for evolution; implement idempotent upsert",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design a scalable streaming ingestion with enrichment, schema evolution, and governance in AWS.\n\n## Key Concepts\n- Streaming ingestion (Kinesis Data Streams, Kinesis Data Analytics)\n- Parquet on S3 with partitioning\n- Glue Data Catalog and Iceberg table\n- Schema evolution and data quality\n- Idempotent upserts and testing\n- Governance (Lake Formation, KMS)\n\n## Code Example\n```javascript\n// Pseudo Spark Structured Streaming (illustrative)\nconst df = spark.readStream.format(\"kinesis\").option(\"streamName\",\"...\").load()\nval enriched = df.selectExpr(\"CAST(data AS STRING) as json\").transform(parseAndEnrich)\nenriched.writeStream.format(\"iceberg\").option(\"path\",\"s3://data-lake/partner/iceberg\").start()\n```\n\n## Follow-up Questions\n- How would you handle late-arriving data and duplicate IDs?\n- How would you test schema evolution across multiple partitions?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-4",
        "questionId": "q-2985",
        "question": "Scenario: You must ingest daily JSONL logs from an on-prem SFTP into S3. Design a beginner-friendly, serverless ingestion using AWS Transfer Family to pull files, convert to Parquet with daily partitions, catalog via Glue, and orchestrate via Step Functions. What steps would you implement, including naming, partitioning, quality checks, and testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Land daily CSVs via AWS Transfer Family to S3 raw, then a Glue Spark job to cast types, write Parquet to curated with date partitions. Crawlers update the Data Catalog; enabled schema evolution to acc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Hybrid batch/streaming pipeline: multi-region JSON lands in regional S3; streaming uses Kinesis Data Streams + Firehose to Parquet; Glue ETL with Spark handles schema evolution; Glue Data Catalog + La",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Serverless SFTP ingest: Use AWS Transfer Family to pull daily JSONL from on-prem SFTP into S3 at logs/date=YYYY-MM-DD/. Run a Glue Spark ETL to convert to Parquet with daily partitions and register in",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-region Kinesis Data Streams feeding into Kinesis Firehose to S3 in Parquet, partitioned by date and region. Deduplicate with a DynamoDB registry and idempotent producers to achieve effectively",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical use of serverless ingest for on-prem sources, with a safe beginner path using Transfer Family, Glue, and Step Functions.\n\n## Key Concepts\n- AWS Transfer Family for SFTP pulls\n- Glue Spark ETL and Data Catalog\n- Parquet with daily partitions\n- Step Functions orchestration and basic data quality\n\n## Code Example\n```python\n# Glue ETL outline to convert JSONL to Parquet and write daily partitions\nimport sys\nfrom awsglue.context import GlueContext\nfrom awsglue.transforms import *\nfrom awsglue.utils import getResolvedOptions\n# ... minimal skeleton\n```\n\n## Follow-up Questions\n- How would you handle schema drift or missing fields in JSONL?\n- How would you monitor Step Functions failures and alert on retries?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-5",
        "questionId": "q-24199",
        "question": "Scenario: You receive daily JSON events from a partner at s3://partners/ingest/YYYY/MM/DD/events.json. Design a beginner-level AWS data pipeline that: validates JSON against a schema (allowing minimal evolution), ingests to Parquet at s3://partners/warehouse/YYYY/MM/DD/ partitioned by date, registers/upserts a Glue Data Catalog table, and enforces idempotent processing. Include a simple data quality check and a testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use KDS with a streaming ETL (Glue or Lambda) to write Parquet to S3 partitioned by tenant_id/date; catalog in Glue Data Catalog; enforce Lake Formation permissions per tenant; apply dynamic masking f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest via Kinesis Data Streams; Glue Spark parses JSON, masks PII, and writes Parquet to S3 partitioned by tenant_id/date. Catalog with Glue; enable cross-account Lake Formation grants for Snowflake/",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Read daily events.json from the ingest path (s3://partners/ingest/YYYY/MM/DD/), validate against a provided JSON schema with support for backward-compatible schema evolution, cast types and handle missing fields with ap…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a Glue PySpark job that reads from s3://data-lake/finance/parquet/YYYY/MM/DD/, coalesces to approximately 200MB output files, and writes to s3://data-lake/warehouse/finance/YYYY/MM/DD/ with the same date partitio…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical skills in building a repeatable, auditable data ingestion flow using AWS Glue, addressing real-world concerns like schema evolution, data quality, and idempotence.\n\n## Key Concepts\n- AWS Glue ETL jobs with bookmarks for state management and idempotent processing\n- JSON Schema validation with backward compatibility for minor schema evolution\n- Parquet formatting with date-based partitioning for optimized query performance\n- Glue Data Catalog table registration and schema updates\n- Data quality checks including NOT NULL constraints, value range validation, and duplicate detection\n\n## Code Example\n```python\nimport json\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, when, to_date, current_date\n\nspark = SparkSession.builder.getOrCreate()\n\nINGEST_PATH = \"s3://partners/ingest/\"\nWAREHOUSE_PATH = \"s3://partners/warehouse/\"\n\ndef validate_and_transform(df, schema):\n    for field in schema.get(\"properties\", {}).items():\n        field_name, field_def = field\n        dtype = field_def.get(\"type\", \"string\")\n        default = field_def.get(\"default\")\n        \n        if dtype == \"integer\" and default is not None:\n            df = df.withColumn(field_name, col(field_name).cast(\"integer\").fillna(default))\n        elif dtype == \"number\" and default is not None:\n            df = df.withColumn(field_name, col(field_name).cast(\"double\").fillna(default))\n        \n        if field_def.get(\"required\", False):\n            df = df.withColumn(f\"{field_name}_valid\", col(field_name).isNotNull())\n    \n    return df\n\ndef run_daily_etl(date_str):\n    year, month, day = date_str.split(\"-\")\n    input_path = f\"{INGEST_PATH}{year}/{month}/{day}/events.json\"\n    output_path = f\"{WAREHOUSE_PATH}year={year}/month={month}/day={day}/\"\n    \n    df = spark.read.json(input_path)\n    \n    with open(\"schema.json\") as f:\n        schema = json.load(f)\n    \n    df_validated = validate_and_transform(df, schema)\n    \n    df_validated.write.partitionBy(\"year\", \"month\", \"day\").mode(\"append\").parquet(output_path)\n    \n    glueContext.create_or_update_table(\n        database_name=\"partners_db\",\n        table_name=\"events\",\n        parquet_output_location=WAREHOUSE_PATH\n    )\n\nif __name__ == \"__main__\":\n    run_daily_etl(\"2024-01-15\")\n```\n\n## Testing Plan\n- **Unit Tests**: Validate schema parsing and transformation logic with mocked DataFrames\n- **Integration Tests**: Test full pipeline against sample data in mocked S3\n- **Data Quality Tests**: Assert row counts, null percentages, and duplicate detection thresholds\n- **Idempotence Tests**: Verify running the same job twice produces identical outputs\n- **Schema Evolution Tests**: Confirm pipeline handles new optional fields gracefully",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-6",
        "questionId": "q-30224",
        "question": "Scenario: Global mobile app emits events to Kinesis Data Streams in Region A and Region B. Design an end-to-end AWS data pipeline to: 1) flatten and mask PII in-flight, then store as Parquet with hourly partitions in S3; 2) register schemas and enable evolution in Glue Data Catalog; 3) replicate data across regions for analytics with minimal lag; 4) guarantee idempotent processing for both streaming and batch loads; 5) include data quality checks and an end-to-end test plan. Justify services, partitioning, and testing approaches?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Kinesis Data Streams with a streaming transform (Kinesis Data Analytics or Lambda) to mask PII in-flight, then land as Parquet in S3 with hourly partitions. Use Glue Data Catalog for schema e",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use AWS Glue Job with bookmarks to read s3://corp-lake/raw/appX/YYYY/MM/DD/events.json, flatten nested fields, coerce types, and mask PII (redact email local-part). Write Parquet to s3://corp-lake/pro",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a single source-of-truth data contract (Avro) in Glue Schema Registry, enforced by Glue Data Catalog. Streaming: Kinesis Data Streams to Firehose writes Parquet to S3 with daily partitions; Batch:",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Glue Spark job with a pre-scan to detect file delimiter and headers, unify to a canonical schema, infer schema via a Glue crawler, and write Parquet partitioned by date and region. Include basic",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-region ingestion, real-time masking, schema evolution, idempotency, and end-to-end testing in an AWS data lake context.\n\n## Key Concepts\n- Global data lake design across regions\n- In-flight data masking and encryption\n- Parquet with time-based partitioning\n- Glue Data Catalog and schema evolution\n- Cross-region replication and latency considerations\n- Idempotent processing and deduplication\n- Data quality checks and end-to-end tests\n\n## Code Example\n```javascript\n// Pseudo: deduplicate by composite key in Spark/Glue job\nval df = spark.read.parquet(\"s3://lake/events/*/\")\ndf.withColumn(\"dedup_id\", concat_ws(\"_\", col(\"region\"), col(\"stream\"), col(\"event_id\")))\n  .dropDuplicates(\"dedup_id\")\n```\n\n## Follow-up Questions\n- How would you handle a breaking schema evolution (e.g., int to string) in Glue Catalog?\n- What monitoring would you implement to track cross-region replication latency and data freshness?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-7",
        "questionId": "q-23005",
        "question": "Design an end-to-end pipeline to ingest multi-source data into an AWS lakehouse using Apache Iceberg on S3, supporting upserts from CDC, per-tenant isolation via Lake Formation, and schema evolution. Data arrives as JSON logs in S3 and CDC changes from Postgres via DMS. Include partitioning, idempotency, testing, and cost/ops considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a region-scoped ingestion pipeline using S3 landing zones in both us-east-1 and eu-west-1. Use AWS Glue Spark jobs with a unified schema registry to read JSON and CSV sources, transform and write partitioned Parq…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an end-to-end pipeline to ingest multi-source data into an AWS lakehouse using Apache Iceberg on S3. The pipeline must support upserts from CDC, per-tenant isolation via Lake Formation, and schema evolution. Data…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a Glue Spark job (Python) to read TSV, handle evolving headers via DynamicFrame.resolveChoice, and write Parquet partitioned by date and service_name to s3://saas-logs/processed/YYYY/MM/DD/service",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a pipeline where JSON events are ingested into S3 as Apache Iceberg tables partitioned by event_date, with schema evolution via Glue Schema Registry and upserts via MERGE on (customer_id, event_id). The pipeline…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design a scalable lakehouse architecture with CDC integration, per-tenant isolation, and robust schema evolution in AWS.\n\n## Key Concepts\n- **Iceberg on S3**: Provides ACID transactions, upserts, and schema evolution capabilities\n- **DMS CDC**: Streams changes from PostgreSQL in real-time\n- **Lake Formation**: Enables fine-grained, per-tenant access control\n- **Idempotent writes**: Achieved using stable primary keys and merge operations\n- **Partitioning strategies**: Use tenant_id/date composite partitioning for optimal query performance\n- **Testing strategies**: Include data validation, schema compatibility checks, and failure recovery patterns\n- **Cost/ops considerations**: Optimize storage costs with tiered storage, manage compute costs through proper sizing of Glue jobs, and implement monitoring for operational excellence\n\n## Code Example\n```python\n# Pseudocode: AWS Glue Iceberg Merge Job\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col\n\ndef process_cdc_batch(input_path, table_name):\n    spark = SparkSession.builder.getOrCreate()\n    \n    # Read CDC changes from DMS S3 landing zone\n    cdc_df = spark.read.json(input_path)\n    \n    # Perform merge (upsert) into Iceberg table\n    spark.sql(f\"\"\"\n        MERGE INTO {table_name} target\n        USING cdc_df source\n        ON target.id = source.id\n        WHEN MATCHED THEN UPDATE SET *\n        WHEN NOT MATCHED THEN INSERT *\n    \"\"\")\n```",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-8",
        "questionId": "q-20349",
        "question": "Scenario: A partner uploads daily gzip-compressed CSVs to s3://corp/raw/sales/YYYY/MM/DD/sales.csv.gz. Design a beginner-level AWS data ingestion pipeline using AWS Glue to decompress, normalize headers, validate data types, deduplicate by order_id, convert to Parquet, partition by date, and register in Glue Data Catalog. Include idempotent re-run handling, testing strategy, and how to monitor for failures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Glue Spark job triggered on new objects or a daily run. Read the gzip CSV from S3, normalize headers, cast types (order_id as string, amount as double, date as timestamp), drop bad rows, dedupli",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a Glue Spark job read s3://telemetry/raw/edgeA/YYYY/MM/DD/*.tsv, infer schema, write Parquet to s3://telemetry/processed/edgeA/YYYY/MM/DD/ with daily partitions, and update Glue Data Catalog. Enab",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Glue Spark job that reads daily NDJSON, handles schema evolution via a versioned Glue Catalog table, writes Parquet to s3://data-lake/processed/accounts/YYYY/MM/DD/, partitions by date, and u",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Route IoT Core data to Kinesis Firehose, writing Parquet to s3://lake/iot/... partitions by date. Enable Apache Hudi on an EMR Spark job to upsert and provide time-travel, with Glue Data Catalog integ",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe task tests basic ETL design, handling compressed inputs, data validation, and idempotent upserts using Glue.\n\n## Key Concepts\n- AWS Glue Spark jobs, PySpark\n- S3 event triggers, gzip decompression\n- Data validation, schema casting\n- Deduplication strategies and idempotency\n- Parquet storage, partitioning, Glue Data Catalog\n- Monitoring with CloudWatch\n\n## Code Example\n```python\n# Placeholder example: PySpark snippet outline\nfrom pyspark.sql import functions as F\n\n# read gzip CSV\ndf = spark.read.csv('s3://corp/raw/sales/.../sales.csv.gz', header=True, inferSchema=True)\n# normalize headers and cast\ndf = df.selectExpr([F.col(c).cast('string').alias(c) for c in df.columns])\ndf = df.withColumn('amount', F.col('amount').cast('double'))\n# filter/validate\ndf = df.filter(F.col('order_id').isNotNull() & (F.col('amount') >= 0))\n# dedupe by (partition_date, order_id)\ndf = df.dropDuplicates(['partition_date','order_id'])\n# write\npath = f's3://corp/warehouse/sales/{partition_date}/'\ndf.write.parquet(path, mode='append')\n```\n\n## Follow-up Questions\n- How would you handle schema drift if new columns appear?\n- What testing would you add to ensure no duplicates after re-runs?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-9",
        "questionId": "q-11597",
        "question": "Intermediate: Telemetry JSON from multiple apps is ingested into Kinesis Data Streams. Design an end-to-end pipeline to land, deduplicate, transform to Parquet in S3 with daily partitions, register in Glue Data Catalog, and feed a SageMaker Feature Store for offline/online use. Include schema evolution strategy, late-arriving data, idempotent upserts, data quality checks, testing plan, and ops monitoring. Compare Kinesis Data Streams vs Firehose trade-offs and justify your choice?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a streaming-first pipeline: Kinesis Data Streams across 3 accounts feeding a central Kinesis Data Firehose that writes Parquet to S3 with daily partitions. Catalog via Glue Data Catalog; use Icebe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use MSK as the ingest backbone with 20 regional producers publishing to topics partitioned by region. Deploy a Kinesis Data Analytics for Apache Flink application to read from MSK, perform initial deduplication using ev…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a streaming-to-ML pipeline: land JSON in Kinesis Data Streams, deduplicate by event_id, transform to Parquet in S3 with daily partitions, register in Glue Data Catalog, and feed SageMaker Feat",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Spark Structured Streaming to read from Kinesis and write to a Delta table on S3, performing upserts via DeltaTable.merge with a watermark for late events. Partition by date and region; enable sch",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical streaming pipelines, data provenance, schema evolution, and ML feature store integration in AWS, plus real-world trade-offs between KDS and Firehose, idempotent processing, late data, and testing.\n\n## Key Concepts\n- Streaming ingestion patterns (KDS vs Firehose)\n- Parquet with daily partitions\n- Glue Data Catalog\n- SageMaker Feature Store online/offline\n- Schema evolution and registry\n- Deduplication and idempotency\n- Late-arriving data handling\n- Data quality checks and testing\n- Monitoring and observability\n\n## Code Example\n```javascript\nfunction upsertRecord(store, key, rec) {\n  const existing = store[key] || {};\n  store[key] = { ...existing, ...rec };\n}\n```\n\n## Follow-up Questions\n- How would you handle schema drift in the catalog without breaking pipelines?\n- What metrics/alarms would you set for end-to-end latency and data loss?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-10",
        "questionId": "q-14811",
        "question": "Scenario: A daily data feed includes two sources delivering user events: CSV files in s3://data-lake/raw/csv/users/YYYY/MM/DD/events.csv and JSON files in s3://data-lake/raw/json/users/YYYY/MM/DD/events.json. Design a beginner-level AWS data ingestion pipeline that merges both sources into a single Parquet dataset at s3://data-lake/warehouse/users/YYYY/MM/DD/events.parquet, partitions by date, registers in Glue Data Catalog, applies a canonical schema, performs basic data quality checks, and uses a simple dedup strategy across runs. Outline services, data layout, and testing approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose multi-region, multi-account ingestion: protobuf from regional MSK into S3 as partitioned Parquet (region/date); tenant isolation via Lake Formation; Glue Data Catalog for schemas; cross-region",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest JSON via Kinesis Data Firehose, convert to Parquet, partition by tenant_id/date, and land in S3. Use Glue Data Catalog with Lake Formation to enforce per-tenant row-level access. Implement dedu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Glue Spark ETL job that MERGEs incoming events into Iceberg on S3 using event_id as the primary key, enabling idempotent upserts and daily partitions. Enable Iceberg schema evolution, and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Glue Job (Python) reads both CSV and JSON, maps to a fixed schema: user_id, event_ts, action, value; unions into one DataFrame; writes Parquet to s3://data-lake/warehouse/users/YYYY/MM/DD/events",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes ability to design a cross-format ingestion pipeline, canonicalize schemas, leverage Glue Data Catalog for unified analytics, implement a simple idempotent path, and outline practical data quality tests.\n\n## Key Concepts\n\n- AWS Glue ETL for multi-format ingestion\n- Canonical schema mapping across CSV/JSON\n- Parquet output with partitioning by date\n- Glue Data Catalog registration\n- Lightweight data quality checks and dedup strategy\n\n## Code Example\n\n```javascript\nfunction mapRecord(rec) {\n  return {\n    user_id: (rec.user_id || rec.id),\n    event_ts: (rec.event_ts || rec.timestamp),\n    action: (rec.action || \"\"),\n    value: Number(rec.value || 0)\n  };\n}\n```\n\n## Follow-up Questions\n\n- How would you handle occasional schema drift where new fields appear?\n- What test scenarios would you run to ensure dedup across days remains correct?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-11",
        "questionId": "q-26071",
        "question": "You operate an AWS-based data lake with multiple sources: mobile app events via Kinesis Data Streams (JSON) and partner data via MSK (Avro). Design an ingestion pipeline that lands all data into a single S3-based Iceberg table, enabling upserts and time travel. Include schema evolution strategy, idempotent writes, late-arriving data handling, cross-account access, and a practical end-to-end testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a lineage-first design: sources push metadata to Glue Catalog, ETL jobs emit lineage events to a centralized metadata store (S3/Glue) with per-region tagging; use Lake Formation permissions ti",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Iceberg-on-S3 with Spark jobs (Glue/EMR) consolidating JSON (Kinesis) and Avro (MSK) into one upsertable, time-travel table. Partition by event_date, region; use Iceberg's schema evolution; implement",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Read NDJSON from s3://health/raw/claims/YYYY/MM/DD/, flatten and enforce schema with Glue, write Parquet to s3://health-processed/claims/YYYY/MM/DD/ partitioned by date. For records missing required f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument each ETL run with a unique run_id and emit comprehensive lineage records (source→transformation→target) to a centralized lineage ledger stored in S3, with corresponding metadata managed in the Glue Data Catal…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to blend streaming sources into a unified, upsertable lake using Iceberg, addressing schema evolution, late data handling, and cross-account access.\n\n## Key Concepts\n- Iceberg on S3, Spark/Glue, Kinesis/MSK ingestion\n- Upserts and time travel via Iceberg\n- Schema evolution, late-arriving data, cross-account access with Lake Formation\n- End-to-end testing and monitoring\n\n## Code Example\n```sql\nCREATE TABLE iceberg_db.events (\n  event_date date,\n  region string,\n  payload MAP<string,string>\n) USING iceberg\nPARTITIONED BY (event_date, region);\n```\n\n```sql\nMERGE INTO iceberg_db.events AS t\nUSING staging AS s\nON t.event_id = s.event_id\nWHEN MATCHED THEN UPDATE SET t.payload = s.payload\nWHEN NOT MATCHED THEN INSERT (event_date, region, payload) VALUES (s.event_date, s.region, s.payload);\n```\n\n## Follow-up Questions\n- How would you detect and remediate schema drift automatically?\n- How would you adapt the pipeline for late-arriving events with minimal user impact?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-12",
        "questionId": "q-15075",
        "question": "Design a lineage-aware pipeline for fraud-detection events where raw JSON lands in S3, Glue Spark ETL writes Parquet with a provenance log (raw_id, event_ts, source, transforms, version), and engineered features populate SageMaker Feature Store for training and online inference. How would you capture end-to-end lineage, handle schema evolution, and enforce cross-account access controls? Be concrete about services, formats, and tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Lineage-aware pipeline: ingest raw JSON to S3, Glue Spark ETL to Parquet in partitioned folders with a provenance table (raw_id, event_ts, source, transforms, version). Publish engineered features to",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use AWS Glue with a simple Spark job that reads CSV and JSON via DynamicFrame, coerces to a unified schema, writes Parquet to the processed path partitioned by date, and uses Job bookmarks for idempot",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kinesis Firehose to land JSON into S3 with daily partitions, then a Glue ETL job to emit Parquet and populate the catalog. Enable cross‑account analytics via Lake Formation. Handle schema evolutio",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a pipeline: IoT devices emit evolving JSON to S3. AWS Glue Spark ETL jobs flatten nested JSON to Parquet, partitioned by date and device_id. Implement idempotent upserts using a deduplication table with a commit…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests end-to-end data lineage, schema evolution, and integration with ML features.\n\n## Key Concepts\n- Lineage provenance management; - SageMaker Feature Store versioning; - Glue Catalog/Lake Formation tags; - Parquet partitioning; - End-to-end testing\n\n## Code Example\n```javascript\n// Placeholder\n```\n\n## Follow-up Questions\n- How would you automate failure recovery for failed feature writes?\n- How do you enforce access controls across accounts for raw and feature data?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-13",
        "questionId": "q-22905",
        "question": "You're collecting per-tenant events from a SaaS app via Kinesis Data Streams. Design an incremental ingestion that lands JSON events as Parquet in S3, partitioned daily by tenant and date, with Glue for cataloging. Ensure per-tenant schema evolution, idempotent replays, data quality checks, and a robust end-to-end test plan. Include late-arrival handling and replay-safe reprocessing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use S3 as the raw landing, a Glue crawler to create the catalog, and a Glue ETL job (Python) to flatten JSON to a tabular schema, write Parquet to s3://lake/{tenant_id}/{date}/, and generate a lineage",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Land streaming JSON events to s3/raw/{tenant_id}/{YYYY}/{MM}/{DD}. Use a Glue Streaming ETL job to read from Kinesis or Kafka, leveraging Glue Schema Registry to handle schema evolution and capture schema drift. Write P…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set up an hourly ETL: decrypt ORC, normalize to Parquet, and write to S3 with hourly partitions. Track lastProcessedHour in DynamoDB; emit tombstones for deletes to propagate removals. Use Glue for ca",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage Kinesis Data Streams with a Glue Structured Streaming (Glue 4.0) job to read JSON and write Parquet to S3, partitioned by tenant_id and date. Use Glue Schema Registry for per-tenant schema ev",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design streaming ingestion with multi-tenant data, schema evolution, and robust replay semantics.\n\n## Key Concepts\n\n- Streaming ingestion with AWS Glue Structured Streaming\n- Per-tenant partitioning and schema evolution via Glue Schema Registry\n- Idempotent writes using composite key (tenant_id, event_id)\n- Late-arrival handling with watermarks and end-to-end replay testing\n\n## Code Example\n\n```javascript\n// Pseudocode: Spark Structured Streaming pipeline skeleton\n// read from Kinesis, map to Parquet with partitionBy(\"tenant_id\",\"date\"), writeStream\n```\n\n## Follow-up Questions\n\n- How would you enforce per-tenant schema changes without breaking older partitions?\n- What monitoring would you put in place to detect duplicates or missing tenants in real time?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-14",
        "questionId": "q-26777",
        "question": "Scenario: Daily LDJSON ad-impression logs arrive at s3://ad-logs/raw/YYYY/MM/DD/logs.ldjson with occasional missing fields and new columns over time. Design a beginner AWS data pipeline that (1) converts to Parquet partitioned by date, (2) registers in Glue Data Catalog, (3) processes idempotently with Glue Job bookmarks, (4) tracks schema drift, and (5) provides an Athena view for analytics with a simple alert when new columns appear?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an AWS Glue workflow: read LDJSON with a DynamicFrame, normalize to a fixed schema, and write Parquet partitioned by date to s3://ad-logs/parquet/YYYY/MM/DD. Enable Job Bookmarks for idempotence,",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage Debezium CDC to MSK, Glue Spark to write to Iceberg on S3 partitioned by date. Upserts/deletes use Iceberg tombstones and rewrite; evolving schemas via Iceberg schema evolution. Integrate Gre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a secure data ingestion pipeline using a KMS-encrypted S3 bucket to store raw JSON files with hourly prefixes. Configure a Glue Crawler to infer the initial schema and populate the Glue Data Catalog. Develop a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest streams via Kinesis Firehose to S3 with TLS and SSE-S3. Partition Parquet by version/source, e.g., s3://ml-data/version=20260313/source=a/. Use Glue Data Catalog for schema evolution; Spark ETL",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical ingestion of line-delimited JSON with evolving schema; exercises idempotence, cataloging, and alerting.\n\n## Key Concepts\n- LDJSON handling in Glue\n- DynamicFrame vs DataFrame\n- Parquet with date partitioning\n- Glue Job bookmarks for idempotence\n- Glue Data Catalog and Athena integration\n- Schema drift detection and alerting\n\n## Code Example\n```python\n# PySpark (Glue) snippet\nfrom pyspark.sql.functions import col, to_date\ninput_path = 's3://ad-logs/raw/...'\ndf = spark.read.json(input_path)\ndf = df.withColumn('date', to_date(col('date'), 'yyyy-MM-dd'))\ndf.write.partitionBy('date').parquet('s3://ad-logs/parquet/...')\n```\n\n## Follow-up Questions\n- How would you test schema drift detection in this pipeline?\n- How would you extend this for higher throughput and error handling?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-15",
        "questionId": "q-6876",
        "question": "Scenario: Ingest chat transcripts and user events from a mobile app via Kinesis Data Streams into S3 as daily-partitioned Parquet. The ML/Analytics teams will use Snowflake for analytics and Hugging Face models for NLP features. Design the end-to-end pipeline: ingestion and schema evolution, Parquet partitioning strategy, offline/online feature store approach, data governance, and a testing plan to validate end-to-end reliability before serving?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an AWS Glue Spark job to read the daily CSV with a defined schema (user_id string, event_type string, event_ts timestamp, metadata string nullable), write Parquet to a date-partitioned path, and u",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a simple, idempotent pipeline: read both raw paths with Glue jobs, map fields to a canonical schema, coerce types (e.g., strings to ints where safe), union datasets, write Parquet to curated pa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest with Kinesis to S3 Parquet (daily partitions). Use Glue ETL to enforce schema evolution and register schemas in Glue Data Catalog. Build offline features in Snowflake on top of Parquet tables;",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Approach: implement a Glue Spark job that reads CSV with DynamicFrame, uses mergeSchema to tolerate new columns, writes Parquet to the daily partition path, and updates the Glue Data Catalog. Add a si",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end streaming to lakehouse with ML feature exposure and governance.\n\n## Key Concepts\n- Kinesis streaming; Parquet on S3; daily partitions\n- Glue ETL and Data Catalog; schema evolution\n- Snowflake offline analytics; Hugging Face NLP features online\n- Lake Formation governance and access controls\n- End-to-end testing: synthetic data, drift, lineage\n\n## Code Example\n```javascript\n// Pseudo Glue Spark job sketch\nconst df = spark.readStream.format('kinesis').load(\"stream-name\");\nval out = df.writeStream.format('parquet').option('partitionBy','date').start(\"s3://bucket/ Parquet/\");\n```\n\n## Follow-up Questions\n- How would you test schema drift between old and new events?\n- How would you handle feature store consistency if the Hugging Face model updates?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-16",
        "questionId": "q-27295",
        "question": "Scenario: A geo-distributed data lake collects autonomous-vehicle telemetry in JSON from Europe and needs near-real-time analytics in the US. Design an end-to-end ingestion and serving pipeline using AWS services to land Parquet in s3://telemetry/year=YYYY/month=MM/hour=HH/, register schemas in Glue Schema Registry, enable cross-region replication, enforce idempotent processing, handle schema evolution, and implement data quality checks. Include a testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Push telemetry from Europe to Kinesis Data Streams; a Kinesis Data Analytics app writes Parquet to s3://telemetry/year=YYYY/month=MM/hour=HH/ in the US. Enable CRR, register schemas in Glue Schema Reg",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Glue ETL to convert JSON to Parquet, partitioned by tenant/date under s3://lake/{tenant}/{YYYY}/{MM}/{DD}/. Manage Iceberg tables in the Glue Catalog for atomic commits and schema evolution. Imple",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an AWS Glue workflow: read LDJSON with a DynamicFrame, normalize to a fixed schema, and write Parquet partitioned by date to s3://ad-logs/parquet/YYYY/MM/DD. Enable Job Bookmarks for idempotence,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a pipeline: Glue Spark reads JSON with nested fields, flattens it, and writes to an Iceberg table on S3 with daily partitions. Upsert by a stable PK using MERGE INTO, and enable Iceberg schema",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-region dataflow design, schema contracts, idempotence, late data handling, and testing strategies in production-grade AWS pipelines.\n\n## Key Concepts\n- Cross-region replication (S3 CRR) and secure data transfer\n- Glue Schema Registry for contracts and evolving schemas\n- Idempotent ingestion using ingest_id with DynamoDB state\n- Parquet format with hourly partitions and schema evolution handling\n- Data quality checks (null rates, ranges, duplicates)\n- End-to-end and late-arriving data testing strategies\n\n## Code Example\n```python\n# PySpark dedup by ingest_id\ndf = df.dropDuplicates([\"ingest_id\"])\n```\n\n## Follow-up Questions\n- How would you handle late-arriving data and out-of-order events?\n- How would you validate schema evolution without breaking downstream consumers?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-17",
        "questionId": "q-16148",
        "question": "You need to ingest PostgreSQL CDC events (via Debezium) streaming into AWS, landing as Parquet on S3 with daily partitions, using Glue for catalog, and ensuring idempotent upserts and schema evolution. Describe the end-to-end pipeline, including handling late-arriving data, upserts, schema changes, and how you would test end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Glue-based ETL pipeline: crawl raw CSVs to infer schema; a Spark job reads CSVs, deduplicates by order_id, fills missing discount values with 0, validates non-null order_id and amount >= 0, casts amount to d…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a streaming pipeline using Debezium into MSK, Spark Structured Streaming with foreachBatch to upsert into s3://lake/data/events/date=YYYY-MM-DD/; use Glue Data Catalog and partition projection",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Set up an hourly ETL: decrypt ORC, normalize to Parquet, and write to S3 with hourly partitions. Track lastProcessedHour in DynamoDB; emit tombstones for deletes to propagate removals. Use Glue for ca",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Glue ETL to convert JSON to Parquet, partitioned by tenant/date under s3://lake/{tenant}/{YYYY}/{MM}/{DD}/. Manage Iceberg tables in the Glue Catalog for atomic commits and schema evolution. Imple",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a real streaming CDC pipeline with schema evolution and robust upserts in a lake.\n\n## Key Concepts\n- CDC with Debezium, MSK, Spark Structured Streaming\n- Parquet lake storage with daily partitions and tombstone handling\n- Glue Data Catalog, partition projection, schema evolution via Glue Schema Registry\n- Late-arriving data, deduplication, and deletes\n- End-to-end testing with synthetic CDC and checkpoints\n\n## Code Example\n```scala\n// pseudo: read from MSK, upsert to Parquet by batch, commit with transaction-like approach\nimport org.apache.spark.sql.functions._\nval df = spark.readStream.format(\"kafka\").option(\"kafka.bootstrap.servers\",\"...\" ).load()\nval parsed = df.selectExpr(\"CAST(value AS STRING) as json\").select(from_json(col(\"json\"), schema).as(\"r\")).select(\"r.*\")\nparsed.writeStream\n  .outputMode(\"append\")\n  .foreachBatch { (batchDF, batchId) =>\n    // upsert into Parquet by PK, e.g., write to temp + merge\n  }\n  .option(\"checkpointLocation\",\"s3://lake/checkpoints/...\")\n  .start()\n```\n\n## Follow-up Questions\n- How would you handle a schema change that adds a new column without breaking downstream queries?\n- How would you validate that dedup works after a restart?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-18",
        "questionId": "q-23103",
        "question": "You're integrating a new streaming source that emits Avro-encoded records into a multi-account, multi-region data lake on AWS. Data arrives via Kinesis; downstream is Athena/Glue. Describe an end-to-end ingestion pipeline that handles evolving schemas, ensures idempotent processing, implements schema evolution with Glue Schema Registry, partitions Parquet in S3 by date, masks PII in-flight, registers in Glue Data Catalog, enforces cross-account access with Lake Formation, and provides automated data quality checks and simple end-to-end tests. Be specific about services and configurations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest semi-structured JSON events from multiple microservices into a shared AWS data lake with tenant isolation via Lake Formation. Store Parquet files partitioned by tenant and date, registered in the Glue Data Catalo…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Glue Spark job (Python) that reads daily Parquet/CSV from s3://data-lake/raw/sales/YYYY/MM/DD/, flattens nested fields, and writes Parquet to s3://data-lake/warehouse/sales/YYYY/MM/DD/ par",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I’d route Firehose to a raw S3 bucket, run a Glue ETL job to normalize CSV/JSON to Parquet, and partition by date/region. Glue Catalog tracks schemas; Lake Formation enforces per‑user access. Implemen",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Kinesis Data Streams ingest of Avro records with a Glue Streaming ETL using DynamicFrame that adapts to evolving schemas via Glue Schema Registry. Write Parquet to S3 partitioned by date, mask P",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design cross-account, real-time data pipelines with schema evolution, data masking, and governance. Requires knowledge of AWS Glue, Schema Registry, Lake Formation, and end-to-end testing in a scalable lake.\n\n## Key Concepts\n- Multi-account data lake\n- Glue Schema Registry and dynamic schemas\n- Lake Formation cross-account access\n- Idempotent streaming processing\n- In-flight data masking\n- Parquet partitioning and cataloging\n\n## Code Example\n```python\n# PseudoGlueStreaming\nfrom awsglue.dynamicframe import DynamicFrame\n# read from KDS, apply masking, manage schema evolution, write Parquet partitioned by date\n```\n\n## Follow-up Questions\n- How would you detect and resolve schema conflicts across regions?\n- How do you test idempotency under data replays or shard rebalances?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-19",
        "questionId": "q-16925",
        "question": "Scenario: daily NDJSON logs arrive in s3://edge-logs/YYYY/MM/DD/events.ndjson. Design a beginner-level AWS Glue-based ingestion that flattens nested fields, deduplicates by a composite key, and writes Parquet to s3://edge-processed/YYYY/MM/DD/ partitioned by date; register the data in the Glue Data Catalog. Include a simple schema-drift strategy (new fields default to null) and a basic data quality gate: deviceId required; ts parseable; temperature within plausible range. Outline a lightweight end-to-end test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a PySpark Glue job that reads the NDJSON, flattens nested fields via ApplyMapping, and deduplicates on (deviceId, ts). Write Parquet to s3://edge-processed/YYYY/MM/DD/ with daily partitioning and",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a streaming+batch pipeline: raw JSON per tenant lands in S3; a Glue Spark job evolves the schema to Parquet (Iceberg) and partitions by tenant/date; apply PII masking in ETL; register in Glu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest via Kinesis Data Streams; Glue Spark parses JSON, masks PII, and writes Parquet to S3 partitioned by tenant_id/date. Catalog with Glue; enable cross-account Lake Formation grants for Snowflake/",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Glue Spark job read s3://telemetry/raw/edgeA/YYYY/MM/DD/*.tsv, infer schema, write Parquet to s3://telemetry/processed/edgeA/YYYY/MM/DD/ with daily partitions, and update Glue Data Catalog. Enab",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical, beginner-friendly ingestion using Glue, covering NDJSON handling, flattening, dedupe, partitioning, and a lightweight schema-drift strategy plus data quality and alerting.\n\n## Key Concepts\n\n- AWS Glue PySpark and DynamicFrame\n- NDJSON loading and field flattening\n- Parquet partitioning by date and Glue Catalog integration\n- Schema drift handling with null defaults\n- Lightweight data quality gates and SNS alerts\n\n## Code Example\n\n```python\n# Pseudo sketch for Glue\nfrom awsglue.context import GlueContext\nfrom awsglue.transforms import ApplyMapping\nfrom pyspark.context import SparkContext\n\nsc = SparkContext()\ngc = GlueContext(sc)\ndf = gc.create_dynamic_frame.from_options(\n    connection_type=\"s3\",\n    connection_options={\"paths\": [\"s3://edge-logs/YYYY/MM/DD/events.ndjson\"]},\n    format=\"ndjson\",\n)\nmapped = ApplyMapping.apply(frame=df, mappings=[\n    (\"device.id\", \"string\", \"deviceId\", \"string\"),\n    (\"payload.temp\", \"double\", \"temperature\", \"double\"),\n    (\"timestamp\", \"string\", \"ts\", \"timestamp\"),\n])\n# Deduplicate and write as Parquet with partitioning\n# (Actual code would use dropDuplicates and writeDynamicFrame with partitionKeys)\n```\n\n## Follow-up Questions\n\n- How would you test end-to-end when new fields appear in events?\n- What changes would you make to handle high-cardinality devices without blowing up the catalog?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-20",
        "questionId": "q-29545",
        "question": "You manage a two-account AWS data lake and ingest nightly clickstream JSON events from an external platform into S3 as daily Parquet partitions (YYYY/MM/DD). Design an end-to-end pipeline that (1) validates evolving JSON schema against Glue Schema Registry with backward/forward compatibility, (2) performs idempotent upserts into the partitioned Parquet data to accommodate late-arriving events, (3) preserves end-to-end lineage via Glue Data Catalog and S3 versioning, and (4) includes data quality gates, testing, and a rollback plan if a breaking schema arrives. Include concrete services, schemas, and testing steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a batch pipeline: ingest s3://tenant-logs/appA/YYYY/MM/DD/events.json, convert to Parquet, write to s3://tenant-data/appA/YYYY/MM/DD/ partitioned by date, register in Glue Data Catalog. Ensure",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a hybrid streaming/batch ingestion architecture where Avro events from Kinesis Data Streams are written to S3 as Parquet with hourly partitions using either a Kinesis Data Firehose delivery stream or a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Glue Spark job (DynamicFrame) to map to the canonical schema via ApplyMapping, ResolveChoice, and drop unexpected columns. Read all CSVs, fill missing fields with defaults, cast types, and write",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Glue Schema Registry to enforce backward/forward compatibility, Spark-based upserts in Glue ETL to merge late-arriving events into daily Parquet partitions using a version or sequence column for i",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical mastery of schema evolution, idempotent ingest, and cross-account governance in AWS data lakes. It ensures concrete use of Glue Schema Registry, Spark ETL patterns, and lineage controls.\n\n## Key Concepts\n- Glue Schema Registry for compatible evolution\n- Spark-based upsert/merge into Parquet per partition\n- Versioned keys for idempotence and late data handling\n- Glue Data Catalog + S3 object/versioning for lineage\n- Lake Formation for cross-account access and governance\n- Data quality gates and end-to-end testing\n\n## Code Example\n```javascript\n// Pseudocode: upsert in Glue ETL (PySpark-like)\nval newData = spark.read.json(\"s3://..../raw/date=\")\nval existing = spark.read.parquet(\"s3://..../data/date=\")\nval merged = existing.alias(\"e\").join(newData.alias(\"n\"), Seq(\"id\"), \"outer\")\n  .selectExpr(\"coalesce(n.id, e.id) as id\", \"coalesce(n.value, e.value) as value\", \"n.version as ver\")\nmerged.write.mode(\"overwrite\").partitionBy(\"date\").parquet(\"s3://..../data/date=\")\n```\n\n## Follow-up Questions\n- How would you handle schema evolution between producers and registry while minimizing downtime?\n- How would you validate rollback and ensure consumers see consistent results during a schema upgrade?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-21",
        "questionId": "q-5587",
        "question": "You run a multi-tenant SaaS that writes per-tenant JSON events to S3 at s3://saas-events/raw/{tenantId}/{YYYY}/{MM}/{DD}. Design a beginner–level pipeline to flatten and convert to Parquet, land in s3://saas-events/warehouse/{tenantId}/{YYYY}/{MM}/{DD}/, and catalog in Glue. Include partitioning by tenant and date, simple data quality checks, a schema evolution plan for new fields, a lightweight test plan, and tenant isolation in the IAM/Data Catalog?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Glue Schema Registry to enforce backward/forward compatibility, Spark-based upserts in Glue ETL to merge late-arriving events into daily Parquet partitions using a version or sequence column for i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an AWS Glue Spark job (PySpark) to read events.json from s3://events/raw/, convert it to Parquet format, and write to s3://events/processed/YYYY/MM/DD/ with date-based partitioning. Catalog the data in the Glue D…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a practical solution: use a Spark-based AWS Glue job to read s3://saas-events/raw/{tenantId}/{YYYY}/{MM}/{DD}.json, flatten nested fields, write Parquet to s3://saas-events/warehouse/{tenantId",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design idea: inbound via Kinesis Data Streams; a per-tenant schema registry (Glue Schema Registry) and a Lambda/Glue ETL stage validates JSON against tenant-specific schemas, masks PII, and writes Par",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a tenant-isolated, cost-conscious data pipeline using standard AWS tools, focusing on beginner-level ETL, data quality, and cataloging.\n\n## Key Concepts\n\n- Per-tenant partitioning and isolation in S3 and Glue\n- Glue Spark (Python/Scala) ETL to flatten JSON and write Parquet\n- Glue Data Catalog integration and partitioning\n- Simple data quality checks and schema evolution (nullable new fields)\n- Lightweight end-to-end test plan with synthetic data\n\n## Code Example\n\n```python\n# PySpark-like pseudocode for ETL\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, explode, from_json\n\nspark = SparkSession.builder.getOrCreate()\n# read per-tenant data placeholder\ndf = spark.read.json(\"s3://saas-events/raw/*/*/*/*.json\")\n# flatten example (adjust to actual schema)\ndf_flat = df.select(\"tenantId\", \"eventTime\", \"payload.*\")\n# write partitioned Parquet\nout_path = \"s3://saas-events/warehouse/\" + \"${tenantId}\" + \"/\" + \"${YYYY}\" + \"/\" + \"${MM}\" + \"/\" + \"${DD}\" + \"/\"\ndf_flat.write.partitionBy(\"tenantId\", \"date\").parquet(out_path)\n```\n\n## Follow-up Questions\n\n- How would you test late-arriving data and re-partition crawls?\n- How do you enforce tenant isolation in IAM and Glue Catalog?\n- What would you monitor for failures (alerts, retries, backfills)?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-22",
        "questionId": "q-29149",
        "question": "You operate a multi-region data lake on S3 where Region A writes evolving JSON events to s3://lake/region-a/events/yyyy/mm/dd. Design an advanced ingestion and cataloging pipeline that flattens JSON to Parquet (daily partitions), propagates schema changes to Glue Data Catalog, ensures idempotent processing and cross-region replication to a central lake, and masks PII in transit. Include services, data formats, failure handling, and end-to-end test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kinesis Data Analytics to enrich JSON streams, then write Parquet to S3 with daily partitions and register in Glue Catalog. Leverage Glue Schema Registry for evolution; implement idempotent upsert",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a multi-region ingestion and cataloging pipeline: **Architecture Overview:** 1. **Ingestion Layer:** Configure S3 Event Notifications to trigger AWS Lambda on new JSON object creation in s3://lake/region-a/events…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I would design a Spark-based AWS Glue job that reads the JSON events, flattens nested structures using Spark's built-in functions or lateral view Explode, deduplicates on a composite key (partner_event_id + ingest_ts),…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a serverless AWS Glue pipeline that reads gzipped NDJSON logs from s3://shop-raw/events/YYYY/MM/DD/events.ndjson.gz, decompresses and parses the records, applies data quality validations (eventType in allowed…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to architect a robust multi-region data pipeline with evolving schemas, idempotency, PII handling, and catalog synchronization. It evaluates practical knowledge of AWS data lake services and real-world ETL patterns.\n\n## Key Concepts\n\n- **Cross-region replication (S3 CRR):** Enables automatic asynchronous replication of objects between buckets in different AWS regions\n- **Glue Spark ETL with schema evolution:** Handles evolving data structures without breaking downstream pipelines\n- **Glue Data Catalog and Schema Registry:** Provides centralized schema management and version control for data assets\n- **Idempotent processing:** Ensures the same input produces the same output regardless of how many times it is processed, critical for reliable data pipelines\n- **PII masking in transit:** Protects sensitive data during processing through techniques like hashing and tokenization",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-23",
        "questionId": "q-30405",
        "question": "Scenario: An IoT fleet emits JSON messages with evolving schemas to S3; design an advanced ingestion pipeline that flattens to Parquet, partitions by date and device_id, uses Glue Data Catalog; enables idempotent upserts for device state; masks PII in transit/at rest; handles schema evolution with Glue Schema Registry or Avro; and includes an end-to-end test plan with data quality checks and cross-region replication?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a source-agnostic ingestion: Kinesis Data Streams for JSON, S3 event-based for CSV; land raw data in per-source/date prefixes; Glue Spark jobs write to Apache Hudi datasets in S3 Parquet with upse",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a pipeline: IoT devices emit evolving JSON to S3. AWS Glue Spark ETL jobs flatten nested JSON to Parquet, partitioned by date and device_id. Implement idempotent upserts using a deduplication table with a commit…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Hybrid ingestion design: batch feeds land CSVs into S3 under date partitions; a real-time stream of events arrives on Kinesis Data Streams and is written to S3 as JSON via Firehose. Glue Spark jobs pe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Land streaming JSON events to s3/raw/{tenant_id}/{YYYY}/{MM}/{DD}. Use a Glue Streaming ETL job to read from Kinesis or Kafka, leveraging Glue Schema Registry to handle schema evolution and capture schema drift. Write P…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design an end-to-end data ingestion system for semi-structured, evolving schemas at scale with strict data governance requirements. It evaluates knowledge across AWS services, data engineering patterns, and operational considerations.\n\n## Key Concepts\n- Ingesting evolving JSON to S3\n- Flattening to Parquet with partitioning by date and device_id\n- Idempotent upserts for device state\n- PII masking in transit and at rest\n- Schema evolution via Glue Schema Registry or Avro\n- Glue Data Catalog integration\n- Cross-region data replication and comprehensive testing\n\n## Code Example\n```python\n# PySpark sketch for flattening and upsert stamping (conceptual)\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, to_json, from_json, current_timestamp\n\ndef flatten_and_upsert(batch_df, batch_id, dedup_table):\n    flattened = batch_df.select(\n        col(\"device_id\"),\n        col(\"timestamp\"),\n        col(\"payload\").getItem(\"temperature\").alias(\"temperature\"),\n        col(\"payload\").getItem(\"humidity\").alias(\"humidity\"),\n        col(\"payload\").getItem(\"metadata\").alias(\"metadata\")\n    ).withColumn(\"processed_ts\", current_timestamp())\n    \n    flattened.createOrReplaceTempView(\"incoming\")\n    \n    dedup_query = \"\"\"\n        SELECT i.* FROM incoming i\n        LEFT JOIN {0} d ON i.device_id = d.device_id \n            AND i.timestamp = d.timestamp\n        WHERE d.device_id IS NULL\n    \"\"\".format(dedup_table)\n    \n    unique_records = spark.sql(dedup_query)\n    unique_records.writeTo(dedup_table).append()\n    \n    return unique_records\n```",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-24",
        "questionId": "q-2653",
        "question": "Scenario: A global product analytics data lake ingests streaming clickstream events from mobile apps with evolving JSON schemas containing PII. You must mask PII in the shared dataset before analytics, support cross-account access, and keep raw data auditable for compliance. Design end-to-end ingestion using AWS (Kinesis or MSK, Glue, S3, Lake Formation/Macie, KMS). Address schema evolution, late data, data quality, testing, and rollout?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-region data lake architecture that replicates regional raw JSON data into a centralized Iceberg-based data warehouse. Implement S3 Cross-Region Replication with KMS encryption, use AWS Glue ETL jobs to tr…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-branch streaming: ingest with Kinesis (or MSK); route to a masking Spark job in Glue that redacts PII but preserves IDs; emit masked Parquet to S3 in daily partitions (date, region, app). Use Glue",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Outline a practical solution: use a Spark-based AWS Glue job to read s3://saas-events/raw/{tenantId}/{YYYY}/{MM}/{DD}.json, flatten nested fields, write Parquet to s3://saas-events/warehouse/{tenantId",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a streaming-to-ML pipeline: land JSON in Kinesis Data Streams, deduplicate by event_id, transform to Parquet in S3 with daily partitions, register in Glue Data Catalog, and feed SageMaker Feat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\\nThis question probes practical privacy-preserving data ingestion at scale, cross-account access, and governance.\\n\\n## Key Concepts\\n- Streaming data pipelines (Kinesis/MSK)\\n- PII masking in ETL (Glue Spark UDFs)\\n- Data lake governance (Glue Catalog, Lake Formation)\\n- PII discovery (Macie) and KMS-based encryption\\n\\n## Code Example\\n```python\\n# Glue Spark masking example (conceptual)\\ndef mask(record):\\n  record['email'] = hash_value(record.get('email'))\\n  record['phone'] = mask_phone(record.get('phone'))\\n  return record\\n```\\n\\n## Follow-up Questions\\n- How would you test schema evolution and backward compatibility across partitions?\\n- How would you monitor data quality and alert on masking failures in production?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-01T06:27:37.682Z",
    "version": 1
  }
]