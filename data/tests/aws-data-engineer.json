[
  {
    "id": "test-aws-data-engineer",
    "channelId": "aws-data-engineer",
    "channelName": "AWS Data Engineer",
    "title": "AWS Data Engineer Knowledge Test",
    "description": "Test your understanding of AWS Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "aws-data-engineer-q-0",
        "questionId": "q-10167",
        "question": "You have a global data lake ingesting semi-structured JSON events from mobile apps into S3 as daily Parquet partitions by region and tenant. Design an end-to-end pipeline using Kinesis Firehose, Glue Data Catalog, and Lake Formation to enforce per-tenant isolation, handle schema drift, implement GDPR tombstone deletes, and test end-to-end in a multi-region setup. Include data quality checks and governance considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Glue Spark job with a pre-scan to detect file delimiter and headers, unify to a canonical schema, infer schema via a Glue crawler, and write Parquet partitioned by date and region. Include basic",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Kinesis Data Firehose with a Lambda-based preprocessor to transform streaming trade events into Parquet in S3, partitioned by date. Enable cross-region replication to a secondary bucket and enforc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a comprehensive multi-tenant data lake pipeline using Kinesis Firehose, Glue Data Catalog, and Lake Formation. **Pipeline Architecture:** Configure Kinesis Firehose with a Lambda transformation function (or built…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a multi-source ingestion: Kinesis streams and S3 dumps fed into Spark-based Glue ETL writing Iceberg Parquet tables on S3, partitioned by date and registered in Glue Data Catalog. Ensure ide",
            "isCorrect": false
          }
        ],
        "explanation": "This question validates expertise in building governed multi-tenant data lakes on AWS with emphasis on security, compliance, and operational reliability. Key concepts tested: (1) Lake Formation row-level security with TFAC for true tenant isolation - not just table-level permissions; (2) Glue Schema Registry for versioned schema management with proper compatibility settings - crawlers alone cannot handle drift; (3) GDPR compliance requires both soft-delete markers (logical deletion) AND physical compaction for compliance - simple file deletion is insufficient; (4) Multi-region requires both data replication (S3 CRR) AND metadata governance strategy across regions; (5) Data quality should be automated via Glue Data Quality rules or Deequ for continuous validation; (6) Governance encompasses IAM, CloudTrail auditing, Lake Formation permission tracking, and tag-based access control. The answer demonstrates end-to-end thinking from ingestion through deletion, addressing both technical implementation and regulatory requirements.",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-1",
        "questionId": "q-20135",
        "question": "An IoT fleet uploads hourly JSON telemetry to s3://iot-telemetry/global/YYYY/MM/DD/. Late data can arrive up to 2 hours after the hour. Design an end-to-end AWS pipeline: hot Parquet table for last 14 days and archival Parquet for older data, with schema evolution, idempotent upserts, and fast analytics in Athena. Use S3, Glue, and an Iceberg/Hudi table on Spark (EMR/Glue). How would you implement and test it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use AWS DMS to capture CDC from all sources into an S3 landing bucket, convert JSON to Parquet via Glue ETL with dynamic frames, partition by date, and store in a multi-region bucket with S3 replicati",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use AWS Glue to read daily partner CSVs from s3://partner-data/campaigns/YYYY/MM/DD/campaign.csv, write Parquet to s3://data-lake/warehouse/campaigns/YYYY/MM/DD, partitioned by date, and register in G",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Apache Iceberg on Spark (EMR or Glue) with a tiered storage strategy: a hot table for the last 14 days partitioned by day, with older data archived to cold Parquet storage. Ingest telemetry using MERGE INTO statemen…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Leverage Kinesis Data Streams to ingest protobuf events, and manage evolving schemas with AWS Glue Schema Registry. Run a Spark Structured Streaming job on Glue that reads the stream, applies the Regi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates advanced data lake architecture skills, including upsert handling, Iceberg/Hudi table formats, late data processing, tiered storage design, and testing strategies for data pipelines.\n\n## Key Concepts\n\n- **Iceberg/Hudi upserts**: Using MERGE INTO for idempotent writes that handle duplicates from retries\n- **Partition pruning**: Daily partitioning enables efficient queries on recent data while reducing scan costs\n- **Schema evolution**: Iceberg supports add, rename, and remove columns without table rewrites\n- **Data provenance**: Tracking source files and timestamps enables debugging and auditability\n- **Late data handling**: Watermarking or arrival-time-based deduplication ensures accurate results\n\n## Code Example\n\n```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, window, current_timestamp\n\nspark = SparkSession.builder.getOrCreate()\n\ntelemetry_df = spark.read.json(\"s3://iot-telemetry/global/\")\n\nspark.sql(\"\"\"\nMERGE INTO iot_db.telemetry_table t\nUSING (\n    SELECT \n        device_id,\n        timestamp,\n        payload,\n        file_name,\n        arrival_time\n    FROM staging_table\n    WHERE arrival_time > CAST('{checkpoint}' AS TIMESTAMP)\n) s\nON t.device_id = s.device_id AND t.timestamp = s.timestamp\nWHEN MATCHED THEN UPDATE SET *\nWHEN NOT MATCHED THEN INSERT *\n\"\"\")\n```\n\n## Follow-up Questions\n\n- **How would you monitor the pipeline?**\n  Implement CloudWatch metrics for record counts, latency, and error rates. Use Glue job monitoring and S3 access logging.\n\n- **How would you test failure modes?**\n  Simulate late data arrival, duplicate writes, schema changes, and partition failures. Validate idempotency by re-running jobs with the same input.",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-2",
        "questionId": "q-12216",
        "question": "Daily user activity CSVs arrive in s3://cust-lake/raw/users/YYYY/MM/DD/events.csv. Design a beginner-level ingestion to land as Parquet in s3://cust-lake/processed/users/YYYY/MM/DD/events.parquet, partitioned by date, with masking of PII (emails, phone) in the ETL, and a Glue Data Catalog table. Include data quality checks and a simple end-to-end test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kinesis Data Streams with Global Streams to replicate real-time events from us-east-1 to us-west-2, with Firehose delivering to S3 Parquet in both regions. Glue Catalog stays in sync via a centralized drift detectio…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Glue Spark ETL job that MERGEs incoming events into Iceberg on S3 using event_id as the primary key, enabling idempotent upserts and daily partitions. Enable Iceberg schema evolution, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a beginner pipeline: trigger from new CSVs in s3://cust-lake/raw/users/YYYY/MM/DD/events.csv, run a Glue Spark job that masks PII (emails via regex, phone numbers) to tokens, writes Parquet",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a hybrid streaming/batch pipeline in AWS for a multi-tenant analytics platform. Ingest streaming events from Kinesis and batch exports to S3; normalize to Parquet with per-tenant prefixes and d",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests ability to design a compliant data pipeline with privacy in mind, plus Glue cataloging and simple tests.\n\n## Key Concepts\n- PII masking in ETL with Spark\n- Parquet partitioning and Glue Catalog\n- Lightweight data quality checks\n- End-to-end testing strategies\n\n## Code Example\n```javascript\n// Pseudo Spark masking in JS-like syntax\nconst df = readCsv('s3://cust-lake/raw/users/YYYY/MM/DD/events.csv');\nconst masked = df\n  .withColumn('email', regexReplace(col('email'), /\\b[\\w.%-]+@[\\w.-]+\\.[A-Za-z]{2,}\\b/, '<REDACTED>'))\n  .withColumn('phone', regexReplace(col('phone'), /\\d{3}-\\d{3}-\\d{4}/, '<REDACTED>'));\nmasked.writeParquet('s3://cust-lake/processed/users/YYYY/MM/DD/events.parquet');\n```\n\n## Follow-up Questions\n- How would you validate that masking did not alter non-PII fields?\n- How to ensure idempotent processing without duplicates?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-3",
        "questionId": "q-7353",
        "question": "Design a beginner-friendly ingestion pipeline for two app sources that emit daily JSON logs into separate S3 prefixes: app1/logs/YYYY/MM/DD/log.json and app2/logs/YYYY/MM/DD/log.json. Create a pipeline to land a unified Parquet dataset in s3://data-lake/cleaned/logs/YYYY/MM/DD/, partitioned by date, using AWS Glue for ETL and cataloging. Include schema evolution handling for new optional fields, deduplication by event_id, basic data quality checks, and a minimal end-to-end test plan. Which steps and services would you choose?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use AWS Glue Job with bookmarks to read s3://corp-lake/raw/appX/YYYY/MM/DD/events.json, flatten nested fields, coerce types, and mask PII (redact email local-part). Write Parquet to s3://corp-lake/pro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use two Glue crawlers to infer schemas, a PySpark Glue job to union both sources, deduplicate on event_id, normalize optional fields into a unified schema, and write Parquet partitioned by date to s3:",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a pipeline: IoT devices emit evolving JSON to S3. AWS Glue Spark ETL jobs flatten nested JSON to Parquet, partitioned by date and device_id. Implement idempotent upserts using a deduplication table with a commit…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest JSON events regionally via Kinesis Data Streams, fan out to S3 Parquet with hourly partitions using Apache Hudi for upserts and schema evolution. Use Glue Schema Registry + Glue Data Catalog; q",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a practical, beginner-friendly ETL using AWS Glue that handles multiple sources, schema drift, dedup, and validation, plus a concrete end-to-end test plan.\n\n## Key Concepts\n- Glue crawlers and dynamic frames for schema inference and evolution\n- Merging JSON sources with a unified Parquet target and date partitioning\n- Deduplication on event_id to handle replays\n- Basic data quality checks (nulls, types) and schema evolution strategy\n- End-to-end validation using Athena queries and simple alerts\n\n## Code Example\n```python\n# PySpark skeleton (Glue job) to deduplicate and unify schemas\nfrom awsglue.context import GlueContext\nfrom pyspark.context import SparkContext\nsc = SparkContext()\ngc = GlueContext(sc)\nspark = gc.spark_session\npaths = [\"s3://data-lake/raw/app1/\", \"s3://data-lake/raw/app2/\"]\nf = spark.read.json(paths).selectExpr(\"*\", \"cast(event_id as string) as event_id\")\ndf = f.dropDuplicates([\"event_id\"]).withColumnRenamed(\"date\", \"partition_date\")\ndf.write.parquet(\"s3://data-lake/cleaned/logs/YYYY/MM/DD/\", mode=\"append\")\n```\n\n## Follow-up Questions\n- How would you handle late-arriving data or corrections after initial ingest?\n- How would you monitor data quality and schema drift over time?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-4",
        "questionId": "q-2060",
        "question": "Design a multi-region, streaming-plus-batch ingestion pipeline for a mobile app that emits JSON events at high volume. Ingest via Kinesis Data Streams to S3 Parquet using Apache Iceberg tables with a Glue catalog, supporting upserts, deletes, and schema evolution. Include multi-source CDC, data quality checks, governance via Lake Formation, and end-to-end testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Kinesis Data Streams; mask PII in a streaming Lambda before S3; store as Parquet with tenant/date partitions; enforce tenancy via Lake Formation permissions and per-tenant Glue databases; h",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Apache Iceberg on S3 to support CDC upserts and full history. Ingest via DMS into a staging Parquet layer, then Spark MERGE INTO to the Iceberg table; maintain a separate 'latest' snapshot for das",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Apache Hudi on S3 to enable upserts and deletes with per-tenant partitions. Ingest telemetry via streaming into S3, process with Spark to write to Hudi tables, and run periodic compaction. Glue Da",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage Iceberg tables on S3 with a Glue catalog, landing JSON from Kinesis via a streaming job that MERGEs into Iceberg to support upserts/deletes; replicate CDC from multiple sources using DMS across regions with sch…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes design for scalable, auditable data lakes across regions, balancing mutating events with schema changes and governance.\n\n## Key Concepts\n- Apache Iceberg on S3 with Glue catalog\n- Upserts/deletes via MERGE semantics\n- Multi-source CDC (DMS) across regions\n- Schema evolution and data quality checks (Deequ)\n- Lake Formation governance and end-to-end testing\n\n## Code Example\n```javascript\nMERGE INTO iceberg_db.events AS t\nUSING staging.events AS s\nON t.event_id = s.event_id\nWHEN MATCHED THEN UPDATE SET t.payload = s.payload, t.ts = s.ts\nWHEN NOT MATCHED THEN INSERT (event_id, payload, ts) VALUES (s.event_id, s.payload, s.ts)\n```",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-5",
        "questionId": "q-21284",
        "question": "Design an intermediate-level, multi-source ingestion to a single analytics table in S3 using upserts. Data sources: mobile app events in JSON via Kinesis, web events in Parquet via Kinesis Firehose to S3, and daily CSV sales logs on S3. Explain how you would implement incremental upserts with Apache Hudi (or Iceberg) on Glue Spark, including idempotence, late-arriving data handling, schema evolution, daily partitioning, and access via Glue Catalog/Athena. Include testing and monitoring plan and trade-offs between Hudi and Iceberg?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an AWS Glue job triggered by S3 PUT events or a daily schedule to read invoices.csv, apply a fixed target schema, fill missing columns with defaults, cast data types appropriately, and write Parquet to s3://company-…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Glue PySpark job that reads s3://lake/raw/events/YYYY/MM/DD/events.json, flattens nested JSON, fills missing with nulls, and writes Parquet to s3://lake/curated/events/YYYY/MM/DD/ partitio",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an AWS Glue Spark job to read JSON logs from s3://logs/raw/daily/YYYY/MM/DD/events.json, enforce data quality by filtering out rows with null values in required fields, and write partitioned Parquet files to s3://lo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a multi-source ingestion pipeline to a single analytics table in S3 using upserts. Data sources include: mobile app events in JSON format via Kinesis, web events in Parquet format via Kinesis Firehose to S3, and…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates the ability to design an end-to-end data ingestion pipeline with upsert capabilities, handle late-arriving data, and manage schema evolution across multiple sources—skills essential for building production-grade data platforms at scale.\n\n## Key Concepts\n\n- Upsertable data lake formats using Hudi or Iceberg\n- AWS Glue Spark jobs and Data Catalog integration\n- Idempotent deduplication and late-arriving data handling\n- Partitioning strategy and cross-source schema normalization\n- Testing and monitoring for data quality and pipeline reliability\n\n## Code Example\n\n```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, when, to_timestamp\n\nspark = SparkSession.builder(\n    .appName(\"MultiSourceIngestion\")\n    .config(\"spark.serializer\", \"org.apache.spark.serializer.KryoSerializer\")\n    .config(\"spark.sql.extensions\", \"org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions\")\n    .getOrCreate()\n\n# Define the target table path\ntable_path = \"s3://analytics-bucket/warehouse/analytics_table\"\n\n# Read mobile events from Kinesis\nmobile_df = spark.readStream \\\n    .format(\"kinesis\") \\\n    .option(\"streamName\", \"mobile-events-stream\") \\\n    .option(\"initialPosition\", \"LATEST\") \\\n    .load()\n\n# Parse JSON and normalize schema\nmobile_parsed = mobile_df.select(col(\"data.*\")).withColumn(\"event_timestamp\", to_timestamp(col(\"timestamp\")))\n\n# Read web events from S3 (Parquet)\nweb_df = spark.read.parquet(\"s3://raw-bucket/web-events/\")\n\n# Read sales logs from S3 (CSV)\nsales_df = spark.read.csv(\"s3://raw-bucket/sales-logs/\", header=True)\n\n# Normalize schemas across all sources\ndef normalize_schema(df, source_name):\n    return df.select(\n        col(\"event_id\").alias(\"record_id\"),\n        col(\"event_timestamp\"),\n        col(\"event_type\"),\n        col(\"user_id\"),\n        col(\"properties\"),\n        col(\"event_timestamp\").cast(\"date\").alias(\"partition_date\"),\n        col(\"event_timestamp\").cast(\"timestamp\").alias(\"ingestion_timestamp\"),\n        col(\"source\")\n    )\n\nmobile_normalized = normalize_schema(mobile_parsed, \"mobile\").withColumn(\"source\", lit(\"mobile\"))\nweb_normalized = normalize_schema(web_df, \"web\").withColumn(\"source\", lit(\"web\"))\nsales_normalized = normalize_schema(sales_df, \"sales\").withColumn(\"source\", lit(\"sales\"))\n\n# Union all sources\nunified_df = mobile_normalized.unionByName(web_normalized, sales_normalized)\n\n# Write to Iceberg table with upsert using MERGE INTO\nunified_df.writeTo(table_path).partitionedBy(\"partition_date\").createOrReplace()\n\n# For incremental upserts with deduplication\nspark.sql(f\"\"\"\n    MERGE INTO {table_path} target\n    USING (\n        SELECT record_id, MAX(event_timestamp) as event_timestamp, MAX(event_type) as event_type,\n               MAX(user_id) as user_id, MAX(properties) as properties,\n               MAX(partition_date) as partition_date,\n               MAX(ingestion_timestamp) as ingestion_timestamp, MAX(source) as source\n        FROM source_table\n        GROUP BY record_id\n    ) source\n    ON target.record_id = source.record_id\n    WHEN MATCHED AND source.ingestion_timestamp > target.ingestion_timestamp THEN\n        UPDATE SET *\n    WHEN NOT MATCHED THEN\n        INSERT *\n\"\"\")\n```\n\n## Schema Evolution Handling\n\n```python\n# Handle schema evolution with Iceberg\nfrom pyspark.sql.types import StructType, StructField, StringType, IntegerType, TimestampType\n\n# Define evolved schema with new fields\nnew_schema = StructType([\n    StructField(\"record_id\", StringType(), False),\n    StructField(\"event_timestamp\", TimestampType(), True),\n    StructField(\"event_type\", StringType(), True),\n    StructField(\"user_id\", StringType(), True),\n    StructField(\"properties\", StringType(), True),\n    StructField(\"partition_date\", StringType(), True),\n    StructField(\"ingestion_timestamp\", TimestampType(), True),\n    StructField(\"source\", StringType(), True),\n    StructField(\"new_field\", StringType(), True)  # New field added\n])\n\n# Iceberg handles schema evolution automatically:\n# - ADD: New columns are added seamlessly\n# - DROP: Columns can be removed (data still readable)\n# - RENAME: Columns can be renamed\n# - UPDATE: Type promotions (int → long)\n```\n\n## Late-Arriving Data Handling\n\n```python\n# Configure late-data handling with watermarking\nfrom pyspark.sql.functions import window\n\n# Set watermark for late data (e.g., 7 days)\nwatermarked_df = unified_df \\\n    .withWatermark(\"event_timestamp\", \"7 days\") \\\n    .groupBy(window(\"event_timestamp\", \"1 hour\"), \"record_id\") \\\n    .agg(max(\"*\").alias(\"latest_record\"))\n\n# Upsert with late-data handling\nspark.sql(f\"\"\"\n    MERGE INTO {table_path} target\n    USING (\n        SELECT * FROM (\n            SELECT *, ROW_NUMBER() OVER (PARTITION BY record_id ORDER BY event_timestamp DESC) as rn\n            FROM source_with_watermark\n        ) WHERE rn = 1\n    ) source\n    ON target.record_id = source.record_id\n       AND target.event_timestamp >= source.event_timestamp - INTERVAL '7' DAYS\n    WHEN MATCHED AND source.event_timestamp > target.event_timestamp THEN\n        UPDATE SET *\n    WHEN NOT MATCHED THEN\n        INSERT *\n\"\"\")\n```\n\n## Testing Plan\n\n1. **Unit Tests**: Test schema normalization functions, deduplication logic\n2. **Integration Tests**: Verify end-to-end pipeline with mock data sources\n3. **Data Quality Tests**: Validate record counts, null checks, uniqueness constraints\n4. **Performance Tests**: Measure throughput and latency under load\n\n## Monitoring Plan\n\n- **CloudWatch Metrics**: Pipeline latency, record counts, error rates\n- **Data Quality Checks**: Deequ or Great Expectations for schema and value validation\n- **Table Metrics**: Iceberg provides built-in metadata tables for file sizes, commit history\n- **Alerting**: SNS notifications on pipeline failures or data quality issues\n\n## Hudi vs Iceberg Trade-offs\n\n| Aspect | Hudi | Iceberg |\n|--------|------|---------|\n| Schema Evolution | Limited (add only) | Full support (add, drop, rename, update) |\n| Time Travel | Via versioning | Native and efficient |\n| AWS Integration | Native (Glue, Athena) | Native (Glue, Athena) |\n| Performance | Optimized for upserts | Optimized for analytics |\n| Community | Active | Very active |\n| Hidden Partitioning | No | Yes |\n| Partition Evolution | Limited | Yes |",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-6",
        "questionId": "q-28661",
        "question": "You manage a multi-region data lake for **Cloudflare** and **Netflix**-scale workloads. A new JSON source lands in S3 with evolving schema and PII. Design a production-grade, AWS-native ingestion and observability pipeline that delivers data as Parquet, tracks end-to-end lineage, handles schema drift, and enforces per-dataset RBAC via Lake Formation. Include concrete components and data flows?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a streaming-to-ML pipeline: land JSON in Kinesis Data Streams, deduplicate by event_id, transform to Parquet in S3 with daily partitions, register in Glue Data Catalog, and feed SageMaker Feat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a practical solution: use a Spark-based AWS Glue job to read s3://saas-events/raw/{tenantId}/{YYYY}/{MM}/{DD}.json, flatten nested fields, write Parquet to s3://saas-events/warehouse/{tenantId",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an event-driven pipeline with Glue ETL, Glue Data Catalog, and Lake Formation RBAC. Enable Glue lineage for source-to-target mapping and S3-level encryption. Use Glue Schema Registry for evolution",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-region Kinesis Data Streams feeding into Kinesis Firehose to S3 in Parquet, partitioned by date and region. Deduplicate with a DynamoDB registry and idempotent producers to achieve effectively",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end data observability, handle schema drift, and RBAC in a multi-region lake.\n\n## Key Concepts\n- Glue lineage\n- Lake Formation RBAC\n- Schema Registry\n- Data Quality Rules\n- Upserts/Idempotency\n- EventBridge observability\n\n## Code Example\n```python\n# Skeleton Spark processing for idempotent upsert\ndef upsert(df, target_table, key):\n    # merge into target using key, write with committer\n    pass\n```\n\n## Follow-up Questions\n- How would you scale lineage collection across thousands of datasets?\n- What are the trade-offs of using Glue Data Quality vs external validation tools?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-7",
        "questionId": "q-23412",
        "question": "Ingest JSON events from a mobile app into AWS in real-time. Design an end-to-end pipeline that lands Parquet files in S3 partitioned by date and tenant_id, uses Glue for ETL and cataloging, and supports schema evolution, idempotent upserts, and data quality checks with a dead-letter bucket. Include service choices, handling for backfills, testing strategy, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Advanced pipeline design: Regional JSON event streams are ingested through Kinesis Data Firehose or Kafka MSK, then processed by a Spark-based AWS Glue job that flattens nested JSON structures and writes Parquet files t…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest via Kinesis Data Streams, land to S3 with Firehose creating daily prefixes, use Glue Data Catalog for schema, and Spark ETL for upserts with Apache Hudi on S3. Handle schema evolution with Glue",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Ingest JSON events regionally via Kinesis Data Streams, fan out to S3 Parquet with hourly partitions using Apache Hudi for upserts and schema evolution. Use Glue Schema Registry + Glue Data Catalog; q",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage IoT Core to push protobuf telemetry into Kinesis Data Firehose, which writes Parquet to S3 with daily partitions. Use Glue Data Catalog with a schema registry for evolution (AVRO/Protobuf). I",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design an end-to-end, scalable, and fault-tolerant data ingestion pipeline with evolving schemas, upserts, and data quality controls in an AWS-centric data lake.\n\n## Key Concepts\n- Kinesis Data Streams and Firehose for real-time ingestion and landing\n- AWS Glue Data Catalog and Glue Schema Registry for discovery and evolution\n- Apache Hudi (on S3 via Glue Spark) for upserts and historical queries\n- Parquet format with date and tenant_id partitions\n- Dead-letter queue/bucket for bad records\n- End-to-end testing with replayable sample events\n\n## Code Example\n```python\n# PySpark snippet sketch (in Glue Spark job) for upsert with Hudi\nfrom pyspark.context import SparkContext\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col\nspark = SparkSession.builder.getOrCreate()\ndf = spark.read.parquet(\"s3://lake/raw/events/*/\")\nupsert = df.select(\"id\", \"tenant_id\", \"event_time\", \"payload\").withColumn(\"_hoodie_op\", lit(\"upsert\"))\nupsert.write.format(\"org.apache.hudi\").options(\n  HoodieTableType=\"MERGE_ON_READ\",\n  HoodieDataSourceWriteConfig.TABLE_NAME=\"events\",\n  HoodieOptions={\"hoodie.insert.shuffle.path\":\"/tmp\"}\n).mode(\"append\").save(\"s3://lake/processed/events/tenant_id/date/\")\n```\n\n## Follow-up Questions\n- How would you handle cross-region replication and cost optimization?\n- What monitoring would you place for data freshness and DLQ backlogs?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-8",
        "questionId": "q-5980",
        "question": "For a multi-account AWS data lake used by Adobe/Tesla, design an end-to-end data governance solution that uses Lake Formation column-level permissions, Glue Data Catalog, and S3 tagging to restrict PII in a customer_events dataset. Show how you would grant a data scientist read access to non-PII columns only, implement masking in Athena/Glue queries, and verify access and auditability with tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Grant data scientist read access only to non-PII columns via Lake Formation column-level permissions on the Glue Data Catalog, explicitly denying access to PII columns. Implement data masking by creating views or UDFs t…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Kinesis Data Streams + Firehose to land JSON into S3 as Parquet, partitioned by tenant_id/date. Catalog in Glue; enforce per-tenant isolation with Lake Formation by granting database/table permiss",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an event-driven pipeline with Glue ETL, Glue Data Catalog, and Lake Formation RBAC. Enable Glue lineage for source-to-target mapping and S3-level encryption. Use Glue Schema Registry for evolution",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Land partner JSON events into S3 landing bucket, run a Glue ETL to convert to Parquet, partition by country and date, and catalog with Glue. Use Lake Formation for fine‑grained access. Implement schem",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical data governance design in a multi-account AWS data lake, focusing on column-level access controls, data masking, and auditability—critical capabilities for enterprise-scale data programs at companies like Adobe and Tesla.\n\n## Key Concepts\n- Lake Formation column-level permissions\n- Glue Data Catalog and cross-account access\n- S3 object tagging for data classification\n- Data masking with views and UDFs\n- Auditing with Lake Formation logs and CloudTrail\n\n## Code Example\n```javascript\n// Pseudo CLI: grant non-PII column access (LF) and illustrate masking\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-9",
        "questionId": "q-24413",
        "question": "Scenario: A streaming analytics project ingests high-volume trade events from Kinesis into S3 as Parquet, partitioned by date. design a scalable, cost-aware ingest pipeline that uses Glue Catalog, enables cross-region replication to a second analytics lake, enforces Lake Formation access controls, handles schema evolution, includes data quality checks, and provides a rollback path for late-arriving data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kinesis Data Firehose with a Lambda-based preprocessor to transform streaming trade events into Parquet in S3, partitioned by date. Enable cross-region replication to a secondary bucket and enforc",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage AWS Glue Schema Registry with a compatibility policy, treat new fields as optional, and enable mergeSchema when writing Parquet. Ingest JSON with a Glue DynamicFrame, map types, and write dai",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Streams with Glue Schema Registry to enforce evolving schemas (Avro/JSON). Stream to Spark Structured Streaming, write Parquet to S3 with hourly partitions, and deduplicate by event_i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Glue Crawlers to populate the Data Catalog, ETL with Glue Studio (Python) to convert raw JSON to Parquet in s3://fin-data/processed/apiX/YYYY/MM/DD, partition by date and product, and query via At",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end streaming data lakes with cost and governance in mind.\n\n## Key Concepts\n- Kinesis Firehose with Lambda transform; Parquet; date partitions\n- Glue Catalog; schema evolution; Lake Formation access controls\n- Cross-region replication; tombstones for late-arriving data; idempotent writes\n\n## Code Example\n\n```javascript\n// Example: minimal Firehose transform shim\nconst AWS = require('aws-sdk');\nexports.handler = async (event) => { /* validate and pass through */ };\n```\n\n## Follow-up Questions\n- How would you test schema drift and automate schema updates?\n- How do you monitor cross-region replication consistency?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-10",
        "questionId": "q-30248",
        "question": "You collect high-velocity JSON events from IoT devices feeding into AWS; design an end-to-end ingestion and storage pipeline that lands into S3 in Parquet with daily partitions, validates schemas, supports schema evolution, and is idempotent while handling late-arriving events; specify services, how you test end-to-end, monitoring, and cost considerations. End with ?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive multi-tenant data lake pipeline using Kinesis Firehose, Glue Data Catalog, and Lake Formation. **Pipeline Architecture:** Configure Kinesis Firehose with a Lambda transformation function (or built…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed solution: Ingest from KDS to S3 via a Glue streaming ETL or Firehose to Parquet, with daily partitions. Use Glue Schema Registry to version and evolve the nested JSON schema; keep tx_timestam",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage AWS Glue Schema Registry with a compatibility policy, treat new fields as optional, and enable mergeSchema when writing Parquet. Ingest JSON with a Glue DynamicFrame, map types, and write dai",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Kinesis Data Streams for input, Glue Schema Registry to enforce and evolve JSON schemas, and a streaming layer (Lambda or KDA) to validate data and produce Parquet in S3 with daily partitions. Ens",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical, production-ready thinking: streaming ingestion, schema evolution, idempotency, late-arriving data, and end-to-end testing under budget constraints.\n\n## Key Concepts\n- Streaming ingestion with Kinesis Data Streams and optional Firehose path\n- Glue Schema Registry for versioned JSON schemas and evolution\n- Parquet with daily partitions in S3\n- Idempotent writes via per-event dedup keys stored (e.g., DynamoDB)\n- Late data handling with watermarking and bounded backfill\n- End-to-end testing with synthetic bursts, replay, and monitoring\n\n## Code Example\n```javascript\n// Lambda-like pseudo-code for dedupe check\nconst AWS = require('aws-sdk');\nconst ddb = new AWS.DynamoDB.DocumentClient();\nexports.handler = async (event) => {\n  const id = event.id;\n  const exists = await ddb.get({TableName:'dedupe', Key:{id}}).promise();\n  if (exists.Item) return {status:'duplicate'};\n  await ddb.put({TableName:'dedupe', Item:{id}}).promise();\n  // proceed with validation and write to Parquet path in S3\n  return {status:'ok'};\n};\n```\n\n## Follow-up Questions\n- How would you migrate schema across regions without downtime?\n- How would you backfill data when a schema change occurs?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-11",
        "questionId": "q-9000",
        "question": "Stream PostgreSQL CDC via Debezium into MSK; use Glue Spark jobs to materialize into S3 as Apache Iceberg tables partitioned by day. Handle upserts/deletes with Iceberg tombstones and rewriting; manage schema evolution via Iceberg's schema evolution; enforce data quality with Great Expectations; test end-to-end with simulated CDC; enable cross-region S3 replication and Iceberg catalog replication?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kinesis Data Streams with a Flink app (KDA) for streaming enrichment, writing upserts to S3 via Apache Hudi (or Iceberg) to enable time travel and current views. Glue Schema Registry manages evolv",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Debezium CDC to MSK, Glue Spark to write to Iceberg on S3 partitioned by date. Upserts/deletes use Iceberg tombstones and rewrite; evolving schemas via Iceberg schema evolution. Integrate Gre",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Apache Iceberg with Glue Catalog, Spark on EMR to maintain an upsertable Iceberg table on S3. Define a primary key (id), partition by date, enable schema evolution, and perform MERGE INTO updates",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enable AWS Glue job bookmarks to ensure idempotent processing by tracking processed files. In the ETL script, read JSON files from s3://app-raw/events/YYYY/MM/DD/events.json, flatten nested JSON structures, normalize ti…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical CDC-to-ship integration, Iceberg-on-S3 proficiency, and end-to-end reliability across regions.\n\n## Key Concepts\n- CDC capture: Debezium, PostgreSQL, MSK\n- Data format: Iceberg on S3, day partitions\n- Upserts/Deletes: Iceberg tombstones, rewrite\n- Schema evolution: Iceberg evolution\n- Quality & testing: Great Expectations, synthetic CDC tests\n- Observability: monitoring, cross-region replication\n\n## Code Example\n```python\n# PySpark example: append to Iceberg table\ndf.write.format(\"iceberg\").mode(\"append\").save(\"s3://bucket/iceberg/db.table\");\n```\n\n## Follow-up Questions\n- How would you handle late-arriving data and partition pruning?\n- How would you validate lineage and catalog consistency across regions?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-12",
        "questionId": "q-7959",
        "question": "Design an end-to-end pipeline to ingest high-volume IoT telemetry where devices emit protobuf payloads into AWS; propose a streaming path using Kinesis, a Spark/Glue ETL to Parquet in S3 partitioned by day, handle protobuf schema evolution via Glue Schema Registry, ensure idempotent processing and data-quality checks, and outline verification and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kinesis Data Streams to ingest protobuf payloads, decode them with Glue Schema Registry, and run a Glue Spark streaming job to parse and write Parquet to S3 partitioned by date. Manage protobuf schema evolution thro…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "In a multi-tenant, multi-account data lake, ingest JSON events from microservices via Kinesis Data Streams, land to S3 as Parquet partitioned by date and tenant_id. Use Glue for ETL and cataloging; en",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run a Glue Crawler on the S3 path to automatically update the Data Catalog with new fields",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Lake Formation governance with per-tenant databases mapped to S3 prefixes (s3://lake/tenant-id/...). Expose cross-account access via resource links and tag-based ACLs, audited by Lake Formation an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of protobuf ingestion, schema evolution, and end-to-end testing in AWS data lakes.\n\n## Key Concepts\n- Streaming ingestion, protobuf decoding, Glue Schema Registry, Parquet partitioning\n- Idempotent processing, upserts, data quality checks\n- End-to-end testing, cost awareness\n\n## Code Example\n```python\n# Pseudocode: Spark Structured Streaming in Glue to decode protobuf and write Parquet\nfrom pyspark.sql.functions import col\ninput_df = spark.readStream.format(\"kinesis\").option(\"streamName\",\"telemetry\").load()\ndecoded = input_df.withColumn(\"payload\", decode_protobuf(col(\"data\")))\nquality_df = decoded.filter(validate_data_quality(col(\"payload\")))\nquality_df.writeStream.format(\"parquet\").partitionBy(\"date\").option(\"path\",\"s3://telemetry/\").start()\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-13",
        "questionId": "q-27225",
        "question": "Global streaming platform ingestion: Ingest real-time clickstream from web and mobile apps into AWS Kinesis Data Streams, then land as Apache Iceberg tables in S3 partitioned daily. Design a cross-region, fault-tolerant pipeline that supports schema evolution, upserts by event_id, and time-travel queries, while using Lake Formation for access control and Glue Data Catalog for metadata. Include testing and DR considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest streaming events via Kinesis Data Firehose to S3 as Parquet files, partitioned by date (year/month/day) for optimal query performance. Enable schema evolution using AWS Glue Schema Registry to handle evolving tra…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use KDS with a streaming ETL (Glue or Lambda) to write Parquet to S3 partitioned by tenant_id/date; catalog in Glue Data Catalog; enforce Lake Formation permissions per tenant; apply dynamic masking f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest via Kinesis Data Streams partitioned by tenant_id; encrypt at rest with per-tenant KMS keys, TLS in transit. Glue Spark ETL reads events, enriches with catalog metadata, writes Parquet to s3://",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Glue Spark ETL job that MERGEs incoming events into Iceberg on S3 using event_id as the primary key, enabling idempotent upserts and daily partitions. Enable Iceberg schema evolution, and",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses hands-on experience with Iceberg, upserts, schema evolution, cross-region DR, and data governance on AWS.\n\n## Key Concepts\n- Apache Iceberg on S3\n- Schema evolution and time travel\n- Upserts using MERGE\n- Cross-region replication\n- Lake Formation and Glue Data Catalog\n\n## Code Example\n```sql\nMERGE INTO iceberg_db.clickstream AS t\nUSING staging AS s\nON t.event_id = s.event_id\nWHEN MATCHED THEN UPDATE SET *\nWHEN NOT MATCHED THEN INSERT *;\n```\n\n## Follow-up Questions\n- How would you handle late-arriving data and schema drift?\n- What are cost and consistency tradeoffs of cross-region replication?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-14",
        "questionId": "q-29664",
        "question": "Scenario: Two daily data sources arrive: s3://data-lake/raw/events/YYYY/MM/DD/events.csv and s3://data-lake/raw/products/YYYY/MM/DD/products.csv. Build a beginner-level AWS Glue pipeline that joins on product_id, flattens nested fields, handles missing values, outputs Parquet to s3://data-lake/warehouse/combined/YYYY/MM/DD/, partitions by date, and registers a Glue Data Catalog table. Describe schema evolution handling, idempotency, and a concrete end-to-end test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Glue-based pipeline that reads daily raw logs from s3://data-lake/raw/logs/YYYY/MM/DD/logs.json, maps three schemas into a canonical schema (user_id string, timestamp timestamp, event string,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Read daily events.json from the ingest path (s3://partners/ingest/YYYY/MM/DD/), validate against a provided JSON schema with support for backward-compatible schema evolution, cast types and handle missing fields with ap…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Glue Spark job that reads both CSV files via DynamicFrames, performs a left join on product_id, casts data types, fills missing fields with defaults, and writes Parquet output to the warehouse partitioned by date.…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Architect a real-time CDC lake using Kinesis Data Streams ingested by Spark on Glue or EMR, writing to S3 as Apache Hudi upsertable tables with partitioning by date and region. Use Glue Schema Registr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests your ability to integrate multiple data sources, perform joins, and manage schema evolution at a beginner level. It also validates your understanding of idempotent processing and end-to-end testing.\n\n## Key Concepts\n\n- AWS Glue ETL using DynamicFrames and Spark\n- Left join on product_id\n- Data type casting and default values for missing fields\n- Parquet output with date partitioning\n- Glue Data Catalog integration\n- Job bookmarks for idempotency\n- Schema evolution strategy with forward compatibility\n\n## Code Example\n\n```python\nimport sys\nfrom awsglue.context import GlueContext\nfrom awsglue.job import Job\nfrom awsglue.utils import getResolvedOptions\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, lit, when, to_date\nfrom awsglue.gluetypes import *\n\nspark = SparkSession.builder.getOrCreate()\nglueContext = GlueContext(spark)\njob = Job(glueContext)\n\nargs = getResolvedOptions(sys.argv, ['JOB_NAME'])\njob.init(args['JOB_NAME'], args)\n\n# Read events with job bookmark for idempotency\nevents_dyf = glueContext.create_dynamic_frame.from_options(\n    connection_type='s3',\n    connection_options={\n        'paths': ['s3://data-lake/raw/events/'],\n        'recurse': True\n    },\n    format='csv',\n    format_options={'withHeader': True}\n)\n\n# Read products\nproducts_dyf = glueContext.create_dynamic_frame.from_options(\n    connection_type='s3',\n    connection_options={\n        'paths': ['s3://data-lake/raw/products/'],\n        'recurse': True\n    },\n    format='csv',\n    format_options={'withHeader': True}\n)\n\n# Flatten nested fields and normalize schema\ndef flatten_and_cast(dyf, source):\n    df = dyf.toDF()\n    for col_name in df.columns:\n        df = df.withColumn(col_name, col(col_name).cast('string'))\n    return DynamicFrame.fromDF(df, glueContext, source)\n\nevents_flat = flatten_and_cast(events_dyf, 'events')\nproducts_flat = flatten_and_cast(products_dyf, 'products')\n\n# Handle missing values with defaults\nevents_filled = events_flat.make_missing_values_null().resolveChoice(\n    spec=[('product_id', 'cast:string')]\n)\nproducts_filled = products_flat.make_missing_values_null().resolveChoice(\n    spec=[('product_id', 'cast:string')]\n)\n\n# Left join on product_id\njoined_dyf = Join.apply(\n    frame1=events_filled,\n    frame2=products_filled,\n    keys1=['product_id'],\n    keys2=['product_id'],\n    transformation_ctx='joined'\n)\n\n# Apply defaults for null values\ndef apply_defaults(dyf):\n    df = dyf.toDF()\n    df = df.withColumn('created_at', when(col('created_at').isNull(), lit('1970-01-01')).otherwise(col('created_at')))\n    df = df.withColumn('category', when(col('category').isNull(), lit('unknown')).otherwise(col('category')))\n    return DynamicFrame.fromDF(df, glueContext, 'with_defaults')\n\nfinal_dyf = apply_defaults(joined_dyf)\n\n# Write Parquet with date partitioning\noutput_path = 's3://data-lake/warehouse/combined/'\nglueContext.write_dynamic_frame.from_options(\n    frame=final_dyf,\n    connection_type='s3',\n    connection_options={\n        'path': output_path,\n        'partitionBy': ['dt']\n    },\n    format='parquet'\n)\n\n# Register Glue Data Catalog table\ntarget_table = glueContext.create_table_from_catalog(\n    database='warehouse',\n    table_name='combined_events',\n    schema=final_dyf.schema(),\n    partition_keys=['dt'],\n    connection_options={\n        'path': output_path\n    }\n)\n\njob.commit()\n```\n\n## Schema Evolution\n\n1. **AddColumns (forward compatibility)**: New columns in source data are handled via `resolveChoice` with `make_missing_values_null`. Existing jobs continue reading without modification.\n2. **DropColumns**: Use explicit schema projection to ignore unexpected fields—filter columns explicitly before the join.\n3. **Type changes**: Cast all fields to String during ingestion, then cast to desired types downstream. This buffers against upstream type changes.\n4. **Version tracking**: Store schema version in the Glue Data Catalog table properties for auditing.\n\n## Idempotency\n\n1. **Job Bookmarks**: Enable `enableBookmarks` in the Glue job configuration. Glue tracks processed file names and last-modified timestamps in DynamoDB.\n2. **Overwrite mode**: Use `write` mode `overwrite` for Parquet writes to handle retries cleanly.\n3. **Unique output paths**: Partition by date ensures no data overlap between runs.\n4. **Upsert pattern**: For late-arriving data, use a merge/upsert strategy with a staging table instead of simple overwrite.\n\n## End-to-End Test Plan\n\n| Test Case | Input | Expected Output |\n|-----------|-------|------------------|\n| Happy path | Valid events.csv and products.csv with matching product_ids | Parquet with all joined records |\n| Missing product_id | events.csv with null product_id | Record retained with null join result |\n| No matching products | events.csv with unknown product_ids | Records kept via left join, null product fields |\n| Empty events file | Zero-row events.csv | Empty Parquet partition created |\n| New column added | products.csv with extra 'discount' column | Column added to output (forward compat) |\n| Duplicate product_ids | products.csv with duplicate product_ids | Cross-join behavior, all combinations returned |\n| Schema evolution | product_id type changed from int to string | Handled via String cast |\n\n**Test automation**:\n1. Generate test data using localstack S3 mock.\n2. Run Glue job in dev endpoint with `--additional-python-files` for test fixtures.\n3. Query Athena against the output table and assert row counts and sample values.\n4. Use AWS Step Functions to orchestrate test pipeline with cleanup teardown.",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-15",
        "questionId": "q-24758",
        "question": "Scenario: Two CSV data sources on S3 deliver daily files. Design a beginner-level ingestion using AWS Glue to convert to Parquet in s3://lake/data/YYYY/MM/DD/, partitioned by date and cataloged in Glue. Use AWS Step Functions to coordinate two Glue jobs and a Lambda stage that validates row counts and pushes bad files to a DLQ. How would you test end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use two Glue crawlers to infer schemas, a PySpark Glue job to union both sources, deduplicate on event_id, normalize optional fields into a unified schema, and write Parquet partitioned by date to s3:",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would run two Glue jobs in parallel to read each CSV, normalize to a unified schema, write Parquet to s3://lake/data/YYYY/MM/DD/ with date partitioning, and register in Glue Data Catalog. A Step Fun",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Glue Schema Registry with per-stream Avro/JSON schemas, versioned and backward/forward compatible. Producers send schemaId in headers; a validation step enforces the contract at ingest. Write to S",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Spark Structured Streaming (Glue or EMR) to read from Kinesis Data Streams, write to an Iceberg table on S3 with daily partitions, register in Glue Catalog, enable schema evolution with MERGE operations for upserts,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests orchestration of multiple sources with basic data quality and error handling, a core beginner skill.\n\n## Key Concepts\n- Glue ETL jobs; dynamic frames; Parquet; partitioning\n- Step Functions for parallel execution and sequencing\n- Lambda validation; DLQ (SQS) for bad files\n- Idempotent re-runs and catalog consistency\n\n## Code Example\n```python\n# Lambda handler skeleton for validation\nimport json, boto3\nsqs = boto3.client('sqs')\nDLQ = 'https://sqs.us-east-1.amazonaws.com/123456789012/bad-files'\n\ndef lambda_handler(event, context):\n  # event contains counts and file keys\n  for rec in event['records']:\n    if rec['rows'] == 0:\n      sqs.send_message(QueueUrl=DLQ, MessageBody=json.dumps(rec))\n  return {'status': 'validated'}\n```\n\n## Follow-up Questions\n- How would you implement schema evolution across Glue jobs?\n- How would you test failure handling and replay semantics?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-16",
        "questionId": "q-2985",
        "question": "Scenario: You must ingest daily JSONL logs from an on-prem SFTP into S3. Design a beginner-friendly, serverless ingestion using AWS Transfer Family to pull files, convert to Parquet with daily partitions, catalog via Glue, and orchestrate via Step Functions. What steps would you implement, including naming, partitioning, quality checks, and testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Kinesis Data Streams with a streaming transform (Kinesis Data Analytics or Lambda) to mask PII in-flight, then land as Parquet in S3 with hourly partitions. Use Glue Data Catalog for schema e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Streaming design: Kinesis Data Streams feeds a Glue Spark job (or Kinesis Data Analytics) that writes Parquet to S3 with daily partition by trade_date and tenant_id. Catalog via Glue Data Catalog. Imp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Serverless SFTP ingest: Use AWS Transfer Family to pull daily JSONL from on-prem SFTP into S3 at logs/date=YYYY-MM-DD/. Run a Glue Spark ETL to convert to Parquet with daily partitions and register in",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design idea: inbound via Kinesis Data Streams; a per-tenant schema registry (Glue Schema Registry) and a Lambda/Glue ETL stage validates JSON against tenant-specific schemas, masks PII, and writes Par",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical use of serverless ingest for on-prem sources, with a safe beginner path using Transfer Family, Glue, and Step Functions.\n\n## Key Concepts\n- AWS Transfer Family for SFTP pulls\n- Glue Spark ETL and Data Catalog\n- Parquet with daily partitions\n- Step Functions orchestration and basic data quality\n\n## Code Example\n```python\n# Glue ETL outline to convert JSONL to Parquet and write daily partitions\nimport sys\nfrom awsglue.context import GlueContext\nfrom awsglue.transforms import *\nfrom awsglue.utils import getResolvedOptions\n# ... minimal skeleton\n```\n\n## Follow-up Questions\n- How would you handle schema drift or missing fields in JSONL?\n- How would you monitor Step Functions failures and alert on retries?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-data-engineer-q-17",
        "questionId": "q-14343",
        "question": "Design an end-to-end data pipeline for a fintech use case that ingests streaming events (transactions and fraud alerts) via Kinesis Data Firehose into S3 as Parquet, then loads into Snowflake for near-real-time analytics using Snowpipe. Include data formats, partition strategy, schema evolution, PII masking, and monitoring. Explain how you would test ingestion, correctness, and recovery from partial failures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-region ingestion and cataloging pipeline: **Architecture Overview:** 1. **Ingestion Layer:** Configure S3 Event Notifications to trigger AWS Lambda on new JSON object creation in s3://lake/region-a/events…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest streaming events via Kinesis Data Firehose to S3 as Parquet files, partitioned by date (year/month/day) for optimal query performance. Enable schema evolution using AWS Glue Schema Registry to handle evolving tra…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Ingest JSON events regionally via Kinesis Data Streams, fan out to S3 Parquet with hourly partitions using Apache Hudi for upserts and schema evolution. Use Glue Schema Registry + Glue Data Catalog; q",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Spark Structured Streaming (Glue or EMR) to read from Kinesis Data Streams, write to an Iceberg table on S3 with daily partitions, register in Glue Catalog, enable schema evolution with MERGE operations for upserts,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical integration between AWS streaming services and Snowflake, evaluating real-world experience with real-time analytics, schema evolution, data masking, and system reliability—all critical skills in fintech data engineering.\n\n## Key Concepts\n- Snowpipe auto-ingest from S3 for real-time loading\n- Parquet format with daily partitioning strategy\n- AWS Glue Schema Registry for evolving schemas\n- Snowflake dynamic data masking for PII protection\n- End-to-end testing and comprehensive monitoring\n\n## Code Example\n```javascript\n// Pseudocode: validate Snowpipe ingestion\nconst validateIngestion = async () => {\n  // Test data completeness\n  const sourceCount = await kinesis.getRecordCount();\n  const snowflakeCount = await snowflake.query('SELECT COUNT(*) FROM transactions');\n  \n  // Validate schema consistency\n  const schemaValidation = await validateSchema(sourceSchema, targetSchema);\n  \n  // Test recovery scenarios\n  await simulateFailure();\n  await verifyRecovery();\n};\n```\n\n## Testing Strategy\n1. **Ingestion Testing**: Validate record counts match between source and target\n2. **Correctness Testing**: Compare sample records for data integrity\n3. **Recovery Testing**: Simulate partial failures and verify Snowpipe's retry mechanism",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-18",
        "questionId": "q-12190",
        "question": "Scenario: A regulated fintech uses a high-velocity stream of JSON events from mobile wallets via Kinesis Data Firehose to S3. Design a secure, auditable ingestion pipeline that lands Parquet in s3://fintech-data/events/YYYY/MM/DD/, partitions by date, catalogs in Glue, enforces Lake Formation access controls, and traces data lineage with Glue CloudTrail. Address schema evolution, data quality checks, and how you would test end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kinesis Data Streams -> EMR Serverless Spark job writing to S3 via Apache Hudi (MERGE_ON_READ). Key by device_id, event_time; partition by event_date. Enable schema evolution with Glue Schema Regi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest via Kinesis Data Streams, land to S3 with Firehose creating daily prefixes, use Glue Data Catalog for schema, and Spark ETL for upserts with Apache Hudi on S3. Handle schema evolution with Glue",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt a hybrid ingestion: streaming via AWS MSK or Kinesis for low-latency events, plus nightly batch CDC. Store in S3 as Lake tables with Apache Hudi or Iceberg, registered in Glue Catalog. Support u",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage Kinesis Data Firehose to stream JSON to S3 with SSE-KMS, transform to Parquet, write into s3://fintech-data/events/YYYY/MM/DD/ partitioned by date. Use Glue Data Catalog and Lake Formation fo",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nReal-time ingestion with governed access and evolving schemas is a common challenge in fintech data platforms.\n\n## Key Concepts\n- Streaming ingestion, Parquet, Glue Data Catalog\n- Lake Formation access controls and schema evolution\n- Data quality checks and end-to-end testing\n\n## Code Example\n```javascript\n// Pseudo Spark/Glue snippet for schema evolution\nif (!schema.hasField('customer_id')) { schema.addField('customer_id','string') }\ndf.write.parquet(path, { mode: 'append' })\n```\n\n## Follow-up Questions\n- How would you implement lineage and audit trails across services?\n- What changes if events arrive out of order or duplicates appear?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-19",
        "questionId": "q-25291",
        "question": "Design a cross-account, streaming-to-batch ingestion for a media analytics platform that emits JSON events with event_id, needing late-arriving updates that upsert a sink Parquet dataset on S3 partitioned by event_date. Ingest in account A, replicate to account B, catalog in Glue, and compare using Apache Hudi vs Iceberg for upserts. Outline ETL, partitioning, schema evolution, access controls, and a testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest images via multipart S3 uploads behind a deduplicated manifest; store metadata in a versioned, upsertable Iceberg table on S3 (via Glue/Spark) keyed by asset_id + batch_id; handle schema evolut",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Glue Spark job (Python) to read JSONL from S3, safely cast fields (device_id string, ts timestamp, reading double), fill missing with defaults, and write Parquet to s3://iot-logs/processed/YYYY/",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Streams for ingestion; land raw events in S3 as Parquet with daily partitions and Glue catalog. Run Apache Hudi on Glue Spark jobs to upsert into target Parquet, enabling dedup and la",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Choose Apache Hudi (Copy-on-Write) to upsert Parquet datasets on S3 by event_id, partitioned by event_date. Ingest JSON in account A via Glue Spark job, upsert into a partitioned Parquet sink, catalog",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssess cross-account data governance, upsert semantics on S3, and real-world testing complexities.\n\n## Key Concepts\n- Upserts on S3 with Hudi vs Iceberg\n- Cross-account replication and Lake Formation access\n- Partitioning by event_date; idempotent writes; late-arriving data\n- Glue Catalog integration and end-to-end testing\n\n## Code Example\n```javascript\n// Pseudocode: Spark job using Hudi for upsert\ndf.write.format(\"org.apache.hudi\").options(\n  {\n    \"hoodie.datasource.write.recordkey\": \"event_id\",\n    \"hoodie.datasource.write.precombine field\": \"timestamp\",\n    \"hoodie.datasource.write.operation\": \"upsert\",\n    \"hoodie.datasource.write.table_type\": \"COPY_ON_WRITE\",\n    \"hoodie.upsert.reconcile.batch\": \"true\"\n  }\n).mode(\"append\").save(\"s3://bucket/path/events\")\n```\n\n## Follow-up Questions\n- How would you monitor for data skew and late-arriving data?\n- How would you test schema evolution with a Glue Catalog registry?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-20",
        "questionId": "q-18206",
        "question": "You operate a multi-region data lake on AWS with data arriving from numerous SaaS sources in varying schemas. Propose a production approach to implement end-to-end data lineage, schema drift detection, and impact analysis for downstream BI dashboards. Include services, data formats, lineage propagation across regions, governance with Lake Formation, and alerting/rollback procedures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kinesis Data Streams for input, Glue Schema Registry to enforce and evolve JSON schemas, and a streaming layer (Lambda or KDA) to validate data and produce Parquet in S3 with daily partitions. Ens",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an AWS Glue job triggered by S3 PUT events or a daily schedule to read invoices.csv, apply a fixed target schema, fill missing columns with defaults, cast data types appropriately, and write Parquet to s3://company-…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a production approach to implement end-to-end data lineage, schema drift detection, and impact analysis for downstream BI dashboards in a multi-region AWS data lake. Use OpenLineage with Glue E",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "In a multi-tenant, multi-account data lake, ingest JSON events from microservices via Kinesis Data Streams, land to S3 as Parquet partitioned by date and tenant_id. Use Glue for ETL and cataloging; en",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cross-region data lineage, drift detection, and governance in a real-world AWS data lake.\n\n## Key Concepts\n\n- Data lineage via OpenLineage integrated with Glue ETL\n- Schema drift detection and automatic catalog updates\n- Cross-region propagation with EventBridge and S3 replication\n- Fine‑grained governance with Lake Formation\n- Observability and rollback strategies for catalog changes\n\n## Code Example\n\n```javascript\n// Example OpenLineage emission (pseudo)\nimport { OpenLineageClient } from '@openlineage/client';\nconst client = new OpenLineageClient({ endpoint: 'https://openlineage.example' });\nclient.emit({\n  eventType: 'START',\n  job: { name: 'etl_orders', namespace: 'aws:data-lake' },\n  inputs: [{ uri: 's3://data-lake/raw/orders/' }],\n  outputs: [{ uri: 's3://data-lake/curated/orders/' }]\n});\n```\n\n## Follow-up Questions\n\n- How would you validate drift detection in CI/CD pipelines?\n- What rollback strategy would you adopt for multi-region catalog updates?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-21",
        "questionId": "aws-data-engineer-data-ingestion-1776520270890-0",
        "question": "An organization streams 100+ JSON logs from EC2 instances into a centralized data lake on S3 with daily partitions and minimal maintenance. Which AWS ingestion pattern best achieves this at scale with durable delivery?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a multi-account lake architecture with Lake Formation governance. Raw data ingests to lake/raw/tenant_id/date, where Glue Crawlers automatically discover schemas and Lake Formation enforces fine-grained access…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition by partner_id and date; store under s3://data-lake/partner_id/date/. Use a single Glue Data Catalog with per-partner databases and Lake Formation grants so each partner can only query its da",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Kinesis Data Firehose with S3 destination and optional data format conversion to Parquet",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest the daily CSV with a Glue Spark job, flatten, cast types, and write Parquet to the processed path partitioned by date. Enrich with a lightweight S3 mapping for category. Validate non-null keys",
            "isCorrect": false
          }
        ],
        "explanation": "## Correct Answer\nA: Kinesis Data Firehose with S3 destination and optional data format conversion to Parquet\n\nFirehose scales to high throughput, manages buffering, and can deliver directly to S3 with optional format conversion (e.g., JSON to Parquet), reducing operational overhead and enabling partitioned data layout for analytics.\n\n## Why Other Options Are Wrong\n- B: Kinesis Data Streams with a Lambda hook requires managing shards, backpressure, and potential scaling/configuration complexity, increasing maintenance for many producers.\n- C: AWS Data Pipeline is older, less real-time, and requires more configuration and maintenance for continuous ingestion.\n- D: Direct PutObject from many agents is not scalable and lacks centralized fault tolerance and batching benefits.\n\n## Key Concepts\n- Kinesis Data Firehose, S3, Parquet/ORC format conversion\n- Managed ingestion with serverless scaling and minimal ops\n\n## Real-World Application\n- Use when ingesting large volumes of semi-structured logs into a data lake with minimal custom code and you want analytics-ready formats automatically stored in S3.\n",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-22",
        "questionId": "q-18443",
        "question": "Design an advanced AWS data ingestion and governance pipeline for a multi-tenant SaaS product: tenants emit JSON events to a central Kinesis Data Stream; ingest into an S3 data lake with per-tenant isolation (partitioned by tenant/date), TLS in transit, and field-level masking before storage; manage schema evolution in Glue, implement idempotent writes, and run data quality checks (Deequ). Enable cross-region DR via S3 replication and catalog replication; provide an end-to-end test plan and example table definitions. What trade-offs and decisions would you make?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Kinesis Data Streams; mask PII in a streaming Lambda before S3; store as Parquet with tenant/date partitions; enforce tenancy via Lake Formation permissions and per-tenant Glue databases; h",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a beginner-level AWS data ingestion pipeline using the following architecture: **Services:** - S3 (source and destination buckets) - AWS Glue (ETL jobs and Data Catalog) - Amazon EventBridge or S3 Event Notificat…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement Apache Iceberg on S3 to store multi-source data; partition by date and source; register with Glue Data Catalog. Use Iceberg upserts (MERGE) and schema evolution; ingest Kinesis and RDS feeds",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a multi-source ingest using DMS CDC to S3, Glue Spark ETL to map each source into a canonical schema registered in Glue Schema Registry, enabling evolution. Persist as Parquet partitioned by d",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess ability to design end-to-end AWS data pipelines with multi-tenancy, schema evolution, data quality, governance, and DR.\n\n## Key Concepts\n\n- Kinesis + Lambda streaming; TLS in transit; per-tenant isolation; partitioning by tenant/date\n- Lake Formation permissions; Glue Data Catalog; schema evolution with Glue Schema Registry\n- Idempotent writes via deduplication on a unique event_id; Deequ-based data quality checks\n- Cross-region DR with S3 replication and catalog replication; testing strategy\n\n## Code Example\n\n```python\n# Pseudo-code: deduplicate by event_id before upsert\ndef upsert_batch(df, key_cols, id_col):\n    dedup = df.dropDuplicates([id_col])\n    # merge into target table (upsert semantics depends on destination)\n    return dedup\n```\n\n## Follow-up Questions\n\n- How would you monitor data quality and automatically rollback bad ingests?\n- How would you handle schema evolution without breaking downstream dashboards?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-data-engineer-q-23",
        "questionId": "q-16148",
        "question": "You need to ingest PostgreSQL CDC events (via Debezium) streaming into AWS, landing as Parquet on S3 with daily partitions, using Glue for catalog, and ensuring idempotent upserts and schema evolution. Describe the end-to-end pipeline, including handling late-arriving data, upserts, schema changes, and how you would test end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Iceberg on S3 with Glue Data Catalog for ACID upserts and schema evolution. Ingest via Kinesis Data Streams into a Spark job (Glue) writing Parquet to s3://lake/events/date/, partitioned dail",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use MSK as the ingest backbone with 20 regional producers publishing to topics partitioned by region. Deploy a Kinesis Data Analytics for Apache Flink application to read from MSK, perform initial deduplication using ev…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a streaming pipeline using Debezium into MSK, Spark Structured Streaming with foreachBatch to upsert into s3://lake/data/events/date=YYYY-MM-DD/; use Glue Data Catalog and partition projection",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Kinesis Firehose to land JSON into S3 with daily partitions, then a Glue ETL job to emit Parquet and populate the catalog. Enable cross‑account analytics via Lake Formation. Handle schema evolutio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a real streaming CDC pipeline with schema evolution and robust upserts in a lake.\n\n## Key Concepts\n- CDC with Debezium, MSK, Spark Structured Streaming\n- Parquet lake storage with daily partitions and tombstone handling\n- Glue Data Catalog, partition projection, schema evolution via Glue Schema Registry\n- Late-arriving data, deduplication, and deletes\n- End-to-end testing with synthetic CDC and checkpoints\n\n## Code Example\n```scala\n// pseudo: read from MSK, upsert to Parquet by batch, commit with transaction-like approach\nimport org.apache.spark.sql.functions._\nval df = spark.readStream.format(\"kafka\").option(\"kafka.bootstrap.servers\",\"...\" ).load()\nval parsed = df.selectExpr(\"CAST(value AS STRING) as json\").select(from_json(col(\"json\"), schema).as(\"r\")).select(\"r.*\")\nparsed.writeStream\n  .outputMode(\"append\")\n  .foreachBatch { (batchDF, batchId) =>\n    // upsert into Parquet by PK, e.g., write to temp + merge\n  }\n  .option(\"checkpointLocation\",\"s3://lake/checkpoints/...\")\n  .start()\n```\n\n## Follow-up Questions\n- How would you handle a schema change that adds a new column without breaking downstream queries?\n- How would you validate that dedup works after a restart?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-data-engineer-q-24",
        "questionId": "q-4098",
        "question": "A two-account AWS data lake receives a daily CSV telemetry feed from a 3rd-party vendor into s3://telemetry/vendorX/YYYY/MM/DD/data.csv.gz. Design an end-to-end pipeline to ingest, clean, and store as Parquet in the data lake, with per-tenant isolation, schema evolution for new columns, basic data quality checks, and time-travel via Athena. Include services, formats, partitioning, governance, and a testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To satisfy these requirements: Use a two-account Lake Formation data lake. Ingest daily CSVs with a Glue ETL job (Python/DynamicFrame) that reads CSV, applies drift-tolerant schema evolution (merges new columns into exi…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest streams via Kinesis Firehose to S3 with TLS and SSE-S3. Partition Parquet by version/source, e.g., s3://ml-data/version=20260313/source=a/. Use Glue Data Catalog for schema evolution; Spark ETL",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Glue Schema Registry for evolution, and real-time validators in Kinesis Data Analytics or Lambda to enforce rules (non-null IDs, valid timestamps, sane ranges) and tag bad records. Quarantine",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a Glue-based beginner ingestion: trigger on new s3 object inventory.csv under s3://partner-data/inventory/YYYY/MM/DD/; read CSV, write Parquet to s3://warehouse/inventory/YYYY/MM/DD/; partitio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDesigning cross-account data ingestion with schema evolution and per-tenant isolation tests production readiness for a real data lake.\n\n## Key Concepts\n- AWS Lake Formation, Glue ETL, DynamicFrame, Parquet, Athena\n- Schema evolution and drift detection\n- Tenant isolation, access controls\n- Data quality checks, test strategy\n\n## Code Example\n```python\n# Pseudo Glue script: merge schema with new columns\nfrom awsglue.dynamicframe import DynamicFrame\n# Load CSV, infer/merge with existing schema, and write Parquet\n```\n\n## Follow-up Questions\n- How would you handle late-arriving",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-21T11:43:40.245Z",
    "version": 1
  }
]