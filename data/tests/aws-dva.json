[
  {
    "id": "test-aws-dva",
    "channelId": "aws-dva",
    "channelName": "AWS DVA",
    "title": "AWS DVA Knowledge Test",
    "description": "Test your understanding of AWS DVA with 25 questions.",
    "questions": [
      {
        "id": "aws-dva-q-0",
        "questionId": "q-16163",
        "question": "Design a cross-region, multi-tenant telemetry pipeline that enforces per-tenant data provenance, embargo windows for analytics export, and tenant-aware cost controls. Use AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, plus OpenTelemetry. Explain tenant-scoped partitioning, idempotent writes, late data handling, audit trails, and governance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Key ideas: use MERGE INTO Iceberg with event_id as PK for upserts; route late data with a bounded lateness window and a retry path; leverage Iceberg schema evolution with defaults for new cols; ensure",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use tenant_id and version in Kinesis partitions; stage a new processing version and deterministically replay from a versioned checkpoint stored in DynamoDB. Deduplicate by event_id, or composite (tena",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Candidates should outline: (1) monitor shard metrics (GetRecords per shard) and set shard count/upscale; (2) implement partition key by device_type/date to distribute shards; (3) set Lambda reserved c",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant partition keys (tenantId/date) across KDS streams; raw events to S3; then a Lambda that deduplicates with DynamoDB using conditional writes on (tenantId, deviceId, ts); store curated Pa",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes design thinking for scalable, governed telemetry pipelines with multi-tenant isolation across regions, including provenance, embargoed exports, and cost controls.\n\n## Key Concepts\n- Tenant isolation and partitioning\n- Exactly-once through idempotent sink writes and dedupe\n- OpenTelemetry integration across producers and sinks\n- Late data handling and audit trails\n- Governance, embargo windows, and cost governance\n\n## Code Example\n```python\n# Pseudo: conditional PutItem in DynamoDB to dedupe\nimport boto3\n\n# PutItem with ConditionExpression to avoid duplicates\ndef put_dedupe(table, item):\n    table.put_item(Item=item, ConditionExpression='attribute_not_exists(pk)')\n```\n\n## Follow-up Questions\n- How would you test replay scenarios with late data?\n- How would you extend for new tenants without schema changes?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-1",
        "questionId": "q-18176",
        "question": "Architect a cross-region telemetry ingestion with per-tenant data residency: EU tenants' data must stay in EU; US tenants can be replicated for analytics in a separate region without violating isolation. Use AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue Data Catalog, Athena, DynamoDB, and OpenTelemetry. Explain tenancy isolation, region-bound retention, per-tenant quotas, data movement controls, and how you'd test end-to-end compliance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a region-scoped, per-tenant throttling design: a pre-ingest Lambda consults DynamoDB to check the tenant’s 1-minute window usage; if within limit, forward to regional KDS and atomically increm",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: route KDS to a KDA SQL app with a 5 minute tumbling window per device to compute avgTemp and stdTemp; flag anomalies where abs(temp - avgTemp) > 3*stdTemp; emit anomaly records to S",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Streams to ingest telemetry from thousands of devices. A Lambda function reads each tenant's correction policy from DynamoDB, applies deterministic corrections using a composite idempotency key (deviceI…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design uses region-scoped streams and S3 buckets per tenant, with a tenancy table in DynamoDB and per-tenant IAM. EU data lands in EU buckets and is lineage-tracked in Glue; US data is replicated to a",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests data residency, cross-region replication, tenancy isolation, and data catalog lineage. Also checks governance, observability, and how to validate compliance.\n\n## Key Concepts\n- Per-tenant residency and isolation\n- Region-aware replication and access control\n- Glue Data Catalog and Athena partitioning\n- OpenTelemetry tracing across services\n- Audit, policy checks, and drift testing\n\n## Code Example\n```javascript\n// Pseudo-routing: route by tenant residency\nfunction route(event) {\n  const tenant = event.tenant_id;\n  const region = get_tregion(tenant);\n  if (region === 'EU') {\n    return `s3://eu-${tenant}-telemetry`;\n  } else {\n    return `s3://us-${tenant}-telemetry`;\n  }\n}\n```\n\n## Follow-up Questions\n- How would you enforce least privilege across tenants in IAM and KMS?\n- How would you test for data leakage if a tenant migrates regions?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-2",
        "questionId": "q-19681",
        "question": "Beginner AWS DVA task: design a one-region streaming pipeline using Amazon MSK (Kafka) to ingest 10k JSON telemetry events/min from edge devices into an MSK topic. Each event must include a required correlationId. Build a Lambda consumer (via KCL) that validates a simple JSON schema, computes a 0-1 data quality score, and stores valid events to S3 Parquet partitioned by date; invalid records go to an SQS DLQ. Include end-to-end tracing with X-Ray/OpenTelemetry, a 3-day backfill, and a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed architecture: Kinesis ingest; Lambda attaches provenance (eventId, deviceId, firmwareVersion, ingestionTs, pipelineId); DynamoDB lineage table; valid events to S3 Parquet with provenance; inv",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition by tenantId+region in KDS and S3 (tenant/date/region). Use DynamoDB dedupe on (tenantId,eventId). Glue streaming with event-time windows and a 2-hour late-data watermark. Replay via a Dynamo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Attach a provenance graph to each event by adding a provenanceId that links to a per-tenant graph stored in DynamoDB; each processing step writes a Edge {from, to, step, ts, tenant, hash}. Cross-regio",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a one-region streaming pipeline using Amazon MSK (Kafka) to ingest 10,000 JSON telemetry events per minute from edge devices into an MSK topic. Each event must include a required correlationId field. Build a Lamb…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question explores a modern data streaming architecture using Amazon MSK (Kafka) instead of Kinesis, emphasizing practical skills in data quality validation, distributed tracing, and testing.\n\n## Key Concepts\n- **Amazon MSK/Kafka**: Fully managed Kafka service for building streaming pipelines\n- **Correlation ID Propagation**: Ensures end-to-end request tracking across services\n- **End-to-End Tracing**: X-Ray or OpenTelemetry for visibility into distributed systems\n- **JSON Schema Validation**: Validates event structure before processing\n- **Data Quality Scoring**: Computes a normalized quality metric (0-1) for each event\n- **S3 Parquet Storage**: Columnar format optimized for analytics, partitioned by date\n- **SQS DLQ**: Captures invalid records for debugging and reprocessing\n- **Backfill/Replay**: Replaying events from the past 3 days to handle gaps or downstream failures\n- **LocalStack Testing**: Local AWS emulation for integration testing without cloud costs\n\n## Code Example\n```python\nimport json\nimport jsonschema\nfrom dataclasses import dataclass\n\nschema = {\n    \"type\": \"object\",\n    \"required\": [\"correlationId\"],\n    \"properties\": {\n        \"correlationId\": {\"type\": \"string\"},\n        \"timestamp\": {\"type\": \"string\", \"format\": \"date-time\"},\n        \"deviceId\": {\"type\": \"string\"},\n        \"payload\": {\"type\": \"object\"}\n    }\n}\n\n@dataclass\nclass ValidationResult:\n    is_valid: bool\n    quality_score: float\n    error_message: str = None\n\ndef validate_event(event: dict) -> ValidationResult:\n    try:\n        jsonschema.validate(instance=event, schema=schema)\n        quality_score = compute_quality_score(event)\n        return ValidationResult(is_valid=True, quality_score=quality_score)\n    except jsonschema.ValidationError as e:\n        return ValidationResult(is_valid=False, quality_score=0.0, error_message=str(e))\n\ndef compute_quality_score(event: dict) -> float:\n    score = 1.0\n    if not event.get(\"timestamp\"):\n        score -= 0.25\n    if not event.get(\"deviceId\"):\n        score -= 0.25\n    if not event.get(\"payload\"):\n        score -= 0.25\n    return max(0.0, score)\n```",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-3",
        "questionId": "q-18881",
        "question": "Design a cross-region telemetry pipeline with 2M events/min, focusing on real-time per-device anomaly scoring and automatic quarantine. Specify scoring logic, late-data handling, regional isolation, and idempotent writes; architecture using Kinesis Data Streams, Lambda/Fargate, S3 Parquet, Glue/Athena, DynamoDB, and OpenTelemetry. Include monitoring, alerting, and a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Edge routing at ingestion: DynamoDB stores per-tenant residency and replication rules; a policy check in Lambda validates event.region against policy and writes to the tenant’s regional Kinesis stream",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest into Kinesis with tenantId as the partition key; fan out to Lambda for streaming transforms and Fargate for heavy analytics. Use idempotent writes to DynamoDB and store raw/curated data in per-",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant data tiers in S3 with lifecycle rules keyed by tenant_id and data_age; route streaming via Kinesis to Lambda that writes to tenant-specific S3 prefixes; use Glue to crawl and partition metadata; cre…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cross-region telemetry pipeline handling 2M events/min with real-time per-device anomaly scoring and automatic quarantine. ## Architecture Overview **Ingestion Layer:** - Regional Kinesis Data Streams (one per…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests cross-region distributed systems design, real-time analytics, and operational robustness. It validates understanding of stream processing at scale, stateful computation, and infrastructure-as-code testing.\n\n## Key Concepts\n- **Kinesis Data Streams:** Managed streaming service with exactly-once processing via checkpointing\n- **Lambda/Fargate:** Serverless versus container-based processing—Lambda for stateless operations, Fargate for ML inference workloads\n- **DynamoDB:** Low-latency per-device state management with conditional writes ensuring idempotency\n- **S3 + Parquet + Glue/Athena:** Cost-effective analytical storage with columnar Parquet format, automated schema discovery via Glue, and SQL queries through Athena\n- **OpenTelemetry:** Vendor-agnostic instrumentation for distributed tracing and metrics collection\n- **Regional Isolation:** Design pattern ensuring failure domains are contained within regions while maintaining global availability",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-4",
        "questionId": "q-20881",
        "question": "Design a cross-region telemetry ingestion pipeline across two AWS regions/accounts with per-device governance: PII-labeled events must land only in a secured, KMS-encrypted S3 bucket; non-PII events go to a standard S3 bucket. Use AWS DVA primitives (Kinesis Data Streams, Lambda/Fargate, S3, Glue Data Catalog, Lake Formation) and a central policy store to enforce access, enable idempotent writes by deviceId+ts, and support late data up to 24 hours. Include observability and a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate each JSON record against a versioned schema stored in DynamoDB; compute a per-record risk score using recent historical data; route low-risk events to S3 in Parquet format (partitioned by region and date) and h…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition Kinesis streams by tenantId and region to ensure tenant-scoped sharding. Use idempotent Lambda or Fargate consumers that perform deduplicated writes to DynamoDB and emit immutable events to S3 with tenant/date…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a 3-region pipeline: devices publish via AWS IoT Core to Kinesis Data Streams. A Lambda validates schema, dedups by deviceId+ts, assigns a quality score, and writes valid events to S3 Parque",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Cross-region telemetry with per-device governance: producers in region A push to Kinesis Data Streams; Lambda consults a policy store to classify PII vs non-PII and routes accordingly. PII lands in a",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of cross-region data governance, policy-based routing, and end-to-end observability in AWS DVA pipelines. It also probes idempotency, late data handling, and LocalStack testing.\n\n## Key Concepts\n- Cross-region data movement and access control\n- PII handling with encrypted destinations\n- Idempotent writes (deviceId+timestamp)\n- Late-arriving data strategies\n- Observability with OpenTelemetry across services\n- LocalStack testing plan for end-to-end validation\n\n## Code Example\n```javascript\n// Pseudo-Lambda outline for routing based on policy\nexports.handler = async (event) => {\n  const policy = await getPolicy(event.deviceId); // from DynamoDB or policy store\n  const targetBucket = policy.isPII ? 'pii-secured-bucket' : 'standard-bucket';\n  await writeToS3(targetBucket, event);\n  await upsertDeviceState(event.deviceId, event.ts);\n};\n```\n\n## Follow-up Questions\n- How would you validate policy updates without impacting live traffic?\n- What metrics and alarms would you place to detect misrouting or drift in governance?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-5",
        "questionId": "q-18305",
        "question": "Design a two-region streaming pipeline with per-device envelope encryption. Ingest events via Kinesis across regions, decrypt in Lambda using per-device envelope keys managed by KMS with daily rotation, validate against Glue Schema Registry, and store decrypted payloads as S3 Parquet partitioned by region/date. Enforce exactly-once processing using a provenance DynamoDB, handle late data up to 12 hours, and implement OpenTelemetry traces with a LocalStack testing plan. Explain failure modes and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a two-region streaming pipeline with envelope encryption: events arrive in Kinesis, Lambda decrypts using per-device envelope keys managed by KMS, with daily key rotation. Validate against G",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a cross-region, multi-tenant telemetry pipeline with per-tenant isolation, per-tenant rate limits, and in-tenant ordering. Route via regional KDS, dedupe in DynamoDB on (tenantId, deviceId, ts)",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a multi-region telemetry ingestion path that enforces per-tenant masking policies in real time. Store tenant masking schemas in DynamoDB encrypted by KMS; stream events via regional Kinesis Dat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Regional Kinesis streams feed a Fargate scorer that emits per-tenant anomaly scores to tenant-scoped Parquet in S3. Use tenantId as the partition key, and (tenantId, deviceId, ts) as the dedupe key wi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to architect secure, region-aware streaming with data protection, provenance, and observability under real-world latency.\n\n## Key Concepts\n\n- Envelope encryption with per-device keys in KMS\n- Multi-region Kinesis ingestion\n- OpenTelemetry tracing across Lambda and S3\n- Idempotent writes to a provenance store (DynamoDB)\n- Late data handling and backfill strategies\n\n## Code Example\n\n```javascript\n// Pseudo-code: decrypt per-device payload using envelope key from KMS\nfunction decryptPayload(encryptedKey, ciphertext, kmsClient) {\n  const dataKey = kmsClient.decrypt({ CiphertextBlob: encryptedKey, KeyId: 'alias/device-key' });\n  // assume AES-GCM with dataKey.Plaintext\n  const plaintext = decryptAESGCM(ciphertext, dataKey.Plaintext);\n  return JSON.parse(plaintext);\n}\n```\n\n## Follow-up Questions\n\n- How would you test key rotation without impacting live traffic?\n- How would you model provenance queries in Athena for per-tenant auditing?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-6",
        "questionId": "q-1381",
        "question": "A serverless data ingestion pipeline: an S3 PUT triggers a Lambda that transforms JSON logs into CSV and writes to a separate bucket; failed records go to a DLQ. Explain how you implement idempotent writes, choose between DLQ mechanisms (Lambda DLQ vs SQS), and set up minimal monitoring/alerts to catch processing failures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: In two AWS regions, ingest via Kinesis Data Streams with a Lambda consumer. Use DynamoDB for dedupe by deviceId+ts, applying schema_versioned validation (v1: deviceId, ts, temp; v2: +battery",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a tenant-aware routing layer that reads tenant metadata from DynamoDB to decide the primary DR region; publish to Kinesis in that region; write to region-specific S3 prefixes (tenant/date); tr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a stable composite key from source object key and a per-record id, store seen IDs in DynamoDB, and upsert only when not seen to avoid duplicates. Route failures to an SQS DLQ for decoupled retries",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a multi-region telemetry ingestion path that enforces per-tenant masking policies in real time. Store tenant masking schemas in DynamoDB encrypted by KMS; stream events via regional Kinesis Dat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical handling of retries, deduplication, and observability in a common serverless ELT setup. It prompts decision between DLQ patterns and concrete idempotency strategy.\n\n## Key Concepts\n- Idempotent writes with deterministic IDs\n- Dead-letter queues: Lambda DLQ vs SQS DLQ\n- Observability: metrics, alarms, and alerting\n- Minimal tooling: DynamoDB for seen IDs, CloudWatch for alerts\n\n## Code Example\n```javascript\n// Lambda pseudo-code (Node.js)\nconst AWS = require('aws-sdk');\nconst ddb = new AWS.DynamoDB.DocumentClient();\nexports.handler = async (event) => {\n  for (const rec of event.Records) {\n    const body = JSON.parse(rec.body); // or rec.kinesis, etc.\n    const id = `${rec.s3.object.key}:${body.recordId}`;\n    // Idempotency: check or upsert\n    const exists = await ddb.get({TableName:'SeenIds', Key:{id}}).promise();\n    if (exists.Item) continue;\n    await ddb.put({TableName:'SeenIds', Item:{id}}).promise();\n    // transform and write to target bucket\n    // ... write CSV to target bucket\n  }\n  return {status: 'done'};\n};\n```\n\n## Follow-up Questions\n- How would you adapt for out-of-order arrivals?\n- What changes if the per-record id is not available in the source payload?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-7",
        "questionId": "q-3882",
        "question": "In a multi-region telemetry ingestion pipeline, devices in three regions emit JSON events to region-local Kinesis streams. Design a beginner-friendly data-lineage approach that records a lineageId at ingest, stores provenance in DynamoDB, and writes Parquet files to S3 with region/date partitions, ensuring lineage is traceable across stages. Describe how to backfill the last 24 hours and how you would validate lineage integrity during queries in Athena. Include a simple monitoring plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Do dedupe first with DynamoDB keyed by sha256(deviceId+ts); in a regional Lambda mask deviceId and customerId using HMAC-SHA256 with a per-region KMS key, replacing only the payload while leaving the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-region Kinesis streams ingested by Lambda/Fargate, store raw events in S3 and catalog Parquet in Glue/Athena, manage schema evolution with Glue Schema Registry and backward/forward compatibili",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Assign a UUID lineageId at ingestion in Lambda, add it to the event, redact PII, and write to S3 Parquet partitioned by region/date. Persist provenance in DynamoDB (lineageId, eventId, region, ingestT",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a single-region pipeline with a preprocessing Lambda triggered by Kinesis. Validate required fields (deviceId, ts, metric), redact PII (emails, phones) in-flight, and enforce schema (ts nume",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical implementation of data lineage in a real-world, multi-region DVA pipeline, emphasizing traceability from ingest to storage and query.\n\n## Key Concepts\n- Ingestion-time lineage generation (UUIDs)\n- PII redaction and provenance propagation\n- Parquet writes with region/date partitioning\n- DynamoDB as a lightweight lineage map\n- 24h backfill strategy with Glue\n- Athena queries over lineage-aware datasets\n\n## Code Example\n```javascript\n// Lambda ingest snippet (pseudocode)\nconst lineageId = uuid.v4();\nrecord.lineageId = lineageId;\nredactPII(record);\nsendToRegionSink(record); // writes to S3 Parquet partitioned by region/date\n// store mapping\ndb.put({TableName: 'LineageMap', Item: { lineageId, eventId: record.id, region: record.region, ingestTime: now() }});\n```\n\n## Follow-up Questions\n- How would you test lineage completeness across regions?\n- What would trigger a backfill reprocessing job and how would you avoid duplicates?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-8",
        "questionId": "q-21565",
        "question": "Beginner AWS DVA: In a single-region streaming pipeline ingesting ~1k JSON telemetry events/min via a Kinesis Data Stream, extend to handle three event types (metrics, events, alerts) each with its own schema. Implement per-type JSON schema validation stored in DynamoDB, assign a per-record quality score, and route valid records to S3 Parquet under prefixes by type and date. Invalid records go to a DLQ. Maintain a lightweight schema registry with versioning per type and a Glue catalog entry. Include a LocalStack test plan and an outline for a new type backfill and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-type validation in a single Lambda layer using type-specific JSON Schemas stored in DynamoDB (type, version, schema). Validate each record, compute a simple dataQualityScore (presence+",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest regionally via KDS, deduplicate by deviceId+ts in DynamoDB, compute per-tenant rolling 5m avg of metricX using a streaming job (Lambda/KDA) and emit anomalies to an alert stream, write valid re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a hybrid on-prem MQTT-to-AWS telemetry ingestion pipeline where devices stream to an on-prem broker, with messages securely buffered and delivered to AWS. The architecture must support per-tenant residency, order…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt per-tenant IAM roles and Lake Formation permissions mapped to a tenant tag in the data catalog; route raw data into partitioned Parquet under /tenants/{tenant}/year=YYYY/month=MM/day=DD; Glue jo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEvaluates ability to design a practical, beginner-friendly multi-type streaming pipeline with governance and testing using familiar AWS primitives.\n\n## Key Concepts\n\n- Kinesis Data Streams and Lambda fan-out\n- Per-type JSON Schemas in DynamoDB\n- Simple dataQualityScore logic\n- DynamoDB for schema registry with versioning\n- S3 Parquet partitioned by type/date\n- DLQ routing and CloudWatch observability\n- LocalStack test approach for end-to-end validation\n\n## Code Example\n\n```javascript\n// lightweight validator sketch\nconst Ajv = require('ajv');\nconst ajv = new Ajv();\nconst schema = {/* fetch by type/version from DynamoDB */};\nfunction isValid(record, type){\n  const valid = ajv.validate(schema[type], record);\n  return !!valid;\n}\n```\n\n## Follow-up Questions\n\n- How would you test schema evolution across types?\n- How would you scale to higher throughput while maintaining idempotence?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-9",
        "questionId": "q-10670",
        "question": "Design an advanced cross-region telemetry ingestion and governance pipeline for a multi-tenant SaaS app that must export per-tenant data to an external analytics lake (e.g., Databricks) while preserving lineage and enabling legal-hold across regions. Use AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, plus OpenTelemetry. Explain per-tenant isolation, idempotent processing, cross-region replication, data lineage capture, and a compliant data-retention/hold policy with automated purge triggers. Provide deployment steps and anticipated failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition Kinesis by tenantId, replicate across regions with a Lambda cross-region replicator writing to the destination region’s KDS. Deduplicate in DynamoDB using a composite key (tenantId,eventId)",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a cross-region telemetry ingestion and governance pipeline for a multi-tenant SaaS with per-tenant isolation, idempotent processing, lineage, and hold-based purges. Use Kinesis Data Streams,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use region-scoped Kinesis shards keyed by tenantId to isolate workloads, with Lambda/Fargate processing writing to S3 Parquet and a DynamoDB metadata store. Enforce per-tenant IAM and CMEK-enabled S3,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a multi-tenant data mesh that ingests telemetry from Kinesis Data Streams, materializes per-tenant data marts in S3 via Glue jobs, and serves via Athena. Implement Lake Formation for fine-gr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess ability to design end-to-end data governance at scale, including lineage, retention holds, cross-region resiliency, and external analytics integration.\n\n## Key Concepts\n- Tenant isolation, idempotent writes, data lineage, legal-hold policy\n- Cross-region replication, OpenTelemetry, Glue Catalog, Athena\n- External export (Databricks) with governed retention\n\n## Code Example\n```python\n# Pseudo dedupe using DynamoDB for idempotent writes\ndef should_write(event_id):\n  seen = dynamodb.get_item(Key={\"event_id\": event_id})\n  if seen: return False\n  dynamodb.put_item(Item={\"event_id\": event_id, \"ts\": now})\n  return True\n```\n\n## Follow-up Questions\n- How would you test cross-region failover and DR consistency?\n- How would you verify lineage accuracy across stores and during schema evolution?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-10",
        "questionId": "q-1925",
        "question": "You're building a beginner AWS DVA pipeline: 2,000 devices emit JSON telemetry to S3 as daily JSONL; a Lambda validates lines and writes valid records as Parquet to a partitioned dataset, while invalid lines go to a DLQ. Propose a minimal approach to ensure idempotent processing, schema evolution, and late-data handling, with concrete services, data formats, and a simple monitoring plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a DynamoDB dedup table keyed by recordId to guard a per-record idempotent path in Lambda; write valid records to Parquet with date-based partitions in S3, and store re-ingested late data via date",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement with API Gateway as the producer to an SQS queue, preserve X-Ray trace context in message attributes, Lambda consumer polls the queue, validates fields, computes a per-record quality score,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a concrete architecture: tenantId as the Kinesis shard key; DynamoDB-backed per-tenant quotas; envelope encryption with KMS; IAM/resource policies plus VPC endpoints; per-tenant partitions for",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed architecture: Kinesis ingest; Lambda attaches provenance (eventId, deviceId, firmwareVersion, ingestionTs, pipelineId); DynamoDB lineage table; valid events to S3 Parquet with provenance; inv",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a robust, beginner-friendly DVA pipeline with idempotency, late-data handling, and schema evolution using common AWS services.\n\n## Key Concepts\n- Idempotent processing using a dedup store (DynamoDB)\n- Parquet partitions by date\n- Schema evolution via Glue Catalog / optional fields\n- Late-arriving data handling and re-ingest strategy\n- Observability via DLQ and CloudWatch alarms\n\n## Code Example\n````javascript\n// Pseudo-code for idempotent ingest\nconst exists = await dynDB.get({ PK: recordId });\nif (exists) return; // duplicate\nawait dynDB.put({ PK: recordId, ts: now });\nawait writeParquet(validRecord, datePartition);\n````\n\n## Follow-up Questions\n- How would you scale the dedup store and handle hot partitions?\n- How to test idempotency and schema evolution safely?\n- What are alternative approaches for late data without reprocessing all history?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-11",
        "questionId": "q-18288",
        "question": "In a multi-region AWS DVA telemetry pipeline, design a cross-tenant analytics strategy that enforces strict data isolation while enabling global BI queries. Use Kinesis for ingestion, Lambda/Fargate for enrichment, S3 for storage, Glue Data Catalog, Athena, DynamoDB for metadata, and OpenTelemetry for tracing. Explain how you would partition catalogs per tenant, implement region-specific sinks, enforce access controls, handle schema evolution, and support per-tenant audit trails?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant catalog databases (tenant_<id>) with IAM/Lake Formation controls and tenant-prefixed S3 prefixes (tenant=<id>/region). Ingest via Kinesis to Lambda, inject tenant_id, store Parque",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Partition Kinesis by tenantId, replicate across regions with a Lambda cross-region replicator writing to the destination region’s KDS. Deduplicate in DynamoDB using a composite key (tenantId,eventId)",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Attach a provenance graph to each event by adding a provenanceId that links to a per-tenant graph stored in DynamoDB; each processing step writes a Edge {from, to, step, ts, tenant, hash}. Cross-regio",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "In two regions, devices publish 20k JSON/min to region-specific Kinesis streams. Implement a cross-region analytics lake in a third region using S3, Glue, and Athena. Mask PII in Lambda before storage",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design multi-region isolation with a practical data catalog and analytics workflow, including cross-region governance and traceability.\n\n## Key Concepts\n\n- Per-tenant Glue Data Catalog databases and S3 prefixes to ensure data isolation\n- IAM/Lake Formation policies for access control and cross-region replication\n- Parquet partitioning by tenant and date for efficient Athena queries\n- OpenTelemetry end-to-end tracing across ingestion, enrichment, and analytics\n- Schema evolution handling via Glue Schema Registry compatible with Athena\n\n## Code Example\n\n```javascript\n// Pseudo: attach tenant context to catalog bindings\nconst tenant = getTenantFromEvent(event);\nbindCatalogToTenant(tenant, `tenant_${tenant.id}`);\n```\n\n## Follow-up Questions\n\n- How would you test cross-tenant isolation and audit trails across regions?\n- What failure modes could break isolation and how would you mitigate them?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-12",
        "questionId": "q-17130",
        "question": "Architect a blue/green rollout strategy for a cross-region AWS DVA telemetry pipeline using Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, and DynamoDB. The goal is zero-downtime upgrades, strict per-tenant isolation, and immutable audit trails. Describe rollout steps, traffic shift, rollback criteria, how to validate data integrity, and how backfill is handled during cutover?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a blue/green deployment strategy for the cross-region telemetry pipeline as follows: **1. Environment Setup** Deploy the green (new) environment in the target region with identical infrastructure to the existi…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a multi-tenant telemetry pipeline using Kinesis Data Streams for ingestion, with tenant-scoped partition keys (tenantId + eventType) ensuring ordered processing per tenant. Implement dynamic enrollment by provisi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant CMEK in AWS KMS and per-tenant S3 prefixes for raw/processed data. Ingest via Kinesis Data Streams and route through tenant-scoped Lambda/Fargate workers with IAM isolation. Employ enve",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed answer: Implement a Kinesis Data Streams + Lambda pipeline; validate JSON against a schema with optional fields; compute a simple anomaly flag by comparing temp to a device-specific rolling a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates the ability to deploy critical streaming pipelines safely across regions while maintaining strict data governance and auditability. It assesses deployment discipline, rollback rigor, and data integrity validation under production load.\n\n## Key Concepts\n\n- **Blue/Green Deployment**: Staged traffic shifting across regions with zero-downtime upgrades\n- **Per-Tenant Isolation**: IAM governance and routing rules ensuring strict tenant data separation\n- **Immutable Audit Trails**: Provenance validation and exact-once semantics for streaming data\n- **Data Integrity**: Idempotent writes and cross-environment validation during rollout\n- **Backfill Handling**: Replaying Kinesis retention data and historical S3 data via Glue jobs during cutover",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-13",
        "questionId": "q-23094",
        "question": "Design a 3-region, multi-tenant telemetry pipeline using AWS DVA primitives to ingest MQTT telemetry from industrial devices via AWS IoT Core, streaming into Kinesis Data Streams. Include: (1) a Lambda that validates schema, deduplicates by deviceId+ts, computes a data-quality score, and writes valid data to S3 Parquet partitioned by region/date/tenant; (2) a Kinesis Data Analytics app that detects per-device anomalies on 5-minute windows and publishes alerts to SNS; (3) late data within 5 minutes backfilled to a DLQ; (4) per-tenant IAM isolation and observability; (5) a LocalStack testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: regional Kinesis stream fed into a Lambda that validates with a small JSON schema, masks PII (email, phone, SSN) before storage, dedups by deviceId+ts in DynamoDB, writes valid records to S3",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would deploy two region-specific Kinesis streams, a routing Lambda to fan out to a common processor and a separate firmwareUpgrade path. Deduplicate per region with DynamoDB using (deviceId, ts) as",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition by tenant_id and event_date; route all telemetry through a Kinesis Data Stream consumed by a Lambda/Fargate worker that validates against Glue Schema Registry, deduplicates with a per-tenant",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a 3-region pipeline: devices publish via AWS IoT Core to Kinesis Data Streams. A Lambda validates schema, dedups by deviceId+ts, assigns a quality score, and writes valid events to S3 Parque",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-region data engineering, data quality, deduplication, anomaly detection, and tenant isolation under AWS DVA. It also probes error handling (DLQ) and testing strategy.\n\n## Key Concepts\n- IoT Core to Kinesis Streams integration for MQTT devices\n- Lambda data-quality pipeline: validation, dedup, scoring, partitioned S3 output\n- Parquet partitioning by region/date/tenant for efficient queries\n- Kinesis Data Analytics with rolling windows for anomaly detection and SNS alerts\n- DLQ for late/invalid records; testing with LocalStack where feasible\n- Per-tenant IAM isolation and cost/observability considerations\n\n## Code Example\n```sql\n-- Example KDA SQL: compute rolling mean per device\nSELECT deviceId,\n       AVG(metric) OVER (PARTITION BY deviceId ORDER BY ts ROWS BETWEEN 299 FOLLOWING AND CURRENT ROW) AS rolling_avg,\n       CASE WHEN ABS(metric - rolling_avg) > threshold THEN 'ANOMALY' ELSE 'OK' END AS status\nFROM input_stream\n```\n\n## Follow-up Questions\n- How would you scale the DLQ processing across regions and tenants?\n- What metrics and alarms would you place to monitor data quality, latency, and tenant budgets?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-14",
        "questionId": "q-11895",
        "question": "Beginner AWS DVA task: Ingest 20k JSON telemetry events per minute from devices across two regions. Build a minimal pipeline that validates against a schema, computes a per-record quality score, routes events by deviceType to separate S3 Parquet prefixes (with distinct schemas), deduplicates by deviceId+ts via DynamoDB, and sends invalids to a DLQ. Introduce a routing policy stored in SSM Parameter Store to drive the deviceType-based routing, implement a 7-day backfill, and add basic observability. Also update a Glue catalog entry and provide a simple Athena query to verify counts by deviceType?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-region telemetry pipeline handling 2M events/min with real-time per-device anomaly scoring and automatic quarantine. ## Architecture Overview **Ingestion Layer:** - Regional Kinesis Data Streams (one per…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use one Kinesis stream with tenant_id, route to per-tenant S3 prefixes, and enforce Lake Formation with per-tenant CMKs. Implement dedupe via a DynamoDB-based cache of event_ids, support schema evolut",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Do dedupe first with DynamoDB keyed by sha256(deviceId+ts); in a regional Lambda mask deviceId and customerId using HMAC-SHA256 with a per-region KMS key, replacing only the payload while leaving the",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a two-region Kinesis Data Streams architecture with Lambda functions that validate JSON schemas, compute per-record quality scores, and deduplicate using DynamoDB composite keys (deviceId+timestamp). Route val…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical capabilities beyond basic ingestion: deviceType-based routing with dynamic policies, schema evolution supporting distinct Parquet schemas, and governance through Glue catalog integration and Athena validation. It also evaluates runtime configuration management, observability implementation, and backfill strategy design.\n\n## Key Concepts\n- Data validation with schema compliance checks\n- Per-record quality scoring algorithms\n- Idempotent deduplication using DynamoDB composite keys\n- Multi-prefix Parquet storage with type-specific schemas\n- Dynamic routing policies via SSM Parameter Store\n- Dead Letter Queue handling for invalid records\n- Glue catalog integration for schema metadata\n- Athena query optimization for partitioned data\n- CloudWatch metrics for pipeline observability\n- Backfill strategy implementation for historical data",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-15",
        "questionId": "q-5056",
        "question": "Beginner AWS DVA task: 2,000 devices emit JSON events to regional Kinesis streams. Build a minimal pipeline that validates against a versioned schema, adds lineage fields (ingestRegion, ingestTime, schemaVersion), and computes a 0–100 data quality score. Write valid events to S3 Parquet partitioned by region/date; store an eventAudit in DynamoDB (eventId, source, ingestTime, schemaVersion, status); route invalids to a DLQ. Include 7-day backfill and basic observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set up a single Kinesis Data Stream; a Lambda consumer with batch size 100 validates JSON against a schema (requires correlationId, deviceId, ts); masks user emails/phones in payload; computes a per-r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement with regional Kinesis Streams and a Lambda that validates JSON against a versioned schema, adds lineage fields (ingestRegion, ingestTime, schemaVersion), and computes a 0–100 data quality sc",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a two-region Kinesis + Lambda model. Validate JSON via a Glue schema, enrich with calibration data from a DynamoDB table, normalize timestamps, and convert units in Lambda. Deduplicate by deviceId",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Utilize per-tenant PartitionKey in Kinesis streams; implement idempotent writes to DynamoDB and S3 (parquet) keyed by tenantId+ts; fan-out through a cross-region stream to minimize latency; Glue catal",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to add data lineage, schema versioning, and end-to-end data routing in a beginner AWS DVA context.\n\n## Key Concepts\n- Versioned JSON schema and evolution\n- Per-event lineage (ingestRegion, ingestTime, schemaVersion)\n- Parquet on S3 with regional/date partitioning\n- DynamoDB as a lightweight audit/log for provenance\n- DLQ for bad records; 7-day backfill with Glue\n- Observability basics (CloudWatch)\n\n## Code Example\n```javascript\n// Pseudo-code to enrich event with lineage fields\nfunction enrich(event, region){\n  const now = new Date().toISOString();\n  return {...event, ingestRegion: region, ingestTime: now, schemaVersion: 1};\n}\n```\n\n## Follow-up Questions\n- How would you handle schema evolution for optional fields?\n- How would you test idempotent processing and backfill integrity?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-16",
        "questionId": "q-7000",
        "question": "Ingest 3,000 devices per minute sending JSON telemetry to a regional Kinesis Data Stream. Build a minimal pipeline that validates JSON against AWS Glue Schema Registry, computes a per-record data quality score, deduplicates by deviceId+ts with DynamoDB, writes valid events to S3 as Parquet partitioned by region/date, routes invalid records to a DLQ, and supports a 5-day backfill. Include basic observability and a concrete test strategy using a small synthetic data generator?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-tenant telemetry ingestion pipeline where each tenant can select a data residency mode—regional-only or regional-with-backup. Ingest telemetry via regional Kinesis Data Streams, then process through Lambd…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest 8k/min in a single region via Kinesis Data Streams. Validate each JSON using AWS Glue Schema Registry for evolving schema with backward/forward compatibility. Deduplicate by deviceId+ts in Dyna",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Streams with enhanced fan-out for sub-200 ms latency, routing through Lambda/Fargate with per-tenant partitions. Implement idempotent writes via a DynamoDB dedupe table keyed by (tena",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a regional KDS to ingest; a Lambda/Fargate consumer validates against Glue Schema Registry, computes a data-quality score (required fields, ranges), deduplicates deviceId+ts in DynamoDB with",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical streaming ingestion with validation, dedupe, and durable storage, plus backfill and observability. It tests familiarity with Glue Schema Registry, DynamoDB TTL, Parquet partitioning, DLQs, and cost-conscious design.\n\n## Key Concepts\n- AWS Glue Schema Registry\n- Kinesis Data Streams\n- DynamoDB TTL\n- Parquet in S3\n- DLQ with SQS\n- Backfill strategies\n- Observability (CloudWatch, X-Ray)\n\n## Code Example\n```javascript\n// Skeleton Lambda\nexports.handler = async (event) => {\n  const record = JSON.parse(event.body || '{}');\n  const isValid = record?.deviceId && record?.ts && record?.metrics;\n  if (!isValid) return { statusCode: 400 };\n  // dedupe and store logic goes here\n};\n```\n\n## Follow-up Questions\n- How would you test the five-day backfill with synthetic data?\n- How would you handle schema drift and optional fields?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-17",
        "questionId": "q-4711",
        "question": "Design an ingestion path for multi-tenant telemetry that captures end-to-end data lineage for every event (source, transforms, load) with immutable provenance, queryable in Athena. Use AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, DynamoDB, and OpenTelemetry. How would you model lineage as a graph, store provenance, handle schema evolution, and support backfill without breaking processing guarantees?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-region telemetry pipeline ingesting 30k JSON events/min from devices across three regions, enforcing PII masking in flight and at rest with per-record privacy policies. Route masked dat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest events to Kinesis; propagate provenance with OpenTelemetry; write a Provenance item (tenant_id, event_id, source, transforms, ts) to DynamoDB and a lineage-augmented copy of the event to S3 wit",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Ingest via Kinesis; a Lambda masks PII (phone, name) per-tenant using KMS keys, writing masked events to S3 Parquet. Unmasked copies go to a separate, access-controlled bucket. DynamoDB audits redacti",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would deploy two region-specific Kinesis streams, a routing Lambda to fan out to a common processor and a separate firmwareUpgrade path. Deduplicate per region with DynamoDB using (deviceId, ts) as",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design end-to-end data lineage in a multi-tenant AWS pipeline using DVA primitives, focusing on governance, immutability, and operability in Athena.\n\n## Key Concepts\n\n- End-to-end lineage graph modeling (nodes and edges per event)\n- OpenTelemetry provenance propagation\n- DynamoDB as a fast, immutable provenance store\n- S3 object tagging/metadata for event-level lineage\n- Glue Data Catalog and Lake Formation for isolation\n- Athena as the query layer for lineage visibility\n- Backfill and idempotency considerations\n\n## Code Example\n\n```python\n# Pseudocode: store lineage\nprovenance = {\n  \"tenant_id\": tenant,\n  \"event_id\": event_id,\n  \"source\": source,\n  \"transforms\": [\"lambdaA\",\"glueJobB\"],\n  \"ts\": timestamp\n}\ndynamodb.put_item(Item=provenance)\n# also tag S3 object with lineage keys\n```\n\n## Follow-up Questions\n\n- How would you ensure lineage remains consistent across schema evolutions?\n- How would you query and visualize lineage relationships in Athena?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-18",
        "questionId": "q-26150",
        "question": "Design a cross-region telemetry pipeline with per-tenant data residency constraints: EU tenants processed in eu-west-1, US in us-east-1, APAC in ap-southeast-2; implement exactly-once ingestion, per-tenant isolation, and cross-region replication to support global dashboards. Use Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. Explain routing, idempotence, audit trails, and tenant-level cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a two-region pipeline with a single Kinesis stream per region keyed by tenantId; enforce per-tenant Lake Formation policies and per-tenant KMS keys; producers emit tenantId; a Lambda/Fargate consu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Place tenant policy in DynamoDB: tenantId → region, locality, retention. Ingest into tenant-scoped Kinesis streams; implement idempotent writes with sequence numbers and a dedupe store. Use OpenTeleme",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a resilient two-region IoT telemetry pipeline to ingest approximately 60,000 events per minute while maintaining strict per-tenant data residency requirements. The pipeline must route raw events to each tenant's…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Region-scoped ingestion per tenant; deduplicate with DynamoDB composite key (tenantId+seqNo) to guarantee exactly-once; replicate to a global sink via a cross-region Lambda to a secondary bucket for g",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes cross-region data residency, per-tenant isolation, and exactly-once guarantees in a DVA-like stack, plus cost controls and observability.\n\n## Key Concepts\n- Per-tenant regional routing and cross-region replication\n- Exactly-once via idempotent writes with tenant+seq keys\n- OpenTelemetry propagation across producers/consumers\n- Data residency via region-specific KMS keys and bucket policies\n- Cost governance per tenant\n\n## Code Example\n```javascript\n// Pseudo ingestion flow\nif (!existsInDDB(tenantId, seqNo)) {\n  putDDB(tenantId, seqNo);\n  writeToS3(rawEvent);\n}\n```\n\n## Follow-up Questions\n- How would you test exactly-once guarantees under late data arrivals?\n- How would you handle schema evolution and DLQ strategies across regions?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-19",
        "questionId": "q-10885",
        "question": "Beginner AWS DVA task: design a minimal, region-isolated pipeline to ingest 20k JSON telemetry events per minute from devices across two regions into regional Kinesis Data Streams. Validate JSON, compute a per-record data quality score, deduplicate by deviceId+ts with DynamoDB, store valid events to S3 Parquet partitioned by region/date, route invalid to a DLQ, and plan a 7-day backfill with observability. Include LocalStack testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Start with two regional Kinesis streams; a Lambda consumer validates JSON, computes a 0–1 quality score, and upserts a dedupe key (deviceId+ts) in DynamoDB. If valid, write to S3 Parquet regional/date",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Cross-region per-tenant telemetry ingestion leveraging AWS DVA primitives. Telemetry flows through regional Kinesis Data Streams, where each tenant's data is automatically routed to their designated region based on resi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant Kinesis shards with dedicated DynamoDB metadata mappings to ensure strict isolation. Encrypt PII using envelope encryption with tenant-specific AWS KMS customer master keys (CMKs), storing encrypted paylo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Solution Architecture: A producer Lambda ingests JSON telemetry into a Kinesis Data Stream. A consumer Lambda validates the schema (required fields: deviceId, timestamp), redacts PII (emails, phones, credit card numbers…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end data flow, validation, deduplication, and storage with observability. Adds testing with LocalStack to validate locally before deploying.\n\n## Key Concepts\n- AWS DVA primitives: Kinesis Data Streams, Lambda, DynamoDB, S3 Parquet, DLQ (SQS), CloudWatch, Glue, backfill\n- Best practices: idempotence, region/date partitioning, backfill strategy\n- Local testing: LocalStack for simulating AWS services\n\n## Code Example\n```javascript\n// Pseudo Lambda sketch\nexports.handler = async (event) => {\n  // parse, validate, score\n  // if valid: upsert DynamoDB (sensorId+ts) and write Parquet to S3 (region/date)\n  // if invalid: publish to SQS DLQ\n  // emit metrics\n}\n```\n\n## Follow-up Questions\n- How would you test idempotency with duplicate events?\n- How would you extend to handle schema evolution with optional fields?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-20",
        "questionId": "q-7280",
        "question": "Design a cross-region telemetry ingestion path for millions of devices that enforces per-tenant data quality gates and runtime policy enforcement. Use AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, plus OpenTelemetry. Include tenant isolation, schema evolution with backward/forward compatibility, idempotent writes, and automatic handling of non-compliant records (DLQ + audit). Explain how you would implement it and discuss trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a daily Glue ETL to validate schema and output Parquet, plus an Athena-based delta check to compare day-over-day row counts. Emit a quality score JSON to s3://telemetry/quality-reports/YYYY/M",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Store per-tenant contracts in DynamoDB with versioning; validate inbound records in a Lambda gate against the tenant’s active contract. Use Glue Catalog + Lake Formation for isolation; write verified",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a tenant-isolated path: ingest via Kinesis partitioned by tenant_id; dedupe with a DynamoDB idempotency table (tenant_id, message_id); Normalize with OpenTelemetry in Lambda; write to per-te",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed: per-tenant Kinesis streams feed a Lambda validator that checks types and required fields against Glue Schema Registry; valid events deduplicate with a DynamoDB idempotency table keyed by dev",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests handling of cross-region, multi-tenant telemetry with data quality gates and runtime policy enforcement, requiring the candidate to design a robust data lineage, idempotence, and schema evolution using DVA primitives.\n\n## Key Concepts\n\n- Per-tenant isolation with Kinesis Streams and DynamoDB catalog\n- Glue Schema Registry for backward/forward compatibility\n- Runtime data quality checks and DLQ handling\n- Idempotent writes and cross-region S3 replication\n- OpenTelemetry tracing across services\n\n## Code Example\n\n```javascript\n// Pseudo-idempotent write to DynamoDB\nasync function idempotentWrite(event, db) {\n  const key = event.deviceId + '|' + event.ts;\n  await db.put({\n    TableName: 'idempotency',\n    Item: { key, ts: event.ts },\n    ConditionExpression: 'attribute_not_exists(key)'\n  }).promise();\n  return true;\n}\n```\n\n## Follow-up Questions\n\n- How would you enforce per-tenant retention vs. cost controls?\n- How would you monitor SLA and detect drift in the pipeline?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-21",
        "questionId": "q-23329",
        "question": "Beginner AWS DVA: In a single region, design a streaming ingestion pipeline that receives 2k–4k JSON telemetry events/min from devices via Kinesis Data Streams. Each event includes tenantId and deviceId. Route records to per-tenant S3 prefixes (e.g., s3://bucket/tenantId/date/), and enforce per-tenant data lifecycle using object tags for retention. Implement a normalization Lambda that validates the schema and computes a simple severity score. Route low-score events to a DLQ. Add X-Ray tracing with sampling and outline a 24-hour backfill plan; include a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a single-region pipeline with a preprocessing Lambda triggered by Kinesis. Validate required fields (deviceId, ts, metric), redact PII (emails, phones) in-flight, and enforce schema (ts nume",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement policy-driven PII masking at ingestion in a cross-region AWS DVA telemetry pipeline. EU tenant data must be redacted per policy while immutable audit trails remain intact. Use Kinesis Data S",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a data quality gate at ingestion: valid events feed the main Kinesis stream and partitioned Parquet in S3; corrupted ones go to a quarantined DLQ with Object Lock. Enforce per-tenant schema",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Kinesis Data Streams pipeline with a normalization Lambda validating tenantId/deviceId/schema, then a routing stage that writes valid events to per-tenant S3 prefixes (tenant/date) with reten",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of multi-tenant data routing, per-tenant retention, and end-to-end tracing in a beginner-friendly AWS DVA task.\n\n## Key Concepts\n- Kinesis Data Streams, Lambda, S3 partitioning\n- Per-tenant routing and object tagging for retention\n- Schema validation, basic scoring, DLQ handling\n- Idempotent writes, deduplication, X-Ray tracing\n- Backfill strategy and LocalStack validation\n\n## Code Example\n```javascript\n// Pseudo: normalization function sketch\nfunction normalize(event){\n  if(!event.tenantId || !event.deviceId || !event.ts) throw new Error('Invalid');\n  // normalize schema, coerce types, add required fields\n  return {\n    tenantId: String(event.tenantId),\n    deviceId: String(event.deviceId),\n    ts: Number(event.ts),\n    payload: event.payload || {}\n  };\n}\n```\n\n## Follow-up Questions\n- How would you adapt this for automatic schema evolution?\n- How would you monitor SLAs per tenant and cost across prefixes?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-22",
        "questionId": "q-28918",
        "question": "Beginner AWS DVA: In a single region, ingest 1k–2k JSON telemetry events/min from IoT devices into a Kinesis Data Stream. Implement a lightweight, version-aware transformer: each event carries a version tag; the Lambda transformer upgrades older versions to vLatest, validates required fields, and forwards non-conforming records to an SQS DLQ. Valid records write as Parquet to S3/date. Include a simple canary test plan and a LocalStack test plan for end-to-end validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a cross-region ingestion using two Kinesis streams (one per region) with tenant-scoped partition keys, and per-tenant sampling rates stored in DynamoDB. Use Lambda/Fargate workers; apply ide",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deploy a Lambda transformer function that consumes events from the Kinesis Data Stream. For each event, inspect the version field and upgrade older schemas to vLatest using a version migration map. Validate the upgraded…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Streams as ingress, Lambda to compute per-sensor 1-minute windows, deduplicate with DynamoDB (sensor_id+window_start), and write aggregates as Parquet to S3 (partitioned by date). Cat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cross-region, multi-tenant streaming pipeline: use per-region Kinesis streams, store tenant-scoped data in S3 prefixes by region/date, and apply DynamoDB dedup keyed by tenantId+deviceId+ts.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical knowledge of version-aware data transformation, schema evolution handling, and observable canary deployment within a realistic AWS streaming pipeline. It combines multiple AWS services (Kinesis, Lambda, S3, SQS) and tests understanding of error handling patterns and testing strategies.\n\n## Key Concepts\n- Versioned event schemas with on-the-fly upgrades\n- Lambda as a stateless, idempotent transformer\n- Dead Letter Queue routing for observability\n- Canary deployment for safe rollout\n- LocalStack for local end-to-end validation\n\n## Code Example\n\n```python\nimport json\nimport boto3\nimport pandas as pd\nfrom io import BytesIO\nfrom datetime import datetime\n\nMIGRATION_MAP = {\n    \"v1\": {\n        \"legacy_field\": \"new_field\",\n        \"old_ts\": \"timestamp\"\n    }\n}\nREQUIRED_FIELDS = [\"event_id\", \"timestamp\", \"device_id\", \"payload\"]\n\ndef transform_record(record):\n    version = record.get(\"version\", \"vLatest\")\n    \n    if version != \"vLatest\" and version in MIGRATION_MAP:\n        for old_key, new_key in MIGRATION_MAP[version].items():\n            if old_key in record:\n                record[new_key] = record.pop(old_key)\n        record[\"version\"] = \"vLatest\"\n    \n    return record\n\ndef validate_record(record):\n    return all(field in record for field in REQUIRED_FIELDS)\n\ndef write_to_s3(records):\n    df = pd.DataFrame(records)\n    buffer = BytesIO()\n    df.to_parquet(buffer, engine=\"pyarrow\")\n    \n    date = datetime.utcnow()\n    key = f\"telemetry/year={date.year}/month={date.month:02d}/day={date.day:02d}/data.parquet\"\n    \n    s3 = boto3.client(\"s3\")\n    s3.put_object(Bucket=\"telemetry-bucket\", Key=key, Body=buffer.getvalue())\n\ndef send_to_dlq(record, reason):\n    sqs = boto3.client(\"sqs\")\n    sqs.send_message(\n        QueueUrl=\"https://sqs.region.amazonaws.com/account/dlq-url\",\n        MessageBody=json.dumps({\"record\": record, \"reason\": reason})\n    )\n\ndef handler(event, context):\n    valid_records = []\n    \n    for record in event[\"records\"]:\n        payload = json.loads(record[\"data\"])\n        transformed = transform_record(payload)\n        \n        if validate_record(transformed):\n            valid_records.append(transformed)\n        else:\n            send_to_dlq(payload, \"validation_failed\")\n    \n    if valid_records:\n        write_to_s3(valid_records)\n    \n    return {\"statusCode\": 200, \"body\": f\"Processed {len(valid_records)} records\"}\n```\n\nThis implementation demonstrates a clean separation of concerns: migration logic handles schema evolution, validation ensures data quality, and DLQ routing enables observability—all hallmarks of production-grade streaming pipelines.",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-23",
        "questionId": "q-4746",
        "question": "Design an ingestion path for multi-tenant telemetry where an in-flight data quality gate validates schema and field types, enforces value ranges, and flags anomalies with a per-tenant score. Valid events pass to per-tenant Parquet in S3; invalid go to quarantine with alerts. Guarantee exactly-once semantics across retries and enable backfill within 48 hours without duplicates. Use AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. How would you implement per-tenant validation schemas, idempotent writes, and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant quotas using Kinesis Data Streams with dedicated shards per tenant, backed by DynamoDB budget tracking. A lightweight proxy layer enforces quota limits and returns HTTP 429 responses with Retry-Afte…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement at ingestion a per-tenant schema in Glue, validate JSON against Avro, enforce ranges, and assign anomaly score; valid events land in per-tenant Parquet in S3; invalids go to quarantine with",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a 3-Lambda chain: Validator checks required fields; Redactor masks emails/phones in payload; Sink writes Parquet to S3/date-hour. Invalid events go to a DLQ. Maintain a DynamoDB audit table",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Mask PII per-tenant at ingestion using a policy store (DynamoDB) and a masking function in a Lambda/Fargate consumer; route redacted events to regional S3 Parquet (region/date); keep raw events in a s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world data quality at ingestion is critical; this question probes schema management, idempotence, backfill safety, and observability across AWS primitives.\n\n## Key Concepts\n- Ingestion-time data quality gates and anomaly scoring\n- Per-tenant schema governance with Glue catalog\n- Exactly-once guarantees with sequence tracking in DynamoDB\n- Quarantine path and alerting\n- Backfill strategies and deduplication\n- OpenTelemetry tracing across KDS, Lambda, and S3\n\n## Code Example\n```javascript\n// Pseudocode: conditional write to DynamoDB to enforce idempotence\nconst committed = await dynamo.putItem({\n  TableName: 'IngestedEvents',\n  Item: { tenant: t, offset: o, eventId, ... },\n  ConditionExpression: 'attribute_not_exists(eventId)'\n}).then(()=>true).catch(()=>false);\n```\n\n## Follow-up Questions\n- How would you test backfill safety under bursts?\n- What failure modes require quarantining more aggressively?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-24",
        "questionId": "q-13169",
        "question": "Beginner AWS DVA task: In a single AWS region, 8k JSON telemetry events per minute from ~200 devices must be ingested. Build a pipeline that validates against a Glue Schema Registry evolving schema with backward/forward compatibility, deduplicates by deviceId+ts using DynamoDB, stores valid events to S3 Parquet partitioned by region/date, routes invalids to a DLQ, and exposes two consumers: a real-time dashboard via Kinesis Data Analytics and a batch path via Glue ETL. Include LocalStack tests and basic observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a cross-region, multi-tenant governance flow with per-tenant holds, immutable raw data, and auditable exports. Use Kinesis for ingestion, Lambda/Fargate for processing, DynamoDB for per-tena",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant, low-latency path: Kinesis Data Streams with tenant_id as the partition key and Enhanced Fan-Out; real-time inference in Lambda/Fargate; emit anomalies to DynamoDB and store results in S3 P",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an ingress path that enforces per-tenant sampling caps via a DynamoDB quota table, routing high-priority events to a fast, exactly-once path to Kinesis while trimming lower-priority data. Use",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest 8k/min in a single region via Kinesis Data Streams. Validate each JSON using AWS Glue Schema Registry for evolving schema with backward/forward compatibility. Deduplicate by deviceId+ts in Dyna",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests handling of evolving schemas, deduplication, and parallel consumers while keeping tests beginner-friendly. It also covers testing with LocalStack and basic observability.\n\n## Key Concepts\n- Glue Schema Registry for evolving schemas and compatibility\n- DynamoDB-based deduplication by deviceId+ts\n- Two consumption paths: real-time analytics and batch ETL\n- Observability with CloudWatch and basic tracing\n\n## Code Example\n```javascript\n// Pseudo: Lambda validates JSON against Glue Schema Registry and writes to S3\nexports.handler = async (evt)=>{ /* validate and route */ }\n```\n\n## Follow-up Questions\n- How would you handle schema evolution conflicts in production?\n- What changes would you make to support higher throughputs or cross-region replication?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-25T07:11:08.174Z",
    "version": 1
  }
]