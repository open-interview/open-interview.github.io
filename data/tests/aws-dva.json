[
  {
    "id": "test-aws-dva",
    "channelId": "aws-dva",
    "channelName": "AWS DVA",
    "title": "AWS DVA Knowledge Test",
    "description": "Test your understanding of AWS DVA with 25 questions.",
    "questions": [
      {
        "id": "aws-dva-q-0",
        "questionId": "q-916",
        "question": "In an AWS-based DVA pipeline ingesting telemetry from 2000 devices/sec via Kinesis Data Streams, with a Spark/Glue path writing Parquet to S3 and cataloged in Glue, design an end-to-end strategy for robust schema evolution, data validation, and partitioning that minimizes reprocessing and supports backfills. Include schema registry, idempotence, DLQ, and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt Glue Schema Registry with versioned Avro schemas; Spark Structured Streaming validates records against the latest compatible schema and writes Parquet to S3 partitioned by device/hour. Route inc",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect a multi-tenant telemetry pipeline across two AWS accounts: ingest 15k-25k events/min from three tenants via separate Kinesis streams, validate and enrich in a shared Lambda, store valid reco",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end multi-tenant pipeline: a single Kinesis Data Stream ingests JSON; a Lambda deduplicates by tenant_id + event_id and writes per-tenant Parquet to S3 with daily partitions; register",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Trigger via S3 to Lambda; validate header exactly as [timestamp, tenant_id, metric], error otherwise. Convert CSV to Parquet and write to a partitioned path tenantId/date/file.parquet. Ensure idempote",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes practical UX for evolving data schemas in streaming pipelines, ensuring data quality and low reprocessing costs during changes.\n\n## Key Concepts\n- Glue Schema Registry and versioning\n- Schema compatibility strategies (backward/forward)\n- Spark Structured Streaming validation and checkpointing\n- Idempotent writes and deduplication stores\n- DLQ routing and backfill procedures\n- Observability with CloudWatch and Glue metrics\n\n## Code Example\n```javascript\n// Spark reads latest compatible Avro schema from Glue, writes Parquet partitioned by device/hour\ndf.write\n  .partitionBy(\"device_id\", \"hour\")\n  .format(\"parquet\").save(\"s3://bucket/data/\");\n```\n\n## Follow-up Questions\n- How would you handle backward-incompatible schema changes without downtime?\n- What metrics signal schema drift or data quality issues?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-1",
        "questionId": "q-6844",
        "question": "Design an advanced, multi-tenant telemetry ingestion path that supports per-tenant sampling rates and per-tenant retention, ensuring critical tenants have higher fidelity during outages. Outline a concrete architecture using AWS DVA: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. Include how you store per-tenant sampling config, apply sampling in real time, handle deduping, and onboarding/offboarding while preserving auditability and cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant sampling config is stored in DynamoDB; a Kinesis stream feeds Lambda that applies tenant-specific rate limits (scaling with traffic). High-fidelity data for critical tenants routes to a ded",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect a dual-path ingest: high-priority data goes to a dedicated Kinesis stream with low-latency Lambda processing; low-priority data is funneled to a separate stream with probabilistic downsampli",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage a single region Kinesis Stream with tenantId as the partition key. Validate JSON via a Lambda JSON Schema; dedupe by (tenantId, deviceId, ts) in DynamoDB; route valid events to S3 as Parquet",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant isolation using Lake Formation LF-tags with policy-based grants, expose only non-sensitive columns through masked views, and enforce row-level security filters in Athena queries. Leverage CloudTrail…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes design for cost-aware, per-tenant data fidelity with real-time decisions, auditability, and scalable governance.\n\n## Key Concepts\n\n- Per-tenant data contracts and retention\n- Real-time sampling and data routing\n- Deduplication and idempotency across sinks\n- OpenTelemetry across Kinesis, Lambda, and storage\n\n## Code Example\n\n```javascript\nexports.handler = async (event) => {\n  const tenant = event.tenantId;\n  const rate = await getRateFromDynamo(tenant);\n  if (Math.random() > rate) return null;\n  return event;\n};\n```\n\n## Follow-up Questions\n\n- How would you test latency and correctness of sampling under burst traffic?\n- How would you handle tenant onboarding/offboarding without data loss or audit gaps?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-2",
        "questionId": "q-7404",
        "question": "500 devices emit JSON telemetry to a regional Kinesis Data Stream. Build a beginner-friendly pipeline that validates against a versioned JSON schema (supporting optional fields), computes a per-record risk score from recent history, routes low-risk to S3 Parquet (partitioned by region/date) and high-risk to a second Kinesis stream for real-time analytics, and enable a 3-day backfill with basic observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a single-region KDS with a Lambda enrichment that caches device metadata in DynamoDB and uses a deterministic, idempotent S3 key (region/date/deviceId+ts). The Lambda retries API cal",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route each tenant's events to its home region's Kinesis Data Streams and S3, using a per-tenant KMS key for encryption. Enforce exactly-once processing with a DynamoDB deduplication table (tenantId, eventId) checked bef…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end multi-tenant pipeline: a single Kinesis Data Stream ingests JSON; a Lambda deduplicates by tenant_id + event_id and writes per-tenant Parquet to S3 with daily partitions; register",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate each JSON record against a versioned schema stored in DynamoDB; compute a per-record risk score using recent historical data; route low-risk events to S3 in Parquet format (partitioned by region and date) and h…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design a practical, beginner-friendly DVA pipeline that handles evolving schemas, implements per-record scoring, and provides dual output paths. It also assesses understanding of backfill capabilities and basic observability requirements.\n\n## Key Concepts\n- Versioned schema validation using DynamoDB\n- Lightweight per-record risk scoring from recent historical data\n- Conditional routing between S3 Parquet storage and real-time Kinesis processing\n- Backfill window management and observability implementation\n\n## Code Example\n```javascript\n// Pseudo-logic sketch for Lambda handler\n```\n\n## Follow-up Questions\n- How would you scale the risk scoring to thousands of concurrent records?\n- What strategies would you implement for schema evolution and backward compatibility?\n- How would you enhance observability for production monitoring?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-3",
        "questionId": "q-2788",
        "question": "Beginner AWS DVA task: 5,000 IoT sensors publish JSON telemetry to a Kinesis Data Stream. Design a resilient pipeline that produces 1-minute windowed aggregates stored as Parquet in S3, with a Glue catalog and Athena table. Ensure idempotent writes, late data handling up to 15 minutes, and backfill capability for prior days. Include monitoring, cost considerations, and a simple failure plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kinesis Data Streams as ingress, Lambda to compute per-sensor 1-minute windows, deduplicate with DynamoDB (sensor_id+window_start), and write aggregates as Parquet to S3 (partitioned by date). Cat",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Kinesis Data Streams as the inlet; a Lambda validates JSON and deduplicates via DynamoDB (deviceId+ts). Valid events go to a second Lambda that writes Parquet to S3 partitioned by region/date; inv",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement regional Kinesis Data Streams with per-tenant shard allocation using enhanced fan-out consumers. Each tenant receives dedicated streams in their primary region with cross-region replication through Kinesis Dat…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Edge routing at ingestion: DynamoDB stores per-tenant residency and replication rules; a policy check in Lambda validates event.region against policy and writes to the tenant’s regional Kinesis stream",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests building a real-time analytics pipeline with windowing, deduplication, and late data handling, plus backfill.\n\n## Key Concepts\n- Kinesis Data Streams and Lambda\n- Windowed aggregates and watermarking\n- Parquet in S3 and Glue catalog + Athena\n- Idempotent writes with DynamoDB\n- Backfill and monitoring with CloudWatch\n\n## Code Example\n```javascript\n// Pseudo code: idempotent upsert in DynamoDB\nconst pk = `sensor:${sensorId}:window:${windowStart}`;\nconst params = {\n  TableName: 'sensor_window_aggregates',\n  Item: {pk, sum, count, lastUpdate},\n  ConditionExpression: 'attribute_not_exists(pk)'\n};\n// retry-safe PutItem with conditional expression\n```\n\n## Follow-up Questions\n- How would you test late-arriving data handling?\n- How would you scale to millions of sensors?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-4",
        "questionId": "q-14618",
        "question": "Beginner AWS DVA task: design a two-region telemetry ingestion pipeline. 2k JSON events/min flow into regional Kinesis Data Streams. Validate against a Glue Schema Registry, assign a schema version, deduplicate by deviceId+ts with DynamoDB, store valid records in S3 Parquet partitioned by region/date, route invalids to a DLQ, and populate basic data lineage in the Glue Data Catalog. Include a 7-day backfill plan and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "The architecture leverages a DynamoDB tenant registry that maps tenant_id to target region, with producers routing events to regional Kinesis Data Streams based on this mapping. Events are partitioned using tenant_id+ti…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Regional Kinesis Data Streams ingest approximately 2,000 JSON events per minute per region. Lambda functions validate each JSON record against the AWS Glue Schema Registry, attach the corresponding schema version, and c…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Approach: regional Kinesis stream fed into a Lambda that validates with a small JSON schema, masks PII (email, phone, SSN) before storage, dedups by deviceId+ts in DynamoDB, writes valid records to S3",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement with API Gateway as the producer to an SQS queue, preserve X-Ray trace context in message attributes, Lambda consumer polls the queue, validates fields, computes a per-record quality score,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of cross-region data ingestion, schema registry validation, idempotent deduplication strategies, and data lineage implementation in a beginner-friendly AWS architecture.\n\n## Key Concepts\n- Glue Schema Registry validation and versioning\n- Regional Kinesis Data Streams for scalable ingestion\n- DynamoDB deduplication using composite keys\n- S3 Parquet storage with region/date partitioning\n- Glue Data Catalog for lineage metadata\n- CloudWatch observability and Step Functions for backfill orchestration\n\n## Code Example\n```javascript\n// Pseudo: onKinesisEvent(event) {\n//   if (validate(event)) { \n//     writeParquet(deduped(event)); \n//   } else { \n//     sendDLQ(event); \n//   }\n// }\n```",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-5",
        "questionId": "q-28178",
        "question": "Beginner AWS DVA: In a single region, ingest 1k–3k JSON telemetry events/min from IoT devices via API Gateway -> SQS; a Lambda consumer reads from SQS, validates required fields, assigns a per-record quality score, and upserts a DynamoDB dedupe by deviceId+ts; writes validated records to S3 Parquet/date; invalid to a DLQ (SQS); enable X-Ray tracing across API Gateway, Lambda, and S3 writes; outline a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a single-region pipeline ingesting 2k JSON telemetry/min into a Kinesis Stream with a required correlationId. Implement schema evolution with v1/v2 formats and a Canonicalizer Lambda that maps",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest via Kinesis Data Streams in each region; a masking layer (Lambda/Fargate) applies tenant-scoped redaction/tokenization before writing per-tenant Parquet to S3 and audit logs to DynamoDB. Use La",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement with API Gateway as the producer to an SQS queue, preserve X-Ray trace context in message attributes, Lambda consumer polls the queue, validates fields, computes a per-record quality score,",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Lambda masking layer triggered after ingestion into Kinesis Data Streams. Policy per tenant stored in DynamoDB with versioned function references. Masked payloads go to S3 Parquet; an audit even",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests a non-Kinesis ingest path, queue-based decoupling, and traceability end-to-end. It also checks idempotent dedupe, effective error handling, and end-to-end testing in LocalStack.\n\n## Key Concepts\n- API Gateway to SQS producer path\n- SQS DLQ handling\n- DynamoDB upsert for per-device dedupe\n- S3 Parquet writes partitioned by date\n- X-Ray trace propagation across services\n- LocalStack end-to-end testing\n\n## Code Example\n```javascript\n// Lambda: processFromSQS.js\nexports.handler = async (event) => {\n  for (const rec of event.Records) {\n    const payload = JSON.parse(rec.body);\n    // validate, score, write to DynamoDB/S3 as appropriate\n  }\n};\n```\n\n## Follow-up Questions\n- How would you ensure at-least-once vs exactly-once semantics?\n- How would you handle out-of-order events per device?\n- What metrics would you surface to CloudWatch for this pipeline?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-6",
        "questionId": "q-3150",
        "question": "Design a secure, multi-tenant telemetry pipeline in AWS DVA with per-tenant isolation in a shared data lake. Ingest via a single Kinesis stream with tenant_id, route to S3 prefixes per tenant, and use Lake Formation + CMKs for access control. Include schema evolution, late-arriving data, and idempotent writes with a dedupe store; log changes in DynamoDB. Trade-offs: isolation granularity vs. ops complexity?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use one Kinesis stream with tenant_id, route to per-tenant S3 prefixes, and enforce Lake Formation with per-tenant CMKs. Implement dedupe via a DynamoDB-based cache of event_ids, support schema evolut",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use per-tenant CMEK in AWS KMS and per-tenant S3 prefixes for raw/processed data. Ingest via Kinesis Data Streams and route through tenant-scoped Lambda/Fargate workers with IAM isolation. Employ enve",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant Kinesis partitioning with a DynamoDB dedupe keyed by tenant+sequence to guarantee exactly-once ingestion. Audit trails land in S3 with Object Lock Governance; replicate immutabilit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant validators in a streaming stage (Lambda/Fargate) that enforce required fields, types, and value ranges; route valid events to the sink; quarantine bad records to a per-tenant bad-record bucket with…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to architect multi-tenant data lakes, balancing isolation, security, and operational complexity in AWS DVA.\n\n## Key Concepts\n- Per-tenant isolation in a shared lake (Lake Formation, CMKs)\n- Ingestion routing (tenant_id) and partitioning in S3\n- Idempotent writes and deduplication state\n- Schema evolution, late-arriving data handling, and auditing\n\n## Code Example\n```python\ndef dedupe_key(record):\n    return f\"{record['tenant_id']}|{record['event_id']}\"\n```\n\n## Follow-up Questions\n- How would you handle tenant onboarding/offboarding with minimal risk?\n- What monitoring would you add to detect cross-tenant data access violations?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-7",
        "questionId": "q-23648",
        "question": "In one region, implement a content-based routing streaming pipeline: edge devices send JSON telemetry (deviceId, ts, metric, severity, correlationId) at ~2k/min into a Kinesis Data Stream. A Lambda validates fields and routes records with severity >= 80 to a HighSeverity stream, others to a Normal stream. Downstream Lambdas enrich and write valid events to S3 Parquet by date; invalid records go to a DLQ. Include X-Ray tracing and a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Kinesis Data Streams in each region; a masking layer (Lambda/Fargate) applies tenant-scoped redaction/tokenization before writing per-tenant Parquet to S3 and audit logs to DynamoDB. Use La",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Shard Kinesis streams by tenant and enforce per-tenant budgets in DynamoDB. In Lambda/Fargate, validate OpenTelemetry context, assign a tenant-scoped sequence, and write raw events to S3 under tenant-",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: use two Kinesis streams (High and Normal). A validation Lambda checks required fields (deviceId, ts, correlationId) and severity; routes by severity to the appropriate stream. Downstream Lam",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use tenant_id as the Kinesis partition key to ensure per-tenant ordering and data locality. Deploy regional Kinesis Data Streams in each tenant's designated region, with an enrichment layer (Lambda or Fargate) processin…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design content-based routing, with strict per-record validation and multi-stream fan-out. Emphasizes end-to-end tracing, fault handling, and test strategy in a realistic, beginner-friendly context.\n\n## Key Concepts\n- Content-based routing with multiple Kinesis streams\n- Lambda validation of JSON schema and required fields\n- Per-record tracing with AWS X-Ray\n- Parquet writing to S3 and DLQ for invalid data\n- LocalStack end-to-end testing\n\n## Code Example\n```javascript\n// Pseudo-validator outline\nfunction validate(record) {\n  const hasFields = record.deviceId && record.ts && record.correlationId && typeof record.severity === 'number';\n  const okSeverity = record.severity >= 0 && record.severity <= 100;\n  return hasFields && okSeverity;\n}\n```\n\n## Follow-up Questions\n- How would you handle backpressure if one stream lags significantly?\n- What metrics would you emit to monitor data quality and latency?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-8",
        "questionId": "q-5233",
        "question": "1,000 devices per minute emit JSON telemetry to a regional Kinesis Data Stream. Build a minimal, cost-aware pipeline that validates JSON, deduplicates by deviceId+ts using DynamoDB, writes valid events to S3 as Parquet partitioned by region/date, routes invalid JSON to a DLQ, and emits an SNS alert when temperature exceeds a threshold. Include 7-day backfill and basic observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via regional KDS; Lambda1 validates JSON; on success, record flows to a staging path. Deduplicate with DynamoDB using deviceId+ts. Lambda2 enriches with device_meta from DynamoDB, then writes e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use tenant_id and version in Kinesis partitions; stage a new processing version and deterministically replay from a versioned checkpoint stored in DynamoDB. Deduplicate by event_id, or composite (tena",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Streams as the inlet; a Lambda validates JSON and deduplicates via DynamoDB (deviceId+ts). Valid events go to a second Lambda that writes Parquet to S3 partitioned by region/date; inv",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a single-region pipeline: 1k–3k JSON events/min flow from devices into a Kinesis stream. A dedup Lambda uses messageId with DynamoDB to drop duplicates. An enrichment Lambda fetches device meta",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end‑to‑end data ingestion, fault tolerance, and basic anomaly alerting in a beginner-friendly AWS DVA task.\n\n## Key Concepts\n- Ingestion: KDS; Validation: Lambda; Deduping: DynamoDB; Storage: S3 Parquet; Invalid data: DLQ; Alerts: SNS; Backfill: Glue; Observability: CloudWatch.\n\n## Code Example\n```javascript\n// Sandbox Lambda skeleton for validation and dedupe\nconst AWS = require('aws-sdk');\nconst ddb = new AWS.DynamoDB.DocumentClient();\nexports.handler = async (evt) => {\n  const rec = JSON.parse(evt.body || evt);\n  if (!rec.deviceId || !rec.ts) throw new Error('Invalid');\n  const key = { deviceId: rec.deviceId, ts: rec.ts };\n  // dedupe logic\n  // ... put item with conditional expression\n  return { status: 'ok' };\n};\n```\n\n## Follow-up Questions\n- How would you test idempotency guarantees in this pipeline?\n- What changes would you make to handle high arrival bursts and cost control?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-9",
        "questionId": "q-9216",
        "question": "Design an autonomous, tenant-aware observability and self-healing pipeline for cross-region telemetry ingestion using Kinesis, Lambda/Fargate, S3, Glue, Athena, DynamoDB and OpenTelemetry. Describe how per-tenant SLAs drive automatic failover, how traces/metrics guide remediation, and how to prevent cascading outages while preserving exactly-once semantics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement regional Kinesis Data Streams with per-tenant shard allocation using enhanced fan-out consumers. Each tenant receives dedicated streams in their primary region with cross-region replication through Kinesis Dat…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Store immutable raw in S3 with event_id as key; create a DynamoDB audit row per event: event_id, shard_id, ingest_ts, process_ts, status, transformations_hash. Use idempotent writes via conditional Pu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "An autonomous tenant-aware observability and self-healing pipeline with per-tenant SLAs mapped to Kinesis shard pools, OpenTelemetry traces across Lambda/Fargate and S3, and an idempotent writer to Dy",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose per-tenant regional Kinesis Data Streams deployed in each tenant's designated region, paired with per-tenant AWS KMS keys for encryption. Implement region-local S3 data lakes with tenant-scoped prefixes, Glue ca…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design autonomous, tenant-aware resilience in a distributed telemetry pipeline, emphasizing observability, per-tenant governance, and cross-region failure handling.\n\n## Key Concepts\n\n- OpenTelemetry integration across Kinesis → Lambda/Fargate → S3 → Glue/Athena\n- Per-tenant SLAs guiding auto remediation and failover\n- Exactly-once semantics with idempotent writes and deduplication\n- Self-healing, circuit breakers, and backpressure to prevent cascading outages\n\n## Code Example\n\n```javascript\n// Minimal idempotent writer for deduplication\nasync function writeEvent(event, store){\n  const id = event.dedupToken;\n  if (await store.exists(id)) return;\n  await store.put(id, event.payload);\n}\n```\n\n## Follow-up Questions\n\n- How would you simulate regional outages and verify auto-remediation?\n- How would you measure SLA compliance per tenant and tune shard allocation?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-10",
        "questionId": "q-25919",
        "question": "Design an adaptive telemetry ingestion design for 100k–1M events/min across 3 tenants in multi-account AWS. Implement edge sampling to cap throughput per tenant, with per-tenant quotas enforced at ingestion, exactly-once processing, and purge rules within 24h. Use Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. Explain how per-tenant sampling rates are computed, idempotency, and backpressure handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Store immutable, per-tenant records in S3 with versioning and a per-tenant checkpoint in DynamoDB. Replay job reads the selected window from S3/Glue catalog, re-ingests to Kinesis with idempotent writ",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant CMEK in AWS KMS and per-tenant S3 prefixes for raw/processed data. Ingest via Kinesis Data Streams and route through tenant-scoped Lambda/Fargate workers with IAM isolation. Employ enve",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a cross-tenant adaptive telemetry ingestion system for 100k–1M events/min across 3 tenants in multi-account AWS. Implement edge sampling using Kinesis Data Streams with Lambda/Fargate consumers. Use a determinist…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Adapter path: MQTT and HTTPS feed a single Kinesis stream. Normalize to {deviceId, ts, metric, correlationId}. Validate required fields and numeric ranges, dedupe by deviceId+ts with DynamoDB, and sto",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests advanced multi-tenant system design skills, including quota management, adaptive sampling, exactly-once semantics, and cross-account architectures.\n\n## Key Concepts\n- **Adaptive sampling per tenant**: Compute sampling rates dynamically based on tenant quota configuration and current throughput\n- **Per-tenant quotas in DynamoDB**: Store quota limits and current usage counters for real-time enforcement at the edge\n- **Exactly-once processing**: Use idempotent event processing with eventId deduplication to prevent duplicates\n- **OpenTelemetry instrumentation**: Add distributed tracing across Kinesis, Lambda, and Fargate components\n- **Cross-account data flows**: Implement secure data movement between producer accounts and a central ingestion account\n- **Purge policies**: Configure S3 lifecycle rules to automatically delete data within 24 hours\n\n## Code Example\n```javascript\n// Adaptive sampling decision using deterministic hashing\nfunction shouldSample(tenantId, eventId, quotaMap) {\n  const quota = quotaMap[tenantId];\n  if (!quota || quota.allowBurst) return true;\n  \n  const hash = hashFunction(tenantId + eventId) % 100;\n  return hash < quota.sampleRate;\n}\n\n// Idempotent event processing\nasync function processEvent(event) {\n  const dedupeKey = `dedup:${event.tenantId}:${event.eventId}`;\n  \n  const existing = await dynamodb.get({ Key: { pk: dedupeKey } });\n  if (existing) return { status: 'duplicate', eventId: event.eventId };\n  \n  await dynamodb.put({ Item: { pk: dedupeKey, ttl: Date.now() + 86400 } });\n  await processEventOnce(event);\n  return { status: 'processed', eventId: event.eventId };\n}\n\n// Backpressure handling via Kinesis consumer scaling\nconst consumerConfig = {\n  maxBatchSize: 10000,\n  maxRecordAge: 30000,\n  retryStrategy: { maxRetries: 3 }\n};\n```",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-11",
        "questionId": "q-15066",
        "question": "Beginner AWS DVA task: Ingest 10k JSON telemetry events/min from devices across two regions. Implement a two-region pipeline where a regional Kinesis stream feeds a Lambda that applies dynamic per-deviceModel validation rules (stored in DynamoDB), computes a data quality score, deduplicates by deviceId+ts, writes valid records to S3 Parquet partitioned by region/date/deviceModel, and routes invalids to a DLQ. Plan a 3-day backfill and include observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a fixed 60-second watermark window stored in a DynamoDB table keyed by deviceId with a lastSeen timestamp. A validator Lambda attaches a qualityScore and routes records with missing required fields to the DLQ. For l…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would build a two-region Kinesis-driven pipeline where regional steady streams feed a Lambda validator. It uses a DynamoDB rules table keyed by deviceModel for dynamic checks, assigns a per-record q",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Trigger via S3 to Lambda; validate header exactly as [timestamp, tenant_id, metric], error otherwise. Convert CSV to Parquet and write to a partitioned path tenantId/date/file.parquet. Ensure idempote",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Route data by tenantId, storing events in s3://bucket/tenants/{tenantId}/{region}/{date}/ as Parquet; use per-tenant dedupe in DynamoDB with deviceId+seq; create tenant-scoped IAM roles granting acces",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\n\nTests ability to design per-model dynamic validation, regional data handling, and backfill.\n\nKey Concepts\n\n- DynamoDB-based dynamic validation rules per deviceModel\n- Kinesis Data Streams function integration pattern\n- Parquet partitioning by region/date/deviceModel in S3\n- Deduplication strategy using deviceId+ts\n- DLQ for invalid records and observability\n\nCode Example\n\n```javascript\n// Pseudo: Lookup rules by model and validate fields\nconst rules = await rulesTable.getItem({model: record.deviceModel});\nif (!applyRules(record, rules)) { enqueue DLQ; continue; }\n```\n\nFollow-up Questions\n\n- How would you test schema evolution and versioned rules?\n- How would you implement idempotent writes for backfill?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-12",
        "questionId": "q-12189",
        "question": "Beginner AWS DVA task: design a two-region telemetry pipeline for 5k JSON events/min from IoT devices, including client-side pre-validation to filter obviously invalid data before ingestion. In AWS, regional Kinesis streams feed a Lambda that computes a per-record quality score, deduplicates by deviceId+ts with DynamoDB, stores valid events to S3 Parquet by region/date, and routes invalids to a DLQ; plan a 3-day backfill and basic observability. Include a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed: per-tenant Kinesis streams feed a Lambda validator that checks types and required fields against Glue Schema Registry; valid events deduplicate with a DynamoDB idempotency table keyed by dev",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design pattern: Ingest uses DynamoDB to track per-tenant quotas (hourly limit, sampling rate). Compute a stable hash of tenant, deviceId, and hour; if hash falls inside the current sampling window, pu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-region Kinesis Data Streams; a Lambda subscribes to each stream and applies tenant-specific masking rules stored in DynamoDB, emitting masked JSON to S3 Parquet (partitioned by region/date) vi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a two-region telemetry pipeline with client-side pre-validation to drop obviously invalid events before ingestion. In AWS: regional Kinesis Data Streams feed a Lambda that computes a per-rec",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests a practical beginner-friendly AWS DVA pipeline across regions, with client-side pre-validation, per-record quality scoring, deduplication, and clear routing of valid/invalid data. It also touches backfill planning and observability.\n\n## Key Concepts\n- Client-side validation to reduce cloud load\n- Kinesis Data Streams, Lambda, DynamoDB for dedupe\n- S3 Parquet partitioning by region/date\n- DLQ routing for invalids\n- Backfill planning and CloudWatch observability\n- LocalStack testing\n\n## Code Example\n\n```javascript\n// Example snippet\nfunction validate(rec) {\n  const ok = rec && typeof rec === 'object' && typeof rec.deviceId === 'string' && typeof rec.ts === 'number';\n  if (!ok) return false;\n  const { temp, humidity } = rec;\n  return (temp >= -50 && temp <= 150) && (humidity >= 0 && humidity <= 100);\n}\n```\n\n## Follow-up Questions\n- How would you test idempotence of dedupe writes?\n- What changes if data rate doubles?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-13",
        "questionId": "q-9817",
        "question": "Design an advanced cross-tenant telemetry pipeline that automatically tiers data by tenant based on freshness and access patterns: hot data remains in S3 Standard for immediate Athena queries; cold data moves to S3 Infrequent/Glacier, with per-tenant retention (e.g., 90-365 days). Use Kinesis Data Streams, Lambda/Fargate, Glue, Athena, Lake Formation, and OpenTelemetry. Explain tenant-aware lifecycle policies, idempotent writes, cross-region replication, and cost governance. How would you implement it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two regional Kinesis streams; Lambda maintains per-device rolling stats (mean and std) in DynamoDB for a 5-minute window and computes a z-score per event. If |z|>3, emit an alert to an SNS topic; writ",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition Kinesis streams by tenant ID and region to ensure data locality and tenant isolation. Implement deduplication in Lambda using a composite key of tenant ID and message ID, storing processed IDs in DynamoDB with…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: use two Kinesis streams (High and Normal). A validation Lambda checks required fields (deviceId, ts, correlationId) and severity; routes by severity to the appropriate stream. Downstream Lam",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant data tiers in S3 with lifecycle rules keyed by tenant_id and data_age; route streaming via Kinesis to Lambda that writes to tenant-specific S3 prefixes; use Glue to crawl and partition metadata; cre…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design tenant-isolated, scalable data lifecycles with automated cost governance across AWS DVA primitives.\n\n## Key Concepts\n- Tenant-aware S3 lifecycle and retention metadata in DynamoDB\n- Kinesis ingest, Lambda/Fargate orchestration, Glue crawlers, Athena views\n- Lake Formation RBAC and OpenTelemetry instrumentation\n\n## Code Example\n```javascript\n// Pseudo policy mapping for per-tenant retention tier\nconst tierForTenant = (tenant, ageDays) => ageDays < 30 ? 'HOT' : ageDays < 365 ? 'WARM' : 'COLD';\n```\n\n## Follow-up Questions\n- How would you test data tier transitions and validate retention policies?\n- What strategies would you use for handling schema evolution across tenant data?\n- How do you ensure data consistency during cross-region replication failures?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-14",
        "questionId": "q-8174",
        "question": "In a multi-tenant analytics pipeline where each tenant demands reproducible processing across upgrades, design a streaming path that uses Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB and OpenTelemetry to enable deterministic replay of a tenant's events under a new processing version without downtime. Include versioned schemas, per-tenant partitioning, and a rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed: a 3-region pipeline where device telemetry feeds per-region Kinesis Data Streams; Kinesis Data Analytics runs an anomaly scoring model in real time; a Lambda subsequently writes valid events",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: Ingest telemetry data in Account A via Kinesis Data Streams; process with a Lambda function that validates JSON structure and enriches records with region and date information; implement deduplication…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Glue Schema Registry for versioned schemas, write to Iceberg tables on S3 to support schema evolution and upserts with a deterministic key, implement idempotent writes via a normalized primary key",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use tenant_id and version in Kinesis partitions; stage a new processing version and deterministically replay from a versioned checkpoint stored in DynamoDB. Deduplicate by event_id, or composite (tena",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design upgrade-safe, reproducible pipelines with per-tenant isolation and end-to-end observability.\n\n## Key Concepts\n\n- Deterministic replay across processing versions\n- Versioned schemas and per-tenant partitioning\n- Idempotent writes keyed by event_id\n- Backward/forward compatibility and rollback plan\n- Distributed tracing with OpenTelemetry across components\n\n## Code Example\n\n```javascript\n// Pseudocode: versioned replay checkpoint lookup\nasync function replayFromCheckpoint(tenantId, version) {\n  const ckpt = await DynamoDB.get({partitionKey: `${tenantId}#v${version}`, sortKey: 'checkpoint'});\n  return ckpt?.offset ?? 0;\n}\n```\n\n## Follow-up Questions\n\n- How would you implement a safe rollback if the new version underperforms?\n- How would you minimize replay latency while preserving exactly-once semantics?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-15",
        "questionId": "q-8934",
        "question": "Beginner AWS DVA task: Ingest telemetry from a fleet of devices into a regional Kinesis Data Stream. Build a minimal, cost-aware pipeline that: 1) validates each JSON payload and routes malformed records to a DLQ; 2) deduplicates by deviceId+ts with a DynamoDB table; 3) writes valid events to S3 as Parquet, partitioned by region/date; 4) issues an offline alert if no data from a device for 15 minutes within any 24-hour window via a scheduled Lambda; include a 7-day backfill plan and basic observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition by tenant in Kinesis shards and DynamoDB namespaces; use per-tenant KMS keys for envelope encryption and S3 prefixes per tenant. Maintain a per-tenant sequence counter to guarantee exactly-o",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: Ingest via Kinesis Data Streams; Lambda validates JSON and routes malformed payloads to a DLQ. Use DynamoDB to dedupe by deviceId+ts. Write valid events to S3 as Parquet, partitioned by region/date. I…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Partition by tenantId in Kinesis; idempotent writes via DynamoDB dedupe (tenantId+deviceId+ts); mask fields per-tenant from a policy table during Lambda enrichment; store valid events as Parquet in S3",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant masking at ingress using a Lambda/Fargate policy evaluation engine that reads tenant config from DynamoDB; route masked data to a dedicated S3 prefix per region; keep raw PII in e",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a beginner's ability to build a real-time pipeline with validation, deduplication, and durable storage, plus a simple heartbeat alert and backfill plan. It also introduces observability basics without overwhelming complexity.\n\n## Key Concepts\n\n- Kinesis Data Streams ingest pattern\n- JSON validation and DLQ routing\n- Deduplication using DynamoDB (deviceId+ts)\n- Parquet output to S3 with region/date partitioning\n- Heartbeat/offline alert via last_seen + scheduled Lambda\n- 7-day backfill plan and basic monitoring\n\n## Code Example\n\n```javascript\nexports.handler = async (event) => {\n  // Implementation would go here\n};\n```",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-16",
        "questionId": "q-5088",
        "question": "Design an AWS DVA telemetry ingest with per-tenant data residency, exact-once processing, and deterministic replay for 50 tenants and 10M devices. Describe tenant-scoped partitioning, watermark-based late data handling, idempotent producers, cross-region replication, and cost-aware autoscaling using Kinesis, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. How would you handle failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: Ingest via a single-region Kinesis Data Stream; a Lambda consumer validates each JSON (optionally against Glue Schema Registry). If valid, upsert latest per-device (deviceId) in Dyn",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a stable composite key from source object key and a per-record id, store seen IDs in DynamoDB, and upsert only when not seen to avoid duplicates. Route failures to an SQS DLQ for decoupled retries",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a multi-tenant, real-time differential privacy-enabled ingestion pipeline using Kinesis Data Streams for high-throughput event collection. Store per-tenant epsilon budgets in DynamoDB with atomic counters, enfor…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use tenant-scoped partitioning by tenant_id in Kinesis; enforce exact-once with a DynamoDB offset store keyed by (tenant_id, producer_id) and a monotonically increasing sequence; deduplicate using a c",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes end-to-end design for multi-tenant telemetry with residency, exactly-once semantics, and replay capabilities, using concrete primitives.\n\n## Key Concepts\n\n- Tenant-scoped partitioning and per-tenant state\n- Exactly-once via offset store and idempotent writes\n- Watermark-based late data handling\n- Deterministic replay across regions\n- OpenTelemetry tracing across Lambda/Fargate\n- Cost-aware autoscaling and backpressure\n\n## Code Example\n\n```javascript\n// Pseudo idempotent producer\nfunction produce(event, tenantId, producerId, seq) {\n  const key = `${tenantId}:${event.id}`;\n  if (store.checkAndMark(key, seq)) { // DynamoDB conditional put\n    stream.put({PartitionKey: tenantId, Data: event})\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test exactly-once guarantees under bursty traffic?\n- What metrics signal backpressure or skew, and how would you remediate?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-17",
        "questionId": "q-28256",
        "question": "Design a cross-region telemetry ingestion pipeline where tenants can dynamically choose data residency modes (regional-only vs. regional+backup), with per-tenant encryption keys rotated via AWS KMS and a policy engine that gates cross-region replication. Use AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. Explain data flow, guarantees, and testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Kinesis producer + Lambda consumer. Validator consults a DynamoDB schema registry (versions v1/v2) to validate fields/types by event.version, routing invalids to a DLQ. Valid events are transfor",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Glue Schema Registry for versioned schemas, write to Iceberg tables on S3 to support schema evolution and upserts with a deterministic key, implement idempotent writes via a normalized primary key",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an AWS data pipeline in a single region that ingests 5,000 JSON events per minute, each containing customerId and deviceId. Implement strict multi-tenant isolation using per-tenant S3 prefixes (e.g., s3://bucket/…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a multi-tenant telemetry ingestion pipeline where each tenant can select a data residency mode—regional-only or regional-with-backup. Ingest telemetry via regional Kinesis Data Streams, then process through Lambd…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question evaluates a candidate's ability to architect multi-tenant systems with data residency controls, policy-driven access patterns, and reliability guarantees at scale using AWS Developer Tools.\n\n## Key Concepts\n- **Data Residency Modes**: Tenants choose between regional-only storage or regional storage with cross-region backup, enforced at the policy level\n- **Per-Tenant Encryption**: Dedicated KMS CMKs with automated rotation ensure isolation and compliance without operational overhead\n- **Policy Engine**: Gates all cross-region replication decisions, evaluating tenant permissions before any data movement occurs\n- **Delivery Guarantees**: Kinesis provides at-least-once delivery; idempotent writes (using tenant ID + sequence tokens) ensure exactly-once semantics in S3\n- **Observability**: OpenTelemetry traces span ingest, processing, policy evaluation, and replication across all regions\n\n## Code Example\n```javascript\n// Policy evaluation at ingest time\nconst replicationRequest = {\n  tenantId: context.tenantId,\n  action: 'cross-region-replicate',\n  sourceRegion: context.region,\n  targetRegion: config.backupRegion\n};\n\nconst policyResult = policyEngine.evaluate(replicationRequest);\nif (!policyResult.allowed) {\n  logger.warn('Replication denied', { tenantId: context.tenantId });\n  throw new PolicyViolationError('Cross-region replication not permitted for this tenant');\n}\n\n// Proceed with encrypted cross-region write\nawait replicateWithEncryption(context, policyResult.appliedKeys);\n```\n\n## Follow-up Questions\n1. How would you handle KMS key cross-region access without exposing credentials?\n2. What happens when a tenant switches from regional-only to backup mode mid-ingestion?\n3. How do you test cross-region failover scenarios without actual data movement?\n4. What monitoring alerts would indicate policy engine failures?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-18",
        "questionId": "q-20195",
        "question": "In a cross-region, multi-tenant telemetry pipeline, design a governance and cost-visibility layer that enforces per-tenant ingest quotas, per-tenant data masking policies, and per-tenant cost accounting while using AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. Describe how you implement quota enforcement, tenant-specific masking rules, and cross-region cost attribution, including failure modes, latency impact, and a practical testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant quotas via DynamoDB counters and a hybrid token bucket on ingestion; enforce at the Kinesis producer or Lambda layer and backpressure when limit hit. Store tenant masking rules in Dynam",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use tenantId as the Kinesis partition key and a DynamoDB-backed sequence tracker to ensure idempotent writes across regions; emit to S3 with tenant/date partitions; Glue catalog and Athena queries; la",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Streams (2-3 shards) to ingest 10k events/min. A Lambda consumer validates records against Glue Schema Registry using the supplied schemaVersion. Valid records are written to S3 Parqu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Kinesis-driven pipeline: MQTT gateway publishes JSON to KDS; a Validation Lambda checks against Glue Schema Registry, computes a 0–1 quality score, and uses DynamoDB to deduplicate on (dev",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\\nTests ability to design governance, data isolation, and cost visibility in a scalable multi-tenant pipeline. It requires concrete use of DVA primitives and cross-region considerations.\\n\\n## Key Concepts\\n- Per-tenant quotas and backpressure\\n- Tenant-specific masking rules and privacy controls\\n- Cost attribution via tagging and per-tenant accounting\\n- Cross-region data movement and observability\\n\\n## Code Example\\n```javascript\\n// Pseudo: quota check at ingest\\nfunction checkQuota(tenantId){\\n  const {limit, consumed} = quotas[tenantId];\\n  if(consumed >= limit) throw new BackpressureError('Quota exceeded');\\n  quotas[tenantId].consumed += 1;\\n}\\n```\\n\\n## Follow-up Questions\\n- How would you test quota hardening and masking correctness? \\n- How would you handle tenant onboarding/offboarding without data gaps?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-19",
        "questionId": "q-12341",
        "question": "Design a two-region IoT telemetry pipeline: ingest ~60k events/min; enforce per-tenant residency so raw data stays in the home region of each tenant; validate schemas in Lambda, route invalids to DLQ. Store validated events in regional S3 as Parquet; compute per-tenant aggregates in DynamoDB/S3 with idempotent writes for exactly-once delivery. Emit real-time anomaly alerts per tenant within 1 minute; trace with OpenTelemetry; test via LocalStack?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a two-region Kinesis per-tenant feed with Lambda/Fargate shims that deduplicate by tenant+recordId, writing idempotent records into S3 Parquet under tenant/date/region and updating a DynamoDB",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a resilient two-region IoT telemetry pipeline to ingest approximately 60,000 events per minute while maintaining strict per-tenant data residency requirements. The pipeline must route raw events to each tenant's…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose per-region Kinesis streams with Glue Schema Registry for validation and evolution (compatibility modes). Compute a per-record score (completeness, conformance, freshness) in a Lambda, route va",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Step Functions workflow with 5 tasks: ValidateAndScore (schema, required fields, quality score), Deduplicate (DynamoDB conditional write on deviceId+ts), WriteParquet (S3 Parquet with date pa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates a candidate's ability to design distributed systems that address multi-region data residency, streaming reliability, exactly-once processing, and observability requirements in a concrete IoT telemetry scenario.\n\n## Key Concepts\n\n- **Multi-region architecture**: Two-region deployment with intelligent routing based on tenant residency\n- **Data residency enforcement**: Ensuring raw telemetry data remains within the tenant's designated home region\n- **Exactly-once semantics**: Idempotent writes to prevent duplicate processing during retries\n- **Schema validation**: Real-time event validation with DLQ routing for malformed messages\n- **Real-time processing**: Sub-minute anomaly detection and alerting per tenant\n- **Observability**: End-to-end OpenTelemetry tracing for pipeline monitoring\n- **Local development**: LocalStack integration for comprehensive CI and local testing\n\n## Code Example\n\n```javascript\n// Lambda function to route events by tenant home region\nexports.handler = async (event) => {\n  const tenantHomeRegion = await getTenantHomeRegion(event.tenantId);\n  \n  if (tenantHomeRegion !== process.env.AWS_REGION) {\n    await forwardToRegion(event, tenantHomeRegion);\n    return { statusCode: 200, body: 'Routed to home region' };\n  }\n  \n  // Process in home region\n  await validateAndStore(event);\n  return { statusCode: 200, body: 'Processed successfully' };\n};\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-20",
        "questionId": "q-25849",
        "question": "Beginner AWS DVA: In a single region, ingest 1–2k JSON telemetry events/min from IoT devices into a Kinesis Data Stream. Validate against a Glue Schema Registry with backward compatibility; if valid, enrich with device metadata from DynamoDB and write Parquet to S3/date; if invalid, route to a DLQ. Add X-Ray tracing and a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement per-tenant shards and DynamoDB-backed dedupe, with region-local raw and replicated derived data, using Kinesis shard splitting and Lambda + Fargate for processing; OpenTelemetry traces p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: Ingest 1–2k JSON events/min into a Kinesis Data Stream in a single region. Validate against Glue Schema Registry with backward compatibility; if valid, enrich with device metadata f",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a Kinesis producer + Lambda consumer. Validator consults a DynamoDB schema registry (versions v1/v2) to validate fields/types by event.version, routing invalids to a DLQ. Valid events are transfor",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a regional KDS to ingest; a Lambda/Fargate consumer validates against Glue Schema Registry, computes a data-quality score (required fields, ranges), deduplicates deviceId+ts in DynamoDB with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question introduces Glue Schema Registry and schema evolution, a common but underrepresented real-world constraint for beginner engineers. It tests practical streaming ingestion, data validation, and basic tracing.\n\n## Key Concepts\n- Glue Schema Registry\n- Backward-compatible schema\n- Data enrichment from DynamoDB\n- Parquet on S3 with date partition\n- Kinesis Data Streams\n- DLQ and observability (X-Ray)\n- LocalStack testing\n\n## Code Example\n```javascript\n// Pseudocode for the consumer path\nasync function handleRecord(record){\n  const valid = validateWithSchema(registry='Telemetry', version='latest', record);\n  if(valid){\n    const meta = await dynamo.getItem({deviceId: record.deviceId});\n    const enriched = {...record, meta};\n    await s3.putParquet(enriched, partitionBy='date');\n  } else {\n    await dlq.push(record);\n  }\n}\n```\n\n## Follow-up Questions\n- How would you handle schema evolution with multiple producers using backward compatibility?\n- How would you test end-to-end locally when Glue Schema Registry is not available?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-21",
        "questionId": "q-25484",
        "question": "Design an advanced cross-region ingestion strategy that enforces per-tenant data residency while ensuring exactly-once processing and cost control. Outline architecture using AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. Include tenant isolation, dedup, late data handling, audit trails, and observability across regions. What trade-offs would you make?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an advanced cross-region telemetry pipeline supporting 3 tenants with per-tenant failover SLAs. Requirements include in-region primary with cross-region secondary, auto-failover within a 15-minute RTO, per-tenant…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition Kinesis streams by tenantId and region to ensure tenant-scoped sharding. Use idempotent Lambda or Fargate consumers that perform deduplicated writes to DynamoDB and emit immutable events to S3 with tenant/date…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route each tenant's events to its home region's Kinesis Data Streams and S3, using a per-tenant KMS key for encryption. Enforce exactly-once processing with a DynamoDB deduplication table (tenantId, eventId) checked bef…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Partition Kinesis streams by tenant ID and region to ensure data locality and tenant isolation. Implement deduplication in Lambda using a composite key of tenant ID and message ID, storing processed IDs in DynamoDB with…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your understanding of multi-tenant architectures, data residency compliance, exactly-once semantics, and observability at scale using AWS primitives.\n\n## Key Concepts\n- **Per-tenant residency**: Route tenant data to their designated home region using Kinesis and S3, with dedicated KMS keys for encryption\n- **Exactly-once processing**: Use DynamoDB deduplication with idempotent consumers to prevent duplicate processing\n- **Region-local streams**: Deploy Kinesis and storage in each region while enabling global analytics via Athena cross-region queries\n- **OpenTelemetry tracing**: Implement distributed tracing across Lambda, Fargate, and Kinesis services with W3C TraceContext propagation\n- **Cost controls**: Leverage S3 lifecycle policies, Glue catalog partitioning, and DynamoDB TTL for efficient resource utilization\n\n## Code Example\n```javascript\nconst AWS = require('aws-sdk');\nconst ddb = new AWS.DynamoDB.DocumentClient();\nconst kinesis = new AWS.Kinesis();\n\nasync function processEvent(tenantId, event) {\n  const dedupKey = `${tenantId}#${event.eventId}`;\n  \n  // Check deduplication\n  try {\n    await ddb.put({\n      TableName: 'dedup-table',\n      Item: { id: dedupKey, ttl: Date.now() + 86400000 },\n      ConditionExpression: 'attribute_not_exists(id)'\n    }).promise();\n  } catch (e) {\n    if (e.code === 'ConditionalCheckFailedException') return; // Duplicate\n    throw e;\n  }\n  \n  // Emit to tenant's regional Kinesis\n  await kinesis.putRecord({\n    StreamName: `tenant-${tenantId}-stream`,\n    Data: JSON.stringify(event),\n    PartitionKey: tenantId\n  }).promise();\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-22",
        "questionId": "q-25350",
        "question": "Design an intermediate AWS DVA pipeline to ingest telemetry from a fleet of devices deployed across two regions (us-east-1 and eu-west-1) and two AWS accounts. Ingest 15k–25k JSON events per minute, validate against an evolving schema with Glue Schema Registry, deduplicate by deviceId+ts using DynamoDB, store valid records to S3 Parquet partitioned by region/date, route invalid JSON to a DLQ, and achieve near-exactly-once semantics via Kinesis Data Streams and Lambda. Include a 7-day backfill, observability, and a LocalStack testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use two region streams with a cross-account producer/consumer role. Devices publish to us-east-1 stream; a Lambda pipeline validates with Glue Schema Registry, deduplicates via a DynamoDB deviceId+ts",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage a daily Glue ETL to validate schema and output Parquet, plus an Athena-based delta check to compare day-over-day row counts. Emit a quality score JSON to s3://telemetry/quality-reports/YYYY/M",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A token-bucket per-tenant quota stored in DynamoDB governs per-minute sampling rates. The ingesting Lambda atomically decrements tokens; events within quota publish to Kinesis Data Streams using tenant ID as the partiti…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant privacy controls using a DynamoDB policy table keyed by tenantId. Ingest telemetry through regional Kinesis Data Streams, where Lambda functions apply tenant-specific masking policies—PII redaction,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests cross-region data flow, schema evolution, deduplication, and observable ops under realistic constraints.\n\n## Key Concepts\n\n- Cross-region data ingestion with Kinesis; multi-account IAM roles\n- Glue Schema Registry for evolving schemas\n- Idempotent sinks to S3 and DynamoDB deduplication\n- Backfill strategy and late-arrival handling\n- Observability with CloudWatch and X-Ray; LocalStack testing\n\n## Code Example\n\n```javascript\n// Pseudo: idempotent write to DynamoDB + S3\n```\n\n## Follow-up Questions\n\n- How would you verify exactly-once in practice?\n- How would you test schema evolution with backward-compatibility?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-23",
        "questionId": "q-5622",
        "question": "In a multi-tenant telemetry system on AWS, tenants can enable or disable features at runtime and mutate event schemas. Design a streaming ingestion path that guarantees per-tenant data isolation, supports dynamic feature flags, and provides deterministic replay for a single tenant without affecting others. Include a concrete architecture using Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, Lake Formation, and OpenTelemetry. Explain tenant-scoped keys, schema registry with evolution, replay semantics, and security controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed tenancy-aware pipeline: a single Kinesis Data Stream partitioned by tenant_id; per-tenant prefixes in S3 for Parquet storage; DynamoDB for deduplication and metadata keyed by tenant_id+device_id+timestamp; Glue…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition by tenant_id and event_date; route all telemetry through a Kinesis Data Stream consumed by a Lambda/Fargate worker that validates against Glue Schema Registry, deduplicates with a per-tenant",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Approach: use two Kinesis streams (High and Normal). A validation Lambda checks required fields (deviceId, ts, correlationId) and severity; routes by severity to the appropriate stream. Downstream Lam",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cross-region telemetry pipeline ingesting 30k JSON events/min from devices across three regions, enforcing PII masking in flight and at rest with per-record privacy policies. Route masked dat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end multi-tenant design, runtime feature toggles, schema evolution, and deterministic replay using AWS DVA primitives.\n\n## Key Concepts\n- Multi-tenant isolation with Lake Formation and tenant-scoped keys\n- Schema evolution via Glue Schema Registry with backward/forward compatibility\n- Deterministic replay using per-tenant replay token in DynamoDB\n- Idempotent writes by deduping event_id in DynamoDB\n- End-to-end observability with OpenTelemetry; secure access controls\n\n## Code Example\n```javascript\n// Pseudo dedupe and write path\nasync function processEvent(evt){\n  const {tenant_id, event_id, payload} = evt;\n  // ensure idempotent dedupe\n  const exists = await dynamo.get({TableName:'TenantDedup', Key:{tenant_id, event_id}});\n  if (exists.Item) return;\n  await dynamo.put({TableName:'TenantDedup', Item:{tenant_id, event_id, ts: Date.now()}});\n  // write raw to S3 with partition tenant/date (implementation omitted)\n}\n```\n\n## Follow-up Questions\n- How would you handle a schema-breaking change in a live tenant dataset?\n- How would you validate replay correctness under a backfill window?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-24",
        "questionId": "q-10420",
        "question": "You’re receiving ~800 devices sending JSON telemetry into a regional Kinesis Data Stream. Design a minimal, beginner-friendly pipeline using AWS DVA primitives to validate JSON, redact PII in-flight (emails, phones, tokens), deduplicate by deviceId+ts, and write clean records to S3 Parquet partitioned by region/date. Route invalid JSON to a DLQ and keep an auditable log of redactions. Include a 7-day backfill plan and basic observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed: produce 2k JSON events/min with a correlationId, push to Kinesis Data Stream in one region. A Lambda consumer validates JSON, computes a per-record quality score, and writes valid records to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Do dedupe first with DynamoDB keyed by sha256(deviceId+ts); in a regional Lambda mask deviceId and customerId using HMAC-SHA256 with a per-region KMS key, replacing only the payload while leaving the",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a KDS-based pipeline: a Lambda validates JSON, redacts PII fields (emails, phones, tokens) before parsing, deduplicates by deviceId+ts with DynamoDB, writes valid events to S3 Parquet partition",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Architect a 3-region pipeline: devices publish via AWS IoT Core to Kinesis Data Streams. A Lambda validates schema, dedups by deviceId+ts, assigns a quality score, and writes valid events to S3 Parque",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a compliant, auditable streaming pipeline focusing on PII redaction, idempotent writes, and backfill—a common real-world requirement for beginner to mid-level roles.\n\n## Key Concepts\n- PII redaction in streaming data\n- Deduplication with DynamoDB keyed by deviceId+ts\n- Backfill strategy using Glue or similar\n- Parquet partitioning by region/date\n- Observability with CloudWatch and simple SLIs\n\n## Code Example\n```javascript\nfunction redact(record){\n  const r = JSON.parse(record);\n  if (r.userEmail) r.userEmail=\"REDACTED\";\n  if (r.phone) r.phone=\"REDACTED\";\n  if (r.token) r.token=\"REDACTED\";\n  return JSON.stringify(r);\n}\n```\n\n## Follow-up Questions\n- How would you validate schema evolution with optional fields?\n- How would you handle partial failures without dropping data?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-08T06:57:58.478Z",
    "version": 1
  }
]