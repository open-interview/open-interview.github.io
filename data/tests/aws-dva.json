[
  {
    "id": "test-aws-dva",
    "channelId": "aws-dva",
    "channelName": "AWS DVA",
    "title": "AWS DVA Knowledge Test",
    "description": "Test your understanding of AWS DVA with 25 questions.",
    "questions": [
      {
        "id": "aws-dva-q-0",
        "questionId": "q-9216",
        "question": "Design an autonomous, tenant-aware observability and self-healing pipeline for cross-region telemetry ingestion using Kinesis, Lambda/Fargate, S3, Glue, Athena, DynamoDB and OpenTelemetry. Describe how per-tenant SLAs drive automatic failover, how traces/metrics guide remediation, and how to prevent cascading outages while preserving exactly-once semantics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-region, multi-tenant telemetry pipeline that applies per-tenant differential privacy at ingestion. Ingest events to Kinesis Data Streams, fan out to per-tenant Lambda/Fargate workers that enforce a privac…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "An autonomous tenant-aware observability and self-healing pipeline with per-tenant SLAs mapped to Kinesis shard pools, OpenTelemetry traces across Lambda/Fargate and S3, and an idempotent writer to Dy",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement per-tenant catalog databases (tenant_<id>) with IAM/Lake Formation controls and tenant-prefixed S3 prefixes (tenant=<id>/region). Ingest via Kinesis to Lambda, inject tenant_id, store Parque",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant Kinesis partitioning with a DynamoDB dedupe keyed by tenant+sequence to guarantee exactly-once ingestion. Audit trails land in S3 with Object Lock Governance; replicate immutabilit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design autonomous, tenant-aware resilience in a distributed telemetry pipeline, emphasizing observability, per-tenant governance, and cross-region failure handling.\n\n## Key Concepts\n\n- OpenTelemetry integration across Kinesis → Lambda/Fargate → S3 → Glue/Athena\n- Per-tenant SLAs guiding auto remediation and failover\n- Exactly-once semantics with idempotent writes and deduplication\n- Self-healing, circuit breakers, and backpressure to prevent cascading outages\n\n## Code Example\n\n```javascript\n// Minimal idempotent writer for deduplication\nasync function writeEvent(event, store){\n  const id = event.dedupToken;\n  if (await store.exists(id)) return;\n  await store.put(id, event.payload);\n}\n```\n\n## Follow-up Questions\n\n- How would you simulate regional outages and verify auto-remediation?\n- How would you measure SLA compliance per tenant and tune shard allocation?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-1",
        "questionId": "q-13226",
        "question": "Design a two-region, multi-tenant pipeline where each tenant stores a JSON schema and rules in DynamoDB. Regional Kinesis streams feed a Lambda validator that applies tenant rules, computes a per-record quality score, and deduplicates by tenant+recordId. Valid events go to S3 Parquet (tenant/date); invalids to a DLQ. Use versioned schemas, backfill, and per-tenant Glue/Athena catalogs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement tenant-aware validation with per-tenant JSON schemas, validation rules, and schema versions stored in DynamoDB. Regional Kinesis Data Streams invoke Lambda validators that apply tenant-specific rules, compute…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest telemetry through a regional Kinesis Data Stream. A Lambda function validates JSON structure and computes a data quality score from 0-100. Implement deduplication using DynamoDB with a composite key of (deviceId,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant isolation using Lake Formation LF-tags with policy-based grants, expose only non-sensitive columns through masked views, and enforce row-level security filters in Athena queries. Leverage CloudTrail…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an end-to-end multi-tenant pipeline: a single Kinesis Data Stream ingests JSON; a Lambda deduplicates by tenant_id + event_id and writes per-tenant Parquet to S3 with daily partitions; register",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design tenant-aware schema versioning, dynamic validation, and cross-region data governance with minimal operational overhead.\n\n## Key Concepts\n- Tenant isolation and dynamic validation\n- Schema versioning and backward compatibility\n- Data quality scoring and deduplication\n- DLQ routing and backfill strategies\n- Glue cataloging and Athena queries per tenant\n\n## Code Example\n```javascript\n// pseudo-validator sketch\n```\n\n## Follow-up Questions\n- How would you migrate tenants to a new validation rule without downtime?\n- How do you test schema evolution across tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-2",
        "questionId": "q-18955",
        "question": "Beginner AWS DVA task: design a one-region streaming pipeline that ingests 15k JSON telemetry events per minute from 4 device classes into a Kinesis Data Stream. Enforce per-record integrity with a correlationId, validate against a lightweight JSON schema in Lambda, and implement field-level data masking (redacting PII like email, phone, SSN) before persistence. Store valid events to S3 Parquet partitioned by deviceClass/date; deduplicate by deviceId+ts in DynamoDB; route invalid records to a DLQ. Include a 24h backfill plan and basic observability, plus a LocalStack test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an end-to-end observability plan using AWS DVA primitives with OpenTelemetry for cross-region, multi-tenant telemetry pipelines. Implement a unified collector that receives OTLP data from all layers, with tenant-…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Do dedupe first with DynamoDB keyed by sha256(deviceId+ts); in a regional Lambda mask deviceId and customerId using HMAC-SHA256 with a per-region KMS key, replacing only the payload while leaving the",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: regional Kinesis stream fed into a Lambda that validates with a small JSON schema, masks PII (email, phone, SSN) before storage, dedups by deviceId+ts in DynamoDB, writes valid records to S3",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a regional KDS with tenant-scoped keys, per-tenant Lambda/Fargate workers, and Lake Formation isolation. Deduplicate with DynamoDB (tenant+eventId). Persist Parquet per tenant/date in S3; register",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical design of a privacy-conscious, end-to-end streaming pipeline that validates, masks, deduplicates, and persists data with observability.\n\n## Key Concepts\n- Kinesis Data Streams, Lambda, DynamoDB dedup, S3 Parquet storage, DLQ\n- JSON schema validation, field-level masking of PII, correlationId\n- Backfill strategy, LocalStack testing, basic observability (CloudWatch, X-Ray)\n\n## Code Example\n```javascript\nfunction maskPII(record) {\n  const r = {...record};\n  if (r.email) r.email = r.email.replace(/[^@]+@/, '***@');\n  if (r.phone) r.phone = r.phone.length > 4 ? r.phone.slice(0, 3) + '****' + r.phone.slice(-4) : r.phone;\n  if (r.ssn) r.ssn = '***-**-' + r.ssn.slice(-4);\n  return r;\n}\n```\n\n## Follow-up Questions\n- How would you test the masking logic end-to-end, including replays?\n- How would you ensure no PII leaks to DLQ or downstream sinks?\n- How would you scale this to higher event rates or additional device classes?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-3",
        "questionId": "q-3435",
        "question": "Design a global telemetry pipeline with data residency rules: APAC streams stay in APAC, with optional cross-region replication only where allowed; guarantee exactly-once processing; support per-tenant schema evolution; enforce retention/legal hold; and mask PII in real-time. Propose concrete AWS DVA architecture and discuss idempotent writes, cross-region replication, watermarking, failure modes, and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: per-region Kinesis Data Streams with region-local processing; optional cross-region replication only where permitted. Exactly-once via idempotent writes to DynamoDB, deduplicated S3 Parquet",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a two-region Kinesis Data Streams architecture with Lambda functions that validate JSON schemas, compute per-record quality scores, and deduplicate using DynamoDB composite keys (deviceId+timestamp). Route val…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest through region-specific Kinesis Data Streams with tenant-aware routing to enforce data residency requirements. Implement a validation layer using AWS Lambda or Fargate that maintains per-tenant schema versions an…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a dual-path ingest: high-priority data goes to a dedicated Kinesis stream with low-latency Lambda processing; low-priority data is funneled to a separate stream with probabilistic downsampli",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests handling of data residency constraints at scale, combining streaming ingestion with governance, and real-time processing guarantees. Requires reasoning about tenant isolation, schema evolution, and production-grade failure handling.\n\n## Key Concepts\n\n- Data residency and cross-region replication controls\n- Exactly-once with idempotent writes across stores\n- Schema evolution using Glue Schema Registry\n- Late data handling with watermarks\n- Real-time PII masking in streaming path\n- Observability and failover strategies\n\n## Code Example\n\n```javascript\n// Pseudo-idempotent write to deduplicate\nfunction upsertDeduplicate(record) {\n  const key = record.deviceId + '|' + record.timestamp;\n  if (DynamoDB.putIfNotExists({Key: key, Item: record})) {\n    // write to Parquet sink in S3 via Glue job placeholder\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test cross-region residency controls and failover?\n- What metrics would you surface to detect residency violations and schema drift?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-4",
        "questionId": "q-22161",
        "question": "Design a cross-region, multi-tenant telemetry pipeline that ingests 1M events/min from 100+ tenants with per-tenant isolation, dynamic backpressure, and budget-based cost controls. Use AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. Explain tenant-scoped partitioning, quota enforcement, exactly-once enrichment, retention, and observability. What concrete architecture and failure modes would you implement?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a cross-region, multi-tenant telemetry pipeline ingesting 1M events/min from 100+ tenants using AWS primitives. Implement tenant-scoped Kinesis Data Streams partitioning with a central quota controller backed…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Proposed: produce 2k JSON events/min with a correlationId, push to Kinesis Data Stream in one region. A Lambda consumer validates JSON, computes a per-record quality score, and writes valid records to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Utilize per-tenant PartitionKey in Kinesis streams; implement idempotent writes to DynamoDB and S3 (parquet) keyed by tenantId+ts; fan-out through a cross-region stream to minimize latency; Glue catal",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: use two Kinesis streams (High and Normal). A validation Lambda checks required fields (deviceId, ts, correlationId) and severity; routes by severity to the appropriate stream. Downstream Lam",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses advanced capabilities in designing multi-tenant, multi-region telemetry systems with strict isolation requirements. It tests understanding of cost controls, backpressure mechanisms, and exactly-once processing semantics—critical skills for building resilient, scalable observability platforms at enterprise scale.\n\n## Key Concepts\n\n- **Tenant Isolation**: Partition-based isolation with deterministic routing to prevent tenant noise\n- **Quota Enforcement**: Central quota controller with per-tenant budgets and dynamic throttling\n- **Dynamic Backpressure**: Auto-scaling compute layer with circuit breakers and queue depth monitoring\n- **Exactly-Once Enrichment**: Idempotent writes with deduplication keys stored in DynamoDB\n- **Retention & Archival**: Tiered storage with S3 lifecycle policies and cross-region replication\n- **Observability**: OpenTelemetry with tenant context propagation throughout the pipeline\n\n## Failure Modes\n\n- **Throttling**: Tenant exceeds quota → 429 response with retry-after header, events buffered in overflow queue\n- **Regional outage**: Failover to secondary region with Kinesis stream replication\n- **Enrichment failure**: Dead-letter queue with replay mechanism after fix\n- **Partition hotting**: Rebalance partitions based on tenant traffic patterns\n\n## Follow-up Questions\n\n- How would you validate per-tenant SLAs and detect SLA violations in real-time?\n- How would you handle tenant migration or data residency requirements?\n- What cost optimization strategies would you implement for idle tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-5",
        "questionId": "q-1617",
        "question": "Design a per-tenant data lake access system on AWS where billions of Parquet files in S3 are consumed by Athena/Glue; describe how you would implement tenant isolation, masking, and row-level security using Lake Formation, and how you would validate auditing and performance under burst workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant isolation using Lake Formation LF-tags with policy-based grants, expose only non-sensitive columns through masked views, and enforce row-level security filters in Athena queries. Leverage CloudTrail…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Single Kinesis stream partitioned by tenantId; Lambda masks PII and validates against per-tenant schemaVersion stored in DynamoDB. Valid events route to S3 Parquet under tenant/year=ver; Glue Spark jo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design uses two KDS streams feeding a single adapter Lambda that normalizes to a common schema. Validate schemas, dedupe by deviceId+ts in DynamoDB, and write valid events to S3 Parquet by tenant/date",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adapter path: MQTT and HTTPS feed a single Kinesis stream. Normalize to {deviceId, ts, metric, correlationId}. Validate required fields and numeric ranges, dedupe by deviceId+ts with DynamoDB, and sto",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nMulti-tenant data lakes require fine-grained access control, data masking, and robust auditing; this evaluates policy design and operational validation skills.\n\n## Key Concepts\n- Lake Formation LF-tags and permissions for tenant isolation\n- Data masking and restricted views in Athena/Glue\n- Auditing via CloudTrail, Lake Formation logs, and CloudWatch metrics\n- Performance validation under burst workloads\n\n## Code Example\n```sql\n-- Example: masked view for a tenant\nCREATE VIEW tenant_view AS\nSELECT tenant_id,\n       CASE WHEN is_sensitive(col1) THEN 'REDACTED' ELSE col1 END\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-6",
        "questionId": "q-29913",
        "question": "Design a cross-account, multi-tenant telemetry pipeline that enforces per-tenant data residency (device data stays in the tenant’s AWS account/region) while supporting real-time ingestion for 1–5k events/sec per tenant, exactly-once processing, and tenant-level cost accounting. Use AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, plus OpenTelemetry. Explain tenant isolation, per-tenant quotas, cross-account access, and data export?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a cross-account, multi-tenant pipeline where each tenant has a dedicated Kinesis Stream in their account/region; a hub aggregator fan-ins to tenant-specific S3 buckets, with per-tenant quotas",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design uses region-scoped streams and S3 buckets per tenant, with a tenancy table in DynamoDB and per-tenant IAM. EU data lands in EU buckets and is lineage-tracked in Glue; US data is replicated to a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a multi-region Kinesis Data Streams with per-tenant access via Lake Formation, Glue Data Catalog for schema, and OpenTelemetry traces. Ingest 30k JSON/min, validate against a dynamic registry, ded",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a single-region streaming path using Kinesis for ingress, a DynamoDB token-bucket per device to throttle S3 writes to Parquet, and DLQ logging for quota-exceeded events. Include X-Ray tracing a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-account tenancy, data residency, and end-to-end guarantees at scale. Evaluates combining per-tenant Kinesis streams, secure cross-account access, and exactly-once semantics with observability and per-tenant cost accounting.\n\n## Key Concepts\n- Cross-account IAM roles and resource policies\n- Per-tenant data residency and isolation\n- Exactly-once processing across distributed components\n- Per-tenant cost accounting and quotas\n- OpenTelemetry across multi-account architecture\n- Data export and governance\n\n## Code Example\n```javascript\n// Pseudo dedupe key per record\nfunction getDedupKey(event, tenantId) {\n  return `${tenantId}#${event.deviceId}#${event.ts}`;\n}\n```\n\n## Follow-up Questions\n- How would you test schema changes across tenants without downtime?\n- What metrics would you collect to ensure per-tenant SLAs?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-7",
        "questionId": "q-16947",
        "question": "Design an advanced cross-region telemetry pipeline where each tenant supplies an evolving data contract (schema) for device telemetry, validated at ingestion via a central schema registry (DynamoDB + Glue Data Catalog), with per-tenant schema versions, exactly-once processing, and a safe migration path that preserves audit trails. Describe how you would implement tenant isolation, schema evolution, late data handling, and testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition data by tenant in Kinesis and a DynamoDB-based dedupe store keyed by (tenant, eventId) to enforce exactly-once processing; Lambda processors write to per-tenant S3 prefixes and register Glue",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest across regions with a per-tenant Kinesis Data Stream, Lambda/Fargate for validation against a dynamic registry (Glue Catalog + DynamoDB). Enforce tenant-isolated prefixes, versioned contracts,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use one Kinesis stream with tenant_id, route to per-tenant S3 prefixes, and enforce Lake Formation with per-tenant CMKs. Implement dedupe via a DynamoDB-based cache of event_ids, support schema evolut",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Producer writes to SQS; Consumer Lambda validates against a DynamoDB-stored schema (versions v1/v2), deduplicates by deviceId+ts, and writes valid events to S3 Parquet with daily partitioning. Invalid",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical multi-tenant schema management, exactly-once semantics, cross-region data flows, and how to validate evolving contracts in production.\n\n## Key Concepts\n- Dynamic schema registry (Glue Data Catalog + DynamoDB)\n- Exactly-once processing across regions (idempotent writes, dedupe)\n- Tenant isolation via prefixes and IAM scoping\n- Schema evolution strategies (backward/forward compatibility)\n- Observability with OpenTelemetry\n\n## Code Example\n```javascript\n// Pseudo ingestion validation\nfunction validate(record, version) {\n  // fetch contract by tenant and version from registry\n  // validate fields, types, requiredness\n  // return normalized record or throw\n}\n```\n\n## Follow-up Questions\n- How to test schema migration with mixed-version tenants?\n- How to handle late-arriving data that references old contracts?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-8",
        "questionId": "q-8724",
        "question": "Design a cost-aware, multi-tenant telemetry routing pipeline that down-samples low-priority tenant events in real time while guaranteeing 95th percentile latency under 2 seconds for high-priority tenants. Use Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. Explain tenant-scoped partitioning, downsampling rules, latency targets, backpressure handling, and per-tenant cost accounting?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a 3-region pipeline: devices publish via AWS IoT Core to Kinesis Data Streams. A Lambda validates schema, dedups by deviceId+ts, assigns a quality score, and writes valid events to S3 Parque",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a per-tenant SLA breach alerting flow: ingest traces from all tenants into a Kinesis Data Stream, enrich with Lambda/Fargate, and score latency per tenant using OpenTelemetry spans (baggage",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a single-region KDS with a Lambda enrichment that caches device metadata in DynamoDB and uses a deterministic, idempotent S3 key (region/date/deviceId+ts). The Lambda retries API cal",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a dual-path ingest: high-priority data goes to a dedicated Kinesis stream with low-latency Lambda processing; low-priority data is funneled to a separate stream with probabilistic downsampli",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes design skills for cost-aware, multi-tenant telemetry at scale with tenancy isolation and latency SLAs.\n\n## Key Concepts\n- Multi-path ingest by priority\n- Real-time downsampling strategies\n- Per-tenant partitioning and access control\n- OpenTelemetry correlation for latency visibility\n- Cost governance via TTL, budgets, and lifecycle\n\n## Code Example\n```javascript\n// Pseudo: decide whether to sample a record for a tenant\nfunction shouldSample(priority, tenantId, rate) {\n  // priority: 'high' or 'low'; rate: 0<rate<=1; return boolean\n  if (priority === 'high') return false;\n  // simple hash-based sampling per tenant\n  const h = hash(tenantId) % Math.floor(1/rate);\n  return h !== 0;\n}\n```\n\n## Follow-up Questions\n- How would you validate SLA under burst traffic?\n- How would you adapt sampling per-tenant in production?\n- How would you test cost impact with simulated workloads?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-9",
        "questionId": "q-20417",
        "question": "Design a cross-region telemetry pipeline for privacy-conscious devices. Ingest 50k events/min per region into Kinesis Data Streams, apply on-the-fly PII redaction in a Lambda consumer, store valid events to S3 Parquet partitioned by region/date, and update per-device state in DynamoDB with idempotent writes. Encrypt at rest with KMS envelope keys, rotate keys monthly, and enforce region-scoped access via Lake Formation. Plan 48h backfill and a DLQ for invalids?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Key ideas: use MERGE INTO Iceberg with event_id as PK for upserts; route late data with a bounded lateness window and a retry path; leverage Iceberg schema evolution with defaults for new cols; ensure",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Cross-region telemetry: ingest 50k/min per region into Kinesis Data Streams, apply on-the-fly PII redaction in a Lambda consumer, store valid events to S3 Parquet partitioned by region/date, and upser",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use per-tenant Kinesis shards with dedicated DynamoDB metadata mappings to ensure strict isolation. Encrypt PII using envelope encryption with tenant-specific AWS KMS customer master keys (CMKs), storing encrypted paylo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Attach a provenance graph to each event by adding a provenanceId that links to a per-tenant graph stored in DynamoDB; each processing step writes a Edge {from, to, step, ts, tenant, hash}. Cross-regio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical handling of privacy, cross-region data flow, and robust guarantees (idempotent writes, backfill, DLQ) using AWS DVA primitives.\n\n## Key Concepts\n- Cross-region data ingress with Kinesis Data Streams\n- On-the-fly PII redaction in Lambda\n- Parquet storage with region/date partitioning in S3\n- DynamoDB upserts with idempotency keys\n- KMS envelope encryption and key rotation\n- Lake Formation access control in multi-region context\n- DLQ, 48h backfill, OpenTelemetry observability\n\n## Code Example\n```javascript\n// Example: redact PII in streaming payloads (simplified)\nexports.handler = async (event) => {\n  const redacted = event.records.map(r => {\n    const payload = JSON.parse(Buffer.from(r.kinesis.data, 'base64').toString('utf-8'));\n    payload.userEmail = 'REDACTED';\n    payload.phone = payload.phone ? 'REDACTED' : payload.phone;\n    return {\n      ...r,\n      body: JSON.stringify(payload)\n    };\n  });\n  // forward redacted records to destination\n  return { records: redacted };\n};\n```\n\n## Follow-up Questions\n- How would you validate key rotations and guarantee no data loss?\n- How would you model regional access policies in Lake Formation for multi-region workloads?\n- What metrics and dashboards would you implement to monitor latency, failure, and data quality?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-10",
        "questionId": "q-26516",
        "question": "In a single AWS region, design a beginner-level AWS DVA pipeline that ingests 2k JSON telemetry events/min from IoT devices into a Kinesis Data Stream. Introduce a versioned Schema Registry stored in DynamoDB (versions v1, v2). A Lambda validator uses event.version to enforce required fields and types, routing invalid records to a DLQ. Valid records are transformed to Parquet and written to S3/date. Include X-Ray tracing and a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-region telemetry ingestion and anomaly-detection pipeline for millions of IoT devices across tenants. Ingest via two Kinesis Data Streams (regionA/regionB); feature extraction in Fargate/Lambda; score wit…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design uses region-scoped streams and S3 buckets per tenant, with a tenancy table in DynamoDB and per-tenant IAM. EU data lands in EU buckets and is lineage-tracked in Glue; US data is replicated to a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Kinesis producer + Lambda consumer. Validator consults a DynamoDB schema registry (versions v1/v2) to validate fields/types by event.version, routing invalids to a DLQ. Valid events are transfor",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a resilient two-region streaming pipeline: Deploy Kinesis Data Streams in each region to ingest approximately 6,000 JSON events per minute. Implement Lambda functions to validate records against JSON schemas (sup…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the candidate's ability to model schema evolution, simple validation, and end-to-end tracing in a beginner-friendly AWS DVA pipeline.\n\n## Key Concepts\n- Kinesis Data Streams ingestion\n- DynamoDB as a lightweight schema registry\n- Lambda-based validation using version-aware schemas\n- Parquet storage in S3 with date partitioning\n- DLQ routing and X-Ray traceability\n\n## Code Example\n```javascript\n// Pseudo impl: Lambda validator fetches schema by version from DynamoDB, validates fields/types, and routes accordingly\n```\n\n## Follow-up Questions\n- How would you test schema evolution across versions in LocalStack?\n- What changes would you make to support a third version without downtime?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-11",
        "questionId": "q-20889",
        "question": "Design a cross-region, multi-tenant telemetry pipeline in AWS DVA with per-tenant isolation, rate limits, and in-tenant ordering. Ingest into regional KDS, dedupe in DynamoDB by tenantId+deviceId+ts, write valid events to S3 Parquet, and mirror to a secondary region. Use a control plane to adjust quotas; support schema evolution via Glue Registry; discuss trade-offs (latency, cost). Include LocalStack tests and OpenTelemetry/X-Ray observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Glue Schema Registry for versioned schemas, write to Iceberg tables on S3 to support schema evolution and upserts with a deterministic key, implement idempotent writes via a normalized primary key",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a cross-region, multi-tenant telemetry pipeline with per-tenant isolation, per-tenant rate limits, and in-tenant ordering. Route via regional KDS, dedupe in DynamoDB on (tenantId, deviceId, ts)",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement per-tenant Kinesis Data Streams shards with a quota management system stored in DynamoDB for real-time rate limiting. At the edge (API Gateway), apply adaptive sampling algorithms to under-loaded tenants while…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument all Lambdas with OpenTelemetry; run ADOT collector to export traces to X-Ray and CloudWatch Logs. Propagate trace IDs across Kinesis producer, Lambda, and sink. Emit per-record latency and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world multi-tenant data governance, cross-region reliability, and operational controls.\n\n## Key Concepts\n- Per-tenant isolation and quota management\n- Ordering guarantees within a tenant\n- Deduplication with composite keys in DynamoDB\n- Schema evolution via Glue Registry\n- Cross-region replication and cost/latency trade-offs\n\n## Code Example\n```javascript\n// Pseudo-code: tenant-scoped routing logic and dedupe check\nasync function processEvent(evt){\n  const key = `${evt.tenantId}#${evt.deviceId}#${evt.ts}`;\n  const exists = await dynamo.get({Key: {pk: key}});\n  if(exists){ return; }\n  await dynamo.put({Item: {pk: key, ...evt}});\n  // forward to region-specific S3 writer\n}\n```\n\n## Follow-up Questions\n- How would you test per-tenant quotas under bursty traffic?\n- What metrics would you emit to detect skew among tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-12",
        "questionId": "q-4176",
        "question": "Beginner AWS DVA task: design a two-tenant telemetry pipeline where each tenant's data lands in a separate S3 prefix and DynamoDB table; implement per-tenant IAM roles to restrict access; specify data schema choices, deduplication strategy, and a simple 7-day backfill plan. Use Kinesis, Lambda, S3, Glue, Athena, and DynamoDB. How would you implement tenant isolation and governance end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set up a Kinesis Data Stream in the region, a Node.js Lambda to validate each JSON line, assign a data quality score, and check for required fields. Use a single DynamoDB table with composite keys ten",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route data by tenantId, storing events in s3://bucket/tenants/{tenantId}/{region}/{date}/ as Parquet; use per-tenant dedupe in DynamoDB with deviceId+seq; create tenant-scoped IAM roles granting acces",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design an end-to-end observability plan using AWS DVA primitives with OpenTelemetry for cross-region, multi-tenant telemetry pipelines. Implement a unified collector that receives OTLP data from all layers, with tenant-…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest via Kinesis; a Lambda masks PII (phone, name) per-tenant using KMS keys, writing masked events to S3 Parquet. Unmasked copies go to a separate, access-controlled bucket. DynamoDB audits redacti",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProbes practical tenant isolation and governance in a single-streaming pipeline using standard AWS DVA primitives.\n\n## Key Concepts\n- Tenant isolation via prefixes and per-tenant IAM\n- Data deduping with per-tenant keys\n- Lifecycle: backfill, Glue catalog, Athena queries\n\n## Code Example\n```javascript\n// Pseudo IAM policy fragment per tenant\n{\n  \"Effect\": \"Allow\",\n  \"Action\": [\"s3:GetObject\",\"s3:PutObject\"],\n  \"Resource\": [\"arn:aws:s3:::bucket/tenants/tenantA/*\"]\n}\n```\n\n## Follow-up Questions\n- How would you scale IAM roles as tenants grow?\n- How would you test the isolation boundaries?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-13",
        "questionId": "q-6670",
        "question": "Design a cross-region telemetry ingestion path for a global fleet of devices that must enforce per-tenant isolation, automatic data minimization, and regulatory purge requests within 24 hours. Use AWS DVA primitives: Kinesis Data Streams, Kinesis Data Firehose, Lambda/Fargate, S3, Glue, Athena, DynamoDB, Lake Formation, and IAM. Explain tenant-scoped partition keys, encryption, purge workflow, backfill handling, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-tenant telemetry pipeline using Kinesis Data Streams for ingestion, with tenant-scoped partition keys (tenantId + eventType) ensuring ordered processing per tenant. Implement dynamic enrollment by provisi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a multi-tenant telemetry pipeline across two AWS accounts: ingest 15k-25k events/min from three tenants via separate Kinesis streams, validate and enrich in a shared Lambda, store valid reco",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a multi-region, tenant-aware pipeline: route events to tenant-scoped Kinesis streams, shard by region, write Parquet in S3 per tenant via Firehose, catalog with Glue, query via Athena; metadat",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Approach: In two AWS regions, ingest via Kinesis Data Streams with a Lambda consumer. Use DynamoDB for dedupe by deviceId+ts, applying schema_versioned validation (v1: deviceId, ts, temp; v2: +battery",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design multi-region data isolation, regulatory purge, and schema evolution with specific AWS primitives.\n\n## Key Concepts\n- Tenant isolation, KMS encryption, Lake Formation permissions\n- Cross-region data movement and purge workflows\n- Schema evolution with optional fields in Glue/Parquet\n\n## Code Example\n```javascript\n// No code required for this level\n```\n\n## Follow-up Questions\n- How would you test purge latency and data backfill scenarios?\n- What are the failure modes and monitoring signals for such a pipeline?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-14",
        "questionId": "q-21692",
        "question": "Design a cross-region, multi-tenant telemetry pipeline with per-tenant data residency: some tenants stay in their home region; others are replicated for analytics. Describe the end-to-end flow using Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. Include tenant-aware routing, per-tenant retention, exactly-once processing, auditability, testing, and cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant Kinesis streams in regional pairs; Lambda/Fargate fan-out; raw events to S3 as Parquet partitioned by tenant/date; Glue catalog + Athena for queries. DynamoDB stores per-tenant retentio",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Edge routing at ingestion: DynamoDB stores per-tenant residency and replication rules; a policy check in Lambda validates event.region against policy and writes to the tenant’s regional Kinesis stream",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Architect a cross-region telemetry pipeline with per-tenant isolation and SageMaker-based anomaly scoring. Use Kinesis streams per region, Lambda/Fargate enrichment, and S3 Parquet storage with tenant",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose region-local KDS streams per tenant, a dynamic Glue Schema Registry for validation, and a per-tenant dedupe store in DynamoDB. Ingested events route to S3 Parquet partitions by tenant/date, wi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a compliant, scalable multi-region telemetry pipeline with per-tenant governance, residency rules, and cost controls using AWS DVA primitives and OpenTelemetry.\n\n## Key Concepts\n\n- Tenant policy management in DynamoDB\n- Residency-aware routing to region streams\n- Exactly-once processing and idempotent writes\n- End-to-end tracing with OpenTelemetry across components\n- Data lifecycle: per-tenant retention, purge strategies, and cost controls\n- Testing: residency canaries, audit validation, DR scenarios\n\n## Code Example\n\n```typescript\n// Pseudo routing function\nfunction routeEvent(evt: TelemetryEvent){\n  const policy = getPolicy(evt.tenantId);\n  if (policy.residency === 'home' && evt.region !== policy.homeRegion){\n    throw new Error('Residency violation');\n  }\n  const targetStream = policy.replicate ? `${policy.homeRegion}-stream` : `${policy.homeRegion}-stream`;\n  return writeToStream(targetStream, evt);\n}\n```\n\n## Follow-up Questions\n\n- How would you test residency rules in CI/CD?\n- How would you monitor cross-region data drift and retry semantics?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-15",
        "questionId": "q-12734",
        "question": "Design a cross-region AWS DVA telemetry pipeline that enforces per-tenant data residency, detects schema drift at ingestion, quarantines non-conforming records, and supports SLA-driven backfill. Describe tenancy isolation, evolving schemas, quarantine workflow, automated replay, and OpenTelemetry traces across regions. Include data modeling, idempotency, and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest through region-specific Kinesis Data Streams with tenant-aware routing to enforce data residency requirements. Implement a validation layer using AWS Lambda or Fargate that maintains per-tenant schema versions an…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a shared Kinesis stream with tenantId in the partition key and a DynamoDB-backed quota store to enforce per-tenant limits. Use Lambda/Fargate to enrich and write Parquet to S3; implement idemp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route each event by tenantId to a per-tenant consumer path. In latency-sensitivity mode, batch events into 1s windows through a micro-batching sink; otherwise stream directly to Lambda/Fargate. Ensure",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a KDS→Lambda→S3 pipeline: validate JSON against a minimal schema, compute a 0–100 quality score, route valid records to S3 Parquet by region/date and invalids to a DLQ. Use DynamoDB to track",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of enterprise-scale data pipeline challenges including multi-region architecture, schema evolution management, data governance compliance, automated recovery mechanisms, and observability patterns in production environments.\n\n## Key Concepts\n- Cross-region data residency enforcement with tenant isolation\n- Schema evolution and drift detection at ingestion boundaries\n- Quarantine workflows for non-conforming data preservation\n- SLA-driven automated backfill with replay mechanisms\n- Distributed tracing with OpenTelemetry across regional boundaries\n- Idempotent processing patterns and cost-optimized resource allocation\n\n## Code Example\n```javascript\n// Schema validation with tenant awareness\nfunction validateRecord(record, tenantSchema) {\n  if (record.schemaVersion !== tenantSchema.version) {\n    return { valid: false, reason: 'schema_version_mismatch' };\n  }\n  \n  const requiredFields = tenantSchema.fields.filter(f => f.required);\n  const missingFields = requiredFields.filter(f => !(f.name in record));\n  \n  return missingFields.length === 0 \n    ? { valid: true } \n    : { valid: false, reason: 'missing_fields', fields: missingFields };\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-16",
        "questionId": "q-22664",
        "question": "Beginner AWS DVA task: In a single region, ingest 1k–2k events/min from two producers into a Kinesis Data Stream. Each event includes tenantId, ts, deviceId, metric. Validate schema, enforce per-tenant quotas using a DynamoDB counter, write only valid events to S3 Parquet under tenantId/date prefixes, and route invalids to a DLQ. Add end-to-end tracing with X-Ray and provide a LocalStack test plan focusing on tenant isolation and backfill?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a 1-region, 2-producer pipeline: Kinesis ingestion for 1k–2k events/min, each with tenantId. Validate schema, increment per-tenant quota in DynamoDB, drop or send to DLQ if over quota, persist",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use OpenTelemetry end-to-end. Propagate trace context through Kinesis payloads, have each Lambda/Fargate task start a span with the incoming trace-id, and export via OTLP to a central collector in a h",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest events via Kinesis Data Streams using per-tenant shard keys to ensure even distribution across shards. A Lambda or Fargate consumer processes the stream and writes Parquet files to S3, partitioned by tenant and d…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Store immutable, per-tenant records in S3 with versioning and a per-tenant checkpoint in DynamoDB. Replay job reads the selected window from S3/Glue catalog, re-ingests to Kinesis with idempotent writ",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical streaming design with per-tenant isolation and simple quota enforcement, plus observability and local testing using LocalStack. It fills gaps around multi-tenant data handling not covered by prior questions.\n\n## Key Concepts\n- Kinesis Data Streams ingestion\n- DynamoDB for per-tenant quotas\n- S3 Parquet partitioning by tenant/date\n- DLQ handling for invalid records\n- X-Ray tracing across services\n- LocalStack for end-to-end local validation\n\n## Code Example\n```javascript\n// Pseudo: validate and route records with quota check\nexports.handler = async (event) => {\n  // parse records\n  // validate tenantId, ts, deviceId, metric\n  // increment quota in DynamoDB; if over limit, push to DLQ\n  // otherwise write to S3 Parquet under tenant/date\n  // propagate X-Ray trace\n}\n```\n\n## Follow-up Questions\n- How would you test tenant quota exhaustion and backpressure in LocalStack?\n- How would you handle late-arriving events that arrive after quota is checked?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-17",
        "questionId": "q-12605",
        "question": "In a beginner AWS DVA scenario, design a single-region telemetry pipeline for 10k JSON events/min from edge devices. Build end-to-end ingestion with Kinesis Data Streams, a Lambda that validates JSON and assigns a simple data-quality score, DynamoDB for dedupe (deviceId+ts), store valid events in S3 as Parquet partitioned by region/date, route invalids to a DLQ (SQS), and plan a 24h backfill with a lineage manifest in S3. Include basic observability and a LocalStack test plan. Include cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Candidate should describe: Kinesis config and correlationId enforcement; control stream to DynamoDB quotas; Lambda logic to apply per-record sampling, JSON validation, dedupe, and Parquet write; DLQ r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Kinesis Data Streams pipeline with a normalization Lambda validating tenantId/deviceId/schema, then a routing stage that writes valid events to per-tenant S3 prefixes (tenant/date) with reten",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Streams with auto-scaling shards to ingest 10k JSON/min; a Lambda validates and computes a data-quality score (0-1) per record, deduplicates via DynamoDB (deviceId+ts), writes valid e",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a concrete architecture: tenantId as the Kinesis shard key; DynamoDB-backed per-tenant quotas; envelope encryption with KMS; IAM/resource policies plus VPC endpoints; per-tenant partitions for",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical design of end-to-end data ingestion with common AWS DVA primitives, plus data quality, deduplication, and governance. It also probes testing realism (LocalStack) and cost awareness.\n\n## Key Concepts\n- Kinesis Data Streams scaling for ingestion\n- Idempotent processing with DynamoDB dedupe by deviceId+ts\n- Basic data-quality scoring and DLQ routing\n- Parquet storage in S3 with region/date partitioning\n- Data lineage manifest and 24h backfill plan\n- Observability via CloudWatch; LocalStack testing plan\n\n## Code Example\n```javascript\n// Pseudo Lambda handler sketch for validation and scoring\n```\n\n## Follow-up Questions\n- How would you handle schema evolution with optional fields?\n- What changes would you make to support multi-region backfill consistency?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-18",
        "questionId": "q-21418",
        "question": "Beginner AWS DVA: In a single-region setup, ingest 1k–2k telemetry events/min from 4 device types into a Kinesis Data Stream. Implement a Lambda-based validation that enforces required fields and numeric ranges, assigns a per-record severity score, and routes valid events to two S3 Parquet buckets partitioned by severity. Invalids go to a DLQ. Deduplicate via DynamoDB (deviceId+ts). Add CloudWatch dashboards and an alert on DLQ depth and consumer lag; outline a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a cross-region telemetry pipeline: ingest in Region A via Kinesis Data Streams, validate against Glue Schema Registry, deduplicate with DynamoDB on deviceId+ts, and store valid events to S3",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two regional Kinesis streams; Lambda maintains per-device rolling stats (mean and std) in DynamoDB for a 5-minute window and computes a z-score per event. If |z|>3, emit an alert to an SNS topic; writ",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a single-region pipeline: producers send to Kinesis, a Lambda validates required fields and numeric ranges, assigns a severity score (0-3), and routes to S3 Parquet buckets by severity. Invalid",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Validate each JSON record against a versioned schema stored in DynamoDB; compute a per-record risk score using recent historical data; route low-risk events to S3 in Parquet format (partitioned by region and date) and h…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nA practical, end-to-end data validation and routing exercise that stays beginner-friendly while testing data quality, deduping, and observability.\n\n## Key Concepts\n- Kinesis Data Streams and Lambda triggers\n- JSON validation, numeric ranges, and per-record scoring\n- Severity-based routing to S3 Parquet\n- DynamoDB for deduplication (deviceId+ts)\n- DLQ handling and CloudWatch alerts\n- LocalStack test strategy\n\n## Code Example\n```javascript\nexports.handler = async (event) => {\n  const rec = JSON.parse(Buffer.from(event.Records[0].kinesis.data, 'base64').toString());\n  if (!rec.deviceId || !rec.ts) throw new Error('Missing fields');\n  if (typeof rec.temp !== 'number' || typeof rec.humidity !== 'number') throw new Error('Bad types');\n  let sev = 0;\n  if (rec.temp > 80 || rec.humidity > 90) sev = 2; else sev = 0;\n  // placeholder: write to DynamoDB for dedupe, route to S3 parquet in two prefixes by severity\n  return { status: 'ok', severity: sev };\n};\n```\n\n## Follow-up Questions\n- How would you test idempotency and dedupe in DynamoDB?\n- How would you simulate and monitor lag and DLQ depth in CloudWatch during a spike?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-19",
        "questionId": "q-30114",
        "question": "In a single AWS region, ingest 1k–3k JSON telemetry events/min from IoT devices into a Kinesis Data Stream. Validate per-record fields, route invalid records to an SQS DLQ, and write valid records to S3 Parquet with hourly partitioning. Add a lightweight analytics path: use Kinesis Data Analytics to compute per-device 5-minute rolling temperature average and persist to DynamoDB. Include X-Ray tracing across all components, and a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design uses a primary Kinesis Data Stream for ingestion, a Lambda function for per-record validation, and a dedicated valid-stream for validated records. Invalid records are routed to an SQS dead-letter queue. Kinesis D…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Glue Schema Registry for versioned schemas, write to Iceberg tables on S3 to support schema evolution and upserts with a deterministic key, implement idempotent writes via a normalized primary key",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a dual-path ingest: high-priority data goes to a dedicated Kinesis stream with low-latency Lambda processing; low-priority data is funneled to a separate stream with probabilistic downsampli",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a shared Kinesis stream with tenantId in the partition key and a DynamoDB-backed quota store to enforce per-tenant limits. Use Lambda/Fargate to enrich and write Parquet to S3; implement idemp",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to combine streaming validation, dual sinks (S3 and DynamoDB), and a simple streaming analytics path. It adds a new analytics angle (rolling average) with per-record validation and a dead-letter queue, plus distributed tracing and local testing capabilities.\n\n## Key Concepts\n- Kinesis Data Streams and Firehose for real-time data ingestion and delivery\n- Lambda-based validation with SQS DLQ routing for error handling\n- S3 Parquet format with hourly time-based partitioning\n- Kinesis Data Analytics for windowed stream aggregations\n- DynamoDB for persisting computed metrics\n- AWS X-Ray for end-to-end distributed tracing\n- LocalStack for local integration testing\n\n## Code Example\n```javascript\n// Lambda validator with DLQ routing (pseudo-code)\nexports.handler = async (event) => {\n  const dlqPromises = [];\n  const validRecords = [];\n  \n  for (const record of event.records) {\n    const payload = JSON.parse(Buffer.from(record.data, 'base64').toString());\n    \n    const validation = validateTelemetry(payload);\n    if (!validation.valid) {\n      dlqPromises.push(sendToDLQ({ payload, error: validation.error }));\n    } else {\n      validRecords.push(record);\n    }\n  }\n  \n  await Promise.all(dlqPromises);\n  return { records: validRecords.map(r => ({ recordId: r.recordId, result: 'Ok' })) };\n};\n\nfunction validateTelemetry(payload) {\n  const required = ['deviceId', 'temperature', 'timestamp'];\n  const missing = required.filter(f => !payload[f]);\n  if (missing.length > 0) return { valid: false, error: `Missing fields: ${missing.join(', ')}` };\n  if (typeof payload.temperature !== 'number' || payload.temperature < -50 || payload.temperature > 150) {\n    return { valid: false, error: 'Temperature out of range' };\n  }\n  return { valid: true };\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-20",
        "questionId": "q-22450",
        "question": "Beginner AWS DVA task: design a one-region streaming pipeline that ingests 1k–2k JSON telemetry events/min from two device families into separate Kinesis Data Streams, then a single adapter Lambda that normalizes to a unified schema (deviceId, ts, metric, tenantId, source, correlationId). Validate both schemas, dedupe by deviceId+ts, write to S3 Parquet by tenant/date, and log per-tenant audit in DynamoDB. If lag exceeds threshold, route overflow to a SQS DLQ. Include X-Ray tracing and a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Solution Architecture: A producer Lambda ingests JSON telemetry into a Kinesis Data Stream. A consumer Lambda validates the schema (required fields: deviceId, timestamp), redacts PII (emails, phones, credit card numbers…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design uses two KDS streams feeding a single adapter Lambda that normalizes to a common schema. Validate schemas, dedupe by deviceId+ts in DynamoDB, and write valid events to S3 Parquet by tenant/date",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use tenant-scoped partition keys in Kinesis and DynamoDB dedup (tenant+device+ts) to enable idempotent writes. Ingest with Lambda/Fargate, validate, and push valid data to regional S3 Parquet; catalog",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant prefixes in S3 and dedicated Kinesis streams per region; implement idempotence with a DynamoDB dedupe table keyed by tenantId+recordId+ts and store only once; emit OpenTelemetry traces",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe scenario tests end-to-end streaming wiring, cross-schema normalization, deduplication, per-tenant isolation, and basic backpressure handling—core DVA skills for real-world ingestion.\n\n## Key Concepts\n- Kinesis Data Streams symbiosis with a single adapter Lambda\n- Schema normalization and JSON validation\n- Deduplication with DynamoDB (deviceId+ts)\n- Parquet writes to S3 with tenant/date partitions\n- Simple backpressure to SQS DLQ when lag grows\n- Observability with AWS X-Ray; LocalStack testing\n\n## Code Example\n```javascript\n// Adapter Lambda sketch: validate input, map to common schema, upsert dedupe item, write to S3 Parquet\n```\n\n## Follow-up Questions\n- How would you implement idempotent writes in DynamoDB to handle retries?\n- How would you simulate a backpressure scenario in LocalStack and verify DLQ routing?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-21",
        "questionId": "q-7698",
        "question": "Design a multi-region, multi-tenant telemetry observability stack: each tenant publishes OpenTelemetry traces and metrics to Kinesis Data Streams, with tenant_id in the partition key. Lambda/Fargate pipelines normalize events, store raw in S3 per-tenant prefixes, and catalog in Glue. Use DynamoDB for tenant config and retention, and Athena for queries. Include per-tenant sampling, cross-region aggregation, and idempotent writes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use tenantId as the Kinesis partition key and a DynamoDB-backed sequence tracker to ensure idempotent writes across regions; emit to S3 with tenant/date partitions; Glue catalog and Athena queries; la",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route events by tenantId into region-scoped Kinesis streams; redact PII in a Lambda preprocessor; deduplicate via DynamoDB (tenantId+sequenceId) and write to S3 Parquet partitioned by tenant/date; imp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant regional routing by tenantId, use a shard per tenant in Kinesis Data Streams, deduplicate with a composite key tenantId+deviceId+ts stored in DynamoDB, enrich in Lambda, write Par",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Tenants publish OpenTelemetry traces/metrics to Kinesis with tenant_id as the partition key. Lambdas/Fargates normalize events, store raw data in per-tenant S3 prefixes, and update Glue catalogs. Dyna",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n Assesses ability to design cross-region, multi-tenant telemetry with OpenTelemetry and AWS DVA primitives, balancing cost, isolation, and queryability.\n\n## Key Concepts\n - OpenTelemetry integration across regions\n - Tenant isolation and per-tenant retention\n - Glue cataloging and Athena-driven analytics\n - Idempotent writes and sampling at ingest\n\n## Code Example\n ```javascript\n// Pseudo-config: tenant-scoped shard and OpenTelemetry exporter\nconst tenantId = getTenantId();\nconst streamName = `telemetry-${tenantId}`;\n```\n\n## Follow-up Questions\n - How would you enforce per-tenant quotas and handle burst traffic?\n - How would you evolve schemas for new trace attributes without downtime?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-22",
        "questionId": "q-12035",
        "question": "Beginner AWS DVA task: Two regions emit ~3k JSON telemetry lines per minute. Design a minimal, region-isolated pipeline using AWS DVA primitives that validates against Glue Schema Registry (supporting evolution with optional fields), redacts PII, deduplicates by deviceId+ts with DynamoDB, and writes valid events to regional S3 Parquet partitioned by region/date. Route invalids to a DLQ; plan a 7-day backfill and basic observability. Include testing strategy with LocalStack?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a region-scoped, per-tenant throttling design: a pre-ingest Lambda consults DynamoDB to check the tenant’s 1-minute window usage; if within limit, forward to regional KDS and atomically increm",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use tenantId partitioning on Kinesis; dedupe with DynamoDB (deviceId+ts). Ingest budget checks in Lambda: atomically decrement budget; throttle/drop if insufficient. Write to regional S3 with Parquet",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest with Kinesis partitionKey=tenantId; store per-tenant offsets in DynamoDB. Replicate only high-priority tenants to the second region via a fan-out (EventBridge to cross-region KDS/Firehose). Pro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use two regional KDS streams; Lambda validates against Glue Schema Registry (evolving schema with optional fields), redacts PII, and outputs to regional S3 Parquet (partition by region/date). Deduplic",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nRationale: tests ability to design region-isolated pipelines with schema registry, PII redaction, dedupe, backfill, and observability.\n\n## Key Concepts\n- Glue Schema Registry for runtime validation and evolution\n- PII redaction in streaming\n- Deduplication strategy with deviceId+ts\n- Region isolation and 7-day backfill\n- Observability and cost considerations\n\n## Code Example\n```javascript\n// Pseudocode outline for Lambda handler\nfunction handle(record){/* validate, redact, publish or DLQ */}\n```\n\n## Follow-up Questions\n- How would you test schema evolution with optional fields without breaking existing data?\n- How would you monitor DLQ backpressure and cost implications?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-23",
        "questionId": "q-11120",
        "question": "Design an adaptive enrichment pipeline for multi-tenant telemetry using AWS DVA primitives, where per-tenant QoS auto-switches enrichment between a fast Lambda path and a heavier Fargate path based on live rate and SLAs. Ensure exactly-once delivery to S3 Parquet, schema evolution for optional fields, 24h backfill, per-tenant quotas, canary rollout of logic, and OpenTelemetry traces across all components. Explain data model, partitioning, failure modes, and cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via per-region Kinesis streams; enrich with OpenTelemetry in a containerized Lambda/Fargate step; store raw and enriched events to S3 under tenant-region prefixes; deduplicate with a DynamoDB t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an adaptive enrichment pipeline: route per-tenant workload to a fast Lambda path or heavier Fargate path based on live rate and SLA. Use a DynamoDB routing table with feature flags to switch pa",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a Lake Formation-based architecture with per-tenant databases and tables mapped to /tenants/{tenantId}/ S3 locations, accessible through tenant-specific IAM roles assumed from a central analytics account. Enfo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use tenant_id and version in Kinesis partitions; stage a new processing version and deterministically replay from a versioned checkpoint stored in DynamoDB. Deduplicate by event_id, or composite (tena",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to reason about dynamic, production-grade routing and cost controls in a multi-tenant telemetry pipeline using AWS DVA. It also probes understanding of exactly-once guarantees, schema evolution, and end-to-end observability.\n\n## Key Concepts\n- Adaptive routing: per-tenant path selection between Lambda and Fargate\n- Exactly-once delivery and idempotent writes\n- Per-tenant quotas and backpressure\n- Schema evolution for optional fields with Glue/Athena\n- Canary rollout and rollback with feature flags\n- OpenTelemetry tracing across all components\n\n## Code Example\n```javascript\n// Pseudo routing decision (per-tenant)\nfunction selectPath(tenantId, rate, sla) {\n  const cfg = routingTable[tenantId];\n  if (!cfg) return 'lambda';\n  if (rate > cfg.threshold || sla < 99.9) return cfg.enrichPath; // 'fargate' or 'lambda'\n  return 'lambda';\n}\n```\n\n## Follow-up Questions\n- How would you model backfill consistency if a tenant skips data for a day?\n- What metrics and alarms ensure SLA adherence under burst traffic?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-24",
        "questionId": "q-26185",
        "question": "Design a cross-region telemetry pipeline that ingests 30k JSON events/min from devices across three regions, ensuring PII masking in flight and at rest with per-record privacy policies. Store masked events to S3 Parquet partitioned by region/date and either redact or securely vault unmasked data. Use Kinesis Data Streams, Lambda/Fargate, DynamoDB, Glue/Athena, and a DLQ; include tracing, a cost model, and LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an AWS data pipeline in a single region that ingests 5,000 JSON events per minute, each containing customerId and deviceId. Implement strict multi-tenant isolation using per-tenant S3 prefixes (e.g., s3://bucket/…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Glue Streaming or Spark on Glue to read from Kinesis, validate against Glue Schema Registry, and write per-tenant Parquet with Hudi for upserts. Maintain a dedup table in DynamoDB keyed by tenant+",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a cross-region telemetry pipeline ingesting 30k JSON events/min from devices across three regions, enforcing PII masking in flight and at rest with per-record privacy policies. Route masked dat",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a fixed 60-second watermark window stored in a DynamoDB table keyed by deviceId with a lastSeen timestamp. A validator Lambda attaches a qualityScore and routes records with missing required fields to the DLQ. For l…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nA cross-region privacy-preserving telemetry pipeline tests masking policies, region-aware storage, and end-to-end tracing, plus governance and cost.\n\n## Key Concepts\n- PII masking in streaming\n- Per-record privacy policies stored in DynamoDB\n- Region-based Parquet partitioning in S3\n- Exactly-once semantics and DLQ in Kinesis/Lambda\n- Observability with AWS X-Ray or OpenTelemetry\n- LocalStack testing strategy\n\n## Code Example\n```javascript\nfunction maskPII(rec){\n  const maskedPhone = rec.phone ? rec.phone.replace(/\\\\d(?=\\\\d{4})/g, '*') : null;\n  const maskedEmail = rec.email ? rec.email.replace(/[^@]+(?=@)/, '****') : null;\n  return { ...rec, phone: maskedPhone, email: maskedEmail };\n}\n```\n\n## Follow-up Questions\n- How would you ensure idempotent masking and policy updates across regions?\n- What testing plan would you implement to validate privacy rules with late-arriving data in LocalStack?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-23T03:02:32.840Z",
    "version": 1
  }
]