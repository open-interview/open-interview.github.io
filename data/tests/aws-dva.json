[
  {
    "id": "test-aws-dva",
    "channelId": "aws-dva",
    "channelName": "AWS DVA",
    "title": "AWS DVA Knowledge Test",
    "description": "Test your understanding of AWS DVA with 25 questions.",
    "questions": [
      {
        "id": "aws-dva-q-0",
        "questionId": "q-14498",
        "question": "Intermediate: Design a multi-region telemetry ingestion path that enforces per-tenant data masking policies in real time. Each tenant provides a masking schema for PII fields; detail how to store these policies, apply them in streaming, and persist masked data to S3 Parquet with per-tenant/date partitions. Include policy storage, KMS key management, observability, and how you'd test with LocalStack. What would your concrete architecture look like and how would you validate latency and governance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose per-region Kinesis streams with Glue Schema Registry for validation and evolution (compatibility modes). Compute a per-record score (completeness, conformance, freshness) in a Lambda, route va",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a multi-region telemetry ingestion path that enforces per-tenant masking policies in real time. Store tenant masking schemas in DynamoDB encrypted by KMS; stream events via regional Kinesis Dat",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use regional Kinesis streams per tenant, TLS with a per-tenant CMK in KMS; dedupe with a DynamoDB table keyed by tenantId+deviceId+ts to achieve sink-time exactly-once; enrich and serialize to S3 Parq",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a producer Lambda that publishes telemetry events to a Kinesis Data Stream. Create a data-masking Lambda that redacts PII fields (userId, email, phone, ip) from the payload before a Parquet writer writes the d…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a real-time masking pipeline with per-tenant policies, security controls, and observability. It also probes latency implications and how to validate regulatory compliance via testing.\n\n## Key Concepts\n\n- Real-time masking; tenant-scoped masking policies in DynamoDB; KMS key management\n- Streaming path: Kinesis Data Streams, Lambda/Fargate\n- Sinks: S3 Parquet; Glue/Athena for queries\n- Observability: OpenTelemetry traces; DLQ for bad records\n\n## Code Example\n\n```javascript\nfunction mask(record, policy){\n  // apply redact and hash rules per tenant\n}\n```\n\n## Follow-up Questions\n\n- How would you validate policy updates without downtime?\n- How would you detect and remediate policy drift across tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-1",
        "questionId": "q-6389",
        "question": "Design a cross-region telemetry ingestion and anomaly-detection pipeline for millions of IoT devices across tenants. Ingest via two Kinesis Data Streams (regionA/regionB); feature extraction in Fargate/Lambda; score with SageMaker; persist raw and scored data to S3 as Parquet partitioned by region/tenant/date; deduplicate with DynamoDB on deviceId+ts; catalog with Glue; query with Athena; isolate tenants with Lake Formation; enable 24h cross-region replay from S3; discuss trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route events by tenantId into region-scoped Kinesis streams; redact PII in a Lambda preprocessor; deduplicate via DynamoDB (tenantId+sequenceId) and write to S3 Parquet partitioned by tenant/date; imp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed: per-tenant Kinesis streams feed a Lambda validator that checks types and required fields against Glue Schema Registry; valid events deduplicate with a DynamoDB idempotency table keyed by dev",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a cross-region telemetry ingestion and anomaly-detection pipeline for millions of IoT devices across tenants. Ingest via two Kinesis Data Streams (regionA/regionB); feature extraction in Fargate/Lambda; score wit…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Central per-tenant schema registry (AVRO/JSON with optional fields) backed by Glue Catalog and DynamoDB; data written to S3 Parquet partitioned by tenant/date/region; Kinesis Data Streams feed Lambda/",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end, multi-region data pipelines with real-time scoring and strict tenant isolation.\n\n## Key Concepts\n- Cross-region streaming\n- Idempotence and deduplication\n- Lake Formation per-tenant isolation\n- SageMaker integration for real-time scoring\n- Data replay and backfill within 24 hours\n\n## Code Example\n```javascript\n// Pseudo wiring for components\n```\n\n## Follow-up Questions\n- How would you test replay correctness at scale?\n- Which metrics would you surface for latency, cost, and accuracy?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-2",
        "questionId": "q-7404",
        "question": "500 devices emit JSON telemetry to a regional Kinesis Data Stream. Build a beginner-friendly pipeline that validates against a versioned JSON schema (supporting optional fields), computes a per-record risk score from recent history, routes low-risk to S3 Parquet (partitioned by region/date) and high-risk to a second Kinesis stream for real-time analytics, and enable a 3-day backfill with basic observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use tenant-scoped partitioning by tenant_id in Kinesis; enforce exact-once with a DynamoDB offset store keyed by (tenant_id, producer_id) and a monotonically increasing sequence; deduplicate using a c",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Validate each JSON record against a versioned schema stored in DynamoDB; compute a per-record risk score using recent historical data; route low-risk events to S3 in Parquet format (partitioned by region and date) and h…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Deploy a Lambda transformer function that consumes events from the Kinesis Data Stream. For each event, inspect the version field and upgrade older schemas to vLatest using a version migration map. Validate the upgraded…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant isolation using Lake Formation LF-tags with policy-based grants, expose only non-sensitive columns through masked views, and enforce row-level security filters in Athena queries. Leverage CloudTrail…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design a practical, beginner-friendly DVA pipeline that handles evolving schemas, implements per-record scoring, and provides dual output paths. It also assesses understanding of backfill capabilities and basic observability requirements.\n\n## Key Concepts\n- Versioned schema validation using DynamoDB\n- Lightweight per-record risk scoring from recent historical data\n- Conditional routing between S3 Parquet storage and real-time Kinesis processing\n- Backfill window management and observability implementation\n\n## Code Example\n```javascript\n// Pseudo-logic sketch for Lambda handler\n```\n\n## Follow-up Questions\n- How would you scale the risk scoring to thousands of concurrent records?\n- What strategies would you implement for schema evolution and backward compatibility?\n- How would you enhance observability for production monitoring?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-3",
        "questionId": "q-8970",
        "question": "Design a streaming telemetry data quality gate for a multi-tenant fleet across 8 regions. Ingested events must pass per-tenant schema version checks; late-arriving schema updates default safely. Corrupted events go to a quarantine DLQ; valid events feed a main stream and partitioned Parquet in S3. Use AWS DVA primitives: Kinesis, Lambda/Fargate, S3, Glue, Athena, DynamoDB, OpenTelemetry. Explain partitioning, idempotency, backfills, monitoring, and failure plans, plus trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a data quality gate at ingestion: valid events feed the main Kinesis stream and partitioned Parquet in S3; corrupted ones go to a quarantined DLQ with Object Lock. Enforce per-tenant schema",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest via Kinesis Data Streams, validate payloads against Glue Schema Registry, add ingestionTs in a Lambda, upsert latest device state in DynamoDB (tenantId+deviceId), store valid events to S3 Parqu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition Kinesis by tenantId, replicate across regions with a Lambda cross-region replicator writing to the destination region’s KDS. Deduplicate in DynamoDB using a composite key (tenantId,eventId)",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Route each event by tenantId to a per-tenant consumer path. In latency-sensitivity mode, batch events into 1s windows through a micro-batching sink; otherwise stream directly to Lambda/Fargate. Ensure",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design robust, scalable data quality controls in a multi-tenant streaming pipeline, including schema evolution, late data handling, and quarantine workflows.\n\n## Key Concepts\n\n- Data quality gates and quarantines\n- Per-tenant schema versions and defaults\n- Idempotent writes and deduplication\n- Backfill strategies and replay\n- Observability with OpenTelemetry and CloudWatch\n\n## Code Example\n\n```javascript\n// validate per-tenant event against version map\nfunction validate(event, versionMap){\n  const v = event._schemaVer ?? 1;\n  const required = versionMap[v] || [];\n  return required.every(f => f in event);\n}\n```\n\n## Follow-up Questions\n\n- How would you test quarantine and backfill reliability?\n- What metrics and alarms would you configure to detect drift or failures?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-4",
        "questionId": "q-22800",
        "question": "Design a cross-region telemetry pipeline for a multi-tenant platform where each tenant has a dynamic enrichment policy stored centrally (enable geolocation lookups, apply per-tenant rate limiting, or suppress PII). Ingest from per-region Kinesis streams, apply the policy during ingestion in Lambda, and store enriched events as Parquet in S3. Ensure tenant isolation, exactly-once processing, late data handling, auditing, and OpenTelemetry traces?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adapter path: MQTT and HTTPS feed a single Kinesis stream. Normalize to {deviceId, ts, metric, correlationId}. Validate required fields and numeric ranges, dedupe by deviceId+ts with DynamoDB, and sto",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement policy-driven PII masking at ingestion in a cross-region AWS DVA telemetry pipeline. EU tenant data must be redacted per policy while immutable audit trails remain intact. Use Kinesis Data S",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed approach: shard-per-tenant on Kinesis; per-tenant quotas tracked in DynamoDB; Lambda deduplicates on (tenantId, eventId); cross-region replication via mirrored streams and a fan-out Lambda to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant policy table in DynamoDB with TTL and a small in‑memory cache inside the ingestion Lambda. Fetch policy once per tenant, cache it, and apply rules (geolocation on/off, PII masking, pe",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses policy-driven enrichment, tenant isolation, and end‑to‑end observability in a cross‑region setup using AWS DVA primitives.\n\n## Key Concepts\n\n- Per-tenant governance and dynamic policy\n- Ingestion-time gating and feature flags\n- Exactly-once processing and deduplication\n- Late data handling and auditing\n- End-to-end tracing with OpenTelemetry\n\n## Code Example\n\n```javascript\n// Pseudo: fetchPolicy(tenantId) -> cache with TTL; if policy.geolocationEnabled => enrichGeo();\n// idempotentWrite(tenantId, deviceId, ts, payload);\n```\n\n## Follow-up Questions\n\n- How would you test policy evolution without impacting tenants?\n- How would you audit masking decisions and ensure data residency?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-5",
        "questionId": "q-21692",
        "question": "Design a cross-region, multi-tenant telemetry pipeline with per-tenant data residency: some tenants stay in their home region; others are replicated for analytics. Describe the end-to-end flow using Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. Include tenant-aware routing, per-tenant retention, exactly-once processing, auditability, testing, and cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement per-tenant shards and DynamoDB-backed dedupe, with region-local raw and replicated derived data, using Kinesis shard splitting and Lambda + Fargate for processing; OpenTelemetry traces p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a cross-region, multi-tenant governance flow with per-tenant holds, immutable raw data, and auditable exports. Use Kinesis for ingestion, Lambda/Fargate for processing, DynamoDB for per-tena",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kinesis Data Streams (2-3 shards) to ingest 10k events/min. A Lambda consumer validates records against Glue Schema Registry using the supplied schemaVersion. Valid records are written to S3 Parqu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Edge routing at ingestion: DynamoDB stores per-tenant residency and replication rules; a policy check in Lambda validates event.region against policy and writes to the tenant’s regional Kinesis stream",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a compliant, scalable multi-region telemetry pipeline with per-tenant governance, residency rules, and cost controls using AWS DVA primitives and OpenTelemetry.\n\n## Key Concepts\n\n- Tenant policy management in DynamoDB\n- Residency-aware routing to region streams\n- Exactly-once processing and idempotent writes\n- End-to-end tracing with OpenTelemetry across components\n- Data lifecycle: per-tenant retention, purge strategies, and cost controls\n- Testing: residency canaries, audit validation, DR scenarios\n\n## Code Example\n\n```typescript\n// Pseudo routing function\nfunction routeEvent(evt: TelemetryEvent){\n  const policy = getPolicy(evt.tenantId);\n  if (policy.residency === 'home' && evt.region !== policy.homeRegion){\n    throw new Error('Residency violation');\n  }\n  const targetStream = policy.replicate ? `${policy.homeRegion}-stream` : `${policy.homeRegion}-stream`;\n  return writeToStream(targetStream, evt);\n}\n```\n\n## Follow-up Questions\n\n- How would you test residency rules in CI/CD?\n- How would you monitor cross-region data drift and retry semantics?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-6",
        "questionId": "q-10194",
        "question": "Design a per-tenant, low-latency anomaly-detection pipeline for a multi-region telemetry system that must operate within ~150 ms end-to-end under peak load. Use AWS DVA primitives (Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB) and OpenTelemetry. Include schema evolution with optional fields, a 24-hour backfill for missed windows, per-tenant isolation, and cost/latency trade-offs. What would you implement and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an S3-triggered Lambda to validate JSONs against a JSON schema. On success, write a Parquet file to s3://data-logs/processed/YYYY/MM/DD/ with a deterministic key (hash of content) for idempotency.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Attach a durable lineageId to every event; emit a parallel lineage event to a dedicated Kinesis stream and store provenance in a DynamoDB table keyed by lineageId and region. Propagate lineageId throu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant, low-latency path: Kinesis Data Streams with tenant_id as the partition key and Enhanced Fan-Out; real-time inference in Lambda/Fargate; emit anomalies to DynamoDB and store results in S3 P",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design an adaptive enrichment pipeline: route per-tenant workload to a fast Lambda path or heavier Fargate path based on live rate and SLA. Use a DynamoDB routing table with feature flags to switch pa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design a scalable, low-latency streaming solution with multi-tenant isolation, schema evolution, and recoverability, using AWS DVA primitives and observability.\n\n## Key Concepts\n\n- Per-tenant sharding on Kinesis and Enhanced Fan-Out for latency\n- Real-time inference in Lambda or Fargate\n- Idempotent writes via stable dedupe keys\n- Schema evolution with optional fields and backward compatibility\n- 24h backfill via replay path\n- Observability with OpenTelemetry across services\n- DLQ and backpressure handling; cost vs latency trade-offs\n\n## Code Example\n\n```javascript\n// Deduplicate by tenant+ts\nfunction dedupeKey(evt){ return evt.tenant_id+\":\"+evt.ts; }\n```\n\n## Follow-up Questions\n\n- How would you validate end-to-end latency under burst traffic?\n- How would you test replay correctness and idempotence across regions?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-7",
        "questionId": "q-28748",
        "question": "Design a cross-region telemetry pipeline to ingest 2M events/min from 100k devices across three regions. Use Kinesis Data Streams with tenant-scoped partition keys (tenantId+deviceId) and dynamic shard management to avoid hot shards. Guarantee exactly-once processing with per-record sequence tracking in DynamoDB and idempotent sinks to S3/Parquet. Add OpenTelemetry traces, Glue catalog, and Athena queries, plus a burst-aware load test?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Single Kinesis stream partitioned by tenantId; Lambda masks PII and validates against per-tenant schemaVersion stored in DynamoDB. Valid events route to S3 Parquet under tenant/year=ver; Glue Spark jo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition Kinesis streams by tenant ID and region to ensure data locality and tenant isolation. Implement deduplication in Lambda using a composite key of tenant ID and message ID, storing processed IDs in DynamoDB with…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition keys: tenantId+deviceId; KDS shards autoscale to avoid hotspots; guarantee exactly-once with a per-record sequence token in DynamoDB and idempotent sinks to S3/Parquet; Lambda/Fargate for pr",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Regional Kinesis streams per tenant feed a Lambda/Fargate pipeline that normalizes events, redacts PII, and writes to S3 Iceberg/Parquet. Glue Data Catalog with Lake Formation controls tenant isolatio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a high-scale, multi-region telemetry pipeline with robust shard management, per-tenant isolation, and strict processing guarantees. It also probes observability and data discovery aspects using OpenTelemetry, Glue, and Athena.\n\n## Key Concepts\n\n- Kinesis shard strategy and dynamic rebalancing\n- Tenant-scoped partition keys to prevent hot shards\n- Exactly-once processing using per-record sequence state in DynamoDB\n- Idempotent sinks to S3/Parquet\n- Cross-region data flow and latency considerations\n- Observability with OpenTelemetry\n- Data catalog and ad-hoc analytics with Glue/Athena\n- Burst-load testing and SLA validation\n\n## Code Example\n\n```python\n# Pseudo-idempotent sink sketch\ndef sink_event(event, db, s3, bucket):\n    key = f\"{event['tenantId']}/{event['deviceId']}/{event['ts']}\"\n    if db.get_item(Key={'pk': key}):\n        return\n    db.put_item(Item={'pk': key})\n    s3.put_object(Bucket=bucket, Key=f\"events/{event['ts']}.json\", Body=bytes(str(event), 'utf-8'))\n```\n\n## Follow-up Questions\n\n- How would you validate shard rebalancing and latency under burst bursts across regions?\n- What metrics and alarms would you set to detect hotspotting, skew, and SLA breaches, and how would you mitigate them in flight?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-8",
        "questionId": "q-24371",
        "question": "Edge-first telemetry: edge gateways buffer offline and batch-send to AWS IoT Core, which fans out to two regional Kinesis Data Streams. Design end-to-end processing with per-record validation, deduplication by deviceId+ts, cross-region replication, and a 7-day backfill. Include tracing from edge to S3, schema evolution handling, and cost-aware storage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition by tenantId in Kinesis; idempotent writes via DynamoDB dedupe (tenantId+deviceId+ts); mask fields per-tenant from a policy table during Lambda enrichment; store valid events as Parquet in S3",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design uses two-region Kinesis Data Streams with fan-out to Lambda/Fargate. A masking step uses KMS envelope encryption to redact PII fields before writing to a masked S3 Parquet dataset; raw events g",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Edge-first telemetry: edge gateways buffer offline and batch-send to AWS IoT Core, which fans out to two regional Kinesis Data Streams. A Lambda consumer validates against Glue Schema Registry, comput",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-tenant privacy controls using a DynamoDB policy table keyed by tenantId. Ingest telemetry through regional Kinesis Data Streams, where Lambda functions apply tenant-specific masking policies—PII redaction,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical edge-to-cloud ingestion, cross-region dataflow, and end-to-end observability for real-world telemetry workloads.\n\n## Key Concepts\n- Edge buffering, MQTT/TLS\n- AWS IoT Core and multi-region Kinesis Data Streams\n- Schema validation with Glue Schema Registry\n- Idempotent dedupe with DynamoDB\n- S3 Parquet storage with lifecycle\n\n## Code Example\n```javascript\n// Pseudo: Lambda dedupe flow\nasync function process(record){\n  const exists = await checkDedupe(record.deviceId, record.ts);\n  if(exists) return; // idempotent\n  await store(record);\n}\n```\n\n## Follow-up Questions\n- How would you ensure exactly-once semantics across regions? \n- How would you simulate and test edge offline buffering and reprojection to cloud?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-9",
        "questionId": "q-20630",
        "question": "Design a cross-region AWS DVA telemetry pipeline that provides per-tenant data provenance tracking: immutable lineage from deviceId through each processing step, including transformations, with per-tenant isolation and a replayable audit trail. Use Kinesis Data Streams, Lambda/Fargate, S3, Glue, DynamoDB, Athena, and a provenance store. Explain how you model the graph, ensure scalability to 1M events/min, and support backfill/replay while preserving security and cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Attach a provenance graph to each event by adding a provenanceId that links to a per-tenant graph stored in DynamoDB; each processing step writes a Edge {from, to, step, ts, tenant, hash}. Cross-regio",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a fidelity gate at ingestion: tenants have a fidelity policy stored in DynamoDB; a Lambda pre-processor applies masking or redaction before producing to Kinesis. Ensure exactly-once with ide",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant data tiers in S3 with lifecycle rules keyed by tenant_id and data_age; route streaming via Kinesis to Lambda that writes to tenant-specific S3 prefixes; use Glue to crawl and partition metadata; cre…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Kinesis Data Streams with auto-scaling shards to ingest 10k JSON/min; a Lambda validates and computes a data-quality score (0-1) per record, deduplicates via DynamoDB (deviceId+ts), writes valid e",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical data lineage across a multi-region, multi-tenant telemetry pipeline, a core reliability and security concern.\n\n## Key Concepts\n- Per-tenant provenance graph in DynamoDB; Edge records capture step lineage\n- Idempotent producers and exactly-once-ish processing across regions\n- Backfill/replay of provenance with versioned snapshots; query with Glue/Athena\n\n## Code Example\n```javascript\n// Example: write provenance edge\nconst putEdge = async (child, parent, step, tenant, ts)=>{\n  await dynamo.putItem({\n    TableName: 'ProvenanceEdges',\n    Item: { Child:{S:child}, Parent:{S:parent}, Step:{S:step}, Tenant:{S:tenant}, TS:{N:String(ts)} }\n  });\n};\n```\n\n## Follow-up Questions\n- How would you test provenance integrity under partial failures?\n- How do you handle schema changes in provenance edges?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-10",
        "questionId": "q-5983",
        "question": "Design an advanced, multi-tenant real-time anomaly detection pipeline on AWS DVA. Tenants send events to a shared Kinesis Data Stream with per-tenant partition keys; a Fargate inference service computes per-event anomaly scores, versioned models, and writes results to DynamoDB and artifacts to S3 (per-tenant prefixes). Alerts are routed to per-tenant channels via SNS. Describe data schema, idempotence, backpressure, model/versioning, governance, and cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a regional KDS with tenant-scoped keys, per-tenant Lambda/Fargate workers, and Lake Formation isolation. Deduplicate with DynamoDB (tenant+eventId). Persist Parquet per tenant/date in S3; register",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant partition keys on a shared Kinesis Data Stream, deploy a Fargate inference service with versioned ML models, store anomaly scores in DynamoDB with tenant-specific GSIs, archive model artifacts to S3…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Regional Kinesis streams feed a Fargate scorer that emits per-tenant anomaly scores to tenant-scoped Parquet in S3. Use tenantId as the partition key, and (tenantId, deviceId, ts) as the dedupe key wi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant prefixes in S3 and dedicated Kinesis streams per region; implement idempotence with a DynamoDB dedupe table keyed by tenantId+recordId+ts and store only once; emit OpenTelemetry traces",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis realistic scenario evaluates the ability to design real-time, multi-tenant telemetry systems with proper isolation, versioned ML models, and comprehensive cost controls. The question probes deep understanding of data schema evolution, idempotence, backpressure handling, and governance across AWS DVA primitives.\n\n## Key Concepts\n\n- **Multi-tenant isolation** through partition keys and IAM policies\n- **Real-time inference** with Fargate container orchestration\n- **Model versioning** and drift detection strategies\n- **Data schema design** with backward compatibility\n- **Idempotence** for exactly-once processing guarantees\n- **Backpressure and retry** handling in streaming systems\n- **Cost controls** through quotas and auto-scaling policies\n\n## Code Example\n\n```javascript\n// Event schema with tenant isolation\nclass AnomalyEvent {\n  constructor(tenantId, eventId, timestamp, features) {\n    this.tenantId = tenantId;\n    this.eventId = eventId; // UUID for idempotence\n    this.timestamp = timestamp;\n    this.features = features;\n    this.partitionKey = `${tenantId}:${eventId.slice(-8)}`;\n  }\n}\n\n// Fargate inference service with model versioning\nclass InferenceService {\n  async processEvent(event) {\n    const model = await this.getModelVersion(event.tenantId);\n    const score = await model.predict(event.features);\n    \n    // Idempotent write to DynamoDB\n    await this.writeResult(event, score);\n    \n    // Alert routing if threshold exceeded\n    if (score > this.getThreshold(event.tenantId)) {\n      await this.routeAlert(event, score);\n    }\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you handle schema evolution for tenant-specific features?\n- What strategies would you implement for model A/B testing across tenants?\n- How do you ensure data consistency during model version rollbacks?\n- What monitoring and alerting would you implement for the inference pipeline?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-11",
        "questionId": "q-10618",
        "question": "Design a beginner AWS DVA pipeline for 1,400 devices/min sending JSON with a tenantId to a regional Kinesis Data Stream. Build a minimal, tenant-isolated workflow: validate JSON, deduplicate by tenantId+deviceId+ts (DynamoDB), write valid events to S3 Parquet partitioned by tenant/date/region, route invalid JSON to a DLQ, and add a simple per-record data quality score. Include a 7-day backfill plan and basic observability and cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Glue Schema Registry for versioned schemas, write to Iceberg tables on S3 to support schema evolution and upserts with a deterministic key, implement idempotent writes via a normalized primary key",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up a Kinesis Data Stream in the region, a Node.js Lambda to validate each JSON line, assign a data quality score, and check for required fields. Use a single DynamoDB table with composite keys ten",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement policy-driven PII masking at ingestion in a cross-region AWS DVA telemetry pipeline. EU tenant data must be redacted per policy while immutable audit trails remain intact. Use Kinesis Data S",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "This design routes per-tenant telemetry across two regions using Kinesis streams, keyed by tenantId-region-deviceId-ts. Raw events are persisted to S3 in Parquet format and cataloged via AWS Glue for Athena analytics. A…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical hands-on capabilities with tenant isolation, streaming basics, and simple data quality workflows in a beginner-friendly stack.\n\n## Key Concepts\n- Kinesis, Lambda, DynamoDB, S3 Parquet\n- Per-tenant partitioning and dedupe\n- DLQ and backfill strategies\n- Observability and cost controls\n\n## Code Example\n```javascript\n// Example Lambda skeleton handling validation, dedupe, and storage\nexports.handler = async (event) => {\n  // parse, validate\n  // compute quality score\n  // dedupe via DynamoDB conditional write\n  // write Parquet to S3 or DLQ\n}\n```\n\n## Follow-up Questions\n- How would you scale to spikes?\n- How would you handle schema evolution for optional fields?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-12",
        "questionId": "q-23931",
        "question": "Design a multi-tenant telemetry pipeline: 50 customers across 3 regions publish device events via MQTT into regional Kinesis Data Streams. Create end-to-end isolation so data never leaks between tenants, enforce per-tenant quotas, and support dynamic tenant onboarding. Include IAM/resource policies, KMS encryption, VPC endpoints, per-tenant partitioning, a burst-traffic guardrail, testing with LocalStack, and a deterministic rollback/replay plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use tenant_id and version in Kinesis partitions; stage a new processing version and deterministically replay from a versioned checkpoint stored in DynamoDB. Deduplicate by event_id, or composite (tena",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest via Kinesis Data Streams in each region; a masking layer (Lambda/Fargate) applies tenant-scoped redaction/tokenization before writing per-tenant Parquet to S3 and audit logs to DynamoDB. Use La",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy a Lambda transformer function that consumes events from the Kinesis Data Stream. For each event, inspect the version field and upgrade older schemas to vLatest using a version migration map. Validate the upgraded…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a concrete architecture: tenantId as the Kinesis shard key; DynamoDB-backed per-tenant quotas; envelope encryption with KMS; IAM/resource policies plus VPC endpoints; per-tenant partitions for",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-tenant isolation, cross-region data handling, and security controls in DVA workflows.\n\n## Key Concepts\n- Multi-tenant isolation\n- IAM/resource policies and VPC endpoints\n- KMS envelope encryption\n- Per-tenant quotas and shard strategy\n- LocalStack testing for leakage checks\n\n## Code Example\n```json\n{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [\n    {\n      \"Effect\": \"Allow\",\n      \"Action\": [\"kinesis:PutRecord\"],\n      \"Resource\": \"arn:aws:kinesis:REGION:ACCOUNT:stream/tenant-<tenantId>-stream\"\n    }\n  ]\n}\n```\n\n## Follow-up Questions\n- How would you onboard new tenants with zero downtime?\n- What failure modes affect isolation and how would you validate them?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-13",
        "questionId": "q-24792",
        "question": "Design an intermediate AWS DVA telemetry pipeline for a multi-tenant fleet management service. Devices publish JSON events via AWS IoT Core to a regional Kinesis Data Stream. Implement per-tenant data isolation by routing into separate streams or using tenant as a partition key. Validate against Glue Schema Registry, redact PII fields (driverName, VIN) in real time, compute a per-event risk score in a Lambda, and deduplicate by deviceId+ts using DynamoDB. Persist valid events to S3 Parquet partitioned by tenant/date, route invalid records to a DLQ, and maintain an immutable audit trail of redaction decisions in DynamoDB. Include observability, backpressure handling, and a 24h backfill plan. Include a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Routing: per-tenant shard in KDS or tenant-keyed stream; validate with Glue Schema Registry; redact PII fields in a pre-processing step; compute risk score in Lambda; deduplicate by deviceId+ts using",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Two AWS accounts: ingoing KDS in A, processing via Lambda/Fargate with per-tenant partition keys; dedupe by deviceId+ts for exactly-once; store raw plus enriched Parquet in region-scoped S3; Glue cata",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set up a single Kinesis Data Stream; a Lambda consumer with batch size 100 validates JSON against a schema (requires correlationId, deviceId, ts); masks user emails/phones in payload; computes a per-r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose per-tenant isolation via prefixes in S3 and a tenantId field; mask PII in a streaming transform (Lambda) before persistence; store raw and masked outputs in separate S3 paths; use Glue Schema",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical handling of multi-tenant isolation, data redaction, and end-to-end observability in a streaming pipeline.\n\n## Key Concepts\n- Per-tenant data routing and partitioning\n- Real-time redaction and auditability\n- Glue Schema Registry validation\n- Event deduplication with DynamoDB\n- Parquet storage and partitioning; DLQ routing\n- Observability and LocalStack test plan\n\n## Code Example\n```javascript\n// Pseudo-code for redaction and scoring step\nconst redact = (evt) => {\n  const redacted = {...evt};\n  if (redacted.driverName) redacted.driverName = 'REDACTED';\n  if (redacted.VIN) redacted.VIN = 'REDACTED';\n  return redacted;\n};\n```\n\n## Follow-up Questions\n- How would you validate schema evolution across versions?\n- How would you test end-to-end backfill latency under burst?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-14",
        "questionId": "q-2202",
        "question": "Design a real-time payments fraud detector handling 100k events/sec peak with Kinesis Data Streams. End-to-end latency <200 ms, exactly-once processing, and cross-region DR. Data schemas evolve with optional fields; late data allowed within 10 minutes backfill. Propose concrete AWS DVA architecture using KDS, Lambda/Fargate, S3, Glue, Athena, DynamoDB; explain idempotent writes, partitioning, backpressure, monitoring, and failure plans?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would deploy two region-specific Kinesis streams, a routing Lambda to fan out to a common processor and a separate firmwareUpgrade path. Deduplicate per region with DynamoDB using (deviceId, ts) as",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Cross-region telemetry with per-device governance: producers in region A push to Kinesis Data Streams; Lambda consults a policy store to classify PII vs non-PII and routes accordingly. PII lands in a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose region-local KDS streams per tenant, a dynamic Glue Schema Registry for validation, and a per-tenant dedupe store in DynamoDB. Ingested events route to S3 Parquet partitions by tenant/date, wi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Kinesis Data Stream with per-record keys and sequence numbers to enable idempotent writes; fan-out to Lambda for fast scoring and to Fargate for ML-based scoring. Maintain online state in Dynamo",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests design of real-time fraud pipelines at scale with strong reliability, schema evolution, late data handling, and DR.\n\n## Key Concepts\n- Exactly-once semantics with sequence numbers and dedup keys\n- Split real-time scoring (Lambda) and heavier ML (Fargate)\n- DynamoDB as online feature/state store; S3/Parquet for history\n- Glue catalog for evolving schema; Parquet lineage in Athena\n- Late-data backfill within 10 minutes; cross-region DR\n\n## Code Example\n```javascript\n// Pseudo: idempotent write to DynamoDB\ndynamo.put({TableName, Item, ConditionExpression: 'attribute_not_exists(transaction_id)'}, cb)\n```\n\n## Follow-up Questions\n- How would you validate end-to-end latency under burst traffic?\n- What failure modes exist and how would you mitigate them?",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-15",
        "questionId": "q-29666",
        "question": "Design a multi-tenant telemetry ingestion pipeline in AWS DVA where 50k events/min per tenant across three regions. Each tenant (A/B/C) must be isolated at the data plane: per-tenant Kinesis streams/shards, deduplicate by deviceId+ts within the tenant, validate with a dynamic schema per-tenant stored in Glue Schema Registry, enrich via per-tenant rules stored in DynamoDB, and write valid events to S3 Parquet under tenant/date prefixes; route invalids to DLQ. Plan for late-arriving data up to 24h and cross-region aggregation via Glue/Athena; implement per-tenant quotas, alerts, and observability with per-tenant CloudWatch metrics and encryption via KMS?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition by tenant in Kinesis shards and DynamoDB namespaces; use per-tenant KMS keys for envelope encryption and S3 prefixes per tenant. Maintain a per-tenant sequence counter to guarantee exactly-o",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a two-region, two-tenant pipeline: ingest 40k events/min via region-local Kinesis Streams; a Fargate processor partitions by tenant/date into S3 Parquet; keep per-tenant latest state in DynamoD",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant CMEK in AWS KMS and per-tenant S3 prefixes for raw/processed data. Ingest via Kinesis Data Streams and route through tenant-scoped Lambda/Fargate workers with IAM isolation. Employ enve",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a tenant-isolated ingestion pipeline handling 50k events/min per tenant across three regions. Each tenant (A/B/C) receives dedicated Kinesis streams with auto-scaling shards. Deduplication occurs via a tenant-sco…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates multi-tenant isolation design, per-tenant data governance, and cross-region analytics under realistic throughput and latency constraints. Strong answers demonstrate understanding of AWS data streaming services, schema evolution, and tenant billing/observability patterns.\n\n## Key Concepts\n- **Tenant isolation**: Per-tenant streams/shards ensure data plane separation\n- **Deduplication**: Composite key (tenant, deviceId, ts) prevents duplicate ingestion\n- **Schema governance**: Glue Schema Registry enables tenant-specific schema evolution with backward compatibility\n- **Enrichment**: DynamoDB-backed rule engine allows runtime configuration without redeployment\n- **Late-arriving data**: 24-hour retention window accommodates out-of-order events with timestamp-based ordering\n- **Cross-region analytics**: Glue/Athena federates partitioned data across regions for aggregation\n- **Observability**: Per-tenant CloudWatch metrics enable granular SLAs and chargeback reporting\n- **Security**: Tenant-specific KMS keys enforce encryption-at-rest with minimal blast radius\n\n## Code Example\n```javascript\n// Multi-tenant Kinesis event processor with per-tenant enrichment\nconst { KinesisStreamProcessor } = require('./processors');\nconst { SchemaValidator } = require('./validation');\nconst { EnrichmentEngine } = require('./enrichment');\n\nclass TenantPipeline {\n  constructor(tenantId, region) {\n    this.tenantId = tenantId;\n    this.streamName = `tenant-${tenantId}-ingest-${region}`;\n    this.schemaRegistry = new SchemaValidator(tenantId);\n    this.enricher = new EnrichmentEngine(tenantId);\n    this.dlqWriter = new DLQWriter(tenantId);\n  }\n\n  async processRecord(record) {\n    const { deviceId, ts, payload } = JSON.parse(record.Data);\n    const dedupKey = `${this.tenantId}#${deviceId}#${ts}`;\n\n    if (await this.deduplicate(dedupKey)) {\n      return { status: 'duplicate', key: dedupKey };\n    }\n\n    const schema = await this.schemaRegistry.getSchema(this.streamName);\n    const validation = schema.validate(payload);\n\n    if (!validation.valid) {\n      await this.dlqWriter.send({ record, errors: validation.errors });\n      return { status: 'invalid', errors: validation.errors };\n    }\n\n    const enriched = await this.enricher.apply(payload, {\n      tenantId: this.tenantId,\n      timestamp: ts,\n      region\n    });\n\n    return { status: 'valid', data: enriched };\n  }\n}\n\nmodule.exports = { TenantPipeline };\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-16",
        "questionId": "q-12905",
        "question": "Two-region SaaS analytics pipeline for multiple tenants. Some tenants require PII redaction at ingestion and per-tenant retention. Design an end-to-end AWS DVA flow that: (a) applies tenant-specific masking at ingress, (b) stores redacted events in regional S3 Parquet (region/date), (c) preserves raw events in a separate immutable bucket with Object Lock, (d) enforces per-tenant retention in Glue/Athena, (e) emits audit events to a DLQ, (f) supports replay for audits without exposing PII. Include trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Mask PII per-tenant at ingestion using a policy store (DynamoDB) and a masking function in a Lambda/Fargate consumer; route redacted events to regional S3 Parquet (region/date); keep raw events in a s",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a fidelity gate at ingestion: tenants have a fidelity policy stored in DynamoDB; a Lambda pre-processor applies masking or redaction before producing to Kinesis. Ensure exactly-once with ide",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant admission control using DynamoDB to throttle ingress traffic based on tenant-specific quotas. Map tenant tokens to Kinesis shards with dynamic reallocation to handle bursty workloads, and propagate…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Regional Kinesis streams feed a Lambda that (a) validates against a versioned schema, (b) computes a 0–1 quality score using required fields and ranges, (c) routes score <0.8 to a DLQ, (d) writes vali",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical knowledge of data masking, data residency, retention governance, and auditing in real-world AWS DVA pipelines. Requires integrating Kinesis/Data Streams, Lambda/Fargate, S3 with Object Lock, Glue cataloging, Athena queries, and a DLQ for auditability. Also evaluates replay strategies without exposing PII.\n\n## Key Concepts\n\n- Tenant-scoped masking policies and a centralized policy store (DynamoDB)\n- Ingress processing with Lambda/Fargate per-tenant logic\n- Dual-path storage: redacted data in regional S3 Parquet, raw data in immutable bucket\n- Data retention controls via Glue Catalog tables and Athena views\n- Audit visibility via Dead Letter Queue\n- Replay semantics using raw data with idempotent re-ingest\n\n## Code Example\n\n```javascript\nfunction maskPII(record, policy) {\n  // simplistic example: redact email and phone if policy requires\n  if (policy.redact.includes('email')) record.email = 'REDACTED@domain.com';\n  if (policy.redact.includes('phone')) record.phone = 'REDACTED';\n  return record;\n}\n```\n\n## Follow-up Questions\n\n- How would you test masking correctness and retention compliance across tenants?\n- What are the latency, storage, and cost trade-offs of maintaining raw + redacted copies and enabling replay?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-17",
        "questionId": "q-23879",
        "question": "Design a single-region AWS DVA telemetry pipeline that ingests 5k JSON events/min, each containing customerId and deviceId. Implement strict multi-tenant isolation: per-tenant S3 prefixes, per-tenant Glue data catalog entries, and IAM policies that prevent cross-tenant access. Ensure dedupe by customerId+deviceId+ts with DynamoDB, write valid records to S3 Parquet, route invalids to a per-tenant DLQ, and provide end-to-end tracing. Discuss testing strategy, including cross-tenant leakage tests and LocalStack plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an AWS data pipeline in a single region that ingests 5,000 JSON events per minute, each containing customerId and deviceId. Implement strict multi-tenant isolation using per-tenant S3 prefixes (e.g., s3://bucket/…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Kinesis Data Streams to ingest telemetry from thousands of devices. A Lambda function reads each tenant's correction policy from DynamoDB, applies deterministic corrections using a composite idempotency key (deviceI…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "An inter-region pipeline uses Kinesis Data Streams in each region; a per-record Lambda masks PII fields (phone, email, deviceId) and replaces with a stable surrogate. Glue Schema Registry enforces a m",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest data via Kinesis Data Streams in a single region, validate against a Glue Schema Registry (handling optional fields) in a Lambda function, mask PII such as emails and phone numbers before storage, deduplicate usi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your ability to design multi-tenant data pipelines with strong isolation guarantees, governance controls, and end-to-end observability—critical skills for building enterprise-grade data ingestion and processing solutions.\n\n## Key Concepts\n- **Tenant Isolation**: Per-tenant S3 prefixes, Glue Catalog databases and tables, and IAM policies scoped to individual tenant resources prevent cross-tenant data access.\n- **Deduplication**: Exactly-once processing semantics using DynamoDB with composite keys (customerId+deviceId+ts) ensures no duplicate records are written.\n- **Data Routing**: Valid records flow to S3 in Parquet format for analytics efficiency, while invalid records route to per-tenant DLQs for downstream investigation and reprocessing.\n- **Observability**: X-Ray provides end-to-end tracing to diagnose latency issues and monitor pipeline health across all components.\n\n## Testing Strategy\n- **Unit Testing**: Validate deduplication logic, schema validation rules, and data transformation functions in isolation.\n- **Integration Testing**: Verify end-to-end flow from Kinesis through Lambda/Glue to S3, including DLQ routing.\n- **Cross-Tenant Leakage Tests**: Deploy multiple simulated tenants and attempt cross-tenant access via IAM policy evaluation, S3 bucket policies, and Glue Catalog permissions to confirm isolation holds.\n- **Performance Testing**: Load test with 5k events/min to validate Kinesis shard provisioning, Lambda concurrency, and throughput.\n\n## LocalStack Plan\nLocalStack enables local testing without AWS costs. Provision Kinesis, S3, DynamoDB, Lambda, and Glue resources via Terraform or the LocalStack AWS CLI. Mock X-Ray for tracing validation. Use test data generators to simulate multi-tenant event streams and validate isolation, deduplication, and routing logic in a CI/CD pipeline.",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-18",
        "questionId": "q-9882",
        "question": "Beginner AWS DVA task: 2,000 devices emit JSON telemetry to a regional Kinesis Data Stream. Build a minimal pipeline that validates JSON, computes a data-quality score per record, deduplicates by deviceId+ts, and writes valid events to S3 Parquet partitioned by region/date, with invalid records sent to a DLQ. Add a deterministic sampling step (one event per 10 per device per hour) before storage, a 7-day backfill plan, and basic observability; explain idempotence and failure handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a concrete pipeline: ingest 2,000 devices via regional Kinesis Data Streams, validate JSON, compute a data quality score, deduplicate by deviceId+ts, and write valid events to S3 Parquet parti",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use blue/green across region-bound components (Kinesis shards, Lambda/Fargate, S3 prefixes, Glue/Athena catalogs). Route tenants by per-tenant residency flags and feature toggles stored in DynamoDB/Ap",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route each event by tenantId to a per-tenant consumer path. In latency-sensitivity mode, batch events into 1s windows through a micro-batching sink; otherwise stream directly to Lambda/Fargate. Ensure",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Route events by tenant to region-specific KDS streams, enforce residency with IAM/KMS policies, and use idempotent writes to a per-tenant DynamoDB offset store. Enrich in Lambda, store valid records t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\\nTests end-to-end ingestion, data quality scoring, and deduplication in a beginner-friendly, cost-conscious setup.\\n\\n## Key Concepts\\n- Ingestion using regional DVA primitives\\n- Deterministic sampling to cap costs\\n- Idempotent writes and per-record QC score\\n- Partitioning and 7-day backfill\\n- Basic observability and failure handling\\n\\n## Code Example\\n```javascript\\n// placeholder config\\n```\\n\\n## Follow-up Questions\\n- How would you implement per-device hour deterministic sampling?\\n- What metrics verify backfill progress?",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-19",
        "questionId": "q-30083",
        "question": "Design a cross-region telemetry ingestion pipeline using AWS DVA primitives that enforces per-tenant data residency, supports dynamic per-tenant retention/privacy rules evaluated at ingest via OPA, and maintains immutable audit trails. Ensure exactly-once processing, late data handling within 12 hours, and purge within tenant retention windows. Include LocalStack testing plan. What would you implement and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-region telemetry pipeline with two AWS regions, each running MQTT gateways that receive JSON events from IoT devices. Events are fanned out to regional Kinesis Data Streams. Use the Glue Schema Registry t…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a chaos-injection plan that randomly delays or drops 0.5-2% of records between Kinesis and the consumer. Use idempotent writes to DynamoDB and S3, enable OpenTelemetry traces across producer,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest 1,000 sensors via Kinesis Data Streams; Lambda validates schema, normalizes to region/ts; update a DynamoDB window state with per-region-minute sums and counts; compute a 5-minute rolling avera",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Cross-region per-tenant telemetry ingestion leveraging AWS DVA primitives. Telemetry flows through regional Kinesis Data Streams, where each tenant's data is automatically routed to their designated region based on resi…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question assesses a candidate's ability to design cross-region data localization architectures, implement policy-driven governance at scale, and manage production-grade data lifecycles using AWS primitives. It tests knowledge of observability patterns, regulatory compliance requirements, and local testing strategies with tools like LocalStack.\n\n## Key Concepts\n- Per-tenant data residency with enforced regional storage boundaries\n- Dynamic retention and privacy rules evaluated at ingest via OPA\n- Exactly-once processing semantics with idempotent writes\n- Immutable audit trails for compliance and traceability\n- Late data handling and time-windowed retention enforcement",
        "difficulty": "advanced"
      },
      {
        "id": "aws-dva-q-20",
        "questionId": "q-28848",
        "question": "Beginner AWS DVA: In a single region, ingest 2k JSON telemetry events/min from IoT devices into a Kinesis Data Stream. Use a Kinesis Firehose delivery stream with a Lambda transformer to validate records against a DynamoDB-stored versioned schema (per event.version) and route invalid records to an SQS DLQ. Valid records are written to S3 Parquet partitioned by date. Enable X-Ray tracing across publisher → Firehose → S3, and provide a LocalStack test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Configure a Kinesis Data Stream as the ingestion source, connected to a Kinesis Firehose delivery stream with an inline Lambda transformation. The Lambda function retrieves the schema version from DynamoDB and validates…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest events to Kinesis; propagate provenance with OpenTelemetry; write a Provenance item (tenant_id, event_id, source, transforms, ts) to DynamoDB and a lineage-augmented copy of the event to S3 wit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed answer: Implement a Kinesis Data Streams + Lambda pipeline; validate JSON against a schema with optional fields; compute a simple anomaly flag by comparing temp to a device-specific rolling a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect per-tenant Kinesis Data Streams with tenant_id as the shard key; use Lambda or Fargate ETL to write Parquet to S3 (partitioned by tenant/date) and update a DynamoDB dedupe table keyed by (tenant_id, event_id,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question evaluates end-to-end data flow knowledge across multiple AWS services—Kinesis Data Streams, Firehose, and Lambda—along with schema management using DynamoDB as a versioned registry. It assesses understanding of error handling through DLQ routing, data format transformation to Parquet with date partitioning, and observability via X-Ray tracing. The LocalStack test plan component validates local development and testing capabilities.\n\n## Key Concepts\n- Kinesis Data Streams and Firehose integration\n- Lambda transformations within Firehose pipelines\n- DynamoDB-based versioned schema registry\n- S3 partitioning strategies (by date)\n- SQS dead-letter queue patterns\n- Distributed tracing with X-Ray",
        "difficulty": "beginner"
      },
      {
        "id": "aws-dva-q-21",
        "questionId": "q-2906",
        "question": "In a real-time telemetry pipeline for thousands of devices across regions, design a per-tenant dynamic data-correction flow: each event must be corrected according to a tenant-specific policy version stored in DynamoDB, applied deterministically in Lambda, and written to S3 as Parquet partitioned by tenant/date. Policies can change; ensure 24h replayability, idempotency, and auditable lineage using Glue/Athena. Include trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-region Kinesis Data Streams; a Lambda subscribes to each stream and applies tenant-specific masking rules stored in DynamoDB, emitting masked JSON to S3 Parquet (partitioned by region/date) vi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Kinesis Data Streams to ingest telemetry from thousands of devices. A Lambda function reads each tenant's correction policy from DynamoDB, applies deterministic corrections using a composite idempotency key (deviceI…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Architect a privacy-first telemetry pipeline: ingest in Kinesis with tenant-scoped keys, mask PII before storage, store raw in S3 with per-tenant prefixes, catalog via Glue, query via Athena, track te",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Store immutable raw in S3 with event_id as key; create a DynamoDB audit row per event: event_id, shard_id, ingest_ts, process_ts, status, transformations_hash. Use idempotent writes via conditional Pu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Design\nThis question tests your ability to combine four key requirements: dynamic rules per tenant, data isolation, replay capability, and auditability—all in a streaming system with thousands of devices.\n\n## Core Components\n\n**1. Ingestion Layer (Kinesis Data Streams)**\nKinesis handles high-throughput ingestion with built-in ordering, replication, and fan-out capabilities. Each shard processes events in order, enabling deterministic corrections.\n\n**2. Policy Storage (DynamoDB)**\n```python\n# DynamoDB Table: tenant_policies\n{\n    'tenant_id': 'tenant-123',      # Partition key\n    'policy_version': '2024-01-15v3', # Sort key\n    'rules': {\n        'outlier_threshold': 100,\n        'scaling_factor': 1.05,\n        'null_handling': 'interpolate',\n        'unit_conversion': {'celsius_to_fahrenheit': True}\n    },\n    'effective_from': '2024-01-15T00:00:00Z',\n    'created_at': '2024-01-15T10:30:00Z'\n}\n```\n\n**3. Lambda Processing**\n```python\nimport boto3\nimport json\nimport hashlib\nfrom datetime import datetime\nimport pyarrow as pa\nimport pyarrow.parquet as pq\nfrom dataclasses import dataclass\nfrom typing import Dict, Any\n\ndynamodb = boto3.resource('dynamodb')\ns3 = boto3.client('s3')\n\nPOLICY_TABLE = 'tenant_policies'\nPROCESSED_TABLE = 'processed_events'\nOUTPUT_BUCKET = 'telemetry-corrected'\n\n@dataclass\nclass TelemetryEvent:\n    device_id: str\n    tenant_id: str\n    sequence_num: int\n    timestamp: str\n    payload: Dict[str, Any]\n    raw_policy_version: str\n\ndef get_policy(tenant_id: str, effective_time: str = None) -> Dict:\n    \"\"\"Retrieve latest active policy for tenant.\"\"\"\n    table = dynamodb.Table(POLICY_TABLE)\n    \n    if effective_time:\n        response = table.query(\n            KeyConditionExpression='tenant_id = :tid AND effective_from <= :et',\n            ExpressionAttributeValues={\n                ':tid': tenant_id,\n                ':et': effective_time\n            },\n            ScanIndexForward=False,\n            Limit=1\n        )\n    else:\n        response = table.query(\n            KeyConditionExpression='tenant_id = :tid',\n            ExpressionAttributeValues={':tid': tenant_id},\n            ScanIndexForward=False,\n            Limit=1\n        )\n    \n    items = response.get('Items', [])\n    return items[0] if items else None\n\ndef compute_idempotency_key(event: TelemetryEvent, policy_version: str) -> str:\n    \"\"\"Deterministic composite key for idempotency.\"\"\"\n    composite = f\"{event.tenant_id}:{event.device_id}:{event.sequence_num}:{policy_version}\"\n    return hashlib.sha256(composite.encode()).hexdigest()\n\ndef is_already_processed(idempotency_key: str) -> bool:\n    \"\"\"Check if event was already processed.\"\"\"\n    table = dynamodb.Table(PROCESSED_TABLE)\n    try:\n        table.get_item(Key={'idempotency_key': idempotency_key})\n        return True\n    except table.meta.client.exceptionsConditionalCheckFailedException:\n        return False\n\ndef apply_corrections(event: TelemetryEvent, policy: Dict) -> Dict[str, Any]:\n    \"\"\"Apply deterministic corrections based on tenant policy.\"\"\"\n    rules = policy.get('rules', {})\n    corrected = event.payload.copy()\n    \n    # Outlier handling\n    if 'value' in corrected and corrected['value'] > rules.get('outlier_threshold', float('inf')):\n        corrected['value'] = rules.get('outlier_threshold')\n        corrected['corrected'] = True\n        corrected['correction_reason'] = 'outlier_capped'\n    \n    # Unit conversion\n    if rules.get('unit_conversion', {}).get('celsius_to_fahrenheit'):\n        if 'temperature' in corrected:\n            corrected['temperature'] = corrected['temperature'] * 9/5 + 32\n            corrected['unit'] = 'fahrenheit'\n    \n    # Null handling\n    if corrected.get('value') is None and rules.get('null_handling') == 'interpolate':\n        corrected['value'] = corrected.get('prev_value', 0)\n        corrected['corrected'] = True\n        corrected['correction_reason'] = 'null_interpolated'\n    \n    return corrected\n\ndef write_parquet_partition(tenant_id: str, dt: datetime, data: list):\n    \"\"\"Write Parquet file partitioned by tenant/date/hour.\"\"\"\n    table = pa.Table.from_pylist(data)\n    \n    partition_path = (\n        f\"tenant_id={tenant_id}/\"\n        f\"date={dt.strftime('%Y-%m-%d')}/\"\n        f\"hour={dt.strftime('%H')}/\"\n        f\"{dt.strftime('%Y%m%d%H%M%S')}_{uuid.uuid4().hex[:8]}.parquet\"\n    )\n    \n    buffer = io.BytesIO()\n    pq.write_table(table, buffer, compression='snappy')\n    buffer.seek(0)\n    \n    s3.put_object(\n        Bucket=OUTPUT_BUCKET,\n        Key=partition_path,\n        Body=buffer.getvalue()\n    )\n    \n    return partition_path\n\ndef lambda_handler(event, context):\n    \"\"\"Main Lambda handler for telemetry correction.\"\"\"\n    for record in event['Records']:\n        raw_data = json.loads(base64.b64decode(record['kinesis']['data']))\n        \n        telemetry = TelemetryEvent(\n            device_id=raw_data['device_id'],\n            tenant_id=raw_data['tenant_id'],\n            sequence_num=raw_data['sequence_num'],\n            timestamp=raw_data['timestamp'],\n            payload=raw_data['payload'],\n            raw_policy_version=raw_data.get('policy_version', 'unknown')\n        )\n        \n        # Get applicable policy\n        policy = get_policy(telemetry.tenant_id, telemetry.timestamp)\n        if not policy:\n            continue\n        \n        policy_version = policy['policy_version']\n        idempotency_key = compute_idempotency_key(telemetry, policy_version)\n        \n        # Idempotency check\n        if is_already_processed(idempotency_key):\n            continue\n        \n        # Apply corrections\n        corrected = apply_corrections(telemetry, policy)\n        \n        # Prepare output record\n        output_record = {\n            'tenant_id': telemetry.tenant_id,\n            'device_id': telemetry.device_id,\n            'sequence_num': telemetry.sequence_num,\n            'timestamp': telemetry.timestamp,\n            'policy_version_applied': policy_version,\n            'raw_policy_version': telemetry.raw_policy_version,\n            'payload': corrected,\n            'lineage_id': idempotency_key,\n            'processed_at': datetime.utcnow().isoformat()\n        }\n        \n        # Write to S3\n        event_dt = datetime.fromisoformat(telemetry.timestamp)\n        s3_path = write_parquet_partition(telemetry.tenant_id, event_dt, [output_record])\n        \n        # Mark as processed (with TTL for 24h replay window)\n        table = dynamodb.Table(PROCESSED_TABLE)\n        table.put_item(\n            Item={\n                'idempotency_key': idempotency_key,\n                's3_path': s3_path,\n                'processed_at': int(datetime.utcnow().timestamp()),\n                'ttl': int((datetime.utcnow() + timedelta(hours=24)).timestamp())\n            },\n            ConditionExpression='attribute_not_exists(idempotency_key)'\n        )\n```\n\n**4. Parquet Partitioning Strategy**\n- **Path**: `s3://bucket/tenant_id=<tenant>/date=<YYYY-MM-DD>/hour=<HH>/<file>.parquet`\n- **Benefits**: Athena queries filter by tenant first (reduces scan), then date/hour (time-range pruning)\n- **File sizing**: Target 128-256MB files using Kinesis Aggregation or periodic buffering\n- **Schema**: Explicit columnar format enables predicate pushdown and efficient compression\n\n**5. 24-Hour Replay Architecture**\n- Raw events stored in: `s3://raw-events/tenant=<tenant>/year=<YYYY>/month=<MM>/day=<DD>/<event>.json`\n- Replay Lambda reads raw events + applies current policy\n- Idempotency keys include policy_version to detect changes\n- Reprocess only affected partitions based on policy effective dates\n\n**6. Audit Lineage (Glue/Athena)**\n```sql\n-- Athena table for lineage tracking\nCREATE TABLE lineage_audit (\n    lineage_id STRING,\n    tenant_id STRING,\n    device_id STRING,\n    sequence_num BIGINT,\n    raw_policy_version STRING,\n    applied_policy_version STRING,\n    corrections_applied MAP<STRING, STRING>,\n    s3_path STRING,\n    processed_at TIMESTAMP\n) PARTITIONED BY (tenant_id, date STRING)\nLOCATION 's3://telemetry-corrected/lineage/';\n\n-- Query for specific device lineage\nSELECT * FROM lineage_audit\nWHERE device_id = 'device-456'\n  AND tenant_id = 'tenant-123'\nORDER BY processed_at DESC;\n```\n\n## Trade-offs\n\n| Aspect | Choice | Trade-off |\n|--------|--------|----------|\n| **Idempotency** | DynamoDB composite key | Strong consistency but adds latency per event |\n| **Policy Storage** | DynamoDB | Fast reads but cost scales with tenant count |\n| **File Format** | Parquet | Excellent compression but requires schema evolution strategy |\n| **Replay** | S3 raw + reprocess | Simple but 2x storage cost; faster option: Kinesis Replay API |\n| **Partition Granularity** | tenant/date/hour | Balances query performance vs file size; too fine = too many files |\n\n## Common Pitfalls\n- **Missing TTL**: Processed events table grows unbounded without TTL cleanup\n- **Hot partitions**: Skewed tenant distribution causes Kinesis shard imbalance\n- **Policy version drift**: Not capturing policy_version at ingestion breaks replay\n- **Small files**: Writing one event per Parquet file overwhelms Athena with metadata overhead",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-22",
        "questionId": "q-4746",
        "question": "Design an ingestion path for multi-tenant telemetry where an in-flight data quality gate validates schema and field types, enforces value ranges, and flags anomalies with a per-tenant score. Valid events pass to per-tenant Parquet in S3; invalid go to quarantine with alerts. Guarantee exactly-once semantics across retries and enable backfill within 48 hours without duplicates. Use AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, and OpenTelemetry. How would you implement per-tenant validation schemas, idempotent writes, and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-region design: IoT Core MQTT gateway -> Region A/B Kinesis Data Streams; Lambda validates against Glue Schema Registry, then computes a per-record quality score. Deduplicate on deviceId+ts using D",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement at ingestion a per-tenant schema in Glue, validate JSON against Avro, enforce ranges, and assign anomaly score; valid events land in per-tenant Parquet in S3; invalids go to quarantine with",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a data quality gate at ingestion: valid events feed the main Kinesis stream and partitioned Parquet in S3; corrupted ones go to a quarantined DLQ with Object Lock. Enforce per-tenant schema",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a producer Lambda that publishes telemetry events to a Kinesis Data Stream. Create a data-masking Lambda that redacts PII fields (userId, email, phone, ip) from the payload before a Parquet writer writes the d…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world data quality at ingestion is critical; this question probes schema management, idempotence, backfill safety, and observability across AWS primitives.\n\n## Key Concepts\n- Ingestion-time data quality gates and anomaly scoring\n- Per-tenant schema governance with Glue catalog\n- Exactly-once guarantees with sequence tracking in DynamoDB\n- Quarantine path and alerting\n- Backfill strategies and deduplication\n- OpenTelemetry tracing across KDS, Lambda, and S3\n\n## Code Example\n```javascript\n// Pseudocode: conditional write to DynamoDB to enforce idempotence\nconst committed = await dynamo.putItem({\n  TableName: 'IngestedEvents',\n  Item: { tenant: t, offset: o, eventId, ... },\n  ConditionExpression: 'attribute_not_exists(eventId)'\n}).then(()=>true).catch(()=>false);\n```\n\n## Follow-up Questions\n- How would you test backfill safety under bursts?\n- What failure modes require quarantining more aggressively?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-23",
        "questionId": "q-11403",
        "question": "Two-region, multi-tenant telemetry pipeline requires strict per-tenant data isolation, on-demand data deletion, and safe replay for audits. Design a streaming ingestion path using AWS DVA primitives (Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB) plus OpenTelemetry. Explain tenant-scoped partitioning, per-tenant Lake Formation rules, tenant-key management, tombstone-based deletions, and a replay mechanism that doesn't violate consistency. Include testing plan and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a multi-tenant data mesh that ingests telemetry from Kinesis Data Streams, materializes per-tenant data marts in S3 via Glue jobs, and serves via Athena. Implement Lake Formation for fine-gr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a blue/green deployment strategy for the cross-region telemetry pipeline as follows: **1. Environment Setup** Deploy the green (new) environment in the target region with identical infrastructure to the existi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a two-region pipeline with a single Kinesis stream per region keyed by tenantId; enforce per-tenant Lake Formation policies and per-tenant KMS keys; producers emit tenantId; a Lambda/Fargate consu",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Proposed approach: shard-per-tenant on Kinesis; per-tenant quotas tracked in DynamoDB; Lambda deduplicates on (tenantId, eventId); cross-region replication via mirrored streams and a fan-out Lambda to",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests isolation, deletion/replay guarantees, and cross-region orchestration under realistic multi-tenant loads.\n\n## Key Concepts\n- Tenant isolation via Lake Formation tagging and per-tenant KMS keys\n- Deletion tombstones with replay-safe semantics\n- Replay window controls and idempotent writes\n\n## Code Example\n```javascript\n// Pseudo: extractTenant, route to region, write Parquet, tombstone handling\n```\n\n## Follow-up Questions\n- How would you test cross-region deletion replay integrity?\n-What metrics ensure deletion/replay latency stays within SLA?",
        "difficulty": "intermediate"
      },
      {
        "id": "aws-dva-q-24",
        "questionId": "q-7932",
        "question": "Design a real-time telemetry ingestion path that enforces per-tenant data quality rules (required fields, types, ranges) at ingestion time using AWS DVA primitives: Kinesis Data Streams, Lambda/Fargate, S3, Glue, Athena, DynamoDB, plus OpenTelemetry. Include quarantining bad records, per-tenant quality metrics, handling schema evolution, and backpressure management for near-zero data loss?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant region-scoped streams in Kinesis with per-tenant KMS keys; ingest via Lambda, process with Fargate for heavy transforms; store raw and curated data in S3 (Parquet, tenant/date par",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed: produce 2k JSON events/min with a correlationId, push to Kinesis Data Stream in one region. A Lambda consumer validates JSON, computes a per-record quality score, and writes valid records to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant validators in a streaming stage (Lambda/Fargate) that enforce required fields, types, and value ranges; route valid events to the sink; quarantine bad records to a per-tenant bad-record bucket with…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Kinesis Data Streams as the inlet; a Lambda validates JSON and deduplicates via DynamoDB (deviceId+ts). Valid events go to a second Lambda that writes Parquet to S3 partitioned by region/date; inv",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design robust, scalable ingestion with strong per-tenant data governance. Probes data quality enforcement, quarantine strategy, schema evolution, and backpressure handling under real-world load.\n\n## Key Concepts\n\n- Per-tenant data quality validation at ingestion\n- Quarantine (bad records) with audit trails\n- Schema evolution using Glue catalog and compatible formats\n- Backpressure handling via DLQ and adaptive sampling\n- Observability: per-tenant quality metrics and alerts\n\n## Code Example\n\n```javascript\n// Pseudocode: per-tenant validation at ingress\nfunction validateAndRoute(event, tenantConfig) {\n  const validationResult = validateEvent(event, tenantConfig);\n  \n  if (validationResult.isValid) {\n    writeToValidStream(event);\n    updateMetrics(tenantConfig.tenantId, 'valid');\n  } else {\n    quarantineRecord(event, validationResult.errors, tenantConfig);\n    updateMetrics(tenantConfig.tenantId, 'invalid');\n  }\n}\n```",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-12T09:35:25.996Z",
    "version": 1
  }
]