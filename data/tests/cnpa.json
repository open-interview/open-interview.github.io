[
  {
    "id": "test-cnpa",
    "channelId": "cnpa",
    "channelName": "Cnpa",
    "title": "Cnpa Knowledge Test",
    "description": "Test your understanding of Cnpa with 25 questions.",
    "questions": [
      {
        "id": "cnpa-q-0",
        "questionId": "q-27241",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis experiences cache stampede during peak load. Design an end-to-end plan to prevent stampede while preserving freshness, including Redis locking strategy, per-key rate limiting, API backpressure, Kafka production pacing, and instrumentation. Provide concrete configs, metrics, and a validation plan with staged rollout and failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement request coalescing at the API layer and a Redis-based lock (SETNX) to serialize concurrent cache misses, with TTL-based refreshes to avoid stampede. Add per-key rate limits and a circuit bre",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Store the flag in Redis as feature:new-field:percent with a 10% seed; gate the API by user_id%10 == 0 for the new field. Make the new column nullable in PostgreSQL and populate only when the flag is e",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a runtime feature toggle in CNPA: admin HTTP endpoint updates a PostgreSQL toggles table; publish a Redis Pub/Sub message; all instances refresh their in‑memory cache and invalidate keys wit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Wrap the order write in a MongoDB multi-document transaction with readConcern snapshot and writeConcern majority, and use an outbox pattern to publish to Kafka with an idempotent producer. Invalidate",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical cache-coherence strategies under CNPA load, covering Redis locking, API backpressure, and Kafka pacing with realistic validation.\n\n## Key Concepts\n- Cache stampede prevention\n- Redis distributed locking (SETNX, EX) and TTL refresh\n- Per-key rate limiting and circuit breakers\n- Backpressure across HTTP, Redis, and Kafka\n- Observability and staged rollout validation\n\n## Code Example\n```javascript\n// Node.js sketch for cache refresh with Redis lock\nconst lockKey = `lock:${cacheKey}`;\nif (await redis.set(lockKey, '1', 'NX', 'EX', 30)) {\n  const value = await loadFromDBOrCompute(cacheKey);\n  await redis.set(cacheKey, value, 'EX', 300);\n  await redis.del(lockKey);\n} else {\n  // another worker refreshes\n  await sleep(50);\n}\n```\n\n## Follow-up Questions\n- How would you monitor lock contention and adjust TTLs at scale?\n- How would you test resilience when Redis cluster experiences partial outages?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-1",
        "questionId": "q-2041",
        "question": "In a CNPA stack (HTTP API -> Postgres -> Kafka -> Redis) you notice tail latency spikes during peak hours. Outline a concrete, beginner-friendly plan to diagnose end-to-end latency using distributed tracing. Include what to instrument, where to insert spans (HTTP handler, DB query, Kafka producer/consumer, Redis ops), how to propagate trace context, minimal metrics, and a simple verification checklist to confirm fixes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement OpenTelemetry across all services in the CNPA stack. Create a root span for each HTTP request and child spans for Postgres queries, Kafka producer/consumer operations, and Redis commands. Propagate trace conte…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Postgres Row-Level Security with a tenant_id on all tables and policies enforcing it; pass tenant_id from API and attach to Kafka headers; namespace Redis keys per tenant. Migration: add tenant_id",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a cache-aside plan: on HTTP request, read from Redis; on miss, fetch from PostgreSQL, then populate Redis with a TTL (e.g., 300s). Add cache-stampede protection with a short distributed lock",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-phase purge: (1) in a single Postgres transaction, set pii_deleted_at and emit a tombstone to an outbox; (2) publish tombstones to a compacted Kafka topic and invalidate Redis keys via a purge eve",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDemonstrates practical observability skills and a systematic approach to diagnosing end-to-end latency issues in distributed systems.\n\n## Key Concepts\n- **Distributed Tracing with OpenTelemetry**: Industry-standard for tracing across service boundaries\n- **Span Hierarchy**: HTTP handler → Postgres → Kafka → Redis operations\n- **Context Propagation**: Trace headers and Kafka message metadata maintain correlation\n- **Latency Metrics**: p95/p99 percentiles for tail latency analysis\n- **Visualization Dashboards**: End-to-end request flow monitoring\n\n## Implementation Plan\n1. **Instrumentation Points**: HTTP handlers, database queries, message producers/consumers, cache operations\n2. **Trace Context**: W3C Trace Context headers for HTTP, custom headers for Kafka messages\n3. **Span Creation**: Root spans for entry points, child spans for downstream operations\n4. **Metrics Collection**: Duration, error rates, and throughput per service component\n5. **Dashboard Setup**: Service mesh visualization and latency heatmaps\n\n## Verification Checklist\n- [ ] Trace spans appear for all service components\n- [ ] Context properly propagates across HTTP and Kafka boundaries\n- [ ] Latency metrics show p95/p99 trends during peak hours\n- [ ] Dashboards display complete end-to-end request flows\n- [ ] Alert thresholds configured for tail latency spikes",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-2",
        "questionId": "q-14334",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis experiences data consistency issues under peak load: user_signup events may diverge between DB and Kafka. Design a beginner-friendly plan to guarantee end-to-end consistency, including concrete checks (FK constraints, idempotent producers/consumers, outbox pattern), minimal code changes, and validation steps under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route events to tenant.{tenantId}.events via a routing layer with a feature flag; keep global.events as fallback during migration. Use Kafka transactions and idempotent sinks; emit to both topics in c",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument all hops with OpenTelemetry: HTTP server middleware starts a root span; DB driver and queries are wrapped; Kafka producer/consumer spans are created and linked; downstream analytics message",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an end-to-end consistency solution using the transactional outbox pattern to reliably publish database changes to Kafka, enforce foreign key constraints and unique identifiers, enable idempotent producers and…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "To diagnose performance bottlenecks during peak load, I'll implement a systematic debugging approach across the CNPA stack. First, deploy distributed tracing across all components to establish end-to-end visibility. For…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates understanding of data consistency challenges across CNPA stack components and the ability to design practical solutions for production environments.\n\n## Key Concepts\n- End-to-end consistency across HTTP API, PostgreSQL, Kafka, and Redis\n- Transactional outbox pattern for reliable event publication\n- Idempotent producers and consumers to handle retries safely\n- Foreign keys and unique constraints to preserve data integrity\n- Load testing validation with automated consistency checks and backfill procedures\n\n## Code Example\n```javascript\n// Pseudo-outbox implementation\nconst signupTransaction = await db.transaction(async (trx) => {\n  // Insert user with FK constraints\n  const user = await trx('users').insert(userData);\n  \n  // Create outbox record in same transaction\n  await trx('outbox').insert({\n    event_type: 'user_signup',\n    payload: { user_id: user.id, ...userData },\n    idempotency_key: generateUUID()\n  });\n  \n  return user;\n});\n```",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-3",
        "questionId": "q-1150",
        "question": "In a CNPA stack: HTTP API ingests events, writes to PostgreSQL, and publishes to Kafka; during spikes, retries duplicate processed events. Propose a concrete, end-to-end plan to guarantee idempotent processing and prevent duplicates under retry storms. Include idempotency key strategy, dedup enforcement point, Kafka/DB coordination, and validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a transactional outbox: write balance_delta and a matching event_id in PostgreSQL within the same transaction, publish via Kafka transactions so the event only emits after commit succeeds, a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To diagnose, reproduce under peak load and trace Redis, Kafka, and DB paths. Collect Redis latency and hit/miss ratio, Redis key eviction events, Kafka consumer lag, and DB query latencies. If stale r",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a request-id per submission and make request_id unique in Postgres; perform INSERT into the events table with ON CONFLICT DO NOTHING so retries don't duplicate writes. Use a transactional outbox pattern with a Kafka…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a robust cache-aside strategy for Redis to protect against stampedes under peak load. Use per-key locks (REDLOCK), a Lua atomic fetch-or-set for misses, and a warmup process when a new item is",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to reason about retries, deduplication, and cross-service guarantees in CNPA pipelines with proper transactional coordination.\n\n## Key Concepts\n\n- Idempotent processing with database constraints\n- Transactional outbox pattern for Kafka\n- Postgres unique constraints + ON CONFLICT\n- Kafka transactional producer with exactly-once semantics\n- Multi-layer dedup enforcement (API → DB → Consumer)\n- Request-id as idempotency key\n\n## Code Example\n\n```javascript\n// API layer - generate and validate request-id\napp.post('/events', async (req, res) => {\n  const requestId = req.headers['x-request-id'] || uuidv4();\n  \n  const query = `\n    INSERT INTO events (request_id, payload, created_at) \n    VALUES ($1, $2, NOW()) \n    ON CONFLICT (request_id) DO NOTHING\n    RETURNING id\n  `;\n  \n  const result = await client.query(query, [requestId, req.body]);\n  if (!result.rows.length) {\n    return res.status(409).json({ error: 'Duplicate request' });\n  }\n  \n  // Transactional outbox\n  await client.query(`\n    INSERT INTO event_outbox (event_id, topic, payload, created_at)\n    VALUES ($1, 'events', $2, NOW())\n  `, [result.rows[0].id, req.body]);\n  \n  res.json({ requestId, eventId: result.rows[0].id });\n});\n\n// Outbox poller with transactional producer\nconst pollOutbox = async () => {\n  const producer = kafka.producer({ transactionalId: 'event-producer' });\n  await producer.connect();\n  \n  while (true) {\n    const txn = await producer.transaction();\n    try {\n      const records = await client.query(`\n        SELECT * FROM event_outbox \n        WHERE processed = false \n        ORDER BY created_at ASC \n        LIMIT 100\n      `);\n      \n      for (const record of records.rows) {\n        await txn.send({\n          topic: record.topic,\n          messages: [{ value: record.payload, key: record.event_id }]\n        });\n        \n        await client.query(`\n          UPDATE event_outbox \n          SET processed = true \n          WHERE id = $1\n        `, [record.id]);\n      }\n      \n      await txn.commit();\n    } catch (error) {\n      await txn.abort();\n    }\n    await sleep(100);\n  }\n};\n\n// Consumer with idempotency cache\nconst consumer = kafka.consumer({ groupId: 'event-processors' });\nconst processedCache = new Redis();\n\nawait consumer.subscribe({ topic: 'events', fromBeginning: true });\n\nawait consumer.run({\n  eachMessage: async ({ message }) => {\n    const requestId = message.key.toString();\n    const exists = await processedCache.exists(requestId);\n    if (exists) return;\n    \n    await processEvent(message.value);\n    await processedCache.setex(requestId, 3600, 'processed');\n  }\n});\n```\n\n## Follow-up Questions\n\n- How would you handle schema evolution in the outbox table?\n- What's your strategy for exactly-once processing across multiple consumers?\n- How do you ensure the outbox poller doesn't become a bottleneck during traffic spikes?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-4",
        "questionId": "q-3073",
        "question": "CNPA stack: HTTP API writes to Postgres and publishes to Kafka; downstream analytics consumes from Kafka and loads into a data warehouse. A schema evolution adds an optional field to the event payload used by dashboards. Describe a concrete plan to roll this out without downtime, covering: versioned schemas and topic management, writer/reader compatibility rules in a schema registry, backfill strategy, feature flags to toggle new field usage, and validation metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "End-to-end latency: HTTP handler time, Kafka publish, stream-processor throughput/latency, and Snowflake load time. Track Kafka consumer lag, topic backlogs, retry rate, and query times. Use idempoten",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a staged chaos engineering experiment to inject HTTP 5xx bursts, database timeouts, Kafka consumer lag, and Redis eviction events with progressively increasing blast radius. Implement end-to-end latency tracing u…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy a new Avro schema version in the registry with the optional field configured for BACKWARD compatibility. Maintain existing data flow while implementing a wrapper payload and dual-topic approach to support both le…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement an idempotent signup path: enforce a signup_id on each request, store it in Redis with a TTL (e.g., 1 day) to dedupe. In PostgreSQL, upsert on (signup_id) for users; publish the signup event",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests concrete planning for safe schema evolution in a CNPA pipeline; evaluates ability to coordinate registry management, topic architecture, backfill processes, and controlled rollout with measurable validation.\n\n## Key Concepts\n\n- Schema Registry, Avro, compatibility modes (BACKWARD/FORWARD)\n- Topic versioning and aliases, wrapper payloads\n- Backfill strategies, canary deployments, feature flags\n- Validation metrics: compatibility errors, consumer lag, dashboard drift\n\n## Code Example\n\n```bash\n# Register new schema with BACKWARD compatibility\ncurl -X POST http://schema-registry-host:8081/subjects/event-value/versions \\\n -H \"Content-Type: application/vnd.schemaregistry.v1+json\" \\\n -d '{\"schema\": \"{\\\"type\\\": \\\"record\\\", \\\"name\\\": \\\"Event\\\", \\\"fields\\\": [{\\\"name\\\": \\\"id\\\", \\\"type\\\": \\\"string\\\"}, {\\\"name\\\": \\\"newField\\\", \\\"type\\\": [\\\"null\\\", \\\"string\\\"], \\\"default\\\": null}]}\"}'\n```",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-5",
        "questionId": "q-11409",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and a downstream analytics pipeline uses Avro schemas with Confluent Schema Registry. Design a practical plan to evolve the Kafka event schema during a live rollout while preserving backward compatibility, avoiding message drops, and preventing consumer breakage. Include versioning, migration steps, tests, and rollback criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant quotas using a Redis-backed token bucket with an atomic Lua script. Derive tenant id from JWT; load quotas from Postgres and cache them in Redis with short TTL. On each request, L",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Keep backward-compatible Avro in Schema Registry and lock backward compatibility. Publish new schema with is_email_verified as optional; deploy a canary producer behind a feature flag and route a subs",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement OpenTelemetry across the stack, propagate traceparent header, enable DB, Kafka, and Redis instrumentation, and export to Tempo/Jaeger. Use 5–10% sampling in canary, collect latency percentil",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use backward-compatible changes only; publish a new Avro schema version in the Registry; upgrade producers to emit the new field with a default while readers use the old schema. Do a canary rollout, v",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes schema evolution discipline in a CNPA stack, focusing on backward compatibility, producer/consumer choreography, and rollout safety during live updates. It tests practical use of Schema Registry, canary strategies, and rollback criteria.\n\n## Key Concepts\n\n- Avro schema evolution under backward/forward compatibility\n- Schema Registry versioning and compatibility checks\n- Canary rollout, DLQ monitoring, consumer lag\n- Rollback and rollback criteria, instrumentation\n\n## Code Example\n\n```javascript\n// Avro-like schema evolution sketch (JSON stored in Registry)\n{\n  \"type\": \"record\",\n  \"name\": \"UserEvent\",\n  \"fields\": [\n    {\"name\": \"id\", \"type\": \"string\"},\n    {\"name\": \"version\", \"type\": \"int\"},\n    {\"name\": \"createdAt\", \"type\": \"long\"},\n    {\"name\": \"email\", \"type\": [\"null\", \"string\"], \"default\": null}\n  ]\n}\n```\n\n## Follow-up Questions\n\n- How would you enforce runtime validation of messages against the latest schema without breaking existing consumers?\n- What metrics signal a breaking change in production, and how would you automate rollback?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-6",
        "questionId": "q-7727",
        "question": "In a CNPA stack with an HTTP API, PostgreSQL, Kafka, and Redis, a new optional field 'consent' must be added to user profiles with zero downtime and backward/forward compatibility across reads, writes, Kafka events, and Redis caches. Design a concrete migration plan with steps, data backfill, feature flags, and verification?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use explicit invalidate: keys product:list and product:ID. On write: update DB, delete both keys, publish a Kafka event to invalidate caches. TTL 60s as safety net. Employ a lightweight distributed lo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use PostgreSQL Row-Level Security with a per-row tenant_id and policies (USING (tenant_id = current_setting('cnpa.tenant')::int)). Apply topic prefixes by tenant (cnpa-tenantA-*, cnpa-tenantB-*) and e",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Add a nullable JSONB consent column on users; keep API backward compatible by treating missing as null. Use transactional outbox to publish writes to Kafka. Implement a feature flag to enable the new",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a versioned envelope for events. Step 1: add a new optional field in a v2 payload and publish to a bridging topic while continuing v1 writes. Step 2: extend consumers to accept both versions",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to perform zero-downtime schema changes in a CNPA stack and keep data consistency across Postgres, Kafka, and Redis.\n\n## Key Concepts\n- Backward/forward compatible migrations\n- Transactional outbox and event sourcing\n- Feature flags and canary rollouts\n- Cache invalidation and backfill strategy\n- Observability and rollback plan\n\n## Code Example\n```sql\n-- Add the new nullable field without locking reads\nALTER TABLE users ADD COLUMN consent JSONB NULL;\n```\n\n## Follow-up Questions\n- How would you validate canary success and rollback safely?\n- How do you handle Kafka schema evolution and downstream consumers?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-7",
        "questionId": "q-7176",
        "question": "In a CNPA stack with an HTTP API, PostgreSQL, Kafka, and Redis, you must guarantee read-after-write for profile updates under high throughput. Design an end-to-end plan to ensure correctness across HTTP writes, DB commits, Kafka publishing, and Redis cache reads, including a transactional outbox, idempotent sinks, cache invalidation, tests, and metrics to verify success?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Roll out the enrichment step with a per-tenant feature flag. Make enrichment asynchronous with a bounded retry queue and a fallback that returns un-enriched events if the external service is down. Pro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use transactional outbox: wrap profile update and an outbox row in one DB transaction. A separate idempotent sink publishes the outbox to Kafka, then marks sent. Invalidate Redis on commit success. Ha",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement an outbox table in Postgres, publish to Kafka within a DB transaction, and build idempotent sinks in Redis keyed by (entity_id, event_id). Add a reconciliation pass that confirms all publish",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument CNPA with OpenTelemetry: trace HTTP requests across handler, DB, Kafka, and Redis. Collect per-span latency (p95/p99), error rate, and queue depths. Propagate trace context in HTTP headers,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical data correctness guarantees in asynchronous CNPA pipelines, focusing on end-to-end consistency, failure handling, and detection of regressions under load.\n\n## Key Concepts\n- transactional outbox\n- idempotent sinks\n- cache invalidation\n- read-after-write latency\n- monitoring and tests\n\n## Code Example\n\n```javascript\n// Pseudo flow: within a transaction, write profile and outbox\ndb.transaction(() => {\n  updateProfile({ id, name, email })\n  insertOutbox({ aggregate_id: id, payload: change })\n})\n```\n\n## Follow-up Questions\n\n- How would you test read-after-write guarantees under concurrent updates?\n- How would you observe and alert on missing tombstone events in Kafka?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-8",
        "questionId": "q-30426",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis experiences cache stampede during sudden traffic surge. The API serves user profile data; Redis cache misses trigger DB reads and Kafka events for analytics, causing latency spikes. Design a concrete plan to prevent stampede and keep latency under 100 ms: specify exact changes (coalescing key, in-flight flags with Redis SET NX, TTL tuning, background refresh, circuit breaker, canary flag), metrics to monitor, and how you’d verify impact?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement end-to-end signing: sign each HTTP body with a per-tenant key (ECDSA P-256) and attach a KeyID in a custom header; include a canonical payload (timestamp, nonce, body hash). Validate signatu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement request coalescing by using a Redis in-flight flag (cache:profile:<id>:inflight) set with SET NX and a short 5-second TTL. When a cache miss occurs, the first request acquires the lock; subsequent concurrent r…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a phased canary rollout for a Kafka event schema change using Schema Registry versioning. Start with staging validation: register v2 schema with COMPATIBILITY_MODE=BACKWARD, run consumer tests against v1 and v2 p…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To diagnose, reproduce under peak load and trace Redis, Kafka, and DB paths. Collect Redis latency and hit/miss ratio, Redis key eviction events, Kafka consumer lag, and DB query latencies. If stale r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses the ability to design pragmatic, fault-tolerant caching solutions under burst traffic conditions, with concrete Redis patterns and observability strategies.\n\n## Key Concepts\n\n- **Cache stampede prevention**: Using in-flight flags and coalescing to ensure only one request fetches data on cache miss\n- **Redis in-flight flags and NX semantics**: SET NX guarantees atomic lock acquisition; prevents multiple simultaneous cache refreshes\n- **Background refresh and read-through caching**: Proactive cache population before expiration reduces miss rates\n- **Backpressure and circuit breakers**: Protects downstream systems from cascading failures during overload\n- **Observability**: Tracking miss rate, latency percentiles, in-flight counts, and circuit breaker states\n\n## Code Example\n\n```javascript\n// Pseudocode: acquire in-flight lock then refresh\nasync function getProfile(userId) {\n  const cacheKey = `profile:${userId}`;\n  const lockKey = `${cacheKey}:inflight`;\n  \n  const cached = await redis.get(cacheKey);\n  if (cached) return JSON.parse(cached);\n  \n  // Try to acquire lock\n  const acquired = await redis.set(lockKey, '1', 'NX', 'EX', 5);\n  \n  if (!acquired) {\n    // Another request is fetching; wait and retry\n    await sleep(50);\n    return getProfile(userId);\n  }\n  \n  try {\n    const profile = await db.query('SELECT * FROM users WHERE id = ?', userId);\n    await redis.setex(cacheKey, 60 + rand(10), JSON.stringify(profile));\n    return profile;\n  } finally {\n    await redis.del(lockKey);\n  }\n}\n```\n\n## Follow-up Questions\n\n- Trade-offs between staleness and freshness in burst scenarios\n- How to validate burst handling end-to-end with canary testing\n- Impact of Redis failure on the in-flight flag pattern itself",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-9",
        "questionId": "q-19496",
        "question": "Design an end-to-end tracing plan for a CNPA stack (HTTP API, PostgreSQL, Kafka, Redis) to identify tail latency and error sources under peak load. Specify how trace context is propagated across HTTP, DB calls, Kafka producers/consumers, and Redis, which OpenTelemetry instruments to enable, sampling strategy, how to aggregate and visualize metrics, and how you would validate improvements with controlled experiments?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect end-to-end latency, queue depth, and replica lag; enable slow query logs and run explain on top slow queries; verify index coverage with compound indexes; apply index hints or add a composite",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by instrumenting the flag evaluation path and trace spans across HTTP, DB, Kafka, and Redis for canary vs control. Collect p95/p99 latency per component, per user segment, with OpenTelemetry. Ru",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use asynchronous enrichment with backpressure and a per-key rate limiter; add a two-tier cache (in-process TTL plus Redis) to reduce external lookups; make writes idempotent (upsert in Postgres) and u",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement distributed tracing with OpenTelemetry across all components. Propagate trace context via HTTP headers, thread it through the SQL driver, Kafka clients, and Redis calls. Create spans for HTT",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess practical ability to instrument and diagnose across a CNPA stack, ensuring end-to-end visibility and data-driven tuning of tail latency.\n\n## Key Concepts\n\n- Distributed tracing\n- OpenTelemetry\n- Context propagation\n- Sampling and backends\n- End-to-end latency metrics\n\n## Code Example\n\n```javascript\n// Initialize OpenTelemetry with OTLP exporter\nconst provider = new NodeTracerProvider();\nconst exporter = new OTLPTraceExporter({ url: 'http://collector:4317' });\nprovider.addSpanProcessor(new SimpleSpanProcessor(exporter));\n```\n\n## Follow-up Questions\n\n- How would you adjust sampling during peak load?\n- How would you verify that tracing instrumentation doesn't introduce significant overhead?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-10",
        "questionId": "q-23327",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis must support a new, queryable customer_tier index. Design a concrete plan for a scalable secondary index: compare storage options (Postgres index with transactional outbox, Redis sorted-set, or an external index service), detail backfill/online migration, ensure non-blocking writes and eventual consistency, and define concrete acceptance metrics and rollback criteria, including peak-load verification?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Redis-based lockout flag with TTL (e.g., 15 minutes). Sign-in checks first read Redis; if locked, return 429. If unlocked, verify policy in Postgres and on failure publish a Kafka event wi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement the transactional outbox pattern: atomically write the business record and outbox event in a single database transaction, then publish to Kafka via a transactional producer. Use a unique ingestion_id as an ide…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Storage Options Decision Matrix | Option | P99 Latency | Consistency | Operational Complexity | Scalability | Best For | |--------|-------------|-------------|------------------------|-------------|----------| | **Po…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Describe a reproducible plan to diagnose data-staleness across the CNPA pipeline: measure Kafka consumer lag for the indexing topic, check PostgreSQL write latency, inspect Elasticsearch refreshes and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Matters\n\nThis question tests your ability to design a secondary index in a distributed system where you must balance three competing needs: fast reads, strong consistency guarantees, and safe, non-disruptive migration. The CNPA stack—CNPG (PostgreSQL), Kafka, and Redis—represents a common production architecture where this skill is directly applicable.\n\n## Key Concepts Demonstrated\n\n**Transactional Outbox Pattern**\nThe outbox pattern solves the dual-write problem without distributed transactions. By writing the business event AND an outbox entry in the same Postgres transaction, we guarantee atomicity. The outbox processor (Kafka connect or standalone consumer) then publishes to message bus. This eliminates the race condition where the database write succeeds but the message publish fails.\n\n**Idempotent Consumer Design**\nKafka guarantees at-least-once delivery. Using Redis ZADD with the NX flag (only set if not exists) or storing version vectors makes consumer operations idempotent. This allows safe retries without duplicating data.\n\n**Chunked Backfill with Keyset Pagination**\nCursor-based pagination (`WHERE id > $last_id`) is essential for large table backfills. OFFSET-based pagination degrades linearly as the table grows; keyset pagination maintains constant time regardless of table size. Batch sizes of 10k rows balance throughput against database load.\n\n**Shadow Mode Migration**\nDeploying new infrastructure in shadow mode (writes happen but reads don't use it) allows validation without risk. Traffic shadowing (reading from both old and new systems) reveals consistency issues before they affect users.\n\n**Metrics That Actually Matter**\n- Lag metrics catch consumption bottlenecks before they become user-visible\n- Drift metrics catch data consistency issues that lag metrics miss\n- Memory/throughput metrics catch capacity issues before OOM events\n\n## Common Pitfalls This Question Avoids\n\n1. **Not handling duplicate messages** → Idempotent writes prevent accumulation\n2. **Backfill locking production tables** → Keyset pagination with row count limits\n3. **Assuming strong consistency is free** → Eventual consistency has real latency costs\n4. **No rollback plan** → Feature flags enable instant traffic reversal\n5. **Skip load testing** → 3x peak validates behavior under pressure, not just correctness",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-11",
        "questionId": "q-22902",
        "question": "In a CNPA stack with an HTTP API, PostgreSQL, Kafka, and Redis, a sudden surge causes downstream sinks to lag, spiking end‑to‑end latency and 5xxs. Propose a concrete plan to enforce graceful degradation and backpressure: specify where to apply per‑call timeouts and retries, how to implement circuit breakers, traffic shaping (rate limits, canaries, feature flags), and what instrumentation, thresholds, and acceptance tests you would use to verify no data loss and acceptable user experience under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Apply per‑service timeouts and a circuit breaker with backoff. HTTP 2s, PostgreSQL 2s, Kafka publish 1s; retries with exponential backoff up to 3 attempts. Circuit breaker triggers on 5 consecutive fa",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Decouple export by enqueueing to a dedicated worker pool; implement per-user quotas and backpressure on the HTTP path; add an export_jobs table with status and retry fields; use Redis to enforce quota",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Execute staged chaos injections: 1) pause Kafka partitions regionally for 60–120s; 2) throttle DB pool to exhaust connections; 3) induce Redis eviction pressure; 4) apply HTTP backpressure with client",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an end-to-end peak load test that injects known cohorts via the HTTP API and traces to Postgres, Kafka and the dashboard path. Use a transactional outbox in Postgres; ensure Kafka sinks are",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical hardening of CNPA systems under load, focusing on backpressure, degradation, and verifiable safety nets rather than theoretical patterns.\n\n## Key Concepts\n\n- Graceful degradation\n- Per‑service timeouts and backoff\n- Circuit breakers\n- Traffic shaping (rate limits, canaries, feature flags)\n- Observability and testing under load\n\n## Code Example\n\n```javascript\n// Example timeout/config sketch\nconst cfg = {\n  httpTimeoutMs: 2000,\n  dbTimeoutMs: 2000,\n  kafkaPublishMs: 1000,\n  maxRetries: 3\n}\n```\n\n## Follow-up Questions\n\n- How would you validate this plan under synthetic burst load?\n- How would multi‑region latency affect thresholds and canary rollout changes?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-12",
        "questionId": "q-25249",
        "question": "CNPA stack with an HTTP API, PostgreSQL, Kafka, and Redis introduces a feature flag gated by Redis that enables an expensive data enrichment path for a subset of users. On rollout, traffic targeting the flag spikes DB load and cache churn, causing 5xx errors. Outline a practical, end-to-end plan to diagnose and rollback safely: instrumentation, metrics, concrete mitigations (canary, cap enrichment, feature flag gating), and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Probe end-to-end path with explicit path metrics: HTTP handler latency, DB query plans, Redis cache hit/miss, and Kafka backlog during a canary rollout. If KPIs worsen, revert: disable enrichment path",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Instrument end-to-end tracing across HTTP, Redis, Postgres, and Kafka; collect p99 latency, Redis hit rate, DB query times, and Kafka publish latency under load. Reproduce with cold caches to observe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Wrap the order write in a MongoDB multi-document transaction with readConcern snapshot and writeConcern majority, and use an outbox pattern to publish to Kafka with an idempotent producer. Invalidate",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an end-to-end consistency solution using the transactional outbox pattern to reliably publish database changes to Kafka, enforce foreign key constraints and unique identifiers, enable idempotent producers and…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes real-world risk management during feature flag rollouts in CNPA stacks, emphasizing end-to-end measurement and safe rollback.\n\n## Key Concepts\n- Feature flags and Redis-backed gating\n- End-to-end observability across HTTP, DB, cache, and messaging\n- Safe rollback, backfill, and idempotency\n\n## Code Example\n```javascript\n// Redis rate limiter (pseudo)\nconst limiterKey = `enrich:${userId}`;\nconst allowed = await redis.incr(limiterKey);\nif (allowed > MAX_PER_MIN) throw new Error('Rate limited');\n```\n\n## Follow-up Questions\n- How would you test rollback safety under peak load?\n- What would you monitor to ensure no data corruption after rollback?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-13",
        "questionId": "q-28516",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, Redis, and a data lake sink operates in multiple regions. Design a concrete chaos engineering experiment to validate fault tolerance when Region A goes offline: how to simulate, what metrics to monitor (global p95/p99 latency, cross-region replication lag, consumer lag), and how to rollback safely without data loss?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a chaos experiment that simulates Region A going offline in a CNPA stack (HTTP API, PostgreSQL, Kafka, Redis, data lake). Inject latency, CPU pressure, and network partitions; monitor global p9",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Add OpenTelemetry across the stack: instrument HTTP middleware to start/finish spans; wrap PostgreSQL calls with a traced driver; instrument Kafka producers/consumers to emit spans and propagate conte",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement transactional outbox: write to DB and an outbox in one transaction; publish via Kafka with a transactional producer; sinks are idempotent by (entity_id, event_id) and dedupe; partition event",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument all hops with OpenTelemetry: HTTP server middleware starts a root span; DB driver and queries are wrapped; Kafka producer/consumer spans are created and linked; downstream analytics message",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design resilience experiments across CNPA stack and validate data integrity under regional outages.\n\n## Key Concepts\n- Chaos engineering, blast radius design, cross-region replication lag\n- End-to-end data integrity and idempotence in multi-region pipelines\n- Observability and rollback procedures\n\n## Code Example\n```\n# Example: trigger latency spike in region A using a chaos tool\nimport chaoslib\n\nchaoslib.inject('region','A','latency',duration=60,latency_ms=200)\n```\n\n## Follow-up Questions\n- How would you determine safe blast radius?\n- Which metrics and alerting thresholds would you set for automatic failover?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-14",
        "questionId": "q-28055",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis experiences cache stampede when a hot key is requested after eviction. Propose a beginner-friendly plan to prevent thundering herd while preserving data consistency, including concrete changes (per-key locks, Redis SET NX, TTL tuning, cache warming), tests, and how you'd verify reduced DB load under burst?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use cache-aside with per-key Redis lock: on miss, attempt SET key_lock NX PX 1000; if lock acquired, read from PostgreSQL, populate Redis with a TTL of 300s, then release lock; if not, wait briefly an",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use an atomic outbox pattern. In the HTTP handler's DB transaction, insert into audit_logs and into an outbox table for Kafka, then commit. Include correlation_id, user_id, action, endpoint, and times",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Collect: HTTP commit rate, PG replication lag, Kafka consumer lag, sink error counts, and ES refresh lag. Steps: enable end-to-end hashes per event_id, implement idempotent sink (UPSERT in ES, dedup i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Redis-backed token bucket in API middleware. Key rate_limit:{client}:{path} holds tokens and last_refill_ts. A Lua script atomically refills tokens based on elapsed time and consumes one per req",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nCache stampede is a practical beginner risk when caches expire and many requests hit the DB simultaneously. This tests understanding of cache-aside patterns, per-key locking, TTL tuning, and simple load-testing strategies.\n\n## Key Concepts\n\n- Cache-aside pattern\n- Per-key locking (Redis SET NX)\n- TTL tuning and cache warming\n- Observability: cache hit rate vs DB load\n\n## Code Example\n\n```javascript\nasync function getFromCacheOrDb(key) {\n  const cacheVal = await redis.get(key)\n  if (cacheVal) return JSON.parse(cacheVal)\n\n  const lockKey = `lock:${key}`\n  const hasLock = await redis.set(lockKey, '1', { NX: true, PX: 1000 })\n  if (hasLock) {\n    const data = await db.query('SELECT * FROM items WHERE id = $1', [key])\n    await redis.set(key, JSON.stringify(data.rows[0]), { EX: 300 })\n    await redis.del(lockKey)\n    return data.rows[0]\n  } else {\n    await sleep(50)\n    return getFromCacheOrDb(key)\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test race conditions in this flow?\n- What potential pitfalls exist with long-lived locks and how would you mitigate them?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-15",
        "questionId": "q-21945",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and a Snowflake sink experiences data lag in dashboards during peak load. Describe a concrete debugging plan to determine whether the lag stems from HTTP throughput, DB writes, Kafka publish, or Snowflake ingest, including exact metrics to collect, sampling strategy, and concrete fixes (transactional outbox, idempotent sinks, Snowpipe tuning, streaming vs micro-batching)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instrument end-to-end latency (p95/p99 for HTTP, DB write, Kafka publish, and Snowflake ingest), backlog, and consumer lag per partition; run a 1-hour canary with feature flags; apply transactional ou",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "End-to-end exactly-once: implement transactional outbox for HTTP->DB writes; publish from outbox to Kafka in the same DB transaction; make Kafka consumers idempotent and store processed_id in PostgreS",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument latency by layer: HTTP handler, MongoDB query, Vault fetch. Enable Vault credential caching with a TTL, reuse tokens, and reduce per-request Vault calls. Add a MongoDB index on the query pa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly plan to add bulk updates to a CNPA stack without changing the public API. Enforce per-record idempotency keys, validate and order items, perform a single transactional DB write using PostgreSQ…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess practical debugging of cross-system data pipelines under load, requiring end-to-end observability and concrete fixes across HTTP, DB, messaging, and analytics sinks.\n\n## Key Concepts\n\n- end-to-end latency across CNPA stages (HTTP → Postgres → Kafka → Snowflake)\n- observability: per-stage metrics, backlog, and lag\n- reliability patterns: transactional outbox, idempotent sinks, deduplication\n- ingestion tuning: Kafka batching, Snowpipe vs streaming ingest\n\n## Code Example\n\n```sql\n-- sample query to monitor end-to-end latency components across recent window\nSELECT percentile_cont(0.95) WITHIN GROUP (ORDER BY latency_ms) AS p95_latency\nFROM latency_metrics\nWHERE ts >= NOW() - INTERVAL '5 minutes';\n```\n\n## Follow-up Questions\n\n- How would you validate a fix without regressing other services?\n- What indicators would trigger rollback during canary rollout?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-16",
        "questionId": "q-1851",
        "question": "In a CNPA stack: HTTP API ingests events, writes to PostgreSQL, and publishes to Kafka. A new enrichment step guarded by a feature flag calls a 3rd-party service. Design a concrete, end-to-end rollout plan that minimizes risk: per-tenant flag rollout, fallback behavior when the service is down, tracing with correlation IDs, circuit breaker, and backpressure; metrics to monitor (p50/p95 latency, error rate, backlog), rollback criteria, and validation steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a transactional outbox: write to main DB and an outbox table in the same transaction; publish via Kafka with an idempotent producer and per-event keys; assign stable, monotonic IDs (e.g., UUID v7)",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Roll out the enrichment step with a per-tenant feature flag. Make enrichment asynchronous with a bounded retry queue and a fallback that returns un-enriched events if the external service is down. Pro",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement end-to-end sequence IDs and transactional outbox. Propagate a per-user sequence from HTTP to DB and to Kafka; use an outbox table to atomically commit DB writes and event payloads; enable Ka",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement the outbox pattern with Kafka transactions and consumer deduplication. On HTTP write, wrap the DB write and an outbox insert in one PostgreSQL transaction; a publisher uses a Kafka transacti",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of safe feature rollouts and resilience when integrating external services.\n\n## Key Concepts\n- Feature flags and per-tenant rollout\n- Async enrichment and fallbacks\n- End-to-end tracing and correlation IDs\n- Circuit breaker and backpressure\n- Observability metrics and rollback criteria\n\n## Code Example\n```javascript\n// pseudo-code illustrating flag check and fallback\n```\n\n## Follow-up Questions\n- How would you handle partial failures across tenants during rollback?\n- What metrics and dashboards would you add to validate a successful rollout?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-17",
        "questionId": "q-16517",
        "question": "**CNPA Live Schema Migration Under Peak Load**\n\nIn a CNPA stack with HTTP API, PostgreSQL, Kafka, Redis, and a downstream search index, plan a live schema evolution: adding a new optional field 'preferred_language' to user profiles and ensure it is stored in Postgres, published to Kafka, cached in Redis, and surfaced to the search index without downtime. Describe rollout strategy, compatibility guarantees, migration steps, canary criteria, and rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan a live, backward-compatible migration adding 'preferred_language' with default 'en', coordinate DB ALTER TABLE with minimal lock, publish an idempotent Kafka update, populate Redis in a canary sh",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement the Outbox pattern combined with Kafka transactions. In a single database transaction, write the HTTP API result to its primary table and insert a corresponding row into the Outbox table (event_id, payload, to…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument end-to-end latency (p95/p99 for HTTP, DB write, Kafka publish, and Snowflake ingest), backlog, and consumer lag per partition; run a 1-hour canary with feature flags; apply transactional ou",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Isolate by tenant end-to-end: DB shard by tenant_id (schemas or RLS); Kafka topic-per-tenant with quotas and per-producer ACLs; Redis namespace per tenant with TTLs; API enforces tenant_id in all writ",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to coordinate cross-service schema evolution with zero downtime and measurable rollback.\n\n## Key Concepts\n\n- Schema evolution compatibility (backward/forward)\n- Canary rollout and feature-flag control\n- Atomicity across DB write and Kafka publish\n- Cache refresh and cache invalidation in Redis\n- Search index propagation\n- Observability and rollback criteria\n\n## Code Example\n\n```javascript\n// Pseudo-migration flow (high level, not production-ready)\nasync function migrateUserProfile(user){\n  // 1) DB: add default column if not exists (non-blocking)\n  // 2) DB: upsert user with new field\n  // 3) Kafka: publish updated user event transactionally\n  // 4) Redis: refresh cache for affected user\n  // 5) Search index: enqueue index update\n}\n```\n\n## Follow-up Questions\n\n- What are canary rollout criteria and rollback triggers?\n- How would you test for backward/forward compatibility and data consistency?\n- How to measure migration impact (latency, errors, cache miss rate)?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-18",
        "questionId": "q-20980",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, Redis, and a data lake sink processes PII. A regulatory requirement demands real-time PII masking at ingestion and an immutable audit trail, with downstream services never emitting raw PII. Design a concrete plan to implement end-to-end privacy controls: where masking occurs, how to store masked vs raw data, how to build a tamper-evident audit log, and how you’d verify privacy guarantees under peak load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Redis-based lock and request coalescing pattern. On a cache miss, acquire a distributed lock using NX with a short TTL—only the lock holder queries PostgreSQL and repopulates the cache; all other requests wa…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I’d implement a backward-compatible, dual-write migration: 1) add new nullable columns and evolve code to write to both old and new schemas; 2) backfill the new fields asynchronously; 3) route reads t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement masking at the HTTP edge and during Kafka ingestion. Publish masked events downstream while storing only hashed PII in PostgreSQL. Maintain an immutable, tamper-evident audit log in a separate Kafka topic with…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Redis-based per-key token bucket with an atomic Lua script to reserve tokens on each request, returning 429 when empty. Implement a middleware that calls Redis to acquire a token, optionally all",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes privacy-by-design in a CNPA pipeline, ensuring PII is never exposed downstream while maintaining auditable history. It tests end-to-end thinking from ingress to sink under peak load.\n\n## Key Concepts\n- **Edge masking**: Apply PII masking at the HTTP gateway before data enters the system\n- **Dual storage**: Masked/hashed data in PostgreSQL for downstream consumption; raw PII in a secure vault (encrypted, access-controlled) for regulatory needs\n- **Tamper-evident logging**: Use cryptographic hashing (e.g., SHA-256) with chain integrity—each log entry includes the previous entry's hash, creating an immutable chain\n- **Kafka audit trail**: Dedicated topic with write-only semantics, storing digest signatures\n- **Data lake compliance**: Sink connector applies masking policies before persisting to lake storage\n\n## Verification Under Load\n- **Privacy regression tests**: Automated tests that scan outputs for PII leakage\n- **Load testing**: Validate masking latency doesn't become a bottleneck; use asynchronous processing if needed\n- **Chaos engineering**: Inject faults to ensure audit log integrity checksums still pass\n- **Data lineage tracing**: Verify each record's audit chain from ingestion to sink\n\n## Code Example\n```javascript\nfunction maskPII(record) {\n  const masked = { ...record };\n  if (masked.email) masked.email = hash(masked.email);\n  if (masked.phone) masked.phone = hash(masked.phone);\n  return masked;\n}\n\nfunction appendAuditLog(record, prevHash) {\n  const entry = { record, timestamp: Date.now(), prevHash };\n  entry.hash = sha256(JSON.stringify(entry));\n  kafka.send('audit-log', entry);\n  return entry.hash;\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-19",
        "questionId": "q-3865",
        "question": "CNPA stack where an HTTP API writes to Postgres, publishes to Kafka, and a Redis cache backs a downstream dashboard. Latency telemetry sometimes misses spans across Kafka and Redis, breaking traceability. Design a concrete debugging plan to verify trace propagation across HTTP, Kafka, and Redis, pinpoint where spans disappear, and implement fixes with concrete metrics and validation steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable end-to-end tracing (OpenTelemetry), propagate traceparent across HTTP, Kafka messages, and Redis operations; instrument Kafka producer/consumer and Redis clients. Set 100% sampling during test",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Active-active across Regions A and B with Kafka MirrorMaker 2 and PostgreSQL logical replication. Use an outbox to coordinate writes, enforce idempotent consumers, and track causal offsets. Route read",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant token buckets in Redis with a Lua script; enforce at HTTP ingress and in-app, refilling at rate R. If tokens exist, proceed; else return 429 and queue via an outbox. Use PostgreSQ",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I’d implement a backward-compatible, dual-write migration: 1) add new nullable columns and evolve code to write to both old and new schemas; 2) backfill the new fields asynchronously; 3) route reads t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to diagnose tracing gaps across asynchronous boundaries and storage layers, a common production issue in CNPA stacks. Tests practical instrumentation choices, propagation formats, and concrete validation steps.\n\n## Key Concepts\n\n- End-to-end distributed tracing across HTTP, Kafka, Redis\n- Trace context propagation (traceparent, baggage) across boundaries\n- Instrumentation of HTTP handlers, Kafka producers/consumers, Redis clients\n- Validation, sampling strategies, and canary rollout\n\n## Code Example\n\n```javascript\n// Example: attaching trace context to Kafka message headers\nproducer.send({ topic, value, headers: { traceparent }})\n```\n\n## Follow-up Questions\n\n- How would you verify propagation with a single 60-second test window?\n- Which metrics indicate complete cross-service traces and how would you alert on gaps?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-20",
        "questionId": "q-9439",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis now needs a per-endpoint feature flag to enable a read-through cache path for hot endpoints during peak hours. Describe a beginner-friendly plan to implement the flag, rollout strategy, testing, and rollback—include config source, cache coherence, write-through invalidation, and concrete metrics to verify improvement?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement tenant isolation end-to-end: PostgreSQL RLS policies keyed by tenant_id, with tenant-scoped indexes and FK constraints; Redis namespaces per tenant with strict ACLs; Kafka with tenant-bound",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement the Outbox pattern combined with Kafka transactions. In a single database transaction, write the HTTP API result to its primary table and insert a corresponding row into the Outbox table (event_id, payload, to…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a backward-compatible migration and transactional outbox to ensure zero-downtime propagation of a new optional field. Add a nullable JSONB column 'preferences' in Postgres; ship a Kafka schema whe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-endpoint flag in a central config service with short TTL and a client-side cache. Roll out via canary (5%), then 25%. If enabled, API checks the flag, reads from Redis; on miss fetches",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nIntroduces feature flags and cache strategies with safe rollout and rollback, a practical skill for production CNPA systems.\n\n## Key Concepts\n\n- Feature flags and governance\n- Read-through cache pattern with Redis\n- Cache invalidation/write-through on updates\n- Canary rollout and observable rollback\n\n## Code Example\n\n```javascript\n// Pseudo-code: flag check and cached read\nfunction getEndpointData(endpoint){\n  if (config.getFlag('cache:' + endpoint)){\n    const v = cache.get(endpoint)\n    if (v) return v\n    const data = db.query(endpoint)\n    cache.set(endpoint, data, TTL)\n    return data\n  }\n  return db.query(endpoint)\n}\n```\n\n## Follow-up Questions\n\n- How would you test cache invalidation under concurrent writes?\n- How would you handle a cache stampede or Redis outage?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-21",
        "questionId": "q-6177",
        "question": "In a CNPA stack (HTTP API, PostgreSQL, Kafka, Redis) that processes sensitive telemetry, design an end-to-end signing and verification workflow to guarantee integrity and non-repudiation from ingress to storage and cache. Specify algorithms, key management, where verification occurs, and how you validate correctness under request retries, partial failures, and network partitions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement OpenTelemetry across all services in the CNPA stack. Create a root span for each HTTP request and child spans for Postgres queries, Kafka producer/consumer operations, and Redis commands. Propagate trace conte…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement tenant isolation at the API boundary by validating tenant_id early, enable PostgreSQL Row Level Security with a tenant policy, switch Redis to per-tenant namespaces, and tag all Kafka messages with tenant head…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement end-to-end signing: sign each HTTP body with a per-tenant key (ECDSA P-256) and attach a KeyID in a custom header; include a canonical payload (timestamp, nonce, body hash). Validate signatu",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Instrument end-to-end latency (p95/p99 for HTTP, DB write, Kafka publish, and Snowflake ingest), backlog, and consumer lag per partition; run a 1-hour canary with feature flags; apply transactional ou",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design a secure, auditable CNPA pipeline with cross-service signing, key lifecycle, and failure-tolerant verification.\n\n## Key Concepts\n\n- End-to-end signing\n- Key management and rotation\n- Cross-service verification\n- Idempotency and audit trails\n- Failure-mode testing\n\n## Code Example\n\n```javascript\n// sign payload\nfunction sign(payload, key) { /* crypto sign */ }\n\n// verify signature\nfunction verify(payload, sig, key) { /* crypto verify */ }\n\n// pseudo-flow\n```\n\n## Follow-up Questions\n\n- How would you test key rotation without downtime?\n- How would you handle clock skew and replay protection?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-22",
        "questionId": "q-17405",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis introduces a new ingestion enrichment path that calls an external ML service to augment events during HTTP intake. Design a production-safe rollout plan: backward/forward-compatible schemas, feature flags, idempotent enrichment, backfill strategy, canary criteria, and observability to verify latency and correctness under peak load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an outbox table in Postgres, publish to Kafka within a DB transaction, and build idempotent sinks in Redis keyed by (entity_id, event_id). Add a reconciliation pass that confirms all publish",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enable enrichment via a feature flag with a canary rollout. Ensure idempotent enrichment by deduplicating on (event_id, enrichment_version) and write enriched payload to a separate Kafka topic while p",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Zero-downtime, backward-compatible migration for CNPA stack: add a nullable audit_reason column and a separate audit_events table in Postgres; emit a new Kafka schema version while producing both old",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Add nickname as an optional, nullable field to the PostgreSQL users table and Redis cache structure; extend Kafka user profile events with an optional nickname field using a backward-compatible Avro schema with default…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess practical rollout strategy for a new ingestion feature that touches HTTP intake, streaming, and external dependencies. Tests ability to design safe migrations, handle partial failures, and observe impact under load.\n\n## Key Concepts\n\n- Feature flags and canary rollout\n- Idempotent enrichment and deduplication\n- Backward/forward compatibility of schemas\n- Backfill and migration strategy\n- External service reliability (circuit breakers, timeouts) and observability\n\n## Code Example\n\n```javascript\n// Pseudo enrichment wrapper\nasync function enrich(event) {\n  if (!config.enrichmentEnabled) return event;\n  // dedupe by id and version\n  const key = `${event.id}:${event.enrichmentVersion||1}`\n  if (cache.dedup.get(key)) return event;\n  const enriched = await mlEnrich(event);\n  // publish to enriched topic\n  kafka.produce('events.enriched', enriched)\n  cache.dedup.set(key, true)\n  return enriched;\n}\n```\n\n## Follow-up Questions\n\n- How would you test the canary rollout under peak load and roll back safely?\n- How would you ensure exactly-once semantics across HTTP, Kafka, and the enrichment path while keeping backward compatibility?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-23",
        "questionId": "q-24676",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis under burst load shows inconsistent analytics counters. Design a concrete experiment and plan to guarantee end-to-end exactly-once processing in writes and downstream sinks. Include steps, concrete changes (outbox pattern, Kafka transactions, idempotent sinks), metrics to collect, sampling, and how you'd validate correctness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by capturing pg_locks and pg_stat_activity for the hot table and migration; perform wait analysis and lock mode verification; monitor HTTP path latency and Kafka sink backlog; implement online migration using a sh…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement the transactional outbox pattern: write to the primary table and an outbox row in a single SQL transaction, then publish those events to Kafka within a Kafka transaction. Use a Redis-based deduplication key to…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a transactional outbox: writes to Postgres enqueue Kafka; Redis keyed per product with per-segment TTL and STALE-WHILE-REVALIDATE; canary with feature flag, backfill in batches, monitor cach",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Redis-based lockout flag with TTL (e.g., 15 minutes). Sign-in checks first read Redis; if locked, return 429. If unlocked, verify policy in Postgres and on failure publish a Kafka event wi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests understanding of end-to-end exactly-once semantics across HTTP, database, and streaming sinks, along with practical validation under burst load conditions.\n\n## Key Concepts\n- **Transactional Outbox**: Ensuring atomicity between business data and event persistence\n- **Kafka Transactions**: Providing exactly-once semantics at the producer level\n- **Idempotent Sinks & Redis Dedup Keys**: Preventing duplicate processing in downstream consumers\n- **Read-Model Consistency**: Validating that materialized views match source data\n- **Replay Validation**: Verifying correctness by replaying events from offset zero\n- **Backfill Strategies**: Rebuilding analytics counters when inconsistencies are detected\n- **Observability Metrics**: Latency histograms, processing rates, duplicate counts, and lag indicators\n\n## Concrete Experiment Design\n\n### 1. Instrument the Pipeline\n- Add tracing headers (correlation IDs) across HTTP request, DB write, and Kafka producer\n- Tag each event with a unique idempotency key stored in Redis with a TTL\n\n### 2. Load Testing\n- Use k6 or Gatling to simulate burst load (10k+ requests/second)\n- Inject random failures at Kafka broker and consumer to test recovery\n\n### 3. Metrics to Collect\n- End-to-end latency (HTTP request to sink completion)\n- Duplicate event count in sinks (compare unique keys vs. total events)\n- Kafka consumer lag and rebalance frequency\n- Redis dedup cache hit/miss ratio\n- Outbox table processing latency\n\n### 4. Validation\n- Run a consistency check: query primary table, compare aggregated counts against sink metrics\n- Perform a replay test: reset consumer offset to beginning and verify no data corruption or duplicate writes\n- Use a checksum comparison between source table and downstream read model\n\n## Code Example\n```java\ntry (Connection conn = dataSource.getConnection()) {\n  conn.setAutoCommit(false);\n  \n  // Insert order into primary table\n  PreparedStatement psOrder = conn.prepareStatement(\n    \"INSERT INTO orders (id, amount, created_at) VALUES (?, ?, ?)\",\n    Statement.RETURN_GENERATED_KEYS\n  );\n  psOrder.setString(1, orderId);\n  psOrder.setBigDecimal(2, amount);\n  psOrder.setTimestamp(3, Timestamp.from(Instant.now()));\n  psOrder.executeUpdate();\n  \n  // Insert into outbox atomically\n  PreparedStatement psOutbox = conn.prepareStatement(\n    \"INSERT INTO outbox (aggregate_id, event_type, payload, created_at) VALUES (?, ?, ?, ?)\"\n  );\n  psOutbox.setString(1, orderId);\n  psOutbox.setString(2, \"ORDER_CREATED\");\n  psOutbox.setString(3, jsonPayload);\n  psOutbox.setTimestamp(4, Timestamp.from(Instant.now()));\n  psOutbox.executeUpdate();\n  \n  conn.commit();\n}\n\n// Kafka producer within transaction\ntry (KafkaProducer<String, String> producer = new KafkaProducer<>(producerProps)) {\n  producer.initTransactions();\n  producer.beginTransaction();\n  \n  List<OutboxRecord> records = outboxRepository.fetchUnpublished();\n  for (OutboxRecord record : records) {\n    producer.send(new ProducerRecord<>(\"orders-events\", record.getAggregateId(), record.getPayload()));\n    redisDedup.set(\"dedup:\" + record.getAggregateId(), \"1\", Duration.ofDays(7));\n  }\n  \n  producer.commitTransaction();\n}\n```\n\n## Guaranteeing Exactly-Once\n1. **DB Layer**: Single atomic transaction combines business write + outbox insert\n2. **Kafka Layer**: Transactional producer ensures outbox events are only committed after successful DB commit\n3. **Consumer Layer**: Redis dedup key prevents duplicate sink writes\n4. **Sink Layer**: Upsert operations with idempotency keys ensure at-least-once becomes exactly-once",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-24",
        "questionId": "q-6981",
        "question": "Design a security-focused plan for a multi-tenant CNPA stack (HTTP API, PostgreSQL, Kafka, Redis) where an admin export job could leak cross-tenant data. Propose concrete changes to enforce tenant isolation: PostgreSQL Row-Level Security, tenant-scoped queries, Kafka per-tenant topics and ACLs, Redis key namespaces, and end-to-end audit logging. Include a pragmatic test plan with metrics, a rollback strategy, and success criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outbox pattern in PostgreSQL: in the same transaction, insert (session_id, event_type, payload, created_at) into sessions and into an outbox table. Kafka: transactional producer with a unique 'transac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enable Kafka transactions (transactional.id) with acks=all and idempotent producers; commit offsets only after writes to Postgres and Redis succeed; Postgres logical replication with region-based part",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement strict per-tenant isolation across the CNPA stack: enable PostgreSQL Row-Level Security with a tenant_id context, scope all HTTP DB joins to the tenant, enforce Kafka topic ACLs and per-tena",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement end-to-end exactly-once processing through a coordinated approach: wrap database writes and Kafka publishing in atomic transactions using Kafka's transactional producers with exactly-once semantics. Deploy the…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to enforce strict per-tenant isolation and auditing in a CNPA stack during sensitive admin operations. It also probes multi-system consistency and rollback behavior under load.\n\n## Key Concepts\n- PostgreSQL Row-Level Security\n- Tenant-scoped queries\n- Kafka ACLs and per-tenant topics\n- Redis namespace prefixes\n- End-to-end audit logging and export breadcrumbs\n\n## Code Example\n```javascript\n// illustrative SQL policy (for reference)\nCREATE POLICY tenant_access ON users\nUSING (tenant_id = current_setting('app.tenant_id')::int);\n```\n\n## Follow-up Questions\n- How would you test for cross-tenant data access during a load test?\n- What metrics indicate isolation failure and how would you mitigate?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-16T07:27:52.098Z",
    "version": 1
  }
]