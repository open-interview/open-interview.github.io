[
  {
    "id": "test-cnpa",
    "channelId": "cnpa",
    "channelName": "Cnpa",
    "title": "Cnpa Knowledge Test",
    "description": "Test your understanding of Cnpa with 25 questions.",
    "questions": [
      {
        "id": "cnpa-q-0",
        "questionId": "q-22352",
        "question": "A CNPA stack with an HTTP API, PostgreSQL, Kafka, and a downstream analytics sink shows end-to-end latency spikes under load. Design a beginner-friendly plan to implement end-to-end tracing with OpenTelemetry: where to instrument (HTTP server, DB driver, Kafka producer/consumer, analytics SDK), how to propagate trace context (traceparent), sampling, export, and verification steps to confirm full path coverage and bottleneck identification?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement end-to-end observability by propagating a unique request_id across all components in your CNPA stack. Add lightweight middleware to inject the request_id in HTTP headers, thread it through SQL queries via a co…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument all hops with OpenTelemetry: HTTP server middleware starts a root span; DB driver and queries are wrapped; Kafka producer/consumer spans are created and linked; downstream analytics message",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a Redis-based lock and request coalescing pattern. On a cache miss, acquire a distributed lock using NX with a short TTL—only the lock holder queries PostgreSQL and repopulates the cache; all other requests wa…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Collect end-to-end latency, queue depth, and replica lag; enable slow query logs and run explain on top slow queries; verify index coverage with compound indexes; apply index hints or add a composite",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTo assess practical ability to diagnose cross-service latency using tracing in CNPA stacks, a common real-world issue for beginners transitioning to production.\n\n## Key Concepts\n- End-to-end tracing\n- OpenTelemetry instrumentation and propagating trace context\n- Span linking across HTTP, DB, Kafka, and analytics\n\n## Code Example\n```python\n# Pseudo\nfrom opentelemetry import trace\nspan = trace.get_tracer(\"cnpa.http\").start_span(\"HTTP /items\")\nwith trace.use_span(span, end_on_exit=True):\n    resp = http_call(\"/items\")\n    db_cursor.execute(\"SELECT ...\")\n    producer.send(\"events\", value=payload, headers={\"traceparent\": span.get_span_context().trace_id})\n```\n\n## Follow-up Questions\n- How would you adjust sampling at peak load?\n- How do you verify tracing covers failures and retries?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-1",
        "questionId": "q-14334",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis experiences data consistency issues under peak load: user_signup events may diverge between DB and Kafka. Design a beginner-friendly plan to guarantee end-to-end consistency, including concrete checks (FK constraints, idempotent producers/consumers, outbox pattern), minimal code changes, and validation steps under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant rate limiting and backpressure in a CNPA stack (HTTP API, PostgreSQL, Kafka, Redis) to prevent a single tenant from starving others. Use a Redis-based leaky-bucket per tenant (Lua",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument Redis with per-key TTLs and cache-hit metrics; log invalidations and Redis reads. Track Kafka lag and HTTP latency. Use canary: switch to cache-aside with 15–30s TTL; invalidate on Postgres",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument end-to-end tracing across HTTP, Redis, Postgres, and Kafka; collect p99 latency, Redis hit rate, DB query times, and Kafka publish latency under load. Reproduce with cold caches to observe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an end-to-end consistency solution using the transactional outbox pattern to reliably publish database changes to Kafka, enforce foreign key constraints and unique identifiers, enable idempotent producers and…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates understanding of data consistency challenges across CNPA stack components and the ability to design practical solutions for production environments.\n\n## Key Concepts\n- End-to-end consistency across HTTP API, PostgreSQL, Kafka, and Redis\n- Transactional outbox pattern for reliable event publication\n- Idempotent producers and consumers to handle retries safely\n- Foreign keys and unique constraints to preserve data integrity\n- Load testing validation with automated consistency checks and backfill procedures\n\n## Code Example\n```javascript\n// Pseudo-outbox implementation\nconst signupTransaction = await db.transaction(async (trx) => {\n  // Insert user with FK constraints\n  const user = await trx('users').insert(userData);\n  \n  // Create outbox record in same transaction\n  await trx('outbox').insert({\n    event_type: 'user_signup',\n    payload: { user_id: user.id, ...userData },\n    idempotency_key: generateUUID()\n  });\n  \n  return user;\n});\n```",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-2",
        "questionId": "q-19246",
        "question": "CNPA stack with an HTTP API, PostgreSQL, Redis, and Kafka adds a per-tenant rate-limiter feature driven by quotas stored in Postgres. During rollout, several tenants see sharp bursts of 429s and others stay underutilized. Propose a concrete, end-to-end plan to implement scalable quotas, ensure fair distribution under load, and verify no regressions in latency or availability, including data model changes, caching strategy, instrumentation, and rollback criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant quotas using a Redis-backed token bucket with an atomic Lua script. Derive tenant id from JWT; load quotas from Postgres and cache them in Redis with short TTL. On each request, L",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Two-phase purge: (1) in a single Postgres transaction, set pii_deleted_at and emit a tombstone to an outbox; (2) publish tombstones to a compacted Kafka topic and invalidate Redis keys via a purge eve",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant rate limiting and circuit breaking using a Redis-backed token bucket algorithm. Enforce limits at the HTTP edge, allocate quotas per tenant, and trip circuit breakers after a configurable failure th…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: add a nullable column customer_segment to Postgres; emit versioned payloads starting with version 2 that include the field; implement a short dual-write period to upgrade producers/consumers; ba",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical quota enforcement at scale, covering data modeling, caching, and safe rollback under skewed traffic.\n\n## Key Concepts\n- Per-tenant quotas and dynamic policy\n- Redis token bucket with atomic Lua script\n- Postgres as source of truth; Redis cache with TTL\n- End-to-end latency, 429 distribution, backpressure\n\n## Code Example\n```javascript\n// Pseudo: token check via Redis Lua script (illustrative)\nlocal tenant = KEYS[1]\nlocal now = tonumber(ARGV[1])\nlocal tokens = redis.call('GET', tenant .. ':tokens')\nif not tokens then return -1 end\nif tonumber(tokens) <= 0 then return 0 end\nredis.call('DECR', tenant .. ':tokens')\nreturn 1\n```\n```\n// Rollback: disable tenant quotas by removing Lua script and falling back to global quota\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-3",
        "questionId": "q-15735",
        "question": "In a CNPA stack with an HTTP API, PostgreSQL, and Kafka, a new enrichment microservice consumes events from 'events.raw', calls an external rate-limited API for enrichment, and writes to 'events.enriched' and to Postgres. Under peak load, external lookups throttle latency. Propose a concrete plan to keep latency bounded: backpressure, local caching, idempotent processing, Kafka producer/consumer tuning, and a validation strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement OpenTelemetry tracing across all CNPA layers. Instrument HTTP handler to start a span, wrap DB calls with SQL instrumentation, annotate Kafka producer/consumer spans, and trace Redis reads.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use asynchronous enrichment with backpressure and a per-key rate limiter; add a two-tier cache (in-process TTL plus Redis) to reduce external lookups; make writes idempotent (upsert in Postgres) and u",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use additive schema changes: make the field optional in the database and Kafka payload, version the messages, implement dual writes during a limited window, and backfill historic records with a sensible default.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "End-to-end exactly-once: implement transactional outbox for HTTP->DB writes; publish from outbox to Kafka in the same DB transaction; make Kafka consumers idempotent and store processed_id in PostgreS",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical, end-to-end handling of external API bottlenecks within a CNPA stack, including backpressure, caching, and idempotent semantics across HTTP, DB, and Kafka. It tests decision-making under load and how to validate impact.\n\n## Key Concepts\n- Backpressure and rate limiting\n- Caching strategies (TTL, Redis)\n- Idempotent writes across DB and Kafka\n- Kafka producer tuning (acks, in-flight, idempotence)\n- End-to-end validation under peak load\n\n## Code Example\n```javascript\nasync function enrich(event) {\n  if (!cache.has(event.key)) {\n    const data = await externalLookup(event.key)\n    cache.set(event.key, data)\n    // enqueue enriched event and upsert to Postgres\n  }\n}\n```\n\n## Follow-up Questions\n- How would you measure tail latency under burst traffic?\n- How would you handle cache stampede and external API faults during peak load?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-4",
        "questionId": "q-24400",
        "question": "tail latency spikes in a CNPA stack with an HTTP API, PostgreSQL, Kafka, and Redis. Design a beginner-friendly plan to implement end-to-end tracing using OpenTelemetry across the HTTP API, DB driver, Kafka producer, and Redis client, including how to propagate trace context, sampling strategy, essential span attributes, and where to ship traces (Jaeger/Tempo)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a transactional outbox in Postgres to atomically persist the update and publish a Kafka event. Build an idempotent Elasticsearch sink keyed by (user_id, version) that upserts docs, plus Redi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement end-to-end OpenTelemetry tracing across HTTP API, PostgreSQL, Kafka, and Redis. Propagate trace context from HTTP header via all calls; instrument DB driver, Kafka producer, and Redis client",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design an idempotent event processing path using the transactional outbox pattern combined with Kafka's exactly-once semantics. Assign a globally unique event_id (UUID v4 or snowflake) to each event. Within a single Pos…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: publish each HTTP event to Kafka using a transactional producer and include a unique event_id. On the sink, implement an upsert into PostgreSQL keyed by event_id and a dedup table storing proces",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nGauges practical observability skills; beginner-friendly but shows depth in distributed tracing across CNPA components.\n\n## Key Concepts\n- OpenTelemetry instrumentation and propagation\n- Span attributes and sampling\n- Backends: Jaeger/Tempo integration\n- Verification via synthetic load and trace analysis\n\n## Code Example\n```javascript\n// Pseudo: add otel initialization, inject context in HTTP handlers, and DB/Kafka/Redis clients\n```\n\n## Follow-up Questions\n- How would you adjust sampling for non-test environments?\n- What metrics would you correlate with tail latency?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-5",
        "questionId": "q-17936",
        "question": "CNPA stack (HTTP API, PostgreSQL, Kafka, Redis) must add a new optional field (e.g., user_tier) to user events without downtime. Design a schema-evolution plan using Avro + Schema Registry, detailing backward/forward compatibility, migration phases, data backfill, producer/consumer changes, and verification under peak load that ensures no dropped events and consistent reads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Zero-downtime, backward-compatible migration for CNPA stack: add a nullable audit_reason column and a separate audit_events table in Postgres; emit a new Kafka schema version while producing both old",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a backwards-compatible Avro schema by adding `user_tier` as an optional string field with a default value of `null`. This ensures that old records can be parsed correctly while new producers can emit the field. Impl…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a transactional outbox to publish 'active_user_sessions' to Kafka in the same ACID as the PostgreSQL write; add a backward-compatible schema, a changelog topic, and a feature flag to switch reads",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a safe rollout using a backward-compatible schema: add a nullable customer_segment field in the event, publish both schemas via a Schema Registry, dual-write to ensure old and new sinks read b",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests advanced knowledge of schema evolution, cross-service coordination, and zero-downtime migration strategies in distributed systems. It evaluates understanding of how to evolve data contracts in a CNPA stack (HTTP API, PostgreSQL, Kafka, Redis) while maintaining system reliability and data consistency.\n\n## Key Concepts\n\n- Avro schema evolution and Schema Registry for managing schema versions\n- Backward and forward compatibility in distributed data pipelines\n- Zero-downtime migrations with phased rollout strategies\n- Data backfill techniques and drift detection\n- Producer/consumer tolerance for handling both old and new field representations\n- Circuit breaking and canary deployments for safe migration\n\n## Code Example\n\n```javascript\n// Avro schema excerpt (new field with default)\n{\n  \"type\": \"record\",\n  \"name\": \"UserEvent\",\n  \"fields\": [\n    {\"name\": \"userId\", \"type\": \"string\"},\n    {\"name\": \"event\", \"type\": \"string\"},\n    {\n      \"name\": \"user_tier\",\n      \"type\": [\"null\", \"string\"],\n      \"default\": null,\n      \"doc\": \"User tier level (free, premium, enterprise)\"\n    }\n  ],\n  \"doc\": \"User event schema with optional user_tier field\"\n}\n\n// Producer: Emit events with optional field\nconst produceUserEvent = async (userId, event, userTier = null) => {\n  const event = {\n    userId,\n    event,\n    user_tier: userTier  // Optional; defaults to null for old consumers\n  };\n  await kafkaProducer.send({\n    topic: 'user-events',\n    messages: [{ value: avroSerializer.serialize(event) }]\n  });\n};\n\n// Consumer: Handle both old and new schemas gracefully\nconst consumeUserEvent = async (message) => {\n  const event = avroDeserializer.deserialize(message.value);\n  // Safely access optional field with fallback\n  const tier = event.user_tier ?? 'free';\n  console.log(`User ${event.userId} at tier: ${tier}`);\n};\n```",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-6",
        "questionId": "q-26508",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis is failing an end-to-end trace during peak load due to missing traces in downstream services. Propose a concrete debugging plan to implement end-to-end tracing across services, including how to instrument HTTP handlers, DB clients, Kafka producers/consumers, Redis calls, sampling strategy, and a verification plan with synthetic load; specify what metrics to collect and how to validate traces are complete?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable OpenTelemetry in the HTTP API and propagate a trace_id from requests through PostgreSQL and Kafka. Add a span per HTTP handler, a db span around SQL calls, and a producer span for each Kafka pu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a staged migration with a feature flag: keep old schema active while introducing new Avro schema and a new Postgres column; enable dual-write to both old and new Kafka topics and to a shadow",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument HTTP API, Postgres, Kafka, and Redis with OpenTelemetry; propagate trace IDs through HTTP headers and Kafka message headers; add spans for HTTP, each DB query, publish/consume, and Redis re",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-request correlation_id (UUID) passed via HTTP header X-CNPA-Trace-Id and propagated as span IDs through HTTP, DB, Kafka, and Redis. Log at HTTP with timestamp, service, event_id, corre",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to implement robust observability across CNPA components, ensuring end-to-end visibility and debugability under load.\n\n## Key Concepts\n- Distributed tracing\n- OpenTelemetry propagation\n- Sampling strategies\n- Instrumentation across HTTP API, Postgres, Kafka, Redis\n\n## Code Example\n```javascript\n// Pseudo-setup for OpenTelemetry initialization and propagation\n```\n\n## Follow-up Questions\n- How would you measure trace completeness in high-cardinality requests?\n- How would you mitigate performance overhead of tracing?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-7",
        "questionId": "q-5231",
        "question": "CNPA stack with an HTTP API, PostgreSQL, and Kafka; dashboards show missing events downstream after deployment. Provide a beginner-friendly, concrete plan to determine where end-to-end causality is broken using distributed tracing (OpenTelemetry). Include: how to propagate trace context across HTTP, DB, and Kafka; what spans to add and where; sampling strategy; how to verify trace completeness and deduplicate traces; and concrete, minimal code changes to implement this plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement end-to-end exactly-once processing through a coordinated approach: wrap database writes and Kafka publishing in atomic transactions using Kafka's transactional producers with exactly-once semantics. Deploy the…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument a trace per HTTP request with spans for HTTP handler, DB insert, and Kafka publish; propagate the W3C traceparent header across API, DB driver, and Kafka producer; enable 100% sampling in s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a chaos experiment that simulates Region A going offline in a CNPA stack (HTTP API, PostgreSQL, Kafka, Redis, data lake). Inject latency, CPU pressure, and network partitions; monitor global p9",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an outbox table in PostgreSQL with columns: id (UUID), profile_id (UUID), event_type (VARCHAR), payload (JSONB), created_at (TIMESTAMP), processed_at (TIMESTAMP), and status (ENUM: pending/processed/failed). E…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEnd-to-end visibility across services helps beginners diagnose where causality breaks in CNPA pipelines, a common production issue after deployments.\n\n## Key Concepts\n\n- Distributed tracing across HTTP, DB, and messaging layers\n- Trace propagation (W3C traceparent) and context maintenance\n- Sampling strategies and exporters (OpenTelemetry, Jaeger/OTLP)\n- End-to-end verification with test injections and trace reconciliation\n\n## Code Example\n\n```javascript\n// Example using OpenTelemetry in Node.js\nconst { trace } = require('@opentelemetry/api');\nconst tracer = trace.getTracer('cnpa-api');\nasync function handle(req, res){\n  const span = tracer.startSpan('http_handler');\n  // perform DB insert and Kafka publish here\n  span.end();\n  res.end('ok');\n}\n```\n\n## Follow-up Questions\n\n- How would you verify trace propagation when a client library strips headers?\n- How would you spot and fix sampling bias that hides rare failures?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-8",
        "questionId": "q-17441",
        "question": "CNPA stack (HTTP API, PostgreSQL, Kafka, Redis) needs to add an optional user profile field 'preferred_language' with backward compatibility. Design a beginner-friendly rollout plan that preserves old clients, DB and Kafka schemas, and introduces the field safely. Include concrete steps for DB migration, Kafka schema evolution, HTTP API versioning/feature flags, tests, and rollback criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a zero downtime rollout: perform a backward-compatible DB migration (add nullable column, default NULL), deploy a dual Kafka schema window via a feature flag, and implement a dual-write path (ol",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enable enrichment via a feature flag with a canary rollout. Ensure idempotent enrichment by deduplicating on (event_id, enrichment_version) and write enriched payload to a separate Kafka topic while p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Postgres: ALTER TABLE users ADD COLUMN preferred_language VARCHAR(32) NULL; Kafka: update Avro schema with an optional field defaulting to null and enable backward/forward compatibility; HTTP: version",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement middleware that emits structured audit events to a Kafka audit topic and persists to a Postgres audit table; fields: action, user_id, resource, timestamp, correlation_id. Redact emails and p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to plan safe schema evolution across CNPA without breaking existing clients or data pipelines. Encourages concrete steps, risk assessment, and rollback criteria.\n\n## Key Concepts\n\n- Additive schema changes with backward compatibility\n- Schema Registry evolution for Kafka messages\n- API versioning and feature flag strategies\n- Safe migrations and rollback planning\n\n## Code Example\n\n```javascript\n-- Postgres\nALTER TABLE users ADD COLUMN preferred_language VARCHAR(32) NULL;\n\n-- Kafka (conceptual)\n// Update Avro/Schema Registry to include optional field: \"preferred_language\" default null\n// Producers set field if present; readers handle absence as null\n```\n\n## Follow-up Questions\n\n- How would you verify backward compatibility if a consumer lags behind schema updates?\n- What metrics indicate a successful rollout and when would you rollback?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-9",
        "questionId": "q-9713",
        "question": "In a CNPA stack (HTTP API, PostgreSQL, Kafka, Redis) at scale, design end-to-end data lineage and auditability for regulated data. Provide a concrete plan to: 1) attach a verifiable provenance trail to every user event, 2) perform compliant erasure while preserving immutable audit logs, 3) enable safe cross-service replay for debugging without exposing PII. Include steps, components, and metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pinpoint drift between event-time and processing-time windows. Check: 1) producer event_time vs stored event_time in Postgres; 2) Kafka record timestamps and any broker clock drift; 3) consumer window",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Attach a per-event correlation ID and include a cryptographic signature on all emitted events. Maintain an append-only audit ledger in PostgreSQL with a hash chain, and use Kafka with at-least-once de",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a beginner-friendly plan to ensure idempotent processing in a CNPA stack (HTTP API, PostgreSQL, Kafka, Redis) under retry storms. Describe where to deduplicate requests (e.g., client-provided request_id), how to…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument end-to-end tracing across HTTP, Redis, Postgres, and Kafka; collect p99 latency, Redis hit rate, DB query times, and Kafka publish latency under load. Reproduce with cold caches to observe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design robust data lineage and privacy controls across a CNPA stack, balancing auditability with performance. \n\n## Key Concepts\n- End-to-end provenance, correlation IDs, cryptographic signing\n- Immutable audit logs with hash chaining\n- Safe data erasure across HTTP, DB, cache, and message bus\n- Replayability with privacy safeguards and idempotent processing\n\n## Code Example\n```javascript\n// Pseudo: sign and publish event with provenance\nfunction publishEvent(event, secret) {\n  const sig = sign(JSON.stringify(event), secret)\n  const payload = { ...event, signature: sig }\n  producer.send({ topic: 'events', value: payload })\n}\n```\n\n## Follow-up Questions\n- How would you test the audit trail integrity end-to-end? \n- What metrics indicate successful erasure without breaking lineage?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-10",
        "questionId": "q-2450",
        "question": "CNPA stack: HTTP API writes to PostgreSQL and publishes to Kafka. A feature flag routes writes through a shadow sink and a shadow Kafka topic. When enabled, dashboards show delayed data and downstream consumers occasionally duplicate messages due to retries. Provide a concrete debugging plan to verify data correctness and latency impact, including idempotency, outbox, transactional boundaries, and rollback procedures; specify metrics, sampling, and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a policy engine with per-field scrubbing rules, implement request/response middleware to redact fields, enforce redaction on ORM writes and Kafka producers via value transformers, and configure",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Tag every incoming event with a stable event_id at ingress, propagate it via HTTP headers and Kafka message headers, write a lineage row in PostgreSQL (event_id, source, destination, transforms, timestamp), and emit a l…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Reproduce in staging with the feature flag on/off; enable full tracing across HTTP, DB, Kafka, and the shadow sink. Enforce correctness with a transactional outbox: commit writes to Postgres and the o",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a Redis-based lockout flag with TTL (e.g., 15 minutes). Sign-in checks first read Redis; if locked, return 429. If unlocked, verify policy in Postgres and on failure publish a Kafka event wi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical debugging under feature-flagged shadow paths, end-to-end data correctness, and rollback safety in CNPA at scale.\n\n## Key Concepts\n- End-to-end tracing across HTTP, Postgres, Kafka, and caches\n- Transactional outbox pattern and idempotent publishing\n- Shadow sinks, data drift detection, rollback procedures\n- Latency tail metrics and data reconciliation\n\n## Code Example\n```javascript\n// Example: basic transactional outbox write\nawait db.transaction(async (trx) => {\n  await trx.insert({table:'events', data})\n  await trx.insert({table:'outbox', payload: data, event_id})\n});\n// publish from outbox with idempotent key\n```\n\n## Follow-up Questions\n- How would you test exactly-once semantics under replay?\n- How would you monitor drift between Postgres and the shadow sink?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-11",
        "questionId": "q-27982",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and an audit requirement: every API request must produce a durable, immutable audit log entry with an audit_id that travels end-to-end across components. Design a beginner-friendly plan to implement this across HTTP layer, DB, and Kafka without adding significant latency. Include where to generate the audit_id, how to pass it via HTTP and Kafka, schema ideas, tests, and how you'd verify end-to-end traceability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an idempotency key on the HTTP endpoint; persist the key with a timestamp in PostgreSQL and purge old keys; add a unique constraint on (idempotency_key) to block duplicates; use an idempoten",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Generate audit_id at the HTTP ingress, pass via headers, store a row in PostgreSQL with audit_id, and attach the same id to the Kafka message headers and downstream data lake writes. Use a minimal aud",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Add a new loyalty_points int to Kafka events and Postgres, using Avro schemas with schema registry for backward/forward compatibility; deploy a read-time flag to route reads to either old or new schem",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Collect end-to-end latency, queue depth, and replica lag; enable slow query logs and run explain on top slow queries; verify index coverage with compound indexes; apply index hints or add a composite",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAuditing across a CNPA stack is critical for compliance; beginners should show end-to-end thinking and minimal latency impact.\n\n## Key Concepts\n- End-to-end provenance via a single audit_id\n- Propagation through HTTP headers and Kafka message headers\n- Lightweight audit table and minimal schema to avoid latency\n- End-to-end tests that verify trace consistency\n\n## Code Example\n```javascript\n// Generate and propagate audit_id in Node.js\nfunction handleRequest(req, res) {\n  const auditId = req.headers['x-audit-id'] || generateAuditId();\n  res.setHeader('x-audit-id', auditId);\n  // log audit locally\n  logAudit({ auditId, endpoint: req.path, userId: req.user?.id, ts: Date.now() });\n  // forward to DB and Kafka with the header\n  db.insert({ audit_id: auditId, ...req.body });\n  kafka.publish('events', { payload: req.body, headers: { 'x-audit-id': auditId } });\n  res.end('ok');\n}\n```\n\n## Follow-up Questions\n- How would you test end-to-end audit trace under load? \n- What metrics would you monitor for audit latency and completeness?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-12",
        "questionId": "q-10743",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, Redis, and a downstream analytics job. Peak-load latency spikes are observed in the HTTP path but it's unclear where—HTTP, DB, Kafka, or cache. Propose a concrete instrumentation and debugging plan using distributed tracing to identify slow legs, including how to propagate trace context across producer/consumer boundaries, what metrics to collect (p95 latency, tail latency, queue backpressure), and concrete thresholds and actions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Version API contracts; add DB column with default and backfill; deploy API v2 behind a feature flag; migrate Kafka to a new schema via a registry; publish both old and new formats during a transition;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt JSON Schema at API boundary and Avro with a Schema Registry for Kafka. Maintain versioned contracts and enforce backward/forward compatibility in the registry. Validate requests and events at in",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a comprehensive distributed tracing strategy using OpenTelemetry across the entire CNPA pipeline. Begin by instrumenting the HTTP API endpoints, PostgreSQL queries, Kafka producers/consumers, and Redis cache o…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Mask PII at the Kafka stream layer using per-tenant HMAC for emails and tokenized redaction for phones, so Kafka carries masked identifiers. Raw PII stays in PostgreSQL with strict RBAC and an auditab",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProduction CNPA systems require robust observability to diagnose performance issues during peak loads. This tests practical knowledge of distributed systems debugging and real-world monitoring strategies.\n\n## Key Concepts\n- Distributed tracing with OpenTelemetry\n- Context propagation across service boundaries\n- Tail latency analysis and SLA management\n- Performance monitoring and alerting thresholds\n- Load testing and canary deployment strategies\n\n## Code Example\n```javascript\n// OpenTelemetry configuration for CNPA pipeline\nconst { NodeSDK } = require('@opentelemetry/sdk-node');\nconst { HttpInstrumentation } = require('@opentelemetry/instrumentation-http');\nconst { PgInstrumentation } = require('@opentelemetry/instrumentation-pg');\nconst { RedisInstrumentation } = require('@opentelemetry/instrumentation-redis');\n\nconst sdk = new NodeSDK({\n  instrumentations: [\n    new HttpInstrumentation(),\n    new PgInstrumentation(),\n    new RedisInstrumentation()\n  ],\n  serviceName: 'cnpa-service'\n});\n\n// Kafka context propagation example\nconst produceWithTracing = async (topic, message, span) => {\n  const headers = {\n    traceparent: span.spanContext().traceparent,\n    'x-request-id': span.spanContext().traceId\n  };\n  await producer.send({ topic, messages: [{ value: message, headers }] });\n};\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-13",
        "questionId": "q-11274",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis handles a real-time recommendation feed. Under peak load, response times spike and the cache becomes stale. Propose a concrete, implementable plan to design and validate a robust cache-aside strategy and stampede protection across Redis and DB, including data flow, locking, TTLs, warmup, and how you’d verify under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement end-to-end tracing with OpenTelemetry across HTTP API, PostgreSQL, Kafka, and Redis. Thread a trace-id through HTTP requests, Kafka messages, and Redis reads; create spans for HTTP, DB, Kafk",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a robust cache-aside strategy for Redis to protect against stampedes under peak load. Use per-key locks (REDLOCK), a Lua atomic fetch-or-set for misses, and a warmup process when a new item is",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement end-to-end OpenTelemetry tracing across HTTP API, PostgreSQL, Kafka, and Redis. Propagate trace context from HTTP header via all calls; instrument DB driver, Kafka producer, and Redis client",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement distributed tracing to attribute latency across each service component; compare p99 latency metrics with JWT rotation enabled versus disabled; collect JWKS fetch duration, authentication check latency, databas…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical cache strategy across CNPA with Redis stampede protection, TTL tuning, and warmup under high load.\n\n## Key Concepts\n- Cache-aside pattern\n- Redis per-key locking (RedLock)\n- Lua scripts for atomic fetch-or-set\n- TTL strategy and cache warmup\n- End-to-end verification under load\n\n## Code Example\n```javascript\n// Pseudo-code: Redis fetch-or-set using Lua for atomicity\nlocal key = KEYS[1]\nlocal exists = redis.call('EXISTS', key)\nif exists == 1 then\n  return redis.call('GET', key)\nelse\n  local val = ARGV[1]\n  redis.call('SET', key, val, 'EX', ARGV[2])\n  return val\nend\n```\n\n## Follow-up Questions\n- How would you adapt this to multi-region deployment?\n- How would you measure improvements in cache-hit ratio and latency?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-14",
        "questionId": "q-11880",
        "question": "In a CNPA stack (HTTP API, PostgreSQL, Kafka), design a beginner-friendly idempotent create-user endpoint to handle client retries. Outline concrete steps to guarantee no duplicates across HTTP retries and Kafka publishes, including idempotency key strategy, dedup store, DB constraints, and a simple test plan that proves no duplicates under retry scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use additive schema changes: make the field optional in the database and Kafka payload, version the messages, implement dual writes during a limited window, and backfill historic records with a sensible default.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Add a nullable tenant_id column to the events table, backfill in batches, update API and DB layer to pass tenant_id, then enforce NOT NULL and create a composite index on (tenant_id, event_time). Depl",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use transactional outbox: wrap profile update and an outbox row in one DB transaction. A separate idempotent sink publishes the outbox to Kafka, then marks sent. Invalidate Redis on commit success. Ha",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an idempotency_key header; store in PostgreSQL a dedup table with idempotency_key and status; define unique constraints on (idempotency_key) and (email) to block duplicates; check Redis cache for recent keys first,…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of idempotence across HTTP, DB, and Kafka in a CNPA stack, with a concrete, beginner-friendly pattern that scales to real systems.\n\n## Key Concepts\n\n- Idempotency keys and at-least-once semantics\n- DB constraints vs idempotent producers/outbox patterns\n- Redis as a dedup cache with TTL\n- Kafka delivery guarantees and transactional outbox\n- Practical tests for retry scenarios and race conditions\n\n## Code Example\n\n```javascript\n// Pseudo: dedup flow for idempotent create-user\nfunction handleCreateUser(req, res) {\n  const key = req.headers['idempotency_key'];\n  \n  // 1. Check Redis cache first\n  if (await redis.get(key)) {\n    return await getCachedResponse(key);\n  }\n  \n  // 2. Check dedup table\n  const existing = await db.query(\n    'SELECT response FROM dedup WHERE idempotency_key = $1',\n    [key]\n  );\n  \n  if (existing) {\n    await redis.setex(key, 3600, existing.response);\n    return existing.response;\n  }\n  \n  // 3. Create user with constraints\n  const result = await db.transaction(async (trx) => {\n    const user = await trx.query(`\n      INSERT INTO users (email, name) \n      VALUES ($1, $2) \n      ON CONFLICT (email) DO NOTHING\n      RETURNING *\n    `, [req.body.email, req.body.name]);\n    \n    if (!user) {\n      throw new Error('Email already exists');\n    }\n    \n    // 4. Outbox pattern for Kafka\n    await trx.query(`\n      INSERT INTO outbox (event_type, payload) \n      VALUES ('user_created', $1)\n    `, [JSON.stringify(user)]);\n    \n    return user;\n  });\n  \n  // 5. Cache and store response\n  const response = { user: result };\n  await db.query(`\n    INSERT INTO dedup (idempotency_key, response) \n    VALUES ($1, $2)\n  `, [key, JSON.stringify(response)]);\n  \n  await redis.setex(key, 3600, JSON.stringify(response));\n  \n  return res.json(response);\n}\n```\n\n## Test Plan\n\n1. **Single Request**: Verify user creation and Kafka event\n2. **Immediate Retry**: Same idempotency_key returns cached response\n3. **Delayed Retry**: After cache expiry, dedup table prevents duplicate\n4. **Concurrent Requests**: Race condition handling with DB constraints\n5. **Kafka Failure**: Outbox ensures eventual consistency",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-15",
        "questionId": "q-28069",
        "question": "In a CNPA stack with an HTTP API, PostgreSQL, Kafka, Redis, and a downstream analytics job, design and implement an idempotent delivery mechanism that guarantees a user-signup event is reliably published to Kafka and consumed downstream without distributed transactions. Use the outbox pattern: atomic write to user and outbox, a worker to publish, and a consumer that deduplicates and updates Redis. Outline schema changes, failure modes, and validation steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: enable BACKWARD compatibility in the schema registry; evolve Avro by adding region as an optional field with default 'unknown'. Deploy a new producer version to a canary and update the consumer",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use the Outbox pattern. On HTTP signup, in one DB transaction insert the user and an outbox row (aggregate_id, event_type, payload, processed=false). A separate worker publishes outbox events to Kafka",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement end-to-end OpenTelemetry tracing across HTTP API, PostgreSQL, Kafka, and Redis. Propagate trace context from HTTP header via all calls; instrument DB driver, Kafka producer, and Redis client",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement the outbox pattern: atomically write the event to the main table and an outbox table within one DB transaction, then flush the outbox row to Kafka using a transactional producer (enable.idem",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to guarantee reliable cross-system delivery without distributed transactions, leveraging the Outbox pattern, idempotent consumption, and robust validation under partial failures.\n\n## Key Concepts\n\n- Outbox pattern\n- Exactly-once delivery\n- Idempotent consumers\n- Durable Kafka keys\n- Redis deduplication and caching\n\n## Code Example\n\n```sql\n-- PostgreSQL tables\nCREATE TABLE users (\n  id UUID PRIMARY KEY,\n  email TEXT,\n  created_at TIMESTAMPTZ DEFAULT now()\n);\n\nCREATE TABLE outbox (\n  id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  aggregate_id UUID,\n  event_type TEXT,\n  payload JSONB,\n  processed BOOLEAN DEFAULT FALSE,\n  created_at TIMESTAMPTZ DEFAULT now(),\n  last_attempt TIMESTAMPTZ\n);\n```\n\n```sql\n-- Atomic signup write\nBEGIN;\nINSERT INTO users (id, email) VALUES (?, ?);\nINSERT INTO outbox (aggregate_id, event_type, payload) VALUES (?, 'UserSignedUp', to_jsonb(?));\nCOMMIT;\n```\n\n```javascript\n// Pseudo: publish outbox entries and mark processed\nwhile (row = fetchUnprocessedOutbox()) {\n  publishToKafka(row.payload, { key: row.aggregate_id });\n  markOutboxProcessed(row.id);\n}\n```\n\n## Follow-up Questions\n\n- How would you test idempotency across retries and partial failures?\n- How would you handle schema evolution for the outbox payload without breaking consumers?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-16",
        "questionId": "q-25783",
        "question": "In a CNPA stack with an HTTP API, PostgreSQL, Kafka, and Redis, tail latency spikes occur under peak load. Design a concrete end-to-end observability plan to pinpoint whether the bottleneck lies in the HTTP path, DB queries, Kafka publishing/consuming, or Redis caching, including instrumentation points, correlation IDs, sampling, dashboards, and an example change you would verify end-to-end impact?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement strict per-tenant isolation across the CNPA stack: enable PostgreSQL Row-Level Security with a tenant_id context, scope all HTTP DB joins to the tenant, enforce Kafka topic ACLs and per-tena",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enforce PostgreSQL unique constraint on order_id; use a transactional outbox to publish to Kafka after write; implement Redis-based idempotency (SETNX) per (user_id, order_id) TTL 1h; enable Kafka pro",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use OpenTelemetry to propagate a trace_id across HTTP, PostgreSQL, Kafka, and Redis. Instrument HTTP handler timing, DB queries (pg_stat_statements), Kafka producer/consumer latency, and Redis command",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement lightweight OpenTelemetry tracing across the CNPA stack. Propagate TraceContext from HTTP requests through DB calls, Kafka producers, and Redis reads; instrument HTTP handlers, DB drivers, a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTail-latency across CNPA is a common production challenge. This question probes practical, end-to-end observability skills across multiple components, including tracing, instrumentation, sampling, and validation.\n\n## Key Concepts\n\n- Distributed tracing with OpenTelemetry\n- Context propagation across HTTP, PostgreSQL, Kafka, and Redis\n- End-to-end latency analysis with tail metrics (p95/p99)\n- Sampling strategies and dashboards/alerts\n- Validation via canary or small controlled rollout\n\n## Code Example\n\n```javascript\n// OpenTelemetry trace propagation across CNPA (illustrative)\nimport { trace, propagation, context, SpanKind } from '@opentelemetry/api';\nconst tracer = trace.getTracer('cnpa');\nconst span = tracer.startSpan('handleRequest', { kind: SpanKind.SERVER });\n// Inject trace context into downstream calls\nconst reqHeaders = {};\npropagation.inject(context.active(), reqHeaders, {\n  set: (carrier, key, value) => { carrier[key] = value; }\n});\n// ... downstream calls carry reqHeaders\nspan.end();\n```\n\n## Follow-up Questions\n\n- How would you adjust sampling to balance insight and overhead at 1000 RPS vs 100k RPS?\n- How would you validate that tracing changes do not introduce measurable latency overhead?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-17",
        "questionId": "q-22596",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, Redis, and a downstream analytics sink. Design a controlled chaos test to validate SLOs under multi-service failures. Specify failure modes (HTTP 5xx bursts, DB timeouts, Kafka lag, Redis eviction), blast radius, observability signals, and automated remediations (timeouts, circuit breakers, backpressure, retry budgets) and how you'd verify SLOs during and after failures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a staged chaos engineering experiment to inject HTTP 5xx bursts, database timeouts, Kafka consumer lag, and Redis eviction events with progressively increasing blast radius. Implement end-to-end latency tracing u…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement OpenTelemetry tracing across all CNPA layers. Instrument HTTP handler to start a span, wrap DB calls with SQL instrumentation, annotate Kafka producer/consumer spans, and trace Redis reads.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Pinpoint drift between event-time and processing-time windows. Check: 1) producer event_time vs stored event_time in Postgres; 2) Kafka record timestamps and any broker clock drift; 3) consumer window",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a practical tamper-evident audit trail across HTTP input, Postgres writes, Kafka publications, and Redis reads. Use per-tenant HMAC signatures on payloads, add a chained audit_log with row_hash",
            "isCorrect": false
          }
        ],
        "explanation": "This question evaluates resilience planning skills across the full CNPA stack under realistic failure scenarios. It probes the ability to specify concrete failure modes, define blast radius boundaries, implement targeted remediations, and validate SLOs through systematic verification. Key concepts include chaos engineering principles, SLO management, end-to-end observability with distributed tracing, circuit breakers for fault isolation, backpressure for traffic regulation, retry budgets with jitter to prevent thundering herd problems, and safe rollback procedures for rapid recovery.",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-18",
        "questionId": "q-4780",
        "question": "CNPA stack: HTTP API writes to PostgreSQL, publishes events to Kafka with Avro, and a downstream BI pipeline consumes from Kafka. A new event version adds optional fields and changes a field type; describe a concrete plan to implement schema evolution with backward/forward compatibility, including staging readers with dual schemas, feature flags, and a rollback strategy, plus how you would validate no data loss during rollout?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a backward- and forward-compatible Avro schema in Confluent Schema Registry, publish a new version with default values for new fields, implement a dual-writer rollout, and gate readers behind a fe",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement end-to-end tracing with OpenTelemetry across HTTP API, PostgreSQL, Kafka, and Redis. Thread a trace-id through HTTP requests, Kafka messages, and Redis reads; create spans for HTTP, DB, Kafk",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Tag every incoming event with a stable event_id at ingress, propagate it via HTTP headers and Kafka message headers, write a lineage row in PostgreSQL (event_id, source, destination, transforms, timestamp), and emit a l…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-phase, non-breaking migration: 1) ALTER TABLE events ADD COLUMN region TEXT NULL; deploy code with a feature flag so writes/reads can ignore region for now and Kafka events embed region when prese",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests schema evolution discipline across CNPA, rollout safety, and end-to-end validation.\n\n## Key Concepts\n- Schema evolution with Avro, backward/forward compatibility\n- Dual-writer rollout, feature flags, and phased deprecation\n- Validation metrics: lag, DLQ, schema compatibility, data correctness\n\n## Code Example\n```javascript\n// Example: wrapping a new event version with defaults for new fields\nfunction normalizeEventV1toV2(evt) {\n  return {\n    user_id: evt.user_id,\n    action: evt.action,\n    ts: evt.ts,\n    country: evt.country ?? null,\n    device: evt.device ?? null\n  }\n}\n```\n\n## Follow-up Questions\n- How would you enforce and verify backward/forward compatibility in the registry?\n- How would you safely remove the old readers after full migration?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-19",
        "questionId": "q-7095",
        "question": "CNPA stack with an HTTP API, PostgreSQL, and Kafka: add a new optional field address_url to user profiles with zero downtime. Provide a beginner-friendly migration plan that keeps the API online, preserves data integrity, and coordinates DB changes, API/producer/consumer schema, feature flags, and canary tests. Include concrete steps and validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route writes to the current primary region; use a transactional outbox to publish to Kafka and an idempotent sink to guarantee at-least-once delivery; invalidate Redis keys on region boundary and warm",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Add a nullable address_url column (TEXT) with no backfill by default; deploy API with a feature flag that exposes the field only when enabled; run a batch job to backfill existing users in small chunk",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design end-to-end exactly-once delivery using the transactional outbox pattern. First, persist the user profile update and the Kafka message together in a single database transaction via an outbox table. Then, use a sep…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce end-to-end tenant isolation: PostgreSQL: enable RLS on all tables and require tenant_id in every query; API: extract tenant_id from JWT and enforce it in all endpoints; Redis: namespace keys w",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDesigning zero-downtime migrations in a CNPA stack tests end-to-end understanding of online schema changes, backward/forward compatibility, and rollout safety.\n\n## Key Concepts\n- Online schema changes (PostgreSQL) with nullable new column and backfill\n- Feature flag driven rollout to keep API stable\n- Kafka schema evolution (AVRO) and producer/consumer compatibility\n- Canary testing and rollback strategies\n\n## Code Example\n```javascript\n-- PostgreSQL migration (for demonstration)\nALTER TABLE users ADD COLUMN address_url TEXT;\n```\n\n## Follow-up Questions\n- How would you backfill without blocking reads during peak traffic?\n- How would you validate rollback correctness and data integrity after rollouts?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-20",
        "questionId": "q-15412",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis uses Redis as a read-through cache for user profiles. After a write to PostgreSQL, reads sometimes return stale data from Redis under peak load. Design a beginner-friendly plan to ensure cache correctness and minimize latency, detailing caching strategy (cache-aside vs write-through), invalidation triggers, TTL defaults, and verification steps under load. Provide concrete changes and tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a write-through caching strategy with Redis as the authoritative read source for user profiles. On each PostgreSQL write, immediately update Redis synchronously before confirming the write as complete. Use a 60-se…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement transactional outbox: write to DB and an outbox in one transaction; publish via Kafka with a transactional producer; sinks are idempotent by (entity_id, event_id) and dedupe; partition event",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a write-through strategy: update PostgreSQL first, then atomically refresh Redis in a Lua script to ensure cache reflects the new value. Use a versioned key (user:<id>:v) and TTL 30s; publish an i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt an idempotent enrichment flow: attach a stable event_id, use a Redis-based dedup cache per event_window, and enable Kafka producer idempotence. Wrap DB read, enrichment, and publish in a single",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates understanding of cache consistency patterns in CNPA stacks (Containerized/Cloud-Native, PostgreSQL, Apache Kafka, Redis). It tests knowledge of write-through vs cache-aside strategies, cache invalidation under concurrency, and maintaining data freshness while minimizing latency during high-load scenarios.\n\n## Key Concepts\n\n- **Cache Coherence**: Ensuring Redis and PostgreSQL remain synchronized, especially under concurrent writes\n- **Write-Through vs Cache-Aside**: Write-through writes to both DB and cache simultaneously, guaranteeing consistency at the cost of slightly higher write latency; cache-aside is faster for writes but risks stale reads\n- **TTL and Eviction**: Time-to-live settings balance staleness risk against cache memory pressure and read performance\n- **Invalidation Triggers**: Immediate invalidation on write, TTL expiration, and event-driven invalidation via Kafka\n- **Observability**: Cache hit rate, stale read detection, end-to-end latency percentiles\n\n## Solution Design\n\n### Caching Strategy: Write-Through\nWrite-through ensures the cache is updated synchronously on every write, eliminating stale reads. The application writes to PostgreSQL first, then immediately updates Redis before acknowledging the operation.\n\n### Invalidation Triggers\n1. **Synchronous**: On profile update, immediately update Redis\n2. **TTL Expiration**: 60-second TTL forces automatic refresh\n3. **Event-Driven**: Publish invalidation event to Kafka for distributed cache warming\n\n### TTL Defaults\n- **Active Data (User Profiles)**: 60 seconds\n- **Static/Reference Data**: 5-15 minutes\n- **Hot Keys**: 30 seconds with active refresh\n\n## Code Implementation\n\n```javascript\n// Write-through profile update\nasync function updateProfile(id, data) {\n  // Transaction ensures atomicity\n  await db.query(\n    'UPDATE profiles SET name = ?, email = ?, updated_at = NOW() WHERE id = ?',\n    [data.name, data.email, id]\n  );\n  \n  // Immediate cache update (synchronous write-through)\n  await redis.set(\n    `profile:${id}`,\n    JSON.stringify({ ...data, updatedAt: new Date().toISOString() }),\n    'EX',\n    60\n  );\n  \n  // Publish invalidation event for consistency across services\n  await kafka.publish('profile-updated', { id, timestamp: Date.now() });\n}\n\n// Cache-aside read with stale-check\nasync function getProfile(id) {\n  const cached = await redis.get(`profile:${id}`);\n  \n  if (cached) {\n    return JSON.parse(cached);\n  }\n  \n  // Cache miss - load from DB and populate cache\n  const [profile] = await db.query('SELECT * FROM profiles WHERE id = ?', [id]);\n  \n  if (profile) {\n    await redis.set(`profile:${id}`, JSON.stringify(profile), 'EX', 60);\n  }\n  \n  return profile;\n}\n```\n\n## Verification Under Load\n\n### Load Test Scenarios\n1. **Concurrent Writes**: 100 threads updating the same profile simultaneously\n2. **Read-Heavy Peak**: 10,000 reads/second with periodic writes\n3. **Cache Expiry Storm**: All TTLs expire simultaneously\n\n### Verification Steps\n1. **Consistency Check**: After each write, immediately read and verify cache matches DB\n2. **Stale Read Detection**: Run reads in a tight loop post-write, flag any mismatches\n3. **Hit Rate Monitoring**: Track Redis INFO stats for keyspace_hits/keyspace_misses\n4. **Latency Validation**: P50/P95/P99 response times under load must remain within SLA\n\n### Test Example\n```javascript\nasync function testCacheConsistency() {\n  const testId = 'test-user-123';\n  \n  // Write new profile\n  await updateProfile(testId, { name: 'Alice', email: 'alice@example.com' });\n  \n  // Immediately read back - cache should be fresh\n  const cached = await getProfile(testId);\n  const [dbRecord] = await db.query('SELECT * FROM profiles WHERE id = ?', [testId]);\n  \n  assert.strictEqual(cached.name, dbRecord.name);\n  assert.strictEqual(cached.email, dbRecord.email);\n  \n  // Concurrent update stress test\n  const updates = Array(50).fill(null).map((_, i) => \n    updateProfile(testId, { name: `User${i}`, email: `user${i}@test.com` })\n  );\n  \n  await Promise.all(updates);\n  \n  // Verify final consistency\n  const final = await getProfile(testId);\n  const [finalDb] = await db.query('SELECT * FROM profiles WHERE id = ?', [testId]);\n  \n  assert.strictEqual(final.name, finalDb.name);\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-21",
        "questionId": "q-26375",
        "question": "In a CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis, user profiles are read frequently but occasionally stale. Design a beginner-friendly plan to implement a read-through cache with Redis that keeps data fresh and avoids cache stampede, including eviction TTL, a simple lock for miss handling, cache invalidation on writes via Kafka, and how you'd verify performance under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement the outbox pattern: within a single database transaction, insert both the domain change (e.g., order creation) and a corresponding outbox record. A background worker then polls the outbox table, publishes even…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an Idempotency-Key header on signup requests. First, check Redis for the idempotency key; if it exists, return a 200 response (user already processed). If the key is new, attempt to insert the user into PostgreSQL u…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Read-Through Cache Implementation for GET /user/{id}** Implement a read-through cache for user profiles using Redis as a cache-aside layer: 1. **Cache Hit**: On request, check Redis first. If the user profile exists,…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Upgrade Avro schema with an optional field default, set Schema Registry compatibility to BACKWARD, and roll out via a canary topic. Publish new schema alongside the old one, route 5–10% traffic to the",
            "isCorrect": false
          }
        ],
        "explanation": "## Purpose\n\nThis question assesses practical CNPA caching patterns that engineers can implement, testing the balance between data freshness, stampede prevention, and cross-service cache invalidation via Kafka.\n\n## Key Concepts\n\n- **Cache-aside**: The application first checks Redis before querying PostgreSQL. If the data exists in cache, it's returned directly; otherwise, the application fetches from the database and populates the cache.\n\n- **Stampede protection**: Using Redis locks (e.g., `SETNX`) prevents multiple concurrent requests from simultaneously hitting the database on a cache miss. Only one request acquires the lock, fetches from the source, and populates the cache while others wait.\n\n- **Eviction TTL**: Setting a 5–10 minute TTL ensures stale data automatically expires, maintaining eventual consistency even if invalidation events are missed.\n\n- **Event-driven invalidation**: Publishing cache invalidation events through Kafka enables all consuming services to evict stale entries, keeping distributed caches synchronized across the system.",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-22",
        "questionId": "q-1794",
        "question": "In a CNPA stack—HTTP API, PostgreSQL, Kafka, Redis—latency spikes appear on a new endpoint that touches all components. Outline a practical plan to implement end-to-end tracing with a correlation_id: where to instrument, which metrics to collect (end-to-end latency, per-service latency, queue time, DB time, cache misses), and concrete changes (propagate correlation_id in HTTP, persist it in DB, attach it to Kafka messages, emit trace spans). How would you validate in staging before production?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create tenant-prefixed topics (tenantX.events) and Redis namespaces (tenantX:*). Run a dual-write window: emit to both old and new prefixes, while readers consume both streams. Flip traffic via featur",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a single correlation_id header across HTTP, DB writes, Kafka messages, and Redis ops; emit distributed traces for HTTP handler, DB query, Redis access, and Kafka producer/consumer. Track end",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose measuring per-stage latency: HTTP handler time, DB write duration, Kafka producer latency, and consumer lag. Use distributed tracing IDs, per-tenant sampling, and a backpressure monitor. Verif",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement end-to-end signing: sign each HTTP body with a per-tenant key (ECDSA P-256) and attach a KeyID in a custom header; include a canonical payload (timestamp, nonce, body hash). Validate signatu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical end-to-end tracing skills at CNPA scale, focusing on observable instrumentation and real-world validation.\n\n## Key Concepts\n- End-to-end tracing and correlation IDs\n- OpenTelemetry instrumentation\n- Latency breakdown across HTTP, DB, Kafka, Redis\n\n## Code Example\n```javascript\n// Propagate correlation_id across calls\nconst corrId = req.headers['Correlation-Id'] || generateId();\nres.setHeader('Correlation-Id', corrId);\nawait db.query('INSERT INTO events (...) VALUES (...)', [/* params */, corrId]);\nproducer.send({ topic: 'events', messages: [{ value: payload, headers: { 'Correlation-Id': corrId } }] });\n```\n\n## Follow-up Questions\n- How would you handle high cardinality correlation IDs?\n- How would you sample traces to limit overhead?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-23",
        "questionId": "q-29910",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis: design a concrete plan to implement per-tenant rate limiting that is enforced at both the API gateway and service layer, uses Redis-backed token buckets, ensures no cross-tenant bleed, propagates tenant context into Kafka producers, handles bursts gracefully with backoff and Retry-After, and validates impact under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instrument CNPA with OpenTelemetry: trace HTTP requests across handler, DB, Kafka, and Redis. Collect per-span latency (p95/p99), error rate, and queue depths. Propagate trace context in HTTP headers,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Generate or propagate a correlation_id on every request. Thread it through the HTTP handler, insert it in PostgreSQL (corr_id column), attach it to Kafka message headers, mirror in Redis keys, and emit trace events for…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Add a nullable new column with a concurrent index; backfill in background; route reads to either column via a feature flag and gradually shift; update writes to populate both columns; once backfill co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant rate limits using a Redis token-bucket with a Lua script for atomicity; enforce at the gateway (Envoy) and in-service as a fallback. Tag requests and Kafka messages with tenant ID",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cross-component, tenant-aware rate limiting with strict isolation and graceful degradation under load.\n\n## Key Concepts\n\n- Per-tenant quotas and isolation\n- Redis-backed token bucket with atomic Lua script\n- Ingress and service-layer enforcement\n- Propagating tenant context to Kafka producers\n- Backoff and Retry-After semantics; observability\n\n## Code Example\n\n```javascript\n// Pseudo Lua for Redis token bucket (atomic check & decrement)\n```\n\n## Follow-up Questions\n\n- How would clock skew affect bursts and how to compensate?\n- How to test in canary without impacting production tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-24",
        "questionId": "q-19632",
        "question": "In a CNPA stack with an HTTP API, PostgreSQL, Kafka, and a data lake sink, during peak traffic HTTP 5xx errors and lake data drift occur; design a concrete end-to-end plan to guarantee exactly-once ingestion and lake parity, detailing the architecture, transaction boundaries (outbox/2PC), idempotency keys, replay protection, and a robust verification strategy under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Redis-backed token bucket in API middleware. Key rate_limit:{client}:{path} holds tokens and last_refill_ts. A Lua script atomically refills tokens based on elapsed time and consumes one per req",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Store the flag in Redis as feature:new-field:percent with a 10% seed; gate the API by user_id%10 == 0 for the new field. Make the new column nullable in PostgreSQL and populate only when the flag is e",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement the transactional outbox pattern: atomically write the business record and outbox event in a single database transaction, then publish to Kafka via a transactional producer. Use a unique ingestion_id as an ide…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Attach a per-event correlation ID and include a cryptographic signature on all emitted events. Maintain an append-only audit ledger in PostgreSQL with a hash chain, and use Kafka with at-least-once de",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates your ability to design end-to-end exactly-once guarantees across HTTP, PostgreSQL, Kafka, and a data lake—including replay handling, failure modes, and verification under load.\n\n## Key Concepts\n\n- **Transactional Outbox Pattern**: Write business data and outbox events atomically within a single database transaction, eliminating orphaned events.\n- **Idempotency Keys**: Assign a unique ingestion_id to each request; persist it with the payload to enable deterministic deduplication on replay.\n- **Two-Phase Commit (2PC)**: Coordinate atomic commits across PostgreSQL and Kafka when tighter coupling is acceptable, though the outbox pattern offers better decoupling and resilience.\n- **Exactly-Once Semantics**: Combine at-least-once delivery with idempotent consumers and deduplication to achieve exactly-once end-to-end.\n\n## Architecture & Transaction Boundaries\n\n1. **Ingestion Layer**: API receives requests with ingestion_id; validates and extracts idempotency key.\n2. **Transaction Boundary**: Single PostgreSQL transaction encompasses: (a) business record insert, (b write with status ') outbox eventPENDING'.\n3. **Publish**: On commit, a Kafka transactional producer sends the outbox event; a separate relay process or CDC monitors the outbox table and confirms delivery.\n4. **Consumer**: Kafka consumer reads events, checks ingestion_id for deduplication, writes to the data lake, and acknowledges offset.\n\n## Idempotency & Replay Protection\n\n- Store ingestion_id in both the business table and outbox; consumer queries lake sink before write to detect duplicates.\n- Use a dedicated idempotency table or Bloom filter for fast lookups under high load.\n- Configure Kafka consumer with isolation.level=read_committed to ensure transactional consistency.\n\n## Verification Under Load\n\n- Inject synthetic load with known ingestion_ids; assert zero duplicates in lake and zero missing records.\n- Use chaos engineering (kill producers, brokers, consumers) and verify outbox relay recovers without data loss or duplication.\n- Monitor lag, dead-letter queues, and Lakehouse consistency via checksum validation.",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-23T05:25:28.046Z",
    "version": 1
  }
]