[
  {
    "id": "test-cnpa",
    "channelId": "cnpa",
    "channelName": "Cnpa",
    "title": "Cnpa Knowledge Test",
    "description": "Test your understanding of Cnpa with 25 questions.",
    "questions": [
      {
        "id": "cnpa-q-0",
        "questionId": "q-27982",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and an audit requirement: every API request must produce a durable, immutable audit log entry with an audit_id that travels end-to-end across components. Design a beginner-friendly plan to implement this across HTTP layer, DB, and Kafka without adding significant latency. Include where to generate the audit_id, how to pass it via HTTP and Kafka, schema ideas, tests, and how you'd verify end-to-end traceability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Generate audit_id at the HTTP ingress, pass via headers, store a row in PostgreSQL with audit_id, and attach the same id to the Kafka message headers and downstream data lake writes. Use a minimal aud",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement masking at the HTTP edge and during Kafka ingestion. Publish masked events downstream while storing only hashed PII in PostgreSQL. Maintain an immutable, tamper-evident audit log in a separate Kafka topic with…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan to diagnose tail latency on the feature-flag evaluation path in CNPA under peak load. Instrument per-hop timings (HTTP, flag service, Redis, DB), enable an in-process cache for hot flags, batch e",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument with OpenTelemetry to trace HTTP handler, DB queries, and Kafka publish; collect p95/p99 latency, error rate, and queue backpressure. Reproduce under load (k6). If HTTP slow, tune keep-aliv",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAuditing across a CNPA stack is critical for compliance; beginners should show end-to-end thinking and minimal latency impact.\n\n## Key Concepts\n- End-to-end provenance via a single audit_id\n- Propagation through HTTP headers and Kafka message headers\n- Lightweight audit table and minimal schema to avoid latency\n- End-to-end tests that verify trace consistency\n\n## Code Example\n```javascript\n// Generate and propagate audit_id in Node.js\nfunction handleRequest(req, res) {\n  const auditId = req.headers['x-audit-id'] || generateAuditId();\n  res.setHeader('x-audit-id', auditId);\n  // log audit locally\n  logAudit({ auditId, endpoint: req.path, userId: req.user?.id, ts: Date.now() });\n  // forward to DB and Kafka with the header\n  db.insert({ audit_id: auditId, ...req.body });\n  kafka.publish('events', { payload: req.body, headers: { 'x-audit-id': auditId } });\n  res.end('ok');\n}\n```\n\n## Follow-up Questions\n- How would you test end-to-end audit trace under load? \n- What metrics would you monitor for audit latency and completeness?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-1",
        "questionId": "q-11526",
        "question": "In a CNPA stack (HTTP API, PostgreSQL, Kafka, Redis), a profile update triggers a DB write, a Redis cache invalidation, and a Kafka event. During peak load, Redis sometimes serves stale data for minutes after update. Design a beginner-friendly plan to fix cache invalidation and ensure consistency, including concrete steps, timing, and tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: implement cache-aside with versioned Redis keys. On update, bump version in PostgreSQL, delete stale cache keys, and publish a profile_updated event to Kafka. Consumers read latest version from",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Instrument all HTTP requests with a persistent trace ID and propagate it through DB writes, Kafka messages, and Redis reads. Use OpenTelemetry with Jaeger, add Kafka headers for correlation, implement",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement the transactional outbox: each HTTP write to user_profiles also inserts an audit_outbox row with id, user_id, action, ts, payload_hash, and source in the same DB transaction. A dedicated wor",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Postgres: ALTER TABLE users ADD COLUMN preferred_language VARCHAR(32) NULL; Kafka: update Avro schema with an optional field defaulting to null and enable backward/forward compatibility; HTTP: version",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes understanding of cache invalidation, versioned keys, outbox patterns, and end-to-end consistency in CNPA stacks. It also checks how to observe impact and plan safe rollouts.\n\n## Key Concepts\n- Cache invalidation strategies and TTLs\n- Redis key versioning and cache-aside pattern\n- Event-driven invalidation via Kafka\n- Safe reads with read-fallbacks and refresh\n- Observability of latency and hit ratios\n\n## Code Example\n```javascript\n// Update path\nBEGIN;\nUPDATE profiles SET name=$1, email=$2, version = version + 1 WHERE id=$id;\nCOMMIT;\nredis.del(`profile:${id}:v*`);\npublishKafka('profile_updated', { id, version: NEW_VERSION });\n```\n\n## Follow-up Questions\n- How would you test race conditions when read happens concurrently with update?\n- How would you tune TTLs and decide when to prune old keys?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-2",
        "questionId": "q-18857",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, Redis, and a data lake sink encounters a rolling deploy that replays in-flight events, causing duplicates across DB and downstream stores. Design a robust, end-to-end idempotent processing path that tolerates restarts and partial failures. Include concrete steps, tooling, and validation to ensure no duplicates or data loss while preserving ordering where required?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate all inbound JSON against a JSON Schema at the HTTP gateway; reject non-conforming payloads with 400. Enforce constraints in PostgreSQL (CHECKs) and ensure only validated messages are publishe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a comprehensive schema evolution plan for user events in a CNPA stack (HTTP API, Kafka, PostgreSQL, Redis). Implement a schema registry with versioned envelopes, optional fields with sensible defaults, and canary…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan a backward/forward-compatible rollout using a dual protobuf version and a shim. Add v2 fields as optional, route 10% of traffic to a shim that translates v1 to v2, run contract tests, then increm",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an idempotent event processing path using the transactional outbox pattern combined with Kafka's exactly-once semantics. Assign a globally unique event_id (UUID v4 or snowflake) to each event. Within a single Pos…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nRolling deploys that replay in-flight events threaten data integrity. This question probes mastery of end-to-end exactly-once processing, transactional outboxes, and cross-store deduplication strategies.\n\n## Key Concepts\n\n- Idempotent processing across HTTP, PostgreSQL, Kafka, Redis, and data lake\n- Transactional outbox pattern combined with Kafka transactions\n- Deduplication using event_id at Redis and lake sinks\n- Ordering guarantees across downstream consumers\n\n## Implementation Steps\n\n1. **Event Generation**: Assign globally unique event_id (UUID) to each incoming request\n2. **Transaction Boundary**: Write business data + outbox record in single PostgreSQL transaction\n3. **Kafka Publish**: Use transactional producer to publish outbox events atomically\n4. **Consumer Deduplication**: Check Redis for event_id before processing; skip if exists\n5. **Data Lake Sink**: Use event_id as primary key for deduplication on write\n6. **Offset Commit**: Commit Kafka offsets only after successful processing and downstream acknowledgment",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-3",
        "questionId": "q-20816",
        "question": "In a CNPA stack with HTTP API, PostgreSQL, Kafka, Redis, and a downstream analytics job, implement end-to-end data privacy: redact PII before it leaves Kafka, while keeping full PII in PostgreSQL for compliance backups and enabling analytics on masked data. Propose a concrete plan: where to redact (producer, stream processor, or sink), the masking method, key management, latency impact, and how you'd validate privacy reach under peak load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Mask PII at the Kafka stream layer using per-tenant HMAC for emails and tokenized redaction for phones, so Kafka carries masked identifiers. Raw PII stays in PostgreSQL with strict RBAC and an auditab",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement exactly-once delivery by wrapping the profile update in a single PostgreSQL transaction that writes the new state and inserts an outbox event. Publish using a Kafka idempotent producer with a unique event_id,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: add a nullable column customer_segment to Postgres; emit versioned payloads starting with version 2 that include the field; implement a short dual-write period to upgrade producers/consumers; ba",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an outbox table in Postgres, publish to Kafka within a DB transaction, and build idempotent sinks in Redis keyed by (entity_id, event_id). Add a reconciliation pass that confirms all publish",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess ability to implement privacy-by-design in a CNPA pipeline, balancing latency, security, and analytics usability.\n\n## Key Concepts\n\n- Data masking and tokenization\n- End-to-end privacy across streaming sinks\n- Key management and rotation\n- Data lineage and auditing\n\n## Code Example\n\n```javascript\n// Pseudocode: masks PII before Kafka publish\nfunction maskPII(record, tenantSalt){\n  if(!record) return record;\n  record.email = hash(record.email + tenantSalt);\n  record.phone = redactPhone(record.phone);\n  return record;\n}\n```\n\n## Follow-up Questions\n\n- How would you rotate masking keys without service downtime?\n- How would you validate that analytics still yield correct aggregates with masked data?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-4",
        "questionId": "q-22873",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis deployed across three regions. During regional outages, reads become stale and writes incur latency. Propose a concrete plan for cross-region replication, consistency, and failover to maintain availability and data integrity. Include topology, cache invalidation, event replay, tests, and rollback criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect end-to-end latency, queue depth, and replica lag; enable slow query logs and run explain on top slow queries; verify index coverage with compound indexes; apply index hints or add a composite",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a 3-region setup: Postgres primary in region A with synchronous commits for critical writes, asynchronous replication to regions B and C. Redis caches local to each region with invalidation via",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Enforce PostgreSQL unique constraint on order_id; use a transactional outbox to publish to Kafka after write; implement Redis-based idempotency (SETNX) per (user_id, order_id) TTL 1h; enable Kafka pro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an idempotent event processing path using the transactional outbox pattern combined with Kafka's exactly-once semantics. Assign a globally unique event_id (UUID v4 or snowflake) to each event. Within a single Pos…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of multi-region availability, data integrity, and disaster recovery in CNPA stacks.\n\n## Key Concepts\n- Multi-region replication topology and consistency guarantees\n- Cross-region cache invalidation strategies\n- Event replay/read-side idempotence\n- Canary deployments, rollback criteria, and validation of SLAs\n\n## Code Example\n```yaml\nreplication:\n  primaryRegion: us-east-1\n  syncCommit: true\n  replicas:\n    - us-west-2\n    - eu-central-1\ncache:\n  regional:\n    - region: us-east-1\n      name: redis-east\n```\n\n## Follow-up Questions\n- How would you validate no data loss during region failover?\n- Which metrics signal cross-region issues and how would you respond?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-5",
        "questionId": "q-11641",
        "question": "Design an end-to-end observability plan for a CNPA stack (HTTP API, PostgreSQL, Kafka, Redis) that adds cross-service tracing. How would you propagate a trace across HTTP, DB write, Kafka publish, and Redis reads, and what are the concrete instrumentation points, span relationships, sampling rules, and alerting? Include verification steps with realistic workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enforce end-to-end tenant isolation: PostgreSQL: enable RLS on all tables and require tenant_id in every query; API: extract tenant_id from JWT and enforce it in all endpoints; Redis: namespace keys w",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Focus on running targeted micro-traces: enable per-tenant metrics in DB queries, Kafka producer acks, and Redis cache misses; compare hot tenants vs cold; use query plans, examine lock waits, enable p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement end-to-end tracing with OpenTelemetry across HTTP API, PostgreSQL, Kafka, and Redis. Thread a trace-id through HTTP requests, Kafka messages, and Redis reads; create spans for HTTP, DB, Kafk",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Instrument all CNPA services with OpenTelemetry; propagate trace context across HTTP, SQL via JDBC/ORM, Kafka producers/consumers, and Redis clients; create spans for HTTP server, DB query, Kafka publ",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nObservability across CNPA is critical for diagnosing bottlenecks and ensuring data integrity in high-throughput pipelines. This question tests end-to-end tracing design, context propagation, sampling strategies, and verification under load.\n\n## Key Concepts\n\n- Distributed tracing\n- OpenTelemetry\n- Span relationships\n- Adaptive sampling\n- End-to-end verification\n\n## Code Example\n\n```javascript\n// Pseudo OpenTelemetry init\nconst tracer = otel.trace.getTracer('cnpa')\n```\n\n## Follow-up Questions\n\n- How would you handle sampling drift across regions?\n- How would you validate that telemetry overhead does not skew latencies?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-6",
        "questionId": "q-2351",
        "question": "CNPA pipeline: HTTP API writes to PostgreSQL and publishes events to Kafka. A rollout requires strict per-user ordering across a high-throughput, multi-partition topic; out-of-order deliveries break dashboards. Describe a concrete plan to guarantee per-user ordering while preserving throughput: data-path changes (outbox with transactional writes, per-user partitioning), producer settings (acks=all, enable.idempotence, max.in.flight=1, batch/linger), idempotent sinks, testing (replay, verifications per user), and rollback. Include concrete metrics and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement request coalescing at the API layer and a Redis-based lock (SETNX) to serialize concurrent cache misses, with TTL-based refreshes to avoid stampede. Add per-key rate limits and a circuit bre",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition by user_id; use an outbox and publish via Kafka transactions with idempotence; max.in.flight=1; acks=all; ensure a single partition per user to preserve order; sinks must be idempotent; test",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a transactional outbox: writes to Postgres enqueue Kafka; Redis keyed per product with per-segment TTL and STALE-WHILE-REVALIDATE; canary with feature flag, backfill in batches, monitor cach",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an end-to-end tracing plan using OpenTelemetry to identify bottlenecks across HTTP, PostgreSQL, Kafka, and Redis. Propagate trace context via HTTP headers (W3C Trace Context or B3), Kafka headers (using the same…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nNew per-user ordering constraint across partitions requires understanding coupling points and strong guarantees.\n\n## Key Concepts\n- Per-user partitioning and outbox pattern\n- Kafka transactions, idempotence, max.in.flight\n- Idempotent sinks and strict ordering verification\n\n## Code Example\n```javascript\n// Pseudo: write to outbox and commit Kafka txn atomically\nawait db.transaction(async trx => {\n  await trx.into('outbox').insert({user_id, event, seq});\n  await kafkaProducer.beginTransaction();\n  await kafkaProducer.send({topic, messages:[{key: String(user_id), value: event}]});\n  await kafkaProducer.commitTransaction();\n});\n```\n\n## Follow-up Questions\n- How would you monitor and alert on per-user ordering violations?\n- How would you handle a hot user skew if a single user dominates partition leadership?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-7",
        "questionId": "q-18677",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis handles user_session events. A canary release leads to duplicates and occasional misses under peak load. Propose a concrete plan to achieve end-to-end exactly-once processing: describe how to implement Kafka transactions on the producer, DB upserts, and a Redis-based dedup mechanism, plus idempotent consumers and rollback/verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: enable BACKWARD compatibility in the schema registry; evolve Avro by adding region as an optional field with default 'unknown'. Deploy a new producer version to a canary and update the consumer",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Active-active across Regions A and B with Kafka MirrorMaker 2 and PostgreSQL logical replication. Use an outbox to coordinate writes, enforce idempotent consumers, and track causal offsets. Route read",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To diagnose performance bottlenecks during peak load, I'll implement a systematic debugging approach across the CNPA stack. First, deploy distributed tracing across all components to establish end-to-end visibility. For…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "End-to-end exactly-once: wrap HTTP->Kafka->DB in transactional boundaries; use session_id as the PK and perform atomic upserts; add Redis-based dedup keyed by session_id+offset with TTL; ensure idempo",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of end-to-end exactly-once semantics in a CNPA stack, covering transactional boundaries, dedup strategies, and failure scenarios during peak load.\n\n## Key Concepts\n\n- Exactly-once processing across HTTP, Kafka, and DB\n- Kafka transactions and producer idempotence\n- Upsert patterns in PostgreSQL with stable keys\n- Redis-based deduplication and TTL management\n- Idempotent consumers and rollback/verification\n\n## Code Example\n\n```javascript\n// Pseudo-code: dedup check before DB upsert\nif (!redis.exists(session_id)) {\n  beginTransaction()\n  upsertPostgres(session_id, payload)\n  redis.set(session_id, true, ttl=60)\n  commitTransaction()\n}\n```\n\n## Follow-up Questions\n\n- How would you test failure scenarios ( broker downtime, DB split-brain )? \n- What metrics indicate successful exactly-once behavior during load tests?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-8",
        "questionId": "q-18260",
        "question": "In a CNPA stack with an HTTP API, PostgreSQL, and Kafka, a downstream Elasticsearch index and Redis cache must remain in sync with user profile updates under peak load. Design a concrete plan to guarantee cross-service consistency and low latency: outline the data path, how to implement an atomic write with an outbox, idempotent sink to Elasticsearch, cache invalidation, and a reconciliation process; specify metrics, rollback and testing steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Read-Through Cache Implementation for GET /user/{id}** Implement a read-through cache for user profiles using Redis as a cache-aside layer: 1. **Cache Hit**: On request, check Redis first. If the user profile exists,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Require Idempotency-Key header for POST /orders; store key in Redis with 24h TTL; insert order with INSERT INTO orders(idempotency_key, user_id, amount, status) VALUES($1,$2,$3,'OPEN') RETURNING id ON CONFLICT(idempoten…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a transactional outbox in Postgres to atomically persist the update and publish a Kafka event. Build an idempotent Elasticsearch sink keyed by (user_id, version) that upserts docs, plus Redi",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement request coalescing at the API layer and a Redis-based lock (SETNX) to serialize concurrent cache misses, with TTL-based refreshes to avoid stampede. Add per-key rate limits and a circuit bre",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes cross-service consistency and end-to-end flow under load, a realistic pain point when CNPA spans DB, Kafka, and search/cache sinks.\n\n## Key Concepts\n\n- Transactional outbox pattern across Postgres and Kafka\n- Idempotent sinks to Elasticsearch\n- Cache invalidation and read-your-writes strategy with Redis\n- Reconciliation and drift detection\n\n## Code Example\n\n```sql\nCREATE TABLE outbox (\n  id BIGINT PRIMARY KEY,\n  aggregate_type TEXT NOT NULL,\n  aggregate_id TEXT NOT NULL,\n  event_type TEXT NOT NULL,\n  payload JSONB NOT NULL,\n  created_at TIMESTAMPTZ NOT NULL,\n  processed BOOLEAN DEFAULT FALSE,\n  processed_at TIMESTAMPTZ\n);\n```\n\n```python\n# Pseudo Python sink sketch\ndef process_outbox_batch(batch):\n    for row in batch:\n        if row['processed']:\n            continue\n        key = (row['aggregate_id'], row['event_type'], row['payload'].get('version'))\n        if dedupe_store.exists(key):\n            mark_processed(row['id'])\n            continue\n        es.index(index='users', id=row['aggregate_id'], body=row['payload'])\n        dedupe_store.insert(key)\n        mark_processed(row['id'])\n```\n\n## Follow-up Questions\n\n- How would you monitor drift between Postgres, Kafka, ES, and Redis, and what would trigger a hotfix rollout?\n- What changes would you make for multi-region deployments with eventual consistency?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-9",
        "questionId": "q-3340",
        "question": "You run a CNPA stack where an HTTP API ingests events, writes to PostgreSQL, and publishes to Kafka with Avro schemas; analytics consume the Kafka topics. After a schema change, new events fail to publish while old consumers parse v1. Outline a concrete, end-to-end migration plan to keep services live with no downtime. Include steps, metrics, and concrete changes (schema registry usage, topic versions, dual writing, canary) and how you would verify success?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use deterministic event_id and multi-layer dedupe: require client to send event_id; enforce unique PK on (event_id) in Postgres; cache seen_ids in Redis (TTL) to drop duplicates at HTTP layer; ensure",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a staged schema evolution with a registry and topic versioning. Run backward-compatible changes first, publish to a new topic v2, and dual-write during migration. Canary the rollout, validate en",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Enforce PostgreSQL unique constraint on order_id; use a transactional outbox to publish to Kafka after write; implement Redis-based idempotency (SETNX) per (user_id, order_id) TTL 1h; enable Kafka pro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a write-through caching strategy with Redis as the authoritative read source for user profiles. On each PostgreSQL write, immediately update Redis synchronously before confirming the write as complete. Use a 60-se…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to manage schema evolution in CNPA pipelines with zero downtime, cross-language compatibility, and reliable validation. It probes practical use of a schema registry, topic versioning, and staged rollout.\n\n## Key Concepts\n- Schema evolution and backward/forward compatibility\n- Kafka Schema Registry and topic versioning\n- Dual-writing and canary rollout\n- End-to-end data validation and monitoring\n- Rollback and decommission strategy\n\n## Code Example\n```yaml\nschema_registry:\n  url: https://registry.example\ncompatibility: BACKWARD\ntopics:\n  v1: events.v1\n  v2: events.v2\n```\n\n## Follow-up Questions\n- How would you enforce compatibility across multiple languages and teams?\n- How would you test migration with real traffic in staging and ensure no data loss?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-10",
        "questionId": "q-18440",
        "question": "CNPA stack with an HTTP API, PostgreSQL, Kafka, and Redis. A GDPR-like request comes in to delete a user’s PII. Propose a concrete, low-downtime plan to purge data from Postgres, emit tombstone events to Kafka, and invalidate Redis caches, ensuring end-to-end data lineage and verifiability. Include data flow steps, rollback strategy, and measurable checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outbox pattern in PostgreSQL: in the same transaction, insert (session_id, event_type, payload, created_at) into sessions and into an outbox table. Kafka: transactional producer with a unique 'transac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Add a nullable address_url column (TEXT) with no backfill by default; deploy API with a feature flag that exposes the field only when enabled; run a batch job to backfill existing users in small chunk",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement batched non-blocking migration (add nullable column, backfill in small batches), with advisory locks and short timeouts to avoid long locks; roll out via feature flag/canary; Kafka: enable i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: redact PII in Postgres in a single transaction (set emails/PII to NULL, delete or redact), add an outbox tombstone in the same transaction, and have a durable sink publish tombstones to Kafka an",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests end-to-end data deletion across HTTP API, Postgres, Kafka, and Redis, with strong consistency guarantees and verifiability.\n\n## Key Concepts\n- Outbox pattern for cross‑service guarantees\n- Soft delete vs redaction and deleted_at metadata\n- Kafka tombstones and idempotent sinks\n- Cache invalidation strategies and visibility delays\n- End-to-end verification and data lineage\n\n## Code Example\n```javascript\n// Pseudocode: redact and record tombstone in one transaction\nawait client.query('BEGIN');\nawait client.query(\n  `UPDATE users SET email=NULL, pii=NULL, phone=NULL, redacted=true, deleted_at=NOW() WHERE id=$1`,\n  [userId]\n);\nawait client.query(\n  `INSERT INTO outbox_tombstones (topic, key, payload, emitted_at) VALUES ('user.deletes', $1, $2, NOW())`,\n  [userId, JSON.stringify({ deleted_at: new Date().toISOString() })]\n);\nawait client.query('COMMIT');\n```\n\n## Follow-up Questions\n- How would you test this in a multi-region setup with eventual consistency?\n- What metrics would you monitor to ensure no PII remains and tombstones are processed correctly?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-11",
        "questionId": "q-11089",
        "question": "In a CNPA stack with an HTTP API, PostgreSQL, and Kafka, implement end-to-end tracing with OpenTelemetry. Outline a concrete plan to propagate TraceContext across HTTP, DB calls, and Kafka producers/consumers, specify span names, and a sampling rate. Describe a synthetic end-to-end test that exercises all three components and how you verify trace integrity?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Add a nullable JSONB consent column on users; keep API backward compatible by treating missing as null. Use transactional outbox to publish writes to Kafka. Implement a feature flag to enable the new",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an atomic outbox pattern. In the HTTP handler's DB transaction, insert into audit_logs and into an outbox table for Kafka, then commit. Include correlation_id, user_id, action, endpoint, and times",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement comprehensive end-to-end tracing with OpenTelemetry across the HTTP API, PostgreSQL, and Kafka components. Configure TraceContext propagation through HTTP headers, database connection parameters, and Kafka mes…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan to enforce transactional outbox in DB, idempotent Kafka sinks, and end-to-end reconciliation with hash-based lineage; measure tail latency, duplicate rate, backfill coverage; automate verificatio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEnd-to-end tracing across CNPA stacks reveals performance bottlenecks, service dependencies, and ensures system observability in distributed architectures.\n\n## Key Concepts\n\n- OpenTelemetry instrumentation and configuration\n- W3C Trace Context propagation mechanisms\n- Structured span naming conventions\n- Probability-based sampling strategies\n- Cross-component trace integrity verification\n\n## Code Example\n\n```javascript\n// Pseudo-code for OpenTelemetry initialization and context injection\nconst tracer = opentelemetry.trace.getTracer('cnpa-service');\n// Configure span creation",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-12",
        "questionId": "q-903",
        "question": "In a CNPA stack, an HTTP API writes to Postgres, publishes to Kafka, and a Redis-backed dashboard consumes the stream. During peak load, HTTP latency tails spike. Provide a concrete debugging plan to isolate whether the bottleneck is HTTP, DB, Kafka, producer, consumer, or Redis, with exact metrics, sampling, and concrete changes (pooling, prepared statements, acks, batch sizes, caching strategies) and how you would verify impact?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use cache-aside with Redis TTLs, a simple mutex for token refresh, and explicit logout invalidation. Add a Redis Lua script for atomic refresh, ensure idempotent refresh, and run a canary test simulat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement circuit breakers and timeouts between your HTTP API and PostgreSQL/Kafka services. For thresholds, use 5 consecutive failures within a 10-second rolling window to trip the breaker, with a 300ms per-call timeou…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "# Debugging High Latency in a CNPA Stack ## The Problem Your HTTP API writes to Postgres, publishes to Kafka, and a Redis-backed dashboard reads from Kafka. During peak hours, some requests become very slow. We need to…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Isolate each layer under controlled load and measure per-layer tail latency (p95/p99) and error rates to attribute bottlenecks accurately. Implement bulk writes with MongoDB writeConcern='majority' and journaling for du…",
            "isCorrect": false
          }
        ],
        "explanation": "# Why This Matters\n\nThis question tests real-world debugging skills for distributed systems. You'll face these problems at any backend or SRE job.\n\n## What You Need to Know\n\n1. **Tracing**: Use OpenTelemetry/Jaeger to find which service is slow\n2. **Postgres**: Connection pooling, query optimization, indexes\n3. **Kafka**: Batch sizes, acknowledgment settings, consumer lag\n4. **Redis**: Pipelines, connection pools, local caching\n\n## Key Metrics to Watch\n\n- p50, p95, p99 latency at each layer\n- Connection pool utilization\n- Consumer lag\n- CPU and memory usage\n\n## How to Practice\n\nSet up a test stack locally and intentionally slow down each component to see how it behaves.",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-13",
        "questionId": "q-15950",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis must enforce a per-user rate limit of 60 requests per minute during peak. Propose a beginner-friendly plan to implement and verify this limit end-to-end: where to enforce (gateway, API service, or Redis Lua script), how to model the quota (bucket vs fixed window), concrete config changes, how to monitor latency/throughput, and a minimal test plan that avoids false positives under jitter?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Require Idempotency-Key header for POST /orders; store key in Redis with 24h TTL; insert order with INSERT INTO orders(idempotency_key, user_id, amount, status) VALUES($1,$2,$3,'OPEN') RETURNING id ON CONFLICT(idempoten…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Require Idempotency-Key header; if missing, generate one server-side. Use a Redis Lua script to atomically check-and-set the key with a TTL (24h). If exists, return cached result. If new, write to Pos",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Require event_id on HTTP; route through a transactional outbox in PostgreSQL, linking DB commit with outbox readiness; publish via a Kafka producer with transactions, committing once the outbox row is",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Redis-backed per-user token bucket (60 reqs/min) enforced at the API gateway with a Lua script that atomically checks and decrements tokens; if allowed, proceed to PostgreSQL and publish t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical rate-limiting design across CNPA, focusing on correctness and minimal latency impact.\n\n## Key Concepts\n- Token bucket and fixed-window rate limiting\n- Redis Lua scripting for atomic ops\n- Gateway vs service-level enforcement\n- Observability: latency, misses, saturation\n\n## Code Example\n```javascript\n// Redis Lua script: atomically check/decrement tokens\nconst lua = `\nlocal key = KEYS[1]\nlocal toTake = tonumber(ARGV[1])\nlocal tokens = tonumber(redis.call('GET', key) or '0')\nif tokens >= toTake then\n  redis.call('DECRBY', key, toTake)\n  return 1\nelse\n  return 0\nend\n` \n```\n\n## Follow-up Questions\n- How would you handle burst traffic without allowing abuse?\n- How would you test for false positives under jitter?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-14",
        "questionId": "q-30518",
        "question": "CNPA stack with HTTP API, MongoDB, Kafka, and Redis spans two regions. A write of an order updates MongoDB and publishes to Kafka; reads show stale data after a regional failover. Design a concrete debugging plan and fix to guarantee monotonic reads post-failover, including steps, metrics, and concrete changes (transaction boundaries, read/write concerns, Kafka exact-once via idempotent producer and outbox, Redis cache invalidation) and how you'd verify impact?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement middleware that emits structured audit events to a Kafka audit topic and persists to a Postgres audit table; fields: action, user_id, resource, timestamp, correlation_id. Redact emails and p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument end-to-end latency with per-hop timers and a 1k-sample peak window; isolate by bypassing the ML inference or vector-store path; validate with synthetic load on the ML service; track p95/p99",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a staged migration with a feature flag: keep old schema active while introducing new Avro schema and a new Postgres column; enable dual-write to both old and new Kafka topics and to a shadow",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Wrap the order write in a MongoDB multi-document transaction with readConcern snapshot and writeConcern majority, and use an outbox pattern to publish to Kafka with an idempotent producer. Invalidate",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical handling of cross-region failover, monotonic reads, and coupling between database, messaging, and cache layers in a CNPA stack.\n\n## Key Concepts\n- MongoDB transactions across shards/replicas\n- Read/Write concerns for consistency\n- Outbox pattern for reliable Kafka publishing\n- Redis cache invalidation strategies\n- Observability: replication lag, read-after-write latency, cache metrics\n\n## Code Example\n```javascript\n// Pseudo-node example using MongoDB driver\nconst session = client.startSession();\ntry {\n  await session.withTransaction(async () => {\n    await orders.insertOne(order, { session });\n    await outbox.insertOne({\n      aggregateId: order._id,\n      event: 'ORDER_CREATED',\n      payload: { /* ... */ },\n      processed: false\n    }, { session });\n  }, { readConcern: { level: 'snapshot' }, writeConcern: { w: 'majority' } });\n} finally {\n  await session.endSession();\n}\n```\n\n## Follow-up Questions\n- How would you test the failover scenario end-to-end and validate no monotonicity regression?\n- Which metrics dashboards would you instrument to detect regressions after deploys?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-15",
        "questionId": "q-8718",
        "question": "A CNPA stack with an HTTP API, PostgreSQL, Kafka, and Redis must enforce strict per-tenant data isolation under a new regulator rule. Design an end-to-end plan that prevents cross-tenant data leakage while preserving performance at scale. Detail schema, access control, messaging boundaries, and testing/verification steps, with concrete implementation choices?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable Kafka MirrorMaker 2 for cross-region topic replication, deploy Redis replicas in both regions with local reads and bounded-staleness fallbacks, route reads to local cache and use region failover when needed, and…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use transactional outbox: wrap profile update and an outbox row in one DB transaction. A separate idempotent sink publishes the outbox to Kafka, then marks sent. Invalidate Redis on commit success. Ha",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enforce end-to-end tenant isolation: PostgreSQL: enable RLS on all tables and require tenant_id in every query; API: extract tenant_id from JWT and enforce it in all endpoints; Redis: namespace keys w",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement adaptive concurrency limits at the HTTP layer, circuit-breakers per endpoint, Kafka consumer pause/resume on lag, and a Redis cooldown with guarded warmup. Instrument p95 latency, qps, Kafka",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical, implementation-focused thinking about end-to-end data isolation in a CNPA stack, not just theory.\n\n## Key Concepts\n- PostgreSQL Row-Level Security with tenant_id\n- JWT-based tenant binding and API gating\n- Redis namespaces and TTLs per tenant\n- Kafka headers/ACLs and per-tenant routing\n- End-to-end testing and auditability\n\n## Code Example\n```sql\n-- example RLS policy\nCREATE POLICY tenant_rls ON users\n  USING (tenant_id = current_setting('myapp.tenant_id')::int);\n```\n\n## Follow-up Questions\n- How would you validate there’s zero cross-tenant leakage under peak load?\n- What changes to monitoring would you add to spot violations in real time?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-16",
        "questionId": "q-24429",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis needs end-to-end tracing to diagnose intermittent latency. Design a beginner-friendly plan to add lightweight OpenTelemetry tracing across HTTP handlers, DB calls, and Kafka producers, ensuring trace context propagates via HTTP headers, DB drivers, and Kafka config. Include steps, libraries, sampling, exporters, and validation of full-path traces end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by reproducing peak-load conditions and tracing where latency spikes first appear. Instrument three rails: HTTP handler (execution time, blocking I/O), DB pool (max connections, active/waiting),",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use the Outbox pattern. On HTTP signup, in one DB transaction insert the user and an outbox row (aggregate_id, event_type, payload, processed=false). A separate worker publishes outbox events to Kafka",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: measure latency with tracing on vs off, log payload size, exporter queue times, CPU and GC. Do AB test with tracing disabled on hot HTTP paths, enable adaptive tail sampling, and trim attributes",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement lightweight OpenTelemetry tracing across the CNPA stack. Propagate TraceContext from HTTP requests through DB calls, Kafka producers, and Redis reads; instrument HTTP handlers, DB drivers, a",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nEnd-to-end tracing across HTTP, DB, Kafka, and Redis helps diagnose issues that span services and reveals propagation gaps.\n\n## Key Concepts\n- OpenTelemetry\n- Trace Context propagation\n- Span lifetimes and sampling\n- Exporters and collectors\n\n## Code Example\n```javascript\n// Minimal OpenTelemetry setup and propagation\nconst tracer = opentelemetry.trace.getTracer('cnpa');\napp.use((req, res, next) => {\n  const span = tracer.startSpan('HTTP '+ req.path);\n  req.span = span;\n  res.on('finish', ()=> span.end());\n  next();\n});\n```\n\n## Follow-up Questions\n- How would you verify traces in the collector UI?\n- How would you handle retry loops that disrupt trace continuity?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-17",
        "questionId": "q-28540",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and a data lake sink is introducing a background enrichment worker that consumes from Kafka, fetches user profile data from PostgreSQL, and republishes enriched events to Kafka and the data lake. During onboarding, enrichment yields duplicates and occasional misses. Design a beginner-friendly plan to achieve idempotent enrichment and end-to-end exactly-once delivery, with concrete changes (message IDs, dedup cache, transactional writes, producer config), tests, and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Focus on running targeted micro-traces: enable per-tenant metrics in DB queries, Kafka producer acks, and Redis cache misses; compare hot tenants vs cold; use query plans, examine lock waits, enable p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement end-to-end tracing to isolate the HTTP-to-Redis data path. Instrument Kafka producers with per-partition timestamps to detect ordering issues; monitor consumer lag specifically for the new topic; validate cons…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose: 1) Per-tenant quotas at the API gateway with a global in-flight request cap to prevent any single tenant from monopolizing resources; 2) HTTP handler implements circuit breakers and request deduplication to bac…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt an idempotent enrichment flow: attach a stable event_id, use a Redis-based dedup cache per event_window, and enable Kafka producer idempotence. Wrap DB read, enrichment, and publish in a single",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to reason about idempotency and delivery guarantees in CNPA patterns, at a beginner level, while nudging toward practical implementation details.\n\n## Key Concepts\n\n- Idempotent enrichment\n- Exactly-once delivery\n- Outbox pattern\n- Deduplication cache\n- Producer config (idempotence, acks)\n\n## Code Example\n\n```javascript\n// Pseudo: dedup check before processing\nasync function enrich(event, db, cache) {\n  const id = event.event_id;\n  if (await cache.has(id)) return; // already processed\n  const profile = await db.getProfile(event.user_id);\n  const enriched = { ...event, profile };\n  await cache.add(id, /* ttl */ 60000);\n  await publishToKafka(enriched);\n  await writeOutbox(enriched);\n}\n```\n\n## Follow-up Questions\n\n- How would you test edge cases like cache eviction and outbox failures?\n- How would you adapt if events arrive out of order or with duplicate IDs?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-18",
        "questionId": "q-21086",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, Redis, and a new personalization endpoint. During peak load, downstream personalization outputs arrive out of order, causing mismatches in user segments. Propose a concrete plan to guarantee correct ordering and at-least-once delivery across HTTP, DB writes, Kafka, and Redis, with exact steps, metrics to collect, and concrete fixes (sequence IDs, transactional outbox, Kafka idempotence, cache invalidation)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a request-id per submission and make request_id unique in Postgres; perform INSERT into the events table with ON CONFLICT DO NOTHING so retries don't duplicate writes. Use a transactional outbox pattern with a Kafka…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement end-to-end sequence IDs and transactional outbox. Propagate a per-user sequence from HTTP to DB and to Kafka; use an outbox table to atomically commit DB writes and event payloads; enable Ka",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Add a nullable address_url column (TEXT) with no backfill by default; deploy API with a feature flag that exposes the field only when enabled; run a batch job to backfill existing users in small chunk",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement the outbox pattern: atomically write the event to the main table and an outbox table within one DB transaction, then flush the outbox row to Kafka using a transactional producer (enable.idem",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests end-to-end consistency in a CNPA pipeline, focusing on ordering, dedup, and cross-service integrity under load.\n\n## Key Concepts\n- End-to-end ordering across HTTP, DB, Kafka, Redis\n- Transactional outbox pattern\n- Kafka idempotence and acks\n- Cache invalidation and deduplication\n- Monitoring: lag, duplicates, tail latency\n\n## Code Example\n```sql\nBEGIN;\nINSERT INTO customers (...) VALUES (...);\nINSERT INTO outbox (aggregate_type, aggregate_id, payload, occurred_at) VALUES (...);\nCOMMIT;\n```\n\n```java\nProperties props = new Properties();\nprops.put(\"enable.idempotence\",\"true\");\nprops.put(\"acks\",\"all\");\n```\n\n## Follow-up Questions\n- How would you test end-to-end under load for ordering guarantees?\n- What rollback strategy ensures no duplicates if the outbox commit partially fails?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-19",
        "questionId": "q-24589",
        "question": "In a CNPA stack with an HTTP API, PostgreSQL, and Kafka, design an end-to-end deduplication strategy to ensure at-least-once retries do not create duplicates across HTTP requests, DB writes, and Kafka publishes. Include where to store idempotency keys, how to handle retries, and how you would verify correctness under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a versioned envelope for events. Step 1: add a new optional field in a v2 payload and publish to a bridging topic while continuing v1 writes. Step 2: extend consumers to accept both versions",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement region-aware offset reconciliation with a transactional outbox pattern in PostgreSQL to ensure Kafka offsets align with Snowflake ingestion timestamps. Use idempotent Snowflake sinks with MERGE upserts keyed o…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an idempotency key per request. In PostgreSQL, upsert a deduplication row first, then perform the DB write and an outbox insert in a single transaction. Finally, publish the outbox entries to Kafka. On retry, detect…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Postgres Row-Level Security with a tenant_id on all tables and policies enforcing it; pass tenant_id from API and attach to Kafka headers; namespace Redis keys per tenant. Migration: add tenant_id",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end reliability in CNPA pipelines and familiarity with patterns like idempotency, the outbox pattern, and transactional writes across databases and messaging systems.\n\n## Key Concepts\n- Idempotency keys to uniquely identify requests\n- Outbox pattern for atomic DB and Kafka writes\n- Transactional writes ensuring consistency\n- Retry handling with deduplication checks\n- Load testing to verify correctness under scale\n\n## Code Example\n```javascript\nasync function upsertDedup(key, payload) {\n  const client = await pool.connect();\n  try {\n    await client.query('BEGIN');\n    \n    // Upsert idempotency key\n    await client.query(\n      'INSERT INTO idempotency_keys (key, result) VALUES ($1, NULL) ON CONFLICT (key) DO NOTHING',\n      [key]\n    );\n    \n    // Check if already processed\n    const existing = await client.query(\n      'SELECT result FROM idempotency_keys WHERE key = $1',\n      [key]\n    );\n    if (existing.rows[0]?.result) {\n      await client.query('COMMIT');\n      return JSON.parse(existing.rows[0].result);\n    }\n    \n    // Process and write to outbox\n    await client.query(\n      'INSERT INTO outbox (payload) VALUES ($1)',\n      [JSON.stringify(payload)]\n    );\n    \n    await client.query('COMMIT');\n  } catch (e) {\n    await client.query('ROLLBACK');\n    throw e;\n  } finally {\n    client.release();\n  }\n}\n```\n\n## Verification Under Load\n- Run concurrent retries with identical idempotency keys\n- Verify no duplicate Kafka messages appear\n- Check database consistency after forced failures mid-transaction\n- Test with high-throughput load to ensure lock contention is manageable",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-20",
        "questionId": "q-27547",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis faces intermittent stale reads after a partial outage and reconciliation. Propose a concrete plan to achieve robust eventual consistency: implement an outbox pattern, ensure exactly-once ingestion into Kafka, design idempotent sinks to Redis, and include a reconciliation workflow and test plan for outages, with concrete changes and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an online, zero-downtime schema migration by first adding a nullable boolean column `customer_email_verified`, then backfilling data in batches using cursor-based pagination. Route reads and writes through fea…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement OpenTelemetry across all services in the CNPA stack. Create a root span for each HTTP request and child spans for Postgres queries, Kafka producer/consumer operations, and Redis commands. Propagate trace conte…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enable a schema registry (Avro) with backward and forward compatibility. Use an outbox/dual-write so Postgres and Kafka stay in sync during migration. Roll out in canaries, validate end-to-end traces",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an outbox table in Postgres, publish to Kafka within a DB transaction, and build idempotent sinks in Redis keyed by (entity_id, event_id). Add a reconciliation pass that confirms all publish",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-service consistency, patterns to prevent data drift, and failure scenarios.\n\n## Key Concepts\n- Outbox pattern, exactly-once semantics, idempotent sinks\n- Reconciliation, deduplication, TTL tuning\n- Observability and tests under outages\n\n## Code Example\n```javascript\n// Pseudo: check idempotent sink\nasync function applyEvent(event){\n  const key = `${event.entity}:${event.id}`;\n  if(await redis.get(key)) return; // dedupe\n  await redis.set(key, true, 'EX', TTL);\n  // apply to cache\n}\n```\n\n## Follow-up Questions\n- How would you test reconciliation under Kafka retention constraints?\n- What metrics signal drift after recovery?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-21",
        "questionId": "q-9229",
        "question": "CNPA stack with an HTTP API, PostgreSQL, Kafka, and Redis. A regulatory requirement demands deterministic replay of user activity for audits, while keeping latency stable. Design a concrete plan to implement exactly-once event emission using the outbox pattern, transactional writes, and Kafka transactions, including schema changes, test plans, rollback, and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a staged schema evolution with a registry and topic versioning. Run backward-compatible changes first, publish to a new topic v2, and dual-write during migration. Canary the rollout, validate en",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use asynchronous enrichment with backpressure and a per-key rate limiter; add a two-tier cache (in-process TTL plus Redis) to reduce external lookups; make writes idempotent (upsert in Postgres) and u",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement Outbox: wrap User insert and an Outbox row in a single DB transaction; flush to Kafka via a transactional, idempotent producer, using a stable key (user_id + event_id) and a Kafka transactio",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement the outbox pattern with transactional writes from the HTTP API to Postgres and a Kafka producer that commits within the same transaction. Add an outbox table, a tx_id, and a background worke",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes depth in cross-service consistency, auditing needs, and production-ready reliability for CNPA stacks.\n\n## Key Concepts\n- Outbox pattern with transactional writes\n- Kafka transactions and idempotent producers\n- Postgres row-level integrity and auditing\n- End-to-end replay testing and deduplication checks\n\n## Code Example\n```javascript\n-- Postgres: define outbox and usage (shown in SQL, but presented here for syntax highlighting)\nCREATE TABLE outbox (\n  id BIGINT PRIMARY KEY,\n  event_type TEXT,\n  payload JSONB,\n  processed BOOLEAN DEFAULT FALSE,\n  occurred_at TIMESTAMPTZ DEFAULT now(),\n  tx_id TEXT\n);\n```\n\n## Follow-up Questions\n- How do you test end-to-end replay in a multi-region deployment?\n- What are failure modes and how would you prevent unbounded outbox growth?",
        "difficulty": "advanced"
      },
      {
        "id": "cnpa-q-22",
        "questionId": "q-7846",
        "question": "CNPA stack: HTTP API, PostgreSQL, Kafka, Redis. A user requests data deletion under GDPR. Design a beginner-friendly plan to purge all personal data across the HTTP API, DB, Kafka, and Redis. Include concrete changes (soft-delete flag, tombstones, log compaction, cache invalidation) and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a safe rollout using a backward-compatible schema: add a nullable customer_segment field in the event, publish both schemas via a Schema Registry, dual-write to ensure old and new sinks read b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a beginner-friendly GDPR delete flow across CNPA: a delete request triggers Postgres soft-delete (deleted_at), publishes a tombstone to Kafka with key=user_id, enables topic compaction, and inv",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: introduce the field additively with backward compatibility. Use a schema-registry with BACKWARD compatibility, publish events to a new topic version or mark the field optional. Validate HTTP inp",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: implement per-user rate limiting with a Redis Lua script that atomically increments a counter (limit 5/min) with a 60s TTL. Use an idempotency-key header to cache responses and ensure retries re",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTest understanding of cross-service data deletion, tombstones, and compliance in a CNPA stack.\n\n## Key Concepts\n\n- GDPR delete flow across API, DB, Kafka, Redis\n- Soft-delete vs hard delete\n- Kafka tombstones and log compaction\n- Cache invalidation and read consistency\n- End-to-end verification and auditability\n\n## Code Example\n\n```javascript\n// Pseudo snippet: mark rows deleted and publish tombstone\nawait db.query(\"UPDATE users SET deleted_at = NOW() WHERE user_id = $1\", [userId]);\nproducer.send({ key: userId, value: null, topic: 'user-events' });\n```\n\n## Follow-up Questions\n\n- How would you test race conditions with concurrent deletes?\n- How would you scale tombstone handling for large datasets?",
        "difficulty": "beginner"
      },
      {
        "id": "cnpa-q-23",
        "questionId": "q-10215",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis shows rising tail latency in the checkout flow under peak load. Design a concrete end-to-end tracing plan to determine whether bottlenecks are in the HTTP handler, DB path, Kafka path, or Redis cache. Include trace propagation details, exact metrics per layer, sampling strategy, and concrete changes (timeouts, pooling, prepared statements, idempotent sinks, cache invalidation) and a verification plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Redis-based lockout flag with TTL (e.g., 15 minutes). Sign-in checks first read Redis; if locked, return 429. If unlocked, verify policy in Postgres and on failure publish a Kafka event wi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument with OpenTelemetry to trace HTTP handler, DB queries, and Kafka publish; collect p95/p99 latency, error rate, and queue backpressure. Reproduce under load (k6). If HTTP slow, tune keep-aliv",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use explicit invalidate: keys product:list and product:ID. On write: update DB, delete both keys, publish a Kafka event to invalidate caches. TTL 60s as safety net. Employ a lightweight distributed lo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create an end-to-end tracing plan for checkout across HTTP API, PostgreSQL, Kafka, and Redis under peak load. Define trace propagation, per-layer metrics (p50/p95/p99, tail latency, errors), sampling",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design end-to-end observability and actionable debugging plans for critical checkout flows, ensuring latency budgets are met under peak load and across service boundaries.\n\n## Key Concepts\n\n- Distributed tracing (OpenTelemetry), trace context propagation (traceparent, baggage)\n- End-to-end latency budgets and tail latency analysis\n- Per-layer metrics: HTTP, DB (latency, queue time), Kafka (producer/consumer lag), Redis (cache hit rate)\n- Sampling strategies that balance overhead and signal strength\n- Debugging steps: slow query logs, partition lag, cache invalidation timing, idempotent sinks\n- Concrete fixes: connection pools, prepared statements, cache invalidation, backpressure handling\n\n## Code Example\n\n```javascript\n// OpenTelemetry setup example: propagate trace context across HTTP and DB drivers\nconst { NodeTracerProvider } = require('@opentelemetry/sdk-trace-node');\nconst { SimpleSpanProcessor } = require('@opentelemetry/sdk-trace-base');\nconst { ConsoleSpanExporter } = require('@opentelemetry/exporter-console-span');\n\nconst provider = new NodeTracerProvider();\nprovider.addSpanProcessor(new SimpleSpanProcessor(new ConsoleSpanExporter()));\nprovider.register();\n\n// Instrumentations (pseudo-imports; actual setup may vary by stack)\nrequire('@opentelemetry/instrumentation-http');\nrequire('@opentelemetry/instrumentation-pg');\n```\n\n## Follow-up Questions\n\n- How would you validate end-to-end plan under load and which SLA metrics would you lock to?\n- How would you adjust sampling and instrumentation overhead in production without losing signal?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnpa-q-24",
        "questionId": "q-15412",
        "question": "CNPA stack with HTTP API, PostgreSQL, Kafka, and Redis uses Redis as a read-through cache for user profiles. After a write to PostgreSQL, reads sometimes return stale data from Redis under peak load. Design a beginner-friendly plan to ensure cache correctness and minimize latency, detailing caching strategy (cache-aside vs write-through), invalidation triggers, TTL defaults, and verification steps under load. Provide concrete changes and tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a lightweight HTTP middleware that activates when the X-Trace header is set to true. Generate a request_id, capture timestamp, user_id, action, and a DB row pointer; persist asynchronously to a dedicated trace…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enable OpenTelemetry across HTTP middleware, PostgreSQL driver, and Kafka producer; propagate TraceContext via HTTP headers and Kafka message headers; sample 10-20%; collect p95/p99 latency, error rat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt a write-through caching strategy with Redis as the authoritative read source for user profiles. On each PostgreSQL write, immediately update Redis synchronously before confirming the write as complete. Use a 60-se…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: measure latency with tracing on vs off, log payload size, exporter queue times, CPU and GC. Do AB test with tracing disabled on hot HTTP paths, enable adaptive tail sampling, and trim attributes",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates understanding of cache consistency patterns in CNPA stacks (Containerized/Cloud-Native, PostgreSQL, Apache Kafka, Redis). It tests knowledge of write-through vs cache-aside strategies, cache invalidation under concurrency, and maintaining data freshness while minimizing latency during high-load scenarios.\n\n## Key Concepts\n\n- **Cache Coherence**: Ensuring Redis and PostgreSQL remain synchronized, especially under concurrent writes\n- **Write-Through vs Cache-Aside**: Write-through writes to both DB and cache simultaneously, guaranteeing consistency at the cost of slightly higher write latency; cache-aside is faster for writes but risks stale reads\n- **TTL and Eviction**: Time-to-live settings balance staleness risk against cache memory pressure and read performance\n- **Invalidation Triggers**: Immediate invalidation on write, TTL expiration, and event-driven invalidation via Kafka\n- **Observability**: Cache hit rate, stale read detection, end-to-end latency percentiles\n\n## Solution Design\n\n### Caching Strategy: Write-Through\nWrite-through ensures the cache is updated synchronously on every write, eliminating stale reads. The application writes to PostgreSQL first, then immediately updates Redis before acknowledging the operation.\n\n### Invalidation Triggers\n1. **Synchronous**: On profile update, immediately update Redis\n2. **TTL Expiration**: 60-second TTL forces automatic refresh\n3. **Event-Driven**: Publish invalidation event to Kafka for distributed cache warming\n\n### TTL Defaults\n- **Active Data (User Profiles)**: 60 seconds\n- **Static/Reference Data**: 5-15 minutes\n- **Hot Keys**: 30 seconds with active refresh\n\n## Code Implementation\n\n```javascript\n// Write-through profile update\nasync function updateProfile(id, data) {\n  // Transaction ensures atomicity\n  await db.query(\n    'UPDATE profiles SET name = ?, email = ?, updated_at = NOW() WHERE id = ?',\n    [data.name, data.email, id]\n  );\n  \n  // Immediate cache update (synchronous write-through)\n  await redis.set(\n    `profile:${id}`,\n    JSON.stringify({ ...data, updatedAt: new Date().toISOString() }),\n    'EX',\n    60\n  );\n  \n  // Publish invalidation event for consistency across services\n  await kafka.publish('profile-updated', { id, timestamp: Date.now() });\n}\n\n// Cache-aside read with stale-check\nasync function getProfile(id) {\n  const cached = await redis.get(`profile:${id}`);\n  \n  if (cached) {\n    return JSON.parse(cached);\n  }\n  \n  // Cache miss - load from DB and populate cache\n  const [profile] = await db.query('SELECT * FROM profiles WHERE id = ?', [id]);\n  \n  if (profile) {\n    await redis.set(`profile:${id}`, JSON.stringify(profile), 'EX', 60);\n  }\n  \n  return profile;\n}\n```\n\n## Verification Under Load\n\n### Load Test Scenarios\n1. **Concurrent Writes**: 100 threads updating the same profile simultaneously\n2. **Read-Heavy Peak**: 10,000 reads/second with periodic writes\n3. **Cache Expiry Storm**: All TTLs expire simultaneously\n\n### Verification Steps\n1. **Consistency Check**: After each write, immediately read and verify cache matches DB\n2. **Stale Read Detection**: Run reads in a tight loop post-write, flag any mismatches\n3. **Hit Rate Monitoring**: Track Redis INFO stats for keyspace_hits/keyspace_misses\n4. **Latency Validation**: P50/P95/P99 response times under load must remain within SLA\n\n### Test Example\n```javascript\nasync function testCacheConsistency() {\n  const testId = 'test-user-123';\n  \n  // Write new profile\n  await updateProfile(testId, { name: 'Alice', email: 'alice@example.com' });\n  \n  // Immediately read back - cache should be fresh\n  const cached = await getProfile(testId);\n  const [dbRecord] = await db.query('SELECT * FROM profiles WHERE id = ?', [testId]);\n  \n  assert.strictEqual(cached.name, dbRecord.name);\n  assert.strictEqual(cached.email, dbRecord.email);\n  \n  // Concurrent update stress test\n  const updates = Array(50).fill(null).map((_, i) => \n    updateProfile(testId, { name: `User${i}`, email: `user${i}@test.com` })\n  );\n  \n  await Promise.all(updates);\n  \n  // Verify final consistency\n  const final = await getProfile(testId);\n  const [finalDb] = await db.query('SELECT * FROM profiles WHERE id = ?', [testId]);\n  \n  assert.strictEqual(final.name, finalDb.name);\n}\n```",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-08T06:44:03.716Z",
    "version": 1
  }
]