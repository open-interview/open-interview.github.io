[
  {
    "id": "test-ckne",
    "channelId": "ckne",
    "channelName": "Ckne",
    "title": "Ckne Knowledge Test",
    "description": "Test your understanding of Ckne with 25 questions.",
    "questions": [
      {
        "id": "ckne-q-0",
        "questionId": "q-5392",
        "question": "Design a CKNE-aware Kubernetes canary release controller for a shared microservices platform serving multiple tenants. Each tenant has a CKNE score 0–1 that aggregates error rate, latency, and saturation. Explain how CKNE maps to per-tenant rollout speed, traffic shaping, and alerting; specify a minute-by-minute scheduler that slows low-CKNE tenants' canary rate and accelerates high-CKNE tenants, while preserving a baseline SLA. Include a minimal payload example and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Map CKNE to per-tenant resources in streaming: CKNE=0.0 implies small windows and tight state budgets; CKNE=1.0 implies larger, low-latency windows with higher memory. Implement a per-minute rebalance",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Base TTLs: 30s. Healthy CKNE raises prefetch depth to 3 items; degradation drops TTL to 10s and caps prefetch to 1. Admission uses per-tenant CKNE-derived score to gate cache entries. Edge caches comp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a CKNE-based policy for a shared model-serving tier: map CKNE to per-tenant maxConcurrency, maxBatchSize, and embeddingCacheQuota. Implement a minute-by-minute scheduler that rebalances budget",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a CKNE-aware canary controller for a shared Kubernetes service. Map CKNE to per-tenant canary weights (0–100%), ramping high-CKNE tenants faster while enforcing a baseline SLA for all tenants. U",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical multi-tenant rollout control, CKNE integration, and real-time traffic shaping.\n\n## Key Concepts\n- CKNE-to-rollout mapping across tenants\n- Canary deployments and traffic split control\n- Per-tenant fairness and SLA guarantees\n- Telemetry, alerts, and safe preemption\n\n## Code Example\n```go\n// Pseudo-code weight mapping\nfunc computeWeight(ckne float64) float64 {\n  return 0.05 + ckne*0.95\n}\n```\n\n## Follow-up Questions\n- How to test tail-latency and burst traffic scenarios?\n- How would you handle delays in CKNE signals and spike mitigation?",
        "difficulty": "intermediate"
      },
      {
        "id": "ckne-q-1",
        "questionId": "q-17468",
        "question": "Design a CKNE-aware per-tenant data backfill scheduler for a multi-tenant data ingestion pipeline (Kafka -> ClickHouse). Each tenant has CKNE in [0,1]. Explain how CKNE maps to per-tenant backfill priority, batch window size, and resource quotas (CPU, IO). Propose a minute-by-minute rebalancing loop that reallocates bandwidth toward higher-CKNE tenants while preserving a baseline SLA. Include a minimal payload example and a test plan. Payload example: {tenant:'A',ckne:0.25,batchWindowMs:120000,table:'events'}?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Map per-tenant CKNE to a token budget with p = 0.05 + 0.95*ckne, cap at 1. Route higher-CKNE tenants first via priority queues; enforce circuit-breakers for tenants dipping below SLA. Run a 60s contro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "An API gateway routes traffic by CKNE; map CKNE to per-tenant tokens with a weighted token bucket, tune max concurrent requests and per-endpoint quotas, and enforce circuit-breakers for CKNE below 0.1",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "CKNE-driven backfill: allocate CPU/IO credits by CKNE, with high-CKNE tenants getting larger batch windows and higher priority. Implement a minute-level planner using a CKNE-weighted queue, with preem",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a CKNE-weighted token bucket per tenant and endpoint. Base tokens per minute = B; adjust by CKNE via multiplier M = 1 + CKNE; effective tokens = B*M, capped by a shared pool for bursts. Every minu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design CKNE-aware scheduling for data backfill with real-world constraints like data volume, SLA, and multi-tenant fairness.\n\n## Key Concepts\n- CKNE-to-resource mapping\n- Backfill planning and batching\n- Minutely rebalancing and safety controls\n- Telemetry and SLA guarantees\n\n## Code Example\n```javascript\n// Pseudo scheduling function\nfunction planBackfill(tenants, ckneMap, capacity) {\n  // simple illustration\n  return tenants\n}\n```\n\n## Follow-up Questions\n- How would you test with synthetic CKNE distributions?\n- How to handle sudden CKNE spikes and ensure monotonic SLA?",
        "difficulty": "intermediate"
      },
      {
        "id": "ckne-q-2",
        "questionId": "q-21683",
        "question": "Design a CKNE-aware per-tenant feature-flag evaluation service for a CKNE-powered app. Each tenant has CKNE in [0,1]. Explain how CKNE maps to per-tenant flag evaluation latency budget, edge-cache TTL, and rollout probability. Propose a minute-by-minute loop that biases high-CKNE tenants toward faster rollouts while preserving a baseline SLA. Include a minimal payload example and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CKNE maps to throughput and latency budgets: per-tenant send_rate = base_rate * (0.5 + 0.5*ckne), queue_depth_limit, and a capped retry-backoff window. Implement a minute loop with a shared token pool",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "CKNE maps to three knobs: cacheTTL_i = baseTTL*(1+0.8*ckne_i), evalBudget_i = baseBudget*(1+0.5*ckne_i), and rolloutChance_i = min(1, baseP + 0.4*ckne_i). A per-minute loop nudges TTLs and budgets tow",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "CKNE maps to: T_t = baseThroughput*(1+α*ckne), P_t = floor(minP + ckne*maxP). Implement a weighted partition rebalancer across brokers using ckne as the weight, plus a per-minute backpressure threshol",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "CKNE maps to per-tenant: batch_size, max_concurrency, retry_backoff, and message TTL. Implement a priority queue keyed by CKNE; create a minute-by-minute loop that trims low-CKNE batch sizes and expan",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of CKNE as a resource tuning signal and how it governs per-tenant feature flags, latency budgets, and caching. It also checks ability to design a simple adaptive loop with SLA guarantees.\n\n## Key Concepts\n- CKNE to TTL, budget, rollout mapping\n- Per-tenant latency budgets and fairness\n- Edge caching and prefetching strategies\n- Telemetry and SLA verification\n\n## Code Example\n```javascript\nfunction mapCKNETuning(ckne) {\n  const baseTTL = 60; // seconds\n  const baseBudget = 100; // ms\n  const baseP = 0.2;\n  return {\n    ttl: baseTTL * (1 + 0.8 * ckne),\n    budget: baseBudget * (1 + 0.5 * ckne),\n    rollout: Math.min(1, baseP + 0.4 * ckne)\n  };\n}\n``` \n\n## Follow-up Questions\n- How would you test the per-minute loop under bursty CKNE distributions?\n- How would you ensure fairness across tenants with identical CKNE scores?",
        "difficulty": "beginner"
      },
      {
        "id": "ckne-q-3",
        "questionId": "q-16902",
        "question": "Architect a CKNE-aware read path for a shared document store (MongoDB-like) where 120 tenants have CKNE scores in [0,1]. How would CKNE map to per-tenant read concurrency, index usage, and cache warmup, and implement a 1-minute rebalancing loop to raise high-CKNE tenants while preserving a baseline SLA? Include a concrete payload example and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CKNE (Cooperative Kernel Nash Equilibrium) maps to per-tenant resource allocation through three formulas: - **Max Concurrency**: `maxConcurrency = min(2 + floor(8 * ckne), 10)` — tenants with higher CKNE values receive…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Map CKNE to per-tenant read concurrency budget and cache priority; implement per-tenant token buckets and a 1-minute rebalancing loop that rises high-CKNE tenants' parallelism and warms caches, while",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "- Fully qualified domain name: `payments.billing.svc.cluster.local`. - From a pod in namespace `frontend`, reach the billing service with that FQDN, e.g. `curl http://payments.billing.svc.cluster.local/health`. - Patter…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "CKNE maps to per-tenant: batch_size, max_concurrency, retry_backoff, and message TTL. Implement a priority queue keyed by CKNE; create a minute-by-minute loop that trims low-CKNE batch sizes and expan",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design CKNE-aware resource budgeting for a shared data path with per-tenant guarantees.\n\n## Key Concepts\n\n- CKNE-to-budget mapping\n- Per-tenant queues and token-buckets\n- Fairness vs. starvation\n- SLA preservation under dynamic reallocation\n\n## Code Example\n\n```javascript\nfunction budgetFor(ckne, base=4){\n  // higher CKNE gets more concurrent reads allocated\n  return Math.max(1, Math.round(base + ckne * 6));\n}\n```\n\n## Follow-up Questions\n\n- How would you implement safety bounds if CKNE spikes?\n- How would you test the 1-minute rebalancing loop under bursty tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "ckne-q-4",
        "questionId": "q-954",
        "question": "Scenario: A three-service order flow (API gateway -> inventory -> payment) runs in one region. Design a CKNE-aware tracing approach where each service propagates a CKNE health signal in trace metadata and employs an adaptive sampling policy: base 10% with a health-adjusted factor that can raise sampling to 50% during degradation. Specify data structures for per-service health, the trace metadata payload, and a minute-by-minute workflow to compute health and adjust sampling for the next window. Provide a minimal code snippet showing the payload and health update logic?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Map CKNE to per-tenant log rate and alert budgets using a minute-based token-bucket. rate_t = max(minRate, baseRate*(1-ckne)); s_t = rate_t/baseRate. Verbose logging kept for high-CKNE tenants, reduce",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a per-tenant CKNE quota ledger that translates CKNE into max concurrent warm workers, plus a cap for cold-start retries. A 60s rebalancer increases slots for higher-CKNE tenants up t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Map CKNE to per-tenant resources in streaming: CKNE=0.0 implies small windows and tight state budgets; CKNE=1.0 implies larger, low-latency windows with higher memory. Implement a per-minute rebalance",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Base sampling = 10%; a CKNE health factor increases sampling up to 50% during degradation. Compute per-service health from the last 60s using p95 latency, error rate, and queue depth. Propagate a CKNE",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThe question tests practical CKNE integration in a typical microservice flow, emphasizing real-time health computation and adaptive tracing overhead.\n\n## Key Concepts\n- CKNE health signals and per-service aggregation\n- Adaptive sampling policies with bounds\n- Trace metadata payload design and propagation\n- Windowed health computation and update cadence\n\n## Code Example\n```javascript\n// Payload sketch\nconst tracePayload = {\n  service: 'inventory',\n  traceId: 'abc123',\n  ckneHealth: 'degraded', // ok | degraded | failed\n  timestamp: Date.now()\n};\n\n// Health update (60s window)\nfunction updateHealth(samples) {\n  const p95 = calcP95(samples.latencies);\n  const err = samples.errors / samples.total;\n  const health = (p95 > 300 || err > 0.01 || samples.queueDepth > 50) ? 'degraded' : 'ok';\n  return health;\n}\n```\n\n## Follow-up Questions\n- How would you test the health computation in a 1-minute window with bursty traffic?\n- How would you extend this to a multi-region setup with synchronized CKNE signals?",
        "difficulty": "beginner"
      },
      {
        "id": "ckne-q-5",
        "questionId": "q-846",
        "question": "Design a real-time CKNE failure detector for a distributed microservice mesh. Specify the data pipeline, latency budget, how you compute p95 latency and error rate, and how you implement replay and backpressure for fault tolerance. Include testing strategies and production validation to demonstrate correctness and resilience?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a CKNE-based policy for a shared model-serving tier: map CKNE to per-tenant maxConcurrency, maxBatchSize, and embeddingCacheQuota. Implement a minute-by-minute scheduler that rebalances budget",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "CKNE scales to edge. Map per-tenant CKNE to function placement priority, CPU/memory quotas, and network egress budgets; throttle low-CKNE tenants by temporarily reducing concurrency. Implement a minut",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To diagnose a CKNE microservice anomaly in real time, implement a windowed streaming detector (5s tumbling windows) with trace-aware metrics, compute p95 latency and error rate, and emit correlated al",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "CKNE-to-canary mapping: 0-0.25 -> 5% canary with 1-minute ramp; 0.25-0.5 -> 15%; 0.5-0.75 -> 30%; 0.75-1 -> 50%. A minute-by-minute loop shifts traffic in 1% steps toward higher-CKNE tenants, staying",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess real-time system design, telemetry strategy, and fault tolerance in CKNE-scale services.\n\n## Key Concepts\n- Streaming pipelines, windowing\n- Latency budgets, p95/throughput\n- Backpressure, circuit breakers, replay\n- Observability and tests\n\n## Code Example\n\n```javascript\n// Pseudo-implementation sketch\n```\n\n## Follow-up Questions\n- How would you scale the detector to thousands of nodes?\n- How to ensure idempotent replay and exactly-once semantics?",
        "difficulty": "advanced"
      },
      {
        "id": "ckne-q-6",
        "questionId": "q-5098",
        "question": "Design a CKNE-aware cross-tenant data synchronization service for a shared data mesh built on Apache Kafka. Each tenant has a CKNE score (0–1). Explain how CKNE maps to per-tenant partition allocation, producer throttling, and consumer lag budgets, then implement a minute-by-minute degradation loop that reallocates partitions from low-CKNE tenants to high-CKNE tenants while preserving a baseline data freshness SLA. Include a minimal payload example and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CKNE maps to per-tenant partition weight, max produce rate, and lag budget. Higher CKNE gets more partitions and looser throttling; lower CKNE faces tighter throttles and potential reallocation away f",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Map CKNE to exposure via per-tenant weights and a CKNE-normalized randomization. Update allocations every minute to preserve baseline SLA, boosting high-CKNE tenants within QoS budgets. Use p_i propor",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "CKNE → per-tenant ingestion rate, partition affinity, and in-flight memory budgets. Use a per-tenant token bucket: baseRate plus ckne*(burst); every minute recompute quotas and rebalance partitions/co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "CKNE-aware per-tenant cache partitions: quota_i = base + ckne_i*(max-base); eviction_latency_i = L*(1-ckne_i/2); hot_key_prefetch_ms = max(50, 100*ckne_i). Minute loop rebalances 5–15% of total cache",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design a CKNE-driven, real-time data-partitioning system across tenants using Kafka-like semantics, focusing on safety, fairness, and SLA adherence.\n\n## Key Concepts\n\n- CKNE to resource budgets (partitions, rate limits, lag budgets)\n- Safe dynamic partition reallocation and at-least-once data delivery\n- Monitoring, telemetry, and anomaly detection during rebalancing\n- Test plan to validate correctness and SLA guarantees\n\n## Code Example\n\n```javascript\n// Budget calculation example\nfunction budgetForTenant(ckne, maxParts){\n  return Math.max(1, Math.floor(ckne * maxParts));\n}\n```\n\n## Follow-up Questions\n\n- How would you validate fairness when CKNEs are highly skewed?\n- How would you recover from a rebalance that temporarily causes data loss or duplication?",
        "difficulty": "intermediate"
      },
      {
        "id": "ckne-q-7",
        "questionId": "q-2698",
        "question": "Design a CKNE-aware per-tenant API rate limiter at an edge gateway that enforces per-tenant QoS using CKNE scores to scale capacity. How would you map CKNE to per-tenant capacity, implement a minute-by-minute degradation loop, and provide a minimal payload example plus a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Map CKNE to capacity by scaling allowed_rate = base_rate * CKNE (e.g., CKNE 1.0 => 100% base, 0.4 => 40%). Use a per-tenant token bucket refilling at that rate. Degrade: if CKNE < 0.6 for 2 consecutiv",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "CKNE maps directly to per-tenant replica allocation and batching configuration. The replica count formula is: replica = base_replicas × (1 + 0.9 × CKNE), capped at the maximum allowed. The batching window formula is: ba…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "CKNE-aware background job queue: map CKNE to dynamic per-tenant weights and a minute-by-minute reallocation. Base SLA = baseline slots; extra capacity allocated proportionally to w_i = base + scale*(C",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a CKNE-aware per-tenant FaaS scheduler: map CKNE to per-tenant concurrency, cold-start budgets, and timeouts; enforce a global cap with dynamic quotas. Implement a minute-by-minute rebalancing",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEdge gateways must enforce multi-tenant QoS using dynamic health signals. This tests a practical, beginner-friendly CKNE rate-limiting pattern that combines health signals with per-tenant control.\n\n## Key Concepts\n- CKNE-to-capacity mapping and fairness\n- Per-tenant token bucket implementation\n- Minute-by-minute degradation loop with cooldowns\n- Minimal payloads and testability\n\n## Code Example\n```javascript\n// Minimal token bucket per tenant\nclass TB {\n  constructor(baseRate){ this.baseRate=baseRate; this.ckne=1; this.tokens=baseRate; this.last=Date.now(); }\n  refill(now){\n    const dt=(now-this.last)/1000;\n    this.tokens = Math.min(this.tokens + dt * this.baseRate * this.ckne, this.baseRate);\n    this.last = now;\n  }\n  tryConsume(n=1){\n    this.refill(Date.now());\n    if(this.tokens >= n){ this.tokens -= n; return true; }\n    return false;\n  }\n}\n```\n\n## Follow-up Questions\n- How would you validate fairness when many tenants degrade concurrently?\n- What metrics would you monitor to detect CKNE drift vs. traffic patterns?",
        "difficulty": "beginner"
      },
      {
        "id": "ckne-q-8",
        "questionId": "q-19262",
        "question": "CKNE-aware multi-tenant GPU scheduler for real-time video analytics on a shared GPU cluster. Each tenant CKNE in [0,1]. Explain mapping CKNE to per-tenant GPU time slices, memory quotas, and preemption thresholds; preserve baseline SLA (e.g., 95th percentile latency under target ms). Each minute, compute CKNE-weighted shares, reallocate idle GPU seconds to higher-CKNE tenants, and preempt low-CKNE tasks when safe. Include payload example: {tenant:'A',job:'detection',ckne:0.25,gpuReq:{timeMs:60000,memMB:4096}} and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a per-tenant CKNE quota ledger that translates CKNE into max concurrent warm workers, plus a cap for cold-start retries. A 60s rebalancer increases slots for higher-CKNE tenants up t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Map CKNE to per-tenant GPU time slices, memory quotas, and preemption thresholds. Preserve baseline SLA (e.g., 95th percentile latency under target ms). Each minute, compute CKNE-weighted shares, reallocate idle GPU sec…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a 512-slot CKNE-aware FaaS scheduler where CKNE sets per-tenant concurrency caps, queue budgets, and cold-start budgets. Each minute compute a score: CKNE weight + queue pressure - latency,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant ingest rate = baseRate × (0.5 + CKNE). Cap at maxRate; CKNE also drives shardCount and diskQueueDepth, allocating more resources to higher CKNE tenants. Every minute: recompute deltas, thro",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to translate a scalar CKNE (Compute Kernel Needs Estimation) score into concrete GPU resource governance for real-time workloads, balancing fairness with priority.\n\n## Key Concepts\n\n- **CKNE-to-resource mapping**: Translating CKNE values [0,1] into GPU time slices, memory quotas, and preemption thresholds\n- **Minute-by-minute rebalancing loop**: Dynamic reallocation based on CKNE-weighted shares\n- **Preemption safety**: Ensuring preemption only occurs when SLA guarantees can be maintained\n- **Telemetry, fairness, and backpressure handling**: Monitoring and enforcing policies under load\n\n## Code Example\n\n```python\n# Sketch: compute per-tenant shares and schedule accordingly\ndef rebalance(tenants, total_gpu_ms, sla_manager):\n    total_ckne = sum(t['ckne'] for t in tenants)\n    for tenant in tenants:\n        share = (tenant['ckne'] / total_ckne) * total_gpu_ms\n        tenant['time_slice_ms'] = share\n        tenant['mem_quota_mb'] = int(share / total_gpu_ms * TOTAL_GPU_MEM_MB)\n        \n    # Reallocate idle GPU seconds to higher-CKNE tenants\n    idle_ms = sum(t.get('idle_ms', 0) for t in tenants)\n    if idle_ms > 0:\n        high_ckne_tenants = [t for t in tenants if t['ckne'] > 0.5]\n        bonus_per_tenant = idle_ms // len(high_ckne_tenants)\n        for t in high_ckne_tenants:\n            t['time_slice_ms'] += bonus_per_tenant\n    \n    # Preempt low-CKNE tasks when safe (SLA not violated)\n    for tenant in sorted(tenants, key=lambda t: t['ckne']):\n        if not sla_manager.can_preempt(tenant):\n            continue\n        # Preemption logic here\n```\n\n## Payload Example\n\n```json\n{\n  \"tenant\": \"A\",\n  \"job\": \"detection\",\n  \"ckne\": 0.25,\n  \"gpuReq\": {\n    \"timeMs\": 60000,\n    \"memMB\": 4096\n  }\n}\n```\n\n## Test Plan\n\n1. **Unit Tests**: Verify CKNE-to-share calculation correctness\n2. **Integration Tests**: End-to-end scheduling with mock tenants\n3. **SLA Verification**: Confirm 95th percentile latency under load\n4. **Preemption Tests**: Ensure safe preemption without SLA violations\n5. **Stress Tests**: Validate behavior under high contention scenarios",
        "difficulty": "intermediate"
      },
      {
        "id": "ckne-q-9",
        "questionId": "ckne-k8s-networking-fundamentals-1768971446111-2",
        "question": "From a pod in namespace frontend, what is the fully qualified domain name to reach a service named payments in namespace billing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CKNE maps to per-tenant throughput, buffer depth, and shard weight. Let t_t = base_rate*(1 + 0.8*CKNE) with a floor; buffer_depth ∝ (1-CKNE); shard_weight ∝ CKNE. Rebalance minute-by-minute to honor b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a CKNE-aware multi-tenant in-memory cache layer that dynamically allocates resources based on tenant CKNE scores. Map CKNE values to per-tenant memory quotas, eviction cadence, and prefetch budgets. Implement a m…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define per-tenant visibility profiles keyed by CKNE: if ckne < 0.3 redact PII fields; if 0.3 ≤ ckne < 0.7 hash identifiers; if ckne ≥ 0.7 emit non-sensitive fields and apply privacy-aware aggregation.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "- Fully qualified domain name: `payments.billing.svc.cluster.local`. - From a pod in namespace `frontend`, reach the billing service with that FQDN, e.g. `curl http://payments.billing.svc.cluster.local/health`. - Patter…",
            "isCorrect": true
          }
        ],
        "explanation": "In Kubernetes, intra-cluster DNS resolves service names as <service>.<namespace>.svc.cluster.local. The pod in frontend can reach the billing service via that FQDN because core DNS provides cross-namespace resolution for services. If the cluster uses a non-default domain, adjust the suffix accordingly. This name maps to the service's ClusterIP endpoint; it remains consistent across nodes. Edge cases: headless services or ExternalName require different DNS handling.",
        "difficulty": "intermediate"
      },
      {
        "id": "ckne-q-10",
        "questionId": "q-9146",
        "question": "Design a CKNE-aware real-time event router for a multi-tenant messaging service (Slack-like). Each tenant CKNE in [0,1]. Explain how CKNE maps to per-tenant routing priority, backpressure, and per-tenant rate quotas. Propose a minute-by-minute loop that biases dispatch toward high-CKNE tenants while preserving baseline SLA. Include a minimal payload example and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant namespaces, a central CKNE controller, and a minute-by-minute loop. CKNE maps to max parallel deploys (0–5), canary replicas (0–2), and CPU shares. Recompute quotas each minute from CKN",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "CKNE (Critical Knowledge Need Index) directly maps to three key operational controls in the data lake: 1. **Per-tenant ingestion concurrency**: CKNE [0,1] scales linearly from `min_concurrency` (e.g., 1) to `max_concurr…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a CKNE-driven scheduler for multi-tenant model serving on a shared GPU fleet. Map CKNE to per-tenant concurrency (0–8), batch size (1–128), and memory budget (MB). Implement minute-by-minute r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "CKNE informs routing priority and per-tenant quotas. High CKNE tenants receive larger share of dispatch tokens and lower backpressure, while low CKNE tenants are throttled. Implement a minute-by-minut",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of CKNE mapping to routing decisions in low-latency messaging.\n\n## Key Concepts\n\n- CKNE to per-tenant quotas\n- Backpressure and fairness\n- Minute-by-minute rebalancing and SLA\n\n## Code Example\n\n```javascript\n// Skeleton router enforcing CKNE-aware quotas\nclass CKNERouter {\n  constructor() {}\n  route(event, ckne) {\n    // simplified: high CKNE => higher priority\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you collect telemetry to detect CKNE drift?\n- How would you handle tenant churn with CKNE changes?",
        "difficulty": "beginner"
      },
      {
        "id": "ckne-q-11",
        "questionId": "q-6772",
        "question": "Design a CKNE-aware API gateway for a multi-tenant serverless platform. Each tenant has a CKNE score in [0,1]. Explain CKNE mapping to per-tenant concurrency, burst budgets, and cold-start allowances, then implement a minute-by-minute degradation loop that throttles low-CKNE tenants and reallocates warm concurrency to high-CKNE tenants while preserving baseline SLA. Include fairness guarantees, preemption, telemetry, and a minimal payload: {tenant:'A',endpoint:'/search',ckne:0.25}?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CKNE maps to per-tenant concurrency budgets, burst credits, and cold-start allowances. Implement a minute-by-minute loop: throttle low-CKNE tenants by trimming burst budgets and delaying non-critical",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "CKNE is used as a weight to influence edge affinity, cache budgets, and routing. Prefer edges with higher CKNE, maintain a minimum per-tenant cache quota, and set eviction thresholds to avoid thrashin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "CKNE-aware storage tiering maps tenant criticality (0–1) to storage classes: 0–0.3 → Standard (baseline SLA), 0.3–0.7 → Infrequent Access (reduced durability, extended TTL), 0.7–1.0 → Archive (minimal durability, maximu…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Map CKNE to per-tenant GPU time slices, memory quotas, and preemption thresholds. Preserve baseline SLA (e.g., 95th percentile latency under target ms). Each minute, compute CKNE-weighted shares, reallocate idle GPU sec…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nAssesses ability to translate CKNE into runtime gating for a serverless gateway, with multi-tenant fairness and SLA enforcement.\\n\\n## Key Concepts\\n- CKNE-to-resource mapping\\n- Degradation loop design\\n- Fairness, preemption, telemetry\\n- Minimal payload semantics\\n\\n## Code Example\\n```javascript\\nfunction decideAllocations(ckneScores, totalConns) {\\n  // simple proportional mapping\\n  const ordered = Object.entries(ckneScores).sort((a,b)=> b[1]-a[1]);\\n  const alloc = {};\\n  let used = 0;\\n  for (const [t, ck] of ordered) {\\n    const take = Math.max(1, Math.round((ck) * totalConns));\\n    alloc[t] = Math.min(take, totalConns - used);\\n    used += alloc[t];\\n  }\\n  return alloc;\\n}\\n```\\n\\n## Follow-up Questions\\n- How would you test under burst CKNE swings?\\n- How would you ensure SLA across tenants during reallocation?\\n",
        "difficulty": "advanced"
      },
      {
        "id": "ckne-q-12",
        "questionId": "q-24276",
        "question": "Design a CKNE-aware autoscaler for a multi-tenant ML inference service on Kubernetes. Each tenant has CKNE in [0,1]. Explain how CKNE maps to per-tenant replica limits and batching window. Propose a minute-by-minute loop that adjusts per-tenant replicas to favor higher-CKNE tenants while preserving baseline SLA. Include a minimal payload example and a basic test plan? Payload example: {tenant:'A',ckne:0.42}?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Map CKNE to a fixed resource budget: CKNE scales replicas and quotas; use a baseline minReplicas for all tenants. Each minute, compute targets: targetReplicas = max(minReplicas, round(baseReplicas * (",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "CKNE maps directly to per-tenant replica allocation and batching configuration. The replica count formula is: replica = base_replicas × (1 + 0.9 × CKNE), capped at the maximum allowed. The batching window formula is: ba…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose per-tenant quotas derived from CKNE: concur limit, rps cap, and cache TTL. Implement a centralized, minute-granularity loop that lowers quotas for ckne < threshold, disables nonessential endpo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant token-bucket with budget B_t = SLA_RPS * (1 - CKNE_t); higher CKNE gets more burst. Use fair-queueing with CKNE-based priorities. A minute loop monitors utilization; during spikes throttle",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to translate a CKNE score into concrete autoscaling decisions while balancing fairness and SLA preservation in a multi-tenant environment. It evaluates intuition for per-tenant isolation, dynamic resource allocation, and practical scheduling loop design without requiring complex infrastructure knowledge.\n\n## Key Concepts\n- CKNE-to-resource mapping (linear scaling with configurable weight)\n- Per-tenant isolation and fairness across CKNE spectrum\n- Periodic scheduling loop (60-second interval)\n- SLA guarantees with baseline protection for lower-priority tenants\n\n## Code Example\n```javascript\nfunction computeReplica(base, ckne, max) {\n  return Math.min(max, Math.floor(base * (1 + 0.9 * ckne)));\n}\n```\n\n## Follow-up Questions\n- How would you handle tenant churn (new tenants joining or existing tenants leaving)?\n- What safeguards prevent a high-CKNE tenant from monopolizing cluster resources?\n- How do you handle latency spikes during rebalancing?",
        "difficulty": "beginner"
      },
      {
        "id": "ckne-q-13",
        "questionId": "q-28671",
        "question": "Design a CKNE-aware multi-tenant data-analytics pipeline that ingests streaming events into a shared data lake and computes per-tenant aggregations in near real-time. Each tenant has CKNE in [0,1]. Propose CKNE-based partitioning, tiered storage, and compute quotas, plus a minute-by-minute degradation loop that reclaims capacity from low-CKNE tenants during spikes while preserving baseline SLA. Include a minimal payload: {tenant:'A',ckne:0.65}. Provide test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CKNE links a tenant's priority to its allowed request rate, queue depth, and retry budget. Map CKNE to: high CKNE -> higher rate, larger queue, more aggressive retries; low CKNE -> tighter limits, sma",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a 512-slot CKNE-aware FaaS scheduler where CKNE sets per-tenant concurrency caps, queue budgets, and cold-start budgets. Each minute compute a score: CKNE weight + queue pressure - latency,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "CKNE-driven data-analytics pipeline: partition ingestion by tenant (per-tenant shards or keyed topics), assign compute quotas q_t = base + ckne*delta; hot data in fast storage, warm/cold with TTLs; en",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Base sampling = 10%; a CKNE health factor increases sampling up to 50% during degradation. Compute per-service health from the last 60s using p95 latency, error rate, and queue depth. Propagate a CKNE",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to translate CKNE into scalable data pipelines, resource governance, and degradation strategies under multi-tenant load.\n\n## Key Concepts\n- CKNE-to-quotas mapping and dynamic partitioning\n- Tiered storage with per-tenant TTLs and cost-aware cooling\n- Degradation loop with per-tenant SLAs and fairness guarantees\n- Observability: latency, throughput, accuracy\n\n## Code Example\n```javascript\n// Simple quota formula\nfunction quotaForCKNE(base, delta, ckne) { return base + ckne * delta; }\n```\n\n## Follow-up Questions\n- How would you test CKNE drift and its impact on latency?\n- How would you ensure data correctness when lowering per-tenant compute during spikes?",
        "difficulty": "advanced"
      },
      {
        "id": "ckne-q-14",
        "questionId": "q-9053",
        "question": "Design a CKNE-aware multi-tenant log ingestion and storage pipeline for a centralized logging service used by hundreds of tenants. Each tenant has CKNE in [0,1]. Explain how CKNE maps to per-tenant ingest rate, retention window, and storage tier, and propose a minute-by-minute loop that throttles low-CKNE tenants, rebalances retention toward high-CKNE tenants, and preserves baseline search latency. Include a minimal payload example: payload: {tenant:'A',log:'login',ckne:0.25} and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design CKNE-aware data lineage for a 3-stage ETL (ingest → transform → load) servicing thousands of tenants. Propagate CKNE at each hop; degrade by keeping tenant, timestamp, and essential markers whi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a CKNE-aware shared in-memory cache layer for multi-tenant workloads. Map CKNE to per-tenant quota q_t = base + ckne*max; implement a CKNE-weighted eviction policy that prioritizes higher-CKNE",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would map CKNE to three knobs: ingestRate, retentionDays, and storageTier. Each minute, compute per-tenant budget proportional to CKNE, throttle low-CKNE tenants to 50% of baseline, and reallocate t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Route per-tenant events with a CKNE-aware fan-out policy: per-tenant token buckets governed by CKNE health and SLA. Baseline 1000 events/s; scale to 5000 when CKNE health >0.8 and queue <70%; drop to 200 when CKNE healt…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis checks practical understanding of CKNE-guided resource budgeting, fairness, and small-step schedulers in a real service path (logging), with measurable SLAs.\n\n## Key Concepts\n\n- CKNE-to-resource mapping (ingest rate, retention, storage tier)\n- minute-by-minute reallocation loop\n- baseline vs. elastic budgets and SLA guarantees\n- telemetry and safety checks (rate limiting, backoff)\n\n## Code Example\n\n```javascript\nfunction budgetForTenant(ckne, baseline) {\n  // simple proportional budgeting with floor/ceiling guards\n  const ratio = Math.max(0.0, Math.min(1.0, ckne));\n  return {\n    ingestRate: Math.round(baseline.ingestRate * (0.5 + ratio * 1.0)),\n    retentionDays: Math.round(baseline.retentionDays * (0.5 + ratio * 1.0)),\n    storageTier: ratio > 0.75 ? 'premium' : ratio > 0.25 ? 'standard' : 'cold'\n  };\n}\n```\n\n## Follow-up Questions\n\n- How would you monitor CKNE fairness over time and detect drift?\n- How would you handle sudden CKNE spikes or tenant churn?",
        "difficulty": "beginner"
      },
      {
        "id": "ckne-q-15",
        "questionId": "q-7376",
        "question": "Design a CKNE-aware shared message broker for a multi-tenant SaaS using a Kafka-like model. Each tenant has a CKNE score in [0,1]. Explain how CKNE maps to per-tenant topic-partition quotas, consumer fetch rates, and broker backpressure, then propose a minute-by-minute degradation loop that throttles low-CKNE tenants and rebinding allocations to high-CKNE tenants while preserving a baseline SLA. Include fairness guarantees, preemption rules, telemetry, a minimal payload example: payload: {tenant:'A',topic:'events',ckne:0.25} and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CKNE scales tenants by allocating topic partitions and fetch budgets proportional to CKNE. High CKNE gets more partitions, higher max in-flight messages, and shorter backoff; low CKNE shares are throttled with reduced p…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-tenant rate limiting using a CKNE-adjusted token bucket: R_t = baseR + ckne*maxR, Burst_t = baseB + ckne*maxB. A CKNE-driven priority queue favors high-CKNE tenants during spikes while guaranteein",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design CKNE-aware auth by mapping CKNE to policy-eval latency budgets, token-cache TTLs, and verifier concurrency per tenant. Use per-tenant queues and batched token validation; low CKNE throttles get",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## CKNE-Aware Per-Tenant ETL Queue Design Map CKNE to per-tenant max concurrent ETL jobs using a linear scale (minimum 1, configurable maximum such as 50). For retry handling, implement exponential backoff with a 60-sec…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical CKNE-driven multi-tenant resource control in a publish/subscribe system, including dynamic reallocation, fairness, and observable metrics.\n\n## Key Concepts\n\n- CKNE-to-quotas mapping for partitions and fetch budgets\n- Minute-by-minute degradation loop and rebind strategy\n- Backpressure, preemption rules, and baseline SLA guarantees\n- Telemetry requirements and test plan validation\n- Fairness guarantees with preemption protection\n- Real-time quota rebalancing algorithms\n\n## Code Example\n\n```javascript\n// Complete quota recomputation with fairness\ndef recomputeQuotas(state, metrics):\n    # Apply CKNE weighting while preserving baseline\n    for tenant in state.tenants:\n        tenant.quota = max(baseline, tenant.ckne * total_capacity)\n        # Enforce fairness constraints\n        if tenant.quota < previous_quota * 0.5:\n            tenant.quota = previous_quota * 0.5\n```\n\n## Follow-up Questions\n\n- How handle sudden CKNE spikes causing oscillations?\n- How to scale this to thousands of tenants with fast convergence?\n- What strategies for preventing quota thrashing in volatile environments?",
        "difficulty": "intermediate"
      },
      {
        "id": "ckne-q-16",
        "questionId": "q-19666",
        "question": "Design a CKNE-aware multi-tenant ML inference service running on a shared Kubernetes cluster. Each tenant has CKNE in [0,1]. Explain how CKNE maps to per-tenant inference concurrency, GPU memory residency, and latency budgets; propose a minute-by-minute reallocation loop that increases resources for high CKNE tenants while preserving a baseline tail latency SLA. Include a minimal payload example and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Map CKNE to per-tenant log rate and alert budgets using a minute-based token-bucket. rate_t = max(minRate, baseRate*(1-ckne)); s_t = rate_t/baseRate. Verbose logging kept for high-CKNE tenants, reduce",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Expose per-tenant CKNE metrics including ckne_score (0–100) and queue_depth; feed these into per-tenant autoscalers (KEDA/HPA) to independently scale stream-processor replicas. Define per-tenant min/max replicas, cooldo…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## CKNE-Aware Multi-Tenant ML Inference Service Design **CKNE Mapping to Resources:** - **Per-Tenant Concurrency:** CKNE directly determines the maximum concurrent inference requests a tenant can have in flight. A tenan…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a Flink/Spark Structured Streaming job with per-tenant CKNE. Map CKNE to max parallelism and checkpoint cadence; high CKNE gets more parallelism and tighter checkpoints, low CKNE gets thrott",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question evaluates a candidate's ability to translate an abstract priority metric (CKNE) into practical multi-tenant ML serving constraints. It tests understanding of resource allocation, scheduling algorithms, and SLA enforcement in shared infrastructure environments.\n\n## Key Concepts\n- **CKNE to Concurrency Mapping:** CKNE directly controls the number of concurrent inference requests a tenant can have in flight, translating priority into throughput capacity\n- **Cache Residency:** Higher CKNE values warrant pinning models in GPU memory to minimize cold-start latency and improve response times\n- **Latency Budgets:** CKNE influences acceptable P99 tail latency thresholds per tenant, ensuring high-priority workloads meet stricter performance requirements\n- **Dynamic Reallocation:** The minute-by-minute scheduler must balance reactivity (responding to CKNE changes) with stability (avoiding thrashing) while preserving baseline SLAs for all tenants",
        "difficulty": "intermediate"
      },
      {
        "id": "ckne-q-17",
        "questionId": "q-3686",
        "question": "Design a CKNE-aware multi-tenant CI/CD runner fleet on Kubernetes. Each tenant has a CKNE score (0–1). Explain how CKNE maps to per-tenant concurrent jobs, runner isolation, and retry budgets, then implement a minute-by-minute degradation loop that throttles low-CKNE tenants and rebinds runners to high-CKNE tenants while preserving a baseline SLA. Include a minimal payload example and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Allocate cache proportionally to CKNE, with hot-feature promotion and TTL-based eviction. Use a per-minute loop to rebalance hot feature shards, throttle requests from tenants with CKNE below a thresh",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose CKNE-driven edge budgets: max_rps = 100 + 900*ckne, max_concurrent = 1 + floor(4*ckne), queue_depth = floor(20*(1-ckne)) with appropriate caps. Use a control plane to push configuration to Envoy (via xDS) or NGI…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a priority queue indexed by tenant CKNE, where C_t = baseC * (1 + k*ckne) with k>0 and a floor, plus per-tenant admission thresholds. Implement admission logic in a controller that adjusts all",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "CKNE maps to per-tenant max parallel jobs, runner isolation, and retry budgets. The scheduler recomputes every minute, throttling low-CKNE tenants by lowering their parallel limits and rebinding idle",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests CKNE-aware scheduling and production readiness for multi-tenant CI/CD runners.\n\n## Key Concepts\n- CKNE to concurrency and retry budgets\n- Minute-by-minute degradation loop\n- Preemption and rebound policies\n- Telemetry and test plans\n\n## Code Example\n```javascript\n// Priority budgeting example\nfunction limitsForTenant(ckne, baseConcurr = 8) {\n  const cap = Math.max(1, Math.floor(baseConcurr * ckne));\n  return {maxParallel: cap};\n}\n```\n\n## Follow-up Questions\n- How would you handle burst CKNE score changes?\n- What telemetry metrics ensure fairness and SLA?",
        "difficulty": "intermediate"
      },
      {
        "id": "ckne-q-18",
        "questionId": "q-25456",
        "question": "Design a CKNE-aware API gateway for a shared ingress surface used by 150 microservices. Each tenant has CKNE in [0,1]. Show how CKNE maps to per-tenant admission rate, request batching, and circuit-breaker thresholds; describe dynamic rate updates as CKNE changes and a backpressure protocol; include a minimal payload example: {tenant:'A',ckne:0.42}?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a CKNE-weighted batch queue where per-tenant start rate S_t = baseRate * (1 + alpha*ckne), CPU/memory reservations R_t proportional to ckne, and preemption sensitivity P_t = ckne. A minute-by-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a CKNE-aware API gateway for a shared ingress surface used by 150 microservices. Each tenant has CKNE in [0,1]. Show how CKNE maps to per-tenant admission rate, request batching, and circuit-breaker thresholds; d…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "## Solution Overview Maintain a global capacity cap `C` for concurrent DAG runs across all tenants. For each tenant `t`, compute a weight based on CKNE: `w_t = CKNE_t²`. Derive per-tenant budget slots as `Budget_t = max…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "CKNE budgets: samplingRate = base + ckne*(max-base); retentionWindowDays = base + ckne*(max-base); hotStorageShare = base + ckne*(1-base). A minute controller recomputes quotas per tenant, enforcing g",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates your ability to design a production-grade control plane that dynamically responds to CKNE (Consumer-Known-Network-Exposure) drift to preserve SLAs under burst conditions. It tests understanding of multi-tenant resource management, adaptive rate limiting, and fault-tolerance patterns.\n\n## Key Concepts\n\n- **CKNE-aware admission control**: Dynamically adjusting tenant quotas based on network exposure scores\n- **Token-bucket rate limiting**: Algorithmic approach to burst-tolerant request throttling\n- **Backpressure and circuit-breaker patterns**: Preventing cascade failures in shared infrastructure\n- **Dynamic CKNE updates via control channel**: Real-time policy synchronization across gateway instances\n\n## Code Example\n\n```javascript\nfunction maxRps(ckne) {\n  return Math.min(1000, Math.round(100 + 900 * ckne));\n}\n\nfunction computeBackpressure(ckne, currentRps) {\n  const threshold = maxRps(ckne);\n  return {\n    admitted: currentRps <= threshold,\n    throttlePercent: currentRps > threshold \n      ? Math.round(((currentRps - threshold) / currentRps) * 100) \n      : 0,\n    retryAfterMs: currentRps > threshold ? 100 : 0\n  };\n}\n\nfunction circuitBreakerState(ckne, errorRate, p99LatencyMs) {\n  const errorThreshold = 0.05 - (ckne * 0.03);\n  const latencySla = 200 - (ckne * 100);\n  \n  return {\n    shouldOpen: errorRate > errorThreshold || p99LatencyMs > latencySla,\n    errorThreshold,\n    latencySla\n  };\n}\n\nconst examplePayload = { tenant: 'A', ckne: 0.42 };\nconst rps = maxRps(examplePayload.ckne);\nconsole.log(`Max RPS for tenant A: ${rps}`);\n```\n\n**Mapping Details:**\n- **Admission Rate**: Token bucket with `max_tokens = 100 + 900 * ckne` (range: 100–1000 RPS)\n- **Request Batching**: 20–50ms window, batch size inversely proportional to CKNE\n- **Circuit Breaker**: Opens when error rate > 5% - (3% × CKNE) OR p99 latency > 200ms - (100ms × CKNE)\n- **Dynamic Updates**: CKNE changes trigger immediate recalculation; control channel pushes deltas within 100ms",
        "difficulty": "advanced"
      },
      {
        "id": "ckne-q-19",
        "questionId": "ckne-k8s-networking-fundamentals-1768971446111-0",
        "question": "In a cluster with a headless service (ClusterIP: None) backing a StatefulSet, which DNS records are returned when resolving the service name from a client pod?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CKNE-to-concurrency: allocate a base quota per tenant plus CKNE-weighted extra units; cold-start budgets scale with CKNE; memory tiers scale with CKNE to improve latency for high-CKNE tenants. Schedul",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "CKNE maps to per-tenant tokens that cap concurrency from a shared pool. CKNE=0 yields baseline rate; CKNE=1 yields maximum allocation. A 1-minute loop reduces tokens for low-CKNE tenants first and shi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a tiered mapping where CKNE 0.8-1.0 gets 100 requests/minute, 0.5-0.7 gets 50 requests/minute, and below 0.5 gets 10 requests/minute, with a guaranteed minimum of 5 requests for all tenants to maintain basic service.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "- Multiple A records, one per Pod IP backing the StatefulSet. A headless Service (ClusterIP: None) has no single endpoint; DNS resolution of the service name returns the set of Pod IPs that back the StatefulSet. If N po…",
            "isCorrect": true
          }
        ],
        "explanation": "Headless Services expose endpoints directly. CoreDNS (in-cluster) returns an A record per ready endpoint (pod IP). For StatefulSets this means N records for N ready replicas; as pods scale up/down or enter/exit readiness, the DNS results change quickly (TTL is typically short, ~5s). EndpointSlices may distribute endpoints across slices but DNS still resolves to all endpoint IPs. This design enables clients to connect to any backing pod and supports client-side load balancing. Note: StatefulSet hostnames (statefulset-0, -1, …) are separate DNS names; the service DNS provides the endpoints, not a single canonical IP.",
        "difficulty": "intermediate"
      },
      {
        "id": "ckne-q-20",
        "questionId": "q-9744",
        "question": "Design a CKNE-aware API gateway for a multi-tenant SaaS with hundreds of tenants. Each tenant CKNE in [0,1]. Explain how CKNE maps to per-tenant request concurrency, circuit-breaker thresholds, and retry budgets; propose a minute-by-minute loop that throttles low-CKNE tenants and reallocates capacity to high-CKNE tenants while preserving a baseline latency SLA. Include a minimal payload example and a test plan? payload example: {tenant:'A',endpoint:'/v1/data',ckne:0.25}?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CKNE maps to per-tenant tokens that cap concurrency from a shared pool. CKNE=0 yields baseline rate; CKNE=1 yields maximum allocation. A 1-minute loop reduces tokens for low-CKNE tenants first and shi",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "CKNE drives three knobs: policy strictness, retraining cadence, and retry budgets. High CKNE increases checks, speeds model updates, and affords larger retry windows; low CKNE reduces checks and caps",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant token-bucket with budget B_t = SLA_RPS * (1 - CKNE_t); higher CKNE gets more burst. Use fair-queueing with CKNE-based priorities. A minute loop monitors utilization; during spikes throttle",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An API gateway routes traffic by CKNE; map CKNE to per-tenant tokens with a weighted token bucket, tune max concurrent requests and per-endpoint quotas, and enforce circuit-breakers for CKNE below 0.1",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess how CKNE scores translate to practical controls in an API gateway, including per-tenant concurrency, circuit-breakers, and retry budgets, plus a simple scheduling loop and test plan.\n\n## Key Concepts\n\n- CKNE-to-resource mapping\n- Fairness and SLA preservation\n- Lightweight telemetry and safe preemption\n\n## Code Example\n\n```javascript\n// Pseudo minute loop\nfunction ckneMinuteLoop(state){\n  for (const t of state.tenants){\n    if (t.ckne < 0.5) { t.concurrency = Math.max(1, t.concurrency-1); }\n    else { t.concurrency = Math.min(state.maxConcurrency, t.concurrency+1); }\n    t.retryBudget = t.ckne > 0.7 ? t.retryBudget : 1;\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you validate fairness across tenants during bursts?\n- How would you extend this to multi-region deployments?",
        "difficulty": "beginner"
      },
      {
        "id": "ckne-q-21",
        "questionId": "q-24390",
        "question": "Design a CKNE-aware global request router for a multi-tenant API gateway with two backends (A and B) differing in latency and capacity. Each tenant's CKNE in [0,1] should map to per-tenant routing weights, retry budgets, and deadline skew. Explain the mapping you would implement in Envoy or a similar gateway using dynamic metadata, how weights update without restart, and a test plan. Example payload: {tenant:'A',ckne:0.6}?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a CKNE-aware multi-tenant CI/CD runner scheduler with the following architecture: **CKNE Mapping Algorithm:** - Build parallelism: `slots = floor(ckne * max_slots) + baseline_slots` - Cache budget: `cache_gb = ck…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Map CKNE to per-tenant GPU time slices, memory quotas, and preemption thresholds. Preserve baseline SLA (e.g., 95th percentile latency under target ms). Each minute, compute CKNE-weighted shares, reallocate idle GPU sec…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Map CKNE to routing weight as w = 0.5 + 0.5*ckne for the primary; secondary gets 1-w. Tie retry budget to ckne (e.g., 1+2*ckne capped at 5) and adjust per-tenant deadline skew by delta = (1-ckne)*50ms",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Map CKNE to per-tenant ETL concurrency and batch window: c_i = clamp(round(base + alpha*CKNE_i), 1, maxConcur); w_i = clamp(round(minWindow + beta*(maxWindow-minWindow)*CKNE_i), minWindow, maxWindow).",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design per-tenant dynamic routing with CKNE-driven SLAs, including control-plane updates and real-world observability needs.\n\n## Key Concepts\n- CKNE-to-routing-weight mapping and fallback behavior\n- Per-tenant retry budgets and deadline skew\n- Dynamic metadata in Envoy and hot-reloadable config\n- Validation via controlled load tests and metrics\n\n## Code Example\n```javascript\nfunction mapCKNE(ckne){\n  const primary = 0.5 + 0.5 * ckne;\n  const secondary = 1 - primary;\n  const retryBudget = Math.min(1 + 2 * ckne, 5);\n  const deadlineSkewMs = (1 - ckne) * 50;\n  return {primary, secondary, retryBudget, deadlineSkewMs};\n}\n```\n\n## Follow-up Questions\n- How would you handle CKNE drift across tenants during peak hours?\n- What metrics validate correctness of routing and SLA guarantees?",
        "difficulty": "advanced"
      },
      {
        "id": "ckne-q-22",
        "questionId": "q-1214",
        "question": "Design a CKNE-aware data lineage policy for a three-stage ETL pipeline (ingest → transform → load) servicing thousands of tenants. Each hop attaches CKNE health in trace metadata. Propose a per-tenant degradation policy that preserves auditability for high‑SLA tenants while shedding heavy lineage data during degradation. Include payload schema, a minute-by-minute decision loop, and a minimal payload example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CKNE maps to per-tenant: batch_size, max_concurrency, retry_backoff, and message TTL. Implement a priority queue keyed by CKNE; create a minute-by-minute loop that trims low-CKNE batch sizes and expan",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Map per-tenant CKNE to a token budget with p = 0.05 + 0.95*ckne, cap at 1. Route higher-CKNE tenants first via priority queues; enforce circuit-breakers for tenants dipping below SLA. Run a 60s contro",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design CKNE-aware data lineage for a 3-stage ETL (ingest → transform → load) servicing thousands of tenants. Propagate CKNE at each hop; degrade by keeping tenant, timestamp, and essential markers whi",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Map CKNE to per-tenant cache quotas (bytes) and eviction priorities. Use a minute-based loop to reclaim a portion of low-CKNE tenants' cache keys during spikes and rebind to high-CKNE tenants, preserv",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical CKNE usage in data lineage, balancing auditability with performance across tenants.\n\n## Key Concepts\n- CKNE health propagation across ETL hops\n- Per-tenant degradation policy with auditability\n- Minimal vs full data lineage during health degradation\n- Minute-by-minute control loop and testability\n\n## Code Example\n```javascript\n// Decide whether to drop heavy lineage fields based on CKNE health\nfunction shouldDropLineage(health, thresholdLatency=200, thresholdQueue=1000){\n  const degraded = health.latency > thresholdLatency || health.queueDepth > thresholdQueue || health.errorRate > 0.01;\n  return degraded;\n}\n```\n\n## Follow-up Questions\n- How would you test with synthetic tenants and traffic bursts?\n- How do you ensure privacy/compliance while preserving essential auditing?",
        "difficulty": "beginner"
      },
      {
        "id": "ckne-q-23",
        "questionId": "q-13168",
        "question": "Design a CKNE-aware per-tenant feature flag rollout for a shared frontend serving 1k tenants. Each tenant has CKNE in [0,1]. Explain mapping CKNE to per-tenant rollout fraction for a new dashboard feature, and implement a minute-by-minute reallocation loop that preserves baseline exposure while favoring high-CKNE tenants. Include payload example: {tenant:'A',feature:'new-dashboard',ckne:0.25} and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a CKNE-aware multi-tenant in-memory cache layer that dynamically allocates resources based on tenant CKNE scores. Map CKNE values to per-tenant memory quotas, eviction cadence, and prefetch budgets. Implement a m…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Map rollout fraction per tenant as f = base + gamma * CKNE, with base=0.05 and gamma=0.9. Recompute every minute and push to a distributed flag store read by the frontend router. Baseline exposure 5%;",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "CKNE maps to rollout cadence and rollback windows: CKNE 0-0.3 slows rollout and tightens rollback; CKNE >0.7 accelerates rollout with longer rollback windows. Use per-tenant feature namespaces with op",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a CKNE-weighted cache controller. Each minute, derive per-tenant weight from CKNE, cap = base_cap * (0.5 + 0.5*CKNE). Evict with per-tenant LFU while applying time-decay to prevent starvatio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design CKNE-aware rollout with fairness, SLA, and dynamic reallocation.\n\n## Key Concepts\n- CKNE-based prioritization\n- Feature flag rollout semantics\n- Telemetry and rollback criteria\n- Multi-tenant routing and caching effects\n\n## Code Example\n```javascript\n// function to compute rollout fraction per tenant\nfunction rolloutFraction(base, gamma, ckne){return base + gamma*ckne}\n```\n\n## Follow-up Questions\n- How would you handle tenants with volatile CKNE?\n- How to ensure monotonic adoption without SLA breaches?",
        "difficulty": "intermediate"
      },
      {
        "id": "ckne-q-24",
        "questionId": "q-4675",
        "question": "Design a CKNE-aware multi-tenant FaaS scheduler for a shared edge region (serverless platform powering responsive microservices). Each tenant has a CKNE score 0–1. Explain how CKNE maps to per-tenant function concurrency, memory budgets, and cold-start behavior. Propose a minute-by-minute degradation loop that throttles low-CKNE tenants (e.g., cap concurrent invocations, extend cold-start latency) and rebinding capacity to high-CKNE tenants while preserving a baseline SLA. Include a minimal payload example: payload: {tenant:'A',fn:'checkout',ckne:0.25} and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CKNE maps to per-tenant publish rate cap, consumer parallelism, and backlog pruning cadence. Implement a minute loop that computes backlog and latency per tenant, throttles low-CKNE tenants by lowerin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "CKNE maps to per-tenant concurrency caps, memory budgets, and a dynamic worker pool. The loop runs every minute: if CKNE < 0.3, throttle by lowering max concurrent invocations and mildly extending col",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a CKNE-aware shared in-memory cache layer for multi-tenant workloads. Map CKNE to per-tenant quota q_t = base + ckne*max; implement a CKNE-weighted eviction policy that prioritizes higher-CKNE",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To diagnose a CKNE microservice anomaly in real time, implement a windowed streaming detector (5s tumbling windows) with trace-aware metrics, compute p95 latency and error rate, and emit correlated al",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes the ability to design a real-time scheduler that adapts to CKNE-based fairness across tenants, ensuring SLA for high-CKNE tenants while containing impact on others.\n\n## Key Concepts\n- CKNE-to-resource mapping: concurrency caps, memory budgets, worker pools\n- Minute-by-minute degradation loop with fair preemption\n- Baseline SLA guarantees and telemetry signals\n\n## Code Example\n```javascript\n// Pseudo-code for minute loop concept\nfunction adjustWeights(tenants){ /*...*/ }\n```\n\n## Follow-up Questions\n- How would you test for starvation under skewed CKNE distributions?\n- What failure modes require emergency preemption and how would you detect them?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-19T05:22:27.837Z",
    "version": 1
  }
]