[
  {
    "id": "test-pca",
    "channelId": "pca",
    "channelName": "PCA",
    "title": "PCA Knowledge Test",
    "description": "Test your understanding of PCA with 25 questions.",
    "questions": [
      {
        "id": "pca-q-0",
        "questionId": "q-29632",
        "question": "You're collecting 32 numeric features per image frame from a manufacturing camera, with occasional missing pixels causing missing values and lighting drift changing distributions. Design a beginner PCA-based feature extractor to reduce to 4 components for a lightweight edge classifier that flags potential defects in real time. Specify preprocessing (imputation and scaling), whether to fit PCA globally or per-camera, how to select 4 components under a strict 15 ms latency budget, and how to interpret loadings into defect signals. Include a concrete example of a top-loading feature and its defect-related meaning?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement online Incremental PCA with 12 components. Preprocess numeric features with EWMA scaling; normalize embedding features; handle missing data in batches. Decide global vs regional PCA by data",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Maintain a rank-k online PCA; choose k by cumulative explained variance and drift-aware thresholds. Update with a forgetting factor (e.g., 0.95) and online imputation (EM/mean). Detect drift via chang",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Impute numeric features with mean; keep binary indicators as 0/1 and standardize only the numeric subset. Apply PCA on standardized numerics (avoid inflating variance from one-hot encodings). Select n",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Impute missing values using column-wise mean imputation, then standardize all features to zero mean and unit variance. Use IncrementalPCA with n_components=4, fitted globally across all cameras, to satisfy the 15 ms lat…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to translate a PCA pipeline to a real-time edge scenario with missing data and drift. Evaluates decisions on global versus per-device PCA, component selection under latency constraints, and interpretation of loadings for actionable defect signals.\n\n## Key Concepts\n- IncrementalPCA for streaming data\n- Imputation and scaling\n- Global versus per-camera PCA\n- Latency budgeting and component selection\n- Interpreting loadings as defect signals\n\n## Code Example\n```python\nfrom sklearn.impute import SimpleImputer\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.decomposition import IncrementalPCA\n\nimputer = SimpleImputer(strategy='mean')\nscaler = StandardScaler()\npca = IncrementalPCA(n_components=4)\n\nX_imputed = imputer.fit_transform(X)\nX_scaled = scaler.fit_transform(X_imputed)\npca.fit(X_scaled)\n\nX_reduced = pca.transform(X_scaled)\n```",
        "difficulty": "beginner"
      },
      {
        "id": "pca-q-1",
        "questionId": "q-14350",
        "question": "In a PayPal mobile wallet analytics stream, 60 numeric features per session must be reduced on-device to meet an 8 MB RAM constraint. Propose a beginner PCA-based preprocessing plan that fits memory, selecting n_components and minibatch, handling missing data, and mapping top loadings to 3–5 per-session risk flags for a lightweight fraud detector?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Sparse PCA (SPCA) to enforce interpretable loadings (L1 penalty) and handle missing data via EM-SPCA or prior imputation (MICE). Run a grid over n_components and sparsity lambda, selecting by",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an RBF kernel with random Fourier features to approximate kernel PCA for the 40 streaming features. Map these to 128 Fourier features, then maintain a 6-component representation via online SVD with small-batch updat…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an edge-compatible IncrementalPCA pipeline: impute missing values with mean imputation, scale features using StandardScaler, and apply IncrementalPCA with n_components=6 using batch_size=128 to satisfy the 8 M…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Build an end-to-end pipeline: (1) Impute missing values - median for continuous, most_frequent for binary/categorical, with missing indicators. (2) Scale - StandardScaler for continuous, keep binary 0/1 as-is. (3) Compo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical implementation of on-device PCA under strict memory constraints for real-world fintech applications, evaluating understanding of data imputation, feature scaling, and the critical skill of translating statistical loadings into actionable fraud detection signals.\n\n## Key Concepts\n- IncrementalPCA for memory-efficient, streaming dimensionality reduction\n- Optimizing component count to balance variance preservation against computational limits\n- Interpreting component loadings to generate concise, actionable risk indicators\n\n## Code Example\n```python\nfrom sklearn.impute import SimpleImputer\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.decomposition import IncrementalPCA\nimport numpy as np\n\n# Initialize pipeline components\nimputer = SimpleImputer(strategy='mean')\nscaler = StandardScaler()\nipca = IncrementalPCA(n_components=6, batch_size=128)\n\n# Process data in streaming batches\ndef process_batch(batch):\n batch_imputed = imputer.fit_transform(batch)\n batch_scaled = scaler.fit_transform(batch_imputed)\n return ipca.partial_fit(batch_scaled)\n\n# Extract risk flags from loadings\ndef extract_risk_flags(ipca, n_flags=4):\n components = ipca.components_\n risk_flags = []\n for i, component in enumerate(components):\n  top_features_idx = np.argsort(np.abs(component))[-n_flags:]\n  risk_flags.append({\n   'component': i,\n   'top_features': top_features_idx.tolist(),\n   'loadings': component[top_features_idx].tolist()\n  })\n return risk_flags\n```\n\n## Memory Considerations\nWith 60 features, batch_size=128 requires approximately 60×128×8 bytes ≈ 61 KB per batch, well within the 8 MB constraint. IncrementalPCA maintains constant memory complexity proportional to batch_size×n_features.",
        "difficulty": "beginner"
      },
      {
        "id": "pca-q-2",
        "questionId": "q-7868",
        "question": "In a real-time gateway for PayPal, streaming 60 numeric features per event with occasional missing values and nonstationary drift, design a streaming PCA solution to reduce to 6 components with <5 ms per event. Explain online imputation, robust centering, incremental SVD, drift-detection windowing, and how to translate top loadings into governance-safe alerts while preserving latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use online PCA with a sliding window and robust updates (e.g., River IncrementalPCA with online mean imputation, robust centering, and incremental SVD). Keep 6 components by tracking explained varianc",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use online PCA (incremental CCIPCA/Oja) with sparse input support; impute missing values by feature means, then standardize. Retain components to cover ~95% variance, updating on e.g. 1k events. Detec",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "IPCA with partial_fit on micro-batches; maintain running mean/std for scaling; streaming imputation via forward-fill; drift detection with ADWIN and trigger re-fit; retain 12 components to explain ~0.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an online PCA using incremental SVD with running mean/variance. Online imputation uses last observed value; standardize features on the fly. Keep k=12 components to cover ~95% variance; update every 10k events…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nInterview context explanation.\n\n## Key Concepts\n\n- Concept 1\n- Concept 2\n- Concept 3\n\n## Code Example\n\n```javascript\n// Implementation code here\n```\n\n## Follow-up Questions\n\n- Follow-up question 1\n- Follow-up question 2",
        "difficulty": "intermediate"
      },
      {
        "id": "pca-q-3",
        "questionId": "q-15885",
        "question": "In a federated analytics setup for Cloudflare’s edge telemetry, each data center streams 40 numeric features per sample and locally updates a 24-component PCA without sharing raw data. Describe how to perform incremental SVD, online-imputation for missing values, drift detection on loadings, and translating drift into robust, low-latency alerts for a global 3-second dashboard?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a real-time tensor PCA approach using CP decomposition over rolling windows (features × time × location). Update with streaming SGD; select mode ranks by incremental explained variance. Handle",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Halko-style incremental SVD to update U, S, V matrices with arriving mini-batches while preserving orthonormality. Apply online imputation for missing values using feature-wise exponential moving averages or lightwe…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a Federated Incremental PCA architecture with secure aggregation, differential privacy (DP) noise, and drift-adaptive learning. Each tenant maintains a local buffer, imputes missing values using a trained regres…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an online PCA using incremental SVD to maintain a 12-component subspace on a stream of 150 features with missing data and nonstationarity. Propose online imputation, robust scaling, and a robus",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical knowledge of online PCA in a privacy-preserving federated setting, combining incremental linear algebra, robust imputation, drift detection on eigenvectors, and latency-sensitive alerting.\n\n## Key Concepts\n- **Online PCA (Incremental SVD)**: Halko-style randomized methods for efficient batch-wise updates to the principal components\n- **Federated Averaging**: Aggregating covariance or second-order statistics without centralizing raw data\n- **Online Imputation**: Maintaining running statistics (EMAs) or lightweight predictors to handle missing values as data streams\n- **Drift Detection**: Monitoring eigenvalue stability and loading vector changes via cumulative sum (CUSUM) or Page-Hinkley tests\n- **Latency Budgeting**: End-to-end pipeline must complete within the 3-second dashboard refresh window\n\n## Code Example\n```python\nfrom sklearn.decomposition import IncrementalPCA\nimport numpy as np\n\nipca = IncrementalPCA(n_components=24)\nema_means = np.zeros(40)  # feature-wise EMA for imputation\nema_counts = np.zeros(40)\n\ndef online_impute(batch, ema_means, ema_counts, alpha=0.01):\n    # Update EMA for each feature\n    for i in range(batch.shape[1]):\n        mask = ~np.isnan(batch[:, i])\n        if mask.any():\n            ema_means[i] = alpha * batch[mask, i].mean() + (1 - alpha) * ema_means[i]\n            ema_counts[i] += mask.sum()\n    # Fill NaN with current EMA\n    batch = np.where(np.isnan(batch), ema_means, batch)\n    return batch, ema_means, ema_counts\n\nfor batch in data_stream:\n    batch, ema_means, ema_counts = online_impute(batch, ema_means, ema_counts)\n    ipca.partial_fit(batch)\n    \n    # Drift detection on loadings\n    current_loadings = ipca.components_\n    loading_change = np.linalg.norm(current_loadings - prev_loadings) if prev_loadings is not None else 0\n    if loading_change > drift_threshold:\n        emit_alert(loading_change)\n    prev_loadings = current_loadings\n```\n\n## Alerting Strategy\n1. Compute Mahalanobis distance between current batch loadings and baseline\n2. Apply exponentially weighted moving average to smooth noise\n3. Trigger alerts when cumulative drift exceeds configurable epsilon\n4. Route alerts to dashboard with sub-second latency via streaming query engine",
        "difficulty": "intermediate"
      },
      {
        "id": "pca-q-4",
        "questionId": "q-29537",
        "question": "In a multi-tenant financial analytics platform, 120 numeric market-features per instrument per second arrive with missing data and heavy tails. Design a streaming PCA solution that maintains 12 components globally, with per-instrument drift monitoring and per-tenant fairness constraints. Explain how to handle missing data, compare Probabilistic PCA vs Robust Incremental PCA, set update cadence and latency, and translate top loadings into risk signals or regulatory alerts. Include an example top-loading feature and its interpretation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement online probabilistic PCA with missing-data support and mini-batch updates. Impute numerics with median, proxies with region-specific statistics; use IncrementalPCA to maintain 8 components.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I propose an online PCA pipeline using IncrementalPCA with 10 components, processing streaming mini-batches with mean imputation for missing values and z-score scaling. For drift detection, implement a sliding window ap…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use online PCA (incremental CCIPCA/Oja) with sparse input support; impute missing values by feature means, then standardize. Retain components to cover ~95% variance, updating on e.g. 1k events. Detec",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a streaming Robust Incremental PCA with 12 components, anchored globally but calibrated per-instrument. Implement online imputation (per-instrument mean with variance-aware scaling) and robust sca",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests designing a streaming PCA system for high-throughput finance with missing data, heavy tails, drift, and fairness constraints, plus translating components into actionable risk signals.\n\n## Key Concepts\n\n- Streaming/incremental PCA\n- Robust imputation and scaling for heavy tails\n- Drift detection via loadings stability and explained variance\n- Global vs per-instrument calibration\n- Fairness/regulatory alerting across tenants\n\n## Code Example\n\n```javascript\n// Skeleton initialization\nimport { RobustIncrementalPCA } from 'pricing-pca'\nconst pca = new RobustIncrementalPCA({ nComponents: 12 })\npca.partialFit(batchOfFeatures)\nconsole.log(pca.explainedVarianceRatio_)\n```\n\n## Follow-up Questions\n\n- How would you set drift thresholds and escalation paths?\n- How would you validate fairness and regulatory compliance in production?",
        "difficulty": "advanced"
      },
      {
        "id": "pca-q-5",
        "questionId": "q-23275",
        "question": "In a real-time content-embedding pipeline used for ad targeting, 2048-dim feature vectors arrive per frame with occasional missing channels due to compression. Design an online PCA that preserves 64 components for low-latency anomaly detection. Include imputation, update cadence, drift detection, and translating drift into alerts; compare to Probabilistic PCA and Robust PCA in streaming; provide an evaluation plan and deployment notes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an online CCIPCA/Oja-based PCA on a rolling window with GPU kernels for vectorized operations. Use online mean imputation with per-feature scaling, updating 12 components every 50 ms to consistently meet the 1…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Online PCA with a rolling covariance window and masked imputation. Maintain 64 components; update every 5 frames; drift via KL divergence of loading vectors; translate drift thresholds into alert leve",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Impute missing with feature means, scale to [0,1] using training mins/maxs, apply a fixed 5x40 PCA basis learned offline on global data, then project y = (x-mean)·W on-device. Global PCA keeps cross-u",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a federated online PCA variant that shares encrypted sufficient statistics (e.g., per-tenant covariance updates) and aggregates to 12 global components. Apply DP by adding Gaussian noise to the pe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design online PCA in production, handling drift, latency, and cross-method trade-offs in a high-throughput content-embedding pipeline.\n\n## Key Concepts\n\n- Online/incremental PCA with rolling covariance\n- Missing-channel imputation in streaming\n- Drift detection on loading vectors (e.g., KL divergence, CDF shifts)\n- Latency/memory constraints in a real-time system\n- Deployment in a streaming feature store\n\n## Code Example\n\n```python\nimport numpy as np\n\n# skeleton: incremental update using masked data\ndef online_pca_update(X_batch, Mask, components, n_components=64):\n    # X_batch: (B, D); Mask: (B, D) boolean mask of observed\n    # components: current principal axes (D, n_components)\n    # Implement masked covariance update and re-fit incrementally\n    pass\n```\n\n## Follow-up Questions\n\n- How would you validate drift thresholds offline vs online?\n- How would you handle rapid concept drift without alert floods?",
        "difficulty": "advanced"
      },
      {
        "id": "pca-q-6",
        "questionId": "q-12683",
        "question": "At Snap, design a GPU-accelerated streaming PCA module for a real-time ad-quality scoreboard: 300 numeric features per impression arriving at 8k impressions/sec, with frequent missing values and nonstationarity. Keep 25 components; use online imputation and per-feature scaling; update loadings via incremental SVD/Oja every 100 ms; implement drift detection on loadings with robust alert thresholds. How do you translate drift into alerts while keeping latency under 50 ms per batch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Impute missing values with feature means, scale features with a stable scaler, drop near-zero variance features, and apply Incremental PCA to derive 6 components. Choose components by explaining at le",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a streaming DP-PCA that perturbs the covariance to satisfy epsilon-delta, then update the top-12 components incrementally. Use a fixed privacy budget split over time, monitor loadings drift with",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a GPU-accelerated streaming PCA that maintains 25 components from 300 features per impression at 8k IPS, with missing data and nonstationarity. Use online imputation and per-feature scaling; up",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Incremental PCA on-device with 12 components, updated in ~100 ms blocks to meet 200 ms latency. Per-feature running z-score normalization and forward-fill for missing values; compare IPCA, PPCA, and R",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests a real-time, GPU-accelerated streaming PCA design with online imputation, drift handling, and actionable alerts under tight latency.\n\n## Key Concepts\n- Streaming incremental PCA (SVD/Oja)\n- Online imputation and scaling\n- Drift detection on loadings; alerting strategies\n- GPU memory, batching, and latency trade-offs\n\n## Code Example\n```javascript\n// Pseudo\ninitialize(PCA_K=25)\nfor batch in stream:\n  X = impute(batch)\n  X = scale(X)\n  update_loadings(X)\n  if drift(loadings):\n     raise_alert()\n```\n\n## Follow-up Questions\n- How would you validate drift thresholds in production?\n- How handle regime shifts with hysteresis and fallback paths?",
        "difficulty": "intermediate"
      },
      {
        "id": "pca-q-7",
        "questionId": "q-9560",
        "question": "You're operating an online analytics platform processing a high-velocity stream of 120 numeric features per session. Data drift over time requires a real-time, compact PCA representation for anomaly detection. Propose an online PCA approach (incremental PCA or randomized SVD) that updates with a sliding window, handles missing values, preserves orthogonality, and decides when to refresh components. Outline latency, memory, and drift validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Impute missing values with median, scale features to unit variance, then apply DP-aware PCA on the noisy data. Keep 10 components that explain about 75–85% of variance. Interpret top loadings to flag",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Incremental PCA with a sliding window of recent batches, using partial_fit to update the top components and re-orthogonalization to preserve orthogonality. Impute missing values within the window (mea",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Each bank runs online imputation and z-scoring, then updates a 12-component Incremental PCA on every batch. Use secure aggregation to combine local loadings into a global model without sharing raw dat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement online incremental PCA using an updating SVD or Oja-style rule over a rolling window (e.g., 5k samples) to produce 6 components. Use windowed mean imputation for missing data and streaming s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design an online PCA pipeline for streaming data, balancing drift handling, latency, and memory, with a clear refresh policy.\n\n## Key Concepts\n- Online vs batch PCA\n- Incremental SVD/partial_fit\n- Sliding window vs forgetting factor\n- Streaming missing value handling\n- Orthogonality maintenance\n- Drift detection criteria (explained variance, reconstruction error, CUSUM)\n- Latency and memory budgets\n\n## Code Example\n```javascript\n// Skeleton: OnlinePCA with incremental updates\nclass OnlinePCA {\n  constructor(nComponents, windowSize) { /* init */ }\n  fitInitial(batch) { /* compute first components */ }\n  partialFit(batch) { /* update components with new data */ }\n  transform(batch) { /* project data */ }\n}\n```\n\n## Follow-up Questions\n- How would you validate drift in production and trigger refresh?\n- How would you adapt window size if latency spikes?\n- How would you handle new features or dropped features in the stream?",
        "difficulty": "intermediate"
      },
      {
        "id": "pca-q-8",
        "questionId": "q-1534",
        "question": "Edge PCA on 30 numeric sensors: design a beginner-friendly pipeline that runs on a Raspberry Pi for real-time data compression and anomaly flagging. Describe normalization and simple imputation, how to pick components (explained variance with knee), how to interpret top loadings for operators, and a lightweight drift check over a day with few dependencies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use z-score normalization and mean imputation; implement IncrementalPCA to stay light on RAM. Pick n_components to reach 85–90% explained variance, using a knee heuristic. Top loadings reveal sensor groups driving varia…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Outline an online SPCA approach for a 1000-feature streaming telemetry dataset with missing values and drift. Target 30 sparse components; compare online SPCA (thresholding) vs incremental SVD with sp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use online sparse-group PCA with a forgetting factor on a rolling window; enforce block-sparsity via mixed L1/L2 regularization by block during incremental SVD updates; update every 1–5 seconds to kee",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Impute missing values using column-wise mean imputation, then standardize all features to zero mean and unit variance. Use IncrementalPCA with n_components=4, fitted globally across all cameras, to satisfy the 15 ms lat…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical PCA deployment at the edge, including data prep, component selection, loadings interpretation, and drift monitoring under resource limits.\n\n## Key Concepts\n- Incremental/online PCA for constrained devices\n- Handling missing values with simple imputation\n- Variance-based component selection and knee method\n- Interpreting loadings for actionable monitoring\n- Lightweight drift detection and re-fitting criteria\n\n## Code Example\n```python\nfrom sklearn.decomposition import IncrementalPCA\nfrom sklearn.impute import SimpleImputer\nfrom sklearn.preprocessing import StandardScaler\nimport numpy as np\n\nclass EdgePCA:\n    def __init__(self, n_components=None, variance_threshold=0.85):\n        self.scaler = StandardScaler()\n        self.imputer = SimpleImputer(strategy='mean')\n        self.pca = IncrementalPCA(n_components=n_components)\n        self.variance_threshold = variance_threshold\n        \n    def fit(self, X_batch, batch_size=100):\n        # Impute and normalize\n        X_clean = self.imputer.fit_transform(X_batch)\n        X_scaled = self.scaler.fit_transform(X_clean)\n        \n        # Incremental fitting\n        for i in range(0, len(X_scaled), batch_size):\n            batch = X_scaled[i:i+batch_size]\n            self.pca.partial_fit(batch)\n            \n        # Select components based on explained variance\n        cumvar = np.cumsum(self.pca.explained_variance_ratio_)\n        n_components = np.argmax(cumvar >= self.variance_threshold) + 1\n        self.pca.n_components = n_components\n        \n    def transform(self, X):\n        X_clean = self.imputer.transform(X)\n        X_scaled = self.scaler.transform(X_clean)\n        return self.pca.transform(X_scaled)\n        \n    def get_top_loadings(self, n_top=3):\n        loadings = self.pca.components_.T\n        top_indices = np.argsort(np.abs(loadings), axis=0)[-n_top:]\n        return top_indices\n        \n    def drift_check(self, X_new, window_size=1000):\n        X_transformed = self.transform(X_new)\n        X_reconstructed = self.pca.inverse_transform(X_transformed)\n        reconstruction_error = np.mean((X_new - X_reconstructed) ** 2)\n        return reconstruction_error\n```",
        "difficulty": "beginner"
      },
      {
        "id": "pca-q-9",
        "questionId": "q-20204",
        "question": "Design a federated, privacy-preserving PCA pipeline to reduce 200 numeric features per session on a global SaaS telemetry stream to 12 components for a real-time anomaly dashboard. The data tenants must not share raw features. Explain (1) how to compute global loadings from local updates without raw data exchange, (2) how to apply differential privacy to the updates and its impact on explained variance, (3) how to detect drift in component loadings across tenants and alert operators, (4) latency constraints and trade-offs between local preprocessing vs central aggregation, (5) a validation plan using synthetic data and tell-tale metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement online PCA with incremental SVD on a rolling window (e.g., 2000 events) and a forgetting factor. Normalize and log-transform skewed features; streaming-impute missing values with last observ",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a federated online PCA variant that shares encrypted sufficient statistics (e.g., per-tenant covariance updates) and aggregates to 12 global components. Apply DP by adding Gaussian noise to the pe",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Incremental SVD with a robust normalization (MAD-based scale) and online EM-like imputation guided by current loadings. Update 12 components in micro-batches (≤100 ms). Detect drift via principal",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use IncrementalPCA with a fixed number of components (e.g., 6) and small batches (e.g., 200 samples). Center and scale per batch, maintain running mean and variance. Update components hourly, monitor explained variance,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes federated privacy, differential privacy, and real-time PCA under strict data ownership. It tests handling of drift, latency, and validation in a practical setup.\n\n## Key Concepts\n- Federated PCA on tabular data\n- Differential privacy for sufficient statistics\n- Drift detection in component loadings across tenants\n- Real-time constraints and secure aggregation\n\n## Code Example\n```javascript\n// placeholder example: not executed in interview\n```\n\n## Follow-up Questions\n- How would you extend to non-Gaussian features or missing data?",
        "difficulty": "intermediate"
      },
      {
        "id": "pca-q-10",
        "questionId": "q-14739",
        "question": "Design a privacy-preserving PCA in a distributed analytics platform where tenants never share raw features. Use secure multi-party computation or homomorphic encryption to compute a common 20-component subspace, handle missing values, select components, and update loadings with drift detection. Explain translating top loadings into governance-safe alerts within a 2-second latency budget?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline an online SPCA approach for a 1000-feature streaming telemetry dataset with missing values and drift. Target 30 sparse components; compare online SPCA (thresholding) vs incremental SVD with sp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose an edge streaming Incremental PCA: use mean imputation and running scaler, encode cyclic features with sin/cos, apply IncrementalPCA to 12 components. Choose components to explain ~95% varianc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose computing covariance or SVD via MPC/HE with secret sharing to avoid revealing raw features. Use per-tenant imputation and standardization, then aggregate to 20 components by explained variance",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement online probabilistic PCA with missing-data support and mini-batch updates. Impute numerics with median, proxies with region-specific statistics; use IncrementalPCA to maintain 8 components.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests privacy-preserving distributed PCA design, trade-offs MPC vs HE, handling missing data, drift-aware alerting, and latency.\n\n## Key Concepts\n- Privacy-preserving analytics across tenants\n- Missing-value handling in PCA pipelines\n- Drift detection on loadings and alerting\n- Latency-aware incremental PCA updates\n\n## Code Example\n```python\ndef drift_detect(curr, prev, thresh):\n    import numpy as np\n    return np.linalg.norm(curr - prev) > thresh\n```\n\n## Follow-up Questions\n- Compare MPC vs HE for this workload; what changes latency?\n- How to validate drift thresholds and minimize false alerts?",
        "difficulty": "advanced"
      },
      {
        "id": "pca-q-11",
        "questionId": "q-15105",
        "question": "In a Plaid/Lyft/PayPal-like platform ingesting 15k numeric features from thousands of institutions, design a scalable PCA-based pipeline that enforces structured sparsity for interpretability and governance-friendly loadings. Describe online updates, missing-value handling, drift detection, how to map top loadings to per-group alerts, and ensure dashboard latency stays within a few seconds?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use incremental PCA with streaming SVD on a fixed-size window; impute missing values by feature-wise running means; scale with running mean/variance; retain components to explain 95% variance; monitor",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement online PCA with incremental SVD updates, keeping a running mean and a forgetting factor. For missing values, impute online with the current feature means. Track subspace drift using Procrust",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use online sparse Bayesian PCA with group-Lasso to enforce structured sparsity, update components incrementally, and apply per-group imputation. Track drift via loading stability and explained varianc",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use IncrementalPCA with n_components=12 and per-region online preprocessing: median imputation for 70 features, StandardScaler with partial_fit, then IncrementalPCA.partial_fit on 5–10s windows. Apply",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests scalable, interpretable PCA at scale with grouped features, drift handling, and governance-facing alerting. It probes online learning, missing value strategies, and latency-aware design.\n\n## Key Concepts\n\n- Online sparse PCA with group sparsity\n- Per-group imputation and normalization\n- Concept-drift detection on loadings and explained variance\n- Governance-friendly alerting mapped from loadings\n\n## Code Example\n\n```python\n# Pseudo: online update of group-sparse PCA state\ndef online_pca_update(batch, state):\n    X = impute_missing(batch)\n    state.components = incremental_pca_update(state.components, X, state.group_map)\n    state.loadings = project_loadings(state.components)\n    return state\n```\n\n## Follow-up Questions\n\n- How would you evaluate the stability of loadings across rolling windows?\n- What latency profiling would you implement to guarantee sub-second updates on dashboards?",
        "difficulty": "advanced"
      },
      {
        "id": "pca-q-12",
        "questionId": "q-10736",
        "question": "Design a distributed online PCA for real-time fraud detection across three regional data planes. Each event has 40 numeric features with occasional masking; you must maintain 12 principal components with sub-second inference latency. Describe initialization, per-region imputation/normalization, incremental SVD update cadence, cross-region component alignment, drift detection, and how to translate top loadings into governance-safe alerts while respecting latency and memory constraints?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use mixed-data PCA (PCAmix/CA-PCA) with incremental updates; impute with time-aware regional mean for numeric and a simple mode for binaries, scale numerics, keep binaries as 0/1. Maintain a global mo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use River's IncrementalPCA (or randomized SVD) with partial_fit; impute missing values via streaming median per feature; scale online with running mean/std; select 15 components to retain 90%+ explain",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a federated online PCA variant that shares encrypted sufficient statistics (e.g., per-tenant covariance updates) and aggregates to 12 global components. Apply DP by adding Gaussian noise to the pe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a distributed online PCA system using incremental SVD on region-local minibatches to maintain 12 principal components. Each regional data plane independently handles missing value imputation using local mean v…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates expertise in distributed streaming PCA, cross-region model consistency, drift detection, and governance-compliant alerting under strict latency constraints—critical skills for large-scale fraud detection systems.\n\n## Key Concepts\n\n- Distributed online PCA for real-time processing\n- Incremental SVD for efficient model updates\n- Cross-region alignment using Procrustes analysis\n- Drift detection with ADWIN algorithms\n- Governance-safe feature interpretation\n\n## Code Example\n\n```python\n# Pseudocode: incremental update per region\nU, S, Vt = incremental_svd_update(\n    batch_minibatch, \n    current_components,\n    n_components=12\n)\n\n# Cross-region alignment\naligned_components = procrustes_align(\n    region1_components,\n    region2_components, \n    region3_components\n)\n\n# Drift detection\nif adwin_detector.detect_drift(reconstruction_error):\n    trigger_model_retraining()\n```",
        "difficulty": "advanced"
      },
      {
        "id": "pca-q-13",
        "questionId": "q-18175",
        "question": "In a real-time fraud-detection pipeline for a payments system, you process 100 numeric features plus 40 categorical features encoded as target-encoded scalars per transaction, aiming to preserve 10 PCA components for a 200 ms scoring step. Propose a rolling-window PCA with adaptive forgetting (alpha) and online SVD; specify preprocessing, how to handle missingness and categorical encoding drift, how to compute dynamic explained variance, and trigger latency-safe alerts?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a rolling-window PCA with adaptive forgetting for 200 ms scoring: 100 numeric features, 40 categorical via target encoding. Use online SVD to update 10 components every batch. Address missing v",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement Incremental Probabilistic PCA (IPCA) with a sliding window and warm starts. Use per-turbine PCA models initialized with shared global statistics to maintain stability while capturing device-specific behavior.…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy Incremental PCA with per-region whitening and soft imputation; normalize using region-specific statistics; retain 6 components to meet the 5 ms latency constraint on edge GPUs. Detect drift via subspace angle and…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an online IncrementalPCA pipeline with minibatch processing and partial_fit for streaming adaptation. Employ online mean imputation and feature scaling to handle missing data while maintaining normalization. D…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests building a real-time, drift-aware dimensionality reducer with production latency constraints.\n\n## Key Concepts\n- Rolling-window PCA with online SVD\n- Adaptive forgetting and drift detection in loadings\n- Handling mixed features via encoding before PCA\n- Latency budgeting and alerting strategies\n\n## Code Example\n```javascript\nclass RollingPCA {\n  constructor(k, windowMs){/*...*/}\n  update(batch){/* online SVD update */}\n  score(x){/* project and reconstruct error */}\n}\n```\n\n## Follow-up Questions\n- How would you monitor for drift false positives?\n- How would you validate component stability across data shifts?",
        "difficulty": "intermediate"
      },
      {
        "id": "pca-q-14",
        "questionId": "q-16548",
        "question": "Design a cross-tenant, streaming PCA pipeline in a Databricks/Snowflake lakehouse that ingests 80 features across 60 tenants with varying schemas; implement sparse, privacy-aware PCA to retain 20 components, handle missing data, and detect drift in component loadings with per-tenant alerts; describe dataflow, latency goals (<1s micro-batch), governance-ready explanations, and evaluation plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use streaming PPCA with a variational posterior over 16 factors, updated via incremental statistics on a sliding window. Handle MNAR with Bayesian imputation and ELBO-driven component reallocation. Tr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement online PCA with incremental SVD updates, keeping a running mean and a forgetting factor. For missing values, impute online with the current feature means. Track subspace drift using Procrust",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement online incremental PCA (IPCA) with a moving window and forgetting factor to adapt to concept drift. Support dynamic feature addition/removal through partial-fit embedding of new dimensions while maintaining or…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: implement sparse incremental PCA in a Spark-based lakehouse to serve 60 tenants with heterogeneous schemas. Use a shared feature atlas, online updates for 20 components, and per-tenant DP bu",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAdvanced multi-tenant PCA with streaming data tests scalable online learning, privacy budgets, drift detection, and explainability in a lakehouse setting.\n\n## Key Concepts\n- Sparse incremental PCA and online updates\n- Multi-tenant feature atlas and schema alignment\n- Per-tenant differential privacy budgets\n- Drift detection on component loadings and alerting\n- Low-latency streaming in Databricks/Snowflake environments\n\n## Code Example\n\n```javascript\n// Implementation example drift detection helper\nfunction driftScore(loadA, loadB) {\n  const norm = arr => Math.sqrt(arr.reduce((s,v)=>s+v*v,0));\n  const a = loadA.map((v,i)=> v / (norm(loadA) || 1));\n  const b = loadB.map((v,i)=> v / (norm(loadB) || 1));\n  let dot = 0;\n  for (let i=0; i<Math.min(a.length,b.length); i++) dot += a[i]*b[i];\n  return dot;\n}\n```\n\n## Follow-up Questions\n- How would you design a testing strategy to simulate cross-tenant drift and validate alert accuracy?\n- What metrics would you track to balance privacy, latency, and accuracy in this setup?",
        "difficulty": "advanced"
      },
      {
        "id": "pca-q-15",
        "questionId": "q-26933",
        "question": "In a telemetry pipeline for cloud workloads, 30 numeric features per session arrive with occasional missing values. You want to train a PCA-based feature extractor reducing to 4 components for a lightweight on-device clustering. Describe preprocessing (imputation, scaling), global vs per-region PCA, how to justify 4 components under a 3 ms inference budget, and how to translate loadings into cohort definitions with an example top-loading feature and cohort interpretation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an online IncrementalPCA with partial_fit on micro-batches, maintaining a sliding window. Determine n_components by 95% explained variance with a drift detector (ADWIN) to resize. Impute missing w",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Impute missing values with feature-wise median, then scale to unit variance. Use a global projection to keep cohort semantics consistent across regions. Select 4 components by explained variance (aim",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement an online CCIPCA/Oja-based PCA on a rolling window with GPU kernels for vectorized operations. Use online mean imputation with per-feature scaling, updating 12 components every 50 ms to consistently meet the 1…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose computing covariance or SVD via MPC/HE with secret sharing to avoid revealing raw features. Use per-tenant imputation and standardization, then aggregate to 20 components by explained variance",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical PCA design under latency constraints, including imputation, scaling, projection deployment, and translating loadings into actionable cohorts.\n\n## Key Concepts\n\n- PCA in embedded contexts\n- Imputation and scaling\n- Global vs regional projection\n- Interpreting loadings for cohort rules\n\n## Code Example\n\n```javascript\n// Pseudo: build projection matrix W (shape 30x4) from training data\nconst W = loadProjection(); // 30x4\n// inference\nconst xCenter = x.map((v,i)=> (v - med[i])/std[i]);\nconst z = multiply(xCenter, W); // 4-dim\n```\n\n## Follow-up Questions\n\n- How would you handle concept drift in the 4-component space over time?\n- How would you validate that the chosen 4 components remain stable across regions?",
        "difficulty": "beginner"
      },
      {
        "id": "pca-q-16",
        "questionId": "q-4687",
        "question": "In a real-time risk gateway used by a popular trading app, you have 60 numeric features capturing order book stats, price changes, volatility, and volume. Data streams continuously with occasional missing values and outliers. You must deploy an online PCA that updates on a rolling window to feed a lightweight anomaly detector. Describe: online PCA approach, window size, missing data handling, component drift checks, and how to translate top loadings into actionable alerts within low latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Impute missing with feature-wise mean, then standardize to unit variance. Apply PCA globally (same loadings for all users) to 5 components since 5x40 weights fit in 1KB; verify by memory calc: 200 wei",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use IncrementalPCA on a rolling window of 1000 samples, adapts to drift. Impute missing with feature-wise mean; scale to unit variance. Retain enough components to explain about 75% variance (likely 6",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement online incremental PCA using an updating SVD or Oja-style rule over a rolling window (e.g., 5k samples) to produce 6 components. Use windowed mean imputation for missing data and streaming s",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Impute numeric features with mean; scale to unit variance. Apply PCA on the 40 numeric features and keep 4 components to fit a ~50 ms per-event budget. Keep 15 binary flags as-is as 0/1. Interpret loa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical online PCA design, drift handling, and production-ready alerting.\n\n## Key Concepts\n- Incremental PCA (online SVD / Oja)\n- Streaming imputation and scaling\n- Drift detection and dynamic component count\n- Actionable loadings-to-alert mapping with latency bounds\n\n## Code Example\n```javascript\n// Pseudo: incremental PCA step\n```\n\n## Follow-up Questions\n- How would you evaluate drift and compare with batch PCA baselines?\n- What latency tests and monitoring would you implement?",
        "difficulty": "intermediate"
      },
      {
        "id": "pca-q-17",
        "questionId": "q-23522",
        "question": "Design a GPU-accelerated streaming PCA solution for a real-time ad-tech dashboard processing 200 numeric features per event with missing values and nonstationary distributions. Maintain 12 components within a 100 ms latency budget. Compare online mean imputation vs streaming PCA techniques, detail how to update loadings, trigger drift alerts on loadings, and validate with synthetic drift scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant normalization followed by a shared 16 components online PCA, with per-tenant offsets to capture drift. Use differential privacy noise calibrated to a tight epsilon, clipping inputs to bound",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Impute missing values with feature-wise median, then scale to unit variance. Use a global projection to keep cohort semantics consistent across regions. Select 4 components by explained variance (aim",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an online CCIPCA/Oja-based PCA on a rolling window with GPU kernels for vectorized operations. Use online mean imputation with per-feature scaling, updating 12 components every 50 ms to consistently meet the 1…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Incremental PCA with a sparsity constraint to 12 components, plus a periodic rotation (e.g., every 10 minutes) to improve interpretability. Compare IPCA, PPCA, Sparse PCA for streaming; implement",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical streaming PCA design with GPU acceleration, online imputation, drift detection, and strict latency constraints in a production setting.\n\n## Key Concepts\n- **Streaming PCA**: CCIPCA and Oja variants for incremental updates\n- **Online Imputation and Scaling**: Mean imputation and feature-wise normalization computed incrementally\n- **Drift Detection in Loadings**: Monitoring subspace angles to detect distribution shifts\n- **GPU Memory Management and Kernel Design**: Efficient memory allocation and parallel kernel execution\n- **Validation**: Drift injection and backtesting for robustness verification\n\n## Code Example\n```javascript\n// Online CCIPCA update (simplified)\nfunction dot(a, b) {\n  return a.reduce((sum, x, i) => sum + x * b[i], 0);\n}\n\nfunction updatePCA(x, w, eta) {\n  const proj = dot(x, w);\n  for (let i = 0; i < w.length; i++) {\n    w[i] += eta * (x[i] * proj - w[i] * proj * proj);\n  }\n  const norm = Math.sqrt(dot(w, w));\n  for (let i = 0; i < w.length; i++) {\n    w[i] /= norm;\n  }\n  return w;\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "pca-q-18",
        "questionId": "q-28514",
        "question": "In a PayPal-like mobile wallet, 200 features per transaction include numeric and categorical data. Design a PCA-based anomaly detector that runs on-device within a 5 ms latency budget. Address (a) mixed-data handling (PCAmix or encoding), (b) missing value strategies, (c) global vs region-specific PCA, (d) selecting the number of components under latency, (e) translating top loadings into actionable fraud alerts, and (f) a concrete top-loading example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Global DP preprocessing: clip features, impute with in-data mean, and scale using DP-safe statistics. Compute a single global PCA to 6 components. Estimate explained variance under DP noise (bootstrap",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement online PCA (incremental SVD) to update the basis with daily data. For drift, compute per-component explained variance change and loadings cosine similarity; refresh if >0.1 drift on 2+ compo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use mixed-data PCA (PCAmix) or hybrid encoding for numeric+categorical data on-device. Impute missing values (median for numeric, most frequent for categorical), normalize, then compute a global 6-com",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Apply median imputation to handle missing values, use log1p transformation to address positive skew, then standardize features. Compute PCA to capture at least 95% explained variance (typically 5-8 components for this f…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to apply PCA to heterogeneous data under strict on-device latency, ensuring interpretability for fraud alerts.\n\n## Key Concepts\n\n- Mixed-data PCA (PCAmix) or equivalent encoding\n- On-device latency constraints and lightweight inference\n- Imputation strategies for numeric and categorical features\n- Global vs region-specific PCA implications\n- Interpreting loadings to derive actionable fraud signals\n\n## Code Example\n\n```javascript\n// Pseudo-code illustrating a PCAmix-like pipeline\nconst numericCols = [...];\nconst catCols = [...];\nfunction preprocess(x){\n  const n = medianImpute(x[numericCols]);\n  const c = modeImpute(x[catCols]);\n  const nScaled = scale(n);\n  const cEncoded = oneHotEncode(c);\n  return concat(nScaled, cEncoded);\n}\nconst pca = new PCAMix({nComponents:6, global: true});\nconst z = pca.fitTransform(dataset.map(preprocess));\n// Alert rule from top loading feature interpretations\n```\n\n## Follow-up Questions\n\n- How would you validate latency and drift in production?\n- How would you refresh components across app regions without exporting raw data?",
        "difficulty": "advanced"
      },
      {
        "id": "pca-q-19",
        "questionId": "q-21178",
        "question": "Design a streaming PCA to maintain 12 components from a 320-feature telemetry stream with missing values and nonstationarity. Propose an online robust subspace tracker with decay, per-feature masking for missing data, and online imputation. Explain drift detection in loadings and explained variance, and how to translate drift into actionable alerts while meeting a 1-second latency budget?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use incremental PCA with partial_fit on a sliding window, impute numeric gaps streaming-wise and one-hot encode categoricals, then apply PCA or MCA-style mix for interpretability. Keep a mapping from",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Online PCA with sliding window (e.g., 5 min) over 60 features. Use IncrementalPCA or online SVD to update components per window. Missing values: simple mean imputation within window; scale after imput",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a streaming PCA with 320 features and 12 components using online robust subspace tracking (weighted Oja/CCIPCA) and per-feature masking for missing data. Apply exponential decay to updates and",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Maintain a rank-k online PCA; choose k by cumulative explained variance and drift-aware thresholds. Update with a forgetting factor (e.g., 0.95) and online imputation (EM/mean). Detect drift via chang",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests ability to build a scalable, robust streaming PCA with drift detection and alerting in production telemetry.\n\n## Key Concepts\n\n- Online robust PCA\n- Drift detection via subspace angles and eigenvalue trends\n- Missing data handling with masking/imputation\n- Latency and memory management in streaming ML\n- Evaluation with drift-injection scenarios\n\n## Code Example\n\n```javascript\n// Pseudocode: online update sketch\nfunction onlinePCAUpdate(X, mask, W, decay) {\n  // apply masked update, decay previous components, re-orthogonalize\n  // placeholder implementation\n  return W;\n}\n```\n\n## Follow-up Questions\n\n- How would you extend to non-Gaussian noise and different drift types?\n- How would you monitor and bound the drift alert false positive rate?",
        "difficulty": "advanced"
      },
      {
        "id": "pca-q-20",
        "questionId": "q-17323",
        "question": "Design a streaming Tensor PCA pipeline for a 3D data cube (features × sensors × time) with irregular time steps and partial missing data. Maintain 12 components for a real-time dashboard. Pick an online tensor decomposition (online CP-ALS or streaming HOSVD), specify missing-data handling, normalization, and update cadence; detail drift detection in factor matrices and how to translate drift into actionable alerts with latency targets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Clip each feature to [-3,3], impute with tenant median, then perform DP-friendly PCA on per-tenant data with Gaussian noise added to covariance estimates. Pick 5 components to satisfy latency and aim",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Complete Pipeline Design ### (a) Data Preprocessing Normalize each of 70 features using running statistics: - Maintain cumulative mean (μ) and standard deviation (σ) via Welford's algorithm - Transform: z = (x - μ) /…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose streaming tensor PCA using online CP (ALS with forgetting factor) on a 3D data cube. Handle missing data with ALS-based imputation, normalize per slice, and use a sliding window. Keep 12 compo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "## On-Device PCA Pipeline for Real-Time Safety Alerts ### Overview Deploy IncrementalPCA to reduce 25 features to 4 components with <10ms latency per event on Uber's driver app. ### 1. Preprocessing Pipeline ``` Raw Fea…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nExtends PCA to a streaming, multi-dimensional setting, highlighting tensor methods, drift detection, and real-time alerts in production data.\n\n## Key Concepts\n- Streaming tensor PCA (online CP-ALS or streaming HOSVD)\n- Missing-data imputation in tensors\n- Time irregularity handling and windowing\n- Drift detection in factor matrices (subspace angles, KL divergence)\n- Latency budgets and memory usage\n\n## Code Example\n```python\n# Pseudocode: online CP-ALS skeleton\ndef update_cp_online(X_t, A, B, C, forgetting=0.99):\n    # X_t: new frontal slice (features x sensors) at time t\n    # A,B,C: factor matrices for modes 1,2,3\n    # Perform one ALS pass with forgetting, update A,B,C in place\n    # Impute missing entries in X_t during updates\n    pass\n```\n\n## Follow-up Questions\n- How to set drift thresholds robustly across tenants?\n- How would you validate the approach with synthetic drift scenarios?\n- How would privacy constraints (DP or federated) alter the streaming tensor PCA design?",
        "difficulty": "advanced"
      },
      {
        "id": "pca-q-21",
        "questionId": "q-28100",
        "question": "In a ride-hailing/food-delivery platform, 72 numeric trip features arrive in real time with intermittent missingness and heavy-tailed noise. Design a robust probabilistic PCA streaming pipeline to retain 10 components for a 20 ms latency dashboard. Compare PPCA, Robust PCA, and online EM-PCA for missing data and nonstationarity; specify preprocessing, whether to apply PCA per-region or globally, how to justify 10 components under latency, and how to translate top loadings into actionable signals; provide a concrete top-loading feature and its interpretation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Joint Multi-View PCA spanning two views: 60 numeric transaction features and 20 embedding signals per event. Use EM imputation for missing values and robust scaling to address nonstationarity. The method maximi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a lightweight streaming PCA: impute missing values with median (or a separate 'missing' flag), scale with a rolling mean/std, and apply IncrementalPCA chunked over minibatches (e.g., 100 message",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Impute missing per feature with the mean; scale features to unit variance; apply PCA on globally standardized data to 6 components to minimize cross-tenant drift. Select 6 to preserve roughly 85–90% v",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt streaming robust PPCA with online-EM updates; impute missing data via model-based methods and scale with robust statistics; keep 10 components that explain about 90% of variance within the 20 ms",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests advanced PCA approaches for streaming data with missing values, nonstationarity, and strict latency. Requires evaluating PPCA, Robust PCA, and online EM-PCA under real-world constraints, plus practical signal translation from loadings.\n\n## Key Concepts\n\n- Robust Probabilistic PCA vs. Robust PCA vs. online EM-PCA\n- Streaming imputation and robust scaling\n- Latency-budgeted component selection and region/global scope\n- Interpreting loadings as operational signals\n\n## Code Example\n\n```python\n# Pseudocode: streaming_ppca_update\nstate = load_state()  # contains components, mean, etc.\nx = impute(x)           # robust, model-based imputation\nx = scale(x)            # robust scaler\nz = project(x, state.components)\n# E-step / M-step updates on incoming x\nstate = update_ppca(state, x, z)\n```\n\n## Follow-up Questions\n\n- How would you monitor drift in component loadings and decide retraining cadence?\n- What evaluation would you run to validate latency and explainability trade-offs?",
        "difficulty": "advanced"
      },
      {
        "id": "pca-q-22",
        "questionId": "q-7788",
        "question": "In a streaming fraud detection gateway for a ride-sharing platform, 100 numeric features per event include sparse one-hot encodings and missing values. You must reduce to 10 components in real time to feed a lightweight anomaly detector. Describe: (a) sparsity-aware PCA approach and missing-value strategy, (b) how to pick the number of components in a streaming setting with drift, (c) how to map top loadings to deterministic alert rules while keeping latency under 5 ms per event?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use SparsePCA or TruncatedSVD on a 100-feature sparse matrix to 10 components, with column-mean imputation and zeros for missing entries. Select components by explained variance and a streaming elbow.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a streaming PCA service processing 1s windows of 200 features with missing values and drift. Maintain 20 components using online imputation and a robust covariance estimator (pairwise M-estimat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use explained variance, scree, and cross-validated reconstruction error to pick k; for large data, use IncrementalPCA and scale features. Validate by nested CV: train a linear regressor on the k compo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Incremental SVD/CCIPCA with online imputation. Update using only non-zero features to handle sparsity; use a drift detector (Page-Hinkley) to adjust the number of components based on reconstruction er",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical PCA deployment in streaming, handling sparsity, drift, and real-time alerting.\n\n## Key Concepts\n- Sparse/PCA options for sparse data\n- Missing value handling in streaming\n- Component selection under drift\n- Mapping components to actionable alerts\n\n## Code Example\n```python\nfrom sklearn.decomposition import SparsePCA\nimport numpy as np\n# X is a CSR sparse matrix with shape (n_samples, 100)\nX = impute_mean_columns(X)  # implement column-wise mean for missing values\npca = SparsePCA(n_components=10, random_state=42)\nZ = pca.fit_transform(X)\n```\n\n## Follow-up Questions\n- How would you detect drift in component relevance and trigger recalibration?\n- How would you validate that alert rules remain stable across feature reordering or vendor changes?",
        "difficulty": "intermediate"
      },
      {
        "id": "pca-q-23",
        "questionId": "q-15658",
        "question": "Design a beginner PCA-based streaming telemetry pipeline that uses a rolling window of 300 samples to update components every 60 seconds with IncrementalPCA, and specify how to detect concept drift, choose how many components to retain, and translate top loadings into real-time alerts under strict latency constraints for a Netflix/Cloudflare-like platform?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an incremental PCA (Oja-style) with exponential forgetting to maintain 20 components; apply robust centering and scaling via rolling MAD to handle outliers. Detect per-tenant drift with CUSUM on t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use IncrementalPCA on a rolling 10-minute window, median imputation, skew handling (Box-Cox/Yeojohnson as allowed), and scaling with RobustScaler. Choose 8 components to preserve ~85–90% variance with",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use IncrementalPCA with n_components=12 and per-region online preprocessing: median imputation for 70 features, StandardScaler with partial_fit, then IncrementalPCA.partial_fit on 5–10s windows. Apply",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a rolling window of 300 samples and update IncrementalPCA every 60 seconds. Impute using column means and scale to unit variance. Retain enough components to explain at least 90% of variance. Dete",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests online PCA handling, drift detection, and real-time alerting in streaming telemetry. Ties to latency and scalability in edge/cloud contexts like Netflix/Cloudflare.\n\n## Key Concepts\n- IncrementalPCA and rolling windows\n- Streaming imputation and scaling\n- Variance-explained threshold for component retention\n- Concept drift detection via loadings and eigenvalues\n- Translating loadings into actionable alerts with latency constraints\n\n## Code Example\n```python\nfrom sklearn.decomposition import IncrementalPCA\nimport numpy as np\n\n# Pseudo-implementation outline\nwindow = 300  # samples\nipc = IncrementalPCA(n_components=12)\nfor batch in streaming_batches:\n    X = preprocess(batch)  # impute+scale\n    ipc.partial_fit(X)\n    # monitor drift via eigenvalues/loadings\n```\n\n## Follow-up Questions\n- How would you validate drift detection thresholds across tenants?\n- How would you adapt component count if variance explained drops below 85%?\n- How to test latency under peak load?",
        "difficulty": "beginner"
      },
      {
        "id": "pca-q-24",
        "questionId": "q-18774",
        "question": "In a mobile analytics pipeline for a gaming app with 40 numeric features per event and strict on-device privacy, design a DP-aware incremental PCA that runs on-device preserving 12 components and sends only a compact sketch to the cloud. Describe how to allocate the privacy budget across updates, handle missing values, choose and justify the component count, and assess utility under drift?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a DP incremental PCA that maintains online mean and covariance, adding Gaussian noise to the covariance with a fixed epsilon per window; clip features to bound sensitivity; set k=12 based on",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "IncrementalPCA with partial_fit on rolling windows, robustly normalize and impute (median/last). Adapt k to retain 95% explained variance using eigenvalue gaps. Use RPCA when outliers spike. Translate",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement streaming PCA with an incremental method (River/IncrementalPCA) using a sliding window and exponential forgetting to adapt to drift while capping at a small number of components. Use online",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Halko-style incremental SVD to update U, S, V matrices with arriving mini-batches while preserving orthonormality. Apply online imputation for missing values using feature-wise exponential moving averages or lightwe…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design privacy-preserving, streaming PCA that balances utility, latency, and data protection across edge/cloud boundaries.\n\n## Key Concepts\n- Differential privacy in PCA via noisy covariance updates\n- Incremental / streaming PCA under memory and compute constraints\n- Privacy budget allocation over time and per-update sensitivity\n- Missing data handling in online settings\n- Utility evaluation under drift and streaming latency\n\n## Code Example\n```python\n# Pseudocode: DP incremental PCA sketch update\nclass DPIIncrementalPCA:\n    def __init__(self, n_features, n_components, eps):\n        self.n, self.k = n_features, n_components\n        self.eps = eps\n        self.mean = np.zeros(n_features)\n        self.cov = np.zeros((n_features, n_features))\n        self.count = 0\n    def update(self, x):\n        x = np.array(x)\n        x = np.nan_to_num(x, nan=np.nanmean(x))  # simple imputation\n        delta = x - self.mean\n        self.cov += np.outer(delta, delta)\n        self.mean += delta / (self.count + 1)\n        self.count += 1\n        # add DP noise to covariance periodically or per window\n        noisy_cov = self.cov + np.random.normal(scale=self.noise_scale(), size=self.cov.shape)\n        # compute projection basis from noisy_cov (e.g., eigenvectors)\n        w, v = np.linalg.eigh(noisy_cov)\n        idx = np.argsort(w)[-self.k:][::-1]\n        self.components = v[:, idx]\n    def noise_scale(self):\n        # simple DP noise scale as function of eps and n_features\n        return 1.0 / max(self.eps, 1e-6)\n```\n\n## Follow-up Questions\n- How would you extend to federated devices with secure aggregation?\n- How would you detect and adapt to non-stationarity without leaking privacy?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-30T13:58:04.974Z",
    "version": 1
  }
]