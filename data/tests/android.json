[
  {
    "id": "test-android",
    "channelId": "android",
    "channelName": "Android",
    "title": "Android Knowledge Test",
    "description": "Test your understanding of Android with 25 questions.",
    "questions": [
      {
        "id": "android-q-0",
        "questionId": "q-27955",
        "question": "Scenario: Implement an offline-first note-sync module that stores notes in Room, runs a foreground SyncService boundary with WorkManager triggers, uses a CRDT-like conflict resolution for concurrent edits, encrypts notes end-to-end with per-note keys wrapped by the user passphrase in Android Keystore, and tombstones for deletes. Provide data models, IPC surface (AIDL), and a concrete test plan. How would you implement this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a CRDT-like note model in Room (observed-remove with per-note version vectors), encrypt payloads with AES-GCM using a master key wrapped in Android Keystore; store tombstones for deletes. Run a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a real-time Android audio effects app. Run the DSP in an isolated process (android:process + android:isolatedProcess) with IPC via AIDL; target sub-15 ms latency using Ashmem shared buffers",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe the MVVM setup: Note entity (id auto-generated, title, content, timestamp); NoteDao with insert(note) and getAllNotes(): List<Note>; NoteRepository mediates between DAO and ViewModel; NoteVie",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: store labeled samples in Room; train a compact TensorFlow Lite model on-device; deliver incremental delta-weights via backend updates; track modelVersion in DataStore; apply updates with ide",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests offline-first sync, CRDT conflict resolution, Android security with Keystore, and IPC design.\n\n## Key Concepts\n- CRDTs, tombstones, End-to-end encryption, Room, Foreground Service, WorkManager, AIDL.\n\n## Code Example\n```kotlin\n@Entity data class Note(@PrimaryKey val id: String, val contentEnc: ByteArray, val version: Long)\n```\n\n## Follow-up Questions\n- How would you handle key rotation and data re-encryption in-flight?\n- How would you test edge cases with flaky network and concurrent edits?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-1",
        "questionId": "q-26748",
        "question": "Design an on-device ML inference service for Android that runs in an isolated process, loads a quantized TensorFlow Lite model from signed assets, decrypts it with a per-model envelope stored in Android Keystore, and serves requests via a defined AIDL surface. Enforce a strict memory budget, provide a secure model update path, and include a concrete test plan for latency, memory, and security?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a CRDT-based notes sync engine that runs in an isolated Android process. Each note uses a per-note AES-GCM key stored in Keystore; CRDT state lives in Room encrypted at rest. IPC via AIDL expos",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Room to store Message entities with fields id, convoId, senderId, timestamp, encryptedPayload, and a stored encryptionVersion. Generate per-user keys in Android Keystore and derive per-device sess",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an on-device ML inference service for Android that runs in an isolated process, loads a quantized TensorFlow Lite model from signed assets, and decrypts it using a per-model envelope stored in Android Keystore. S…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design an Accessibility Extensions Framework: load extensions as dynamic feature modules; run each in its own isolated process (android:process, android:isolatedProcess=true). Define AIDL IA11yExt wit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question explores secure, scalable on-device ML in multi-process Android environments, with emphasis on process isolation, IPC mechanisms, and memory constraints.\n\n## Key Concepts\n\n- Android isolated processes and Binder IPC via AIDL\n- On-device ML inference with TensorFlow Lite\n- Android Keystore (hardware-backed when available) for per-model envelope encryption\n- Secure model update and remote wipe semantics\n- Memory budgeting and crash resilience strategies\n\n## Code Example\n\n```javascript\n// AIDL interface definition for ML inference\ninterface IMLInference {\n  float[] infer(in byte[] inputTensor);\n  void updateModel(in byte[] signedModel);\n  boolean isModelLoaded();\n}\n\n// Server implementation in isolated process\nclass MLInferenceService extends IMLInference.Stub {\n    private Interpreter tfliteInterpreter;\n    private final MemoryBudget memoryBudget;\n    \n    @Override\n    public float[] infer(byte[] inputTensor) {\n        validateMemoryBudget();\n        return tfliteInterpreter.run(inputTensor);\n    }\n    \n    private void validateMemoryBudget() {\n        Runtime runtime = Runtime.getRuntime();\n        long usedMemory = runtime.totalMemory() - runtime.freeMemory();\n        if (usedMemory > memoryBudget.maxAllowed) {\n            System.gc();\n            throw new OutOfMemoryError(\"Memory budget exceeded\");\n        }\n    }\n}\n```\n\n## Architecture Overview\n\n```\n┌─────────────────┐     AIDL/Binder      ┌────────────────────┐\n│   Client App    │ ───────────────────► │  Isolated Process  │\n└─────────────────┘                      │                    │\n                                         │  ┌──────────────┐  │\n                                         │  │ TFLite        │  │\n                                         │  │ Interpreter   │  │\n                                         │  └──────────────┘  │\n                                         │  ┌──────────────┐  │\n                                         │  │ Envelope Key │  │\n                                         │  │ (Keystore)   │  │\n                                         │  └──────────────┘  │\n                                         └────────────────────┘\n```\n\n## Security Model\n\n1. **Envelope Encryption**: Each model is encrypted with a unique AES-256-GCM key\n2. **Keystore Integration**: Keys stored in Android Keystore with `KEY_ALIAS` per model\n3. **Signed Assets**: Models bundled as APK assets, verified at install time\n4. **Process Isolation**: Runs as isolatedProcess with no UID sharing\n5. **Remote Wipe**: Model deletion triggered via binder call with signature verification\n\n## Memory Budget Enforcement\n\n| Metric | Limit | Action on Breach |\n|--------|-------|------------------|\n| Heap Usage | 80% of available | GC + throttling |\n| Native Allocations | 64MB | OOM crash |\n| Model Size | Configurable | Pre-load validation |\n\n## Secure Update Path\n\n1. Model signed with model owner's signing key\n2. Bundle includes version, checksum, and signature\n3. Service validates signature against trusted certificate in Keystore\n4. Decryption key fetched from envelope after signature verification\n5. Atomic replace with rollback on failure",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-2",
        "questionId": "q-22037",
        "question": "Design a real-time Android audio effects app that processes microphone input through a DSP chain, runs the DSP in an isolated process (android:process + android:isolatedProcess) with IPC via AIDL, and supports hot-swappable DSP modules and encrypted user presets in Keystore. Outline data models, IPC surface, DSP lifecycle, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On startup: Retrofit fetch of /tasks, save results to Room via TaskDao.insertAll. Repository exposes LiveData<List<Task>> by observing the DB and pushing updates after API refresh; NetworkBoundResourc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-profile, offline-first contact cache in an isolated Android process. Keys live in Android Keystore (hardware-backed if available) with rotation every 90 days. Store encrypted data in a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a real-time Android audio effects app. Run the DSP in an isolated process (android:process + android:isolatedProcess) with IPC via AIDL; target sub-15 ms latency using Ashmem shared buffers",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design an offline-first moderation queue: store flags in Room with fields contentId, flaggerDeviceId, timestamp, verdict, and a per-device signing key in Android Keystore (nightly rotation). Submissio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess cross-process design, real-time constraints, and security in Android.\n\n## Key Concepts\n- Real-time audio path, AIDL, android:process, isolatedProcess\n- Ashmem shared buffers for low latency\n- AAudio/Oboe for native path\n- Android Keystore for envelope-preserved presets; hot-swappable DSP modules\n- Lifecycle: Doze, app standby, foreground service\n\n## Code Example\n```java\n// AIDL-like surface (conceptual)\ninterface IDspSurface {\n  void start(int samplerate, int channels);\n  void process(in ByteBuffer input, out ByteBuffer output);\n  void loadModule(String moduleId);\n  void unloadModule(String moduleId);\n}\n```\n\n## Follow-up Questions\n- How would you verify latency and jitter under Doze and BT headphones?\n- What failure modes exist and how would you mitigate them?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-3",
        "questionId": "q-12866",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build a privacy vault module that runs in a separate sandboxed process. It should accept data from multiple in-app modules, encrypt and store it in a single Room database, and expose a bounded AIDL surface for data export and audit logging. Implement per-module data scoping, current-key rotation with Android Keystore, and a battery/network aware background export using WorkManager. Explain data model, IPC design, and testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an on-device AI inference service for Android that loads pluggable backends as isolated processes, communicates via AIDL, and supports versioned models loaded securely from a remote manifest. Enforce per-backend…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a host using SplitInstallManager with a dedicated sandboxed process, pin a public key and verify module signatures before installation, enforce permissions via a PluginSandbox, and store per-plugin dat…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-module data is stored encrypted in a single Room DB; each module has a distinct key from a wrapping key in Android Keystore; keys rotate on schedule; data exported via a bounded AIDL surface; on e",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement an isolated Android service (android:process) that loads a DSP plugin from a signed asset and exposes an AIDL IAudioProcessor interface to stream audio frames to and from the UI. Use Room with Android Keystore…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests modular sandboxing, per-module data governance, IPC, encryption, and background export reliability.\n\n## Key Concepts\n\n- Android sandboxed processes and AIDL IPC\n- Room + per-module encryption keys via Android Keystore\n- Key rotation policies and audit logging\n- WorkManager with constraints for exports\n\n## Code Example\n\n```javascript\n// A rough sketch of AIDL-like interface\ninterface IPrivacyVault {\n  void export(String moduleId, String filter);\n  void rotateKey(String moduleId);\n}\n```\n\n```\n\n## Follow-up Questions\n\n- How would you design per-module key rotation policies and key storage?\n- How would you ensure export integrity across process crashes?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-4",
        "questionId": "q-17402",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: design a privacy-first on-device analytics pipeline that runs in a sandboxed Android service and exposes IPC via a ContentProvider instead of AIDL. It must buffer encrypted events in Room with per-event governance (redaction, retention), batch-upload only on Wi-Fi and charging, and include a user-consent-driven opt-in UI flow. Outline data model, IPC contract, lifecycle, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a Room entity FeedItem, a Flow-based DAO, and a Repository exposing Flow<List<FeedItem>>. A Worker with Constraints(networkType = UNMETERED, requiresCharging = true) fetches JSON, ma",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an isolated Android analytics pipeline that uses a sandboxed service and a ContentProvider for IPC. It should encrypt events with a Keystore-backed key, store in Room with per-event redaction a",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Retrofit to fetch weather, store results in a Room entity WeatherData with a unique id and a lastUpdated timestamp. On success, upsert into the DB; on offline, load from DB. Use WorkManager with a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Kotlin CoroutineWorker scheduled via WorkManager as a PeriodicWorkRequest(24h, 15m). Enforce constraints: network connected, battery not low. Fetch JSON from CDN with HttpClient, parse with kotl",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\n- Tests IPC design choices beyond AIDL, privacy-conscious data governance, and lifecycle in a sandboxed process.\n\nKey Concepts\n- ContentProvider IPC, android:isolatedProcess isolation, Room with encryption, per-event governance, WorkManager batching, consent UX, test strategy.\n\nCode Example\n```kotlin\nclass AnalyticsProvider : ContentProvider() {\n    override fun insert(uri: Uri, values: ContentValues?): Uri? {\n        // Encrypt and persist event in encrypted Room DB\n        return uri\n    }\n}\n```\n\nFollow-up Questions\n- How would you enforce per-event retention across user opt-out and device loss?\n- How would you verify privacy compliance (data deletion, audit trails) in CI/CD?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-5",
        "questionId": "q-22234",
        "question": "Design a secure, multi-process on-device video captioning pipeline for a video-messaging app. The captioner runs in an isolated process (android:process, android:isolatedProcess), accepts audio frames over AIDL from the main app, and streams real-time transcripts back to UI. Use privacy-preserving models with per-user swappable weights, shared memory buffers, and transcripts encrypted at rest. Outline IPC surfaces, sandboxing, memory budget, latency targets, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would create a FeatureFlagEngine hosted in an isolated Android service (android:process/isolatedProcess). Flags live in an encrypted Room table, using Keystore-backed AES keys. Expose getFlag, setOv",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a real-time Android audio effects app. Run the DSP in an isolated process (android:process + android:isolatedProcess) with IPC via AIDL; target sub-15 ms latency using Ashmem shared buffers",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a PluginHost that loads plugins from a signed store, spins a distinct Service per plugin in an isolatedProcess, and communicates via IPluginAidl. Verify signatures with PackageManager, restr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement ICaptions AIDL: startStream(audioConfig), sendFrame(buffer, ts), stopStream, with ICaptionListener for transcripts. Captioner runs in an isolated process; audio buffers shared via HardwareBu",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes cross-process security, real-time media handling, and privacy controls under Android's sandboxing. It requires concrete IPC design, buffer lifecycles, cryptography choices, and test strategy.\n\n## Key Concepts\n- Android IPC via AIDL\n- android:process and android:isolatedProcess\n- Shared memory via HardwareBuffer/ashmem\n- On-device ML privacy and per-user model selection\n- Keystore encryption and key management\n- Real-time latency and buffering strategies\n\n## Code Example\n```kotlin\n// Pseudo-implementation sketch for AIDL-driven caption service\nclass CaptionService : ICaptions.Stub() {\n  override fun startStream(cfg: AudioConfig, callback: ICaptionListener) { /* ... */ }\n  override fun stopStream() { /* ... */ }\n  override fun sendFrame(frame: HardwareBuffer, ts: Long) { /* ... */ }\n}\n```\n\n## Follow-up Questions\n- How would you harden against IPC impersonation or buffer leaks?\n- What’s your strategy for model swapping without user-perceived latency spikes?\n- How would you test for privacy leaks across process boundaries?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-6",
        "questionId": "q-2143",
        "question": "You're building a beginner Android checklist app: store items in Room (id, title, completed, dueDate), display them in a RecyclerView, and support add/edit/delete. Implement a one-time reminder using WorkManager that triggers a Notification at dueDate, with a backoff if scheduling fails or the device is asleep. Describe data flow (DAO/Repository), Worker implementation, and a test plan for offline item creation and overdue reminders?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use EncryptedSharedPreferences with a MasterKey from AndroidX Security. First, create the MasterKey with hardware-backed security: ```kotlin val masterKey = MasterKey.Builder(context) .setKeyScheme(MasterKey.KeyScheme.A…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Notes live in Room with fields id, title, completed, dueDate; a Repository exposes a Flow<List<Note>> consumed by a ViewModel and observed by the UI. ReminderWorker schedules a one-time work with an i",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Enroll a FIDO2 credential via WebAuthn on Android, store credentialId and publicKey in Room, keep the private key in Android Keystore (prefer hardware-backed) and gate use with BiometricPrompt. Login",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-user DashboardManifest entity storing layout as a JSON blob and a DashboardBlock table for blocks; render by mapping blocks to Compose composables. Cache manifest and UI state in Room with A",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests local persistence, MVVM, and background work; verifies handling of Doze and backoff; covers testing offline flows.\n\n## Key Concepts\n- Room: DAO, entities, Flow\n- Repository + ViewModel + RecyclerView\n- WorkManager: OneTimeWorkRequest, initialDelay, backoff\n- Notifications and channels\n- Testing offline and timer-based flows\n\n## Code Example\n```javascript\nclass ReminderWorker(appContext: Context, params: WorkerParameters) : CoroutineWorker(appContext, params) {\n  override suspend fun doWork(): Result {\n    // fetch due item, post notification, return Result.success() or retry()\n    return Result.success()\n  }\n}\n```\n\n## Follow-up Questions\n- How to handle time zones and daylight saving changes?\n- How to update or cancel a reminder when dueDate changes?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-7",
        "questionId": "q-24899",
        "question": "Context: android/general; Difficulty: beginner; Topics: android. Scenario: Create a small Android screen that shows a list of tasks fetched from a public REST API. Implement an offline-first experience by caching results in Room. Build a simple repository returning LiveData<List<Task>> and wire it to a ViewModel, then display in a RecyclerView. Provide a minimal Task data model, Retrofit API interface, Room DAO, and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On startup: Retrofit fetch of /tasks, save results to Room via TaskDao.insertAll. Repository exposes LiveData<List<Task>> by observing the DB and pushing updates after API refresh; NetworkBoundResourc",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design an on-device AI inference service for Android that loads pluggable backends as isolated processes, communicates via AIDL, and supports versioned models loaded securely from a remote manifest. Enforce per-backend…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a data model with InventoryItem { id, name, quantity, location, notes, lastModified, revision }. Use Room with per-record AES-GCM keys stored in Android Keystore; encrypt fields or the whole r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a bound AIDL service exposing getTile(x,y,z) that loads encrypted Vector Tiles from a Room store; use Android Keystore to wrap per-user AES keys; implement LRU eviction with a configurable m",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical Android data layer skills: Room, Retrofit, LiveData, ViewModel, and offline-first patterns.\n\n## Key Concepts\n- Room entities/DAO; Retrofit API; Repository pattern; LiveData/ViewModel wiring; offline caching strategy.\n\n## Code Example\n```javascript\nclass Task { constructor(id, title, done){ this.id = id; this.title = title; this.done = done; } }\n```\n\n## Follow-up Questions\n- How would you handle API errors and user-initiated refresh?\n- How would you adapt for paging or large datasets?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-8",
        "questionId": "q-24122",
        "question": "Context: android/general; Difficulty: beginner; Topics: android. Scenario: Build a minimal feature that saves user display preferences (theme: dark/light; fontSize) using Jetpack Security EncryptedSharedPreferences. Provide code to initialize, write/read, and migrate from plaintext to encrypted on first run, plus a basic test plan. How would you structure this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use EncryptedSharedPreferences to persist theme (dark/light) and fontSize. Create a companion crypto config with MasterKey from Jetpack Security. Implement a simple SettingsRepository that reads/write",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use EncryptedSharedPreferences with a MasterKey from AndroidX Security. First, create the MasterKey with hardware-backed security: ```kotlin val masterKey = MasterKey.Builder(context) .setKeyScheme(MasterKey.KeyScheme.A…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an Accessibility Extensions Framework: load extensions as dynamic feature modules; run each in its own isolated process (android:process, android:isolatedProcess=true). Define AIDL IA11yExt wit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a Jetpack Compose screen with two TextFields (name, email). Validate email with a simple regex and display inline error messages. Persist draft values in DataStore (Preferences) so the fields p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to apply Android Security Library and local persistence basics in a safe, beginner-friendly way.\n\n## Key Concepts\n- Jetpack Security: EncryptedSharedPreferences, MasterKey\n- Data migration from plaintext to encrypted stores\n- Repository pattern and simple unit tests\n\n## Code Example\n```kotlin\nimport android.content.Context\nimport androidx.security.crypto.EncryptedSharedPreferences\nimport androidx.security.crypto.MasterKey\n\nfun createEncryptedPrefs(context: Context): SharedPreferences {\n  val masterKey = MasterKey.Builder(context)\n    .setKeyScheme(MasterKey.KeyScheme.AES256_GCM)\n    .build()\n  return EncryptedSharedPreferences.create(\n    context,\n    SETTINGS_PREFS_NAME, // defined elsewhere\n    masterKey,\n    EncryptedSharedPreferences.PrefKeyEncryptionScheme.AES256_SIV,\n    EncryptedSharedPreferences.PrefValueEncryptionScheme.AES256_GCM\n  )\n}\n```\n\n## Follow-up Questions\n- How would you test migration with existing plaintext values?\n- How would you handle cases where the device lacks hardware-backed keystore?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-9",
        "questionId": "q-2824",
        "question": "Design an Android module for offline-first, real-time co-op game state using CRDTs to merge concurrent updates; use Room for local persistence, WebSocket for server sync, and per-user encryption keys stored in Android Keystore with nightly ephemeral key rotation. Describe architecture, data model, CRDT design, sync protocol, and a test plan for latency, churn, and conflict resolution?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a CRDT-based approach (state-based OR-Set + MVRegister) to model per-entity state. Each device signs ops with a per-user key in Keystore; store encrypted deltas locally in Room; sync via WebSo",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a cross-process camera filter pipeline using isolated Android processes (android:process=\"isolated\") loaded from signed APK modules. IPC is handled via AIDL with fixed-size frame buffers to ensure predictable mem…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define Note as a Room @Entity with id (auto), title, content, timestamp. Implement NoteDao with insert, update, delete and a search query by title or content. Create a NoteRepository and a NoteViewMod",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Store a boolean key 'dark_mode' in DataStore<Preferences>, expose a Flow<Boolean> in a ThemeViewModel, and toggle it via a UI switch. In Compose, drive MaterialTheme from that Flow. On startup, collec",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of offline-first CRDT design, encryption, key rotation, and practical trade-offs in a mobile environment.\n\n## Key Concepts\n\n- CRDT types (OR-Set, MVRegister)\n- End-to-end encryption with Android Keystore\n- Room-based offline persistence\n- WebSocket-based sync with anti-entropy\n- Key rotation strategy and impact on data at rest\n\n## Code Example\n\n```kotlin\ndata class MVRegister<T>(val value: T, val ts: Long)\n\nfun <T> merge(a: MVRegister<T>, b: MVRegister<T>): MVRegister<T> = if (a.ts > b.ts) a else b\n```\n\n## Follow-up Questions\n\n- How would you handle tombstones to bound memory?\n- How would you verify multi-device convergence under churn?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-10",
        "questionId": "q-16068",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: Build a privacy-preserving offline translation system using a central BridgeService that communicates with multiple isolated plugin processes (android:process + android:isolatedProcess). Each plugin provides a translation engine, sharing dictionaries stored in an encrypted Room DB. Plugins fetch updates on Wi‑Fi + charging via WorkManager, and enforce per-plugin rate limits. Outline data model, IPC surface, plugin lifecycle, and a concrete test plan (security, performance, and privacy)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a module that stores telemetry in Room, maintains a local asset map, and applies server delta patches offline via WorkManager. Patches are signed with per-device keys from Android Keystore (",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a BridgeService interface (AIDL) for per-plugin requests; each plugin runs in its own isolatedProcess with a unique UID; dictionaries stored in an encrypted Room (SQLCipher); per-plugin keys st",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Create a Jetpack Compose screen with two TextFields (name, email). Validate email with a simple regex and display inline error messages. Persist draft values in DataStore (Preferences) so the fields p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bind to a sandboxed inference service (android:process/isolate) via AIDL; load a quantized TensorFlow Lite model from Remote Config, decrypt in Android Keystore, and mmap for fast inferences. Transmit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a multi-process plugin system with strict isolation, privacy, and offline-first behavior. Requires integrating dynamic feature modules, IPC, encrypted storage, and update governance in a realistic translation use case.\n\n## Key Concepts\n\n- Android multi-process IPC with AIDL\n- android:process and android:isolatedProcess isolation\n- SQLCipher-encrypted Room database for dictionaries\n- Android Keystore per-plugin keys and rotation\n- WorkManager for Wi‑Fi/charging gated updates\n- Per-plugin rate limiting and auditing\n\n## Code Example\n\n```java\n// AIDL sketch for plugin bridge\ninterface IPluginBridge {\n  void translate(String pluginId, String text, ResultCb cb);\n}\n```\n\n## Follow-up Questions\n\n- How would you test plugin isolation under misbehaving plugins? \n- How would you handle key rotation without service downtime?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-11",
        "questionId": "q-21341",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build a privacy-preserving, offline-first background uploader for user media clips with per-clip encryption, chunked resumable uploads, and Doze-aware execution. Describe the data model, encryption flow (per-clip keys, envelope), chunk protocol, and a robust retry strategy using WorkManager for background work?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a data model where each media clip is represented as a ClipRecord containing the encrypted blob, nonce, chunk manifest, and upload state. Encrypt each clip with AES-256-GCM using a per-clip key derived via HKDF f…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a bounded circular frame buffer, encode frames with MediaCodec to H.264, stream data via UDP with FEC and a TCP channel for control, and use a ForegroundService with WakeLock to persist in D",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a Room model with Article(id, title, contentUrl, manifestVersion) and a UserEdit(entity for highlights/notes) encrypted via Android Keystore. Fetch manifests via GraphQL, render content with J",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Retrofit to fetch headlines from a public REST API, map to a NewsItem(id, title, summary, timestamp, source). Cache in Room with NewsDao (insertAll, getAll). UI built with Jetpack Compose LazyColu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess the ability to design a secure, battery-efficient media upload system in Android, covering envelope encryption, lifecycle handling, and resilient background work.\n\n## Key Concepts\n\n- Android WorkManager with constraints (network, charging, Doze)\n- AES-256-GCM encryption with per-clip keys\n- HKDF key derivation and Android Keystore integration\n- Chunked resumable upload protocol with idempotent retries\n- Data modeling for ClipRecord and state machine",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-12",
        "questionId": "q-26646",
        "question": "Design an isolated, cross-process note-sync feature for Android: UI communicates with a dedicated isolated service via AIDL to store user notes in an encrypted Room database, keys wrapped in Android Keystore, with per-note encryption and a simple forward-secret sync protocol over TLS. Include data model, IPC surface, lifecycle, and a robust test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage an isolated service (android:isolatedProcess) that exposes AIDL for CRUD on a Room-backed, SQLCipher-encrypted notes DB. Store per-note keys wrapped by Android Keystore and derive data keys w",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a bounded circular frame buffer, encode frames with MediaCodec to H.264, stream data via UDP with FEC and a TCP channel for control, and use a ForegroundService with WakeLock to persist in D",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a foreground WebSocket service using OkHttp to receive live prices, store last 100 prices in an encrypted Room database (SQLCipher) keyed from Android Keystore, expose data via Kotlin Flow f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement offline-first messaging using Room encrypted with SQLCipher; store per-user master key in Android Keystore and rotate by re-wrapping the DB key and re-encrypting affected rows. Build a WorkM",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design IPC across an isolated Android service, encryption, and robust testing.\n\n## Key Concepts\n- Android isolated processes\n- AIDL IPC\n- Android Keystore and per-note keys\n- Room encryption and data migrations\n- Test plan including crash recovery and offline scenarios\n\n## Code Example\n\n```javascript\n// Pseudo-code illustrating AIDL surface and data model\n```\n\n## Follow-up Questions\n\n- How would you implement per-note key rotation and key-wrapping in Keystore?\n- How do you handle offline conflict resolution and sync retry strategies?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-13",
        "questionId": "q-8824",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: You must design a battery-conscious, offline-first Android app that loads on-demand UI and logic via dynamic feature modules (SplitInstallManager). Describe how you'd implement a secure, versioned plugin API, module isolation, on-device feature presence testing, per-user opt-out, offline manifest caching, and a rollback path. Include testing plan and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use SplitInstallManager to load dynamic features on demand. Create a stable PluginApi interface in the base module with @Version annotations. Verify module integrity using Play Signing keys and validate SHA256 hashes be…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use dynamic feature modules via App Bundles (SplitInstallManager) with per-module isolated processes and a shared core API surface. Data model: ModuleMeta (name, version, status, lastUpdate, deps); Mo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Envelope encryption with per-device KEK; notes in Room encrypted via a DEK wrapped by KEK. Sync uses a CRDT-like delta graph over a GraphQL manifest; deletions are tombstones. A KEK rotation re-wraps",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement contact IDs as salted SHA-256 hashes; server patches use op: add/remove with id, group_id, ts signed by per-device keys from Keystore; apply patches idempotently in Room with rollback suppor",
            "isCorrect": false
          }
        ],
        "explanation": "## Implementation Details\n\n### 1. Secure Versioned Plugin API\n\n```kotlin\n// Base module: Stable interface all plugins must implement\n@Retention(AnnotationRetention.RUNTIME)\n@Target(AnnotationTarget.CLASS, AnnotationTarget.FUNCTION)\nannotation class PluginVersion(val major: Int, val minor: Int, val patch: Int)\n\ninterface PluginApi {\n    fun initialize(context: Context)\n    fun getFeatures(): List<PluginFeature>\n    fun onDestroy()\n}\n\n// Version compatibility checker\nobject PluginVersionChecker {\n    fun isCompatible(requested: PluginVersion, available: PluginVersion): Boolean {\n        return requested.major == available.major && \n               requested.minor <= available.minor\n    }\n}\n\n// ProGuard rules to preserve interfaces:\n// -keep class com.app.base.plugin.** { *; }\n// -keepclassmembers class * implements com.app.base.plugin.PluginApi { *; }\n```\n\n### 2. Module Isolation with Custom ClassLoaders\n\n```kotlin\nclass IsolatedModuleLoader(\n    private val context: Context,\n    private val moduleName: String\n) {\n    private var classLoader: ClassLoader? = null\n    \n    fun loadModule(modulePath: String): PluginApi? {\n        val apkFile = File(context.filesDir, \"modules/$modulePath.apk\")\n        \n        // Verify SHA256 hash before loading\n        if (!verifyModuleIntegrity(apkFile)) {\n            return null\n        }\n        \n        classLoader = CustomClassLoader(\n            apkFile.absolutePath,\n            context.classLoader.parent,\n            moduleName\n        )\n        \n        val clazz = classLoader!!.loadClass(\"com.plugin.FeatureModule\")\n        return clazz.newInstance() as PluginApi\n    }\n    \n    private fun verifyModuleIntegrity(apkFile: File): Boolean {\n        val digest = MessageDigest.getInstance(\"SHA-256\")\n        val hash = digest.digest(apkFile.readBytes())\n            .joinToString(\"\") { \"%02x\".format(it) }\n        return hash == getExpectedHash(moduleName)\n    }\n    \n    fun unload() {\n        classLoader = null\n        System.gc()\n    }\n}\n```\n\n### 3. Feature Presence Testing\n\n```kotlin\nclass FeaturePresenceChecker(\n    private val splitInstallManager: SplitInstallManager\n) {\n    fun isFeatureInstalled(featureId: String): Boolean {\n        val installedModules = splitInstallManager.installedModules\n        return installedModules.contains(featureId)\n    }\n    \n    fun getInstalledFeatures(): Set<String> {\n        return splitInstallManager.installedModules.toSet()\n    }\n    \n    // Also check via PackageManager for split APKs\n    fun getSplitApkInfo(packageName: String): List<PackageInfo> {\n        return context.packageManager\n            .getPackageInfo(packageName, PackageManager.GET_SPLITS)\n            .splitNames\n    }\n}\n```\n\n### 4. Per-User Opt-Out with Offline Support\n\n```kotlin\n@Entity(tableName = \"user_preferences\")\ndata class UserPreference(\n    @PrimaryKey val odId: String,\n    val featureId: String,\n    val optOut: Boolean,\n    val syncStatus: SyncStatus = SyncStatus.PENDING,\n    val updatedAt: Long = System.currentTimeMillis()\n)\n\nclass UserPreferenceRepository(\n    private val dao: UserPreferenceDao\n) {\n    suspend fun setOptOut(userId: String, featureId: String, optOut: Boolean) {\n        dao.upsert(UserPreference(\n            odId = userId,\n            featureId = featureId,\n            optOut = optOut,\n            syncStatus = SyncStatus.PENDING\n        ))\n    }\n    \n    suspend fun isOptedOut(userId: String, featureId: String): Boolean {\n        return dao.getPreference(userId, featureId)?.optOut ?: false\n    }\n    \n    suspend fun syncPendingChanges() {\n        val pending = dao.getPendingSync()\n        pending.forEach { pref ->\n            try {\n                syncToServer(pref)\n                dao.updateSyncStatus(pref.odId, SyncStatus.SYNCED)\n            } catch (e: Exception) {\n                // Keep as PENDING for retry\n            }\n        }\n    }\n}\n```\n\n### 5. Offline Manifest Caching\n\n```kotlin\n@Entity(tableName = \"feature_manifest\")\ndata class CachedManifest(\n    @PrimaryKey val featureId: String,\n    val version: String,\n    val downloadUrl: String,\n    val sha256Hash: String,\n    val sizeBytes: Long,\n    val dependencies: String, // JSON array\n    val cachedAt: Long,\n    val expiresAt: Long\n)\n\nclass ManifestCacheManager(\n    private val dao: ManifestDao,\n    private val networkApi: FeatureManifestApi\n) {\n    suspend fun getManifest(featureId: String): CachedManifest? {\n        val cached = dao.getManifest(featureId)\n        if (cached != null && !isExpired(cached)) {\n            return cached\n        }\n        \n        // Try network fetch if online\n        return try {\n            val remote = networkApi.fetchManifest(featureId)\n            dao.insert(remote)\n            remote\n        } catch (e: Exception) {\n            // Return stale cache as fallback\n            cached?.takeIf { isStaleButUsable(it) }\n        }\n    }\n    \n    private fun isExpired(manifest: CachedManifest): Boolean {\n        return System.currentTimeMillis() > manifest.expiresAt\n    }\n}\n```\n\n### 6. Dynamic Module Installation with Rollback\n\n```kotlin\nclass DynamicModuleInstaller(\n    private val splitInstallManager: SplitInstallManager\n) {\n    private val moduleVersionHistory = mutableMapOf<String, String>()\n    \n    suspend fun installModule(\n        featureId: String,\n        version: String,\n        onProgress: (Int) -> Unit\n    ): Result<Unit> {\n        val sessionParams = SplitInstallSessionParams.Builder()\n            .setModuleNames(listOf(featureId))\n            .setInstallStrategy(InstallStrategy.IDLE)\n            // Defer until charging + WiFi for battery-conscious behavior\n            .setRequiresCharging(true)\n            .setRequiresWifi(true)\n            .build()\n        \n        val request = SplitInstallRequest.Builder()\n            .addModule(featureId)\n            .setSessionParams(sessionParams)\n            .build()\n        \n        return splitInstallManager.startInstallation(request)\n            .addOnSuccessListener { sessionId -> \n                moduleVersionHistory[featureId] = version\n                saveVersionHistory()\n            }\n            .addOnFailureListener { e -> \n                // Rollback handled by Play Store automatically\n                // Additional custom rollback:\n                performRollback(featureId)\n            }\n    }\n    \n    suspend fun performRollback(featureId: String) {\n        // Record rollback in history\n        moduleVersionHistory.remove(featureId)\n        saveVersionHistory()\n        \n        // Uninstall problematic module\n        splitInstallManager.deferredUninstall(listOf(featureId))\n        \n        // Load fallback UI from base module\n        loadFallbackFeature(featureId)\n    }\n    \n    fun loadFallbackFeature(featureId: String): Fragment {\n        return when (featureId) {\n            \"advanced_analytics\" -> AnalyticsFallbackFragment()\n            \"video_processing\" -> VideoFallbackFragment()\n            else -> DefaultFallbackFragment()\n        }\n    }\n}\n```\n\n### 7. Testing Plan\n\n| Test Type | Approach |\n|-----------|----------|\n| Unit Tests | Test version compatibility logic, opt-out persistence, manifest caching |\n| Integration Tests | Verify ClassLoader isolation, module loading/unloading |\n| Instrumented Tests | Test SplitInstallManager flow, feature presence detection |\n| Monkey Tests | Validate app stability with dynamic modules loaded/unloaded |\n| Offline Tests | Verify manifest cache, opt-out sync queue works without network |\n| Rollback Tests | Simulate failed installs, verify fallback UI renders correctly |\n\n### 8. Trade-offs\n\n| Aspect | Trade-off |\n|--------|-----------|\n| ClassLoader Isolation | Extra memory overhead per module, more complex debugging |\n| Offline-First | Stale manifest risk, more local storage used |\n| Battery-Conscious | Slower feature availability (waits for WiFi/charging) |\n| Rollback | User may experience brief unavailability during rollback |\n| Version Compatibility | Maintaining backward compatibility limits refactoring |\n\nThe base module should always contain minimal fallback UI/logic for critical features to ensure the app remains functional during module issues.",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-14",
        "questionId": "q-20928",
        "question": "Design an on-device AI inference service for Android that loads pluggable backends as isolated processes, communicates via AIDL, and supports versioned models loaded securely from a remote manifest. Enforce per-backend permissions, model signing with Keystore, and a rollbackable hot-swap strategy. Describe IPC interfaces, data models, update flow, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an on-device AI inference service for Android that loads pluggable backends as isolated processes, communicates via AIDL, and supports versioned models loaded securely from a remote manifest. Enforce per-backend…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect a cross-process analytics service isolated via android:isolatedProcess and bounded AIDL surface. Use Room with SQLCipher for encrypted buffering; generate per-event keys in Android Keystore",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a local Room schema for bursts and CRDT metadata, encrypt payloads with a device-bound KeyStore key, and batch uploads via GraphQL mutations. Schedule constrained uploads with WorkManager an",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant encrypted Room databases (tenant_<id>.db) protected by SQLCipher. Store encryption keys in Android Keystore, wrapped per-tenant for isolation. Use Room with a SupportFactory to supply the cipher and…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests multi-process Android architecture, secure model loading, and runtime plugin management. It evaluates understanding of IPC design, security boundaries, and operational reliability under offline and online update scenarios.\n\n## Key Concepts\n\n- **AIDL-based IPC** across isolated backend processes\n- **Versioned, signed models** loaded from private storage via a remote manifest\n- **Per-backend permissions** and resource budgeting\n- **Hot-swap with rollback** paths and audit trails\n- **Testing** for offline updates, integrity verification, and IPC attack surfaces\n\n## Solution Overview\n\nCreate a host application that manages inference requests and delegates to isolated backend services via AIDL. Each backend runs in a sandboxed process with limited privileges. Models are signed using a private key stored in Keystore, with verification against a remote manifest containing version, hash, and metadata. The system supports atomic deployment with automatic rollback on failure, maintaining full audit logs for compliance.",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-15",
        "questionId": "q-4945",
        "question": "Design an Android module for a mobile IDE that enables real-time collaborative coding across devices using a CRDT-based code tree to merge edits, with offline edits stored in Room, per-device encryption via Android Keystore, and server reconciliation over gRPC to a central workspace; include data model, conflict rules, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Store POS events in Room with a CRDT-like delta graph to merge offline edits; encrypt payloads with per-device keys stored in Android Keystore and rotate keys on app install/update. Reconcile by pushi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a privacy-first Android module for group audio chat (3+ participants) with E2EE: per-device ephemeral keys rotated nightly in Android Keystore, a group-key envelope, and WebRTC for low-latency",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a privacy-preserving analytics module using Room to buffer events locally, with each event payload encrypted using AES-256-GCM via Android Keystore (KeyGenParameterSpec, wrapped keys). Use W",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I expect a CRDT-based code tree (AST-like) with per-node IDs and op encodings for insert/delete/move, offline edits persisted in Room, payload encryption via Android Keystore, and gRPC-based reconcili",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a scalable, offline-first code collaboration system on Android, combining CRDTs with on-device security and remote reconciliation.\n\n## Key Concepts\n- CRDT-based code tree for merging edits across devices\n- Operation encoding: insert/delete/move with unique IDs\n- Offline persistence: Room as the local log\n- Security: per-device keys in Android Keystore, encryption in transit and at rest\n- Sync: gRPC to central workspace with version vectors for conflict resolution\n- Test strategy: concurrent edits, latency under churn, large files, and feature correctness (code completion)\n\n## Code Example\n```kotlin\ndata class CodeNode(val id: String, val kind: String, val text: String, val children: List<String>)\ndata class Op(val type: String, val nodeId: String, val payload: Map<String, Any>)\n```\n\n## Follow-up Questions\n- How to handle large ASTs with memory constraints?\n- How would you extend to multiple languages with language-server protocols?\n- How to detect and recover from divergent forks in offline mode?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-16",
        "questionId": "q-6860",
        "question": "Context: android/general; Difficulty: beginner; Topics: android. Scenario: Build a minimal Android Kotlin module named WeatherSnapshot that fetches daily forecasts from a REST API at 6:00 each morning, caches results in Room with per-device encryption via Android Keystore, and displays a simple RecyclerView with swipe-to-refresh. Ensure offline fallback and a WorkManager constraint to run only on unmetered networks and when charging. Include data model Forecast(date: String, high: Int, low: Int, condition: String), DAO, repository with encryption, and a concise test plan. How would you implement it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create an Event entity with id, timestamp, type, and payload; a Room DAO selecting last 50 by timestamp; initialize a SQLCipher-encrypted RoomDatabase with a keystore-wrapped master key; expose a simp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To implement, use FusedLocationProvider with a foreground service for geofence checks to minimize GPS use; store Geofence and LocationEvent in Room encrypted via Android Keystore; batch uploads with W",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kotlin coroutines with a ViewModel. Wire the editor to a MutableStateFlow<String>, feed input via a TextWatcher, and collect text with debounce(500) in viewModelScope to call a small Repository.sa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline WeatherSnapshot: Retrofit fetch of daily forecast (date, high, low, condition). Room entity Forecast(id, date, high, low, condition). DAO: getAll, insertAll. Encrypt cached payload with Androi",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nNew angle: practical offline-first data with encryption and time-based background sync using WorkManager.\n\n## Key Concepts\n- WorkManager constraints and initialDelay for 6am alignment\n- Room entities/DAOs for simple Forecast data\n- Android Keystore-based encryption for cached data\n- Retrofit data flow and repository pattern\n\n## Code Example\n```kotlin\n@Entity\ndata class Forecast(\n  @PrimaryKey val id: Int,\n  val date: String,\n  val high: Int,\n  val low: Int,\n  val condition: String\n)\n\n@Dao\ninterface ForecastDao {\n  @Query(\"SELECT * FROM Forecast\")\n  fun getAll(): List<Forecast>\n  @Insert(onConflict = OnConflictStrategy.REPLACE)\n  fun insertAll(vararg items: Forecast)\n}\n```\n\n## Follow-up Questions\n- How would you test 6am alignment across time zones?\n- How would you handle API failures and data corruption in the local cache?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-17",
        "questionId": "q-29157",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: Implement a privacy-preserving cross-process camera filter pipeline. Filter plugins run in isolated Android processes loaded from signed modules; frames pass via a defined AIDL interface, returning processed frames. Encrypt in-memory buffers, avoid disk IO, and enforce per-user keys in Android Keystore. Provide IPC surface, plugin lifecycle, resource budgets, and a test plan covering latency, memory, and security?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-process camera filter pipeline using isolated Android processes (android:process=\"isolated\") loaded from signed APK modules. IPC is handled via AIDL with fixed-size frame buffers to ensure predictable mem…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Adopt a two-tier key scheme: each file encrypted with AES-256-GCM, its key wrapped by a per-album key stored in Android Keystore. Vault runs in an isolated process; decryption via a strict AIDL surfac",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an on-device privacy-preserving video analytics pipeline: CameraX captures frames; frames are transferred to an isolated process via AIDL for real-time analysis with a pluggable module; use Ash",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Room with SQLCipher (or AndroidX Security Crypto library) to encrypt the notes table at rest. Generate a master key in Android Keystore and wrap per-note data encryption keys using envelope encryption. Expose a read…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nTests the ability to architect cross-process plugins with strong privacy guarantees and reason about real-time constraints on Android.\n\n## Key Concepts\n\n- Android isolated processes and AIDL for secure IPC\n- In-memory cryptography with Android Keystore integration\n- Streaming frame data with backpressure-controlled ring buffers\n- Plugin lifecycle management, module signing, and dynamic loading\n- Performance benchmarking and security validation strategies\n\n## Code Example\n\n```java\n// AIDL interface for frame data exchange\ninterface IFrameProcessor {\n    void processFrame(in byte[] frameData, int frameId);\n    ProcessedFrameResult getProcessedFrame(int frameId);\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-18",
        "questionId": "q-13343",
        "question": "Context: android/general; Difficulty: beginner; Topics: android. Scenario: Build a simple Android app with one Activity that uses Room to store a to-do list. Pre-populate the DB with 3 tasks, display them in a RecyclerView, allow adding and deleting tasks, and persist state across rotation via ViewModel/LiveData. How would you structure the DAO, Entity, Repository, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an ExoPlayer-based playlist service with Jetpack Compose UI. Cache metadata in Room encrypted with SQLCipher using a KeyStore-derived key; prefetch upcoming items via WorkManager under netwo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement Room with a Task @Entity(id, text, done). Create TaskDao with insert, delete, and getAll(): LiveData<List<Task>>. Build a Repository that exposes getAllTasks(): LiveData<List<Task>> and addT",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a data model with InventoryItem { id, name, quantity, location, notes, lastModified, revision }. Use Room with per-record AES-GCM keys stored in Android Keystore; encrypt fields or the whole r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a comprehensive SecretsManager for Android with envelope encryption architecture. The API surface includes: getSecret(key) retrieves decrypted secrets, rotateSecret(key) generates new per-secret keys with forw…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests basic Android data persistence and UI wiring using familiar components.\n\n## Key Concepts\n- Room: Entity, DAO, database\n- LiveData and ViewModel\n- Repository pattern\n- Prepopulation via RoomDatabase.Callback\n- Rotation handling with LiveData\n\n## Code Example\n```kotlin\n@Entity\ndata class Task(\n  @PrimaryKey(autoGenerate = true) val id: Long = 0,\n  val text: String,\n  val done: Boolean = false\n)\n@Dao\ninterface TaskDao {\n  @Query(\"SELECT * FROM Task\") fun getAll(): LiveData<List<Task>>\n  @Insert fun insert(task: Task): Long\n  @Delete fun delete(task: Task)\n}\n@Database(entities = [Task::class], version = 1)\nabstract class AppDb : RoomDatabase() {\n  abstract fun taskDao(): TaskDao\n}\n```\n\n## Follow-up Questions\n- How would you test prepopulation and DAO methods?\n- How would you migration plan for Task schema changes?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-19",
        "questionId": "q-27848",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: Build a privacy-preserving on-device feature-flag evaluator that runs in an isolated Android process. It should load a signed DSL interpreter from assets, validate rules via Android Keystore, and expose an AIDL API evaluate(flag, context). Each decision must be auditable with a Merkle-hashed log stored in Room and batch-uploaded to backend only with explicit user consent. Provide IPC surface, lifecycle, security considerations, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an offline-first messaging module using Room encrypted with SQLCipher, with Conversation and Message entities and Keystore-backed keys. Use WorkManager for periodic sync with network constra",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Insert a pending message into Room with a client-generated id, then enqueue a OneTimeWorkRequest with network constraint and exponential backoff. In the Worker, fetch unsent messages, POST to the serv",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a RecyclerView with a custom adapter extending RecyclerView.Adapter. Use ViewHolder pattern with data binding for name/email TextViews. Implement onCreateViewHolder to inflate item layout, onBi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an on-device feature-flag evaluator running in an isolated Android process. Load a signed DSL interpreter from assets and verify its signature using Android Keystore. Expose the `evaluate(flag, context)` method v…",
            "isCorrect": true
          }
        ],
        "explanation": "**Why This Is Asked:** Tests understanding of process isolation, secure dynamic rule loading, and auditable decision-making in a privacy-focused feature-flag system. Probes IPC design patterns, Keystore integration, and robust testing strategies.\n\n**Key Concepts:**\n- Android `isolatedProcess` with cross-process IPC via AIDL\n- Signature verification of dynamic rules using Android Keystore\n- On-device DSL interpreter with deterministic evaluation\n- Merkle-audited logs stored in Room with consent-driven uploads\n\n**Security Considerations:** Keystore-backed signing keys prevent interpreter tampering; isolated process limits blast radius; Room audit logs provide non-repudiation; consent gating ensures GDPR/privacy compliance.\n\n**Test Plan:**\n1. Unit: DSL rule parsing, Merkle hash chain integrity, Keystore signature validation\n2. Integration: AIDL IPC binding lifecycle (bind/unbind), isolated process crash recovery\n3. Security: Fuzz test malformed DSL; verify logs immutable post-insertion\n4. Consent: Confirm upload only after explicit opt-in; test revocation behavior",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-20",
        "questionId": "q-2547",
        "question": "Design an Android module that performs on-device NLP inference using a Hugging Face model for chat intent detection. Build a ModelManager with components: ModelRepo (versioned storage), InferenceEngine (off-UI thread), MemoryMonitor, and Updater (WorkManager with backoff). It should load from internal storage, download updates when online, switch between a large and compact model based on memory, support cancellation with timeouts, and rollback on failure. Describe architecture, data flow, and a concrete test plan for offline updates, memory pressure, and model-load failure?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a plugin framework where extensions are discovered via PackageManager, verified against host signing keys, started in isolated processes, and communicate through a limited AIDL API (initialize,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use ViewModel with LiveData to preserve data across configuration changes. Save critical state in onSaveInstanceState() for process death scenarios. For simple UI state, use onSaveInstanceState() with Bundle as a lightw…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "ModelManager orchestrates a comprehensive on-device NLP inference system with versioned model management. The ModelRepo component provides versioned storage in internal app storage, while the MemoryMonitor continuously…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a dynamic Android plugin framework where plugins are distributed as signed AARs, verified using Android Keystore, loaded into an isolated process via DexClassLoader, and exposed to the host app via AIDL. Support…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests advanced on-device ML lifecycle management including versioned model caching, memory-adaptive loading strategies, background update mechanisms, and robust rollback protocols.\n\n## Key Concepts\n- On-device inference with versioned ModelRepo architecture\n- Memory-aware model selection using real-time MemoryMonitor\n- Background updates via WorkManager with exponential backoff strategy\n- Safe rollback mechanisms and comprehensive failure recovery\n\n## Code Example\n```kotlin\nclass ModelRepo {\n    fun load(version: String): Model?\n    fun save(version: String, data: ByteArray) {}\n}\n\nclass InferenceEngine(private val repo: ModelRepo) {\n    suspend fun infer(input: String): Re",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-21",
        "questionId": "q-22468",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: Build an on-device, privacy-preserving analytics and A/B testing runner that collects events from multiple apps, isolates each app’s data in its own sandboxed Android process, and exposes a shared aggregation API via AIDL. Implement per-event governance, PII redaction, consent modeling, and a secure batched uploader that transmits only when opted-in and on Wi‑Fi/charging. Provide data model, IPC surface, lifecycle, testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Room entity Note(id: Int, title: String, content: String). DAO: getAllNotes(): Flow<List<Note>>, getNoteById(id): Flow<Note?>; a NoteRepository with suspend insert/update; a NotesViewModel",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create an Event entity with id, timestamp, type, and payload; a Room DAO selecting last 50 by timestamp; initialize a SQLCipher-encrypted RoomDatabase with a keystore-wrapped master key; expose a simp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a host ProcessManager that binds to a dedicated isolated process per plugin. Define an AIDL IThemeProvider interface with methods such as getPalette(), getTypography(), and getShape(). Verify plugin signatures…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a central sandboxed aggregator service exposed via AIDL; each app writes events to its own sandboxed Room DB, encrypted with a device Keystore key; redact PII at write, apply per-event governance,",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world Android apps collect analytics from multiple clients while preserving privacy. This tests cross-process design, data governance, and offline readiness in a production-like setting.\n\n## Key Concepts\n- **Cross-process IPC** via AIDL\n- **Per-app sandboxes** using isolated processes\n- **PII redaction** and consent modeling\n- **Encrypted on-device storage** (Room + Keystore)\n- **Battery/network-aware uploads** (WorkManager)\n\n## Code Example\n```javascript\n// Pseudo AIDL-like surface (illustrative)\nparcelable Event;\ninterface IAggregator {\n  void submitEvent(in Event event);\n}\n```\n```javascript\n// Pseudo usage sketch\nIAggregator aggregator = IAggregator.Stub.asInterface(binder);\nEvent e = new Event(/* redacted fields */);\naggregator.submitEvent(e);\n```\n\n## Follow-up Questions\n- How would you test per-event governance in scenarios with partial consent?\n- How would you handle key rotation without data loss?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-22",
        "questionId": "q-29872",
        "question": "Context: android/general; Difficulty: beginner; Topics: android. Scenario: Create a minimal Android feature that uses CameraX to capture a photo, applies a simple on-device filter (grayscale), and stores the result encrypted on disk using a per-user AES-GCM key sourced from Android Keystore. Wrap the per-file key with a per-user envelope and expose the image to other apps via a small ContentProvider with a read-only URI contract. Outline data models, encryption envelope, ContentProvider contract, and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use CameraX ImageCapture to take a photo, run a lightweight grayscale filter in a short coroutine, then encrypt bytes with AES-GCM using a per-user key stored in Android Keystore. Wrap the image key w",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Deploy a real-time AR filter pipeline on Android with an isolated capture process; share frames via Binder/ashmem; run a native filter chain in a separate isolated module; protect per-session keys in Android Keystore; s…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use RecyclerView with ViewHolder pattern: create custom ViewHolder extending RecyclerView.ViewHolder, implement onCreateViewHolder to inflate item layout, onBindViewHolder to bind data, and use DiffUtil for efficient li…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a ViewModel with a MutableStateFlow<String> for query, a StateFlow<List<Item>> for results, and a loading state. Debounce input with queryFlow.debounce(300). Retrofit service: suspend fun searchIt",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a compact cryptography pipeline, Android Keystore integration, and a minimal data sharing contract via ContentProvider, all at beginner level but with realistic constraints.\n\n## Key Concepts\n- CameraX ImageCapture\n- Bitmap processing with coroutines\n- AES-GCM encryption with per-user key in Android Keystore\n- Envelope wrapping for per-file keys\n- ContentProvider contract and URI permissions\n- Tests: encryption/decryption integrity, permission edge cases, lifecycle\n\n## Code Example\n\n```java\n// Minimal skeleton to illustrate wrapping and encryption\nclass CryptoUtil {\n  // placeholder for key generation, wrapping, and AES-GCM encrypt/decrypt\n}\n```\n\n## Follow-up Questions\n- How would you rotate keys without re-encrypting all files?\n- What changes if the target device lacks hardware-backed Keystore?\n- How would you handle key loss or user change in a multi-profile device?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-23",
        "questionId": "q-8526",
        "question": "Context: android/general; Difficulty: beginner; Topics: android. Scenario: Build a Kotlin + Jetpack Compose module that fetches article headlines from a REST API, caches minimal metadata (id, title, timestamp) in Room, and renders a simple list. Add a Settings toggle to enable Text-to-Speech that reads the first two headlines on demand, using Android's TTS engine with proper lifecycle handling. Explain data model, Retrofit service/DAO, TTS integration, and a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-tenant data model in Room (tenant_id, form_id, record). All data is encrypted per-tenant with Keystore keys and isolated by tenant_id. Templates are fetched online and cached; offline in",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implemented offline-first sync using Room with Note(id, text, lastModified, serverVersion, locallyChanged). A WorkManager sync job pushes local changes to /notes and pulls remote updates, resolving co",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement ephemeral messaging with end-to-end encryption: per-conversation AES-256 keys rotated daily; messages encrypted with AES-GCM using random IV; per-convo keys wrapped in Android Keystore; ciph",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Room entity Headline(id: String, title: String, timestamp: Long); DAO with insert/getAll; Retrofit service for REST headlines; Repository exposing Flow<List<Headline>>; Compose LazyColumn",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests knowledge of core Android app architecture: data layer with Room, network layer with Retrofit, UI with Jetpack Compose, and a practical accessibility feature (TTS). It also touches lifecycle awareness and basic testing strategies.\n\n## Key Concepts\n\n- Room: entity, DAO, migrations (basic)\n- Retrofit: service interfaces and data models\n- Kotlin Flow: streaming data from repository to UI\n- Jetpack Compose: LazyColumn rendering\n- TextToSpeech: initialization, lifecycle handling, easing for UI\n- Testing: unit tests for data layer; instrumentation for TTS and UI flows\n\n## Code Example\n\n```kotlin\n// Entity\n@Entity(tableName = \"headlines\")\ndata class Headline(\n  @PrimaryKey val id: String,\n  val title: String,\n  val timestamp: Long\n)\n\n// DAO\n@Dao\ninterface HeadlineDao {\n  @Insert(onConflict = OnConflictStrategy.REPLACE)\n  suspend fun insertAll(headlines: List<Headline>)\n\n  @Query(\"SELECT * FROM headlines ORDER BY timestamp DESC\")\n  fun getAll(): Flow<List<Headline>>\n}\n```\n\n```kotlin\n// Retrofit service\ninterface HeadlinesService {\n  @GET(\"headlines\")\n  suspend fun fetchHeadlines(): List<Headline>\n}\n```\n\n```kotlin\n// Repository sketch\nclass HeadlinesRepo(private val dao: HeadlineDao, private val svc: HeadlinesService) {\n  val headlines: Flow<List<Headline>> = dao.getAll()\n\n  suspend fun refresh() {\n    val fresh = svc.fetchHeadlines()\n    dao.insertAll(fresh)\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you handle offline-first behavior and cache invalidation?\n- How would you test TTS integration and ensure accessibility support across locales?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-24",
        "questionId": "q-12482",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build an offline-first map tile cache for a navigation-style Android app. Tiles are fetched from a REST API as PNGs, stored encrypted on disk using Android Keystore; implement a two-tier cache: an in-memory LRU for the current viewport and a disk cache with per-tile encryption; prefetch tiles for the current viewport via WorkManager and invalidate cached tiles when a delta manifest indicates updates. Explain data model, eviction policy, encryption strategy, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a data model where each media clip is represented as a ClipRecord containing the encrypted blob, nonce, chunk manifest, and upload state. Encrypt each clip with AES-256-GCM using a per-clip key derived via HKDF f…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a three-tier cache: an in-memory LRU for visible tiles, a disk-based encrypted store (AES-GCM with per-tile keys from Keystore), and a small header index. Tiles are fetched in batche",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a ViewModel with a MutableStateFlow<String> for query, a StateFlow<List<Item>> for results, and a loading state. Debounce input with queryFlow.debounce(300). Retrofit service: suspend fun searchIt",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run an isolated service with android:process and android:isolatedProcess, exposing an AIDL for send/receive. Use a master key from Android Keystore (hardware-backed when available) to derive per-conve",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical Android caching, encryption, and offline resilience in a real feature. It also probes trade-offs between memory, battery, and network.\n\n## Key Concepts\n- In-memory LRU vs disk-backed encrypted tile cache\n- Android Keystore-based encryption (AES-GCM per tile)\n- WorkManager-driven prefetch and backfill\n- Delta manifest-based invalidation and cache coherence\n- Testing: eviction pressure, offline paths, data integrity\n\n## Code Example\n```kotlin\ninterface TileCache {\n  fun getTile(x: Int, y: Int, z: Int): ByteArray?\n  fun putTile(x: Int, y: Int, z: Int, data: ByteArray): Unit\n  fun invalidateTiles(tiles: List<TileCoordinate>)\n}\n```\n\n## Follow-up Questions\n- How would you enforce privacy when tiles are used for routing, not just display?\n- How would you test the delta-invalidations under intermittent connectivity?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-21T03:02:27.899Z",
    "version": 1
  }
]