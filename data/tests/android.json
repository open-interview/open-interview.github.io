[
  {
    "id": "test-android",
    "channelId": "android",
    "channelName": "Android",
    "title": "Android Knowledge Test",
    "description": "Test your understanding of Android with 25 questions.",
    "questions": [
      {
        "id": "android-q-0",
        "questionId": "q-9907",
        "question": "Design an offline-first, end-to-end encrypted ephemeral messaging module for Android that supports self-destruct after a configurable timer; use per-conversation keys rotated daily, Android Keystore-wrapped keys, AES-GCM with a random IV; store ciphertexts in Room and sync deltas via WebSocket; implement tombstones and per-device revocation. Provide data model, encryption scheme, sync protocol, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Room to store Message entities with fields id, convoId, senderId, timestamp, encryptedPayload, and a stored encryptionVersion. Generate per-user keys in Android Keystore and derive per-device sess",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a small MVVM module in Kotlin: Retrofit REST client to fetch 7-day forecast, Room to cache Forecast entities, a Repository mediates between API and DB, and a RecyclerView UI via LiveData/Flow. A",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a per-tenant data model in Room (tenant_id, form_id, record). All data is encrypted per-tenant with Keystore keys and isolated by tenant_id. Templates are fetched online and cached; offline in",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement ephemeral messaging with end-to-end encryption: per-conversation AES-256 keys rotated daily; messages encrypted with AES-GCM using random IV; per-convo keys wrapped in Android Keystore; ciph",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes secure local storage, key management, offline-first sync, and tombstone handling for ephemeral data across devices.\n\n## Key Concepts\n\n- Ephemeral messaging and per-convo keys\n- Android Keystore wrapping, AES-GCM\n- Room persistence and offline-first sync\n- WebSocket-based delta propagation and tombstones\n- Device revocation and key rollover\n\n## Code Example\n\n```kotlin\n// Kotlin snippet illustrating AES-GCM encryption\nfun encryptMessage(plaintext: ByteArray, key: SecretKey, iv: ByteArray): ByteArray {\n  val cipher = Cipher.getInstance(\"AES_256_GCM_NoPadding\")\n  val spec = GCMParameterSpec(128, iv)\n  cipher.init(Cipher.ENCRYPT_MODE, key, spec)\n  return cipher.doFinal(plaintext)\n}\n```\n\n## Follow-up Questions\n\n- How would you handle messages arriving out of order relative to expiry?\n- How would you test cross-device revocation and key rollover?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-1",
        "questionId": "q-18878",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: Implement a secure multi-device session sync for an enterprise Android app used across multiple devices. Each device stores an encrypted session state (permissions, tokens, lastActive) in Room and synchronizes with the cloud using a CRDT-inspired merge. Enrollment uses device attestation via FIDO2 and OIDC; keys rotate in Android Keystore. Describe data model, sync protocol, revocation, and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a CRDT-based notes sync engine that runs in an isolated Android process. Each note uses a per-note AES-GCM key stored in Keystore; CRDT state lives in Room encrypted at rest. IPC via AIDL expos",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Session state is persisted locally in a Room database encrypted with a per-device AES-256 key derived from Android Keystore. Synchronization uses a CRDT-inspired OR-Set (Observed-Remove Set) structure to handle concurre…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use the Activity Result API: register for RequestPermission(CAMERA) and grant path; on approval, launch TakePicturePreview to get a Bitmap, then save it to internal storage with a timestamped filename",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Room with an FTS4 index for notes (id, title, content, updatedAt, version, tombstone). Maintain a searchable mirror and reindex on updates. Sync via WorkManager: push local changes with exponentia",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates the ability to design secure cross-device state synchronization for enterprise Android applications. It tests understanding of cryptographic hygiene, device attestation mechanisms, offline-first reconciliation, and revocation strategies.\n\n## Key Concepts\n\n- **CRDT-based sync**: OR-Sets enable conflict-free merging of permissions and flags across devices without centralized coordination\n- **Encrypted local storage**: Room with SQLCipher or EncryptedSharedPreferences using per-device keys isolated in Android Keystore\n- **Device attestation**: FIDO2 provides hardware-rooted device identity; OIDC establishes user authentication context\n- **Key rotation**: Android Keystore supports automatic key rotation with cryptographic binding to device state\n- **Revocation**: Signed revocation tokens broadcast to all devices with session invalidation\n\n## Data Model\n\n```kotlin\n@Entity\ndata class DeviceSession(\n    @PrimaryKey val deviceId: String,\n    val encryptedState: ByteArray,\n    val vectorClock: Map<String, Long>,\n    val lastActive: Long,\n    val attestationId: String,\n    val keyVersion: Int\n)\n\n@Entity\ndata class SyncPermission(\n    @PrimaryKey val permissionId: String,\n    val orSet: Set<String>, // CRDT container\n    val tombstone: Set<String>\n)\n```\n\n## Sync Protocol\n\n1. Local changes are written to Room immediately with vector clock increment\n2. Sync worker batches local changes, signs with Keystore key\n3. Cloud receives batches, applies CRDT merge logic\n4. Remote changes pulled, decrypted, merged with local state\n5. Conflicts resolved by LWW (Last-Writer-Wins) on vector clock timestamp\n\n## Revocation Strategy\n\n- Master revocation token signed by server\n- Push notification triggers local key invalidation\n- Android Keystore key is destroyed via deleteEntry()\n- Session state cleared from Room on next app launch\n\n## Test Plan\n\n- Unit: CRDT merge logic, vector clock ordering\n- Integration: Full sync cycle with mock cloud backend\n- Security: Key extraction prevention, attestation verification\n- Chaos: Network interruption during sync, concurrent edits",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-2",
        "questionId": "q-17700",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: You’re building an offline-first bank-statement parser for a finance app. Build a component that accepts PDF or CSV statements, extracts transactions, deduplicates them, and stores them encrypted in Room. It must run in a sandboxed service (android:process and android:isolatedProcess) and expose an AIDL surface for the UI to submit documents and request results. Implement per-user encryption with Android Keystore, optional ML-assisted OCR, model rotation, and a background sync that uploads reconciled statements to backend only on Wi‑Fi/charging. Provide data model, IPC surface, parsing/workflow, and a robust test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use WorkManager: define a Reminder data class, a Worker that builds a NotificationCompat, enqueue a UniquePeriodicWork with 24h interval, and compute initialDelay to the next occurrence of hour:minute",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect Room entities POI(id, name, lat, lon), TileIndex(tileId, data, lastUpdated); a WorkManager workflow with constraints and delta sync; in-memory Trie-based ranking with an LRU cache; storage c",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Retrofit to fetch Article DTOs and map them to Room entities for local caching. The UI observes a Flow<List<Article>> from the DAO, loading cached data on startup and triggering a network refresh when connectivity i…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a component to parse PDFs/CSVs into encrypted Room entities: Statement and Transaction with dedup via externalId. Expose an AIDL IPC: submitDocument(byte[] data, String mimeType) -> Result and",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design on-device parsing, cross-process IPC, secure storage, and offline-first syncing for fintech data.\n\n## Key Concepts\n\n- Android sandboxing with android:process and android:isolatedProcess\n- AIDL IPC surfaces and lifecycle\n- Room with Encrypted type converters and Android Keystore\n- PDF/CSV parsing, optional ML OCR, dedup and reconciliation\n- WorkManager constraints for background uploads\n\n## Code Example\n\n```kotlin\ndata class Statement(val id: String, val period: String, val source: String, val transactions: List<Transaction>)\ndata class Transaction(val id: String, val stmtId: String, val date: LocalDate, val amount: Long, val description: String, val merchant: String, val currency: String, val externalId: String)\n```\n\n## Follow-up Questions\n\n- How would you test IPC reliability across process restarts?\n- What strategies ensure idempotent uploads and key rotation impact minimization?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-3",
        "questionId": "q-10088",
        "question": "Build a beginner-friendly Android feature: fetch a list of notes from a REST API, cache them in Room with content encrypted via Android Keystore, and display the titles in a RecyclerView. Implement a WorkManager background task to refresh the cache every 15 minutes on Wi‑Fi and charging. Explain data model, encryption, refresh policy, and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Model a Note with id, title, encryptedContent, timestamp. Encrypt content with AES-GCM using a Keystore-stored key; decrypt for display. Repository handles API, local cache, and crypto. Schedule a Wor",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a CRDT-based document model stored in Room where each document is a collection of atoms with unique IDs; edits produce operations that are partitioned by user and merged by vector clocks. Encry",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would model Tenant, Survey, Section, Field, and Answer with per-field revisions. Keys live in Android Keystore and encrypt sensitive answers in Room (SQLCipher or on-disk crypto). Delta sync runs at",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design: Room database encrypted with SQLCipher, per-user envelope keys in Android Keystore, data keys wrapped and rotated. Use WorkManager to batch-sync when network is available, running a foreground",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of Android data flow, local caching, and background work with practical crypto basics.\n\n## Key Concepts\n- Room data model with simple encryption\n- Android Keystore key management\n- WorkManager constraints and periodic work\n- Data flow between API, cache, and UI\n\n## Code Example\n```kotlin\n// Pseudo-code: Entity, DAO, and Worker sketches\n```\n\n## Follow-up Questions\n- How would you handle key rotation and tombstones?\n- How to test WorkManager work with and without network?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-4",
        "questionId": "q-15373",
        "question": "Context: android/general; Difficulty: beginner; Topics: android. Scenario: Build a Notes screen that supports add/edit/delete notes, stores them locally using Room, shows them in a RecyclerView, and includes a simple search filter. Provide Note entity, DAO, Repository, and a ViewModel plus minimal Activity and Adapter wiring. Describe data flow, lifecycle handling, and a basic test plan. What code would you write to implement this from scratch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a module that stores telemetry in Room, maintains a local asset map, and applies server delta patches offline via WorkManager. Patches are signed with per-device keys from Android Keystore (",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Room persistence library: @Entity Note(id: Int = 0, title: String, body: String, ts: Long). @Dao: fun getAll(): Flow<List<Note>>; fun search(query: String): Flow<List<Note>> using LIKE. suspend fun insert(n: Note),…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Adopt a CRDT-based collaborative editor. Use a two CRDT approach: an Op-based CRDT for cell edits and an OR-Set for row/column inserts/deletes. Ops buffered in Room with per-edit tombstones; on reconn",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a Jetpack Compose screen with two TextFields (name, email). Validate email with a simple regex and display inline error messages. Persist draft values in DataStore (Preferences) so the fields p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question tests fundamental Android development skills including Room database persistence, the MVVM architecture pattern, and basic UI wiring—key competencies for entry-level Android positions.\n\n## Key Concepts\n\n- Room entities and DAOs for local persistence\n- Kotlin Flow for reactive, lifecycle-safe data streams\n- RecyclerView adapter pattern for efficient list rendering\n- SQL LIKE queries for basic search functionality\n- Separation of concerns: Entity → DAO → Repository → ViewModel → UI\n\n## Code Example\n\n```kotlin\n@Entity\ndata class Note(\n    @PrimaryKey(autoGenerate = true) val id: Int = 0,\n    val title: String,\n    val body: String,\n    val timestamp: Long = System.currentTimeMillis()\n)\n\n@Dao\ninterface NoteDao {\n    @Query(\"SELECT * FROM notes ORDER BY timestamp DESC\")\n    fun getAll(): Flow<List<Note>>\n\n    @Query(\"SELECT * FROM notes WHERE title LIKE '%' || :query || '%' OR body LIKE '%' || :query || '%'\")\n    fun search(query: String): Flow<List<Note>>\n\n    suspend fun insert(note: Note)\n    suspend fun update(note: Note)\n    suspend fun delete(note: Note)\n}\n\nclass NoteRepository(private val noteDao: NoteDao) {\n    fun getAllNotes(): Flow<List<Note>> = noteDao.getAll()\n    fun searchNotes(query: String): Flow<List<Note>> = noteDao.search(query)\n    suspend fun insert(note: Note) = noteDao.insert(note)\n    suspend fun update(note: Note) = noteDao.update(note)\n    suspend fun delete(note: Note) = noteDao.delete(note)\n}\n\nclass NoteViewModel(private val repository: NoteRepository) : ViewModel() {\n    private val _searchQuery = MutableStateFlow(\"\")\n    val notes: Flow<List<Note>> = _searchQuery.flatMapLatest { query ->\n        if (query.isBlank()) repository.getAllNotes()\n        else repository.searchNotes(query)\n    }\n\n    fun setSearchQuery(query: String) { _searchQuery.value = query }\n    suspend fun addNote(note: Note) = repository.insert(note)\n    suspend fun updateNote(note: Note) = repository.update(note)\n    suspend fun deleteNote(note: Note) = repository.delete(note)\n}\n```\n\n## Data Flow\n\n1. User interacts with UI (adds/edits/deletes/searches)\n2. Activity calls ViewModel methods\n3. ViewModel delegates to Repository\n4. Repository interacts with DAO\n5. Room executes queries on SQLite database\n6. Flow emits updated data back through the chain\n7. UI observes Flow and updates RecyclerView automatically\n\n## Lifecycle Handling\n\n- ViewModel survives configuration changes (rotation, etc.)\n- Flow collection in Activity/Fragment uses repeatOnLifecycle or collectLatest to prevent leaks\n- Room handles threading via Coroutines\n\n## Basic Test Plan\n\n1. **Unit Tests**: Test Repository and ViewModel logic with in-memory database\n2. **Instrumented Tests**: Test DAO operations with real Room database\n3. **UI Tests**: Test add/edit/delete/search user flows with Espresso",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-5",
        "questionId": "q-10393",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build an offline-first note-taking module that records audio notes, transcribes them on-device with ML Kit, stores audio and transcripts encrypted in Room using Android Keystore, and enables cross-device search and sync via a CRDT-based tombstone model. Implement chunked audio processing, background transcription with WorkManager, and robust conflict resolution. Explain data model, encryption, sync rules, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CRDT AR graph: each stroke is a node anchored to an ARCore point with parent edges. Ops encoded in protobuf: op(ADD/MOVE/DELETE), strokeId, color, thickness, coords, parentId, deviceId, ts. Queue in R",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Spawn an android:isolatedProcess service to load dynamic feature module plugins. Each plugin exposes a render(IHost) method via AIDL; host injects a minimal UI surface. Use a versioned manifest and in",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a Jetpack Compose screen with two TextFields (name, email). Validate email with a simple regex and display inline error messages. Persist draft values in DataStore (Preferences) so the fields p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Note data model: {id,title,timestamp,audioPath,transcript,lastModified,tombstone}. Encrypt with Android Keystore in Room. Transcribe on-device with ML Kit, chunk audio into 15s blocks and append trans",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design an offline-first, encrypted note system with on-device ML, chunked processing, background work, and robust cross-device sync.\n\n## Key Concepts\n- Android Keystore encryption with Room\n- on-device ML Kit transcription\n- chunked audio processing\n- CRDT tombstones for conflict resolution\n\n## Code Example\n```kotlin\n// Pseudo Kotlin sketch\ndata class Note(...)\n```\n\n## Follow-up Questions\n- How would you handle large note histories without user-perceived latency?\n- How would you performance-test the transcription pipeline?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-6",
        "questionId": "q-24654",
        "question": "Deploy a real-time AR filter pipeline on Android with an isolated capture process; share frames via Binder/ashmem; run a native filter chain in a separate isolated module; protect per-session keys in Android Keystore; synchronize timestamps with a ring buffer; design tests for latency, jitter, frame drops, and privacy constraints?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy a real-time AR filter pipeline on Android with an isolated capture process; share frames via Binder/ashmem; run a native filter chain in a separate isolated module; protect per-session keys in Android Keystore; s…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement an ExplainEngine that hooks into the inference path, producing a local heatmap (gradient-based) and a per-image explanation API. Patches update explanation rules; apply during idle windows w",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a local payments log in Room with fields: id, amount, cardId, status, sequence, txnId; sign each entry with a per-device key from Android Keystore; rotate keys nightly and publish public key",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed approach: use Room tables Segment(id, name, criteriaJson, variant, version) and Patch(id, version, opsJson, signature). Patch ops: Add/Update/Delete. Sign patches with per-device keys in Keys",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes cross-process media pipelines, real-time constraints, and security in AR applications—common in mobile vision workloads at scale.\n\n## Key Concepts\n- Android isolated processes and Binder IPC\n- Ashmem zero-copy buffers and BufferQueue\n- JNI/NDK integration for native filters\n- Android Keystore hardware-backed keys\n- Real-time constraints: latency, jitter, frame drops\n- Testing: end-to-end latency, memory budgets, security threats\n\n## Code Example\n\n```java\n// Pseudo Binder interface\npublic interface IFrameBuffer extends IInterface {\n    void pushFrame(byte[] frameData, long timestamp, int sequenceNum);\n    void registerCallback(IFrameCallback callback);\n}\n\n// On the service side (isolated process)\npublic class FrameBufferService extends Service {\n    private final IBinder mBinder = new FrameBufferBinder();\n    \n    @Override\n    public IBinder onBind(Intent intent) {\n        return mBinder;\n    }\n}\n\n// Native filter module (separate isolated process)\npublic class NativeFilterService extends Service {\n    static {\n        System.loadLibrary(\"arfilter\");\n    }\n    \n    private native long nativeInit(String keystoreKeyId);\n    private native void nativeProcessFrame(long handle, int fd, long timestamp);\n    private native void nativeRelease(long handle);\n}\n\n// Ring buffer for timestamp synchronization\npublic class TimestampRingBuffer {\n    private static final int CAPACITY = 64;\n    private final long[] timestamps = new long[CAPACITY];\n    private final long[] sequenceNums = new long[CAPACITY];\n    private int writeIndex = 0;\n    \n    public synchronized void write(long timestamp, long seqNum) {\n        timestamps[writeIndex] = timestamp;\n        sequenceNums[writeIndex] = seqNum;\n        writeIndex = (writeIndex + 1) % CAPACITY;\n    }\n    \n    public synchronized long[] readSince(long sinceSeqNum) {\n        // Return timestamps for frames since the given sequence number\n    }\n}\n\n// Android Keystore key protection\nprivate KeyStore.KeyProtection getKeyProtection() {\n    return new KeyStore.KeyProtection.Builder(\n        KeyProperties.PURPOSE_ENCRYPT | KeyProperties.PURPOSE_DECRYPT)\n        .setUserAuthenticationRequired(true)\n        .setUserAuthenticationParameters(0,\n            KeyProperties.AUTH_BIOMETRIC_STRONG)\n        .setInvalidatedByBiometricEnrollment(true)\n        .build();\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-7",
        "questionId": "q-10810",
        "question": "Build a privacy-preserving, multi-user commenting system for an offline-capable note app. It must support offline edits, end-to-end encryption on-device (Android Keystore), and delta-based replication across devices using a CRDT. Define data model (Note, Comment, User), per-field encryption, delta encoding, conflict resolution strategy, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Play Feature Delivery for dynamic features and a server-driven flag service (Remote Config or REST). Cache flags in Proto DataStore with a version tag; refresh via WorkManager; determine cana",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a per-note CRDT (sequence CRDT for comments) stored in Room; encrypt the serialized note payload with AES-256-GCM using Android Keystore keys, at-rest per-device with encrypted keys. Sync del",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "**Data Model (Entity):** ```kotlin @Entity data class Note( @PrimaryKey val id: String, val content: String, val updatedAt: Long, val synced: Boolean = false ) ``` **DAO:** ```kotlin @Dao interface NoteDao { @Insert(onC…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Room entity Article(id, title, bodyEncrypted, timestamp, source). DAO provides upsert and query operations. Background sync via WorkManager with constraints: unmetered network and battery not low; fetch only on Wi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes offline collaboration, end-to-end encryption, and CRDT-based reconciliation in Android apps.\n\n## Key Concepts\n\n- CRDT for notes and comments (per-note, tombstones)\n- On-device encryption with Android Keystore (AES-256-GCM)\n- Delta encoding with op-id, author, timestamp\n- Deterministic merge and non-deterministic tombstone handling\n- End-to-end testing across offline/online flows\n\n## Code Example\n\n```javascript\n// Pseudo CRDT op for adding a comment\n{ op: 'addComment', noteId: 'note1', commentId: 'c123', text: 'Hello', actor: 'u1', clock: 12345 }\n```\n\n## Follow-up Questions\n\n- How would you handle large media attachments securely?\n- How would you verify CRDT convergence across devices?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-8",
        "questionId": "q-2824",
        "question": "Design an Android module for offline-first, real-time co-op game state using CRDTs to merge concurrent updates; use Room for local persistence, WebSocket for server sync, and per-user encryption keys stored in Android Keystore with nightly ephemeral key rotation. Describe architecture, data model, CRDT design, sync protocol, and a test plan for latency, churn, and conflict resolution?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Retrofit to fetch Article DTOs and map them to Room entities for local caching. The UI observes a Flow<List<Article>> from the DAO, loading cached data on startup and triggering a network refresh when connectivity i…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a CRDT-based approach (state-based OR-Set + MVRegister) to model per-entity state. Each device signs ops with a per-user key in Keystore; store encrypted deltas locally in Room; sync via WebSo",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a flag service that stores flags in Room with fields: name, value, version, rollout, deps, lastSynced. Evaluation is deterministic: active if hash(ANDROID_ID||name) mod 100 < rollout. Use Da",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single Room database with a tenantId column and per-tenant encryption keys stored in Keystore; derive keys via a per-tenant alias; encrypt payloads before storage; use WorkManager to batch and r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of offline-first CRDT design, encryption, key rotation, and practical trade-offs in a mobile environment.\n\n## Key Concepts\n\n- CRDT types (OR-Set, MVRegister)\n- End-to-end encryption with Android Keystore\n- Room-based offline persistence\n- WebSocket-based sync with anti-entropy\n- Key rotation strategy and impact on data at rest\n\n## Code Example\n\n```kotlin\ndata class MVRegister<T>(val value: T, val ts: Long)\n\nfun <T> merge(a: MVRegister<T>, b: MVRegister<T>): MVRegister<T> = if (a.ts > b.ts) a else b\n```\n\n## Follow-up Questions\n\n- How would you handle tombstones to bound memory?\n- How would you verify multi-device convergence under churn?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-9",
        "questionId": "q-2664",
        "question": "You're building an Android module for real-time, offline-first collaborative document editing. Each document uses a CRDT to merge concurrent edits locally and remotely. The app syncs with a central server over WebSocket with a REST fallback; local changes are persisted in Room; security via per-document encryption keys. Describe architecture, data model, CRDT design, sync protocol, and testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an isolated Android service using android:isolatedProcess that streams activity labels via AIDL to the UI, while running a TensorFlow Lite model inside the same isolated environment. Encrypt i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect an AIDL surface IEventAggregator with submitEvent(Event), fetchBatch(), and rotateKeys(). Run the processor in an android:isolatedProcess, store raw events encrypted in Room using a Keystore",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an architecture that stores per-document CRDT state in Room, uses a lightweight CRDT (e.g., Add-Wins OR-Set) to merge concurrent local edits, and syncs via WebSocket with a REST fallback. Incl",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Leverage Room entities with fields id, last_modified, and synced flag. Use a CoroutineWorker triggered by NETWORK_CONNECTED; query items where synced = false or last_modified > serverVersion. Upload w",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses the ability to design offline-first, real-time collaboration on mobile, including CRDT data models, sync protocols, conflict resolution, and security.\n\n## Key Concepts\n- CRDTs (Add-Wins / OR-Set)\n- Room persistence\n- WebSocket sync with REST fallback\n- Tombstones and delete semantics\n- Per-document encryption keys\n\n## Code Example\n```kotlin\n// Minimal Add-Wins CRDT sketch\ndata class Dot(val actor: String, val clock: Long)\nclass AddWinsCRDT {\n  private val adds = mutableMapOf<String, MutableSet<Long>>()\n  fun add(e: String, d: Dot) { adds.computeIfAbsent(e) { mutableSetOf() }.add(d.clock) }\n  fun value(): Set<String> = adds.filter { it.value.isNotEmpty() }.keys\n}\n```\n\n## Follow-up Questions\n- How would you handle offline deletes and tombstones?\n- How would you test consistency across multiple clients with network partitions?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-10",
        "questionId": "q-6451",
        "question": "Context: android general; Difficulty: advanced; Topics: android. Scenario: Design an offline-first collaborative translation memory module for a global app catalog. Each device stores entries (source, language, translation, notes, translator, timestamp) in Room with per-device encryption via Android Keystore, and merges edits using a CRDT delta graph. When online, reconcile with a GraphQL backend, keeping audit trails and per-entry attribution. Describe the data model, CRDT operations, conflict resolution, encryption strategy, and a concrete test plan for cross-device merges, rollback, and auditability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Load a quantized on-device model, collect user deltas offline as next-token predictions, encode as gradient updates, encrypt deltas with per-device Android Keystore keys (signed with attestation), and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a WorkManager PeriodicWorkRequest with a daily constraint to fetch app-config.json from a remote URL using Retrofit. Parse the response into a Config data class and persist it to DataStore (Proto or Preference…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an offline-first collaborative translation memory module with the following architecture: Store Entry entities (id, source, language, translation, notes, translator, timestamp, tombstone) in Room database; implem…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Store events in Room with a pending_events table and a batches table; batch up to 100 events or 512KB, then flush via Retrofit. Run a Worker with constraints: charging and unmetered network; use batch",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates ability to design offline-first CRDT-backed data stores with per-entry encryption and auditability.\n\n## Key Concepts\n- CRDT delta graphs, vector clocks, tombstones\n- Android Keystore encryption and key rotation\n- Room data model and migrations\n- Server reconciliation via GraphQL and audit trails\n\n## Code Example\n```kotlin\ndata class Entry(\n    val id: String,\n    val source: String,\n    val language: String,\n    val translation: String,\n    val notes: String,\n    val translator: String,\n    val timestamp: Long,\n    val tombstone: Boolean\n)\n```\n\n## Follow-up Questions",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-11",
        "questionId": "q-6668",
        "question": "Context: android general; Difficulty: advanced; Topics: android. Scenario: Design a server-driven UI module that fetches a per-user UI manifest (JSON) from a GraphQL backend, renders with Jetpack Compose, and supports offline-first operation. Users can customize layout locally (reorder sections, hide widgets) which must merge with server state via a CRDT-like delta graph, stored in Room with per-device encryption via Android Keystore. Explain data model, merge rules, encryption strategy, and a concrete test plan for cross-device consistency and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a CRDT-augmented manifest: a tree of nodes (id, type, props, children) where UI order is a sequence CRDT (RGA). Each device stores a per-device signed delta in Room, encrypted with Android Keystor",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement a Weather entity with city as primary key, tempC, description, timestamp. DAO: getLatest(city): Weather? and upsert(weather). Repository: refreshWeather() uses Retrofit to call the A",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Room-based append-only ChangeLog with the following fields: change_id (UUID), entity_id, entity_type, operation, timestamp, device_id, and payload. On every mutation, write a corresponding ChangeLog entry. A…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use MutableStateFlow<UiState> with sealed class (Loading/Success/Error) and collect with catch operator for error handling in coroutine scope.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design server-driven UI with offline-first operation, conflict resolution, and security.\n\n## Key Concepts\n\n- Server-driven UI manifests\n- CRDT-based merge for UI topology\n- Encryption with Android Keystore\n- Offline-first rendering with Jetpack Compose\n- GraphQL reconciliation\n\n## Code Example\n\n```kotlin\ndata class ManifestNode(val id: String, val type: String, val props: Map<String, Any>, val children: List<String>)\n```\n\n## Follow-up Questions\n\n- How would you handle schema migrations for manifests offline?\n- How would you measure latency impact on render?\n- How would you test conflict resolution under rapid, concurrent edits from multiple devices?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-12",
        "questionId": "q-7002",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: Build a server-driven dashboard module that renders a per-user layout from a GraphQL manifest into Jetpack Compose UI, with charts drawn on a Canvas. Implement offline-first caching of manifest and UI state in Room with per-device encryption, and deliver heavy charts via dynamic feature modules to reduce startup cost. Describe data model, synchronization, encryption, and a concrete test plan for cross-device consistency, rollback, and memory pressure handling. How would you implement end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a bound AIDL service exposing getTile(x,y,z) that loads encrypted Vector Tiles from a Room store; use Android Keystore to wrap per-user AES keys; implement LRU eviction with a configurable m",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "KeyVaultService runs in an isolated Android process; AIDL exposes sign(hash, algo) and generateKey(). Keys live in the Android Keystore (hardware-backed if available) with user-auth. Rotate keys on sc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-user DashboardManifest entity storing layout as a JSON blob and a DashboardBlock table for blocks; render by mapping blocks to Compose composables. Cache manifest and UI state in Room with A",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Kotlin to create a LoginViewModel that exposes LiveData for username, password, and a computed isFormValid flag. Implement a PasswordValidator to enforce password strength: minimum 8 characters, at least one upperca…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests server-driven UI mapping, offline-first design, encryption, and dynamic feature delivery under real-world constraints.\n\n## Key Concepts\n- Server-driven UI\n- Jetpack Compose mapping\n- Room encryption with Android Keystore\n- Dynamic feature modules\n- GraphQL deltas and conflict resolution\n- WorkManager background sync\n- Memory pressure handling\n\n## Code Example\n```kotlin\ndata class DashboardManifest(\n  val userId: String,\n  val layoutJson: String,\n  val version: Int\n)\n```\n\n## Follow-up Questions\n- How would you test offline-first correctness across devices?\n- How would you handle manifest schema migrations?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-13",
        "questionId": "q-29551",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: Build a secure Accessibility Extensions Framework that loads approved extensions as dynamic feature modules, each running in its own isolated process (android:process and android:isolatedProcess=true) and communicating with the host via a defined AIDL surface. Extensions expose onAssistiveEvent and renderOverlay. Outline lifecycle, governance, per-extension resource budgets, trust boundary, IPC surface, and a robust test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a Room model with Article(id, title, contentUrl, manifestVersion) and a UserEdit(entity for highlights/notes) encrypted via Android Keystore. Fetch manifests via GraphQL, render content with J",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an on-device ML inference service for Android that runs in an isolated process, loads a quantized TensorFlow Lite model from signed assets, and decrypts it using a per-model envelope stored in Android Keystore. S…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement ICaptions AIDL: startStream(audioConfig), sendFrame(buffer, ts), stopStream, with ICaptionListener for transcripts. Captioner runs in an isolated process; audio buffers shared via HardwareBu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an Accessibility Extensions Framework: load extensions as dynamic feature modules; run each in its own isolated process (android:process, android:isolatedProcess=true). Define AIDL IA11yExt wit",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design secure, extensible Android architectures with process isolation, IPC, and governance for third-party extensions.\n\n## Key Concepts\n- Dynamic feature modules and per-extension manifests\n- android:process and android:isolatedProcess usage\n- AIDL surface for defined extension APIs\n- Resource budgets, trust boundary, and verifier-based validation\n- Accessibility testing and security hardening\n\n## Code Example\n```aidl\n// IA11yExt.aidl\npackage com.example.a11y;\nparcelable AssistEvent;\nparcelable OverlaySpec;\ninterface IA11yExt {\n  void onAssistiveEvent(AssistEvent event);\n  OverlaySpec renderOverlay(in OverlaySpec spec);\n}\n```\n```\n\n## Follow-up Questions\n- How would you enforce memory and CPU quotas per extension at runtime?\n- How would upgrade/downgrade and rollback of extensions be handled safely?\n- What are key attack vectors and mitigations in this trust-bound IPC model?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-14",
        "questionId": "q-15693",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build an offline-first, privacy-preserving clipboard audit system in an Android app. The app records copy events locally, encrypts them in Room with a per-device KEK sourced from Android Keystore, runs a sandboxed service (android:process) exposing an AIDL interface to the main app for querying and exporting event hashes, and optionally uploads hashed summaries via WorkManager when Sync is enabled. Provide data model, IPC surface, encryption/key management choices, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use MVVM with a LoginViewModel that exposes email, password, and error states via StateFlow. Validate email with Patterns.EMAIL_ADDRESS and require password length >= 8 on every input; bind to Compose",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Define a Room @Entity Note(id: Int, title: String, body: String, lastModified: Long) with a corresponding DAO for fetching notes. Implement a ContentProvider in a dedicated process (android:process=\":notesProvider\") tha…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a PluginRouter in the host app that auto-discovers plugins via PackageManager and binds to each plugin’s AIDL service, each running in an isolatedProcess boundary. Enforce per-plugin identit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an offline clipboard audit with privacy in Android: store copy events in encrypted Room using a device KEK from Android Keystore; run a sandboxed service (android:process) that exposes an AI",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests cross process security, encryption, and background syncing in a privacy conscious feature.\n\n## Key Concepts\n- Android Keystore and hardware backed keys\n- Room encryption and per record hashing\n- AIDL IPC with isolatedProcess\n- WorkManager for deferred uploads\n- Privacy first data minimization\n\n## Code Example\n```kotlin\n// Example data model\ndata class ClipboardEvent(val textHash: String, val timestamp: Long, val sessionId: String)\n```\n\n## Follow-up Questions\n- How would key rotation affect existing data?\n- How to verify no PII leakage during export?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-15",
        "questionId": "q-30282",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build a privacy-preserving **clipboard guardian** in an isolated Android process (android:process + android:isolatedProcess) exposing an AIDL API to authorized apps to read the last clipboard item. It must intercept changes, redact sensitive data by policy, encrypt storage with hardware-backed keys, and enforce per-app access via rotating tokens. Provide data model, IPC surface, lifecycle, and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "In the isolated process, implement a ContentObserver on ClipboardManager to capture changes, redact sensitive data by policy, and store the last item encrypted with hardware-backed keys. Expose getLas",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect an isolated AnalyticsService running in a separate process, buffering events in an encrypted Room database, applying local differential privacy before batching. Respect per-user opt-ins, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Room with SQLCipher (or AndroidX Security Crypto library) to encrypt the notes table at rest. Generate a master key in Android Keystore and wrap per-note data encryption keys using envelope encryption. Expose a read…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would design a modular Android analytics SDK that logs raw location pings to an encrypted Room table, derives privacy-preserving aggregates by adding calibrated differential privacy noise with per-device k-anonymity,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nReal-world IPC, process isolation, and policy enforcement are needed for privacy-critical features. This question tests ability to design a trusted mediator, enforce per-app access, and handle token rotation without destabilizing apps.\\n\\n## Key Concepts\\n\\n- Android isolatedProcess and binder IPC via AIDL\\n- Clipboard interception and privacy filtering\\n- Hardware-backed Keystore encryption and per-app keys\\n- Policy-driven redaction and auditing\\n- Lifecycle and crash resilience\\n\\n## Code Example\\n\\n```kotlin\\n// AIDL and service skeleton (conceptual)\\ninterface IClipboardGuard {\\n    String getLastClipboard();\\n    void rotateToken(String appId, String token);\\n}\\n\\nclass ClipboardGuardService : Binder(), IClipboardGuard {\\n    // stub: access control and interception logic\\n}\\n```\\n\\n## Follow-up Questions\\n\\n- How would you implement per-app access verification across IPC calls?\\n- What testing strategy ensures policy correctness and resilience to clipboard noise?\\n",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-16",
        "questionId": "q-26526",
        "question": "Context: android/general; Difficulty: beginner; Topics: android. Scenario: Implement a daily reminder feature that schedules a notification at a user-specified time every day using WorkManager. Specify the data model (id, title, hour, minute, timeZone), the Worker that builds the notification, how to schedule with a PeriodicWorkRequest and initialDelay to the next occurrence, and how you would test end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Retrofit to fetch Article DTOs and map them to Room entities for local caching. The UI observes a Flow<List<Article>> from the DAO, loading cached data on startup and triggering a network refresh when connectivity i…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use WorkManager: define a Reminder data class, a Worker that builds a NotificationCompat, enqueue a UniquePeriodicWork with 24h interval, and compute initialDelay to the next occurrence of hour:minute",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage an isolated service (android:isolatedProcess) that exposes AIDL for CRUD on a Room-backed, SQLCipher-encrypted notes DB. Store per-note keys wrapped by Android Keystore and derive data keys w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an offline-first moderation queue: store flags in Room with fields contentId, flaggerDeviceId, timestamp, verdict, and a per-device signing key in Android Keystore (nightly rotation). Submissio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests translating user-facing requirements into robust background work using WorkManager, plus precise time calculations and testability.\n\n## Key Concepts\n- Data modeling for a simple reminder (id, title, hour, minute, timeZone)\n- WorkManager: PeriodicWorkRequest, initialDelay, unique work\n- Notification construction and end-to-end testing\n\n## Code Example\n\n```java\n// Worker skeleton\nclass ReminderWorker extends Worker { ... }\n```\n\n## Follow-up Questions\n- How would you handle DST changes affecting scheduled times?\n- How would you test time-zone changes across devices?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-17",
        "questionId": "q-25284",
        "question": "Implement a cross-conversation ephemeral chat store in Android that runs in a dedicated sandboxed service (android:process) and exposes an AIDL interface to the UI. Store messages encrypted at rest with per-conversation keys wrapped by Android Keystore, support key rotation and forward secrecy, and a offline-outbox for queued messages. Data models, IPC surface, lifecycle, and a test plan required?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an isolated Android service using android:isolatedProcess that streams activity labels via AIDL to the UI, while running a TensorFlow Lite model inside the same isolated environment. Encrypt i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a sandboxed NotificationRouter in an isolated process that accepts userId and payload via AIDL, routes to a per-user NotificationChannel, and enqueues with NotificationCompat. Persist mapping",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an isolated service (android:process) hosting IChatStore. Use Room with tables Conversation, Message, Key. Encrypt messages with AES-256-GCM per conversation; wrap keys in Android Keystore (RSA",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Retrofit to fetch headlines from a public REST API, map to a NewsItem(id, title, summary, timestamp, source). Cache in Room with NewsDao (insertAll, getAll). UI built with Jetpack Compose LazyColu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests IPC, secure storage, and nuanced lifecycle in a real app.\n\n## Key Concepts\n- Android isolated service, android:process\n- AIDL IPC, binder surfaces\n- Room persistence for conversations and messages\n- Android Keystore, envelope encryption, per-conversation keys, key rotation\n- Offline queue with reliable sync and TLS\n\n## Code Example\n```javascript\n// Pseudo-code: data shapes\nconst Conversation = { id: \"\", participants: [] };\nconst Message = { id: \"\", convId: \"\", ciphertext: null, iv: null, status: \"\" };\n\n// Pseudo-AIDL-like surface\ninterface IChatStore {\n  void storeMessage(in Message msg);\n  Message readMessage(in String id);\n}\n```\n\n## Follow-up Questions\n- How would you test IPC performance under Doze?\n- How to handle key compromise and key rotation strategies?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-18",
        "questionId": "q-15127",
        "question": "**Context** android/general; **Difficulty** beginner; **Topics** android. Scenario: Build a beginner-friendly weather cache feature: fetch current weather from a public REST API, persist the latest result in a Room database encrypted with SQLCipher (Keystore-backed key), and refresh every 15 minutes via WorkManager with an unmetered Wi‑Fi constraint. Describe: 1) data model Weather and DAO with getLatest(city) and upsert(weather), 2) repository method refreshWeather() that fetches, encrypts, and stores, 3) simple UI flow to display cached data and trigger refresh, 4) a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a separate isolated process 'com.app.camera.filters', with AIDL interfaces IFrameSender and IFilter, and use Binder IPC for frame streaming. Encrypt buffers in-memory with KeyStore, and only p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a vault using Room with per-entry fields (website, username, password, notes) encrypted with AES-256-GCM; keys wrapped by a hardware-backed Keystore key in the isolated Vault process. IPC via AIDL with explicit i…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a Weather entity with city as primary key, tempC, description, timestamp. DAO: getLatest(city): Weather? and upsert(weather). Repository: refreshWeather() uses Retrofit to call the A",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Kotlin coroutines with a ViewModel. Wire the editor to a MutableStateFlow<String>, feed input via a TextWatcher, and collect text with debounce(500) in viewModelScope to call a small Repository.sa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical understanding of Android data layers, encryption, background work, and simple UI binding at a beginner level.\n\n## Key Concepts\n\n- Room with encryption (SQLCipher)\n- Android Keystore integration\n- Retrofit-based API calls\n- WorkManager with periodic work and network/charging constraints\n- Repository pattern and LiveData\n\n## Code Example\n\n```kotlin\n@Entity(tableName = \"weather\")\ndata class Weather(\n  @PrimaryKey val city: String,\n  val tempC: Float,\n  val description: String,\n  val timestamp: Long\n)\n\n@Dao\ninterface WeatherDao {\n  @Query(\"SELECT * FROM weather WHERE city = :city ORDER BY timestamp DESC LIMIT 1\")\n  fun getLatest(city: String): Weather?\n\n  @Insert(onConflict = OnConflictStrategy.REPLACE)\n  fun upsert(weather: Weather)\n}\n```\n\n## Follow-up Questions\n\n- How would you test the SQLCipher integration and ensure data at rest is inaccessible without the key?\n- What changes would you make to handle API failures or stale data gracefully?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-19",
        "questionId": "q-22564",
        "question": "Design a cross-process, on-device UI theming engine that loads user-managed themes as isolated modules. Each theme runs in its own Android process (android:isolatedProcess) and is rendered via a defined AIDL surface. Implement per-theme isolation, remote asset loading over TLS, per-theme keys in Keystore, memory budgets, IPC timeouts, and a policy to prevent access to sensitive data. Provide data models, IPC surface, lifecycle, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would run each theme renderer in its own isolated Android process (android:isolatedProcess), exposing an IThemeRenderer interface via AIDL. Each theme would return a ParcelFileDescriptor containing the rendered view b…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Proposed model: Note(id: String, text: String, tag: String, last_modified: Long). Use a Room DAO for upsert and search by tag, with a small repository. Schedule a WorkManager ExportWorker that runs wh",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Load a quantized on-device model, collect user deltas offline as next-token predictions, encode as gradient updates, encrypt deltas with per-device Android Keystore keys (signed with attestation), and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-user sandbox: each profile gets an isolated, encrypted Room database shard, keyed with Android Keystore material unique to that user. All access goes through a Binder/AIDL mediator running in a privilege…",
            "isCorrect": false
          }
        ],
        "explanation": "This question validates expertise in cross-process UI isolation, dynamic module loading, and secure asset handling at scale. Key concepts include implementing cross-process rendering using android:isolatedProcess and AIDL, managing dynamic feature modules with per-theme encryption keys in Keystore, enforcing memory budgets and IPC timeouts for system stability, and establishing UI lifecycle management with policy enforcement to prevent unauthorized data access.\n\nCode Example\n```java\n// IThemeRenderer AIDL interface\npublic interface IThemeRenderer extends IInterface {\n    ParcelFileDescriptor renderTheme(String themeId, int width, int height);\n    void setMemoryBudget(long bytes);\n    void setIpcTimeout(long timeoutMs);\n}\n```\n\nFollow-up Questions\n- How would you handle theme update propagation without restarting the host application?\n- What strategy would you use to invalidate cached theme bitmaps when system memory is low?\n- How would you implement a fallback mechanism if a theme process crashes repeatedly?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-20",
        "questionId": "q-26646",
        "question": "Design an isolated, cross-process note-sync feature for Android: UI communicates with a dedicated isolated service via AIDL to store user notes in an encrypted Room database, keys wrapped in Android Keystore, with per-note encryption and a simple forward-secret sync protocol over TLS. Include data model, IPC surface, lifecycle, and a robust test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use TensorFlow Lite with a manifest-driven model lifecycle; fetch updates via WorkManager under battery/network constraints; cache models in internal storage and swap without UI jank; prefer NNAPI del",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use MutableStateFlow<UiState> with sealed class (Loading/Success/Error) and collect with catch operator for error handling in coroutine scope.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a CRDT-based cart (OR-Set) in Room: CartItem with productId, qty, and per-item timestamp; tombstones for deletions. Generate delta patches for adds/updates/removals, merge server-side idempotently",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage an isolated service (android:isolatedProcess) that exposes AIDL for CRUD on a Room-backed, SQLCipher-encrypted notes DB. Store per-note keys wrapped by Android Keystore and derive data keys w",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design IPC across an isolated Android service, encryption, and robust testing.\n\n## Key Concepts\n- Android isolated processes\n- AIDL IPC\n- Android Keystore and per-note keys\n- Room encryption and data migrations\n- Test plan including crash recovery and offline scenarios\n\n## Code Example\n\n```javascript\n// Pseudo-code illustrating AIDL surface and data model\n```\n\n## Follow-up Questions\n\n- How would you implement per-note key rotation and key-wrapping in Keystore?\n- How do you handle offline conflict resolution and sync retry strategies?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-21",
        "questionId": "q-17131",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: Design an on-device, multi-user sandbox for a single app supporting multiple profiles on a shared device. Each user has an isolated encrypted Room shard; a system process mediates all access via a Binder/AIDL API. Include per-user privacy rules, data migration between users, and a fast foreground switch with minimal downtime. Provide data models, IPC, lifecycle, and a robust test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Data model: Note(id, title, body, createdAt). DAO: insert, update, delete, getAll, search(query) using LIKE. Repository exposes Flow<List<Note>> and a coroutine-based UI layer. WorkManager: PeriodicWo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use EncryptedSharedPreferences with a MasterKey from AndroidX Security. First, create the MasterKey with hardware-backed security: ```kotlin val masterKey = MasterKey.Builder(context) .setKeyScheme(MasterKey.KeyScheme.A…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-user sandbox: each profile gets an isolated, encrypted Room database shard, keyed with Android Keystore material unique to that user. All access goes through a Binder/AIDL mediator running in a privilege…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a host ProcessManager that binds to a dedicated isolated process per plugin. Define an AIDL IThemeProvider interface with methods such as getPalette(), getTypography(), and getShape(). Verify plugin signatures…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses multi-user data isolation, IPC design, and test planning in Android at scale. Candidates must demonstrate understanding of per-user encryption, lifecycle handling, and fast user switching with measurable performance budgets.\n\n## Key Concepts\n- Android multi-user data isolation architecture\n- Encrypted Room shards with Android Keystore-backed keys\n- AIDL/Binder IPC with per-user validation and context checking\n- User lifecycle management, state restoration, and data migration\n- Performance testing for user switch latency\n\n## Code Example\n```kotlin\n// Multi-user Room shard with per-user encryption\n@Entity(tableName = \"user_data\")\ndata class UserData(\n    @PrimaryKey val id: Long,\n    val userId: Int,\n    val payload: ByteArray\n)\n\nclass EncryptedUserShard(\n    private val userId: Int,\n    private val keyStore: AndroidKeyStore\n) {\n    private val keyAlias = \"user_shard_key_$userId\"\n    private val db: UserDatabase by lazy {\n        val key = getOrCreateUserKey(keyAlias)\n        Room.databaseBuilder(\n            appContext,\n            UserDatabase::class.java,\n            \"user_shard_$userId.db\"\n        )\n        .openHelperFactory(SQLCipherOpenHelperFactory(key))\n        .build()\n    }\n    \n    private fun getOrCreateUserKey(alias: String): ByteArray {\n        if (!keyStore.containsAlias(alias)) {\n            val keyGenerator = KeyGenerator.getInstance(\n                KeyProperties.KEY_ALGORITHM_AES, \"AndroidKeyStore\"\n            )\n            val spec = KeyGenParameterSpec.Builder(\n                alias,\n                KeyProperties.PURPOSE_ENCRYPT or KeyProperties.PURPOSE_DECRYPT\n            )\n            .setBlockModes(KeyProperties.BLOCK_MODE_GCM)\n            .setEncryptionPaddings(KeyProperties.ENCRYPTION_PADDING_NONE)\n            .setUserAuthenticationRequired(true)\n            .build()\n            keyGenerator.init(spec)\n            keyGenerator.generateKey()\n        }\n        return keyStore.getKey(alias, null).encoded\n    }\n}\n\n// Binder service with user context validation\ninterface IUserDataService {\n    fun getData(userId: Int, key: String): List<UserData>\n    fun setData(userId: Int, data: UserData)\n    fun migrateUserData(fromUserId: Int, toUserId: Int)\n    fun switchUser(targetUserId: Int): Boolean\n}\n\nclass UserDataService : IUserDataService {\n    private val userSessionCache = LruCache<Int, UserSession>(5)\n    \n    private fun validateUserContext(callingUid: Int, requestedUserId: Int): Boolean {\n        val context = ActivityManager.getService().getUserInfo(\n            UserHandle.getUserId(callingUid)\n        )\n        return context.id == requestedUserId\n    }\n    \n    override fun getData(userId: Int, key: String): List<UserData> {\n        if (!validateUserContext(Binder.getCallingUid(), userId)) {\n            throw SecurityException(\"User context mismatch\")\n        }\n        return getShard(userId).userDataDao().getByKey(key)\n    }\n    \n    override fun switchUser(targetUserId: Int): Boolean {\n        val startTime = SystemClock.elapsedRealtime()\n        // Pre-warm target user's shard\n        val session = userSessionCache.get(targetUserId) \n            ?: UserSession(targetUserId).also { userSessionCache.put(targetUserId, it) }\n        session.activate()\n        val switchTime = SystemClock.elapsedRealtime() - startTime\n        return switchTime < 100 // Enforce sub-100ms target\n    }\n}\n```\n\n## Data Migration Strategy\n1. Export source user's encrypted shard to intermediate storage\n2. Re-encrypt with target user's Keystore key\n3. Import into target user's shard\n4. Verify integrity via checksum\n5. Securely wipe source data\n\n## Test Plan\n| Test Case | Success Criteria |\n|-----------|------------------|\n| User isolation | Data from User A not accessible by User B |\n| Encryption verification | Shard unreadable without correct Keystore key |\n| User switch latency | Switch completes in <100ms |\n| Migration integrity | All data transferred with matching checksum |\n| Context spoofing | Binder call with wrong UID throws SecurityException |\n| Concurrent access | Race condition handling via database locks |",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-22",
        "questionId": "q-16579",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: An app loads third-party feature plugins at runtime from an internal catalog. Design a trusted plugin loader that runs in an isolated Android process (android:process and android:isolatedProcess) and validates plugins before loading: pin a root certificate in Android Keystore, verify plugin APK signatures, check plugin manifest (id, version, permissions), support offline verification when network is unavailable, implement an attestation token per plugin, and a lightweight audit log; describe data model, IPC via AIDL, plugin lifecycle, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an Android Kotlin module named Exercise Library that fetches /exercises via REST, caches results in Room, and displays a Compose list with search and a Favorite toggle. It must work offline",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a PluginRouter in the host app that auto-discovers plugins via PackageManager and binds to each plugin’s AIDL service, each running in an isolatedProcess boundary. Enforce per-plugin identit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Answer example: Use a central orchestrator that runs filters in isolated Android processes (android.isolatedProcess). IPC via AIDL with a FrameInOut surface. Exchange frames through encrypted ashmem m",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a PluginVerifier in an isolated Android process that loads plugins only after attestation. Pin a root certificate in Android Keystore and verify each plugin APK signature against it; parse t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes on-device supply-chain security for a plugin architecture, focusing on runtime attestation, isolation, and offline verification.\n\n## Key Concepts\n- android:isolatedProcess and AIDL IPC\n- Signature verification against a pinned root cert in Keystore\n- Manifest validation and per-plugin attestation tokens\n- Offline revocation lists and auditing\n\n## Code Example\n```javascript\n// AIDL-like surface (pseudo)\ninterface IAttestation {\n    AttestationToken attest(in PluginDescriptor descriptor);\n}\n```\n\n```javascript\n// Kotlin-like data model (illustrative)\ndata class PluginDescriptor(val id: String, val version: String, val permissions: List<String>)\n```\n\n## Follow-up Questions\n- How would you handle plugin revocation and key rotation without forcing app updates?\n- How would you scale attestation as the plugin catalog grows to thousands of plugins?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-23",
        "questionId": "q-6622",
        "question": "Context: android/general; Difficulty: beginner; Topics: android. Scenario: Build a lightweight location-based reminders module that works offline and syncs when online. Requirements: store reminders in Room with fields id, title, note, createdAt, latitude, longitude, radius, and synced flag; use WorkManager to sync only on Wi-Fi; implement a geofence so entering a radius triggers a local notification; describe data model, DAO, geofencing integration, sync constraints, and a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design the data model with Episode, TranscriptSegment, Highlight; audio stored on external storage; transcripts encrypted with AES-GCM via per-device key in Android Keystore; Transcript index using Ro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use DataStore Preferences. Define an integer key using `intPreferencesKey` with the name 'onboarding_step'. Create a DataStore instance using the preferences delegate: `val Context.dataStore: DataStore<Preferences> by p…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a Room table with id, timestamp, value, version, and isDeleted tombstone. Use a WorkManager worker to batch dirty rows, send delta patches with baseVersion, and let server resolve conflicts (s",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Room entity Reminder(id: String, title: String, note: String, createdAt: Long, lat: Double, lon: Double, radius: Float, synced: Boolean); DAO: insert, update, getAll, getPending; GeofencingClient setu",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical offline-first design with location triggers, common Android primitives (Room, WorkManager, Geofencing), and how to test them.\n\n## Key Concepts\n- Room data modeling\n- WorkManager constraints\n- Geofencing integration\n- Notification scheduling\n\n## Code Example\n```kotlin\n// skeleton outline for Reminder data class and DAO\n```\n\n## Follow-up Questions\n- How would you handle multiple geofences with overlapping regions?\n- What happens if the device reboots before a sync completes?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-24",
        "questionId": "q-29421",
        "question": "Implement a beginner Android login feature (Kotlin): a screen with username and password; a LoginViewModel exposes LiveData for username, password, and a computed isFormValid flag; enforce password strength (min 8 chars, at least one uppercase, one digit); outline UI binding and a minimal unit test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use MVVM with Room and DataStore. Visit entity(id, siteName, timestamp). DAO supports insert and query with sort order. Repository mediates. ViewModel exposes LiveData<List<Visit>> driven by a sortOrder preference from…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "CRDT AR graph: each stroke is a node anchored to an ARCore point with parent edges. Ops encoded in protobuf: op(ADD/MOVE/DELETE), strokeId, color, thickness, coords, parentId, deviceId, ts. Queue in R",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Kotlin to create a LoginViewModel that exposes LiveData for username, password, and a computed isFormValid flag. Implement a PasswordValidator to enforce password strength: minimum 8 characters, at least one upperca…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design an on-device tamper-evident audit log for a financial Android app: an append-only, encrypted Room store for actions; chunk entries into 64KB blocks and compute a Merkle root per block for integ",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical Android MVVM architecture, LiveData usage, input validation, and unit testing fundamentals—all core skills without requiring network or persistence complexity.\n\n## Key Concepts\n\n- **LiveData and ViewModel**: Lifecycle-aware state management\n- **MediatorLiveData**: Combining multiple sources for derived state\n- **Functional validation**: Password strength enforcement\n- **Unit testing**: Testable, isolated component verification\n\n## Follow-up Questions\n\n- How would you test isFormValid with unit tests?\n- How would you adapt this for Jetpack Compose?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-03T05:54:23.284Z",
    "version": 1
  }
]