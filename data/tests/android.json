[
  {
    "id": "test-android",
    "channelId": "android",
    "channelName": "Android",
    "title": "Android Knowledge Test",
    "description": "Test your understanding of Android with 25 questions.",
    "questions": [
      {
        "id": "android-q-0",
        "questionId": "q-1715",
        "question": "You're building an Android module to support background, continuous BLE logging from an OBD-II dongle for a fleet-tracking app. Requirements: maintain a stable BLE connection with automatic reconnect and backoff; buffer data locally in Room during disconnect; periodically upload batched logs via WorkManager with exponential backoff; use a ForegroundService while in the background; respect Doze/App Standby; outline data flow, architecture, and a minimal skeleton of the BLE manager, DAO, and Worker; include a test plan for connectivity changes and battery constraints?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ble path: a BleManager with a lifecycle-aware coroutine scope handles connect/disconnect and an exponential reconnect backoff. Logs stored as Room entities (LogEntry: id, ts, rssi, value, synced). A U",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design an ImageDecodingService that runs in an isolated Android process, exposed via AIDL. Avatars are downloaded, cached encrypted on disk with AES-GCM, keys wrapped in a per-user Keystore key (hardw",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use WorkManager: define a Reminder data class, a Worker that builds a NotificationCompat, enqueue a UniquePeriodicWork with 24h interval, and compute initialDelay to the next occurrence of hour:minute",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an on-device personalization engine for Android that trains a small local ML model in an isolated process to generate UI recommendations. Expose a strict AIDL IPC surface to the app, store inputs and models encry…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes robust BLE handling, offline buffering, and reliable background uploads in Android. It tests lifecycle, Doze awareness, and system-resilience trade-offs.\n\n## Key Concepts\n\n- BLE connection lifecycle and reconnection strategies\n- Room for local buffering and tombstone-ish semantics\n- WorkManager for batched uploads with exponential backoff\n- ForegroundService in background and Doze compatibility\n- Testing strategies for connectivity, power modes, and data integrity\n\n## Code Example\n\n```kotlin\nclass BleManager(context: Context) {\n    private val scope = CoroutineScope(Dispatchers.IO + SupervisorJob())\n    private var gatt: BluetoothGatt? = null\n\n    fun connect(device: BluetoothDevice) { /* initiate and monitor gatt; on failure schedule reconnect */ }\n    fun disconnect() { /* clean up gatt */ }\n    private fun scheduleReconnect(delayMs: Long) { /* ... */ }\n}\n```\n\n```kotlin\n@Entity\ndata class LogEntry(\n  @PrimaryKey val id: String,\n  val ts: Long,\n  val rssi: Int,\n  val value: String,\n  val synced: Boolean\n)\n```\n\n```kotlin\nclass UploadWorker(appContext: Context, params: WorkerParameters) : CoroutineWorker(appContext, params) {\n    override suspend fun doWork(): Result {\n        // fetch unsynced logs, batch and POST, mark as synced on success, retry on failure\n        return Result.success()\n    }\n}\n```\n\n## Follow-up Questions\n\n- How would you test this with simulated BLE disconnects and network outages in CI?\n- How would you handle data schema evolution for LogEntry without breaking older devices?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-1",
        "questionId": "q-29700",
        "question": "Build a robust cross-feature module integrity system for Android apps using dynamic feature modules loaded at runtime. Implement a module attestation surface that verifies each module’s signature against a per-installation attestation token from Play Integrity, and rejects tampered or revoked modules by validating an embedded manifest hash in memory before execution. Provide IPC (AIDL) surface, data model, verification workflow, and a concrete test plan (offline tampering, online revocation)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a single Room database with a tenantId column and per-tenant encryption keys stored in Keystore; derive keys via a per-tenant alias; encrypt payloads before storage; use WorkManager to batch and r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an isolated bound service (android:process + android:isolatedProcess) exposing an AIDL surface for a background image downloader. It must support resumable downloads, per-user token rotation",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To implement a robust module integrity system, obtain a per-installation attestation token from Play Integrity. Then verify each dynamic feature module's signature against this attestation token and validate an embedded…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Session state is persisted locally in a Room database encrypted with a per-device AES-256 key derived from Android Keystore. Synchronization uses a CRDT-inspired OR-Set (Observed-Remove Set) structure to handle concurre…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates a candidate's ability to design secure, real-world Android applications using dynamic feature modules with runtime verification and robust IPC mechanisms.\n\n## Key Concepts\n\n- **Play Integrity API**: Provides per-installation attestation to detect tampering and unauthorized environments\n- **Dynamic Feature Modules**: Feature modules loaded at runtime via SplitInstallManager\n- **Module Signing & Manifest Hash Validation**: Ensures module integrity by verifying signatures against embedded hashes\n- **AIDL IPC**: Defines the inter-process communication surface for module loading and verification\n- **Testing Strategies**: Offline tampering tests and online revocation validation\n\n## Code Example\n\n```java\n// AIDL interface for module verification\ninterface IModuleVerifier {\n    boolean verifyModule(byte[] manifestHash, byte[] signature);\n    ModuleInfo getModuleInfo(String moduleName);\n    boolean isModuleRevoked(String moduleName);\n}\n```\n\n## Follow-up Questions\n\n- How would you handle edge cases where the Play Integrity API is unavailable?\n- What strategies would you use to protect the attestation token from extraction and replay attacks?\n- How do you ensure security when modules are loaded across different app processes?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-2",
        "questionId": "q-25303",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build a secure on-device content moderation pipeline that processes user-generated videos in an isolated Android service. It must accept video chunks via AIDL, perform on-device face detection and redaction with a lightweight model, and store redacted outputs encrypted at rest in Android Keystore-backed storage. Expose a read-only IPC surface for the UI to monitor progress and retrieve results. Provide data model, IPC surface, lifecycle, and a robust test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a Room Note entity with id as the primary key, a DAO with an upsert method, a Retrofit NotesService to fetch notes, and a Repository that maps network models to database entities. Schedule a PeriodicWorkRequest w…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a privacy-first Android module for group audio chat (3+ participants) with E2EE: per-device ephemeral keys rotated nightly in Android Keystore, a group-key envelope, and WebRTC for low-latency",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Room-based append-only ChangeLog with the following fields: change_id (UUID), entity_id, entity_type, operation, timestamp, device_id, and payload. On every mutation, write a corresponding ChangeLog entry. A…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an isolated service (android:process + android:isolatedProcess) with an AIDL interface IModeration. Data model: VideoChunk {id, ownerId, srcPath, status, resultPath, keyId}. Encrypt outputs wit",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to architect a secure, isolated video moderation workflow with IPC, encrypted storage, per-user cryptography, and a lifecycle-resilient design. It also probes practical ML integration and end-to-end testability.\n\n## Key Concepts\n\n- Isolated process and AIDL IPC\n- Per-user Android Keystore keys and EncryptedFile storage\n- Lightweight on-device ML for face redaction\n- Data model and state management for streaming chunks\n- Lifecycle resilience, crash recovery, and robust test strategy\n\n## Code Example\n\n```kotlin\n// Skeleton data model\ndata class VideoChunk(\n  val id: String,\n  val ownerId: String,\n  val srcPath: String,\n  val status: String,\n  val resultPath: String,\n  val keyId: String\n)\n```\n\n## Follow-up Questions\n\n- How would you test privacy guarantees and key rotation in production-like devices?\n- How would you handle model updates and rollback without data loss?\n- What instrumentation would you add to monitor IPC latency and throughput?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-3",
        "questionId": "q-1880",
        "question": "You’re building an Android app that streams live telemetry video from a drone's camera over an unstable network. Using CameraX, encoding with H.264, and a hybrid UDP/TCP transport, design a solution that includes buffering and backpressure, a foreground service with Doze-friendly behavior, adaptive bitrate, and robust error handling. Provide data flow, key classes (StreamingManager, PacketSender), and a skeleton implementation plus an end-to-end test plan for packet loss and latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement offline-first messaging using Room encrypted with SQLCipher; store per-user master key in Android Keystore and rotate by re-wrapping the DB key and re-encrypting affected rows. Build a WorkM",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a bounded circular frame buffer, encode frames with MediaCodec to H.264, stream data via UDP with FEC and a TCP channel for control, and use a ForegroundService with WakeLock to persist in D",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a dedicated content moderation service running in an isolated process (android:process=\"com.app.moderator\") exposed through AIDL IPC. Define ImageData as a parcelable container for image metadata and byte arra…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Retrofit to fetch Article DTOs and map them to Room entities for local caching. The UI observes a Flow<List<Article>> from the DAO, loading cached data on startup and triggering a network refresh when connectivity i…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design resilient media streaming over unreliable networks on Android, balancing power, latency, and data integrity.\n\n## Key Concepts\n- CameraX and MediaCodec for H.264\n- UDP/TCP hybrid transport with backpressure\n- ForegroundService, WakeLock, Doze compatibility\n- Adaptive bitrate using RTT and packet loss\n- Testing for jitter, drops, and backoffs\n\n## Code Example\n```javascript\nclass StreamingManager {\n  constructor(bufferCapacity = 1024) {}\n  start() {}\n  pushFrame(frame) {}\n}\nclass PacketSender {\n  constructor() {}\n  sendFrame(frame) {}\n}\n```\n\n## Follow-up Questions\n- How would you measure and log end-to-end latency in production?\n- What failure modes would you test (outages, jitter, reordering) and how would you simulate them?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-4",
        "questionId": "q-6991",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build an offline-first article reader module that fetches per-user article manifests from a GraphQL backend, caches content in Room encrypted with Android Keystore, and renders with Jetpack Compose. Implement local edits (highlights and notes) and allow offline-first reading across devices. Explain data model, synchronization strategy, encryption, and a test plan that covers cross-device conflicts and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a Room model with Article(id, title, contentUrl, manifestVersion) and a UserEdit(entity for highlights/notes) encrypted via Android Keystore. Fetch manifests via GraphQL, render content with J",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ble path: a BleManager with a lifecycle-aware coroutine scope handles connect/disconnect and an exponential reconnect backoff. Logs stored as Room entities (LogEntry: id, ts, rssi, value, synced). A U",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would run each theme renderer in its own isolated Android process (android:isolatedProcess), exposing an IThemeRenderer interface via AIDL. Each theme would return a ParcelFileDescriptor containing the rendered view b…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a foreground WebSocket service using OkHttp to receive live prices, store last 100 prices in an encrypted Room database (SQLCipher) keyed from Android Keystore, expose data via Kotlin Flow f",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design an offline-first data flow, encryption, and conflict handling for a real Android app.\n\n## Key Concepts\n\n- Offline-first data modeling and synchronization\n- Per-device encryption with Android Keystore\n- WorkManager-based background sync\n- Per-field conflict resolution (last-write-wins)\n\n## Code Example\n\n```javascript\n// Pseudocode: last-write-wins merge for edits\nfunction mergeEdits(local, remote) {\n  const out = { ...remote };\n  for (const key of Object.keys(local)) {\n    const l = local[key];\n    const r = remote[key];\n    out[key] = (l?.ts > r?.ts) ? l.value : r.value;\n  }\n  return out;\n}\n```\n\n## Follow-up Questions\n\n- How would you handle key rotation without data loss?\n- What telemetry would you add to verify sync correctness in CI?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-5",
        "questionId": "q-16318",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: Design an offline DRM audio cache for a streaming app. Encrypt audio chunks on disk, issue per-song licenses, and decrypt on-the-fly in a dedicated playback service using Android Keystore and MediaDrm/MediaCrypto. Integrate with ExoPlayer via a custom DataSource; support license revocation, secure key wipe, and seamless offline playback. Provide data model, flow, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an offline DRM audio cache system with the following components: **Data Model:** - `License`: songId, licenseKeyRef (Keystore alias), expirationTimestamp, revocationVersion - `EncryptedChunk`: chunkId, songId, en…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement an offline-first order drafting and submission module using a secure, layered architecture. Leverage Room database encrypted with SQLCipher or Jetpack Security for persistent storage, while storing ECC key pai…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Load items.json from assets on a background thread, parse with kotlinx.serialization into a data class Item(name, code). Bind to a RecyclerView with a simple Adapter/ViewHolder. Add an EditText with a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would create a FeatureFlagEngine hosted in an isolated Android service (android:process/isolatedProcess). Flags live in an encrypted Room table, using Keystore-backed AES keys. Expose getFlag, setOv",
            "isCorrect": false
          }
        ],
        "explanation": "## Overview\n\nThis question evaluates your ability to design secure, offline DRM systems with proper key management and seamless integration with media playback pipelines.\n\n## Key Concepts\n\n- **Android Keystore**: Hardware-backed security module for cryptographic key storage\n- **MediaDrm/MediaCrypto**: Android's standard DRM framework for content decryption\n- **ExoPlayer Custom DataSource**: Integration point for feeding decrypted content to the playback pipeline\n- **Per-Song Licensing**: Unique keys per content item for granular access control\n- **License Revocation**: Mechanism to invalidate previously granted licenses\n- **Secure Key Wipe**: Cryptographic erasure of keys when access is revoked\n\n## Implementation Architecture\n\n### Core Components\n\n```kotlin\n// License entity representing per-song access rights\ndata class License(\n    val songId: String,\n    val licenseKeyRef: String,  // Keystore alias\n    val expirationTimestamp: Long,\n    val revocationVersion: Int\n)\n\n// Encrypted audio chunk storage model\ndata class EncryptedChunk(\n    val chunkId: String,\n    val songId: String,\n    val encryptedData: ByteArray,\n    val iv: ByteArray,\n    val chunkIndex: Int\n)\n\n// Playback session tracking\ndata class PlaybackSession(\n    val sessionId: String,\n    val songId: String,\n    val licenseRef: String,\n    val currentPosition: Long,\n    val lastAccessed: Long\n)\n```\n\n### DRM DataSource Implementation\n\n```kotlin\nclass DrmDataSource(\n    private val context: Context,\n    private val licenseManager: LicenseManager,\n    private val chunkStorage: ChunkStorage\n) : BaseDataSource() {\n\n    private var currentSongId: String? = null\n    private var mediaCrypto: MediaCrypto? = null\n    private var decryptionSession: DrmSession? = null\n\n    override fun open(dataSpec: DataSpec) {\n        currentSongId = dataSpec.uri.lastPathSegment\n        initializeDrmSession(currentSongId!!)\n    }\n\n    private fun initializeDrmSession(songId: String) {\n        val license = licenseManager.getLicense(songId)\n            ?: throw SecurityException(\"No valid license for: $songId\")\n        \n        if (licenseManager.isRevoked(license)) {\n            secureWipe(license)\n            throw SecurityException(\"License revoked for: $songId\")\n        }\n        \n        val drmInitData = licenseManager.getDrmInitData(songId)\n        mediaCrypto = MediaCrypto(drmInitData.uuid, drmInitData.data)\n        decryptionSession = DrmSessionManager\n            .newDrmSessionManager(drmInitData.uuid, mediaCrypto!!)\n    }\n\n    override fun read(buffer: ByteArray, offset: Int, length: Int): Int {\n        val songId = currentSongId ?: return -1\n        val chunk = chunkStorage.readChunk(songId, currentPosition)\n        val decrypted = decryptChunk(chunk)\n        System.arraycopy(decrypted, 0, buffer, offset, length)\n        return length\n    }\n\n    private fun decryptChunk(chunk: EncryptedChunk): ByteArray {\n        val cipher = Cipher.getInstance(\"AES/GCM/NoPadding\")\n        val key = KeyStoreManager.getKey(chunk.songId)\n        cipher.init(Cipher.DECRYPT_MODE, key, GCMParameterSpec(128, chunk.iv))\n        return cipher.doFinal(chunk.encryptedData)\n    }\n\n    private fun secureWipe(license: License) {\n        KeyStoreManager.deleteKey(license.licenseKeyRef)\n    }\n}\n```\n\n## Test Strategy\n\n| Test Category | Description |\n|--------------|-------------|\n| Unit Tests | Key generation, encryption/decryption, license validation |\n| Integration Tests | Offline download → playback flow, position resume |\n| Security Tests | Key extraction attempts, revocation propagation, secure wipe |\n| Performance Tests | Decryption latency, chunk buffer management |\n| UX Tests | Network loss during download, license expiration prompts |\n\n## Common Pitfalls\n\n1. **Storing keys in software**: Always use hardware-backed Keystore\n2. **Single key for all songs**: Implement per-song licensing for granular revocation\n3. **In-memory plaintext caching**: Minimize decrypted data lifespan in memory\n4. **Ignoring revocation**: Check revocation status on every playback attempt",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-6",
        "questionId": "q-30091",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: Design a privacy-preserving on-device feature flag registry that executes in a sandboxed Android process. Expose an AIDL interface Engine { evaluate(FlagQuery) returns FlagResult; }. Plugins implement evaluation logic and are loaded as isolated modules with signature verification via Android Keystore (hardware-backed where available). How would you ensure data minimization, offline fallback, deterministic replay for QA, and provide a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a host process delegating evaluation to a sandboxed EngineService via AIDL. Define Parcelable FlagQuery and FlagResult; load signed PluginEngine modules with verification against Android Keystore (hardware-backe…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a ViewModel exposing a Flow<String> for the query, debounce 250ms, and map to a filtered list. Use a Repository with a static movie list, filter by contains(query, ignoreCase=true). Collect in",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would design an Android module featuring a Room-backed audit log for comprehensive sync event tracking and policy violation documentation. WorkManager would orchestrate constrained background synchronization requiring…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Define Note entity with id, title, content, createdAt. DAO: List<Note> getAllNotes(). Repository.exportNotes(context): create jsonFile in getExternalFilesDir(null) as notes.json, write with Gson, obta",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question evaluates cross-process isolation, plugin governance, and secure configuration evaluation while keeping user data on-device.\n\n## Key Concepts\n- Android isolated processes and AIDL IPC\n- Dynamic plugins with manifest and signature verification\n- Android Keystore (hardware-backed)\n- Data minimization and offline-first design\n- Deterministic replay for QA and security review\n\n## Code Example\n```aidl\n// IFlagEngine.aidl\ninterface IFlagEngine {\n  FlagResult evaluate(in FlagQuery query);\n}\n```\n\n```aidl\n// FlagQuery.parcelable\nparcelable FlagQuery {\n  int requestId;\n  String flagKey;\n  long hashedUserId;\n}\n```\n\n```aidl\n// FlagResult.parcelable\nparcelable FlagResult {\n  int requestId;\n  boolean enabled;\n  String variant;\n  long evaluatedAt;\n}\n```\n\n## Architecture Overview\n\n**Sandboxed EngineService** runs as an isolated Android process with no permissions. The host app communicates strictly through AIDL. Plugin modules are APK files signed with a known key; before loading, the system verifies the signature against a certificate stored in Android Keystore. This prevents unauthorized code execution and ensures plugin integrity.\n\n**Data Minimization** is enforced by restricting data flow to only what the evaluation requires: the flag key and a one-way hash of the user identifier. Raw user data never enters the sandbox.\n\n**Offline Fallback** leverages an encrypted SQLite database within the sandbox, updated on successful network fetches. A TTL value determines staleness; when offline, the engine returns the cached result with a staleness indicator.\n\n**Deterministic Replay** is supported by accepting an optional seed parameter in FlagQuery. The plugin uses a seeded PRNG, making evaluation outcomes reproducible for automated testing and security audits.\n\n## Test Plan\n\n| Scenario | Expected Behavior |\n|---|---|\n| Load unsigned plugin | Rejected with SecurityException |\n| Load plugin signed with unknown key | Rejected; logged to audit store |\n| Valid plugin, online | Fetch latest config; cache result |\n| Valid plugin, offline (fresh cache) | Return cached result |\n| Valid plugin, offline (stale cache) | Return cached result with staleness flag |\n| Replay with same seed | Identical FlagResult across multiple invocations |\n| Host process crash | EngineService restarts independently; no data leak |\n| Corrupted cache | Engine falls back to default-off policy |",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-7",
        "questionId": "q-19380",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: You need to implement a privacy-first event processing pipeline that runs entirely on-device, with a separate isolated process to perform data aggregation on user interaction events. The app collects events from the UI, ships to the isolated process via a defined AIDL interface, stores raw events encrypted in a Room database, and periodically archives batches to a cloud backend only when the device is plugged in and on Wi-Fi. Outline the IPC surface, data models, encryption strategy (Keystore), process lifecycle, and a concrete test plan including security and performance tests. Provide trade-offs and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Model EpisodeManifest, EpisodeAudioBlob, and PlaybackState keyed by episodeId and deviceId. Encrypt audio blobs with AES-GCM via Android Keystore; save to external storage in per-episode folders. Implement resumable chu…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Store sensor samples in Room with fields: id, ts, accX/accY/accZ, gyrX/gyrY/gyrZ, lat, lon, deviceHash. Encrypt payload with a nightly-rotated Android Keystore key; re-encrypt batches using new keys.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a host ProcessManager that binds to a dedicated isolated process per plugin. Define an AIDL IThemeProvider interface with methods such as getPalette(), getTypography(), and getShape(). Verify plugin signatures…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect an AIDL surface IEventAggregator with submitEvent(Event), fetchBatch(), and rotateKeys(). Run the processor in an android:isolatedProcess, store raw events encrypted in Room using a Keystore",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests on-device privacy, cross-process data handling, and production-grade IPC boundaries under real-world constraints.\n\n## Key Concepts\n\n- Android isolated processes\n- AIDL IPC and surface design\n- Keystore-based encryption and key rotation\n- WorkManager scheduling with network constraints\n\n## Code Example\n\n```javascript\n// Pseudo AIDL surface\ninterface IEventAggregator {\n  void submitEvent(in Event e);\n  List<EventBatch> fetchBatch();\n  void rotateKeys();\n}\n```\n\n## Follow-up Questions\n\n- How would you test IPC boundary violations without leaking data?\n- What failure modes exist if the isolated process crashes mid-rotation?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-8",
        "questionId": "q-24384",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build an offline-first, encrypted messaging module for Android that must survive process death and network variability. Use Room (encrypted with SQLCipher) for local storage, Android Keystore for keys, WorkManager for periodic sync, and a foreground service for real-time delivery. Outline data models, encryption strategy, conflict resolution, migration plan, and a practical test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To implement, use FusedLocationProvider with a foreground service for geofence checks to minimize GPS use; store Geofence and LocationEvent in Room encrypted via Android Keystore; batch uploads with W",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an offline-first messaging module using Room encrypted with SQLCipher, with Conversation and Message entities and Keystore-backed keys. Use WorkManager for periodic sync with network constra",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement an isolated service that subscribes to per-symbol streams via AIDL, decrypts protobuf payloads with Keystore keys, verifies signatures, applies backpressure with a bounded buffer, and pushes",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a Weather entity with city as primary key, tempC, description, timestamp. DAO: getLatest(city): Weather? and upsert(weather). Repository: refreshWeather() uses Retrofit to call the A",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical design of offline-first data sync with encryption, multi-process resilience, and test planning. Evaluates mastery of Room + SQLCipher, Keystore-based key management, WorkManager constraints, and a foreground service for real-time delivery.\n\n## Key Concepts\n\n- Room with SQLCipher encryption\n- Android Keystore for key material\n- WorkManager for constrained background sync\n- Foreground service for real-time delivery\n- Conflict resolution via versioning and tombstones\n- Data migrations with offline data\n\n## Code Example\n\n```javascript\n// Example Room entity definitions (pseudo-Kotlin in JS block for formatting)\n@Entity(tableName = \"messages\")\ndata class MessageEntity(\n  @PrimaryKey val id: String,\n  val conversationId: String,\n  val text: String,\n  val timestamp: Long,\n  val remoteVersion: Long,\n  val delivered: Boolean\n)\n```\n\n## Follow-up Questions\n\n- How would you migrate the database when schema changes while users are offline?\n- How would you test encryption key rotation without data loss?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-9",
        "questionId": "q-10341",
        "question": "Design a pluggable plugin architecture for an Android app where plugins are delivered as dynamic feature modules from a private repository. Build a secure host that loads plugins in an isolated process, verifies digital signatures, enforces per-plugin permissions, and stores per-plugin data encrypted with Android Keystore. Include data model for plugin metadata, a safe loading protocol, an offline-first revocation mechanism, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a PluginHost that loads plugins from a signed store, spins a distinct Service per plugin in an isolatedProcess, and communicates via IPluginAidl. Verify signatures with PackageManager, restr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Model a Note with id, title, encryptedContent, timestamp. Encrypt content with AES-GCM using a Keystore-stored key; decrypt for display. Repository handles API, local cache, and crypto. Schedule a Wor",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a host using SplitInstallManager with a dedicated sandboxed process, pin a public key and verify module signatures before installation, enforce permissions via a PluginSandbox, and store per-plugin dat…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a BridgeService interface (AIDL) for per-plugin requests; each plugin runs in its own isolatedProcess with a unique UID; dictionaries stored in an encrypted Room (SQLCipher); per-plugin keys st",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests on-device modular architectures, security, and offline reliability for enterprise apps. It probes practical decisions around plugin delivery, isolation, and revocation beyond basic app design.\n\n## Key Concepts\n- Dynamic feature modules\n- Sandboxed plugin execution\n- Signature pinning with a private repository\n- Android Keystore encrypted storage per plugin\n- Offline revocation policy and auditing\n\n## Code Example\n```kotlin\n// Example: signature verification skeleton\nfun verifySignature(data: ByteArray, signature: ByteArray, publicKey: PublicKey): Boolean {\n  val sig = Signature.getInstance(\"SHA256withRSA\")\n  sig.initVerify(publicKey)\n  sig.update(data)\n  return sig.verify(signature)\n}\n```\n\n## Test Plan\n1. Load valid/invalid signed modules\n2. Test permission enforcement and sandbox isolation\n3. Verify encrypted data storage and retrieval\n4. Test offline revocation scenarios\n5. Performance and memory leak validation",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-10",
        "questionId": "q-6159",
        "question": "Context: android general; Difficulty: advanced; Topics: android. Scenario: Design a modular Android feature-flag and experiment-tracking module that operates offline-first, merges updates using a CRDT-style delta graph, stores flags and experiment state in Room with per-device encryption via Android Keystore, and reconciles with a central GraphQL backend. Describe data model, merge rules, encryption strategy, and a concrete test plan for cross-device updates, rollback, and audit trails?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a local Flag entity with id, value, and a per-device vector clock. Use a Delta table to store offline edits; merge updates by causality on reconnect, ensuring idempotent application. Encrypt",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Store sensor samples in Room with fields: id, ts, accX/accY/accZ, gyrX/gyrY/gyrZ, lat, lon, deviceHash. Encrypt payload with a nightly-rotated Android Keystore key; re-encrypt batches using new keys.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: load each AR stage as a Google Play Core dynamic delivery module in isolated processes. An Orchestrator wires frames from CameraX through a FrameProcessor AIDL API. Modules have independent vers",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a host that loads modules as separate processes via dynamic features; each module exposes an AIDL surface and is signed/validated before loading. Run modules in isolated sandboxes with per-m",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design offline-first feature flags with deterministic merges and secure storage.\n\n## Key Concepts\n\n- Offline-first data sync\n\n- CRDT-like delta graph\n\n- Room persistence and Android Keystore\n\n- GraphQL reconciliation and audit trails\n\n## Code Example\n\n```kotlin\ndata class Flag(val id: String, val value: String, val vc: Map<String, Int>)\n```\n\n## Follow-up Questions\n\n- How to scale vector clocks across millions of devices?\n\n- How to handle schema migrations without downtime?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-11",
        "questionId": "q-17822",
        "question": "Context: android/general; Difficulty: advanced; Topics: android. Scenario: Build an on-device privacy-preserving assistant that handles user queries and weather lookups entirely offline unless the user explicitly grants per-session online access. Implement a sandboxed service (android:process and android:isolatedProcess) with a bound AIDL interface, and a per-session capability token that gates network calls. Describe data flow, encryption choices, IPC surface, and a test plan for privacy, performance, and rollback/fallback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use NavHost with nested NavGraphs, HiltViewModel for scoped VMs, SavedStateHandle for deep link parameters, and rememberSaveable for state preservation across configuration changes.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a sandboxed service in an isolated process exposing an AIDL bound interface to the client app. Gate any network requests with a per-session token issued by a secure UI flow; store conversations an",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design: Room database encrypted with SQLCipher, per-user envelope keys in Android Keystore, data keys wrapped and rotated. Use WorkManager to batch-sync when network is available, running a foreground",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a CRDT-based approach (state-based OR-Set + MVRegister) to model per-entity state. Each device signs ops with a per-user key in Keystore; store encrypted deltas locally in Room; sync via WebSo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design strict boundary isolation, per-session permissioning, and on-device privacy controls for real-world assistants.\n\n## Key Concepts\n- Android isolated processes and AIDL surfaces\n- Per-session capability tokens gating network access\n- On-device NLP cache with SQLCipher; Keystore-backed keys\n- Privacy-first data flow and rate/permission governance\n\n## Code Example\n```java\n// Bound service interface sketch (AIDL-like)\ninterface IAssistant {\n  String query(String text, String sessionToken);\n  void grantOnlineAccess(String sessionId);\n}\n```\n\n## Follow-up Questions\n- How would you test token revocation and immediate offline fallback?\n- What metrics and instrumentation would you add to monitor privacy violations in production?",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-12",
        "questionId": "q-13052",
        "question": "Build a secure Android plugin runner that loads UI plugins packaged as dynamic feature modules into an isolated process (android:isolatedProcess). Each plugin must expose a UI surface via AIDL (IPlugin with render(IHost host)). Provide manifest schema, versioning, provisioning with Play Integrity and Keystore, sandboxed lifecycle, and rollback strategy. Include IPC contracts, a minimal host API, and a test harness that validates isolation, latency, and upgrade/downgrade paths?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a CRDT-augmented manifest: a tree of nodes (id, type, props, children) where UI order is a sequence CRDT (RGA). Each device stores a per-device signed delta in Room, encrypted with Android Keystor",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-utterance isolated NLU: orchestrator spins up a dedicated android:process isolated service for each request; IPC via AIDL; audio streamed via Binder; a compact quantized NLU model runs in the isol",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Insert a pending message into Room with a client-generated id, then enqueue a OneTimeWorkRequest with network constraint and exponential backoff. In the Worker, fetch unsent messages, POST to the serv",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Spawn an android:isolatedProcess service to load dynamic feature module plugins. Each plugin exposes a render(IHost) method via AIDL; host injects a minimal UI surface. Use a versioned manifest and in",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a secure, isolated plugin system with clear IPC contracts and robust lifecycle.\n\n## Key Concepts\n- android:isolatedProcess and sandboxing\n- AIDL IPC surfaces and lifecycle\n- Dynamic feature modules and versioned manifests\n- Security: Play Integrity, Android Keystore\n- Rollback and compatibility checks\n- Test strategy for isolation and latency\n\n## Code Example\n```java\n// AIDL interface example\ninterface IPlugin {\n  void render(in IHost host);\n}\n```\n\n## Follow-up Questions\n- How would you handle plugin permissions and data leakage risk across plugins?\n- How would you simulate network-restricted environments in tests to ensure rollback works?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-13",
        "questionId": "q-21503",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Design a low-power background data sync service that runs in an isolated process (android:process + android:isolatedProcess) with IPC via AIDL. It must batch requests by dataType per user, respect Doze, deduplicate concurrent client requests, and encrypt payloads at rest using Android Keystore keys. Provide data model, IPC surface, sync lifecycle, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a SyncManager that runs in an isolated Android process and exposes an AIDL surface to clients. Use WorkManager with Doze-friendly constraints; batch requests by dataType per user, persisting a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Room for local storage with Note(id: String, title: String, content: String, modifiedAt: Long, synced: Boolean). DAO: insert/update/markSynced, queryUnsynced. Repository coordinates DB and a Retro",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a separate isolated process 'com.app.camera.filters', with AIDL interfaces IFrameSender and IFilter, and use Binder IPC for frame streaming. Encrypt buffers in-memory with KeyStore, and only p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use EncryptedSharedPreferences with a MasterKey from AndroidX Security. First, create the MasterKey with hardware-backed security: ```kotlin val masterKey = MasterKey.Builder(context) .setKeyScheme(MasterKey.KeyScheme.A…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross‑process design, privacy, and power efficiency for real‑world sync.\n\n## Key Concepts\n- Android isolated processes, AIDL IPC\n- Doze/WorkManager backoff and batching\n- Data persistence with Room; per‑user queues\n- Android Keystore AES-GCM encryption\n\n## Code Example\n```javascript\n// Pseudo-AIDL surface example (shown for illustration; actual file is .aidl)\ninterface IBackgroundSync {\n  void requestSync(in SyncRequest req);\n}\n```\n\n## Follow-up Questions\n- How would you test Doze edge cases and backoff jitter?\n- How would you audit and monitor the queue under churn?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-14",
        "questionId": "q-10096",
        "question": "Design a beginner-friendly Android module: a two-pane master-detail UI in Jetpack Compose that shows a static list of notes. On phones, present as a single list; on tablets, show list and detail side-by-side. Use a ViewModel with SavedStateHandle to preserve the selected note across rotations, and implement accessible semantics (ContentDescription, TalkBack). Provide the data model, layout approach, and a minimal test plan to verify layout changes and state restoration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use SplitInstallManager to fetch the dynamic feature module on demand. Declare a dedicated process `:secureSticker` in the manifest for isolation. Expose an `IStickerHost` AIDL interface for secure communication between…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To implement a robust module integrity system, obtain a per-installation attestation token from Play Integrity. Then verify each dynamic feature module's signature against this attestation token and validate an embedded…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Room-based append-only ChangeLog with the following fields: change_id (UUID), entity_id, entity_type, operation, timestamp, device_id, and payload. On every mutation, write a corresponding ChangeLog entry. A…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a data class Note(id, title, body). Implement a simple Repository that returns a static list. In Compose, detect window size and render a two-pane Row for tablets or a single-column LazyColumn for",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to implement responsive layouts, state persistence, and accessibility in a beginner-friendly Android module.\n\n## Key Concepts\n- Jetpack Compose responsive layouts\n- ViewModel + SavedStateHandle\n- Window size classes / configuration\n- Accessibility semantics\n- Basic UI testing\n\n## Code Example\n```kotlin\ndata class Note(val id: String, val title: String, val body: String)\n\nclass NotesViewModel(savedStateHandle: SavedStateHandle) : ViewModel() {\n  val notes = listOf(\n    Note(\"1\", \"First\", \"Hello\"),\n    Note(\"2\", \"Second\", \"World\")\n  )\n  private val _selected = MutableStateFlow<Note?>(notes.first())\n  val selected: StateFlow<Note?> = _selected.asStateFlow()\n  fun select(note: Note) { _selected.value = note }\n}\n```\n\n## Follow-up Questions\n- How would you adapt to dynamic data and handle state restoration after process death?\n- How would you add unit tests for layout selection across small/large screens?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-15",
        "questionId": "q-11621",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build an offline-first, multi-tenant field survey module that uses per-tenant encryption keys in Android Keystore, supports on-device tenant switching, and delta-based per-field sync to a REST backend. Model Survey/Section/Field/Answer with per-field revisions, implement per-field conflict resolution prompts, and run battery-conscious background sync with WorkManager. Explain your design decisions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would model Tenant, Survey, Section, Field, and Answer with per-field revisions. Keys live in Android Keystore and encrypt sensitive answers in Room (SQLCipher or on-disk crypto). Delta sync runs at",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a playback orchestrator that uses a LocalMeshController (Nearby Connections) for synchronized audio, with ExoPlayer as the playback engine and a Room queue for offline buffering. Sign playhe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use EncryptedSharedPreferences to persist theme (dark/light) and fontSize. Create a companion crypto config with MasterKey from Jetpack Security. Implement a simple SettingsRepository that reads/write",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Retrofit to fetch JSON from the API, map to Kotlin data classes, and persist into Room with a ProductEntity. Expose a Flow or LiveData from a Repository via a ViewModel, consumed by a Fragment wit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design multi-tenant data with per-field granularity, secure local storage, and battery-conscious syncing.\n\n## Key Concepts\n\n- Data modeling: Tenant, Survey, Section, Field, Answer; per-field versioning\n- Security: Android Keystore keys, field-level encryption\n- Sync: Field-level deltas, conflict resolution prompts, optimistic merges\n- Background work: WorkManager with battery/network constraints\n\n## Code Example\n\n```kotlin\n@Entity\ndata class Field(val id: String, val valueEnc: ByteArray, val revision: Long)\n```\n\n## Follow-up Questions\n\n- How would you handle key rotation for tenants without data re-encryption on-device?\n- How would you test conflict resolution across network outages?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-16",
        "questionId": "q-15843",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build a DAG-based background work orchestrator for Android that accepts a JSON DAG definition (nodes: id, worker class, constraints, retry policy) and executes tasks via WorkManager(beginWith/then). Ensure idempotent execution, data passing via Data, and robust cancellation/backoff. Provide a concrete DAG example: fetchMetadata -> downloadPayload -> decryptPayload -> uploadResults; discuss testing strategy and how to handle dynamic DAG updates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Develop a privacy-preserving error-reporting framework with third-party plugins as dynamic feature modules. Each plugin runs in an isolated process and communicates with the host via AIDL, submitting sanitized crash dat…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a DAG-based orchestrator that parses a JSON description of nodes (containing id, worker class, constraints, and retry policy), builds a WorkManager chain using beginWith/then, and uses unique work name…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement an isolated Service (android:process) with AIDL IAnalyticsLogger; UI calls logEvent(sessionId, type, payload) which gets encrypted with AES-GCM using a hardware-backed key from Android Keyst",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run an isolated service with android:process and android:isolatedProcess, exposing an AIDL for send/receive. Use a master key from Android Keystore (hardware-backed when available) to derive per-conve",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses the ability to design modular, testable background processing systems with proper concurrency handling, failure recovery, and dynamic task orchestration—all critical skills for building robust Android applications.\n\n## Key Concepts\n\n- **DAG Parsing**: Converting JSON node definitions into executable work chains\n- **WorkManager Chaining**: Using beginWith/then to create sequential and parallel task flows\n- **Constraints**: Applying conditions like network availability, charging state, or device idle\n- **Exponential Backoff**: Implementing robust retry policies with increasing delays\n- **Data Propagation**: Passing results between workers via WorkManager's Data objects\n- **Idempotency**: Ensuring safe retry behavior through unique work names and state checks\n- **Cancellation Semantics**: Handling graceful termination and cleanup\n\n## Code Example\n\n```kotlin\ndata class DAGNode(\n    val id: String,\n    val workerClass: String,\n    val constraints: Constraints?,\n    val retryPolicy: RetryPolicy?\n)\n\nclass DAGOrchestrator(private val context: Context) {\n    fun execute(dagJson: String) {\n        val nodes = parseDAG(dagJson)\n        val workManager = WorkManager.getInstance(context)\n        \n        val sortedNodes = topologicalSort(nodes)\n        var previousWork: OneTimeWorkRequest? = null\n        \n        sortedNodes.forEach { node ->\n            val workRequest = buildWorkRequest(node)\n            previousWork = if (previousWork == null) {\n                workManager.beginWith(workRequest).let { null }\n            } else {\n                previousWork!!.then(workRequest)\n                previousWork\n            }\n        }\n    }\n}\n```\n\n## Follow-up Questions\n\n- **Dynamic DAG Updates**: How would you support runtime DAG reconfiguration without restarting workers in progress?\n- **Deterministic Ordering**: How would you ensure deterministic execution ordering when multiple branches can execute in parallel?\n- **Failure Recovery**: What strategies would you use to handle partial failures and resume from the last successful checkpoint?\n- **Memory Pressure**: How would you manage large data passing between workers without hitting WorkManager's size limits?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-17",
        "questionId": "q-1388",
        "question": "Implement a field survey photo capture in Android: tap Capture to take a photo, request CAMERA permission via the Activity Result API, use TakePicturePreview to obtain a Bitmap, save it to internal storage with a timestamped filename, and display a preview in an ImageView. Ensure rotation safety and a graceful denial/retry flow. What code would you write?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a data model where each media clip is represented as a ClipRecord containing the encrypted blob, nonce, chunk manifest, and upload state. Encrypt each clip with AES-256-GCM using a per-clip key derived via HKDF f…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a PluginVerifier in an isolated Android process that loads plugins only after attestation. Pin a root certificate in Android Keystore and verify each plugin APK signature against it; parse t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use the Activity Result API: register for RequestPermission(CAMERA) and grant path; on approval, launch TakePicturePreview to get a Bitmap, then save it to internal storage with a timestamped filename",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "I would implement a DAG-based orchestrator that parses a JSON description of nodes (containing id, worker class, constraints, and retry policy), builds a WorkManager chain using beginWith/then, and uses unique work name…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of runtime permissions, modern Android Activity Result APIs, simple photo capture flow, and lifecycle-safe storage.\n\n## Key Concepts\n- Activity Result API (RequestPermission, TakePicturePreview)\n- Runtime permissions and user rationale\n- Bitmap handling and internal storage with openFileOutput\n- Rotation-safe UI with ViewModel/Lifecycle\n- Basic error handling and retry\n\n## Code Example\n```javascript\n// (Note: labeled as javascript per formatting requirement; represents Kotlin-style approach)\nclass PhotoViewModel extends ViewModel {\n  var imagePath: String? = null\n}\n\nclass PhotoActivity extends AppCompatActivity {\n  private finalPhotoViewModel = viewModels<PhotoViewModel>()\n  private lateinit var imageView: ImageView\n\n  private val requestCameraPermission = registerForActivityResult(ActivityResultContracts.RequestPermission()) { granted ->\n    if (granted) launchTakePreview()\n    else showRationale()\n  }\n\n  private val takePreview = registerForActivityResult(ActivityResultContracts.TakePicturePreview()) { bmp ->\n    bmp?.let {\n      val filename = \"survey_${System.currentTimeMillis()}.jpg\"\n      openFileOutput(filename, MODE_PRIVATE).use { out -> it.compress(Bitmap.CompressFormat.JPEG, 90, out) }\n      viewModel.imagePath = filename\n      imageView.setImageBitmap(it)\n    }\n  }\n\n  private fun launchTakePreview() { takePreview.launch(null) }\n\n  private fun onCaptureClicked() { requestCameraPermission.launch(Manifest.permission.CAMERA) }\n\n  private fun showRationale() { /* show dialog and offer retry */ }\n}\n```\n\n## Follow-up Questions\n- How would you handle saving very large photos without exhausting memory?\n- How would you test permission denial paths across rotation and app restart?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-18",
        "questionId": "q-5224",
        "question": "Design an Android module for offline-first, per-user note syncing in a notes app. The module queues local edits when offline, syncs with a central REST service when online, uses a pluggable ConflictResolver to merge edits, stores data in Room, and uses per-device encryption keys in Android Keystore with nightly rotation; refresh tokens via OAuth. Make the sync Doze/App Standby-aware using WorkManager for incremental patches. Include data model, protocol, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an architecture that stores per-document CRDT state in Room, uses a lightweight CRDT (e.g., Add-Wins OR-Set) to merge concurrent local edits, and syncs via WebSocket with a REST fallback. Incl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an on-device ML inference service for Android that runs in an isolated process, loads a quantized TensorFlow Lite model from signed assets, and decrypts it using a per-model envelope stored in Android Keystore. S…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a CRDT-based document model stored in Room where each document is a collection of atoms with unique IDs; edits produce operations that are partitioned by user and merged by vector clocks. Encry",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Core approach: Note entity {id, content, localVersion, remoteVersion, lastModified}; EditsQueue; delta patch sync via REST with optimistic locks; pluggable ConflictResolver (default last-write-wins);",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses real-world offline-sync design, conflict handling, and security under Doze. Evaluates reasoning about data modeling, patching, and test strategy in a mobile context.\n\n## Key Concepts\n- Room data model for notes and an offline edits queue\n- Delta/patch sync via REST with optimistic locking\n- Pluggable ConflictResolver with a sane default\n- Android Keystore per-device encryption and rotation\n- OAuth token refresh and secure storage\n- WorkManager orchestration with Doze/App Standby awareness\n\n## Code Example\n```javascript\n// Pseudo-code: ConflictResolver contract\nclass ConflictResolver {\n  resolve(local, remote) {\n    // default: last-write-wins\n    return (local.lastModified >= remote.lastModified) ? local : remote;\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test edge cases like out-of-order patches and token expiry?\n- How would you scale the queue and ensure user-visible consistency during long offline periods?",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-19",
        "questionId": "q-19648",
        "question": "Design a privacy-preserving on-device policy engine for an Android app suite that runs in an isolated process, exposes an AIDL IPC surface, and evaluates per-event policies locally. It should store policies and evaluation logs in an encrypted Room database, hash/anonymize any telemetry, and batch uploads only on Wi‑Fi and when charging. Include data model, IPC surface, rollback/update strategy, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a background chat sync module with encrypted Room storage, using Android Keystore AES-GCM; sync via WorkManager with network constraint or a foreground service; apply server diffs idempotently",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Policy Engine Architecture ### Overview A privacy-preserving on-device policy engine implemented as an Android isolated service that evaluates per-event policies locally while maintaining strong security and privacy…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design an isolated cross-process notification handler that receives encrypted push payloads, decrypts them in a dedicated process (using android:process and android:isolatedProcess), validates signatures, and exposes a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a PluginVerifier in an isolated Android process that loads plugins only after attestation. Pin a root certificate in Android Keystore and verify each plugin APK signature against it; parse t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question evaluates the ability to design cross-process Android components with privacy constraints, implement robust update paths, and balance security with usability in enterprise or large-app contexts.\n\n## Key Concepts Demonstrated\n\n- **Isolated Process**: Using `android:isolatedProcess` or a private UID to run the policy engine in a sandboxed environment with minimal privileges, preventing direct access from other apps\n\n- **AIDL IPC Surface**: Defining a clean contract with methods like `evaluate(event, context) → Decision` and `getPolicySnapshot()` for client interaction\n\n- **Encrypted Storage**: Leveraging Room with SQLCipher for policies and evaluation logs, ensuring data at rest cannot be read without the encryption key\n\n- **Privacy-Preserving Telemetry**: Hashing event identifiers with per-device salts and avoiding PII collection to meet compliance requirements\n\n- **Constrained Uploads**: Using WorkManager constraints to ensure bandwidth-efficient and battery-conscious data synchronization",
        "difficulty": "advanced"
      },
      {
        "id": "android-q-20",
        "questionId": "q-17549",
        "question": "Context: android/general; Difficulty: beginner; Topics: android. Scenario: Build a minimal Android feature that shows a list of items (e.g., restaurants) in a RecyclerView, backed by a Room cache. On startup, load from Room; if network is available, fetch from a REST API via Retrofit and refresh Room; add a swipe-to-refresh that triggers the fetch. Ensure data flow uses LiveData/Flow, handles offline gracefully, and includes a simple UI state (loading, empty, error)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Architecture Overview The pipeline uses a sandboxed collector service (`:collector`) with its own process, communicating via AIDL to a background uploader service. Events are encrypted at rest using SQLCipher + per-e…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a DRM-enabled media player with license handling in an isolated Android process. The UI talks to a LicenseProvider AIDL surface to fetch licenses; the service uses MediaDrm to create sessions a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Repository that exposes `Flow<List<Restaurant>>` with Room as the backing store. At startup, observe the cached data from Room. If the network is available, fetch fresh data via Retrofit, save it to Room, and the…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Retrofit to fetch JSON from the API, map to Kotlin data classes, and persist into Room with a ProductEntity. Expose a Flow or LiveData from a Repository via a ViewModel, consumed by a Fragment wit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates a candidate's ability to combine offline caching, network fetching, and observable data streams in a beginner-friendly Android task. It reflects common real-world app patterns while keeping the scope manageable.\n\n## Key Concepts\n- Room database caching with Flow/LiveData\n- Retrofit for REST API calls and JSON serialization\n- Repository pattern for single source of truth\n- UI state management (loading, empty, error)\n- Swipe-to-refresh integration\n- Basic error handling for offline scenarios\n\n## Code Example\n```kotlin\n@Entity\ndata class Restaurant(\n    @PrimaryKey val id: String,\n    val name: String,\n    val rating: Float,\n    val address: String\n)\n\n@Dao\ninterface RestaurantDao {\n    @Query(\"SELECT * FROM restaurants\")\n    fun getAllRestaurants(): Flow<List<Restaurant>>\n\n    @Insert(onConflict = OnConflictStrategy.REPLACE)\n    suspend fun insertAll(restaurants: List<Restaurant>)\n\n    @Query(\"DELETE FROM restaurants\")\n    suspend fun deleteAll()\n}\n\nclass RestaurantRepository(\n    private val api: RestaurantApi,\n    private val dao: RestaurantDao\n) {\n    fun getRestaurants(): Flow<List<Restaurant>> = dao.getAllRestaurants()\n\n    suspend fun refresh(): Result<Unit> {\n        return try {\n            val response = api.getRestaurants()\n            dao.deleteAll()\n            dao.insertAll(response)\n            Result.success(Unit)\n        } catch (e: Exception) {\n            Result.failure(e)\n        }\n    }\n}\n```\n\n## Follow-up Questions\n1. How would you handle pagination for large datasets?\n2. What strategies would you use to prevent duplicate network requests?\n3. How would you test the Repository layer?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-21",
        "questionId": "q-23537",
        "question": "Context: android/general; Difficulty: intermediate; Topics: android. Scenario: Build an offline-first, multi-tenant analytics client that stores events in per-tenant encrypted Room databases; implement a pluggable Cache interface to fetch missing events from the server and a tenant-switch workflow that rotates encryption keys without data loss. Describe data models, DB schema, and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant encrypted Room databases (tenant_<id>.db) protected by SQLCipher. Store encryption keys in Android Keystore, wrapped per-tenant for isolation. Use Room with a SupportFactory to supply the cipher and…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Run a privacy-preserving ML model in an isolated Android process (android:isolatedProcess) and expose an AIDL interface for inferences. Load the model from a signed remote package; enforce per-request",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Note data model: {id,title,timestamp,audioPath,transcript,lastModified,tombstone}. Encrypt with Android Keystore in Room. Transcribe on-device with ML Kit, chunk audio into 15s blocks and append trans",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a module that stores telemetry in Room, maintains a local asset map, and applies server delta patches offline via WorkManager. Patches are signed with per-device keys from Android Keystore (",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical capabilities in Android data isolation, encryption, offline-first patterns, multi-tenant architecture, and test planning.\n\n## Key Concepts\n- Android Room with per-tenant databases and encryption\n- SQLCipher integration with Room and Android Keystore\n- Pluggable Cache interface for repository pattern\n- WorkManager for background sync\n- Data migration and multi-tenant isolation testing\n\n## Code Example\n```kotlin\n// Snippet showing TenantDatabase.forTenant and cipher config\nclass TenantDatabase private constructor(\n    private val db: SupportSQLiteDatabase\n) {\n    companion object {\n        suspend fun forTenant(context: Context, tenantId: String): TenantDatabase {\n            val key = KeystoreHelper.getKey(tenantId)\n            val factory = SupportFactory(key)\n            val db = Room.databaseBuilder(\n                context,\n                SupportSQLiteDatabase::class.java,\n                \"tenant_$tenantId.db\"\n            )\n                .openHelperFactory(factory)\n                .build()\n            return TenantDatabase(db)\n        }\n    }\n}\n```\n\n## Data Models\n\n```kotlin\n@Entity(tableName = \"events\")\ndata class Event(\n    @PrimaryKey val id: String,\n    val tenantId: String,\n    val eventType: String,\n    val payload: String,\n    val timestamp: Long,\n    val syncStatus: SyncStatus\n)\n\nenum class SyncStatus { PENDING, SYNCED, FAILED }\n```\n\n## Database Schema\n| Table | Description |\n|-------|-------------|\n| events | Per-tenant analytics events with sync status |\n| tenant_metadata | Tenant configuration and key references |\n| sync_queue | Pending operations for background sync |\n\n## Test Plan\n\n### Unit Tests\n- Verify key rotation preserves data integrity\n- Test Cache interface implementations\n- Validate tenant isolation (no cross-tenant data access)\n\n### Integration Tests\n- End-to-end tenant-switch workflow\n- Offline sync when connectivity restores\n- SQLCipher encryption verification\n\n### Manual Tests\n- Verify Keystore key management across app restarts\n- Test multi-tenant edge cases (concurrent switches)",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-22",
        "questionId": "q-6780",
        "question": "Context: android/general; Difficulty: beginner; Topics: android. Scenario: Build a lightweight, offline-first translation dictionary module. Create a Kotlin Android module that fetches word lists from a REST API over Wi‑Fi only, stores translations in Room with the translation encrypted via a per-device key in Android Keystore, and provides a fast prefix search. Describe the data model, DAO, background sync with WorkManager constraints, encryption setup, and a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a CRDT-based document model stored in Room where each document is a collection of atoms with unique IDs; edits produce operations that are partitioned by user and merged by vector clocks. Encry",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a local-first dictionary module: use Room, per-word encryption, and wifi-only sync. Data model: word PK, translationEncrypted, timestamp. Encryption via Android Keystore AES-GCM with a per-d",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design an isolated Android analytics pipeline that uses a sandboxed service and a ContentProvider for IPC. It should encrypt events with a Keystore-backed key, store in Room with per-event redaction a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use RecyclerView.Adapter with getItemViewType() to return different view types. Implement the ViewHolder pattern for each type, use DiffUtil for efficient updates, and enable setHasFixedSize(true). For large datasets, i…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests offline-first data flow, security, and background work integration with WorkManager in a realistic Android feature.\n\n## Key Concepts\n- Room data model with a primary key on word\n- Android Keystore for per-device AES-GCM keys\n- Encrypt/decrypt translations; store IV with ciphertext\n- Prefix search via SQL LIKE queries\n- WorkManager constraints for Wi‑Fi synchronization\n\n## Code Example\n```kotlin\n@Entity(tableName = \"translations\")\ndata class Translation(\n  @PrimaryKey val word: String,\n  @ColumnInfo(name = \"translation_encrypted\") val translationEncrypted: ByteArray,\n  val timestamp: Long\n)\n```\n\n## Follow-up Questions\n- How would you handle key rotation and re-encryption of existing data?\n- How would you scale the search index for large vocabularies and handle locale-specific rules?",
        "difficulty": "beginner"
      },
      {
        "id": "android-q-23",
        "questionId": "q-258",
        "question": "How would you design a reactive Android ViewModel using StateFlow with sealed classes to handle network API responses, ensuring proper error handling and loading states?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe how you would implement an offline-first notes module using Room for local storage and WorkManager to sync with a mock REST API when the device is online and charging. Specify data models, DA",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: store labeled samples in Room; train a compact TensorFlow Lite model on-device; deliver incremental delta-weights via backend updates; track modelVersion in DataStore; apply updates with ide",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use MutableStateFlow<UiState> with sealed class (Loading/Success/Error) and collect with catch operator for error handling in coroutine scope.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "onCreate() is the first lifecycle method called when an Activity is created. It receives a Bundle savedInstanceState parameter containing previous state data for activity recreation. You must call super.onCreate(savedIn…",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nCombining StateFlow with sealed classes creates a type-safe, reactive state management pattern perfect for handling API responses. The StateFlow holds the current UI state while sealed classes define all possible states, enabling compile-time safety when handling different scenarios.\n\n## Implementation Details\n\n### 1. Define Sealed Class for UI States\n```kotlin\nsealed class UiState<out T> {\n    object Loading : UiState<Nothing>()\n    data class Success<T>(val data: T) : UiState<T>()\n    data class Error(val exception: Throwable) : UiState<Nothing>()\n}\n```\n\n### 2. ViewModel Implementation\n```kotlin\nclass UserRepository @Inject constructor(\n    private val apiService: ApiService\n) {\n    private val _usersState = MutableStateFlow<UiState<List<User>>>(UiState.Loading)\n    val usersState: StateFlow<UiState<List<User>>> = _usersState.asStateFlow()\n\n    fun fetchUsers() {\n        viewModelScope.launch {\n            _usersState.value = UiState.Loading\n            apiService.getUsers()\n                .catch { exception ->\n                    _usersState.value = UiState.Error(exception)\n                }\n                .collect { users ->\n                    _usersState.value = UiState.Success(users)\n                }\n        }\n    }\n}\n```\n\n### 3. Activity/Fragment Collection\n```kotlin\nlifecycleScope.launch {\n    viewModel.usersState.collect { state ->\n        when (state) {\n            is UiState.Loading -> showProgressBar()\n            is UiState.Success -> showUsers(state.data)\n            is UiState.Error -> showError(state.exception.message)\n        }\n    }\n}\n```\n\n## Common Pitfalls\n- **Memory Leaks**: Always use `viewModelScope` or `lifecycleScope` for coroutines\n- **State Conflation**: StateFlow is conflated by default - rapid updates may be lost\n- **Backpressure**: Use `buffer()` or `conflate() strategically for high-frequency updates\n- **Initial State**: Always initialize StateFlow with a meaningful default state\n- **Error Propagation**: Don't forget the `catch` operator to prevent cancellation",
        "difficulty": "intermediate"
      },
      {
        "id": "android-q-24",
        "questionId": "q-4863",
        "question": "Design an Android module for offline-capable feature flag evaluation in a real-time trading app. Flags come from a central server and may depend on user tier and locale. Implement: (1) local storage schema (Room/DataStore) with versioning, (2) deterministic evaluation using hash(ANDROID_ID||flagName) < rollout, (3) background sync via WorkManager to refresh and reconcile flags when online, with rollback on failure, (4) safe defaults and dependency handling, (5) a test plan for offline-first correctness, rollout drift, and dependency resolution?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Store notes in Room with fields id (PK), text, timestamp, synced. Implement a Repository exposing getAllNotes(), addNote(), markSynced(id). Create a WorkManager Worker that queries unsynced notes, cal",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a flag service that stores flags in Room with fields: name, value, version, rollout, deps, lastSynced. Evaluation is deterministic: active if hash(ANDROID_ID||name) mod 100 < rollout. Use Da",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design an isolated crash-reporting service (android:isolatedProcess) with an AIDL interface exposing reportCrash(CrashDump), flush(), and setUserOptIn(boolean). Redact PII in dumps, hash-identify fiel",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a modular enterprise analytics client with multi-tenant isolation. Use Dynamic Feature Modules for widgets, Encrypted Room for events, and WorkManager with constraints (network=unmetered, charg",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a robust, offline-first feature flag system with deterministic evaluation and safe rollback, critical in finance or enterprise apps.\n\n## Key Concepts\n\n- Deterministic feature rollouts using device-scoped hash\n- Local persistence with Room/DataStore, and versioning\n- Background syncing with WorkManager and backoff\n- Dependency handling and sane defaults\n- Rollback/audit trail and rollback safety\n\n## Code Example\n\n```kotlin\nval rollout = 30\nval deviceHash = hash(androidId + flagName)\nval active = (deviceHash % 100) < rollout\n```\n\n## Follow-up Questions\n\n- How would you handle changes to rollout percentages without causing mid-flight inconsistencies?\n- How would you ensure security and integrity of flag updates from server (signatures, TLS pinning).",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-20T05:40:19.457Z",
    "version": 1
  }
]