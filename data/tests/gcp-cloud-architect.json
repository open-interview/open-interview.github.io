[
  {
    "id": "test-gcp-cloud-architect",
    "channelId": "gcp-cloud-architect",
    "channelName": "GCP Cloud Architect",
    "title": "GCP Cloud Architect Knowledge Test",
    "description": "Test your understanding of GCP Cloud Architect with 25 questions.",
    "questions": [
      {
        "id": "gcp-cloud-architect-q-0",
        "questionId": "q-22098",
        "question": "Design a beginner-friendly, event-driven data ingestion pipeline on GCP for a multi-tenant marketplace: tenants upload daily CSVs to Cloud Storage. Build an API on Cloud Run behind Endpoints that accepts uploads, validates the tenant, and publishes a Pub/Sub message with a file reference. A separate worker Cloud Run pulls from Pub/Sub, validates schema, redacts PII, and writes per-tenant results to dedicated BigQuery datasets. Include idempotency, per-tenant isolation, observability, and a simple test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route events using Pub/Sub message attributes to carry tenant identification. A Dataflow streaming job consumes messages and writes to per-tenant BigQuery datasets using tenant-specific service accounts. Enforce IAM con…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Devices publish JSON to region-scoped Pub/Sub; a Dataflow streaming job normalizes events and writes to per-tenant BigQuery datasets using insertId for dedup. IAM binds a dedicated Dataflow service ac",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "API on Cloud Run behind Endpoints that accepts CSV file uploads per tenant, validates tenant credentials, and publishes a Pub/Sub message with the file reference. A separate worker Cloud Run service consumes from Pub/Su…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement cross-region DR by replicating Pub/Sub to a paired region and running a streaming Dataflow pipeline that writes per-tenant aggregates to BigQuery in both primary and DR projects. Use a warm",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design a practical, beginner-friendly, event-driven data ingestion pipeline using GCP's serverless components. It evaluates understanding of tenant isolation, data privacy, idempotency, and observability in a realistic multi-tenant scenario.\n\n## Key Concepts\n- Event-driven architecture with Pub/Sub for decoupled processing\n- Cloud Run for both API and worker compute workloads\n- Per-tenant isolation using separate BigQuery datasets and dedicated service accounts\n- Idempotency via file checksum and Pub/Sub message deduplication\n- PII redaction and basic observability with Cloud Logging and Monitoring\n\n## Architecture Overview\n1. **Upload API** (Cloud Run + API Gateway/Endpoints): Validates tenant, stores file to Cloud Storage, publishes event to Pub/Sub\n2. **Storage**: Bucket with per-tenant prefixes for logical separation\n3. **Worker** (Cloud Run): Pulls messages, validates schema, redact PII, writes to BigQuery\n4. **BigQuery**: Dedicated dataset per tenant with row-level access controls\n\n## Idempotency Strategy\n- Calculate SHA-256 checksum on upload; store in metadata to detect duplicates\n- Use Pub/Sub message_id for deduplication at worker level\n- Check BigQuery for existing records before insert\n\n## Code Example\n```javascript\n// Producer: Upload API\nconst { Storage } = require('@google-cloud/storage');\nconst { PubSub } = require('@google-cloud/pubsub');\n\nasync function handleUpload(req, res) {\n  const { tenantId, checksum } = req.body;\n  const bucket = storage.bucket('tenant-files');\n  const file = bucket.file(`${tenantId}/${Date.now()}.csv`);\n  \n  await file.save(req.file.buffer, { metadata: { metadata: { tenantId, checksum } } });\n  \n  const pubsub = new PubSub();\n  await pubsub.topic('file-events').publishMessage({\n    data: { tenantId, filePath: file.name, checksum },\n    attributes: { messageId: req.messageId }\n  });\n  \n  res.status(202).json({ status: 'queued' });\n}\n\n// Consumer: Worker Cloud Run\nconst { BigQuery } = require('@google-cloud/bigquery');\n\nasync function processFile(event, context) {\n  const { tenantId, filePath, checksum } = JSON.parse(Buffer.from(event.data, 'base64').toString());\n  const bq = new BigQuery({ projectId: tenantId });\n  \n  const existing = await bq.query(`SELECT 1 FROM ${tenantId}.files WHERE checksum = '${checksum}' LIMIT 1`);\n  if (existing.length > 0) return; // Idempotent skip\n  \n  const rows = await readAndRedact(filePath);\n  await bq.dataset(tenantId).table('records').insert(rows);\n}\n\nfunction readAndRedact(filePath) {\n  // Parse CSV, redact PII fields (email, phone, SSN)\n  return rows.map(row => ({ ...row, email: '[REDACTED]', phone: '[REDACTED]' }));\n}\n```\n\n## Test Plan\n| Test | Expected Result |\n|------|------------------|\n| Valid tenant upload | 202 Accepted, message published |\n| Invalid tenant | 401 Unauthorized |\n| Duplicate file (same checksum) | 202 Accepted, no duplicate processing |\n| Malformed CSV | 400 Bad Request at API |\n| Schema mismatch | Worker logs error, moves to dead letter queue |\n| PII fields redacted | Email/phone show '[REDACTED]' in BigQuery |\n| Cross-tenant access | Denied via IAM policies |\n\n## Observability\n- **Cloud Logging**: Structured logs for upload events, processing steps, errors\n- **Cloud Monitoring**: Custom metrics for queue depth, processing latency, error rates\n- **Alerting**: Email/DLP alerts on anomalous error patterns",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-1",
        "questionId": "q-19182",
        "question": "Design a beginner-friendly, multi-tenant API pipeline on GCP. Each tenant is served by a separate Cloud Run backend behind a single HTTP(S) Load Balancer and API Gateway. Tenant isolation via per-tenant buckets and IAM, writes enqueued to Pub/Sub with a Dataflow/BigQuery sink. Include per-tenant quotas, idempotent writes, observability, and a simple test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement per-tenant datasets in BigQuery with a tenant-scoped service account and IAM bindings. A single Dataflow streaming job subscribes to a per-tenant Pub/Sub topic and writes to that tenant’",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant isolation via separate Cloud SQL schemas or databases and per-tenant service accounts. Bind each Cloud Run service to a tenant-scoped IAM role with conditions on a tenant_id label; requ",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route tenantId via API Gateway to per-tenant Cloud Run backends behind an HTTP(S) Load Balancer. Isolate tenants with separate storage buckets and service accounts; decouple writes with Pub/Sub and a",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest from Pub/Sub with tenant_id, route to a regional Dataflow streaming job that deduplicates on event_id and writes to per-tenant BigQuery datasets located in the event’s region. Use per-tenant se",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a scalable multi-tenant GCP workflow with proper isolation, routing, observability, and basic guardrails for a beginner.\n\n## Key Concepts\n\n- API Gateway routing and per-tenant isolation\n- Cloud Run tenancy patterns and IAM\n- Pub/Sub as an event queue and idempotent sinks\n- Dataflow/BigQuery integration and observability\n\n## Code Example\n\n```javascript\n// Placeholder example: not a full implementation\n```\n\n## Follow-up Questions\n\n- How would you validate per-tenant quotas?\n- How would you test idempotency with duplicate messages?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-2",
        "questionId": "q-14638",
        "question": "Design a cost-aware, compliant analytics pipeline on GCP for a multi-tenant SaaS app generating 100 GB/day of events via Pub/Sub. Build a streaming ingestion path using Dataflow to BigQuery with per-tenant datasets, ensure strict IAM least privilege and tenant isolation, apply PII masking via Data Loss Prevention or SQL UDFs, log correlation_id in Cloud Logging, alert on cost and data-skew in Cloud Monitoring, and support backward-compatible schema evolution with canary rollouts and rollback. What trade-offs and components would you choose?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a region-aware Pub/Sub eventing model that triggers Dataflow streaming jobs per region. Validate CSV schemas per tenant, route data to tenant-specific partitions in regional BigQuery datasets, secure with envi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant isolation via separate BigQuery datasets and Pub/Sub subscriptions; streaming with Dataflow exactly-once delivery to partitioned, clustered tables keyed by tenant. Enforce least-privilege IAM with tenant-…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a single Cloud Run service that inspects a signed JWT to derive tenant_id for every request. Create per-tenant storage: Cloud Storage buckets and BigQuery datasets with strict IAM bindings. Apply",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "API on Cloud Run behind Endpoints that accepts CSV file uploads per tenant, validates tenant credentials, and publishes a Pub/Sub message with the file reference. A separate worker Cloud Run service consumes from Pub/Su…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical GCP design for a scalable, cost-conscious, multi-tenant analytics stack with governance and observability.\n\n## Key Concepts\n- Tenant isolation across datasets and topics\n- Streaming ingestion with Dataflow (exactly-once, windowing)\n- Least-privilege IAM and tenant-scoped service accounts\n- PII masking via DLP or safe SQL UDFs\n- Cloud Logging with correlation_id and Cloud Monitoring alerts\n- Schema evolution with backward-compatible changes and canary rollback\n\n## Trade-offs\n- **Cost vs. Latency**: Per-tenant isolation increases storage costs but provides better governance and query performance\n- **Complexity vs. Control**: Multiple service accounts increase management overhead but ensure strict security boundaries\n- **DLP API Costs**: Real-time PII masking adds processing costs but provides compliance assurance\n\n## Code Example\n```python\n# PII masking in Dataflow (illustrative)\nimport apache_beam as beam\nfrom google.cloud import dlp_v2\n\ndef mask_pii_record(record, dlp_client):\n    # Apply DLP transformations for sensitive fields\n    if 'email' in record:\n        record['email'] = dlp_client.deidentify_content(\n            parent=f'projects/{project_id}',\n            deidentify_config={'info_types': [{'name': 'EMAIL_ADDRESS'}]}\n        ).item.value\n    \n    # Hash tenant_id for additional privacy\n    record['tenant_id_hash'] = hashlib.sha256(\n        record['tenant_id'].encode()\n    ).hexdigest()[:16]\n    \n    return record\n\n# correlation_id propagation\ndef add_correlation_id(record):\n    record['correlation_id'] = record.get('correlation_id', str(uuid.uuid4()))\n    return record\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-3",
        "questionId": "q-26462",
        "question": "Design a beginner-friendly per-tenant data-processing service on GCP: events arrive to Pub/Sub; a Cloud Run worker processes each event and writes results to per-tenant BigQuery datasets. Implement observability: structured Cloud Logging with correlation_id, per-tenant Cloud Monitoring metrics, and a simple alert if error rate exceeds 5% in a 5-minute window. Include a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement: Pub/Sub triggers Cloud Run; per-tenant BigQuery datasets; correlation_id in all logs; custom metrics per tenant (events_seen, events_failed, events_processed); Cloud Monitoring aler",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "On Cloud Run API, enqueue a Cloud Tasks task with payload {tenantId, imageUri, size}. Worker Cloud Run downloads, resizes, and writes to tenant-specific bucket. Idempotent via image digest and per-tas",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose per-tenant Cloud SQL instances behind a Shared VPC, with a single Cloud Run frontend behind IAP. Use per-tenant service accounts with least-privilege roles (sql.client, bigquery.jobUser) and C",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a multi-tenant data lake on Google Cloud using per-tenant Cloud Storage buckets and BigQuery datasets, each secured with resource-level IAM bindings following the principle of least privilege. For streaming inges…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTo assess ability to design tenant isolation, observability, and basic SRE patterns in GCP, focusing on beginner-level integration.\n\n## Key Concepts\n- Pub/Sub, Cloud Run, BigQuery\n- Per-tenant isolation\n- Structured logging with correlation_id\n- Custom metrics in Cloud Monitoring\n- Alerting and test plan\n\n## Code Example\n```javascript\n// Example: emit correlation_id in logs\nconst log = (tenantId, event, correlationId) => {\n  console.log(JSON.stringify({'tenant': tenantId, 'event': event, 'correlation_id': correlationId}));\n};\n```\n```python\n# Example: push a custom metric (pseudo-snippet)\nfrom google.cloud import monitoring_v3\nclient = monitoring_v3.MetricServiceClient()\n# Build and create TimeSeries with tenant label\n```\n\n## Follow-up Questions\n- How would you implement exactly-once semantics for Pub/Sub events in this pipeline?\n- How would you test the alerting rule with unit/integration tests?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-4",
        "questionId": "q-2451",
        "question": "Design a cost-conscious, multi-tenant data pipeline on GCP for a SaaS product. Each tenant's data must live in isolated Cloud Storage buckets and BigQuery datasets with strict IAM boundaries. Implement a daily event ingestion from Pub/Sub to Dataflow, partitioned tables, CMEK encryption, and per-tenant retention. How would you validate tenant isolation, auditability, and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming pipeline for multi-tenant telemetry. Pub/Sub ingests events; Dataflow streaming template fans out by tenant_id to per-tenant BigQuery partitions (or per-tenant datasets). Enforce pe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant buckets and datasets; Pub/Sub per-tenant topics; Dataflow template ingests events into day-partitioned BigQuery tables, with CMEK per tenant. Enforce IAM least privilege and per-tenant",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a cost-aware, privacy-preserving data ingestion and analytics path on GCP for a multi-tenant app. Isolate each tenant's data in per-tenant GCS buckets and BigQuery datasets. Ingest via Pub/Sub,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed architecture: Tenant projects; Pub/Sub per tenant; regional Cloud Storage; Dataflow streaming to per-tenant BigQuery datasets; private Cloud Run API via Private Service Connect; IAM least pri",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a scalable, compliant multi-tenant data pipeline on GCP with strong data isolation, cost controls, and rollback capabilities.\n\n## Key Concepts\n- Per-tenant isolation across Cloud Storage and BigQuery\n- Pub/Sub topic-per-tenant architecture\n- Dataflow templating for reliable ingestion into partitioned BigQuery tables\n- CMEK encryption per tenant and IAM least-privilege boundaries\n- Storage lifecycle retention and audit logging with correlation IDs\n- Rollback and canary deployment strategies\n\n## Code Example\n```java\n// Pseudo Dataflow template outline showing per-tenant topic handling\nPipeline p = Pipeline.create(options);\nPCollection<String> events = p.apply(\"ReadEvents\", PubsubIO.readStrings().fromTopic(TENANT_TOPIC));\nevents.apply(\"Parse\", ParDo.of(new DoFn<String, TenantEvent>() { ... }));\nevents.apply(\"WriteBQ\", BigQueryIO.writeTableRows()\n  .to(\"project.dataset.${TENANT_ID}.table$DATE\").withSchema(SCHEMA));\n```\n\n## Follow-up Questions\n- How would you test cross-tenant data leakage and performance at scale?\n- How would you onboard new tenants and enforce isolation automatically?\n- What metrics and budgets would you monitor to keep costs predictable while meeting SLAs?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-5",
        "questionId": "q-15585",
        "question": "Design a real-time fraud-detection pipeline on GCP for a payments provider. Ingest events from Pub/Sub into a streaming Dataflow job that calls a Vertex AI deployed model for online inference, and write per-tenant risk scores to BigQuery while archiving raw events to per-tenant Cloud Storage. Enforce strict per-tenant isolation (separate service accounts, CMEK), enable Private Service Connect to Vertex AI, and apply VPC Service Controls. Include observability, a cost-optimization plan, and a two-region failover strategy with a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Primary: Dataflow streaming in us-central1 reads Pub/Sub us-central1 and writes to BigQuery us-central1. Standby: identical pipeline in us-east1 with mirrored Pub/Sub and BigQuery. Failover via subscr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose per-tenant buckets and datasets, region-aware residency, and a Pub/Sub/Dataflow path to tenant-bigquery. Use dedicated service accounts with least privilege, VPC Service Controls, and per-tena",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant, event-driven pipeline: daily CSVs land in per-tenant GCS buckets; publish a Pub/Sub message with tenant_id and file_md5; a Dataflow batch job validates schema, writes to per-te",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Pub/Sub to Dataflow streaming pipeline that calls Vertex AI deployed model for real-time inference, writes per-tenant risk scores to BigQuery, and archives raw events to per-tenant Cloud S",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design real-time, multi-tenant data pipelines with secure, auditable boundaries and cross-region resilience.\n\n## Key Concepts\n\n- Pub/Sub and Dataflow streaming\n- Vertex AI model integration and endpoint security\n- Tenant isolation: service accounts, CMEK, IAM\n- Private Service Connect and VPC Service Controls\n- Data lineage, observability, cost control, and DR\n\n## Code Example\n\n```javascript\n// Pseudo snippet: route Pub/Sub messages to Vertex AI for inference\nfunction onMessage(msg){\n  const features = extractFeatures(msg);\n  return vertexAi.invokeEndpoint(features);\n}\n```\n\n## Follow-up Questions\n\n- How would you validate model drift and update cadence without downtime?\n- What metrics would define acceptable latency and SLOs for multi-tenant workloads?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-6",
        "questionId": "q-7979",
        "question": "Design a beginner-friendly, cost-conscious disaster recovery plan for a small GCP app using Cloud Run and Cloud SQL that spans two regions; specify RPO/RTO targets, a standby region behind a Global HTTP(S) Load Balancer, cross-region Cloud SQL read replicas, a failover workflow, and a simple test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a real-time, tenant-isolated alert pipeline on GCP for a fraud-detection service. Ingest partner events via Pub/Sub with tenant_id, process in a streaming Dataflow job that enriches with a BigQuery reference tabl…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-region DR for a Cloud Run + Cloud SQL app: primary in us-central1, secondary in europe-west1. Deploy Cloud Run in both regions behind a Global HTTP(S) Load Balancer. Cloud SQL: primary in us-centr",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use per-tenant Kubernetes namespaces and a dedicated GPU node pool with taints; enforce NetworkPolicy isolation; implement WorkloadIdentity so pods assume tenant-scoped IAM roles; buckets per tenant w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Route all traffic through API Gateway in front of Cloud Run. Enforce per-tenant API keys and quotas via API config, with environment isolation in separate projects. Use per-tenant IAM service accounts",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical DR design with beginner-accessible GCP services. Requires naming regions, enabling cross-region replicas, and coordinating LB, DNS, and DB promotion. Emphasizes testability and cost-awareness.\n\n## Key Concepts\n- Disaster Recovery planning\n- Cross-region Cloud SQL replicas\n- Global HTTP(S) Load Balancer traffic distribution\n- Cloud Run deployment in multiple regions\n- Manual failover runbook and DNS cutover\n\n## Code Example\n```javascript\n// Pseudo JS snippet: switch app DB endpoint on failover\nconst DB_HOST = process.env.DB_HOST;\nprocess.env.DB_HOST = newRegionalReplicaHost; // when failover\n```\n\n## Follow-up Questions\n- How would you automate the replica promotion and DNS switch for a semi-automated drill?\n- What metrics would you monitor to validate RPO/RTO?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-7",
        "questionId": "q-23541",
        "question": "You're building a beginner-friendly, privacy-preserving analytics pipeline on GCP for a mobile app. Ingest per-tenant events via a Cloud Run API, route through Pub/Sub, process with Dataflow (Beam) to per-tenant BigQuery datasets, and store raw events in region-specific Cloud Storage. Enforce per-tenant isolation, idempotency, observability, and a rollback/test plan. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a single BigQuery table with tenant_id; choose BigQuery Row-Level Security or authorized views to restrict access. Attach dataset IAM to enforce access, enable Cloud Audit Logs for read events, an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Authenticate each tenant using a JWT bound to a tenant-specific service account. Publish events to Pub/Sub with tenant_id and event_id for idempotency. Use Dataflow to read tenant_id and route writes to per-tenant BigQu…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Pub/Sub with a subscriber to feed a Cloud Run Python worker. Implement idempotence by deduplicating on error_id using a Redis cache in Memorystore (or rely on BigQuery insertId for streaming inserts). Batch errors b…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a tenant-aware ingestion path: an API Gateway routes to a per-tenant Cloud Run service; each tenant has its own region-bound Cloud Storage bucket and Pub/Sub topic; a dedicated service account",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests understanding of privacy-conscious, multi-tenant data pipelines on GCP, balancing isolation, scalability, and observability. It evaluates the candidate's grasp of event-driven architecture, including Cloud Run for ingestion, Pub/Sub for message routing, Dataflow for processing, and per-tenant BigQuery storage. Additionally, it probes knowledge of rollback strategies and testing methodologies for data pipelines.\n\n## Key Concepts\n\n- Multi-tenant isolation with per-tenant datasets, storage buckets, and IAM policies\n- Idempotent ingestion using event_id and deduplication\n- Observability through structured logging, tracing, and tenant-aware metrics\n- Rollback and testing strategies including canary deployments, replay mechanisms, and data validation",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-8",
        "questionId": "q-18658",
        "question": "Design a beginner-friendly per-tenant secret management pattern on GCP for a multi-tenant SaaS app: store tenant-specific API keys in Secret Manager, enforce tenant isolation (per-tenant service accounts and separate projects or folders), implement automatic rotation via Cloud Scheduler and Cloud KMS, and provide basic observability with Cloud Logging/Audit Logs. Include a simple test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ensure EU residency by splitting pipelines: ingest to Pub/Sub EU, Dataflow streaming in EU, and BigQuery EU dataset for raw data; aggregate tables copied to a global dataset with controlled views. Use",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a per-tenant secret strategy using Secret Manager. Create tenant-scoped secrets, assign per-tenant service accounts with least privilege, isolate tenants in separate projects or folders, enabl",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-tenant isolation: separate BigQuery datasets with per-tenant CMEK keys; a Cloud Run ingest API with least-privilege IAM that publishes to tenant-scoped Pub/Sub topics; Dataflow pipelines (tenant_i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Dataflow batch (Python Beam) reads daily CSVs from a shared GCS bucket, validates schema, normalizes dates, and writes to per-tenant BigQuery tables via dynamic destinations in dev/stage/prod datasets",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses understanding of secrets, access control, and tenancy boundaries in GCP.\n\n## Key Concepts\n- Secret Manager scoping and labeling\n- IAM least privilege and per-tenant service accounts\n- Project/folder isolation strategy\n- Rotation with Cloud Scheduler and KMS\n- Cloud Audit Logs for observability\n\n## Code Example\n```javascript\n// Placeholder example: integration sketch, not production code\n```\n\n## Follow-up Questions\n- How would you test rotation without impacting runtime?\n- How would you handle leaked keys across tenants?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-9",
        "questionId": "q-6446",
        "question": "Design a multi-tenant streaming ingestion on GCP for a SaaS product with 1,000 tenants. Ingest JSON events from Partner Pub/Sub, store per-tenant raw data in separate Cloud Storage and BigQuery datasets, enforce isolation via per-tenant IAM/service accounts and Private Service Connect, and emit anonymized cross-tenant metrics to a shared BigQuery dataset. Provide architecture, Dataflow templates, access control, testing, and rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-tenant streaming ingestion in Dataflow that subscribes to Partner Pub/Sub, validates JSON, writes per-tenant raw data to separate Cloud Storage and BigQuery datasets, and emits anonymized cross-tenant metr…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect per-tenant isolation by creating separate GCS buckets, BigQuery datasets, and Vertex AI projects. Ingest uploads via signed URLs into the tenant bucket; a Cloud Function validates signatures",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a single BigQuery data lake with per-tenant access using row-level security on a shared dataset; enforce isolation via tenant_id and SESSION_USER(); mask PII with Cloud DLP before loading, a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single Cloud Run ingestion service behind per-tenant Pub/Sub subscriptions; authenticate via Workload Identity Federation; stream with Dataflow to normalize JSON to a canonical schema and emit t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design scalable, auditable multi-tenant ingestion with strict isolation and cross-tenant analytics.\n\n## Key Concepts\n\n- Data isolation per tenant via separate Cloud Storage/BigQuery datasets and per-tenant IAM/service accounts\n- Streaming ingestion with Pub/Sub and Dataflow templates\n- Private networking with Private Service Connect and VPC Service Controls\n- Auditability via Data Catalog and Cloud Audit Logs\n- Rollback and testing strategies\n\n## Code Example\n\n```python\n# Dataflow template skeleton illustrating per-tenant routing\nfrom apache_beam.options.pipeline_options import PipelineOptions\nfrom apache_beam import Pipeline\nfrom apache_beam.io import ReadFromPubSub\nfrom apache_beam.io.gcp.bigquery import WriteToBigQuery\nfrom apache_beam.io.gcp.gcsio import GCSIO\n\nclass TenantRoutingOptions(PipelineOptions):\n    @classmethod\n    def _add_argparse_args(cls, parser):\n        parser.add_argument('--partner-topic', required=True)\n        parser.add_argument('--metrics-dataset', required=True)\n\ndef route_by_tenant(element, tenant_config):\n    tenant_id = element.get('tenant_id')\n    if tenant_id not in tenant_config:\n        raise ValueError(f'Unknown tenant: {tenant_id}')\n    return tenant_config[tenant_id]\n\ndef run():\n    options = PipelineOptions()\n    tenant_routing = TenantRoutingOptions()\n    \n    with Pipeline(options=options) as p:\n        events = (p | 'ReadFromPubSub' >> ReadFromPubSub(\n                    subscription=tenant_routing.partner_topic)\n                    | 'ParseJSON' >> Map(parse_json)\n                    | 'ValidateSchema' >> Map(validate_schema))\n        \n        # Per-tenant routing\n        tenant_events = (events | 'RouteByTenant' >> Map(\n            lambda x: route_by_tenant(x, TENANT_CONFIG)))\n        \n        # Write to tenant-specific storage\n        (tenant_events | 'WriteToTenantGCS' >> Map(\n            lambda x: write_to_tenant_gcs(x))\n                    | 'WriteToTenantBQ' >> WriteToBigQuery(\n                        table=lambda x: x['tenant_table'],\n                        schema=EVENT_SCHEMA))\n        \n        # Emit anonymized metrics\n        (events | 'AnonymizeMetrics' >> Map(anonymize_metrics)\n                | 'WriteToMetricsBQ' >> WriteToBigQuery(\n                    table=tenant_routing.metrics_dataset,\n                    schema=METRICS_SCHEMA))\n```",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-10",
        "questionId": "q-1825",
        "question": "Design a cross-region streaming analytics pipeline on GCP where raw events from EU users must remain data-resident, while derived aggregates are queried globally. Describe your architecture using Pub/Sub, Dataflow (Streaming), BigQuery, and Cloud Storage; enforce least-privilege IAM and per-environment isolation; implement data residency checks and automated rollbacks. How would you validate and test this end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route events to correct tenant with a TenantRouter Cloud Run that reads tenant_id from Pub/Sub message attributes; assign per-tenant service accounts with least privilege roles for Pub/Sub, Dataflow,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ensure EU residency by splitting pipelines: ingest to Pub/Sub EU, Dataflow streaming in EU, and BigQuery EU dataset for raw data; aggregate tables copied to a global dataset with controlled views. Use",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Ingest Pub/Sub events per environment through a Dataflow streaming pipeline, deduplicate by event_id using a stateful DoFn, and write to per-tenant, partitioned BigQuery tables. Enforce isolation with",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Split ingestion per tenant with Pub/Sub topics and per-tenant BigQuery datasets. Use Dataflow Streaming with exactly-once semantics to write to per-tenant tables, enforce row-level security via author",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests architecture for data residency, cross-region dataflow, and concrete controls in GCP.\n\n## Key Concepts\n- Data residency and egress controls\n- Pub/Sub, Dataflow streaming, BigQuery datasets\n- IAM least privilege and per-environment isolation\n- Observability and automated validation\n\n## Code Example\n```javascript\n// Placeholder for pseudo-implementation ideas\n```\n\n## Follow-up Questions\n- How would you enforce per-project isolation across dev/stage/prod?\n- How would you validate end-to-end residency periodically?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-11",
        "questionId": "q-29802",
        "question": "Design a multi-tenant real-time fraud-detection pipeline on GCP: ingest events via Pub/Sub, stream to Dataflow, enrich from a per-tenant metadata store, write windowed results to per-tenant BigQuery datasets, and publish per-tenant alerts to a Cloud Tasks queue that triggers a remediation Cloud Run service. Enforce least-privilege IAM, per-tenant isolation, VPC Service Controls, and a two-region failover plan. Include observability and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pub/Sub topics per tenant for ingestion, with Dataflow (Beam) jobs processing both batch and streaming data via reusable templates. Write to per-tenant BigQuery datasets with strict isolation enforced through IAM co…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect with per-tenant Pub/Sub topics, a Dataflow streaming pipeline that enriches events from a per-tenant metadata store, writes windowed results to tenant-scoped BigQuery datasets, and publishes",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose per-tenant buckets and datasets, region-aware residency, and a Pub/Sub/Dataflow path to tenant-bigquery. Use dedicated service accounts with least privilege, VPC Service Controls, and per-tena",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-region private pipeline: Pub/Sub ingests events; Dataflow streaming writes to per-tenant BigQuery datasets with IAM/dataset ACL isolation; Cloud Run API behind Internal HTTP(S) Load Balancer; CMEK",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design scalable, secure, observable, multi-tenant pipelines on GCP with real-time constraints.\n\n## Key Concepts\n- Multi-tenant isolation and per-tenant resources\n- Dataflow streaming with enrichment\n- Per-tenant BigQuery datasets and partitioning\n- Cloud Tasks and Cloud Run integration\n- IAM least privileges, VPC Service Controls, cross-region DR\n- Observability: logs, metrics, traces; testing strategies\n\n## Code Example\n```javascript\n// Example pseudo-config for tenant isolation\nconst tenantId = getTenant();\n```\n\n## Follow-up Questions\n- How would you handle late-arriving events in Dataflow?\n- How would you audit cross-tenant data access across regions?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-12",
        "questionId": "q-11961",
        "question": "Design an end-to-end GCP data ingestion and analytics pipeline for multi-tenant device telemetry. Vendors upload per-tenant JSONL compressed to Cloud Storage via signed URLs; build an event-driven path (Storage -> Pub/Sub -> Dataflow) that ingests into per-tenant BigQuery datasets with schema evolution, late-arrival handling, and per-environment isolation (dev/stage/prod). Include privacy, access governance, and cost controls; outline testing and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant pipeline: vendors upload a compressed JSONL per tenant to tenant-specific Cloud Storage via signed URLs. A Storage event publishes to a per-tenant Pub/Sub topic; Dataflow reads, validates a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Architecture Overview Design a three-tiered Dataflow pipeline that processes daily JSONL event logs from Cloud Storage through PII redaction and delivers sanitized data to both Cloud Storage and BigQuery on a per-ten…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A Dataflow batch pipeline pulls daily CSVs from the partner bucket, validates against a schema registry, then loads into per-environment partitioned BigQuery datasets. Use separate input buckets and B",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I’d implement three Cloud Run services (api-dev, api-stg, api-prod) with isolated IAM and revisions, a Cloud Build trigger on main that builds a Docker image, pushes to Artifact Registry, and deploys",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design a scalable, secure, and observable multi-tenant ingestion path on GCP with evolving schemas and strict governance.\n\n## Key Concepts\n- Tenant isolation in Storage and BigQuery\n- Event-driven path: Storage -> Pub/Sub -> Dataflow\n- Schema evolution and late-arrival handling\n- Privacy controls, IAM least privilege, and VPC Service Controls\n- Cost controls with per-tenant quotas and autoscaling\n\n## Code Example\n```javascript\n// Example Cloud Function (Node.js) triggered by Pub/Sub\nexports.ingestTelemetry = (event, context) => {\n  const data = Buffer.from(event.data, 'base64').toString();\n  const json = JSON.parse(data);\n  // Validate and write to BigQuery\n};\n```\n\n## Follow-up Questions\n- How would you test schema evolution and backfills?\n- How would you enforce per-tenant cost quotas and detect anomalies?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-13",
        "questionId": "q-17824",
        "question": "Design a tenant-aware ingestion path for a multi-tenant analytics API on GCP. Each tenant must have data residency guarantees, strict isolation, and per-tenant quotas. Outline the architecture, data flows, and security model (IAM, service accounts, and network). Include idempotent ingestion, observability, rollback, and end-to-end testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a beginner-friendly data ingestion flow on GCP: daily partner CSV exports uploaded to Cloud Storage. Build a lightweight pipeline using Cloud Functions (Python) triggered on finalization to val",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement structured logging in Cloud Run with correlation IDs for request tracing, apply PII redaction before log export using custom filtering functions, and store redacted logs in environment-isolated Cloud Storage b…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a tenant-aware ingestion path: an API Gateway routes to a per-tenant Cloud Run service; each tenant has its own region-bound Cloud Storage bucket and Pub/Sub topic; a dedicated service account",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest Pub/Sub events per environment through a Dataflow streaming pipeline, deduplicate by event_id using a stateful DoFn, and write to per-tenant, partitioned BigQuery tables. Enforce isolation with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design multi-tenant, region-aware ingestion pipelines with strict isolation, per-tenant quotas, and robust observability on GCP.\n\n## Key Concepts\n- Per-tenant isolation and data residency\n- Idempotent ingestion using event_id and manifests\n- Event-driven architecture with API Gateway, Pub/Sub, and Cloud Run\n- Least-privilege IAM and per-tenant service accounts\n- Observability via Cloud Logging/Monitoring and end-to-end tests\n\n## Code Example\n```yaml\n# Sketch: per-tenant resources (high level)\napiGateway:\n  routes:\n    - path: /ingest/{tenantId}\n      target: cloudrun://tenant-ingest-{tenantId}\ntenantResources:\n  - tenantId: string\n    bucket: gs://${TENANT_BUCKET_${tenantId}}\n    topic: projects/${PROJECT_ID}/topics/tenant-${tenantId}\n    dataset: ${TENANT_DATASET}_${tenantId}\n```\n\n## Follow-up Questions\n- How would you validate idempotency at scale across tenants?\n- How would you enforce per-tenant quotas and handle bursts?\n- What rollback steps would you implement per-tenant, and how would you test them?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-14",
        "questionId": "q-6420",
        "question": "Design a cost-aware, compliant data ingestion and analytics pipeline on Google Cloud for a fintech partner delivering real-time event streams (JSON) to per-tenant BigQuery datasets with strict tenant isolation, cross-region disaster recovery, and least-privilege IAM. Use Pub/Sub, Dataflow, BigQuery, Cloud Logging, and Cloud Monitoring; include auto-scaling, data lineage, and a rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest GPS events to Pub/Sub, process in Dataflow (Beam) with per-tenant fraud checks, write to per-tenant tables in Spanner and BI to BigQuery with tenant-scoped views. Use Private Service Connect an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant data plane: route real-time JSON events via Pub/Sub with tenant_id to a streaming Dataflow job that writes into per-tenant BigQuery datasets in the primary region; replicate to a secondary region…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Ingest regional events to regional Pub/Sub, process with Dataflow streaming templates, write results to regionally isolated BigQuery datasets encrypted with CMEK, and emit audit trails to Cloud Loggin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest tenant-scoped data feeds via Pub/Sub, process messages in a Cloud Run worker to redact PII (e.g., emails, phone numbers) from JSON/CSV payloads, and write redacted results to per-tenant Cloud Storage buckets. Ens…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design tenant-isolated, scalable data pipelines that balance cost, disaster recovery, and governance for real-time analytics in regulated fintech environments.\n\n## Key Concepts\n\n- Tenant isolation via per-tenant datasets/projects and dedicated service accounts\n- Pub/Sub schema design and routing by tenant_id\n- Dataflow streaming with idempotent writes and checkpointing\n- Cross-region disaster recovery and data replication\n- IAM least privilege and cost governance with Budgets/Alerts\n- Auto-scaling with Dataflow flex templates and BigQuery reservations\n- Data lineage through Cloud Data Catalog integration\n- Monitoring and alerting with Cloud Logging/Monitoring\n- Rollback capabilities using versioned templates and time travel\n\n## Code Example\n\n```python\n# Dataflow streaming pipeline skeleton: route by tenant\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\nclass TenantRouting(beam.DoFn):\n    def process(self, element, tenant_id):\n        # Route to tenant-specific BigQuery table\n        yield beam.pvalue.TaggedOutput(f'tenant_{tenant_id}', element)\n\nwith beam.Pipeline(options=PipelineOptions()) as p:\n    events = (p \n              | 'ReadFromPubSub' >> beam.io.ReadFromPubSub(subscription='tenant-events')\n              | 'ParseJSON' >> beam.Map(lambda x: json.loads(x))\n              | 'ExtractTenant' >> beam.WithKeys(lambda x: x['tenant_id'])\n              | 'RouteByTenant' >> beam.ParDo(TenantRouting()))\n    \n    # Write to tenant-specific BigQuery tables\n    for tenant in tenant_list:\n        (f'tenant_{tenant}' >> events\n         | f'WriteToBQ_{tenant}' >> beam.io.WriteToBigQuery(\n             table=f'project_tenant_{tenant}.dataset.events'))\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-15",
        "questionId": "q-29956",
        "question": "Design a real-time, multi-tenant event ingestion pipeline on Google Cloud for a SaaS product. Inbound partner webhooks arrive at Cloud Run, validate using HMAC, deduplicate by (tenant_id, event_id), and publish to per-tenant Pub/Sub topics. A Dataflow streaming job consumes these topics and writes to per-tenant, date-partitioned BigQuery datasets. Enforce least-privilege IAM, CMEK, VPC Service Controls, and data residency by region. Include observability, testing strategy, and failover considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger a per-tenant purge via Cloud Scheduler→Dataflow. Data in Cloud Storage: run DLP to locate PII, redact or delete objects older than policy, track with a purge manifest. BigQuery: delete or arch",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a data-quality driven ingestion for daily CSV exports into GCS. A Cloud Function (Python) reads each file and applies JSON-defined rules stored in a separate bucket (type, non-null, range check",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a real-time, multi-tenant ingest: Cloud Run validates HMAC signatures, deduplicates by (tenant_id, event_id), and publishes to per-tenant Pub/Sub topics. A Dataflow streaming job writes to p",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest events via per-tenant Pub/Sub topics, with a streaming Dataflow job that key-compacts by tenant and load_id to achieve exactly-once semantics. Write to per-tenant, partitioned BigQuery datasets",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design real-time, multi-tenant data pipelines with strong isolation, regulatory residency, and secure access controls in GCP. Probes observability and testing strategies.\n\n## Key Concepts\n\n- Real-time ingestion with Cloud Run, Pub/Sub, Dataflow\n- Tenant isolation: per-tenant topics and datasets\n- HMAC signature validation, deduplication\n- IAM least privilege, CMEK, VPC Service Controls\n- Data residency by region, resilience and failover\n\n## Code Example\n\n```python\nimport hmac, hashlib\n\ndef is_valid_signature(payload, secret, signature):\n    mac = hmac.new(secret.encode(), payload, hashlib.sha256).hexdigest()\n    return hmac.compare_digest(mac, signature)\n```\n\n## Follow-up Questions\n\n- How would you test cross-tenant data residency changes?\n- How would you validate exactly-once processing at scale?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-16",
        "questionId": "q-30373",
        "question": "Design a global telemetry ingestion pipeline for a SaaS on GCP where customers select a data residency region (US/EU/APAC). Ingest real-time events via Pub/Sub, process with Dataflow, store raw in per-region Cloud Storage, and load per-tenant aggregates into per-region BigQuery datasets. Enforce per-tenant IAM, per-tenant KMS keys, cost controls, and a two-region failover plan. Explain trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant isolation via separate Cloud SQL schemas or databases and per-tenant service accounts. Bind each Cloud Run service to a tenant-scoped IAM role with conditions on a tenant_id label; requ",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Region-scoped, tenant-isolated telemetry pipeline using Pub/Sub per region streams, a Dataflow streaming job, raw data stored in region-specific Cloud Storage, and per-tenant aggregates in region BigQ",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a GitOps-style pipeline: push triggers per env; build images in Artifact Registry; enforce Binary Authorization to allow only signed images; generate SBOM and run container scans; enforce per-tena",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-environment buckets, a finalization Cloud Function that validates schema, runs DLP to redact PII, writes cleaned data to a processed bucket, and loads into a partitioned BigQuery table.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a compliant, scalable multi-region pipeline with data residency, tenant isolation, and security controls across Pub/Sub, Dataflow, Cloud Storage, and BigQuery. Requires thinking through cost governance and DR trade-offs.\n\n## Key Concepts\n- Data residency and tenant isolation across regions\n- Pub/Sub topic routing by region\n- Dataflow streaming pipelines with per-tenant schemas\n- Region-specific Cloud Storage and BigQuery datasets\n- Per-tenant IAM roles and Cloud KMS CMKs\n- Cost governance and disaster-recovery considerations\n\n## Code Example\n```python\n# Pseudo Dataflow skeleton: read Pub/Sub, windowed aggregations, write BigQuery\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\ndef run():\n    with beam.Pipeline(options=PipelineOptions()) as p:\n        (p | 'Read' >> beam.io.ReadFromPubSub(topic='projects/..../region-topic')\n           | 'Window' >> beam.WindowInto(beam.window.SlidingWindows(60, 5))\n           | 'Group' >> beam.GroupByKey()\n           | 'Write' >> beam.io.WriteToBigQuery(table='project:dataset.table'))\n```\n\n## Follow-up Questions\n- How to onboarding tenants and migrating data between regions with minimal downtime?\n- What observability and alerting would you add to guarantee data residency compliance and cost controls?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-17",
        "questionId": "q-8230",
        "question": "Design an intermediate‑level GCP data ingestion architecture for daily partner CSVs arriving in a shared GCS bucket: implement per‑environment isolation (dev/stage/prod) with separate buckets and datasets, validate schema via a registry, perform idempotent loads into partitioned BigQuery, expose results to downstream teams via Authorized Views, and ensure observability with Cloud Logging plus correlation IDs. Include error handling for late data and a rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest regional events to regional Pub/Sub, process with Dataflow streaming templates, write results to regionally isolated BigQuery datasets encrypted with CMEK, and emit audit trails to Cloud Loggin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a Cloud Run API that checks Redis cache for a flag; on miss, read from Firestore under the tenant namespace, then cache the value with a TTL and return it. Provide a write path to update the fla",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A Dataflow batch pipeline pulls daily CSVs from the partner bucket, validates against a schema registry, then loads into per-environment partitioned BigQuery datasets. Use separate input buckets and B",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a GitOps-style pipeline: push triggers per env; build images in Artifact Registry; enforce Binary Authorization to allow only signed images; generate SBOM and run container scans; enforce per-tena",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real‑world patterns: per‑env isolation, schema validation, idempotent ingest, and auditable sharing. It also probes handling of late data, rollbacks, and observability.\n\n## Key Concepts\n- Per-environment isolation with separate buckets and BigQuery datasets\n- Central schema registry for validation\n- Idempotent loads via file checksums or load manifests\n- Authorized Views for downstream access control\n- Cloud Logging with correlation_id for end‑to‑end tracing\n- Late-arriving data handling and rollback strategy\n\n## Code Example\n```javascript\n// Pseudo Dataflow skeleton (illustrative)\n// 1. Read CSVs from env-specific GCS bucket\n// 2. Validate against registry schema\n// 3. Write to per-env partitioned BigQuery\n``` \n\n## Follow-up Questions\n- How would you test idempotency and rollback across environments?\n- How would schema evolution be rolled out without downtime?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-18",
        "questionId": "q-7212",
        "question": "Design a compliant, audit-ready multi-tenant data platform on Google Cloud for a fintech app with strict data residency, no public endpoints, and per-tenant isolation. Explain how you would configure resource boundaries (projects/folders), implement org policies and IAM least privilege, use CMEK for storage, Private Service Connect, and VPC Service Controls, plus policy-as-code and drift detection. Include a test plan and rollback approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a streaming ingestion: Pub/Sub → Dataflow (Beam) pipeline that validates and enriches events with tenant_id and event_ts, deduplicates by event_id per tenant, and writes to per-tenant BigQue",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route events using Pub/Sub message attributes to carry tenant identification. A Dataflow streaming job consumes messages and writes to per-tenant BigQuery datasets using tenant-specific service accounts. Enforce IAM con…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a compliant, audit-ready multi-tenant data platform on GCP with per-tenant isolation, no public endpoints, and strict data residency. Use folders/projects per tenant, least-privilege IAM, or",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a tenant-aware ingestion path: an API Gateway routes to a per-tenant Cloud Run service; each tenant has its own region-bound Cloud Storage bucket and Pub/Sub topic; a dedicated service account",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess ability to design governance-heavy architecture with strict isolation and compliance.\n\n## Key Concepts\n- Per-tenant isolation using GCP resource hierarchy\n- Organizational policies, least privilege, CMEK\n- Private connectivity (Private Service Connect, VPC Service Controls)\n- Policy-as-code and drift detection\n- Auditability and data residency requirements\n\n## Code Example\n```terraform\nprovider \"google\" {\n  project = \"org-policy-demo\"\n}\nresource \"google_project\" \"tenant\" {\n  name       = \"Tenant A\"\n  project_id = \"tenant-a\"\n  org_id     = \"123456789\"\n}\n```\n\n## Follow-up Questions\n- How would you test drift detection and rollback in prod?\n- How do you handle cross-tenant data access if a shared service is required?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-19",
        "questionId": "q-17567",
        "question": "Design a cost-conscious, multi-tenant streaming analytics pipeline on GCP for a real-time chat app. Ingest events via Pub/Sub, process with a Dataflow streaming job that isolates tenants (tenant_id partitioning and per-tenant datasets), handles late data, and applies per-tenant billing labels. Store results in partitioned BigQuery tables; expose a read API. Include observability, testing plan, and disaster recovery plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pub/Sub as ingress; messages include tenant_id and message_id. Create per-tenant BigQuery datasets and a Cloud Run worker with a dedicated service account per tenant to transform and insert rows i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Dataflow streaming reads Pub/Sub messages, extracting the tenant_id, and writes to per-tenant BigQuery datasets partitioned by day. It uses event-time windows with allowed lateness to handle late data and ensures idempo…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design an event-driven workflow using Cloud Storage, Pub/Sub, and Cloud Run (Python). Each tenant maintains a dedicated bucket and service account for strict isolation. The validator verifies the manifest, checks file c…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a header like X-Correlation-Id passed across HTTP ingress, Cloud Run, and Cloud Functions; instrument both services with OpenTelemetry (OTLP export) to Cloud Trace, Cloud Logging, and Cloud Monito",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design multi-tenant, cost-aware streaming pipelines with strong isolation, observability, and recoverability.\n\n## Key Concepts\n- **Tenant isolation**: Per-tenant datasets, IAM least privilege, and VPC Service Controls ensure strong tenant boundaries.\n- **Streaming semantics**: Event-time windows, allowed lateness, and idempotent writes guarantee correct processing of out-of-order and late-arriving data.\n- **Cost controls**: Per-tenant billing labels, quotas, and careful resource sizing optimize costs across tenants.\n- **Disaster recovery**: Regional replication and per-tenant rollback manifests enable rapid recovery.\n\n## Code Example\n```javascript\n// Pseudo Dataflow skeleton (not runnable)\nclass TenantPipeline {\n  constructor(tenantId, projectId) {\n    this.tenantId = tenantId;\n    this.projectId = projectId;\n    this.datasetId = `tenant_${tenantId}`;\n  }\n\n  buildPipeline(pubsubSubscription, watermarkDelayMs = 60000) {\n    return (\n      pipeline\n        .apply('ReadFromPubSub', PubsubIO.readMessages()\n          .fromSubscription(this.pubsubSubscription)\n          .withTimestampAttribute('event_timestamp'))\n        .apply('ExtractTenant', ParDo.of(new ExtractTenantDoFn()))\n        .apply('WindowInto', Window.into(FixedWindows.of(60 * 60 * 1000))\n          .withAllowedLateness(watermarkDelayMs)\n          .triggering(AfterWatermark.pastEndOfWindow()\n            .withEarlyFirings(AfterProcessingTime.pastFirstElementInPane()\n              .plusDelayOf(5 * 60 * 1000))))\n        .apply('ApplyTenantLabels', ParDo.of(new AddBillingLabelsDoFn(this.tenantId)))\n        .apply('WriteToBigQuery', BigQueryIO.write()\n          .to(tableSpec => this.getTableSpec(tableSpec))\n          .withSchema(this.getSchema())\n          .withWriteDisposition(BigQuery.WriteDisposition.WRITE_APPEND)\n          .withCreateDisposition(BigQuery.CreateDisposition.CREATE_NEVER));\n    }\n  }\n\n  getTableSpec(tableSpec) {\n    return `${this.projectId}:${this.datasetId}.${tableSpec}`;\n  }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-20",
        "questionId": "q-3071",
        "question": "Design an advanced, scalable, tenant-aware analytics pipeline on Google Cloud for a SaaS product that streams events from many tenants. Ingest via Pub/Sub, process with Dataflow (Python), and write per-tenant results to a shared BigQuery dataset. Enforce access with BigQuery Row-Level Security and IAM Conditions, and export per-tenant aggregates to dedicated Cloud Storage buckets via signed URLs. Include per-tenant isolation, least-privilege IAM, auditability, rollback, and a testing strategy. How would you approach end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest client events to Pub/Sub; a Cloud Run worker processes messages with idempotent deduplication (event_id); write per-tenant rows to separate BigQuery datasets and daily aggregates to per-tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant buckets and datasets; Pub/Sub per-tenant topics; Dataflow template ingests events into day-partitioned BigQuery tables, with CMEK per tenant. Enforce IAM least privilege and per-tenant",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would design a tenant-aware analytics pipeline using a single shared Pub/Sub topic per environment with tenant_id embedded in each event. The Dataflow Python pipeline would extract tenant_id from events, validate agai…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-tenant data plane: route real-time JSON events via Pub/Sub with tenant_id to a streaming Dataflow job that writes into per-tenant BigQuery datasets in the primary region; replicate to a secondary region…",
            "isCorrect": false
          }
        ],
        "explanation": "This question evaluates expertise in designing multi-tenant streaming analytics architectures with strict data isolation and granular access controls. Key concepts include: Pub/Sub for scalable event ingestion, Dataflow Python for stream processing with tenant routing, BigQuery Row-Level Security and IAM Conditions for data isolation, Cloud Storage with signed URLs for tenant exports, and comprehensive governance through auditing, rollback mechanisms, and testing strategies. The response should demonstrate understanding of cross-service integration, security best practices, and operational considerations for enterprise SaaS platforms.",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-21",
        "questionId": "q-4492",
        "question": "In a multi-tenant SaaS on GCP with 12+ tenants, design a streaming data ingestion and analytics pipeline that preserves strict per-tenant isolation, uses per-tenant encryption keys, and scales to peak load. Data arrives via Pub/Sub; process with a Dataflow streaming template; land in per-tenant BigQuery datasets. Describe security (IAM, encryption, VPC), idempotency (dedupe, MERGE), error handling, and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set up: Pub/Sub topic feeds events into a Python Dataflow streaming pipeline; validate JSON, extract tenant_id, and emit to per-tenant BigQuery tables partitioned by ingestion_date. Data model: tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose tenant isolation using either per-tenant projects with dedicated IAM bindings or an in- project data model with tenant_id prefixes. Use least privilege service accounts for Cloud Run, Firestor",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Dataflow streaming template subscribing to Pub/Sub; route events to per-tenant BigQuery datasets; implement idempotency with a staging table and MERGE into final tables using event_id; enforce p",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a Dataflow streaming job triggered by GCS events to parse each new file, validate against a schema, compute a file checksum, and drop duplicates using a per-file key table. Route records to",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design scalable, secure, multi-tenant data pipelines on GCP with strong isolation, encryption, and observability. Tests knowledge of streaming patterns, idempotent processing, and operational maturity.\n\n## Key Concepts\n\n- Streaming ingestion using Pub/Sub and Dataflow\n- Multi-tenant isolation via datasets/projects and IAM/per-tenant keys\n- Idempotent processing with staging + MERGE\n- Private egress (PSC) and VPC considerations\n- Observability: structured logging, dashboards, alerts\n\n## Code Example\n\n```python\n# Pseudo Dataflow skeleton: reads Pub/Sub, dedup by event_id, writes to BigQuery with MERGE\n# Real implementation would parameterize tenant_id routing and keys\n```\n\n## Follow-up Questions\n\n- How handle late-arriving data and schema evolution?\n- What are cost-controls and scaling strategies under traffic spikes?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-22",
        "questionId": "q-3537",
        "question": "Design a beginner-friendly GCP pipeline that ingests daily JSON logs from partner-signed URLs into a regional GCS bucket, then uses a Dataflow (Python Beam) batch job to redact PII fields (email, phone) and load sanitized records into per-environment BigQuery tables. Include per-environment isolation (dev/stage/prod), a simple idempotent per-file checksum strategy, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Dataflow batch pipeline reading new files via FileIO, apply a ParDo DoFn to redact PII, and write to per-environment BigQuery tables (one dataset per env). Compute a per-file SHA256 checksum to",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-tenant real-time fraud score: Pub/Sub ingress, Dataflow (Beam) with tenant_id key, windowed aggregates for risk scores, sink to tenant-scoped BigQuery datasets, and a per-tenant Cloud Run API fron",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest daily partner CSV files into Cloud Storage, triggering a Python Cloud Function on finalization that leverages Cloud DLP API to classify and redact PII content. Process redacted data into environment-specific BigQ…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest JSON telemetry to Pub/Sub, process with Dataflow (Beam) to normalize fields and apply idempotent deduping (insertId) before writing to a single BigQuery dataset that includes a tenant_id. Use p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProbes practical data privacy, per-environment isolation, and idempotent processing in a real GCP workflow. It also tests ability to translate a policy (PII redaction) into concrete transforms and tests.\n\n## Key Concepts\n- Dataflow (Python Beam) batch processing\n- DoFn-based PII redaction\n- per-file checksum for idempotency\n- environment isolation with separate buckets and datasets\n- basic testing strategy (unit and integration)\n\n## Code Example\n```python\nclass RedactPIIFn(beam.DoFn):\n    def process(self, element):\n        obj = json.loads(element)\n        for f in [\"email\",\"phone\"]:\n            if f in obj:\n                obj[f] = \"***REDACTED***\"\n        yield json.dumps(obj)\n```\n\n## Follow-up Questions\n- How would you adapt to changing JSON schemas?\n- How would you test end-to-end with large files and ensure idempotency under retries?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-23",
        "questionId": "q-15733",
        "question": "Design a beginner-friendly, secure, multi-tenant API on GCP that ingests events via API Gateway, queues processing with Cloud Tasks to a Cloud Run worker, writes raw events to per-tenant Cloud Storage, and stores aggregates in per-tenant BigQuery tables. Include per-tenant isolation, API-key auth, idempotent processing, observability, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a beginner-friendly data ingestion flow on GCP: daily partner CSV exports uploaded to Cloud Storage. Build a lightweight pipeline using Cloud Functions (Python) triggered on finalization to val",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Ingestion Architecture** Tenants upload daily CSV files to per-tenant paths in Cloud Storage (`gs://tenant-data-{tenant_id}/imports/`). A Cloud Function (triggered by GCS object finalization via Eventarc) invokes a Da…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A strong answer would outline: API Gateway to publish events to Cloud Tasks; a Cloud Run worker processes jobs; tenant_id drives per-tenant buckets gs://tenant-<id>/raw; per-tenant ServiceAccount; API",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a streaming Dataflow job (Python) reading from per-tenant Pub/Sub topics, normalizing fields, validating schemas, and upserting into per-tenant BigQuery datasets (or a tenant_id-partitioned table)",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical skills in designing a multi-tenant, event-driven API on GCP using API Gateway, Cloud Tasks, and Cloud Run, with per-tenant isolation and observable patterns. It focuses on real components, trade-offs, and testability.\n\n## Key Concepts\n- API Gateway\n- Cloud Tasks\n- Cloud Run\n- per-tenant isolation (buckets and service accounts)\n- idempotent processing\n- observability (Cloud Logging/Monitoring)\n\n## Code Example\n```javascript\n// Pseudo dedupe check against Firestore\nasync function isDuplicate(eventId, tenantId) {\n  const doc = await Firestore.collection('dedup').doc(`${tenantId}_${eventId}`).get();\n  if (doc.exists) return true;\n  await Firestore.collection('dedup').doc(`${tenantId}_${eventId}`).set({ seenAt: Date.now() });\n  return false;\n}\n```\n\n## Follow-up Questions\n- How would you test idempotency and failure paths end-to-end?\n- How would you monitor and alert on retries or quota exhaustion in Cloud Tasks?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-24",
        "questionId": "q-25093",
        "question": "Design a streaming pipeline in GCP for a multi-tenant SaaS like Slack: ingest events from Pub/Sub, deduplicate by event_id using a stateful Dataflow DoFn, and write per-tenant, partitioned BigQuery tables. Enforce per-tenant isolation with separate service accounts, IAM, and GCS buckets; implement exactly-once semantics with streaming inserts and idempotent writes. Include a cross-region DR plan and a practical test approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a per-tenant data plane: isolated BigQuery datasets and per-tenant GCS buckets with CMEK and IAM Conditions for least privilege. Ingest partner CSVs into a staging bucket via signed URLs; Da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose tenant isolation using either per-tenant projects with dedicated IAM bindings or an in- project data model with tenant_id prefixes. Use least privilege service accounts for Cloud Run, Firestor",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a streaming ingestion with one Pub/Sub topic per partner and a Dataflow pipeline that deduplicates and writes exactly-once to a per-partner, per-environment BigQuery dataset (partitioned by day).",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest Pub/Sub events per environment through a Dataflow streaming pipeline, deduplicate by event_id using a stateful DoFn, and write to per-tenant, partitioned BigQuery tables. Enforce isolation with",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a real, per-tenant scalable pipeline with strong isolation guarantees and production DR. It also probes deduplication and exactly-once semantics in a streaming context.\n\n## Key Concepts\n- Dataflow streaming and windowing\n- Pub/Sub ingestion patterns\n- BigQuery partitioning and per-tenant isolation\n- Stateful DoFn for deduplication\n- IAM least privilege and cross-project isolation\n- Cross-region disaster recovery\n\n## Code Example\n```python\n# Pseudo-code for a stateful dedup DoFn\nclass Dedup(beam.DoFn):\n    def process(self, element, state=beam.DoFn.StateParam):\n        event_id = element['event_id']\n        if event_id in state.read():\n            return\n        state.add(event_id)\n        yield element\n```\n\n## Follow-up Questions\n- How would you verify exactly-once in presence of late data?\n- How would you test cross-region failover and rollback?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-11T15:56:29.591Z",
    "version": 1
  }
]