[
  {
    "id": "test-gcp-cloud-architect",
    "channelId": "gcp-cloud-architect",
    "channelName": "GCP Cloud Architect",
    "title": "GCP Cloud Architect Knowledge Test",
    "description": "Test your understanding of GCP Cloud Architect with 25 questions.",
    "questions": [
      {
        "id": "gcp-cloud-architect-q-0",
        "questionId": "q-14682",
        "question": "Design a beginner-friendly, cost-conscious data redaction workflow on GCP for daily event logs stored in Cloud Storage as JSONL. Build a Dataflow (Python) batch pipeline that reads new files, redacts PII (emails, phones, card numbers) via DLP or regex, and writes per-tenant redacted data to Cloud Storage and corresponding per-tenant tables in BigQuery. Include environment isolation (dev/stage/prod), rollback, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant log sinks via tenant_id labels, restrict log access with IAM roles scoped to each tenant, redact PII before logging (emails, IDs), and create per-tenant log-based metrics for Clou",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Architecture Overview Design a three-tiered Dataflow pipeline that processes daily JSONL event logs from Cloud Storage through PII redaction and delivers sanitized data to both Cloud Storage and BigQuery on a per-ten…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement cross-region DR by replicating Pub/Sub to a paired region and running a streaming Dataflow pipeline that writes per-tenant aggregates to BigQuery in both primary and DR projects. Use a warm",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Organize per-tenant isolation with separate Cloud Storage buckets and BigQuery datasets, driven by a Dataflow (Beam) streaming pipeline from Pub/Sub to storage and then into per-tenant datasets. Enfor",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical GCP data engineering skills, specifically:\n- Real-time batch pipeline design using managed services\n- Data privacy compliance through automated PII redaction\n- Multi-tenant architecture implementation\n- Production considerations including monitoring, cost optimization, and disaster recovery\n\n## Key Concepts\n\n**Dataflow Processing:**\n- Apache Beam Python SDK for distributed batch processing\n- Windowing strategies for daily log processing\n- Dynamic destination splitting for per-tenant outputs\n\n**PII Redaction Approaches:**\n- Cloud DLP API: Comprehensive but higher cost, slower throughput\n- Regex patterns: Faster, cheaper but limited coverage\n- Hybrid strategy balances cost vs. accuracy requirements\n\n**GCP Integration Patterns:**\n- Cloud Storage for scalable file-based input/output\n- BigQuery partitioned tables for analytics and time-travel\n- Cloud Functions for event-driven pipeline orchestration\n\n## Code Example\n\n```python\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nimport re\nimport json\nfrom google.cloud import dlp_v2\nimport hashlib\n\nclass PIIRedactor(beam.DoFn):\n    def __init__(self, use_dlp=True, sample_rate=0.1):\n        self.use_dlp = use_dlp\n        self.sample_rate = sample_rate\n        if use_dlp:\n            self.dlp_client = dlp_v2.DlpServiceClient()\n            self.parent = f\"projects/{os.getenv('GCP_PROJECT')}/locations/global\"\n    \n    def redact_with_regex(self, text):\n        # Email redaction\n        text = re.sub(r'\\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}\\b', \n                     '[REDACTED_EMAIL]', text)\n        # Phone redaction (simplified)\n        text = re.sub(r'\\b\\d{3}-?\\d{3}-?\\d{4}\\b', '[REDACTED_PHONE]', text)\n        # Credit card redaction\n        text = re.sub(r'\\b\\d{4}[\\s-]?\\d{4}[\\s-]?\\d{4}[\\s-]?\\d{4}\\b', \n                     '[REDACTED_CARD]', text)\n        return text\n    \n    def process(self, element):\n        import random\n        tenant_id, record = element\n        \n        # Hybrid approach: sample for DLP, use regex for rest\n        if self.use_dlp and random.random() < self.sample_rate:\n            # DLP-based redaction for sample\n            text = json.dumps(record)\n            inspect_config = {\n                'info_types': [{'name': 'EMAIL_ADDRESS'}, \n                              {'name': 'PHONE_NUMBER'}, \n                              {'name': 'CREDIT_CARD_NUMBER'}]\n            }\n            response = self.dlp_client.redact_content(\n                request={'parent': self.parent, \n                        'item': {'value': text},\n                        'inspect_config': inspect_config}\n            )\n            redacted_text = response.item.value\n        else:\n            # Regex-based redaction for bulk\n            redacted_text = self.redact_with_regex(json.dumps(record))\n        \n        yield (tenant_id, json.loads(redacted_text))\n\nclass TenantSplitter(beam.PTransform):\n    def expand(self, pcoll):\n        return (pcoll\n                | 'ExtractTenant' >> beam.Map(\n                    lambda x: (x.get('tenant_id', 'default'), x))\n                | 'RedactPII' >> beam.ParDo(PIIRedactor())\n                | 'WindowByTenant' >> beam.WindowInto(\n                    beam.window.FixedWindows(300)))  # 5-min windows\n\ndef run_pipeline():\n    options = PipelineOptions([\n        '--project', os.getenv('GCP_PROJECT'),\n        '--region', 'us-central1',\n        '--temp_location', 'gs://dataflow-temp/tmp',\n        '--staging_location', 'gs://dataflow-temp/staging'\n    ])\n    \n    with beam.Pipeline(options=options) as p:\n        (p\n         | 'ReadFiles' >> beam.io.ReadFromText(\n             'gs://logs-raw/*/*.jsonl')\n         | 'ParseJSON' >> beam.Map(json.loads)\n         | 'SplitAndRedact' >> TenantSplitter()\n         | 'WriteToGCS' >> beam.io.WriteToText(\n             'gs://logs-redacted/',\n             file_name_suffix='.jsonl')\n         | 'WriteToBQ' >> beam.io.WriteToBigQuery(\n             table='events_{tenant_id}',\n             dataset='redacted_logs',\n             project=os.getenv('GCP_PROJECT'),\n             schema='tenant_id:STRING, event_data:STRING',\n             write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND,\n             create_disposition=beam.io.BigQueryDisposition.CREATE_IF_NEEDED))\n\nif __name__ == '__main__':\n    run_pipeline()\n```\n\n## Production Considerations\n\n**Cost Optimization:**\n- DLP API quotas and rate limiting\n- Dataflow autoscaling with max workers\n- BigQuery partition clustering for query efficiency\n\n**Monitoring & Alerting:**\n- Cloud Monitoring dashboards for pipeline metrics\n- Error budgets and SLO tracking\n- Automated failure notification via Pub/Sub\n\n**Security & Compliance:**\n- Service account IAM最小权限原则\n- Customer-managed encryption keys (CMEK)\n- Audit logging for all data access",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-1",
        "questionId": "q-21075",
        "question": "Design a real-time, multi-tenant analytics pipeline on GCP that ingests streaming JSON events from Cloud Pub/Sub, deduplicates and masks PII, and writes per-tenant results to partitioned BigQuery tables. Include per-environment isolation (dev/stage/prod), exactly-once delivery with DLQ, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant Pub/Sub ingestion, a streaming Dataflow job that deduplicates on event_id, materializes as Parquet in Cloud Storage, and then loads into per-tenant partitioned BigQuery tables.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a data-quality driven ingestion for daily CSV exports into GCS. A Cloud Function (Python) reads each file and applies JSON-defined rules stored in a separate bucket (type, non-null, range check",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Centralize Pub/Sub with a restricted SA for publishers; Dataflow uses a per‑tenant routing key to emit into tenant‑specific BigQuery datasets; grant minimal roles (Pub/Sub Editor/Viewer, BigQuery Data",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Pub/Sub trigger to a Dataflow streaming job (Python). Stateful dedupe on (tenant_id, event_id); apply Cloud DLP masking for PII; write to per-tenant, partitioned BigQuery tables with streaming i",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design a real-time, multi-tenant data pipeline with strong isolation, data masking, and exactly-once semantics. Tests practical choices for ingestion, deduplication, masking, storage layout, failure handling, and observability.\n\n## Key Concepts\n\n- Cloud Pub/Sub streaming ingestion\n- Dataflow (Python) with stateful deduplication\n- Data Loss Prevention (PII masking)\n- BigQuery per-tenant partitioned tables and load manifests\n- Exactly-once delivery and dead-letter handling\n- Environment isolation (dev/stage/prod)\n- Observability and testing strategy\n\n## Code Example\n\n```javascript\n// Pseudocode illustrating the pipeline steps (Dataflow-inspired, JS syntax for illustration)\nconst pipeline = createPipeline({ triggers: PubSubTrigger(topic) });\npipeline\n  .apply(deduplicate({ keys: ['tenant_id','event_id'] }))\n  .pipe(maskPII())\n  .toBigQuery({ perTenant: true, partition: 'DATE(event_ts)' })\n  .onError(dlqPublish);\n```\n\n## Follow-up Questions\n\n- How would you test idempotence and dedupe under replayed events?\n- What monitoring would you add to catch data skew across tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-2",
        "questionId": "q-24300",
        "question": "Design an advanced, multi-tenant data ingestion and analytics pipeline on Google Cloud for a SaaS platform. Each tenant has isolated BigQuery datasets, with streaming data from Pub/Sub into per-tenant tables via Dataflow. Include per-tenant IAM, CMEK-protected storage, data lineage, audit logging, and a disaster-recovery strategy across regions with automated failover. Provide cost controls and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Python Cloud Function triggered by CSV upload finalization to a staging Cloud Storage bucket. The function invokes Cloud DLP to scan for PII/PCI data; flagged files are automatically moved to a quarantine bucke…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant isolation via separate Cloud SQL schemas or databases and per-tenant service accounts. Bind each Cloud Run service to a tenant-scoped IAM role with conditions on a tenant_id label; requ",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I’d implement per-tenant datasets in BigQuery with a tenant-scoped service account and IAM bindings. A single Dataflow streaming job subscribes to a per-tenant Pub/Sub topic and writes to that tenant’",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant Pub/Sub topics feed a single Dataflow streaming pipeline that writes to per-tenant BigQuery datasets. Enforce tenant isolation with IAM Conditions and per-tenant view-based access, encrypt",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design scalable, isolated multi-tenant data pipelines with strong governance and DR.\n\n## Key Concepts\n- Tenant isolation: per-tenant BigQuery datasets and IAM bindings\n- Streaming ingestion: Pub/Sub + Dataflow with idempotent writes\n- Security: CMEK, IAM least privilege, Cloud Audit Logs\n- DR: cross-region replication and failover, budget controls\n\n## Code Example\n```javascript\n// Pseudo Dataflow template config (illustrative)\nconst tenant = process.env.TENANT_ID;\npubsub.subscription = `projects/PROJ/subscriptions/${tenant}`;\nBigQueryIO.write(`dataset_${tenant}.table_${tenant}`)\n   .withInsertIdFn(record => record.id)\n   .withCreateDisposition('CREATE_IF_NEEDED');\n```\n\n## Follow-up Questions\n- How would you automate tenant onboarding/teardown and IAM bindings?\n- How would you validate data lineage and perform cost forecasting at scale?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-3",
        "questionId": "q-878",
        "question": "How would you implement a beginner-friendly, auditable deployment pipeline in Google Cloud for a Cloud Run app that reads from Cloud SQL and writes logs to Cloud Logging, ensuring least-privilege IAM, per-project isolation, and no public endpoints?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-tenant streaming pipeline on GCP for IoT. Pub/Sub feeds a Dataflow stream routing events to per-tenant BigQuery partitions; a cross-tenant aggregate view powers dashboards. Enforce per-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub with tenant_id attribute; run a Dataflow streaming job that key-groups by tenant_id, writes to per-tenant BigQuery tables via BigQueryIO with insertId = tenant_id + event_id to enable dedu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant Pub/Sub topics with dedicated service accounts that subscribe only to their respective topics and write exclusively to tenant-scoped BigQuery datasets. Enforce least-privilege IAM with service accou…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a dedicated deploy service account per project with minimal roles (roles/run.admin, roles/iam.serviceAccountUser) and grant it to Cloud Build to trigger Cloud Run revisions. Use Private Service",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical knowledge of basic CI/CD in GCP with security and auditability for sensitive workloads.\n\n## Key Concepts\n- Least-privilege IAM across projects\n- Cloud Run, Cloud Build, Cloud SQL, Cloud Logging\n- Private Service Connect and internal networking\n- Cloud Audit Logs for governance\n\n## Code Example\n```javascript\n// Example: create SA and grant roles\ngcloud iam service-accounts create deploy-sa --display-name \"Deploy Service Account\"\ngcloud projects add-iam-policy-binding your-project-id --member \"serviceAccount:deploy-sa@your-project-id.iam.gserviceaccount.com\" --role \"roles/run.admin\"\n```\n\n## Follow-up Questions\n- How would you verify there are no public endpoints exposed to the internet for the deployed app?\n- Which logs and metrics would you route and store for auditability of deployments?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-4",
        "questionId": "q-6798",
        "question": "You are starting a data lake on GCP for partner exports: daily JSON/CSV files arrive via partner-signed URLs and land in a shared Cloud Storage bucket. Propose a beginner-friendly ingest path using Cloud Functions (Python) to validate and transform, a Dataflow batch job to convert to Parquet and load into per-tenant BigQuery partitions, and a lightweight schema registry in Cloud Firestore to handle schema evolution. Include environment isolation (dev/stage/prod), per-file idempotency, and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "In practice, use per-tenant Pub/Sub push subscriptions, a Dataflow streaming job keyed by tenant_id with idempotent deduplication, and write to separate BigQuery datasets per tenant. Enforce per-tenan",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a multi-tenant data plane with isolated per-tenant BigQuery datasets and Cloud Storage buckets, ingesting via Pub/Sub and processing in Dataflow. Use per-tenant service accounts with least priv",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "End-to-end: ingest to GCS from partner URLs; Cloud Function (Python) validates JSON/CSV, computes a checksum, and emits a load event to Pub/Sub. Dataflow batch reads manifests, converts to Parquet, an",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Adopt a single Cloud KMS key in a dedicated ring, with automatic 90‑day rotation. Grant the Cloud Run service account only cryptoKeyEncrypter and the minimal storage permissions. Enable CMEK on Cloud",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses end-to-end data ingestion design with beginner-friendly GCP services, including data quality gates, idempotency, and environment isolation. Emphasizes practical trade-offs between Cloud Functions and Dataflow, plus simple schema governance.\n\n## Key Concepts\n\n- GCS ingestion from partner exports and signed URLs\n- Cloud Functions (Python) for validation and transformation\n- Pub/Sub orchestration for decoupled workflow\n- Dataflow batch pipeline (Python) with Parquet output\n- BigQuery per-tenant partitions and data organization\n- Cloud Firestore as a lightweight schema registry\n- Environment isolation via separate projects\n- Idempotency via per-file checksum and load manifests\n\n## Code Example\n\n```python\n# Python: Cloud Function skeleton\nfrom google.cloud import storage\n\ndef gcs_to_pubsub(event, context):\n    bucket = event['bucket']\n    name = event['name']\n    data = _read_gcs(bucket, name)\n    if not _validate_schema(data):\n        raise ValueError(\"Invalid schema\")\n    checksum = _sha256(data)\n    _publish_to_pubsub({'name': name, 'checksum': checksum, 'bucket': bucket})\n```\n\n## Follow-up Questions\n\n- How would you handle schema evolution without breaking existing loads?\n- What monitoring would you implement for data freshness and failure recovery?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-5",
        "questionId": "q-22451",
        "question": "Design a real-time analytics pipeline on Google Cloud to process mobile app clickstream for a multi-tenant product. Ingest events from Pub/Sub per region, run a streaming Dataflow job with windowed aggregations to compute per-user engagement and funnel metrics, and store results in region- and tenant-scoped BigQuery tables. Save raw events to regional Cloud Storage, enforce per-tenant isolation (datasets and service accounts), implement exactly-once semantics with insertId, and provide observability and rollback strategy. Include testing plan and failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant isolation with separate projects, datasets, and buckets; Pub/Sub streams feed a per-tenant Dataflow streaming pipeline into BigQuery partitions; access via Private Service Connect; enforce",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a streaming analytics pipeline: ingest events from Pub/Sub per region into a Dataflow (Python) job that window-aggregates 5-minute segments to compute per-user engagement and funnel metrics,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a stateless Cloud Run REST API behind Cloud Endpoints to evaluate flags per tenant. Store per-tenant configs in Firestore, cache hot-paths in Memorystore, and enforce isolation with per-tenant",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Cloud Run service behind API Gateway that reads per-tenant flags from Firestore. The tenant_id selects a document containing the enabled flag and suffix configuration. Cache the result in memory for the duration o…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a scalable, compliant streaming pipeline with real-time analytics, tenant isolation, and robust observability on GCP. Emphasizes data residency, exactly-once semantics, and operational resilience.\n\n## Key Concepts\n- Pub/Sub regional ingestion and partitioning by region\n- Dataflow streaming with fixed windows and per-tenant aggregation\n- BigQuery datasets/tables per region and per tenant for isolation\n- Cloud Storage regional raw event landing\n- Exactly-once semantics using insertId and idempotent writes\n- Least-privilege IAM per tenant and service accounts\n- Observability: Cloud Monitoring dashboards, Cloud Logging, error budgets, tracing\n- Rollback and replay via load manifests and timestamp-based reprocessing\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nimport json\n\ndef parse(msg):\n    return json.loads(msg.data)\n\ndef key_by_tenant(event):\n    return (event['tenant_id'], event['region'])\n\nwith beam.Pipeline(options=PipelineOptions()) as p:\n    (p\n     | 'ReadFromPubSub' >> beam.io.ReadFromPubSub(subscription='projects/PROJECT/subscriptions/REGION-sub')\n     | 'Decode' >> beam.Map(lambda m: json.loads(m.decode('utf-8')))\n     | 'Window' >> beam.WindowInto(beam.window.FixedWindows(300))\n     | 'KeyTenant' >> beam.Map(lambda e: (e['tenant_id'], e))\n     | 'Aggregate' >> beam.CombinePerKey(beam.combiners.Count())\n     | 'ToBQ' >> beam.io.WriteToBigQuery(table='PROJECT:REGION.DATASET.TENANT_TABLE', insert_retry_policy='RETRY_ON',\n                                      schema='tenant_id:STRING, count:INTEGER, window_start:TIMESTAMP'))\n```\n\n## Follow-up Questions\n- How would you validate idempotency and handle late data?\n- How would you test per-tenant isolation and data residency compliance across regions?\n- What monitoring dashboards would you surface for latency, error budgets, and data freshness?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-6",
        "questionId": "q-4492",
        "question": "In a multi-tenant SaaS on GCP with 12+ tenants, design a streaming data ingestion and analytics pipeline that preserves strict per-tenant isolation, uses per-tenant encryption keys, and scales to peak load. Data arrives via Pub/Sub; process with a Dataflow streaming template; land in per-tenant BigQuery datasets. Describe security (IAM, encryption, VPC), idempotency (dedupe, MERGE), error handling, and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Cost-aware, per-tenant static content delivery: assets under tenant prefixes in Cloud Storage; a Cloud Run front-end authenticates each request and issues time-limited signed URLs; enable Cloud CDN fo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Pub/Sub ingest; Dataflow streaming; route events by tenant_id to per-tenant BigQuery datasets; IAM-based isolation and per-tenant quotas; handle schema evolution with permissive schema updates; dead-l",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Dataflow streaming template subscribing to Pub/Sub; route events to per-tenant BigQuery datasets; implement idempotency with a staging table and MERGE into final tables using event_id; enforce p",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Leverage cross-region Pub/Sub replication, regional GCS with CMEK, and region-bound Dataflow templates. Raw data goes to regional buckets; Dataflow writes partitioned BigQuery, redacted summaries to a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design scalable, secure, multi-tenant data pipelines on GCP with strong isolation, encryption, and observability. Tests knowledge of streaming patterns, idempotent processing, and operational maturity.\n\n## Key Concepts\n\n- Streaming ingestion using Pub/Sub and Dataflow\n- Multi-tenant isolation via datasets/projects and IAM/per-tenant keys\n- Idempotent processing with staging + MERGE\n- Private egress (PSC) and VPC considerations\n- Observability: structured logging, dashboards, alerts\n\n## Code Example\n\n```python\n# Pseudo Dataflow skeleton: reads Pub/Sub, dedup by event_id, writes to BigQuery with MERGE\n# Real implementation would parameterize tenant_id routing and keys\n```\n\n## Follow-up Questions\n\n- How handle late-arriving data and schema evolution?\n- What are cost-controls and scaling strategies under traffic spikes?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-7",
        "questionId": "q-13269",
        "question": "Design a beginner-friendly rollout plan for a multi-tenant Cloud Run API behind a Global HTTP(S) Load Balancer across two regions. Implement per-tenant feature flags, canary rollout by traffic-splitting revisions, and cost controls (min/max instances, concurrency). Include a safe rollback strategy and a simple test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Cloud Run service with traffic-splitting capabilities behind a Global HTTP(S) Load Balancer across two regions. Leverage Cloud Run revisions for canary deployments with a 90/10 traffic split between stable a…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "End-to-end approach includes per-env Cloud Storage buckets for input and a separate de-identified bucket for outputs; Vertex AI hosting endpoint accessed via Private Service Connect; use Data Loss Pre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant isolation with separate Cloud Storage buckets and BigQuery datasets, trigger a Python Cloud Function on new logs to redact PII, validate JSON schema, and append to per-tenant BigQ",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Pub/Sub to ingest JSON events, connected to a Dataflow streaming job that validates a simple JSON schema, masks PII fields, and writes to per-tenant BigQuery tables (one table per tenant per environment). Implement…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical deployment discipline on Google Cloud Platform, focusing on safe rollout strategies, tenant isolation, and cost management in a multi-tenant environment.\n\n## Key Concepts\n- Cloud Run revisions and traffic-splitting mechanisms\n- Global HTTP(S) Load Balancer with regional failover\n- Per-tenant feature flag management in Firestore\n- Resource optimization: min/max instances, concurrency settings\n- Automated rollback strategies and testing frameworks\n\n## Code Example\n```javascript\n// Cloud Run traffic split configuration\ngcloud run services update api-service \\\n  --region us-central1 \\\n  --to-revisions canary=10,stable=90\n```",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-8",
        "questionId": "q-14440",
        "question": "You’re architecting a multi-tenant analytics platform on GCP. Tenants upload CSVs via signed URLs to a tenant-scoped GCS bucket; a Dataflow pipeline validates, partitions by tenant/date, and writes per-tenant BigQuery tables. How would you enforce per-tenant IAM, prevent cross-tenant data leakage, handle schema drift, ensure idempotent processing, and verify data quality end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cost-aware, privacy-preserving data ingestion and analytics path on GCP for a multi-tenant app. Isolate each tenant's data in per-tenant GCS buckets and BigQuery datasets. Ingest via Pub/Sub,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Dataflow (Beam) pipeline reading from tenant-scoped GCS paths, validate with a strict schema, and write to per-tenant BigQuery datasets. Enforce per-tenant IAM with conditional bindings, use Aut",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Architect a private, per-tenant ingestion path: devices push to PSC-enabled Pub/Sub; Dataflow streaming reads per-tenant topics, writes to dedicated BigQuery datasets and Cloud Storage, with per-tenan",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest user activity events via Pub/Sub into a Dataflow streaming pipeline that applies real-time PII redaction using the Data Loss Prevention API with per-tenant masking policies retrieved from a centralized configurat…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAdvanced multi-tenant isolation, IAM granularity, idempotent processing, and schema-evolution handling in a real ingestion pipeline.\n\n## Key Concepts\n- Tenant isolation with IAM conditions and per-tenant datasets\n- Dataflow/Beam validation and idempotent writes\n- Row-level security via Authorized Views or policy tags\n- Schema evolution, drift detection, and data quality checks\n\n## Code Example\n```python\n# simplified tenant routing in Beam\ndef to_table(tenant_id: str) -> str:\n    return f\"project.dataset.tenant_{tenant_id.replace('-', '_')}\"\n```\n\n## Follow-up Questions\n- How would you test failure modes (partial failures, retries) in this pipeline?\n- How would you audit data lineage across tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-9",
        "questionId": "q-27666",
        "question": "Design an intermediate GCP architecture for a multi-tenant streaming analytics service that ingests partner event streams via Pub/Sub, performs real-time enrichment in per-tenant Cloud Run workers, stores results in per-tenant BigQuery datasets, and uses Vertex AI Feature Store for online/offline features; describe tenant isolation via IAM and network controls, observability, rollback, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instantiate a new tenant by IaC: create a per-tenant BigQuery dataset, a dedicated Cloud SQL database (or schema), and a Cloud Storage bucket; attach a tenant service account with least-privilege role",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a multi-tenant governance pattern: each tenant uses its own BigQuery dataset; partner CSVs land via signed URLs to Cloud Storage and are ingested by a Cloud Function. Enforce per-tenant row-le",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Cloud Storage trigger on the shared bucket to start a Dataflow/Beam pipeline per file. Validate against a central schema registry, redact PII with DLP, then call a Cloud Run enrichment ser",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Tenant-scoped projects with dedicated service accounts; Pub/Sub topics feed per-tenant Cloud Run enrichment; Dataflow handles batch aggregations; per-tenant BigQuery datasets store results; Vertex AI",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a scalable, secure multi-tenant data path on GCP, balancing real-time enrichment with offline analytics while maintaining strict isolation and observable operations.\n\n## Key Concepts\n\n- Multi-tenant isolation (projects, service accounts, IAM, VPC)\n- Pub/Sub-driven event ingestion\n- Real-time enrichment with Cloud Run\n- Dataflow for batch/stream processing\n- Vertex AI Feature Store for online/offline features\n- Observability (Cloud Monitoring/Logging) and correlation IDs\n- Rollback and versioning per tenant\n\n## Code Example\n\n```yaml\n# Example per-tenant IAM binding (illustrative)\nbindings:\n- role: roles/viewer\n  members:\n  - serviceAccount:tenant-1-sa@prod-project.iam.gserviceaccount.com\n```\n\n## Follow-up Questions\n\n- How would you test tenant isolation boundaries and cross-tenant data access?\n- What failure modes require immediate rollback and how would you trigger them?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-10",
        "questionId": "q-17567",
        "question": "Design a cost-conscious, multi-tenant streaming analytics pipeline on GCP for a real-time chat app. Ingest events via Pub/Sub, process with a Dataflow streaming job that isolates tenants (tenant_id partitioning and per-tenant datasets), handles late data, and applies per-tenant billing labels. Store results in partitioned BigQuery tables; expose a read API. Include observability, testing plan, and disaster recovery plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Dataflow streaming reads Pub/Sub messages, extracting the tenant_id, and writes to per-tenant BigQuery datasets partitioned by day. It uses event-time windows with allowed lateness to handle late data and ensures idempo…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a multi-tenant API on Cloud Run behind a global HTTP(S) Load Balancer. Authenticate users with Identity Platform; route every request to a tenant-specific Firestore path using a tenantId in",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "In practice, implement per-tenant Pub/Sub topics, a single Dataflow streaming job that keys on tenantId and uses per-tenant windowing, allocate a separate BigQuery dataset per tenant, and enforce leas",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single Pub/Sub topic with tenant_id in attributes. A Python Dataflow pipeline validates a simple telemetry schema, partitions into per-tenant BigQuery datasets, and stores raw records in per-ten",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design multi-tenant, cost-aware streaming pipelines with strong isolation, observability, and recoverability.\n\n## Key Concepts\n- **Tenant isolation**: Per-tenant datasets, IAM least privilege, and VPC Service Controls ensure strong tenant boundaries.\n- **Streaming semantics**: Event-time windows, allowed lateness, and idempotent writes guarantee correct processing of out-of-order and late-arriving data.\n- **Cost controls**: Per-tenant billing labels, quotas, and careful resource sizing optimize costs across tenants.\n- **Disaster recovery**: Regional replication and per-tenant rollback manifests enable rapid recovery.\n\n## Code Example\n```javascript\n// Pseudo Dataflow skeleton (not runnable)\nclass TenantPipeline {\n  constructor(tenantId, projectId) {\n    this.tenantId = tenantId;\n    this.projectId = projectId;\n    this.datasetId = `tenant_${tenantId}`;\n  }\n\n  buildPipeline(pubsubSubscription, watermarkDelayMs = 60000) {\n    return (\n      pipeline\n        .apply('ReadFromPubSub', PubsubIO.readMessages()\n          .fromSubscription(this.pubsubSubscription)\n          .withTimestampAttribute('event_timestamp'))\n        .apply('ExtractTenant', ParDo.of(new ExtractTenantDoFn()))\n        .apply('WindowInto', Window.into(FixedWindows.of(60 * 60 * 1000))\n          .withAllowedLateness(watermarkDelayMs)\n          .triggering(AfterWatermark.pastEndOfWindow()\n            .withEarlyFirings(AfterProcessingTime.pastFirstElementInPane()\n              .plusDelayOf(5 * 60 * 1000))))\n        .apply('ApplyTenantLabels', ParDo.of(new AddBillingLabelsDoFn(this.tenantId)))\n        .apply('WriteToBigQuery', BigQueryIO.write()\n          .to(tableSpec => this.getTableSpec(tableSpec))\n          .withSchema(this.getSchema())\n          .withWriteDisposition(BigQuery.WriteDisposition.WRITE_APPEND)\n          .withCreateDisposition(BigQuery.CreateDisposition.CREATE_NEVER));\n    }\n  }\n\n  getTableSpec(tableSpec) {\n    return `${this.projectId}:${this.datasetId}.${tableSpec}`;\n  }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-11",
        "questionId": "q-19233",
        "question": "Design a scalable, cost-aware multi-tenant data ingestion pipeline on Google Cloud for a SaaS analytics platform. Customers emit telemetry in JSON to Pub/Sub; build an end-to-end flow that preserves per-tenant isolation, supports schema evolution, and ensures idempotent processing and observability. Outline components, data model, and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest JSON telemetry to Pub/Sub, process with Dataflow (Beam) to normalize fields and apply idempotent deduping (insertId) before writing to a single BigQuery dataset that includes a tenant_id. Use p",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a per-tenant Pub/Sub ingestion, a streaming Dataflow job that deduplicates on event_id, materializes as Parquet in Cloud Storage, and then loads into per-tenant partitioned BigQuery tables.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a GitOps-style pipeline: push triggers per env; build images in Artifact Registry; enforce Binary Authorization to allow only signed images; generate SBOM and run container scans; enforce per-tena",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Dataflow batch (Python Beam) reads daily CSVs from a shared GCS bucket, validates schema, normalizes dates, and writes to per-tenant BigQuery tables via dynamic destinations in dev/stage/prod datasets",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world design across ingestion, normalization, multi-tenant isolation, and observability on GCP. It probes tradeoffs between per-tenant isolation vs shared datasets and the practicalities of schema evolution.\n\n## Key Concepts\n- Pub/Sub, Dataflow (Beam) for streaming normalization and deduplication\n- BigQuery data model: tenant_id, ingestion_time partitioning, streaming inserts\n- Isolation: Authorized Views and IAM vs dataset-per-tenant\n- Schema evolution and backward compatibility\n- Observability: Cloud Monitoring dashboards, Cloud Logging alerts\n\n## Code Example\n```python\nimport apache_beam as beam\n# Pseudo-transform: parse, normalize, dedupe by insertId, write to BigQuery\ndef transform(element):\n    obj = json.loads(element)\n    tenant = obj.get('tenant_id')\n    yield {\n        'tenant_id': tenant,\n        'ingestion_time': obj.get('ts'),\n        'metric': obj.get('metric'),\n        'value': obj.get('value')\n    }\n\nwith beam.Pipeline() as p:\n    (p\n     | beam.io.ReadFromPubSub(topic='projects/xxx/topics/telemetry')\n     | beam.Map(lambda x: transform(x.decode('utf-8')))\n     | beam.io.WriteToBigQuery('project.dataset.telemetry', schema='SCHEMA_AUTODETECT', write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND, insert_retry_limit=3))\n```\n\n## Follow-up Questions\n- How would you validate idempotency at scale and detect duplicates? \n- How would you handle late data and TAP dedup? \n- What are failure modes and rollback strategies for schema changes?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-12",
        "questionId": "q-22625",
        "question": "Design a beginner-friendly, cost-conscious GCP API that serves per-tenant configuration data. Build with a Cloud Run REST API, enforce per-tenant isolation via separate Cloud Storage buckets and service accounts, and use Cloud Tasks to enqueue a worker Cloud Run to refresh configs from an upstream source. Include idempotent refresh, caching in Memorystore, and a basic observability and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Dataflow streaming job that subscribes to Pub/Sub per env, joins with a nightly BigQuery reference table sourced from Cloud SQL, and writes per-tenant aggregates (1-min windows) to environ",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a router Cloud Run service that reads TenantID from a header, computes a stable hash, and forwards to backend Revision A or Revision B with a controlled split. Store per-tenant flags in Secret Man",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "The API serves per-tenant configs. Use a Cloud Run REST service, with separate Cloud Storage buckets and Service Accounts per tenant, and Cloud Tasks to enqueue a worker Cloud Run that refreshes data",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "In a multi-tenant GCP setup, create a Pub/Sub topic per tenant, a Dataflow streaming pipeline that writes raw data to per-tenant Cloud Storage and refined data to per-tenant BigQuery datasets. Isolate",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design multi-tenant isolation, event-driven refresh, and cost-conscious scaling on GCP using Cloud Run, Cloud Tasks, and Memorystore.\n\n## Key Concepts\n- Per-tenant isolation with separate buckets and service accounts\n- Event-driven refresh via Cloud Tasks and a worker Cloud Run\n- Caching layer with Memorystore for low-latency reads\n- Idempotent refresh using last_modified/ETag\n- Observability: logging, metrics, tracing, and simple tests\n\n## Code Example\n\n```javascript\n// Pseudo: refresh trigger check\nif (upstreamLastModified === cachedLastModified && upstreamETag === cachedETag) {\n  return; // idempotent no-op\n}\n// fetch and update tenant data\n```\n\n## Follow-up Questions\n- How would you validate idempotency under concurrent refreshes?\n- How would you enforce strict per-tenant IAM boundaries across projects?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-13",
        "questionId": "q-19745",
        "question": "Design a multi-region, zero-downtime deployment strategy for a GCP-hosted Cloud Run API that enforces per-tenant isolation using separate service accounts and supports regional failover between us-central1 and europe-west1. Include traffic shifting, DB replication, audit logging, and a concrete rollback plan with observability and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a multi-region Cloud Run API behind a global HTTP(S) Load Balancer with per-tenant isolation via separate service accounts. Use canary-style revisions and traffic-splitting (e.g., 5% increments)",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "End-to-end approach includes per-env Cloud Storage buckets for input and a separate de-identified bucket for outputs; Vertex AI hosting endpoint accessed via Private Service Connect; use Data Loss Pre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Publishers attach a unique message_id to each event. The Cloud Run worker checks a Firestore deduplication set and skips any repeated messages to ensure idempotent processing. New messages are masked for PII using a dat…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a streaming Dataflow pipeline: Pub/Sub → per‑tenant BigQuery datasets; Dataflow handles windowed aggregates and lookups to Cloud SQL tenant catalog. Enforce per‑tenant IAM with IAM Conditions",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes multi-region deployment design, per-tenant isolation, and operational discipline under real-world constraints.\n\n## Key Concepts\n- Multi-region, global load balancing\n- Canary deployments and traffic shifting\n- Per-tenant IAM/service accounts\n- Cross-region data replication and failover\n- Observability and compliance (Cloud Audit Logs, VPC Service Controls)\n\n## Code Example\n```javascript\n// example: shift traffic to new revision\ngcloud run services update-traffic api --to-revisions api-abc-123=95,api-abc-124=5 --region us-central1\n```\n\n## Follow-up Questions\n- How would you validate zero-downtime during cutover?\n- What monitoring indicators signal a rollback is needed?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-14",
        "questionId": "q-13791",
        "question": "Design a multi-region streaming data path on GCP for a high-volume bidding platform: partner Pub/Sub topics ingest ~1M events/sec; Dataflow (streaming) processes with exactly-once semantics and writes per-tenant aggregates to regional BigQuery datasets. Include DR strategy with cross-region replication, DNS failover, and SLA targets; outline partitioning, IAM, and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an architecture with Pub/Sub cross-region topics, Dataflow streaming with exactly-once semantics, sink to region-specific BigQuery datasets and Cloud Storage, mutual TLS and VPC Service Contro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a multi-region streaming path: partner Pub/Sub topics feed a Dataflow (streaming) job with exactly-once semantics and inserts to regional BigQuery datasets (per-tenant). Mirror streams to a seco",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a Cloud Run service behind API Gateway that reads per-tenant flags from Firestore. The tenant_id selects a document containing the enabled flag and suffix configuration. Cache the result in memory for the duration o…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a GCP-based, multi-tenant analytics pipeline where partner events arrive on Pub/Sub, processed by Dataflow into per-tenant BigQuery datasets with authorized views; stream changes to Snowflak",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world design skills for high-volume, low-latency data paths, DR planning, and security.\n\n## Key Concepts\n- Streaming dataflow with exactly-once semantics; insertId handling in BigQuery.\n- Multi-region data residency and DR strategies.\n- DNS-based failover and global load balancing.\n- IAM least-privilege and per-tenant quotas; observability with Cloud Monitoring/Logging.\n\n## Code Example\n```javascript\n// Pseudo snippet illustrating insertId usage for dedup\nconst row = { tenantId, event, ts: Date.now() }\nbqClient.insert('dataset.table', row, { insertId: `${tenantId}_${ts}` })\n```\n\n## Follow-up Questions\n- How would you validate exactly-once guarantees under network partitioning?\n- What are the latency implications of cross-region replication and how would you mitigate them?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-15",
        "questionId": "q-17295",
        "question": "Design a secure, cost-aware, multi-tenant analytics pipeline on Google Cloud for a SaaS app that ingests user events from mobile/web clients into Pub/Sub, processes with Dataflow, and stores per-tenant data in BigQuery. No public endpoints; enforce per-tenant isolation, CMEK encryption, strict IAM, and VPC Service Controls. Outline architecture, retention, access, monitoring, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest from Pub/Sub with tenant_id, route to a regional Dataflow streaming job that deduplicates on event_id and writes to per-tenant BigQuery datasets located in the event’s region. Use per-tenant se",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a Cloud Function (Python) with a dedicated service account that has Storage Object Admin on the bucket, BigQuery DataEditor on the target dataset, and Cloud Logging rights. Validate the partner",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed solution: route events to per-tenant Pub/Sub topics, stream with Dataflow using per-tenant windows, and write to per-tenant BigQuery datasets encrypted with CMEK. Enforce least-privilege IAM,",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-tenant Pub/Sub topics with a single Cloud Run worker; configure a pull/push subscription with a Dead Letter topic and max delivery attempts. Implement idempotent processing by deduplicating on",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-tenant isolation, cost control, and production readiness of streaming analytics on GCP.\n\n## Key Concepts\n- Pub/Sub per-tenant isolation and schema evolution\n- Dataflow streaming with per-tenant windows and auto-scaling\n- BigQuery per-tenant datasets with CMEK and partitioning\n- IAM least privilege, service accounts, workload identity federation\n- VPC Service Controls and Private Service Connect for no public endpoints\n\n## Code Example\n```yaml\n# IaC sketch to create per-tenant Pub/Sub topics and BigQuery datasets\nresources:\n  - type: pubsub_topic\n    name: tenant-${tenant_id}\n    iam_bindings:\n      - role: roles/pubsub.publisher\n        members:\n          - serviceAccount:tenant-${tenant_id}@project.iam.gserviceaccount.com\n```\n\n## Follow-up Questions\n- How would you test per-tenant data isolation under peak load?\n- What changes for a new tenant onboarding workflow?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-16",
        "questionId": "q-24734",
        "question": "Design a real-time fraud-detection pipeline on GCP for a multi-tenant fintech app. Data enters via Pub/Sub, processed by Dataflow streaming to per-tenant BigQuery datasets and Cloud Storage, with strict per-tenant isolation, auditability, and cost-controls. Include IAM least privilege, Private Service Connect, VPC Service Controls, dead-letter handling, and a rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pub/Sub with tenant_id attribute; run a Dataflow streaming job that key-groups by tenant_id, writes to per-tenant BigQuery tables via BigQueryIO with insertId = tenant_id + event_id to enable dedu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a tenant-scoped Dataflow streaming pipeline: Pub/Sub ingests events with tenant_id; Dataflow writes to per-tenant BigQuery datasets and Cloud Storage. Enforce isolation with per-tenant service acc",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Pub/Sub push/pull to feed a Cloud Run container with auto-scaling; implement idempotency by recording processed message_ids in a small Cloud SQL or Firestore table per tenant; write into per-tenan",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Route requests through a tenant router in Cloud Run that maps tenant_id to region, storage bucket, and CMEK key; isolate data by region using regional Cloud SQL instances and CMEK-protected Cloud Stor",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses multi-tenant data isolation, real-time processing, and security controls in GCP at scale.\n\n## Key Concepts\n- Tenant isolation: per-tenant BigQuery datasets and Cloud Storage buckets\n- Streaming: Pub/Sub to Dataflow with idempotent writes\n- Security: IAM least privilege, Private Service Connect, VPC Service Controls\n- Reliability: dead-letter queues, checkpointing, replayable pipelines\n- Cost: per-tenant budgets, autoscaling, efficient sinks\n\n## Code Example\n```python\n# Pseudo Dataflow transform sketch\nclass ProcessFraudEvent(DoFn):\n    def process(self, element, tenant_id=None):\n        if is_duplicate(element, tenant_id):\n            return\n        write_to_sink(tenant_id, element)\n```\n\n## Follow-up Questions\n- How would you test cross-tenant data isolation and budgeting?\n- What rollback/DR steps would you automate for this pipeline?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-17",
        "questionId": "q-19525",
        "question": "Design a two-region, per-tenant ingestion pipeline: partners upload signed CSVs to per-tenant Cloud Storage; a Dataflow (Python) batch job validates schema, normalizes timestamps, and applies DLP redaction; writes de-identified data to per-tenant BigQuery datasets; enforce least-privilege IAM, Private Service Connect, and per-tenant service accounts; implement tests and cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a Pub/Sub trigger feeding a Cloud Run worker that validates with jsonschema, normalizes timestamps to UTC, flags anomalies, and writes per-tenant normalized JSON to per-tenant Cloud Storage b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a two-region, per-tenant ingestion pipeline: partners upload signed CSVs to per-tenant Cloud Storage; a Dataflow (Python) batch job validates schema, normalizes timestamps, and applies DLP reda",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a single Pub/Sub topic with tenant_id in attributes. A Python Dataflow pipeline validates a simple telemetry schema, partitions into per-tenant BigQuery datasets, and stores raw records in per-ten",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage cross-region Pub/Sub replication, regional GCS with CMEK, and region-bound Dataflow templates. Raw data goes to regional buckets; Dataflow writes partitioned BigQuery, redacted summaries to a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests cross-region DR, per-tenant isolation, secure partner ingestion, and auditing across GCS, Dataflow, DLP, BigQuery, IAM, PSC, and logging. Emphasizes end-to-end security, data privacy, and cost governance in a real-world fintech context.\n\n## Key Concepts\n\n- Multi-tenant isolation with per-tenant datasets and IAM\n- Data validation and de-identification via Dataflow + DLP\n- Partner-signed URL ingestion into Cloud Storage\n- Private connectivity (Private Service Connect) and VPC controls\n- Two-region DR, audit logging, and cost governance\n\n## Code Example\n\n```javascript\n// Pseudo-pipeline sketch (illustrative)\nfunction ingestFile(fileUri, tenantId){\n  // validate schema\n  // redact PII with DLP\n  // route to per-tenant BigQuery\n}\n```\n\n## Follow-up Questions\n\n- How would you guarantee idempotent processing for reuploads?\n- What metrics and alarms would you implement for cost spikes and data freshness?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-18",
        "questionId": "q-977",
        "question": "In a beginner setup, you deploy a Cloud Run API behind Private Service Connect, with logs going to Cloud Logging and traces to Cloud Trace. Outline a practical observability plan: which metrics, logs, and traces to collect; how to build a useful dashboard; how to configure a low-noise alert for 5xx latency; and a simple test to validate instrumentation and alerting?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest from Pub/Sub with tenant_id, route to a regional Dataflow streaming job that deduplicates on event_id and writes to per-tenant BigQuery datasets located in the event’s region. Use per-tenant se",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument a Cloud Run API with: metrics (request count, latency, 5xx rate), structured logs with trace IDs, enabled Cloud Trace; a single Monitoring dashboard; a low-noise alert for p95 latency > 500",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "In a real-time, multi-tenant event ingestion pipeline on GCP: Use Pub/Sub as input, Dataflow (Beam) for streaming transforms, route data into per-tenant BigQuery datasets/partitions, and encrypt using",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a per-tenant data plane with separate BigQuery datasets and GCS prefixes, real-time ingestion via Pub/Sub to Dataflow that writes partitioned tables; expose cross-tenant dashboards on anonymiz",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical setup of observability for a basic Cloud Run service, focusing on end-to-end data flows, dashboards, and alerting to catch regressions early.\n\n## Key Concepts\n\n- Cloud Logging: structured logs with trace context\n- Cloud Monitoring: dashboards and alerting policies\n- Cloud Trace: distributed tracing for latency breakdown\n- Private Service Connect: private connectivity for isolation\n- Health checks and end-to-end tests\n\n## Code Example\n\n```javascript\n// Example instrumentation snippet for Cloud Logging\nconst {Logging} = require('@google-cloud/logging');\nconst logging = new Logging();\nconst log = logging.log('api-logs');\nconst metadata = {resource: {type: 'cloud_run_revision', labels: {service: 'my-api', revision: 'rev-1'}}, severity: 'INFO'};\nconst traceId = 'TRACE_ID';\nconst entry = log.entry(metadata, {message: 'request received', trace: `projects/PROJECT_ID/traces/${traceId}`});\nlog.write(entry);\n```\n\n## Follow-up Questions\n\n- How would you adjust the dashboard and alerting as traffic scales?\n- What lightweight sampling strategy would you apply to avoid alert fatigue?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-19",
        "questionId": "q-9117",
        "question": "Design a beginner-friendly multi-tenant API gateway architecture on GCP for a SaaS app. Frontend calls a Cloud Run service behind API Gateway; implement per-tenant API keys with quotas, and environment isolation (dev/stage/prod) using separate projects or folders. Include Cloud Armor rules, per-tenant IAM roles, a cheap caching plan (Cloud CDN), and a simple test plan with smoke tests and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Dataflow batch (Python Beam) reads daily CSVs from a shared GCS bucket, validates schema, normalizes dates, and writes to per-tenant BigQuery tables via dynamic destinations in dev/stage/prod datasets",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route all traffic through API Gateway in front of Cloud Run. Enforce per-tenant API keys and quotas via API config, with environment isolation in separate projects. Use per-tenant IAM service accounts",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Provide a per-tenant API on Cloud Run behind API Gateway; require tenant_id in a header; route requests to a single service that reads per-tenant config from Firestore and stores assets in a tenant-sc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed approach: use a Shared VPC with per-tenant IAM and service accounts; store raw CSVs in per-tenant subfolders in Cloud Storage (path: gs://bucket/{tenant-id}/raw/); trigger Dataflow (Beam) from Pub/Sub on file n…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-tenant API design, security, and cost-aware choices using GCP primitives: API Gateway config, IAM scoping, Cloud Armor protections, and basic rollback.\n\n## Key Concepts\n- API Gateway with per-tenant keys and quotas\n- Environment isolation (dev/stage/prod)\n- IAM service accounts and roles per tenant\n- Cloud Armor and Cloud CDN for cost-smart protection\n- Monitoring/alerts and config-versioned rollback\n\n## Code Example\n```yaml\n# Small example of an API config snippet (illustrative)\napiConfig:\n  name: tenant-config\n  httpRules:\n  - selector: '*'\n    methods: [GET,POST]\n    url: */v1/*\n```\n\n## Follow-up Questions\n- How would you test quota enforcement under bursty traffic?\n- What changes would you make to onboard/offboard tenants with zero downtime?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-architect-q-20",
        "questionId": "q-9525",
        "question": "Design a secure, multi-tenant data ingestion and processing path on GCP for a SaaS product that receives daily CSVs via partner-signed URLs into a central Cloud Storage bucket, validates and partitions data by tenant, and loads into per-tenant BigQuery tables. Include cross-project IAM with workload identity federation, VPC Service Controls, CMEK, idempotent processing, rollback, and a test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a policy-driven software supply chain using per-service Artifact Registry repositories, Cloud Build triggers for automated CI/CD, and container signing with Cosign. Enforce Binary Authorization with tenant-scoped…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed solution: route events to per-tenant Pub/Sub topics, stream with Dataflow using per-tenant windows, and write to per-tenant BigQuery datasets encrypted with CMEK. Enforce least-privilege IAM,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant gateway on Cloud Run using OAuth 2.0/OIDC, with per-tenant service accounts and least-privilege IAM. Route to each tenant's Cloud SQL and Storage via Private Service Connect, enfor",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe a secure, multi-tenant ingestion on GCP: partner CSVs arrive to a central GCS via signed URLs; a Cloud Function validates schema and metadata, then publishes messages to Pub/Sub; a Dataflow j",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world multi-tenant data pipelines demand strong isolation, auditable control planes, and robust failure handling. This question probes identity management, data residency, and operational resilience in GCP.\n\n## Key Concepts\n- Partner-signed URLs for secure ingestion\n- Cloud Functions validation and Pub/Sub as a decoupler\n- Dataflow (streaming/batch) with per-tenant partitioning\n- CMEK and tenant-scoped IAM + Workload Identity Federation\n- VPC Service Controls perimeter to prevent exfiltration\n- Idempotent processing and replay protection\n- Rollback and test strategies\n\n## Code Example\n```javascript\n// Example: publish validated message to Pub/Sub from a Cloud Function (Node.js)\nconst {PubSub} = require('@google-cloud/pubsub');\nconst pubsub = new PubSub();\nexports.validateAndPublish = async (event) => {\n  const record = JSON.parse(event.body);\n  // basic schema check\n  if (!record.tenant_id || !record.file_uri) throw new Error('Invalid');\n  await pubsub.topic('tenant-ingest').publish(Buffer.from(JSON.stringify(record)));\n};\n```\n\n## Follow-up Questions\n- How would you validate per-tenant data isolation in BigQuery? \n- How would you test handling of schema evolution and data drift?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-21",
        "questionId": "q-3775",
        "question": "Design a real-time analytics DR pipeline in GCP that tolerates regional failure. Data sources publish to Pub/Sub in us-central1. Build a Dataflow streaming job that reads from Pub/Sub and writes to BigQuery in us-central1, with a hot standby in us-east1 that can take over with minimal changes. Explain failover triggers, latency goals, data-loss bounds, idempotent writes, monitoring, and testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Architecture Choice: Per-Tenant Datasets in BigQuery** Use per-tenant datasets with a parameterized Dataflow pipeline writing to partitioned tables by tenant_id and event_date. This approach provides stronger isolatio…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Primary: Dataflow streaming in us-central1 reads Pub/Sub us-central1 and writes to BigQuery us-central1. Standby: identical pipeline in us-east1 with mirrored Pub/Sub and BigQuery. Failover via subscr",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Dataflow batch (Python Beam) reads daily CSVs from a shared GCS bucket, validates schema, normalizes dates, and writes to per-tenant BigQuery tables via dynamic destinations in dev/stage/prod datasets",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant data plane: route real-time JSON events via Pub/Sub with tenant_id to a streaming Dataflow job that writes into per-tenant BigQuery datasets in the primary region; replicate to a secondary region…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests DR planning for streaming pipelines across regions, including data integrity, latency, and operational testing.\n\n## Key Concepts\n- Streaming data pipelines with Dataflow, Pub/Sub, BigQuery\n- Regional DR and hot-standby designs\n- Exactly-once semantics with insertId and Streaming Engine\n- Failover mechanisms and cutover automation\n- Monitoring, SLAs, and DR testing cadence\n\n## Code Example\n```python\n# Pseudo: Dataflow template skeleton for Pub/Sub -> BigQuery with insertId for idempotence\nfrom apache_beam.options.pipeline_options import PipelineOptions\n with PipelineOptions(flags=None) as p:\n  (p\n   | 'Read' >> beam.io.ReadFromPubSub(topic='projects/.../topics/pc-us-central')\n   | 'Parse' >> beam.Map(lambda x: {'payload': x, 'insertId': extract_id(x)})\n   | 'WriteBQ' >> beam.io.WriteToBigQuery('dataset.table', insert_dupes=True))\n```\n\n## Follow-up Questions\n- How would you validate end-to-end failover without impacting production?\n- What metrics and alarms ensure rapid detection of DR issues and minimize data loss?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-22",
        "questionId": "q-17597",
        "question": "Design a real-time, per-tenant streaming analytics pipeline on GCP: partner logs are published to Pub/Sub, consumed by a Dataflow streaming job that computes per-tenant metrics (count of events, average session length) and writes results to per-tenant BigQuery datasets. Include per-tenant isolation (IAM, datasets, and storage), exactly-once processing, schema evolution handling, observability, and a test/rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pub/Sub ingestion; a Dataflow streaming pipeline with per-tenant windowing computes metrics and writes to per-tenant BigQuery datasets (partitioned by date). Enforce per-tenant IAM via service account",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Cloud Build as the CI/CD engine, fetch dependencies from a per-tenant private Artifact Registry, and require a Binary Authorization attestation before each Cloud Run deployment. Isolate tenants wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a GCP-based, multi-tenant analytics pipeline where partner events arrive on Pub/Sub, processed by Dataflow into per-tenant BigQuery datasets with authorized views; stream changes to Snowflak",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest JSON telemetry to Pub/Sub, process with Dataflow (Beam) to normalize fields and apply idempotent deduping (insertId) before writing to a single BigQuery dataset that includes a tenant_id. Use p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the candidate's ability to design a real-time, multi-tenant pipeline with isolation, correctness, and operability.\n\n## Key Concepts\n- Streaming ETL with Pub/Sub and Dataflow\n- Per-tenant isolation via separate datasets, IAM, and storage\n- Exactly-once semantics, dead-letter handling, and schema evolution\n\n## Code Example\n```javascript\n// Pseudo Apache Beam-like pipeline skeleton (illustrative)\nfunction buildPipeline(options) {\n  // Read from Pub/Sub\n  // Key by tenant, apply windowing, aggregate metrics\n  // Write to per-tenant BigQuery tables\n}\n```\n\n## Follow-up Questions\n- How would you validate per-tenant isolation during deployment and schema changes?\n- How would you handle late-arriving events and data skew?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-architect-q-23",
        "questionId": "q-2627",
        "question": "Design an end-to-end, compliance-focused model-prediction pipeline on GCP for sensitive user data. Data arrives as CSV in per-env Cloud Storage, then is de-identified by Data Loss Prevention and fed to Vertex AI hosting behind Private Service Connect. Enforce per-project isolation, least-privilege IAM, no public endpoints, and robust data lineage. Outline architecture, IAM, perimeters, and a testing plan including replay-safe ingestion and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest from Pub/Sub with tenant_id, route to a regional Dataflow streaming job that deduplicates on event_id and writes to per-tenant BigQuery datasets located in the event’s region. Use per-tenant se",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would publish each tenant's telemetry to its own Pub/Sub topic for complete isolation, route messages to the appropriate regional endpoint using tenant configuration, validate payloads against a schema registry within…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose per-tenant Cloud SQL instances behind a Shared VPC, with a single Cloud Run frontend behind IAP. Use per-tenant service accounts with least-privilege roles (sql.client, bigquery.jobUser) and C",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "End-to-end approach includes per-env Cloud Storage buckets for input and a separate de-identified bucket for outputs; Vertex AI hosting endpoint accessed via Private Service Connect; use Data Loss Pre",
            "isCorrect": true
          }
        ],
        "explanation": "Why This Is Asked\nTests ability to design a secure, scalable ML data path with privacy controls and observability in GCP.\n\nKey Concepts\n- Data De-identification with Data Loss Prevention\n- Vertex AI with Private Service Connect\n- Per-env isolation and least-privilege IAM\n- VPC Service Controls and no public endpoints\n- Data lineage and auditability via Data Catalog and Cloud Logging\n\nCode Example\n```yaml\n# sample IAM policy snippet for restricting access\nbindings:\n  - role: roles/storage.objectAdmin\n    members:\n      - serviceAccount:ml-pipeline-sa@project.iam.gserviceaccount.com\n```\n\nFollow-up Questions\n- How would you test for DLP misses and false positives?  \n- What rollback mechanisms would you implement for failed inferences or data de-identification errors?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-architect-q-24",
        "questionId": "q-8029",
        "question": "Design a secure, multi-tenant gateway on Google Cloud for a SaaS API where each tenant's data sits in its own Cloud SQL (PostgreSQL) and Cloud Storage bucket. Build a Cloud Run front-end that authenticates with OAuth 2.0/OIDC, enforces per-tenant RBAC, and uses Private Service Connect to reach backend services. Include deployment steps, least-privilege IAM, testing plan, and a DR strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger a Python Cloud Function on GCS finalization; validate CSV, derive schema, and compute a per-file digest for idempotency. Create or update a date-partitioned BigQuery table in the environment-s",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant isolation with separate Cloud Storage buckets and BigQuery datasets, trigger a Python Cloud Function on new logs to redact PII, validate JSON schema, and append to per-tenant BigQ",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant gateway on Cloud Run using OAuth 2.0/OIDC, with per-tenant service accounts and least-privilege IAM. Route to each tenant's Cloud SQL and Storage via Private Service Connect, enfor",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a streaming analytics pipeline: ingest events from Pub/Sub per region into a Dataflow (Python) job that window-aggregates 5-minute segments to compute per-user engagement and funnel metrics,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nMulti-tenant isolation and auditable access across per-tenant data stores.\n\n## Key Concepts\n- Per-tenant isolation across Cloud SQL and Cloud Storage\n- Least-privilege IAM and service accounts\n- Private Service Connect and VPC Service Controls\n- Correlation IDs in Cloud Logging for traceability\n- DR planning and testability\n\n## Code Example\n```javascript\n// Example IAM binding (illustrative)\n{\n  \"bindings\": [\n    {\"role\": \"roles/iam.serviceAccountUser\", \"members\": [\"serviceAccount:tenant-123@gcp-sa.iam.gserviceaccount.com\"]},\n    {\"role\": \"roles/cloudsql.client\", \"members\": [\"serviceAccount:tenant-123@project.iam.gserviceaccount.com\"]}\n  ]\n}\n```\n\n## Follow-up Questions\n- How would you rotate tenant credentials without downtime?\n- How would you test a failed tenant isolation boundary?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-11T05:14:08.514Z",
    "version": 1
  }
]