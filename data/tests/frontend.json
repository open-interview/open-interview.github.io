[
  {
    "id": "test-frontend",
    "channelId": "frontend",
    "channelName": "Frontend",
    "title": "Frontend Knowledge Test",
    "description": "Test your understanding of Frontend with 25 questions.",
    "questions": [
      {
        "id": "frontend-q-0",
        "questionId": "q-15759",
        "question": "In a multi-tenant React dashboard, design a PluggableModuleHost that supports runtime registration of UI modules. Each module is loadable via React.lazy and Suspense, and the host passes a per-tenant slice of a Redux-like store via useSyncExternalStore. Implement a memoized selector keyed by tenantId/moduleId. On tenant switch, unmount active modules to prevent stale closures and listeners. Provide code sketches for: ModuleRegistry, memoizedSelector, LazyModuleContainer, and host wiring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Load 20 items with fetch('/api/feed?limit=20'), render to a list; open WebSocket to wss://feed.example.com; on message, dedupe by id and prepend; use minimal DOM updates; Service Worker caches assets",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Open a WebSocket for live updates and load initial data with fetch('/api/items').then(r=>r.json()). Render updates by reusing a single container, building a DocumentFragment, and updating DOM once per",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a PluggableModuleHost with a ModuleRegistryContext. Modules register {moduleId, loader}. The host lazy-loads each module via React.lazy and Suspense, creating per-tenant store slices exposed",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use CSS Grid with desktop 4 columns, tablet 3, mobile 1; give cards a 4:3 image area via aspect-ratio. Implement a CSS-only density toggle using a hidden checkbox (#density-toggle) to swap CSS custom",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-world composition of runtime modules, per-tenant isolation, and advanced hooks patterns.\n\n## Key Concepts\n\n- React.lazy and Suspense for on-demand modules\n- useSyncExternalStore to subscribe to external per-tenant data\n- Memoized selectors keyed by tenantId/moduleId\n- Runtime Module Registry pattern\n- Safe unmount and cleanup on tenant switches\n\n## Code Example\n\n```javascript\n// Pseudo-code: Module registry and host wiring\nimport React, { createContext, useContext, Suspense, lazy } from 'react'\n\nexport const ModuleRegistryContext = createContext({ register: () => {} })\n\nexport function ModuleHost({ tenantId }) {\n  const { getModuleLoader } = useContext(ModuleRegistryContext)\n  const modules = ['ModuleA', 'ModuleB']\n  return (\n    <div>\n      {modules.map((id) => {\n        const Loader = lazy(getModuleLoader(id))\n        return (\n          <Suspense key={id} fallback={<div>Loading {id}...</div>}>\n            <Loader tenantId={tenantId} />\n          </Suspense>\n        )\n      })}\n    </div>\n  )\n}\n```\n\n## Follow-up Questions\n\n- How would you implement versioned module contracts and feature flags per tenant?\n- How would you test for memory leaks and ensure complete cleanup on tenant switches?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-1",
        "questionId": "q-7832",
        "question": "Design a FocusedDashboard in a React app used by enterprise teams (IBM/LinkedIn/PayPal). The app renders ~150 widgets in a grid. Implement a dynamic registry with lazy-loaded widgets, per-tenant feature flags via Context and Redux, and a global focus mode toggle. Describe how you memoize per-widget visibility with selectors, use a virtualization wrapper, and use React's useTransition to animate focus changes. Include accessibility: keyboard navigation and ARIA labels. Finish with a concise code sketch for a memoized selector and a WidgetCard wrapper?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Here is a complete implementation: ```javascript function createPageLoader(fetchFn) { const cache = new Map(); const inflight = new Map(); function getCacheKey(page, pageSize, sortKey) { return `${page}-${pageSize}-${so…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a registry of widgets with lazy imports; derive visibleIds via a memoized selector that combines tenant features and focus state; render with a virtualization grid (react-window) and a memoized",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use fetchAlerts() to load 20 alerts, store in a Map keyed by alertId, and render with a document fragment. On WebSocket message, if the id is new, enqueue the item and render in a batched frame. Servi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single WebSocket feed for live updates and REST for initial data, batch DOM updates via a renderQueue using requestIdleCallback, and sync across tabs with BroadcastChannel. Service Worker caches",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical design for large dashboards with multi-tenant feature flags, dynamic loading, and performance.\n\n## Key Concepts\n- Memoized per-widget visibility selectors (reselect)\n- Virtualized grid rendering for 150 widgets\n- Context + Redux for flags and global UI state\n- useTransition for burst updates; React.memo for widgets\n- Accessibility: keyboard nav, ARIA labels\n\n## Code Example\n```javascript\nimport { createSelector } from 'reselect';\nconst getWidgetsById = state => state.widgets.byId;\nconst getTenantFeatures = state => state.tenants.byId[state.tenantId].features;\nconst getFocusActive = state => state.ui.focusActive;\n\nexport const getVisibleWidgetIds = createSelector(\n  [getWidgetsById, getTenantFeatures, getFocusActive],\n  (widgets, features, focusActive) =>\n    Object.keys(widgets).filter(id => widgets[id].visible && features[id]?.enabled && (!focusActive || !widgets[id].requiresFocus))\n);\n```\n```javascript\nconst WidgetCard = React.memo(({ id, data }) => { /* render */ return null; });\n```\n\n## Follow-up Questions\n- How would you measure and reduce re-renders in this setup?\n- How would you handle persistence of focus state across sessions?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-2",
        "questionId": "q-13721",
        "question": "You're building a React application that needs to handle optimistic updates for user interactions (like liking a post or adding to cart) while maintaining data consistency and providing rollback functionality if the API call fails. How would you implement a state management pattern using React's useReducer combined with async actions and error boundaries to create a robust optimistic update system?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Here is a complete implementation: ```javascript function createPageLoader(fetchFn) { const cache = new Map(); const inflight = new Map(); function getCacheKey(page, pageSize, sortKey) { return `${page}-${pageSize}-${so…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement optimistic updates using useReducer with action types for pending, success, and failure states, storing previous values for rollback, and using error boundaries to catch and handle failed AP",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a custom React hook that combines useState with the browser's History API, using useEffect to manage popstate events and synchronizing state through history entries while handling persistence with sessionStora…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Next.js dynamic imports to lazy-load heavy routes (chart dashboards) while SSR remains for critical path. Enforce a 700–800KB initial bundle with explicit split points for vendors and charts. Move",
            "isCorrect": false
          }
        ],
        "explanation": "To implement optimistic updates, I'd create a useReducer-based state management system with three-phase actions: OPTIMISTIC_UPDATE, UPDATE_SUCCESS, and UPDATE_FAILURE. The reducer would maintain both the current state and a rollback stack containing previous values. When a user triggers an action, the OPTIMISTIC_UPDATE action immediately applies the change to the UI while storing the previous state in the rollback stack. The async action then makes the API call - if successful, UPDATE_SUCCESS clears the rollback entry, if it fails, UPDATE_FAILURE restores the previous state from the rollback stack. This pattern ensures immediate UI feedback while maintaining data integrity. I'd wrap components in error boundaries to catch any unexpected failures during the async operations and provide fallback UI or retry mechanisms. The solution would also include loading states and conflict resolution for cases where the server state differs from the optimistic state.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-3",
        "questionId": "q-6354",
        "question": "Implement a vanilla JS utility: createCachedFetcher(url) that returns { fetchJson, invalidate }. fetchJson() should fetch the URL once and cache the JSON in a closure; subsequent calls return the cached Promise. invalidate() clears the cache for that URL. Show usage with two URLs and demonstrate cached reuse?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a closure that holds a cache Map keyed by URL. fetchJson() returns cached Promise if present, otherwise fetch(url).then(res => res.json()) and stores the Promise. invalidate() clears the URL en",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use CSS Grid with a masonry-like technique: `grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))` combined with `grid-auto-rows: 20px` and `grid-row: span N` classes for different aspect ratios. For responsive…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a reusable data loader factory: createDataLoader(fetchFn, ttlMs) returns { load(key), invalidate(key), onChange(key, cb) }. The load(key) method must deduplicate concurrent requests for the same key, cache res…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a WebSocket sequence with idempotent replay, track a local pendingEvents queue and a server-placed sequence number; on reconnect, flush pending events, dedupe by id, and apply a CRDT-like merge fo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of closures for private state and promises for asynchronous data fetching, in a lightweight, reusable pattern useful in dashboards.\n\n## Key Concepts\n\n- Closures and private state\n- Promises and fetch API\n- Per-URL caching and cache invalidation\n- Idempotent fetch behavior in UI components\n\n## Code Example\n\n```javascript\nfunction createCachedFetcher(url) {\n  const cache = new Map();\n  return {\n    fetchJson: () => {\n      if (cache.has(url)) return cache.get(url);\n      const p = fetch(url).then(res => {\n        if (!res.ok) throw new Error(res.statusText);\n        return res.json();\n      });\n      cache.set(url, p);\n      return p;\n    },\n    invalidate: () => cache.delete(url)\n  };\n}\n```\n\n```javascript\nconst a = createCachedFetcher('https://api.example.com/one');\na.fetchJson().then(console.log);\na.fetchJson(); // uses cache\n```\n\n## Follow-up Questions\n\n- How to prevent caching of failed fetch results?\n- How to extend to multiple URLs with a shared cache?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-4",
        "questionId": "q-24170",
        "question": "In a multi-tenant frontend, implement a PerTenantFormComposer that renders a dynamic form driven by a per-tenant field-schema stream (useSyncExternalStore) and a per-tenant permissions map (via Context). Requirements: 1) stream wrapper for field definitions; 2) memoized selector keyed by (tenantId, formId) computing initialValues, fieldVisible, and fieldEnabled; 3) optimistic updates for field edits with rollback on server rejection; 4) support for nested groups and conditional fields; 5) cleanup on tenant switch; 6) provide code sketches for streamWrapper, selector, FormField, and FormRenderer wiring; ensure proper cleanup on tenant switch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "PerTenantFormComposer subscribes to a per-tenant field-schema stream via useSyncExternalStore and uses a tenant/formId key to memoize initialValues and visibility. It applies optimistic field edits wi",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement a WebSocket-backed stream wrapper feeding a Redux-like store via useSyncExternalStore, modeling documents as {documentId, sections: [{id, text}]}. Edits are optimistic: apply locally",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a state structure of [past, present, future] with a reducer handling UNDO, REDO, and STATE_UPDATE actions. Implement immutable updates, configurable history depth limits, and selective state tracking to manage memor…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use useReducer with a pending state to track optimistic changes, implementing rollback logic in the error case. For race conditions, use request IDs or abort patterns to ignore stale responses.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to wire useSyncExternalStore with per-tenant memoized selectors, manage optimistic updates, and cleanly reset on tenant switches. It also probes multi-level form schemas, nested groups, and conditional fields under real-world constraints.\n\n## Key Concepts\n- useSyncExternalStore for per-tenant streams\n- memoized selectors keyed by tenantId, formId\n- optimistic UI updates with rollback on failure\n- contextual permissions and complex form schemas (groups/conditions)\n\n## Code Example\n```javascript\n// streamWrapper: subscribes to tenant form schema stream\nfunction createTenantSchemaStream(tenantId, formId){ /* ... */ }\n\n// selector: memoized per (tenantId, formId)\nconst selectFormState = memoize((tenantId, formId, store) => {\n  // compute initialValues, visibility, enablement\n  return { initialValues: {}, visible: true, enabled: true };\n});\n\n// FormField, FormRenderer wrappers would render based on schema\n```\n\n## Follow-up Questions\n- How would you handle dynamic schema changes mid-form?\n- How do you measure and mitigate memory growth with many tenants and forms?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-5",
        "questionId": "q-14055",
        "question": "You're building a React application where form state needs to be persisted across component unmounts and shared between different routes, but you also need to handle optimistic updates and rollback functionality. How would you design a state management layer using localStorage synchronization combined with a state reducer pattern that ensures data consistency between the in-memory state and persistent storage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a closure-scoped Map keyed by JSON.stringify({query, page}) to cache per-key results. On load(query, page), reuse fresh cache entries (TTL: 5 minutes); if a request is already in-flight, abort it before starting a n…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up a CSS grid with 3 columns (>=1024px), 2 columns (600–1023px), 1 column (<600px). Each card has header/body/footer; header uses Flexbox to place title and a CSS-only dropdown toggled by focus-wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a canvas-centric architecture with a single rendering loop powered by requestAnimationFrame. The WebSocket wrapper batches stroke events into an operation log and sends deltas to clients. A CRDT-like merge function…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a custom hook that combines useReducer with localStorage synchronization, using middleware to handle optimistic updates, validation, and automatic rollback on failures while maintaining state consistency acros…",
            "isCorrect": true
          }
        ],
        "explanation": "To solve this challenge, I would create a custom hook called `usePersistentState` that wraps React's useReducer with localStorage persistence. The hook would use a reducer function that handles actions like 'UPDATE_FIELD', 'SUBMIT_FORM', 'OPTIMISTIC_UPDATE', and 'ROLLBACK'. Each state change would be immediately persisted to localStorage using a debounced sync mechanism to avoid excessive writes, while also maintaining an in-memory version for immediate UI updates.\n\nFor optimistic updates, I would implement a middleware pattern where the reducer first applies the optimistic change to the state, then attempts the actual API call. If the API call succeeds, the optimistic state becomes the canonical state. If it fails, the middleware automatically triggers a rollback action that restores the previous state from localStorage. The hook would also include validation logic to ensure data integrity before persisting, and would handle edge cases like localStorage quota exceeded or browser storage disabled by falling back to in-memory state with appropriate user notifications.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-6",
        "questionId": "q-15031",
        "question": "You're building a dashboard with multiple components that need access to user authentication state, theme preferences, and notification settings. How would you architect the state management for this application, and what are the tradeoffs of your approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: implement a StreamedLayoutStore that streams per-org layout patches and feed it to the app with useSyncExternalStore. Use a memoized selector keyed by (orgId, widgetId) to compute per-widget",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use IndexedDB for offline storage, implement exponential backoff for WebSocket reconnection, and use Operational Transform (OT) for conflict resolution.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use React Context with useReducer for global state that doesn't change frequently, or a lightweight library like Zustand for better performance with frequent updates. For a dashboard with auth, theme, and notifications,…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Service Workers intercept network requests by registering event listeners for 'fetch' events, allowing them to control request handling through the Cache API and implement various caching strategies.",
            "isCorrect": false
          }
        ],
        "explanation": "For a dashboard requiring user authentication, theme preferences, and notification settings, I'd recommend a layered state management approach. React Context is ideal for authentication state since it's accessed throughout the application but rarely changes—it's simple, built-in, and doesn't require additional dependencies. Using useReducer locally helps manage complex dashboard state with multiple interrelated values (such as filtering, sorting, and pagination that need to work together). For theme and notifications, which may update more frequently, a lightweight store like Zustand offers better performance and a more intuitive API compared to Context, while avoiding the boilerplate of Redux. The tradeoff is that adding Zustand introduces an external dependency, though it's minimal compared to Redux. This hybrid approach balances simplicity for stable state (Context) with performance for frequently-updated state (Zustand).",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-7",
        "questionId": "q-17510",
        "question": "Context: You’re building a multi-tenant trading dashboard. Lighthouse flags per-route main-thread work and large bundles. Propose a practical plan to achieve per-route code splitting with lazy loading, enforce a per-route performance budget (initial bundle under 120 KB gzipped; LCP under 2.8s), add on-demand prefetching, and a Lighthouse CI workflow. Include minimal code sketches for a route guard, dynamic import, and a per-route Lighthouse config?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: Implement route-level code splitting using React.lazy() for each route, guarded by per-tenant feature flags so heavy widgets load only for eligible tenants. Cap the initial bundle to 120 KB gzipped via webpack/Nex…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a reusable data loader factory: createDataLoader(fetchFn, ttlMs) returns { load(key), invalidate(key), onChange(key, cb) }. The load(key) method must deduplicate concurrent requests for the same key, cache res…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a closure that holds a cache Map keyed by URL. fetchJson() returns cached Promise if present, otherwise fetch(url).then(res => res.json()) and stores the Promise. invalidate() clears the URL en",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a custom hook that synchronizes state with localStorage using debounced useEffect operations, handles race conditions through versioning mechanisms, validates data with schema validation libraries, and provide…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical skills in per-route code splitting, performance budgeting, lazy loading, and automated quality gates in CI/CD workflows—essential competencies for building performant multi-tenant applications at scale.\n\n## Key Concepts\n\n- Route-based code splitting and feature flags for tenant-specific functionality\n- Lighthouse CI with per-route thresholds and custom budgets\n- On-demand prefetching strategies and hydration trade-offs\n\n## Code Examples\n\n**Route Guard:**\n```javascript\nexport function withTenantGuard(WrappedComponent, tenantKey) {\n  const isAllowed = useTenantFlag(tenantKey);\n  return isAllowed ? <WrappedComponent /> : <Redirect to=\"/no-access\" />;\n}\n```\n\n**Dynamic Import:**\n```javascript\nconst HeavyWidget = React.lazy(() => import('./HeavyWidget'));\n```\n\n**Per-Route Lighthouse Config:**\n```json\n{\n  \"ci\": {\n    \"collect\": {\n      \"url\": \"https://app.example.com/dashboard\",\n      \"numberOfRuns\": 3\n    },\n    \"assert\": {\n      \"assertions\": {\n        \"first-contentful-paint\": [\"warn\", { \"maxNumericValue\": 2800 }],\n        \"interactive\": [\"warn\", { \"maxNumericValue\": 3800 }],\n        \"uses-text-compression\": [\"error\", {}]\n      }\n    }\n  }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-8",
        "questionId": "q-6455",
        "question": "You're shipping a large data analytics dashboard with a grid rendering 800+ columns; the initial bundle is 2.3MB and Lighthouse scores poorly due to heavy scripting. Propose a concrete approach to implement progressive hydration with per-column lazy-loading: how would you structure code-splitting, virtualization, and worker offloading, and how would you validate improvements using Lighthouse budgets and metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement progressive hydration for the grid by splitting columns into visible and offscreen groups. Hydrate only visible columns on initial mount, then lazy-load remaining columns via dynamic imports triggered by scrol…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I'd implement a resilient pipeline: first fetch initial items via REST, then subscribe to live alerts over WebSocket. Events are stored in an in-memory Map with a persisted queue in IndexedDB. A custom DOM-diff engine b…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Load initial alerts via REST (limit 50), render to DOM, then connect a WebSocket for live alerts. Deduplicate by alertId and preserve order; debounce DOM updates to 100ms; provide a keyboard-accessibl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a tenant-scoped streaming feature flags store using useSyncExternalStore for real-time updates, with a memoized selector selectFeature(tenantId, featureName) that returns {enabled, loading, visible}. T…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to apply progressive hydration, per-column lazy-loading, and worker offloading to meet Lighthouse budgets in a heavy dashboard. Also probes SSR/CSR trade-offs and how to validate improvements.\n\n## Key Concepts\n\n- Progressive hydration\n- Dynamic imports and code-splitting\n- Virtualization\n- Web Workers\n- Lighthouse budgets and metrics\n- IntersectionObserver for loading offscreen content\n\n## Code Example\n\n```javascript\n// Example skeleton: dynamic import of heavy cell renderer\nimport dynamic from 'next/dynamic';\nconst HeavyCell = dynamic(() => import('./cells/HeavyCell'));\n\n// Progressive hydration with IntersectionObserver\nconst useColumnHydration = (columnRef) => {\n  const [isHydrated, setIsHydrated] = useState(false);\n  \n  useEffect(() => {\n    const observer = new IntersectionObserver(\n      ([entry]) => {\n        if (entry.isIntersecting) {\n          setIsHydrated(true);\n          observer.disconnect();\n        }\n      },\n      { threshold: 0.1 }\n    );\n    \n    if (columnRef.current) {\n      observer.observe(columnRef.current);\n    }\n    \n    return () => observer.disconnect();\n  }, []);\n  \n  return isHydrated;\n};\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-9",
        "questionId": "q-10349",
        "question": "You're developing a React application where components need to access shared state but should only re-render when the specific state they depend on changes, not when other parts of the state change. How would you implement a state management solution using custom hooks with selector patterns to achieve granular subscription and prevent unnecessary re-renders?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Optimistic updates involve updating the UI immediately before server confirmation, with a rollback mechanism for failures. The implementation stores the previous state before applying the optimistic change, updates the…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To solve, encapsulate a cache Map and in-flight promises inside a closure. load(id) checks the cache; if a pending promise exists, return it; otherwise call fetchFn(id), store the Promise with a TTL t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "The CSS box model defines how elements are rendered with content, padding, border, and margin layers. With box-sizing: content-box (default), width/height apply only to content. With box-sizing: border-box, width/height…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a state management solution using custom hooks with selector patterns, leveraging React's useCallback and shallow comparison to allow components to subscribe only to specific state slices th",
            "isCorrect": true
          }
        ],
        "explanation": "To implement granular state subscription, I'd create a custom hook that manages the shared state internally and accepts a selector function as a parameter. This selector function would extract only the specific piece of state that the component needs. The hook would use React's useCallback to memoize the selector and implement a subscription mechanism that only triggers re-renders when the selected state actually changes, using shallow comparison to detect changes efficiently.\n\nThe implementation would involve a state store object that holds all shared state and a registry of subscriber functions. Each component using the hook would register its selector function, and the store would only notify subscribers when their selected state changes. This approach provides the benefits of shared state without the performance drawbacks of Context API, which causes all consumers to re-render when any context value changes.\n\nFor real-world usage, this pattern works well for applications like dashboards where different components need different pieces of data (e.g., user info vs. theme settings vs. notifications) but shouldn't re-render when unrelated data changes. The selector pattern makes the API intuitive - components just call `useSharedState(state => state.user.profile)` and only get updates when the user profile changes.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-10",
        "questionId": "q-16530",
        "question": "In a React app using a Redux-like store and a ThemeContext, implement a PerTenantLeaderboardPanel that shows a real-time top-N leaderboard for the currently selected tenant. Data streams over a WebSocket-like store emitting {tenantId, userId, score, ts}. Use useSyncExternalStore to subscribe to the external stream, a memoized selector keyed by (tenantId, topN) that computes a rolling 5-minute top-N list, render each row with a React.memo LeaderboardRow, and virtualization with react-window. Tenant switching occurs via a TenantContext; ensure subscriptions are cleaned up on switch. Provide code sketches for: stream wrapper, selector, LeaderboardRow, and panel integration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: build a per-tenant stream wrapper used by useSyncExternalStore to feed a top-N leaderboard. Implement a memoized selector keyed by tenantId and topN that filters events within a 5-minute win",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Uses a single WebSocket; per-instrument private state lives in a closure via a Map{instrumentId: {listeners, latest, timer, active, ver}}. subscribe registers a callback and opens WS on first subscrib",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "useState is ideal for managing simple, independent state variables where the next state depends only on the current state and a simple value, while useReducer is designed for complex state logic involving multiple sub-v…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use webpack-bundle-analyzer with performance budgets, implement dynamic imports with React.lazy, and set up CI checks to enforce bundle size limits.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the candidate's ability to design a real-time, per-tenant UI with robust dataflow, per-tenant memoization, and careful rendering to avoid churn during rapid tenant switches.\n\n## Key Concepts\n- useSyncExternalStore for external streams per tenant\n- memoized selectors with composite keys (tenantId, topN)\n- React.memo on row components\n- virtualization with react-window for lists\n- per-tenant context switching and cleanup\n\n## Code Example\n```javascript\n// Stream wrapper (per-tenant)\nfunction createTenantStreamStore() {\n  const listeners = new Map();\n  let state = {};\n  return {\n    subscribe(tenantId, cb) {\n      if (!listeners.has(tenantId)) listeners.set(tenantId, new Set());\n      listeners.get(tenantId).add(cb);\n      return () => listeners.get(tenantId).delete(cb);\n    },\n    emit(record) {\n      const { tenantId } = record;\n      state[tenantId] = state[tenantId] || [];\n      state[tenantId].push(record);\n      // keep small window for demo purposes\n      (listeners.get(tenantId) || new Set()).forEach((cb) => cb(state[tenantId]));\n    },\n    getSnapshot(tenantId) {\n      return (state[tenantId] || []).slice();\n    }\n  };\n}\n\n// Selector (per-tenant top-N with 5-min window)\nconst topNSelector = (state, tenantId, topN) => {\n  const now = Date.now();\n  const items = (state[tenantId] || []);\n  const windowItems = items.filter(i => now - i.ts <= 5 * 60 * 1000);\n  return windowItems\n    .slice()\n    .sort((a, b) => b.score - a.score)\n    .slice(0, topN);\n};\n\n// LeaderboardRow (memoized)\nconst LeaderboardRow = React.memo(function Row({ item, rank }) {\n  return (\n    <div className=\"row\" role=\"listitem\" aria-label={`#${rank} ${item.userId}`}>#{rank} {item.userId} — {item.score}</div>\n  );\n});\n\n// Panel integration (pseudocode)\nfunction PerTenantLeaderboardPanel({ topN = 10 }) {\n  const { tenantId } = useContext(TenantContext);\n  const streamStore = useMemo(createTenantStreamStore, []);\n  const snapshot = useSyncExternalStore(\n    (cb) => streamStore.subscribe(tenantId, cb),\n    () => streamStore.getSnapshot(tenantId),\n    () => streamStore.getSnapshot(tenantId)\n  );\n  const topList = useMemo(() => topNSelector(snapshot, tenantId, topN), [snapshot, tenantId, topN]);\n  return (\n    <List height={400} itemCount={topList.length} itemSize={40} width={300}>\n      {({ index, style }) => (\n        <LeaderboardRow item={topList[index]} rank={index + 1} style={style} />\n      )}\n    </List>\n  );\n}\n```\n\n## Follow-up Questions\n- How would you test cleanup on rapid tenant switches?\n- How would you handle missing ts clocks or out-of-order events in a live stream?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-11",
        "questionId": "q-28746",
        "question": "In a multi-workspace React app (ThemeContext for visuals; per-workspace streaming store via useSyncExternalStore), design a CrossWorkspaceAlertsPanel that shows a live, paginated list of alerts for the active workspace. Data arrives as a per-workspace stream. Requirements: implement a memoized selector keyed by (workspaceId, severity) returning the top 100 alerts; render items with a React.memo AlertRow; virtualization via react-window; handle bursts with a simple backpressure policy; ensure cleanup on workspace switch; enable cross-tab acknowledgement via BroadcastChannel?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Uses a single WebSocket; per-instrument private state lives in a closure via a Map{instrumentId: {listeners, latest, timer, active, ver}}. subscribe registers a callback and opens WS on first subscrib",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a memoized selector keyed by (workspaceId, severity) with an LRU cache capped at 100 items for the top alerts. Subscribe to the per-workspace stream via useSyncExternalStore; render each item",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To implement BranchActivityPanel: subscribe to a per-repo activity stream with useSyncExternalStore; memoize a selector keyed by [repoId, branchName] that aggregates 24h buckets; render with a React.m",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a combination of Context for implicit state passing and useReducer for centralized state updates. Use React.memo and separate contexts for state vs dispatch to prevent unnecessary re-renders of co",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical mastery of real-time React patterns: useSyncExternalStore integration, per-context memoization, and cross-tab coordination, all with efficient rendering via virtualization.\n\n## Key Concepts\n- useSyncExternalStore for a per-workspace stream\n- memoized selectors with bounded caches (LRU)\n- React.memo for item components and react-window for list virtualization\n- BroadcastChannel for cross-tab acknowledgments\n- cleanup on workspace switch and basic backpressure handling\n\n## Code Example\n```javascript\n// Skeleton selector cache (conceptual)\nclass SelectorCache {\n  constructor(limit = 100) {\n    this.limit = limit\n    this.map = new Map();\n  }\n  get(key) {\n    const v = this.map.get(key);\n    if (!v) return undefined;\n    // simple LRU touch\n    this.map.delete(key);\n    this.map.set(key, v);\n    return v;\n  }\n  set(key, value) {\n    if (this.map.has(key)) {\n      this.map.delete(key);\n    } else if (this.map.size >= this.limit) {\n      const firstKey = this.map.keys().next().value;\n      this.map.delete(firstKey);\n    }\n    this.map.set(key, value);\n  }\n}\n```\n\n## Follow-up Questions\n- How would you simulate burst bursts in tests and verify backpressure behavior?\n- What strategy would you use to ensure memory never grows unbounded under long-running streams?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-12",
        "questionId": "q-21733",
        "question": "Build a beginner chat widget: fetch last 20 messages with /api/messages?roomId=ROOM, then connect a WebSocket for live messages. Use a Service Worker to cache assets and chat payload for offline use. Ensure offline renders show cached messages, deduplicate by messageId, and append only new messages to the DOM. Provide core sketches for REST fetch, WebSocket handling, SW cache strategy, and a minimal render function?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Keep an in-memory Map<string, Trade> to dedupe by tradeId; queue updates and diff the DOM to append only unseen trades; on WebSocket error, switch to cached polling with a cancelable fetch; Service Wo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Service Workers intercept network requests by registering event listeners for 'fetch' events, allowing them to control request handling through the Cache API and implement various caching strategies.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Fetch the last 20 messages via GET /api/messages?roomId=ROOM, then establish a WebSocket connection for real-time updates. Maintain a Set of seen messageId values to deduplicate incoming messages and only append new mes…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a StoreContext with useReducer, a reducer handling SET_STATUS and UPDATE_LAST, and a selector function selectUserSlice(userId, state). StatusBadge is React.memo-wrapped and reads the current user",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to combine REST fetch, WebSocket, and Service Worker offline strategies in a practical, real-world scenario.\n\n## Key Concepts\n- **DOM updates**: Append-only rendering to avoid duplicate display\n- **Fetch**: Initial payload load for historical messages\n- **WebSocket**: Real-time message streaming\n- **Service Worker**: Offline caching for assets and payload\n- **Deduplication**: Using a Set to track seen messageId values\n\n## Code Examples\n\n### REST Fetch\n```javascript\nasync function loadMessages(roomId) {\n  const res = await fetch(`/api/messages?roomId=${roomId}&limit=20`);\n  const messages = await res.json();\n  messages.forEach(msg => addMessage(msg));\n}\n```\n\n### WebSocket Handling\n```javascript\nconst ws = new WebSocket(`ws://api.example.com/chat/${roomId}`);\nws.onmessage = (event) => {\n  const msg = JSON.parse(event.data);\n  if (!seenIds.has(msg.id)) {\n    seenIds.add(msg.id);\n    addMessage(msg);\n  }\n};\n```\n\n### Service Worker Cache Strategy\n```javascript\n// sw.js\nself.addEventListener('fetch', (e) => {\n  if (e.request.url.includes('/api/messages')) {\n    e.respondWith(\n      caches.open('chat-cache').then(cache => \n        cache.match(e.request).then(res => res || fetch(e.request))\n      )\n    );\n  }\n});\n```\n\n### Minimal Render Function\n```javascript\nconst seenIds = new Set();\nfunction addMessage(msg) {\n  if (seenIds.has(msg.id)) return;\n  seenIds.add(msg.id);\n  const el = document.createElement('div');\n  el.textContent = msg.text;\n  document.getElementById('chat').appendChild(el);\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-13",
        "questionId": "q-20954",
        "question": "Design a patch-based DOM runner for LiveGrid Studio: load the initial grid layout via REST, cache assets with a Service Worker (SW) using stale-while-revalidate, then subscribe to a real-time patch stream over WebSocket delivering per-cell updates as {cellId, op, value, ts, actor}. Maintain per-cell sequence numbers, buffer out-of-order patches, and apply patches by mutating DOM nodes; implement reconnection with exponential backoff and jitter. Provide minimal code sketches for REST/WS wrapper, patch processor, and per-cell renderer?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a TenantStreamWrapper for each tenant that exposes subscribe(callback), unsubscribe(callback), and getSnapshot() methods. Use useSyncExternalStore in the RealTimeTenantPanel to subscribe to the active tenant's…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a patch-based DOM renderer for LiveGrid Studio: load the initial grid layout via REST, cache assets with a Service Worker (SW) using stale-while-revalidate, then subscribe to a real-time patch stream over WebSock…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a combination of Context API with selectors, memoization, and subscription patterns to efficiently share real-time data while preventing unnecessary re-renders across components.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Keep an in-memory Map<string, Trade> to dedupe by tradeId; queue updates and diff the DOM to append only unseen trades; on WebSocket error, switch to cached polling with a cancelable fetch; Service Wo",
            "isCorrect": false
          }
        ],
        "explanation": "## Purpose\nTests the ability to implement offline-first, real-time collaboration with a patch-based DOM updater and proper sequencing. Covers DOM operations, fetch, WebSocket handling, Service Worker caching, and error handling with reconnection strategies.\n\n## Key Concepts\n- Patch-based DOM updates using small, immutable operations\n- Per-cell sequencing and out-of-order patch buffering\n- Offline-first architecture with Service Worker stale-while-revalidate\n- Reconnection strategy with exponential backoff and jitter\n\n## Code Example\n```javascript\n// REST/WS wrapper with reconnection\nclass GridClient {\n  constructor(url) { this.url = url; this.ws = null; this.seq = {}; }\n  async init() {\n    const layout = await fetch(this.url + '/grid').then(r => r.json());\n    this.render(layout);\n    this.connect();\n  }\n  connect() {\n    this.ws = new WebSocket(this.url.replace('http','ws') + '/stream');\n    this.ws.onmessage = e => this.process(JSON.parse(e.data));\n    this.ws.onclose = () => setTimeout(() => this.connect(), this.backoff());\n  }\n  backoff() { return Math.min(30000, (this.attempts || 1) * 1000 * (0.5 + Math.random())); }\n\n  // Patch processor with buffering\n  process(patch) {\n    const { cellId, seq } = patch;\n    if (!this.seq[cellId] || seq > this.seq[cellId]) this.pending[cellId]?.push(patch);\n    while (this.pending[cellId]?.length) {\n      const p = this.pending[cellId].sort((a,b) => a.seq - b.seq).shift();\n      if (p.seq === this.seq[cellId] + 1) { this.apply(p); this.seq[cellId] = p.seq; }\n    }\n  }\n  apply({cellId, op, value}) {\n    const el = document.getElementById(cellId);\n    if (op === 'set') el.textContent = value;\n    else if (op === 'add') el.insertAdjacentHTML('beforeend', value);\n    else if (op === 'del') el.remove();\n  }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-14",
        "questionId": "q-7969",
        "question": "Build a small real-time comments panel: on load, fetch the latest comments for a post via fetch; open a WebSocket to receive new comments live; use a Service Worker to cache assets and the last fetched comments for offline use; render new comments by appending to the DOM with a subtle slide-in animation; implement retry logic for failed fetches and automatic WebSocket reconnection with backoff; ensure offline mode shows cached comments?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Fetch initial comments for a post using the fetch API, then establish a WebSocket connection to receive new comments in real-time. Register a Service Worker to cache assets and the most recent comments for offline acces…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "- CLS risk: images loaded without explicit dimensions cause layout shifts as the browser discovers intrinsic size. - Impact: increases CLS in Core Web Vitals, degrades UX, and can hurt SEO. - Mitigations (state-of-the-a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set up a 6-column grid on desktop with grid-template-columns: repeat(6,1fr); scale down to 4 and 2 columns at 1200px and 800px. Use grid-auto-rows: minmax(8rem, auto) and grid-auto-flow: dense to allo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use CSS Grid with a masonry-like technique: `grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))` combined with `grid-auto-rows: 20px` and `grid-row: span N` classes for different aspect ratios. For responsive…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical integration of core frontend APIs (fetch, WebSocket, Service Worker) in a cohesive UI pattern with offline resilience.\n\n## Key Concepts\n- Fetch API for initial data retrieval and error handling\n- WebSocket lifecycle management and message handling\n- Service Worker registration and caching strategies\n- DOM manipulation with subtle animations and batching\n- Offline degradation patterns and data recovery\n- Exponential backoff for retry logic\n- Automatic reconnection mechanisms\n\n## Code Example\n```javascript\nasync function loadComments(postId) {\n  const res = await fetch(`/api/comments?postId=${p`",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-15",
        "questionId": "q-18818",
        "question": "Design and implement a client module LiveCanvas for a collaborative design app. Requirements: fetch initial canvas via REST, render shapes as DOM elements, subscribe to a WebSocket delta stream (patch format: {seq, op, shapeId, data}), apply patches in strict sequence order, buffer out-of-order patches and resync on gaps, implement exponential backoff for reconnect, offline edits stored in IndexedDB and flushed on reconnect, Service Worker caches assets for offline use, and describe integration points and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Redux-like pattern with Context + useReducer to hold a flags map. Implement actions: SET_FLAG for single flag updates and PATCH_FLAGS for bulk updates. Create a custom debounce hook (200ms) to batch server updates…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: Create a PluginManager that subscribes to tenant-scoped external streams using useSyncExternalStore and renders plugins via a Portal root. Each plugin receives data through memoized selectors keyed by tenantId…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: Implement route-level code splitting using React.lazy() for each route, guarded by per-tenant feature flags so heavy widgets load only for eligible tenants. Cap the initial bundle to 120 KB gzipped via webpack/Nex…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Fetch the initial canvas state via REST API, then establish a WebSocket connection to subscribe to a delta stream. Patches arrive in the format {seq, op, shapeId, data} with sequence numbers; apply patches in strict seq…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests real-time DOM updates, patch sequencing, offline-first architecture, and Service Worker integration for a collaborative design application.\n\n## Key Concepts\n- Patch sequencing with buffering for out-of-order delivery\n- WebSocket reconnection with exponential backoff\n- Offline storage using IndexedDB for pending mutations\n- Service Worker asset caching\n- DOM-based rendering vs virtual DOM diffing approaches\n\n## Code Example\n```javascript\nfunction applyPatch(state, patch) {\n  // minimal patch applier; ensure seq consistency\n  const { op, shapeId, data } = patch;\n  switch (op) {\n    case 'add':\n      state.shapes[shapeId] = data;\n      break;\n    case 'update':\n      Object.assign(state.shapes[shapeId], data);\n      break;\n    case 'delete':\n      delete state.shapes[shapeId];\n      break;\n  }\n  return state;\n}\n```\n\n## Integration Points and Trade-offs\n\n| Component | Integration Point | Trade-off |\n|-----------|------------------|-----------|\n| WebSocket | Delta stream subscription | Complexity vs real-time consistency |\n| IndexedDB | Offline mutation queue | Storage limits vs offline capability |\n| Service Worker | Asset caching | Stale assets vs offline access |\n| DOM Rendering | Shape display | Simplicity vs performance at scale |\n\n## Follow-up Questions\n1. How would you handle merge conflicts when reconnecting?\n2. What strategies would you use to prevent memory leaks in the patch buffer?\n3. How do you ensure idempotency when reapplying patches after resync?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-16",
        "questionId": "q-4494",
        "question": "You're building a high-density analytics dashboard for a retail platform. Implement a responsive widget grid using CSS Grid that supports 40+ widgets with variable heights; provide layout presets (compact, balanced, spacious) switchable via a settings panel. Ensure smooth reflow animations on preset switch, respect reduced-motion, and accessible keyboard navigation for focusing presets. How would you architect the CSS to achieve this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Load initial metrics with fetch('/api/metrics') and store them in an in-memory Map keyed by id. Open a WebSocket to ws://example/metrics for live updates. When WS messages arrive, update the Map with diffs and schedule…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a grid using CSS Grid with minmax columns and auto-fit, and expose three presets (compact, balanced, spacious) by toggling a class on the grid container. Each preset remaps grid-temp",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage a memoized selector to bucket the WebSocket stream into 1s intervals, feed only the latest bucket to a virtualized chart (react-window) inside a React.memo component, and guard re-renders wit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "React compares新旧VDOM trees using a heuristic O(n) algorithm, calculating minimal DOM patches. Keys enable stable element identity for efficient list updates.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a responsive, high-density grid with smooth reflow when layouts switch, while respecting accessibility and motion preferences.\n\n## Key Concepts\n- CSS Grid with minmax and auto-fit\n- Presets via CSS variables and class toggling\n- Animated reflow and accessibility considerations\n\n## Code Example\n```css\n.dashboard { display: grid; grid-template-columns: repeat(auto-fit, minmax(var(--col-w), 1fr)); grid-auto-rows: var(--row-h); gap: var(--gap); transition: grid-template-columns 320ms ease, grid-auto-rows 320ms ease; contain: layout paint; }\n.dashboard.compact { --col-w: 240px; --row-h: 90px; }\n.dashboard.balanced { --col-w: 260px; --row-h: 120px; }\n.dashboard.spacious { --col-w: 320px; --row-h: 150px; }\n@media (prefers-reduced-motion: reduce) { .dashboard { transition: none; } }\n```\n\n## Follow-up Questions\n- How would you handle widgets with intrinsic heights (e.g., charts with variable axis sizes) without layout jank?\n- How would you optimize for accessibility when using keyboard shortcuts to switch presets?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-17",
        "questionId": "q-16793",
        "question": "You're building a React application that makes API calls to update data. The user experience feels sluggish because the UI waits for the server response before updating. How would you implement optimistic updates with proper error handling and rollback in your state management, and what strategies would you use to handle race conditions when multiple rapid updates occur?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a patch-based DOM renderer for LiveGrid Studio: load the initial grid layout via REST, cache assets with a Service Worker (SW) using stale-while-revalidate, then subscribe to a real-time patch stream over WebSock…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: Build a PerTenantFormDesigner with a per-tenant Redux-like store, a per-tenant event bus for live schema updates, and a FormContext for theming and i18n. Subscriptions use useSyncEx",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Update the UI immediately on user action while making the API call in the background. Use a pending or optimistic state in your store to track in-flight requests. If the call succeeds, confirm the optimistic update; if…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "REST wrapper fetchCatalog() caches responses in a Service Worker using stale-while-revalidate; WebSocket client streams {productId, price, stock, ts} and uses exponential backoff on disconnect; a DOM",
            "isCorrect": false
          }
        ],
        "explanation": "Optimistic updates involve updating the UI state immediately when a user action occurs, before the server responds, to create a snappy user experience. This is typically implemented by maintaining both the current data and a 'pending' or 'optimistic' version of state in your store. When an action is dispatched, you immediately update the state optimistically, then make the API call. If the call succeeds, you confirm the optimistic update; if it fails, you rollback to the previous state and display an error message to the user.\n\nIn practice, this can be done with useReducer by tracking a 'status' field alongside your data, or with libraries like RTK Query which provide optimistic updates out of the box. For race conditions, you can implement request deduplication using unique request IDs, cancel previous pending requests when a new one is triggered, or use debouncing to batch rapid updates into a single request.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-18",
        "questionId": "q-4327",
        "question": "In a React dashboard app, a Redux-backed layout schema drives a grid of 200 widgets. Implement a LayoutRenderer that virtualizes rendering, updates via drag-and-drop, and preserves widget instances. Describe per-widget memoized selectors, useTransition for drag updates, and provide a concise code sketch for a memoized Widget and a virtualization wrapper?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use useSyncExternalStore to subscribe to the tenant-scoped stream. Implement a memoized selector keyed by tenant and minute to bucket events, feeding a virtualized Bar chart. Wrap Bar with React.memo.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use CSS Grid with 3 columns at wide widths, 2 at tablet, 1 on mobile. For each card, enforce 4:3 via aspect-ratio: 4/3 on the image area; overlay title and a read-time badge with position:absolute. Im",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-widget selectors with reselect to derive isVisible and data for the currently rendered subset; wrap each widget in React.memo with stable keys; implement a virtualization wrapper that renders",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a per-tenant Map of page entries, each storing {promise, data, expiresAt}. getPage(tenantId,page) checks cache; if missing or stale, return existing in-flight promise or start fetch('/api/'+tenant",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to reason about large, dynamic UIs where layout changes are frequent. It probes selectors, memoization, virtualization, and concurrent UI updates.\n\n## Key Concepts\n- Memoized per-widget selectors (reselect)\n- React.memo for widget instances\n- Virtualization to render in-view widgets only\n- useTransition during drag updates to avoid misses\n- Context for theming, Redux for layout\n\n## Code Example\n```javascript\n// Example memoized widget component\nconst Widget = React.memo(function Widget({ id, data }) {\n  // rendering logic\n  return null;\n});\n\n// Simple virtualization sketch\nfunction VirtualGrid({ items, renderItem, viewportHeight }) {\n  // simplified virtualizer\n  return items.map(item => renderItem(item));\n}\n```\n\n## Follow-up Questions\n- How would you ensure accessibility with virtualization (aria roles, live regions)?\n- How would you test this with a snapshot and a performance test for 200+ widgets?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-19",
        "questionId": "q-16729",
        "question": "Design a tiny FeatureToggle system in React using a Redux-like store (Context + useReducer). Build: 1) FeatureFlagProvider with actions SET_FLAG and PATCH_FLAGS; 2) a debounced server updater (200ms) that pushes flag changes; 3) a useFeatureFlag(name) hook returning [enabled, setEnabled]; 4) a DebugPanel that renders a memoized FlagRow per flag. Ensure re-renders are limited to changed flags and cleanup on unmount?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use CSS Grid with grid-template-columns: repeat(5, 1fr) and media queries (3 at medium, 1 for small). Reserve space with aspect-ratio: 1/1. Animate entrance with fade-up and stagger via nth-child dela",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: 1) Route-split dashboard with dynamic import and a small skeleton. 2) Use IntersectionObserver to toggle loading of HeavyChart via a lazy import only when visible. 3) Lazy-load images and precon",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use webpack-bundle-analyzer with performance budgets, implement dynamic imports with React.lazy, and set up CI checks to enforce bundle size limits.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Redux-like pattern with Context + useReducer to hold a flags map. Implement actions: SET_FLAG for single flag updates and PATCH_FLAGS for bulk updates. Create a custom debounce hook (200ms) to batch server updates…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to architect a simple, scalable UI state pattern with Context + useReducer, while handling real-time updates and preventing unnecessary re-renders. It also exercises memoization and debouncing—common patterns in feature-flag management UIs.\n\n## Key Concepts\n- React Context + useReducer for centralized state management\n- React.memo for selective re-render optimization\n- Debouncing external updates to reduce server load\n- Clean-up on unmount to prevent memory leaks\n\n## Code Example\n```javascript\n// FeatureFlagContext.tsx\nimport React, { createContext, useContext, useReducer, useEffect, useRef, useCallback } from 'react';\n\ntype Action =\n  | { type: 'SET_FLAG'; name: string; value: boolean }\n  | { type: 'PATCH_FLAGS'; patches: Record<string, boolean> };\n\ntype State = Record<string, boolean>;\n\nconst reducer = (state: State, action: Action): State => {\n  switch (action.type) {\n    case 'SET_FLAG':\n      return { ...state, [action.name]: action.value };\n    case 'PATCH_FLAGS':\n      return { ...state, ...action.patches };\n    default:\n      return state;\n  }\n};\n\nconst FeatureFlagContext = createContext<{\n  flags: State;\n  dispatch: React.Dispatch<Action>;\n} | null>(null);\n\n// Debounce hook\nconst useDebounce = (callback: () => void, delay: number) => {\n  const timeoutRef = useRef<NodeJS.Timeout>();\n  const callbackRef = useRef(callback);\n  \n  useEffect(() => { callbackRef.current = callback; });\n  \n  useEffect(() => {\n    return () => clearTimeout(timeoutRef.current);\n  }, []);\n  \n  return useCallback(() => {\n    clearTimeout(timeoutRef.current);\n    timeoutRef.current = setTimeout(() => callbackRef.current(), delay);\n  }, [delay]);\n};\n\nexport const FeatureFlagProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {\n  const [flags, dispatch] = useReducer(reducer, {});\n  const debouncedUpdate = useDebounce(() => {\n    // Push flag changes to server\n    fetch('/api/flags', { method: 'PATCH', body: JSON.stringify(flags) });\n  }, 200);\n\n  useEffect(() => { debouncedUpdate(); }, [flags]);\n\n  return (\n    <FeatureFlagContext.Provider value={{ flags, dispatch }}>\n      {children}\n    </FeatureFlagContext.Provider>\n  );\n};\n\n// Custom hook\nexport const useFeatureFlag = (name: string): [boolean, (value: boolean) => void] => {\n  const { flags, dispatch } = useContext(FeatureFlagContext)!;\n  const enabled = flags[name] ?? false;\n  const setEnabled = useCallback((value: boolean) => {\n    dispatch({ type: 'SET_FLAG', name, value });\n  }, [name]);\n\n  return [enabled, setEnabled];\n};\n\n// Debug Panel\nconst FlagRow = React.memo(({ name, value, onToggle }: { name: string; value: boolean; onToggle: () => void }) => (\n  <div>\n    <span>{name}</span>\n    <button onClick={onToggle}>{value ? 'ON' : 'OFF'}</button>\n  </div>\n));\n\nexport const DebugPanel = ({ flags, dispatch }: { flags: State; dispatch: React.Dispatch<Action> }) => {\n  return (\n    <div>\n      {Object.entries(flags).map(([name, value]) => (\n        <FlagRow\n          key={name}\n          name={name}\n          value={value}\n          onToggle={() => dispatch({ type: 'SET_FLAG', name, value: !value })}\n        />\n      ))}\n    </div>\n  );\n};\n```",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-20",
        "questionId": "q-8269",
        "question": "Design and implement a live chat panel with offline support: fetch initial history via REST, subscribe to a WebSocket for real-time messages, and register a Service Worker to cache the shell and API responses. When offline, queue outgoing messages in IndexedDB and sync on reconnect with exponential backoff. Ensure DOM updates are batched to avoid reflows and deduplicate incoming messages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Map-based cache keyed by URL + serialized opts and a separate in-flight map for in-progress promises. On call, return cached value if it exists and is fresh; otherwise reuse the in-flight promis",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A robust approach would fetch history via fetch('/api/chat/history'), render it, then connect to '/ws/chat' with exponential backoff on disconnect. Use IndexedDB to queue outbound messages offline and",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a CSS Grid mosaic with fixed baseline heights and containment to prevent layout thrashing. Configure `grid-auto-rows: 72px` with `minmax(72px, 12rem)` for scalable tiles, enable `grid-auto-flow: dense` for optimal p…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a CSS-only responsive media gallery: desktop two-column grid (image 60%, info 40%), mobile stacked. Each item uses a hidden checkbox as a Play/Pause toggle to control a caption animation via CS",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests integrating REST, WebSocket, a Service Worker, and offline-first UX in a cohesive module.\n\n## Key Concepts\n- Offline-first with IndexedDB\n- WebSocket reconnect/backoff and message ordering\n- Service Worker cache strategies (shell + API)\n- Batched DOM rendering to avoid reflows\n\n## Code Example\n```javascript\n// Sketch\nclass ChatClient { /* ... */ }\n```\n\n## Follow-up Questions\n- How would you test offline syncing with flaky networks?\n- How would you scale to multi-room chat and presence?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-21",
        "questionId": "q-20049",
        "question": "Design a RealTimeFeatureDashboard in a React app using ThemeContext and a per-tenant Redux-like store. Features stream per-tenant via useSyncExternalStore. Build a memoized selector keyed by (tenantId, featureName) to compute per-feature UI state, render a React.memo FeatureRow, and gate rendering with a FeatureGate wrapper. Provide code sketches for: store wrapper, selector, FeatureRow, and panel integration; ensure proper cleanup on tenant switch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a Lighthouse-first optimization for a Next.js app: 1) enforce per-route bundles with named chunks via dynamic imports, 2) lazy-load heavy UI (Banner) with next/dynamic and a fallback, 3) opt",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use CSS containment for layout/paint isolation, will-change sparingly for compositor hints, transform3d for GPU acceleration, virtualized rendering to handle large lists efficiently, and requestAnimationFrame batching t…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement progressive hydration for the grid by splitting columns into visible and offscreen groups. Hydrate only visible columns on initial mount, then lazy-load remaining columns via dynamic imports triggered by scrol…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a tenant-scoped streaming feature flags store using useSyncExternalStore for real-time updates, with a memoized selector selectFeature(tenantId, featureName) that returns {enabled, loading, visible}. T…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests your ability to design per-tenant, real-time UI systems with correct subscription lifecycles andization.\n\n## Key Concepts\n performance optimization through memo- useSyncExternalStore with per-tenant data streams\n- memoized selectors with stable identity keys\n- React.memo for row component optimization\n- conditional rendering with FeatureGate wrapper\n- proper cleanup on tenant switch\n\n## Code Example\n```javascript\n// Store wrapper - creates per-tenant subscription\nfunction createTenantFlagStore(tenantId) {\n  let flags = {};\n  const subscribers = new Set();\n  \n  return {\n    getSnapshot: () => flags,\n    getServerSnapshot: () => ({}),\n    subscribe: (callback) => {\n      subscribers.add(callback);\n      return () => subscribers.delete(callback);\n    },\n    updateFlag: (featureName, value) => {\n      flags = { ...flags, [featureName]: value };\n      subscribers.forEach(cb => cb(flags));\n    }\n  };\n}\n\n// Memoized selector keyed by tenantId and featureName\nconst featureSelectorCache = new Map();\nfunction selectFeature(tenantId, featureName, tenantStore) {\n  const key = `${tenantId}:${featureName}`;\n  if (!featureSelectorCache.has(key)) {\n    featureSelectorCache.set(key, createSelector(tenantStore, flags => ({\n      enabled: flags[featureName]?.enabled ?? false,\n      loading: flags[featureName]?.loading ?? true,\n      visible: flags[featureName]?.visible ?? true\n    })));\n  }\n  return featureSelectorCache.get(key)(useSyncExternalStore(\n    tenantStore.subscribe,\n    tenantStore.getSnapshot,\n    tenantStore.getServerSnapshot\n  ));\n}\n\n// Memoized FeatureRow\nconst FeatureRow = React.memo(function FeatureRow({ featureName, tenantId, store }) {\n  const feature = selectFeature(tenantId, featureName, store);\n  return (\n    <div className=\"feature-row\">\n      {feature.loading ? <Spinner /> : \n       feature.enabled ? <FeatureContent name={featureName} /> : \n       <FeatureDisabled />}\n    </div>\n  );\n}, (prevProps, nextProps) => {\n  return prevProps.tenantId === nextProps.tenantId && \n         prevProps.featureName === nextProps.featureName;\n});\n\n// FeatureGate wrapper\nfunction FeatureGate({ tenantId, featureName, children, fallback = null }) {\n  const store = useTenantStore(tenantId);\n  const feature = selectFeature(tenantId, featureName, store);\n  \n  return feature.enabled ? children : fallback;\n}\n\n// Panel integration with cleanup\nfunction FeatureDashboard({ tenantId }) {\n  const [currentTenant, setCurrentTenant] = useState(tenantId);\n  \n  useEffect(() => {\n    if (currentTenant !== tenantId) {\n      setCurrentTenant(tenantId);\n    }\n  }, [tenantId]);\n  \n  return (\n    <FeatureGate tenantId={tenantId} featureName=\"dashboard_v2\">\n      <FeatureRow \n        tenantId={tenantId} \n        featureName=\"realtime_metrics\"\n        store={useTenantStore(tenantId)}\n      />\n    </FeatureGate>\n  );\n}\n```\n\n## Key Takeaways\nThe solution demonstrates proper separation of concerns: the store handles subscription management, selectors compute derived state efficiently, and components use React.memo to avoid unnecessary re-renders. The FeatureGate provides declarative access control, and cleanup on tenant switch is handled by useEffect's cleanup function in the store subscription.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-22",
        "questionId": "q-21803",
        "question": "Design a CSS-only responsive tutorial gallery for a learning portal. Desktop grid: 3 columns, tablet: 2, mobile: 1. Each card has a 16:9 thumbnail area, a bold title, and a short description with a small tag. Implement a CSS-only density toggle (compact vs comfortable) using CSS variables to adjust padding and font size. Add a CSS entrance animation with a stagger when the page loads. Ensure accessibility: focus-visible outlines and prefers-reduced-motion. Provide a minimal HTML/CSS sketch with three cards?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement CSS grid with 3 columns on desktop, 2 on tablet, 1 on mobile using grid-template-columns. Each card contains a 16:9 thumbnail, a bold title, and a description. Add a CSS-only density toggle",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implemented a CSS-only dashboard layout using Grid with named areas: left Filters column and right Content with a 3-up card grid. The Filters pane collapses on small screens via a CSS-only checkbox to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Bootstrap initial data by fetching /init?tenantId=... via Response.body.getReader() and parsing the NDJSON stream. Establish a WebSocket connection to receive real-time events in the format {tenantId, id, ts, value}. Im…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use IndexedDB for offline storage, implement exponential backoff for WebSocket reconnection, and use Operational Transform (OT) for conflict resolution.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical CSS layout skills, responsive design, and CSS-only interactivity. It also checks accessibility considerations (focus-visible, reduced motion) and animation timing.\n\n## Key Concepts\n- CSS Grid for responsive columns\n- CSS variables and a checkbox hack for density toggle\n- Entrance animations with stagger and motion preferences\n- Accessibility: focus-visible outlines and prefers-reduced-motion\n\n## Code Example\n```javascript\n<!-- HTML skeleton (three cards) -->\n<div class=\"gallery\">\n  <div class=\"card\">\n    <div class=\"thumb\"></div>\n    <h3>Title</h3>\n    <p>Description <span class=\"tag\">New</span></p>\n  </div>\n  <div class=\"card\">...</div>\n  <div class=\"card\">...</div>\n</div>\n```\n```javascript\n/* CSS (density vars and grid) */\n:root{ --pad:16px; --gap:16px; --font:16px; }\nbody.compact{ --pad:8px; --gap:12px; --font:14px; }\n.gallery{ display:grid; grid-template-columns: repeat(3, 1fr); gap: var(--gap); padding: var(--pad); }\n@media (max-width: 900px){ .gallery{ grid-template-columns: repeat(2, 1fr); } }\n@media (max-width: 600px){ .gallery{ grid-template-columns: 1fr; } }\n.card{ padding: calc(var(--pad) * 0.75); border:1px solid #ddd; border-radius:8px; background:#fff; animation: enter 0.4s var(--delay,0s) ease; }\n.thumb{ aspect-ratio:16/9; background:#eee; border-radius:6px; }\n@keyframes enter{ from{ transform: translateY(6px); opacity:0; } to{ transform: translateY(0); opacity:1; } }\n```\n\n## Follow-up Questions\n- How would you extend density toggling to also adjust line-height for legibility? \n- How would you adapt the grid to gracefully handle cards with variable content heights without breaking alignment?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-23",
        "questionId": "q-29934",
        "question": "You're maintaining a small e-commerce product grid app. Lighthouse flags a large initial JS bundle and a non-critical analytics script loaded upfront. Propose a concrete plan to reduce the initial bundle, enable lazy-loading for non-critical parts, and implement a visibility gate to mount a heavy widget only when in view. Include a minimal code sketch for dynamic import and a Lighthouse CI validation approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Split the app into a core bundle and lazy-loaded parts; defer non-critical analytics to after user interaction or behind a feature flag. Use dynamic import for heavy components with React.lazy and Sus",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Approach: implement a StreamedLayoutStore that streams per-org layout patches and feed it to the app with useSyncExternalStore. Use a memoized selector keyed by (orgId, widgetId) to compute per-widget",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To solve, encapsulate a cache Map and in-flight promises inside a closure. load(id) checks the cache; if a pending promise exists, return it; otherwise call fetchFn(id), store the Promise with a TTL t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use useRef to track the latest callback value, implement proper cleanup in useEffect, and leverage useCallback to maintain a stable reference while ensuring compatibility with React's concurrent rendering model.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests beginner ability to reason about Lighthouse issues, bundle splitting, and lazy-loading with a realistic small app.\n\n## Key Concepts\n\n- Code splitting with dynamic imports\n- Lazy-loading assets/images\n- Visibility gate using IntersectionObserver\n- Lighthouse CI validation\n\n## Code Example\n\n```javascript\n// lazy load heavy widget\nconst Heavy = React.lazy(() => import('./HeavyWidget'));\n\nfunction Gate() {\n  const ref = useRef(null);\n  const [show, setShow] = useState(false);\n  useEffect(() => {\n    const io = new IntersectionObserver(([e]) => { if (e.isIntersecting) setShow(true); }, {threshold: 0.1});\n    if (ref.current) io.observe(ref.current);\n    return () => io.disconnect();\n  }, []);\n  return (\n    <div ref={ref}>\n      {show ? (\n        <Suspense fallback={<div>Loading…</div>}><Heavy/></Suspense>\n      ) : null}\n    </div>\n  );\n}\n```\n\n## Follow-up Questions\n\n- How would you translate this plan to a Lighthouse CI workflow?\n- How would you handle SSR or non-React pages?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-24",
        "questionId": "q-23642",
        "question": "Scenario: A data-dense React dashboard ships with a 2.5MB JS bundle and multiple heavy widgets. Design a Lighthouse-focused plan to meet a Performance score >= 90 and TTI <= 2.5s on a mid-tier device. Requirements: 1) route-based code splitting using React.lazy for heavy widgets; 2) implement dynamic imports with a simple prefetch strategy and a skeleton fallback; 3) build a lightweight Lighthouse-oriented metric wrapper using PerformanceObserver and a CI script to verify bundle size and entrypoints. Provide code sketches?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: implement route-based code-splitting for heavy widgets with React.lazy and Suspense; use prefetch hints for critical modules and skeletons while loading. Build a PerformanceObserver wrapper to l",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a state structure of [past, present, future] with a reducer handling UNDO, REDO, and STATE_UPDATE actions. Implement immutable updates, configurable history depth limits, and selective state tracking to manage memor…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use CSS Grid with CSS custom properties for dynamic sizing, CSS transforms for animations, and virtualization for performance.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a FormEditor using TenantContext and a per-tenant Redux-like store (useReducer + createContext). The fields array contains objects with id, label, type, required, min, and max properties. Implement the following…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical locally measurable performance tuning with real-world constraints: bundle size, lazy-loading, and Lighthouse metrics.\n\n## Key Concepts\n- Lighthouse metrics (TTI, FCP, Speed Index)\n- Code splitting strategies and prefetching\n- Skeleton UX and Suspense fallbacks\n- PerformanceObserver-based custom metrics\n\n## Code Example\n```javascript\n// heavy widget lazy load + skeleton\nconst HeavyWidget = React.lazy(() => import('./widgets/HeavyWidget'));\n```\n\n## Follow-up Questions\n- How would you handle edge cases with long tasks blocking the main thread?\n- How would you automate CI checks for Lighthouse scores on PRs?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-05T06:23:51.407Z",
    "version": 1
  }
]