[
  {
    "id": "test-frontend",
    "channelId": "frontend",
    "channelName": "Frontend",
    "title": "Frontend Knowledge Test",
    "description": "Test your understanding of Frontend with 25 questions.",
    "questions": [
      {
        "id": "frontend-q-0",
        "questionId": "q-5328",
        "question": "You're building a lightweight analytics dashboard for a new product used by MongoDB/LinkedIn teams. Lighthouse flags long main-thread tasks and oversized bundles. Describe a concrete plan to: 1) split code so a heavy chart loads via dynamic import and only when visible, 2) lazy-load feed images, 3) enforce a simple performance budget (initial JS under 120 KB gzip, LCP under 2.5s). Include practical code sketches and testing steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Split the chart into a lazy-loaded component using React.lazy and import() so it's not part of the initial bundle. Use an IntersectionObserver to mount it only when the chart area enters the viewport. For images, implem…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use useMemo to cache expensive Fibonacci calculations, memoizing results to prevent O(2^n) recursion. Trade-offs: memoization uses recursion + cache (O(n) space), tabulation uses iteration (O(1) space",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a robust state management pattern using useReducer with middleware to handle optimistic updates by immediately updating local state, processing server responses, and rolling back on failures while managing rac…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a canvas-centric architecture with a single rendering loop powered by requestAnimationFrame. The WebSocket wrapper batches stroke events into an operation log and sends deltas to clients. A CRDT-like merge function…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical skills in performance-focused frontend engineering: code-splitting, lazy-loading, and measuring impact with Lighthouse budgets, all in a realistic dashboard scenario.\n\n## Key Concepts\n- Code-splitting with dynamic import and React.lazy\n- IntersectionObserver for lazy-loading components and images\n- Native image lazy-loading with loading=\"lazy\" attribute\n- Performance budgets in webpack/parcel configuration\n- Lighthouse performance metrics and CI validation\n- Bundle analysis and optimization strategies\n\n## Implementation Strategy\nThe solution addresses all three requirements systematically: dynamic component loading with visibility detection, image lazy-loading with both native and fallback approaches, and enforceable performance budgets. The IntersectionObserver pattern ensures resources load only when needed, while webpack performance configuration provides automated budget enforcement during builds.",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-1",
        "questionId": "q-576",
        "question": "How would you optimize a React app's Lighthouse score from 65 to 90+ using bundle analysis and lazy loading?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a small DOM-diff engine that consumes patch objects with strict sequence IDs and timestamps, applying insert/delete/move changes only to affected seat nodes. Use fetch for initial da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Start by analyzing your bundle using webpack-bundle-analyzer to identify large dependencies and unused code. Then implement code splitting with React.lazy() for route-level components and dynamic imports for heavy compo…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use CSS Grid with responsive columns via media queries; mark featured tiles with data-featured and set grid-column: span 2; grid-row: span 2. Use grid-auto-flow: dense to fill gaps and fixed aspect-ra",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "- Microtasks: Promise.then, queueMicrotask, MutationObserver. - Macrotasks: setTimeout, setInterval, I/O callbacks. - Event loop per tick: - pop a macrotask from the macrotask queue and run it - drain the microtask queu…",
            "isCorrect": false
          }
        ],
        "explanation": "## Bundle Analysis\n- Use webpack-bundle-analyzer to identify large chunks and dependencies\n- Look for duplicate libraries, unused code, and oversized assets\n\n## Code Splitting Strategies\n```javascript\nconst LazyComponent = React.lazy(() => import('./HeavyComponent'))\n```\n- Implement route-level splitting with React.lazy() and Suspense\n- Use dynamic imports for heavy components and third-party libraries\n\n## Performance Optimizations\n- Enable tree shaking in webpack configuration\n- Replace moment.js with lighter alternatives like date-fns\n- Implement WebP images with appropriate fallbacks\n- Add prefetch and preload directives for critical resources\n- Remove unused dependencies and optimize asset delivery",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-2",
        "questionId": "q-25497",
        "question": "Implement a front-end dynamic module loader: createModuleLoader() returning { load(url), invalidate(url) }. load(url) should use dynamic import(), deduplicate concurrent loads for the same URL, cache the module namespace for a TTL, refresh in background near expiry, and cap in-flight imports to 4. Show usage with two URLs and TTL refresh?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Analyze the bundle with webpack-bundle-analyzer to identify large chunks, implement code splitting using React.lazy() for route-level components, add Suspense with loading states, and optimize assets by converting image…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a memoized selector keyed by segment and 1-second buckets; return a stable 2D array of counts for visible segments. Wrap the heatmap grid in React.memo and render via windowed virtualization. U",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "```javascript function createModuleLoader() { const cache = new Map(); const inflight = new Map(); const queue = []; const MAX_CONCURRENT = 4; const TTL = 5 * 60 * 1000; // 5 minutes const REFRESH_THRESHOLD = 0.8; // re…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use CSS Grid with breakpoints: 4 columns desktop, 2 tablet, 1 mobile. Each tile is a flex column for image and caption. A CSS-only toggle (checkbox #captions) shows/hides captions via :checked rules;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical JavaScript skills around dynamic imports, per-resource caching, and concurrency control in browser environments. It requires reasoning about promise sharing, memory management, and safe cache invalidation in real-world applications.\n\n## Key Concepts\n\n- **Single-flight pattern**: Deduplicates concurrent requests for the same URL using an `inflight` Map, ensuring multiple callers share the same promise\n- **TTL-based cache**: Stores module namespaces with timestamps, enabling time-based expiration\n- **Concurrency cap (semaphore)**: Limits in-flight imports to 4 using a queue mechanism when the limit is reached\n- **Background refresh**: Proactively refreshes cached modules when 80% of TTL has elapsed, keeping modules warm without blocking\n- **Cache invalidation**: Provides explicit `invalidate(url)` for manual cache clearing\n\n## Implementation Details\n\n1. **Promise deduplication**: When multiple `load()` calls target the same URL while loading is in progress, they share the same promise rather than triggering duplicate imports\n2. **Queue management**: When the concurrency limit (4) is reached, additional requests queue and process as slots become available\n3. **Stale-while-revalidate**: Modules are served from cache immediately while refreshing in the background near TTL expiry\n4. **Clean lifecycle**: The `inflight` Map is cleaned up in `.finally()` to prevent memory leaks\n\nThis pattern is commonly used in production applications for code splitting, lazy loading, and managing external script or component imports with optimal performance.",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-3",
        "questionId": "q-14706",
        "question": "You're building a React application with user preferences and form data that needs to persist across browser sessions and sync with a backend API, but you want to avoid blocking the UI during persistence operations. How would you implement a state management solution that handles optimistic updates, conflict resolution, and offline-first synchronization while maintaining a smooth user experience?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a closure-based queue that tracks active uploads, uses Promise.allSettled for partial failure handling, and manages concurrency with a semaphore pattern.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To solve, encapsulate a cache Map and in-flight promises inside a closure. load(id) checks the cache; if a pending promise exists, return it; otherwise call fetchFn(id), store the Promise with a TTL t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a layered state management solution with optimistic updates, using localStorage for immediate persistence and a background sync queue with conflict resolution strategies.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a tiny store: state = { theme:'light', toasts:[] }; reducer handles 'TOGGLE_THEME' and 'ADD_TOAST'. StoreProvider passes {state, dispatch}. useStoreSelector(sel) uses useMemo over sel(state)",
            "isCorrect": false
          }
        ],
        "explanation": "I would architect a comprehensive state management approach that prioritizes user experience through optimistic updates. First, create a custom hook combining useState for local state management with useEffect to persist changes to localStorage for immediate offline capability. The optimistic update pattern would apply changes to local state instantly while queuing background synchronization with the backend API.\n\nFor conflict resolution, implement a versioning system with timestamps or a last-write-wins strategy. Design a state machine with distinct states like 'synced', 'pending', 'conflict', and 'offline' to manage synchronization status. This ensures smooth UI performance by never blocking user interactions while maintaining data integrity through robust conflict detection and resolution mechanisms.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-4",
        "questionId": "q-10179",
        "question": "Implement a memoized fetchWithCache(url, options) that caches responses per URL with a TTL, deduplicates concurrent requests for the same URL using a closure-scoped in-flight map, and retries failed fetches with exponential backoff. Ensure TTL expiry, Cache-Control respect, and abort support via AbortController. Provide code sketches and discuss edge cases?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Redux-like pattern with Context + useReducer to hold a flags map. Implement actions: SET_FLAG for single flag updates and PATCH_FLAGS for bulk updates. Create a custom debounce hook (200ms) to batch server updates…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Load initial metrics with fetch('/api/metrics') and store them in an in-memory Map keyed by id. Open a WebSocket to ws://example/metrics for live updates. When WS messages arrive, update the Map with diffs and schedule…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a TenantWebSocket wrapper that subscribes to patches for a given tenantId; a patchApplier(target, patch) applies {op, path, value} to the DOM using data-path attributes with a batched update",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a closure-scoped Map cache: url -> {data, expiry}. fetchWithCache checks expiry; if valid, return cached data. If an inFlight[url] exists, await it; else start fetch with exponential backoff retry",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests mastery of closures, per-key caching, and robust fetch patterns without external libs. It also probes how edge cases like TTL invalidation and aborts are handled.\n\n## Key Concepts\n- Closure-scoped caches\n- In-flight deduplication\n- Exponential backoff\n- Cache-Control / max-age handling\n\n## Code Example\n```javascript\n// (sketch)\nfunction createFetchWithCache({ defaultTTL = 60000, maxRetries = 3 } = {}) {\n  const cache = new Map();\n  const inflight = new Map();\n  return async function fetchWithCache(url, opts = {}) {\n    // simplified\n  };\n}\n```\n\n## Follow-up Questions\n- How would you test race conditions when two calls occur almost simultaneously for the same URL?\n- How would you adapt this for streaming responses or large payloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-5",
        "questionId": "q-17170",
        "question": "You're building a React application that needs to implement optimistic updates - updating the UI immediately before a server request completes, but rolling back to the previous state if the request fails. How would you implement this pattern using useReducer, and what strategies would you use to handle race conditions when multiple rapid updates occur?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a small DOM-diff engine that consumes patch objects with strict sequence IDs and timestamps, applying insert/delete/move changes only to affected seat nodes. Use fetch for initial da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use grid: display: grid; grid-template-columns: repeat(4,1fr); gap:16px; @media (max-width:1200px){grid-template-columns: repeat(2,1fr);} @media (max-width:600px){grid-template-columns:1fr;} Tile uses",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use useReducer with pending, success, and error states, and implement a rollback mechanism by storing the previous state before applying optimistic updates. Handle race conditions using AbortController to cancel in-flig…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a combination of useRef to preserve form state across unmounts, useState for current values, and useEffect to sync with session storage, implementing a custom hook that encapsulates this logic wit",
            "isCorrect": false
          }
        ],
        "explanation": "Implementing optimistic updates with useReducer involves structuring your state to track the status of async operations. Define action types for OPTIMISTIC_UPDATE, UPDATE_SUCCESS, and UPDATE_ERROR. When dispatching the optimistic update, store the previous state in case the operation fails. On success, confirm the change; on error, dispatch the previous state back to restore it.\n\nTo handle race conditions with rapid updates, you can use an AbortController to cancel in-flight requests when new ones initiate. Alternatively, track a request ID or timestamp to ensure only the most recent response updates state, ignoring any stale responses.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-6",
        "questionId": "q-28933",
        "question": "When using a custom state management solution with useReducer, how would you implement selector functions to allow components to subscribe to specific slices of state while avoiding unnecessary re-renders? Explain the difference between primitive selectors and object selectors and why selector stability matters for performance.",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Maintain an in-memory Map<ProductId, {stock, price}> and a processedDeltaIds Set. WebSocket messages carry {deltaId, productId, stock, price, ts}. Apply deltas in arrival order, ignore duplicates or t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Primitive selectors (returning primitives) are memoization-friendly since === comparison works reliably. Object selectors cause constant re-renders because new object references break memoization. Use",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a factory function to encapsulate a private cache and an in-flight request map via closure. The get(path) method constructs a canonical URL, checks the cache for a fresh (non-expired) response, and returns it if ava…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant WebSocket bound with useSyncExternalStore to stream audit events. Memoize a selector keyed by (tenantId, timeWindow) that yields a bounded array of the last 120 events; render with React.memo EventRow c…",
            "isCorrect": false
          }
        ],
        "explanation": "Selectors are functions that extract specific pieces of state from a larger state tree, enabling components to subscribe only to the data they need. The key challenge is ensuring that a component re-renders only when its selected data actually changes, not when unrelated state updates.\n\nPrimitive selectors return simple values (strings, numbers, booleans). Since these are compared by value with ===, memoization works reliably—useMemo will only recompute when the actual value changes. For example, `state => state.user.name` returns a string; if the name stays 'Alice', the reference stays the same, preventing unnecessary re-renders.\n\nObject selectors return new object references on every render, even when the contained values haven't changed. This happens because JavaScript creates a new object literal each time the selector runs. Using `state => ({ items: state.items, total: state.total })` will cause constant re-renders because `{} !== {}`. The solution is either using shallow equality checks with useSelector patterns, creating stable references through memoization, or splitting into multiple primitive selectors.\n\nSelector stability is critical for performance. Inline arrow functions in JSX create new function references on every render, which breaks memoization. Best practice is to define selectors outside components or use useCallback with stable dependencies. For complex derived state, a selector factory pattern can cache computations based on input parameters.\n\nA practical implementation uses a custom hook that wraps the context consumer, passing a selector function and an optional equality function. Components call `useStore(selector, equalityFn?)` and receive only the selected slice. The equality function determines when a re-render should occur—shallowEqual compares each property, while deepEqual does recursive comparison for nested objects.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-7",
        "questionId": "q-28950",
        "question": "Design a beginner-friendly React module that uses a Redux-like store (useReducer + Context) to manage two prefs: darkMode (boolean) and locale (string). Hydrate data from a simulated server stream via a useSyncExternalStore-like wrapper; emit updates on a timer. Build PrefsPanel with two controls: a Switch for darkMode and a Select for locale. Memoize a selector keyed by [theme, locale] returning a derived greeting and UI state. Render each control with a small React.memo Row. Cleanup on accountId switch. Provide code sketches for the store, stream wrapper, selector, and wiring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a closure to hold a cache Map (url -> {data, expiry, promise}). The load(url) method checks if a valid (non-expired) entry exists and returns cached data via Promise.resolve. If a fetch is already in-flight for the…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "- CLS risk: images loaded without explicit dimensions cause layout shifts as the browser discovers intrinsic size. - Impact: increases CLS in Core Web Vitals, degrades UX, and can hurt SEO. - Mitigations (state-of-the-a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "UseReducer + Context store with actions TOGGLE_DARK and SET_LOCALE; ThemeContext provides current theme; a useSyncExternalStore-like wrapper subscribes to a simulated stream that pushes updates; a mem",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "React compares新旧VDOM trees using a heuristic O(n) algorithm, calculating minimal DOM patches. Keys enable stable element identity for efficient list updates.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to combine React context with a Redux-like store in a beginner-friendly scenario, and to reason about subscriptions, cleanup, memoization, and small optimizations.\n\n## Key Concepts\n\n- useReducer and Context for simple store\n- useSyncExternalStore-like wrapper for streams\n- memoization with a selector keyed by [theme, locale]\n- React.memo for row components\n- effect cleanup when accountId changes\n\n## Code Example\n\n```javascript\n// store, stream, selector wiring sketches\n```\n\n## Follow-up Questions\n\n- How would you test the memoization correctness?\n- What changes if the stream can emit rapid bursts?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-8",
        "questionId": "q-408",
        "question": "You're building a responsive dashboard with a complex grid layout that must support dynamic widget resizing, reordering via drag-and-drop, and maintain performance with 100+ widgets. How would you architect the CSS grid system to handle these requirements while ensuring smooth animations and preventing layout thrashing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use CSS Grid with CSS custom properties for dynamic sizing, CSS transforms for animations, and virtualization for performance.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a grid container: display: grid; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); gap: 16px. Each card: position: relative; overflow: hidden; transition: transform .15s ease, box-shad",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan to maintain a Map(id -> item) and apply inserts/updates/deletes from delta events; only patch changed items. Batch DOM updates with a small document fragment. Persist last 100 items in IndexedDB",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Update the UI immediately on user action while making the API call in the background. Use a pending or optimistic state in your store to track in-flight requests. If the call succeeds, confirm the optimistic update; if…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nOkta needs engineers who can build complex, performant UIs at scale. This tests understanding of CSS Grid architecture, performance optimization, and real-world dashboard challenges.\n\n## Expected Answer\nStrong candidates will discuss: CSS Grid with custom properties for dynamic sizing, transform-based animations to avoid layout thrashing, virtualization techniques for large widget counts, and fallback strategies for older browsers.\n\n## Code Example\n```css\n.dashboard {\n  display: grid;\n  grid-template-columns: repeat(var(--cols, 12), 1fr);\n  gap: var(--gap, 16px);\n  will-change: transform;\n}\n\n.widget {\n  grid-column: span var(--span, 3);\n  transform: translate3d(0, 0, 0);\n  transition: transform 0.2s ease;\n}\n\n.widget.dragging {\n  z-index: 1000;\n  transform: scale(1.05) translate3d(0, 0, 0);\n}\n```\n\n## Follow-up Questions\n- How would you handle widget persistence across page refreshes?\n- What strategies would you use to minimize reflow during drag operations?\n- How would you implement responsive breakpoints for different screen sizes?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-9",
        "questionId": "q-664",
        "question": "Question: Create a responsive 3-column feature grid using CSS Grid that collapses to a single column on narrow viewports, with a hover animation that gently scales each card and reveals a caption with a slide-in effect, while keeping focus-visible styles and respecting prefers-reduced-motion?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Explain using CSS Grid: container with display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 16px. Cards have a hover transform: scale(1.03) translateY(-4px) and a subtle bo",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a real-time NotificationCenterPanel for a multi-tenant admin dashboard. Each tenant receives its own streaming feed via useSyncExternalStore. Implement a memoized selector keyed by (tenantId, notificationId) to d…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-widget selectors with reselect to derive isVisible and data for the currently rendered subset; wrap each widget in React.memo with stable keys; implement a virtualization wrapper that renders",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Split bundles by route using React.lazy for non-critical features to reduce initial payload. Implement lazy-loading for heavy charts using IntersectionObserver with Suspense to defer rendering until components enter the…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical CSS skills for layout, responsiveness, and accessible motion.\n\n## Key Concepts\n\n- CSS Grid\n- Accessibility: prefers-reduced-motion, focus-visible\n- CSS transitions/transform\n\n## Code Example\n\n```css\n.grid{ display:grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap:16px; }\n.card{ transition: transform .25s ease, box-shadow .25s ease; }\n.card:hover{ transform: scale(1.03) translateY(-4px); box-shadow: 0 6px 16px rgba(0,0,0,.15); }\n.card-caption{ transform: translateY(8px); opacity:.0; transition: transform .25s ease, opacity .25s ease; }\n.card:hover .card-caption{ transform: translateY(0); opacity:1; }\n@media (prefers-reduced-motion: reduce){ .card, .card-caption{ transition: none; } }\n```\n\n## Follow-up Questions\n\n- How would you implement keyboard accessibility for the hover effect?\n- How would you adapt this to a fluid breakpoint system without using fixed min widths?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-10",
        "questionId": "q-20193",
        "question": "Design a real-time per-organization collaborative dashboard in a React app. The active organization is provided via OrganizationContext. Users drag-and-drop resizable widgets in a grid (e.g., react-grid-layout). Layout updates are streamed from the server via a streaming store consumed with useSyncExternalStore. Implement optimistic layout edits with rollback on server rejection, and memoize a selector keyed by (orgId, widgetId) to compute per-widget placements. Ensure cleanup on organization switch and SSR compatibility. Provide code sketches for: stream wrapper, optimistic patcher, per-org selector, and grid integration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a FilterContext with a reducer for {query, category}. Create a useDebounce hook returning a debounced value. LightSearchBar updates context instantly but uses debouncedQuery to dispatch a Re",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a central manifest and dynamic import() per route, with a RouteLoader that maps route to import('./apps/'+name). Add link rel=prefetch hints for likely routes. Implement a BudgetEnforcer that hook",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a CSS-only solution: a grid container with container-type inline-size and grid-template-columns: repeat(3, 1fr). Use @container queries to switch to 1 column on narrow viewports. Each card has",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: implement a StreamedLayoutStore that streams per-org layout patches and feed it to the app with useSyncExternalStore. Use a memoized selector keyed by (orgId, widgetId) to compute per-widget",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design real-time, multi-user UIs with proper isolation across organizations; evaluates useSyncExternalStore, memoization, optimistic UI, and SSR considerations.\n\n## Key Concepts\n\n- useSyncExternalStore\n- React-grid-layout drag-and-drop\n- Memoized selectors keyed by (orgId, widgetId)\n- Optimistic patches with rollback\n- Organization-scoped cleanup; SSR-safe\n\n## Code Example\n\n```javascript\n// sketch: Stream wrapper\nexport function createOrgLayoutStream(orgId) { /* ... */ }\n\n// sketch: optimistic patcher\nfunction patchLayoutOptimistically(current, patch) { /* ... */ }\n\n// sketch: per-org selector\nconst selectOrgPlacement = (state, orgId, widgetId) => { ... }\n```\n\n## Follow-up Questions\n\n- How to test concurrent drags across users?\n- How to reconcile out-of-order events or secure patches?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-11",
        "questionId": "q-18330",
        "question": "You're building a knowledge-base landing page for a large engineering org. Create a CSS‑only responsive layout using CSS Grid and Flexbox that features a fixed left filter rail on desktop (180px) that collapses to a bottom-sheet on mobile via a CSS toggle, and a 4-column article grid on desktop, 2 on tablet, 1 on mobile. Cards must be equal height, with an image area, title, and meta row; add a CSS entrance animation with a subtle stagger, and ensure focus-visible and prefers-reduced-motion?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a state management solution with optimistic updates using useReducer for state transitions, separate loading/error tracking, and rollback functionality when API calls fail.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a knowledge-base landing page with a fixed left rail (180px) on desktop that collapses to a bottom-sheet on mobile via a CSS toggle; a 4-column article grid on desktop, 2 on tablet, 1 on mobile.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Fetch initial items via fetch('/api/feed'), render them, then connect via new WebSocket('wss://example.com/feed'). On message, parse JSON and push to a local array; batch DOM updates with requestAnima",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant Map of page entries, each storing {promise, data, expiresAt}. getPage(tenantId,page) checks cache; if missing or stale, return existing in-flight promise or start fetch('/api/'+tenant",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses mastery of complex CSS layouts that blend Grid and Flexbox, with a CSS-only responsive rail toggle and accessible animation. It mirrors real-world knowledge-base UIs at scale, demanding careful height management, predictable reflow, and robust keyboard accessibility.\n\n## Key Concepts\n\n- CSS Grid for multi-column responsive layouts\n- Flexbox for consistent card heights\n- CSS-only state toggles (checkbox hack)\n- Entrance animation with stagger; prefers-reduced-motion\n- focus-visible and keyboard accessibility\n\n## Code Example\n\n```css\n.grid { display:grid; grid-template-columns: repeat(4, 1fr); gap:1rem; }\n.card { display:flex; flex-direction:column; }\n@media (max-width:1024px){ .grid{ grid-template-columns: repeat(2,1fr);} }\n@media (max-width:600px){ .rail{ transform: translateY(100%); } .grid{ grid-template-columns: 1fr; } }\n@keyframes fadeUp { from{opacity:0; transform: translateY(6px);} to{opacity:1; transform: translateY(0);} }\n.card{ animation: fadeUp .25s ease forwards; animation-delay: .0s; }\n.card:nth-child(2){ animation-delay:.04s; }\n```\n\n## Follow-up Questions\n\n- How would you verify accessibility and animation performance across devices?\n- How would you adapt for RTL, theming, and content density?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-12",
        "questionId": "q-1342",
        "question": "You're building a React application where certain components need to maintain state that persists across page refreshes but doesn't need to be shared globally. How would you implement a state management solution that combines local component state with browser storage, and what are the key considerations for handling synchronization, performance, and potential race conditions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use useRef to track the latest callback value, implement proper cleanup in useEffect, and leverage useCallback to maintain a stable reference while ensuring compatibility with React's concurrent rendering model.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a custom hook that combines useState with localStorage/sessionStorage, implementing proper error handling, synchronization patterns, and performance optimizations for persistent local component state.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "The implementation comprises three key components. First, a fetch wrapper with in-flight request deduplication and stale-while-revalidate caching to serve chat history efficiently. Second, a WebSocket wrapper that manag…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "- Virtualize the list with @tanstack/react-virtual to render only visible rows (10k+ items) and amortize updates. - Memoize per-row components (React.memo) and stabilize props with useCallback, passing item.id instead o…",
            "isCorrect": false
          }
        ],
        "explanation": "The optimal solution involves developing a custom hook that integrates useState with localStorage/sessionStorage. Wrap storage operations in try-catch blocks to handle errors like quota exceeded or private browsing mode. Initialize state from storage on first render, use useEffect to persist changes, and include a loading state for asynchronous storage access. For synchronization, implement a storage event listener to detect cross-tab changes and use version keys for schema migrations. Performance considerations include debouncing frequent writes, implementing lazy loading, and using JSON.parse/stringify efficiently while handling potential race conditions between storage reads and writes.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-13",
        "questionId": "q-19748",
        "question": "Front-end/performance scenario: A React SPA with a heavy dashboard route triggers a 1.1 MB gzipped bundle and long main-thread tasks per Lighthouse. Design a concrete plan to improve LCP and TTI by: 1) route-based code splitting with dynamic imports for the dashboard and its widgets, 2) lazy-loading a heavy charting library only when the dashboard becomes visible, 3) a simple performance budget gate in CI and a Lighthouse CI check that initial JS <= 120 KB gzip and LCP <= 2.5s. Provide concrete code sketches for dynamic import, visibility-triggered preload, and budgeting test?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a BidStore (Map bidId->bid) and a renderPatch(fn) that diffs the list against DOM, updating only changed nodes. On fetch, populate store and render top-20 by amount. On WebSocket mes",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Explain a concrete approach: fetch REST data with a controlled timeout and retry policy; Service Worker caches REST and tile data using stale-while-revalidate; maintain a WebSocket connection with exp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement route-based splitting: const Dashboard = React.lazy(()=>import('./Dashboard')); render inside Suspense. Lazy-load heavy charting lib on first dashboard visibility via IntersectionObserver, t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use useMemo to cache expensive Fibonacci calculations, memoizing results to prevent O(2^n) recursion. Trade-offs: memoization uses recursion + cache (O(n) space), tabulation uses iteration (O(1) space",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical split, lazy loading, and Lighthouse budgets under realistic constraints.\n\n## Key Concepts\n- Route splitting, lazy loading, Lighthouse budgets, visibility APIs.\n\n## Code Example\n```javascript\n// Dynamic import sketch\nconst Dashboard = React.lazy(() => import('./Dashboard'));\n```\n\n```javascript\n// Visibility-triggered preload\nuseEffect(() => {\n  const io = new IntersectionObserver(([e]) => {\n    if (e.isIntersecting) import('./heavy-chart-lib');\n  });\n  if (containerRef.current) io.observe(containerRef.current);\n  return () => io.disconnect();\n}, []);\n```\n\n```bash\n# CI budget check pseudo\nif [ $INITIAL_JS_KB_GZIP -gt 120 ]; then exit 1; fi\nif [ $LCP_MS -gt 2500 ]; then exit 1; fi\n```\n\n## Follow-up Questions\n- How would you measure LCP more robustly in CI and local dev?\n- How would you adapt budgets for mobile vs desktop?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-14",
        "questionId": "q-27608",
        "question": "Design a LiveInfoPanel for a beginner frontend app: load initial analytics via fetch, then subscribe to live events via a WebSocket channel. Cache responses with a Service Worker using stale-while-revalidate to support offline usage. Implement a minimal DOM patch engine to update a metrics table and an events feed, ordering by timestamp and deduplicating by id. Reconnect WebSocket with exponential backoff on disconnects; queue offline events or edits for replay. Provide code sketches for: fetch wrapper, WebSocket wrapper, and DOM patcher?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a fetch wrapper that serves from the Cache API with a short TTL and SW update, a WebSocket wrapper that auto-reconnects with exponential backoff and pings, and a DOM patcher that maintains a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a FormEditor using TenantContext and a per-tenant Redux-like store (useReducer + createContext). The fields array contains objects with id, label, type, required, min, and max properties. Implement the following…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Load the AuditTrail panel only when it becomes visible or on first user interaction by using dynamic import('./AuditTrailPanel') behind a gate. Show a skeleton while loading, and cache the loaded modu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Fetch initial feed, on success render; on failure, load from IndexedDB cache. WebSocket connects to ws://example/ws, on message parse and append only if new ID not seen. Service Worker caches assets a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical integration of fetch, Service Worker offline caching, and WebSocket real-time streaming, plus a minimal DOM patch engine. It also exercises offline-first UX with a replay queue and robust reconnect/backoff.\n\n## Key Concepts\n- Cache API / Service Worker lifecycle\n- WebSocket reconnect with exponential backoff\n- Minimal DOM patching / diffing\n- Ordering by timestamp and dedup by id\n- Offline queue and replay\n\n## Code Example\n```javascript\n// Minimal fetch wrapper\nasync function fetchWithCache(url, opts = {}) {\n  const cacheKey = url;\n  const cache = await caches.open('liveinfo-cache');\n  // Try cache first\n  const cached = await cache.match(cacheKey);\n  if (cached) {\n    const text = await cached.text();\n    try { return JSON.parse(text); } catch { /* fallthrough */ }\n  }\n  // Fallback to network\n  const res = await fetch(url, opts);\n  if (res.ok) {\n    const clone = res.clone();\n    cache.put(cacheKey, clone);\n    return res.json();\n  }\n  throw new Error('Network error');\n}\n```\n```javascript\n// Minimal WebSocket wrapper\nfunction createWebSocket(url, onMessage) {\n  let ws;\n  let delay = 1000;\n  let stopped = false;\n  const connect = () => {\n    if (stopped) return;\n    ws = new WebSocket(url);\n    ws.addEventListener('message', (e) => onMessage(JSON.parse(e.data)));\n    ws.addEventListener('close', () => {\n      if (stopped) return;\n      setTimeout(connect, delay);\n      delay = Math.min(delay * 2, 30000);\n    });\n  };\n  connect();\n  return {\n    close() { stopped = true; ws && ws.close(); },\n    send(d) { ws && ws.readyState === WebSocket.OPEN && ws.send(JSON.stringify(d)); }\n  };\n}\n```\n```javascript\n// Minimal DOM patcher\nclass PatchStore {\n  constructor(root) {\n    this.root = root;\n    this.items = new Map();\n  }\n  patch(update) {\n    const { id, ts, ...rest } = update;\n    const existing = this.items.get(id);\n    if (!existing || ts >= existing.ts) {\n      this.items.set(id, { ts, ...rest });\n    }\n  }\n  render() {\n    // naive render: rebuilds list in order\n    const list = [...this.items.values()].sort((a,b)=> a.ts - b.ts);\n    this.root.innerHTML = '';\n    for (const it of list) {\n      const el = document.createElement('div');\n      el.textContent = `${it.id}: ${it.value} @ ${new Date(it.ts).toLocaleTimeString()}`;\n      this.root.appendChild(el);\n    }\n  }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-15",
        "questionId": "q-17797",
        "question": "On a product-listing page, Lighthouse flags a bloated JS bundle and slow LCP. Propose a concrete, beginner-friendly plan using: 1) code-splitting with dynamic imports so heavy components load only when visible, 2) lazy-loading images and fonts with proper font-display, 3) a simple Lighthouse CI workflow to enforce budgets (initial JS under 100 KB gzipped, LCP under 2.8s). Include a small snippet showing a visibility gate wrapper around a heavy widget?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build a minimal DOM with a list container and a Retry button. On page load, fetch the REST endpoint to render the initial list, then open a WebSocket connection for live updates. Implement a Service Worker to cache the…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design and implement a per-key resource loader in vanilla JavaScript. Expose createResourceLoader(fetcher) returning { load(key, ttl), invalidate(key) }. load(key, ttl) should deduplicate in-flight loads per key, cap co…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use fetchAlerts() to load 20 alerts, store in a Map keyed by alertId, and render with a document fragment. On WebSocket message, if the id is new, enqueue the item and render in a batched frame. Servi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: split the UI with dynamic imports and a visibility gate so heavy components load only when on screen. Use images and fonts lazy-loading (font-display: swap). Add skeletons and measure impact. Va",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of applying beginner-friendly performance optimizations and verifying them with a CI workflow in a realistic page.\n\n## Key Concepts\n- Code-splitting with dynamic imports\n- Visibility-driven loading (IntersectionObserver)\n- Lazy-loading media and web fonts\n- Skeleton placeholders and Lighthouse budget testing\n- Lighthouse CI integration\n\n## Code Example\n```javascript\nimport React, { Suspense, useState, useEffect } from 'react';\nconst HeavyWidget = React.lazy(() => import('./HeavyWidget'));\n\nfunction VisibleGate({ children }) {\n  const [show, setShow] = useState(false);\n  useEffect(() => {\n    const io = new IntersectionObserver(([e]) => {\n      if (e.isIntersecting) { setShow(true); io.disconnect(); }\n    });\n    const el = document.getElementById('gate');\n    if (el) io.observe(el);\n    return () => io.disconnect();\n  }, []);\n  return show ? <Suspense fallback={<div className=\"skeleton\" />}>{children}</Suspense> : null;\n}\n\nexport default function Page() {\n  return (\n    <div>\n      <div id=\"gate\"></div>\n      <VisibleGate>\n        <HeavyWidget />\n      </VisibleGate>\n    </div>\n  );\n}\n```\n\n## Follow-up Questions\n- How would you quantify impact after deployment?\n- What trade-offs come with SSR vs SPA for this plan?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-16",
        "questionId": "q-9877",
        "question": "You're developing a React application that needs to handle optimistic updates for user interactions while maintaining state consistency when API calls fail. How would you design a state management pattern using a combination of useState, useReducer, and custom hooks to implement rollback mechanisms and loading states, ensuring the UI remains responsive and data integrity is preserved?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Replace moment.js with date-fns (80% smaller), use lodash-es for tree-shaking, implement code splitting with React.lazy() for routes, add dynamic imports for heavy components, and use webpack-bundle-analyzer to track im…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "REST wrapper fetchCatalog() caches responses in a Service Worker using stale-while-revalidate; WebSocket client streams {productId, price, stock, ts} and uses exponential backoff on disconnect; a DOM",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Uses a single WebSocket; per-instrument private state lives in a closure via a Map{instrumentId: {listeners, latest, timer, active, ver}}. subscribe registers a callback and opens WS on first subscrib",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a state management solution with optimistic updates using useReducer for state transitions, separate loading/error tracking, and rollback functionality when API calls fail.",
            "isCorrect": true
          }
        ],
        "explanation": "To implement optimistic updates with rollback capabilities, I'd design a state management system centered around useReducer to handle complex state transitions. The reducer would manage three key states: the 'current' state (what's displayed), the 'pending' state (optimistic changes), and a queue of operations that need to be synced with the server. This separation allows us to quickly update the UI while tracking what needs to be committed or rolled back.\n\nThe implementation would include custom hooks like useOptimisticState that wrap the reducer logic and provide methods for optimistic updates. These hooks would handle the async API calls, automatically applying optimistic changes immediately, then either committing them on success or rolling back on failure. The state structure would include loading indicators and error states for each operation, ensuring the UI remains responsive throughout the process.\n\nFor data integrity, I'd implement operation IDs and timestamp tracking to prevent duplicate operations and handle race conditions. The custom hooks would also expose cleanup methods to handle component unmounting, preventing memory leaks and ensuring that pending operations are properly resolved or cancelled.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-17",
        "questionId": "q-7427",
        "question": "You're building a real-time trading dashboard with 80+ live widgets that render charts and KPIs in a responsive CSS Grid mosaic. Updates may reflow tiles and cause CLS. Propose a CSS strategy to minimize layout shifts while preserving smooth animations, including how to fix baseline heights, containment, and accessible motion. Provide an implementation sketch with key CSS rules?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Context API is ideal for simple, localized state like authentication or theme that doesn't change frequently. Redux excels for complex state with many actions, time-travel debugging, and middleware requirements.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a CSS Grid mosaic with fixed baseline heights and containment to prevent layout thrashing. Configure `grid-auto-rows: 72px` with `minmax(72px, 12rem)` for scalable tiles, enable `grid-auto-flow: dense` for optimal p…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use useReducer with pending, success, and error states, and implement a rollback mechanism by storing the previous state before applying optimistic updates. Handle race conditions using AbortController to cancel in-flig…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Fetch the last 20 messages via GET /api/messages?roomId=ROOM, then establish a WebSocket connection for real-time updates. Maintain a Set of seen messageId values to deduplicate incoming messages and only append new mes…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis assesses practical CSS performance and layout stability for dense, dynamic dashboards, focusing on containment, grid behavior, and motion design under continuous data updates.\n\n## Key Concepts\n\n- CSS containment (`contain: layout paint`)\n- Grid auto-placement and fixed row heights\n- Motion design: transforms vs height changes\n- Accessibility: `prefers-reduced-motion`, focus management\n\n## Code Example\n\n```css\n.grid {\n  display: grid;\n  grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));\n  grid-auto-rows: 72px;\n  grid-auto-flow: dense;\n  gap: 12px;\n  contain: layout paint;\n}\n\n.widget {\n  contain: layout style paint;\n  will-change: transform;\n  transition: transform 0.3s ease;\n}\n\n.widget.updating {\n  transform: scale(0.98);\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .widget {\n    transition: none;\n  }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-18",
        "questionId": "q-20503",
        "question": "Design a multi-tenant live document viewer: REST loads the doc, WebSocket streams patches filtered by tenantId, and a Service Worker caches payloads/assets for offline use. Implement a patch engine: patches like {op, path, value} apply to the DOM via data-path with a batched updater (requestAnimationFrame). On tenant switch, close WS, purge per-tenant caches, and rebind to the new tenant. Provide code sketches for: 1) TenantWebSocket, 2) patchApplier, 3) SW fetch/cache, 4) fetchWithETag, 5) tenant-switch flow?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a patch-based DOM updater for a multi-tenant chat feed: fetch the initial thread snapshot via REST, then open a WebSocket to receive live patches as {op:'insert'|'update'|'delete', path:stri",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a minimal patch engine: maintain a Map<string,Element> keyed by path; on patch, locate target, apply add/replace/remove with precise DOM ops (insertBefore, replaceChild). WebSocket r",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a custom hook that reads from localStorage only after mounting (to avoid hydration mismatches), debounce/throttle storage writes to prevent race conditions, and separate the persistence logic from",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a TenantWebSocket wrapper that subscribes to patches for a given tenantId; a patchApplier(target, patch) applies {op, path, value} to the DOM using data-path attributes with a batched update",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses real‑world experience wiring REST + WebSocket streams with per‑tenant isolation, while keeping the DOM efficient and offline resilient.\n\n## Key Concepts\n- WebSocket filtering by tenant and patch dispatching\n- JSON Patch-like operations applied to DOM via data-path mapping\n- Batched DOM updates via requestAnimationFrame\n- Service Worker offline cache with stale-while-revalidate\n- Tenant lifecycle: cleanup and rebinding\n\n## Code Example\n```javascript\n// TenantWebSocket sketch\nclass TenantWebSocket {\n  constructor(url, tenantId, onPatch){...}\n  connect(){...}\n  close(){...}\n}\n```\n\n## Follow-up Questions\n- How ensure patch order correctness when bursts exceed frame budget?\n- How handle conflicting patches to same node?\n- How test offline mode with simulated network conditions?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-19",
        "questionId": "q-12624",
        "question": "You're building a responsive product docs portal used by engineers at Tesla/IBM/Google. Create a CSS layout using CSS Grid and Flexbox that keeps a fixed 240px left navigation on desktop, converts to a 48px collapsed rail on mobile via a CSS toggle, and a main content area that uses a card grid with equal heights. Implement a CSS-only entrance animation for cards with a subtle stagger and ensure accessibility (focus-visible, prefers-reduced-motion). Provide a minimal HTML/CSS sketch for the layout and one tile?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a memoized selector to bucket the WebSocket stream into 1s intervals, feed only the latest bucket to a virtualized chart (react-window) inside a React.memo component, and guard re-renders wit",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a two-column grid on desktop: grid-template-columns: 1fr auto 1fr; place events in alternating cells to align left/right with a central timeline line via a pseudo-element on the timeline. Cards ar",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: split the UI with dynamic imports and a visibility gate so heavy components load only when on screen. Use images and fonts lazy-loading (font-display: swap). Add skeletons and measure impact. Va",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a two‑pane grid: on wide viewports 240px left rail and 1fr content; on mobile collapse the rail to 48px with a CSS toggle (no JS). The content grid uses grid-template-columns: repeat(auto-fill,",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests mastery of modern CSS layout: two-pane grids, CSS-only UI state (no JS), and production-friendly animation with accessibility in mind. It also checks how candidates balance visual polish with stability at scale.\n\n## Key Concepts\n\n- CSS Grid two-pane layouts with responsive breakpoints\n- CSS-only toggle for mobile collapsible rail\n- grid-auto-columns and minmax for fluid card grids\n- subtle entrance animations with stagger and motion preferences\n- accessibility: focus-visible outlines and reduced-motion handling\n\n## Code Example\n\n```css\n.grid { display: grid; grid-template-columns: 240px 1fr; gap: 16px; }\n@media (max-width: 800px) {\n  .grid { grid-template-columns: 48px 1fr; }\n}\n.card { animation: fadeUp .4s var(--delay) both; }\n@keyframes fadeUp { from { transform: translateY(8px); opacity:0 } to { transform: translateY(0); opacity:1 } }\n```\n\n## Follow-up Questions\n\n- How would you adapt this with container queries for nested components with variable content heights?\n- How would you verify accessibility across assistive technologies and ensure robust reduced-motion behavior?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-20",
        "questionId": "q-419",
        "question": "You're building a real-time food delivery tracking app. How would you implement a WebSocket connection that handles network interruptions and maintains order status updates when the app goes offline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use WebSocket with exponential backoff retry, service worker for offline caching, and IndexedDB for local state persistence.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement createStreamingFetcher(url) using fetch with an AbortController. Get resp.body.getReader() and stream NDJSON lines. Expose stream as an async iterable that yields parsed JSON objects per line. Maintain an inte…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a CSS grid with 3 columns desktop, 2 tablet, 1 mobile. Cards are flex-column; caption slides in via transform/opacity on hover/focus. Implement a CSS-only play toggle: a checkbox that switches a C",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Load initial comments with fetch and a timeout via AbortController; then open a WebSocket for live updates with exponential backoff reconnect. Use a Service Worker to cache shell and GET /api/comments",
            "isCorrect": false
          }
        ],
        "explanation": "For a real-time delivery tracking app, you need to handle network reliability:\n\n1. **WebSocket Implementation**: Create a WebSocket connection with automatic reconnection using exponential backoff strategy\n2. **Service Worker**: Register a service worker to intercept network requests and cache order data\n3. **Offline Storage**: Use IndexedDB to store order status updates locally when offline\n4. **Sync Strategy**: Implement background sync to push cached updates when connection restores\n5. **State Management**: Maintain optimistic UI updates while confirming with server\n\nKey considerations: connection state management, conflict resolution, data consistency, and user experience during offline scenarios.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-21",
        "questionId": "q-9931",
        "question": "In a multi-tenant React frontend (ThemeContext for visuals; data from a Redux-like store streaming 100k events/min across tenants via WebSocket), design a RealTimeActivityPanel that shows a 1-minute bucketed activity timeline for the currently selected tenant. Requirements: useSyncExternalStore to subscribe to the external stream; a memoized selector keyed by tenantId and minute returning a 60-item array; a React.memo ListItem; virtualization (react-window); and useTransition for bursts. Include code sketches for the store wrapper, selector, ListItem, and panel integration; ensure proper cleanup on tenant switch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Fetch initial feed, on success render; on failure, load from IndexedDB cache. WebSocket connects to ws://example/ws, on message parse and append only if new ID not seen. Service Worker caches assets a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Fetch initial dashboard config per tenant, e.g., GET /api/tenant/{id}/dashboard; wrap a per-tenant WebSocket for live mutations {tenantId, op, item}; batch DOM updates via a Fragment and requestAnimat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: build a per-tenant stream wrapper used by useSyncExternalStore to feed a top-N leaderboard. Implement a memoized selector keyed by tenantId and topN that filters events within a 5-minute win",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Answer: Implement useSyncExternalStore to subscribe to a tenant-scoped WebSocket stream; bucket events by minute in a Map<tenantId, MinuteBucket[]>, with a memoized selector (tenantId, minute) returni",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to thread real-time data through React rendering while avoiding churn when tenants switch. It combines useSyncExternalStore, memoized selectors, virtualization, and transitions under realistic load.\n\n## Key Concepts\n- useSyncExternalStore for external streams\n- tenant-scoped memoized selectors\n- virtualization with react-window\n- React.memo for item components\n- useTransition for burst updates\n- proper cleanup on tenant switch\n\n## Code Example\n```javascript\n// Pseudo: store wrapper and selector sketch\n```\n\n## Follow-up Questions\n- How would you test memory usage under tenant switching?\n- How to handle out-of-order events and late arrivals?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-22",
        "questionId": "q-18504",
        "question": "In a React app with ThemeContext and a per-organization Redux-like store, design an OrganizationDataGrid that shows a paginated list of users for the currently selected organization. Data arrives via a server stream exposed through useSyncExternalStore. Requirements: implement an optimistic edit flow for inline title edits with rollback on server rejection; memoize a selector keyed by (orgId, rowId) to compute per-row display; render rows with a React.memo UserRow; virtualize with react-window; ensure cleanup on organization switch. Provide code sketches for: stream wrapper, selector, UserRow, and grid integration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe an OrganizationDataGrid that subscribes to a per-org stream with useSyncExternalStore, uses a memoized selector keyed by (orgId, rowId) for per-row display, and renders via React.memo UserRow",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a patch-based DOM renderer for LiveGrid Studio: load the initial grid layout via REST, cache assets with a Service Worker (SW) using stale-while-revalidate, then subscribe to a real-time patch stream over WebSock…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a LivePresence panel that maintains a per-channel Map of userId to status. A WebSocket wrapper handles connect/reconnect with backoff; on each message, update the map and apply a min",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement CSS grid with 3 columns on desktop, 2 on tablet, 1 on mobile using grid-template-columns. Each card contains a 16:9 thumbnail, a bold title, and a description. Add a CSS-only density toggle",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world patterns: per-organization data isolation, real-time streams, and performance through memoization and virtualization\n\n## Key Concepts\n- useSyncExternalStore for external streams\n- memoized selectors with composite keys\n- React.memo for row components\n- optimistic UI updates with rollback\n- virtualization (react-window) and cleanup on context switches\n\n## Code Example\n```javascript\n// Stream wrapper: sub per org, expose currentUsers and editsPending\nfunction createOrgStream(orgId){ /* ... */ }\n// Selector: derive row display from orgId, rowId, store\nconst selectRow = (state, orgId, rowId) => { /* ... */ }\n// UserRow: memoized row\nconst UserRow = React.memo(({row}) => {/* ... */})\n// Grid integration: useSyncExternalStore + VirtualizedList\n```\n\n## Follow-up Questions\n- How would you write tests for optimistic update paths?\n- How do you handle reconnection and partial data reconciliation?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-23",
        "questionId": "q-20083",
        "question": "You're building a React application that needs to implement undo/redo functionality for a form editor. How would you design a state management solution using useReducer that maintains a history of state changes, handles optimistic updates, and implements proper rollback when operations fail?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a combination of Context API with selectors, memoization, and subscription patterns to efficiently share real-time data while preventing unnecessary re-renders across components.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a memoized selector (reselect) to bucket incoming 50k events/sec into 1s intervals, keeping raw stream in a separate Redux slice. The chart is a React.memo component receiving only the current buc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use useReducer with a history array containing past and future states, implement undo/redo actions that swap current state with history entries, and maintain a separate 'pending' state for optimistic",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use useReducer or useState with pending/success/error states, maintain previous state for rollback, and implement AbortController/version tracking to handle race conditions.",
            "isCorrect": false
          }
        ],
        "explanation": "To implement undo/redo with optimistic updates, you'd structure your reducer to maintain three key pieces of state: past (array of previous states), present (current state), and future (array of states for redo). The reducer would handle actions like UNDO, REDO, UPDATE, and COMMIT or ROLLBACK for async operations.\n\nFor optimistic updates, when a user makes a change, you immediately update the 'present' state while storing the previous state in case of failure. When the async operation succeeds, you push the old state to 'past' and clear the temporary rollback state. On failure, you restore from the stored previous state. This approach ensures the UI feels responsive while maintaining data integrity.\n\nKey implementation patterns include: using immutable update patterns (like Immer) for clean state mutations, lazy initialization for expensive initial state calculations, batching multiple related updates, and using useReducer's dispatch for complex state transitions. You should also consider limiting history size to prevent memory issues and implementing middleware-like patterns for side effects.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-24",
        "questionId": "q-6792",
        "question": "You're building an enterprise React 18 dashboard with a ThemeContext and a Redux store. A waveform widget streams up to 50k events/sec via WebSocket and must render a 2-second rolling window at 60fps. Design an end-to-end approach: data path, off-main-thread bucketing with a WebWorker, memoized selectors bucketed by 33ms, a forwardRef memoized chart component using OffscreenCanvas when available, and useTransition for bursts. Include concrete code sketches and integration points?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: implement a StreamedLayoutStore that streams per-org layout patches and feed it to the app with useSyncExternalStore. Use a memoized selector keyed by (orgId, widgetId) to compute per-widget",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a patch-based DOM updater for a multi-tenant chat feed: fetch the initial thread snapshot via REST, then open a WebSocket to receive live patches as {op:'insert'|'update'|'delete', path:stri",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a streaming path where raw events go into Redux, a WebWorker buckets data into 33ms frames, a memoized selector derives the rolling 2-second window, and a memoized chart renders with a canvas",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Split the page into a core shell and lazy-loaded widgets. Load the hero gallery in the main bundle, and import('./Reviews') for the reviews widget behind a loading threshold (viewport enter). Use Susp",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes high-performance React patterns, multi-threaded data processing, and real-world integration between Context, Redux, and UI rendering at scale.\n\n## Key Concepts\n- useTransition for bursty updates\n- Web Workers and OffscreenCanvas for heavy data processing\n- memoized selectors and useMemo\n- useSyncExternalStore or Redux selectors\n- ARIA updates for accessibility\n\n## Code Example\n```javascript\n// worker.js\nlet bucketSize = 33;\nonmessage = (e) => {\n  const events = e.data.events;\n  const buckets = [];\n  for (let ev of events) {\n    const idx = Math.floor(ev.ts / bucketSize);\n    buckets[idx] = (buckets[idx] || 0) + 1;\n  }\n  postMessage({ buckets });\n}\n```\n\n```javascript\n// main.jsx\nconst Chart = React.memo(React.forwardRef(function Chart({ buckets }, ref){\n  // draw into canvas\n}));\n```\n\n```javascript\n// wiring\nconst w = new Worker('./worker.js');\nw.onmessage = (ev)=> setBuckets(ev.data.buckets);\n```\n\n## Follow-up Questions\n- How would you test frame timing and memory?\n- How would you handle worker failures or backpressure?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-23T11:28:29.429Z",
    "version": 1
  }
]