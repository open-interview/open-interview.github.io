[
  {
    "id": "test-frontend",
    "channelId": "frontend",
    "channelName": "Frontend",
    "title": "Frontend Knowledge Test",
    "description": "Test your understanding of Frontend with 25 questions.",
    "questions": [
      {
        "id": "frontend-q-0",
        "questionId": "q-23096",
        "question": "Front-end design challenge: Build a per-tenant, offline-capable collaborative dashboard. Requirements: initial data via fetch('/api/tenant/{id}/dashboard'), subscribe to per-tenant updates over a WebSocket feed, and render a virtualized list of items. Use a Service Worker to cache assets and the feed payload for offline use; reconcile incoming mutations with local edits; handle tenant switch cleanup to avoid memory leaks. Provide code sketches for: data fetch, WebSocket wrapper, DOM batching strategy, and Service Worker cache strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use justify-content: space-between on the container, and set the middle item to margin: 0 auto or position it absolutely centered.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a robust state management pattern using useReducer with middleware to handle optimistic updates by immediately updating local state, processing server responses, and rolling back on failures while managing rac…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set up a 6-column grid on desktop with grid-template-columns: repeat(6,1fr); scale down to 4 and 2 columns at 1200px and 800px. Use grid-auto-rows: minmax(8rem, auto) and grid-auto-flow: dense to allo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Fetch initial dashboard config per tenant, e.g., GET /api/tenant/{id}/dashboard; wrap a per-tenant WebSocket for live mutations {tenantId, op, item}; batch DOM updates via a Fragment and requestAnimat",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests building an offline-first, multi-tenant real-time UI with robust subscription management and DOM efficiency.\n\n## Key Concepts\n- DOM batching and virtualization\n- per-tenant WebSocket streams and cleanup\n- fetch + offline fallback with Service Worker cache\n- conflict resolution for local edits vs remote mutations\n\n## Code Example\n```javascript\n// pseudo: WebSocket wrapper per tenant\nfunction createTenantSocket(tenantId, onMutation){/* ... */}\n// DOM batching via document fragment and RAF\nfunction renderBatch(items){/* ... */}\n```\n\n## Follow-up Questions\n- How would you test tenant switch cleanup? \n- How to scale if tenant has 10k items?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-1",
        "questionId": "q-12765",
        "question": "Design a PerTenantComposablePanel in React that renders per-tenant widgets gated by feature flags stored in a Redux-like streaming store. Widgets (ChartWidget, ActivityLog) load lazily via React.lazy and Suspense and render only when the tenant’s flag is on. Subscribe to per-tenant updates with useSyncExternalStore; reset subscriptions on tenant switch; memoize a selector keyed by tenantId and widgetId to avoid unnecessary re-renders. Provide code sketches for: per-tenant store wrapper, memoized selector, lazy WidgetContainer, panel integration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a PerTenantComposablePanel in React that renders per-tenant widgets gated by feature flags stored in a Redux-like streaming store. Widgets (ChartWidget, ActivityLog) load lazily via React.lazy and Suspense and re…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create a hybrid state management system that uses a custom hook to intelligently route state between useState for local state and a global store when components are composed together, using context pr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a reusable data loader factory: createDataLoader(fetchFn, ttlMs) returns { load(key), invalidate(key), onChange(key, cb) }. The load(key) method must deduplicate concurrent requests for the same key, cache res…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a real-time NotificationCenterPanel for a multi-tenant admin dashboard. Each tenant receives its own streaming feed via useSyncExternalStore. Implement a memoized selector keyed by (tenantId, notificationId) to d…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests advanced React patterns including per-tenant state management, lazy loading optimization, and proper subscription cleanup using modern React primitives.\n\n## Key Concepts\n- **useSyncExternalStore**: Provides consistent per-tenant streaming updates without tearing\n- **React.lazy and Suspense**: Enable on-demand widget loading for better performance\n- **Memoized selector pattern**: Prevents unnecessary re-renders by caching selectors keyed by tenantId and widgetId\n- **Subscription cleanup**: Critical for preventing memory leaks when switching between tenants",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-2",
        "questionId": "q-8635",
        "question": "Build a client-side analytics loader that accepts a set of entityIds and returns their analytics in a single batched call. Requirements: coalesce concurrent requests for overlapping IDs into one fetch to /api/analytics?ids=..., memoize results with a TTL-based cache, support cancellation via AbortController, implement exponential backoff retries on 5xx, and ensure rapid filter changes don't trigger duplicate fetches. Provide code sketches for the loader, a memoized per-IDs cache, and a React hook that consumes it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use React Context with useReducer for global state that doesn't change frequently, or a lightweight library like Zustand for better performance with frequent updates. For a dashboard with auth, theme, and notifications,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a closure-friendly cache keyed by a sorted IDs string, with a pending map of in-flight promises. If a request for the same key exists, return that Promise. Fetch with AbortController, store result",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage CSS Grid for the layout and a CSS-only density toggle using a hidden checkbox to swap CSS variables: --cols, --gap, --cardH, and image aspect-ratio. Desktop: 4 columns, tablet: 2, mobile: 1. Implement a respons…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a fixed-height card grid: grid-template-columns: repeat(5, 1fr) desktop, 3 tablet, 2 mobile. Each card is relative; poster uses aspect-ratio:16/9. Overlay at bottom is absolute and initially trans",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests designing a deduplicating, batched data loader with caching and cancellation for dashboards at scale.\n\n## Key Concepts\n\n- Closure-scoped caches and in-flight maps\n- Batched fetches and TTL caching\n- AbortController and cancellation\n- Exponential backoff retry strategy\n- Race-condition mitigation with rapid filter changes\n\n## Code Example\n\n```javascript\n// Loader sketch for analytics batching and caching\nfunction createAnalyticsLoader(fetchFn, ttl = 60000) {\n  const cache = new Map();\n  const inFlight = new Map();\n\n  const keyFor = (ids) => Array.from(new Set(ids)).sort().join(',');\n\n  async function load(ids, { signal } = {}) {\n    const key = keyFor(ids);\n    const now = Date.now();\n    const cached = cache.get(key);\n    if (cached && cached.expiresAt > now) return cached.value;\n\n    if (inFlight.has(key)) return inFlight.get(key);\n\n    const p = (async () => {\n      let attempt = 0;\n      const controller = new AbortController();\n      const merged = signal\n        ? (signal.addEventListener ? signal : { addEventListener: () => {} }).bind(null)\n        : null;\n      while (attempt <= 3) {\n        try {\n          const resp = await fetchFn(`/api/analytics?ids=${encodeURIComponent(key)}`, {\n            signal: signal ?? controller.signal,\n          });\n          if (!resp.ok) throw new Error('Fetch error');\n          const data = await resp.json();\n          cache.set(key, { value: data, expiresAt: now + ttl });\n          inFlight.delete(key);\n          return data;\n        } catch (e) {\n          if (signal?.aborted) throw new DOMException('aborted', 'AbortError');\n          attempt++;\n          if (attempt > 3) {\n            inFlight.delete(key);\n            throw e;\n          }\n          await new Promise((r) => setTimeout(r, Math.pow(2, attempt) * 100));\n        }\n      }\n    })();\n\n    inFlight.set(key, p);\n    return p;\n  }\n\n  return { load };\n}\n```\n\n## Follow-up Questions\n\n- How would you extend this to support cache invalidation when analytics data changes?\n- How would you test deduplication behavior with concurrent requests in Jest?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-3",
        "questionId": "q-13111",
        "question": "You're building a CMS-era design system timeline component. Implement a responsive timeline using CSS Grid and Flexbox that shows events in two columns on desktop with a central vertical line and alternating left/right cards, collapsing to a single column on mobile. Cards should have equal widths, a subtle entrance animation with stagger, and accessible focus indicators, respecting prefers-reduced-motion. Provide a minimal HTML/CSS sketch with six events and the CSS rules to achieve the behavior?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a two-column grid on desktop: grid-template-columns: 1fr auto 1fr; place events in alternating cells to align left/right with a central timeline line via a pseudo-element on the timeline. Cards ar",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage container queries by giving the widget container a container-type: inline-size and letting grid-template-columns respond to the widget width: 4 columns at wide inline sizes, 3 at mid, and 1 a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a LiveNotes widget that fetches /notes on load, connects to a WebSocket for real-time updates, and renders changes with efficient diff-based DOM updates. Use a sequenceId for in-order delivery and deduplicate…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-route runtime budget that monitors TTI/LCP with PerformanceObserver and adjusts dynamic imports accordingly. Track route-specific bundle sizes, and gate non-critical imports behind use",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDemonstrates architectural thinking for a reusable timeline with CSS Grid and no JS for layout, while enforcing accessibility and motion constraints.\n\n## Key Concepts\n- Grid with alternating placement for left/right alignment\n- Central timeline line via pseudo-element\n- Animation timing with per-item delays\n- Responsive switch to single column; prefers-reduced-motion\n\n## Code Example\n```javascript\n/* CSS sketch for timeline grid and item styles */\n.timeline{ display:grid; grid-template-columns:1fr auto 1fr; gap: 1rem;}\n.timeline > .item{ display:flex; align-items:center; padding:1rem; }\n.timeline > .item:nth-child(odd){ grid-column:1 / 2; justify-content:flex-end; }\n.timeline > .item:nth-child(even){ grid-column:3 / 4; justify-content:flex-start; }\n.timeline:before{ content:''; grid-column:2; width:2px; background:#bbb; align-self:stretch; }\n@media (max-width: 800px){ .timeline{ grid-template-columns:1fr; } .timeline:before{ display:none; } }\n```\n\n## Follow-up Questions\n- How would accessibility be affected if the timeline contained long content?\n- How would you adapt for dynamic data loading with CSS only?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-4",
        "questionId": "q-5710",
        "question": "You're building a client-side image processing pipeline that must resize multiple images in parallel but cap concurrency at 4; implement a function createQueue(limit) that uses closures to track active tasks and returns enqueue(task) => Promise, ensuring that a failed task rejects its Promise and doesn't crash the pool, and that once a task completes, the next queued task starts?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a FleetDashboard module: load initial statuses via REST, cache responses with per-URL TTL and lastUpdate; open a WebSocket for live updates and apply a minimal DOM diff to update only changed r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "PerTenantFormComposer subscribes to a per-tenant field-schema stream via useSyncExternalStore and uses a tenant/formId key to memoize initialValues and visibility. It applies optimistic field edits wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage CSS Grid for the layout and a CSS-only density toggle using a hidden checkbox to swap CSS variables: --cols, --gap, --cardH, and image aspect-ratio. Desktop: 4 columns, tablet: 2, mobile: 1. Implement a respons…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a closure-based pool: keep a queue and an active counter. enqueue(task) returns a Promise; whenever active < limit, shift a task, increment active, and run it. On completion, decrement active and",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nConcurrency control in the browser is critical for perf and stability. A closed, bounded queue surfaces realistic debugging and trade-offs.\n\n## Key Concepts\n\n- Closure-based concurrency state (queue, active, limit)\n- Promise queue with drain logic\n- Correct error propagation and pool resilience\n- Real-world cap (4) and dynamic scaling\n\n## Code Example\n\n```javascript\nfunction createQueue(limit) {\n  const q = [];\n  let active = 0;\n  const next = () => {\n    if (active >= limit || q.length === 0) return;\n    const { task, resolve, reject } = q.shift();\n    active++;\n    Promise.resolve()\n      .then(() => task())\n      .then(resolve)\n      .catch(reject)\n      .finally(() => {\n        active--;\n        next();\n      });\n  };\n  return {\n    enqueue: (task) => new Promise((resolve, reject) => {\n      q.push({ task, resolve, reject });\n      next();\n    })\n  };\n}\n```\n\n## Follow-up Questions\n\n- How would you handle cancellation of enqueued tasks?\n- How would you adapt for priority queues or dynamic limits?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-5",
        "questionId": "q-13429",
        "question": "Design a FleetDashboard module: load initial statuses via REST, cache responses with per-URL TTL and lastUpdate; open a WebSocket for live updates and apply a minimal DOM diff to update only changed rows; a Service Worker caches assets and the latest payload for offline viewing. Include WebSocket reconnect with backoff and a reconciliation pass on reconnect?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a FleetDashboard module: load initial statuses via REST, cache responses with per-URL TTL and lastUpdate; open a WebSocket for live updates and apply a minimal DOM diff to update only changed r",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use useReducer with a state structure that tracks field values, validation errors, and touched status. Implement memoized selectors for derived validation logic and dispatch actions that batch related",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Map-based cache keyed by URL + serialized opts and a separate in-flight map for in-progress promises. On call, return cached value if it exists and is fresh; otherwise reuse the in-flight promis",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement an offline-first code-review viewer by loading file diffs with fetch, subscribing to a WebSocket for new comments and presence, and using a DOM-diffing pass to update only changed li",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests integration of REST, WebSocket, and Service Worker in a practical frontend scenario with offline considerations and real DOM updates. It probes data freshness, reconnection strategies, and efficient rendering under network variability.\n\n## Key Concepts\n- DOM diffing: efficient patching to update only changed rows\n- REST + caching: per-URL TTL, lastUpdate, and offline fallback\n- WebSocket: robust reconnect with exponential backoff\n- Service Worker: offline caching for assets and latest payload\n- Reconciliation: merge in-flight WS updates after reconnect without data loss\n\n## Code Example\n```javascript\n// Lightweight patch helper (conceptual)\nfunction patchRows(container, changes) {\n  changes.forEach(({ id, data }) => {\n    const row = container.querySelector(`[data-id=\"${id}\"]`);\n    if (row) {\n      Object.assign(row.dataset, data);\n      row.textContent = data.status;\n    } else {\n      const el = document.createElement('div');\n      el.dataset.id = id;\n      el.textContent = data.status;\n      container.appendChild(el);\n    }\n  });\n}\n```\n\n## Follow-up Questions\n- How would you test offline-first behavior across network partitions?\n- How would you validate reconciliation guarantees between REST<Timeline> and WS updates?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-6",
        "questionId": "q-4593",
        "question": "You're working on a React application where multiple components need to access and modify the same piece of state, but you want to avoid prop drilling while maintaining better performance than Context API. How would you implement a state management solution using a custom hook with React's useRef and useCallback to create a subscription-based pattern that allows components to selectively subscribe to state changes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To solve, encapsulate a cache Map and in-flight promises inside a closure. load(id) checks the cache; if a pending promise exists, return it; otherwise call fetchFn(id), store the Promise with a TTL t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a custom hook using useRef to store state and callbacks, implementing a subscription pattern with useCallback for selective updates. This avoids Context API's cascade re-renders while eliminating prop drilling.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use useSyncExternalStore to subscribe to the tenant-scoped stream. Implement a memoized selector keyed by tenant and minute to bucket events, feeding a virtualized Bar chart. Wrap Bar with React.memo.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a CSS-only responsive media gallery: desktop two-column grid (image 60%, info 40%), mobile stacked. Each item uses a hidden checkbox as a Play/Pause toggle to control a caption animation via CS",
            "isCorrect": false
          }
        ],
        "explanation": "The solution involves creating a custom hook that manages state in a ref object and maintains a list of subscriber callbacks. The state persists across re-renders using useRef without triggering updates itself. Each component registers a callback function using useCallback, which only invokes when the specific state slice it cares about changes. This creates a subscription pattern where components re-render exclusively when their subscribed state changes, unlike Context API which causes all consumers to re-render. The implementation includes functions to update state and notify only relevant subscribers based on the specific state changes.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-7",
        "questionId": "q-168",
        "question": "Explain the CSS box model and how box-sizing affects layout calculations. What's the difference between border-box and content-box?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "- Microtasks: Promise.then, queueMicrotask, MutationObserver. - Macrotasks: setTimeout, setInterval, I/O callbacks. - Event loop per tick: - pop a macrotask from the macrotask queue and run it - drain the microtask queu…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "The CSS box model defines how elements are rendered with content, padding, border, and margin layers. With box-sizing: content-box (default), width/height apply only to content. With box-sizing: border-box, width/height…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a Redux-like pattern with Context + useReducer to hold a flags map. Implement actions: SET_FLAG for single flag updates and PATCH_FLAGS for bulk updates. Create a custom debounce hook (200ms) to batch server updates…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a tiny store: state = { theme:'light', toasts:[] }; reducer handles 'TOGGLE_THEME' and 'ADD_TOAST'. StoreProvider passes {state, dispatch}. useStoreSelector(sel) uses useMemo over sel(state)",
            "isCorrect": false
          }
        ],
        "explanation": "## Core Components\n- **Content**: Actual text/images (width/height target)\n- **Padding**: Space between content and border\n- **Border**: Edge around padding\n- **Margin**: Space outside border (collapses vertically)\n\n## Box-Sizing Impact\n```css\n/* content-box: 200px + 40px padding + 4px border = 244px total */\n.content-box { box-sizing: content-box; width: 200px; padding: 20px; border: 2px; }\n\n/* border-box: total width = 200px (content shrinks to 156px) */\n.border-box { box-sizing: border-box; width: 200px; padding: 20px; border: 2px; }\n```\n\n## Practical Applications\n- **Responsive Design**: border-box prevents overflow in flexible layouts\n- **Component Systems**: Consistent sizing across UI libraries\n- **Grid Layouts**: Predictable column widths with padding/borders\n\n## Common Gotchas\n- Margin collapsing: adjacent vertical margins combine to max of both\n- Inline elements: vertical margins/padding don't affect layout height\n- calc() function: `width: calc(50% - 10px)` for precise adjustments",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-8",
        "questionId": "q-22527",
        "question": "In a multi-tenant React app with a ThemeContext and a Redux-like streaming store, implement a PerTenantLiveChart that visualizes the active tenant's real-time metric series. Data streams arrive via useSyncExternalStore from a per-tenant WebSocket-like feed emitting {tenantId, ts, value}. Requirements: per-tenant memoized selector keyed by (tenantId, metric='rps', windowMs=60000) returning a rolling 60s bucketed series; a Canvas-based chart updated with requestAnimationFrame; cleanup subscriptions on tenant switch; handle missing data gracefully; support rapid tenant switches without memory leaks. Provide code sketches for: stream wrapper, selector, ChartCanvas, and panel integration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set up a per-tenant feed wrapper and useSyncExternalStore to feed a rolling 60s bucketed series for the active tenant. Memoize a selector keyed by (tenantId, metric='rps', windowMs=60000) returning a bucketed time serie…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement a module that initializes by REST-fetching /history, opens a WebSocket for real-time ticks with exponential backoff on reconnect, and uses a Service Worker to cache assets and REST r",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use grid: display: grid; grid-template-columns: repeat(4,1fr); gap:16px; @media (max-width:1200px){grid-template-columns: repeat(2,1fr);} @media (max-width:600px){grid-template-columns:1fr;} Tile uses",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed approach: Build a PerTenantFormDesigner with a per-tenant Redux-like store, a per-tenant event bus for live schema updates, and a FormContext for theming and i18n. Subscriptions use useSyncEx",
            "isCorrect": false
          }
        ],
        "explanation": "**Why This Is Asked**\n\nThis question tests your ability to design per-tenant streaming UI components with robust memoization, proper resource cleanup, and performant rendering under rapid tenant switches—critical skills for building SaaS dashboards.\n\n**Key Concepts**\n\n- useSyncExternalStore for subscribing to per-tenant WebSocket streams\n- Memoized selectors keyed by (tenantId, metric, windowMs) to avoid unnecessary recalculations\n- Canvas-based rendering with device pixel ratio (DPR) and requestAnimationFrame for smooth 60fps updates\n- Careful subscription management: unsubscribe from previous tenant's feed when switching\n- Graceful handling of missing data points in the rolling window\n- Memory leak prevention through proper cleanup of intervals, subscriptions, and animation frames\n\n**Code Example**\n\n```javascript\n// Per-tenant feed wrapper with subscription management\nfunction createTenantFeedManager(wsFactory) {\n  const connections = new Map(); // tenantId -> WebSocket\n  const subscribers = new Map(); // tenantId -> Set of callbacks\n\n  function connect(tenantId) {\n    if (connections.has(tenantId)) return;\n    const ws = wsFactory(tenantId);\n    ws.onmessage = (e) => {\n      const data = JSON.parse(e.data);\n      subscribers.get(tenantId)?.forEach(cb => cb(data));\n    };\n    connections.set(tenantId, ws);\n    subscribers.set(tenantId, new Set());\n  }\n\n  function subscribe(tenantId, callback) {\n    connect(tenantId);\n    subscribers.get(tenantId).add(callback);\n    return () => subscribers.get(tenantId)?.delete(callback);\n  }\n\n  function disconnect(tenantId) {\n    connections.get(tenantId)?.close();\n    connections.delete(tenantId);\n    subscribers.delete(tenantId);\n  }\n\n  return { subscribe, disconnect };\n}\n\n// Memoized selector for rolling bucketed series\nconst makeRpsSelector = (tenantId, metric = 'rps', windowMs = 60000) => {\n  const cache = new Map();\n  return (dataPoints) => {\n    const now = Date.now();\n    const bucketSize = 1000; // 1 second buckets\n    const buckets = {};\n    \n    for (let i = 0; i < windowMs / bucketSize; i++) {\n      buckets[now - i * bucketSize] = 0;\n    }\n    \n    dataPoints\n      .filter(d => d.ts > now - windowMs)\n      .forEach(d => {\n        const bucket = Math.floor(d.ts / bucketSize) * bucketSize;\n        buckets[bucket] = (buckets[bucket] || 0) + d.value;\n      });\n    \n    return Object.entries(buckets)\n      .map(([ts, value]) => ({ ts: Number(ts), value }))\n      .sort((a, b) => a.ts - b.ts);\n  };\n};\n\n// Canvas-based chart with rAF\nfunction ChartCanvas({ data, width = 600, height = 300 }) {\n  const canvasRef = useRef(null);\n  const rafRef = useRef(null);\n  const dataRef = useRef(data);\n\n  useEffect(() => { dataRef.current = data; }, [data]);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    const ctx = canvas.getContext('2d');\n    const dpr = window.devicePixelRatio || 1;\n    \n    canvas.width = width * dpr;\n    canvas.height = height * dpr;\n    ctx.scale(dpr, dpr);\n\n    const draw = () => {\n      ctx.clearRect(0, 0, width, height);\n      const points = dataRef.current;\n      if (!points.length) return;\n      \n      const maxVal = Math.max(...points.map(p => p.value), 1);\n      const xScale = width / (points.length - 1 || 1);\n      const yScale = height / maxVal;\n\n      ctx.beginPath();\n      ctx.strokeStyle = '#3b82f6';\n      ctx.lineWidth = 2;\n      points.forEach((p, i) => {\n        const x = i * xScale;\n        const y = height - p.value * yScale;\n        i === 0 ? ctx.moveTo(x, y) : ctx.lineTo(x, y);\n      });\n      ctx.stroke();\n      \n      rafRef.current = requestAnimationFrame(draw);\n    };\n    \n    draw();\n    return () => cancelAnimationFrame(rafRef.current);\n  }, [width, height]);\n\n  return <canvas ref={canvasRef} style={{ width, height }} />;\n}\n\n// Panel integration with tenant switching\nfunction LiveChartPanel({ tenantId }) {\n  const feedRef = useRef(createTenantFeedManager(createWebSocket));\n  const [data, setData] = useState([]);\n\n  useEffect(() => {\n    const feed = feedRef.current;\n    const unsubscribe = feed.subscribe(tenantId, (point) => {\n      setData(prev => [...prev.slice(-599), point]); // Keep last 60s\n    });\n    \n    return () => {\n      unsubscribe();\n      feed.disconnect(tenantId);\n    };\n  }, [tenantId]);\n\n  const selector = useMemo(\n    () => makeRpsSelector(tenantId, 'rps', 60000),\n    [tenantId]\n  );\n  const bucketedData = useMemo(() => selector(data), [selector, data]);\n\n  return <ChartCanvas data={bucketedData} />;\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-9",
        "questionId": "q-20582",
        "question": "Build a small chat module in React using a Redux-like store (useReducer + Context). The app has rooms; messages are stored per room. Implement optimistic sending: on send, append a pending message with a tempId; simulate server ack after 400ms replacing it with a confirmed message; if ack fails, rollback. Add a memoized selector keyed by roomId that returns {unread, lastMessages} and render messages with a React.memo MessageItem. Clean up timeouts on room switch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use useReducer with a history array containing past and future states, implement undo/redo actions that swap current state with history entries, and maintain a separate 'pending' state for optimistic",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Redux-like store with useReducer + Context; define actions: SEND_MESSAGE, ACK_MESSAGE, ROLLBACK_MESSAGE. On SEND_MESSAGE, optimistically append {id: 'temp-' + Date.now(), text, status: 'pending'}; simulate server…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a state management solution with optimistic updates using useReducer for state transitions, separate loading/error tracking, and rollback functionality when API calls fail.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a custom hook using useReducer for complex async state management, combining loading, error, and data states with cleanup mechanisms and AbortController for race condition prevention.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to architect a Redux-like store with optimistic UI updates and per-room scope. It probes how a candidate manages derived data (unread count, last messages) using memoized selectors, implements React.memo for efficient list rendering, and handles cleanup of async operations when switching contexts.\n\n## Key Concepts\n- useReducer + Context for a minimal Redux-like store\n- Optimistic UI patterns with rollback on failure\n- Memoized selectors keyed by roomId using useMemo\n- React.memo for performant list item rendering\n- Cleanup of timeouts and subscriptions on room changes\n\n## Code Example\n```javascript\n// State shape: { rooms: { [roomId]: { messages: [], unread: number } }, currentRoomId }\nfunction reducer(state, action) {\n  switch (action.type) {\n    case 'SEND_MESSAGE':\n      return {\n        ...state,\n        rooms: {\n          ...state.rooms,\n          [state.currentRoomId]: {\n            ...state.rooms[state.currentRoomId],\n            messages: [\n              ...state.rooms[state.currentRoomId].messages,\n              { id: action.payload.tempId, text: action.payload.text, status: 'pending' }\n            ]\n          }\n        }\n      };\n    case 'ACK_MESSAGE':\n      // Replace temp message with confirmed message\n      // ...\n    case 'ROLLBACK_MESSAGE':\n      // Remove failed message\n      // ...\n    default:\n      return state;\n  }\n}\n\n// Memoized selector\nconst useRoomMessages = (roomId) => {\n  return useMemo(() => {\n    const room = state.rooms[roomId];\n    return { \n      unread: room.unread, \n      lastMessages: room.messages.slice(-20) \n    };\n  }, [state.rooms[roomId]]);\n};\n\n// Memoized message item\nconst MessageItem = React.memo(({ message }) => (\n  <div className={message.status}>{message.text}</div>\n));\n```",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-10",
        "questionId": "q-17425",
        "question": "Design a LivePresence panel for a chat app that shows online users in the current channel. WebSocket emits {channelId, userId, status, ts} and updates a per-channel map; a small diff computes adds/removes and updates only the corresponding DOM nodes. Channels switch with cleanup. If WebSocket stalls, seed via REST /channels/{id}/presence and cache last presence in a Service Worker for offline. Include code sketches for WebSocket wrapper, diff, PresenceRow, and wiring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a two‑pane grid: on wide viewports 240px left rail and 1fr content; on mobile collapse the rail to 48px with a CSS toggle (no JS). The content grid uses grid-template-columns: repeat(auto-fill,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a SW with cache-first for static assets and a network-first path for messages, versioned cache. Use fetchWithTimeout(url, {signal}, 4000) and fallback to CacheStorage on timeout. Render WebS",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a LivePresence panel that maintains a per-channel Map of userId to status. A WebSocket wrapper handles connect/reconnect with backoff; on each message, update the map and apply a min",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a 2-column grid: .timeline{display:grid; grid-template-columns: 1fr 1fr; align-items:stretch}. Cards alternate via :nth-child(odd){grid-column:1} :nth-child(even){grid-column:2}. Year rail is a fi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to coordinate real-time data with WebSocket, implement a per-channel scope, and perform efficient DOM updates via a small diff rather than full re-renders. It also covers offline behavior via REST seed and Service Worker caching, plus cleanups on channel changes.\n\n## Key Concepts\n- WebSocket lifecycle with per-channel state\n- Lightweight DOM diff to minimize reflows\n- Channel-scoped subscriptions and cleanup\n- REST seed and Service Worker cache for offline\n- Reconnect/polling fallback strategy\n\n## Code Example\n```javascript\n// PresenceWS: tiny wrapper sketch\nclass PresenceWS {\n  constructor(url) { this.url = url; this.ws = null; this.reconnect = true; }\n  connect(onMessage) { /* connect and call onMessage({channelId, userId, status, ts}) */ }\n  close() { this.reconnect = false; this.ws?.close(); }\n}\n\n// Presence diff sketch\nfunction diffPresence(prev, next) {\n  const toAdd = [];\n  const toRemove = [];\n  const nextSet = new Set(next.map(n => n.userId));\n  for (const u of prev) if (!nextSet.has(u.userId)) toRemove.push(u);\n  for (const n of next) if (!prev.find(p => p.userId === n.userId)) toAdd.push(n);\n  return { toAdd, toRemove };\n}\n\n// PresenceRow component sketch (no framework)\nfunction renderRow(user) { const el = document.createElement('div'); el.textContent = user.userId + ' ' + user.status; el.id = 'u-' + user.userId; return el; }\n```\n\n## Follow-up Questions\n- How would you test the per-channel diff logic and offline fallback?\n- How would you handle message ordering and late arrivals for a fast-moving presence list?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-11",
        "questionId": "q-19923",
        "question": "Build a Live Alerts panel that on mount fetches initial alerts via REST /api/alerts?limit=50, renders them, then opens a WebSocket to ws://alerts.example for real-time alerts. The module must deduplicate by alertId, preserve in-order delivery, debounce DOM updates to 100ms, and expose a keyboard-accessible 'Mark all read' control. Use a Service Worker to cache assets and the last 50 alerts for offline viewing; implement exponential backoff for reconnect and handle offline gracefully?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a PerTenantServicePanel that subscribes to tenant-scoped health events via useSyncExternalStore, maintains a per-tenant ring buffer of the last 100 samples, and renders a virtualized HealthR",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Split the chart into a lazy-loaded component using React.lazy and import() so it's not part of the initial bundle. Use an IntersectionObserver to mount it only when the chart area enters the viewport. For images, implem…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "The outer layout uses CSS Grid to create a responsive structure with 4 columns on desktop, 2 on tablet, and 1 on mobile. Each panel is structured as a grid with header, body, and footer sections. Inside the body, contai…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Load initial alerts via REST (limit 50), render to DOM, then connect a WebSocket for live alerts. Deduplicate by alertId and preserve order; debounce DOM updates to 100ms; provide a keyboard-accessibl",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests combined experience with REST, WebSocket, and Service Worker flows in a beginner-friendly context, emphasizing data integrity and offline behavior.\n\n## Key Concepts\n- Fetch error handling and retry basics\n- WebSocket lifecycle with backoff\n- Debounce and batching DOM updates\n- Deduplication by id and preserving order\n- Service Worker caching for assets and feed data\n\n## Code Example\n```javascript\n// skeleton API calls and WS hookup\n```\n\n## Follow-up Questions\n- How would you test offline fallback without a real server?\n- What changes if alerts update in-place vs. new items only?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-12",
        "questionId": "q-23142",
        "question": "Context: Frontend/performance beginner. Scenario: A Nvidia product catalog web page loads a large JavaScript bundle and shows suboptimal Lighthouse scores (LCP). Describe a practical plan to improve by: 1) splitting heavy charting code with dynamic import triggered when the charts tab is opened or becomes visible, 2) lazy-loading non-critical images (logos) with a lightweight wrapper, 3) establishing a minimal Lighthouse budget and a Lighthouse CI workflow to verify gains. Include concrete steps and simple code sketches?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Map per user to hold { data, expiry, inFlight }. get(userId) returns existing inFlight or starts fetchFn(userId) and caches the Promise; on success store data and set expiry. refresh(userId) for",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Trigger dynamic import of heavy charting code only when the charts tab is activated; keep the initial bundle lean (around 80–100 KB gzipped). Implement a tiny LazyImage wrapper using IntersectionObser",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Build a CSS-only, container-query-driven dashboard grid that adapts from 4 columns to 3 to 1 based on the grid container width, using grid-auto-flow: dense and per-card data-span to emulate masonry (g",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a LiveNotes widget that fetches /notes on load, connects to a WebSocket for real-time updates, and renders changes with efficient diff-based DOM updates. Use a sequenceId for in-order delivery and deduplicate…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests translating Lighthouse findings into concrete, measurable steps that balance UX and bundle size, plus CI validation.\n\n## Key Concepts\n- Dynamic imports and code splitting\n- Lazy loading of assets\n- Lighthouse budgets and Lighthouse CI basics\n\n## Code Example\n```javascript\n// Plan sketch: dynamic import on tab visibility\ndocument.querySelector('#chartsTab').addEventListener('focus', async () => {\n  const { initCharts } = await import('./charts/ChartsModule.js');\n  initCharts();\n});\n```\n\n```javascript\n// Lazy image wrapper (concept)\nfunction LazyImg({src, alt}) {\n  const ref = useRef(null);\n  useEffect(()=>{\n    const io = new IntersectionObserver(entries => {\n      if (entries[0].isIntersecting) {\n        ref.current.src = src;\n        io.disconnect();\n      }\n    });\n    io.observe(ref.current);\n  },[]);\n  return <img ref={ref} alt={alt} data-src={src} />;\n}\n```\n\n## Follow-up Questions\n- How would you validate improvements with Lighthouse CI?\n- What edge cases might cause regressions after splitting and lazy-loading?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-13",
        "questionId": "q-17022",
        "question": "Implement a memoized async wrapper: createMemoizedAsync(fn, ttl) that returns memoized(id) which caches the promise for each id for ttl ms, deduplicates concurrent calls, and evicts entries after ttl. Ensure errors clear the cache and that a new call after TTL triggers a fresh fetch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a PluggableModuleHost with a ModuleRegistryContext. Modules register {moduleId, loader}. The host lazy-loads each module via React.lazy and Suspense, creating per-tenant store slices exposed",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Split the dashboard into widget-group bundles served via viewport-based code splitting; lazy-load off-screen widgets with React.lazy and Suspense, triggered by IntersectionObserver; apply rel preload/",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a TenantWebSocket wrapper that subscribes to patches for a given tenantId; a patchApplier(target, patch) applies {op, path, value} to the DOM using data-path attributes with a batched update",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a memoized async wrapper that caches per-id promises for a TTL, deduplicates concurrent calls, and evicts expired entries. If a promise rejects, remove it from the cache. After TTL, a fresh cal",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of closures, promise sharing, and TTL eviction in a small utility.\n\n## Key Concepts\n- Closures and per-id state\n- Promise deduplication for in-flight calls\n- TTL-based eviction and error handling\n\n## Code Example\n```javascript\nfunction createMemoizedAsync(fn, ttl) {\n  const cache = new Map();\n  return async (id) => {\n    const now = Date.now();\n    const entry = cache.get(id);\n    if (entry && now - entry.t < ttl) return entry.p;\n    const p = fn(id);\n    cache.set(id, { t: now, p });\n    setTimeout(() => {\n      const e = cache.get(id);\n      if (e && e.p === p) cache.delete(id);\n    }, ttl);\n    try {\n      return await p;\n    } catch (e) {\n      cache.delete(id);\n      throw e;\n    }\n  };\n}\n```\n\n## Follow-up Questions\n- How would you test TTL eviction and in-flight deduplication?\n- How would you adapt for cancelable promises or revocation of in-flight calls?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-14",
        "questionId": "q-22926",
        "question": "How would you implement a small JS module that exports makeCachedFetcher(fetchFn) and returns load(id) that (a) caches results in a closure Map, (b) shares a single in-flight Promise for repeated id requests, and (c) invalidates cache after 2 minutes? Include a short usage example with a fake fetchFn(id) that resolves in 100ms and returns {id, value}?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a grid container: display: grid; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); gap: 16px. Each card: position: relative; overflow: hidden; transition: transform .15s ease, box-shad",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Next.js dynamic imports to lazy-load heavy routes (chart dashboards) while SSR remains for critical path. Enforce a 700–800KB initial bundle with explicit split points for vendors and charts. Move",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use makeCachedFetcher to close over a Map cache. On load(id) check cache[id]; if valid, return value; if an in-flight promise exists, return it; otherwise start fetchFn(id), store the promise immediat",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create a hybrid state management system that uses a custom hook to intelligently route state between useState for local state and a global store when components are composed together, using context pr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of closures and promises in a realistic data loader, including deduplicating in-flight requests and time-based invalidation.\n\n## Key Concepts\n- Closures and encapsulation\n- Promise deduplication for identical requests\n- Mutable in-memory cache with TTL\n- Edge cases: error handling, cache busting\n\n## Code Example\n```javascript\nfunction makeCachedFetcher(fetchFn) {\n  const TTL = 2 * 60 * 1000;\n  const cache = new Map();\n  return async function load(id) {\n    const entry = cache.get(id);\n    const now = Date.now();\n    if (entry?.value !== undefined && (now - entry.time) < TTL) {\n      return entry.value;\n    }\n    if (entry?.promise) {\n      return entry.promise;\n    }\n    const p = fetchFn(id).then(res => {\n      cache.set(id, { value: res, time: Date.now() });\n      return res;\n    }).catch(err => {\n      cache.delete(id);\n      throw err;\n    });\n    cache.set(id, { promise: p });\n    return p;\n  };\n}\n\n// Usage\nconst fakeFetch = async (id) => ({ id, value: 'val-'+id });\nconst loadUser = makeCachedFetcher(fakeFetch);\nloadUser(1).then(console.log); // { id: 1, value: 'val-1' }\n```\n\n## Follow-up Questions\n- How would you expose a manual invalidate(id) API and a global clearCache() for testing? \n- How would you adapt this pattern to avoid memory leaks in long-lived apps?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-15",
        "questionId": "q-23428",
        "question": "Advanced frontend/performance interview: You're optimizing a product-detail page (hero gallery + reviews widget). The initial bundle is heavy; Lighthouse mobile target is 92+. Describe a concrete plan using **bundle splitting** and **lazy-loading** to hit the target: (1) how you split bundles and lazy-load, (2) how you skeleton-loads and hydrate, (3) prefetch/dns-prefetch, preconnect strategies, (4) any tooling or metrics. Include a minimal code sketch for a dynamic import wrapper and how to configure the bundler to generate separate chunks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Split the page into a core shell and lazy-loaded widgets. Load the hero gallery in the main bundle, and import('./Reviews') for the reviews widget behind a loading threshold (viewport enter). Use Susp",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Split the analytics chart into a separate chunk using dynamic import and gate its loading with an IntersectionObserver so the HeavyChart component loads only when visible. Implement React.lazy with Suspense for the char…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Fetch initial comments for a post using the fetch API, then establish a WebSocket connection to receive new comments in real-time. Register a Service Worker to cache assets and the most recent comments for offline acces…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Set a per-route performance budget (max 900KB JS, 70KB CSS) using bundler reports; implement route-based code-splitting with dynamic import() and a LazyBoundary wrapper; extract critical CSS to inline",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes ability to translate Lighthouse targets into concrete bundling and lazy-loading strategies, and to justify trade-offs.\n\n## Key Concepts\n- Lighthouse metrics (LCP, TTI, CLS)\n- Bundle splitting and dynamic imports\n- Lazy-loading triggers (IntersectionObserver)\n- Skeletons and progressive hydration\n- Performance budgets and tooling (webpack, Vite)\n\n## Code Example\n```javascript\n// dynamic import wrapper example\nexport const loadReviews = () => import('./Reviews');\n```\n\n## Follow-up Questions\n- How would you test impact across devices and A/B test the lazy-loading threshold?\n- How would you handle SEO implications for lazy-loaded content?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-16",
        "questionId": "q-11837",
        "question": "Implement a lightweight ES6 DataLoader module for user profiles. Expose a function loadUser(id) that returns a Promise. Requirements: - if multiple calls for the same id occur while the first fetch is in-flight, deduplicate to a single network request and return the same Promise to all callers; - cap total in-flight requests at 3; additional calls should queue and start as slots free; - cache successful results for 5 minutes; within that window, calls for the same id should resolve immediately with cached data; - use closures to hide internal state, and leverage Map, async/await. Provide code sketch and a short usage example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "useState is ideal for managing simple, independent state variables where the next state depends only on the current state and a simple value, while useReducer is designed for complex state logic involving multiple sub-v…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A closure-scoped loader maintains an inFlight map, a time-based cache, and a queue. loadUser(id) checks cache, reuses in-flight promises for the same id, or enqueues a new request. A simple throttle e",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Create a custom hook that consolidates form state and validation using useReducer for complex state logic, memoize validation functions with useCallback, and optimize re-renders by returning stable ob",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use useReducer or useState with pending/success/error states, maintain previous state for rollback, and implement AbortController/version tracking to handle race conditions.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of closures, promise deduping, in-flight concurrency control, and simple caching in a realistic data-loading scenario.\n\n## Key Concepts\n- Closure-scoped internal state (inFlight, cache, queue)\n- Promise coalescing for same id\n- Concurrency limit with a slot-based throttle\n- Time-based cache with TTL\n\n## Code Example\n```javascript\nfunction createUserLoader() {\n  const inFlight = new Map(); // id -> Promise\n  const cache = new Map(); // id -> { data, expiry }\n  const queue = [];\n  const MAX_CONCURRENCY = 3;\n  let active = 0;\n\n  const fetchUser = async (id) => {\n    const res = await fetch(`/api/user/${id}`);\n    if (!res.ok) throw new Error('Network error');\n    return res.json();\n  };\n\n  const tryStart = () => {\n    while (active < MAX_CONCURRENCY && queue.length) {\n      const { id, resolve, reject } = queue.shift();\n      active++;\n      const p = fetchUser(id)\n        .then((data) => {\n          cache.set(id, { data, expiry: Date.now() + 5 * 60 * 1000 });\n          inFlight.delete(id);\n          resolve(data);\n        })\n        .catch((err) => reject(err))\n        .finally(() => { active--; tryStart(); });\n      inFlight.set(id, p);\n    }\n  };\n\n  const loadUser = (id) => {\n    const cached = cache.get(id);\n    if (cached && cached.expiry > Date.now()) return Promise.resolve(cached.data);\n    if (inFlight.has(id)) return inFlight.get(id);\n    return new Promise((resolve, reject) => {\n      queue.push({ id, resolve, reject });\n      tryStart();\n    });\n  };\n\n  return { loadUser };\n}\n\n// Usage\n// const loader = createUserLoader();\n// loader.loadUser('123').then(console.log);\n```",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-17",
        "questionId": "q-5473",
        "question": "**Question**: Build a 3-field tag input panel where each input fetches suggestions from `/api/suggest?q=TERM` as the user types. Debounce per field, cancel previous in-flight requests on new input, and ensure only the latest response updates the UI. Enforce a global limit of 4 concurrent fetches. Use closures to encapsulate per-field state and Promises to handle results?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage per-field closures to store a debounce timer and an AbortController; on input, reset the timer and, when it fires, start a fetch with its own controller. Track a global in-flight counter to cap concurrent reque…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: 1) Route-split dashboard with dynamic import and a small skeleton. 2) Use IntersectionObserver to toggle loading of HeavyChart via a lazy import only when visible. 3) Lazy-load images and precon",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a PluginHost component that manages a registry of plugin modules. Load widgets dynamically using React.lazy and Suspense for code-splitting. Isolate each widget with its own per-plugin Context to prevent state co…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Optimistic updates require maintaining a temporary state that can be reverted on failure. The pattern involves: (1) storing the previous state before applying the optimistic change, (2) immediately updating the UI to re…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests real-world patterns: per-field state via closures, per-field debouncing, cancellation of outdated requests with AbortController, and a global concurrency limiter to prevent overload, plus mapping responses to correct field.\n\n## Key Concepts\n\n- Closures for per-field state\n- Debounce patterns\n- AbortController cancellation\n- Concurrency limit with a simple semaphore\n- Mapping async results to UI per-field\n\n## Code Example\n\n```javascript\n// Minimal concurrency-limited debounced fetch per field using closures\nfunction createFieldController(fetchUrl, maxConcurrent = 4) {\n  let debounceTimer;\n  let abortController;\n  \n  return {\n    onInput: async (term) => {\n      // Cancel previous request\n      if (abortController) {\n        abortController.abort();\n      }\n      \n      // Clear existing debounce\n      clearTimeout(debounceTimer);\n      \n      // Set new debounce\n      debounceTimer = setTimeout(async () => {\n        // Check global concurrency limit\n        if (globalInFlight >= maxConcurrent) {\n          await waitForSlot();\n        }\n        \n        globalInFlight++;\n        abortController = new AbortController();\n        \n        try {\n          const response = await fetch(`${fetchUrl}?q=${term}`, {\n            signal: abortController.signal\n          });\n          \n          if (!abortController.signal.aborted) {\n            const suggestions = await response.json();\n            updateFieldUI(suggestions);\n          }\n        } catch (error) {\n          if (error.name !== 'AbortError') {\n            console.error('Fetch error:', error);\n          }\n        } finally {\n          globalInFlight--;\n        }\n      }, 300);\n    }\n  };\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-18",
        "questionId": "q-215",
        "question": "How would you implement a custom useDebounce hook that works with React's concurrent features and prevents stale closures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement createStreamingFetcher(url) using fetch with an AbortController. Get resp.body.getReader() and stream NDJSON lines. Expose stream as an async iterable that yields parsed JSON objects per line. Maintain an inte…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a custom hook using useRef to store state and callbacks, implementing a subscription pattern with useCallback for selective updates. This avoids Context API's cascade re-renders while eliminating prop drilling.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use useRef to track the latest callback value, implement proper cleanup in useEffect, and leverage useCallback to maintain a stable reference while ensuring compatibility with React's concurrent rendering model.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Context for AppConfig and a Redux-like store via useReducer with actions SET_QUERY, SET_RESULTS. DebouncedSearchInput updates query after 300ms. A memoized selector derives groups by first lette",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nA custom useDebounce hook defers function execution until a specified delay period has elapsed, making it essential for optimizing search inputs, API calls, and other performance-critical operations in React applications.\n\n## Implementation Details\n- Utilize useRef to maintain the latest callback reference, preventing stale closures across renders\n- Implement comprehensive cleanup logic in useEffect to cancel pending debounced invocations\n- Return a useCallback wrapper with stable dependencies for optimal concurrent rendering performance\n- Handle component unmounting scenarios to prevent memory leaks and unexpected behavior\n- Ensure proper cleanup of timers to maintain application stability\n\n## Code Example\n```javascript\nimport { useCallback, useRef, useEffect } from 'react';\n\nexport function useDebounce(callback, delay) {\n  const callbackRef = useRef(callback);\n  const timeoutRef = useRef(null);\n\n  useEffect(() => {\n    callbackRef.current = callback;\n  }, [callback]);\n\n  useEffect(() => {\n    return () => {\n      if (timeoutRef.current) {\n        clearTimeout(timeoutRef.current);\n      }\n    };\n  }, []);\n\n  return useCallback((...args) => {\n    if (timeoutRef.current) {\n      clearTimeout(timeoutRef.current);\n    }\n    \n    timeoutRef.current = setTimeout(() => {\n      callbackRef.current(...args);\n    }, delay);\n  }, [delay]);\n}\n```\n\n## Key Benefits\n- Prevents stale closures through proper ref management\n- Ensures cleanup on component unmount\n- Maintains stable reference for concurrent rendering\n- Handles edge cases and prevents memory leaks",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-19",
        "questionId": "q-665",
        "question": "Design and implement a minimal real-time dashboard that loads initial data via fetch('/api/data?limit=200'), then opens a WebSocket to wss://host/ws for live updates, and uses a Service Worker to cache the app shell and latest API responses for offline use. Describe how you batch DOM updates to minimize reflows, ensure reconnection logic, and implement a cache-first strategy with network fallback. Provide core code snippets and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-route splits with React.lazy and route guards, using Suspense fallbacks that render lightweight skeletons to keep focus. Set per-route budgets in Lighthouse CI (e.g., /reports: 1.2MB, 2.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use justify-content: space-between on the container, and set the middle item to margin: 0 auto or position it absolutely centered.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Load 200 items via fetch('/api/data?limit=200'), then open a WebSocket to wss://host/ws for live updates. Batch DOM writes using a diff queue with requestAnimationFrame to minimize reflows. Service Wo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement createLoader(baseUrl) that uses a closure-based cache and an inFlight map. load(path) fetches baseUrl+path and returns res.json(). cachedLoad(path) serves from cache when fresh, otherwise st",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests real-world integration of fetch, WebSocket, and Service Worker for offline resilience and efficient DOM updates.\n\n## Key Concepts\n- Real-time data with **WebSocket**\n- Offline support with **Service Worker**\n- DOM update batching and diffing strategy\n- Cache-first strategy with network fallback\n- Reconnection/backoff handling\n\n## Code Example\n```javascript\n// fetch initial\nfetch('/api/data?limit=200')\n  .then(r => r.json())\n  .then(data => render(data));\n\n// websocket\nconst ws = new WebSocket('wss://host/ws');\nws.addEventListener('message', e => {\n  const update = JSON.parse(e.data);\n  enqueueUpdate(update);\n  requestAnimationFrame(flushDOM);\n});\n\n// service worker registration\nif ('serviceWorker' in navigator) {\n  navigator.serviceWorker.register('/sw.js');\n}\n```\n\n## Follow-up Questions\n- How would you test the batching strategy for high-frequency updates?\n- How would you ensure idempotent updates and avoid duplicates?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-20",
        "questionId": "q-11104",
        "question": "You're building a React application that needs to manage both local component state and global shared state, and you want to implement a solution that allows components to seamlessly transition between local and global state without changing their internal logic. How would you design a state management system using a combination of useState and a custom global store that can detect whether state should be local or global based on component composition, and what patterns would you use to make this transition transparent?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a PluggableModuleHost with a ModuleRegistryContext. Modules register {moduleId, loader}. The host lazy-loads each module via React.lazy and Suspense, creating per-tenant store slices exposed",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a hybrid state management system that uses a custom hook to intelligently route state between useState for local state and a global store when components are composed together, using context pr",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Context for simple, low-frequency global state (themes, auth) that doesn't change often, and state management libraries when you need frequent updates, complex logic, or performance optimizations",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a module that initializes by REST-fetching /history, opens a WebSocket for real-time ticks with exponential backoff on reconnect, and uses a Service Worker to cache assets and REST r",
            "isCorrect": false
          }
        ],
        "explanation": "The solution involves creating a custom hook, let's call it `useSmartState`, that automatically detects whether a component should use local or global state. This hook would first check if the component is wrapped in a specific context provider that indicates shared state usage. If the provider is present, it would connect to a global store (possibly using Zustand, Jotai, or a custom implementation with useReducer and subscriptions). If no provider is found, it would fall back to regular useState for local component state. The global store would implement a subscription pattern where components only re-render when the specific state slice they depend on changes, using selectors similar to Redux's useSelector. To make the transition seamless, you'd implement a context provider that can be conditionally applied at different levels of the component tree, allowing the same component to work in both local and global scenarios without code changes. The provider would supply a dispatch function and state getter to the custom hook, while the hook would maintain the same API surface regardless of whether it's using local or global state internally.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-21",
        "questionId": "q-363",
        "question": "You have a navigation bar with 3 items that should be evenly spaced. The middle item needs to be centered while the first and last items stick to the edges. How would you implement this using CSS Flexbox?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Service Workers intercept network requests by registering event listeners for 'fetch' events, allowing them to control request handling through the Cache API and implement various caching strategies.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Explain how you would implement an OfflineResilientDocViewer that loads a document via fetch, caches with a Service Worker using stale-while-revalidate, and subscribes to live edits via a WebSocket. I",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use justify-content: space-between on the container, and set the middle item to margin: 0 auto or position it absolutely centered.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Explain progressive hydration: move heavy modules to a non-critical bundle, render a lightweight shell first, then lazy-load widgets after the first interaction. Use a loadAfterInteract utility to imp",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests fundamental Flexbox understanding and practical layout skills that are essential for frontend development at HRT.\n\n## Expected Answer\nCandidate should explain justify-content: space-between distributes outer items to edges, then discuss centering the middle item using margin: 0 auto or flex: 1 with text-align: center.\n\n## Code Example\n```css\n.nav {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n}\n.nav-item:first-child,\n.nav-item:last-child {\n  flex: 0 0 auto;\n}\n.nav-item:nth-child(2) {\n  margin: 0 auto;\n}\n```\n\n## Follow-up Questions\n- How would you handle this with CSS Grid?\n- What if the navigation items have different widths?\n- How would you make this responsive for mobile devices?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-22",
        "questionId": "q-16094",
        "question": "Design a PerTenantWidgetRouter in a React app with a Redux-like streaming store of per-tenant feature flags; the active tenant's widgets render only those enabled. Widgets are lazy-loaded via React.lazy and wrapped with React.memo. Use useSyncExternalStore to subscribe to the stream, a memoized selector keyed by tenantId and widgetId that returns an ordered list of enabled widgets, and reset subscriptions on tenant switch. Include code sketches for store wrapper, selector, lazy loader, and router integration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a PerTenantWidgetRouter that subscribes to a streaming per-tenant flags store with useSyncExternalStore. A memoized selector keyed by [tenantId, widgetId] yields an ordered list of e",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create a custom hook using useReducer with React.memo and useCallback to build a lightweight state management solution that minimizes re-renders by implementing selector patterns and memoized state sl",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Plan:** Implement route-based code-splitting with dynamic `import()` and named chunks, lazy-load heavy components (e.g., charting libraries) with `React.lazy`/`Suspense`, and defer non-critical CSS. Enforce a build-ti…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "REST wrapper fetchCatalog() caches responses in a Service Worker using stale-while-revalidate; WebSocket client streams {productId, price, stock, ts} and uses exponential backoff on disconnect; a DOM",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests advanced React patterns: useSyncExternalStore for per-tenant streams, memoization to avoid re-renders, code-splitting with React.lazy, and per-tenant isolation with dynamic rendering.\n\n## Key Concepts\n\n- useSyncExternalStore for streaming per-tenant flags\n- Memoized selectors keyed by [tenantId, widgetId]\n- React.memo wrappers for widgets\n- React.lazy for lazy loading widgets\n- Cleanup on tenant switch\n\n## Code Example\n\n```javascript\n// store wrapper and selector skeleton\nexport function createTenantFlagsStore(url) {\n  // connect to streaming source and expose subscribe/getSnapshot\n}\n\nexport const makeWidgetSelector = (tenantId) => (state) => {\n  // return ordered list of enabled widgetIds for this tenant\n}\n\n// router\nexport function PerTenantWidgetRouter({ tenantId }) {\n  // useSyncExternalStore(…) to subscribe; map to lazy widgets; render with memoized items\n}\n```\n\n## Follow-up Questions\n\n- How would you test memoization guarantees for widgetList changes?\n- How would you handle a tenant that briefly has no enabled widgets?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-23",
        "questionId": "q-25520",
        "question": "In a React app with ThemeContext visuals and a Redux-like store fed by useSyncExternalStore, design a BranchActivityPanel that shows real-time commit activity per branch for the currently selected repository. Requirements: memoized selector keyed by (repoId, branchName) returning 24h bucket counts; render rows with a React.memo BarRow; virtualization via react-window; support bursts with useTransition; ensure cleanup on repository switch. Provide code sketches for: stream wrapper, selector, BarRow, and panel wiring; include memory cleanup strategies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use CSS containment for layout/paint isolation, will-change sparingly for compositor hints, transform3d for GPU acceleration, virtualized rendering to handle large lists efficiently, and requestAnimationFrame batching t…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To implement BranchActivityPanel: subscribe to a per-repo activity stream with useSyncExternalStore; memoize a selector keyed by [repoId, branchName] that aggregates 24h buckets; render with a React.m",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Optimistic updates require maintaining a temporary state that can be reverted on failure. The pattern involves: (1) storing the previous state before applying the optimistic change, (2) immediately updating the UI to re…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a responsive hero grid using CSS Grid with a center featured card that spans two columns and two rows on desktop; scale down to 3 columns on tablets and 2 on mobile. Include hover lift, focus-v",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time data streaming integration in a per-repo UI, memoization, and rendering performance under bursts.\n\n## Key Concepts\n- useSyncExternalStore\n- memoized selectors with composite keys\n- React.memo and virtualization\n- useTransition for bursty updates\n- cleanup on repo switches\n\n## Code Example\n```javascript\n// Stream wrapper: useSyncExternalStore subscription to per-repo feed\n```\n\n## Follow-up Questions\n- How would you test memory usage under 10k branches?\n- How would you simulate bursts in tests and verify useTransition behavior?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-24",
        "questionId": "q-25037",
        "question": "Design a dynamic dashboard engine in React (advanced) that subscribes to a per-tenant feature-flag stream via a Redux-like store and uses a server-provided manifest to render widgets (ChartWidget, TableWidget) in a per-tenant dashboard. Load widgets lazily with React.lazy, render with a per-tenant LayoutRenderer that maps manifest widgets to registered components, and use useSyncExternalStore to subscribe. Implement optimistic layout edits (reorder / add widget) with rollback if the server rejects; ensure tenant switch cleanup and correct memoization of a selector keyed by (tenantId, widgetId). Provide code sketches for: manifest wrapper, dynamic registry, layout renderer, and optimistic update flow?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I'd implement a resilient pipeline: first fetch initial items via REST, then subscribe to live alerts over WebSocket. Events are stored in an in-memory Map with a persisted queue in IndexedDB. A custom DOM-diff engine b…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant manifest-driven dashboard engine with dynamic lazy widget registry, layout renderer, and real-time feature flag subscription. Uses useSyncExternalStore for external store subscription, memoized selectors keye…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Optimistic updates require maintaining a temporary state that can be reverted on failure. The pattern involves: (1) storing the previous state before applying the optimistic change, (2) immediately updating the UI to re…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a closure to store an inFlight map and a simple cache. get(key) returns cached value or a shared promise for that key. When a new key arrives, push it into a batch; if no timer, set one for batchD",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your ability to build a manifest-driven, tenant-scoped UI system with real-time data streams, dynamic component loading, and robust optimistic updates.\n\n## Key Concepts\n- useSyncExternalStore for per-tenant external data subscription\n- React.lazy with dynamic widget registry for code splitting\n- Manifest-driven LayoutRenderer mapping widgets to registered components\n- Memoized selectors keyed by (tenantId, widgetId) for performance optimization\n- Optimistic UI updates with automatic rollback on server rejection\n- Proper cleanup and memory management on tenant switch\n\n## Code Example\n```javascript\n// Widget Registry with lazy loading\nconst widgetRegistry = {\n  ChartWidget: React.lazy(() => import('./widgets/ChartWidget')),\n  TableWidget: React.lazy(() => import('./widgets/TableWidget')),\n};\n\n// useSyncExternalStore hook for manifest subscription\nconst useManifest = (tenantId) => useSyncExternalStore(\n  (onStoreChange) => manifestStore.subscribe(tenantId, onStoreChange),\n  () => manifestStore.getSnapshot(tenantId),\n  () => manifestStore.getServerSnapshot(tenantId)\n);\n\n// Memoized selector for layout props\nconst useLayoutSelector = (tenantId, widgetId) => \n  useMemo(() => createSelector(tenantId, widgetId), [tenantId, widgetId]);\n```\n\n## Follow-up Questions\n- How would you handle widget configuration persistence across tenant switches?\n- What strategies would you use to debounce rapid layout changes before syncing to the server?\n- How would you implement error boundaries for failed widget lazy loads?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-21T10:14:01.962Z",
    "version": 1
  }
]