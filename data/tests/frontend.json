[
  {
    "id": "test-frontend",
    "channelId": "frontend",
    "channelName": "Frontend",
    "title": "Frontend Knowledge Test",
    "description": "Test your understanding of Frontend with 25 questions.",
    "questions": [
      {
        "id": "frontend-q-0",
        "questionId": "q-17886",
        "question": "Implement a tiny data loader factory in plain JavaScript: function createDataLoader(fetchFn, ttlMs) returns { load(url): Promise<any> }. It should cache results by URL for ttlMs using a closure-based map, and coalesce concurrent loads for the same URL into a single Promise. Include usage examples and edge cases (expired cache, network errors, invalid URLs). How would you implement it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan end-to-end: profile with Lighthouse CI, identify heavy modules via bundle analyzer, lazy-load the analytics SDK on-demand, split routes and components, prefetch critical routes, and enforce bundler budgets with per…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a closure to hold a cache Map (url -> {data, expiry, promise}). The load(url) method checks if a valid (non-expired) entry exists and returns cached data via Promise.resolve. If a fetch is already in-flight for the…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a WebSocket sequence with idempotent replay, track a local pendingEvents queue and a server-placed sequence number; on reconnect, flush pending events, dedupe by id, and apply a CRDT-like merge fo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Split the dashboard into widget-group bundles served via viewport-based code splitting; lazy-load off-screen widgets with React.lazy and Suspense, triggered by IntersectionObserver; apply rel preload/",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests closures, promises, and lightweight caching without external frameworks. It demonstrates understanding of in-flight request deduplication and cache expiration.\n\n## Key Concepts\n- **Closures** for private state encapsulation\n- **Promise deduplication** to prevent duplicate network requests for the same URL\n- **TTL-based cache invalidation** with timestamp checking\n- **Error handling** for network failures and edge cases\n\n## Key Considerations\n- Store both data and the in-flight promise to handle concurrent requests\n- Use URL as the cache key for O(1) lookups\n- Handle invalid URLs by rejecting early\n- Clear cache entries on error to allow retry",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-1",
        "questionId": "q-9039",
        "question": "You're optimizing a Plaid-like fintech dashboard built with React and Next.js. The initial bundle is ~1.8MB and Lighthouse scores sit in the 60s due to heavy multichart components and a data grid. Propose a concrete, implementable plan focusing on: 1) per-route lazy-loading with code-splitting, 2) a Lighthouse-friendly bundle budget and pre-connect hints, 3) non-blocking data fetching and caching strategies, 4) a CI workflow to verify improvements. What exact steps and code would you add?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a combination of useRef to preserve form state across unmounts, useState for current values, and useEffect to sync with session storage, implementing a custom hook that encapsulates this logic wit",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a custom hook using useReducer for complex async state management, combining loading, error, and data states with cleanup mechanisms and AbortController for race condition prevention.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a CSS-only dashboard grid: grid-auto-flow: dense with 5 columns desktop, 3 tablet, 1 mobile; each card has a 16:9 preview and a caption that slides up on hover/focus. Use container-type: inline",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Next.js dynamic imports to lazy-load heavy routes (chart dashboards) while SSR remains for critical path. Enforce a 700–800KB initial bundle with explicit split points for vendors and charts. Move",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design route-level lazy loading, manage bundle budgets, implement non-blocking data fetch, and integrate CI-based verification, all aligned with real-world fintech performance needs.\n\n## Key Concepts\n- Route-level code-splitting and dynamic imports\n- SSR vs CSR trade-offs for dashboards\n- Lighthouse targets: LCP, TTI, total blocking time\n- Non-blocking data fetch with caching (React Query/SWR)\n\n## Code Example\n```javascript\n// Next.js dynamic import for charts\nimport dynamic from 'next/dynamic';\nconst HeavyChart = dynamic(() => import('./charts/HeavyChart'), { ssr: false });\n```\n\n```javascript\n// Simple data fetch with caching using React Query\nimport { useQuery } from 'react-query';\nfunction fetchData() { return fetch('/api/data').then(r => r.json())}\nfunction Data() { const { data } = useQuery('data', fetchData, { staleTime: 1000 * 60 * 5 }); return <Chart data={data} /> }\n```\n\n## Follow-up Questions\n- How would you measure impact on CI and which metrics would you track? \n- What are potential downsides of disabling SSR for certain components?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-2",
        "questionId": "q-11509",
        "question": "You're working on a React application that manages form data across multiple components, where users can navigate between different sections while preserving their input. However, you notice that the state becomes inconsistent when users use the browser's back/forward buttons. How would you implement a state management solution that syncs React state with the browser's History API to maintain consistency across navigation, and what techniques would you use to handle edge cases like component unmounting or browser refresh?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a closure-scoped Map cache: url -> {data, expiry}. fetchWithCache checks expiry; if valid, return cached data. If an inFlight[url] exists, await it; else start fetch with exponential backoff retry",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use useReducer or useState with pending/success/error states, maintain previous state for rollback, and implement AbortController/version tracking to handle race conditions.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant manifest-driven dashboard engine with dynamic lazy widget registry, layout renderer, and real-time feature flag subscription. Uses useSyncExternalStore for external store subscription, memoized selectors keye…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a custom React hook that combines useState with the browser's History API, using useEffect to manage popstate events and synchronizing state through history entries while handling persistence with sessionStora…",
            "isCorrect": true
          }
        ],
        "explanation": "The solution involves creating a custom hook that bridges React state with browser history management. Start by implementing a hook that uses useState for the form data and useEffect to listen for popstate events from the History API. When state changes occur, update both the React state and push a new history entry with the serialized state. For navigation, use pushState/replaceState to update the URL without triggering a page reload, and restore state from the history object when users navigate back/forward.\n\nTo handle edge cases, implement proper cleanup in useEffect's return function to remove event listeners when components unmount. Use sessionStorage to persist form data across browser refreshes, and restore this persisted state when the component mounts. Additionally, implement error handling for malformed state data and provide fallback mechanisms for scenarios where history state is unavailable or corrupted.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-3",
        "questionId": "q-239",
        "question": "How would you implement a React useMemo hook to optimize a recursive Fibonacci function with memoization, and what are the key trade-offs between top-down memoization vs bottom-up tabulation in this context?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Split the chart into a lazy-loaded component using React.lazy and import() so it's not part of the initial bundle. Use an IntersectionObserver to mount it only when the chart area enters the viewport. For images, implem…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Maintain an in-memory Map<ProductId, {stock, price}> and a processedDeltaIds Set. WebSocket messages carry {deltaId, productId, stock, price, ts}. Apply deltas in arrival order, ignore duplicates or t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use useMemo to cache expensive Fibonacci calculations, memoizing results to prevent O(2^n) recursion. Trade-offs: memoization uses recursion + cache (O(n) space), tabulation uses iteration (O(1) space",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a closure to hold a cache Map (url -> {data, expiry, promise}). The load(url) method checks if a valid (non-expired) entry exists and returns cached data via Promise.resolve. If a fetch is already in-flight for the…",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nDynamic programming optimizes recursive problems by caching subproblem results. In React, useMemo prevents expensive recalculations during re-renders.\n\n## Implementation Details\n### Top-Down Memoization (Recursive)\n```javascript\nconst fibonacci = useMemo(() => {\n  const memo = {};\n  const fib = (n) => {\n    if (n in memo) return memo[n];\n    if (n <= 1) return n;\n    memo[n] = fib(n - 1) + fib(n - 2);\n    return memo[n];\n  };\n  return fib;\n}, []);\n```\n\n### Bottom-Up Tabulation (Iterative)\n```javascript\nconst fibonacci = useMemo(() => {\n  return (n) => {\n    if (n <= 1) return n;\n    let prev = 0, curr = 1;\n    for (let i = 2; i <= n; i++) {\n      [prev, curr] = [curr, prev + curr];\n    }\n    return curr;\n  };\n}, []);\n```\n\n## Common Pitfalls\n- **Dependency array**: Empty array [] ensures function isn't recreated\n- **Memory leaks**: Large memo objects can cause memory issues\n- **Over-optimization**: useMemo overhead may exceed benefits for simple calculations\n- **Referential equality**: useMemo preserves function reference for child components",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-4",
        "questionId": "q-24631",
        "question": "In a React app with ThemeContext and a per-tenant audit stream, design a PerTenantAuditTrail panel that replays user actions from a WebSocket/stream with time-based playback (last 2 minutes), supports pause/resume and seeking to a timestamp. Use useSyncExternalStore to subscribe, memoize a selector keyed by (tenantId, timeWindow) returning a bounded events array, render each row with a React.memo EventRow, and clean up on tenant switch. Provide code sketches for: stream wrapper, selector, EventRow, and panel wiring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use React.lazy to import VideoGrid only when the panel is opened; render a skeleton meanwhile. Lazy-load thumbnails with loading='lazy' and an IntersectionObserver gating non-critical widgets. Ensure",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Map per user to hold { data, expiry, inFlight }. get(userId) returns existing inFlight or starts fetchFn(userId) and caches the Promise; on success store data and set expiry. refresh(userId) for",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-tenant WebSocket bound with useSyncExternalStore to stream audit events. Memoize a selector keyed by (tenantId, timeWindow) that yields a bounded array of the last 120 events; render with React.memo EventRow c…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement progressive hydration for the grid by splitting columns into visible and offscreen groups. Hydrate only visible columns on initial mount, then lazy-load remaining columns via dynamic imports triggered by scrol…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design a streaming, per-tenant UI with time-based playback, memory controls, and precise cleanup when tenants switch. It exercises useSyncExternalStore, memoized selectors keyed by (tenantId, timeWindow), bounded ring buffers to cap memory, React.memo for EventRow components, playback controls (play/pause and seek), and tenant switch cleanup.\n\n## Key Concepts\n- useSyncExternalStore for external streams\n- memoized selectors keyed by (tenantId, timeWindow)\n- Bounded ring buffer to cap memory\n- React.memo for EventRow components\n- Playback controls: play/pause and seek\n- Tenant switch cleanup\n\n## Code Example\n```javascript\n// Stream wrapper using useSyncExternalStore\nfunction useAuditStream(tenantId) {\n  const storeRef = useRef(null);\n  if (!storeRef.current) {\n    storeRef.current = createAuditStore(tenantId);\n  }\n  return useSyncExternalStore(\n    storeRef.current.subscribe,\n    storeRef.current.getSnapshot,\n    storeRef.current.getServerSnapshot\n  );\n}\n\n// Memoized selector keyed by (tenantId, timeWindow)\nconst selectBoundedEvents = memoize((tenantId, timeWindow) => {\n  return (events) => {\n    const cutoff = Date.now() - timeWindow;\n    return events\n      .filter(e => e.tenantId === tenantId && e.timestamp > cutoff)\n      .slice(-120); // Bounded to 120 events\n  };\n});\n\n// React.memo EventRow\nconst EventRow = memo(({ event }) => (\n  <div>{event.action} - {event.timestamp}</div>\n));\n\n// Panel wiring\nfunction AuditPanel({ tenantId }) {\n  const events = useAuditStream(tenantId);\n  const boundedEvents = useMemo(\n    () => selectBoundedEvents(tenantId, 120000),\n    [tenantId]\n  )(events);\n  \n  return (\n    <div>\n      {boundedEvents.map(e => <EventRow key={e.id} event={e} />)}\n    </div>\n  );\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-5",
        "questionId": "q-20193",
        "question": "Design a real-time per-organization collaborative dashboard in a React app. The active organization is provided via OrganizationContext. Users drag-and-drop resizable widgets in a grid (e.g., react-grid-layout). Layout updates are streamed from the server via a streaming store consumed with useSyncExternalStore. Implement optimistic layout edits with rollback on server rejection, and memoize a selector keyed by (orgId, widgetId) to compute per-widget placements. Ensure cleanup on organization switch and SSR compatibility. Provide code sketches for: stream wrapper, optimistic patcher, per-org selector, and grid integration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Redux-like pattern with Context + useReducer to hold a flags map. Implement actions: SET_FLAG for single flag updates and PATCH_FLAGS for bulk updates. Create a custom debounce hook (200ms) to batch server updates…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a real-time NotificationCenterPanel for a multi-tenant admin dashboard. Each tenant receives its own streaming feed via useSyncExternalStore. Implement a memoized selector keyed by (tenantId, notificationId) to d…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Split bundles by route using React.lazy for non-critical features to reduce initial payload. Implement lazy-loading for heavy charts using IntersectionObserver with Suspense to defer rendering until components enter the…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: implement a StreamedLayoutStore that streams per-org layout patches and feed it to the app with useSyncExternalStore. Use a memoized selector keyed by (orgId, widgetId) to compute per-widget",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design real-time, multi-user UIs with proper isolation across organizations; evaluates useSyncExternalStore, memoization, optimistic UI, and SSR considerations.\n\n## Key Concepts\n\n- useSyncExternalStore\n- React-grid-layout drag-and-drop\n- Memoized selectors keyed by (orgId, widgetId)\n- Optimistic patches with rollback\n- Organization-scoped cleanup; SSR-safe\n\n## Code Example\n\n```javascript\n// sketch: Stream wrapper\nexport function createOrgLayoutStream(orgId) { /* ... */ }\n\n// sketch: optimistic patcher\nfunction patchLayoutOptimistically(current, patch) { /* ... */ }\n\n// sketch: per-org selector\nconst selectOrgPlacement = (state, orgId, widgetId) => { ... }\n```\n\n## Follow-up Questions\n\n- How to test concurrent drags across users?\n- How to reconcile out-of-order events or secure patches?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-6",
        "questionId": "fr-172",
        "question": "How would you optimize rendering performance for a React component displaying a large list (10,000+ items) with frequent real-time updates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Fetch initial feed, on success render; on failure, load from IndexedDB cache. WebSocket connects to ws://example/ws, on message parse and append only if new ID not seen. Service Worker caches assets a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Split the app into a core bundle and lazy-loaded parts; defer non-critical analytics to after user interaction or behind a feature flag. Use dynamic import for heavy components with React.lazy and Sus",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "useState is for simple local state, useReducer handles complex state logic, and Context API shares state across components without prop drilling.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "- Virtualize the list with @tanstack/react-virtual to render only visible rows (10k+ items) and amortize updates. - Memoize per-row components (React.memo) and stabilize props with useCallback, passing item.id instead o…",
            "isCorrect": true
          }
        ],
        "explanation": "This approach centers on windowing to keep render work proportional to viewport, not list size. TanStack Virtual handles dynamic heights with estimateSize and on-height changes, while overscan minimizes blank space during fast updates. Memoizing Row avoids re-render cascades when non-visible items update. useTransition and useDeferredValue help keep the visible portion responsive during high-frequency updates, and batching via a reducer reduces render storms. Edge cases: height changes require re-measure, large updates may warrant a worker, and ensure keys are stable to prevent unnecessary mounts.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-7",
        "questionId": "q-28296",
        "question": "You have a React SPA where Lighthouse shows poor performance due to a large initial bundle and long main-thread tasks. Propose a concrete plan to reduce the initial bundle and improve Lighthouse scores using route-based code-splitting, lazy-loading, and a runtime performance budget. Include concrete steps, small code sketches (LazyRoute wrapper, budgetHook, dynamic import naming), and testing strategy with Lighthouse CI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a CSS grid with 3 columns desktop, 2 tablet, 1 mobile. Cards are flex-column; caption slides in via transform/opacity on hover/focus. Implement a CSS-only play toggle: a checkbox that switches a C",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a custom useForm hook that encapsulates form state logic, then compose it with useValidation hook for shared rules. Use factory pattern to generate form-specific instances while maintaining com",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Plan:** Implement route-based code-splitting with dynamic `import()` and named chunks, lazy-load heavy components (e.g., charting libraries) with `React.lazy`/`Suspense`, and defer non-critical CSS. Enforce a build-ti…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Set up a 6-column grid on desktop with grid-template-columns: repeat(6,1fr); scale down to 4 and 2 columns at 1200px and 800px. Use grid-auto-rows: minmax(8rem, auto) and grid-auto-flow: dense to allo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates a candidate's practical skills in diagnosing bundle bloat, applying code-splitting patterns, and instrumenting performance budgets—core competencies for production React performance work.\n\n## Key Concepts\n- Route-level code-splitting via dynamic imports\n- Lazy-loading primitives (`React.lazy`, `Suspense`)\n- Build-time performance budgets and CI gate enforcement\n- Lighthouse CI integration for regression detection\n\n## Code Examples\n- `LazyRoute` wrapper demonstrating lazy loading architecture\n- `budgetHook` for build-time threshold enforcement\n- Named chunk configuration for optimal caching and parallel loading",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-8",
        "questionId": "q-23454",
        "question": "Design an offline-first live notification feed: fetch initial items via REST, then subscribe to live alerts over WebSocket. Cache both catalog and updates in a Service Worker, and render with a custom DOM-diff engine that batches mutations to a list. Ensure deduplication by id, backpressure, and replay of missed events on reconnect. If WS fails, fall back to HTTP polling with backoff. Provide skeletons for REST wrapper, WebSocket wrapper, and the DOM-diff engine?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a grid container with three columns on wide view, two on tablet, one on mobile; grid-auto-rows: minmax(var(--row, 120px), auto); grid-auto-flow: dense; each widget uses a hidden checkbox to t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a closure encapsulating two Maps: `cache` stores entries with timestamps, and `inflight` holds active Promises to deduplicate concurrent requests. The `load(url, {ttl=30000, signal})` method returns cached data if f…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement CSS grid with 3 columns on desktop, 2 on tablet, 1 on mobile using grid-template-columns. Each card contains a 16:9 thumbnail, a bold title, and a description. Add a CSS-only density toggle",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I'd implement a resilient pipeline: first fetch initial items via REST, then subscribe to live alerts over WebSocket. Events are stored in an in-memory Map with a persisted queue in IndexedDB. A custom DOM-diff engine b…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design robust client-side data pipelines with offline support and minimal DOM churn in a real-world feed scenario.\n\n## Key Concepts\n- DOM diffing without a framework\n- WebSocket resilience and backoff strategies\n- Service Worker caching and offline support\n- IndexedDB persistence for offline replay\n- Deduplication, backpressure handling, and event replay mechanisms\n\n## Code Example\n```javascript\n// REST wrapper skeleton\nasync function fetchInitial() { /* ... */ }\n\n// WebSocket wrapper skeleton\nclass WSClient { /* ... */ }\n\n// DOM diff engine skeleton\nclass DOMDiffEngine { /* ... */ }\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-9",
        "questionId": "q-16323",
        "question": "Design a beginner-friendly LiveNotes widget for a product page. It loads notes with fetch('/notes'), subscribes to note updates via WebSocket, and uses a Service Worker to cache assets and the latest notes for offline use. It should allow adding notes via a simple form, queue writes when offline, and sync on reconnect with exponential backoff. Ensure in-order delivery, dedupe by noteId, and debounced DOM renders?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement React.lazy() with Suspense for route splitting, dynamic imports for heavy components, webpack's splitChunks for vendor chunks, and analyze with webpack-bundle-analyzer. Use prefetch/preload for critical paths,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a closure encapsulating two Maps: `cache` stores entries with timestamps, and `inflight` holds active Promises to deduplicate concurrent requests. The `load(url, {ttl=30000, signal})` method returns cached data if f…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Fetch initial dashboard config per tenant, e.g., GET /api/tenant/{id}/dashboard; wrap a per-tenant WebSocket for live mutations {tenantId, op, item}; batch DOM updates via a Fragment and requestAnimat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a LiveNotes widget that fetches /notes on load, connects to a WebSocket for real-time updates, and renders changes with efficient diff-based DOM updates. Use a sequenceId for in-order delivery and deduplicate…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests candidates' familiarity with DOM manipulation, fetch, WebSocket, and Service Workers in a practical offline-first scenario.\n\n## Key Concepts\n- **Offline-first UX**: Using IndexedDB and Service Worker for data persistence and asset caching\n- **Real-time updates**: WebSocket integration with in-order delivery (sequenceId) and deduplication (noteId)\n- **Efficient rendering**: Debounced renders and diff-based DOM updates to minimize reflows\n- **Offline queue**: Storing writes in IndexedDB when offline, then syncing with exponential backoff on reconnect\n\n## Code Example\n```javascript\nfunction debounceRender(render, wait = 120) {\n  let timeout;\n  return function(...args) {\n    clearTimeout(timeout);\n    timeout = setTimeout(() => render.apply(this, args), wait);\n  };\n}\n\nclass NotesWidget {\n  constructor() {\n    this.notes = new Map();\n    this.pendingQueue = [];\n    this.sequenceId = 0;\n    this.render = debounceRender(this.render.bind(this));\n  }\n\n  async init() {\n    await this.loadNotes();\n    this.connectWebSocket();\n    this.registerServiceWorker();\n    this.setupOfflineQueue();\n  }\n\n  async loadNotes() {\n    const res = await fetch('/notes');\n    const notes = await res.json();\n    notes.forEach(note => this.notes.set(note.id, note));\n    this.render();\n  }\n\n  connectWebSocket() {\n    this.ws = new WebSocket('wss://api.example.com/notes');\n    this.ws.onmessage = (event) => {\n      const { data } = JSON.parse(event.data);\n      if (data.sequenceId >= this.sequenceId) {\n        this.sequenceId = data.sequenceId;\n        if (!this.notes.has(data.noteId)) {\n          this.notes.set(data.noteId, data);\n          this.render();\n        }\n      }\n    };\n  }\n\n  async addNote(content) {\n    const note = { id: crypto.randomUUID(), content, timestamp: Date.now() };\n    if (navigator.onLine) {\n      await fetch('/notes', {\n        method: 'POST',\n        body: JSON.stringify(note),\n        headers: { 'Content-Type': 'application/json' }\n      });\n    } else {\n      await this.queueNote(note);\n    }\n  }\n\n  async queueNote(note) {\n    const db = await indexedDB.open('notes-queue', 1);\n    const tx = db.transaction('pending', 'readwrite');\n    tx.objectStore('pending').add({ ...note, retryCount: 0 });\n  }\n\n  async syncQueue() {\n    const db = await indexedDB.open('notes-queue', 1);\n    const tx = db.transaction('pending', 'readwrite');\n    const store = tx.objectStore('pending');\n    const request = store.getAll();\n    \n    request.onsuccess = async () => {\n      for (const note of request.result) {\n        try {\n          await fetch('/notes', {\n            method: 'POST',\n            body: JSON.stringify(note),\n            headers: { 'Content-Type': 'application/json' }\n          });\n          store.delete(note.id);\n        } catch (e) {\n          await this.exponentialBackoff(note, note.retryCount++);\n        }\n      }\n    };\n  }\n\n  async exponentialBackoff(note, attempt) {\n    const delay = Math.min(1000 * Math.pow(2, attempt), 30000);\n    setTimeout(() => this.queueNote(note), delay);\n  }\n\n  render() {\n    // Diff-based DOM update logic here\n  }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-10",
        "questionId": "q-25784",
        "question": "In a multi-tenant React app using ThemeContext visuals and a Redux-like store fed by useSyncExternalStore streaming per-tenant alerts, implement a CriticalAlertsPanel showing a 60-second timeline of alerts for the currently selected tenant. Key points: memoized selector keyed by (tenantId, service) returning top 50 alerts; React.memo AlertRow; react-window virtualization; handle bursts with useTransition; accessibility: aria-live region for new items and keyboard nav; auto-scroll behavior at bottom; cleanup on tenant switch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a registry of widgets with lazy imports; derive visibleIds via a memoized selector that combines tenant features and focus state; render with a virtualization grid (react-window) and a memoized",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a module with a closure-based per-id cache using a Map to store the last 5 results, a debounced render mechanism using requestAnimationFrame, and a Promise chain to serialize UI updates. Incoming batch…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a tiny store: state = { theme:'light', toasts:[] }; reducer handles 'TOGGLE_THEME' and 'ADD_TOAST'. StoreProvider passes {state, dispatch}. useStoreSelector(sel) uses useMemo over sel(state)",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe wiring useSyncExternalStore to a per-tenant alert stream, a memoized selector keyed by (tenantId, service) that returns top 50 alerts, a React.memo AlertRow, and a react-window list. Include",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time data handling, per-tenant state, and accessibility within React, using hooks, context, and virtualization.\n\n## Key Concepts\n- useSyncExternalStore for per-tenant streams\n- memoized selectors with composite keys\n- React.memo for list items and react-window for virtualization\n- aria-live for live updates and keyboard navigation\n\n## Code Example\n```javascript\n// skeletons show per-tenant stream hookup, memoized selector, and list wiring\n```\n\n## Follow-up Questions\n- How to validate focus management and screen reader announcements?\n- What are trade-offs between local memoization vs server-driven paging?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-11",
        "questionId": "q-12291",
        "question": "Design a beginner-friendly frontend module for a live-feed panel that loads initial items via REST, subscribes to real-time updates over WebSocket, and uses a Service Worker to cache assets and the feed payload. Explain how you'd implement offline fallback (cached data), a minimal DOM update strategy that appends only new items, and a simple retry/backoff for failed REST requests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a 4-column grid on wide viewports, 2 columns on medium screens, and 1 column on small screens. Cards have an image area with aspect-ratio, a title, a short blurb, and a CTA button that animates in",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up a 6-column grid on desktop with grid-template-columns: repeat(6,1fr); scale down to 4 and 2 columns at 1200px and 800px. Use grid-auto-rows: minmax(8rem, auto) and grid-auto-flow: dense to allo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a minimal DOM with a list container and a Retry button. On page load, fetch the REST endpoint to render the initial list, then open a WebSocket connection for live updates. Implement a Service Worker to cache the…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Fetch initial feed, on success render; on failure, load from IndexedDB cache. WebSocket connects to ws://example/ws, on message parse and append only if new ID not seen. Service Worker caches assets a",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests familiarity with core web platform features at a practical, beginner level, ensuring offline resiliency and real-time updates.\n\n## Key Concepts\n- REST fetch with error handling and offline fallback\n- WebSocket lifecycle: open, message handling, and backoff on disconnect\n- Service Worker caching strategies and fetch interception\n- Lightweight DOM diff/update to minimize reflows\n- IndexedDB/cache API for offline data\n- Debouncing/render batching\n\n## Code Example\n```javascript\n// Fetch with fallback to IndexedDB\nasync function loadFeed() {\n  try {\n    const resp = await fetch('/api/feed');\n    if (!resp.ok) throw new Error('Network response was not ok');\n    const data = await resp.json();\n    renderFeed(data);\n    // cache data\n  } catch (e) {\n    const cached = await idbGet('feed');\n    renderFeed(cached || []);\n  }\n}\n```\n\n```javascript\n// WebSocket connect with backoff\nlet ws, retry = 1;\nfunction connect() {\n  ws = new WebSocket('ws://example/ws');\n  ws.onmessage = (ev) => {\n    const m = JSON.parse(ev.data);\n    if (!isDuplicate(m.id)) appendItem(m);\n  };\n  ws.onclose = () => {\n    setTimeout(connect, Math.min(30000, 1000 * retry));\n    retry = Math.min(30, retry + 1);\n  };\n}\n```\n\n```javascript\n// Service Worker basic fetch handler\nself.addEventListener('install', e => {\n  caches.open('v1').then(c => c.addAll(['/','/index.html','/styles.css']));\n});\nself.addEventListener('fetch', e => {\n  if (e.request.url.endsWith('/api/feed')) {\n    e.respondWith(\n      fetch(e.request).catch(() => caches.match('/api/feed'))\n    );\n  }\n});\n```\n\n## Follow-up Questions\n- How would you test offline mode and WebSocket reconnection behavior?\n- How would you handle message ordering conflicts during sync after reconnecting?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-12",
        "questionId": "q-19777",
        "question": "Implement a paginated user list in React using a lightweight store (Context + useReducer). Cache pages by key `${page}-${perPage}` for getUsers(page, perPage). Actions: SET_PAGE, SET_USERS, CLEAR_CACHE. Include a Pager; prefetch next page when nearing the end. Provide code sketches for the store, hook, and component?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a CacheContext with a reducer handling actions like { type: 'SET_USERS', key, users } and { type: 'SET_PAGE', key, page }. Read current data via useContext, trigger fetch if cache miss, and store",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan a Lighthouse-first lazy-loading strategy for a Slack-like multi-tenant dashboard. Use a two-stage bootstrap: core bundle loads first; per-tenant feature bundles load on demand via dynamic imports",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a closure-based queue that tracks active uploads, uses Promise.allSettled for partial failure handling, and manages concurrency with a semaphore pattern.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I'd implement a modular architecture that fetches initial widget data via REST API, establishes a WebSocket connection for live updates, and leverages a Service Worker to cache assets and the latest payload for offline…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to implement a small, dependency-free data cache with React Context and useReducer, plus basic async fetch and cleanup.\n\n## Key Concepts\n- useReducer + useContext for a tiny in-memory store\n- page-keyed cache and eviction strategy\n- optimistic UI for page switches and prefetching\n- cleanup on unmount to avoid leaks\n\n## Code Example\n```javascript\n// sample skeleton showing context, reducer, and hook\nexport const CacheContext = React.createContext(null);\nfunction cacheReducer(state, action){\n  switch(action.type){\n    case 'SET_USERS': return { ...state, [action.key]: action.users };\n    case 'SET_PAGE':  return { ...state, currentKey: action.key, currentPage: action.page };\n    case 'CLEAR_CACHE': return {};\n    default: return state;\n  }\n}\n```\n\n## Follow-up Questions\n- How would you invalidate cache after an update?\n- How would you adapt for server-side rendering and hydration?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-13",
        "questionId": "q-16729",
        "question": "Design a tiny FeatureToggle system in React using a Redux-like store (Context + useReducer). Build: 1) FeatureFlagProvider with actions SET_FLAG and PATCH_FLAGS; 2) a debounced server updater (200ms) that pushes flag changes; 3) a useFeatureFlag(name) hook returning [enabled, setEnabled]; 4) a DebugPanel that renders a memoized FlagRow per flag. Ensure re-renders are limited to changed flags and cleanup on unmount?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a function factory to encapsulate a cache. When fetchUserProfile(userId) is called, check if a valid (non-expired) entry exists in the cache and return it via Promise.resolve(). If a fetch for that userId is already…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Map to track in-flight requests per key and a separate Map for cached results. On call, check the cache first and return cached data if available. If a request for the same key is already in-flight, return the exi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Redux-like pattern with Context + useReducer to hold a flags map. Implement actions: SET_FLAG for single flag updates and PATCH_FLAGS for bulk updates. Create a custom debounce hook (200ms) to batch server updates…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a set of radio inputs with a shared `name` attribute and corresponding `<label>` elements for each tab section. The `:checked` state of each radio controls visibility of its associated panel via adjacent sibling sel…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to architect a simple, scalable UI state pattern with Context + useReducer, while handling real-time updates and preventing unnecessary re-renders. It also exercises memoization and debouncing—common patterns in feature-flag management UIs.\n\n## Key Concepts\n- React Context + useReducer for centralized state management\n- React.memo for selective re-render optimization\n- Debouncing external updates to reduce server load\n- Clean-up on unmount to prevent memory leaks\n\n## Code Example\n```javascript\n// FeatureFlagContext.tsx\nimport React, { createContext, useContext, useReducer, useEffect, useRef, useCallback } from 'react';\n\ntype Action =\n  | { type: 'SET_FLAG'; name: string; value: boolean }\n  | { type: 'PATCH_FLAGS'; patches: Record<string, boolean> };\n\ntype State = Record<string, boolean>;\n\nconst reducer = (state: State, action: Action): State => {\n  switch (action.type) {\n    case 'SET_FLAG':\n      return { ...state, [action.name]: action.value };\n    case 'PATCH_FLAGS':\n      return { ...state, ...action.patches };\n    default:\n      return state;\n  }\n};\n\nconst FeatureFlagContext = createContext<{\n  flags: State;\n  dispatch: React.Dispatch<Action>;\n} | null>(null);\n\n// Debounce hook\nconst useDebounce = (callback: () => void, delay: number) => {\n  const timeoutRef = useRef<NodeJS.Timeout>();\n  const callbackRef = useRef(callback);\n  \n  useEffect(() => { callbackRef.current = callback; });\n  \n  useEffect(() => {\n    return () => clearTimeout(timeoutRef.current);\n  }, []);\n  \n  return useCallback(() => {\n    clearTimeout(timeoutRef.current);\n    timeoutRef.current = setTimeout(() => callbackRef.current(), delay);\n  }, [delay]);\n};\n\nexport const FeatureFlagProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {\n  const [flags, dispatch] = useReducer(reducer, {});\n  const debouncedUpdate = useDebounce(() => {\n    // Push flag changes to server\n    fetch('/api/flags', { method: 'PATCH', body: JSON.stringify(flags) });\n  }, 200);\n\n  useEffect(() => { debouncedUpdate(); }, [flags]);\n\n  return (\n    <FeatureFlagContext.Provider value={{ flags, dispatch }}>\n      {children}\n    </FeatureFlagContext.Provider>\n  );\n};\n\n// Custom hook\nexport const useFeatureFlag = (name: string): [boolean, (value: boolean) => void] => {\n  const { flags, dispatch } = useContext(FeatureFlagContext)!;\n  const enabled = flags[name] ?? false;\n  const setEnabled = useCallback((value: boolean) => {\n    dispatch({ type: 'SET_FLAG', name, value });\n  }, [name]);\n\n  return [enabled, setEnabled];\n};\n\n// Debug Panel\nconst FlagRow = React.memo(({ name, value, onToggle }: { name: string; value: boolean; onToggle: () => void }) => (\n  <div>\n    <span>{name}</span>\n    <button onClick={onToggle}>{value ? 'ON' : 'OFF'}</button>\n  </div>\n));\n\nexport const DebugPanel = ({ flags, dispatch }: { flags: State; dispatch: React.Dispatch<Action> }) => {\n  return (\n    <div>\n      {Object.entries(flags).map(([name, value]) => (\n        <FlagRow\n          key={name}\n          name={name}\n          value={value}\n          onToggle={() => dispatch({ type: 'SET_FLAG', name, value: !value })}\n        />\n      ))}\n    </div>\n  );\n};\n```",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-14",
        "questionId": "q-5402",
        "question": "Build a responsive hero grid for a streaming homepage: desktop 4 columns, tablet 3, mobile 2. Center a featured card by using grid-column: 2 / span 2 and grid-row: span 2. Add hover lift and keyboard focus styling, plus reduced-motion support. Provide a minimal HTML skeleton and CSS illustrating the layout and interactions. How would you implement this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a responsive hero grid using CSS Grid with a center featured card that spans two columns and two rows on desktop; scale down to 3 columns on tablets and 2 on mobile. Include hover lift, focus-v",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "- API: useRenderCounter(threshold, { onWarn, once = true } = {}) returns the current render count. - Core: useRef stores count and a warned flag; increment count during render; useEffect runs after render to warn once w…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Normalize state using IDs as keys with a separate IDs array, memoize derived values with useMemo keyed to both the source data and dependencies, and consider creating a custom hook that encapsulates t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On desktop, build a flex header with three top-level items that expose nested dropdown menus via :hover and :focus-within. On mobile, replace the menus with a CSS-only drawer activated by a hidden checkbox (the \"checkbo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world CSS skills: grid complexity, responsive layouts, animation, and accessibility.\n\n## Key Concepts\n- CSS Grid responsive layouts\n- Centered featured element via grid-column/grid-row\n- Hover lift plus reduced-motion\n- Keyboard navigation in a grid\n\n## Code Example\n```javascript\ndocument.querySelectorAll('.card').forEach((el, idx, arr) => {\n  el.addEventListener('keydown', (e) => {\n    const cols = 4;\n    const delta = {ArrowLeft:-1, ArrowRight:1, ArrowUp:-cols, ArrowDown:cols}[e.key];\n    if (delta !== undefined) {\n      const next = arr[(idx + delta + arr.length) % arr.length];\n      next.focus();\n    }\n  });\n  el.setAttribute('tabindex','0');\n});\n```\n\n## Follow-up Questions\n- How would you adapt this for dynamic content changes (infinite loading) while preserving focus order?\n- How would you audit performance and ensure 60fps during reflow?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-15",
        "questionId": "q-22602",
        "question": "You're building a compound component pattern in React where a parent component manages state and multiple child components need to share and update that state implicitly, without prop drilling. How would you implement this using React Context with useReducer to create a flexible, type-safe compound component API, and what strategies would you use to prevent unnecessary re-renders when the Context value changes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: build a per-tenant stream wrapper used by useSyncExternalStore to feed a top-N leaderboard. Implement a memoized selector keyed by tenantId and topN that filters events within a 5-minute win",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use useReducer with a history array containing past and future states, implement undo/redo actions that swap current state with history entries, and maintain a separate 'pending' state for optimistic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a useSyncExternalStore-based WebSocket client to feed a Redux read-model. Memoize a selector that buckets events by second, render with a virtualization list (react-window), and memoized item components to minimize…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a combination of Context for implicit state passing and useReducer for centralized state updates. Use React.memo and separate contexts for state vs dispatch to prevent unnecessary re-renders of co",
            "isCorrect": true
          }
        ],
        "explanation": "The compound components pattern is best implemented using React Context with a provider that wraps the parent component. Create a context using `createContext` that holds both the state and dispatch function. The parent component uses useReducer to manage state and passes both to the Provider value.\n\nTo prevent unnecessary re-renders, separate the state and dispatch into two different contexts - one for state and one for dispatch functions. This way, components that only need dispatch (like button handlers) won't re-render when state changes, and vice versa. Additionally, wrap child components in React.memo and use useCallback for any event handlers passed down.\n\nFor type safety, define proper TypeScript interfaces for the state shape and action types. Use the \"render props\" or \"children as function\" pattern if you need more flexibility. The provider should also use memoization on its value prop using useMemo to prevent creating new object references on every render.\n\nA practical example is a Tab component where TabList, Tab, and TabPanel share state - the TabList acts as the parent managing which tab is active, and individual Tab components implicitly communicate with it through context without knowing about each other.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-16",
        "questionId": "q-30326",
        "question": "In a CSS-driven analytics dashboard, implement a CSS-only responsive layout using CSS Grid with named areas: a persistent left Filters panel and a right Content area with a 3-column card grid. Collapse Filters on small screens via a CSS-only checkbox toggle. Each card has a 16:9 chart region and a toolbar; cards stagger on load with per-card animation delays via nth-child. Density adjustable with CSS vars; honor prefers-reduced-motion and focus-visible outlines?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use CSS Grid masonry with grid-auto-rows: 8px and per-card grid-row-end: span var(--span,18). 3/2/1 columns via media queries. Rail fixed on desktop; CSS-only toggle collapses it into a bottom sheet o",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage per-field closures to store a debounce timer and an AbortController; on input, reset the timer and, when it fires, start a fetch with its own controller. Track a global in-flight counter to cap concurrent reque…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use CSS Grid with CSS custom properties for dynamic sizing, CSS transforms for animations, and virtualization for performance.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implemented a CSS-only dashboard layout using Grid with named areas: left Filters column and right Content with a 3-up card grid. The Filters pane collapses on small screens via a CSS-only checkbox to",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses advanced CSS proficiency across grid layouts, responsive design, and CSS-only interactivity with accessibility considerations.\n\n## Key Concepts\n\n- CSS Grid with named areas\n- Responsive breakpoints and layout reflow\n- CSS-only interactivity using a checkbox toggle\n- Per-card animation staggering via nth-child\n- CSS custom properties for density control\n- prefers-reduced-motion and focus-visible outlines\n\n## Code Example\n\n```javascript\n/* HTML structure (illustrative) */\n<div class=\"dashboard\">\n  <aside class=\"filters\" aria-label=\"Filters\">...</aside>\n  <section class=\"content\">\n    <article class=\"card\">...</article>\n  </section>\n</div>\n```\n\n## Follow-up Questions\n\n- How would you test accessibility across screen readers and keyboard navigation?\n- How would you adapt this to RTL layouts and dynamic data loading?",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-17",
        "questionId": "q-5050",
        "question": "You're developing a React application with forms that have complex validation logic, where validation rules depend on the values of other form fields and need to run both on user input and form submission. How would you implement a state management pattern using useReducer with derived state and memoization to handle field dependencies, prevent unnecessary re-validations, and maintain performance as the form scales to 20+ fields?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Bootstrap initial data by fetching /init?tenantId=... via Response.body.getReader() and parsing the NDJSON stream. Establish a WebSocket connection to receive real-time events in the format {tenantId, id, ts, value}. Im…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Map per user to hold { data, expiry, inFlight }. get(userId) returns existing inFlight or starts fetchFn(userId) and caches the Promise; on success store data and set expiry. refresh(userId) for",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use CSS Grid with virtual scrolling, CSS transforms for animations, and requestAnimationFrame for batched DOM updates to maintain 60fps.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use useReducer with a state structure that tracks field values, validation errors, and touched status. Implement memoized selectors for derived validation logic and dispatch actions that batch related",
            "isCorrect": true
          }
        ],
        "explanation": "The solution starts with a useReducer hook that manages form state as a single object containing field values, validation errors, touched status, and dependency mappings. The reducer should handle actions like FIELD_CHANGE, BLUR_FIELD, and SUBMIT_FORM, with the FIELD_CHANGE action triggering validation for dependent fields while other actions skip validation entirely. Use React.memo to wrap field components and implement custom comparison functions to prevent re-renders when field-specific state hasn't changed.\n\nFor field dependencies, create a validation schema that maps each field to its dependent fields and the validation functions that should run. When a field changes, use useCallback with proper dependencies to run validation only for the changed field and its direct dependents, storing results in state. Implement useMemo to compute derived state like overall form validity or error counts, preventing expensive recalculations on every render.\n\nThe key performance optimization is batching updates - use unstable_batchedUpdates or React 18's automatic batching to group field changes and validation updates. Also implement validation debouncing for text inputs and selective validation triggers based on user interaction patterns. This approach scales well because validation complexity grows linearly with field count rather than exponentially, and the single source of truth eliminates state synchronization issues.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-18",
        "questionId": "q-27791",
        "question": "Design a CSS-only responsive media gallery: desktop two-column grid (image 60%, info 40%), mobile stacked. Each item uses a hidden checkbox as a Play/Pause toggle to control a caption animation via CSS. Add a hover tilt on desktop, a staggered entrance for grid items, and ensure focus-visible outlines and prefers-reduced-motion support. No JavaScript required. Provide a minimal HTML/CSS sketch with two items?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a CSS Grid catalog inside a fixed container, using container queries to switch from 1 to 3 columns as width grows. Each card includes a 4:3 image area, a title, and a details panel o",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I'd implement a CSS grid container with `grid-template-columns: repeat(6, 1fr)` and use media queries to reduce to 4 and 2 columns. A `data-layout` attribute toggles gaps and typography (dense vs cozy). Cards would anim…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Redux-like store with a useSelector-like hook to read items and a local query. Memoize the filtered list with useMemo and render MenuItem as React.memo. Implement a KeyboardNav hook to manage fo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a CSS-only responsive media gallery: desktop two-column grid (image 60%, info 40%), mobile stacked. Each item uses a hidden checkbox as a Play/Pause toggle to control a caption animation via CS",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests building a responsive gallery layout with CSS Grid and minimal interactivity using only CSS. It also probes accessibility patterns (focus-visible, reduced motion) and a practical CSS trick (checkbox-driven state) that remains robust across devices.\n\n## Key Concepts\n\n- CSS Grid for responsive two-column vs stacked layouts\n- CSS-only state management with hidden checkboxes\n- Animations with stagger and motion preferences\n\n## Code Example\n\n```javascript\n<!-- Minimal HTML/CSS sketch (conceptual; not functional here) -->\n```\n\n## Follow-up Questions\n\n- How would you extend to a1430-item grid while preserving performance?\n- What accessibility tweaks would you add for screen readers?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-19",
        "questionId": "q-25076",
        "question": "You're building a React accordion component where multiple panels need to manage their open/closed state, but you also need to allow only one panel to be open at a time (accordion behavior) while supporting both controlled and uncontrolled usage patterns. How would you implement this using compound components with a custom hook, and what patterns would you use to ensure type-safe prop passing between parent and child components without prop drilling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Load the AuditTrail panel only when it becomes visible or on first user interaction by using dynamic import('./AuditTrailPanel') behind a gate. Show a skeleton while loading, and cache the loaded modu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a layered state management solution with optimistic updates, using localStorage for immediate persistence and a background sync queue with conflict resolution strategies.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Fetch initial dashboard config per tenant, e.g., GET /api/tenant/{id}/dashboard; wrap a per-tenant WebSocket for live mutations {tenantId, op, item}; batch DOM updates via a Fragment and requestAnimat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a custom hook that returns both a context provider and the hook itself, combined with forwardRef and explicit prop typing to create a type-safe compound component pattern that supports both controlled and uncontroll…",
            "isCorrect": true
          }
        ],
        "explanation": "This question tests understanding of the compound component pattern combined with flexible state management. The solution involves creating a custom hook that manages both controlled and uncontrolled state simultaneously, using a context to share state between parent and children without prop drilling, and leveraging TypeScript generics for type safety. The hook returns both a context provider wrapper and a useAccordion hook that children use to access the accordion state and methods. For controlled/uncontrolled support, the hook detects whether the value prop was provided and uses that for state management, falling back to internal state when no prop is supplied.",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-20",
        "questionId": "q-22323",
        "question": "Design and implement a client-side CatalogFeed for a storefront that loads the initial catalog via REST, caches API responses with a Service Worker using stale-while-revalidate, then subscribes to live price and stock updates over WebSocket and updates the DOM with a virtualized, batched diff renderer. Include offline mode, reconnection with exponential backoff, and per-item update coalescing; provide minimal code sketches for REST/WS wrappers and the diff engine?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "REST wrapper fetchCatalog() caches responses in a Service Worker using stale-while-revalidate; WebSocket client streams {productId, price, stock, ts} and uses exponential backoff on disconnect; a DOM",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a memoized selector (reselect) to bucket incoming 50k events/sec into 1s intervals, keeping raw stream in a separate Redux slice. The chart is a React.memo component receiving only the current buc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "useState is for simple local state, useReducer handles complex state logic, and Context API shares state across components without prop drilling.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use useReducer with a state structure that tracks field values, validation errors, and touched status. Implement memoized selectors for derived validation logic and dispatch actions that batch related",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess practical integration of REST fetch, Service Worker caching, WebSocket live updates, and efficient DOM rendering with virtualization and offline resiliency.\n\n## Key Concepts\n- Service Worker caching with stale-while-revalidate for API responses\n- WebSocket reconnection with exponential backoff\n- DOM diffing with per-item state and batched updates\n- Virtualization for large catalogs\n- IndexedDB for offline queueing and replay\n\n## Code Example\n```javascript\n// REST wrapper\nasync function fetchCatalog() {\n  const r = await fetch('/api/catalog');\n  return r.json();\n}\n\n// WS skeleton\nclass CatalogWS {\n  constructor(url) { this.url = url; this.conn = null; this.backoff = 1000; }\n  connect() {\n    this.conn = new WebSocket(this.url);\n    this.conn.onmessage = () => {/* handle diffs */};\n    this.conn.onclose = () => setTimeout(() => this.connect(), this.backoff *= 2);\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test offline-first behavior end-to-end?\n- How would you detect and recover from missed or out-of-order live updates?",
        "difficulty": "intermediate"
      },
      {
        "id": "frontend-q-21",
        "questionId": "q-10901",
        "question": "In a React app using a Redux-like central store and ThemeContext, implement an AccessibleNavMenu that renders a searchable list of navigation items from the store. Include: 1) a useSelector-like hook to read items filtered by a query in local state; 2) a KeyboardNav custom hook to move focus with Arrow keys and Home/End; 3) ARIA roles (role='menu', aria-activedescendant); 4) a memoized MenuItem component; 5) ensure cleanup on unmount and when query changes. Provide minimal code sketches?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Map per user to hold { data, expiry, inFlight }. get(userId) returns existing inFlight or starts fetchFn(userId) and caches the Promise; on success store data and set expiry. refresh(userId) for",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "On load, fetch('/docs/:id') and cache via SW; connect to '/ws/docs/:id' for deltas with {seq, op}; apply ops to DOM in order; if a gap occurs, fetch full doc to resync; reconnect with exponential back",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Redux-like store with a useSelector-like hook to read items and a local query. Memoize the filtered list with useMemo and render MenuItem as React.memo. Implement a KeyboardNav hook to manage fo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a RealTimeFormPanel subscribing to per-workspace form state via useSyncExternalStore. Use a memoized selector keyed by (workspaceId, formId, fieldName) to compute field validity and pristine",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to compose a store-driven UI with accessible keyboard navigation, memoization, and context-driven styling in a beginner-friendly way.\n\n## Key Concepts\n- ARIA roles and attributes for menus\n- Keyboard navigation patterns (Arrow keys, Home/End)\n- useSelector-like subscription and local memoization\n- React.memo for item components and useMemo for derived data\n\n## Code Example\n```javascript\nfunction MenuItem({ item }) {\n  return <li role=\"menuitem\" id={item.id}>{item.label}</li>\n}\n```\n\n## Follow-up Questions\n- How would you test focus management with screen readers?\n- How to handle dynamic item updates without losing keyboard state?",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-22",
        "questionId": "q-5328",
        "question": "You're building a lightweight analytics dashboard for a new product used by MongoDB/LinkedIn teams. Lighthouse flags long main-thread tasks and oversized bundles. Describe a concrete plan to: 1) split code so a heavy chart loads via dynamic import and only when visible, 2) lazy-load feed images, 3) enforce a simple performance budget (initial JS under 120 KB gzip, LCP under 2.5s). Include practical code sketches and testing steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use useReducer to manage multiple state slices (pending, error, original), implement optimistic updates by storing the pre-update state, and use a try-catch pattern with a rollback mechanism that rest",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a patch-based DOM renderer for LiveGrid Studio: load the initial grid layout via REST, cache assets with a Service Worker (SW) using stale-while-revalidate, then subscribe to a real-time patch stream over WebSock…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Split the chart into a lazy-loaded component using React.lazy and import() so it's not part of the initial bundle. Use an IntersectionObserver to mount it only when the chart area enters the viewport. For images, implem…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Optimistic updates require maintaining a temporary state that can be reverted on failure. The pattern involves: (1) storing the previous state before applying the optimistic change, (2) immediately updating the UI to re…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical skills in performance-focused frontend engineering: code-splitting, lazy-loading, and measuring impact with Lighthouse budgets, all in a realistic dashboard scenario.\n\n## Key Concepts\n- Code-splitting with dynamic import and React.lazy\n- IntersectionObserver for lazy-loading components and images\n- Native image lazy-loading with loading=\"lazy\" attribute\n- Performance budgets in webpack/parcel configuration\n- Lighthouse performance metrics and CI validation\n- Bundle analysis and optimization strategies\n\n## Implementation Strategy\nThe solution addresses all three requirements systematically: dynamic component loading with visibility detection, image lazy-loading with both native and fallback approaches, and enforceable performance budgets. The IntersectionObserver pattern ensures resources load only when needed, while webpack performance configuration provides automated budget enforcement during builds.",
        "difficulty": "beginner"
      },
      {
        "id": "frontend-q-23",
        "questionId": "q-12316",
        "question": "Design a client-side workflow for a real-time dashboard that loads initial widget data via REST, subscribes to live updates over WebSocket, and leverages a Service Worker to cache assets and the latest payload for offline use. Provide (1) a minimal in-browser patch-based DOM updater that applies changes to widgets, (2) a WebSocket reconnect strategy with exponential backoff, (3) a fetch wrapper that deduplicates in-flight requests and caches responses for 60s, and (4) cross-tab state consistency via BroadcastChannel. Include a concrete API sketch and a short code example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement createLoader(baseUrl) that uses a closure-based cache and an inFlight map. load(path) fetches baseUrl+path and returns res.json(). cachedLoad(path) serves from cache when fresh, otherwise st",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a closure-based pool: keep a queue and an active counter. enqueue(task) returns a Promise; whenever active < limit, shift a task, increment active, and run it. On completion, decrement active and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: split the UI with dynamic imports and a visibility gate so heavy components load only when on screen. Use images and fonts lazy-loading (font-display: swap). Add skeletons and measure impact. Va",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I'd implement a modular architecture that fetches initial widget data via REST API, establishes a WebSocket connection for live updates, and leverages a Service Worker to cache assets and the latest payload for offline…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates full-stack frontend capabilities by testing multiple advanced concepts in a single feature: DOM diffing algorithms, network resilience patterns, offline-first architecture, request optimization, and cross-browser tab synchronization.\n\n## Key Concepts\n\n- Patch-based DOM updates for efficient rendering\n- WebSocket reconnection with exponential backoff strategy\n- Service Worker for intelligent caching and payload storage\n- In-flight request deduplication for performance optimization\n- BroadcastChannel API for cross-tab state synchronization\n\n## Code Example\n\n```javascript\n// API Sketch\nGET /api/widgets          // Initial widget data\nWS /api/realtime          // WebSocket endpoint\n\n// Fetch wrapper with deduplication and caching\nconst fetchCache = new Map();\nconst pendingRequests = new Map();\n\nconst smartFetch = async (url, options = {}) => {\n  const cacheKey = `${url}:${JSON.stringify(options)}`;\n  const cached = fetchCache.get(cacheKey);\n  \n  if (cached && Date.now() - cached.timestamp < 60000) {\n    return cached.data;\n  }\n  \n  if (pendingRequests.has(cacheKey)) {\n    return pendingRequests.get(cacheKey);\n  }\n  \n  const promise = fetch(url, options).then(response => response.json());\n  pendingRequests.set(cacheKey, promise);\n  \n  try {\n    const data = await promise;\n    fetchCache.set(cacheKey, { data, timestamp: Date.now() });\n    return data;\n  } finally {\n    pendingRequests.delete(cacheKey);\n  }\n};\n\n// WebSocket with exponential backoff\nconst connectWebSocket = (url, maxRetries = 5) => {\n  let retryCount = 0;\n  \n  const connect = () => {\n    const ws = new WebSocket(url);\n    \n    ws.onopen = () => {\n      retryCount = 0;\n      console.log('WebSocket connected');\n    };\n    \n    ws.onclose = () => {\n      if (retryCount < maxRetries) {\n        const delay = Math.min(1000 * Math.pow(2, retryCount), 30000);\n        setTimeout(connect, delay);\n        retryCount++;\n      }\n    };\n    \n    return ws;\n  };\n  \n  return connect();\n};\n\n// Cross-tab state synchronization\nconst broadcast = new BroadcastChannel('dashboard-state');\n\nbroadcast.onmessage = (event) => {\n  if (event.data.type === 'widget-update') {\n    updateWidget(event.data.payload);\n  }\n};\n\n// DOM patch updater\nconst updateWidget = (changes) => {\n  const widget = document.getElementById(changes.widgetId);\n  if (!widget) return;\n  \n  // Simple patch-based update\n  changes.patches.forEach(patch => {\n    const element = widget.querySelector(patch.selector);\n    if (element) {\n      if (patch.type === 'text') {\n        element.textContent = patch.value;\n      } else if (patch.type === 'attr') {\n        element.setAttribute(patch.name, patch.value);\n      }\n    }\n  });\n};\n```",
        "difficulty": "advanced"
      },
      {
        "id": "frontend-q-24",
        "questionId": "q-11837",
        "question": "Implement a lightweight ES6 DataLoader module for user profiles. Expose a function loadUser(id) that returns a Promise. Requirements: - if multiple calls for the same id occur while the first fetch is in-flight, deduplicate to a single network request and return the same Promise to all callers; - cap total in-flight requests at 3; additional calls should queue and start as slots free; - cache successful results for 5 minutes; within that window, calls for the same id should resolve immediately with cached data; - use closures to hide internal state, and leverage Map, async/await. Provide code sketch and a short usage example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a 4-column grid on wide viewports, 2 columns on medium screens, and 1 column on small screens. Cards have an image area with aspect-ratio, a title, a short blurb, and a CTA button that animates in",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deeply nested state causes referential equality bugs and update complexity. Normalizing state into flat maps by entity type (users, posts, comments) with IDs as keys solves this. Use selector function",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set up a CSS grid with 3 columns (>=1024px), 2 columns (600–1023px), 1 column (<600px). Each card has header/body/footer; header uses Flexbox to place title and a CSS-only dropdown toggled by focus-wi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A closure-scoped loader maintains an inFlight map, a time-based cache, and a queue. loadUser(id) checks cache, reuses in-flight promises for the same id, or enqueues a new request. A simple throttle e",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of closures, promise deduping, in-flight concurrency control, and simple caching in a realistic data-loading scenario.\n\n## Key Concepts\n- Closure-scoped internal state (inFlight, cache, queue)\n- Promise coalescing for same id\n- Concurrency limit with a slot-based throttle\n- Time-based cache with TTL\n\n## Code Example\n```javascript\nfunction createUserLoader() {\n  const inFlight = new Map(); // id -> Promise\n  const cache = new Map(); // id -> { data, expiry }\n  const queue = [];\n  const MAX_CONCURRENCY = 3;\n  let active = 0;\n\n  const fetchUser = async (id) => {\n    const res = await fetch(`/api/user/${id}`);\n    if (!res.ok) throw new Error('Network error');\n    return res.json();\n  };\n\n  const tryStart = () => {\n    while (active < MAX_CONCURRENCY && queue.length) {\n      const { id, resolve, reject } = queue.shift();\n      active++;\n      const p = fetchUser(id)\n        .then((data) => {\n          cache.set(id, { data, expiry: Date.now() + 5 * 60 * 1000 });\n          inFlight.delete(id);\n          resolve(data);\n        })\n        .catch((err) => reject(err))\n        .finally(() => { active--; tryStart(); });\n      inFlight.set(id, p);\n    }\n  };\n\n  const loadUser = (id) => {\n    const cached = cache.get(id);\n    if (cached && cached.expiry > Date.now()) return Promise.resolve(cached.data);\n    if (inFlight.has(id)) return inFlight.get(id);\n    return new Promise((resolve, reject) => {\n      queue.push({ id, resolve, reject });\n      tryStart();\n    });\n  };\n\n  return { loadUser };\n}\n\n// Usage\n// const loader = createUserLoader();\n// loader.loadUser('123').then(console.log);\n```",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-05T12:27:43.361Z",
    "version": 1
  }
]