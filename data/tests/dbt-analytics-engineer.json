[
  {
    "id": "test-dbt-analytics-engineer",
    "channelId": "dbt-analytics-engineer",
    "channelName": "DBT Analytics Engineer",
    "title": "DBT Analytics Engineer Knowledge Test",
    "description": "Test your understanding of DBT Analytics Engineer with 25 questions.",
    "questions": [
      {
        "id": "dbt-analytics-engineer-q-0",
        "questionId": "q-21106",
        "question": "Design a per-tenant user identity unification in Snowflake/dbt: map staging.tenant_events.user_id into analytics_{{tenant}}.customers via a per-tenant HMAC-SHA256 hash using a salt stored in analytics_{{tenant}}.salts. Ensure deterministic hashing for late data, implement a macro hash_user_id, 1-day late data handling, not_null and unique tests on (tenant_id, day_start, hashed_user_id), and enforce per-tenant RLS masking. Include CI gate to block production if any tenant's hashed_user_id drift or salt missing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-tenant identity unification using a dbt macro hash_user_id that applies HMAC-SHA256 with a per-tenant salt from analytics_{{tenant}}.salts to staging_tenant_events.user_id, materialize a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Develop a per-tenant data quality score flow. A macro builds analytics_{{tenant}}.quality_score and updates a global analytics.lineage. Score = 0.7*(valid_event_ratio) + 0.3*(latency_OK_ratio) over th",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a dbt macro that iterates all tenants to materialize analytics_{{tenant}}.anomaly_daily, deriving day_start from occurred_at and computing a 7-day moving average per tenant/event_type. Flag is",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement analytics.sales.daily_user_spend as an incremental model keyed by (user_id, order_date), joining staging.orders with staging.users as needed. Compute total_spend = sum(amount) and order_count = count(*). Confi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design privacy-preserving identity mapping, per-tenant salt governance, and robust CI gates.\n\n## Key Concepts\n\n- dbt macros for hashing\n- per-tenant isolation and RLS\n- late-arriving data handling\n- CI/CD checks for missing salts and drift\n\n## Code Example\n\n```jinja\n{% macro hash_user_id(user_id, salt) -%}\n  {{ return(LOWER(TO_HEX(HMAC_SHA256(CAST({{ user_id }} AS STRING), '{{ salt }}')) )) }}\n{%- endmacro %}\n```\n\n## Follow-up Questions\n\n- How would you test determinism of the hash across tenants with different salts?\n- How would you handle salt rotation without breaking historical mappings?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-1",
        "questionId": "q-8836",
        "question": "Design a per-tenant, privacy-preserving event funnel in Snowflake with dbt: from staging.tenant_events to analytics_{{tenant}}.funnel_daily via a macro that masks user_id for non-full-access roles. Include 1-day late data, schema drift guards, tests: not_null on keys, unique on (tenant_id, day_start, user_id), a global analytics.lineage, and CI checks that every per-tenant view exists and data is isolated. Explain row-level security and masking with dbt and Snowflake policies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a dbt incremental analytics.daily_user_events: group by tenant_id, date(occurred_at), event_type; count distinct user_id. Enrich using left join dimensions.countries on country_code; use a smal",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a dbt macro to generate analytics_{{tenant}}.funnel_daily from staging.tenant_events, masking user_id for roles lacking FULL_ACCESS using a Snowflake policy plus deterministic hashing for jo",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a per-tenant cohort dbt flow in Snowflake using a macro that boots analytics_{{tenant}}.session_cohort from staging.sessions (tenant_id, session_id, started_at, ended_at, device_type) and analytics.dim_devices. P…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement analytics_base.freshness_check(tenant, table, date_col) to compute latest_lag_minutes = floor((current_timestamp - max(date_col))/60) and status 'OK' or 'LAG'. Build per-tenant incremental a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n Assesses ability to combine dbt macros with Snowflake security policies to enforce tenant isolation while preserving analytics usability.\n\n## Key Concepts\n - dbt macro generation for multi-tenant objects\n - Snowflake Row Access Policy and masking integration\n - Incremental processing with late-arrival data\n - Data lineage and CI validation for governance\n\n## Code Example\n ```javascript\n// Pseudo macro sketch\nmacro create_funnel_daily(tenant) {\n  // create analytics_{{tenant}}.funnel_daily as incremental from staging.tenant_events\n}\n```\n\n## Follow-up Questions\n - How would you test cross-tenant leakage in CI?\n - What are potential performance implications of masking on large joins?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-2",
        "questionId": "q-2403",
        "question": "Beginner dbt task: from staging.events (event_id, user_id, event_type, occurred_at, region_code, delete_flag) and dimensions.regions, implement an incremental model analytics.daily_event_summary that counts distinct users per day, event_type, country_code (via region_code). Exclude deleted events by default; provide a macro to toggle inclusion for audits. Add tests not_null(event_id, occurred_at) and unique(event_id)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a dbt macro that generates analytics_{{tenant}}.churn_score incrementally from staging.tenant_events and analytics_common.customer_dim, with 1-day late data. Enforce masking policies on PII, an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement analytics.daily_event_summary as an incremental Snowflake model: aggregate date_trunc('day', occurred_at), event_type, country_code (via regions) with count(distinct user_id). Use a macro de",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a macro tenant_summary that writes analytics_{{tenant}}.daily_signup_summary from staging.tenant_signups. Include tenant_id, signup_date, new_users. Allow 1-day late data with a separate lat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To implement per-tenant rollback, create analytics_{{tenant}}.control with rollback_needed, target_date; a macro perform_rollback() that clones analytics_{{tenant}}.facts to a rollback variant as of t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests a candidate's ability to implement a simple, robust incremental model with data quality checks and a governance hook (macro) for audits. Emphasizes joins, aggregation, and proper filtering.\n\n## Key Concepts\n- Incremental modeling in dbt\n- Basic joins to enrich with dimensions.regions\n- Simple data governance via macros for optional audit paths\n- Tests: not_null and unique\n\n## Code Example\n```sql\n-- analytics/daily_event_summary.sql\nwith src as (\n  select\n    date_trunc('day', occurred_at) as day,\n    e.event_type,\n    r.country_code,\n    count(distinct e.user_id) as active_users\n  from {{ source('staging','events') }} e\n  join {{ ref('dimensions_regions') }} r on e.region_code = r.region_code\n  where {{ delete_filter(false) }}\n  group by 1,2,3\n)\nselect * from src;\n```\n\n```sql\n-- macros/delete_filter.sql\n{% macro delete_filter(include_deleted=false) -%}\n  {% if include_deleted %}\n    1=1\n  {% else %}\n    delete_flag = false\n  {% endif %}\n{%- endmacro %}\n```\n\n## Follow-up Questions\n- How would you test the macro across multiple models?\n- How would you adapt this to handle late-arriving updates in staging?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-3",
        "questionId": "q-18659",
        "question": "Design a per-tenant anomaly-flag pipeline in Snowflake/dbt: from staging.tenant_events to analytics_{{tenant}}.anomalies, using a global baseline analytics_base.anomaly_baseline and per-tenant calibration analytics_{{tenant}}.calibration. Include a macro resolve_anomaly_threshold(tenant) that selects threshold by tenant tier, supports 1-day late data, enforces schema drift guards, and a CI gate ensuring per-tenant anomalies exist and are tenant-isolated. Provide macro names, tests, and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build a macro that on a new tenant creates analytics_{tenant} schemas and hourly_engagement incremental models, plus a Snowflake stream for staging.tenant_events. Use a global analytics_lineage table",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "The solution uses a dbt macro `analytics_base.assign_cohort(event_ts)` to return cohorts in 'YYYY-MM' format. A per-tenant model `analytics_{{tenant}}.cohorts` joins `staging.tenant_events` to assign cohorts based on si…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "JSON-driven data quality: rules.json defines per-tenant required_fields and max_null_pct; the dq_rules macro auto-generates not_null tests and a null-rate test for each tenant/table/field. Create anal",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant anomaly pipeline from staging.tenant_events to analytics_{{tenant}}.anomalies, driven by a threshold macro resolve_anomaly_threshold(tenant) using per-tenant tier, with 1-day la",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-tenant isolation, governance, and threshold dynamics in dbt/macros.\n\n## Key Concepts\n- per-tenant thresholding\n- schema drift guards\n- CI governance gates\n- lineage tracking\n\n## Code Example\n```sql\n-- macros/thresholds.sql\n{% macro resolve_anomaly_threshold(tenant) %}\n  {% set tier = run_query(\"select tier from analytics_base.tenant_tiers where tenant = '\" ~ tenant ~ \"'\").rows[0].tier %}\n  {{ 0.98 if tier == 'enterprise' else 0.92 }}\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you test drift in the threshold over time?\n- What performance considerations arise with per-tenant thin views vs. materialized tables?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-4",
        "questionId": "q-21211",
        "question": "Design a beginner per-tenant incremental dbt flow in Snowflake that maps staging.tenant_events to analytics_{{tenant}}.secure_engagement. Implement mask_user_pii() to hash user_id for non-FULL_ACCESS roles, exporting analytics_{{tenant}}.secure_engagement_view with tenant_id, day, hashed_user_id, engagements. Support 1-day late data. Add not_null and unique tests; CI gate fails if hashed_user_id is NULL; discuss cross-tenant leakage prevention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a dbt macro that creates analytics_{{tenant}}.rolling_engagement_7d per tenant. Compute daily_active_users from staging.tenant_events and derive a 7-day rolling sum with a 2-day late window.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant incremental dbt flow from staging.tenant_events to analytics_{{tenant}}.secure_engagement; use a mask_user_pii() macro to hash user_id for non-FULL_ACCESS roles. Expose analytics_{{tenant}}",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Approach: implement a single incremental model that builds analytics_{{tenant}}.daily_summary by joining staging.tenant_events on tenant_id. A macro drives per-tenant materialization from seeds.tenant",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an incremental analytics.daily_events that aggregates counts by day, event_type, and region_code from staging.event_logs joined to dims.regions on region_code. Use a 2-day late window, enfor",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical dbt design: per-tenant flow, privacy via masking, and CI guards.\n\n## Key Concepts\n- dbt incremental models per tenant\n- macro-driven PII masking with role awareness\n- secure views for data access control\n- CI gates validating data quality and privacy\n\n## Code Example\n```javascript\n--- a/macros/mask_user_pii.sql\n{% macro mask_user_pii(user_id, role) -%}\n  {% if role != 'FULL_ACCESS' %}\n    sha256({{ user_id }})\n  {% else %}\n    {{ user_id }}\n  {% endif %}\n{%- endmacro %}\n```\n\n## Follow-up Questions\n- How would you test that non FULL_ACCESS roles never see unmasked IDs?\n- How would you extend this for new tenants with different privacy requirements?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-5",
        "questionId": "q-16763",
        "question": "Design a per-tenant anomaly detection pipeline in Snowflake/dbt. From staging.tenant_events to analytics_{{tenant}}.anomalies_daily, compute per-tenant event_rate and a z-score-based anomaly flag using a per-tenant config stored in analytics_{{tenant}}.config (window_size, threshold). Implement a macro to auto-create the per-tenant schema and a global analytics.lineage entry. Include 1-day late data, schema drift guards, tests: not_null on keys, unique on (tenant_id, day), and CI checks that per-tenant config exists and anomalies are isolated by tenant?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose macro analytics_base.verify_sourcedrift(tenant) that compares analytics_{{tenant}}.source_mappings to analytics_base.source_registry, returns per-tenant drift_score (mismatches/total_sources).",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an incremental model analytics.daily_finance that upserts sum(amount) and transaction_count by day, currency, country_code, and status; allow late data within a 2-day window via a MERGE-based upse",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant predictive-scoring pipeline in Snowflake/dbt: analytics_{{tenant}}.scores derived from staging.tenant_events and analytics_{{tenant}}.features using analytics_base.ml_models. Imple",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build an incremental per-tenant anomaly detection pipeline: compute a daily event_rate per tenant from staging.tenant_events, fetch per-tenant thresholds from analytics_{{tenant}}.config via a macro, calculate z-scores…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests end-to-end implementation of per-tenant anomaly detection, macro-driven schema creation, config-driven thresholds, 1-day late data handling, schema drift guards, lineage tracking, and CI quality gates.\n\n## Key Concepts\n- **Per-tenant config**: Table analytics_{{tenant}}.config storing window_size and threshold\n- **Z-score anomaly detection**: Statistical approach using per-tenant rolling statistics\n- **Incremental models**: Efficient processing of new data per tenant\n- **Macro-driven schema creation**: Automated per-tenant schema generation for isolation\n- **Data quality**: Drift guards and CI validation\n\n## Code Example\n```sql\n-- Per-tenant anomaly detection with config-driven thresholds\nWITH tenant_stats AS (\n    SELECT \n        tenant_id,\n        event_day,\n        COUNT(*) AS event_count,\n        AVG(COUNT(*)) OVER (\n            PARTITION BY tenant_id \n            ORDER BY event_day \n            ROWS BETWEEN {{ window_size }} PRECEDING AND 1 PRECEDING\n        ) AS rolling_avg,\n        STDDEV(COUNT(*)) OVER (\n            PARTITION BY tenant_id \n            ORDER BY event_day \n            ROWS BETWEEN {{ window_size }} PRECEDING AND 1 PRECEDING\n        ) AS rolling_stddev\n    FROM {{ ref('staging_tenant_events') }}\n    WHERE event_day < CURRENT_DATE() - 1\n    GROUP BY tenant_id, event_day\n)\nSELECT \n    s.tenant_id,\n    s.event_day,\n    s.event_count AS event_rate,\n    CASE \n        WHEN s.rolling_stddev > 0 \n        THEN (s.event_count - s.rolling_avg) / s.rolling_stddev \n    END AS z_score,\n    CASE \n        WHEN s.rolling_stddev > 0 \n             AND ABS((s.event_count - s.rolling_avg) / s.rolling_stddev) > c.threshold\n        THEN TRUE \n    END AS anomaly_flag\nFROM tenant_stats s\nJOIN {{ ref('config') }} c ON s.tenant_id = c.tenant_id\n```",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-6",
        "questionId": "q-2901",
        "question": "Design a multi-tenant dbt pipeline that creates per-tenant-per-region analytics schemas (analytics.{tenant}_{region}). From staging.events (tenant_id, region_id, user_id, event_type, occurred_at, ingestion_id), implement an incremental analytics.{tenant}_{region}.hourly_metrics counting distinct users by event_type per hour, with late data handling of 1 day. Add a tenant-region users snapshot for cohort changes and a tenancy-isolation macro to block cross-region joins. Include tests (not_null, unique) and cross-tenant lineage validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a macro analytics_base.compare_counts(tenant,left_table,right_table,keys) returning drift_pct by comparing row counts and key-level aggregates; add per-tenant analytics_{{tenant}}.snapshot_a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe a per-tenant-per-region dbt flow creating schemas analytics.{tenant}_{region} with an incremental hourly_metrics model over staging.events(tenant_id, region_id, user_id, event_type, occurred_",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a per-tenant identity unification using a dbt macro hash_user_id that applies HMAC-SHA256 with a per-tenant salt from analytics_{{tenant}}.salts to staging_tenant_events.user_id, materialize a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a per-tenant revenue flow via a macro that creates analytics_{{tenant}}.daily_revenue from staging.billing_events, normalizing to USD with a currencies table. Add analytics_{{tenant}}.data_avail",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to enforce tenancy boundaries in a dynamic multi-tenant, multi-region dbt setup, including late-arriving data and per-tenant snapshots.\n\n## Key Concepts\n- Dynamic per-tenant-per-region schemas analytics.{tenant}_{region}\n- Incremental hourly_metrics from staging.events\n- Deduplication via ingestion_id with ROW_NUMBER windowing\n- Late data window of 1 day; schema-drift guards via tests\n- Tenancy isolation macro preventing cross-region joins\n- Snapshots for analytics.{tenant}_{region}.users and lineage checks\n\n## Code Example\n```sql\n-- models/analytics/{{tenant}}_{{region}}/hourly_metrics.sql\nwith src as (\n  select * from {{ ref('staging_events') }}\n  where tenant_id = '{{ tenant }}' and region_id = '{{ region }}'\n),\ndedup as (\n  select *,\n         row_number() over (partition by ingestion_id order by occurred_at desc) as rn\n  from src\n)\nselect\n  date_trunc('hour', occurred_at) as hour,\n  event_type,\n  count(distinct user_id) as active_users\nfrom dedup\nwhere rn = 1\ngroup by 1,2\n```\n\n## Follow-up Questions\n- How would you validate cross-region lineage in the catalog?\n- What strategies ensure resilience if a region is degraded?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-7",
        "questionId": "q-2840",
        "question": "You’re building a beginner dbt flow for multi-tenant analytics. With staging.events(tenant_id, user_id, event_type, occurred_at) and a tenants.csv seed containing tenant_id and risk_multiplier, design analytics.{tenant}.daily_event_score that sums events per day by type, applies the per-tenant risk_multiplier, tolerates 1 day late data, and includes a per-tenant users snapshot and basic tests. How would you implement this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant anomaly layer with a rolling 7- or 14-day z-score per event_type, compute per-tenant anomaly_flag, and incrementally materialize analytics.{tenant}.anomalies by day. Include 2-day",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-tenant incremental model analytics.{tenant}.daily_event_score built on staging.events, joined to seeds.tenants for multiplier. Bucket occurred_at to day, group by tenant_id, day, event_type;",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a per-tenant macro to create analytics_{{tenant}} schemas. Build analytics_{{tenant}}.daily_metrics by tenant_id/day from staging.tenant_users/engagement; compute DAU and a z-score anomaly flag in",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a tenant-scoped DAU pipeline using an incremental model `analytics_{{tenant}}.dau_incremental` that reads from `staging.tenant_clicks(event_ts, user_id, email, country_code)` to produce `daily_active_users` per t…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a practical, per-tenant incremental flow using seeds for parameters, with data freshness, tests, and a snapshot to track user cohorts.\n\n## Key Concepts\n- Incremental models per tenant\n- Seeds for tenant metadata\n- Per-tenant schemas/isolation\n- Data contracts with tests and snapshots\n- Late data handling and lineage\n\n## Code Example\n```sql\n-- analytics/{tenant}.daily_event_score.sql\nwith e as (\n  select * from {{ ref('staging__events') }}\n)\nselect\n  tenant_id,\n  date_trunc('day', occurred_at) as day,\n  event_type,\n  count(distinct user_id) as user_count,\n  count(distinct user_id) * t.multiplier as score\nfrom e\njoin {{ ref('tenants_seed') }} t on t.tenant_id = e.tenant_id\ngroup by 1,2,3\n```\n\n## Follow-up Questions\n- How would you validate late data beyond the 1-day window, and alert on anomalies?\n- How would you handle tenants missing a multiplier in the seed and prevent failures?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-8",
        "questionId": "q-25647",
        "question": "Design a per-tenant reconciliation pipeline in Snowflake/dbt: analytics_{{tenant}}.reconciled_costs drawing from staging.tenant_billing and analytics_{{tenant}}.usage_logs by tenant_id, product_id, and period. Use a shared analytics_base.metrics for thresholds; implement 2-day late data; mask PII in notes; enforce per-tenant RLS; add a CI gate to block production if reconciliation_error_rate > 5% in any tenant over the last 7 days. Include macro names and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant daily session summary in dbt: an incremental model analytics.{tenant}.daily_session_summary using a tenant-scoped macro to reference staging.sessions. Define unique key as (tena",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a macro `analytics_base.validate_schema(tenant)` that reads `information_schema` for both `analytics_{{tenant}}.fct` and `analytics_base.fct`, computes a JSON diff of columns, data types, and nullabili…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a macro that materializes analytics_{{tenant}}.consent_audit from staging.consent_events, rounded to day_start with daily counts per consent_type. Use an incremental load with a 1-day late window to capture delay…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a per-tenant reconciliation pipeline that compares analytics_{{tenant}}.reconciled_costs with external billing data for each tenant, product, and period. Implement 2-day late data, use analyti",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests end-to-end dbt multi-tenant design, data quality checks, and governance when reconciling external billing with internal usage data.\n\n## Key Concepts\n- Incremental per-tenant models and cross-source joins\n- Data quality thresholds via analytics_base.metrics\n- PII masking, RLS, and access control\n- CI gates for reconciliation errors\n\n## Code Example\n```javascript\n-- macro: analytics_base.generate_reconciliation_checks\nselect count(*) from {{ this }} where mismatch is true;\n```\n\n## Follow-up Questions\n- How would you parameterize thresholds per tenant?\n- How to test backfill scenarios when late data breaches thresholds?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-9",
        "questionId": "q-4894",
        "question": "Design a per-tenant churn-risk dbt pipeline in Snowflake. From staging.tenant_actions (tenant_id, user_id, action, occurred_at) and staging.tenant_users (tenant_id, user_id, signup_date), build an incremental model that computes analytics.{tenant}.churn_risk using features like last_seen_days, days_since_signup, and average_session_minutes (derived from event streams). Create a per-tenant analytics.{tenant}.schema and a global analytics.lineage table. Include 3-day late data handling, not_null on keys, and a min_score guard. Outline tests, a simple macro for tenant onboarding, and CI checks to prevent cross-tenant leakage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a per-tenant masked engagement flow by defining a Snowflake masking policy on user_id and exposing analytics_{{tenant}}.masked_engagement via a dbt macro analytics_build_view(tenant). The ma",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant daily_engagement via an incremental dbt model from staging.tenant_events to analytics_{{tenant}}.daily_engagement using a macro that auto-creates tenant schemas, masks user_id f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage per-tenant namespaced incremental models with a MERGE-based upsert on analytics.{tenant}.daily_metrics, keyed by (tenant_id, day, event_type, user_id). Use a surrogate daily_key to enable ide",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an incremental dbt model per tenant that derives churn_risk from tenant_actions and tenant_users. Upsert analytics.{tenant}.churn_risk by tenant_id and user_id, compute features (last_seen_d",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to architect multi-tenant churn analytics with late data, isolation, and governance beyond basic models.\n\n## Key Concepts\n- Per-tenant schemas and global lineage\n- Incremental upserts with MERGE in Snowflake\n- Feature engineering for churn signals\n- Late-arrival handling and schema-drift guards\n- CI governance for tenant onboarding\n\n## Code Example\n```javascript\n// dbt model sketch: churn_risk.sql\nwith staging as (\n  select tenant_id, user_id, max(occurred_at) as last_seen\n  from {{ ref('staging__tenant_actions') }}\n  group by 1,2\n)\nselect t.tenant_id, t.user_id,\n  datediff('day', signup_date, last_seen) as last_seen_days,\n  datediff('day', signup_date, current_date()) as signup_age_days,\n  avg_session_minutes\nfrom staging s\njoin {{ ref('staging__tenant_users') }} u on s.tenant_id = u.tenant_id and s.user_id = u.user_id\n```\n\n## Follow-up Questions\n- How would you test cross-tenant isolation and lineage drift in CI?\n- How would you onboard a new tenant without downtime to analytics schemas?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-10",
        "questionId": "q-6767",
        "question": "NEW ANGLE: Privacy-focused per-tenant analytics. Given seeds.tenants(tenant_id, privacy_class) and staging.user_events(tenant_id, user_id, event, pii_email, occurred_at), design a beginner dbt flow that uses a macro to create analytics_{{tenant}}.daily_privacy_events with counts by event by day, while hashing pii_email per tenant (central macro using SHA256) and emitting hashed_user_id instead of raw IDs. Include 1-day late data, tests not_null/unique, a global analytics.lineage, and a CI check ensuring per-tenant tables exist and new tenants auto-create. How would you validate cross-tenant isolation and hashing consistency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a macro-driven per-tenant diagnostics flow: generate analytics_{{tenant}}.diagnostics_daily from staging.tenant_events with latency_ms, completeness_pct, and error_rate_pct per day; use MERGE f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a macro to create analytics_{{tenant}}.daily_privacy_events with daily event counts, hashing pii_email via SHA256 per tenant and emitting hashed_user_id. Build incrementally from staging.user_even",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a per-tenant incremental MERGE from staging.{tenant}.events into analytics.{tenant}.daily_revenue (key: tenant_id, date, currency) with a 2-day late data window and record_hash for idempoten",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cross-tenant data quality and lineage validation pipeline using the following approach: **1. Per-Tenant Schema Structure** - `analytics_{{tenant}}.events` — tenant-specific event data - `analytics_base.shared_m…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical dbt macro usage, per-tenant isolation, and simple privacy controls in a beginner task. It also tests incremental modeling, tests, and CI validation without requiring deep production-scale infra.\n\n## Key Concepts\n- dbt macros for tenant-scoped objects\n- incremental models with late-arrival data\n- data masking via hashing (per-tenant salt concept)\n- tests for data quality and uniqueness\n- cross-tenant isolation checks in CI\n\n## Code Example\n```sql\n-- sample macro usage snippet (conceptual, not full model)\nselect\n  tenant_id,\n  date(occurred_at) as day,\n  event,\n  sha2(pii_email, 256) as hashed_email\nfrom {{ ref('staging__user_events') }}\n```\n\n## Follow-up Questions\n- How would you manage salt-per-tenant for hashing to avoid identical hashes across tenants?\n- How would you extend tests to detect accidental leakage of pii fields in non-privacy tables?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-11",
        "questionId": "q-29026",
        "question": "Design a centralized multi-tenant Data Quality framework in Snowflake/dbt. Per-tenant schemas: analytics_{{tenant}} and shared analytics_base. Implement analytics_base.dq_score(tenant) using five checks (not_null primary keys, valid_foreign_keys, non_empty_critical_fields, recency of last_event_ts, stable daily row_count). Create analytics_{{tenant}}.dq_report storing date, score, and violations; analytics_base.dq_dashboard aggregating across tenants. Add per-tenant RLS on dq_violations. CI gate: fail prod if any tenant score < 80 for 2 consecutive days or violations > 3 in a day. Include tests and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement analytics.daily_engagement as an incremental model that aggregates distinct users per date and event_type, joining dimensions.regions on region_code. Late data is allowed in a 3-day wind",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an incremental per-tenant data-quality pipeline in Snowflake/dbt that derives analytics_{{tenant}}.quality_score from staging.tenant_logs using analytics_base.quality_model and analytics_{{tenant}}.thresholds. Th…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement analytics_base.sync_schema(tenant) to diff tenant schemas against a central catalog, emit per-tenant analytics_{{tenant}}.schema_changes, and auto-generate analytics_{{tenant}}.migrate_schem",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Compute a 0–100 score per tenant from five checks: not_null(pk), valid_fk, non_empty_critical_fields, recency of last_event_ts, and stable daily row_count. macro analytics_base.dq_score(tenant) return",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design a scalable, centralized data quality framework for multi-tenant Snowflake/dbt environments with strong governance and CI integration.\n\n## Key Concepts\n- Centralized macros and per-tenant schemas\n- Data quality scoring and violation tracking\n- Cross-tenant dashboards and RBAC integration\n- CI gates for production safety\n\n## Code Example\n```sql\n-- Minimal sketch: macro to compute a per-tenant score (pseudo-SQL)\nCREATE OR REPLACE MACRO analytics_base.dq_score(tenant STRING)\nRETURNS VARIANT\nAS $$\n  SELECT 100\n  - (CASE WHEN NOT_NULL(pk) THEN 0 ELSE 10 END)\n  - (CASE WHEN valid_fk THEN 0 ELSE 20 END)\n  -- additional checks omitted for brevity\n  AS score\n$$;\n```\n\n## Follow-up Questions\n- How would you rotate baseline rules to adapt to schema drift?\n- How would you test SLAs across tenants in CI?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-12",
        "questionId": "q-29475",
        "question": "In a Snowflake/dbt setup with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a per-tenant snapshot strategy for analytics_{{tenant}}.snapshot_costs to capture late-arriving staging_tenant_cost_events up to 5 days late. Implement SCD Type 2 history with a tenant-scoped primary key (tenant_id, cost_id, snapshot_date). Provide macro analytics_base.snapshot_refresh(tenant, src_table, dest_table, date) and a minimal schema outline. Include not_null tests on keys and a unique(tenant_id, snapshot_date, cost_id). Add a CI gate to block production if any tenant's day-over-day delta exceeds 25% for 2 consecutive days?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a tenant-aware macro to render analytics.{tenant}.daily_user_event_count from staging.events, with an incremental key (tenant_id, date(occurred_at), event_type). Late data 1 day via",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement an incremental MERGE into analytics_{{tenant}}.snapshot_costs from analytics_tenant_cost_events, keyed by (tenant_id, cost_id, snapshot_date). The macro analytics_base.snapshot_refre",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Ingest per-tenant events, dedupe by tenant_id/event_id, aggregate daily counts by action_type and total_duration, and map action_type to category via a macro and analytics_base.event_definitions. Impl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Macro swap_tenant_debug(tenant, mode) to toggle analytics_{{tenant}}.hourly_engagement between prod and a debug mirror analytics_{{tenant}}.debug_hourly_engagement; backfill uses Snowflake Time Travel",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical skills in per-tenant data modeling, late-arrival handling, and production safety gates in a dbt/Snowflake context.\n\n## Key Concepts\n- Incremental MERGE-based snapshots per tenant\n- SCD Type 2 history preservation\n- Late-arrival window (5 days)\n- Macro design for reusable refresh logic\n- CI gates for cross-tenant variance anomalies\n\n## Code Example\n```javascript\n// macro signature sketch (analytics_base.snapshot_refresh)\n{% macro snapshot_refresh(tenant, src_table, dest_table, date) %}\n  -- pseudocode: compute deltas, MERGE into dest_table, end previous rows, insert new\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you test for lineage and cross-tenant dependencies with this snapshot?\n- How would you handle concurrent snapshot_refresh runs for the same tenant?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-13",
        "questionId": "q-29167",
        "question": "Design a per-tenant schema drift guard: in Snowflake/dbt with per-tenant analytics_{{tenant}} schemas and a shared analytics_base, implement a macro analytics_base.validate_schema(tenant) that compares analytics_{{tenant}}.fct to analytics_base.fct and reports a JSON diff of columns, types, and nullability; add a test that loops all tenants and fails if drift exists; wire a CI gate to block deploys when drift is detected?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a macro `analytics_base.validate_schema(tenant)` that reads `information_schema` for both `analytics_{{tenant}}.fct` and `analytics_base.fct`, computes a JSON diff of columns, data types, and nullabili…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a dbt macro that reads per-tenant JSON quality_rules from tenants and applies them during the load into analytics_{{tenant}}.quality_summary, validating tenant_id, day_start, and metric not",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a multi-tenant incremental dbt flow in Snowflake that computes analytics.{tenant}.monthly_cost from staging.sales_events (tenant_id, cost_cents, currency, billed_at, event_hash). Build analytics.{tenant}.monthly_…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose staging per domain, a single grain, incremental models with a 2-day late-arrival window, a surrogate key like domain_key + occurred_at + id for cross-domain joins, a macro-based cross-domain r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical multi-tenant schema governance, focusing on contract maintenance across tenants, macro design, and CI integration.\n\n## Key Concepts\n\n- Schema drift detection across tenants\n- Using `information_schema` inside dbt macros\n- Enumerating tenants dynamically\n- JSON diff construction and interpretation\n- CI gates and `run-operation` integration\n\n## Code Example\n\n```sql\n{% macro analytics_base.validate_schema(tenant) -%}\n  {% set tenant_schema = 'ANALYTICS_' ~ tenant | upper %}\n\n  with canonical as (\n    select column_name, data_type, is_nullable\n    from {{ target.database }}.analytics_base.information_schema.columns\n    where table_name = 'fct'\n  ),\n  tenant_schema_cols as (\n    select column_name, data_type, is_nullable\n    from {{ target.database }}.{{ tenant_schema }}.information_schema.columns\n    where table_name = 'fct'\n  ),\n  diff as (\n    select\n      coalesce(c.column_name, t.column_name) as column_name,\n      case\n        when c.column_name is null then 'missing_in_canonical'\n        when t.column_name is null then 'missing_in_tenant'\n        when c.data_type != t.data_type then 'type_mismatch'\n        when c.is_nullable != t.is_nullable then 'nullable_mismatch'\n        else null\n      end as drift_type,\n      c.data_type as canonical_type,\n      t.data_type as tenant_type,\n      c.is_nullable as canonical_nullable,\n      t.is_nullable as tenant_nullable\n    from canonical c\n    full outer join tenant_schema_cols t\n      on c.column_name = t.column_name\n    where c.column_name != t.column_name\n       or c.data_type != t.data_type\n       or c.is_nullable != t.is_nullable\n  )\n\n  select to_json(array_agg(object_construct(*))) as drift_report\n  from diff\n{%- endmacro %}\n\n{% macro analytics_base.validate_all_tenant_schemas() -%}\n  {% set tenants = ref('tenants') %}\n  {% set drift_found = false %}\n  {% set all_diffs = [] %}\n\n  {% for tenant in run_query('SELECT tenant_id FROM ' ~ tenants) %}\n    {% set tenant_id = tenant[0] %}\n    {% set drift = run_query(analytics_base.validate_schema(tenant_id)) %}\n    {% if drift[0][0] is not none %}\n      {% set drift_found = true %}\n      {% set all_diffs = all_diffs + [{'tenant': tenant_id, 'drift': drift}] %}\n    {% endif %}\n  {% endfor %}\n\n  {% if drift_found %}\n    {{ exceptions.raise_compiler_error('Schema drift detected: ' ~ all_diffs) }}\n  {% endif %}\n{%- endmacro %}\n```\n\n## Key Takeaways\n\n1. **Full outer join** ensures detection of columns missing from either schema\n2. **Object construct** in Snowflake builds JSON objects for readable drift reports\n3. **Fail fast** using `exceptions.raise_compiler_error` for CI gate integration\n4. **Config-driven tenants** via `ref('tenants')` keeps tenant list maintainable",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-14",
        "questionId": "q-26361",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a beginner-friendly per-tenant data quality sampling workflow. Create a macro analytics_base.sample_tuples(tenant, table, sample_size) that deterministically returns N rows per tenant from analytics_{{tenant}}.{{table}} using a per-tenant seed. Build a dbt model analytics_{{tenant}}.qa_sample from staging.tenant_events and add tests not_null and unique for sample_id and tenant. Add a CI gate to fail prod if any tenant's latest-day sample_count is below sample_size for 7 consecutive days?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A central retention policy table `analytics_base.retention_policies` stores tenant, table_name, retention_days, and archive_schema. The `analytics_base.apply_retention(tenant, table)` macro moves rows older than retenti…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a macro to materialize analytics_{{tenant}}.daily_revenue by incrementally merging staging.sales and staging.refunds per day. Build a global analytics.lineage to track dependencies. Backfill with tenant-specific dat…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a macro redaction_policy(tenant_id) that reads a central tenant_config(redact_pii). If true, return masking expressions for email and phone; otherwise pass-through. In analytics.{tenant}.hou",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a beginner-friendly per-tenant data quality sampling workflow. Create a macro analytics_base.sample_tuples(…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests a practical, beginner-friendly data QA pattern that scales by tenant, leveraging dbt macros and per-tenant lineage. It encourages deterministic sampling, traceable QA artifacts, and CI gates for automated quality enforcement.\n\n## Key Concepts\n- Deterministic per-tenant sampling using a seed for reproducibility\n- dbt macro design and Jinja templating for reusable SQL\n- Per-tenant QA model creation with tests (not_null, unique)\n- CI gating based on multi-day sample sufficiency thresholds\n\n## Code Example\n```sql\n{% macro sample_tuples(tenant, table, sample_size) -%}\nSELECT *\nFROM analytics_{{ tenant }}.{{ table }}\nQUALIFY ROW_NUMBER() OVER (ORDER BY MD5(CONCAT('{{ tenant }}', '_', _))) % {{ sample_size }} = 0\n{%- endmacro %}\n```\n\n```yaml\n# models/analytics_{{tenant}}/qa_sample.yml\nversion: 2\nmodels:\n  - name: qa_sample\n    tests:\n      - not_null:\n          column_name: sample_id\n      - not_null:\n          column_name: tenant\n      - unique:\n          column_name: sample_id\n```\n\n```yaml\n# dbt_project.yml\non-run-start:\n  - \"{% if target.name == 'prod' %}\"\n  - \"{% set failing_tenants = api.get_adapter().execute('...') %}\"\n  - \"{% if failing_tenants|length > 0 %}\"\n  - \"{{ exceptions.raise_compiler_error('CI Gate Failed: ' ~ failing_tenants|join(', ') ~ ' have insufficient samples') }}\"\n  - \"{% endif %}\"\n  - \"{% endif %}\"\n```\n\nThis workflow provides consistent, reproducible QA samples per tenant while automating quality gates in CI/CD pipelines.",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-15",
        "questionId": "q-29840",
        "question": "Design a per-tenant column-conformance check in Snowflake/dbt: for analytics_{{tenant}}.fact_transactions compare its column definitions against analytics_base.transaction_registry; build a macro analytics_base.column_drift(tenant, table) producing per-column drift scores and a per-tenant analytics_{{tenant}}.transactions_conformance with a rolling 14-day window; add tests drift_missing_columns and drift_type_mismatch, and a CI gate blocking prod if any tenant drift_score exceeds 0.04; include logging for lineage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To implement: use day_start = date_trunc('day', occurred_at); aggregate active_users = count(distinct user_id) where is_deleted = false; implement incremental by tenant schema; create analytics.{tenan",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a macro analytics_base.column_drift(tenant, table) that queries information_schema.columns and compares column_name, data_type, and is_nullable against analytics_base.transaction_reg",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a tenant-scoped data quality automation: a central DSL macro analytics_base.dq_test(label, table, condition) to declare tests, per-tenant analytics_{{tenant}}.dq_run to emit results into analy",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an incremental analytics_{{tenant}}.hourly_metrics table, built by a macro that creates analytics_{{tenant}} schemas and a central analytics.lineage. Partition by hour_start; ingest 1-day late data using dbt's…",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\n\nAssesses ability to design a scalable, tenant-aware conformance gate focusing on column metadata drift, not just row data. Combines macro design, metadata queries, rolling-window metrics, tests, and a CI gate while preserving lineage visibility.\n\nKey Concepts\n\n- dbt macro design and usage\n- Snowflake information_schema for column metadata\n- Cross-tenant governance and a central registry\n- Rolling-window drift computation\n- Tests for missing columns and type mismatches\n- CI gating and lineage logging\n\nCode Example\n\n```javascript\n{% macro analytics_base.column_drift(tenant, table) -%}\n-- pseudo-implementation: compare metadata against analytics_base.transaction_registry\nSELECT\n  c.column_name,\n  c.data_type,\n  c.is_nullable,\n  CASE WHEN r.column_name IS NULL THEN 1 ELSE 0 END AS drift_flag\nFROM information_schema.columns c\nLEFT JOIN analytics_base.transaction_registry r\n  ON r.schema = 'analytics_base' AND r.table_name = 'transaction_registry' AND r.column_name = c.column_name\nWHERE c.table_schema = 'analytics_' || :tenant AND c.table_name = :table\n{% endmacro %}\n```\n\nFollow-up Questions\n\n- How would you scale this to hundreds of tenants without increasing CI gate latency?\n- How would you handle legitimate schema evolution (e.g., deprecating a column) without triggering false positives?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-16",
        "questionId": "q-8164",
        "question": "Design a tenant-aware data quality and security flow in Snowflake + dbt. Create a macro to build analytics_{{tenant}}.hourly_engagement from staging.tenant_events, plus a global analytics.lineage. Implement per-tenant row-level security via Snowflake policies tied to a tenants registry, ensuring cross-tenant isolation. Include 1-day late data, schema-drift guards, and tests (not_null on keys, unique on (tenant_id, hour_start, user_id)). Add CI to verify policies exist for all tenants and auto-provision on registry changes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a macro to materialize analytics_{{tenant}}.daily_revenue by incrementally merging staging.sales and staging.refunds per day. Build a global analytics.lineage to track dependencies. Backfill with tenant-specific dat…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a macro redaction_policy(tenant_id) that reads a central tenant_config(redact_pii). If true, return masking expressions for email and phone; otherwise pass-through. In analytics.{tenant}.hou",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an incremental per-tenant model that writes to analytics.{tenant}.daily_event_engagement using staging.events, joined to dimensions.regions by country_code to group by region_name. Aggregate",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Macro-driven per-tenant schemas and hourly_engagement from staging; per-tenant ROW ACCESS POLICIES tied to a tenants registry enforce isolation; 1-day late data with a Snowflake TASK; tests cover not_",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design scalable, secure, multi-tenant dbt pipelines with automated provisioning and governance.\n\n## Key Concepts\n\n- Tenant-aware schemas and macros\n- Snowflake ROW ACCESS POLICIES linked to a runtime registry\n- Late-arrival data handling and drift guards\n- Global lineage and CI validation\n\n## Code Example\n\n```sql\n-- Macro sketch (dbt) to create tenant schema and table\n{% macro create_tenant_schema(tenant) -%}\ncreate schema if not exists analytics_{{ tenant }};\ncreate table analytics_{{ tenant }}.hourly_engagement (...);\n{% endmacro %}\n```\n\n```sql\n-- Policy sketch (Snowflake)\nCREATE OR REPLACE ROW ACCESS POLICY tenant_rls ON analytics_{{tenant}}.hourly_engagement USING (tenant_id = current_tenant_id());\n```\n\n## Follow-up Questions\n\n- How would you test cross-tenant leakage in CI?\n- How would you scale policy management for hundreds of tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-17",
        "questionId": "q-28334",
        "question": "Design a per-tenant calibration gate in Snowflake/dbt: analytics_{{tenant}}.calibrated_metrics derived from analytics_{{tenant}}.raw_metrics using a shared analytics_base.calibration_model and per-tenant analytics_{{tenant}}.calibration_params (slope and intercept). Support 1-day late data; implement macro analytics_base.apply_calibration(metric, value, tenant); enforce per-tenant RLS isolation; CI gate blocks production if any tenant's calibration_error > 0.08; include tests not_null on keys and unique(metric_id, day) and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a macro create_analytics_schema(tenant) to generate analytics_{{tenant}}.quality_issues and analytics_{{tenant}}.hourly_gap_summary. hourly_gap_summary computes, for each day×hour, a gap flag by l",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Approach Implement a per-tenant calibration pipeline using a shared base model with tenant-specific parameters. ### 1. Shared Calibration Model (`analytics_base.calibration_model`) ```sql SELECT raw.tenant_id, raw.me…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a comprehensive cross-tenant data quality and governance framework: **1. Freshness Macro** ```jinja {% macro freshness_by_tenant(tenant) %} SELECT MAX(event_ts) AS max_event_ts, CURRENT_TIMESTAMP() AS evaluate…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant incremental pipeline using a dbt macro that produces analytics_{{tenant}}.anomaly_scores. The macro joins staging.tenant_events to analytics.tenant_config to retrieve per-tenant thresholds, then comp…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates a candidate's ability to design multi-tenant analytics infrastructure with governance safeguards, code reuse patterns, late-data handling, and production-quality testing.\n\n## Key Concepts\n\n- **Per-tenant isolation**: Separate schemas (`analytics_{{tenant}}`) with RLS enforcement ensure data isolation\n- **Shared vs. tenant-specific**: Shared `calibration_model` promotes DRY; per-tenant `calibration_params` allows business customization\n- **Macro abstraction**: `apply_calibration(metric, value, tenant)` enables consistent calibration logic across all metrics and tenants\n- **Late data handling**: The 1-day buffer accommodates reporting delays common in analytics pipelines\n- **CI gate**: Blocking deployments when `calibration_error > 0.08` prevents drift from propagating to production",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-18",
        "questionId": "q-25259",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a dynamic, tenant-scoped data lineage and impact-analysis system that surfaces downstream impact of upstream schema changes in real-time. Requirements: (1) a dbt macro analytics_base.lineage_impact(source_table, column) that traces to dependent models across tenants, (2) a per-tenant materialized view analytics_{{tenant}}.lineage_summary updated daily, (3) a central analytics_base.lineage_dashboard aggregating cross-tenant lineage health, (4) per-tenant RLS to restrict lineage visibility, (5) a CI gate to block production if drift in lineage_count exceeds 3 per week. Include tests and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant incremental model that ingests staging.clicks into analytics_{tenant}.hourly_conversion_funnel via a macro that creates analytics_{tenant} schema, plus a global analytics.lineage registry for depe…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a macro `analytics_base.validate_schema(tenant)` that reads `information_schema` for both `analytics_{{tenant}}.fct` and `analytics_base.fct`, computes a JSON diff of columns, data types, and nullabili…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant audit trail from staging.tenant_events to analytics_{{tenant}}.audit, leveraging Snowflake Streams and Tasks to apply only new events. Enforce an exact event_id contract, support 1",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a tenant-scoped lineage system: add analytics_base.lineage_impact(source_table, column) to compute downstream models across analytics_{{tenant}}; materialize per-tenant analytics_{{tenant}}.lin",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design cross-tenant governance and lineage tooling in a realistic dbt/Snowflake setup.\n\n## Key Concepts\n- Data lineage and cross-tenant impact analysis\n- Tenant isolation with RLS; central governance surface\n- Incremental lineage_summary and a dashboard\n- Tests: not_null, unique keys for lineage entries\n\n## Code Example\n```sql\n-- dbt macro: lineage_impact (pseudo)\n{% macro lineage_impact(source_table, column) %}\n-- returns downstream models across tenants\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you scale lineage computations with many tenants?\n- How would you detect and handle circular lineage references?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-19",
        "questionId": "q-17236",
        "question": "Design a beginner dbt flow in Snowflake to generate analytics_{{tenant}}.daily_summary from staging.tenant_events for all tenants listed in seeds.tenants, with a macro that auto-drops analytics_{{tenant}} when a tenant becomes inactive. Include 1-day late data, tests not_null(tenant_id, day_start, sum_events) and unique(tenant_id, day_start), a global analytics.lineage, and a CI step to verify active tenants have a view and inactive tenants' views are removed?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: implement a single incremental model that builds analytics_{{tenant}}.daily_summary by joining staging.tenant_events on tenant_id. A macro drives per-tenant materialization from seeds.tenant",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement an incremental model that materializes analytics_{{tenant}}.hourly_anomalies from staging.tenant_events, joining analytics_{{tenant}}.thresholds refreshed nightly from analytics_base.global_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: build a cross-tenant LTV framework using cohort-based revenue in Snowflake/dbt. Implement analytics_base.cohort_key(event_date, cohort_month) to assign users to cohorts; analytics_{{tenant}}",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cross-tenant data-quality and lineage enforcement framework in Snowflake/dbt using per-tenant schemas `analytics_{{tenant}}` and a shared `analytics_base`. Implement 2-day late data tolerance, per-tenant Row-Le…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nPractical multi-tenant dbt patterns in a beginner-friendly scenario: tenant registry, macro-driven provisioning and cleanup, incremental surface, and basic lineage/CI checks.\n\n## Key Concepts\n\n- Seeds as tenant registry\n- Macros for conditional materialization and cleanup\n- Incremental per-tenant views\n- Data lineage and basic CI validation\n\n## Code Example\n\n```javascript\n// Macro sketch (dbt uses Jinja; this is illustrative)\n{% macro build_tenant_view(tenant) %}\n  CREATE OR REPLACE VIEW analytics_{{ tenant }}.daily_summary AS\n  SELECT tenant_id,\n         DATE(event_time) AS day_start,\n         COUNT(*) AS sum_events\n  FROM {{ source('staging','tenant_events') }}\n  WHERE tenant_id = '{{ tenant }}'\n  GROUP BY 1,2;\n{% endmacro %}\n```\n\n## Follow-up Questions\n\n- How would you handle new tenants added to seeds.tenants without breaking existing views?\n- How would you handle tenant deactivation timing and data backfill?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-20",
        "questionId": "q-22466",
        "question": "Design a per-tenant privacy-preserving cohort-analytics pipeline in Snowflake/dbt that derives analytics_{{tenant}}.cohorts from staging.tenant_user_events. Use a tenant-scoped macro to map user_id to a k-anonymized_id, add differential privacy noise to counts, enforce RLS, and include a CI gate that blocks production if any cohort has noisy_count < 10 or if retention drift vs baseline exceeds 5%?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant data quality flow using a dbt macro that materializes analytics_{{tenant}}.quality_flags from staging.tenant_events. Flag nulls on tenant_id/event_id, validate event_date, enforce",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I’d implement a per-tenant cohort pipeline: a tenant-scoped macro hashes user_id to a k-anonymized_id, then aggregate retention cohorts in analytics_{{tenant}}.cohorts from staging.tenant_user_events.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a macro redaction_policy(tenant_id) that reads a central tenant_config(redact_pii). If true, return masking expressions for email and phone; otherwise pass-through. In analytics.{tenant}.hou",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a dbt macro to auto-create analytics_{{tenant}}.dq_daily from staging.tenant_events, joining to a tenants seed for referential checks. Compute per-tenant day_start aggregates: null_user_id, n",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests ability to design privacy-preserving, per-tenant analytics with robust data governance and CI controls.\n\nKey Concepts\n- Tenant-scoped k-anonymity mapping\n- Differential privacy noise on cohort counts\n- Row-level security and per-tenant isolation\n- CI gates to block drift or too-small counts\n- Tests: not_null on keys, unique constraints\n\nCode Example\n```javascript\n-- dbt macro: tenant_k_anonymize.sql\n{% macro tenant_k_anonymize(user_id, tenant_id, k=5) %}\n  sha2(concat(tenant_id, ':', user_id), 256) || '' as anon_user_id\n{% endmacro %}\n\n-- model: analytics_{{tenant}}.cohorts.sql\nwith s as (\n  select *, {{- tenant_k_anonymize('user_id', 'tenant_id') -}} from {{ ref('staging__tenant_user_events') }}\n)\nselect tenant_id, cohort_start, count(distinct anon_user_id) as cohort_size\nfrom s\ngroup by 1,2\n```\n\nFollow-up Questions\n- How would you verify that the DP noise doesn’t leak PII while preserving signal?\n- How would you test cross-tenant isolation in CI for dynamic tenant populations?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-21",
        "questionId": "q-30033",
        "question": "In a Snowflake/dbt setup with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a beginner-friendly per-tenant data quality gate for referential integrity between facts and dimensions. Implement a macro analytics_base.check_fk(tenant, child_table, parent_table, child_key, parent_key) that returns a SQL predicate identifying orphans, and apply it to analytics_{{tenant}}.sales_facts (customer_id) referencing analytics_{{tenant}}.customers(customer_id). Provide a minimal SQL sketch, a dbt test snippet, and describe CI gating behavior?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a tenant-aware macro to render analytics.{tenant}.daily_user_event_count from staging.events, with an incremental key (tenant_id, date(occurred_at), event_type). Late data 1 day via",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create analytics_base.check_fk(tenant, child_table, parent_table, child_key, parent_key) to return a boolean predicate flagging orphans, e.g., NOT EXISTS(SELECT 1 FROM analytics_{{tenant}}.{parent_tab",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To implement, define a macro that, given tenant_id and region, creates analytics_{tenant} schema and configures dbt to target a per-tenant warehouse from a regional map. Use an incremental model for a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a dbt macro to generate per-tenant canary and production models (analytics_canary_{{tenant}}, analytics_{{tenant}}) from staging.tenant_events, compare lightweight row hashes, and fail CI if",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nPer-tenant pipelines risk orphaned rows; a simple macro-based FK check lets tests run without duplicating logic.\n\n## Key Concepts\n- dbt tests and macros\n- per-tenant schemas analytics_{{tenant}}\n- SQL predicate for FK integrity\n- CI gating on data quality\n\n## Code Example\n```sql\n{% macro analytics_base.check_fk(tenant, child_table, parent_table, child_key, parent_key) -%}\nSELECT 1\nFROM analytics_{{tenant}}.{{child_table}} AS c\nLEFT JOIN analytics_{{tenant}}.{{parent_table}} AS p\n  ON p.{{parent_key}} = c.{{child_key}}\nWHERE p.{{parent_key}} IS NULL\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you adapt this for composite keys?\n- How would you integrate this into a CI gate that runs on pull requests and blocks deployment when orphaned rows exist?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-22",
        "questionId": "q-16410",
        "question": "Design a beginner dbt flow for per-tenant daily_summary in Snowflake that respects tenant lifecycle. Read tenant status from seeds.tenants(tenant_id, status) and generate analytics_{{tenant}}.daily_summary only for status='active' using a macro that loops tenants. From staging.tenant_events to daily_summary, 1-day late data. Include tests not_null(tenant_id, day_start, total_events) and unique(tenant_id, day_start). Add CI checks that per-tenant tables exist for active tenants and are skipped for inactive tenants, and explain how to validate cross-tenant isolation when tenants toggle status?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a dbt macro that materializes analytics_{{tenant}}.quality_summary per day. For each tenant, compute freshness = max(event_time) and late_data = datediff('day', freshness, current_timestamp(",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant macro-driven schema analytics_{tenant} with an hourly_session_summary model incremental by hour_start; source from staging.session_events. Support 1-day late data, include schem",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a tenant-scoped audit trail in Snowflake using dbt: ingest data from staging.tenant_events and staging.tenant_changes into tenant-specific analytics_{{tenant}}.audit_daily tables, capturing operation type, event…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a dbt macro that reads active tenants, loops over them, and materializes analytics_{{tenant}}.daily_summary incrementally from staging.tenant_events with 1-day late data. Include not",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of dbt macros, dynamic models, and multi-tenant governance.\n\n## Key Concepts\n\n- dbt macros and dynamic model generation\n- incremental models and late-arrival data\n- per-tenant lifecycle governance and CI checks\n\n## Code Example\n\n```sql\n-- macro example\n{% macro per_tenant_daily_summary_sql(tenant) -%}\nselect tenant_id, date_trunc('day', event_time) as day_start, count(*) as total_events\nfrom {{ ref('staging_tenant_events') }}\nwhere tenant_id = '{{ tenant }}'\ngroup by 1,2\n{% endmacro %}\n```\n\n## Follow-up Questions\n\n- How would you automate onboarding/offboarding tenants in CI?\n- What are the risks of dynamic model generation for schema drift?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-23",
        "questionId": "q-7888",
        "question": "Design a per-tenant cohort dbt flow in Snowflake using a macro that boots analytics_{{tenant}}.session_cohort from staging.sessions (tenant_id, session_id, started_at, ended_at, device_type) and analytics.dim_devices. Produce per-tenant weekly cohorts (first_session_week, device_type) and churn flags. Implement incremental by week, allow 2-day late data, guard against schema drift, include tests (not_null on tenant_id, cohort_week, first_session, churn; unique on (tenant_id, cohort_week, device_type)); add a tenant registry seeds table tenants(tenant_id, created_at) to auto-create analytics_{{tenant}} schemas via macro; maintain a global analytics.lineage table. Provide CI steps to verify bootstrap of new tenants and per-tenant isolation by filtering on tenant_id. Also discuss trade-offs of per-tenant schemas vs single schema with tenant_id?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant cohort dbt flow in Snowflake using a macro that boots analytics_{{tenant}}.session_cohort from staging.sessions (tenant_id, session_id, started_at, ended_at, device_type) and analytics.dim_devices. P…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Describe a per-tenant incremental flow that creates analytics_{tenant} schemas and analytics_{tenant}.anomaly_summary from staging.events, including predicted_anomaly_score. Use a macro for schema cre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a (tenant_id, date( occurred_at ), event_type) keyed incremental model. Compute distinct user_id from staging.user_events, redact email via a macro: if privacy_flag then 'REDACTED' else email. Lat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a macro analytics_base.compare_counts(tenant,left_table,right_table,keys) returning drift_pct by comparing row counts and key-level aggregates; add per-tenant analytics_{{tenant}}.snapshot_a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses macro-driven tenant bootstrapping, multi-tenant isolation, and cohort analytics with late-data handling, plus lineage and CI validation.\n\n## Key Concepts\n- Macro-driven schema creation per tenant\n- Incremental weekly cohort modeling\n- Cross-tenant isolation via tenant_id scoping\n- Schema drift guards and tests\n- Data lineage table analytics.lineage\n\n## Code Example\n```sql\n-- Pseudo macro for schema bootstrap\n{% macro bootstrap_tenant(tenant_id) %}\n  -- creates analytics_{{tenant_id}} schemas and tables\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you test tenant isolation in CI?\n- What monitoring would you implement for schema drift?\n- How do you handle tenant deprovisioning?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-24",
        "questionId": "q-22343",
        "question": "In a Snowflake/dbt setup with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a cross-tenant session_quality metric pipeline that uses analytics_{{tenant}}.session_events and analytics_{{tenant}}.calibration to populate analytics_base.metrics and analytics_{{tenant}}.session_quality_by_tenant. Include 2-day late data, per-tenant masking, and an CI gate that fails if any tenant's drift exceeds 4%?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-tenant session_quality metric: compute analytics_base.metrics.session_quality using per-tenant analytics_{{tenant}}.calibration and analytics_{{tenant}}.session_events to populate analy",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a per-tenant incremental model analytics.{tenant}.daily_event_score built on staging.events, joined to seeds.tenants for multiplier. Bucket occurred_at to day, group by tenant_id, day, event_type;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement analytics_{{tenant}}.product_scd_type2 as an incremental model that uses surrogate key product_sk generated by analytics_base.hash_product(tenant, product_id). Track start_date, end_date, is",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant masking dbt flow: use a macro mask_pii(policy, field) that returns redaction, hashing, or partial masking based on mask_policy per tenant. Generate analytics_{{tenant}}.masked_a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess ability to design cross-tenant analytics without leakage, leveraging macros and lineage in dbt+Snowflake.\n\n## Key Concepts\n- Cross-tenant data modeling; per-tenant vs shared schemas\n- dbt macros to generate per-tenant models\n- Row-level security and masking; PII safeguards\n- CI gates for data drift and lineage checks\n\n## Code Example\n```javascript\n// Example placeholders showing model relationships\n```\n\n## Follow-up Questions\n- How would you test drift thresholds across tenants? \n- How to handle calibration drift backfills without reprocessing raw events?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-07T04:26:01.030Z",
    "version": 1
  }
]