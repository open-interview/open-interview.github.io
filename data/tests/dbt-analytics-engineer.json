[
  {
    "id": "test-dbt-analytics-engineer",
    "channelId": "dbt-analytics-engineer",
    "channelName": "DBT Analytics Engineer",
    "title": "DBT Analytics Engineer Knowledge Test",
    "description": "Test your understanding of DBT Analytics Engineer with 25 questions.",
    "questions": [
      {
        "id": "dbt-analytics-engineer-q-0",
        "questionId": "q-12165",
        "question": "In Snowflake with dbt, design an advanced tenant-aware drift-detection pipeline for analytics. Create a macro that auto-generates analytics_{{tenant}}.stats from staging.tenant_events, plus a per-tenant analytics.lineage entry recording hourly deltas against a gold table. Include 1-hour late data, schema drift guards, and tests (not_null on keys, unique on (tenant_id, bucket, metric)). CI should verify per-tenant views exist, drift thresholds are respected, and lineage is complete. Explain how you'd scale to thousands of tenants and handle drift without data bleed?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant anomaly pipeline from staging.tenant_events to analytics_{{tenant}}.anomalies, driven by a threshold macro resolve_anomaly_threshold(tenant) using per-tenant tier, with 1-day la",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a macro `analytics_base.validate_schema(tenant)` that reads `information_schema` for both `analytics_{{tenant}}.fct` and `analytics_base.fct`, computes a JSON diff of columns, data types, and nullabili…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a tenant-aware drift-detection flow in Snowflake/dbt. Use a macro to auto-generate analytics_{{tenant}}.stats from staging.tenant_events and populate analytics.lineage with hourly deltas agains",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create an incremental analytics.daily_engagement that counts distinct user_id per day, grouped by event_type and country_code, joining staging.events with staging.users and dimensions.countries. Allow",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to model, automate, and govern multi-tenant analytics with drift detection, runtime late data, and robust lineage.\n\n## Key Concepts\n- dbt macros for tenant provisioning\n- Snowflake drift detection and guards\n- per-tenant isolation and lineage tracking\n- CI validation of views, drift, and lineage\n\n## Code Example\n```sql\n-- macro example (pseudo)\n{% macro tenant_stats(tenant) -%}\n  analytics_{{ tenant }}.stats as (\n    select * from {{ ref('staging_tenant_events') }}\n  );\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you scale provisioning and tests as tenants scale to 10k+?\n- How would you handle late-arriving data breaking drift thresholds and alerting?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-1",
        "questionId": "q-22758",
        "question": "Design a per-tenant cost-analytics pipeline in Snowflake/dbt: analytics_{{tenant}}.cost_summary from staging.tenant_cost_events, using a shared analytics_base.cost_model and a per-tenant analytics_{{tenant}}.cost_scale calibration. Support 3-day late data; implement analytics_base.mask_pii to redact PII in cost_notes for restricted roles; enforce per-tenant RLS isolation; add a CI gate that blocks production if any tenant's cost_growth WoW exceeds 20%. Include tests not_null on keys and unique(tenant_id, period_start, cost_id); provide macro names and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an incremental per-tenant daily_revenue model analytics_{{tenant}}.daily_revenue that aggregates staging.tenant_revenue_events (tenant_id, revenue_ts, amount, currency) by day and converts all amounts to USD u…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: implement an incremental dbt model analytics_{{tenant}}.cost_summary sourcing from staging.tenant_cost_events, governed by analytics_base.cost_model with per-tenant analytics_{{tenant}}.cost_sca",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Build an incremental model analytics_{{tenant}}.daily_campaign_performance from staging.tenant_campaign_events, allowing 1-day late data. Create freshness_check() to compute per-tenant days_late; CI f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement analytics.sales.daily_user_spend as an incremental model keyed by (user_id, order_date), joining staging.orders with staging.users as needed. Compute total_spend = sum(amount) and order_count = count(*). Confi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design per-tenant cost analytics with late data handling, PII masking, RLS, and CI governance in a Snowflake/dbt setup.\n\n## Key Concepts\n\n- Incremental dbt models with tenant partitioning\n- Snowflake tasks for late-arriving data\n- Macros for PII masking\n- Row-Level Security per tenant\n- CI gates with thresholds\n\n## Code Example\n\n```sql\n-- macro: analytics_base.mask_pii(text)\nCASE WHEN is_restricted_role() THEN REGEXP_REPLACE(text, '\\\\d{4}-\\\\d{2}-\\\\d{2}', 'REDACTED') ELSE text END\n```\n\n## Follow-up Questions\n\n- How would you validate PII masking across roles?\n- How would you test CI gate coverage for tenants with uneven data volumes?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-2",
        "questionId": "q-15798",
        "question": "In dbt with Snowflake, design a beginner flow to compute analytics_{{tenant}}.daily_engagement from two sources: staging.tenant_events and staging.app_usage. Implement an incremental model that backfills 1 day for late data, enforce a data_contract macro that checks exact columns/types against analytics_base.contract, and fail CI on any drift. Include tests not_null(tenant_id, day_start, engagement_score) and unique(tenant_id, day_start). Explain how you validate per-tenant isolation in CI and handle tenants missing from the registry?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Read tenant policy from a central table, generate per-tenant schemas via a macro, and build incremental retention_summary views with 7/14/30 day buckets. Use a 1-day late data watermark; enforce isola",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a dbt macro that emits analytics_{{tenant}}.ltv_cohorts by joining staging.tenant_events and staging.tenant_revenue, aggregating by tenant_id and cohort_start (date_trunc day minus cohort_length",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Compute a per-tenant data quality score daily. Use a macro to create analytics_{{tenant}}.dq_score_daily and aggregate three components: completeness (missing critical fields), timeliness (lag vs day_",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An incremental per-tenant model analytics_{{tenant}}.daily_engagement should union staging.tenant_events and staging.app_usage by (tenant_id, day_start), backfill 1 day for late data, and apply a data",
            "isCorrect": true
          }
        ],
        "explanation": "Why This Is Asked\n- Tests foundational dbt concepts: incremental models, multi-source merging, and CI-driven validation.\n- Ensures understanding of data contracts, schema drift checks, and late-data handling in a realistic tenant-based flow.\n\nKey Concepts\n- Incremental models in dbt\n- Data contracts and drift detection\n- Late-data backfill (1 day)\n- Per-tenant isolation via schemas and a registry-driven CI check\n\nCode Example\n```sql\n-- dbt model: analytics_{{tenant}}.daily_engagement (conceptual)\nwith events as (\n  select tenant_id, date_trunc('day', event_time) as day_start, engagement_score\n  from {{ source('staging', 'tenant_events') }}\n  union all\n  select tenant_id, date_trunc('day', usage_time) as day_start, engagement_score\n  from {{ source('staging', 'app_usage') }}\n),\nagg as (\n  select tenant_id, day_start, sum(engagement_score) as engagement_score\n  from events\n  group by 1, 2\n)\nselect * from agg\n```\n\nFollow-up Questions\n- How would you prevent duplicates if late data arrives for a day already processed?\n- How would you scale this to 1000+ tenants while keeping CI fast?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-3",
        "questionId": "q-9205",
        "question": "Design a per-tenant audit-logs integrity flow in dbt with Snowflake streams. A macro creates analytics_{{tenant}} schemas; ingest staging.audit_logs via a stream into analytics_{{tenant}}.audit_summary by day, computing a SHA256 digest over tenant_id, event_id, and occurred_at. Store per-tenant hash in analytics_{{tenant}}.audit_hash and a global analytics.lineage row. 1-day late data; tests: not_null on keys, unique (tenant_id, day); digest compared to external checksum. CI validates per-tenant views and data isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: implement a single incremental model that builds analytics_{{tenant}}.daily_summary by joining staging.tenant_events on tenant_id. A macro drives per-tenant materialization from seeds.tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A central retention policy table `analytics_base.retention_policies` stores tenant, table_name, retention_days, and archive_schema. The `analytics_base.apply_retention(tenant, table)` macro moves rows older than retenti…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a per-tenant audit-logs integrity flow in dbt with Snowflake streams. A macro creates analytics_{{tenant}} schemas; ingest staging.audit_logs via a stream into analytics_{{tenant}}.audit_summa",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Build analytics.daily_metrics as an incremental Snowflake model that counts distinct user_id by day and event_type from staging.event_logs, enriched via a join to dimensions.countries on country. Upse",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess ability to design tenant‑isolated integrity checks for audit logs using dbt, Snowflake streams, and CI. \n\n## Key Concepts\n- Per-tenant macro-driven schema creation\n- Snowflake streams for CDC from staging.audit_logs\n- Day-level aggregation with SHA256 digest as tamper evidence\n- Global lineage table and per-tenant isolation checks\n\n## Code Example\n```javascript\n// Pseudo macro sketch for per-tenant audit aggregation\n{% macro build_audit_summary(tenant) -%}\n  select tenant_id, cast(occured_at as date) as day, digest('SHA256', concat(tenant_id, event_id, occured_at)) as digest\n  from analytics_{{tenant}}.audit_summary\n{%- endmacro %}\n```\n\n## Follow-up Questions\n- How would you validate the external digest source? \n- How do you handle late events or out-of-order data while preserving idempotency?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-4",
        "questionId": "q-26696",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design an automated, tenant-aware rollback and hotfix deployment strategy for breaking changes in dbt models. Requirements: (1) implement per-tenant feature flags and a safe rollback path, (2) enable blue/green deployments with analytics_base.deploy_active and analytics_base.deploy_staging, (3) ensure no cross-tenant data exposure during rollbacks, (4) idempotent CI gates that block production if any tenant fails tests after switch, (5) document minimal schema changes and tests. Include macro names and a short outline of deployment steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable per-tenant rollback by introducing tenant-scoped feature flags with a blue/green deployment path. Use the `analytics_base.is_rollback_active(tenant)` macro combined with `analytics_base.deploy_active` and `analyt…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement an incremental model analytics.{tenant}.daily_user_activity using staging.events per tenant, UTC-normalize occurred_at to date, and aggregate distinct user_id by action. Add tests: not_null",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage a tenant-scoped macro to create analytics_{{tenant}} schemas and analytics_{{tenant}}.hourly_engagement_funnel from staging.events, plus a global analytics.lineage. Implement incremental by h",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a macro create_analytics_schema(tenant) to generate analytics_{{tenant}}.quality_issues and analytics_{{tenant}}.hourly_gap_summary. hourly_gap_summary computes, for each day×hour, a gap flag by l",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Matters\nThis tests deployment discipline, tenant isolation, and rollback safety in a multi-tenant dbt/Snowflake environment, including automated gating and minimal schema changes.\n\n## Key Concepts\n- Blue/green deployment for dbt models with per-tenant isolation\n- Per-tenant feature flags enabling selective rollbacks\n- Strict isolation preventing cross-tenant data exposure during rollback operations\n- Idempotent CI gates enforcing test success across all tenants before production switches\n\n## Implementation Approach\n\n**1. Feature Flags Table**\n```sql\nCREATE TABLE analytics_base.tenant_feature_flags (\n    tenant_id      VARCHAR,\n    feature_name    VARCHAR,\n    is_enabled      BOOLEAN DEFAULT FALSE,\n    updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP(),\n    PRIMARY KEY (tenant_id, feature_name)\n);\n```\n\n**2. Macro: Check Rollback Status**\n```sql\n{% macro is_rollback_active(tenant) %}\n    {{ log('Checking rollback status for tenant: ' ~ tenant, info=True) }}\n    {% set query %}\n        SELECT is_enabled\n        FROM analytics_base.tenant_feature_flags\n        WHERE tenant_id = '{{ tenant }}'\n          AND feature_name = 'model_rollback'\n    {% endset %}\n    {% set result = run_query(query) %}\n    {{ return(result[0][0] if result | length > 0 else false) }}\n{% endmacro %}\n```\n\n**3. Blue/Green Schema Strategy**\n- `analytics_base.deploy_active` – Points to current production models\n- `analytics_base.deploy_staging` – Points to new deployment candidates\n- Schema rotation via views ensures atomic switches\n\n**4. Safe Rollback Path**\n- Toggle feature flags per tenant to route to previous schema\n- No cross-tenant exposure since each tenant references isolated schemas\n- Views maintain referential integrity during transitions\n\n**5. Idempotent CI Gate**\n```yaml\n# ci gating logic\n- name: run_tenant_tests\n  foreach: \"{{ var('tenants') }}\"\n  where: \"{{ notodules.is_rollback_active(tenant) }}\"\n  fail_fast: true\n```\n\n**6. Minimal Schema Changes**\n- Only feature flag table requires new objects\n- Views abstract schema rotation without altering underlying tables\n- Additive changes only; no destructive modifications during deployment",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-5",
        "questionId": "q-29801",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a beginner-friendly per-tenant deduplicated events pipeline. Build an incremental model analytics_{{tenant}}.events_dedup from staging.tenant_events(event_ts, user_id, event_id, event_type, country_code) that deduplicates on event_id and keeps the latest per day. Partition by date(event_ts). Use analytics_base.hash_event(event_id) to redact internal identifiers in outputs. Add not_null(user_id) and unique(tenant, date(event_ts), event_id). Propose a macro analytics_base.deduplicate_merge to perform merge and a minimal schema outline. Include a CI gate: fail prod if daily_dedup_count for any tenant on a day drops by more than 5% WoW?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant data freshness: analytics_{{tenant}}.freshness_raw aggregates per day max(event_ts) from staging.tenant_events; analytics_base.age_in_days(date_col) returns DATEDIFF(day, date_col",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement analytics_{{tenant}}.daily_actions_incremental from staging.events. Use an incremental model that aggregates actions by tenant, user, and day with a 1-day lateness window. Deduplicate by (te",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an incremental per-tenant events_dedup model from staging tenant_events, deduplicating by event_id and keeping the latest per day. Partition by date(event_ts). Redact event_id using analytics_b",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-tenant embeddings pipeline: analytics_{{tenant}}.user_embeddings derived from staging.tenant_events by applying a shared analytics_base.embeddings_model with tenant-specific privacy bu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to implement a simple, robust per-tenant deduplication flow using dbt incremental models, with explicit redaction, per-day partitioning, and minimal schema/tests. It also evaluates macro design for a reusable merge operation and a practical CI gate for data quality.\n\n## Key Concepts\n- dbt incremental models across tenants\n- de-duplication by event_id with latest per day\n- data redaction via analytics_base.hash_event\n- per-tenant constraints: not_null, unique\n- macro design for deduplicate_merge\n- basic CI gating on data quality metrics\n\n## Code Example\n```sql\n-- Skeleton macro: analytics_base.deduplicate_merge(source, target)\n{% macro analytics_base.deduplicate_merge(source, target) -%}\n-- Implementation goes here\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you test idempotence of the incremental run?\n- How would you handle late-arriving events that belong to a previous day?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-6",
        "questionId": "q-9699",
        "question": "In a Snowflake/dbt multi-tenant setup, design a beginner workflow that computes per-tenant daily_engagement by aligning events to tenant time zones. Use a macro to create analytics_{{tenant}}.daily_engagement from staging.tenant_events, convert occurred_at with tenants.time_zone, and date_trunc('day'). Allow 1-day late data. Add not_null on keys, unique on (tenant_id, day_start, user_id); populate a global analytics.lineage; CI checks that per-tenant views exist and time_zone alignment is valid against tenants. Explain cross-tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose macro analytics_base.verify_sourcedrift(tenant) that compares analytics_{{tenant}}.source_mappings to analytics_base.source_registry, returns per-tenant drift_score (mismatches/total_sources).",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement analytics_{{tenant}}.cohort_by_day as an incremental model from staging.user_events, applying analytics_base.cohort_core to compute day-0 and day-7 retention per tenant. Enforce 3-da",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To implement this, describe a macro that emits analytics_{{tenant}}.daily_engagement from staging.tenant_events, converting occurred_at to the tenant's time zone via tenants.time_zone and truncating t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "I’d implement analytics.daily_engagement as an incremental model that aggregates distinct users per date and event_type, joining dimensions.regions on region_code. Late data is allowed in a 3-day wind",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests understanding of per-tenant data modeling, time zone handling, and basic dbt testing in a multi-tenant Snowflake setup. It also touches lineage tracking and CI checks.\n\n## Key Concepts\n- Time zone aware daily aggregates\n- Macro-driven per-tenant schema generation\n- Basic integrity tests (not_null, unique)\n- Global lineage and CI validations for isolation\n\n## Code Example\n```sql\n-- Pseudo dbt macro outline\n{% macro per_tenant_daily_engagement(tenant) -%}\n  select\n    tenant_id,\n    date_trunc('day', convert_time_zone('UTC', {{ tenant.time_zone }}, occurred_at)) AS day_start,\n    user_id\n  from {{ ref('staging__tenant_events') }}\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you test cross-tenant isolation in CI?\n- How would schema drift be guarded for new tenants or time zones?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-7",
        "questionId": "q-14231",
        "question": "Design a multi-tenant observability-friendly dbt flow to compute per-tenant daily_engagement from staging.tenant_events into analytics_{{tenant}}.daily_engagement, using a macro that auto-creates per-tenant schemas, enforces 1-day late data, and applies tenant-level privacy by masking user_id for restricted roles. Include a global analytics.lineage, drift guards, tests (not_null on keys, unique on (tenant_id, day_start, user_id)), and a CI alert rule that raises when a per-tenant table is missing or drift is detected?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant daily_engagement via an incremental dbt model from staging.tenant_events to analytics_{{tenant}}.daily_engagement using a macro that auto-creates tenant schemas, masks user_id f",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I’d implement analytics_{{tenant}}.freshness by deriving latest_event_date per tenant from staging.tenant_events. Compute days_since_last_event = datediff(CURRENT_DATE, latest_event_date, DAY). Use a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I’d implement analytics.daily_engagement as an incremental model that aggregates distinct users per date and event_type, joining dimensions.regions on region_code. Late data is allowed in a 3-day wind",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a macro `analytics_base.detect_schema_drift(tenant)` that loads the analytics_{{tenant}}.schema definitions, queries information_schema.columns for analytics_{{tenant}}.facts and analytics_{{tenant}}.events, a…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes ability to design tenant-isolated, observable pipelines with macros, privacy controls, and data-quality gates. It also tests how to enforce drift monitoring and CI alerts across many tenants.\n\n## Key Concepts\n- dbt macros for tenant-scoped schema creation\n- incremental modeling with late data handling\n- row-level privacy masking and role-based access\n- data lineage, drift detection, and central alerting\n- CI checks ensuring per-tenant materializations exist and are isolated\n\n## Code Example\n```javascript\n-- dbt macro sketch (tenant_schema.sql)\n{% macro tenant_schema(tenant) %}\nanalytics_{{ tenant }}\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you test cross-tenant leakage in CI?\n- What strategies handle schema drift in this setup?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-8",
        "questionId": "q-2502",
        "question": "Design a contract-validated, multi-tenant revenue analytics flow in dbt on Snowflake. Raw events live in staging.{tenant}.events (tenant_id, user_id, event_type, amount, currency, occurred_at). Build a incremental analytics.{tenant}.daily_revenue that sums revenue per day/tenant/currency, with 2-day late data tolerance. Create contracts.tenants describing required fields and a macro that fails tests when missing fields. Add per-tenant analytics.{tenant}.users snapshot for status changes. Enforce per-tenant schemas, schema-drift guards, and tests (not_null, unique). Explain cross-tenant lineage and performance considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build a per-tenant macro that creates analytics_{{tenant}} schemas and two incremental models: analytics_{{tenant}}.daily_revenue and analytics_{{tenant}}.channel_performance from staging.tenant_event",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant incremental MERGE from staging.{tenant}.events into analytics.{tenant}.daily_revenue (key: tenant_id, date, currency) with a 2-day late data window and record_hash for idempoten",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: Create a tenant macro that reads tenants seed, creates analytics_{{tenant}}.daily_error_events and analytics.lineage per tenant, materializes per-day by day_start using occurred_at, handles 1-da",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build per-tenant analytics_{{tenant}}.daily_usage_summary with a 7-day rolling sum of sessions from staging.tenant_events, using a 1-day lag. Implement a macro enforce_contract() to compare with analy",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design contract-driven, multi-tenant dbt pipelines with late data handling and governance.\n\n## Key Concepts\n- Incremental models and MERGE in Snowflake\n- Data contracts and custom tests\n- Per-tenant schema isolation and lineage\n- Snapshots for slowly changing user state\n- Schema drift guards and test coverage\n\n## Code Example\n```sql\n-- example placeholder: actual implementation in repo\n```\n\n## Follow-up Questions\n- How would you automate contract drift detection across tenants?\n- How do you evolve contracts without breaking dashboards?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-9",
        "questionId": "q-23933",
        "question": "In a Snowflake/dbt analytics setup with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a data quality framework for analytics_{{tenant}}.metrics that detects drift against analytics_base.drift_metrics, supports 2-day late data, uses a macro analytics_base.mask_pii, enforces per-tenant RLS, and adds a CI gate that blocks production if any tenant drift exceeds a threshold unless approved. List macro names and show a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Add per-tenant feature flags gating in a Snowflake/dbt setup. Create analytics_base.feature_flags(tenant_id, flag_name, enabled). Implement analytics_base.flag_filter(sql, flag_name) to append a tenan",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Incremental model analytics_{{tenant}}.quality_score from staging.tenant_events; macro quality_score() consumes analytics_base.quality_model to produce 0–100 per tenant/day; supports 1-day late data v",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant data quality framework: detect drift via analytics_base.compute_drift(tenant, metric, window_days) comparing analytics_{{tenant}}.metrics to analytics_base.drift_metrics; handle 2-",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a macro-driven per-tenant dbt flow that reads staging.events (tenant_id, user_id, event_type, email, ip_address, occurred_at) and loads analytics_{{tenant}}.hourly_events. Enforce privacy: if p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the candidate's ability to design cross-tenant data quality controls, macros, and governance in a multi-tenant dbt/Snowflake setup. Evaluates drift detection strategy, CI gating, and data protection patterns under late data and RLS constraints.\n\n## Key Concepts\n\n- dbt macros for drift and masking\n- cross-tenant drift baselines\n- CI gating and approvals\n- late data handling and RLS\n\n## Code Example\n\n```sql\n-- pseudo macro sketch\n{% macro compute_drift(tenant, metric, window_days) %}\n-- compares current metric values to analytics_base.drift_metrics baselines\n{% endmacro %}\n```\n\n## Follow-up Questions\n\n- How would you test drift gates in CI? How to handle changing baselines?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-10",
        "questionId": "q-11370",
        "question": "NEW ANGLE: multi-tenant data quality SLA verifier in Snowflake/dbt. From staging.tenant_events to analytics_{{tenant}}.quality_summary, build a macro that computes per-tenant daily freshness (max(event_time)) and a late_data flag for data >1 day late, plus an anomaly_score from a simple z-score of daily counts. Include 1-day late data, schema drift guards, tests (not_null on keys, unique on (tenant_id, day, check_type)), a global analytics.lineage, and CI that ensures every tenant has a daily quality row and flags SLA breaches?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a dbt macro that materializes analytics_{{tenant}}.quality_summary per day. For each tenant, compute freshness = max(event_time) and late_data = datediff('day', freshness, current_timestamp(",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement an incremental dbt model analytics_{{tenant}}.daily_visits from staging.tenant_events updating with 1-day late data; deduplicate on (tenant_id, visit_id), and join a date_dim. Add tests not_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant anomaly profiling: build incremental analytics_{{tenant}}.anomaly_daily from staging.tenant_events. A macro creates per-tenant schemas, computes rolling 7-day mean and stddev for the metric",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a tenant-scoped daily_summary as an incremental analytics.{tenant}.daily_summary built from staging.events (tenant_id, user_id, event_type, occurred_at) joined to dimensions.countries, with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests macro-driven per-tenant aggregation, data freshness, simple anomaly scoring, and CI gates for SLA enforcement. It also exercises lineage tracking and drift guards.\n\n## Key Concepts\n- dbt macros for per-tenant materializations\n- Snowflake date/time functions (max, datediff, date_trunc)\n- basic z-score anomalyScore across tenants\n- tests: not_null, unique\n- global lineage table analytics.lineage\n- CI gates for daily tenant coverage and SLA violations\n\n## Code Example\n```sql\n-- simplified macro sketch\n{% macro build_quality_summary(tenant) -%}\nwith e as (\n  select event_time\n  from {{ ref('staging__tenant_events') }}\n  where tenant_id = '{{ tenant }}'\n)\nselect\n  '{{ tenant }}' as tenant_id,\n  date_trunc('day', max(event_time)) as day,\n  max(event_time) as freshness,\n  (datediff('day', max(event_time), current_timestamp()) > 1) as late_data,\n  count(*) as daily_count\nfrom e\ngroup by 1\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you scale anomaly_score computation as tenants scale?\n- How would you integrate this with alerting for SLA breaches?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-11",
        "questionId": "q-15918",
        "question": "Using Snowflake and dbt, design an incremental per-tenant anomaly pipeline: macro produces analytics_{{tenant}}.anomaly_scores from staging.tenant_events joined to analytics.tenant_config for per-tenant thresholds, using MAD-based score with dynamic cutoffs. Persist (tenant_id, day, score). Include 2-day late data, schema-drift guards, tests (not_null, unique). Global lineage; CI validates all tenant schemas exist and no cross-tenant bleed?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline an incremental, per-tenant cohort funnel with differential privacy. Use a macro to generate analytics_{{tenant}}.cohort_funnel and a per-tenant epsilon budget tracked in analytics_privacy; app",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a macro-driven per-tenant data quality framework: automatically generate drift and null-rate checks for analytics_{{tenant}}.hourly_engagement, feed results into a global analytics.data_qualit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an incremental model that materializes analytics_{{tenant}}.hourly_anomalies from staging.tenant_events, joining analytics_{{tenant}}.thresholds refreshed nightly from analytics_base.global_",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant incremental pipeline using a dbt macro that produces analytics_{{tenant}}.anomaly_scores. The macro joins staging.tenant_events to analytics.tenant_config to retrieve per-tenant thresholds, then comp…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to implement per-tenant, configurable analytics with robust data governance, dynamic thresholds, and operational discipline.\n\n## Key Concepts\n- **dbt macros** for tenant-scoped model generation\n- **Per-tenant thresholds** via analytics.tenant_config table\n- **MAD-based anomaly scoring** with dynamic cutoffs for adaptive detection\n- **2-day late data window** to accommodate delayed event processing\n- **Schema-drift guards** to detect and handle unexpected schema changes\n- **Lineage tracking** and CI data-isolation validation\n\n## Code Example\n```sql\n-- dbt macro for per-tenant anomaly scores\n{% macro generate_anomaly_scores(tenant_id) %}\n\n{{ config(\n    materialized='incremental',\n    unique_key='(tenant_id, day)',\n    incremental_strategy='merge',\n    cluster_by=['tenant_id', 'day']\n) }}\n\nWITH tenant_config AS (\n    SELECT threshold, mad_multiplier, late_data_days\n    FROM {{ ref('analytics__tenant_config') }}\n    WHERE tenant_id = '{{ tenant_id }}'\n),\n\nlate_data_window AS (\n    SELECT * FROM {{ ref('staging__tenant_events') }}\n    WHERE tenant_id = '{{ tenant_id }}'\n    AND event_date >= DATEADD(day, -2, CURRENT_DATE())\n),\n\nenriched_events AS (\n    SELECT \n        e.tenant_id,\n        e.day,\n        e.value,\n        PERCENTILE_CONT(e.value) WITHIN GROUP (ORDER BY e.value) \n            OVER (PARTITION BY e.tenant_id) AS median_value\n    FROM late_data_window e\n),\n\nscored AS (\n    SELECT \n        tenant_id,\n        day,\n        ABS(value - median_value) / NULLIF(AVG(ABS(value - median_value)) \n            OVER (PARTITION BY tenant_id), 0) AS mad_score\n    FROM enriched_events\n)\n\nSELECT \n    tenant_id,\n    day,\n    mad_score AS score\nFROM scored\nWHERE mad_score > (SELECT mad_multiplier FROM tenant_config)\n\n{% endmacro %}\n```\n\n## Testing Strategy\n- **not_null** test on tenant_id and day\n- **unique** test on (tenant_id, day) composite key\n- **CI validation**: Verify all tenant schemas exist and no cross-tenant data leakage occurs",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-12",
        "questionId": "q-25004",
        "question": "In a Snowflake/dbt setup with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a beginner-friendly plan to add a daily rollup analytics_{{tenant}}.daily_sales_rollup derived from analytics_{{tenant}}.sales and analytics_base.sales_facts. Requirements: (1) a macro analytics_base.normalize_date(dt) that returns the date at midnight; (2) a per-tenant incremental model analytics_{{tenant}}.daily_sales_rollup computing daily totals and upserting to the rollup; (3) tests: not_null on the rollup key, unique on (tenant_id, rollup_date); (4) a CI gate that blocks prod if any tenant's WoW daily total delta exceeds 5%; (5) a minimal schema outline. Include macro names and a rough schema?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an incremental per-tenant data-quality pipeline in Snowflake/dbt that derives analytics_{{tenant}}.quality_score from staging.tenant_logs using analytics_base.quality_model and analytics_{{tenant}}.thresholds. Th…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Plan: Implement a per-tenant daily rollup model** `analytics_{{tenant}}.daily_sales_rollup` is derived from `analytics_{{tenant}}.sales` and `analytics_base.sales_facts`. 1. **Macro**: `analytics_base.normalize_date(d…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a per-tenant, on-demand export path that materializes analytics_export.{tenant}.daily_export for a specified date range. Describe a macro-driven Snowflake model that creates daily export table",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a per-tenant data-quality and lineage validator that uses Snowflake streams on analytics_{{tenant}}.quality, sourced from staging.tenant_events, with a shared analytics_base.mappings for cross",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis assesses hands-on ability to extend a dbt project with a complete daily rollup implementation, covering macros, per-tenant data isolation, incremental modeling, tests, and CI quality gates. It tests understanding of multi-tenant architecture patterns and production-ready data pipeline design.\n\n## Key Concepts\n\n- **dbt incremental models** with upsert strategies for efficient data processing\n- **Macro design** for reusability across tenants\n- **Per-tenant data isolation** with shared base schemas\n- **CI data-quality gates** for anomaly detection\n\n## Testing Notes\n\nAdd the following to your schema.yml for the rollup model:\n\n```yaml\nmodels:\n  - name: daily_sales_rollup\n    tests:\n      - dbt_utils.recency:\n          datepart: day\n          field: rollup_date\n          interval: 1\n      - dbt_utils.unique_combination_of_columns:\n          combination_of_columns:\n            - tenant_id\n            - rollup_date\n```",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-13",
        "questionId": "q-2403",
        "question": "Beginner dbt task: from staging.events (event_id, user_id, event_type, occurred_at, region_code, delete_flag) and dimensions.regions, implement an incremental model analytics.daily_event_summary that counts distinct users per day, event_type, country_code (via region_code). Exclude deleted events by default; provide a macro to toggle inclusion for audits. Add tests not_null(event_id, occurred_at) and unique(event_id)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement analytics.daily_event_summary as an incremental Snowflake model: aggregate date_trunc('day', occurred_at), event_type, country_code (via regions) with count(distinct user_id). Use a macro de",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose an analytics_base.run_metrics table keyed by (tenant, run_id, started_at) and a macro analytics_base.emit_run_metrics(tenant, run_id, metrics_json) that inserts a JSON payload and updates a me",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design and implement a comprehensive three-region multi-tenant analytics architecture in Snowflake using dbt. Develop a dynamic macro that systematically generates analytics_{{tenant}} tables across us_east, eu_west, an…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose macro analytics_base.verify_sourcedrift(tenant) that compares analytics_{{tenant}}.source_mappings to analytics_base.source_registry, returns per-tenant drift_score (mismatches/total_sources).",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests a candidate's ability to implement a simple, robust incremental model with data quality checks and a governance hook (macro) for audits. Emphasizes joins, aggregation, and proper filtering.\n\n## Key Concepts\n- Incremental modeling in dbt\n- Basic joins to enrich with dimensions.regions\n- Simple data governance via macros for optional audit paths\n- Tests: not_null and unique\n\n## Code Example\n```sql\n-- analytics/daily_event_summary.sql\nwith src as (\n  select\n    date_trunc('day', occurred_at) as day,\n    e.event_type,\n    r.country_code,\n    count(distinct e.user_id) as active_users\n  from {{ source('staging','events') }} e\n  join {{ ref('dimensions_regions') }} r on e.region_code = r.region_code\n  where {{ delete_filter(false) }}\n  group by 1,2,3\n)\nselect * from src;\n```\n\n```sql\n-- macros/delete_filter.sql\n{% macro delete_filter(include_deleted=false) -%}\n  {% if include_deleted %}\n    1=1\n  {% else %}\n    delete_flag = false\n  {% endif %}\n{%- endmacro %}\n```\n\n## Follow-up Questions\n- How would you test the macro across multiple models?\n- How would you adapt this to handle late-arriving updates in staging?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-14",
        "questionId": "q-22887",
        "question": "Design an advanced per-tenant observability and rollback flow in Snowflake/dbt: build analytics_{{tenant}}.observability that records daily count_diff and anomaly_score using a shared analytics_base.observability_model with per-tenant thresholds in analytics_{{tenant}}.thresholds. Ingest 1-day late data, apply per-tenant masking via analytics_base.mask_pii, and enforce RLS. A CI gate must block production if any tenant’s drift exceeds 25% for 3 consecutive days. Include tests not_null on keys and unique(tenant_id, day, metric_id). Include macro names and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a multi-tenant dbt/Snowflake approach for schema evolution: tenants drop new fields into staging_{{tenant}}.events; use analytics_base.mappings to drive per-tenant analytics_{{tenant}}.facts c",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant Snowflake streams on staging.tenant_events and an incremental analytics_{{tenant}}.dq_status(tenant, table_name, row_count, freshness_ts, lag_min, dq_score). Add analytics_base.lineage_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a dbt macro to generate analytics_{{tenant}}.quality_score and analytics_{{tenant}}.quality_facts, computed by a Python UDF that blends data completeness, timeliness, and anomaly flags. Backfill 1",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant observability/rollback flow: create analytics_{{tenant}}.observability with daily count_diff and anomaly_score using a shared analytics_base.observability_model and per-tenant thre",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAdvanced data governance and reliability require not just correct transformations but proactive health monitoring and safe rollback paths across tenants. This question tests end-to-end design: per-tenant observability, late data handling, privacy controls, access governance, and production safeguards.\n\n## Key Concepts\n- Per-tenant thresholds and models\n- Observability metrics and anomaly scoring\n- Data latency handling and masking\n- Row-level security and CI gates for drift-based rollbacks\n- Tests ensuring data integrity (not_null, unique constraints)\n\n## Code Example\n```sql\n-- macro: analytics_base.mask_pii\n-- Applies masking rules based on role/sessioncontext\n```\n\n## Follow-up Questions\n- How would you implement a rollback strategy when drift is detected?\n- How to simulate tenant-specific failures in CI for safe gating?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-15",
        "questionId": "q-21835",
        "question": "Design a beginner dbt incremental model analytics_user_daily_summary that consumes staging.web_events and staging.users to produce a daily summary by event_type. Use a macro to normalize event_ts to UTC-day, implement 1-day late data, and materialize incrementally by day. Include tests not_null(day, event_type, users) and unique(day, event_type). Describe how you would validate correctness in CI and how you would handle late data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a macro to materialize analytics_{{tenant}}.daily_revenue by incrementally merging staging.sales and staging.refunds per day. Build a global analytics.lineage to track dependencies. Backfill with tenant-specific dat…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would add a macro normalize_day(ts) returning date_trunc('day', ts AT TIME ZONE 'UTC'). Build analytics_user_daily_summary as incremental by day from staging.web_events joined to staging.users, grou",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a per-tenant drift observability macro analytics_base.ensure_schema_consistency to compare analytics_{{tenant}}.staging against analytics_{{tenant}}.facts after the nightly load. Measure drift on",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a dbt macro to generate analytics_{{tenant}}.quality_score and analytics_{{tenant}}.quality_facts, computed by a Python UDF that blends data completeness, timeliness, and anomaly flags. Backfill 1",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests a beginner dbt workflow focusing on incremental modeling, simple macros, and basic data quality checks.\n\n## Key Concepts\n- Incremental models by day\n- Macros for date normalization\n- not_null and unique tests\n- Simple CI gate for late data\n\n## Code Example\n```sql\n-- macros/normalize_day.sql\n{% macro normalize_day(field) %}\n  date_trunc('day', {{ field }}::timestamp_ntz at time zone 'UTC')\n{% endmacro %}\n```\n\n```sql\n-- models/analytics_user_daily_summary.sql\nwith src as (\n  select\n    {{ normalize_day('e.event_ts') }} as day,\n    e.event_type,\n    e.user_id\n  from {{ ref('staging.web_events') }} e\n  join {{ ref('staging.users') }} u on u.user_id = e.user_id\n)\nselect day, event_type, count(distinct user_id) as users\nfrom src\ngroup by day, event_type\n```\n\n## Follow-up Questions\n- How would you adapt this for timezone changes or DST?\n- What edge cases occur if events have null event_ts or duplicate user_id-event_type-day rows?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-16",
        "questionId": "q-11195",
        "question": "NEW ANGLE: Tenant-aware data quality auditor. In Snowflake/dbt, design a beginner flow that creates analytics_{{tenant}}.week_visit_quality from staging.tenant_visits. Use a macro to auto-create per-tenant schemas and the quality table, compute week_start via date_trunc, aggregate week counts, and compare to prior week; flag anomalies when the delta exceeds 25%. Support 1-day late data, include schema-drift guards, tests (not_null on tenant_id, week_start; unique on (tenant_id, week_start)); add global analytics.lineage and CI to verify per-tenant tables exist and data is isolated. Explain how you would validate cross-tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant hourly metrics by materializing analytics_{{tenant}}.hourly_metrics from staging.tenant_events_hourly using analytics_base.metrics_model; per-tenant config analytics_{{tenant}}.me",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a dbt macro create_tenant_quality(tenant) that creates analytics_{{tenant}}.week_visit_quality from staging.tenant_visits, compute week_start = date_trunc('week', occurred_at), aggregate counts",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design and implement a comprehensive three-region multi-tenant analytics architecture in Snowflake using dbt. Develop a dynamic macro that systematically generates analytics_{{tenant}} tables across us_east, eu_west, an…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a tenant-scoped daily_summary as an incremental analytics.{tenant}.daily_summary built from staging.events (tenant_id, user_id, event_type, occurred_at) joined to dimensions.countries, with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests macro-driven multi-tenant data quality, per-tenant schemas, weekly rollups, simple anomaly detection, late data handling, lineage, and CI across tenants.\n\n## Key Concepts\n- dbt macros for per-tenant objects\n- Snowflake date_trunc week\n- not_null/unique tests\n- lineage and multi-tenant isolation\n- CI checks for existence and isolation\n\n## Code Example\n```sql\n{% macro create_tenant_quality(tenant) %}\n-- Creates analytics_{{tenant}}.week_visit_quality from staging.tenant_visits\n-- with week_start and weekly counts, plus anomaly flag logic (not shown)\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you validate cross-tenant data leakage in CI?\n- How would you scale this approach to hundreds of tenants?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-17",
        "questionId": "q-10095",
        "question": "Design a beginner dbt flow to compute per-tenant daily revenue from staging.sales_events (tenant_id, sale_date, amount) into analytics_{{tenant}}.daily_revenue. Implement a macro that auto-creates per-tenant schemas, aggregates by day, and supports 1-day late data. Include schema-drift guards, tests: not_null on keys and unique on (tenant_id, day). Add a global analytics.lineage and CI that verifies per-tenant views exist for all seeds.tenants and enforces data isolation. Explain cross-tenant isolation considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant daily revenue in Snowflake/dbt with a macro that auto-creates analytics_{{tenant}}.daily_revenue and aggregates staging.sales_events (tenant_id, sale_date, amount) by day. Support 1-day lat",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a per-tenant freshness SLA using a dbt macro that emits a Snowflake Task per tenant to populate analytics_{{tenant}}.facts hourly from staging.tenant_events. Store per-tenant lag in metadata.te",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant schemas in Snowflake: implement analytics.daily_metrics as an incremental model scoped to analytics.${tenant}. daily by (day, tenant_id, event_type). Late data window: 2 days; use MERGE int",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To implement, use an idempotent SCD2 pattern: end_date on changed rows, insert a new row with current event_ts as effective_date; rely on analytics_base.scd2_merge(tenant, staging.users, analytics_{{t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question adds a revenue-centric, per-tenant pattern not covered, emphasizing practical macro generation, late data handling, and data isolation.\n\n## Key Concepts\n- dbt macros for per-tenant object creation\n- Snowflake tenant-scoped schemas and isolation\n- Tests for not_null and uniqueness\n- Global lineage tracking and CI validation\n\n## Code Example\n```sql\n{% macro per_tenant_revenue_sql(tenant) %}\nselect\n  '{{ tenant }}' as tenant_id,\n  date_trunc('day', sale_date) as day,\n  sum(amount) as revenue\nfrom {{ ref('staging_sales_events') }}\nwhere tenant_id = '{{ tenant }}'\ngroup by 1, 2\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you implement drift detection across tenants?\n- How would you scale to 100+ tenants and ensure timely data archival?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-18",
        "questionId": "q-13473",
        "question": "In Snowflake/dbt, design a tenant-scoped data quality contracts engine. For each tenant in seeds.tenants create analytics_{{tenant}}.quality_contracts with row_count, distinct_user_ids, null_user_ids, and SLA_target. Use a macro to materialize per-tenant quality tables from staging.tenant_events, allow 1-day late data, add schema-drift guards, and tests (not_null on keys, unique on (tenant_id, day, contract_id)). Include global analytics.lineage and CI to verify every tenant contract exists and drift is flagged?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed: implement qa_latency_ri to compute max delay between ingest_ts and event_ts using QUERY_HISTORY; build analytics_{{tenant}}.qa_summary incrementally (tenant, src_table, dim_table, latency_da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a per-tenant data-quality and lineage validator that uses Snowflake streams on analytics_{{tenant}}.quality, sourced from staging.tenant_events, with a shared analytics_base.mappings for cross",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a seeds-driven macro looping tenants to create daily analytics_{{tenant}}.quality_contracts from staging_tenant_events. Track row_count, distinct_user_ids, null_user_ids, and SLA_target per day. P",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Describe a per-tenant-per-region dbt flow creating schemas analytics.{tenant}_{region} with an incremental hourly_metrics model over staging.events(tenant_id, region_id, user_id, event_type, occurred_",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nHelps assess ability to automate multi-tenant data quality with dbt macros, seeds, and lineage.\n\n## Key Concepts\n- Seeds-driven macro expansion across tenants\n- Per-tenant quality contracts and SLA tracking\n- 1-day late data handling with watermark\n- Schema-drift guards and tests for integrity\n- Global lineage and CI validation\n\n## Code Example\n```sql\n{% macro quality_contracts_for_tenant(tenant_id) -%}\n  -- creates analytics_{{{{tenant_id}}}}.quality_contracts and seeds metrics\n{%- endmacro %}\n```\n\n## Follow-up Questions\n- How would you scale this to thousands of tenants and handle churn?\n- How would you test drift across environments and alert on SLA breaches?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-19",
        "questionId": "q-23091",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a practical plan to validate slowly changing dimensions for analytics_{{tenant}}.dims using dbt snapshot. Include a 1-day late data window, a macro analytics_base.mask_pii to redact PII, and per-tenant RLS isolation. Propose a CI gate that blocks production if any tenant's dimension history loses continuity for more than a day. Include tests and macro names?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: build a per-tenant data-quality framework on analytics_base.quality. The macro validate_freshness(tenant, upstream, downstream) compares max(event_ts) per tenant across staging and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a dbt snapshot on analytics_{{tenant}}.dims to manage slowly changing dimensions, backed by analytics_base.dim_versions. Enable a 1-day late data window, and route pii fields through analytics_bas",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a macro-driven per-tenant weekly forecast that merges staging.tenant_events with analytics_base.forecast_base and analytics_{{tenant}}.calibration to produce analytics_{{tenant}}.weekly_foreca",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement analytics_{{tenant}}.daily_actions_incremental from staging.events. Use an incremental model that aggregates actions by tenant, user, and day with a 1-day lateness window. Deduplicate by (te",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design SCD validation with per-tenant isolation and lineage checks, using dbt snapshot and a shared base, plus CI governance.\n\n## Key Concepts\n- dbt snapshot and SCD types\n- late data window handling\n- per-tenant RLS and pii masking\n- lineage validation and CI gates\n- test coverage for keys and relations\n\n## Code Example\n\n```javascript\n// Example: macro usage for mask\nanalytics_base.mask_pii('cost_notes', 'restricted')\n```\n\n## Follow-up Questions\n- How would you monitor snapshot failures in production?\n- How would you handle tenant onboarding changes?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-20",
        "questionId": "q-13011",
        "question": "NEW ANGLE: Per-tenant anomaly detection. Given staging.tenant_events (tenant_id, event_type, occurred_at, count) and a tenants registry, design a beginner dbt flow that uses a macro to materialize analytics_{{tenant}}.anomaly_daily (tenant_id, day_start, event_type, is_anomaly). Define anomaly as daily_count > 2x 7-day moving average. Include 1-day late data, schema-drift guards, not_null and unique tests, a global analytics.lineage, and CI that verifies per-tenant tables exist and data is isolated?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a dbt macro that iterates all tenants to materialize analytics_{{tenant}}.anomaly_daily, deriving day_start from occurred_at and computing a 7-day moving average per tenant/event_type. Flag is",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a per-tenant SLA model: create analytics_{{tenant}}.slas updated daily via incremental dbt from analytics_{{tenant}}.events, computing 95th percentile latency against analytics_base.latency_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest per-tenant events, dedupe by tenant_id/event_id, aggregate daily counts by action_type and total_duration, and map action_type to category via a macro and analytics_base.event_definitions. Impl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant daily revenue in Snowflake/dbt with a macro that auto-creates analytics_{{tenant}}.daily_revenue and aggregates staging.sales_events (tenant_id, sale_date, amount) by day. Support 1-day lat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a beginner dbt flow around per-tenant anomaly detection, focusing on incremental modeling, validation, and lineage.\n\n## Key Concepts\n- Macro-driven per-tenant objects\n- Windowed aggregations (7-day MA)\n- Data quality tests (not_null, unique)\n- Global lineage tracking\n- CI validation for isolation and completeness\n\n## Code Example\n```sql\n-- dbt macro pseudo-skeleton (sql)\n{% macro build_anomaly(tenant) %}\n  select\n    tenant_id,\n    date_trunc('day', occurred_at) as day_start,\n    event_type,\n    count(*) as daily_count,\n    case when count(*) > 2 * avg(count(*)) over (partition by tenant_id, event_type order by day_start rows between 6 days preceding and current row) then true else false end as is_anomaly\n  from {{ ref('staging__tenant_events') }}\n  where tenant_id = '{{ tenant }}'\n  group by tenant_id, day_start, event_type\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you unit test the macro for a subset of tenants without deploying all tenants?\n- What changes would you make to accommodate tenants with irregular data cadence or missing days?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-21",
        "questionId": "q-2442",
        "question": "In a dbt project, given staging.events(user_id, event_type, occurred_at, country_code) and staging.users(user_id, created_at, country_code), implement an incremental model analytics.daily_engagement that counts distinct users per day by event_type and country_code, enriched by dimensions.countries. Include 1-day late data tolerance, schema-drift guards, tests (not_null, unique), and a snapshot on analytics.users to track country changes. Describe lineage and isolation notes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create an incremental analytics.daily_engagement that counts distinct user_id per day, grouped by event_type and country_code, joining staging.events with staging.users and dimensions.countries. Allow",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a per-tenant incremental model: analytics_{{tenant}}.daily_engagement from staging.tenant_events with a 1-day lag. Add a contract macro to validate columns against analytics_base.contract, p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant governance check by introducing a macro analytics_base.validate_lineage that parses dbt manifest and ensures no unapproved cross-tenant dependencies are added. Build analytics_{",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant daily engagement quality score in Snowflake/dbt. Build an incremental analytics_{{tenant}}.engagement_score from staging.tenant_events (tenant_id, user_id, action, occurred_at) wit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess ability to design a beginner-friendly incremental model, enforce data quality, and capture slowly changing dimensions with snapshots.\n\n## Key Concepts\n\n- Incremental modeling in dbt for daily aggregates\n- Data quality tests: not_null and unique\n- Late-arriving data handling (1-day tolerance)\n- Snapshot usage for slowly changing dimensions\n- Referential integrity with dimensions.countries and cross-model lineage\n\n## Code Example\n\n```sql\nwith src as (\n  select\n    e.user_id,\n    date( e.occurred_at ) as day,\n    e.event_type,\n    e.country_code\n  from {{ ref('staging_events') }} e\n)\nselect\n  day,\n  event_type,\n  country_code,\n  count(distinct user_id) as user_cnt\nfrom src\ngroup by 1,2,3\n```\n\n## Follow-up Questions\n\n- How would you test that country_code exists in dimensions.countries?\n- How would you incorporate a 1-day late data window into your model without double-counting users?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-22",
        "questionId": "q-27176",
        "question": "Design a per-tenant data quality gate in Snowflake/dbt for analytics_{{tenant}}.fact_sales that tolerates a 2-day late arrival and enforces referential integrity to analytics_{{tenant}}.dim_products and analytics_{{tenant}}.dim_customers. Propose (1) a macro analytics_base.verify_fk(tenant, child_table, fk_cols, parent_table) to detect orphans across tenants, (2) per-tenant analytics_{{tenant}}.quality_sales with a rolling 7-day quality_score, (3) tests not_null on keys and orphan_free FK test, (4) a CI gate blocking production if any tenant's orphaned_rate > 1% or quality_score < 0.95. Include macro names and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Define analytics_base.dq_gate(tenant) that returns: customer_id IS NOT NULL AND order_total >= 0 AND currency_code IN (SELECT code FROM analytics_base.currency_dim). Create a dbt test on analytics_{{t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant quality pass with a shared analytics_base macro analytics_base.verify_fk that scans analytics_{{tenant}}.fact_sales for orphaned product_id or customer_id across tenants, emits",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a per-tenant masking dbt flow: use a macro mask_pii(policy, field) that returns redaction, hashing, or partial masking based on mask_policy per tenant. Generate analytics_{{tenant}}.masked_a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant drift observability macro analytics_base.ensure_schema_consistency to compare analytics_{{tenant}}.staging against analytics_{{tenant}}.facts after the nightly load. Measure drift on",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nPractical data quality governance in a multi-tenant dbt/Snowflake setup, focusing on late data, referential integrity across tenants, and a production guard.\n\n## Key Concepts\n- per-tenant data quality gates\n- macro-based FK verification across tenants\n- rolling quality_scores and CI gates\n- late-arrival tolerance\n\n## Code Example\n```sql\n-- macro signature sketch\n{% macro analytics_base.verify_fk(tenant, child_table, fk_cols, parent_table) %}\n  -- implement FK checks across tenant schemas\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you adapt if late data arrives after 2 days?\n- How would you visualize cross-tenant quality metrics?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-23",
        "questionId": "q-16993",
        "question": "Design a beginner-friendly daily revenue flow: from staging.sales_events(order_date, amount, country_code) to analytics.daily_revenue(date, country_code, revenue) using a simple incremental model with a 1-day late data window. Include a macro that validates country_code against a seeded countries table and a CI check that flags any new country_code not present in countries. How would you implement and test this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a dbt macro compute_anomaly(tenant) that queries staging.tenant_events, computes rolling mean and stddev(metric_value) over the previous 7 days per tenant, and emits analytics_{{tena",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-tenant drift observability macro analytics_base.ensure_schema_consistency to compare analytics_{{tenant}}.staging against analytics_{{tenant}}.facts after the nightly load. Measure drift on",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Incrementally populate analytics.daily_revenue by date and country_code from staging.sales_events(amount). The macro validates country_code against the seeded countries table and fails CI if a new cod",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "I would implement a macro that generates analytics_{{tenant}}.daily_summary by selecting tenant_id, date_trunc('day', occurred_at) as day, event_type, count(*) as cnt from staging.tenant_events where",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical dbt skills: incremental modeling, seeds, and custom macros. It focuses on data quality governance with a simple country_code contract and CI checks appropriate for a beginner.\n\n## Key Concepts\n- Incremental models and late-arriving data\n- Macros to enforce data contracts\n- Seeds and cross-table validation in CI\n- Basic tests: not_null and unique\n\n## Code Example\n```javascript\n// pseudo snippet: macro enforces country_code in countries seed\n{% macro assert_country_in_seed(country_code) %}\n  {% set valid = (return value of select country_code from {{ ref('countries_seed') }}) %}\n  {{ if not loop over valid %} {{ exceptions }} {% endif %}\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you adapt this for multiple regions or a dynamic country list?\n- What would you add to handle out-of-order data in the 1-day window?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-24",
        "questionId": "q-23711",
        "question": "Design a per-tenant data-quality and lineage validator in Snowflake/dbt: analytics_{{tenant}}.quality fed from staging.tenant_events, with analytics_base.mappings for cross-tenant lineage. Include a 2-day late data window, per-tenant masking, and an RLS policy. Propose macro analytics_base.build_qc and tests not_null(keys) and unique(tenant_id, day, qc_id). CI gates production if any tenant’s quality_score drifts >5% for two consecutive days?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design per-tenant dau incrementally from staging.tenant_events with 1-day lag, dedup by (tenant_id, user_id, day). Macro enforce_minimum_volume() blocks tenants with dau < 50. Tests: not_null(tenant_i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an incremental model that materializes analytics_{{tenant}}.hourly_anomalies from staging.tenant_events, joining analytics_{{tenant}}.thresholds refreshed nightly from analytics_base.global_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a per-tenant data-quality and lineage validator that uses Snowflake streams on analytics_{{tenant}}.quality, sourced from staging.tenant_events, with a shared analytics_base.mappings for cross",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Outline an on-demand repair for late-arriving events up to 3 days. Use a per-tenant MERGE from staging.tenant_events into analytics_{{tenant}}.facts with an idempotent key; implement via analytics_bas",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design data quality and lineage checks in a multi-tenant dbt/Snowflake setup, focusing on late data, masking, RLS, and governance.\n\n## Key Concepts\n- Snowflake streams, dbt tests, and macros\n- Per-tenant lineage with analytics_base.mappings\n- Data quality metrics and drift thresholds\n- CI gates and alerting\n\n## Code Example\n```sql\n-- macro: analytics_base.build_qc\n{% macro build_qc(tenant) %}\nselect\n  '{{tenant}}' as tenant_id,\n  count(*) as total_checks,\n  sum(case when status = 'pass' then 1 else 0 end) as passes\nfrom {{ ref('quality_results') }}\nwhere tenant_id = '{{ tenant }}';\n{% endmacro %}\n```\n\n## Follow-up Questions\n- What metrics would you track to monitor validator efficacy?\n- How would you test macro idempotence across tenants with varying schemas?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-05T10:42:16.410Z",
    "version": 1
  }
]