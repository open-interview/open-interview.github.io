[
  {
    "id": "test-dbt-analytics-engineer",
    "channelId": "dbt-analytics-engineer",
    "channelName": "DBT Analytics Engineer",
    "title": "DBT Analytics Engineer Knowledge Test",
    "description": "Test your understanding of DBT Analytics Engineer with 25 questions.",
    "questions": [
      {
        "id": "dbt-analytics-engineer-q-0",
        "questionId": "q-8429",
        "question": "In Snowflake with dbt, given staging.events(tenant_id, user_id, event, occurred_at) and seeds/tenants(tenant_id, time_zone), design a beginner dbt flow that builds analytics_{{tenant}}.daily_engagement using occurred_at converted to tenant local date via a macro that creates analytics_{{tenant}} schemas and a global analytics.lineage. Include 1-day late data, schema-drift guards, tests not_null on keys, unique (tenant_id, day_start, user_id), and a CI check that per-tenant analytics exist and no cross-tenant bleed in time zones. Explain how you'd validate isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I'll design a tenant-aware dbt flow that builds per-tenant analytics with proper time zone conversion and isolation safeguards. The solution uses a macro that loops through active tenants, creates dedicated analytics_{{…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement a macro analytics_base.column_drift(tenant, table) that queries information_schema.columns and compares column_name, data_type, and is_nullable against analytics_base.transaction_reg",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a dbt macro to generate analytics_{{tenant}}.funnel_daily from staging.tenant_events, masking user_id for roles lacking FULL_ACCESS using a Snowflake policy plus deterministic hashing for jo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose macro analytics_base.verify_sourcedrift(tenant) that compares analytics_{{tenant}}.source_mappings to analytics_base.source_registry, returns per-tenant drift_score (mismatches/total_sources).",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests critical skills in multi-tenant data architecture: time zone handling, dynamic modeling, and isolation validation. It's essential for SaaS companies where tenant data must remain properly segmented while supporting local time contexts.\n\n## Key Concepts\n\n- Per-tenant time zone conversion using CONVERT_TIMEZONE\n- Macro-driven schema generation (analytics_{{tenant}}.*)\n- Schema drift protection with data type guards\n- Global lineage tracking with tenant metadata\n- CI validation for tenant parity and isolation\n\n## Code Example\n\n```sql\n{% macro tenant_model(tenant_id, time_zone) %}\nSELECT\n  '{{ tenant_id }}' AS tenant_id,\n  user_id,\n  DATE_TRUNC('day', CONVERT_TIMEZONE('{{ time_zone }}', occurred_at)) AS day_start,\n  event\nFROM {{ ref('staging_events') }}\nWHERE tenant_id = '{{ tenant_id }}'\n  AND occurred_at >= DATEADD(day, -1, CURRENT_TIMESTAMP())\n{% endmacro %}\n```\n\n## Validation Approach\n\nValidate isolation through: 1) Per-tenant schema separation ensuring data segregation, 2) Time zone conversion testing confirming accurate local date mapping, 3) Unique constraint verification preventing cross-tenant contamination, and 4) CI checks validating tenant completeness and schema consistency across all active tenants.",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-1",
        "questionId": "q-11566",
        "question": "Design a per-tenant analytics flow in Snowflake with dbt that derives per-tenant channel and campaign dimensions from a VARIANT attributes column in staging.tenant_events, building analytics_{{tenant}}.daily_revenue and analytics_{{tenant}}.channel_performance via a macro that creates per-tenant schemas and a global analytics.lineage. Include 1-day late data, schema drift guards, tests (not_null on keys; unique on (tenant_id, day_start, channel_id)), and CI checks that per-tenant artifacts exist and data is isolated. Explain how you model JSON attributes and enforce cross-tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant SLA model: create analytics_{{tenant}}.slas updated daily via incremental dbt from analytics_{{tenant}}.events, computing 95th percentile latency against analytics_base.latency_",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose staging per domain, a single grain, incremental models with a 2-day late-arrival window, a surrogate key like domain_key + occurred_at + id for cross-domain joins, a macro-based cross-domain r",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement analytics.sales.daily_user_spend as an incremental model keyed by (user_id, order_date), joining staging.orders with staging.users as needed. Compute total_spend = sum(amount) and order_count = count(*). Confi…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a per-tenant macro that creates analytics_{{tenant}} schemas and two incremental models: analytics_{{tenant}}.daily_revenue and analytics_{{tenant}}.channel_performance from staging.tenant_event",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design tenant-scoped data products using dbt+Snowflake, including JSON extraction, lineage tracking, and isolation controls.\n\n## Key Concepts\n- dbt macros for per-tenant schema creation\n- JSON/VARIANT data extraction in Snowflake\n- incremental models with late-arriving data\n- schema drift guards and lineage tracking\n\n## Code Example\n```sql\n-- macro: create_tenant_schema.sql\nCREATE OR REPLACE SCHEMA analytics_{{tenant}};\n-- model: daily_revenue.sql\nSELECT tenant_id, CAST(CURRENT_DATE - 1 AS DATE) AS day_start,\n       SUM(amount) AS revenue\nFROM {{ ref('staging__tenant_events') }}\nWHERE action = 'purchase'\nGROUP BY tenant_id, day_start;\n```\n\n## Follow-up Questions\n- How would you test cross-tenant data bleed in CI?\n- How would you extend to add per-tenant currency normalization?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-2",
        "questionId": "q-29801",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a beginner-friendly per-tenant deduplicated events pipeline. Build an incremental model analytics_{{tenant}}.events_dedup from staging.tenant_events(event_ts, user_id, event_id, event_type, country_code) that deduplicates on event_id and keeps the latest per day. Partition by date(event_ts). Use analytics_base.hash_event(event_id) to redact internal identifiers in outputs. Add not_null(user_id) and unique(tenant, date(event_ts), event_id). Propose a macro analytics_base.deduplicate_merge to perform merge and a minimal schema outline. Include a CI gate: fail prod if daily_dedup_count for any tenant on a day drops by more than 5% WoW?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a per-tenant adaptive path: read analytics_{{tenant}}.config.velocity and pick a MERGE upsert when velocity exceeds a threshold, else a delta full-refresh. Use a macro to route tenan",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a macro `analytics_base.quality_signal(tenant_id, table)` that returns a 0-100 composite score derived from three signals: null ratio across key columns, FK integrity against `analytics_base.dimensions`, and 7-da…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an incremental per-tenant events_dedup model from staging tenant_events, deduplicating by event_id and keeping the latest per day. Partition by date(event_ts). Redact event_id using analytics_b",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "## Per-Tenant Schema Architecture - `analytics.{tenant}.hourly_metrics` - incremental model - `analytics.{tenant}.users_cohort` - snapshot table - `analytics.lineage` - global lineage tracking ## Incremental Model: anal…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to implement a simple, robust per-tenant deduplication flow using dbt incremental models, with explicit redaction, per-day partitioning, and minimal schema/tests. It also evaluates macro design for a reusable merge operation and a practical CI gate for data quality.\n\n## Key Concepts\n- dbt incremental models across tenants\n- de-duplication by event_id with latest per day\n- data redaction via analytics_base.hash_event\n- per-tenant constraints: not_null, unique\n- macro design for deduplicate_merge\n- basic CI gating on data quality metrics\n\n## Code Example\n```sql\n-- Skeleton macro: analytics_base.deduplicate_merge(source, target)\n{% macro analytics_base.deduplicate_merge(source, target) -%}\n-- Implementation goes here\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you test idempotence of the incremental run?\n- How would you handle late-arriving events that belong to a previous day?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-3",
        "questionId": "q-16478",
        "question": "Design a beginner dbt flow in Snowflake to compute per-tenant feature_usage from staging.tenant_events using a tenants seed, producing analytics_{{tenant}}.feature_usage incrementally. Include a macro to enforce a simple data_contract (exact columns/types against analytics_base.contract), 2-day late data, and tests (not_null on keys; unique). Describe CI checks for per-tenant existence and isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a macro that builds analytics_{{tenant}}.daily_metrics by converting occurred_at to the tenant's timezone and aggregating counts by day and action with a 1-day lag from staging.events; add t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a dbt macro that generates analytics_{{tenant}}.churn_score incrementally from staging.tenant_events and analytics_common.customer_dim, with 1-day late data. Enforce masking policies on PII, an",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a tenant-aware macro to render analytics.{tenant}.daily_user_event_count from staging.events, with an incremental key (tenant_id, date(occurred_at), event_type). Late data 1 day via",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a per-tenant incremental dbt model that builds analytics_{{tenant}}.feature_usage from staging.tenant_events using a tenants seed. A macro generates the incremental model, filters ev",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a novice’s ability to design per-tenant dbt pipelines, macros, and basic data contracts with isolation safeguards.\n\n## Key Concepts\n- dbt incremental models per tenant\n- macros for model generation and data_contract checks\n- 2-day late data window and basic tests\n- per-tenant isolation in CI\n\n## Code Example\n```javascript\n// pseudocode: macro usage to generate per-tenant model\n{% macro per_tenant_feature_usage(tenant) %}\n    select\n      {{ tenant }}.tenant_id,\n      date(event_ts) as day,\n      user_id,\n      feature,\n      count(*) as uses\n    from {{ ref('staging__tenant_events') }}\n    where feature_flag = true\n    group by 1,2,3,4\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you extend tests to cover schema drift over time? \n- How would you handle new tenants added mid-pipeline without triggering CI failure?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-4",
        "questionId": "q-11941",
        "question": "Design a beginner dbt flow for per-tenant data drift detection: from staging.tenant_events_raw to analytics_{{tenant}}.daily_event_summary. Create a macro that computes today, yesterday, and 7-day average counts, derives a drift metric, and flags drift when it exceeds 5%. Include 1-day late data, not_null and unique tests on keys, a drift_flag per tenant, a global analytics.lineage, and a CI step to verify per-tenant daily_event_summary exists for all tenants and report tenants with drift?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement analytics_{{tenant}}.daily_actions_incremental from staging.events. Use an incremental model that aggregates actions by tenant, user, and day with a 1-day lateness window. Deduplicate by (te",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design per-tenant dau incrementally from staging.tenant_events with 1-day lag, dedup by (tenant_id, user_id, day). Macro enforce_minimum_volume() blocks tenants with dau < 50. Tests: not_null(tenant_i",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a macro `analytics_base.detect_schema_drift(tenant)` that loads the analytics_{{tenant}}.schema definitions, queries information_schema.columns for analytics_{{tenant}}.facts and analytics_{{tenant}}.events, a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement per-tenant drift detection in dbt by creating a macro that computes today, yesterday, and 7-day average event counts from `analytics_{{tenant}}.daily_event_summary`, deriving a drift metric that flags…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical dbt skills in building multi-tenant analytics with automated data quality monitoring, including macro-driven dynamic objects, drift detection algorithms, and CI/CD integration.\n\n## Key Concepts\n- dbt macros for dynamic tenant-specific object generation\n- Statistical drift calculation across time-windowed daily summaries\n- Data quality testing with not_null and unique constraints\n- Cross-tenant isolation and global lineage tracking\n- Automated CI validation for tenant completeness\n\n## Code Example\n```sql\n{% macro drift_flag(today, yesterday, seven_day) %}\n  SELECT tenant_id,\n         CASE WHEN seven_day = 0 THEN NULL\n              ELSE ABS(today - seven_day) / seven_day\n         END AS drift\n  FROM {{ this }}\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you implement alerting mechanisms for detected drift?\n- What strategies would you use to handle tenants with seasonal data patterns?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-5",
        "questionId": "q-3470",
        "question": "Design a beginner dbt flow to compute per-tenant daily metrics from staging.events that include an is_deleted flag for tombstones. Build analytics.{tenant}.daily_metrics by day_start (date of occurred_at) and event_type for rows where is_deleted = false; also create a per-tenant analytics.{tenant}.users snapshot with last_seen. Ensure incremental processing with tenant isolation (per-tenant schemas), plus not_null and unique tests and a data-contract macro. Explain tombstone handling and lineage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a per-tenant incremental dbt model that builds analytics_{{tenant}}.feature_usage from staging.tenant_events using a tenants seed. A macro generates the incremental model, filters ev",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a macro tenant_summary that writes analytics_{{tenant}}.daily_signup_summary from staging.tenant_signups. Include tenant_id, signup_date, new_users. Allow 1-day late data with a separate lat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant, region-aware funnel in Snowflake: a macro creates analytics_{{tenant}}.daily_funnel by joining staging.events for tenant_id, normalizing occurred_at to the tenant's timezone, and applying a 2-day la…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To implement: use day_start = date_trunc('day', occurred_at); aggregate active_users = count(distinct user_id) where is_deleted = false; implement incremental by tenant schema; create analytics.{tenan",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of beginner-friendly dbt patterns in a multi-tenant context, focusing on incremental flows, simple tombstone logic, and data contracts.\n\n## Key Concepts\n\n- Multi-tenant isolation via per-tenant schemas\n- Incremental models and day-based partitioning\n- Tombstones with is_deleted and its impact on active_users\n- Snapshot usage for users with last_seen\n- Data-contract macros and basic tests (not_null, unique)\n\n## Code Example\n\n```sql\n-- Incremental daily_metrics (illustrative)\nwith src as (\n  select tenant_id, user_id, event_type, occurred_at, is_deleted\n  from staging_events\n)\nselect\n  tenant_id,\n  date_trunc('day', occurred_at) as day_start,\n  event_type,\n  count(distinct case when is_deleted = false then user_id end) as active_users\nfrom src\nwhere is_deleted = false\ngroup by 1,2,3\n```\n\n```sql\n-- Snapshot concept (last_seen)\nselect\n  tenant_id,\n  user_id,\n  max(occurred_at) as last_seen\nfrom staging_events\ngroup by 1,2\n```\n\n## Follow-up Questions\n\n- How would you handle late tombstones that retroactively remove users from prior days?\n- How would you validate and monitor per-tenant lineage across dbt models?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-6",
        "questionId": "q-21083",
        "question": "Design a per-tenant rollback workflow in dbt/Snowflake: introduce a rollback_control table analytics_{{tenant}}.control with a rollback_needed flag and a target_date. When flagged, implement a per-tenant rollback macro that clones analytics_{{tenant}}.facts to a rollback snapshot as of target_date, swaps the active table safely, and re-runs incremental builds for affected days. Include 1-day late data, per-tenant isolation via RLS, and CI gating that fails if any tenant has rollback_needed=true?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A central retention policy table `analytics_base.retention_policies` stores tenant, table_name, retention_days, and archive_schema. The `analytics_base.apply_retention(tenant, table)` macro moves rows older than retenti…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an incremental per-tenant model that writes to analytics.{tenant}.daily_event_engagement using staging.events, joined to dimensions.regions by country_code to group by region_name. Aggregate",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To implement per-tenant rollback, create analytics_{{tenant}}.control with rollback_needed, target_date; a macro perform_rollback() that clones analytics_{{tenant}}.facts to a rollback variant as of t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Build analytics.{tenant}.daily_cohort as an incremental model sourced from staging.user_events. Compute daily cohorts by lifecycle_stage and event_type with a 2-day late-data window, upserting via MER",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world incidents require safe, per-tenant recovery without affecting others. This tests dbt macro design, Snowflake cloning, and governance controls.\n\n## Key Concepts\n- Snowflake CLONE and zero-copy rollback\n- dbt macros for per-tenant orchestration\n- RLS-based isolation and CI gates for safety\n- Tests for rollback flags and target_date validity\n\n## Code Example\n```javascript\n-- dbt macro sketch: macros/rollback.sql\n{% macro rollback_to_target(tenant, target_date) -%}\n  {% set src = 'analytics_' ~ tenant ~ '.facts' %}\n  {% set dst = 'analytics_' ~ tenant ~ '.facts_rollback_' ~ target_date | replace('-', '') %}\n  {% do run_query('CREATE OR REPLACE TABLE ' ~ dst ~ ' CLONE ' ~ src) %}\n  {% do run_query('ALTER TABLE ' ~ dst ~ ' ADD CONSTRAINT _target_date CHECK (event_date <= DATE ' ~ target_date ~ ')') %}\n  {% do run_query('ALTER MATERIALIZED VIEW ' ~ src ~ ' REFRESH') %}\n  {{ return(dst) }}\n{%- endmacro %}\n```\n\n## Follow-up Questions\n- How would you test for partial failures during rollback and ensure idempotence?\n- How would you extend to multi-region deployments with cross-tenant rollbacks?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-7",
        "questionId": "q-848",
        "question": "You're designing a dbt analytics pipeline for an e-commerce platform. The 'staging.events_raw' table ingests page views and purchases and is partitioned by day. Data arrives both on time and as late as 24 hours. Design a practical incremental dbt model 'analytics.daily_sales' that aggregates revenue by day, category, and customer_segment. Explain how you'd implement incremental logic, handle late data, guard against schema drift, and validate with tests and snapshots?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an incremental model keyed by day, category, and customer_segment. Compute revenue as sum(price*quantity) from staging.events_raw. Upsert into analytics.daily_sales via MERGE on (day, category, cu",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: implement an incremental model analytics.{tenant}.daily_event_summary with a composite key (tenant_id, day, event_type, user_id). Use MERGE-like upserts via dbt incremental to accommodate late d",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A central retention policy table `analytics_base.retention_policies` stores tenant, table_name, retention_days, and archive_schema. The `analytics_base.apply_retention(tenant, table)` macro moves rows older than retenti…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a macro-driven per-tenant daily rollup from staging.tenant_events to analytics_{{tenant}}.daily_summary, using day_start = date_trunc('day', occurred_at). The pipeline accepts 1-day late data to handle event d…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess ability to design scalable, fault-tolerant dbt pipelines handling late data and schema drift.\n\n## Key Concepts\n\n- Incremental models and idempotent MERGE\n- Late-arrival handling and backfill windows\n- Schema drift detection via tests and snapshots\n- Performance at scale and CI validation\n\n## Code Example\n\n```sql\n-- Example incremental sql sketch\nWITH s AS (\n  SELECT day, category, customer_segment,\n         SUM(price * quantity) AS revenue\n  FROM {{ ref('staging__events_raw') }}\n  WHERE day >= (SELECT MIN(day) FROM analytics.daily_sales)\n  GROUP BY day, category, customer_segment\n)\nMERGE INTO analytics.daily_sales AS t\nUSING s AS s\nON t.day = s.day AND t.category = s.category AND t.customer_segment = s.customer_segment\nWHEN MATCHED THEN UPDATE SET revenue = s.revenue\nWHEN NOT MATCHED THEN INSERT (day, category, customer_segment, revenue) VALUES (s.day, s.category, s.customer_segment, s.revenue);\n```\n\n## Follow-up Questions\n\n- How would you test for idempotency and late-arrival correctness?\n- How would you adapt this for a lakehouse (Parquet) or streaming source?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-8",
        "questionId": "q-16993",
        "question": "Design a beginner-friendly daily revenue flow: from staging.sales_events(order_date, amount, country_code) to analytics.daily_revenue(date, country_code, revenue) using a simple incremental model with a 1-day late data window. Include a macro that validates country_code against a seeded countries table and a CI check that flags any new country_code not present in countries. How would you implement and test this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Incrementally populate analytics.daily_revenue by date and country_code from staging.sales_events(amount). The macro validates country_code against the seeded countries table and fails CI if a new cod",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a per-tenant drift observability macro analytics_base.ensure_schema_consistency to compare analytics_{{tenant}}.staging against analytics_{{tenant}}.facts after the nightly load. Measure drift on",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: implement a per-tenant dbt macro that creates analytics_{{tenant}}.daily_anomalies from staging.tenant_events, computing a 7-day moving average of events per tenant/day and flags days where the rate diverges b…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Data Quality Plan for `analytics_{{tenant}}.daily_events` ### (1) Macro: `analytics_base.check_monotonic_date(date_col)` ```sql {% macro check_monotonic_date(date_col) %} with date_gaps as ( select tenant, {{ date_co…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical dbt skills: incremental modeling, seeds, and custom macros. It focuses on data quality governance with a simple country_code contract and CI checks appropriate for a beginner.\n\n## Key Concepts\n- Incremental models and late-arriving data\n- Macros to enforce data contracts\n- Seeds and cross-table validation in CI\n- Basic tests: not_null and unique\n\n## Code Example\n```javascript\n// pseudo snippet: macro enforces country_code in countries seed\n{% macro assert_country_in_seed(country_code) %}\n  {% set valid = (return value of select country_code from {{ ref('countries_seed') }}) %}\n  {{ if not loop over valid %} {{ exceptions }} {% endif %}\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you adapt this for multiple regions or a dynamic country list?\n- What would you add to handle out-of-order data in the 1-day window?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-9",
        "questionId": "q-16485",
        "question": "Design a per-tenant analytics sandbox in Snowflake using dbt: implement a macro that creates analytics_{{tenant}}_sandbox and analytics_{{tenant}}_daily_users, derived from staging.tenant_events, with a 1-day lag and a deterministic 5k-row stratified sample seeded by tenant_id. Enforce a strict data-contract for column names/types and a Snowflake masking policy on user_id and email for non-full-access roles. Add tests for not_null on keys and unique on (tenant, day, user_id); CI must verify every tenant has a sandbox and no PII exposure?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a tenant-aware macro that injects tenant_id filters into all models, enabling a single dbt project to serve analytics.{tenant} schemas with strict isolation. Build analytics.{tenant}.hourly_",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a tenant-aware macro create_sandbox(tenant) that builds analytics_{{tenant}}_sandbox and analytics_{{tenant}}_daily_users from staging.tenant_events. Apply a 1-day lag and a deterministic 5k-",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement analytics.daily_event_summary as an incremental Snowflake model: aggregate date_trunc('day', occurred_at), event_type, country_code (via regions) with count(distinct user_id). Use a macro de",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement analytics_base.sync_schema(tenant) to diff tenant schemas against a central catalog, emit per-tenant analytics_{{tenant}}.schema_changes, and auto-generate analytics_{{tenant}}.migrate_schem",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProbes practical multi-tenant sandboxing, data contracts, and privacy controls beyond basic pipelines.\n\n## Key Concepts\n- Tenant-scoped dbt macros\n- Snowflake masking policies and RBAC\n- Data contracts and CI validation\n- Deterministic sampling and lag\n\n## Code Example\n```sql\n{% macro create_sandbox(tenant) -%}\n-- macro skeleton\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you verify sampling determinism across runs?\n- How would you automate sandbox cleanup and tenant onboarding?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-10",
        "questionId": "q-3939",
        "question": "Design a beginner dbt flow in Snowflake to compute analytics.{tenant}.daily_user_engagement from staging.events (tenant_id, user_id, event_type, occurred_at, country_code), enriched by dimensions.countries and dimensions.products; produce per-tenant daily counts by event_type and country, with 2-day late data handling, per-tenant schema isolation, schema-drift guards, and tests (not_null on tenant_id, user_id, occurred_at; unique on (tenant_id, occurred_at, user_id)); include a snapshot analytics.{tenant}.users to capture cohort changes and a data-contract macro validating staging fields; explain cross-tenant lineage and privacy considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a macro that builds analytics_{{tenant}}.daily_metrics by converting occurred_at to the tenant's timezone and aggregating counts by day and action with a 1-day lag from staging.events; add t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a per-tenant incremental model analytics.{tenant}.daily_user_engagement, scoped to a tenant-specific schema. Build incrementally by day, joining staging.events with countries and products dime",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a per-tenant lineage governance with: (a) analytics_base.lineage_filter(tenant) to constrain sources to the tenant, (b) analytics_base.lineage_audit(tenant, model, sources, last_seen) popula",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a tenant-scoped data quality automation: a central DSL macro analytics_base.dq_test(label, table, condition) to declare tests, per-tenant analytics_{{tenant}}.dq_run to emit results into analy",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design per-tenant, incremental dbt flows with late data, tests, snapshots, and isolation, plus governance via data contracts.\n\n## Key Concepts\n- Incremental, tenant-scoped models in Snowflake\n- Late-arriving data handling (2 days)\n- Schema-drift guards and tests (not_null, unique)\n- Snapshots for cohort tracking\n- Data-contract macro to validate sources\n- Cross-tenant lineage and privacy considerations\n\n## Code Example\n```sql\n-- simplified incremental model sketch\nwith base as (\n  select tenant_id, user_id, event_type, occurred_at, country_code\n  from {{ source('staging','events') }}\n  where occurred_at >= dateadd(day, -2, current_date())\n)\nselect\n  tenant_id,\n  date(occurred_at) as day,\n  event_type,\n  country_code,\n  count(distinct user_id) as active_users\nfrom base\ngroup by 1,2,3,4\n```\n\n## Follow-up Questions\n- How would you implement the data-contract macro to validate staging fields before run?\n- What are the trade-offs of per-tenant schema isolation versus a shared schema with tenant_id partitioning?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-11",
        "questionId": "q-15354",
        "question": "In Snowflake with dbt, design an intermediate per-tenant retention analytics pipeline that ingests from staging.tenant_events into analytics_{{tenant}}.retention_cohort daily. Build a macro to auto-create per-tenant schemas, enforce 1-day late data, apply RLS masking for user_id, include schema-drift guards, and tests: not_null on keys, unique on (tenant_id, cohort_week, user_hashed_id). Describe CI checks for isolation and leakage monitoring in production?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-tenant incremental daily_metrics using MERGE-based upserts on (tenant_id, day, event_type) to deduplicate by record_hash, with 2-day late data. Enforce per-tenant isolation via schemas,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a tenant-aware macro that injects tenant_id filters into all models, enabling a single dbt project to serve analytics.{tenant} schemas with strict isolation. Build analytics.{tenant}.hourly_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a macro to auto-create analytics_{{tenant}} schemas and a per-tenant retention model analytics_{{tenant}}.retention_cohort, fed by a daily incremental from staging.tenant_events. Hash user_id to h",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create a central macro that diffs columns for upstream vs downstream, store a per-tenant score and timestamp in schema_evolution, and surface a dashboard metric. Implement incremental runs, per-tenant",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical multi-tenant data governance with dbt/Snowflake, focusing on isolation, latency, and tests.\n\n## Key Concepts\n- Per-tenant schema generation via macros\n- Row-level security masking and hashed identifiers\n- Incremental + late-data handling\n- Schema-drift guards and CI data-quality checks\n\n## Code Example\n```javascript\n// example dbt macro call sketch; actual SQL in macros\n```\n\n## Follow-up Questions\n- How would you test cross-tenant lineage in CI?\n- How would you monitor leakage in prod and automate alerts?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-12",
        "questionId": "q-12935",
        "question": "NEW ANGLE: Per-tenant data quality and lineage monitoring. Given a tenants registry and staging.tenant_events, design a dbt-driven flow that materializes analytics_{{tenant}}.dq_summary and analytics_{{tenant}}.dq_checks per day. Use a macro to create per-tenant checks, implement 1-day late data, enforce schema drift guards, include not_null on keys and unique on (tenant_id, day_start, event_id), populate global analytics.lineage, and CI that runs per-tenant checks and flags anomalies. Also explain auto-creating checks for new tenants via the registry?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "In Snowflake/dbt, build analytics_{{tenant}}.anomalies from staging.tenant_events using analytics_base.anomaly_model; thresholds come from analytics_{{tenant}}.thresholds. Use a macro to mask PII for",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant data-quality module: a macro that builds analytics_{{tenant}}.dq_summary and analytics_{{tenant}}.dq_checks from staging.tenant_events via a 1-day-late incremental MERGE; comput",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a per-tenant drift observability macro analytics_base.ensure_schema_consistency to compare analytics_{{tenant}}.staging against analytics_{{tenant}}.facts after the nightly load. Measure drift on",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant anomaly flow with a macro generating analytics_{{tenant}}.anomaly_flags and a global lineage table. From staging.tenant_events, compute daily counts per tenant, apply a simple z-sc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nExplores automated per-tenant data quality policing, macro-driven model creation, and end-to-end validation in a multi-tenant Snowflake/dbt setup.\n\n## Key Concepts\n- Per-tenant macros and registries\n- Incremental merges with 1-day latency\n- Schema drift guards and not_null/unique tests\n- Global lineage analytics.lineage and tenant-aware CI\n\n## Code Example\n```javascript\n-- macro create_dq_tables(tenant)\nCREATE OR REPLACE VIEW analytics_{{tenant}}.dq_summary AS\nSELECT tenant_id, day_start, COUNT(*) AS total_events\nFROM {{ ref('staging_tenant_events') }}\nWHERE occurred_at >= DATEADD(day, -1, CURRENT_DATE())\nGROUP BY 1,2;\n```\n\n## Follow-up Questions\n- How would you alert on cross-tenant data quality breaches and isolate faulty tenants?\n- How would you handle tenants added to the registry after initial runs?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-13",
        "questionId": "q-8888",
        "question": "Design a beginner dbt + Snowflake flow that consumes staging.tenant_metrics(tenant_id, metric, value, recorded_at) with up to 7 days of late data. Create analytics_{{tenant}}.daily_quality_metrics via incremental materialization and a macro that auto-creates analytics_{{tenant}} schemas plus a central analytics.lineage. Add tests not_null on (tenant_id, recorded_at, metric) and unique on (tenant_id, day_start, metric), plus a CI check ensuring analytics_{{tenant}}.daily_quality_metrics exists for all tenants and per-tenant isolation. Explain latency watermarking and cross-tenant guard rails?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant observability/rollback flow: create analytics_{{tenant}}.observability with daily count_diff and anomaly_score using a shared analytics_base.observability_model and per-tenant thre",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a dbt snapshot on analytics_{{tenant}}.dims to manage slowly changing dimensions, backed by analytics_base.dim_versions. Enable a 1-day late data window, and route pii fields through analytics_bas",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a macro to auto-create analytics_{{tenant}} schemas and a central analytics.lineage table, plus an incremental daily_quality_metrics per tenant with a 7-day late-data watermark on recorded_at to handle…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a tenant-scoped data quality automation: a central DSL macro analytics_base.dq_test(label, table, condition) to declare tests, per-tenant analytics_{{tenant}}.dq_run to emit results into analy",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical dbt patterns for per-tenant isolation, late data handling, and lineage tracking, with a beginner-friendly incremental setup.\n\n## Key Concepts\n- dbt macros to dynamically create tenant-specific schemas\n- incremental models with watermark for late-arriving data\n- data lineage tracking across tenants\n- CI checks for per-tenant existence and isolation\n\n## Code Example\n```sql\n-- sketch of daily_quality_metrics model\nwith src as (\n  select tenant_id, metric, value, recorded_at\n  from {{ source('staging', 'tenant_metrics') }}\n), refined as (\n  select tenant_id,\n    date_trunc('day', recorded_at) as day_start,\n    metric,\n    value\n  from src\n  where recorded_at >= dateadd('day', -7, current_date())\n)\nselect * from refined\n```\n\n## Latency Watermarking\nA 7-day watermark on recorded_at ensures we process late-arriving data while maintaining reasonable latency. The watermark acts as a cutoff - data older than 7 days is excluded to balance completeness with freshness.\n\n## Cross-Tenant Guard Rails\n- Schema isolation: analytics_{{tenant}} prevents cross-tenant data access\n- CI validation: ensures all tenants have their daily_quality_metrics table\n- Lineage tracking: analytics.lineage provides visibility into data dependencies\n- Unique constraints: prevent duplicate metrics across tenant/day combinations",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-14",
        "questionId": "q-14501",
        "question": "NEW ANGLE: Per-tenant data quality registry. Given seeds.tenants(tenant_id, max_latency_days, metrics_json) and staging.quality_events(tenant_id, metric, value, occurred_at), design a beginner dbt flow from staging.quality_events to analytics_{{tenant}}.daily_quality_metrics (tenant_id, day_start, metric, value) that creates per-tenant schemas via a macro, supports 1-day late data, enforces not_null on keys and unique on (tenant_id, day_start, metric), and populates a global analytics.lineage table. Add CI to verify every tenant has a daily per-tenant table and that values respect max_latency_days thresholds; explain how you'd validate cross-tenant isolation and how to surface anomalies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant, region-aware funnel in Snowflake: a macro creates analytics_{{tenant}}.daily_funnel by joining staging.events for tenant_id, normalizing occurred_at to the tenant's timezone, and applying a 2-day la…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant daily_quality_metrics flow: use a macro to create analytics_{{tenant}} schemas, ingest 1-day-late data from staging.quality_events into analytics_{{tenant}}.daily_quality_metric",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a single macro that, for each tenant, creates analytics_{{tenant}}.revenue by day_start from staging.transactions, hashing customer_id via SHA2(256) to anonymize. Use an incremental model wi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a tenant-aware drift-detection flow in Snowflake/dbt. Use a macro to auto-generate analytics_{{tenant}}.stats from staging.tenant_events and populate analytics.lineage with hourly deltas agains",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests macro use, per-tenant schema creation, and basic CI in dbt.\n\n## Key Concepts\n- dbt macros for dynamic schemas\n- 1-day late data handling\n- Tests: not_null, unique, value thresholds\n- global lineage table and per-tenant isolation\n\n## Code Example\n```sql\n-- macro: tenant_schema(tenant_id)\n-- models/..daily_quality_metrics.sql uses analytics_{{ tenant_id }}.daily_quality_metrics\n```\n\n## Follow-up Questions\n- How would you surface anomalies in daily_quality_metrics?  \n- How would you extend tests for new metrics shipped by seeds?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-15",
        "questionId": "q-5080",
        "question": "Design an on-demand, per-tenant export flow in a dbt + Snowflake setup that generates daily export tables analytics_export.{tenant}.daily_export for a given date range. Describe a macro-driven model that materializes exports incrementally by date, uses Snowflake streams for CDC, enforces tenant isolation, and includes tests (row_count within bounds, not_null keys, and tenant_id integrity). Explain how you would validate correctness and security in CI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I'll design a tenant-aware dbt flow that builds per-tenant analytics with proper time zone conversion and isolation safeguards. The solution uses a macro that loops through active tenants, creates dedicated analytics_{{…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a per-tenant, on-demand export path that materializes analytics_export.{tenant}.daily_export for a specified date range. Describe a macro-driven Snowflake model that creates daily export table",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a macro that creates analytics_{{tenant}}.daily_summary from raw.events with sum(amount) and count(*) grouped by (tenant_id, event_date). Allow 1-day late data via a late_data window. Add tests: n",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant ingestion-lag monitor using a base table analytics_base.ingest_lag with columns tenant, table_name, max_lag_hours, snapshot_ts. Implement a macro analytics_base.ingest_lag(tenant,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical multi-tenant export design, combining dbt macros, Snowflake CDC, and tests.\n\n## Key Concepts\n- Macro-driven models for on-demand objects\n- Snowflake streams and tasks for incremental exports\n- Tenants isolation and governance\n- Data quality via tests: not_null, unique, row_count bounds\n- CI validation and security controls\n\n## Code Example\n```sql\n-- macro skeleton\n{% macro export_per_tenant(tenant_id, start_date, end_date) %}\n  -- create analytics_export.{tenant_id}.daily_export\n  -- incremental by date, attach to stream\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How to scale to 100s of tenants without hitting catalog limits?\n- How to enforce least privilege in Snowflake roles for exports?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-16",
        "questionId": "q-8032",
        "question": "Design a beginner dbt exercise to compute per-tenant weekly retention by cohort. With staging.signups(tenant_id, user_id, signed_up_at) and staging.active_days(tenant_id, user_id, day, active), build analytics_{{tenant}}.weekly_cohort_retention that groups cohorts by week_start(signed_up_at) and measures retention by days_since_signup (0-6, 7-13). Use a macro to create per-tenant schemas analytics_{{tenant}} and a global analytics.lineage. Include 1-day late data, not_null tests, and CI ensuring per-tenant tables exist and data stays isolated?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a dbt macro that materializes analytics_{{tenant}}.weekly_cohort_retention as an incremental table from staging.signups and staging.active_days, computing week_start and days_since_signup, a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a macro-driven per-tenant weekly forecast that merges staging.tenant_events with analytics_base.forecast_base and analytics_{{tenant}}.calibration to produce analytics_{{tenant}}.weekly_foreca",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement per-tenant drift detection in dbt by creating a macro that computes today, yesterday, and 7-day average event counts from `analytics_{{tenant}}.daily_event_summary`, deriving a drift metric that flags…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant SLA model: create analytics_{{tenant}}.slas updated daily via incremental dbt from analytics_{{tenant}}.events, computing 95th percentile latency against analytics_base.latency_",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests per-tenant cohort retention design, macro scaffolding, and CI for isolation, a realistic starter for multi-tenant analytics.\n\n## Key Concepts\n- dbt macros for per-tenant objects\n- incremental models and late-arriving data\n- cohort retention calculations\n- cross-tenant isolation and lineage\n\n## Code Example\n```sql\n-- macro example (pseudo)\n{% macro analytics_tenant_weekly_retention() %}\n  SELECT\n    tenant_id,\n    week_start(signed_up_at) AS week_start,\n    COUNT(DISTINCT user_id) AS retained_users\n  FROM {{ ref('staging_signups') }} s\n  JOIN {{ ref('staging_active_days') }} a USING (tenant_id, user_id)\n  WHERE a.active = TRUE\n  GROUP BY tenant_id, week_start\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you adjust for tenants with sparse data?\n- How would you test cross-tenant bleed in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-17",
        "questionId": "q-3669",
        "question": "Design a multi-tenant dbt pipeline for a Discord/Snap like platform that computes analytics.{tenant}.event_summary from staging.events (tenant_id, user_id, event_type, occurred_at, country). Build an incremental model by hour and event_type with 1 day late data, tenant isolation via per tenant schemas, and a tenant-filter macro; add dynamic schema drift guards and tests (not_null on hour_start, tenant_id, event_type; unique on (tenant_id, hour_start, event_type)); include per-tenant analytics.{tenant}.users snapshot, analytics.lineage, and analytics.{tenant}.alerts for anomalies; outline testing and alerting strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-tenant isolation with analytics.{tenant} schemas, an hourly incremental by hour_start and event_type, 1-day late data, macro to inject tenant filters, dynamic schema drift guards and tes",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a per-tenant daily_quality_metrics flow: use a macro to create analytics_{{tenant}} schemas, ingest 1-day-late data from staging.quality_events into analytics_{{tenant}}.daily_quality_metric",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create analytics_{{tenant}}.monthly_spend as an incremental model from staging.billing_events. Build currency_to_usd(rate_table, amount, currency, day) to derive amount_usd. Enforce analytics_base.con",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement analytics_{{tenant}}.product_scd_type2 as an incremental model that uses surrogate key product_sk generated by analytics_base.hash_product(tenant, product_id). Track start_date, end_date, is",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design scalable multi-tenant dbt flows with strict isolation, drift guards, lineage, and alerting. Must show pragmatic handling of late data, per-tenant materializations, and automated tests.\n\n## Key Concepts\n\n- Multi-tenant isolation via per-tenant schemas\n- Incremental models by hour_start and event_type\n- Late data handling (1 day)\n- Tenant-filter macro for consistent isolation\n- Dynamic schema drift guards and tests (not_null, unique)\n- Snapshots for user cohorts per tenant\n- Central lineage exposure for dashboards\n- Per-tenant alerts for anomalies\n\n## Code Example\n\n```jinja\n{% macro tenant_filter(table) %}\n  {% if var('tenant_id') is defined %}\n    SELECT * FROM {{ table }} WHERE tenant_id = '{{ var('tenant_id') }}'\n  {% else %}\n    SELECT * FROM {{ table }}\n  {% endif %}\n{% endmacro %}\n```\n\n```sql\n-- sample: ensure a per-tenant incremental by hour_start\nwith source as (\n  select tenant_id, user_id, event_type, date_trunc('hour', occurred_at) as hour_start, country\n  from {{ ref('staging__events') }}\n  where tenant_id = '{{ var('tenant_id', '') }}'\n)\nselect * from source\n```\n\n## Follow-up Questions\n\n- How would you scale this for thousands of tenants with varying data volumes?\n- How would you automate schema-drift guard updates and test coverage across tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-18",
        "questionId": "q-4978",
        "question": "Design a multi-tenant dbt flow in Snowflake that computes per-tenant daily revenue from staging.sales and staging.refunds. Create a macro that materializes analytics_{tenant}.daily_revenue and a global analytics.lineage; support 1-day late data, idempotent upserts, and tests (not_null, unique on tenant_id/day/order_id, revenue >= 0). Include per-tenant customers snapshot and outline CI isolation and backfill strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant data enrichment and de-identification pipeline in dbt/Snowflake for analytics_{{tenant}} events. Begin with staging.tenant_events, join analytics_{{tenant}}.mapping_user to enrich records, apply dete…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a macro to materialize analytics_{{tenant}}.daily_revenue by incrementally merging staging.sales and staging.refunds per day. Build a global analytics.lineage to track dependencies. Backfill with tenant-specific dat…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a per-tenant quality pass with a shared analytics_base macro analytics_base.verify_fk that scans analytics_{{tenant}}.fact_sales for orphaned product_id or customer_id across tenants, emits",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a dbt macro that generates analytics_{{tenant}}.churn_score incrementally from staging.tenant_events and analytics_common.customer_dim, with 1-day late data. Enforce masking policies on PII, an",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests multi-tenant data modeling, incremental logic, and data safety in production dbt.\n\nKey Concepts\n- Macro-driven per-tenant materializations\n- Snowflake MERGE upserts for daily revenue\n- Global lineage for auditable dependencies\n- Late-arrival data handling and schema guards\n- Cross-tenant isolation validated in CI\n\nCode Example\n```sql\n{% macro revenue_per_tenant(tenant) %}\nselect\n  date_trunc('day', occurred_at) as day,\n  tenant_id,\n  order_id,\n  sum(amount - coalesce(discount, 0)) as revenue\nfrom {{ ref('staging_sales') }} s\nleft join {{ ref('staging_refunds') }} r\n  on```",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-19",
        "questionId": "q-21525",
        "question": "Design a per-tenant incremental dbt pipeline in Snowflake that materializes analytics_{{tenant}}.session_metrics from staging.tenant_sessions and analytics_base.user_dim, with a 2-day late-data window, per-tenant thresholds analytics_{{tenant}}.thresholds to derive a high_risk flag, and a tenant context macro to inject tenant_id and enforce RLS. Include tests not_null and unique, and a CI gate that blocks production if any tenant's high_risk_rate drifts >15% in a day. Provide macro names, a minimal incremental SQL, and test stubs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement `analytics_{{tenant}}.revenue_daily` as an incremental model from `staging.orders` (order_ts, tenant_id, revenue, customer_ssn) and `staging.payments`, keyed on `date(order_ts)`. Use `analytics_base.redact_ssn…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Per-Tenant Data Catalog Export Feature ### Design Overview Create a macro `analytics_base.export_catalog(tenant)` that reads the dbt project's `manifest.json`, extracts model metadata, and writes it to a per-tenant c…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an incremental model analytics_{{tenant}}.session_metrics driven by staging.tenant_sessions and analytics_base.user_dim, with a 2-day late-data window and per-tenant thresholds analytics_{{ten",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "To address this, implement a tenant-scoped incremental pipeline. A macro creates analytics_{{tenant}} schemas and per-tenant hourly_engagement tables. Use Snowflake masking policies on PII in analytic",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTesting multi-tenant isolation, dynamic per-tenant scoring, and CI controls in a dbt/Snowflake pipeline.\n\n## Key Concepts\n- Incremental models with late-arrival windows\n- Multi-tenant isolation via RLS and tenant context macros\n- Per-tenant thresholds to compute high_risk\n- CI gates gating per-tenant validation\n\n## Code Example\n```javascript\n{{ config(materialized='incremental') }}\nwith s as (\n  select tenant_id, user_id, date_trunc('day', session_start) as day_start, sum(duration) as total_seconds\n  from {{ ref('staging_tenant_sessions') }}\n  group by 1,2,3\n)\nselect s.*, t.threshold as threshold\nfrom s\nleft join {{ ref('analytics_base.user_dim') }} u on u.user_id = s.user_id\nleft join {{ ref('analytics_{{tenant}}.thresholds') }} t on t.tenant_id = s.tenant_id\n{% if is_incremental() %}\nwhere day_start > (select max(day_start) from analytics_{{tenant}}.session_metrics)\n{% endif %}\n```\n\n## Follow-up Questions\n- How would you test cross-tenant data bleed and RBAC in CI?\n- What are the trade-offs of the per-tenant macro approach vs. per-tenant schemas?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-20",
        "questionId": "q-4274",
        "question": "In a multi-tenant Snowflake dbt project, staging.events has tenant_id, user_id, event_type, occurred_at. Create an incremental analytics.{tenant}.daily_events that counts events per tenant/day by event_type from staging.events. Implement 1-day late data, per-tenant schemas via a macro, and a lightweight analytics.lineage table. Tests: not_null on key columns and unique on (tenant_id, day, event_type). How would you ensure isolation and maintainability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an incremental per-tenant model analytics.{tenant}.daily_events from staging.events, aggregating by tenant_id, date(occurred_at), event_type. Allow 1-day late data. Use a macro to render per-te",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "A tenancy-aware dbt run parameterizes the target schema per tenant, isolates seeds and sources with tenant prefixes, and uses a tenancy-aware test macro to scope checks. In CI, create ephemeral tenant",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant weekly retention pipeline in analytics.tenant_retention that cohorts users by their first_seen_at week and computes weekly active users from staging.events per tenant_id. Use MERGE statements for…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a dbt macro that generates analytics_{{tenant}}.churn_score incrementally from staging.tenant_events and analytics_common.customer_dim, with 1-day late data. Enforce masking policies on PII, an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes ability to implement a simple, tenant-aware incremental model with light data-quality checks, while introducing macros and a lineage artifact to address maintainability and isolation—common beginner tasks in real dbt workflows at scale.\n\n## Key Concepts\n\n- Incremental models with late data handling\n- Tenant-scoped schemas and macros\n- Basic data-contract tests\n- Lightweight lineage tracking for cross-tenant visibility\n\n## Code Example\n\n```sql\n-- Example incremental model outline\nwith src as (\n  select tenant_id, cast(date(occurred_at) as date) as day, event_type\n  from {{ source('staging', 'events') }}\n)\nselect tenant_id, day, event_type, count(*) as events\nfrom src\ngroup by tenant_id, day, event_type\n{% if is_incremental() %}\n  where day < (select max(day) from analytics.lineage where tenant_id = tenant_id)\n{% endif %}\n```\n\n## Follow-up Questions\n\n- How would you implement a generic tests macro to enforce required fields for all tenants?\n- How would you scale the lineage table as tenants are added?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-21",
        "questionId": "q-29026",
        "question": "Design a centralized multi-tenant Data Quality framework in Snowflake/dbt. Per-tenant schemas: analytics_{{tenant}} and shared analytics_base. Implement analytics_base.dq_score(tenant) using five checks (not_null primary keys, valid_foreign_keys, non_empty_critical_fields, recency of last_event_ts, stable daily row_count). Create analytics_{{tenant}}.dq_report storing date, score, and violations; analytics_base.dq_dashboard aggregating across tenants. Add per-tenant RLS on dq_violations. CI gate: fail prod if any tenant score < 80 for 2 consecutive days or violations > 3 in a day. Include tests and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed: implement qa_latency_ri to compute max delay between ingest_ts and event_ts using QUERY_HISTORY; build analytics_{{tenant}}.qa_summary incrementally (tenant, src_table, dim_table, latency_da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Compute a 0–100 score per tenant from five checks: not_null(pk), valid_fk, non_empty_critical_fields, recency of last_event_ts, and stable daily row_count. macro analytics_base.dq_score(tenant) return",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a seeds-driven macro looping tenants to create daily analytics_{{tenant}}.quality_contracts from staging_tenant_events. Track row_count, distinct_user_ids, null_user_ids, and SLA_target per day. P",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "This interview question evaluates a candidate's ability to architect a scalable, compliant data masking framework in a multi-tenant dbt/Snowflake environment. The solution requires: (1) a dbt macro `analytics_base.apply…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design a scalable, centralized data quality framework for multi-tenant Snowflake/dbt environments with strong governance and CI integration.\n\n## Key Concepts\n- Centralized macros and per-tenant schemas\n- Data quality scoring and violation tracking\n- Cross-tenant dashboards and RBAC integration\n- CI gates for production safety\n\n## Code Example\n```sql\n-- Minimal sketch: macro to compute a per-tenant score (pseudo-SQL)\nCREATE OR REPLACE MACRO analytics_base.dq_score(tenant STRING)\nRETURNS VARIANT\nAS $$\n  SELECT 100\n  - (CASE WHEN NOT_NULL(pk) THEN 0 ELSE 10 END)\n  - (CASE WHEN valid_fk THEN 0 ELSE 20 END)\n  -- additional checks omitted for brevity\n  AS score\n$$;\n```\n\n## Follow-up Questions\n- How would you rotate baseline rules to adapt to schema drift?\n- How would you test SLAs across tenants in CI?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-22",
        "questionId": "q-6457",
        "question": "NEW ANGLE: Beginner dbt flow for per-tenant consent auditing. Given seeds.tenants(tenant_id, status) and staging.consent_events(tenant_id, user_id, consent_type, consent_ts), design a macro-driven flow that creates analytics_{{tenant}}.consent_audit with daily counts by consent_type. Include 1-day late data, not_null tests for tenant_id, user_id, consent_type, and a unique on (tenant_id, consent_type, day_start). Add a global analytics.lineage and CI that every tenant has analytics_{{tenant}}.consent_audit and auto-creates for new tenants. Explain cross-tenant isolation validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a macro that materializes analytics_{{tenant}}.consent_audit from staging.consent_events, rounded to day_start with daily counts per consent_type. Use an incremental load with a 1-day late window to capture delay…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a tenant-aware enrichment macro that generates analytics_{tenant}.hourly_engagement tables from staging_events, with a central analytics.lineage table tracking cross-tenant dependencies. The macro validates en…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create analytics_{{tenant}}.anomaly_scores incrementally, using analytics_base.daily_metrics to compute a 7-day mean and stddev for revenue per tenant, via a macro analytics_base.compute_zscore(val, m",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a tenant-aware macro to render analytics.{tenant}.daily_user_event_count from staging.events, with an incremental key (tenant_id, date(occurred_at), event_type). Late data 1 day via",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests macro-driven multi-tenant object creation, incremental processing, and data-quality controls at beginner level.\n\n## Key Concepts\n- dbt macro scaffolding per-tenant models\n- incremental loads with late data windows\n- not_null and unique tests\n- lineage tracking and CI per-tenant validation\n- cross-tenant isolation checks\n\n## Code Example\n```sql\n-- example macro usage (conceptual)\n{% macro per_tenant_consent_audit(tenant) %}\nselect '{{ tenant }}'::text as tenant_id, \n       consent_type, \n       date_trunc('day', consent_ts) as day_start, \n       count(*) as cnt\nfrom s\n```",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-23",
        "questionId": "q-26361",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a beginner-friendly per-tenant data quality sampling workflow. Create a macro analytics_base.sample_tuples(tenant, table, sample_size) that deterministically returns N rows per tenant from analytics_{{tenant}}.{{table}} using a per-tenant seed. Build a dbt model analytics_{{tenant}}.qa_sample from staging.tenant_events and add tests not_null and unique for sample_id and tenant. Add a CI gate to fail prod if any tenant's latest-day sample_count is below sample_size for 7 consecutive days?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a beginner-friendly per-tenant data quality sampling workflow. Create a macro analytics_base.sample_tuples(…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a multi-tenant incremental dbt flow in Snowflake that computes analytics.{tenant}.monthly_cost from staging.sales_events (tenant_id, cost_cents, currency, billed_at, event_hash). Build analytics.{tenant}.monthly_…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Provide a dbt macro `generate_features(tenant_id, feature_name, ...)` that creates `analytics_{{tenant}}.features_{{feature_name}}` as an incremental model sourced from staging events, applying per-tenant hash-based row…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a macro-driven per-tenant diagnostics flow: generate analytics_{{tenant}}.diagnostics_daily from staging.tenant_events with latency_ms, completeness_pct, and error_rate_pct per day; use MERGE f",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests a practical, beginner-friendly data QA pattern that scales by tenant, leveraging dbt macros and per-tenant lineage. It encourages deterministic sampling, traceable QA artifacts, and CI gates for automated quality enforcement.\n\n## Key Concepts\n- Deterministic per-tenant sampling using a seed for reproducibility\n- dbt macro design and Jinja templating for reusable SQL\n- Per-tenant QA model creation with tests (not_null, unique)\n- CI gating based on multi-day sample sufficiency thresholds\n\n## Code Example\n```sql\n{% macro sample_tuples(tenant, table, sample_size) -%}\nSELECT *\nFROM analytics_{{ tenant }}.{{ table }}\nQUALIFY ROW_NUMBER() OVER (ORDER BY MD5(CONCAT('{{ tenant }}', '_', _))) % {{ sample_size }} = 0\n{%- endmacro %}\n```\n\n```yaml\n# models/analytics_{{tenant}}/qa_sample.yml\nversion: 2\nmodels:\n  - name: qa_sample\n    tests:\n      - not_null:\n          column_name: sample_id\n      - not_null:\n          column_name: tenant\n      - unique:\n          column_name: sample_id\n```\n\n```yaml\n# dbt_project.yml\non-run-start:\n  - \"{% if target.name == 'prod' %}\"\n  - \"{% set failing_tenants = api.get_adapter().execute('...') %}\"\n  - \"{% if failing_tenants|length > 0 %}\"\n  - \"{{ exceptions.raise_compiler_error('CI Gate Failed: ' ~ failing_tenants|join(', ') ~ ' have insufficient samples') }}\"\n  - \"{% endif %}\"\n  - \"{% endif %}\"\n```\n\nThis workflow provides consistent, reproducible QA samples per tenant while automating quality gates in CI/CD pipelines.",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-24",
        "questionId": "q-25710",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, implement a beginner-friendly per-tenant data lineage validator. Create a macro analytics_base.export_lineage(tenant) that reads manifest.json and writes a JSON graph of sources -> models to analytics_base.lineage_{{tenant}}. Add a test that every model has at least one source. Wire a CI gate to fail deploy if any lineage file is missing or empty for any tenant?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant weekly retention pipeline in analytics.tenant_retention that cohorts users by their first_seen_at week and computes weekly active users from staging.events per tenant_id. Use MERGE statements for…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Aggregate revenue by tenant and day from staging.sales_events into analytics_{{tenant}}.daily_revenue via an incremental model with unique_key (tenant_id, day). Use day = date_trunc('day', event_date)",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a tenant-scoped data quality automation: a central DSL macro analytics_base.dq_test(label, table, condition) to declare tests, per-tenant analytics_{{tenant}}.dq_run to emit results into analy",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a per-tenant lineage validator: a macro analytics_base.export_lineage(tenant) that reads manifest.json, emits a JSON lineage graph analytics_base.lineage_{{tenant}} mapping sources to downstre",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nChecks basic dbt graph understanding and multi-tenant discipline. Exercises macro creation and artifact export. Ensures starter data governance with lineage validation.\n\n## Key Concepts\n- dbt graph, manifest.json, multi-tenant schemas, macro development, CI gates.\n\n## Code Example\n```javascript\n// Implementation sketch\n{% macro export_lineage(tenant) %}\n  // parse manifest.json for tenant and emit analytics_base.lineage_{{tenant}} as JSON\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you handle incremental lineage exports across deploys?\n- How would you adapt to changing sources or models in lineage?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-13T06:17:46.340Z",
    "version": 1
  }
]