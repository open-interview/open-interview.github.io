[
  {
    "id": "test-dbt-analytics-engineer",
    "channelId": "dbt-analytics-engineer",
    "channelName": "DBT Analytics Engineer",
    "title": "DBT Analytics Engineer Knowledge Test",
    "description": "Test your understanding of DBT Analytics Engineer with 25 questions.",
    "questions": [
      {
        "id": "dbt-analytics-engineer-q-0",
        "questionId": "q-19086",
        "question": "Design a beginner dbt per-tenant daily_engagement pipeline: analytics_{{tenant}}.daily_engagement from staging.tenant_events with 1-day lag. Add a macro enforce_contract() comparing to analytics_base.contract and tests not_null(tenant_id, day, engagements), unique(tenant_id, day). Include a day-over-day non-decrease check. CI validates all tenants exist and new tenants trigger a full run; discuss cross-tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant incremental model: analytics_{{tenant}}.daily_engagement from staging.tenant_events with a 1-day lag. Add a contract macro to validate columns against analytics_base.contract, p",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Build a tenant_schema macro that creates analytics_{{tenant}} namespaces and a daily_engagement model materialized incrementally by day_start. Gate late data with occurred_at <= current_date - 1. Add",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant sanitized session summary pipeline in Snowflake/dbt: from staging.tenant_sessions to analytics_{{tenant}}.sanitized_session_summary daily; use a macro to mask user_id and session_i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an incremental analytics_{{tenant}}.hourly_metrics table, built by a macro that creates analytics_{{tenant}} schemas and a central analytics.lineage. Partition by hour_start; ingest 1-day late data using dbt's…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nIntroduces per-tenant incremental pipelines, simple data contracts, and CI checks—core for multi-tenant analytics platforms.\n\n## Key Concepts\n- dbt models, macros, incremental materializations\n- data contracts and tests, per-tenant isolation\n- CI validation across tenants\n\n## Code Example\n```sql\n-- macro example (simplified)\n{% macro enforce_contract(table) %}\n  -- compare columns to analytics_base.contract\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you scale the contract checks as tenants grow?\n- What if a tenant lag affects late data handling?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-1",
        "questionId": "q-12663",
        "question": "Design a per-tenant anomaly detection flow in Snowflake/dbt: from staging.tenant_events to analytics_{{tenant}}.anomaly_daily. Build with a macro that creates per-tenant schemas, computes rolling 7-day mean/stddev for the target metric, flags anomalies via z-score, and supports 1-day late data. Include schema-drift guards, tests (not_null on keys, unique (tenant_id, day_start)), a global analytics.lineage, and CI that ensures every tenant has an anomaly view and auto-creates it from a tenants registry. Explain scaling considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant feature-flag pipeline using Databricks and dbt that materializes analytics_{{tenant}}.feat_engagement from staging.tenant_events with 1-hour late data tolerance. Use the shared analytics_base.feature…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant hourly metrics by materializing analytics_{{tenant}}.hourly_metrics from staging.tenant_events_hourly using analytics_base.metrics_model; per-tenant config analytics_{{tenant}}.me",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant anomaly profiling: build incremental analytics_{{tenant}}.anomaly_daily from staging.tenant_events. A macro creates per-tenant schemas, computes rolling 7-day mean and stddev for the metric",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "I would implement a per-tenant drift-aware daily pipeline. A dbt incremental analytics_{{tenant}}.daily_engagement from staging.tenant_events into per-tenant schemas via a macro, plus analytics_{{tena",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests ability to design scalable, tenant-isolated analytics with drift guards and robust testing in dbt/Snowflake.\n\nKey Concepts\n- Per-tenant schema generation via macros\n- Rolling window statistics for anomaly detection\n- Data latency handling and drift guards\n- Tests and lineage for cross-tenant safety\n- Registry-driven auto-creation and CI validation\n\nCode Example\n```sql\n-- Pseudo: macro to create analytics_{{tenant}}.anomaly_daily\nCREATE OR REPLACE VIEW analytics_{{tenant}}.anomaly_daily AS\nSELECT tenant_id,\n       day_start,\n       metric_value,\n       CASE WHEN ABS((metric_value - mean7)/stddev7) > 3 THEN 1 ELSE 0 END AS is_anomaly\nFROM (\n  SELECT tenant_id, day_start, metric_value,\n         AVG(metric_value) OVER (PARTITION BY tenant_id ORDER BY day_start ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) AS mean7,\n         STDDEV(metric_value) OVER (PARTITION BY tenant_id ORDER BY day_start ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) AS stddev7\n  FROM staging.tenant_events\n  WHERE day_start >= DATEADD(DAY, -7, CURRENT_DATE())\n)\n```\n\nFollow-up Questions\n- How would you handle tenants with sparse data or cold-start?\n- What monitoring would you add for model drift or threshold alerts?\n- How would you test cross-tenant isolation in CI with registry changes?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-2",
        "questionId": "q-17030",
        "question": "Design a canary-driven, per-tenant dbt pipeline that emits analytics_canary_{{tenant}} from staging.tenant_events and compares it against production analytics_{{tenant}} to fail CI on drift beyond a tenant-specific tolerance. Include 1-day late data, schema-drift guards, and tests: not_null on keys, unique on (tenant_id, day_start, user_id). Explain how you enforce per-tenant isolation and how canaries feed lineage and governance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a dbt macro to generate per-tenant canary and production models (analytics_canary_{{tenant}}, analytics_{{tenant}}) from staging.tenant_events, compare lightweight row hashes, and fail CI if",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Explain per-tenant schemas analytics_{{tenant}} and a per-tenant export analytics_export_{{tenant}}.digest; digest_day = date(occurred_at). Hash user_id with tenant_salt using HMAC_SHA256 to hashed_us",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Add per-tenant feature flags gating in a Snowflake/dbt setup. Create analytics_base.feature_flags(tenant_id, flag_name, enabled). Implement analytics_base.flag_filter(sql, flag_name) to append a tenan",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "This interview question evaluates a candidate's ability to architect a scalable, compliant data masking framework in a multi-tenant dbt/Snowflake environment. The solution requires: (1) a dbt macro `analytics_base.apply…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes canary-driven data quality amid multi-tenant isolation. It tests the ability to design per-tenant models, automated drift detection, and CI gates that fail on cross-tenant data drift while preserving lineage and governance. It also checks 1-day late data handling and schema-drift guards.\n\n## Key Concepts\n- Canary models per tenant\n- Drift detection with tenant tolerance\n- Data contracts and lineage\n- Tenant isolation via schemas/permissions\n\n## Code Example\n```javascript\n// Pseudo drift check between canary and production\nfunction driftOk(canaryHash, prodHash, tol) { return Math.abs(canaryHash - prodHash) <= tol; }\n```\n\n## Follow-up Questions\n- How would you scale to thousands of tenants? \n- How would you version tenant contracts across regions?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-3",
        "questionId": "q-6767",
        "question": "NEW ANGLE: Privacy-focused per-tenant analytics. Given seeds.tenants(tenant_id, privacy_class) and staging.user_events(tenant_id, user_id, event, pii_email, occurred_at), design a beginner dbt flow that uses a macro to create analytics_{{tenant}}.daily_privacy_events with counts by event by day, while hashing pii_email per tenant (central macro using SHA256) and emitting hashed_user_id instead of raw IDs. Include 1-day late data, tests not_null/unique, a global analytics.lineage, and a CI check ensuring per-tenant tables exist and new tenants auto-create. How would you validate cross-tenant isolation and hashing consistency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Compute health_score as a weighted sum of freshness_ok, deviation_ok, pii_ok, and stability_ok; propose macros analytics_base.calc_health, analytics_{{tenant}}.freshness, analytics_{{tenant}}.deviatio",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a per-tenant governance audit flow: nightly macro-generated analytics_{{tenant}}.audit_access tables compare staging.tenant_access to detect any cross-tenant exposures, mask PII for restricted",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an incremental model analytics.{tenant}.daily_user_activity using staging.events per tenant, UTC-normalize occurred_at to date, and aggregate distinct user_id by action. Add tests: not_null",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a macro to create analytics_{{tenant}}.daily_privacy_events with daily event counts, hashing pii_email via SHA256 per tenant and emitting hashed_user_id. Build incrementally from staging.user_even",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical dbt macro usage, per-tenant isolation, and simple privacy controls in a beginner task. It also tests incremental modeling, tests, and CI validation without requiring deep production-scale infra.\n\n## Key Concepts\n- dbt macros for tenant-scoped objects\n- incremental models with late-arrival data\n- data masking via hashing (per-tenant salt concept)\n- tests for data quality and uniqueness\n- cross-tenant isolation checks in CI\n\n## Code Example\n```sql\n-- sample macro usage snippet (conceptual, not full model)\nselect\n  tenant_id,\n  date(occurred_at) as day,\n  event,\n  sha2(pii_email, 256) as hashed_email\nfrom {{ ref('staging__user_events') }}\n```\n\n## Follow-up Questions\n- How would you manage salt-per-tenant for hashing to avoid identical hashes across tenants?\n- How would you extend tests to detect accidental leakage of pii fields in non-privacy tables?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-4",
        "questionId": "q-5861",
        "question": "Design a multi-tenant dbt workflow in Snowflake that computes analytics.{tenant}.weekly_engagement_score from staging.tenant_events (tenant_id, user_id, action, occurred_at, device). Use a macro to create per-tenant analytics_{tenant} schemas and a global analytics.lineage, with 3-day late data, dynamic schema-drift guards, and tests (not_null on keys; unique on (tenant_id, week_start, user_id)). Include a per-tenant churn snapshot and CI checks for cross-tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a macro `tenant_summary()` that reads from `registry.tenants`, dynamically builds per-tenant `analytics_{{tenant}}.daily_summary` tables from `staging.tenant_events` with a 1-day data lag, and registers lineage…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an incremental per-tenant events_dedup model from staging tenant_events, deduplicating by event_id and keeping the latest per day. Partition by date(event_ts). Redact event_id using analytics_b",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a macro-driven per-tenant schema, incremental weekly_engagement_score by week_start with action weights, 3-day late data, lineage updates, and tests for not_null keys and uniqueness. Snapshot",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Approach: implement a single incremental model that builds analytics_{{tenant}}.daily_summary by joining staging.tenant_events on tenant_id. A macro drives per-tenant materialization from seeds.tenant",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to architect multi-tenant dbt flows with per-tenant isolation, lineage, and late-data handling beyond simple aggregations.\n\n## Key Concepts\n\n- dbt macros for tenant scaffolding\n- per-tenant schemas and global lineage\n- incremental by week_start with late data windows\n- schema drift guards and data quality tests\n- CI checks for cross-tenant isolation\n\n## Code Example\n\n```javascript\n// pseudo macro skeleton illustrating tenant-scoped schema creation\nfunction analyticsTenantSchema(tenant) {\n  return `analytics_${tenant}`;\n}\n```\n\n## Follow-up Questions\n\n- How would you extend to support changing action weights per tenant?\n- How would you expose lineage metadata to dashboards while preserving isolation?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-5",
        "questionId": "q-10481",
        "question": "Design a tenant-scoped data quality pipeline in Snowflake/dbt: from staging.tenant_events to analytics_{{tenant}}.quality_score and analytics_{{tenant}}.quality_facts daily. Use a macro that invokes a Python UDF to compute a composite score from data completeness, timeliness, and anomaly flags. Backfill 1 day, guard schema drift with column checks, and add tests: not_null on keys, unique on (tenant_id, day_start). CI validates per-tenant views exist and quarantines tenants with no data. Explain UDF deployment strategy and dependencies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable per-tenant rollback by introducing tenant-scoped feature flags with a blue/green deployment path. Use the `analytics_base.is_rollback_active(tenant)` macro combined with `analytics_base.deploy_active` and `analyt…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-tenant data enrichment and de-identification pipeline in dbt/Snowflake for analytics_{{tenant}} events. Begin with staging.tenant_events, join analytics_{{tenant}}.mapping_user to enrich records, apply dete…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a dbt macro to generate analytics_{{tenant}}.quality_score and analytics_{{tenant}}.quality_facts, computed by a Python UDF that blends data completeness, timeliness, and anomaly flags. Backfill 1",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design an incremental per-tenant model analytics.{tenant}.daily_events from staging.events, aggregating by tenant_id, date(occurred_at), event_type. Allow 1-day late data. Use a macro to render per-te",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design tenant-scoped data quality pipelines, integrate Python UDFs with Snowflake, automate per-tenant materializations via dbt macros, and build CI for isolation and data health.\n\n## Key Concepts\n\n- per-tenant materialization via macros\n- Python UDFs in Snowflake\n- schema drift guards and data quality tests\n- lineage and CI validation across tenants\n- seed-driven tenant registry\n\n## Code Example\n\n```sql\n-- Example macro invocation (pseudo)\n{% macro quality_score_sql(tenant) -%}\nSELECT '{{tenant}}' AS tenant_id, ...\n{% endmacro %}\n```\n\n## Follow-up Questions\n\n- How would you manage UDF versioning and dependencies in Snowflake?\n- How would you scale CI as the tenant set grows?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-6",
        "questionId": "q-9699",
        "question": "In a Snowflake/dbt multi-tenant setup, design a beginner workflow that computes per-tenant daily_engagement by aligning events to tenant time zones. Use a macro to create analytics_{{tenant}}.daily_engagement from staging.tenant_events, convert occurred_at with tenants.time_zone, and date_trunc('day'). Allow 1-day late data. Add not_null on keys, unique on (tenant_id, day_start, user_id); populate a global analytics.lineage; CI checks that per-tenant views exist and time_zone alignment is valid against tenants. Explain cross-tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To implement this, describe a macro that emits analytics_{{tenant}}.daily_engagement from staging.tenant_events, converting occurred_at to the tenant's time zone via tenants.time_zone and truncating t",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a per-tenant embeddings pipeline: analytics_{{tenant}}.user_embeddings derived from staging.tenant_events by applying a shared analytics_base.embeddings_model with tenant-specific privacy bu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a seeds-driven macro looping tenants to create daily analytics_{{tenant}}.quality_contracts from staging_tenant_events. Track row_count, distinct_user_ids, null_user_ids, and SLA_target per day. P",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I’d implement analytics.daily_engagement as an incremental model that aggregates distinct users per date and event_type, joining dimensions.regions on region_code. Late data is allowed in a 3-day wind",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests understanding of per-tenant data modeling, time zone handling, and basic dbt testing in a multi-tenant Snowflake setup. It also touches lineage tracking and CI checks.\n\n## Key Concepts\n- Time zone aware daily aggregates\n- Macro-driven per-tenant schema generation\n- Basic integrity tests (not_null, unique)\n- Global lineage and CI validations for isolation\n\n## Code Example\n```sql\n-- Pseudo dbt macro outline\n{% macro per_tenant_daily_engagement(tenant) -%}\n  select\n    tenant_id,\n    date_trunc('day', convert_time_zone('UTC', {{ tenant.time_zone }}, occurred_at)) AS day_start,\n    user_id\n  from {{ ref('staging__tenant_events') }}\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you test cross-tenant isolation in CI?\n- How would schema drift be guarded for new tenants or time zones?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-7",
        "questionId": "q-24135",
        "question": "In a Snowflake/dbt setup with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a data-quality and reconciliation pipeline that ensures event_ids are unique per tenant, supports idempotent upserts from staging.tenant_events, and reconciles 2-day-late data without duplicates. Propose a macro analytics_base.has_idempotent_upsert, a test suite (not_null, unique), and a CI gate that blocks prod if daily_event_count drift exceeds 5% for 2 consecutive days. Outline schema changes and where reconciliation metadata lives?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a central macro that diffs columns for upstream vs downstream, store a per-tenant score and timestamp in schema_evolution, and surface a dashboard metric. Implement incremental runs, per-tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-tenant idempotent upsert and reconciliation: use a hash-based key on (tenant, event_id) and upsert from staging.tenant_events into analytics_{{tenant}}.facts; implement a macro analytics_",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "The analytics_base.tag_query(query_sql, tenant_id) macro injects a QUERY_TAG session parameter to attribute all queries to their respective tenant. A Snowflake TASK materializes analytics_base.perf_costs by joining ACCO…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant quality pass with a shared analytics_base macro analytics_base.verify_fk that scans analytics_{{tenant}}.fact_sales for orphaned product_id or customer_id across tenants, emits",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design robust idempotent upserts, late-data reconciliation, and governance hooks in a multi-tenant dbt environment.\n\n## Key Concepts\n- Idempotent upserts across retries and late arrivals\n- Per-tenant isolation with shared base macros\n- Data quality tests and CI gates tying data health to deployments\n- Reconciliation metadata storage for observability\n\n## Code Example\n```javascript\n// Pseudo-dn macro sketch for has_idempotent_upsert\nfunction has_idempotent_upsert(tenant, id, keys){\n  // compare staging vs target for duplicates and missing keys\n  return true;\n}\n```\n\n## Follow-up Questions\n- How would you backfill reconciled records without double-counting? \n- How would you extend tests to cover cross-tenant offsets in timestamps?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-8",
        "questionId": "q-24940",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design an on-demand repair workflow for late-arriving events up to 3 days that backfills analytics_{{tenant}}.facts from staging.tenant_events. The workflow must be idempotent, use a macro analytics_base.backfill_merge, enforce per-tenant RLS, and trigger a CI gate if backfills widen data_age beyond 2 hours. Include macro names and a minimal SQL sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design per-tenant dau incrementally from staging.tenant_events with 1-day lag, dedup by (tenant_id, user_id, day). Macro enforce_minimum_volume() blocks tenants with dau < 50. Tests: not_null(tenant_i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a dbt macro to auto-create analytics_{{tenant}}.dq_daily from staging.tenant_events, joining to a tenants seed for referential checks. Compute per-tenant day_start aggregates: null_user_id, n",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline an on-demand repair for late-arriving events up to 3 days. Use a per-tenant MERGE from staging.tenant_events into analytics_{{tenant}}.facts with an idempotent key; implement via analytics_bas",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-tenant incremental model: analytics_{{tenant}}.daily_engagement from staging.tenant_events with a 1-day lag. Add a contract macro to validate columns against analytics_base.contract, p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design robust backfill workflows in a multi-tenant dbt Snowflake setup, focusing on idempotence, per-tenant access controls, and production safety.\n\n## Key Concepts\n- Late-data backfills in dbt/Snowflake\n- Idempotent MERGE strategies per tenant\n- Macro design for reusability\n- CI gating on backfill impact and data_age\n\n## Code Example\n```sql\n-- backfill skeleton\nMERGE INTO analytics_{{tenant}}.facts AS F\nUSING staging.tenant_events AS S\nON F.event_id = S.event_id\nWHEN MATCHED THEN UPDATE SET ... \nWHEN NOT MATCHED THEN INSERT (...);\n```\n\n## Follow-up Questions\n- How would you handle out-of-order events and dedupe?\n- What tests would ensure correctness under concurrent repairs?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-9",
        "questionId": "q-15317",
        "question": "NEW ANGLE: Tenant-aware data quality drift check. Given a seeds table tenants(tenant_id, status) and source raw.events(tenant_id, event_date, amount), design a beginner dbt flow that uses a macro to generate analytics_{{tenant}}.daily_summary with daily sums(amount) and event counts, grouped by tenant_id and event_date. Include 1-day late data, basic schema-drift guards, tests (not_null on keys, unique on (tenant_id, event_date)), and a CI check that analytics_{{tenant}}.daily_summary exists for all tenants. Explain how you'd validate cross-tenant isolation in CI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a tenant-scoped macro to create analytics_{{tenant}} schemas and analytics_{{tenant}}.hourly_engagement_funnel from staging.events, plus a global analytics.lineage. Implement incremental by h",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement macro analytics_base.refresh_cache(tenant) to run an incremental refresh of analytics_{{tenant}}.cache_user_weekly from analytics_{{tenant}}.raw_events, grouping by ISO week and user_id, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a macro that creates analytics_{{tenant}}.daily_summary from raw.events with sum(amount) and count(*) grouped by (tenant_id, event_date). Allow 1-day late data via a late_data window. Add tests: n",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement analytics_{{tenant}}.daily_actions_incremental from staging.events. Use an incremental model that aggregates actions by tenant, user, and day with a 1-day lateness window. Deduplicate by (te",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests basic dbt macro usage, per-tenant modeling, simple data quality, and CI checks that scale with tenants.\n\n## Key Concepts\n- dbt macros and per-tenant objects\n- 1-day late data handling\n- schema-drift guards and basic tests\n- CI validation for multi-tenant existence and isolation\n- lineage tracking with analytics.lineage\n\n## Follow-up Questions\n- How would you extend to multiple data sources with differing schemas?\n- How would you monitor and alert on tenant-level data drift over time?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-10",
        "questionId": "q-18621",
        "question": "In Snowflake/dbt, design a beginner-friendly per-tenant daily signup analytics flow that sources from staging.tenant_signups to analytics_{{tenant}}.daily_signup_summary using a macro-generated per-tenant model, supports 1-day late data, includes schema-drift guards, and tests not_null(tenant_id, signup_date) and unique(tenant_id, signup_date). Add a global analytics.lineage and a CI check that analytics_{{tenant}}.daily_signup_summary exists for all tenants in the registry. Explain how you'd validate cross-tenant isolation in CI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a macro tenant_summary that writes analytics_{{tenant}}.daily_signup_summary from staging.tenant_signups. Include tenant_id, signup_date, new_users. Allow 1-day late data with a separate lat",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would add a macro normalize_day(ts) returning date_trunc('day', ts AT TIME ZONE 'UTC'). Build analytics_user_daily_summary as incremental by day from staging.web_events joined to staging.users, grou",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a dbt macro that reads active tenants, loops over them, and materializes analytics_{{tenant}}.daily_summary incrementally from staging.tenant_events with 1-day late data. Include not",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a multi-tenant dbt/Snowflake approach for schema evolution: tenants drop new fields into staging_{{tenant}}.events; use analytics_base.mappings to drive per-tenant analytics_{{tenant}}.facts c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTo assess practical macro-driven per-tenant modeling, schema drift guards, and CI validation for tenant isolation.\n\n## Key Concepts\n- dbt macros to generate per-tenant models\n- schema drift guards via data contracts\n- 1-day late data handling with a late data window\n- tests: not_null and unique on keys\n- global lineage and tenant-existence checks in CI\n\n## Code Example\n```javascript\n// Macro skeleton: tenant_summary(tenant)\ndef tenant_summary(tenant) {\n  select tenant_id, signup_date, count(*) as new_users\n  from {{ ref('staging.tenant_signups') }}\n  where tenant_id = '{{tenant}}'\n  group by 1,2\n}\n```\n\n## Follow-up Questions\n- How would you simulate a schema drift in CI and fail the build?\n- How would you extend tests if a tenant migrates to a new date format?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-11",
        "questionId": "q-21835",
        "question": "Design a beginner dbt incremental model analytics_user_daily_summary that consumes staging.web_events and staging.users to produce a daily summary by event_type. Use a macro to normalize event_ts to UTC-day, implement 1-day late data, and materialize incrementally by day. Include tests not_null(day, event_type, users) and unique(day, event_type). Describe how you would validate correctness in CI and how you would handle late data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would add a macro normalize_day(ts) returning date_trunc('day', ts AT TIME ZONE 'UTC'). Build analytics_user_daily_summary as incremental by day from staging.web_events joined to staging.users, grou",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement a dbt macro that auto-generates analytics_{{tenant}}.hourly_metrics from staging.tenant_events, enforcing a data-contract stored in analytics_base.contract. The macro creates per-ten",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a dbt macro that reads cadence from a tenants seed and emits analytics_{{tenant}}.summary_hourly or analytics_{{tenant}}.summary_daily. Build incrementally, allow 1-day late data, guard against dr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant audit trail from staging.tenant_events to analytics_{{tenant}}.audit, leveraging Snowflake Streams and Tasks to apply only new events. Enforce an exact event_id contract, support 1",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests a beginner dbt workflow focusing on incremental modeling, simple macros, and basic data quality checks.\n\n## Key Concepts\n- Incremental models by day\n- Macros for date normalization\n- not_null and unique tests\n- Simple CI gate for late data\n\n## Code Example\n```sql\n-- macros/normalize_day.sql\n{% macro normalize_day(field) %}\n  date_trunc('day', {{ field }}::timestamp_ntz at time zone 'UTC')\n{% endmacro %}\n```\n\n```sql\n-- models/analytics_user_daily_summary.sql\nwith src as (\n  select\n    {{ normalize_day('e.event_ts') }} as day,\n    e.event_type,\n    e.user_id\n  from {{ ref('staging.web_events') }} e\n  join {{ ref('staging.users') }} u on u.user_id = e.user_id\n)\nselect day, event_type, count(distinct user_id) as users\nfrom src\ngroup by day, event_type\n```\n\n## Follow-up Questions\n- How would you adapt this for timezone changes or DST?\n- What edge cases occur if events have null event_ts or duplicate user_id-event_type-day rows?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-12",
        "questionId": "q-10165",
        "question": "In Snowflake with dbt, design a per-tenant governance pipeline where tenants are registered in a central registry (tenants table). On activation, auto-provision analytics_{{tenant}} schemas and a per-tenant daily_snapshot from staging.tenant_events to analytics_{{tenant}}.daily_snapshot, with 1-day late data. Implement RLS-based isolation and a masking policy on user_id for non_full_access roles. Add schema drift guards and tests (not_null on keys, unique on (tenant_id, snapshot_date, user_id)). CI should verify daily_snapshot exists for all active tenants and that lineage is preserved. Explain how you would reconcile late data with per-tenant retention and global lineage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a per-tenant governance path: a central tenants registry triggers auto-provisioning of analytics_{{tenant}} schemas; a macro creates analytics_{{tenant}}.daily_snapshot from staging.tenant_e",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a macro that creates analytics_{{tenant}}.daily_metrics as an incremental by day_start from staging.tenant_events (tenant_id, user_id, event_type, occurred_at, revenue) and a customers snaps",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant daily_quality_metrics flow: use a macro to create analytics_{{tenant}} schemas, ingest 1-day-late data from staging.quality_events into analytics_{{tenant}}.daily_quality_metric",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would add a macro normalize_day(ts) returning date_trunc('day', ts AT TIME ZONE 'UTC'). Build analytics_user_daily_summary as incremental by day from staging.web_events joined to staging.users, grou",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes automation, governance, and cross-tenant isolation in a real Snowflake/dbt setup.\n\n## Key Concepts\n- Central registry-driven provisioning; per-tenant schemas.\n- Row-level security and masking policies; data isolation guarantees.\n- Schema drift guards; incremental by date; 1-day late data.\n- Tests: not_null and unique; CI checks for all tenants and lineage.\n\n## Code Example\n```javascript\n// Placeholder: illustrates macro usage for per-tenant objects\n```\n\n## Follow-up Questions\n- How would you scale the macro to support hundreds of tenants? Provide performance considerations.\n- How would you audit data access and mask policy changes across tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-13",
        "questionId": "q-23342",
        "question": "In a Snowflake/dbt multi-tenant setup with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a real-time-like incremental path to populate analytics_{{tenant}}.impressions_facts from staging.tenant_impressions using analytics_base.impression_model. Implement a 15-minute late window, per-tenant RLS, and a macro analytics_base.mask_pii for restricted roles. Propose a CI gate that blocks production if any tenant's daily impression_volume drifts >30% vs prior day. Include tests and macro names?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an incremental per-tenant data-quality pipeline in Snowflake/dbt that derives analytics_{{tenant}}.quality_score from staging.tenant_logs using analytics_base.quality_model and analytics_{{tenant}}.thresholds. Th…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-tenant anomaly-detection pipeline that uses a macro to create analytics_{{tenant}}.daily_anomalies from daily metrics computed on staging.tenant_events. Baseline 14 days, compute rolling",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-tenant incremental path from staging.tenant_impressions into analytics_{{tenant}}.impressions_facts using analytics_base.impression_model as the common transformation. 15-minute late window;",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: implement an incremental dbt model analytics_{{tenant}}.cost_summary sourcing from staging.tenant_cost_events, governed by analytics_base.cost_model with per-tenant analytics_{{tenant}}.cost_sca",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to architect a scalable, low-latency, per-tenant pipeline that shares a common transformation while preserving isolation and data governance.\n\n## Key Concepts\n- Incremental per-tenant models in dbt with a shared base\n- Late-arrival handling and windowing\n- Data masking and RLS for restricted roles\n- Production gates driven by per-tenant data drift\n\n## Code Example\n```sql\n-- skeleton of incremental model (analytics_{{tenant}}.impressions_facts)\nwith s as (\n  select * from {{ ref('staging__tenant_impressions') }}\n)\nselect\n  tenant_id,\n  event_day,\n  impression_id,\n  masked_user_id as user_id,\n  -- derived metrics from analytics_base.impression_model\n  metric_a,\n  metric_b\nfrom s\n{% if is_incremental() %}\nwhere event_day >= dateadd(day, -1, current_date())\n{% endif %}\n```\n```\n\n## Follow-up Questions\n- How would you test late-arrival handling per tenant?\n- How would you extend to multi-region deployments with cross-region replication?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-14",
        "questionId": "q-1590",
        "question": "Design a two-model incremental dbt flow in Snowflake: 1) analytics.first_session derives first_session_at per user and seeds analytics.users(user_id, first_session_at, country_code, cohort). 2) analytics.daily_metrics counts daily active users by day and event_type, enriched with dimensions.countries. Include late-data handling (3 days), schema-drift guards, tests (not_null, unique), a snapshot, and expose lineage to dashboards?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a single macro that, for each tenant, creates analytics_{{tenant}}.revenue by day_start from staging.transactions, hashing customer_id via SHA2(256) to anonymize. Use an incremental model wi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a macro to materialize analytics_{{tenant}}.daily_revenue by incrementally merging staging.sales and staging.refunds per day. Build a global analytics.lineage to track dependencies. Backfill with tenant-specific dat…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: Implement a two-model incremental dbt flow in Snowflake. First, analytics.first_session computes min(occurred_at) per user as first_session_at and seeds analytics.users with user_id, first_session_at, country_…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-tenant daily session summary in dbt: an incremental model analytics.{tenant}.daily_session_summary using a tenant-scoped macro to reference staging.sessions. Define unique key as (tena",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design robust incremental pipelines, handle late data, and maintain lineage.\n\n## Key Concepts\n- Incremental models and ref() usage\n- First-session derivation and cohort calculation\n- Late-data watermark and idempotent upserts\n- Schema drift guards and tests; snapshots for slowly changing fields\n- Dashboards exposure and lineage\n\n## Code Example\n```sql\n-- first_session model (simplified)\nwith e as (\n  select user_id, occurred_at, country_code\n  from {{ source('raw','events_log') }}\n)\nselect user_id, min(occurred_at) as first_session_at, country_code,\n      \"",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-15",
        "questionId": "q-1257",
        "question": "Design a drift-aware dbt workflow across dev/staging/prod in Snowflake. How would you detect schema drift between sources and models using snapshots, tests, and sources, enforce a drift threshold, and automatically fail CI when drift exceeds the threshold? Describe the macro, tests, and alert/rollback mechanism, plus how you version thresholds?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-tenant data-quality and lineage validator that uses Snowflake streams on analytics_{{tenant}}.quality, sourced from staging.tenant_events, with a shared analytics_base.mappings for cross",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant Snowflake streams on staging.tenant_events and an incremental analytics_{{tenant}}.dq_status(tenant, table_name, row_count, freshness_ts, lag_min, dq_score). Add analytics_base.lineage_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a dbt macro drift_check that compares per-column metadata (data type, nullability, default, and max length) between a source table and its corresponding model, plus a runtime comparison of r",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Proposed approach: implement a dbt macro drift_check(tenant) that compares analytics_{{tenant}}.facts to analytics_base.contract, emits drift rows into analytics_{{tenant}}.drift_metrics (tenant_id, t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world pipelines require automated detection of schema drift to prevent deployment of broken analytics. This question tests practical macro design, test coverage, and CI/CD integration.\n\n## Key Concepts\n- dbt snapshots and sources for metadata\n- Per-column drift checks (type, nullability, max length)\n- Threshold-driven gating in CI/CD and rollback policies\n- Canary prod lineage validation and alerting\n\n## Code Example\n```jinja\n{% macro drift_check(source_schema, source_table, model_schema, model_table, thresholds) -%}\n-- pseudo-logic: compare metadata and basic stats, emit drift_count if > thresholds\n{%- endmacro %}\n```\n\n## Follow-up Questions\n- How would you scale per-table thresholds across hundreds of tables?\n- How would you distinguish benign drift (e.g., new optional column) from breaking drift?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-16",
        "questionId": "q-25259",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a dynamic, tenant-scoped data lineage and impact-analysis system that surfaces downstream impact of upstream schema changes in real-time. Requirements: (1) a dbt macro analytics_base.lineage_impact(source_table, column) that traces to dependent models across tenants, (2) a per-tenant materialized view analytics_{{tenant}}.lineage_summary updated daily, (3) a central analytics_base.lineage_dashboard aggregating cross-tenant lineage health, (4) per-tenant RLS to restrict lineage visibility, (5) a CI gate to block production if drift in lineage_count exceeds 3 per week. Include tests and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "JSON-driven data quality: rules.json defines per-tenant required_fields and max_null_pct; the dq_rules macro auto-generates not_null tests and a null-rate test for each tenant/table/field. Create anal",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Macro-driven per-tenant schemas and hourly_engagement from staging; per-tenant ROW ACCESS POLICIES tied to a tenants registry enforce isolation; 1-day late data with a Snowflake TASK; tests cover not_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a tenant-scoped lineage system: add analytics_base.lineage_impact(source_table, column) to compute downstream models across analytics_{{tenant}}; materialize per-tenant analytics_{{tenant}}.lin",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose an analytics_base.run_metrics table keyed by (tenant, run_id, started_at) and a macro analytics_base.emit_run_metrics(tenant, run_id, metrics_json) that inserts a JSON payload and updates a me",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design cross-tenant governance and lineage tooling in a realistic dbt/Snowflake setup.\n\n## Key Concepts\n- Data lineage and cross-tenant impact analysis\n- Tenant isolation with RLS; central governance surface\n- Incremental lineage_summary and a dashboard\n- Tests: not_null, unique keys for lineage entries\n\n## Code Example\n```sql\n-- dbt macro: lineage_impact (pseudo)\n{% macro lineage_impact(source_table, column) %}\n-- returns downstream models across tenants\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you scale lineage computations with many tenants?\n- How would you detect and handle circular lineage references?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-17",
        "questionId": "q-9509",
        "question": "In a Snowflake + dbt multi-tenant deployment, design a per-tenant diagnostics pipeline from staging.tenant_events to analytics_{{tenant}}.diagnostics_daily. Implement a macro that builds a per-tenant diagnostics view with metrics like latency_ms, completeness_pct, and error_rate_pct for each day. Ensure 1-day late data, schema drift guards, tests not_null on (tenant_id, day) and unique on (tenant_id, day). Include a global analytics.lineage and CI that verifies every tenant has a diagnostics view and auto-creates for new tenants?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Approach Implement a per-tenant calibration pipeline using a shared base model with tenant-specific parameters. ### 1. Shared Calibration Model (`analytics_base.calibration_model`) ```sql SELECT raw.tenant_id, raw.me…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a macro-driven per-tenant diagnostics flow: generate analytics_{{tenant}}.diagnostics_daily from staging.tenant_events with latency_ms, completeness_pct, and error_rate_pct per day; use MERGE f",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a cross-tenant data quality audit: add a dbt macro analytics_base.lineage_qa(source, target, keys) to validate end-to-end lineage from staging.raw_events to analytics_{{tenant}}.facts. Create",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a dbt macro that auto-generates analytics_{{tenant}}.hourly_metrics from staging.tenant_events, enforcing a data-contract stored in analytics_base.contract. The macro creates per-ten",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes macro usage, tenant isolation, data quality, and automation in a production dbt Snowflake flow.\n\n## Key Concepts\n- dbt macros generating per-tenant objects\n- Snowflake schema drift guards and 1-day late data\n- Tests: not_null on keys, unique on (tenant_id, day)\n- Global lineage table analytics.lineage; CI tenant provisioning\n\n## Code Example\n```sql\n{% macro per_tenant_diagnostics(tenant) -%}\ncreate or replace view analytics_{{tenant}}.diagnostics_daily as\nselect\n  tenant_id,\n  date_trunc('day', occurred_at) as day,\n  avg(latency_ms) as latency_ms,\n  avg(case when status = 'OK' then 1 else 0 end) * 100 as completeness_pct,\n  sum(case when error_code is not null then 1 else 0 end) / count(*) * 100 as error_rate_pct\nfrom {{ source('staging','tenant_events') }}\ngroup by tenant_id, day;\n{% endmacro -%}\n```\n\n## Follow-up Questions\n- How would you auto-provision per-tenant schemas when a new tenant is added to a registry?\n- How would you validate cross-tenant isolation in CI to prevent data bleed?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-18",
        "questionId": "q-3755",
        "question": "Design a beginner-friendly multi-tenant dbt exercise in Snowflake: for each tenant, build analytics.{tenant}.daily_summary from staging.events (tenant_id, user_id, event_type, occurred_at) enriched by dimensions.countries, producing daily counts by event_type per tenant. Implement a 2-day late data window, per-tenant isolation via schemas, and tests: not_null on tenant_id, occurred_at, event_type; unique on (tenant_id, occurred_at, event_type); a per-tenant data_contract that country_code exists in countries; and a cross-tenant leakage test ensuring analytics.{tenant} only reads its own data. Outline approach and tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a dbt macro to generate analytics_{{tenant}}.funnel_daily from staging.tenant_events, masking user_id for roles lacking FULL_ACCESS using a Snowflake policy plus deterministic hashing for jo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Macro-driven per-tenant view creation analytics_{{tenant}}.secure_metrics from staging.tenant_events, with a row-level policy using SESSION_CONTEXT('tenant_id') to restrict rows. 1-day late data, sche",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a tenant-scoped daily_summary as an incremental analytics.{tenant}.daily_summary built from staging.events (tenant_id, user_id, event_type, occurred_at) joined to dimensions.countries, with",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-tenant incremental model: analytics_{{tenant}}.daily_engagement from staging.tenant_events with a 1-day lag. Add a contract macro to validate columns against analytics_base.contract, p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical dbt setup for multi-tenant isolation, late data handling, and data quality tests.\n\n## Key Concepts\n\n- Tenant-scoped schemas\n- Incremental daily aggregation\n- Data contracts and cross-tenant leakage tests\n- Late data window and watermarking\n\n## Code Example\n\n```javascript\n-- Pseudo dbt macro usage snippet\n```\n\n## Follow-up Questions\n\n- How would you automate tenant onboarding in this pattern?\n- How would you monitor data freshness across tenants?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-19",
        "questionId": "q-26159",
        "question": "Design a beginner-friendly **per-tenant feature-flag gating** in a Snowflake/dbt setup (analytics_{{tenant}} schemas, analytics_base shared). Build: 1) a table analytics_base.feature_flags(tenant_id, flag_name, enabled), 2) a macro analytics_base.flag_filter(sql, flag_name) that injects a tenant_id filter and gates the feature when enabled, 3) apply to analytics_{{tenant}}.revenue_summary to include promo_revenue only if the flag is true, 4) tests and a CI gate that blocks production if a tenant lacks an entry or has disabled flag?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant incremental model analytics_{{tenant}}.daily_metrics built from staging.tenant_events, with a 1-day late-data window and idempotent upserts using (tenant_id, event_date, event_i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Compute health_score as a weighted sum of freshness_ok, deviation_ok, pii_ok, and stability_ok; propose macros analytics_base.calc_health, analytics_{{tenant}}.freshness, analytics_{{tenant}}.deviatio",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Add per-tenant feature flags gating in a Snowflake/dbt setup. Create analytics_base.feature_flags(tenant_id, flag_name, enabled). Implement analytics_base.flag_filter(sql, flag_name) to append a tenan",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a per-tenant incremental model analytics_{{tenant}}.daily_metrics that sources from staging.tenant_events, filtering to active tenants from the tenants registry. Implement a macro ensure_tenant_metric(tenant_id)…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical DBT multi-tenant concerns: feature flagging, macro usage, simple tests, and a CI gate, all at a beginner-friendly level.\n\n## Key Concepts\n- Per-tenant data control\n- dbt macros and Jinja\n- Feature flags table design\n- CI gates for feature rollout\n- Basic tests for presence and state\n\n## Code Example\n```sql\n-- Pseudo macro usage\n{% macro flag_filter(sql, flag_name) %}\n  {{ return(sql ~ \" AND tenant_id = \" ~ this.tenent_id ~ \" AND enabled = true\") }}\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you audit flag changes and rollout strategy?\n- How would you extend to handle multi-tenant flag ownership and overrides?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-20",
        "questionId": "q-27554",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, implement a beginner-friendly per-tenant row-level access gate using a centralized policy analytics_base.tenant_policies(tenant, role, filter). Create a macro analytics_base.rls_predicate(tenant, role) that returns a SQL predicate string, and apply it to a model analytics_{{tenant}}.sales_facts by appending a where clause. Provide a minimal SQL sketch, a per-tenant test, and describe CI gate requirement?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build a dbt macro to auto-generate tenant-specific analytics_{{tenant}}.daily_event_quality tables from staging.tenant_events, establish per-tenant baselines using analytics.daily_event_baseline, and implement cross-ten…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: implement a macro analytics_base.rls_predicate(tenant, role) returning a predicate like tenant = '{{tenant}}' AND user_role IN (SELECT role FROM analytics_base.tenant_policies WHERE tenant = '{{",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To implement, join staging.tenant_events with analytics_base.ltv_model and analytics_{{tenant}}.rates to compute analytics_{{tenant}}.ltv, materialized incrementally. Use a 2-day lateness window, a ma",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement analytics.daily_event_summary as an incremental Snowflake model: aggregate date_trunc('day', occurred_at), event_type, country_code (via regions) with count(distinct user_id). Use a macro de",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of dbt macros, centralized policy tables, and simple RBAC-style filtering at the data layer. It also checks how to propagate a policy through models with minimal changes.\n\n## Key Concepts\n- dbt macro design and usage\n- Snowflake predicate-based RBAC\n- Centralized policy tables and dynamic filters\n- Lightweight testing and CI gating for policy compliance\n\n## Code Example\n```sql\n-- analytics_base/rls_predicate.sql\n{% macro rls_predicate(tenant, role) %}\n  \"tenant\" = '{{ tenant }}' AND \"user_role\" IN (\n    SELECT role FROM analytics_base.tenant_policies\n    WHERE tenant = '{{ tenant }}' AND policy_enabled = TRUE\n  )\n{% endmacro %}\n```\n\n```sql\n-- analytics_{{ tenant }}.sales_facts\nSELECT *\nFROM analytics_{{ tenant }}.sales_facts\n{% if is_incremental() %}\nWHERE {{ analytics_base.rls_predicate(tenant, current_user_role) }}\n{% else %}\nWHERE {{ analytics_base.rls_predicate(tenant, current_user_role) }}\n{% endif %}\n```\n\n## Follow-up Questions\n- How would you unit-test the macro across multiple tenants?\n- How would you evolve policies without breaking existing tenants?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-21",
        "questionId": "q-16763",
        "question": "Design a per-tenant anomaly detection pipeline in Snowflake/dbt. From staging.tenant_events to analytics_{{tenant}}.anomalies_daily, compute per-tenant event_rate and a z-score-based anomaly flag using a per-tenant config stored in analytics_{{tenant}}.config (window_size, threshold). Implement a macro to auto-create the per-tenant schema and a global analytics.lineage entry. Include 1-day late data, schema drift guards, tests: not_null on keys, unique on (tenant_id, day), and CI checks that per-tenant config exists and anomalies are isolated by tenant?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create analytics_{{tenant}}.anomaly_weekly via a dbt macro and a Snowflake Python UDF to compute tenant-specific z-scores from staging.tenant_events, with 1-day late data. Add drift guards and tests:",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an incremental dbt model per tenant that derives churn_risk from tenant_actions and tenant_users. Upsert analytics.{tenant}.churn_risk by tenant_id and user_id, compute features (last_seen_d",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Compute per-tenant drift by comparing daily category distribution from staging.tenant_events to a per-tenant baseline in analytics_base.category_profiles. Build analytics_{{tenant}}.category_drift wit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build an incremental per-tenant anomaly detection pipeline: compute a daily event_rate per tenant from staging.tenant_events, fetch per-tenant thresholds from analytics_{{tenant}}.config via a macro, calculate z-scores…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests end-to-end implementation of per-tenant anomaly detection, macro-driven schema creation, config-driven thresholds, 1-day late data handling, schema drift guards, lineage tracking, and CI quality gates.\n\n## Key Concepts\n- **Per-tenant config**: Table analytics_{{tenant}}.config storing window_size and threshold\n- **Z-score anomaly detection**: Statistical approach using per-tenant rolling statistics\n- **Incremental models**: Efficient processing of new data per tenant\n- **Macro-driven schema creation**: Automated per-tenant schema generation for isolation\n- **Data quality**: Drift guards and CI validation\n\n## Code Example\n```sql\n-- Per-tenant anomaly detection with config-driven thresholds\nWITH tenant_stats AS (\n    SELECT \n        tenant_id,\n        event_day,\n        COUNT(*) AS event_count,\n        AVG(COUNT(*)) OVER (\n            PARTITION BY tenant_id \n            ORDER BY event_day \n            ROWS BETWEEN {{ window_size }} PRECEDING AND 1 PRECEDING\n        ) AS rolling_avg,\n        STDDEV(COUNT(*)) OVER (\n            PARTITION BY tenant_id \n            ORDER BY event_day \n            ROWS BETWEEN {{ window_size }} PRECEDING AND 1 PRECEDING\n        ) AS rolling_stddev\n    FROM {{ ref('staging_tenant_events') }}\n    WHERE event_day < CURRENT_DATE() - 1\n    GROUP BY tenant_id, event_day\n)\nSELECT \n    s.tenant_id,\n    s.event_day,\n    s.event_count AS event_rate,\n    CASE \n        WHEN s.rolling_stddev > 0 \n        THEN (s.event_count - s.rolling_avg) / s.rolling_stddev \n    END AS z_score,\n    CASE \n        WHEN s.rolling_stddev > 0 \n             AND ABS((s.event_count - s.rolling_avg) / s.rolling_stddev) > c.threshold\n        THEN TRUE \n    END AS anomaly_flag\nFROM tenant_stats s\nJOIN {{ ref('config') }} c ON s.tenant_id = c.tenant_id\n```",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-22",
        "questionId": "q-11195",
        "question": "NEW ANGLE: Tenant-aware data quality auditor. In Snowflake/dbt, design a beginner flow that creates analytics_{{tenant}}.week_visit_quality from staging.tenant_visits. Use a macro to auto-create per-tenant schemas and the quality table, compute week_start via date_trunc, aggregate week counts, and compare to prior week; flag anomalies when the delta exceeds 25%. Support 1-day late data, include schema-drift guards, tests (not_null on tenant_id, week_start; unique on (tenant_id, week_start)); add global analytics.lineage and CI to verify per-tenant tables exist and data is isolated. Explain how you would validate cross-tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Provide a dbt macro `generate_features(tenant_id, feature_name, ...)` that creates `analytics_{{tenant}}.features_{{feature_name}}` as an incremental model sourced from staging events, applying per-tenant hash-based row…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an incremental model analytics.daily_finance that upserts sum(amount) and transaction_count by day, currency, country_code, and status; allow late data within a 2-day window via a MERGE-based upse",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an incremental per-tenant daily_revenue model analytics_{{tenant}}.daily_revenue that aggregates staging.tenant_revenue_events (tenant_id, revenue_ts, amount, currency) by day and converts all amounts to USD u…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a dbt macro create_tenant_quality(tenant) that creates analytics_{{tenant}}.week_visit_quality from staging.tenant_visits, compute week_start = date_trunc('week', occurred_at), aggregate counts",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests macro-driven multi-tenant data quality, per-tenant schemas, weekly rollups, simple anomaly detection, late data handling, lineage, and CI across tenants.\n\n## Key Concepts\n- dbt macros for per-tenant objects\n- Snowflake date_trunc week\n- not_null/unique tests\n- lineage and multi-tenant isolation\n- CI checks for existence and isolation\n\n## Code Example\n```sql\n{% macro create_tenant_quality(tenant) %}\n-- Creates analytics_{{tenant}}.week_visit_quality from staging.tenant_visits\n-- with week_start and weekly counts, plus anomaly flag logic (not shown)\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you validate cross-tenant data leakage in CI?\n- How would you scale this approach to hundreds of tenants?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-23",
        "questionId": "q-17988",
        "question": "In a multi-tenant dbt project in Snowflake, design a beginner-level data quality workflow that computes a per-tenant daily quality score (0-100) for analytics_{{tenant}}.quality_metrics using a macro applied to staging.events and seeds.tenants. Include 1-day late data, components: completeness (not_null on tenant_id, event_type, occurred_at), timeliness (days_lag <= 1), and consistency (no duplicate (tenant_id, day)). Create tests not_null and between 0-100; a global analytics.lineage. Add a CI gate that fails if any tenant is missing yesterday's score or if a new tenant exists without a score. Explain how you'd validate cross-tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a per-tenant governance path: a central tenants registry triggers auto-provisioning of analytics_{{tenant}} schemas; a macro creates analytics_{{tenant}}.daily_snapshot from staging.tenant_e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant daily quality score using a simple macro-driven approach. Compute score as 40% completeness (not_null on tenant_id, event_type, occurred_at), 40% timeliness (max occurred_at wit",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage a single incremental model analytics.hourly_metrics across tenants, using a macro to resolve tenant_schema(tenant_id) for source and destination. Compute hour_start as date_trunc('hour', occu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a macro redaction_policy(tenant_id) that reads a central tenant_config(redact_pii). If true, return masking expressions for email and phone; otherwise pass-through. In analytics.{tenant}.hou",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design a beginner-level data quality workflow with per-tenant scope, macro usage, and CI guards.\n\n## Key Concepts\n- Macro-driven per-tenant metrics\n- Incremental daily modeling\n- Data quality tests and CI gates\n- Cross-tenant isolation via schemas/roles\n\n## Code Example\n```sql\n-- macro: macros/quality_score.sql\n{% macro per_tenant_quality_score(tenant) %}\n  -- compute completeness, timeliness, consistency\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you adapt scoring for changing data patterns or late arrivals beyond 1 day?\n- How to monitor scores over time and alert on drift?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-24",
        "questionId": "q-5003",
        "question": "Design a beginner dbt flow in Snowflake to compute analytics.sales.daily_user_spend from staging.orders (order_id, user_id, amount, created_at) and staging.users (user_id). Aggregate by user_id and date(created_at) to compute total_spend and order_count. Allow 1-day late data; tests: not_null on user_id and order_date; unique on (user_id, order_date). Snapshot analytics.sales.users for first_order_date. Explain local run and CI isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: implement a macro analytics_base.rls_predicate(tenant, role) returning a predicate like tenant = '{{tenant}}' AND user_role IN (SELECT role FROM analytics_base.tenant_policies WHERE tenant = '{{",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement analytics.sales.daily_user_spend as an incremental model keyed by (user_id, order_date), joining staging.orders with staging.users as needed. Compute total_spend = sum(amount) and order_count = count(*). Confi…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a dbt macro that materializes per-tenant analytics_{{tenant}}.enriched_events views by joining staging.tenant_events to analytics.enrichment_policies on tenant_id and field, applying dynamic masking/redaction…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a tenant-aware drift-detection flow in Snowflake/dbt. Use a macro to auto-generate analytics_{{tenant}}.stats from staging.tenant_events and populate analytics.lineage with hourly deltas agains",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests building a small, practical dbt flow with incremental logic, late data tolerance, and basic quality checks. It also covers simple snapshot usage to audit user history and CI isolation for environments.\n\n## Key Concepts\n- Incremental materializations in dbt\n- Late-arriving data handling with date window\n- Basic tests: not_null and unique\n- Snapshots for slowly changing data\n- Environment isolation via per-env targets/schemas\n\n## Code Example\n```sql\n-- dbt model: analytics.sales.daily_user_spend\nwith orders as (\n  select \n    order_id, \n    user_id, \n    amount, \n    created_at::date as order_date\n  from {{ ref('staging.orders') }}\n),\nfinal as (\n  select \n    user_id,\n    order_date,\n    sum(amount) as total_spend,\n    count(*) as order_count\n  from orders\n  where order_date >= dateadd(day, -1, '{{ var('run_date') }}')\n  group by user_id, order_date\n)\nselect * from final\n```\n\n## Local vs CI Isolation\nLocal runs use dev schema/target; CI uses separate schema with clean state to prevent cross-environment contamination.",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-19T09:42:17.498Z",
    "version": 1
  }
]