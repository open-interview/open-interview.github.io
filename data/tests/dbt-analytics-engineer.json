[
  {
    "id": "test-dbt-analytics-engineer",
    "channelId": "dbt-analytics-engineer",
    "channelName": "DBT Analytics Engineer",
    "title": "DBT Analytics Engineer Knowledge Test",
    "description": "Test your understanding of DBT Analytics Engineer with 25 questions.",
    "questions": [
      {
        "id": "dbt-analytics-engineer-q-0",
        "questionId": "q-25085",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, implement a per-tenant ingestion-lag monitoring and alerting framework. Requirements: (1) a macro analytics_base.ingest_lag(tenant, table, watermark_col) that computes hours of lag against a centralized source timestamp, (2) a per-tenant TASK analytics_{{tenant}}.ingest_lag_hourly that materializes analytics_base.ingest_lag_summary, (3) a CI gate that blocks production if any tenant's lag exceeds 6 hours for 3 consecutive days, (4) tests (not_null, max) and a minimal schema. Provide macro names and a sketch of the schema?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build a macro that on a new tenant creates analytics_{tenant} schemas and hourly_engagement incremental models, plus a Snowflake stream for staging.tenant_events. Use a global analytics_lineage table",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline an incremental, per-tenant cohort funnel with differential privacy. Use a macro to generate analytics_{{tenant}}.cohort_funnel and a per-tenant epsilon budget tracked in analytics_privacy; app",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline an on-demand repair for late-arriving events up to 3 days. Use a per-tenant MERGE from staging.tenant_events into analytics_{{tenant}}.facts with an idempotent key; implement via analytics_bas",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant ingestion-lag monitor using a base table analytics_base.ingest_lag with columns tenant, table_name, max_lag_hours, snapshot_ts. Implement a macro analytics_base.ingest_lag(tenant,",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nNew angle: ingestion-lag monitoring across tenants with centralized governance.\n\n## Key Concepts\n- dbt macros, Snowflake Tasks, per-tenant isolation, CI gates, ingestion latency.\n- Cross-tenant governance via analytics_base tables.\n\n## Code Example\n```sql\n{% macro ingest_lag(tenant, table, watermark_col) %}\n  SELECT '{{tenant}}' AS tenant,\n         '{{table}}' AS table_name,\n         MAX(CASE WHEN {{ watermark_col }} IS NOT NULL THEN TIMESTAMPDIFF(HOUR, {{ watermark_col }}, CURRENT_TIMESTAMP()) END) AS max_lag_hours\n  FROM analytics_{{tenant}}.{{table}};\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How to handle late-arriving data?\n- How to scale the CI gate across many tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-1",
        "questionId": "q-14683",
        "question": "NEW ANGLE: Build a beginner-friendly per-tenant data-quality layer that auto-creates analytics_{{tenant}}.daily_summary from staging.tenant_events via a macro driven by registry.tenants. Include 1-day late data, per-tenant schema drift guards, and tests (not_null on keys, unique on (tenant_id, day_start, summary_id)); a global analytics.lineage; and CI validating all tenants have daily_summary and auto-create for new tenants. Explain cross-tenant bleed prevention in CI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant reconciliation and drift-detection pipeline in Snowflake/dbt that compares `analytics_{{tenant}}.user_history` (tenant_id, user_id, event, occurred_at) against `staging.users` (user_id, last_seen, st…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a tenant-scoped macro to create analytics_{{tenant}} schemas and analytics_{{tenant}}.hourly_engagement_funnel from staging.events, plus a global analytics.lineage. Implement incremental by h",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Compute health_score as a weighted sum of freshness_ok, deviation_ok, pii_ok, and stability_ok; propose macros analytics_base.calc_health, analytics_{{tenant}}.freshness, analytics_{{tenant}}.deviatio",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a macro `tenant_summary()` that reads from `registry.tenants`, dynamically builds per-tenant `analytics_{{tenant}}.daily_summary` tables from `staging.tenant_events` with a 1-day data lag, and registers lineage…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests the candidate's ability to automate tenant provisioning and quality checks using a registry-driven macro, plus basic lineage tracking and CI integration.\n\n## Key Concepts\n- dbt macros and Jinja templating\n- Per-tenant schemas with dynamic object naming\n- 1-day data lateness handling\n- Basic tests: not_null constraints, unique constraints\n- Global lineage tracking and CI validation\n\n## Code Example\n```jinja\n-- dbt macro: macros/tenant_summary.sql\n{% macro tenant_summary(tenant) -%}\n  select\n    tenant_id,\n    date_trunc('day', occurred_at) as day_start,\n    count(*) as event_count,\n    summary_id\n  from {{ source('staging', 'tenant_events') }}\n  where tenant_id = '{{ tenant }}'\n    and occurred_at >= dateadd(day, -1, current_date)\n  group by tenant_id, day_start, summary_id\n{%- endmacro %}\n\n-- Dynamic model execution\n{% for tenant in api.Relation.create(database=var('database'), schema='registry', identifier='tenants') %}\n{{ dbt_utils.create_table_as(\n    relation=api.Relation.create(database=var('database'), schema='analytics_' + tenant, identifier='daily_summary'),\n    sql=tenant_summary(tenant)\n) }}\n{% endfor %}\n```\n\n## Cross-Tenant Bleed Prevention in CI\n- Use separate database schemas per tenant (`analytics_{{tenant}}`) to ensure data isolation\n- Validate that `tenant_id` values in each table match the target schema prefix\n- Run CI tests that cross-check schema names against `registry.tenants` to detect misconfigurations\n- Implement row-level security checks ensuring queries only return data for the intended tenant",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-2",
        "questionId": "q-17207",
        "question": "Design a per-tenant incremental dbt flow in Snowflake that normalizes divergent staging schemas into a canonical analytics_{{tenant}}.normalized_events with columns (tenant_id, event_time, event_type, payload). Implement a mapping macro that aligns tenant-specific fields to the canonical schema, allowing new tenants with minimal changes. Backfill 1 day of data. Include schema drift guards, tests: not_null on keys, unique on (tenant_id, event_time, event_type), a global analytics.lineage, and CI checks that every per-tenant normalization exists and data is isolated. Explain how you would validate cross-tenant schema compatibility in CI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-tenant identity unification using a dbt macro hash_user_id that applies HMAC-SHA256 with a per-tenant salt from analytics_{{tenant}}.salts to staging_tenant_events.user_id, materialize a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement analytics_base.redact_email(tenant, email) using sha256(email||tenant_salt) and return a fixed-length hex; build analytics_{{tenant}}.cohort_retention as an incremental model from staging.te",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant daily_quality_metrics flow: use a macro to create analytics_{{tenant}} schemas, ingest 1-day-late data from staging.quality_events into analytics_{{tenant}}.daily_quality_metric",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant normalization macro normalize_events that maps tenant-specific fields to a canonical (tenant_id, event_time, event_type, payload) and materializes analytics_{{tenant}}.normalize",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to unify heterogeneous tenant schemas, maintain performance with incremental models, and validate isolation in CI.\n\n## Key Concepts\n- dbt macros for dynamic field mapping\n- per-tenant incremental normalization\n- canonical schema design and payload storage\n- schema drift guards and lineage\n\n## Code Example\n```sql\n-- example macro\n{% macro normalize_fields(tenant) -%}\nSELECT\n  COALESCE(t.tenant_id, '{{' || tenant || '}}')::int AS tenant_id,\n  t.event_time AS event_time,\n  t.event_type AS event_type,\n  to_json(t.payload) AS payload\nFROM {{ source('staging', 'tenant_events_' ~ tenant) }} t\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you test onboarding of a brand-new tenant with no historical data?\n- How would you evolve canonical schema without breaking existing tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-3",
        "questionId": "q-16716",
        "question": "Design a **per-tenant cost governance flow** in Snowflake/dbt: from staging.tenant_events to analytics_{{tenant}}.query_costs_daily. Build a macro **cost_guard** that estimates credits using warehouse, duration, and data scanned; gate expensive queries; enforce a data contract on columns/types; include a **2 hour late data window**, schema drift guards, tests (not_null on tenant_id, day_start, cost_id; unique on (tenant_id, day_start, cost_id)); a global analytics.lineage; CI validating per tenant views exist and budgets align with a global quota. Explain how you'd test isolation and budget enforcement in CI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a macro-driven per-tenant dbt flow that reads staging.events (tenant_id, user_id, event_type, email, ip_address, occurred_at) and loads analytics_{{tenant}}.hourly_events. Enforce privacy: if p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant incremental model analytics_{{tenant}}.daily_metrics built from staging.tenant_events, with a 1-day late-data window and idempotent upserts using (tenant_id, event_date, event_i",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a macro-driven per-tenant diagnostics flow: generate analytics_{{tenant}}.diagnostics_daily from staging.tenant_events with latency_ms, completeness_pct, and error_rate_pct per day; use MERGE f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a dbt macro `cost_guard` that reads a per-tenant budgets table and estimates credits per query using warehouse size, runtime duration, and bytes scanned; fail builds when usage would exceed the tenant's alloca…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design comprehensive cost governance in a multi-tenant dbt/Snowflake environment. It covers macro-driven credit estimation, per-tenant budget enforcement, data contract validation, schema drift detection, lineage tracking, and CI-based isolation validation.\n\n## Key Concepts\n- **dbt macros** for runtime cost estimation and query gating\n- **Per-tenant budgets** with Snowflake credit consumption tracking\n- **Data contracts** enforcing column names, types, and constraints\n- **Schema drift guards** detecting unexpected schema changes\n- **Global lineage** for dependency tracking across tenants\n- **CI validation** ensuring isolation and budget alignment\n\n## Testing Isolation and Budget Enforcement in CI\n1. **Isolation Testing**: Create integration tests that query each tenant's data from the shared staging table and verify no tenant can access another tenant's data. Run cross-tenant queries and assert they return empty or raise errors.\n2. **Budget Enforcement Testing**: Mock the budgets table with known limits, execute queries, and verify the `cost_guard` macro correctly passes or fails based on simulated credit consumption. Use dbt's `flags.WHICH` to skip cost guards in local development while enforcing in CI/production.\n3. **View Existence Validation**: Query `information_schema.views` to confirm all `analytics_{{tenant}}.query_costs_daily` views exist for tenants defined in the global config.\n4. **Quota Alignment**: Sum all tenant budgets from the budgets table and assert the total equals the global quota defined in configuration.",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-4",
        "questionId": "q-21478",
        "question": "Design a beginner dbt pipeline to compute per-tenant daily revenue from staging.sales_events by tenant. Build an incremental model analytics_{{tenant}}.daily_revenue that aggregates revenue by day using event_date. Implement 1-day late data by excluding the current day in the incremental filter. Include macro enforce_contract() to validate analytics_base.contract column names/types and fail CI on drift. Add tests not_null(tenant_id, day, revenue) and unique(tenant_id, day)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant anomaly profiling: build incremental analytics_{{tenant}}.anomaly_daily from staging.tenant_events. A macro creates per-tenant schemas, computes rolling 7-day mean and stddev for the metric",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a macro `analytics_base.validate_schema(tenant)` that reads `information_schema` for both `analytics_{{tenant}}.fct` and `analytics_base.fct`, computes a JSON diff of columns, data types, and nullabili…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Aggregate revenue by tenant and day from staging.sales_events into analytics_{{tenant}}.daily_revenue via an incremental model with unique_key (tenant_id, day). Use day = date_trunc('day', event_date)",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Proposed: implement qa_latency_ri to compute max delay between ingest_ts and event_ts using QUERY_HISTORY; build analytics_{{tenant}}.qa_summary incrementally (tenant, src_table, dim_table, latency_da",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis checks beginners' grasp of per-tenant denormalization, incremental loads with late data, and data-contract validation. It also touches tests and CI gating.\n\n## Key Concepts\n\n- Incremental models with unique_key\n- Date handling for late data\n- Macros for schema contracts\n- Tests for data quality\n\n## Code Example\n\n```sql\n-- example incremental model skeleton\nwith src as (\n  select tenant_id,\n         date_trunc('day', event_date) as day,\n         sum(revenue) as revenue\n  from {{ ref('staging_sales_events') }}\n  group by 1,2\n)\nselect * from src\n{% if is_incremental() %}\nwhere day <= dateadd(day, -1, current_date())\n{% endif %}\n```\n\n## Follow-up Questions\n\n- How would you adapt if revenue could be negative due to refunds?\n- How would you handle new tenants without breaking contracts?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-5",
        "questionId": "q-26159",
        "question": "Design a beginner-friendly **per-tenant feature-flag gating** in a Snowflake/dbt setup (analytics_{{tenant}} schemas, analytics_base shared). Build: 1) a table analytics_base.feature_flags(tenant_id, flag_name, enabled), 2) a macro analytics_base.flag_filter(sql, flag_name) that injects a tenant_id filter and gates the feature when enabled, 3) apply to analytics_{{tenant}}.revenue_summary to include promo_revenue only if the flag is true, 4) tests and a CI gate that blocks production if a tenant lacks an entry or has disabled flag?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Add per-tenant feature flags gating in a Snowflake/dbt setup. Create analytics_base.feature_flags(tenant_id, flag_name, enabled). Implement analytics_base.flag_filter(sql, flag_name) to append a tenan",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-tenant external share uses analytics_contract_{{tenant}} to enforce exact column names/types and a macro to materialize analytics_{{tenant}}.shared_view from staging.tenant_events. Implement a 2-d",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant schemas in Snowflake: implement analytics.daily_metrics as an incremental model scoped to analytics.${tenant}. daily by (day, tenant_id, event_type). Late data window: 2 days; use MERGE int",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A multi-tenant, privacy-preserving daily cohort retention model in Snowflake/dbt. Ingest staging.tenant_signups and staging.tenant_events, hash user_id with a tenant salt to form user_hash, compute co",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical DBT multi-tenant concerns: feature flagging, macro usage, simple tests, and a CI gate, all at a beginner-friendly level.\n\n## Key Concepts\n- Per-tenant data control\n- dbt macros and Jinja\n- Feature flags table design\n- CI gates for feature rollout\n- Basic tests for presence and state\n\n## Code Example\n```sql\n-- Pseudo macro usage\n{% macro flag_filter(sql, flag_name) %}\n  {{ return(sql ~ \" AND tenant_id = \" ~ this.tenent_id ~ \" AND enabled = true\") }}\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you audit flag changes and rollout strategy?\n- How would you extend to handle multi-tenant flag ownership and overrides?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-6",
        "questionId": "q-2315",
        "question": "Design a beginner-friendly per-tenant daily_session_summary in Snowflake/dbt. Source: staging.sessions(tenant_id, user_id, session_start). Build analytics.{tenant}.daily_session_summary counting distinct users per tenant per day, with enrichment from dimensions.tenants (plan, region). Implement late data tolerance of 1 day, schema-drift guards, tests (not_null, unique). Include a data-contract macro to validate staging.sessions fields/types and a snapshot on analytics.{tenant}.users. Explain how you ensure cross-tenant isolation and lineage in dashboards?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant incremental hourly_metrics model that extracts event_type from payload.eventType (fallback to payload.type) and aggregates active_users by tenant_id/hour_start/event_type. Handl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I’d implement a per-tenant near-real-time anomaly pipeline using a Snowflake stream on staging.tenant_events and a dbt model analytics_{{tenant}}.anomalies that computes a rolling 15-minute z-score ag",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant daily session summary in dbt: an incremental model analytics.{tenant}.daily_session_summary using a tenant-scoped macro to reference staging.sessions. Define unique key as (tena",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant reconciliation: implement analytics_{{tenant}}.reconciled_events as an incremental upsert of staging.tenant_events joined to analytics_base.canonical_events after applying analytics_{{tenan",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nAssesses ability to design an end-to-end per-tenant metric with incremental logic, data contracts, and snapshots, plus isolation and lineage considerations.\n\nKey Concepts\n- Per-tenant isolation and lineage\n- Incremental models and late data tolerance\n- Data-contract macros and schema-drift tests\n- Snapshots to capture cohort changes\n\nCode Example\n```sql\nwith s as (\n  select tenant_id, date_trunc('day', session_start) as session_date, user_id\n  from {{ ref('staging_sessions') }}\n  where session_start is not null\n)\nselect tenant_id, session_date, count(distinct user_id) as active_users\nfrom s\ngroup by 1,2\n```\n\nFollow-up Questions\n- How would you validate that late sessions arrive within the 1-day window?\n- How would you handle tenants added after initial deployment (new analytics.{tenant} schemas)?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-7",
        "questionId": "q-22658",
        "question": "Design a per-tenant governance and lineage validation pipeline in Snowflake/dbt: analytics_{{tenant}}.staging_events feeds analytics_{{tenant}}.facts via a shared analytics_base.mappings, with a macro analytics_base.validate_lineage that inspects dbt artifacts for cross-tenant dependencies. Implement 2-day late data, per-tenant PII masking, RLS isolation, and a CI gate that blocks production if new cross-tenant dependency is introduced without approval. Include tests not_null on keys and unique(tenant_id, day) and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant governance check by introducing a macro analytics_base.validate_lineage that parses dbt manifest and ensures no unapproved cross-tenant dependencies are added. Build analytics_{",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use an incremental model with is_incremental(). Join staging events to pricing.exchange_rates on date and currency, then aggregate revenue in USD by date, city, ride_type. Implement a 2-day late-data",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant retention policy table analytics_{{tenant}}.policy with fields retention_days and on_hold. Build a dbt macro purge_old that deletes data older than retention_days from analytics",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Define analytics_base.dq_gate(tenant) that returns: customer_id IS NOT NULL AND order_total >= 0 AND currency_code IN (SELECT code FROM analytics_base.currency_dim). Create a dbt test on analytics_{{t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to enforce cross-tenant governance, lineage checks, and privacy controls in a dbt/Snowflake pipeline under real-world constraints.\n\n## Key Concepts\n- dbt manifest lineage validation macro\n- per-tenant schemas and RBAC/RLS\n- late-arrival windowing and masking policies\n- CI gates tied to lineage drift\n\n## Code Example\n```javascript\n// pseudo macro sketch\n{% macro validate_lineage() %}\n  // parse manifest.json, detect new dependencies across tenants\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you extend to handle dynamic tenant onboarding without breaking CI?\n- How would you test masking policies across roles in Snowflake?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-8",
        "questionId": "q-24135",
        "question": "In a Snowflake/dbt setup with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a data-quality and reconciliation pipeline that ensures event_ids are unique per tenant, supports idempotent upserts from staging.tenant_events, and reconciles 2-day-late data without duplicates. Propose a macro analytics_base.has_idempotent_upsert, a test suite (not_null, unique), and a CI gate that blocks prod if daily_event_count drift exceeds 5% for 2 consecutive days. Outline schema changes and where reconciliation metadata lives?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a macro tenant_summary that writes analytics_{{tenant}}.daily_signup_summary from staging.tenant_signups. Include tenant_id, signup_date, new_users. Allow 1-day late data with a separate lat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an incremental model with is_incremental(). Join staging events to pricing.exchange_rates on date and currency, then aggregate revenue in USD by date, city, ride_type. Implement a 2-day late-data",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant idempotent upsert and reconciliation: use a hash-based key on (tenant, event_id) and upsert from staging.tenant_events into analytics_{{tenant}}.facts; implement a macro analytics_",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a per-tenant sanitized session summary pipeline in Snowflake/dbt: from staging.tenant_sessions to analytics_{{tenant}}.sanitized_session_summary daily; use a macro to mask user_id and session_i",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design robust idempotent upserts, late-data reconciliation, and governance hooks in a multi-tenant dbt environment.\n\n## Key Concepts\n- Idempotent upserts across retries and late arrivals\n- Per-tenant isolation with shared base macros\n- Data quality tests and CI gates tying data health to deployments\n- Reconciliation metadata storage for observability\n\n## Code Example\n```javascript\n// Pseudo-dn macro sketch for has_idempotent_upsert\nfunction has_idempotent_upsert(tenant, id, keys){\n  // compare staging vs target for duplicates and missing keys\n  return true;\n}\n```\n\n## Follow-up Questions\n- How would you backfill reconciled records without double-counting? \n- How would you extend tests to cover cross-tenant offsets in timestamps?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-9",
        "questionId": "q-6996",
        "question": "Design a beginner dbt flow for per-tenant funnel analytics. Use a macro to create analytics_{{tenant}}.daily_funnel with (day_start, stage, user_count) from staging.funnel_events(tenant_id, user_id, stage, occurred_at) covering signup/view/add_to_cart/purchase. Build an incremental model with a 1-day late window, tests not_null on tenant_id/day_start/stage and unique on (tenant_id, day_start, stage). Create a global analytics.lineage. Add CI to ensure analytics_{{tenant}}.daily_funnel exists for all tenants and auto-creates for new tenants. Explain cross-tenant isolation validation in CI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a macro-driven per-tenant dbt flow that reads staging.events (tenant_id, user_id, event_type, email, ip_address, occurred_at) and loads analytics_{{tenant}}.hourly_events. Enforce privacy: if p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a macro that creates analytics_{{tenant}}.daily_funnel with day_start, stage, user_count. Build an incremental model over staging.funnel_events to produce daily counts for signup, view, add_",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Macro-driven per-tenant view creation analytics_{{tenant}}.secure_metrics from staging.tenant_events, with a row-level policy using SESSION_CONTEXT('tenant_id') to restrict rows. 1-day late data, sche",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a dbt macro drift_check that compares per-column metadata (data type, nullability, default, and max length) between a source table and its corresponding model, plus a runtime comparison of r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nGauges macro-driven per-tenant analytics, incremental models, late data handling, tests, lineage, and CI automation for multi-tenant isolation.\n\n## Key Concepts\n\n- dbt macros for per-tenant objects\n- incremental models with late data window\n- tests: not_null, unique\n- global analytics.lineage\n- CI for tenant provisioning and isolation\n\n## Code Example\n\n```javascript\n{% macro daily_funnel_sql(tenant) -%}\nselect '{{tenant}}' as tenant_id, date_trunc('day', occurred_at) as day_start, stage,\n       count(distinct user_id) as user_count\nfrom {{ source('staging','funnel_events') }}\nwhere tenant_id = '{{tenant}}'\ngroup by 1,2,3\n{%- endmacro %}\n```\n\n## Follow-up Questions\n\n- How would you test that a new tenant triggers creation of analytics_{{tenant}}.daily_funnel?\n- How would you scale the macro to handle missing stage values?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-10",
        "questionId": "q-7312",
        "question": "Design a per-tenant, incremental dbt flow in Snowflake to compute analytics.{tenant}.anomaly_summary from staging.events (tenant_id, user_id, event_type, value, occurred_at, predicted_anomaly_score). Build via a macro that creates analytics_{tenant} schemas and analytics_{tenant}.anomaly_summary, plus a global analytics.lineage table. Include 1-day late data, schema-drift guards, tests (not_null on keys, not_null on predicted_anomaly_score, unique on (tenant_id, day_start, event_type)), and a per-tenant users snapshot. Explain cross-tenant isolation validation and anomaly lineage auditing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe a per-tenant incremental flow that creates analytics_{tenant} schemas and analytics_{tenant}.anomaly_summary from staging.events, including predicted_anomaly_score. Use a macro for schema cre",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a macro analytics_base.compare_counts(tenant,left_table,right_table,keys) returning drift_pct by comparing row counts and key-level aggregates; add per-tenant analytics_{{tenant}}.snapshot_a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an analytics_base.run_metrics table keyed by (tenant, run_id, started_at) and a macro analytics_base.emit_run_metrics(tenant, run_id, metrics_json) that inserts a JSON payload and updates a me",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a dbt macro that generates analytics_{{tenant}}.daily_anomaly_score, using a CTE to count daily events by tenant, then compute z = (count - avg(count) over 7 days for tenant) / stddev(...) over",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests multi-tenant data governance when ML-influenced signals drive analytics. It emphasizes dynamic schema creation, robust lineage, and isolation checks in CI.\n\n## Key Concepts\n- Macro-driven per-tenant schemas and tables\n- Incremental daily anomalySummary with ML scores\n- Global analytics.lineage for cross-tenant tracing\n- Schema drift guards and data-quality tests\n\n## Code Example\n```jinja\n{% macro create_anomaly_schema(tenant) %}\n  {% set tbl = 'analytics_' ~ tenant ~ '.anomaly_summary' %}\n  -- dbt macro to initialize tenant schema objects\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you validate cross-tenant isolation in CI?\n- How would you monitor anomaly_score drift across tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-11",
        "questionId": "q-22723",
        "question": "Design a multi-tenant dbt/Snowflake pipeline that handles per-tenant schema evolution with rolling updates: tenants append new fields to staging_{{tenant}}.events; analytics_base.mappings tracks per-tenant columns; implement a generic analytics_base.sync_schema macro that detects missing columns in analytics_{{tenant}}.facts and issues ALTER TABLE analytics_{{tenant}}.facts ADD COLUMN with the correct type, preserving existing data. Include 1-day late data, per-tenant RLS isolation, and a CI gate that blocks production if a new column is proposed without approval. Include tests not_null on keys and unique(tenant_id, day) and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a tenant-aware macro to render analytics.{tenant}.daily_user_event_count from staging.events, with an incremental key (tenant_id, date(occurred_at), event_type). Late data 1 day via",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: build a cross-tenant LTV framework using cohort-based revenue in Snowflake/dbt. Implement analytics_base.cohort_key(event_date, cohort_month) to assign users to cohorts; analytics_{{tenant}}",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A macro `generate_alerts(tenant)` materializes `analytics_{{tenant}}.alerts` from `staging.tenant_events` and `analytics_base.alert_rules`. The macro reads global alert rules (containing `rule_id`, `severity`, `sql_cond…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a multi-tenant dbt/Snowflake approach for schema evolution: tenants drop new fields into staging_{{tenant}}.events; use analytics_base.mappings to drive per-tenant analytics_{{tenant}}.facts c",
            "isCorrect": true
          }
        ],
        "explanation": "Why This Is Asked\nTests ability to design a maintainable multi-tenant schema-evolution strategy with a centralized capability.\n\n## Key Concepts\n- dbt macros for dynamic schema evolution\n- Per-tenant isolation via Snowflake RLS\n- Central mappings driving tenant-specific tables\n- Safe, idempotent ALTER operations preserving data\n- Governance via CI gates and tests\n\n## Code Example\n```sql\n-- dbt macro skeleton\n{% macro sync_schema(tenant) -%}\n  -- pseudo: fetch staging columns, compare to analytics_{{tenant}}.facts\n  -- if missing, ALTER TABLE analytics_{{tenant}}.facts ADD COLUMN …\n{%- endmacro %}\n```\n\n## Follow-up Questions\n- How would you test this macro in CI without touching production?\n- How would you handle column deletions or type conflicts across tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-12",
        "questionId": "q-21036",
        "question": "In Snowflake/dbt, implement a beginner per-tenant daily_active_users pipeline: from staging.tenant_events to analytics_{{tenant}}.dau with 1-day lag, incremental materialization, and a deduplicated key (tenant_id, user_id, day). Include a macro enforce_minimum_volume() that prevents materialization when per-tenant DAU < 50, and tests not_null(tenant_id, day, user_id) and unique(tenant_id, day, user_id). Add a CI gate that fails if any tenant’s DAU moves >30% day-over-day. Provide a simple lineage sketch and notes on data isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement analytics_base.qc_aggregate(tenant) as a macro that parses manifest.json and run_results.json to populate analytics_base.quality_{{tenant}} with fields: tenant, model, test_name, status, and score. Define per-…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design per-tenant dau incrementally from staging.tenant_events with 1-day lag, dedup by (tenant_id, user_id, day). Macro enforce_minimum_volume() blocks tenants with dau < 50. Tests: not_null(tenant_i",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I’d implement a per-tenant cohort pipeline: a tenant-scoped macro hashes user_id to a k-anonymized_id, then aggregate retention cohorts in analytics_{{tenant}}.cohorts from staging.tenant_user_events.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant macro to create analytics_{{tenant}}.daily_user_engagement and backfill with a Snowflake stream on staging.tenant_events; perform an incremental MERGE by day_start with a dedup key (t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical dbt usage for a common metric, emphasizing per-tenant isolation, incremental builds, and data quality gates that are manageable at a beginner level.\n\n## Key Concepts\n- Incremental models and deduplication\n- Macros for gatekeeping and data validation\n- Basic tests (not_null, unique)\n- Simple CI data-drift guard per tenant\n\n## Code Example\n```sql\n-- models/analytics/{{tenant}}/dau.sql\nwith s as (\n  select tenant_id, user_id, date_trunc('day', event_time) as day\n  from {{ source('staging','tenant_events') }}\n  where event_type = 'login'\n)\nselect tenant_id, day, count(distinct user_id) as dau\nfrom s\ngroup by 1,2\n```\n\n## Follow-up Questions\n- How would you handle backfills when 2–3 days of events arrive late?\n- How would you test macro behavior across multiple tenants with varying data volumes?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-13",
        "questionId": "q-13827",
        "question": "Create analytics_{{tenant}}.anomaly_weekly via a dbt macro and a Snowflake Python UDF to compute tenant-specific z-scores from staging.tenant_events, with 1-day late data. Include drift guards and tests: not_null on keys, unique on (tenant_id, week_start, entity_id). Build global analytics.lineage and CI checks for per-tenant isolation. Use masking policies to protect user_id for restricted roles and verify no cross-tenant bleed?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-tenant identity unification using a dbt macro hash_user_id that applies HMAC-SHA256 with a per-tenant salt from analytics_{{tenant}}.salts to staging_tenant_events.user_id, materialize a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-tenant reconciliation and drift-detection pipeline in Snowflake/dbt that compares `analytics_{{tenant}}.user_history` (tenant_id, user_id, event, occurred_at) against `staging.users` (user_id, last_seen, st…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create analytics_{{tenant}}.anomaly_weekly via a dbt macro and a Snowflake Python UDF to compute tenant-specific z-scores from staging.tenant_events, with 1-day late data. Add drift guards and tests:",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Incrementally populate analytics.daily_revenue by date and country_code from staging.sales_events(amount). The macro validates country_code against the seeded countries table and fails CI if a new cod",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests building a privacy-aware, cross-tenant analytics path with per-tenant isolation, drift checks, and robust tests.\n\n## Key Concepts\n- dbt macros for tenant scaffolding\n- Snowflake Python UDF for per-tenant z-scores\n- masking policies and RLS for data protection\n- drift guards, lineage, CI validation\n\n## Code Example\n```sql\nCREATE FUNCTION z_score(val FLOAT, mean FLOAT, stddev FLOAT) RETURNS FLOAT\nLANGUAGE JAVASCRIPT\nAS $$ (val - mean) / stddev $$;\n```\n\n## Follow-up Questions\n- How would you audit privacy and confirm no cross-tenant bleed in production?\n- How would you scale the UDF and tenant creation as tenants grow?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-14",
        "questionId": "q-19014",
        "question": "In Snowflake/dbt, given a tenants registry table tenants(tenant_id, status) and a raw log table staging.events with (tenant_id, event_time, user_id, action), design a beginner dbt flow that uses a macro to auto-create per-tenant analytics_{{tenant}}.daily_engagement by aggregating 1 day of events from staging.events. The flow should support 1-day late data, include a simple data quality macro and tests (not_null on tenant_id, event_time; unique on (tenant_id, event_time, user_id)), and a CI guard that verifies all active tenants have analytics_{{tenant}}.daily_engagement and that new tenants appear automatically. Explain how you'd validate this in CI and ensure isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create an incremental analytics.daily_engagement that counts distinct user_id per day, grouped by event_type and country_code, joining staging.events with staging.users and dimensions.countries. Allow",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a drift- and quality-monitor macro that fingerprints each analytics_{{tenant}} schema and compares against a stored baseline; on drift or quality drop beyond a threshold, emit a tenant alert a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a macro tenant_model(tenant_id) that generates analytics_{{tenant_id}}.daily_engagement for each active tenant. Build an incremental model from staging.events with day_start = date(event_time)",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Outline an on-demand repair for late-arriving events up to 3 days. Use a per-tenant MERGE from staging.tenant_events into analytics_{{tenant}}.facts with an idempotent key; implement via analytics_bas",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to automate per-tenant analytics objects via macros, driven by a registry, plus CI guards for data quality and isolation.\n\n## Key Concepts\n- dbt macros for dynamic object naming\n- incremental models with late-arriving data\n- data quality tests and CI checks\n- basic tenancy isolation via access policies\n\n## Code Example\n```javascript\n{% macro tenant_model(tenant_id) %}\n  analytics_${tenant_id}.daily_engagement\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you handle schema drift across tenants?\n- How would you extend tests to catch late data gaps?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-15",
        "questionId": "q-22874",
        "question": "Design a beginner dbt workflow for a single-tenant daily_action_metric: from staging.events, create analytics_{{tenant}}.daily_actions_incremental using an incremental model that sums actions per user per day with a 1-day data lateness. Use a shared analytics_base.mask_pii macro for restricted roles, enforce dedup with a unique(tenant_id, day, event_id) seed, and add tests not_null(user_id, day, event_id) and a simple comparison to a seed. Explain handling of late data and how you would scale to multi-tenant?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: implement an incremental dbt model analytics_{{tenant}}.cost_summary sourcing from staging.tenant_cost_events, governed by analytics_base.cost_model with per-tenant analytics_{{tenant}}.cost_sca",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a tenants registry to auto-generate per-tenant analytics schemas and an incremental model analytics.{tenant}.interactions from staging.events. Late data tolerance: 2 days. Use a tenant-aware",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage a tenant-scoped macro to create analytics_{{tenant}} schemas and analytics_{{tenant}}.hourly_engagement_funnel from staging.events, plus a global analytics.lineage. Implement incremental by h",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement analytics_{{tenant}}.daily_actions_incremental from staging.events. Use an incremental model that aggregates actions by tenant, user, and day with a 1-day lateness window. Deduplicate by (te",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nA practical intro to dbt incremental logic, late data handling, and basic data quality checks for multi-tenant analytics.\n\n## Key Concepts\n\n- dbt incremental models\n- late data windows\n- dedup with composite key\n- PII masking macro\n- basic tests (not_null, unique)\n\n## Code Example\n\n```javascript\n-- dbt incremental example (SQL-like)\nselect\n  tenant_id,\n  date(created_at) as day,\n  event_id,\n  count(*) as actions\nfrom {{ ref('staging_events') }}\nwhere created_at < current_timestamp - interval '1 day'\ngroup by tenant_id, day, event_id\n```\n\n## Follow-up Questions\n\n- How would you adapt this for multi-tenant with per-tenant schemas?\n- How would you extend tests for late-arriving data?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-16",
        "questionId": "q-19820",
        "question": "Design a beginner dbt flow (Snowflake) to build analytics_{{tenant}}.normalized_events from staging.tenant_events by enriching with a canonical_event_id via a map_event() macro that consults analytics_base.events_lookup for each tenant. Include 1-day late data, tests not_null(tenant_id, day, event_type, canonical_event_id) and unique(tenant_id, day, canonical_event_id). Add a CI check that every tenant in tenants seeds has analytics_{{tenant}}.normalized_events; discuss cross-tenant isolation and contract drift?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a per-tenant incremental model analytics_{{tenant}}.normalized_events from staging.tenant_events using map_event() to resolve canonical_event_id from analytics_base.events_lookup. Allow 1-day",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a seeds-driven macro looping tenants to create daily analytics_{{tenant}}.quality_contracts from staging_tenant_events. Track row_count, distinct_user_ids, null_user_ids, and SLA_target per day. P",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest per-tenant events, dedupe by tenant_id/event_id, aggregate daily counts by action_type and total_duration, and map action_type to category via a macro and analytics_base.event_definitions. Impl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a per-tenant quality score calculated from data_freshness, null_ratio, and FK_consistency. Implement a shared macro analytics_base.score_quality(tenant) returning 0-100. Add per-tenant tests f",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests macro usage, per-tenant models, seeds, and basic CI checks for isolation.\n\n## Key Concepts\n- dbt macros and refs, incremental models, per-tenant naming, tests, seeds.\n- Data contracts and cross-tenant isolation.\n\n## Code Example\n```sql\n-- macro example (pseudo)\n{% macro map_event(tenant_id, event_type) %}\n  SELECT canonical_event_id\n  FROM analytics_base.events_lookup\n  WHERE tenant_id = {{ tenant_id }} AND event_type = {{ event_type }}\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you handle missing canonical_event_id in map_event() without failing CI?\n- How would you extend tests to catch per-tenant drift over time?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-17",
        "questionId": "q-12713",
        "question": "Design a tenant-scoped run-audit telemetry for Snowflake/dbt: after each model run, a macro writes an entry to analytics_{{tenant}}.audit_log with tenant_id, run_id, model_name, status, duration_ms, executed_at. Ensure on-boarding tenants auto-create audit_log; enforce not_null and unique constraints, plus a global analytics.lineage view. Provide CI that confirms every tenant has an audit_log and that runs in the last 14 days are captured. Include masking to prevent cross-tenant data leaks in audit writes and discuss performance considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant quality score in analytics_{{tenant}}.quality_metrics derived from staging.tenant_events with 2-day late data. Score = 0.5*recency + 0.3*null_rate + 0.2*schema_drift; cap at 100",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-tenant quality pipeline: a dbt macro creates analytics_{{tenant}}.quality_summary and analytics.lineage, then computes rolling 7-day event counts from staging.tenant_events with a 1-day l",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a dbt macro invoked by on-run-end to insert into analytics_{{tenant}}.audit_log; use an AFTER_RUN hook that creates the per-tenant log table if missing; include run_id, model_name, status, duration_ms, execute…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a dbt macro to generate analytics_{{tenant}} schemas and an incremental daily model analytics_{{tenant}}.daily_anomaly from staging.tenant_events. Compute per-tenant daily counts, then 30-day roll",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTo test observability and multi-tenant governance in a dbt Snowflake setup, emphasizing automation, data isolation, and comprehensive auditability across tenant environments.\n\n## Key Concepts\n- dbt on-run-end hooks and macros for automated audit logging\n- Per-tenant schemas with analytics_{{tenant}} naming convention\n- Data integrity through NOT NULL and UNIQUE constraints\n- Cross-tenant CI validation to ensure consistent monitoring\n- Role-based access control (RBAC) and dynamic masking for audit isolation\n- Performance optimization for high-volume audit writes\n\n## Code Examples\n```sql\n-- Audit log table creation\nCREATE TABLE IF NOT EXISTS analytics_{{tenant}}.audit_log (\n    tenant_id VARCHAR NOT NULL,\n    run_id VARCHAR NOT NULL,\n    model_name VARCHAR NOT NULL,\n    status VARCHAR NOT NULL,\n    duration_ms NUMBER NOT NULL,\n    executed_at TIMESTAMP_NTZ NOT NULL,\n    CONSTRAINT unique_audit_record UNIQUE (tenant_id, run_id, model_name)\n);\n\n-- Global lineage view with masking\nCREATE OR REPLACE VIEW analytics.lineage AS\nSELECT tenant_id, run_id, model_name, status, duration_ms, executed_at\nFROM analytics_{{tenant}}.audit_log\nWHERE CURRENT_ROLE() = 'admin' OR tenant_id = CURRENT_TENANT();\n```",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-18",
        "questionId": "q-4033",
        "question": "Design a per-tenant incremental model in Snowflake that computes analytics.{tenant}.retention_daily from staging.events (tenant_id, user_id, event_date, first_seen, country_code). Include a macro-driven approach to switch between per-tenant daily retention and rolling-7-day retention, handle late data up to 3 days, enforce per-tenant schema isolation, and add tests (not_null on tenant_id, user_id, event_date; unique on (tenant_id, user_id, event_date)). Also add a snapshot analytics.{tenant}.users_cohort for first_seen and country changes. Explain how you would implement cross-tenant lineage and per-tenant data quality checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a tenant-scoped audit trail in Snowflake using dbt: ingest data from staging.tenant_events and staging.tenant_changes into tenant-specific analytics_{{tenant}}.audit_daily tables, capturing operation type, event…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant macro system that dynamically configures retention_window_days (1 or 7) and processing mode (daily or rolling). Build analytics.{tenant}.retention_daily incrementally from staging.events (tenant_i…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a per-tenant embeddings pipeline: analytics_{{tenant}}.user_embeddings derived from staging.tenant_events by applying a shared analytics_base.embeddings_model with tenant-specific privacy bu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To address this, implement a tenant-scoped incremental pipeline. A macro creates analytics_{{tenant}} schemas and per-tenant hourly_engagement tables. Use Snowflake masking policies on PII in analytic",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical multi-tenant dbt design capabilities including macro-driven per-tenant logic, incremental processing with late data handling, and governance artifacts.\n\n## Key Concepts\n- Macros for tenant-specific configuration and logic switching\n- Incremental models with late-arriving data handling\n- Per-tenant schemas and snapshots for data isolation\n- Data quality tests and cross-tenant lineage tracking\n\n## Code Example\n```javascript\n// Placeholder implementation\n```\n\n## Follow-up Questions\n- How would you test macro correctness across tenants with different retention windows?\n- What strategies would you use for cross-tenant lineage tracking?\n- How would you implement per-tenant data quality checks at scale?",
        "difficulty": "advanced"
      },
      {
        "id": "dbt-analytics-engineer-q-19",
        "questionId": "q-26594",
        "question": "In a Snowflake/dbt setup with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a beginner-friendly data quality framework that automatically runs per-tenant tests and surfaces a simple health score. Include: (1) a macro analytics_base.create_health_score(value_count, nulls_pct) that returns a 0-100 score, (2) an incremental model analytics_{{tenant}}.health_status that aggregates latest row counts and null_pct for key tables, (3) a global analytics_base.health_dashboard that unions tenant scores, (4) a CI gate blocking deployment if any tenant health_score < 70, (5) not_null and unique tests on tenant primary keys. Provide macro names and a minimal schema outline and example usage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a macro analytics_base.column_drift(tenant, table) that queries information_schema.columns and compares column_name, data_type, and is_nullable against analytics_base.transaction_reg",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a health score by combining per-table row counts and null rates into a 0-100 score; per-tenant health_status aggregates latest checks; a global health_dashboard unions all tenants; CI blocks de",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement an incremental analytics.daily_events that aggregates counts by day, event_type, and region_code from staging.event_logs joined to dims.regions on region_code. Use a 2-day late window, enfor",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build an incremental per-tenant anomaly detection pipeline: compute a daily event_rate per tenant from staging.tenant_events, fetch per-tenant thresholds from analytics_{{tenant}}.config via a macro, calculate z-scores…",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\\nTests a beginner-friendly approach to data quality across multi-tenant schemas using dbt macros and incremental models. It evaluates ability to compute a composite health score, maintain per-tenant health, and drive CI gates.\\n\\nKey Concepts\\n- dbt macros and incremental models\\n- per-tenant isolation with unified dashboards\\n- basic data quality tests and CI gating\\n\\nCode Example\\n```sql\\n-- Pseudo macro usage sketch\\nSELECT analytics_base.create_health_score(row_count, nulls_pct) AS health_score\\nFROM analytics_base.score_metrics\\n```\\n\\nFollow-up Questions\\n- How would you test for skewed null rates across tenant tables?\\n- How would you extend the score to handle multi-table dependencies?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-20",
        "questionId": "q-8950",
        "question": "Design a beginner dbt+Snowflake workflow to surface per-tenant hourly data quality gaps: from seeds tenants(tenant_id, status) and source staging.tenant_events(tenant_id, occurred_at, hour_start, event_type), create a macro analytics_{{tenant}}.quality_issues and a per-tenant hourly_gap_summary that flags missing hours for each tenant. Include 1-day late data handling, tests (not_null on tenant_id, hour_start; unique on (tenant_id, hour_start)), a global analytics.lineage, and CI checks that every analytics_{{tenant}}.quality_issues exists for all tenants and that no tenant lacks a 24h window of hourly data. Explain how you'd validate isolation and how the macro works?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Macro: analytics_base.health_check() returns per-tenant health flags. Tests: health_not_null_tenant, daily_metrics_complete, daily_metrics_lag. Approach: compute for each tenant whether tenant_id is n",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement analytics_{{tenant}}.user_features via incremental upserts from analytics_{{tenant}}.raw_events using a macro that sources from analytics_base.features_base and targets the tenant schema. Co",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a macro redaction_policy(tenant_id) that reads a central tenant_config(redact_pii). If true, return masking expressions for email and phone; otherwise pass-through. In analytics.{tenant}.hou",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a macro create_analytics_schema(tenant) to generate analytics_{{tenant}}.quality_issues and analytics_{{tenant}}.hourly_gap_summary. hourly_gap_summary computes, for each day×hour, a gap flag by l",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to craft per-tenant data quality observability in dbt, including macro-driven schema creation, gap detection, and CI validation.\n\n## Key Concepts\n- dbt macros for per-tenant schema generation\n- per-tenant quality tables and hourly data gap detection\n- 1-day late data handling and data freshness checks\n- lineage tracking and CI validation for isolation\n\n## Code Example\n```sql\n-- illustrative per-tenant gap check (simplified)\nwith events as (\n  select tenant_id, date_trunc('hour', occurred_at) as hour_start\n  from {{ ref('staging.tenant_events') }}\n)\nselect tenant_id, hour_start, count(*) as cnt\nfrom events\nwhere tenant_id = 'EXAMPLE_TENANT'\ngroup by 1,2;\n```\n\n## Follow-up Questions\n- How would you extend this to multiple tenants concurrently without hitting race conditions?\n- What are the security implications of per-tenant schemas and how would you audit grants?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-21",
        "questionId": "q-7518",
        "question": "NEW ANGLE: Privacy-driven per-tenant dbt flow. Given a tenants table (tenant_id, privacy_tier) and staging.events(tenant_id, user_id, event_type, email, ip_address, occurred_at), design a macro-driven analytics_{{tenant}}.hourly_events that enforces privacy rules by redacting or hashing PII for non-public tenants. Load 1-day late data, include tests (not_null on keys, unique (tenant_id, hour_start, event_type)); snapshot analytics_{{tenant}}.users; and a global analytics.lineage. Explain how this scales to 100s of tenants and how CI validates cross-tenant isolation and privacy compliance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a macro-driven per-tenant dbt flow that reads staging.events (tenant_id, user_id, event_type, email, ip_address, occurred_at) and loads analytics_{{tenant}}.hourly_events. Enforce privacy: if p",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose staging per domain, a single grain, incremental models with a 2-day late-arrival window, a surrogate key like domain_key + occurred_at + id for cross-domain joins, a macro-based cross-domain r",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement analytics_base.qc_aggregate(tenant) as a macro that parses manifest.json and run_results.json to populate analytics_base.quality_{{tenant}} with fields: tenant, model, test_name, status, and score. Define per-…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a dbt macro that auto-generates analytics_{{tenant}}.hourly_metrics from staging.tenant_events, enforcing a data-contract stored in analytics_base.contract. The macro creates per-ten",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a realistic, privacy‑aware multi‑tenant pipeline. Requires macro generation of tenant-scoped models, conditional data masking, and governance hooks (lineage, snapshots) under CI.\n\n## Key Concepts\n\n- dbt macros for tenant-scoped models\n- per-tenant privacy rules (PII redaction/hash)\n- incremental loading with a 1-day late window\n- tests: not_null, unique keys\n- snapshots and lineage governance\n- CI validation across many tenants\n\n## Code Example\n\n```javascript\n// Pseudo macro sketch (dbt uses Jinja in SQL)\n{% macro redact_fields(field, privacy_tier) %}\n  {% if privacy_tier == 'public' %}\n    {{ field }}\n  {% else %}\n    SHA2({{ field }}, 256)\n  {% endif %}\n{% endmacro %}\n```\n\n## Follow-up Questions\n\n- How would you monitor privacy violations in production and adjust masking rules?\n- How would you scale testing and CI for 100s of tenants without slowing commits?",
        "difficulty": "intermediate"
      },
      {
        "id": "dbt-analytics-engineer-q-22",
        "questionId": "q-17462",
        "question": "In a Snowflake/dbt setup onboarding tenants dynamically, design a beginner incremental pipeline that creates analytics_{{tenant}}.daily_user_signals from staging.tenant_events. Use a macro tenant_enabled() that checks seeds.tenants (tenant_id, enabled) and only materializes for enabled tenants. Include 1-day late data, tests not_null(tenant_id, day_start) and unique(tenant_id, day_start). Explain CI validation for new tenants and how to prevent cross-tenant data leakage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Compute per-tenant drift by comparing daily category distribution from staging.tenant_events to a per-tenant baseline in analytics_base.category_profiles. Build analytics_{{tenant}}.category_drift wit",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose an idempotent incremental MERGE via analytics_base.tenant_upsert to align analytics_{{tenant}}.sales_fact with staging.tenant_sales_events, joining on tenant_id and sale_id to tolerate 2-day l",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create an incremental model analytics_{{tenant}}.daily_user_signals from staging.tenant_events gated by a macro tenant_enabled() that reads seeds.tenants and only runs for enabled tenants. Handle 1-da",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Build a per-tenant revenue flow via a macro that creates analytics_{{tenant}}.daily_revenue from staging.billing_events, normalizing to USD with a currencies table. Add analytics_{{tenant}}.data_avail",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical, zero-drag onboarding of tenants using dbt macros and seeds; checks incremental logic and data governance.\n\n## Key Concepts\n\n- dbt macros and seeds\n- per-tenant incremental materializations\n- tenant registry validation in CI\n- data isolation to prevent cross-tenant leakage\n\n## Code Example\n\n```javascript\n// Pseudo macro example (not production code)\nfunction tenant_enabled(tenant_id) {\n  // read seeds.tenants and return true if enabled\n  return true;\n}\n```\n\n## Follow-up Questions\n\n- How would you unit-test the tenant_enabled macro?\n- How would you detect and fix a misconfigured tenant in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-23",
        "questionId": "q-16993",
        "question": "Design a beginner-friendly daily revenue flow: from staging.sales_events(order_date, amount, country_code) to analytics.daily_revenue(date, country_code, revenue) using a simple incremental model with a 1-day late data window. Include a macro that validates country_code against a seeded countries table and a CI check that flags any new country_code not present in countries. How would you implement and test this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: implement an incremental model analytics.{tenant}.daily_event_summary with a composite key (tenant_id, day, event_type, user_id). Use MERGE-like upserts via dbt incremental to accommodate late d",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a per-tenant incremental dbt model that builds analytics_{{tenant}}.feature_usage from staging.tenant_events using a tenants seed. A macro generates the incremental model, filters ev",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To implement: use day_start = date_trunc('day', occurred_at); aggregate active_users = count(distinct user_id) where is_deleted = false; implement incremental by tenant schema; create analytics.{tenan",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Incrementally populate analytics.daily_revenue by date and country_code from staging.sales_events(amount). The macro validates country_code against the seeded countries table and fails CI if a new cod",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical dbt skills: incremental modeling, seeds, and custom macros. It focuses on data quality governance with a simple country_code contract and CI checks appropriate for a beginner.\n\n## Key Concepts\n- Incremental models and late-arriving data\n- Macros to enforce data contracts\n- Seeds and cross-table validation in CI\n- Basic tests: not_null and unique\n\n## Code Example\n```javascript\n// pseudo snippet: macro enforces country_code in countries seed\n{% macro assert_country_in_seed(country_code) %}\n  {% set valid = (return value of select country_code from {{ ref('countries_seed') }}) %}\n  {{ if not loop over valid %} {{ exceptions }} {% endif %}\n{% endmacro %}\n```\n\n## Follow-up Questions\n- How would you adapt this for multiple regions or a dynamic country list?\n- What would you add to handle out-of-order data in the 1-day window?",
        "difficulty": "beginner"
      },
      {
        "id": "dbt-analytics-engineer-q-24",
        "questionId": "q-26019",
        "question": "In a Snowflake/dbt environment with per-tenant schemas analytics_{{tenant}} and a shared analytics_base, design a beginner-level data quality gate for ingestion latency and FK integrity. Include: (1) a macro analytics_base.qa_latency_ri(tenant, src_table, dim_table) computing max days late from QUERY_HISTORY, (2) a per-tenant analytics_{{tenant}}.qa_summary incremental model, (3) tests not_null/unique and a CI gate blocking prod if latency > 1 day or FK failures in any tenant, and a minimal schema outline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed: implement qa_latency_ri to compute max delay between ingest_ts and event_ts using QUERY_HISTORY; build analytics_{{tenant}}.qa_summary incrementally (tenant, src_table, dim_table, latency_da",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a per-tenant masking dbt flow: use a macro mask_pii(policy, field) that returns redaction, hashing, or partial masking based on mask_policy per tenant. Generate analytics_{{tenant}}.masked_a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build analytics.{tenant}.daily_cohort as an incremental model sourced from staging.user_events. Compute daily cohorts by lifecycle_stage and event_type with a 2-day late-data window, upserting via MER",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "JSON-driven data quality: rules.json defines per-tenant required_fields and max_null_pct; the dq_rules macro auto-generates not_null tests and a null-rate test for each tenant/table/field. Create anal",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nGauges practical data quality in a multi-tenant dbt setup, testing macro design, incremental per-tenant modeling, and CI gates.\n\n## Key Concepts\n- Macro design in analytics_base\n- Incremental per-tenant models\n- Data quality tests (not_null, unique, FK)\n- CI gates for cross-tenant health\n\n## Code Example\n```javascript\n// Implementation sketch (pseudo)\nCREATE MACRO analytics_base.qa_latency_ri(tenant, src_table, dim_table) AS ...\n```\n\n## Follow-up Questions\n- How to scale latency checks across many tenants?\n- How to store and visualize latency trends for dashboards?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-04T05:46:30.308Z",
    "version": 1
  }
]