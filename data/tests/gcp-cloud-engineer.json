[
  {
    "id": "test-gcp-cloud-engineer",
    "channelId": "gcp-cloud-engineer",
    "channelName": "GCP Cloud Engineer",
    "title": "GCP Cloud Engineer Knowledge Test",
    "description": "Test your understanding of GCP Cloud Engineer with 25 questions.",
    "questions": [
      {
        "id": "gcp-cloud-engineer-q-0",
        "questionId": "q-25752",
        "question": "Advanced: Architect a real-time, multi-tenant telemetry pipeline on GCP. Telemetry from mobile apps is published to Pub/Sub per tenant. Build a Dataflow streaming job that validates schema, deduplicates by event_id, and emits 30s windowed aggregates per tenant. Write raw events to gs://raw-telemetry/{tenant}/{date}/ and aggregates to a partitioned BigQuery table by date/tenant. Enforce least-privilege IAM, enable VPC Service Controls, set Dataflow autoscaling and a budget cap. Include a test plan and rollback path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An end-to-end approach: Pub/Sub per-tenant ingestion, a Dataflow streaming job validates messages, deduplicates by event_id, and emits 30s windowed aggregates per tenant. Write raw events to gs://raw-",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement with a Cloud Function triggered on finalization of gs://company-uploads/incoming/*.zip. Use Python zipfile to extract CSV, read header, validate user_id,event_ts,amount and parse event_ts. O",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Terraform provisions: Pub/Sub topic telemetry.raw, Cloud Function telemetry-processor (Node.js) triggered by the topic, and two GCS buckets with versioning: telemetry-raw and telemetry-sanitized. The",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest logs via Pub/Sub with tenant_id; a Dataflow streaming job validates schema, extracts tenant_id, and writes to a partitioned BigQuery table keyed by date with a tenant_id column. Use BigQuery ro",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time data engineering and per-tenant isolation.\n\n## Key Concepts\n- Pub/Sub, Dataflow streaming, schema validation, deduplication, windowing\n- BigQuery partitioning, GCS storage, IAM least privilege, VPC Service Controls\n- Cost control and rollback strategy\n\n## Code Example\n```python\n# Dataflow skeleton: schema check, dedupe, windowed aggregation\nclass ValidateAndDedup(DoFn):\n    def process(self, elem, timestamp=DoFn.TimestampParam, state=DoFn.StateParam):\n        # validate and deduplicate by event_id within tenant window\n        yield (elem[\"tenant\"], elem[\"date\"], elem[\"metrics\"])\n```\n\n## Follow-up Questions\n- How to handle schema evolution with backward/forward compatibility?\n- How to test failure scenarios and ensure exactly-once semantics?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-1",
        "questionId": "q-13819",
        "question": "Design an intermediate end-to-end GCP ingestion pipeline for telemetry data from an automotive IoT fleet. Vehicles publish JSON lines to Pub/Sub; a Dataflow streaming job reads, enriches with device metadata from Firestore, deduplicates on event_id, and writes to BigQuery. Implement per-tenant data isolation with CMEK and per-tenant partitions; configure DLQ; test idempotency and monitoring dashboards. Include a test plan for misformatted data and high-throughput spikes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an end-to-end ingestion pipeline: vehicles publish JSON lines to Pub/Sub telemetry-in; a Dataflow streaming job reads, validates, and enriches with device metadata from Firestore; deduplicate",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement with Pub/Sub to Dataflow (Python Beam). Deduplicate by event_id, window 30s per user, compute per-user fail_rate (fails/attempts) in window, emit alert if fail_rate > 0.3; write enriched eve",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Cloud Function validates header, normalizes ts to ISO8601, and publishes enriched JSON to Pub/Sub on CSV Finalize. Cloud Run, triggered by Pub/Sub, writes to partitioned BigQuery analytics.enriched_ev",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Nightly cleanup: Cloud Scheduler triggers a Cloud Function that scans gs://user-uploads/temp for files older than 7 days, copies each to gs://archive/YYYY/MM/DD/ with the original name, deletes the source file, and logs…",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests ability to design streaming pipelines, data enrichment, fault handling, and security at scale with CMEK and per-tenant isolation.\n\nKey Concepts\n- Dataflow streaming pipelines\n- Pub/Sub DLQ and retries\n- Firestore enrichment\n- BigQuery partitioning by tenant_id\n- CMEK and IAM least privilege\n- Idempotency with event_id/insertId\n- Monitoring dashboards and alerting\n- End-to-end testing with misformatted data and spikes\n\nCode Example\n```javascript\n// Pseudo-behavioral snippet showing core steps\nfunction process(event) {\n  const parsed = JSON.parse(event);\n  const enriched = enrich(parsed, getDeviceMeta(parsed.device_id));\n  const deduped = deduplicate(enriched, \"event_id\");\n  writeToBigQuery(deduped, { partition: deduped.tenant_id });\n}\n```\n\nFollow-up Questions\n- How would you handle late-arriving data and out-of-order events?\n- How would you implement per-tenant cost caps and quotas?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-2",
        "questionId": "q-1413",
        "question": "Design a real-time image ingestion pipeline on GCP for a multi-tenant SaaS app. Customers upload images to per-tenant Cloud Storage buckets with CMEK; a Pub/Sub topic notifies on new uploads; a Dataflow streaming job processes images to extract features and writes results to BigQuery, while archived copies are moved to per-tenant archive buckets. Outline architecture, IAM bindings, service account isolation, exactly-once guarantees (e.g., row_id dedup), and cost/latency trade-offs. Include monitoring and failure-response plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a Cloud Function triggered on gs finalized events, parse CSV, normalize headers to lowercase, validate customer_id, timestamp, amount, coerce timestamp to ISO 8601, then upsert each row to Big",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Cloud Function triggered by new objects in gs://app-raw/events/. It loads each event, validates against a versioned schema in gs://schemas/v{current}/schema.json. On success, writes to BigQuery",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe end-to-end architecture for a multi-tenant real-time image pipeline on GCP with per-tenant CMEK buckets, Pub/Sub notifications, Dataflow streaming processing, BigQuery storage, and per-tenant",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Real-time data validation and upsert pipeline on GCP: Pub/Sub -> Dataflow (Beam) validates against a dynamic AVRO schema, applies business rules, and upserts into BigQuery analytics.events using an in",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-tenant data isolation, customer-managed encryption keys, streaming dataflow, and BigQuery deduplication. Also covers IAM scoping, service account separation, and operational resilience.\n\n## Key Concepts\n- Tenant isolation with CMEK and per-tenant storage\n- Pub/Sub as the ingestion trigger\n- Dataflow streaming pipeline and BigQuery write with deduplication via row_id\n- Archival strategy and cost-aware design\n- Monitoring, alerting, and failure handling\n\n## Code Example\n```javascript\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nclass ParseEvent(beam.DoFn):\n  def process(self, element):\n    import json\n    msg = json.loads(element.decode('utf-8'))\n    yield {\n      'tenant_id': msg['tenant_id'],\n      'image_id': msg['image_id'],\n      'row_id': f\"{msg['image_id']}_{msg['timestamp']}\",\n      'embeddings': msg.get('embeddings', None)\n    }\n\n# skeleton; actual IOs would be configured with real project details\nwith beam.Pipeline(options=PipelineOptions()) as p:\n  (p\n   | beam.io.ReadFromPubSub(topic='projects/PROJECT/topics/images')\n   | beam.ParDo(ParseEvent())\n   | beam.io.WriteToBigQuery(\n       table='PROJECT:DATASET.TABLE',\n       schema='SCHEMA_AUTODETECT',\n       write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND,\n       create_disposition=beam.io.BigQueryDisposition.CREATE_IF_NEEDED\n     )\n  )\n```\n\n## Follow-up Questions\n- How would you implement row-level security for tenants in BigQuery?\n- What would you monitor to detect data skew or latency regressions?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-3",
        "questionId": "q-7052",
        "question": "Design an advanced, production-grade real-time telemetry pipeline on GCP for a global financial app. Ingest high-volume events from Pub/Sub across regions, process with Dataflow streaming to remove duplicates by event_id, and write to BigQuery telemetry.events using insertId to ensure idempotence. Apply watermarking for late data, and emit Cloud Monitoring alerts if data stalls. Include IAM, testing plan, and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger a Cloud Function on file finalize for gs://company-logs/tenant-*/incoming/*.json. Validate each record, redact PII using policies.json, and emit sanitized rows to BigQuery analytics.events par",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Dataflow streaming pipeline from Pub/Sub to BigQuery with insertId = user_id|event_time|session_id to guarantee idempotency. Handle late data via fixed windows and allowed lateness, support s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Dataflow streaming reads from Pub/Sub across regions, deduplicates by event_id via a stateful DoFn, and writes to BigQuery telemetry.events using insertId to guarantee idempotence. Implement watermark",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Dataflow streaming (Apache Beam) reading Pub/Sub region topics, validate JSON, deduplicate by event_id via per-key state, apply 1-minute fixed windows to compute per-account anomaly scores, and wr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses end-to-end real-time ingestion, deduplication guarantees, and exactly-once semantics across regional boundaries. Tests Dataflow configuration (watermarks, lateness), BigQuery writes with insertId, and robust monitoring.\n\n## Key Concepts\n\n- Dataflow streaming and Pub/Sub in multi-region setups\n- Stateful deduplication by event_id\n- BigQuery insertId for idempotent sinks\n- Watermarking and allowed lateness\n- Cloud Monitoring alerts and incident wiring\n- IAM least privilege and service accounts\n\n## Code Example\n\n```java\nclass DedupDoFn extends DoFn<Event, Event> {\n  @StateId(\"seen\") private final StateSpec<ValueState<Boolean>> seen = null;\n  @ProcessElement\n  public void process(@Element Event e, @StateId(\"seen\") ValueState<Boolean> seen) {\n    if (Boolean.FALSE.equals(seen.read())) {\n      seen.write(true);\n      out.output(e);\n    }\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test idempotence and backpressure under traffic spikes?\n- What monitoring dashboards would you set up for latency and data-lag, and how would you handle alert fatigue?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-4",
        "questionId": "q-10526",
        "question": "Design a real-time analytics pipeline on GCP with strict EU data residency: ingest JSON events stored in a EU bucket to Pub/Sub; a Dataflow streaming job deduplicates by event_id, enriches with country, and writes to eu.analytics.events (partitioned by date) with insertId for idempotency. Simultaneously emit 1-minute country_counts to us.analytics. Ensure raw stays EU, enable CMEK, apply least-privilege IAM, and provide a test plan with duplicates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pub/Sub in a single region, Dataflow streaming with stateful dedup and event-time windows; write to BigQuery using insertId from event_id+ingest_time; enforce regional residency; implement schema",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a GCP streaming analytics pipeline with strict EU data residency. Ingest JSON events stored in EU bucket to Pub/Sub; a Dataflow streaming job deduplicates by event_id, enriches with country, an",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Secret Manager to store API keys; Cloud Scheduler triggers a Cloud Function that creates a new secret version, then updates Cloud Run service to use the new key (env var or volume). Log a BigQuery",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Pub/Sub for ingestion, Dataflow (Beam) streaming with a stateful dedup by event_id, writing to analytics.events_YYYYMMDD with insertId to guarantee idempotence. Leverage watermarking and windowing",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end streaming pipelines under data residency constraints with idempotent writes and cross-region aggregates.\n\n## Key Concepts\n- EU data residency with GCS, Pub/Sub, and BigQuery\n- Streaming Dataflow (Beam) with deduplication by event_id\n- Exactly-once semantics using insertId and partitioned BigQuery tables\n- CMEK encryption and granular IAM roles\n- Validation and test plans for duplicates and cross-region outputs\n\n## Code Example\n```python\n# Pseudo Beam snippet: dedup by event_id and write to EU BigQuery\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nimport json\n\ndef to_event(x):\n    return json.loads(x)\n\nwith beam.Pipeline(options=PipelineOptions()) as p:\n    events = (p\n              | 'ReadEU' >> beam.io.ReadFromText('gs://eu/raw-events/*.json')\n              | 'Parse' >> beam.Map(to_event)\n              | 'Dedup' >> beam.Map(lambda e: (e['event_id'], e))\n              | 'Group' >> beam.GroupByKey()\n              | 'WriteEU' >> beam.io.WriteToBigQuery('eu:analytics.events', write_disposition=\\\n                    beam.io.BigQueryDisposition.WRITE_APPEND))\n```\n\n## Follow-up Questions\n- How would you test idempotency and cross-region consistency in CI?\n- What monitoring dashboards and alerts would you implement for data residency violations or failed dedupes?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-5",
        "questionId": "q-19431",
        "question": "Design an end-to-end GCP streaming pipeline for real-time fraud detection. Events arrive on Pub/Sub topic transactions.events as JSON with transaction_id, user_id, amount, currency, ts, and risk_score. Build a Dataflow (Beam) job that validates the schema, deduplicates by transaction_id, applies 1-minute fixed windows per user_id to compute per-user count, total amount, and average. Write results to BigQuery analytics.user_transactions with insertId = transaction_id. Route bad records to an error Pub/Sub topic via dead-letter, and alert on error rate. Include minimal IAM bindings and a concrete test plan. Explain trade-offs Dataflow vs BigQuery streaming for this workload?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a single bucket with Lifecycle rules: after 45 days transition to ARCHIVE, after 365 days to COLDLINE. Cloud Run HTTP API accepts bucket, object, and range; reads the specified byte range via the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest telemetry from Pub/Sub, process with a Dataflow streaming job, partition outputs by region to per-region BigQuery tables, and ensure idempotent writes via insertId. Use VPC Service Controls and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an event-driven ingest: Pub/Sub to Cloud Run for validation; tenancy rules via a single source of truth (Firestore) mapping region per tenant; route to BigQuery streaming in that region or f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Dataflow (Beam) streaming job that subscribes to Pub/Sub transactions.events, validates JSON schema, deduplicates by transaction_id, and uses 1-minute fixed windows by user_id to compute per-",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses real-time correctness, deduplication, windowing strategy, error paths, and operational concerns for large-scale GCP data pipelines.\n\n## Key Concepts\n\n- Pub/Sub dead-lettering and alerting\n- Beam stateful deduplication by transaction_id\n- 1-minute tumbling windows per user_id with per-window aggregates\n- Idempotent BigQuery writes via insertId\n- Least-privilege IAM for Pub/Sub and BigQuery\n\n## Code Example\n\n```javascript\n// Pseudo-Beam-style sketch (illustrative, not runnable)\n\"\"\" \n// 1) Read from Pub/Sub\n// 2) Validate JSON schema\n// 3) Stateful dedupe on transaction_id\n// 4) Window by user_id, 1-min FixedWindows\n// 5) Compute count, sum(amount), avg(amount)\n// 6) Write to BigQuery with insertId = transaction_id\n\"\"\"\n```\n\n## Follow-up Questions\n\n- How would you test idempotency and dedup at scale?\n- How would you adjust for late data and watermark skew in this pipeline?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-6",
        "questionId": "q-28587",
        "question": "Design a beginner-friendly GCP workflow to enforce regional data residency for telemetry: incoming JSON lines land in gs://raw-telemetry with a region field. Implement a Cloud Function to validate the schema and route each line to gs://telemetry-us/YYYY/MM/DD or gs://telemetry-eu/YYYY/MM/DD accordingly. Maintain an audit log in BigQuery with insertId = filePath+date+region, plus minimal IAM and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pub/Sub for ingestion and a Dataflow streaming job to parse logs and write into a single BigQuery table partitioned by tenant_id and date. Enforce isolation with per-tenant authorized views, enabl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Pub/Sub ingests with a strict schema; Dataflow streaming uses watermarking and insertId to write idempotent, tenant-scoped partitions to BigQuery (tenant/date). Raw events archive to Cloud Storage by",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Trigger a Cloud Function on GCS object finalize events. Parse each JSON line, validate its schema against expected fields, and route the data to the appropriate regional bucket: gs://telemetry-us/YYYY/MM/DD for US data…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Proposed approach: Pub/Sub triggers a Cloud Function that parses event_id, emits a row to BigQuery with insertId = event_id to achieve idempotency. The function computes per-user minute aggregates and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design a region-aware ingestion pipeline with clear boundaries, idempotency, and compliance considerations—common constraints in real-world telemetry systems.\n\n## Key Concepts\n- Event-driven architecture using Cloud Functions triggered by GCS object finalize events\n- Region-based routing via a multi-bucket strategy\n- BigQuery audit logging with composite insertId to ensure idempotent writes\n- IAM least-privilege principles for secure access control\n- Test planning to validate cross-region data routing\n\n## Code Example\n```java\n// Cloud Function triggered on GCS object finalize\npublic class TelemetryRouter implements BackgroundFunction<CloudEvent> {\n    private final Storage storage = StorageOptions.getDefaultInstance().getService();\n    private final BigQuery bigquery = BigQueryOptions.getDefaultInstance().getService();\n\n    @Override\n    public void accept(CloudEvent event, Context context) {\n        String bucket = event.getBucket();\n        String objectName = event.getObjectName();\n        \n        if (!bucket.equals(\"raw-telemetry\")) return;\n        \n        String content = new String(storage.get(bucket, objectName).getContent());\n        \n        for (String line : content.split(\"\\n\")) {\n            JsonNode record = new ObjectMapper().readTree(line);\n            validateSchema(record);\n            \n            String region = record.get(\"region\").asText();\n            String targetBucket = region.equals(\"us\") ? \"telemetry-us\" : \"telemetry-eu\";\n            String destPath = LocalDate.now().format(DateTimeFormatter.ofPattern(\"yyyy/MM/dd\")) + \"/\" + objectName;\n            \n            storage.get(targetBucket, destPath).getWriter().write(record.toString());\n            \n            String insertId = objectName + \"_\" + LocalDate.now() + \"_\" + region;\n            insertAuditLog(insertId, record, targetBucket);\n        }\n    }\n    \n    private void validateSchema(JsonNode record) {\n        requireNonNull(record.get(\"region\"), \"Missing region field\");\n        requireNonNull(record.get(\"timestamp\"), \"Missing timestamp field\");\n        // Additional field validation...\n    }\n    \n    private void insertAuditLog(String insertId, JsonNode record, String targetBucket) {\n        TableId tableId = TableId.of(\"audit\", \"telemetry_routing_log\");\n        InsertAllRequest.Builder builder = InsertAllRequest.newBuilder(tableId);\n        builder.addRow(Stream.of(\n            \"insertId\", insertId,\n            \"region\", record.get(\"region\").asText(),\n            \"destination\", targetBucket,\n            \"processedAt\", Instant.now().toString()\n        ).collect(Collectors.toMap(e -> e[0], e -> e[1])));\n        bigquery.insertAll(builder.build());\n    }\n}\n```\n\n## Test Plan\n1. **Unit Tests**: Validate schema enforcement and region routing logic with mocked GCS/BigQuery clients\n2. **Integration Tests**: Deploy to staging, upload sample JSON lines to gs://raw-telemetry, verify correct bucket placement and audit log entries\n3. **Negative Cases**: Test malformed JSON, missing region fields, and invalid regions to confirm proper error handling\n4. **Compliance Verification**: Confirm no cross-region data leakage by inspecting bucket ACLs and Cloud Audit Logs",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-7",
        "questionId": "q-16087",
        "question": "Design an advanced GCP multi-tenant telemetry ingest pipeline with strict isolation and cost controls: tenants publish to per-tenant Pub/Sub topics; a Dataflow streaming job shards by tenant_id, redacts PII, and deduplicates on event_id; writes to per-tenant, partitioned BigQuery tables; enforce least-privilege IAM, CMEK encryption, and per-tenant quotas. Include a test plan and migration strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an advanced GCP multi-tenant telemetry ingest: tenants publish to per-tenant Pub/Sub topics; a Dataflow streaming job shards by tenant_id, redacts PII, and de-duplicates on event_id; writes to",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest login events via Pub/Sub, execute a Dataflow (Apache Beam) streaming pipeline with 5-minute fixed windows to compute per-user risk scores from features including IP address, device fingerprint, geographic region,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "An end-to-end real-time feature store for fraud detection: ingest via Pub/Sub at 120k/s, stream with Dataflow to Vertex AI Feature Store online and BigQuery offline; enable cross-region replication an",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest via Pub/Sub; Dataflow streaming with exactly-once semantics (insertId for BigQuery writes, watermark-based windows); store raw events in region-bound Cloud Storage; export aggregates to region-",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests ability to design scalable, secure multi-tenant data pipelines on GCP, balancing isolation, cost, and operational concerns.\n\n## Key Concepts\n- Pub/Sub per-tenant isolation and throughput planning\n- Dataflow stateful processing, event-time dedup, and redaction\n- BigQuery per-tenant partitioning and access control\n- IAM scoping, CMEK, quotas and cost governance\n\n## Code Example\n```javascript\n// Pseudo Dataflow skeleton for per-tenant processing\n```\n\n## Follow-up Questions\n- How would you test tenant churn and data leakage?\n- What metrics and dashboards would you implement to monitor cost vs. throughput?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-8",
        "questionId": "q-9143",
        "question": "Design an on-demand, cost-aware data redaction pipeline on GCP for sensitive financial events. Ingest events from Pub/Sub topic payments-raw, redact PII using Data Loss Prevention in a Dataflow Apache Beam transform, and write sanitized records to BigQuery for analytics. Archive redacted copies in Cloud Storage. Include IAM least privilege, per-project quotas, idempotent writes, and a test plan with synthetic PII data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Explain a streaming Dataflow (Beam) pipeline for per-tenant clickstreams: Pub/Sub input per-tenant, enrichment with catalog data from BigQuery, fixed 5-minute windows with 2-minute lateness, raw enric",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a Dataflow pipeline that reads Pub/Sub payments-raw, applies PII redaction using Cloud DLP in a DoFn, writes sanitized rows to BigQuery, and stores redacted copies in gs://payments/redacted/. Us",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Trigger onFinalize for CSVs in gs://data-ingest/raw. The Cloud Function reads the header and validates against schema.json; if valid, copy to gs://data-ingest/valid/YYYY/MM/DD/<name> and publish a Pub",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a streaming Dataflow template that subscribes to region-local Pub/Sub topics, processes per-tenant events with fixed windows, and writes to per-tenant BigQuery datasets in the same region using CM",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical GCP data governance: integrating Pub/Sub, Dataflow, DLP, BigQuery, and Cloud Storage; enforcing least privilege; and designing testable, idempotent pipelines.\n\n## Key Concepts\n- Pub/Sub to Dataflow integration\n- Apache Beam transforms with DLP redaction\n- BigQuery ingestion patterns for analytics\n- Durable archiving for audits\n- IAM roles, service accounts, and resource permissions\n- Idempotent writes and retry/backoff\n\n## Code Example\n```python\n# Pseudo-implementation sketch\n```\n\n## Follow-up Questions\n- How would you scale this for 10x throughput and ensure DLP performance stays within SLA?\n- How would you monitor data quality, DLP coverage, and auditability across tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-9",
        "questionId": "q-10763",
        "question": "Design a beginner-friendly data anonymization pipeline on GCP: when a CSV file lands in gs://raw-data/uploads/, a Cloud Function reads the file, masks emails and phone numbers, writes the sanitized CSV to gs://sanitized/processed/YYYY/MM/DD/<filename>, and logs a row to BigQuery analytics.audit with insertId = filename+timestamp. Keep IAM minimal, trigger on finalize, and include a basic test plan with sample data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a serverless data anonymization pipeline using Cloud Functions triggered by Cloud Storage finalize events on gs://raw-data/uploads/{file}. The function reads the CSV file, masks emails and phone numbers using…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "In this task, implement an environment-aware ingestion pipeline: a Cloud Function triggers on gs://ingest-{env}/uploads finalize, validates tenantId matches env, and size under 5 MB. On success, publi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a cross-region telemetry ingest pipeline: Pub/Sub regional topics → Dataflow streaming with per-event insertId dedup by device_id+sequence → BigQuery telemetry.events (partitioned by day). Add",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Extract .gz, parse NDJSON, validate event_id/user_id/ts/event_type; publish each record to Pub/Sub; Dataflow batch reads Pub/Sub, deduplicates on event_id, and writes to BigQuery with insertId=event_i",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical data engineering skills including event-driven architecture, PII protection, idempotent operations, and security best practices in a real-world pipeline scenario.\n\n## Key Concepts\n- Cloud Functions with Cloud Storage triggers\n- Regex-based data masking for emails and phone numbers\n- BigQuery insertId for deduplication and auditability\n- Date-partitioned storage paths and least privilege IAM\n- Event-driven serverless processing patterns\n\n## Code Example\n```javascript\n// Cloud Function implementation (Node.js)\nexports.maskPii = async (event)",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-10",
        "questionId": "q-20392",
        "question": "Design a streaming telemetry pipeline on GCP: devices publish JSON to Pub/Sub; a Dataflow pipeline computes per-device hourly aggregates using fixed windows with 15 minutes of allowed lateness, writes to a partitioned BigQuery table, and uses insertId for dedup to ensure exactly-once semantics. Include failover, retries, multi-region residency, IAM bindings, monitoring dashboards, and a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Cloud Functions triggered on finalize of gs://media/uploads. Compute SHA-256; consult a hash_manifest (BigQuery or Cloud SQL) to detect duplicates; if new, copy to gs://media/processed/<hash> and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline topic/message schema, Dataflow windowing (FixedWindows 1h, lateness 900s), BigQuery partitioning and insertId-based dedup, idempotent writes, Pub/Sub retry/backoff, least-privilege IAM, multi-",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Pub/Sub with a per-event id and schema validation; implement a Dataflow streaming job (ReadFromPubSub) that validates, deduplicates via state keyed by player_id and event_id, applies 5-minute Fixe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single Pub/Sub topic carrying event payloads with tenant_id metadata. Implement a streaming Dataflow job that deduplicates events, writes to a partitioned BigQuery table keyed by tenant, and app",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design real-time pipelines at scale with data correctness and operations.\n\n## Key Concepts\n\n- Streaming windowing with allowed lateness\n- Pub/Sub and Dataflow integration\n- Exactly-once semantics with insertId\n- BigQuery partitioning and schema evolution\n- Multi-region residency and IAM least privilege\n- Monitoring, alerts, and testing\n\n## Code Example\n\n```python\nimport apache_beam as beam\ndef parse(x):\n  import json\n  return json.loads(x)\nwith beam.Pipeline(...) as p:\n  (p\n   | beam.io.ReadFromPubSub(...)\n   | beam.Map(parse)\n   | beam.WindowInto(beam.window.FixedWindows(60*60), allowed_lateness=15*60)\n   | beam.CombinePerKey(...)\n   | beam.io.WriteToBigQuery(...)\n  )\n```\n\n## Follow-up Questions\n- How would you verify exactly-once with insertId in BigQuery?\n- How would you simulate late data during testing?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-11",
        "questionId": "q-7294",
        "question": "Design a beginner-friendly video transcoding pipeline on GCP: when a new video lands in gs://customer-videos/originals, trigger a Cloud Run service (ffmpeg in container) to output 720p and 480p MP4 files in gs://customer-videos/exports with names <basename>_720p.mp4 and <basename>_480p.mp4. Ensure idempotent writes, minimal IAM bindings, and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming pipeline: Pub/Sub per region -> Dataflow -> BigQuery. Use Pub/Sub message IDs for exactly-once semantics with dedup. Partition BigQuery tables by region and ticker; allow schema evo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Publish each webhook JSON to Pub/Sub. A Dataflow pipeline reads from Pub/Sub, applies Cloud DLP redaction, validates against a schema, writes the raw event to a multi-region Cloud Storage bucket with",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest via multi-region Pub/Sub; Dataflow streaming dedups on event_id (stateful DoFn) and enriches with Firestore profile; sink to partitioned BigQuery using insertId(event_id) for idempotency; emit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Eventarc to route Storage finalize events from gs://customer-videos/originals to a Cloud Run service that runs ffmpeg in a container to create 720p and 480p MP4s in gs://customer-videos/exports wi",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical data processing robotics: event-driven triggers, video processing with containerized ffmpeg, and idempotent outputs.\n\n## Key Concepts\n- Event routing via Eventarc from Storage\n- Cloud Run containerized ffmpeg transcoding\n- Idempotent writes with deterministic names\n- Minimal IAM and secure service accounts\n\n## Code Example\n```javascript\n// No code in candidate answer; pseudocode omitted due to interview framing\n```\n\n## Follow-up Questions\n- How would you scale the transcoding to many concurrent files?\n- How would you monitor failures and retry policies?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-12",
        "questionId": "q-1269",
        "question": "Design a real-time data pipeline on GCP for a multi-tenant SaaS where each tenant's data must reside in a specified region, is encrypted with CMEK, and access is strictly controlled per-tenant using IAM Conditions and VPC Service Controls; use Pub/Sub, Dataflow, and BigQuery, ensure idempotent writes and exactly-once semantics, and include monitoring and incident response steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a streaming Dataflow template that subscribes to region-local Pub/Sub topics, processes per-tenant events with fixed windows, and writes to per-tenant BigQuery datasets in the same region using CM",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a beginner-friendly GCP workflow to monitor IAM policy changes: export Cloud Audit Logs to Pub/Sub, a Cloud Function filters events for policy_change, classifies as ADD/REMOVE/UPDATE, and write",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a region-scoped, CMEK-protected pipeline: publish tenant events to per-tenant Pub/Sub topics, Dataflow streaming to region-specific BigQuery datasets, enable CMEK on Cloud Storage and BigQue",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a multi-region ingestion: gs://payments/raw/ → Cloud Function validates JSON schema, publishes to Pub/Sub; Dataflow reads Pub/Sub, deduplicates by (tenant_id, event_id), and writes to a partit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a cross-tenant pipeline with data residency and security controls, spanning Pub/Sub, Dataflow, and BigQuery, while addressing reliability (idempotency) and observability.\n\n## Key Concepts\n\n- Data residency by tenant/region\n- CMEK on Storage and BigQuery\n- IAM Conditions and VPC Service Controls\n- Exactly-once vs at-least-once semantics\n- Idempotent writes and deduplication\n- Observability and incident response\n\n## Code Example\n\n```python\n# Example Beam pipeline skeleton (Python)\nimport apache_beam as beam\nimport json\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\ndef to_row(msg):\n    obj = json.loads(msg.decode('utf-8'))\n    obj['row_id'] = f\"{obj['tenant']}_{obj['event_id']}\"\n    return obj\n\nopts = PipelineOptions(streaming=True, project='PROJECT')\nwith beam.Pipeline(options=opts) as p:\n    (p\n     | 'ReadFromPubSub' >> beam.io.ReadFromPubSub(topic='projects/PROJECT/topics/tenant-events')\n     | 'Parse' >> beam.Map(to_row)\n     | 'ToBQ' >> beam.io.WriteToBigQuery(\n           table='PROJECT:DATASET.TENANT_EVENTS',\n           schema='tenant STRING, event_id STRING, payload STRING, row_id STRING',\n           write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND,\n           create_disposition=beam.io.BigQueryDisposition.CREATE_IF_NEEDED)\n    )\n```\n\n## Follow-up Questions\n\n- How would you validate data residency policy compliance across regions in production?\n- What strategies would you use to evolve the schema per-tenant without downtime?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-13",
        "questionId": "q-23971",
        "question": "Design a streaming data ingestion pipeline in GCP that handles multi-tenant user events arriving on Pub/Sub, validates schema, deduplicates by event_id, enriches with user profile from Firestore, and writes to a partitioned BigQuery table with insertId to ensure idempotence. Include per-tenant isolation, security controls, late data handling, monitoring, and a test plan. How would you implement and what trade-offs would you consider?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set up Pub/Sub topic raw-sensor-events; deploy a Cloud Function triggered by Pub/Sub to validate required fields (sensor_id, timestamp, value) and discard malformed records. For valid messages, write",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Dataflow streaming pipeline (Beam) that reads Pub/Sub events, validates JSON/Avro, deduplicates by event_id within a small window, enriches with Firestore user profiles, and writes to a partitio",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement with Pub/Sub to Dataflow (Python Beam). Deduplicate by event_id, window 30s per user, compute per-user fail_rate (fails/attempts) in window, emit alert if fail_rate > 0.3; write enriched eve",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Strategy: Pub/Sub -> Dataflow Streaming with fixed windows (5 min) and late data handling, write to a partitioned BigQuery table using insertId to ensure idempotence. Use regional Dataflow templates,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design scalable, fault-tolerant streaming architectures that handle multi-tenant data, ensure exactly-once semantics, enforce isolation, and integrate cross-service enrichments.\n\n## Key Concepts\n\n- Dataflow/Beam streaming pipelines\n- Pub/Sub as the ingestion buffer\n- Schema validation and event deduplication (event_id) with windowing\n- Enrichment via Firestore user profiles\n- BigQuery writes with insertId for idempotence and partitioned tables\n- Per-tenant isolation (datasets + authorized views), CMEK, and IAM controls\n- Late data handling (allowed lateness), monitoring, and cost controls\n\n## Code Example\n\n```python\nimport apache_beam as beam\n\n# Pseudo-implementation for clarity\n\ndef parse(msg):\n  # decode and validate\n  return {\"event_id\": ..., \"tenant\": ..., \"payload\": ...}\n\nclass EnrichFn(beam.DoFn):\n  def process(self, elem):\n    # lookup Firestore profile by tenant/user id\n    elem[\"profile\"] = fetch_profile(elem[\"payload\"][\"user_id\"])\n    yield elem\n\nwith beam.Pipeline(options=opts) as p:\n  (p\n   | 'Read PubSub' >> beam.io.ReadFromPubSub(topic=TOPIC)\n   | 'Parse' >> beam.Map(parse)\n   | 'Deduplicate' >> beam.WithKeys(lambda e: e['event_id'])\n   | 'Group' >> beam.GroupByKey()\n   | 'TakeOne' >> beam.Map(lambda kv: kv[1][0])\n   | 'Enrich' >> beam.ParDo(EnrichFn())\n   | 'WriteBQ' >> beam.io.WriteToBigQuery(\n       table='project:dataset.events',\n       insert_retry_limit=3,\n       insert_data=True,\n       method=beam.io.WriteToBigQuery.Method.FILE_PER_PARTITION,\n       with_id=lambda e: e['event_id']\n     )\n  )\n```\n\n## Follow-up Questions\n\n- How would you test exactly-once guarantees with duplicate Pub/Sub messages?\n- How do you evolve the schema while preserving backward compatibility?\n- What are trade-offs between per-tenant datasets vs shared tables with access controls?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-14",
        "questionId": "q-14955",
        "question": "Design a beginner-friendly GCP workflow to ingest mobile telemetry files arriving in gs://telemetry/raw. Add a lightweight data provenance layer: a Cloud Function extracts filePath, sourceService, and ingestionTime, and writes a provenance row to BigQuery provenance.table with insertId=filePath+ingestionTime. Validate schema, ensure idempotency, and keep raw vs processed data separated. Include minimal IAM and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pub/Sub for ingestion and a Dataflow streaming job to parse logs and write into a single BigQuery table partitioned by tenant_id and date. Enforce isolation with per-tenant authorized views, enabl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Solution Design ### Architecture Overview An event-driven GCP workflow processes mobile telemetry files as follows: 1. **Ingestion**: Files arrive in `gs://telemetry/raw` 2. **Trigger**: Cloud Storage `google.storage…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a streaming Dataflow job subscribing to Pub/Sub, a Beam DoFn validates against a schema, and writes to BigQuery with insertId = event_id for idempotence. Simultaneously emit Parquet into gs://tele",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a real-time pipeline with Pub/Sub -> Dataflow (Beam) -> BigQuery. Ingest JSON events into Pub/Sub, Dataflow enriches using a cached profile lookup, deduplicates by event_id, and writes to a date-p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates understanding of beginner-friendly, event-driven data ingestion with a provenance layer and simple idempotency mechanisms.\n\n## Key Concepts\n- **Event-driven architecture**: Cloud Functions triggered by Cloud Storage events\n- **Schema validation**: Lightweight JSON validation for telemetry data integrity\n- **Idempotency**: BigQuery's `insertId` field prevents duplicate records\n- **Data lineage**: Tracking the flow from raw to processed data\n- **Separation of concerns**: Keeping raw and processed data in distinct locations\n\n## Architecture Highlights\nThis design prioritizes simplicity and reliability. The single Cloud Function handles both provenance tracking and file movement, reducing operational complexity. Using `insertId` for idempotency eliminates the need for separate duplicate detection logic, while the raw/processed directory structure ensures clear data lifecycle management.",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-15",
        "questionId": "q-3014",
        "question": "Design a beginner-friendly privacy-conscious ingestion workflow on GCP for CSV analytics logs. When a CSV file lands in gs://analytics-logs/csv/, a Cloud Function validates the header and redacts PII fields (emails and phone numbers) in every row, writing a sanitized file to gs://redacted-logs/csv/ with the same name. A second Cloud Function loads the sanitized file into a date-partitioned BigQuery table analytics.logs, using a deterministic loadId for idempotency. Outline IAM least privilege, a basic test plan with sample input including PII, and how you would verify redaction and idempotent loads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Advanced GCP real-time telemetry pipeline design: vehicles emit events to Pub/Sub per region at 1–10 MB/s. Implement Dataflow streaming that handles schema evolution, deduplicates by event_id, compute",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a real-time multi-tenant event pipeline on GCP: Pub/Sub ingress, Dataflow streaming processing, BigQuery sink. Enforce per-tenant quotas and data residency with regional per-tenant datasets, guarantee exactly-onc…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a line-by-line Cloud Function triggered by Storage finalization to validate fields and types; publish valid events to Pub/Sub; a Cloud Run container consumes and upserts into BigQuer",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two Cloud Functions: the first validates the CSV header, redacts PII (emails and phone numbers) using regex patterns on every row, then writes the sanitized file to gs://redacted-logs/csv/ with the same filename. The se…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical data sanitization, event-driven processing, and BigQuery loading under real-world constraints.\n\n## Key Concepts\n- Cloud Functions triggered by Cloud Storage events\n- PII redaction with regex patterns and data privacy fundamentals\n- Idempotent loads using deterministic IDs\n- Least-privilege IAM and minimal security surface area\n- End-to-end test planning with sample inputs\n\n## Code Example\n```javascript\nfunction redactRow(row) {\n  row.email = '[REDACTED_EMAIL]';\n  row.phone = '[REDACTED_PHONE]';\n  return row;\n}\n```\n\n## Follow-up Questions\n- How would you test idempotency for reprocessing the same file?\n- What monitoring and alerting would you implement for this workflow?\n- How would you handle schema evolution in the CSV files?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-16",
        "questionId": "q-7252",
        "question": "You're designing a cross-region log ingestion pipeline for external partners. Partners post JSON events to a gateway; implement event-driven ingestion using Cloud Run, Pub/Sub, Dataflow, and BigQuery. Requirements: immutable raw in a multi-region Cloud Storage bucket with versioning, processed analytics in Parquet in another bucket, Cloud DLP redaction of PII, exactly-once processing semantics, audit trails, minimal IAM roles, and robust failure handling (DLQ, retries)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Publish each webhook JSON to Pub/Sub. A Dataflow pipeline reads from Pub/Sub, applies Cloud DLP redaction, validates against a schema, writes the raw event to a multi-region Cloud Storage bucket with",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement end-to-end observability in a simple GCP data pipeline: a Cloud Function validates the CSV from gs://incoming-csvs/ (schema check, redacts PII), publishes valid rows to Pub/Sub; a Cloud Run",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Pub/Sub to Dataflow streaming pipeline with a central per-tenant Schema Registry in Cloud Storage. Dataflow DoFns validate events against the tenant’s current schema version, migrate older",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "The architecture ingests per-tenant telemetry into Pub/Sub, runs a policy engine (Cloud Run) to validate IAM bindings against a baseline in Cloud Storage, redacts PII, and writes idempotently to BigQu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDesigns a cross-region, secure, reproducible log ingestion pipeline with real-world constraints: data governance, idempotency, and auditability. Tests understanding of event-driven design, GCS regional storage, and streaming analytics.\n\n## Key Concepts\n- Pub/Sub, Dataflow/Beam, Cloud DLP, Cloud Storage multi-region, BigQuery streaming, IAM.\n- Idempotency via insertId and structured naming; dead-letter queues; cross-region replication.\n\n## Code Example\n```python\n# Pseudo Dataflow skeleton\np = beam.Pipeline(options=opts)\n(p\n  | 'Read' >> beam.io.ReadFromPubSub(...)\n  | 'Redact' >> beam.Map(lambda e: dlp_redact(e))\n  | 'Validate' >> beam.ParDo(ValidateFn())\n  | 'WriteRaw' >> beam.io.WriteToText('gs://logs/raw/partition/', file_name_suffix='.json')\n  | 'ToParquet' >> beam.io.parquetio.WriteToParquet('gs://logs/processed', schema=SCHEMA)\n)\n```\n\n## Follow-up Questions\n- How would you monitor end-to-end latency and failure rates? \n- How to handle schema evolution without breaking downstream analytics?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-17",
        "questionId": "q-29148",
        "question": "Design an advanced tenant-isolated data pipeline on GCP that ingests per-tenant JSON events from Pub/Sub, streams via Dataflow, redacts PII with DLP, and writes to per-tenant BigQuery datasets encrypted with CMEK. How would you enforce least privilege with IAM Conditions and VPC Service Controls, include a DLQ, tests, and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "This question assesses advanced multi-tenant architecture skills, focusing on data governance, isolation, and cost optimization in a streaming pipeline context. To answer this comprehensively: **Pipeline Architecture**…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "On finalize of a new object in gs://ingest-bucket, a Cloud Function copies it to gs://dr-bucket in europe-west1 with the same path. Idempotency is achieved by checking destination MD5; if md5 matches,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a regional streaming pipeline: on‑prem and GKE emit to region Pub/Sub topics; Dataflow streaming enforces exactly-once, writes partitioned regional BigQuery tables, and triggers Vertex AI scori",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a multi-tenant data lake on GCP: raw lands in gs://data-lake/raw/{tenant}, Dataflow enforces schema and writes to curated gs://data-lake/curated/{tenant}/. Each tenant gets a separate BigQuery",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses advanced multi-tenant architecture skills, focusing on data governance, isolation, and cost optimization in a streaming pipeline context.\n\n## Key Concepts\n- Per-tenant datasets in BigQuery for strict data isolation\n- Pub/Sub topic partitioning by tenant_id for proper event routing\n- Dataflow streaming jobs with DLP integration for PII redaction\n- Customer-managed encryption keys (CMEK) for encryption-at-rest controls\n- IAM Conditions for attribute-based access control (ABAC)\n- VPC Service Controls to define security perimeters\n- Dead Letter Queue (DLQ) for failed message handling and retry mechanisms",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-18",
        "questionId": "q-3609",
        "question": "Design a fault-tolerant, multi-region ingestion pipeline for real-time clickstream analytics on GCP: events arrive as JSON in regional Pub/Sub topics; a Dataflow streaming job deduplicates by event_id and writes to per-region BigQuery datasets with insertId, while a centralized BigQuery dataset aggregates near-real-time metrics. Explain exactly-once semantics, cross-region replication, IAM boundaries, and a test plan including regional failover scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a fault-tolerant, multi-region ingestion pipeline using regional Pub/Sub topics for event ingestion, Dataflow streaming jobs with stateful deduplication by event_id and BigQuery insertId for exactly-once write…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a single Pub/Sub topic carrying tenantId; a Dataflow streaming pipeline (Beam) deduplicates by insertId, writes to per-tenant BigQuery partitions (dataset/tablename + ingestion date). Enforce stri",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I'll implement a Cloud Function triggered by new gs://iot-raw/YYYY/MM/DD/*.jsonl, validate each line against the schema, hash device_id with SHA-256, and drop invalid lines. Valid records go into BigQ",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a GCP streaming ingestion using Pub/Sub Protobuf messages and Dataflow. Decode payloads, verify against a schema registry, deduplicate by message_id, and compute per-tenant 1-minute aggregat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates expertise in building production-grade, cross-region data pipelines with strict consistency guarantees using GCP managed services (Pub/Sub, Dataflow, BigQuery) and proper IAM governance.\n\n## Key Concepts\n- Exactly-once processing semantics across distributed regions\n- Dual-layer deduplication: event_id in Dataflow + insertId in BigQuery\n- Stateful stream processing with proper windowing and checkpointing\n- Multi-region BigQuery architecture: regional datasets + centralized aggregation\n- IAM boundaries with region-scoped service accounts and least-privilege access controls",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-19",
        "questionId": "q-21279",
        "question": "Design an intermediate GCP workflow to continuously detect and remediate overly permissive IAM bindings across a production project. The project hosts Cloud Run services and GKE clusters. Use IAM Recommender and Asset Inventory to surface bindings granting roles/editor or allUsers/allAuthenticatedUsers. Implement a Cloud Function triggered by Asset Inventory changes that idempotently patches risky bindings to least-privilege roles, logs remediation events to BigQuery (insertId = project+bindingHash), and supports a dry-run mode. Include test plan and rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a streaming Dataflow job consuming Pub/Sub; key events by (tenant_id, event_id) to guarantee idempotent inserts into per-tenant BigQuery datasets. Route data to region-specific datasets for reside",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Terraform provisions: Pub/Sub topic telemetry.raw, Cloud Function telemetry-processor (Node.js) triggered by the topic, and two GCS buckets with versioning: telemetry-raw and telemetry-sanitized. The",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Dataflow with Pub/Sub input, 5-minute windows, and BigQuery sink; enforce exactly-once via Dataflow’s semantics and BigQuery insertId; implement event_id-based deduplication in a stateful DoFn; ro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a GCP workflow to continuously detect and remediate overly permissive IAM bindings in a production project. The project hosts Cloud Run services and GKE clusters. Use IAM Recommender and Asset Inventory to identi…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical IAM governance skills in production environments, testing end-to-end tooling knowledge, safety mechanisms, and operational considerations.\n\n## Key Concepts\n- **IAM Recommender and Asset Inventory**: Detect risky bindings and monitor changes in real-time\n- **Pub/Sub triggers**: Enable continuous monitoring of IAM policy modifications\n- **Idempotent remediation**: Ensure repeated executions produce consistent results without duplication\n- **Least-privilege principles**: Replace overly permissive roles with minimal required permissions\n- **BigQuery logging with deterministic insertIds**: Prevent duplicate log entries using project+bindingHash\n- **Dry-run mode**: Validate remediation logic safely before applying changes\n- **Rollback strategy**: Emergency recovery mechanism to restore previous binding states\n\n## Code Example\n```javascript\nconst { AssetServiceClient } = require('@google-cloud/asset');\nconst { BigQuery } = require('@google-cloud/bigquery');\nconst { IAMClient } = require('@google-cloud/iam');\n\nconst assetClient = new AssetServiceClient();\nconst bqClient = new BigQuery();\nconst iamClient = new IAMClient();\n\nconst RISKY_ROLES = ['roles/editor', 'roles/owner'];\nconst PUBLIC_MEMBERS = ['allUsers', 'allAuthenticatedUsers'];\n\nexports.remediateIamBinding = async (event, context) => {\n  const pubSubMessage = JSON.parse(Buffer.from(event.data, 'base64').toString());\n  const assetName = pubSubMessage.assetName;\n  \n  if (!assetName.includes('policy')) return;\n  \n  const [policy] = await assetClient.getIamPolicy({ resource: assetName });\n  const bindingHash = generateHash(policy.bindings);\n  \n  const dryRun = process.env.DRY_RUN === 'true';\n  const remediationRecords = [];\n  \n  for (const binding of policy.bindings) {\n    const isRisky = RISKY_ROLES.includes(binding.role);\n    const isPublic = binding.members.some(m => PUBLIC_MEMBERS.includes(m));\n    \n    if (isRisky || isPublic) {\n      const suggestedRole = await getRecommenderSuggestion(binding, policy);\n      \n      if (!dryRun) {\n        await iamClient.patchServiceAccountIamPolicy({\n          policy: updateBinding(policy, binding, suggestedRole)\n        });\n      }\n      \n      remediationRecords.push({\n        projectId: process.env.GCP_PROJECT,\n        bindingHash,\n        role: binding.role,\n        suggestedRole,\n        dryRun,\n        timestamp: new Date()\n      });\n    }\n  }\n  \n  await logToBigQuery(remediationRecords);\n};\n\nasync function logToBigQuery(records) {\n  for (const record of records) {\n    await bqClient.dataset('iam_remediation').table('logs').insert([{\n      insertId: `${record.projectId}+${record.bindingHash}`,\n      ...record\n    }]);\n  }\n}\n\nfunction generateHash(bindings) {\n  const crypto = require('crypto');\n  return crypto.createHash('sha256').update(JSON.stringify(bindings)).digest('hex').slice(0, 16);\n}\n```\n\n## Test Plan\n1. **Unit tests**: Mock IAM Recommender responses and verify binding transformation logic\n2. **Integration tests**: Deploy to staging project and validate dry-run output matches expected changes\n3. **Chaos testing**: Inject malformed policies and verify graceful error handling\n4. **Load testing**: Verify BigQuery logging handles concurrent remediation events\n\n## Rollback Strategy\n- Store pre-remediation policy snapshots in Cloud Storage before applying changes\n- Implement a Cloud Function HTTP endpoint to restore specific snapshots\n- Maintain a 30-day retention window for rollback artifacts\n- Alert on remediation failures to enable manual intervention",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-20",
        "questionId": "q-29389",
        "question": "Design an intermediate GCP workflow to enforce regional data residency and retention for streaming analytics. Ingest user events to gs://region-logs/{region}/raw.json, validate with a Cloud Function, redact PII, publish sanitized records to Pub/Sub, and have a Cloud Run consumer write to region-scoped BigQuery datasets with idempotent upserts. Implement per-tenant IAM conditions, exponential backoff, dead-letter handling, testing, and audit logging?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a real-time telemetry ingestion pipeline using Pub/Sub as the entry point, Dataflow streaming for per-minute windowed aggregations grouped by device and region, and BigQuery as the sink with exactly-once seman…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route region-specific events to gs://region-logs/{region}/, where a Cloud Function validates payloads, redacts PII using a library like google-data-masking, and publishes sanitized records to a region-scoped Pub/Sub top…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a multi-tenant SaaS logs pipeline with Pub/Sub topics per tenant, a Dataflow streaming job that deduplicates by log_id, writes to per-tenant partitioned BigQuery tables, and stores raw JSON",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Trigger on gs://datasets/input/file.csv; a Cloud Function validates the CSV header and size, then publishes a Pub/Sub message to datasets-transform. A Cloud Run service consumes the message, streams t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design data residency, isolation, and reliable streaming with Cloud Run/Functions, Pub/Sub, and BigQuery. Realistic constraints include per-tenant IAM conditions, idempotent writes, and regulatory retention requirements. This also probes testing discipline for schema drift and data lifecycle management.\n\n## Key Concepts\n- **Data residency**: Regional GCS buckets, Pub/Sub topics, and BigQuery datasets scoped per region\n- **Idempotent writes**: Deterministic insertId generation (region:tenant:event_id) + MERGE or INSERT with ON CONFLICT to handle duplicates\n- **IAM Conditions**: Resource tags on Pub/Sub subscriptions and BigQuery datasets for per-tenant least-privilege access\n- **Resilience**: Exponential backoff with jitter on Pub/Sub delivery, dead-letter queues for poison messages\n- **Auditability**: Cloud Audit Logs with Data Access logs enabled for compliance\n\n## Code Example\n```python\nfrom google.cloud import bigquery\nimport hashlib\nimport logging\n\ndef generate_insert_id(region: str, tenant: str, event_id: str) -> str:\n    raw = f\"{region}:{tenant}:{event_id}\"\n    return hashlib.sha256(raw.encode()).hexdigest()[:16]\n\ndef upsert_event(region: str, tenant: str, event: dict) -> None:\n    client = bigquery.Client()\n    table_id = f\"{region}.{tenant}_events\"\n    insert_id = generate_insert_id(region, tenant, event[\"id\"])\n    \n    row = {\"insert_id\": insert_id, \"region\": region, \"tenant\": tenant, **event}\n    errors = client.insert_rows_json(table_id, [row])\n    \n    if errors:\n        logging.error(f\"Upsert failed: {errors}\")\n        raise RuntimeError(errors)\n```\n\n## Testing Strategy\n- **Unit**: Mock BigQuery client, verify insertId determinism\n- **Integration**: Use Pub/Sub emulator + local BigQuery; assert idempotency by publishing duplicate event_ids\n- **Chaos**: Inject network failures to validate exponential backoff and dead-letter routing\n\n## Retention Implementation\nConfigure BigQuery dataset default partition expiration and table clustering by tenant for cost-efficient retention policies aligned with regional compliance requirements.",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-21",
        "questionId": "q-16424",
        "question": "Design a production-grade, multi-tenant log ingestion pipeline on GCP for a SaaS product. Incoming per-tenant logs arrive as JSON in Pub/Sub (topic per tenant); implement a Dataflow streaming job that deduplicates by log_id, enforces exactly-once semantics, and writes to per-tenant, partitioned BigQuery tables. Store raw logs in CMEK-protected GCS. Include IAM least privilege, schema evolution strategy, DLQ/replay safety, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set up a Pub/Sub pipeline: publish audit events to audit-logs; a Cloud Run service subscribes for enrichment (normalize fields, add user role from BigQuery) and republishes to audit-logs-processed. A",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up a streaming ingestion: partner CSVs in gs://partner-data/tenant-*/incoming, Pub/Sub per tenant -> Dataflow (Beam) pipeline to parse CSV, validate against a schema registry in Firestore, enrich",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a real-time telemetry pipeline on GCP for multiple customers. Telemetry arrives at Pub/Sub topics per tenant; a Cloud Dataflow streaming job computes a 5-minute sliding-window anomaly score, writes results to per…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a multi-tenant SaaS logs pipeline with Pub/Sub topics per tenant, a Dataflow streaming job that deduplicates by log_id, writes to per-tenant partitioned BigQuery tables, and stores raw JSON",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssess end-to-end data contracts, idempotency, security, and observability for multi-tenant workloads.\n\n## Key Concepts\n- Streaming ETL, exactly-once semantics, per-tenant data isolation, CMEK, DLQ, schema evolution\n\n## Code Example\n```python\n# Pseudo-DoFn for dedup on log_id (conceptual)\nclass DedupDoFn(DoFn):\n  def process(self, element, watermark=None):\n    log_id = element[\"log_id\"]\n    if not self._seen(log_id):\n      self._mark_seen(log_id)\n      yield element\n```\n\n## Follow-up Questions\n- How would you implement schema evolution for changing log_id formats across tenants?\n- How would you implement replay-safe reprocessing and alerting for data loss?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-22",
        "questionId": "q-5521",
        "question": "Design a beginner-friendly CSV ingestion gate on GCP: when a CSV lands in gs://customer-data/incoming/, a Cloud Function validates the header (id,email,signup_ts) and computes a file_hash; if unseen in BigQuery processed_files, it writes a sanitized copy to gs://customer-data/validated/ with a timestamp suffix and records the hash. A second Function loads rows into partitioned BigQuery customer.full_data using insertId composed of id+hash+row. Include minimal IAM bindings, exponential backoff retries, and a basic test plan with duplicates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-stage CSV ingestion: a Cloud Function on finalization validates header (id,email,signup_ts) and computes a file_hash; if unseen in BigQuery processed_files, it writes a sanitized copy to gs://cust",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Pub/Sub → Dataflow (Apache Beam) streaming pipeline to compute per-device 5-minute rolling features, invoke a Cloud Run scoring service via HTTP, and MERGE results into a BigQuery table in a separate project with per-te…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest payment events through a Pub/Sub subscription with exactly-once delivery enabled; implement a Dataflow streaming pipeline with sliding 5-minute windows keyed by customer_id using Beam's SlidingWindows; compute pe…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Cloud Function triggered by Cloud Storage on gs://secure-uploads/. Use Cloud DLP to inspect for secrets; if detected, copy to gs://quarantine/YYYY/MM/DD and insert a BigQuery row with inse",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a small, auditable ingestion gate with idempotency and two-stage processing.\n\n## Key Concepts\n\n- Cloud Functions trigger on GCS Finalize\n- Header validation and schema enforcement\n- Idempotency via file_hash and BigQuery insertId\n- Two-stage data flow to BigQuery partitioned table\n- Basic IAM least privilege\n\n## Code Example\n\n```javascript\n// Skeleton of validation function\nexports.validateCsvHeader = (event, context) => {\n  // ...\n};\n```\n\n## Follow-up Questions\n\n- How would you test/simulate duplicates in a local sandbox?\n- How would you handle schema drift (e.g., extra columns)?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-23",
        "questionId": "q-18723",
        "question": "Design a scalable, cost-aware real-time ingestion pipeline on GCP for global order events: events published to Pub/Sub by region; a Dataflow (Beam) job validates against the latest Avro schema, deduplicates by event_id within a 5-minute window, enriches with region geohash, and writes to a partitioned BigQuery table orders (date partition). Ensure exactly-once semantics, watermarking, backpressure handling, and canary rollout. Include a test plan with failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a streaming cost anomaly detector: Pub/Sub ingest of usage events; Dataflow streaming job deduplicates by usage_id, computes hourly per-tenant cost, and queries Vertex AI for expected spend. C",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a Cloud Function triggered by gs://telemetry/input-csvs/ that reads the env header, copies the file to gs://telemetry/prod-inputs/ or gs://telemetry/staging-inputs/, and publishes a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an advanced, multi-region telemetry ingest using Pub/Sub per region, a streaming Dataflow pipeline (BigQueryIO) writing to regionally partitioned BigQuery tables with event_id as insertId to en",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a single, scalable Dataflow (Beam) pipeline reading Pub/Sub subscriptions per region, validate via Avro schema registry, apply dedup with event_id using stateful processing for a 5-minute win",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design regional, streaming ingestion with Dataflow, Pub/Sub, and BigQuery at scale, including correctness guarantees.\n\n## Key Concepts\n\n- Streaming dataflow (Beam)\n- Avro schema validation and dynamic schema handling\n- Deduplication using event_id and state\n- Watermarks, late data handling, and windowing\n- Partitioned BigQuery write and idempotent semantics\n- Canary deployments and observability\n\n## Code Example\n\n```javascript\n// Pseudo-beam pipeline sketch\nimport apache_beam as beam\ndef validate_and_enrich(e):\n  # validate against avro schema\n  # deduplicate using event_id in 5-min window\n  # enrich with geohash\n  return enriched\n```\n\n## Follow-up Questions\n\n- How would you handle schema evolution without downtime?\n- How would you simulate backpressure and measure cost impact?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-24",
        "questionId": "q-19659",
        "question": "Design an advanced cross-project GCP pipeline that ingests authentication logs from multiple apps into a centralized BigQuery dataset in near real-time via Pub/Sub and Dataflow, deduplicates by request_id, masks PII with Cloud DLP, partitions by day, and publishes an anomaly score to Cloud Monitoring alerts; include IAM boundaries, a rollback plan, and testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a fault-tolerant, multi-region ingestion pipeline using regional Pub/Sub topics for event ingestion, Dataflow streaming jobs with stateful deduplication by event_id and BigQuery insertId for exactly-once write…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a cross-project pipeline: publish auth logs to Pub/Sub, stream via Dataflow DoFn deduplicating on request_id using a keyed state store, mask PII with Cloud DLP before writing to a daily-partitioned BigQuery ta…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "**TTL-Enforced Ingestion Workflow on GCP** **Architecture Overview:** - **Input**: Files land in `gs://customer-data/raw` with `ttlDays` metadata (default: 7) - **Step 1**: Cloud Function (triggered on object finalize)…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Pub/Sub ingests with a strict schema; Dataflow streaming uses watermarking and insertId to write idempotent, tenant-scoped partitions to BigQuery (tenant/date). Raw events archive to Cloud Storage by",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses your ability to build a secure, scalable, real-time pipeline across GCP projects with data governance, idempotency, and operational alerting.\n\n## Key Concepts\n- **Pub/Sub**: Cross-project streaming ingestion with exactly-once delivery semantics\n- **Dataflow**: Windowing and deduplication by request_id using keyed state\n- **Cloud DLP**: PII masking before storage to meet compliance requirements\n- **BigQuery**: Daily partitioning for query performance and cost optimization\n- **Cloud Monitoring**: Anomaly score alerting with retry logic and backpressure handling\n- **IAM & Security**: Least privilege access, VPC Service Controls, and network boundaries\n\n## Implementation Details\n\n### Pipeline Architecture\n```python\n# Dataflow: deduplicate by request_id\nclass DeduplicateFn(beam.DoFn):\n  def process(self, element, request_id=beam.DoFn.KeyParam):\n    # Use keyed state to track seen request_ids\n    seen = beam.DoFn.StateParam('seen_state', beam.state.BagStateSpec('bag'))\n    if request_id not in seen:\n      seen.add(request_id)\n      yield element\n```\n\n### IAM Boundaries\n- Source projects: Pub/Sub Publisher IAM role\n- Pipeline project: Dataflow Worker, BigQuery Editor, DLP User\n- Analytics project: BigQuery Read-Only with column-level security\n\n### Rollback Plan\n1. Pause Dataflow job and switch to fallback Pub/Sub to GCS archival\n2. Redeploy previous Dataflow template version\n3. Verify data integrity via checksum validation\n\n### Testing Strategy\n- **Unit**: DoFn logic with Beam TestPipeline\n- **Integration**: Pub/Sub emulator to BigQuery with sample data\n- **Performance**: Load test with 10k messages/sec, monitor latency and backpressure",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-15T02:51:15.619Z",
    "version": 1
  }
]