[
  {
    "id": "test-gcp-cloud-engineer",
    "channelId": "gcp-cloud-engineer",
    "channelName": "GCP Cloud Engineer",
    "title": "GCP Cloud Engineer Knowledge Test",
    "description": "Test your understanding of GCP Cloud Engineer with 25 questions.",
    "questions": [
      {
        "id": "gcp-cloud-engineer-q-0",
        "questionId": "q-3628",
        "question": "Design a real-time content moderation pipeline on GCP for a globally distributed social app. Messages arrive via regional Pub/Sub topics; implement a Dataflow streaming job that calls a Vertex AI deployed moderation endpoint to score each post. If score >= threshold, publish to a human-review Pub/Sub and write a row to BigQuery with insertId=event_id. Enforce regional processing for data residency; discuss latency, scaling, idempotency, and failure handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Regional Pub/Sub → Dataflow streaming → Vertex AI moderation endpoint (regional) → results to BigQuery (insertId=event_id) and flagged items to moderation-queue Pub/Sub. Process in source region to sa",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I'll implement a Cloud Function triggered by new gs://iot-raw/YYYY/MM/DD/*.jsonl, validate each line against the schema, hash device_id with SHA-256, and drop invalid lines. Valid records go into BigQ",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Cloud Function triggered by the arrival of a new JSONL file in gs://telemetry-raw/. It runs a DLP scan to classify PII, applies a Data Catalog policy tag (PII-High/PII-Low) to the target BigQ",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A Cloud Function triggered on FINALIZE moves files by copying to gs://archive/YYYY/MM/DD/filename and deleting the source. It upserts a manifest row in BigQuery with insertId=filePath+archivedDate. Us",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a real-time ML-assisted moderation pipeline with data locality, idempotency, and robust failure handling. It also probes how to connect Pub/Sub, Dataflow, Vertex AI, and BigQuery in a low-latency chain while maintaining governance.\n\n## Key Concepts\n- Streaming Dataflow pipelines with DoFn calls to Vertex AI endpoints\n- Regional data residency and regional endpoints\n- Idempotent writes via insertId and exactly-once semantics\n- Alerting and human-review handoff for flagged content\n\n## Code Example\n```python\nimport apache_beam as beam\n\nclass ModerateFn(beam.DoFn):\n    def process(self, element, endpoint_client):\n        content = element['text']\n        score = endpoint_client.moderate(content)\n        yield {'event_id': element['event_id'], 'score': score}\n```\n\n## Follow-up Questions\n- How would you handle model drift and retraining cadence without impacting latency?\n- How would you test end-to-end latency and correctness at scale (e.g., 100k msgs/s) with backpressure scenarios?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-1",
        "questionId": "q-26205",
        "question": "Design a global, low-latency feature-flag platform on GCP for a high-traffic mobile app. Flags live in Cloud Firestore with per-tenant rules; evaluation runs on Cloud Run across regions, served through Cloud CDN; canary rollouts via Pub/Sub events to value updates; ensure sub-20ms latency, auditing, RBAC, encryption, and a robust test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A Cloud Function, triggered on Cloud Storage object finalization, receives files uploaded to gs://telemetry/raw/YYYY/MM/DD/. It validates each CSV against a defined schema, normalizes headers to a canonical format, mask…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Active-passive DR with a warm standby in europe-west1. Primary: us-central1; standby: europe-west1. Cloud SQL Postgres with cross-region read replicas and automatic failover; Pub/Sub multi-region topi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "This design uses two Cloud Functions: **Function 1 (Validation Trigger)** - Event-triggered on uploads to `gs://telemetry/raw/YYYY/MM/DD` - Validates schema and copies valid files to `gs://telemetry/validated/YYYY/MM/DD…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy Cloud Run in 3+ regions behind Cloud CDN; use Memorystore Redis for hot-flag caching, synced from Cloud Firestore via periodic pull with Pub/Sub invalidations on changes. Evaluate per-tenant ru",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes global, low-latency design, cache strategy, and production-worthy controls for feature flags.\n\n## Key Concepts\n- Global multi-region deployments and edge caching\n- Cache invalidation and event-driven updates\n- Canary rollouts, rollback safety, and per-tenant isolation\n- IAM RBAC, encryption at rest, and audit logging\n\n## Code Example\n```javascript\n// Pseudo evaluation flow\nfunction evaluateFlag(tenantId, flagKey){\n  const v = cache.get(`${tenantId}:${flagKey}`);\n  return v ?? fetchFromFirestore(tenantId, flagKey);\n}\n```\n\n## Follow-up Questions\n- How would you mitigate cache stampede during a hot flag change?\n- What metrics would indicate a rollout should rollback?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-2",
        "questionId": "q-6810",
        "question": "Advanced: Ingest mobile analytics that arrive as JSONL in GCS analytics/raw; a Pub/Sub event triggers a Dataflow job that validates, deduplicates by event_id per day, and writes to a date-partitioned BigQuery table analytics.mobile_events using streaming inserts with insertId. Design for exactly-once semantics, schema evolution handling, backfill, a dead-letter queue, and IAM bindings?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Dataflow streaming pipeline triggered by Pub/Sub messages that carry file paths. Dataflow reads JSONL from GCS, validates schema, and deduplicates by event_id within a per-day window. Writes to",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a Dataflow streaming pipeline that reads events from Pub/Sub, deduplicates by event_id, and writes to BigQuery with insertId=event_id to ensure idempotence. JSONL lands in Cloud Storage, publishin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Cloud Function triggered by new objects in gs://app-raw/events/. It loads each event, validates against a versioned schema in gs://schemas/v{current}/schema.json. On success, writes to BigQuery",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a streaming Dataflow template that subscribes to region-local Pub/Sub topics, processes per-tenant events with fixed windows, and writes to per-tenant BigQuery datasets in the same region using CM",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes advanced GCP data ingestion, event-driven design, idempotency, and operational concerns.\n\n## Key Concepts\n\n- Pub/Sub triggers Dataflow\n- BigQuery streaming inserts with insertId\n- Per-day deduplication and schema evolution\n- DLQ and backfill strategies\n\n## Code Example\n\n```javascript\n// Pseudocode: Dataflow DoFn for validation and dedup\n```\n\n## Follow-up Questions\n\n- How would you handle late-arriving events after 24h?\n- How would you test idempotency at scale?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-3",
        "questionId": "q-17383",
        "question": "Design an end-to-end real-time telemetry ingestion pipeline on GCP for a global mobile fleet. Devices publish JSON to Pub/Sub; implement a Dataflow streaming pipeline that uses event-time windows, deduplicates by event_id, and handles late data via allowed lateness. Write per-device data to a partitioned BigQuery table, route failed events to a Cloud Storage DLQ, and alert on anomalies with Cloud Monitoring. Include IAM, cost controls, and a concrete test plan with sample events?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Publish each webhook JSON to Pub/Sub. A Dataflow pipeline reads from Pub/Sub, applies Cloud DLP redaction, validates against a schema, writes the raw event to a multi-region Cloud Storage bucket with",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub for ingress, a Dataflow streaming job built on Apache Beam, with 5-minute fixed event-time windows and 2-minute lateness. Deduplicate by event_id using stateful processing; write per-devic",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Streaming fraud score pipeline on GCP: ingest payments from Pub/Sub; a Dataflow (Beam) job enriches with risk signals from BigQuery and a Redis cache, computes a 5-minute sliding-window score, writes",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Publish telemetry events to a Pub/Sub topic, configure a push subscription to deliver messages to a Cloud Run HTTP endpoint, validate the JSON schema, and perform streaming inserts into BigQuery's telemetry.events table…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a resilient, scalable streaming pipeline on GCP, balancing correctness (exactly-once semantics for dedup + late data) with operational concerns (DLQ, alerts, IAM, budgets).\n\n## Key Concepts\n- Dataflow streaming with Apache Beam\n- Event-time windows and lateness\n- Deduplication with stateful processing\n- Pub/Sub as ingestion layer\n- BigQuery partitioning\n- Dead-letter in Cloud Storage\n- Cloud Monitoring alerts and budgets\n- IAM least privilege\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nimport json\n\nclass DedupFn(beam.DoFn):\n  def process(self, element, timestamp=beam.DoFn.TimestampParam, state=beam.DoFn.StateParam):\n    yield element\n\ndef run():\n  options = PipelineOptions()\n  with beam.Pipeline(options=options) as p:\n    (p\n     | \"ReadFromPubSub\" >> beam.io.ReadFromPubSub(topic=\"projects/PROJECT/topics/telemetry\")\n     | \"ParseJson\" >> beam.Map(lambda b: json.loads(b))\n     | \"KeyByDevice\" >> beam.Map(lambda x: (x[\"device_id\"], x))\n     | \"Window\" >> beam.WindowInto(beam.window.FixedWindows(300))\n     | \"Deduplicate\" >> beam.groupby.KeyedBag()  # simplified placeholder\n     | \"ToBigQuery\" >> beam.io.WriteToBigQuery(\n         table=\"PROJECT:DATASET.telemetry\",\n         schema={\"fields\":[{\"name\":\"device_id\",\"type\":\"STRING\"},{\"name\":\"event_id\",\"type\":\"STRING\"},{\"name\":\"payload\",\"type\":\"STRING\"}]},\n         write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND,\n         create_disposition=beam.io.BigQueryDisposition.CREATE_IF_NEEDED)\n    )\n\nif __name__ == \"__main__\":\n  run()\n```\n\n## Follow-up Questions\n- How would you validate late data handling and dedup at scale?\n- What changes would you make to support multi-region data consistency and cost control?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-4",
        "questionId": "q-5541",
        "question": "You operate a fleet of services across three GCP regions, each emitting VPC flow logs to region-local Cloud Storage buckets (gs://vpc-logs-region-1/, gs://vpc-logs-region-2/, gs://vpc-logs-region-3). Design a real-time pipeline that ingests new logs, deduplicates events across regions, computes per-minute anomaly counts (e.g., spikes in rejected connections), writes the totals to a central BigQuery dataset with exactly-once semantics, and publishes alerts to Pub/Sub when a threshold is exceeded. Outline architecture, Dataflow windowing/triggers/insertId, failure modes, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a streaming Dataflow Flex template that subscribes to Pub/Sub in us-central1, validates incoming Avro/JSON payloads, and deduplicates events per tenant using the combination of tenant_id and event_id. Write to per-t…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Pipeline design: region logs -> Cloud Function publishes to central Pub/Sub; Dataflow streaming reads from Pub/Sub, deduplicates by logId, computes 1-minute fixed windows with 2-minute lateness, and w",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a Cloud Function that triggers when new JSON files land in gs://tagging-requests/ingest, validates each record for required fields (userId, photoId, tags: string[]), publishes valid events to Pub/Sub topic tag…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a streaming pipeline: two Pub/Sub topics (region A and B) feeding a Dataflow (Apache Beam) streaming job that ingests from both, deduplicates by event_id, and writes to a single BigQuery table",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nDemonstrates real-time cross-region ingestion, dedup, windowing, idempotency, and alerting under outages; tests production readiness beyond basic pipelines.\n\n## Key Concepts\n\n- Streaming pipelines with Apache Beam/Dataflow\n- Cross-region deduplication\n- Windowing and late data handling\n- Exactly-once semantics with insertId\n- Failure modes and observability\n\n## Code Example\n\n```javascript\n// Pseudo-beam-like sketch (Python-like)\nfunction addInsertId(event){ return { ...event, insertId: event.logId + '|' + event.ts }; }\n```\n\n## Follow-up Questions\n\n- How would you validate idempotence with synthetic/real logs?\n- How would you simulate regional outages and verify failover behavior?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-5",
        "questionId": "q-24528",
        "question": "Design a beginner-friendly GCP streaming pipeline that ingests device telemetry from Pub/Sub, validates JSON schema, redacts PII with regex, and writes sanitized events to BigQuery with insertId = event_id. Add a dead-letter Pub/Sub for malformed messages and a 1-minute windowed aggregation in Dataflow with per-device counts written to BigQuery. Keep IAM minimal and idempotent writes; include a basic test plan and a rollback option?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set up a Cloud Run HTTPS endpoint to receive partner telemetry and publish to Pub/Sub. A Cloud Function subscribes, validates JSON lines, redacts PII, and writes enriched records to gs://telemetry/enr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Trigger on gs://datasets/input/file.csv; a Cloud Function validates the CSV header and size, then publishes a Pub/Sub message to datasets-transform. A Cloud Run service consumes the message, streams t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline: Cloud Function validates mime type, creates a Cloud Tasks task to invoke a Cloud Run resize endpoint with payload {path,size}. Cloud Run reads image from gs://live-uploads/, writes 256x256.jp",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Dataflow streaming job (Apache Beam, Python) to read Pub/Sub telemetry, validate JSON against a schema, redact PII with regex, and write sanitized events to BigQuery with insertId = event_id. Ro",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical streaming data skills and resilience in a beginner-friendly way, focusing on end-to-end ingestion, validation, and idempotent writes.\n\n## Key Concepts\n- Pub/Sub to Dataflow (Beam)\n- JSON schema validation\n- PII redaction with regex\n- BigQuery insertId for idempotency\n- Dead-letter Pub/Sub\n- Windowed aggregations and per-device counts\n- Minimal IAM configuration\n\n## Code Example\n```python\n# minimal skeleton of a Beam streaming pipeline\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\ndef sanitize(event):\n  # validation and redaction logic here\n  return event\n\nwith beam.Pipeline(options=PipelineOptions()) as p:\n  (p\n   | 'Read' >> beam.io.ReadFromPubSub(topic='projects/…/topics/telemetry')\n   | 'Parse' >> beam.Map(lambda b: json.loads(b))\n   | 'Sanitize' >> beam.Map(sanitize)\n   | 'WriteBigQuery' >> beam.io.WriteToBigQuery('project:dataset.table', insert_disposition=...) \n  )\n```\n\n## Follow-up Questions\n- How would you test message ordering and late data in this pipeline?\n- How would you parameterize schema changes and versioning?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-6",
        "questionId": "q-2826",
        "question": "Design an intermediate-level, end-to-end GCP streaming ingestion for telemetry data from 10k devices. Devices push JSON messages to a Cloud Pub/Sub topic. Build an idempotent pipeline using Dataflow (Beam) that deduplicates by event_id, aggregates per minute per device_type, and writes results to a partitioned BigQuery table telemetry.events (date partition). Include exactly-once considerations, dead-letter handling, and IAM least privilege. Provide testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Dataflow (Beam) pipeline that reads JSON lines from gs://tenant-raw/, applies a DoFn to redact PII, and writes to per-tenant BigQuery tables (auto-create, partitioned) within separate datasets t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Trigger: Cloud Function on finalization of gs://reports/raw/*.csv. Validate header matches expected columns, e.g., id,timestamp,value. Convert to Parquet with PyArrow, store at gs://reports/processed/",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest via Pub/Sub to a Dataflow streaming job, route by tenant_id to per-tenant BigQuery datasets with exactly-once inserts. Use parameterized Dataflow templates, CMEK for each dataset, and dedicated",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a streaming telemetry pipeline: devices send JSON to Pub/Sub with exactly-once delivery; a Dataflow (Beam) job deduplicates on event_id, windows by 1 minute, computes per-device_type sums, and",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical streaming architecture in GCP, focusing on exactly-once semantics, deduplication, windowed aggregations, and error handling, all with real-world IAM constraints.\n\n## Key Concepts\n\n- Pub/Sub exactly-once delivery\n- Dataflow/Beam windowing and dedup by event_id\n- BigQuery partitioned tables for time-partitioned analytics\n- Dead-letter queues and message retries\n- IAM least privilege and service accounts\n\n## Code Example\n\n```python\n# PyBeam skeleton for ingestion, dedupe, and windowed aggregation\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\ndef run():\n    p = beam.Pipeline(options=PipelineOptions(streaming=True))\n    (p\n     | 'ReadFromPubSub' >> beam.io.ReadFromPubSub(subscription='projects/PROJECT/subscriptions/SUB')\n     | 'ParseJson' >> beam.Map(lambda x: json.loads(x))\n     | 'Deduplicate' >> beam.Filter(lambda m: m['event_id'] not in seen)\n     | 'Window' >> beam.WindowInto(beam.window.SlidingWindows(60, 60))\n     | 'KeyByDevice' >> beam.Map(lambda m: (m['device_type'], m['value']))\n     | 'Sum' >> beam.CombinePerKey(sum)\n     | 'ToRow' >> beam.Map(lambda kv: {'device_type': kv[0], 'sum': kv[1], 'dt': ...})\n     | 'WriteToBQ' >> beam.io.WriteToBigQuery('project:dataset.telemetry_events', schema='...', write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND)\n    )\n    p.run()\n```\n\n## Follow-up Questions\n\n- How would you handle late-arriving data and watermarking? \n- How would you test idempotency and failure scenarios? \n- What monitoring metrics would you set up in Cloud Monitoring for SLAs?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-7",
        "questionId": "q-28510",
        "question": "Design a beginner-friendly GCP deployment workflow to rollout a new Cloud Run service with canary traffic: you have an existing service 'orders-api' and a new revision built from a container image in Artifact Registry. Describe how you would split 10% of traffic to the new revision, monitor error rate, and automatically rollback to the stable revision if a 5-minute error rate exceeds 2%. Include minimal IAM bindings and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Event-driven ingestion: Cloud Storage finalization triggers a Cloud Function that validates event schema, upserts with insertId=event_id to BigQuery analytics.events partitioned by date, and logs fail",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Extract .gz, parse NDJSON, validate event_id/user_id/ts/event_type; publish each record to Pub/Sub; Dataflow batch reads Pub/Sub, deduplicates on event_id, and writes to BigQuery with insertId=event_i",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy new revision in Cloud Run using canary: assign 10% traffic to the new revision and 90% to the stable one; set a Cloud Monitoring alert for error rate >2% over 5 minutes; on alert, automatically",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Outline a Dataflow streaming pipeline: Pub/Sub -> Dataflow -> BigQuery per-tenant partitions; DLP redaction and per-tenant tokenization via envelope encryption with CMEK; enforce IAM conditions and se",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical end-to-end deployment, observability, and rollback in a serverless GCP workflow.\n\n## Key Concepts\n- Cloud Run revisions and traffic splitting\n- Artifact Registry delivery\n- Cloud Monitoring alerts and auto-rollback\n- Minimal IAM bindings and safety checks\n\n## Code Example\n```bash\n# Deploy new revision and split traffic\ngcloud run services update-traffic orders-api --to-revisions orders-api-stable-00001=90,orders-api-canary-00002=10 --region us-central1\n\n# Create an alerting policy for error rate\ngcloud monitoring policies create --notification-channels=CHAN --condition-display-name=\"Error rate\" --condition-filter='metric.type=\"cloud_run_revision/request_count\" AND resource.label.\"service_name\"=\"orders-api\"' --duration='300s' --comparison='COMPARISON_GT' --threshold-value=2\n```\n\n## Follow-up Questions\n- How would you test the rollback path manually and automatically?\n- How do you handle cold starts and latency in a canary strategy?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-8",
        "questionId": "q-8610",
        "question": "Design a beginner-friendly pipeline: when a CSV lands in gs://data-uploads/csv/, a Cloud Function validates header (user_id,event,ts,value), normalizes ts to ISO8601, and publishes enriched JSON lines to Pub/Sub. A Cloud Run service subscribes, writes to partitioned BigQuery table analytics.enriched_events with insertId = user_id+'_'+ts_iso. Include minimal IAM, retries, and a test plan with sample CSVs including duplicates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Cloud Function validates header, normalizes ts to ISO8601, and publishes enriched JSON to Pub/Sub on CSV Finalize. Cloud Run, triggered by Pub/Sub, writes to partitioned BigQuery analytics.enriched_ev",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Engineered a real-time analytics pipeline on GCP for a rideshare app: streaming ride events from mobile clients via cross-region Pub/Sub, a Dataflow streaming job performing 5-minute fixed window aggregations with water…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I'll implement a Cloud Function triggered by new gs://iot-raw/YYYY/MM/DD/*.jsonl, validate each line against the schema, hash device_id with SHA-256, and drop invalid lines. Valid records go into BigQ",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Pub/Sub for ingestion, Dataflow (Beam) streaming with a stateful dedup by event_id, writing to analytics.events_YYYYMMDD with insertId to guarantee idempotence. Leverage watermarking and windowing",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end data flow with light-weight components, basic validation, simple enrichment, and idempotency using Pub/Sub push to Cloud Run.\n\n## Key Concepts\n- Cloud Functions event-driven ingestion\n- Pub/Sub messaging and push subscriptions\n- Cloud Run integration for processing\n- BigQuery partitioning and insertId for idempotency\n- Minimal IAM and retry strategies\n\n## Code Example\n```javascript\n// Example: normalize timestamp to ISO8601\nfunction toIso(ts) {\n  return new Date(ts).toISOString();\n}\n```\n\n## Follow-up Questions\n- How would you handle schema changes in header or new fields?\n- How would you test idempotency if a message is redelivered?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-9",
        "questionId": "q-21239",
        "question": "Design a beginner-friendly GCP workflow to normalize and validate partner CSV uploads: partner users drop CSVs into gs://partner-uploads/. Headers vary in case and date formats; build a Cloud Function that validates required columns (customer_id, timestamp, amount), normalizes headers to canonical lowercase names, converts timestamps to ISO 8601, and streams rows into BigQuery table partner_sales.sales with insertId = CONCAT(customer_id, '_', date, '_', CAST(row_num AS STRING)). Failing rows go to gs://partner-uploads/invalid/YYYY/MM/DD; log a summary to BigQuery. Include minimal IAM, idempotency, and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a Cloud Function triggered on gs finalized events, parse CSV, normalize headers to lowercase, validate customer_id, timestamp, amount, coerce timestamp to ISO 8601, then upsert each row to Big",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Grant the Cloud Run service account the roles/cloudsql.client, enable the sqladmin API, deploy with --add-cloudsql-instances PROJECT:REGION:INSTANCE, and connect using the Unix socket /cloudsql/INSTAN",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a streaming telemetry pipeline: devices send JSON to Pub/Sub with exactly-once delivery; a Dataflow (Beam) job deduplicates on event_id, windows by 1 minute, computes per-device_type sums, and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Dataflow streaming job (Apache Beam) to ingest regional Pub/Sub topics, compute per-minute aggregates, and write to a centralized BigQuery table with a deterministic insertId (region|timestamp|m",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to build a robust CSV ingestion path with data quality checks, idempotency, and minimal IAM. It exercises deterministic insertIds, error routing, and observable test plans.\n\n## Key Concepts\n- Cloud Functions event-driven processing on GCS finalization\n- CSV parsing with header normalization and type coercion\n- Idempotent BigQuery streaming inserts using insertId\n- Error handling: moving invalid files to a dedicated bucket and logging summaries\n- IAM least privilege and simple retry/DLQ strategy\n\n## Code Example\n```javascript\n// Node.js Cloud Function skeleton\nexports.normalizeCsv = async (event, context) => {\n  const bucket = event.bucket;\n  const name = event.name;\n  // 1) read CSV from gs://${bucket}/${name}\n  // 2) normalize headers to lowercase; validate required cols\n  // 3) for each row, transform timestamp to ISO 8601; compute insertId\n  // 4) write to BigQuery: partner_sales.sales with insertId\n  // 5) on error, move original file to invalid/YYYY/MM/DD and log summary\n};\n```\n\n## Follow-up Questions\n- How would you handle very large CSVs without loading whole file into memory?\n- How would you ensure exactly-once semantics if Cloud Function is retried?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-10",
        "questionId": "q-23252",
        "question": "Design a beginner-friendly GCP ingestion workflow for daily mobile telemetry uploads. A Cloud Storage bucket gs://app-logs/input receives a daily CSV file. A Cloud Function triggered on new objects validates a simple schema (columns: user_id, timestamp, event), normalizes column names, writes cleaned lines to gs://app-logs/processed/YYYY/MM/DD and inserts a summary row into BigQuery with insertId=filePath+date. Include minimal IAM bindings and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Cloud Function triggered by storage.object.finalize on gs://app-logs/input. Parse the CSV, validate headers, skip invalid lines, normalize to userId, ts, event, and write cleaned lines to",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a beginner-friendly pipeline: a Cloud Function validates the CSV uploaded to gs://telemetry/raw/, converts rows to JSONL, and publishes them to Pub/Sub. A Dataflow batch job reads Pub/Sub, c",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a beginner-friendly GCP pipeline for validating and collecting user feedback from mobile apps: when a JSON file lands in gs://feedback/input, a Cloud Function triggers on file finalization, validates the JSON sch…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I’d implement a Cloud Function (Python) triggered on finalization of gs://raw-activity/YYYY/MM/DD/logs.jsonl. It validates lines, deduplicates by (user_id,event_id), and writes to a date‑partitioned B",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a simple, observable data ingestion with GCP services and idempotent writes.\n\n## Key Concepts\n- Cloud Functions event triggers, Cloud Storage; CSV parsing; BigQuery insert with insertId; IAM least privilege; basic testing.\n\n## Code Example\n```javascript\n// Pseudocode: read csv, validate header, map columns, write to processed, insert summary\n```\n\n## Follow-up Questions\n- How would you add backoff for transient storage failures?\n- How would you extend to support immutability and retry idempotency across retries?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-11",
        "questionId": "q-5521",
        "question": "Design a beginner-friendly CSV ingestion gate on GCP: when a CSV lands in gs://customer-data/incoming/, a Cloud Function validates the header (id,email,signup_ts) and computes a file_hash; if unseen in BigQuery processed_files, it writes a sanitized copy to gs://customer-data/validated/ with a timestamp suffix and records the hash. A second Function loads rows into partitioned BigQuery customer.full_data using insertId composed of id+hash+row. Include minimal IAM bindings, exponential backoff retries, and a basic test plan with duplicates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Storage-triggered Cloud Function to validate JSONL lines for tenant, timestamp, and event; redact PII; and write sanitized lines to gs://telemetry/sanitized/{tenant}/{YYYY/MM/DD}/. A Cloud Ru",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-stage CSV ingestion: a Cloud Function on finalization validates header (id,email,signup_ts) and computes a file_hash; if unseen in BigQuery processed_files, it writes a sanitized copy to gs://cust",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Ingest to Pub/Sub, Dataflow streaming with insertId dedup, tenant-aware BigQuery writes, DLQ for failed records, exactly-once via idempotent writes and transactional sinks, per-tenant partitioning, an",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a GCP real-time moderation pipeline: trigger a Cloud Function on uploads to gs://live-content/, publish a message to Pub/Sub, process with Dataflow streaming, call Vertex AI for classification,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a small, auditable ingestion gate with idempotency and two-stage processing.\n\n## Key Concepts\n\n- Cloud Functions trigger on GCS Finalize\n- Header validation and schema enforcement\n- Idempotency via file_hash and BigQuery insertId\n- Two-stage data flow to BigQuery partitioned table\n- Basic IAM least privilege\n\n## Code Example\n\n```javascript\n// Skeleton of validation function\nexports.validateCsvHeader = (event, context) => {\n  // ...\n};\n```\n\n## Follow-up Questions\n\n- How would you test/simulate duplicates in a local sandbox?\n- How would you handle schema drift (e.g., extra columns)?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-12",
        "questionId": "q-13262",
        "question": "Design a beginner-friendly GCP ingestion workflow: a mobile app logs to gs://app-logs/raw/ as JSONL nightly. Implement a Cloud Function triggered by new objects to validate lines for event_id, device_id, ts, event_type; publish valid lines to Pub/Sub app-logs.events; a Dataflow streaming job writes to BigQuery analytics.app_logs with insertId = event_id. Invalid lines go to gs://app-logs/invalid. Include minimal IAM, dead-lettering, and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a serverless ingestion pipeline where a Cloud Function validates each JSONL line for required fields (event_id, device_id, ts, event_type), publishes valid records to Pub/Sub topic app-logs.events, and a Dataflow…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Pub/Sub → Dataflow (Apache Beam) streaming pipeline to compute per-device 5-minute rolling features, invoke a Cloud Run scoring service via HTTP, and MERGE results into a BigQuery table in a separate project with per-te…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Active-passive DR with a warm standby in europe-west1. Primary: us-central1; standby: europe-west1. Cloud SQL Postgres with cross-region read replicas and automatic failover; Pub/Sub multi-region topi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a real-time telemetry pipeline on GCP for multiple customers. Telemetry arrives at Pub/Sub topics per tenant; a Cloud Dataflow streaming job computes a 5-minute sliding-window anomaly score, writes results to per…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis evaluates end-to-end familiarity with GCP serverless components, streaming ETL patterns, and data governance best practices including validation, error handling, and idempotency.\n\n## Key Concepts\n- Event-driven architecture: Cloud Functions with GCS triggers\n- Message queuing: Pub/Sub for reliable delivery\n- Stream processing: Dataflow Beam jobs with exactly-once semantics\n- Data governance: Schema validation and dead-letter handling\n- Idempotency: BigQuery insertId for duplicate prevention\n\n## Implementation Approach\n```javascript\n// Cloud Function: validate JSONL lines\nconst { Storage } = require('@google-cloud/storage');\nconst { PubSub } = require('@google-cloud/pubsub');\n\nexports.validateLogs = async (event, context) => {\n  const storage = new Storage();\n  const pubsub = new PubSub();\n  const bucket = storage.bucket(event.bucket);\n  const file = bucket.file(event.name);\n  \n  const contents = await file.download();\n  const lines = contents.toString().split('\\n').filter(line => line.trim());\n  \n  const validRecords = [];\n  const invalidLines = [];\n  \n  lines.forEach((line, index) => {\n    try {\n      const record = JSON.parse(line);\n      const required = ['event_id', 'device_id', 'ts', 'event_type'];\n      \n      if (required.every(field => record[field])) {\n        validRecords.push({ data: Buffer.from(JSON.stringify(record)) });\n      } else {\n        invalidLines.push({ line, error: 'Missing required fields', lineNumber: index });\n      }\n    } catch (error) {\n      invalidLines.push({ line, error: 'Invalid JSON', lineNumber: index });\n    }\n  });\n  \n  // Publish valid records\n  if (validRecords.length > 0) {\n    await pubsub.topic('app-logs.events').publish(validRecords);\n  }\n  \n  // Move invalid lines to dead-letter bucket\n  if (invalidLines.length > 0) {\n    const invalidContent = invalidLines.map(item => \n      JSON.stringify({ ...item, timestamp: new Date().toISOString() })\n    ).join('\\n');\n    \n    await storage.bucket('app-logs-invalid')\n      .file(`invalid/${event.name}`)\n      .save(invalidContent);\n  }\n  \n  // Move processed file\n  await file.move('processed/' + event.name);\n};\n```\n\n```python\n# Dataflow streaming job\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\nclass ProcessLogRecord(beam.DoFn):\n    def process(self, element):\n        import json\n        record = json.loads(element.decode('utf-8'))\n        \n        yield {\n            'event_id': record['event_id'],\n            'device_id': record['device_id'],\n            'ts': record['ts'],\n            'event_type': record['event_type'],\n            'insertId': record['event_id']\n        }\n\ndef run():\n    options = PipelineOptions([\n        '--project=your-project',\n        '--region=us-central1',\n        '--streaming',\n        '--temp_location=gs://your-temp/temp'\n    ])\n    \n    with beam.Pipeline(options=options) as p:\n        (p \n         | 'ReadFromPubSub' >> beam.io.ReadFromPubSub(\n             subscription='projects/your-project/subscriptions/app-logs-sub')\n         | 'ProcessRecords' >> beam.ParDo(ProcessLogRecord())\n         | 'WriteToBigQuery' >> beam.io.WriteToBigQuery(\n             'analytics.app_logs',\n             schema='event_id:STRING,device_id:STRING,ts:TIMESTAMP,event_type:STRING',\n             write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND,\n             create_disposition=beam.io.BigQueryDisposition.CREATE_IF_NEEDED,\n             insert_id_strategy='USE_FIELD',\n             insert_id_field_name='insertId'))\n```\n\n## IAM Configuration\n```yaml\n# Cloud Function service account\nroles:\n  - roles/storage.objectViewer  # Read from source bucket\n  - roles/storage.objectCreator  # Write to processed and invalid buckets\n  - roles/pubsub.publisher      # Publish to app-logs.events\n\n# Dataflow service account\nroles:\n  - roles/pubsub.subscriber     # Read from app-logs-events subscription\n  - roles/bigquery.dataEditor   # Write to analytics.app_logs\n  - roles/dataflow.worker       # Execute Dataflow jobs\n```\n\n## Dead-Letter Strategy\n- Invalid JSONL lines stored in gs://app-logs/invalid/ with metadata\n- Include error details and line numbers for debugging\n- Set up Cloud Monitoring alerts on invalid log rates\n- Schedule periodic review of dead-letter contents\n\n## Testing Plan\n1. **Unit Tests**: Cloud Function validation logic\n2. **Integration Tests**: End-to-end pipeline with sample data\n3. **Load Tests**: High-volume streaming scenario\n4. **Failure Scenarios**: Malformed JSON, missing fields, Pub/Sub failures\n5. **Idempotency Tests**: Duplicate event_id handling in BigQuery\n```",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-13",
        "questionId": "q-19972",
        "question": "Design a streaming Dataflow pipeline that ingests Pub/Sub JSON events for a fintech fraud-detection system. Enforce exactly-once semantics by deduplicating on insertId, write to BigQuery (partitioned by date) and to an Iceberg table on GCS for the data lake. Include cross-region replication, CMEK, strict IAM, observability, and a rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest events from per-tenant Pub/Sub topics with tenant_id in headers; a Dataflow streaming job deduplicates by event_id, writes raw JSON to gs://telemetry/raw/{tenant}/{date}/, and loads enriched ro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Streaming Dataflow pipeline ingests Pub/Sub JSON events; deduplicate by insertId to ensure exactly-once, write to BigQuery (partitioned by date) and to Iceberg on GCS for the lake. Enable cross-region",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implementation: 1) Cloud Function with Storage trigger reads each JSONL file from gs://raw-telemetry, validates required fields (userId, event, ts), and publishes valid records to Pub/Sub with filePath as a message attr…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Dataflow streaming job (Apache Beam, Python) to read Pub/Sub telemetry, validate JSON against a schema, redact PII with regex, and write sanitized events to BigQuery with insertId = event_id. Ro",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes real-world data engineering at scale: exact-once semantics, multi-sink writes, and governance across regions in GCP.\n\n## Key Concepts\n\n- Exactly-once ingestion and idempotent sinks\n- Beam/Dataflow vs Spark streaming choices\n- BigQuery: streaming inserts vs batch loads, partitioning\n- Iceberg on GCS for data lake\n- Cross-region replication, CMEK, IAM least privilege\n- Schema evolution, observability and rollback\n\n## Code Example\n\n```javascript\n// Pseudo Dataflow-like pipeline sketch\nconst pipeline = createPipeline({/* options */});\npipeline\n  .readPubSub('projects/my/proj/subs/events', {format: 'json'})\n  .map(e => JSON.parse(e))\n  .distinctBy(e => e.insertId)\n  .tee([\n     batchToBigQuery({table:'fraud.analytics', partitionBy:'date'}),\n     writeIcebergOnGCS({path:'gs://lake/fraud/', table:'fraud iceberg'})\n  ]);\n```\n\n## Follow-up Questions\n\n- How would you test idempotency end-to-end under burst load?\n- What approach would you use if Pub/Sub ordering becomes a challenge across regions?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-14",
        "questionId": "q-10827",
        "question": "Design a streaming ingestion pipeline on GCP for telemetry data from thousands of IoT devices arriving as Protobuf messages on Pub/Sub. Build a Dataflow streaming job that decodes Protobuf, validates against a schema registry, deduplicates by message_id, and computes per-tenant 1-minute aggregates (event_count and unique_devices). Late data allowed 60s. Write to a partitioned BigQuery table tenant_id, minute_ts. Use Pub/Sub dead-letter for bad messages, ensure exactly-once semantics, monitor with Cloud Monitoring, and provide a test plan with synthetic messages and duplicates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Cloud Storage onFinalize trigger. Validate each record with a lightweight JSON Schema in the Cloud Function; if valid, write to a partitioned BigQuery table using a deterministic write-id (date + file hash) for id…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a batch Dataflow pipeline that reads Parquet files from gs://tenant-events/raw/YYYY/MM/DD/ for the target date, normalizes to a canonical schema, deduplicates by event_id, and writes to BigQuery w",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest via Pub/Sub, Cloud Function validates schema and redacts PII, publish to validated topic. Dataflow streaming reads validated data, partitions by region, writes to BigQuery (insertId = event_id)",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a GCP streaming ingestion using Pub/Sub Protobuf messages and Dataflow. Decode payloads, verify against a schema registry, deduplicate by message_id, and compute per-tenant 1-minute aggregat",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-time ingestion, schema validation, deduping, and correct windowing at scale.\n\n## Key Concepts\n\n- Streaming ingestion with Pub/Sub and Dataflow\n- Protobuf decoding and schema registry validation\n- Per-tenant 1-minute window aggregations\n- Exactly-once semantics and sink choices\n- Late data handling and dead-letter workflows\n\n## Code Example\n\n```javascript\n// Placeholder: Dataflow skeleton in Java/Python\n```\n\n## Follow-up Questions\n\n- How to test idempotence across retries?\n- How would you scale for 100k devices?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-15",
        "questionId": "q-4267",
        "question": "Design a secure, cost-aware telemetry ingestion pipeline for multi-tenant data on GCP. Inbound JSON logs arrive in gs://telemetry-raw and include tenant_id and event_id. Build an end-to-end flow with Cloud Functions (finalize), Pub/Sub, Dataflow, and BigQuery that isolates tenants, ensures idempotent processing (insertId = tenant_id|event_id|ts), supports schema evolution, and quarantines invalid events. Include a minimal IAM policy and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Cloud Function triggered on gs://telemetry-raw finalize to publish each event to Pub/Sub telemetry.events. Dataflow streaming reads Pub/Sub, validates against a registry-defined schema, and writ",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a CSV ingestion flow where a Cloud Function validates header, enforces types per column, and splits valid and invalid rows; valid rows load to BigQuery reports.items (partitioned by date), fail",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a streaming Dataflow pipeline that ingests Pub/Sub events, deduplicates by a per-event id, writes metrics to BigQuery with insertId to prevent duplicates, and publishes anomalies to a separate",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single bucket with Lifecycle rules: after 45 days transition to ARCHIVE, after 365 days to COLDLINE. Cloud Run HTTP API accepts bucket, object, and range; reads the specified byte range via the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests designing end-to-end data ingestion with tenant isolation, idempotency, schema evolution, and operational guards in GCP.\n\n## Key Concepts\n- Cloud Functions triggers on GCS\n- Pub/Sub and dead-letter topics\n- Dataflow streaming and BigQuery writes\n- insertId-driven idempotency and MERGE\n- Schema evolution and registry\n- ACLs and least privilege\n\n## Code Example\n```javascript\n// pseudo-code for insertId and MERGE\n```\n\n## Follow-up Questions\n- How would you handle schema drift across tenants?\n- How would you verify idempotency in production?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-16",
        "questionId": "q-2365",
        "question": "Design a multi-tenant data ingestion and analytics pipeline on GCP for three customers (Snap, Nvidia, Zoom). Each tenant streams events to Pub/Sub, which Dataflow consumes and writes to a central BigQuery data lake with per-tenant isolation using CMEK-encrypted Cloud Storage staging and BigQuery Authorized Views/Row-Level Security. Include idempotent processing, audit logging, cost controls, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Pub/Sub, process with Dataflow to a central BigQuery lake, enforce tenant isolation with CMEK-encrypted Cloud Storage staging and per-tenant Authorized Views/row-level security in BigQuery.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Pub/Sub for ingestion, Dataflow (Beam) streaming with a stateful dedup by event_id, writing to analytics.events_YYYYMMDD with insertId to guarantee idempotence. Leverage watermarking and windowing",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a Cloud Function triggered by GCS upload to validate image MIME type and size, then enqueue a Cloud Tasks task that invokes a Cloud Run service. The Cloud Run service calls Google Vision API for label extraction,…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a fault-tolerant, multi-region ingestion pipeline using regional Pub/Sub topics for event ingestion, Dataflow streaming jobs with stateful deduplication by event_id and BigQuery insertId for exactly-once write…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes ability to design scalable, secure, multi-tenant data pipelines on GCP, balancing isolation, cost control, and reliability in real-world enterprises.\n\n## Key Concepts\n- Pub/Sub and Dataflow streaming, exactly-once or dedup strategies\n- BigQuery partitioning, per-tenant isolation with Authorized Views and row-level security\n- CMEK for Cloud Storage staging, IAM least privilege\n- Audit logging, monitoring, cost controls, testing strategies\n\n## Code Example\n```javascript\n// Deduplication helper (conceptual)\nfunction isDuplicate(event, seen) {\n  const key = event.event_id + '|' + event.event_time;\n  if (seen.has(key)) return true;\n  seen.add(key);\n  return false;\n}\n```\n\n## Follow-up Questions\n- How would you validate idempotency in production?\n- What rollback strategy for failed batches might you implement?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-17",
        "questionId": "q-24695",
        "question": "In GCP, design a robust DR workflow for a SaaS app deployed across two regions. The frontend runs on Cloud Run behind a global Load Balancer; primary data is Cloud SQL PostgreSQL in us-central1 with a synchronous replica in europe-west1 for EU reads. Define automated failover promotion, data-consistency guarantees, DNS failover strategy, IAM minimal bindings, and a test plan simulating a regional outage. Include observable metrics and alerting?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design cross-region DR: global Load Balancer routes to Cloud Run in two regions; Cloud SQL primary in us-central1 with a synchronous replica in europe-west1. Automate promotion via a controlled runboo",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Pub/Sub with a per-event id and schema validation; implement a Dataflow streaming job (ReadFromPubSub) that validates, deduplicates via state keyed by player_id and event_id, applies 5-minute Fixe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Trigger a Cloud Function on new files in gs://clicks/input/YYYY/MM/DD/. The function validates each JSONL line (fields userId, ts, event, payload), deduplicates by insertId=userId|ts|event, and writes",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Dataflow streaming pipeline from Pub/Sub to BigQuery with insertId = user_id|event_time|session_id to guarantee idempotency. Handle late data via fixed windows and allowed lateness, support s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world DR planning across compute, database, and DNS layers with automation.\n\n## Key Concepts\n- Cross-region failover orchestration\n- Cloud SQL read replica promotion\n- DNS-based failover and TTL tuning\n- IAM least privilege and auditability\n- Observability and alerting across services\n\n## Code Example\n```yaml\n# DR config sketch\nprimary_region: us-central1\nreplica_region: europe-west1\npromote_on_fail: true\n```\n\n## Follow-up Questions\n- How would you test idempotency during failover?\n- What metrics indicate a healthy DR transition?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-18",
        "questionId": "q-16424",
        "question": "Design a production-grade, multi-tenant log ingestion pipeline on GCP for a SaaS product. Incoming per-tenant logs arrive as JSON in Pub/Sub (topic per tenant); implement a Dataflow streaming job that deduplicates by log_id, enforces exactly-once semantics, and writes to per-tenant, partitioned BigQuery tables. Store raw logs in CMEK-protected GCS. Include IAM least privilege, schema evolution strategy, DLQ/replay safety, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a multi-tenant SaaS logs pipeline with Pub/Sub topics per tenant, a Dataflow streaming job that deduplicates by log_id, writes to per-tenant partitioned BigQuery tables, and stores raw JSON",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest Pub/Sub events from activity-events, validate JSON schema, deduplicate by event_id within a 10-minute lateness window, enrich with user_profiles in BigQuery, compute per-user 1-hour rolling act",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Dataflow streaming job: read Pub/Sub, ParDo redact PII fields, validate schema; use a stateful DoFn for dedup on event_id within a 5-minute window; write to BigQuery with insertId=event_id, part",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt Pub/Sub with event_id as the dedupe key; implement a Dataflow (Beam) streaming job using a stateful DoFn to deduplicate, applying 5-minute fixed windows; write to analytics.events_raw with inser",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess end-to-end data contracts, idempotency, security, and observability for multi-tenant workloads.\n\n## Key Concepts\n- Streaming ETL, exactly-once semantics, per-tenant data isolation, CMEK, DLQ, schema evolution\n\n## Code Example\n```python\n# Pseudo-DoFn for dedup on log_id (conceptual)\nclass DedupDoFn(DoFn):\n  def process(self, element, watermark=None):\n    log_id = element[\"log_id\"]\n    if not self._seen(log_id):\n      self._mark_seen(log_id)\n      yield element\n```\n\n## Follow-up Questions\n- How would you implement schema evolution for changing log_id formats across tenants?\n- How would you implement replay-safe reprocessing and alerting for data loss?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-19",
        "questionId": "q-1413",
        "question": "Design a real-time image ingestion pipeline on GCP for a multi-tenant SaaS app. Customers upload images to per-tenant Cloud Storage buckets with CMEK; a Pub/Sub topic notifies on new uploads; a Dataflow streaming job processes images to extract features and writes results to BigQuery, while archived copies are moved to per-tenant archive buckets. Outline architecture, IAM bindings, service account isolation, exactly-once guarantees (e.g., row_id dedup), and cost/latency trade-offs. Include monitoring and failure-response plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Event-driven audio transcription: uploads to gs://audio-uploads; Cloud Function validates format/metadata, publishes Pub/Sub message; Cloud Run transcribes and writes to gs://transcripts/{region}/{nam",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe a Dataflow streaming pipeline that consumes Pub/Sub telemetry, deduplicates by message_id, applies 1‑minute fixed windows, computes per-device min/max/avg, enriches with device metadata from",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest via multi-region Pub/Sub; Dataflow streaming dedups on event_id (stateful DoFn) and enriches with Firestore profile; sink to partitioned BigQuery using insertId(event_id) for idempotency; emit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe end-to-end architecture for a multi-tenant real-time image pipeline on GCP with per-tenant CMEK buckets, Pub/Sub notifications, Dataflow streaming processing, BigQuery storage, and per-tenant",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-tenant data isolation, customer-managed encryption keys, streaming dataflow, and BigQuery deduplication. Also covers IAM scoping, service account separation, and operational resilience.\n\n## Key Concepts\n- Tenant isolation with CMEK and per-tenant storage\n- Pub/Sub as the ingestion trigger\n- Dataflow streaming pipeline and BigQuery write with deduplication via row_id\n- Archival strategy and cost-aware design\n- Monitoring, alerting, and failure handling\n\n## Code Example\n```javascript\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nclass ParseEvent(beam.DoFn):\n  def process(self, element):\n    import json\n    msg = json.loads(element.decode('utf-8'))\n    yield {\n      'tenant_id': msg['tenant_id'],\n      'image_id': msg['image_id'],\n      'row_id': f\"{msg['image_id']}_{msg['timestamp']}\",\n      'embeddings': msg.get('embeddings', None)\n    }\n\n# skeleton; actual IOs would be configured with real project details\nwith beam.Pipeline(options=PipelineOptions()) as p:\n  (p\n   | beam.io.ReadFromPubSub(topic='projects/PROJECT/topics/images')\n   | beam.ParDo(ParseEvent())\n   | beam.io.WriteToBigQuery(\n       table='PROJECT:DATASET.TABLE',\n       schema='SCHEMA_AUTODETECT',\n       write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND,\n       create_disposition=beam.io.BigQueryDisposition.CREATE_IF_NEEDED\n     )\n  )\n```\n\n## Follow-up Questions\n- How would you implement row-level security for tenants in BigQuery?\n- What would you monitor to detect data skew or latency regressions?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-20",
        "questionId": "q-29990",
        "question": "Design a beginner-friendly GCP workflow to ingest partner telemetry via a webhook into Cloud Storage, validate and redact PII, then generate a nightly error-rate summary in BigQuery. Use: HTTPS Cloud Run endpoint -> Pub/Sub -> Cloud Function (validate, redacts PII, writes to gs://telemetry/enriched/YYYY/MM/DD/NAME.jsonl with insertId = messageId+date) -> nightly BigQuery load to compute error-rate per service. Minimal IAM, idempotency, and a test plan with sample payloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Cloud Build to clone the repo, build and push to Artifact Registry, then deploy to Cloud Run with traffic split 50/50 for 15 minutes, then 100%. Schedule via Cloud Scheduler at 02:00 UTC. Least-pr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A streaming Dataflow job consumes events from Pub/Sub, enriched with a tenantId, and applies 5-minute fixed windows with watermark triggering for late-data handling. The pipeline uses BigQueryIO with insertId deduplicat…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set up a Cloud Run HTTPS endpoint to receive partner telemetry and publish to Pub/Sub. A Cloud Function subscribes, validates JSON lines, redacts PII, and writes enriched records to gs://telemetry/enr",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Parse a Pub/Sub message in a Cloud Function (Node.js): require deviceId, ts, and reading. If valid, insert into BigQuery iot.telemetry with insertId = deviceId + '_' + ts; if invalid, publish to iot-telemetry-dlq.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end data ingestion, validation, redaction, and end-of-day analytics using beginner-friendly GCP primitives, plus a focus on security and idempotency.\n\n## Key Concepts\n\n- Cloud Run HTTPS endpoints\n- Pub/Sub integration\n- Cloud Functions for validation and redaction\n- Cloud Storage with date-based prefixes\n- BigQuery nightly ETL\n- Idempotency via insertId\n- Minimal IAM bindings and testability\n\n## Code Example\n\n```javascript\n// Pseudo Cloud Function outline\nexports.validateAndRedact = (event, context) => {\n  const lines = readLinesFromGCS(event);\n  lines.forEach(l => {\n    if (validate(l)) {\n      const redacted = redactPII(l);\n      writeToGCS(redacted);\n    }\n  });\n}\n```\n\n## Follow-up Questions\n\n- How would you test idempotency for duplicate Pub/Sub messages?\n- How would you scale the nightly BigQuery load for larger data volumes while keeping the solution beginner-friendly?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-21",
        "questionId": "q-23665",
        "question": "Design a beginner-friendly GCP data routing pipeline: when a JSONL file lands in gs://tenant-logs/raw/{tenantId}/{date}.jsonl, a Cloud Function validates each line against a minimal schema (tenantId: string, eventTime: ISO8601, level: string, message: string). Route valid lines to gs://tenant-logs/valid/{tenantId}/{date}/ and invalid lines to gs://tenant-logs/invalid/{tenantId}/{date}/. Publish a per-tenant daily summary to BigQuery with insertId = tenantId+date+filename. Include minimal IAM bindings and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "When a new object lands in gs://raw-telemetry, a Cloud Function reads it line by line, validates each JSON object against a schema, redacts PII (emails, phone numbers) with regex, writes sanitized lin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a fault-tolerant, multi-region ingestion pipeline using regional Pub/Sub topics for event ingestion, Dataflow streaming jobs with stateful deduplication by event_id and BigQuery insertId for exactly-once write…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Trigger a Cloud Function on finalization of gs://tenant-logs/raw/{tenantId}/{date}.jsonl, read the file line by line, validate a minimal schema (tenantId: string, eventTime: ISO8601, level: string, me",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create a Pub/Sub topic fleet.telemetry; a Dataflow streaming job (Apache Beam) parses JSON lines, deduplicates by message_id, detects anomalies (speed > 120, abnormal geo drift), and writes to BigQuer",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of event-driven data routing, per-tenant isolation, and idempotent processing in a beginner-friendly GCP setup.\n\n## Key Concepts\n- Cloud Storage finalization events\n- Cloud Functions line-by-line processing\n- JSON schema validation\n- GCS path routing by tenant/date\n- BigQuery insert with insertId for idempotence\n- Least-privilege IAM\n\n## Code Example\n```javascript\n// Pseudo: on finalization, read lines, validate, route to valid/invalid buckets, emit BigQuery summary\n```\n\n## Follow-up Questions\n- How would you test idempotency and avoid duplicate summaries?\n- How would you scale to many tenants with parallel processing?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-22",
        "questionId": "q-4553",
        "question": "Design a real-time anomaly detection pipeline for user login events across multiple regions on GCP. Ingest via Pub/Sub, run a streaming Dataflow (Beam) pipeline computing a rolling per-user risk score using features like IP, device, region, and time of day. Persist features and scores to BigQuery, publish anomalies to a separate Pub/Sub topic, and trigger a Cloud Run remediation service when a threshold is crossed. Consider idempotency, privacy, tracing, and a practical test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Dataflow (Beam) streaming job that subscribes to Pub/Sub transactions.events, validates JSON schema, deduplicates by transaction_id, and uses 1-minute fixed windows by user_id to compute per-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement with Pub/Sub to Dataflow (Python Beam). Deduplicate by event_id, window 30s per user, compute per-user fail_rate (fails/attempts) in window, emit alert if fail_rate > 0.3; write enriched eve",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A Storage-triggered Cloud Function reads each JSONL line from gs://pii-raw/logs/, redacts email, phone_number, and ip_address, and writes redacted lines to gs://pii-redacted/logs/YYYY/MM/DD/original.j",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest login events via Pub/Sub, execute a Dataflow (Apache Beam) streaming pipeline with 5-minute fixed windows to compute per-user risk scores from features including IP address, device fingerprint, geographic region,…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your ability to design real-time data pipelines that operate across multiple geographic regions while addressing production concerns including latency requirements, fault tolerance, and enterprise-grade operational practices.\n\n## Key Concepts\n- **Pub/Sub**: Decoupled ingestion mechanism for login events and downstream alerting\n- **Dataflow/Apache Beam**: Streaming processing with windowed aggregations and per-key state management\n- **BigQuery**: Feature storage with idempotent write patterns using MERGE operations\n- **Cloud Run Integration**: Event-driven remediation via Pub/Sub triggers\n- **Observability**: End-to-end tracing implementation using Cloud Trace/OpenTelemetry\n- **Data Privacy**: PII masking and anonymization techniques\n- **Testing Strategy**: Synthetic traffic generation, historical data replay, backfill validation, and cost optimization analysis\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nfrom apache_beam.transforms.window import FixedWindows\nimport json\n\nclass ComputeRiskScore(beam.DoFn):\n    def process(self, element, window=beam.DoFn.WindowParam):\n        # Risk scoring logic using IP, device, region, time features\n        risk_score = self._calculate_risk(element)\n        yield {\n            'user_id': element['user_id'],\n            'risk_score': risk_score,\n            'window_end': window.end.to_utc_datetime()\n        }\n\ndef run_pipeline():\n    options = PipelineOptions(streaming=True)\n    \n    with beam.Pipeline(options=options) as p:\n        (p\n         | 'ReadFromPubSub' >> beam.io.ReadFromPubSub(\n             subscription='projects/project/subscriptions/login-events')\n         | 'ParseJSON' >> beam.Map(lambda x: json.loads(x.decode('utf-8')))\n         | 'Window' >> beam.WindowInto(FixedWindows(300))  # 5-minute windows\n         | 'ComputeRisk' >> beam.ParDo(ComputeRiskScore())\n         | 'WriteToBigQuery' >> beam.io.WriteToBigQuery(\n             'project:dataset.user_login_features',\n             schema='user_id:STRING, risk_score:FLOAT64, window_end:TIMESTAMP',\n             write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND,\n             create_disposition=beam.io.BigQueryDisposition.CREATE_IF_NEEDED)\n         | 'FilterAnomalies' >> beam.Filter(\n             lambda x: x['risk_score'] > ANOMALY_THRESHOLD)\n         | 'PublishAlerts' >> beam.io.WriteToPubSub(\n             'projects/project/topics/login-anomalies'))\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-23",
        "questionId": "q-19268",
        "question": "Design a beginner-focused GCP pipeline for IoT telemetry: devices publish JSON to Pub/Sub topic **iot-telemetry**. Create a **Cloud Function** triggered by Pub/Sub that validates required fields (**deviceId**, **ts**, **reading**). If valid, insert into **BigQuery** **iot.telemetry** with insertId = deviceId + '_' + ts; if invalid, publish to **iot-telemetry-dlq**. Include minimal **IAM** bindings, idempotency, and a basic test plan with valid, missing-field, and duplicate messages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a real-time fraud-detection pipeline on GCP that ingests transactions via Pub/Sub, processes them through Dataflow streaming with a 5-minute sliding window to compute per-user velocity metrics and anomaly scores,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a real-time GCP pipeline with these components: Pub/Sub receives device events with a schema for automatic validation. Dataflow streaming job reads from the subscription, validates incoming JSON, removes duplicate…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Devices publish JSON lines to Pub/Sub. A Cloud Function validates the JSON schema, drops invalid messages, and writes valid rows to a partitioned BigQuery table using an insertId derived from messageI",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Parse a Pub/Sub message in a Cloud Function (Node.js): require deviceId, ts, and reading. If valid, insert into BigQuery iot.telemetry with insertId = deviceId + '_' + ts; if invalid, publish to iot-telemetry-dlq.",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests understanding of event-driven processing, basic schema validation, and downstream error handling. It also probes familiarity with idempotent streaming inserts and minimal IAM considerations in GCP.\n\n## Key Concepts\n\n- **Pub/Sub topics and subscriptions**: Understanding message publishing and subscribing patterns\n- **Cloud Functions triggered by Pub/Sub**: Serverless event-driven architecture\n- **BigQuery streaming inserts and insertId for deduplication**: Ensuring exactly-once semantics\n- **Dead-letter topics for remediation**: Handling failed messages gracefully\n- **IAM least privilege**: Following security best practices\n\n## Code Example\n\n```javascript\nconst { BigQuery } = require('@google-cloud/bigquery');\nconst { PubSub } = require('@google-cloud/pubsub');\n\nconst bigquery = new BigQuery();\nconst pubsub = new PubSub();\n\nconst REQUIRED_FIELDS = ['deviceId', 'ts', 'reading'];\nconst DATASET = 'iot';\nconst TABLE = 'telemetry';\nconst DLQ_TOPIC = 'iot-telemetry-dlq';\n\nexports.handleTelemetry = async (message, context) => {\n  const data = JSON.parse(Buffer.from(message.data, 'base64').toString());\n  \n  const missingFields = REQUIRED_FIELDS.filter(field => !data[field]);\n  \n  if (missingFields.length > 0) {\n    await pubsub.topic(DLQ_TOPIC).publishMessage({\n      data: Buffer.from(JSON.stringify({ ...data, error: `Missing fields: ${missingFields.join(', ')}` })).toString('base64')\n    });\n    return;\n  }\n  \n  const insertId = `${data.deviceId}_${data.ts}`;\n  \n  await bigquery.dataset(DATASET).table(TABLE).insert([data], { insertId });\n};\n```\n\n## IAM Bindings\n\n```yaml\nbindings:\n- role: roles/bigquery.dataEditor\n  members:\n  - serviceAccount: YOUR_FUNCTION_SA@project.iam.gserviceaccount.com\n- role: roles/pubsub.publisher\n  members:\n  - serviceAccount: YOUR_FUNCTION_SA@project.iam.gserviceaccount.com\n```\n\n## Test Plan\n\n| Test Case | Input | Expected Result |\n|-----------|-------|----------------|\n| Valid message | `{\"deviceId\": \"sensor_01\", \"ts\": \"1699900000\", \"reading\": 23.5}` | Inserted into BigQuery |\n| Missing deviceId | `{\"ts\": \"1699900000\", \"reading\": 23.5}` | Published to DLQ |\n| Missing ts | `{\"deviceId\": \"sensor_01\", \"reading\": 23.5}` | Published to DLQ |\n| Missing reading | `{\"deviceId\": \"sensor_01\", \"ts\": \"1699900000\"}` | Published to DLQ |\n| Duplicate message | Same valid message sent twice | Only one record inserted (deduplicated via insertId) |\n\n## Idempotency\n\nThe `insertId = deviceId + '_' + ts` ensures that duplicate messages within 24 hours are deduplicated by BigQuery, providing exactly-once semantics for the insert operation.",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-24",
        "questionId": "q-4913",
        "question": "Design a beginner-friendly event ingestion pipeline on GCP for mobile game events arriving as JSONL in gs://mobile-events/raw. On object_finalize, a Cloud Function should validate a simple schema (user_id:string, event:string, ts:int). Valid lines go to BigQuery analytics.events with insertId = user_id+\"_\"+ts+\"_\"+line_num and a date partition by ts. Invalid lines go to gs://mobile-events/dead with metadata. Include minimal IAM bindings, retries, and a test plan with good/bad samples?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe a Cloud Function triggered on finalize of a CSV in logs-origin that validates headers, filters invalid rows, and deduplicates by a per-row key. It should insert into a date-partitioned BigQue",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Cloud Function triggered by storage.object.finalize on gs://app-logs/input. Parse the CSV, validate headers, skip invalid lines, normalize to userId, ts, event, and write cleaned lines to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage a Dataflow streaming pipeline with Pub/Sub at the input, normalized schema, and deduplication via insertId on the BigQuery sink. Use a lightweight de-dup store (BigTable) for cross-region dup",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Cloud Function triggered on Finalize for gs://mobile-events/raw. It reads JSONL lines, validates a schema: user_id (string), event (string), ts (int). Writes valid lines to BigQuery analytics",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical design of event ingestion with schema validation, idempotent writes, and dead-letter handling.\n\n## Key Concepts\n- Cloud Functions, Cloud Storage triggers\n- JSONL parsing and per-line validation\n- BigQuery insert with insertId, date partitioning\n- Dead-letter handling and log metadata\n- IAM least privilege and retries\n\n## Code Example\n```python\ndef handler(event, context):\n    bucket = event['bucket']\n    name = event['name']\n    if not name.endswith('.jsonl'):\n        return\n    # pseudo-reading logic\n    for line in read_lines(bucket, name):\n        data = json.loads(line)\n        if is_valid(data):\n            write_to_bq(data)\n        else:\n            write_dead_letter(data)\n```\n\n## Follow-up Questions\n- How would you test idempotency when a function retries after partial success?\n- How would you monitor failures and alert on dead-letter growth?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-30T13:58:04.831Z",
    "version": 1
  }
]