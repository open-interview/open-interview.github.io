[
  {
    "id": "test-gcp-cloud-engineer",
    "channelId": "gcp-cloud-engineer",
    "channelName": "GCP Cloud Engineer",
    "title": "GCP Cloud Engineer Knowledge Test",
    "description": "Test your understanding of GCP Cloud Engineer with 25 questions.",
    "questions": [
      {
        "id": "gcp-cloud-engineer-q-0",
        "questionId": "q-2061",
        "question": "Design an end-to-end cross-region streaming data pipeline on GCP to ingest high-volume telemetry from Pub/Sub into BigQuery with real-time dashboards. Use Dataflow (Beam) for streaming, enable exactly-once processing, implement automatic primary/DR region failover, use multi-region Pub/Sub topics and cross-region BigQuery datasets, handle schema evolution, and provide monitoring and rollback strategies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a two-region streaming pipeline with primary deployment in us-central1 and disaster recovery in europe-west1. Utilize Pub/Sub multi-region topics for cross-region message distribution, implement Dataflow jobs wit…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Eventarc to route Storage finalize events from gs://customer-videos/originals to a Cloud Run service that runs ffmpeg in a container to create 720p and 480p MP4s in gs://customer-videos/exports wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Cloud Function that triggers when new JSON files land in gs://tagging-requests/ingest, validates each record for required fields (userId, photoId, tags: string[]), publishes valid events to Pub/Sub topic tag…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest payment events through a Pub/Sub subscription with exactly-once delivery enabled; implement a Dataflow streaming pipeline with sliding 5-minute windows keyed by customer_id using Beam's SlidingWindows; compute pe…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses real-world cross-region streaming architecture design, exactly-once processing semantics, disaster recovery failover mechanisms, and operational governance practices.\n\n## Key Concepts\n- Cross-region Pub/Sub topics and BigQuery datasets for geographic redundancy\n- Apache Beam Dataflow exactly-once processing guarantees\n- Schema evolution governance and backward compatibility\n- Comprehensive observability and automated rollback strategies\n\n## Code Example\n\n```python\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\nclass Deduplicate(beam.DoFn):\n    def __init__(self):\n        self.seen = set()\n    \n    def process(self, event):\n        if event['event_id'] in self.seen:\n            return\n        self.seen.add(event['event_id'])\n        yield event\n\noptions = PipelineOptions(\n    streaming=True,\n    project='your-project',\n    region='us-central1',\n    job_name='cross-region-streaming'\n)\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-1",
        "questionId": "q-19972",
        "question": "Design a streaming Dataflow pipeline that ingests Pub/Sub JSON events for a fintech fraud-detection system. Enforce exactly-once semantics by deduplicating on insertId, write to BigQuery (partitioned by date) and to an Iceberg table on GCS for the data lake. Include cross-region replication, CMEK, strict IAM, observability, and a rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Publish telemetry events to a Pub/Sub topic, configure a push subscription to deliver messages to a Cloud Run HTTP endpoint, validate the JSON schema, and perform streaming inserts into BigQuery's telemetry.events table…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Streaming Dataflow pipeline ingests Pub/Sub JSON events; deduplicate by insertId to ensure exactly-once, write to BigQuery (partitioned by date) and to Iceberg on GCS for the lake. Enable cross-region",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a schema-aware ingest using a central Avro schema registry in Cloud Storage, a Pub/Sub topic for raw events, and a Dataflow/Beam pipeline that validates each event against the registry. If v",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Dataflow (Beam) streaming job that subscribes to Pub/Sub transactions.events, validates JSON schema, deduplicates by transaction_id, and uses 1-minute fixed windows by user_id to compute per-",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes real-world data engineering at scale: exact-once semantics, multi-sink writes, and governance across regions in GCP.\n\n## Key Concepts\n\n- Exactly-once ingestion and idempotent sinks\n- Beam/Dataflow vs Spark streaming choices\n- BigQuery: streaming inserts vs batch loads, partitioning\n- Iceberg on GCS for data lake\n- Cross-region replication, CMEK, IAM least privilege\n- Schema evolution, observability and rollback\n\n## Code Example\n\n```javascript\n// Pseudo Dataflow-like pipeline sketch\nconst pipeline = createPipeline({/* options */});\npipeline\n  .readPubSub('projects/my/proj/subs/events', {format: 'json'})\n  .map(e => JSON.parse(e))\n  .distinctBy(e => e.insertId)\n  .tee([\n     batchToBigQuery({table:'fraud.analytics', partitionBy:'date'}),\n     writeIcebergOnGCS({path:'gs://lake/fraud/', table:'fraud iceberg'})\n  ]);\n```\n\n## Follow-up Questions\n\n- How would you test idempotency end-to-end under burst load?\n- What approach would you use if Pub/Sub ordering becomes a challenge across regions?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-2",
        "questionId": "q-9578",
        "question": "Context: Global SaaS with multi-region ingestion. Ingest user activity from Pub/Sub in us-central1, process with Dataflow (Beam) to emit: (1) real-time metrics into BigQuery using insertId-based dedup, (2) an anomaly feed to Pub/Sub for flagged events. Ensure exactly-once semantics, event-id hashing, cross-region replay protection, backpressure handling, and a dead-letter path. Provide data model, failure modes, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger a Cloud Function on GCS object finalize events. Parse each JSON line, validate its schema against expected fields, and route the data to the appropriate regional bucket: gs://telemetry-us/YYYY/MM/DD for US data…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Publish telemetry to a cross-region Pub/Sub, process with Dataflow streaming (Beam) enabling exactly-once semantics, write to a centralized BigQuery dataset (insertId=event_id) and mirror to gs://tele",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed solution: Use Pub/Sub to buffer, Cloud Function for schema validation and publishing to telemetry-validated, Dataflow streaming to run windowed aggregates and call Vertex AI for anomaly score",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a streaming Dataflow pipeline that ingests Pub/Sub events, deduplicates by a per-event id, writes metrics to BigQuery with insertId to prevent duplicates, and publishes anomalies to a separate",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time ingestion, cross-region reliability, and robust deduplication in a serverless/streaming pipeline.\n\n## Key Concepts\n- Apache Beam on Dataflow streaming\n- exactly-once semantics with BigQuery insertId\n- deduplication using event_id hashing\n- multi-region resilience and replay protection\n- dead-letter queues and backpressure tuning\n\n## Code Example\n```javascript\n// Pseudo-DoFn showing dedup by event_id with windowing\nclass DedupDoFn extends DoFn {\n  process(context, {event_id}) {\n    if (state.seen(event_id)) return;\n    state.upsert(event_id, true);\n    // emit to metrics or anomaly topics based on event content\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test idempotency across regional failover?\n- What metrics would you monitor to detect backpressure and replay risks?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-3",
        "questionId": "q-25593",
        "question": "Design an advanced GCP pipeline to ingest multi-tenant telemetry from Pub/Sub, normalize heterogeneous schemas in Dataflow, redact PII using a per-tenant policy stored in Cloud Storage, and write to a partitioned BigQuery table keyed by tenant_id and date. Ensure exactly-once semantics, per-tenant isolation via IAM, and cost monitoring with budgets. Include test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest with Pub/Sub, Dataflow streaming validates against a versioned JSON schema, normalizes fields, and writes to BigQuery; raw events go to GCS with Data Catalog lineage. Vertex AI provides online",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Dataflow streaming job (Apache Beam) to ingest regional Pub/Sub topics, compute per-minute aggregates, and write to a centralized BigQuery table with a deterministic insertId (region|timestamp|m",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Pub/Sub streaming with per-tenant topics, a Dataflow pipeline that normalizes events via a central schema registry, redacts PII with a policy loaded from Cloud Storage and applied to each event, t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Validate schema and idempotency at ingestion: a Cloud Function triggered by finalization of gs://srv-logs/raw/server-logs-*.jsonl parses each line, drops invalid ones to gs://srv-logs/dlq/, and stream",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess scalability, multi-tenant isolation, and production-readiness in GCP.\n\n## Key Concepts\n- Streaming data pipelines, Pub/Sub, Dataflow\n- Schema normalization, policy-driven PII redaction\n- BigQuery partitioning, insertId for idempotency\n- IAM, VPC Service Controls, cost controls, testing\n\n## Code Example\n```javascript\n// Placeholder: outline of Dataflow DoFn pseudocode\n```\n\n## Follow-up Questions\n- How would you handle schema evolution without breaking older tenants?\n- How would you test redaction correctness at scale?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-4",
        "questionId": "q-8297",
        "question": "Design a beginner-friendly GCP data ingestion workflow: when a CSV lands in gs://reports/customer-csv/raw, a Cloud Function validates headers (customer_id, amount, date) and numeric amount, redacts email/phone fields, and writes sanitized rows to BigQuery reports.customer_daily with insertId = customer_id|date. Invalid rows go to gs://reports/customer-csv/invalid/YYYY/MM/DD and a log is written. Minimal IAM, exponential backoff?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a main Pub/Sub topic for valid events and a DLQ. A Cloud Function subscribes, validates JSON against a lightweight schema, and if valid writes to BigQuery via streaming insert using insertId",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: Trigger Cloud Function on finalize of CSV in gs://reports/customer-csv/raw. Parse headers (customer_id, amount, date) and ensure amount is numeric. Mask email and phone fields. Writ",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a Dataflow streaming job that reads Pub/Sub, validates against a Beam schema, deduplicates by event_id, and writes enriched records to a daily-partitioned BigQuery table rides.events_YYYYMMDD with",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Publish logs to a cross-region Pub/Sub topic per tenant; Dataflow streaming computes per-tenant anomaly scores in 1-minute windows and writes anomalies to BigQuery using insertId to ensure idempotency",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests Cloud Functions and GCS triggers, CSV parsing, data validation, privacy redaction, and BigQuery loading with idempotent inserts.\n\n## Key Concepts\n- GCS finalization trigger\n- Cloud Functions data processing\n- CSV parsing and schema validation\n- Data redaction of PII\n- BigQuery inserts with insertId for idempotency\n- Error routing to invalid bucket and logging\n- Minimal IAM and retry strategy\n\n## Code Example\n```python\ndef process_csv(event, context):\n    bucket = event['bucket']\n    name = event['name']\n    if not name.endswith('.csv'): return\n    # Read, parse, validate headers, redact, and write to BigQuery or invalid path\n```\n\n## Follow-up Questions\n- How would you scale to large files without memory pressure?\n- How would you verify idempotency with duplicates?\n- How would you monitor failures and set up alerts?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-5",
        "questionId": "q-5117",
        "question": "Design a real-world GCP data ingestion pipeline for a high-traffic mobile analytics stream (1-2M events/sec): events arrive via Pub/Sub; build an end-to-end pipeline that ensures exactly-once delivery, dedup by event_id, and time-based aggregation into BigQuery. Use Dataflow (Beam) for streaming, with a DLQ for failed events, and separate raw and summary tables. Include IAM, testing, and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement with a Cloud Function triggered by finalize events on gs://shared-uploads/. Use file name and generation to form insertId; write a row to BigQuery docs.events; copy to gs://docs-archive/YYYY",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Cloud Function triggered by Pub/Sub messages on the telemetry_raw topic. For each message, validate the JSON schema for required fields (userId, event, ts). Upon validation success, invoke Cloud DLP to redac…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt Pub/Sub with event_id as the dedupe key; implement a Dataflow (Beam) streaming job using a stateful DoFn to deduplicate, applying 5-minute fixed windows; write to analytics.events_raw with inser",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "I would implement an HTTP Cloud Function that reads the canary config from Firestore, hashes user_id with SHA-256, computes bucket = hash % 100, and returns enabled when bucket < percent; otherwise di",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design end-to-end streaming pipelines in GCP at scale with correctness guarantees, operational resilience, and proper IAM controls.\n\n## Key Concepts\n\n- Pub/Sub delivery semantics and DLQ\n- Apache Beam stateful processing and windowing\n- BigQuery streaming inserts and insertId for idempotency\n- Dataflow autoscaling, backpressure, and cost awareness\n- IAM least privilege and monitoring\n\n## Code Example\n\n```python\n# Pseudo Beam DoFn for dedup by event_id\nclass DedupDoFn(DoFn):\n  def process(self, element, timestamp=DoFn.TimestampParam, state=DoFn.StateParam):\n    event_id = element['event_id']\n    if not state.read(event_id):\n      state.write(event_id, True)\n      yield element\n```\n\n## Follow-up Questions\n\n- How would you validate exactly-once during chaos testing?\n- How would you evolve schema without breaking readers?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-6",
        "questionId": "q-26675",
        "question": "Design a beginner-friendly GCP workflow to enforce resource labeling across a project. A nightly audit uses a scalable inventory to list resources (Compute Engine, Cloud Storage, Pub/Sub, etc.). If a resource is missing one of the required labels (e.g., env, cost_center), apply a default label set using service-specific APIs, and log remediation events to BigQuery with insertId = resourceId+date. Include minimal IAM bindings and a smoke test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger on finalization of raw video; compute a deterministic id from filename and content hash to enable idempotency. Create a single Transcoder job with 3 outputs (1080p, 720p, 480p) to gs://media-u",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub regional topics for ingestion, Dataflow Streaming with stateful deduplication on (tenantId, eventId), apply DLP masking for PII protection, then write raw events to gs://raw-events/{tenant}/{region}/{date}/…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest per-tenant events from Pub/Sub into a Dataflow streaming job, apply a 1-minute fixed window by tenant_id, compute per-tenant counts and revenue, and enrich with metadata from BigQuery. Sink to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Cloud Asset Inventory for nightly enumeration of all project resources—including Compute Engine, Cloud Storage, and Pub/Sub. For each resource missing required labels (env, cost_center), invoke the service-specific…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical policy enforcement using foundational GCP services. Tests understanding of automation, idempotency, and auditability at scale.\n\n## Key Concepts\n- **Cloud Asset Inventory**: Provides scalable, comprehensive resource discovery across the project\n- **Service-specific labeling APIs**: Compute Engine, Cloud Storage, and Pub/Sub each support idempotent label updates\n- **BigQuery remediation logs**: Deterministic insertId (resourceId+date) enables deduplication and auditability\n- **Least-privilege IAM**: Principle of minimal permissions for automation service accounts\n- **Smoke test plan**: Validates labeling enforcement and remediation pipeline integrity\n\n## Code Example\n```python\nfrom google.cloud import asset_v1, bigquery\nfrom google.cloud.asset_v1.types import Asset\nfrom googleapiclient import discovery\nfrom datetime import date\nimport time\n\nREQUIRED_LABELS = [\"env\", \"cost_center\"]\nDEFAULT_LABELS = {\"env\": \"untagged\", \"cost_center\": \"unknown\"}\n\ndef audit_and_remediate(project_id):\n    client = asset_v1.AssetServiceClient()\n    bq_client = bigquery.Client()\n    \n    content = client.export_assets(\n        request={\"parent\": f\"projects/{project_id}\", \"asset_types\": [\n            \"compute.googleapis.com/Instance\",\n            \"storage.googleapis.com/Bucket\",\n            \"pubsub.googleapis.com/Topic\"\n        ]}\n    )\n    \n    for asset in content.asset_list:\n        resource_name = asset.name.split(\"/\")[-1]\n        labels = getattr(asset.resource, \"labels\", {})\n        missing = [l for l in REQUIRED_LABELS if l not in labels]\n        \n        if missing:\n            merged = {**labels, **{l: DEFAULT_LABELS[l] for l in missing}}\n            apply_label(asset.asset_type, resource_name, merged)\n            log_remediation(bq_client, project_id, resource_name, merged)\n\ndef apply_label(asset_type, resource_name, labels):\n    retry_count = 0\n    while retry_count < 3:\n        try:\n            if \"compute\" in asset_type:\n                compute = discovery.build(\"compute\", \"v1\")\n                compute.instances().setLabels(\n                    project=PROJECT, instance=resource_name,\n                    body={\"labels\": labels, \"labelFingerprint\": \"0\"}\n                ).execute()\n            elif \"storage\" in asset_type:\n                storage = discovery.build(\"storage\", \"v1\")\n                storage.buckets().patch(\n                    bucket=resource_name, body={\"labels\": labels}\n                ).execute()\n            elif \"pubsub\" in asset_type:\n                pubsub = discovery.build(\"pubsub\", \"v1\")\n                pubsub.projects().topics().patch(\n                    name=resource_name, body={\"labels\": labels}\n                ).execute()\n            return\n        except Exception as e:\n            retry_count += 1\n            time.sleep(2 ** retry_count)\n\ndef log_remediation(bq_client, project_id, resource_id, labels):\n    table = bq_client.dataset(\"remediation_logs\").table(\"label_events\")\n    insert_id = f\"{resource_id}_{date.today().isoformat()}\"\n    bq_client.insert_rows_json(table, [{\n        \"insertId\": insert_id,\n        \"projectId\": project_id,\n        \"resourceId\": resource_id,\n        \"labelsApplied\": labels,\n        \"timestamp\": bigquery舍 Client().timestamp()\n    }])\n```\n\n## Smoke Test Plan\n1. Deploy to staging project and run nightly job manually\n2. Verify unlabeled test VM receives default labels via Compute API\n3. Confirm BigQuery row inserted with correct insertId\n4. Re-run on labeled resource—verify no duplicate rows (deduplication works)\n5. Test IAM: verify service account lacks delete permissions\n6. Simulate API failure—confirm retry logic executes",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-7",
        "questionId": "q-27685",
        "question": "Design a beginner-friendly GCP workflow to support tenant-scoped upserts from CSV uploads: partner places gs://tenant-uploads/YYYY/MM/DD/file.csv with columns tenant_id, record_id, value. A Cloud Function validates schema, computes an insertId per row, and streams to a staging BigQuery table. A Cloud Scheduler triggers a MERGE into the production table on (tenant_id, record_id). Include minimal IAM bindings, idempotency, and a basic smoke test?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a nightly drift-detection pipeline: Cloud Scheduler triggers a Function that fetches current IAM bindings for all tenants (via Cloud Asset Inventory/Resource Manager) and compares them to a bas",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Eventarc to route Storage finalize events from gs://customer-videos/originals to a Cloud Run service that runs ffmpeg in a container to create 720p and 480p MP4s in gs://customer-videos/exports wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route region-specific events to gs://region-logs/{region}/, where a Cloud Function validates payloads, redacts PII using a library like google-data-masking, and publishes sanitized records to a region-scoped Pub/Sub top…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Cloud Function triggered by gs://tenant-uploads finalize to validate schema and normalize data. For each row compute insertId = tenant_id|record_id|date and stream to a staging BigQuery table. A",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests end-to-end data upserts, idempotency, and using Cloud Scheduler to control merges, which isn't covered by the existing. It emphasizes minimal IAM and testing.\n\n## Key Concepts\n- Cloud Functions with Cloud Storage triggers\n- BigQuery streaming inserts and insertId\n- BigQuery MERGE for upserts\n- Cloud Scheduler and idempotent processing\n- Least privilege IAM\n\n## Code Example\n```javascript\n// Example Cloud Function skeleton (Node.js)\nexports.processCsv = async (event, context) => {\n  // read row, validate\n  // compute insertId = tenant_id|record_id|date\n  // stream to staging BigQuery with insertId\n  // ...\n}\n```\n\n## Follow-up Questions\n- How would you handle schema drift and validation failures at scale?\n- What monitoring would you add to detect stalled merges or repeated failures?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-8",
        "questionId": "q-1413",
        "question": "Design a real-time image ingestion pipeline on GCP for a multi-tenant SaaS app. Customers upload images to per-tenant Cloud Storage buckets with CMEK; a Pub/Sub topic notifies on new uploads; a Dataflow streaming job processes images to extract features and writes results to BigQuery, while archived copies are moved to per-tenant archive buckets. Outline architecture, IAM bindings, service account isolation, exactly-once guarantees (e.g., row_id dedup), and cost/latency trade-offs. Include monitoring and failure-response plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest logs via Pub/Sub with tenant_id; a Dataflow streaming job validates schema, extracts tenant_id, and writes to a partitioned BigQuery table keyed by date with a tenant_id column. Use BigQuery ro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Pub/Sub -> Dataflow streaming with idempotent sink into BigQuery (upsert by device_id+event_time) -> partitioned telemetry.events; raw messages archived to Cloud Storage (lifecycle rules) with CMEK. U",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe end-to-end architecture for a multi-tenant real-time image pipeline on GCP with per-tenant CMEK buckets, Pub/Sub notifications, Dataflow streaming processing, BigQuery storage, and per-tenant",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest with Pub/Sub, Dataflow streaming validates against a versioned JSON schema, normalizes fields, and writes to BigQuery; raw events go to GCS with Data Catalog lineage. Vertex AI provides online",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-tenant data isolation, customer-managed encryption keys, streaming dataflow, and BigQuery deduplication. Also covers IAM scoping, service account separation, and operational resilience.\n\n## Key Concepts\n- Tenant isolation with CMEK and per-tenant storage\n- Pub/Sub as the ingestion trigger\n- Dataflow streaming pipeline and BigQuery write with deduplication via row_id\n- Archival strategy and cost-aware design\n- Monitoring, alerting, and failure handling\n\n## Code Example\n```javascript\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nclass ParseEvent(beam.DoFn):\n  def process(self, element):\n    import json\n    msg = json.loads(element.decode('utf-8'))\n    yield {\n      'tenant_id': msg['tenant_id'],\n      'image_id': msg['image_id'],\n      'row_id': f\"{msg['image_id']}_{msg['timestamp']}\",\n      'embeddings': msg.get('embeddings', None)\n    }\n\n# skeleton; actual IOs would be configured with real project details\nwith beam.Pipeline(options=PipelineOptions()) as p:\n  (p\n   | beam.io.ReadFromPubSub(topic='projects/PROJECT/topics/images')\n   | beam.ParDo(ParseEvent())\n   | beam.io.WriteToBigQuery(\n       table='PROJECT:DATASET.TABLE',\n       schema='SCHEMA_AUTODETECT',\n       write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND,\n       create_disposition=beam.io.BigQueryDisposition.CREATE_IF_NEEDED\n     )\n  )\n```\n\n## Follow-up Questions\n- How would you implement row-level security for tenants in BigQuery?\n- What would you monitor to detect data skew or latency regressions?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-9",
        "questionId": "q-19988",
        "question": "Design a beginner-friendly GCP workflow to perform daily schema drift checks between a new CSV landing in gs://customer-exports/daily/ and a canonical schema defined in BigQuery. Use a Cloud Function triggered by Cloud Storage events to read the CSV header, fetch the BigQuery table schema for customers_raw, and emit a drift summary to BigQuery drift.summary with insertId = filePath+date. Include minimal IAM bindings, idempotent writes, a basic test plan with sample files, and a simple retry strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Pub/Sub, Cloud Function validates schema and redacts PII, publish to validated topic. Dataflow streaming reads validated data, partitions by region, writes to BigQuery (insertId = event_id)",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Trigger: Cloud Function on finalization of gs://reports/raw/*.csv. Validate header matches expected columns, e.g., id,timestamp,value. Convert to Parquet with PyArrow, store at gs://reports/processed/",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a beginner-friendly GCP workflow for daily schema drift detection. When a new CSV lands in gs://customer-exports/daily/, a Cloud Function triggered by Cloud Storage (finalize) events will: 1) Read the CSV header…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design an advanced GCP multi-tenant telemetry ingest: tenants publish to per-tenant Pub/Sub topics; a Dataflow streaming job shards by tenant_id, redacts PII, and de-duplicates on event_id; writes to",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical Cloud Storage event trigger usage and schema drift detection with BigQuery. It assesses the ability to design a deterministic, observable, and fault-tolerant data quality gate with minimal permissions.\n\n## Key Concepts\n- Cloud Storage event triggers for automated pipeline execution\n- BigQuery schema retrieval and comparison logic\n- Drift reporting with idempotent upserts using insertId\n- Minimal IAM bindings with least-privilege RBAC\n- Exponential backoff retry strategies for resilience\n- Test planning for data pipelines\n\n## Code Example\n```python\nfrom google.cloud import bigquery, storage\nimport functions_framework\nimport hashlib\nfrom datetime import datetime\n\nbq_client = bigquery.Client()\nstorage_client = storage.Client()\n\n@functions_framework.cloud_event\ndef check_schema_drift(cloud_event):\n    data = cloud_event.data\n    bucket_name = data['bucket']\n    file_path = data['name']\n    \n    # Read CSV header from Cloud Storage\n    bucket = storage_client.bucket(bucket_name)\n    blob = bucket.blob(file_path)\n    header = blob.download_as_text().split('\\n')[0]\n    csv_columns = [col.strip() for col in header.split(',')]\n    \n    # Fetch BigQuery schema\n    table = bq_client.get_table('project.dataset.customers_raw')\n    bq_columns = {field.name: field.field_type for field in table.schema}\n    \n    # Detect drift\n    drift = []\n    for col in csv_columns:\n        if col not in bq_columns:\n            drift.append({'column': col, 'issue': 'missing_in_bq'})\n    for col in bq_columns:\n        if col not in csv_columns:\n            drift.append({'column': col, 'issue': 'missing_in_csv'})\n    \n    # Write drift summary (idempotent)\n    date_str = datetime.utcnow().strftime('%Y-%m-%d')\n    insert_id = f\"{file_path}{date_str}\"\n    \n    rows = [{\n        'file_path': file_path,\n        'check_date': date_str,\n        'drift_details': str(drift),\n        'insert_id': insert_id\n    }]\n    \n    errors = bq_client.insert_rows_json('project.dataset.drift.summary', rows)\n    return {'status': 'success' if not errors else 'failed', 'drift': drift}\n```\n\n## IAM Bindings\n```\n# Cloud Function Service Account\nroles/bigquery.dataViewer (project): read customers_raw schema\nroles/bigquery.jobUser (project): write to drift.summary\nroles/storage.objectViewer (bucket): read CSV files\n```\n\n## Test Plan\n1. Upload sample CSV with matching schema → expect no drift\n2. Upload CSV with extra column → expect 'missing_in_bq' drift\n3. Upload CSV missing a column → expect 'missing_in_csv' drift\n4. Re-upload same file → verify idempotent behavior (single row)\n\n## Retry Strategy\n- Use Cloud Function 2nd gen with built-in retry for transient failures\n- Implement exponential backoff (1s, 2s, 4s) for BigQuery rate limits\n- Dead letter queue for persistent failures after 3 attempts",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-10",
        "questionId": "q-23252",
        "question": "Design a beginner-friendly GCP ingestion workflow for daily mobile telemetry uploads. A Cloud Storage bucket gs://app-logs/input receives a daily CSV file. A Cloud Function triggered on new objects validates a simple schema (columns: user_id, timestamp, event), normalizes column names, writes cleaned lines to gs://app-logs/processed/YYYY/MM/DD and inserts a summary row into BigQuery with insertId=filePath+date. Include minimal IAM bindings and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Cloud Function triggered by storage.object.finalize on gs://app-logs/input. Parse the CSV, validate headers, skip invalid lines, normalize to userId, ts, event, and write cleaned lines to",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a streaming Dataflow pipeline that ingests Pub/Sub events, deduplicates by a per-event id, writes metrics to BigQuery with insertId to prevent duplicates, and publishes anomalies to a separate",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Cloud Function validates header, normalizes ts to ISO8601, and publishes enriched JSON to Pub/Sub on CSV Finalize. Cloud Run, triggered by Pub/Sub, writes to partitioned BigQuery analytics.enriched_ev",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate the flag file in gs://flags/new-feature.json with a deterministic version and dedupe id. Publish a rollout message to Pub/Sub; regional Cloud Run services subscribe, apply the flag with per-r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a simple, observable data ingestion with GCP services and idempotent writes.\n\n## Key Concepts\n- Cloud Functions event triggers, Cloud Storage; CSV parsing; BigQuery insert with insertId; IAM least privilege; basic testing.\n\n## Code Example\n```javascript\n// Pseudocode: read csv, validate header, map columns, write to processed, insert summary\n```\n\n## Follow-up Questions\n- How would you add backoff for transient storage failures?\n- How would you extend to support immutability and retry idempotency across retries?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-11",
        "questionId": "q-4541",
        "question": "Design a real-time data masking and routing pipeline on GCP for Pub/Sub telemetry-raw. Build a Dataflow streaming pipeline that reads from Pub/Sub, sources masking rules from Firestore (collection policy.telemetry) and applies dynamic masking to PII fields (e.g., email, phone_number, device_id). Support rule updates without redeploy, write masked events to BigQuery (telemetry.region tables partitioned by event_date) and archive a copy to Cloud Storage. Late data handling, dead-letter Pub/Sub, idempotent writes, and minimal IAM must be considered. Include a concise test plan and policy-update scenario?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Pub/Sub, Cloud Function validates schema and redacts PII, publish to validated topic. Dataflow streaming reads validated data, partitions by region, writes to BigQuery (insertId = event_id)",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest IoT data via Pub/Sub, run a Dataflow streaming job that computes per-device anomaly scores in event-time windows, write anomalies to BigQuery (using insertId for idempotence), and trigger a Sla",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Nightly cleanup: Cloud Scheduler triggers a Cloud Function that scans gs://user-uploads/temp for files older than 7 days, copies each to gs://archive/YYYY/MM/DD/ with the original name, deletes the source file, and logs…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Beam Dataflow streaming job reading telemetry-raw from Pub/Sub, with Firestore as a dynamic side-input for masking rules. Mask fields like email, phone_number, and device_id; refresh rules perio",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time data privacy engineering, dynamic policy management, and multi-sink pipelines on GCP. Evaluates handling of late data, dead-letter routing, and IAM least privilege in a realistic telemetry scenario.\n\n## Key Concepts\n- Apache Beam (Dataflow) streaming with Pub/Sub\n- Firestore as dynamic policy store and live updates\n- Field-level masking for PII (email, phone, device_id)\n- Exactly-once-like semantics with idempotent writes to BigQuery\n- Multi-sink: BigQuery partitioned tables and Cloud Storage archives\n- Dead-letter topics and observability/alerting\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\nclass MaskPII(beam.DoFn):\n    def __init__(self, policy_ref):\n        self.policy_ref = policy_ref\n    def process(self, element, policy):\n        # element is JSON dict; apply masking per policy\n        # mask email, phone_number, device_id accordingly\n        yield masked_element\n\ndef run():\n    options = PipelineOptions(streaming=True, project='my-gcp-project')\n    with beam.Pipeline(options=options) as p:\n        raw = (p\n               | 'ReadPubSub' >> beam.io.ReadFromPubSub(topic='projects/myproj/topics/telemetry-raw')\n               | 'ParseJSON' >> beam.Map(lambda b: json.loads(b))\n               | 'Mask' >> beam.ParDo(MaskPII(policy_ref='policies/telemetry'))\n               | 'WriteBigQuery' >> beam.io.WriteToBigQuery(table='mydataset.telemetry_${region}', write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND)\n               | 'Archive' >> beam.io.WriteToFiles('gs://telemetry-archive')\n        )\n\nif __name__ == '__main__':\n    run()\n```\n\n## Follow-up Questions\n- How would you test dynamic policy updates without redeploying a pipeline?\n- How do you ensure idempotency across restarts with event_id as a key?\n- How would you monitor masking latency and policy-refresh failures?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-12",
        "questionId": "q-15937",
        "question": "Design a beginner-friendly GCP pipeline to ingest multi-language telemetry where each message includes locale. A Cloud Function validates the JSON lines from gs://telemetry-raw, extracts locale, and publishes to locale-scoped Pub/Sub topics (telemetry-raw-<locale>); a Dataflow streaming job subscribes to all locale topics, normalizes fields to a canonical schema, and writes to BigQuery table telemetry.events with insertId = event_id|locale. Include minimal IAM bindings, idempotent writes, and a test plan with sample events in two locales and a malformed line?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a multi-tenant data lake on GCP: raw lands in gs://data-lake/raw/{tenant}, Dataflow enforces schema and writes to curated gs://data-lake/curated/{tenant}/. Each tenant gets a separate BigQuery",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a multi-tenant telemetry pipeline with per-tenant Pub/Sub topics for isolation, a Dataflow streaming job to normalize events, landing raw data in BigQuery and pushing features to Vertex AI Feature Store. Implemen…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement: a Cloud Function to validate the JSON line, ensure required fields (event_id, device_id, timestamp, locale, metrics), redact PII, and publish to a locale-specific Pub/Sub topic like",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Leverage a single, scalable Dataflow (Beam) pipeline reading Pub/Sub subscriptions per region, validate via Avro schema registry, apply dedup with event_id using stateful processing for a 5-minute win",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design multi-locale routing, schema normalization, idempotency, and end-to-end observability in GCP using beginner-friendly components.\n\n## Key Concepts\n- Cloud Functions for validation and routing by locale\n- Pub/Sub as a decoupled ingestion layer\n- Dataflow streaming for cross-locale normalization\n- BigQuery insertId-based idempotency\n- IAM least privilege and test strategy\n\n## Code Example\n```python\n# Cloud Function (pseudo)\nimport json\nfrom google.cloud import pubsub_v1\npublisher = pubsub_v1.PublisherClient()\nTOPIC = 'projects/PROJECT_ID/topics/telemetry-raw-en-US'\n\ndef route(event, context):\n    line = event.data.decode('utf-8')\n    data = json.loads(line)\n    locale = data.get('locale')\n    if not locale or 'event_id' not in data:\n        raise ValueError('Invalid message')\n    topic = f'projects/PROJECT_ID/topics/telemetry-raw-{locale}'\n    publisher.publish(topic, json.dumps(data).encode())\n```\n\n## Follow-up Questions\n- How would you add support for a new locale with minimal changes?\n- How would you monitor for schema drift and alert on malformed lines?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-13",
        "questionId": "q-25752",
        "question": "Advanced: Architect a real-time, multi-tenant telemetry pipeline on GCP. Telemetry from mobile apps is published to Pub/Sub per tenant. Build a Dataflow streaming job that validates schema, deduplicates by event_id, and emits 30s windowed aggregates per tenant. Write raw events to gs://raw-telemetry/{tenant}/{date}/ and aggregates to a partitioned BigQuery table by date/tenant. Enforce least-privilege IAM, enable VPC Service Controls, set Dataflow autoscaling and a budget cap. Include a test plan and rollback path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Dataflow streaming pipeline triggered by Pub/Sub messages that carry file paths. Dataflow reads JSONL from GCS, validates schema, and deduplicates by event_id within a per-day window. Writes to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a single Pub/Sub topic with tenant_id in attributes; a Dataflow streaming job reads, applies exactly-once processing using Pub/Sub message_ids and BigQuery insertId for idempotent writes; write re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Pub/Sub as ingress, a Dataflow streaming pipeline with fixed windows to compute anomaly scores, and a BigQuery sink that uses insertId = deviceId|windowEnd to ensure idempotent writes. Add a dead-",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An end-to-end approach: Pub/Sub per-tenant ingestion, a Dataflow streaming job validates messages, deduplicates by event_id, and emits 30s windowed aggregates per tenant. Write raw events to gs://raw-",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time data engineering and per-tenant isolation.\n\n## Key Concepts\n- Pub/Sub, Dataflow streaming, schema validation, deduplication, windowing\n- BigQuery partitioning, GCS storage, IAM least privilege, VPC Service Controls\n- Cost control and rollback strategy\n\n## Code Example\n```python\n# Dataflow skeleton: schema check, dedupe, windowed aggregation\nclass ValidateAndDedup(DoFn):\n    def process(self, elem, timestamp=DoFn.TimestampParam, state=DoFn.StateParam):\n        # validate and deduplicate by event_id within tenant window\n        yield (elem[\"tenant\"], elem[\"date\"], elem[\"metrics\"])\n```\n\n## Follow-up Questions\n- How to handle schema evolution with backward/forward compatibility?\n- How to test failure scenarios and ensure exactly-once semantics?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-14",
        "questionId": "q-15773",
        "question": "Design a beginner-friendly GCP ingestion pattern using Pub/Sub as the decoupled channel: when a CSV file lands in gs://tenant-uploads/, a Cloud Function validates header and row count, publishes a per-day message to Pub/Sub, and a Cloud Run worker consumes those messages to append to BigQuery analytics.logs with insertId = filePath+date. Include minimal IAM, idempotent writes, and a test plan with duplicate uploads and partial failures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement an HTTP Cloud Function that reads the canary config from Firestore, hashes user_id with SHA-256, computes bucket = hash % 100, and returns enabled when bucket < percent; otherwise di",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement with a Cloud Function triggered by finalize events on gs://shared-uploads/. Use file name and generation to form insertId; write a row to BigQuery docs.events; copy to gs://docs-archive/YYYY",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "The approach uses a storage trigger to validate structure, Pub/Sub for decoupling, and a Cloud Run worker for idempotent writes. Ensure insertId ties to file path+date, configure least-privilege servi",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Data Catalog + Data Loss Prevention for lineage and PII redaction; tag Pub/Sub messages and BigQuery tables with lineage metadata. Dataflow streaming job writes enriched events to BigQuery using i",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to craft a simple, resilient dataflow using GCP primitives and idempotency.\n\n## Key Concepts\n- Pub/Sub as a decoupling mechanism\n- Cloud Functions for validation\n- Cloud Run for processing and BigQuery writes\n- Idempotent inserts via insertId\n\n## Code Example\n```javascript\n// Placeholder: not included in this answer\n```\n\n## Follow-up Questions\n- How would you monitor failure modes and implement dead-letter queues?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-15",
        "questionId": "q-8312",
        "question": "Design a beginner-friendly data enrichment pipeline on GCP where a CSV lands in gs://incoming/csv, a Cloud Function validates the header and rows, calls an external REST API per row to obtain enrichment, and writes the enriched rows to a date-partitioned BigQuery table data.enriched_events. Include per-row insertId for idempotency, minimal IAM, exponential backoff retries, and a basic test plan with a sample CSV and a mocked API?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pub/Sub streaming with per-tenant topics, a Dataflow pipeline that normalizes events via a central schema registry, redacts PII with a policy loaded from Cloud Storage and applied to each event, t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Trigger a Cloud Function on finalize of gs://incoming/csv; validate header and rows; for each row call an per-row REST API to fetch enrichment; write enriched rows to BigQuery data.enriched_events, pa",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Extract .gz, parse NDJSON, validate event_id/user_id/ts/event_type; publish each record to Pub/Sub; Dataflow batch reads Pub/Sub, deduplicates on event_id, and writes to BigQuery with insertId=event_i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a streaming Dataflow template that subscribes to region-local Pub/Sub topics, processes per-tenant events with fixed windows, and writes to per-tenant BigQuery datasets in the same region using CM",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis checks practical event-driven design using Cloud Functions, Storage triggers, and BigQuery with idempotency and retries.\n\n## Key Concepts\n- Cloud Functions triggers on Cloud Storage finalize\n- Per-row API enrichment and error handling\n- Idempotent inserts with insertId\n- Minimal IAM and exponential backoff\n- Date-partitioned BigQuery loads\n\n## Code Example\n```javascript\n// skeleton: Cloud Function outline for per-row enrichment\nimport {BigQuery} from '@google-cloud/bigquery';\nconst bigquery = new BigQuery();\nexports.enrichCsv = async (event, ctx) => {\n  // parse CSV, validate headers, call external API for each row, write to BigQuery\n};\n```\n\n## Follow-up Questions\n- How would you test idempotency edge cases when API responses vary?\n- How would you monitor and alert on enrichment failures without noisy alerts?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-16",
        "questionId": "q-25208",
        "question": "Design an advanced GCP workflow that enforces per-tenant IAM least privilege and data residency while ingesting telemetry into Pub/Sub, uses a policy engine (Cloud Run) to validate bindings against a baseline in Cloud Storage, redacts PII, writes idempotently to BigQuery, and emits drift alerts to Cloud Monitoring with per-tenant dashboards; include testing, retries, and a failover plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Dataflow (Python) streaming pipeline: read Pub/Sub, dedup on event_id, write to BigQuery analytics.events with insertId=event_id. Use event_time as the watermark for daily partitions, enforce per-tena",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "The architecture ingests per-tenant telemetry into Pub/Sub, runs a policy engine (Cloud Run) to validate IAM bindings against a baseline in Cloud Storage, redacts PII, and writes idempotently to BigQu",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "In this task, implement an environment-aware ingestion pipeline: a Cloud Function triggers on gs://ingest-{env}/uploads finalize, validates tenantId matches env, and size under 5 MB. On success, publi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Debezium CDC on GKE to publish Cloud SQL transactions to Pub/Sub; Dataflow streaming deduplicates by transaction_id and uses Pub/Sub IDs and BigQuery insertId for exactly-once semantics; enrich with reference data f…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests multi-tenant governance, data residency, and production reliability in a real-time GCP pipeline. It exercises IAM least privilege, policy-as-code, data redaction, idempotent writes, and observability across services.\n\n## Key Concepts\n- Per-tenant IAM least privilege and data residency\n- Policy engine in Cloud Run validating bindings against a baseline in Cloud Storage\n- PII redaction and deterministic BigQuery writes (idempotent inserts)\n- Drift detection and alerting via Cloud Monitoring/Asset Inventory\n- Testing with drift scenarios, retries/backoff, and failover planning\n\n## Code Example\n```python\ndef validate_bindings(tenant_id, bindings, baseline_path):\n    baseline = load_json_from_gcs(baseline_path)\n    expected = baseline.get(tenant_id)\n    return bindings == expected\n```\n\n## Follow-up Questions\n- How would you test policy drift across tenants and environments?\n- What are the key failure modes and how would you mitigate them?\n- How would you measure and enforce per-tenant data residency over time?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-17",
        "questionId": "q-16144",
        "question": "You run a global payment API with services deployed in two GKE regions. Traffic is unpredictable and latency must stay under 50ms p95 for 99.99% of requests. Design an end-to-end, scalable, secure production pipeline on GCP using GKE, Cloud Run, Pub/Sub, Spanner, Cloud CDN, and IAM. Include autoscaling, cross-region failover, data locality, observability, and a DR plan with RPO/RTO targets. What would you implement and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Cloud Scheduler to trigger a nightly Cloud SQL export to Cloud Storage; grant Cloud SQL SA write access to the bucket; create a Cloud Function triggered on objectFinalize to gzip and rename to bac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a single Pub/Sub topic carrying event payloads with tenant_id metadata. Implement a streaming Dataflow job that deduplicates events, writes to a partitioned BigQuery table keyed by tenant, and app",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Read Pub/Sub with a streaming Dataflow pipeline (Python), window 1 minute, enrich via a BigQuery lookup, and write to a partitioned BigQuery sink using insert_id = user_id + event_time to achieve exac",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Key choices: regional microservices on GKE with multi-region load balancing; Cloud Run for bursty endpoints; Pub/Sub for event streams; Spanner for globally consistent data; regional Cloud SQL; Cloud",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a multi-region, latency-sensitive system on GCP, balancing strong consistency, failover, data locality, and DR.\n\n## Key Concepts\n\n- Global vs regional deployment and latency targets\n- Data store choices: Spanner (global) vs Cloud SQL (regional)\n- Event-driven architecture: Pub/Sub + Dataflow\n- DR planning: RPO/RTO, failover orchestration, testing\n- Observability and security: IAM least privilege, Cloud Monitoring/Logging\n\n## Code Example\n\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: payment-service\nspec:\n  replicas: 2\n  template:\n    spec:\n      containers:\n      - image: gcr.io/PROJECT/payment-service:latest\n```\n\n## Follow-up Questions\n\n- How would you validate cross-region failover and DR drills?\n- Which metrics and dashboards prove 99.99% availability and sub-50ms p95 latency?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-18",
        "questionId": "q-24083",
        "question": "In GCP, design an intermediate-level, scalable audit pipeline for a multi-tenant SaaS where each tenant's IAM bindings must be validated nightly against a central policy, detecting drift, enforcing least privilege, and generating an audit summary to BigQuery with insertId = tenantId+date. The pipeline should be event-driven (Cloud Scheduler/Cloud Functions), use a policy baseline in Cloud Storage, and include retry/backoff and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Parse a Pub/Sub message in a Cloud Function (Node.js): require deviceId, ts, and reading. If valid, insert into BigQuery iot.telemetry with insertId = deviceId + '_' + ts; if invalid, publish to iot-telemetry-dlq.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub in a single region, Dataflow streaming with stateful dedup and event-time windows; write to BigQuery using insertId from event_id+ingest_time; enforce regional residency; implement schema",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a nightly drift-detection pipeline: Cloud Scheduler triggers a Function that fetches current IAM bindings for all tenants (via Cloud Asset Inventory/Resource Manager) and compares them to a bas",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Dataflow streaming job: read Pub/Sub, ParDo redact PII fields, validate schema; use a stateful DoFn for dedup on event_id within a 5-minute window; write to BigQuery with insertId=event_id, part",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nA realistic, scalable audit scenario for multi-tenant IAM in GCP; tests accuracy, reliability, and safety in enforcing least privilege.\n\n## Key Concepts\n- IAM bindings and drift detection\n- Cloud Asset Inventory/Resource Manager\n- Cloud Storage baselines, BigQuery insertId strategy\n- Pub/Sub orchestration and Cloud Scheduler\n- Retries/backoff and idempotent remediation\n\n## Code Example\n```javascript\n// Node.js Cloud Function skeleton\nexports.checkIamDrift = async (req, res) => {\n  // 1) list bindings per tenant\n  // 2) compare with baseline\n  // 3) emit drift events to Pub/Sub and BigQuery\n  res.status(200).send('ok');\n};\n```\n\n## Follow-up Questions\n- How would you test drift at scale across 1000s of tenants?\n- How would you handle late-arriving data and eventual consistency?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-19",
        "questionId": "q-20915",
        "question": "Design a cross-region, multi-tenant data ingestion and analytics pipeline on GCP. Events published to Pub/Sub in us-central1 must be validated, deduplicated per tenant, and written to per-tenant partitioned BigQuery tables via Dataflow. Raw events should land in Cloud Storage with a replay manifest. Include dead-letter handling, idempotent upserts via insertId, IAM, monitoring, and a plan for cross-region failover to us-east1?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route region-specific events to gs://region-logs/{region}/, where a Cloud Function validates payloads, redacts PII using a library like google-data-masking, and publishes sanitized records to a region-scoped Pub/Sub top…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a beginner-friendly GCP workflow to monitor IAM policy changes: export Cloud Audit Logs to Pub/Sub, a Cloud Function filters events for policy_change, classifies as ADD/REMOVE/UPDATE, and write",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a streaming Dataflow Flex template that subscribes to Pub/Sub in us-central1, validates incoming Avro/JSON payloads, and deduplicates events per tenant using the combination of tenant_id and event_id. Write to per-t…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Trigger a Cloud Function on object_finalize for gs://payments/ingest/. Validate headers (sid, amount, currency, ts) and normalize currency to USD using a tiny map. Write cleaned rows to gs://payments/",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to design robust, scalable, region-aware data pipelines with cross-region failover, multi-tenancy isolation, and exactly-once semantics. It also evaluates knowledge of dead-letter handling, replayability, IAM best practices, monitoring strategies, and disaster recovery planning.\n\n## Key Concepts\n\n- **Streaming Ingestion**: Pub/Sub to Dataflow for real-time event processing\n- **Multi-Tenancy**: Per-tenant partitioning in BigQuery with insertId for idempotent upserts\n- **Data Archival**: Raw event persistence in Cloud Storage with a replay ledger for recovery scenarios\n- **Exactly-Once Semantics**: Using insertId in BigQuery to handle duplicates and ensure data integrity\n- **Cross-Region Failover**: Pub/Sub mirroring to replicate messages between regions\n- **Observability**: Cloud Monitoring for latency, error rates, and backlog metrics",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-20",
        "questionId": "q-15691",
        "question": "Design an advanced GCP ingestion pipeline for a multi-tenant app that enforces per-tenant data residency. Ingest JSON events into Pub/Sub; a Cloud Run service validates schema and residency using a tenant-registry; route to BigQuery streaming inserts in the tenant's region or archive to Cloud Storage if the region is unavailable. How would you ensure idempotency with a deterministic insertId, a DLQ, per-tenant IAM bindings, exponential backoff, and a robust test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest logs via Pub/Sub with tenant_id; a Dataflow streaming job validates schema, extracts tenant_id, and writes to a partitioned BigQuery table keyed by date with a tenant_id column. Use BigQuery ro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Trigger a Cloud Function on finalize of gs://incoming/csv; validate header and rows; for each row call an per-row REST API to fetch enrichment; write enriched rows to BigQuery data.enriched_events, pa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Pub/Sub streaming with per-tenant topics, a Dataflow pipeline that normalizes events via a central schema registry, redacts PII with a policy loaded from Cloud Storage and applied to each event, t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an event-driven ingest: Pub/Sub to Cloud Run for validation; tenancy rules via a single source of truth (Firestore) mapping region per tenant; route to BigQuery streaming in that region or f",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates ability to design multi-tenant data residency, DR, idempotency, testing, and production-grade reliability in GCP.\n\n## Key Concepts\n- Pub/Sub, Cloud Run, BigQuery streaming, per-tenant residency, DLQ, insertId, IAM, retries, canary testing.\n\n## Code Example\n```javascript\nfunction makeInsertId(tenantId, eventId, ts) {\n  return `${tenantId}::${eventId}::${ts}`;\n}\n```\n\n## Follow-up Questions\n- How would you implement the tenant-registry and enforce dynamic residency changes?\n- How would you simulate regional outages and validate DR failover in canary tests?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-21",
        "questionId": "q-25313",
        "question": "Design an intermediate GCP data pipeline for real-time telemetry from edge devices: devices publish JSON events to Pub/Sub; a Dataflow streaming job validates schema, deduplicates by messageId, enriches with device metadata from Firestore, writes clean rows to BigQuery and archives raw messages to Cloud Storage. Include dead-letter handling, idempotency guarantees, and observability with Cloud Logging, Monitoring dashboards, and tracing. How would you implement and test this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build a real-time GCP pipeline with these components: Pub/Sub receives device events with a schema for automatic validation. Dataflow streaming job reads from the subscription, validates incoming JSON, removes duplicate…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement a Dataflow pipeline (Python/Java) that reads Pub/Sub, validates against a strict JSON schema, deduplicates by event_id via stateful processing, applies fixed windows on event_time, a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed approach: Trigger Cloud Function on finalize of CSV in gs://reports/customer-csv/raw. Parse headers (customer_id, amount, date) and ensure amount is numeric. Mask email and phone fields. Writ",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single bucket with Lifecycle rules: after 45 days transition to ARCHIVE, after 365 days to COLDLINE. Cloud Run HTTP API accepts bucket, object, and range; reads the specified byte range via the",
            "isCorrect": false
          }
        ],
        "explanation": "**Why this design works:**\n\n1. **Pub/Sub + Schema**: Devices publish to a topic with a schema attached. Pub/Sub rejects invalid messages before they reach the pipeline, reducing processing overhead.\n\n2. **Dataflow Streaming**: Handles the pipeline logic - validation, deduplication, enrichment. Use `withIdAttribute` for Pub/Sub-level deduplication or Dataflow's exactly-once mode.\n\n3. **Firestore Side Inputs**: Firestore provides low-latency lookups. Dataflow caches metadata as side inputs for efficient joins without external calls per message.\n\n4. **BigQuery Storage Write API**: Low-latency streaming inserts with exactly-once semantics. InsertIds ensure no duplicates on retries.\n\n5. **GCS Archival**: Write raw JSON to GCS using FileIO for compliance and reprocessing capability.\n\n6. **Dead-Letter Queue**: Failed records (validation failures, enrichment errors) route to a separate topic for investigation.\n\n7. **Observability**: Structured Cloud Logging, Monitoring dashboards for throughput/latency/errors, and Cloud Trace for end-to-end request tracing.\n\n**Testing approach:** Unit test each DoFn in Dataflow (validation, deduplication, enrichment). Use generated test data for integration tests. Verify end-to-end flow with sample device events.",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-22",
        "questionId": "q-21658",
        "question": "You're responsible for a real-time telemetry pipeline for a mobile app used globally. Telemetry events are published to Pub/Sub topics per region and must be ingested, deduplicated, and stored with minimal latency. Design a robust, cost-aware GCP architecture that: 1) streams events into Dataflow with event-time processing and windowed per-minute dashboards; 2) writes raw events to Cloud Storage in AVRO with partitioning by region/date; 3) computes per-region aggregates and loads into BigQuery; 4) guarantees at-least-once or better deduping with idempotent sinks; 5) supports regional failover and secure IAM with least privilege; 6) includes a test plan and rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "The pipeline uses two Cloud Functions and Pub/Sub. Function A triggers on csv finalize, validates header and numeric columns, rejects malformed files, and publishes a message with path and date to Pub",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest with Pub/Sub per tenant; Dataflow streaming (Beam) enforces exactly-once, writes to BigQuery partitioned by date and tenant. Use insertId = tenantId+eventId to de-duplicate and a shared dataset",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Dataflow (Beam) streaming job that subscribes to Pub/Sub transactions.events, validates JSON schema, deduplicates by transaction_id, and uses 1-minute fixed windows by user_id to compute per-",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single Dataflow streaming job reading from regional Pub/Sub topics, apply event-time windowing of 1 minute, and emit per-region aggregates to BigQuery with INSERT_ID based on eventId to ensure i",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time ingestion, deduplication, multi-region failover, and cost-aware design. It also probes sink idempotency, windowing strategies, and robust testing.\n\n## Key Concepts\n- Dataflow streaming with Pub/Sub sources and event-time windows\n- Idempotent BigQuery writes via INSERT_ID\n- Cloud Storage AVRO partitions for raw data\n- Least-privilege IAM and regional failover\n- Dead-letter queues and backoff strategies\n\n## Code Example\n```text\n# Pseudo-beam skeleton for the Dataflow pipeline\npipeline = Pipeline.create(options)\nlines = pipeline.apply(ReadStep)\n```\n\n## Follow-up Questions\n- How to handle late data?\n- How to implement cross-region failover and data consistency?\n- How to test end-to-end with replay and backfill scenarios?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-23",
        "questionId": "q-14148",
        "question": "Design a beginner-friendly GCP file-processing workflow: when a new file lands in gs://shared-uploads/, trigger a Cloud Function that reads metadata (name, size, content-type), writes a row to BigQuery docs.events with insertId = name+timestamp, copies the file to gs://docs-archive/YYYY/MM/DD/ with the same name, and deletes the source after a successful copy. Include minimal IAM bindings, retries, and a basic test plan that covers a duplicate upload?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement with a Cloud Function triggered by finalize events on gs://shared-uploads/. Use file name and generation to form insertId; write a row to BigQuery docs.events; copy to gs://docs-archive/YYYY",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a streaming pipeline: Pub/Sub per region -> Dataflow -> BigQuery. Use Pub/Sub message IDs for exactly-once semantics with dedup. Partition BigQuery tables by region and ticker; allow schema evo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a single Pub/Sub topic carrying event payloads with tenant_id metadata. Implement a streaming Dataflow job that deduplicates events, writes to a partitioned BigQuery table keyed by tenant, and app",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Dataflow streaming job with Pub/Sub source, a per-key stateful dedup (tenantId, eventId) and write to daily-partitioned BigQuery tables per tenant (telemetry_<tenantId>_YYYYMMDD). Use eventId as",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests event-driven design, idempotency, and basic GCP permissions.\n\n## Key Concepts\n- Cloud Functions, Cloud Storage triggers, BigQuery inserts, object copy and delete, insertId for dedup\n- IAM least privilege, retry/backoff basics.\n\n## Code Example\n```javascript\n// Cloud Function skeleton for finalized Cloud Storage event\nconst {BigQuery} = require('@google-cloud/bigquery');\nexports.processFile = async (event, ctx) => {\n  const name = event.name;\n  const generation = event.generation;\n  // write to BigQuery with insertId = name+generation\n  // copy to archive path and delete source on success\n};\n```\n\n## Follow-up Questions\n- How would you test idempotency with concurrent uploads?\n- What monitoring would you add for failed copies?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-24",
        "questionId": "q-12798",
        "question": "Design a beginner-friendly GCP data pipeline for a mobile app: events arrive as newline-delimited JSON (JSONL) files uploaded to gs://events/raw/. Build a validation step that checks for fields event_id, user_id, timestamp, event_type; redact PII fields user_id and device_id via one-way hash; publish valid events to Pub/Sub topic analytics.events; a Cloud Run service subscribes to that topic and writes to BigQuery dataset analytics.events using insertId = event_id; preserve originals in gs://events/raw/original and redacted copies in gs://events/redacted. Include minimal IAM bindings, retries, and a basic test plan with sample events and privacy checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger on finalization of raw video; compute a deterministic id from filename and content hash to enable idempotency. Create a single Transcoder job with 3 outputs (1080p, 720p, 480p) to gs://media-u",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To implement, use Pub/Sub to feed Dataflow streaming; validate against a Firestore-stored schema and apply dedup by event_id; write to BigQuery with insertId=event_id; store a daily partitioned raw ev",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Cloud Function triggered by Cloud Storage to validate JSONL events from gs://events/raw/, redact PII fields (user_id, device_id) using salted SHA-256 hashing, publish valid events to Pub/Sub topic analytics.…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Event-driven audio transcription: uploads to gs://audio-uploads; Cloud Function validates format/metadata, publishes Pub/Sub message; Cloud Run transcribes and writes to gs://transcripts/{region}/{nam",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis scenario tests comprehensive GCP data engineering skills including end-to-end data flow design, data validation, privacy compliance, and reliability patterns for beginners. It specifically evaluates understanding of idempotent writes via insertId and proper data retention practices.\n\n## Key Concepts\n- Cloud Functions (event-driven processing)\n- Pub/Sub (asynchronous messaging)\n- Cloud Run (containerized services)\n- BigQuery (data warehousing)\n- Cloud Storage (object storage)\n- Data validation and schema enforcement\n- PII redaction and privacy protection\n- Idempotent operations and deduplication\n- IAM principles and least privilege\n- Retry strategies and error handling\n\n## Code Example\n```javascript\nconst crypto = require('crypto');\n\nfunction redactPII(event, salt) {\n  if (event.user_id) {\n    event.user_id = crypto.createHash('sha256')\n      .update(event.user_id + salt)\n      .digest('hex');\n  }\n  if (event.device_id) {\n    event.device_id = crypto.createHash('sha256')\n      .update(event.device_id + salt)\n      .digest('hex');\n  }\n  return event;\n}\n```",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-15T14:18:05.250Z",
    "version": 1
  }
]