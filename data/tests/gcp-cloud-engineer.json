[
  {
    "id": "test-gcp-cloud-engineer",
    "channelId": "gcp-cloud-engineer",
    "channelName": "GCP Cloud Engineer",
    "title": "GCP Cloud Engineer Knowledge Test",
    "description": "Test your understanding of GCP Cloud Engineer with 25 questions.",
    "questions": [
      {
        "id": "gcp-cloud-engineer-q-0",
        "questionId": "q-21658",
        "question": "You're responsible for a real-time telemetry pipeline for a mobile app used globally. Telemetry events are published to Pub/Sub topics per region and must be ingested, deduplicated, and stored with minimal latency. Design a robust, cost-aware GCP architecture that: 1) streams events into Dataflow with event-time processing and windowed per-minute dashboards; 2) writes raw events to Cloud Storage in AVRO with partitioning by region/date; 3) computes per-region aggregates and loads into BigQuery; 4) guarantees at-least-once or better deduping with idempotent sinks; 5) supports regional failover and secure IAM with least privilege; 6) includes a test plan and rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a multi-tenant telemetry pipeline on GCP: each tenant publishes to a tenant-scoped Pub/Sub topic; a Dataflow streaming job normalizes events, enriches with tenant metadata from Cloud Datasto",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a single Dataflow streaming job reading from regional Pub/Sub topics, apply event-time windowing of 1 minute, and emit per-region aggregates to BigQuery with INSERT_ID based on eventId to ensure i",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Set up a Pub/Sub pipeline: publish audit events to audit-logs; a Cloud Run service subscribes for enrichment (normalize fields, add user role from BigQuery) and republishes to audit-logs-processed. A",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Cloud Function triggered by GCS events on gs://customer-reports/incoming. Validate headers against ['customer_id','order_date','amount'], normalize dates to ISO, convert to Parquet, and write to",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time ingestion, deduplication, multi-region failover, and cost-aware design. It also probes sink idempotency, windowing strategies, and robust testing.\n\n## Key Concepts\n- Dataflow streaming with Pub/Sub sources and event-time windows\n- Idempotent BigQuery writes via INSERT_ID\n- Cloud Storage AVRO partitions for raw data\n- Least-privilege IAM and regional failover\n- Dead-letter queues and backoff strategies\n\n## Code Example\n```text\n# Pseudo-beam skeleton for the Dataflow pipeline\npipeline = Pipeline.create(options)\nlines = pipeline.apply(ReadStep)\n```\n\n## Follow-up Questions\n- How to handle late data?\n- How to implement cross-region failover and data consistency?\n- How to test end-to-end with replay and backfill scenarios?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-1",
        "questionId": "q-18923",
        "question": "Design a real-time telemetry pipeline on GCP for a global fleet of devices. Devices publish JSON telemetry to Pub/Sub (multi-region). Build a Dataflow streaming job that validates schema, deduplicates by event_id, enriches with device metadata, and writes to a partitioned BigQuery table telemetry.events (insertId = event_id). Store raw events in Cloud Storage. Include multi-region deployment, idempotence, IAM boundaries, monitoring, and a test plan with failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a serverless ingestion pipeline where a Cloud Function validates each JSONL line for required fields (event_id, device_id, ts, event_type), publishes valid records to Pub/Sub topic app-logs.events, and a Dataflow…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed solution: A multi-region Pub/Sub topic collects telemetry; a Dataflow streaming job validates payloads, deduplicates by event_id, enriches with device metadata, and writes to a partitioned Bi",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Set up a streaming ingestion: partner CSVs in gs://partner-data/tenant-*/incoming, Pub/Sub per tenant -> Dataflow (Beam) pipeline to parse CSV, validate against a schema registry in Firestore, enrich",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe the end-to-end flow: Billing export to BigQuery; Cloud Scheduler triggers a Cloud Function that queries the last 7 days to compute a baseline per service; it compares yesterday’s spend to 1.5",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time dataflow design, cross-region resilience, and end-to-end correctness with idempotent writes.\n\n## Key Concepts\n- Streaming pipelines, Pub/Sub multi-region\n- Dataflow stateful deduplication by event_id\n- BigQuery inserts with insertId, partitioning\n- IAM boundaries and monitoring\n\n## Code Example\n```javascript\n// Placeholder high-level wiring: PubSub -> Dataflow -> BigQuery\n```\n\n## Follow-up Questions\n- How would you validate schema evolution in the streaming path?\n- What cost controls would you apply for billions of events per day?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-2",
        "questionId": "q-2214",
        "question": "You run a SaaS platform on GCP and collect telemetry per tenant via Pub/Sub. Design an end-to-end, multi-region ingestion and analytics pipeline that ensures tenant isolation, exactly-once processing, and scalable cost control. Requirements: (a) topic/subscription layout with a central ingress project and per-tenant downstream sinks; (b) Dataflow streaming job that deduplicates using event_id, enriches with metadata from a centralized store, and writes to daily-partitioned BigQuery tables per tenant; (c) reliable dead-letter handling; (d) security (IAM, CMEK, and VPC Service Controls); (e) cross-region DR and testing plan. Provide a diagram?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To implement, use Pub/Sub to feed Dataflow streaming; validate against a Firestore-stored schema and apply dedup by event_id; write to BigQuery with insertId=event_id; store a daily partitioned raw ev",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a streaming ingest: per-tenant Pub/Sub topics in a central ingress project feed into a Dataflow streaming job that deduplicates on event_id, enriches with tenant metadata, and writes to dail",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "In this task, implement an environment-aware ingestion pipeline: a Cloud Function triggers on gs://ingest-{env}/uploads finalize, validates tenantId matches env, and size under 5 MB. On success, publi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline topic/message schema, Dataflow windowing (FixedWindows 1h, lateness 900s), BigQuery partitioning and insertId-based dedup, idempotent writes, Pub/Sub retry/backoff, least-privilege IAM, multi-",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDesigning a scalable, secure, multi-tenant pipeline with strong guarantees is critical for production SaaS on GCP.\n\n## Key Concepts\n- Pub/Sub topic/subscription topology for multi-tenancy and isolation\n- Dataflow streaming enforces dedupe and exactly-once writes\n- BigQuery per-tenant, daily-partitioned datasets\n- Dead-letter handling, CMEK, IAM scoping, and VPC Service Controls\n- Cross-region replication and DR strategies\n\n## Code Example\n```python\n# Pseudocode: Dataflow DoFn dedups by event_id and writes with insertId\nfrom apache_beam import DoFn\nclass DedupEnrich(DoFn):\n    def process(self, element, *args, **kwargs):\n        event_id = element['event_id']\n        # check and write to BigQuery using insertId=event_id\n        yield element\n```\n\n## Follow-up Questions\n- How would you validate idempotent behavior and monitor late or out-of-order events?\n- How would you evolve tenant schemas without breaking existing pipelines?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-3",
        "questionId": "q-21088",
        "question": "Design a streaming log pipeline on GCP: Pub/Sub -> Dataflow -> BigQuery. Deduplicate events, enrich with geolocation, redact PII, and apply a 30-day TTL cleanup. Ensure at-least-once processing, idempotent writes, and least-privilege IAM. Provide data schema, failure handling, and a practical test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a real-time pipeline with Pub/Sub -> Dataflow (Beam) -> BigQuery. Ingest JSON events into Pub/Sub, Dataflow enriches using a cached profile lookup, deduplicates by event_id, and writes to a date-p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt Pub/Sub with event_id as the dedupe key; implement a Dataflow (Beam) streaming job using a stateful DoFn to deduplicate, applying 5-minute fixed windows; write to analytics.events_raw with inser",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Dataflow streaming: Pub/Sub -> ParDo dedupe (event_id), PII redaction, and geolocation enrichment, then partitioned BigQuery sink. TTL cleanup via scheduled job. IAM least privilege for Dataflow, Pub/",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest with Pub/Sub per tenant; Dataflow streaming (Beam) enforces exactly-once, writes to BigQuery partitioned by date and tenant. Use insertId = tenantId+eventId to de-duplicate and a shared dataset",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates building a robust, real-time data pipeline with correctness, security, and testing considerations in GCP.\n\n## Key Concepts\n- Streaming pipelines, deduplication, and windowing\n- Data enrichment and PII redaction\n- TTL cleanup and partitioned sinks\n\n## Code Example\n```javascript\n// Pseudo-DoFn for deduplication\nfunction DedupFn(event) {\n  if (seen(event.event_id)) return null;\n  mark_seen(event.event_id);\n  return event;\n}\n```\n\n## Follow-up Questions\n- How would you measure latency and backpressure?\n- How would you simulate failures and verify exactly-once behavior?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-4",
        "questionId": "q-4146",
        "question": "Design a beginner-friendly file-audit pipeline on GCP: when a CSV lands in gs://payments/ingest/, a Cloud Function validates headers (sid, amount, currency, ts), normalizes currency to USD, and writes cleaned rows to gs://payments/clean/YYYY/MM/DD/filename.cleaned.csv. It should also append an audit row to BigQuery (insertId = filePath+ts). If any row is invalid, publish a message to Pub/Sub and quarantine the file. How would you implement this end-to-end, including idempotency and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger a Cloud Function on object_finalize for gs://payments/ingest/. Validate headers (sid, amount, currency, ts) and normalize currency to USD using a tiny map. Write cleaned rows to gs://payments/",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design an advanced GCP multi-tenant telemetry ingest: tenants publish to per-tenant Pub/Sub topics; a Dataflow streaming job shards by tenant_id, redacts PII, and de-duplicates on event_id; writes to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Cloud Function triggered by Cloud Storage events on gs://logs/input/. It processes the file line-by-line, parses JSON, and if record.severity in ['ERROR','CRITICAL'] logs to BigQuery table",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-stage CSV ingestion: a Cloud Function on finalization validates header (id,email,signup_ts) and computes a file_hash; if unseen in BigQuery processed_files, it writes a sanitized copy to gs://cust",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end event-driven thinking with basic data validation, simple transformation, and auditing on GCP. It combines Cloud Functions, Cloud Storage, BigQuery, and Pub/Sub with a focus on idempotency and minimal IAM.\n\n## Key Concepts\n\n- Event-driven processing on object finalize\n- Lightweight data validation and field normalization\n- Idempotent writes via insertId and deterministic output paths\n- Audit logging in BigQuery and error routing via Pub/Sub\n\n## Code Example\n\n```javascript\n// Node.js Cloud Function skeleton for object finalize\nexports.handleIngest = async (event, context) => {\n  const bucket = event.bucket;\n  const name = event.name;\n  // read object from Storage, parse CSV, validate headers\n  // for each row: validate, normalize currency, write to cleaned path\n  // on success: insert audit row to BigQuery with insertId\n  // on invalid: publish message to Pub/Sub and quarantine\n};\n```\n\n## Follow-up Questions\n\n- How would you implement idempotency for reprocessing the same file?\n- What minimal IAM bindings would you apply for each service involved?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-5",
        "questionId": "q-23971",
        "question": "Design a streaming data ingestion pipeline in GCP that handles multi-tenant user events arriving on Pub/Sub, validates schema, deduplicates by event_id, enriches with user profile from Firestore, and writes to a partitioned BigQuery table with insertId to ensure idempotence. Include per-tenant isolation, security controls, late data handling, monitoring, and a test plan. How would you implement and what trade-offs would you consider?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a streaming ingestion and enrichment pipeline: Pub/Sub -> Dataflow Streaming -> BigQuery. Deduplicate with event_id and use BigQuery insertId to ensure idempotent writes; window by user and da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a single Pub/Sub topic carrying tenantId; a Dataflow streaming pipeline (Beam) deduplicates by insertId, writes to per-tenant BigQuery partitions (dataset/tablename + ingestion date). Enforce stri",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Dataflow streaming pipeline (Beam) that reads Pub/Sub events, validates JSON/Avro, deduplicates by event_id within a small window, enriches with Firestore user profiles, and writes to a partitio",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement an HTTPS Cloud Function that validates the webhook JSON and checks a simple dedup store in a Cloud Storage bucket for clientEventId. If unseen, write the raw payload to gs://ingest/YYYY/MM/D",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design scalable, fault-tolerant streaming architectures that handle multi-tenant data, ensure exactly-once semantics, enforce isolation, and integrate cross-service enrichments.\n\n## Key Concepts\n\n- Dataflow/Beam streaming pipelines\n- Pub/Sub as the ingestion buffer\n- Schema validation and event deduplication (event_id) with windowing\n- Enrichment via Firestore user profiles\n- BigQuery writes with insertId for idempotence and partitioned tables\n- Per-tenant isolation (datasets + authorized views), CMEK, and IAM controls\n- Late data handling (allowed lateness), monitoring, and cost controls\n\n## Code Example\n\n```python\nimport apache_beam as beam\n\n# Pseudo-implementation for clarity\n\ndef parse(msg):\n  # decode and validate\n  return {\"event_id\": ..., \"tenant\": ..., \"payload\": ...}\n\nclass EnrichFn(beam.DoFn):\n  def process(self, elem):\n    # lookup Firestore profile by tenant/user id\n    elem[\"profile\"] = fetch_profile(elem[\"payload\"][\"user_id\"])\n    yield elem\n\nwith beam.Pipeline(options=opts) as p:\n  (p\n   | 'Read PubSub' >> beam.io.ReadFromPubSub(topic=TOPIC)\n   | 'Parse' >> beam.Map(parse)\n   | 'Deduplicate' >> beam.WithKeys(lambda e: e['event_id'])\n   | 'Group' >> beam.GroupByKey()\n   | 'TakeOne' >> beam.Map(lambda kv: kv[1][0])\n   | 'Enrich' >> beam.ParDo(EnrichFn())\n   | 'WriteBQ' >> beam.io.WriteToBigQuery(\n       table='project:dataset.events',\n       insert_retry_limit=3,\n       insert_data=True,\n       method=beam.io.WriteToBigQuery.Method.FILE_PER_PARTITION,\n       with_id=lambda e: e['event_id']\n     )\n  )\n```\n\n## Follow-up Questions\n\n- How would you test exactly-once guarantees with duplicate Pub/Sub messages?\n- How do you evolve the schema while preserving backward compatibility?\n- What are trade-offs between per-tenant datasets vs shared tables with access controls?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-6",
        "questionId": "q-11164",
        "question": "Design a beginner-friendly GCP workflow for on-demand image resizing requests. A mobile app uploads an image to gs://live-uploads/, with a signed URL. A Cloud Function validates the image and enqueues a Cloud Tasks task to invoke a Cloud Run service that resizes the image to 256x256 and stores the result in gs://thumbnails/ with a deterministic name. Log an entry in BigQuery (insertId = path+size). Include minimal IAM bindings, exponential backoff retries, and a basic test plan with a sample image and a retry scenario?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design cross-region DR: global Load Balancer routes to Cloud Run in two regions; Cloud SQL primary in us-central1 with a synchronous replica in europe-west1. Automate promotion via a controlled runboo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Cloud Function triggered by Storage events, read the file line-by-line, fetch the latest schema from gs://schemas/telemetry/latest.json, validate fields, write valid lines to gs://partner-teleme",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline: Cloud Function validates mime type, creates a Cloud Tasks task to invoke a Cloud Run resize endpoint with payload {path,size}. Cloud Run reads image from gs://live-uploads/, writes 256x256.jp",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Active-active DR: replicate Pub/Sub to us-central1 and us-east4; run Dataflow streaming jobs in both regions with exactly-once sinks to regional BigQuery datasets; use a Cloud Storage replay buffer fo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical familiarity with event-driven GCP components (Cloud Functions, Cloud Tasks, Cloud Run) and basic dataflow between storage and analytics. It also probes idempotency, IAM least privilege, and retry strategies.\n\n## Key Concepts\n- Event-driven ingestion and validation in Cloud Functions\n- Cloud Tasks as a reliable, retryable trigger for Cloud Run\n- Serverless image processing in Cloud Run with GCS I/O\n- Deterministic naming and idempotency via insertId\n- Minimal IAM configuration and proper service accounts\n\n## Code Example\n```javascript\n// Cloud Function (Node.js) outline\nexports.enqueueResize = async (event, ctx) => {\n  const path = event.name;\n  if (!path.endsWith('.jpg') && !path.endsWith('.png')) return;\n  // validate mime and enqueue task to Cloud Tasks\n  await tasksClient.createTask({ payload: { path, size: 256 } });\n};\n```\n\n## Follow-up Questions\n- How would you test idempotency if retries occur at different stages?\n- What monitoring would you add to observe end-to-end latency and failures?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-7",
        "questionId": "q-19431",
        "question": "Design an end-to-end GCP streaming pipeline for real-time fraud detection. Events arrive on Pub/Sub topic transactions.events as JSON with transaction_id, user_id, amount, currency, ts, and risk_score. Build a Dataflow (Beam) job that validates the schema, deduplicates by transaction_id, applies 1-minute fixed windows per user_id to compute per-user count, total amount, and average. Write results to BigQuery analytics.user_transactions with insertId = transaction_id. Route bad records to an error Pub/Sub topic via dead-letter, and alert on error rate. Include minimal IAM bindings and a concrete test plan. Explain trade-offs Dataflow vs BigQuery streaming for this workload?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a multi-tenant SaaS logs pipeline with Pub/Sub topics per tenant, a Dataflow streaming job that deduplicates by log_id, writes to per-tenant partitioned BigQuery tables, and stores raw JSON",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a streaming telemetry pipeline: devices send JSON to Pub/Sub with exactly-once delivery; a Dataflow (Beam) job deduplicates on event_id, windows by 1 minute, computes per-device_type sums, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Dataflow (Beam) streaming job that subscribes to Pub/Sub transactions.events, validates JSON schema, deduplicates by transaction_id, and uses 1-minute fixed windows by user_id to compute per-",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Pub/Sub for regional events, Dataflow streaming job with guaranteed exactly-once via watermarking and insertId for BigQuery, and cross-region replication with CMEK. Use region metadata in analytic",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses real-time correctness, deduplication, windowing strategy, error paths, and operational concerns for large-scale GCP data pipelines.\n\n## Key Concepts\n\n- Pub/Sub dead-lettering and alerting\n- Beam stateful deduplication by transaction_id\n- 1-minute tumbling windows per user_id with per-window aggregates\n- Idempotent BigQuery writes via insertId\n- Least-privilege IAM for Pub/Sub and BigQuery\n\n## Code Example\n\n```javascript\n// Pseudo-Beam-style sketch (illustrative, not runnable)\n\"\"\" \n// 1) Read from Pub/Sub\n// 2) Validate JSON schema\n// 3) Stateful dedupe on transaction_id\n// 4) Window by user_id, 1-min FixedWindows\n// 5) Compute count, sum(amount), avg(amount)\n// 6) Write to BigQuery with insertId = transaction_id\n\"\"\"\n```\n\n## Follow-up Questions\n\n- How would you test idempotency and dedup at scale?\n- How would you adjust for late data and watermark skew in this pipeline?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-8",
        "questionId": "q-3014",
        "question": "Design a beginner-friendly privacy-conscious ingestion workflow on GCP for CSV analytics logs. When a CSV file lands in gs://analytics-logs/csv/, a Cloud Function validates the header and redacts PII fields (emails and phone numbers) in every row, writing a sanitized file to gs://redacted-logs/csv/ with the same name. A second Cloud Function loads the sanitized file into a date-partitioned BigQuery table analytics.logs, using a deterministic loadId for idempotency. Outline IAM least privilege, a basic test plan with sample input including PII, and how you would verify redaction and idempotent loads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two Cloud Functions: the first validates the CSV header, redacts PII (emails and phone numbers) using regex patterns on every row, then writes the sanitized file to gs://redacted-logs/csv/ with the same filename. The se…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a Cloud Function triggered by finalize events on gs://events/raw/. For each line: mask email to first character + '***@redacted.com', hash user_id with SHA-256 hex, and emit sanitized JSONL",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy new revision in Cloud Run using canary: assign 10% traffic to the new revision and 90% to the stable one; set a Cloud Monitoring alert for error rate >2% over 5 minutes; on alert, automatically",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a Dataflow streaming pipeline: Pub/Sub -> Dataflow -> BigQuery per-tenant partitions; DLP redaction and per-tenant tokenization via envelope encryption with CMEK; enforce IAM conditions and se",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical data sanitization, event-driven processing, and BigQuery loading under real-world constraints.\n\n## Key Concepts\n- Cloud Functions triggered by Cloud Storage events\n- PII redaction with regex patterns and data privacy fundamentals\n- Idempotent loads using deterministic IDs\n- Least-privilege IAM and minimal security surface area\n- End-to-end test planning with sample inputs\n\n## Code Example\n```javascript\nfunction redactRow(row) {\n  row.email = '[REDACTED_EMAIL]';\n  row.phone = '[REDACTED_PHONE]';\n  return row;\n}\n```\n\n## Follow-up Questions\n- How would you test idempotency for reprocessing the same file?\n- What monitoring and alerting would you implement for this workflow?\n- How would you handle schema evolution in the CSV files?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-9",
        "questionId": "q-25514",
        "question": "Design an advanced, real-time, multi-tenant event ingestion pipeline on Google Cloud: each tenant emits events to Pub/Sub; build a streaming Dataflow (Beam) job that deduplicates via insertId tenantId-eventId, writes to a partitioned BigQuery table (partition by day, with tenant_id), supports per-tenant IAM isolation, handles schema evolution via a registry in Cloud Storage, and includes DLQ handling and observability. Describe data model, failure modes, and testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement an HTTP Cloud Function that reads the canary config from Firestore, hashes user_id with SHA-256, computes bucket = hash % 100, and returns enabled when bucket < percent; otherwise di",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Pipeline design: region logs -> Cloud Function publishes to central Pub/Sub; Dataflow streaming reads from Pub/Sub, deduplicates by logId, computes 1-minute fixed windows with 2-minute lateness, and w",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest with Pub/Sub per tenant; Dataflow streaming (Beam) enforces exactly-once, writes to BigQuery partitioned by date and tenant. Use insertId = tenantId+eventId to de-duplicate and a shared dataset",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design and describe a secure, multi-tenant telemetry ingest pipeline on GCP for a fintech app; events arrive to gs://telemetry-raw/{tenant}/{date}/; a Cloud Function publishes to Pub/Sub; a Dataflow s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design real-time, multi-tenant data pipelines with exactly-once processing, schema evolution, IAM isolation, and observability.\n\n## Key Concepts\n- Pub/Sub ingestion, Dataflow/Beam streaming, BigQuery partitioning\n- Duplicate suppression via insertId, idempotent writes\n- Schema evolution and storage-based registry\n- DLQ, IAM least- privilege, monitoring, DR\n\n## Code Example\n```python\n# Beam skeleton (pseudocode)\nimport apache_beam as beam\nwith beam.Pipeline(...) as p:\n  (p\n   | 'Read' >> beam.io.ReadFromPubSub(topic='projects/...')\n   | 'Parse' >> beam.Map(parse_event)\n   | 'Deduplicate' >> beam.Deduplicate()\n   | 'WriteBQ' >> beam.io.WriteToBigQuery('dataset.table', insert_disposition='WRITE_APPEND', create_disposition='CREATE_IF_NEEDED'))\n```\n\n## Follow-up Questions\n- How would you test exactly-once guarantees in a sandbox?\n- How would you handle tenant quota and shard key selection?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-10",
        "questionId": "q-27036",
        "question": "Design a beginner-friendly GCP pipeline: mobile telemetry arrives on Pub/Sub topic telemetry_raw; implement a Cloud Function to validate JSON schema (fields: userId, event, ts), redact PII (emails, phones) using Cloud DLP, publish sanitized events to telemetry_valid Pub/Sub, and load into BigQuery daily_partitioned.sanitized_events with insertId derived from messageId for idempotency. Keep IAM minimal and include a smoke test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Dataflow streaming job reading Pub/Sub, key by tenant_id, apply 1h fixed windows, and emit per-tenant revenue to BigQuery with insertId = tenant_id|window_start to guarantee idempotence. Enforce tenant isolation t…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deploy a real-time fraud pipeline on GCP: stream transactions to Pub/Sub; Dataflow streaming processes features and calls Vertex AI for scoring; write results to BigQuery and fire alerts via Cloud Fun",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Cloud Function triggered by Pub/Sub messages on the telemetry_raw topic. For each message, validate the JSON schema for required fields (userId, event, ts). Upon validation success, invoke Cloud DLP to redac…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Outline a Dataflow streaming pipeline: Pub/Sub -> Dataflow -> BigQuery per-tenant partitions; DLP redaction and per-tenant tokenization via envelope encryption with CMEK; enforce IAM conditions and se",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical GCP service integration with minimal friction, demonstrates basic data privacy handling via DLP, and emphasizes idempotent writes and testing in a beginner-friendly workflow.\n\n## Key Concepts\n- **Pub/Sub**: Event-driven message processing with automatic retries\n- **Cloud Functions**: Serverless trigger for processing telemetry events\n- **Cloud DLP**: API-based PII detection and redaction (emails, phones)\n- **BigQuery**: Daily partitioned tables with insertId for exactly-once semantics\n- **IAM**: Principle of least privilege with minimal role assignments\n\n## Code Example\n```javascript\nconst { BigQuery } = require('@google-cloud/bigquery');\nconst { DLP } = require('@google-cloud/dlp');\n\nconst bigquery = new BigQuery();\nconst dlp = new DLP();\n\nconst SCHEMA = ['userId', 'event', 'ts'];\n\nasync function validateSchema(data) {\n  return SCHEMA.every(field => data[field] !== undefined);\n}\n\nasync function redactPII(data) {\n  const item = { stringValue: JSON.stringify(data) };\n  const infoTypes = [{ name: 'EMAIL_ADDRESS' }, { name: 'PHONE_NUMBER' }];\n  const deidentifyConfig = { infoTypeTransformations: { transformations: [{ infoTypes, primitiveTransformation: { replaceWithInfoTypeConfig: {} } }] } };\n  const [result] = await dlp.deidentifyContent({ parent: process.env.PROJECT_ID, item, deidentifyConfig });\n  return JSON.parse(result.item.stringValue);\n}\n\nexports.onTelemetryRaw = async (message) => {\n  const data = JSON.parse(message.data.toString());\n  if (!await validateSchema(data)) throw new Error('Invalid schema');\n  const sanitized = await redactPII(data);\n  const insertId = message.publishTime + '_' + message.messageId;\n  await bigquery.dataset('daily_partitioned').table('sanitized_events').insert([{ ...sanitized, insertId }]);\n  return { status: 'success' };\n};\n```\n\n## Smoke Test Plan\n1. Publish test message to telemetry_raw with known PII (test@example.com, 555-1234)\n2. Verify function executes without errors\n3. Query BigQuery: SELECT * FROM daily_partitioned.sanitized_events WHERE userId = 'test_user'\n4. Confirm email/phone fields are redacted (replaced with [EMAIL_ADDRESS] / [PHONE_NUMBER])",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-11",
        "questionId": "q-5411",
        "question": "Design a beginner-friendly GCP data-redaction pipeline: when a CSV lands in gs://datasets/raw/, a Cloud Function triggers on finalization, applies a simple DLP redaction to mask PII in key columns, writes the redacted copy to gs://datasets/redacted/ with the same filename, and logs a row to BigQuery with insertId = filePath+timestamp. Keep IAM minimal, implement a retry policy, and describe a basic test plan with sample files and how you verify idempotency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Dataflow streaming template that subscribes to per-tenant Pub/Sub, masks sensitive fields with DLP, and writes to a tenant-partitioned BigQuery table using insertId (e.g., event_id) for id",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Cloud Function triggers on finalization of CSVs in gs://datasets/raw/. It applies a simple DLP redaction to mask PII in key columns, writes the redacted copy to gs://datasets/redacted/ with the same f",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a single Pub/Sub topic carrying tenantId; a Dataflow streaming pipeline (Beam) deduplicates by insertId, writes to per-tenant BigQuery partitions (dataset/tablename + ingestion date). Enforce stri",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement with a Storage Finalize trigger. Validate header ids present: id,name,timestamp; ensure size <= 50MB and .csv extension; dedupe by filename per date; on success, atomically copy to processed",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical use of Cloud Functions, Storage triggers, DLP redaction basics, BigQuery logging, and idempotency via insertId, all in a beginner context with minimal IAM and retry considerations.\n\n## Key Concepts\n\n- Cloud Functions finalize triggers\n- Cloud DLP redaction basics\n- GCS path handling and idempotent writes\n- BigQuery insertId for deduplication\n- IAM least privilege and retry policies\n\n## Code Example\n\n```javascript\n// Node.js skeleton for the Cloud Function\nconst {Storage} = require('@google-cloud/storage');\nexports.redactCsv = functions.storage.object().finalize(async (object) => {\n  // fetch, redact, write redacted, log to BigQuery\n});\n```\n\n## Follow-up Questions\n\n- How would you test redaction correctness and completeness?\n- How would you scale if multiple files arrive concurrently?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-12",
        "questionId": "q-2365",
        "question": "Design a multi-tenant data ingestion and analytics pipeline on GCP for three customers (Snap, Nvidia, Zoom). Each tenant streams events to Pub/Sub, which Dataflow consumes and writes to a central BigQuery data lake with per-tenant isolation using CMEK-encrypted Cloud Storage staging and BigQuery Authorized Views/Row-Level Security. Include idempotent processing, audit logging, cost controls, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Pub/Sub, process with Dataflow to a central BigQuery lake, enforce tenant isolation with CMEK-encrypted Cloud Storage staging and per-tenant Authorized Views/row-level security in BigQuery.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Nightly cleanup: Cloud Scheduler triggers a Cloud Function that scans gs://user-uploads/temp for files older than 7 days, copies each to gs://archive/YYYY/MM/DD/ with the original name, deletes the source file, and logs…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a pipeline using Cloud Storage, Cloud Functions (Python) triggered on object finalize, a BigQuery table for daily health rows, and a quarantine bucket for invalid files. Emphasize idempotent w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Pub/Sub → Dataflow Streaming → BigQuery with insertId = transaction_id for idempotence; route malformed messages to a dead-letter Pub/Sub; add hourly aggregates and robust monitoring. Compare Data",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes ability to design scalable, secure, multi-tenant data pipelines on GCP, balancing isolation, cost control, and reliability in real-world enterprises.\n\n## Key Concepts\n- Pub/Sub and Dataflow streaming, exactly-once or dedup strategies\n- BigQuery partitioning, per-tenant isolation with Authorized Views and row-level security\n- CMEK for Cloud Storage staging, IAM least privilege\n- Audit logging, monitoring, cost controls, testing strategies\n\n## Code Example\n```javascript\n// Deduplication helper (conceptual)\nfunction isDuplicate(event, seen) {\n  const key = event.event_id + '|' + event.event_time;\n  if (seen.has(key)) return true;\n  seen.add(key);\n  return false;\n}\n```\n\n## Follow-up Questions\n- How would you validate idempotency in production?\n- What rollback strategy for failed batches might you implement?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-13",
        "questionId": "q-13418",
        "question": "Design an end-to-end, real-time content moderation pipeline on GCP: new images uploaded to gs://live-content/ trigger a Cloud Function that validates format/size and publishes to Pub/Sub; a Dataflow streaming job reads messages, calls Vertex AI for classification, writes results to BigQuery, and moves images to gs://moderated/ or gs://moderated/rejected/ with deterministic names. Include idempotent writes, a dead-letter Pub/Sub topic, exponential backoff retries, minimal IAM bindings, and a concrete test plan with sample data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a multi-tenant telemetry pipeline on GCP: each tenant publishes to a tenant-scoped Pub/Sub topic; a Dataflow streaming job normalizes events, enriches with tenant metadata from Cloud Datasto",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement: a Cloud Function to validate the JSON line, ensure required fields (event_id, device_id, timestamp, locale, metrics), redact PII, and publish to a locale-specific Pub/Sub topic like",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Trigger: Cloud Function on finalization of gs://reports/raw/*.csv. Validate header matches expected columns, e.g., id,timestamp,value. Convert to Parquet with PyArrow, store at gs://reports/processed/",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a GCP real-time moderation pipeline: trigger a Cloud Function on uploads to gs://live-content/, publish a message to Pub/Sub, process with Dataflow streaming, call Vertex AI for classification,",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a real-time, fault-tolerant pipeline across Cloud Functions, Pub/Sub, Dataflow, and Vertex AI, with idempotency and DLQ.\n\n## Key Concepts\n- Event-driven architecture, idempotent sinks, DLQ, retries\n- Least-privilege IAM, service accounts\n- Observability: logging, Dataflow metrics, BigQuery tables\n\n## Code Example\n```python\n# Pseudocode: Dataflow DoFn invoking Vertex AI with idempotent write\nclass ClassifyDoFn(DoFn):\n    def process(self, element, *args, **kwargs):\n        # call Vertex AI for classification\n        yield classification_result\n```\n\n## Follow-up Questions\n- How would you test idempotency across retries?\n- How do you monitor cost and latency in this pipeline?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-14",
        "questionId": "q-9484",
        "question": "Design an event-driven pipeline: Pub/Sub receives user events; a Dataflow streaming job deduplicates by event_id using state and watermarks, writes to BigQuery (partitioned), and archives raw JSON to Cloud Storage. Create a Data Catalog entry for the dataset with tags; replicate outputs to us-central1 and europe-west1 runners. Apply least-privilege IAM, dead-lettering, and a test plan with replay checks and sample events?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a streaming pipeline: Pub/Sub -> Dataflow. Deduplicate by event_id within a 24h window using state; write to a partitioned BigQuery table and archive the original JSON to Cloud Storage. Crea",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "**Streaming Pipeline Architecture:** **Ingestion (Pub/Sub):** - Topic subscription with exactly-once delivery enabled (`enable_exactly_once_delivery=true`) - Schema validated against registered Protobuf schema registry…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Pub/Sub for regional events, Dataflow streaming job with guaranteed exactly-once via watermarking and insertId for BigQuery, and cross-region replication with CMEK. Use region metadata in analytic",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Dataflow streaming reads from Pub/Sub across regions, deduplicates by event_id via a stateful DoFn, and writes to BigQuery telemetry.events using insertId to guarantee idempotence. Implement watermark",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical streaming pipeline design, dedup, idempotency, cross-region replication, and governance integration.\n\n## Key Concepts\n- Pub/Sub exactly-once/dedup strategies\n- Dataflow streaming with stateful processing\n- BigQuery partitioning and streaming ingestion\n- Data Catalog tagging and governance\n- Cloud Storage archival and backups\n- IAM least privilege and access controls\n- Dead-lettering and replay testing\n\n## Code Example\n```javascript\n// Pseudo-code: stateful dedup per event_id with a window; write to BigQuery and Storage\n```\n\n## Follow-up Questions\n- How would you handle schema evolution in BigQuery table schemas?\n- How would you validate idempotency with replayed events?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-15",
        "questionId": "q-16932",
        "question": "Design a beginner-friendly GCP pipeline to ingest real-time logs from Pub/Sub into BigQuery. The Pub/Sub messages are JSON objects with fields: service, level, timestamp, message. Build a Cloud Function triggered by Pub/Sub that validates required fields, drops invalid messages, deduplicates by a composite key, and writes valid rows to BigQuery using streaming inserts (insertId = composite key). Include a minimal IAM setup and a basic test plan with duplicates and corrupted messages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger: Cloud Function on finalization of gs://reports/raw/*.csv. Validate header matches expected columns, e.g., id,timestamp,value. Convert to Parquet with PyArrow, store at gs://reports/processed/",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a real-time pipeline with Pub/Sub -> Dataflow (Beam) -> BigQuery. Ingest JSON events into Pub/Sub, Dataflow enriches using a cached profile lookup, deduplicates by event_id, and writes to a date-p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Cloud Function triggered by Pub/Sub that validates required fields, drops invalid messages, compute a dedupe key from service+timestamp+hash(message) and use it as insertId for BigQuery st",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Dataflow (Beam) nightly: read gs://tenant-data per-tenant, run DLP tagging/redaction, enrich with metadata, write per-tenant summaries to BigQuery (insertId=tenantId+date). Drift checks against centra",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of real-time data ingestion on GCP, basic data validation, idempotency via insertId, and integration between Pub/Sub and BigQuery using a simple Cloud Function. Emphasizes low-ops design, immutable inserts, and basic testing.\n\n## Key Concepts\n\n- Pub/Sub triggers and message acknowledgement\n- JSON validation and field presence\n- De-duplication strategy with insertId\n- BigQuery streaming inserts and insertId semantics\n- Minimal IAM roles and test planning\n\n## Code Example\n\n```javascript\n// Example Cloud Function skeleton (Node.js)\nconst {BigQuery} = require('@google-cloud/bigquery');\nconst crypto = require('crypto');\nconst bigquery = new BigQuery();\n\nexports.incomingLogs = async (message, context) => {\n  let data;\n  try {\n    data = JSON.parse(Buffer.from(message.data, 'base64').toString());\n  } catch (e) {\n    console.error('Invalid JSON');\n    return;\n  }\n  const required = ['service','level','timestamp','message'];\n  if (!required.every(k => data.hasOwnProperty(k))) {\n    console.error('Missing fields');\n    return;\n  }\n  const sign = data.service + data.timestamp + data.message;\n  const insertId = crypto.createHash('md5').update(sign).digest('hex');\n  const row = {\n    insertId,\n    service: data.service,\n    level: data.level,\n    timestamp: data.timestamp,\n    message: data.message\n  };\n  await bigquery.dataset('analytics').table('logs').insert(row);\n};\n```\n\n## Follow-up Questions\n\n- How would you handle schema evolution and new fields?\n- How would you monitor failure rates and poison messages?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-16",
        "questionId": "q-21819",
        "question": "Design an advanced GCP real-time telemetry pipeline for connected vehicles with regional Pub/Sub, Dataflow streaming, and multi-region storage. Ingest events to Pub/Sub per region, deduplicate by event_id, handle schema evolution, compute a rolling 5-minute risk score, store raw Parquet to GCS partitioned by date, and write metrics to BigQuery with insertId. Enforce CMEK, VPC Service Controls, least-privilege IAM, and include a replay and drift test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use region-scoped Pub/Sub topics per tenant, a Dataflow streaming job with native exactly-once semantics, deduplicating by eventId and tenantId, and persisting to partitioned per-tenant BigQuery datas",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Advanced GCP real-time telemetry pipeline design: vehicles emit events to Pub/Sub per region at 1–10 MB/s. Implement Dataflow streaming that handles schema evolution, deduplicates by event_id, compute",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a Dataflow streaming pipeline that reads events from Pub/Sub, deduplicates by event_id, and writes to BigQuery with insertId=event_id to ensure idempotence. JSONL lands in Cloud Storage, publishin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement an HTTP Cloud Function that reads the canary config from Firestore, hashes user_id with SHA-256, computes bucket = hash % 100, and returns enabled when bucket < percent; otherwise di",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes streaming correctness, data governance, and security under real-time load.\n\n## Key Concepts\n- Streaming Dataflow with Pub/Sub input and BigQuery sink\n- Schema evolution handling and deduping by event_id\n- Parquet cold storage in GCS with date partitioning\n- CMEK encryption, VPC Service Controls, IAM least privilege\n- Replay and drift testing in production-like cadence\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nimport json\n\ndef parse(element):\n  return json.loads(element.decode('utf-8'))\n\np = beam.Pipeline(options=PipelineOptions())\n(p | 'Read' >> beam.io.ReadFromPubSub(topic='projects/org/topics/telemetry')\n   | 'Parse' >> beam.Map(parse)\n   | 'Dedup' >> beam.Distinct(lambda e: e['event_id'])\n   | 'Window' >> beam.WindowInto(beam.window.SlidingWindows(300, 60))\n   | 'Score' >> beam.ParDo(lambda e: e.update({'risk_score': 0.0}) or e)\n   | 'ToBQ' >> beam.io.WriteToBigQuery('project:dataset.telemetry_metrics', insert_id_fn=lambda e: e['event_id'] ))\n\np.run()\n```\n\n## Follow-up Questions\n- How would you implement schema drift detection and rollback?\n- What are your approaches to ensure end-to-end exactly-once semantics across Pub/Sub, Dataflow, and BigQuery?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-17",
        "questionId": "q-27685",
        "question": "Design a beginner-friendly GCP workflow to support tenant-scoped upserts from CSV uploads: partner places gs://tenant-uploads/YYYY/MM/DD/file.csv with columns tenant_id, record_id, value. A Cloud Function validates schema, computes an insertId per row, and streams to a staging BigQuery table. A Cloud Scheduler triggers a MERGE into the production table on (tenant_id, record_id). Include minimal IAM bindings, idempotency, and a basic smoke test?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a streaming Dataflow template that subscribes to region-local Pub/Sub topics, processes per-tenant events with fixed windows, and writes to per-tenant BigQuery datasets in the same region using CM",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe a concrete architecture: Pub/Sub topics per region with tenant routing; a Dataflow streaming job that uses per-tenant throttling, fixed 1‑minute windows, and watermarking; sink to per-tenant",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a real-time multi-tenant event pipeline on GCP: Pub/Sub ingress, Dataflow streaming processing, BigQuery sink. Enforce per-tenant quotas and data residency with regional per-tenant datasets, guarantee exactly-onc…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Cloud Function triggered by gs://tenant-uploads finalize to validate schema and normalize data. For each row compute insertId = tenant_id|record_id|date and stream to a staging BigQuery table. A",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests end-to-end data upserts, idempotency, and using Cloud Scheduler to control merges, which isn't covered by the existing. It emphasizes minimal IAM and testing.\n\n## Key Concepts\n- Cloud Functions with Cloud Storage triggers\n- BigQuery streaming inserts and insertId\n- BigQuery MERGE for upserts\n- Cloud Scheduler and idempotent processing\n- Least privilege IAM\n\n## Code Example\n```javascript\n// Example Cloud Function skeleton (Node.js)\nexports.processCsv = async (event, context) => {\n  // read row, validate\n  // compute insertId = tenant_id|record_id|date\n  // stream to staging BigQuery with insertId\n  // ...\n}\n```\n\n## Follow-up Questions\n- How would you handle schema drift and validation failures at scale?\n- What monitoring would you add to detect stalled merges or repeated failures?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-18",
        "questionId": "q-4267",
        "question": "Design a secure, cost-aware telemetry ingestion pipeline for multi-tenant data on GCP. Inbound JSON logs arrive in gs://telemetry-raw and include tenant_id and event_id. Build an end-to-end flow with Cloud Functions (finalize), Pub/Sub, Dataflow, and BigQuery that isolates tenants, ensures idempotent processing (insertId = tenant_id|event_id|ts), supports schema evolution, and quarantines invalid events. Include a minimal IAM policy and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Cloud Function triggered on gs://telemetry-raw finalize to publish each event to Pub/Sub telemetry.events. Dataflow streaming reads Pub/Sub, validates against a registry-defined schema, and writ",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a cost-aware, event-driven disaster recovery pipeline on GCP for a data analytics platform. Data lands in gs://analytics-raw, where a Dataflow streaming job processes it and writes to BigQuery in the primary regi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt Pub/Sub with event_id as the dedupe key; implement a Dataflow (Beam) streaming job using a stateful DoFn to deduplicate, applying 5-minute fixed windows; write to analytics.events_raw with inser",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Route region-specific events to gs://region-logs/{region}/, where a Cloud Function validates payloads, redacts PII using a library like google-data-masking, and publishes sanitized records to a region-scoped Pub/Sub top…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests designing end-to-end data ingestion with tenant isolation, idempotency, schema evolution, and operational guards in GCP.\n\n## Key Concepts\n- Cloud Functions triggers on GCS\n- Pub/Sub and dead-letter topics\n- Dataflow streaming and BigQuery writes\n- insertId-driven idempotency and MERGE\n- Schema evolution and registry\n- ACLs and least privilege\n\n## Code Example\n```javascript\n// pseudo-code for insertId and MERGE\n```\n\n## Follow-up Questions\n- How would you handle schema drift across tenants?\n- How would you verify idempotency in production?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-19",
        "questionId": "q-20915",
        "question": "Design a cross-region, multi-tenant data ingestion and analytics pipeline on GCP. Events published to Pub/Sub in us-central1 must be validated, deduplicated per tenant, and written to per-tenant partitioned BigQuery tables via Dataflow. Raw events should land in Cloud Storage with a replay manifest. Include dead-letter handling, idempotent upserts via insertId, IAM, monitoring, and a plan for cross-region failover to us-east1?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Dataflow streaming (Apache Beam) reading Pub/Sub region topics, validate JSON, deduplicate by event_id via per-key state, apply 1-minute fixed windows to compute per-account anomaly scores, and wr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a streaming Dataflow job subscribing to Pub/Sub, a Beam DoFn validates against a schema, and writes to BigQuery with insertId = event_id for idempotence. Simultaneously emit Parquet into gs://tele",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a streaming Dataflow Flex template that subscribes to Pub/Sub in us-central1, validates incoming Avro/JSON payloads, and deduplicates events per tenant using the combination of tenant_id and event_id. Write to per-t…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a streaming cost anomaly detector: Pub/Sub ingest of usage events; Dataflow streaming job deduplicates by usage_id, computes hourly per-tenant cost, and queries Vertex AI for expected spend. C",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to design robust, scalable, region-aware data pipelines with cross-region failover, multi-tenancy isolation, and exactly-once semantics. It also evaluates knowledge of dead-letter handling, replayability, IAM best practices, monitoring strategies, and disaster recovery planning.\n\n## Key Concepts\n\n- **Streaming Ingestion**: Pub/Sub to Dataflow for real-time event processing\n- **Multi-Tenancy**: Per-tenant partitioning in BigQuery with insertId for idempotent upserts\n- **Data Archival**: Raw event persistence in Cloud Storage with a replay ledger for recovery scenarios\n- **Exactly-Once Semantics**: Using insertId in BigQuery to handle duplicates and ensure data integrity\n- **Cross-Region Failover**: Pub/Sub mirroring to replicate messages between regions\n- **Observability**: Cloud Monitoring for latency, error rates, and backlog metrics",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-20",
        "questionId": "q-14758",
        "question": "Design a beginner-friendly GCP workflow to rotate API keys stored in Secret Manager for a mobile app. Use Cloud Scheduler to trigger a Cloud Function that creates a fresh key version, updates a Cloud Run service's access to the new key, and logs the rotation event to BigQuery with insertId = keyName+date. Include minimal IAM bindings, a smoke-test plan, and rollback considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable billing export to BigQuery and set a daily Cloud Scheduler to trigger a Python Cloud Function. The function queries last 24h spend by project, computes a moving-average (14 days) directly in Bi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest events from per-tenant Pub/Sub topics with tenant_id in headers; a Dataflow streaming job deduplicates by event_id, writes raw JSON to gs://telemetry/raw/{tenant}/{date}/, and loads enriched ro",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest login events via Pub/Sub, execute a Dataflow (Apache Beam) streaming pipeline with 5-minute fixed windows to compute per-user risk scores from features including IP address, device fingerprint, geographic region,…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Secret Manager to store API keys; Cloud Scheduler triggers a Cloud Function that creates a new secret version, then updates Cloud Run service to use the new key (env var or volume). Log a BigQuery",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical, beginner-friendly rotation workflow using GCP primitives, IAM minimalism, and observability.\n\n## Key Concepts\n- Secret Manager secret versioning\n- Cloud Scheduler + Cloud Functions\n- Cloud Run environment bindings\n- BigQuery logging and insertId strategy\n\n## Code Example\n```javascript\n// Cloud Function: rotateSecretVersion (pseudo)\nconst {SecretManagerServiceClient} = require('@google-cloud/secret-manager');\nconst {CloudRunClient} = require('@google-cloud/run');\n\nasync function rotate(req, res) {\n  // create new secret version\n  // update Cloud Run env to new version\n  // log to BigQuery\n  res.status(200).send('ok');\n}\n```\n\n## Follow-up Questions\n- How would you implement idempotent retries and a canary test?\n- What are the security implications of automating secret rotation?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-21",
        "questionId": "q-10084",
        "question": "Context: Intermediate GCP cloud engineer role. You run a multi-tenant SaaS on GCP. Tenants send event streams to Pub/Sub using a single ingestion topic with attributes tenant_id and event_id. Design a scalable pipeline to deduplicate per-tenant, persist data into per-tenant BigQuery datasets with residency in region, enforce throughput quotas, and ensure exactly-once processing from Pub/Sub to BigQuery. Include error handling and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest telemetry from Pub/Sub, process with a Dataflow streaming job, partition outputs by region to per-region BigQuery tables, and ensure idempotent writes via insertId. Use VPC Service Controls and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest path uses Storage finalize events to Pub/Sub, then a streaming Dataflow (or Cloud Run) that parses, deduplicates via insertId, and writes to BigQuery with idempotent inserts. Compare Dataflow’s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a streaming Dataflow job consuming Pub/Sub; key events by (tenant_id, event_id) to guarantee idempotent inserts into per-tenant BigQuery datasets. Route data to region-specific datasets for reside",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "A Cloud Function, triggered on Cloud Storage object finalization, receives files uploaded to gs://telemetry/raw/YYYY/MM/DD/. It validates each CSV against a defined schema, normalizes headers to a canonical format, mask…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProbes real-world concerns: multi-tenant isolation, exactly-once processing, residency, and throughput controls.\n\n## Key Concepts\n- Pub/Sub exactly-once-like delivery, Dataflow streaming, BigQuery per-tenant partitioned datasets, insertId deduplication, region routing, dead-letter queues, monitoring.\n\n## Code Example\n```javascript\n// Pseudocode: Dataflow DoFn deduplicates using (tenant_id, event_id)\n```\n\n## Follow-up Questions\n- How to handle late events while preserving exactly-once semantics?\n- How would you scale quotas across many tenants without starving bursts?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-cloud-engineer-q-22",
        "questionId": "q-5782",
        "question": "Design an advanced GCP data lifecycle and on-demand rehydration pipeline for a data lake. Raw data lands in gs://lake/raw/YYYY/MM/DD/*.parquet. Implement: (1) lifecycle rules to transition data to ARCHIVE after 45 days and to COLDLINE after 365 days within the same bucket; (2) a Cloud Run HTTP API that rehydrates a byte-range from a specified object on request and streams it to the client; (3) per-request idempotency with a request_id; (4) IAM conditions restricting access to rehydration endpoints and enabling audit logging of access; (5) a concise test plan with sample objects. How would you approach this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Cloud Function triggered on object finalize in gs://mobile-logs/raw. It streams each line, validates against a fixed JSON schema: {userId:string, ts:int, eventType:string, properties:objec",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub for ingestion, Dataflow (Beam) streaming with a stateful dedup by event_id, writing to analytics.events_YYYYMMDD with insertId to guarantee idempotence. Leverage watermarking and windowing",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a single bucket with Lifecycle rules: after 45 days transition to ARCHIVE, after 365 days to COLDLINE. Cloud Run HTTP API accepts bucket, object, and range; reads the specified byte range via the",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest with Pub/Sub per tenant; Dataflow streaming (Beam) enforces exactly-once, writes to BigQuery partitioned by date and tenant. Use insertId = tenantId+eventId to de-duplicate and a shared dataset",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAdvanced lifecycle, cost-aware storage, and on-demand rehydration tests real-world trade-offs and security controls.\n\n## Key Concepts\n- Storage lifecycle rules and storage classes (ARCHIVE, COLDLINE)\n- Cloud Run HTTP APIs and streaming large objects\n- Byte-range reads from Cloud Storage\n- Idempotency with request_id and replay safety\n- IAM conditions and audit logging for access controls\n\n## Code Example\n```python\nfrom google.cloud import storage\nclient = storage.Client()\nblob = client.bucket('lake').blob('raw/2026/01/01/file.parquet')\nstart, end = 0, 1024\ndata = blob.download_as_bytes(start=start, end=end-1)\n# stream 'data' to HTTP response in Cloud Run\n```\n\n## Follow-up Questions\n- How would you test backpressure and large-range requests?\n- How would you monitor lifecycle rule effectiveness and cost impact?\n- How would you handle partial failures during streaming and retries?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-cloud-engineer-q-23",
        "questionId": "q-17266",
        "question": "Design a beginner-friendly GCP workflow to process gzipped NDJSON activity logs uploaded by a mobile app. The app writes to gs://app-logs/raw/*.gz; a Cloud Function extracts each JSON record, validates a minimal schema (required fields: event_id, user_id, ts, event_type), and publishes them to a Pub/Sub topic; a Dataflow batch job reads Pub/Sub, deduplicates on event_id, and appends to BigQuery table analytics.app_events with insertId = event_id. Ensure idempotency, provide minimal IAM bindings, and a test plan with sample payloads and a duplicate event?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy regional Pub/Sub topics (us-central1, europe-west1) with corresponding Cloud Storage buckets organized by device_id. Implement a Dataflow streaming job that ingests from regional topics, normalizes JSONL payloads…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a scalable, cost-aware GCP streaming pipeline: ingest events via Pub/Sub, process with Dataflow (Beam) in streaming mode using windowed deduplication by event_id, store to a partitioned BigQuer",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "This question assesses advanced multi-tenant architecture skills, focusing on data governance, isolation, and cost optimization in a streaming pipeline context. To answer this comprehensively: **Pipeline Architecture**…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Extract .gz, parse NDJSON, validate event_id/user_id/ts/event_type; publish each record to Pub/Sub; Dataflow batch reads Pub/Sub, deduplicates on event_id, and writes to BigQuery with insertId=event_i",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to wire simple GCP components (Cloud Functions, Pub/Sub, Dataflow, BigQuery) end-to-end with real-world data formats and idempotency requirements. It also probes test plans and IAM setup for least privilege.\n\n## Key Concepts\n- GCS gzipped NDJSON ingestion\n- Minimal schema validation in Cloud Functions\n- Pub/Sub as the decoupled transport\n- Dataflow batch with dedup by event_id and BigQuery insertId for idempotency\n- Minimal IAM bindings and testing strategy\n\n## Code Example\n```javascript\n// Example pseudo- Beam pipeline skeleton for dedup and BigQuery insertId\n```\n\n## Follow-up Questions\n- How would you handle out-of-order events and late data?\n- How would you test at scale and validate idempotency?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-cloud-engineer-q-24",
        "questionId": "q-26823",
        "question": "Design a beginner-friendly GCP ingestion pipeline for IoT sensor data: devices publish JSON lines to Pub/Sub; a Cloud Function validates the schema, drops invalid messages, and writes valid rows to a partitioned BigQuery table using an insertId derived from messageId and device and timestamp to ensure idempotency. Use minimal IAM bindings and include a basic test plan with a simulated duplicate message?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable billing export to BigQuery and set a daily Cloud Scheduler to trigger a Python Cloud Function. The function queries last 24h spend by project, computes a moving-average (14 days) directly in Bi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a streaming Dataflow job subscribing to Pub/Sub, a Beam DoFn validates against a schema, and writes to BigQuery with insertId = event_id for idempotence. Simultaneously emit Parquet into gs://tele",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a serverless data anonymization pipeline using Cloud Functions triggered by Cloud Storage finalize events on gs://raw-data/uploads/{file}. The function reads the CSV file, masks emails and phone numbers using…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Devices publish JSON lines to Pub/Sub. A Cloud Function validates the JSON schema, drops invalid messages, and writes valid rows to a partitioned BigQuery table using an insertId derived from messageI",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests basic streaming ingestion, Pub/Sub, Cloud Functions, and BigQuery interaction; checks ability to implement idempotent writes with insertId; ensures knowledge of minimal IAM and basic test planning.\n\n## Key Concepts\n\n- Pub/Sub ingestion and ordering\n- Cloud Functions for schema validation\n- BigQuery insertId for deduplication\n- IAM minimally scoped\n- Test plan including duplicates and invalid messages\n\n## Code Example\n\n```javascript\nconst {BigQuery} = require('@google-cloud/bigquery');\nconst bigquery = new BigQuery();\n\nexports.ingestSensor = async (message, context) => {\n  const data = Buffer.from(message.data, 'base64').toString();\n  let parsed;\n  try { parsed = JSON.parse(data); } catch (e) { return; }\n  if (!parsed.device || typeof parsed.ts === 'undefined' || typeof parsed.value !== 'number') {\n    return;\n  }\n  const insertId = `${message.id}_${parsed.device}_${parsed.ts}`;\n  await bigquery.dataset('iot').table('readings').insert([Object.assign({}, parsed, {insertId})]);\n};\n```\n\n## Follow-up Questions\n\n- How would you test idempotency and ensure dedup when messages are out of order?\n- How would you adapt this to handle schema evolution changes or late-arriving data?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-05T10:42:16.461Z",
    "version": 1
  }
]