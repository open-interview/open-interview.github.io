[
  {
    "id": "test-react-native",
    "channelId": "react-native",
    "channelName": "React Native",
    "title": "React Native Knowledge Test",
    "description": "Test your understanding of React Native with 25 questions.",
    "questions": [
      {
        "id": "react-native-q-0",
        "questionId": "q-29563",
        "question": "Context: advanced React Native; you’re building a cross‑platform GPU-accelerated charting engine (Metal/Vulkan/OpenGL) for real-time telemetry in a diagnostics app. The module must render up to 1M points at 120fps, streaming data from JS via a TurboModule/JSI bridge. Expose a minimal API: initChart(width,height,opts), pushBatch(points), onFrame(cb), setViewport(x,y,w,h), stopChart. Design the native render loop, multi-buffering, memory management, and backpressure between JS and native threads. Provide a minimal API sketch, usage snippet, and discuss thermal/power, GC impact, and testing on-device?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: run a dedicated native render thread with triple-buffering for vertex data and a lock-free queue to receive pushBatch(point) from JS. Preallocate GPU buffers, store points as fixed-point, and pe",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use FlatList with data filtered by useMemo; renderItem wrapped in React.memo; keyExtractor returns stable ids; implement getItemLayout for fixed-height items; debounce search input to limit re-renders",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a functional ItemFeed with state for data, loading, and refreshing. Use useEffect to fetch from a mock endpoint, FlatList with keyExtractor={(i)=>i.id}, and renderItem for a compact card. Sh",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a TurboModule (JSI) exposing startStream, onFrame, stopStream. Run a native depth-pipeline thread that feeds a reusable frame buffer to a JS callback via a microtask queue. Backpressure via inFlig",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design high-throughput native modules with tight JS<->native coupling and GPU rendering. It probes multi-threading, memory management, and hardware-accelerated rendering choices across iOS and Android.\n\n## Key Concepts\n- TurboModule/JSI bridge and async queues\n- Metal/Vulkan/OpenGL interoperability\n- ringbuffer, triple buffering, lock-free structures\n- backpressure, memory management, GC interaction\n- thermal/power diagnostics and on-device testing\n\n## Code Example\n```javascript\n// Minimal API sketch\ninitChart(800,600,{capacity:1000000});\npushBatch(points); // array of {x,y} with fixed-point encoding\nonFrame(() => { /* render-ready callback */ });\n```\n\n## Follow-up Questions\n- How would you calibrate backpressure thresholds for bursty data?\n- What strategies would you use to verify performance across devices with different GPUs?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-1",
        "questionId": "q-25343",
        "question": "Design a cross-platform React Native permission module that aggregates camera, microphone, and storage permissions behind a single API and streams status changes; implement JS API (checkPermissions, requestPermissions, onStatusChange), sketch iOS (Swift) and Android (Kotlin) bridges, provide a usage snippet, and discuss caching, permission priority, and UX when denied?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a TurboModule with two dedicated native threads: a compute thread running GPU shaders (Metal on iOS, Vulkan on Android) for physics calculations, and a render thread synchronized with the React Native UI at 60…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: expose a RNPermissions module that tracks a PermissionStatus map for camera, microphone, storage. checkPermissions returns current statuses; requestPermissions prompts for missing ones in a",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a TurboModule exposing methods: initTutorial({ steps, startIndex }), next(), prev(), getCurrentStep(), onStepChange(cb). Store currentStep in SharedPreferences/NSUserDefaults for offline per",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a TurboModule running a dedicated performance thread that samples frameDuration, mainThreadIdle, and vsyncOffset at a configurable rate and streams metrics to JavaScript via the onMetrics callback. ## iOS (Swift)…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cross-platform bridges, handle asynchronous permission flows, UX when denied, and keeping state consistent across RN and OS.\n\n## Key Concepts\n\n- Cross-platform RN bridges (RCTBridgeModule) in Swift and Kotlin\n- Unified permission model and statuses\n- UX for denied/blocked permissions and settings redirection\n- Caching results and avoiding redundant prompts\n\n## Code Example\n\n```javascript\nimport { NativeModules, NativeEventEmitter } from 'react-native';\nconst { RNPermissions } = NativeModules;\nconst emitter = new NativeEventEmitter(RNPermissions);\nRNPermissions.checkPermissions(['camera','microphone','storage']).then(set => { /* ... */ });\nRNPermissions.requestPermissions(['camera']).then(res => { /* ... */ });\n```\n\n## Follow-up Questions\n\n- How to handle permission changes when app moves to background?\n- How to test mocked permission flows in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-2",
        "questionId": "q-24885",
        "question": "Context: advanced React Native; you’re building a cross-platform native module that streams camera frames through a GPU shader graph for real-time effects and UI compositing at 60fps. Expose a minimal React Native API: initPipeline(config), pushFrame(data), onProcessed(callback), shutdown. Describe your threading model, memory management, backpressure strategy, and testing under thermal throttling. Include a minimal API sketch and usage snippet, plus trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a TurboModule (JSI) that loads a quantized TF Lite model on iOS/Android, runs inference in a dedicated worker thread, and streams gesture events to JS at ~30 Hz. API: initModel(modelPath, op",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a GPU-accelerated frame pipeline on a dedicated native thread; use a compact ring buffer to hold frames, reuse buffers, and avoid per-frame allocations. Expose initPipeline, pushFrame, onPro",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a cross‑platform TurboModule that streams per‑frame telemetry (input events, layout/measure timings, GPU frame timestamps) at ~100 Hz from a dedicated native sampling thread to JavaScript. Use a ring buffer wi…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a small native module (React Native bridge) exposing `initStore`, `putValue`, `getValue`, `removeValue`, `observeValue`, and `clearStore` methods. Use per-name storage with platform-specific implementations: S…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of native modules, threading, memory mgmt, and performance under thermal constraints; requires concrete API thoughts and testing plan rather than theory.\n\n## Key Concepts\n- Native GPU pipelines in RN\n- Dedicated native threads and memory reuse\n- Backpressure and frame dropping\n- Cross-platform shader integration (Metal/Vulkan)\n- Performance testing & instrumentation\n\n## Code Example\n```javascript\n// Minimal API sketch (TypeScript-like)\ntype PipelineConfig = { width: number; height: number; shaderSet: string; };\nclass PipelineModule { initPipeline(cfg: PipelineConfig): void; pushFrame(data: ArrayBuffer): void; onProcessed(cb: (frame: ArrayBuffer) => void): void; shutdown(): void; }\n```\n\n## Follow-up Questions\n- How handle Metal vs Vulkan capability differences and fallback paths?\n- What policy ensures perceived latency vs quality when frames are dropped?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-3",
        "questionId": "q-22328",
        "question": "Context: beginner React Native; implement a cross-platform persistent key-value store for user preferences via a tiny native bridge. API: setValue(key, value), getValue(key), removeValue(key), onChange(callback). Expose a JS API via a native bridge (iOS and Android) and ensure data persists across restarts, serialize values as JSON, IO on a background thread, and emit changes to JS. Provide a usage snippet and discuss serialization choices and thread-safety?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a native frame pipeline in a separate worker with a shared buffer pool; implement a TurboModule/JSI bridge to expose startPipeline, addFilter, onFrame, finish, cancel. Use platform-optimized ker",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: implement a TurboModule with init(config), start(), stop(), and configure(params: channels, sampleRate, filters). Expose data via an event emitter for frame arrays. Use a fixed-size",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a TurboModule (JSI surface) with a minimal API: createBoard(id), onStroke(stroke, timestamp), flushDeltas(), pause(), resume(), and onUpdate(callback). Use a dedicated native render thread a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: implement a small cross-platform native bridge with a singleton preference store. Persist data in Android SharedPreferences / iOS NSUserDefaults, JSON.stringify values, and expose setValue, getV",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of bridging RN with native code, cross-platform persistence, and data flow from JS to native and back.\n\n## Key Concepts\n- React Native bridge basics\n- Platform storage: SharedPreferences/NSUserDefaults\n- JSON serialization and type fidelity\n- Threading and non-blocking IO\n- Event emission for onChange\n\n## Code Example\n```javascript\nimport {NativeModules, NativeEventEmitter} from 'react-native';\nconst {PreferenceStore} = NativeModules;\nexport const setValue = (k,v)=> PreferenceStore.setValue(k,v);\nexport const getValue = (k, fallback)=> PreferenceStore.getValue(k, fallback);\nexport const removeValue = (k)=> PreferenceStore.removeValue(k);\nexport const addListener = (cb)=> new NativeEventEmitter(PreferenceStore).addListener('change', cb);\n```\n\n## Follow-up Questions\n- How would you handle binary data or non-JSON values?\n- How would you test this across both platforms?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-4",
        "questionId": "q-15131",
        "question": "Develop a cross-platform native module for React Native that streams the device camera feed into a GPU-accelerated compute pipeline (Metal on iOS, Vulkan on Android) to perform real-time background segmentation and optional style transfer, delivering per-frame metadata back to React Native at 60fps. Outline the native API, synchronization model, and a minimal usage snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a local-first cache with a Post[] store and an LRU image cache. Use region-based viewport to fetch pages, throttle to 1 req/sec, and prefetch 1–2 pages ahead. Merge remote updates with per-post revision numbers a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a TurboModule with a dedicated worker thread that reads fused IMU data, runs a quantized model via NNAPI/Metal, and streams results back via a JS callback. Use double buffering, a ring buffe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a native WebRTC bridge where we run the WebRTC stack on a dedicated native thread and expose a tiny JS API: startCall(config), joinRoom(roomId), switchCamera, setBandwidth(limit), onFrame(ca",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a cross-platform native module (VideoRefine) that streams camera frames into a GPU compute pipeline (Metal on iOS, Vulkan on Android) for real-time segmentation and optional style transfer.",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nHardware-accelerated video pipelines on RN require careful threading and memory life-cycle. This question probes API design, cross-platform GPU choices, frame synchronization, backpressure, and lifecycle under power constraints.\n\n## Key Concepts\n- Cross-platform native modules (TurboModule/JSI)\n- GPU pipelines (Metal/Vulkan)\n- Real-time framing, backpressure, and memory management\n- Threading: dedicated compute thread + RN bridge\n- Minimal usage sketch and calibration/testing considerations\n\n## Code Example\n\n```javascript\n// Minimal usage sketch in RN\nimport VideoRefine from 'VideoRefine';\nVideoRefine.start(1280,720,'NV12');\nVideoRefine.setModel('seg+style');\nVideoRefine.processFrame(framePtr, frameSize, ts);\nVideoRefine.stop();\n```\n\n## Follow-up Questions\n\n- How would you test frame drops under UI thread pressure?\n- How would you ensure deterministic 60fps across devices?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-5",
        "questionId": "q-4503",
        "question": "You’re building a beginner-friendly React Native app that shows city events fetched from an API. Implement a local cache with AsyncStorage and a hook useCacheList(endpoint) that loads cached data first, then fetches fresh data, and merges by id while preserving user order. Also handle cache invalidation and duplicates. Provide a minimal hook sketch and a small component example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To implement TagInput, use forwardRef with useImperativeHandle to expose addTag(text), removeTag(index), getTags(), clearTags(). Internally manage tags array, split on commas, trim, dedupe, render chi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build an offline-first image pipeline using a Rust-native module bridged to React Native. Store images locally with SQLite (ImageRecord: id, path, region, metadata, status, lastModified, syncVersion, checksum). The Rust…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To stream 3 sensors at 60 Hz, implement a native thread that samples accelerometer, gyroscope, and barometer into a fixed-size ring buffer, fuses the data on-device using a Kalman filter, and emits 60 Hz batches to JS v…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Load cached events from AsyncStorage under 'events_cache', render it immediately, then fetch fresh data. Merge by id keeping the cached order, replacing with fresh items when ids match, and append any new ids. Handle ca…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests offline caching, data merging by id, and simple hook design for React Native with AsyncStorage.\n\n## Key Concepts\n- AsyncStorage usage for simple persistence\n- useEffect/useState lifecycle and dependency on endpoint\n- Merging lists by id while preserving user-visible order\n- Exposing a small API (data, loading, error, refetch) for components\n\n## Code Example\n```javascript\nimport { useState, useEffect } from 'react';\nimport AsyncStorage from '@react-native-async-storage/async-storage';\n\nexport function useCacheList(endpoint) {\n  const [data, setData] = useState([]);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  useEffect(() => {\n    const loadData = async () => {\n      try {\n        // Load cached data first\n        const cached = await AsyncStorage.getItem(`cache_${endpoint}`);\n        if (cached) {\n          setData(JSON.parse(cached));\n        }\n\n        // Fetch fresh data\n        const response = await fetch(endpoint);\n        const freshData = await response.json();\n\n        // Merge by id, preserving cached order\n        const merged = mergeById(data, freshData);\n        setData(merged);\n\n        // Update cache\n        await AsyncStorage.setItem(`cache_${endpoint}`, JSON.stringify(merged));\n      } catch (err) {\n        setError(err);\n      } finally {\n        setLoading(false);\n      }\n    };\n\n    loadData();\n  }, [endpoint]);\n\n  return { data, loading, error, refetch: () => loadData() };\n}\n\nfunction mergeById(cached, fresh) {\n  const map = new Map();\n  \n  // Preserve cached order\n  cached.forEach(item => map.set(item.id, item));\n  \n  // Update with fresh data\n  fresh.forEach(item => {\n    if (map.has(item.id)) {\n      map.set(item.id, item);\n    } else {\n      // Append new items\n      cached.push(item);\n    }\n  });\n  \n  return Array.from(map.values());\n}\n```\n\n## Component Example\n```javascript\nfunction EventsList() {\n  const { data, loading, error } = useCacheList('/api/events');\n\n  if (loading) return <ActivityIndicator />;\n  if (error) return <Text>Error: {error.message}</Text>;\n\n  return (\n    <FlatList\n      data={data}\n      keyExtractor={item => item.id}\n      renderItem={({ item }) => <EventCard event={item} />}\n    />\n  );\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-6",
        "questionId": "q-13302",
        "question": "In a React Native app for remote telemetry collection, implement a cross‑platform native module to capture high‑frequency events (up to 1000/s), compress them on-device (e.g., LZ4), and expose a minimal API to React Native that yields a ReadableStream‑like interface for consumption on the JS side. Include APIs for start, pause, resume, adjust rate/batchSize, and flush targets (network/disk). Address backpressure, memory budgeting, and lifecycle cleanup?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Rust-native module (JSI/TurboModule) with a single ingest thread and a zero-copy ring buffer feeding JS. Handle 500–1000 updates/sec, batch serialize, and minimize allocations to reduce GC pa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement lazy loading with React.lazy() and Suspense for screen components, use React.memo() for expensive renders, optimize FlatList with getItemLayout and removeClippedSubviews, implement proper cleanup in useEffect…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by requesting mic permissions on both platforms, configure the native audio session, and implement a native thread to read PCM samples (16-bit PCM 44.1kHz). Bridge chunks via an event emitter; b",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a TurboModule with a dedicated native worker and a ring buffer that ingests up to 1000 events/sec, compresses them with LZ4, and exposes a ReadableStream-like API to JS. APIs: start(), pause(),",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests bridging performance, backpressure handling, and end‑to‑end data flow between native code and React Native JS under tight latency constraints.\n\n## Key Concepts\n- TurboModules/JSI integration for low‑overhead bridges\n- Native worker thread + ring buffer for streaming data\n- On‑device compression (e.g., LZ4) and batch sizing\n- ReadableStream‑like JS API with start/pause/resume and flush control\n- Memory budgeting and lifecycle cleanup\n\n## Code Example\n```javascript\n// Usage sketch (JS side)\nconst stream = TelemetryModule.createStream({ maxBufferBytes: 8 * 1024 * 1024 });\nstream.on('data', chunk => sendToServer(chunk));\nstream.start();\nstream.pause();\nstream.resume();\nstream.setBatchSize(512);\nstream.setFlushTarget('net');\n```\n\n## Follow-up Questions\n- How would you validate backpressure behavior under bursty traffic?\n- What metrics would you expose to monitor memory and latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-7",
        "questionId": "q-16757",
        "question": "Context: intermediate React Native; you’re building a cross‑platform native module that measures high‑frequency frame timing data (frame duration, main‑thread stall, vsync offset) on a dedicated native thread and streams metrics to React Native. Provide a minimal API: start(), stop(), setSampleRate(ms), onMetrics(callback). Sketch iOS (Swift) and Android (Kotlin) bindings, a JS usage snippet, and discuss minimizing overhead and lifecycle cleanup?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a native module using platform schedulers (CADisplayLink on iOS, Choreographer on Android) with a small worker that watches battery, thermal state, and per-frame timings. Expose TurboModule",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a TurboModule running a dedicated performance thread that samples frameDuration, mainThreadIdle, and vsyncOffset at a configurable rate and streams metrics to JavaScript via the onMetrics callback. ## iOS (Swift)…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "API: init(config), applyLocalChange(change), onRemoteChange(callback), flush(), stopSync. Execution: dedicated background thread for replication; per-entity CRDT state for conflict-free merging; tombstone garbage collec…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cross‑platform RN native module that streams a high‑rate pose/sensor fusion pipeline to JS using a dedicated C++ thread and a lock‑free ring buffer. Expose startPipeline(config), updateParams",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests a candidate's ability to design native modules that interact with React Native's architecture. It evaluates understanding of:\n\n- **Native threading**: Running frame sampling on a dedicated thread ensures measurements don't skew the very metrics being measured\n- **TurboModule or legacy Native Module patterns**: Candidates should know the modern approach and be able to fall back to legacy if needed\n- **Cross-platform mobile development (iOS and Android)**: The ability to write equivalent implementations in both Swift and Kotlin\n- **Lifecycle management and memory cleanup**: Ensuring native resources are properly released to avoid leaks\n- **JavaScript-native bridge communication patterns**: Understanding how to stream data efficiently from native to JavaScript\n\n## Key Concepts\n\n- **CADisplayLink (iOS)**: A timer object that synchronizes with the display refresh rate, ideal for frame timing measurements\n- **Choreographer (Android)**: Coordinates frame timing callbacks, similar in purpose to CADisplayLink\n- **Thread safety**: When sharing data between native threads and the main thread, use atomic operations or lock-free data structures\n- **Bridge vs. JSI**: The traditional bridge adds serialization overhead; JSI enables direct JavaScript engine access for lower latency",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-8",
        "questionId": "q-26704",
        "question": "Context: beginner React Native; design a cross-platform native module 'FontScaleBridge' exposing getFontScale() and onFontScaleChange(callback). Implement minimal native hooks on iOS (Swift) and Android (Kotlin) to listen to font scale changes and emit events to JS. Show a usage snippet and discuss coupling, lifecycle, and accessibility considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-platform native module called `FontScaleBridge` exposing two methods: `getFontScale()` returns the current system font scale, and `onFontScaleChange(callback)` subscribes to font scale changes, invoking t…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I’d implement a TurboModule with native background workers: Android WorkManager and iOS BackgroundTasks. Data is partitioned into per-record sync jobs with optimistic locking; conflicts resolved by a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implemented a CADisplayLink/Choreographer-based frame timer (FrameSync) that yields a fixed 16.666ms tick, honors timeScale, and supports per-animation priority queues. Uses JSI bridge with a tiny Tur",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed implementation outline: create a small native module ImageCache with JS API: initCache(config), cacheImage(url, {ttl, compress}), getCachedPath(url), clearCache(), onCacheEvent(cb). On iOS: u",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question tests fundamental React Native bridging concepts, cross-platform API design, and awareness of accessibility requirements.\n\n## Key Concepts\n- React Native native modules and the EventEmitter pattern for bidirectional communication\n- Android: Kotlin configuration listeners for observing system settings changes\n- iOS: Swift notification observers for dynamic type updates\n- Accessibility: respecting system font scale to build inclusive UIs\n- Lifecycle management: proper subscription creation and cleanup",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-9",
        "questionId": "q-9479",
        "question": "Design a cross-platform native module (JSI/TurboModule) that streams barcode results from the camera on a dedicated worker. API: useBarcodeStream({ mode:'live', maxBacklog:120 }) => { code, symbology, ts }. Implement a native ring buffer with backpressure that drops oldest when full; emit via a small JS hook. UI: live list with Pause/Resume. Privacy: decode on-device, avoid saving images, and throttle to 60 Hz?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a TurboModule with a dedicated native thread decoding frames; feed results through a JS-visible event emitter. Build a native ring buffer; when full, drop oldest scans to honor maxBacklog. Expo",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a TurboModule-backed analytics sink that buffers events in SQLite, batches by batchSize or timer, and uploads only when online. Anonymize by hashing user IDs, remove PII, and allow optional",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect the bridge as a TurboModule that runs ARKit/ARCore on a dedicated native thread; expose startSession(config), updateOverlay(params), onFrame(callback), and stopSession. Use a lock-free ring",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To meet 120fps determinism, run the engine on a dedicated native thread with fixed 8.3ms timesteps. Use double-buffered native state with a lock-free ring buffer for output. JS API: startEngine(config), step(dt), onFram…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design a responsive bridge between camera input and React Native UI, involving threading, memory management, and privacy controls.\n\n## Key Concepts\n- Native modules (JSI/TurboModule) and threading\n- Backpressure with a ring buffer\n- Event emitters and React hooks\n- On-device processing and privacy trade-offs\n\n## Code Example\n```javascript\nimport { useEffect, useState } from 'react';\nimport { useBarcodeStream } from 'barcode-module';\nfunction Scanner(){\n  const [codes, setCodes] = useState([]);\n  useBarcodeStream({ mode:'live', maxBacklog:120 }).on('barcode', (b)=>setCodes(c=>[...c,b]));\n  return null;\n}\n```\n\n## Follow-up Questions\n- How would you test backpressure under bursty frame rates?\n- How would you extend the API to filter by symbology or implement duplicate suppression?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-10",
        "questionId": "q-17313",
        "question": "Context: React Native AR feature for iOS/Android. Build a cross‑platform native module that renders a 3D UI anchored in world space using ARKit (iOS) and ARCore (Android). Sustain 60fps, enable depth occlusion and light estimation, and degrade gracefully on devices without AR. Provide a minimal native API sketch for both platforms, a JS usage snippet, and discuss synchronization, memory, and lifecycle?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Prototype a Rust-based TurboModule that subscribes to a canvas stroke stream, writes to a fixed-size ring buffer, and a background thread emits stroke deltas at a configurable Hz. Expose useStrokeStream(roomId, { Hz, bu…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a cross-platform native module called `FontScaleBridge` exposing two methods: `getFontScale()` returns the current system font scale, and `onFontScaleChange(callback)` subscribes to font scale changes, invoking t…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a cross‑platform AR bridge using ARKit/ARCore with a TurboModule API: startAR(config), addAnchor(id, pose), updateAnchor(id, pose), removeAnchor(id), onFrame(callback); and a minimal JS interfa",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a TurboModule with a dedicated native render thread and a lock-free ring buffer shared between JS and the GPU path. iOS Metal-backed 2D canvas; Android Vulkan/OpenGL surface. API: startStrea",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a cross‑platform AR bridge, coordinate lifecycles, implement a dedicated render loop, and manage backpressure between native and JS layers.\n\n## Key Concepts\n\n- Cross‑platform AR integration (ARKit/ARCore)\n- TurboModules/JSI bridge for AR data\n- Depth occlusion and light estimation\n- Frame pacing and backpressure between native and JS\n- Graceful degradation on devices without AR hardware\n\n## Code Example\n\n```javascript\n// Minimal usage sketch\nconst AR = require('react-native-ar');\nAR.startAR({mode:'world', planeDetection:true});\nAR.addAnchor('hud', {x:0, y:0, z:-1, rotation:[0,0,0,1]});\nAR.onFrame((frame)=>{ /* render overlays using frame.camera, frame.anchors */ });\n```\n\n## Follow-up Questions\n\n- How would you test AR performance across devices with different sensors?\n- How would you manage memory for a large number of anchors and frames?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-11",
        "questionId": "q-533",
        "question": "How would you optimize a React Native app with 50+ screens that's experiencing slow navigation and memory leaks, particularly on lower-end devices?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a data module with three functions: - **fetchProfiles()**: Checks NetInfo for connectivity. If online, fetches from `/api/profiles`, saves `{ ts: Date.now(), data }` to AsyncStorage under key 'profiles_v1', th…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement lazy loading with React.lazy() and Suspense for screen components, use React.memo() for expensive renders, optimize FlatList with getItemLayout and removeClippedSubviews, implement proper cleanup in useEffect…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a tile-based GPU renderer with a dedicated render thread and a background tile loader. Maintain a fixed-size tile pool and a double-buffered atlas; pass tiles via a bounded queue to the rend",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a BatteryStream native module that uses a NativeEventEmitter to push {level, charging, ts} to JS. Android: register a foreground BatteryChanged listener and sample every intervalMs via a Handle",
            "isCorrect": false
          }
        ],
        "explanation": "## Performance Optimization Strategies\n\n### Code Splitting & Lazy Loading\n- Use React.lazy() for screen components wrapped in Suspense\n- Implement route-based code splitting to reduce initial bundle size\n- Leverage Metro bundler's RAM bundles for improved memory efficiency\n\n### Memory Management\n- Proper cleanup in useEffect hooks to prevent memory leaks\n- Avoid closures in long-lived components that retain references\n- Use WeakMap/WeakSet for large data structures\n- Implement proper event listener cleanup\n\n### List Optimization\n```javascript\n<FlatList\n  data={items}\n  getItemLayout={(data, index) => ({length: ITEM_HEIGHT, offset: ITEM_HEIGHT * index, index})}\n  removeClippedSubviews={true}\n  maxToRenderPerBatch={10}\n  windowSize={10}\n  initialNumToRender={10}\n/>\n```\n\n### Engine & Build Optimizations\n- Use Hermes JavaScript engine for better performance\n- Implement bundle splitting and code optimization\n- Utilize React.memo() and useMemo() for expensive computations\n- Profile and optimize re-renders with React DevTools",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-12",
        "questionId": "q-24024",
        "question": "Context: advanced React Native; you’re building a cross-platform native module for real-time, low-latency, multi-user voice chat with spatial audio in a ride-sharing coordination app. The module runs an audio pipeline on a dedicated native thread, uses a minimal JS API (startEngine(config), joinRoom(roomId, opts), onAudioFrame(cb), setListener(pos, orient), setSpatialParams(params), mute/unmute, stopEngine), and delivers 3D positional audio via HRTF with network transport via WebRTC. Describe architecture, thread coordination with React Native UI, backpressure, power, and testing under Doze/background modes, plus a usage snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Hermes uses ahead-of-time (AOT) compilation to convert JavaScript to optimized bytecode during build time, reducing startup by 30-50% and memory usage by 20-30% compared to JavaScriptCore's just-in-time (JIT) compilatio…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: implement a small cross-platform native bridge with a singleton preference store. Persist data in Android SharedPreferences / iOS NSUserDefaults, JSON.stringify values, and expose setValue, getV",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Tail a log file on a background thread, buffering entries in a fixed-size ring buffer before batching them to JavaScript via the native bridge (e.g., DirectEvent). Use a serial dispatch queue to ensure ordering. Handle…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a dedicated native audio thread at 48kHz with circular buffers and a lightweight AEC/NS path. Bridge to React Native via a minimal JSI API: startEngine(config), joinRoom(roomId, opts), onAudioFram",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\ntests ability to design a real-time audio pipeline with strict latency, cross‑platform native integration, and didactic handling of background modes.\n\n## Key Concepts\n- Real-time native audio thread at 48kHz\n- JSI/TurboModules bridge to JS\n- 3D spatial audio using HRTF\n- Backpressure and frame dropping strategies\n- Doze/background mode testing and power management\n\n## Code Example\n```javascript\n// Implementation sketch\nclass SpatialAudioModule {\n  startEngine(config) {}\n  joinRoom(roomId, opts) {}\n  onAudioFrame(cb) {}\n  setListener(pos, orient) {}\n  setSpatialParams(params) {}\n  mute() {}\n  stopEngine() {}\n}\n```\n\n## Follow-up Questions\n- How would you measure end-to-end latency and jitter?\n- What tests ensure consistent audio quality across Doze and background states?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-13",
        "questionId": "q-2841",
        "question": "You're building a React Native feed that displays images from a CDN with three size variants (thumb, medium, large). Implement an adaptive image loader that selects the best variant based current network speed, device pixel density, and battery saver mode, with graceful fallbacks on flaky networks. Describe data models, caching (SQLite/Realm) strategy, and provide a minimal hook useAdaptiveImage that returns {uri, loading, error} and triggers prefetches. Provide a small code sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design as a TurboModule with a dedicated native render thread (Metal iOS, Vulkan Android). Maintain a double-buffered vertex buffer and a lock-free ring buffer for incoming points; updateData enqueues",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a cross-platform TurboModule that wraps a local SQLite queue for pending changes. Expose a comprehensive JS surface: `startSync(endpoint, { pollMs, backoff }, onRecordSynced)`, `pauseSync()`, `resumeSync()`, `…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a native Box2D-like physics world on a dedicated C++ thread, exposed via TurboModule. API sketch: initWorld(gravity: {x,y}), addBody(id, shape, mass, bodyDef), setGravity, step(dt),",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use NetInfo to estimate bandwidth and battery mode, and PixelRatio to pick a variant (thumb/medium/large). Prefetch non-selected variants and cache the chosen URI with TTL in Realm/SQLite. The hook us",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a responsive image loading strategy that balances UX and data usage, incorporating network state, device capabilities, and offline caching in React Native.\n\n## Key Concepts\n- Variant selection logic using NetInfo, PixelRatio, and battery saver state\n- Local caching strategy (Realm/SQLite) with TTL and cache busting\n- Image loading lifecycle and prefetching to minimize jank\n\n## Code Example\n```javascript\nimport { useEffect, useState, useCallback } from 'react'\nimport NetInfo from '@react-native-community/netinfo'\nimport { PixelRatio } from 'react-native'\n\nfunction useAdaptiveImage(sizes, baseUri) {\n  const [uri, setUri] = useState(sizes.medium)\n  const [loading, setLoading] = useState(true)\n  const [error, setError] = useState(null)\n\n  const resolveVariant = useCallback((info) => {\n    const bandwidth = info.type === 'wifi' ? 1000 : info.details?.speed ?? 500\n    const dpr = PixelRatio.get()\n    const batterySaver = info.batterySaverEnabled\n    let chosen = sizes.medium\n    if (!batterySaver && bandwidth > 800 && dpr >= 2) chosen = sizes.large\n    else if (bandwidth < 400) chosen = sizes.thumb\n    return chosen\n  }, [sizes])\n\n  useEffect(() => {\n    let mounted = true\n    NetInfo.fetch().then(info => {\n      if (!mounted) return\n      const variant = resolveVariant(info)\n      setUri(variant)\n      // pretend fetch/cache logic here\n      setLoading(false)\n    }).catch(err => {\n      if (mounted) { setError(err); setLoading(false) }\n    })\n    return () => { mounted = false }\n  }, [resolveVariant])\n\n  const refresh = useCallback(() => {\n    setLoading(true)\n    NetInfo.fetch().then(info => {\n      const variant = resolveVariant(info)\n      setUri(variant)\n      setLoading(false)\n    }).catch(err => setError(err))\n  }, [resolveVariant])\n\n  return { uri, loading, error, refresh }\n}\n```\n\n## Follow-up Questions\n- How would you validate cache invalidation and handle remote URL changes?\n- How would you test behavior under intermittent connectivity and battery saver toggling?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-14",
        "questionId": "q-4402",
        "question": "You're building a beginner-friendly React Native screen that lists latest articles from a paginated API. The screen auto-refreshes every 30 seconds and supports pull-to-refresh. Ensure that in-flight fetches are canceled when a new fetch starts or the screen unmounts, using AbortController. Provide a small hook useCancelableFetch(url) and show minimal UI wiring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Cancelable fetch with AbortController; keep a ref to the latest request id; on each refresh cancel the previous request and start a new one; clear the interval on unmount; only update state if t",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a cross‑platform native module (TurboModule/JSI) that polls battery level on Android (BatteryManager) and iOS (UIDevice) on a background thread, emitting { level, ts } and a boolean belowThr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use SQLite to store messages locally with fields (id, convoId, text, sender, ts, status). On sync, resolve conflicts by taking the newer timestamp. The useChatSync(conversationId) hook maintains a que",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design as a TurboModule with a dedicated native render thread (Metal iOS, Vulkan Android). Maintain a double-buffered vertex buffer and a lock-free ring buffer for incoming points; updateData enqueues",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical React Native fetch patterns: cancellation prevents stale data and memory leaks during auto-refresh and manual refresh. It also checks hook design and safe state updates in unmount scenarios.\n\n## Key Concepts\n\n- AbortController usage in fetch calls\n- useEffect cleanup and interval management\n- Race condition avoidance with latest-request checks\n- Small custom hook design for reusable cancellation logic\n\n## Code Example\n\n```javascript\nimport { useEffect, useRef, useState } from 'react';\n\nexport function useCancelableFetch(url) {\n  const [data, setData] = useState(null);\n  const [loading, setLoading] = useState(false);\n  const [error, setError] = useState(null);\n  const currentReq = useRef(0);\n\n  useEffect(() => {\n    let isActive = true;\n    const ctrl = new AbortController();\n    const id = ++currentReq.current;\n    setLoading(true);\n    fetch(url, { signal: ctrl.signal })\n      .then(res => res.json())\n      .then(d => {\n        if (isActive && id === currentReq.current) {\n          setData(d);\n          setLoading(false);\n        }\n      })\n      .catch(e => {\n        if (isActive && e.name !== 'AbortError') setError(e);\n        if (isActive) setLoading(false);\n      });\n    return () => {\n      isActive = false;\n      ctrl.abort();\n    };\n  }, [url]);\n\n  return { data, loading, error };\n}\n```\n\n## Follow-up Questions\n\n- How would you test useCancelableFetch with RN Testing Library? \n- How would you extend it to support pagination without race conditions?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-15",
        "questionId": "q-17714",
        "question": "Context: beginner React Native; you’re building a simple cross-platform chat UI. Implement a reusable ChatPanel component that renders messages with FlatList, a text input, and a Send button. Data should persist locally using AsyncStorage and survive app restarts. Simulate a server delay with a mock API; implement optimistic UI updates, error handling, and a callback onMessagesChanged. Provide a minimal API: initMessages(messages), sendMessage(text), onMessagesChanged(cb). Include a usage sketch and a brief discussion of tradeoffs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed bridge: VibrationBridge with startVibration(pattern, {repeat}) and stopVibration(). On Android, call Vibrator.vibrate(VibrationEffect.createWaveform(pattern, -1)); on iOS, schedule a sequence",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To implement real-time auto-captioning, use a dedicated native thread for microphone capture and a low-latency on-device ASR (Speech framework on iOS, ML Kit on Android) with optional server fallback",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a small cross-platform TurboModule exposing startHaptic(pattern: number[]) and stopHaptic(). Add a native event lastHapticTs and a React hook useLastHapticTime() that subscribes to it. UI: a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a ChatPanel with FlatList, TextInput, and Button. Use useEffect to load initial messages from AsyncStorage, store with key 'chat', and persist after each send. Implement optimistic append, then",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to build a small, robust UI with RN basics: list rendering, local persistence, and optimistic updates. It also touches lifecycle, simple data flow, and error handling.\n\n## Key Concepts\n- FlatList basics and performance props\n- AsyncStorage persistence\n- Optimistic UI pattern and reconciliation\n- Basic error handling and retry strategy\n\n## Code Example\n```javascript\n// mock API\nconst mockSend = (text) => new Promise(res => setTimeout(() => res({ id: Date.now(), text }), 400));\n```\n\n## Follow-up Questions\n- How would you handle duplicate messages when server returns after optimistic update?\n- How to support offline queueing and retry on reconnect?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-16",
        "questionId": "q-560",
        "question": "You're building a React Native app that needs to display a list of user profiles with images. The list should be performant with 1000+ items and support pull-to-refresh. How would you implement this using FlatList and what optimizations would you apply?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a TurboModule exposing startPoseStream(config) and stopPoseStream(). Run a dedicated native thread with a fixed 120 Hz loop that fuses ARKit/ARCore pose data with IMU streams, emitting compact",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a notification router that maps payload to a deep link, persists lastRoute in AsyncStorage, and guards against duplicate navigations. Use a central NavigationService with a queue, handle foregr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement lazy loading with React.lazy() and Suspense for screen components, use React.memo() for expensive renders, optimize FlatList with getItemLayout and removeClippedSubviews, implement proper cleanup in useEffect…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use FlatList with keyExtractor for stable item identification, getItemLayout for optimized rendering calculations, and initialNumToRender to control the initial render batch. Implement pull-to-refresh functionality with…",
            "isCorrect": true
          }
        ],
        "explanation": "## Key Implementation\n- Use FlatList instead of ScrollView for efficient virtualized rendering with large datasets\n- Implement keyExtractor for stable item identification and optimal diffing\n- Add getItemLayout to enable smooth scrolling by providing item dimensions upfront\n- Configure initialNumToRender and maxToRenderPerBatch to balance performance and user experience\n\n## Performance Optimizations\n- Memoize renderItem components with React.memo to prevent unnecessary re-renders\n- Use windowSize prop to control the render window and reduce memory footprint\n- Implement image caching with react-native-fast-image for efficient image loading\n- Consider removeClippedSubviews for memory optimization with complex lists\n- Optimize data structure to minimize update calculations\n\n## Refresh Pattern\n```javascript\n<FlatList\n  data={profiles}\n  onRefresh={handleRefresh}\n  refreshing={isRefreshing}\n  keyExtractor={(item) => item.id}\n  getItemLayout={(data, index) => ({\n    length: ITEM_HEIGHT,\n    offset: ITEM_HEIGHT * index,\n    index,\n  })}\n  initialNumToRender={20}\n  maxToRenderPerBatch={10}\n  windowSize={10}\n  removeClippedSubviews={true}\n  renderItem={memoizedItemRenderer}\n/>\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-17",
        "questionId": "q-20806",
        "question": "Implement a cross-platform Toast utility in React Native with a minimal native API: showToast(message: string, durationMs: number). Android uses the system Toast; iOS uses a lightweight UILabel that fades in/out. Ensure a small toast queue to avoid overlaps, proper cleanup on app background/foreground, and a simple JS wrapper (import { NativeModules } from 'react-native'; NativeModules.ToastModule.showToast(...)). Provide a usage snippet and discuss thread-safety and accessibility considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a TurboModule with a dedicated native thread feeding a lock-free ring buffer. Expose JS API: startFeed(config), onTick(callback), updateRegion(params), stopFeed. Bridge via JSI; render with GPU he",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a TurboModule with a dedicated native render thread and a lock-free ring buffer shared between JS and the GPU path. iOS Metal-backed 2D canvas; Android Vulkan/OpenGL surface. API: startStrea",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed approach: FlatList with windowSize tuned, getItemLayout, and memoized item render; a TurboModule MetadataModule.fetchMetadata(ids: string[]) returns {id: meta}. useLazyMetadata(ids) batches i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe a minimal cross-platform Toast utility with a JS wrapper that calls a native module via NativeModules.ToastModule.showToast(message, duration). Implement Android side using Toast and iOS side",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests bridging basics, cross-platform behavior, and lightweight UI feedback.\n\n## Key Concepts\n- NativeModules bridging\n- Platform-specific implementations (Android Toast, iOS UILabel)\n- Toast queuing and lifecycle cleanup\n- Accessibility label/announce for assistive tech\n\n## Code Example\n```javascript\nimport { NativeModules } from 'react-native';\nexport const Toast = {\n  showToast: (msg, duration = 2000) => {\n    NativeModules.ToastModule.showToast(msg, duration);\n  }\n};\n```\n\n## Follow-up Questions\n- How would you test the queuing under rapid calls (e.g., 5 toasts in 1s)?\n- How would you ensure accessibility announcements on both platforms?\n- How would you handle orientation changes and app foreground/background transitions? \n```\n\n## Diagram\nflowchart TD\n  A[JS (RN)] --> B[ToastModule.native]\n  B --> C[Toast view on screen]\n  C --> D[Queue manager prevents overlap]\n  D --> E[Toast dismissed]",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-18",
        "questionId": "q-9945",
        "question": "Design a cross-platform React Native native module (JSI/TurboModule) that runs a lightweight on-device ML model for real-time object detection on a dedicated native thread and streams results to JS via useLiveInference(modelName, { fps, bufSize }) => { detections, ts }. Include backpressure that drops oldest frames when JS lags, a minimal camera overlay UI snippet to render boxes, and discuss latency, power usage, thermal throttling, and security considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a TurboModule that runs a dedicated native thread for a lightweight on-device model, feeding frames into a fixed-size ring buffer at the requested fps. Emit DetResult { label, score, bbox, t",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a native module that captures a fixed rear-camera feed, runs a small on-device model on a dedicated native thread, and streams gesture events to React Native at up to 30fps. Backpressure via t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Answer: Build a minimal native module (Android Kotlin, iOS Swift) exposing getBatteryLevel(): Promise<{level:number, charging:boolean}> and a native EventEmitter for changes. JS: implement useBattery(",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe a lightweight NewsList that fetches articles from an endpoint, renders via FlatList, supports pull-to-refresh, and caches results in AsyncStorage for offline use. Expose a minimal API: loadNe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a real-time, cross-thread RN native module using JSI/TurboModules, with careful backpressure and memory considerations.\n\n## Key Concepts\n\n- TurboModule/JSI bridges\n- Dedicated native thread and real-time streaming\n- Backpressure and memory management\n- Secure model handling and lifecycle\n\n## Code Example\n\n```javascript\n// useLiveInference usage sketch\nconst { detections, ts } = useLiveInference(\"mobile-ssd\", { fps: 15, bufSize: 60 });\n```\n\n## Follow-up Questions\n\n- How would you measure end-to-end latency and jitter? How would you test thermal throttling and battery impact?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-19",
        "questionId": "q-26060",
        "question": "Design a cross-platform React Native native module to perform on-device real-time object recognition using TF Lite on iOS and Android. It should stream recognition results at ~20-30 Hz to JS with a simple API: startStream(modelPath, config), onDetection, pause, resume, stop. Include model swapping, memory constraints, backpressure, and privacy safeguards?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a FlatList with virtualization and memoized items. keyExtractor: item => item.id; getItemLayout: index => ({length: 100, offset: 100 * index, index}); initialNumToRender: 10; windowSize: 21. Memoi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "API: unlockSession(options) -> Promise<void>, getSessionToken() -> Promise<string>, resetSession() -> void. Biometric prompts and secure storage via iOS Keychain/LocalAuthentication and Android Keysto",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a JS bridge exposing showToast({ message, duration }), hideToast(). On Android map duration to Toast.LENGTH_SHORT/LONG; on iOS present a transient alert view that auto dismisses. Add a singl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a TurboModule with a native worker per platform and a shared TF Lite interpreter. StartStream loads a model into a dedicated thread, maintains a bounded in-memory queue for frames, and posts",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a robust cross-platform native module around on-device ML with real-time streaming, focusing on performance, memory safety, and privacy.\n\n## Key Concepts\n\n- Cross-platform native module (TurboModule/JSI)\n- On-device TF Lite integration and dynamic model swapping\n- Real-time streaming (20-30 Hz) with bounded queues\n- Backpressure management and thread separation\n- Privacy: on-device processing, minimal logging, and secure data paths\n\n## Code Example\n\n```javascript\n// Usage sketch\nconst recognizer = new NativeObjectRecognizerModule();\nrecognizer.startStream(\"models/food.tflite\", {fps: 25, maxDetections: 50});\nrecognizer.onDetection((d) => console.log(d));\n```\n\n## Follow-up Questions\n\n- How would you handle model swaps mid-stream without dropping frames?\n- How would you measure and constrain memory usage across both platforms?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-20",
        "questionId": "q-16370",
        "question": "Context: you are building a cross-platform React Native module to perform low-latency, GPU-accelerated video augmentation in a privacy-conscious camera app. The native layer must stream 30fps frames to JS, apply overlays with Metal/Vulkan, and adapt frame rate based on battery state while keeping buffers zero-copy. Design the native API, frame buffer lifecycle, and backpressure; include a usage sketch and discuss threading, memory, and privacy considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use separate JavaScript isolates (V8 or JavaScriptCore) per sandbox instance to architect a native sandbox. Execute user code within a strict event loop, enforcing per-run timeouts and memory budgets. The API exposes fo…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a TurboModule API with the following surface: startCapture(config, onFrame), updateOverlay(params), setFrameRate(maxFps), pause(), resume(), and stop(). Frames are passed as native textures using zero-copy semant…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "StartHeatmap(config) initializes a GPU-accelerated, tile-based heatmap path; updatePoints(batch) streams points to a dedicated render thread (Metal on iOS, Vulkan/GL on Android); onFrame(cb) yields fr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a TurboModule with two dedicated native threads: a compute thread running GPU shaders (Metal on iOS, Vulkan on Android) for physics calculations, and a render thread synchronized with the React Native UI at 60…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates understanding of React Native bridging, native performance paths, and privacy-conscious media pipelines. It also examines threading, memory management, and GPU integration across platforms.\n\n## Key Concepts\n\n- TurboModules/JSI bridge for high-frequency data transfer\n- Zero-copy frame buffers and external textures\n- GPU-accelerated overlays (Metal/Vulkan)\n- Backpressure and thread affinity\n- Privacy: on-device processing, no persistent storage\n\n## Code Example\n\n```javascript\n// Sketch: TurboModule API surface (pseudocode)\nexport function startCapture(config: CaptureConfig, onFrame: (frame: Frame) => void): void;\nexport function updateOverlay(params: OverlayParams): void;\nexport function setFrameRate(maxFps: number): void;\nexport function pause(): void;\nexport function resume(): void;\nexport function stop(): void;\n```",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-21",
        "questionId": "q-10905",
        "question": "In a real-time AR camera app, design a cross-platform React Native native module that runs per-frame video frame analysis (e.g., color histogram or edge density) on a dedicated worker thread and streams a compact summary back to React Native at up to 30fps. It must support backpressure by dropping oldest frames when the JS thread lags, expose a minimal API (startStream(config), stopStream(), onFrameSummary), and include a brief API sketch and usage snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To stream 3 sensors at 60 Hz, implement a native thread that samples accelerometer, gyroscope, and barometer into a fixed-size ring buffer, fuses the data on-device using a Kalman filter, and emits 60 Hz batches to JS v…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a TurboModule backed by JSI with a native worker thread. Maintain a fixed-size ring buffer for raw frames; cap at 30fps; if the JS side lags, drop oldest frames before processing. API: startStream",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a FlatList with virtualization and memoized items. keyExtractor: item => item.id; getItemLayout: index => ({length: 100, offset: 100 * index, index}); initialNumToRender: 10; windowSize: 21. Memoi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a local-first cache with a Post[] store and an LRU image cache. Use region-based viewport to fetch pages, throttle to 1 req/sec, and prefetch 1–2 pages ahead. Merge remote updates with per-post revision numbers a…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-time framing with RN bridges tests threading, memory hygiene, and backpressure in production constraints.\n\n## Key Concepts\n- TurboModules/JSI and a dedicated native worker thread\n- Circular buffer, frame dropping strategy, backpressure\n- Zero-copy buffers, memory lifecycle, backpressure-induced latency\n- Cross-platform API design and usage patterns\n\n## Code Example\n```javascript\n// API sketch\ninterface FrameSummary { ts: number; hist: number[] }\nstartStream(config: { width:number; height:number; fps?:number; format?:string }): void\nstopStream(): void\nonFrameSummary(callback: (s: FrameSummary)=>void): void\n```\n\n## Follow-up Questions\n- How would you test backpressure behavior across devices? \n- How would you extend to higher frame rates (e.g., 60fps) while keeping latency bounded?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-22",
        "questionId": "q-15920",
        "question": "Design a cross-platform native module that runs a real-time on-device object detection pipeline in a React Native app. The module should ingest camera frames at up to 30 FPS, perform pre-processing, run a neural network on a dedicated thread (TensorFlow Lite or CoreML), apply non-maximum suppression, and stream results back to React Native with deterministic latency. Provide a native API sketch (JSI/TurboModule), thread model, and a minimal usage snippet. Discuss frame pacing, memory management, and energy considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a TurboModule called `ObjectDetectorModule` that ingests camera frames in YUV format, runs a quantized neural network model on a dedicated worker thread via TensorFlow Lite (Android/iOS) or CoreML (iOS), applies…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a lightweight native module (RNSQLite) that bridges iOS and Android to SQLite. Implement initSchema() to create a tasks table with columns (id TEXT PRIMARY KEY, text TEXT, done INTEGER, timestamp INTEGER), addTas…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To maintain smooth scrolling with 1,000+ items, leverage FlatList's virtualization features: getItemLayout for predictable heights, initialNumToRender to control initial load, maxToRenderPerBatch to limit concurrent ren…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a local-first analytics module using a time-series store (WatermelonDB/SQLite) with schema Points(id, ts, value, tag). Implement on-device windowed rollups (minute/hour) and a Skia-driven ch",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your understanding of on-device machine learning integration, multi-threading in React Native, and JSI/TurboModule bridge design for low-latency computer vision tasks.\n\n## Key Concepts\n- On-device inference pipelines for real-time object detection\n- JSI/TurboModules for high-performance native-JavaScript communication\n- Multi-threading models with dedicated worker queues\n- Frame pacing, memory management, and energy optimization on mobile devices\n\n## Native API Sketch (JSI/TurboModule)\n```cpp\n// ObjectDetectorModule.h (C++ TurboModule)\nclass ObjectDetectorModule : public JavaScriptModule {\npublic:\n  void startDetection(FrameSource source);\n  void stopDetection();\n  void setModel(ModelConfig config);\n  void onDetectionResult(DetectionCallback callback);\n};\n```\n\n## Thread Model\n- **Main Thread**: Handles React Native bridge communication and callback dispatching\n- **Camera Thread**: Dedicated thread capturing frames from camera feed at 30 FPS\n- **Inference Thread**: Worker thread running TensorFlow Lite/CoreML for neural network execution\n- **Post-Processing Thread**: Applies NMS and formats results\n\n## Minimal Usage Snippet\n```javascript\nimport { ObjectDetectorModule } from 'react-native-object-detector';\n\nconst detector = new ObjectDetectorModule({\n  modelPath: 'models/yolov8n.tflite',\n  numThreads: 4,\n  confidenceThreshold: 0.5,\n  nmsThreshold: 0.4,\n});\n\ndetector.onResults((detections) => {\n  console.log('Detected objects:', detections);\n});\n\nawait detector.start({ source: 'camera', fps: 30 });\n```\n\n## Critical Considerations\n\n### Frame Pacing\n- Use a triple-buffer scheme to decouple camera capture from inference processing\n- Implement a frame drop strategy when inference latency exceeds frame interval (33ms at 30 FPS)\n- Timestamp each frame to maintain temporal ordering in the output stream\n\n### Memory Management\n- Pre-allocate tensor buffers to avoid dynamic allocation during inference\n- Use object pooling for detection result objects to minimize GC pressure\n- Release camera frames immediately after tensor conversion to prevent memory buildup\n\n### Energy Considerations\n- Run inference on the dedicated NPU/DSP when available (delegate to EdgeTPU, Neural Engine)\n- Throttle to lower FPS (15) when the app is backgrounded or screen is off\n- Batch results instead of emitting each detection individually to reduce bridge overhead",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-23",
        "questionId": "q-15623",
        "question": "Create a minimal cross-platform React Native feature: a dynamic form renderer that consumes a server-provided JSON schema (fields, types, validators, and conditional visibility). Implement a JS-only API that loads and applies schemas, renders inputs, and persists drafts offline. Include a usage sketch and discuss offline edge cases?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a native WebSocket bridge (TurboModule/JSI) per room with a bounded outbound queue and delivery receipts. Expose useChat(roomId,{bufferMs}) => { messages, sendMessage, isConnected, latency }",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "API sketch: startDocument(id, text), edit(id, delta), onRemoteEdit(id, delta), getText(id), closeDocument(id). Native module CRDTEditor (iOS/Android) uses per-device vector clocks and a deterministic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a CatalogCache module (TypeScript) with initCache(endpoint, ttlMs), getProducts(forceRefresh?: boolean), onCacheUpdate(callback). Persist {ts, data} in AsyncStorage under 'catalog_cache'. ge",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a JS-only DynamicForm that renders inputs from JSON schema (text, number, select) with per-field validators. Use an in-memory FormStore plus AsyncStorage for drafts. Implement applySchema(schema",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a server-driven form renderer with offline resilience, a common real-world need in apps like Instacart.\n\n## Key Concepts\n- JSON schema-driven UI, basic validators, conditional visibility\n- State management for forms and per-field errors\n- Offline persistence (AsyncStorage) and schema versioning\n- JS-only API surface for rapid iteration\n\n## Code Example\n```javascript\n// sample DynamicForm usage\n<DynamicForm onSubmit={handleSubmit} schema={schema} />\n```\n\n## Follow-up Questions\n- How would you handle schema evolution where fieldIds change but existing drafts must be migrated?\n- How would you test validators and offline persistence?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-24",
        "questionId": "q-5355",
        "question": "You're building a cross-platform React Native chat app with end-to-end encryption. Design the crypto layer as a Rust-native module bridged via JSI to React Native. Explain key management (long-term device keys, ephemeral session keys, forward secrecy), secure storage (Keychain/Keystore), onboarding/verification flow (out-of-band trust, key ratcheting), and offline messaging with rekeying. Include a minimal hook useEncryptAndSend() and a UI snippet that shows sending an encrypted message?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a CRDT-based delta model with per-document version vectors for conflict-free merges. Expose a TurboModule bridge implementing loadChecklist, applyRemoteDelta, getLocalVersion, onConflict, and st",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To stream 3 sensors at 60 Hz, implement a native thread that samples accelerometer, gyroscope, and barometer into a fixed-size ring buffer, fuses the data on-device using a Kalman filter, and emits 60 Hz batches to JS v…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a native ToastModule exposing `showToast(message, duration)` to JavaScript. On Android, bridge via TurboModule (or legacy Bridge NativeModules), using `Toast.makeText(context, message, duration).show()` where…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Rust crypto module bridged to RN via JSI; maintain a per-device long-term X25519 key and ephemeral session keys; achieve forward secrecy with a Double Ratchet-like ratchet; store private keys in",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests integration of high-assurance cryptography with a mobile bridge, plus secure storage, verification UX, and offline resilience.\n\n## Key Concepts\n- End-to-end encryption with per-device long-term keys and per-session keys\n- Rust-native module bridged to RN via JSI for performance and safety\n- Forward secrecy via a ratcheting scheme (Double Ratchet-like)\n- Secure storage using Keychain/Keystore with biometric unlock\n- Out-of-band verification and key rotation flow\n- Offline messaging with rekeying and queueing\n\n## Code Example\n```javascript\n// Minimal sketch for useEncryptAndSend()\nfunction useEncryptAndSend() {\n  // returns sendEncrypted(toId, plaintext) and status\n  // actual implementation rests in Rust module exposed to JS\n  return {\n    sendEncrypted: async (toId, text) => {\n      // placeholder: calls into Rust crypto layer\n      return await crypto.encryptAndSend(toId, text)\n    },\n    status: 'idle'\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test property-based security guarantees (secrecy, authenticity, forward secrecy)?\n- How would you handle key compromise and re-key strategies in a multi-device user scenario?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-13T04:12:12.750Z",
    "version": 1
  }
]