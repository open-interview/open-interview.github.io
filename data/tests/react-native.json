[
  {
    "id": "test-react-native",
    "channelId": "react-native",
    "channelName": "React Native",
    "title": "React Native Knowledge Test",
    "description": "Test your understanding of React Native with 25 questions.",
    "questions": [
      {
        "id": "react-native-q-0",
        "questionId": "q-17382",
        "question": "Context: advanced React Native; you’re designing a cross-platform native module that streams depth frames from a LiDAR sensor (e.g., iPad Pro LiDAR) into the React Native UI at 30–60fps. The module must run on a dedicated native thread, expose a minimal JS API (startStream(config), onFrame(callback), stopStream), and sync with React Native render loop. Describe the API, threading model, a minimal usage snippet, and discuss backpressure, GC pauses, and power considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a TurboModule (JSI) exposing startStream, onFrame, stopStream. Run a native depth-pipeline thread that feeds a reusable frame buffer to a JS callback via a microtask queue. Backpressure via inFlig",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Store locally in WatermelonDB/SQLite with fields: id, formId, fields (JSON), version, dirty, attachments. On save, increment version and mark dirty. Sync with POST /forms/{id}/sync; conflicts resolved",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement writeCache(key,value), readCache(key), deleteCache(key) via a thread-safe in-process map and persist to a private cache dir per platform. Expose promises to TS/JS, wrap in a small hook useCa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Key approach: implement a TurboModule with startStream, stopStream, setModel, setBackend, onFrame. Use a dedicated native thread for frame capture, a lock-free ring buffer for frames, and a separate M",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a real-time native module with deterministic delivery, proper threading, and memory/power trade-offs.\n\n## Key Concepts\n- Native thread for sensor data;  - JSI/TurboModule bridge;  - ring/circular buffers;  - backpressure policy (drop vs queue);  - memory reuse and GC impact;  - lifecycle cleanup and power management.\n\n## Code Example\n```javascript\n// Minimal usage\nimport {DepthModule} from 'react-native-depth';\nDepthModule.startStream({fps:60});\nDepthModule.onFrame(frame => {\n  // process depth frame\n});\nDepthModule.stopStream();\n```\n\n## Follow-up Questions\n- How would you verify frame timing and backpressure on mid/low-end devices?\n- How would you scale to multiple LiDAR sources and fuse frames?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-1",
        "questionId": "q-5576",
        "question": "You're building a beginner-friendly onboarding flow in a React Native app for a fintech use case. Design an accessible three-screen onboarding with smooth transitions between screens on low-end devices, using either Animated or Reanimated. Include a skip/Next, progress indicator, and ensure screen reader order. Provide a small hook useOnboardingStep() and a minimal UI snippet to illustrate usage. What would you implement and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a three-screen onboarding with a single Animated.Value for progress, slide screens with translateX, and fade in/out text. Use accessibilityLabel, accessibilityHint, and accessible wrappers t",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a TurboModule exposing loadImage(url, cacheKey, { ttl }), getCachedImage(cacheKey), and clearCache(). Implement disk LRU with a version tag; on load, check cache, fetch if missing, write to dis",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a native module with a batteryChange event and a getBatteryInfo method. On iOS, enable UIDevice.batteryMonitoringEnabled and return batteryLevel and batteryState; on Android, listen for ACTI",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a TurboModule-backed analytics sink that buffers events in SQLite, batches by batchSize or timer, and uploads only when online. Anonymize by hashing user IDs, remove PII, and allow optional",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses practical skills in cross-platform UI, accessibility, and basic animation with React Native—core for production onboarding flows on constrained devices.\n\n## Key Concepts\n\n- Accessibility in React Native (accessibilityLabel, accessibilityHint, accessible)\n- Basic animation patterns (Animated or Reanimated) and sequencing\n- State flow for multi-screen experiences\n- Performance considerations on lower-end devices\n\n## Code Example\n\n```javascript\nimport React from 'react'\n```\n\n```javascript\n// Minimal hook and usage sketch\nexport function useOnboardingStep(initial = 0) {\n  const [step, setStep] = React.useState(initial)\n  function next() { setStep((s) => Math.min(2, s + 1)) }\n  function skip() { setStep(2) }\n  const isLast = step === 2\n  return { step, next, skip, isLast }\n}\n```\n\n```javascript\n// Minimal UI snippet\nimport { Animated, View, Text, Button } from 'react-native'\nexport function OnboardingScreen({ label, index, anim }) {\n  return (\n    <Animated.View style={{ transform: [{ translateX: anim }] }}>\n      <Text accessibilityLabel={`Onboarding step ${index + 1}`}>\n        {label}\n      </Text>\n    </Animated.View>\n  )\n}\n```\n\n## Follow-up Questions\n\n- How would you test accessibility across iOS and Android devices?\n- How would you adjust the flow for dynamic screens loaded from a remote config?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-2",
        "questionId": "q-2631",
        "question": "You're building a React Native field map app that shows a live route panel with up to 500 waypoints delivered over a WebSocket. Design a component architecture that minimizes re-renders, handles backpressure when bursts occur, and keeps the map smooth at 60fps. Provide a concise implementation sketch for a useLiveWaypoints hook (buffering and a render queue) and a minimal native bridge sketch if needed. What tests would you add to verify performance and correctness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To stream 3 sensors at 60 Hz, implement a native thread that samples accelerometer, gyroscope, and barometer into a fixed-size ring buffer, fuses the data on-device using a Kalman filter, and emits 60 Hz batches to JS v…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "startMixer initializes a ring buffer sized for worst-case latency (e.g., 4096 frames) and spawns a dedicated real-time DSP thread. pushInput queues incoming PCM frames for processing; an output loop dequeues mixed frame…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage a native toast manager with a queue exposed via a NativeModule (showToast, hideToast) and a JS listener. iOS: post accessibility announcements with UIAccessibility.postNotification. Android:",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a data path that decouples ingestion from rendering: a ring buffer accepts WebSocket messages, a RAF-tied render queue drains a fixed batch per frame, and a memoized selector updates a capped po",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical RN performance patterns: streaming data, backpressure, and rendering at 60fps. The candidate must show how to separate data ingestion from rendering and explain trade-offs.\n\n## Key Concepts\n- WebSocket streaming and buffering\n- Render queue driven by requestAnimationFrame\n- Immutable data updates and memoization\n- Map overlay with a capped, debounced point set\n- Memory profiling and cross-device testing\n\n## Code Example\n```javascript\nimport { useEffect, useRef, useState } from 'react';\n\nexport function useLiveWaypoints(wsUrl) {\n  const [points, setPoints] = useState([]);\n  const buffer = useRef([]);\n  const raf = useRef<number | null>(null);\n\n  useEffect(() => {\n    const ws = new WebSocket(wsUrl);\n    ws.onmessage = (ev) => {\n      const p = JSON.parse(ev.data); // {lat, lng, ts, id}\n      buffer.current.push(p);\n    };\n    return () => ws.close();\n  }, [wsUrl]);\n\n  useEffect(() => {\n    const loop = () => {\n      const b = buffer.current;\n      if (b.length) {\n        const batchSize = Math.min(b.length, 30);\n        const batch = b.splice(0, batchSize);\n        setPoints((prev) => {\n          const merged = prev.concat(batch);\n          return merged.slice(-500);\n        });\n      }\n      raf.current = requestAnimationFrame(loop);\n    };\n    raf.current = requestAnimationFrame(loop);\n    return () => {\n      if (raf.current) cancelAnimationFrame(raf.current);\n    };\n  }, []);\n\n  return points;\n}\n```\n\n## Follow-up Questions\n- How would you test backpressure behavior under bursty data?\n- How would you adapt this for offline caching and replay after reconnect?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-3",
        "questionId": "q-24372",
        "question": "Context: field-tech AR measurement tool—design a cross-platform native module for iOS/Android that measures real-world distance between two on-screen taps using ARKit/ARCore. Stream 3D coordinates and distance to React Native at 40 Hz, support tap-to-place, drag to adjust points, and backpressure. Provide a minimal native API, a JS interface, and a usage snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use FlatList with a memoized row, manage loading, error, and empty states, and debounce the search input. Cache results in AsyncStorage and load from cache on mount, then refresh in the background. Us",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed answer: measure via ARKit/ARCore, resolve two taps to 3D world points, compute Euclidean distance, stream 40 Hz to JS via TurboModule; setPoint updates by index; onSample carries {p1,p2,dist,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a FlatList with numColumns determined by screen width (2 on phones, 3 on tablets). Render images in a grid with resizeMode='cover'. Implement a tiny in-memory LRU cache for image URLs; preload nea",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A platform-native CV pipeline running on a dedicated worker thread. Detects a predefined marker set using iOS Vision/ARKit or Android ML Kit/ARCore, and streams lightweight landmark data {id, x, y, z, yaw, pitch, roll,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design cross-platform native modules with real-time streaming, AR data handling, and privacy.\n\n## Key Concepts\n- ARKit/ARCore world-space reconstruction\n- TurboModule/Bridge for perf\n- High-rate streaming and backpressure\n- Tap-to-place UX and coordinate transforms\n\n## Code Example\n```javascript\n// Usage sketch\nNativeDistance.start({fps:40});\nNativeDistance.setPoint(0, x1, y1);\nNativeDistance.setPoint(1, x2, y2);\nNativeDistance.onSample(cb);\n```\n\n## Follow-up Questions\n- How would you test drift across devices?\n- How would you handle device rotation and AR session restarts?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-4",
        "questionId": "q-18256",
        "question": "Context: advanced React Native; you are building a cross-platform native module for real-time audio processing (noise suppression plus beamforming) in a conferencing app. Expose minimal JS API: startStream(config), onAudioFrame(callback), updateConfig(params), stopStream. Run DSP on a dedicated thread with a zero-copy ring buffer; negotiate sample rate and latency; degrade gracefully on DSP-limited devices. Provide a minimal API sketch, usage snippet, and discuss latency, threading, focus, and thermal throttling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would design a cross-platform dynamic form engine in React Native with a native FormRenderer bridge API exposing these methods: loadSchema(schema, version), setValue(fieldId, value), getValue(fieldId), validate(formId…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: implement a cross‑platform TurboModule/JSI that executes a fixed‑priority native audio thread DSP chain using Android Oboe and iOS Audio Units. Use a preallocated lock‑free ring buf",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design and implement a cross-platform React Native module that leverages TurboModule/JSI architecture to stream high-frequency sensor data (up to 200 Hz) from multiple Bluetooth peripherals. The solution incorporates ef…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cross-platform native module for real-time audio processing (noise suppression + beamforming) in a conferencing app. Run DSP on a dedicated thread with a zero-copy ring buffer; JS API: startS",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a real-time audio subsystem that crosses platform boundaries, with strict latency and power constraints. Requires understanding of threading, buffer management, and React Native integration without leaking to the JS thread.\n\n## Key Concepts\n\n- Real-time audio pipeline and DSP threading\n- Zero-copy ring buffers and memory ownership\n- JNI/JSI/TurboModule interaction and latency budgets\n- Sample-rate negotiation and graceful degradation on limited hardware\n- Audio focus management and thermal throttling implications\n\n## Code Example\n\n```javascript\n// Minimal API sketch\nclass AudioModule {\n  static startStream(config) {}\n  static onAudioFrame(callback) {}\n  static updateConfig(params) {}\n  static stopStream() {}\n}\n```\n\n## Follow-up Questions\n\n- How would you test end-to-end latency on Android and iOS across devices?\n- What backpressure or overflow strategies would you implement for buffer overruns?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-5",
        "questionId": "q-11664",
        "question": "Design a cross-platform React Native native module for real-time audio processing (iOS/Android) that runs on a dedicated native thread using JSI/TurboModule. It must capture PCM from the mic, apply noise suppression and AEC in under 20 ms, and expose a minimal JS API to push input frames and receive processed frames for live transmission. Outline the API, data flow, backpressure strategy, lifecycle management, and privacy considerations; how would you implement it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a cross-platform native CRDT store for offline inspection data. Use per-record envelopes and a mixed CRDT (OR-Set for fields, RGA for ordered sections), AES-GCM at-rest encryption, delta-based synchronization, a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "API: initialize(config), start(), stop(), pushInputFrame(buffer, ts), pullOutputFrame(ts), onError. Run on a dedicated native thread with lock-free ring buffers; target ~20 ms frames. Use iOS NSE/NSE",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a NativeModule that exposes setBadge(count), getBadge(): Promise<number>, clearBadge(), and onBadgeChanged(callback). On iOS, update the badge on the main thread using UIApplication.sharedApplication.applicati…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a TurboModule exposing useControlStream(symbol,{rateHz,bufSize}) => {event, seq, ts}. Implement a native ring buffer with 64-bit sequence, monotonic timestamps, and a dedicated worker thread. O",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design a real-time audio module with cross‑platform constraints, low latency, and privacy considerations.\n\n## Key Concepts\n- Real-time latency and frame sizing (≈20 ms)\n- Platform-native AEC/noise suppression libraries\n- TurboModule/JSI bridge efficiency\n- Lock-free buffers and backpressure\n- Lifecycle management and privacy practices\n\n## Code Example\n```javascript\n// Minimal usage sketch\nconst audioProc = AudioProcModule.initialize({ sampleRate: 48000, channels: 1 });\naudioProc.start();\naudioProc.pushInputFrame(frameBuffer, timestamp);\naudioProc.pullOutputFrame((buf, ts)=> {\n  // send over network or render\n});\n```\n\n## Follow-up Questions\n- How would you test latency and stability across devices?\n- How would you handle state restoration after app suspend?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-6",
        "questionId": "q-11847",
        "question": "Context: You’re building an offline-first inspection app for field technicians. Design a cross‑platform native module (JSI/TurboModule) implementing an offline CRDT‑based data store for inspection reports. Include per‑record envelopes, per‑field conflict resolution, AES‑GCM at‑rest encryption, delta‑based sync, and backpressure‑aware streaming to RN. Provide a minimal API sketch, data model, and a usage snippet. Address consistency guarantees and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a local-first cache with a Post[] store and an LRU image cache. Use region-based viewport to fetch pages, throttle to 1 req/sec, and prefetch 1–2 pages ahead. Merge remote updates with per-post revision numbers a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design and implement a cross-platform React Native module that leverages TurboModule/JSI architecture to stream high-frequency sensor data (up to 200 Hz) from multiple Bluetooth peripherals. The solution incorporates ef…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Initialize state: data, page, loading, refreshing, error. On mount call fetchPage(1, 20). fetchPage(page, limit) fetches from the mock API, sets data to page 1 or appends for later pages, and handles",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a cross-platform native CRDT store for offline inspection data. Use per-record envelopes and a mixed CRDT (OR-Set for fields, RGA for ordered sections), AES-GCM at-rest encryption, delta-based synchronization, a…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests designing a robust offline-first data store with CRDTs, native modules, and careful backpressure for React Native UIs, plus encryption and delta synchronization considerations.\n\n## Key Concepts\n- CRDTs: OR-Set for fields, RGA for ordered lists; per-record envelopes\n- Delta-based synchronization and backpressure control\n- JSI/TurboModule bridging; AES-GCM encryption at rest\n- Conflict resolution: per-field last-writer-wins with server tie-breaker\n- Failure modes: partial synchronization, divergence, encryption/key rotation\n\n## Code Example\n```javascript\n// RN usage sketch\nimport {NativeCRDTStore} from 'react-native-crdt-store';\n\nconst store = new NativeCRDTStore({\n  encryptionKey: 'base64-key',\n  syncEndpoint: 'https://api.example.com/sync'\n});\n\n// Create inspection record\nconst report = await store.create('inspection', {\n  technicianId: 'tech_123',\n  timestamp: Date.now(),\n  items: ['item1', 'item2'] // RGA for ordered items\n});\n\n// Offline updates automatically merge\nawait store.update(report.id, {\n  status: 'completed',\n  notes: 'Inspection finished'\n});\n\n// Sync with backpressure awareness\nstore.startSync({\n  batchSize: 100,\n  maxConcurrency: 3\n});\n```",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-7",
        "questionId": "q-1537",
        "question": "You're building a React Native app for field engineers in regulated environments. It collects GPS and accelerometer telemetry, signs each record with a device key, and batches uploads to a backend with offline-first guarantees and tamper-evident auditing. Describe the architecture, data flow, and provide a small code sketch: a LocalAuditLog module backed by SQLite and TweetNaCl for Ed25519 signing, plus a transport adapter with exponential backoff and key-rotation handling. Include edge cases around tenant isolation, data retention, and replay protection?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a LocalAuditLog backed by SQLite to store signed records. Sign each entry with Ed25519 (TweetNaCl) using a per-tenant device key, include a timestamp and previous hash for chaining. Batch 50–100 r",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Adopt a CRDT-based delta model with per-document version vectors for conflict-free merges. Expose a TurboModule bridge implementing loadChecklist, applyRemoteDelta, getLocalVersion, onConflict, and st",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a cross‑platform RN native module that streams a high‑rate pose/sensor fusion pipeline to JS using a dedicated C++ thread and a lock‑free ring buffer. Expose startPipeline(config), updateParams",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a TurboModule exposing loadImage(url, cacheKey, { ttl }), getCachedImage(cacheKey), and clearCache(). Implement disk LRU with a version tag; on load, check cache, fetch if missing, write to dis",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes end-to-end data integrity in offline-first mobile apps, with regulatory considerations and tenant isolation.\n\n## Key Concepts\n- Local SQLite storage for durable offline data\n- Ed25519 signing (TweetNaCl) for tamper-evident logs\n- Batch transmission with exponential backoff and idempotent retries\n- Tenant isolation and per-tenant key rotation\n- Replay protection via nonces and chained hashes\n- Data-retention and auditability guarantees\n\n## Code Example\n```javascript\n// LocalAuditLog.ts (sketch)\nimport { open } from 'sqlite';\nimport sqlite3 from 'sqlite3';\nimport nacl from 'tweetnacl';\nimport naclUtil from 'tweetnacl-util';\n\ntype AuditRecord = {\n  id: string;\n  tenantId: string;\n  timestamp: number;\n  nonce: string;\n  payload: string;\n  hash?: string;\n  signature?: string;\n};\n\nexport class LocalAuditLog {\n  private db: any;\n  private keyPair: { publicKey: string; secretKey: string };\n\n  private constructor(db: any, keyPair: { publicKey: string; secretKey: string }) {\n    this.db = db; this.keyPair = keyPair;\n  }\n\n  static async create(dbPath: string): Promise<LocalAuditLog> {\n    const db = await open({ filename: dbPath, driver: sqlite3.Database });\n    await db.exec(`CREATE TABLE IF NOT EXISTS audits (\n      id TEXT PRIMARY KEY, tenantId TEXT, timestamp INTEGER, nonce TEXT,\n      payload TEXT, hash TEXT, signature TEXT, sent INTEGER DEFAULT 0\n    )`);\n    // placeholder keypair; in practice load from secure storage and rotate as needed\n    const keyPair = nacl.sign.keyPair();\n    return new LocalAuditLog(db, {\n      publicKey: naclUtil.encodeBase64(keyPair.publicKey),\n      secretKey: naclUtil.encodeBase64(keyPair.secretKey)\n    });\n  }\n\n  async append(tenantId: string, payload: string, nonce: string): Promise<void> {\n    const record: AuditRecord = {\n      id: crypto.randomUUID(), tenantId, timestamp: Date.now(), nonce, payload\n    };\n    const data = `${record.tenantId}|${record.timestamp}|${record.nonce}|${record.payload}`;\n    const sig = nacl.sign.detached(naclUtil.decodeBase64(this.keyPair.secretKey), naclUtil.decodeUTF8(data));\n    record.hash = this.hashRecord(record);\n    record.signature = naclUtil.encodeBase64(sig);\n    await this.db.run(`INSERT INTO audits (id, tenantId, timestamp, nonce, payload, hash, signature) VALUES (?, ?, ?, ?, ?, ?, ?)`,\n      record.id, record.tenantId, record.timestamp, record.nonce, record.payload, record.hash, record.signature);\n  }\n\n  async getBatch(limit: number): Promise<AuditRecord[]> {\n    const rows = await this.db.all(`SELECT * FROM audits WHERE sent = 0 ORDER BY timestamp ASC LIMIT ?`, limit);\n    return rows as AuditRecord[];\n  }\n\n  async markSent(ids: string[]): Promise<void> {\n    const placeholders = ids.map(() => '?').join(',');\n    await this.db.run(`UPDATE audits SET sent = 1 WHERE id IN (${placeholders})`, ids);\n  }\n\n  private hashRecord(r: AuditRecord): string {\n    return naclUtil.crypto_hash(`${r.tenantId}|${r.timestamp}|${r.nonce}|${r.payload}`);\n  }\n}\n```\n\n## Follow-up Questions\n- How would you securely store and rotate keys on device across app updates?\n- How would you migrate existing records during a key rotation without data loss?\n- How would you verify server integrity and prevent replay attacks in case of network replayed requests?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-8",
        "questionId": "q-15596",
        "question": "Design a cross-platform React Native native module that runs a lightweight on-device gesture recognizer (e.g., a rear-camera swipe gesture) for a DoorDash driver app. Expose a JS API to start/stop, configure sensitivity, and emit events {gestureName, confidence, timestamp} up to 30fps. Specify platform-native APIs (iOS CoreML/Vision vs Android ML Kit/TensorFlow Lite), threading, privacy prompts, and a minimal usage snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a native spatial index (R-tree) to bucket 1k devices by lat/lon and expose updates through a TurboModule. On JS, implement a backpressure-enabled queue that batches updates by map tiles and thrott",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a cross-platform TurboModule (JSI) exposing: startFeed({url, symbols, emaAlpha}), stopFeed(), onDiff(callback). A native worker thread reads a WebSocket feed, applies EMA smoothing, diffs wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a native module that captures a fixed rear-camera feed, runs a small on-device model on a dedicated native thread, and streams gesture events to React Native at up to 30fps. Backpressure via t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a functional ItemFeed with state for data, loading, and refreshing. Use useEffect to fetch from a mock endpoint, FlatList with keyExtractor={(i)=>i.id}, and renderItem for a compact card. Sh",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nInterviews often test real-world integration of native ML with RN, strict latency, and privacy.\n\n## Key Concepts\n- On-device ML with platform-specific stacks\n- Dedicated native thread for inference\n- JS bridge backpressure and event semantics\n- Privacy prompts and lifecycle cleanup\n\n## Code Example\n```javascript\n// Minimal JS bridge sketch\nclass GestureModule {\n  start(config: { gesture: string; fps?: number; sensitivity: number }, cb: (e: any) => void): void;\n  stop(): void;\n}\n```\n\n## Follow-up Questions\n- How would you measure and bound tail latency?\n- How to handle model updates and hot-swapping without app restart?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-9",
        "questionId": "q-6290",
        "question": "You're building an enterprise React Native app for on-device analytics that streams high-frequency sensor data (accelerometer/gyroscope) to a remote server. Design a native module (Rust via JSI bridge on iOS/Android) that samples at configurable rates, uses a ring buffer, caches locally with SQLite, and batches uploads when online. Include a JS hook useSensorStream() and a minimal UI snippet to visualize live metrics; discuss latency, battery, and privacy trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Realm for offline data graph (Checklist, Items, Attachments) with a per-record revision. Local SyncQueue holds pending mutations. Detect connectivity via NetInfo; a background worker batches and a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I'd implement a high-frequency sensor module in Rust via a JSI bridge, exposing start/stop, rate, and a stream of samples to RN. Use a ring buffer in native memory, cache samples locally with SQLite,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a TurboModule (JSI surface) with a minimal API: createBoard(id), onStroke(stroke, timestamp), flushDeltas(), pause(), resume(), and onUpdate(callback). Use a dedicated native render thread a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Minimal API Sketch** ```typescript interface PreviewConfig { fps: number; facing: 'front' | 'back'; resolution?: '720p' | '1080p'; } interface BackgroundConfig { method: 'segmentation' | 'blur'; blurRadius?: number; b…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests building a high-throughput, battery-conscious data path with a native module bridged to RN, plus local caching and batch uploads.\n\n## Key Concepts\n- React Native native modules via JSI/TurboModules\n- High-frequency data ingestion and memory efficiency\n- Local SQLite caching and batched network uploads\n- Privacy and battery optimization strategies\n\n## Code Example\n```javascript\n// Minimal useSensorStream hook sketch\nimport { useEffect, useState } from 'react';\nimport { NativeModules, NativeEventEmitter } from 'react-native';\nconst { SensorModule } = NativeModules;\nexport function useSensorStream(rate = 100) {\n  const [samples, setSamples] = useState([]);\n  useEffect(() => {\n    const onSample = (e) =>\n      setSamples((s) => [...s, e.sample].slice(-512));\n    const emitter = new NativeEventEmitter(SensorModule);\n    const sub = emitter.addListener('SensorSample', onSample);\n    SensorModule.start(rate);\n    return () => { sub.remove(); SensorModule.stop(); };\n  }, [rate]);\n  return samples;\n}\n```\n\n## Follow-up Questions\n- How would you adapt the sampling rate for power constraints in real-time?\n- What privacy safeguards and encryption would you implement for cached data?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-10",
        "questionId": "q-7107",
        "question": "Design a cross-platform React Native module to render a 1,000×1,000 heatmap in a logistics analytics app. Build a TurboModule/JSI native data source (Rust/Swift/Kotlin) that streams tiles on demand on a dedicated thread with backpressure (bufSize). Expose a hook useHeatmapTiles(viewport) => { x, y, value, ts } and a minimal TileCanvas component for rendering. Describe data model, caching, eviction, latency, and battery trade-offs, with a small UI sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a TurboModule-backed analytics sink that buffers events in SQLite, batches by batchSize or timer, and uploads only when online. Anonymize by hashing user IDs, remove PII, and allow optional",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implementation sketch: run a dedicated native STT thread, feed mic input into a streaming audio buffer, use iOS Speech framework and Android on‑device models; emit onChunk with text and timestamp, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage a Rust/TurboModule data source on a dedicated worker thread with a bounded ring buffer (bufSize) to stream tiles on-demand. Expose Tile { x, y, value, ts } and a hook useHeatmapTiles(viewport",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a TurboModule with a native tile decoder running on a dedicated worker thread; expose decodeTile and evictOldTiles. Implement a bounded LRU cache in JS and feed imgs to a FlatList. Backpressure vi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-language TurboModule design, on-device streaming, and cache-aware rendering for large heatmaps under tight memory and battery constraints. It also exercises evaluation of backpressure, thread usage, and rendering paths in React Native.\n\n## Key Concepts\n- TurboModule/JSI integration between JS and native (Rust/Swift/Kotlin)\n- Dedicated worker thread with a bounded buffer for streaming tiles\n- Tile data model and viewport-driven prefetch\n- JS-side LRU caching and eviction policy\n- Canvas/GL-based rendering with stable frame timing\n\n## Code Example\n```javascript\n// useHeatmapTiles hook sketch (pseudo)\nfunction useHeatmapTiles(viewport) {\n  const [tiles, setTiles] = React.useState([]);\n  // subscribe to native TileStream, map incoming tiles to state\n  // drop/overwrite logic to respect backpressure\n  return tiles;\n}\n```\n\n## Follow-up Questions\n- How would you scale to multi-resolution zoom and dynamic viewport changes?\n- How would you test backpressure behavior under bursty tile requests and network hiccups?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-11",
        "questionId": "q-14798",
        "question": "Context: you must design a cross-platform native module that renders a high-frequency chart directly in native code (Skia on Android, Metal on iOS). The app streams up to 1M points, supports 120fps panning, zoom, and offline cache. Expose a minimal API via JSI/TurboModule: initChart(config), pushData(seriesId, timestamp, value), setViewport(startTime, endTime), setVisible(seriesId, boolean), resetCache(). Describe thread model, backpressure, and memory budgeting?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a TurboModule that runs a dedicated native thread for a lightweight on-device model, feeding frames into a fixed-size ring buffer at the requested fps. Emit DetResult { label, score, bbox, t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a Rust native module bridged to React Native via TurboModules, storing events in SQLite with per-device AES-GCM encryption. Implement delta diff generation and a streaming API to JavaScript. Expose useAuditSyn…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a cross‑platform native module API to render a high‑frequency chart directly in native code (Skia on Android, Metal on iOS). The app streams up to 1M points, supports 120fps panning, zoom, and",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a JSI bridge exposing useBatteryAwareFetch that delegates to a background fetcher with an SQLite-based LRU cache. When battery saver is on or device is unplugged, serve data from cache with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a performant, platform-optimized visualization bridge with strict frame-rate and memory constraints.\n\n## Key Concepts\n- TurboModule/JSI bridging for a charting core\n- Native rendering paths (Skia/Metal)\n- Real-time data ingestion, backpressure, and downsampling\n- In-process caching and memory budgeting\n\n## Code Example\n```javascript\n// Minimal usage sketch\nconst Chart = require('react-native-native-chart');\nChart.initChart({ width: 1024, height: 480, theme: 'dark' });\nChart.pushData('seriesA', Date.now(), 42.3);\nChart.setViewport(0, Date.now());\n```\n\n## Follow-up Questions\n- How would you test 120fps on mid-range devices?\n- How to handle data gaps and rebase timestamps for synchronized rendering?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-12",
        "questionId": "q-18787",
        "question": "Context: build a cross-platform React Native native module that streams high-frequency financial tick diffs into the UI. Implement a TurboModule/JSI bridge that consumes a WebSocket feed, applies EMA smoothing, diffs each frame, and delivers 60fps updates to React Native. Outline the native API, threading model, backpressure, and a minimal usage snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a cross-platform TurboModule (JSI) exposing: startFeed({url, symbols, emaAlpha}), stopFeed(), onDiff(callback). A native worker thread reads a WebSocket feed, applies EMA smoothing, diffs wi",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a TurboModule called `ObjectDetectorModule` that ingests camera frames in YUV format, runs a quantized neural network model on a dedicated worker thread via TensorFlow Lite (Android/iOS) or CoreML (iOS), applies…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a React Native native module exposing `showDatePicker(options)` that returns a `Promise<Date>`. For iOS: present a modal with a `UIDatePicker` configured by mode, minDate, maxDate, initialDate, and locale, rej…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a two-thread model: a data thread feeding a small platform-optimized NMT engine and a JS thread bridge. Each translate call assigns a monotonic requestId; partial results stream via onProgre",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n- Tests ability to design a high-throughput bridge between native data streams and React Native UI using TurboModules/JSI.\n\n## Key Concepts\n- TurboModule/JSI bridge\n- Native worker thread and WebSocket feed\n- EMA smoothing and diffing\n- Backpressure and frame batching\n- Lifecycle cleanup and memory management\n\n## Code Example\n```javascript\nimport {FinanceFeed} from 'react-native';\nFinanceFeed.startFeed({url:'wss://stream', symbols:['AAPL','GOOGL'], emaAlpha:0.3});\nFinanceFeed.onDiff(payload => renderChart(payload));\n```\n\n## Follow-up Questions\n- How would you test backpressure behavior under bursty data?\n- How would you handle reconnects and out-of-order frames?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-13",
        "questionId": "q-18177",
        "question": "Context: advanced React Native; you’re building a cross-platform native module that implements a low-latency, on-device image-processing pipeline for live sports analytics. It ingests camera frames, runs a custom neural network to detect events, and publishes events to React Native at 120 Hz. Design the minimal native API (JSI/TurboModule), a dedicated worker thread, and a backpressure strategy. Include a minimal API sketch and a usage snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-platform TurboModule with two native threads: capture and encode. On iOS use ScreenCaptureKit/ReplayKit; Android use MediaProjection. Use a hardware encoder (HEVC/H264), a shared ring b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement SettingsRow as a functional component with props {icon,label,onPress,disabled}. Use Pressable with android_ripple for Android and a pressed state for iOS. Include accessibilityRole='button'",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: implement a TurboModule in C++/ObjC with a dedicated worker thread and a small ring buffer for 6 frames. Downscale 4x before enqueue. JS API: startPipeline(config), pushFrame(frame), onEvent",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a delta-CRDT with a core model: Checklist {id, items[]}, Item {id, text, checked}, Comment {id, text}. Each entity carries a vector clock and tombstones. A Rust native module exposes mergeDelta",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes ability to design a low-latency native module interfacing with RN, handling high frame rate, backpressure, memory, and cross-platform concerns.\n\n## Key Concepts\n- TurboModule/JSI interface\n- Dedicated worker thread and ring buffer\n- Zero-copy frame buffers and downscaling path\n- Backpressure strategies and telemetry\n- Cross-platform resource management\n\n## Code Example\n```javascript\n// Minimal API sketch\nstartPipeline({fps:120, downscale:4, modelPath:'/models/event.pt'});\npushFrame(frameYBuffer);\nonEvent(evt => console.log('event', evt.id, evt.confidence));\nstopPipeline();\n```\n\n```cpp\n// Pseudo C++ outline\nclass EventPipeline {\n  void start(Config);\n  void push(Buffer);\n  void onEvent(callback);\n  void stop();\n}\n```\n\n## Follow-up Questions\n- How would you measure and minimize frame drops in production?\n- How would you adapt the design for different device heat profiles and battery constraints?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-14",
        "questionId": "q-14184",
        "question": "Design a cross-platform React Native module to run an on-device OCR pipeline for live camera frames with privacy in mind. Stream frames (up to 15 fps) to a native OCR engine (iOS Vision / Android ML Kit), emit text to RN with low latency, and support pause/resume and ROI cropping. Explain native API (TurboModule/JSI), buffering, threading, backpressure, and testing across platforms?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a TurboModule-backed analytics sink that buffers events in SQLite, batches by batchSize or timer, and uploads only when online. Anonymize by hashing user IDs, remove PII, and allow optional",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement SettingsRow as a functional component with props {icon,label,onPress,disabled}. Use Pressable with android_ripple for Android and a pressed state for iOS. Include accessibilityRole='button'",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a TurboModule exposing startStream(config), stopStream(), setROI(rect), setFrameRate(fps), and onText(results). Maintain a native ring buffer, a dedicated worker thread, and a platform-specific OC",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a TurboModule (JSI surface) with a minimal API: createBoard(id), onStroke(stroke, timestamp), flushDeltas(), pause(), resume(), and onUpdate(callback). Use a dedicated native render thread a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a cross-platform native module under latency and privacy constraints.\n\n## Key Concepts\n\n- TurboModule/JSI bridge\n- Native thread pools and ring buffers\n- Backpressure and frame lifetime\n- Cross-platform API and lifecycle\n\n## Code Example\n\n```javascript\n// Example API sketch\nclass OCRModule extends TurboModule {\n  startStream(config); stopStream(); setROI(rect); onText(cb);\n}\n```\n\n## Follow-up Questions\n\n- How would you measure and reduce RTT latency on mid-range devices?\n- How to handle camera and data privacy in logs and telemetry?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-15",
        "questionId": "q-2501",
        "question": "You're building a beginner-friendly React Native field-data app for solar installations that allows technicians to submit a checklist and attach photos while offline. Describe an offline-first submission queue design: data model, how to store locally, how to detect connectivity, batching strategy, and a small hook sketch that enqueues items with a retry/backoff policy using AsyncStorage. Include a minimal example snippet for the hook?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Provide a TurboModule with startCapture(config) and stopCapture(), and onSpectrum(callback) to emit 60 Hz spectrum data. Use a real-time DSP thread and a double-buffered ring buffer shared with the br",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a TurboModule exposing useControlStream(symbol,{rateHz,bufSize}) => {event, seq, ts}. Implement a native ring buffer with 64-bit sequence, monotonic timestamps, and a dedicated worker thread. O",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Store a local offline queue in AsyncStorage as JSON: {id, payload, timestamp, retries}. On submit, push to queue and mark pending. Use NetInfo to detect online; when online, batch up to 10 items and P",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a moving viewport window with FlatList virtualization and a local cache (WatermelonDB/SQLite). Deduplicate by id and sequence; apply backpressure by batching updates in requestAnimationFrame and t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests offline-first data capture patterns, local persistence, network awareness, and retry/backoff in a React Native context.\n\n## Key Concepts\n\n- AsyncStorage for persistent queue\n- NetInfo to detect connectivity\n- Batch submission and idempotency\n- Exponential backoff with max cap\n- Test strategies for offline scenarios\n\n## Code Example\n\n```javascript\nimport { useEffect, useState } from 'react';\nimport AsyncStorage from '@react-native-async-storage/async-storage';\nimport NetInfo from '@react-native-community/netinfo';\n\nexport const useOfflineQueue = (storageKey = 'offline_submissions') => {\n  const [queue, setQueue] = useState([]);\n  useEffect(() => {\n    (async () => {\n      const raw = await AsyncStorage.getItem(storageKey);\n      setQueue(raw ? JSON.parse(raw) : []);\n    })();\n  }, [storageKey]);\n\n  const enqueue = async (payload) => {\n    const item = { id: `${Date.now()}-${Math.random()}`, payload, timestamp: Date.now(), retries: 0 };\n    const next = [...queue, item];\n    setQueue(next);\n    await AsyncStorage.setItem(storageKey, JSON.stringify(next));\n  };\n\n  const flush = async () => {\n    if (!queue.length) return;\n    const toSend = queue.map(i => i.payload);\n    try {\n      const res = await fetch('https://example.com/api/submissions', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify(toSend),\n      });\n      if (res.ok) {\n        await AsyncStorage.setItem(storageKey, JSON.stringify([]));\n        setQueue([]);\n      } else {\n        throw new Error('server-error');\n      }\n    } catch (e) {\n      const advanced = queue.map(i => ({ ...i, retries: i.retries + 1 }));\n      setQueue(advanced);\n      await AsyncStorage.setItem(storageKey, JSON.stringify(advanced));\n    }\n  };\n\n  return { queue, enqueue, flush };\n};\n```\n\n## Follow-up Questions\n\n- How would you test offline queue behavior on a real device? \n- How would you adapt the approach for large payloads or binary attachments (photos)?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-16",
        "questionId": "q-2072",
        "question": "You’re building a beginner-friendly React Native gallery that shows a 2x2 grid of images loaded from a remote JSON endpoint. Describe how you would implement offline-first caching with AsyncStorage, automatic data refresh on reconnect, and a small hook sketch to fetch and cache the image list (including error handling and a simple retry strategy)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a TurboModule CamML with startCapture(config), setResolution(width,height,fps), onFrame(callback), pauseResume, stopCapture. Use a high-priority native camera thread feeding a bounded ring buf",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a dedicated native render thread that consumes camera frames from a lock-free queue, runs GPU-accelerated stabilization (Metal on iOS, Vulkan/OpenGL on Android), and returns stabilized frame",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a TurboModule exposing loadImage(url, cacheKey, { ttl }), getCachedImage(cacheKey), and clearCache(). Implement disk LRU with a version tag; on load, check cache, fetch if missing, write to dis",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a hook useImageList that first loads cached results from AsyncStorage, falling back to fetch on miss or stale timestamp; on success, cache data with timestamp. Subscribe to NetInfo to refresh when",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a resilient, beginner-friendly offline-first feature using core RN APIs, with practicalCaching and reconnect handling.\n\n## Key Concepts\n- AsyncStorage for caching\n- NetInfo for connectivity events\n- FlatList with numColumns=2 for a grid\n- Simple retry/backoff and error handling\n- Stale data strategy (timestamp checks)\n\n## Code Example\n```javascript\nimport {useEffect, useState, useCallback} from 'react';\nimport AsyncStorage from '@react-native-async-storage/async-storage';\nimport NetInfo from '@react-native-community/netinfo';\n\nexport function useImageList(endpoint) {\n  const [images, setImages] = useState([]);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  const load = useCallback(async (force = false) => {\n    if (!force) {\n      const cached = await AsyncStorage.getItem('images_cache');\n      if (cached) {\n        setImages(JSON.parse(cached));\n        setLoading(false);\n        return;\n      }\n    }\n    try {\n      const res = await fetch(endpoint);\n      const data = await res.json();\n      setImages(data);\n      await AsyncStorage.setItem('images_cache', JSON.stringify(data));\n      await AsyncStorage.setItem('images_cache_ts', Date.now().toString());\n      setError(null);\n    } catch (e) {\n      setError(e);\n    } finally {\n      setLoading(false);\n    }\n  }, [endpoint]);\n\n  useEffect(() => {\n    load();\n    const unsubscribe = NetInfo.addEventListener(state => {\n      if (state.isConnected) load(true);\n    });\n    return () => unsubscribe();\n  }, [load]);\n\n  return { images, loading, error, retry: () => load(true) };\n}\n```\n\n## Follow-up Questions\n- How would you test the offline cache invalidation strategy?\n- How would you adapt this for paginated loading with large datasets?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-17",
        "questionId": "q-20766",
        "question": "Context: beginner React Native; Scenario: in a mobile wallet app, build a screen listing coupons fetched from a public API. It should render with FlatList, support search filtering, pull-to-refresh, and offline caching with AsyncStorage. Provide a minimal component tree, key props, and a plan for cache invalidation and iOS/Android compatibility?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implemented WebSocketSync with a durable native module (iOS/Android) exposing connect(url), subscribe(event, cb), publish(event, data). Keeps an offline queue in SQLite, reconnects with exponential ba",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use FlatList with data=messages; renderItem returns a simple row bubble aligned left or right based on senderId; include Avatar and a Separator; keyExtractor uses item.id; provide getItemLayout with i",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a JSI-based Rust native module to decode a binary notification stream and expose an EventEmitter to JS. Use a per-channel sequence number with id-based dedup, and a local SQLite store for of",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: create CouponList with state for data, query, refreshing. On mount, load cached data from AsyncStorage, then fetch from API. Use FlatList data filtered by query. Implement pull-to-refresh by set",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nPractical beginner task that mirrors real app patterns: data loading, caching, and offline UX.\n\n## Key Concepts\n\n- FlatList usage with data props\n- AsyncStorage caching with versioned keys\n- Offline-first UX with fallback data\n- Simple search filtering\n\n## Code Example\n\n```javascript\nimport React, { useEffect, useState } from 'react';\nimport { FlatList, TextInput, View, Text } from 'react-native';\nimport AsyncStorage from '@react-native-async-storage/async-storage';\n\nconst API = 'https://example.com/api/coupons';\nexport default function CouponList() {\n  const [data, setData] = useState([]);\n  const [query, setQuery] = useState('');\n  const [refreshing, setRefreshing] = useState(false);\n\n  async function loadCache() {\n    const cached = await AsyncStorage.getItem('coupons_v1');\n    if (cached) setData(JSON.parse(cached));\n  }\n\n  async function fetchData() {\n    try {\n      const resp = await fetch(API);\n      const json = await resp.json();\n      setData(json);\n      await AsyncStorage.setItem('coupons_v1', JSON.stringify(json));\n    } catch (e) {\n      // offline or error\n    }\n  }\n\n  useEffect(() => { loadCache(); fetchData(); }, []);\n\n  const filtered = data.filter(c => c.title.toLowerCase().includes(query.toLowerCase()));\n  return (\n    <View>\n      <TextInput value={query} onChangeText={setQuery} placeholder=\"Search coupons\" />\n      <FlatList data={filtered} keyExtractor={item => item.id} renderItem={({item}) => <Text>{item.title}</Text>} />\n    </View>\n  );\n}\n```\n\n## Follow-up Questions\n\n- How would you test offline caching on a device?\n- How would you extend cache invalidation when API data changes?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-18",
        "questionId": "q-19679",
        "question": "Context: intermediate React Native; design a cross-platform native module to run on-device OCR that processes camera frames in real time. Bind to iOS Vision and Android ML Kit, exposing a streaming API: start(), pause(), resume(), stop(), setCropRegion(x,y,w,h), setLanguage(lang). Emit per-frame OCR results via onFrame events with blocks and bounding boxes. Include a minimal TurboModule/JSI API sketch and a JS usage snippet. Address memory management, frame backpressure, privacy (no raw frames stored), and lifecycle cleanup?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a NativeModule + EventEmitter for both platforms. iOS: enable battery monitoring and observe UIDeviceBatteryLevelDidChange/UIDeviceBatteryStateDidChange; emit {level, charging}. Android: reg",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "TurboModule with useOrderBookStream(symbol) => { price: number, size: number, side: 'bid'|'ask', ts: number } on a dedicated native thread feeding a circular buffer. When full, drop oldest (backpressu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a native Box2D-like physics world on a dedicated C++ thread, exposed via TurboModule. API sketch: initWorld(gravity: {x,y}), addBody(id, shape, mass, bodyDef), setGravity, step(dt),",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cross-platform native module for real-time on-device OCR using iOS Vision and Android ML Kit. Implement a streaming API with the following methods: start(), pause(), resume(), stop(), setCropRegion(x, y, w, h),…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates cross-platform native module design, real-time streaming data handling, memory management, and privacy considerations in mobile development.\n\n### Key Concepts\n- TurboModule/JSI bridge for high-performance native interop\n- Real-time frame streaming with backpressure handling\n- Buffer reuse and memory management\n- Privacy: ephemeral buffers only, no raw frame storage\n\n### Code Example\n```javascript\n// Minimal usage sketch\nimport OCR from 'react-native-ocr';\n\nOCR.start({ \n  cropRegion: { x: 0.1, y: 0.1, w: 0.8, h: 0.8 }, \n  language: 'en' \n});\n\nOCR.onFrame((payload) => {\n  const { blocks, boundingBoxes, timestamp } = payload;\n  blocks.forEach(block => {\n    console.log('Text:', block.text);\n    console.log('Bounding Box:', block.boundingBox);\n  });\n});\n\n// Lifecycle management\nOCR.pause();\nOCR.resume();\nOCR.stop();\n```\n\n### Implementation Considerations\n\n**Memory Management:**\n- Reuse image buffers between frames to avoid allocation overhead\n- Use ring buffers for event emission to prevent memory growth\n- Release native resources immediately on stop()\n\n**Backpressure:**\n- Drop frames when processing cannot keep pace\n- Use a bounded queue with configurable maximum size\n- Implement a strategy to skip intermediate frames rather than queuing\n\n**Privacy:**\n- Process frames in memory only; never persist raw camera data\n- Discard frames immediately after OCR processing completes\n- Avoid logging or exporting raw image content\n\n**Lifecycle:**\n- Automatically pause on app backgrounding\n- Resume processing on foreground\n- Clean up all native resources and event listeners on stop()",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-19",
        "questionId": "q-27766",
        "question": "Context: advanced React Native; you’re building a cross-platform native module that performs GPU-accelerated real-time video compositing with a configurable shader pipeline. The module takes frames from the camera, applies a chain of GPU shaders (denoise, color grading, overlay), and renders into a React Native view at 60fps. Requirements: 1) Minimal JavaScript API: startPipeline(config), addStage(id, shader, params), updateStage(id, spec), onFrame(callback), stopPipeline. 2) Native threading: frame producer on a dedicated thread; render thread; 3) Backpressure: drop frames if UI can't keep up; 4) Dynamic reconfiguration on orientation and thermal state; 5) Memory budget around 300MB; 6) Robust error handling and fallbacks on devices with limited GPU. Provide a minimal API sketch, a usage snippet, and discuss synchronization, resource management, and testing strategies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design BatteryStatusBridge with a native module that emits batteryLevel (0-100) and isPlugged (charging, full, unplugged) via a JS event emitter. Android: BroadcastReceiver for ACTION_BATTERY_CHANGED;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed minimal API: startPipeline(config), addStage(id, shader, params), updateStage(id, spec), onFrame(callback), stopPipeline. Use a dedicated native GPU thread and a render thread; frames buffere",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a TurboModule that runs a dedicated native thread for a lightweight on-device model, feeding frames into a fixed-size ring buffer at the requested fps. Emit DetResult { label, score, bbox, t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a FlatList bound to a local array, filter with a query in useMemo, and memoize renderItem with React.memo. Use keyExtractor={i => i.id} and getItemLayout to a fixed height for smooth",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nRequires designing a GPU-accelerated, cross-platform pipeline integrated with React Native, including threading, memory, and backpressure considerations.\n\n## Key Concepts\n- GPU shader chains on Metal/Vulkan\n- Per-frame backpressure and ring buffers\n- Bridge synchronization with the UI thread\n- Dynamic reconfiguration without glitches\n- Memory and thermal-aware fallbacks\n\n## Code Example\n```javascript\n// Minimal API sketch (JS-binding, pseudocode)\nclass NativeVideoPipeline {\n  startPipeline(config) {}\n  addStage(id, shader, params) {}\n  updateStage(id, spec) {}\n  onFrame(callback) {}\n  stopPipeline() {}\n}\n```\n\n## Follow-up Questions\n- How would you measure GPU memory usage and throttling events?\n- How would you extend the API to support live shader hot-swapping without visible frame drops?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-20",
        "questionId": "q-3579",
        "question": "You're building a React Native app used offline by technicians that stores sensitive data locally (checklists, photos). Propose a secure offline storage design that preserves data-at-rest encryption with per-user keys, biometric unlock, and key rotation. Include data model considerations, library choices (Realm vs SQLite), a key-wrapping strategy, and a minimal hook sketch for encrypting on save and decrypting on read?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I'd implement a cross‑platform native module (Java/Kotlin and Objective‑C) that taps the camera, runs a lightweight denoise path, and exposes a FrameStream to RN. The hook useLiveVideoProcessor({ enab",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement envelope encryption with per-record AES-256-GCM keys wrapped by user-specific master keys stored in platform-backed keystores, protected by biometric authentication for secure offline data storage.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Load data from a mock API and cache results with a timestamp in AsyncStorage (key: 'restaurants_v1'). On component mount, first read from cache to display data immediately, then fetch fresh data from the API to update t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use FlatList with data filtered by useMemo; renderItem wrapped in React.memo; keyExtractor returns stable ids; implement getItemLayout for fixed-height items; debounce search input to limit re-renders",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates your understanding of secure offline persistence, key management, and platform integration within React Native applications.\n\n## Key Concepts\n\n- Client-side encryption using AES-256-GCM\n- Envelope encryption with key wrapping strategies\n- Biometric authentication via iOS Keychain and Android Keystore\n- Key rotation and data re-wrapping processes\n- Offline storage architecture: Realm vs SQLCipher trade-offs\n\n## Code Example\n\n```javascript\nimport { encrypt, decrypt } from 'crypto-lib';\n\nfunction useEncryptedStore() {\n  const saveRecord = async (record) => {\n    // Generate per-record key, wrap with master key, store encrypted fields\n  };\n  const loadRecord = async (id) => {\n    // Decrypt wrapped key, decrypt and return record\n  };\n  \n  return { saveRecord, loadRecord };\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-21",
        "questionId": "q-8945",
        "question": "In a React Native app for a small team, implement a lightweight cross-platform native module that exposes a single function getBatteryLevel() returning {level:number, charging:boolean} and a React hook useBattery() that subscribes to changes. Provide a minimal UI snippet showing a BatteryBadge that updates as level changes, and discuss platform differences and battery impact?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Answer: Build a minimal native module (Android Kotlin, iOS Swift) exposing getBatteryLevel(): Promise<{level:number, charging:boolean}> and a native EventEmitter for changes. JS: implement useBattery(",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a native module using platform schedulers (CADisplayLink on iOS, Choreographer on Android) with a small worker that watches battery, thermal state, and per-frame timings. Expose TurboModule",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement useNumColumns() that derives columns from width breakpoints (e.g., 2 for narrow, 3–4 for tablet). Use FlatList with numColumns={useNumColumns()} and a stable keyExtractor. Provide getItemLay",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a TurboModule bridged native audio module. Implement startCapture(config) configuring OS sessions, run DSP on a dedicated thread, and emit frames to React Native via a callback. Use AVAudioEng",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests bridging between JS and native, plus a small, reusable hook and UI. It emphasizes practicality over theory.\n\n## Key Concepts\n\n- Native module + EventEmitter bridge\n- React hooks for subscribing to native events\n- Lightweight UI binding to async data\n- Platform differences in battery APIs and power modes\n\n## Code Example\n\n```javascript\n// JS usage sketch\nimport { useBattery } from 'react-native-battery';\nfunction BatteryBadge() {\n  const { level, charging } = useBattery();\n  // render circle with level and charging indicator\n}\n```\n\n## Follow-up Questions\n\n- How would you test this on both iOS and Android?\n- How would you handle app state changes (foreground/background) for battery events?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-22",
        "questionId": "q-28722",
        "question": "Context: real-time camera-based AR app on mobile. Design a cross-platform native module for on-device GPU-based color grading and LUT application that processes the camera feed on a dedicated render thread, outputs per-frame metadata to React Native at 30fps, and supports dynamic LUT updates from JS. Specify the native module API (iOS/Android), threading model, memory constraints, backpressure strategy, and a minimal JS usage snippet. Include considerations for power, privacy, and frame drops?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a cross-platform React Native bridge CallEngine to manage a native WebRTC stack with per-peer simulcast, adaptive bitrate control, and hardware-accelerated codecs via a Rust/C++ native module. Encrypt media st…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a TurboModule with a private render thread. Apply LUTs in Metal on iOS and Vulkan on Android via a lightweight shader pass, reusing a pinned CVTexture/Texture2D to avoid copies. Bridge LUT u",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a TurboModule with a dedicated native worker and a ring buffer that ingests up to 1000 events/sec, compresses them with LZ4, and exposes a ReadableStream-like API to JS. APIs: start(), pause(),",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a JS-only DynamicForm that renders inputs from JSON schema (text, number, select) with per-field validators. Use an in-memory FormStore plus AsyncStorage for drafts. Implement applySchema(schema",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates cross‑platform native module design, GPU shader pipelines, and low‑level bridging performance under real‑world constraints (latency, backpressure, memory).\n\n## Key Concepts\n- TurboModule/JSI bridging for high‑throughput data\n- Dedicated render thread and GPU shader path (Metal/Vulkan)\n- Texture sharing and zero-copy data flow\n- Bounded buffering and frame-dropping strategy for backpressure\n- Dynamic LUT management and safe updates from JS\n- Power, privacy, and lifecycle considerations\n\n## Code Example\n```javascript\n// Minimal usage in JS\nimport { NativeModules } from 'react-native';\nconst { ColorGradePipeline } = NativeModules;\nColorGradePipeline.startPipeline({ width, height, lutBuffer }, (meta) => {\n  // handle per-frame metadata: meta.timestamp, meta.exposure, meta.whiteBalance\n});\n```\n\n## Follow-up Questions\n- How to validate latency budget under thermal throttling?\n- How to handle devices without Vulkan/Metal where fallback is required?\n- How to test LUT hot-reloading without frame drops?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-23",
        "questionId": "q-12608",
        "question": "Scenario: you're building a real-time multi-user whiteboard in React Native. Design a cross-platform native module (JSI/TurboModule) that streams ink stroke segments and pointer positions from a native drawing thread to JavaScript. The API should be startInkStream(params) and stopInkStream(), plus a callback or hook like useInkStream({ hz, bufSize }) => { id, segment: Float32Array, ts }, with a backpressure policy that drops oldest segments when the buffer is full. Use a dedicated high-priority thread for network/aggregation and minimize allocations. Include a usage snippet and discuss memory, GC, latency, and lifecycle cleanup?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement a small hook useDataList(url) that fetches data, caches response in AsyncStorage keyed by URL, and uses NetInfo to detect connectivity. On init, load cached data, show loading while netw",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implementation sketch: run a dedicated native STT thread, feed mic input into a streaming audio buffer, use iOS Speech framework and Android on‑device models; emit onChunk with text and timestamp, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a TurboModule (JSI) that loads a quantized TF Lite model on iOS/Android, runs inference in a dedicated worker thread, and streams gesture events to JS at ~30 Hz. API: initModel(modelPath, op",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a TurboModule with startInkStream(params) and onStrokeChunk(callback), plus stopInkStream(). Use a fixed-size circular buffer on the native side; ink thread pushes segments (strokeId, ts, se",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests deep RN bridging, real-time data flow, and memory management under pressure for collaboration apps like Discord.\n\n## Key Concepts\n- React Native TurboModule/JSI bridging\n- Real-time native threading and backpressure\n- Circular buffers and zero-allocation paths\n- Cross-platform memory and GC implications\n- Lifecycle cleanup and thread termination\n\n## Code Example\n```javascript\n// usage sketch (pseudo)\nconst inkStream = useInkStream({ hz: 60, bufSize: 1024 });\n// callback receives: { id, segment, ts }\ninkStream.onTick(chunk => { renderStroke(chunk); });\n```\n\n## Follow-up Questions\n- How would you measure and guarantee end-to-end latency under bursty input?\n- How would you adapt the buffer strategy for mobile devices with varying memory pressure?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-24",
        "questionId": "q-25539",
        "question": "Context: beginner React Native; you’re building a small catalog screen for a shopping app. Implement a tiny data layer that fetches /products from a REST API, caches responses in AsyncStorage with a 1-hour TTL, and returns cached data if offline or data is fresh. Expose a minimal JS API: initCache(endpoint, ttl), getProducts(forceRefresh?), onCacheUpdate(callback). Provide a usage snippet and discuss cache invalidation and offline resilience?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a TurboModule (JSI) exposing startStream, onFrame, stopStream. Run a native depth-pipeline thread that feeds a reusable frame buffer to a JS callback via a microtask queue. Backpressure via inFlig",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a TurboModule bridge and a CRDT-based chat store. API: startSync(config), applyLocalEdit(edit), onRemoteOps(handler), pause(), resume(). Data model: Message {id, author, ts, content, tombsto",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a functional StockList component: on mount, load cachedStocks from AsyncStorage; render with FlatList and keyExtractor; fetch /api/stocks, set data, refresh control; on success, save to AsyncStora",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a CatalogCache module (TypeScript) with initCache(endpoint, ttlMs), getProducts(forceRefresh?: boolean), onCacheUpdate(callback). Persist {ts, data} in AsyncStorage under 'catalog_cache'. ge",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssess practical data caching and offline resilience in RN.\n\n## Key Concepts\n- AsyncStorage\n- Offline-first pattern\n- TTL cache\n- Connectivity checks with NetInfo\n- Cache invalidation strategy\n\n## Code Example\n```javascript\n// CatalogCache skeleton\nexport class CatalogCache {\n  static initCache(endpoint: string, ttlMs: number) { /* ... */ }\n  static async getProducts(forceRefresh?: boolean) { /* ... */ }\n  static onCacheUpdate(callback: (data:any)=>void) { /* ... */ }\n}\n```\n\n## Follow-up Questions\n- How would you test offline behavior?\n- How would you migrate cached data when schema changes?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-02T10:28:05.299Z",
    "version": 1
  }
]