[
  {
    "id": "test-react-native",
    "channelId": "react-native",
    "channelName": "React Native",
    "title": "React Native Knowledge Test",
    "description": "Test your understanding of React Native with 25 questions.",
    "questions": [
      {
        "id": "react-native-q-0",
        "questionId": "q-4850",
        "question": "You're building a React Native app for field technicians to annotate inspection photos offline. Design an offline-first image pipeline that uses a Rust-native module bridged to React Native to downsample, watermark, and generate thumbnails. Include data model and memory constraints, incremental sync with the server, a conflict resolution strategy, and a minimal hook useImagePipeline(region) plus a small UI usage snippet. Discuss trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "API: Clipboard.setString(value: string): void; Clipboard.getString(): Promise<string>. Bindings: Android: ClipboardManager; iOS: UIPasteboard. JS shim: const Clipboard = { setString, getString }. Usag",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: create CouponList with state for data, query, refreshing. On mount, load cached data from AsyncStorage, then fetch from API. Use FlatList data filtered by query. Implement pull-to-refresh by set",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement as a TurboModule using Metal on Apple platforms to run real‑time denoising in a dedicated native thread. Expose useImageDenoise({ enabled, resolution, strength }) => { frameStream } (JSI bri",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build an offline-first image pipeline using a Rust-native module bridged to React Native. Store images locally with SQLite (ImageRecord: id, path, region, metadata, status, lastModified, syncVersion, checksum). The Rust…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Important\nField technicians often work in areas with poor or no connectivity. They need to capture, annotate, and store inspection photos locally, then sync when online. This requires careful handling of large binary data, memory management, and resilient sync logic.\n\n## Key Components\n\n### 1. Local Data Model (SQLite)\n```javascript\nImageRecord = {\n  id: string,              // UUID\n  path: string,            // local file path\n  region: string,         // geographic region for filtering\n  metadata: object,       // timestamp, technician ID, equipment info\n  status: 'pending' | 'synced' | 'conflict' | 'uploading',\n  lastModified: number,    // Unix timestamp\n  syncVersion: number,     // for incremental sync\n  checksum: string,        // SHA-256 for dedup\n  thumbnailPath: string,  // path to generated thumbnail\n  retryCount: number      // for failed uploads\n}\n```\n\n### 2. Rust Image Pipeline (Native Module)\n**Processing Pipeline:**\n- **Downsample**: Reduce resolution using bilinear interpolation; target 1920px max dimension for storage\n- **Watermark**: Composite technician ID, timestamp, GPS coords using imageproc crate\n- **Thumbnail**: Generate 200x200px preview using fast downsampling\n\n**Memory Constraints:**\n- Process images in bounded thread pool (2-4 workers based on device)\n- Stream large images in 1MB chunks instead of loading full buffer\n- Use `Arc<AtomicBool>` for cancellation signals\n- Limit peak memory to ~150MB for image processing\n- Release memory immediately after each operation\n\n```rust\n// Rust pseudo-code\npub fn process_image(path: &str, options: ProcessOptions) -> Result<ProcessedImage> {\n    let img = image::open(path)?;\n    let downsampled = downsample(&img, MAX_DIM);\n    let watermarked = apply_watermark(&downsampled, &options.watermark)?;\n    let thumbnail = downsample_thumbnail(&watermarked, 200);\n    Ok(ProcessedImage { watermarked, thumbnail })\n}\n```\n\n### 3. Incremental Sync Strategy\n- **Sync Protocol**: Send only records where `local.syncVersion > server.syncVersion`\n- **Push**: Batch upload pending images (max 5 at a time to conserve bandwidth)\n- **Pull**: Query server for updates since last sync timestamp\n- **Status Tracking**: Track upload progress, pause/resume on network changes\n\n### 4. Conflict Resolution Strategy\n- **Default**: Last-write-wins based on `lastModified` timestamp\n- **Manual Override**: Flag conflicts for technician review\n- **Merge**: For metadata-only conflicts, merge object properties\n- **Audit Log**: Keep conflict history for debugging\n\n```javascript\nfunction resolveConflict(local, server) {\n  if (local.lastModified > server.lastModified) {\n    return { ...local, status: 'pending', conflictResolved: true };\n  }\n  return { ...server, status: 'synced', conflictResolved: true };\n}\n```\n\n### 5. useImagePipeline Hook\n```javascript\nimport { useImagePipeline } from './native/ImagePipeline';\n\nfunction ImageGallery({ region }) {\n  const { \n    images, \n    processImage, \n    syncNow, \n    pendingCount, \n    isSyncing \n  } = useImagePipeline(region);\n\n  const handleCapture = async (photo) => {\n    await processImage(photo, {\n      watermark: { technicianId: 'T-1234' },\n      generateThumbnail: true\n    });\n  };\n\n  return (\n    <View>\n      {isSyncing && <SyncIndicator count={pendingCount} />}\n      <FlatList\n        data={images}\n        renderItem={({ item }) => (\n          <ImageItem \n            thumbnail={item.thumbnailPath}\n            status={item.status}\n          />\n        )}\n      />\n    </View>\n  );\n}\n```\n\n### 6. Trade-offs Discussion\n- **SQLite vs AsyncStorage**: SQLite handles large datasets better but requires native module; AsyncStorage is simpler but 100KB limit\n- **JPEG vs PNG**: JPEG offers ~10x smaller files for photos; PNG needed only if watermark requires transparency\n- **Sync Frequency**: More frequent = better data freshness but higher battery/cellular cost\n- **Compression Quality**: Lower quality = faster sync but potentially unreadable annotations\n- **Offline Duration**: Longer offline periods increase conflict risk and storage pressure\n- **Thumbnail Size**: Smaller = faster list rendering but less detail for inspection review\n- **Rust vs JS Processing**: Rust is faster but adds native module complexity and compilation overhead",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-1",
        "questionId": "q-25910",
        "question": "Context: advanced React Native; you’re building a cross-platform native module that implements a real-time, low-latency audio mixer for a VOIP app. The module runs a DSP chain on a dedicated native thread and streams PCM frames to React Native at 48 kHz stereo, with per-channel volume controls and dynamic reconfiguration. Expose a minimal JS API: startMixer(config), pushInput(frame), onOutput(callback), setChannelVolume(index, vol), stopMixer. Provide a minimal API sketch, usage snippet, and discuss synchronization, backpressure, latency targets, and battery/thermal considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a cross-platform native module (JSI TurboModule) using Android AudioRecord and iOS AVAudioEngine to emit RMS/peak samples at a configurable rate, via a bounded ring buffer to JS. Expose useA",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A platform-native CV pipeline running on a dedicated worker thread. Detects a predefined marker set using iOS Vision/ARKit or Android ML Kit/ARCore, and streams lightweight landmark data {id, x, y, z, yaw, pitch, roll,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement useNumColumns() that derives columns from width breakpoints (e.g., 2 for narrow, 3–4 for tablet). Use FlatList with numColumns={useNumColumns()} and a stable keyExtractor. Provide getItemLay",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "startMixer initializes a ring buffer sized for worst-case latency (e.g., 4096 frames) and spawns a dedicated real-time DSP thread. pushInput queues incoming PCM frames for processing; an output loop dequeues mixed frame…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses understanding of real-time audio processing, cross-platform native modules, and achieving low latency in mobile environments.\n\n## Key Concepts\n- Real-time threads and lock-free data structures\n- DSP chain architecture with dynamic reconfiguration\n- JS bridge backpressure management and latency budgeting\n- Power and thermal constraints on mobile devices\n\n## Code Example\n```javascript\n// Minimal API sketch\nconst mixer = startMixer({ sampleRate: 48000, channels: 2 });\nmixer.pushInput(frame);\nmixer.onOutput((frame) => { /* render audio */ });\nmixer.setChannelVolume(0, 0.8);\nmixer.stopMixer();\n```\n\n## Discussion Points\n\n**Synchronization**: The native DSP thread runs independently from the JS thread. Communication uses lock-free queues (SPSC) to avoid blocking the real-time audio path. Volume updates use atomic operations or double-buffering to prevent glitches.\n\n**Backpressure**: If the JS thread cannot deliver frames quickly enough, the ring buffer absorbs temporary bursts. For sustained backpressure, the native module should implement a configurable drop-policy (e.g., oldest frame or silence injection) to maintain audio continuity.\n\n**Latency Targets**: Target end-to-end latency under 20ms for real-time VOIP. This constrains buffer size (4096 frames at 48kHz ≈ 85ms), so the design must minimize buffer depth while avoiding underruns.\n\n**Battery/Thermal**: Running a dedicated native thread consumes CPU cycles. Mitigate this by:\n- Using real-time scheduling priority only when actively mixing\n- Processing in small batches to allow CPU idle states\n- Pausing the DSP chain when all inputs are silent\n- Offloading to hardware accelerators where available\n\nThe JS API remains minimal and asynchronous, delegating all real-time work to native code while providing callbacks for audio output.",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-2",
        "questionId": "q-8033",
        "question": "Design a cross-platform native module bridged to React Native that generates on-device image thumbnails. The module should take a list of image URIs, a target thumbnail size, and output thumbnails asynchronously on a worker thread, with a JS hook useImageThumbs({ uris, size, quality }) => { thumbs, progress, error }. Include a minimal React Native UI sketch and discuss memory usage, cache strategy, and Android/iOS parity?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a TurboModule exposing useControlStream(symbol,{rateHz,bufSize}) => {event, seq, ts}. Implement a native ring buffer with 64-bit sequence, monotonic timestamps, and a dedicated worker thread. O",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a NativeModule that exposes setBadge(count), getBadge(): Promise<number>, clearBadge(), and onBadgeChanged(callback). On iOS, update the badge on the main thread using UIApplication.sharedApplication.applicati…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Native Modules bridge JavaScript and native code to access platform-specific APIs not available in JS. Use for Bluetooth, biometrics, camera controls, or performance-critical operations. Consider bridge overhead, thread…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On iOS, use CGImageSourceCreateThumbnailAtIndex with maxDimension; on Android, use BitmapFactory.Options.inSampleSize and Bitmap.createScaledBitmap. Run on a background thread; support cancelation; st",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests bridging, threading, and practical image processing in RN without blocking the UI.\n\n## Key Concepts\n- Native modules bridged to React Native\n- Background work and cancelation\n- Cross-platform image thinning: iOS CG APIs vs Android Bitmap APIs\n- Memory budgets and caching for thumbnails\n\n## Code Example\n```javascript\n// useImageThumbs hook skeleton\nimport { NativeModules, NativeEventEmitter } from 'react-native';\nconst { ImageThumbModule } = NativeModules;\nexport function useImageThumbs({ uris, size, quality }) {\n  // Implementation would subscribe to native events and manage state\n  return { thumbs: [], progress: 0, error: null };\n}\n```\n\n## Follow-up Questions\n- How would you handle progressive loading vs batch completion and cancelation across platforms?\n- How would you validate memory usage under low-memory conditions and adjust cache sizing?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-3",
        "questionId": "q-17382",
        "question": "Context: advanced React Native; you’re designing a cross-platform native module that streams depth frames from a LiDAR sensor (e.g., iPad Pro LiDAR) into the React Native UI at 30–60fps. The module must run on a dedicated native thread, expose a minimal JS API (startStream(config), onFrame(callback), stopStream), and sync with React Native render loop. Describe the API, threading model, a minimal usage snippet, and discuss backpressure, GC pauses, and power considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Expose a native module API: setChain(chainSpec), updateParam(nodeId, param, value), replaceChain(newSpec), start(), stop(), and getLatency(). Run DSP on a RT thread with a lock-free circular buffer br",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement as a TurboModule using Metal on Apple platforms to run real‑time denoising in a dedicated native thread. Expose useImageDenoise({ enabled, resolution, strength }) => { frameStream } (JSI bri",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a TurboModule (JSI) exposing startStream, onFrame, stopStream. Run a native depth-pipeline thread that feeds a reusable frame buffer to a JS callback via a microtask queue. Backpressure via inFlig",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Architect a TurboModule with a dedicated native thread and per-symbol ring buffer. Expose useMarketStream(symbol,{ Hz, bufSize }) => { tick, ts }. Backpressure drops oldest ticks when full; ticks push",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a real-time native module with deterministic delivery, proper threading, and memory/power trade-offs.\n\n## Key Concepts\n- Native thread for sensor data;  - JSI/TurboModule bridge;  - ring/circular buffers;  - backpressure policy (drop vs queue);  - memory reuse and GC impact;  - lifecycle cleanup and power management.\n\n## Code Example\n```javascript\n// Minimal usage\nimport {DepthModule} from 'react-native-depth';\nDepthModule.startStream({fps:60});\nDepthModule.onFrame(frame => {\n  // process depth frame\n});\nDepthModule.stopStream();\n```\n\n## Follow-up Questions\n- How would you verify frame timing and backpressure on mid/low-end devices?\n- How would you scale to multiple LiDAR sources and fuse frames?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-4",
        "questionId": "q-25343",
        "question": "Design a cross-platform React Native permission module that aggregates camera, microphone, and storage permissions behind a single API and streams status changes; implement JS API (checkPermissions, requestPermissions, onStatusChange), sketch iOS (Swift) and Android (Kotlin) bridges, provide a usage snippet, and discuss caching, permission priority, and UX when denied?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: create CouponList with state for data, query, refreshing. On mount, load cached data from AsyncStorage, then fetch from API. Use FlatList data filtered by query. Implement pull-to-refresh by set",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a TurboModule running a dedicated performance thread that samples frameDuration, mainThreadIdle, and vsyncOffset at a configurable rate and streams metrics to JavaScript via the onMetrics callback. ## iOS (Swift)…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implementation sketch: run a dedicated native STT thread, feed mic input into a streaming audio buffer, use iOS Speech framework and Android on‑device models; emit onChunk with text and timestamp, and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: expose a RNPermissions module that tracks a PermissionStatus map for camera, microphone, storage. checkPermissions returns current statuses; requestPermissions prompts for missing ones in a",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cross-platform bridges, handle asynchronous permission flows, UX when denied, and keeping state consistent across RN and OS.\n\n## Key Concepts\n\n- Cross-platform RN bridges (RCTBridgeModule) in Swift and Kotlin\n- Unified permission model and statuses\n- UX for denied/blocked permissions and settings redirection\n- Caching results and avoiding redundant prompts\n\n## Code Example\n\n```javascript\nimport { NativeModules, NativeEventEmitter } from 'react-native';\nconst { RNPermissions } = NativeModules;\nconst emitter = new NativeEventEmitter(RNPermissions);\nRNPermissions.checkPermissions(['camera','microphone','storage']).then(set => { /* ... */ });\nRNPermissions.requestPermissions(['camera']).then(res => { /* ... */ });\n```\n\n## Follow-up Questions\n\n- How to handle permission changes when app moves to background?\n- How to test mocked permission flows in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-5",
        "questionId": "q-20683",
        "question": "You’re building a cross-platform native module to guarantee deterministic frame pacing for complex RN UI, coordinating a high-priority render loop with the Fabric scheduler. Target 120fps; RTL/fontScale/accessibility. Minimal API: startScheduler(config), setFrameRate(fps), onFrame(cb), pause, resume, stopScheduler. Provide API sketch, usage, and discuss synchronization with JS thread, backpressure, and cross-platform testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a dedicated native render loop on a high-priority thread (iOS CADisplayLink; Android Choreographer) targeting 120fps. Expose startScheduler(config), setFrameRate(fps), onFrame(cb), pause, re",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a native thread running a GPU-accelerated render loop (Metal on iOS, Vulkan on Android) with a minimal JS API: beginPipeline(config), onFrame(callback), pushFrame(frameData), stopPipeline. Use a dedicated render…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a data module with three functions: - **fetchProfiles()**: Checks NetInfo for connectivity. If online, fetches from `/api/profiles`, saves `{ ts: Date.now(), data }` to AsyncStorage under key 'profiles_v1', th…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a TurboModule exposing startInference(modelPath, inputDims, options), swapModel(path), pause/resume, and onResult callback with top1/topk and confidences. Run a dedicated worker thread with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests capability to architect cross‑platform frame pacing with native threads and the RN Fabric scheduler.\n\n## Key Concepts\n- Deterministic frame pacing\n- Native render loops (CADisplayLink/Choreographer)\n- TurboModule/JSI integration\n- Backpressure and frame dropping\n- RTL, fontScale, accessibility, cross-platform testing\n\n## Code Example\n```javascript\n// Minimal API sketch\nstartScheduler(config: {fps:number, priority:string}): void;\nsetFrameRate(fps:number): void;\nonFrame(cb:(info:{timestamp:number, frameId:number}) => void): void;\npause(): void;\nresume(): void;\nstopScheduler(): void;\n```\n\n## Follow-up Questions\n- How would you measure/guarantee frame latency on diverse devices?\n- How to detect and fix native loop leaks during long runs?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-6",
        "questionId": "q-22593",
        "question": "Context: live telemetry from multiple sensors (accelerometer, gyroscope, barometer) in a React Native app. Design a cross-platform native module that streams fused sensor data at 60 Hz from native to JS. Implement a dedicated native worker, on-device fusion, and a minimal JS API: startStream(config), onSample, pause, resume, stop. Include data freshness, timestamps, and backpressure handling to keep UI responsive?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a two-thread model: a data thread feeding a small platform-optimized NMT engine and a JS thread bridge. Each translate call assigns a monotonic requestId; partial results stream via onProgre",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a cross‑platform banners module with a single native queue. JS calls enqueue banners via showBanner; a native worker dequeues and renders one at a time, respecting each duration. Dismissal shou",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a TurboModule in Swift/Kotlin with a native CRDT core (OR-Set + sequence CRDT). Run a dedicated native worker to merge local edits with remote updates, persisting deltas to a per-document lo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To stream 3 sensors at 60 Hz, implement a native thread that samples accelerometer, gyroscope, and barometer into a fixed-size ring buffer, fuses the data on-device using a Kalman filter, and emits 60 Hz batches to JS v…",
            "isCorrect": true
          }
        ],
        "explanation": "## Interview Goal\n\nThis question evaluates the ability to design a cross-platform native module with multi-threading, on-device processing, and low-latency streaming to React Native. It requires knowledge of TurboModules/JSI, memory management, and power considerations.\n\n## Key Concepts\n\n- React Native TurboModules and JSI bridge for high-performance native-JS communication\n- Dedicated native worker thread with a ring buffer to handle backpressure\n- Sensor fusion using Kalman filtering with monotonic timestamps for temporal accuracy\n- Zero-copy data transfer via SharedArrayBuffer or direct buffer access\n- Battery and power management with proper thread synchronization\n\n## Code Example\n\n```javascript\n// Minimal API sketch\nSensorsStreamModule.startStream({ frequency: 60, sensors: ['accel', 'gyro', 'baro'] });\n\nSensorsStreamModule.onSample((sample) => {\n  // { data: Float32Array, timestamp: number, freshness: number }\n});\n\nSensorsStreamModule.pause();\nSensorsStreamModule.resume();\nSensorsStreamModule.stop();\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-7",
        "questionId": "q-2459",
        "question": "You're building a cross-platform React Native app that requires a native module 'TileCache' to prefetch and cache map tiles for offline use. Outline the architecture, how the JavaScript bridge would interact with the iOS and Android implementations, memory management and eviction strategy, and a minimal code sketch showing the JS API and a native bridge interface to fetch a tile by z/x/y within a viewport. Include testing considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a minimal API surface with a `createFeed({ endpoint, pageSize, cacheKey })` function that returns an interface with `refresh()`, `loadMore()`, and `onItemsChange(cb)`. Initialize a FeedModule that fetches paginat…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "TileCache is a TurboModule exposing preloadTiles(bounds, zoom), getTile(x,y,z), and clearCache. iOS (Swift) and Android (Kotlin) back the cache with a disk LRU, synchronized with a background task que",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a TurboModule that runs a dedicated native thread for a lightweight on-device model, feeding frames into a fixed-size ring buffer at the requested fps. Emit DetResult { label, score, bbox, t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a TurboModule with a dedicated worker thread that reads fused IMU data, runs a quantized model via NNAPI/Metal, and streams results back via a JS callback. Use double buffering, a ring buffe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nInterfacing JS with native modules across platforms is error-prone; this tests depth in native modules, memory, and caching.\n\n## Key Concepts\n\n- React Native bridges (JSI/TurboModules)\n- Cross-platform native modules (iOS/Android)\n- Disk + in-memory LRU cache and eviction\n- Background work and synchronization\n- Robust testing: misses, eviction, corruption, offline paths\n\n## Code Example\n\n```javascript\nexport type TileKey = { x: number; y: number; z: number; viewportId?: string };\nexport interface TileCacheApi {\n  preloadTiles(bounds: { ne: { lat: number; lng: number }; sw: { lat: number; lng: number } }, zoom: number): void;\n  getTile(x: number, y: number, z: number): Promise<string | null>;\n  clearCache(): void;\n}\n```\n\n## Follow-up Questions\n\n- How would you expose progress events and error codes to JS?\n- How would you test cache eviction under rapid viewport changes?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-8",
        "questionId": "q-6008",
        "question": "You're building a cross-platform React Native app used for enterprise document collaboration. Implement an offline-first, full-text search index for thousands of documents stored locally, with delta sync when online. Propose a Rust-native module bridged to RN to power indexing and search. Include data model, inverted index, tokenization, delta sync, and a JS API useIndexSearch(query, options) plus a minimal UI snippet. Address security, storage, and testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a Rust-native index module bridged to RN powering an offline-first full-text search over documents. Data model: inverted index with term dictionary, document IDs, field weights; tokenizer and",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use FlatList with data filtered by useMemo; renderItem wrapped in React.memo; keyExtractor returns stable ids; implement getItemLayout for fixed-height items; debounce search input to limit re-renders",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a native module that captures a fixed rear-camera feed, runs a small on-device model on a dedicated native thread, and streams gesture events to React Native at up to 30fps. Backpressure via t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "API sketch: startDocument(id, text), edit(id, delta), onRemoteEdit(id, delta), getText(id), closeDocument(id). Native module CRDTEditor (iOS/Android) uses per-device vector clocks and a deterministic",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nThis question probes knowledge of native modules, offline-first search, and practical indexing trade-offs in RN at scale.\\n\\n## Key Concepts\\n- React Native bridge (JSI) with Rust for perf\\n- Inverted index, tokenizer, stop-words, IDF-like weighting\\n- Delta sync, conflict handling, incremental indexing\\n- Encryption at rest, key management, access control\\n- Testing: indexing latency, correctness, offline UX\\n\\n## Code Example\\n```javascript\\n// Example API usage\\nconst hits = await index.useIndexSearch('design docs', {limit: 25, fuzzy: true});\\n```\\n\\n## Follow-up Questions\\n- How would you test index accuracy with evolving docs?\\n- How would you migrate index format across app versions?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-9",
        "questionId": "q-11711",
        "question": "Design and implement a cross‑platform Accordion component in React Native that renders a vertical list of sections (each with a header and hidden content). Only one section may be expanded at a time. Use Animated to interpolate height for smooth expand/collapse, measure content height on first expand, and expose a minimal API: Accordion({ sections }), toggleSection(index), and a usage sketch with a sample sections array. Include accessibility concerns and a small performance note?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "API: initialize(config), start(), stop(), pushInputFrame(buffer, ts), pullOutputFrame(ts), onError. Run on a dedicated native thread with lock-free ring buffers; target ~20 ms frames. Use iOS NSE/NSE",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement VibrateModule on Android (Kotlin) using Vibrator and VibrationEffect, and on iOS (Swift) via AudioServicesPlaySystemSound. In JS, expose vibrate(ms) via NativeModules.VibrateModule and fallb",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design and implement a minimal cross-platform React Native image picker bridge. API: initPicker(config), pickImage(), onResult(callback). On iOS use PHPhotoLibrary; on Android use MediaStore; request",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "```javascript import React, { useState, useRef, useCallback } from 'react'; import { View, Text, TouchableOpacity, Animated, Platform, UIManager, LayoutAnimation, UIManager, } from 'react-native'; if (Platform.OS === 'a…",
            "isCorrect": true
          }
        ],
        "explanation": "## Overview\nThis question evaluates React Native proficiency, specifically the Animated API, layout measurement, state management, and accessibility.\n\n## Key Concepts\n\n### 1. Animated Height Interpolation\n- Each section has an `Animated.Value` (0 = collapsed, 1 = expanded)\n- `interpolate()` maps the 0→1 input range to pixel heights (0→measured height)\n- `Animated.spring` provides smooth physics-based animation with configurable friction\n- `useNativeDriver: false` is required since height is a layout property\n\n### 2. Dynamic Content Measurement\n- Use `onLayout` on the content View to capture actual rendered height\n- Store measured heights in state to avoid re-measuring on subsequent renders\n- Fallback to minimal height (1) prevents interpolation errors before measurement\n- `LayoutAnimation.configureNext` ensures smooth transitions on Android\n\n### 3. Single Section Expansion\n- `openIndex` tracks the currently expanded section (null = all closed)\n- Clicking an open section closes it; clicking a different section switches\n- All other sections animate to 0 while the active one animates to 1\n\n### 4. Accessibility\n- `accessibilityRole=\"button\"` for interactive headers\n- `accessibilityState={{ expanded: }}` informs screen readers of state\n- `accessibilityLabel` provides descriptive text for touch targets\n- `accessibilityState={{ hidden: }}` on content indicates visibility to AT\n\n### 5. Performance Notes\n- `useCallback` and `useRef` prevent unnecessary re-renders\n- Heights are cached after first measurement to avoid repeated calculations\n- Consider `React.memo` for AccordionItem if sections are static\n- For many sections, use `FlatList` with optimized rendering",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-10",
        "questionId": "q-1065",
        "question": "You're building a React Native app for live collaboration that streams up to 4 simultaneous camera feeds using WebRTC. Describe end-to-end architecture for capture, encoding, and transport, how you'd implement backpressure and frame pacing to sustain ~30fps per feed, and provide a small implementation sketch (camera hook + simple backpressure queue) in code?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use four RN camera streams via WebRTC with separate RTCPeerConnections and tracks. Capture frames with VisionCamera, encode in a native module using hardware encoders, and push to the correct WebRTC t",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a GPU-accelerated frame pipeline on a dedicated native thread; use a compact ring buffer to hold frames, reuse buffers, and avoid per-frame allocations. Expose initPipeline, pushFrame, onPro",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a FlatList with numColumns determined by screen width (2 on phones, 3 on tablets). Render images in a grid with resizeMode='cover'. Implement a tiny in-memory LRU cache for image URLs; preload nea",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a NativeModule + EventEmitter for both platforms. iOS: enable battery monitoring and observe UIDeviceBatteryLevelDidChange/UIDeviceBatteryStateDidChange; emit {level, charging}. Android: reg",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world RN streaming with WebRTC, multi-sensor coordination, and performance under backpressure.\n\n## Key Concepts\n- WebRTC integration in React Native\n- Camera capture pipelines and encoders\n- Backpressure, frame pacing, queue management\n- Resource constraints on mobile devices and cross-platform differences\n\n## Code Example\n```javascript\nfunction createBackpressureQueue(limit){\n  const q = [];\n  return {\n    enqueue(f){ if (q.length < limit){ q.push(f); return true } return false; },\n    dequeue(){ return q.shift(); },\n    size(){ return q.length; }\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test backpressure behavior under network jitter?\n- What are the trade-offs between software vs. native encoding paths in RN?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-11",
        "questionId": "q-22474",
        "question": "Context: intermediate RN app needs on‑device NLP inference with a quantized transformer (distilBERT) for text summarization. Build a cross‑platform native module that runs inference with Core ML/ML Kit, streams tokens to React Native in real time, and supports pause/resume and OTA model updates. Design iOS/Android APIs (TurboModule/JSI), a minimal JS interface, and a usage snippet. Include privacy, memory, threading, and cleanup considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build a native module that reads the accelerometer on a dedicated background thread and streams data to JavaScript at up to 30 Hz via an onData callback. Expose a minimal API: startSensor({updateIntervalMs}), stopSensor…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a light cross‑platform Toast module. Android: overlay view with a queue to avoid overlaps; show(message, duration) and hide() run on the main thread. iOS: a singleton UIWindow hosting a UILabel",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a cross‑platform TurboModule that runs on‑device transformer inference with a quantized model. Expose: loadModel({modelUri, quantized, maxTokens}), inferStream(inputTokens, {onToken, onError",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Build as a TurboModule with a persistent background worker (WorkManager on Android, BGTask on iOS). Use SQLite to buffer outbound/inbound messages and AES-256-GCM to encrypt payloads. Track per-conver",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design RN native modules for on-device ML with streaming output, OTA updates, and privacy-conscious operation under memory constraints.\n\n## Key Concepts\n\n- TurboModule/JSI bridge patterns for ML inference\n- Streaming token delivery and backpressure\n- OTA model swapping and memory budgeting\n- Privacy and lifecycle cleanup\n\n## Code Example\n\n```javascript\n// Minimal usage\nimport { NativeModules } from 'react-native';\nconst { TransformerModule } = NativeModules;\n\nTransformerModule.loadModel({ modelUri: 'model://distilbert', quantized: true, maxTokens: 512 });\n\nTransformerModule.inferStream(['The', 'result'], {\n  onToken: t => console.log('tok', t),\n  onEnd: () => console.log('done')\n});\n```\n\n## Follow-up Questions\n\n- How would you handle model hot-swapping mid-stream?\n- What memory and battery safeguards would you implement and test?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-12",
        "questionId": "q-11664",
        "question": "Design a cross-platform React Native native module for real-time audio processing (iOS/Android) that runs on a dedicated native thread using JSI/TurboModule. It must capture PCM from the mic, apply noise suppression and AEC in under 20 ms, and expose a minimal JS API to push input frames and receive processed frames for live transmission. Outline the API, data flow, backpressure strategy, lifecycle management, and privacy considerations; how would you implement it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a tile-based GPU renderer with a dedicated render thread and a background tile loader. Maintain a fixed-size tile pool and a double-buffered atlas; pass tiles via a bounded queue to the rend",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a FlatList bound to a local array, filter with a query in useMemo, and memoize renderItem with React.memo. Use keyExtractor={i => i.id} and getItemLayout to a fixed height for smooth",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "API: initialize(config), start(), stop(), pushInputFrame(buffer, ts), pullOutputFrame(ts), onError. Run on a dedicated native thread with lock-free ring buffers; target ~20 ms frames. Use iOS NSE/NSE",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "API: unlockSession(options) -> Promise<void>, getSessionToken() -> Promise<string>, resetSession() -> void. Biometric prompts and secure storage via iOS Keychain/LocalAuthentication and Android Keysto",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design a real-time audio module with cross‑platform constraints, low latency, and privacy considerations.\n\n## Key Concepts\n- Real-time latency and frame sizing (≈20 ms)\n- Platform-native AEC/noise suppression libraries\n- TurboModule/JSI bridge efficiency\n- Lock-free buffers and backpressure\n- Lifecycle management and privacy practices\n\n## Code Example\n```javascript\n// Minimal usage sketch\nconst audioProc = AudioProcModule.initialize({ sampleRate: 48000, channels: 1 });\naudioProc.start();\naudioProc.pushInputFrame(frameBuffer, timestamp);\naudioProc.pullOutputFrame((buf, ts)=> {\n  // send over network or render\n});\n```\n\n## Follow-up Questions\n- How would you test latency and stability across devices?\n- How would you handle state restoration after app suspend?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-13",
        "questionId": "q-29268",
        "question": "Context: advanced React Native; you’re building a cross-platform native module that provides low-latency, real-time audio DSP for a multiplayer game. Run DSP on a dedicated native thread; expose a minimal JS API: startEngine(config), pushInputFrame(buffer, ts), onOutputFrame(callback), stopEngine. How would you design the thread model, lock-free ring buffers, sample-rate negotiation, and cross-platform fallbacks? Include a minimal API sketch and usage snippet, and discuss testing, profiling, and power implications?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build an offline-first image pipeline using a Rust-native module bridged to React Native. Store images locally with SQLite (ImageRecord: id, path, region, metadata, status, lastModified, syncVersion, checksum). The Rust…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a real-time native thread with real-time priority and a lock-free ring-buffer pair to shuttle audio frames between DSP and the JS-facing layer. Negotiate a fixed sample-rate (prefer 48kHz) with mi",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use four RN camera streams via WebRTC with separate RTCPeerConnections and tracks. Capture frames with VisionCamera, encode in a native module using hardware encoders, and push to the correct WebRTC t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use FlatList with data filtered by useMemo; renderItem wrapped in React.memo; keyExtractor returns stable ids; implement getItemLayout for fixed-height items; debounce search input to limit re-renders",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a high-performance real-time audio module in RN, focusing on thread model, memory management, cross-platform constraints, and testing approach.\n\n## Key Concepts\n- Real-time audio thread priority and scheduler hints on iOS/Android\n- Lock-free ring buffers and memory reuse\n- Sample-rate negotiation and dynamic resampling\n- TurboModule/JSI bridging to minimize latency\n\n## Code Example\n```javascript\n// Minimal API usage sketch (pseudo)\nawait NativeAudioEngine.startEngine({sampleRate:48000, channels:2});\nawait NativeAudioEngine.pushInputFrame(inputBuffer, timestamp);\nNativeAudioEngine.onOutputFrame((frame, ts)=>{ play(frame); });\nawait NativeAudioEngine.stopEngine();\n```\n\n## Follow-up Questions\n- How would you verify latency under thermal throttling?\n- How to unit-test the DSP path without audio hardware?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-14",
        "questionId": "q-27736",
        "question": "Context: a React Native video call app requires live captions while preserving user privacy. Design a cross‑platform native module that runs on‑device ASR and streams text chunks to React Native with latency ~200 ms. Include partial results, final results, and optional speaker diarization. Propose native APIs (iOS/Android), a minimal JS interface (startStream(config), onChunk, onFinal, stop), and a usage snippet. Address privacy and backpressure?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a TurboModule exposing startPoseStream(config) and stopPoseStream(). Run a dedicated native thread with a fixed 120 Hz loop that fuses ARKit/ARCore pose data with IMU streams, emitting compact",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implementation sketch: run a dedicated native STT thread, feed mic input into a streaming audio buffer, use iOS Speech framework and Android on‑device models; emit onChunk with text and timestamp, and",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Measure frame times with a small hook using a blind-spot: requestAnimationFrame to log delta times during scroll, then wrap the item component in React.memo and stabilize props. Tune FlatList: windowS",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a CRDT-based diff model: every shape has a unique ID, and edits emit non-destructive patches that merge deterministically. Persist locally using SQLite with a dedicated sync table for pending patches, then stream pa…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design on‑device streaming ASR, privacy guarantees, and bridge design between native code and React Native. Requires thinking about latency budgets, thread model, and backpressure.\n\n## Key Concepts\n\n- on‑device ASR\n- streaming partial and final results\n- speaker diarization (optional)\n- backpressure and audio buffering\n- TurboModule/JSI bridge and lifecycle\n\n## Code Example\n\n```javascript\n// Usage sketch\nimport { startStream, stopStream, onChunk, onFinal } from 'react-native-stt';\nconst config = { language: 'en-US', diarization: true };\nstartStream(config);\nonChunk((c)=>{ /* update live captions */ });\nonFinal((f)=>{ /* final utterance */ });\n```\n\n## Follow-up Questions\n\n- How would you test latency under CPU load?\n- How would you extend for multi-language switching at runtime?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-15",
        "questionId": "q-5906",
        "question": "You're building a React Native e‑commerce app that must display a responsive product grid on phones and tablets. How would you implement a FlatList-driven grid that adapts numColumns to screen width, minimizes reflows, and remains smooth during rotation? Provide a small hook useNumColumns() and a minimal renderItem, plus any performance tweaks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Provide a TurboModule with startCapture(config) and stopCapture(), and onSpectrum(callback) to emit 60 Hz spectrum data. Use a real-time DSP thread and a double-buffered ring buffer shared with the br",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a LocalAuditLog backed by SQLite to store signed records. Sign each entry with Ed25519 (TweetNaCl) using a per-tenant device key, include a timestamp and previous hash for chaining. Batch 50–100 r",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement useNumColumns() that derives columns from width breakpoints (e.g., 2 for narrow, 3–4 for tablet). Use FlatList with numColumns={useNumColumns()} and a stable keyExtractor. Provide getItemLay",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a cross-platform TurboModule that maintains a TLS WebSocket to a market feed on a dedicated native thread, with per-channel ring buffers and a 2s replay window after reconnect. JS API: connect(",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical RN layout skill: building a responsive grid with FlatList, ensuring smooth rotations, and avoiding layout thrash. It also evaluates performance-minded patterns like getItemLayout and memoization.\n\n## Key Concepts\n- FlatList numColumns and dynamic layouts\n- Responsive breakpoints with useWindowDimensions\n- getItemLayout for fixed heights\n- Memoization of renderItem and stable keys\n- Orientation changes and re-render strategy\n\n## Code Example\n```javascript\nimport { useWindowDimensions, FlatList } from 'react-native'\nimport React, { useMemo } from 'react'\n\nexport function useNumColumns(breaks = [400, 800, 1200]) {\n  const { width } = useWindowDimensions()\n  return useMemo(() => {\n    if (width < breaks[0]) return 2\n    if (width < breaks[1]) return 3\n    if (width < breaks[2]) return 4\n    return 5\n  }, [width, breaks])\n}\n\nfunction Item({ item }) {\n  // simple render, assume fixed height for getItemLayout\n  return <ProductCard data={item} />\n}\n\nexport function ProductGrid({ data }) {\n  const numColumns = useNumColumns()\n  const ITEM_HEIGHT = 180\n  const getItemLayout = (_, index) => (\n    { length: ITEM_HEIGHT, offset: ITEM_HEIGHT * index, index }\n  )\n\n  const renderItem = useCallback(({ item }) => <Item item={item} />, [])\n\n  return (\n    <FlatList\n      data={data}\n      renderItem={renderItem}\n      keyExtractor={item => item.id}\n      numColumns={numColumns}\n      getItemLayout={getItemLayout}\n      initialNumToRender={10}\n      maxToRenderPerBatch={10}\n      windowSize={21}\n    />\n  )\n}\n```\n\n## Follow-up Questions\n- How would you test on different devices and during rotation?\n- How would you adjust for dynamic image heights within grid items?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-16",
        "questionId": "q-4742",
        "question": "You're building a beginner-friendly React Native app that displays a grid of product cards. Implement a reusable Card component with accessible labels for iOS and Android, support a light/dark theme via the Appearance API, and provide a small hook useTheme() to expose the current theme. Show minimal usage and discuss accessibility considerations and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a local emoji index (char,name) in memory. Create useEmojiSearch(query) that debounces input (~150ms) and uses useMemo to derive results by name.includes(query.toLowerCase()). Use React.memo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "startSync(config) initializes a per-user SQLite store and registers a background sync task; enqueueDelta(table, delta) queues local mutations for later propagation; onConflict(callback) allows JavaScript to handle confl…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a native spatial index (R-tree) to bucket 1k devices by lat/lon and expose updates through a TurboModule. On JS, implement a backpressure-enabled queue that batches updates by map tiles and thrott",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a reusable Card component as a Pressable with accessibilityLabel derived from props, accessibilityHint 'View details', and accessibilityRole 'button'. Bind colors to a theme via a useTheme() ho",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical RN skills: building reusable components, accessibility across platforms, and theming with Appearance API, plus a small hook pattern.\n\n## Key Concepts\n\n- Accessibility props (accessibilityLabel, accessibilityHint, accessibilityRole)\n- Theming with Appearance/useColorScheme and React Context\n- Custom hook useTheme and ThemeProvider\n- Performance considerations for re-renders and memoization\n\n## Code Example\n\n```javascript\nimport React, {createContext, useContext, useMemo, useState} from 'react';\nimport {Pressable, Text} from 'react-native';\n\nexport const ThemeContext = createContext('light');\nexport const useTheme = () => useContext(ThemeContext);\n\nexport function ThemeProvider({children}) {\n  // Simplified example; in real code use Appearance or useColorScheme\n  const [scheme] = useState('light');\n  const value = useMemo(() => ({scheme}), [scheme]);\n  return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;\n}\n\nfunction Card({title, price}) {\n  const {scheme} = useTheme();\n  const bg = scheme === 'dark' ? '#222' : '#fff';\n  return (\n    <Pressable accessibilityLabel={title}\n               accessibilityHint=\"View details\"\n               accessibilityRole=\"button\"\n               style={{backgroundColor: bg}}>\n      <Text>{title}</Text>\n      <Text>{price}</Text>\n    </Pressable>\n  );\n}\n```\n\n## Follow-up Questions\n\n- How would you test accessibility coverage across iOS and Android?\n- How would you extend theming to allow user-selected custom colors without breaking contrast?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-17",
        "questionId": "q-25274",
        "question": "Context: beginner React Native; you’re designing a small cross-platform native module that exposes a minimal API to fetch the device's current locale and subscribe to locale changes to refresh UI strings across the app. API: getLocale(), onLocaleChange(callback), stopListener(). Provide a concise usage snippet and discuss memory management and thread safety—how would you implement this and ensure safe cleanup?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt offline-first strategy using WatermelonDB or Realm to store tasks and media locally. Implement a background SyncWorker that batches local changes, fetches remote deltas, and merges conflicts wit",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: implement a small cross-platform native bridge with a singleton preference store. Persist data in Android SharedPreferences / iOS NSUserDefaults, JSON.stringify values, and expose setValue, getV",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I’d implement a small native module using RN's NativeModules and EventEmitter. On iOS: use NSLocale.current.languageCode and NotificationCenter for LocaleDidChange; Android: use Locale.getDefault and",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a cross-platform native module using JSI/TurboModules that subscribes to a WebSocket feed, writes ticks into a native ring buffer, and exposes usePriceStream(ticker, { Hz, bufSize }) returning { price, ts }. T…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a minimal cross‑platform native bridge with lifecycle awareness and cleanup.\n\n## Key Concepts\n\n- RN native module basics, EventEmitter binding\n- iOS Locale APIs and Android Locale APIs\n- Threading, callback lifecycles, memory management\n\n## Code Example\n\n```javascript\nimport { NativeModules, NativeEventEmitter } from 'react-native';\nconst { LocaleBridge } = NativeModules;\nconst emitter = new NativeEventEmitter(LocaleBridge);\nlet locale = LocaleBridge.getLocale();\nemitter.addListener('localeChanged', (l) => { locale = l; /* refresh UI */ });\nLocaleBridge.stopListener();\n```\n\n## Follow-up Questions\n\n- How would you handle batched locale changes during rapid toggling?\n- What testing strategies would you use for multi‑locale apps?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-18",
        "questionId": "q-15596",
        "question": "Design a cross-platform React Native native module that runs a lightweight on-device gesture recognizer (e.g., a rear-camera swipe gesture) for a DoorDash driver app. Expose a JS API to start/stop, configure sensitivity, and emit events {gestureName, confidence, timestamp} up to 30fps. Specify platform-native APIs (iOS CoreML/Vision vs Android ML Kit/TensorFlow Lite), threading, privacy prompts, and a minimal usage snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a TurboModule with startStream(config), onFrame(callback), sendControl(cmd), stopStream. Run the USB I/O on a dedicated native thread, push frames into a ring buffer, and deliver via a JS callb",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a TurboModule on iOS and a parallel native module on Android exposing subscribeToSymbol(symbol, onTick) and unsubscribe. Use per-symbol seqNo with a native ring buffer to ensure in‑order del",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement as a TurboModule using Metal on Apple platforms to run real‑time denoising in a dedicated native thread. Expose useImageDenoise({ enabled, resolution, strength }) => { frameStream } (JSI bri",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a native module that captures a fixed rear-camera feed, runs a small on-device model on a dedicated native thread, and streams gesture events to React Native at up to 30fps. Backpressure via t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nInterviews often test real-world integration of native ML with RN, strict latency, and privacy.\n\n## Key Concepts\n- On-device ML with platform-specific stacks\n- Dedicated native thread for inference\n- JS bridge backpressure and event semantics\n- Privacy prompts and lifecycle cleanup\n\n## Code Example\n```javascript\n// Minimal JS bridge sketch\nclass GestureModule {\n  start(config: { gesture: string; fps?: number; sensitivity: number }, cb: (e: any) => void): void;\n  stop(): void;\n}\n```\n\n## Follow-up Questions\n- How would you measure and bound tail latency?\n- How to handle model updates and hot-swapping without app restart?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-19",
        "questionId": "q-26472",
        "question": "Context: remote wildlife monitoring app on mobile using React Native. Design a cross‑platform native module that uses iOS Metal and Android Vulkan to apply a real‑time image processing pipeline (denoise → edge‑detect → compression) to a live camera feed at 24–30fps. The module should stream processed frames back to React Native as textures, support pause/resume, dynamic pipeline reconfiguration, and backpressure handling. Provide the native API (TurboModule/JSI), a minimal JS interface, and a usage sketch. Consider latency, memory, and power?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Realm for offline data graph (Checklist, Items, Attachments) with a per-record revision. Local SyncQueue holds pending mutations. Detect connectivity via NetInfo; a background worker batches and a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a TurboModule with a native frame queue and two GPU paths (Metal on iOS, Vulkan on Android). Expose startPipeline(config), submitFrame(buf,w,h), onProcessed, updatePipeline, pause, resume, s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To maintain smooth scrolling with 1,000+ items, leverage FlatList's virtualization features: getItemLayout for predictable heights, initialNumToRender to control initial load, maxToRenderPerBatch to limit concurrent ren…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design and implement a minimal cross-platform React Native image picker bridge. API: initPicker(config), pickImage(), onResult(callback). On iOS use PHPhotoLibrary; on Android use MediaStore; request",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cross‑platform, high‑throughput native modules with GPU work, ensuring low latency, controlled memory, and robust backpressure in a React Native app. It also probes integration details of TurboModules/JSI, texture sharing, lifecycle management, and power budgeting across iOS and Android.\n\n## Key Concepts\n\n- Cross‑platform GPU compute integration (Metal, Vulkan) in RN bridges\n- Real‑time streaming with backpressure and frame lifecycle\n- Texture sharing between native code and React Native UI\n- Memory management, synchronization, and power budgeting\n- Lifecycle cleanup and error handling across platforms\n\n## Code Example\n\n```javascript\n// Pseudo usage\nstartPipeline({width:1280,height:720,denoise:0.5,edgeThreshold:0.3})\nsubmitFrame(frameBuffer, w, h)\nonProcessed((textureHandle, ts) => renderTexture(textureHandle))\nupdatePipeline({denoise:0.8})\npause()\nresume()\nst#op()\n```\n\n## Follow-up Questions\n\n- How would you measure and bound latency across both platforms?\n- How would you handle a sudden camera frame rate drop (e.g., device stalls) without crashing?",
        "difficulty": "intermediate"
      },
      {
        "id": "react-native-q-20",
        "questionId": "q-13053",
        "question": "Design a cross-platform React Native native module that runs an on-device anomaly detector for real-time fleet telemetry. The module should process GPS, accelerometer, and vehicle sensors on a dedicated worker thread, run a lightweight ML model (e.g., isolation forest) for online inference, and emit anomaly events to React Native with configurable batching/backpressure (up to 20 Hz). Include API sketches for startProfile(config), setModel(modelBytes), pause(), resume(), onAnomaly, flushBuffer, and a minimal usage snippet. Address lifecycle, offline buffering, and privacy concerns?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement a small native module using RN's NativeModules and EventEmitter. On iOS: use NSLocale.current.languageCode and NotificationCenter for LocaleDidChange; Android: use Locale.getDefault and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a TurboModule that runs an on-device anomaly detector for real-time fleet telemetry, processing GPS, accelerometer, and vehicle sensors on a dedicated worker thread. Expose API: startProfile(co",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Build a light cross‑platform Toast module. Android: overlay view with a queue to avoid overlaps; show(message, duration) and hide() run on the main thread. iOS: a singleton UIWindow hosting a UILabel",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a dedicated native audio thread at 48kHz with circular buffers and a lightweight AEC/NS path. Bridge to React Native via a minimal JSI API: startEngine(config), joinRoom(roomId, opts), onAudioFram",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cross‑platform native modules for real-time telemetry with offline/batching concerns and privacy.\n\n## Key Concepts\n\n- TurboModule/JSI bridge\n- Worker threads and backpressure\n- On‑device ML inference\n- Offline buffering and privacy\n\n## Code Example\n\n```javascript\n// Minimal usage sketch for the native module\nimport {FleetTelemetryDetector} from 'react-native';\nFleetTelemetryDetector.startProfile({ samplingRateHz: 20, offlineBufferSize: 1024 });\nconst modelBytes = /* binary ML model */;\nFleetTelemetryDetector.setModel(modelBytes);\nFleetTelemetryDetector.onAnomaly((ev) => console.log('Anomaly:', ev));\nFleetTelemetryDetector.resume();\n```\n\n## Follow-up Questions\n\n- How would you ensure deterministic latency at 20 Hz with bursty sensor data?\n- How would you test backpressure and offline buffering integrity?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-21",
        "questionId": "q-22781",
        "question": "Context: advanced React Native; you’re building a cross-platform native module that streams automotive CAN bus telemetry from a vehicle network to React Native for real-time dashboards. Requirements: dedicated native IO thread for CAN reads, a lock-free ring buffer, and a JS bridge via JSI/TurboModules. API: startTelemetry(config), onData(callback), setFilters(params), pauseTelemetry, stopTelemetry. Provide a minimal API sketch, a usage snippet, and discuss backpressure, memory management, and fault tolerance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt an offline-first data layer (WatermelonDB or Realm) for POIs and a disk tile cache. Implement incremental sync: push local POI deltas, pull server updates, and resolve conflicts using server-tim",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a cross‑platform native module (TurboModule/JSI) that polls battery level on Android (BatteryManager) and iOS (UIDevice) on a background thread, emitting { level, ts } and a boolean belowThr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a CAN telemetry native module with a dedicated IO thread, a lock-free ring buffer shared with the JS bridge, and a minimal API: startTelemetry(config), onData(callback), setFilters(params), pau",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a TurboModule with a dedicated native thread feeding a lock-free ring buffer. Expose JS API: startFeed(config), onTick(callback), updateRegion(params), stopFeed. Bridge via JSI; render with GPU he",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-time telemetry requires careful threading, low-latency data transfer to JS, and predictable memory use. This probes native thread management, lock-free structures, backpressure, and cross‑platform bridge design.\n\n## Key Concepts\n- Real-time data streams\n- Lock-free ring buffers\n- JSI/TurboModules bridges\n- Cross-platform memory management and fault tolerance\n\n## Code Example\n```javascript\n// Pseudo API usage\nTelemetry.startTelemetry({bus: 'can0', baud: 500000});\nTelemetry.setFilters({ids: ['0x100','0x200']});\nTelemetry.onData((pkt)=>{ /* render in UI */ });\n```\n```\n\n## Follow-up Questions\n- How would you validate backpressure in production?\n- How would you test bursts and frame drops from CAN traffic?\n- What changes if the CAN bridge disconnects intermittently?",
        "difficulty": "advanced"
      },
      {
        "id": "react-native-q-22",
        "questionId": "q-20766",
        "question": "Context: beginner React Native; Scenario: in a mobile wallet app, build a screen listing coupons fetched from a public API. It should render with FlatList, support search filtering, pull-to-refresh, and offline caching with AsyncStorage. Provide a minimal component tree, key props, and a plan for cache invalidation and iOS/Android compatibility?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Store locally in WatermelonDB/SQLite with fields: id, formId, fields (JSON), version, dirty, attachments. On save, increment version and mark dirty. Sync with POST /forms/{id}/sync; conflicts resolved",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a native module that captures a fixed rear-camera feed, runs a small on-device model on a dedicated native thread, and streams gesture events to React Native at up to 30fps. Backpressure via t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "TurboModule with useOrderBookStream(symbol) => { price: number, size: number, side: 'bid'|'ask', ts: number } on a dedicated native thread feeding a circular buffer. When full, drop oldest (backpressu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: create CouponList with state for data, query, refreshing. On mount, load cached data from AsyncStorage, then fetch from API. Use FlatList data filtered by query. Implement pull-to-refresh by set",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nPractical beginner task that mirrors real app patterns: data loading, caching, and offline UX.\n\n## Key Concepts\n\n- FlatList usage with data props\n- AsyncStorage caching with versioned keys\n- Offline-first UX with fallback data\n- Simple search filtering\n\n## Code Example\n\n```javascript\nimport React, { useEffect, useState } from 'react';\nimport { FlatList, TextInput, View, Text } from 'react-native';\nimport AsyncStorage from '@react-native-async-storage/async-storage';\n\nconst API = 'https://example.com/api/coupons';\nexport default function CouponList() {\n  const [data, setData] = useState([]);\n  const [query, setQuery] = useState('');\n  const [refreshing, setRefreshing] = useState(false);\n\n  async function loadCache() {\n    const cached = await AsyncStorage.getItem('coupons_v1');\n    if (cached) setData(JSON.parse(cached));\n  }\n\n  async function fetchData() {\n    try {\n      const resp = await fetch(API);\n      const json = await resp.json();\n      setData(json);\n      await AsyncStorage.setItem('coupons_v1', JSON.stringify(json));\n    } catch (e) {\n      // offline or error\n    }\n  }\n\n  useEffect(() => { loadCache(); fetchData(); }, []);\n\n  const filtered = data.filter(c => c.title.toLowerCase().includes(query.toLowerCase()));\n  return (\n    <View>\n      <TextInput value={query} onChangeText={setQuery} placeholder=\"Search coupons\" />\n      <FlatList data={filtered} keyExtractor={item => item.id} renderItem={({item}) => <Text>{item.title}</Text>} />\n    </View>\n  );\n}\n```\n\n## Follow-up Questions\n\n- How would you test offline caching on a device?\n- How would you extend cache invalidation when API data changes?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-23",
        "questionId": "q-4831",
        "question": "You're building a beginner-friendly React Native chat app with offline support. Outline a minimal local-first layer using SQLite to store messages, with a timestamp-based conflict resolution (latest wins) when syncing with the server. Implement a useChatSync(conversationId) hook that queues outgoing messages, retries failed sends with exponential backoff, and batches receipt/status updates. Include a small UI snippet showing sending and optimistic rendering?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Tail a log file on a background thread, buffering entries in a fixed-size ring buffer before batching them to JavaScript via the native bridge (e.g., DirectEvent). Use a serial dispatch queue to ensure ordering. Handle…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use SQLite to store messages locally with fields (id, convoId, text, sender, ts, status). On sync, resolve conflicts by taking the newer timestamp. The useChatSync(conversationId) hook maintains a que",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a minimal API surface with a `createFeed({ endpoint, pageSize, cacheKey })` function that returns an interface with `refresh()`, `loadMore()`, and `onItemsChange(cb)`. Initialize a FeedModule that fetches paginat…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a TurboModule with a native worker per platform and a shared TF Lite interpreter. StartStream loads a model into a dedicated thread, maintains a bounded in-memory queue for frames, and posts",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design offline-first data flow using a simple local store, conflict resolution, and a hook to manage network-retry semantics. It also touches UI implications for optimistic rendering.\n\n## Key Concepts\n\n- SQLite as local store\n- timestamp-based conflict resolution\n- message queue with exponential backoff\n- optimistic UI\n\n## Code Example\n\n```javascript\n// Minimal sketch\nexport function useChatSync(conversationId) {\n  // queue, retry, and sync logic\n}\n```\n\n## Follow-up Questions\n\n- How extend to handle deletions and edits?\n- How test offline-first reliability?",
        "difficulty": "beginner"
      },
      {
        "id": "react-native-q-24",
        "questionId": "q-5314",
        "question": "You're building a high-frequency React Native dashboard for quantitative traders that streams market data at 500–1000 updates/sec. Propose an architecture to handle data ingestion, local caching, and UI rendering without dropping frames. Specifically, design a native Rust-based module bridged via JSI to perform in-process data processing and a React Native listener that updates a FlatList with minimal GC pressure. Include data model, synchronization strategy, and a small hook sketch useLiveMarketStream() and a minimal UI snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a TurboModule called GPUPointCloud that spawns a dedicated native rendering thread. Use double-buffered device buffers paired with a staging area for JS data; swap and upload asynchronously each frame via Metal o…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Rust-native module (JSI/TurboModule) with a single ingest thread and a zero-copy ring buffer feeding JS. Handle 500–1000 updates/sec, batch serialize, and minimize allocations to reduce GC pa",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage a native toast manager with a queue exposed via a NativeModule (showToast, hideToast) and a JS listener. iOS: post accessibility announcements with UIAccessibility.postNotification. Android:",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a LocalAuditLog backed by SQLite to store signed records. Sign each entry with Ed25519 (TweetNaCl) using a per-tenant device key, include a timestamp and previous hash for chaining. Batch 50–100 r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesss system design under high-frequency data, native module interoperability, and performance debugging in RN. Focus on Rust interop, back-pressure, memory budgets, and test strategy.\n\n## Key Concepts\n- React Native TurboModules and JSI\n- Native thread isolation and back-pressure\n- Zero-copy data sharing and ring buffers\n- FlatList virtualization and key stability\n- GC pressure and memory budgeting\n\n## Code Example\n```javascript\nfunction useLiveMarketStream() {\n  // placeholder hook that subscribes to native events\n  // returns an iterator or array of latest ticks for rendering\n}\n```\n\n## Follow-up Questions\n- How would you verify zero-copy transfers across the JS/N-API boundary?\n- What instrumentation would you add to detect dropped frames and memory spikes?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-28T10:41:06.963Z",
    "version": 1
  }
]