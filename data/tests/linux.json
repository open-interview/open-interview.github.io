[
  {
    "id": "test-linux",
    "channelId": "linux",
    "channelName": "Linux",
    "title": "Linux Knowledge Test",
    "description": "Test your understanding of Linux with 25 questions.",
    "questions": [
      {
        "id": "linux-q-0",
        "questionId": "q-13118",
        "question": "Scenario: A Linux host running a service mesh with many short‑lived containers experiences tail latency spikes (50–200 ms) during bursts. You suspect THP allocator stalls on frequent small allocations. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm THP stalls and correlate with THP activity, (2) identify processes/containers triggering hugepage allocations and their NUMA mappings, and (3) apply a safe mitigation (disable THP, tune nr_hugepages, or adjust allocator hints) while preserving observability. Include exact commands and outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Diagnose with slabtop -o C, cat /proc/slabinfo | head, free -m, vmstat 1 60. If reclaim spikes align with latency, enable zone_reclaim_mode: echo 1 > /proc/sys/vm/zone_reclaim_mode; raise min_free_kby",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify throttled container by CPU quotas. Commands: ps -eo pid,pcpu,cmd --sort=-pcpu | head -n 8 for pid in $(ps -eo pid --sort=-pcpu | head -n 6); do echo PID=$pid; awk -F: '{print $3}' /proc/$pid/",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Verify THP state and defrag; monitor THP counters in /proc/vmstat and THP messages in dmesg; map allocations to PIDs with for pid in /proc/[0-9]*; do [ -r \"$pid/cgroup\" ] && echo $pid; done and cross-",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Begin with a system-wide view, then drill down: - top -b -n1 - vmstat 1 - iostat -xz 1 - pidstat -p ALL 1 Compare CPU idle, IO wait, and memory pressure. If IO-wait is high, enable BFQ (or deadline) a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests diagnosing kernel memory allocator issues under containerized workloads, a subtle root-cause problem often missed by perf counters. THP interactions matter in bursty, high‑allocation environments.\n\n## Key Concepts\n- Transparent Huge Pages state and defrag\n- THP-related vmstat/vmstat counters and dmesg signals\n- Mapping per-process memory behavior to containers via /proc and cgroups\n\n## Code Example\n```javascript\n#!/bin/bash\n# Disable THP as a quick mitigaton example\necho never > /sys/kernel/mm/transparent_hugepage/enabled\n```\n\n## Follow-up Questions\n- If disabling THP helps, what long-term tuning would you apply in a multi-tenant cluster?\n- How would you observe ongoing THP activity without impacting performance measurements?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-1",
        "questionId": "q-19799",
        "question": "Scenario: A Linux host running a streaming API with heavy WAL I/O on NVMe reports intermittent tail latency spikes during bursts, not obvious from throughput. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove IO scheduler or disk bandwidth saturation as the cause, (2) locate the offending block device and the processes driving IO, and (3) apply safe mitigations (switch IO scheduler to a more suitable option, isolate IO with cgroups, tune dirty ratios, and verify observability) with sample commands and outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect IO metrics with iostat -xd 1; detect high await and queue depth; inspect /sys/block/nvme0n1/queue/scheduler; map I/O to PIDs with pidstat -d 1; switch to a better scheduler via echo deadline >",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Run `df -h /tmp` and `df -i /tmp` to differentiate between disk space exhaustion and inode exhaustion. If inode usage approaches 100% while disk space remains available, identify the culprits with: ```bash sudo find /tm…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Monitor latency windows and IRQ pressure to confirm interrupts as root cause; correlate spikes with /proc/interrupts using: watch -n0.5 'cat /proc/interrupts'; identify device by mapping IRQs to net d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use `lsof -i :8080` to list processes on port 8080, then `kill -15 <PID>` for graceful shutdown. If unresponsive, use `kill -9 <PID>` as last resort. Always check process dependencies first.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to diagnose storage-induced tail latency using only built-in tools, without relying on perf or vendor utilities.\n\n## Key Concepts\n- IO schedulers: CFQ vs Deadline\n- I/O metrics: iostat, await, svctm\n- Process-to-device I/O mapping: pidstat\n- Safe mitigations: switch schedulers, cgroup IO limits, page cache tuning\n\n## Code Example\n```javascript\n// Bash-like commands to reproduce steps\necho deadline > /sys/block/nvme0n1/queue/scheduler\niostat -xd 1\npidstat -d 1\n```\n\n## Follow-up Questions\n- How would you extend this for multiple devices?\n- What observability would you add to ensure future regressions are caught quickly?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-2",
        "questionId": "q-20720",
        "question": "Scenario: A Linux server runs a nightly backup daemon started via systemd timer. Some nights backups fail with 'Permission denied' when writing to /var/backups. Using only default Linux tools, design a diagnostic workflow to (1) confirm permissions/ACLs are the cause, (2) identify the exact process and user writing, and (3) apply safe mitigations (fix ownership/permissions or ACLs, or run as dedicated user) while preserving observability. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run systemctl list-timers --all to locate batch.timer; use journalctl -u batch.timer --since \\\"7 days ago\\\" to see last triggers; inspect unit via systemctl status batch.timer batch.service; check dep",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check logs: journalctl -u backup.service -n 100; verify path perms: ls -ld /var/backups; stat /var/backups; identify writer: lsof +D /var/backups | head; fix: sudo chown -R backup:backup /var/backups;",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Instrument with per-CPU freq sampling during bursts: watch -n0.2 'for c in /sys/devices/system/cpu/cpu*/cpufreq/cpuinfo_cur_freq; do echo -n \"CPU$(basename $c): \"; cat $c; done'. If freq dips align wi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a workflow: (1) trace GPU tail with trace-cmd record -e sched_switch -e irq_handler_entry -e irq_handler_exit -e softirq -a --duration 15s during bursts; (2) pidstat -p ALL 1 to map latency to",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses the ability to perform practical, low-risk debugging of permission-related failures using only native Linux tools, with a real nightly job.\n\n## Key Concepts\n\n- systemd timers and services\n- filesystem permissions and ACLs\n- process discovery with lsof/fuser, log inspection with journalctl\n- safe remediation and observability\n\n## Code Example\n\n```javascript\n// Placeholder pseudo-steps for scripting the checks\n```\n\n## Follow-up Questions\n\n- How would you scale this to multiple servers and automate checks?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-3",
        "questionId": "q-7551",
        "question": "Scenario: A Linux host runs Redis in a container and a data-processor in another container. Under peak load, tail latency spikes in Redis and memory bandwidth saturates on NUMA node0. Using only default tools, design a concrete diagnostic workflow to (1) confirm CPU/memory pressure is the cause, (2) pinpoint the exact CPUs and NUMA nodes involved, and (3) apply safe mitigations (e.g., pin critical services with cpuset, adjust CPU frequency governor, disable THP, enforce memcg limits) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use systemd-analyze blame to find the slow boot unit, then systemd-analyze critical-chain to map dependencies. Inspect logs with journalctl -b -u <service> and dmesg for hardware waits. For mitigation",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use: mpstat -P ALL 1; pidstat -p $(pidof redis-server) 1; iostat -xd 1; vmstat 1; numastat 1; top -b -d 1 -n 1 -H. If pressure is confirmed, pin Redis to CPUs 2-5 on node0 with a cpuset, set governor",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To diagnose: run iostat -x 1 2; look for high await and iowait on the MongoDB NVMe device. Inspect the block scheduler with cat /sys/block/nvme0n1/queue/scheduler and switch to deadline via echo deadl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use iostat -dx 1 2 and vmstat 1 2 to observe IO stalls; inspect schedulers with cat /sys/block/sd*/queue/scheduler; map I/O to PIDs with pidstat -d 1; switch to mq-deadline: echo mq-deadline > /sys/bl",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests depth in diagnosing production Linux issues with no specialized tools, emphasizing observability, NUMA awareness, and safe mitigations.\n\n## Key Concepts\n- CPU affinity and cpuset\n- NUMA topology and memcg limits\n- I/O and memory bandwidth patterns\n- Observability and safe containment\n\n## Code Example\n```bash\n# Example monitoring commands\nmpstat -P ALL 1\npidstat -p $(pidof redis-server) 1\n```\n```\n## Follow-up Questions\n- What would you do if latency remains after pinning and THP is already disabled?\n- How would you adapt this workflow for Kubernetes pods and cgroup v2?\n```",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-4",
        "questionId": "q-21570",
        "question": "Scenario: A Linux server hosting a high-traffic REST API shows tail latency spikes (50–200 ms) during bursts, while averages look fine and no I/O bottlenecks are evident. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove stalls correlate with CPU frequency transitions and turbo state, (2) identify the core(s) and thread(s) affected, and (3) apply safe mitigations (pin critical threads, set CPU governor to performance, isolate CPUs, or disable turbo) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Prove latency stems from per-cgroup CPU throttling and fix safely. Commands: 1) cat /sys/fs/cgroup/kubepods.slice/cpu.stat 2) cat /sys/fs/cgroup/kubepods.slice/cgroup.procs 3) ps -p $(cat /sys/fs/cgr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument with per-CPU freq sampling during bursts: watch -n0.2 'for c in /sys/devices/system/cpu/cpu*/cpufreq/cpuinfo_cur_freq; do echo -n \"CPU$(basename $c): \"; cat $c; done'. If freq dips align wi",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Begin by confirming IO wait and tail latency with: iostat -dx 1 5; pidstat -d 1; iotop -b -n 1. Identify contending device from /proc/diskstats and per-device scheduler in /sys/block/*/queue/scheduler",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Begin with systemd wakeups: run systemd-analyze blame and systemctl list-timers --all to spot frequent wakeups. Then map spikes to a pid with pidstat -p ALL 1 and check I/O with iostat -xz 1. If a tim",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical debugging under latency budgets, demanding familiarity with CPU frequency scaling, turbo dynamics, and CPU isolation in production Linux setups.\n\n## Key Concepts\n- CPU frequency scaling and turbo boost behavior\n- CPU governors and performance vs. power trade-offs\n- Thread pinning (sched_affinity) and CPU isolation (isolcpus)\n- Observability using simple, widely available tools\n\n## Code Example\n```bash\n# Simple live freq sampling across all CPUs\nwhile true; do echo -n \"$(date +%s)\"; for c in /sys/devices/system/cpu/cpu*/cpufreq/cpuinfo_cur_freq; do echo -n \"  $((1000#$c)):\"; cat $c; done; echo; sleep 0.5; done\n```\n\n## Follow-up Questions\n- How would you automate detection and mitigation across multiple hosts?\n- What are NUMA implications when isolating CPUs on a multi-socket system?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-5",
        "questionId": "q-4844",
        "question": "On a Linux server hosting a Redis cluster, tail latency spikes under sustained write load while CPU and memory look normal. Using only default tools, design a concrete diagnostic workflow to (1) confirm if tail latency is due to I/O wait, NIC queuing, or page cache, (2) pinpoint the exact subsystem causing it, and (3) propose safe mitigations (IO scheduler, CPU pinning, or cache tuning) with observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Start with iostat -x 1 to gauge I/O wait, vmstat 1 for page/cache pressure, and sar -B for blocked processes. Use iotop -aoW to identify heavy I/O consumers. Inspect /proc/diskstats and, if needed, /p",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Begin with a system-wide view, then drill down: - top -b -n1 - vmstat 1 - iostat -xz 1 - pidstat -p ALL 1 Compare CPU idle, IO wait, and memory pressure. If IO-wait is high, enable BFQ (or deadline) a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Diagnostic Workflow for Tail Latency from Memory Reclaim ### Step 1: Confirm Memory Reclaim as Root Cause ```bash # Check swap configuration and usage cat /proc/swaps # Monitor swap activity and memory pressure vmsta…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run iostat -x 1 60 to spot high await on a device; use pidstat -d 1 and iotop to map I/O to processes. Inspect /sys/block/<dev>/queue/nr_requests and /sys/block/<dev>/queue/read_ahead_kb for depth and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical debugging of tail latency under load with standard tools, emphasizing observable signals and safe mitigations.\n\n## Key Concepts\n- I/O wait vs CPU wait diagnosis\n- Disk scheduler impact on latency\n- NIC queueing and IRQ balancing\n- Observability post-mitigation\n\n## Code Example\n```bash\niostat -x 1\nvmstat 1\nsar -B 1 5\n```\n\n## Follow-up Questions\n- How would you roll back changes if latency returns after mitigation?\n- Which metrics would you monitor long term to detect regressions?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-6",
        "questionId": "q-29267",
        "question": "Scenario: A Linux host running a high-throughput message broker behind a single 100G NIC exhibits tail latency spikes (p95 100–200 ms) during bursts; CPU and I/O stay flat. Using only default Linux tools, design a concrete diagnostic to (1) confirm if RX queue drops or NIC interrupt coalescing are bottlenecks, (2) identify the RX queue, CPU, and process involved, and (3) apply safe mitigations (increase RX queues with ethtool -G, tune RPS/IRQs, pin queues to CPUs) while preserving observability. Provide exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check memory pressure and swap thrash: free -h; vmstat 1 5; cat /proc/meminfo | grep MemAvailable; cat /proc/swaps; top -b -n1 -o %MEM | head -n 6; ps aux --sort=-rss | head -n 5; if a culprit dominat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check RX drops/backlog and map to queues/CPUs, then enable per-queue routing and larger RX queues. Commands: ip -s link show eth0; cat /sys/class/net/eth0/statistics/rx_dropped; cat /proc/net/softnet_",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Identify hot RX IRQs and map to CPUs using cat /proc/interrupts; for each relevant IRQ, cat /proc/irq/IRQ/smp_affinity to see current binding; inspect RX stats with ethtool -S ethX | grep rx; tune: et",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Step 1: Confirm CPU Throttling Inspect throttling statistics for the target cgroup: ```bash cat /sys/fs/cgroup/cpu/myapp.slice/cpu.stat ``` Non-zero values in `nr_throttled` (number of throttled periods) and `throttl…",
            "isCorrect": false
          }
        ],
        "explanation": "Why this is asked: tail latency with a healthy CPU/IO implies NIC/interrupt path or queueing as the bottleneck; verifying RX drops, per-queue CPU affinity, and RPS gives a concrete root-cause path. Key concepts: RX queues, interrupt coalescing, RSS/RPS, IRQ affinity, per-queue CPU mapping. Code example demonstrates commands to collect state and apply safe, observable mitigations. Follow-ups: validate with metrics before/after and consider non-disruptive queue tuning.",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-7",
        "questionId": "q-6880",
        "question": "Scenario: A Linux host runs a Node.js API behind Nginx. During bursts, tail latency spikes while CPU, memory, and disk I/O look normal. Using only default tooling, design a beginner-friendly diagnostic workflow to (1) determine if stalls are due to Node.js event loop, Nginx worker, or kernel scheduling, (2) identify the exact component responsible, and (3) apply a safe mitigation (e.g., adjust Nginx worker_processes, backlog, or CPU affinity; or tune Node.js clustering) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use iostat -dx 1 2 and vmstat 1 2 to observe IO stalls; inspect schedulers with cat /sys/block/sd*/queue/scheduler; map I/O to PIDs with pidstat -d 1; switch to mq-deadline: echo mq-deadline > /sys/bl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Establish performance baselines during normal and burst periods: `top -b -n1`, `pidstat -p ALL 1`, `iostat -dx 1`, `vmstat 1`. Correlate latency spikes with these metrics. Use `strace -tt -p <pid>` to identify blocking…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To diagnose boot delay, run systemd-analyze blame; systemd-analyze critical-chain; and journalctl -b -u <slow-unit> to inspect events. Identify the bottleneck by the dependency chain and by checking d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Start with iostat -dx 1 5 to spot device-level tail latency; sample per-pid IO with top -b -n 1; map suspected PIDs to pods by reading /proc/<pid>/cgroup; locate blkio weight via /sys/fs/cgroup/blkio/",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical Linux troubleshooting skills using default system tools for diagnosing tail latency in Node.js/Nginx deployments.\n\n## Key Concepts\n\n- Performance baseline measurement using top/pidstat/iostat/vmstat\n- Distinguishing user-space vs kernel bottlenecks through strace analysis\n- Safe configuration tuning: Nginx worker optimization, Linux socket parameters, Node.js clustering\n\n## Code Example\n\n```bash\n# Establish baseline metrics\ntop -b -n1\npidstat -p ALL 1\niostat -dx 1\nvmstat 1\n```\n\n## Follow-up Questions\n\n- How would this approach adapt to containerized environments?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-8",
        "questionId": "q-13439",
        "question": "Scenario: A Linux host runs a single microservice inside a container behind a reverse proxy. During bursts, tail latency spikes 100–300 ms, while CPU/IO appear normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm if bursts are caused by container CPU throttling, (2) identify the offending container and its CPU quota/usage, (3) apply a safe mitigation (e.g., adjust --cpuset-cpus or cpu shares, or disable throttling for the critical container) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check entropy availability and correlate TLS handshakes with entropy depletion. Commands: cat /proc/sys/kernel/random/entropy_avail; ss -tanp | grep -i tls; lsof /dev/random; strace -e read -p <pid>",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify container CPU throttling in a Docker-backed service. List IDs: `docker ps -q`. Read quotas: `for id in $(docker ps -q); do echo $id; cat /sys/fs/cgroup/cpu/docker/$id/cpu.cfs_quota_us; cat /s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To diagnose use: cat /proc/interrupts; cat /proc/timer_list; while bursts occur, run pidstat -p ALL 1 and vmstat 1. Correlate spikes to timer/softirq counters. Identify culprit via /proc/<tid>/sched o",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To diagnose, check if stalls come from socket backlog or file-descriptor exhaustion. Commands: ss -tulwnp; ss -tanp; cat /proc/sys/net/core/somaxconn; cat /proc/sys/fs/file-max; ulimit -n; vmstat 1 5;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical debugging of containerized Linux workloads using only default tools, focusing on CPU throttling as a cause of latency bursts. It assesses methodical reasoning, precise commands, and safe mitigation choices while preserving observability.\n\n## Key Concepts\n- Linux cgroups CPU quotas and periods\n- Container resource controls (cpuset, cpu-quota/period)\n- Observability: docker stats, pidstat, kernel accounting\n- Safe, incremental mitigations with verification\n\n## Code Example\n```bash\n# List containers and quotas\ndocker ps -q\nfor id in $(docker ps -q); do echo \"ID=$id\"; echo -n \"quota=\"; cat /sys/fs/cgroup/cpu/docker/$id/cpu.cfs_quota_us; echo -n \" period=\"; cat /sys/fs/cgroup/cpu/docker/$id/cpu.cfs_period_us; done\n\n# Observe usage\ndocker stats --no-stream\npidstat -p ALL 1\n\n# Mitigate\ndocker update --cpuset-cpus '0-1' <id>\n```\n\n## Follow-up Questions\n- If throttling persists, what alternative controls would you adjust and why?\n- How would you monitor regressions across other containers after changes?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-9",
        "questionId": "q-23172",
        "question": "Scenario: A Linux host handling mixed services (Go microservices and PostgreSQL) experiences rare 200–400 ms tail latency during bursts. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove metadata journaling bottlenecks on ext4/NVMe are involved, (2) locate the offending PID and filesystem layer, and (3) apply safe mitigations (mount option tweaks, barrier, data ordering, journal_async, or caching) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by confirming reclaim pressure: vmstat 1 60; watch si/so and /proc/vmstat pswpin/pswpout. Map container PIDs to their cgroups via /proc/[pid]/cgroup and inspect memory.stat and memory.usage_in_b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Diagnostic Workflow for Linux Network Stalls ### Step 1: Reproduce the Issue Under Load Generate load while monitoring system resources: ```bash # Monitor CPU and I/O top -b -n1 iostat -xz 1 5 # Check network interfa…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Complete NUMA-GPU Contention Diagnostic Workflow ### Phase 1: Confirm Cache/Bandwidth Contention on GPU Path **Baseline Performance Capture:** ```bash # System-wide cache performance baseline perf stat -e cycles,inst…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use iostat -xd 1, vmstat 1, and iotop to correlate bursts with metadata I/O. Inspect /proc/diskstats and current mount options; identify hot processes via pidstat -d 1 -p ALL. Check ext4 journal setti",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world issue with storage bottlenecks under bursts; ext4 journaling can stall user IO; need to reason with limited tools.\n\n## Key Concepts\n- Metadata vs data I/O\n- ext4 journaling and barriers\n- Observability with iostat/iotop/pidstat\n- Safe mount option changes\n\n## Code Example\n```bash\n#!/bin/bash\niostat -xd 1 5\niotop -boP -d 2\npidstat -d 1\n```\n\n## Follow-up Questions\n- How would you differentiate journal flush latency from block device latency?\n- How would you validate that a change preserves latency SLAs?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-10",
        "questionId": "q-2598",
        "question": "On a Linux host running a high-throughput data-ingestion service, intermittent 5–10s stalls appear under sustained load. Using only default tooling, design a concrete diagnostic workflow to determine whether stalls are caused by Transparent Huge Pages, memory fragmentation, or IO pressure; identify the exact subsystem responsible; and apply a safe mitigation (e.g., disable THP, adjust NUMA binding) while preserving observability. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use default Linux tools to systematically diagnose CPU throttling issues. First, check per-core frequency scaling governors and current frequencies across all cores: ```bash # Check CPU governor on all cores for cpu in…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run: vmstat 1 60; cat /sys/kernel/mm/transparent_hugepage/enabled; grep -i 'Cached' /proc/meminfo; for p in $(ps -eo pid --no-headers); do echo PID $p; awk '/VmFlags|VmSize|Rss/ {print $0}' /proc/$p/s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use `iostat -xz 1 3` to monitor I/O wait metrics, `pidstat -p ALL 1` to identify CPU contention patterns, and `slabtop -s object` to observe memory allocation churn. Inspect THP configuration with `cat /sys/kernel/mm/tr…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Begin by confirming per-user limits (ulimit -n; cat /proc/$(pidof ci)/limits) and system-wide limits (cat /proc/sys/fs/file-max). Then tally FDs with lsof | wc -l and ls /proc/*/fd | wc -l. Identify s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical ability to diagnose kernel memory behavior and I/O contention using built-in tools, focusing on THP impact and safe mitigations.\n\n## Key Concepts\n- Transparent Huge Pages (THP)\n- Memory fragmentation vs allocation failures\n- I/O wait vs CPU contention\n\n## Code Example\n```bash\n#!/bin/bash\necho \"THP status:\"\ncat /sys/kernel/mm/transparent_hugepage/enabled\necho \"I/O wait (sample):\"\niostat -xz 1 3\n```\n\n## Follow-up Questions\n- How to persist THP disable across reboots?\n- What factors could confound THP diagnostics (defragmentation, memory pressure, NUMA)?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-11",
        "questionId": "q-25968",
        "question": "Scenario: A Linux host running a latency-sensitive API across multiple containers on a quad-socket NUMA machine shows intermittent 150–300 ms tail latency bursts with no obvious CPU/IO bottlenecks. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove memory pressure and page eviction drive the stalls, (2) locate the memory-hungry cgroup and its NUMA node, (3) apply safe mitigations (limit memory via cgroups, set memory.high, pin to a NUMA node, preserve observability)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Step 1: Confirm Memory Pressure** - `free -h` — check available memory and swap usage - `vmstat 1 5` — monitor si/so (swap-in/swap-out) per second - `cat /proc/meminfo` — inspect MemAvailable, AnonPages, Shmem, and sw…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Check per-core governors and freq, and switch to performance if throttling exists. Commands: for f in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do echo $f; cat $f; done for f in /sys/devi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin with baseline monitoring using `iostat -dx 1 5` and `vmstat 1 5` to capture latency patterns. If `await` spikes while `svctm` remains stable, investigate the device queue: `cat /sys/block/sda/queue/nr_requests` an…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To diagnose, inspect per-CPU freq/governor and turbo; map hot CPUs to processes; apply mitigations by pinning critical tasks, forcing performance governor on hot CPUs, and toggling turbo via intel_pst",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to diagnose memory pressure in a multi-tenant, NUMA-aware Linux environment using only default tools, and to translate findings into safe mitigations that preserve observability.\n\n## Key Concepts\n- Memory pressure detection via vmstat, /proc/meminfo, and cgroup memory events\n- Reading memory.stat and memory.events to identify page faults and evictions\n- NUMA topology mapping using numactl and numastat\n- Safe mitigations: memory.high (soft limit), memory.limit_in_bytes (hard limit), and cpuset isolation for NUMA pinning\n\n## Common Pitfalls\n- Confusing memory.high (throttled, non-fatal) with memory.limit_in_bytes (enforced, OOM-triggering)\n- Neglecting swap usage when diagnosing latency spikes\n- Applying NUMA pinning without verifying the target node has sufficient memory bandwidth",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-12",
        "questionId": "q-18588",
        "question": "Scenario: A Linux host runs an HTTP service behind Nginx. During 2–3 minute bursts, tail latency spikes to 150–300 ms while CPU, memory, and network look normal. You suspect background IO pressure from a filesystem scrub or RAID rebuild. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm IO pressure is the cause, (2) identify the device and process responsible for elevated IO wait, and (3) apply a safe mitigation (e.g., pause scrub/rebuild, adjust I/O scheduler, or tune vm.dirty_ratio) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Detect allocator contention by correlating burst allocations with THP activity using vmstat/slabtop, identify hot threads via pidstat -p, map to NUMA with numactl --physcpubind --membind; Disable THP;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "PID=$(pgrep -f 'critical-service'); echo PID=$PID top -b -n1 -H | grep -w $PID pidstat -p $PID 1 mpstat -P ALL 1 if [ -e /sys/fs/cgroup/cpu/cpu.max ]; then echo 'cpu.max:'; cat /sys/fs/cgroup/cpu/cpu.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an end-to-end eBPF trace with bpftrace: attach kprobes/kretprobes on accept, recv, and send, plus the HTTP handler; store start times per tid and compute deltas to build a latency histogram keyed",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a practical workflow: 1) run iostat -dx 1 to confirm sustained high 'await' and 'svctm'. 2) run iotop -o -P to catch the processes with IO. 3) inspect /proc/diskstats for the device, check 'ps",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to use basic Linux IO and process tools to diagnose latency spikes caused by background disk pressure, a common production issue.\n\n## Key Concepts\n- IO wait metrics (iostat), per-process IO (iotop, pidstat), disk statistics (/proc/diskstats)\n- Background maintenance (scrub/RAID rebuild) impact on service latency\n- Safe mitigations that preserve observability (pause tasks, tune vm.dirty_ratio, observe before/after)\n\n## Code Example\n```javascript\n// Example commands to run in sequence\niostat -dx 1\niotop -o -P\npidstat -d 1\n```\n\n## Follow-up Questions\n- How would you schedule background maintenance to minimize user impact?\n- How would you validate observability after mitigation?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-13",
        "questionId": "q-29878",
        "question": "Scenario: A Linux host runs a long-running data ingestion daemon that intermittently hits file descriptor limits during bursts, causing connection failures. Using only default Linux tools, design a concrete diagnostic to (1) confirm FD exhaustion and identify the offending process and open descriptors, (2) measure leak rate and peak usage, (3) apply safe mitigations (raise soft/hard limits, fs.file-max, per-process limits) while preserving observability. Provide exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check PSI to confirm memory-pressure stalls: cat /proc/pressure/memory; map offenders: systemd-cgls | head -n 100; identify heavy RAM users: ps -eo pid,cmd,rss --sort -rss | head -20; locate cgroup pa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Start by confirming IO bottlenecks with: iostat -xz 1; iotop -oa; and check schedulers: cat /sys/block/nvme0n1/queue/scheduler. If await is high on a device, switch to deadline: echo deadline > /sys/b",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check global and per-process FDs, then raise limits safely. Commands: cat /proc/sys/fs/file-nr; ulimit -n; for pid in /proc/[0-9]*/fd; do n=$(ls -1 \"$pid\"/fd 2>/dev/null | wc -l); if [ \"$n\" -gt 1000 ]",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "## Diagnostic Workflow for CPU Frequency Scaling and Latency Spikes ### Step 1: Establish Baseline - Check Current Governor Settings ```bash for c in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do echo \"$c: $…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of file descriptor accounting, per-process vs global limits, and safe, observable mitigations in production.\n\n## Key Concepts\n\n- FD accounting with /proc, file-nr, and file-max\n- Per-process vs global limits (ulimit, limits.conf, LimitNOFILE)\n- Safe, observable mitigations and rollback\n\n## Code Example\n\n```text\n# commands to run (plaintext, not code block in answer)\ncat /proc/sys/fs/file-nr\nulimit -n\nfor pid in /proc/[0-9]*/fd; do n=$(ls -1 \"$pid\"/fd 2>/dev/null | wc -l); if [ \"$n\" -gt 1000 ]; then echo \"PID $(basename \"$pid\"): $n fds\"; fi; done\nsudo sh -c 'echo 100000 > /proc/sys/fs/file-max'\n```\n\n## Follow-up Questions\n\n- How would you automate this detection and alerting?\n- How would you distinguish real leaks from bursty workload spikes?\n- What rollback plan would you implement if limits cause unintended side effects?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-14",
        "questionId": "q-5398",
        "question": "Scenario: A Linux host runs a lightweight server that writes logs to /var/log/app.log. During bursts, log writes slow and latency spikes. Using only default tooling, describe a concrete diagnostic workflow to (1) determine if I/O wait, CPU, or memory is the bottleneck, (2) identify the disk or process causing the bottleneck, and (3) apply a safe mitigation (e.g., adjust logging buffering or fsync behavior) while keeping observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: monitor I/O with iostat -dx 1 5 and vmstat 1 to confirm I/O wait; map I/O to processes with pidstat -d 1; locate culprits via lsof | grep '/dev' and /proc/[pid]/fd; identify device with lsblk; m",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run iostat -xz 1 to confirm IO wait; vmstat 1 to monitor memory and swaps; pid=$(pgrep -f 'myserver'); pidstat -d 1 -p $pid; ls -l /proc/$pid/fd | grep app.log; strace -e write -p $pid -s 256 2>&1 | h",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Step 1: check memory pressure: free -m; grep -i swap /proc/meminfo; vmstat 1 5. Step 2: list top memory users: ps -eo pid,cmd,%mem,rss --sort=-%mem | head -n 5. For a pid X, inspect: pmap -x X | head.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Clocksource jitter can cause timer skew across containers. Validate with: cat /sys/devices/system/clocksource/clocksource0/current_clocksource; cat /sys/devices/system/clocksource/clocksource0/availab",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical, beginner-friendly ability to diagnose IO-bound symptoms via standard tools. It emphasizes mapping IO to processes and safe mitigations without downtime.\n\n## Key Concepts\n- IO wait diagnosis with iostat/vmstat\n- Process-level IO attribution via pidstat/lsproc fd\n- Tracing writes with strace; safe mitigations: buffering, fsync timing, log rotation\n\n## Code Example\n\n```bash\n# example steps\n```\n\n## Follow-up Questions\n- How would you adapt this for a containerized service?\n- Which risks come with removing fsync in a production logger?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-15",
        "questionId": "q-21223",
        "question": "A Linux server hosting a simple stateless service experiences intermittent tail latency spikes (100–300 ms) during load bursts, while CPU, RAM, and I/O look normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm Transparent Huge Pages (THP) allocations are contributing to latency, (2) identify which process triggers large THP allocations and memory maps, and (3) apply a safe mitigation (disable THP at runtime or switch to madvise mode) with exact commands and outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check THP status and huge pages usage. Commands: - cat /sys/kernel/mm/transparent_hugepage/enabled - grep -iHuge /proc/meminfo - for p in /proc/[0-9]*/smaps 2>/dev/null; do grep -Hm1 'AnonHugePages' $",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Step 1: Confirm CPU Frequency Scaling Issues Check the current scaling governor for each CPU: ```bash cat /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor ``` Check the current CPU frequency for each core: ```ba…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run systemd-cgtop -a -m 1 to locate hot cgroups; inspect quotas with cat /sys/fs/cgroup/<cg>/cpu.max and cpu.pressure; identify top processes via ps -eo pid,cmd,%cpu --sort=-%cpu | head -n 20; if thro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Start by confirming cgroup v2 CPU throttling: check quotas with `cat /sys/fs/cgroup/cpu.max` (expect format like '100000 200000' for 100ms quota per 200ms period), then verify throttling with `cat /sys/fs/cgroup/cpu.sta…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses tangible debugging of kernel memory behavior with default tools and safe mitigations.\n\n## Key Concepts\n- Transparent Huge Pages (THP)\n- THP status file: /sys/kernel/mm/transparent_hugepage/enabled\n- AnonHugePages and per‑process memory maps via /proc\n- Safe, observable mitigations: disable THP at runtime, monitor impact\n\n## Code Example\n```bash\nTHP=$(cat /sys/kernel/mm/transparent_hugepage/enabled)\necho \"THP: $THP\"\nif [ -d /proc/*/smaps ]; then\n  for pid in /proc/[0-9]*/smaps; do\n    grep -Hm1 'AnonHugePages' \"$pid\" 2>/dev/null && echo \"$pid contributes\";\ndone\nfi\n```\n\n## Follow-up Questions\n- What observability checks would you add after disabling THP?\n- How would you monitor a mixed workload with containers?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-16",
        "questionId": "q-22871",
        "question": "Scenario: A Linux host runs a small API behind Nginx. On moderate load, requests intermittently stall ~100–200 ms. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm if stalls are due to CPU scheduling, IO wait, or network lag, (2) identify the contributing process or thread, and (3) apply safe mitigations (e.g., adjust nginx worker/process settings, pin to CPUs, or tune keepalive) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by confirming reclaim pressure: vmstat 1 60; watch si/so and /proc/vmstat pswpin/pswpout. Map container PIDs to their cgroups via /proc/[pid]/cgroup and inspect memory.stat and memory.usage_in_b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "1) vmstat 1 5; watch wa spikes. 2) ss -tulnp state established to map active sockets. 3) pidstat -p ALL 1 to locate high-CPU procs during stall. 4) if CPU-bound, pin with taskset -cp 0-3 <pid>; 5) tun",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To diagnose CPU frequency scaling throttling as the cause of latency spikes, first establish a baseline by sampling per-core frequencies during normal operation, then compare against values observed during burst traffic…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed workflow: (1) prove reclaim pressure with vmstat 1 10; slabtop -o; cat /proc/vmstat | grep -E 'pgfault|kswapd'; (2) locate offenders with pidstat -p ALL 1; examine /proc/[pid]/smaps and /proc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical Linux troubleshooting using default tools to differentiate CPU scheduling, IO wait, and network latency. It emphasizes observability, process and thread identification, and safe mitigations that don't regress other workloads.\n\n## Key Concepts\n- vmstat wa/us/sy for bottlenecks\n- ss for socket mapping\n- pidstat for per-process stats\n- taskset for CPU pinning\n- nginx tuning basics (worker_connections, keepalive)\n\n## Code Example\n```javascript\n// Minimal latency probe (conceptual placeholder)\nconsole.log('latency probe');\n```\n\n## Follow-up Questions\n- How would you scale this approach under rising load?\n- What additional metrics would you collect for long-term stability?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-17",
        "questionId": "q-17332",
        "question": "In a Linux web service stack, tail latency spikes occur under bursts while CPU/mem/disk IO look normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm DNS resolution delay is the culprit, (2) locate the DNS lookup path in the request, and (3) mitigate safely (local DNS caching or pre-resolve) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run iostat -dx 1 to confirm high await and disk latency, then pidstat -d 1 to map I/O to processes. Identify device with lsblk and check scheduler via cat /sys/block/sdX/queue/scheduler. Correlate to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To diagnose NUMA-related tail latency, follow this systematic workflow: **Step 1: Confirm allocator pressure** ```bash # Monitor system-wide memory pressure and reclaim activity vmstat 1 5 # Look for increasing 'so' (sw…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Start with `iostat -dx 1 2` to confirm elevated await times on the storage device. Use `iotop -P -o -d 1` to identify the specific process responsible for heavy I/O. Run `blktrace -d /dev/nvme0n1 -D /tmp/blktrace` follo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Reproduce bursts; time DNS with curl -w '%{time_namelookup} %{time_connect} %{time_starttransfer}\\n' -o /dev/null -s http://service/health. If time_namelookup dominates, DNS is culprit. Inspect /etc/r",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical DNS latency debugging in a Linux stack, focusing on a real tail-latency scenario beyond CPU/IO metrics.\n\n## Key Concepts\n\n- DNS resolution path and resolver caching on Linux\n- Tools: curl timing, /etc/resolv.conf, getent, strace\n- Observability: correlate DNS timings with tail latency and verify mitigations\n\n## Code Example\n\n```bash\n# Reproduce timing\ncurl -w \"%{time_namelookup} %{time_connect} %{time_starttransfer}\\n\" -o /dev/null -s http://service/health\n# Resolver info\ncat /etc/resolv.conf\nsystemctl is-active systemd-resolved\n# Trace delay\nstrace -f -e trace=network curl -s http://service/health\n```\n\n## Follow-up Questions\n\n- How would you validate caching improvements in a containerized environment?\n- What risks accompany pre-resolving hosts at startup and how to mitigate them?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-18",
        "questionId": "q-27732",
        "question": "On a quad-socket Linux host running a containerized data service, occasional tail latency spikes occur under bursts. Using only default Linux tools, design a diagnostic workflow to (1) prove tail latency is caused by per-container CPU throttling due to quotas, (2) locate offending container(s) and quotas, (3) apply safe mitigations (increase quota, rebalance, or move containers to different CPUs) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run free -m; vmstat 1 5; grep Swap /proc/meminfo to confirm pressure. Use docker stats --no-stream and ps aux --sort=-%mem to identify culprit. If found, apply: sysctl -w vm.swappiness=10; docker upda",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by confirming IO wait and tail latency with: iostat -dx 1 5; pidstat -d 1; iotop -b -n 1. Identify contending device from /proc/diskstats and per-device scheduler in /sys/block/*/queue/scheduler",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Identify per-container throttling by mapping each container to its cgroup, then read cpu quota and throttling counters. Commands: - for each container: cat /proc/self/cgroup; cat /sys/fs/cgroup/cpu/<p",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Baseline: cat /proc/interrupts; lspci -nnk | grep -i ethernet; reproduce burst and observe spike patterns; locate IRQ lines that rise during load; fix by pinning the offending IRQ to an idle CPU: sudo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical ability to diagnose container CPU throttling vs other latency sources, using default tools only, mapping containers to quotas and throttling counters.\n\n## Key Concepts\n- Linux cgroups CPU controller (quota/period)\n- Throttling counters in cpu.stat\n- Tools: cat, pidstat, iostat, /proc/*/cgroup, top\n\n## Code Example\n```bash\n# Map container PID to cgroup and quotas\nproc_cgroup=$(grep -n 'cpu' /proc/$$/cgroup)\ncpu_path=$(echo \"$proc_cgroup\" | cut -d: -f3)\necho \"Quota:\" $(cat \"/sys/fs/cgroup/cpu/$cpu_path/cpu.cfs_quota_us\")\necho \"Period:\" $(cat \"/sys/fs/cgroup/cpu/$cpu_path/cpu.cfs_period_us\")\n```\n\n## Follow-up Questions\n- How would you handle cgroup v2 where cpu.max is used?\n- How would you validate that the mitigation doesn't starve other workloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-19",
        "questionId": "q-20849",
        "question": "In a Linux host running a local REST service in a Docker container, nightly backups cause intermittent 1–2s startup latency for new connections while overall metrics look fine. Using only default Linux tools, design a practical workflow to (1) determine whether CPU scheduling, memory pressure, or network stack is the culprit, (2) identify the exact process or container responsible for the delay, and (3) apply safe mitigations (adjust container memory limits, stagger backups, or tweak NIC settings) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline with: vmstat 1 10; iostat -xz 1 10; ps -eo pid,comm,%cpu,%mem --sort=-%cpu | head -n 6; identify container: docker ps; docker stats --no-stream; watch for entry times that align with backup.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Run turbostat to correlate latency with per-core frequency during bursts. Commands: ```bash # Monitor per-core frequency and p-states during latency bursts sudo turbostat -n 1 --interval 1 -c 0-$(nproc --all) 2>&1 | hea…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Baseline Diagnostics Capture baseline metrics before any burst: ```bash cat /proc/interrupts # Interrupt counts per IRQ line and CPU iostat -xz 1 3 # Check disk I/O saturation vmstat 1 5 # Monitor context switches an…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use vmstat 1 to monitor swap in/out, iostat -dx 1 for device waits, and slabtop to gauge cache pressure. Use pidstat -d 1 -p ALL to spot thrashing PIDs; read /proc/meminfo and /proc/vmstat for PageCac",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to isolate latency causes under simple resource contention using default tools. It evaluates basic instrumentation, container mapping, and safe mitigations that preserve observability.\n\n## Key Concepts\n- Resource contention between host backups and services\n- Basic Linux monitoring (vmstat, iostat, ps, docker ps)\n- Mapping host processes to containers and back\n- Safe mitigations: memory limits, cron jitter, NIC tuning\n\n## Code Example\n```javascript\n# baseline commands\nvmstat 1 10\niostat -xz 1 10\nps -eo pid,comm,%cpu,%mem --sort=-%cpu | head -n 6\ndocker ps\n```\n\n## Follow-up Questions\n- How would you verify observability after changes?\n- If iostat shows high iowait but ps shows low CPU, what next?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-20",
        "questionId": "q-21291",
        "question": "A Linux host under bursty load exhibits tail latency spikes while CPU and I/O look normal. Using only default tools, diagnose whether softirq/scheduler delays are the bottleneck, identify the offending thread and CPU, and outline safe mitigations (pinning, PREEMPT tweaks, governor changes) while preserving observability. End with exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin with baseline monitoring using `iostat -dx 1 5` and `vmstat 1 5` to capture latency patterns. If `await` spikes while `svctm` remains stable, investigate the device queue: `cat /sys/block/sda/queue/nr_requests` an…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check entropy bottleneck by reading /proc/sys/kernel/random/entropy_avail and dmesg for entropy warnings; if low, handshakes block. Use ss -tanp to map TLS ports (e.g., :443) to PIDs. Capture: entrop",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by examining per-CPU utilization with `mpstat -P ALL 1` to identify any single CPU dominating the load. Simultaneously, monitor softirq activity using `watch -n1 'cat /proc/softirq'` to detect NET_RX/NET_TX spikes…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "1) tc -s qdisc show dev <iface> to reveal queue depths; 2) ss -tiap | grep ESTAB to map sockets to PIDs and observe backlog pressure; 3) apply safe mitigations: ip link set dev <iface> txqueuelen 1000",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTail latency under bursty load with normal CPU and I/O utilization is a classic symptom of kernel-level contention—specifically softirq backlogs or scheduler delays. This question tests the candidate's ability to navigate kernel observability interfaces and apply targeted mitigations without degrading system visibility.\n\n## Key Concepts\n\n- **Softirq handling**: NET_RX/NET_TX softirqs process network packets and can monopolize CPU when traffic bursts\n- **Scheduler delays**: Runqueue latency occurs when runnable threads wait for CPU time\n- **Observation interfaces**: /proc/softirq, /proc/sched_debug, and related tools provide visibility into these bottlenecks\n\n## Expected Outputs\n\n- `mpstat -P ALL 1` shows per-CPU breakdown; one CPU may show high utilization while others remain idle\n- `/proc/softirq` reveals elevated NET_RX/NET_TX counts during latency spikes\n- `/proc/sched_debug` exposes runqueue lengths and wait times per task\n- `ps` with the psr column (processor affinity) identifies which CPU each softirq thread runs on\n\n## Mitigations\n\n- **CPU pinning**: Use `taskset` or `cset` to bind latency-sensitive threads to dedicated cores\n- **CPU governor**: Switch to 'performance' mode to eliminate frequency scaling latency\n- **Preemption tuning**: Consider PREEMPT kernels for lower scheduler latency, though this increases kernel overhead\n\n## Trade-offs\n\nPinning cores reduces contention but may reduce overall throughput. Changing preemption models affects interrupt latency vs. throughput trade-offs. Always measure before and after applying mitigations to validate improvement.",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-21",
        "questionId": "q-18367",
        "question": "Scenario: A Linux host running a microservices stack experiences intermittent tail latency spikes during bursts while CPU usage appears normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove bursts trigger CPU frequency throttling, (2) identify hot CPUs and the threads running on them, and (3) apply safe mitigations (set cpufreq governor to performance, disable turbo, pin critical services) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a practical workflow: 1) run iostat -dx 1 to confirm sustained high 'await' and 'svctm'. 2) run iotop -o -P to catch the processes with IO. 3) inspect /proc/diskstats for the device, check 'ps",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enable ftrace sched_switch and sched_wakeup to measure scheduling latency during bursts, collect the trace, map long wakeup-to-run gaps to PIDs, then pin the offender with taskset -cp and tune schedul",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To diagnose CPU frequency throttling during bursts, first verify that throttling is occurring by comparing `scaling_cur_freq` across all CPUs and scanning `dmesg` for throttle-related messages. Next, identify hot thread…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Verify THP state and defrag; monitor THP counters in /proc/vmstat and THP messages in dmesg; map allocations to PIDs with for pid in /proc/[0-9]*; do [ -r \"$pid/cgroup\" ] && echo $pid; done and cross-",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical ability to diagnose low-level Linux performance issues under bursty load, focusing on CPU frequency management and thread placement using only default tools. It evaluates whether a candidate can move beyond surface-level metrics like average CPU usage and reason about frequency scaling, thermal throttling, and per-core behavior.\n\n## Key Concepts\n- **CPU frequency scaling and turbo control**: Linux exposes per-CPU frequency and governor settings through `/sys/devices/system/cpu/`. Under bursty workloads, the default `ondemand` or `schedutil` governor may not ramp frequency quickly enough, and thermal limits can trigger throttling that is invisible in aggregate CPU metrics.\n- **Per-thread CPU usage and affinity**: Tail latency spikes often trace back to a small number of threads landing on throttled or contended cores. Identifying these threads and their CPU placement is essential.\n- **Safe, observable mitigations**: Governor tuning, turbo disable, and CPU pinning are reversible changes that can be applied without downtime while preserving the ability to monitor their effects.\n\n## Diagnostic Workflow\n\n### Step 1: Prove Bursts Trigger CPU Frequency Throttling\n\nCapture current frequencies across all CPUs and check for throttle events:\n\n```bash\n# Snapshot per-CPU frequencies\ncat /sys/devices/system/cpu/cpu*/cpufreq/scaling_cur_freq\n\n# Watch frequency changes in real time during a burst\nwatch -n 0.5 \"cat /sys/devices/system/cpu/cpu*/cpufreq/scaling_cur_freq\"\n\n# Check kernel logs for thermal or frequency throttle messages\ndmesg | grep -i -E 'throttl|frequency'\n```\n\n**Example output:**\n```\n/sys/devices/system/cpu/cpu0/cpufreq/scaling_cur_freq:2400000\n/sys/devices/system/cpu/cpu1/cpufreq/scaling_cur_freq:1200000   # <-- throttled\n/sys/devices/system/cpu/cpu2/cpufreq/scaling_cur_freq:2400000\n/sys/devices/system/cpu/cpu3/cpufreq/scaling_cur_freq:1200000   # <-- throttled\n```\n\n```\n[  482.103] CPU1: Package temperature above threshold, cpu clock throttled\n[  482.103] CPU3: Package temperature above threshold, cpu clock throttled\n```\n\nA frequency drop on specific CPUs coinciding with burst timing confirms throttling.\n\n### Step 2: Identify Hot CPUs and the Threads Running on Them\n\n```bash\n# Per-thread CPU usage with core assignment\ntop -H -b -n1 | head -30\n\n# Sorted by CPU consumption\nps -eo pid,tid,pcpu,psr,comm --sort=-pcpu | head -20\n```\n\n**Example output:**\n```\n  PID   TID %CPU PSR COMMAND\n 4821  4825 98.2   1 payment-svc\n 4821  4826 95.7   3 payment-svc\n 5102  5108 42.1   0 gateway\n```\n\nHere, `payment-svc` threads are pinned to CPUs 1 and 3, which are the same cores experiencing throttling. The `PSR` column confirms thread-to-core mapping.\n\n### Step 3: Apply Safe Mitigations While Preserving Observability\n\n**Set the cpufreq governor to performance on all CPUs:**\n```bash\nfor cpu in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do\n  echo performance | sudo tee \"$cpu\"\ndone\n```\n\n**Disable turbo boost to reduce thermal pressure:**\n```bash\n# Intel systems\necho 1 | sudo tee /sys/devices/system/cpu/intel_pstate/no_turbo\n```\n\n**Pin critical services to non-throttled CPUs:**\n```bash\n# Move payment-svc threads to CPUs 0 and 2\nsudo taskset -apc 0,2 4821\n\n# Verify the new affinity\ntaskset -pc 4821\n```\n\n**Verify mitigations are effective:**\n```bash\n# Confirm governor is set\ncat /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor\n\n# Monitor frequencies remain stable under load\nwatch -n 1 \"cat /sys/devices/system/cpu/cpu*/cpufreq/scaling_cur_freq\"\n\n# Confirm no new throttle messages\ndmesg -w | grep -i throttl\n```\n\nThese mitigations are fully reversible: restore the original governor with `echo ondemand | sudo tee ...`, re-enable turbo with `echo 0 | sudo tee .../no_turbo`, and reset affinity with `taskset -apc 0-3 <pid>`.",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-22",
        "questionId": "q-27904",
        "question": "Scenario: A Linux host running a lightweight HTTP API experiences intermittent 50–150 ms tail latency spikes during CPU bursts. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm if CPU frequency scaling or thermal throttling is capping performance, (2) identify the specific core(s) throttling and correlate with load, and (3) apply safe mitigations (set CPU governor to performance, adjust cooling/BIOS if possible, and pin critical workload to steady cores) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use default Linux tools to systematically diagnose CPU throttling issues. First, check per-core frequency scaling governors and current frequencies across all cores: ```bash # Check CPU governor on all cores for cpu in…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Check THP status and huge pages usage. Commands: - cat /sys/kernel/mm/transparent_hugepage/enabled - grep -iHuge /proc/meminfo - for p in /proc/[0-9]*/smaps 2>/dev/null; do grep -Hm1 'AnonHugePages' $",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check dmesg for 'Out of memory' messages and /var/log/messages. Use `free -h` to see memory usage, `ps aux --sort=-%mem` to find memory hogs, and `cat /proc/<pid>/status` for VmRSS. If OOM, check `/proc/sys/vm/panic_on_…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A systematic diagnostic workflow to identify whether cgroup CPU throttling causes tail latency spikes: **Step 1: Identify containers and their cgroups** ```bash for id in $(docker ps -q); do pid=$(docker inspect -f '{{.…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates understanding of how CPU frequency scaling and thermal throttling introduce latency variability under burst load, and the ability to diagnose these issues using only built-in Linux tools without external monitoring packages.\n\n## Key Concepts\n\n- **CPU frequency governors**: `performance` maintains maximum clock speed; `powersave` and `ondemand` reduce power consumption but introduce latency during frequency ramp-up.\n- **Thermal throttling**: Hardware-generated signals logged via dmesg when CPU temperatures exceed thermal thresholds, causing immediate frequency reduction.\n- **Core affinity**: Pinning critical workloads to specific cores avoids throttled cores and provides predictable latency characteristics.\n\n## Evaluation Criteria\n\nA strong response demonstrates:\n\n1. Knowledge of `/sys` filesystem interfaces for CPU frequency and thermal data.\n2. Understanding of kernel logging mechanisms (`dmesg`).\n3. Ability to correlate observable metrics (frequency, temperature, utilization) with latency symptoms.\n4. Practical mitigation steps that balance performance and system stability.\n5. Awareness of trade-offs: performance governor increases power consumption and heat generation.",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-23",
        "questionId": "q-17931",
        "question": "Scenario: A Linux host runs a web service behind Nginx; during traffic bursts, latency spikes of 150–250 ms appear though CPU, RAM, and IO metrics look fine. Using only default Linux tools, design a beginner-friendly workflow to (1) confirm whether CPU frequency scaling/power-saving is causing stalls, (2) identify when governors change and which CPUs are affected, and (3) apply a safe mitigation (e.g., set cpufreq governor to performance or schedutil and optionally pin critical CPUs) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Step 1: Confirm CPU Frequency Scaling Issues Check the current scaling governor for each CPU: ```bash cat /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor ``` Check the current CPU frequency for each core: ```ba…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Run `vmstat 1 60` and `iostat -xz 1 60`, then `top -b -n1` to identify CPU, memory, and I/O pressure. If iowait is elevated, map I/O to processes with `pidstat -d 1`. If a git/build task is causing the issue, throttle p…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run vmstat, iostat, and top in batch to correlate latency with system metrics, then pidstat to isolate the offender. If disk I/O wait dominates, switch a block device’s scheduler to deadline/bfq and a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check per-core governors and freq, and switch to performance if throttling exists. Commands: for f in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do echo $f; cat $f; done for f in /sys/devi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates a candidate's knowledge of Linux performance fundamentals, specifically CPU frequency scaling governors and observability discipline. It tests whether the candidate can diagnose performance issues without specialized tooling, using only built-in Linux utilities. This is a common real-world scenario where hardware metrics appear healthy but application latency suffers due to CPU power management.\n\n## Key Concepts\n\n- **CPU frequency governors:** Kernel drivers that control CPU clock speed. Common governors include:\n  - `powersave`: Runs at minimum frequency to conserve energy, causing latency spikes during sudden load increases\n  - `performance`: Runs at maximum frequency consistently, providing lowest latency at the cost of higher power consumption\n  - `schedutil`: Dynamically adjusts frequency based on scheduler utilization (default in modern kernels)\n  - `ondemand`: Similar to schedutil but uses a sampling-based approach\n\n- **Latency correlation:** When CPU cores ramp up from powersave (e.g., 800 MHz) to full speed (e.g., 3.5 GHz), there is a delay. During traffic bursts, this delay manifests as 150-250ms latency spikes even when CPU utilization appears low in aggregate metrics.\n\n## What Good Answers Demonstrate\n\n1. Understanding that default monitoring tools (top, vmstat) show percentage-based CPU usage, which can mask frequency-related delays\n2. Knowing where to find frequency information in `/sys/devices/system/cpu/`\n3. Recognizing that the solution must balance performance (performance governor) vs. dynamic scaling (schedutil)\n4. Understanding CPU affinity pinning as an additional layer of isolation for critical processes",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-24",
        "questionId": "q-24844",
        "question": "Memory pressure from kernel reclaim is causing tail latency spikes on a multi-tenant Linux host running several containers with memory limits. Using only default Linux tools, design a diagnostic workflow to (1) confirm reclaim/memory THP as the culprit, (2) identify containers hitting memory quotas and the pages they touch, (3) apply safe mitigations (adjust memory.high/memory.max, disable THP for stability, throttle noncritical workloads) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Start with iostat -xz 1 to confirm high await on the target disk, then inspect /proc/diskstats and /proc/[pid]/io to map IO to the offending process. If writeback pressure is the issue, tune kernel VM",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify pressure via: vmstat 1 60; slabtop -o size; for each container, read /sys/fs/cgroup/memory/.../memory.stat and memory.usage_in_bytes; check THP: cat /sys/kernel/mm/transparent_hugepage/enable",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Begin by confirming memory pressure and reclaim delay: vmstat 1 5; free -m; sar -r 1 60; dmesg | tail -n 100 | grep -i memory -m 5. Identify the culprit with docker stats and PID mapping, then tie to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "1) Gather per-socket state and retransmission hints: ss -ti; ss -tp; cat /proc/net/netstat; 2) Capture RTT behavior and retries via tcpdump for bursts and inspect retransmissions locally; 3) Apply saf",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical memory-pressure diagnosis in multi-tenant Linux environments using default tools, focusing on kernel reclaim and THP interactions within containers. It emphasizes safe mitigations that preserve observability.\n\n## Key Concepts\n- Memory pressure signals and reclaim latency\n- Transparent Huge Pages (THP) interactions\n- cgroups memory controller and per-container quotas\n- Observability: per-cgroup usage vs host metrics\n\n## Code Example\n```bash\nvmstat 1 60\nfor c in /sys/fs/cgroup/memory/*; do echo 'CG:' $c; cat $c/memory.usage_in_bytes; done\ncat /sys/kernel/mm/transparent_hugepage/enabled\n```\n\n## Follow-up Questions\n- How would you validate that the change did not starve noncritical tenants?\n- How would you safely roll back if latency regresses?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-24T03:02:49.810Z",
    "version": 1
  }
]