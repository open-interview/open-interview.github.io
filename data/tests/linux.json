[
  {
    "id": "test-linux",
    "channelId": "linux",
    "channelName": "Linux",
    "title": "Linux Knowledge Test",
    "description": "Test your understanding of Linux with 25 questions.",
    "questions": [
      {
        "id": "linux-q-0",
        "questionId": "q-5830",
        "question": "NUMA-Aware Latency: A Linux host in a multi-tenant analytics cluster shows intermittent 5–20 ms tail latency on a service under sustained I/O. Using only default tooling, design a concrete diagnostic workflow to (1) confirm if latency is caused by remote NUMA memory access, (2) identify the offending process or memory pattern, and (3) apply a safe mitigation such as binding to a local NUMA node or selective interleaving, while preserving observability. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use iostat -xd 1, vmstat 1, and iotop to correlate bursts with metadata I/O. Inspect /proc/diskstats and current mount options; identify hot processes via pidstat -d 1 -p ALL. Check ext4 journal setti",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a practical workflow: 1) run iostat -dx 1 to confirm sustained high 'await' and 'svctm'. 2) run iotop -o -P to catch the processes with IO. 3) inspect /proc/diskstats for the device, check 'ps",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Verify throttling with: cat /sys/fs/cgroup/containers/db/cpu.max; cat /sys/fs/cgroup/containers/db/cpu.stat. Example: 500000 1000000 and nr_periods 12345 throttled_time 230000000. Map processes: grep",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: 1) numactl --hardware to map NUMA nodes; 2) numastat -p <pid> and cat /proc/<pid>/numa_maps to spot remote allocations; correlate spikes with remote pages; 3) if remote access dominates, apply m",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests NUMA topology reasoning and safe production mitigation under load.\n\n## Key Concepts\n- NUMA topology and memory policies\n- Per-process NUMA accounting with /proc and numastat\n- Safe mitigation via membind/cpunodebind and thread pinning\n\n## Code Example\n```bash\nnumactl --hardware\nnumastat -p 12345\ncat /proc/12345/numa_maps\n```\n\n## Follow-up Questions\n- How would you scale this to hundreds of processes?\n- What are the risks of binding to a single node under mixed workloads?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-1",
        "questionId": "q-18588",
        "question": "Scenario: A Linux host runs an HTTP service behind Nginx. During 2–3 minute bursts, tail latency spikes to 150–300 ms while CPU, memory, and network look normal. You suspect background IO pressure from a filesystem scrub or RAID rebuild. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm IO pressure is the cause, (2) identify the device and process responsible for elevated IO wait, and (3) apply a safe mitigation (e.g., pause scrub/rebuild, adjust I/O scheduler, or tune vm.dirty_ratio) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run free -h; vmstat 1 5; top -b -n1; ps aux --sort=-%mem | head -5; cat /proc/meminfo; check dmesg | grep -i -E 'oom|out of memory'; identify the offender PID and cgroup with cat /proc/<pid>/status an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by examining /proc/interrupts to identify active NIC IRQ lines and their distribution across CPU cores, then verify SMP affinity settings and temporarily bind high-frequency IRQs to dedicated CPUs while monitoring…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a practical workflow: 1) run iostat -dx 1 to confirm sustained high 'await' and 'svctm'. 2) run iotop -o -P to catch the processes with IO. 3) inspect /proc/diskstats for the device, check 'ps",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: monitor I/O with iostat -dx 1 5 and vmstat 1 to confirm I/O wait; map I/O to processes with pidstat -d 1; locate culprits via lsof | grep '/dev' and /proc/[pid]/fd; identify device with lsblk; m",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to use basic Linux IO and process tools to diagnose latency spikes caused by background disk pressure, a common production issue.\n\n## Key Concepts\n- IO wait metrics (iostat), per-process IO (iotop, pidstat), disk statistics (/proc/diskstats)\n- Background maintenance (scrub/RAID rebuild) impact on service latency\n- Safe mitigations that preserve observability (pause tasks, tune vm.dirty_ratio, observe before/after)\n\n## Code Example\n```javascript\n// Example commands to run in sequence\niostat -dx 1\niotop -o -P\npidstat -d 1\n```\n\n## Follow-up Questions\n- How would you schedule background maintenance to minimize user impact?\n- How would you validate observability after mitigation?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-2",
        "questionId": "q-19572",
        "question": "Scenario: A Linux host runs a single microservice inside a cgroup with a small CPU share. Under bursts, tail latency spikes though overall CPU stays low. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm CPU throttling via the cgroup controller, (2) identify the hottest thread in the group, and (3) propose a safe mitigation (adjust quotas, remove throttling) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Find the target process and its cgroup, then inspect quotas and monitor bursts: pid=$(pgrep -f my-service); cg=$(awk -F: '/name/ {print $3}' /proc/$pid/cgroup); echo $cg; cat /sys/fs/cgroup/$cg/cpu.ma",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Baseline with iostat -dx 1, vmstat 1, and kubectl top; use eBPFtrace to map per-process IO latency: tracepoint:block:block_rq_issue { @s[pid] = nsecs; } tracepoint:block:block_rq_complete { @d[pid] =",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check CPU throttling in the cgroup: cat /sys/fs/cgroup/cpu/mygroup/cpu.stat; if throttled_usec > 0, throttling is active. Then inspect quotas: cat /sys/fs/cgroup/cpu/mygroup/cpu.cfs_quota_us and cpu.c",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "## Diagnostic and Mitigation Approach ### 1. Confirm UDP Drops and Check MTU/GRO/LSO Involvement **UDP Drop Statistics:** ```bash # Overall UDP socket statistics netstat -su # Socket-level UDP details ss -u -a # Kernel…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of Linux CPU throttling in cgroups, a common beginner scenario when services slow under bursts.\n\n## Key Concepts\n- cgroups CPU controller and quotas\n- cpu.stat throttled_usec\n- per-thread CPU usage with ps/top\n\n## Code Example\n```javascript\n# Commands shown in the answer; executed in shell\ncat /sys/fs/cgroup/cpu/mygroup/cpu.stat\ncat /sys/fs/cgroup/cpu/mygroup/cpu.cfs_quota_us\ncat /sys/fs/cgroup/cpu/mygroup/cpu.cfs_period_us\nps -eo pid,pcpu,comm --sort=-pcpu | head\ntop -H -p <pid> -n1\n``` \n\n## Follow-up Questions\n- How would you automate these checks with a small script?\n- What’s the impact of increasing quotas on other services sharing the host?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-3",
        "questionId": "q-16265",
        "question": "Scenario: A Linux server running a MongoDB shard and several analytics containers experiences intermittent tail latency spikes on GETs during nightly backups. CPU and average I/O look normal, but tail latency >100 ms appears during backup windows. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm I/O-layer saturation is the culprit, (2) identify the contending block devices and I/O queues, and (3) apply safe mitigations (e.g., per-device IO scheduler tuning, backup throttling via ionice, isolating backups with cgroups, adjusting read-ahead) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify throttling with: cat /sys/fs/cgroup/containers/db/cpu.max; cat /sys/fs/cgroup/containers/db/cpu.stat. Example: 500000 1000000 and nr_periods 12345 throttled_time 230000000. Map processes: grep",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify cgroup throttling by confirming usage vs limits and tracing to the service's cgroup, then mitigate by adjusting limits or quotas. Commands: pid=$(pidof myservice); cat /proc/$pid/cgroup; cgrp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by confirming IO wait and tail latency with: iostat -dx 1 5; pidstat -d 1; iotop -b -n 1. Identify contending device from /proc/diskstats and per-device scheduler in /sys/block/*/queue/scheduler",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Check TIME_WAIT and local port usage with ss -s and ss -tulpen; inspect port range with cat /proc/sys/net/ipv4/ip_local_port_range. If needed widen with sudo sysctl -w net.ipv4.ip_local_port_range='10",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to diagnose production I/O tail latency caused by storage contention using only core Linux tools, a common real-world issue in multi-tenant databases and analytics jobs.\n\n## Key Concepts\n\n- IO wait and tail latency detection\n- Per-device I/O scheduling and contention\n- Proactive throttling and observability preservation\n\n## Code Example\n\n```bash\n# show per-device scheduler options\nfor d in /sys/block/sd*; do echo -n \"${d}: \"; cat $d/queue/scheduler; done\n``` \n\n```bash\n# throttle backup process by IO priority\nionice -c 3 -p $(pgrep -f backup) \n```\n\n## Follow-up Questions\n\n- How would you scale this to a 16-disk array with multiple MongoDB shards?\n- What long-term instrumentation changes would you propose to avoid repeating this issue?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-4",
        "questionId": "q-9985",
        "question": "A Linux host running multiple containers shows recurrent 50–150 ms tail latency bursts; clocksource flips are observed during bursts in logs. Using only default tools, design a concrete diagnostic workflow to (1) confirm clocksource instability as the tail-latency cause, (2) identify affected CPUs/contexts, (3) apply safe mitigations (e.g., pin to a stable clocksource, BIOS fix, disable dynamic switching) while preserving observability. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use top/vmstat to identify bottleneck, then docker stats --no-stream to spot the hogging container. Correlate with docker ps. If CPU or memory is the issue, throttle with docker update --cpu-shares 51",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Clocksource jitter can cause timer skew across containers. Validate with: cat /sys/devices/system/clocksource/clocksource0/current_clocksource; cat /sys/devices/system/clocksource/clocksource0/availab",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Begin by confirming reclaim pressure: vmstat 1 60; watch si/so and /proc/vmstat pswpin/pswpout. Map container PIDs to their cgroups via /proc/[pid]/cgroup and inspect memory.stat and memory.usage_in_b",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run: iostat -dx 1 2 to verify high await on the NVMe device during bursts. Check the scheduler: cat /sys/block/nvme0n1/queue/scheduler. If needed: echo mq-deadline > /sys/block/nvme0n1/queue/scheduler",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to diagnose subtle kernel-timekeeping issues that impact multi-tenant workloads. It requires reading low-level system state, correlating with latency tails, and executing safe mitigations without breaking observability.\n\n## Key Concepts\n- Clocksource and timer events\n- dmesg, /sys clocksource, /proc/timer_list\n- Observability in multi-tenant environments\n- Safe remediation: stable clocksource, BIOS settings\n\n## Code Example\n```javascript\n// Placeholder: not used in answer, shown for structure\n```\n\n## Follow-up Questions\n- How would you verify across multiple hosts? \n- What if BIOS/firmware blocks changing clocksource?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-5",
        "questionId": "q-26193",
        "question": "Scenario: A Linux host running a latency‑sensitive microservice stack on a quad‑socket NUMA system shows intermittent tail latency spikes during traffic bursts, while average latency remains low. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove memory allocator stalls or THP fragmentation are the cause, (2) locate the offending thread(s) and their NUMA nodes and memory policies, and (3) apply safe mitigations (disable Transparent Huge Pages, pin critical services, tune memory-related settings, isolate CPUs) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin with top -b -n1, vmstat 1 5, iostat -dx 1 5, pidstat -d 1 5 -p ALL, and docker stats --no-stream to map I/O wait to a container. If iowait is high and a container shows heavy block IO, throttle",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: verify THP fragmentation and allocator stalls as tail latency cause, identify hot threads and NUMA nodes, and apply safe mitigations with observability. Check THP state, allocator pressure,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Check logs: journalctl -u backup.service -n 100; verify path perms: ls -ld /var/backups; stat /var/backups; identify writer: lsof +D /var/backups | head; fix: sudo chown -R backup:backup /var/backups;",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check host memory pressure: free -m; vmstat 1 5; grep Swap /proc/meminfo. If swap used or MemAvailable is low, locate VMs with high RSS: ps -eo pid,comm,rss,cmd | grep -E 'qemu|kvm'. For each VM, run",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to diagnose memory allocator and THP‑related tail latency in a real NUMA, multi‑socket environment using only default tools, emphasizing practical observability and safe mitigations.\n\n## Key Concepts\n\n- Transparent Huge Pages (THP) and fragmentation\n- Slab allocator pressure and kmem caches\n- NUMA topology and memory policies\n- CPU affinity and isolating critical workloads\n- Lightweight observability signals (iostat, vmstat, pidstat, slabtop)\n\n## Code Example\n\n```javascript\n// Example diagnostic commands\ncat /sys/kernel/mm/transparent_hugepage/enabled\nslabtop -o Size\nnumactl --hardware\npidstat -w -p ALL 1\n```\n\n## Follow-up Questions\n\n- How would you automate this into a one-shot diagnostic script?\n- How would you validate that the mitigations don’t degrade throughput or increase tail latency under other workloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-6",
        "questionId": "q-6322",
        "question": "Scenario: A Linux server hosting several containerized services suffers intermittent network latency spikes during bursts. The system shows high soft IRQ load on various CPUs. Using only default tools, design a concrete diagnostic workflow to (1) confirm IRQ/NET-related latency, (2) identify the offending NIC or interrupt vector, and (3) apply a safe mitigation (e.g., isolate IRQs to dedicated CPUs, adjust affinity, disable irqbalance) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check limits and usage: cat /proc/sys/fs/file-max; ulimit -n; grep OpenFiles /proc/$(pgrep -f nginx)/limits 2>/dev/null. Identify top FDs: for p in /proc/[0-9]*; do [ -r \\\"$p/fd\\\" ] && echo -n \\\"${p##",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Verify IRQ impact with vmstat 1 and mpstat -P ALL 1, then inspect /proc/interrupts to find the top vector: cat /proc/interrupts | sort -nrk2 | head -n1. Note its device (e.g., eth0). Pin it: echo <mas",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Begin by checking FD limits: ulimit -n; cat /proc/sys/fs/file-max; list listening sockets with ss -ltnp; inspect per-process limits with cat /proc/<pid>/limits. If EMFILE/backlog appear, increase fs.f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Steps: 1) vmstat 1 60 to reveal swap/cache pressure; 2) slabtop -o to monitor kernel caches; 3) for each cgroup: cat /sys/fs/cgroup/memory/*/memory.usage_in_bytes and memory.limit_in_bytes; 4) ps -eo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical debugging of multi-tenant Linux systems where network latency spikes are driven by IRQ contention. It tests observation, diagnosis, and safe mitigation without extra tooling.\n\n## Key Concepts\n- Linux IRQs, /proc/interrupts, smp_affinity, isolcpus, irqbalance\n- Observability: vmstat, mpstat, sar, ping, baseline vs post-change\n- Caution: avoid starving essential interrupts during isolation\n\n## Code Example\n```bash\nvmstat 1\nmpstat -P ALL 1\ncat /proc/interrupts | sort -nrk2 | head -n1\n```\n\n## Follow-up Questions\n- How would you validate multi-NIC environments with overlapping interrupts?\n- What trade-offs exist between isolation and CPU utilization?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-7",
        "questionId": "q-6789",
        "question": "Scenario: A Linux host serving a MongoDB shard in containers on a NUMA machine shows 95th percentile latency spikes under peak load. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm if CPU frequency scaling or C-state transitions align with latency tails, (2) locate the exact CPUs/NUMA nodes involved, (3) apply safe mitigations (e.g., set governors to performance, pin critical processes, disable deep C-states) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Step 1: Confirm CPU Frequency Scaling Issues Check the current scaling governor for each CPU: ```bash cat /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor ``` Check the current CPU frequency for each core: ```ba…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: use ss -s and ss -tanp to detect backlog/port pressure, inspect /proc/net/sockstat and the relevant sysctl values, then apply mitigations: sysctl -w net.core.somaxconn=65535; sysctl -w net.ipv4.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a workflow to determine if latency tails follow CPU power transitions. (1) correlate latency with CPUfreq/C-states via pidstat, iostat, and /sys/devices/system/cpu/cpu*/cpufreq/scaling_cur_freq",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Check nf_conntrack usage and adjust: cat /proc/sys/net/netfilter/nf_conntrack_max; wc -l /proc/net/nf_conntrack; if usage is high, run: sysctl -w net.netfilter.nf_conntrack_max=300000; sysctl -w net.n",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests advanced Linux diagnostics for production latency issues in NUMA/containerized workloads, focusing on power management and scheduling interactions rather than generic performance.\n\n## Key Concepts\n- CPU freq scaling, C-states, and latency tail effects\n- NUMA topology and per-node contention\n- Safe mitigations: CPU isolation, governor tuning, process pinning, observability hooks\n\n## Code Example\n```bash\n# Example commands\ngrep -H . /sys/devices/system/cpu/cpu*/cpufreq/scaling_cur_freq\ncat /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor\nnumactl -H\npidstat 1\nperf stat -e cycles,instructions,cache-references,cache-misses -p <pid> & sleep 5; kill $!;```\n```\n\n## Follow-up Questions\n- How would you validate a remediation without regressing throughput?\n- Which metrics would you surface to SRE dashboards to detect reoccurrence?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-8",
        "questionId": "q-27640",
        "question": "A Linux host running a local data ingestion service experiences intermittent 100–200 ms tail latency bursts during bursts of queue depth, while CPU and I/O metrics appear normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm page cache pressure or file-system cache churn as the cause, (2) identify the processes and files driving cache thrash, and (3) apply safe mitigations (tune vm.swappiness and vm.vfs_cache_pressure, adjust file access patterns, consider direct I/O hints or prefetch controls) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: 1) verify contention with vmstat 1, iostat -x 1, and numastat -c; 2) locate hotspots with pidstat -m -p ALL and perf top -p <pid>, then inspect /proc/<pid>/smaps for RSS/Swap; 3) mitigate by bin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To prove memory pressure causes tail latency, monitor with: free -m; vmstat 1 60; cat /proc/meminfo. Identify culprit: map PID to container via /proc/[pid]/cgroup; find large RSS with: ps -eo pid,cmd,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run vmstat 1 5 to confirm IO wait; free -m and cat /proc/meminfo to gauge page cache; cat /proc/diskstats to map device IO; top -b -n1 to spot high IO processes; if available, iostat -dx 1; then set v",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Analyze tail latency by correlating bursts with per-core frequency transitions. Check across /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor and scaling_cur_freq; inspect dmesg for turbo or p-st",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical debugging of cache and memory dynamics on a real Linux host, a common beginner-to-intermediate scenario in production.\n\n## Key Concepts\n- Page cache behavior and swappiness impact\n- Per-device IO mapping without third-party tooling\n- Observability preservation during mitigations\n\n## Code Example\n```bash\nvmstat 1 5\nfree -m\ncat /proc/meminfo | sed -n '1,40p'\ncat /proc/diskstats\ntop -b -n1 | head -n 20\n```\n\n## Follow-up Questions\n- What if latency persists after changes? what next?\n- How would you validate observability after mitigations without disrupting service?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-9",
        "questionId": "q-5472",
        "question": "Scenario: A Linux host running a data-processing daemon intermittently reports EMFILE when creating thousands of temp files during bursts. CPU and I/O appear normal. Using only default tooling, outline a beginner-friendly diagnostic workflow to (1) confirm if file descriptors are the bottleneck vs CPU/memory/disk, (2) identify the offending limit or process, and (3) apply a safe mitigation (e.g., raise soft/hard limits, adjust fs.file-max, or tune the app) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run `df -h /tmp` and `df -i /tmp` to differentiate between disk space exhaustion and inode exhaustion. If inode usage approaches 100% while disk space remains available, identify the culprits with: ```bash sudo find /tm…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by confirming memory pressure and reclaim delay: vmstat 1 5; free -m; sar -r 1 60; dmesg | tail -n 100 | grep -i memory -m 5. Identify the culprit with docker stats and PID mapping, then tie to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by checking whether file descriptors are exhausted. Commands: cat /proc/sys/fs/file-max; ulimit -n; lsof | wc -l; for pid in /proc/[0-9]*/; do echo -n $pid: ; ls -1 /proc/$pid/fd 2>/dev/null | wc -l; done",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "To diagnose CPU throttling from container quotas, first list running containers using `docker ps`, then examine cgroup metrics for each container at `/sys/fs/cgroup/cpu/docker/<container_id>/cpu.{cfs_quota_us,cfs_period…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical, beginner-friendly debugging of a common Linux constraint: file descriptor exhaustion under bursts, using default tools only.\n\n## Key Concepts\n- File descriptors, limits (ulimit, fs.file-max)\n- Process fd usage (lsof, /proc/<pid>/fd)\n- Safe mitigations (limits.conf, sysctl, service restart)\n\n## Code Example\n```\n# Check current limits\ncat /proc/sys/fs/file-max\nulimit -n\nlsof | wc -l\n```\n\n## Follow-up Questions\n- How would you monitor fd usage over time in production?\n- What would you change to avoid starvation under spikes without compromising security?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-10",
        "questionId": "q-1445",
        "question": "A Linux host running GPU-accelerated video processing containers shows intermittent frame latency spikes during peak load, with no obvious CPU/memory/I/O bottlenecks. Without downtime, describe a concrete, real-world diagnostic workflow using only default tools to (1) verify if latency is caused by page cache pressure or Transparent Huge Pages, (2) identify the subsystem or container involved, and (3) apply a safe mitigation (e.g., disable THP on affected NUMA nodes, tune swappiness, or pin tasks) with minimal impact and observable results. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline THP state and memory pressure signals, then correlate with latency: - cat /sys/kernel/mm/transparent_hugepage/enabled - dmesg | grep -i thp - cat /proc/meminfo | grep -iHuge - for each cgroup",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Workflow: verify spike with top/iostat, inspect THP and page cache, locate offending container, and apply a safe mitigation. Commands: cat /sys/kernel/mm/transparent_hugepage/enabled; vmstat 1 5; iost",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Run: top -b -n1; vmstat 1 5; cat /proc/loadavg; if iowait >5%, inspect /proc/diskstats and ps -eo pid,pcpu,pmem,cmd --sort=-pcpu | head. If IO bound, throttle ingestion via CPU cgroups: mkdir -p /sys/",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enable ftrace sched_switch and sched_wakeup to measure scheduling latency during bursts, collect the trace, map long wakeup-to-run gaps to PIDs, then pin the offender with taskset -cp and tune schedul",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses hands-on Linux troubleshooting for latency with containerized workloads, focusing on memory reclamation, THP behavior, and safe, minimal-impact mitigations.\n\n## Key Concepts\n- Transparent Huge Pages (THP) behavior and toggling\n- Page cache vs memory pressure\n- Container isolation and CPU pinning\n- Default tooling for on-call debugging\n\n## Code Example\n```bash\n# Check THP state\ncat /sys/kernel/mm/transparent_hugepage/enabled\n# Observe memory/caching behavior\nvmstat 1 5\n# Disk and I/O detail\niostat -dx 1\n# Identify running containers\ndocker ps\n# See heavy processes inside a container\ndocker top <id>\n# Mitigation: disable THP and pin a pid to CPUs\nsudo tee /sys/kernel/mm/transparent_hugepage/enabled <<< 'never'\ntaskset -cp 0-7 <pid>\n```\n\n## Follow-up Questions\n- How would you validate no performance regression after disabling THP? \n- How could you automate this workflow for on-call use?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-11",
        "questionId": "q-5646",
        "question": "Scenario: A Linux host serving a Python API experiences sudden tail latency spikes during bursts; CPU, memory, and disk I/O look normal. Using only default tooling, design a concrete diagnostic workflow to (1) determine if the bottleneck is network (sockets, NIC IRQs, or queue depth) or compute/IO, (2) identify the offending interface, process, or socket, and (3) apply a safe mitigation (e.g., adjust socket buffers, net core tunables, or scale workers) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run quick baselines: `top -b -n1`, `vmstat 1 5`, `iostat -xd 1 5`, and `ps -eo pid,pcpu,pmem,cmd --sort=-pcpu | head -n 6`. If CPU is hot, assign affinity with `taskset -pc <cpus> <pid>`; if I/O bound",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run iostat -dx 1 to spot IO wait, sar -n DEV 1 for NIC pressure, and ss -tpan to inspect sockets. Inspect /proc/interrupts and ethtool -S ethX to locate hot IRQs and RX/TX queues. Use pidstat -p ALL 1",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "**1. Prove I/O Scheduler or Disk Queue Pressure is the Bottleneck** ```bash # Monitor disk latency in real-time (1-second intervals for 60 seconds) iostat -dx 1 60 # Sample output during burst: # Device r/s w/s rkB/s wk…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Start with top to gauge CPU/IO wait. Run vmstat 1 10 and iostat -xz 1 10 to spot disk or device latency. Use ss -tulpn and ss -s to watch sockets and backlog. If backlog grows, raise somaxconn and ngi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDesigning a diagnostic workflow to distinguish network vs CPU/IO bottlenecks on a live Linux host is essential. The candidate should justify using baseline observability, then converge on a bottleneck with concrete commands.\n\n## Key Concepts\n- IO wait vs network pressure\n- Tools: iostat, sar, ss, ethtool, pidstat\n- Safe mitigations: socket buffers, backlog, worker tuning\n\n## Code Example\n```javascript\n// pseudo parsing of iostat outputs in JS (illustrative) \n```\n\n## Follow-up Questions\n- How would you validate the mitigation's effectiveness?\n- How would you scale this approach to multiple hosts?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-12",
        "questionId": "q-17603",
        "question": "Scenario: A Linux host serving a high-throughput streaming pipeline experiences tail latency spikes during bursts while CPU, memory, and network look healthy. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove blocked I/O or scheduler saturation is causing stalls, (2) locate the exact block device and I/O queue responsible and the process(es) issuing the I/O, and (3) apply a safe mitigation (e.g., switch to mq-deadline or bfq, adjust queue_depth/read-ahead, or enable/disable a specific offload) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run vmstat 1 5 to gauge CPU/mem, sample /proc/diskstats for IO waits, read /proc/meminfo for pressure, and ps -eo pid,cmd,%cpu --sort=-%cpu | head to spot hot processes. Check NUMA with numactl --hard",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use iostat -dx 1 2 and vmstat 1 2 to observe IO stalls; inspect schedulers with cat /sys/block/sd*/queue/scheduler; map I/O to PIDs with pidstat -d 1; switch to mq-deadline: echo mq-deadline > /sys/bl",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use iostat -xd 1, vmstat 1, and iotop to correlate bursts with metadata I/O. Inspect /proc/diskstats and current mount options; identify hot processes via pidstat -d 1 -p ALL. Check ext4 journal setti",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check clocksource and timer warnings: cat /sys/devices/system/clocksource/clocksource0/current_clocksource and dmesg | grep -i clock. Compare host vs container times: date +%s%N on host and inside con",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical debugging of block I/O and scheduler behavior under bursty load, requiring hands-on use of standard Linux tools and safe mitigations.\n\n## Key Concepts\n- Block I/O schedulers (mq-deadline, bfq, cfq)\n- Per-disk metrics (iostat, vmstat)\n- Scheduler visibility via /sys/block/.../queue/scheduler\n- Process-level I/O mapping (pidstat, similar)\n\n## Code Example\n```bash\niostat -dx 1 2\nvmstat 1 2\ncat /sys/block/sda/queue/scheduler\npidstat -d 1\n```\n```",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-13",
        "questionId": "q-8002",
        "question": "Scenario: A Linux host running a Python microservice in Docker experiences occasional 1-2s latency spikes under sustained load. With default tools only, design a concrete diagnostic workflow to (1) confirm if latency is kernel scheduler or memory pressure, (2) locate offending process/container, (3) apply safe mitigations (cpuset/timeslice/cgroup limits) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run vmstat 1 120 to confirm wa/si and swap thrash; free -m and grep Swap /proc/meminfo to quantify pressure; run iostat -dx 1 60 to spot device latency; map memory to NUMA with numactl --hardware and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Diagnose tail latency by correlating per-CPU load with time windows, then drill into /proc/sched_debug to map back to offending runqueues and tasks. Verify memory pressure via /proc/vmstat and min_fre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**1. Prove I/O Scheduler or Disk Queue Pressure is the Bottleneck** ```bash # Monitor disk latency in real-time (1-second intervals for 60 seconds) iostat -dx 1 60 # Sample output during burst: # Device r/s w/s rkB/s wk…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Begin with system-wide metrics: vmstat 1 60 to spot wa and swapping; if wa or swaps spike, run pidstat -u -p ALL 1 60 to identify cpu hogs. Map PID to container via /proc/[pid]/cgroup, then pin with t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical Linux performance diagnostics using only default tools, covering CPU, memory, and container boundaries in production-like setups.\n\n## Key Concepts\n\n- vmstat and pidstat for system and per-process metrics\n- /proc/[pid]/cgroup to map hosts to containers\n- taskset and container cpuset updates for safe isolation\n- memory.max / memory.limit_in_bytes for memory pressure mitigation\n\n## Code Example\n\n```bash\nvmstat 1 60\npidstat -u -p ALL 1 60\n```\n\n## Follow-up Questions\n\n- How would you handle a multi-tenant host with noisy neighbor containers?\n- What non-disruptive observability improvements would you add to prevent regression?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-14",
        "questionId": "q-26832",
        "question": "Scenario: A Linux host runs a stateful video-processing service with local NVMe storage. During bursts, I/O tail latency spikes appear while CPU/disk wait seems normal. Using only default Linux tools, design a concrete diagnostic to (1) confirm if I/O scheduling or block layer is the bottleneck, (2) identify the exact device/queue and the process owning the IO, and (3) apply safe mitigations (mq-deadline, queue_depth, read_ahead) while preserving observability. Provide exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run vmstat 1 5 to gauge CPU/mem, sample /proc/diskstats for IO waits, read /proc/meminfo for pressure, and ps -eo pid,cmd,%cpu --sort=-%cpu | head to spot hot processes. Check NUMA with numactl --hard",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Diagnosing CPU Throttling on a Linux Server** Your Python daemon slows during bursts while CPU and memory appear normal. The issue is likely CPU throttling via cgroups. Here's a beginner-friendly workflow using only d…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To diagnose container CPU throttling, first examine cgroup statistics: `cat /sys/fs/cgroup/cpu/mycontainer/cpu.stat` and watch for spikes in `nr_throttled` during latency bursts. Identify the specific container with `do…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run: iostat -dx 1 2 to verify high await on the NVMe device during bursts. Check the scheduler: cat /sys/block/nvme0n1/queue/scheduler. If needed: echo mq-deadline > /sys/block/nvme0n1/queue/scheduler",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical storage bottleneck debugging at the kernel/block layer with minimal tooling, a common enterprise scenario.\n\n## Key Concepts\n- Block layer scheduling (mq-deadline vs alternatives)\n- Per-queue depth and read-ahead tuning\n- IO-to-process mapping via pidstat and /proc/diskstats\n\n## Code Example\n```bash\n# diagnose\niostat -dx 1 2\ncat /sys/block/nvme0n1/queue/scheduler\n# mitigate\necho mq-deadline > /sys/block/nvme0n1/queue/scheduler\necho 1024 > /sys/block/nvme0n1/queue_depth\n```\n\n## Follow-up Questions\n- How would you validate long-term observability after changes?\n- What are risks of increasing queue_depth on shared NVMe pools?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-15",
        "questionId": "q-19076",
        "question": "Scenario: A Linux host behind a small web app experiences tail latency spikes during bursts while average metrics look fine. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm whether NIC queue pressure or interrupts are the cause, (2) identify the interface and IRQ bottleneck, and (3) apply a safe mitigation (e.g., bind IRQs, enable irqbalance, tune RX/TX rings or GRO/TSO) while preserving observability. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To diagnose container CPU throttling, first examine cgroup statistics: `cat /sys/fs/cgroup/cpu/mycontainer/cpu.stat` and watch for spikes in `nr_throttled` during latency bursts. Identify the specific container with `do…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Strategy: 1) verify swap pressure with free -h and vmstat 1 5 to observe swap in/out. 2) identify top swap users by parsing /proc/*/status VmSwap for all pids; map to commands via ps -eo pid,ppid,cmd,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check per-cgroup quotas and usage to confirm throttling, then identify the offending group and CPU. Commands: cat /sys/fs/cgroup/cpu/*/cpu.stat; cat /sys/fs/cgroup/cpu/<grp>/cpu.cfs_quota_us; pidstat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Baseline traffic; monitor interface stats and IRQs during bursts. Commands: ip -s link show eth0; cat /proc/interrupts | grep eth0; sar -n DEV 1 10; if a hot IRQ is seen, bind it: echo 1 > /proc/irq/4",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical NIC troubleshooting under bursty load using only default tools.\n\n## Key Concepts\n- NIC queue pressure and per-IRQ load\n- Interrupt affinity and irqbalance\n- GRO/TSO offloads and RX ring tuning\n- Observability of network tail latency\n\n## Code Example\n```javascript\n// Diagnostic sequence (conceptual)\nip -s link show eth0\ncat /proc/interrupts | grep eth0\nsar -n DEV 1 10\n```\n\n## Follow-up Questions\n- What if multiple IRQs are hot across interfaces?\n- How would you roll back changes if latency worsens?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-16",
        "questionId": "q-22121",
        "question": "Scenario: A Linux host running a high-concurrency, multi-tenant service experiences sporadic tail latency spikes during bursts, with CPU and I/O shown as normal. Using only default Linux tools, design a concrete diagnostic to (1) confirm kernel memory allocator fragmentation or slab pressure as the bottleneck, (2) locate the offending slab caches and the processes touching them, and (3) apply safe mitigations (increase vm.min_free_kbytes, drop_caches, consider THP tweaks or cgroup memory limits) while preserving observability. Provide exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Essential Linux commands span file operations (ls, cp, mv, rm, find), text processing (grep, sed, awk, cut, sort, uniq), process management (ps, top, htop, kill, nice, jobs), network diagnostics (netstat, ss, curl, wget…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Diagnose with slabtop -o C, cat /proc/slabinfo | head, free -m, vmstat 1 60. If reclaim spikes align with latency, enable zone_reclaim_mode: echo 1 > /proc/sys/vm/zone_reclaim_mode; raise min_free_kby",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run `slabtop -o` once to identify hot caches (e.g., kmem_cache, dentry, inode_cache). For continuous monitoring, use `watch -n1 'slabtop -o | head -20'`. Simultaneously, monitor VM activity with `vmstat 1 60` and examin…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Run the following diagnostics to identify kernel memory pressure: ```bash # Memory overview free -m # Watch for page reclaim activity (pswpin/pswpout spikes) vmstat 1 5 # Check page fault and reclaim counters grep -E 'p…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests real-world ability to diagnose kernel allocator issues under high-concurrency, multi-tenant load using only default Linux tools—a common but often under-specified root cause of tail latency spikes. It evaluates pattern recognition, correct tool selection, and the ability to apply safe mitigations without destabilizing production systems.\n\n## Key Concepts\n- Slab memory behavior and identification of hot caches\n- Correlation between tail latency spikes and kernel allocator code paths\n- Preserving observability while applying mitigations\n\n## Diagnostic Commands\n`slabtop -o` displays per-CPU slab cache statistics in real-time, sorted by object count. `cat /proc/slabinfo` provides raw slab allocator data. `vmstat 1` reveals page faults and swap activity that accompany allocator contention. `/proc/vmstat` contains granular VM counters including `slab_reclaimable`, `slab_unreclaimable`, and `nrf_slab`.",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-17",
        "questionId": "q-15313",
        "question": "Scenario: A Linux host runs a Kubernetes cluster on a quad-socket NUMA machine. During bursts, a latency-sensitive service exhibits tail latency spikes (50–200 ms) while averages are normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove tail latency stems from cross-NUMA thread migrations, (2) identify offending thread(s) and their NUMA nodes, and (3) apply safe mitigations (pin critical threads with cpuset/numactl, bind memory, minimize migrations, and preserve observability). Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check logs: journalctl -u backup.service -n 100; verify path perms: ls -ld /var/backups; stat /var/backups; identify writer: lsof +D /var/backups | head; fix: sudo chown -R backup:backup /var/backups;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run top in batch to spot heavy CPU: top -b -n1; then dump top processes: ps -eo pid,comm,pcpu,pmem --sort=-pcpu | head -5. Check IO with iostat -dx 1 2 and memory with free -m. If a process hogs, grac",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Pin the service’s threads to a single NUMA node and monitor migrations with numastat -p <pid> and /proc/<pid>/sched. Capture scheduler switches with perf sched record -e sched_switch -p <pid> -a. Miti",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Begin by confirming reclaim pressure: vmstat 1 60; watch si/so and /proc/vmstat pswpin/pswpout. Map container PIDs to their cgroups via /proc/[pid]/cgroup and inspect memory.stat and memory.usage_in_b",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical troubleshooting of NUMA-induced tail latency, a common real-world bottleneck in multi-socket hosts.\n\n## Key Concepts\n- NUMA affinity, cross-node migrations, numastat, /proc/[pid]/sched, perf sched.\n- CPUSets and numactl for binding and memory binding.\n\n## Code Example\n```bash\nnumactl --hardware\npid=12345\nnumastat -p $pid\ncat /proc/$pid/sched\nperf sched record -e sched_switch -p $pid -a --duration 30\n```\n\n## Follow-up Questions\n- How would you validate observability after applying bindings? \n- What are potential trade-offs of aggressive binding in a busy cluster?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-18",
        "questionId": "q-24001",
        "question": "Scenario: A Linux host running a latency-critical service experiences intermittent tail latency spikes under bursty traffic, with no obvious I/O or CPU bottlenecks. Using only default Linux tools, design a concrete diagnostic to (1) prove kernel lock contention or preemption is the bottleneck, (2) pinpoint the exact lock and code path, and (3) apply safe mitigations (pin threads, tune scheduler, or rework critical sections) while preserving observability. Provide exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run vmstat 1 5 to confirm IO wait; free -m and cat /proc/meminfo to gauge page cache; cat /proc/diskstats to map device IO; top -b -n1 to spot high IO processes; if available, iostat -dx 1; then set v",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Start by checking THP: cat /sys/kernel/mm/transparent_hugepage/enabled and /defrag; monitor reclaim with vmstat 1; identify large RSS processes via ps aux --sort=-rss; map memory with pmap -x <pid>. M",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Step 1: Confirm I/O Scheduler as Bottleneck** ```bash # Monitor I/O latency during traffic bursts iostat -xz 1 vmstat 1 ``` Look for elevated `await` times (target > 50ms) on NVMe devices correlating with traffic spik…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enable ftrace sched_switch and sched_wakeup to measure scheduling latency during bursts, collect the trace, map long wakeup-to-run gaps to PIDs, then pin the offender with taskset -cp and tune schedul",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes deep knowledge of Linux scheduling, tracing, and reliability under bursts. It requires translating observations into surgical mitigations without changing application code.\n\n## Key Concepts\n- Kernel scheduling latency and preemption\n- ftrace scheduling tracepoints (sched_switch, sched_wakeup)\n- Process pinning and scheduler tunables\n- Observability trade-offs when applying fixes\n\n## Code Example\n```javascript\n// Demonstrative commands (not real JS) for enabling tracing\n// echo function > /sys/kernel/debug/tracing/current_tracer\n// echo 1 > /sys/kernel/debug/tracing/events/sched/sched_switch/enable\n```\n\n## Follow-up Questions\n- If pinning moves burden to other CPUs, how would you validate global throughput?\n- How would you automate trace collection in a small production window?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-19",
        "questionId": "q-16219",
        "question": "Scenario: A Linux host runs a Dockerized Node.js API behind Nginx; during HTTP bursts, tail latency spikes ~100-200 ms while CPU and I/O appear normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm the spikes are caused by CPU throttling from cgroup quotas, (2) locate the exact container/process and its cgroup limits and CPU share settings, and (3) apply a safe mitigation (raise quota, isolate CPUs with cpuset, or switch to guaranteed CPU) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by confirming reclaim pressure: vmstat 1 60; watch si/so and /proc/vmstat pswpin/pswpout. Map container PIDs to their cgroups via /proc/[pid]/cgroup and inspect memory.stat and memory.usage_in_b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run: `top -b -n1`; `vmstat 1`; `iostat -xz 1 2`; `pidstat -p ALL 1` to identify hot processes; then `perf stat -e cache-references,cache-misses,cycles -p <pid> -I 1000`; `perf top -p <pid>`. If cache misses are high, pi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: identify bottleneck with ethtool -G eth0 and /proc/interrupts; quantify kernel load with perf stat -e softirq -p <PID>; bind IRQs via /proc/irq/IRQ/smp_affinity; grow RX/TX rings with ethtool -G",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check if latency spikes are caused by container CPU throttling. Identify the container, read its cgroup CPU quotas, and correlate with latency. Commands: docker ps; docker inspect --format '{{.Id}}' <",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to diagnose container-level CPU throttling using only native tools. \n\n## Key Concepts\n- cgroups CPU quotas and periods\n- cpuset isolation and container CPU pinning\n- correlating latency with resource limits using minimal tooling\n- observability after changes\n\n## Code Example\n```bash\n# quick quota check (container path from docker inspect)\nQUOTA=$(cat /sys/fs/cgroup/cpu/your_container_path/cpu.cfs_quota_us)\nPERIOD=$(cat /sys/fs/cgroup/cpu/your_container_path/cpu.cfs_period_us)\necho $QUOTA $PERIOD\n```\n\n## Follow-up Questions\n- How would you verify changes stuck only to CPU-bound requests? \n- What are trade-offs of increasing quota vs. isolating CPUs in a mixed workload environment?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-20",
        "questionId": "q-11255",
        "question": "Scenario: A Linux host running a streaming data platform in containers experiences tail latency spikes during bursts, while CPU, memory, and disk I/O appear normal. Using only default Linux tools, design a diagnostic workflow to (1) confirm whether page cache pressure or slab fragmentation drives the tail latency, (2) locate the container or process consuming memory or cache, (3) apply safe mitigations (e.g., tune vfs_cache_pressure, min_free_kbytes, drop_caches, adjust memory limits) while preserving observability. Include concrete commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use iostat -dx 1 to verify device wait, vmstat 1 to track cache/page activity, and slabtop -o Size -a to reveal slab pressure. Use ps -eo pid,cmd,rss --sort=-rss to identify memory hogs in containers.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Workflow: verify spike with top/iostat, inspect THP and page cache, locate offending container, and apply a safe mitigation. Commands: cat /sys/kernel/mm/transparent_hugepage/enabled; vmstat 1 5; iost",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To diagnose use: cat /proc/interrupts; cat /proc/timer_list; while bursts occur, run pidstat -p ALL 1 and vmstat 1. Correlate spikes to timer/softirq counters. Identify culprit via /proc/<tid>/sched o",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Begin by confirming IO wait and tail latency with: iostat -dx 1 5; pidstat -d 1; iotop -b -n 1. Identify contending device from /proc/diskstats and per-device scheduler in /sys/block/*/queue/scheduler",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests triage of latent tail latency due to kernel memory pressure, a subtle yet critical production issue.\n\n## Key Concepts\n- Page cache vs slab allocator pressure\n- slabtop, vmstat, iostat, dmesg\n- cgroup memory considerations and safe mitigations\n\n## Code Example\n```javascript\n#!/usr/bin/env node\nconst {execSync} = require('child_process');\nconsole.log(execSync('iostat -dx 1 2').toString());\nconsole.log(execSync('vmstat 1 5').toString());\n```\n\n## Follow-up Questions\n- How would you validate the effect of a mitigation across a live cluster?\n- How would you adjust observability to detect regressions after changes?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-21",
        "questionId": "q-14232",
        "question": "Scenario: A Linux host with a multi-tenant microservice stack on cgroups shows 50–200 ms tail latency during bursts, but CPU/IO/network look fine. Using only default tools, design a workflow to (1) prove CPU throttling via cpu.cfs_quota_us, (2) locate the throttled container and its CPU, (3) safely mitigate (increase quotas, set cpuset/isolate_cpus) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run numactl --hardware and numastat -m to verify NUMA topology and locality. Use mpstat -P ALL 1 and iostat -xz 1 to detect CPU throttling or IO waits; inspect /proc/<pid>/numa_maps for hotspots. If l",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check quotas with cat /sys/fs/cgroup/cpu/my.slice/cpu.stat; cat /sys/fs/cgroup/cpu/my.slice/cpu.cfs_quota_us; systemd-cgls --unit my.slice; ps -eo pid,pcpu,cmd --sort=-pcpu | head; mitigate by echo 20",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Start with `vmstat 1 5` to confirm I/O wait spikes while CPU remains idle; run `iostat -xz 1 5` to identify the device with high await. Use `iotop -oaP` to map I/O by PID, then inspect `/sys/block/sdX/queue/scheduler` a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run `slabtop -o` once to identify hot caches (e.g., kmem_cache, dentry, inode_cache). For continuous monitoring, use `watch -n1 'slabtop -o | head -20'`. Simultaneously, monitor VM activity with `vmstat 1 60` and examin…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of cgroup CPU throttling as a production tail-latency cause and how to diagnose with defaults. It also covers safe, observable mitigation in multi-tenant environments.\n\n## Key Concepts\n- CPU quotas and throttling in cgroups\n- cgroup v1 paths and cpu.stat outputs\n- Identifying throttled tasks and their containers\n- Safe mitigations: adjust quotas, cpuset isolation, observability\n\n## Code Example\n```bash\n#!/bin/bash\nSLICE=${1:-my.slice}\necho \"Quota US: $(cat /sys/fs/cgroup/cpu/$SLICE/cpu.cfs_quota_us)\"\necho \"Period US: $(cat /sys/fs/cgroup/cpu/$SLICE/cpu.cfs_period_us)\"\n```\n\n## Follow-up Questions\n- How would this differ on cgroup v2? What lines change in cpu.stat?\n- What are the risks of increasing quotas on a shared host, and how would you validate no regression elsewhere?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-22",
        "questionId": "q-29637",
        "question": "Scenario: A containerized microservice suite on a Linux host uses cgroup v2 for per-namespace CPU quotas. During bursts, tail latency spikes appear while overall CPU usage remains low. Using only default Linux tools, design a diagnostic workflow to (1) prove tail latency is caused by CPU throttling from cpu.max quotas, (2) locate the throttled thread(s) and their container, (3) apply safe mitigations (adjust cpu.max, migrate to a more flexible quota policy, or isolate offending cgroups) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by collecting memory and IO metrics with vmstat, free/meminfo, slabtop, and iostat; map NUMA layout with numactl --hardware; identify pressure sources per container via /proc/*/smaps or docker s",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by confirming reclaim pressure: vmstat 1 60; watch si/so and /proc/vmstat pswpin/pswpout. Map container PIDs to their cgroups via /proc/[pid]/cgroup and inspect memory.stat and memory.usage_in_b",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Diagnose CPU throttling using cpu.stat to check throttle metrics: ```bash # View throttling statistics for a container cgroup cat /sys/fs/cgroup/system.slice/Container-app.service/cpu.stat # Sample output: # nr_periods…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: map high-CPU PIDs to container cgroups, verify quotas, then raise quotas or pin CPUs. Commands:\\nps -eo pid,pcpu,comm --sort=-pcpu | head -n5\\nfor pid in $(ps -eo pid --no-headers); do echo -n \"",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question validates understanding of cgroup v2 CPU throttling mechanics and how per-container quotas can cause tail latency despite low aggregate host CPU utilization. It assesses practical skills in correlating system metrics to application performance and implementing safe, measurable mitigations.\n\n## Key Concepts\n- cgroup v2 cpu.stat and cpu.max interfaces for quota enforcement\n- CPU throttling detection via throttled_time and throttled_count metrics\n- Process-to-cgroup mapping via /proc/[pid]/cgroup\n- Mitigation strategies: quota adjustment, weight-based scheduling (cpu.weight), and core isolation (cpuset)\n- Preserving observability through maintained metric access\n\n## Diagnostic Workflow\n\n**Step 1: Detect throttling**\n```bash\ncat /sys/fs/cgroup/unified/IoT-app.slice/cpu.stat\n# throttled_count > 0 confirms quota enforcement is active\n```\n\n**Step 2: Map bursts to PIDs**\n```bash\ncat /proc/1234/cgroup\n# Output: 0::/system.slice/IoT-app.slice\n```\n\n**Step 3: Identify offending threads**\n```bash\ncat /sys/fs/cgroup/unified/IoT-app.slice/cgroup.procs\n```\n\n**Step 4: Apply mitigation**\n```bash\n# Option A: Increase quota (600ms per 100ms period = 6 CPUs)\necho \"600000 100000\" > /sys/fs/cgroup/unified/IoT-app.slice/cpu.max\n\n# Option B: Migrate to weight-based sharing\n# Assign relative weight instead of hard quota\n# cpu.weight accepts values 1-10000 (default: 100)\necho \"500\" > /sys/fs/cgroup/unified/IoT-app.slice/cpu.weight\n\n# Option C: Isolate to specific cores\n# Pin container to cores 4-7\nmkdir /sys/fs/cgroup/unified/isolated.slice\necho \"4-7\" > /sys/fs/cgroup/unified/isolated.slice/cpuset.cpus\n```\n\n**Step 5: Verify observability**\nAfter applying mitigations, monitor cpu.stat to confirm throttling decreases while preserving the ability to track future issues.",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-23",
        "questionId": "q-13914",
        "question": "Scenario: A Linux host running a Rust microservice experiences tail latency spikes (150-500 ms) during bursts, while CPU and I/O appear normal. You suspect kernel memory reclaim pressure or THP activity. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm memory pressure/THP correlates with tail latency, (2) identify the source (anon, file cache, THP, or slab), and (3) apply safe mitigations (adjust vm.swappiness, disable THP, tweak vfs_cache_pressure, or pin critical data) with minimal observability impact. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use default tools to prove NUMA locality issues and pin workloads: 1) check topology: numactl --hardware; 2) map per-container memory: for each container pid, cat /proc/PID/numa_maps and numastat -p P",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: Confirm that tail latency correlates with CPU frequency scaling and thermal throttling, then isolate affected cores and apply targeted mitigations. **Step 1: Verify CPU Frequency Scaling and Throttling** Check cur…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run: vmstat 1 60; iostat -dx 1; cat /proc/vmstat; cat /proc/meminfo; cat /sys/kernel/mm/transparent_hugepage/enabled. If si/so spikes and THP counters rise during latency, disable THP: echo never > /s",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Run vmstat 1 120 to confirm wa/si and swap thrash; free -m and grep Swap /proc/meminfo to quantify pressure; run iostat -dx 1 60 to spot device latency; map memory to NUMA with numactl --hardware and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical memory-management diagnostics under bursts, ensuring the candidate correlates latency with kernel reclaim signals and THP activity using only built‑in tools.\n\n## Key Concepts\n\n- Linux memory management and THP\n- Observability with vmstat, iostat, /proc files\n- Safe memory knob tweaks (swappiness, vfs_cache_pressure)\n\n## Code Example\n\n```bash\nvmstat 1 60\niostat -dx 1\n```\n\n```bash\ncat /proc/vmstat\ncat /proc/meminfo\ncat /sys/kernel/mm/transparent_hugepage/enabled\n```\n\n```\n## Follow-up Questions\n\n- If latency remains after THP disable, what’s next?\n- How would you verify observability after changes without regressions?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-24",
        "questionId": "q-20849",
        "question": "In a Linux host running a local REST service in a Docker container, nightly backups cause intermittent 1–2s startup latency for new connections while overall metrics look fine. Using only default Linux tools, design a practical workflow to (1) determine whether CPU scheduling, memory pressure, or network stack is the culprit, (2) identify the exact process or container responsible for the delay, and (3) apply safe mitigations (adjust container memory limits, stagger backups, or tweak NIC settings) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: verify memory pressure via /proc/vmstat, /proc/meminfo, and vmstat; detect reclaim activity (pgfault, pgrefill). Identify containers with high rss via ps, top, and pmap; check NUMA",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check limits and port usage: run ulmit -n and cat /proc/sys/fs/file-max. If exhausted, expect EAGAINs. Map active connections with ss -tanp | grep -E 'ESTAB|TIME-WAIT'. Inspect ephemeral range with ca",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check quotas and throttle via /sys and per-process data. Use mpstat to confirm throttling, and map PIDs to container via /proc/<pid>/cgroup. If quota shows a bottleneck, raise quota (e.g., for v2: sud",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Baseline with: vmstat 1 10; iostat -xz 1 10; ps -eo pid,comm,%cpu,%mem --sort=-%cpu | head -n 6; identify container: docker ps; docker stats --no-stream; watch for entry times that align with backup.",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to isolate latency causes under simple resource contention using default tools. It evaluates basic instrumentation, container mapping, and safe mitigations that preserve observability.\n\n## Key Concepts\n- Resource contention between host backups and services\n- Basic Linux monitoring (vmstat, iostat, ps, docker ps)\n- Mapping host processes to containers and back\n- Safe mitigations: memory limits, cron jitter, NIC tuning\n\n## Code Example\n```javascript\n# baseline commands\nvmstat 1 10\niostat -xz 1 10\nps -eo pid,comm,%cpu,%mem --sort=-%cpu | head -n 6\ndocker ps\n```\n\n## Follow-up Questions\n- How would you verify observability after changes?\n- If iostat shows high iowait but ps shows low CPU, what next?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-01T11:13:39.480Z",
    "version": 1
  }
]