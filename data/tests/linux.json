[
  {
    "id": "test-linux",
    "channelId": "linux",
    "channelName": "Linux",
    "title": "Linux Knowledge Test",
    "description": "Test your understanding of Linux with 25 questions.",
    "questions": [
      {
        "id": "linux-q-0",
        "questionId": "q-4769",
        "question": "On a single Linux host with NUMA, tail latency spikes under sustained load from a real-time event processor while CPU usage remains moderate. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm whether stalls are caused by Transparent Huge Pages or page cache thrashing, (2) identify the offending memory consumer and its allocations, and (3) apply a safe mitigation (e.g., disable THP, adjust swappiness, pin the hot thread) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Diagnose memory pressure and swap thrash as the tail-latency root cause on a containerized Linux host. Steps: 1) free -m; vmstat 1 60; 2) ps -eo pid,comm,rss --sort=-rss | head; 3) map to cgroups via",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Step 1: Confirm I/O Scheduler as Bottleneck** ```bash # Monitor I/O latency during traffic bursts iostat -xz 1 vmstat 1 ``` Look for elevated `await` times (target > 50ms) on NVMe devices correlating with traffic spik…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check THP: cat /sys/kernel/mm/transparent_hugepage/enabled; memory pressure: free -m; vmstat 1 5; Identify heavy consumer: ps -eo pid,cmd,%mem,%cpu --sort=-%mem | head; inspect mappings: head -n 20 /p",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use `ethtool` to inspect per-queue statistics and NAPI behavior. Check `/proc/interrupts` for interrupt distribution across CPUs, and verify TX queue depth via `/sys/class/net/<iface>/queues/<n>/tx_queue_len`. For inter…",
            "isCorrect": false
          }
        ],
        "explanation": "This question tests practical debugging of latency while using default tooling. Candidates must verify THP/page cache impact, locate the heavy memory consumer via ps and smaps, and apply safe mitigations (disable THP, tune swappiness, pin CPU affinity) while preserving observability.",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-1",
        "questionId": "q-11227",
        "question": "Scenario: A Linux host runs a Dockerized REST API behind Nginx. During bursts, response latency spikes 100–300 ms, CPU/RAM look idle. Using only default tooling, describe a beginner-friendly workflow to (1) confirm whether CPU throttling by cgroups is the cause, (2) map throttling events to the exact container and process, and (3) apply a safe mitigation (e.g., raise the container’s CPU quota or pin it to cores) while preserving observability. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed workflow: (1) prove reclaim pressure with vmstat 1 10; slabtop -o; cat /proc/vmstat | grep -E 'pgfault|kswapd'; (2) locate offenders with pidstat -p ALL 1; examine /proc/[pid]/smaps and /proc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by examining per-CPU utilization with `mpstat -P ALL 1` to identify any single CPU dominating the load. Simultaneously, monitor softirq activity using `watch -n1 'cat /proc/softirq'` to detect NET_RX/NET_TX spikes…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check throttling with: cat /sys/fs/cgroup/cpu.stat (or cpu.max on v2) to read throttled_time. Map the responsible container by combining docker ps with docker top and inspecting /proc/<pid>/cgroup for",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "To diagnose use: cat /proc/interrupts; cat /proc/timer_list; while bursts occur, run pidstat -p ALL 1 and vmstat 1. Correlate spikes to timer/softirq counters. Identify culprit via /proc/<tid>/sched o",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to diagnose container-level CPU throttling using native Linux tooling, emphasizing observability and safe mitigation.\n\n## Key Concepts\n- cgroups v1/v2 throttling data\n- Mapping PIDs to containers\n- Safe quota adjustments and CPU pinning\n\n## Code Example\n```\n# Example commands provided in question\n```\n\n## Follow-up Questions\n- How would you verify no QoS regressions after increasing quotas?\n- What are potential side effects of CPU pinning?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-2",
        "questionId": "q-16146",
        "question": "Scenario: A Linux host running a Kubernetes node with multiple containers experiences tail I/O latency during daily backups. CPU and network appear healthy, but 95th percentile I/O latency spikes upward. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm I/O wait is driving tail latency and attribute it to a specific container/cgroup, (2) identify the block device, I/O scheduler, and perf counter signals involved, and (3) apply a safe mitigation (per-cgroup blkio throttling, cpuset isolation, or scheduler tuning) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Steps: 1) vmstat 1 60 to reveal swap/cache pressure; 2) slabtop -o to monitor kernel caches; 3) for each cgroup: cat /sys/fs/cgroup/memory/*/memory.usage_in_bytes and memory.limit_in_bytes; 4) ps -eo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Telemetry over 60s: free -h; vmstat 1 60; slabtop -o; check MemAvailable and SwapUsed to decide if pressure is from anonymous memory or cache. Map cgroups: systemd-cgls | grep -i memory; for each path",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use `lsof -i :8080` or `netstat -tulpn | grep 8080` to find the PID, then `kill -9 <PID>` to terminate it. For a safer approach, try `kill -15 <PID>` first to allow graceful shutdown.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run iostat -dx 1 to confirm high await and disk latency, then pidstat -d 1 to map I/O to processes. Identify device with lsblk and check scheduler via cat /sys/block/sdX/queue/scheduler. Correlate to",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical ability to diagnose I/O tail latency in a containerized Linux environment using only default tools, tying observable metrics to a container, and applying safe mitigations that preserve observability.\n\n## Key Concepts\n- I/O wait metrics and tail latency\n- Per-process vs per-container I/O attribution\n- Block device, scheduler, and cgroup interfaces\n- Safe mitigations: throttling, pinning, and scheduler tweaks\n\n## Code Example\n```bash\n# Basic discovery of I/O health and mapping\niostat -dx 1\npidstat -d 1\nlsblk\ncat /sys/block/sdX/queue/scheduler\n```\n\n## Follow-up Questions\n- How would you adapt this to cgroup v2 io.max and controllers?\n- What instrumentation would you add to automate ongoing detection and alerting?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-3",
        "questionId": "q-10636",
        "question": "Scenario: A Linux host serves a lightweight REST API behind Nginx. During bursts, requests stall 1–2 seconds even though CPU and I/O look normal. Using only default Linux tools, design a beginner-friendly workflow to (1) confirm if stalls are due to file descriptor exhaustion, (2) identify the exact process hitting the limit and its open files, and (3) apply a safe mitigation (e.g., raise per-process or system limits, adjust Nginx worker_connections, or enable a simple watchdog) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify throttling with: cat /sys/fs/cgroup/containers/db/cpu.max; cat /sys/fs/cgroup/containers/db/cpu.stat. Example: 500000 1000000 and nr_periods 12345 throttled_time 230000000. Map processes: grep",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Diagnostic Workflow ### Step 1: Confirm IO Subsystem Saturation Run iostat to identify elevated await times and queue depth: ```bash $ iostat -dx 1 5 ``` **Sample output showing latency spikes:** ``` Device r_await w…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Confirm FD exhaustion by checking each nginx worker's limits and fd count: for p in $(pgrep -f nginx); do echo PID=$p; grep -w 'Open files' /proc/$p/limits; ls -1 /proc/$p/fd | wc -l; done. If near th",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "During bursts, collect: iostat -x 1, pidstat -p ALL 1, and inspect /sys/devices/system/cpu/cpu*/cpuidle/state*/residency to see deep C-states; check /sys/devices/system/cpu/intel_pstate/no_turbo for t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical debugging of resource limits under bursty load using familiar Linux tools.\n\n## Key Concepts\n- File descriptor limits (ulimit, /proc/[pid]/limits)\n- Per-service limits with systemd (LimitNOFILE)\n- Nginx worker_connections and upstream backlogs\n- Observability: logs, curl tests, fd counts\n\n## Code Example\n```bash\n# inspect limits and fds\nfor p in $(pgrep -f nginx); do echo PID=$p; grep -w 'Open files' /proc/$p/limits; ls -1 /proc/$p/fd | wc -l; done\n```\n\n## Follow-up Questions\n- How would you automate this with a script?\n- What are safe thresholds for changing limits and how to monitor impact?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-4",
        "questionId": "q-20556",
        "question": "Scenario: A Linux host runs a UDP-based service that experiences bursty packet loss and jitter, not visible in CPU/I/O. Using only default Linux tools, design a diagnostic to (1) confirm UDP drops and MTU/GRO/LSO involvement, (2) identify affected sockets/ports and NIC features, and (3) apply safe mitigations (disable GRO/LSO, adjust MTU) while preserving observability. Provide exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Diagnostic and Mitigation Approach ### 1. Confirm UDP Drops and Check MTU/GRO/LSO Involvement **UDP Drop Statistics:** ```bash # Overall UDP socket statistics netstat -su # Socket-level UDP details ss -u -a # Kernel…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Diagnostic Workflow ### Step 1: Confirm IO Subsystem Saturation Run iostat to identify elevated await times and queue depth: ```bash $ iostat -dx 1 5 ``` **Sample output showing latency spikes:** ``` Device r_await w…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use iostat -xd 1, vmstat 1, and iotop to correlate bursts with metadata I/O. Inspect /proc/diskstats and current mount options; identify hot processes via pidstat -d 1 -p ALL. Check ext4 journal setti",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run vmstat 1 120 to confirm wa/si and swap thrash; free -m and grep Swap /proc/meminfo to quantify pressure; run iostat -dx 1 60 to spot device latency; map memory to NUMA with numactl --hardware and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates a candidate's ability to diagnose network performance issues in Linux environments, specifically understanding the interaction between UDP traffic characteristics and NIC hardware offloads. It tests practical knowledge of default Linux tooling rather than specialized diagnostic software.\n\n## Key Concepts Covered\n\n- **UDP vs TCP Drop Behavior**: Unlike TCP, UDP has no built-in retransmission or flow control, making it vulnerable to burst-induced packet loss at buffers and the NIC level. The kernel simply drops packets when receive buffers overflow.\n\n- **NIC Offload Impact**: GRO (Generic Receive Offload) and LSO (Large Send Offload) can compound UDP jitter issues by batching packets in ways that interact poorly with latency-sensitive applications.\n\n- **MTU and Fragmentation**: Incorrect MTU settings cause fragmentation, increasing jitter and drop probability for UDP traffic.\n\n- **Observability Preservation**: The best mitigations preserve the ability to monitor network health through standard tools like netstat, ss, and ethtool.",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-5",
        "questionId": "q-2426",
        "question": "Context: A Linux host runs multiple containers on a single NIC behind a load balancer. During a traffic spike, a critical API shows increased latency and occasional 5xx errors. Using only default Linux tools, outline a concrete, practical diagnostic workflow to (1) determine whether latency comes from CPU, memory pressure, disk I/O, or network contention, (2) identify the exact container or process responsible, and (3) apply a safe mitigation (e.g., throttle or move to a different cgroup, adjust IO scheduler) with minimal disruption and full observability. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Vmstat during a burst to confirm CPU wait. Map container IDs from PIDs via /proc/*/cgroup. Inspect per-container cpu throttling with /sys/fs/cgroup/cpu/docker/<id>/cpu.stat (or cpu.max on v2). If thro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run vmstat 1 5 to capture swpd, free, buff/cache; iostat -xz 1 5 to observe device util and wa; top -b -n1 | head to spot hogs; ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu | head to identify culprit. I",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I’d start by collecting signals and mapping PIDs to containers: vmstat 1 5; iostat -xz 1; pidstat -ru 1; docker stats --no-stream. Then map PIDs to containers: grep -R '/docker/' /proc/$PID/cgroup. If",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "## Diagnostic Workflow for TLS Connection Failures Caused by File Descriptor Exhaustion ### Step 1: Prove FD Exhaustion Is the Bottleneck ```bash # System-wide FD limit (kernel ceiling) cat /proc/sys/fs/file-max # Curre…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests hands-on diagnostic reasoning for multi-tenant containers using only default tools, plus safe live mitigations without downtime.\n\n## Key Concepts\n- Container-to-process mapping via /proc/$PID/cgroup\n- Resource controllers: CPUQuota, memory.max\n- Observability: vmstat, iostat, pidstat, docker stats\n- Safe throttling to preserve availability\n\n## Code Example\n```\nvmstat 1 5\niostat -xz 1\npidstat -ru 1\ndocker stats --no-stream\ngrep -R '/docker/' /proc/$PID/cgroup\n```\n\n## Follow-up Questions\n- What if no container shows high CPU but latency persists?\n- How to verify no downtime after applying throttling (e.g., gradual ramp, canary container)?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-6",
        "questionId": "q-5318",
        "question": "On a Linux server hosting multiple services, intermittent latency spikes occur under memory pressure with THP enabled. Using only default tools, design a concrete diagnostic workflow to (1) verify if hugepage fragmentation is causing allocator delays, (2) identify the specific process or container consuming memory aggressively, and (3) apply a safe mitigation (e.g., disable THP on affected zone, adjust min_free_kbytes, or migrate memory pressure) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To diagnose CPU throttling from container quotas, first list running containers using `docker ps`, then examine cgroup metrics for each container at `/sys/fs/cgroup/cpu/docker/<container_id>/cpu.{cfs_quota_us,cfs_period…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a practical workflow: 1) run iostat -dx 1 to confirm sustained high 'await' and 'svctm'. 2) run iotop -o -P to catch the processes with IO. 3) inspect /proc/diskstats for the device, check 'ps",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Baseline THP state and memory pressure signals, then correlate with latency: - cat /sys/kernel/mm/transparent_hugepage/enabled - dmesg | grep -i thp - cat /proc/meminfo | grep -iHuge - for each cgroup",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Investigate IRQ tail latency on a container host with multiple NIC queues using this systematic diagnostic workflow: **Step 1: Identify IRQ hotspots** ```bash # View interrupt distribution per NIC queue grep -i eth0 /pr…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nMemory allocator stalls under pressure can stem from THP fragmentation; diagnosing with default tools tests hypothesis without specialized profilers.\n\n## Key Concepts\n- Transparent Huge Pages (THP)\n- Memory fragmentation and allocator latency\n- Per‑cgroup memory accounting\n- Safe kernel knob changes with observability\n\n## Code Example\n```bash\n# Commands mentioned in answer (for reference)\ncat /sys/kernel/mm/transparent_hugepage/enabled\ndmesg | grep -i thp\ngrep -i Huge /proc/meminfo\nfor c in /sys/fs/cgroup/memory/*; do echo \"$c\"; cat \"$c/memory.stat\"; done\n```\n\n## Follow-up Questions\n- After disabling THP, how would you validate no regression across services?\n- What persistent config would you apply to avoid regressions in future bursts?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-7",
        "questionId": "q-22853",
        "question": "Scenario: A Linux host runs a containerized event-processing service under bursty load. Tail latency spikes 200–400 ms while average latency remains fine and CPU, IO, and network metrics look normal. Using only default Linux tools design a concrete diagnostic workflow to (1) prove memory reclaim or allocator contention is the cause, (2) locate hot caches and offending threads with NUMA awareness, and (3) apply safe mitigations such as vm.zone_reclaim_mode=0, pinning memory to NUMA nodes, and per-cgroup mem limits, while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Baseline System Assessment ```bash # Capture current system state: load averages, memory, and any throttling messages top -bn1 && vmstat 1 5 && dmesg | grep -i throttle ``` ## Step 1: CPU Frequency Throttling Investi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check space: df -h. Find large files/dirs: du -hx --max-depth=1 / 2>/dev/null | sort -rh | head -n 5. Focus on /var/log: du -xh /var/log | sort -rh | head -n 5. Mitigate safely: tar czf /backup/logs-$",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin with a system-wide view, then drill down: - top -b -n1 - vmstat 1 - iostat -xz 1 - pidstat -p ALL 1 Compare CPU idle, IO wait, and memory pressure. If IO-wait is high, enable BFQ (or deadline) a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed workflow: (1) prove reclaim pressure with vmstat 1 10; slabtop -o; cat /proc/vmstat | grep -E 'pgfault|kswapd'; (2) locate offenders with pidstat -p ALL 1; examine /proc/[pid]/smaps and /proc",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to diagnose subtle kernel memory reclaim issues causing tail latencies, using only built-in tools and safe mitigations that preserve observability. It emphasizes NUMA awareness and careful workarounds rather than blunt quotas.\n\n## Key Concepts\n- Linux memory reclaim, slab allocator, kswapd\n- NUMA memory policies and per-cgroup limits\n- vm.zone_reclaim_mode and memory binding techniques\n\n## Code Example\n```bash\nvmstat 1 10\nslabtop -o\ngrep -E 'pgfault|kswapd' /proc/vmstat\n```\n\n## Follow-up Questions\n- How would you validate that the mitigation didn’t regress other services?\n- What are trade-offs of binding memory vs anonymizing across NUMA nodes?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-8",
        "questionId": "q-14690",
        "question": "Scenario: A Linux host running a TLS-heavy microservice stack shows sporadic tail latency spikes during bursts, while average metrics stay flat. You suspect entropy starvation is delaying TLS handshakes. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm entropy depletion causes the TLS stalls, (2) locate the offending process and handshake pattern, and (3) apply a safe mitigation (e.g., adjust TLS behavior or entropy strategy) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check entropy availability and correlate TLS handshakes with entropy depletion. Commands: cat /proc/sys/kernel/random/entropy_avail; ss -tanp | grep -i tls; lsof /dev/random; strace -e read -p <pid>",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Step 1: Confirm CPU Throttling Inspect throttling statistics for the target cgroup: ```bash cat /sys/fs/cgroup/cpu/myapp.slice/cpu.stat ``` Non-zero values in `nr_throttled` (number of throttled periods) and `throttl…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run: vmstat 1 60; cat /sys/kernel/mm/transparent_hugepage/enabled; grep -i 'Cached' /proc/meminfo; for p in $(ps -eo pid --no-headers); do echo PID $p; awk '/VmFlags|VmSize|Rss/ {print $0}' /proc/$p/s",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed approach: verify memory pressure via /proc/vmstat, /proc/meminfo, and vmstat; detect reclaim activity (pgfault, pgrefill). Identify containers with high rss via ps, top, and pmap; check NUMA",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nDiagnostics of tail latency in TLS-heavy stacks can hinge on entropy starvation, a subtle but real bottleneck. This tests ability to form a concrete workflow using only built-in Linux tools and to connect low-level entropy behavior with application latency.\n\n## Key Concepts\n\n- Entropy pool vs /dev/random vs /dev/urandom\n- TLS handshake latency and per-process visibility\n- Observability: correlating handshake events with system metrics\n\n## Code Example\n\n```bash\n# Basic diagnostic steps\ncat /proc/sys/kernel/random/entropy_avail\nss -tanp | grep -i tls\nlsof /dev/random\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-9",
        "questionId": "q-6373",
        "question": "Scenario: A Linux host running a MongoDB shard experiences sporadic 2–5 second stalls under high write load. Using only default Linux tools, design a concrete diagnostic workflow that (1) confirms storage I/O as the stall source, (2) identifies the exact device/queue causing latency, and (3) prescribes a safe mitigation (e.g., io-scheduler tuning, queue depth adjustments, or multi-queue enablement) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Diagnose memory pressure and swap thrash as the tail-latency root cause on a containerized Linux host. Steps: 1) free -m; vmstat 1 60; 2) ps -eo pid,comm,rss --sort=-rss | head; 3) map to cgroups via",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check THP status and memory pressure; use vmstat and meminfo; commands: cat /sys/kernel/mm/transparent_hugepage/enabled; grep Huge /proc/meminfo; vmstat 1 5; identify hot Java thread with ps -eo pid,c",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Start with `iostat -dx 1 2` to confirm elevated await times on the storage device. Use `iotop -P -o -d 1` to identify the specific process responsible for heavy I/O. Run `blktrace -d /dev/nvme0n1 -D /tmp/blktrace` follo…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Run `slabtop -o` once to identify hot caches (e.g., kmem_cache, dentry, inode_cache). For continuous monitoring, use `watch -n1 'slabtop -o | head -20'`. Simultaneously, monitor VM activity with `vmstat 1 60` and examin…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAdvanced storage-path diagnostics are critical for latency-sensitive workloads like MongoDB. This question probes practical use of default tools to isolate I/O bottlenecks, map them to devices and queues, and implement safe, observable mitigations.\n\n## Key Concepts\n- I/O wait diagnostics with iostat and iotop\n- Block-layer tracing with blktrace/blkparse\n- MongoDB latency visibility via serverStatus\n- Kernel I/O schedulers and per-device tuning\n\n## Code Example\n```bash\n# Example commands for quick reference\niostat -dx 1 2\niotop -P -o -d 1\nblktrace -d /dev/nvme0n1 -D /tmp/blktrace\nblkparse /tmp/blktrace\n```",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-10",
        "questionId": "q-27112",
        "question": "Scenario: A Linux host with quad-socket NUMA and a containerized microservices stack experiences sporadic 50-150 ms tail latency bursts during traffic spikes. Observability is limited to basic metrics. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove the tail latency is caused by memory pressure and page reclamation, (2) locate the offending process and its NUMA node and memory hotspots, (3) apply safe mitigations (tune swappiness, memory.use_percent, enable per-process memory binding or cgroup limits, and CPU/NUMA pinning) while preserving observability. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**1. Prove I/O Scheduler or Disk Queue Pressure is the Bottleneck** ```bash # Monitor disk latency in real-time (1-second intervals for 60 seconds) iostat -dx 1 60 # Sample output during burst: # Device r/s w/s rkB/s wk…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Prove latency stems from per-cgroup CPU throttling and fix safely. Commands: 1) cat /sys/fs/cgroup/kubepods.slice/cpu.stat 2) cat /sys/fs/cgroup/kubepods.slice/cgroup.procs 3) ps -p $(cat /sys/fs/cgr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "1) Run vmstat 1 60 to detect si/so and swap activity; 2) scan /proc for high VmSwap/VmRSS and map to NUMA via /proc/<pid>/numa_maps to find hot processes and their NUMA nodes; 3) mitigate by pinning h",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Run iostat -xz 1 to observe device latency; inspect /proc/interrupts to locate NIC IRQs and their counts; check per-process IO wait with pidstat -d 1 -p <pid>; view /proc/sched_debug for CPU throttlin",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nExamines practical, low-level diagnostic thinking under real constraints, focusing on memory pressure as a tail-latency driver and how to isolate and mitigate it without advanced tooling.\n\n## Key Concepts\n- Memory pressure signals (si/so, VmSwap, VmRSS)\n- Per-process NUMA mapping and binding\n- Safe mitigations (swappiness, cgroups, CPU pinning) with observability preserved\n\n## Code Example\n```bash\n# Example commands from the answer\nvmstat 1 60\nfor p in /proc/[0-9]*/status; do if [ -r \"$p\" ]; then echo -n \"$p: \"; grep -E 'VmSwap|VmRSS' \"$p\"; fi; done | sort -k3 -nr | head -n5\n```\n\n## Follow-up Questions\n- How would you adapt this workflow if swap is disabled? \n- What metrics would you add to your observability to catch regressions after mitigation?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-11",
        "questionId": "q-12180",
        "question": "Scenario: A Linux host runs a multi-tenant logging pipeline (rsyslog in containers, a data-forwarder, and a Redis cache) on a 16-core NUMA machine. During bursts, 95th percentile tail latency for log ingestion climbs to 150–200 ms while CPU, I/O, and network metrics look normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm memory/cache pressure and page reclaim as the tail-latency cause, (2) locate the affected containers and NUMA zones, and (3) apply safe mitigations (tune vm.swappiness, vm.vfs_cache_pressure, min_free_kbytes; pin critical containers to specific NUMA nodes with numactl or cgroups; ensure observability). Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check quotas and throttle via /sys and per-process data. Use mpstat to confirm throttling, and map PIDs to container via /proc/<pid>/cgroup. If quota shows a bottleneck, raise quota (e.g., for v2: sud",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To diagnose, check backlog and FD usage: 1) ss -tanp | awk '{print $1, $2, $5, $7}' 2) ls /proc/$(pgrep nginx|head -1)/fd | wc -l 3) ss -tanp | grep LISTEN to identify offender; lsof -iTCP -nP | grep",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed approach: verify memory pressure via /proc/vmstat, /proc/meminfo, and vmstat; detect reclaim activity (pgfault, pgrefill). Identify containers with high rss via ps, top, and pmap; check NUMA",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: verify memory pressure with free -m, vmstat 1 60, and cat /proc/meminfo. Inspect slab usage via cat /proc/slabinfo and slabtop -a; identify top RSS processes with ps -eo pid,cmd,rss --sort=-rss",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to reason about memory pressure tail latency in multi-tenant containers using only default tools. It ties kernel memory behavior to observability and safe mitigations.\n\n## Key Concepts\n- Memory reclaim and slab pressure\n- NUMA awareness and container pinning\n- Cgroups/resource controls\n- Safe kernel parameter tuning and observability\n\n## Code Example\n```javascript\ngrep -iE 'pgfault|pgmajfault|pgrefill|pgsteal' /proc/vmstat\nfree -h\nvmstat 1 5\ncat /proc/meminfo | head\nnumactl --hardware\nnumastat\nps -eo pid,ppid,cmd,rss --sort rss -h | head\n```\n\n## Follow-up Questions\n- How would you validate that improvements stem from these changes?\n- What risks accompany NUMA pinning and swappiness tweaks?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-12",
        "questionId": "q-25241",
        "question": "Scenario: A Linux host runs a latency-sensitive service inside a cpuset-limited cgroup. During bursts, tail latency spikes while overall CPU usage stays flat. Using only default Linux tools, design a diagnostic workflow to (1) confirm if CFS scheduling or CPU throttling is the bottleneck, (2) identify the offending process and the CPU/core it stalls on, and (3) apply safe mitigations (pin to a dedicated cpuset, adjust cpu.max/cpu.weight, or temporarily disable throttling) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed diagnostic plan: verify memory pressure with vmstat 1; free -m; cat /proc/meminfo; identify hot slabs via slabtop -a and /proc/slabinfo; map to processes with pmap and ls /proc/<pid>/fd. Miti",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "1) Confirm FD exhaustion: check limits/global max and watch bursts: cat /proc/sys/fs/file-max; ulimit -n. 2) Identify offender: for pid in /proc/[0-9]*/fd; do p=$(basename $(dirname $pid)); echo -n \"$",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Start with triage: check per-process limits with ulimit -n and /proc/$PID/limits; monitor fds in use via ls /proc/*/fd | wc -l; watch I/O and CPU wait with iostat -dx 1 2 and vmstat 1; inspect logs vi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "PID=$(pgrep -f 'critical-service'); echo PID=$PID top -b -n1 -H | grep -w $PID pidstat -p $PID 1 mpstat -P ALL 1 if [ -e /sys/fs/cgroup/cpu/cpu.max ]; then echo 'cpu.max:'; cat /sys/fs/cgroup/cpu/cpu.",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to diagnose scheduler-related tail latency and apply confinement with cpusets and cgroups, without adding new tools. It emphasizes observability during targeted mitigations.\n\n## Key Concepts\n- CFS scheduling and CPU throttling via cpuset and cpu.max\n- /proc/sched_debug and /sys/fs/cgroup interfaces\n- pidstat/mpstat for per-process and per-CPU visibility\n- Safe mitigations: pinning, adjusting quotas/weights\n\n## Code Example\n```bash\n#!/usr/bin/env bash\nPID=$(pgrep -f 'critical-service')\necho PID=$PID\ntop -b -n1 -H | grep -w $PID\npidstat -p $PID 1\nmpstat -P ALL 1\nif [ -e /sys/fs/cgroup/cpu/cpu.max ]; then echo 'cpu.max:'; cat /sys/fs/cgroup/cpu/cpu.max; fi\n# Mitigations\ntaskset -cp 0-3 $PID\nif [ -e /sys/fs/cgroup/cpu/cpu.max ]; then echo 'Setting cpu.max to 100000 100000'; echo '100000 100000' > /sys/fs/cgroup/cpu/cpu.max; fi\n```\n\n## Follow-up Questions\n- How would you scale this approach across a cluster with dozens of services?\n- What metrics would you collect long-term to validate stabilization?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-13",
        "questionId": "q-14781",
        "question": "Scenario: A Linux host running GPU-accelerated inference workloads in containers experiences intermittent tail latency spikes during bursts, while average latency and CPU usage appear normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm kernel vs user-space source of the tail latency, (2) identify the exact kernel path or NIC/interrupt vector involved, and (3) apply a safe mitigation (e.g., CPU isolation, container pinning, disabling irqbalance, or XDP offload tuning) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check CPU throttling in the cgroup: cat /sys/fs/cgroup/cpu/mygroup/cpu.stat; if throttled_usec > 0, throttling is active. Then inspect quotas: cat /sys/fs/cgroup/cpu/mygroup/cpu.cfs_quota_us and cpu.c",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use perf to sample scheduling during bursts: perf record -e cycles -aR -- sleep 60; perf report -i perf.data; employ ftrace to monitor sched_switch and wakeups; map culprits to container cgroups via /",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: Confirm that tail latency correlates with CPU frequency scaling and thermal throttling, then isolate affected cores and apply targeted mitigations. **Step 1: Verify CPU Frequency Scaling and Throttling** Check cur…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Step 1: Confirm I/O Scheduler as Bottleneck** ```bash # Monitor I/O latency during traffic bursts iostat -xz 1 vmstat 1 ``` Look for elevated `await` times (target > 50ms) on NVMe devices correlating with traffic spik…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to combine kernel tracing, perf-driven instrumentation, and CPU/container affinity controls to diagnose and mitigate tail-latency on GPU workloads. It emphasizes observability and safe, incremental changes.\n\n## Key Concepts\n- perf scheduling and trace events (cycles, sched_switch)\n- ftrace for kernel scheduling paths\n- container cgroups, cpuset/isolcpus, taskset\n- observability: sar, perf stats, consistent outputs\n\n## Code Example\n```bash\n# Baseline profiling\nperf record -e cycles -aR -- sleep 60\nperf report -i perf.data\n\n# Kernel scheduling trace\necho 1 > /sys/kernel/debug/tracing/events/sched/sched_switch/enable\ncat /sys/kernel/debug/tracing/trace | head -n 200\n\n# CPU isolation / pinning (example)\nsudo isolated-cpus 2-3 taskset -c 2-3 -p <pid>\n```\n\n## Follow-up Questions\n- How would you verify the horizon latency change after reconfiguration?\n- What changes would you make if NIC offload paths are involved?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-14",
        "questionId": "q-10693",
        "question": "Scenario: A Linux host running a Java-based API experiences rare 200–800 ms tail latency during traffic spikes. The latency seems decoupled from CPU, I/O, and contention. Diagnose using only default tools to (1) confirm THP-related page reclaim is contributing, (2) identify the affected process and memory pages, and (3) apply a safe mitigation (e.g., disable Transparent Huge Pages, pin hot threads) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use vmstat 1 300, free -m, and slabtop to confirm pressure. Monitor per-process page faults with pidstat -r -p <pid> and inspect /proc/vmstat for major/minor faults. Map allocations with numastat -p <",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To diagnose CPU frequency scaling throttling as the cause of latency spikes, first establish a baseline by sampling per-core frequencies during normal operation, then compare against values observed during burst traffic…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Diagnose with slabtop -o C, cat /proc/slabinfo | head, free -m, vmstat 1 60. If reclaim spikes align with latency, enable zone_reclaim_mode: echo 1 > /proc/sys/vm/zone_reclaim_mode; raise min_free_kby",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check THP status and memory pressure; use vmstat and meminfo; commands: cat /sys/kernel/mm/transparent_hugepage/enabled; grep Huge /proc/meminfo; vmstat 1 5; identify hot Java thread with ps -eo pid,c",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of Linux memory management pitfalls (THP) and tail latency, plus how to diagnose with default tools and implement safe mitigations without invasive changes.\n\n## Key Concepts\n- Transparent Huge Pages behavior under memory pressure\n- /proc/meminfo and HugePages metrics\n- vmstat and lightweight process pinning for observability\n\n## Code Example\n```bash\n# Diagnostic steps\ncat /sys/kernel/mm/transparent_hugepage/enabled\ngrep Huge /proc/meminfo\nvmstat 1 5\n```\n\n## Follow-up Questions\n- How would you verify NUMA effects or alternative THP tunings?\n- What are risks of disabling THP in a production API server?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-15",
        "questionId": "q-29457",
        "question": "A Linux host running a simple API behind systemd experiences sporadic 503s during burst traffic, with no obvious CPU or IO spikes. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm memory pressure/OOM events are causing the outages, (2) identify the offending process and its cgroup, and (3) apply safe mitigations (adjust vm.swappiness, set MemoryLimit via systemd, or isolate via cgroups) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: map high-CPU PIDs to container cgroups, verify quotas, then raise quotas or pin CPUs. Commands:\\nps -eo pid,pcpu,comm --sort=-pcpu | head -n5\\nfor pid in $(ps -eo pid --no-headers); do echo -n \"",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To diagnose CPU frequency scaling issues causing tail latency spikes in a low-latency REST API, follow this structured workflow using default Linux tools: ```bash # 1. Baseline: Check current governors and frequencies f…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "1) dmesg | tail -n 100 | rg -i oom; 2) free -m; ps -eo pid,pmem,pcpu,cmd --sort=-pmem | head -n 5; for p in $(ps -eo pid --no-headers); do echo PID $p; cat /proc/$p/status | rg -n 'VmSize|VmRSS'; done",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Run free -h; vmstat 1 5; top -b -n1; ps aux --sort=-%mem | head -5; cat /proc/meminfo; check dmesg | grep -i -E 'oom|out of memory'; identify the offender PID and cgroup with cat /proc/<pid>/status an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests memory-pressure diagnosis, OOM event detection, and safe mitigations using only built-in tools. Emphasizes observability when applying cgroup limits or systemd MemoryLimit.\n\n## Key Concepts\n- OOM events, /proc, dmesg\n- Memory pressure, vm.swappiness, cgroups\n- systemd MemoryLimit and observability via journalctl/top\n\n## Code Example\n\n```bash\n# Commands shown in answer\n```\n\n## Follow-up Questions\n- How would you automate this checklist in a one-shot script?\n- How would you validate that memory limit changes don’t regress other services?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-16",
        "questionId": "q-28089",
        "question": "Scenario: A Linux host in a Kubernetes node experiences sporadic 50–200 ms tail latency in a latency‑sensitive API under bursts, while CPU/I/O look normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove kernel memory reclaim/page cache pressure causes the stalls, (2) identify the offending memory cgroup and processes, and (3) apply safe mitigations (tune memory cgroups, vm.min_free_kbytes, vm.vfs_cache_pressure, THP toggling, swap) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline: cat /proc/interrupts; lspci -nnk | grep -i ethernet; reproduce burst and observe spike patterns; locate IRQ lines that rise during load; fix by pinning the offending IRQ to an idle CPU: sudo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "1) Run vmstat 1 60 to detect si/so and swap activity; 2) scan /proc for high VmSwap/VmRSS and map to NUMA via /proc/<pid>/numa_maps to find hot processes and their NUMA nodes; 3) mitigate by pinning h",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Steps: 1) vmstat 1 60 to reveal swap/cache pressure; 2) slabtop -o to monitor kernel caches; 3) for each cgroup: cat /sys/fs/cgroup/memory/*/memory.usage_in_bytes and memory.limit_in_bytes; 4) ps -eo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Start with top to gauge CPU/IO wait. Run vmstat 1 10 and iostat -xz 1 10 to spot disk or device latency. Use ss -tulpn and ss -s to watch sockets and backlog. If backlog grows, raise somaxconn and ngi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical diagnostics when multi-tenant memory pressure causes tail latency spikes, a gap from IO or scheduler focus. It tests using default tooling to validate reclaim pressure, locate the responsible cgroup/process, and apply mitigations without sacrificing observability.\n\n## Key Concepts\n\n- Kernel memory reclaim, page cache pressure, memcg accounting\n- cgroup v1/v2 memory.stat usage and per‑group limits\n- Observability: metrics before/after and safe mitigations\n\n## Code Example\n\n```bash\nvmstat 1 60\nslabtop -o\nfor g in /sys/fs/cgroup/memory/*/memory.usage_in_bytes; do echo $g; cat $g; done\nps -eo pid,cmd,%mem,%cpu --sort=-%mem | head -n 10\nfor pid in $(ps -eo pid --sort=-%mem | head -n 1); do echo \"PID $pid\"; sed -n '1,20p' /proc/$pid/smaps; done\n```\n\n## Follow-up Questions\n\n- How would you validate if toggling THP or adjusting swapiness affected latency?\n- What changes would you consider if memcg accounting is disabled or in migration to cgroup v2?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-17",
        "questionId": "q-16590",
        "question": "On a Linux host serving a latency-sensitive API on a Kubernetes node with cgroup v2, tail latency spikes occur during bursts while averages stay normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm tail latency is caused by CPU bandwidth throttling in a cgroup, (2) locate the exact cgroup path and container, and (3) apply a safe mitigation (increase cpu.max or relocate workload to less-restricted CPUs) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: 1) verify contention with vmstat 1, iostat -x 1, and numastat -c; 2) locate hotspots with pidstat -m -p ALL and perf top -p <pid>, then inspect /proc/<pid>/smaps for RSS/Swap; 3) mitigate by bin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check memory pressure and swap thrash: free -h; vmstat 1 5; cat /proc/meminfo | grep MemAvailable; cat /proc/swaps; top -b -n1 -o %MEM | head -n 6; ps aux --sort=-rss | head -n 5; if a culprit dominat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "1) Gather per-socket state and retransmission hints: ss -ti; ss -tp; cat /proc/net/netstat; 2) Capture RTT behavior and retries via tcpdump for bursts and inspect retransmissions locally; 3) Apply saf",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed approach: 1) systemd-cgls | grep kubepods to locate the cgroup path. 2) cat /sys/fs/cgroup/<path>/cpu.max and cat /sys/fs/cgroup/<path>/cpu.stat to inspect quotas and throttling. 3) If thrott",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to diagnose CPU bandwidth throttling in cgroup v2 using only default tools, mapping latency to a specific cgroup and implementing safe mitigations that preserve observability.\n\n## Key Concepts\n- cgroup v2 cpu.max and cpu.stat\n- systemd-cgls for hierarchy\n- throttling vs usage\n- safe mitigations: adjust quota vs isolate CPUs\n\n## Code Example\n```bash\n# locate cgroup\nsystemd-cgls | grep kubepods\n# inspect quotas\ncat /sys/fs/cgroup/<path>/cpu.max\ncat /sys/fs/cgroup/<path>/cpu.stat\n```\n\n## Follow-up Questions\n- How would you automate detection across nodes and report throttled events?\n- What risks exist when increasing quotas on a busy node?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-18",
        "questionId": "q-22019",
        "question": "Scenario: A Linux host with a quad-socket NUMA machine runs a memory-heavy microservice stack. Tail latency spikes appear during bursts while standard metrics stay flat. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove the tail latency is caused by memory reclaim or swap pressure, (2) locate the offending processes and their NUMA nodes, and (3) apply safe mitigations (tune vm.swappiness, reduce overcommit, pin critical services, isolate NUMA nodes) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run: vmstat 1 5; mpstat -P ALL 1; slabtop -s a; pidstat -d -p ALL 1; grep -i reclaim /var/log/dmesg; cat /proc/meminfo; numactl --hardware. If reclaim dominates, set vm.swappiness=10; pin hot process:",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To diagnose memory reclaim tail latency: verify swap pressure with vmstat 1 20 and free -m; check /proc/swaps and meminfo for SwapTotal. Identify heavy RSS processes and their CPU-NUMA bindings: ps -e",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Run vmstat 1 60; free -m; cat /proc/meminfo; top -b -n1; ps -eo pid,cmd,%mem --sort=-%mem | head -20; cat /proc/self/cgroup; locate the container’s cgroup and read memory.current; then implement mitig",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Detect allocator contention by correlating burst allocations with THP activity using vmstat/slabtop, identify hot threads via pidstat -p, map to NUMA with numactl --physcpubind --membind; Disable THP;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of memory reclaim pressure and NUMA-aware debugging, a gap from I/O and scheduler questions.\n\n## Key Concepts\n- Memory reclaim, swap pressure, overcommit\n- NUMA awareness, cpuset, isolcpus\n- Observability using default tools: vmstat, free, ps, numactl\n\n## Code Example\n```bash\n# 1) detect swap pressure\nvmstat 1 20\nfree -m\ncat /proc/swaps\n# 2) locate heavy allocators\nps -eo pid,psr,rss,cmd --sort=-rss | head\n# 3) pin critical service\nsudo systemctl set-property myservice.slice CPUS=\"0-3\"\n```\n\n## Follow-up Questions\n- How would you validate long-term observability after mitigation?\n- How do you handle memory reclaim on NUMA with many memory hotplugs?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-19",
        "questionId": "q-9267",
        "question": "On a Linux host running a Java microservice cluster in Docker, tail latency spikes occur during bursts while CPU usage appears normal. Using only default Linux tools, design a diagnostic workflow to (1) confirm CPU throttling caused by cgroup CPU bandwidth limits, (2) identify the throttled container and the CPUs involved, (3) apply a safe mitigation (raise cpu.max, remove the limit, or pin the container) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Inspect each cgroup's throttling: cat /sys/fs/cgroup/cpu/MyService/cpu.stat and cat /sys/fs/cgroup/cpu/MyService/cpu.cfs_quota_us; compare with cpu.cfs_period_us to derive throttled_percentage. Identi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check RX drops/backlog and map to queues/CPUs, then enable per-queue routing and larger RX queues. Commands: ip -s link show eth0; cat /sys/class/net/eth0/statistics/rx_dropped; cat /proc/net/softnet_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Diagnose via cgroups CPU throttling. Commands: docker ps; for each id: cat /sys/fs/cgroup/cpu/docker/<id>/cpu.stat; cat /sys/fs/cgroup/cpu/docker/<id>/cpu.max; pidstat -p ALL 1; docker stats --no-stre",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Baseline check hot threads with: ps -eo pid,tid,pcpu,psr,comm | grep myapi; measure scheduler impact with: perf stat -p <tid> -e cycles,instructions,cache-references,cache-misses 1s; inspect /proc/sch",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of Linux CPU bandwidth controls, how throttling shows up in cpu.stat, and how to map that to a container and CPU set with minimal disruption.\n\n## Key Concepts\n- cgroups v1/v2 CPU throttling and cpu.max\n- Throttled_time, cpu.stat, and cpu.max outputs\n- Mapping workloads to CPUs with cpuset/taskset\n- Maintaining observability during mitigations\n\n## Code Example\n```bash\n#!/bin/bash\nfor id in $(docker ps -q); do\n  echo \"Container $id\"\n  echo -n \"throttled_time: \"; cat /sys/fs/cgroup/cpu/docker/$id/cpu.stat | grep throttled_time\n  echo -n \"cpu.max: \"; cat /sys/fs/cgroup/cpu/docker/$id/cpu.max\ndone\n```\n\n## Follow-up Questions\n- How would you adapt if using a unified cgroup v2 hierarchy without per-container dirs?\n- How would you verify mitigations don’t degrade other workloads?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-20",
        "questionId": "q-6880",
        "question": "Scenario: A Linux host runs a Node.js API behind Nginx. During bursts, tail latency spikes while CPU, memory, and disk I/O look normal. Using only default tooling, design a beginner-friendly diagnostic workflow to (1) determine if stalls are due to Node.js event loop, Nginx worker, or kernel scheduling, (2) identify the exact component responsible, and (3) apply a safe mitigation (e.g., adjust Nginx worker_processes, backlog, or CPU affinity; or tune Node.js clustering) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To diagnose CPU frequency scaling issues causing tail latency spikes in a low-latency REST API, follow this structured workflow using default Linux tools: ```bash # 1. Baseline: Check current governors and frequencies f…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run perf sched and perf top during traffic bursts to confirm scheduler activity patterns; examine /proc/net/softnet_stat and /proc/net/dev statistics to identify queue bottlenecks; use ss and netstat to detect socket ba…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Establish performance baselines during normal and burst periods: `top -b -n1`, `pidstat -p ALL 1`, `iostat -dx 1`, `vmstat 1`. Correlate latency spikes with these metrics. Use `strace -tt -p <pid>` to identify blocking…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Step 1: check memory pressure: free -m; grep -i swap /proc/meminfo; vmstat 1 5. Step 2: list top memory users: ps -eo pid,cmd,%mem,rss --sort=-%mem | head -n 5. For a pid X, inspect: pmap -x X | head.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical Linux troubleshooting skills using default system tools for diagnosing tail latency in Node.js/Nginx deployments.\n\n## Key Concepts\n\n- Performance baseline measurement using top/pidstat/iostat/vmstat\n- Distinguishing user-space vs kernel bottlenecks through strace analysis\n- Safe configuration tuning: Nginx worker optimization, Linux socket parameters, Node.js clustering\n\n## Code Example\n\n```bash\n# Establish baseline metrics\ntop -b -n1\npidstat -p ALL 1\niostat -dx 1\nvmstat 1\n```\n\n## Follow-up Questions\n\n- How would this approach adapt to containerized environments?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-21",
        "questionId": "q-13715",
        "question": "Scenario: A Linux host running a low-latency REST API shows tail latency spikes (100–300 ms) during bursts, while CPU and IO metrics look normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm whether CPU frequency scaling is causing the stalls, (2) identify the core(s) and time windows responsible, and (3) apply a safe mitigation (pin hot threads to fixed CPUs and switch the relevant cores to performance governor) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To diagnose CPU frequency scaling issues causing tail latency spikes in a low-latency REST API, follow this structured workflow using default Linux tools: ```bash # 1. Baseline: Check current governors and frequencies f…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use `iostat -xz 1 3` to monitor I/O wait metrics, `pidstat -p ALL 1` to identify CPU contention patterns, and `slabtop -s object` to observe memory allocation churn. Inspect THP configuration with `cat /sys/kernel/mm/tr…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Diagnostic and Mitigation Workflow for cgroup Memory Pressure ### Step 0: Detect cgroup Version ```bash # Check cgroup version mount | grep cgroup # If /sys/fs/cgroup/memory exists → cgroup v1 # If /sys/fs/cgroup/cgr…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check THP status and huge pages usage. Commands: - cat /sys/kernel/mm/transparent_hugepage/enabled - grep -iHuge /proc/meminfo - for p in /proc/[0-9]*/smaps 2>/dev/null; do grep -Hm1 'AnonHugePages' $",
            "isCorrect": false
          }
        ],
        "explanation": "This question evaluates practical Linux performance debugging skills by testing the ability to:\n\n1. **Diagnose CPU scaling effects**: Identify frequency throttling as the root cause of latency spikes despite normal CPU/IO metrics\n2. **Use built-in tools effectively**: Leverage standard Linux utilities (top, taskset, sysfs) without additional software\n3. **Implement targeted mitigations**: Apply precise fixes (thread pinning + governor changes) that address the specific issue\n4. **Maintain observability**: Preserve monitoring capabilities while resolving the performance problem\n\nKey technical concepts demonstrated:\n- CPU frequency scaling mechanics (governors, scaling_cur_freq)\n- Thread affinity management with taskset\n- Real-time system monitoring using watch and sysfs\n- Performance measurement with curl timing options\n\nThe solution shows systematic progression from diagnosis through verification, ensuring safe and measurable improvements to latency-sensitive applications.",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-22",
        "questionId": "q-1266",
        "question": "Context: Linux node in a Kubernetes cluster hosting a high-throughput data ingestion service. Intermittent tail latency spikes (>1s) appear during peak traffic, affecting processing. Without downtime, design a concrete troubleshooting workflow to (1) confirm whether CPU, I/O, or network is the bottleneck, (2) identify the exact subsystem or process responsible, and (3) implement a safe mitigation with minimal impact while maintaining observability. Include exact commands and realistic outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Step 1: Confirm CPU Throttling Inspect throttling statistics for the target cgroup: ```bash cat /sys/fs/cgroup/cpu/myapp.slice/cpu.stat ``` Non-zero values in `nr_throttled` (number of throttled periods) and `throttl…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check THP status and defrag state; inspect /proc/meminfo for AnonHugePages and HugePages; identify memory-heavy processes with ps -eo pid,cmd,%mem --sort=-%mem | head; map each pid to NUMA nodes via /",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin with per-second metrics: iostat -xz 1 2; mpstat -P ALL 1; vmstat 1. Identify hot processes with pidstat -p ALL 1 | head. If IO lag appears, run perf stat -p <pid> -e cycles,instructions,cache-re",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Start with iostat -x 1 to gauge I/O wait, vmstat 1 for page/cache pressure, and sar -B for blocked processes. Use iotop -aoW to identify heavy I/O consumers. Inspect /proc/diskstats and, if needed, /p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical, end-to-end debugging in real-world Linux systems, including multi-subsystem correlation and safe mitigations with minimal downtime.\n\n## Key Concepts\n- Tail latency diagnosis across CPU, IO, and network\n- Per-second monitoring with iostat/mpstat/vmstat\n- Process-level tracing with pidstat and perf\n- I/O schedulers and CPU pinning as mitigations\n\n## Code Example\n```javascript\n// Example: parse perf output snapshot (illustrative)\nfunction parsePerf(line){ /* simplistic parser */ }\n```\n\n## Follow-up Questions\n- How would you adapt this workflow for containerized workloads (Kubernetes, CNI, cgroups)?\n- How would you validate the mitigation under synthetic peak load?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-23",
        "questionId": "q-19708",
        "question": "A Kubernetes node runs dozens of pods. During load spikes, a single pod's HTTP API tail latency spikes 100–300 ms while averages stay low and CPU/I/O look normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove latency stems from in-guest memory pressure and swap thrashing within that pod's cgroup, (2) pinpoint the processes/pages swapped and map them to that pod, (3) apply safe mitigations (cap memory with cgroup, tighten vm.swappiness, or throttle swap usage) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use: mpstat -P ALL 1; pidstat -p $(pidof redis-server) 1; iostat -xd 1; vmstat 1; numastat 1; top -b -d 1 -n 1 -H. If pressure is confirmed, pin Redis to CPUs 2-5 on node0 with a cpuset, set governor",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Execute this diagnostic workflow using default system tools: 1. **Identify bottleneck type**: Run `iostat -xz 1 2` to check device await times, `vmstat 1` to monitor I/O wait (wa) and memory pressure 2. **Map to process…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check swap pressure via /proc/meminfo SwapTotal/SwapFree and vmstat si/sy; map the pod's cgroup to its PIDs and inspect /proc/<pid>/smaps Swap lines to identify swapped pages; apply caps with memory.l",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "To diagnose use: cat /proc/interrupts; cat /proc/timer_list; while bursts occur, run pidstat -p ALL 1 and vmstat 1. Correlate spikes to timer/softirq counters. Identify culprit via /proc/<tid>/sched o",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes ability to diagnose memory pressure and swap thrashing in containerized Linux workloads using only default tools, then to translate findings into safe mitigations without losing observability.\n\n## Key Concepts\n- cgroups memory controller, memory.limit_in_bytes/memory.max\n- /proc/meminfo, vmstat si/sy, /proc/<pid>/smaps Swap fields\n- vm.swappiness control and swap behavior\n\n## Code Example\n```bash\n# locate pod cgroup\ngrep -E 'memory' /proc/self/cgroup\n# show swap stats\ngrep -i 'Swap' /proc/meminfo\n# per-pid swapped pages\nfor pid in $(cat /sys/fs/cgroup/memory/<pod>/cgroup.procs); do echo 'PID='$pid; awk '/Swap:/{print $2}' /proc/$pid/smaps; done\n```\n\n## Follow-up Questions\n- How would you implement a non-disruptive mitigation for a live cluster?\n- How would you verify that latency returns to baseline after applying caps?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-24",
        "questionId": "q-16680",
        "question": "A Linux host with a quad-socket NUMA machine running a microservices stack shows intermittent 100–500 ms tail latency bursts during load, while average latency remains healthy. Design a concrete diagnostic workflow (using only default Linux tools) to (1) determine if CPU power management states (C-states and P-states) or Turbo boost contribute to the tail latency, (2) identify the core(s) and thread(s) affected during bursts, and (3) apply safe mitigations (pin critical workloads, set CPU governor to performance, and optionally disable deep C-states) while keeping observability intact. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "During bursts, collect: iostat -x 1, pidstat -p ALL 1, and inspect /sys/devices/system/cpu/cpu*/cpuidle/state*/residency to see deep C-states; check /sys/devices/system/cpu/intel_pstate/no_turbo for t",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Check THP status and memory pressure; use vmstat and meminfo; commands: cat /sys/kernel/mm/transparent_hugepage/enabled; grep Huge /proc/meminfo; vmstat 1 5; identify hot Java thread with ps -eo pid,c",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run: vmstat 1 5; top -b -n1 | head -n 5; ss -tulpen | grep :80; ps -eo pid,ppid,comm,%cpu --sort=-%cpu | head. If wa>0 in vmstat, IO wait; if r high, CPU scheduling. Top reveals culprit. Mitigate by e",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Verify THP state and defrag; monitor THP counters in /proc/vmstat and THP messages in dmesg; map allocations to PIDs with for pid in /proc/[0-9]*; do [ -r \"$pid/cgroup\" ] && echo $pid; done and cross-",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nTests practical debugging of latency that isn’t caused by obvious CPU or I/O saturation, but by how the CPU scales power and idles. It checks ability to reason about non-contiguous microarchitectural effects and to implement safe, observable mitigations.\\n\\n## Key Concepts\\n- CPU power management (C-states, P-states, turbo)\\n- CPU frequency governors and turbo controls\\n- Thread/process pinning and isolating CPUs\\n- Observability during mitigations (latency, throughput, cpu utilization)\\n\\n## Code Example\\n```bash\n# Baseline latency & utilization\niostat -x 1\npidstat -p ALL 1\n# Inspect C-state residency\ngrep -R \"state[0-9]\" /sys/devices/system/cpu/cpu*/cpuidle\n# Turbo flag (intel CPUs)\ncat /sys/devices/system/cpu/intel_pstate/no_turbo\n# Pinning a critical PID (example)\ntaskset -p 0x00000001 <PID>\n# Set performance governor\nfor p in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do echo performance > \"$p\"; done\n```\n\n## Follow-up Questions\\n- How would you measure the impact of disabling deep C-states on other workloads?\\n- If the node runs an AMD CPU, what equivalents to intel_pstate/no_turbo would you check?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-29T08:40:45.654Z",
    "version": 1
  }
]