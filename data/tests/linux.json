[
  {
    "id": "test-linux",
    "channelId": "linux",
    "channelName": "Linux",
    "title": "Linux Knowledge Test",
    "description": "Test your understanding of Linux with 25 questions.",
    "questions": [
      {
        "id": "linux-q-0",
        "questionId": "q-9267",
        "question": "On a Linux host running a Java microservice cluster in Docker, tail latency spikes occur during bursts while CPU usage appears normal. Using only default Linux tools, design a diagnostic workflow to (1) confirm CPU throttling caused by cgroup CPU bandwidth limits, (2) identify the throttled container and the CPUs involved, (3) apply a safe mitigation (raise cpu.max, remove the limit, or pin the container) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Step 1: Confirm Network Path Stalls** Use `ping` to measure latency to the local gateway and a remote host: ```bash ping -c 100 -i 0.2 192.168.1.1 ``` Sample output demonstrating latency spikes: ``` 64 bytes from 192.…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Diagnose via cgroups CPU throttling. Commands: docker ps; for each id: cat /sys/fs/cgroup/cpu/docker/<id>/cpu.stat; cat /sys/fs/cgroup/cpu/docker/<id>/cpu.max; pidstat -p ALL 1; docker stats --no-stre",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "## Diagnostic Workflow ### Step 1: Confirm SYN Backlog Saturation ```bash # Check current somaxconn value cat /proc/sys/net/core/somaxconn # View SYN queue depth (non-established connections) ss -tanp state syn-recv | w…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Workflow: verify spike with top/iostat, inspect THP and page cache, locate offending container, and apply a safe mitigation. Commands: cat /sys/kernel/mm/transparent_hugepage/enabled; vmstat 1 5; iost",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of Linux CPU bandwidth controls, how throttling shows up in cpu.stat, and how to map that to a container and CPU set with minimal disruption.\n\n## Key Concepts\n- cgroups v1/v2 CPU throttling and cpu.max\n- Throttled_time, cpu.stat, and cpu.max outputs\n- Mapping workloads to CPUs with cpuset/taskset\n- Maintaining observability during mitigations\n\n## Code Example\n```bash\n#!/bin/bash\nfor id in $(docker ps -q); do\n  echo \"Container $id\"\n  echo -n \"throttled_time: \"; cat /sys/fs/cgroup/cpu/docker/$id/cpu.stat | grep throttled_time\n  echo -n \"cpu.max: \"; cat /sys/fs/cgroup/cpu/docker/$id/cpu.max\ndone\n```\n\n## Follow-up Questions\n- How would you adapt if using a unified cgroup v2 hierarchy without per-container dirs?\n- How would you verify mitigations don’t degrade other workloads?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-1",
        "questionId": "q-20849",
        "question": "In a Linux host running a local REST service in a Docker container, nightly backups cause intermittent 1–2s startup latency for new connections while overall metrics look fine. Using only default Linux tools, design a practical workflow to (1) determine whether CPU scheduling, memory pressure, or network stack is the culprit, (2) identify the exact process or container responsible for the delay, and (3) apply safe mitigations (adjust container memory limits, stagger backups, or tweak NIC settings) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline with: vmstat 1 10; iostat -xz 1 10; ps -eo pid,comm,%cpu,%mem --sort=-%cpu | head -n 6; identify container: docker ps; docker stats --no-stream; watch for entry times that align with backup.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "To prove memory pressure causes tail latency, monitor with: free -m; vmstat 1 60; cat /proc/meminfo. Identify culprit: map PID to container via /proc/[pid]/cgroup; find large RSS with: ps -eo pid,cmd,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use `lsof -i :8080` or `netstat -tulpn | grep 8080` to find the PID, then `kill -9 <PID>` to terminate it. For a safer approach, try `kill -15 <PID>` first to allow graceful shutdown.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check limits and port usage: run ulmit -n and cat /proc/sys/fs/file-max. If exhausted, expect EAGAINs. Map active connections with ss -tanp | grep -E 'ESTAB|TIME-WAIT'. Inspect ephemeral range with ca",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to isolate latency causes under simple resource contention using default tools. It evaluates basic instrumentation, container mapping, and safe mitigations that preserve observability.\n\n## Key Concepts\n- Resource contention between host backups and services\n- Basic Linux monitoring (vmstat, iostat, ps, docker ps)\n- Mapping host processes to containers and back\n- Safe mitigations: memory limits, cron jitter, NIC tuning\n\n## Code Example\n```javascript\n# baseline commands\nvmstat 1 10\niostat -xz 1 10\nps -eo pid,comm,%cpu,%mem --sort=-%cpu | head -n 6\ndocker ps\n```\n\n## Follow-up Questions\n- How would you verify observability after changes?\n- If iostat shows high iowait but ps shows low CPU, what next?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-2",
        "questionId": "q-13947",
        "question": "Scenario: A binary API service runs in a systemd slice on a Linux host sharing CPUs with other services. During bursts, 150–300 ms tail latency appears though overall CPU usage looks normal. Using only default Linux tools, design a diagnostic workflow to (1) confirm CPU quota/throttling in the slice is the cause, (2) identify the throttled process and CPU, (3) apply a safe mitigation (increase quota or pin to dedicated CPUs) and verify observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Start by correlating tail latency with ksmd activity. Run: ps -C ksmd -o pid,pcpu,cmd; watch -n1 'cat /sys/kernel/mm/ksm/pages_to_scan /sys/kernel/mm/ksm/pages_sharing'; inspect per-pid memory with /p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run perf stat -e LLC-load-misses,L1-dcache-load-misses,L2-load-misses,TLB-load-misses -p <pid> -t for target processes; inspect /proc/<pid>/numa_maps, /proc/meminfo; verify policies with numactl --har",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check the slice quotas to confirm throttling: cat /sys/fs/cgroup/cpu/<slice>/cpu.cfs_quota_us; cat /sys/fs/cgroup/cpu/<slice>/cpu.cfs_period_us. Identify the throttled pid with pidstat -p ALL 1. If qu",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Monitor latency windows and IRQ pressure to confirm interrupts as root cause; correlate spikes with /proc/interrupts using: watch -n0.5 'cat /proc/interrupts'; identify device by mapping IRQs to net d",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of real-world CPU resource management, cgroup behavior, and observability under bursts. It requires using default tools to diagnose throttling, map it to a specific process, and apply a conservative fix without destabilizing other workloads.\n\n## Key Concepts\n- Cgroup CPU quotas and throttling\n- Process-level visibility under bursts\n- Safe isolation and re-observation\n\n## Code Example\n```bash\n# 1) Identify quotas\ncat /sys/fs/cgroup/cpu/<slice>/cpu.cfs_quota_us\ncat /sys/fs/cgroup/cpu/<slice>/cpu.cfs_period_us\n# 2) Find contending processes\npidstat -p ALL 1\n# 3) Mitigate\necho 200000 > /sys/fs/cgroup/cpu/<slice>/cpu.cfs_quota_us\ntaskset -pc 0-3 <pid>\n```\n\n## Follow-up Questions\n- How would you handle v2 unified cgroups differently?\n- What metrics would you monitor to ensure no regressions after mitigation?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-3",
        "questionId": "q-17898",
        "question": "Scenario: A Linux workstation used for data science becomes unresponsive for 3–6 seconds during heavy notebook builds. CPU/memory look fine, but iostat -xz 1 3 shows sustained IO wait on /dev/nvme0n1. Using only default tools, design a diagnostic workflow to (1) confirm IO wait is the bottleneck, (2) identify top IO‑consuming processes and files, and (3) propose a safe mitigation (e.g., stagger IO, tune IO scheduler or VM dirty ratios) while preserving observability. Include commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check THP status and defrag state; inspect /proc/meminfo for AnonHugePages and HugePages; identify memory-heavy processes with ps -eo pid,cmd,%mem --sort=-%mem | head; map each pid to NUMA nodes via /",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Diagnostic Workflow ### Step 1: Confirm IO Wait is the Bottleneck Run iostat to observe disk metrics across the workload: ```bash iostat -xz 1 3 ``` **Sample Output:** ``` Device r/s w/s rkB/s wkB/s rrqm/s wrqm/s %ut…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To diagnose scheduler throttling via cgroups and identify the root cause of tail latency, follow this systematic workflow: ## 1. Confirm CPU Quota Throttling ```bash # Check CPU quotas for the critical service cat /sys/…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check TIME_WAIT and local port usage with ss -s and ss -tulpen; inspect port range with cat /proc/sys/net/ipv4/ip_local_port_range. If needed widen with sudo sysctl -w net.ipv4.ip_local_port_range='10",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the practical ability to diagnose IO bottlenecks using native Linux tools, map IO to specific processes and files, and implement safe mitigations without sacrificing observability. It demonstrates understanding of per-process IO accounting, device-level metrics, and basic sysctl tuning.\n\n## Key Concepts\n\n- **IO wait** and disk metrics interpretation via `iostat`\n- **Per-process IO accounting** using `pidstat` and `/proc/<pid>/fd`\n- **Mapping IO to specific files** using `lsof` for targeted remediation\n- **Safe mitigations**: IO prioritization with `ionice`, scheduler tuning, and kernel dirty page ratio adjustments\n- **Observability trade-offs**: Balancing performance fixes with maintaining visibility into system behavior",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-4",
        "questionId": "q-13715",
        "question": "Scenario: A Linux host running a low-latency REST API shows tail latency spikes (100–300 ms) during bursts, while CPU and IO metrics look normal. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm whether CPU frequency scaling is causing the stalls, (2) identify the core(s) and time windows responsible, and (3) apply a safe mitigation (pin hot threads to fixed CPUs and switch the relevant cores to performance governor) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To diagnose CPU frequency scaling issues causing tail latency spikes in a low-latency REST API, follow this structured workflow using default Linux tools: ```bash # 1. Baseline: Check current governors and frequencies f…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Run vmstat 1 5, free -m, and iostat -x 1; use ps aux --sort=-%mem to ID heavy mem users; check swap usage; implement: sysctl vm.swappiness=10; sysctl vm.dirty_ratio=20; if needed, create a memory limi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Audit IRQ flow and NIC queue mapping to diagnose tail latency. Key steps: 1) show IRQs: cat /proc/interrupts | grep -i eth0; 2) read per-IRQ affinity: for irq in $(grep -i eth0 /proc/interrupts | awk",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Diagnosis steps: 1) ss -s to check socket counts; 2) ss -a | awk '/TIME-WAIT/ {print}' to quantify TIME_WAIT; 3) check ephemeral range: cat /proc/sys/net/ipv4/ip_local_port_range; 4) view nf_conntrack",
            "isCorrect": false
          }
        ],
        "explanation": "This question evaluates practical Linux performance debugging skills by testing the ability to:\n\n1. **Diagnose CPU scaling effects**: Identify frequency throttling as the root cause of latency spikes despite normal CPU/IO metrics\n2. **Use built-in tools effectively**: Leverage standard Linux utilities (top, taskset, sysfs) without additional software\n3. **Implement targeted mitigations**: Apply precise fixes (thread pinning + governor changes) that address the specific issue\n4. **Maintain observability**: Preserve monitoring capabilities while resolving the performance problem\n\nKey technical concepts demonstrated:\n- CPU frequency scaling mechanics (governors, scaling_cur_freq)\n- Thread affinity management with taskset\n- Real-time system monitoring using watch and sysfs\n- Performance measurement with curl timing options\n\nThe solution shows systematic progression from diagnosis through verification, ensuring safe and measurable improvements to latency-sensitive applications.",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-5",
        "questionId": "q-17603",
        "question": "Scenario: A Linux host serving a high-throughput streaming pipeline experiences tail latency spikes during bursts while CPU, memory, and network look healthy. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove blocked I/O or scheduler saturation is causing stalls, (2) locate the exact block device and I/O queue responsible and the process(es) issuing the I/O, and (3) apply a safe mitigation (e.g., switch to mq-deadline or bfq, adjust queue_depth/read-ahead, or enable/disable a specific offload) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use iostat -dx 1 2 and vmstat 1 2 to observe IO stalls; inspect schedulers with cat /sys/block/sd*/queue/scheduler; map I/O to PIDs with pidstat -d 1; switch to mq-deadline: echo mq-deadline > /sys/bl",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Run iostat -dx 1 5 to confirm IO-tail latency on the NVMe device during bursts. Inspect /sys/block/nvme0n1/queue/scheduler to see the active scheduling policy. If tails align with high await, switch t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check THP: cat /sys/kernel/mm/transparent_hugepage/enabled; memory pressure: free -m; vmstat 1 5; Identify heavy consumer: ps -eo pid,cmd,%mem,%cpu --sort=-%mem | head; inspect mappings: head -n 20 /p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To diagnose file descriptor exhaustion as the bottleneck: (1) Verify current limits and usage with `ulimit -n`, `cat /proc/sys/fs/file-max`, `cat /proc/sys/fs/file-nr`, and `ss -s`. If the allocated file descriptors in…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical debugging of block I/O and scheduler behavior under bursty load, requiring hands-on use of standard Linux tools and safe mitigations.\n\n## Key Concepts\n- Block I/O schedulers (mq-deadline, bfq, cfq)\n- Per-disk metrics (iostat, vmstat)\n- Scheduler visibility via /sys/block/.../queue/scheduler\n- Process-level I/O mapping (pidstat, similar)\n\n## Code Example\n```bash\niostat -dx 1 2\nvmstat 1 2\ncat /sys/block/sda/queue/scheduler\npidstat -d 1\n```\n```",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-6",
        "questionId": "q-4967",
        "question": "On a Linux host delivering real-time API responses, tail latency spikes during bursts even though overall CPU and network usage look reasonable. Using only default tooling, design a concrete diagnostic workflow to (1) determine if stalls are caused by IRQ/NAPI imbalance or driver contention, (2) identify the exact NIC IRQ lines and CPU cores involved, and (3) implement a safe mitigation (e.g., pin IRQs to specific CPUs, toggle NIC offloads, or adjust irqbalance) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by examining /proc/interrupts to identify active NIC IRQ lines and their distribution across CPU cores, then verify SMP affinity settings and temporarily bind high-frequency IRQs to dedicated CPUs while monitoring…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Check host memory pressure: free -m; vmstat 1 5; grep Swap /proc/meminfo. If swap used or MemAvailable is low, locate VMs with high RSS: ps -eo pid,comm,rss,cmd | grep -E 'qemu|kvm'. For each VM, run",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Diagnostic Workflow for Containerized HTTP API Latency Spikes ### Step 1: Confirm Memory Pressure as Root Cause Run these default Linux tools to validate whether memory pressure (page cache or slab allocator) is caus…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Correlation of latency spikes with CPU frequency and idle-state transitions. Commands: cat /proc/stat; for c in /sys/devices/system/cpu/cpu*/cpufreq/scaling_cur_freq; do echo $c': '$(cat $c); done; ec",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTail latency in production networks frequently originates from interrupt contention rather than application code paths. This question evaluates practical debugging skills using only system-default tools.\n\n## Key Concepts\nIRQ/NAPI mechanisms, CPU affinity, NIC offloads, irqbalance service, per-CPU latency measurement\n\n## Code Example\n```bash\n# Identify active NIC IRQs\ngrep -E 'eth|eno' /proc/interrupts\n# Verify current affinity mask\ncat /proc/irq/45/smp_affinity_list\n# Bind IRQ 45 to CPUs 0-1\necho 0-1 | sudo tee /proc/irq/45/smp_affinity_list\n# Disable RX offload for testing\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-7",
        "questionId": "q-7231",
        "question": "Scenario: A Linux host runs multiple containerized microservices on a dense NUMA machine. At peak IO, 99th percentile latency spikes for a database-like service. Using only default tools, design a concrete diagnostic workflow to (1) confirm whether memory pressure or page cache eviction contributes to latency tails, (2) identify the NUMA node(s) and container/process(es) contributing to the pressure, and (3) apply safe mitigations (e.g., cap containers with cgroups, adjust swapiness, enforce per-NUMA memory binding) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run mpstat -P ALL 1 5 to confirm per-core throttling; inspect cpu.stat for the group: cat /sys/fs/cgroup/cpu/myapp/cpu.stat (check nr_throttled, throttled_time). Map PIDs to cgroups via cat /proc/<pid",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run: iostat -dx 1 5; pidstat -d 1 5; perf stat -e block_rq_issue,block_rq_complete -a; lsblk -d -o NAME,MODEL,SIZE; cat /sys/block/nvme*/queue_depth (if present). Then if await spikes align with a sin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by mapping containers to their cgroups using `systemd-cgls` to establish the container-to-cgroup relationship. Then systematically inspect per-cgroup CPU throttling statistics: `for cg in /sys/fs/cgroup/*cpu*/; do…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Begin by collecting memory and IO metrics with vmstat, free/meminfo, slabtop, and iostat; map NUMA layout with numactl --hardware; identify pressure sources per container via /proc/*/smaps or docker s",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests deep knowledge of Linux memory management, NUMA behavior, and containerization under load. The responder must reason about memory reclaim, page cache vs swap, kernel slab allocations, and per-container memory pressure, plus practical mitigations that preserve observability.\n\n## Key Concepts\n- Memory pressure signals in /proc/meminfo, vmstat, slabtop\n- NUMA node binding and cpuset/numactl usage\n- Per-container memory limits and swapping behavior\n- Observability with iostat/vmstat/sar and container metrics\n\n## Code Example\n```javascript\n#!/usr/bin/env bash\n# collect metrics for 60s\nvmstat 1 60 > /tmp/vmstat.log\niostat -dx 1 60 > /tmp/iostat.log\n```\n\n## Follow-up Questions\n- How would you test the effect of increasing MemAvailable vs lowering swapiness?\n- What pitfalls exist when binding memory to a NUMA node across many containers?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-8",
        "questionId": "q-28586",
        "question": "Scenario: A Linux host running a build agent writes thousands of tiny temp files under /tmp on a tmpfs mount. Under bursts, builds fail with ENOSPC for inodes even though disk space looks fine. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm inode exhaustion vs disk space, (2) identify which directories or processes are consuming most inodes, and (3) apply safe mitigations (cleanup policy, remount tmpfs with more inodes, or move /tmp to disk) while preserving observability. Provide exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run perf top to spot hot threads with cache misses; for suspects run: perf stat -e cache-references,cache-misses,cycles -p <pid> sleep 2. Check /proc/<pid>/numa_maps to map NUMA node. Pin critical ser",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify per-container throttling by mapping each container to its cgroup, then read cpu quota and throttling counters. Commands: - for each container: cat /proc/self/cgroup; cat /sys/fs/cgroup/cpu/<p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run `df -h /tmp` and `df -i /tmp` to differentiate between disk space exhaustion and inode exhaustion. If inode usage approaches 100% while disk space remains available, identify the culprits with: ```bash sudo find /tm…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Diagnose memory pressure and swap thrash as the tail-latency root cause on a containerized Linux host. Steps: 1) free -m; vmstat 1 60; 2) ps -eo pid,comm,rss --sort=-rss | head; 3) map to cgroups via",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical Linux troubleshooting skills, focusing on inode exhaustion—a common but often overlooked pitfall with tmpfs and build workloads.\n\n## Key Concepts\n- **inode vs. space**: Using `df -h` vs. `df -i` to diagnose the actual constraint\n- **Identifying culprits**: Leveraging `find` and `awk` to pinpoint high-inode directories by process or service\n- **Safe mitigations**: Cleanup policies, tmpfs remounting with adjusted `nr_inodes`, or relocating `/tmp` to disk-backed storage\n- **Preserving observability**: Using `journalctl` and `dmesg` to maintain system visibility",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-9",
        "questionId": "q-5748",
        "question": "Scenario: A Linux host runs a MongoDB primary. During bursts, new data file creation occasionally fails with ENOSPC even though df shows free space. Using only default tooling, describe a concrete, beginner-friendly diagnostic workflow to (1) determine whether space, inodes, or reserved blocks are the bottleneck, (2) identify the offending filesystem or mount, and (3) apply a safe mitigation (e.g., reduce reserved blocks, prune files, or expand storage) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check quotas and throttle via /sys and per-process data. Use mpstat to confirm throttling, and map PIDs to container via /proc/<pid>/cgroup. If quota shows a bottleneck, raise quota (e.g., for v2: sud",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use systemd-analyze blame to find the slow boot unit, then systemd-analyze critical-chain to map dependencies. Inspect logs with journalctl -b -u <service> and dmesg for hardware waits. For mitigation",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run df -h and df -i to distinguish space vs inodes. If ENOSPC persists, run du -xS /var/lib/mongo | sort -n -r | head -n 20 to locate large dirs. Check mount with mount | grep mongo and inspect filesy",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Check nf_conntrack usage and adjust: cat /proc/sys/net/netfilter/nf_conntrack_max; wc -l /proc/net/nf_conntrack; if usage is high, run: sysctl -w net.netfilter.nf_conntrack_max=300000; sysctl -w net.n",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical disk/resource troubleshooting on Linux with MongoDB, focusing on a subtle ENOSPC case where space looks available.\n\n## Key Concepts\n- Space vs inode vs reserved blocks\n- Filesystem tuning (reserved blocks) and safe mitigation\n- Using default tools to locate large dirs and verify mounts\n\n## Code Example\n```bash\ndf -h\ndf -i\ndu -xS /var/lib/mongo | sort -n -r | head -n 20\nsudo tune2fs -m 1 /dev/sdXN\n```\n\n## Follow-up Questions\n- How would you validate the safety of reducing reserved blocks in production?\n- What alerts would you add to detect reoccurrence?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-10",
        "questionId": "q-23489",
        "question": "Scenario: A Linux host runs a multi-tenant web API in containers. During bursts, tail latency spikes for some tenants while CPU/I/O look normal. Memory pressure indicators rise in a subset of cgroups. Using only default Linux tools, design a concrete diagnostic to (1) prove memory pressure in specific cgroups is driving latency, (2) identify offending container(s) and their processes, and (3) apply safe mitigations (limit memory for those cgroups; use memory.max or memory.limit_in_bytes depending on cgroup version; adjust swap behavior) while preserving observability. Provide exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Diagnostic and Mitigation Workflow for cgroup Memory Pressure ### Step 0: Detect cgroup Version ```bash # Check cgroup version mount | grep cgroup # If /sys/fs/cgroup/memory exists → cgroup v1 # If /sys/fs/cgroup/cgr…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Complete NUMA-GPU Contention Diagnostic Workflow ### Phase 1: Confirm Cache/Bandwidth Contention on GPU Path **Baseline Performance Capture:** ```bash # System-wide cache performance baseline perf stat -e cycles,inst…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check timers and cron for triggers, then pin and throttle the culprit. Steps: systemctl list-timers --all; crontab -l; top -b -n1; ps -eo pid,pcpu,cmd --sort=-pcpu | head. If a timer/cron is the cause",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Diagnose tail latency by correlating per-CPU load with time windows, then drill into /proc/sched_debug to map back to offending runqueues and tasks. Verify memory pressure via /proc/vmstat and min_fre",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Matters\nThis tests advanced multi-tenant Linux isolation skills, validating the ability to map processes to cgroups, quantify per-cgroup memory pressure, and apply safe limits without impacting other tenants.\n\n## Key Concepts\n\n### 1. cgroup Version Detection\nDetermine whether the system uses cgroup v1 (hierarchical, separate controllers) or cgroup v2 (unified hierarchy).\n- v1 path: `/sys/fs/cgroup/memory/<cgroup>/`\n- v2 path: `/sys/fs/cgroup/<cgroup>/`\n\n### 2. Process-to-cgroup Mapping\n- `/proc/[pid]/cgroup` reveals which cgroup owns each process\n- `systemd-cgtop` provides real-time per-cgroup resource metrics\n- Critical for identifying which tenants are affected\n\n### 3. Memory Pressure Indicators\n- `memory.usage_in_bytes` / `memory.current` approaching `memory.limit_in_bytes` / `memory.max`\n- Elevated `pgpgin`, `pgfault` in `memory.stat`\n- Swap activity (si/so in `vmstat`) when memory is constrained\n\n### 4. Mitigation Strategy\n- Set `memory.limit_in_bytes` (v1) or `memory.max` (v2) to cap memory per cgroup\n- Use `memory.swappiness` (v1) or `memory.swap.max` (v2) to control swap behavior\n- Always verify limits post-application to ensure they took effect\n- Monitor to confirm latency normalizes after limits are applied",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-11",
        "questionId": "q-17290",
        "question": "Scenario: A Linux host serving a REST API shows intermittent tail latency spikes (100–300 ms) during bursts, while CPU/disk seem fine. Using only default Linux tools, design a concrete diagnostic workflow to (1) prove stalls relate to TCP receive window/congestion control, (2) locate the exact socket and process, and (3) apply a safe mitigation (e.g., adjust tcp_rmem/tcp_wmem or CC) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trace latency with per-flow cwnd growth. Use: ss -tiap | grep ESTAB to map PID, ss -tip to show cwnd/rtt, and cat /proc/net/tcp to correlate. Validate with tcpdump -i <iface> 'tcp and port <port>'. Mi",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Inspect per-container CPU quotas: cat /sys/fs/cgroup/cpu/*/cpu.cfs_quota_us and cpu.cfs_period_us; map to processes with ps -eo pid,cmd -cg <slice>. If quotas are tight, raise quota or move to a dedic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by correlating latency with IO wait and IRQ activity, using iostat -xz 1 and inspecting /proc/interrupts. Locate the hot NIC IRQ, then pin it to dedicated CPUs (echo 0x000000FF > /proc/irq/NNN/s",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run a per‑CPU frequency/idle check during bursts to prove stalls link to CPU state changes. Key commands: mpstat -P ALL 1 2 for c in /sys/devices/system/cpu/cpu*/cpufreq/scaling_cur_freq; do echo \"CPU",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical network debugging with Linux basics and teaches safe, observable tuning.\n\n## Key Concepts\n- TCP flow control, cwnd, rtt, congestion_control\n- ss and /proc/net/tcp for per-flow visibility\n- tcpdump for validation; sysctl for tunables\n\n## Code Example\n```javascript\n// example placeholder (commands above)\n```\n\n## Follow-up Questions\n- How would you verify that the change doesn't degrade other flows?\n- What are risks of increasing rmem/wmem and how would you mitigate?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-12",
        "questionId": "q-22892",
        "question": "Scenario: A Linux host running a high-throughput API experiences tail latency spikes during bursts despite stable CPU/IO, with kernel page reclaim activity surging. Using only default Linux tools, design a concrete diagnostic workflow to (1) confirm THP/page reclaim as the cause, (2) identify affected processes/containers, and (3) apply safe mitigations (disable THP, tune vm.swappiness, adjust memory cgroups) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run: vmstat 1 5; free -m; cat /proc/meminfo; numactl --hardware; ps -eo pid,cmd,rss --sort -rss | head -20. If reclaim is high, disable aggressive reclaim: sudo sh -c 'echo 0 > /proc/sys/vm/zone_recla",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run: vmstat 1 60; cat /sys/kernel/mm/transparent_hugepage/enabled; grep -i 'Cached' /proc/meminfo; for p in $(ps -eo pid --no-headers); do echo PID $p; awk '/VmFlags|VmSize|Rss/ {print $0}' /proc/$p/s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: identify bottleneck with ethtool -G eth0 and /proc/interrupts; quantify kernel load with perf stat -e softirq -p <PID>; bind IRQs via /proc/irq/IRQ/smp_affinity; grow RX/TX rings with ethtool -G",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Diagnostic Workflow ### Step 1: Confirm SYN Backlog Saturation ```bash # Check current somaxconn value cat /proc/sys/net/core/somaxconn # View SYN queue depth (non-established connections) ss -tanp state syn-recv | w…",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests ability to diagnose a subtle OS memory-management issue under load using only built-in tools, focusing on THP and reclaim behavior rather than common CPU bottlenecks.\n\nKey Concepts\n- Linux memory management: THP, page reclaim, swappiness\n- Process memory introspection: /proc/[pid]/smaps\n- Safe mitigations: disable THP, tune vm.swappiness, leverage memory cgroups\n\nCode Example\n```javascript\n// No code required for answer; see commands above in 'answer'.\n```\n\nFollow-up Questions\n- How would you validate observability after applying mitigations?\n- How would you adapt this workflow for containerized workloads with restricted host access?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-13",
        "questionId": "q-24537",
        "question": "Scenario: A Linux host acting as a NAT gateway for bulk web traffic intermittently drops new TCP connections during bursty traffic. CPU/Disk look ok. Using only default Linux tools, design a concrete diagnostic to (1) confirm the nf_conntrack table exhaustion bottleneck, (2) identify the offending flow(s) and the number of tracked connections, and (3) apply safe mitigations (increase nf_conntrack_max and nf_conntrack_buckets, adjust timeouts) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run iostat -x 1 60 to spot high await on a device; use pidstat -d 1 and iotop to map I/O to processes. Inspect /sys/block/<dev>/queue/nr_requests and /sys/block/<dev>/queue/read_ahead_kb for depth and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check if latency spikes are caused by container CPU throttling. Identify the container, read its cgroup CPU quotas, and correlate with latency. Commands: docker ps; docker inspect --format '{{.Id}}' <",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check nf_conntrack usage and adjust: cat /proc/sys/net/netfilter/nf_conntrack_max; wc -l /proc/net/nf_conntrack; if usage is high, run: sysctl -w net.netfilter.nf_conntrack_max=300000; sysctl -w net.n",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Start by correlating tail latency with ksmd activity. Run: ps -C ksmd -o pid,pcpu,cmd; watch -n1 'cat /sys/kernel/mm/ksm/pages_to_scan /sys/kernel/mm/ksm/pages_sharing'; inspect per-pid memory with /p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical diagnostic reasoning under NAT traffic pressure using only built‑in tools.\n\n## Key Concepts\n- nf_conntrack lifecycle and limits\n- Connection tracking table sizing and hashing\n- Observability while making safe kernel parameter changes\n\n## Code Example\n```bash\n# View current max and usage\ncat /proc/sys/net/netfilter/nf_conntrack_max\nwc -l /proc/net/nf_conntrack\n# Increase capacity safely\nsysctl -w net.netfilter.nf_conntrack_max=300000\nsysctl -w net.netfilter.nf_conntrack_buckets=262144\n# Inspect active flows (sample)\ngrep -i 'tcp' /proc/net/nf_conntrack | head -n 50\n# Ongoing observation\nwatch -n1 'wc -l /proc/net/nf_conntrack'\n```\n\n## Follow-up Questions\n- How would you validate impact without transiently breaking connections?\n- What are safe thresholds for max and buckets on a busy gateway?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-14",
        "questionId": "q-26501",
        "question": "Scenario: A Linux host serving a high-throughput web API exhibits intermittent failures to establish new TCP connections during bursts, while CPU/memory/disk look normal. Using only default Linux tools, design a concrete diagnostic to (1) confirm if SYN backlog or ephemeral ports are bottlenecked, (2) identify the offending process and the backlog/socket states, and (3) apply safe mitigations (raise net.core.somaxconn and net.ipv4.tcp_max_syn_backlog, widen net.ipv4.ip_local_port_range, enable tcp_tw_reuse) with observability preserved. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline with: vmstat 1 10; iostat -xz 1 10; ps -eo pid,comm,%cpu,%mem --sort=-%cpu | head -n 6; identify container: docker ps; docker stats --no-stream; watch for entry times that align with backup.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Step 1: check memory pressure: free -m; grep -i swap /proc/meminfo; vmstat 1 5. Step 2: list top memory users: ps -eo pid,cmd,%mem,rss --sort=-%mem | head -n 5. For a pid X, inspect: pmap -x X | head.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: use ss -s and ss -tanp to detect backlog/port pressure, inspect /proc/net/sockstat and the relevant sysctl values, then apply mitigations: sysctl -w net.core.somaxconn=65535; sysctl -w net.ipv4.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "## Diagnostic Workflow ### Step 1: Confirm Memory Pressure or Container Throttling ```bash # Check memory availability free -h # Monitor memory and swap activity over 5 seconds vmstat 1 5 ``` **Sample Output:** ``` tota…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical TCP backlog and port exhaustion diagnostics using only default Linux tools, plus safe, observable mitigations.\n\n## Key Concepts\n\n- SYN backlog and somaxconn\n- tcp_max_syn_backlog vs backlog\n- ip_local_port_range\n- tcp_tw_reuse\n- ss and /proc/net/sockstat\n- Observability during changes\n\n## Code Example\n\n```bash\nss -s\nss -tanp\ncat /proc/net/sockstat\ncat /proc/sys/net/ipv4/tcp_max_syn_backlog\ncat /proc/sys/net/core/somaxconn\n```\n\n## Follow-up Questions\n\n- How would you verify the mitigation's effectiveness after deployment?\n- What are potential side effects of increasing backlog and port ranges?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-15",
        "questionId": "q-10462",
        "question": "Scenario: A Linux node runs a mixed Go service and a data collector. During bursts, tail latency spikes to 800 ms for a subset of requests, while CPU ~70% and I/O looks normal. Using only default tools, design a concrete workflow to (1) prove memory bandwidth/cache contention is the tail-latency driver, (2) identify the offending PID/threads and memory access, and (3) apply a safe mitigation (pin to a NUMA node, cpuset/isolcpus, or memcg limit) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check THP status and defrag state; inspect /proc/meminfo for AnonHugePages and HugePages; identify memory-heavy processes with ps -eo pid,cmd,%mem --sort=-%mem | head; map each pid to NUMA nodes via /",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Find the target process and its cgroup, then inspect quotas and monitor bursts: pid=$(pgrep -f my-service); cg=$(awk -F: '/name/ {print $3}' /proc/$pid/cgroup); echo $cg; cat /sys/fs/cgroup/$cg/cpu.ma",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use iostat -x 1 to detect iowait, pidstat -d 1 -p $(pgrep redis-server) to map IO to Redis, and vmstat 1 to watch swapping. If iowait spikes, switch the device scheduler to deadline and adjust Redis p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: 1) verify contention with vmstat 1, iostat -x 1, and numastat -c; 2) locate hotspots with pidstat -m -p ALL and perf top -p <pid>, then inspect /proc/<pid>/smaps for RSS/Swap; 3) mitigate by bin",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical use of Linux observability to diagnose tail latency caused by resource contention, not just CPU.\n\n## Key Concepts\n\n- Memory bandwidth, cache contention, NUMA affinity\n- pidstat, vmstat, iostat, numastat, perf top\n- cpuset, isolcpus, numactl, memcg\n\n## Code Example\n\n```bash\nvmstat 1\nnumastat -c\nperf stat -e LLC-load,LLC-store -p <pid>\n```\n\n## Follow-up Questions\n\n- How would you automate this workflow for a multi-tenant cluster?\n- What failure modes or false positives would you guard against?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-16",
        "questionId": "q-26538",
        "question": "A Linux host running a small REST API begins to respond slowly under load, while free memory seems sufficient and CPU usage is modest. Swap usage increases and swap I/O rises intermittently. Using only default tools, design a concrete diagnostic workflow to (1) confirm swap pressure is the slowdown cause, (2) identify the processes driving swap activity, and (3) apply safe mitigations (adjust vm.swappiness, consider memory limits, or tune swapoff timing) with observability preserved?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: use ss -s and ss -tanp to detect backlog/port pressure, inspect /proc/net/sockstat and the relevant sysctl values, then apply mitigations: sysctl -w net.core.somaxconn=65535; sysctl -w net.ipv4.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-container CPU visibility via ps and map PIDs to cgroups in /proc/<pid>/cgroup; inspect quotas at /sys/fs/cgroup/cpu/<cg>/cpu.cfs_quota_us and cpu.cfs_period_us. If quotas limit CPU, raise quot",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: Confirm that tail latency correlates with CPU frequency scaling and thermal throttling, then isolate affected cores and apply targeted mitigations. **Step 1: Verify CPU Frequency Scaling and Throttling** Check cur…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Strategy: 1) verify swap pressure with free -h and vmstat 1 5 to observe swap in/out. 2) identify top swap users by parsing /proc/*/status VmSwap for all pids; map to commands via ps -eo pid,ppid,cmd,",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to diagnose memory pressure and swap using only default tools, a common production scenario.\n\n## Key Concepts\n- Memory management and swap\n- Vm.swappiness tuning\n- Per-process memory usage\n- Observability and safe mitigation\n\n## Code Example\n```bash\nfree -h\nvmstat 1 5\nfor pid in $(ps -eo pid --no-headers); do awk '/VmSwap/ {print \"PID:\" pid \" Swap:\" $2}' /proc/$pid/status 2>/dev/null; done | sort -nr -k2 | head\n```\n\n## Follow-up Questions\n- How would you verify no performance regressions after increasing per-process memory limits?\n- What are risks of lowering vm.swappiness on a production API?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-17",
        "questionId": "q-5318",
        "question": "On a Linux server hosting multiple services, intermittent latency spikes occur under memory pressure with THP enabled. Using only default tools, design a concrete diagnostic workflow to (1) verify if hugepage fragmentation is causing allocator delays, (2) identify the specific process or container consuming memory aggressively, and (3) apply a safe mitigation (e.g., disable THP on affected zone, adjust min_free_kbytes, or migrate memory pressure) while preserving observability. Include exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run quick baselines: `top -b -n1`, `vmstat 1 5`, `iostat -xd 1 5`, and `ps -eo pid,pcpu,pmem,cmd --sort=-pcpu | head -n 6`. If CPU is hot, assign affinity with `taskset -pc <cpus> <pid>`; if I/O bound",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Start with triage: check per-process limits with ulimit -n and /proc/$PID/limits; monitor fds in use via ls /proc/*/fd | wc -l; watch I/O and CPU wait with iostat -dx 1 2 and vmstat 1; inspect logs vi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Baseline traffic; monitor interface stats and IRQs during bursts. Commands: ip -s link show eth0; cat /proc/interrupts | grep eth0; sar -n DEV 1 10; if a hot IRQ is seen, bind it: echo 1 > /proc/irq/4",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Baseline THP state and memory pressure signals, then correlate with latency: - cat /sys/kernel/mm/transparent_hugepage/enabled - dmesg | grep -i thp - cat /proc/meminfo | grep -iHuge - for each cgroup",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nMemory allocator stalls under pressure can stem from THP fragmentation; diagnosing with default tools tests hypothesis without specialized profilers.\n\n## Key Concepts\n- Transparent Huge Pages (THP)\n- Memory fragmentation and allocator latency\n- Per‑cgroup memory accounting\n- Safe kernel knob changes with observability\n\n## Code Example\n```bash\n# Commands mentioned in answer (for reference)\ncat /sys/kernel/mm/transparent_hugepage/enabled\ndmesg | grep -i thp\ngrep -i Huge /proc/meminfo\nfor c in /sys/fs/cgroup/memory/*; do echo \"$c\"; cat \"$c/memory.stat\"; done\n```\n\n## Follow-up Questions\n- After disabling THP, how would you validate no regression across services?\n- What persistent config would you apply to avoid regressions in future bursts?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-18",
        "questionId": "q-26832",
        "question": "Scenario: A Linux host runs a stateful video-processing service with local NVMe storage. During bursts, I/O tail latency spikes appear while CPU/disk wait seems normal. Using only default Linux tools, design a concrete diagnostic to (1) confirm if I/O scheduling or block layer is the bottleneck, (2) identify the exact device/queue and the process owning the IO, and (3) apply safe mitigations (mq-deadline, queue_depth, read_ahead) while preserving observability. Provide exact commands and sample outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a concise workflow: 1) monitor device latency with iostat -xz 1 to spot rising await and wrqm; 2) correlate with processes using pidstat -d 1 -p ALL during bursts; 3) verify with perf record -",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run: iostat -dx 1 2 to verify high await on the NVMe device during bursts. Check the scheduler: cat /sys/block/nvme0n1/queue/scheduler. If needed: echo mq-deadline > /sys/block/nvme0n1/queue/scheduler",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Baseline check hot threads with: ps -eo pid,tid,pcpu,psr,comm | grep myapi; measure scheduler impact with: perf stat -p <tid> -e cycles,instructions,cache-references,cache-misses 1s; inspect /proc/sch",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Check backlog and buffering first:** Inspect kernel-level connection queue limits and current socket states: ```bash # View the global maximum socket backlog (somaxconn) cat /proc/sys/net/core/somaxconn # Inspect all…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical storage bottleneck debugging at the kernel/block layer with minimal tooling, a common enterprise scenario.\n\n## Key Concepts\n- Block layer scheduling (mq-deadline vs alternatives)\n- Per-queue depth and read-ahead tuning\n- IO-to-process mapping via pidstat and /proc/diskstats\n\n## Code Example\n```bash\n# diagnose\niostat -dx 1 2\ncat /sys/block/nvme0n1/queue/scheduler\n# mitigate\necho mq-deadline > /sys/block/nvme0n1/queue/scheduler\necho 1024 > /sys/block/nvme0n1/queue_depth\n```\n\n## Follow-up Questions\n- How would you validate long-term observability after changes?\n- What are risks of increasing queue_depth on shared NVMe pools?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-19",
        "questionId": "q-466",
        "question": "You're debugging a production Linux server where processes are randomly dying with 'Out of memory' errors, but `free -m` shows 8GB available RAM. How would you diagnose and fix this issue?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check `dmesg | grep -i oom-killer` for OOM events. Use `cat /proc/meminfo` to examine memory fragmentation. Review `overcommit_memory` and `overcommit_ratio` in `/proc/sys/vm/`. Monitor with `sar -r`",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Telemetry over 60s: free -h; vmstat 1 60; slabtop -o; check MemAvailable and SwapUsed to decide if pressure is from anonymous memory or cache. Map cgroups: systemd-cgls | grep -i memory; for each path",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run per-process CPU checks with pidstat, inspect quota and throttling in the cgroup via /sys/fs/cgroup/.../cpu.max and cpu.stat, confirm throttling by comparing quota usage to period, then reallocate",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Diagnose by systematically checking each potential bottleneck: 1. **TCP Backlog Analysis:** ```bash ss -ltnp | head -5 # Check listening queues ss -s # Show socket statistics cat /proc/sys/net/core/somaxconn # Current b…",
            "isCorrect": false
          }
        ],
        "explanation": "## Memory Management Issues\n\nLinux OOM killer activates when available memory + swap < pages_min * 4, not when RAM is fully used.\n\n## Diagnostic Steps\n\n- Check OOM killer logs: `dmesg | grep -i oom-killer`\n- Examine memory fragmentation: `cat /proc/meminfo | grep -E '(MemFree|MemAvailable|Slab|PageTables)'`\n- Monitor memory pressure: `cat /proc/pressure/memory`\n- Review overcommit settings: `cat /proc/sys/vm/overcommit_memory`\n\n## Common Causes\n\n- Memory fragmentation preventing large allocations\n- Overcommitment allowing more memory than physically available\n- Kernel memory usage (slabs, page tables) not visible in `free`\n- Memory leaks in kernel modules\n\n## Solutions\n\n```bash\n# Disable overcommit\necho 0 > /proc/sys/vm/overcommit_memory\n\n# Add swap space\nfallocate -l 2G /swapfile\nchmod 600 /swapfile\nmkswap /swapfile\nswapon /swapfile\n\n# Tune memory management\necho 65536 > /proc/sys/vm/min_free_kbytes\n```",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-20",
        "questionId": "q-8690",
        "question": "A Linux host runs multiple containers hosting a Python API behind Nginx. During traffic bursts, 95th percentile response time spikes to ~1-2s, while CPU, memory, and disk I/O appear normal. Using only default Linux tools, outline a beginner-friendly diagnostic workflow to (1) determine whether the bottleneck is I/O wait, CPU, or memory, (2) identify the container or process responsible for the tail latency and the disk device involved, and (3) apply a safe mitigation (e.g., adjust container CPU shares, reorder startup, throttle background work) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Start with iostat -dx 1 5 to spot device-level tail latency; sample per-pid IO with top -b -n 1; map suspected PIDs to pods by reading /proc/<pid>/cgroup; locate blkio weight via /sys/fs/cgroup/blkio/",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run: iostat -dx 1 2 to verify high await on the NVMe device during bursts. Check the scheduler: cat /sys/block/nvme0n1/queue/scheduler. If needed: echo mq-deadline > /sys/block/nvme0n1/queue/scheduler",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Diagnose memory pressure and swap thrash as the tail-latency root cause on a containerized Linux host. Steps: 1) free -m; vmstat 1 60; 2) ps -eo pid,comm,rss --sort=-rss | head; 3) map to cgroups via",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Begin with top -b -n1, vmstat 1 5, iostat -dx 1 5, pidstat -d 1 5 -p ALL, and docker stats --no-stream to map I/O wait to a container. If iowait is high and a container shows heavy block IO, throttle",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to diagnose containerized Linux tail latency using basic tools, linking symptoms to a specific process or container and selecting safe mitigations.\n\n## Key Concepts\n\n- I/O wait and disk pressure\n- Per-process IO accounting (pidstat, /proc)\n- Container-level resource controls (CPU shares, blkio)\n- Observability and non-disruptive mitigations\n\n## Code Example\n\n```bash\ntop -b -n1 | head -n 20\nvmstat 1 5\niostat -dx 1 5\npidstat -d 1 5 -p ALL\ndocker stats --no-stream\n```\n\n## Follow-up Questions\n\n- How would you distinguish between multiple containers contributing to I/O? \n- What non-disruptive long-term mitigations would you consider if bursts are periodic?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-q-21",
        "questionId": "q-7437",
        "question": "Scenario: A multi-tenant Linux host with containerized services runs on a dual-socket NUMA system. Under sustained load, tail latency spikes appear for a payment service. Using only default tools, design a concrete diagnostic workflow to (1) verify NUMA imbalance and cross-node memory access, (2) identify the containers and memory nodes involved, and (3) apply a safe mitigation (e.g., pin memory/CPUs with numactl, isolate a socket, or adjust memory policy) while preserving observability. Include exact commands and example outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a NUMA-aware diagnostic workflow. Steps: 1) Verify topology and per-node memory distribution using `lscpu; numactl --hardware; numastat -m` to establish baseline NUMA configuration and identify existing memory i…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "To diagnose CPU throttling from container quotas, first list running containers using `docker ps`, then examine cgroup metrics for each container at `/sys/fs/cgroup/cpu/docker/<container_id>/cpu.{cfs_quota_us,cfs_period…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check OOM: dmesg | grep -i oom; dmesg | tail -n 60; free -m; grep -i MemAvailable /proc/meminfo; for cg in /sys/fs/cgroup/memory/*; do [ -f $cg/memory.usage_in_bytes ] && echo '---' $cg && cat $cg/mem",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Start by confirming cgroup v2 CPU throttling: check quotas with `cat /sys/fs/cgroup/cpu.max` (expect format like '100000 200000' for 100ms quota per 200ms period), then verify throttling with `cat /sys/fs/cgroup/cpu.sta…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nReal-world systems often suffer tail latency due to NUMA memory locality on multi-socket hosts. This question probes practical detection, attribution, and mitigation actions using default tooling, without relying on vendor-specific utilities.\n\n## Key Concepts\n\n- NUMA topology and memory locality\n- Per-node memory pressure and cross-node references\n- CPU pinning and memory binding\n- Observability of latency vs locality interactions\n\n## Code Example\n\n```bash\n# Step 1: Verify NUMA topology\nlscpu\nnumactl --hardware\nnumastat -m\n\n# Step 2: Monitor process-level memory access\npidstat -p ALL 1\npmap -x <PID>\nnumastat -p <PID>\n\n# Step 3: Apply mitigation\nnumactl --cpunodebind=0 --membind=0 <start_container>\n# OR\nsystemd-run --slice=payment.slice --property=CPUAffinity=0-15 --property=MemoryPolicy=prefer --property=MemoryNode=0\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-22",
        "questionId": "q-4465",
        "question": "On a dual-socket Linux server running a CPU-bound analytics workload, latency spikes occur under peak load even when CPU usage seems normal. Using only default tooling, design a concrete diagnostic workflow to (1) determine if the spike is NUMA memory locality, CPU throttling, or kernel I/O, (2) pinpoint the offending subsystem or process, and (3) apply a safe mitigation (e.g., bind memory/threads with numactl, isolate CPUs, or adjust memory policies) while preserving observability. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run numactl --hardware and numastat -m to verify NUMA topology and locality. Use mpstat -P ALL 1 and iostat -xz 1 to detect CPU throttling or IO waits; inspect /proc/<pid>/numa_maps for hotspots. If l",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Run: vmstat 1 5; top -b -n1 | head -n 5; ss -tulpen | grep :80; ps -eo pid,ppid,comm,%cpu --sort=-%cpu | head. If wa>0 in vmstat, IO wait; if r high, CPU scheduling. Top reveals culprit. Mitigate by e",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Diagnosis steps: 1) ss -s to check socket counts; 2) ss -a | awk '/TIME-WAIT/ {print}' to quantify TIME_WAIT; 3) check ephemeral range: cat /proc/sys/net/ipv4/ip_local_port_range; 4) view nf_conntrack",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Identify pid: ps -eo pid,cmd | grep -w myservice; map to cgroup: cat /proc/<pid>/cgroup; inspect limits: cat /sys/fs/cgroup/cpu.max; cat /sys/fs/cgroup/io.max; locate container path (grep 'docker' /pr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses NUMA-aware debugging and safe mitigation using defaults, critical in high-saturation production.\n\n## Key Concepts\n\n- NUMA locality\n- /proc/<pid>/numa_maps\n- mpstat, iostat\n- numactl, taskset\n\n## Code Example\n\n```bash\nnumactl --hardware\nnumastat -m\n```\n\n## Follow-up Questions\n\n- How would you validate that the mitigation didn't impact cross-node memory access?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-q-23",
        "questionId": "q-4294",
        "question": "On a Linux host serving low-latency ML inference REST endpoints, tail latency spikes under peak load while average latency stays acceptable. Using only default tools, design a concrete diagnostic workflow to (1) determine if tail latency is due to CPU, memory, IO, or interrupts, (2) identify the bottlenecked core/NUMA domain and the offending process, and (3) propose a safe mitigation (e.g., CPU pinning, shielding, IO scheduler tweaks) while preserving observability. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check throttling with: cat /sys/fs/cgroup/cpu.stat (or cpu.max on v2) to read throttled_time. Map the responsible container by combining docker ps with docker top and inspecting /proc/<pid>/cgroup for",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an end-to-end eBPF trace with bpftrace: attach kprobes/kretprobes on accept, recv, and send, plus the HTTP handler; store start times per tid and compute deltas to build a latency histogram keyed",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run a triage: (a) vmstat 1; iostat -xz 1; cat /proc/interrupts to spot IO/IRQ pressure; (b) pidstat -p ALL 1; ps -eo pid,ppid,cmd,%cpu --sort=-%cpu | head; mpstat -P ALL 1 to map cores to load; (c) mi",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Check TIME_WAIT and local port usage with ss -s and ss -tulpen; inspect port range with cat /proc/sys/net/ipv4/ip_local_port_range. If needed widen with sudo sysctl -w net.ipv4.ip_local_port_range='10",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal latency issues demand structured triage with OS visibility, using only built-in tools.\n\n## Key Concepts\n- diagnose tail latency sources: CPU contention, memory pressure, IO wait, interrupts\n- map per-core activity to NUMA domains and processes\n- safe mitigations: CPU pinning, shielding, IRQ balance, IO scheduler tweaks, observability\n\n## Code Example\n```bash\nvmstat 1\niostat -xz 1\ncat /proc/interrupts\n```\n\n## Follow-up Questions\n- How would you validate observability after applying mitigations?\n- What risks arise from CPU shielding on a production host?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-q-24",
        "questionId": "q-1570",
        "question": "A Linux CI node running multiple git builds experiences occasional stalls during parallel jobs. Using only default tooling and no downtime, describe a concrete diagnostic workflow to (1) determine if CPU, memory, or I/O is the bottleneck, (2) identify the specific component (e.g., git, filesystem, network) causing the stall, and (3) apply a safe mitigation (e.g., throttle parallel jobs, adjust I/O scheduler, or raise file descriptor limits) while monitoring impact. Include exact commands and expected outputs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin with system-wide metrics: vmstat 1 60 to spot wa and swapping; if wa or swaps spike, run pidstat -u -p ALL 1 60 to identify cpu hogs. Map PID to container via /proc/[pid]/cgroup, then pin with t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Start by verifying swap thrash. Run free -h and vmstat 1 5 to spot swap activity during bursts. Identify top swapped processes with a script over /proc/[pid]/status reading VmSwap and sorting by value",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by correlating latency with memory pressure using vmstat 1; free -h; and swap activity. Inspect per‑cg memory with /sys/fs/cgroup/memory/<cg>/memory.usage_in_bytes and memory.limit_in_bytes; loc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run `vmstat 1 60` and `iostat -xz 1 60`, then `top -b -n1` to identify CPU, memory, and I/O pressure. If iowait is elevated, map I/O to processes with `pidstat -d 1`. If a git/build task is causing the issue, throttle p…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical skills in diagnosing common Linux bottlenecks using default tools and linking observations to concrete mitigations in a CI context.\n\n## Key Concepts\n- Baseline and interpret vmstat/iostat outputs\n- I/O wait (iowait) vs CPU\n- PID-level I/O mapping with pidstat\n- Safe mitigations: adjust concurrency, swappiness, fd limits, scheduler\n\n## Code Example\n```bash\nvmstat 1 60\niostat -xz 1 60\ntop -b -n1 | head\n```\n\n## Follow-up Questions\n- How would you verify that mitigation didn't degrade other workloads?\n- Which metrics would you log for post-change validation?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-09T11:12:32.836Z",
    "version": 1
  }
]