[
  {
    "id": "test-terraform",
    "channelId": "terraform",
    "channelName": "Terraform",
    "title": "Terraform Knowledge Test",
    "description": "Test your understanding of Terraform with 25 questions.",
    "questions": [
      {
        "id": "terraform-q-0",
        "questionId": "q-20428",
        "question": "Context: terraform/general; Difficulty: intermediate; Scenario: A multi-tenant platform uses a shared networking module deployed to AWS and GCP. Each tenant stores its state in a central backend with per-tenant keys. Design a plan-gate and module interface that (1) restricts changes to a tenant's CIDR and IAM bindings, (2) enforces per-tenant tags via a policy-as-code layer, (3) prevents cross-tenant references, and (4) ensures provider aliases stay in sync across clouds. Provide skeletons for: 1) backend.tfvars per tenant, 2) a module interface taking tenant_id, region, provider_alias, 3) an OPA policy snippet for plan-time checks, 4) a GitHub Actions gate to run plan for a specific tenant and fail on violations. Include minimal code references?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan gate should enforce tenant isolation and provider symmetry. Propose: 1) backend.tfvars per tenant with a per-tenant backend key, 2) module interface exposing tenant_id, region, provider_alias, 3)",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a wrapper module that accepts env (dev/stage/prod) and outputs env-specific tags and name prefixes. Use a single backend with key envs/${var.env}/terraform.tfstate to isolate state. Gate in",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Pin the module using a git ref and add a local vendored fallback controlled by a boolean. Use a single module block with a conditional source so CI can fetch from the private registry or fall back to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a root module with a single input 'target_cloud' to select AWS or GCP via provider aliases. Create a shared 'networking' module that provisions VPC/VNet, private DNS, and peering using environment",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests an intermediate candidate's ability to design cross-cloud, multitenant Terraform workflows with strong isolation, policy-as-code enforcement, and CI gates that operate at plan time.\n\n## Key Concepts\n\n- Terraform remote state isolation per tenant\n- Provider aliasing and multi-cloud coordination\n- Policy-as-code (OPA) for plan-time checks\n- CI gates that validate plans before apply\n\n## Code Example\n\n```json\n{\n  \"example\": \"not executable code in this context\"\n}\n```\n\n## Follow-up Questions\n\n- How would you extend the OPA policy to include drift detection between intended and actual state?\n- What are the trade-offs of storing per-tenant state in a single backend vs. separate backends per tenant?",
        "difficulty": "intermediate"
      },
      {
        "id": "terraform-q-1",
        "questionId": "q-19230",
        "question": "Context: terraform/general; Difficulty: beginner; Scenario: A mono-repo provisions a VPC via a shared module. Add a CI gate that runs terraform plan, outputs JSON, and fails if the plan includes changes to any resource labeled prod-critical (e.g., IAM roles or production SGs). Provide minimal code references for: 1) gate script parsing plan JSON, 2) a GitHub Actions step invoking it, 3) a prod-critical selector example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run terraform plan -out=plan.tfplan; terraform show -json plan.tfplan > plan.json; use jq to scan plan.json for resource_changes that match prod-critical resources (e.g., aws_iam_role, aws_security_gr",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use an HTTP data source to pull environment-specific feature flags from a centralized repository, pass them to your module as an enabled_features map, and gate production deployments with a GitHub Actions step that runs…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Terraform setup where each tenant deploys to their own AWS account using a shared, versioned module from a private registry. For each tenant, configure an AWS provider alias that assumes a cross-account IAM rol…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "For each tenant, create per-tenant provider aliases (snowflake.tenant1, aws.tenant1) and a per-tenant backend. Provisions include Snowflake database/schema, warehouse, and a network policy; credential",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n- Tests JSON-plan-based safety gates in CI, a common real-world practice.\n- Ensures plan-level checks catch production-impact changes early.\n\n## Key Concepts\n- Terraform plan JSON, resource_changes, jq filtering, CI gates, prod-critical tagging.\n- Safe-guarding apply against risky resource types.\n- Reproducible checks in GitHub Actions.\n\n## Code Example\n```bash\n# gate_prod_changes.sh\nPLAN_JSON plan.json\nif jq -e '.resource_changes[]?.address' plan.json | grep -E 'aws_(iam_role|security_group)' >/dev/null; then\n  echo \"Prod-critical changes detected\"; exit 1\nfi\n```\n```yaml\n# .github/workflows/terraform.yml\n- name: Gate prod changes\n  run: bash ./gate_prod_changes.sh\n```\n\n## Follow-up Questions\n- How would you extend the gate for multi-environment backends?\n- How would you test this gate locally before CI run?",
        "difficulty": "beginner"
      },
      {
        "id": "terraform-q-2",
        "questionId": "q-29053",
        "question": "Context: terraform/general; Difficulty: advanced; Topics: terraform; Scenario: A SaaS uses Cloudflare as the edge for tenants. Each tenant gets a dedicated Worker, Route, and TLS cert. Design a Terraform strategy that creates per-tenant Worker scripts, Routes, and secrets (KV or Vault-backed), with per-tenant state management and isolated deployments. Include a policy gate for prod changes and a minimal Terratest harness to verify route isolation and idempotence?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a tenants map to generate per-tenant provider aliases and backends. Retrieve tenant firewall rules via an external data source (calling the REST policy service) keyed by tenant_id, feeding per-tenant variables into…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a tenant map and for_each to deploy per-tenant Cloudflare Workers, Routes, and TLS certs under separate Cloudflare Zones or subdomains. Store per-tenant secrets in Cloudflare Secrets (or Vault-bac",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a wrapper module env_logs with input map<string, object{enabled bool; bucket_suffix string}>. Use for_each to create an S3 bucket and a CloudWatch log group when enabled, exposing the bucket",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "The design employs per-tenant provider aliases for AWS, Azure, and GCP, paired with per-tenant remote backends (S3, Blob Storage, and GCS) to ensure complete state isolation. Each tenant provisions isolated networking a…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design multi-tenant edge provisioning with strong isolation, policy gates, and a test harness.\n\n## Key Concepts\n- Cloudflare provider and edge resources (Workers, Routes, TLS certs)\n- for_each over a tenants map for per-tenant resources\n- per-tenant secrets management (Cloudflare Secrets or Vault)\n- per-tenant state/workspaces to guarantee isolation\n- policy-as-code gate (OPA or Sentinel) for prod changes\n- Terratest harness for idempotence and route isolation\n\n## Code Example\n```hcl\nvariable \"tenants\" {\n  type = map(any)\n}\nresource \"cloudflare_worker_script\" \"tenant\" {\n  for_each = var.tenants\n  name     = \"${each.key}-worker\"\n  content  = each.value.script\n}\nresource \"cloudflare_worker_route\" \"tenant_route\" {\n  for_each = var.tenants\n  zone_id  = each.value.zone_id\n  pattern  = each.value.route_pattern\n  script   = cloudflare_worker_script.tenant[each.key].name\n}\n``` \n\n## Follow-up Questions\n- How would you scale this to 100+ tenants with minimal toil? \n- How do you rotate TLS certs without disruption?",
        "difficulty": "advanced"
      },
      {
        "id": "terraform-q-3",
        "questionId": "q-3531",
        "question": "Design a practical, scalable approach to manage Terraform for a multi-tenant SaaS on AWS where each tenant's resources (VPC, subnets, services) live in separate accounts but state is centralized in a single backend. Provide (a) a reusable tenant module with per-tenant provider aliases and a backend key derivation, (b) a wrapper that injects tenant context, (c) CI gating that rejects plans touching resources outside the tenant, and (d) drift checks and promotion workflow. Include concrete backend config snippet and a sketch of the wrapper module?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Fetch per-env policy with data 'terraform_remote_state' from the central policy repo, then drive SG rules with for_each over policy.inbound[env]. Use a CI gate that runs init/plan and a small script t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Terratest-driven harness that spins up a sandbox, runs terraform init/apply for the module, asserts resources exist (VPC, subnets) and have Environment/CostCenter tags in the sandbox, then re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a shared module registry with per-tenant state backends (S3 for AWS, GCS for GCP) and provider aliases for cross-cloud resources (aws.primary, gcp.dr). Implement a DR submodule that mirrors critical assets to GCP, g…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant backend keys and provider aliases derived from tenant_id, using a wrapper module that sets alias maps and shared backend config. CI runs terraform plan for each tenant, exporting",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nExplores scale, multi-tenant state, and governance in Terraform.\n\n## Key Concepts\n- Terraform backends per tenant\n- Provider aliases and module wrappers\n- CI plan gating with tenant-scoped changes\n- Drift detection and controlled promotion\n\n## Code Example\n```hcl\n# pseudo-backend config sketch\nbackend \"s3\" {\n  bucket = \"tf-state-central\"\n  key    = \"tenant_${tenant_id}/terraform.tfstate\"\n  region = \"us-east-1\"\n  dynamodb_table = \"tf-lock\"\n}\n```\n\n## Follow-up Questions\n- How would you handle cross-tenant dependencies?\n- How do you audit tenant changes over time?",
        "difficulty": "advanced"
      },
      {
        "id": "terraform-q-4",
        "questionId": "q-10612",
        "question": "Design a two-stage Terraform pattern: network/cluster repo writes cluster_id and kubeconfig to remote state; apps repo reads them via data.terraform_remote_state. Gate: CI runs stage2 plan and fails if the plan touches shared cluster resources (aws_eks_cluster, kubeconfig, node_pool) without approval. Include a minimal data source and an OPA rule + gating steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Switch from local to S3 backend with DynamoDB locking. Add a backend block: backend \\\"s3\\\" { bucket = \\\"tf-state-bucket\\\" key = \\\"prod/terraform.tfstate\\\" region = \\\"us-east-1\\\" dynamodb_table = \\\"tf-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-stage pattern: network/cluster repo writes cluster_id and kubeconfig to remote state; apps repo reads them via data.terraform_remote_state. Gate: CI runs stage2 plan and fails if the plan touches",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Adopt a governance repo storing required_tags; consumers fetch with data.terraform_remote_state and merge into resource tags. A CI gate runs terraform plan -out, parses the JSON plan to ensure every r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline concrete approach: Each tenant uses a dedicated S3 bucket with SSE-KMS and a key derived from tenant name; backend.tf maps tenant/env to bucket/key. A single shared core module with a tenants/",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates cross-repo governance for shared state, two-stage planning, and policy enforcement in CI.\n\n## Key Concepts\n- remote state data sources, two-stage plan, per-environment backends, policy-as-code, gating.\n\n## Code Example\n```opa\npackage terraform.policy\ndefault deny = true\ndeny[msg] {\n  input.kind == \\\"plan\\\"\n  some rc\n  rc := input.resource_changes[_]\n  rc.change.before != null\n  rc.change.after != null\n  rc.type == \\\"aws_eks_cluster\\\"\n  rc.change.after.cluster_id != input.approved_cluster_id\n}\n```\n\n## Follow-up Questions\n- How would you test gating with plan JSON invariants?\n- How to handle drift if cluster_id changes via a legitimate upgrade?",
        "difficulty": "intermediate"
      },
      {
        "id": "terraform-q-5",
        "questionId": "q-30442",
        "question": "Context: terraform/general; Difficulty: intermediate; Topics: terraform; Scenario: A mid-size fintech uses a shared Terraform module registry to provision per-environment sandboxes across AWS, Azure, and GCP from a single repo. Design a practical Terraform strategy that (1) pins module versions and maintains a cross-cloud compatibility matrix, (2) isolates remote state per environment with per-cloud backends and a consistent naming scheme, (3) injects secrets from a centralized vault using provider aliases and data sources, (4) enforces a CI gate that blocks plans if a required secret is missing or a provider upgrade introduces breaking changes. Provide skeletons for module layout, backend configuration, policy snippet, and a GitHub Actions gate that validates plans?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A practical Terraform strategy involves pinning module versions with strict constraints and maintaining a cross-cloud compatibility matrix to ensure consistency across AWS, Azure, and GCP. Remote state should be isolate…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a per-tenant network-guard module that ingests a policy map (tenant => allowed CIDRs, ports, action). Use for_each to create firewall rules across AWS security groups and GCP firewall rules wit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a provider-agnostic core network module, plumb per-customer modules with for_each, backends keyed by customer_id, and provider aliases for AWS and GCP. Enforce IAM/policy via OPA/Sentinel. Add",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a wrapper module that accepts env and applies standard tags Environment and CostCenter to all resources. Allocate 3 subnets (public, private, isolated) across 2 AZs with for_each over a CIDR",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates the ability to design multi-cloud Terraform workflows that balance module versioning, environment isolation, secrets management, and automated quality gates. It assesses practical understanding of trade-offs between configuration drift, security posture, and developer velocity.\n\n## Key Concepts\n\n- Module version pinning and cross-cloud compatibility matrices\n- Per-environment remote state isolation across multiple cloud providers\n- Provider aliases and secret injection via Vault data sources\n- CI gates enforcing secret presence and controlled provider upgrades\n\n## Code Example\n\n```terraform\n# Module layout example\nmodules/\n├── base/\n│   ├── networking/\n│   │   ├── versions.tf\n│   │   ├── main.tf\n│   │   └── outputs.tf\n│   └── security/\n│       └── ...\n├── app/\n│   └── ...\n└── data/\n    └── ...\n\n# versions.tf with pinning\nterraform {\n  required_version = \">= 1.5.0\"\n  required_providers {\n    aws = {\n      source  = \"hashicorp/aws\"\n      version = \"~> 5.0\"\n    }\n    azurerm = {\n      source  = \"hashicorp/azurerm\"\n      version = \"~> 3.0\"\n    }\n    google = {\n      source  = \"hashicorp/google\"\n      version = \"~> 5.0\"\n    }\n  }\n}\n\n# Backend configuration per environment\n# environments/prod/backend.hcl\nterraform {\n  backend \"s3\" {\n    bucket         = \"terraform-state-prod\"\n    key            = \"prod/networking/terraform.tfstate\"\n    region         = \"us-east-1\"\n    encrypt        = true\n    dynamodb_table = \"terraform-locks\"\n  }\n}\n\n# Azure backend for prod\n# environments/prod/azure/backend.hcl\nterraform {\n  backend \"azurerm\" {\n    resource_group_name = \"terraform-state-prod\"\n    storage_account_name = \"terraformstateprod\"\n    container_name       = \"prod-state\"\n    key                  = \"networking/terraform.tfstate\"\n  }\n}\n\n# Provider aliases for multi-cloud\nprovider \"aws\" {\n  alias  = \"prod\"\n  region = \"us-east-1\"\n}\n\nprovider \"azurerm\" {\n  alias      = \"prod\"\n  features   = {}\n  subscription_id = var.azure_subscription_id\n}\n\nprovider \"google\" {\n  alias     = \"prod\"\n  project   = var.gcp_project_id\n  region    = \"us-central1\"\n}\n\n# Vault secrets injection\nprovider \"vault\" {\n  alias   = \"shared\"\n  address = var.vault_address\n}\n\ndata \"vault_kv_secret_v2\" \"db_credentials\" {\n  provider = vault.shared\n  name     = \"database-credentials\"\n  mount    = \"secret\"\n}\n\n# Sentinel policy snippet (OPA/Gatekeeper)\npackage terraform\n\nimport futuretf.plan as tfplan\n\n# Check provider versions\ndeny_providers[msg] {\n  some p\n  tfplan.resource_changes[p].type == \"aws_instance\"\n  tfplan.resource_changes[p].provider_name != \"registry.terraform.io/hashicorp/aws\"\n  msg = \"Must use official AWS provider\"\n}\n\n# Block if secrets are missing\ndeny_missing_secrets[msg] {\n  not data.vault_secrets\n  msg = \"Required secrets not available in Vault\"\n}\n\n# GitHub Actions CI gate\nname: Terraform Plan Validator\non:\n  pull_request:\n    paths:\n      - '**.tf'\n      - 'environments/**'\n\njobs:\n  terraform-validate:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n      - uses: hashicorp/setup-terraform@v3\n        with:\n          terraform_version: ${{ vars.TERRAFORM_VERSION }}\n      - name: Verify Vault secrets\n        run: |\n          for env in dev staging prod; do\n            for secret in ${{ secrets.REQUIRED_SECRETS }}; do\n              if ! vault kv get -field=value secret/$env/$secret 2>/dev/null; then\n                echo \"Missing secret: $secret for $env\"\n                exit 1\n              fi\n            done\n          done\n      - name: Check provider changes\n        run: |\n          old_providers=$(git show ${{ github.base_ref }}:versions.tf 2>/dev/null | grep -A20 required_providers || echo \"\")\n          new_providers=$(cat versions.tf | grep -A20 required_providers)\n          if [ \"$old_providers\" != \"$new_providers\" ]; then\n            echo \"Provider changes detected - ensure compatibility matrix is updated\"\n          fi\n      - name: Terraform Init\n        run: terraform init -backend-config=environments/${{ matrix.environment }}/backend.hcl\n      - name: Terraform Plan\n        run: terraform plan -out=tfplan -var-file=environments/${{ matrix.environment }}/prod.tfvars\n        env:\n          VAULT_TOKEN: ${{ secrets.VAULT_TOKEN }}\n      - name: Validate Plan\n        run: |\n          terraform show -json tfplan | jq '.resource_changes | length' > resources.json\n          echo \"Plan contains $(cat resources.json) resource changes\"\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "terraform-q-6",
        "questionId": "q-8757",
        "question": "In a multi-repo Terraform setup, a core-network repo exposes VPC CIDR and security groups via remote state. The app repo should consume these outputs only (read-only) and not modify core-network. Propose an implementation: (1) minimal remote_state data usage to pull vpc_cidr and sg_ids, (2) how to pass into an app_vpc module, (3) a CI gate that fails if the plan includes changes to core-network or VPC CIDR. Include minimal code sketches and a sample GitHub Actions gate?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a data \\\"terraform_remote_state\\\" block in the app, e.g. data \\\"terraform_remote_state\\\" \\\"core\\\" { backend = \\\"s3\\\" config = { bucket = \\\"tf-core-network\\\" key = \\\"prod/terraform.tfstate\\\" region",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Infrastructure Drift occurs when actual infrastructure state diverges from the desired state defined in code, typically due to manual changes or concurrent modifications.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Terratest unit test for the shared VPC module. The test spins up the module in a temp directory, passes a CIDR like 10.1.0.0/16, and asserts that vpc_id output is non-empty and that at lea",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Gate design: run terraform plan -out=plan.tfplan; terraform show -json plan.tfplan > plan.json. Implement gate.py to load environments/*/*.tfvars, scan for secret-like keys with regex (?i)(password|se",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to compose across repos and enforce read-only remotes. It checks multi-repo data sharing, and CI policy gates.\n\n## Key Concepts\n- data.terraform_remote_state usage\n- module input wiring, outputs isolation\n- CI gate enforcing drift/modify limits across remotes\n\n## Code Example\n```terraform\n# app main\ndata \"terraform_remote_state\" \"core\" {\n  backend = \"s3\"\n  config = {\n    bucket = \"tf-core-network\"\n    key    = \"prod/terraform.tfstate\"\n    region = \"eu-west-1\"\n  }\n}\n\nmodule \"app_vpc\" {\n  source   = \"../../modules/app_vpc\"\n  vpc_cidr = data.terraform_remote_state.core.outputs.vpc_cidr\n}\n```\n\n```yaml\n# CI gate snippet (GitHub Actions)\n- name: Plan app only\n- run: terraform plan -out=plan.out -target=module.app\n- run: terraform show -json plan.out > plan.json\n- run: |\n    if jq '.resource_changes[].address' plan.json | grep -qE 'core|aws_.*$'; then\n      echo \"Core-network changes detected\"; exit 1; fi\n```\n\n## Follow-up Questions\n- How would you test in Terratest that read-only remote state is prevented from drift?\n- How would you handle secret-backed outputs?",
        "difficulty": "advanced"
      },
      {
        "id": "terraform-q-7",
        "questionId": "q-25030",
        "question": "Context: terraform/general; Difficulty: beginner; Scenario: A mono-repo provisions an AWS VPC via a shared module across environments dev, stage, prod. Implement per-environment CIDR selection by reading ip_allocations.json with Terraform's external data source, derive a local map, and use for_each to create subnets per environment using that environment’s CIDR. Add a wrapper module injecting Environment and CostCenter tags, plus a per-env backend in S3. Provide minimal code references for: 1) ip_allocations.json, 2) external data source + locals + subnet module usage, 3) wrapper module and backend.tf?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a local backend for development and switch to an S3 backend with DynamoDB locking for prod. Create a small storage module that provisions the bucket (versioning, basic lifecycle) and a separate lo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline concrete approach: Each tenant uses a dedicated S3 bucket with SSE-KMS and a key derived from tenant name; backend.tf maps tenant/env to bucket/key. A single shared core module with a tenants/",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a governance repo exporting monthly budgets per env. In each env repo, fetch budgets with data.terraform_remote_state and estimate planned_cost from resource costs or a standard cost metric.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single shared VPC module, read ip_allocations.json via Terraform's external data source, convert it to a local map env_cidr, then use for_each over [dev, stage, prod] to create subnets per environment using env_ci…",
            "isCorrect": true
          }
        ],
        "explanation": "This question validates the ability to compose modules with environment-driven data while avoiding duplication and maintaining clean state separation. It covers essential skills including Terraform's external data source and locals for environment-specific configuration, for_each to create per-environment resources from a map, wrapper modules for consistent tagging (Environment, CostCenter), per-environment backend configuration for isolated state, and CI gates with fmt/validate/tflint.\n\n## Code Example\n```terraform\n# ip_allocations.json\n{\n  \"dev\": \"10.0.0.0/16\",\n  \"stage\": \"10.1.0.0/16\",\n  \"prod\": \"10.2.0.0/16\"\n}\n```\n\n```terraform\n# main.tf (external data source + locals + subnet module usage)\ndata \"external\" \"ip_allocations\" {\n  program = [\"python3\", \"${path.module}/read_ip_allocations.py\"]\n}\n\nlocals {\n  env_cidr = jsondecode(data.external.ip_allocations.result)\n}\n\nmodule \"subnets\" {\n  source   = \"./modules/subnets\"\n  for_each = toset([\"dev\", \"stage\", \"prod\"])\n\n  vpc_id     = module.vpc.vpc_id\n  cidr_block = local.env_cidr[each.key]\n  environment = each.key\n}\n```\n\n```terraform\n# wrapper module (tags)\n# modules/vpc-wrapper/main.tf\nlocals {\n  default_tags = {\n    Environment = var.environment\n    CostCenter  = var.cost_center\n  }\n}\n\nmodule \"vpc\" {\n  source = \"../shared-vpc\"\n\n  tags = merge(local.default_tags, var.extra_tags)\n}\n```\n\n```terraform\n# backend.tf (per-environment)\nterraform {\n  backend \"s3\" {\n    key = \"${var.environment}/terraform.tfstate\"\n  }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "terraform-q-8",
        "questionId": "q-11051",
        "question": "Design a gating pattern to prevent security group rule bloat across Terraform environments. Create a central policy that caps per-SG rule count and disallows 0.0.0.0/0 for non-whitelisted ports, validated across dev and prod via a CI gate that analyzes plan JSON. Show a minimal Terraform snippet to expose SG IDs and a gating script and an OPA policy skeleton. How would you implement this end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan approach: 1) Wrapper module injects Environment and CostCenter tags via a shared tags map. 2) Per-env backend in one S3 bucket with key 'analytics/${var.environment}/terraform.tfstate'. 3) Provid",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a gating workflow that leverages a central policy data source to analyze Terraform plan-JSON output, automatically rejecting any plan that creates or expands Snowflake or Databricks shares to PUBLIC or externa…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a comprehensive governance solution by generating Terraform plan JSON across all environments, parsing security group configurations, and enforcing centralized quotas and CIDR restrictions through automated CI…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-tenant provider aliases and a per-tenant backend in a shared GCS bucket, drive module instances with for_each over a tenants map, isolate IAM bindings per tenant, and enforce prod-safety via a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis evaluates your ability to design enterprise-grade infrastructure governance that spans multiple environments, demonstrating practical implementation of policy-as-code, automated compliance validation, and cross-repository coordination.\n\n## Key Concepts\n- Terraform plan JSON parsing and analysis\n- Centralized policy management with OPA\n- Security group quota enforcement\n- CIDR whitelist validation\n- CI/CD automated compliance gates\n- Cross-environment policy consistency\n\n## Code Example\n```javascript\n// Example snippet: gating script implementation\nconst plan = JSON.parse(fs.readFileSync('terraform-plan.json'));\nconst sgRules = plan.configuration.root_module.resources\n  .filter(r => r.type === 'aws_security_group_rule');\n\n// Enforce per-SG rule limits\nconst sgRuleCounts = {};\nsgRules.forEach(rule => {\n  const sgId = rule.values.security_group_id;\n  sgRuleCounts[sgId] = (sgRuleCounts[sgId] || 0) + 1;\n});\n\n// Check for 0.0.0.0/0 on non-whitelisted ports\nconst restrictedPorts = [22, 3389, 3306];\nconst violations = sgRules.filter(rule => \n  rule.values.cidr_blocks?.includes('0.0.0.0/0') &&\n  restrictedPorts.includes(rule.values.from_port)\n);\n```\n\n## Implementation Approach\nThe solution combines Terraform's JSON output format with OPA's policy engine to create a centralized compliance gate that validates security group configurations against organizational standards before deployment.",
        "difficulty": "intermediate"
      },
      {
        "id": "terraform-q-9",
        "questionId": "q-17859",
        "question": "Design a Terraform-driven multi-tenant onboarding flow using a shared, versioned module registry. Each tenant must deploy in a separate AWS account with per-tenant remote state and isolated IAM scopes, while a common Networking+Cluster module handles VPCs and EKS. Include provider aliases, for_each tenancy, per-tenant backends, policy checks (Sentinel/OPA), and an automated Terratest/kitchen-terraform validation that proves first apply creates resources and second apply is a no-op?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a wrapper module that injects Environment and CostCenter tags into all resources via a merged tags map. Configure a single S3 backend with key prefix per environment (e.g., terraform-dev/ter",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Terraform setup where each tenant deploys to their own AWS account using a shared, versioned module from a private registry. For each tenant, configure an AWS provider alias that assumes a cross-account IAM rol…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Configure qa backend.tfvars with bucket tf-state-bucket, key qa/terraform.tfstate. Extend the wrapper to set Environment='qa' and CostCenter; wire qa/main.tf to instantiate the shared module with qa t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Pin module refs and provider versions per environment via a policy JSON. Gate CI to init/plan and fail if the module git ref or provider version drifts from the allowed map. Include minimal: 1) Private module block with…",
            "isCorrect": false
          }
        ],
        "explanation": "## Architecture Overview\n\n### 1. Module Registry Structure\n```hcl\n# Caller configuration (each tenant)\nmodule \"networking_cluster\" {\n  source  = \"registry.example.com/networking-cluster/aws\"\n  version = \"1.2.0\"\n  \n  tenant_name = \"acme-corp\"\n  environment = \"production\"\n  cidr_block  = \"10.0.0.0/16\"\n}\n```\n- Module source uses full registry path with version pin (@1.2.0)\n- Semantic versioning with ~> allows 1.2.x updates\n- Module encapsulates VPC, subnets, NAT gateways, and EKS cluster\n\n### 2. for_each Tenant Configuration\n```hcl\n# variables.tf\nvariable \"tenants\" {\n  type = map(object({\n    account_id     = string\n    role_name      = string\n    region         = string\n    vpc_cidr       = string\n    cluster_version = string\n  }))\n}\n\n# main.tf\nprovider \"aws\" {\n  alias = \"tenant\"\n  assume_role {\n    role_arn = \"arn:aws:iam::${tenant.account_id}:role/${tenant.role_name}\"\n  }\n}\n\nresource \"aws_s3_bucket\" \"tenant_state\" {\n  for_each = var.tenants\n  bucket   = \"${each.key}-terraform-state\"\n}\n```\n\n### 3. Per-Tenant Remote State Backends\n```hcl\n# backends.tf\nterraform {\n  backend \"s3\" {\n    bucket         = \"company-terraform-state\"\n    key            = \"tenants/${local.tenant_key}/terraform.tfstate\"\n    region         = \"us-east-1\"\n    dynamodb_table = \"terraform-locks\"\n    encrypt        = true\n  }\n}\n```\n- Each tenant state stored at unique S3 key path\n- DynamoDB table with partition key (LockID) prevents concurrent applies\n- State encryption at rest enabled\n\n### 4. Provider Aliases for Cross-Account Access\n```hcl\nprovider \"aws\" {\n  alias = each.value.account_id\n  assume_role {\n    role_arn = \"arn:aws:iam::${each.value.account_id}:role/TenantAdmin\"\n  }\n}\n```\n\n### 5. Security Policy Enforcement (OPA/Sentinel)\n```rego\n# opa/iam_least_privilege.rego\npackage terraform.iam\n\ndeny[msg] {\n  input.resource_changes[_].type == \"aws_iam_role\"\n  input.resource_changes[_].change.after.managed_policy_arns != []\n  msg := \"Tenants must use inline policies, not managed ARNs\"\n}\n```\n- Policies validate tags, naming conventions, IAM boundaries\n- CI pipeline gates deployments on policy pass/fail\n- Prevents overprivileged roles and misconfigured VPCs\n\n### 6. Terratest Validation\n```go\nfunc TestTenantOnboarding(t *testing.T) {\n    // First apply creates resources\n    terraform.InitAndApply(t, terraformOptions)\n    \n    // Verify EKS cluster exists\n    eks := aws.GetEksCluster(t, \"test-cluster\", \"us-east-1\")\n    assert.Equal(t, \"ACTIVE\", *eks.Status)\n    \n    // Second apply is idempotent no-op\n    output := terraform.Apply(t, terraformOptions)\n    assert.Contains(t, output, \"No changes. Infrastructure is up-to-date.\")\n}\n```\n- Proves first apply creates resources\n- Asserts second apply produces zero changes (idempotency)\n- Validates EKS cluster status and resource existence",
        "difficulty": "advanced"
      },
      {
        "id": "terraform-q-10",
        "questionId": "q-27564",
        "question": "Context: terraform/general; Difficulty: beginner; Scenario: In a mono-repo, add a wrapper module that provisions multiple S3 buckets across environments using a map-based configuration. The wrapper should accept env_buckets = map(object{ name_prefix string; versioning bool; encryption bool }), and use for_each to create one bucket per env. Bucket name derived as '${each.key}-${each.value.name_prefix}'. Conditionally enable versioning and SSE based on flags. Provide minimal code references for: 1) wrapper module definition, 2) environments/<ENV>.tfvars, 3) GitHub Actions steps to fmt, validate, and plan per env, failing if prod would change?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a provider-agnostic module with tenants[]. Each tenant uses a dedicated backend key, per-tenant IAM roles, and provider aliases for AWS and GCP. Enforce security defaults via a policy gate (Sen",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a monorepo with per-environment backends (env/dev/backend.tf, env/stage/backend.tf, env/prod/backend.tf) and provider aliases for AWS and GCP resources. Share common modules while maintaining state isolation, and…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe a wrapper module that accepts env_buckets = map(object{ name_prefix string; versioning bool; encryption bool }), and uses for_each to create one aws_s3_bucket per env. Bucket name derived as",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "I would place per-environment flags in env_flags.yaml, read them using yamldecode(file(\"path/to/env_flags.yaml\")), derive enable_logging dynamically from terraform.workspace, and pass the flag to the VPC module to contr…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design modular, environment-scoped infrastructure with a simple map-driven pattern, a common practice at scale.\n\n## Key Concepts\n- Terraform modules and for_each with maps\n- Conditional resource configuration via variables\n- Environment-specific variable files and CI gates\n- Minimal code references for quick in-repo patterns\n\n## Code Example\n```terraform\n# wrapper module\nvariable \"env_buckets\" { type = map(object{ name_prefix string; versioning bool; encryption bool }) }\nresource \"aws_s3_bucket\" \"env\" {\n  for_each = var.env_buckets\n  bucket   = \"${each.key}-${each.value.name_prefix}\"\n  acl      = \"private\"\n  versioning {\n    enabled = each.value.versioning\n  }\n  dynamic \"server_side_encryption_configuration\" {\n    for_each = each.value.encryption ? [1] : []\n    content {\n      rule { apply_server_side_encryption_by_default { sse_algorithm = \"AES256\" } }\n    }\n  }\n}\n```\n\n## Follow-up Questions\n- How would you validate env_buckets inputs in CI before plan?\n- How would you handle bucket naming collisions across environments?",
        "difficulty": "beginner"
      },
      {
        "id": "terraform-q-11",
        "questionId": "q-27009",
        "question": "Context: terraform/general; Difficulty: advanced; Topics: terraform; Scenario: A SaaS platform provides per-tenant data planes with primary in AWS and disaster-recovery targets in GCP, all using a shared, versioned Terraform module registry. Design a Terraform layout that (1) uses per-tenant state backends and provider aliases for cross-cloud resources, (2) enforces per-tenant DR readiness flags and retention policies via a policy-as-code layer, and (3) includes an automated Terratest (or kitchen-terraform) harness that validates the primary plan creates resources and the DR plan is a no-op when failover is not required. Provide minimal scaffolding?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Terraform strategy for per-tenant data stores via a shared module registry. Drive it with a tenants map to create provider aliases and backend keys like tenants/<name>/terraform.tfstate. Give",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a wrapper module that reads policy.json via data external and builds a per-env enabled map. Then apply for_each on aws_nat_gateway and aws_flow_log resources so they only deploy when policy[env].e",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a single root that calls two submodules: aws-network and azure-network, selected by a top-level var cloud with allowed values aws|azure. Define a shared module interface: input map[string]string f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a shared module registry with per-tenant state backends (S3 for AWS, GCS for GCP) and provider aliases for cross-cloud resources (aws.primary, gcp.dr). Implement a DR submodule that mirrors critical assets to GCP, g…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design cross-cloud disaster recovery with strict tenant isolation, governance enforcement, and automated validation—a realistic, high-stakes enterprise scenario.\n\n## Key Concepts\n- Cross-cloud provider aliasing and per-tenant state backends\n- DR readiness flags and retention policies as code\n- Shared module registry usage for multi-tenant DR fabric\n- Automated testing with Terratest or kitchen-terraform\n\n## Code Example\n```terraform\n# Minimal skeleton showing per-tenant aliasing\nprovider \"aws\" {\n  alias  = \"primary\"\n  region = \"us-east-1\"\n}\nprovider \"google\" {\n  alias   = \"dr\"\n  project = var.dr_project\n}\n\n# Per-tenant state backend\nterraform {\n  backend \"s3\" {\n    bucket = \"tenant-${var.tenant_id}-state\"\n    key    = \"primary/terraform.tfstate\"\n    region = \"us-east-1\"\n  }\n}\n\n# DR readiness flag\nvariable \"dr_enabled\" {\n  type    = bool\n  default = false\n}\n\n# DR submodule mirrors critical resources to GCP when enabled\nmodule \"dr_replication\" {\n  source  = \"../modules/dr-replication\"\n  count   = var.dr_enabled ? 1 : 0\n  providers = {\n    aws.primary = aws.primary\n    google.dr   = google.dr\n  }\n}\n```\n\n## Testing Strategy\n```go\nfunc TestPrimaryCreatesResources(t *testing.T) {\n    terraformOptions := &terraform.Options{\n        TerraformDir: \"../fixtures/primary\",\n    }\n    defer terraform.Destroy(t, terraformOptions)\n    terraform.InitAndApply(t, terraformOptions)\n\n    assertResourceCount(t, terraform.OutputRequired(t, terraformOptions, \"resource_ids\"), 1)\n}\n\nfunc TestDRNoOpWhenDisabled(t *testing.T) {\n    terraformOptions := &terraform.Options{\n        TerraformDir: \"../fixtures/dr\",\n        Vars:          map[string]interface{}{\"dr_enabled\": false},\n    }\n    plan := terraform.Plan(t, terraformOptions)\n    assertEmptyPlan(t, plan)\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "terraform-q-12",
        "questionId": "q-18536",
        "question": "**Context**: terraform/general; **Difficulty**: intermediate; **Topic**: terraform; **Scenario**: A company maintains a single Terraform root that provisions multi-cloud resources (AWS and Azure) via a shared module. You want per-tenant feature flags that gate plan/apply across environments. Design a practical approach: (1) per-tenant flags.json stored in the repo and loaded at plan time, (2) a module interface that consumes these flags to enable/disable features, (3) an OPA policy to block prod changes unless tenant.prodEnabled is true and a prodBypass annotation exists, (4) a GitHub Actions gate that runs plan for all tenants and fails if any prod feature is touched without explicit approval. Provide skeletons for: 1) tenant-flags.json, 2) module interface changes, 3) policy snippet, 4) workflow gate?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a single Snowflake-focused Terraform module with per-tenant provider aliases (snowflake.tenantA, snowflake.tenantB) and per-tenant backend keys. Retrieve credentials from Vault; scope roles, wareh",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant flags.json checked in the repo and a pre-plan script that injects a -var-file for each tenant. The shared module reads these flags to gate features across AWS and Azure. Enforce an OPA",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a centralized Terraform pattern using provider aliases for each AWS region (aws.us-east-1, aws.us-west-2, etc.), utilizing for_each to iterate across regions with per-environment backends keyed by environment. Th…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cross-cloud object store module driven by var cloud and a single interface: bucket_name, versioning, public_read, environment. Use provider aliases (aws, google, azurerm) and a per-cloud reso",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to implement cross-cloud, multi-tenant gating with feature flags, policy-as-code, and CI gates. It evaluates how to keep tenants isolated while preserving a single code path across AWS and Azure.\n\n## Key Concepts\n\n- Per-tenant feature flags loaded at plan time\n- Shared module interface that consumes flags to enable/disable features\n- Policy-as-code using OPA to guard prod changes\n- GH Actions gate validating plans for all tenants\n\n## Code Example\n\n```json\n// tenant-flags.json\n{\n  \"tenantId\": \"tenant-a\",\n  \"prodEnabled\": false,\n  \"features\": {\n    \"advancedNetworking\": true\n  }\n}\n```\n\n```rego\n# policy snippet (opa.tfguard)\npackage tf.guard\n\ndefault allow = false\n\nallow {\n  input.plan_is_valid\n  input.tenant.prodEnabled\n  input.tenant.annotations[_] == \"prodBypass\"\n}\n```\n\n## Follow-up Questions\n\n- How would you test this gating in a PR workflow and simulate flag toggles safely?\n- What are the risks if tenant-flags.json drifts from actual env names, and how would you mitigate it?",
        "difficulty": "intermediate"
      },
      {
        "id": "terraform-q-13",
        "questionId": "q-23279",
        "question": "Context: terraform/general; Difficulty: beginner; Scenario: A mono-repo provisions a storage bucket via a shared module. Add per-environment input to configure bucket_name and public_read flag. Implement a wrapper module injecting Environment and CostCenter tags, and a CI gate that runs fmt, validate, and plan for all envs, failing if non-prod buckets would be created with public_read = true. Provide minimal code references for: 1) wrapper module, 2) bucket module inputs, 3) workflow steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run terraform plan -out=plan.tfplan; terraform show -json plan.tfplan > plan.json; use jq to scan plan.json for resource_changes that match prod-critical resources (e.g., aws_iam_role, aws_security_gr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: implement a lightweight env_tags wrapper module that merges global tags with Environment and CostCenter; configure backend with per-environment state keys (dev.tfstate, staging.tfstate, prod.tfstate); create GitHu…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a wrapper module that injects Environment and CostCenter tags and computes bucket_name as '${var.environment}-data-${var.bucket_suffix}'. The shared storage module expects bucket_name (string) a",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a wrapper module that injects Environment and CostCenter tags into all resources via a merged tags map. Configure a single S3 backend with key prefix per environment (e.g., terraform-dev/ter",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical TF mastery around modules, inputs, and CI gates without duplicating existing coverage. It emphasizes safe defaults and per-environment governance.\n\n## Key Concepts\n- Terraform module composition with wrapper modules\n- Input validation and defaults for storage resources\n- CI gates using terraform fmt/validate/plan and plan JSON parsing\n\n## Code Example\n```mermaid\nflowchart TD\n  A[Wrapper Module] --> B[Env Prefix & Tags]\n  B --> C[Shared Storage Module]\n  C --> D[CI Gate: fmt/validate/plan]\n  D --> E[Apply]\n```\n\n## Follow-up Questions\n- How would you extend to enforce encryption at rest for all buckets?\n- How would you test the gate against a fake plan in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "terraform-q-14",
        "questionId": "q-3638",
        "question": "You're deploying a small fleet of EC2 instances into an existing AWS VPC that is not Terraform-managed. Describe a beginner-friendly plan to reference the VPC and its subnets via data sources, place the resources under a stable module boundary, and enable per-environment isolation with a single backend. Include concrete data source usage, a minimal wrapper module, and a CI gate that only allows changes to compute resources, failing if the plan touches VPC or subnets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Terraform strategy for per-tenant data stores via a shared module registry. Drive it with a tenants map to create provider aliases and backend keys like tenants/<name>/terraform.tfstate. Give",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Pins per-tenant plan outcomes through a YAML lockfile that records: tenant, environment, provider versions, module refs, and plan hash. Gate script runs terraform plan -out=plan.tfplan, converts to JS",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a per-env feature-flag stored in a central git-backed JSON. Use data external to pull the flag as a bool flag_enabled. Gate the regional failover resource with a conditional: count = flag_enab",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use `data \"aws_vpc\"` and `data \"aws_subnet\"` to fetch existing resources by ID or filters, then reference those in a dedicated module that provisions EC2 instances. Wrap with a simple environment module (dev/prod) and s…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests data-source familiarity, module boundaries, and a pragmatic gating strategy. It emphasizes using existing infrastructure safely while enabling basic per-environment state separation with one backend. It checks the ability to define scope, enforce drift guards in CI, and keep changes to compute resources isolated.\n\n## Key Concepts\n- Terraform data sources (aws_vpc, aws_subnet)\n- Module boundaries and wrappers\n- Backend per-environment isolation\n- CI gating on plan outputs\n- Drift avoidance for non-managed resources\n\n## Code Example\n```hcl\ndata \"aws_vpc\" \"existing\" {\n  id = \"vpc-12345678\"\n}\n\ndata \"aws_subnet\" \"private\" {\n  vpc_id = data.aws_vpc.existing.id\n  filter {\n    name   = \"tag:Environment\"\n    values = [\"private\"]\n  }\n}\n\nmodule \"compute\" {\n  source = \"./modules/compute\"\n  vpc_id  = data.aws_vpc.existing.id\n  subnet_ids = data.aws_subnet.private[*].id\n  instance_type = \"t3.micro\"\n}\n```\n\n## CI Gate Example\n```bash\n#!/bin/bash\nterraform plan -out=tfplan\ntfplan_json=$(terraform show -json tfplan)\n\n# Fail if VPC or subnet changes detected\nif echo \"$tfplan_json\" | jq -e '.resource_changes[] | select(.type == \"aws_vpc\" or .type == \"aws_subnet\")' > /dev/null; then\n  echo \"ERROR: VPC or subnet changes detected - not allowed\"\n  exit 1\nfi\n\necho \"Plan contains only compute resource changes - OK\"\n```",
        "difficulty": "beginner"
      },
      {
        "id": "terraform-q-15",
        "questionId": "q-28326",
        "question": "Context: terraform/general; Difficulty: advanced; Topics: terraform; Scenario: In a single EKS cluster, provision per-tenant namespaces, RBAC (RoleBindings), and a per-tenant ConfigMap of feature flags via Terraform. Use provider aliases and per-tenant backends. Include an external data source to fetch flags from an internal API with fallback, plus a per-tenant S3 audit log sink. Enforce idempotence, drift detection, and prod RBAC gates. Minimal scaffolding?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-env backends (lake-dev, lake-stage, lake-prod) with a central shared state bucket for the Glue catalog. In lake-ingest, fetch via data.terraform_remote_state. Gate: run terraform init && terra",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Terraform setup where each tenant deploys to their own AWS account using a shared, versioned module from a private registry. For each tenant, configure an AWS provider alias that assumes a cross-account IAM rol…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Read the central CIDR registry via data.terraform_remote_state from the network repo, then allocate tenant CIDRs with explicit constraints and materialize them in per-tenant vars. Enforce via an OPA/G",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Provision per-tenant Kubernetes isolation within a single EKS cluster by creating dedicated Namespaces and ServiceAccounts for each tenant. Configure tenant-scoped RoleBindings to enforce RBAC policies and create per-te…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses a candidate's ability to design multi-tenant Kubernetes infrastructure with Terraform within a shared cluster. It evaluates competency across per-tenant isolation, provider aliasing, backend state management, external data source integration, policy gates, and drift and idempotence verification.\n\n## Key Concepts\n\n- Terraform provider aliasing for multi-tenant Kubernetes configurations\n- Per-tenant backends and state isolation\n- External data sources for dynamic configuration with graceful fallbacks\n- Kubernetes RBAC and ConfigMap management per tenant",
        "difficulty": "advanced"
      },
      {
        "id": "terraform-q-16",
        "questionId": "q-9310",
        "question": "Design an advanced Terraform architecture to provision two AWS regions and one Azure region in a single repository. Include: 1) provider aliasing and a wrapper module that enforces mandatory Environment and CostCenter tags, 2) per-env workspaces with a shared backend across clouds, 3) a Sentinel (or OPA) policy enforcing no plaintext secrets in state and requiring approvals for non-prod changes, 4) a drift-detection workflow that runs plan in a separate drift environment and fails if drift exists, 5) a pragmatic testing plan using Terratest or kitchen-terraform to simulate drift and policy violations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Terraform solution in a single monorepo using a provider-agnostic networking module and a shared module registry. Implement per-tenant remote backends in Terraform Cloud with unique state key",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a wrapper module that accepts env and applies standard tags Environment and CostCenter to all resources. Allocate 3 subnets (public, private, isolated) across 2 AZs with for_each over a CIDR",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Terraform-driven DR pattern that mirrors per-tenant resources to a secondary region. Explain module layout, how to isolate per-tenant/state, and how CI gates validate a DR plan targets only r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use AWS and Azure providers with region-specific aliases, a wrapper module that injects Environment and CostCenter tags into all resources, and a single repository layout with per-environment workspac",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical multi-cloud Terraform design, policy-as-code, drift handling, and test automation at scale—core for Oracle, Tesla, and LinkedIn-like environments.\n\n## Key Concepts\n\n- Provider aliasing for multi-region/multi-cloud setups\n- Wrapper modules to standardize tagging and policy constraints\n- Terraform Cloud/Sentinel or OPA for governance\n- Drift detection workflows and isolated drift environments\n- End-to-end testing with Terratest or kitchen-terraform\n\n## Code Example\n\n```hcl\n# Wrapper module injecting standard tags\nvariable \"environment\" { type = string }\nvariable \"cost_center\" { type = string }\nvariable \"tags\" { type = map(string) }\n\nmodule \"service\" {\n  source = \"../aws-azure-base\"\n  providers = {\n    aws   = aws.us_east_1\n    aws_eu = aws.eu_west_1\n    azurerm = azurerm.westus2\n  }\n  tags = merge(var.tags, {\n    Environment = var.environment\n    CostCenter  = var.cost_center\n  })\n}\n```\n\n## Follow-up Questions\n\n- How would you structure a Sentinel policy to enforce tag requirements and deny secrets in state?\n- How would you simulate drift in tests and verify policy compliance across clouds?",
        "difficulty": "advanced"
      },
      {
        "id": "terraform-q-17",
        "questionId": "q-21210",
        "question": "Context: terraform/general; Difficulty: beginner; Scenario: Extend an existing VPC provisioning repo that uses a shared module. New angle: introduce environment-specific configuration via a data source (AWS SSM Parameter Store or S3) supplying per-env rules, and a wrapper module that consumes these rules to populate security group ingress limits. Add a CI gate that runs 'terraform plan' for dev and validates that only allowed ingress rules are planned, while plans for prod are blocked if prod resources would be touched. Provide minimal code references for: 1) wrapper module consuming the data source, 2) the data source definition, 3) the gate script that enforces the policy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To implement this, fetch per-env policy from AWS SSM Parameter Store using data \"aws_ssm_parameter\"; jsondecode its value to derive allowed_ingress CIDRs; pass these to the wrapper module via a variab",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a read-only policy bridge via data.terraform_remote_state to pull an allowed-changes map from a separate policies repo. In the services repo, add a small policy/validate_changes module that compar",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I’d implement a small S3 bucket pattern: 1) module 'modules/s3-bucket' with inputs bucket_name, enable_versioning, kms_key_arn, tags; 2) wrapper 'environments/env' injecting env and owner tags and der",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Pin a versioned network module in the private registry and expose read-only outputs; use per-environment backends (dev/stg/prod) with distinct keys; consume network data in app and observability repos",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to introduce data-driven per-environment configuration and enforce it via CI gates, using beginner-friendly Terraform patterns.\n\n## Key Concepts\n- data sources (aws_ssm_parameter or s3 data)\n- jsondecode to turn JSON into Terraform vars\n- wrapper module consuming external policy\n- for_each over security group ingress rules\n- plan-based policy gates in CI\n\n## Code Example\n```terraform\ndata \"aws_ssm_parameter\" \"env_rules\" {\n  name = \"/terraform/env/${var.environment}/rules\"\n}\nlocals {\n  rules = jsondecode(data.aws_ssm_parameter.env_rules.value)\n}\n```\n\n```terraform\nmodule \"vpc_wrapper\" {\n  source        = \"./modules/wrapper\"\n  environment   = var.environment\n  ingress_rules = local.rules.ingress\n}\n```\n\n## Follow-up Questions\n- How would you test the gate with dev changes that attempt to modify prod resources?\n- What are the risks of relying on SSM Parameter at plan time and how would you mitigate them?",
        "difficulty": "beginner"
      },
      {
        "id": "terraform-q-18",
        "questionId": "q-21399",
        "question": "Context: terraform/general; Difficulty: intermediate; Topic: terraform; Scenario: In a monorepo, a shared network module provisions VPCs/VPNs in AWS and GCP for multiple tenants. Each tenant uses per-env Terraform Cloud workspaces with per-tenant backends. Implement a lockfile-based plan gate that records exact provider and module versions used by each plan and blocks upgrades or unknown module versions. Provide skeletons for: 1) lockfile schema, 2) gate script, 3) GitHub Action gate, 4) backend/module references?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pins per-tenant plan outcomes through a YAML lockfile that records: tenant, environment, provider versions, module refs, and plan hash. Gate script runs terraform plan -out=plan.tfplan, converts to JS",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Snapshot the current trust_policy via data.terraform_remote_state; run terraform plan -out=plan.tfplan and terraform show -json plan.tfplan; implement a minimal OPA rego policy that rejects any princi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Terraform Cloud with a Sentinel policy that blocks plans altering remote_state resources (VPC/Subnets/SG). In CI, run plan to JSON, feed to Sentinel, and fail on disallowed changes. Allow onl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a tenants map to generate per-tenant provider aliases and backends. Retrieve tenant firewall rules via an external data source (calling the REST policy service) keyed by tenant_id, feeding per-tenant variables into…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to enforce reproducibility and governance across tenants while handling multi-cloud modules.\n\n## Key Concepts\n- Terraform lockfiles for providers/modules\n- plan gates via CLI + JSON diff\n- per-tenant backends and workspaces\n- GitHub Actions integration\n\n## Code Example\n```yaml\n# lockfile.yaml (example)\ntenant: acme\nenvironment: dev\nproviders:\n  aws: 3.50.0\n  google: 4.78.0\nmodules:\n  network: v1.2.3\nplan_hash: sha256:...\n```\n\n```bash\n# gate script sketch (bash)\nPLAN_OUT=plan.tfplan\nterraform plan -out=$PLAN_OUT -input=false\nPLAN_JSON=$(terraform show -json $PLAN_OUT)\n# compare with lockfile logic\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "terraform-q-19",
        "questionId": "q-24162",
        "question": "Context: terraform/general; Difficulty: intermediate; Scenario: In a single monorepo, implement a cross-cloud baseline (AWS and GCP) using a shared networking module that provisions VPC/VNet, private DNS, and interconnects with per-environment backends. Requirements: (1) environment-scoped backends with unique keys; (2) a provider-agnostic networking module with strict tagging and consistent naming; (3) per-environment IAM bindings enforced via policy-as-code (OPA/Sentinel); (4) a CI gate that blocks plans touching prod resources unless a PROD flag is set and a code-review approval is present. Provide skeletons for module layout, backend.tfvars, policy snippet, and a GitHub Actions gate?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a wrapper module that accepts environment, project, and region variables. The wrapper passes these to a child module that creates an S3 bucket with SSE encryption (AES256) and a CloudWatch log group, both named w…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a centralized Terraform pattern using provider aliases for each AWS region (aws.us-east-1, aws.us-west-2, etc.), utilizing for_each to iterate across regions with per-environment backends keyed by environment. Th…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a wrapper module that loads rules.yaml with yamldecode(file(\"rules.yaml\")) and uses for_each to create aws_security_group_rule resources for a target sg_id. The GitHub Action runs terraform",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a root module with a single input 'target_cloud' to select AWS or GCP via provider aliases. Create a shared 'networking' module that provisions VPC/VNet, private DNS, and peering using environment",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design cross-cloud multi-env baselines with centralized governance using Terraform core concepts and policy-as-code.\n\n## Key Concepts\n- Cross-cloud provider aliasing and a single root module\n- Reusable networking module with strict, consistent tagging\n- Per-env remote backends keyed by workspace/environment\n- Policy-as-code enforcement (OPA/Sentinel) for IAM tagging and env constraints\n- CI gate logic for prod safeguards (flag + review)\n\n## Code Example\n```hcl\n# root/main.tf\nvariable \"target_cloud\" { type = string }\n\nprovider \"aws\" { alias = \"aws\" region = var.aws_region }\nprovider \"google\" { alias = \"gcp\" project = var.gcp_project }\n\nmodule \"networking\" {\n  source = \"./modules/networking\"\n  providers = {\n    aws = aws.aws\n    google = google.gcp\n  }\n  environment = var.environment\n  target_cloud = var.target_cloud\n}\n```\n\n```hcl\n# modules/networking/variables.tf\nvariable \"environment\" { type = string }\nvariable \"target_cloud\" { type = string }\n```\n\n```json\n# policy snippet (OPA/Sentinel-like)\npackage terraform.networking\n\ndefault allow = false\n\nviolation[\"tier\"] {\n  input.environment == \"prod\" && input.resource.tags[\"Env\"] != \"prod\"\n}\n```\n\n## Follow-up Questions\n- How would you handle drift between AWS and GCP networking configurations?\n- How would you extend gating to multiple prod regions/accounts and audit logs?",
        "difficulty": "intermediate"
      },
      {
        "id": "terraform-q-20",
        "questionId": "q-18091",
        "question": "In a multi-tenant SaaS, design a Terraform-driven DR pattern that mirrors per-tenant resources to a secondary region. Explain module layout, how to isolate per-tenant/state, and how CI gates validate a DR plan targets only recovery resources and stays within budget. Include skeletons for 1) dr_module, 2) region-specific backend-config, 3) GH Actions gate for DR plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: create a small context wrapper module that adds tags Environment, Region, and CostCenter to all resources. Use a single backend (S3) with per-env/per-region keys. In each env's main",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: create a per-PR Terraform Cloud workspace named pr-${BRANCH} with a backend key pr/${BRANCH} for the shared module. Enforce an OPA policy that blocks prod resource changes unless a label or variable e…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Terraform-driven DR pattern that mirrors per-tenant resources to a secondary region. Explain module layout, how to isolate per-tenant/state, and how CI gates validate a DR plan targets only r",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create a security-group module that uses for_each over a map like {web:{...}, app:{...}, db:{...}} to emit three SGs. A wrapper module adds Environment and CostCenter tags and prefixes names. Use a pe",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\n\nTests your ability to design disaster recovery with Terraform across regions, ensuring strict state isolation per tenant and safe failover via CI gates.\n\nKey Concepts\n\n- Terraform modules and per-tenant state isolation\n- Cross-region DR patterns and remote backends\n- CI gates validating DR-target plans and budget/SLA constraints\n- Failover readiness and drift handling\n\nCode Example\n\n```hcl\n# dr_module/main.tf (skeleton)\nresource \"aws_s3_bucket\" \"dr_state\" { }\n\nresource \"null_resource\" \"dr_trigger\" { }\n```\n\n```yaml\n# .github/workflows/dr-gate.yml (skeleton)\n- name: DR Plan Gate\n  run: |\n    terraform init -backend-config=region_dr.tfvars\n    terraform plan -var-file=dr.tfvars -out=dr.plan\n```\n\nFollow-up Questions\n\n- How would drift between primary and DR be detected and corrected?\n- How would you simulate failover without impacting production? \n- How do you handle cost spikes during DR tests?",
        "difficulty": "intermediate"
      },
      {
        "id": "terraform-q-21",
        "questionId": "q-27365",
        "question": "Context: terraform/general; Difficulty: intermediate; Scenario: A mono-repo provisions per-tenant environments for AWS and Azure. Implement environment-scoped cost governance and drift detection: (1) a shared budget module that creates per-env budgets in AWS Budgets and Azure Cost Management; (2) a wrapper that wires env to budgets via tags; (3) a policy-as-code gate (OPA/Sentinel) that uses an external cost estimator to reject plans exceeding the monthly cap; (4) a CI gate that runs plan and cost-forecast against a baseline. Provide skeletons for module layout, budget definitions, policy snippet, and CI gate?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a wrapper root module that injects an environment label (env) into two provider aliases (google/gcp and oci/oci) and delegates to per-env child modules. Drive backends via environment-specific tfv",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Read the central CIDR registry via data.terraform_remote_state from the network repo, then allocate tenant CIDRs with explicit constraints and materialize them in per-tenant vars. Enforce via an OPA/G",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Wrap the shared module with an env_wrap that takes var.environment and passes name_prefix = \"${var.environment}-\" to the module. Compute a local full_prefix and use it for all resource names (vpc, sub",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a shared budget module that accepts env, cloud, and max_monthly_usd and provisions per-env AWS Budgets and Azure budgets. Integrate budgets via environment tags on resources. Add a policy (OPA",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to implement cost governance and drift detection across multi-cloud in a monorepo, ensuring budgets constrain changes and drift is surfaced early.\n\n## Key Concepts\n- Environment-scoped budgets (AWS Budgets, Azure Cost Management)\n- Tag-based association of budgets to resources\n- Policy-as-code (OPA/Sentinel) with an external cost estimator\n- CI integration for plan + cost-forecast vs baseline\n\n## Code Example\n```hcl\n# budget module skeleton\nvariable \"env\" { type = string }\nvariable \"cloud\" { type = string }\nvariable \"max_monthly_usd\" { type = number }\n\n# pseudo resources to illustrate budgeting per env\nresource \"aws_budgets_budget\" \"env_budget\" {\n  name              = \"${var.env}-budget\"\n  budget_type       = \"MONTHLY\" \n  limit_amount      = var.max_monthly_usd\n  time_unit         = \"MONTHLY\"\n}\n\n# Azure Budget example would be analogous via azurerm_cost_management_budget\n```\n\n## Follow-up Questions\n- How would you handle multi-cloud drift detection and rollback triggers?\n- How would you test budgets in CI across new tenants without incurring real costs?",
        "difficulty": "intermediate"
      },
      {
        "id": "terraform-q-22",
        "questionId": "q-22115",
        "question": "Context: terraform/general; Difficulty: beginner; Scenario: A mono-repo provisions a shared VPC via a core networking module used by prod and dev in a single AWS account. Implement a wrapper module injecting Environment and CostCenter tags to all resources. Create a per-env backend using the same S3 bucket with key prefixes env/prod and env/dev. Build a CI gate that runs terraform fmt, validate, and plan for both envs, then compares each plan against a stored golden plan and fails if any difference is detected. Provide minimal code references for: 1) wrapper module injecting tags, 2) backend configuration per env, 3) gate script comparing plans?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a wrapper_tags module that injects Environment and CostCenter tags via a tags map to all resources. Configure per-environment state in a single S3 bucket using prefixes env/prod and env/dev, with DynamoDB for…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a wrapper module env-tags merging base_tags with { Environment = var.environment }; apply as tags in resources. Backend key env-${var.environment}.tfstate. Gate: plan -out plan.out; show -json pla",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a single interface module with provider aliases for aws and google, and a tenants map to drive per-tenant resources. Enforce quotas and naming via a unified outputs schema and per-tenant locals. G",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Bastion module and instantiate it conditionally with count. In the root module: module \"bastion\" { source = \"./modules/bastion\" count = var.enable_bastion ? 1 : 0; public_subnet_id = data.aws_s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical Terraform module composition, per-environment backend configuration, and CI gating with deterministic plan comparison for drift control.\n\n## Key Concepts\n- Module composition with tagging strategy\n- Per-environment remote state management with locking\n- CI gates comparing plans against golden baselines\n\n## Code Example\n```hcl\n# wrapper_tags module usage\nmodule \"wrapper_tags\" {\n  source = \"./modules/wrapper_tags\"\n  environment = var.environment\n  cost_center = var.cost_center\n}\n```\n\n## Follow-up Questions\n- How would you extend the gate to tolerate minor plan differences due to timing or non-deterministic resources?",
        "difficulty": "beginner"
      },
      {
        "id": "terraform-q-23",
        "questionId": "q-9888",
        "question": "Design a cross-cloud Terraform module that provisions a data lake on AWS and GCP from a single interface. The module must support per-tenant quotas, enforce consistent cross-cloud naming, and governance via an Open Policy Agent policy that evaluates the planned changes. Describe the module interface, provider aliasing, and a CI gate that runs plan, exports to JSON, and fails on quota or naming violations. Include skeleton code for the interface and a sample policy, plus gate commands?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a registry module (e.g., 'log-sink') with AWS and GCP provider aliases. For each tenant, provision an isolated S3 bucket or GCS bucket and a dedicated IAM service account, all feeding a centralized cross-region sink…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan approach: 1) Wrapper module injects Environment and CostCenter tags via a shared tags map. 2) Per-env backend in one S3 bucket with key 'analytics/${var.environment}/terraform.tfstate'. 3) Provid",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define a wrapper module with var env_roles = map(object({ enabled=bool, role_name=string, policy_json=string })); use for_each on { for e, r in var.env_roles: e => r if r.enabled }; create aws_iam_rol",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single interface module with provider aliases for aws and google, and a tenants map to drive per-tenant resources. Enforce quotas and naming via a unified outputs schema and per-tenant locals. G",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests cross-cloud module design, provider aliasing, and policy-driven governance at scale.\n\n## Key Concepts\n- Terraform multi-provider modules\n- Provider aliasing and per-tenant scoping\n- Open Policy Agent integration with terraform show -json plan\n- CI gates and fail-fast\n\n## Code Example\n```javascript\n// Module interface skeleton (Terraform-like)\nmodule data_lake {\n  source = ./modules/data_lake\n  tenants = {\n    t1 = { cloud = aws, region = us-east-1 }\n    t2 = { cloud = gcp, region = us-central1 }\n  }\n  quotas = {\n    t1 = { data_lake_size_gb = 100 }\n    t2 = { data_lake_size_gb = 200 }\n  }\n}\n```\n\n```javascript\n// Sample OPA policy skeleton\npackage terraform.policy\ndefault allow = false\nallow {\n  input.resource_changes[_].change.actions[_] == create\n}\n```\n\n## Follow-up Questions\n- How would you test cross-cloud drift?\n- How would you version and rollback policy changes?",
        "difficulty": "advanced"
      },
      {
        "id": "terraform-q-24",
        "questionId": "q-9126",
        "question": "You manage two Terraform repos: lake-core and lake-ingest. A central per-environment backend stores a shared Glue catalog configuration; lake-ingest uses data.terraform_remote_state to read it. Propose a concrete per-env backend layout, remote state data source snippet, and a GitHub Actions gate that runs init/plan and fails if the plan changes the shared catalog or IAM bindings without approval?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant workspaces using a tenant-to-cloud/region mapping. Leverage provider aliases for AWS and GCP, with a shared networking module enforcing consistent tagging across clouds. A wrapper module generates p…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-env backends (lake-dev, lake-stage, lake-prod) with a central shared state bucket for the Glue catalog. In lake-ingest, fetch via data.terraform_remote_state. Gate: run terraform init && terra",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a wrapper module to inject Environment and CostCenter tags into all resources. Drive a per-env env_features map (dev/stage/prod) to toggle optional resources with count/for_each. Backend: s3-based",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: create inventory env with its own backend; implement data sources for VPC and subnets; no resources, so plan shows No Changes.```javascript data \"aws_vpc\" \"inventory\" { filter { name = \"",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cross-repo state sharing with per-env backends and governance hooks.\n\n## Key Concepts\n\n- terraform_remote_state\n- per-environment backends\n- gating plans and approvals\n- cross-resource drift checks\n\n## Code Example\n\n```hcl\n# backend per env (example)\nterraform {\n  backend \"s3\" {\n    bucket = \"lake-terraform-${var.env}\"\n    key    = \"${var.env}/terraform.tfstate\"\n    region = \"us-east-1\"\n  }\n}\n```\n\n```hcl\ndata \"terraform_remote_state\" \"shared_glue\" {\n  backend = \"s3\"\n  config = {\n    bucket = \"lake-shared-state\"\n    key    = \"prod/glue_catalog/terraform.tfstate\"\n    region = \"us-east-1\"\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you implement drift checks for remote state?\n- How would you handle approvals for shared-resource changes?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-11T03:44:07.481Z",
    "version": 1
  }
]