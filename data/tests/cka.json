[
  {
    "id": "test-cka",
    "channelId": "cka",
    "channelName": "CKA",
    "title": "CKA Knowledge Test",
    "description": "Test your understanding of CKA with 25 questions.",
    "questions": [
      {
        "id": "cka-q-0",
        "questionId": "q-6733",
        "question": "Scenario: A 7-node cluster runs a 5-replica StatefulSet backed by PVs and a Redis shard cluster. A security policy requires offline reconfiguration of a shard while preserving service availability. Provide a concrete, command-driven plan to drain, scale down, migrate data, apply a rolling update with minimal downtime, and validate data integrity with kubectl and redis-cli, ensuring zero data loss?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Investigate the CSI mount path and refresh policy, using kubectl describe secretproviderclass and CSI driver logs. From a sample pod, read /mnt/secrets-store/cred and the app’s env (if any) to reprodu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify Pending PVC with kubectl get pvc -n <ns>, then describe pvc <name> -n <ns> to read events. List available PVs and StorageClasses (kubectl get pv, kubectl get sc). If no binding is possible, c",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Drain a node, scale the StatefulSet to 4 replicas, migrate shard data with a safe script, then perform a rolling update with partition=1. Monitor rollout with kubectl rollout status statefulset/my-sta",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use kubectl auth can-i to confirm the SA can create pods in etl, then inspect RoleBindings and Roles in etl. If the binding is missing, create a namespace-scoped Role and a RoleBinding for etl-runner",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical expertise in safely performing stateful upgrades with data migrations, including drain/scale cycles and integrity verification under real-world constraints.\n\n## Key Concepts\n- StatefulSet rolling updates and partitioning\n- Data migration strategies for shard reconfiguration\n- PV health checks and Redis integrity verification\n\n## Code Example\n```bash\n# drain a node safely\nkubectl drain node01 --ignore-daemonsets\n\n# scale down StatefulSet\nkubectl scale sts/redis-shard --replicas=4\n\n# data migration (example placeholder script)\n./migrate_shard.sh\n\n# monitor rollout\nkubectl rollout status sts/redis-shard\n\n# verify data integrity\nredis-cli -h redis-main ping\nredis-cli -h redis-main cluster info\n```\n\n## Follow-up Questions\n- How would you handle partial migration failures and ensure idempotence?\n- What rollback steps would you automate if the rolling upgrade starts failing mid-process?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-1",
        "questionId": "q-15022",
        "question": "You're operating a Kubernetes cluster that uses Secrets Store CSI driver to mount dynamic DB credentials from an external secret store into pods as volumes. Credentials rotate every 15 minutes. Design a concrete, end-to-end verification plan to validate zero-downtime rotation: how to observe the refresh, how to force or detect a refresh, and how to validate the app uses new credentials without restart?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: verify CRD conversion strategy, apply the new CRD, deploy app-ctrl:v2, create a paused canary AppRollout to validate changes, promote if healthy, otherwise rollback CRD and controller. Use kubec",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Troubleshooting StatefulSet PVC Binding Issues ### Step 1: Verify PVC Status First, check the PVC status and gather detailed information: ```bash kubectl get pvc -n <namespace> kubectl describe pvc <pvc-name> -n <nam…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Verification Plan for Zero-Downtime Secret Rotation 1. **Initial Deployment & Verification**: - Deploy a pod with the secret mounted as a volume using a SecretProviderClass - Verify the credential file exists at the…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Run kubectl get deploy and kubectl describe pod <pod> to see the current state; kubectl logs <pod> --previous reveals the crash reason; check restartCounts and exit codes. If OOM, raise cpu/memory req",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Matters\nThis question evaluates practical knowledge of Secrets Store CSI driver integration, external secret stores, and rotation semantics under zero-downtime constraints. It assesses understanding of how dynamic secrets function in production Kubernetes environments and whether the candidate can design reliable verification procedures for critical security workflows.\n\n## Key Concepts\n- **Secrets Store CSI Driver & SecretProviderClass**: The CSI driver mounts secrets from external providers (e.g., Azure Key Vault, AWS Secrets Manager, HashiCorp Vault) into pods using the Kubernetes CSI specification. The SecretProviderClass defines which secrets to mount and how they're formatted.\n- **External Secret Rotation**: Rotation is triggered by the external secret provider and propagated to pods via the CSI driver's polling mechanism. The driver periodically checks the secret store for updates and atomically updates the mounted files when changes are detected.\n- **Volume vs. Environment Variable Mounts**: Secrets mounted as volumes are refreshed in place without requiring pod restart, while secrets mounted as environment variables are immutable once set at container startup.",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-2",
        "questionId": "q-17396",
        "question": "An in-cluster ValidatingWebhook enforces that every Pod in namespace analytics has a non-nil securityContext and CPU requests per container. Several Pods fail to start with 'failed calling webhook mywebhook.svc'. Provide a concrete, step-by-step plan using kubectl to verify webhook config, inspect API server logs, check the CA trust, reproduce a Pod admission, fix DNS/service reachability if needed, and verify that new pods start with policy enforced?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin with a 5-step kubectl triage: get pods and events, pull recent logs, inspect container status, check memory/CPU with top, and verify Redis connectivity from a pod. If latency ties to pool size o",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by listing the ValidatingWebhookConfiguration (kubectl get validatingwebhookconfigurations -o yaml) to verify the webhook name and client config. Check API server logs for webhook errors and ins",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan to audit RBAC scope: 1) enumerate RoleBindings and ClusterRoleBindings across all namespaces for wildcards or Secrets. 2) from a test pod in finance, attempt to read a secret in another namespace",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-team namespaces, enforce RBAC scoped to each namespace, apply NetworkPolicies to isolate ingress/egress, set ResourceQuotas and LimitRanges to cap CPU/memory and pods, enable Pod Securit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess troubleshooting of admission webhooks, a common gating issue in prod clusters.\n\n## Key Concepts\n\n- ValidatingWebhookConfiguration, CA bundles\n- API server logs, webhook reachability\n- DNS/service health, webhook service\n\n## Code Example\n\n```bash\nkubectl get validatingwebhookconfigurations -o yaml\nkubectl logs -n kube-system <kube-apiserver-pod> | grep webhook\n```\n\n## Follow-up Questions\n\n- How would you monitor webhook latency and failures?\n- How would you implement a safe bypass for maintenance without downtime?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-3",
        "questionId": "q-19696",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. A CronJob is scheduled to perform nightly database cleanup, but occasionally two runs overlap and cause DB contention and slower cleanup. Outline a beginner-friendly plan using kubectl to verify CronJob concurrencyPolicy and active Jobs, and implement a minimal fix (e.g., set concurrencyPolicy to Forbid and adjust startingDeadlineSeconds) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Deployment with requests: cpu: 100m, memory: 256Mi; limits: cpu: 1, memory: 512Mi; readinessProbe: /health, initialDelaySeconds: 5; livenessProbe: /health, periodSeconds: 15; HPA targetCPUUtiliz",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Plan:** Inspect the CronJob spec and active Jobs using kubectl to confirm the current concurrencyPolicy setting and identify any overlapping runs. Patch the CronJob to set concurrencyPolicy to Forbid and configure a r…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Identify affected StatefulSets and PVCs, inspect PV/PVC events and CSI logs, verify topology constraints and Node readiness, reproduce in a test namespace with explicit topology labels and bounded con",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Zero-downtime rotation using Secrets Store CSI: verify credentials come from a CSI volume, ensure the SecretProviderClass refreshInterval is set, and rely on the CSI driver to refresh the mounted file",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical, beginner-friendly ability to diagnose and fix overlapping CronJobs—a common reliability issue in Kubernetes environments. It validates understanding of the controls available to prevent concurrent workloads and ensure predictable maintenance windows.\n\n## Key Concepts\n- **CronJob**: A Kubernetes resource that runs Jobs on a recurring schedule.\n- **concurrencyPolicy**: Controls whether multiple Job instances can run simultaneously:\n  - `Allow` (default): Allows concurrent executions\n  - `Forbid`: Prevents concurrent runs; skips a new Job if one is already running\n  - `Replace`: Replaces the currently running Job with a new one when a new execution is due\n- **startingDeadlineSeconds**: Defines a window of time (in seconds) after the scheduled time during which the CronJob is allowed to start. If a scheduled run is missed (due to cluster issues or a concurrent Job), this setting ensures the Job still runs within the deadline rather than being skipped entirely.",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-4",
        "questionId": "q-22665",
        "question": "In a Kubernetes cluster, a Job named db-import in namespace analytics runs a Pod that mounts a Secret (db-creds) for a DB connection. Pods crash with 403 when mounting the Secret; the Secret exists and the Pod's ServiceAccount appears correct. Outline a beginner-friendly plan using kubectl to verify whether RBAC denies secret access, and implement a minimal fix (Role/RoleBinding granting get/watch on secrets to that ServiceAccount) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: 1) confirm SA and RBAC deny: kubectl -n analytics get sa db-import-sa; kubectl -n analytics describe sa db-import-sa. 2) test access: kubectl auth can-i get secrets --as=system:serviceaccount:an",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Diagnose MutatingWebhook causing duplicate sidecars after upgrade. Steps: review MutatingWebhookConfiguration rules and webhook server logs; query PodSpec and container counts to confirm sidecar prese",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Identify the SA and deny rule: kubectl describe sa prod-sa -n prod; kubectl get rolebindings -n prod | grep prod-sa. Test: kubectl auth can-i get secret -n prod --as=system:serviceaccount:prod:prod-sa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run: kubectl describe quota -n <ns>; kubectl get pods -n <ns>; kubectl get deploy -n <ns>. Compare total pod requests against quota hard limits. If exhausted, patch the quota or reduce the Deployment'",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests RBAC basics in a realistic startup task and shows practical debugging of Secrets mounting.\n\n## Key Concepts\n- RBAC: Roles, RoleBindings, and service accounts\n- Secrets mounting in Pods\n- can-i checks and minimal policy grants\n- Verifying fixes via pod restarts and logs\n\n## Code Example\n```bash\nkubectl -n analytics get sa db-import-sa\nkubectl -n analytics describe sa db-import-sa\nkubectl auth can-i get secrets --as=system:serviceaccount:analytics:db-import-sa\n```\n\n## Follow-up Questions\n- How would you validate least-privilege across environments? \n- What are risks of broader secret access?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-5",
        "questionId": "q-13538",
        "question": "In a Kubernetes cluster, a DaemonSet runs a node-collector that mounts a ConfigMap as a file to configure log levels. After updating the ConfigMap with a new log level, existing pods do not reflect the change. Outline a beginner-friendly plan using kubectl to verify config propagation, determine whether a live reload is supported, and implement a minimal fix so the updated log level takes effect for all pods, with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Identify failing pods and PVCs with kubectl describe pod and kubectl get pvc; inspect VolumeAttachment with kubectl get volumeattachments -A and CSI driver logs; reproduce the issue in a test namespac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check how the DaemonSet reads its config: 1) kubectl get cm log-collector-config -n ns -o yaml; 2) kubectl get pods -n ns -l app=log-collector; 3) kubectl exec -n ns -it <pod> -- cat /etc/log-collecto",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Verify mounting: inspect the Pod and SecretProviderClass to confirm the secret is mounted via Secrets Store CSI and not env vars. Reproduce: kubectl exec <pod> -- cat /mnt/secrets-store/cred to confir",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A systematic approach to troubleshoot DNS resolution failures under load in a Kubernetes cluster: 1. **Verify DNS config inside pods**: Examine `/etc/resolv.conf` inside affected pods to confirm Kubernetes DNS settings…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests a candidate's ability to reason about config propagation in Kubernetes, DaemonSets, and ConfigMaps. It checks practical use of kubectl for validation, and whether they know how to trigger or reason about reloads vs restarts.\n\n## Key Concepts\n- DaemonSet configuration reload behavior\n- ConfigMap mounting as a file\n- Rolling restart vs live reload\n- pod verification via exec/logs\n\n## Code Example\n```javascript\nkubectl get cm log-collector-config -n ns -o yaml\nkubectl get pods -n ns -l app=log-collector\nkubectl rollout restart ds/log-collector -n ns\n```\n\n## Follow-up Questions\n- How would you automate this validation in a CI workflow?\n- What changes would you make to ensure zero-downtime reloads if config updates are frequent?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-6",
        "questionId": "q-13752",
        "question": "In a large Kubernetes cluster running ML inference workloads with strict latency SLAs, design a practical maintenance plan that avoids disruptions. Include taints and tolerations, PodDisruptionBudget, rolling update constraints, canary deployments, and verification steps. How would you implement and validate this in production?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Zero-downtime rotation using Secrets Store CSI: verify credentials come from a CSI volume, ensure the SecretProviderClass refreshInterval is set, and rely on the CSI driver to refresh the mounted file",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Maintenance plan: taint maintenance nodes, drain non-daemonsets, enforce PodDisruptionBudget(minAvailable: 80%) for critical deployments; limit rolling updates to maxUnavailable: 1, maxSurge: 25%; imp",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: use kubectl top to confirm memory usage spikes on the failing pod; kubectl describe pod to see OOMKilled status and which container; inspect Deployment YAML for memory requests/limits and any si",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## End-to-End CA Rotation Plan for MutatingAdmissionWebhook ### 1. Verify Current CA Trust Mismatch ```bash # Retrieve current webhook configuration and extract CA bundle kubectl get mutatingwebhookconfiguration <webhoo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe scenario tests the ability to design disruption-tolerant maintenance using Kubernetes primitives and production verification.\n\n## Key Concepts\n- PodDisruptionBudget\n- Taints and tolerations\n- Rolling updates controls (maxUnavailable, maxSurge)\n- Canary/Blue-Green deployments\n- Custom metrics for HPA\n- PreStop and graceful draining\n\n## Code Example\n```yaml\napiVersion: policy/v1\nkind: PodDisruptionBudget\nmetadata:\n  name: critical-pdb\nspec:\n  minAvailable: 80%\n  selector:\n    matchLabels:\n      app: critical-service\n```\n\n## Follow-up Questions\n- How would you adapt this for stateful workloads?\n- How would you simulate maintenance to validate SLAs?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-7",
        "questionId": "q-21377",
        "question": "In a shared Kubernetes cluster, a new service must reach api.example.com:443, but a default NetworkPolicy blocks all egress. Provide a concrete, minimally invasive plan to allow only that service's pods to access the API, including pod labels, the NetworkPolicy, test/verification steps, and a rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Locate cluster-wide bindings for the SA, replace with a namespace-scoped Role/RoleBinding, then rollout the Deployment to use the new SA with a rolling update. Validate least privilege via kubectl aut",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "kubectl rollout status deployment/web; kubectl get pods -l app=web -o wide; kubectl describe pod <pod>; kubectl get events --sort-by=.lastTimestamp; kubectl get configmap web-config -o yaml; adjust re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by checking `kubectl describe pod <pod>` to identify CrashLoopBackOff events and examine pod status, then use `kubectl logs -p <pod>` to view previous container logs and identify startup errors. Verify the image e…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Label pods with app=enricher in namespace enrichment. Create a NetworkPolicy that allows egress to api.example.com:443 from those pods (adjusting to IPs if FQDN isn’t supported). Validate with a test",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests practical network policy design, scope reduction, and verification in a shared environment, including rollback.\n\n## Key Concepts\n\n- NetworkPolicy: egress rules\n- Pod labeling and namespace scoping\n- DNS vs IP-based allowlists in CNI\n- Validation: test pod, logs, and events\n- Rollback: remove policy safely\n\n## Code Example\n\n```yaml\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: allow-enricher-api-egress\n  namespace: enrichment\nspec:\n  podSelector:\n    matchLabels:\n      app: enricher\n  policyTypes:\n  - Egress\n  egress:\n  - to:\n    - ipBlock:\n        cidr: 203.0.113.0/24\n    ports:\n    - protocol: TCP\n      port: 443\n```\n\nNote: If CNI supports FQDN, replace ipBlock with to.fqdn: api.example.com.\n\n## Follow-up Questions\n\n- How would you monitor and audit egress to ensure no unintended traffic?\n- How would you adapt this for multiple external services with different namespaces?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-8",
        "questionId": "q-14503",
        "question": "In a Kubernetes namespace 'apps', a Deployment with 3 replicas runs a web app, but new Pods stay Pending due to a Namespace ResourceQuota that caps CPU at 1 core and memory at 2Gi. Outline a beginner-friendly plan using kubectl to confirm quota is the bottleneck, identify the bottlenecked resource, and implement a minimal fix with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: 1) verify Deployment containerPort and Service targetPort reflect the new 9090. 2) inspect Endpoints to confirm 9090 is the target. 3) patch the Service to targetPort:9090 (or roll back image) a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Verify that the Secret was correctly rotated, confirm the mounted credentials are updated inside the pods, and reload them via a rolling restart. Follow these steps: 1. **Verify the Secret contents**: ```bash kubectl ge…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run: kubectl get quota -n apps; kubectl describe quota -n apps; kubectl get deploy web -n apps -o jsonpath='{.spec.template.spec.containers[*].resources}'; kubectl get pods -n apps -o wide. If usage m",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Identify whether a NetworkPolicy blocks cross-namespace traffic, e.g., kubectl get networkpolicy --all-namespaces; curl from a frontend pod to backend:80; if blocked, craft a minimal policy allowing t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical debugging of Namespace ResourceQuota with real kubectl commands and minimal, safe fixes suitable for beginners.\n\n## Key Concepts\n- Namespace ResourceQuota\n- CPU/memory requests vs limits\n- Pod scheduling vs Pending state\n\n## Code Example\n```bash\nkubectl get quota -n apps\nkubectl describe quota -n apps\nkubectl get deploy web -n apps -o jsonpath='{.spec.template.spec.containers[*].resources}'\n```\n\n## Follow-up Questions\n- How would you set up basic quota monitoring alerts?\n- What trade-offs arise when lowering requests versus increasing quotas?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-9",
        "questionId": "q-17620",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. A recent image upgrade changed the API port from 8080 to 9090 without updating the Service's targetPort. Traffic results in 503s. Outline a beginner-friendly plan using kubectl to diagnose the port mismatch, verify the pod container port, service port/targetPort, and endpoints, and implement a minimal fix with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Identify misbehaving pods caused by webhook behavior and reproduce safely. Provide concrete kubectl steps to (1) locate affected Deployments and nodes, (2) check API server and webhook logs plus sidec",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: 1) verify Deployment containerPort and Service targetPort reflect the new 9090. 2) inspect Endpoints to confirm 9090 is the target. 3) patch the Service to targetPort:9090 (or roll back image) a",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: identify the namespace and policy; create a debug pod and test DNS and TLS reachability to api.partner.com; inspect current policy egress rules and ports; update the NetworkPolicy to allow egres",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check memory pressure and pod-level metrics with kubectl top pod -n <ns>, then describe the affected pod and inspect events. Review resource requests/limits and node memory usage. If a pod is constrai",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests practical debugging of port mappings between Deployments and Services after an image update, a common pitfall in production.\n\n## Key Concepts\n\n- Kubernetes Service targetPort and port mapping\n- Endpoints and how they reflect targetPort\n- Rolling updates and rollout restarts\n- Basic in-cluster validation with curl from a pod\n\n## Code Example\n\n```javascript\n# verify ports\nkubectl -n default get deployment my-api -o jsonpath='{.spec.template.spec.containers[0].ports[0].containerPort}'\nkubectl -n default get svc my-api -o jsonpath='{.spec.ports[0].targetPort}'\n# show endpoints\nkubectl -n default get endpoints my-api\n```\n\n## Follow-up Questions\n\n- How would you handle multiple namespaces with differing port configurations?\n- What are the risks of patching a Service vs updating the Deployment port and image simultaneously?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-10",
        "questionId": "q-4388",
        "question": "Scenario: A 3-node, etcd-backed Kubernetes control plane on bare metal managed by kubeadm has suffered a regional outage that left only one master up. Provide a precise disaster-recovery plan to restore a healthy 3-member etcd quorum and API control plane within 20 minutes, including etcd snapshot restoration, rejoining control-plane nodes, updating manifests, and verification steps with exact commands?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On surviving master: restore etcd from latest snapshot to /var/lib/etcd, start etcd with the restored member in the quorum, then kubeadm reset the failed master and kubeadm init with the same --contro",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Rotate kube-apiserver and etcd certificates without downtime using a controlled rolling-update workflow: 1. **Backup existing certificates** - Copy all current TLS certificates and CA bundles from `/etc/kubernetes/pki`…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Gather facts with kubectl describe pod <name> and kubectl get events; check memory requests/limits and container resources; verify probes (readiness, liveness); review sidecar logs; roll back with kub",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-namespace default-deny NetworkPolicy, then add two policies that allow all traffic within each namespace and an egress rule allowing outbound HTTPS to api.external.com:443. Do not allo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests hands-on DR skills for control-plane repair, including etcd backup/restore, member reconfiguration, and kubeadm bootstrap under time pressure.\n\n## Key Concepts\n\n- etcd snapshot/restore\n- control-plane HA\n- kubeadm reset/init\n- static pod bootstrap and API server repair\n\n## Code Example\n\n```bash\n# On surviving master\netcdctl snapshot restore /var/backups/etcd/snapshot.db \\\n  --name=master1 --initial-cluster master1=https://10.0.0.1:2380,master2=https://10.0.0.2:2380,master3=https://10.0.0.3:2380 \\\n  --data-dir=/var/lib/etcd\nsystemctl restart etcd\n```\n\n```bash\n# Rebuild control plane\nkubeadm reset -f\nkubeadm init --control-plane-endpoint \"k8s-master:6443\" --upload-certs\n```\n\n## Follow-up Questions\n\n- How would you validate cluster health after recovery?\n- What are failure modes if time skew is present across masters?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-11",
        "questionId": "cka-troubleshoot-1776520293793-1",
        "question": "A Deployment's pods remain Pending with the event '0/3 nodes are available: 3 node(s) had taints that the pod didn't tolerate'. What is the most likely cause?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "The pod's tolerations do not match the node taints",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: in a default-deny cluster, create two NetworkPolicy objects. Allow from ns-app pods to ns-db:5432, allow DNS egress on port 53 (TCP/UDP), and deny all other ingress/egress. Validate with a test",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run: kubectl get quota -n apps; kubectl describe quota -n apps; kubectl get deploy web -n apps -o jsonpath='{.spec.template.spec.containers[*].resources}'; kubectl get pods -n apps -o wide. If usage m",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create namespace-level ResourceQuota and LimitRange to bound resources; for example: hard: cpu: 2000m, memory: 4Gi, pods: 40, requests.cpu: 1000m, requests.memory: 2Gi, limits.cpu: 2000m, limits.memor",
            "isCorrect": false
          }
        ],
        "explanation": "## Correct Answer\nThe pod's tolerations do not match the node taints, so no nodes are considered eligible for scheduling, leaving the Pod Pending.\n\n## Why Other Options Are Wrong\n- API server downtime would cause broader control-plane failures, not a taint-specific scheduling issue.\n- An invalid image pull secret would lead to ImagePullBackOff during scheduling or startup, not a taint-driven Pending state.\n- A DNS service outage affects name resolution, not the scheduler's ability to match taints to tolerations.\n\n## Key Concepts\n- Taints and tolerations govern where pods can be scheduled.\n- If no node matches due to taints, pods remain Pending.\n\n## Real-World Application\n- Inspect node taints with kubectl describe nodes, review the Pod spec for tolerations, and either remove taints or add matching tolerations to the Pod to enable scheduling.\n",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-12",
        "questionId": "q-18436",
        "question": "You're operating a multi-namespace Kubernetes cluster for a fintech product. A new NetworkPolicy was introduced to isolate pods; after deployment, the payment webhook between frontend and billing backend intermittently fails under load. Outline a precise, end-to-end diagnostic and remediation plan with concrete kubectl commands, testing steps, and a safe rollback strategy. Include how you'd harden policy with default-deny and explicit allows?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Diagnose MutatingWebhook causing duplicate sidecars after upgrade. Steps: review MutatingWebhookConfiguration rules and webhook server logs; query PodSpec and container counts to confirm sidecar prese",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Beginner-Friendly Plan to Fix Kubernetes HPA Not Scaling ### Step 1: Verify Metrics Server is Installed and Healthy ```bash # Check if Metrics Server deployment exists kubectl get deployment metrics-server -n kube-sy…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Diagnose by validating policy effect with a test pod, reproduce, inspect policy, verify connectivity, and rollback if blocked. Use concrete steps: 1) kubectl get netpol -A; 2) kubectl describe netpol",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Trace by collecting PVC/PV events across namespaces, describe affected PVCs/PVs, and inspect CSI controller logs and topology data; reproduce with a multi-namespace Job that creates parallel PVCs in a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world debugging of NetworkPolicy impact, end-to-end thinking, and safe rollback strategies. It probes concrete command usage and hardening practices beyond theory.\n\n## Key Concepts\n- NetworkPolicy semantics and default deny\n- Diagnosing inter-service traffic failures under load\n- Safe rollback and incremental hardening\n- Crafting explicit allow rules with proper selectors\n\n## Code Example\n```javascript\nkubectl get netpol -A\nkubectl describe netpol -n frontend <name>\nkubectl run testpod --image=curlimages/curl:7.90.0 --restart=Never -- sh -c 'curl -sS http://billing-service:443/health'\n```\n\n## Follow-up Questions\n- How would you test policy changes in CI/CD without impacting prod?\n- How do you monitor policy efficacy over time and detect regressions?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-13",
        "questionId": "q-12127",
        "question": "A StatefulSet running a partitioned Kafka cluster on a CSI-enabled PVC experiences a rolling update where a broker pod is stuck Terminating due to its PVC detaching slowly. Explain the root cause, how you would diagnose, and concrete steps to safely complete the rollout without data loss, including finalizers, detachment, and StorageClass settings?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify the Secrets Store CSI mount path inside a pod, inspect the SecretProviderClass for the secret source and refreshPeriod, and reproduce the value by reading the mounted secret file in a pod. Rota",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Root cause is CSI detachment race leaving the PVC bound; fix by auditing PVC/PV status, finalizers, and CSI logs. Diagnose with kubectl describe pod/pvc, kubectl get pvc/pv, and events for detach erro",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Inspect NetPols in the namespace, create a test pod, curl the payment gateway from the pod, and verify if egress is blocked. If blocked, patch the NetPol to allow the gateway IP/port (least privilege)",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check endpoints with kubectl get endpoints my-api-svc; get selector with kubectl get svc my-api-svc -o jsonpath={.spec.selector}; compare with pods kubectl get pods -l app=my-api --show-labels. If mis",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of PVC lifecycle, CSI detachment, finalizers, and StatefulSet rollouts.\n\n## Key Concepts\n- PVC/PV lifecycle and detach\n- CSI driver detach mechanics\n- StatefulSet rollout semantics\n- pvc-protection finalizer and reclaimPolicy\n\n## Code Example\n```javascript\n// Pseudocode for detection and remediation\nif (pod.status?.phase === 'Terminating' && pvc.finalizers?.includes('kubernetes.io/pvc-protection')) {\n  patchPVCFinalizers(pvc, []);\n  deletePod(pod);\n}\n```\n\n## Follow-up Questions\n- How would you verify data safety during detach?\n- How would you adjust StorageClass/CSI settings to prevent future stalls?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-14",
        "questionId": "q-3703",
        "question": "In a 4-node Kubernetes cluster, enable mutual TLS across all namespaces using Linkerd, enforce mTLS by default, and verify that only services with Linkerd sidecars can communicate with a backend API on port 8080 while a plain pod cannot. Provide exact commands to install Linkerd, inject sidecars, deploy apps, and perform tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Install Linkerd control plane and check: curl -sL https://run.linkerd.io/install | sh; linkerd install | kubectl apply -f -; linkerd check; enable auto-injection on app namespace: kubectl label ns app",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "1. Identify a test pod: `kubectl get pods -n <namespace> -l app=api` 2. Check existing NetworkPolicies: `kubectl get networkpolicies -n <namespace>` 3. Inspect policy details: `kubectl describe networkpolicy <policy-nam…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Strategy: convert from env-based secret to CSI Secrets Store with Vault, set SecretProviderClass with refreshInterval (e.g., 3600s), mount as volume, and use a small in-cluster watcher/sidecar to sign",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Install cert-manager and create a ClusterIssuer; deploy a Certificate for ingress-tls referencing the issuer; cert-manager renews before expiry and updates the Secret. Ingress reloads automatically on",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical service-mesh setup, mTLS enforcement, and verification of sidecar behavior in real clusters.\n\n## Key Concepts\n- Linkerd installation and validation\n- Namespace auto-injection and sidecar presence\n- mTLS enforcement and trust domain verification\n- In-cluster end-to-end testing with injected vs plain pods\n\n## Code Example\n```javascript\n# Install and check\ncurl -sL https://run.linkerd.io/install | sh\nlinkerd install | kubectl apply -f -\nlinkerd check\n\n# Enable auto-injection and roll out\nkubectl label ns app linkerd.io/inject=enabled\nkubectl rollout restart deploy -n app\n\n# Verify sidecars\nkubectl get pods -n app -o jsonpath='{.items[*].spec.containers[*].name}'\n\n# Tests\n# Injected pod test\nkubectl run test-injected --rm -it --image=busybox -n app -- sh\n# inside pod: wget or curl to backend\ncurl http://backend:8080/health\n\n# Plain pod test\nkubectl run test-plain --rm -it --image=busybox -n app -- sh\n# inside pod: curl to backend (should fail due to mTLS)\ncurl http://backend:8080/health\n```\n\n## Follow-up Questions\n- How would you rotate the trust anchor without downtime?\n- How would you monitor and alert if sidecar injection drifts across namespaces?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-15",
        "questionId": "q-4163",
        "question": "Scenario: In production, a Deployment named order-processor in namespace prod uses a ConfigMap named app-config with LOG_LEVEL and a Secret app-secret with API_KEY. The Pod fails to start because API_KEY is missing. Provide a practical fix: 1) ensure Secret exists and mounted, 2) wire LOG_LEVEL and API_KEY into env vars from ConfigMap/Secret, 3) add readinessProbe on port 8080 and a livenessProbe, 4) outline a 0-downtime rollout approach and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: migrate from environment variable to mounted Secret volume and implement a reload mechanism (in-application or sidecar) enabling the application to detect and reload secret changes from /etc/secret without pod res…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Inspect events and pod descriptions. Confirm readiness and startupProbe settings, adjust startupProbe to tolerate longer init times, and ensure livenessProbe doesn't target unhealthy pods. Apply a Pod",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create or verify the Secret and patch the Deployment to pull LOG_LEVEL from the ConfigMap and API_KEY from the Secret via envFrom/valueFrom. Add readiness and liveness probes (httpGet /health on 8080)",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement External Secrets to inject secrets as files, remove secret env vars, and use mounted files in pods; perform a canary rollout swapping to file-based secrets; rotate keys and revoke old ones;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical Kubernetes ops: wiring config and secrets, ensuring startup with proper probes, and performing safe rolling updates. It emphasizes real-world constraints like zero-downtime deploys and quick verification.\n\n## Key Concepts\n- ConfigMap/Secret envFrom and valueFrom\n- Probes: readiness and liveness\n- Rolling updates and maxUnavailable\n- Verification: kubectl rollout status, in-cluster health checks\n- Secrets handling and least-privilege access\n\n## Code Example\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: order-processor\n  namespace: prod\nspec:\n  template:\n    spec:\n      containers:\n      - name: order-processor\n        image: example/order-processor:latest\n        env:\n        - name: LOG_LEVEL\n          valueFrom:\n            configMapKeyRef:\n              name: app-config\n              key: LOG_LEVEL\n        - name: API_KEY\n          valueFrom:\n            secretKeyRef:\n              name: app-secret\n              key: API_KEY\n        readinessProbe:\n          httpGet:\n            path: /health\n            port: 8080\n          initialDelaySeconds: 5\n          periodSeconds: 10\n        livenessProbe:\n          httpGet:\n            path: /health\n            port: 8080\n          initialDelaySeconds: 15\n          periodSeconds: 20\n```",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-16",
        "questionId": "q-17817",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a TLS-enabled API that authenticates to an upstream service using a client certificate stored in a Secret. The Secret rotates daily, but existing pods continue to fail TLS handshakes until a restart. Outline a beginner-friendly plan using kubectl to verify secret propagation to pods and implement a minimal fix so certificate rotations take effect without manual restarts, with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Capture startup timings and initContainer logs, kubectl describe pod, and events; monitor readiness transitions and image pulls. If init work is heavy, move health checks to readiness, cache results,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: identify affected PVCs/pods in cache-prod; inspect PVC status, VolumeBinding, and quota events; capture CSI/driver logs if available; reproduce in a test namespace by recreating failing PVCs; im",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Inspect the Secret and mounts per pod, confirm the updated cert is visible inside each container, and test TLS. Then trigger a rolling restart to ensure existing pods reload the secret. Commands: kube",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Identify drifted objects and etcd revisions with kubectl get all --all-namespaces -o yaml, then verify revisions via etcdctl get /registry/...; inspect member health and WAL/compaction with etcdctl en",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical debugging of secret rotation and rollout mechanics in Kubernetes.\n\n## Key Concepts\n\n- Secrets and VolumeMounts\n- Rolling updates and rollout status\n- TLS certificates and rotation\n- Verification with openssl and curl\n\n## Code Example\n\n```javascript\n# shell commands to verify and roll out\nkubectl get secret tls-client -n app -o json\nkubectl get pods -n app -o wide\nkubectl exec -n app api-0 -- ls /etc/certs\nkubectl rollout restart deploy/api\n```\n\n## Follow-up Questions\n\n- How would you automate secret rotation with an operator or external secret store?\n- How would you verify zero downtime during rotation?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-17",
        "questionId": "q-29257",
        "question": "Advanced CKA: A Kubernetes cluster uses a MutatingAdmissionWebhook to inject a tracing sidecar into prod-namespaces (env=prod). After a cluster upgrade, some deployments in prod get the sidecar inconsistently, others don’t, causing memory swings. Provide a reproducible kubectl-based plan to (1) identify affected Deployments/Pods, (2) inspect the webhook configuration and admission events, and (3) reproduce in a test namespace with controlled labels, and (4) implement a safe remediation (tighten the webhook selector or add an explicit exclusion) with validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enumerate affected deployments by env label, inspect MutatingWebhookConfiguration for namespaceSelector and the admission logs, reproduce in a test-namespace by applying env=prod label to a minimal De",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Test DNS resolution from a pod (nslookup kubernetes.default.svc.cluster.local). Gather CoreDNS state: kubectl get pods -n kube-system -l k8s-app=kube-dns, kubectl logs -n kube-system <dns-pod>, and th",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Inspect the Service and Endpoints using `kubectl describe svc <name>` and `kubectl get endpoints <name>` to verify connectivity. Compare the Service's `targetPort` with the container's listening port by examining the De…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Identify misbehaving pods caused by webhook behavior and reproduce safely. Provide concrete kubectl steps to (1) locate affected Deployments and nodes, (2) check API server and webhook logs plus sidec",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to diagnose webhook-driven behavior, critical for production reliability and multi-tenant environments.\n\n## Key Concepts\n- MutatingAdmissionWebhook configuration selectors\n- Kubernetes admission flow and audit logs\n- Repro in isolated namespace with controlled labels\n- Safe remediation: patch selector, add exclusions, validate\n\n## Code Example\n```bash\nkubectl get mutatingwebhookconfigurations -o yaml\nkubectl get deploy --all-namespaces -o json | jq -r '.items[] | select(.metadata.labels.env==\"prod\") | {ns:.metadata.namespace,name:.metadata.name}'\n```\n\n## Follow-up Questions\n- How would you detect if multiple webhooks conflict?\n- How would you verify regression across namespaces?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-18",
        "questionId": "q-2656",
        "question": "In a three-AZ Kubernetes cluster, design an operator to keep replicas of Deployments labeled app=svc spread across zones, even during node drains. Describe the CRD you would add, the reconcile logic, and how you would validate distribution with tests. Provide a minimal manifest example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Configure HPA on deployment web-app with minReplicas 4, maxReplicas 20 and target CPU 70%. Set container resources: requests CPU 200m memory 256Mi; limits 500m memory 512Mi. Deploy CA with minNodes 3,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run: kubectl get ingress -A; kubectl describe ingress <name> -n <ns>; kubectl get secret <tls> -n <ns>; curl -k https://<LB_IP>/health; kubectl logs -n <ns> -l app=ingress-nginx -c controller; kubectl",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Explain how to build a Kubernetes operator (Go) that enforces zone-aware pod distribution for Deployments labeled app=svc in a multi-AZ cluster. Include a CRD (ZoneSpread) to declare minDistributions,",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: use kubectl top to confirm memory usage spikes on the failing pod; kubectl describe pod to see OOMKilled status and which container; inspect Deployment YAML for memory requests/limits and any si",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical operator design and CRD usage for real-world cluster reliability.\n\n## Key Concepts\n- Kubernetes operators and the controller pattern\n- PodTopologySpread / TopologySpreadConstraints\n- CustomResourceDefinitions and idempotent reconcile\n- Handling node drains and eviction risk in production\n- Testing: envtest, fake clients, and integration tests\n\n## Code Example\n```yaml\napiVersion: apiextensions.k8s.io/v1\nkind: CustomResourceDefinition\nmetadata:\n  name: zonespreads.acme.example.com\nspec:\n  group: acme.example.com\n  versions:\n  - name: v1beta1\n    served: true\n    storage: true\n    schema:\n      openAPIV3Schema:\n        type: object\n        properties:\n          spec:\n            type: object\n            properties:\n              minDistributions:\n                type: object\n                additionalProperties:\n                  type: integer\n  scope: Namespaced\n  names:\n    plural: zonespreads\n    singular: zonespread\n    kind: ZoneSpread\n```\n\n## Follow-up Questions\n- How would you test behavior during an AZ failure and node drain?\n- What are the trade-offs of using a CRD-driven reconciler vs. relying on built-in topology spread constraints?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-19",
        "questionId": "q-7216",
        "question": "You're operating a **multi-tenant** Kubernetes cluster with 15 namespaces and a shared control plane. A namespace **ci-runner** is inadvertently logging secret values to stdout. How would you design a solution to prevent secret leakage and enforce least privilege using RBAC, admission controls (Gatekeeper/Kyverno), and audit logging, plus network segmentation? Provide concrete steps and example YAML for a dedicated ServiceAccount RBAC, a policy constraint, and a NetworkPolicy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a dedicated service account for CI with least privilege: no cluster-admin; grant read/list in its namespace only; explicitly deny access to Secrets. Enable Audit and enforce admission policy via G",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Update the ConfigMap with kubectl (apply or createconfigmap) and then force a rolling restart: kubectl rollout restart deployment/my-deploy; kubectl rollout status deployment/my-deploy. Verify new pod",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "First, identify and remove the cluster-wide Secret read binding: `kubectl describe clusterrolebinding | grep -A 10 'Secrets\\|secrets'`. Remove any ClusterRoleBinding granting Secret read access to the analytics-job Serv…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a NetworkPolicy analytics-egress in analytics namespace allowing egress from app=analytics-ingest pods to DW pods app=dw on TCP 5432, with a default deny for other egress. Apply the policy, ver",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess mastery of Kubernetes security controls in a realistic, production-like setting: RBAC discipline, policy-driven admission control, and network segmentation for multi-tenant clusters. It also tests practical thinking about secret management and auditability.\n\n## Key Concepts\n\n- Least privilege RBAC\n- Kubernetes Audit logging\n- Open Policy Agent (Gatekeeper) / Kyverno\n- Centralized secret management (Vault, AWS Secrets Manager)\n- Namespace NetworkPolicy isolation\n\n## Code Example\n\n```yaml\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  name: ci-runner-binding\n  namespace: ci-runner\nsubjects:\n- kind: ServiceAccount\n  name: ci-runner-sa\n  namespace: ci-runner\nroleRef:\n  apiGroup: rbac.authorization.k8s.io\n  kind: Role\n  name: ci-runner-read\n```\n\n```yaml\napiVersion: kyverno.io/v1\nkind: ClusterPolicy\nmetadata:\n  name: forbid-secret-env\nspec:\n  rules:\n  - name: deny-secret-in-env\n    match:\n      any:\n        - pattern: \"*\"\n    validate:\n      message: \"Secrets must not be exposed in pod spec env.\"\n      pattern:\n        spec:\n          containers:\n          - env:\n            - name: \"*\"\n              valueFrom:\n                secretKeyRef:\n                  name: \"*\"\n```\n\n```yaml\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: ci-isolate\n  namespace: ci-runner\nspec:\n  podSelector: {}\n  policyTypes:\n  - Ingress\n  - Egress\n  ingress: []\n  egress:\n  - to:\n    - namespaceSelector:\n        matchLabels:\n          app: vault\n```\n\n## Follow-up Questions\n\n- How would you test the policy enforcement in a staging cluster?\n- What metrics would you monitor to detect leakage attempts?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-20",
        "questionId": "q-6607",
        "question": "In a 20-node on-prem Kubernetes cluster, etcd becomes unavailable. You have a verified backup from yesterday. Provide a practical, step-by-step restoration plan to restore etcd and the API server with minimal downtime, preserving workloads, and validating cluster health. Include concrete commands you would run for backup verification, restoration, kubeadm configuration, and end-to-end checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Maintenance plan: taint maintenance nodes, drain non-daemonsets, enforce PodDisruptionBudget(minAvailable: 80%) for critical deployments; limit rolling updates to maxUnavailable: 1, maxSurge: 25%; imp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would check the rollout and Istio weights, then pull key metrics from Prometheus (P95 latency, error rate) and review recent pod logs. If the canary violates SLOs, I pause the traffic shift, rollbac",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan a rolling restore: 1) verify backup integrity and timestamp; 2) boot a temporary etcd cluster and restore from backup; 3) point the API server to the new etcd data dir and restart components one",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: verify the secret mount and env usage in payments-api pods; reproduce current certs with kubectl exec and openssl to inspect /etc/tls; add a minimal auto-reload sidecar that watches /etc/tls and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical disaster recovery skills for an etcd-backed control plane, including backup verification, restore sequencing, API server restart order, and RBAC/certs rotation under downtime pressure.\n\n## Key Concepts\n\n- etcd backup/restore and snapshot integrity\n- API server restart order and kubeadm reconfiguration\n- RBAC, cert rotation, and smoke tests\n\n## Code Example\n\n```javascript\n// Simple script skeleton for backup verification\nconst {execSync} = require('child_process');\ntry {\n  console.log(execSync('etcdctl snapshot status /backups/etcd-snapshot.db').toString());\n} catch (e) {\n  console.error('Backup check failed', e);\n}\n```\n\n## Follow-up Questions\n\n- How would you handle multi-tenant workloads during restore?\n- What metrics indicate a healthy restoration?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-21",
        "questionId": "q-14696",
        "question": "In a shared Kubernetes cluster, a Job in namespace 'analytics' runs under ServiceAccount 'analytics-job'. A misconfigured ClusterRoleBinding grants read access to Secrets cluster-wide. Provide an exact, practical plan to verify the binding, reproduce the privilege from within a pod, and fix with least-privilege: remove the cluster-wide binding, create a namespace-scoped RoleBinding that permits read access to Secrets only in analytics, and validate via can-i and a test-secret read from the Job pod?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by verifying cross-namespace service reachability through DNS resolution and endpoint inspection, examine NetworkPolicy configurations in both namespaces, and validate traffic flow using a temporary busybox pod. E…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify Pods failing admission via webhook errors in events, locate MutatingWebhookConfiguration, inspect CABundle and secret TLS, and verify API server connectivity. Reproduce in a test namespace wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "First, identify and remove the cluster-wide Secret read binding: `kubectl describe clusterrolebinding | grep -A 10 'Secrets\\|secrets'`. Remove any ClusterRoleBinding granting Secret read access to the analytics-job Serv…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Run: kubectl auth can-i get pods --as=system:serviceaccount:prod:app-sa -n prod; same for another namespace. Inspect: kubectl describe clusterrolebindings; kubectl describe rolebindings -n prod app-sa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests real-world RBAC hardening and least-privilege implementation in shared Kubernetes environments.\n\n## Key Concepts\n- RBAC scope differences: ClusterRoleBinding (cluster-wide) vs RoleBinding (namespace-scoped)\n- Runtime verification using `kubectl auth can-i`\n- Namespace isolation for sensitive resources like Secrets\n- Principle of least privilege in multi-tenant clusters\n\n## Code Example\n```yaml\n# Namespace-scoped RoleBinding for Secret read access in analytics namespace only\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  name: analytics-secret-reader\n  namespace: analytics\nsubjects:\n- kind: ServiceAccount\n  name: analytics-job\n  namespace: analytics\nroleRef:\n  kind: Role\n  name: secret-reader\n  apiGroup: rbac.authorization.k8s.io\n```\n\n## Verification Steps\n1. `kubectl auth can-i get secret -n analytics --as=system:serviceaccount:analytics:analytics-job`\n2. Create test secret in analytics and verify read access from pod\n3. Attempt to read secrets from other namespaces (should be denied)",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-22",
        "questionId": "q-22366",
        "question": "You're operating a Kubernetes cluster where namespace payments runs a Deployment payment-service (4 replicas) talking to an external vendor API at vendor.example.com:443 and an internal Redis. Previously, egress was allowed to all destinations. Create a NetworkPolicy that restricts egress so pods can only reach vendor.example.com:443 and the Redis service IP:6379, and blocks everything else without impacting readiness probes. Then provide exact kubectl/apply steps, test plan with a busybox test pod to resolve DNS and attempt curl to vendor and to a random external host, plus a rollback strategy if traffic drops. Include concrete commands?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: write a NetworkPolicy in namespace payments selecting app=payment-service to allow egress to vendor.example.com:443 and to the Redis service on 6379, deny all other egress. Validate by applying",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Data backup first; 1) kubectl apply -f crd-v1.yaml; 2) kubectl -n ops get crd; 3) kubectl -n ops scale deploy/db-operator --replicas=1; 4) kubectl set image deployment/db-operator db-operator=repo/db-",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Identify compromised SAs and bindings, revoke elevated ClusterRoleBindings, rotate SA tokens and leaked credentials, enforce namespace-scoped RBAC, remove risky RoleBindings, apply a NetworkPolicy to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Inspect the mounted ConfigMap and the deployment: kubectl get cm app-config -o yaml; kubectl describe pod <pod> | grep -i config; if the key is missing, patch the ConfigMap and trigger a rollout: kube",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical understanding of Kubernetes NetworkPolicy, with real-world constraints like DNS-based destinations and in-cluster services. Candidates must translate security intent into concrete policy syntax and a verifiable test plan.\n\n## Key Concepts\n\n- NetworkPolicy scope (namespaced, podSelector, namespaceSelector)\n- Egress rules, DNS-based destinations vs IP blocks\n- Testing strategy with temporary pods and DNS/port checks\n- Rollback and safety checks for production readiness\n\n## Code Example\n\n```yaml\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: restrict-payments-egress\n  namespace: payments\nspec:\n  podSelector:\n    matchLabels:\n      app: payment-service\n  policyTypes:\n  - Egress\n  egress:\n  - to:\n    - dnsName: vendor.example.com\n      ports:\n      - protocol: TCP\n        port: 443\n  - to:\n    - podSelector:\n        matchLabels:\n          app: redis\n    ports:\n    - protocol: TCP\n      port: 6379\n```\n\n> Note: dnsName is supported by some CNIs (e.g., Calico). If unsupported, substitute with exact IP blocks after validating DNS resolution in a test pod.\n\n## Follow-up Questions\n\n- What are the risks of DNS-based policies and DNS TTL caching?\n- How would you automate rollback in CI/CD when a policy blocks legitimate traffic?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-23",
        "questionId": "q-16970",
        "question": "In a Kubernetes StatefulSet of 3 replicas uses a Secret projected into a volume at /etc/db/creds for DB credentials; the Secret is rotated externally. Provide a concrete, end-to-end plan to rotate the secret with zero downtime, including: verifying the projection, triggering a controlled rolling restart, implementing a graceful reload or sidecar to pick up new creds without service disruption, and validating that all pods are using the new credentials by performing a DB handshake from each pod?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Rotate the secret in the external store, then perform a rolling restart of the StatefulSet to remount the projected secret. Commands: kubectl rollout restart statefulset/my-app -n ns; kubectl rollout",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "**Reproduction Steps:** 1. Identify affected AppConfig CRs and observe current state: `kubectl get appconfig -n prod` 2. Apply a test change: `kubectl patch appconfig <name> -n prod --type=merge -p '{\"spec\":{\"replicas\":…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a canary StatefulSet redis-canary:7.0 with its own PVC and a headless service redis-canary; deploy a weighted route (e.g., 20% canary) via Istio VirtualService; monitor with redis-cli and INFO",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan to reproduce and fix: identify pods using both inline ephemeral volumes and PVCs, collect PV/PVC events and CSI logs, reproduce with controlled concurrency in a test namespace, then implement a s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nUnderstanding secret rotation with projected volumes and zero-downtime deployments is essential at scale.\n\n## Key Concepts\n- Secret projection and automatic updates\n- StatefulSet rolling restart semantics\n- Zero-downtime rollout with readiness gates\n- In-pod credential reload patterns (SIGHUP/sidecar)\n\n## Code Example\n```bash\n# example commands\nkubectl rollout restart statefulset/my-app -n ns\nkubectl rollout status statefulset/my-app -n ns\nfor p in $(kubectl get pods -l app=my-app -n ns -o name); do\n  kubectl exec $p -n ns -- cat /etc/db/creds/username\ndone\n```\n\n## Follow-up Questions\n- How would you handle a credential format change (e.g., new field) without downtime?\n- How would you test rollback safety if the new creds fail?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-24",
        "questionId": "q-28707",
        "question": "Advanced CKA: In a cluster with a CSI driver and VolumeSnapshot integration, an exercise requires restoring a production PVC from a VolumeSnapshot into a test namespace to validate DR readiness. After restore, a set of Pods fail to mount due to mismatched RestoreSize, SnapshotContent status, or StorageClass parameters. Provide a reproducible kubectl-based plan to (1) identify affected PVCs and snapshots, (2) inspect VolumeSnapshotContent, VolumeSnapshot, and CSI logs, (3) reproduce the restore in a test namespace with controlled concurrency, and (4) implement a robust DR upgrade path (clone vs reuse, adjust StorageClass, quota, and post-restore data integrity checks)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe the ImagePullBackOff, verify a dockerconfig secret named reg-secret exists in prod (kubectl get secret reg-secret -n prod -o json), ensure imagePullSecrets on the Deployment or SA include reg",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: First, identify existing NetworkPolicies using `kubectl get netpol -A` and inspect their egress rules with `kubectl describe netpol <name> -n <namespace>`. Next, deploy a debug pod in the API namespace and use `ku…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: 1) confirm SA and RBAC deny: kubectl -n analytics get sa db-import-sa; kubectl -n analytics describe sa db-import-sa. 2) test access: kubectl auth can-i get secrets --as=system:serviceaccount:an",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach centers on deterministic DR restores using snapshot lineage. Identify the PVCs and their VolumeSnapshots, verify SnapshotContent ReadyToUse and RestoreSize, then examine CSI driver logs for R",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n DR readiness hinges on reliable volume restoration from snapshots. Candidates must map restore workflows to KV-level checks and validate idempotent behavior under concurrency.\n\n## Key Concepts\n - VolumeSnapshot and VolumeSnapshotContent lifecycle\n - CSI driver restore semantics and ReadyToUse/RestoreSize\n - StorageClass parameters affecting restores and topology\n - Controlled concurrency for reproducible tests\n\n## Code Example\n```yaml\n# kubectl commands illustrating the flow\nkubectl get volumesnapshots -A\nkubectl describe volumesnapshots <snapname> -n <ns>\nkubectl get pvc -n <ns>\nkubectl describe pvc <pvc> -n <ns>\n```\n\n## Follow-up Questions\n- How would you validate data integrity post-restore in a multi-tenant environment?\n- What failure modes exist if RestoreSize is misreported, and how would you mitigate them?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-26T03:04:34.797Z",
    "version": 1
  }
]