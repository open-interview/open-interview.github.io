[
  {
    "id": "test-cka",
    "channelId": "cka",
    "channelName": "CKA",
    "title": "CKA Knowledge Test",
    "description": "Test your understanding of CKA with 25 questions.",
    "questions": [
      {
        "id": "cka-q-0",
        "questionId": "q-25195",
        "question": "In a Kubernetes namespace, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. When applying a scale-up to 6 replicas, new Pods remain Pending and never reach Ready. Outline a beginner-friendly plan using kubectl to determine if a ResourceQuota or LimitRange is blocking scheduling, and implement a minimal fix (adjust requests, or quota) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a reconciler that watches Namespace ResourceQuota objects and per-namespace usage via the metrics server, computes remaining headroom, and gates new Pod creations. If a request would exceed quo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by inspecting namespace quota and limits, then adjust. Steps: kubectl describe quota -n ns; kubectl get limitrange -n ns; kubectl describe deployment mydep -n ns; kubectl scale deployment mydep",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Run: kubectl get quota -n apps; kubectl describe quota -n apps; kubectl get deploy web -n apps -o jsonpath='{.spec.template.spec.containers[*].resources}'; kubectl get pods -n apps -o wide. If usage m",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check how the DaemonSet reads its config: 1) kubectl get cm log-collector-config -n ns -o yaml; 2) kubectl get pods -n ns -l app=log-collector; 3) kubectl exec -n ns -it <pod> -- cat /etc/log-collecto",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical debugging of scheduling constraints caused by ResourceQuota and LimitRange, a common beginner hurdle.\n\n## Key Concepts\n- ResourceQuota and LimitRange impact pod scheduling\n- How to inspect quotas, limits, and events with kubectl\n- Rollout and scale verification\n\n## Code Example\n```javascript\napiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: compute-resources\nspec:\nhard:\n  requests.cpu: \"4\"\n  requests.memory: 8Gi\n  limits.cpu: \"8\"\n  limits.memory: 16Gi\n```\n\n## Follow-up Questions\n- How would you verify that increasing quota actually enables scheduling?\n- How would you handle existing Pending pods after quota adjustment?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-1",
        "questionId": "q-2282",
        "question": "Implement namespace-scoped network isolation for namespace 'team-a' using Kubernetes NetworkPolicy. Ensure pods in team-a can reach only the DNS service and a single internal log sink at 'log-sink.logging.svc.cluster.local:9200', and cannot reach other namespaces or external hosts. Provide the manifests (default-deny and allow rules), kubectl commands to apply, and verify using a tester pod with targeted tests for DNS, log sink, and external access?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create or verify the Secret and patch the Deployment to pull LOG_LEVEL from the ConfigMap and API_KEY from the Secret via envFrom/valueFrom. Add readiness and liveness probes (httpGet /health on 8080)",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Verify mounting: inspect the Pod and SecretProviderClass to confirm the secret is mounted via Secrets Store CSI and not env vars. Reproduce: kubectl exec <pod> -- cat /mnt/secrets-store/cred to confir",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a default-deny egress policy for namespace team-a, then add two allow rules: (1) DNS egress to kube-dns in kube-system on port 53 (TCP/UDP), (2) access to log-sink.logging.svc.cluster.local:",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Install cert-manager and create a ClusterIssuer; deploy a Certificate for ingress-tls referencing the issuer; cert-manager renews before expiry and updates the Secret. Ingress reloads automatically on",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design and implement strict network isolation, understand default-deny semantics, DNS, and inter-namespace access; requires translating policy into concrete manifests and validation steps.\n\n## Key Concepts\n- NetworkPolicy basics: podSelector, namespaceSelector, policyTypes\n- default-deny, explicit egress allows\n- DNS egress patterns to kube-system kube-dns pods\n- Testing with tester pod using dig and curl\n- Drift monitoring and auditing\n\n## Code Example\n```yaml\n# Default deny (egress)\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: team-a-egress-deny\n  namespace: team-a\nspec:\n  podSelector: {}\n  policyTypes:\n  - Egress\n  egress: []\n```\n```yaml\n# Allow DNS and Log Sink\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: team-a-egress-allow-dns-log\n  namespace: team-a\nspec:\n  podSelector: {}\n  policyTypes:\n  - Egress\n  egress:\n  - to:\n    - namespaceSelector:\n        matchLabels:\n          name: kube-system\n    ports:\n    - protocol: UDP\n      port: 53\n  - to:\n    - namespaceSelector:\n        matchLabels:\n          name: kube-system\n      podSelector:\n        matchLabels:\n          k8s-app: kube-dns\n    ports:\n    - protocol: TCP\n      port: 53\n  - to:\n    - namespaceSelector:\n        matchLabels:\n          name: logging\n    ports:\n    - protocol: TCP\n      port: 9200\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-2",
        "questionId": "q-19978",
        "question": "You're debugging a Deployment whose Pods in namespace sales cannot reach an external SMTP server to send emails. A restrictive NetworkPolicy appears to block egress to the internet, while DNS works. Describe exact steps to diagnose from within a Pod using an ephemeral container (kubectl debug), verify DNS, MTU, and egress rules, and propose a minimal, verifiable fix?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Plan:** Verify the projected token path and expiry within a running pod; reproduce by inspecting the token file and decoding the JWT to confirm the expiration claim. Implement a minimal refresh mechanism using a sidec…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify current RBAC bindings across all namespaces; test app-sa permissions by running kubectl auth can-i --as=system:serviceaccount:apps:app-sa get secrets -n apps and kubectl auth can-i --as=syste",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Plan:** Inspect the CronJob spec and active Jobs using kubectl to confirm the current concurrencyPolicy setting and identify any overlapping runs. Patch the CronJob to set concurrencyPolicy to Forbid and configure a r…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Attach an ephemeral container to a running sales-app Pod to run dig to verify MX, curl to SMTP host, and ping to test reachability. Inspect namespace NetworkPolicy rules, then add the narrow allow rul",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes hands-on debugging of network policies and ephemeral containers, a common real-world workflow.\n\n## Key Concepts\n- NetworkPolicy egress filtering\n- Ephemeral containers via kubectl debug\n- DNS validation, MTU considerations, and SMTP connectivity\n\n## Code Example\n```bash\n# Attach an ephemeral container to diagnose\nkubectl debug deploy/sales-app -n sales --image=busybox --target=web --container=diag\n\n# Inside the ephemeral container\nnslookup smtp.example.com\nnc -zv smtp.example.com 25\nping -c 3 smtp.example.com\n```\n\n## Follow-up Questions\n- How would you prevent recurrence without widening all egress? \n- What changes would you make to CI to validate network policies against SMTP in test runs?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-3",
        "questionId": "q-7449",
        "question": "In a 3-node Kubernetes cluster, a MutatingWebhookConfiguration backed by an external service intermittently returns 5xx during a deployment of a critical API. Outline a precise debugging plan to restore reliability with zero downtime: verify TLS/CA in the webhook, test reachability from a pod, adjust timeouts/backoffs, implement a safe in-cluster fallback webhook and patch the configuration, and validate via api-server logs and rollout status. Include exact commands and a rollback path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check the node's disk usage and free up space (e.g., clear logs, remove large unused files), then monitor the node",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify the SA and deny rule: kubectl describe sa prod-sa -n prod; kubectl get rolebindings -n prod | grep prod-sa. Test: kubectl auth can-i get secret -n prod --as=system:serviceaccount:prod:prod-sa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Namespace-per-tenant with hard ResourceQuota and LimitRange, a default-deny NetworkPolicy, PodSecurity admission, and RBAC scoping admin roles to each tenant with separate ServiceAccounts. Enforce ima",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: 1) `kubectl get mutatingwebhookconfiguration -A; kubectl describe <name>` to verify service configuration, CA bundle, and timeoutSeconds settings; 2) From a cluster pod, execute `curl -k -s https://<service>:443/v…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-world webhook reliability skills including TLS validation, timeout management, and safe fallback implementation while maintaining API availability.\n\n## Key Concepts\n\n- MutatingWebhookConfiguration validation and troubleshooting\n- TLS/CA certificate management and rotation procedures\n- In-cluster connectivity testing using pod-based curl\n- Fallback webhook strategies and failurePolicy implications\n- Comprehensive monitoring through API server logs and rollout status\n\n## Code Example\n\n```bash\nkubectl patch mutatingwebhookconfiguration <name> -p '{\"webhooks\":[{\"name\":\"<webhook-name>\",\"clientConfig\":{\"service\":{\"name\":\"fallback-webhook\",\"namespace\":\"default\",\"path\":\"/validate\"}},\"failurePolicy\":\"Ignore\"}]}' --type=merge\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-4",
        "questionId": "q-23049",
        "question": "You’re operating a 3-node Kubernetes control plane with an external etcd, and TLS certificates for kube-apiserver and etcd are expiring. Design a reproducible, kubectl-driven plan to rotate these certificates without downtime, including verification steps for clients during rotation and a safe rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Rotate kube-apiserver and etcd certificates without downtime using a controlled rolling-update workflow: 1. **Backup existing certificates** - Copy all current TLS certificates and CA bundles from `/etc/kubernetes/pki`…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Start by verifying health with etcdctl: endpoint status, member list, and health metrics; check RAM use and watch counts. Defragment and compact to reclaim space. Take a consistent snapshot for rollba",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Secrets Store CSI with Vault and a refresh-aware sidecar to avoid stale creds in memory. Ensure the app reloads credentials on a trigger (SIGHUP or file change) and set a 60s propagation wind",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: reproduce in a test namespace, inspect pod volumes to confirm the ConfigMap mounted as /etc/dns-config, compare the actual file with kubectl describe/pod and the ConfigMap data, trigger a reload",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question evaluates a candidate's ability to plan and execute certificate rotation in a live Kubernetes cluster with minimal or no downtime. It assesses understanding of TLS certificate management, control plane high availability, and operational safety procedures.\n\n## Key Concepts\n- TLS certificate rotation in Kubernetes\n- Rolling control-plane updates\n- etcd TLS configuration\n- kubeconfig refresh and client validation\n- Safe rollback strategies and backups\n\n## Code Example\n```bash\n# 1. Backup existing certificates\ncp -r /etc/kubernetes/pki /etc/kubernetes/pki.bak\ncp -r /etc/etcd/pki /etc/etcd/pki.bak\n\n# 2. Generate new CA (using kubeadm)\nkubeadm init phase certs ca --cert-dir /etc/kubernetes/pki\n\n# 3. Generate new kube-apiserver certificate\nkubeadm init phase certs apiserver --cert-dir /etc/kubernetes/pki\n\n# 4. Generate new etcd CA and certificates\nkubeadm init phase certs etcd-ca --cert-dir /etc/etcd/pki\nkubeadm init phase certs etcd-server --cert-dir /etc/etcd/pki\n\n# 5. Restart kubelet to pick up new certificates\nsystemctl restart kubelet\n\n# 6. Verify certificates\nopenssl x509 -in /etc/kubernetes/pki/apiserver.crt -noout -dates\nopenssl x509 -in /etc/etcd/pki/server.crt -noout -dates\n```",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-5",
        "questionId": "q-28381",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas runs a config-driven service that reads a ConfigMap at startup to set feature flags. The config changes are not consistently reflected; some pods boot with stale values. Outline a beginner-friendly plan using kubectl to diagnose whether the ServiceAccount used by the pods lacks permission to read the ConfigMap, and implement a minimal fix (e.g., create a Role/RoleBinding to grant read access and trigger a rollout) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Identify affected PVCs/pods by node rack label (rack-1). Inspect PV events and VolumeAttachment/CSI logs for topology mismatch. Reproduce in a test namespace by tainting or labeling a node as rack-1 a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Inspect the pod’s ServiceAccount and RBAC bindings, then verify ConfigMap access. Steps: 1) identify SA from the pod: kubectl get pod <name> -n <ns> -o jsonpath='{.spec.serviceAccountName}'; 2) test a",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Start by verifying health with etcdctl: endpoint status, member list, and health metrics; check RAM use and watch counts. Defragment and compact to reclaim space. Take a consistent snapshot for rollba",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement namespace isolation per team, enforce resource quotas and limits, configure NetworkPolicy for traffic control, and apply Pod Security Standards via OPA Gatekeeper. Deploy Horizontal Pod Autoscaling with CPU/me…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests RBAC basics and real-world troubleshooting of startup-time configuration. Candidates must connect kubectl checks to a concrete fix, ensuring least privilege and minimal disruption.\n\n## Key Concepts\n\n- RBAC: Roles, RoleBindings, ServiceAccounts\n- ConfigMaps and startup config loading\n- kubectl auth can-i, jsonpath\n- Rolling out a fix with a deployment rollout\n\n## Code Example\n\n```bash\n# Identify SA for a pod\nkubectl get pod <pod-name> -n <ns> -o jsonpath='{.spec.serviceAccountName}'\n\n# Check access as that SA\nkubectl auth can-i get configmaps -n <ns> --as=system:serviceaccount:<ns>:<sa>\n\n# If denied, create Role and RoleBinding\ncat <<EOF | kubectl apply -f -\napiVersion: rbac.authorization.k8s.io/v1\nkind: Role\nmetadata:\n  namespace: <ns>\n  name: cm-read\nrules:\n- apiGroups: [\"\"]\n  resources: [\"configmaps\"]\n  verbs: [\"get\"]\n---\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  name: cm-read-binding\n  namespace: <ns>\nsubjects:\n- kind: ServiceAccount\n  name: <sa>\n  namespace: <ns>\nroleRef:\n  kind: Role\n  name: cm-read\n  apiGroup: rbac.authorization.k8s.io\nEOF\n\n# Redeploy to pick up new permissions\nkubectl rollout restart deployment <deploy-name> -n <ns>\n\n# Verify startup uses updated ConfigMap\nkubectl logs -l app=<app-label> -n <ns> | tail -n 20\n```\n\n## Follow-up Questions\n\n- How would you audit RBAC across the cluster for least privilege?\n- What tests verify the fix persists after ConfigMap changes?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-6",
        "questionId": "q-24063",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. After a ConfigMap update, requests intermittently return 502s while pods stay Ready. The API reads a feature flag from the ConfigMap at startup. Outline a beginner-friendly plan using kubectl to verify the ConfigMap key exists and is mounted, implement a minimal fix (add the missing key or restart pods), and provide verification steps with load testing that 502s disappear?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify the Deployment’s pod template imagePullSecrets and the ServiceAccount bindings. Check pod events for ImagePullBackOff, inspect the SA and secret, then rebind the secret if missing or invalid: k",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan to reproduce and fix: identify pods using both inline ephemeral volumes and PVCs, collect PV/PVC events and CSI logs, reproduce with controlled concurrency in a test namespace, then implement a s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Locate cluster-wide bindings for the SA, replace with a namespace-scoped Role/RoleBinding, then rollout the Deployment to use the new SA with a rolling update. Validate least privilege via kubectl aut",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Inspect the mounted ConfigMap and the deployment: kubectl get cm app-config -o yaml; kubectl describe pod <pod> | grep -i config; if the key is missing, patch the ConfigMap and trigger a rollout: kube",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical Kubernetes troubleshooting: ConfigMaps, volume mounts, and rollout flow, plus concrete commands.\n\n## Key Concepts\n- ConfigMap keys and volume mounts\n- Pod describe, events, and rollout restart\n- Verifying service endpoints and basic load verification\n\n## Code Example\n```bash\nkubectl get cm app-config -o yaml\nkubectl describe pod <pod> | grep -i ConfigMap\nkubectl patch cm app-config -p '{\"data\":{\"FEATURE_FLAG\":\"on\"}}'\nkubectl rollout restart deploy api\n``` \n```\n\n## Follow-up Questions\n- How would you handle multiple ConfigMaps or key renames without downtime?\n- What are pitfalls if the config key is read only at startup?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-7",
        "questionId": "q-12786",
        "question": "In a Kubernetes cluster, a Deployment with 4 replicas serves an internal REST API behind a ClusterIP Service. Under load, some requests intermittently fail with 502s. Outline a beginner-friendly plan using kubectl to diagnose whether an egress NetworkPolicy blocks calls to an upstream auth service, and implement a minimal fix with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check HPA and metrics first: kubectl get hpa; kubectl describe hpa <name>. Verify Metrics Server is healthy: kubectl get deployment metrics-server -n kube-system and kubectl top nodes. Review resource",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Inspect NetworkPolicy and Endpoints in the API namespace. Steps: 1) kubectl get networkpolicy -n api-namespace; 2) kubectl get endpoints upstream-auth -n auth-namespace; 3) kubectl run tmp --rm -i --image=alpine -- sh -…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Verify with kubectl: read the updated secret data, confirm the mounted file contains the new value, then trigger a controlled rollout to remount the CSI secret without downtime. Steps: kubectl get sec",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "First cordon the node: kubectl cordon <node>. Then drain: kubectl drain <node> --ignore-daemonsets --delete-local-data. Monitor rescheduling with kubectl get pods -A -o wide; verify the Deployment sho",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests diagnosing a common, real-world network-policy issue that beginners can resolve with basic kubectl skills.\n\n## Key Concepts\n- NetworkPolicy\n- Egress rules\n- kubectl troubleshooting\n- Endpoints and DNS\n\n## Code Example\n```yaml\n# Example NetworkPolicy fragment allowing traffic to upstream-auth\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: allow-egress-to-auth\nspec:\n  podSelector: {}\n  policyTypes:\n  - Egress\n  egress:\n  - to:\n    - namespaceSelector:\n        matchLabels: {name: auth}\n    ports:\n    - protocol: TCP\n      port: 80\n```\n\n## Follow-up\nAfter applying the NetworkPolicy, verify connectivity with the same wget command and monitor for 502 errors resolution.",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-8",
        "questionId": "q-1737",
        "question": "You're deploying a 3-replica Deployment in Kubernetes for a payment app. Suddenly 2 pods crash with CrashLoopBackOff. Describe a practical debugging workflow to identify if the issue is container startup, config, resources, or image, and outline the exact kubectl commands and file checks you would perform. Include how you would propose a minimal fix and a rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Debug CrashLoopBackOff by isolating startup, config, resources, or image issues. Run: kubectl get pods -l app=payment; kubectl describe pod <pod>; kubectl logs <pod>; kubectl logs <pod> --previous; ku",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Identify drifted objects and etcd revisions with kubectl get all --all-namespaces -o yaml, then verify revisions via etcdctl get /registry/...; inspect member health and WAL/compaction with etcdctl en",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a fault-tolerant Spark-on-Kubernetes deployment: run the streaming job with dynamic allocation; enable checkpointing to durable storage; use taints/tolerations and a PodDisruptionBudget; set ba",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: deploy a parallel CSI-backed Deployment with its own PVC, copy data, cut over traffic, verify integrity, and provide a rollback path. Steps: apply canary.yaml; apply canary-pvc.yaml; create data",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical debugging steps for Kubernetes CrashLoopBackOff, focusing on actionable commands.\n\n## Key Concepts\n\n- CrashLoopBackOff diagnosis\n- kubectl tooling (describe, logs, events)\n- ConfigMap/Secret patching\n- Rollback strategies\n\n## Code Example\n\n```bash\nkubectl get pods -l app=payment\n```\n\n## Follow-up Questions\n\n- How would you distinguish image pull errors from runtime crashes?\n- What are the risks of rolling back a deployment in production?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-9",
        "questionId": "q-6650",
        "question": "**Scenario**: A single-node Kubernetes cluster runs a StatefulSet of 2 Pods hosting a Java app. Over the last 15 minutes, pods intermittently crash with ExitCode 137. Provide a beginner‑friendly, kubectl‑driven debugging plan to confirm root cause and apply a minimal fix, including verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use kubectl set image deployment/myapp myapp=myrepo/myapp:v1.0.2; wait for rollout to complete with kubectl rollout status deployment/myapp; verify all pods are Ready and running the new image with ku",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "kubectl rollout status deployment/web; kubectl get pods -l app=web -o wide; kubectl describe pod <pod>; kubectl get events --sort-by=.lastTimestamp; kubectl get configmap web-config -o yaml; adjust re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Rotate kube-apiserver and etcd certificates without downtime using a controlled rolling-update workflow: 1. **Backup existing certificates** - Copy all current TLS certificates and CA bundles from `/etc/kubernetes/pki`…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Begin by inspecting pod state and events: kubectl describe pod <pod>, kubectl get events; check memory via kubectl top pod. ExitCode 137 usually means OOMKilled. Compare container limits to node memor",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical debugging mindset for beginner knowledge of Kubernetes, kubectl, and resource tuning.\n\n## Key Concepts\n\n- OOMKilled and exit codes\n- Resources requests/limits\n- Pod and node diagnostics (describe, logs, top)\n- Basic rolling update for minimal fix\n\n## Code Example\n\n```bash\nkubectl describe pod <pod>\nkubectl logs <pod> --previous\nkubectl top node\n```\n\n## Follow-up Questions\n\n- How would you prevent recurrence without over-allocating memory?\n- How would you add a readiness/liveness probe to catch this issue earlier?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-10",
        "questionId": "q-10253",
        "question": "An 3-node MongoDB replica set deployed as a StatefulSet with a Headless Service and PVCs across three zones must be upgraded from mongo:5.0 to mongo:6.0 with zero downtime. Outline a concrete plan that (1) upgrades non-primary members first via StatefulSet RollingUpdate, (2) handles elections and majority during upgrade, (3) notes that PodDisruptionBudget doesn't apply to StatefulSets, and (4) includes concrete kubectl and mongo shell commands to verify health after each pod upgrade?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Identify affected objects via kubectl: all pvc and volumesnapshot in all namespaces, check VolumeAttachment/VolumeSnapshot statuses. Inspect CSI driver pods (logs) and the snapshot controller logs for",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Upgrade non-primary members first with a StatefulSet RollingUpdate to 6.0; after each pod is ready, run rs.status() and db.version() to confirm SECONDARY state and replication health. When non-primari",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Update the ConfigMap with kubectl (apply or createconfigmap) and then force a rolling restart: kubectl rollout restart deployment/my-deploy; kubectl rollout status deployment/my-deploy. Verify new pod",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose migrating to a projected secret mounted as files, not env vars, and add a credential reload path. Use a sidecar that watches the secret file and signals the main process (SIGHUP) to reload the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests planful, real‑world upgrading of a stateful MongoDB deployment on Kubernetes, focusing on upgrade order, replica set health, and disruption controls.\n\n## Key Concepts\n- StatefulSet RollingUpdate semantics\n- MongoDB replica set elections and majority safety\n- Upgrading secondary members before primary\n- rs.stepDown() usage for primary upgrade\n- Validation via rs.status(), db.version(), and simple write tests\n- PDB limitations with StatefulSets\n\n## Code Example\n```javascript\nkubectl patch statefulset mongo -p '{\"spec\":{\"updateStrategy\":{\"type\":\"RollingUpdate\",\"rollingUpdate\":{\"partition\":1}}}}'\n// After each pod update\nkubectl exec -it mongo-0 -- mongo --eval \"rs.status();db.version()\"\n```\n\n## Follow-up Questions\n- How would you monitor replication lag and upgrade failure signals during the process?\n- How would this change with a 5-node cluster or sharded topology?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-11",
        "questionId": "q-25533",
        "question": "In a 5-node Kubernetes cluster, a namespace runs a microservice that must connect only to a single external Postgres DB endpoint over TLS. Outbound traffic is unrestricted. Design a precise plan to enforce egress restrictions using a NetworkPolicy, validate that only the DB endpoint is reachable from pods in that namespace, and demonstrate verification steps with concrete kubectl commands and a temporary test pod. Include rollback approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify the Secrets Store CSI mount path inside a pod, inspect the SecretProviderClass for the secret source and refreshPeriod, and reproduce the value by reading the mounted secret file in a pod. Rota",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a namespace-scoped NetworkPolicy that allows egress to the DB endpoint (port 5432, TLS) and DNS (port 53) only, then apply a deny-all-egress baseline if supported. Validate with a netshoot pod:",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Trigger a fresh secret refresh by rolling the CSI driver mount cache and then restarting the deployment: 1) kubectl rollout restart deployment/your-app. 2) Verify mount details with kubectl describe p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Investigate the CSI mount path and refresh policy, using kubectl describe secretproviderclass and CSI driver logs. From a sample pod, read /mnt/secrets-store/cred and the app’s env (if any) to reprodu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical ability to translate security policy into Kubernetes NetworkPolicy and verify with live tests.\n\n## Key Concepts\n\n- NetworkPolicy egress rules\n- DNS resolution vs IP allowlisting\n- Test pods for network policy validation\n- Rollback and auditing of policy changes\n\n## Code Example\n\n```yaml\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: allow-db-egress\n  namespace: target-ns\nspec:\n  podSelector: {}\n  policyTypes:\n  - Egress\n  - Ingress\n  egress:\n  - to:\n    - ipBlock:\n        cidr: <DB_IP>/32\n    ports:\n    - protocol: TCP\n      port: 5432\n  - to:\n    - namespaceSelector:\n        matchLabels: {}\n    ports:\n    - protocol: UDP\n      port: 53\n```\n\n## Follow-up Questions\n\n- How would you monitor and alert if policy violations occur?\n- How would you adapt this for a dynamic DB endpoint (DNS rewrite or multiple replicas)?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-12",
        "questionId": "q-29672",
        "question": "Advanced CKA: In a multi-tenant cluster with a per-namespace StorageClass that enforces quotas, a quota-controller rollout causes dynamic CSI provisioning to create PVCs that never bind under high parallel Job submissions. PVCs appear Pending, VolumeAttachment stalls, and Pods queue. Provide a reproducible kubectl-based plan to (1) identify affected PVCs and Jobs by namespace, (2) inspect PVC/PV events and quota/controller logs, (3) reproduce in a test namespace with controlled concurrency, and (4) implement a safe upgrade path (adjust quota checks, make binding atomic, or throttle provisioning) with validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: identify the namespace and policy; create a debug pod and test DNS and TLS reachability to api.partner.com; inspect current policy egress rules and ports; update the NetworkPolicy to allow egres",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify the primary with rs.status(); on primary run db.fsyncLock(); create VolumeSnapshots for each mongo-data PVC (one per StatefulSet pod); wait for all snapshots to become ReadyToUse; run db.fsyn",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: inspect NetPol in payments, verify egress to gateway:443 and podSelector matches app labels; use a debug pod (kubectl run dbg -n payments --image=curlimages/curl:8.2 --command -- sh) to curl gat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Step 1: Identify Affected PVCs and Jobs by Namespace** ```bash # List all Pending PVCs grouped by namespace kubectl get pvc -A --field-selector=status.phase=Pending -o json | \\ jq -r '.items[] | \"\\(.metadata.namespace…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates a candidate's ability to diagnose quota-driven provisioning race conditions in Container Storage Interface (CSI) environments, design incremental remediation strategies, and execute safe cluster upgrades without disrupting tenant workloads.\n\n## Key Concepts\n\n- **CSI Provisioning Lifecycle**: Understanding the sequence from PVC creation through provisioner invocation, PV creation, VolumeAttachment, and final pod binding—and where race conditions can occur at each stage.\n- **Namespace Quotas and Admission Order**: How ResourceQuota admission interacts with StorageClass binding, and why execution ordering matters when quota-controller rollouts coincide with high parallel Job submissions.\n- **Parallel Job Scheduling Under Quota**: How concurrent pod creation attempts compete for limited PVC quota, potentially causing a cascade of Pending states when the quota window closes mid-provisioning cycle.\n- **CSI VolumeBindingMode**: The critical distinction between Immediate and WaitForFirstConsumer binding modes, and how each affects PVC finalization timing under load.\n- **Quota Controller Behavior**: How the resource-quota-controller reconciles quota state and why rollout events can temporarily desynchronize quota accounting from actual resource allocation.\n\n## What Strong Answers Demonstrate\n\nA comprehensive answer should:\n1. Use kubectl-based diagnostics to isolate the problem scope (Pending PVCs, stuck VolumeAttachments, owner-referenced Jobs).\n2. Correlate quota-controller and CSI provisioner logs to pinpoint the desynchronization window.\n3. Provide a safe, reproducible test scenario that mirrors production concurrency patterns.\n4. Offer multiple remediation paths (quota headroom, atomic binding, throttling) with clear trade-offs and validation steps.\n\n## Common Pitfalls\n\n- Focusing solely on CSI provisioner logs without examining quota-controller state.\n- Applying quota increases without understanding the root cause (binding atomicity vs. capacity).\n- Skipping validation steps, which is critical when modifying cluster-wide quota enforcement.\n- Neglecting to address the WaitForFirstConsumer binding mode implications for multi-tenant scheduling.",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-13",
        "questionId": "q-11128",
        "question": "In a shared Kubernetes cluster used by three teams (prod, analytics, frontend), a team-analytics namespace occasionally hits node pressure causing evictions in other namespaces. Outline a concrete plan to enforce resource governance and isolation: (1) create per-namespace ResourceQuota and LimitRange, (2) configure NetworkPolicy and PodSecurityAdmission to enforce least privilege, (3) set up HorizontalPodAutoscaler with target utilization and fallback, and (4) how you'd monitor and roll out changes with minimal disruption. Provide sample YAML snippets and kubectl commands?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check PVC status with kubectl get pvc -n ns; describe pvc to see Pending reason; list PVs to find a binding. If none, create a Static PV and bind, or install a StorageClass and patch the PVC storageCl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Inspect the mounted TLS secret and its fingerprint inside a live pod: ```bash kubectl exec -n <namespace> <pod-name> -c <container> -- ls -la /etc/tls/ openssl x509 -fingerprint -noout -in /etc/tls/tls.crt ``` To reprod…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Namespace-per-tenant with hard ResourceQuota and LimitRange, a default-deny NetworkPolicy, PodSecurity admission, and RBAC scoping admin roles to each tenant with separate ServiceAccounts. Enforce ima",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create namespace-level ResourceQuota and LimitRange to bound resources; for example: hard: cpu: 2000m, memory: 4Gi, pods: 40, requests.cpu: 1000m, requests.memory: 2Gi, limits.cpu: 2000m, limits.memor",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-world governance for multi-namespace clusters, focusing on enforceable limits, isolation, and safe rollout.\n\n## Key Concepts\n\n- ResourceQuota\n- LimitRange\n- NetworkPolicy\n- Pod Security Admission\n- HorizontalPodAutoscaler\n- Metrics Server\n\n## Code Example\n\n```yaml\napiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: analytics-quota\n  namespace: analytics\nspec:\n  hard:\n    requests.cpu: \"1000m\"\n    requests.memory: \"2Gi\"\n    limits.cpu: \"2000m\"\n    limits.memory: \"4Gi\"\n    pods: \"40\"\n```\n\n## Follow-up Questions\n\n- How would you adapt quotas for bursty workloads?\n- How would you monitor quotas and alert on breaches?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-14",
        "questionId": "q-9482",
        "question": "In a 4-node Kubernetes cluster, a StatefulSet runs a replicated Postgres-like database with PVCs. Upgrading to image db:v2 requires a live data migration. Provide a precise, zero-downtime plan using a canary rollout, a separate MigrationJob, traffic shifting via Services, read/write window controls, and concrete kubectl steps to verify data integrity during and after the rollout?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a canary rollout: deploy db:v2 as a 1/4 canary behind the same Service, run a MigrationJob to perform the data migration against the primary, then scale the canary to 2 replicas while monitoring r",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Enable comprehensive Kubernetes auditing by first creating an audit policy that captures critical security events. Apply audit-policy.yaml with Metadata-level logging for system namespaces and Request-level logging for…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan calls for immutable deployments with Canary or Blue/Green, progressive traffic shifts (start 1–5% canary and ramp), readiness/liveness probes, and PodDisruptionBudget with automated rollback on f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Gatekeeper policy that enforces readOnlyRootFilesystem and runAsNonRoot on all Pods, excluding kube-system, and forbids hostNetwork. Create a ConstraintTemplate with a Rego rule, then a Co",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests real-world strategies for zero-downtime data migrations during stateful upgrades, a common pitfall in production clusters.\n\n## Key Concepts\n- Canary rollouts with StatefulSets\n- MigrationJob orchestration\n- Traffic shifting and service routing\n- Rollback criteria and observability\n\n## Code Example\n```javascript\n// Example commands for a canary migration\nkubectl apply -f canary.yaml\nkubectl rollout status sts/db\nkubectl create job migrate-db --image=db:v2 -- /bin/sh -c \"psql ... Migrate\"\n```\n\n## Follow-up Questions\n- How would you handle vacuum/reindex during migration?\n- What metrics signal readiness to promote to full rollout?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-15",
        "questionId": "q-21134",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. A ConfigMap named api-config supplies a startup flag that the app reads on boot. After updating the ConfigMap, requests intermittently fail with 500s while pods show Ready. Outline a beginner-friendly plan using kubectl to diagnose whether the ConfigMap is the cause and implement a minimal fix (e.g., trigger a rollout or mount refresh) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: 1) List NetworkPolicies in the target namespace and inspect their podSelector, namespaceSelector, and ports configurations to identify restrictive rules. 2) Create a test pod in a different namespace and curl the…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify the last good etcd snapshot with etcdctl snapshot status; stop etcd on the corrupted member; restore from the snapshot to a fresh data-dir; configure initial-cluster with all members; start e",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Rotate kube-apiserver and etcd certificates without downtime using a controlled rolling-update workflow: 1. **Backup existing certificates** - Copy all current TLS certificates and CA bundles from `/etc/kubernetes/pki`…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "1) Check deployment and ConfigMap mounts: kubectl describe deploy <name> and kubectl get configmap api-config -o yaml. 2) Verify the pod reads the config (kubectl exec <pod> -- cat /etc/api/config.jso",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of config-driven behavior and the need for rollout to refresh in Kubernetes.\n\n## Key Concepts\n- ConfigMaps as volume mounts\n- Pod startup vs runtime config reload\n- kubectl rollout restart and status\n- Observability with curl and pod inspection\n\n## Code Example\n```bash\nkubectl describe deploy <name>\nkubectl get configmap api-config -o yaml\n```\n\n## Follow-up Questions\n- How would you implement zero-downtime config refresh? \n- How would you monitor for config drift across pods?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-16",
        "questionId": "q-6085",
        "question": "In a Kubernetes cluster, a Deployment named web-app in namespace cka with 3 replicas targets a private registry image. Pods remain in ImagePullBackOff. Provide a concrete, beginner‑friendly debugging plan using kubectl to identify the root cause and implement a minimal fix, including verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Install cert-manager and create a ClusterIssuer; deploy a Certificate for ingress-tls referencing the issuer; cert-manager renews before expiry and updates the Secret. Ingress reloads automatically on",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: verify secret mounting and values, then fix and rollout. Steps: 1) inspect a pod: kubectl describe pod api-0; kubectl exec api-0 -- ls -l /etc/db/creds && cat /etc/db/creds/username; 2) inspect",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Diagnose ImagePullBackOff for daily-migration in data namespace with kubectl describe pod and kubectl describe job daily-migration to read events. Verify the secret regcred exists; if missing, create",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe the pod to surface ImagePullBackOff, then check the Deployment image path and tag. Inspect imagePullSecrets in the namespace and verify the default ServiceAccount binds to the secret. If miss",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical debugging of image pull failures in a beginner-friendly way, focusing on kubectl basics.\n\n## Key Concepts\n\n- ImagePullBackOff and private registry access\n- imagePullSecrets and ServiceAccount bindings\n- kubectl describe, kubectl get events, kubectl patch\n- Verifying and updating secrets without downtime\n- Rollout restart and validation basics\n\n## Code Example\n\n```javascript\nkubectl describe pod <pod-name>\nkubectl get serviceaccount default -o yaml\nkubectl get secret <secret-name> -o yaml\nkubectl patch serviceaccount default -p '{\"imagePullSecrets\":[{\"name\":\"<secret-name>\"}]}'\nkubectl rollout restart deployment/<name>\nkubectl rollout status deployment/<name>\n```\n\n## Follow-up Questions\n\n- How would you verify the registry is reachable from cluster nodes?\n- How would you test image pull before a full rollout?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-17",
        "questionId": "q-22563",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. Intermittent 502s appear from a namespace after a recent NetworkPolicy update. Outline a beginner-friendly plan using kubectl to determine if a NetworkPolicy is blocking traffic and implement a minimal fix (e.g., add an allow rule) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify mounting: inspect the Pod and SecretProviderClass to confirm the secret is mounted via Secrets Store CSI and not env vars. Reproduce: kubectl exec <pod> -- cat /mnt/secrets-store/cred to confir",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Explain how to build a Kubernetes operator (Go) that enforces zone-aware pod distribution for Deployments labeled app=svc in a multi-AZ cluster. Include a CRD (ZoneSpread) to declare minDistributions,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Our investigation reveals CPU contention within the critical namespace during peak hours. To stabilize the environment immediately, we will isolate the noisy workload using node taints, enforce per-namespace ResourceQuo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: 1) List NetworkPolicies in the target namespace and inspect their podSelector, namespaceSelector, and ports configurations to identify restrictive rules. 2) Create a test pod in a different namespace and curl the…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical debugging of NetworkPolicy impact on traffic—a common production issue that beginners encounter when securing Kubernetes clusters.\n\n## Key Concepts\n- Kubernetes NetworkPolicy basics, including selectors (podSelector, namespaceSelector) and ingress/egress rules\n- Cross-namespace traffic testing using kubectl\n- Safe, incremental policy changes with proper verification\n\n## Code Example\n```yaml\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: allow-api-ingress\n  namespace: myns\nspec:\n  podSelector: {}\n  ingress:\n  - from:\n    - namespaceSelector:\n        matchLabels:\n          project: my-project\n    ports:\n    - protocol: TCP\n      port: 80\n```",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-18",
        "questionId": "q-17751",
        "question": "In a multi-tenant Kubernetes cluster, a Deployment runs under the ServiceAccount 'tenant-app' and a ClusterRoleBinding grants broad cluster-level permissions. Provide an actionable remediation plan to enforce least privilege without downtime: locate over-privileged bindings, replace with a namespace-scoped Role/RoleBinding for the SA, roll the Deployment to use the new SA, verify with kubectl auth can-i and dry-run API calls, and confirm via audit logs that no excessive privileges were exercised during the migration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a 3-AZ HA deployment of a StatefulSet-backed service on Kubernetes. Use durable PVCs with a multi-AZ StorageClass, enable pod anti-affinity, set a PodDisruptionBudget of 1, and implement readin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "First, identify and remove the cluster-wide Secret read binding: `kubectl describe clusterrolebinding | grep -A 10 'Secrets\\|secrets'`. Remove any ClusterRoleBinding granting Secret read access to the analytics-job Serv…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Start by collecting etcd metrics, check disk I/O, CPU, memory on API server and etcd members; verify etcd quorum and member health. If disk pressure, relocate etcd data to faster disks or resize; take",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Locate cluster-wide bindings for the SA, replace with a namespace-scoped Role/RoleBinding, then rollout the Deployment to use the new SA with a rolling update. Validate least privilege via kubectl aut",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses hands-on RBAC hardening and migration without downtime.\n\n## Key Concepts\n- RBAC scoping: ClusterRoleBinding vs RoleBinding\n- Least privilege migration strategy\n- Verification via kubectl can-i and audit logs\n- Zero-downtime rollout techniques\n\n## Code Example\n```yaml\n# Example RoleBinding to namespace tenant-namespace\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  name: tenant-app-binding\n  namespace: tenant-namespace\nroleRef:\n  apiGroup: rbac.authorization.k8s.io\n  kind: Role\n  name: tenant-app-role\nsubjects:\n- kind: ServiceAccount\n  name: tenant-app\n  namespace: tenant-namespace\n```\n\n## Follow-up Questions\n- How would you automate discovery of over-privileged bindings?\n- How would you validate in CI before applying changes?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-19",
        "questionId": "q-3374",
        "question": "Scenario: In a 5-node cluster (3 workers, 2 control-planes) with many namespaces, introduce a new requirement to strictly enforce tenant-level network egress controls using Calico without downtime. Outline a concrete rollout plan to implement namespace-scoped egress policies with default deny, ensuring existing pods can still reach DNS, API server, and private registry. Include policy snippets, rollout steps, and verification methods?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan a two-step canary: deploy MutatingWebhookConfiguration v2 to a small set of namespaces, keep v1 active elsewhere. Update the CA bundle, tune timeoutSeconds to a safe value, and set failurePolicy",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Beginner-Friendly Plan to Fix Kubernetes HPA Not Scaling ### Step 1: Verify Metrics Server is Installed and Healthy ```bash # Check if Metrics Server deployment exists kubectl get deployment metrics-server -n kube-sy…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Label each tenant namespace; apply a canary Calico NetworkPolicy per-namespace that allows DNS, kubernetes API server, and private registry traffic, then enforce a cluster-wide default deny egress. Va",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Label pods with app=enricher in namespace enrichment. Create a NetworkPolicy that allows egress to api.example.com:443 from those pods (adjusting to IPs if FQDN isn’t supported). Validate with a test",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical knowledge of Calico NetworkPolicy, namespace-scoped controls, and safe multi-tenant rollouts without downtime.\n\n## Key Concepts\n- Calico NetworkPolicy and per-namespace scoping\n- Default deny egress baseline\n- Canary rollout across namespaces\n- In-cluster validation using test pods\n- Rollback and monitoring strategies\n\n## Code Example\n```javascript\n// Calico namespace policy (illustrative)\n{\n  apiVersion: \"projectcalico.org/v3\",\n  kind: \"NetworkPolicy\",\n  metadata: { name: \"ns-egress-allow-core\", namespace: \"tenant-a\" },\n  spec: {\n    types: [\"Egress\"],\n    egress: [\n      { destination: { ports: [{ protocol: \"UDP\", port: 53 }] } },\n      { destination: { nets: [\"10.0.0.0/8\"], ports: [{ protocol: \"TCP\", port: 443 }] } },\n      { action: \"Allow\", destination: { nets: [\"<PRIVATE_REGISTRY_CIDR>\"] } }\n    ]\n  }\n}\n```\n\n## Follow-up Questions\n- How would you validate a rollback and define rollback criteria?\n- What monitoring would you add to detect policy misconfigurations early?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-20",
        "questionId": "q-3703",
        "question": "In a 4-node Kubernetes cluster, enable mutual TLS across all namespaces using Linkerd, enforce mTLS by default, and verify that only services with Linkerd sidecars can communicate with a backend API on port 8080 while a plain pod cannot. Provide exact commands to install Linkerd, inject sidecars, deploy apps, and perform tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Install Linkerd control plane and check: curl -sL https://run.linkerd.io/install | sh; linkerd install | kubectl apply -f -; linkerd check; enable auto-injection on app namespace: kubectl label ns app",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Inspect the Pod volumes to confirm an ephemeral emptyDir vs a PVC, e.g., kubectl get pod <name> -o yaml | grep -i emptyDir or volumeMounts. If ephemeral, create a PVC (StorageClass: standard) and patc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Canary rollout with Argo Rollouts or Kubernetes native patterns. Start with 10% traffic to the new revision, monitor latency, error rate, and saturation against SLOs for 5–10 minutes, then ramp",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan a two-step canary: deploy MutatingWebhookConfiguration v2 to a small set of namespaces, keep v1 active elsewhere. Update the CA bundle, tune timeoutSeconds to a safe value, and set failurePolicy",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical service-mesh setup, mTLS enforcement, and verification of sidecar behavior in real clusters.\n\n## Key Concepts\n- Linkerd installation and validation\n- Namespace auto-injection and sidecar presence\n- mTLS enforcement and trust domain verification\n- In-cluster end-to-end testing with injected vs plain pods\n\n## Code Example\n```javascript\n# Install and check\ncurl -sL https://run.linkerd.io/install | sh\nlinkerd install | kubectl apply -f -\nlinkerd check\n\n# Enable auto-injection and roll out\nkubectl label ns app linkerd.io/inject=enabled\nkubectl rollout restart deploy -n app\n\n# Verify sidecars\nkubectl get pods -n app -o jsonpath='{.items[*].spec.containers[*].name}'\n\n# Tests\n# Injected pod test\nkubectl run test-injected --rm -it --image=busybox -n app -- sh\n# inside pod: wget or curl to backend\ncurl http://backend:8080/health\n\n# Plain pod test\nkubectl run test-plain --rm -it --image=busybox -n app -- sh\n# inside pod: curl to backend (should fail due to mTLS)\ncurl http://backend:8080/health\n```\n\n## Follow-up Questions\n- How would you rotate the trust anchor without downtime?\n- How would you monitor and alert if sidecar injection drifts across namespaces?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-21",
        "questionId": "q-21103",
        "question": "In a 3-node cluster, Namespace prod hosts a Deployment 'web-app' with 6 replicas. Each pod requests 200m CPU and 256Mi memory. Namespace prod has ResourceQuota: CPU 1, Memory 1Gi. New pods fail to schedule while existing run. Outline exact kubectl steps to verify quota enforcement, identify Pending pods, and implement a minimal fix (scale down the deployment by 2 or reduce requests) with verification?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: verify quota enforcement with kubectl describe quota prod-quota -n prod; identify Pending pods via kubectl get pods -n prod; read current requests from web-app Deployment (kubectl get deploy web",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Inspect CronJob lineage and events, then drill into image pull issues. Steps: kubectl describe cj <name> -n <ns>; kubectl get jobs -n <ns>; kubectl get pods -l job-name=<job> -n <ns>; kubectl describe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Inspect readiness failures and the probe configuration. Steps: kubectl describe pod <pod-name> to identify NotReady reasons; kubectl get deployment api -o yaml to review the current readinessProbe configuration; from wi…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Inspect the Service and Endpoints using `kubectl describe svc <name>` and `kubectl get endpoints <name>` to verify connectivity. Compare the Service's `targetPort` with the container's listening port by examining the De…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of Kubernetes scheduling and quota enforcement, and how to safely adjust live workloads without violating constraints.\n\n## Key Concepts\n- ResourceQuota; - Pending vs Running; - Pod resources requests/limits; - Scaling and patching deployments\n\n## Code Example\n```yaml\n# Example patch to reduce requests\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: web-app\nspec:\n  template:\n    spec:\n      containers:\n      - name: web-app\n        resources:\n          requests:\n            cpu: 150m\n            memory: 256Mi\n          limits:\n            cpu: 300m\n            memory: 512Mi\n```\n\n## Follow-up Questions\n- How would you prevent this in the future (quota-aware autoscaling)?\n- What metrics would you monitor to detect quota pressure early?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-22",
        "questionId": "q-26849",
        "question": "You run a 5-node Kubernetes cluster with a dedicated analytics namespace. A Job ingests data from a data warehouse service DW on port 5432. Implement a NetworkPolicy to allow egress from analytics-ingest pods to DW:5432 and deny all other egress. Provide a manifest, describe how you verify with kubectl, and how you simulate a blocked-traffic test and rollback if needed?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Audit RBAC bindings across all namespaces with kubectl get clusterrolebindings and kubectl get rolebindings --all-namespaces. Inspect SA-fin: kubectl describe sa SA-fin -n finance and test access with",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a NetworkPolicy analytics-egress in analytics namespace allowing egress from app=analytics-ingest pods to DW pods app=dw on TCP 5432, with a default deny for other egress. Apply the policy, ver",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Define a Deployment with 3 replicas and a ClusterIP Service. Add readinessProbe and livenessProbe, and set resources: requests cpu: 100m, memory: 128Mi; limits cpu: 500m, memory: 256Mi. Populate APP_M",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Identify the primary with rs.status(); on primary run db.fsyncLock(); create VolumeSnapshots for each mongo-data PVC (one per StatefulSet pod); wait for all snapshots to become ReadyToUse; run db.fsyn",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nUnderstanding and enforcing network isolation with NetworkPolicy is critical in multi-tenant clusters. This tests precise policy writing, namespace scoping, and end‑to‑end verification.\n\n## Key Concepts\n- NetworkPolicy basics and policyTypes: Egress\n- podSelector vs namespaceSelector\n- Egress rules and ports\n- Verification with test pod and policy tracing\n- Rollback and impact analysis\n\n## Code Example\n```yaml\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: analytics-egress\n  namespace: analytics\nspec:\n  podSelector:\n    matchLabels:\n      app: analytics-ingest\n  policyTypes:\n  - Egress\n  egress:\n  - to:\n    - podSelector:\n        matchLabels:\n          app: dw\n    ports:\n    - protocol: TCP\n      port: 5432\n```\n\n## Follow-up Questions\n- How would you adapt if DW is in another namespace or behind a separate service account?\n- How would you monitor policy enforcement and surface violations in CI/CD?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-23",
        "questionId": "q-19696",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. A CronJob is scheduled to perform nightly database cleanup, but occasionally two runs overlap and cause DB contention and slower cleanup. Outline a beginner-friendly plan using kubectl to verify CronJob concurrencyPolicy and active Jobs, and implement a minimal fix (e.g., set concurrencyPolicy to Forbid and adjust startingDeadlineSeconds) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: identify the namespace and policy; create a debug pod and test DNS and TLS reachability to api.partner.com; inspect current policy egress rules and ports; update the NetworkPolicy to allow egres",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Plan:** Inspect the CronJob spec and active Jobs using kubectl to confirm the current concurrencyPolicy setting and identify any overlapping runs. Patch the CronJob to set concurrencyPolicy to Forbid and configure a r…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "## Step 1: Identify Affected Resources Run a namespace-scoped audit to identify impacted pods and PVCs: ```bash # Identify affected pods stuck in Pending or ContainerCreating state kubectl get pods -A -o wide | grep -E…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe the pod to surface ImagePullBackOff, then check the Deployment image path and tag. Inspect imagePullSecrets in the namespace and verify the default ServiceAccount binds to the secret. If miss",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical, beginner-friendly ability to diagnose and fix overlapping CronJobs—a common reliability issue in Kubernetes environments. It validates understanding of the controls available to prevent concurrent workloads and ensure predictable maintenance windows.\n\n## Key Concepts\n- **CronJob**: A Kubernetes resource that runs Jobs on a recurring schedule.\n- **concurrencyPolicy**: Controls whether multiple Job instances can run simultaneously:\n  - `Allow` (default): Allows concurrent executions\n  - `Forbid`: Prevents concurrent runs; skips a new Job if one is already running\n  - `Replace`: Replaces the currently running Job with a new one when a new execution is due\n- **startingDeadlineSeconds**: Defines a window of time (in seconds) after the scheduled time during which the CronJob is allowed to start. If a scheduled run is missed (due to cluster issues or a concurrent Job), this setting ensures the Job still runs within the deadline rather than being skipped entirely.",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-24",
        "questionId": "q-26177",
        "question": "Advanced CKA: In a 5-node cluster with a CSI driver that supports ephemeral inline volumes, a feature gate enabling ephemeral inline volumes was flipped on and several prod pods fail to mount ephemeral volumes with errors like volumeMount failed to mount ephemeral inline volume. Provide a reproducible kubectl-based plan to (1) identify affected pods and their ephemeral inline volumes, (2) verify CSI driver and kubelet logs for inline volume creation errors, (3) reproduce the failure in a test namespace, and (4) implement a safe rollback (disable gate or revert to PVC-backed volumes) with validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt the Secrets Store CSI Driver with a configured refresh interval to auto-rotate mounted secrets from Vault. Add a lightweight controller (or use Reloader) that watches for Secret changes and annotates Deployments t…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify affected pods and their ephemeral inline volumes in the PodSpec; inspect CSI Controller and NodePublishVolume logs for errors; reproduce the failure in a test namespace with a pod using an ep",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Root cause is CSI detachment race leaving the PVC bound; fix by auditing PVC/PV status, finalizers, and CSI logs. Diagnose with kubectl describe pod/pvc, kubectl get pvc/pv, and events for detach erro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create namespace-level ResourceQuota and LimitRange to bound resources; for example: hard: cpu: 2000m, memory: 4Gi, pods: 40, requests.cpu: 1000m, requests.memory: 2Gi, limits.cpu: 2000m, limits.memor",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to diagnose advanced CSI ephemeral inline volume issues, including feature gates, pod specs, and logs, and to plan safe rollback without broad disruption.\n\n## Key Concepts\n\n- Ephemeral inline volumes and their lifecycle\n- CSI driver logs and kubelet publish/unstage paths\n- Feature gates and compatibility implications\n- Safe rollback strategies and validation checks\n\n## Code Example\n\n```javascript\n// example: list pods in prod with ephemeral volumes\nconst {execSync} = require('child_process');\ntry {\n  const pods = execSync(\"kubectl get pods -n prod -o json\").toString();\n  console.log(pods);\n} catch (e) {\n  console.error(e);\n}\n```\n\n## Follow-up Questions\n\n- What metrics would you collect to confirm rollback success?\n- How would you minimize blast radius during a rollback in production?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-21T15:36:13.730Z",
    "version": 1
  }
]