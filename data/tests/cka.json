[
  {
    "id": "test-cka",
    "channelId": "cka",
    "channelName": "CKA",
    "title": "CKA Knowledge Test",
    "description": "Test your understanding of CKA with 25 questions.",
    "questions": [
      {
        "id": "cka-q-0",
        "questionId": "q-5525",
        "question": "In a 3-replica Deployment, two pods show ImagePullBackOff after a tag bump. Using kubectl, outline a beginner-friendly debugging plan to identify the root cause (registry access, image name, credentials, or imagePullPolicy) and implement a minimal fix, including verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "First, inspect the MutatingWebhookConfiguration to verify current timeoutSeconds and failurePolicy settings. Then, reproduce the issue by creating a test Deployment in the prod namespace and use kubectl describe to exam…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check the pod events with kubectl describe pod <name> -n <ns> to confirm ImagePullBackOff and error. Inspect the deployment image tag with kubectl get deploy app -o yaml. Verify imagePullSecrets in th",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Run: kubectl describe quota -n <ns>; kubectl get pods -n <ns>; kubectl get deploy -n <ns>. Compare total pod requests against quota hard limits. If exhausted, patch the quota or reduce the Deployment'",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check DNS end-to-end: kubectl get ep web-app -n ns-a; kubectl get svc web-app -n ns-a; kubectl get pods -n kube-system -l k8s-app=coredns; kubectl logs -n kube-system -l k8s-app=coredns; kubectl exec",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nInterview context explains that this tests practical debugging of image pull failures and understanding imagePullSecrets, registry auth, and patching deployments.\n\n## Key Concepts\n\n- ImagePullBackOff and its relation to image and tag\n- imagePullSecrets in the target namespace\n- kubectl describe/get/patch commands\n- Rollout status verification\n\n## Code Example\n\n```yaml\n# Example patch to add imagePullSecrets\nspec:\n  template:\n    v1:\n      imagePullSecrets:\n        - name: reg-cred\n```\n\n## Follow-up Questions\n\n- What other causes can lead to ImagePullBackOff in a multi-tenant cluster?\n- How would you validate that all namespaces have valid imagePullSecrets without auditing manually?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-1",
        "questionId": "q-30284",
        "question": "Advanced CKA: In a 7-node cluster using a CSI driver that supports both dynamic PVC provisioning and inline ephemeral volumes, a driver regression creates a race during container startup where pods start with ephemeral volumes but later attempt to attach a persistent PVC, causing mount failures. Provide a reproducible kubectl-based plan to (1) isolate affected pods and PVCs, (2) inspect PV/PVC events and CSI driver logs, (3) reproduce with controlled concurrency in a test namespace, and (4) implement a safe mitigation path (disable inline ephemeral for affected workloads or enforce serial provisioning during upgrade) with validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check taint and pods: kubectl get nodes -o wide and kubectl describe node node-1 to confirm the taint. To remove: kubectl taint nodes node-1 NoSchedule-. Or add toleration to the Pod/Deployment: toler",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan to reproduce and fix: identify pods using both inline ephemeral volumes and PVCs, collect PV/PVC events and CSI logs, reproduce with controlled concurrency in a test namespace, then implement a s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: 1) List NetworkPolicies in the target namespace and inspect their podSelector, namespaceSelector, and ports configurations to identify restrictive rules. 2) Create a test pod in a different namespace and curl the…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Identify affected StatefulSets and PVCs, inspect PV/PVC events and CSI logs, verify topology constraints and Node readiness, reproduce in a test namespace with explicit topology labels and bounded con",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\\n\\nThis question probes your ability to diagnose complex CSI-related race conditions involving mixed inline ephemeral and persistent volumes, and to design reproducible tests and safe upgrade paths. It emphasizes precise visibility into pod, PVC, PV, and CSI driver events and the need for idempotent, conservative fixes that can be validated with minimal production risk.\\n\\n## Key Concepts\\n- CSI driver behavior with inline ephemeral and dynamic PVC provisioning\\n- Race conditions during attach/publish when volumes initialize concurrently\\n- Reproducibility in a test namespace with controlled concurrency\\n- Safe upgrade strategies: disable conflicting features, serial provisioning, and validation\\n\\n## Code Example\\n```javascript\n// Example kubectl commands to start investigation (placeholders):\nconsole.log('kubectl get pods -A -o json');\nconsole.log('kubectl describe pod <pod> -n <ns>');\n```\\n\\n## Follow-up Questions\\n- How would you instrument the CSI driver to detect inline vs persistent volume races in real time?\\n- What guardrails would you add to prevent this race in future upgrades or feature-gate changes?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-2",
        "questionId": "q-24109",
        "question": "Advanced CKA: In a 12-node Kubernetes cluster, a StatefulSet of 3 Redis replicas uses a CSI-backed PV per replica. After a namespace quota reconfiguration, PVC binding stalls in namespace 'cache-prod' while 'cache-dev' remains healthy. Provide a reproducible kubectl-based plan to (1) identify affected PVCs/pods, (2) inspect PV binding, PVC events, and quota usage, (3) reproduce the failure in a test namespace, and (4) implement a safe recovery: adjust quotas or StorageClass topology, perform a controlled rolling restart, and validate data availability with a canary read/write test?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by inspecting pod state and events: kubectl describe pod <pod>, kubectl get events; check memory via kubectl top pod. ExitCode 137 usually means OOMKilled. Compare container limits to node memor",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: identify affected PVCs/pods in cache-prod; inspect PVC status, VolumeBinding, and quota events; capture CSI/driver logs if available; reproduce in a test namespace by recreating failing PVCs; im",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "1) describe the Ingress TLS secret; 2) compare the secret's TLS cert with cert-manager's Certificate status; 3) from a pod, run openssl s_client to inspect the presented cert (CN, dates); 4) if old ce",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Update StatefulSet with RollingUpdate and partition 0 so pods update one by one, updating the primary last. Enforce a PodDisruptionBudget minAvailable=2. Rely on readiness via pg_isready and a preStop",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests expertise in PVC binding, quotas, and CSI integration under real-world constraints. Emphasizes reproducibility, safe rollback, and validation with data-plane tests.\n\n## Key Concepts\n\n- PVC binding lifecycle and VolumeBinding phase\n- Namespace quotas and their impact on dynamic provisioning\n- CSI driver behavior and logs debugging\n- Safe rollback and canary validation in stateful apps\n\n## Code Example\n\n```bash\n# Identify affected PVCs and pods\nkubectl get pvc -n cache-prod\nkubectl get pods -n cache-prod -l app=redis\n\n# Inspect details and quota\nkubectl describe pvc <pvc-name> -n cache-prod\nkubectl describe quota -n cache-prod\n\n# Reproduce in test namespace\nkubectl create namespace test-scenario\n```\n\n## Follow-up Questions\n\n- How would you monitor quota violations at scale?\n- How would you prevent quota-induced binding failures in future deployments?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-3",
        "questionId": "q-26177",
        "question": "Advanced CKA: In a 5-node cluster with a CSI driver that supports ephemeral inline volumes, a feature gate enabling ephemeral inline volumes was flipped on and several prod pods fail to mount ephemeral volumes with errors like volumeMount failed to mount ephemeral inline volume. Provide a reproducible kubectl-based plan to (1) identify affected pods and their ephemeral inline volumes, (2) verify CSI driver and kubelet logs for inline volume creation errors, (3) reproduce the failure in a test namespace, and (4) implement a safe rollback (disable gate or revert to PVC-backed volumes) with validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check taint and pods: kubectl get nodes -o wide and kubectl describe node node-1 to confirm the taint. To remove: kubectl taint nodes node-1 NoSchedule-. Or add toleration to the Pod/Deployment: toler",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify affected pods and their ephemeral inline volumes in the PodSpec; inspect CSI Controller and NodePublishVolume logs for errors; reproduce the failure in a test namespace with a pod using an ep",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Begin by confirming per-pod memory limits and actual usage with kubectl describe pod and metrics; then tune Spark: set spark.executor.memory and spark.driver.memory to safe values, add memoryOverhead,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check the node's disk usage and free up space (e.g., clear logs, remove large unused files), then monitor the node",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to diagnose advanced CSI ephemeral inline volume issues, including feature gates, pod specs, and logs, and to plan safe rollback without broad disruption.\n\n## Key Concepts\n\n- Ephemeral inline volumes and their lifecycle\n- CSI driver logs and kubelet publish/unstage paths\n- Feature gates and compatibility implications\n- Safe rollback strategies and validation checks\n\n## Code Example\n\n```javascript\n// example: list pods in prod with ephemeral volumes\nconst {execSync} = require('child_process');\ntry {\n  const pods = execSync(\"kubectl get pods -n prod -o json\").toString();\n  console.log(pods);\n} catch (e) {\n  console.error(e);\n}\n```\n\n## Follow-up Questions\n\n- What metrics would you collect to confirm rollback success?\n- How would you minimize blast radius during a rollback in production?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-4",
        "questionId": "q-13453",
        "question": "You have a 3-replica Deployment using hostPath data for a stateful app. A storage migration to a CSI-backed PV is required with zero downtime and no data loss. Outline a precise, concrete plan: create a parallel Deployment with new PVCs, copy data, perform a controlled switch, run data integrity checks, and a rollback path with explicit kubectl commands and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Identify whether a NetworkPolicy blocks cross-namespace traffic, e.g., kubectl get networkpolicy --all-namespaces; curl from a frontend pod to backend:80; if blocked, craft a minimal policy allowing t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a 3-node HA control plane across AZs with etcd snapshots every 5 minutes and 30-day retention. Enforce per-namespace RBAC with Roles and RoleBindings, isolate tenants with NetworkPolicy (Calico",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: deploy a parallel CSI-backed Deployment with its own PVC, copy data, cut over traffic, verify integrity, and provide a rollback path. Steps: apply canary.yaml; apply canary-pvc.yaml; create data",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: 1) kubectl get networkpolicy -A; 2) kubectl describe networkpolicy -n <ns> <name>; 3) from a pod in the deployment, kubectl exec -n <ns> <pod> -- curl -v https://payments.example.com; 4) if bloc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical data-mmigration skills: plan, execute, verify, and rollback with minimal downtime.\n\n## Key Concepts\n- Migration planning with zero downtime\n- Data integrity checks (checksums)\n- Canary/parallel deployment strategy\n- Rollback pathways and verifications\n\n## Code Example\n```javascript\n// Example commands in the plan are: apply canary.yaml, create data-copy Job, wait for completion, rollout status, switch service selector\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-5",
        "questionId": "q-14436",
        "question": "Scenario: A Kubernetes cluster uses a MutatingWebhookConfiguration to inject an observability sidecar into every Pod. A new webhook image is deployed, but some nodes fail TLS handshake due to CA rotation lag, causing mutations to fail and pods to start without the sidecar. Describe a concrete plan to achieve a zero-downtime webhook rollout, detect node-specific webhook failures, and verify all nodes use the new CA. Include exact commands, minimal YAML snippets for the webhook and canary rollout, and a rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a v1 CRD with a conversion webhook (strategy: Webhook) to translate spec.replicas to spec.replicaCount while preserving existing CRs. Deploy webhook service, upgrade the CRD, then run a one-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: canary rollout of the MutatingWebhookConfiguration and webhook deployment. Update the CABundle and migrate to a canary service; deploy image with a canary tag. Roll out with RollingUpdate, maxUn",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Identify affected VolumeSnapshotContents and snapshots, e.g. kubectl get volumesnapshotcontents -o json; inspect events with kubectl describe pvc, volumesnapshotcontents, and the snapshot-controller p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check endpoints with kubectl get endpoints my-api-svc; get selector with kubectl get svc my-api-svc -o jsonpath={.spec.selector}; compare with pods kubectl get pods -l app=my-api --show-labels. If mis",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to coordinate API server webhook rollout, CA rotation, and observability without service disruption. Highlights risk detection and rollback discipline.\n\n## Key Concepts\n\n- MutatingWebhookConfiguration\n- CA bundle rotation and webhooks\n- RollingUpdate strategy\n- Canary deployment patterns\n- cluster observability and rollback\n\n## Code Example\n\n```javascript\n// Pseudo-code using Kubernetes client (Node.js)\nconst k8s = require('@kubernetes/client-node');\nconst kc = new k8s.KubeConfig();\nkc.loadFromDefault();\nconst k8sApi = kc.makeApiClient(k8s.AdmissionregistrationV1Api);\nawait k8sApi.patchMutatingWebhookConfiguration('obs-sidecar', {\n  // patch body\n});\n```\n\n## Follow-up Questions\n\n- How would you monitor TLS handshake failures and distinguish node-specific issues from cluster-wide problems?\n- What are safe rollback criteria and automatic rollback triggers for webhook upgrades?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-6",
        "questionId": "q-23049",
        "question": "You’re operating a 3-node Kubernetes control plane with an external etcd, and TLS certificates for kube-apiserver and etcd are expiring. Design a reproducible, kubectl-driven plan to rotate these certificates without downtime, including verification steps for clients during rotation and a safe rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-namespace ResourceQuota and LimitRange policies with container requests and limits derived from load testing (e.g., 300m CPU, 512Mi RAM per pod). Configure Horizontal Pod Autoscaler using both CPU and cust…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Verify the Secret mount by running `kubectl describe pod <pod-name>` to confirm the secretRef exists. Then exec into a running pod to read the certificate at `/etc/tls/client.crt` and fingerprint it with `openssl x509 -…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Rotate kube-apiserver and etcd certificates without downtime using a controlled rolling-update workflow: 1. **Backup existing certificates** - Copy all current TLS certificates and CA bundles from `/etc/kubernetes/pki`…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Begin by inspecting metrics and events: kubectl top to confirm memory pressure, kubectl describe pod to see OOMKilled, and logs for leaks. Compare memory usage against requests/limits to gauge QoS. Pr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question evaluates a candidate's ability to plan and execute certificate rotation in a live Kubernetes cluster with minimal or no downtime. It assesses understanding of TLS certificate management, control plane high availability, and operational safety procedures.\n\n## Key Concepts\n- TLS certificate rotation in Kubernetes\n- Rolling control-plane updates\n- etcd TLS configuration\n- kubeconfig refresh and client validation\n- Safe rollback strategies and backups\n\n## Code Example\n```bash\n# 1. Backup existing certificates\ncp -r /etc/kubernetes/pki /etc/kubernetes/pki.bak\ncp -r /etc/etcd/pki /etc/etcd/pki.bak\n\n# 2. Generate new CA (using kubeadm)\nkubeadm init phase certs ca --cert-dir /etc/kubernetes/pki\n\n# 3. Generate new kube-apiserver certificate\nkubeadm init phase certs apiserver --cert-dir /etc/kubernetes/pki\n\n# 4. Generate new etcd CA and certificates\nkubeadm init phase certs etcd-ca --cert-dir /etc/etcd/pki\nkubeadm init phase certs etcd-server --cert-dir /etc/etcd/pki\n\n# 5. Restart kubelet to pick up new certificates\nsystemctl restart kubelet\n\n# 6. Verify certificates\nopenssl x509 -in /etc/kubernetes/pki/apiserver.crt -noout -dates\nopenssl x509 -in /etc/etcd/pki/server.crt -noout -dates\n```",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-7",
        "questionId": "q-11714",
        "question": "In a production Kubernetes cluster, a 5-replica StatefulSet under heavy load shows rising memory usage and intermittent OOMKilled events. Describe a concrete diagnostic plan, including which metrics and tools you would inspect, how you would fix memory limits and requests, and how you would validate the fix with a canary rollout before full promotion?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: apply a PodDisruptionBudget with minAvailable=2; perform a RollingUpdate with partition=1 so one pod upgrades at a time; upgrade pods sequentially; after each step run rs.status() and check the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Start with kubectl top and Prometheus memory_usage dashboards to confirm per-pod growth. Check events/logs; capture heap profiles (Go pprof or Java Flight Recorder) to distinguish leak vs spike. Set p",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Begin with a 5-step kubectl triage: get pods and events, pull recent logs, inspect container status, check memory/CPU with top, and verify Redis connectivity from a pod. If latency ties to pool size o",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "First verify if ConfigMap changes trigger a reload by inspecting CM data and deployment status. If no automatic reload, force a rollout: kubectl rollout restart deployment/api and monitor with kubectl",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to diagnose memory pressure in a stateful, high-stakes Kubernetes deployment and to design safe ramp-up strategies.\n\n## Key Concepts\n\n- Memory metrics and profiling\n- Resource requests/limits and QoS\n- Canary deployments in StatefulSets\n\n## Code Example\n\n```yaml\n# Sample limits for memory-aware scheduling\napiVersion: v1\nkind: LimitRange\nmetadata:\n  name: mem-limit\n  namespace: default\nspec:\n  limits:\n  - max:\n      memory: 4Gi\n    min:\n      memory: 512Mi\n    type: Container\n```\n\n## Follow-up Questions\n\n- How would you differentiate JVM vs Go memory leaks in this scenario?\n- What would you automate to validate canary safety and rollback?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-8",
        "questionId": "q-1946",
        "question": "On a 3-node Kubernetes cluster, a Deployment’s Pods stay Pending while 2 nodes are Ready. The scheduler shows a NoSchedule taint on node-1. Explain exact commands to identify the taint, check the pod’s tolerations, and either remove the taint or add a matching toleration so the workload schedules. How do you validate after changes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check taint and pods: kubectl get nodes -o wide and kubectl describe node node-1 to confirm the taint. To remove: kubectl taint nodes node-1 NoSchedule-. Or add toleration to the Pod/Deployment: toler",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Maintain an odd-quorum 3-node etcd across AZs with TLS/mTLS and cert rotation. Automate backups using etcdctl snapshot to object storage (daily, rotated). Keep a hot DR cluster in a separate region an",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run: kubectl get ingress -A; kubectl describe ingress <name> -n <ns>; kubectl get secret <tls> -n <ns>; curl -k https://<LB_IP>/health; kubectl logs -n <ns> -l app=ingress-nginx -c controller; kubectl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Begin by confirming per-pod memory limits and actual usage with kubectl describe pod and metrics; then tune Spark: set spark.executor.memory and spark.driver.memory to safe values, add memoryOverhead,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical ability to diagnose and fix scheduling using taints/tolerations in a real cluster.\n\n## Key Concepts\n- taints and tolerations\n- kubectl diagnostics\n- Deployment rollout verification\n\n## Code Example\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: example\nspec:\n  template:\n    spec:\n      tolerations:\n      - key: \"node-role.kubernetes.io/node\"\n        operator: \"Exists\"\n        effect: \"NoSchedule\"\n```\n\n## Follow-up Questions\n- How would you limit the toleration to a subset of pods?\n- What are risks of removing a NoSchedule taint on a node?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-9",
        "questionId": "q-2197",
        "question": "In a Kubernetes cluster running mixed workloads, a critical data-processing job occasionally starves QoS pods during peak load. Provide a concrete plan to enforce SLA guarantees: specify resource requests/limits with appropriate QoS, enable Pod Priority and Preemption, configure HorizontalPodAutoscaler and ClusterAutoscaler, and outline validation steps to prove SLAs hold under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: assign meaningful resource requests/limits per pod to establish QoS, enable Pod Priority and Preemption, configure HorizontalPodAutoscaler and ClusterAutoscaler for demand, apply Namespace quota",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Enable in-place secret refresh with Secrets Store CSI by setting refreshInterval in the SecretProviderClass to a short window (e.g., 1m) and use file-based secrets read by the app. Ensure the app relo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Root cause: the app caches config from a ConfigMap and does not reload on change. Plan: confirm the ConfigMap mount and new data, then force a restart or signal-based reload. Steps: kubectl get config",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Maintenance plan: taint maintenance nodes, drain non-daemonsets, enforce PodDisruptionBudget(minAvailable: 80%) for critical deployments; limit rolling updates to maxUnavailable: 1, maxSurge: 25%; imp",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nEvaluates ability to translate SLAs into concrete Kubernetes controls under real-world pressure.\n\n## Key Concepts\n- QoS classes and resource requests/limits\n- Pod Priority and Preemption\n- HorizontalPodAutoscaler and ClusterAutoscaler\n- Namespace quotas and taints for isolation\n- Validation: load testing, SLIs/SLOs, monitoring signals\n\n## Code Example\n```javascript\n// Example: Resource requests/limits snippet\nconst pod = {\n  apiVersion: \"v1\",\n  kind: \"Pod\",\n  metadata: { name: \"data-job\" },\n  spec: {\n    priorityClassName: \"high-prio\",\n    containers: [{\n      name: \"worker\",\n      image: \"registry/data-job:latest\",\n      resources: {\n        requests: { cpu: \"500m\", memory: \"1Gi\" },\n        limits: { cpu: \"1000m\", memory: \"2Gi\" }\n      }\n    }]\n  }\n}\n```\n\n## Follow-up Questions\n- How would you measure SLA adherence across namespaces?\n- What trade-offs exist between aggressive autoscaling vs. cost, and how would you cap bursts?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-10",
        "questionId": "q-9607",
        "question": "You're deploying a stateless service in Kubernetes that must handle traffic spikes and support zero-downtime upgrades. Outline a Deployment with a rollingUpdate strategy (maxUnavailable, maxSurge), readiness and liveness probes, resource requests/limits, ConfigMap/Secret usage, and a PodDisruptionBudget. Explain how you'd test failover and monitor rollout status?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Deployment with rollingUpdate: maxUnavailable: 1, maxSurge: 25%; add readinessProbe and livenessProbe with sensible delays; specify resourceRequests and limits; mount a ConfigMap and a Secret (e",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage Secrets Store CSI with Vault and a refresh-aware sidecar to avoid stale creds in memory. Ensure the app reloads credentials on a trigger (SIGHUP or file change) and set a 60s propagation wind",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Kafka or Redis Streams backed queue with stateless workers behind a HorizontalPodAutoscaler. Enforce backpressure with a token-bucket limiter shared via Redis. Ensure idempotence by recording pr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Inspect the pod’s ServiceAccount and RBAC bindings, then verify ConfigMap access. Steps: 1) identify SA from the pod: kubectl get pod <name> -n <ns> -o jsonpath='{.spec.serviceAccountName}'; 2) test a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question mirrors production constraints for CKA: validating rolling upgrades, fault tolerance, and configuration management in Kubernetes.\n\n## Key Concepts\n\n- Deployment rolling updates\n- Probes (readiness/liveness)\n- Resource requests/limits\n- ConfigMaps and Secrets usage\n- PodDisruptionBudget and test/rollback\n\n## Code Example\n\n```javascript\n// Simple rollout-check helper (illustrative)\nfunction isRolloutComplete(status) {\n  return status.availableReplicas === status.replicas\n    && status.updatedReplicas === status.replicas;\n}\n```\n\n## Follow-up Questions\n\n- How would you detect a failed rollout and perform an automatic rollback?\n- What monitoring would you put in place to observe rollout health?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-11",
        "questionId": "q-9938",
        "question": "Scenario: A 5-node Kubernetes cluster uses Secrets Store CSI to mount an external secret (Vault) as a file. Vault rotates the database password every 24 hours. After rotation, the app cannot connect to the DB. Outline a precise plan to verify the new secret is propagated to pods, trigger a safe secret reload without downtime, and validate database connectivity during rollout?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: in a default-deny cluster, create two NetworkPolicy objects. Allow from ns-app pods to ns-db:5432, allow DNS egress on port 53 (TCP/UDP), and deny all other ingress/egress. Validate with a test",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "1) Inspect NetPolicy in app-ns and confirm default-deny. 2) Create a test pod in app-ns and curl the API service in api-ns to reproduce failure. 3) If blocked, apply a minimal cross-namespace egress p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Verify with kubectl: read the updated secret data, confirm the mounted file contains the new value, then trigger a controlled rollout to remount the CSI secret without downtime. Steps: kubectl get sec",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Begin by diagnosing the issue with `kubectl describe pod` to confirm the ImagePullBackOff status and verify the correct image name, tag, and registry URL. Create a docker-registry secret using `kubectl create secret doc…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEvaluates handling of external secret rotation via Secrets Store CSI, ensuring propagation, minimal downtime, and robust validation during rollout.\n\n## Key Concepts\n\n- Secrets Store CSI behavior and refresh semantics\n- Rolling restart strategies for secret-driven changes\n- Propagating secret updates to running apps and validating DB connectivity\n\n## Code Example\n\n```javascript\n// Verification and rollout plan (pseudo-commands)\nkubectl get secret vault-secret -n ns -o json\nkubectl get secret vault-secret -n ns -o jsonpath='{.data.password}' | base64 -d\nkubectl exec -n ns <pod> -- cat /mnt/secrets-store/db-password\nkubectl rollout restart deployment/app -n ns\nkubectl rollout status deployment/app -n ns\n```\n\n## Follow-up Questions\n\n- How would you test rollback safety if the secret update breaks connectivity?\n- How would you scale this approach for hundreds of secrets and namespaces?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-12",
        "questionId": "q-23688",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. The API calls an external payment gateway at payments.example.com and intermittently fails with 502 during peak hours. Outline a beginner-friendly plan using kubectl to diagnose whether a NetworkPolicy or egress rule is blocking outbound traffic, and implement a minimal fix with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement External Secrets to inject secrets as files, remove secret env vars, and use mounted files in pods; perform a canary rollout swapping to file-based secrets; rotate keys and revoke old ones;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Verify the Secret mount by running `kubectl describe pod <pod-name>` to confirm the secretRef exists. Then exec into a running pod to read the certificate at `/etc/tls/client.crt` and fingerprint it with `openssl x509 -…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check how the DaemonSet reads its config: 1) kubectl get cm log-collector-config -n ns -o yaml; 2) kubectl get pods -n ns -l app=log-collector; 3) kubectl exec -n ns -it <pod> -- cat /etc/log-collecto",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: 1) kubectl get networkpolicy -A; 2) kubectl describe networkpolicy -n <ns> <name>; 3) from a pod in the deployment, kubectl exec -n <ns> <pod> -- curl -v https://payments.example.com; 4) if bloc",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical troubleshooting of outbound connectivity in Kubernetes, focusing on NetworkPolicy and egress rules, which are common beginner pain points.\n\n## Key Concepts\n- NetworkPolicy and egress rules\n- Outbound connectivity from pods\n- kubectl inspection and targeted testing\n- Minimal, verifiable policy changes\n\n## Code Example\n```javascript\nkubectl get networkpolicy -A\nkubectl describe networkpolicy -n default <name>\nkubectl exec -it <pod> -n default -- curl -v https://payments.example.com\n```\n\n## Follow-up Questions\n- How would you validate that external DNS or CI/CD proxies aren’t interfering?\n- What are risks of opening egress ports broadly, and how can you mitigate them with precise allow rules?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-13",
        "questionId": "q-8900",
        "question": "You manage a four-node cluster with a Deployment that currently reads a Secret via an environment variable. You must rotate the secret with zero downtime. Outline a precise plan to move to a mounted Secret volume, implement a reload mechanism (in-app or sidecar) to pick up changes without pod restarts, and verify zero-downtime during rotation with concrete kubectl commands?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Diagnose by validating policy effect with a test pod, reproduce, inspect policy, verify connectivity, and rollback if blocked. Use concrete steps: 1) kubectl get netpol -A; 2) kubectl describe netpol",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: migrate from environment variable to mounted Secret volume and implement a reload mechanism (in-application or sidecar) enabling the application to detect and reload secret changes from /etc/secret without pod res…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "First, inspect the SecretProviderClass and CSI driver status to confirm which Vault secret/version each pod uses. From a running pod, read the mounted secret file (e.g., /mnt/secrets/db-creds) to capt",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: diagnose if Pending due to Namespace ResourceQuota; inspect quota usage and the Deployment's resource requests, then apply a minimal fix. Commands: kubectl get quota -n ns; kubectl describe quot",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests secret management expertise, zero-downtime operational capabilities, and real-time configuration handling in production Kubernetes environments.\n\n## Key Concepts\n- Secrets as mounted files versus environment variables\n- In-application reload patterns versus sidecar-based approaches\n- Pod lifecycle and disruption management during updates\n- Monitoring and verification strategies during secret rotation\n\n## Code Example\n```yaml\n# Deployment snippet (conceptual)\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-app\nspec:\n  template:\n    spec:\n      containers:\n        - name: app\n          volumeMounts:\n            - name: secret-volume\n              mountPath: /etc/secret\n              readOnly: true\n      volumes:\n        - name: secret-volume\n          secret:\n            secretName: app-secret\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-14",
        "questionId": "q-24405",
        "question": "You're running a 6-rep GPU inference Deployment on NVIDIA GPUs. After a rolling update, some pods report 'no CUDA-capable device is detected'. Outline an end-to-end, commands-first plan to diagnose GPU allocation, verify device plugin health, reproduce GPU visibility from a pod, and implement a minimal, safe fix (e.g., pin pods to GPU-enabled nodes or correct resources) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Drain a node, scale the StatefulSet to 4 replicas, migrate shard data with a safe script, then perform a rolling update with partition=1. Monitor rollout with kubectl rollout status statefulset/my-sta",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: 1) kubectl get networkpolicy -A; 2) kubectl describe networkpolicy -n <ns> <name>; 3) from a pod in the deployment, kubectl exec -n <ns> <pod> -- curl -v https://payments.example.com; 4) if bloc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check the pod events with kubectl describe pod <name> -n <ns> to confirm ImagePullBackOff and error. Inspect the deployment image tag with kubectl get deploy app -o yaml. Verify imagePullSecrets in th",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run commands to confirm GPU availability and binding: kubectl get nodes -o wide; kubectl describe node <node>; kubectl get pods -l app=gpu-infer -o wide; kubectl logs -l app=gpu-infer -c <container>;",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real GPU scheduling and debugging under real workloads, not just theory.\n\n## Key Concepts\n\n- GPU resource requests/limits (nvidia.com/gpu)\n- NVIDIA device plugin health and DaemonSet\n- Pod placement strategies: nodeSelector, taints/tolerations, topology\n\n## Code Example\n\n```bash\nkubectl get nodes -o wide\nkubectl describe node <node>\nkubectl get pods -l app=gpu-infer -o wide\nkubectl logs -n kube-system ds/nvidia-device-plugin-daemonset-<id>\nkubectl exec <pod> -c <container> -- nvidia-smi\n```\n\n## Follow-up Questions\n\n- How would you prevent future drift in GPU allocation after upgrades?\n- How would you monitor GPU utilization across nodes?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-15",
        "questionId": "q-28707",
        "question": "Advanced CKA: In a cluster with a CSI driver and VolumeSnapshot integration, an exercise requires restoring a production PVC from a VolumeSnapshot into a test namespace to validate DR readiness. After restore, a set of Pods fail to mount due to mismatched RestoreSize, SnapshotContent status, or StorageClass parameters. Provide a reproducible kubectl-based plan to (1) identify affected PVCs and snapshots, (2) inspect VolumeSnapshotContent, VolumeSnapshot, and CSI logs, (3) reproduce the restore in a test namespace with controlled concurrency, and (4) implement a robust DR upgrade path (clone vs reuse, adjust StorageClass, quota, and post-restore data integrity checks)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a canary rollout with Argo Rollouts (or Istio TrafficSplit). Start v2 at 5% traffic, monitor latency, error rate, and saturation with Prometheus-based SLI/SLOs. Expand to 20% after 15 minutes; ena",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Inspect the mounted ConfigMap and the deployment: kubectl get cm app-config -o yaml; kubectl describe pod <pod> | grep -i config; if the key is missing, patch the ConfigMap and trigger a rollout: kube",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "The CoreDNS Corefile contains a syntax error or invalid directive after the update",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach centers on deterministic DR restores using snapshot lineage. Identify the PVCs and their VolumeSnapshots, verify SnapshotContent ReadyToUse and RestoreSize, then examine CSI driver logs for R",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n DR readiness hinges on reliable volume restoration from snapshots. Candidates must map restore workflows to KV-level checks and validate idempotent behavior under concurrency.\n\n## Key Concepts\n - VolumeSnapshot and VolumeSnapshotContent lifecycle\n - CSI driver restore semantics and ReadyToUse/RestoreSize\n - StorageClass parameters affecting restores and topology\n - Controlled concurrency for reproducible tests\n\n## Code Example\n```yaml\n# kubectl commands illustrating the flow\nkubectl get volumesnapshots -A\nkubectl describe volumesnapshots <snapname> -n <ns>\nkubectl get pvc -n <ns>\nkubectl describe pvc <pvc> -n <ns>\n```\n\n## Follow-up Questions\n- How would you validate data integrity post-restore in a multi-tenant environment?\n- What failure modes exist if RestoreSize is misreported, and how would you mitigate them?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-16",
        "questionId": "q-5928",
        "question": "In a Kubernetes cluster, a Deployment with 4 replicas behind a ClusterIP service experiences intermittent 5-10s timeouts from pods in a separate namespace during business hours. Outline a beginner-friendly, kubectl-based debugging plan to identify whether NetworkPolicy blocks, DNS issues, or misconfigured selectors, and implement a minimal fix with verification?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by verifying cross-namespace service reachability through DNS resolution and endpoint inspection, examine NetworkPolicy configurations in both namespaces, and validate traffic flow using a temporary busybox pod. E…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: verify quota enforcement with kubectl describe quota prod-quota -n prod; identify Pending pods via kubectl get pods -n prod; read current requests from web-app Deployment (kubectl get deploy web",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define a Deployment with 3 replicas and a ClusterIP Service. Add readinessProbe and livenessProbe, and set resources: requests cpu: 100m, memory: 128Mi; limits cpu: 500m, memory: 256Mi. Populate APP_M",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Identify affected PVCs/pods by node rack label (rack-1). Inspect PV events and VolumeAttachment/CSI logs for topology mismatch. Reproduce in a test namespace by tainting or labeling a node as rack-1 a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical cross-namespace service debugging using kubectl, focusing on real-world failure modes like policy blocks, DNS, and selectors.\n\n## Key Concepts\n- Service DNS and Endpoints\n- NetworkPolicy impact and inspection\n- Cross-namespace traffic debugging\n\n## Code Example\n```bash\nkubectl get svc -n app\nkubectl get endpoints -n app\nkubectl exec -n other-ns some-pod -- nslookup svc-app.default.svc.cluster.local\nkubectl get netpol -A\nkubectl describe pod -n other-ns some-pod\n```\n\n## Follow-up Questions\n- How would you verify fix without policy changes?\n- What metrics would indicate resolution?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-17",
        "questionId": "q-18719",
        "question": "Advanced: In a 3-node etcd-backed Kubernetes cluster, rotate the CA root that signs API server and worker certs without downtime. After rotation, pods report certificate errors. Outline a concrete, zero-downtime plan to rotate the CA, refresh kubeconfigs, update ValidatingWebhookConfiguration/MutatingWebhookConfiguration CA bundles, and verify cluster health with end-to-end checks. Include concrete commands and a rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement namespace isolation per team, enforce resource quotas and limits, configure NetworkPolicy for traffic control, and apply Pod Security Standards via OPA Gatekeeper. Deploy Horizontal Pod Autoscaling with CPU/me…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a namespace-per-tenant architecture with per-namespace RBAC (Roles/RoleBindings), NetworkPolicy isolation, and PodSecurity Standards. Enforce quotas via ResourceQuota and LimitRange, and implement admission contro…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan to rotate CA with kubeadm certs renew all, roll API server and etcd in rolling fashion, refresh kubeconfigs, and update webhook CA bundles. Validate with: kubectl get nodes -A; etcdctl endpoint h",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Inspect NetworkPolicy and Endpoints in the API namespace. Steps: 1) kubectl get networkpolicy -n api-namespace; 2) kubectl get endpoints upstream-auth -n auth-namespace; 3) kubectl run tmp --rm -i --image=alpine -- sh -…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests certificate lifecycle mastery in a live cluster, including zero-downtime rotation, webhook trust updates, and rollback readiness. It emphasizes minimal service disruption and safe handling of CA trust stores.\n\n## Key Concepts\n- Certificate rotation in Kubernetes\n- kubeadm certs renew all\n- webhook CA bundles (Validating/MutatingWebhookConfiguration)\n- rolling restarts and canary deployments\n- etcd quorum and rollback strategies\n\n## Code Example\n```\nkubeadm certs renew all\nkubectl -n kube-system get validatingwebhookconfigurations\nkubectl -n kube-system get mutatingwebhookconfigurations\n```\n\n## Follow-up Questions\n- How would you handle webhook configurations that reference in-cluster secrets when renewing certs?\n- What monitoring would you add to detect certificate trust failures during the rollout?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-18",
        "questionId": "q-21377",
        "question": "In a shared Kubernetes cluster, a new service must reach api.example.com:443, but a default NetworkPolicy blocks all egress. Provide a concrete, minimally invasive plan to allow only that service's pods to access the API, including pod labels, the NetworkPolicy, test/verification steps, and a rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Configure HPA on deployment web-app with minReplicas 4, maxReplicas 20 and target CPU 70%. Set container resources: requests CPU 200m memory 256Mi; limits 500m memory 512Mi. Deploy CA with minNodes 3,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Label pods with app=enricher in namespace enrichment. Create a NetworkPolicy that allows egress to api.example.com:443 from those pods (adjusting to IPs if FQDN isn’t supported). Validate with a test",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Debug CrashLoopBackOff by isolating startup, config, resources, or image issues. Run: kubectl get pods -l app=payment; kubectl describe pod <pod>; kubectl logs <pod>; kubectl logs <pod> --previous; ku",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On surviving master: restore etcd from latest snapshot to /var/lib/etcd, start etcd with the restored member in the quorum, then kubeadm reset the failed master and kubeadm init with the same --contro",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests practical network policy design, scope reduction, and verification in a shared environment, including rollback.\n\n## Key Concepts\n\n- NetworkPolicy: egress rules\n- Pod labeling and namespace scoping\n- DNS vs IP-based allowlists in CNI\n- Validation: test pod, logs, and events\n- Rollback: remove policy safely\n\n## Code Example\n\n```yaml\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: allow-enricher-api-egress\n  namespace: enrichment\nspec:\n  podSelector:\n    matchLabels:\n      app: enricher\n  policyTypes:\n  - Egress\n  egress:\n  - to:\n    - ipBlock:\n        cidr: 203.0.113.0/24\n    ports:\n    - protocol: TCP\n      port: 443\n```\n\nNote: If CNI supports FQDN, replace ipBlock with to.fqdn: api.example.com.\n\n## Follow-up Questions\n\n- How would you monitor and audit egress to ensure no unintended traffic?\n- How would you adapt this for multiple external services with different namespaces?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-19",
        "questionId": "q-27227",
        "question": "In a multi-tenant cluster, the namespace data-science sporadically fails pod scheduling after notebooks are requested via JupyterHub. A ResourceQuota limits the namespace and a LimitRange sets minimum/maximum requests. Outline exact kubectl steps to verify quota usage, inspect per-pod requests/limits, and implement a minimal fix (e.g., adjust LimitRange or quota) with verification commands and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check quota usage with kubectl describe quota -n data-science; inspect per-pod requests/limits with kubectl get pods -n data-science -o jsonpath='{.items[*].spec.containers[*].resources}'; review Limi",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Describe the ImagePullBackOff, verify a dockerconfig secret named reg-secret exists in prod (kubectl get secret reg-secret -n prod -o json), ensure imagePullSecrets on the Deployment or SA include reg",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Identify affected PVCs/pods by node rack label (rack-1). Inspect PV events and VolumeAttachment/CSI logs for topology mismatch. Reproduce in a test namespace by tainting or labeling a node as rack-1 a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: apply a PodDisruptionBudget with minAvailable=2; perform a RollingUpdate with partition=1 so one pod upgrades at a time; upgrade pods sequentially; after each step run rs.status() and check the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests diagnosing quota-driven scheduling failures in a real multi-tenant setup, requiring precise, repeatable kubectl steps over guesswork.\n\n## Key Concepts\n\n- ResourceQuota and LimitRange interactions\n- Pod scheduling semantics under quotas\n- kubectl describe/patch for quotas and limits\n- Verifying scheduling by creating a test pod\n\n## Code Example\n\n```bash\n# Check quota usage\nkubectl describe quota -n data-science\n\n# Inspect per-pod requests/limits\nkubectl get pods -n data-science -o jsonpath='{.items[*].spec.containers[*].resources}'\n\n# Review limits\nkubectl get limitrange -n data-science -o yaml\n\n# Patch quota/limits (example)\nkubectl patch quota data-science-quota -n data-science --patch '{\"spec\":{\"hard\":{\"requests.cpu\":\"12\",\"requests.memory\":\"24Gi\"}}}'\n```\n\n## Follow-up Questions\n\n- How would you monitor quota usage over time?\n- What race conditions could occur during patching?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-20",
        "questionId": "q-16252",
        "question": "In a Kubernetes cluster, a 3-replica Deployment uses the Secrets Store CSI driver to mount DB credentials from AWS Secrets Manager as a volume. A secret rotation occurs in the secret store, but pods keep authenticating with the previous password. Provide a concrete plan to (1) verify how the secret is mounted and sourced, (2) reproduce the exact credential the pod is using, and (3) perform a minimal, zero-downtime rotation so all pods begin using the new secret with verifications?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger a fresh secret refresh by rolling the CSI driver mount cache and then restarting the deployment: 1) kubectl rollout restart deployment/your-app. 2) Verify mount details with kubectl describe p",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create namespace tenant-a and serviceaccount batch-sa; define Role read-pods-cm with verbs get/list for pods and configmaps only; bind via RoleBinding batch-sa-read-pods-cm; verify Secret access denial with can-i comman…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: 1) kubectl get ds log-shipper -n logging; confirm pods healthy and spread. 2) Canary upgrade on one node with maxUnavailable=1 and drain others. 3) On the node, inspect /var/log/containers, veri",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Patch the ValidatingWebhookConfiguration to Ignore on failure, apply the patch in prod-adv, then revert to Fail and verify with events and rollout. Commands: kubectl get validatingwebhookconfiguration",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests knowledge of dynamic secret rotation in CSI-driven mounts and how to avoid full redeploys.\n\n## Key Concepts\n\n- Secrets Store CSI driver behavior\n- Secret rotation semantics\n- Zero-downtime secret refresh strategies\n\n## Code Example\n\n```bash\nkubectl rollout restart deployment/your-app\nkubectl describe pod <pod>\nkubectl exec -it <pod> -- cat /mnt/secrets-store/db-credentials\n```\n```\n\n## Follow-up Questions\n\n- How would you implement automatic secret refresh without restarts?\n- How would you ensure auditability and compliance for rotated secrets?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-21",
        "questionId": "q-17876",
        "question": "Scenario: A Kubernetes cluster uses the Secrets Store CSI driver with Vault as the secret store to mount a DB credential into a Deployment. Vault rotated the credential yesterday, but 4 pods still connect with the old password. Provide an end‑to‑end plan to verify the mount path and refresh policy, reproduce the exact credential the pod sees, and implement a minimal, production‑safe fix (e.g., enforce file‑based refresh or add a lightweight in‑pod reloader) with verifiable steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run kubectl get pods -n default; kubectl describe pod <pod> -n default; kubectl logs <pod> -c <container> --previous; kubectl get events -n default; kubectl rollout status deployment/<name> -n default",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Diagnose PVC/PV binding issues and node taints using kubectl. Start by checking pod status: `kubectl get pods -n <namespace>`; identify the pending pod and describe it: `kubectl describe pod <pod-name>` (look for PVC bi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would perform a canary upgrade on a single node: drain node1, upgrade the CSI driver deployment to v2.0 with maxUnavailable=0, and monitor with kubectl rollout status. Validate all PVCs attach, run",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Investigate the CSI mount path and refresh policy, using kubectl describe secretproviderclass and CSI driver logs. From a sample pod, read /mnt/secrets-store/cred and the app’s env (if any) to reprodu",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to reason about secret refresh in dynamic secret stores and CSI‑driven mounts; digs into how changes propagate without full redeploys.\n\n## Key Concepts\n\n- Secrets Store CSI Driver and SecretProviderClass\n- Secret rotation, refresh semantics, and when mounted secrets are re-read\n- File-based secrets vs environment variables\n- In-pod reloader vs rolling restart for credential rotation\n- Observability: CSI logs, driver sidecar, and kubectl diagnostics\n\n## Code Example\n\n```bash\nkubectl get secretproviderclass -n ns my-secrets -o yaml\nkubectl describe secretproviderclass my-secrets -n ns\nkubectl logs -l app=my-app -c secrets-store\n```\n\n## Follow-up Questions\n\n- How would you test race conditions when rotation happens during rollout?\n- What are trade-offs of file-based refresh vs restarting pods?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-22",
        "questionId": "q-21134",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. A ConfigMap named api-config supplies a startup flag that the app reads on boot. After updating the ConfigMap, requests intermittently fail with 500s while pods show Ready. Outline a beginner-friendly plan using kubectl to diagnose whether the ConfigMap is the cause and implement a minimal fix (e.g., trigger a rollout or mount refresh) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Inspect the mounted TLS secret and its fingerprint inside a live pod: ```bash kubectl exec -n <namespace> <pod-name> -c <container> -- ls -la /etc/tls/ openssl x509 -fingerprint -noout -in /etc/tls/tls.crt ``` To reprod…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Zero-Downtime Kubernetes Upgrade: v1.26 to v1.28 ### Step 1: Pre-Upgrade Checks ```bash # Verify all nodes are Ready kubectl get nodes # Check all control-plane components are healthy kubectl get componentstatuses #…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "1) Check deployment and ConfigMap mounts: kubectl describe deploy <name> and kubectl get configmap api-config -o yaml. 2) Verify the pod reads the config (kubectl exec <pod> -- cat /etc/api/config.jso",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: verify quota enforcement with kubectl describe quota prod-quota -n prod; identify Pending pods via kubectl get pods -n prod; read current requests from web-app Deployment (kubectl get deploy web",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of config-driven behavior and the need for rollout to refresh in Kubernetes.\n\n## Key Concepts\n- ConfigMaps as volume mounts\n- Pod startup vs runtime config reload\n- kubectl rollout restart and status\n- Observability with curl and pod inspection\n\n## Code Example\n```bash\nkubectl describe deploy <name>\nkubectl get configmap api-config -o yaml\n```\n\n## Follow-up Questions\n- How would you implement zero-downtime config refresh? \n- How would you monitor for config drift across pods?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-23",
        "questionId": "q-25667",
        "question": "In a Kubernetes cluster, a Deployment with 2 replicas serves a REST API behind a ClusterIP Service. A Secret named db-credentials stores DB username and password and is mounted as files at /etc/db/creds in each Pod. After rotating the secret, new Pods fail with authentication errors until a rollout occurs. Outline a beginner-friendly plan using kubectl to verify the Secret mounting, and implement a minimal fix (recreate the Secret, trigger a rollout) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "First, verify how the Secret is mounted by examining the Deployment configuration: `kubectl get deployment app -o yaml`. Check whether the Secret is referenced under `env` (environment variable) or `volumeMounts` (volum…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Label one replica as canary (kubectl label statefulset/data-node-0 canary=true). Create a NetworkPolicy that allows egress from pods with canary=true to whitelisted endpoints only; apply; validate from the canary pod us…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: verify secret mounting and values, then fix and rollout. Steps: 1) inspect a pod: kubectl describe pod api-0; kubectl exec api-0 -- ls -l /etc/db/creds && cat /etc/db/creds/username; 2) inspect",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a canary rollout with Argo Rollouts (or Istio TrafficSplit). Start v2 at 5% traffic, monitor latency, error rate, and saturation with Prometheus-based SLI/SLOs. Expand to 20% after 15 minutes; ena",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of Kubernetes Secrets, how they mount into pods, and the lifecycle for picking up secret changes. It also evaluates how to verify mounting from inside a pod and how to safely roll out updated credentials.\n\n## Key Concepts\n- Secrets as file-based volumes\n- Pod lifecycle and rollout restart\n- In-pod validation of mounted data\n\n## Code Example\n```javascript\n# Commands shown in the answer\n```\n\n## Follow-up Questions\n- How would you automate secret refresh without restart?\n- What are security considerations for mounting credentials as files?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-24",
        "questionId": "q-2519",
        "question": "In a Kubernetes cluster, a Deployment named web-app with 3 replicas in namespace prod suddenly fails after a ConfigMap update that injects environment variables. Pods crash due to a missing DB_PASSWORD from the ConfigMap. Describe exact steps and commands to diagnose, patch the ConfigMap, and roll out a fix with minimal downtime while ensuring the deployment restarts cleanly?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: inspect connectivity and trust: kubectl get validatingwebhookconfigurations; describe; curl from a pod to the webhook; rotate TLS certs for the webhook, update the CA bundle in the ValidatingWeb",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Triaging with: kubectl describe pod -n prod -l app=web-app; kubectl logs -n prod <pod>. Inspect the ConfigMap: kubectl get configmap web-app-config -n prod -o yaml. If DB_PASSWORD is missing, patch: k",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "First, diagnose the issue by checking how the application consumes the ConfigMap. If the app reads from the ConfigMap only at startup (typical behavior), changes won't propagate automatically to running pods. Use `kubec…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Our investigation reveals CPU contention within the critical namespace during peak hours. To stabilize the environment immediately, we will isolate the noisy workload using node taints, enforce per-namespace ResourceQuo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical Kubernetes troubleshooting skills for a real config drift issue, focusing on ConfigMaps, envFrom, and rolling updates.\n\n## Key Concepts\n\n- Diagnosing with describe/logs\n- Inspecting and patching ConfigMaps\n- Rolling restart and wait for readiness\n- Safe downtime and service validation\n\n## Code Example\n\n```bash\nkubectl describe pod -n prod -l app=web-app\nkubectl logs -n dev <pod>\nkubectl get configmap web-app-config -n prod -o yaml\nkubectl patch configmap web-app-config -n prod --type merge -p '{\"data\":{\"DB_PASSWORD\":\"secret\"}}'\nkubectl rollout restart deployment/web-app -n prod\nkubectl rollout status deployment/web-app -n prod\n```\n\n## Follow-up Questions\n\n- How would you automate this rollback if patching the ConfigMap introduces an error?\n- What checks ensure you don’t expose secrets via ConfigMaps?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-13T11:23:31.254Z",
    "version": 1
  }
]