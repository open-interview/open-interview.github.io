[
  {
    "id": "test-cka",
    "channelId": "cka",
    "channelName": "CKA",
    "title": "CKA Knowledge Test",
    "description": "Test your understanding of CKA with 25 questions.",
    "questions": [
      {
        "id": "cka-q-0",
        "questionId": "q-3950",
        "question": "Scenario: A 3-replica Deployment behind a ClusterIP Service experiences sporadic pod restarts with OOMKilled during peak load. Provide a concrete debugging plan and implement fixes: add memory requests/limits, readinessProbe and livenessProbe, and a horizontal autoscaler. Include exact kubectl commands and YAML fragments you would apply?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable comprehensive Kubernetes auditing by first creating an audit policy that captures critical security events. Apply audit-policy.yaml with Metadata-level logging for system namespaces and Request-level logging for…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Start by collecting pod events and logs: kubectl describe pod <pod>; kubectl logs <pod> -c <container> --previous; kubectl top pod <pod>. If memory overuse, patch the Deployment with resources: reques",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Define a Deployment with 3 replicas and a ClusterIP Service. Add readinessProbe and livenessProbe, and set resources: requests cpu: 100m, memory: 128Mi; limits cpu: 500m, memory: 256Mi. Populate APP_M",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Canary rollout with Argo Rollouts or Kubernetes native patterns. Start with 10% traffic to the new revision, monitor latency, error rate, and saturation against SLOs for 5–10 minutes, then ramp",
            "isCorrect": false
          }
        ],
        "explanation": "\"## Why This Is Asked\"\\n\\nTests practical Kubernetes debugging and implementation steps candidates would perform in real environments, focusing on convex changes to stabilize a service during peak load.\\n\\n\"## Key Concepts\"\\n\\n- Resource requests/limits and their impact on scheduling and OOM protection\\n- Pod lifecycle probes (readiness and liveness)\\n- Diagnostic commands (describe, logs, top)\\n- Horizontal Pod Autoscaler basics and redeploy workflow\\n\\n\"## Code Example\"\\n\\n```yaml\\nresources:\\n  requests:\\n    memory: 128Mi\\n    cpu: 100m\\n  limits:\\n    memory: 256Mi\\n    cpu: 250m\\nreadinessProbe:\\n  httpGet:\\n    path: /health\\n    port: 8080\\nlivenessProbe:\\n  httpGet:\\n    path: /health\\n    port: 8080\\n  initialDelaySeconds: 30\\n  periodSeconds: 15\\n```\\n\\n\"## Follow-up Questions\"\\n\\n- How would you test changes in a staging environment before production cutover?\\n- How would you apply cluster-wide defaults for resource usage (LimitRange/ResourceQuota) per namespace?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-1",
        "questionId": "q-27804",
        "question": "Advanced CKA: In a 5-node cluster with a private TLS-enabled image registry, an API-server upgrade causes a subset of Deployments in namespace prod to fail image pulls, reporting ImagePullBackOff with ErrCertificateUnknownCA. Provide a reproducible kubectl-based plan to (1) identify affected Deployments and nodes, (2) inspect image pull secrets, registry CA configuration, and kubelet logs, (3) reproduce the issue in a test namespace using a controlled registry, and (4) implement a safe remediation such as rotating CA certs in secrets and triggering rolling updates, with validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "1) Reproduce: kubectl get netpol -n app-ns -o yaml; kubectl run test-pod -n app-ns --rm -ti --image=busybox --restart=Never -- sh -c 'sleep 5; wget -qO- db:5432 || true'; 2) Verify policy: kubectl des",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Define CRD SnowSyncJob with apiEndpoint, schedule, targetSnowflakeTable, and credentialsRef. Build a controller with a single-writer reconcile loop, idempotent ingest via payload hash, a staging area,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by checking `kubectl describe pod <pod>` to identify CrashLoopBackOff events and examine pod status, then use `kubectl logs -p <pod>` to view previous container logs and identify startup errors. Verify the image e…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Identify affected deployments in prod, collect pods with ImagePullBackOff, inspect imagePullSecrets and registry CA chain, peek kubelet/containerd logs, and reproduce with a test namespace and mock re",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates practical incident handling around registry TLS, CA rotation, and controlled rollout.\n\n## Key Concepts\n- ImagePullBackOff, TLS CA rotation, imagePullSecrets\n- kubelet/containerd logs, registry certificate chain\n- safe rolling updates and validation\n\n## Code Example\n```javascript\n// not actual code required here\n```\n\n## Follow-up Questions\n- How would you automate detection and remediation across namespaces?\n- What are risks of rotating CA in a live cluster and how to mitigate?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-2",
        "questionId": "q-15603",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. An InitContainer seeds a per-pod config in a Secret, but API requests intermittently fail during startup with 503 until the pod is Ready. Outline a beginner-friendly plan using kubectl to verify whether the InitContainer is failing due to a missing Secret, and implement a minimal fix (e.g., create and mount the Secret, then trigger a rollout) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Identify the last good etcd snapshot with etcdctl snapshot status; stop etcd on the corrupted member; restore from the snapshot to a fresh data-dir; configure initial-cluster with all members; start e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Update the ConfigMap with kubectl (apply or createconfigmap) and then force a rolling restart: kubectl rollout restart deployment/my-deploy; kubectl rollout status deployment/my-deploy. Verify new pod",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check InitContainer status/logs: kubectl describe pod; kubectl logs -c init-container <pod>. Verify Secret exists: kubectl get secret api-config. If missing, create it and patch/deploy to mount it (en",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-team namespaces, enforce RBAC scoped to each namespace, apply NetworkPolicies to isolate ingress/egress, set ResourceQuotas and LimitRanges to cap CPU/memory and pods, enable Pod Securit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests basic InitContainer debugging, secret management, and rollout flow in a beginner-friendly way.\n\n## Key Concepts\n- InitContainers, Secrets, Volumes/Env\n- kubectl primitives: describe, logs, get, patch, rollout restart\n- Readiness vs startup sequencing\n\n## Code Example\n```javascript\n// not actual code; commands shown above are the expected steps\n```\n\n## Follow-up Questions\n- What if the Secret payload is large and must be templated? how would you manage it?\n- How would you automate this check in a cluster-wide startup script?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-3",
        "questionId": "q-27357",
        "question": "You're in a 5-node Kubernetes cluster. A Job in namespace etl using SA 'etl-runner' fails with RBAC denial when creating pods. Outline a precise, verifiable plan to diagnose with kubectl, identify missing RBAC objects (Role/ClusterRole/Binding), implement the minimal fix (a namespace-scoped RoleBinding binding etl-runner to a Role that allows create/get/list/watch on pods and create Jobs), and validate with can-i checks and a test run?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use kubectl auth can-i to confirm the SA can create pods in etl, then inspect RoleBindings and Roles in etl. If the binding is missing, create a namespace-scoped Role and a RoleBinding for etl-runner",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "**Step 1: Locate Affected CRs** ```bash # Verify CRD acceptance conditions kubectl get crd <crd-name> -o jsonpath='{.status.conditions[?(@.type==\"NamesAccepted\")].status}' # Identify CRs in Unknown state kubectl get <re…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a canary rollout: deploy db:v2 as a 1/4 canary behind the same Service, run a MigrationJob to perform the data migration against the primary, then scale the canary to 2 replicas while monitoring r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Zero-Downtime Kubernetes Upgrade: v1.26 to v1.28 ### Step 1: Pre-Upgrade Checks ```bash # Verify all nodes are Ready kubectl get nodes # Check all control-plane components are healthy kubectl get componentstatuses #…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nDiagnosing RBAC issues under realistic constraints requires identifying whether a Role, ClusterRole, or Binding exists, and applying the least-privilege fix scoped to a namespace. This tests practical troubleshooting and careful permission scoping.\n\n## Key Concepts\n\n- RBAC: Role, ClusterRole, RoleBinding, ClusterRoleBinding\n- Namespace-scoped vs cluster-scoped permissions\n- kubectl auth can-i and kubectl describe\n- Least privilege and auditing changes\n\n## Code Example\n\n```bash\nkubectl auth can-i create pods -n etl\nkubectl get sa etl-runner -n etl -o yaml\nkubectl get rolebindings -n etl\nkubectl apply -f fix-etl-rbac.yaml\nkubectl auth can-i create pods -n etl\nkubectl apply -f test-job.yaml\nkubectl wait --for=condition=complete --timeout=300s job/test-job\n```\n\n## Follow-up Questions\n\n- How would you automate this RBAC validation in a CI pipeline without impacting production?\n- What are the security implications of using ClusterRoleBinding versus namespace-scoped RoleBinding in multi-tenant clusters?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-4",
        "questionId": "q-28883",
        "question": "Advanced CKA: A CRD-backed operator upgrades API from v1alpha1 to v1; during rolling upgrade, CRs end Unknown due to a failing conversion webhook and sticky finalizers. Provide a reproducible kubectl-based plan to (1) locate affected CRs, (2) inspect webhook/API server logs, (3) reproduce conversion in a test namespace with a sample CR, and (4) implement a safe upgrade (disable webhook, idempotent converter, reapply CRs) with validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Deployment with requests: cpu: 100m, memory: 256Mi; limits: cpu: 1, memory: 512Mi; readinessProbe: /health, initialDelaySeconds: 5; livenessProbe: /health, periodSeconds: 15; HPA targetCPUUtiliz",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Step 1: Locate Affected CRs** ```bash # Verify CRD acceptance conditions kubectl get crd <crd-name> -o jsonpath='{.status.conditions[?(@.type==\"NamesAccepted\")].status}' # Identify CRs in Unknown state kubectl get <re…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: 1) kubectl get networkpolicy -A; 2) kubectl describe networkpolicy -n <ns> <name>; 3) from a pod in the deployment, kubectl exec -n <ns> <pod> -- curl -v https://payments.example.com; 4) if bloc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a namespace-wide deny-all egress policy for payments and a second policy allowing TCP 6379 to the payments-redis service. Validate by launching a test pod in payments and trying to reach paymen",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates a candidate's ability to debug complex CRD upgrades under production pressure. It specifically tests:\n- Deep understanding of Kubernetes admission and conversion webhooks\n- Familiarity with CR status propagation and Unknown state recovery\n- Hands-on debugging skills using kubectl and cluster observability tools\n- Ability to design safe, idempotent upgrade procedures with zero data loss\n\n## Key Concepts\n\n1. **CRD Conversion Webhooks**: Kubernetes extensions that transform Custom Resources between API versions. When these webhooks fail, CRs may enter an Unknown state because the API server cannot reconcile the stored version with the requested version.\n\n2. **Sticky Finalizers**: Finalizers prevent deletion of resources with dependencies. During upgrades, stale finalizer references can block CR updates if the associated controllers are unavailable or if webhook failures prevent finalizer removal.\n\n3. **Idempotent Conversion**: A properly designed converter must handle multiple invocations without side effects. This is critical because webhooks may retry conversions, and the same CR may be converted repeatedly during debugging.\n\n4. **Rolling Upgrades and Unknown State**: When the API server cannot convert a CR, it may report the phase as Unknown. This is a protective measure to prevent data corruption but requires manual intervention to resolve.\n\n## Common Pitfalls\n\n- **Incomplete Backup**: Always export CRs before modifying webhooks. Webhook removal can cause data loss if CRs reference non-existent versions.\n- **Skipping Validation**: Re-enabling webhooks without verifying conversion correctness can cause cascading failures.\n- **Ignoring Finalizer Order**: Removing finalizers before resolving their dependencies can orphan related resources.\n\n## Best Practices\n\n1. **Test in Non-Production First**: Always reproduce conversion issues in a dedicated namespace before applying fixes cluster-wide.\n2. **Use Dry-Run**: Utilize `kubectl apply --dry-run=server` to validate changes before committing.\n3. **Monitor Continuously**: Watch CR status changes during the upgrade process using `kubectl get <resource> -w`.\n4. **Document the Procedure**: Maintain runbooks for future incidents to reduce mean time to resolution.",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-5",
        "questionId": "q-24063",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. After a ConfigMap update, requests intermittently return 502s while pods stay Ready. The API reads a feature flag from the ConfigMap at startup. Outline a beginner-friendly plan using kubectl to verify the ConfigMap key exists and is mounted, implement a minimal fix (add the missing key or restart pods), and provide verification steps with load testing that 502s disappear?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a canary StatefulSet redis-canary:7.0 with its own PVC and a headless service redis-canary; deploy a weighted route (e.g., 20% canary) via Istio VirtualService; monitor with redis-cli and INFO",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Inspect the mounted ConfigMap and the deployment: kubectl get cm app-config -o yaml; kubectl describe pod <pod> | grep -i config; if the key is missing, patch the ConfigMap and trigger a rollout: kube",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Define CRD SnowSyncJob with apiEndpoint, schedule, targetSnowflakeTable, and credentialsRef. Build a controller with a single-writer reconcile loop, idempotent ingest via payload hash, a staging area,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: use kubectl top to confirm memory usage spikes on the failing pod; kubectl describe pod to see OOMKilled status and which container; inspect Deployment YAML for memory requests/limits and any si",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical Kubernetes troubleshooting: ConfigMaps, volume mounts, and rollout flow, plus concrete commands.\n\n## Key Concepts\n- ConfigMap keys and volume mounts\n- Pod describe, events, and rollout restart\n- Verifying service endpoints and basic load verification\n\n## Code Example\n```bash\nkubectl get cm app-config -o yaml\nkubectl describe pod <pod> | grep -i ConfigMap\nkubectl patch cm app-config -p '{\"data\":{\"FEATURE_FLAG\":\"on\"}}'\nkubectl rollout restart deploy api\n``` \n```\n\n## Follow-up Questions\n- How would you handle multiple ConfigMaps or key renames without downtime?\n- What are pitfalls if the config key is read only at startup?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-6",
        "questionId": "q-5525",
        "question": "In a 3-replica Deployment, two pods show ImagePullBackOff after a tag bump. Using kubectl, outline a beginner-friendly debugging plan to identify the root cause (registry access, image name, credentials, or imagePullPolicy) and implement a minimal fix, including verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Step 1: Locate Affected CRs** ```bash # Verify CRD acceptance conditions kubectl get crd <crd-name> -o jsonpath='{.status.conditions[?(@.type==\"NamesAccepted\")].status}' # Identify CRs in Unknown state kubectl get <re…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Plan:** Inspect the CronJob spec and active Jobs using kubectl to confirm the current concurrencyPolicy setting and identify any overlapping runs. Patch the CronJob to set concurrencyPolicy to Forbid and configure a r…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Plan: Diagnose Secret Loading Behavior** 1. **Verify current state**: Inspect pod environment variables to confirm the secret is loaded as env vars: ```bash kubectl get pods -l app=<app-name> -o jsonpath='{.items[0].s…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check the pod events with kubectl describe pod <name> -n <ns> to confirm ImagePullBackOff and error. Inspect the deployment image tag with kubectl get deploy app -o yaml. Verify imagePullSecrets in th",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nInterview context explains that this tests practical debugging of image pull failures and understanding imagePullSecrets, registry auth, and patching deployments.\n\n## Key Concepts\n\n- ImagePullBackOff and its relation to image and tag\n- imagePullSecrets in the target namespace\n- kubectl describe/get/patch commands\n- Rollout status verification\n\n## Code Example\n\n```yaml\n# Example patch to add imagePullSecrets\nspec:\n  template:\n    v1:\n      imagePullSecrets:\n        - name: reg-cred\n```\n\n## Follow-up Questions\n\n- What other causes can lead to ImagePullBackOff in a multi-tenant cluster?\n- How would you validate that all namespaces have valid imagePullSecrets without auditing manually?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-7",
        "questionId": "cka-troubleshoot-1776520293793-2",
        "question": "CoreDNS on the kube-system namespace is in CrashLoopBackOff after an edit to the CoreDNS ConfigMap Corefile. What is the most likely root cause and fix?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "The CoreDNS Corefile contains a syntax error or invalid directive after the update",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create a dedicated ServiceAccount `mongo-backup-sa` in namespace `db-prod` and bind it to a minimal RBAC Role granting permissions for `batch:jobs` and `batch:cronjobs` resources, plus `secrets:get` for accessing backup…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "First, identify and remove the cluster-wide Secret read binding: `kubectl describe clusterrolebinding | grep -A 10 'Secrets\\|secrets'`. Remove any ClusterRoleBinding granting Secret read access to the analytics-job Serv…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: identify affected PVCs/pods in team-a via kubectl get pvc -A and describe; check StorageClass topology with kubectl get sc -o yaml and compare allowedTopologies; inspect VolumeAttachment status",
            "isCorrect": false
          }
        ],
        "explanation": "## Correct Answer\nA misconfigured CoreDNS Corefile (syntax error or invalid directive) is a common cause for CoreDNS crashing, which results in DNS resolution failures within the cluster.\n\n## Why Other Options Are Wrong\n- API server being down would affect the entire control plane, not specifically CoreDNS startup.\n- Node DNS resolver misconfiguration affects external resolution but CoreDNS itself failing is the primary symptom.\n- Etcd fullness affects data storage for the control plane but would not directly cause CoreDNS to crash.\n\n## Key Concepts\n- CoreDNS ConfigMap Corefile syntax validity is essential for DNS in-cluster.\n- CoreDNS crash stops all in-cluster DNS queries, impacting pod networking.\n\n## Real-World Application\n- Inspect CoreDNS Corefile in the kube-system ConfigMap, validate syntax, and apply a corrected Corefile. Restart CoreDNS pods or redeploy to ensure DNS resolution resumes.\n",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-8",
        "questionId": "q-24304",
        "question": "You're maintaining a 4-replica Deployment that uses a projected ServiceAccount token for API calls. After a token rotation, two pods return 401 while two succeed. Describe exact kubectl steps to inspect the token projection in pods, pull the current token and decode its expiry, and implement a minimal refresh mechanism (e.g., a sidecar that re-requests a fresh token via the TokenRequest API or a rolling restart) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Capture startup timings and initContainer logs, kubectl describe pod, and events; monitor readiness transitions and image pulls. If init work is heavy, move health checks to readiness, cache results,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Beginner-friendly plan: 1) kubectl top pod -n <ns> to see memory usage; 2) kubectl describe pod <pod> -n <ns> to confirm OOMKilled events and container stats; 3) kubectl get deploy <name> -n <ns> -o y",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Inspect token projection and expiry: kubectl get pod <pod> -o yaml | grep -A5 projected; fetch token: kubectl exec <pod> -- cat /var/run/secrets/kubernetes.io/serviceaccount/token; decode expiry: echo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: verify secret mounting and values, then fix and rollout. Steps: 1) inspect a pod: kubectl describe pod api-0; kubectl exec api-0 -- ls -l /etc/db/creds && cat /etc/db/creds/username; 2) inspect",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests SA token projection, rotation semantics, and ways to refresh tokens without downtime. It also probes practical debugging steps and light-weight fixes.\n\n## Key Concepts\n- ServiceAccount tokens, Projection, TokenRequest API\n- Rolling restart vs automatic refresh\n- Observability via curl tests and token decoding\n\n## Code Example\n```javascript\n// example: curl test with token\nconst token = process.env.TOKEN\nfetch('https://protected', {headers: {Authorization: `Bearer ${token}`}})\n```\n\n## Follow-up Questions\n- How would you implement seamless automatic token refresh in production?\n- What are security implications of TokenRequest lifetimes and sidecar token caches?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-9",
        "questionId": "q-9045",
        "question": "You manage a 5-node Kubernetes cluster spanning two AZs. A mission-critical CSI driver needs to be upgraded to v2.0 with zero downtime. Provide a concrete, step-by-step upgrade plan including canary strategy, PodDisruptionBudget values for CSI components, how to handle in-flight I/O, and verification of volume accessibility and data integrity during rollout?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check how the DaemonSet reads its config: 1) kubectl get cm log-collector-config -n ns -o yaml; 2) kubectl get pods -n ns -l app=log-collector; 3) kubectl exec -n ns -it <pod> -- cat /etc/log-collecto",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check InitContainer status/logs: kubectl describe pod; kubectl logs -c init-container <pod>. Verify Secret exists: kubectl get secret api-config. If missing, create it and patch/deploy to mount it (en",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would perform a canary upgrade on a single node: drain node1, upgrade the CSI driver deployment to v2.0 with maxUnavailable=0, and monitor with kubectl rollout status. Validate all PVCs attach, run",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Identify the primary with rs.status(); on primary run db.fsyncLock(); create VolumeSnapshots for each mongo-data PVC (one per StatefulSet pod); wait for all snapshots to become ReadyToUse; run db.fsyn",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical zero-downtime upgrade planning for storage components, a common pitfall in real clusters.\n\n## Key Concepts\n- Canary rollout and node draining\n- PodDisruptionBudget for CSI components\n- In-flight I/O handling during upgrades\n- Volume accessibility checks and data integrity verification\n\n## Code Example\n```bash\n# example commands to monitor rollout\nkubectl rollout status deployment/csi-driver -n kube-system\n```\n\n## Follow-up Questions\n- How would you rollback if the canary fails?\n- How would you validate the upgrade in a multi-AZ failure scenario?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-10",
        "questionId": "q-24970",
        "question": "Scenario: A DaemonSet named log-shipper runs on all 5 nodes to ship container logs. After upgrading the image to log-shipper:2.0, several nodes report crashes and logs stop shipping. Outline a concrete, end-to-end plan to reproduce the failure on a controlled node, verify the DaemonSet and node state, inspect the host paths and entrypoint, implement a minimal fix (e.g., guard against missing /var/log/containers or add an initContainer to create needed files), perform a rolling update, and validate end-to-end log shipping with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "First verify if ConfigMap changes trigger a reload by inspecting CM data and deployment status. If no automatic reload, force a rollout: kubectl rollout restart deployment/api and monitor with kubectl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Start with kubectl top and Prometheus memory_usage dashboards to confirm per-pod growth. Check events/logs; capture heap profiles (Go pprof or Java Flight Recorder) to distinguish leak vs spike. Set p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: 1) kubectl get ds log-shipper -n logging; confirm pods healthy and spread. 2) Canary upgrade on one node with maxUnavailable=1 and drain others. 3) On the node, inspect /var/log/containers, veri",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: 1) etcd health check: etcdctl endpoint health. 2) Identify unhealthy member and add a fresh member; remove the bad one. 3) Stop etcd on the failed node; restore latest snapshot to /var/lib/etcd/",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDiagnoses real-world DaemonSet rollout issues, canary upgrades, and node-local debugging with minimal downtime. Tests troubleshooting, Kubernetes familiarity, and practical logging validation.\n\n## Key Concepts\n- DaemonSet upgrade strategy and canaries\n- Node-level debugging of hostPath mounted logs\n- InitContainers and startup guards\n- End-to-end log shipping verification\n\n## Code Example\n```javascript\n// Example: guard for missing log path in startup\nif (!fs.existsSync('/var/log/containers')) {\n  fs.mkdirSync('/var/log/containers', { recursive: true });\n}\n```\n\n## Follow-up Questions\n- How would you automate the canary upgrade and rollback?\n- What metrics would you monitor during the rollout?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-11",
        "questionId": "q-1737",
        "question": "You're deploying a 3-replica Deployment in Kubernetes for a payment app. Suddenly 2 pods crash with CrashLoopBackOff. Describe a practical debugging workflow to identify if the issue is container startup, config, resources, or image, and outline the exact kubectl commands and file checks you would perform. Include how you would propose a minimal fix and a rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "First, verify how the application consumes the ConfigMap by checking the mount path and file content: `kubectl get configmap <name> -o yaml` to see the configuration, then `kubectl exec -n <namespace> <pod> -- cat /path…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: validate CoreDNS health and DNS from a pod, then restart CoreDNS if DNS errors persist. Steps: 1) kubectl get pods -n kube-system -l k8s-app=kube-dns. 2) kubectl logs -n kube-system -l k8s-app=k",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Debug CrashLoopBackOff by isolating startup, config, resources, or image issues. Run: kubectl get pods -l app=payment; kubectl describe pod <pod>; kubectl logs <pod>; kubectl logs <pod> --previous; ku",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan focused on zero-downtime: choose a logical-dump path with pg_dumpall from the primary, restore to a freshly provisioned Postgres in cluster-b, enable streaming replication until cutover, and swit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical debugging steps for Kubernetes CrashLoopBackOff, focusing on actionable commands.\n\n## Key Concepts\n\n- CrashLoopBackOff diagnosis\n- kubectl tooling (describe, logs, events)\n- ConfigMap/Secret patching\n- Rollback strategies\n\n## Code Example\n\n```bash\nkubectl get pods -l app=payment\n```\n\n## Follow-up Questions\n\n- How would you distinguish image pull errors from runtime crashes?\n- What are the risks of rolling back a deployment in production?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-12",
        "questionId": "q-27844",
        "question": "In a 3-replica deployment, a projected service account token is mounted at /var/run/secrets/kubernetes.io/serviceaccount/token with a 1-hour TTL. After rotation, pods still call the API server with the old token due to in-process caching. Outline a precise, minimal plan to verify the projection, reproduce the active token inside a pod, and implement a safe refresh mechanism that reloads the token in running processes without full pod restart (e.g., a sidecar that signals the main process when the token file changes). Include concrete kubectl commands and end-to-end verification?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Plan:** Verify the projected token path and expiry within a running pod; reproduce by inspecting the token file and decoding the JWT to confirm the expiration claim. Implement a minimal refresh mechanism using a sidec…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: after rotating CA Secret, ensure pods load new CA. Implement a watcher (sidecar or operator) that detects changes to the secret file and triggers a rolling restart or in-process truststore reloa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by listing pods and events, then inspect pod details for OOMKilled. Use kubectl describe pod <pod> and kubectl get events to confirm memory pressure. Check kubectl top pod for actual usage. If O",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Apply a default-deny NetworkPolicy in api-prod, then two allow policies: (1) api-prod pods to ns-logging on 8080; (2) api-prod pods to ns-db-prod on 5432. Add reciprocal ingress rules in ns-logging an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nToken rotation for service account tokens introduces subtle failure modes when applications cache credentials in memory. A robust token refresh mechanism prevents downtime when tokens expire or rotate. This question evaluates a candidate's understanding of Kubernetes security primitives, sidecar patterns, and graceful process reloading.\n\n## Key Concepts\n- **ServiceAccount Token Projection and TTL**: Projected tokens are mounted as volume mounts with configurable expiration (default: 1 hour). The kubelet automatically rotates tokens before expiry.\n- **In-Process Token Caching**: Applications often read the token once at startup and cache it, causing failures when the kubelet rotates the underlying token file.\n- **Sidecar Pattern**: A companion container can monitor the token file and signal the main process to reload without requiring a pod restart.\n- **Signal-Based Reloading**: SIGHUP is a conventional Unix signal for requesting configuration reload, ideal for triggering token refresh.\n\n## Common Pitfalls\n1. Assuming token files are static—projected tokens change automatically.\n2. Implementing polling-based refresh instead of efficient filesystem watching.\n3. Not handling token reload atomically, risking partial reads.\n4. Missing error handling when the main process exits before token rotation completes.",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-13",
        "questionId": "q-9873",
        "question": "In a 9-node multi-zone Kubernetes cluster spanning three regions, the production namespace 'critical' hosts 12 deployments. During peak load, CPU throttling affects several services, risking SLA breaches. Outline an actionable stabilization plan for the next hour and share concrete manifests for ResourceQuota, LimitRange, PodDisruptionBudget, and an HPA policy, plus a plan to isolate noisy workloads. Include acceptance criteria and rollback steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Our investigation reveals CPU contention within the critical namespace during peak hours. To stabilize the environment immediately, we will isolate the noisy workload using node taints, enforce per-namespace ResourceQuo…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "List network policies in the namespace and inspect selectors and ports. From a test pod, curl the backend service from a frontend pod context. If traffic is blocked, modify or create a policy to allow",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Backup etcd (etcdctl snapshot save) and validate integrity; enable a conversion webhook to migrate v1alpha1 to v1; perform canary upgrades on a subset of CRs, monitor status with kubectl get crd/statu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Begin by listing pods and events, then inspect pod details for OOMKilled. Use kubectl describe pod <pod> and kubectl get events to confirm memory pressure. Check kubectl top pod for actual usage. If O",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses your ability to design a comprehensive production stabilization strategy using Kubernetes primitives under time pressure. It evaluates practical knowledge of quota enforcement, resource limits, high availability configurations, autoscaling mechanisms, and workload isolation techniques in multi-zone environments.\n\n## Key Concepts\n\n- ResourceQuota for namespace-level resource governance\n- LimitRange for default resource request/limit enforcement\n- PodDisruptionBudget for availability guarantees\n- HorizontalPodAutoscaler for dynamic scaling\n- Taints and tolerations for workload isolation",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-14",
        "questionId": "q-24556",
        "question": "In a Kubernetes cluster, a Deployment uses a Secrets Store CSI volume to load DB credentials from Vault. Vault rotates credentials every 12 hours, but pods keep old credentials until a restart. Propose a concrete live-rotation plan (CSI Secrets Store, SecretProviderClass, refreshInterval, and any sidecar or trigger) that achieves zero downtime and a rolling update strategy. Include exact kubectl commands and manifest changes and how to verify success?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run commands to confirm GPU availability and binding: kubectl get nodes -o wide; kubectl describe node <node>; kubectl get pods -l app=gpu-infer -o wide; kubectl logs -l app=gpu-infer -c <container>;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create or verify the Secret and patch the Deployment to pull LOG_LEVEL from the ConfigMap and API_KEY from the Secret via envFrom/valueFrom. Add readiness and liveness probes (httpGet /health on 8080)",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Strategy: convert from env-based secret to CSI Secrets Store with Vault, set SecretProviderClass with refreshInterval (e.g., 3600s), mount as volume, and use a small in-cluster watcher/sidecar to sign",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-namespace default-deny NetworkPolicy, then add two policies that allow all traffic within each namespace and an egress rule allowing outbound HTTPS to api.external.com:443. Do not allo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests live secret rotation without downtime, a common production challenge when external vaults rotate credentials.\n\n## Key Concepts\n- Secrets management with CSI Secrets Store\n- Secret rotation triggers, in-cluster reloads\n- Rollout strategies and observability\n\n## Code Example\n```yaml\n# SecretProviderClass example (Vault)\napiVersion: secrets-store.csi.x-k8s.io/v1alpha1\nkind: SecretProviderClass\nmetadata:\n  name: vault-creds\nspec:\n  provider: vault\n  parameters:\n    roleName: \"db-role\"\n    vaultAddress: \"https://vault.example.com\"\n    objects: |\n      - objectName: db-credentials\n        secretPath: secret/data/db/creds\n        secretKey: creds\n    refreshInterval: \"3600s\"\n```\n\n## Follow-up Questions\n- How would you validate no client downtime during rotation?\n- What monitoring would you add for rotation failures?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-15",
        "questionId": "q-26491",
        "question": "In a 3-node Kubernetes cluster, a Deployment uses a Secrets Store CSI Driver backed by Vault to mount DB credentials. After a Vault rotation, some pods still authenticate with the old password. Provide a precise end-to-end plan to verify how the CSI secret is fetched, reproduce the current secret contents inside a pod, and implement a minimal, production-safe refresh (or rolling restart) with validation steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify with kubectl: read the updated secret data, confirm the mounted file contains the new value, then trigger a controlled rollout to remount the CSI secret without downtime. Steps: kubectl get sec",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "First, inspect the SecretProviderClass and CSI driver status to confirm which Vault secret/version each pod uses. From a running pod, read the mounted secret file (e.g., /mnt/secrets/db-creds) to capt",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage Secrets Store CSI with Vault and a refresh-aware sidecar to avoid stale creds in memory. Ensure the app reloads credentials on a trigger (SIGHUP or file change) and set a 60s propagation wind",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Inspect token projection and expiry: kubectl get pod <pod> -o yaml | grep -A5 projected; fetch token: kubectl exec <pod> -- cat /var/run/secrets/kubernetes.io/serviceaccount/token; decode expiry: echo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical troubleshooting of external secret management and CSI refresh behavior, a real-world reliability concern.\n\n## Key Concepts\n- Secrets Store CSI Driver and Vault integration\n- Secret refresh vs rolling restart\n- Pod debugging with kubectl exec and inspect mounted files\n\n## Code Example\n```bash\n# Inspect secret version and mounted path\nkubectl get secret my-vault-cred -o jsonpath='{.data.password}' | base64 -d\nkubectl describe pod <pod>\nkubectl exec -it <pod> -- ls -l /mnt/secrets/\nkubectl exec -it <pod> -- cat /mnt/secrets/db-creds/password\n```\n\n## Follow-up Questions\n- How would you validate without downtime if refresh is unsupported?\n- What are the trade-offs of sidecar refresh vs rolling restart?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-16",
        "questionId": "q-27985",
        "question": "In a 5-node cluster, a Deployment with 4 replicas serves a service 'web-app' via a ClusterIP. DNS lookups for web-app.namespace.svc.cluster.local fail intermittently from pods in ns-a and ns-b, but succeed from ns-c. CoreDNS pods show Ready. Outline a precise, minimal diagnostic plan using kubectl and dig/nslookup to identify whether CoreDNS, the Service, or DNS caching is at fault, and implement a safe fix with verification?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-namespace ResourceQuota and LimitRange policies with container requests and limits derived from load testing (e.g., 300m CPU, 512Mi RAM per pod). Configure Horizontal Pod Autoscaler using both CPU and cust…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: identify all trust roots (kubeconfigs, Secrets, webhooks) pointing to the old CA; on a standby, generate a new root CA and sign all certificates (apiserver, controller-manager, kubelet, front-pr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "1) Reproduce: kubectl get netpol -n app-ns -o yaml; kubectl run test-pod -n app-ns --rm -ti --image=busybox --restart=Never -- sh -c 'sleep 5; wget -qO- db:5432 || true'; 2) Verify policy: kubectl des",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check DNS end-to-end: kubectl get ep web-app -n ns-a; kubectl get svc web-app -n ns-a; kubectl get pods -n kube-system -l k8s-app=coredns; kubectl logs -n kube-system -l k8s-app=coredns; kubectl exec",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to debug DNS in Kubernetes, including CoreDNS config reloads, Endpoints, and caching interactions under namespace variation.\n\n## Key Concepts\n- DNS resolution in Kubernetes; CoreDNS config and reloads\n- Endpoints and Service locality; pod namespaces\n- Safe remediation: rolling restart vs config reload\n\n## Code Example\n\n```javascript\n// No code required for this question\n```\n\n## Follow-up Questions\n- What additional metrics would you collect to distinguish CoreDNS vs network policy issues?\n- How would you implement a zero-downtime DNS cache refresh in CoreDNS?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-17",
        "questionId": "q-15492",
        "question": "In a Kubernetes Deployment, TLS certs are mounted from a Secret as files for mutual TLS with an external service. When the Secret is rotated, existing pods keep serving the old certs until they are restarted. Design a practical in‑cluster approach to achieve zero‑downtime rotation using a sidecar that watches the Secret mount and triggers a hot reload in the main process (or a reload endpoint). Include exact kubectl steps to implement and verify?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enumerate affected deployments by env label, inspect MutatingWebhookConfiguration for namespaceSelector and the admission logs, reproduce in a test-namespace by applying env=prod label to a minimal De",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Define a Deployment with 3 replicas and a ClusterIP Service. Add readinessProbe and livenessProbe, and set resources: requests cpu: 100m, memory: 128Mi; limits cpu: 500m, memory: 256Mi. Populate APP_M",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Add a sidecar that watches the Secret mount with inotify and signals the main TLS app to reload certs in place (SIGHUP) or via a reload endpoint. Rotate the Secret; verify new certs are used by a cana",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: validate CoreDNS health and DNS from a pod, then restart CoreDNS if DNS errors persist. Steps: 1) kubectl get pods -n kube-system -l k8s-app=kube-dns. 2) kubectl logs -n kube-system -l k8s-app=k",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design zero‑downtime credential rotation using in‑pod hot reload, a common real‑world constraint in production clusters.\n\n## Key Concepts\n- Secrets mounted as files\n- Sidecar pattern for in‑place reload\n- Process signaling (SIGHUP) or in‑app reload endpoint\n- Rolling updates and canary validation\n\n## Code Example\n```yaml\n# Pod spec snippet (simplified)\ncontainers:\n  - name: app\n    image: myapp:latest\n  - name: cert-watcher\n    image: alpine:3.18\n    command: [\"sh\",\"-c\",\"apk add --no-cache inotify-tools; while inotifywait -e modify /etc/tls/secret; do kill -HUP 1; done\"]\n    volumeMounts:\n      - name: tls\n        mountPath: /etc/tls\n```\n\n## Follow-up Questions\n- How would you test this under high traffic with a canary rollout?\n- What failure modes exist (e.g., client handshake during reload) and mitigations?",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-18",
        "questionId": "q-15443",
        "question": "In a Kubernetes cluster, a MongoDB replica set runs as a StatefulSet in namespace db-prod with a serviceaccount mongo-sa. A nightly backup CronJob must reach MongoDB on port 27017 but NetworkPolicy currently blocks it. Describe the minimal RBAC and NetworkPolicy changes to enable backups with least privilege, and provide concrete manifest snippets and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan to diagnose why Endpoints are missing: 1) check api-service selector and compare with pod labels, 2) inspect Endpoints object, 3) verify namespace alignment, 4) confirm Deployment is labeling pod",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Label one replica as canary (kubectl label statefulset/data-node-0 canary=true). Create a NetworkPolicy that allows egress from pods with canary=true to whitelisted endpoints only; apply; validate from the canary pod us…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a dedicated ServiceAccount `mongo-backup-sa` in namespace `db-prod` and bind it to a minimal RBAC Role granting permissions for `batch:jobs` and `batch:cronjobs` resources, plus `secrets:get` for accessing backup…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "**Plan:** Verify the projected token path and expiry within a running pod; reproduce by inspecting the token file and decoding the JWT to confirm the expiration claim. Implement a minimal refresh mechanism using a sidec…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design least-privilege RBAC and NetworkPolicy for a backup workflow against a MongoDB StatefulSet, plus practical validation steps.\n\n## Key Concepts\n- **Kubernetes RBAC scoping**: Using Role (namespace-scoped) instead of ClusterRole for least privilege\n- **NetworkPolicy egress controls**: Using pod selectors to restrict traffic to only necessary destinations\n- **Secrets mounting**: Injecting backup credentials securely into CronJobs\n- **StatefulSet-backed MongoDB**: Backup workflows using mongodump/mongorestore tools\n\n## Key Design Principles\n1. **Separate ServiceAccount**: A dedicated `mongo-backup-sa` isolates backup operations from other workloads\n2. **Role (not ClusterRole)**: Limits RBAC to the `db-prod` namespace only\n3. **Specific resource names**: The secret permission is restricted to `mongo-backup-credentials` only\n4. **Egress-only NetworkPolicy**: Allows outbound traffic from backup pods to MongoDB on port 27017 while blocking all other egress",
        "difficulty": "advanced"
      },
      {
        "id": "cka-q-19",
        "questionId": "q-20122",
        "question": "A Kubernetes cluster has a ClusterRoleBinding that binds a ServiceAccount in namespace payments to a ClusterRole allowing get/list on secrets in all namespaces. Describe a concrete, end-to-end plan to verify the binding scope with kubectl, reproduce cross-namespace secret access from a test Pod, and implement a minimal fix that restores namespace isolation with minimal disruption to workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Identify the binding with kubectl get clusterrolebinding -o yaml | kubectl describe; observe the bound SA and ClusterRole; test cross-namespace access by creating a test Pod in payments using that SA",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: canary rollout of the MutatingWebhookConfiguration and webhook deployment. Update the CABundle and migrate to a canary service; deploy image with a canary tag. Roll out with RollingUpdate, maxUn",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Diagnose a pod OOMKilled in Kubernetes: review events and pod description, check current resource requests/limits with kubectl describe pod, and inspect node memory with kubectl top node. If memory li",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check PVC status with kubectl get pvc -n ns; describe pvc to see Pending reason; list PVs to find a binding. If none, create a Static PV and bind, or install a StorageClass and patch the PVC storageCl",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nOverly broad RBAC can leak secrets across namespaces; this checks detection and remediation under realistic constraints.\n\n## Key Concepts\n- RBAC scope (cluster vs namespace)\n- kubectl can-i to validate permissions\n- Safe remediation: move from ClusterRoleBinding to namespace-scoped RoleBinding\n\n## Code Example\n```yaml\n# payments namespace only\napiVersion: rbac.authorization.k8s.io/v1\nkind: Role\nmetadata:\n  name: secret-reader\n  namespace: payments\nrules:\n- apiGroups: [\"\"]\n  resources: [\"secrets\"]\n  verbs: [\"get\",\"list\"]\n```\n```yaml\napiVersion: rbac.authorization.k8s.io/v1\nkind: RoleBinding\nmetadata:\n  name: payments-secret-reader\n  namespace: payments\nsubjects:\n- kind: ServiceAccount\n  name: <SA_NAME>\n  namespace: payments\nroleRef:\n  kind: Role\n  name: secret-reader\n  apiGroup: rbac.authorization.k8s.io\n```\n\n## Follow-up Questions\n- How would you implement an automated audit for RBAC scope changes across the cluster?\n- What are the risks of using bound SA tokens in long-lived pods and how would you mitigate them?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-20",
        "questionId": "q-11869",
        "question": "Your 6-node Kubernetes cluster must forward audit events to an external SIEM via a webhook sink. Detail the exact YAMLs and kubectl commands to enable an API-server audit policy, configure a canary webhook backend with retry and rate-limiting, perform a canary rollout, verify delivery to the SIEM, and implement a rollback if delivery stalls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify the Secrets Store CSI mount path inside a pod, inspect the SecretProviderClass for the secret source and refreshPeriod, and reproduce the value by reading the mounted secret file in a pod. Rota",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Configure API server auditing with a Metadata policy and a webhook sink; apply a canary sink (audit-policy.yaml, audit-sink-canary.yaml), generate test events, validate SIEM ingestion, then switch to full production dep…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Begin diagnostics by running `etcdctl member list` and `etcdctl endpoint status` to verify etcd cluster health and quorum. Use `kubectl get cs` and `kubectl get nodes` to assess overall cluster status, and examine API s…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Identify leaked ephemeral volumes and pods; collect CSI controller and kubelet logs, events, and Pod specs; reproduce in a test namespace with controlled churn to simulate multi-tenant usage; implemen",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAuditing is central to security and compliance; this tests practical setup of an external webhook sink with a canary rollout and safe rollback procedures.\n\n## Key Concepts\n- Kubernetes Audit policy configuration\n- AuditSink and webhook sink implementation\n- Canary rollout strategy and rollback mechanisms\n- Retry logic, exponential backoff, and rate limiting\n\n## Code Example\n```yaml\n# audit-policy.yaml\napiVersion: audit.k8s.io/v1\nkind: Policy\nrules:\n- level: Metadata\n  resources:\n  - group: \"\"\n    resources: [\"pods\",\"configmaps\",\"secrets\"]\n```\n\n```yaml\n# audit-sink.yaml (canary sink)\napiVersion: auditregistration.k8s.io/v1alpha1\nkind: AuditSink\nmetadata:\n  name: audit-sink-canary\nspec:\n  webhook:\n    clientConfig:\n      service:\n        name: audit-webhook-service\n        namespace: audit-system\n        path: \"/audit\"\n      timeout: 30s\n    throttle:\n      qps: 10\n      burst: 20\n    retryBackoff:\n      base: 1s\n      max: 30s\n```\n\n```bash\n# Apply audit policy and canary sink\nkubectl apply -f audit-policy.yaml\nkubectl apply -f audit-sink-canary.yaml\n\n# Generate test events\nkubectl run test-pod --image=nginx --restart=Never\nkubectl delete pod test-pod\n\n# Verify SIEM ingestion and proceed to full rollout\ncurl -X POST https://siem-endpoint/audit/check\nkubectl apply -f audit-sink-full.yaml\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-21",
        "questionId": "q-30194",
        "question": "In a Kubernetes cluster, a Deployment with 3 replicas serves a REST API behind a ClusterIP Service. A new InitContainer warms a cache during startup, causing startup latency. After deployment, requests intermittently time out during traffic spikes even though pods show Ready. Outline a beginner-friendly plan using kubectl to diagnose whether the InitContainer delays readiness, and implement a minimal fix (e.g., move warm-up to a periodic background Job or lazy-load) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check DNS end-to-end: kubectl get ep web-app -n ns-a; kubectl get svc web-app -n ns-a; kubectl get pods -n kube-system -l k8s-app=coredns; kubectl logs -n kube-system -l k8s-app=coredns; kubectl exec",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Audit RBAC bindings across all namespaces with kubectl get clusterrolebindings and kubectl get rolebindings --all-namespaces. Inspect SA-fin: kubectl describe sa SA-fin -n finance and test access with",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Check InitContainer behavior: kubectl describe deploy <dep> -n <ns> and kubectl logs -c init-container <pod> to see the warm-up duration. Compare with readiness by curling the API from inside a pod (k",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Attach an ephemeral container to a running sales-app Pod to run dig to verify MX, curl to SMTP host, and ping to test reachability. Inspect namespace NetworkPolicy rules, then add the narrow allow rul",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses understanding of InitContainer impact on startup and readiness, and the ability to perform hands-on debugging with kubectl. It also tests the candidate’s ability to propose a minimal, production-safe refactor (moving heavy startup work to a Job or lazy-loading) and validate with a controlled rollout.\n\n## Key Concepts\n\n- InitContainer execution order and its impact on pod readiness\n- Distinguishing startupProbe vs readinessProbe effects\n- Safe rollout strategies and minimal, verifiable fixes\n\n## Code Example\n\n```bash\n# Inspect init behavior\nkubectl describe deploy <dep> -n <ns>\nkubectl logs -c init-container <pod> -n <ns>\n\n# Basic readiness check from inside cluster\nkubectl exec -it <pod> -n <ns> -- curl -sSf http://<service>/health\n```\n\n## Follow-up Questions\n\n- What are trade-offs of moving startup work to a separate Job vs lazy-loading in-app?\n- How would you measure startup latency and its impact on SLOs during a rolling update?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-22",
        "questionId": "q-1946",
        "question": "On a 3-node Kubernetes cluster, a Deployment’s Pods stay Pending while 2 nodes are Ready. The scheduler shows a NoSchedule taint on node-1. Explain exact commands to identify the taint, check the pod’s tolerations, and either remove the taint or add a matching toleration so the workload schedules. How do you validate after changes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Plan:** Verify the projected token path and expiry within a running pod; reproduce by inspecting the token file and decoding the JWT to confirm the expiration claim. Implement a minimal refresh mechanism using a sidec…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Check taint and pods: kubectl get nodes -o wide and kubectl describe node node-1 to confirm the taint. To remove: kubectl taint nodes node-1 NoSchedule-. Or add toleration to the Pod/Deployment: toler",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Audit RBAC bindings across all namespaces to identify any cluster-wide admin ClusterRoleBindings that may be inadvertently applying to prod workloads. Reproduce the issue in a test namespace to confirm the root cause. C…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a namespace-per-tenant architecture with per-namespace RBAC (Roles/RoleBindings), NetworkPolicy isolation, and PodSecurity Standards. Enforce quotas via ResourceQuota and LimitRange, and implement admission contro…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical ability to diagnose and fix scheduling using taints/tolerations in a real cluster.\n\n## Key Concepts\n- taints and tolerations\n- kubectl diagnostics\n- Deployment rollout verification\n\n## Code Example\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: example\nspec:\n  template:\n    spec:\n      tolerations:\n      - key: \"node-role.kubernetes.io/node\"\n        operator: \"Exists\"\n        effect: \"NoSchedule\"\n```\n\n## Follow-up Questions\n- How would you limit the toleration to a subset of pods?\n- What are risks of removing a NoSchedule taint on a node?",
        "difficulty": "beginner"
      },
      {
        "id": "cka-q-23",
        "questionId": "q-24405",
        "question": "You're running a 6-rep GPU inference Deployment on NVIDIA GPUs. After a rolling update, some pods report 'no CUDA-capable device is detected'. Outline an end-to-end, commands-first plan to diagnose GPU allocation, verify device plugin health, reproduce GPU visibility from a pod, and implement a minimal, safe fix (e.g., pin pods to GPU-enabled nodes or correct resources) with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run commands to confirm GPU availability and binding: kubectl get nodes -o wide; kubectl describe node <node>; kubectl get pods -l app=gpu-infer -o wide; kubectl logs -l app=gpu-infer -c <container>;",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create a namespace-scoped NetworkPolicy in the finance namespace with a pod selector matching app=finance-app. Configure egress rules to allow traffic only to the internal API at 10.0.1.5/32:443 and the logging service…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Identify affected StatefulSets and PVCs, inspect PV/PVC events and CSI logs, verify topology constraints and Node readiness, reproduce in a test namespace with explicit topology labels and bounded con",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Inspect the Secret and mounts per pod, confirm the updated cert is visible inside each container, and test TLS. Then trigger a rolling restart to ensure existing pods reload the secret. Commands: kube",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real GPU scheduling and debugging under real workloads, not just theory.\n\n## Key Concepts\n\n- GPU resource requests/limits (nvidia.com/gpu)\n- NVIDIA device plugin health and DaemonSet\n- Pod placement strategies: nodeSelector, taints/tolerations, topology\n\n## Code Example\n\n```bash\nkubectl get nodes -o wide\nkubectl describe node <node>\nkubectl get pods -l app=gpu-infer -o wide\nkubectl logs -n kube-system ds/nvidia-device-plugin-daemonset-<id>\nkubectl exec <pod> -c <container> -- nvidia-smi\n```\n\n## Follow-up Questions\n\n- How would you prevent future drift in GPU allocation after upgrades?\n- How would you monitor GPU utilization across nodes?",
        "difficulty": "intermediate"
      },
      {
        "id": "cka-q-24",
        "questionId": "q-24756",
        "question": "In a Kubernetes cluster, a Namespace named 'shop' hosts two Deployments: 'frontend' and 'backend' behind ClusterIP Services. A NetworkPolicy blocks all ingress/egress by default. Frontend calls backend service, but requests fail. Outline a beginner-friendly plan using kubectl to diagnose whether the policy is the cause, and implement a minimal fix by allowing frontend pods to reach backend, with verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Check PVC status with kubectl get pvc -n ns; describe pvc to see Pending reason; list PVs to find a binding. If none, create a Static PV and bind, or install a StorageClass and patch the PVC storageCl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: 1) kubectl -n shop get networkpolicies; 2) launch a diag pod and curl backend-service:80 to verify traffic; 3) if blocked, apply a minimal NetworkPolicy allowing from pods labeled app=frontend t",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Mount the Secret as a file in a projected volume and fix the in-app cache so credentials refresh on change. Detect stale creds by a simple credential check on login, and implement a reload path trigge",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: verify the secret mount and env usage in payments-api pods; reproduce current certs with kubectl exec and openssl to inspect /etc/tls; add a minimal auto-reload sidecar that watches /etc/tls and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis checks understanding of NetworkPolicy basics, namespace scoping, and practical debugging in clusters.\n\n## Key Concepts\n- NetworkPolicy basics, default deny semantics\n- Pod selectors and namespace scoping\n- kubectl-based troubleshooting and minimal changes\n\n## Code Example\n```yaml\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: allow-frontend-to-backend\n  namespace: shop\nspec:\n  podSelector:\n    matchLabels:\n      app: backend\n  ingress:\n  - from:\n    - podSelector:\n        matchLabels:\n          app: frontend\n    ports:\n    - protocol: TCP\n      port: 80\n```\n\n## Follow-up Questions\n- How would you test across multiple namespaces?\n- What are security implications of permissive policies?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-21T06:52:18.257Z",
    "version": 1
  }
]