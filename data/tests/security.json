[
  {
    "id": "test-security",
    "channelId": "security",
    "channelName": "Security",
    "title": "Security Knowledge Test",
    "description": "Test your understanding of Security with 25 questions.",
    "questions": [
      {
        "id": "security-q-0",
        "questionId": "q-26767",
        "question": "You're maintaining a public REST API behind a gateway on AWS/GCP used by mobile clients. Production error responses leak stack traces. Propose a beginner-friendly plan to prevent data leakage while keeping debugging possible. Include concrete steps and a minimal Express error handler concept (no full code)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Centralize error handling and return a generic 500 in production. Log full details to a secure sink, but redact secrets from responses. Use the NODE_ENV flag to switch between verbose developer messag",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage per-tenant envelope encryption with keys in a KMS (AWS KMS/GCP KMS) and data-at-rest encryption for MongoDB Atlas and Delta Lake. Apply field-level encryption for PII, TLS/mTLS between servic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a multi-tenant data processing pipeline where each tenant's data is processed inside hardware enclaves such as Intel SGX or AMD SEV. Provision per-tenant keys at runtime from a cloud KMS (e.g., AWS KMS, Azure Key…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a layered defense: enforce container image provenance with Sigstore/Cosign, require SBOM verification in CI and at runtime, deploy via immutable registries with admission controls (OPA) and",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked: Tests the ability to implement safe, production-grade error handling without sacrificing debugging. Key concepts: error handling patterns, environment-based behavior, secure logging, and data redaction. Code Example: see below. Follow-ups evaluate nuance in error types and practical guardrails.",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-1",
        "questionId": "q-7945",
        "question": "You manage a payment API used by major mobile apps. A new feature requires token binding, mTLS, and hardware-backed signing for access tokens, plus secure OTA updates for client libraries. Propose an end-to-end security design that handles key management with an HSM, per-tenant SBOM enforcement, rotation, revocation, and incident playbooks. Provide concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-model artifact signing (Ed25519) at upload, store the signature and signer key ID in MongoDB. Verify signatures before serving models; reject unsigned or mismatched versions. Maintain an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an HSM-backed key hierarchy with per-tenant signing keys stored in a FIPS 140-2 Level 3 HSM, rotated every 90 days. Issue access tokens using RS256 signatures bound to TLS client certificates via token binding…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Require per-tenant HMAC-SHA256 signatures using rotated secrets stored in an HSM, include timestamp and nonce in the signature payload, verify at the edge with a strict replay window (e.g., 5 minutes), store nonces in a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an end-to-end isolation plan for a multi-tenant security API that spawns ephemeral AWS Lambda and Google Cloud Run tasks to process tenant data across clouds. How would you enforce tenant boundaries, manage keys,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design end-to-end secure token issuance with hardware roots, SBOM enforcement, rotation, and incident response—reflecting realistic risk considerations for large-scale payment platforms.\n\n## Key Concepts\n- HSM-based PKI, per-tenant keys, token binding, mTLS\n- SBOM enforcement, reproducible builds, attestation\n- Key rotation, revocation, audits, incident playbooks\n\n## Code Example\n```javascript\n// Pseudo: issue token using HSM\nfunction issueToken(payload, hsmKey) {\n  const header = { alg: 'RS256', typ: 'JWT' };\n  return signWithHsm(JSON.stringify(payload), header,\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-2",
        "questionId": "q-17488",
        "question": "In a globally distributed, multi-tenant financial data platform, streaming market data feeds from exchanges are ingested and routed to tenant apps. Propose a concrete, end-to-end security design that prevents data leakage, tampering, or access abuse. Include per-tenant keys and signing, envelope encryption, data-plane isolation, runtime attestation, tamper-evident logs, and an incident-response playbook with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant keys provisioned from a KMS or HSM with 24-hour rotation. Sign each data packet using HMAC-SHA256 with the tenant-specific key to ensure integrity and authenticity. Apply envelope encryption: a per-…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use per-endpoint HMAC signatures with a nonce in each webhook payload. On receipt, verify the signature and check the nonce against a Redis set for that webhook ID; if seen, reject. TTL for nonces: 5",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement sandboxed UDF runtimes using WebAssembly (WASM) to enforce strict isolation and prevent unauthorized system access. Block network egress from UDFs by default and require explicit approval for data transfers. E…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce token binding and anti-replay at the gateway. Use short-lived JWTs with unique jti values, track seen jti in Redis per region, validate JWT via JWKS with issuer and audience checks, bind tokens to mTLS or per-cl…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nReal-time, multi-tenant data platforms face significant risks: data leakage between tenants, tampering during transmission, and supply-chain attacks. This question assesses depth in cryptography, access control, and incident response across data-in-motion and data-at-rest scenarios.\n\n## Key Concepts\n\n- **Per-tenant keys and signing**: Ensures data integrity and tenant isolation through dedicated cryptographic keys\n- **Envelope encryption and rotation**: Protects data-at-rest while limiting exposure through frequent key rotation\n- **Data-plane isolation**: Prevents cross-tenant data leakage by enforcing separate storage and network boundaries\n- **Runtime attestation**: Verifies that code executes within trusted execution environments, protecting against supply-chain attacks\n- **Tamper-evident logs**: Uses cryptographic chaining and Merkle trees to detect unauthorized modifications to audit trails\n- **Incident-response playbook**: Provides automated rollback, replay protection, and escalation procedures for rapid recovery",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-3",
        "questionId": "q-26153",
        "question": "Design a secure, multi-tenant telemetry ingestion pipeline for a streaming service (devices -> ingest -> analytics -> storage). How do you enforce tenant isolation, guard against data leakage, enable trusted ML inference, and detect poisoning with low latency? Include architectural choices, security controls, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sanitize server-side SVG uploads to prevent XSS. Accept only image/svg+xml and cap size; run DOMPurify with jsdom to strip <script>, onload, and javascript: URLs, remove external references; render vi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Explain a secure, multi-tenant telemetry ingestion pipeline (devices -> ingest -> analytics -> storage). Describe tenant isolation, data leakage guards (encryption at transit/rest, per-tenant keys), t",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Enforce per-tenant IaC isolation: module signing and a mandatory Terraform plan review; use tenant-scoped remote state encrypted at rest by Vault; deploy Vault dynamic secrets with short leases and au",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Standardize errors to generic messages (401/403/400) without exposing secrets. Redact token fields from error payloads and logs. Implement a central Express error handler that returns status and a saf",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical security design for multi-tenant telemetry pipelines, coupling data protection with real-time analytics and ML.\n\n## Key Concepts\n\n- Multi-tenant isolation across ingestion, processing, and storage\n- Data provenance, immutable logs, and secure auditing\n- Per-tenant keys, envelope encryption, and key management\n- Trusted ML inference: model signing, verification, and sandboxing\n- Poisoning detection, anomaly scoring, and rollback strategies\n\n## Code Example\n\n```javascript\n// Pseudo-configuration illustrating per-tenant key usage\nconst tenantId = req.headers['X-Tenant-Id'];\nconst key = KMS.getKey(`tenant:${tenantId}`);\nencrypted = encrypt(batch, key);\n```\n\n## Follow-up Questions\n\n- What tests would validate tenant isolation and poisoning detection under load?\n- How would you monitor data drift and model integrity in production?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-4",
        "questionId": "q-6327",
        "question": "You're building a login API for an enterprise web app used by security-conscious teams at Zoom, LinkedIn, or Goldman Sachs. Describe a beginner-friendly login flow that defends against credential stuffing and timing attacks, including per-user rate limiting, an explicit account-lockout policy, and a basic MFA fallback, plus how you would test end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Passkeys implement passwordless authentication using device-generated public-key cryptography where private keys are securely stored in hardware enclaves and synchronized across devices via secure cloud services, while…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement tenant-scoped RBAC in the API, use per-tenant views/tokenized PII in MongoDB (field-level encryption), enable dynamic masking at query time for dashboards, add CI checks to reject any PII pr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-user rate limiting with a Redis token bucket (e.g., 10 attempts per 15 minutes) and an account lockout after 5 consecutive failures within 15 minutes. Validate passwords with bcrypt using cons",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Standardize errors to generic messages (401/403/400) without exposing secrets. Redact token fields from error payloads and logs. Implement a central Express error handler that returns status and a saf",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical security fundamentals at junior level with real-world constraints.\n\n## Key Concepts\n- Rate limiting and lockout; constant-time comparisons; password hashing; MFA basics; secure logging/privacy.\n\n## Code Example\n```javascript\n// Pseudo login flow illustrating bcrypt compare, MFA, and session issuance\nasync function login(req, res) {\n  const { username, password, mfaToken } = req.body;\n  const user = await getUser(username);\n  if (!user) return res.status(401).send('Invalid credentials');\n  const ok = await bcrypt.compare(password, user.hash);\n  if (!ok) return res.status(401).send('Invalid credentials');\n  if (user.mfaEnabled) {\n    if (!mfaToken) return res.status(401).send('MFA required');\n    const verified = speakeasy.totp.verify({ secret: user.mfaSecret, encoding: 'base32', token: mfaToken });\n    if (!verified) return res.status(401).send('Invalid MFA');\n  }\n  const session = createSession(user.id);\n  res.cookie('session', session, { httpOnly: true, secure: true });\n  return res.send({ ok: true });\n}\n```\n\n## Follow-up Questions\n- How would you adapt for distributed rate limiting?\n- How would you test lockout and recovery flows?\n- How would you store and rotate MFA seeds securely?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-5",
        "questionId": "q-14771",
        "question": "Your web app allows file uploads, then serves a public link for sharing. A security scan finds that files are stored with their original filenames and served with client-provided Content-Type, enabling spoofed downloads. Propose a beginner-friendly fix plan covering MIME detection, header enforcement, and testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant envelope encryption using KMS/Vault, separate storage prefixes per tenant, ABAC with OPA, and mutual TLS between services. Sign all artifacts with Sigstore and enforce per-tenant audit logs",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Server-side: ignore the client-supplied Content-Type; detect mime by inspecting file signatures (magic bytes) and map to a safe, canonical mime. Whitelist allowed types and reject others. Serve with C",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Restrict CORS to a single origin and avoid reflecting Origin. Change Access-Control-Allow-Origin from * to https://app.example.com, keep credentials true, and ensure cookies are HttpOnly, Secure, and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a SPIFFE-based mTLS with a hardware-backed CA using an HSM. Use short-lived leaf certs that rotate every few minutes, automatic revocation via CRL/OCSP, and per-service attestation at sideca",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nA concrete, beginner-friendly file upload hardening task that touches MIME handling and HTTP headers.\n\n## Key Concepts\n\n- MIME type detection by file signatures\n- Content-Type vs Content-Disposition\n- Whitelisting and secure defaults\n- Server-side validation and test automation\n\n## Code Example\n\n```javascript\n// Node.js pseudo: detect mime by magic bytes\nfunction detectMime(bytes) {\n  if (bytesstartsWith([0xFF,0xD8])) return \"image/jpeg\";\n  if (bytesstartsWith([0x89,0x50])) return \"image/png\";\n  // ... simple mapping\n  return \"application/octet-stream\";\n}\n```\n\n## Follow-up Questions\n\n- How would you test this with large files and streaming to avoid memory pressure?\n- How would you extend to prevent mime-type sniffing by browsers?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-6",
        "questionId": "q-11012",
        "question": "In a high-scale, multi-tenant chat service that supports third-party plugins, design a complete runtime security model to prevent data leakage from compromised plugins. Include per-plugin attestation, hardware-backed key material, per-plugin isolation (WASM or microVM), strict IPC, dynamic revocation, and an incident runbook; specify technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enforce cache-key isolation: partition caches by tenant, derive keys from tenant_id, URL, method, and a hash of headers that affect content; reject or canonicalize Vary/Accept-Encoding; implement per-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Require per-tenant HMAC-SHA256 signatures using rotated secrets stored in an HSM, include timestamp and nonce in the signature payload, verify at the edge with a strict replay window (e.g., 5 minutes), store nonces in a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a runtime plugin sandbox using WebAssembly/WASI or Firecracker microVMs for per-plugin isolation. Deploy TPM/HSM-backed attestation with per-plugin digital signatures to cryptographically verify each plugin be…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Short-lived, tenant-bound tokens (OAuth2/OIDC with token binding) plus per-tenant scopes; SPIRE mTLS for service trust; gateway policy enforcement via OPA/REGO to block cross-tenant access; data-plane",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical design for plugin trust, isolation, and incident response in a multi-tenant service.\n\n## Key Concepts\n- Plugin isolation (WASM/microVM)\n- Attestation and TPM/HSM roots\n- Per-plugin key management\n- Service mesh mTLS/SPIFFE\n- Dynamic revocation and audits\n- Incident runbook\n\n## Code Example\n```javascript\n// Pseudo: attestation flow for a loaded plugin\nfunction loadPlugin(p) {\n  const capsule = attest(p); // sign/verify capsule with TPM/HSM\n  if (!capsule.valid) throw new Error('Attestation failed')\n  sandbox.run(p) // WASM or microVM\n}\n```\n\n## Follow-Up",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-7",
        "questionId": "q-15764",
        "question": "In a production federated learning platform where millions of devices train a shared model on-device and upload updates, design an end-to-end security plan to defend against data poisoning, model tampering, and gradient leakage. Include threat modeling, secure aggregation, differential privacy budgets, remote attestation, per-update signing, SBOM enforcement, and an incident-response playbook with rollback and forensics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Threat model and threat sources. Use Bonawitz secure aggregation with per-round masking; enforce differential privacy budgets per client; require remote attestation and device-bound keys for updates;",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement tenant-scoped envelope encryption with an HSM-backed KMS (Vault/AWS KMS) and per-tenant keys, ensure mTLS with SPIFFE, and strictly isolate brokers and processors via namespace and network p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-IP rate limiting at the API layer using a Redis-backed store for scale. Use a 15-minute window and a conservative max (e.g., 100 requests), enable standard rate headers, log blocked atte",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Require policy bundles signed by a trusted authority and verified by the gateway before loading. Enforce per-tenant scopes with mTLS, deny-by-default routing, and explicit allow rules. Keep immutable,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical defense-in-depth for federated learning, a high-risk area where updates can be poisoned or leaked. It tests threat modeling, cryptographic controls, and incident response in a distributed system.\n\n## Key Concepts\n- Threat modeling for FL and endpoints\n- Secure aggregation (Bonawitz) with per-round masking\n- Differential privacy budgets and gradient clipping\n- Remote attestation and device-bound keys\n- Model signing and SBOM enforcement\n- Anomaly detection on gradients and incident response\n\n## Code Example\n```javascript\n// Pseudocode sketch\nfunction processUpdate(update, attestation) {\n  if (!verifyAttestation(attestation)) throw 'Bad attestation';\n  const signed = verifySignature(update);\n  return applySecureAggregation(signed);\n}\n```\n\n## Follow-up Questions\n- How would you test the system against data-poisoning attacks in a staging environment?\n- How would you handle key rotation without downtime for producers and consumers?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-8",
        "questionId": "q-11562",
        "question": "In a multi-tenant, globally distributed serverless platform using ephemeral function containers, a tenant's code could attempt data exfiltration or memory-based side channels. Propose a concrete runtime integrity and data-leak prevention design that covers: cold-start attestation, per-tenant isolation, memory-safe runtimes, short-lived credentials, service-mesh with mTLS, tamper-evident logs, and automated breach response including revocation and rollback. Include specific technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant isolation with Firecracker microVMs or WASM sandboxes and memory-safe runtimes (Rust/Go). Attest function integrity at cold-start via TPM/Secure Enclave or SPIFFE/SVID attestation",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-tenant envelope encryption using KMS/Vault, separate storage prefixes per tenant, ABAC with OPA, and mutual TLS between services. Sign all artifacts with Sigstore and enforce per-tenant audit logs",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Bind tenant context at the edge using mTLS authentication and per-request tenancy tokens; enforce strict per-tenant Redis namespaces with ACL-based access controls; implement envelope encryption with tenant-scoped keys…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement WebAuthn passkeys, device fingerprinting, behavioral biometrics, and microsegmented access controls with real-time threat detection.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical runtime security for multi-tenant serverless platforms, focusing on isolation, attestation, and automated breach response rather than purely static controls.\n\n## Key Concepts\n- Runtime attestation at cold-start\n- Per-tenant isolation (microVMs or WASM sandboxes)\n- Memory-safe runtimes (Rust/Go/WASM)\n- Short-lived credentials and mTLS (service mesh)\n- Tamper-evident logging and immutable storage\n- Automated breach response (revocation, quarantine, rollback)\n\n## Code Example\n```javascript\n// Pseudo-startup attestation check\nfunction verifyAttestation(attestation) {\n  // verify with TPM/TEE-backed assertions and issuer policy\n  return true;\n}\n```\n\n## Follow-up Questions\n- How would you quantify the startup latency and its impact on cold-start performance?\n- What failure modes exist for attestation and how would you mitigate them?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-9",
        "questionId": "q-1234",
        "question": "You operate a CDN edge platform that lets customers deploy WebAssembly modules for request processing. Outline a practical, auditable approach to securely load, validate, and sandbox these modules, covering authentication (signatures/attestation), host-function access, resource quotas, revocation, and incident response?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a runtime plugin sandbox using WebAssembly/WASI or Firecracker microVMs for per-plugin isolation. Deploy TPM/HSM-backed attestation with per-plugin digital signatures to cryptographically verify each plugin be…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Require per-tenant WASM modules to be digitally signed with Ed25519 and attested by a hardware-backed KMS; whitelist host functions; apply strict linear memory and CPU quotas via sandbox (WASM isolate",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage per-tenant envelope encryption with keys in a KMS (AWS KMS/GCP KMS) and data-at-rest encryption for MongoDB Atlas and Delta Lake. Apply field-level encryption for PII, TLS/mTLS between servic",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce a strict bound and type: limit size to 2 MB and require image/* Content-Type. Validate by checking magic bytes. Scan with a lightweight virus check. Use an IAM role with PutObject access only",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests secure edge module loading, trust, and lifecycle management under multi-tenant pressure.\n\n## Key Concepts\n- WASM sandboxing at the edge\n- Per-tenant module signing and attestation\n- Host-function access control and least privilege\n- Resource quotas (CPU/memory) and isolation\n- Revocation, kill-switch, SBOM visibility, and immutable auditing\n\n## Code Example\n```javascript\nimport nacl from 'tweetnacl';\nconst ok = nacl.sign.detached.verify(moduleBytes, signature, publicKey);\n```\n\n## Follow-up Questions\n- How do you rotate keys and revoke modules without downtime?\n- How would you monitor for anomalous crypto API usage without impacting latency?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-10",
        "questionId": "q-8140",
        "question": "In a SPA admin console used across a large platform, production errors leak emails and IDs in responses and logs. Propose a concrete, beginner-friendly fix: global error handling with generic messages, PII redaction in logs, structured JSON logs at production level, tests injecting PII to verify masking, and a safe rollout via feature flags and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a global error handler that returns a generic message; redact PII (emails, IDs, tokens) from logs and error payloads; switch to structured JSON logs with production-level verbosity and no st",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Adopt a strict WASM sandbox: limit syscalls, cap memory, enforce a narrow API surface, and reject any transformer attempting to breach boundaries. Isolate tenants via dedicated streams with strict data routing and netwo…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use ECDHE for forward secrecy, implement HKDF for key derivation, and use a hierarchical key management system with short-lived session keys.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant HSM-backed signing keys that issue short-lived JWTs (5-15 min) bound to device attestation and the target resource. Use mTLS between edge and auth services, token binding (audience, nonce),",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nGauges practical understanding of secure error handling, privacy in logging, and safe rollouts—core beginner-level issues in real apps.\n\n## Key Concepts\n- Global error handling\n- PII redaction in logs\n- Structured logging\n- Production logging level\n- Feature flags and monitoring\n\n## Code Example\n```javascript\n// Example: basic Express error handler\napp.use((err, req, res, next) => {\n  const safe = redactPII(err);\n  res.status(500).json({ message: 'Internal server error' });\n});\nfunction redactPII(e){ /* dummy */ return e; }\n```\n\n## Follow-up Questions\n- How would you test log redaction end-to-end?\n- What are potential pitfalls when masking errors in production? \n```",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-11",
        "questionId": "q-4548",
        "question": "You're building a webhook receiver for a payment processor in a microservice. Describe a minimal, production-ready verification workflow: (a) signature validation with the processor's public key, (b) replay-attack prevention with a timestamp window or nonce, (c) idempotency using a delivery-id store, (d) redaction of PII in logs, (e) key rotation and monitoring tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant CMKs in a hardware-backed KMS and envelope-encrypt data with per-tenant data keys cached with short TTL. Rotate CMKs with zero-downtime re-wrapping of all data keys. Enforce least privi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant WASM sandboxes with strict API surface and memory quotas; require plugin signing with Ed25519 and short-lived JWT tokens; run plugins in a confidential runtime with attestation (T",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Validate Ed25519 signatures using the processor's public key stored in a KMS, verify the payload's cryptographic signature against a canonicalized JSON payload, require a timestamp header within a 5-minute window to pre…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Explain a secure, multi-tenant telemetry ingestion pipeline (devices -> ingest -> analytics -> storage). Describe tenant isolation, data leakage guards (encryption at transit/rest, per-tenant keys), t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical webhook security discipline at a beginner-to-intermediate level, focusing on real-world steps teams use to secure event-driven integrations.\n\n## Key Concepts\n- Signature verification with public keys (Ed25519)\n- Replay protection (timestamps/nonces)\n- Idempotency keys and delivery tracking\n- PII redaction in logs\n- Key rotation and test coverage\n\n## Code Example\n```javascript\n// Node.js style pseudo-code for verification\nfunction verifyWebhook(payload, signatureB64, pubKeyPem) {\n  const data = JSON.stringify(sortKeys(payload));\n  const sig = Buffer.from(signatureB64, 'base64');\n  return crypto.verify(null, data, pubKeyPem, sig);\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-12",
        "questionId": "q-476",
        "question": "How would you prevent SQL injection in a web application and what are the common attack vectors?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use parameterized queries/prepared statements instead of string concatenation. Validate and sanitize all user inputs. Implement least privilege database access. Common vectors include login forms, search functionality,…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement strict input validation with an allowlist using Joi or Yup; never merge raw input into queries; reject keys starting with '$' or '.'; use a fixed query builder that only accepts known fields",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an end-to-end secure supply chain: sign/verify every image with Sigstore/cosign; generate SBOMs and enforce reproducible builds; use per-tenant envelope encryption with KMS and short-lived cre",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant keys using envelope encryption, with data keys signed by an HSM-backed master key. Employ Vault Transit or cloud KMS with per-tenant Key Encryption Keys (KEKs), implementing scheduled rotation and emergency r…",
            "isCorrect": false
          }
        ],
        "explanation": "## Prevention Methods\n- Use parameterized queries with placeholders\n- Input validation and sanitization\n- ORM frameworks that escape parameters\n- Stored procedures with proper parameter binding\n\n## Common Attack Vectors\n- Login authentication forms\n- Search functionality\n- URL parameters for data filtering\n- API endpoints accepting user data\n\n## Defense in Depth\n- Web Application Firewall (WAF)\n- Regular security audits\n- Error message sanitization\n- Database access restrictions",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-13",
        "questionId": "q-8984",
        "question": "In a cloud service that runs user-provided ML models on a shared GPU pool for real-time inference, design a security architecture that prevents cross-tenant data leakage and model theft. Your plan must include hardware enclave usage (TDX/SEV or equivalent), envelope encryption with per-tenant keys, attestation, tamper-evident logging, secure updates, and an incident response workflow. Also discuss latency and cost trade-offs and deployment steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Server-side: ignore the client-supplied Content-Type; detect mime by inspecting file signatures (magic bytes) and map to a safe, canonical mime. Whitelist allowed types and reject others. Serve with C",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Define a field-level authorization map (field → required permission). Extend resolvers with a wrapper that checks user roles and tenantId in the Graph context before resolving each field; prune or err",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant GPU inference stack using hardware enclaves (Intel TDX/AMD SEV). Enforce memory isolation, disable dedup, and wrap each tenant’s model with envelope encryption managed by a KMS/HSM",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "XSS (Cross-Site Scripting) attacks inject malicious scripts into trusted websites that execute in users' browsers, while CSRF (Cross-Site Request Forgery) tricks authenticated users into performing unwanted actions on w…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes threat modeling for multi-tenant GPU inference, balancing isolation, performance, and incident response. It requires concrete mechanisms rather than high-level talk.\n\n## Key Concepts\n- Hardware enclaves: Intel TDX, AMD SEV\n- Envelope encryption, per-tenant keys, KMS/HSM\n- Attestation, tamper-evident logging\n- Side-channel risk minimization, secure update\n- Incident response playbooks\n\n## Code Example\n```javascript\n// Example: mock policy to select tenant key and verify attestation\nfunction getTenantKey(tenantId, attestation) {\n  // in real life: fetch from KMS/HSM\n  if (!verifyAttestation(attestation, tenantId)) throw new Error(\"Attestation failed\");\n  return fetchTenantKeyFromKMS(tenantId);\n}\n```\n\n## Follow-up Questions\n- How would you verify attestation with a cloud provider's SEV/TDX implementation?\n- What telemetry would you collect to detect late-stage enclave compromise?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-14",
        "questionId": "q-21325",
        "question": "Describe a secure OTA firmware update pipeline for a global EV fleet (edge/vehicle) that prevents supply-chain tampering, per-vehicle attestation, secure boot, and rollback while preserving privacy and auditability. Include: artifact signing in an HSM, SBOM validation, per-vehicle attestation over mTLS, canary rollout, tamper-evident logs, revocation, and an incident playbook?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Artifacts must be reproducible and signed within an HSM; SBOMs are validated against a trusted feed. Vehicles attest bootloader, firmware, and update package over mTLS using per-vehicle certificates. Implement canary ro…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-IP rate limiting at the API layer using a Redis-backed store for scale. Use a 15-minute window and a conservative max (e.g., 100 requests), enable standard rate headers, log blocked atte",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement Cloudflare edge rate limiting with per-IP and per-account counters: 20 login attempts per IP per minute and 5 failed attempts per account within 10 minutes. Trigger progressive CAPTCHA challenges upon threshol…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant isolation must be enforced at ingestion, storage, and compute. Use envelope encryption with tenant-scoped KEKs managed by a HSM-backed KMS, rotate keys regularly, and revoke on compromise.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes end-to-end OTA security for fleets, combining supply-chain integrity, device attestation, and incident response—critical competencies in edge and vehicle ecosystems like those at Cloudflare and Tesla.\n\n## Key Concepts\n\n- Secure OTA pipelines and reproducible builds\n- Hardware Security Modules (HSM) for artifact signing\n- SBOM validation and trusted software provenance\n- Per-vehicle attestation over mTLS\n- Canary rollout and safe rollback strategies\n- Tamper-evident logging and key revocation\n- Automated incident response playbooks\n\n## Code Example\n\n```python\ndef verify_update(update_bytes, sbom, vehicle_cert, hsm_signature):\n    if not hsm_signature.verify(update_bytes):\n        raise SecurityError(\"Invalid HSM signature\")\n    if not sbom.validate_trusted_feed():\n        raise SecurityError(\"SBOM validation failed\")\n    if not vehicle_attest(vehicle_cert):\n        raise SecurityError(\"Attestation failed\")\n    return True\n```",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-15",
        "questionId": "q-21466",
        "question": "You run a multi-tenant cloud data platform with a web frontend, API gateway, and service mesh. A compromised API key could access data from multiple tenants. Propose an end-to-end plan to prevent cross-tenant leakage: short-lived tokens bound to per-tenant identities with token binding, per-tenant scopes, SPIRE‑mTLS, gateway policies enforcing tenant isolation (OPA/REGO), and runtime controls (data masking, query whitelisting). Outline deployment steps and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Short-lived, tenant-bound tokens (OAuth2/OIDC with token binding) plus per-tenant scopes; SPIRE mTLS for service trust; gateway policy enforcement via OPA/REGO to block cross-tenant access; data-plane",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use per-endpoint HMAC signatures with a nonce in each webhook payload. On receipt, verify the signature and check the nonce against a Redis set for that webhook ID; if seen, reject. TTL for nonces: 5",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Store JWTs in HttpOnly, SameSite cookies; drop localStorage usage. Add per-session CSRF tokens (double-submit or header check) and rotate refresh tokens on use. Implement Express middleware to read co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant isolation must be enforced at ingestion, storage, and compute. Use envelope encryption with tenant-scoped KEKs managed by a HSM-backed KMS, rotate keys regularly, and revoke on compromise.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess ability to design robust multi-tenant security with runtime enforcement and policy-driven access.\n\n## Key Concepts\n- Token binding, OAuth2/OIDC, PKCE\n- SPIRE mTLS\n- Policy-as-code (OPA/REGO)\n- Data masking and query whitelisting\n- Automated revocation and incident playbooks\n\n## Code Example\n```rego\npackage example.tenants\n\ndefault allow = false\n\nallow {\n  input.tenant_id == data.tenant_id\n  input.scopes[_] == \\\"data.read\\\"\n}\n```\n\n## Follow-up Questions\n- How would you test token binding in CI?\n- How would you validate fail-open/containment during revocation?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-16",
        "questionId": "q-15947",
        "question": "In a Node.js Express API, POST /login accepts username and password. A security test shows brute-force risk due to high request rate. Propose a beginner-friendly, end-to-end plan to mitigate: implement rate-limiting by IP and by username, temporary lockouts after repeated failures, optional CAPTCHA, and safe logging. Include minimal code snippets and testing steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant enclaves with isolated sealing keys; require remote attestation before any processing; encrypt data at rest and in transit with per-tenant envelope keys stored in a hardware security module",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement dual-layer rate limiting: 1) per-IP window of 15 minutes with a cap (e.g., 100 requests), 2) per-username limiter with a 10-minute window. After 5 failed logins, enforce a 15-minute lockout",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose an end-to-end secure supply chain: sign/verify every image with Sigstore/cosign; generate SBOMs and enforce reproducible builds; use per-tenant envelope encryption with KMS and short-lived cre",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt structured JSON logs and mask sensitive fields at emission, avoiding raw payload logging. Use a reusable redact function that replaces sensitive data (e.g., emails, API keys, passwords) with masked values like `**…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nBrute-force defense at the API layer is a common beginner task with real impact. It tests practical knowledge of rate limiting, lockouts, and safe logging without overengineering.\n\n## Key Concepts\n- Dual-layer rate limiting (IP and username)\n- Temporary lockouts after repeated failures\n- CAPTCHA as a human-verify fallback\n- Safe logging with redaction\n- Basic testing with automated brute-force attempts\n\n## Code Example\n```javascript\nconst rateLimit = require('express-rate-limit');\nconst ipLimiter = rateLimit({ windowMs: 15*60*1000, max: 100 });\n// Per-username limiter would require a store; here's a conceptual sketch\n// app.use('/login', (req, res, next) => { /* increment per-user counter and enforce */ });\n```\n\n## Follow-up Questions\n- How would you migrate from in-memory to Redis for distributed rate limiting?\n- How would you handle accessibility and legitimate users who are temporarily locked out?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-17",
        "questionId": "q-18786",
        "question": "In a multi-tenant serverless platform, each tenant's data is envelope-encrypted with per-tenant KMS keys. A rogue function could misuse KMS to exfiltrate data. Propose a concrete end-to-end guardrail design to prevent data leakage via KMS misuse, covering IAM controls, per-tenant key isolation, admission checks for crypto ops, data-key caching, immutable logs, anomaly detection, key rotation, and an incident response playbook?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use PKCE with short-lived access tokens (15 minutes) and rotation; keep refresh tokens server-side with strict scope checks; avoid storing tokens on disk; set HttpOnly, Secure cookies with SameSite; i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a comprehensive secrets management strategy by migrating sensitive data like DB_PASSWORD to a secure vault service (AWS Secrets Manager or HashiCorp Vault), eliminating secrets from code repositories and CI co…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Store JWTs in HttpOnly, SameSite cookies; drop localStorage usage. Add per-session CSRF tokens (double-submit or header check) and rotate refresh tokens on use. Implement Express middleware to read co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant envelope encryption with KMS: isolate keys by tenant, enforce strict IAM policies, route Encrypt/Decrypt through an admission service that validates tenant scope, cache data keys with short",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAdvanced guardrails prevent data leakage via cryptographic operations. The candidate should articulate concrete controls, threat modeling, and automation.\n\n## Key Concepts\n- Tenant isolation at the key layer (per-tenant KMS keys)\n- Admission control for crypto ops\n- Data-key caching with safe TTLs\n- Immutable, centralized logging and tamper resistance\n- Key rotation, revocation, and incident response\n\n## Code Example\n```javascript\n// Pseudo: mapping tenant -> KMS key ARN\nconst TENANT_KEYS = { t1: 'arn:aws:kms:...:key/abc', t2: 'arn:aws:kms:...:key/def' };\n```\n\n## Follow-up Questions\n- How would you test this in CI/CD and during incident drills?\n- How would you handle cross-tenant data access anomalies and key compromise implications?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-18",
        "questionId": "q-21870",
        "question": "Design an edge-first authentication flow for a global CDN-backed app. Ensure access tokens are never cached at edge, support token revocation within 5 minutes, and enable per-tenant RBAC across 3 regions. Propose architecture, protocols, and key rotation strategy, including how you’d test and monitor it in production?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an end-to-end secure data pipeline for a multi-tenant real-time video moderation service across edge and cloud. Include per-tenant envelope encryption with keys in an HSM, TEEs (SGX/Nitro) for",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-model artifact signing (Ed25519) at upload, store the signature and signer key ID in MongoDB. Verify signatures before serving models; reject unsigned or mismatched versions. Maintain an",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement edge-first OAuth-like tokens: issue very short-lived, opaque tokens signed per-tenant by a central KMS, with a per-request nonce and client fingerprint to prevent reuse at edge. Do not cache",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Enforce per-tenant IaC isolation: module signing and a mandatory Terraform plan review; use tenant-scoped remote state encrypted at rest by Vault; deploy Vault dynamic secrets with short leases and au",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests edge security design, token handling, rotation, revocation, and per-tenant RBAC in a globally distributed environment.\n\n## Key Concepts\n- Edge computing, token lifetimes, and JWKS\n- Per-tenant key rotation and revocation\n- Data minimization at edge and canary deployments\n\n## Code Example\n```javascript\n// Pseudo in-edge validator\nfunction validate(token, jwks){\n  const t = decrypt(token);\n  const valid = verifySignature(t, jwks);\n  return valid && !isRevoked(t.id);\n}\n```\n\n## Follow-up Questions\n- How would you securely rotate tenant keys without downtime?\n- How would you verify no token leakage occurred during a canary rollout?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-19",
        "questionId": "q-9762",
        "question": "You run a multi-cloud Kubernetes SaaS where a single Postgres database holds all tenant data. A breach vector targets high‑privilege services and logs to exfiltrate data. Propose a concrete plan to prevent data leakage at runtime and during incident response. Include: service identity (SPIRE), mTLS in a service mesh (Istio/Linkerd), envelope encryption with KMS, per-tenant Row-Level Security, tamper-evident logging, and an automated incident-response workflow with rollback and forensics steps. List tools, deployment steps, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement short-lived, audience-scoped JWTs bound to TLS connections using token binding or mutual TLS (mTLS). Enforce per-connection unique nonces, maintain least privilege access patterns, protect WebSocket streams wi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify sensitive fields (emails, user IDs) in request data and logs; implement redaction at log emit points (middleware or logger) using a whitelist/regex; switch to structured logs with redacted pl",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a zero-trust multi-cloud kubernetes plan: deploy SPIRE for per-service identities, enable mTLS across the mesh (Istio/Linkerd), store data with envelope encryption using KMS-backed keys, apply",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Chain-of-trust: sign every model artifact with the vendor's KMS key; require SBOM (CycloneDX) and a SHA-256 hash stored in the registry; load-time verification occurs inside a TEEs (Intel SGX/AMD SEV)",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests practical integration of identity, cryptography, data governance, observability, and incident response in a multi-cloud environment.\n\n## Key Concepts\n\n- SPIRE for service identity\n- mTLS in a service mesh (Istio/Linkerd)\n- Envelope encryption with cloud KMS\n- Postgres Row-Level Security per tenant\n- Tamper-evident logging and immutable audit trails\n- Runtime security (Falco) and tracing (OpenTelemetry)\n- Automated IR playbooks and rollback procedures\n\n## Code Example\n\n```sql\n-- Postgres Row-Level Security example\nCREATE POLICY tenant_rls ON data\n  USING (tenant_id = current_setting('app.current_tenant')::int);\nALTER TABLE data ENABLE ROW LEVEL SECURITY;\n```\n\n```yaml\n# Istio mTLS policy\napiVersion: security.istio.io/v1beta1\nkind: PeerAuthentication\nmetadata:\n  name: default\nspec:\n  mtls:\n    mode: STRICT\n```\n\n## Follow-up Questions\n\n- How would you validate isolation during chaos testing?\n- What metrics indicate leakage risk and how would you alert on them?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-20",
        "questionId": "q-29450",
        "question": "Scenario: A containerized web app runs on Kubernetes across multiple namespaces. Secrets for DB and API keys are currently stored in ConfigMaps and mounted as files, risking exposure if a build cache is compromised. Propose a beginner-friendly end-to-end plan to secure secrets: migrate to Kubernetes Secrets with encryption at rest and a Vault/AWS Secrets Manager integration (sidecar) for rotation; mount secrets as environment variables or projected volumes; enable an admission controller to block secrets in images; add a CI gate to prevent secrets in commits; include a minimal example illustrating how to read a secret in Node.js via process.env.DB_PASSWORD?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a secret store (Vault or AWS Secrets Manager) and inject credentials at container startup via an entrypoint or sidecar, never bake them into code or images. Remove secrets from repos, enable CI",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a secure E2E chat design for an enterprise video platform. Use per-chat ephemeral keys stored in an HSM, server-assisted metadata for moderation without decrypting content, forward secrecy via",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Threat model and threat sources. Use Bonawitz secure aggregation with per-round masking; enforce differential privacy budgets per client; require remote attestation and device-bound keys for updates;",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Migrate from ConfigMaps to Kubernetes Secrets with encryption at rest enabled. Implement Vault or AWS Secrets Manager via a sidecar pattern for automated credential rotation. Mount secrets as environment variables or pr…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis evaluates practical understanding of secret lifecycle management and secure deployment practices in real-world Kubernetes environments, not just theoretical knowledge.\n\n## Key Concepts\n- **Kubernetes Secrets with encryption at rest**: Protects secrets from unauthorized access when stored in etcd.\n- **Automated secrets rotation**: Using Vault or AWS Secrets Manager via a sidecar pattern eliminates manual credential updates and reduces exposure window.\n- **Secret mounting strategies**: Environment variables are simpler for quick adoption; projected volumes offer better security with features like short-lived tokens and automatic updates.\n- **Admission controllers**: Tools like Kyverno or OPA Gatekeeper can enforce policies that reject workloads attempting to use embedded secrets.\n- **CI/CD gates**: Pre-commit hooks and scanning tools prevent secrets from entering version control.\n\n## Minimal Node.js Example\n```javascript\n// Read secret from environment variable\nconst dbPassword = process.env.DB_PASSWORD;\n\nif (!dbPassword) {\n  throw new Error('DB_PASSWORD environment variable is not set');\n}\n\n// Use the secret to connect to your database\nconst connection = await db.connect({\n  password: dbPassword\n});\n```\n\n## Best Practices\n1. Always enable encryption at rest for etcd\n2. Use short-lived tokens whenever possible\n3. Rotate secrets frequently\n4. Never log or expose secrets in error messages\n5. Audit secret access regularly",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-21",
        "questionId": "q-15369",
        "question": "You're running a confidential ML inference platform in public clouds for multiple tenants. Tenants submit models and data, sharing GPUs and host OS. An attacker may attempt cross-tenant data leakage via memory side-channels or misconfigured enclaves. Propose a concrete end-to-end security design: TEEs (TDX/SEV), disable memory dedup, data handling, monitoring, and incident response with tooling and trade-offs. How would you implement it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enforce tenant isolation with hardware TEEs (Intel TDX/AMD SEV) for model runtimes; disable memory dedup; use per-tenant keys and envelope encryption; partition GPU memory or use confidential GPUs; enforce encrypted dat…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Begin with removing all hard-coded keys and env vars; attach least-privilege IAM roles to each Lambda; store secrets in AWS Secrets Manager with automatic rotation; reference them at runtime via SDK (",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt workload identity (SPIRE) with short-lived JWTs signed by keys in an HSM (CloudHSM/Thales). Use mTLS with token-binding, zero-downtime rotation via JWKS, per-tenant ACLs via OPA, and short TTLs",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use short-lived OAuth2 tokens issued by an authorization server (OIDC). Bind tokens to tenant, scope, and a limited IP range; issue JWTs with aud, iss, iat, exp, jti; protect via JWKS; enable token in",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical confidential computing design for multi-tenant ML workloads, specifically addressing side-channel attacks, enclave misconfigurations, and cross-tenant data leakage vectors. It evaluates the candidate's understanding of hardware security primitives, threat modeling for shared infrastructure, and the ability to balance security with operational requirements.\n\n## Key Concepts\n\n- **Hardware TEEs and Attestation**: Intel TDX and AMD SEV-SNP provide hardware-enforced isolation with remote attestation to verify the runtime environment's integrity before granting access to sensitive data.\n- **Side-Channel and Memory Deduplication Mitigations**: Disabling memory deduplication prevents cross-tenant information leakage via memory page collision attacks; memory encryption prevents physical tampering.\n- **Per-Tenant Key Management and Envelope Encryption**: Each tenant receives dedicated encryption keys with envelope encryption for data-at-rest, ensuring tenant data remains logically isolated even on shared infrastructure.\n- **Auditability and Tamper-Evident Logs**: Immutable audit trails using append-only storage or blockchain-based logging ensure all data access and model executions are traceable for compliance and forensics.\n- **Incident Response, Forensics, and Rollback**: Automated containment, forensic data collection, and tenant-specific rollback mechanisms enable rapid recovery from security incidents.\n\n## Code Example\n\n```javascript\n// Pseudo attestation flow for confidential ML inference\nasync function attestRuntime(tenantId, attestationQuote) {\n  // 1. Verify TEE attestation quote from hardware\n  const attestationResult = await verifyAttestationQuote(attestationQuote);\n  \n  if (!attestationResult.isValid) {\n    throw new Error('Attestation failed: runtime integrity compromised');\n  }\n  \n  // 2. Verify measured boot hash matches expected values\n  const expectedHash = await getExpectedRuntimeHash(attestationResult.teeType);\n  if (attestationResult.measuredHash !== expectedHash) {\n    throw new Error('Runtime hash mismatch: possible tampering detected');\n  }\n  \n  // 3. Issue tenant-specific session credentials\n  const sessionKey = await deriveSessionKey(tenantId, attestationResult.measurement);\n  \n  return {\n    authorized: true,\n    sessionToken: sessionKey,\n    enclavePublicKey: attestationResult.enclaveKey\n  };\n}\n\n// Example: Initialize secure inference pipeline\nasync function initializeSecureInference(tenantId, modelId, data) {\n  const { sessionToken, enclavePublicKey } = await attestRuntime(\n    tenantId, \n    await getAttestationQuote()\n  );\n  \n  // Encrypt data with enclave key before sending to TEE\n  const encryptedData = await encryptForEnclave(data, enclavePublicKey);\n  \n  // Execute inference within confidential context\n  const result = await executeInference(encryptedData, sessionToken);\n  \n  return result;\n}\n```\n\n## Follow-up Questions\n\n- How to validate side-channel mitigations and test for residual leakage vectors?\n- What are the performance trade-offs when migrating ML workloads to confidential GPUs?\n- How do you handle key rotation in a multi-tenant TEE environment without service disruption?\n- Design the incident response workflow when a tenant reports potential data exposure.\n- How would you implement zero-trust networking between tenants in a shared inference cluster?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-22",
        "questionId": "q-9120",
        "question": "In a globally distributed edge CDN with edge workers handling TLS termination and origin fetches, a suspected supply-chain compromise alters the edge image to leak tokens. Design a defense-in-depth plan to detect, prevent, and respond in real time: attestation and SBOM enforcement, runtime integrity checks, signed, auditable updates with rapid revocation, incident playbooks, and forensics. Include concrete tools and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Define a multi-layered chain: build-time SBOM with verifiable provenance; hardware-backed attestation for every edge image (TPM/SE); runtime integrity checks on startup and during updates; per-region",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-tenant envelope encryption using KMS/Vault, separate storage prefixes per tenant, ABAC with OPA, and mutual TLS between services. Sign all artifacts with Sigstore and enforce per-tenant audit logs",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a defense-in-depth strategy spanning ingestion, storage, and access layers. Enforce per-tenant namespaces for both data and metadata to guarantee strict isolation. Validate all ingested records against strict,…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a phased, backward-compatible PQ migration: keep ES256 while adding a PQ verifier and a hybrid sig path. Use per-tenant envelope keys in KMS with versioned data keys. Phase 1: tokens carry both",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end edge security with supply-chain risk, attestation, and rapid response across geographies.\n\n## Key Concepts\n- Supply-chain security, SBOM, attestation\n- Runtime integrity, downgrade protection\n- Key management, revocation, tamper-evident logging\n- Incident response and forensics\n\n## Code Example\n```javascript\n// Pseudo: verify image attestation against public key\nconst manifest = fetchManifest();\nconst ok = verifySignature(manifest.signature, manifest, PUBLIC_KEY);\nif(!ok) throw new Error('Attestation failed');\n```\n\n## Follow-up Questions\n- How would you measure false positives in runtime integrity checks?\n- What rollback strategy would you use if an edge region cannot be attested for 24 hours?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-23",
        "questionId": "q-241",
        "question": "How would you implement JWT authentication with RS256 signing and refresh token rotation to prevent token replay attacks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use RS256 for asymmetric signing, 15-min access tokens, rotating refresh tokens stored in httpOnly cookies, and invalidate old tokens on each refresh.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-tenant envelope encryption: each tenant has a data key wrapped by a master key in an HSM (e.g., Vault with AWS KMS/CloudHSM). Use Sigstore for build integrity, SBOMs, and OPA for policy.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end isolation plan for a multi-tenant security API that spawns ephemeral AWS Lambda and Google Cloud Run tasks to process tenant data across clouds. How would you enforce tenant boundaries, manage keys,…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement envelope encryption with per-tenant DEKs wrapped by regional KMS/HSM master keys. Enforce strict access control using short-lived credentials, tenant- and region-scoped IAM policies, and network segmentation t…",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nJWT authentication with RS256 provides asymmetric cryptography for enhanced security. Refresh token rotation prevents replay attacks by generating new refresh tokens on each use.\n\n## Implementation Details\n- **RS256 Algorithm**: Uses private/public key pair instead of shared secret\n- **Access Token**: 15-minute expiration, contains user ID and role\n- **Refresh Token**: 7-day expiration, stored in httpOnly secure cookie\n- **Token Rotation**: New refresh token issued each time, old one invalidated\n\n## Code Example\n```javascript\n// Generate JWT with RS256\nconst token = jwt.sign(\n  { userId: user.id, role: user.role },\n  privateKey,\n  { algorithm: 'RS256', expiresIn: '15m' }\n);\n\n// Refresh token rotation\napp.post('/refresh', async (req, res) => {\n  const oldRefreshToken = req.cookies.refresh_token;\n  const newTokens = await generateNewTokens(oldRefreshToken);\n  await invalidateRefreshToken(oldRefreshToken);\n  res.cookie('refresh_token', newTokens.refreshToken, {\n    httpOnly: true, secure: true, sameSite: 'strict'\n  });\n});\n```\n\n## Common Pitfalls\n- Using HS256 instead of RS256 (shared secret vulnerability)\n- Storing refresh tokens in localStorage (XSS risk)\n- Not implementing proper token rotation\n- Missing secure cookie flags\n- Forgetting to invalidate old refresh tokens",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-24",
        "questionId": "q-23115",
        "question": "Design an end-to-end plan to detect and contain a data-exfiltration attempt from a tenant notebook in a shared GPU cluster. The plan must use per-tenant namespaces, eBPF/XDP network policies, strict file-access controls, tamper-evident logging, runtime attestation of compute nodes, and an incident-response playbook including rollback. Include concrete technologies (eBPF, Falco, SPIRE, Vault, HSM, OPA), telemetry sources, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-IP and per-token rate-limiting using a Redis-backed token-bucket with a Lua script. Apply different limits per route (login stricter) and a small burst tolerance (e.g., 20). Return 429 w",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose an end-to-end secure supply chain: sign/verify every image with Sigstore/cosign; generate SBOMs and enforce reproducible builds; use per-tenant envelope encryption with KMS and short-lived cre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use PKCE with short-lived access tokens (15 minutes) and rotation; keep refresh tokens server-side with strict scope checks; avoid storing tokens on disk; set HttpOnly, Secure cookies with SameSite; i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce per-tenant namespaces; use eBPF/XDP to block anomalous egress (DNS/TLS to unapproved destinations); feed a Falco detector with tenant context; require runtime attestation of compute nodes via",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to translate security concepts into a concrete, production-ready plan for a multi-tenant GPU cluster. It probes runtime enforcement, attestation, and incident response under realistic pressure.\n\n## Key Concepts\n- Per-tenant isolation in multi-tenant GPUs\n- eBPF/XDP for outbound traffic control\n- Tamper-evident logs and SBOM-like telemetry\n- Runtime attestation (SPIRE/TPM) and HSM-backed keys (Vault)\n- Policy and response orchestration (OPA, Falco)\n\n## Code Example\n```yaml\n# Falco rule snippet (conceptual)\n- rule: TenantExfilDet\n  desc: Detect exfiltration attempt by tenant notebook\n  condition: spawned_process.owner_tenant != allowed_tenant AND outbound_dns_tcp.dest in {unapproved domains}\n  output: \"EXFIL DETECT: tenant=%tenant process=%proc p=%file dest=%dest\"\n  priority: Warning\n```\n\n## Follow-up Questions\n- How would you test the evasion of eBPF policies under high throughput? \n- What performance/operational trade-offs come with node attestation in a GPU cluster?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-08T05:21:57.902Z",
    "version": 1
  }
]