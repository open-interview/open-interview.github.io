[
  {
    "id": "test-security",
    "channelId": "security",
    "channelName": "Security",
    "title": "Security Knowledge Test",
    "description": "Test your understanding of Security with 25 questions.",
    "questions": [
      {
        "id": "security-q-0",
        "questionId": "q-29036",
        "question": "You manage a global, multi-tenant telemetry platform that ingests web-app data into a streaming pipeline (Kafka-like) and runs per-tenant processing. A misconfigured policy could leak data across tenants. Propose an end-to-end hardening plan covering: 1) per-tenant access controls (RBAC/ABAC) and data-plane isolation, 2) envelope encryption with per-tenant keys and rotation, 3) topic ACLs and service-mesh isolation, 4) runtime attestation for processing containers, 5) tamper-evident logging with secure storage, and 6) an incident-response rollback playbook including data replay/purge. Include concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement tenant-scoped isolation with per-tenant keys and RBAC/ABAC, enforce Kafka topic ACLs, use envelope encryption with a KMS (per-tenant CMK), source/processing attestation (e.g., SPIRE/NodeAtt)",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect a per-tenant WASM runtime with strict sandboxing (Wasmtime) and memory isolation; require module signing and remote attestation against an HSM-backed root. Use per-tenant keys in the HSM, SP",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "XSS (Cross-Site Scripting) attacks inject malicious scripts into trusted websites that execute in users' browsers, while CSRF (Cross-Site Request Forgery) tricks authenticated users into performing unwanted actions on w…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Multi-Tenant Data Isolation Strategy ### Core Architecture Each tenant gets their own encryption key stored in a centralized Key Management Service (KMS). When data needs to be encrypted: 1. The KMS generates a tenan…",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests ability to design end-to-end security in a real multi-tenant streaming pipeline, balancing isolation, performance, and operability. It probes key areas: access control models, cryptographic key management, data-plane boundaries, runtime integrity, and incident response.\n\nKey Concepts\n- Tenant isolation: RBAC/ABAC, service mesh scoping, and strict data-plane boundaries\n- Cryptography: envelope encryption with per-tenant CMKs; key rotation and revocation\n- Data governance: topic ACLs, least privilege, and tenancy mapping\n- Runtime integrity: container attestation and secure boot for processing pods\n- Auditability: tamper-evident logs stored in WORM or immutable storage\n- Resilience: rollback with point-in-time data purge and replay from audit logs\n\nCode Example\n```javascript\n// Policy check sketch: ensure user can access tenant data only for their tenant\nfunction hasTenantAccess(user, resource) {\n  return user.tenantId === resource.tenantId && user.permissions.includes('ACCESS_TENANT_DATA');\n}\n```\n\nFollow-up Questions\n- How would you test rollback reliability without affecting production tenants?\n- Which trade-offs matter most when choosing a KMS vs. in-HSM solutions for high-volume data streams?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-1",
        "questionId": "q-17461",
        "question": "You operate a global edge platform that lets customers deploy custom eBPF programs for traffic filtering and observability. Malicious or buggy programs can escape isolation or leak data. Provide an end-to-end security design: how to securely accept, sign, and revoke modules; per-tenant isolation (namespaces and maps); minimize allowed helpers and helpers; enforce resource quotas; runtime verification/attestation; tamper-evident logging; and an incident-response playbook including rollback to previous bytecode?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-user keys wrapped by a central KMS, AES-256-GCM, and an OT/CRDT-based sync with conflict resolution. Encrypt data client-side; store signed metadata envelopes; rotate keys quarterly; keep tamp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-event nonces and idempotency keys, sign payloads with HMAC-SHA256 using a partner-secret and a timestamp, reject duplicates within a short window (e.g., 5 minutes). Enforce replay protec",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Threat model: In a multi-tenant GPU inference service with confidential computing support, primary threats include memory-side channel attacks where one tenant may infer information from another tenant's GPU memory acce…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Sign each eBPF program with a tenant-scoped KMS key and verify with the kernel verifier before loading. Isolate tenants using separate cgroups/namespaces and per-tenant maps; prune helper functions to",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nEdge eBPF workloads pose kernel-level risks; need strong signing, isolation, and rollback controls.\n\n## Key Concepts\n- Verifiable module signing with tenant-scoped keys\n- Per-tenant namespaces and maps separation\n- Resource quotas and helper/function allowlists\n- In-kernel attestation and tamper-evident logging\n- Incident response with rollback and forensic replay\n\n## Code Example\n```javascript\n// Pseudocode for verification\nfunction verifyProgram(prog, sig, pubKey){\n  return crypto.verify(prog, sig, pubKey)\n}\n```\n\n## Follow-up Questions\n- How would you test the trust boundary for a new eBPF program?\n- How would you handle key compromise and revocation?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-2",
        "questionId": "q-27381",
        "question": "You're operating a multi-tenant data lakehouse that ingests telemetry from untrusted connectors into shared storage. A compromised connector could exfiltrate tenant data or poison data lineage. Propose an end-to-end security plan covering per-tenant isolation, ingest-time schema validation, envelope encryption with per-tenant keys via KMS/HSM, automatic key rotation, immutable tamper-evident logs, data provenance, and an incident-response playbook including rollback and re-ingestion?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a defense-in-depth strategy spanning ingestion, storage, and access layers. Enforce per-tenant namespaces for both data and metadata to guarantee strict isolation. Validate all ingested records against strict,…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement sandboxed UDF runtimes using WebAssembly (WASM) to enforce strict isolation and prevent unauthorized system access. Block network egress from UDFs by default and require explicit approval for data transfers. E…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Pin base image digest in the Dockerfile, generate SBOM with syft . -o json > sbom.json, scan with grype sbom.json, fail CI if high-severity CVEs appear, sign final image with cosign sign, require cosi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On login, generate a fresh session ID, invalidate prior IDs for that user, and ensure only the new session is accepted by the API. Use HttpOnly, Secure, and SameSite=Strict on the session cookie. Stor",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical security controls for a multi-tenant data architecture where untrusted connectors pose risks to data confidentiality, integrity, and lineage. Interviewers seek a unified response spanning isolation, cryptography, logging, provenance, and incident response—demonstrating the candidate's ability to design holistic security solutions for complex data platforms.\n\n## Key Concepts\n- **Tenant isolation**: Namespace-level separation ensuring tenants cannot access or interfere with each other's data\n- **Envelope encryption**: Per-tenant keys with data encrypted under a master key, enabling efficient per-tenant key rotation and access revocation\n- **Tamper-evident logging**: Using cryptographic techniques such as hash chaining or Merkle trees to detect unauthorized modifications to audit records\n- **Data provenance**: Tracking the complete lifecycle of data from source through transformations to final consumption, ensuring lineage integrity and enabling impact analysis",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-3",
        "questionId": "q-25730",
        "question": "Scenario: A microservices app runs on Kubernetes across multiple teams. Describe a beginner-friendly plan to manage credentials and secrets safely across services: when to use an external secret store vs Kubernetes Secrets, RBAC controls, rotation, auditing, and least privilege. Include a minimal YAML/CLI snippet showing how a pod consumes a secret without embedding it in images. How would you test this in CI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sign each log line with a per-event signature using a KMS-backed key, and hash-chain entries to form a tamper-evident ledger. Ingestor validates signatures and hashes, raising alerts on mismatches. St",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-merchant signing keys reside in a hardware Secure Element (SE), with device attestation required before each use. Offline cryptograms are generated within the SE using a nonce and monotonic counter, then signed loca…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement OAuth2/OIDC with PKCE for public clients, issuing 5–10 minute access tokens and short-lived refresh tokens bound to device attestation; enforce mTLS between microservices; add gateway-level",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt an external secret store (e.g., AWS Secrets Manager) with Kubernetes Secrets Store CSI to mount secrets. Enforce per-service RBAC, rotate credentials automatically, and log access for audit. Do",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical secrets management in cloud-native environments, a common beginner-devops skill.\n\n## Key Concepts\n- External secret stores vs Kubernetes Secrets\n- RBAC and least privilege\n- Secret rotation and auditing\n- Secrets mounted as volumes vs baked into images\n\n## Code Example\n```yaml\n# Minimal Secrets Store CSI example\napiVersion: secrets-store.csi.x-k8s.io/v1\nkind: SecretProviderClass\nmetadata:\n  name: my-aws-secrets\nspec:\n  provider: aws\n  parameters:\n    objects: |\n      - objectName: /prod/db/password\n        objectType: secret\n```\n\n## Follow-up Questions\n- How would you test rotation and access auditing in CI/CD?\n- What failure modes might you see during secret rotation during a rollout?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-4",
        "questionId": "q-16892",
        "question": "You're operating a globally distributed, multi-tenant data lake with a Central Policy Service (CPS) that signs per-tenant access policies. Design an end-to-end hardening plan to prevent data leakage if a policy or key is compromised, including policy signing, key rotation, tenant isolation, tamper-evident logging, rollout/rollback, and incident response?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enforce token binding and anti-replay at the gateway. Use short-lived JWTs with unique jti values, track seen jti in Redis per region, validate JWT via JWKS with issuer and audience checks, bind tokens to mTLS or per-cl…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "An end-to-end security plan for a multi-tenant AI inference platform should incorporate artifact signing and provenance verification using Sigstore or cosign, combined with hardware-backed attestation at load time via T…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-user keys wrapped by a central KMS, AES-256-GCM, and an OT/CRDT-based sync with conflict resolution. Encrypt data client-side; store signed metadata envelopes; rotate keys quarterly; keep tamp",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement policy signing with per-tenant keys stored in an HSM; have the gateway fetch the latest signed policy from CPS over mutual TLS; verify with the tenant’s public key; enforce per-tenant RLS in",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses end-to-end control of policy distribution, tenant isolation, and cryptographic safeguards under breach scenarios. Requires concrete engineering choices for signing, rotation, logging, and incident response.\n\n## Key Concepts\n\n- Cryptographic signing and per-tenant key management (HSM)\n- Secure policy distribution and verification (CPS, mTLS, JWKS-ish flows)\n- Tenant isolation and row-level security in data lakes\n- Tamper-evident logging for forensics\n- Rollout strategies, canary releases, rollback, and revocation\n\n## Code Example\n\n```javascript\n// Pseudocode: policy signature verification outline\nfunction verifyPolicySignature(policyBytes, signature, tenantPublicKey) {\n  // crypto verify using tenantPublicKey\n  // return true if valid, false otherwise\n}\n```\n\n## Follow-up Questions\n\n- How would you detect policy drift or abuse after deployment?\n- What are the key rotation and revocation timelines, and how do you avoid service disruption during rotate?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-5",
        "questionId": "q-13037",
        "question": "You run a real-time analytics platform processing tenant data from Kafka to a Spark streaming job, writing to a data lake in S3 and a Redis cache. A misconfiguration or compromised producer could exfiltrate data through unencrypted fields or weak tenant isolation. Propose a concrete end-to-end security plan that prevents leakage across the pipeline: implement per-tenant keys with envelope encryption (KMS), field-level encryption at ingest and in the lake, strict access control and audit logging, tamper-evident logs, rotation and revocation workflows, and an automated IR playbook with rollback. Include deployment steps, tools, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a redaction middleware (Express/Flask) to mask emails, user_id, and tokens in all logs; switch to structured logs with masked fields; enforce log sink access controls (RBAC/IAM); add CI chec",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan uses per-tenant KMS keys with envelope encryption, end-to-end TLS in Kafka/Spark, and field-level encryption before data hits S3. Enforce rotation and revocation of CMKs, strict IAM/VPC controls,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a runtime plugin sandbox using WebAssembly/WASI or Firecracker microVMs for per-plugin isolation. Deploy TPM/HSM-backed attestation with per-plugin digital signatures to cryptographically verify each plugin be…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant IAM roles, envelope encryption with KMS, TLS in transit, redact/derive tokens for PII, immutable logs with HMACs, and bucket policies enforcing encryption and access. Add CI tests provi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess end-to-end data protection in streaming pipelines and multi-tenant isolation at scale.\n\n## Key Concepts\n- Per-tenant KMS keys and envelope encryption\n- Field-level encryption at ingest and in data lake\n- End-to-end TLS and network controls (VPC endpoints)\n- Tamper-evident logging and integrity verification\n- IR playbooks: rollback, forensics, data recovery\n\n## Code Example\n```javascript\n// Example: rotation policy trigger (pseudo-code)\nfunction rotateKMSKeyIfNeeded() {\n  // check usage metrics and rotate CMKs\n}\n```\n\n## Follow-up Questions\n- How would you validate rotation without downtime?\n- What metrics indicate key misuse or leakage risk?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-6",
        "questionId": "q-310",
        "question": "How does TLS 1.3 improve security compared to TLS 1.2?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "TLS 1.3 enhances security by removing insecure cipher suites, reducing handshake round trips from two to one, and mandating forward secrecy through Ephemeral Diffie-Hellman key exchange.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a hybrid migration: keep ECDSA as the primary signer while introducing lattice-based PQC signing in parallel. Issue tokens with dual signatures (ECDSA+PQC) for a 6–12 month window. Validators acce",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a log redaction layer that masks PII before logging, enforce per-tenant log streams (prefix by tenantId) so access is isolated, and ship logs to a separate encrypted sink (e.g., KMS-backed s",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Set up SLSA (Supply-chain Levels for Software Artifacts) as your security framework. Here's the implementation approach: **Build Time:** - Generate SBOMs (Software Bill of Materials) in SPDX or CycloneDX format for ever…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why Asked\nTests understanding of modern security protocols and their evolution\n\n## Key Concepts\nHandshake optimization, cipher suite changes, forward secrecy requirements\n\n## Code Example\n```\n// TLS 1.3 handshake (1-RTT)\nClientHello --> ServerHello\n[EncryptedExtensions] --> [Finished]\n```\n\n## Follow-up Questions\nWhat cipher suites are mandatory in TLS 1.3? How does 0-RTT work?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-7",
        "questionId": "q-18910",
        "question": "You're building a multi-tenant data-export feature for a fintech platform supporting Coinbase and Square-scale customers. Users export transaction data to S3 as Parquet. Design a secure, end-to-end pipeline that prevents PII leakage, enforces per-tenant access, encrypts in transit and at rest, uses tamper-evident logging, and supports automated validation tests and an incident response playbook. Outline architecture, data handling, key management, and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant IAM roles, envelope encryption with KMS, TLS in transit, redact/derive tokens for PII, immutable logs with HMACs, and bucket policies enforcing encryption and access. Add CI tests provi",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Cosign for SBOM signing/verification and Sigstore signing; enforce reproducible builds (BuildKit, pinned deps) and per-tenant keys in an HSM-backed KMS (CloudHSM/Azure Dedicated HSM) with envelope",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Edge attestation with mutual TLS for secure ingestion; per-tenant encryption keys with envelope encryption; tenant-isolated data partitions; field-level PII masking for data minimization; tamper-evident logs using hash…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce a whitelist: allow only safe mime types/extensions, verify content with content detection (file-type), reject anything executable, store uploads outside the webroot in a sandboxed location wit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes hands-on security design for a critical fintech data export flow, focusing on multi-tenant isolation, encryption, logging integrity, and incident readiness.\n\n## Key Concepts\n- Data isolation and tenant-scoped access control\n- Envelope encryption and KMS key management\n- Tamper-evident logging and immutable storage\n- Data redaction and PII protection\n- Automated validation and incident response playbooks\n\n## Code Example\n```javascript\n// PII redaction example for export stream\nfunction redact(record, fields) {\n  const r = { ...record };\n  for (const f of fields) {\n    if (r[f] !== undefined) r[f] = '[REDACTED]';\n  }\n  return r;\n}\n```\n\n## Follow-up Questions\n- How would you test for data leakage in CI/CD?\n- What trade-offs exist between cost and security in such a pipeline?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-8",
        "questionId": "q-12622",
        "question": "You maintain a Node.js API with user accounts. Passwords are stored as SHA-256 hashes with no salt. Outline a beginner-friendly plan to fix password storage with minimal downtime: select salted hashing (bcrypt/Argon2id) and cost/memory, migrate existing users in batches, handle login compatibility during migration, enforce password policy, and add CI tests to prevent regressions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Switch to per-user salted hashes using Argon2id (memory-cost) or bcrypt with cost 12. Migrate in batches: add a marker column, rehash on login for older hashes, and resync background worker until comp",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use strict tenant isolation: per-tenant runtimes with bounded memory/CPU, no shared state. Encrypt tenant data with envelope encryption via KMS and rotate keys; require hardware attestation (SEV/TDX)",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement tenant-scoped data partitions with per-tenant keys, enforce mTLS/SPIFFE identities, and isolate rules sandbox (WASM) with a minimal API surface. Apply envelope encryption for logs, and tampe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a confidential computing architecture with layered isolation: run tenant WebAssembly modules within Trusted Execution Environments (TEEs) such as AMD SEV or Intel TDX, complemented by strong WebAssembly sandbo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess understanding of secure password storage, practical migration strategy, and minimal downtime.\n\n## Key Concepts\n\n- Salted hashing (Argon2id, bcrypt) vs plain SHA-256\n- Migration strategy: in-place rehash on login, batch processing\n- Password policy and rate limiting\n- Security of logs and secrets during migration\n\n## Code Example\n\n```javascript\n// Hashing with bcrypt\nconst bcrypt = require('bcrypt');\nconst hash = await bcrypt.hash(password, 12);\n// Verifying\nconst ok = await bcrypt.compare(password, hashFromDb);\n```\n\n## Follow-up Questions\n\n- How to handle migrating users who never log in during migration?\n- How to measure migration impact on latency and throughput?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-9",
        "questionId": "q-324",
        "question": "How would you design a zero-trust video conferencing platform using WebAuthn passkeys with continuous authentication for enterprise users?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement WebAuthn passkeys, device fingerprinting, behavioral biometrics, and microsegmented access controls with real-time threat detection.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Multi-Tenant Data Isolation Strategy ### Core Architecture Each tenant gets their own encryption key stored in a centralized Key Management Service (KMS). When data needs to be encrypted: 1. The KMS generates a tenan…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Require policy bundles signed by a trusted authority and verified by the gateway before loading. Enforce per-tenant scopes with mTLS, deny-by-default routing, and explicit allow rules. Keep immutable,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a defense-in-depth strategy spanning ingestion, storage, and access layers. Enforce per-tenant namespaces for both data and metadata to guarantee strict isolation. Validate all ingested records against strict,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Interview Context\nThis question assesses system design skills for security-critical applications, testing understanding of modern authentication patterns, continuous security monitoring, and scalable architecture design.\n\n## Non-Functional Requirements\n- **Authentication**: <99.9% uptime, <100ms login latency\n- **Security**: Zero-trust model, MFA required, session timeout 15min\n- **Scalability**: Support 100K concurrent users, 1M registered\n- **Compliance**: SOC2, GDPR, HIPAA ready\n\n## Architecture Components\n- **Authentication Layer**: WebAuthn passkey server, device registry\n- **Continuous Monitoring**: Behavioral analysis engine, anomaly detection\n- **Access Control**: Microsegmentation, just-in-time permissions\n- **Session Management**: Real-time validation, adaptive re-auth\n\n## Key Calculations\n- **Passkey Storage**: 1M users × 2 devices × 1KB = 2GB\n- **Behavioral Data**: 100K users × 50 events/min × 200B = 600MB/hour\n- **Auth Throughput**: 100K logins/hour ÷ 3600s = 28 auth/sec\n\n## Implementation Details\n```typescript\n// WebAuthn registration flow\nasync function registerPasskey(userId: string) {\n  const challenge = await generateChallenge();\n  const credential = await navigator.credentials.create({\n    publicKey: {\n      challenge,\n      rp: { name: \"SecureConf\" },\n      user: { id: userId, name: userId, displayName: userId },\n      pubKeyCredParams: [{ alg: -7, type: \"public-key\" }]\n    }\n  });\n  return await storeCredential(userId, credential);\n}\n```\n\n## Follow-up Questions\n1. How would you handle passkey recovery when devices are lost?\n2. What behavioral metrics would you track for continuous authentication?\n3. How would you design fallback authentication for legacy systems?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-10",
        "questionId": "q-2878",
        "question": "In a production analytics pipeline (Kafka -> Flink -> ClickHouse), you need to protect PII with differential privacy. Propose concrete steps to implement per-event DP budgets, where to inject noise, how to enforce budgets, and how to validate privacy guarantees in staging. Include concrete config hooks and trade-offs between accuracy and privacy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement DP in the streaming layer: clip per-event values, apply Laplace noise to counts/averages in Flink before persisting to ClickHouse. Track privacy budget per user in Redis; on exhaustion, supp",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-tenant model registry with signed artifacts, canary deployments in shadow mode, and automated rollback on drift/poison alerts. Use Sigstore or The Update Framework for artifact integrity, provenan",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement end-to-end image provenance and runtime controls. Sign all container images with Cosign to cryptographically verify integrity and prevent tampering. Generate SBOMs and scan for CVEs during CI to surface known…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Restrict the allowed origin by replacing `Access-Control-Allow-Origin: *` with the specific origin `https://app.example.com`. When credentials (cookies or authorization headers) are required, set `Access-Control-Allow-C…",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\\n\\nTests ability to apply differential privacy to streaming analytics, including budget accounting, data minimization, and production-grade validation in a security-conscious pipeline.\\n\\nKey Concepts\\n\\n- Differential privacy, epsilon budgeting, and clipping\\n- Noise mechanisms (Laplace/Geometric) in streaming aggregates\\n- Budget ledger, per-user enforcement, and fallback modes\\n- Encryption at rest/in transit and auditability\\n\\nCode Example\\n\\n```javascript\\nfunction addLaplaceNoise(x, b){\\n  return x + (Math.random()*2-1)*b\\n}\\n```\\n\\nFollow-up Questions\\n\\n- How would you CI-test privacy budgets with synthetic data?\\n- How to handle multi-tenant budgets across feature flags?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-11",
        "questionId": "q-230",
        "question": "How would you implement a Content Security Policy (CSP) with nonce-based inline script protection to prevent XSS while maintaining compatibility with third-party analytics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable per-IP login throttling with a Redis-backed counter and a per-tenant failure ledger. Apply exponential backoff after N consecutive failures, and require MFA for tenants flagged as high risk. Ce",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Generate cryptographically secure nonces per request and implement CSP headers with framework-specific middleware, ensuring inline scripts use the nonce while third-party analytics are allowlisted.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Adopt a centralized secret manager (Vault or cloud KMS) with short-lived credentials. Use a sidecar injector to refresh creds in pods, and implement a controlled rolling restart on rotation. Add a lig",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate the upload by checking both MIME type and a file signature, allow only .csv with a safe delimiter; store in a private, SSE-enabled bucket with strict IAM; use a service identity to trigger th",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nContent Security Policy with nonce-based protection provides granular control over inline script execution while preventing XSS attacks. Nonces are unique, cryptographically random values generated per HTTP request that must match both in the CSP header and script tags.\n\n## Implementation Details\n\n### Node.js/Express\n```javascript\nconst crypto = require('crypto');\n\nfunction generateNonce() {\n  return crypto.randomBytes(16).toString('base64');\n}\n\napp.use((req, res, next) => {\n  const nonce = generateNonce();\n  res.locals.nonce = nonce;\n  res.setHeader('Content-Security-Policy', \n    `script-src 'nonce-${nonce}' 'self' https://www.google-analytics.com;`\n  );\n  next();\n});\n```\n\n### React/Next.js\n```javascript\n// pages/_document.js\nimport Document, { Html, Head, Main, NextScript } from 'next/document';\n\nclass MyDocument extends Document {\n  static async getInitialProps(ctx) {\n    const nonce = crypto.randomBytes(16).toString('base64');\n    ctx.res.setHeader('Content-Security-Policy',\n      `script-src 'nonce-${nonce}' 'self' https://analytics.google.com;`\n    );\n    const initialProps = await Document.getInitialProps(ctx);\n    return { ...initialProps, nonce };\n  }\n\n  render() {\n    return (\n      <Html>\n        <Head>\n          <script nonce={this.props.nonce}>\n            // Analytics inline script\n          </script>\n        </Head>\n        <body>\n          <Main />\n          <NextScript nonce={this.props.nonce} />\n        </body>\n      </Html>\n    );\n  }\n}\n```\n\n### Python/Django\n```python\nimport secrets\nfrom django.utils.deprecation import MiddlewareMixin\n\nclass CSPNonceMiddleware(MiddlewareMixin):\n    def process_request(self, request):\n        nonce = secrets.token_urlsafe(16)\n        request.csp_nonce = nonce\n\n    def process_response(self, request, response):\n        nonce = getattr(request, 'csp_nonce', None)\n        if nonce:\n            response['Content-Security-Policy'] = \\\n                f\"script-src 'nonce-{nonce}' 'self' https://www.google-analytics.com;\"\n        return response\n```\n\n### Security Best Practices\n- Use 128+ bit entropy for nonce generation\n- Set strict CSP directives for other resource types\n- Implement CSP violation reporting with `report-uri`\n- Consider `strict-dynamic` for modern browsers instead of nonces\n- Test with CSP Evaluator tools before production deployment\n\n### Third-party Analytics Handling\nModern analytics platforms support CSP compliance:\n- Google Analytics 4: Add `https://www.google-analytics.com` to script-src\n- Segment: Use `https://cdn.segment.com`\n- Hotjar: Add `https://*.hotjar.com`\nFor legacy analytics requiring inline scripts, consider proxy implementations or migrate to CSP-compatible versions.",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-12",
        "questionId": "q-241",
        "question": "How would you implement JWT authentication with RS256 signing and refresh token rotation to prevent token replay attacks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-partner short-lived JWTs signed with rotating keys (identified via `kid` in the header) and distributed through JWKS endpoints. Enforce strict claims validation including `iss` and `aud`, verify `exp` and…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use RS256 for asymmetric signing, 15-min access tokens, rotating refresh tokens stored in httpOnly cookies, and invalidate old tokens on each refresh.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Passkeys implement passwordless authentication using device-generated public-key cryptography where private keys are securely stored in hardware enclaves and synchronized across devices via secure cloud services, while…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant isolation across storage, compute, and network layers through a comprehensive defense-in-depth strategy. Storage isolation utilizes row-level security with tenant-specific data partitioning and name…",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nJWT authentication with RS256 provides asymmetric cryptography for enhanced security. Refresh token rotation prevents replay attacks by generating new refresh tokens on each use.\n\n## Implementation Details\n- **RS256 Algorithm**: Uses private/public key pair instead of shared secret\n- **Access Token**: 15-minute expiration, contains user ID and role\n- **Refresh Token**: 7-day expiration, stored in httpOnly secure cookie\n- **Token Rotation**: New refresh token issued each time, old one invalidated\n\n## Code Example\n```javascript\n// Generate JWT with RS256\nconst token = jwt.sign(\n  { userId: user.id, role: user.role },\n  privateKey,\n  { algorithm: 'RS256', expiresIn: '15m' }\n);\n\n// Refresh token rotation\napp.post('/refresh', async (req, res) => {\n  const oldRefreshToken = req.cookies.refresh_token;\n  const newTokens = await generateNewTokens(oldRefreshToken);\n  await invalidateRefreshToken(oldRefreshToken);\n  res.cookie('refresh_token', newTokens.refreshToken, {\n    httpOnly: true, secure: true, sameSite: 'strict'\n  });\n});\n```\n\n## Common Pitfalls\n- Using HS256 instead of RS256 (shared secret vulnerability)\n- Storing refresh tokens in localStorage (XSS risk)\n- Not implementing proper token rotation\n- Missing secure cookie flags\n- Forgetting to invalidate old refresh tokens",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-13",
        "questionId": "q-26898",
        "question": "You operate a global edge API gateway that authenticates tenants and processes high-volume crypto-asset transfers. Design an end-to-end security plan to prevent per-tenant data leakage and supply-chain compromise. Include per-tenant envelope encryption keys, runtime attestation of edge workers, secure update with SBOM enforcement and rollback, crypto agility including post-quantum readiness, and incident response for key compromise or supply-chain breach. Provide concrete tech choices and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sign each eBPF program with a tenant-scoped KMS key and verify with the kernel verifier before loading. Isolate tenants using separate cgroups/namespaces and per-tenant maps; prune helper functions to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant envelope encryption: generate a per-tenant DEK wrapped by a master KMS; edge workers fetch DEKs via attestation. Use runtime attestation (e.g., Intel TDX/AWS Nitro) for edge instances and s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I'd design a zero-trust, multi-layer security plan: per-tenant envelope encryption with KMS/HSM-backed keys; mutual TLS with SPIFFE IDs for all clients; platform attestation (TEE/TPM) for client SDKs",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a secret manager to store the API key securely and inject it into the function as an ephemeral environment variable, avoiding any hard-coding. Apply a least-privilege IAM role that restricts access to only the neces…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical security design at the edge with multi-tenant isolation, cryptographic agility, and supply-chain defenses—areas critical for Tesla's, Coinbase's, and Cloudflare's security demands.\n\n## Key Concepts\n- Tenant isolation and envelope encryption\n- Runtime attestation and edge trust\n- SBOM, secure updates, rollback\n- Crypto agility and PQC readiness\n- Incident response and key management\n\n## Code Example\n```pseudo\n# Pseudocode: rotate tenant DEK via KMS attestation\nfunction rotateTenantKey(tenantId){\n  const attestation = verifyAttestation(tenantId);\n  const dek = kms.rotateDEK(tenantId);\n  localStore.storeEncryptedDEK(tenantId, dek, attestation);\n}\n```\n\n## Follow-up Questions\n- How would you test the attestation workflow end-to-end?\n- What performance trade-offs arise from frequent key rotation at the edge?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-14",
        "questionId": "q-3169",
        "question": "You're building a fintech profile-update API (Express/Node) used by Coinbase-like customers. Outline a beginner-friendly security plan that covers authentication, authorization, input validation, rate limiting, and audit logging. Include concrete, implementable steps and minimal code/config snippets for common tools such as JWT check, Joi validation, and AWS WAF rule?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a secure E2E chat design for an enterprise video platform. Use per-chat ephemeral keys stored in an HSM, server-assisted metadata for moderation without decrypting content, forward secrecy via",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: implement short-lived JWTs with a refresh flow; enforce per-route authorization via scopes; validate inputs with Joi to reject extra fields and enforce length; rate-limit by IP and user with Red",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Edge attestation with mutual TLS for secure ingestion; per-tenant encryption keys with envelope encryption; tenant-isolated data partitions; field-level PII masking for data minimization; tamper-evident logs using hash…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant event namespaces to ensure logical isolation; use OAuth2/OIDC with audience-scoped tokens to enforce tenant boundaries; encrypt events at rest with envelope encryption (KMS/HSM) and in transit with…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical, beginner-friendly security hygiene for fintech APIs, requiring concrete controls and concrete tool usage. It reveals understanding of authentication, authorization, input validation, rate limiting, and auditing, plus real-world constraints like secrets management.\n\n## Key Concepts\n- JWT-based authentication with short lifetimes and refresh tokens\n- Per-route authorization using scopes/roles\n- Input validation and allowlists to prevent bad data and overposting\n- Rate limiting and bot protection for abuse\n- Structured audit logs and alerting for anomalies\n\n## Code Example\n```javascript\n// jwtMiddleware.js\nfunction jwtMiddleware(req, res, next) {\n  const auth = req.headers.authorization;\n  if (!auth || !auth.startsWith('Bearer ')) return res.status(401).send('Unauthorized');\n  const token = auth.split(' ')[1];\n  try {\n    const payload = jwt.verify(token, process.env.JWT_SECRET);\n    req.user = payload;\n    next();\n  } catch (e) {\n    res.status(401).send('Unauthorized');\n  }\n}\n```\n\n```javascript\n// inputValidation.js\nconst schema = Joi.object({\n  fullName: Joi.string().min(1).max(100).required(),\n  email: Joi.string().email().required(),\n  bio: Joi.string().max(500).optional()\n});\n```\n\n## Follow-up Questions\n- How would you extend this for token revocation and rotation?\n- How would you test rate-limiting under burst load and ensure no user lockouts?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-15",
        "questionId": "q-15369",
        "question": "You're running a confidential ML inference platform in public clouds for multiple tenants. Tenants submit models and data, sharing GPUs and host OS. An attacker may attempt cross-tenant data leakage via memory side-channels or misconfigured enclaves. Propose a concrete end-to-end security design: TEEs (TDX/SEV), disable memory dedup, data handling, monitoring, and incident response with tooling and trade-offs. How would you implement it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a zero-trust multi-cloud kubernetes plan: deploy SPIRE for per-service identities, enable mTLS across the mesh (Istio/Linkerd), store data with envelope encryption using KMS-backed keys, apply",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up SLSA (Supply-chain Levels for Software Artifacts) as your security framework. Here's the implementation approach: **Build Time:** - Generate SBOMs (Software Bill of Materials) in SPDX or CycloneDX format for ever…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enforce tenant isolation with hardware TEEs (Intel TDX/AMD SEV) for model runtimes; disable memory dedup; use per-tenant keys and envelope encryption; partition GPU memory or use confidential GPUs; enforce encrypted dat…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Require policy bundles signed by a trusted authority and verified by the gateway before loading. Enforce per-tenant scopes with mTLS, deny-by-default routing, and explicit allow rules. Keep immutable,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical confidential computing design for multi-tenant ML workloads, specifically addressing side-channel attacks, enclave misconfigurations, and cross-tenant data leakage vectors. It evaluates the candidate's understanding of hardware security primitives, threat modeling for shared infrastructure, and the ability to balance security with operational requirements.\n\n## Key Concepts\n\n- **Hardware TEEs and Attestation**: Intel TDX and AMD SEV-SNP provide hardware-enforced isolation with remote attestation to verify the runtime environment's integrity before granting access to sensitive data.\n- **Side-Channel and Memory Deduplication Mitigations**: Disabling memory deduplication prevents cross-tenant information leakage via memory page collision attacks; memory encryption prevents physical tampering.\n- **Per-Tenant Key Management and Envelope Encryption**: Each tenant receives dedicated encryption keys with envelope encryption for data-at-rest, ensuring tenant data remains logically isolated even on shared infrastructure.\n- **Auditability and Tamper-Evident Logs**: Immutable audit trails using append-only storage or blockchain-based logging ensure all data access and model executions are traceable for compliance and forensics.\n- **Incident Response, Forensics, and Rollback**: Automated containment, forensic data collection, and tenant-specific rollback mechanisms enable rapid recovery from security incidents.\n\n## Code Example\n\n```javascript\n// Pseudo attestation flow for confidential ML inference\nasync function attestRuntime(tenantId, attestationQuote) {\n  // 1. Verify TEE attestation quote from hardware\n  const attestationResult = await verifyAttestationQuote(attestationQuote);\n  \n  if (!attestationResult.isValid) {\n    throw new Error('Attestation failed: runtime integrity compromised');\n  }\n  \n  // 2. Verify measured boot hash matches expected values\n  const expectedHash = await getExpectedRuntimeHash(attestationResult.teeType);\n  if (attestationResult.measuredHash !== expectedHash) {\n    throw new Error('Runtime hash mismatch: possible tampering detected');\n  }\n  \n  // 3. Issue tenant-specific session credentials\n  const sessionKey = await deriveSessionKey(tenantId, attestationResult.measurement);\n  \n  return {\n    authorized: true,\n    sessionToken: sessionKey,\n    enclavePublicKey: attestationResult.enclaveKey\n  };\n}\n\n// Example: Initialize secure inference pipeline\nasync function initializeSecureInference(tenantId, modelId, data) {\n  const { sessionToken, enclavePublicKey } = await attestRuntime(\n    tenantId, \n    await getAttestationQuote()\n  );\n  \n  // Encrypt data with enclave key before sending to TEE\n  const encryptedData = await encryptForEnclave(data, enclavePublicKey);\n  \n  // Execute inference within confidential context\n  const result = await executeInference(encryptedData, sessionToken);\n  \n  return result;\n}\n```\n\n## Follow-up Questions\n\n- How to validate side-channel mitigations and test for residual leakage vectors?\n- What are the performance trade-offs when migrating ML workloads to confidential GPUs?\n- How do you handle key rotation in a multi-tenant TEE environment without service disruption?\n- Design the incident response workflow when a tenant reports potential data exposure.\n- How would you implement zero-trust networking between tenants in a shared inference cluster?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-16",
        "questionId": "q-7537",
        "question": "In a multi-cloud data processing platform used by enterprise customers, design a secure data plane that processes PII in real time while enforcing privacy by design. Propose a confidential computing approach (e.g., enclaves or TEEs), key management, cross-cloud attestation, encrypted data in transit and at rest, and a fail-safe incident playbook. Include concrete technologies, trade-offs, and measurable controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a confidential computing data plane with per-tenant enclaves (Intel SGX/ Nitro Enclaves / Azure Confidential Computing) to decrypt/process data only inside TEEs. Use envelope encryption with KMS",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a hybrid migration: keep ECDSA as the primary signer while introducing lattice-based PQC signing in parallel. Issue tokens with dual signatures (ECDSA+PQC) for a 6–12 month window. Validators acce",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "AES-256-GCM combines Galois/Counter Mode encryption with authentication, providing 256-bit encryption and a 128-bit authentication tag in a single cryptographic operation.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: redact PII in logs, switch to structured logging excluding emails and other identifiers, enforce RBAC on the log sink, and add a CI gate to fail PRs that emit unredacted PII. Include a minimal r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical application of confidential computing across multi-clouds, including key management, attestation, and incident response for privacy-preserving real-time data processing.\n\n## Key Concepts\n\n- Confidential computing TEEs (SGX, Nitro Enclaves, Azure CC)\n- Envelope encryption and KMS key management\n- Cross-cloud attestation and per-tenant isolation\n- mTLS, tamper-evident logging, and incident playbooks\n\n## Code Example\n\n```javascript\n// Pseudo: verify a TEEs attestation before processing sensitive payload\nasync function verifyAttestation(attestation, expectedAudience) {\n  // fetch attestation report from the TEEs\n  const report = await fetchAttestationReport(attestation);\n  // validate signature, nonce, and audience\n  return report.signatureIsValid && report.audience === expectedAudience && !report.revoked;\n}\n```\n\n## Follow-up Questions\n\n- How would you handle key rotation across clouds without service downtime?\n- What are the trade-offs between SGX vs Nitro Enclaves for latency sensitive workloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-17",
        "questionId": "q-23998",
        "question": "You operate a hybrid cloud security platform that enforces tenant-specific runtime policies across 1,000 microservices in Kubernetes and serverless functions. A single misconfigured policy could exfiltrate data or disrupt apps. Propose an end-to-end plan for policy distribution, per-tenant isolation, policy signing and revocation, versioned rollbacks, tamper-evident audit logs, and an incident playbook for policy abuse or accidental outages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an end-to-end policy distribution and enforcement pipeline: a central policy store signs each policy, a distributor pushes updates per tenant, enforcement runs in the workload safe-guarded nam",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "OAuth 2.0 handles authorization with access tokens (Bearer format) for resource access, while OIDC adds authentication layer with ID tokens (JWT) containing user identity. OIDC introduces standard scopes (openid, profil…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-user rate limiting with a Redis token bucket (e.g., 10 attempts per 15 minutes) and an account lockout after 5 consecutive failures within 15 minutes. Validate passwords with bcrypt using cons",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant keys with envelope encryption (KMS, HSM), runtime isolation via WASM sandboxes or gVisor, strict IAM least privilege, and per-tenant data partitioning. Redact metadata at ingest,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests design skills for secure, scalable policy governance in a multi-tenant hybrid cloud, focusing on signing, isolation, versioning, auditability, and incident response.\n\n## Key Concepts\n- Policy as code and signing\n- Per-tenant isolation (namespaces/RBAC)\n- Versioning and safe rollback\n- Tamper-evident logging and immutable storage\n- Auto-disable and incident response workflows\n\n## Code Example\n```javascript\n// Pseudo: verifyPolicySignature(policy, sig, publicKey)\nfunction verifyPolicy(policy, signature, pubKey){\n  // crypto verification placeholder\n  return true\n}\n```\n\n## Follow-up Questions\n- How would you test rollback safety in CI/CD?\n- How would you detect and contain policy drift across tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-18",
        "questionId": "q-29333",
        "question": "Design an end-to-end security plan for a multi-tenant AI inference service that processes tenant prompts in confidential compute enclaves and stores provenance in a tamper-evident ledger. Address per-tenant key isolation, attestation, model integrity, data leakage prevention across vector DB and caches, and incident response for enclave compromise. Compare SGX vs SEV trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant HSM-backed signing keys that issue short-lived JWTs (5-15 min) bound to device attestation and the target resource. Use mTLS between edge and auth services, token binding (audience, nonce),",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement tenant-scoped keys in a KMS with envelope encryption, require attestation of confidential enclaves prior to inference, and record provenance hashes in a tamper-evident ledger. Isolate tenant",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement policy signing with per-tenant keys stored in an HSM; have the gateway fetch the latest signed policy from CPS over mutual TLS; verify with the tenant’s public key; enforce per-tenant RLS in",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use short-lived OAuth2 tokens issued by an authorization server (OIDC). Bind tokens to tenant, scope, and a limited IP range; issue JWTs with aud, iss, iat, exp, jti; protect via JWKS; enable token in",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests advanced security design for multi-tenant AI services, focusing on confidential compute, tenant isolation, attestation, data provenance, and incident response. It also requires evaluating trade-offs between SGX-style enclaves and SEV-style virtualization.\n\n## Key Concepts\n\n- Threat modeling for enclaves and shared services\n- Per-tenant key management and data isolation\n- Attestation, attestation sealing, and model integrity verification\n- Data provenance, tamper-evident logging, and incident response\n- SGX vs SEV trade-offs and performance implications\n\n## Code Example\n\n```javascript\nfunction verifyAttestation(att, expected) {\n  // verify enclave attestation signature and measurements\n  return att.signatureVerified && att.measurements.includes(expected);\n}\n```\n\n## Follow-up Questions\n\n- How would you rotate tenant keys with zero-downtime?\n- How would you test for side-channel leaks in confidential compute environments?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-19",
        "questionId": "q-8759",
        "question": "In a global, multi-tenant data analytics platform, you enable customers to upload custom WASM modules that run against their own data in a sandboxed layer. Design a security model to prevent data leakage, ensure module integrity, enforce per-tenant access, attest runtimes, and provide incident response. Include concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan to prevent leakage in a multi-tenant Snowflake data lake and a containerized ML training service. Include tenant isolation (RBAC/labels), differential privacy for training data, confidential comp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a dual-signed OTA with hardware-backed attestation. Include SBOM and reproducible-build hash in the package; devices verify both against a trusted root and TPM/TEE attestation. Deliver delta",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Module signing with Ed25519, SBOM, runtime sandbox (Wasmtime), per-tenant policy via OPA with SPIFFE IDs, mTLS between runtimes and control plane, data isolation with tenant-scoped namespaces, runtime",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use policy-as-code: add an OPA policy that blocks any Terraform plan that creates an AWS S3 bucket with a public access policy unless a specific tag PUBLIC=true is present. Enforce in CI (terraform va",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design security controls for a multi-tenant runtime extension system with real-world components (WASM, SBOM, attestation).\n\n## Key Concepts\n\n- WASM sandboxing, code signing, SBOM, runtime attestation, per-tenant isolation, supply chain hygiene, incident response.\n- Trade-offs: performance impact, revoke latency, complexity of policy.\n\n## Code Example\n\n```javascript\n// Not required here\n```\n\n## Follow-up Questions\n\n- How would you test this security model under a supply-chain attack?\n- Which metrics indicate successful isolation without harming latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-20",
        "questionId": "q-22386",
        "question": "You're deploying a multi-tenant SaaS where tenants upload policy modules (WASM) that run inside sandboxed microservices to govern workflows. A rogue module could exfiltrate data or bypass fences. Design an end-to-end pipeline to securely deliver, sign, and revoke policies, enforce per-tenant isolation with attestation, minimize API surface, and implement tamper-evident logging plus an incident response playbook for policy compromise. Include concrete tech choices and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Migrate from ConfigMaps to Kubernetes Secrets with encryption at rest enabled. Implement Vault or AWS Secrets Manager via a sidecar pattern for automated credential rotation. Mount secrets as environment variables or pr…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design uses envelope encryption: each tenant has a unique Data Encryption Key (DEK) wrapped by a Central Master Key stored in a FIPS-140-2 HSM/KMS. Use per-tenant IAM roles, no shared DEKs, and automa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant policy signing with an HSM/KMS and strict WASM sandboxing. Load policies with a hash and signature check; bind them to tenant IDs to prevent leakage. Limit WASM bindings to a minimal API su",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a comprehensive secrets management strategy by migrating sensitive data like DB_PASSWORD to a secure vault service (AWS Secrets Manager or HashiCorp Vault), eliminating secrets from code repositories and CI co…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical security design for policy distribution in a multi-tenant WASM runtime, stressing trust, isolation, and incident response.\n\n## Key Concepts\n\n- WASM sandboxing and host bindings\n- Policy signing with KMS/HSM and hash verification\n- Tenant isolation namespaces\n- Tamper-evident logging and audit trails\n- Revocation, versioning, and rollback\n\n## Code Example\n\n```javascript\n// Pseudo verify\nif (!verifySignature(policyBytes, policySignature, publicKey)) { throw new Error('Invalid policy') }\n```\n\n## Follow-up Questions\n\n- How would you test policy updates at scale?\n- How would you detect and respond to policy tampering in production?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-21",
        "questionId": "q-26582",
        "question": "In a shared data lake used by multiple tenants (e.g., partner apps at Meta, Salesforce, Goldman Sachs), tenants can run SQL queries that may join cross-tenant data. Propose a concrete runtime boundary design that enforces per-tenant row-level and column-level access controls, dynamic data redaction, and policy-driven query rewriting. Include a centralized policy store, tests under high concurrency, how to handle policy drift, and rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a runtime boundary that injects tenant-scoped predicates and column masking at query time. Use a central, versioned Policy Store to drive per-tenant ACLs, and extend the query planner to rew",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a multi-layered defense strategy: (1) Partition edge caches by tenant using dedicated namespaces with strict isolation boundaries; (2) Guard encryption keys in hardware-backed TEEs with envelope encryption, ma…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Migrate from ConfigMaps to Kubernetes Secrets with encryption at rest enabled. Implement Vault or AWS Secrets Manager via a sidecar pattern for automated credential rotation. Mount secrets as environment variables or pr…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a hardware root of trust per vehicle (TPM2.0/Edge SEP) with measured boot and a secure OTA agent. Sign each plugin with per-device keys; validate the full certificate chain and SBOM before load; p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess ability to design runtime policy enforcement for multi-tenant data access, addressing drift, performance, and rollback.\n\n## Key Concepts\n\n- Fine-grained access control: row-level and column-level\n- Query rewriting and runtime enforcement\n- Centralized, versioned policy store and drift detection\n- Tamper-evident auditing and safe rollback\n\n## Code Example\n\n```javascript\n// Pseudo: rewriteQuery(query, tenantId, policyStore)\nfunction rewriteQuery(query, tenantId, policyStore) {\n  const policy = policyStore.getLatest(tenantId);\n  const where = `tenant_id = ${escape(tenantId)}`;\n  const redactions = policy.redactions || [];\n  return `SELECT ${policy.columns.map(c => (redactions.includes(c) ? `MASK(${c})` : c)).join(', ')} FROM (${query}) AS t WHERE ${where}`;\n}\n```\n\n## Follow-up Questions\n\n- How would you test under high concurrency and ensure no cross-tenant data leakage?\n- How do you detect policy drift and trigger rollback with replay validation?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-22",
        "questionId": "q-295",
        "question": "How does AES-256-GCM provide both confidentiality and integrity in a single cryptographic operation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition data by tenant using per-tenant prefixes in the data lake and enforce envelope encryption with per-tenant keys via KMS/HSM. Require mTLS between services, least-privilege IAM, and per-tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use express-rate-limit to cap IP login attempts (e.g., 10/min) and a Redis-backed per-user failure counter with 15-minute TTL. On 5th failure, set a user-lock flag and return 429 for 15 minutes. Emit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "AES-256-GCM combines Galois/Counter Mode encryption with authentication, providing 256-bit encryption and a 128-bit authentication tag in a single cryptographic operation.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant envelope encryption with region-local KMS/HSMs; streams encrypted in transit (TLS) and at rest (AES-256-GCM); per-tenant keys rotated automatically; data locality enforced by region-bound s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why Asked\nTests understanding of modern authenticated encryption modes and their security properties.\n\n## Key Concepts\n- AES-256 symmetric encryption\n- Galois/Counter Mode (GCM)\n- Authenticated encryption\n- Integrity verification via authentication tag\n\n## Code Example\n```\nconst key = crypto.randomBytes(32);\nconst iv = crypto.randomBytes(12);\nconst cipher = crypto.createCipher('aes-256-gcm', key, iv);\nlet encrypted = cipher.update(plaintext, 'utf8', 'hex');\nencrypted += cipher.final('hex');\nconst authTag = cipher.getAuthTag();\n```\n\n## Follow-up Questions\n- Why is GCM preferred over CBC mode?\n- How does the authentication tag verify integrity?\n- What are the security implications of nonce reuse in GCM?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-23",
        "questionId": "q-21325",
        "question": "Describe a secure OTA firmware update pipeline for a global EV fleet (edge/vehicle) that prevents supply-chain tampering, per-vehicle attestation, secure boot, and rollback while preserving privacy and auditability. Include: artifact signing in an HSM, SBOM validation, per-vehicle attestation over mTLS, canary rollout, tamper-evident logs, revocation, and an incident playbook?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: enable real-time detection via OpenTelemetry traces, Istio flow logs, and canary signals; isolate impacted tenants by revoking short‑lived tokens and quarantining the feature-flag service in the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a plan to enforce tenant boundaries for UDFs in a multi-tenant analytics platform: isolate each tenant's UDFs in unprivileged WASM sandboxes, enforce strict API surfaces, and pin policies with",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Artifacts must be reproducible and signed within an HSM; SBOMs are validated against a trusted feed. Vehicles attest bootloader, firmware, and update package over mTLS using per-vehicle certificates. Implement canary ro…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a layered approach: private inference in attested enclaves (Intel SGX/AMD SEV) or isolated GPUs; per-tenant namespaces and data minimization; in-memory encryption with KEKs tied to attestation; di",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes end-to-end OTA security for fleets, combining supply-chain integrity, device attestation, and incident response—critical competencies in edge and vehicle ecosystems like those at Cloudflare and Tesla.\n\n## Key Concepts\n\n- Secure OTA pipelines and reproducible builds\n- Hardware Security Modules (HSM) for artifact signing\n- SBOM validation and trusted software provenance\n- Per-vehicle attestation over mTLS\n- Canary rollout and safe rollback strategies\n- Tamper-evident logging and key revocation\n- Automated incident response playbooks\n\n## Code Example\n\n```python\ndef verify_update(update_bytes, sbom, vehicle_cert, hsm_signature):\n    if not hsm_signature.verify(update_bytes):\n        raise SecurityError(\"Invalid HSM signature\")\n    if not sbom.validate_trusted_feed():\n        raise SecurityError(\"SBOM validation failed\")\n    if not vehicle_attest(vehicle_cert):\n        raise SecurityError(\"Attestation failed\")\n    return True\n```",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-24",
        "questionId": "q-23115",
        "question": "Design an end-to-end plan to detect and contain a data-exfiltration attempt from a tenant notebook in a shared GPU cluster. The plan must use per-tenant namespaces, eBPF/XDP network policies, strict file-access controls, tamper-evident logging, runtime attestation of compute nodes, and an incident-response playbook including rollback. Include concrete technologies (eBPF, Falco, SPIRE, Vault, HSM, OPA), telemetry sources, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant envelope encryption: generate a per-tenant DEK wrapped by a master KMS; edge workers fetch DEKs via attestation. Use runtime attestation (e.g., Intel TDX/AWS Nitro) for edge instances and s",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enforce per-tenant namespaces; use eBPF/XDP to block anomalous egress (DNS/TLS to unapproved destinations); feed a Falco detector with tenant context; require runtime attestation of compute nodes via",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Cosign for SBOM signing/verification and Sigstore signing; enforce reproducible builds (BuildKit, pinned deps) and per-tenant keys in an HSM-backed KMS (CloudHSM/Azure Dedicated HSM) with envelope",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Publish policy updates as JSON Web Signatures (JWS) using Ed25519, signed by a central authority with the public key securely pinned to each edge runtime's Hardware Security Module (HSM). Edge runtimes verify signatures…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to translate security concepts into a concrete, production-ready plan for a multi-tenant GPU cluster. It probes runtime enforcement, attestation, and incident response under realistic pressure.\n\n## Key Concepts\n- Per-tenant isolation in multi-tenant GPUs\n- eBPF/XDP for outbound traffic control\n- Tamper-evident logs and SBOM-like telemetry\n- Runtime attestation (SPIRE/TPM) and HSM-backed keys (Vault)\n- Policy and response orchestration (OPA, Falco)\n\n## Code Example\n```yaml\n# Falco rule snippet (conceptual)\n- rule: TenantExfilDet\n  desc: Detect exfiltration attempt by tenant notebook\n  condition: spawned_process.owner_tenant != allowed_tenant AND outbound_dns_tcp.dest in {unapproved domains}\n  output: \"EXFIL DETECT: tenant=%tenant process=%proc p=%file dest=%dest\"\n  priority: Warning\n```\n\n## Follow-up Questions\n- How would you test the evasion of eBPF policies under high throughput? \n- What performance/operational trade-offs come with node attestation in a GPU cluster?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-11T10:03:53.642Z",
    "version": 1
  }
]