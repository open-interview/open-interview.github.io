[
  {
    "id": "test-security",
    "channelId": "security",
    "channelName": "Security",
    "title": "Security Knowledge Test",
    "description": "Test your understanding of Security with 25 questions.",
    "questions": [
      {
        "id": "security-q-0",
        "questionId": "q-534",
        "question": "How would you implement secure session management in a distributed web application to prevent session hijacking and fixation attacks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Threat model: In a multi-tenant GPU inference service with confidential computing support, primary threats include memory-side channel attacks where one tenant may infer information from another tenant's GPU memory acce…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a TUF-based update framework with per-device attestation via TPM/Secure Element, delivering cryptographically signed delta payloads to edge devices. Utilize A/B partitioning for atomic over-the-air updates wit…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement HTTP-only, secure cookies with SameSite=Strict attribute, server-side session storage using Redis, session ID rotation on authentication, short TTL values, CSRF tokens, and origin header validation.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Adopt per-tenant envelope encryption: each tenant gets a DEK used to encrypt secrets; a KEK in an HSM-backed KMS wraps each DEK. Rotate KEKs quarterly and rewrap DEKs. Maintain tamper-evident logs wit",
            "isCorrect": false
          }
        ],
        "explanation": "## Secure Session Management Implementation\n\n### Core Cookie Protections\n- **HTTP-only cookies**: Prevent JavaScript access to session tokens\n- **Secure flag**: Enforce HTTPS-only transmission\n- **SameSite=Strict**: Block cross-site request forgery attempts\n\n### Session Storage & Lifecycle\n- **Redis storage**: Fast, distributed session cache with high availability\n- **Session rotation**: Generate new session IDs after authentication events\n- **Short TTL**: Implement 15-30 minute inactivity timeouts\n\n### Additional Security Controls\n- **CSRF tokens**: Validate anti-forgery tokens on state-changing requests\n- **Origin validation**: Verify Referer and Origin headers\n- **IP binding**: Optional additional validation for high-security endpoints\n\n```javascript\n// Secure session configuration example\napp.use(session({\n  name: 'sessionId',\n  secret: process.env.SESSION_SECRET,\n  cookie: {\n    httpOnly: true,\n    secure: true,\n    sameSite: 'strict',\n    maxAge: 30 * 60 * 1000 // 30 minutes\n  },\n  store: new RedisStore({ client: redisClient }),\n  rolling: true,\n  resave: false,\n  saveUninitialized: false\n}));\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-1",
        "questionId": "q-22530",
        "question": "Design an end-to-end security plan for a new offline-capable POS component used by merchants in Square's ecosystem that can generate transaction cryptograms offline and later sync to the payment network. Specifically, outline per-merchant key provisioning, secure element usage, offline data handling, replay protection, signing/verification, device attestation, tamper-evident logs, secure OTA updates, and incident response with key revocation and rollback. Which technologies would you choose and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant enclaves with isolated sealing keys; require remote attestation before any processing; encrypt data at rest and in transit with per-tenant envelope keys stored in a hardware security module",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-merchant signing keys reside in a hardware Secure Element (SE), with device attestation required before each use. Offline cryptograms are generated within the SE using a nonce and monotonic counter, then signed loca…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "PFS uses Ephemeral Diffie-Hellman (ECDHE) where each session generates unique key pairs, ensuring past sessions remain secure even if long-term private keys are compromised. In ECDHE, client and server exchange ephemera…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce per-tenant namespaces; use eBPF/XDP to block anomalous egress (DNS/TLS to unapproved destinations); feed a Falco detector with tenant context; require runtime attestation of compute nodes via",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked: This question evaluates real-world handling of offline capability and hardware-backed security in a high-stakes payment flow. It requires threat modeling, per-merchant key management, attestation, replay protection, secure updates, and incident response—concepts directly relevant to Square and Apple.\n\n## Key Concepts\n- Hardware Security Modules and Secure Elements\n- Attestation and device identity\n- Replay protection using nonces and monotonic counters\n- Secure OTA with dual signing\n- Tamper-evident logging and SBOM verification\n- Incident response and key revocation\n\n## Code Example: Device Attestation Flow\n\n```python\n# Simplified attestation flow\ndef verify_attestation(attestation_cert, nonce):\n    # Verify certificate chain to trusted root\n    if not verify_certificate_chain(attestation_cert):\n        return False, \"Invalid certificate chain\"\n    \n    # Verify attestation signature\n    signature = attestation_cert.signature\n    if not verify_ecdsa_signature(attestation_cert.public_key, nonce, signature):\n        return False, \"Invalid signature\"\n    \n    return True, \"Attestation verified\"\n\ndef generate_offline_cryptogram(secure_element, transaction_data):\n    # Get current monotonic counter\n    counter = secure_element.get_monotonic_counter()\n    \n    # Generate nonce\n    nonce = secure_element.generate_random(16)\n    \n    # Create cryptogram payload\n    payload = {\n        \"transaction_data\": transaction_data,\n        \"counter\": counter,\n        \"nonce\": nonce,\n        \"timestamp\": get_current_timestamp()\n    }\n    \n    # Sign within Secure Element\n    signature = secure_element.sign(payload)\n    \n    return {\n        \"cryptogram\": payload,\n        \"signature\": signature\n    }\n```\n\n## Deep Dive: Architecture Components\n\n### 1. Per-Merchant Key Provisioning\n- Each merchant receives a unique signing key pair injected during manufacturing or onboarding\n- Keys are derived from a master key using merchant ID as diversification input\n- Private keys never leave the Secure Element\n- Key injection occurs in secure facility with HSM support\n\n### 2. Secure Element Usage\n- SE provides isolated execution environment for cryptographic operations\n- Stores per-merchant keys and handles all signing operations\n- Implements tamper detection (voltage, temperature, glitching)\n- Supports multiple cryptographic algorithms (ECDSA, AES)\n\n### 3. Offline Data Handling\n- Transactions are stored in encrypted local storage when offline\n- Each transaction gets a unique identifier and timestamp\n- Data structure includes: transaction amount, merchant ID, terminal ID, nonce, counter\n- Maximum offline transaction limit enforced based on risk assessment\n\n### 4. Replay Protection\n- Monotonic counter ensures each transaction has a unique incrementing value\n- Backend maintains counter state per device\n- Counter overflow handling with key rotation\n- Nonces prevent replay within same counter epoch\n\n### 5. Signing and Verification\n- ECDSA P-256 for digital signatures\n- Each cryptogram signed with merchant's private key inside SE\n- Backend verifies signature before processing\n- Atomic transaction processing ensures no partial states\n\n### 6. Device Attestation\n- Device identity certificate issued during manufacturing\n- Attestation key derived from device unique identifier (UDID)\n- Certificate chain validated against trusted root\n- Challenge-response protocol prevents replay of attestations\n\n### 7. Tamper-Evident Logs\n- Immutable log entries using hash chaining\n- Each entry includes: previous hash, timestamp, event type, metadata\n- Logs stored both locally and synced to backend\n- Regular integrity checks detect tampering\n\n### 8. Secure OTA Updates\n- Dual signing: manufacturer + vendor keys\n- Update package includes: firmware, manifest, signatures\n- Secure boot chain validates each stage\n- Rollback protection prevents downgrades to vulnerable versions\n\n### 9. Incident Response\n- Real-time monitoring for anomalous patterns\n- Key revocation list distributed to all processing nodes\n- Compromised devices can be remotely deactivated\n- Automatic key rotation on suspected compromise\n- Rollback capability for transaction reversal if needed\n\n## Threat Model Considerations\n\n| Threat Vector | Mitigation |\n|--------------|------------|\n| Physical attacks on SE | Tamper detection, zeroization on breach |\n| Key extraction | Keys never exposed outside SE |\n| Replay attacks | Nonces + monotonic counters |\n| Man-in-middle | TLS 1.3 for all sync communications |\n| Rogue devices | Device attestation required |\n| Rollback attacks | Secure boot + signed updates |\n| Insider threats | HSM-based key injection, separation of duties |\n\n## Technologies Selected\n\n| Component | Technology | Rationale |\n|-----------|-----------|----------|\n| Secure Element | ARM TrustZone + Secure Core | Industry standard, hardware-assisted isolation |\n| Signing Algorithm | ECDSA P-256 | NIST approved, efficient for embedded |\n| Key Diversification | HKDF | Standard key derivation function |\n| Attestation | FIDO Device Attestation | Open standard, broad ecosystem support |\n| Secure Storage | Hardware-encrypted flash | Defense in depth |\n| Transport | TLS 1.3 | Latest protocol with forward secrecy |\n| Log Integrity | Hash chaining (Merkle trees) | Tamper-evident without centralized trust |\n| Update Signing | ED25519 + RSAPSS | Dual signature scheme for extra security |",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-2",
        "questionId": "q-16610",
        "question": "You're distributing a JavaScript/TypeScript SDK to thousands of partner apps via npm and a CDN. If a CDN compromise taints the SDK to exfiltrate tokens, propose an end-to-end hardening plan: HSM-backed artifact signing, reproducible builds, per-version SRI, CDN signing, TLS pinning or origin validation, runtime integrity checks, revocation/hotfix workflow, and an incident-response plan with rollback and customer notification. Include trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a per-tenant data plane using Kubernetes namespaces, Firecracker sandboxed inference, and envelope encryption with per-tenant KMS keys. Ingress uses mutual TLS; data at rest encrypted with t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Validate incoming JSON against a strict schema (no extra properties), reject unknown keys, and sanitize all strings before storing or rendering. Log only redacted or sanitized content; use parameteriz",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline an end-to-end plan to enforce per-tenant data minimization and redaction across a high-traffic edge API gateway proxying to services in multiple clouds. Include policy model (OPA), redaction h",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Sign artifacts with an HSM-backed key and enforce reproducible builds; publish per-version SRI digests; CDN signing; TLS pinning or origin validation for SDK fetch; embed runtime integrity checks in t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nInterview context explanation about supply chain risk for widely distributed SDKs and realistic containment.\n\n## Key Concepts\n\n- Software supply-chain security\n- Code signing with HSM\n- Reproducible builds\n- Per-version SRI/digests\n- CDN trust and signing\n- Runtime integrity checks\n- Revocation and incident response\n\n## Code Example\n\n```javascript\n// Example: verify digest at runtime\nconst fs = require('fs');\nconst crypto = require('crypto');\nconst data = fs.readFileSync(process.argv[2]);\nconst digest = crypto.createHash('sha256').update(data).digest('hex');\nif (digest !== EXPECTED_DIGEST) throw new Error('Integrity check failed');\n```\n\n## Follow-up Questions\n\n- How would you rotate signing keys without breaking partner apps?\n- How would you measure performance impact of integrity checks on startup and in constrained environments?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-3",
        "questionId": "q-21385",
        "question": "In a cloud security platform offering customer-managed encryption keys (CMEK) across multi-region deployments, design an end-to-end incident response for a suspected key compromise. Include detection, key revocation, regional rotation, data re-encryption, access policy updates, audit logging, and post-incident steps; specify technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To secure a webhook endpoint, implement HMAC SHA-256 verification using the provider's secret to compute a signature over the raw body, compare to the X-Signature header, and reject if mismatch. Inclu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Detect anomalies in CMEK usage (cloud logs, HSM attestation). Immediately revoke affected CMKs, rotate regionally, and issue new KEKs. Re-encrypt all tenant data with fresh CMKs, updating envelope key",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement per-tenant keys provisioned from a KMS or HSM with 24-hour rotation. Sign each data packet using HMAC-SHA256 with the tenant-specific key to ensure integrity and authenticity. Apply envelope encryption: a per-…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant HSM-backed signing keys that issue short-lived JWTs (5-15 min) bound to device attestation and the target resource. Use mTLS between edge and auth services, token binding (audience, nonce),",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical end-to-end handling of encryption key compromise, a high-severity incident that spans IAM, KMS, storage, and data planes.\n\n## Key Concepts\n- CMEK lifecycle and rotation\n- Cross-region key management\n- Data re-encryption strategies (streaming, offline)\n- Tamper-evident audit trails\n- Incident response sequencing\n\n## Code Example\n```javascript\n// Pseudo API usage: rotateKey and reencryptData\nasync function rotateAndReencrypt(keyId) {\n  await kms.rotate(keyId);\n  await storage.reencryptAll(keyId);\n}\n```\n\n## Follow-up Questions\n- What are performance implications of re-encryption?\n- How to test rotation safely in prod without downtime?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-4",
        "questionId": "q-9291",
        "question": "You are enabling user-defined functions (UDFs) in a multi-tenant distributed data store similar to MongoDB, with WASM sandboxing for the UDFs. Propose a concrete, deployable security architecture that guarantees tenant data isolation, per-tenant key management with envelope encryption, and verifiable runtime attestation. Include: memory/IO/CPU isolation strategies, secure code signing and update flow, tamper-evident logging and incident response, and discuss trade-offs. Specify technologies and a high‑level deployment plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-tenant architecture for WASM-based UDFs with strict data isolation. Use per-tenant KEKs encrypted by a central KMS (envelope encryption), rotate keys monthly, sign UDF artifacts, requir",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Adopt a strict WASM sandbox: limit syscalls, cap memory, enforce a narrow API surface, and reject any transformer attempting to breach boundaries. Isolate tenants via dedicated streams with strict data routing and netwo…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant namespaces and sandboxed runtimes; sign function packages with HSM-backed tenant keys and verify SBOM and manifest before deploy; require runtime attestation on cold start; issue short-",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use PKCE: generate a high-entropy code_verifier (43–128 chars), compute code_challenge = base64url(SHA-256(code_verifier)). Open the auth page with redirect_uri. After login, exchange the code at the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes ability to design secure, scalable tenant isolation for dynamic code execution with cryptographic guarantees and auditable traceability.\n\n## Key Concepts\n\n- WASM sandboxing and resource quotas\n- Per-tenant envelope encryption with KEKs\n- Remote attestation (SGX/TPM) for runtime integrity\n- Tamper-evident logging and signing\n- Secure update and rollback processes\n- Incident response playbooks\n\n## Code Example\n\n```javascript\n// Pseudo attestation check\nasync function verifyTenantAttestation(tenantId, attestation) {\n  const valid = await attestationService.verify(attestation, tenantId);\n  if (!valid) throw new Error(\"Invalid attestation\");\n  return true;\n}\n```\n\n## Follow-up Questions\n\n- How would you test the attestation flow under tenant churn and key rotation?\n- What failure modes would trigger automatic rollback and how would you verify integrity?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-5",
        "questionId": "q-13754",
        "question": "Scenario: A web app uses session cookies for authentication and serves an API at https://api.example.com. A security test shows that the CORS configuration reflects the request Origin header and allows credentials globally. Provide a beginner-friendly, concrete fix plan to prevent credential leakage, including server-side CORS changes, cookie attributes, and a basic test you would run in CI. Also outline deployment steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Replay CI logs to identify affected artifacts, generate SBOMs for rebuilt images, and isolate registries. Revoke the compromised signing key, roll back production to a known-good base image, and patch",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Require signed plugin attestations (Sigstore/cosign) plus SBOMs; verify at load against a trusted root; sandbox plugins with WASM engine (Wasmtime) with per-plugin quotas and sandboxed syscalls; isola",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Restrict CORS to a single origin and avoid reflecting Origin. Change Access-Control-Allow-Origin from * to https://app.example.com, keep credentials true, and ensure cookies are HttpOnly, Secure, and",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant HMAC keys stored in an HSM with envelope encryption; rotate keys every 24 hours with versioning; require a nonce and timestamp in webhook payload and header; implement strict replay protect",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical handling of CORS misconfig, credentials exposure, and real-world mitigations in a beginner-friendly way.\n\n## Key Concepts\n- CORS allow-list and avoiding wildcard with credentials\n- Cookie attributes: HttpOnly, Secure, SameSite\n- CSRF basics in state-changing requests\n- CI checks for security headers and cookie flags\n\n## Code Example\n```javascript\n// Server-side CORS config (Express)\nconst allowedOrigin = 'https://app.example.com';\napp.use(require('cors')({ origin: allowedOrigin, credentials: true }));\n```\n```javascript\n// Secure cookie header (example)\nresponse.setHeader('Set-Cookie', 'session=abc123; HttpOnly; Secure; SameSite=Strict');\n```\n\n## Follow-up Questions\n- How would you adapt this for multiple allowed origins in a multi-tenant app?\n- What CI tests would guard against regressions in CORS and cookies?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-6",
        "questionId": "q-15785",
        "question": "You have a containerized Python web API deployed on Kubernetes behind an Ingress. Implement a beginner-friendly CI gate that blocks deployment if secrets leak into logs, if dependencies have CVEs, or if the base image is outdated. Outline concrete steps with sample GitHub Actions steps, SBOM, image scanning, and failure criteria for high-severity CVEs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-device authentication by issuing unique device credentials consisting of a short-lived access token and device-specific signing key. Clients authenticate API requests using HMAC-SHA256 signatures over the…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up a GitHub Actions gate that builds with a pinned base image, uses multi-stage builds, generates a CycloneDX SBOM, runs Trivy image scans, and fails on CVEs of HIGH or CRITICAL. Scan the repo for",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a plan to enforce tenant boundaries for UDFs in a multi-tenant analytics platform: isolate each tenant's UDFs in unprivileged WASM sandboxes, enforce strict API surfaces, and pin policies with",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use short-lived access tokens (5 minutes) with refresh tokens, rotate signing keys daily and on incident, and add a revocation check at the gateway. Disable refresh for compromised sessions. Add a CI",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical CI security gating for container deployments, focusing on SBOMs, image scans, and secrets handling rather than theory.\n\n## Key Concepts\n\n- CI/CD security gates\n- SBOM (CycloneDX)\n- Image scanning (Trivy)\n- Secrets in logs/code detection\n- Base image pinning and multi-stage builds\n- Deployment gating criteria\n\n## Code Example\n\n```yaml\nname: Security Gate\non: [push]\njobs:\n  security_gate:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Build and tag\n        run: |\n          docker build -t myapi:latest .\n      - name: Generate SBOM\n        run: |\n          cyclonedx-bom -o bom.xml || true\n      - name: Trivy image scan\n        run: |\n          trivy image myapi:latest --exit-code 7 --no-progress\n      - name: Secrets check\n        run: |\n          grep -R \"SECRET\" . || exit 1\n      - name: Push if green\n        if: ${{ always() }}\n        run: |\n          # push only on success\n          echo \"Deploying...\"\n```\n\n## Follow-up Questions\n\n- How would you adapt this gate for a polyglot tech stack (JS, Python, Go) and different registries?\n- What metrics would you track to measure gate effectiveness over time?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-7",
        "questionId": "q-15947",
        "question": "In a Node.js Express API, POST /login accepts username and password. A security test shows brute-force risk due to high request rate. Propose a beginner-friendly, end-to-end plan to mitigate: implement rate-limiting by IP and by username, temporary lockouts after repeated failures, optional CAPTCHA, and safe logging. Include minimal code snippets and testing steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable PostgreSQL Row-Level Security on tenant tables and set per-request tenant context in your API. **1. Database Setup** ```sql ALTER TABLE orders ENABLE ROW LEVEL SECURITY; ALTER TABLE orders FORCE ROW LEVEL SECURIT…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement dual-layer rate limiting: 1) per-IP window of 15 minutes with a cap (e.g., 100 requests), 2) per-username limiter with a 10-minute window. After 5 failed logins, enforce a 15-minute lockout",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement per-device token binding and short-lived tokens. Bind tokens to a device attestation (TPM/HSM) and a per-request nonce; require mTLS; verify audience/issuer; store jti/nonce in a fast cache",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use short-lived OAuth2 tokens issued by an authorization server (OIDC). Bind tokens to tenant, scope, and a limited IP range; issue JWTs with aud, iss, iat, exp, jti; protect via JWKS; enable token in",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nBrute-force defense at the API layer is a common beginner task with real impact. It tests practical knowledge of rate limiting, lockouts, and safe logging without overengineering.\n\n## Key Concepts\n- Dual-layer rate limiting (IP and username)\n- Temporary lockouts after repeated failures\n- CAPTCHA as a human-verify fallback\n- Safe logging with redaction\n- Basic testing with automated brute-force attempts\n\n## Code Example\n```javascript\nconst rateLimit = require('express-rate-limit');\nconst ipLimiter = rateLimit({ windowMs: 15*60*1000, max: 100 });\n// Per-username limiter would require a store; here's a conceptual sketch\n// app.use('/login', (req, res, next) => { /* increment per-user counter and enforce */ });\n```\n\n## Follow-up Questions\n- How would you migrate from in-memory to Redis for distributed rate limiting?\n- How would you handle accessibility and legitimate users who are temporarily locked out?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-8",
        "questionId": "q-19667",
        "question": "You operate a global edge compute platform (Cloudflare-like) serving multi-tenant web traffic and fleet telemetry (Tesla-like). A timing-channel vulnerability arises from a shared edge cache that could leak tenant data. Propose a concrete end-to-end plan to prevent cross-tenant leakage, covering: cache isolation, per-tenant crypto vaults, hardware-backed TEEs, envelope encryption with per-tenant keys rotated frequently, access controls, tamper-evident logging, and an automated incident-response workflow. Include deployment steps and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant WASM sandboxes (Wasmtime) with bounded memory and CPU quotas, and run each in isolated containers with seccomp and AppArmor. Use WASI for IO isolation; disallow direct filesystem",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a secrets-first workflow: store API keys in Databricks Secret Scopes and fetch with dbutils.secrets.get; remove keys from notebooks and repos. Enforce least privilege on who can access scopes; use environment vari…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A WAF inspects HTTP/S requests, applies security rules, and blocks malicious traffic before it reaches the web application.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a multi-layered defense strategy: (1) Partition edge caches by tenant using dedicated namespaces with strict isolation boundaries; (2) Guard encryption keys in hardware-backed TEEs with envelope encryption, ma…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical knowledge of cross-tenant isolation at the edge, hardware-assisted security protections, and defense-in-depth strategies for multi-tenant infrastructure. It evaluates understanding of how to prevent data leakage in shared environments while maintaining performance and operational efficiency.\n\n## Key Concepts\n- Edge cache isolation by tenant and namespace\n- Trusted Execution Environments (TEEs) for key protection\n- Envelope encryption with per-tenant key management\n- Hardware Security Modules (HSMs) for key rotation\n- Mutual TLS (mTLS) and token binding for authentication\n- Tamper-evident logging with cryptographic verification\n- Automated incident response workflows\n\n## Code Example\n```javascript\n// Derive per-tenant cache key within TEE-protected context\nasync function deriveCacheKey(tenantId, resourceId, kmsEndpoint) {\n  const tenantKey = await kmsEndpoint.fetchTenantKey(tenantId);\n  const cacheKey = crypto\n    .createHmac('sha256', tenantKey)\n    .update(`cache:${tenantId}:${resourceId}`)\n    .digest('hex');\n  return cacheKey;\n}\n```\n\n## Follow-up Questions\n- How would you measure the success of this implementation, and what are the key trade-offs between security, performance, and operational complexity?\n- How would you handle key derivation for cross-tenant metadata operations while maintaining isolation guarantees?\n\n## Deployment Steps\n1. Provision per-tenant KMS namespaces and configure key rotation policies\n2. Deploy TEE-enabled cache nodes with measured boot attestation\n3. Configure API gateway with mTLS and token binding policies\n4. Implement tamper-evident logging infrastructure with immutable storage\n5. Define automated response playbooks for breach scenarios\n6. Conduct chaos engineering exercises to validate isolation boundaries",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-9",
        "questionId": "q-7270",
        "question": "Design an end-to-end secure software supply chain for a multi-tenant token-issuing API used by consumer apps across Microsoft, Tesla, Stripe. Focus on reproducible builds, SBOM, per-tenant keys in an HSM, code signing, artifact verification at deploy, and an incident-response playbook for suspected compromise. Include concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a layered defense: enforce container image provenance with Sigstore/Cosign, require SBOM verification in CI and at runtime, deploy via immutable registries with admission controls (OPA) and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Cosign for SBOM signing/verification and Sigstore signing; enforce reproducible builds (BuildKit, pinned deps) and per-tenant keys in an HSM-backed KMS (CloudHSM/Azure Dedicated HSM) with envelope",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement comprehensive runtime attestation for all edge functions using TPM-based measurements; enforce strict logging controls that disable authentication token logging and implement automatic redaction; deploy envelo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Move tokens to OS secure storage (Keychain/Keystore), never persist in insecure storage. Use short-lived access tokens (15–60 min) with device-bound refresh tokens; protect with biometric unlock. Impl",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a concrete, auditable supply chain for multi-tenant, high-stakes APIs with strong cryptographic controls and rapid incident response.\n\n## Key Concepts\n- SBOM generation and signing with Cosign/Sigstore\n- Reproducible builds (BuildKit, pinned dependencies)\n- Per-tenant HSM-backed keys with envelope encryption\n- Code signing and artifact verification at deploy\n- Runtime attestation (TPM2.0/OpenEnclave, Nitro Enclaves)\n- mTLS/SPIFFE-based identity, incident response workflow\n\n## Code Example\n```bash\n# Sign and verify an artifact SBOM using Sigstore/Cosign\ncosign sign --key cosign.key artifact.tar.gz\ncosign verify --key cosign.pub artifact.tar.gz\n```\n\n## Follow-up Questions\n- How would you handle key rotation without breaking in-flight tokens?\n- What metrics alert on SBOM/signing failures would you monitor?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-10",
        "questionId": "q-15114",
        "question": "You operate a multi-tenant edge compute platform that runs user-supplied scripts in a sandbox on customer devices. Propose a concrete plan to prevent cross-tenant data leakage via side channels and memory dedup, outlining: threat model, isolation boundary (microVMs vs containers vs HW enclaves), memory partitioning, cache/page-table isolation, IPC controls, and verification steps including CI tests and incident response?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sign each eBPF program with a tenant-scoped KMS key and verify with the kernel verifier before loading. Isolate tenants using separate cgroups/namespaces and per-tenant maps; prune helper functions to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enforce a 5 MB cap; validate MIME and extension; store under per-user UUID path in object storage; trigger malware scan before finalizing; publish via time-limited signed URLs; validate content on dow",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement dual-layer rate limiting: 1) per-IP window of 15 minutes with a cap (e.g., 100 requests), 2) per-username limiter with a 10-minute window. After 5 failed logins, enforce a 15-minute lockout",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: model cross-tenant leakage via cache/memory. Isolate each tenant in a microVM (Firecracker) or HW enclave (TDX/SNP). Disable memory dedup; enable KPTI; enforce per-tenant address spaces; strict",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssess practical hardening of multi-tenant edge workloads against side-channel leaks with realistic constraints and hardware options.\n\n## Key Concepts\n- Threat modeling for edge tenants\n- HW-assisted isolation (TDX/SNP, SEV) vs microVMs\n- Memory dedup, KPTI, CAT, per-tenant keys\n- Sandboxing (seccomp), IPC, encryption, incident response\n\n## Code Example\n```javascript\n// Pseudo test stub for CI to catch cross-tenant leaks\nfunction testCrossTenantLeak() {\n  // simulate attacker tenant and victim tenant timing\n}\n```\n\n## Follow-up Questions\n- Tradeoffs of Firecracker vs TDX in latency\n- How to validate side-channel resilience continuously",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-11",
        "questionId": "q-15593",
        "question": "How would you migrate a global API gateway from ECDSA to lattice-based post-quantum signatures for per-tenant request authentication, ensuring backward compatibility, safe key rotation, revocation, performance, and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Edge attestation (SPIRE) before enrollment; inference in TEEs (SGX/SEV) with redaction prior to send; mTLS for transit and envelope encryption at rest (KMS); ABAC per-tenant in the data lake; tamper-e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Define a field-level authorization map (field → required permission). Extend resolvers with a wrapper that checks user roles and tenantId in the Graph context before resolving each field; prune or err",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a hybrid migration: keep ECDSA as the primary signer while introducing lattice-based PQC signing in parallel. Issue tokens with dual signatures (ECDSA+PQC) for a 6–12 month window. Validators acce",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "## Secure OTA Update Supply Chain for Autonomous Vehicles ### 1. Build Phase (Reproducibility & Provenance) - **Build System**: Use Bazel or Nix with hermetic build environments (all dependencies pinned via content-addr…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nQuantum-era threats demand practical migration paths. This question tests how to maintain compatibility, performance, and security during a live crypto upgrade.\n\n## Key Concepts\n- Hybrid crypto migration and dual signatures\n- Key management, rotation, and revocation\n- Backward compatibility with existing clients\n- Observability, validation, and rollback strategies\n\n## Code Example\n```python\n# Pseudo-code: dual-signature signer during migration\n\ndef sign_token(payload, ecsda_key, pqc_key):\n    sig_ecdsa = sign_ecdsa(payload, ecsda_key)\n    sig_pqc = sign_pqc(payload, pqc_key)\n    return {\"payload\": payload, \"sig_ecdsa\": sig_ecdsa, \"sig_pqc\": sig_pqc}\n```\n\n## Follow-up Questions\n- How would you measure post-quantum verification latency on edge gateways?\n- What tests would you run to validate rollback safety and no-rollback data integrity?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-12",
        "questionId": "gh-69",
        "question": "How does Zero Trust Security implement identity-based access control with micro-segmentation using modern cloud infrastructure and identity providers?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a concrete plugin deployment pipeline for a model-serving platform: CI builds sign artifacts (SBOM + Sigstore); runtime attestation on load; sandboxed isolation (WASM or gVisor); per-plugin IA",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Zero Trust implements identity-based access control through continuous authentication via IdP integration (Okta, Azure AD), granular RBAC policies enforced at API gateways and service meshes, micro-segmentation using Ku…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design an end-to-end defense plan for a multi-tenant ML inference platform used by a global ride-hailing network. Include training-data supply-chain controls, model signing/versioning, per-tenant isol",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Verify authenticity by computing an HMAC-SHA256 over the raw request body with the shared secret and compare it to the signature header using a constant-time comparison. Use a timestamp to reject old",
            "isCorrect": false
          }
        ],
        "explanation": "## Identity Foundation\nZero Trust starts with strong identity proofing using IdPs like Okta, Azure AD, or Auth0. Every request requires valid JWT tokens with fine-grained scopes and claims. Multi-factor authentication is mandatory for privileged access.\n\n## Policy Enforcement\nAuthorization happens at multiple layers:\n- **API Gateway**: Kong, Ambassador, or AWS API Gateway enforce rate limiting and RBAC\n- **Service Mesh**: Istio/Linkerd implement mTLS and fine-grained policies\n- **Network Layer**: Kubernetes NetworkPolicies, AWS Security Groups, NSX\n\n## Micro-segmentation Strategy\nNetwork zones are defined by workload identity, not IP ranges:\n```yaml\napiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: payment-svc-policy\nspec:\n  podSelector:\n    matchLabels:\n      app: payment-service\n  policyTypes:\n  - Ingress\n  ingress:\n  - from:\n    - podSelector:\n        matchLabels:\n          role: frontend\n    ports:\n    - protocol: TCP\n      port: 8443\n```\n\n## Continuous Monitoring\nReal-time policy enforcement with tools like Open Policy Agent (OPA) combined with logging/monitoring (Splunk, Datadog) detects anomalous access patterns and automatically revokes compromised credentials.",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-13",
        "questionId": "q-13536",
        "question": "You run a real-time chat service (WebSocket-based) used by a fintech audience. A security test finds missing Origin checks on the WebSocket upgrade, risking cross-origin presence data leakage. Provide a beginner-friendly, concrete patch plan: Origin whitelist enforcement during handshake, require authenticated WS upgrade with a token, per-connection scopes, and a simple tests plan (Origin, token presence, and no leakage). Include a minimal code sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Replace the compromised global API key with per-device access tokens using short lifetimes (5-15 minutes) and secure refresh tokens; implement PKCE for mobile/SPA authentication flows; bind tokens to device fingerprints…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt per-tenant RBAC/ABAC, enforce ROW-LEVEL SECURITY and dynamic masking in the warehouse, and expose only authorized fields. Encrypt at rest with CMEK via KMS/HSM; enforce mTLS between data service",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Secure your WebSocket connections with these four steps: 1. **Origin Whitelist Enforcement**: Only allow WebSocket connections from trusted domains (e.g., your app). Reject any request with a missing or unlisted Origin…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Switch to per-user salted hashes using Argon2id (memory-cost) or bcrypt with cost 12. Migrate in batches: add a marker column, rehash on login for older hashes, and resync background worker until comp",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Matters\nWebSocket connections accept requests from any origin by default. Without origin checks, attackers can steal real-time data by tricking users into visiting malicious sites that initiate WebSocket connections to your service.\n\n## The Patch Plan\n\n### Step 1: Origin Whitelist Enforcement\nCheck the `Origin` header during the WebSocket upgrade handshake. Only allow domains you explicitly trust (e.g., `https://app.example.com`):\n\n```javascript\nconst ALLOWED_ORIGINS = ['https://app.example.com'];\n\nfunction validateOrigin(origin) {\n  return ALLOWED_ORIGINS.includes(origin);\n}\n```\n\n### Step 2: Token Authentication Requirement\nRequire a valid auth token in the upgrade request. Validate the token before proceeding with the WebSocket upgrade. Reject the connection if the token is missing, invalid, or expired.\n\n### Step 3: Bind Token to Connection\nAfter validation, attach the token's user data (userId, permissions, scopes) to the WebSocket connection object. This allows tracking who is connected throughout the session.\n\n### Step 4: Per-Connection Scopes\nWhen sending messages, check the bound user data and only send data that the connected user is authorized to access. Implement channel-level or resource-level access control.\n\n## Minimal Code Sketch\n\n```javascript\nconst WebSocket = require('ws');\nconst ALLOWED_ORIGINS = ['https://app.example.com'];\n\nconst wss = new WebSocket.Server({ noServer: true });\n\nwss.on('connection', (ws, req) => {\n  const user = req.user; // Set by auth middleware\n  ws.userId = user.id;\n  ws.scopes = user.scopes;\n  \n  // Only send data this user is authorized to see\n  ws.send = (data) => {\n    if (authorize(user, data)) {\n      ws.send(JSON.stringify(data));\n    }\n  };\n});\n\nwss.on('upgrade', (request, socket, head) => {\n  const origin = request.headers.origin;\n  \n  // Step 1: Origin check\n  if (!ALLOWED_ORIGINS.includes(origin)) {\n    socket.write('HTTP/1.1 403 Forbidden\\r\\n\\r\\n');\n    socket.destroy();\n    return;\n  }\n  \n  // Step 2: Token validation\n  const token = request.headers['sec-websocket-protocol'] || getTokenFromUrl(request.url);\n  const user = validateToken(token);\n  if (!user) {\n    socket.write('HTTP/1.1 401 Unauthorized\\r\\n\\r\\n');\n    socket.destroy();\n    return;\n  }\n  \n  // Step 3: Proceed with upgrade\n  request.user = user;\n  wss.handleUpgrade(request, socket, head, (ws) => {\n    wss.emit('connection', ws, request);\n  });\n});\n```\n\n## Test Plan\n\n| Test Case | Expected Result |\n|-----------|------------------|\n| Request from allowed origin + valid token | Connection accepted |\n| Request from disallowed origin | 403 Forbidden |\n| Request with no token | 401 Unauthorized |\n| Request with expired/invalid token | 401 Unauthorized |\n| User A connects, User B sends message | User A should not receive User B's private data |",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-14",
        "questionId": "q-9401",
        "question": "You operate a serverless edge platform that runs untrusted WebAssembly modules for tenants. Design a security model to isolate tenants, protect secrets, and prevent code leakage. Include attestation-based code loading, envelope encryption for per-tenant keys, a verifiable update mechanism with SBOMs, and a robust incident response. Be concrete about technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a confidential computing architecture with layered isolation: run tenant WebAssembly modules within Trusted Execution Environments (TEEs) such as AMD SEV or Intel TDX, complemented by strong WebAssembly sandbo…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Serverless image-processing pipeline: enforce per-user encryption using envelope encryption with a KMS CMK. API Gateway authenticates; Lambda calls GenerateDataKey for a per-user data key (DEK), uses",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use PKCE: generate a high-entropy code_verifier (43–128 chars), compute code_challenge = base64url(SHA-256(code_verifier)). Open the auth page with redirect_uri. After login, exchange the code at the",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-IP and per-token rate-limiting using a Redis-backed token-bucket with a Lua script. Apply different limits per route (login stricter) and a small burst tolerance (e.g., 20). Return 429 w",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates expertise in multi-tenant edge security, specifically assessing knowledge of confidential computing, supply chain risk management, and incident response capabilities. It tests the ability to select appropriate TEE technologies, design attestation flows, and implement scalable key management strategies.\n\n## Key Concepts\n- Confidential computing (TEEs, WebAssembly sandboxing)\n- Envelope encryption and Key Management Service (KMS)\n- Remote attestation and code provenance verification\n- Software Bill of Materials (SBOMs), signed updates, and rollback mechanisms\n- Incident response workflows and tamper-evident logging\n\n## Code Example\n\n```javascript\n// Implementation code here\n```\n\n## Follow-up Questions\n- How would you handle supply-chain attacks where an update payload is signed but malicious?\n- What monitoring and alerting strategies would you implement for TEE compromise detection?\n- How do you balance performance overhead with security guarantees in a multi-tenant environment?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-15",
        "questionId": "q-18140",
        "question": "You're deploying a shared AI model API for multi-tenant teams where users can supply custom plugins. Propose a concrete, end-to-end security plan covering per-tenant sandboxing (WASM/containers) with limited I/O, plugin signing and revocation, runtime attestation, mTLS and HSM-backed keys, SBOM enforcement, tamper-evident logs, and an incident-response playbook including rollback. How would you implement and validate it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a secret manager to store the API key securely and inject it into the function as an ephemeral environment variable, avoiding any hard-coding. Apply a least-privilege IAM role that restricts access to only the neces…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant WASM sandboxes with strict API surface and memory quotas; require plugin signing with Ed25519 and short-lived JWT tokens; run plugins in a confidential runtime with attestation (T",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a plan to enforce tenant boundaries for UDFs in a multi-tenant analytics platform: isolate each tenant's UDFs in unprivileged WASM sandboxes, enforce strict API surfaces, and pin policies with",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Store the session token in a HttpOnly, Secure cookie with SameSite=Strict; never localStorage. Use SameSite to prevent CSRF; add CSP and strict input encoding. Test with XSS payloads, cookie leakage a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses end-to-end security for multi-tenant AI plugin execution with concrete controls and verification.\n\n## Key Concepts\n\n- Tenant isolation through sandboxing and resource quotas\n- Code signing, revocation, and supply-chain SBOM checks\n- Attestation for confidential execution environments\n- Strong key management (HSM) and restricted I/O surfaces\n- Tamper-evident logging and incident response with rollback\n\n## Code Example\n\n```javascript\n// Pseudo-implementation sketch\n```\n\n## Follow-up Questions\n\n- How would you validate the effectiveness of plugin revocation during a zero-day?\n- What metrics would you monitor to detect sandboxed-runtime escape attempts?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-16",
        "questionId": "q-359",
        "question": "You discover a reflected XSS vulnerability in a search feature. The search term is displayed back to the user without sanitization. How would you fix this, and what's the difference between reflected XSS and stored XSS in terms of impact and remediation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sanitize output using HTML encoding and implement CSP. Reflected XSS affects individual users, stored XSS can impact all users visiting the compromised page.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Validate tenant binding at every hop: require the gateway to bind tenant_id from JWT to the request path or body and reject if mismatch. Enforce claims validation, audience, and issuer; drop tokens th",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt per-tenant data encryption keys (DEKs) wrapped by a centralized KMS master key, using envelope encryption with AES-256-GCM. Rotate envelope keys quarterly and replicate across regions. When a tenant key is comprom…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt Kubernetes namespaces per tenant, Istio service mesh with mTLS, and SPIRE for workload attestation. Encrypt data with envelope encryption using per-tenant CMKs in AWS KMS and store in tenant-sco",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical security knowledge, understanding of XSS types, and real-world remediation strategies - critical for Fortinet's security products.\n\n## Expected Answer\nStrong candidates will explain: 1) Input validation vs output encoding, 2) CSP implementation, 3) The key difference in attack scope between reflected (single user) vs stored (all users), 4) Server-side vs client-side remediation approaches.\n\n## Code Example\n```typescript\n// Fix: HTML encode output\nfunction sanitizeSearchTerm(term: string): string {\n  return term.replace(/[&<>\"]/g, (match) => {\n    const escape: Record<string, string> = {\n      '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;'\n    }\n    return escape[match]\n  })\n}\n\n// CSP header\napp.use((req, res, next) => {\n  res.setHeader('Content-Security-Policy', \"default-src 'self'; script-src 'self'\")\n  next()\n})\n```\n\n## Follow-up Questions\n- How would you test this fix for bypasses?\n- What if the search term appears in JavaScript context?\n- How does CSP help prevent XSS even with imperfect sanitization?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-17",
        "questionId": "q-27617",
        "question": "A Node.js REST API uses a server-side session with a cookie-based authentication token. A security test uncovers potential session fixation: a new login could leave an old session valid, enabling reuse. Propose a beginner-friendly, concrete fix plan: rotate session IDs on login, invalidate previous sessions, set HttpOnly, Secure, and SameSite=Strict cookies, store session IDs in Redis with per-user keys and a short TTL, and add a basic test to verify old sessions are rejected?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Standardize errors to generic messages (401/403/400) without exposing secrets. Redact token fields from error payloads and logs. Implement a central Express error handler that returns status and a saf",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "On login, generate a fresh session ID, invalidate prior IDs for that user, and ensure only the new session is accepted by the API. Use HttpOnly, Secure, and SameSite=Strict on the session cookie. Stor",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement envelope encryption with per-tenant DEKs wrapped by regional KMS/HSM master keys. Enforce strict access control using short-lived credentials, tenant- and region-scoped IAM policies, and network segmentation t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce token binding and anti-replay at the gateway. Use short-lived JWTs with unique jti values, track seen jti in Redis per region, validate JWT via JWKS with issuer and audience checks, bind tokens to mTLS or per-cl…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nRationale: session fixation is a common beginner pitfall when using server-side sessions. The plan demonstrates practical, verifiable steps and testing.\n\n## Key Concepts\n\n- Session fixation and rotation\n- HttpOnly, Secure, SameSite cookie flags\n- Server-side session store (Redis)\n- Per-user session invalidation\n- Basic automated tests for session validity\n\n## Code Example\n\n```javascript\n// Express-like sketch\napp.post('/login', async (req,res)=>{\n  const user = await authenticate(req.body);\n  if(!user) return res.status(401).end();\n  const sessionId = generateSessionId();\n  await redis.set(`sess:${user.id}`, sessionId, 'EX', 3600);\n  res.cookie('session', sessionId, { httpOnly: true, secure: true, sameSite: 'Strict' });\n  res.json({ ok: true });\n});\n\napp.use((req,res,next)=>{\n  const sid = req.cookies?.session;\n  if(!sid) return res.status(401).end();\n  // validate sid against Redis\n  // deny if not found or mismatched\n  next();\n});\n```\n\n## Follow-up Questions\n\n- How would you test this in a distributed setup with multiple app instances?\n- How would you adapt for mobile clients that require cookie-less auth flows?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-18",
        "questionId": "q-25550",
        "question": "You're operating an enterprise AI assistant service that processes highly sensitive data from multiple tenants and runs customer-supplied prompts on NVIDIA GPUs in shared hardware. Propose a concrete, end-to-end plan to defend against model inversion and membership inference attacks, while preserving performance. Include data handling, private inference options (enclaves/secure computation), per-tenant isolation, access controls, and an incident response playbook with logs and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "API security integrates authentication, authorization, encryption, and monitoring to protect endpoints from threats while enabling secure access for legitimate users.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an end-to-end remediation plan spanning logging hygiene, secrets management, memory safety, access controls, and incident response. Adopt per-tenant RBAC with short Vault lease durations (15 minutes). Ensure a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a layered approach: private inference in attested enclaves (Intel SGX/AMD SEV) or isolated GPUs; per-tenant namespaces and data minimization; in-memory encryption with KEKs tied to attestation; di",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use envelope encryption with per-tenant CMKs backed by an external KMS/HSM. Isolate each tenant in separate vaults, enforce least-privilege access, and rewrap data during key rotation with zero-downti",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates ability to design privacy-preserving inference across tenants with realistic hardware constraints, requiring practical mitigations beyond theory.\n\n## Key Concepts\n- Model inversion risk in multi-tenant inference\n- Membership inference attacks\n- Private inference options (SGX/SEV, confidential GPUs)\n- Per-tenant isolation and data minimization\n- Output perturbation and differential privacy\n- Tamper-evident logging and incident response\n\n## Code Example\n```javascript\nfunction perturbOutput(out, epsilon) {\n  // Laplace noise placeholder for DP\n  const noise = 0; // implement Laplace sample here\n  return out + noise;\n}\n```\n\n## Follow-up Questions\n- How would you validate defenses in a red-team exercise?\n- What DP-accuracy trade-offs would you consider in production workloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-19",
        "questionId": "q-24470",
        "question": "In a SaaS app using PostgreSQL to store tenant data, developers currently filter by tenant_id in queries. A security review flags potential cross-tenant leakage if a filter is forgotten. Propose a beginner-friendly, practical plan to enforce per-tenant isolation using PostgreSQL Row-Level Security (RLS). Include concrete steps and a minimal code snippet showing policy and session setup?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "PFS uses Ephemeral Diffie-Hellman (ECDHE) where each session generates unique key pairs, ensuring past sessions remain secure even if long-term private keys are compromised. In ECDHE, client and server exchange ephemera…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enable PostgreSQL Row-Level Security (RLS) on the data table and enforce per-session tenant isolation. Use a policy like USING (tenant_id = current_setting('myapp.tenant_id')::int). Set the session te",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Sign each eBPF program with a tenant-scoped KMS key and verify with the kernel verifier before loading. Isolate tenants using separate cgroups/namespaces and per-tenant maps; prune helper functions to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement policy-signing with a trusted root, require 2-person approvals and a separate policy-signing step, store policies in an immutable ledger (e.g., etcd with revision history or a git-like store",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical knowledge of enforcing data isolation in a real DB setting, using native features rather than just relying on application code.\n\n## Key Concepts\n\n- PostgreSQL Row-Level Security (RLS)\n- current_setting and session context\n- Per-request tenant isolation\n- Parameterized queries\n- Testing RLS policies in CI\n\n## Code Example\n\n```javascript\n// SQL policy as a string\n`ALTER TABLE items ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_isolate ON items USING (tenant_id = current_setting('myapp.tenant_id')::int);` \n```\n\n```javascript\n// App side (pseudo)\ndb.query(\"SET myapp.tenant_id = ?\", [tenantId]);\ndb.query(\"SELECT * FROM items WHERE tenant_id = ? AND id = ?\", [tenantId, itemId]);\n```\n\n## Follow-up Questions\n\n- How would you test RLS policies in CI?\n- What are potential pitfalls with current_setting usage?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-20",
        "questionId": "q-13774",
        "question": "In a Databricks lakehouse powering a marketplace like Airbnb, design a concrete, end-to-end plan to prevent DLP/exfiltration of PII and secrets through notebooks, jobs, and shared data. Include per-tenant access with Unity Catalog and Row-Level Security, dynamic masking, secret scopes with restricted principals, strict workspace egress controls, and a CI/CD validation suite plus production monitoring. Explain components and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement tenant-scoped Unity Catalog with per-tenant schemas; enforce Row-Level Security and dynamic masking on all PII fields; store secrets in Databricks Secret Scopes with least-privilege access a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement tenant-scoped data partitions with per-tenant keys, enforce mTLS/SPIFFE identities, and isolate rules sandbox (WASM) with a minimal API surface. Apply envelope encryption for logs, and tampe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Server-side: ignore the client-supplied Content-Type; detect mime by inspecting file signatures (magic bytes) and map to a safe, canonical mime. Whitelist allowed types and reject others. Serve with C",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement HTTP-only, secure cookies with SameSite=Strict attribute, server-side session storage using Redis, session ID rotation on authentication, short TTL values, CSRF tokens, and origin header validation.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess hands-on design for tenant isolation, data leakage prevention, and operational controls in a Databricks production environment.\n\n## Key Concepts\n\n- Unity Catalog tenant isolation\n- Row-Level Security and masking\n- Secret scopes and rotation\n- Network egress controls (VPC/PrivateLink)\n- CI/CD notebook validation for data leakage\n- Audit logs and incident response\n\n## Code Example\n\n```javascript\n// Example policy pseudo-snippet\nconst policy = { tenant_id: \"TENANT_ID\", maskPII: true };\n```\n\n## Follow-up Questions\n\n- How would you test RLS performance at scale?\n- What are trade-offs of masking vs. redaction in dashboards?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-21",
        "questionId": "q-4501",
        "question": "Describe a beginner-friendly approach to implementing tamper-evident security event logging for authentication across a microservices-based travel platform. Specify what data to log, how to prevent tampering (append-only storage, cryptographic signing), how to validate logs during an incident, and how to handle privacy/PII?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Server-side: ignore the client-supplied Content-Type; detect mime by inspecting file signatures (magic bytes) and map to a safe, canonical mime. Whitelist allowed types and reject others. Serve with C",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a PKCE-enabled OAuth 2.0 flow with per-tenant issuer and audience, bind access tokens to tenant_id and client_id, and enforce short-lived access tokens (e.g., 15 minutes) with rotating refresh t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enforce a 5 MB cap; validate MIME and extension; store under per-user UUID path in object storage; trigger malware scan before finalizing; publish via time-limited signed URLs; validate content on dow",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement append-only logs per service with the following data fields: user_id (hashed), timestamp, event_type, IP address, device information, and authentication outcome, while ensuring all PII is properly redacted. St…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design practical, auditable security logging systems that remain functional under abuse or compromise while demonstrating understanding of integrity constraints and privacy requirements within beginner-friendly limitations.\n\n## Key Concepts\n- Tamper-evident logging mechanisms\n- Append-only storage architectures\n- Cryptographic signing (HMAC, digital signatures)\n- PII redaction and privacy protection\n- Incident validation procedures\n\n## Code Example\n```javascript\nconst crypto = require('crypto');\nconst logLine = JSON.stringify(logEntry);\nconst signature = crypto.createHmac('sha256', SECRET_KEY).update(logLine).digest('hex');\n```\n\n## Follow-up Questions\n- How would you handle key rotation for long-term log integrity?\n- What monitoring would you implement to detect log tampering attempts in real-time?\n- How would you scale this approach across hundreds of microservices?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-22",
        "questionId": "q-8347",
        "question": "You deploy a multi-tenant API at the edge (e.g., with workers) and tenants report uneven rate limiting: some customers hit limits while others are idle. Propose a beginner-safe plan to implement tenant-scoped quotas using per-tenant tokens, validate x-tenant headers, and minimize latency, including observability and safeguard against header spoofing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use PKCE: generate a high-entropy code_verifier (43–128 chars), compute code_challenge = base64url(SHA-256(code_verifier)). Open the auth page with redirect_uri. After login, exchange the code at the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage per-tenant isolation via data partitioning and namespace scoping; use tenant-scoped envelope encryption with KMS keys; run inference inside Nitro/Azure Confidential Compute enclaves with atte",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-tenant token bucket at the edge. Validate x-tenant against allowed tenants; keep quotas in a fast store (KV/cache) and refill tokens per minute; enforce burst with bucket size; reject reques",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use hardware-backed attestation (TPM/SEV) for each worker, require notebooks signed with per-tenant keys, verify notebook hash against a signed SBOM, and only start execution after a validated attesta",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nInterview context explanation.\n\n## Key Concepts\n- Per-tenant quotas\n- Edge rate limiting\n- Header validation and spoofing\n- Observability and latency trade-offs\n\n## Code Example\n```javascript\n// Example edge rate limiter snippet\n```\n\n## Follow-up Questions\n- How would you test this in staging?\n- How would you scale quotas with hundreds of tenants?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-23",
        "questionId": "q-26068",
        "question": "In a multi-tenant platform, tenants submit webhooks that trigger workflows. A compromised tenant webhook could exfiltrate data or break isolation. Design an end-to-end webhook runtime: per-tenant HMAC signing with keys in an HSM, request verification, replay protection, per-tenant API surface minimization, tamper-evident logs, and an incident response including rapid key rotation and tenant revoke?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a token-binding approach in a multi-cluster mesh: require mTLS with SPIFFE IDs for all service calls, issue short-lived JWTs (5–10 minutes) bound to a per-call ephemeral key, rotate JWKS eve",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant keys stored in an HSM to sign outbound payloads and verify them at ingress, enforce nonce+timestamp replay protection, and minimize endpoints per tenant. Implement tamper-evident loggin",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement tenant-scoped Unity Catalog with per-tenant schemas; enforce Row-Level Security and dynamic masking on all PII fields; store secrets in Databricks Secret Scopes with least-privilege access a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "TLS 1.3 enhances security by removing insecure cipher suites, reducing handshake round trips from two to one, and mandating forward secrecy through Ephemeral Diffie-Hellman key exchange.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design secure, scalable webhook workflows with tenant isolation and incident response.\n\n## Key Concepts\n- Per-tenant keys stored in an HSM\n- HMAC verification\n- Replay protection\n- Tenant isolation\n- Tamper-evident logs\n- Revocation and rotation\n- Incident response playbook\n\n## Code Example\n```javascript\n// Node.js example: verify HMAC signature per tenant\nconst crypto = require('crypto');\nfunction verifyTenantWebhook(tenantId, payload, signature, getSecret){\n  const secret = getSecret(tenantId); // fetch from HSM-backed store\n  const hmac = crypto.createHmac('sha256', secret).update(payload).digest('hex');\n  return crypto.timingSafeEqual(Buffer.from(hmac, 'hex'), Buffer.from(signature, 'hex'));\n}\n```\n\n## Follow-up Questions\n- How would you test for replay and clock skew?\n- How would you handle cross-tenant revocation latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-24",
        "questionId": "q-276",
        "question": "How would you design a secure job scheduling system for a microservices environment that prevents privilege escalation while ensuring reliable execution across distributed services?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt Kubernetes namespaces per tenant, Istio service mesh with mTLS, and SPIRE for workload attestation. Encrypt data with envelope encryption using per-tenant CMKs in AWS KMS and store in tenant-sco",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement Kubernetes CronJobs with pod security policies, resource quotas, non-root users, read-only filesystems, network policies, RBAC, secrets management via Vault, audit logging, and circuit breakers for distributed…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement per-device token binding and short-lived tokens. Bind tokens to a device attestation (TPM/HSM) and a per-request nonce; require mTLS; verify audience/issuer; store jti/nonce in a fast cache",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement end-to-end image provenance and runtime controls. Sign all container images with Cosign to cryptographically verify integrity and prevent tampering. Generate SBOMs and scan for CVEs during CI to surface known…",
            "isCorrect": false
          }
        ],
        "explanation": "## System Design Overview\n\nA secure job scheduling system in microservices requires layered security controls and distributed coordination mechanisms.\n\n## Security Architecture\n\n**Pod Security Standards**: Enforce restricted baseline with non-root containers, read-only root filesystem, and drop all Linux capabilities\n\n**RBAC Implementation**: Service accounts with least-privilege permissions, namespace-scoped roles, and PSP admission controllers\n\n**Network Isolation**: Network policies restricting egress traffic, mTLS via service mesh (Istio/Linkerd), and API server whitelisting\n\n## Scheduling Patterns\n\n**Kubernetes CronJobs**: Native distributed scheduling with leader election, failover handling, and horizontal scaling\n\n**Service Mesh Integration**: Sidecar proxies for authentication, authorization, and encrypted inter-service communication\n\n## Secrets Management\n\n**Vault Integration**: External secrets operator for dynamic credentials, short-lived tokens, and audit trails\n\n**K8s Secrets**: Encrypted at rest with automatic rotation and namespace isolation\n\n## Reliability Features\n\n**Circuit Breakers**: Hystrix patterns for downstream service failures with fallback mechanisms\n\n**Retry Logic**: Exponential backoff with jitter, max retry limits, and dead letter queues for failed jobs\n\n## Monitoring & Observability\n\n**Distributed Tracing**: OpenTelemetry for end-to-end job execution visibility\n\n**Metrics Collection**: Prometheus exporters for job success rates, execution times, and resource utilization\n\n## Failure Handling\n\n**Job Dependencies**: Argo Workflows for complex DAG-based job orchestration\n\n**Dead Letter Queues**: RabbitMQ/Kafka for failed job analysis and manual recovery\n\n## Resource Management\n\n**Resource Quotas**: CPU/memory limits per namespace to prevent resource exhaustion attacks\n\n**Priority Classes**: Preemption policies for critical vs non-critical job execution\n\n## Compliance & Auditing\n\n**Audit Logging**: OPA Gatekeeper for policy enforcement and compliance reporting\n\n**Immutable Infrastructure**: Container image signing with Cosign and SBOM generation",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-04T06:50:05.859Z",
    "version": 1
  }
]