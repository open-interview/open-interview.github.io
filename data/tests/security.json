[
  {
    "id": "test-security",
    "channelId": "security",
    "channelName": "Security",
    "title": "Security Knowledge Test",
    "description": "Test your understanding of Security with 25 questions.",
    "questions": [
      {
        "id": "security-q-0",
        "questionId": "q-387",
        "question": "Design a zero-trust authentication system for Snowflake's data warehouse that supports MFA, passkeys, and handles 100M+ daily auth requests. How would you prevent replay attacks while ensuring sub-100ms latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt per-tenant RBAC/ABAC, enforce ROW-LEVEL SECURITY and dynamic masking in the warehouse, and expose only authorized fields. Encrypt at rest with CMEK via KMS/HSM; enforce mTLS between data service",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Validate file size (e.g., max 5MB) and allowed extensions on the server; verify MIME types to ensure they match the expected types; stream uploads directly to a private cloud bucket without storing files on local disk;…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Move API key usage server-side: add a backend proxy that signs requests to the third-party API with per-tenant keys stored in a KMS, remove keys from frontend, rotate keys, enforce strict scopes and I",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement device-bound passkeys with short-lived JWTs, rate limiting, and distributed caching. Use WebAuthn with hardware-backed keys and nonce-based request signing.",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests system design skills, security knowledge, and performance optimization - critical for Snowflake's enterprise-scale authentication infrastructure.\n\n## Expected Answer\nStrong candidates discuss: WebAuthn implementation, device fingerprinting, distributed rate limiting, cache-aside pattern, circuit breakers, and monitoring for anomaly detection. They should address replay attack prevention through nonces, timestamp validation, and request signing.\n\n## Code Example\n```typescript\nclass ZeroTrustAuth {\n  async authenticate(request: AuthRequest): Promise<AuthResponse> {\n    const nonce = this.generateNonce();\n    const deviceFingerprint = await this.getDeviceFingerprint(request);\n    \n    // Rate limit check\n    await this.rateLimiter.check(deviceFingerprint);\n    \n    // Validate passkey signature with nonce\n    const isValid = await this.validatePasskey(\n      request.credential, \n      nonce,\n      deviceFingerprint\n    );\n    \n    if (!isValid) throw new UnauthorizedError();\n    \n    // Issue short-lived JWT\n    return this.issueJWT(request.userId, '5m');\n  }\n}\n```\n\n## Follow-up Questions\n- How would you handle key rotation without service interruption?\n- What monitoring metrics would you track for security incidents?\n- How would you design fallback for passkey-only devices?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-1",
        "questionId": "q-5483",
        "question": "For a real-time ML inference platform deployed on Kubernetes, how would you secure the model supply chain from training to inference against tampering and exfiltration? Design a defense-in-depth plan covering artifact signing, reproducible builds, SBOM enforcement, a secure model registry with per-model access controls, runtime attestation, and an incident-playbook including rollback and revocation steps. List concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sanitize output using HTML encoding and implement CSP. Reflected XSS affects individual users, stored XSS can impact all users visiting the compromised page.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a comprehensive defense-in-depth strategy: Sign all artifacts at every stage (training data, models, containers) using cosign with Sigstore; enforce reproducible builds via Bazel/Nix and generate SBOMs using C…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement sandboxed UDF runtimes using WebAssembly (WASM) to enforce strict isolation and prevent unauthorized system access. Block network egress from UDFs by default and require explicit approval for data transfers. E…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use algorithm agility with a hybrid signing flow that signs both PQC and legacy keys during migration. Store per-tenant keys in an HSM-backed KMS with remote attestation and strong RNG. Enforce short-",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nDemonstrates ability to secure ML model supply chains end-to-end with practical, production-ready tooling.\n\n## Key Concepts\n\n- Artifact signing (cosign/Sigstore)\n- Reproducible builds (Bazel/Nix)\n- SBOM enforcement (CycloneDX)\n- Secure model registry with per-model RBAC\n- Runtime attestation (SPIFFE/JWT + OPA)\n- Key management (KMS/HSM)\n- Rollback & revocation playbooks\n- Attack simulation and tabletop testing\n\n## Code Example\n\n```javascript\n// Pseudocode: verify signed model artifact using Sigstore cosign bindings\nimport { verifyArtifact } from 'sigstore';\nconst isValid = await verifyArtifact(modelArtifact, signature);\nif (!isValid) {\n  throw new Error('Model artifact signature verification failed');\n}\n```\n\n## Implementation Notes\n\nThe solution layers security controls at each stage of the ML lifecycle: development (reproducible builds, SBOMs), deployment (artifact signing, secure registry), and runtime (attestation, policy enforcement). The incident response capabilities (rollback, revocation) ensure operational resilience when security controls detect threats.",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-2",
        "questionId": "q-27381",
        "question": "You're operating a multi-tenant data lakehouse that ingests telemetry from untrusted connectors into shared storage. A compromised connector could exfiltrate tenant data or poison data lineage. Propose an end-to-end security plan covering per-tenant isolation, ingest-time schema validation, envelope encryption with per-tenant keys via KMS/HSM, automatic key rotation, immutable tamper-evident logs, data provenance, and an incident-response playbook including rollback and re-ingestion?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Artifacts must be reproducible and signed within an HSM; SBOMs are validated against a trusted feed. Vehicles attest bootloader, firmware, and update package over mTLS using per-vehicle certificates. Implement canary ro…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-user rate limiting with a Redis token bucket (e.g., 10 attempts per 15 minutes) and an account lockout after 5 consecutive failures within 15 minutes. Validate passwords with bcrypt using cons",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-environment secrets in GitHub Actions to ensure build logs never echo sensitive values. Enable secret scanning to detect exposed credentials before they reach the repository. Lock dependency versions to pr…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a defense-in-depth strategy spanning ingestion, storage, and access layers. Enforce per-tenant namespaces for both data and metadata to guarantee strict isolation. Validate all ingested records against strict,…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical security controls for a multi-tenant data architecture where untrusted connectors pose risks to data confidentiality, integrity, and lineage. Interviewers seek a unified response spanning isolation, cryptography, logging, provenance, and incident response—demonstrating the candidate's ability to design holistic security solutions for complex data platforms.\n\n## Key Concepts\n- **Tenant isolation**: Namespace-level separation ensuring tenants cannot access or interfere with each other's data\n- **Envelope encryption**: Per-tenant keys with data encrypted under a master key, enabling efficient per-tenant key rotation and access revocation\n- **Tamper-evident logging**: Using cryptographic techniques such as hash chaining or Merkle trees to detect unauthorized modifications to audit records\n- **Data provenance**: Tracking the complete lifecycle of data from source through transformations to final consumption, ensuring lineage integrity and enabling impact analysis",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-3",
        "questionId": "q-12442",
        "question": "In a globally distributed security analytics API with 100+ microservices, logs flow into a central store. A breach on one host could let an attacker tamper logs. Propose a tamper‑evident logging design: where to sign events, how to chain hashes, how ingestion verifies integrity, and how to rotate/revoke keys. Include concrete technologies and trade‑offs (HSM/KMS, immutable storage, audit trails, rollback)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant GPU inference stack using hardware enclaves (Intel TDX/AMD SEV). Enforce memory isolation, disable dedup, and wrap each tenant’s model with envelope encryption managed by a KMS/HSM",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Artifacts must be reproducible and signed within an HSM; SBOMs are validated against a trusted feed. Vehicles attest bootloader, firmware, and update package over mTLS using per-vehicle certificates. Implement canary ro…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To prevent data leakage, design around confidential computing enclaves (SGX/SEV) with per-tenant sealed keys and envelope encryption for model/data, remote attestation to a policy service, signed mani",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Sign each log line with a per-event signature using a KMS-backed key, and hash-chain entries to form a tamper-evident ledger. Ingestor validates signatures and hashes, raising alerts on mismatches. St",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nIllustrates mismatch between security controls and high-volume logging. Tests ability to design tamper-evident systems with concrete tech, key management, and incident response.\n\n## Key Concepts\n- Tamper-evident logging\n- Signature schemes and KMS/HSM\n- Immutable storage\n- Key rotation and revocation\n- Ingestion verification and alerting\n\n## Code Example\n```javascript\n// Example signing function using Node.js crypto and AWS KMS\nconst crypto = require('crypto');\nasync function signLogEntry(keyId, payload) {\n  const signer = crypto.createSign('SHA256');\n  signer.update(payload);\n  signer.end();\n  // In real use, sign with KMS: return await kms.sign({KeyId: keyId, Message: signer.sign()...})\n  return signer.sign(null, 'base64');\n}\n```\n\n## Follow-up Questions\n- How would you detect a stale signature after key rotation?\n- What are the trade-offs of per-event signing vs per-batch signing?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-4",
        "questionId": "q-4928",
        "question": "Design a real-world, end-to-end security design for a new microservice that validates payment tokens for a high-volume API used by consumer apps (e.g., Tesla, Stripe, Meta). The design should cover code signing, artifact integrity, runtime attestation, mTLS, token binding, key management in an HSM, SBOM enforcement, and an incident-playbook with rotation and revocation steps. List concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant envelope encryption with KMS: isolate keys by tenant, enforce strict IAM policies, route Encrypt/Decrypt through an admission service that validates tenant scope, cache data keys with short",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a multi-layered defense strategy: (1) Partition edge caches by tenant using dedicated namespaces with strict isolation boundaries; (2) Guard encryption keys in hardware-backed TEEs with envelope encryption, ma…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement envelope encryption with per-tenant DEKs wrapped by regional KMS/HSM master keys. Enforce strict access control using short-lived credentials, tenant- and region-scoped IAM policies, and network segmentation t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a zero-trust flow: sign and verify all code/artifacts with cosign; enforce SBOMs and attestations; bind OAuth2 tokens to TLS using mTLS and SPIFFE IDs; sign/verify JWTs with hardware-backed",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests ability to design a practical, scalable security fabric for high-stakes payments, spanning CI/CD, runtime, and incident response, with real toolchains.\n\n## Key Concepts\n\n- Zero trust, SBOM, artifact signing (cosign/Sigstore)\n- mTLS, SPIFFE/SPIRE, token binding\n- Hardware security modules (HSM), key management\n- Runtime attestation, policy enforcement (OPA)\n- Incident response, key rotation, revocation\n\n## Code Example\n\n```javascript\n// Pseudo: verify JWT with key from HSM/KMS\nconst jwt = require('jsonwebtoken');\nconst publicKey = getPublicKeyFromHSM(); \njwt.verify(token, publicKey, { algorithms: ['RS256'] });\n```\n\n## Follow-up Questions\n\n- How would you handle key rotation without service downtime?\n- How would you prove SBOM compliance in CI/CD?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-5",
        "questionId": "q-18033",
        "question": "Scenario: A Discord-enabled analytics service stores event data in Snowflake. The API currently uses a single Snowflake role with broad privileges and secrets in environment variables. A security test flags over-privileged access and insecure key storage. Propose a concrete, beginner-friendly remediation plan: implement least-privilege roles per service, separate warehouses, parameterized queries, rotate credentials, and add basic monitoring for unusual data access?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant hardware-backed keys, envelope encryption, extension signing and integrity checks, tenant-scoped key rotation and revocation, runtime attestation, tamper-evident logs, and an incident-r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Split Snowflake access by service and enforce least privilege: create per-service roles with only needed schemas/tables; assign a dedicated warehouse per service; move API keys/secrets to a vault and",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I'd immediately calculate the CVSS score, document the impact assessment, and create a remediation timeline. I'd escalate to security leadership with a risk-benefit analysis comparing data exposure costs vs launch delay…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-field ABAC with a policy engine (OPA) at the GraphQL layer; enforce field-level ACLs in resolvers and generate filtered responses by default. Disable or gate schema introspection in prod",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nPractical plan to minimize blast radius, enforce least privilege, and automate secrets management across cloud services.\n\n## Key Concepts\n- Least privilege across Snowflake objects and service boundaries\n- Secrets management and rotation\n- Parameterized queries and SQL injection mitigation\n- Simple data-access monitoring and alerts\n\n## Code Example\n```javascript\n// Example: parameterized query with Snowflake SDK\nconst sql = 'SELECT * FROM events WHERE user_id = ?';\nconnection.execute({ sqlText: sql, binds: [userId], complete: function(err, stmt, rows){ /* ... */ } });\n```\n\n## Follow-up Questions\n- How would you test the effectiveness of the new least-privilege model in a CI pipeline?\n- What metrics would you monitor to detect privilege abuse?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-6",
        "questionId": "q-20039",
        "question": "Advanced interview question: You’ll deploy a multi-tenant GPU inference service on Nvidia hardware with confidential computing (enclave) support. Tenant data is processed in GPU memory and logs may reveal inputs. Design a concrete plan to prevent leakage via memory-side channels while preserving performance. Include threat model, concrete controls (kernel design, attestation, memory encryption, and logging rules), and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate file size (e.g., max 5MB) and allowed extensions on the server; verify MIME types to ensure they match the expected types; stream uploads directly to a private cloud bucket without storing files on local disk;…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant isolation must be enforced at ingestion, storage, and compute. Use envelope encryption with tenant-scoped KEKs managed by a HSM-backed KMS, rotate keys regularly, and revoke on compromise.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement strict validation: check magic bytes for JPEG/PNG, confirm extension, reject executable mime types, cap size at 1 MB, save in a non-executable upload dir, use a randomized filename, sanitize",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Threat model: In a multi-tenant GPU inference service with confidential computing support, primary threats include memory-side channel attacks where one tenant may infer information from another tenant's GPU memory acce…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to reason about hardware security primitives, side-channel vulnerabilities, and incident response in a scalable GPU-backed service. It evaluates practical knowledge of confidential computing deployment at scale.\n\n## Key Concepts\n\n- **Hardware Enclaves**: Nvidia confidential computing with SEV-SNP provides hardware-backed isolation and memory encryption\n- **Memory-Side Channels**: Attacks exploiting timing variations or access patterns in shared GPU memory\n- **Data-Oblivious Programming**: Writing kernels whose behavior is independent of actual data values\n- **Remote Attestation**: Verifying the integrity of the execution environment before trusting it with sensitive workloads\n- **Constant-Time Kernels**: Ensuring execution time does not leak information through timing side channels\n- **ORAM (Oblivious RAM)**: Protocols that obscure access patterns to prevent pattern-based attacks\n- **Auditability**: Maintaining security logs while ensuring they do not become a leakage vector\n\n## Code Example\n\n```c\n// Constant-time access pattern to prevent data-dependent indexing\nstatic inline uint8_t ct_get(const uint8_t *a, size_t i, size_t n, uint8_t def) {\n    uint8_t v = def;\n    for (size_t j = 0; j < n; j++) {\n        v |= a[j] * (1 - secure_cmp(i, j));\n    }\n    return v;\n}\n\n// Secure comparison using constant-time arithmetic\nstatic inline uint8_t secure_cmp(size_t a, size_t b) {\n    volatile size_t diff = a ^ b;\n    return (diff | -diff) >> (sizeof(size_t) * 8 - 1);\n}\n```\n\nThis pattern ensures consistent memory access patterns regardless of the actual index being accessed, preventing timing-based information leakage.",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-7",
        "questionId": "q-27533",
        "question": "In a small fintech API (Express/Node) that issues JWT on login, a security review finds no login rate limiting or audit log. Propose a beginner-friendly plan to mitigate within 24 hours: implement per-IP login rate-limiting, per-user lockouts after 5 failed attempts for 15 minutes, and an audit log capturing timestamp, user_id, IP, and outcome. Outline concrete steps, tech choices (express-rate-limit, Redis or in-memory store, Winston), and a simple CI check to verify middleware is loaded?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Switch to per-user salted hashes using Argon2id (memory-cost) or bcrypt with cost 12. Migrate in batches: add a marker column, rehash on login for older hashes, and resync background worker until comp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Threat model and threat sources. Use Bonawitz secure aggregation with per-round masking; enforce differential privacy budgets per client; require remote attestation and device-bound keys for updates;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use express-rate-limit to cap IP login attempts (e.g., 10/min) and a Redis-backed per-user failure counter with 15-minute TTL. On 5th failure, set a user-lock flag and return 429 for 15 minutes. Emit",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement pre-commit secret scanning (git-secrets), use a central vault (HashiCorp Vault or cloud secret manager) with per-service roles, inject at runtime via CI/CD, avoid hard-coding. Use short-live",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe plan tests practical, beginner-friendly security hardening with minimal ops impact.\n\n## Key Concepts\n- Rate limiting basics, per-IP vs per-user\n- Temporary lockouts and TTL semantics\n- Audit logging for login events\n- Non-blocking, observable metrics in CI\n\n## Code Example\n```javascript\nconst rateLimit = require('express-rate-limit');\nconst RedisStore = require('rate-limit-redis');\nconst redisClient = require('redis').createClient();\n\nconst loginLimiter = rateLimit({\n  windowMs: 60 * 1000,\n  max: 10,\n  store: new RedisStore({ client: redisClient }),\n  message: 'Too many login attempts, please try again later.'\n});\n```\n\n## Follow-up Questions\n- How would you test the lockout behavior in CI?\n- What are the trade-offs of in-memory vs Redis-backed rate limiting in a distributed system?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-8",
        "questionId": "q-8347",
        "question": "You deploy a multi-tenant API at the edge (e.g., with workers) and tenants report uneven rate limiting: some customers hit limits while others are idle. Propose a beginner-safe plan to implement tenant-scoped quotas using per-tenant tokens, validate x-tenant headers, and minimize latency, including observability and safeguard against header spoofing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a zero-trust multi-cloud kubernetes plan: deploy SPIRE for per-service identities, enable mTLS across the mesh (Istio/Linkerd), store data with envelope encryption using KMS-backed keys, apply",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To secure a webhook endpoint, implement HMAC SHA-256 verification using the provider's secret to compute a signature over the raw body, compare to the X-Signature header, and reject if mismatch. Inclu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-tenant token bucket at the edge. Validate x-tenant against allowed tenants; keep quotas in a fast store (KV/cache) and refill tokens per minute; enforce burst with bucket size; reject reques",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-IP rate limiting at the API layer using a Redis-backed store for scale. Use a 15-minute window and a conservative max (e.g., 100 requests), enable standard rate headers, log blocked atte",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nInterview context explanation.\n\n## Key Concepts\n- Per-tenant quotas\n- Edge rate limiting\n- Header validation and spoofing\n- Observability and latency trade-offs\n\n## Code Example\n```javascript\n// Example edge rate limiter snippet\n```\n\n## Follow-up Questions\n- How would you test this in staging?\n- How would you scale quotas with hundreds of tenants?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-9",
        "questionId": "q-15369",
        "question": "You're running a confidential ML inference platform in public clouds for multiple tenants. Tenants submit models and data, sharing GPUs and host OS. An attacker may attempt cross-tenant data leakage via memory side-channels or misconfigured enclaves. Propose a concrete end-to-end security design: TEEs (TDX/SEV), disable memory dedup, data handling, monitoring, and incident response with tooling and trade-offs. How would you implement it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enforce tenant isolation with hardware TEEs (Intel TDX/AMD SEV) for model runtimes; disable memory dedup; use per-tenant keys and envelope encryption; partition GPU memory or use confidential GPUs; enforce encrypted dat…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Enable per-IP login throttling with a Redis-backed counter and a per-tenant failure ledger. Apply exponential backoff after N consecutive failures, and require MFA for tenants flagged as high risk. Ce",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use short-lived access tokens (5 minutes) with refresh tokens, rotate signing keys daily and on incident, and add a revocation check at the gateway. Disable refresh for compromised sessions. Add a CI",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Move tokens to OS secure storage (Keychain/Keystore), never persist in insecure storage. Use short-lived access tokens (15–60 min) with device-bound refresh tokens; protect with biometric unlock. Impl",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical confidential computing design for multi-tenant ML workloads, specifically addressing side-channel attacks, enclave misconfigurations, and cross-tenant data leakage vectors. It evaluates the candidate's understanding of hardware security primitives, threat modeling for shared infrastructure, and the ability to balance security with operational requirements.\n\n## Key Concepts\n\n- **Hardware TEEs and Attestation**: Intel TDX and AMD SEV-SNP provide hardware-enforced isolation with remote attestation to verify the runtime environment's integrity before granting access to sensitive data.\n- **Side-Channel and Memory Deduplication Mitigations**: Disabling memory deduplication prevents cross-tenant information leakage via memory page collision attacks; memory encryption prevents physical tampering.\n- **Per-Tenant Key Management and Envelope Encryption**: Each tenant receives dedicated encryption keys with envelope encryption for data-at-rest, ensuring tenant data remains logically isolated even on shared infrastructure.\n- **Auditability and Tamper-Evident Logs**: Immutable audit trails using append-only storage or blockchain-based logging ensure all data access and model executions are traceable for compliance and forensics.\n- **Incident Response, Forensics, and Rollback**: Automated containment, forensic data collection, and tenant-specific rollback mechanisms enable rapid recovery from security incidents.\n\n## Code Example\n\n```javascript\n// Pseudo attestation flow for confidential ML inference\nasync function attestRuntime(tenantId, attestationQuote) {\n  // 1. Verify TEE attestation quote from hardware\n  const attestationResult = await verifyAttestationQuote(attestationQuote);\n  \n  if (!attestationResult.isValid) {\n    throw new Error('Attestation failed: runtime integrity compromised');\n  }\n  \n  // 2. Verify measured boot hash matches expected values\n  const expectedHash = await getExpectedRuntimeHash(attestationResult.teeType);\n  if (attestationResult.measuredHash !== expectedHash) {\n    throw new Error('Runtime hash mismatch: possible tampering detected');\n  }\n  \n  // 3. Issue tenant-specific session credentials\n  const sessionKey = await deriveSessionKey(tenantId, attestationResult.measurement);\n  \n  return {\n    authorized: true,\n    sessionToken: sessionKey,\n    enclavePublicKey: attestationResult.enclaveKey\n  };\n}\n\n// Example: Initialize secure inference pipeline\nasync function initializeSecureInference(tenantId, modelId, data) {\n  const { sessionToken, enclavePublicKey } = await attestRuntime(\n    tenantId, \n    await getAttestationQuote()\n  );\n  \n  // Encrypt data with enclave key before sending to TEE\n  const encryptedData = await encryptForEnclave(data, enclavePublicKey);\n  \n  // Execute inference within confidential context\n  const result = await executeInference(encryptedData, sessionToken);\n  \n  return result;\n}\n```\n\n## Follow-up Questions\n\n- How to validate side-channel mitigations and test for residual leakage vectors?\n- What are the performance trade-offs when migrating ML workloads to confidential GPUs?\n- How do you handle key rotation in a multi-tenant TEE environment without service disruption?\n- Design the incident response workflow when a tenant reports potential data exposure.\n- How would you implement zero-trust networking between tenants in a shared inference cluster?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-10",
        "questionId": "q-17641",
        "question": "You manage a Kubernetes-based microservice that reads DB credentials from a secret store via an in-cluster sidecar injector. A rotation event updates credentials, but some pods miss the refresh, causing outages. Describe a beginner-friendly plan to implement robust secret rotation with zero downtime, including secret manager choice, injection, rollout strategy, and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would require per-model signing with an HSM-backed key, plus runtime attestation and per-tenant sandboxes. On upload, sign the model and attach a SBOM; verify signature before loading. Enforce stric",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-tenant WASM sandbox plan: plugins signed with Vault's Ed25519 keys; per-tenant identity bound to short-lived JWTs for API access; limit plugin host surface with a restricted API gateway;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement end-to-end location attestations from drivers by signing location data within a device-protected key inside a TEE (Android Keystore or DeviceCheck for iOS), wrapped in a COSE-signed token. Use per-ride nonces…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a centralized secret manager (Vault or cloud KMS) with short-lived credentials. Use a sidecar injector to refresh creds in pods, and implement a controlled rolling restart on rotation. Add a lig",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical knowledge of secrets rotation in Kubernetes, focusing on reliability, minimal downtime, and observability rather than theory.\n\n## Key Concepts\n\n- Centralized secret management\n- Short-lived credentials\n- Sidecar injection and pod refresh\n- Rolling updates and health checks\n- Observability of rotation events\n\n## Code Example\n\n```javascript\n// Pseudo rotation workflow\nasync function rotateSecret() {\n  const newCreds = await secretStore.getNew('db-cred');\n  await injector.updateEnv('DB_CRED', newCreds);\n  await patchDeployment();\n}\n```\n\n## Follow-up Questions\n\n- How would you test rotation in CI/CD without downtime?\n- What metrics would you monitor to detect rotation failures early?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-11",
        "questionId": "q-202",
        "question": "How do passkeys implement passwordless authentication using public-key cryptography?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Passkeys implement passwordless authentication using device-generated public-key cryptography where private keys are securely stored in hardware enclaves and synchronized across devices via secure cloud services, while…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a concrete plugin deployment pipeline for a model-serving platform: CI builds sign artifacts (SBOM + Sigstore); runtime attestation on load; sandboxed isolation (WASM or gVisor); per-plugin IA",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use PKCE: generate a high-entropy code_verifier (43–128 chars), compute code_challenge = base64url(SHA-256(code_verifier)). Open the auth page with redirect_uri. After login, exchange the code at the",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-device token binding and short-lived tokens. Bind tokens to a device attestation (TPM/HSM) and a per-request nonce; require mTLS; verify audience/issuer; store jti/nonce in a fast cache",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nPasskeys revolutionize authentication by replacing passwords with asymmetric cryptography, where each user possesses a unique key pair generated and managed by their devices. The private key never leaves the device, residing in secure hardware like TPMs or Secure Enclaves, while the public key is safely stored on the server during registration.\n\n## Key Generation Process\nThe key generation begins when a user creates an account or adds a passkey. The device's authenticator generates a cryptographically strong key pair using algorithms like ECDSA with P-256 or EdDSA. The private key is wrapped with device-specific hardware keys and stored in the secure enclave, making it virtually impossible to extract. The public key is transmitted to the server along with attestation data proving the key was generated by trusted hardware.\n\n## Cross-Device Synchronization\nModern passkey ecosystems (Apple iCloud Keychain, Google Password Manager, Microsoft Authenticator) enable seamless cross-device synchronization through encrypted cloud storage. When a user signs into a new device, their private keys are securely transferred using end-to-end encryption protocols. Each device maintains its own secure storage while participating in the synchronization network, ensuring that even if one device is compromised, the private keys remain protected by hardware security.\n\n## Authentication Flow\nDuring login, the server sends a challenge that can only be signed by the private key. The user authorizes the operation with biometrics or device PIN, the private key signs the challenge within the secure enclave, and the signature is verified using the stored public key. This process eliminates phishing attacks since the signature is bound to the specific server domain, and even if the server is compromised, no sensitive credential data can be extracted.",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-12",
        "questionId": "q-14481",
        "question": "You are building a file upload endpoint in a backend API used by a mobile/web app. A pentest finds that crafted filenames enable path traversal, letting an attacker read private files outside the upload directory. Propose a beginner-friendly, concrete plan to fix this, detailing validation, storage isolation, and testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a PKCE-enabled OAuth 2.0 flow with per-tenant issuer and audience, bind access tokens to tenant_id and client_id, and enforce short-lived access tokens (e.g., 15 minutes) with rotating refresh t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement allow-list extensions, sanitize with filepath.Clean, reject any path escaping the upload dir (../ or absolute). Store uploads in a fixed, non-public directory with restricted permissions. Ad",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Module signing with Ed25519, SBOM, runtime sandbox (Wasmtime), per-tenant policy via OPA with SPIFFE IDs, mTLS between runtimes and control plane, data isolation with tenant-scoped namespaces, runtime",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement append-only logs per service with the following data fields: user_id (hashed), timestamp, event_type, IP address, device information, and authentication outcome, while ensuring all PII is properly redacted. St…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests practical input validation, path handling, and secure storage isolation for file uploads—common in consumer apps.\n\n## Key Concepts\n\n- Path traversal mitigation\n- Safe path joining with filepath.Clean\n- Upload directory isolation and permissions\n- Secure defaults and CI testing\n\n## Code Example\n\n```python\nfrom flask import Flask, request\nimport os\nfrom werkzeug.utils import secure_filename\n\nUPLOAD_DIR = '/srv/app/uploads'\n\ndef save_file(f):\n    name = secure_filename(f.filename)\n    path = os.path.join(UPLOAD_DIR, name)\n    if not os.path.commonpath([path, UPLOAD_DIR]) == UPLOAD_DIR:\n        raise ValueError('Invalid path')\n    f.save(path)\n```\n\n## Follow-up Questions\n\n- How would you adapt if uploads are served directly by a CDN?\n- How would you handle large file streaming to avoid memory bloat?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-13",
        "questionId": "q-423",
        "question": "You discovered a critical security vulnerability in your team's production system that could expose customer data, but fixing it requires delaying a major product launch. How would you handle this situation, considering CVSS scoring, stakeholder communication, and risk-benefit analysis?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant keys stored in an HSM to sign outbound payloads and verify them at ingress, enforce nonce+timestamp replay protection, and minimize endpoints per tenant. Implement tamper-evident loggin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I'd immediately calculate the CVSS score, document the impact assessment, and create a remediation timeline. I'd escalate to security leadership with a risk-benefit analysis comparing data exposure costs vs launch delay…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement an HSM-backed key hierarchy with per-tenant signing keys stored in a FIPS 140-2 Level 3 HSM, rotated every 90 days. Issue access tokens using RS256 signatures bound to TLS client certificates via token binding…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Make all session cookies HttpOnly and Secure, and set SameSite to Lax with tenant-scoped domains. Express: app.use(session({ cookie: { httpOnly: true, secure: true, sameSite: 'Lax', domain: '.tenant.e",
            "isCorrect": false
          }
        ],
        "explanation": "## Interview Context\nTests security incident response, risk assessment, and cross-functional leadership under pressure.\n\n## Key Components\n- **CVSS Scoring**: Calculate vulnerability severity using base, temporal, and environmental metrics\n- **Risk Assessment**: Quantify potential data exposure costs vs launch delay impact\n- **Stakeholder Communication**: Coordinate with security, product, legal, PR, and executive teams\n- **Remediation Strategy**: Balance immediate mitigation with comprehensive fix timeline\n\n## Code Example\n```javascript\n// Risk assessment calculation\nconst riskAnalysis = {\n  cvssScore: 9.8, // Critical\n  potentialLoss: 2500000, // Estimated data breach cost\n  launchDelayCost: 500000, // Revenue impact\n  mitigationOptions: ['WAF rules', 'Rate limiting', 'Database encryption']\n};\n\n// Communication template\nconst incidentReport = {\n  severity: 'CRITICAL',\n  impact: 'Customer data exposure',\n  timeline: '24-48 hours for patch',\n  stakeholders: ['CTO', 'CISO', 'VP Product', 'Legal']\n};\n```\n\n## Follow-up Questions\n- How would you handle this if the launch was already in progress?\n- What security frameworks would you reference for incident response?\n- How do you determine when to proceed vs delay the launch?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-14",
        "questionId": "q-9291",
        "question": "You are enabling user-defined functions (UDFs) in a multi-tenant distributed data store similar to MongoDB, with WASM sandboxing for the UDFs. Propose a concrete, deployable security architecture that guarantees tenant data isolation, per-tenant key management with envelope encryption, and verifiable runtime attestation. Include: memory/IO/CPU isolation strategies, secure code signing and update flow, tamper-evident logging and incident response, and discuss trade-offs. Specify technologies and a high‑level deployment plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Publish policy updates as JSON Web Signatures (JWS) using Ed25519, signed by a central authority with the public key securely pinned to each edge runtime's Hardware Security Module (HSM). Edge runtimes verify signatures…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Move tokens to HttpOnly Secure cookies with SameSite, avoid localStorage. Use short-lived access tokens (5-15 min) with server-side rotation and refresh. Implement CSRF protection (CSRF tokens or doub",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a multi-tenant architecture for WASM-based UDFs with strict data isolation. Use per-tenant KEKs encrypted by a central KMS (envelope encryption), rotate keys monthly, sign UDF artifacts, requir",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Enforce per-tenant IaC isolation: module signing and a mandatory Terraform plan review; use tenant-scoped remote state encrypted at rest by Vault; deploy Vault dynamic secrets with short leases and au",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes ability to design secure, scalable tenant isolation for dynamic code execution with cryptographic guarantees and auditable traceability.\n\n## Key Concepts\n\n- WASM sandboxing and resource quotas\n- Per-tenant envelope encryption with KEKs\n- Remote attestation (SGX/TPM) for runtime integrity\n- Tamper-evident logging and signing\n- Secure update and rollback processes\n- Incident response playbooks\n\n## Code Example\n\n```javascript\n// Pseudo attestation check\nasync function verifyTenantAttestation(tenantId, attestation) {\n  const valid = await attestationService.verify(attestation, tenantId);\n  if (!valid) throw new Error(\"Invalid attestation\");\n  return true;\n}\n```\n\n## Follow-up Questions\n\n- How would you test the attestation flow under tenant churn and key rotation?\n- What failure modes would trigger automatic rollback and how would you verify integrity?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-15",
        "questionId": "q-14869",
        "question": "In a real-time enterprise messaging service marketed as end-to-end encrypted for chat, design a secure key-management and message-routing architecture that preserves E2E for content while enabling server-side features like indexing and moderation. Specify how per-chat ephemeral keys are generated and stored, how keys are cached and rotated, how HSM-backed storage and device attestation are integrated, how forward secrecy via a ratcheting scheme is achieved, how SBOM enforcement and revocation fit in, and how rollout handles offline clients?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant isolation must be enforced at ingestion, storage, and compute. Use envelope encryption with tenant-scoped KEKs managed by a HSM-backed KMS, rotate keys regularly, and revoke on compromise.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a secure E2E chat design for an enterprise video platform. Use per-chat ephemeral keys stored in an HSM, server-assisted metadata for moderation without decrypting content, forward secrecy via",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "## Immediate Mitigation (Hotfix) 1. **Redis Cache Isolation**: Flush affected Redis instance, implement tenant-scoped keys with format `tenant:{tenant_id}:{resource}:{id}`, enable Redis ACLs or use separate Redis databa…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement append-only logs per service with the following data fields: user_id (hashed), timestamp, event_type, IP address, device information, and authentication outcome, while ensuring all PII is properly redacted. St…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical understanding of E2EE integration in a real-time, scale-out system, including trusted hardware, per-chat keys, and secure rotation.\n\n## Key Concepts\n\n- End-to-end encryption with per-chat ephemeral keys\n- HSM-backed key storage and device attestation\n- Forward secrecy via ratcheting; 24h rotation\n- SBOM enforcement and revocation\n\n## Code Example\n\n```javascript\n// rotateChatKeys(chatId, oldKey, newKey)\nfunction rotateChatKeys(chatId, oldKey, newKey) {\n  // derive new ratchet state and store securely\n}\n```\n\n## Follow-up Questions\n\n- How would you detect and respond to key leakage or rogue devices?\n- What metrics ensure rotation cadence aligns with risk?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-16",
        "questionId": "q-6699",
        "question": "You’re building a simple SPA with a REST API that requires authentication via a session token. Propose a minimal, secure token storage strategy in the browser, compare HttpOnly cookies vs localStorage, and outline how you would defend against XSS and CSRF, plus a basic test plan to verify token safety?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Require signed plugin attestations (Sigstore/cosign) plus SBOMs; verify at load against a trusted root; sandbox plugins with WASM engine (Wasmtime) with per-plugin quotas and sandboxed syscalls; isola",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use express-rate-limit to cap IP login attempts (e.g., 10/min) and a Redis-backed per-user failure counter with 15-minute TTL. On 5th failure, set a user-lock flag and return 429 for 15 minutes. Emit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Store the session token in a HttpOnly, Secure cookie with SameSite=Strict; never localStorage. Use SameSite to prevent CSRF; add CSP and strict input encoding. Test with XSS payloads, cookie leakage a",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Architect a per-tenant WASM runtime with strict sandboxing (Wasmtime) and memory isolation; require module signing and remote attestation against an HSM-backed root. Use per-tenant keys in the HSM, SP",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical browser security decisions around token storage and common web-app risks for beginners.\n\n## Key Concepts\n- HttpOnly cookies\n- SameSite CSRF protection\n- CSP and input/output encoding\n- XSS and token leakage risk\n- Testing token safety in CI\n\n## Code Example\n```javascript\n// Node/Express\nres.cookie('session', token, { httpOnly: true, secure: true, sameSite: 'Strict' });\n```\n\n## Follow-up Questions\n- How would you adapt this approach for a mobile app that cannot rely on cookies?\n- What tests would you automate to verify CSRF and XSS defenses during CI?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-17",
        "questionId": "q-21870",
        "question": "Design an edge-first authentication flow for a global CDN-backed app. Ensure access tokens are never cached at edge, support token revocation within 5 minutes, and enable per-tenant RBAC across 3 regions. Propose architecture, protocols, and key rotation strategy, including how you’d test and monitor it in production?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Switch to per-user salted hashes using Argon2id (memory-cost) or bcrypt with cost 12. Migrate in batches: add a marker column, rehash on login for older hashes, and resync background worker until comp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a secure E2E chat design for an enterprise video platform. Use per-chat ephemeral keys stored in an HSM, server-assisted metadata for moderation without decrypting content, forward secrecy via",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement Kubernetes CronJobs with pod security policies, resource quotas, non-root users, read-only filesystems, network policies, RBAC, secrets management via Vault, audit logging, and circuit breakers for distributed…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement edge-first OAuth-like tokens: issue very short-lived, opaque tokens signed per-tenant by a central KMS, with a per-request nonce and client fingerprint to prevent reuse at edge. Do not cache",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests edge security design, token handling, rotation, revocation, and per-tenant RBAC in a globally distributed environment.\n\n## Key Concepts\n- Edge computing, token lifetimes, and JWKS\n- Per-tenant key rotation and revocation\n- Data minimization at edge and canary deployments\n\n## Code Example\n```javascript\n// Pseudo in-edge validator\nfunction validate(token, jwks){\n  const t = decrypt(token);\n  const valid = verifySignature(t, jwks);\n  return valid && !isRevoked(t.id);\n}\n```\n\n## Follow-up Questions\n- How would you securely rotate tenant keys without downtime?\n- How would you verify no token leakage occurred during a canary rollout?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-18",
        "questionId": "q-14802",
        "question": "Scenario: A Stripe-like payments API receives webhooks and stores payloads. After a change, retries can replay old events. Propose a beginner-friendly plan to prevent replay attacks: implement per-endpoint HMAC with a nonce, track nonces per webhook ID in a fast store, enforce a replay window, and test with a replay attempt?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Edge trust plan for a real-time platform: enforce per-edge attestation and strict isolation, use SPIRE identities with mTLS via Istio/Linkerd, and employ envelope encryption with KMS for all data in t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a layered approach: private inference in attested enclaves (Intel SGX/AMD SEV) or isolated GPUs; per-tenant namespaces and data minimization; in-memory encryption with KEKs tied to attestation; di",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt a centralized secret manager (Vault or cloud KMS) with short-lived credentials. Use a sidecar injector to refresh creds in pods, and implement a controlled rolling restart on rotation. Add a lig",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-endpoint HMAC signatures with a nonce in each webhook payload. On receipt, verify the signature and check the nonce against a Redis set for that webhook ID; if seen, reject. TTL for nonces: 5",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nReplay protection for webhooks is a practical beginner challenge in production security, especially for payment flows. It tests understanding of authenticity, freshness, and idempotence without being abstract.\n\n## Key Concepts\n- Webhook signature verification (HMAC)\n- Nonce-based replay protection\n- Ephemeral storage with TTL (e.g., Redis)\n- Idempotent processing and testing for duplicates\n\n## Code Example\n```javascript\nconst crypto = require('crypto');\n// assume req.rawBody is the raw body buffer and req.headers['x-signature'] is the hex signature\nfunction verify(req, secret) {\n  const payload = req.rawBody;\n  const signature = req.headers['x-signature'];\n  const expected = crypto.createHmac('sha256', secret).update(payload).digest('hex');\n  return crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expected));\n}\n```\n\n## Follow-up Questions\n- How would you rotate per-endpoint secrets without downtime?\n- How would you test this in CI/CD to prevent regressions against replay attacks?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-19",
        "questionId": "q-324",
        "question": "How would you design a zero-trust video conferencing platform using WebAuthn passkeys with continuous authentication for enterprise users?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement envelope encryption with per-tenant DEKs wrapped by regional KMS/HSM master keys. Enforce strict access control using short-lived credentials, tenant- and region-scoped IAM policies, and network segmentation t…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement WebAuthn passkeys, device fingerprinting, behavioral biometrics, and microsegmented access controls with real-time threat detection.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Enable per-IP login throttling with a Redis-backed counter and a per-tenant failure ledger. Apply exponential backoff after N consecutive failures, and require MFA for tenants flagged as high risk. Ce",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Secure OTA Update Supply Chain for Autonomous Vehicles ### 1. Build Phase (Reproducibility & Provenance) - **Build System**: Use Bazel or Nix with hermetic build environments (all dependencies pinned via content-addr…",
            "isCorrect": false
          }
        ],
        "explanation": "## Interview Context\nThis question assesses system design skills for security-critical applications, testing understanding of modern authentication patterns, continuous security monitoring, and scalable architecture design.\n\n## Non-Functional Requirements\n- **Authentication**: <99.9% uptime, <100ms login latency\n- **Security**: Zero-trust model, MFA required, session timeout 15min\n- **Scalability**: Support 100K concurrent users, 1M registered\n- **Compliance**: SOC2, GDPR, HIPAA ready\n\n## Architecture Components\n- **Authentication Layer**: WebAuthn passkey server, device registry\n- **Continuous Monitoring**: Behavioral analysis engine, anomaly detection\n- **Access Control**: Microsegmentation, just-in-time permissions\n- **Session Management**: Real-time validation, adaptive re-auth\n\n## Key Calculations\n- **Passkey Storage**: 1M users × 2 devices × 1KB = 2GB\n- **Behavioral Data**: 100K users × 50 events/min × 200B = 600MB/hour\n- **Auth Throughput**: 100K logins/hour ÷ 3600s = 28 auth/sec\n\n## Implementation Details\n```typescript\n// WebAuthn registration flow\nasync function registerPasskey(userId: string) {\n  const challenge = await generateChallenge();\n  const credential = await navigator.credentials.create({\n    publicKey: {\n      challenge,\n      rp: { name: \"SecureConf\" },\n      user: { id: userId, name: userId, displayName: userId },\n      pubKeyCredParams: [{ alg: -7, type: \"public-key\" }]\n    }\n  });\n  return await storeCredential(userId, credential);\n}\n```\n\n## Follow-up Questions\n1. How would you handle passkey recovery when devices are lost?\n2. What behavioral metrics would you track for continuous authentication?\n3. How would you design fallback authentication for legacy systems?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-20",
        "questionId": "gh-24",
        "question": "What is DevSecOps and how does it differ from traditional DevOps security approaches?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Explain a secure, multi-tenant telemetry ingestion pipeline (devices -> ingest -> analytics -> storage). Describe tenant isolation, data leakage guards (encryption at transit/rest, per-tenant keys), t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a layered guard: set a max query depth (4–5), a cost model (max cost ~1000), and enable persisted queries to prevent ad-hoc bursts. Use a gateway (e.g., Apollo Gateway) to enforce these rule",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt a phased, backward-compatible PQ migration: keep ES256 while adding a PQ verifier and a hybrid sig path. Use per-tenant envelope keys in KMS with versioned data keys. Phase 1: tokens carry both",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "DevSecOps integrates security throughout the entire software development lifecycle by making security a shared responsibility embedded in CI/CD pipelines, rather than a separate final-stage gate.",
            "isCorrect": true
          }
        ],
        "explanation": "DevSecOps represents a cultural and technical shift where security becomes an integral part of development operations rather than an afterthought. Unlike traditional approaches where security teams review code just before deployment, DevSecOps implements automated security checks throughout the development process.\n\n**Key differences from traditional DevOps security:**\n- **Shift-left approach**: Security testing begins early in development, not just before production\n- **Automated security integration**: Security tools are embedded directly in CI/CD pipelines\n- **Shared responsibility**: Developers own security alongside their feature development\n- **Continuous monitoring**: Ongoing security assessment rather than periodic audits\n\n**Core DevSecOps principles:**\n- Security as code: Treating security policies and configurations as version-controlled artifacts\n- Infrastructure as code security: Automated validation of cloud resource configurations\n- Static and dynamic analysis: Code scanning integrated into build processes\n- Secret management: Automated detection and secure handling of credentials\n- Compliance as code: Automated verification against security standards and regulations\n- Continuous security testing: Regular penetration testing and vulnerability assessments",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-21",
        "questionId": "q-29126",
        "question": "Design a secure, multi-tenant data processing pipeline where each tenant's data is processed inside hardware enclaves (SGX or SEV). Outline per-tenant key management, remote attestation, data-in-use encryption, side-channel mitigations, monitoring, and a rollback plan for enclave compromise. Include concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use policy-as-code: add an OPA policy that blocks any Terraform plan that creates an AWS S3 bucket with a public access policy unless a specific tag PUBLIC=true is present. Enforce in CI (terraform va",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Detect anomalies in CMEK usage (cloud logs, HSM attestation). Immediately revoke affected CMKs, rotate regionally, and issue new KEKs. Re-encrypt all tenant data with fresh CMKs, updating envelope key",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Edge attestation with mutual TLS for secure ingestion; per-tenant encryption keys with envelope encryption; tenant-isolated data partitions; field-level PII masking for data minimization; tamper-evident logs using hash…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a multi-tenant data processing pipeline where each tenant's data is processed inside hardware enclaves such as Intel SGX or AMD SEV. Provision per-tenant keys at runtime from a cloud KMS (e.g., AWS KMS, Azure Key…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates understanding of end-to-end confidential computing, including tenant isolation, key lifecycle management, and incident response in cloud environments. It tests practical knowledge of hardware-based security rather than conceptual familiarity alone.\n\n## Key Concepts\n- **Confidential Computing**: Hardware enclaves (Intel SGX, AMD SEV-SNP) provide Trusted Execution Environments where data remains encrypted in use\n- **Per-Tenant Key Management**: Each tenant receives a unique encryption key provisioned from a KMS/HSM into their dedicated enclave using envelope encryption, ensuring tenant data is never exposed to the cloud provider\n- **Remote Attestation**: A cryptographic proof that the enclave runs approved code on trusted hardware; Intel DCAP enables cloud-native attestation while SEV-SNP provides AMD's equivalent mechanism\n- **Data-in-Use Encryption**: Memory encryption engines ensure CPU-resident data remains encrypted, protecting against memory introspection attacks\n- **Side-Channel Mitigations**: Hyper-threading disabling prevents cross-thread speculation leaks; constant-time crypto libraries prevent timing-based key extraction\n- **Rollback Protection**: Attested snapshots combined with key rotation enable rapid recovery without compromising previously processed data",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-22",
        "questionId": "q-6766",
        "question": "Design a secure end-to-end workflow for a distributed ML inference platform powering real-time recommendations on a streaming service (Nvidia GPUs, CPU nodes) where model artifacts are loaded from an internal registry. Explain how you would implement model signing, provenance with SBOM, reproducible builds, delta updates, hardware-backed attestation (TPM/HSM), per-node secure runtime (e.g., NVIDIA GPU acceleration), secure updates with rollback, and incident response playbooks. Include concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant IAM roles with least privilege; restrict s3 access to only the tenant bucket; move secrets to Secrets Manager and fetch at runtime; place Lambda in a VPC with private subnets and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enforce cache-key isolation: partition caches by tenant, derive keys from tenant_id, URL, method, and a hash of headers that affect content; reject or canonicalize Vary/Accept-Encoding; implement per-",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Sign the model artifact in CI with cosign, attach an SPDX SBOM, and enforce reproducible builds. Distribute delta updates via MTLS-protected channels, verify signatures at startup, and require TPM/HSM",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-partner short-lived JWTs signed with rotating keys (identified via `kid` in the header) and distributed through JWKS endpoints. Enforce strict claims validation including `iss` and `aud`, verify `exp` and…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis probes supply-chain security for ML inference across heterogeneous hardware, forcing defenses that span signing, SBOMs, and hardware attestation.\n\n## Key Concepts\n\n- Model signing (cosign/sigstore)\n- SBOM (SPDX) and reproducible builds\n- Delta updates, MTLS, and artifact integrity\n- TPM/HSM-based attestation and secure boot\n- Per-node runtime security and rollback\n\n## Code Example\n\n```javascript\n// Pseudo-verification at startup\nconst artifact = loadModel('registry/model.bin')\nif (!verifySignature(artifact, publicKey)) throw new Error('Bad sig')\nif (!verifySBOM(artifact)) throw new Error('SBOM fail')\n```\n\n## Follow-up Questions\n\n- How would you rotate keys across HSMs and verify revocation?\n- How would you test rollback under peak load?\n- How do you extend SBOM coverage to sub-dependencies?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-23",
        "questionId": "q-373",
        "question": "How would you implement a comprehensive defense-in-depth strategy to prevent SQL injection attacks in a modern web application following OWASP Top 10 guidelines?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable PostgreSQL Row-Level Security on tenant tables and set per-request tenant context in your API. **1. Database Setup** ```sql ALTER TABLE orders ENABLE ROW LEVEL SECURITY; ALTER TABLE orders FORCE ROW LEVEL SECURIT…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement parameterized queries with ORMs like Prisma/TypeORM, use input validation with libraries like Joi/Zod, apply least-privilege database principles, and enable query logging. Combine prepared statements, stored p…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Turn off DEBUG in production, register a global error handler returning a generic JSON error with status 500, and log sanitized messages by redacting secrets like password and token. Add a pytest test",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-user rate limiting with a Redis token bucket (e.g., 10 attempts per 15 minutes) and an account lockout after 5 consecutive failures within 15 minutes. Validate passwords with bcrypt using cons",
            "isCorrect": false
          }
        ],
        "explanation": "## Interview Context\nThis question tests your understanding of secure coding practices and defense-in-depth strategies for database security.\n\n## Key Points\n- **Primary Defense**: Parameterized queries prevent malicious SQL execution\n- **ORM Benefits**: Prisma, TypeORM, Sequelize provide built-in injection protection\n- **Input Validation**: Server-side validation using Joi/Zod before database operations\n- **Least Privilege**: Separate read/write users with minimal permissions\n- **Error Handling**: Generic error messages prevent database schema disclosure\n\n## Code Example\n```typescript\n// Secure approach with Prisma\nconst getUser = async (email: string) => {\n  const validatedEmail = emailSchema.parse(email);\n  return await prisma.user.findUnique({\n    where: { email: validatedEmail }\n  });\n};\n\n// Input validation schema\nconst emailSchema = z.string().email().max(255);\n```\n\n## Follow-up Questions\n1. How would you handle dynamic queries that require flexible conditions?\n2. What monitoring would you implement to detect potential injection attempts?\n3. How do you balance security requirements with application performance?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-24",
        "questionId": "q-15047",
        "question": "You're running a multi-tenant SaaS on Kubernetes with PostgreSQL, Redis, and a mixed Node/Go microservice stack; a bug allows cross-tenant data access via a shared Redis cache and mis-scoped sessions. Propose a concrete end-to-end fix plan for strict data isolation, per-tenant authorization, cache isolation, and incident response with rollback and forensics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Immediate Mitigation (Hotfix) 1. **Redis Cache Isolation**: Flush affected Redis instance, implement tenant-scoped keys with format `tenant:{tenant_id}:{resource}:{id}`, enable Redis ACLs or use separate Redis databa…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Enforce per-tenant namespaces; use eBPF/XDP to block anomalous egress (DNS/TLS to unapproved destinations); feed a Falco detector with tenant context; require runtime attestation of compute nodes via",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Validate incoming JSON against a strict schema (no extra properties), reject unknown keys, and sanitize all strings before storing or rendering. Log only redacted or sanitized content; use parameteriz",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a global error handler that returns a generic message; redact PII (emails, IDs, tokens) from logs and error payloads; switch to structured JSON logs with production-level verbosity and no st",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to translate a real multi-tenant breach into concrete mitigations spanning database, cache, authentication, and incident response - critical skills for platform/infrastructure engineers.\n\n## Key Concepts\n\n- **Data Isolation**: PostgreSQL Row-Level Security (RLS) provides database-enforced tenant isolation at the row level; RLS policies are invisible to application code and cannot be bypassed by application bugs\n- **Cache Isolation**: Shared Redis is high-risk; tenant-scoped key prefixes, ACLs, or dedicated Redis databases prevent cross-tenant cache poisoning\n- **Authorization**: Token-bound tenant claims with verification at every service hop; JWT should contain tenant_id in both `sub` and dedicated claim\n- **Incident Response**: Immutable logs (Kubernetes events, audit trails), atomic rollback capability, forensic snapshots at incident onset\n\n## Code Example\n\n```sql\n-- PostgreSQL RLS setup\nALTER TABLE orders ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY tenant_isolation_policy ON orders\n  FOR ALL\n  USING (tenant_id = current_setting('app.current_tenant_id', true)::uuid);\n\n-- Set tenant context in application (use SET LOCAL for transaction-scoped)\n-- In Node.js with node-postgres:\nawait client.query('SET LOCAL app.current_tenant_id = $1', [tenantId]);\n```\n\n```javascript\n// Redis key isolation (Node.js with ioredis)\nfunction getCacheKey(tenantId, resource, id) {\n  return `tenant:${tenantId}:${resource}:${id}`;\n}\n\n// JWT tenant binding verification\nfunction verifyTenantAccess(token, requestedTenantId) {\n  const decoded = jwt.verify(token, publicKey);\n  if (decoded.tenant_id !== requestedTenantId) {\n    throw new UnauthorizedError('Tenant mismatch');\n  }\n  return decoded;\n}\n```\n\n## Follow-up Questions\n\n- How would you verify no cross-tenant data flows under peak load?\n  - Answer: Continuous chaos engineering with tenant-injection fault testing, e2e tests that deliberately query wrong tenant IDs, Prometheus metrics tracking tenant-scoped query patterns\n\n- Which telemetry would you add to detect subtle leakage post-incident?\n  - Answer: PostgreSQL pgaudit logs for all SELECT queries, Redis MONITOR output analyzed for cross-tenant key patterns, Kubernetes network policies logging cross-pod communication, application-level tenant-aware access logs with tenant_id correlation",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-14T07:37:15.327Z",
    "version": 1
  }
]