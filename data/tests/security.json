[
  {
    "id": "test-security",
    "channelId": "security",
    "channelName": "Security",
    "title": "Security Knowledge Test",
    "description": "Test your understanding of Security with 25 questions.",
    "questions": [
      {
        "id": "security-q-0",
        "questionId": "q-22728",
        "question": "Scenario: A browser-extension component provides per-tenant encryption keys and signs sensitive API requests to the backend for a security dashboard used by tenants at LinkedIn, Square, and Instacart. If the extension is compromised, data exfiltration is possible. Design an end-to-end hardening plan covering: per-tenant hardware-backed keys, envelope encryption, extension signing and integrity checks, tenant-scoped key rotation and revocation, runtime attestation, tamper-evident logs, and an incident-response playbook with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a zero-trust, artifact-centric pipeline: sign models and telemetry using Sigstore/TUF with SBOMs; verify signatures at deployment via policy engines like OPA or Kyverno; run inference in confidential compute envi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Threat model: In a multi-tenant GPU inference service with confidential computing support, primary threats include memory-side channel attacks where one tenant may infer information from another tenant's GPU memory acce…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I'd design a zero-trust, multi-layer security plan: per-tenant envelope encryption with KMS/HSM-backed keys; mutual TLS with SPIFFE IDs for all clients; platform attestation (TEE/TPM) for client SDKs",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant hardware-backed keys, envelope encryption, extension signing and integrity checks, tenant-scoped key rotation and revocation, runtime attestation, tamper-evident logs, and an incident-r",
            "isCorrect": true
          }
        ],
        "explanation": "Why This Is Asked\n\nThis question probes practical client-side secret management, secure extension execution, and incident response coordination—not abstract theory.\n\nKey Concepts\n\n- Per-tenant hardware-backed keys and envelope encryption\n- Secure extension signing and integrity checks\n- Tenant-scoped key rotation and revocation\n- Runtime attestation from the extension to the backend\n- Tamper-evident append-only logs for extension activity\n- Incident response with revocation, key rotation, re-encryption, and rollback\n\nCode Example\n\n```javascript\n// Pseudocode for signing a request\nconst payload = JSON.stringify({tenantId, nonce, path});\nconst sig = signWithTenantKey(payload);\n// attach to header\n```\n\nFollow-up Questions\n\n- How would you detect a compromised extension in production?\n- What CI/CD tests would prevent regression and ensure key-rotation correctness?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-1",
        "questionId": "q-4753",
        "question": "Design a secure runtime attestation and supply-chain verification for a multi-cloud microservices platform spanning AWS, GCP, and on-prem edge. Each service builds from a central repo and uses third-party libraries. Propose concrete architecture and artifacts to prevent a compromised dependency from running in production, including build signing, runtime attestation, key management, revocation, and incident response. Mention tools and workflows (Sigstore, Rekor, Fulcio, OIDC, KMS)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a multi-region envelope encryption design with per-tenant data keys (DEKs) wrapped by a master key (CMK). Use AWS KMS or Vault with HSM-backed CMKs; rotate DEKs quarterly and CMKs every 12-24",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant namespaces and sandboxed runtimes; sign function packages with HSM-backed tenant keys and verify SBOM and manifest before deploy; require runtime attestation on cold start; issue short-",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Sigstore provenance: sign builds with per-service ephemeral keys, publish attestations to Rekor, require runtime attestation via a sidecar that verifies image hashes against Fulcio certificates an",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant enclaves with isolated sealing keys; require remote attestation before any processing; encrypt data at rest and in transit with per-tenant envelope keys stored in a hardware security module",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical expertise in modern software supply chain security, including cryptographic attestation, cross-cloud enforcement, and incident response.\n\n## Key Concepts\n- Verifiable provenance and attestations (Sigstore, Rekor, Fulcio)\n- Runtime attestation and per-service gating\n- Key management and rotation (KMS, short-lived credentials)\n- Revocation workflows and incident response\n- Observability of attestation events across environments\n\n## Code Example\n```javascript\n// Pseudo-verification sketch for an attestation\nfunction verifyAttestation(attestation, trustedRoot) {\n  // Validate signature against Fulcio certs\n  // Cross-check against Rekor entry and allow-list\n  // Enforce that hash and public keys match deployed artifact\n  return isValid;\n}\n```\n\n## Follow-up Questions\n- How would you scale attestation checks for thousands of services with minimal latency?\n- How do you handle legacy dependencies that can’t be signed or attested?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-2",
        "questionId": "q-18203",
        "question": "Scenario: in a multi-tenant SaaS, tenants upload data-processing plugins that run in a sandboxed WASM environment on a shared Kubernetes cluster. Design an end-to-end security plan: plugin signing via Vault, per-tenant identity, API surface minimization, per-tenant isolation and revocation, runtime attestation, tamper-evident logs, and incident response with rollback/tenant quarantine. Include concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant namespaces and sandboxed runtimes; sign function packages with HSM-backed tenant keys and verify SBOM and manifest before deploy; require runtime attestation on cold start; issue short-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Validate file type by magic bytes, not extension; enforce max size and per-type limits; quarantine uploads in an isolated container; run antivirus scans; store encrypted at rest; render via safe viewe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant WASM sandbox plan: plugins signed with Vault's Ed25519 keys; per-tenant identity bound to short-lived JWTs for API access; limit plugin host surface with a restricted API gateway;",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant envelope encryption with KMS: isolate keys by tenant, enforce strict IAM policies, route Encrypt/Decrypt through an admission service that validates tenant scope, cache data keys with short",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests end‑to‑end thinking for multi-tenant isolation, supply-chain security, and incident response in cloud-native apps.\n\n## Key Concepts\n- Sandbox boundaries: WASM vs. full VMs, surface minimization, and host permissions.\n- Identity & signing: Vault-managed keys, JWT binding, rotation.\n- Isolation: Namespaces, NetworkPolicies, PSP/PS Standards.\n- Attestation & logging: IMA/TEE, tamper-evident logs, immutable storage.\n- Incident response: Key revocation, tenant quarantine, rollback.\n\n## Code Example\n```javascript\n// Pseudo: verify plugin before load\nfunction verifyPlugin(blob, sig, pubKey){\n  return crypto.verify(blob, sig, pubKey);\n}\n```\n\n## Follow-up Questions\n- How would you detect drift between signed plugins and runtime policies?\n- How would you test revocation and rollback without affecting tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-3",
        "questionId": "q-14167",
        "question": "Context: You run a multi-tenant security service that loads and executes user-submitted security rules as WebAssembly modules inside a Kubernetes cluster. A malicious rule could try covertly read memory from other tenants or exfiltrate data through shared caches. Propose a concrete, implementable defense-in-depth plan to isolate tenants: sandboxing with Wasmtime/Wasmer and WASI, per-tenant memory/cpu limits, syscall filtering and sandboxed I/O, container runtimes with seccomp/AppArmor, per-tenant KMS/HSM keys, SBOM verification, attestation, and an incident playbook; include validation/tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant WASM sandboxes (Wasmtime) with bounded memory and CPU quotas, and run each in isolated containers with seccomp and AppArmor. Use WASI for IO isolation; disallow direct filesystem",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Require per-tenant HMAC-SHA256 signatures using rotated secrets stored in an HSM, include timestamp and nonce in the signature payload, verify at the edge with a strict replay window (e.g., 5 minutes), store nonces in a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a comprehensive defense-in-depth strategy: Sign all artifacts at every stage (training data, models, containers) using cosign with Sigstore; enforce reproducible builds via Bazel/Nix and generate SBOMs using C…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant envelope encryption with region-local KMS/HSMs; streams encrypted in transit (TLS) and at rest (AES-256-GCM); per-tenant keys rotated automatically; data locality enforced by region-bound s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical isolation strategies for multi-tenant WASM workloads, highlighting concrete controls and validation.\n\n## Key Concepts\n- WASM sandboxing (Wasmtime/Wasmer), WASI\n- Resource quotas, syscall filtering\n- Container security (seccomp/AppArmor)\n- Per-tenant KMS/HSM, SBOM, attestation\n- Validation/testing approaches\n\n## Code Example\n```javascript\n// Not provided for brevity\n```\n\n## Follow-up Questions\n- How would you validate no cross-tenant leakage in CI/CD?\n- What metrics indicate isolation failures?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-4",
        "questionId": "q-27803",
        "question": "You operate a cloud-based Secrets Store used by multiple tenants. Design a per-tenant envelope encryption scheme with KMS-backed KEKs, implement quarterly KEK rotation with re-wrapping of all DEKs, and ensure tamper-evident auditing and rapid key revocation for compromised tenants. Describe components, data flows, and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt per-tenant envelope encryption: each tenant gets a DEK used to encrypt secrets; a KEK in an HSM-backed KMS wraps each DEK. Rotate KEKs quarterly and rewrap DEKs. Maintain tamper-evident logs wit",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a log redaction layer that masks PII before logging, enforce per-tenant log streams (prefix by tenantId) so access is isolated, and ship logs to a separate encrypted sink (e.g., KMS-backed s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use OAuth 2.0 Authorization Code with PKCE for all third-party apps. Enforce HTTPS redirect URIs; issue short‑lived access tokens (15 min) with rotate-on-use refresh tokens bound to the client; store",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a strict WASM sandbox: limit syscalls, cap memory, enforce a narrow API surface, and reject any transformer attempting to breach boundaries. Isolate tenants via dedicated streams with strict data routing and netwo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical design of tenant isolation and cryptographic key lifecycle in a multi-tenant secret store. It probes data protection, incident response, and auditability in production.\n\n## Key Concepts\n- Envelope encryption with per-tenant DEKs\n- KMS/HSM-backed KEK lifecycle and re-wrapping\n- Tamper-evident, append-only audit logs\n- Token lifetimes, mTLS, rapid key revocation\n\n## Code Example\n\n```javascript\n// Pseudo: fetch DEK, decrypt with KEK from HSM, decrypt secret with DEK\n```\n\n## Follow-up Questions\n- How would you detect misuse of a KEK and automate revocation?\n- What are trade-offs of quarterly KEK rotation vs. monthly?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-5",
        "questionId": "q-23046",
        "question": "You're operating a multi-tenant AI inference platform where tenants upload their own model artifacts and data for shared GPU inference. A poisoned artifact could exfiltrate tenant data or poison other tenants' models. Propose an end-to-end security plan addressing provenance and signing of artifacts, hardware-backed loading attestation, per-tenant isolation, SBOM enforcement, runtime checks, tamper-evident logs, leakage/poisoning detection, credential management, and an incident-response playbook including rollback. Include concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Require image signing and attestation for all runtimes (Sigstore/Notary), immutable tamper-evident logs, and runtime attestation before execution (trusted compute base). Use a GitOps pipeline with aut",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "An end-to-end security plan for a multi-tenant AI inference platform should incorporate artifact signing and provenance verification using Sigstore or cosign, combined with hardware-backed attestation at load time via T…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use envelope encryption with per-tenant CMKs backed by an external KMS/HSM. Isolate each tenant in separate vaults, enforce least-privilege access, and rewrap data during key rotation with zero-downti",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant KEKs stored in an HSM, rotate every 4 hours; edge nodes perform TPM/Sgx attestation on boot and before executing each new policy; central policy service signs SBOMs and distributes via",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates end-to-end artifact security in a multi-tenant AI inference platform, testing understanding of the full security lifecycle from artifact ingestion through runtime monitoring and incident response.\n\n## Key Concepts\n- **Artifact provenance and cryptographic signing** ensure artifacts originate from trusted sources and have not been tampered with during storage or transit.\n- **Hardware-backed attestation** (TPM/HSM) verifies the integrity of the execution environment before loading tenant artifacts, ensuring that only trusted code runs on trusted hardware.\n- **Per-tenant isolation** with namespace segmentation prevents cross-tenant data leakage and ensures that malicious tenants cannot access or poison other tenants' models or data.\n\n## Technical Components\n- **Sigstore/cosign** for artifact signing and verification\n- **TPM 2.0 or AWS Nitro Enclaves** for hardware-backed attestation\n- **Kubernetes namespaces and network policies** for per-tenant isolation\n- **OPA/Gatekeeper** for policy enforcement\n- **SBOM tools** (Syft, SPDX) for dependency tracking\n- **Immutable logging** (e.g., AWS CloudTrail, Google Audit Logs) for tamper-evident audit trails\n- **HSM-backed key storage** (AWS KMS, HashiCorp Vault) for credential management\n\n## Trade-offs\n- Attestation checks introduce latency but significantly reduce attack surface\n- Multi-tenant key management adds operational complexity\n- SBOM maintenance requires ongoing effort at scale",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-6",
        "questionId": "q-29971",
        "question": "You operate a multi-tenant AI feature store where third-party models run as sandboxed plugins in a managed service. A compromised plugin could leak tenant data or corrupt outputs. Design an end-to-end security plan focusing on: runtime sandboxing (enclaves vs containers), per-tenant isolation, model signing and attestation, key management with HSMs, transport and data integrity, SBOM enforcement, and an incident-response rollback. Include concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement parameterized queries with ORMs like Prisma/TypeORM, use input validation with libraries like Joi/Zod, apply least-privilege database principles, and enable query logging. Combine prepared statements, stored p…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan to prevent leakage in a multi-tenant Snowflake data lake and a containerized ML training service. Include tenant isolation (RBAC/labels), differential privacy for training data, confidential comp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define a multi-layered chain: build-time SBOM with verifiable provenance; hardware-backed attestation for every edge image (TPM/SE); runtime integrity checks on startup and during updates; per-region",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use SGX/SEV enclaves or WASM sandbox with strict API exposure; enforce per-tenant namespaces; sign every model artifact with Sigstore cosign and require remote attestation to a trusted attestation ser",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests end-to-end security design for a multi-tenant AI plugin marketplace, balancing isolation, verifiable code, and rapid incident response.\n\n## Key Concepts\n- Attestation-based trust for runtime plugins\n- Per-tenant isolation and minimal API surface\n- Hardware-backed keys and signing with SBOM provenance\n- Tamper-evident logs and rapid revocation\n\n## Code Example\n```javascript\nfunction verifyAttestation(attestation, publicKey) {\n  const verify = crypto.createVerify('SHA256');\n  verify.update(attestation.payload);\n  return verify.verify(publicKey, attestation.signature, 'base64');\n}\n```\n\n## Follow-up Questions\n- How would you monitor for plugin behavior to detect data exfiltration without false positives?\n- How would you handle key rotation without breaking tenant workloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-7",
        "questionId": "q-3647",
        "question": "You're building a fintech service that talks to multiple partners via webhooks and mobile clients. Design a secure webhook verification and session management strategy using rotating keys (JWKS), strict claims (iss, aud, exp), nonce/replay protection, and automated key distribution. Compare HMAC vs RSA for signing, and outline zero-downtime key rotation and incident response steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use parameterized queries/prepared statements instead of string concatenation. Validate and sanitize all user inputs. Implement least privilege database access. Common vectors include login forms, search functionality,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant isolation via IAM roles, bucket prefixes, and VPC endpoints; enforce encryption at rest and in transit; deploy tenant-scoped keys and data provenance; add policy-as-code, DLP, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt a PKCE-enabled OAuth 2.0 flow with per-tenant issuer and audience, bind access tokens to tenant_id and client_id, and enforce short-lived access tokens (e.g., 15 minutes) with rotating refresh t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-partner short-lived JWTs signed with rotating keys (identified via `kid` in the header) and distributed through JWKS endpoints. Enforce strict claims validation including `iss` and `aud`, verify `exp` and…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to design end-to-end verification systems, key management infrastructure, and incident response procedures within a security-critical fintech environment.\n\n## Key Concepts\n\n- JWKS-based key rotation with `kid` header identification\n- Claims validation (`iss`, `aud`, `exp`, `iat`) for token integrity\n- Replay protection via nonce enforcement and timestamp windows\n- Webhook signing strategies (RS256 vs HS256) and their trade-offs\n- Incident response protocols and key revocation processes\n\n## Code Example\n\n```javascript\n// Pseudo verification sketch\nfunction verifyWebhook(token, jwksUrl, expectedIssuer, expectedAudience) {\n  // Decode JWT header to get kid\n  const header = JSON.parse(Buffer.from(token.split('.')[0], 'base64'));\n  const kid = header.kid;\n  \n  // Fetch JWKS and find matching key\n  const jwks = await fetch(jwksUrl).then(r => r.json());\n  const publicKey = jwks.keys.find(k => k.kid === kid);\n  \n  // Verify JWT signature and claims\n  const decoded = jwt.verify(token, publicKey, {\n    issuer: expectedIssuer,\n    audience: expectedAudience,\n    algorithms: ['RS256']\n  });\n  \n  // Validate nonce and replay protection\n  if (await isNonceUsed(decoded.nonce)) {\n    throw new Error('Replay attack detected');\n  }\n  \n  return decoded;\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-8",
        "questionId": "q-16721",
        "question": "Your Node.js/Express API exposes a file upload endpoint at /upload. Propose a beginner-friendly secure file upload workflow: validate size and allowed extensions, verify MIME types, stream uploads to a private cloud bucket (not local disk), run a malware scan, sanitize filenames, enforce access controls, and return a time-limited URL. How would you implement this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enforce end-to-end supply chain controls: require SBOMs and CVE scans in CI, sign artifacts with Cosign/Sigstore, pin to content digests, enforce reproducible builds, enable runtime attestation with O",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Sign and verify container images in CI/CD using Cosign and Rekor; generate an SBOM with Syft and fail CI on CVEs; enforce signing in Kubernetes with an OPA Gatekeeper policy that rejects unsigned imag",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Validate file size (e.g., max 5MB) and allowed extensions on the server; verify MIME types to ensure they match the expected types; stream uploads directly to a private cloud bucket without storing files on local disk;…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-namespace RBAC by restricting service accounts to their own namespace, removing any cluster-wide roles. Use Secrets Store CSI with Vault or AWS Secrets Manager to mount tenant-scoped sec",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses practical security hygiene for a common feature: file uploads. It demonstrates understanding of server-side validation, storage separation, malware scanning, and access control patterns.\n\n## Key Concepts\n\n- Server-side validation of size, extensions, and MIME types\n- Safe storage with obfuscated filenames\n- Malware scanning integration\n- Time-limited access URLs\n\n## Code Example\n\n```javascript\nconst storage = multer.memoryStorage();\nconst upload = multer({\n  limits: { fileSize: 5 * 1024 * 1024 },\n  fileFilter: (req, file, cb) => {\n    const allowed = ['image/png', 'image/jpeg', 'application/pdf'];\n    if (allowed.includes(file.mimetype)) cb(null, true);\n    else cb(new Error('Invalid file type'), false);\n  }\n});\n\napp.post('/upload', authenticate, upload.single('file'), async (req, res) => {\n  const safeName = `${Date.now()}-${crypto.randomUUID()}${path.extname(req.file.originalname)}`;\n  \n  const stream = Readable.from(req.file.buffer);\n  await bucket.upload(stream, { destination: safeName });\n  \n  await scanFile(bucketName, safeName);\n  \n  const [url] = await bucket.getSignedUrl({ action: 'read', expires: Date.now() + 3600000 });\n  res.json({ url });\n});\n```",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-9",
        "questionId": "q-241",
        "question": "How would you implement JWT authentication with RS256 signing and refresh token rotation to prevent token replay attacks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant isolation must be enforced at ingestion, storage, and compute. Use envelope encryption with tenant-scoped KEKs managed by a HSM-backed KMS, rotate keys regularly, and revoke on compromise.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Quarantine the affected image digest in the registry, halt pulls, and rollback to the last-good revision using blue/green deployment. Enforce image signing with Sigstore and admission control via OPA",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use RS256 for asymmetric signing, 15-min access tokens, rotating refresh tokens stored in httpOnly cookies, and invalidate old tokens on each refresh.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Adopt a secrets-first workflow: store API keys in Databricks Secret Scopes and fetch with dbutils.secrets.get; remove keys from notebooks and repos. Enforce least privilege on who can access scopes; use environment vari…",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nJWT authentication with RS256 provides asymmetric cryptography for enhanced security. Refresh token rotation prevents replay attacks by generating new refresh tokens on each use.\n\n## Implementation Details\n- **RS256 Algorithm**: Uses private/public key pair instead of shared secret\n- **Access Token**: 15-minute expiration, contains user ID and role\n- **Refresh Token**: 7-day expiration, stored in httpOnly secure cookie\n- **Token Rotation**: New refresh token issued each time, old one invalidated\n\n## Code Example\n```javascript\n// Generate JWT with RS256\nconst token = jwt.sign(\n  { userId: user.id, role: user.role },\n  privateKey,\n  { algorithm: 'RS256', expiresIn: '15m' }\n);\n\n// Refresh token rotation\napp.post('/refresh', async (req, res) => {\n  const oldRefreshToken = req.cookies.refresh_token;\n  const newTokens = await generateNewTokens(oldRefreshToken);\n  await invalidateRefreshToken(oldRefreshToken);\n  res.cookie('refresh_token', newTokens.refreshToken, {\n    httpOnly: true, secure: true, sameSite: 'strict'\n  });\n});\n```\n\n## Common Pitfalls\n- Using HS256 instead of RS256 (shared secret vulnerability)\n- Storing refresh tokens in localStorage (XSS risk)\n- Not implementing proper token rotation\n- Missing secure cookie flags\n- Forgetting to invalidate old refresh tokens",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-10",
        "questionId": "q-25566",
        "question": "Scenario: A cross-platform Electron app stores user notes locally and syncs with a cloud backend. Post-crash forensics show data stored unencrypted on disk. Propose a beginner-friendly plan to enforce encryption at rest and secure key management, using the OS keychain for keys, plus password-based key derivation. Include concrete steps and a minimal code snippet showing how to derive a key from a password and encrypt a local note file?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "AES-256-GCM combines Galois/Counter Mode encryption with authentication, providing 256-bit encryption and a 128-bit authentication tag in a single cryptographic operation.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a global error handler that returns a generic message; redact PII (emails, IDs, tokens) from logs and error payloads; switch to structured JSON logs with production-level verbosity and no st",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Argon2id to derive a per-user AES-256 key from the password; store the salt with the ciphertext and keep the key in the OS keychain. Encrypt each note with AES-GCM before writing to disk and decry",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Proposed end-to-end: edge plugins sandboxed per-tenant with WASM (or microVM for trusted plugins), loaded after HSM-signed verification; runtime attestation before execution; strict API surface; tampe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical, beginner-friendly encryption practices that map to real-world apps without backend changes.\n\n## Key Concepts\n- Encryption at rest with AES-GCM\n- Password-based key derivation (Argon2id)\n- OS-level key storage (Keychain/Keystore)\n- Key rotation and re-encryption on login\n\n## Code Example\n```javascript\n// Derive key from password and encrypt a note (simplified)\nconst crypto = require('crypto');\nfunction deriveKey(password, salt) {\n  // Argon2id in real code; use placeholder PBKDF2 for illustration\n  return crypto.pbkdf2Sync(password, salt, 100000, 32, 'sha256');\n}\nfunction encryptNote(key, plaintext) {\n  const iv = crypto.randomBytes(12);\n  const cipher = crypto.createCipheriv('aes-256-gcm', key, iv);\n  const enc = Buffer.concat([cipher.update(plaintext, 'utf8'), cipher.final()]);\n  const tag = cipher.getAuthTag();\n  return { iv: iv.toString('hex'), data: enc.toString('hex'), tag: tag.toString('hex') };\n}\n```\n\n## Follow-up Questions\n- How would you rotate keys with minimal downtime?\n- What telemetry would you collect to detect misuses of the keychain?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-11",
        "questionId": "q-24969",
        "question": "In a Python Flask API, production responses leak full stack traces on errors due to DEBUG being enabled during deployment. Propose a beginner-friendly, practical remediation: implement a centralized error handler, disable DEBUG, redact secrets in logs, and add a simple test to verify error responses do not expose sensitive fields. Include a minimal code snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement strict validation: check magic bytes for JPEG/PNG, confirm extension, reject executable mime types, cap size at 1 MB, save in a non-executable upload dir, use a randomized filename, sanitize",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a comprehensive secrets management strategy by migrating sensitive data like DB_PASSWORD to a secure vault service (AWS Secrets Manager or HashiCorp Vault), eliminating secrets from code repositories and CI co…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Turn off DEBUG in production, register a global error handler returning a generic JSON error with status 500, and log sanitized messages by redacting secrets like password and token. Add a pytest test",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Sigstore provenance: sign builds with per-service ephemeral keys, publish attestations to Rekor, require runtime attestation via a sidecar that verifies image hashes against Fulcio certificates an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes practical guards against data leakage in error handling, a common beginner pitfall in real apps.\n\n## Key Concepts\n- Centralized error handling\n- Production-ready logging sanitization\n- Negative tests for leak prevention\n- Minimal, verifiable code\n\n## Code Example\n```python\n# Minimal Flask error handling\nfrom flask import Flask, jsonify\napp = Flask(__name__)\napp.config['DEBUG'] = False\n\n@app.errorhandler(Exception)\ndef handle(e):\n    app.logger.error(str(e))\n    return jsonify({'error': 'Server error'}), 500\n```\n\n## Follow-up Questions\n- How would you extend this to handle different exception types with user-friendly messages?\n- What logging framework or middleware would you add for structured, redacted logs across services?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-12",
        "questionId": "q-10098",
        "question": "In a multi-tenant SaaS written with Node.js and Express that allows users to upload files for processing, a red-team test shows that a crafted file path can cause the server to execute code due to unsafe handling. Propose a concrete, beginner-friendly plan to prevent remote code execution via uploads, detailing validation, storage, access controls, and testing steps for a typical Express stack?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enforce a whitelist: allow only safe mime types/extensions, verify content with content detection (file-type), reject anything executable, store uploads outside the webroot in a sandboxed location wit",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose an end-to-end secure supply chain: sign/verify every image with Sigstore/cosign; generate SBOMs and enforce reproducible builds; use per-tenant envelope encryption with KMS and short-lived cre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt a strict sandboxed plugin model: run each plugin in a WASM sandbox per-tenant with a denied host API surface; expose only a minimal plugin API and enforce namespace isolation. Require plugins to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant API keys with versioned signing (v1/v2) using a two-phase rollout: emit v2 keys while maintaining v1 during a grace period, validate both versions via JWKS, then decommission v1 after migration.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nRationale: practical hardening of file uploads in common web stacks; beginner-friendly but requires concrete steps.\n\n## Key Concepts\n\n- File validation with MIME and content checks\n- Storage isolation and permissions\n- Sandboxing and least privilege\n- Post-upload scanning and auditing\n\n## Code Example\n\n```javascript\n// Express Multer config example\nconst upload = multer({\n  dest: '/var/app/uploads',\n  limits: { fileSize: 1024 * 1024 },\n  fileFilter: (req, file, cb) => {\n    const allowed = ['image/png','image/jpeg','application/pdf'];\n    if (allowed.includes(file.mimetype)) cb(null, true);\n    else cb(new Error('Invalid file type'), false);\n  }\n});\n```\n\n## Follow-up Questions\n\n- How would you handle MIME spoofing and double extension tricks?\n- How would you test security controls in CI/CD?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-13",
        "questionId": "q-9041",
        "question": "In a multi-tenant AI inference platform using confidential computing enclaves (e.g., SGX/SEV), tenants upload models and data executed in per-tenant enclaves. Propose a practical security architecture to prevent data leakage via side channels and shared IO, covering: enclave attestation, per-tenant key management with envelope encryption, secure updates, tamper-evident logging, and runtime anomaly detection. Include concrete technologies, trade-offs, and a brief threat model plus an example handshake snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Turn off DEBUG in production, register a global error handler returning a generic JSON error with status 500, and log sanitized messages by redacting secrets like password and token. Add a pytest test",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant isolation must be enforced at ingestion, storage, and compute. Use envelope encryption with tenant-scoped KEKs managed by a HSM-backed KMS, rotate keys regularly, and revoke on compromise.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To prevent data leakage, design around confidential computing enclaves (SGX/SEV) with per-tenant sealed keys and envelope encryption for model/data, remote attestation to a policy service, signed mani",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-tenant CMKs in a hardware-backed KMS and envelope-encrypt data with per-tenant data keys cached with short TTL. Rotate CMKs with zero-downtime re-wrapping of all data keys. Enforce least privi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical confidential computing design for multi-tenant workloads, addressing isolation, attestation, secure updates, and runtime monitoring.\n\n## Key Concepts\n\n- Confidential computing enclaves (SGX/SEV)\n- Per-tenant key management with envelope encryption\n- Remote attestation and policy enforcement\n- Measured boot and secure updates\n- Tamper-evident logging and anomaly detection\n\n## Code Example\n\n```javascript\n// Attestation verification sketch\nfunction verifyAttestation(report, publicKey, nonce) {\n  const valid = verifySignature(report.signature, report.measurements, publicKey);\n  if (!valid) throw new Error('Invalid attestation');\n  if (report.nonce !== nonce) throw new Error('Nonce mismatch');\n  return report.measurements === 'EXPECTED_MEASUREMENTS';\n}\n```\n\n## Follow-up Questions\n\n- How would you quantify and mitigate the performance overhead of attestation and side-channel mitigations at scale? \n- What telemetry would you collect to detect data exfiltration without exposing tenant data, and how would you store and protect those signals?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-14",
        "questionId": "q-8504",
        "question": "In a high-scale chat platform (Discord-like), design a secure data exchange with a third-party moderation service for user-generated content. The design should ensure privacy of chat payloads, integrity/authenticity of data, replay protection, and robust incident response. Include data minimization, encryption in transit and at rest, request/response signing, token binding, per-tenant keys, key rotation, access controls, and auditability. Provide concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a privacy-preserving, consented data path: only forward minimal content (redacted text/metadata). Use mTLS between chat service and moderation API, and sign requests with Ed25519; responses sign",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement device-bound passkeys with short-lived JWTs, rate limiting, and distributed caching. Use WebAuthn with hardware-backed keys and nonce-based request signing.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Remove the hard-coded API key and implement OAuth 2.0 PKCE for mobile apps (or short-lived tokens with refresh tokens). Route auth through an API gateway that validates JWTs, enforce audience/issuer c",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage per-tenant envelope encryption with keys in a KMS (AWS KMS/GCP KMS) and data-at-rest encryption for MongoDB Atlas and Delta Lake. Apply field-level encryption for PII, TLS/mTLS between servic",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design secure, external-service integration under scale, privacy constraints, and incident handling. Encourages concrete choices rather than abstractions.\n\n## Key Concepts\n\n- Data minimization and privacy by design\n- mTLS and request/response signing (Ed25519)\n- Envelope encryption with per-tenant keys and regular rotation\n- Replay protection via nonces/TLL and revocation paths\n- RBAC, auditability, and incident response playbooks\n\n## Code Example\n\n```javascript\n// Example: sign a moderation request\nconst payload = JSON.stringify({content: \"user text\", metadata: {tenant: \"t1\"}});\nconst encoder = new TextEncoder();\nconst sig = await crypto.subtle.sign({name: 'NODE-ED25519'}, key, encoder.encode(payload));\n```\n\n## Follow-up Questions\n\n- How would you rotate keys without downtime across services?\n- How would you verify third-party service integrity and revoke access quickly?\n- How would you test privacy risks with synthetic content and redaction rules?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-15",
        "questionId": "q-23314",
        "question": "In a microservices ecommerce app, payment requests travel from API gateway to a payment service. A JWT carries tenant_id; a misconfiguration could allow cross-tenant data leakage if tenant_id isn't strictly bound to the request path. As a beginner, outline a concrete plan to enforce tenant isolation at gateway and services, including token binding, header propagation, and tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enforce cache-key isolation: partition caches by tenant, derive keys from tenant_id, URL, method, and a hash of headers that affect content; reject or canonicalize Vary/Accept-Encoding; implement per-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Validate tenant binding at every hop: require the gateway to bind tenant_id from JWT to the request path or body and reject if mismatch. Enforce claims validation, audience, and issuer; drop tokens th",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement envelope encryption with per-tenant DEKs wrapped by regional KMS/HSM master keys. Enforce strict access control using short-lived credentials, tenant- and region-scoped IAM policies, and network segmentation t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use short-lived OAuth2 tokens issued by an authorization server (OIDC). Bind tokens to tenant, scope, and a limited IP range; issue JWTs with aud, iss, iat, exp, jti; protect via JWKS; enable token in",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to enforce multi-tenant isolation in a practical gateway-service flow.\n\n## Key Concepts\n\n- JWT validation with issuer, audience, and tenant_id checks\n- Request binding: ensure tenant from token matches tenant in path/header\n- Header propagation and per-tenant RBAC across services\n\n## Code Example\n\n```javascript\n// Middleware example: verify tenant binding\nfunction verifyTenantBinding(req, res, next) {\n  const tokenTenant = req.user?.tenant_id\n  const pathTenant = req.headers['x-tenant-id'] || req.params.tenantId\n  if (!tokenTenant || tokenTenant !== pathTenant) {\n    return res.status(403).send('Tenant mismatch')\n  }\n  next()\n}\n```\n\n## Follow-up Questions\n\n- How would you test this with automated tests for boundary conditions?\n- How would you adapt this approach for non-JWT token schemes or service mesh enforcement?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-16",
        "questionId": "q-24864",
        "question": "You operate a multi-tenant security analytics platform where tenants publish custom rules that run in a sandboxed environment against their own data streams. A compromised rule or misconfigured access could exfiltrate data across tenants. Propose an end-to-end hardening plan covering: per-tenant data isolation, key management, sandbox API surface minimization, runtime attestation, tamper-evident logs, and an incident-response playbook including rollback and forensics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement tenant-scoped data partitions with per-tenant keys, enforce mTLS/SPIFFE identities, and isolate rules sandbox (WASM) with a minimal API surface. Apply envelope encryption for logs, and tampe",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose an end-to-end plan: enforce per-tenant namespaces and ACLs on the event bus, use envelope encryption with customer-managed keys and per-tenant rotation, mandate tenant-prefixed topics, impleme",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a multi-region envelope encryption design with per-tenant data keys (DEKs) wrapped by a master key (CMK). Use AWS KMS or Vault with HSM-backed CMKs; rotate DEKs quarterly and CMKs every 12-24",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a PKCE-enabled OAuth 2.0 flow with per-tenant issuer and audience, bind access tokens to tenant_id and client_id, and enforce short-lived access tokens (e.g., 15 minutes) with rotating refresh t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nUnderstanding real-world tenant isolation, cryptographic key management, and incident response in a sandboxed multi-tenant pipeline.\n\n## Key Concepts\n- Tenant isolation, per-tenant keys, envelope encryption, runtime attestation, SBOM, tamper-evident logs.\n- Sandboxed rules using WASM with restricted APIs and strict surface area.\n\n## Code Example\n```javascript\n// Pseudocode: hash chaining for tamper-evident logs\nfunction hashLog(prevHash, record) {\n  const json = JSON.stringify(record);\n  const data = prevHash + json;\n  return crypto.createHash('sha256').update(data).digest('hex');\n}\n```\n\n## Follow-up Questions\n- How would you test tenancy boundaries under high throughput?\n- What trade-offs exist between sandbox isolation and latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-17",
        "questionId": "q-11328",
        "question": "In a globally distributed edge platform serving per-tenant features, design a secure, scalable secret-rotation workflow for edge runtimes that fetch per-tenant credentials from a central vault. Requirements: immediate revocation, HSM-backed master keys, per-tenant isolation, zero-downtime rotation, audit trails, and canary rollout. What would you implement and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-device cryptographic keys secured within iOS Secure Enclave and Android StrongBox, coupled with remote attestation to a centralized verification service. Employ envelope encryption using cloud HSM-protecte…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant isolation via IAM roles, bucket prefixes, and VPC endpoints; enforce encryption at rest and in transit; deploy tenant-scoped keys and data provenance; add policy-as-code, DLP, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Sign the model artifact in CI with cosign, attach an SPDX SBOM, and enforce reproducible builds. Distribute delta updates via MTLS-protected channels, verify signatures at startup, and require TPM/HSM",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design per-tenant secrets with envelope encryption: edge stores secrets encrypted with a per-tenant data key; data keys are wrapped by an HSM-backed master key in a central vault and rotated on schedu",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical secure secret management at edge scale, covering rotation, revocation, and trust boundaries.\n\n## Key Concepts\n- Envelope encryption with per-tenant data keys\n- HSM-backed master key management\n- Per-tenant isolation and revocation\n- Canary deployments and rollback\n- mTLS with SPIFFE/SPIRE identities\n\n## Code Example\n```javascript\n// Example: decrypt tenant secret using data-key from envelope\nasync function getTenantSecret(tenantId){\n  const wrappedKey = await vault.unwrapDataKey(tenantId);\n  const dataKey = await hsm.unwrap(wrappedKey);\n  const secretEnc = await cache.get(tenantId);\n  return decrypt(secretEnc, dataKey);\n}\n```\n\n## Follow-up Questions\n- How would you monitor for key-compromise and accelerate revocation?\n- What SLAs and metrics would you track to avoid rotation-induced downtime?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-18",
        "questionId": "q-19225",
        "question": "Scenario: You run a multi-tenant data platform where sensitive fields are encrypted at rest with per-tenant keys, and BI tools access anonymized data. A rogue BI token is exfiltrating data across tenants. Design an end-to-end containment and forensics plan: implement tenant-aware envelope encryption, per-tenant keys with automatic rotation, dynamic data masking in analytics, tamper-evident logging, and a 24-hour runbook to detect, contain, and audit the breach. Include concrete steps, tools, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement dual-layer rate limiting: 1) per-IP window of 15 minutes with a cap (e.g., 100 requests), 2) per-username limiter with a 10-minute window. After 5 failed logins, enforce a 15-minute lockout",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a TUF-based update framework with per-device attestation via TPM/Secure Element, delivering cryptographically signed delta payloads to edge devices. Utilize A/B partitioning for atomic over-the-air updates wit…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a tenant-aware envelope encryption with per-tenant keys, enforce Row-Level Security, and add dynamic data masking for analytics. When a BI token is compromised, automatically rotate keys, revok",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant envelope encryption with KMS: isolate keys by tenant, enforce strict IAM policies, route Encrypt/Decrypt through an admission service that validates tenant scope, cache data keys with short",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question tests practical incident response, cryptography hygiene, and data governance in multi-tenant contexts.\n\n## Key Concepts\n- Envelope encryption and per-tenant keys\n- Row-Level Security and dynamic masking\n- Tamper-evident logs and WORM storage\n- Automated incident runbooks and key rotation\n\n## Code Example\n```python\n# Pseudo: rotate tenant keys and invalidate tokens\nrotate_tenant_key(tenant_id)\nrevoke_all_tokens(tenant_id)\n```\n\n## Follow-up Questions\n- How would you validate no residual data leakage after containment?\n- What telemetry would you collect to improve detection without increasing privacy risk?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-19",
        "questionId": "q-12480",
        "question": "In a global log analytics platform handling telemetry for 100s of tenants, design a tenant-isolated storage and query path that prevents cross-tenant data leakage even if worker nodes are compromised. Describe per-tenant envelope encryption with a KMS, data-at-rest and in-flight protections, tenant-scoped access controls, and query isolation via row-level or column-level filters. Include auditability, key rotation, and revocation steps, concrete technologies, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Define a multi-layered chain: build-time SBOM with verifiable provenance; hardware-backed attestation for every edge image (TPM/SE); runtime integrity checks on startup and during updates; per-region",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant envelope encryption using a KMS with per-tenant DEKs wrapped by a master key; isolate data in tenant-specific partitions; enforce query isolation with row-level security (PostgreSQL) or fil",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement pre-commit secret scanning (git-secrets), use a central vault (HashiCorp Vault or cloud secret manager) with per-service roles, inject at runtime via CI/CD, avoid hard-coding. Use short-live",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement tenant-scoped keys in a KMS with envelope encryption, require attestation of confidential enclaves prior to inference, and record provenance hashes in a tamper-evident ledger. Isolate tenant",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to reason about multi-tenant isolation under compromise, cryptography, and operational guardrails. Evaluates practical design choices, threat modeling, and concrete technologies that mitigate data leakage while enabling scalable analytics.\n\n## Key Concepts\n\n- Tenant isolation and data partitioning\n- Envelope encryption and KMS key management\n- In-flight protections (TLS/mTLS)\n- Row-level and column/filters for query isolation\n- Access controls (RBAC/ABAC) and policy engines (OPA)\n- Tamper-evident auditing and SBOM considerations\n- Key rotation, revocation, incident response trade-offs\n\n## Code Example\n\n```sql\nALTER TABLE logs ENABLE ROW LEVEL SECURITY;\nCREATE POLICY tenant_isolation ON logs\n  USING (tenant_id = current_setting('tenant.id')::int);\n```\n\n## Follow-up Questions\n\n- How would you test tenant isolation in CI/CD pipelines? \n- What are the trade-offs between per-tenant keys vs shared keys in a multi-tenant platform?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-20",
        "questionId": "q-16328",
        "question": "A public REST API behind a CDN uses CORS with wildcard origins. A security test shows potential data leakage via cross-origin requests to user endpoints. Propose a beginner-friendly remediation plan: lock down allowed origins to known clients, remove wildcard, disallow credentials for non-whitelisted origins, add Vary: Origin, split public vs private endpoints, and implement a CI gate that fails on wildcard or untrusted origins. Include how you'd test?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Restrict CORS origins to known client applications, remove the wildcard configuration, and disable credentials for any non-whitelisted origins. Add the Vary: Origin header to prevent caching issues. Separate public and…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a defense-in-depth strategy spanning ingestion, storage, and access layers. Enforce per-tenant namespaces for both data and metadata to guarantee strict isolation. Validate all ingested records against strict,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enforce tenant isolation with hardware TEEs (Intel TDX/AMD SEV) for model runtimes; disable memory dedup; use per-tenant keys and envelope encryption; partition GPU memory or use confidential GPUs; enforce encrypted dat…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt an external secret store (e.g., AWS Secrets Manager) with Kubernetes Secrets Store CSI to mount secrets. Enforce per-service RBAC, rotate credentials automatically, and log access for audit. Do",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nCORS misconfigurations are a common and dangerous vulnerability, especially for public APIs. This question tests practical, beginner-friendly remediation steps that reduce data leakage without requiring a complete architecture overhaul.\n\n## Key Concepts\n- CORS origin validation and credential handling\n- Vary: Origin header and caching implications\n- Endpoint scoping (public vs. private data)\n- CI gates for configuration validation\n- Simple cross-origin testing\n\n## Code Example\n```javascript\nconst allowedOrigins = ['https://client.example.com', 'https://admin.example.com'];\n\napp.use(cors({\n  origin: (origin, callback) => {\n    if (!origin) return callback(null, true);\n    if (allowedOrigins.includes(origin)) {\n      return callback(null, true);\n    }\n    return callback(new Error('Not allowed by CORS'), false);\n  },\n  credentials: true\n}));\n```\n\n## How to Test\n1. **Verify whitelisted origins work:** Send requests from allowed domains and confirm successful responses with access-control-allow-origin header\n2. **Test blocked origins:** Attempt requests from untrusted domains and verify they are rejected\n3. **Check credentials mode:** Confirm that credentials (cookies, auth headers) are only sent with whitelisted origins\n4. **Validate Vary: Origin header:** Ensure responses include this header to prevent caching issues\n5. **CI gate test:** Run a script that scans configuration files for wildcard origins (*) and fails the build if detected",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-21",
        "questionId": "q-11109",
        "question": "Scenario: A global, multi-tenant real-time video moderation service runs across edge devices and cloud. Ingested content must be processed with tenant isolation, auditable logs, and quick revocation of compromised keys. Propose an end-to-end security design covering: data-in-transit and at-rest protections, KMS/HSM key management, per-tenant encryption, secure enclaves for inference, model/signature updates, SBOM checks, and incident playbooks. Include concrete components, data flow, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Argon2id to derive a per-user AES-256 key from the password; store the salt with the ciphertext and keep the key in the OS keychain. Encrypt each note with AES-GCM before writing to disk and decry",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an end-to-end secure data pipeline for a multi-tenant real-time video moderation service across edge and cloud. Include per-tenant envelope encryption with keys in an HSM, TEEs (SGX/Nitro) for",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement per-tenant isolation via IAM roles, bucket prefixes, and VPC endpoints; enforce encryption at rest and in transit; deploy tenant-scoped keys and data provenance; add policy-as-code, DLP, and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a comprehensive zero-trust edge security architecture by assigning SPIRE identities to all components—edge proxies, origin services, and client applications—while enforcing mutual TLS authentication across eve…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to architect end-to-end security for a high-velocity, multi-tenant workload with heterogeneous runtimes (edge + cloud). It probes cryptography, supply chain integrity, runtime isolation, and incident readiness.\n\n## Key Concepts\n\n- End-to-end encryption and per-tenant keys\n- Hardware security modules (HSM) and envelope encryption\n- Trusted execution environments for ML inference\n- Signed model updates and SBOM verification\n- Identity and access with SPIFFE/mTLS\n- Tamper-evident logging and rapid key rotation\n\n## Code Example\n\n```javascript\n// Pseudo: envelope encrypt a tenant blob using KMS and per-tenant key\nasync function encryptForTenant(tenantId, plaintext){\n  const key = await kms.getTenantKey(tenantId); // HSM-backed\n  const envelope = await crypto.kmsEncrypt(key, plaintext);\n  return envelope;\n}\n```\n\n## Follow-up Questions\n\n- How would you implement tenant key rotation without service interruption?\n- What metrics and audit controls would validate SBOM integrity in updates?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-22",
        "questionId": "q-5699",
        "question": "In a multi-cloud, real-time chat platform deployed across AWS and GCP, a rogue base image was introduced in CI. Draft a practical plan to prevent supply-chain attacks and detect tampering in production. Include: image signing/SBOM verification, runtime attestation and policy enforcement, deployment-time controls with canaries, and PII privacy safeguards. Name concrete tools and steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a layered defense: enforce container image provenance with Sigstore/Cosign, require SBOM verification in CI and at runtime, deploy via immutable registries with admission controls (OPA) and",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-tenant envelope encryption: generate a per-tenant DEK wrapped by a master KMS; edge workers fetch DEKs via attestation. Use runtime attestation (e.g., Intel TDX/AWS Nitro) for edge instances and s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant sandbox using WASM with a limited API surface; isolate plugins in lightweight containers or user namespaces; enforce model provenance via signing in a trusted registry; require run",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant WASM sandbox plan: plugins signed with Vault's Ed25519 keys; per-tenant identity bound to short-lived JWTs for API access; limit plugin host surface with a restricted API gateway;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nA real-world, cross-cloud supply-chain threat requires concrete controls at build, deployment, and runtime, plus privacy safeguards.\n\n## Key Concepts\n- Container image provenance and SBOM verification\n- Runtime attestation and policy enforcement\n- Canary deployments and safe rollback\n- PII handling and privacy considerations\n\n## Code Example\n```javascript\n// Pseudo-check for image policy (SBOM+signature)\nfunction isImageCompliant(image) {\n  return image.signature != null && image.sbom && image.sbom.version >= 2;\n}\n```\n\n## Follow-up Questions\n- How would you automate SBOM generation and rotation across CI/CD?\n- What telemetry would you collect to detect supply-chain anomalies without leaking PII?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-23",
        "questionId": "q-29392",
        "question": "Propose an end-to-end supply chain and runtime security plan for a Kubernetes-based API gateway handling multi-tenant financial data. How would you prevent malicious container images from reaching production and detect runtime compromises? Include image signing with Cosign, SBOM generation, CVE scanning, admission controls (OPA/Gatekeeper), per-namespace RBAC/PodSecurity, runtime attestation, and a rollback/kill-switch incident response. Name concrete tools and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement end-to-end image provenance and runtime controls. Sign all container images with Cosign to cryptographically verify integrity and prevent tampering. Generate SBOMs and scan for CVEs during CI to surface known…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a hardware root of trust per vehicle (TPM2.0/Edge SEP) with measured boot and a secure OTA agent. Sign each plugin with per-device keys; validate the full certificate chain and SBOM before load; p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant CMKs in a hardware-backed KMS and envelope-encrypt data with per-tenant data keys cached with short TTL. Rotate CMKs with zero-downtime re-wrapping of all data keys. Enforce least privi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a multi-tenant architecture for WASM-based UDFs with strict data isolation. Use per-tenant KEKs encrypted by a central KMS (envelope encryption), rotate keys monthly, sign UDF artifacts, requir",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nMulti-tenant fintech applications face significant supply-chain and runtime risks. This question tests your practical ability to design for image provenance, policy-driven deployments, and runtime defense, as well as your understanding of incident response procedures.\n\n## Key Concepts\n\n- **Image signing with Cosign** ensures container integrity and authenticity by cryptographically verifying that deployed images haven't been tampered with\n- **SBOM generation and CVE scanning** provide vulnerability visibility before deployment, allowing teams to catch known exploits in third-party dependencies\n- **OPA/Gatekeeper admission controllers** enable policy enforcement at deployment time, blocking resources that don't meet security requirements\n- **Per-namespace RBAC and Pod Security Standards** enforce tenant isolation and limit the blast radius of potential breaches\n- **Runtime detection (Falco)** monitors for suspicious behavior such as unexpected network connections, privilege escalation, or file system modifications\n- **Automated rollback and kill-switches** ensure rapid incident response, minimizing exposure time when a compromise is detected\n\n## Trade-off Considerations\n\nImplementing comprehensive security controls introduces latency into CI/CD pipelines and requires ongoing maintenance expertise. Teams must balance security rigor with developer productivity, prioritizing controls based on risk assessment and regulatory requirements specific to financial data handling.",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-24",
        "questionId": "q-10294",
        "question": "You run a PCI‑compliant, multi‑tenant payment service used by merchants. A recent upgrade introduced a supply‑chain risk delivering a container with hidden code to exfiltrate tokens. Propose a concrete plan to harden the software supply chain and runtime controls, covering: SBOMs, reproducible builds, image signing/attestation (cosign/Sigstore), CI gating, per‑tenant isolation in Kubernetes, runtime policy (OPA/ Gatekeeper), runtime security (eBPF/Falco), tamper‑evident logs, and an incident‑response playbook with rollback and forensics. Include tools, deployment steps, and trade‑offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a comprehensive software supply chain hardening strategy with the following components: **Supply Chain Controls:** - Generate and verify SBOMs for all artifacts using SPDX format with automated vulnerability s…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Require signed plugin attestations (Sigstore/cosign) plus SBOMs; verify at load against a trusted root; sandbox plugins with WASM engine (Wasmtime) with per-plugin quotas and sandboxed syscalls; isola",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-partner short-lived JWTs signed with rotating keys (identified via `kid` in the header) and distributed through JWKS endpoints. Enforce strict claims validation including `iss` and `aud`, verify `exp` and…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: implement short-lived JWTs with a refresh flow; enforce per-route authorization via scopes; validate inputs with Joi to reject extra fields and enforce length; rate-limit by IP and user with Red",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end security from CI/CD through runtime for PCI workloads, focusing on supply chain integrity and rapid forensics capabilities.\n\n## Key Concepts\n\n- Software supply chain security and zero-trust principles\n- SBOM generation, attestation, and image signing with Cosign/Sigstore\n- CI/CD pipeline gates and policy-as-code enforcement\n- Kubernetes per-tenant isolation and network segmentation\n- Runtime policy engines using OPA/Gatekeeper\n- Runtime security monitoring with eBPF and Falco\n- Tamper-evident logging and digital forensics\n- Automated incident response with rollback and containment strategies\n- PCI compliance requirements for payment processing systems",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-28T06:19:06.708Z",
    "version": 1
  }
]