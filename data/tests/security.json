[
  {
    "id": "test-security",
    "channelId": "security",
    "channelName": "Security",
    "title": "Security Knowledge Test",
    "description": "Test your understanding of Security with 25 questions.",
    "questions": [
      {
        "id": "security-q-0",
        "questionId": "q-15369",
        "question": "You're running a confidential ML inference platform in public clouds for multiple tenants. Tenants submit models and data, sharing GPUs and host OS. An attacker may attempt cross-tenant data leakage via memory side-channels or misconfigured enclaves. Propose a concrete end-to-end security design: TEEs (TDX/SEV), disable memory dedup, data handling, monitoring, and incident response with tooling and trade-offs. How would you implement it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enforce tenant isolation with hardware TEEs (Intel TDX/AMD SEV) for model runtimes; disable memory dedup; use per-tenant keys and envelope encryption; partition GPU memory or use confidential GPUs; enforce encrypted dat…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement multi-layered validation using JSON Schema with strict whitelisting, recursive nested object validation, type and range constraints, and input sanitization at each level.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant keys stored in an HSM to sign outbound payloads and verify them at ingress, enforce nonce+timestamp replay protection, and minimize endpoints per tenant. Implement tamper-evident loggin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a strict WASM sandbox: limit syscalls, cap memory, enforce a narrow API surface, and reject any transformer attempting to breach boundaries. Isolate tenants via dedicated streams with strict data routing and netwo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical confidential computing design for multi-tenant ML workloads, specifically addressing side-channel attacks, enclave misconfigurations, and cross-tenant data leakage vectors. It evaluates the candidate's understanding of hardware security primitives, threat modeling for shared infrastructure, and the ability to balance security with operational requirements.\n\n## Key Concepts\n\n- **Hardware TEEs and Attestation**: Intel TDX and AMD SEV-SNP provide hardware-enforced isolation with remote attestation to verify the runtime environment's integrity before granting access to sensitive data.\n- **Side-Channel and Memory Deduplication Mitigations**: Disabling memory deduplication prevents cross-tenant information leakage via memory page collision attacks; memory encryption prevents physical tampering.\n- **Per-Tenant Key Management and Envelope Encryption**: Each tenant receives dedicated encryption keys with envelope encryption for data-at-rest, ensuring tenant data remains logically isolated even on shared infrastructure.\n- **Auditability and Tamper-Evident Logs**: Immutable audit trails using append-only storage or blockchain-based logging ensure all data access and model executions are traceable for compliance and forensics.\n- **Incident Response, Forensics, and Rollback**: Automated containment, forensic data collection, and tenant-specific rollback mechanisms enable rapid recovery from security incidents.\n\n## Code Example\n\n```javascript\n// Pseudo attestation flow for confidential ML inference\nasync function attestRuntime(tenantId, attestationQuote) {\n  // 1. Verify TEE attestation quote from hardware\n  const attestationResult = await verifyAttestationQuote(attestationQuote);\n  \n  if (!attestationResult.isValid) {\n    throw new Error('Attestation failed: runtime integrity compromised');\n  }\n  \n  // 2. Verify measured boot hash matches expected values\n  const expectedHash = await getExpectedRuntimeHash(attestationResult.teeType);\n  if (attestationResult.measuredHash !== expectedHash) {\n    throw new Error('Runtime hash mismatch: possible tampering detected');\n  }\n  \n  // 3. Issue tenant-specific session credentials\n  const sessionKey = await deriveSessionKey(tenantId, attestationResult.measurement);\n  \n  return {\n    authorized: true,\n    sessionToken: sessionKey,\n    enclavePublicKey: attestationResult.enclaveKey\n  };\n}\n\n// Example: Initialize secure inference pipeline\nasync function initializeSecureInference(tenantId, modelId, data) {\n  const { sessionToken, enclavePublicKey } = await attestRuntime(\n    tenantId, \n    await getAttestationQuote()\n  );\n  \n  // Encrypt data with enclave key before sending to TEE\n  const encryptedData = await encryptForEnclave(data, enclavePublicKey);\n  \n  // Execute inference within confidential context\n  const result = await executeInference(encryptedData, sessionToken);\n  \n  return result;\n}\n```\n\n## Follow-up Questions\n\n- How to validate side-channel mitigations and test for residual leakage vectors?\n- What are the performance trade-offs when migrating ML workloads to confidential GPUs?\n- How do you handle key rotation in a multi-tenant TEE environment without service disruption?\n- Design the incident response workflow when a tenant reports potential data exposure.\n- How would you implement zero-trust networking between tenants in a shared inference cluster?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-1",
        "questionId": "q-7155",
        "question": "In a multi-tenant edge security gateway for a real-time chat service, propose a concrete zero-trust design enforcing per-tenant identities, short-lived tokens, mTLS, hardware-backed attestation, and artifact integrity checks at runtime. Include technology choices and trade-offs, and how you would test revocation and incident response?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-tenant token bucket at the edge. Validate x-tenant against allowed tenants; keep quotas in a fast store (KV/cache) and refill tokens per minute; enforce burst with bucket size; reject reques",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "AES-256-GCM combines Galois/Counter Mode encryption with authentication, providing 256-bit encryption and a 128-bit authentication tag in a single cryptographic operation.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement layered security controls: require OAuth2 client credentials or mTLS for internal service authentication, enforce TLS 1.3 for all data in transit, encrypt sensitive data at rest using AES-256, issue short-live…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant SPIFFE IDs with short-lived JWTs (15 min) signed by a private CA; enforce mutual TLS between edge and origin via SPIRE; store keys in TPM/HSM and sign edge code; verify SBOM and perform",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes zero-trust at the edge, tenant isolation, and practical mitigations. It tests choice of SPIFFE/SPIRE, mTLS, HSM, SBOM, attestation, and incident playbooks. It also surfaces trade-offs between latency, security, and operability.\n\n## Key Concepts\n- Zero-trust at edge\n- Per-tenant identity (SPIFFE/SPIRE)\n- Short-lived tokens (JWT)\n- mTLS between edge and origin\n- TPM/HSM-backed keys and code signing\n- Runtime attestation and SBOM checks\n- Key rotation and revocation (OCSP/CRLs)\n- Logging and incident response\n\n## Code Example\n```javascript\n// Token rotation config (pseudo)\nconst TOKEN_TTL_MS = 15 * 60 * 1000;\nfunction shouldRotate(last) { return (Date.now() - last) > TOKEN_TTL_MS; }\n```\n\n## Follow-up Questions\n- How would you measure revocation latency across global edge nodes?\n- What are the trade-offs between token lifetime, replay risk, and availability?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-2",
        "questionId": "q-23377",
        "question": "Design a secure, multi-tenant policy engine for an edge security service where tenants submit policy code that runs in a shared runtime. Outline how you validate, sandbox, sign, and enforce these policies across global edge locations. Include per-tenant isolation, policy signing and attestation, key management, policy updates, revocation, and auditability. Recommend concrete tech choices and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an end-to-end isolation plan for a multi-tenant security API that spawns ephemeral AWS Lambda and Google Cloud Run tasks to process tenant data across clouds. How would you enforce tenant boundaries, manage keys,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a strict sandboxed plugin model: run each plugin in a WASM sandbox per-tenant with a denied host API surface; expose only a minimal plugin API and enforce namespace isolation. Require plugins to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a WASM-based policy engine with per-tenant sandboxes, runtime attestation, and policy signing. Validate policies via a code-signing step, enforce with a verifier that only accepts modules si",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement tenant-scoped keys in a KMS with envelope encryption, require attestation of confidential enclaves prior to inference, and record provenance hashes in a tamper-evident ledger. Isolate tenant",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests architecture for secure multi-tenant edge policy code, isolation, and attestation at scale.\n\n## Key Concepts\n- Policy-as-code\n- WASM sandboxing\n- Runtime attestation\n- Per-tenant key management\n- Auditability\n\n## Code Example\n```javascript\n// verifyPolicySignature(publicKey, policy, signature) {\n//   return crypto.verify(signature, crypto.createHash('SHA256').update(policy).digest(), publicKey)\n// }\n```\n\n## Follow-up Questions\n- How would you scale policy evaluation latency across regions?\n- What failure modes require circuit breakers and alerting?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-3",
        "questionId": "q-5699",
        "question": "In a multi-cloud, real-time chat platform deployed across AWS and GCP, a rogue base image was introduced in CI. Draft a practical plan to prevent supply-chain attacks and detect tampering in production. Include: image signing/SBOM verification, runtime attestation and policy enforcement, deployment-time controls with canaries, and PII privacy safeguards. Name concrete tools and steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Threat model and threat sources. Use Bonawitz secure aggregation with per-round masking; enforce differential privacy budgets per client; require remote attestation and device-bound keys for updates;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a PKCE-enabled OAuth 2.0 flow with per-tenant issuer and audience, bind access tokens to tenant_id and client_id, and enforce short-lived access tokens (e.g., 15 minutes) with rotating refresh t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a layered defense: enforce container image provenance with Sigstore/Cosign, require SBOM verification in CI and at runtime, deploy via immutable registries with admission controls (OPA) and",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose an end-to-end plan: enforce per-tenant namespaces and ACLs on the event bus, use envelope encryption with customer-managed keys and per-tenant rotation, mandate tenant-prefixed topics, impleme",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nA real-world, cross-cloud supply-chain threat requires concrete controls at build, deployment, and runtime, plus privacy safeguards.\n\n## Key Concepts\n- Container image provenance and SBOM verification\n- Runtime attestation and policy enforcement\n- Canary deployments and safe rollback\n- PII handling and privacy considerations\n\n## Code Example\n```javascript\n// Pseudo-check for image policy (SBOM+signature)\nfunction isImageCompliant(image) {\n  return image.signature != null && image.sbom && image.sbom.version >= 2;\n}\n```\n\n## Follow-up Questions\n- How would you automate SBOM generation and rotation across CI/CD?\n- What telemetry would you collect to detect supply-chain anomalies without leaking PII?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-4",
        "questionId": "q-404",
        "question": "You're building a financial trading platform at Jane Street. How would you design a secure authentication and authorization system that prevents XSS, CSRF, SQLi, and SSRF attacks while maintaining high performance for real-time trading data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sign each eBPF program with a tenant-scoped KMS key and verify with the kernel verifier before loading. Isolate tenants using separate cgroups/namespaces and per-tenant maps; prune helper functions to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a defense-in-depth security architecture: Content Security Policy headers to prevent XSS, SameSite cookies combined with CSRF tokens for cross-site request protection, parameterized queries with prepared state…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement tenant-scoped RBAC in the API, use per-tenant views/tokenized PII in MongoDB (field-level encryption), enable dynamic masking at query time for dashboards, add CI checks to reject any PII pr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Threat model: In a multi-tenant GPU inference service with confidential computing support, primary threats include memory-side channel attacks where one tenant may infer information from another tenant's GPU memory acce…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nJane Street processes billions of dollars in trading value daily while requiring sub-millisecond latency for real-time trading operations. This question evaluates your ability to balance robust security controls with high-performance requirements in a mission-critical financial environment.\n\n## Expected Answer\nStrong candidates discuss multiple layers of protection: CSP headers to prevent XSS attacks, SameSite cookie attributes combined with CSRF tokens for cross-site request protection, prepared statements and parameterized queries to defend against SQL injection, IP whitelisting with strict request validation and network segmentation to prevent SSRF attacks, plus comprehensive monitoring, logging, and rate limiting for additional defense mechanisms.\n\n## Code Example\n```typescript\n// Secure API endpoint with multiple protections\napp.post('/api/trade', async (req, res) => {\n  // CSRF protection\n  if (!validateCSRF(req.headers['x-csrf-token'])) {\n    return res.status(403).json({ error: 'Invalid CSRF token' });\n  }\n  \n  // Rate limiting\n  if (!checkRateLimit(req.ip)) {\n    return res.status(429).json({ error: 'Rate limit exceeded' });\n  }\n  \n  // Input validation\n  const validatedData = validateTradeRequest(req.body);\n  \n  // Parameterized query\n  const result = await db.execute(\n    'INSERT INTO trades (user_id, symbol, quantity, price) VALUES (?, ?, ?, ?)',\n    [validatedData.userId, validatedData.symbol, validatedData.quantity, validatedData.price]\n  );\n  \n  res.json({ success: true, tradeId: result.insertId });\n});\n```",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-5",
        "questionId": "q-21466",
        "question": "You run a multi-tenant cloud data platform with a web frontend, API gateway, and service mesh. A compromised API key could access data from multiple tenants. Propose an end-to-end plan to prevent cross-tenant leakage: short-lived tokens bound to per-tenant identities with token binding, per-tenant scopes, SPIRE‑mTLS, gateway policies enforcing tenant isolation (OPA/REGO), and runtime controls (data masking, query whitelisting). Outline deployment steps and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant HSM-backed signing keys that issue short-lived JWTs (5-15 min) bound to device attestation and the target resource. Use mTLS between edge and auth services, token binding (audience, nonce),",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Short-lived, tenant-bound tokens (OAuth2/OIDC with token binding) plus per-tenant scopes; SPIRE mTLS for service trust; gateway policy enforcement via OPA/REGO to block cross-tenant access; data-plane",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Require policy bundles signed by a trusted authority and verified by the gateway before loading. Enforce per-tenant scopes with mTLS, deny-by-default routing, and explicit allow rules. Keep immutable,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Split Snowflake access by service and enforce least privilege: create per-service roles with only needed schemas/tables; assign a dedicated warehouse per service; move API keys/secrets to a vault and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess ability to design robust multi-tenant security with runtime enforcement and policy-driven access.\n\n## Key Concepts\n- Token binding, OAuth2/OIDC, PKCE\n- SPIRE mTLS\n- Policy-as-code (OPA/REGO)\n- Data masking and query whitelisting\n- Automated revocation and incident playbooks\n\n## Code Example\n```rego\npackage example.tenants\n\ndefault allow = false\n\nallow {\n  input.tenant_id == data.tenant_id\n  input.scopes[_] == \\\"data.read\\\"\n}\n```\n\n## Follow-up Questions\n- How would you test token binding in CI?\n- How would you validate fail-open/containment during revocation?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-6",
        "questionId": "q-9721",
        "question": "Design a secure, scalable defense-in-depth plan against a supply-chain compromise for serverless runtimes and container images used across Google, DoorDash, and Amazon-scale services. Include image signing/attestation (Sigstore/Notary), runtime attestation, immutable logs, secure update pipelines, and incident response. How would you test and validate the plan before and after deployment?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement tenant-scoped Unity Catalog with per-tenant schemas; enforce Row-Level Security and dynamic masking on all PII fields; store secrets in Databricks Secret Scopes with least-privilege access a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a phased, backward-compatible PQ migration: keep ES256 while adding a PQ verifier and a hybrid sig path. Use per-tenant envelope keys in KMS with versioned data keys. Phase 1: tokens carry both",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Require image signing and attestation for all runtimes (Sigstore/Notary), immutable tamper-evident logs, and runtime attestation before execution (trusted compute base). Use a GitOps pipeline with aut",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-tenant envelope encryption: each tenant has a data key wrapped by a master key in an HSM (e.g., Vault with AWS KMS/CloudHSM). Use Sigstore for build integrity, SBOMs, and OPA for policy.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses depth in securing supply chains, runtime enforcement, and incident response at scale.\n\n## Key Concepts\n- Software supply chain security\n- Image signing and attestation\n- Runtime attestation and enforceable policy\n- Immutable audit logs, tamper-evident design\n- Secure update pipelines and incident response\n\n## Code Example\n```javascript\n// Pseudo-sign-verify example (Node.js)\nconst crypto = require('crypto');\nasync function verify(manifestPath, sigPath, pubKeyPath){\n  const m = await require('fs').promises.readFile(manifestPath);\n  const s = await require('fs').promises.readFile(sigPath);\n  const pub = await require('fs').promises.readFile(pubKeyPath);\n  const v = crypto.createVerify('RSA-SHA256');\n  v.update(m); v.end();\n  return v.verify(pub, s);\n}\n```\n\n## Follow-up Questions\n- How would you test key rotation and revocation?\n- How would you detect and respond to a signing-key compromise?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-7",
        "questionId": "q-21051",
        "question": "An admin API for a multi-tenant analytics service on a platform used by Instacart/Oracle has elevated privileges and can export customer data. Current logs leak tokens and user IDs. Propose a concrete end-to-end plan to prevent data leakage: enforce per-tenant access controls, redact logs centrally, secure exports with envelope encryption and KMS, ensure immutable audit logs, and define automated anomaly response?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant data isolation via Postgres RLS and separate schemas; envelope encryption with per-tenant keys stored in a KMS or Vault Transit, rotated quarterly; mTLS via SPIRE with mutual auth; per-tena",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a multi-tenant cloud storage architecture with strict tenant data isolation, even in the event of storage node compromise. Key components include per-tenant encryption keys managed via a KMS, envelope encryption…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement tenant-scoped isolation with per-tenant keys and RBAC/ABAC, enforce Kafka topic ACLs, use envelope encryption with a KMS (per-tenant CMK), source/processing attestation (e.g., SPIRE/NodeAtt)",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce per-tenant RBAC/ABAC across all admin APIs; route logs through a central proxy that redacts PII before storage; use envelope encryption for exports with a cloud KMS; require time-bound break-g",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design concrete controls for admin data access and logging in a multi-tenant setting.\n\n## Key Concepts\n\n- Per-tenant access control (RBAC/ABAC)\n- Centralized log redaction\n- Envelope encryption with KMS\n- Immutable audit logs / WORM storage\n- Break-glass workflows and automated incident response\n\n## Code Example\n\n```javascript\n// Pseudo-implementation sketch for masking sensitive fields in logs\n```\n\n## Follow-up Questions\n\n- How would you verify log redaction in CI/CD?\n- What metrics would you monitor for early warning of data exfiltration?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-8",
        "questionId": "q-29469",
        "question": "A web API logs user email addresses to a central log stream in plaintext during request handling. Propose a beginner-friendly plan to prevent data leakage while keeping debuggability: redact PII in logs, adopt structured logging with non-PII fields, enforce access controls on the log sink, and add a CI check to enforce the policy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-device cryptographic keys secured within iOS Secure Enclave and Android StrongBox, coupled with remote attestation to a centralized verification service. Employ envelope encryption using cloud HSM-protecte…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant IAM roles with least privilege; restrict s3 access to only the tenant bucket; move secrets to Secrets Manager and fetch at runtime; place Lambda in a VPC with private subnets and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: redact PII in logs, switch to structured logging excluding emails and other identifiers, enforce RBAC on the log sink, and add a CI gate to fail PRs that emit unredacted PII. Include a minimal r",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-tenant CMKs in a hardware-backed KMS and envelope-encrypt data with per-tenant data keys cached with short TTL. Rotate CMKs with zero-downtime re-wrapping of all data keys. Enforce least privi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical logging security in API environments, focusing on redaction, RBAC, and policy automation rather than theory.\n\n## Key Concepts\n\n- PII exposure in logs\n- Redaction and masking\n- Structured logging\n- Access control for log sinks\n- CI gates for policy enforcement\n\n## Code Example\n\n```javascript\nfunction redactLog(entry){\n  if (entry.email) entry.email = '[REDACTED]';\n  if (entry.phone) entry.phone = '[REDACTED]';\n  return entry;\n}\n```\n\n```yaml\nlog:\n  redactPII: true\n  fields:\n    - time\n    - level\n    - message\n    - method\n    - path\n```\n\n## Follow-up Questions\n\n- How would you test the logging policy in CI and staging?\n- What are trade-offs between redaction granularity and debugging?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-9",
        "questionId": "q-16610",
        "question": "You're distributing a JavaScript/TypeScript SDK to thousands of partner apps via npm and a CDN. If a CDN compromise taints the SDK to exfiltrate tokens, propose an end-to-end hardening plan: HSM-backed artifact signing, reproducible builds, per-version SRI, CDN signing, TLS pinning or origin validation, runtime integrity checks, revocation/hotfix workflow, and an incident-response plan with rollback and customer notification. Include trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy per-tenant sandboxed kernels using Firecracker or gVisor with strict filesystem and network isolation; enforce data access through Open Policy Agent (OPA) policies granting access only to explicitly authorized re…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Sign artifacts with an HSM-backed key and enforce reproducible builds; publish per-version SRI digests; CDN signing; TLS pinning or origin validation for SDK fetch; embed runtime integrity checks in t",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Secure your WebSocket connections with these four steps: 1. **Origin Whitelist Enforcement**: Only allow WebSocket connections from trusted domains (e.g., your app). Reject any request with a missing or unlisted Origin…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I'd design a zero-trust, multi-layer security plan: per-tenant envelope encryption with KMS/HSM-backed keys; mutual TLS with SPIFFE IDs for all clients; platform attestation (TEE/TPM) for client SDKs",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nInterview context explanation about supply chain risk for widely distributed SDKs and realistic containment.\n\n## Key Concepts\n\n- Software supply-chain security\n- Code signing with HSM\n- Reproducible builds\n- Per-version SRI/digests\n- CDN trust and signing\n- Runtime integrity checks\n- Revocation and incident response\n\n## Code Example\n\n```javascript\n// Example: verify digest at runtime\nconst fs = require('fs');\nconst crypto = require('crypto');\nconst data = fs.readFileSync(process.argv[2]);\nconst digest = crypto.createHash('sha256').update(data).digest('hex');\nif (digest !== EXPECTED_DIGEST) throw new Error('Integrity check failed');\n```\n\n## Follow-up Questions\n\n- How would you rotate signing keys without breaking partner apps?\n- How would you measure performance impact of integrity checks on startup and in constrained environments?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-10",
        "questionId": "q-28086",
        "question": "An internal multi-tenant web app used by Scale AI and HashiCorp relies on cookies for session management. A security scan flags cookies that lack HttpOnly, Secure, and a proper SameSite attribute, risking session hijacking and cross-tenant leakage. Propose a beginner-friendly, end-to-end plan to fix cookie security in 48 hours, with concrete backend and frontend changes and minimal risk?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sign each log line with a per-event signature using a KMS-backed key, and hash-chain entries to form a tamper-evident ledger. Ingestor validates signatures and hashes, raising alerts on mismatches. St",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Validate file size (e.g., max 5MB) and allowed extensions on the server; verify MIME types to ensure they match the expected types; stream uploads directly to a private cloud bucket without storing files on local disk;…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Make all session cookies HttpOnly and Secure, and set SameSite to Lax with tenant-scoped domains. Express: app.use(session({ cookie: { httpOnly: true, secure: true, sameSite: 'Lax', domain: '.tenant.e",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant data isolation via Postgres RLS and separate schemas; envelope encryption with per-tenant keys stored in a KMS or Vault Transit, rotated quarterly; mTLS via SPIRE with mutual auth; per-tena",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nCookies are a practical attack surface in real apps, especially in multi-tenant contexts. This question tests concrete, implementable fixes rather than theory.\n\n## Key Concepts\n- HttpOnly, Secure, SameSite attributes\n- Tenant-scoped cookie domain and isolation\n- Frontend-backend cookie handling and storage choices\n- Lightweight CI gates to enforce security posture\n\n## Code Example\n```javascript\n// Express session cookie example\napp.use(session({\n  name: 'session',\n  cookie: {\n    httpOnly: true,\n    secure: true,\n    sameSite: 'Lax',\n    domain: '.tenant.example.com'\n  },\n  secret: 'REDACTED',\n  resave: false,\n  saveUninitialized: false\n}));\n```\n```javascript\n// Frontend fetch example\nfetch('/api/data', { credentials: 'include' });\n```\n```yaml\n# Minimal CI gate example (pseudo)\nname: cookie-attribute-check\non: [push, pull_request]\njobs:\n  verify_cookies:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v3\n      - name: Validate cookie options\n        run: |\n          grep -R \"httpOnly: true\" -n src || exit 1\n          grep -R \"secure: true\" -n src || exit 1\n          grep -R \"sameSite\" -n src || exit 1\n```\n\n## Follow-up Questions\n- How would you adapt if cross-origin API calls are required?\n- What tests would you add to ensure regressions are caught automatically?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-11",
        "questionId": "q-255",
        "question": "How would you implement OWASP ASVS L3 input validation for a REST API endpoint that accepts JSON payloads with nested objects?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Require tenant_id consistency: middleware verifies that the JWT's tenant_id matches the resource's tenant_id in every endpoint; reject otherwise. Enable PostgreSQL RLS with policies on all tables to f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement multi-layered validation using JSON Schema with strict whitelisting, recursive nested object validation, type and range constraints, and input sanitization at each level.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design uses envelope encryption: each tenant has a unique Data Encryption Key (DEK) wrapped by a Central Master Key stored in a FIPS-140-2 HSM/KMS. Use per-tenant IAM roles, no shared DEKs, and automa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant data isolation via Postgres RLS and separate schemas; envelope encryption with per-tenant keys stored in a KMS or Vault Transit, rotated quarterly; mTLS via SPIRE with mutual auth; per-tena",
            "isCorrect": false
          }
        ],
        "explanation": "## OWASP ASVS L3 Implementation Strategy\n\nOWASP ASVS Level 3 requires comprehensive input validation for security-critical applications. For REST APIs with nested JSON payloads, implement a defense-in-depth approach:\n\n**Schema Validation Layer:**\nUse JSON Schema or libraries like Joi/Zod to define strict validation rules. Implement recursive validation for nested objects, ensuring each level is validated independently.\n\n**Whitelist-Based Validation:**\nDefine explicit allowed fields at each nesting level. Reject any unknown properties using `additionalProperties: false` in JSON Schema or `stripUnknown()` in Joi.\n\n**Type and Range Validation:**\nEnforce strict type checking with minimum/maximum values, string length limits, and regex pattern matching. Apply constraint validation at all nesting levels to prevent injection attacks.\n\n**Input Sanitization:**\nSanitize string inputs to remove potentially dangerous characters before processing. Apply context-specific encoding based on data usage (HTML, SQL, command-line contexts).\n\n**Recursive Validation Example:**\n```javascript\nconst nestedSchema = {\n  type: \"object\",\n  additionalProperties: false,\n  properties: {\n    user: {\n      type: \"object\",\n      additionalProperties: false,\n      properties: {\n        id: { type: \"integer\", minimum: 1 },\n        profile: {\n          type: \"object\",\n          additionalProperties: false,\n          properties: {\n            email: { type: \"string\", format: \"email\" },\n            preferences: {\n              type: \"object\",\n              additionalProperties: false\n            }\n          }\n        }\n      }\n    }\n  }\n};\n```\n\n**Error Handling:**\nProvide detailed validation error responses without exposing internal system information. Log validation failures for security monitoring while maintaining privacy compliance.",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-12",
        "questionId": "q-23004",
        "question": "You deploy a web app that stores user uploads in an Amazon S3 bucket and a recent security test shows the bucket is publicly readable. Propose a beginner-friendly, concrete plan to fix and prevent such leaks, including verification steps and CI checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "First, identify public access using S3 API commands like `get-bucket-policy` and `get-bucket-acl`. Then, remove any public ACLs and enable the S3 Block Public Access setting at both the bucket and account levels. Next,…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Detect anomalies in CMEK usage (cloud logs, HSM attestation). Immediately revoke affected CMKs, rotate regionally, and issue new KEKs. Re-encrypt all tenant data with fresh CMKs, updating envelope key",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use short-lived OAuth2 tokens issued by an authorization server (OIDC). Bind tokens to tenant, scope, and a limited IP range; issue JWTs with aud, iss, iat, exp, jti; protect via JWKS; enable token in",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt per-tenant data encryption keys (DEKs) wrapped by a centralized KMS master key, using envelope encryption with AES-256-GCM. Rotate envelope keys quarterly and replicate across regions. When a tenant key is comprom…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical knowledge of cloud storage security, including how to identify misconfigurations, remediate them, and implement preventive controls.\n\n## Key Concepts\n- S3 bucket policies, ACLs, and Block Public Access settings\n- IAM least privilege principles and service roles\n- Server-side encryption, versioning, logging, and auditing\n- CI/CD integration and automated security validation\n\n## Code Example\n```bash\n# Check current bucket policy\naws s3api get-bucket-policy --bucket my-bucket\n\n# Check bucket ACL\naws s3api get-bucket-acl --bucket my-bucket\n\n# Block public access at bucket level\naws s3api put-public-access-block \\\n  --bucket my-bucket \\\n  --public-access-block-configuration \"BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true\"\n\n# Apply bucket policy (example: allow only specific IAM role)\naws s3api put-bucket-policy --bucket my-bucket --policy '{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [{\n    \"Effect\": \"Allow\",\n    \"Principal\": {\"AWS\": \"arn:aws:iam::123456789012:role/MyAppRole\"},\n    \"Action\": [\"s3:GetObject\", \"s3:PutObject\"],\n    \"Resource\": \"arn:aws:s3:::my-bucket/*\"\n  }]\n}'\n\n# Enable versioning\naws s3api put-bucket-versioning --bucket my-bucket --versioning-configuration Status=Enabled\n\n# Enable server-side encryption\naws s3api put-bucket-encryption --bucket my-bucket --server-side-encryption-configuration '{\n  \"Rules\": [{\n    \"ApplyServerSideEncryptionByDefault\": {\n      \"SSEAlgorithm\": \"AES256\"\n    }\n  }]\n}'\n```\n\n## Verification Steps\n1. Confirm Block Public Access is enabled at both bucket and account levels\n2. Verify bucket policy grants access only to intended IAM principals\n3. Test access using an unauthorized IAM role to confirm denial\n4. Ensure encryption and versioning are active via `get-bucket-encryption` and `get-bucket-versioning`\n\n## CI/CD Checks\n- Use tools like `cfn-lint`, `checkov`, or `saws` to scan CloudFormation/Terraform for public bucket configurations\n- Add AWS Config rules (e.g., `s3-bucket-public-read-prohibited`) for continuous monitoring\n- Integrate `aws s3api get-bucket-policy-status` in CI pipelines to fail builds on public buckets",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-13",
        "questionId": "q-4928",
        "question": "Design a real-world, end-to-end security design for a new microservice that validates payment tokens for a high-volume API used by consumer apps (e.g., Tesla, Stripe, Meta). The design should cover code signing, artifact integrity, runtime attestation, mTLS, token binding, key management in an HSM, SBOM enforcement, and an incident-playbook with rotation and revocation steps. List concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Secure OTA Update Supply Chain for Autonomous Vehicles ### 1. Build Phase (Reproducibility & Provenance) - **Build System**: Use Bazel or Nix with hermetic build environments (all dependencies pinned via content-addr…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enforce per-tenant namespaces; use eBPF/XDP to block anomalous egress (DNS/TLS to unapproved destinations); feed a Falco detector with tenant context; require runtime attestation of compute nodes via",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a zero-trust flow: sign and verify all code/artifacts with cosign; enforce SBOMs and attestations; bind OAuth2 tokens to TLS using mTLS and SPIFFE IDs; sign/verify JWTs with hardware-backed",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-tenant keys with envelope encryption (KMS, HSM), runtime isolation via WASM sandboxes or gVisor, strict IAM least privilege, and per-tenant data partitioning. Redact metadata at ingest,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests ability to design a practical, scalable security fabric for high-stakes payments, spanning CI/CD, runtime, and incident response, with real toolchains.\n\n## Key Concepts\n\n- Zero trust, SBOM, artifact signing (cosign/Sigstore)\n- mTLS, SPIFFE/SPIRE, token binding\n- Hardware security modules (HSM), key management\n- Runtime attestation, policy enforcement (OPA)\n- Incident response, key rotation, revocation\n\n## Code Example\n\n```javascript\n// Pseudo: verify JWT with key from HSM/KMS\nconst jwt = require('jsonwebtoken');\nconst publicKey = getPublicKeyFromHSM(); \njwt.verify(token, publicKey, { algorithms: ['RS256'] });\n```\n\n## Follow-up Questions\n\n- How would you handle key rotation without service downtime?\n- How would you prove SBOM compliance in CI/CD?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-14",
        "questionId": "q-26068",
        "question": "In a multi-tenant platform, tenants submit webhooks that trigger workflows. A compromised tenant webhook could exfiltrate data or break isolation. Design an end-to-end webhook runtime: per-tenant HMAC signing with keys in an HSM, request verification, replay protection, per-tenant API surface minimization, tamper-evident logs, and an incident response including rapid key rotation and tenant revoke?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a privacy-preserving, consented data path: only forward minimal content (redacted text/metadata). Use mTLS between chat service and moderation API, and sign requests with Ed25519; responses sign",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Threat model: In a multi-tenant GPU inference service with confidential computing support, primary threats include memory-side channel attacks where one tenant may infer information from another tenant's GPU memory acce…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant keys stored in an HSM to sign outbound payloads and verify them at ingress, enforce nonce+timestamp replay protection, and minimize endpoints per tenant. Implement tamper-evident loggin",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a signed plugin manifest and SBOM, load plugins in a WASM sandbox or isolated iframe with a minimal API surface; verify signing via Sigstore/Cosign and hardware-backed attestation; enforce l",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design secure, scalable webhook workflows with tenant isolation and incident response.\n\n## Key Concepts\n- Per-tenant keys stored in an HSM\n- HMAC verification\n- Replay protection\n- Tenant isolation\n- Tamper-evident logs\n- Revocation and rotation\n- Incident response playbook\n\n## Code Example\n```javascript\n// Node.js example: verify HMAC signature per tenant\nconst crypto = require('crypto');\nfunction verifyTenantWebhook(tenantId, payload, signature, getSecret){\n  const secret = getSecret(tenantId); // fetch from HSM-backed store\n  const hmac = crypto.createHmac('sha256', secret).update(payload).digest('hex');\n  return crypto.timingSafeEqual(Buffer.from(hmac, 'hex'), Buffer.from(signature, 'hex'));\n}\n```\n\n## Follow-up Questions\n- How would you test for replay and clock skew?\n- How would you handle cross-tenant revocation latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-15",
        "questionId": "q-28397",
        "question": "You operate a multi-tenant security analytics platform that ingests telemetry from thousands of customers and runs ML inference in confidential compute enclaves. Data from each tenant must never mix, even in shared compute. Design a practical security plan covering per-tenant data isolation, tenant-scoped key management and envelope encryption, secure model signing/verification, data provenance, secure deployment, and incident response for enclave compromise or data leakage. Include concrete tech choices and deployment steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage per-tenant envelope encryption with keys in a KMS (AWS KMS/GCP KMS) and data-at-rest encryption for MongoDB Atlas and Delta Lake. Apply field-level encryption for PII, TLS/mTLS between servic",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement tenant-scoped isolation with per-tenant keys and RBAC/ABAC, enforce Kafka topic ACLs, use envelope encryption with a KMS (per-tenant CMK), source/processing attestation (e.g., SPIRE/NodeAtt)",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Sanitize server-side SVG uploads to prevent XSS. Accept only image/svg+xml and cap size; run DOMPurify with jsdom to strip <script>, onload, and javascript: URLs, remove external references; render vi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage per-tenant isolation via data partitioning and namespace scoping; use tenant-scoped envelope encryption with KMS keys; run inference inside Nitro/Azure Confidential Compute enclaves with atte",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design end-to-end security for a multi-tenant ML pipeline using confidential compute, while enforcing strict data isolation, verifiable model integrity, and robust incident response.\n\n## Key Concepts\n\n- Per-tenant data isolation and namespace scoping\n- Tenant-scoped envelope encryption via KMS\n- Confidential compute enclaves and attestation\n- Secure model signing/verification (Sigstore)\n- Data provenance and immutable logs\n- Policy enforcement (OPA) and deployment safeguards\n- Incident response for enclave compromise\n\n## Code Example\n\n```typescript\n// Pseudo-code: verify model before loading into enclave\nfunction loadModel(modelPath, signature, publicKey) {\n  const valid = verifySignature(modelPath, signature, publicKey);\n  if (!valid) throw new Error('Invalid model signature');\n  return loadIntoEnclave(modelPath);\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt this plan to a multi-cloud, vendor-agnostic deployment?\n- What tests would you automate to validate tenant isolation under high churn and degraded networking?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-16",
        "questionId": "q-15114",
        "question": "You operate a multi-tenant edge compute platform that runs user-supplied scripts in a sandbox on customer devices. Propose a concrete plan to prevent cross-tenant data leakage via side channels and memory dedup, outlining: threat model, isolation boundary (microVMs vs containers vs HW enclaves), memory partitioning, cache/page-table isolation, IPC controls, and verification steps including CI tests and incident response?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Tenant isolation via per-tenant namespaces and envelope encryption of telemetry; sign ML artifacts with Sigstore and verify at deploy time; store models in an HSM-backed registry; run inference in san",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Require policy bundles signed by a trusted authority and verified by the gateway before loading. Enforce per-tenant scopes with mTLS, deny-by-default routing, and explicit allow rules. Keep immutable,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a global error handler that returns a generic message; redact PII (emails, IDs, tokens) from logs and error payloads; switch to structured JSON logs with production-level verbosity and no st",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: model cross-tenant leakage via cache/memory. Isolate each tenant in a microVM (Firecracker) or HW enclave (TDX/SNP). Disable memory dedup; enable KPTI; enforce per-tenant address spaces; strict",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssess practical hardening of multi-tenant edge workloads against side-channel leaks with realistic constraints and hardware options.\n\n## Key Concepts\n- Threat modeling for edge tenants\n- HW-assisted isolation (TDX/SNP, SEV) vs microVMs\n- Memory dedup, KPTI, CAT, per-tenant keys\n- Sandboxing (seccomp), IPC, encryption, incident response\n\n## Code Example\n```javascript\n// Pseudo test stub for CI to catch cross-tenant leaks\nfunction testCrossTenantLeak() {\n  // simulate attacker tenant and victim tenant timing\n}\n```\n\n## Follow-up Questions\n- Tradeoffs of Firecracker vs TDX in latency\n- How to validate side-channel resilience continuously",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-17",
        "questionId": "q-5735",
        "question": "You operate a Hugging Face-style model hosting service backed by MongoDB for metadata. Design a secure end-to-end workflow to prevent model tampering and data poisoning during upload, storage, and inference. Include: model provenance signing and verification, per-model access control, tamper-evident logs, storage encryption, and fast integrity checks that scale to 10k models with low latency. Explain threat model and concrete steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An end-to-end security plan for a multi-tenant AI inference platform should incorporate artifact signing and provenance verification using Sigstore or cosign, combined with hardware-backed attestation at load time via T…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement SPIFFE IDs with mTLS for all internal service communication, enforced by Istio's authorization policies. Issue short-lived credentials through HashiCorp Vault using Kubernetes authentication, rotating every 15…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-model artifact signing (Ed25519) at upload, store the signature and signer key ID in MongoDB. Verify signatures before serving models; reject unsigned or mismatched versions. Maintain an",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Sigstore provenance: sign builds with per-service ephemeral keys, publish attestations to Rekor, require runtime attestation via a sidecar that verifies image hashes against Fulcio certificates an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world ML hosting faces tampering and poisoning threats; this tests end-to-end thinking from artifact signing to immutable logs and provenance.\n\n## Key Concepts\n- Artifact signing and verification\n- Model provenance attestations\n- Append-only tamper-evident logging\n- Per-model RBAC and multi-tenant isolation\n- Scalable integrity checks and poisoning detection\n\n## Code Example\n```javascript\n// verify signature example (high level)\nasync function verifyModelArtifact(modelBuf, sigBuf, pubKeyPem){\n  // pseudo crypto call\n  return true;\n}\n```\n\n## Follow-up Questions\n- How would you rotate signing keys and verify past artifacts?\n- How would you scale the append-only log for TB-scale workloads while keeping latency low?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-18",
        "questionId": "q-12503",
        "question": "In a multi-tenant API gateway feeding dozens of microservices, a compromised policy controller injects routes that bypass tenant isolation, enabling cross-tenant data access. Propose a concrete hardening plan to prevent data leakage, covering (a) policy update authentication and isolation, (b) per-tenant access tokens with strict scopes and mTLS, (c) immutable logs with tamper-evident markers, (d) anomaly detection on policy changes, and (e) rollback/forensics playbooks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: introduce a redaction utility and a log-wrapping layer that masks fields: Authorization, token, password, id, and any per-tenant keys. Enforce structured JSON logs and redact recursively. Add CI",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Require policy bundles signed by a trusted authority and verified by the gateway before loading. Enforce per-tenant scopes with mTLS, deny-by-default routing, and explicit allow rules. Keep immutable,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use per-endpoint HMAC signatures with a nonce in each webhook payload. On receipt, verify the signature and check the nonce against a Redis set for that webhook ID; if seen, reject. TTL for nonces: 5",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Require signed plugin attestations (Sigstore/cosign) plus SBOMs; verify at load against a trusted root; sandbox plugins with WASM engine (Wasmtime) with per-plugin quotas and sandboxed syscalls; isola",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests ability to design robust runtime isolation, not just static controls, in a live gateway.\n\n## Key Concepts\n- Signed policy bundles and verifiable delivery\n- Per-tenant RBAC, mTLS, and deny-by-default routing\n- Tamper-evident logging and anomaly detection\n- Rollback/forensics for policy-driven changes\n\n## Code Example\n```yaml\n# policy bundle (simplified)\nversion: 1\ntenants:\n  - id: tenant-a\n    allow:\n      - path: /tenant-a/**\n        methods: [GET,POST]\nsignature: base64(signature)\n```\n\n## Follow-up Questions\n- How would you test for bypass paths in CI/CD?\n- How would you recover from a compromised policy bundle?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-19",
        "questionId": "q-23314",
        "question": "In a microservices ecommerce app, payment requests travel from API gateway to a payment service. A JWT carries tenant_id; a misconfiguration could allow cross-tenant data leakage if tenant_id isn't strictly bound to the request path. As a beginner, outline a concrete plan to enforce tenant isolation at gateway and services, including token binding, header propagation, and tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant enclaves with isolated sealing keys; require remote attestation before any processing; encrypt data at rest and in transit with per-tenant envelope keys stored in a hardware security module",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Argon2id to derive a per-user AES-256 key from the password; store the salt with the ciphertext and keep the key in the OS keychain. Encrypt each note with AES-GCM before writing to disk and decry",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end defense plan for a multi-tenant ML inference platform used by a global ride-hailing network. Include training-data supply-chain controls, model signing/versioning, per-tenant isol",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate tenant binding at every hop: require the gateway to bind tenant_id from JWT to the request path or body and reject if mismatch. Enforce claims validation, audience, and issuer; drop tokens th",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to enforce multi-tenant isolation in a practical gateway-service flow.\n\n## Key Concepts\n\n- JWT validation with issuer, audience, and tenant_id checks\n- Request binding: ensure tenant from token matches tenant in path/header\n- Header propagation and per-tenant RBAC across services\n\n## Code Example\n\n```javascript\n// Middleware example: verify tenant binding\nfunction verifyTenantBinding(req, res, next) {\n  const tokenTenant = req.user?.tenant_id\n  const pathTenant = req.headers['x-tenant-id'] || req.params.tenantId\n  if (!tokenTenant || tokenTenant !== pathTenant) {\n    return res.status(403).send('Tenant mismatch')\n  }\n  next()\n}\n```\n\n## Follow-up Questions\n\n- How would you test this with automated tests for boundary conditions?\n- How would you adapt this approach for non-JWT token schemes or service mesh enforcement?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-20",
        "questionId": "q-10909",
        "question": "You operate a PCI-DSS compliant payments platform with services across two cloud providers. A supplier pushes a new container image into your CI pipeline that later shows a hidden vulnerability in prod. Outline a concrete supply-chain security plan that covers: SBOM generation, image signing (Cosign/Sigstore), reproducible builds, runtime attestation, automated rollback, and incident response. Include tool choices, deployment steps, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement strict data partitioning: per-tenant topics/indices, unique KMS keys, and per-tenant cache namespaces. Enforce API scopes with OAuth token binding, SPIFFE/SPIRE mTLS for service identity, an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-tenant GPU inference stack using hardware enclaves (Intel TDX/AMD SEV). Enforce memory isolation, disable dedup, and wrap each tenant’s model with envelope encryption managed by a KMS/HSM",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enforce end-to-end supply chain controls: require SBOMs and CVE scans in CI, sign artifacts with Cosign/Sigstore, pin to content digests, enforce reproducible builds, enable runtime attestation with O",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Tenant isolation via per-tenant namespaces and envelope encryption of telemetry; sign ML artifacts with Sigstore and verify at deploy time; store models in an HSM-backed registry; run inference in san",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical runbook for securing software supply chains in a multi-cloud PCI context, focusing on verification, provenance, and quick recovery.\n\n## Key Concepts\n\n- Software supply chain security across multiple clouds\n- SBOM generation and CVE-driven gating\n- Image signing and provenance (Cosign/Sigstore)\n- Reproducible builds and immutable artifacts\n- Runtime attestation and admission controls (OPA/Gatekeeper)\n- Automated rollback and incident-response playbooks\n\n## Code Example\n\n```javascript\n// Example: verify image signature in admission flow (cosign placeholder)\ncosign verify docker.io/org/app@sha256:<digest> --certificate-identity-regexp 'CN=example.com'\n```\n\n## Follow-up Questions\n\n- How would you handle cross-cloud KMS/HSM for keys and key rotation?\n- What is your strategy to minimize CI/CD latency while enforcing strict attestations?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-21",
        "questionId": "q-28822",
        "question": "In a high-throughput, multi-tenant security telemetry pipeline with live dashboards, design an end-to-end per-tenant data isolation strategy. Include tenant-scoped envelope encryption with a KMS/HSM, policy enforcement at ingest/storage/query (OPA), cryptographic data lineage for provenance, and an incident response plan for key compromise or cross-tenant bleed. Provide concrete tech choices and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant isolation must be enforced at ingestion, storage, and compute. Use envelope encryption with tenant-scoped KEKs managed by a HSM-backed KMS, rotate keys regularly, and revoke on compromise.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Move API key usage server-side: add a backend proxy that signs requests to the third-party API with per-tenant keys stored in a KMS, remove keys from frontend, rotate keys, enforce strict scopes and I",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a SPIFFE-based mTLS with a hardware-backed CA using an HSM. Use short-lived leaf certs that rotate every few minutes, automatic revocation via CRL/OCSP, and per-service attestation at sideca",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant isolation via IAM roles, bucket prefixes, and VPC endpoints; enforce encryption at rest and in transit; deploy tenant-scoped keys and data provenance; add policy-as-code, DLP, and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question introduces data governance, isolation, and provenance in a multi-tenant telemetry pipeline—topics not yet covered and critical at scale.\n\n## Key Concepts\n\n- Per-tenant isolation across ingestion, storage, compute\n- Envelope encryption with tenant-scoped KEKs and KMS/HSM\n- Policy enforcement at ingest/storage/query (OPA)\n- Cryptographic data lineage for provenance\n- Incident response: key compromise, tenant bleed\n\n## Code Example\n\n```javascript\n// Sign lineage entry with tenant key (example)\nconst crypto = require('crypto');\nconst sign = crypto.createSign('RSA-SHA256');\nsign.update(JSON.stringify(entry));\nconst signature = sign.sign(tenantPrivateKey);\n```\n\n```rego\npackage data.provenance\ndefault allow = false\nallow {\n  input.tenant == data.tenants[_].id\n  input.action == \"read\" \n}\n```\n\n## Follow-up Questions\n\n- How would you test for cross-tenant bleed under node compromise?\n- How would you audit and prove compliance to SOC 2/ISO 27001 with data lineage?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-22",
        "questionId": "q-21325",
        "question": "Describe a secure OTA firmware update pipeline for a global EV fleet (edge/vehicle) that prevents supply-chain tampering, per-vehicle attestation, secure boot, and rollback while preserving privacy and auditability. Include: artifact signing in an HSM, SBOM validation, per-vehicle attestation over mTLS, canary rollout, tamper-evident logs, revocation, and an incident playbook?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Artifacts must be reproducible and signed within an HSM; SBOMs are validated against a trusted feed. Vehicles attest bootloader, firmware, and update package over mTLS using per-vehicle certificates. Implement canary ro…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a multi-region envelope encryption design with per-tenant data keys (DEKs) wrapped by a master key (CMK). Use AWS KMS or Vault with HSM-backed CMKs; rotate DEKs quarterly and CMKs every 12-24",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-IP and per-token rate-limiting using a Redis-backed token-bucket with a Lua script. Apply different limits per route (login stricter) and a small burst tolerance (e.g., 20). Return 429 w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design per-tenant secrets with envelope encryption: edge stores secrets encrypted with a per-tenant data key; data keys are wrapped by an HSM-backed master key in a central vault and rotated on schedu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes end-to-end OTA security for fleets, combining supply-chain integrity, device attestation, and incident response—critical competencies in edge and vehicle ecosystems like those at Cloudflare and Tesla.\n\n## Key Concepts\n\n- Secure OTA pipelines and reproducible builds\n- Hardware Security Modules (HSM) for artifact signing\n- SBOM validation and trusted software provenance\n- Per-vehicle attestation over mTLS\n- Canary rollout and safe rollback strategies\n- Tamper-evident logging and key revocation\n- Automated incident response playbooks\n\n## Code Example\n\n```python\ndef verify_update(update_bytes, sbom, vehicle_cert, hsm_signature):\n    if not hsm_signature.verify(update_bytes):\n        raise SecurityError(\"Invalid HSM signature\")\n    if not sbom.validate_trusted_feed():\n        raise SecurityError(\"SBOM validation failed\")\n    if not vehicle_attest(vehicle_cert):\n        raise SecurityError(\"Attestation failed\")\n    return True\n```",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-23",
        "questionId": "q-20504",
        "question": "You operate a multi-tenant hosted ML inference platform where tenants upload custom models (e.g., PyTorch/ONNX) that run in shared serverless or containerized sandboxes. A compromised model could leak training data or poison outputs. Propose an end-to-end security plan: model signing and verification, per-tenant isolation, secure model distribution, runtime attestation, access controls, auditing, and an incident-response playbook including rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-event nonces and idempotency keys, sign payloads with HMAC-SHA256 using a partner-secret and a timestamp, reject duplicates within a short window (e.g., 5 minutes). Enforce replay protec",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Unity Catalog ABAC anchored to tenant metadata, enforce Row-Level Security (RLS) to filter rows based on tenant context, and implement dynamic masking for PII columns. Use Delta Lake views to project only permi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would require per-model signing with an HSM-backed key, plus runtime attestation and per-tenant sandboxes. On upload, sign the model and attach a SBOM; verify signature before loading. Enforce stric",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a plan to enforce tenant boundaries for UDFs in a multi-tenant analytics platform: isolate each tenant's UDFs in unprivileged WASM sandboxes, enforce strict API surfaces, and pin policies with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExplores practical, end-to-end security for multi-tenant ML inference, covering supply chain, runtime isolation, attestation, and incident response—areas critical at Snowflake, Discord, and Google scale.\n\n## Key Concepts\n\n- Model signing, SBOMs, and HSM-backed keys\n- Per-tenant sandboxing and strict isolation\n- Runtime attestation and trustworthy loading\n- Tamper-evident logging and immutable audit trails\n- Incident response with safe rollback and revocation procedures\n\n## Code Example\n\n```javascript\n// Pseudo-code: verify model signature before loading\nconst verified = verifySignature(modelBytes, metadata.signature, publicKey);\nif (!verified) throw new Error('Invalid model signature');\nloadModelInSandbox(modelBytes);\n```\n\n## Follow-up Questions\n\n- How would you handle model updates during active inference with zero-downtime rolling updates?\n- What metrics and alerts would you implement to detect model-level poisoning in real time?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-24",
        "questionId": "q-9250",
        "question": "Tesla-like fleet API and Oracle cloud deployment rely on a mono-repo with many services. Secrets and API keys often end up in PRs. As a beginner, outline a practical secrets management and rotation plan across CI/CD: pre-commit scanning, per-service vaults, runtime injection, short-lived credentials, automatic rotation, and rollback steps, with tooling choices and minimal downtime?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement pre-commit secret scanning (git-secrets), use a central vault (HashiCorp Vault or cloud secret manager) with per-service roles, inject at runtime via CI/CD, avoid hard-coding. Use short-live",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use envelope encryption with per-tenant CMKs backed by an external KMS/HSM. Isolate each tenant in separate vaults, enforce least-privilege access, and rewrap data during key rotation with zero-downti",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a multi-tenant architecture for WASM-based UDFs with strict data isolation. Use per-tenant KEKs encrypted by a central KMS (envelope encryption), rotate keys monthly, sign UDF artifacts, requir",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce client and server validation for type and size; verify magic bytes instead of extensions; run malware scanning on uploads; store in a dedicated bucket with least-privilege IAM; generate short-",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses practical secrets management in real-world CI/CD pipelines, tailored to large orgs like Tesla and Oracle. It emphasizes beginner-friendly, actionable steps that map to tooling you can actually use.\n\n## Key Concepts\n\n- Secrets management and vaults\n- Pre-commit secret scanning\n- Centralized secret storage with per-service roles\n- Runtime secret injection in CI/CD\n- Short-lived credentials and automatic rotation\n- RBAC, auditing, and alerting\n\n## Code Example\n\n```bash\n# example pre-commit hook\nif git diff --cached --name-only | grep -E '(\\.env|secret|apikey|password)'; then\n  echo 'Secret detected in commit' ; exit 1\nfi\n```\n\n```\n\n## Follow-up Questions\n\n- How would you extend rotation across multi-team services?\n- What metrics would you monitor during rollout?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-29T06:48:46.377Z",
    "version": 1
  }
]