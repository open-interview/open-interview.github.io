[
  {
    "id": "test-security",
    "channelId": "security",
    "channelName": "Security",
    "title": "Security Knowledge Test",
    "description": "Test your understanding of Security with 25 questions.",
    "questions": [
      {
        "id": "security-q-0",
        "questionId": "q-16305",
        "question": "Design a security plan for a multi-tenant streaming analytics service encrypting data at rest with envelope encryption using per-tenant DEKs wrapped by regional KMS/HSM keys. Insiders could abuse long-lived service credentials to decrypt data. Outline key isolation, automatic DEK rotation (daily), cross-region DR, strict access controls, audit trails, and incident response with test plans?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate MIME and magic bytes; only allow common image types and reject SVGs unless safely sanitized. Sanitize SVG content by removing scripts and event handlers; strip all EXIF/IPTC metadata before s",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition data by tenant using per-tenant prefixes in the data lake and enforce envelope encryption with per-tenant keys via KMS/HSM. Require mTLS between services, least-privilege IAM, and per-tenant",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant enclaves with isolated sealing keys; require remote attestation before any processing; encrypt data at rest and in transit with per-tenant envelope keys stored in a hardware security module",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement envelope encryption with per-tenant DEKs wrapped by regional KMS/HSM master keys. Enforce strict access control using short-lived credentials, tenant- and region-scoped IAM policies, and network segmentation t…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses understanding of insider threat mitigation and cryptographic key management at scale in multi-tenant environments.\n\n## Key Concepts\n- Envelope encryption for efficient large-scale data protection\n- Per-tenant Data Encryption Keys (DEKs) for tenant isolation\n- Regional HSM/KMS master keys for regulatory compliance\n- Automatic key rotation without service interruption\n- Tamper-evident audit trails for compliance\n- Incident response procedures for cryptographic incidents\n\n## Code Example\n```javascript\n// Pseudocode: encryptTenantData(tenantId, plaintext) {\n//   1. Retrieve or generate per-tenant DEK\n//   2. Wrap DEK with regional KMS master key\n//   3. Encrypt plaintext with DEK\n//   4. Store wrapped DEK with encrypted data\n// }\n\n// Pseudocode: rotateDEK(tenantId, region) {\n//   1. Generate new DEK\n//   2. Wrap new DEK with current master key\n//   3. Re-encrypt all data keys with new DEK\n//   4. Archive old DEK securely\n// }\n```",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-1",
        "questionId": "q-18786",
        "question": "In a multi-tenant serverless platform, each tenant's data is envelope-encrypted with per-tenant KMS keys. A rogue function could misuse KMS to exfiltrate data. Propose a concrete end-to-end guardrail design to prevent data leakage via KMS misuse, covering IAM controls, per-tenant key isolation, admission checks for crypto ops, data-key caching, immutable logs, anomaly detection, key rotation, and an incident response playbook?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant IAM roles, envelope encryption with KMS, TLS in transit, redact/derive tokens for PII, immutable logs with HMACs, and bucket policies enforcing encryption and access. Add CI tests provi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant envelope encryption with KMS: isolate keys by tenant, enforce strict IAM policies, route Encrypt/Decrypt through an admission service that validates tenant scope, cache data keys with short",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-tenant build namespaces in Kubernetes; secrets injected only via short-lived tokens minted by a KMS (AWS KMS or Azure Key Vault) linked to tenant identity. Sign every image with cosign, enforce SB",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce per-tenant RBAC/ABAC across all admin APIs; route logs through a central proxy that redacts PII before storage; use envelope encryption for exports with a cloud KMS; require time-bound break-g",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAdvanced guardrails prevent data leakage via cryptographic operations. The candidate should articulate concrete controls, threat modeling, and automation.\n\n## Key Concepts\n- Tenant isolation at the key layer (per-tenant KMS keys)\n- Admission control for crypto ops\n- Data-key caching with safe TTLs\n- Immutable, centralized logging and tamper resistance\n- Key rotation, revocation, and incident response\n\n## Code Example\n```javascript\n// Pseudo: mapping tenant -> KMS key ARN\nconst TENANT_KEYS = { t1: 'arn:aws:kms:...:key/abc', t2: 'arn:aws:kms:...:key/def' };\n```\n\n## Follow-up Questions\n- How would you test this in CI/CD and during incident drills?\n- How would you handle cross-tenant data access anomalies and key compromise implications?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-2",
        "questionId": "q-11549",
        "question": "You operate a hosted ML inference platform with multi-tenant workloads and models from multiple vendors. A vendor pushes a new artifact via a model registry that is loaded into isolated runtimes. Design a practical, end-to-end security approach to guarantee model provenance, integrity, and isolation, addressing signing, attestation, SBOM, per-tenant access, runtime guards, and incident response. Include concrete tech choices and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Restrict CORS origins to known client applications, remove the wildcard configuration, and disable credentials for any non-whitelisted origins. Add the Vary: Origin header to prevent caching issues. Separate public and…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Chain-of-trust: sign every model artifact with the vendor's KMS key; require SBOM (CycloneDX) and a SHA-256 hash stored in the registry; load-time verification occurs inside a TEEs (Intel SGX/AMD SEV)",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement per-device authentication by issuing unique device credentials consisting of a short-lived access token and device-specific signing key. Clients authenticate API requests using HMAC-SHA256 signatures over the…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate tenant binding at every hop: require the gateway to bind tenant_id from JWT to the request path or body and reject if mismatch. Enforce claims validation, audience, and issuer; drop tokens th",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes model provenance, runtime attestation, and multi-tenant isolation in ML platforms, a real-world risk area for Oracle and Hugging Face.\n\n## Key Concepts\n\n- Model provenance and signing (KMS, HSM, PKI)\n- SBOM and hash verification\n- TEEs and runtime attestation\n- Per-tenant isolation and policy enforcement (e.g., OPA)\n- Revocation and rollback mechanisms\n- Tamper-evident logging and immutable audit trails\n\n## Code Example\n\n```python\n# Pseudocode: verify artifact before loading\nartifact = fetch_artifact(id)\nif not verify_signature(artifact, vendor_public_key): raise SecurityError()\nif not verify_hash(artifact, artifact.expected_hash): raise SecurityError()\nif not verify_sbom(artifact.sbom): raise SecurityError()\nte_established = call_tee_attestation()\nif not te_established: raise SecurityError()\nload_in_runtime(artifact)\n```\n\n## Follow-up Questions\n\n- How would you validate this model pipeline against supply-chain threats?\n- What are the trade-offs of using TEEs vs software attestation in this setup?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-3",
        "questionId": "q-8531",
        "question": "Design a security model for a shared data platform used by MongoDB, Lyft, and Databricks where multiple tenants store data in a common data lake and access it via MongoDB Atlas and Databricks workspace. Describe how you enforce tenant isolation, data-at-rest and in-transit encryption, key management, authorization, auditing, and incident response. Include concrete technologies, patterns, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement JWT with RS256 signing, store tokens in HttpOnly cookies, use refresh tokens for long-term sessions, validate claims on each request, implement token revocation list, and use short access token lifespans.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage per-tenant envelope encryption with keys in a KMS (AWS KMS/GCP KMS) and data-at-rest encryption for MongoDB Atlas and Delta Lake. Apply field-level encryption for PII, TLS/mTLS between servic",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Remove the hard-coded API key and implement OAuth 2.0 PKCE for mobile apps (or short-lived tokens with refresh tokens). Route auth through an API gateway that validates JWTs, enforce audience/issuer c",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant keys stored in an HSM to sign outbound payloads and verify them at ingress, enforce nonce+timestamp replay protection, and minimize endpoints per tenant. Implement tamper-evident loggin",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design a scalable, auditable security model for data lakes and multi-tenant access.\n\n## Key Concepts\n\n- Tenant isolation across data lake and databases\n- Envelope encryption and KMS integration\n- Fine-grained access control (ABAC/RBAC), policy engines (OPA/Unity Catalog)\n- Auditing, tamper-evident logs, key rotation, incident response\n\n## Code Example\n\n```yaml\npolicy:\n  effect: allow\n  action: read\n  resource: tenant:12345:data\n  condition:\n    tenant_id: { eq: request.tenant_id }\n```\n\n## Follow-up Questions\n\n- How would you monitor for data exfiltration in this setup?\n- Compare centralized vs per-tenant KMS key management trade-offs.",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-4",
        "questionId": "q-8079",
        "question": "You operate a multi-tenant edge compute platform where tenants upload WebAssembly modules to run at the edge. Design a secure, scalable module loading and execution pipeline that prevents supply-chain and runtime tampering. Include per-tenant key management, module signing, SBOM verification, reproducible builds, a trusted registry, runtime isolation, attestation, revocation, and an incident-response playbook. Provide concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design uses envelope encryption: each tenant has a unique Data Encryption Key (DEK) wrapped by a Central Master Key stored in a FIPS-140-2 HSM/KMS. Use per-tenant IAM roles, no shared DEKs, and automa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-namespace RBAC by restricting service accounts to their own namespace, removing any cluster-wide roles. Use Secrets Store CSI with Vault or AWS Secrets Manager to mount tenant-scoped sec",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a hardware-rooted trust (TPM/TEE) per tenant; sign each WASM module with Ed25519 and verify an SBOM (CycloneDX). Enforce reproducible builds, run modules in a strict WASM sandbox (Wasmtime/WasmEdg",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-tenant keys stored in an HSM to sign outbound payloads and verify them at ingress, enforce nonce+timestamp replay protection, and minimize endpoints per tenant. Implement tamper-evident loggin",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical secure edge module loading, supply-chain controls, and runtime attestation in a multi-tenant setting.\n\n## Key Concepts\n- Provenance: module signing and SBOM validation\n- Trust: per-tenant keys and hardware-backed attestation\n- Isolation: WASM sandboxing and strict runtime policies\n- Operability: reproducible builds, revocation, and incident response\n\n## Code Example\n```javascript\n// Sign module bytes with key\nfunction signModule(bytes, key) {\n  return 'sig';\n}\n```\n\n## Follow-up Questions\n- How would you rotate tenant keys without downtime?\n- How would you detect and respond to a compromised module at the edge?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-5",
        "questionId": "q-25566",
        "question": "Scenario: A cross-platform Electron app stores user notes locally and syncs with a cloud backend. Post-crash forensics show data stored unencrypted on disk. Propose a beginner-friendly plan to enforce encryption at rest and secure key management, using the OS keychain for keys, plus password-based key derivation. Include concrete steps and a minimal code snippet showing how to derive a key from a password and encrypt a local note file?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a layered guard: set a max query depth (4–5), a cost model (max cost ~1000), and enable persisted queries to prevent ad-hoc bursts. Use a gateway (e.g., Apollo Gateway) to enforce these rule",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Argon2id to derive a per-user AES-256 key from the password; store the salt with the ciphertext and keep the key in the OS keychain. Encrypt each note with AES-GCM before writing to disk and decry",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Sign each log line with a per-event signature using a KMS-backed key, and hash-chain entries to form a tamper-evident ledger. Ingestor validates signatures and hashes, raising alerts on mismatches. St",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Make all session cookies HttpOnly and Secure, and set SameSite to Lax with tenant-scoped domains. Express: app.use(session({ cookie: { httpOnly: true, secure: true, sameSite: 'Lax', domain: '.tenant.e",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical, beginner-friendly encryption practices that map to real-world apps without backend changes.\n\n## Key Concepts\n- Encryption at rest with AES-GCM\n- Password-based key derivation (Argon2id)\n- OS-level key storage (Keychain/Keystore)\n- Key rotation and re-encryption on login\n\n## Code Example\n```javascript\n// Derive key from password and encrypt a note (simplified)\nconst crypto = require('crypto');\nfunction deriveKey(password, salt) {\n  // Argon2id in real code; use placeholder PBKDF2 for illustration\n  return crypto.pbkdf2Sync(password, salt, 100000, 32, 'sha256');\n}\nfunction encryptNote(key, plaintext) {\n  const iv = crypto.randomBytes(12);\n  const cipher = crypto.createCipheriv('aes-256-gcm', key, iv);\n  const enc = Buffer.concat([cipher.update(plaintext, 'utf8'), cipher.final()]);\n  const tag = cipher.getAuthTag();\n  return { iv: iv.toString('hex'), data: enc.toString('hex'), tag: tag.toString('hex') };\n}\n```\n\n## Follow-up Questions\n- How would you rotate keys with minimal downtime?\n- What telemetry would you collect to detect misuses of the keychain?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-6",
        "questionId": "q-4433",
        "question": "You're integrating a third-party webhook (e.g., payment notifications) into a service used by users and apps. Describe a secure webhook verification pipeline using HMAC signatures and replay protection, including secret management, timestamp checks, nonce handling, and how you would validate payload integrity across deployments and secret rotations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a tamper-evident logging stack: per-tenant signing with rotated KMS keys, cryptographic chaining of log entries (hash(prev) + payload), append-only immutable storage (S3 Object Lock or similar)",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-endpoint shared secret stored in a vault and rotate it regularly. Validate every webhook by computing an HMAC-SHA256 over the raw body and comparing it to the signature header. Enforce repla",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a multi-region envelope encryption design with per-tenant data keys (DEKs) wrapped by a master key (CMK). Use AWS KMS or Vault with HSM-backed CMKs; rotate DEKs quarterly and CMKs every 12-24",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-plugin isolation by running each WebAssembly module in a dedicated, memory-isolated sandbox with strict system-call denylisting and a minimal bridge API. Enforce capability-based access using per-plugin da…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical webhook security knowledge, including HMAC verification, replay protection, and secret lifecycle in real deployments.\n\n## Key Concepts\n\n- HMAC validation of the raw request body\n- Replay protection using nonces or timestamp windows\n- Secret management and rotation strategies\n- Handling clock skew and payload integrity checks\n- Observability and failure handling\n\n## Code Example\n\n```javascript\n// Example Node.js verification (Express-like pseudo-code)\nconst crypto = require('crypto');\n\nfunction verifyWebhook(req, secret) {\n  const body = req.rawBody; // raw body bytes\n  const signature = req.headers['x-signature'];\n  const h = crypto.createHmac('sha256', secret).update(body).digest('hex');\n  return crypto.timingSafeEqual(Buffer.from(h), Buffer.from(signature));\n}\n```\n\n## Follow-up Questions\n\n- How would you test replay protection under burst traffic or out-of-order deliveries?\n- How would the approach change if the provider signs with RSA instead of an HMAC secret?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-7",
        "questionId": "q-373",
        "question": "How would you implement a comprehensive defense-in-depth strategy to prevent SQL injection attacks in a modern web application following OWASP Top 10 guidelines?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use envelope encryption with per-tenant data keys wrapped by region-bound KEKs stored in HSMs. Rotate data keys quarterly and KEKs annually; version metadata to enable algorithm upgrades without downt",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement parameterized queries with ORMs like Prisma/TypeORM, use input validation with libraries like Joi/Zod, apply least-privilege database principles, and enable query logging. Combine prepared statements, stored p…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Cosign for SBOM signing/verification and Sigstore signing; enforce reproducible builds (BuildKit, pinned deps) and per-tenant keys in an HSM-backed KMS (CloudHSM/Azure Dedicated HSM) with envelope",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce per-tenant IaC isolation: module signing and a mandatory Terraform plan review; use tenant-scoped remote state encrypted at rest by Vault; deploy Vault dynamic secrets with short leases and au",
            "isCorrect": false
          }
        ],
        "explanation": "## Interview Context\nThis question tests your understanding of secure coding practices and defense-in-depth strategies for database security.\n\n## Key Points\n- **Primary Defense**: Parameterized queries prevent malicious SQL execution\n- **ORM Benefits**: Prisma, TypeORM, Sequelize provide built-in injection protection\n- **Input Validation**: Server-side validation using Joi/Zod before database operations\n- **Least Privilege**: Separate read/write users with minimal permissions\n- **Error Handling**: Generic error messages prevent database schema disclosure\n\n## Code Example\n```typescript\n// Secure approach with Prisma\nconst getUser = async (email: string) => {\n  const validatedEmail = emailSchema.parse(email);\n  return await prisma.user.findUnique({\n    where: { email: validatedEmail }\n  });\n};\n\n// Input validation schema\nconst emailSchema = z.string().email().max(255);\n```\n\n## Follow-up Questions\n1. How would you handle dynamic queries that require flexible conditions?\n2. What monitoring would you implement to detect potential injection attempts?\n3. How do you balance security requirements with application performance?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-8",
        "questionId": "q-2878",
        "question": "In a production analytics pipeline (Kafka -> Flink -> ClickHouse), you need to protect PII with differential privacy. Propose concrete steps to implement per-event DP budgets, where to inject noise, how to enforce budgets, and how to validate privacy guarantees in staging. Include concrete config hooks and trade-offs between accuracy and privacy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement device-bound passkeys with short-lived JWTs, rate limiting, and distributed caching. Use WebAuthn with hardware-backed keys and nonce-based request signing.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Switch to per-user salted hashes using Argon2id (memory-cost) or bcrypt with cost 12. Migrate in batches: add a marker column, rehash on login for older hashes, and resync background worker until comp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement DP in the streaming layer: clip per-event values, apply Laplace noise to counts/averages in Flink before persisting to ClickHouse. Track privacy budget per user in Redis; on exhaustion, supp",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Require tenant_id consistency: middleware verifies that the JWT's tenant_id matches the resource's tenant_id in every endpoint; reject otherwise. Enable PostgreSQL RLS with policies on all tables to f",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\\n\\nTests ability to apply differential privacy to streaming analytics, including budget accounting, data minimization, and production-grade validation in a security-conscious pipeline.\\n\\nKey Concepts\\n\\n- Differential privacy, epsilon budgeting, and clipping\\n- Noise mechanisms (Laplace/Geometric) in streaming aggregates\\n- Budget ledger, per-user enforcement, and fallback modes\\n- Encryption at rest/in transit and auditability\\n\\nCode Example\\n\\n```javascript\\nfunction addLaplaceNoise(x, b){\\n  return x + (Math.random()*2-1)*b\\n}\\n```\\n\\nFollow-up Questions\\n\\n- How would you CI-test privacy budgets with synthetic data?\\n- How to handle multi-tenant budgets across feature flags?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-9",
        "questionId": "q-17515",
        "question": "Scenario: A cloud telemetry pipeline collects real-time car sensor data and third-party plugin output from millions of vehicles. Data is sensitive; tenants (brands) must be isolated; design an end-to-end secure ingestion and analytics stack including edge attestation, per-tenant keys, envelope encryption, data minimization, tamper-evident logs, access controls, and an incident response plan with rotation and rollback. Include concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement RSA-2048/OAEP with SHA-256 MGF1 to encrypt a randomly generated AES-256-GCM key, using the AES key to encrypt payment data with unique IVs per transaction, storing RSA private keys in HSM-backed key vaults wit…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Edge attestation with mutual TLS for secure ingestion; per-tenant encryption keys with envelope encryption; tenant-isolated data partitions; field-level PII masking for data minimization; tamper-evident logs using hash…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "End-to-end image supply chain plan: enforce SBOM generation at build time and pin base images to immutable references; sign all images with per-tenant KMS keys via cosign, implementing automated key rotation; enforce pr…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a defense-in-depth strategy using explicit tenant-scoped bucket policies, least-privilege IAM roles with short-lived credentials, and automated CI checks to detect policy drift. **1. Path-Prefix Isolation** En…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to design end-to-end security for a large-scale telemetry pipeline with strict tenant isolation and privacy guarantees. It requires deep knowledge of attestation protocols, envelope encryption, log integrity mechanisms, and incident response planning.\n\n## Key Concepts\n\n- Edge attestation and mutual TLS for trusted ingestion points\n- Tenant isolation with per-tenant encryption keys\n- Envelope encryption and data minimization techniques\n- Tamper-evident logs with verifiable provenance\n- Role-based access control models and automated key rotation\n- Incident response planning with rollback capabilities\n\n## Trade-offs\n\n- **Performance vs. Security**: Mutually authenticated TLS adds latency; consider connection pooling and hardware security modules for key operations.\n- **Complexity vs. Isolation**: Per-tenant partitions increase operational overhead but provide stronger isolation guarantees.\n- **Data Utility vs. Privacy**: Field-level masking balances analytical value against PII exposure risk.\n- **Auditability vs. Volume**: Hash-chained logs grow linearly with events; implement efficient Merkle tree structures for compact verification.",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-10",
        "questionId": "q-22694",
        "question": "In a cloud-native, multi-tenant security analytics platform, ingestion and processing of tenant telemetry occurs across a shared data lake and compute layer. A misconfigured IAM policy could leak other tenants' data. Outline a concrete, end-to-end plan to enforce strict tenant isolation: data partitioning, per-tenant keys, envelope encryption, access control, audit logging, tamper-evident logs, and an incident response playbook including rollback and revocation. Include specific services and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition data by tenant using per-tenant prefixes in the data lake and enforce envelope encryption with per-tenant keys via KMS/HSM. Require mTLS between services, least-privilege IAM, and per-tenant",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design focuses on per-tenant boundaries: dedicated namespaces, partitioned storage, tenant-scoped keys from KMS, mTLS between services, JWT scopes per tenant, TEEs for inference, SBOM-driven CI checks",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a runtime boundary that injects tenant-scoped predicates and column masking at query time. Use a central, versioned Policy Store to drive per-tenant ACLs, and extend the query planner to rew",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Sign each eBPF program with a tenant-scoped KMS key and verify with the kernel verifier before loading. Isolate tenants using separate cgroups/namespaces and per-tenant maps; prune helper functions to",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical tenant isolation in a cloud-native security analytics stack, not just theory.\n\n## Key Concepts\n\n- Tenant isolation in data lake and compute planes\n- Envelope encryption with per-tenant keys (KMS/HSM)\n- Fine-grained IAM, per-tenant ACLs, and data catalog controls\n- Tamper-evident logging and immutable audit trails\n- Incident response: key revocation, token rotation, rollback\n\n## Code Example\n\n```json\n{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [\n    {\n      \"Effect\": \"Allow\",\n      \"Action\": [\"s3:GetObject\"],\n      \"Resource\": [\"arn:aws:s3:::tenant-*/*\"],\n      \"Condition\": {\"StringEquals\": {\"aws:PrincipalTag/tenant\": \"${TenantId}\"}}\n    }\n  ]\n}\n```\n\n## Follow-up Questions\n\n- How would you validate tenant isolation boundaries in CI/CD with automated tests?\n- What metrics signal potential cross-tenant access and how would you respond?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-11",
        "questionId": "q-15263",
        "question": "You're operating a multi-tenant SaaS that allows customers to upload and run plugins inside sandboxed environments (WASM or isolated containers) to automate workflows. A compromised plugin could exfiltrate data or break tenant isolation. Propose a concrete, end-to-end hardening plan covering sandboxing model, API surface minimization, per-tenant isolation, plugin signing and revocation, runtime monitoring with tamper-evident logs, and an incident-response playbook including rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Replace the compromised global API key with per-device access tokens using short lifetimes (5-15 minutes) and secure refresh tokens; implement PKCE for mobile/SPA authentication flows; bind tokens to device fingerprints…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a strict sandboxed plugin model: run each plugin in a WASM sandbox per-tenant with a denied host API surface; expose only a minimal plugin API and enforce namespace isolation. Require plugins to",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Adopt structured JSON logs and mask sensitive fields at emission, avoiding raw payload logging. Use a reusable redact function that replaces sensitive data (e.g., emails, API keys, passwords) with masked values like `**…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a comprehensive chain of trust for multi-language serverless stacks: generate SBOMs during build processes, sign all artifacts with Sigstore for cryptographic attestations, verify signatures before deployment,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design secure plugin architectures and threat modeling for untrusted code.\n\n## Key Concepts\n- Sandboxing WASM/per-tenant isolation\n- Least-privilege API surface\n- Code signing and KMS-based key rotation\n- Audit/log tamper-evidence\n- Incident response with rollback and tenant revocation\n\n## Code Example\n```javascript\n// Verify plugin signature using Node.js crypto\nconst crypto = require('crypto');\nfunction verify(sig, payload, publicKey) {\n  const verify = crypto.createVerify('SHA256');\n  verify.update(payload); verify.end();\n  return verify.verify(publicKey, sig, 'base64');\n}\n```\n\n## Follow-up Questions\n- How would you test plugin security under CI/CD?\n- What trade-offs exist between WASM sandbox vs full container?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-12",
        "questionId": "q-12255",
        "question": "For a global, multi-tenant streaming analytics platform ingesting Airbnb-like user events and Two Sigma-like market data, design a secure data boundary for the data lake. Requirements: strict tenant isolation, per-tenant keys with envelope encryption (KMS/HSM), data localization by region, in-transit and at-rest encryption, end-to-end data provenance and tamper-evidence, automated per-tenant key rotation, and an incident playbook with revocation and recovery steps. Include concrete components, data flow, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a layered guard: set a max query depth (4–5), a cost model (max cost ~1000), and enable persisted queries to prevent ad-hoc bursts. Use a gateway (e.g., Apollo Gateway) to enforce these rule",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant envelope encryption with region-local KMS/HSMs; streams encrypted in transit (TLS) and at rest (AES-256-GCM); per-tenant keys rotated automatically; data locality enforced by region-bound s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use short-lived access tokens (5 minutes) with refresh tokens, rotate signing keys daily and on incident, and add a revocation check at the gateway. Disable refresh for compromised sessions. Add a CI",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage per-tenant isolation via data partitioning and namespace scoping; use tenant-scoped envelope encryption with KMS keys; run inference inside Nitro/Azure Confidential Compute enclaves with atte",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nInterviews require showing ability to design secure multi-tenant data boundaries with practical constraints like locality, key management, and incident response in streaming pipelines.\n\n## Key Concepts\n- Envelope encryption with per-tenant keys\n- Region-local KMS/HSM\n- Data localization and tokenization\n- Audit logs and tamper evidence\n- Key rotation and revocation\n- Incident response and re-encryption\n\n## Code Example\n\n```python\n# Pseudocode for wrapping a data key with a KMS\ndef wrap_key(data_key, tenant_id, region):\n    kms = get_kms_for_region(region)\n    return kms.wrap_key(data_key, context=f'tenant:{tenant_id}:region:{region}')\n```\n\n## Follow-up Questions\n- How would you test tenant isolation under burst traffic?\n- What metrics reveal rotation latency impact?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-13",
        "questionId": "q-11012",
        "question": "In a high-scale, multi-tenant chat service that supports third-party plugins, design a complete runtime security model to prevent data leakage from compromised plugins. Include per-plugin attestation, hardware-backed key material, per-plugin isolation (WASM or microVM), strict IPC, dynamic revocation, and an incident runbook; specify technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Serverless image-processing pipeline: enforce per-user encryption using envelope encryption with a KMS CMK. API Gateway authenticates; Lambda calls GenerateDataKey for a per-user data key (DEK), uses",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement policy signing with per-tenant keys stored in an HSM; have the gateway fetch the latest signed policy from CPS over mutual TLS; verify with the tenant’s public key; enforce per-tenant RLS in",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a runtime plugin sandbox using WebAssembly/WASI or Firecracker microVMs for per-plugin isolation. Deploy TPM/HSM-backed attestation with per-plugin digital signatures to cryptographically verify each plugin be…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: model cross-tenant leakage via cache/memory. Isolate each tenant in a microVM (Firecracker) or HW enclave (TDX/SNP). Disable memory dedup; enable KPTI; enforce per-tenant address spaces; strict",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical design for plugin trust, isolation, and incident response in a multi-tenant service.\n\n## Key Concepts\n- Plugin isolation (WASM/microVM)\n- Attestation and TPM/HSM roots\n- Per-plugin key management\n- Service mesh mTLS/SPIFFE\n- Dynamic revocation and audits\n- Incident runbook\n\n## Code Example\n```javascript\n// Pseudo: attestation flow for a loaded plugin\nfunction loadPlugin(p) {\n  const capsule = attest(p); // sign/verify capsule with TPM/HSM\n  if (!capsule.valid) throw new Error('Attestation failed')\n  sandbox.run(p) // WASM or microVM\n}\n```\n\n## Follow-Up",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-14",
        "questionId": "q-21385",
        "question": "In a cloud security platform offering customer-managed encryption keys (CMEK) across multi-region deployments, design an end-to-end incident response for a suspected key compromise. Include detection, key revocation, regional rotation, data re-encryption, access policy updates, audit logging, and post-incident steps; specify technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a TUF-based update framework with per-device attestation via TPM/Secure Element, delivering cryptographically signed delta payloads to edge devices. Utilize A/B partitioning for atomic over-the-air updates wit…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Detect anomalies in CMEK usage (cloud logs, HSM attestation). Immediately revoke affected CMKs, rotate regionally, and issue new KEKs. Re-encrypt all tenant data with fresh CMKs, updating envelope key",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose an end-to-end policy distribution and enforcement pipeline: a central policy store signs each policy, a distributor pushes updates per tenant, enforcement runs in the workload safe-guarded nam",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Require signed plugin attestations (Sigstore/cosign) plus SBOMs; verify at load against a trusted root; sandbox plugins with WASM engine (Wasmtime) with per-plugin quotas and sandboxed syscalls; isola",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical end-to-end handling of encryption key compromise, a high-severity incident that spans IAM, KMS, storage, and data planes.\n\n## Key Concepts\n- CMEK lifecycle and rotation\n- Cross-region key management\n- Data re-encryption strategies (streaming, offline)\n- Tamper-evident audit trails\n- Incident response sequencing\n\n## Code Example\n```javascript\n// Pseudo API usage: rotateKey and reencryptData\nasync function rotateAndReencrypt(keyId) {\n  await kms.rotate(keyId);\n  await storage.reencryptAll(keyId);\n}\n```\n\n## Follow-up Questions\n- What are performance implications of re-encryption?\n- How to test rotation safely in prod without downtime?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-15",
        "questionId": "q-23046",
        "question": "You're operating a multi-tenant AI inference platform where tenants upload their own model artifacts and data for shared GPU inference. A poisoned artifact could exfiltrate tenant data or poison other tenants' models. Propose an end-to-end security plan addressing provenance and signing of artifacts, hardware-backed loading attestation, per-tenant isolation, SBOM enforcement, runtime checks, tamper-evident logs, leakage/poisoning detection, credential management, and an incident-response playbook including rollback. Include concrete technologies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design uses envelope encryption: each tenant has a unique Data Encryption Key (DEK) wrapped by a Central Master Key stored in a FIPS-140-2 HSM/KMS. Use per-tenant IAM roles, no shared DEKs, and automa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement tenant-scoped RBAC in the API, use per-tenant views/tokenized PII in MongoDB (field-level encryption), enable dynamic masking at query time for dashboards, add CI checks to reject any PII pr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "An end-to-end security plan for a multi-tenant AI inference platform should incorporate artifact signing and provenance verification using Sigstore or cosign, combined with hardware-backed attestation at load time via T…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Threat model and threat sources. Use Bonawitz secure aggregation with per-round masking; enforce differential privacy budgets per client; require remote attestation and device-bound keys for updates;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates end-to-end artifact security in a multi-tenant AI inference platform, testing understanding of the full security lifecycle from artifact ingestion through runtime monitoring and incident response.\n\n## Key Concepts\n- **Artifact provenance and cryptographic signing** ensure artifacts originate from trusted sources and have not been tampered with during storage or transit.\n- **Hardware-backed attestation** (TPM/HSM) verifies the integrity of the execution environment before loading tenant artifacts, ensuring that only trusted code runs on trusted hardware.\n- **Per-tenant isolation** with namespace segmentation prevents cross-tenant data leakage and ensures that malicious tenants cannot access or poison other tenants' models or data.\n\n## Technical Components\n- **Sigstore/cosign** for artifact signing and verification\n- **TPM 2.0 or AWS Nitro Enclaves** for hardware-backed attestation\n- **Kubernetes namespaces and network policies** for per-tenant isolation\n- **OPA/Gatekeeper** for policy enforcement\n- **SBOM tools** (Syft, SPDX) for dependency tracking\n- **Immutable logging** (e.g., AWS CloudTrail, Google Audit Logs) for tamper-evident audit trails\n- **HSM-backed key storage** (AWS KMS, HashiCorp Vault) for credential management\n\n## Trade-offs\n- Attestation checks introduce latency but significantly reduce attack surface\n- Multi-tenant key management adds operational complexity\n- SBOM maintenance requires ongoing effort at scale",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-16",
        "questionId": "q-8984",
        "question": "In a cloud service that runs user-provided ML models on a shared GPU pool for real-time inference, design a security architecture that prevents cross-tenant data leakage and model theft. Your plan must include hardware enclave usage (TDX/SEV or equivalent), envelope encryption with per-tenant keys, attestation, tamper-evident logging, secure updates, and an incident response workflow. Also discuss latency and cost trade-offs and deployment steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "OAuth 2.0 handles authorization with access tokens (Bearer format) for resource access, while OIDC adds authentication layer with ID tokens (JWT) containing user identity. OIDC introduces standard scopes (openid, profil…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-tenant GPU inference stack using hardware enclaves (Intel TDX/AMD SEV). Enforce memory isolation, disable dedup, and wrap each tenant’s model with envelope encryption managed by a KMS/HSM",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Adopt a centralized secret manager (Vault or cloud KMS) with short-lived credentials. Use a sidecar injector to refresh creds in pods, and implement a controlled rolling restart on rotation. Add a lig",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce end-to-end supply chain controls: require SBOMs and CVE scans in CI, sign artifacts with Cosign/Sigstore, pin to content digests, enforce reproducible builds, enable runtime attestation with O",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes threat modeling for multi-tenant GPU inference, balancing isolation, performance, and incident response. It requires concrete mechanisms rather than high-level talk.\n\n## Key Concepts\n- Hardware enclaves: Intel TDX, AMD SEV\n- Envelope encryption, per-tenant keys, KMS/HSM\n- Attestation, tamper-evident logging\n- Side-channel risk minimization, secure update\n- Incident response playbooks\n\n## Code Example\n```javascript\n// Example: mock policy to select tenant key and verify attestation\nfunction getTenantKey(tenantId, attestation) {\n  // in real life: fetch from KMS/HSM\n  if (!verifyAttestation(attestation, tenantId)) throw new Error(\"Attestation failed\");\n  return fetchTenantKeyFromKMS(tenantId);\n}\n```\n\n## Follow-up Questions\n- How would you verify attestation with a cloud provider's SEV/TDX implementation?\n- What telemetry would you collect to detect late-stage enclave compromise?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-17",
        "questionId": "q-22530",
        "question": "Design an end-to-end security plan for a new offline-capable POS component used by merchants in Square's ecosystem that can generate transaction cryptograms offline and later sync to the payment network. Specifically, outline per-merchant key provisioning, secure element usage, offline data handling, replay protection, signing/verification, device attestation, tamper-evident logs, secure OTA updates, and incident response with key revocation and rollback. Which technologies would you choose and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant sandbox using WASM with a limited API surface; isolate plugins in lightweight containers or user namespaces; enforce model provenance via signing in a trusted registry; require run",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a per-tenant WASM runtime with strict sandboxing (Wasmtime) and memory isolation; require module signing and remote attestation against an HSM-backed root. Use per-tenant keys in the HSM, SP",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-merchant signing keys reside in a hardware Secure Element (SE), with device attestation required before each use. Offline cryptograms are generated within the SE using a nonce and monotonic counter, then signed loca…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Enforce tenant-scoped signaling namespaces, per-room ACLs at the gateway, and mTLS with short-lived JWTs for room access. Encrypt signaling in transit (TLS) and at rest (envelope encryption with KMS).",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked: This question evaluates real-world handling of offline capability and hardware-backed security in a high-stakes payment flow. It requires threat modeling, per-merchant key management, attestation, replay protection, secure updates, and incident response—concepts directly relevant to Square and Apple.\n\n## Key Concepts\n- Hardware Security Modules and Secure Elements\n- Attestation and device identity\n- Replay protection using nonces and monotonic counters\n- Secure OTA with dual signing\n- Tamper-evident logging and SBOM verification\n- Incident response and key revocation\n\n## Code Example: Device Attestation Flow\n\n```python\n# Simplified attestation flow\ndef verify_attestation(attestation_cert, nonce):\n    # Verify certificate chain to trusted root\n    if not verify_certificate_chain(attestation_cert):\n        return False, \"Invalid certificate chain\"\n    \n    # Verify attestation signature\n    signature = attestation_cert.signature\n    if not verify_ecdsa_signature(attestation_cert.public_key, nonce, signature):\n        return False, \"Invalid signature\"\n    \n    return True, \"Attestation verified\"\n\ndef generate_offline_cryptogram(secure_element, transaction_data):\n    # Get current monotonic counter\n    counter = secure_element.get_monotonic_counter()\n    \n    # Generate nonce\n    nonce = secure_element.generate_random(16)\n    \n    # Create cryptogram payload\n    payload = {\n        \"transaction_data\": transaction_data,\n        \"counter\": counter,\n        \"nonce\": nonce,\n        \"timestamp\": get_current_timestamp()\n    }\n    \n    # Sign within Secure Element\n    signature = secure_element.sign(payload)\n    \n    return {\n        \"cryptogram\": payload,\n        \"signature\": signature\n    }\n```\n\n## Deep Dive: Architecture Components\n\n### 1. Per-Merchant Key Provisioning\n- Each merchant receives a unique signing key pair injected during manufacturing or onboarding\n- Keys are derived from a master key using merchant ID as diversification input\n- Private keys never leave the Secure Element\n- Key injection occurs in secure facility with HSM support\n\n### 2. Secure Element Usage\n- SE provides isolated execution environment for cryptographic operations\n- Stores per-merchant keys and handles all signing operations\n- Implements tamper detection (voltage, temperature, glitching)\n- Supports multiple cryptographic algorithms (ECDSA, AES)\n\n### 3. Offline Data Handling\n- Transactions are stored in encrypted local storage when offline\n- Each transaction gets a unique identifier and timestamp\n- Data structure includes: transaction amount, merchant ID, terminal ID, nonce, counter\n- Maximum offline transaction limit enforced based on risk assessment\n\n### 4. Replay Protection\n- Monotonic counter ensures each transaction has a unique incrementing value\n- Backend maintains counter state per device\n- Counter overflow handling with key rotation\n- Nonces prevent replay within same counter epoch\n\n### 5. Signing and Verification\n- ECDSA P-256 for digital signatures\n- Each cryptogram signed with merchant's private key inside SE\n- Backend verifies signature before processing\n- Atomic transaction processing ensures no partial states\n\n### 6. Device Attestation\n- Device identity certificate issued during manufacturing\n- Attestation key derived from device unique identifier (UDID)\n- Certificate chain validated against trusted root\n- Challenge-response protocol prevents replay of attestations\n\n### 7. Tamper-Evident Logs\n- Immutable log entries using hash chaining\n- Each entry includes: previous hash, timestamp, event type, metadata\n- Logs stored both locally and synced to backend\n- Regular integrity checks detect tampering\n\n### 8. Secure OTA Updates\n- Dual signing: manufacturer + vendor keys\n- Update package includes: firmware, manifest, signatures\n- Secure boot chain validates each stage\n- Rollback protection prevents downgrades to vulnerable versions\n\n### 9. Incident Response\n- Real-time monitoring for anomalous patterns\n- Key revocation list distributed to all processing nodes\n- Compromised devices can be remotely deactivated\n- Automatic key rotation on suspected compromise\n- Rollback capability for transaction reversal if needed\n\n## Threat Model Considerations\n\n| Threat Vector | Mitigation |\n|--------------|------------|\n| Physical attacks on SE | Tamper detection, zeroization on breach |\n| Key extraction | Keys never exposed outside SE |\n| Replay attacks | Nonces + monotonic counters |\n| Man-in-middle | TLS 1.3 for all sync communications |\n| Rogue devices | Device attestation required |\n| Rollback attacks | Secure boot + signed updates |\n| Insider threats | HSM-based key injection, separation of duties |\n\n## Technologies Selected\n\n| Component | Technology | Rationale |\n|-----------|-----------|----------|\n| Secure Element | ARM TrustZone + Secure Core | Industry standard, hardware-assisted isolation |\n| Signing Algorithm | ECDSA P-256 | NIST approved, efficient for embedded |\n| Key Diversification | HKDF | Standard key derivation function |\n| Attestation | FIDO Device Attestation | Open standard, broad ecosystem support |\n| Secure Storage | Hardware-encrypted flash | Defense in depth |\n| Transport | TLS 1.3 | Latest protocol with forward secrecy |\n| Log Integrity | Hash chaining (Merkle trees) | Tamper-evident without centralized trust |\n| Update Signing | ED25519 + RSAPSS | Dual signature scheme for extra security |",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-18",
        "questionId": "q-7155",
        "question": "In a multi-tenant edge security gateway for a real-time chat service, propose a concrete zero-trust design enforcing per-tenant identities, short-lived tokens, mTLS, hardware-backed attestation, and artifact integrity checks at runtime. Include technology choices and trade-offs, and how you would test revocation and incident response?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant SPIFFE IDs with short-lived JWTs (15 min) signed by a private CA; enforce mutual TLS between edge and origin via SPIRE; store keys in TPM/HSM and sign edge code; verify SBOM and perform",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "OAuth 2.0 handles authorization with access tokens (Bearer format) for resource access, while OIDC adds authentication layer with ID tokens (JWT) containing user identity. OIDC introduces standard scopes (openid, profil…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a dual-signed OTA with hardware-backed attestation. Include SBOM and reproducible-build hash in the package; devices verify both against a trusted root and TPM/TEE attestation. Deliver delta",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement tenant-scoped envelope encryption with an HSM-backed KMS (Vault/AWS KMS) and per-tenant keys, ensure mTLS with SPIFFE, and strictly isolate brokers and processors via namespace and network p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes zero-trust at the edge, tenant isolation, and practical mitigations. It tests choice of SPIFFE/SPIRE, mTLS, HSM, SBOM, attestation, and incident playbooks. It also surfaces trade-offs between latency, security, and operability.\n\n## Key Concepts\n- Zero-trust at edge\n- Per-tenant identity (SPIFFE/SPIRE)\n- Short-lived tokens (JWT)\n- mTLS between edge and origin\n- TPM/HSM-backed keys and code signing\n- Runtime attestation and SBOM checks\n- Key rotation and revocation (OCSP/CRLs)\n- Logging and incident response\n\n## Code Example\n```javascript\n// Token rotation config (pseudo)\nconst TOKEN_TTL_MS = 15 * 60 * 1000;\nfunction shouldRotate(last) { return (Date.now() - last) > TOKEN_TTL_MS; }\n```\n\n## Follow-up Questions\n- How would you measure revocation latency across global edge nodes?\n- What are the trade-offs between token lifetime, replay risk, and availability?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-19",
        "questionId": "q-8612",
        "question": "Scenario: a public webhook receiver for a SaaS product must accept events from many partners, each sending a secret key. A tester replays an old webhook and triggers duplicate actions. Propose a beginner-friendly defense plan that relies on idempotency keys, per-event nonces, a short replay window, and verifiable signatures; outline changes to the receiver, testing, and rollback steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-event nonces and idempotency keys, sign payloads with HMAC-SHA256 using a partner-secret and a timestamp, reject duplicates within a short window (e.g., 5 minutes). Enforce replay protec",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "First, identify public access using S3 API commands like `get-bucket-policy` and `get-bucket-acl`. Then, remove any public ACLs and enable the S3 Block Public Access setting at both the bucket and account levels. Next,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-namespace RBAC by restricting service accounts to their own namespace, removing any cluster-wide roles. Use Secrets Store CSI with Vault or AWS Secrets Manager to mount tenant-scoped sec",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant API keys with versioned signing (v1/v2) using a two-phase rollout: emit v2 keys while maintaining v1 during a grace period, validate both versions via JWKS, then decommission v1 after migration.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess practical webhook security basics: replay protection, signatures, and minimal state.\n\n## Key Concepts\n- Webhook signing and HMAC\n- Idempotency keys and nonce tracking\n- Replay window and TTL\n- Secret rotation and observability\n\n## Code Example\n```javascript\n// Example: verify HMAC and replay\nconst crypto = require('crypto');\nfunction verify(payload, signature, secret, nonce, redis) {\n  const h = crypto.createHmac('sha256', secret).update(payload).digest('hex');\n  if (h !== signature) return false;\n  // idempotency: ensure nonce not seen\n  const seen = redis.setnx(`nonce:${nonce}`, '1');\n  if (!seen) return false;\n  redis.expire(`nonce:${nonce}`, 300);\n  return true;\n}\n```\n\n## Follow-up Questions\n- How would you handle clock skew between partners?\n- How to test replay protections in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-20",
        "questionId": "q-22728",
        "question": "Scenario: A browser-extension component provides per-tenant encryption keys and signs sensitive API requests to the backend for a security dashboard used by tenants at LinkedIn, Square, and Instacart. If the extension is compromised, data exfiltration is possible. Design an end-to-end hardening plan covering: per-tenant hardware-backed keys, envelope encryption, extension signing and integrity checks, tenant-scoped key rotation and revocation, runtime attestation, tamper-evident logs, and an incident-response playbook with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design uses envelope encryption: each tenant has a unique Data Encryption Key (DEK) wrapped by a Central Master Key stored in a FIPS-140-2 HSM/KMS. Use per-tenant IAM roles, no shared DEKs, and automa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enforce per-tenant IaC isolation: module signing and a mandatory Terraform plan review; use tenant-scoped remote state encrypted at rest by Vault; deploy Vault dynamic secrets with short leases and au",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant hardware-backed keys, envelope encryption, extension signing and integrity checks, tenant-scoped key rotation and revocation, runtime attestation, tamper-evident logs, and an incident-r",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant HMAC keys stored in an HSM with envelope encryption; rotate keys every 24 hours with versioning; require a nonce and timestamp in webhook payload and header; implement strict replay protect",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\n\nThis question probes practical client-side secret management, secure extension execution, and incident response coordination—not abstract theory.\n\nKey Concepts\n\n- Per-tenant hardware-backed keys and envelope encryption\n- Secure extension signing and integrity checks\n- Tenant-scoped key rotation and revocation\n- Runtime attestation from the extension to the backend\n- Tamper-evident append-only logs for extension activity\n- Incident response with revocation, key rotation, re-encryption, and rollback\n\nCode Example\n\n```javascript\n// Pseudocode for signing a request\nconst payload = JSON.stringify({tenantId, nonce, path});\nconst sig = signWithTenantKey(payload);\n// attach to header\n```\n\nFollow-up Questions\n\n- How would you detect a compromised extension in production?\n- What CI/CD tests would prevent regression and ensure key-rotation correctness?",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-21",
        "questionId": "q-25289",
        "question": "A payments webhook is delivered to a microservice behind a load balancer in a Kubernetes cluster. The provider signs payloads with a shared secret. How would you implement beginner-friendly webhook verification and replay protection, covering secret storage/rotation, signature verification, timestamp handling, replay detection, and basic tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Store the shared secret in a secrets manager and rotate it periodically. Fetch secrets at runtime with least-privilege access. Verify signatures with HMAC-SHA256 over timestamp||payload using the secr",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a token-binding approach in a multi-cluster mesh: require mTLS with SPIFFE IDs for all service calls, issue short-lived JWTs (5–10 minutes) bound to a per-call ephemeral key, rotate JWKS eve",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-field ABAC with a policy engine (OPA) at the GraphQL layer; enforce field-level ACLs in resolvers and generate filtered responses by default. Disable or gate schema introspection in prod",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use PKCE: generate a high-entropy code_verifier (43–128 chars), compute code_challenge = base64url(SHA-256(code_verifier)). Open the auth page with redirect_uri. After login, exchange the code at the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nWebhook security is a frequent, real-world task. This checks grasp of signature verification, replay protection, and secrets lifecycle at a beginner-friendly level.\n\n## Key Concepts\n- HMAC verification\n- Timestamp windows\n- Nonce/replay protection\n- Secrets management and rotation\n- Idempotent processing and observability\n\n## Code Example\n```javascript\n// Verify signature\nfunction verifyWebhook(secret, payload, timestamp, signature) {\n  const crypto = require('crypto');\n  const h = crypto.createHmac('sha256', secret);\n  h.update(timestamp + '|' + payload);\n  return h.digest('hex') === signature;\n}\n```\n\n```javascript\n// Pseudo replay check\nconst seen = getSeenStore(); // Redis-like\nif (seen.has(timestamp)) throw new Error('Replay detected');\nseen.add(timestamp);\n```\n\n## Follow-up Questions\n- How would you test this with unit/integration tests?\n- What are trade-offs of storing secrets in a central manager vs. in-cluster config?",
        "difficulty": "beginner"
      },
      {
        "id": "security-q-22",
        "questionId": "q-4321",
        "question": "In a Netflix-scale, multi-tenant Kubernetes platform, design a security solution that guarantees only images signed with a trusted Sigstore issuer and with verifiable SBOM attestations are deployed, while preserving developer velocity. Outline the CI/CD signing flow, admission controls (OPA/Kyverno), runtime attestation (SPIRE), secret management (Vault), rollback strategy, and measurement of effectiveness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement strict validation: check magic bytes for JPEG/PNG, confirm extension, reject executable mime types, cap size at 1 MB, save in a non-executable upload dir, use a randomized filename, sanitize",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement parameterized queries with ORMs like Prisma/TypeORM, use input validation with libraries like Joi/Zod, apply least-privilege database principles, and enable query logging. Combine prepared statements, stored p…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a CI/CD + runtime policy for a Netflix-scale multi-tenant Kubernetes cluster to ensure only images signed with a trusted Sigstore issuer are deployed, and each image has an SBOM attestation. Us",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Quarantine the affected image digest in the registry, halt pulls, and rollback to the last-good revision using blue/green deployment. Enforce image signing with Sigstore and admission control via OPA",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical expertise in modern software supply chain security for large-scale, multi-tenant Kubernetes environments. It requires knowledge of image signing, SBOM attestations, admission controls, runtime attestation, and dynamic secrets—core to securing platforms used by major services.\n\n## Key Concepts\n\n- Image signing and SBOM attestations (Sigstore, Cosign)\n- Admission controls (OPA, Gatekeeper, Kyverno)\n- Runtime attestation (SPIRE/SPIFFE)\n- Secrets management (Vault, short‑lived credentials)\n- Multi-tenant isolation, rollback, canary deployments\n- Observability and metrics for attestation success/failure\n\n## Code Example\n\n```rego\npackage kubernetes.admission\n\ndeny[{\"msg\": msg}] {\n  input.request.kind.kind == \"Pod\"\n  image := input.request.object.spec.containers[_].image\n  signed := data.signatures[image]\n  not signed\n  msg := sprintf(\"Image %q is not signed or lacks SBOM attestation\", [image])\n}\n```\n\n```yaml\napiVersion: kyverno.io/v1\nkind: ClusterPolicy\nmetadata:\n  name: require-signed-sbom\nspec:\n  rules:\n  - name: check-image-attestation\n    match:\n      resources:\n        kinds: [\"Deployment\",\"StatefulSet\"]\n    validate:\n      message: \"Each container image must be signed and SBOM-attested\"\n      pattern:\n        spec.template.spec.containers[*].image: \"*\"\n```\n\n## Follow-up Questions\n\n- How would you quantify SBOM coverage across all images?\n- What trade-offs exist between signing frequency and deployment velocity?",
        "difficulty": "advanced"
      },
      {
        "id": "security-q-23",
        "questionId": "q-25905",
        "question": "Design an end-to-end hardening plan for a real-time fraud-detection service that runs user-provided feature transformers inside WASM sandboxes processing streaming tenant data. Include sandbox boundaries, API surface minimization, per-tenant isolation in the streaming topology, transformer signing and revocation, runtime tamper-evident logs, and an incident-response playbook with rollback and data repair. Provide concrete trade-offs and validation steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant keys using envelope encryption, with data keys signed by an HSM-backed master key. Employ Vault Transit or cloud KMS with per-tenant Key Encryption Keys (KEKs), implementing scheduled rotation and emergency r…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a strict WASM sandbox: limit syscalls, cap memory, enforce a narrow API surface, and reject any transformer attempting to breach boundaries. Isolate tenants via dedicated streams with strict data routing and netwo…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement tenant-scoped envelope encryption with an HSM-backed KMS (Vault/AWS KMS) and per-tenant keys, ensure mTLS with SPIFFE, and strictly isolate brokers and processors via namespace and network p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant isolation with Firecracker microVMs or WASM sandboxes and memory-safe runtimes (Rust/Go). Attest function integrity at cold-start via TPM/Secure Enclave or SPIFFE/SVID attestation",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to translate security controls into a streaming pipeline architecture, balancing isolation, performance, and operability. It emphasizes concrete controls (WASM sandboxing, attestation, key management) and practical validation steps.\n\n## Key Concepts\n- **WASM Sandbox Boundaries**: Syscall filtering, memory capping, and narrow API surface enforcement\n- **Per-Tenant Isolation**: Dedicated streams with strict data routing and network segmentation\n- **Code Signing & Revocation**: HSM-backed trust stores with automated key revocation and transformer invalidation\n- **Tamper-Evident Logging**: Append-only logs with cryptographic hash chaining for runtime integrity verification\n- **Incident Response**: Rollback procedures, data replay strategies, and forensic audit capabilities\n\n## Code Example\n```javascript\n// Policy gate for transformer API surface\nconst ALLOWED_WASM_IMPORTS = ['env.memory', 'env.abort', 'env.now'];\n\nfunction validateTransformerImports(module) {\n  const imports = module.imports || [];\n  const violations = imports.filter(i => !ALLOWED_WASM_IMPORTS.includes(i));\n  if (violations.length > 0) {\n    throw new Error(`Forbidden imports: ${violations.join(', ')}`);\n  }\n}\n\n// Enforce memory cap\nconst MAX_WASM_MEMORY_PAGES = 64; // 4MB\n```\n\n## Trade-offs\n- **Security vs. Performance**: Stricter syscall filtering increases isolation but adds latency; balance with allowlists tuned to transformer requirements\n- **Tenant Isolation vs. Resource Efficiency**: Dedicated streams guarantee isolation but may increase infrastructure costs; consider weighted fair queuing as a middle ground\n- **Revocation Speed vs. Availability**: Aggressive revocation protects against compromised keys but risks service disruptions; implement graceful degradation with caching\n\n## Validation Steps\n1. **Static Analysis**: Scan transformer WASM for forbidden syscalls before deployment\n2. **Attestation**: Verify remote attestation reports from WASM runtime before execution\n3. **Chaos Engineering**: Inject compromised transformer signatures to test revocation workflows\n4. **Load Testing**: Verify tenant isolation holds under noisy neighbor conditions\n5. **Log Auditing**: Periodically validate hash chain integrity of tamper-evident logs",
        "difficulty": "intermediate"
      },
      {
        "id": "security-q-24",
        "questionId": "q-18178",
        "question": "You run a global IoT platform where edge gateways collect telemetry, run lightweight inference, and forward redacted results to a cloud data lake. Logs leak PII. Design an end-to-end plan to prevent leakage and prove compliance, covering edge data minimization, device attestation, TEEs for inference, encrypted channels, per-tenant access, and verifiable deletion/auditing. Include concrete tech choices and steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Edge attestation (SPIRE) before enrollment; inference in TEEs (SGX/SEV) with redaction prior to send; mTLS for transit and envelope encryption at rest (KMS); ABAC per-tenant in the data lake; tamper-e",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a multi-tenant cloud storage architecture with strict tenant data isolation, even in the event of storage node compromise. Key components include per-tenant encryption keys managed via a KMS, envelope encryption…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Quarantine the affected image digest in the registry, halt pulls, and rollback to the last-good revision using blue/green deployment. Enforce image signing with Sigstore and admission control via OPA",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant enclaves with isolated sealing keys; require remote attestation before any processing; encrypt data at rest and in transit with per-tenant envelope keys stored in a hardware security module",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExplores new angles in data privacy for IoT edge-to-cloud pipelines, focusing on end-to-end data minimization, attestation, and verifiable auditability rather than generic controls.\n\n## Key Concepts\n\n- Edge data minimization and in-flight redaction\n- Device attestation (SPIRE) and secure TEEs for inference\n- Encrypted communications (mTLS) and envelope encryption (KMS)\n- ABAC for per-tenant access in cloud data lakes\n- Tamper-evident logging and verifiable deletion/auditing\n\n## Code Example\n\n```javascript\n// Example: redactPII at edge before forwarding\nfunction redactPII(record){\n  if(record.email) record.email = \"REDACTED\";\n  if(record.phone) record.phone = \"REDACTED\";\n  if(record.ssn) record.ssn = \"REDACTED\";\n  return record;\n}\n```\n\n## Follow-up Questions\n\n- How would you validate end-to-end privacy guarantees (tests, metrics, and audits)?\n- What failure modes would you simulate, and how would incident response orchestrate recovery?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-21T05:21:18.971Z",
    "version": 1
  }
]