[
  {
    "id": "test-azure-developer",
    "channelId": "azure-developer",
    "channelName": "Azure Developer",
    "title": "Azure Developer Knowledge Test",
    "description": "Test your understanding of Azure Developer with 25 questions.",
    "questions": [
      {
        "id": "azure-developer-q-0",
        "questionId": "q-25771",
        "question": "You're a beginner Azure developer tasked with building a small per-tenant file-upload API using Azure Functions and Blob Storage. Clients send HTTP POST with tenantId header and a file body; function stores files under tenant/{tenantId}/{filename} with isolation from other tenants. Describe the end-to-end flow including security, tenancy validation, secret management with Azure Key Vault, per-env config with Azure App Configuration, and a dev/stage/prod deployment pipeline. Include tests and a cost-conscious approach to storage tiers. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant DP gate in the API: store epsilon budgets in Cosmos DB; on each query, check remaining budget, run the base aggregate against Delta Lake, add Laplace noise to the result, persis",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a canary rollout by directing 5–10% of traffic to a new Azure Functions version through Azure Front Door using header-based routing. Gate access with tenant-scoped feature flags in Azure App Configuration to e…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an HTTP-triggered Azure Function that reads tenantId from a header, validates it against a small allowlist, and writes the uploaded file to Blob Storage at tenant/{tenantId}/{filename}. Use",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Start with per-tenant isolation via storage prefixes and Unity Catalog RBAC, then implement OpenTelemetry tracing across Event Hubs → Databricks → Delta Lake to Application Insights. Build a small end",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of per-tenant isolation in blob storage, secure secret handling, and environment management in a beginner Azure stack. It also covers end-to-end flow from API surface to storage, plus practical testing and cost considerations.\n\n## Key Concepts\n- HTTP-triggered Azure Functions\n- Azure Blob Storage path per tenant (tenant/{tenantId}/{filename})\n- Azure Key Vault for secrets/credentials\n- Azure App Configuration for environment variables\n- Simple tenant validation and access control\n- Local testing with Azurite\n- GitHub Actions multi-environment deployment (dev/stage/prod)\n- Storage tier choices for cost control\n\n## Code Example\n```javascript\nmodule.exports = async function (context, req) {\n  const tenantId = (req.headers['x-tenant-id'] || '').toString();\n  // placeholder allowlist check\n  if (!tenantId.match(/^t\\d+$/)) {\n    context.res = { status: 400, body: 'Invalid tenant' };\n    return;\n  }\n  const filename = req.headers['x-filename'] || 'upload.bin';\n  const blobName = `tenant/${tenantId}/${filename}`;\n  // obtain blob client from environment/config; secret retrieval via Key Vault in practice\n  // upload body to blob storage here\n  context.res = { status: 202, body: 'Accepted' };\n};\n```\n\n## Follow-up Questions\n- How would you enforce per-tenant quotas and billing awareness?\n- What changes would you make to support large file uploads and resumable transfers?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-1",
        "questionId": "q-23778",
        "question": "You're building a real-time fraud-detection service for multiple tenants on Azure. Ingest events from Azure Event Hubs, run a Durable Function orchestrator that calls an external scoring API, and persist per-tenant results in Cosmos DB using tenantId as the partition key. Implement envelope encryption: encrypt PII with tenant-specific keys stored in Azure Key Vault, rotate keys quarterly, and decrypt only within the function chain. Outline architecture, telemetry, and failover tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition Event Hubs by tenant; use a Durable Functions orchestrator to chain enrichment and per-tenant aggregation; store results in Cosmos DB with tenantId as PartitionKey; implement idempotency through event deduplic…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a minimal MVP: HTTP ingest Function writes a tenant-scoped job to Cosmos, stores input in tenant Blob storage, and emits a Grid event to AKS GPU workload. Instrument all layers with OpenTelemet",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a dataflow where Event Hubs triggers a Durable Function orchestrator that calls the scoring API and writes to Cosmos DB with tenantId as the partition key. Encrypt PII with tenant-specific keys",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement geo-distributed IoT ingestion with IoT Hub Region X and cross-region Event Hubs; Databricks Spark streaming per tenant; Delta Lake on ADLS Gen2 with per-tenant keys in Key Vault; Synapse RLS",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes practical multi-tenant security with per-tenant keys, real-time processing, and cross-service telemetry.\n\n## Key Concepts\n- Durable Functions orchestration; Event Hubs; Cosmos DB partitioning; envelope encryption; Key Vault key rotation; OpenTelemetry traces across services.\n\n## Code Example\n```javascript\n// pseudo-implementation sketch showing key fetch, encryption, and store flow\n```\n\n## Follow-up Questions\n- How would you rotate keys with minimal downtime? \n- How would you test encryption policy across tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-2",
        "questionId": "q-21200",
        "question": "Design a beginner Azure image-upload API for multiple tenants using Azure Functions and Blob Storage. Use per-tenant prefixes: tenant-{id}/originals and tenant-{id}/thumbnails, with tenantId validation and minimal RBAC. Describe the HTTP flow (POST /upload), thumbnail generation, and how you'd verify isolation, retries, and observability. Propose a lightweight CI/CD (GitHub Actions) using Azure Key Vault secrets and dev/stage/prod slots?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant Synapse workspaces with separate catalogs and serverless pools, each secured by Managed Identity credentials. Route queries through a boundary proxy that enforces tenant-scoped access and R",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-tier architecture: fast in-region evaluation with central governance. Store versioned flags in Cosmos DB with multi-region replication; cache per-region in Redis Enterprise; evaluate via deterministic hash-based rou…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a two-layer design: 1) per-tenant data isolation in the storage layer (Cosmos DB with RLS or Delta Lake with tenant keys) and 2) per-tenant quotas in the API layer using API Management polic",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single Azure Storage Account with per-tenant prefixes: tenant-{id}/originals and tenant-{id}/thumbnails, protected by tenantId validation. HTTP-triggered Function uploads, then a second Function",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical Azure skills: serverless HTTP, blob storage isolation, and basic IaC/CI. It also checks testing discipline, security basics, and cost-aware scaling.\n\n## Key Concepts\n- Azure Functions HTTP trigger and binding to Blob Storage\n- Tenant isolation via path prefixes and tenantId validation\n- Secret management with Azure Key Vault\n- CI/CD with GitHub Actions and environment promotion (dev/stage/prod)\n- Observability and retries for idempotence\n\n## Code Example\n```javascript\n// Pseudocode: derive blob path from tenantId and fileName\nconst path = `tenant-${tenantId}/originals/${fileName}`;\nawait blobClient.upload(data, data.length);\n```\n\n## Follow-up Questions\n- How would you enforce per-tenant RBAC for the upload API?\n- How would you test isolation and fault scenarios (network failure, partial uploads) end-to-end?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-3",
        "questionId": "q-2059",
        "question": "Design an Azure-native data export service for a fintech that must export per-customer data on demand and on a schedule, with strict data residency, consent checks, and an audit trail. Use ADLS Gen2, Cosmos DB, Event Grid, Durable Functions or Functions, and Key Vault. Explain data partitioning, encryption, access control, idempotency, and failure modes including retries, outages, and rollback. Provide a concrete data model and flow?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Node.js Azure Function with HTTP trigger to POST/GET notes by userId, storing in Cosmos DB (SQL API) with userId as the partition key. DevOps pipeline: npm install; npm test (jest); archiv",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A Durable Functions orchestrator triggers per-customer export jobs after validating consent in Cosmos DB. Data is read from source tables, streamed to ADLS Gen2 under tenant-specific prefixes, encrypted with customer-ma…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Proposed approach: implement an HTTP trigger with route POST /tenants; parse request body, require tenantId, name, and region fields; prioritize X-Tenant-ID header when present, falling back to body; check Redis hash te…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an HTTP-triggered Azure Function that reads x-tenant-id, fetches a per-tenant secret from Key Vault, computes HMAC-SHA256 on the payload, and validates against the X-Signature header. If val",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates real-world enterprise concerns: per-customer data exports, consent management, data residency compliance, auditability, and failure handling within Azure-native architectures.\n\n## Key Concepts\n- Durable Functions orchestration patterns and idempotency\n- Customer-managed keys (CMK) with Azure Key Vault\n- Consent management and compliance workflows\n- Immutable audit logging and cross-region replication\n- Tenant-based data partitioning and AAD access control\n\n## Code Example\n```javascript\n// Pseudo: orchestrator outline\n```\n\n## Follow-up Questions\n- How would you handle large-scale concurrent exports?\n- What monitoring and alerting strategies would you implement?\n- How do you ensure data consistency during partial failures?\n- What strategies would you use for cost optimization?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-4",
        "questionId": "q-11365",
        "question": "You're deploying a real-time fraud-detection pipeline for a global fintech on Azure. Ingest 200k events/sec via Azure Event Hubs, run feature extraction in Databricks, and store per-tenant data in Delta Lake on ADLS Gen2 with strict isolation. How would you design cross-region availability, per-tenant masking, TTL retention, auditing via Purview, and deterministic replay with test harnesses?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition data by tenant in Delta Lake with separate catalogs, enforce Unity Catalog governance, mask PII during ETL, implement TTL via Delta Lake vacuum and time-travel; enable cross-region replicati",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Instrument the HTTP-triggered Function with OpenTelemetry, propagate trace context to Cosmos DB calls and surface traces in Application Insights. Use tenantId as the partition key for isolation. Centr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose Cosmos DB multi-region writes with per-tenant partition keys for DR across Region1 and Region2, plus a separate analytics store in Region2 backed by Synapse external tables on ADLS Gen2. Enfor",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition Delta Lake by tenant_id and date on ADLS Gen2; Ingest via Event Hubs into Spark Structured Streaming in Synapse; write per-tenant data with isolation; implement read-time masking for PII and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical, Azure-based design for a global fraud pipeline with tenant isolation, governance, and DR.\n\n## Key Concepts\n- per-tenant isolation in data lake\n- Delta Lake TTL and vacuum\n- cross-region DR and deterministic replay\n- data masking and audit trails via Purview/Unity Catalog\n\n## Code Example\n```sql\n-- Delta Lake example: simple TTL cleanup for a tenant table\nVACUUM tenant_data.retention_date <= CURRENT_TIMESTAMP - INTERVAL '30 days';\n```\n\n## Follow-up Questions\n- How would you validate deterministic replay across regions during DR tests?\n- What metrics and alerts would you implement to detect cross-region drift or leakage?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-5",
        "questionId": "q-19102",
        "question": "You're building a global, multi-tenant telemetry analytics platform on Azure. Ingest from devices via Event Hubs, process with Spark in Synapse, and store Delta Lake on ADLS Gen2 with tenant isolation. Add cross-tenant analytics using Delta Sharing, masking for PII, TTL retention, and cost-aware autoscaling. How would you design data layout, governance, failure modes, and testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an HTTP-triggered Functions app with two endpoints: POST /webhook and GET /events. Retrieve per-tenant secret from Key Vault, verify HMAC-SHA256(body, secret) against the signature header, w",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Serverless API design: use Azure Functions with a Redis cache (feat:{tenant}:{feature}) for 30s. On miss, fetch tenant flag from Cosmos (PartitionKey=tenant), fallback to App Configuration defaults, t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition Delta Lake by tenant_id and date on ADLS Gen2; Ingest via Event Hubs into Spark Structured Streaming in Synapse; write per-tenant data with isolation; implement read-time masking for PII and",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Delta Lake on ADLS Gen2 for a lakehouse; Spark handles schema drift with versioned tables and time travel; enforce per-tenant isolation via partitioning on tenantId and RBAC; apply dynamic masking at",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design a scalable, compliant multi-tenant pipeline with cross-tenant data sharing and governance.\n\n## Key Concepts\n- Delta Lake partitioning, tenant isolation, Delta Sharing\n- Data masking and TTL\n- Governance with Purview and RBAC\n- End-to-end testing and resilience\n\n## Code Example\n```javascript\n// Pseudo: read tenant data with masking applied in projection\nfunction readTenantView(tenantId, query) {\n  return readDeltaTable(tenantId)\n    .select(maskPII(columns needed for query));\n}\n```\n\n## Follow-up Questions\n- How would you validate data sharing permissions and prevent leakage?\n- What monitoring and alerting would you add for cross-tenant access anomalies?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-6",
        "questionId": "q-16818",
        "question": "You're building a beginner Azure service: a per-tenant CSV report uploader. Expose an HTTP POST /upload that accepts multipart/form-data with tenantId and a CSV file. Validate schema (columns: date, userId, amount). Store raw CSV in a tenant-specific container in Azure Blob Storage; save metadata in Cosmos DB (tenantId, blobUri, size, timestamp). Enforce per-tenant isolation via Function App managed identity RBAC. How would you implement input validation, idempotent uploads, and simple scaling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a two-layer design: 1) per-tenant data isolation in the storage layer (Cosmos DB with RLS or Delta Lake with tenant keys) and 2) per-tenant quotas in the API layer using API Management polic",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition by tenantId in the streaming job, use 5-minute tumbling windows with 1-minute lateness, and maintain per-tenant risk scores in Cosmos DB with TTL and RLS. Emit high-risk alerts to Event Grid",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement idempotent uploads by using a deterministic blob name (tenantId + uploadId). Validate CSV header and each row server-side. Store raw CSV in a tenant-specific Blob container, and write metada",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Partition Delta Lake by tenant_id; bronze (raw) in ADLS Gen2, silver with per-tenant masking; Unity Catalog RBAC for internal access; create Delta Sharing shares per partner with read-only permissions",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to wire simple serverless services across Storage and Cosmos DB with per-tenant isolation, basic reliability, and validation.\n\n## Key Concepts\n- HttpTrigger Functions\n- Blob Storage with per-tenant containers\n- Cosmos DB metadata tracking\n- RBAC and managed identities for isolation\n- Input validation and idempotency, basic scaling\n\n## Code Example\n```javascript\n// Example skeleton: validate headers, parse multipart, and store blob\n```\n\n## Follow-up Questions\n- How would you unit-test idempotency guarantees?\n- How would you monitor per-tenant quotas and failures?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-7",
        "questionId": "q-29903",
        "question": "You're a beginner Azure developer building a small multi-tenant API: an HTTP-triggered Azure Function backed by Cosmos DB with tenantId isolation. Add a per-tenant quota mechanism using Azure Table Storage and a simple rate cap, and gate an endpoint with a Feature Flag in Azure App Configuration. Describe dev/stage/prod CI/CD with GitHub Actions and Key Vault, tests, and validation of per-tenant rollout. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant Cosmos DB partitioning (tenantId), Event Hubs dedupe by eventId, and a Durable Functions orchestrator that fans out to tenant queues. Enrich in-flight with metadata from Salesforce/Slac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a global per-tenant feature-flag system: store flags in Cosmos DB with tenantId partitioning, cache hot flags regionally in Redis, and power a GraphQL gateway that reads Redis first then Cosmos",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a single HTTP-triggered Azure Function (JavaScript) that validates tenantId from JWT, enforces max 100 notes, uses Cosmos DB (SQL API) with partition key /tenantId, upserts notes by id, and record",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would start with the App Configuration feature flag gate to minimize blast radius, then add a per-tenant quota using Azure Table Storage with an ETag-based atomic update for a 1-hour window. Cosmos",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical execution of per-tenant governance, light-weight rate limiting, and feature-flag-driven rollout using core Azure services. It emphasizes minimal blast radius and clean rollback.\n\n## Key Concepts\n\n- Azure App Configuration feature flags\n- Azure Table Storage with optimistic concurrency for per-tenant counters\n- Cosmos DB partitioning by tenantId\n- GitHub Actions, Azure Key Vault, and App Configuration integration\n- Canary testing and rollback strategies\n\n## Code Example\n\n```javascript\n// Pseudo-code: gate by feature flag, then enforce per-tenant quota\n```\n\n## Follow-up Questions\n\n- How would you test burst traffic across multiple tenants?\n- What is your plan for quota counter cleanup and fault tolerance?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-8",
        "questionId": "q-26994",
        "question": "You're a beginner Azure developer tasked with a practical, end-to-end workflow: build an HTTP-triggered Function that validates tenantId and payload, then enqueues the message to a per-tenant Azure Storage Queue. A second Function dequeues and writes to Cosmos DB with tenantId as the partition key. Add a per-tenant feature flag in Azure App Configuration to enable/disable writes, and describe a dev/stage/prod deployment, testing, and rollback strategy. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Blob-triggered Function to read /tenantId/report.csv, validate the header row, count rows, and collect distinct user_ids. Upsert a single Cosmos DB document with partitionKey=tenantId and a vers",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an HTTP-triggered Azure Function that validates the tenantId and payload, then enqueues the message to a per-tenant Azure Storage Queue. A second Function dequeues messages and writes to Cosmos DB using tenantId…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use per-tenant Cosmos DB partitioning (tenantId), Event Hubs dedupe by eventId, and a Durable Functions orchestrator that fans out to tenant queues. Enrich in-flight with metadata from Salesforce/Slac",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan to implement per-tenant isolation with a streaming pipeline: Event Hubs ingress, Functions orchestration, and Synapse-based exports; apply masking/policy checks before export; attach trace contex",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates practical knowledge of decoupled processing patterns, per-tenant isolation strategies, feature flag management, and secure configuration handling in Azure. It also assesses the candidate's ability to design a robust end-to-end workflow with appropriate rollback mechanisms.\n\n## Key Concepts\n\n- HTTP-triggered Azure Functions for API endpoints\n- Azure Storage Queues for per-tenant message decoupling\n- Cosmos DB partitioning using tenantId for data isolation\n- Azure App Configuration for feature flags\n- Azure Key Vault for secrets management\n- CI/CD pipelines across dev, stage, and production environments\n- Rollback strategies and poison message handling\n\n## Code Example\n\n```javascript\n// Function A: HTTP trigger - enqueue to per-tenant queue\nmodule.exports = async function (context, req) {\n  const { tenantId, payload } = req.body;\n  \n  if (!tenantId || !payload) {\n    context.res = { status: 400, body: 'Missing tenantId or payload' };\n    return;\n  }\n\n  const queueName = `tenant-${tenantId}-queue`;\n  const queueClient = new QueueClient(connectionString, queueName);\n  \n  await queueClient.sendMessage(JSON.stringify({ tenantId, payload, timestamp: new Date() }));\n  \n  context.res = { status: 202, body: 'Message enqueued' };\n};\n\n// Function B: Queue trigger - write to Cosmos DB\nmodule.exports = async function (context, msg) {\n  const { tenantId, payload } = JSON.parse(msg.messageText);\n  \n  const featureFlags = await getFeatureFlag(`feature-flag-${tenantId}`);\n  if (!featureFlags.writeEnabled) {\n    context.log(`Writes disabled for tenant: ${tenantId}`);\n    return;\n  }\n\n  const container = cosmosClient.database('tenant-db').container('events');\n  await container.items.create({ id: context.invocationId, tenantId, payload });\n};\n```",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-9",
        "questionId": "q-25949",
        "question": "You're building a fintech reconciliation and analytics platform on Azure for multi-tenant customers. Ingest 50k events/sec from partner APIs via Azure Event Hubs, process in Azure Databricks into Delta Lake on ADLS Gen2, apply per-tenant masking and RBAC, and surface curated views with Synapse Serverless. Add a consent-driven data sharing channel for regulators/partners with revocation, plus end-to-end data lineage and auditability. Design architecture, data layout, governance, testing, and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement tenant validation and partition key enforcement first. This establishes the security foundation before any data operations. Without rigorous tenantId validation and strict partition key usage on all re…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage tenantId as the Cosmos DB partition key for a registrations container storing webhook URLs. On /events, validate tenantId, generate eventId, and enqueue to a per-tenant Storage Queue. A Durab",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Land tenant CSVs into Blob via ADF, create per-tenant Delta tables under Unity Catalog, and perform MERGE upserts on event_id in PySpark. Add schema validation, lightweight audit logs, and exponential",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy a per-tenant Delta Lake layout on ADLS Gen2, partitioned by tenant_id, with Unity Catalog RBAC and dynamic masking. Ingest events via Azure Event Hubs into Azure Databricks, write to Delta tables, and materialize…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nBank-grade data isolation and cross-tenant sharing at scale represent genuine enterprise pain points. This question evaluates your ability to design for architecture, governance, and production-readiness under realistic constraints.\n\n## Key Concepts\n- Delta Lake layout and partitioning strategies; Unity Catalog RBAC; dynamic data masking\n- Delta Sharing with consent management and access revocation\n- End-to-end lineage with Microsoft Purview; telemetry with OpenTelemetry\n- Testing: privacy compliance, failover scenarios, cost optimization\n\n## Architecture Overview\n- **Ingestion**: Azure Event Hubs handles 50k events/sec from partner APIs with auto-scaling and partitioning\n- **Processing**: Azure Databricks processes streams into Delta Lake on ADLS Gen2, applying per-tenant transformation logic\n- **Storage**: Delta Lake layout partitioned by tenant_id ensures logical isolation while optimizing query performance\n- **Governance**: Unity Catalog provides RBAC; dynamic masking enforces data visibility rules per tenant\n- **Serving**: Synapse Serverless delivers curated views to tenant-specific dashboards\n- **Sharing**: Delta Sharing enables consent-driven data access for regulators and partners with real-time revocation capability\n- **Lineage**: Microsoft Purview captures end-to-end data lineage and supports auditability requirements\n\n## Failure Modes\n- Event Hubs throughput exceeded: implement backpressure and scale partition count\n- Databricks job failures: configure auto-retry with dead letter queues for failed events\n- Delta Sharing revocation latency: design idempotent access checks at query time\n- Cross-tenant data leakage: enforce tenant isolation at storage layer with immutable audit logs",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-10",
        "questionId": "q-2484",
        "question": "Design a multi-tenant telemetry pipeline on Azure: events arrive through Event Hubs per-tenant, processed by Durable Functions, stored in per-tenant Cosmos DB, and surfaced via Azure Data Explorer dashboards. How would you implement end-to-end tracing with OpenTelemetry across all components, ensure per-tenant correlation, minimize overhead, handle retries idempotently, and preserve privacy controls (pseudonymization, access controls)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a two-layer design: 1) per-tenant data isolation in the storage layer (Cosmos DB with RLS or Delta Lake with tenant keys) and 2) per-tenant quotas in the API layer using API Management polic",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement OpenTelemetry across Event Hubs, Durable Functions, Cosmos DB, and ADX; propagate traceparent/tracestate and tenant_id in message properties; route traces to Application Insights and data ex",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose Cosmos DB multi-region writes with per-tenant partition keys for DR across Region1 and Region2, plus a separate analytics store in Region2 backed by Synapse external tables on ADLS Gen2. Enfor",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a multi-region Azure stack: ingest events via Event Hubs, orchestrate with Durable Functions, store per-tenant results in Cosmos DB, and gate traffic with a tenant-hash in Redis for determin",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess distributed tracing across serverless, event-driven Azure services; evaluate OpenTelemetry integration, trace propagation, and tenant-scoped correlation; inspect handling of retries, idempotency, and privacy controls.\n\n## Key Concepts\n- OpenTelemetry across Event Hubs, Durable Functions, Cosmos DB, ADX\n- Trace context propagation (traceparent/tracestate)\n- Tenant isolation and RBAC, per-tenant partitioning\n- Privacy controls and sampling\n\n## Code Example\n```javascript\n// Startup: initialize OTLP exporter and tracer\nimport { NodeTracerProvider } from '@opentelemetry/sdk-trace-node';\nimport { SimpleSpanProcessor } from '@opentelemetry/sdk-trace-base';\nimport { OTLPTraceExporter } from '@opentelemetry/exporter-otlp-http';\nconst provider = new NodeTracerProvider();\nprovider.addSpanProcessor(new SimpleSpanProcessor(new OTLPTraceExporter({url:'https://collector.example/otlp'})));\nprovider.register();\n```\n\n## Follow-up Questions\n- How would you verify end-to-end trace availability under partial failures?\n- How would you enforce per-tenant sampling rates and preserve privacy?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-11",
        "questionId": "q-29229",
        "question": "You're building a HIPAA/HITRUST-compliant multi-tenant health analytics platform on Azure. Ingest patient data from clinics via Azure Event Hubs, stream transform with Spark, store in Delta Lake on ADLS Gen2, and expose per-tenant views with Synapse Serverless while enforcing residency by region and strict RBAC via Unity Catalog. Describe the end-to-end architecture, data layout, tenancy model, masking/pseudonymization, audit/lineage controls with Purview, testing plan, failure modes, and a cost-aware autoscaling strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant DP gate in the API: store epsilon budgets in Cosmos DB; on each query, check remaining budget, run the base aggregate against Delta Lake, add Laplace noise to the result, persis",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan to implement per-tenant isolation with a streaming pipeline: Event Hubs ingress, Functions orchestration, and Synapse-based exports; apply masking/policy checks before export; attach trace contex",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a tenant-scoped lifecycle: partition Delta Lake by tenant and timestamp; use VACUUM with a retention window plus a separate tombstone stream for deletions. Delete markers propagate to Purvie",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a HIPAA/HITRUST-compliant multi-tenant health analytics stack on Azure: per-tenant RBAC with Unity Catalog, PHI masking, region-residency controls, Purview lineage, Delta Lake schema design,",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical design of compliant, scalable, multi-tenant data pipelines across regions with real data governance requirements.\n\n## Key Concepts\n\n- HIPAA/HITRUST compliance in cloud data platforms\n- Delta Lake schema design and versioning\n- Unity Catalog RBAC and data masking\n- Purview for lineage and auditing\n- Region residency and cost-aware autoscaling\n\n## Code Example\n\n```python\n# PySpark pseudo-example for masking PHI during read\nfrom pyspark.sql.functions import col, when\ndf = spark.read.format('delta').load('/delta/tenant_data')\ndf_masked = df.withColumn('SSN', when(col('tenant_id').isNotNull(), col('SSN')).otherwise(None))\n```\n\n## Follow-up Questions\n\n- How would you validate region residency compliance end-to-end?\n- What changes are needed if a tenant expands to a new region mid-mission critical window?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-12",
        "questionId": "q-8476",
        "question": "You're building a beginner Azure-native webhook receiver for three partners. An Azure Functions HTTP API ingests events, stores per-tenant metadata in Cosmos DB with partitionKey=tenantId, and enqueues processing to a Service Bus topic for durable work queues. Design authentication using Managed Identity, per-tenant isolation, idempotent processing using eventId, and a testing strategy for failure modes and data consistency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a global flag service with per-tenant Cosmos DB partitions and near-api Redis cache; evaluate by hashing tenantId+flagName to pick a shard, ensuring <200 ms latency. Invalidate cache on upda",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create an HTTP-triggered Azure Function that reads tenantId from a header, authenticates using Managed Identity to access Cosmos DB and Service Bus, and writes per-tenant metadata to Cosmos DB with pa",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Cosmos DB with tenantId as partition key and optimistic concurrency via ETag, couple with a stored procedure to atomically apply flag updates. Ingest via Event Hubs with a FlagUpdated event, store",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implementation outline: extract tenantId from X-Tenant-ID header or query param; read action from query; check Redis key policies:{tenantId}:{action} with 60s TTL; on miss fetch allowedActions from Co",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nGauges practical Azure-native workflow design, tenant isolation, and basic reliability patterns in a beginner-friendly setting.\n\n## Key Concepts\n\n- Azure Functions HTTP trigger and managed identity\n- Cosmos DB partitioning by tenantId\n- Service Bus for durable, decoupled processing\n- Idempotency with eventId\n- Basic testing: unit, integration, failure-mode checks\n\n## Code Example\n\n```javascript\nmodule.exports = async function (context, req) {\n  const tenantId = req.headers['x-tenant-id'];\n  // authenticate via Managed Identity and route accordingly\n  // persist tenant data to Cosmos DB with partitionKey = tenantId\n  // publish eventId-enabled message to Service Bus\n  context.res = { status: 200, body: 'ok' };\n}\n```\n\n## Follow-up Questions\n\n- How would you implement deduplication across multiple function instances?\n- What metrics would you collect to monitor latency and reliability?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-13",
        "questionId": "q-891",
        "question": "Design a beginner-friendly serverless image-upload API on Azure: clients POST JPEGs to an HTTP-triggered Function, which saves the file to Blob Storage, enqueues a processing job, and stores a metadata record in Cosmos DB. How would you ensure idempotency, handle retries with back-off, and keep costs predictable on a Consumption plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design with APIM policies enforcing per-tenant quotas, route to multi-region AKS hosting the model service, and store per-tenant state in Cosmos DB using tenantId as the partition key with geo-replica",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition Delta Lake by tenant_id; bronze (raw) in ADLS Gen2, silver with per-tenant masking; Unity Catalog RBAC for internal access; create Delta Sharing shares per partner with read-only permissions",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a dataflow where Event Hubs triggers a Durable Function orchestrator that calls the scoring API and writes to Cosmos DB with tenantId as the partition key. Encrypt PII with tenant-specific keys",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement idempotency via an idempotency-key header; persist seen keys in Cosmos DB; on duplicates, return 200 and skip work. Decouple with a queue; HTTP function enqueues, blob saved. Processing func",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical serverless data flow, idempotency, and cost control in Azure. It also touches inter-service communication and observability.\n\n## Key Concepts\n\n- Idempotent HTTP endpoints using an idempotency key\n- Decoupling with HTTP → Blob → Queue → Worker\n- Cosmos DB for idempotency store and metadata\n- Retry/backoff and dead-lettering\n- Cost-conscious design on Consumption plan\n\n## Code Example\n\n```javascript\n// Skeleton: HTTP trigger checks idempotency key, writes blob, enqueues, and stores metadata\nmodule.exports = async function (context, req) {\n  const key = req.headers[\"x-idempotency-key\"];\n  // look up key in Cosmos DB; if exists, return 200\n  // otherwise, save blob, enqueue job, write metadata, and return 202\n};\n```\n\n## Follow-up Questions\n\n- How would you test idempotency guarantees in this flow?\n- What metrics would you collect to validate retry/backoff behavior and cost control?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-14",
        "questionId": "q-27941",
        "question": "You're building a real-time, multi-tenant analytics API on Azure with an HTTP-triggered Function fronting Cosmos DB. Tenant isolation uses tenantId as the partition key and per-tenant data residency is required. Design a solution that (1) enforces region-bound residency with multi-region Cosmos writes, (2) propagates tracing across HTTP, Functions, and Cosmos calls into OpenTelemetry/Application Insights, (3) supports zero-downtime, backward-compatible schema migrations with per-tenant versioning, and (4) validates canary rollouts via App Configuration and a GitHub Actions workflow. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant Delta Lake partitions on ADLS Gen2, Databricks Feature Store with versioned schemas, and Azure ML model registry. Inference via Azure Functions behind API Management with tenant RBAC. Purvi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement tenancy-aware routing at API Management with per-tenant quotas (100 rps, burst 10x) backed by Redis; forward requests to AKS inference pods via a per-tenant routing table and Istio. Ingest f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Start with enabling Cosmos DB multi-region writes for per-tenant residency using tenantId as the partition key. Instrument all calls with OpenTelemetry and export traces to Application Insights, ensur",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a per-tenant, versioned feature store: partition Delta Lake by tenant_id and feature_version; keep raw data in a separate bronze table and derived features in a gold table; enforce Unity Catalog R",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design production-grade, multi-region residency, observability, and safe schema evolution in a multi-tenant Azure setup.\n\n## Key Concepts\n\n- Multi-region Cosmos DB residency\n- OpenTelemetry tracing across HTTP, Functions, Cosmos\n- Backward-compatible schema migrations with per-tenant versioning\n- Canary rollouts via App Configuration and CI/CD with automated rollback\n\n## Code Example\n\n```javascript\n// Example: versioned document skeleton\n{\n  id: \"doc1\",\n  tenantId: \"t1\",\n  dataVersion: 2,\n  payload: { /* v2 fields */ }\n}\n```\n\n## Follow-up Questions\n\n- How would you test migrations across tenants with mixed versions?\n- What metrics and alerts would you implement for cross-region latency and trace completeness?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-15",
        "questionId": "q-1962",
        "question": "You’re building a beginner Azure Function (HTTP trigger) that receives event payloads and stores them in a data container. Design a lightweight, auditable approach to log every write without slowing latency. Include how you would generate an immutable audit trail and what storage pattern you’d use. Provide a minimal code snippet to append an audit line with timestamp, eventId, and userId derived from Authorization header?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an HTTP-triggered Function on the Consumption plan that writes to a data container and appends a separate line to an immutable audit blob in the same storage account. Extract userId from a JWT in",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Outline a serverless flow: a Function returns a per-tenant SAS URL for a blob container (tenant prefix or per-tenant container). After upload, a second Function writes metadata to Cosmos DB with parti",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a compensating transaction pattern in Durable Functions: assign a durable id per event, write a precommit marker, then finalize writes only after downstream success; on failure, trigger comp",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Isolate tenants via tenantId partition keys in Cosmos DB and per-tenant indices in Cognitive Search. Use idempotent Event Hub processing with at-least-once delivery; upsert embeddings with versioned r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design lightweight auditing with low latency using Append Blobs and JWT parsing; demonstrates immutability, data-audit separation, and basic error handling.\n\n## Key Concepts\n- Append Blob usage for immutable, append-only logs\n- JWT payload extraction from Authorization header\n- Per-event audit entries with timestamp and eventId\n- Resilient writes with basic retry/backoff\n\n## Code Example\n```javascript\n// Minimal Azure Function snippet (TypeScript)\nimport { ContainerClient, AppendBlobClient } from \"@azure/storage-blob\";\nconst containerClient = new ContainerClient(\"<storage-url>\", new DefaultAzureCredential());\nexport async function run(context, req){\n  const auth = req.headers[\"authorization\"] ?? \"\";\n  const token = auth.split(\" \")[1] ?? \"\";\n  const payload = JSON.parse(Buffer.from(token.split(\".\")[1], 'base64').toString());\n  const userId = payload?.sub ?? \"anonymous\";\n  const eventId = req.body?.eventId ?? crypto.randomUUID();\n  const line = JSON.stringify({ ts: new Date().toISOString(), eventId, userId, status: 'received' }) + \"\\n\";\n  const audit = containerClient.getAppendBlobClient(\"audit.log\");\n  await audit.appendBlock(line, line.length);\n  context.res = { status: 200, body: { eventId } };\n}\n```\n\n## Follow-up Questions\n- How would you extend this to handle audit log rotation or retention?\n- How would you enforce per-tenant isolation for audits?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-16",
        "questionId": "q-22951",
        "question": "You're tasked with building a global, multi-tenant analytics backend on Azure for a marketplace. Each tenant's data must be isolated and billable by tenant. Ingest comes from real-time events via Azure Event Hubs and batch updates via Data Factory into Cosmos DB (tenantId partition key) and a star-schema in Synapse for analytics. Implement end-to-end telemetry with OpenTelemetry across Functions, Event Hubs, and Data Factory into Application Insights; manage secrets with Key Vault and environment settings with App Configuration. Outline architecture, data model, ingestion plan, telemetry strategy, testing, rollback, and autoscaling. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an Azure Function App with HTTP PUT /flags/{tenantId} to upsert a JSON feature set, validated against a schema. Store in Cosmos DB using tenantId as partitionKey and a version field for idempo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two active Azure regions, each hosting Delta Lake on ADLS Gen2 with tenantId partitions. Ingest via Event Hubs and mirror data with cross-region Delta sharing and ADLS replication. Purview catalogs pe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Start by locking per-tenant Cosmos DB model (tenantId partition, controlled RU/s) and a Durable Functions workflow that glues real-time Event Hubs ingest with batch Data Factory to Synapse. Implement",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Serverless API design: use Azure Functions with a Redis cache (feat:{tenant}:{feature}) for 30s. On miss, fetch tenant flag from Cosmos (PartitionKey=tenant), fallback to App Configuration defaults, t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a scalable, compliant multi-tenant data path with both real-time and batch ingestion, telemetry, and controlled configuration across Azure services.\n\n## Key Concepts\n\n- Multi-tenant isolation with per-tenant partitioning and RBAC\n- Real-time (Event Hubs) + batch (Data Factory) pipelines\n- Durable Functions orchestration for cross-service workflow\n- OpenTelemetry across Functions, Event Hubs, and Data Factory; export to Application Insights\n- Secrets and config management via Key Vault and App Configuration\n- Autoscaling and cost-aware planning; end-to-end testing and rollback\n\n## Code Example\n\n```javascript\n// Minimal OpenTelemetry init in an Azure Function\nconst { NodeSDK } = require('@opentelemetry/sdk-node');\nconst { OTLPTraceExporter } = require('@opentelemetry/exporter-otlp-http');\nconst sdk = new NodeSDK({ \n  traceExporter: new OTLPTraceExporter({ url: process.env.OTLP_URL }), \n});\nsdk.start();\n```\n\n## Follow-up Questions\n\n- How would you validate tenant isolation end-to-end in production?\n- What does your rollback plan look like for a failing batch pipeline?\n- How would you measure latency and cost trade-offs to refine autoscaling?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-17",
        "questionId": "q-7480",
        "question": "You're building a geo-telemetry processing pipeline for a multi-tenant ride-hailing platform on Azure. Ingest 150k events/sec from vehicles via Azure Event Hubs, enrich with Azure Maps in a Durable Functions chain, aggregate per-tenant metrics in Cosmos DB, and serve dashboards via a Web API. How would you design for per-tenant isolation, exactly-once processing, scaling, failure handling, and cost control? Include governance and testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Event Hubs intake per tenant (60k msgs/sec), dedupe by (tenantId,eventId), and orchestrate with Durable Functions. Persist to Cosmos DB with tenant-scoped partitions and region-bound accounts",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement ingestion via Event Hubs, perform DP during or after enrichment, store tenant-scoped data in Delta Lake on ADLS Gen2 with partitions per tenant, catalog lineage in Purview, and expose per-te",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition Event Hubs by tenant; use a Durable Functions orchestrator to chain enrichment and per-tenant aggregation; store results in Cosmos DB with tenantId as PartitionKey; implement idempotency through event deduplic…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "AKS: one namespace per tenant with NetworkPolicy; Azure ML handles model registry and canary deployments; a shared inference runtime enforces per-tenant auth and uses a tenant-scoped feature store; st",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess real-world telemetry pipelines, per-tenant isolation, and exactly-once semantics under high throughput.\n\n## Key Concepts\n- Event Hubs partitioning by tenant\n- Durable Functions orchestration\n- Per-tenant Cosmos DB partitioning\n- Idempotency and exactly-once processing\n- Backpressure and scaling, cost controls\n- Data governance and testing\n\n## Code Example\n```typescript\n// Pseudo-Durable Functions orchestrator\nconst orchestrator = function*(context) {\n  const evt = context.getInput();\n  const enriched = yield context.callActivity('EnrichGeo', evt);\n  const agg = yield context.callActivity('AggregateMetrics', enriched);\n  return agg;\n};\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-18",
        "questionId": "q-21269",
        "question": "You're a beginner Azure developer tasked with a per-tenant CSV ingestion API. Implement an HTTP-triggered Function accepting multipart CSV at POST /ingest?tenantId=XYZ, streaming to a tenant-scoped blob container, counting rows on the fly, and writing metadata (tenantId, fileName, uploadTime, rowCount) to Cosmos DB (partition key tenantId). Use Managed Identity for access, dedupe by fileName, and outline a GitHub Actions pipeline with per-env App Configuration and Key Vault?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an HTTP-triggered Function that validates tenantId, computes a SHA256 hash of the payload, upserts a summary {tenantId, timestamp, hash} in Cosmos DB with partitionKey=tenantId, and writes t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingress via Event Hubs; orchestrate with Durable Functions per tenant; persist to Cosmos DB with tenantId as partition key. Ensure idempotency with a durable sequenceId and upsert, plus a dedup store.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant CSV ingestion API using an HTTP-triggered Azure Function that accepts multipart/form-data at POST /ingest?tenantId=XYZ. The function streams incoming CSV data directly to a tenant-scoped blob contain…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement Row-Level Security (RLS) on Azure SQL with a predicate function that returns TRUE when @TenantId = USER_CONTEXT(N'TenantId'); map each service identity to a SQL user restricted to its tenant's rows. Enable dyn…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical Azure fundamentals including HTTP triggers, blob storage streaming, per-tenant isolation, and Cosmos DB partitioning. It also validates understanding of security (Managed Identity), idempotency (deduplication by fileName), and cost-conscious deployment workflows.\n\n## Key Concepts\n\n- HTTP-triggered Azure Functions\n- Multipart/form-data parsing and streaming\n- Azure Blob Storage with per-tenant paths\n- Cosmos DB with tenantId partition key\n- Managed Identity for secure resource access\n- Idempotent ingestion via fileName deduplication\n- GitHub Actions with App Configuration and Key Vault\n\n## Code Example\n\n```javascript\n// index.js\nconst { ContainerClient } = require('@azure/storage-blob');\nconst { CosmosClient } = require('@azure/cosmos');\n\nmodule.exports = async function (context, req) {\n    const tenantId = req.query.tenantId;\n    const fileName = req.body?.filename || req.headers['x-filename'];\n    \n    if (!tenantId || !fileName) {\n        context.res = { status: 400, body: 'Missing tenantId or fileName' };\n        return;\n    }\n\n    const blobServiceClient = new ContainerClient(\n        `https://${process.env.STORAGE_ACCOUNT}.blob.core.windows.net/${tenantId}`,\n        new DefaultAzureCredential()\n    );\n\n    const blockBlobClient = blobServiceClient.getBlockBlobClient(fileName);\n    const uploadStream = async (readableStream) => {\n        let rowCount = 0;\n        return new Promise((resolve, reject) => {\n            readableStream.on('data', (data) => {\n                rowCount += data.toString().split('\\n').length - 1;\n            });\n            readableStream.on('end', () => resolve(rowCount));\n            readableStream.on('error', reject);\n        });\n    };\n\n    const stream = req.body;\n    const rowCount = await uploadStream(stream);\n    await blockBlobClient.uploadStream(stream);\n\n    const cosmosClient = new CosmosClient({\n        endpoint: process.env.COSMOS_ENDPOINT,\n        credential: new DefaultAzureCredential()\n    });\n\n    const container = cosmosClient.database('ingestion').container('files');\n    await container.items.upsert({\n        id: `${tenantId}-${fileName}`,\n        tenantId,\n        fileName,\n        uploadTime: new Date().toISOString(),\n        rowCount\n    });\n\n    context.res = { status: 200, body: { rowCount } };\n};\n```\n\n## GitHub Actions Pipeline\n\n```yaml\nname: Deploy CSV Ingestion API\n\non: [push]\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      \n      - name: Azure Login\n        uses: azure/login@v1\n        with:\n          creds: ${{ secrets.AZURE_CREDENTIALS }}\n\n      - name: Fetch App Config\n        run: |\n          az appconfig kv export \\\n            --name ${{ vars.APP_CONFIG_NAME }} \\\n            --connection-string ${{ secrets.APP_CONFIG_CONN }} \\\n            --prefix CSV_INGESTION_ \\\n            --format json > appconfig.json\n\n      - name: Fetch Key Vault Secrets\n        run: |\n          for secret in STORAGE_ACCOUNT COSMOS_ENDPOINT; do\n            echo \"$secret=${{ secrets.$secret }}\" >> $GITHUB_ENV\n          done\n\n      - name: Azure Functions Deploy\n        uses: Azure/functions-action@v1\n        with:\n          app-name: csv-ingestion-api\n          package: ${{ github.workspace }}/function.zip\n```",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-19",
        "questionId": "q-15488",
        "question": "You're designing a geo-distributed IoT analytics pipeline on Azure for a smart-building network. Ingest 80k messages/sec from devices into an IoT Hub in Region X, fan out to Event Hubs in Regions X and Y, process per-tenant aggregates with Databricks/Stream Analytics, and store results in Delta Lake on ADLS Gen2 with per-tenant encryption keys in Key Vault. How would you ensure deterministic latency, strict data residency, idempotent ingestion, and auditable trails across regions, including testing and failover plans?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement geo-distributed IoT ingestion with IoT Hub Region X and cross-region Event Hubs; Databricks Spark streaming per tenant; Delta Lake on ADLS Gen2 with per-tenant keys in Key Vault; Synapse RLS",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design with tenant-scoped namespaces, tenantId partition keys, and separate storage partitions. Route API traffic via API Management with per-tenant RBAC. Cache hot features in Redis with per-tenant k",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an HTTP-triggered Azure Function that leverages Table Storage with PartitionKey=tenantId and RowKey=fileName to ensure idempotency. First, check if a record exists in Table Storage for the given tenantId and fileNam…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Check Redis first with key `${tenantId}:${productId}`; on miss fetch from Table Storage (PartitionKey=tenantId, RowKey=productId); if found, return and cache the item in Redis with TTL (e.g., 300s). U",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests multi-region IoT ingestion, tenant isolation, encryption, auditing, and DR readiness in Azure, plus practical trade-offs for latency and data residency.\n\n## Key Concepts\n\n- IoT Hub, Event Hubs, Databricks, Stream Analytics\n- Delta Lake, ADLS Gen2\n- Azure Key Vault (BYOK), per-tenant keys\n- Synapse with Row-Level Security\n- Purview for lineage and auditing\n- Cross-region DR and geo-replication\n- Chaos Studio for resilience testing\n\n## Code Example\n\n```javascript\n// Pseudo: fetch per-tenant envelope key from Key Vault\nfunction getTenantEnvelopeKey(tenantId) {\n  // retrieve tenant-specific encryption key\n  return keyVaultClient.getKey(`tenant-${tenantId}-enc-key`);\n}\n```\n\n## Follow-up Questions\n\n- How would you measure and guarantee end-to-end latency across regions?\n- What failure modes would you plan for, and how would you implement idempotent ingestion and exactly-once semantics across regions?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-20",
        "questionId": "q-16193",
        "question": "You're building a multi-tenant real-time analytics API on Azure for a global logistics provider. Ingest events from Azure Event Hubs at up to 80k msgs/sec across regions, perform per-tenant enrichment in Durable Functions, materialize per-tenant summaries in Azure Data Explorer, and serve dashboards via API Management-backed REST endpoints. Ensure strict per-tenant isolation, sub-200 ms end-to-end latency for the event path, at-least-once processing with idempotent retries, and auditable data lineage in Purview with immutable logs in ADLS Gen2. How would you architect error handling, rollback strategies, testing, and deployment?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instrument Functions, AKS, and Databricks with OpenTelemetry. Use W3C TraceContext propagation; carry traceparent across HTTP, queues, and Delta Lake jobs. Export spans via OTLP to Azure Monitor (Log",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-tenant streaming path: Event Hubs ingress, orchestrate per-tenant enrichment with Durable Functions, persist per-tenant state in Cosmos DB, and publish aggregated metrics to Azure Data Ex",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use an HTTP-triggered Function; get tenantId from a header; fetch the vendor secret from Key Vault via DefaultAzureCredential; validate the request with an HMAC SHA-256 signature over the body using t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a latency-aware pipeline with a Durable Functions orchestrator, fan-out to an Azure ML scoring endpoint, per-tenant Cosmos DB writes, Purview tagging, and cross-region audit logs. Include backp",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe candidate must design a robust, scale-out telemetry path with strict tenant isolation, low latency, and end-to-end lineage.\n\n## Key Concepts\n- Tenant isolation, idempotent processing, DLQ\n- Event-driven orchestration with Durable Functions\n- Per-tenant storage strategy (Cosmos DB)\n- Analytics with Azure Data Explorer and Purview governance\n- Testing and deployment strategies (chaos, canaries)\n\n## Code Example\n```javascript\n// Pseudo: orchestrator and enrichment hooks demonstrating idempotent processing\n```\n\n## Follow-up Questions\n- How would you handle regional failover and data residency?\n- What metrics and alerts ensure SLA adherence?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-21",
        "questionId": "q-973",
        "question": "Case: You’re building a beginner-friendly Azure API that accepts events from mobile apps. Each event includes userId, eventType, and timestamp. The API should write a compact summary to Cosmos DB and stream raw events to Event Hubs for analytics. On a Consumption plan, outline the minimal architecture, bindings, and error handling to ensure low latency, safe retries, and no data loss during transient outages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-phase migration: 1) deploy code that starts accepting the new optional field while keeping existing writes; 2) run a tenant-scoped backfill in batches across regions with rate limits. Gate the new",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "HTTP-triggered Azure Function validates payload (userId, eventType, timestamp). Store a compact summary in Cosmos DB using a composite id (userId+timestamp) to enforce idempotency, and emit the full e",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a beginner Azure Functions HTTP API for per-tenant notes with GET /notes/{tenantId} and POST /notes/{tenantId}. Use Cosmos DB partitioned by tenantId. Add API-key authentication by reading t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Isolate by tenantId across all data stores; ingest 200k events/sec via Event Hubs with per-tenant partition strategies; process with Databricks Spark structured streaming, write to Delta Lake on ADLS Gen2 partitioned by…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of a realistic ingestion path: HTTP input, idempotent storage, and streaming analytics with Azure services, plus the constraints of a Consumption plan.\n\n## Key Concepts\n- HTTP trigger in Azure Functions\n- Cosmos DB best practices for idempotent keys\n- Event Hubs for scalable intake of streams\n- Retry/backoff strategies on Consumption plan\n- Bindings and error handling in serverless architectures\n\n## Code Example\n```javascript\nmodule.exports = async function(context, req) {\n  const body = req.body;\n  // basic validation\n  if (!body?.userId || !body?.eventType || !body?.timestamp) {\n    context.res = { status: 400, body: 'Invalid payload' };\n    return;\n  }\n  const id = `${body.userId}|${body.timestamp}`;\n  // write summary to Cosmos DB (idempotent key)\n  context.bindings.cosmosDoc = { id, userId: body.userId, timestamp: body.timestamp, eventType: body.eventType };\n  // publish raw event to Event Hubs\n  context.bindings.outputEventHub = body;\n  context.res = { status: 202, body: 'Accepted' };\n};\n```\n\n## Follow-up Questions\n- How would you validate and test idempotency in this flow? \n- What metrics would you observe to ensure latency stays low during bursts?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-22",
        "questionId": "q-21405",
        "question": "You're building a per-tenant analytics and governance pipeline on Azure for fintech partners. Ingests via Azure Event Hubs; microservices run in AKS and Functions; per-tenant data lives in Cosmos DB with RLS; audit and analytics replicated to ADLS Gen2; enforce data residency by region and Azure AD authorization. How would you design partition keys, RLS, TTL, and cross-region replication to meet latency, sovereignty, and auditable governance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy a separate Azure ML real-time endpoint per tenant behind an API gateway; route via tenant identity to keep latency sub-200ms. Telemetry ingested to Event Hubs and stored in ADLS Gen2 per tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition key: tenantId; implement Cosmos DB RLS to ensure data for a tenant never appears in another tenant's query. Use multi-region Cosmos DB with a primary write region and automatic failover to m",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Create an HTTP-triggered Azure Function that reads tenantId from a header, authenticates using Managed Identity to access Cosmos DB and Service Bus, and writes per-tenant metadata to Cosmos DB with pa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage Azure App Configuration feature flags with per-tenant labels and environment separation. Resolve flags in API Management/Functions, cache decisions per tenant, and use a deterministic hash on",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of per-tenant data governance, data residency, and cross-region consistency in Azure data pipelines.\n\n## Key Concepts\n- Cosmos DB: partition keys, row-level security, multi-region replication\n- Data residency: region controls, CMK in Key Vault\n- Auditing: ADLS Gen2, Change Feed, policy checks\n- Testing: failover, latency SLAs, data residency validation\n\n## Code Example\n```javascript\n// Pseudo Cosmos DB RLS wiring (conceptual)\nCREATE FUNCTION TenantFilter(tenantId string) RETURNS bool AS\n    'c.tenantId = @tenantId';\n```\n\n## Follow-up Questions\n- How would you handle schema evolution across regions?\n- How would you monitor per-tenant quotas and throttling?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-23",
        "questionId": "q-15230",
        "question": "You're building a beginner Azure-based notification service for a multi-tenant SaaS app. Messages arrive via Azure Event Grid and must be delivered as emails per tenant. Design a two-function flow: an Event Grid-triggered processor enqueues to a per-tenant Storage Queue, and a sender function delivers via a mock SMTP. Enforce per-tenant isolation, idempotency via eventId, exponential backoff retries, and a DLQ for failed messages. Outline components and failure handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a two-function flow: an Event Grid-triggered processor enqueues to a per-tenant Storage Queue, and a sender function delivers via a mock SMTP with exponential backoff. Idempotency is ensured by",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Start with per-tenant isolation via storage prefixes and Unity Catalog RBAC, then implement OpenTelemetry tracing across Event Hubs → Databricks → Delta Lake to Application Insights. Build a small end",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an idempotent consumer on Event Hubs keyed by (tenantId, eventId) with checkpointing; feature extraction and scoring run in per-tenant isolated runtimes; store scores and features in Cosmos DB with tenant-spec…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an Azure Functions HTTP-triggered /upload endpoint that accepts tenantId in the X-Tenant-ID header and multipart/form-data file uploads. Use Node.js with Azure SDK: ```javascript const { CosmosClient } = requi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of simple, real-world event-driven flows with per-tenant isolation and reliability basics.\n\n## Key Concepts\n- Event Grid triggers\n- Storage Queue per tenant\n- Idempotency with eventId\n- Exponential backoff retries\n- Dead-letter queue\n- Basic observability (App Insights)\n\n## Code Example\n```javascript\n// Example of idempotent handler scaffold\nmodule.exports = async function(context, eventGridEvent) {\n  const id = eventGridEvent.id;\n  // check per-tenant dedup store\n  const seen = await dedupStore.contains(`dedup:${id}`);\n  if (seen) return;\n  await dedupStore.add(`dedup:${id}`);\n  await enqueueOutbound(eventGridEvent);\n};\n```\n\n## Follow-up Questions\n- How would you test the DLQ path and failure modes?\n- How would you simulate backoff and measure end-to-end latency?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-24",
        "questionId": "q-11100",
        "question": "You're building a serverless data validation pipeline for a multi-tenant SaaS on Azure. Ingest 200k-300k events/sec via Azure Event Hubs, validate each event against a tenant-specific JSON Schema stored per tenant in Azure Blob Storage, orchestrated with Durable Functions to parallelize validation and handle schema updates. Persist valid events to a per-tenant Bronze lake in ADLS Gen2 and emit anomalies to Azure Monitor alerts. How would you design for strict per-tenant isolation, dynamic schema evolution, idempotent retries, and end-to-end latency around 100-200 ms? Include testing and rollback strategies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant OpenTelemetry across all layers—including Event Hubs receivers, Databricks jobs, Delta Lake operations, and Synapse queries—by embedding tenantId as a resource attribute. Propagate W3C trace context…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "HTTP-triggered Azure Function (Python) that accepts JSON with tenantId and payload, validates required fields using Pydantic, and writes the payload as a blob to a Storage Account container named after tenantId. Impleme…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an HTTP-triggered Function API to accept multipart images per tenant. Store originals in blob storage under tenants/{tenantId}/images, trigger a second function to generate thumbnails. Cache",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant schema cache with TTL and a versioned JSON Schema registry in Azure Blob Storage. Validate events using a fast JSON Schema validator with Durable Functions fan-out/fan-in patterns to parallelize c…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a high-throughput, tenant-isolated streaming validation pipeline with dynamic schemas and robust failure handling at cloud scale.\n\n## Key Concepts\n\n- Tenant-scoped JSON Schema versioning and caching\n- Durable Functions orchestration patterns and fan-out/fan-in\n- Idempotent writes and dead-lettering strategies\n- End-to-end latency optimization and distributed tracing\n- Schema evolution and backward compatibility\n\n## Code Example\n\n```javascript\n// Pseudocode: schema fetch and cache\nconst getTenantSchema = async (tenantId, version) => {\n  const cacheKey = `${tenantId}:${version}`;\n  let schema = await cache.get(cacheKey);\n  \n  if (!schema) {\n    schema = await blobStorage.get(`schemas/${tenantId}/v${version}.json`);\n    await cache.set(cacheKey, schema, { ttl: 300 }); // 5 minutes\n  }\n  \n  return schema;\n};\n\n// Validation orchestrator\nconst validateEventsOrchestrator = function* (context) {\n  const events = context.getInput();\n  const tasks = events.map(event => \n    context.df.callActivity('validateEvent', event)\n  );\n  \n  const results = yield context.df.Task.all(tasks);\n  \n  const validEvents = results.filter(r => r.isValid);\n  const anomalies = results.filter(r => !r.isValid);\n  \n  yield context.df.callActivity('persistValidEvents', validEvents);\n  yield context.df.callActivity('emitAnomalies', anomalies);\n};\n```\n\n## Testing Strategy\n\n- Unit tests for individual validation functions\n- Integration tests for schema evolution scenarios\n- Load testing with 300k events/sec to verify latency targets\n- Chaos testing for failure scenarios and rollback procedures\n- Tenant isolation verification tests\n\n## Rollback Strategy\n\n- Maintain previous schema versions in blob storage\n- Use feature flags to enable/disable new schema versions\n- Implement canary deployments for schema changes\n- Monitor validation success rates and auto-rollback on threshold breaches",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-13T05:38:04.468Z",
    "version": 1
  }
]