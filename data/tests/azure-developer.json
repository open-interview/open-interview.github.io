[
  {
    "id": "test-azure-developer",
    "channelId": "azure-developer",
    "channelName": "Azure Developer",
    "title": "Azure Developer Knowledge Test",
    "description": "Test your understanding of Azure Developer with 25 questions.",
    "questions": [
      {
        "id": "azure-developer-q-0",
        "questionId": "q-6380",
        "question": "You're building a beginner Azure Functions HTTP API to issue time-limited SAS tokens for per-tenant blob storage access. Implement GET /token/{tenantId}/{container}/{blobName} that reads tenantId from header X-Tenant-ID or path, checks Redis for a cached token with key token:{tenantId}:{container}:{blobName} TTL 300s, and on miss generate a SAS token with read permission for the blob, valid for 5 minutes. Return {token, url, expiry}. Include error handling and fallback to generating a new token if Redis fails?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use tenantId as the Cosmos DB partition key for image metadata; generate a deterministic jobId (tenantId + timestamp) to support idempotency. Enqueue to an Azure Storage Queue; a Worker Function resizes images, stores r…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement an Azure Function using JavaScript/TypeScript with a GET route that accepts the path parameters `tenantId`, `container`, and `blobName`. First, I'd extract the tenantId from either the `X-Tenant-ID` he…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Assign each tenant a residency tag and map it to a primary region. Route all write operations to a region-scoped ADLS Gen2 data lake in the tenant's designated region. Enable geo-redundant replication to a single DR reg…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Generate a time-limited SAS URL per user, then a Blob-triggered Function validates mime type and size and writes metadata to Azure Table Storage with partitionKey=userId, rowKey=uploadId. Use Managed",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical Azure serverless development skills, including multi-tenant architecture design, caching strategies, and secure Azure Storage integration.\n\n## Key Concepts\n- Azure Functions HTTP triggers and routing\n- Redis caching with TTL management\n- Azure Blob Storage SAS token generation\n- Per-tenant isolation patterns\n- Error handling and fault tolerance\n- Caching fallback strategies\n\n## Code Example\n```javascript\n// Azure Function with Redis caching and SAS generation\nconst { BlobServiceClient } = require('@azure/storage-blob');\nconst redis = require('redis');\n\nmodule.exports = async function (context, req) {\n  const { tenantId, container, blobName } = req.params;\n  const headerTenantId = req.headers['x-tenant-id'];\n  const effectiveTenantId = headerTenantId || tenantId;\n  \n  const cacheKey = `token:${effectiveTenantId}:${container}:${blobName}`;\n  \n  try {\n    // Try Redis cache first\n    const cached = await redis.get(cacheKey);\n    if (cached) {\n      return { status: 200, body: JSON.parse(cached) };\n    }\n  } catch (redisError) {\n    context.log.warn('Redis unavailable, generating new token');\n  }\n  \n  // Generate new SAS token\n  const blobServiceClient = BlobServiceClient.fromConnectionString(process.env.AZURE_STORAGE_CONNECTION);\n  const containerClient = blobServiceClient.getContainerClient(container);\n  const blobClient = containerClient.getBlobClient(blobName);\n  \n  const sasToken = await blobClient.generateBlobSasUrl({\n    permissions: 'r',\n    expiresOn: new Date(Date.now() + 5 * 60 * 1000)\n  });\n  \n  const response = {\n    token: sasToken,\n    url: blobClient.url,\n    expiry: new Date(Date.now() + 5 * 60 * 1000).toISOString()\n  };\n  \n  // Cache the token\n  try {\n    await redis.setex(cacheKey, 300, JSON.stringify(response));\n  } catch (cacheError) {\n    context.log.warn('Failed to cache token');\n  }\n  \n  return { status: 200, body: response };\n};\n```",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-1",
        "questionId": "q-18938",
        "question": "Design and implement a beginner-level Azure Functions HTTP API to manage per-tenant configuration: endpoints POST /config to set a config value (tenantId, key, value) and GET /config?tenantId=XYZ to retrieve all keys for that tenant. Store data in Cosmos DB (partition key tenantId) with simple validation, enforce RBAC with Managed Identity, and add basic unit tests; discuss idempotency, retry/backoff, and isolation guarantees?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a durable HTTP-triggered Azure Function with a Cosmos DB SQL API container partitioned by tenantId; POST upserts (tenantId, key) to store value; GET by tenantId returns all keys. Protect with Azur",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "AKS: one namespace per tenant with NetworkPolicy; Azure ML handles model registry and canary deployments; a shared inference runtime enforces per-tenant auth and uses a tenant-scoped feature store; st",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Serverless API design: use Azure Functions with a Redis cache (feat:{tenant}:{feature}) for 30s. On miss, fetch tenant flag from Cosmos (PartitionKey=tenant), fallback to App Configuration defaults, t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design two parallel lanes: a REST API lane for operations and a telemetry ingest lane. **Architecture**: The REST API handles CRUD operations with Cosmos DB partitioned by `tenantId`. The streaming path consumes events…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis checks practical hands-on with per-tenant data partitioning, HTTP API semantics, and secure access patterns in Azure.\n\n## Key Concepts\n- Azure Functions HTTP triggers\n- Cosmos DB partitioning by tenantId\n- Idempotent upserts\n- Managed Identity and RBAC\n- Unit testing Cosmos interactions\n- Input validation and security\n\n## Code Example\n```javascript\nconst httpTrigger = async (context, req) => {\n  // skeleton omitted\n};\nexport default httpTrigger;\n```\n\n## Follow-up Questions\n- How would you test isolation across tenants?\n- What changes would you make to support schema evolution of config items?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-2",
        "questionId": "q-16555",
        "question": "Describe a Functions HTTP API to manage per-tenant feature flags. Implement POST /flags to set flags and GET /flags?tenantId=XYZ to read, using Cosmos DB (SQL API) with tenantId as the partition key. Add a 60s per-instance cache to reduce reads. Explain isolation, race handling, and testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an HTTP-triggered Azure Function that leverages Table Storage with PartitionKey=tenantId and RowKey=fileName to ensure idempotency. First, check if a record exists in Table Storage for the given tenantId and fileNam…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe a Functions HTTP API to manage per-tenant feature flags. Implement POST /flags to set flags and GET /flags?tenantId=XYZ to read, using Cosmos DB (SQL API) with tenantId as the partition key.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Two architectures: (A) micro-batch: IoT Hub → Event Hubs → Functions for lightweight enrichment; Spark Structured Streaming to Delta Lake on ADLS Gen2; feeds to Data Explorer and AKS inference; per-te",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a three-tier caching system: (1) Azure Front Door with Microsoft CDN (Edge Workers) for sub-5ms reads, (2) regional Azure Cache for Redis for 1-5ms, and (3) Azure App Configuration as the source of truth. When flags…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question checks practical Azure Fundamentals: Functions HTTP triggers, Cosmos DB partitioning by tenantId, a simple per-instance TTL cache, and handling race conditions in a beginner-friendly way. Also touches testing strategy.\n\n## Key Concepts\n- Azure Functions HTTP API design\n- Cosmos DB partition key strategy (tenantId)\n- Per-instance in-memory cache with TTL\n- Concurrency: race conditions on updates; idempotent POST\n- Basic testing: unit, integration, end-to-end\n\n## Code Example\n```javascript\n// simplified endpoint sketch\nmodule.exports = async function (context, req) {\n  const tenantId = (req.query.tenantId || req.body?.tenantId);\n  // pseudo-cache check\n  // read/write to Cosmos DB using container.items.upsert({id: flagName, tenantId, value})\n}\n```\n\n## Follow-up Questions\n- How would you extend to multiple regions and cache invalidation?\n- How would you test for race conditions with concurrent POST /flags calls?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-3",
        "questionId": "q-2379",
        "question": "Build a beginner Azure REST API using Azure Functions (HTTP trigger) to manage product data for multiple tenants. Each tenant must be isolated, configs sourced from Azure App Configuration, and telemetry sent to Application Insights. Describe the authentication model, per-tenant data isolation strategy, and a minimal test plan, plus a small code sketch showing API key validation and tenant extraction?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a tenant-scoped lifecycle: partition Delta Lake by tenant and timestamp; use VACUUM with a retention window plus a separate tombstone stream for deletions. Delete markers propagate to Purvie",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant Delta Lake partitions on ADLS Gen2, Databricks Feature Store with versioned schemas, and Azure ML model registry. Inference via Azure Functions behind API Management with tenant RBAC. Purvi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Assign each tenant a dedicated Triton inference server pod on AKS with GPU limits; isolate logs with tenant IDs; use separate model repositories per tenant; enforce quotas via ResourceQuotas and Limit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an HTTP-triggered Function that reads x-api-key and tenantId headers. Validate the key against a value from Azure App Configuration via a Managed Identity. Use Cosmos DB with partitionKey = tenant",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis checks practical Azure capabilities: serverless API, tenant isolation, config via App Configuration, and observability.\n\n## Key Concepts\n\n- HTTP-triggered Azure Functions\n- Azure App Configuration\n- Managed Identity\n- Cosmos DB partitioning\n- Application Insights telemetry\n\n## Code Example\n\n```javascript\n// Pseudo-code: read headers and validate API key against App Configuration\n```\n\n## Follow-up Questions\n\n- How would you rotate API keys and invalidate old ones without downtime?\n- How would you test multi-tenant isolation end-to-end?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-4",
        "questionId": "q-14929",
        "question": "You're building a per-tenant budgeted real-time analytics gateway on Azure. Ingest 200k events/sec via Azure Event Hubs, route per-tenant compute on AKS with KEDA autoscaling, apply real-time rate-limiting and cost accounting, store usage in Cosmos DB with TTL, and expose REST API for tenants to query remaining budget. How would you ensure strict per-tenant isolation, sub-100ms tail latency, fault tolerance, and accurate cost accounting under burst traffic?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Blob-triggered Azure Function (Consumption plan) to process incoming CSV files. First, parse the CSV and validate that the required header row contains ['id', 'timestamp', 'value']. Drop any rows missing these req…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a timer-triggered Durable Function that polls ExportRequests by tenantId (partition key), reads per-tenant data from Cosmos DB, writes a CSV to ADLS Gen2 at /exports/{tenantId}/export-{timestamp}.csv, and updates…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition Cosmos DB by tenant and enforce budget checks at an ingress gateway. Use Event Hubs namespaces per tenant and KEDA-driven AKS pods to isolate workloads. Cache quotas in Azure Redis to keep h",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "CMK per-tenant: assign a Key Vault key per tenant; Databricks Unity Catalog enforces per-tenant isolation; Delta Lake partitions by tenantId on ADLS Gen2; use managed identities for service access; ro",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time, multi-tenant isolation, scalable compute, and precise cost accounting under burst, using Azure services.\n\n## Key Concepts\n- tenant isolation via Cosmos partitions and network policies\n- ingress-budget checks with Redis as fast quota cache\n- KEDA autoscaling on AKS for per-tenant workloads\n- end-to-end latency, backpressure, and auditability\n\n## Code Example\n```javascript\n// Pseudo gateway budget check (high level)\nasync function handleEvent(event) {\n  const budget = await redis.get(event.tenantId);\n  if (budget <= 0) throw new Error('Budget exhausted');\n  await redis.decr(event.tenantId, event.cost);\n  forwardToTenantCompute(event);\n}\n```\n\n## Follow-up Questions\n- How would you test burst scenarios and ensure SLA under regional outages?\n- What tracing and auditing approach would you implement for per-tenant cost reconciliation?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-5",
        "questionId": "q-12018",
        "question": "You're building a beginner-friendly Azure-based webhook/image upload service for a SaaS app. The flow: HTTP endpoint accepts userId and image, validates an Azure AD JWT, stores the image in Blob Storage under a per-user container, saves metadata in Cosmos DB with userId partition, and emits a message to Event Grid to trigger thumbnail processing. How would you implement end-to-end flow, idempotency, auth, and testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect at scale with per-tenant isolation: separate Delta Lake partitions and Purview lineage; mask PII at ingestion via Spark UDF before write; TTL via ADLS Lifecycle and per-tenant retention wind",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Edge devices push telemetry to IoT Hub; a Functions-based ingest layer extracts tenantId from the JWT, writes idempotently to Cosmos DB with tenantId as the partitionKey, and uses eventId as the dedup",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an HTTP-triggered Azure Function that accepts an image upload, validates an Azure AD JWT, stores the image in Blob Storage under a per-user container, indexes metadata in Cosmos DB with userId",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Regionally deployed Functions with Event Grid inputs; per-tenant state in Cosmos DB (partition key = tenantId); durable functions serialize updates per tenant; canary gating via a version field and fe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of Azure auth, storage, and messaging in a practical upload workflow.\n\n## Key Concepts\n- HTTP trigger and multipart/form data handling\n- JWT validation against Azure AD\n- Blob Storage per-user isolation and lifecycle\n- Cosmos DB partitioning by userId\n- Event Grid for async thumbnail processing\n- Idempotency and deduplication\n- Basic to integration tests and observability\n\n## Code Example\n```javascript\n// Minimal Azure Function skeleton showing HTTP trigger, JWT check, and stubs for storage/queue\nmodule.exports = async function (context, req) {\n  const auth = req.headers?.authorization;\n  // validate JWT (pseudo)\n  // const userId = req.query.userId ?? req.body?.userId;\n  // const image = req.body?.image; // multipart parsing in real code\n  // store blob under per-user path\n  // save metadata to Cosmos DB with partitionKey: userId\n  // emit to Event Grid for thumbnail\n  context.res = { status: 200, body: 'Uploaded' };\n};\n```\n\n## Follow-up Questions\n- How would you implement idempotency across retries for the blob write and metadata write?\n- How would you test this flow with mocked services and ensure end-to-end coverage?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-6",
        "questionId": "q-24728",
        "question": "You're building a private, multi-tenant AI inference service on Azure. Each tenant's data and model must be isolated (no cross-tenant access), with per-tenant routing to a private AKS cluster hosting models from Azure ML. Inputs pass through a gateway with Istio, and telemetry is captured end-to-end via OpenTelemetry to Application Insights. Describe the end-to-end architecture, how you enforce data isolation, and a rollout plan that handles model drift, tenant-specific SLAs, and cost control?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition Delta Lake by tenant_id and date in ADLS Gen2; implement Unity Catalog RBAC for strict tenant isolation and utilize per-tenant encryption keys in Azure Key Vault with envelope encryption; employ idempotent pro…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a globe-spanning telemetry pipeline: MQTT to IoT Hub, events to Event Hubs, Durable Functions for windowed analytics, per-tenant Cosmos DB with tenantId partitioning. Target sub-200ms tails, st",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an Azure Functions HTTP-triggered /upload endpoint that accepts tenantId in the X-Tenant-ID header and multipart/form-data file uploads. Use Node.js with Azure SDK: ```javascript const { CosmosClient } = requi…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a gateway-first flow with per-tenant routing to a multi-tenant AKS cluster. Isolate tenants via Kubernetes namespaces, per-tenant secrets in Key Vault, and AAD-based RBAC. Host models in Azu",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses real-world ability to design private multi-tenant AI workloads on Azure with strong isolation, private services, and end-to-end observability.\n\n## Key Concepts\n- Tenant isolation via namespaces, RBAC, and Key Vault secrets\n- Private endpoints for Azure ML and private AKS networking\n- Istio routing, gateway design, per-tenant inference paths\n- End-to-end observability with OpenTelemetry to Application Insights\n- Drift detection, retraining pipelines, and cost-aware autoscaling\n\n## Code Example\n```javascript\n// OpenTelemetry setup snippet (pseudo)\nconst { NodeTracerProvider } = require('@opentelemetry/node');\nconst provider = new NodeTracerProvider();\nprovider.addSpanProcessor(new SimpleSpanProcessor(new ConsoleSpanExporter()));\nprovider.register();\n// attach tenant_id to trace attributes at ingress\n```\n\n## Follow-up Questions\n- How would you validate isolation boundaries in tests?\n- What would your rollback strategy look like for a failing drift-retrain?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-7",
        "questionId": "q-3999",
        "question": "You're operating a global multi-tenant event pipeline on Azure: Event Hubs ingest, Azure Functions processing, and Cosmos DB storage with per-tenant isolation. How would you implement end-to-end observability using OpenTelemetry and Azure Monitor to achieve tenant-aware tracing across components, with correlation IDs, dynamic sampling, and no cross-tenant leakage? Describe instrumentation points, data retention, and costs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On log ingestion, retrieve the tenant's TTL configuration from Azure App Configuration (using the environment label), set logItem.ttl to the configured ttlDays value, and write the document to Cosmos DB using tenantId a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Read tenantId from headers. Use Azure Cache for Redis to enforce a per-tenant fixed-window quota: key tenant:{tenantId}:{YYYYMMDDHHMM}, INCR on each call and set EXPIRE to 60s. If count > quota (from",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Post /profiles upserts a profile with tenantId as the partition key; GET /profiles?tenantId=XYZ lists profiles. Use a Cosmos DB container with partition key /tenantId. Implement idempotent upsert and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument Event Hubs, Functions, and Cosmos DB with OpenTelemetry. Propagate tenantId and traceId across boundaries, push to Azure Monitor via the OpenTelemetry collector, and store traces in a per-t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical knowledge of end-to-end observability in Azure for multi-tenant data pipelines, including cross-service tracing, privacy, and cost considerations.\n\n## Key Concepts\n- OpenTelemetry integration with Azure Functions and Event Hubs\n- Trace context propagation (tenantId, traceId)\n- Azure Monitor / Log Analytics backend for traces\n- Per-tenant isolation in logs and data paths\n- Dynamic sampling and cost-aware tracing, privacy controls\n\n## Code Example\n```javascript\n// Node.js (simplified sketch)\nconst { trace, context, propagation } = require('@opentelemetry/api');\nconst { registerInstrumentations } = require('@opentelemetry/instrumentation');\nconst { HttpInstrumentation } = require('@opentelemetry/instrumentation-http');\n\nregisterInstrumentations({ instrumentations: [new HttpInstrumentation()], tracerProvider: /*...*/ });\n\nmodule.exports = async function (context, req) {\n  const tenantId = req.headers['x-tenant-id'];\n  const span = trace.getTracer('azure-trace').startSpan('function.process', {\n    attributes: { tenantId }\n  });\n  return await context.bindings?.invoke(req, { parent: span.context() });\n};\n```\n\n## Follow-up Questions\n- How would you validate dynamic sampling across tenants with different load patterns?\n- How would you detect and remediate telemetry leaks that cross tenant boundaries?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-8",
        "questionId": "q-13278",
        "question": "You're building a multi-tenant telemetry analytics pipeline on Azure for a global IoT device fleet. Ingest from devices via IoT Hub to Event Hubs at 200k msgs/sec, process with Azure Databricks into Delta Lake on ADLS Gen2, enforce strict per-tenant isolation (RBAC + per-tenant keys). How would you design data layout, governance, rollback, and per-tenant dashboards?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Assign each tenant a dedicated Triton inference server pod on AKS with GPU limits; isolate logs with tenant IDs; use separate model repositories per tenant; enforce quotas via ResourceQuotas and Limit",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deploy a single AKS cluster with per-tenant namespaces isolated by network policies and mTLS. Implement an Envoy-based gateway that routes requests to tenant-specific inference endpoints using path-based routing. Store…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Generate a per-tenant SAS URL for direct upload to Blob Storage at tenantId/docs/{filename}, then persist metadata in Cosmos DB with partitionKey=tenantId. Use Event Grid to trigger a thumbnail/virus-",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition Delta Lake by tenant_id and date in ADLS Gen2; implement Unity Catalog RBAC for strict tenant isolation and utilize per-tenant encryption keys in Azure Key Vault with envelope encryption; employ idempotent pro…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis evaluates expertise in designing scalable multi-tenant lakehouse architectures with robust data isolation, governance, and operational resilience across Azure services.\n\n## Key Concepts\n- Delta Lake on ADLS Gen2 with tenant_id and date partitioning strategy\n- Databricks Unity Catalog RBAC for tenant-scoped access control\n- Envelope encryption using per-tenant keys managed in Azure Key Vault\n- Delta Time Travel for point-in-time rollbacks and schema evolution\n- Microsoft Purview governance policies and compliance enforcement\n- Per-tenant analytics dashboards powered by Synapse serverless pools\n- Critical testing scenarios: schema evolution validation, retry mechanisms, idempotency verification, and access control auditing\n\n## Code Example\n```python\n# PySpark implementation for idempotent upsert operation\n```\n\nThis solution demonstrates enterprise-grade multi-tenant architecture with comprehensive security, governance, and operational excellence requirements.",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-9",
        "questionId": "q-1248",
        "question": "Design an end-to-end Azure ingestion pipeline for multi-tenant IoT events: thousands of devices per region send JSON to a gateway, per-tenant aggregates stored in Cosmos DB, raw data archived to Data Lake Gen2. Explain chosen services (Event Hub, Function/Durable Function, Cosmos DB with TTL, Data Lake), how you enforce per-tenant isolation and auditability, and how you achieve exactly-once processing and retry semantics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a pipeline: Ingest events via Event Hubs, process with Databricks Structured Streaming to produce per-vehicle aggregates on a sliding window with watermarking; write to Cosmos DB using idemp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Event Hubs in a regional namespace to ingest; route to Durable Functions that implement idempotent writes using a composite key (tenantId, messageId). Cosmos DB with partitionKey=tenantId stores a",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I would first implement core tenant isolation by validating X-Tenant-Id and X-API-Key headers against a configuration map, then routing Cosmos DB reads and writes using tenantId as the partition key. This ensures each t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement OpenTelemetry across Event Hubs, Durable Functions, Cosmos DB, and ADX; propagate traceparent/tracestate and tenant_id in message properties; route traces to Application Insights and data ex",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical Azure data-pipeline design, multi-tenancy, and reliability in production.\n\n## Key Concepts\n- Event Hubs, Durable Functions, Cosmos DB, Data Lake Gen2\n- Per-tenant partitioning, RBAC, Managed Identities\n- Deduplication, idempotent writes, retry strategies\n\n## Code Example\n```javascript\n// Pseudo Durable Function outline for idempotent processing\n```\n\n## Follow-up Questions\n- How would you test for cross-tenant data leakage?\n- How would you monitor latency and backpressure across regions?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-10",
        "questionId": "q-16064",
        "question": "You're building a cross-region, multi-tenant analytics pipeline on Azure for a global fintech client. Ingest 80k events/sec via Event Hubs; process in AKS with a stateful stream operator; store per-tenant aggregates in Cosmos DB (tenantId as partition key); export BI-ready tables to Synapse. How would you guarantee per-tenant isolation, sub-200ms live latency, exactly-once semantics, and end-to-end data lineage? Include testing and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-region pipeline with Event Hubs for ingestion, AKS for stateful processing, and Cosmos DB with tenantId as the partition key. Ensure exactly-once sinks via idempotent writes and dedupli",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a Durable Functions orchestrator to model a per-tenant job lifecycle: enqueue metadata in Cosmos DB (tenantId as partition key), trigger Data Factory/Synapse pipelines, and write results back to C",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design region-local Event Hub shards per tenant, with separate ADLS Gen2 accounts per region for strict data isolation. Use Delta Lake with per-tenant partitioning and Spark jobs in Databricks with deterministic job IDs…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a Durable Functions orchestrator to fan-out per-tenant processing. Route Event Hub events to Functions using a per-tenant partition key and keep workers warm for sub-200ms latency. Use Cosmos",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses real-world trade-offs in multi-region, multi-tenant data pipelines: isolation guarantees, latency budgets, and end-to-end lineage with governance.\n\n## Key Concepts\n\n- Tenant isolation across storage and compute layers\n- Exactly-once processing and deduplication\n- Latency budgeting for live streams\n- Data lineage and governance via Purview\n- Observability with OpenTelemetry\n\n## Code Example\n\n```python\n# Pseudo-idempotent sink sketch\ndef sink(event, state):\n    if not exists_in_store(event.id):\n        write(event.data, tenant=event.tenant, id=event.id)\n        ack(event.id)\n```\n\n## Follow-up Questions\n\n- How would you implement cross-region failover and data reconciliation?\n- What metrics and alerts would you rely on to detect drift or backlog growth?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-11",
        "questionId": "q-1277",
        "question": "Design a real-time multi-tenant feature-store pipeline on Azure for a high-velocity AI platform. Ingest telemetry events via Event Hubs (tenantId, featureName, value, ts). Build end-to-end streaming with exactly-once semantics, isolation by tenant, and low-latency online reads. Specify concrete components (Event Hubs, Spark Structured Streaming, Cosmos DB with tenantId partition, Redis online store), auditability, TTL, and testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would first implement a minimal tenant document in Cosmos DB with tenantId as the partition key, paired with an HTTP-triggered Azure Function that validates the tenant ID header and serves the /config endpoint. A Mana…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Store notes in Cosmos DB with partition key /tenantId and id; implement GET /notes/{tenantId} and POST /notes/{tenantId}. Encrypt content with a Key Vault key via DefaultAzureCredential before save an",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingress: Event Hubs (tenantId key). Processing: Spark Structured Streaming with strict checkpointing for exactly-once; writes to Cosmos DB partitioned by tenantId (upsert for idempotence) plus a Redis",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "An Azure-native, multi-tenant feature-flag service: per-tenant flags live in Azure App Configuration with per-tenant configuration sections; reads are cached in Azure Redis Cache with a short TTL (e.g., 30 seconds) and…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess ability to architect multi-tenant streaming systems on Azure with strong data isolation, idempotence, and end-to-end correctness.\n\n## Key Concepts\n- Event Hubs + Spark Structured Streaming\n- Cosmos DB partitioning by tenantId\n- Upsert semantics for idempotence\n- Redis as online store for latency\n- Audit/logging, TTL, RBAC and data isolation\n\n## Code Example\n```javascript\n// Pseudocode for upsert into Cosmos DB to preserve idempotence\nfunction saveFeature(tenantId, key, value, timestamp) {\n  const doc = { id: `${tenantId}:${key}`, tenantId, key, value, ts: timestamp };\n  cosmos.upsertItem(doc);\n}\n```\n\n## Follow-up Questions\n- How would you test end-to-end with replay and fault injection?\n- How would you enforce data retention, privacy, and tenant isolation in practice?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-12",
        "questionId": "q-4567",
        "question": "You're building a global, real-time feature-flag evaluation service on Azure. End users require a response in ~40ms; flags are stored in Azure App Configuration with per-tenant labels and dynamic rules. How would you design data flow, caching, eviction, audit logging, and failover to meet latency and correctness under partial outages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "HTTP-triggered Azure Function (Python) that accepts JSON with tenantId and payload, validates required fields using Pydantic, and writes the payload as a blob to a Storage Account container named after tenantId. Impleme…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a cache-aside pattern using Azure Cache for Redis with tenant-scoped keys (tenantId:flagName). Authoritative flag configurations reside in Azure App Configuration with per-tenant labels and dynamic rules. On r…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage Azure App Configuration feature flags with per-tenant labels and environment separation. Resolve flags in API Management/Functions, cache decisions per tenant, and use a deterministic hash on",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an HTTP-triggered Azure Function that receives {tenantId, appointmentTime}. Use Cosmos DB with partitionKey = tenantId and id = tenantId|appointmentTime to ensure per-tenant isolation and idempotent writes. Re…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses real-time flag evaluation, caching strategy, per-tenant isolation, and resiliency under partial outages.\n\n## Key Concepts\n\n- Cache-aside with Azure Cache for Redis\n- Azure App Configuration tenant scoping and labels\n- TTL, eviction, prefetching, and cache warmup\n- Event Grid change notifications for invalidation\n- Auditing and tracing with Azure Monitor/OpenTelemetry\n\n## Code Example\n\n```javascript\nasync function getFlag(tenantId, flagName) {\n  const key = `${tenantId}:${flagName}`;\n  let val = await redis.get(key);\n  if (val) return JSON.parse(val);\n  const flag = await appConfig.getFlag(tenantId, flagName);\n  await redis.setex(key, flag.ttl, JSON.stringify(flag));\n  return flag;\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-13",
        "questionId": "q-23378",
        "question": "You're building a real-time telemetry pipeline for a globally distributed game with strict data residency. Ingest 2M events/sec via Azure Event Hubs, process with Databricks Structured Streaming into Delta Lake on ADLS Gen2, and expose aggregated per-tenant dashboards in Synapse Serverless. How would you implement event-time processing with watermarks to handle late arrivals, guarantee exactly-once semantics, and mitigate data skew across tenants? Include testing for late data and a failover plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant identities (AAD) with API Management RBAC to enforce scopes; isolate data in Cosmos DB with tenantId partitioning and separate containers; enable envelope encryption using customer-mana",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Spark Structured Streaming (Databricks) to read from Event Hubs, apply a 10-minute watermark on eventTime, and upsert into Delta Lake with (tenantId, eventId) as the primary key. Delta ACID guaran",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement per-tenant model versioning in Azure ML; route requests via API Management to tenant-scoped Container Apps with canary deployments (5–10% on new model). Isolate data in Delta Lake on ADLS Ge",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Cosmos DB with tenantId as the partition key, enable multi-region writes in a primary region with a configured DR region and explicit failover priorities. Target RPO of minutes and RTO under 1 hou",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-time pipeline design, event-time semantics, and data residency across Azure services with concrete trade-offs and verification needs.\n\n## Key Concepts\n\n- Spark Structured Streaming with watermarks\n- Delta Lake ACID upserts and tombstones\n- Event-time vs processing-time semantics\n- Tenant-aware partitioning and data skew mitigation\n- Checkpointing, fault tolerance, and DR planning\n- Testing with late data scenarios and failover strategies\n\n## Code Example\n\n```scala\n// skeleton: Event Hubs to Delta Lake with watermark\nimport org.apache.spark.sql.functions._\nval df = spark.readStream.format(\"eventhubs\").load()\nval parsed = df.selectExpr(\"CAST(body AS STRING) as json\")\n  .select(from_json(col(\"json\"), schema).as(\"d\"))\n  .select(\"d.*\")\nval withTs = parsed.withColumn(\"eventTime\", col(\"eventTime\"))\nval w = withTs.withWatermark(\"eventTime\", \"10 minutes\")\nval q = w.writeStream.format(\"delta\")\n  .option(\"checkpointLocation\",\"abfss://.../checkpoints\")\n  .option(\"path\",\"abfss://.../delta/tenant\")\n  .start()\n```\n\n## Follow-up Questions\n\n- How would you validate residency across regions and purge non-compliant data?\n- What strategies mitigate spikes and skew across tenants in real time?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-14",
        "questionId": "q-6066",
        "question": "You're designing a per-tenant isolation architecture for a real-time vehicle telemetry analytics service on Azure: use AKS namespaces or a tenant-scoped ML workspace, Event Hubs ingestion, Durable Functions preprocessing, Delta Lake on ADLS Gen2 for tenant data, per-tenant feature flags via App Configuration, data masking in ETL, Purview lineage/audit, TTL policies, and a per-tenant canary rollout with traffic shifting and rollback plan. Include tests and fault-injection?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design should enforce tenant isolation via AKS namespaces or dedicated ML workspaces, with Event Hubs for ingestion, Durable Functions for preprocessing, and Delta Lake on ADLS Gen2 for per-tenant dat",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "**Step 1: Enable System-Assigned Managed Identity** *Portal:* 1. Go to your Function App in Azure Portal 2. Navigate to **Identity** → **System assigned** 3. Set **Status** to **On** 4. Click **Save** 5. Copy the **Obje…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a two-layer design: 1) per-tenant data isolation in the storage layer (Cosmos DB with RLS or Delta Lake with tenant keys) and 2) per-tenant quotas in the API layer using API Management polic",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a versioned schema in Azure Schema Registry, allow additive, nullable changes only; enforce via Delta Lake schema evolution on ADLS Gen2. Implement data quality gates in a dedicated Function tha",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes advanced Azure architecture skills across multi-tenant isolation, streaming ingestion, lakehouse storage, feature flag governance, data masking, and controlled deployment strategies. It also tests testability and disaster recovery planning in a realistic setting.\n\n## Key Concepts\n\n- Per-tenant isolation via AKS namespaces or tenant-scoped ML workspace\n- Ingestion with Event Hubs and event-driven orchestration with Durable Functions\n- Delta Lake on ADLS Gen2 for tenant-scoped storage\n- Feature flags per tenant using Azure App Configuration\n- Data masking during ETL and privacy-by-design\n- Data lineage and audits with Azure Purview\n- TTL policies for data retention\n- Canary rollouts and rollback strategies per tenant\n\n## Code Example\n\n```javascript\n// Pseudo: canary rollout gate per tenant\nfunction shouldRouteToCanary(tenantId, rolloutConfig) {\n  const rate = rolloutConfig[tenantId] || 0;\n  return Math.random() < rate;\n}\n```\n\n## Follow-up Questions\n\n- How would you test tenant isolation boundaries under burst load and simulate data leakage risk?\n- Describe a failure mode and automated rollback process for a tenant during a canary release.",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-15",
        "questionId": "q-2059",
        "question": "Design an Azure-native data export service for a fintech that must export per-customer data on demand and on a schedule, with strict data residency, consent checks, and an audit trail. Use ADLS Gen2, Cosmos DB, Event Grid, Durable Functions or Functions, and Key Vault. Explain data partitioning, encryption, access control, idempotency, and failure modes including retries, outages, and rollback. Provide a concrete data model and flow?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A Durable Functions orchestrator triggers per-customer export jobs after validating consent in Cosmos DB. Data is read from source tables, streamed to ADLS Gen2 under tenant-specific prefixes, encrypted with customer-ma…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use API Management to enforce OAuth per-tenant, with OpenTelemetry context propagated through the Function to Cosmos DB; choose tenantId as the Cosmos partition key and enable Cosmos DB multi-region w",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route events by tenant using Event Hubs partition keys for ordered processing; perform lightweight pre-processing in Azure Functions with deterministic idempotency keys; write per-tenant data to Cosmos DB containers wit…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition by tenantId; store documents plus a CRDT delta log (OR-Set) for concurrent edits; leverage Cosmos DB multi-region writes with Session consistency; use ETag and If-Match for optimistic concur",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates real-world enterprise concerns: per-customer data exports, consent management, data residency compliance, auditability, and failure handling within Azure-native architectures.\n\n## Key Concepts\n- Durable Functions orchestration patterns and idempotency\n- Customer-managed keys (CMK) with Azure Key Vault\n- Consent management and compliance workflows\n- Immutable audit logging and cross-region replication\n- Tenant-based data partitioning and AAD access control\n\n## Code Example\n```javascript\n// Pseudo: orchestrator outline\n```\n\n## Follow-up Questions\n- How would you handle large-scale concurrent exports?\n- What monitoring and alerting strategies would you implement?\n- How do you ensure data consistency during partial failures?\n- What strategies would you use for cost optimization?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-16",
        "questionId": "q-7077",
        "question": "You're building a cross-tenant incident routing system integrating Salesforce and Slack events into an Azure-driven workflow. Ingest events from Salesforce and Slack via Azure Event Hubs (peak ~60k msgs/sec), deduplicate, and route to per-tenant queues. Orchestrate triage with Durable Functions, persist state in Cosmos DB (tenantId as PartitionKey), and log auditable trails. How would you design for latency, fault tolerance, data residency, idempotency, and end-to-end observability across services?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Register per-tenant schemas in Unity Catalog and enforce Delta constraints aligned with each tenant's requirements. Implement a Spark-based drift checker that compares incoming schemas against a tenant registry, then re…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe an end-to-end per-tenant data path: ingest via Event Hubs, tenant-scoped routing using message headers, per-tenant durable functions orchestration, and ADLS Gen2 folders /tenants/{tenantId}/.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant Cosmos DB partitioning (tenantId), Event Hubs dedupe by eventId, and a Durable Functions orchestrator that fans out to tenant queues. Enrich in-flight with metadata from Salesforce/Slac",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a CDC-style pipeline: ingest via Event Hubs, emit tenant-scoped changes, and apply Delta Lake MERGE upserts partitioned by tenantId in ADLS Gen2; apply per-tenant masking on reads, TTL via VACU",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a cross-service, multi-tenant incident routing flow with real-time streaming, serverless orchestration, and strict data isolation. Emphasizes correctness, observability, and rollback in a pragmatic Azure context.\n\n## Key Concepts\n\n- Event deduplication across Event Hubs\n- Durable Functions orchestrations and fan-out\n- Tenant data isolation in Cosmos DB\n- Auditing and traceability with App Insights/OpenTelemetry\n\n## Code Example\n\n```javascript\n// Pseudo snippet: idempotent event handler\n```\n\n## Follow-up Questions\n\n- How would you test idempotency under replay scenarios?\n- How would you implement tenant-specific rate limits and backpressure?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-17",
        "questionId": "q-30457",
        "question": "You're a beginner Azure developer tasked with building a small REST API: an HTTP-triggered Function backed by Cosmos DB. Add per-tenant isolation via tenantId. Implement a new /export endpoint that for a given tenant streams data to a CSV stored in Blob Storage and returns a SAS URL for a download; ensure idempotency with a marker in Cosmos DB and a reasonable time-bounds; describe minimal CI/CD and tests. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Step 1: Enable System-Assigned Managed Identity** *Portal:* 1. Go to your Function App in Azure Portal 2. Navigate to **Identity** → **System assigned** 3. Set **Status** to **On** 4. Click **Save** 5. Copy the **Obje…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement tenant validation and partition key enforcement first. This establishes the security foundation before any data operations. Without rigorous tenantId validation and strict partition key usage on all re…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I would instrument all requests with OpenTelemetry, setting tenantId as a trace attribute and as Cosmos DB partition key. Use HttpClient instrumentation to propagate traceparent to OpenAI API, and exp",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant isolation via Delta Lake databases per tenant managed by Unity Catalog with VNet-protected ADLS Gen2; exactly-once via outbox pattern: commit row then deduplicated sink to Delta Lake, struc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests your ability to design a multi-tenant data export system with proper security isolation, idempotency, and secure download mechanisms—all while demonstrating practical Azure Functions knowledge. It also assesses understanding of CI/CD and testing priorities.\n\n## Key Concepts\n- HTTP-triggered Azure Functions for REST APIs\n- Cosmos DB partition keys for per-tenant isolation\n- Azure Blob Storage with streaming uploads\n- SAS tokens for secure, time-limited downloads\n- Idempotent export markers to prevent duplicate exports\n- Integration testing with emulators\n\n## Follow-up Questions\n- How would you secure the endpoint against abuse (rate limiting, token validation)?\n- How would you handle large datasets that exceed memory limits?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-18",
        "questionId": "q-29170",
        "question": "You're a beginner Azure developer tasked with building a small multi-tenant SaaS API: an HTTP-triggered Azure Function reads tenantId from a header, stores per-tenant metadata in Cosmos DB with tenantId as the partition key, and exposes /config to fetch feature flags from Azure App Configuration. Implement per-tenant access checks, a simple in-memory TTL cache for access tokens (5 minutes), and a minimal dev/stage/prod CI/CD pipeline. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a gateway-first flow with per-tenant routing to a multi-tenant AKS cluster. Isolate tenants via Kubernetes namespaces, per-tenant secrets in Key Vault, and AAD-based RBAC. Host models in Azu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant schema registry with versioned JSON schemas stored in Azure Blob, attach tenantId to each event, and route through Event Hubs. Use Delta Lake schema evolution to add optional fi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a beginner Azure Functions HTTP API for per-tenant notes with GET /notes/{tenantId} and POST /notes/{tenantId}. Use Cosmos DB partitioned by tenantId. Add API-key authentication by reading t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would first implement a minimal tenant document in Cosmos DB with tenantId as the partition key, paired with an HTTP-triggered Azure Function that validates the tenant ID header and serves the /config endpoint. A Mana…",
            "isCorrect": true
          }
        ],
        "explanation": "## What This Question Assesses\n\nThis question evaluates end-to-end understanding of multi-tenant isolation, Azure App Configuration integration, and lightweight caching within an Azure Function. It also tests practical CI/CD knowledge across dev/stage/prod environments and the ability to validate per-tenant behavior.\n\n## Key Concepts\n\n- Cosmos DB with tenantId as the partitionKey for tenant isolation\n- Azure App Configuration for per-tenant feature flags\n- Managed Identity for secure access to App Configuration\n- In-memory TTL cache per tenant (300 seconds) for flag data\n- Header-based tenant identification for request routing",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-19",
        "questionId": "q-29025",
        "question": "You're a beginner Azure developer tasked with building a file-upload API: an HTTP-triggered Function that accepts multipart uploads and stores files in a tenant-scoped Blob container (tenantId from headers). Add a TTL-based cleanup using a Timer Trigger reading TTL per tenant from Azure App Configuration. Describe a small dev/stage/prod CI/CD with GitHub Actions and tests verifying cross-tenant isolation and TTL rollout. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan to implement per-tenant isolation with a streaming pipeline: Event Hubs ingress, Functions orchestration, and Synapse-based exports; apply masking/policy checks before export; attach trace contex",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a globe-spanning telemetry pipeline: MQTT to IoT Hub, events to Event Hubs, Durable Functions for windowed analytics, per-tenant Cosmos DB with tenantId partitioning. Target sub-200ms tails, st",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Event Hubs intake per tenant (60k msgs/sec), dedupe by (tenantId,eventId), and orchestrate with Durable Functions. Persist to Cosmos DB with tenant-scoped partitions and region-bound accounts",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement HTTP-triggered upload to tenant-specific blob containers, with tenantId validated from headers. Add a Timer Trigger cleanup that deletes blobs older than TTL from App Configuration per tenan",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical use of Blob Storage for per-tenant isolation, TTL-based data retention, and a lightweight CI/CD approach.\n\n## Key Concepts\n\n- HTTP-triggered Azure Functions and streaming uploads\n- Azure Blob Storage per-tenant isolation\n- Azure App Configuration for per-tenant TTL\n- Timer-triggered background cleanup\n- Basic end-to-end tests and environment promotion via GitHub Actions\n\n## Code Example\n\n```javascript\n// Upload handler sketch\nimport { BlobServiceClient } from '@azure/storage-blob';\nmodule.exports = async function (context, req) {\n  const tenant = req.headers['tenantid'];\n  const file = req.body; // multipart parsed in real impl\n  const container = client.getContainerClient(tenant);\n  await container.createIfNotExists();\n  const blob = container.getBlockBlobClient(file.name);\n  await blob.uploadData(file.buffer);\n  context.res = { status: 200, body: 'ok' };\n};\n```\n\n## Follow-up Questions\n\n- How would you enforce tenantId validation to prevent cross-tenant access?\n- What are the trade-offs between per-tenant containers vs a single container with tenant prefixes, regarding costs and RBAC?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-20",
        "questionId": "q-5741",
        "question": "You're building a real-time ride dispatch pipeline for a multi-city fleet (Lyft/Uber) on Azure. Ingest GPS pings and trip events at high throughput via Azure Event Hubs, make ETA and dispatch decisions with Durable Functions, store per-city state in Cosmos DB (cityId as PartitionKey), and publish updates to Redis for driver apps and dashboards. How would you design to meet sub-100ms ETA latency, per-city isolation, idempotent retries, and auditable trails across services?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would start by implementing Azure App Configuration to expose per-tenant feature flags, then integrate it with the function runtime to load tenant-specific overrides using the tenantId context. This establishes the fo…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition per city in Cosmos DB (cityId) and use Durable Functions for stateful dispatch. Ingest via Event Hubs with a city-scoped consumer group; compute ETA/dispatch in a low-latency path; cache lat",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Architect a per-tenant real-time risk pipeline: ingest 100k events/sec from Event Hubs; run Databricks Structured Streaming to compute risk scores per tenant; write raw and enriched data to Delta Lake",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant isolation via ADLS Gen2 prefixes and per-tenant accounts; use Event Grid to trigger Durable Functions orchestrations for exports and deletions with a hold; enable immutable blob storage for",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests practical Azure design skills for low-latency, scalable dispatch with strict data isolation across cities.\n\nKey Concepts\n- Event Hubs with city-scoped consumers, Durable Functions for workflow state, Cosmos DB partitioning by city\n- Low-latency data path and per-city caching in Redis; idempotent processing via dedup keys\n- Auditing and observability through Cosmos logs and Application Insights; backpressure and DLQ handling\n\nCode Example\n```javascript\n// Pseudo: deduplicate and process ETA update\nasync function processEvent(evt) {\n  const id = `dedup:${evt.eventId}`;\n  if (await redis.exists(id)) return;\n  await redis.set(id, 1, 'EX', 300);\n  // compute ETA, persist, publish to Redis channels\n}\n```\n\nFollow-up Questions\n- How would you test end-to-end latency under burst load and ensure per-city SLA adherence?\n- What tracing and failure-rollback strategies would you add for reliability under partial outages?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-21",
        "questionId": "q-25079",
        "question": "You're building a privacy-preserving analytics service on Azure for multiple car OEMs. Telemetry streams from IoT Hub are ingested into Delta Lake on ADLS Gen2, with per-tenant isolation and on-ingestion PII redaction. Design cross-region DR (East US and West Europe), Unity Catalog RBAC, and a per-tenant cost-aware autoscale strategy. Include validation and rollback tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would first implement a minimal tenant document in Cosmos DB with tenantId as the partition key, paired with an HTTP-triggered Azure Function that validates the tenant ID header and serves the /config endpoint. A Mana…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect per-tenant isolation via Unity Catalog databases; ingest IoT Hub telemetry into Delta Lake on ADLS Gen2 with on-ingestion PII redaction in Spark; enable cross-region DR with active-active Re",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a compensating transaction pattern in Durable Functions: assign a durable id per event, write a precommit marker, then finalize writes only after downstream success; on failure, trigger comp",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design: Telemetry flows through Event Hubs → Azure Functions trigger → Durable Functions orchestrates the full workflow → writes to tenant storage (Azure Blob or S3-compatible). Each tenant configures dedicated redactio…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDiscusses cross-region DR and per-tenant isolation plus PII redaction.\n\n## Key Concepts\n- Azure IoT Hub\n- Delta Lake on ADLS Gen2\n- Unity Catalog RBAC\n- cross-region DR\n- data redaction at ingestion\n- per-tenant autoscale and cost controls\n\n## Code Example\n```javascript\n// PII redaction UDF (pseudo)\nfunction redactPII(value) {\n  if (value == null) return null;\n  return value.substring(0, 4) + \"***\";\n}\n```\n\n## Follow-up Questions\n- How would you test DR failover and data consistency across regions?\n- Compare per-tenant isolation via separate catalogs vs shared catalogs with row-level security; what are the trade-offs?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-22",
        "questionId": "q-28645",
        "question": "You're a beginner Azure developer tasked with adding per-tenant response caching to a REST API built with an HTTP-triggered Azure Function and Cosmos DB. Use Azure Cache for Redis to cache GET results by tenantId and endpoint path. Describe cache key schema, TTL, cache invalidation on writes, and how you'd validate caching correctness across dev/stage/prod. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a two-layer design: 1) per-tenant data isolation in the storage layer (Cosmos DB with RLS or Delta Lake with tenant keys) and 2) per-tenant quotas in the API layer using API Management polic",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Generate a per-tenant SAS URL for direct upload to Blob Storage at tenantId/docs/{filename}, then persist metadata in Cosmos DB with partitionKey=tenantId. Use Event Grid to trigger a thumbnail/virus-",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "IoT Hub -> Durable Functions orchestrator keyed by tenantId; deduplicate by messageId with a per-tenant marker in Cosmos DB; batch writes to Delta Lake on ADLS Gen2 using MERGE for upserts; encrypt se",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant response caching using Azure Cache for Redis with a key schema of `{tenantId}:{method}:{path}:{queryHash}`. Set a TTL of 300 seconds. On any POST, PUT, or DELETE operation, invalidate the cache by p…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical use of caching to reduce Cosmos DB read latency and RU consumption while maintaining strict per-tenant isolation. Evaluates end-to-end system thinking, including invalidation strategies, key schema design, and deployment considerations across environments.\n\n## Key Concepts\n- Per-tenant cache key schema and TTL configuration\n- Cache-aside pattern with selective invalidation\n- Event-driven cache invalidation via Event Grid\n- Observability: cache hit rate, latency metrics, and eviction counts\n- Environment isolation: separate Redis instances for dev/stage/prod\n\n## Code Example\n```javascript\nfunction redisKey(tenantId, method, path, query) {\n  const queryHash = hashQueryString(query);\n  return `${tenantId}:${method}:${path}:${queryHash}`;\n}\n\nasync function getCachedOrFetch(ctx, tenantId, method, path, query) {\n  const key = redisKey(tenantId, method, path, query);\n  const cached = await redis.get(key);\n  if (cached) return JSON.parse(cached);\n\n  const result = await cosmosQuery(tenantId, path, query);\n  await redis.setex(key, 300, JSON.stringify(result));\n  return result;\n}\n\nasync function invalidateTenantCache(tenantId) {\n  const pattern = `${tenantId}:*`;\n  for await (const key of redis.scanIterator({ match: pattern })) {\n    await redis.del(key);\n  }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-23",
        "questionId": "q-27799",
        "question": "You're a beginner Azure developer building a multi-tenant notes API: an HTTP-triggered Azure Function supports create/read/update/delete notes. Notes stored in Cosmos DB with tenantId as the partition key. Implement idempotent creates and optimistic concurrency control for updates using ETag, and add per-tenant feature flag in Azure App Configuration to enable/disable the update endpoint. Describe a minimal dev/stage/prod deployment, test plan, and how you'd validate per-tenant behavior and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a low-latency read path: GET /flags?tenantId=XYZ&env=prod&userId=ABC returns active flags for that user within ~50ms at 20k rps. The architecture uses Azure App Configuration for centralized flag defin…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement idempotent POSTs by requiring an idempotencyKey header and guarding writes in Cosmos DB (or a small cache) so duplicates are ignored. For updates, use ETag and If-Match for optimistic concur",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use multi-region Cosmos DB with tenantId partition key and multi-region writes for DR; ingest 60-100k events/s via Event Hubs with geo-DR to a DR region; real-time per-tenant aggregates computed by Du",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Route by tenant ID to per-tenant ML endpoints; partition Delta Lake by tenant; use streaming with per-tenant watermarking; manage models with versioning in Azure ML and drift detection; implement quot",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests a candidate's ability to design robust CRUD with multi-tenant isolation, idempotency, and optimistic concurrency in a serverless Azure context.\n\n## Key Concepts\n- Idempotency with idempotency keys across HTTP POST\n- Optimistic concurrency using ETag and If-Match in Cosmos DB\n- Per-tenant feature flags via Azure App Configuration\n- Per-env deployment (dev/stage/prod) and rollback strategies\n\n## Code Example\n```javascript\n// Pseudo: handle POST with idempotencyKey, store in Cosmos, skip duplicates\n```\n\n## Follow-up Questions\n- How would you test concurrent updates to the same note?\n- How would you migrate existing data to surface per-tenant flags without downtime?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-24",
        "questionId": "q-21019",
        "question": "You're building a beginner Azure developer API for per-tenant feature flags. Use an HTTP Function and Cosmos DB (SQL API) with tenantId as the partition key and flagName as the document id. Endpoints: POST /flags/{flagName} to set/override a flag; GET /flags?tenantId=XYZ to list flags. Enforce per-tenant RBAC via Azure AD App Roles, and implement idempotent writes with an upsert and ETag checks. Outline tests and a cost-friendly deployment pipeline (GitHub Actions, Key Vault, App Configuration)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Function App with HTTP trigger; store flags in Cosmos DB with partitionKey=tenantId and id=flagName. POST uses upsert; return 200 with a version id; use ETag for optimistic concurrency. GE",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a tenant-scoped lifecycle: partition Delta Lake by tenant and timestamp; use VACUUM with a retention window plus a separate tombstone stream for deletions. Delete markers propagate to Purvie",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "HTTP-triggered Azure Function (Python) that accepts JSON with tenantId and payload, validates required fields using Pydantic, and writes the payload as a blob to a Storage Account container named after tenantId. Impleme…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Cosmos DB with tenantId as the partition key and multi-region writes enabled across West US and Europe for low-latency reads and resilience. API Management fronts the service and enforces per-tenant RBAC via Azure A…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical Azure dev basics: Functions, Cosmos, RBAC, concurrency, testing, deployment; beginner-friendly yet shows depth.\n\n## Key Concepts\n- Per-tenant isolation via partition key\n- Idempotent writes with upserts and ETag\n- RBAC via Azure AD App Roles\n- Deployment: GH Actions, Key Vault, App Configuration\n\n## Code Example\n```javascript\n// Placeholder example notes\n```\n\n## Follow-up Questions\n- How would you implement TTL cleanup and data retention per tenant?\n- How would you test RBAC enforcements in a CI environment?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-26T11:31:05.978Z",
    "version": 1
  }
]