[
  {
    "id": "test-azure-developer",
    "channelId": "azure-developer",
    "channelName": "Azure Developer",
    "title": "Azure Developer Knowledge Test",
    "description": "Test your understanding of Azure Developer with 25 questions.",
    "questions": [
      {
        "id": "azure-developer-q-0",
        "questionId": "q-17932",
        "question": "You're building a global, multi-tenant feature-flag service for a high-velocity product on Azure. Expose GET /flags?tenantId=XYZ&env=prod&userId=ABC that returns active flags for that user with sub-50ms latency at ~20k req/s. Describe architecture using Azure App Configuration for flag definitions, Azure Cache for Redis for per-tenant maps, and a stateless API (Functions/API Management). Include cache invalidation, tenant isolation, rollout experiments, rollback/versioning, and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a versioned schema in Azure Schema Registry, allow additive, nullable changes only; enforce via Delta Lake schema evolution on ADLS Gen2. Implement data quality gates in a dedicated Function tha",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingress via Event Hubs; orchestrate with Durable Functions per tenant; persist to Cosmos DB with tenantId as partition key. Ensure idempotency with a durable sequenceId and upsert, plus a dedup store.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "HTTP-triggered Azure Function (Python) that accepts JSON with tenantId and payload, validates required fields using Pydantic, and writes the payload as a blob to a Storage Account container named after tenantId. Impleme…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a low-latency read path: GET /flags?tenantId=XYZ&env=prod&userId=ABC returns active flags for that user within ~50ms at 20k rps. The architecture uses Azure App Configuration for centralized flag defin…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses real-world Azure multi-tenant flag system design, evaluating understanding of latency optimization, tenant isolation, and change management in production environments.\n\n## Key Concepts\n\n- Feature flags with targeting rules and environment support\n- Low-latency read path with Redis caching and appropriate TTLs\n- Strict per-tenant data isolation using key scoping (e.g., `flags:{tenantId}:{env}`)\n- Change propagation from App Configuration to Redis via webhook triggers or event-driven updates\n- Observability and auditing using Application Insights and OpenTelemetry\n- Rollback/versioning strategies and experimentation/traffic splitting techniques\n\n## Code Example\n\n```javascript\nasync function getFlags(tenantId, env, userId) {\n  const key = `flags:${tenantId}:${env}`;\n  const cached = await redis.hGet(key, userId);\n  if (cached) return JSON.parse(cached);\n  \n  const flags = await fetchFromAppConfig(tenantId, env);\n  const userFlags = evaluateTargeting(flags, userId);\n  await redis.hSet(key, userId, JSON.stringify(userFlags), { EX: 60 });\n  return userFlags;\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-1",
        "questionId": "q-18046",
        "question": "You're building a global real-time telemetry analytics pipeline on Azure for multi-tenant customers. Ingest 2M events/sec from edge devices via Azure Event Hubs, process with Spark Structured Streaming on Azure Databricks, store tenant-isolated data in Delta Lake on ADLS Gen2 with per-tenant partitions, and surface analytics in Power BI. How would you design for strict per-tenant isolation, dynamic schema evolution, end-to-end latency under 200 ms, canary schema migrations, robust testing, and cost governance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a tenant-aware API gateway using Azure API Management with a rate-limit-by-key policy keyed on tenant-id, backed by Redis token-bucket counters and Azure Front Door for global routing. Persi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Read tenantId from the path or X-Tenant-ID header, validate against an allowlist, then increment rate:{tenantId}:count in Redis with TTL 1s. If the counter exceeds 1, return 429 with Retry-After: 1; o",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition Delta Lake by tenantId; enforce per-tenant RBAC via Unity Catalog and per-tenant keys in Key Vault; support schema evolution with Delta Lake's mergeSchema; use canary deployments and feature",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Durable Functions orchestrator to model a per-tenant job lifecycle: enqueue metadata in Cosmos DB (tenantId as partition key), trigger Data Factory/Synapse pipelines, and write results back to C",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-time telemetry design, multi-tenant isolation, dynamic schema evolution, migration strategies, testing, and cost governance across Azure services.\n\n## Key Concepts\n\n- Tenant isolation in Delta Lake on ADLS Gen2\n- Schema evolution and backward compatibility\n- Canary migrations and feature flags\n- Latency optimization in Spark Structured Streaming\n- Cost governance and autoscaling\n\n## Code Example\n\n```python\n# PySpark: enable schema evolution during write\ndf.write.format(\"delta\").option(\"mergeSchema\",\"true\").save(\"/mnt/delta/tenant_id=ABC/\")\n```\n\n## Follow-up Questions\n\n- How would you validate the 200 ms latency target with real traffic?\n- What are your rollback and data-consistency strategies during a failed canary migration?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-2",
        "questionId": "q-17197",
        "question": "You're building a global, multi-tenant analytics API on Azure that serves ad-hoc aggregates (count, sum) while guaranteeing differential privacy. Design a per-tenant DP layer: privacy budget management, Laplace noise addition, query rewriting, and audit trails. Explain architecture, data flow, caching of DP results, and testing strategy, including edge cases like exhausted budgets or overly noisy requests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant DP gate in the API: store epsilon budgets in Cosmos DB; on each query, check remaining budget, run the base aggregate against Delta Lake, add Laplace noise to the result, persis",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use an HTTP-triggered Function; get tenantId from a header; fetch the vendor secret from Key Vault via DefaultAzureCredential; validate the request with an HMAC SHA-256 signature over the body using t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Regionally deployed Functions with Event Grid inputs; per-tenant state in Cosmos DB (partition key = tenantId); durable functions serialize updates per tenant; canary gating via a version field and fe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Generate a per-tenant SAS URL for direct upload to Blob Storage at tenantId/docs/{filename}, then persist metadata in Cosmos DB with partitionKey=tenantId. Use Event Grid to trigger a thumbnail/virus-",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess ability to integrate DP into a service-by-service Azure data flow, ensuring tenant isolation and auditable privacy.\n\n## Key Concepts\n\n- Differential Privacy, epsilon budgets\n- Per-tenant state in Cosmos DB\n- Query rewriting and noise addition\n- Observability of DP decisions\n\n## Code Example\n\n```javascript\n// Pseudo-code for adding Laplace noise after aggregation\n```\n\n## Follow-up Questions\n\n- How would you test budget exhaustion scenarios?\n- How would you handle complex joins?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-3",
        "questionId": "q-25584",
        "question": "You're a beginner Azure developer tasked with building a multi-tenant note API. Create two HTTP-triggered Functions: CreateNote and ListNotes. Use Cosmos DB SQL API with tenantId as the partition key for per-tenant isolation. Enforce 2KB max note size and 30-day TTL. Configure per-environment settings with Azure App Configuration, store secrets in Key Vault, and GitHub Actions deploys for dev/staging/prod. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition Event Hubs by tenant; use a Durable Functions orchestrator to chain enrichment and per-tenant aggregation; store results in Cosmos DB with tenantId as PartitionKey; implement idempotency through event deduplic…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Start with the data model and validation: define container partition key /tenantId, a Note with tenantId, noteId, content, createdAt. Enforce 2KB payload limit and 30-day TTL on the container. Impleme",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a cross-region pipeline with Event Hubs for ingestion, AKS for stateful processing, and Cosmos DB with tenantId as the partition key. Ensure exactly-once sinks via idempotent writes and dedupli",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest via Event Hubs and Functions, write raw data to Delta Lake on ADLS Gen2 partitioned by tenantId, apply masking in the streaming layer, and expose per-tenant aggregates in Synapse with Row-Level",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical Azure serverless workflow, multi-tenant data isolation, and per-env configuration. It stresses concrete data modeling, input validation, TTL, and repo pipelines instead of theory.\n\n## Key Concepts\n- Cosmos DB SQL API partitioning with /tenantId\n- Input validation and TTL\n- Azure App Configuration and Key Vault for per-env secrets\n- GitHub Actions multi-environment deployment\n- Managed identities for secure DB access\n\n## Code Example\n```javascript\n// Example mock for validation\nfunction validateNoteSize(input){return input.length <= 2048}\n```\n\n## Follow-up Questions\n- How would you test cross-tenant data isolation?\n- How would you handle retry logic and idempotency for CreateNote?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-4",
        "questionId": "q-27328",
        "question": "You're building a global, per-tenant feature-flag service for a marketplace on Azure. Implement a low-latency evaluation path with per-tenant toggles stored in Cosmos DB (tenantId as partition key), a regional Redis cache for hot flags, and a GraphQL gateway that serves responses within a 50ms budget. Describe data model, caching strategy, cache invalidation, consistency guarantees, failure modes, and a staged rollout plan across dev/stage/prod?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a global per-tenant feature-flag system: store flags in Cosmos DB with tenantId partitioning, cache hot flags regionally in Redis, and power a GraphQL gateway that reads Redis first then Cosmos",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a Durable Functions orchestrator to model a per-tenant job lifecycle: enqueue metadata in Cosmos DB (tenantId as partition key), trigger Data Factory/Synapse pipelines, and write results back to C",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Event Hubs ingestion with tenant-scoped consumer groups, a Durable Functions orchestrator for step sequencing, per-tenant Cosmos DB partitions, and Key Vault-based customer-managed keys. Emit immu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a multi-region, per-tenant pipeline: Event Hubs ingest; Spark writes Delta Lake partitions by tenantId on ADLS Gen2; enforce per-tenant isolation with RBAC and Delta sharing. Apply dynamic m",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes multi-region, low-latency feature-flag design with per-tenant isolation and real-time updates.\n\n## Key Concepts\n- Per-tenant data modeling in Cosmos DB with tenantId partition key\n- Regional caching with Redis and cache invalidation via Change Feed\n- GraphQL gateway design for low-latency reads\n- Progressive delivery and rollback strategies across environments\n- Observability and fault-tolerance\n\n## Code Example\n```javascript\nasync function getFlag(tenantId, key){\n  const cacheKey = `${tenantId}:${key}`;\n  let v = await redis.get(cacheKey);\n  if (v) return JSON.parse(v);\n  const doc = await cosmos.findFlag(tenantId, key);\n  await redis.set(cacheKey, JSON.stringify(doc), 'EX', 1);\n  return doc;\n}\n```\n\n## Follow-up Questions\n- How would you handle conflicting updates across regions?\n- How would you simulate regional outages and measure SLOs?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-5",
        "questionId": "q-25949",
        "question": "You're building a fintech reconciliation and analytics platform on Azure for multi-tenant customers. Ingest 50k events/sec from partner APIs via Azure Event Hubs, process in Azure Databricks into Delta Lake on ADLS Gen2, apply per-tenant masking and RBAC, and surface curated views with Synapse Serverless. Add a consent-driven data sharing channel for regulators/partners with revocation, plus end-to-end data lineage and auditability. Design architecture, data layout, governance, testing, and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Event Hubs -> Structured Streaming in Databricks to write to tenant-scoped Delta Lake partitions (tenant_id/date) on ADLS Gen2. Enforce per-tenant RBAC (Unity Catalog), apply masking via Spark SQL",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deploy a per-tenant Delta Lake layout on ADLS Gen2, partitioned by tenant_id, with Unity Catalog RBAC and dynamic masking. Ingest events via Azure Event Hubs into Azure Databricks, write to Delta tables, and materialize…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Partition Cosmos DB by tenantId; GPU AKS with per-tenant namespaces and quotas; route Event Hubs to tenant-scoped consumers; deduplicate with a canonical jobId stored in blob storage; orchestrate with",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design with tenant-scoped namespaces, tenantId partition keys, and separate storage partitions. Route API traffic via API Management with per-tenant RBAC. Cache hot features in Redis with per-tenant k",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nBank-grade data isolation and cross-tenant sharing at scale represent genuine enterprise pain points. This question evaluates your ability to design for architecture, governance, and production-readiness under realistic constraints.\n\n## Key Concepts\n- Delta Lake layout and partitioning strategies; Unity Catalog RBAC; dynamic data masking\n- Delta Sharing with consent management and access revocation\n- End-to-end lineage with Microsoft Purview; telemetry with OpenTelemetry\n- Testing: privacy compliance, failover scenarios, cost optimization\n\n## Architecture Overview\n- **Ingestion**: Azure Event Hubs handles 50k events/sec from partner APIs with auto-scaling and partitioning\n- **Processing**: Azure Databricks processes streams into Delta Lake on ADLS Gen2, applying per-tenant transformation logic\n- **Storage**: Delta Lake layout partitioned by tenant_id ensures logical isolation while optimizing query performance\n- **Governance**: Unity Catalog provides RBAC; dynamic masking enforces data visibility rules per tenant\n- **Serving**: Synapse Serverless delivers curated views to tenant-specific dashboards\n- **Sharing**: Delta Sharing enables consent-driven data access for regulators and partners with real-time revocation capability\n- **Lineage**: Microsoft Purview captures end-to-end data lineage and supports auditability requirements\n\n## Failure Modes\n- Event Hubs throughput exceeded: implement backpressure and scale partition count\n- Databricks job failures: configure auto-retry with dead letter queues for failed events\n- Delta Sharing revocation latency: design idempotent access checks at query time\n- Cross-tenant data leakage: enforce tenant isolation at storage layer with immutable audit logs",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-6",
        "questionId": "q-7441",
        "question": "You are building a beginner Azure Functions HTTP API for per-tenant image uploads. Each tenant uploads images to a tenant/{tenantId}/images/ blob path and receives a SAS URL for direct upload. Validate tenantId from a header, enforce 2 MB max file size, and return the SAS URL or an error. Include a minimal Node.js SAS snippet and a test for isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Event Hubs capture to ADLS Gen2; run Spark Structured Streaming on Databricks to write Delta Lake with per-tenant isolation; enforce RBAC via AAD groups and workspace-scoped access; encrypt a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Cosmos DB with per-tenant partitioning (tenantId) and multi-region writes, Event Hubs across regions feeding Durable Functions orchestrations, write to Cosmos DB with tenant-scoped RU/s, impleme",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an Azure Function App with HTTP PUT /flags/{tenantId} to upsert a JSON feature set, validated against a schema. Store in Cosmos DB using tenantId as partitionKey and a version field for idempo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an HTTP-triggered Azure Function that extracts the tenantId from the request header, constructs a tenant-scoped blob path (tenant/{tenantId}/images/), validates that the requested file size does not exceed 2 M…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical Azure Functions implementation skills, demonstrates understanding of multi-tenant architecture through proper data isolation, and showcases knowledge of secure upload patterns using SAS tokens without requiring complex backend services.\n\n## Key Concepts\n- Azure Functions HTTP triggers and request processing\n- Tenant-based data isolation through blob path scoping\n- Shared Access Signature (SAS) tokens for secure client-side uploads\n- File size validation and comprehensive error handling\n- Proper authentication and authorization patterns\n\n## Code Implementation\n\nThe solution requires:\n1. HTTP trigger function with tenant validation\n2. Blob storage client for SAS token generation\n3. Request validation for file size limits\n4. Error handling for unauthorized or invalid requests\n5. Tenant-specific path construction for data isolation\n\nThis approach ensures secure, scalable image uploads while maintaining strict tenant separation and resource limits.",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-7",
        "questionId": "q-24202",
        "question": "You're a beginner Azure developer building a small multi-tenant REST API with an HTTP-triggered Azure Function and Cosmos DB using tenantId as the partition key. Add per-tenant feature flags via Azure App Configuration and allow tenant overrides. Plan a 1-slot canary deployment (dev→stage) with GitHub Actions, smoke tests, and simple feature toggling. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement HTTP-triggered upload to tenant-specific blob containers, with tenantId validated from headers. Add a Timer Trigger cleanup that deletes blobs older than TTL from App Configuration per tenan",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-tier architecture: fast in-region evaluation with central governance. Store versioned flags in Cosmos DB with multi-region replication; cache per-region in Redis Enterprise; evaluate via deterministic hash-based rou…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe an end-to-end per-tenant data path: ingest via Event Hubs, tenant-scoped routing using message headers, per-tenant durable functions orchestration, and ADLS Gen2 folders /tenants/{tenantId}/.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would start by implementing Azure App Configuration to expose per-tenant feature flags, then integrate it with the function runtime to load tenant-specific overrides using the tenantId context. This establishes the fo…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses practical grounding in per-tenant configuration management, feature flags, and safe deployment strategies for Azure developers.\n\n## Key Concepts\n\n- Azure App Configuration feature flags with tenant-specific overrides\n- Cosmos DB partitioning by tenantId for logical tenant isolation\n- Canary deployments using GitHub Actions with environment promotion gates\n- Secrets governance via Azure Key Vault integration\n\n## Code Example\n\n```javascript\n// Minimal example: fetch a tenant flag from App Configuration\nconst { AzureAppConfigurationClient } = require('@azure/app-configuration');\n\nconst connectionString = process.env.APPCONFIG_CONNECTION_STRING;\nconst client = new AzureAppConfigurationClient(connectionString);\n\nasync function getTenantFlag(tenantId, flagKey) {\n  const tenantOverrideKey = `featureFlags/${tenantId}/${flagKey}`;\n  const defaultKey = `featureFlags/default/${flagKey}`;\n  \n  try {\n    const tenantSetting = await client.getSetting(tenantOverrideKey);\n    return tenantSetting.value === 'true';\n  } catch {\n    const defaultSetting = await client.getSetting(defaultKey);\n    return defaultSetting.value === 'true';\n  }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-8",
        "questionId": "q-2949",
        "question": "Design a beginner Azure data pipeline: land per-tenant CSVs in Azure Blob via Data Factory, create per-tenant Delta tables under Unity Catalog for isolation, and implement MERGE-based upserts in PySpark (using event_id as the key). Include simple validation, audit logging, and retry handling; outline data model, steps, and provide a minimal PySpark MERGE snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Cosmos DB container partitioned by tenantId; store a flags map per tenant with a version (ETag) for optimistic concurrency. Implement GET /flags?tenantId=XYZ and POST/PUT /flags to upsert with i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Land tenant CSVs into Blob via ADF, create per-tenant Delta tables under Unity Catalog, and perform MERGE upserts on event_id in PySpark. Add schema validation, lightweight audit logs, and exponential",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a two-tier Redis cache: store global tenant flags in a shared cache and per-tenant overrides in tenant:{id}:flags. Evaluate by checking the tenant override first (cache miss → Redis tenant key → fallback to global),…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use multi-region Cosmos DB with tenantId partition key and multi-region writes for DR; ingest 60-100k events/s via Event Hubs with geo-DR to a DR region; real-time per-tenant aggregates computed by Du",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical Azure data engineering basics for multi-tenant isolation, Delta Lake usage, and data pipeline reliability.\n\n## Key Concepts\n- Azure Data Factory orchestration\n- Delta Lake MERGE upserts\n- Unity Catalog per-tenant isolation\n- PySpark dataframes and schema validation\n\n## Code Example\n```python\nfrom delta.tables import DeltaTable\n\n# assume df is the new batch with columns: tenant_id, event_id, ...\npath = f\"dbfs:/delta/{tenant_id}/events\"\ndelta_table = DeltaTable.forPath(spark, path)\ndelta_table.alias('t').merge(\n    source=df.alias('s'),\n    condition=\"t.event_id = s.event_id\"\n).whenMatchedUpdateAll().whenNotMatchedInsertAll().execute()\n```\n\n## Follow-up Questions\n- How would you test idempotency across retries?\n- How would you scale to many tenants and manage catalog permissions?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-9",
        "questionId": "q-3999",
        "question": "You're operating a global multi-tenant event pipeline on Azure: Event Hubs ingest, Azure Functions processing, and Cosmos DB storage with per-tenant isolation. How would you implement end-to-end observability using OpenTelemetry and Azure Monitor to achieve tenant-aware tracing across components, with correlation IDs, dynamic sampling, and no cross-tenant leakage? Describe instrumentation points, data retention, and costs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instrument Event Hubs, Functions, and Cosmos DB with OpenTelemetry. Propagate tenantId and traceId across boundaries, push to Azure Monitor via the OpenTelemetry collector, and store traces in a per-t",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Check Redis first with key `${tenantId}:${productId}`; on miss fetch from Table Storage (PartitionKey=tenantId, RowKey=productId); if found, return and cache the item in Redis with TTL (e.g., 300s). U",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use tenantId as Cosmos partition key with per-residency multi-region writes (EU/US). Route via regional Event Hubs and a Durable Functions orchestrator that computes an idempotency key from eventId, t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use multi-region Cosmos DB with tenantId partition key and multi-region writes for DR; ingest 60-100k events/s via Event Hubs with geo-DR to a DR region; real-time per-tenant aggregates computed by Du",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical knowledge of end-to-end observability in Azure for multi-tenant data pipelines, including cross-service tracing, privacy, and cost considerations.\n\n## Key Concepts\n- OpenTelemetry integration with Azure Functions and Event Hubs\n- Trace context propagation (tenantId, traceId)\n- Azure Monitor / Log Analytics backend for traces\n- Per-tenant isolation in logs and data paths\n- Dynamic sampling and cost-aware tracing, privacy controls\n\n## Code Example\n```javascript\n// Node.js (simplified sketch)\nconst { trace, context, propagation } = require('@opentelemetry/api');\nconst { registerInstrumentations } = require('@opentelemetry/instrumentation');\nconst { HttpInstrumentation } = require('@opentelemetry/instrumentation-http');\n\nregisterInstrumentations({ instrumentations: [new HttpInstrumentation()], tracerProvider: /*...*/ });\n\nmodule.exports = async function (context, req) {\n  const tenantId = req.headers['x-tenant-id'];\n  const span = trace.getTracer('azure-trace').startSpan('function.process', {\n    attributes: { tenantId }\n  });\n  return await context.bindings?.invoke(req, { parent: span.context() });\n};\n```\n\n## Follow-up Questions\n- How would you validate dynamic sampling across tenants with different load patterns?\n- How would you detect and remediate telemetry leaks that cross tenant boundaries?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-10",
        "questionId": "q-4104",
        "question": "You're building a beginner Azure per-tenant image-processing pipeline. A user uploads an image to a tenant-scoped blob container; an Event Grid BlobCreated event triggers an Azure Function to generate a 300x300 thumbnail, store it in a per-tenant thumbnails container, and log an audit entry in Azure Table Storage. Enforce per-tenant isolation, idempotent retries, and dead-lettering via a Storage Queue. Outline the end-to-end flow and provide a minimal Node.js blob-trigger function using sharp to resize and save the thumbnail?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Node.js Azure Function with HTTP trigger to POST/GET notes by userId, storing in Cosmos DB (SQL API) with userId as the partition key. DevOps pipeline: npm install; npm test (jest); archiv",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design with tenantId as the partition key across ingestion, processing, and Delta Lake storage. Use Structured Streaming with watermarking and backpressure to target sub-200ms end-to-end latency; isol",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a lakehouse pipeline: IoT Hub → Event Hubs → Databricks Structured Streaming → Delta Lake on ADLS Gen2, partitioned by tenantId. Enforce per-tenant isolation through Delta table RLS/RBAC, secured with AAD, Manage…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Event Grid BlobCreated to trigger an Azure Function; derive tenantId from the blob path; resize to 300x300 with sharp; write thumbnail to tenants/{tenantId}/thumbnails; insert audit row with PartitionKey for tenant…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests event-driven processing basics on Azure, per-tenant isolation, idempotency, and dead-lettering in a tangible, beginner-friendly scenario.\n\n## Key Concepts\n\n- Event Grid and Blob storage integration\n- Per-tenant isolation via path-based or partitioned data\n- Idempotent processing and deduplication\n- Dead-letter queues and robust retry policies\n- Lightweight auditing via Table Storage or Cosmos\n\n## Code Example\n\n```javascript\nmodule.exports = async function(context, blob) {\n  const name = context.bindingData.name; // e.g. tenants/{tenant}/source/{file}\n  const parts = name.split('/');\n  const tenantId = parts[1];\n  const fileName = parts[3];\n  \n  // Idempotency check\n  const thumbnailPath = `tenants/${tenantId}/thumbnails/${fileName}`;\n  \n  try {\n    // Resize with sharp\n    const sharp = require('sharp');\n    const thumbnail = await sharp(blob).resize(300, 300).toBuffer();\n    \n    // Save thumbnail\n    context.bindings.outputBlob = thumbnail;\n    \n    // Audit entry\n    context.bindings.auditTable = {\n      PartitionKey: tenantId,\n      RowKey: `${Date.now()}-${fileName}`,\n      Timestamp: new Date().toISOString(),\n      Status: 'processed'\n    };\n    \n  } catch (error) {\n    // Dead-letter on failure\n    context.bindings.deadLetterQueue = {\n      tenantId,\n      fileName,\n      error: error.message,\n      timestamp: new Date().toISOString()\n    };\n    throw error;\n  }\n};\n```\n\n## End-to-End Flow\n\n1. User uploads image to `tenants/{tenantId}/source/{filename}`\n2. Event Grid emits BlobCreated event\n3. Azure Function triggers, extracts tenantId from path\n4. Function resizes image to 300x300 using sharp\n5. Thumbnail saved to `tenants/{tenantId}/thumbnails/{filename}`\n6. Audit entry logged with tenantId as PartitionKey\n7. On failure, message sent to dead-letter queue for retry",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-11",
        "questionId": "q-22987",
        "question": "You're a beginner Azure developer tasked with exposing a per-tenant REST API by fronting an Azure Functions HTTP API with Azure API Management. Each tenant's isolation is achieved via a tenantId header; implement per-tenant quotas, automatic retries, and abuse protection. Describe the end-to-end setup: authentication via Azure AD, APIM policies for per-tenant rate limits, per-env settings with App Configuration, secrets in Key Vault, and a GitHub Actions workflow for dev/stage/prod with telemetry to Application Insights. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design with APIM policies enforcing per-tenant quotas, route to multi-region AKS hosting the model service, and store per-tenant state in Cosmos DB using tenantId as the partition key with geo-replica",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant DP by applying Gaussian noise at the aggregation step, parameterized by a tenant-specific epsilon stored in Azure App Configuration. Track remaining budget in a Redis cache and De",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Start with APIM routing to the HTTP Function, enforce per-tenant quotas using rate-limit-by-key keyed by tenantId header, and enable retry logic. Add Azure AD authentication, store secrets in Key Vaul",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "I would implement a Durable Functions orchestration triggered by an API gateway. Use per-tenant Cosmos DB with exportId for idempotence, redact rules, export data to ADLS Gen2 in CSV/Parquet; generate",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical understanding of API management, per-tenant isolation, and end-to-end DevOps in Azure. It checks how a beginner reason about secure, scalable exposure of a Function API behind APIM with centralized config and secrets.\n\n## Key Concepts\n\n- Azure API Management policies for per-tenant quotas\n- tenantId header as a multitenancy isolation key\n- OAuth2 authentication via Azure AD\n- Azure Key Vault for secrets\n- Azure App Configuration for per-environment settings\n- GitHub Actions multi-environment deployment\n- Application Insights telemetry and tracing\n\n## Code Example\n\n```xml\n<policies>\n  <inbound>\n    <base />\n    <set-header name=\"x-tenantId\" exists-action=\"override\">\n      <value>@(context.Request.Headers.GetValueOrDefault(\"tenantId\"))</value>\n    </set-header>\n    <rate-limit-by-key calls=\"100\" renewal-period=\"60\" key=\"@(context.Request.Headers.GetValueOrDefault(\"tenantId\",\"anonymous\"))\" />\n  </inbound>\n  <backend>\n    <base />\n  </backend>\n  <outbound>\n    <base />\n  </outbound>\n  <on-error>\n    <base />\n  </on-error>\n</policies>\n```\n\n## Follow-up Questions\n\n- How would you test per-tenant quotas across environments?\n- How would you handle tenant key rotation and credential revocation?\n- What trade-offs exist between header-based tenantId and JWT-scoped tenants?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-12",
        "questionId": "q-2307",
        "question": "You're building a privacy-preserving analytics marketplace on Azure: ingest telemetry via Event Hubs; anonymize with differential privacy during ingestion or enrichment; store tenant-scoped data in Delta Lake on ADLS Gen2 with strict per-tenant partitioning; catalog lineage in Azure Purview; and share results through per-tenant REST APIs with RBAC and data-sharing controls. Outline the architecture, data flow, and trade-offs to meet privacy, latency, and cost targets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement ingestion via Event Hubs, perform DP during or after enrichment, store tenant-scoped data in Delta Lake on ADLS Gen2 with partitions per tenant, catalog lineage in Purview, and expose per-te",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect a per-tenant real-time risk pipeline: ingest 100k events/sec from Event Hubs; run Databricks Structured Streaming to compute risk scores per tenant; write raw and enriched data to Delta Lake",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Databricks Structured Streaming consuming from Event Hubs, and upsert into Delta Lake with tenantId and id as the key. Mask PII during streaming via a UDF. Handle Salesforce deletions with tombsto",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Event Hubs in a regional namespace to ingest; route to Durable Functions that implement idempotent writes using a composite key (tenantId, messageId). Cosmos DB with partitionKey=tenantId stores a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design privacy-aware analytics with governance, cross-service data flow, and cost controls at scale.\n\n## Key Concepts\n- Differential privacy and when to apply it\n- Delta Lake partitioning and tenant isolation\n- Azure Purview for catalog and lineage\n- RBAC, API Management, and per-tenant data sharing\n- Consent management and auditing\n\n## Code Example\n```javascript\n// Pseudocode: simple DP-style anonymization before write\nfunction applyDP(record, epsilon=1.0){\n  const noise = laplace(0, 1/epsilon);\n  record.value += noise;\n  return record;\n}\n```\n\n## Follow-up Questions\n- How would you validate DP parameters to meet regulatory requirements?\n- How would you implement dynamic masking and tenant-aware data sharing budgets?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-13",
        "questionId": "q-18762",
        "question": "You're building a global multi-tenant data ingestion and analytics pipeline on Azure. Ingest streaming events from client apps via Azure Event Hubs; process with Azure Functions and Durable Functions orchestrations; store per-tenant results in Cosmos DB with strict isolation; publish aggregates to Synapse for BI. How would you ensure sub-200ms end-to-end latency for critical events, enforce per-tenant isolation and quotas, support idempotent retries, and provide end-to-end auditing and rollback paths?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a Durable Functions orchestrator to fan-out per-tenant processing. Route Event Hub events to Functions using a per-tenant partition key and keep workers warm for sub-200ms latency. Use Cosmos",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a cache-first flow in Azure Functions (Node.js). Read tenantId from header X-Tenant-ID or route parameter. Check Redis key pricing:{tenantId}:{productId} with 60s TTL. On miss, fetch basePrice from Cosmos DB (Partit…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Assign each tenant to a dedicated Synapse Spark pool with enforced CPU and memory quotas to ensure strict compute isolation and prevent noisy neighbor problems. Route every query through Azure API Management configured…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Generate a time-limited SAS URL per user, then a Blob-triggered Function validates mime type and size and writes metadata to Azure Table Storage with partitionKey=userId, rowKey=uploadId. Use Managed",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDemonstrates a realistic multi-tenant data path with strict latency, isolation, and reliability requirements on Azure.\n\n## Key Concepts\n- Durable Functions orchestration for per-tenant parallel processing\n- Event Hubs partitioning and low-latency Function scaling\n- Cosmos DB per-tenant isolation and idempotent retry patterns\n- Auditing and rollback strategies with telemetry and compensating actions\n\n## Code Example\n```javascript\n// Pseudo-outline of a Durable Orchestrator\nconst orchestrator = ctx => {\n  const events = ctx.props.events;\n  const tasks = events.map(e => ctx.df.callActivity('ProcessTenantEvent', e));\n  return ctx.df.Task.all(tasks);\n}\n```\n\n## Follow-up Questions\n- How would you test end-to-end latency at the 95th percentile?\n- What failure modes require manual failover and how would you implement it?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-14",
        "questionId": "q-18986",
        "question": "You're building a beginner Azure Functions API to manage per-tenant feature flags for a SaaS app. Implement endpoints: POST /flags to upsert a flag with composite key (tenantId, featureName) and GET /flags?tenantId=XYZ to read all flags for a tenant. Store in Cosmos DB (SQL API) with tenantId as partition key. Use idempotent upsert, and a small in-memory cache with TTL plus optional Azure Cache for Redis to reduce reads. Describe data model, code structure, testing, and how you'd ensure isolation and cost awareness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a composite item in Cosmos DB: partitionKey=tenantId; id=`${tenantId}|${featureName}`. POST upserts the flag; GET reads tenantId. Implement a small in-memory cache with TTL and optionally populate",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a durable HTTP-triggered Azure Function with a Cosmos DB SQL API container partitioned by tenantId; POST upserts (tenantId, key) to store value; GET by tenantId returns all keys. Protect with Azur",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an HTTP-triggered Azure Function that reads X-Tenant-Id, queries Azure App Configuration for a feature flag key with an environment label, and returns a minimal JSON of enabled features. Cache res",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement OpenTelemetry across Event Hubs, Durable Functions, Cosmos DB, and ADX; propagate traceparent/tracestate and tenant_id in message properties; route traces to Application Insights and data ex",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nGauges comfort with Azure Functions, Cosmos DB data modeling, and cache strategies in multi-tenant setups, plus practical testing thoughts.\n\n## Key Concepts\n- Azure Functions HTTP bindings\n- Cosmos DB SQL API with partitionKey\n- Idempotent upserts\n- In-memory caching with TTL\n- Azure Cache for Redis as shared cache\n- Observability and basic testing\n\n## Code Example\n```javascript\n// Upsert handler sketch\nasync function upsertFlag(ctx) {\n  const { tenantId, featureName, value } = ctx.req.body;\n  const item = { id: `${tenantId}|${featureName}`, tenantId, featureName, value, ttl: 3600 };\n  await cosmosContainer.items.upsert(item);\n  ctx.res = { status: 200, body: item };\n}\n```\n\n## Follow-up Questions\n- How would you test race conditions with concurrent POSTS?\n- How would you observe cache misses/hits and TTL expiry in production?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-15",
        "questionId": "q-23778",
        "question": "You're building a real-time fraud-detection service for multiple tenants on Azure. Ingest events from Azure Event Hubs, run a Durable Function orchestrator that calls an external scoring API, and persist per-tenant results in Cosmos DB using tenantId as the partition key. Implement envelope encryption: encrypt PII with tenant-specific keys stored in Azure Key Vault, rotate keys quarterly, and decrypt only within the function chain. Outline architecture, telemetry, and failover tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Assign each tenant a residency tag and map it to a primary region. Route all write operations to a region-scoped ADLS Gen2 data lake in the tenant's designated region. Enable geo-redundant replication to a single DR reg…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a blob-triggered Python Azure Function that processes uploaded images by resizing them to 128x128 thumbnails using Pillow, saves results to tenant-specific storage paths, and updates Cosmos DB with processing sta…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "AKS: one namespace per tenant with NetworkPolicy; Azure ML handles model registry and canary deployments; a shared inference runtime enforces per-tenant auth and uses a tenant-scoped feature store; st",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a dataflow where Event Hubs triggers a Durable Function orchestrator that calls the scoring API and writes to Cosmos DB with tenantId as the partition key. Encrypt PII with tenant-specific keys",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes practical multi-tenant security with per-tenant keys, real-time processing, and cross-service telemetry.\n\n## Key Concepts\n- Durable Functions orchestration; Event Hubs; Cosmos DB partitioning; envelope encryption; Key Vault key rotation; OpenTelemetry traces across services.\n\n## Code Example\n```javascript\n// pseudo-implementation sketch showing key fetch, encryption, and store flow\n```\n\n## Follow-up Questions\n- How would you rotate keys with minimal downtime? \n- How would you test encryption policy across tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-16",
        "questionId": "q-1594",
        "question": "You're building a real-time, multi-tenant feature-flag platform on Azure to serve traffic across three regions with sub-50ms evaluation latency. Each tenant has per-flag rules, canary/A/B experiments, and strict audit requirements. Outline end-to-end design: data model for flags and experiments, evaluation path, storage choices (Cosmos DB vs SQL), caching, event sourcing, cross-region synchronization, security (Managed Identities, Key Vault, RBAC), rollback strategy, and failure modes. Include how you'd test canary safety and ensure tenant isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant Delta Lake layers (bronze/silver/gold) partitioned by tenantId, with separate namespaces to enforce isolation. Mask PHI at query time via dynamic data masking policies and Spark views.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-tier architecture: fast in-region evaluation with central governance. Store versioned flags in Cosmos DB with multi-region replication; cache per-region in Redis Enterprise; evaluate via deterministic hash-based rou…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement an HTTP-triggered Azure Function that extracts the tenantId from the request header, constructs a tenant-scoped blob path (tenant/{tenantId}/images/), validates that the requested file size does not exceed 2 M…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design two parallel lanes: a REST API lane for operations and a telemetry ingest lane. **Architecture**: The REST API handles CRUD operations with Cosmos DB partitioned by `tenantId`. The streaming path consumes events…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design low-latency, multi-tenant feature-flag systems on Azure, integrating multiple services and making informed trade-offs.\n\n## Key Concepts\n- Low-latency, region-local evaluation path\n- Multi-region replication and canary experiments\n- Versioned data model for flags/experiments\n- Secure access via Managed Identities and Key Vault\n- Auditability with Event Hubs to ADLS Gen2\n\n## Code Example\n```javascript\nfunction selectFlag(userId, tenantId, flagVersion, canary) { \n  const key = `${tenantId}:${userId}:${flagVersion}`;\n  const bucket = hash(key) % 100;\n```",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-17",
        "questionId": "q-29170",
        "question": "You're a beginner Azure developer tasked with building a small multi-tenant SaaS API: an HTTP-triggered Azure Function reads tenantId from a header, stores per-tenant metadata in Cosmos DB with tenantId as the partition key, and exposes /config to fetch feature flags from Azure App Configuration. Implement per-tenant access checks, a simple in-memory TTL cache for access tokens (5 minutes), and a minimal dev/stage/prod CI/CD pipeline. What would you implement first and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Idempotent per-event processing with messageId dedup; Durable Functions orchestrator per tenant batches and routes to workers; Cosmos DB upsert on tenantId with ETag concurrency; propagate OpenTelemet",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a cache-aside pattern using Azure Cache for Redis with tenant-scoped keys (tenantId:flagName). Authoritative flag configurations reside in Azure App Configuration with per-tenant labels and dynamic rules. On r…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would first implement a minimal tenant document in Cosmos DB with tenantId as the partition key, paired with an HTTP-triggered Azure Function that validates the tenant ID header and serves the /config endpoint. A Mana…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement idempotent uploads by using a deterministic blob name (tenantId + uploadId). Validate CSV header and each row server-side. Store raw CSV in a tenant-specific Blob container, and write metada",
            "isCorrect": false
          }
        ],
        "explanation": "## What This Question Assesses\n\nThis question evaluates end-to-end understanding of multi-tenant isolation, Azure App Configuration integration, and lightweight caching within an Azure Function. It also tests practical CI/CD knowledge across dev/stage/prod environments and the ability to validate per-tenant behavior.\n\n## Key Concepts\n\n- Cosmos DB with tenantId as the partitionKey for tenant isolation\n- Azure App Configuration for per-tenant feature flags\n- Managed Identity for secure access to App Configuration\n- In-memory TTL cache per tenant (300 seconds) for flag data\n- Header-based tenant identification for request routing",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-18",
        "questionId": "q-9933",
        "question": "You're building a cross-tenant real-time analytics platform on Azure for multiple clients (finance, health, retail). Data lands via Azure Event Hubs, raw is stored in ADLS Gen2; Synapse Spark processes to Delta Lake and serves curated views via serverless SQL. How would you enforce strict per-tenant data isolation across storage, compute, and query layers, manage schema evolution and drift, implement tenant-aware lineage with Azure Purview, and enable cost controls? Include testing and rollback strategies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition by tenantId in Delta Lake with per-tenant folders in ADLS Gen2; enforce RBAC at storage, Synapse, and Purview; enable Delta schema evolution and drift alerts; implement tenant-aware lineage",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Enable Cosmos DB multi-region writes (multi-master) with tenantId as the partition key and map tenants to regions to enforce residency. Instrument Functions and Cosmos calls with OpenTelemetry, propag",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a cache-first flow in Azure Functions (Node.js). Read tenantId from header X-Tenant-ID or route parameter. Check Redis key pricing:{tenantId}:{productId} with 60s TTL. On miss, fetch basePrice from Cosmos DB (Partit…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Start with enabling Cosmos DB multi-region writes for per-tenant residency using tenantId as the partition key. Instrument all calls with OpenTelemetry and export traces to Application Insights, ensur",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes real-world governance, isolation, and cost controls in Azure data pipelines across multiple tenants, challenging candidates to design end-to-end security, lineage, and drift handling.\n\n## Key Concepts\n- Per-tenant isolation: storage, compute, query\n- Delta Lake schema evolution and drift\n- Purview lineage and RBAC across ADLS, Synapse\n- Cost governance and autoscaling\n\n## Code Example\n```python\n# PySpark example: enforce per-tenant isolation when writing\nfrom pyspark.sql.functions import col\ntenant_df = df.filter(col('tenantId') == tenant_id)\ntenant_df.write.format('delta').mode('append').partitionBy('tenantId').save('/data/tenants/'+tenant_id)\n```\n\n## Follow-up Questions\n- How would you test schema drift detection and rollback in this setup?\n- What metrics and alerts would you configure for cross-tenant cost overruns?",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-19",
        "questionId": "q-5575",
        "question": "You're building an Azure-based multi-tenant feature-flag API. Implement GET /feature/{tenantId}/{featureName} returning enabled/disabled with: tenant flags in Cosmos DB, global defaults in App Configuration, 30s Redis cache, regional rollout override, deterministic A/B bucketing via X-Experiment, and per-tenant audit logs to ADLS Gen2. Include error handling and tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Serverless API design: use Azure Functions with a Redis cache (feat:{tenant}:{feature}) for 30s. On miss, fetch tenant flag from Cosmos (PartitionKey=tenant), fallback to App Configuration defaults, t",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Partition Delta Lake by tenant_id and date in ADLS Gen2; implement Unity Catalog RBAC for strict tenant isolation and utilize per-tenant encryption keys in Azure Key Vault with envelope encryption; employ idempotent pro…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a Policy Engine in API Management backed by per-tenant configs in App Configuration cached for 60s with Key Vault-stored secrets. Enforce rate limits in APIM policies, verify OAuth2",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an event-driven path: Event Hubs -> Durable Functions (per-tenant fan-out) reads profile from Redis, computes per-event features, and writes incremental aggregates to Azure Data Explorer with tena",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess cross-service integration, tenant isolation, caching strategy, deterministic experimentation, compliance/audit path, and robust failure handling in a scalable azure stack.\n\n## Key Concepts\n\n- Azure Functions HTTP trigger\n- Cosmos DB per-tenant pattern\n- App Configuration for defaults\n- Redis caching with TTL\n- Regional rollout overrides\n- Deterministic A/B bucketing via seed RNG\n- Audit logging to ADLS Gen2\n- Observability and testing\n\n## Code Example\n\n```typescript\nimport { AzureFunction, Context, HttpRequest } from \"@azure/functions\";\n\nconst httpTrigger: AzureFunction = async function (context: Context, req: HttpRequest): Promise<void> {\n  // Skeleton: parse tenantId/featureName, cache, fetch, compute AB bucket, audit, respond\n  context.res = { status: 200, body: { enabled: true } };\n};\nexport default httpTrigger;\n```\n\n## Follow-up Questions\n\n- How would you implement deterministic AB bucketing across tenants with changing rollout percentages?\n- How would you test failure scenarios and ensure audit completeness?",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-20",
        "questionId": "q-20035",
        "question": "You’re designing a global, multi-tenant IoT analytics pipeline on Azure. Edge devices push telemetry to IoT Hub; cloud processing uses Databricks Spark to write Delta Lake on ADLS Gen2, with per-tenant isolation and 90-day TTL. Describe data layout, tenancy boundary enforcement, schema evolution, and test strategy for zero-downtime migrations, plus failure modes and rollback plans?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a cross-tenant sharing layer on top of a Delta Lake on ADLS Gen2. Keep data isolated per-tenant with Unity Catalog RBAC and masking; expose shared subsets via Synapse views or external table",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement an Azure Function using JavaScript/TypeScript with a GET route that accepts the path parameters `tenantId`, `container`, and `blobName`. First, I'd extract the tenantId from either the `X-Tenant-ID` he…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a per-tenant data lakehouse with Delta Lake on ADLS Gen2, tenant-scoped access, and Purview lineage. Enforce isolation via separate databases/PartitionKeys, row-level security, and dynamic d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant partitioning strategy in Delta Lake using composite keys (tenant_id, device_id, timestamp), and enforce tenancy boundaries at both read and write paths using Spark SQL Row-Level Security combined wit…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design scalable, compliant multi-tenant IoT pipelines on Azure, effectively balancing isolation requirements, data lifecycle management, and resilience against failures.\n\n## Key Concepts\n- **Core Services**: IoT Hub, Databricks Spark, Delta Lake, ADLS Gen2\n- **Security**: Tenant isolation, Row-Level Security (RLS), AAD-managed identities\n- **Data Lifecycle**: Schema evolution, canary migrations, TTL handling\n- **Resilience**: Idempotency, retries, end-to-end testing\n\n## Code Example\n```python\n# Enable Delta schema evolution\nspark.conf.set(\"spark.databricks.delta.schema.autoMerge.enabled\", \"true\")\n\n# Write with schema evolution\ndf.write.format(\"delta\").mode(\"append\").option(\"mergeSchema\", \"true\").save(\"/mnt/delta/iot\")\n\n# Implement TTL with vacuum\nfrom delta.tables import DeltaTable\ndt = DeltaTable.forPath(spark, \"/mnt/delta/iot\")\ndt.vacuum(90)  # Retain only last 90 days\n\n# Row-Level Security example\nspark.sql(\"CREATE POLICY tenant_isolation_policy ON TABLE iot_telemetry SELECT * WHERE tenant_id = current_user()\")\n```",
        "difficulty": "advanced"
      },
      {
        "id": "azure-developer-q-21",
        "questionId": "q-15230",
        "question": "You're building a beginner Azure-based notification service for a multi-tenant SaaS app. Messages arrive via Azure Event Grid and must be delivered as emails per tenant. Design a two-function flow: an Event Grid-triggered processor enqueues to a per-tenant Storage Queue, and a sender function delivers via a mock SMTP. Enforce per-tenant isolation, idempotency via eventId, exponential backoff retries, and a DLQ for failed messages. Outline components and failure handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Post /profiles upserts a profile with tenantId as the partition key; GET /profiles?tenantId=XYZ lists profiles. Use a Cosmos DB container with partition key /tenantId. Implement idempotent upsert and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition data by tenant in Delta Lake on ADLS Gen2, secured by Unity Catalog and per-tenant RBAC; ingest through Event Hubs with tenant-based partitions; Databricks notebooks enforce per-tenant acces",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a two-function flow: an Event Grid-triggered processor enqueues to a per-tenant Storage Queue, and a sender function delivers via a mock SMTP with exponential backoff. Idempotency is ensured by",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a multi-tenant API using Functions (or Container Apps) fronted by Front Door, with tenantId as the Cosmos DB partition key for isolation. Ingest events via HTTP and Event Hubs, validate with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of simple, real-world event-driven flows with per-tenant isolation and reliability basics.\n\n## Key Concepts\n- Event Grid triggers\n- Storage Queue per tenant\n- Idempotency with eventId\n- Exponential backoff retries\n- Dead-letter queue\n- Basic observability (App Insights)\n\n## Code Example\n```javascript\n// Example of idempotent handler scaffold\nmodule.exports = async function(context, eventGridEvent) {\n  const id = eventGridEvent.id;\n  // check per-tenant dedup store\n  const seen = await dedupStore.contains(`dedup:${id}`);\n  if (seen) return;\n  await dedupStore.add(`dedup:${id}`);\n  await enqueueOutbound(eventGridEvent);\n};\n```\n\n## Follow-up Questions\n- How would you test the DLQ path and failure modes?\n- How would you simulate backoff and measure end-to-end latency?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-22",
        "questionId": "q-14975",
        "question": "You're building an Azure-native, multi-tenant feature-flag service for a SaaS product. Flags are per-tenant and must be read with sub-millisecond latency while maintaining strict isolation and auditable changes. Describe your end-to-end design, including storage, caching, invalidation, security, testing, and rollback plans. Provide concrete component choices and data flow?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a high-throughput, multi-tenant routing path where per-tenant feature flags govern routing/schema. Ingest from Event Hubs, process in Azure Functions; fetch flags from App Configuration and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "An Azure-native, multi-tenant feature-flag service: per-tenant flags live in Azure App Configuration with per-tenant configuration sections; reads are cached in Azure Redis Cache with a short TTL (e.g., 30 seconds) and…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a lakehouse pipeline: IoT Hub → Event Hubs → Databricks Structured Streaming → Delta Lake on ADLS Gen2, partitioned by tenantId. Enforce per-tenant isolation through Delta table RLS/RBAC, secured with AAD, Manage…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a TimerTrigger Azure Function (every 15 minutes) to fetch 15‑minute metrics, compute a window key like 20260114-0915, and write a single blob named agg-20260114-0915.json. Before writing, check fo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses the ability to design a low-latency, highly available, and auditable per-tenant feature-flag system using Azure-native services. It tests understanding of cache invalidation strategies, security patterns, and multi-tenant isolation in a distributed system.\n\n## Key Concepts\n\n- **Azure App Configuration** serves as the source of truth, with per-tenant configuration sections or separate stores for tenant isolation\n- **Azure Redis Cache** provides sub-millisecond reads with TTL-based expiration and stale-while-revalidate patterns for high availability\n- **Azure Functions** with Managed Identity (MSI) and Azure AD authentication eliminate secrets management and enable secure, tenant-aware access\n- **Event-driven cache invalidation** via Event Grid or Service Bus pub/sub ensures consistency across cache nodes\n- **Cosmos DB** stores immutable audit logs with tenant-scoped partition keys for compliance and traceability\n- **RBAC and Private Endpoints** enforce strict data-plane isolation and network security per tenant\n\n## Code Example\n\n```javascript\nasync function getFlag(tenantId, flagName, { cache, appConfig, logger }) {\n  const cacheKey = `${tenantId}:${flagName}`;\n  \n  let flag = await cache.get(cacheKey);\n  if (flag !== undefined) return flag;\n  \n  try {\n    flag = await appConfig.get({ key: `${tenantId}/${flagName}` });\n    await cache.set(cacheKey, flag, { TTL: 30 });\n  } catch (err) {\n    logger.warn('Flag fetch failed, checking stale cache', { tenantId, flagName });\n    flag = await cache.getStale(cacheKey);\n    if (!flag) throw err;\n  }\n  \n  return flag;\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "azure-developer-q-23",
        "questionId": "q-18340",
        "question": "You're building a beginner Azure data-ingestion service for multi-tenant CSV uploads. Implement a Functions pipeline that triggers on blob upload, validates header ['id','timestamp','value'], converts timestamp to ISO and value to decimal, and writes clean data to ADLS Gen2 at /tenants/{tenantId}/clean. Expose a read API via Functions + Cosmos with per-tenant isolation and TTL. How would you structure data layouts and safeguards?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Databricks Structured Streaming consuming from Event Hubs, and upsert into Delta Lake with tenantId and id as the key. Mask PII during streaming via a UDF. Handle Salesforce deletions with tombsto",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a two-layer design: 1) per-tenant data isolation in the storage layer (Cosmos DB with RLS or Delta Lake with tenant keys) and 2) per-tenant quotas in the API layer using API Management polic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create an HTTP-triggered Azure Function that reads tenantId from a header, authenticates using Managed Identity to access Cosmos DB and Service Bus, and writes per-tenant metadata to Cosmos DB with pa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Blob-triggered Azure Function (Consumption plan) to process incoming CSV files. First, parse the CSV and validate that the required header row contains ['id', 'timestamp', 'value']. Drop any rows missing these req…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests familiarity with Azure data ingestion patterns, per-tenant isolation, and practical data quality safeguards at a beginner level.\n\n## Key Concepts\n\n- Azure Functions blob trigger for event-driven ingestion\n- CSV parsing with strict header validation\n- ADLS Gen2 path design: /tenants/{tenantId}/clean\n- Cosmos DB TTL and per-tenant RBAC for isolation\n- Dead-lettering strategies for error handling\n\n## Code Example\n\n```javascript\n// Blob-triggered Azure Function for CSV ingestion\nmodule.exports = async function (context, myBlob) {\n    const csv = myBlob.toString();\n    const lines = csv.split('\\n');\n    const header = lines[0].split(',');\n    \n    // Validate required header\n    const required = ['id', 'timestamp', 'value'];\n    if (!required.every(h => header.includes(h))) {\n        context.log.error('Invalid header');\n        return;\n    }\n    \n    // Parse and transform rows\n    const cleanData = lines.slice(1)\n        .filter(line => line.trim())\n        .map(line => {\n            const values = line.split(',');\n            return {\n                id: values[0],\n                timestamp: new Date(values[1]).toISOString(),\n                value: parseFloat(values[2])\n            };\n        });\n    \n    // Write to ADLS Gen2 at /tenants/{tenantId}/clean\n};\n```\n\n## Follow-up Questions\n\n- How would you handle evolving CSV schemas across tenants?\n- What partition key strategy would you use in Cosmos DB?\n- How would you implement pagination for the read API?\n- What testing strategies would you use for this pipeline?",
        "difficulty": "beginner"
      },
      {
        "id": "azure-developer-q-24",
        "questionId": "q-9851",
        "question": "You're building a beginner Azure image service: a user uploads an image to a blob container, triggering an Azure Function (JavaScript) via Event Grid to create a 256x256 thumbnail in a 'thumbnails' container. Describe implementation for idempotent processing, basic retry, and observability with Application Insights; outline test cases and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition tenant data by region using separate ADLS Gen2 containers; enable Event Hubs Geo-DR and Delta Lake replication via controlled pipelines. Writes hit the primary region; reads served locally.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement Spark Structured Streaming with foreachBatch to MERGE a Delta table using a unique event_id for deduplication. Use event-time watermarking, per-tenant partitioning, and checkpointing to Delt",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement an Azure Function using JavaScript/TypeScript with a GET route that accepts the path parameters `tenantId`, `container`, and `blobName`. First, I'd extract the tenantId from either the `X-Tenant-ID` he…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an Event Grid-triggered Azure Function in JavaScript that responds to blob creation events, resizes images to 256x256 using the sharp library, and stores the resulting thumbnails in a dedicated 'thumbnails' co…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical Azure data processing patterns at a beginner level, testing understanding of Event Grid integration, Azure Functions, Storage services, and observability practices, with particular emphasis on building reliable, idempotent solutions.\n\n## Key Concepts\n- Event Grid integration with Azure Functions for event-driven architecture\n- Idempotent processing using blob name as unique identifier\n- Retry patterns with exponential backoff for transient failures\n- Observability implementation with Application Insights\n- Comprehensive error handling and monitoring strategies\n- Image processing workflow with blob storage integration",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-25T10:19:43.709Z",
    "version": 1
  }
]