[
  {
    "id": "test-cgoa",
    "channelId": "cgoa",
    "channelName": "Cgoa",
    "title": "Cgoa Knowledge Test",
    "description": "Test your understanding of Cgoa with 25 questions.",
    "questions": [
      {
        "id": "cgoa-q-0",
        "questionId": "q-19571",
        "question": "Design a CGOA binding for a C function void process_ids(const int* ids, size_t n, void (*cb)(int id, int status, void* ctx), void* ctx) that reports per-ID status; expose EnumerateIDs(ids []int) ([]int, error) in Go. Use a Go channel in the callback to collect successful IDs and propagate errors if status != 0. Ensure proper memory ownership (allocate C array, free it) and thread-safety for concurrent calls. Include header, minimal C, Go binding, and a small Go main that runs two batches in parallel?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: wrap process_ids with a CGO binding that allocates a C array for inputs, passes a ctx struct containing a Go channel to the callback, and collects successes while surfacing the firs",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Explain memory ownership, error translation, and a worker-pool pattern that converts C-allocated JSON to Go and frees it safely per block.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Declare the C function in a preamble, call it from Go, convert the returned char* with C.GoString, and defer C.free(ptr) to avoid leaks. Prefer a small wrapper that owns the allocation and exposes a d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a CGOA wrapper for a C streaming API using callbacks. Expose StreamAnalytics(data []float64) ([]string, error). Manage the lifetime of on_chunk and on_done, marshal C strings to Go safely, a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEvaluate ability to design a safe CGOA binding around a C callback API and reason about memory ownership and concurrency.\n\n## Key Concepts\n\n- CGO callbacks and Go pointers\n- Ownership of malloc’d buffers\n- Synchronization for concurrent calls\n- OSThread and callback stability\n\n## Code Example\n\n```go\n// Pseudo-Go binding sketch (illustrative; actual code depends on the C header)\n```\n\n## Follow-up Questions\n\n- How would you handle error propagation if the callback is invoked from another thread?\n- How would you test for memory leaks in this binding?",
        "difficulty": "beginner"
      },
      {
        "id": "cgoa-q-1",
        "questionId": "q-25283",
        "question": "Design a CGOA wrapper for a C streaming API that pushes binary blocks to a callback. C API: int start_streaming(void (*cb)(const uint8_t* data, size_t len, void* user), void* user); int stop_streaming(); Expose in Go: StartStreaming(handler func([]byte)) (stop func(), err error). Include: header, minimal C impl, CGO binding, and a Go program with a worker pool for chunk processing and graceful shutdown, ensuring memory ownership?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## C Header (parser.h) ```c #ifndef PARSER_H #define PARSER_H #include <stdint.h> #include <stddef.h> typedef struct Parser Parser; typedef struct ParserConfig { int max_frame_size; int flags; } ParserConfig; Parser* pa…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Go binding that converts []string to a C array using C.CString for each element, calls build_histogram, copies the returned JSON into a Go string with C.GoString, then properly frees both out_json with C.fre…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use CGO with a per-call context and a callback exported to Go. For each input, convert to C string, register its index, and call batch_process. The C callback copies the result to Go via a channel to maintain order and…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Register a C callback from Go, map a unique id to a Go channel, and have a tiny C wrapper forward data to that id. Copy data into a Go byte slice before enqueue to a worker pool; ensure stop_streaming",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\n- Tests ability to design C<->Go callback bridges with memory ownership and cancellation.\n- Exercises safe passing of binary blocks without copying, with a Go worker pool.\n\n## Key Concepts\n\n- CGO callback lifetime and thread-safety\n- Handling pointers and avoiding Go pointer passing to C\n- Message framing, backpressure, and graceful shutdown\n- Error propagation across CGO boundary\n\n## Code Example\n\n```c\n// header\ntypedef int (*cb_t)(const unsigned char* data, size_t len, void* user);\nint start_streaming(cb_t cb, void* user);\nvoid stop_streaming();\n```\n\n```go\n// binding sketch (high level)\nimport \"C\"\n//go:linkname _start_streaming C.start_streaming\n//export goCallback\nfunc goCallback(data *C.uchar, len C.size_t, user unsafe.Pointer) { /* forward to Go channel by id */ }\n```\n\n## Follow-up Questions\n\n- How would you handle backpressure if callbacks arrive faster than workers process?\n- How would you ensure memory is freed if streaming stops unexpectedly?",
        "difficulty": "advanced"
      },
      {
        "id": "cgoa-q-2",
        "questionId": "q-25787",
        "question": "Design a CGOA wrapper for a C API that returns a heap-allocated dataset struct. Provide: 1) header and minimal C impl; 2) a Go binding exposing type Dataset{N int; Values []float64; Labels []string} and CreateDataset(n int) (Dataset, error) that copies data and frees memory; 3) a Go program that concurrently requests two datasets and prints summaries. How would you implement memory ownership and error handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a single C callback bridged to Go via an exported function; maintain per-stream state in a mutex-protected map keyed by stream_id, storing a Go callback. Register a C shim that calls the Go bridge",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Explain how to wrap a C API that returns a heap-allocated dataset struct via CGOA. Include a header and minimal C impl for dataset_t with n, double* values, const char** labels; implement dataset_crea",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a CGOA wrapper around a C streaming JSON parser with an opaque ParserHandle and APIs: create_parser(), push_chunk(), next_token() returning malloc’d tokens, and free_token(). In Go, expose Json",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a cgo.Handle to store Go channels and export a C callback that routes progress updates back to Go. Pass the handle as user to stream_transform; in C call cb(percent, user) as progress. Allocate ou",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests memory ownership across CGO, how to copy nested C data into Go, and proper cleanup in both directions. It also checks error propagation when allocations fail.\n\n## Key Concepts\n- CGO memory ownership and deep copies\n- Converting C arrays to Go slices\n- Defensive error handling and NULL checks\n\n## Code Example\n```c\n// dataset.h\n#include <stddef.h>\ntypedef struct { size_t n; double* values; const char** labels; } dataset_t;\ndataset_t* dataset_create(size_t n);\nvoid dataset_free(dataset_t* ds);\n```\n```c\n// dataset.c\n#include <stdlib.h>\n#include <string.h>\n#include \"dataset.h\"\ndataset_t* dataset_create(size_t n){\n  dataset_t* ds = (dataset_t*)malloc(sizeof(dataset_t));\n  if(!ds) return NULL;\n  ds->n = n;\n  ds->values = (double*)malloc(n * sizeof(double));\n  ds->labels = (const char**)malloc(n * sizeof(char*));\n  if(!ds->values || !ds->labels){ free(ds->values); free(ds->labels); free(ds); return NULL; }\n  for(size_t i=0;i<n;i++){\n    ds->values[i] = (double)i;\n    char tmp[16];\n    snprintf(tmp, sizeof(tmp), \"v%zu\", i);\n    ds->labels[i] = strdup(tmp);\n  }\n  return ds;\n}\nvoid dataset_free(dataset_t* ds){\n  if(!ds) return;\n  for(size_t i=0;i<ds->n;i++) free((void*)ds->labels[i]);\n  free(ds->labels);\n  free(ds->values);\n  free(ds);\n}\n```\n```go\n// dataset.go (Go binding sketch)\npackage cgowa\n/*\n#include \"dataset.h\"\n*/\nimport \"C\"\nimport (\n  \"errors\"\n)\n\ntype Dataset struct {\n  N      int\n  Values []float64\n  Labels []string\n}\n\n// CreateDataset creates a dataset by calling the C API and copying data\nfunc CreateDataset(n int) (Dataset, error) {\n  if n <= 0 {\n    return Dataset{}, errors.New(\"invalid size\")\n  }\n  ds := C.dataset_create(C.size_t(n))\n  if ds == nil {\n    return Dataset{}, errors.New(\"C allocation failed\")\n  }\n  // copy values\n  vals := make([]float64, int(ds.n))\n  valsPtr := (*[1 << 30]C.double)(unsafe.Pointer(ds.values))\n  for i := 0; i < int(ds.n); i++ {\n    vals[i] = float64(valsPtr[i])\n  }\n  // copy labels\n  labels := make([]string, int(ds.n))\n  for i := 0; i < int(ds.n); i++ {\n    labels[i] = C.GoString(ds.labels[i])\n  }\n  C.dataset_free(ds)\n  return Dataset{N: int(ds.n), Values: vals, Labels: labels}, nil\n}\n```\n\n```go\n// main.go\npackage main\nimport (\n  \"fmt\"\n  cgow \"path/to/your/cgowa\"\n  \"sync\"\n)\nfunc main() {\n  var wg sync.WaitGroup\n  for i := 0; i < 2; i++ {\n    wg.Add(1)\n    go func(size int) {\n      defer wg.Done()\n      d, err := cgow.CreateDataset(size)\n      if err != nil { fmt.Println(\"err\", err); return }\n      fmt.Printf(\"N=%d first_label=%s\\n\", d.N, d.Labels[0])\n    }(5*(i+1))\n  }\n  wg.Wait()\n}\n```\n\n## Follow-up Questions\n- How would you handle partial failures during copies? \n- How would you expose a view into the C memory without owning it? \n- What are the trade-offs of copying vs. zero-copy approaches in CGO?",
        "difficulty": "beginner"
      },
      {
        "id": "cgoa-q-3",
        "questionId": "q-28270",
        "question": "Design a CGOA wrapper for a C function that sorts an array of C strings using a Go-supplied comparator. The C API: int sort_strings(const char** in, size_t n, char*** out_sorted); The function allocates out_sorted with malloc and outputs strings in sorted order, alongside a status. Provide: 1) header and minimal C impl; 2) a Go binding exposing SortStrings(vals []string, cmp func(string,string) int) ([]string, error) via CGO; and 3) a Go program that streams batches of strings to a worker pool, sorts using a Go comparator, and produces a JSON array of sorted strings with original indices, ensuring memory ownership and error propagation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Stream wrapper that owns a C handle and a progress channel. Export a C callback with //export to forward progress into Go, using a stable ctx pointer. Guard all Write calls with a mutex; ensu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implementation strategy: use a trampoline callback in Go (//export) to push JSON into a Go channel; C allocates json with malloc and Go frees it after consumption. Bindings expose StreamBlocks(blocks",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Serialize CGO calls with a mutex, copy the C message to Go, free the C result, and return meaningful errors when nil or code != 0. Pass query as C string and free it. Ensure no Go pointers escape into",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Plan Implement a C wrapper function `sort_strings` that accepts input strings, allocates the output array with `malloc`, and returns sorted results. In the Go binding, export a Go comparator as a C callback using `//…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates a candidate's understanding of CGO memory ownership semantics, cross-language callbacks, and safe concurrent usage patterns. It tests whether the developer can bridge Go and C while maintaining proper memory lifecycle management and error propagation across the language boundary.\n\n## Key Concepts\n\n- **CGO memory ownership**: Allocating and freeing memory on the correct side of the language boundary. Go strings must be converted to C strings using `C.CString`, passed to C, and explicitly freed with `C.free` when the Go binding is responsible for cleanup. Failing to track ownership leads to memory leaks or use-after-free bugs.\n\n- **Cross-language callbacks**: Go functions cannot be called directly from C. The solution uses a handle-based approach: the Go comparator is stored in a map, a unique integer handle is passed to C, and C invokes the comparator through this handle. This indirection avoids passing function pointers across the language boundary.\n\n- **Concurrent worker pools**: Streaming batches through a bounded channel-based worker pool demonstrates Go's concurrency primitives (`goroutines`, `channels`, `sync.WaitGroup`). Proper channel management—closing producers before waiting on consumers—prevents deadlocks.\n\n- **Memory safety with `unsafe.Pointer`**: Reading the C output array requires pointer arithmetic. The code casts the allocated pointer to `uintptr` for arithmetic, then dereferences back to `**C.char`, which is safe provided the allocation is verified to succeed.\n\n## Common Pitfalls\n\n1. **Forgetting to free C-allocated memory**: If C allocates `out_sorted`, Go must not call `C.free` on it. The caller must track ownership boundaries.\n2. **Handle lifecycle bugs**: Forgetting to delete the comparator from the map causes memory leaks and potential stale function references.\n3. **Incorrect pointer arithmetic**: Using `sizeof(char*)` instead of `unsafe.Sizeof` in Go can cause misalignment when reading the output array.\n4. **Race conditions**: The comparator map requires proper locking; read-write locks (`sync.RWMutex`) minimize contention during comparator lookup.\n\n## Evaluation Criteria\n\nStrong candidates will:\n- Correctly manage memory ownership across the language boundary\n- Handle edge cases (empty input, allocation failures)\n- Use idiomatic Go patterns for concurrency\n- Explain the trade-offs of the handle-based callback approach\n- Identify potential race conditions and mitigate them",
        "difficulty": "advanced"
      },
      {
        "id": "cgoa-q-4",
        "questionId": "q-17475",
        "question": "Design a CGOA wrapper for a C function that streams a sequence of compressed data blocks, decompresses them in parallel, and returns a malloc-allocated JSON with per-block decompress size, time, and a checksum; provide: 1) a C header and a minimal C implementation (JSON allocated with malloc, freed by caller), 2) a Go binding exposing StreamDecompress(blocks [][]byte) (string, error) via CGO, and 3) a Go program that uses a worker pool to feed blocks and collect per-block stats with proper memory ownership and error propagation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a per-stream context and a C callback exported to Go. Use a Go channel to forward chunks, copy data into Go memory, feed an io.Pipe writer, and return an io.Reader to the caller. Ensure clean",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a 256-byte C buffer in Go, pass a C string prefix, call generate_token, and if return is non-zero map the error code to a Go error; on success convert the buffer to a Go string with C.GoString.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "StreamDecompress wrapper: C function stream_decompress(const unsigned char** blocks, const size_t* lengths, size_t n, char** out_json); uses zlib to decompress each block concurrently; outputs malloc-",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Go wrapper that creates a per-request channel, registers it with runtime/cgo.Handle, and passes a uintptr ctx to fetch_item_async. In the C callback, recover the handle, read the channel, conver",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\\n\\n- Tests CGO bounds, memory ownership, and cross-language error handling\\n- Exercises per-call resource management and thread safety across FFI\\n\\nKey Concepts\\n- CGO bindings, malloc/free ownership\\n- Multi-threaded C work with per-block stats\\n- JSON marshaling in C, error propagation to Go\\n\\nCode Example\\n```javascript\\n// Implementation code here\\n```\\n\\nFollow-up Questions\\n- How would you handle varying block sizes with backpressure?\\n- What are failure modes and how to test memory leaks across languages?",
        "difficulty": "intermediate"
      },
      {
        "id": "cgoa-q-5",
        "questionId": "q-21171",
        "question": "Design a CGOA wrapper for a C function that processes a string in chunks and reports progress via a callback: int process_with_progress(const char* input, void (*progress)(size_t done, size_t total, void* ctx), void* ctx, char** out_json); The C function allocates out_json with malloc. Expose a Go binding: func ProcessWithProgress(input string) (string, error) that converts the JSON to Go, frees the C buffer, and streams progress updates via a channel. Demonstrate two concurrent invocations and a minimal test harness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use C thread-local storage to bind a tls_ctx to every OS thread. TLS is created on first tls_eval call for that thread and destroyed on program exit. In Go, wrap tls_eval via CGO; pin the goroutine to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Two-Pass CGO Wrapper with Worker Pool ### C Header (format_user.h) ```c #ifndef FORMAT_USER_H #define FORMAT_USER_H #include <stddef.h> int format_user(char* out, size_t out_cap, const char* name); #endif ``` ### C I…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a CGOA wrapper for a C function that processes a string in chunks and reports progress via a callback. The C API: int process_with_progress(const char* input, void (*progress)(size_t done, siz",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a CGOA binding where StartAsync launches a C async task and returns a Go channel for results. Use cgo.Handle to carry the Go callback object through the C layer, ensure the callback switches to",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests CGO memory ownership, callbacks bridging, and basic concurrency.\n\n## Key Concepts\n- CGO memory management across Go/C boundary\n- C to Go callbacks and //export considerations\n- Thread-safe use with goroutines\n- Proper error translation and freeing malloc'd strings\n\n## Code Example\n```c\n/* c_wrapper.h */\nint process_with_progress(const char* input, void (*progress)(size_t done, size_t total, void* ctx), void* ctx, char** out_json);\n```\n```c\n/* c_wrapper.c */\n#include <stdlib.h>\n#include <string.h>\nint process_with_progress(const char* input, void (*progress)(size_t, size_t, void*), void* ctx, char** out_json) {\n  size_t total = strlen(input);\n  size_t done = 0;\n  while (done < total) {\n    size_t step = total - done >= 1024 ? 1024 : total - done;\n    done += step;\n    if (progress) progress(done, total, ctx);\n  }\n  *out_json = malloc(4); strcpy(*out_json, \"{}\");\n  return 0;\n}\n```\n```go\n// Go binding sketch with //export callback\npackage main\nimport \"C\"\nimport \"unsafe\"\n\n//export progress_cb\nfunc progress_cb(done C.size_t, total C.size_t, ctx unsafe.Pointer) {\n  // send progress through a channel stored in ctx\n}\n\nfunc ProcessWithProgress(input string) (string, error) {\n  // convert and call C.process_with_progress, handle out_json and free\n  return \"{}\", nil\n}\n```\n\n## Follow-up Questions\n- How would you safely pass progress updates back to Go without blocking?\n- How would you handle errors and translate errno to Go error values?",
        "difficulty": "beginner"
      },
      {
        "id": "cgoa-q-6",
        "questionId": "q-17507",
        "question": "Design a CGOA wrapper for a C library function that sorts an array of strings using a comparator supplied from Go. The C function signature is int sort_strings(char** in_out, int n, int (*cmp)(const char*, const char*, void*), void* ctx). Provide a header, a minimal C implementation (no global state, uses the comparator), a Go binding exposing SortStringsWithComparator(vals []string, cmp func(string,string) int) ([]string, error) using CGO, and a Go program that processes multiple batches concurrently with a worker pool while ensuring Go closures survive callbacks and C allocations are freed?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Wrap tokenize_line, convert the C array to a Go slice by calling GoString on each token, then free each C string and the tokens array with free_tokens. The binding should be stateless, return ([]strin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Explain designing a CGOA bridge that uses a C library streaming audio via a callback, with a Go-exported callback as a trampoline. Use a Go-managed context id stored in C as a void* but do not pass Go",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a C wrapper that allocates counts and edges, and expose free_histogram. In Go, call histogram, copy into Go slices, then free via free_histogram. Create a small program that starts two gorou",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a CGO wrapper for a C library function that sorts an array of strings using a comparator supplied from Go. The C function signature is `int sort_strings(char** in_out, int n, int (*cmp)(const char*, const char*,…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to bridge Go closures to C, manage memory safely across the language boundary, and implement concurrent multi-batch processing with CGO while keeping the Go garbage collector safe.\n\n## Key Concepts\n- **CGO callback bridge**: Using `cgo.Handle` to safely store and invoke Go closures from C\n- **Pointer safety**: Passing Go pointers to C without violating CGO safety rules\n- **Memory lifecycle**: Allocating C strings, sorting in place, and freeing all allocations appropriately\n- **Concurrency**: Worker pool pattern for processing multiple batches\n\n## Solution\n\n### header.h\n```c\n#ifndef SORT_STRINGS_H\n#define SORT_STRINGS_H\n\n#include <stddef.h>\n\nint sort_strings(char** in_out, size_t n, \n    int (*cmp)(const char*, const char*, void*), \n    void* ctx);\n\n#endif\n```\n\n### sort_strings.c\n```c\n#include \"header.h\"\n#include <stdlib.h>\n#include <string.h>\n\nstatic void swap(char** a, char** b) {\n    char* tmp = *a;\n    *a = *b;\n    *b = tmp;\n}\n\nint sort_strings(char** in_out, size_t n, \n    int (*cmp)(const char*, const char*, void*), \n    void* ctx) {\n    if (!in_out || n == 0 || !cmp) return -1;\n    \n    for (size_t i = 0; i < n - 1; i++) {\n        for (size_t j = 0; j < n - i - 1; j++) {\n            int result = cmp(in_out[j], in_out[j + 1], ctx);\n            if (result > 0) {\n                swap(&in_out[j], &in_out[j + 1]);\n            }\n        }\n    }\n    return 0;\n}\n```\n\n### sort_wrapper.go\n```go\npackage main\n\n/*\n#include \"header.h\"\n#include <stdlib.h>\n\ntypedef int (*CmpFn)(const char*, const char*, void*);\n\nstatic int wrapper_cmp(const char* a, const char* b, void* ctx) {\n    // This is a placeholder; actual invocation happens via CGO\n    return 0;\n}\n*/\nimport \"C\"\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"sync\"\n\t\"unsafe\"\n)\n\ntype sortHandle C.int\n\nvar handleStore = struct {\n\tsync.RWMutex\n\tm map[sortHandle]func(string, string) int\n}{\nm: make(map[sortHandle]func(string, string) int)}\n\n//export goCmpCallback\nfunc goCmpCallback(a, b *C.char, ctx unsafe.Pointer) C.int {\n\thandle := sortHandle(uintptr(ctx))\n\thandleStore.RLock()\n\tcmpFn := handleStore.m[handle]\n\thandleStore.RUnlock()\n\t\n\tif cmpFn == nil {\n\t\treturn 0\n\t}\n\treturn C.int(cmpFn(C.GoString(a), C.GoString(b)))\n}\n\nfunc SortStringsWithComparator(vals []string, cmp func(string, string) int) ([]string, error) {\n\tif cmp == nil {\n\t\treturn nil, fmt.Errorf(\"comparator cannot be nil\")\n\t}\n\n\thandle := sortHandle(runtime.NewCString(\"\")) // dummy allocation for unique handle\n\thandleStore.Lock()\n\thandleStore.m[handle] = cmp\n\thandleStore.Unlock()\n\n\tcStrs := make([]*C.char, len(vals))\n\tfor i, v := range vals {\n\t\tcStrs[i] = C.CString(v)\n\t}\n\n\tresult := C.sort_strings(&cStrs[0], C.size_t(len(cStrs)),\n\t\t(C.CmpFn)(C.wrapper_cmp), unsafe.Pointer(uintptr(handle)))\n\n\tfor i := range cStrs {\n\t\tC.free(unsafe.Pointer(cStrs[i]))\n\t}\n\n\thandleStore.Lock()\n\tdelete(handleStore.m, handle)\n\thandleStore.Unlock()\n\n\tif result != 0 {\n\t\treturn nil, fmt.Errorf(\"sort failed with code %d\", result)\n\t}\n\n\tresultVals := make([]string, len(vals))\n\tfor i, cs := range cStrs {\n\t\tresultVals[i] = C.GoString(cs)\n\t}\n\treturn resultVals, nil\n}\n```\n\n### worker_pool.go\n```go\npackage main\n\nimport (\n\t\"context\"\n\t\"sync\"\n)\n\ntype Job struct {\n\tID    int\n\tInput []string\n}\n\ntype WorkerPool struct {\n\tworkers   int\n\tjobs      chan Job\n\tresults   chan []string\n\terrors    chan error\n\twg        sync.WaitGroup\n}\n\nfunc NewWorkerPool(workers int, bufSize int) *WorkerPool {\n\treturn &WorkerPool{\n\t\tworkers: workers,\n\t\tjobs:    make(chan Job, bufSize),\n\t\tresults: make(chan []string, bufSize),\n\t\terrors:  make(chan error, bufSize),\n\t}\n}\n\nfunc (p *WorkerPool) Start(ctx context.Context, cmp func(string, string) int) {\n\tfor i := 0; i < p.workers; i++ {\n\t\tp.wg.Add(1)\n\t\tgo p.worker(ctx, cmp)\n\t}\n}\n\nfunc (p *WorkerPool) worker(ctx context.Context, cmp func(string, string) int) {\n\tdefer p.wg.Done()\n\tfor {\n\t\tselect {\n\t\tcase <-ctx.Done():\n\t\t\treturn\n\t\tcase job, ok := <-p.jobs:\n\t\t\tif !ok {\n\t\t\t\treturn\n\t\t\t}\n\t\t\tresult, err := SortStringsWithComparator(job.Input, cmp)\n\t\t\tif err != nil {\n\t\t\t\tp.errors <- err\n\t\t\t} else {\n\t\t\t\tp.results <- result\n\t\t\t}\n\t\t}\n\t}\n}\n\nfunc (p *WorkerPool) Submit(job Job) {\n\tp.jobs <- job\n}\n\nfunc (p *WorkerPool) Close() {\n\tclose(p.jobs)\n\tp.wg.Wait()\n\tclose(p.results)\n\tclose(p.errors)\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "cgoa-q-7",
        "questionId": "q-4262",
        "question": "Design a CGOA wrapper for a C event system that streams events on a background thread: \n\nC API:\ntypedef void (*event_cb)(int id, const char* payload, void* user);\nvoid start_events(void);\nvoid stop_events(void);\nvoid register_event_cb(event_cb cb, void* user);\n\nRequirements:\n- In Go, expose func RegisterHandlers(hs []Handler) (cleanup func(), err error) where Handler has id int and Callback func(string)\n- Use cgo.Handle to pass Go callbacks to C safely; ensure callbacks from C invoke the correct Go handler and do not poison GC\n- Support registering multiple independent handlers concurrently; events must route to the right callback; stop_events cleans resources and prevents further callbacks\n- Provide a minimal header and a C implementation that emits events every 50ms on a worker thread\n- Provide a Go binding and a small program that registers three handlers, prints received payloads, then calls the cleanup when done\n- Discuss lifecycle and CGO threading caveats?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a CGOA wrapper that stores Go callbacks in a sync.Map and passes a cgo.Handle integer as the user pointer to the C callback. The C side emits events on a thread and calls back through an exported",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a CGOA wrapper for a C event-source API that registers a callback (void (*cb)(const char* msg, void* ctx)) and delivers messages from a background C thread; implement in Go a binding that allow",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Go wrapper with a central event loop. Store onStatus in a runtime/cgo.Handle and pass its uintptr to C as user. Implement an exported C callback (//export status_cb) that converts the C string w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Wrap resource_t with a Go struct { p *C.resource_t; mu sync.Mutex; closed bool }. NewResource calls resource_create(); Do(cmd string) uses a C string and calls resource_do; Close() calls resource_rele",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to bridge Go and C via CGO under concurrency, including safe callback invocation from C threads, memory management, and lifecycle cleanup. Emphasizes correct use of cgo.Handle and avoiding Go GC pitfalls.\n\n## Key Concepts\n- CGO thread safety and Go callback lifetimes\n- cgo.Handle usage to pass Go values to C\n- Concurrency: multiple independent handlers, event routing\n- Cleanup: unregister, stop C thread, prevent use-after-free\n\n## Code Example\n```c\n// header: event_api.h\ntypedef void (*event_cb)(int id, const char* payload, void* user);\nvoid start_events(void);\nvoid stop_events(void);\nvoid register_event_cb(event_cb cb, void* user);\n```\n```go\n// binding.go (simplified)\npackage eventapi\n\nimport \"C\"\n\n//export goEventHandler\nfunc goEventHandler(id C.int, payload *C.char, user unsafe.Pointer) {\n    // dispatch using the handle stored in user\n}\n```\n\n## Follow-up Questions\n- How would you handle backpressure if event rate overwhelms Go side?\n- What are potential deadlocks with stop_events and in-flight callbacks, and how to mitigate them?",
        "difficulty": "advanced"
      },
      {
        "id": "cgoa-q-8",
        "questionId": "q-4067",
        "question": "Design a CGOA wrapper for a stateful C library exposing ip_init, ip_process, ip_destroy. Build Go bindings providing NewImageProcessor(config string) (*ImageProcessor, error), (p *ImageProcessor) Process(data []byte) ([]byte, error), and (p *ImageProcessor) Close(). Demonstrate two instances in parallel with different inputs to prove per-instance isolation. Include header and minimal C, Go binding, and a small program that runs two goroutines in parallel; how would you implement and verify this binding?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a CGOA binding where StartAsync launches a C async task and returns a Go channel for results. Use cgo.Handle to carry the Go callback object through the C layer, ensure the callback switches to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a Go struct ImageProcessor that stores a C pointer. NewImageProcessor calls ip_init and stores the handle; Process calls ip_process, copies the C output into a Go slice, then frees the C buffer",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "An approach: expose a CGO wrapper where a C function allocates a JSON string (malloc) describing the bloom filter built from inputs; Go passes inputs as C strings, C computes m, k, fpp, and per-input",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a cgo.Handle to store Go channels and export a C callback that routes progress updates back to Go. Pass the handle as user to stream_transform; in C call cb(percent, user) as progress. Allocate ou",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests memory ownership across language boundaries and per-instance isolation when CGO is used with stateful C libraries. It also checks basic error mapping and concurrent usage.\n\n## Key Concepts\n- CGO memory management between C and Go\n- Opaque per-instance handles\n- Error translation from C to Go\n- Safe concurrent usage of CGO-bound objects\n\n## Code Example\n```c\n// header\ntypedef struct ImageProcessor ImageProcessor;\nImageProcessor* ip_init(const char* config, int* err);\nint ip_process(ImageProcessor* ip, const uint8_t* src, size_t len, uint8_t** out, size_t* out_len);\nvoid ip_destroy(ImageProcessor* ip);\n```\n```go\n// binding sketch (Go)\n// type ImageProcessor struct{ p *C.ImageProcessor }\n// func NewImageProcessor(config string) (*ImageProcessor, error) { ... }\n// func (ip *ImageProcessor) Process(data []byte) ([]byte, error) { ... }\n// func (ip *ImageProcessor) Close() { ... }\n```\n\n## Follow-up Questions\n- How would you add timeout handling for Process calls?\n- How would you prevent leaks if ip_process frequently fails mid-copy?",
        "difficulty": "beginner"
      },
      {
        "id": "cgoa-q-9",
        "questionId": "q-19368",
        "question": "Design a CGOA wrapper for a C function that streams per-row data to a Go callback: int process_rows(const double* data, size_t rows, size_t cols, void (*row_cb)(size_t row, const double* row, void* user), void* user); Implement the Go binding exposing StreamRows(matrix [][]float64) error and a minimal C header/implementation. Ensure memory ownership and thread safety when rows are processed by multiple goroutines, and explain how to avoid passing Go pointers to C directly?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use CGO with a Go-exported callback to forward each frame to a Go channel mapped by a per-stream handle. The C side allocates frames (malloc); Go copies len bytes into a new Go slice and calls free on",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a Go CGO bridge that stores a Go callback in a map and passes a stable handle to C (via a cgo.Handle) instead of Go pointers. For each row, allocate a small C buffer, copy the row, invoke the C",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Map C User to Go: FetchUser calls C.fetch_user, checks nil, copies fields with C.GoString, then calls C.free_user(u). Return an idiomatic Go struct. On error, return a descriptive error. Launch two go",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a CGOA wrapper around a C streaming JSON parser with an opaque ParserHandle and APIs: create_parser(), push_chunk(), next_token() returning malloc’d tokens, and free_token(). In Go, expose Json",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests CGO callback boundaries and memory ownership.\n\n## Key Concepts\n- CGO pointer passing rules\n- Go memory management vs C malloc\n- Exposing Go callbacks to C with stable handles\n- Avoiding Go pointers passed to C and deadlocks with worker pools\n\n## Code Example\n```javascript\n// Minimal sketch\n#include <stdlib.h>\n typedef void (*row_cb_t)(size_t, const double*, void*);\n int process_rows(const double* data, size_t rows, size_t cols, row_cb_t cb, void* user){\n   for (size_t r=0; r<rows; ++r){\n     cb(r, data + r*cols, user);\n   }\n   return 0;\n }\n```\n\n## Follow-up Questions\n- How would you test multi-threaded callback safety?\n- How would you extend to backpressure and error propagation?",
        "difficulty": "advanced"
      },
      {
        "id": "cgoa-q-10",
        "questionId": "q-18379",
        "question": "Implement a CGOA wrapper for a C function that builds a singly linked list from an array of strings and returns a heap-allocated list. Provide a C header exposing: typedef struct Node { const char* value; struct Node* next; } Node; Node* build_list(const char** items, size_t n); void free_list(Node* head); The Go binding should expose: func BuildList(items []string) ([]string, error); It must convert the C list into a Go slice, then free the C list via free_list. Ensure safe concurrent calls from multiple goroutines. Include header, minimal C implementation (malloc/strdup-based values), a Go binding, and a small Go program that runs two concurrent calls with distinct inputs and prints results?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a CGO bridge with a Go-exported C callback, assign per-decoder context, and deliver frames through a Go channel. For each decoder, have C allocate frames (malloc) and copy into a Go byte slice before freeing the C b…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a CGO wrapper that calls C.gen_blob(seed,n) producing a Blob{len,data}; copy data into a Go []byte, then call blob_free. Ensure no Go pointers cross into C, validate NULL, and free on error.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Register per-task Go callbacks in a mutex-protected map[int]func(int,string). Create a Go–C bridge: assign a unique task handle, store the Go closure, pass the handle to C as an int with a static C progress callback. Ex…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a CGO wrapper with a Node struct, build_list using strdup for each item, and return the head pointer. The free_list function traverses the list, freeing both the strings (via free()) and the nodes (via free())…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests memory ownership and pointer traversal across language boundaries in a practical CGO task.\n\n## Key Concepts\n- CGO memory management (malloc, strdup, free)\n- Converting C linked lists to Go slices\n- Proper free semantics and ownership transfer\n- Concurrency safety with CGO calls using mutex synchronization\n\n## Code Example\n```c\n/* C header and implementation */\ntypedef struct Node { const char* value; struct Node* next; } Node;\n\nNode* build_list(const char** items, size_t n) {\n    Node* head = NULL;\n    Node* tail = NULL;\n    for (size_t i = 0; i < n; i++) {\n        Node* node = malloc(sizeof(Node));\n        node->value = strdup(items[i]);\n        node->next = NULL;\n        if (tail) tail->next = node;\n        else head = node;\n        tail = node;\n    }\n    return head;\n}\n\nvoid free_list(Node* head) {\n    while (head) {\n        Node* next = head->next;\n        free((void*)head->value);\n        free(head);\n        head = next;\n    }\n}\n```\n\n```go\n// Go binding\npackage main\n\n/*\n#include \"header.h\"\n*/\nimport \"C\"\nimport (\n    \"sync\"\n)\n\nvar mu sync.Mutex\n\nfunc BuildList(items []string) ([]string, error) {\n    mu.Lock()\n    defer mu.Unlock()\n    \n    cItems := make([]*C.char, len(items))\n    for i, s := range items {\n        cItems[i] = C.CString(s)\n    }\n    defer func() {\n        for _, c := range cItems {\n            C.free(unsafe.Pointer(c))\n        }\n    }()\n    \n    head := C.build_list(**C.char(&cItems[0]), C.size_t(len(items)))\n    if head == nil {\n        return nil, nil\n    }\n    \n    var result []string\n    for node := head; node != nil; node = node.next {\n        result = append(result, C.GoString(node.value))\n    }\n    C.free_list(head)\n    return result, nil\n}\n\nfunc main() {\n    var wg sync.WaitGroup\n    wg.Add(2)\n    \n    go func() {\n        defer wg.Done()\n        result, _ := BuildList([]string{\"a\", \"b\", \"c\"})\n        println(\"Goroutine 1:\", result)\n    }()\n    \n    go func() {\n        defer wg.Done()\n        result, _ := BuildList([]string{\"x\", \"y\", \"z\"})\n        println(\"Goroutine 2:\", result)\n    }()\n    \n    wg.Wait()\n}\n```\n\nThe mutex ensures serialized access to C functions, preventing race conditions when multiple goroutines call BuildList concurrently.",
        "difficulty": "beginner"
      },
      {
        "id": "cgoa-q-11",
        "questionId": "q-1294",
        "question": "Implement a CGOA bridge that lets C trigger a Go callback asynchronously when an external sensor fires events. Provide a C header and stub, a CGO binding in Go that registers a Go callback and routes events through an exported Go function, and a safe cleanup mechanism to release resources when producers stop?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-stream bridge: allocate a Go channel, register a CGO-exported Go callback, map ctx to a Go struct with the channel and a cancellation flag. The C side calls log_cb on its thread; the callback checks cancellati…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Wrap a C session with ref counting; NewSession creates and increments, finalizer calls session_unref. Use a per-session Go channel stored via a map guarded by a mutex; pass a cgo.Handle to C as user d",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design approach: Go registers a callback, wraps it with runtime/cgo.NewHandle(cb) and passes the handle’s uintptr to C. An exported bridge function (//export goCallbackBridge) is invoked by C with the",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Go registry (cgo.Handle) to pass a Go channel reference as ctx to stream_strings. The C callback converts the malloc'ed msg to a Go string via C.GoString, frees msg, then pushes to the channel t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nCGOA bridges are common in practice when external C libraries need Go callbacks. This tests safe lifetimes, non-Go-pointer transfer, and cross-language threading.\n\n## Key Concepts\n- runtime/cgo.Handle\n- //exported Go functions\n- passing uintptr_t to C\n- proper cleanup and synchronization\n\n## Code Example\n```c\n/* header */\nvoid register_callback(void (*cb)(int, void*), void* ctx);\n```\n\n## Follow-up Questions\n- How to avoid deadlocks when callbacks re-enter?\n- How to handle panics in the Go callback across threads?",
        "difficulty": "advanced"
      },
      {
        "id": "cgoa-q-12",
        "questionId": "q-17024",
        "question": "Design a CGOA wrapper for a C function that compresses an R x C matrix of doubles using per-row run-length encoding and returns a malloc-allocated JSON string with per-row runs and a global compression ratio. Provide: 1) a C header and a minimal implementation (the JSON is allocated with malloc and must be freed by the caller), 2) a Go binding exposing CompressMatrix(matrix [][]float64) (string, error) via CGO, 3) a Go program that streams batches to a worker pool concurrently, ensuring per-call memory ownership and proper freeing of the C-allocated JSON?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "OpenStore calls kv_open and returns an error if the int* err is nonzero. Get calls kv_get; if non-null, convert with C.GoString and then free the C string; if null, return the error. Close calls kv_cl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Go registry (cgo.Handle) to pass a Go channel reference as ctx to stream_strings. The C callback converts the malloc'ed msg to a Go string via C.GoString, frees msg, then pushes to the channel t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use //export to expose a Go callback, create a C wrapper that calls the exported Go function, and register that wrapper with register_event_source. Store the Go handler in a sync.Map or a single funct",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An ideal answer defines: 1) int compress_matrix_double(const double* data, size_t rows, size_t cols, char** out_json); 2) a minimal C impl that performs per-row run-length encoding, builds JSON with m",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests memory ownership across CGO, dynamic allocation semantics, and safe concurrency in a realistic batch workflow. \n\n## Key Concepts\n- CGO heap ownership and free semantics\n- Safe flattening of 2D Go slices for C\n- Minimal C JSON assembly with malloc\n- Error propagation across CGO boundaries\n- Concurrent batch processing with proper cleanup\n\n## Code Example\n```javascript\n// header example (C-style) to be implemented in C\nint compress_matrix_double(const double* data, size_t rows, size_t cols, char** out_json);\n```\n\n```javascript\n// Go binding sketch (CGO) – actual types and cgo directives omitted for brevity\n// func CompressMatrix(matrix [][]float64) (string, error) { /* flatten and call C.compress_matrix_double */ }\n```\n\n## Follow-up Questions\n- How would you enforce thread-safety if the C implementation uses a shared buffer?\n- How would you handle extremely large matrices that exceed available host memory?",
        "difficulty": "intermediate"
      },
      {
        "id": "cgoa-q-13",
        "questionId": "q-5896",
        "question": "CGOA Binding: Wrap a C API that reports progress via a callback and returns a malloc'ed string?\n\nC API (example):\ntypedef void (*progress_cb)(int percent, void* ctx);\nvoid start_work(progress_cb cb, void* ctx);\nchar* get_result(); // malloc'ed string or NULL on error\n\nTask: implement a CGOA wrapper that exposes:\n- StartWork(param int) (string, error) which runs start_work and then get_result\n- ProgressStream(param int) (<-chan int, error) returning a Go channel of progress updates while the work runs in background\n- Ensure the C callback safely relays progress to Go without CGO rules violations, manage memory (free the result), and allow cancellation.\n\nProvide a C header, a minimal C wrapper, the Go binding, and a small Go program that consumes the progress channel and prints updates; include build steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Strategy: implement a trampoline for C→Go callbacks and a per-task registry. Expose CGO bindings that keep a Go channel for progress; pass a uintptr ctx to C to map to the Go channel. Use //export to",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement a CGOA binding that registers a CGO-exported callback, streams each json_chunk from stream_pca_all into a Go channel, and concatenates into a final JSON. Ownership: on each callback,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A strong answer demonstrates: (1) C API returns a malloc'ed JSON string and exposes free_json; (2) Go binding builds an array of C.float* from [][]float32 blocks, calls fft_batch, uses C.GoString on t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a CGO wrapper that copies the token to C memory with C.CBytes, allocates a C output buffer, calls validate_token, checks the return value, unmarshals the JSON into a Go struct, and frees bot",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests CGO callback wiring, memory ownership, and cross-thread safety; ensures proper Go<->C interaction across goroutines.\n\n## Key Concepts\n- CGO callbacks and //export\n- Avoid passing Go pointers to C across threads\n- Managing malloc'd C strings in Go\n- Concurrency: progress channel + cancellation\n\n## Code Example\n```go\n//go:generate\nimport \"C\"\nimport (\n  \"runtime\"\n  \"unsafe\"\n)\n\n//export goProgressCallback\nfunc goProgressCallback(ctx unsafe.Pointer, progress C.int) {\n  // push progress to channel retrieved via ctx\n  // ...\n  runtime.KeepAlive(ctx)\n}\n```\n\n## Follow-up Questions\n- How would you handle multiple concurrent tasks and ensure callback isolation?\n- What are the risks if the C thread continues after Go side cancels the task?",
        "difficulty": "intermediate"
      },
      {
        "id": "cgoa-q-14",
        "questionId": "q-14582",
        "question": "Design a CGOA wrapper for a C library that streams numeric samples via a callback: void start_stream(const char* device, void (*cb)(double, void*), void* ctx); Expose Go: func StreamReadings(device string, onValue func(float64) error) error. Bridge the C callback to Go, pass a per-stream ctx, and allow onValue errors to cancel the stream. Include a header, C impl, Go binding, and a sample program launching two streams concurrently?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## C Header (tokenizer.h) ```c #ifndef TOKENIZER_H #define TOKENIZER_H typedef struct Token { char* text; int type; } Token; enum TokenType { IDENT = 0, NUM = 1 }; Token* next_token(const char* input, size_t len, int* p…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a CGO wrapper for a C function that verifies a batch of cryptographic signatures on streaming blocks. The C API should be: `int verify_blocks(const uint8_t** blocks, const size_t* block_lens, size_t n, const uint…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a CGOA bridge that binds a C async callback into Go, using a malloc'ed JSON that you own in Go and free. Expose ProcessTelemetry(frames [][]byte) (string, error), ensure memory ownership tra",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bridge a C streaming callback to Go by exporting a Go callback, pass a per-stream context pointer, and propagate Go errors back to C to stop streaming. Ensure memory cleanup for the streaming handle a",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests bridging C callbacks to Go, error propagation from Go to C, and lifecycle cleanup for CGO wrappers. It also touches concurrency safety when running multiple streams.\n\n## Key Concepts\n\n- CGO callback bridging\n- Per-stream context management\n- Error propagation to cancel C streaming\n- Resource cleanup and lifecycle guarantees\n\n## Code Example\n\n```c\n// header only (start_stream declares the API)\nvoid start_stream(const char* device, void (*cb)(double, void*), void* ctx);\n```\n\n## Follow-up Questions\n\n- How would you handle backpressure if onValue is slow?\n- How would you protect against panics in Go code affecting the CGO boundary?",
        "difficulty": "beginner"
      },
      {
        "id": "cgoa-q-15",
        "questionId": "q-24132",
        "question": "Design a CGOA wrapper for a C function that streams PCA results via a callback. The C API is int stream_pca_all(const double** blocks, const size_t* lengths, size_t n, void (*on_result)(const char* json_chunk, void* ctx), void* ctx); Each block is processed into a JSON chunk and delivered to on_result, and a final JSON summary is emitted via a final call or the last chunk. Provide a header and a minimal C implementation using malloc for json chunks, a Go binding exposing StreamPCA(blocks [][]float64) (string, error) that collects chunks from the callback and returns a final JSON string, and a Go program that streams large files in parallel chunks through a worker pool while preserving memory ownership and propagating errors?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: query needed size by calling json_serialize_ints with out=NULL to get required length, allocate a Go buffer of that size, then call the C function with a pointer to the buffer and the actual len",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a CGOA binding that registers a CGO-exported callback, streams each json_chunk from stream_pca_all into a Go channel, and concatenates into a final JSON. Ownership: on each callback,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I would implement process_matrix to allocate a JSON array with per-column stats, compute mean and stddev in a single pass with Welford's algorithm, and return JSON via *out_json. The Go binding conver",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a thread‑safe CGOA bridge that uses a per‑stream Go manager and a buffered channel to deliver tokens. In the C callback, copy value via C.GoStringN, then send JsonToken{Type, Value}",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to bridge C callbacks into Go in a streaming, multi-chunk workflow, focusing on memory ownership and error propagation across boundaries.\n\n## Key Concepts\n- CGO callback bridging and opaque context handles\n- Memory ownership and cross-language malloc/free\n- Streaming guarantees and error propagation\n- Concurrency safety with channels and worker pools\n\n## Code Example\n```c\n// header sketch\nint stream_pca_all(const double** blocks, const size_t* lengths, size_t n, void (*on_result)(const char* json_chunk, void* ctx), void* ctx);\n```\n```go\n// binding sketch: StreamPCA(blocks [][]float64) (string, error)\n```\n\n## Follow-up Questions\n- How would you test memory ownership and guard against leaks in streaming mode?\n- How would you ensure chunk order guarantees if C delivers out-of-order chunks?\n- How would cancellation and timeouts be wired across Go and C boundaries?",
        "difficulty": "advanced"
      },
      {
        "id": "cgoa-q-16",
        "questionId": "q-24680",
        "question": "Design a CGOA wrapper for a C streaming API: void start_streaming(const uint8_t* data, size_t len, void (*chunk_cb)(const char* json, void* ctx), void* ctx); The C library emits a JSON chunk for each processed record via chunk_cb; each chunk is allocated with malloc and must be freed by the receiver after copying. Provide: 1) a header and minimal C impl that registers a callback, streams chunks, and frees each JSON after callback; 2) a Go binding exposing StreamProcess(data []byte) error that wires the callback and streams input; and 3) a Go program that feeds a continuous data stream to a worker pool, collecting per chunk JSON into Go structures, freeing C memory, and propagating errors?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design the wrapper so each Buffer owns its C.Buffer instance. NewBuffer allocates, Push passes a C string (freed after use), and CSV returns a Go string from a malloc'ed CSV then frees with free_csv.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "```c // header.h #ifndef STREAM_WRAPPER_H #define STREAM_WRAPPER_H #include <stddef.h> #include <stdint.h> typedef void (*chunk_callback_t)(const char* json, void* ctx); void start_streaming( const uint8_t* data, size_t…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a CGOA wrapper for a C Session object with per-session state. Provide C: session_create/destroy, session_enqueue, session_finalize (malloc’d JSON). In Go, implement NewSession() (*SessionWrap,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use CGO with a Go-exported callback to forward each frame to a Go channel mapped by a per-stream handle. The C side allocates frames (malloc); Go copies len bytes into a new Go slice and calls free on",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests cross-language callback design, memory ownership, and streaming backpressure between C and Go via CGO. It exercises careful lifetime management and error propagation in real-time data pipelines.\n\n## Key Concepts\n\n### CGO Callbacks and Memory Management\n- **Exported Go functions**: Functions callable from C must be marked with `//export` and use `extern \"C\"` in the C comment block\n- **Memory ownership**: C allocates with `malloc`, Go must call `C.free()` after copying to avoid leaks\n- **cgo.Handle**: Pass context safely across the language boundary without risking garbage collection\n\n### Worker Pool and Backpressure\n- **Channel buffering**: Prevents blocking when producers outpace consumers\n- **Synchronization**: Use `sync.WaitGroup` to coordinate worker shutdown\n- **Error propagation**: Dedicated error channel ensures errors aren't lost in concurrent flows\n\n## Implementation Notes\n\n1. **Callback safety**: The C wrapper function bridges C's raw function pointer to Go's exported function, passing context via `cgo.Handle`\n\n2. **Memory lifecycle**: C allocates JSON → Go copies via `C.GoString` → Go calls `C.free()` immediately after\n\n3. **Worker pool design**: Multiple goroutines consume chunks concurrently, with channels providing backpressure signals\n\n4. **Error handling**: Errors flow through a dedicated channel to avoid blocking the processing pipeline",
        "difficulty": "advanced"
      },
      {
        "id": "cgoa-q-17",
        "questionId": "q-12542",
        "question": "Design a CGOA-based wrapper for a C event-driven API. The C library exposes:\n- void register_event_cb(void (*cb)(int event_id, const char* message, void* ctx), void* ctx);\n- void emit_event(int event_id, const char* message);\nProvide a Go binding exposing StartEventStream(callback func(Event)) error, where Event is type struct { ID int; Message string }. The wrapper must support multiple concurrent streams, deliver events in a thread-safe manner, manage memory for C-allocated strings, and support clean shutdown. Include a header, a minimal C implementation (allocating messages with malloc and freeing in Go), a Go binding using CGO, and a small Go program that starts 4 streams and prints events?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "```go package cgoa // #include <stdint.h> // #include <stdlib.h> // extern int start_work(const uint8_t* in, size_t in_len, uint8_t** out, size_t* out_len); // extern const char* last_error(void); // extern void free_er…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a CGOA bridge using a registry of Go callbacks indexed by a handle. The C side stores a trampoline cb that calls back into Go via a CGO-exported function, routing via a per-handle channel. U",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a C header for tile_analyze_batch, a minimal C that computes per-tile average intensity and a batch checksum, returning JSON via malloc. Go binding should expose TileAnalyze(tiles [][]byte)",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Solution: CGO Wrapper for Streaming C API This solution demonstrates how to use CGO to bind a C streaming API with callback-based data delivery. The Go binding converts the C callback into a Go function, routing each…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a safe cross-language callback bridge, handling lifetime, concurrency, and memory ownership across Go and C.\n\n## Key Concepts\n- CGO callback wiring and trampolines\n- cgo.Handle for passing Go values to C\n- thread-safety and concurrent streams\n- memory ownership: C malloc vs Go GoString + free\n- lifecycle: registration, delivery, and cleanup\n\n## Code Example\n```c\n// Minimal header for the wrapper (example)\nvoid register_event_cb(void (*cb)(int, const char*, void*), void* ctx);\nvoid emit_event(int id, const char* msg);\n```\n```go\n// Go binding skeleton using CGO\npackage main\nimport \"C\"\n//export goEventCallback\nfunc goEventCallback(id C.int, msg *C.char, ctx unsafe.Pointer) {\n  // convert and dispatch to Go callback via handle\n}\n```\n\n## Follow-up Questions\n- How would you handle backpressure if events flood in?\n- How to benchmark throughput and latency of callbacks across CGO?",
        "difficulty": "intermediate"
      },
      {
        "id": "cgoa-q-18",
        "questionId": "q-28012",
        "question": "Design a CGOA wrapper for a C function that streams data blocks and reports per-block CRC32 via a progress callback, while returning a final batch JSON. The C API is: int stream_blocks(const uint8_t** blocks, size_t n, size_t block_len, void (*progress)(size_t idx, uint32_t crc, void* ctx), void* ctx, char** out_json); out_json is malloc'ed JSON array of { \"idx\": i, \"crc\": \"<hex>\", \"ok\": true/false }. Include: 1) a header and a minimal C impl that wires the callback; 2) a Go binding exposing StreamBlocks(blocks [][]byte, ctx context.Context) (string, error) that ensures memory ownership and safe callback bridging; and 3) a Go program that streams blocks via a worker pool, aggregates a final batch CRC over per-block CRCs, and demonstrates cancellation handling and error propagation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Register per-task Go callbacks in a mutex-protected map[int]func(int,string). Create a Go–C bridge: assign a unique task handle, store the Go closure, pass the handle to C as an int with a static C progress callback. Ex…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Bridge CGO callbacks by exporting a C trampoline that calls back into Go using a stable handle. Pass a Go context or cancel channel as the ctx. On cancel, signal C wrapper via an atomic flag to abort.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Describe implementing a CGOA wrapper that binds a C batch-encrypt API and returns a malloc'ed JSON of per-block ciphertext and tag. Provide: (1) C header and a minimal impl using a mock accelerator; (",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Call get_item and check for NULL; copy label via C.GoString; assemble a Go Item{ID: int(it->id), Label: C.GoString(it->label)}; free the label and the item (via a C helper) to avoid leaks; return erro",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nInterviews probe safe CGO callback design, memory ownership, and cancellation semantics when Go and C share state in long-running tasks. This question enforces careful lifetime management and thread-safety in both directions.\n\n## Key Concepts\n- CGO callbacks and trampoline patterns\n- Memory ownership across C/Go boundaries\n- Cancellation signaling and context propagation\n- Concurrency via worker pools and final aggregation\n\n## Code Example\n```c\n// header\nint stream_blocks(const uint8_t** blocks, size_t n, size_t block_len, void (*progress)(size_t idx, uint32_t crc, void* ctx), void* ctx, char** out_json);\n```\n```c\n// minimal C impl: wires callback and returns malloc'ed JSON in out_json\n```\n```go\n// go binding: StreamBlocks(blocks [][]byte, ctx context.Context) (string, error)\n```\n```go\n// Go program: worker pool streaming and final CRC aggregation\n```\n\n## Follow-up Questions\n- How would you test CGO callbacks under goroutine preemption?\n- How would you handle C errors that don’t call back before returning?",
        "difficulty": "advanced"
      },
      {
        "id": "cgoa-q-19",
        "questionId": "q-28825",
        "question": "Design a CGOA wrapper for a C function that merges a stream of JSON fragments into a single JSON array using a streaming C API. The C API is: int merge_json_fragments(const char** fragments, size_t n, char** out_json); fragments are null-terminated strings; out_json is malloc'ed and must be freed by caller. Provide: 1) a C header and a minimal C implementation; 2) a Go binding exposing MergeJsonFragments(fragments []string) (string, error) via CGO; and 3) a Go program that streams fragments through a worker pool, collects the merged result and per-fragment metadata (e.g., index, fragment_len), with proper memory ownership and error propagation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: expose a CGO wrapper that passes a C array of const char* and handles memory ownership by freeing out_json with free(). Implement a C header with: int merge_json_fragments(const char** fragm",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a CGOA wrapper for a C streaming API that calls back with chunks and an is_final flag. The Go binding should collect chunks into a per-call buffer and return the combined result in ProcessDa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Provide the C header greet.h with: char* greet(const char* name); void free_string(char* s); Then implement a C source that allocates via malloc and returns the string. In Go, use // #include \\\"greet.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a C header rng.h with seed_rng(uint64_t) and next_rand(void). Provide a minimal rng.c using a thread-safe xorshift128+ RNG with static state. Go binding: NextRandom() (uint64, error) that ca",
            "isCorrect": false
          }
        ],
        "explanation": "This question tests: memory ownership transfer across CGO boundaries, correct use of malloc/free, and designing a streaming Go worker pool that coordinates with a C function via CGO. It also checks error propagation when C returns non-zero, and how to marshal Go slices to C arrays safely without leaks.",
        "difficulty": "intermediate"
      },
      {
        "id": "cgoa-q-20",
        "questionId": "q-22317",
        "question": "Design a CGOA wrapper for a C library that streams live market data blocks per symbol via a callback. The C API exposes: typedef void (*block_cb)(const void* data, size_t len, void* user); int start_market_stream(const char* symbol, block_cb cb, void* user); void stop_market_stream(void); char* get_summary_json(void); The wrapper must provide: 1) a header and minimal C impl that passes blocks to the Go callback and returns a malloc-allocated summary JSON; memory ownership clear; 2) a Go binding exposing StreamMarket(symbol string) (<-chan []byte, <-chan error) via CGO; 3) a Go program that runs a fixed worker pool subscribing to multiple symbols, processes blocks, propagates errors, and ensures proper shutdown and summary JSON cleanup?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "ProcessBatch calls the C function, checks the return code, copies the C-allocated array into a Go slice, and frees the C buffer. It must handle malloc failures gracefully, avoid sharing C pointers acr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A strong answer outlines: use //export to route progress_cb to Go, map per-call state with a cgo.Handle, convert C messages with C.GoString and free C buffers, spawn a Go goroutine to forward events i",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Safely bridge C and Go by copying each chunk, exporting a C callback (//export) and running the stream on its own thread; use a Go channel per stream and a cancel function to signal termination. Manag",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bind the C callback into Go by using a uintptr or Go-side handle, copy each incoming block into a Go slice in the callback, and send it on a channel. Ensure the C blocks are not freed until after the",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests CGO callback bridging, memory ownership, and cross-language lifecycle management for streaming data. Ensures correct data-copying semantics, error propagation, and clean shutdown in a production-like scenario.\n\n## Key Concepts\n- CGO callback integration and Go <-> C ownership\n- Copy-on-receive to avoid dangling pointers\n- Synchronization between producer (C) and consumer (Go)\n- Cleanup of C-allocated resources from Go\n\n## Code Example\n```javascript\n// market_stream.h\ntypedef void (*block_cb)(const void* data, size_t len, void* user);\nint start_market_stream(const char* symbol, block_cb cb, void* user);\nvoid stop_market_stream(void);\nchar* get_summary_json(void);\n```\n```javascript\n// Minimal C wrapper (market_stream.c)\n#include \"market_stream.h\"\n#include <stdlib.h>\n#include <string.h>\nstatic void bridge_cb(const void* data, size_t len, void* user){\n  // Copy data for Go consumption; actual transfer to Go would be via a registered callback\n  unsigned char* copy = (unsigned char*)malloc(len);\n  memcpy(copy, data, len);\n  // In real impl, enqueue copy to a Go channel via cgo.Handle or similar\n  (void)copy;\n}\nint start_market_stream(const char* symbol, block_cb cb, void* user){\n  // Start streaming and call cb for each block (omitted for brevity)\n  (void)symbol; (void)cb; (void)user;\n  return 0;\n}\nvoid stop_market_stream(void){}\nchar* get_summary_json(void){ return NULL; }\n```\n```javascript\n// Go binding sketch (market_stream.go)\npackage market\n/*\n#include \"market_stream.h\"\n*/\nimport \"C\"\nimport (\n  \"unsafe\"\n)\n\n// StreamMarket starts a market data stream and returns channels (illustrative)\nfunc StreamMarket(symbol string) (<-chan []byte, <-chan error) {\n  ch := make(chan []byte)\n  errc := make(chan error, 1)\n  // Real CGO bridge would register a Go callback and push copies to ch\n  _ = unsafe.StringData // placeholder to avoid unused in real code\n  go func(){ close(ch); close(errc) }()\n  return ch, errc\n}\n```\n\n## Follow-up Questions\n- How would you handle backpressure if the C library emits blocks faster than Go can process?\n- How would you ensure that Stop() waits for in-flight blocks to be copied and that the JSON summary is always freed?",
        "difficulty": "intermediate"
      },
      {
        "id": "cgoa-q-21",
        "questionId": "q-20344",
        "question": "Design a CGOA wrapper for a C function that sorts an int array using a user-provided comparator callback. The C API is void cg_sort(int* data, size_t n, int (*cmp)(const int* a, const int* b, void* ctx), void* ctx); The comparator returns negative if a<b, zero if equal, positive if a>b. Expose Go: SortWithGoComparator(data []int, cmp func(a, b int) int) ([]int, error) that uses a trampoline to call the Go comparator from C, supports concurrent calls, and ensures memory safety (copying data to C, freeing, and returning a new Go slice). Include a header, a minimal C implementation, a Go binding, and a small program that sorts several slices in parallel?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a CGO trampoline with a Go comparator stored in a go:handle. Pass a pointer to a small C context that includes the handle and a cmp callback. In C, copy data to a temporary int array, call cg_sort",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement a CGO bridge that exports a Go callback and passes it as a C function pointer. On each chunk, the callback copies the data with C.GoBytes, pushes it to a Go channel, and frees the C",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "The ideal answer outlines: a C header and a minimal impl for process_tlv_batch validating TLV (type 1 byte, length 4 bytes, payload) and returning malloc'ed JSON; a Go binding that constructs the bloc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use //export to expose a Go callback to C. StartEventStream returns a channel and registers a C callback. The callback converts Event* to a Go struct using C.GoString, sends it on the channel, then fr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to bridge Go and C using CGO, including memory management, callbacks, and concurrency, at beginner-to-intermediate level.\n\n## Key Concepts\n- CGO trampolines and callbacks\n- Passing Go functions to C via a trampoline\n- go.Handle for safe Go-value passing\n- Memory ownership: copy into C buffers and back, free malloc\n\n## Code Example\n```c\n// header (cg_sort.h)\nvoid cg_sort(int* data, size_t n, int (*cmp)(const int* a, const int* b, void* ctx), void* ctx);\n```\n\n```go\n// binding.go\n// sketch: create handle, pass to C, callback bridge\n```\n```\n\n## Follow-up Questions\n- How would you extend to generic types or non-ints?\n- How to handle Go panics in the comparator without crashing C code?",
        "difficulty": "beginner"
      },
      {
        "id": "cgoa-q-22",
        "questionId": "q-1845",
        "question": "Implement a CGOA wrapper around a C function that returns a heap-allocated string: char* greet(const char* name). Provide a header with greet and void free_string(char*). Write a Go binding using CGO to call greet from multiple goroutines and free the result with free_string to avoid leaks. Include the header, a Go binding file, and a small Go program that calls greet(\"Alice\"), greet(\"Bob\"), greet(\"Carol\") concurrently and prints results. Include build steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Wrap resource_t with a Go struct { p *C.resource_t; mu sync.Mutex; closed bool }. NewResource calls resource_create(); Do(cmd string) uses a C string and calls resource_do; Close() calls resource_rele",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Bind batch_validate via CGO, pass Go slices to C as a const char*[] without extra copies, allocate out_json in C and convert to Go string, then free memory in Go. Use a worker pool to feed batches, co",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a cgo.Handle to store Go channels and export a C callback that routes progress updates back to Go. Pass the handle as user to stream_transform; in C call cb(percent, user) as progress. Allocate ou",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Provide the C header greet.h with: char* greet(const char* name); void free_string(char* s); Then implement a C source that allocates via malloc and returns the string. In Go, use // #include \\\"greet.",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests memory ownership across Go and C, and CGO interaction with heap-allocated strings.\n\n## Key Concepts\n- CGO string handling and C memory management\n- Ownership transfer and explicit frees\n- Concurrency safety when crossing language boundaries\n\n## Code Example\n```c\n// greet.h\nchar* greet(const char* name);\nvoid free_string(char* s);\n```\n```c\n// greet.c\n#include <stdlib.h>\n#include <string.h>\n#include \"greet.h\"\nchar* greet(const char* name){ const char* prefix = \"Hello, \"; size_t len = strlen(prefix) + strlen(name) + 2; char* out = (char*)malloc(len); if(!out) return NULL; strcpy(out, prefix); strcat(out, name); return out; }\n```\n```go\n// greet.go\npackage main\nimport \"C\"\nimport (\n  \"fmt\" \n  \"unsafe\"\n)\nfunc greetGo(name string) string {\n  cstr := C.CString(name)\n  defer C.free(unsafe.Pointer(cstr))\n  r := C.greet(cstr)\n  s := C.GoString(r)\n  C.free_string(r)\n  return s\n}\nfunc main() {\n  // spawn 3 goroutines and print\n  // simplified in actual implementation\n  _ = greetGo\n  fmt.Println(greetGo(\"Alice\"))\n  fmt.Println(greetGo(\"Bob\"))\n  fmt.Println(greetGo(\"Carol\"))\n}\n```\n\n## Follow-up Questions\n- How would you extend to support batch processing with a single allocation?\n- How to verify no memory leaks with Go's tests?",
        "difficulty": "beginner"
      },
      {
        "id": "cgoa-q-23",
        "questionId": "q-6838",
        "question": "CGOA wrapper: Given a C API that streams frames via a callback:\\n\\nvoid start_stream(void (*frame_cb)(const uint8_t* frame, size_t len, void* user), void* user);\\n\\nEach frame is allocated by the C side (malloc) and must be freed after copying to Go. Design a CGOA wrapper that exposes:\\n\\nfunc StartStream() (<-chan []byte, func())\\n\\n- The channel delivers copies of each frame; StartStream returns a cancellation function to stop streaming and free resources. Implement header, a minimal C implementation, the Go binding using //export to route callback to Go with a go.Handle mapping, and a small Go program that starts two streams in parallel and reports frame counts. Include build steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Solution: CGO Wrapper for Streaming C API This solution demonstrates how to use CGO to bind a C streaming API with callback-based data delivery. The Go binding converts the C callback into a Go function, routing each…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-library context with a Go callback stored via a handle (cgo.Handle). Export a Go function for C to call, enqueue events on a Go channel, and dispatch in a dedicated worker to preserve Go run",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use CGO with a Go-exported callback to forward each frame to a Go channel mapped by a per-stream handle. The C side allocates frames (malloc); Go copies len bytes into a new Go slice and calls free on",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a CGO bridge for a C event loop that calls a callback from a dedicated thread into Go. Do not pass Go pointers to C; implement a per-stream registry keyed by an opaque handle, use a thread-safe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests memory ownership across CGO, callback routing, and channel-based streaming under concurrency.\n\n## Key Concepts\n- CGO callbacks via //export\n- go.Handle mapping and thread-safety\n- Memory ownership: malloc vs free across Go boundary\n- Cancellation and resource lifecycle\n\n## Code Example\n```c\n// header: void start_stream(void (*frame_cb)(const uint8_t*, size_t, void*), void* user);\n```\n\n```go\n//go:export frameCallback\nfunc frameCallback(data *C.uint8_t, len C.size_t, user unsafe.Pointer) { /* copy to Go slice and send */ }\n```\n\n## Follow-up Questions\n- How would you handle backpressure if Go consumer lags?\n- How to avoid GC stalls due to large frame copies?",
        "difficulty": "advanced"
      },
      {
        "id": "cgoa-q-24",
        "questionId": "q-15142",
        "question": "Design a CGOA wrapper for a C library exposing void process_stream(const double* in, size_t n, void (*cb)(const char* json, size_t len, int status, void* ctx), void* ctx). Provide a Go binding StartStream(chunks) (<-chan string, <-chan error) that queues each chunk to C, uses a callback to deliver a malloc-allocated JSON back to Go, and frees memory after copy. Include a header, a minimal C implementation (JSON via malloc), and a Go main showing two chunks in parallel?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use runtime/cgo.Handle to pass a Go channel as a stable context into C. The C callback allocates JSON with malloc; the bridge copies it to a Go string, frees the C allocation with free, and pushes to",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create a Go type that holds the C pointer, call create_book, convert the C string with C.GoString, and free via free_book in Close or a runtime.SetFinalizer. Title uses book_title. Demonstrate two gor",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "In CGO you cannot pass Go function pointers to C; instead export a Go callback and route through a stable C shim. Use a Go handle (via cgohandle) to store the Go callback per stream, pass the opaque h",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bind the C callback into Go by using a uintptr or Go-side handle, copy each incoming block into a Go slice in the callback, and send it on a channel. Ensure the C blocks are not freed until after the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design safe cross-language async bridges, memory ownership, and robust error handling across CGO boundaries.\n\n## Key Concepts\n\n- CGO callbacks and bridge patterns\n- runtime/cgo.Handle for passing Go values to C safely\n- malloc/free memory ownership across language boundaries\n- concurrent streaming, per-chunk state, and cancellation\n\n## Code Example\n\n```javascript\n// Pseudo: bridging skeleton (Go + C)\n```\n\n## Follow-up Questions\n\n- How would you extend to multiple independent streams with per-stream backpressure?\n- How would you instrument for leak detection and race conditions in this wrapper?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-05-14T02:53:36.562Z",
    "version": 1
  }
]