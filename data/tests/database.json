[
  {
    "id": "test-database",
    "channelId": "database",
    "channelName": "Database",
    "title": "Database Knowledge Test",
    "description": "Test your understanding of Database with 25 questions.",
    "questions": [
      {
        "id": "database-q-0",
        "questionId": "q-14395",
        "question": "You're investigating a fact table 'web_sales' partitioned by monthly RANGE on sale_date. A query aggregates revenue by product_id for 2025-01-01 to 2025-03-31 and region='US', joining to 'products'. Explain how to use EXPLAIN ANALYZE to verify partition pruning, identify indicators pruning is failing, and propose concrete changes (partition design, predicates, or indexes) to enforce pruning and optimize plans; also cover maintenance (attaching/detaching partitions) with minimal downtime?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use tenant_id as the leading column in all indexes, implement partial indexes for tenant-specific queries, and create separate summary tables with materialized views for cross-tenant reporting to bala",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run EXPLAIN ANALYZE and confirm the plan only touches relevant partitions (PartitionPruning). If the plan scans all partitions, predicate on sale_date must be sargable and reference the partition key",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a materialized Top-10 in Redis: a sorted set top24:YYYYMMDD keyed by sku with score=views_last24h and a DynamoDB table regional_availability: {region, sku, available}. On every view/purchase",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use partial indexes for high-selectivity filters to reduce storage footprint, order composite indexes with the highest-cardinality column first (tenant_id), and create covering indexes to eliminate heap fetches. Balance…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical mastery of partitioning, explain plans, and maintenance under real workloads.\n\n## Key Concepts\n\n- Partition pruning and constraint exclusion\n- Explain Analyze plan shapes: Bitmap, Seq Scan, Index Scan\n- Subpartitioning strategies and predicate alignment\n\n## Code Example\n\n```javascript\n-- PostgreSQL example (SQL)\nCREATE TABLE web_sales (\n  id bigserial primary key,\n  sale_date date not null,\n  region text not null,\n  product_id int not null,\n  revenue numeric(12,2) not null\n) PARTITION BY RANGE (sale_date);\n\nCREATE TABLE web_sales_202501 PARTITION OF web_sales FOR VALUES FROM ('2025-01-01') TO ('2025-02-01');\n```\n\n## Follow-up Questions\n\n- How would you detect and alert prune regressions in production?\n- If most queries filter by region but not sale_date, how would you redesign partitions?",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-1",
        "questionId": "q-27834",
        "question": "Scenario: A billing system coordinates MongoDB (customer data), Redis (real-time usage counters), Cassandra (per-minute usage logs), and DynamoDB (invoices). At month-end, design a cross-store finalization to generate a single DynamoDB invoice per customer for the month, with line items stored in MongoDB, a reset of Redis counters, and an immutable audit record in Cassandra. Include schemas, a concrete write path, idempotent guarantees, and a practical test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To model and flow: Redis key stock:<productId> -> {qty, warehouse}, TTL 300s. MongoDB: products collection with productId, name, price, qty. Cassandra: table stock_events (product_id, ts, delta). Dyna",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Freeze monthly usage by running a Redis Lua script that atomically moves counters to a frozen bucket keyed by customerId:month. Retrieve per-minute usage logs from Cassandra, aggregate them, and write a DynamoDB invoice…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Partition the table using a rolling 90-day range on sale_date and bucket by region; cluster by (region, channel, sale_date). This strategy enables effective partition pruning for the 90-day date window while maintaining…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition the events table by month using range/interval partitions to enable pruning for the last 6 months. Create a composite index on (user_id, event_ts, event_type) to support both the filter conditions and aggregat…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis scenario evaluates your ability to design and reason about cross-store finalization patterns, idempotence guarantees, and failure recovery in a realistic billing workflow spanning Redis, Cassandra, MongoDB, and DynamoDB.\n\n## Key Concepts\n\n- Atomic operations in Redis via Lua scripting to ensure counter consistency\n- Deterministic composite keys and upserts in DynamoDB for idempotent invoice writes\n- Immutable audit records in Cassandra for compliance and replay capability\n- Multi-store read-after-write consistency decisions\n- Replay-safe test design for distributed billing workflows\n\n## Code Example\n\n```javascript\nasync function finalizeInvoice(customerId, month) {\n  // Acquire distributed lock in Redis\n  // Atomically move counters to frozen bucket using Lua script\n  // Query Cassandra for aggregated usage logs\n  // Upsert invoice to DynamoDB with deterministic ID\n  // Store line items in MongoDB linked by invoiceId\n  // Write immutable audit record to Cassandra\n  // Reset active Redis counters\n}\n```\n\n## Idempotent Guarantees\n\nUse composite keys (customerId:YYYY-MM) for DynamoDB upserts and check-if-not-exists patterns before writing. The distributed lock prevents concurrent finalizations for the same customer-month.",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-2",
        "questionId": "q-6323",
        "question": "Scenario: A fleet of IoT devices streams telemetry to Redis, MongoDB, Cassandra, and DynamoDB. Design a concrete data model and write path for a new telemetry payload: specify concrete key names, document structures, TTL settings, and how data moves between stores when a device comes online, sends data, and reconnects after downtime. Include dedup, ordering, and eventual consistency basics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Telemetry arrives: Redis: update last_seen for device_id with TTL 7d. MongoDB: upsert device profile; insert telemetry into a telemetry_24h collection with TTL 24h. Cassandra: append to telemetry_by_d",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a cross-city wallet transfer under serializable isolation with multi-shard coordination. Use the following schema: accounts(wallet_id, city, balance) and transactions_log(tx_id, from_wallet, to_wallet, amount, ti…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a composite B-tree index on (customerId, status, createdAt DESC) for the first query, and a BRIN index on createdAt for the time-series aggregation query, while considering the write overhead implications of each…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition events by day with range partitions; if last two weeks are typical, switch to weekly partitions for pruning. Create a multicolumn B-tree index on (region, device_type, event_date) and a BRIN",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests cross-store data modeling, TTL interplay, and simple write paths in a realistic IoT telemetry pipeline across Redis, MongoDB, Cassandra, and DynamoDB.\n\n## Key Concepts\n\n- TTL indexes and per-item TTL in MongoDB, DynamoDB\n- Redis expirations and live state\n- Cassandra append-only writes and read patterns\n- Data flow and eventual consistency across stores\n\n## Code Example\n\n```javascript\n// Sketch of write path for incoming telemetry\nasync function handleTelemetry(deviceId, payload){\n  const ts = Date.now();\n  // Redis: live state\n  await redis.hset(`device:${deviceId}`, 'last_seen', ts);\n  await redis.expire(`device:${deviceId}`, 7*24*60*60);\n  // MongoDB: profile + 24h telemetry window\n  await devices.updateOne({device_id: deviceId}, {$set: {last_seen: ts}});\n  await telemetry24h.insertOne({device_id: deviceId, ts, payload, id: payload.id});\n  // Cassandra: full telemetry stream (append-only)\n  await cassandra.execute('INSERT INTO telemetry_by_device (device_id, ts, payload) VALUES (?, ?, ?)', [deviceId, ts, payload]);\n  // DynamoDB: archive with TTL\n  await dynamo.putItem({TableName:'telemetry_archive', Item: {\n    device_id: {S: deviceId}, ts: {N: String(ts)}, payload: {S: JSON.stringify(payload)}, ttl: {N: String(Math.floor((Date.now()+30*86400000)/1000))}\n  }});\n}\n```\n\n## Follow-up Questions\n\n- How would you test TTL expiration and cross-store consistency end-to-end?\n- How would you handle out-of-order telemetry or late arrivals while preserving an accurate last_seen in Redis?",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-3",
        "questionId": "q-13370",
        "question": "Design a cross-store write path for a fintech fraud alert pipeline using Redis (recent alerts cache), MongoDB (customer profiles), Cassandra (transaction logs), and DynamoDB (immutable audit logs). When a transaction is flagged as suspicious, specify concrete keys, TTLs, an idempotent write workflow, cross-store conflict handling, and a read path to reconstruct the current alert with full context?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a 2PC-like cross-region protocol. Each shard runs a SERIALIZABLE transaction: update region_totals and prepare an insert into central_ledger, returning a prepare id. A lightweight coordinator pr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design composite and covering indexes for a mixed read-write analytics workload, balancing query performance against index maintenance overhead.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition pruning is triggered when a query includes predicates on the partition key (`event_day`) that PostgreSQL can evaluate at planning time. For RANGE partitions, this typically involves simple comparison operators…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single idempotent request_id to drive the workflow: Redis: SET alert:{req_id} with payload, TTL 3600; MongoDB: upsert customer profile to include alert ref; Cassandra: append per-customer transa",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-store consistency, idempotency, and failure handling in a multi-datastore fraud workflow. Emphasizes concrete keys, TTLs, and read-back guarantees.\n\n## Key Concepts\n- Idempotent writes across Redis, MongoDB, Cassandra, DynamoDB\n- TTL management for cache freshness\n- Cross-store conflict resolution and compensating actions\n- Read-path reconstruction from multiple sources\n\n## Code Example\n```javascript\nasync function placeFraudAlert(reqId, payload){\n  // Redis: cache with short TTL\n  await redis.set(`alert:${reqId}`, JSON.stringify(payload), 'EX', 3600);\n\n  // MongoDB: embed alert reference in customer profile\n  await mongo.updateOne({customerId: payload.customerId},\n    { $push: { alerts: { id: reqId, ...payload } } }, { upsert: true });\n\n  // Cassandra: append per-customer transaction event\n  await cassandra.execute(`INSERT INTO alerts_by_customer (customer_id, ts, req_id, details) VALUES (?, toTimeuuid(now()), ?, ?)`,\n    [payload.customerId, reqId, payload]);\n\n  // DynamoDB: immutable audit log\n  await dynamo.putItem({\n    TableName: 'Audit',\n    Item: { PK: { S: `ALERT#${reqId}` },\n            ...payloadToDynamoItem(payload) }\n  });\n}\n```\n\n## Follow-up Questions\n- How would you test race conditions between stores during high load?\n- How would you handle historical backfills if Redis data was evicted earlier than TTL?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-4",
        "questionId": "q-17580",
        "question": "In a PostgreSQL e-commerce database with tables for orders (10M rows), order_items (50M rows), and products (500K rows), you notice slow queries for 'Find all orders from the last 30 days containing products from category X, sorted by total order value, limited to 100 results'. The current execution time is 8 seconds. Walk through your approach to analyze and optimize this query through indexing strategies. Discuss index types you'd consider (B-tree, GIN, BRIN, etc.), the trade-offs of each, how you'd order columns in composite indexes, and any application-level optimizations you'd recommend.",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To avoid inventory oversell, use MVCC with SERIALIZABLE or a FOR UPDATE lock. T1 reads stock, then inserts cart line; T2 checks out and decrements stock. With READ COMMITTED, phantom reads could allow",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a SERIALIZABLE transaction to debit A and credit B in a single commit, locking both rows with SELECT FOR UPDATE and writing to a central ledger. Validate balance before debit; ensure idempotence b",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use MVCC with READ COMMITTED isolation, implement optimistic locking with version columns, and handle conflicts with retry logic.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "First, I'd analyze the query execution plan using EXPLAIN ANALYZE to identify bottlenecks such as sequential scans on orders and order_items or inefficient joins. Then, I'd create B-tree composite indexes with proper co…",
            "isCorrect": true
          }
        ],
        "explanation": "First, I'd run EXPLAIN ANALYZE to identify the bottleneck—likely sequential scans on orders and order_items along with inefficient joins. For this query, a composite index on orders(order_date DESC, total_value DESC) would help with date filtering and sorting. For order_items, a composite index on (order_id, product_id) combined with a partial index on products(category_id) would accelerate the join. If category filtering is frequent, a GIN index on an array of category_ids stored with each order could eliminate the join to products entirely. BRIN indexes might work well for the time-series nature of the orders table, offering a smaller footprint at the cost of less precise indexing. Additionally, I'd consider application-level optimizations such as implementing result caching, using pagination with keyset rather than offset-based limiting, and pre-aggregating data in materialized views if this query pattern runs frequently during reporting periods.",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-5",
        "questionId": "q-18164",
        "question": "In a high-throughput wallet service using PostgreSQL with MVCC, design a transfer(from_id, to_id, amount) pattern that guarantees no overdraw and avoids lost updates under Serializable isolation. Include: 1) schema design for accounts and transfers with idempotency, 2) concrete transaction steps using FOR UPDATE and retry on serialization failure, 3) Redis cache invalidation strategy, and 4) a test plan for concurrent transfers and replay safe retries?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Index design: B-Tree on ts stores leaves as (ts, row_id); hash on user_id maps to row_ids; composite (user_id, ts) uses key (user_id, ts) with row_id. Inserts update all three. a) use composite (42, [",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Serializable transaction in PostgreSQL. Schema: accounts(id, balance), transfers(id, from_id, to_id, amount, txid, status). Pattern: BEGIN; SELECT balance FROM accounts WHERE id IN (from_id, to_",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Write a query joining trips to regions, filter by pickup_time >= now() - interval '24 hours', and group by region name to compute counts, sums, and averages. Index suggestions: trips(pickup_time), trips(region_id), or a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement the transfer as a single transaction that locks both accounts and updates balances atomically. When two concurrent transfers A→B and A←B execute between the same accounts, Read Committed isolation can encounte…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests mastery of MVCC, Serializable isolation, and practical patterns for correctness under high concurrency. It also probes idempotency, cross-store cache patterns, and resilience to serialization failures.\n\n## Key Concepts\n- Serializable isolation and retry on serialization failures (POSTGRES 40001)\n- FOR UPDATE locking on involved rows\n- Idempotent transfers via txid and dedup\n- Cache invalidation strategy with Redis (cache-aside)\n- Testing under contention and replay scenarios\n\n## Code Example\n```sql\n-- schema\nCREATE TABLE accounts (id BIGINT PRIMARY KEY, balance BIGINT NOT NULL);\nCREATE TABLE transfers (\n  id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  from_id BIGINT NOT NULL,\n  to_id BIGINT NOT NULL,\n  amount BIGINT NOT NULL,\n  txid TEXT NOT NULL UNIQUE,\n  status TEXT NOT NULL,\n  created_at TIMESTAMP WITHOUT TIME ZONE DEFAULT now()\n);\n\n-- transfer flow (simplified)\nBEGIN;\nSELECT balance FROM accounts WHERE id IN (from_id, to_id) FOR UPDATE;\nIF balance_from < amount THEN ROLLBACK; ELSE\n  UPDATE accounts SET balance = balance - amount WHERE id = from_id;\n  UPDATE accounts SET balance = balance + amount WHERE id = to_id;\n  INSERT INTO transfers (from_id, to_id, amount, txid, status) VALUES (..., 'committed');\n  COMMIT;\nEND;\n```\n\n## Follow-up Questions\n- How would you test for phantom reads in this setup?\n- How would you evolve this to multi-region deployments?",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-6",
        "questionId": "q-6500",
        "question": "Design a high-concurrency funds-transfer operation in PostgreSQL that moves money between two accounts while preserving ACID semantics; discuss preferred isolation level, how MVCC avoids anomalies such as write skew, and provide a concrete transaction pattern using FOR UPDATE locks, balance checks, and an insert into a transfers log. Include a testing strategy with concurrent clients for overdraft scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "B-tree indexes support range queries and ordered data access, while hash indexes only support exact equality lookups. Choose B-tree for range queries and sorting, hash for exact match operations.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a composite B-tree index on (user_id, event_type, created_at) as primary, with a partial index for recent data. For schema changes, use pg_repack or CONCURRENTLY to avoid blocking.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Shard MongoDB by a hashed region+productId to spread writes; add indexes on (productId, createdAt) for time-bounded queries and (region, rating) for feeds. Redis as cache with keys reviews:region:prod",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use SERIALIZABLE isolation, lock both accounts with SELECT ... FOR UPDATE in a single transaction, check balance before debit, then credit and log transfer. To avoid deadlocks, always lock accounts in",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes practical ACID reasoning and deadlock/anomaly handling under MVCC in a money-transfer workload.\n\n## Key Concepts\n- ACID, MVCC, write skew, phantom reads\n- SERIALIZABLE vs READ COMMITTED\n- FOR UPDATE locking and deadlock avoidance\n- idempotent/atomic logging of transfers\n\n## Code Example\n```javascript\nasync function transfer(pgClient, fromId, toId, amount) {\n  await pgClient.query('BEGIN;');\n  await pgClient.query('SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;');\n  // lock both accounts\n  const resFrom = await pgClient.query('SELECT balance FROM accounts WHERE id=$1 FOR UPDATE', [fromId]);\n  const resTo   = await pgClient.query('SELECT balance FROM accounts WHERE id=$1 FOR UPDATE', [toId]);\n  if (resFrom.rows[0].balance < amount) throw new Error('Insufficient funds');\n  await pgClient.query('UPDATE accounts SET balance = balance - $1 WHERE id = $2', [amount, fromId]);\n  await pgClient.query('UPDATE accounts SET balance = balance + $1 WHERE id = $2', [amount, toId]);\n  await pgClient.query('INSERT INTO transfers (from_account,to_account,amount,status) VALUES ($1,$2,$3,\\'completed\\')', [fromId,toId,amount]);\n  await pgClient.query('COMMIT');\n}\n```\n\n## Follow-up Questions\n- How would you test for deadlocks and starvation under 1000+ concurrent transfers?\n- If transfers cross regions, how would you ensure cross-db consistency?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-7",
        "questionId": "q-24753",
        "question": "Given a PostgreSQL analytics schema with tables: tenants(id), users(id, tenant_id), events(id, user_id, tenant_id, event_type, created_at, payload). For a specific tenant_id and date range, design a query to return per-user counts by event_type, and outline an indexing/partitioning strategy to keep responses under sub-second latency for up to 1M events?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To ensure ACID across transfers, use SERIALIZABLE; lock both rows with SELECT ... FOR UPDATE within one transaction, check balance >= amount, then UPDATE balances and INSERT a movement; commit. If ins",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Composite BTREE index on (user_id, event_type, created_at) for efficient per-user, per-type range scans; HASH index on (event_type) for rapid equality lookups and aggregations; partial BTREE index on created_at WHERE cr…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Schema: tenants(id), users(id, tenant_id), events(id, user_id, tenant_id, event_type, created_at, payload). For a tenant and date window, return per-user counts by event_type. Optimized query: SELECT",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a multi-store write for an edit as follows: MongoDB stores the latest document {docId, version, content, updatedAt}; Redis stores live cursors as {docId:userId} with TTL; Cassandra appends edit events {docId,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical joins across normalized tables, and a concrete plan for indexing and partitioning to meet latency targets in a real analytics workload.\n\n## Key Concepts\n- Joins across normalized tables; composite indexes; range partitioning; index-only scans; MVCC considerations.\n\n## Code Example\n```sql\n-- DDL sketches (simplified)\nCREATE TABLE tenants (id BIGINT PRIMARY KEY);\nCREATE TABLE users (id BIGINT PRIMARY KEY, tenant_id BIGINT REFERENCES tenants(id));\nCREATE TABLE events (id BIGINT PRIMARY KEY, user_id BIGINT REFERENCES users(id), tenant_id BIGINT, event_type TEXT, created_at TIMESTAMPTZ, payload JSONB);\n\n-- Optimized query (example)\nSELECT u.id AS user_id, e.event_type, COUNT(*) AS cnt\nFROM events e JOIN users u ON e.user_id = u.id\nWHERE e.tenant_id = $1 AND e.created_at >= $2 AND e.created_at < $3\nGROUP BY u.id, e.event_type\nORDER BY cnt DESC;\n```\n\n## Follow-up Questions\n- How would you validate partition pruning is active in plans?\n- How to handle skew/seasonality across partitions?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-8",
        "questionId": "q-365",
        "question": "You're designing a real-time analytics system for Discord that processes millions of message events per minute. Your PostgreSQL database is experiencing severe write contention on the message_events table. How would you design a partitioning strategy using declarative partitioning, and what specific index optimizations would you implement to handle both time-series queries and user-based lookups efficiently?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement declarative partitioning with time-based partitions (hourly for high-traffic periods, daily for standard load) on the message_events table. Create BRIN indexes on timestamp columns for efficient range queries,…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a single serializable transaction to move funds atomically. Schema: accounts(id PK, balance, version); transfers(id PK, from_id, to_id, amount, ts, status). Flow: BEGIN; SELECT balance FROM accoun",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition events by day with range partitions; if last two weeks are typical, switch to weekly partitions for pruning. Create a multicolumn B-tree index on (region, device_type, event_date) and a BRIN",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Best plan uses the composite index on (event_type, event_time): equality on event_type plus the time-range; enables index scan with ordering by event_time. The hash index on event_type is not helpful",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDiscord processes millions of message events per minute, making database write contention a critical scalability challenge. This question assesses understanding of PostgreSQL's advanced partitioning capabilities and index optimization strategies for high-throughput, time-series workloads.\n\n## Expected Answer\nStrong candidates will discuss:\n- Declarative partitioning strategy (hourly partitions during peak load, daily partitions for normal periods)\n- BRIN indexes on timestamp columns for space-efficient range scans\n- B-tree indexes on user_id for rapid user-specific queries\n- Composite (user_id, timestamp) indexes for common analytical query patterns\n- Partition pruning and constraint exclusion optimization\n- Automated partition creation and maintenance workflows\n\n## Code Example\n```sql\nCREATE TABLE message_events (\n    id BIGSERIAL,\n    user_id BIGINT NOT NULL,\n    event_type VARCHAR(50),\n    payload JSONB,\n    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()\n) PARTITION BY RANGE (created_at);\n\n-- Create hourly partitions for recent data\nCREATE TABLE message_events_2024_01_01_h00 PARTITION OF message_events\n    FOR VALUES FROM ('2024-01-01 00:00:00') TO ('2024-01-01 01:00:00');\n\n-- Indexes\nCREATE INDEX CONCURRENTLY idx_message_events_user_id ON message_events (user_id);\nCREATE INDEX CONCURRENTLY idx_message_events_timestamp_brin ON message_events USING BRIN (created_at);\nCREATE INDEX CONCURRENTLY idx_message_events_user_time ON message_events (user_id, created_at);\n```",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-9",
        "questionId": "q-11054",
        "question": "Scenario: An online messaging platform uses Redis for sessions and device tokens, MongoDB for user profiles, Cassandra for event streams, and DynamoDB for per-user notification channels with versioned policies. You need to roll out a targeted campaign update that changes each user's preferred channel. Design a cross-store write path with idempotency, TTLs, and cache invalidation. Include key schemas, write order, and a read path for current effective channel; explain rollback for partial failures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a composite B-tree index on (user_id, created_at DESC) to fetch a user’s orders by date, a hash index on (status) for fast equality lookups, and an optional B-tree on (created_at) for daily re",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an idempotent, versioned cross-store write path: 1) Write to DynamoDB using a composite key (campaign_id:user_id:version) with the new channel preference and version number; 2) Update Redis per-user current_ch…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a partial index on (customer_id, created_at) filtering on status='pending', and create a supporting index on customers(tier) for the subquery. Validate with EXPLAIN ANALYZE to confirm index-only scans are being used…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Composite B-tree index on (tenant_id, category, price) enables per-tenant category-range scans; a hash index on (tenant_id, product_id) provides fast tenant lookups; a partial index on (tenant_id, act",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-store consistency, TTL-based cache invalidation, and idempotent multi-store write patterns. It evaluates understanding of write ordering, conflict resolution, and rollback strategies in a heterogeneous technology stack.\n\n## Key Concepts\n- Cross-store transactional boundaries and idempotency patterns\n- Cache invalidation coordination between Redis and DynamoDB with TTL management\n- Data flow design across Redis, MongoDB, Cassandra, and DynamoDB with appropriate schemas\n\n## Code Example\n```javascript\n// Pseudo-code: idempotent write across stores\nconst key = `${campaignId}:${userId}:v${version}`;\ndynamo.put({PK: campaignUserKey, SK: version, channel,",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-10",
        "questionId": "q-20429",
        "question": "Sensor readings stored in MongoDB as { _id, deviceId, location, timestamp, metrics: {temp, humidity, pressure}, tags: [string] }. Design an indexing strategy to (1) fetch latest 15 minutes per device, (2) filter by location and a tag value, (3) support range queries on deviceId with timestamp. Propose concrete index definitions (B-tree composite and multikey), shard-key considerations (hashed on deviceId), and explain query shapes and monitoring of index usage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Prune partitions with a trailing 12-month sale_date filter, join to regions, and aggregate by region. Put an index on (region_id, sale_date) in each partition and rely on constraint exclusion. Verify",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use the existing (user_id, created_at) composite index for user timeline queries, add a separate (event_type, created_at) composite index for analytics queries, and consider a (user_id, event_type, created_at) composite…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest fraud signals with a dedup key {region}:{signalId}, write to DynamoDB alerts with TTL 24h, stream to Cassandra audit logs, update MongoDB user profile with lastFraudAt, and cache a lightweight",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a compound index { deviceId: 1, timestamp: -1 } to efficiently fetch the most recent records for a device and support 15-minute windows. Add a multikey index { location: 1, tags: 1, timestamp:",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests composite indexing for time-series workloads, multikey handling of arrays (tags), and shard-key design for write-heavy workloads.\n\n## Key Concepts\n\n- Composite B-tree for per-device recent reads\n- Multikey compound for array fields\n- Hashed shard key for even data distribution\n- Trade-offs: index size vs update cost; consider TTL if needed later\n\n## Code Example\n\n```javascript\ndb.readings.createIndex({ deviceId: 1, timestamp: -1 }, { name: \"device_latest_15m\" })\ndb.readings.createIndex({ location: 1, tags: 1, timestamp: -1 })\ndb.readings.createIndex({ deviceId: \"hashed\" })\n```\n\n## Follow-up Questions\n\n- How would you adjust if tags array is extremely large?\n- How would you verify index usage in production-like workloads (explain, profiler, metrics)?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-11",
        "questionId": "q-17140",
        "question": "In a partitioned table/collection with monthly partitions (Oracle) or sharded/partitioned (MongoDB), a query filtering by a date range and status runs slowly. How would you use EXPLAIN PLAN (or explain()) to verify partition pruning, and what concrete changes to partitioning or indexing would you propose to improve performance for a typical 3-month range query?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run EXPLAIN PLAN (Oracle) or db.collection.explain() (MongoDB) for the query and examine the execution plan. In Oracle, look for PARTITION RANGE ITERATOR or PARTITION RANGE SUBSTRING in the operation column, along with…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Run EXPLAIN PLAN FOR the query and inspect the plan tree for a Partition Selector showing only 2025 partitions; verify PRUNING. If not pruned, ensure order_date is the leading column in the index and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "MVCC creates multiple versions of data rows, allowing reads to proceed without blocking writes and preventing lost updates through version comparison.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Write a single query joining students to cohorts and programs, and enrollments to compute per-student completed_courses and avg_grade for a specified cohort year and program. Normalize via foreign key",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical ability to validate partition pruning using explain plans and understanding of how partitioning and indexing decisions impact query performance. It also assesses knowledge of aligning partition keys with query predicates across both Oracle and MongoDB environments.\n\n## Key Concepts\n\n- Explain plans and pruning indicators (PARTITION RANGE in Oracle, IXSCAN with bounds in MongoDB)\n- Partitioning strategy alignment with query predicates\n- Composite indexes vs. partition key adjustments\n- Trade-offs between partition granularity and maintenance overhead\n\n## Code Example\n\n```javascript\n// MongoDB: Check query execution plan\nconst plan = db.orders.explain('executionStats').find({\n  order_date: { $gte: ISODate('2024-01-01'), $lt: ISODate('2024-04-01') },\n  status: 'completed'\n}).execute()\n\n// Oracle: Explain plan for partition pruning\nEXPLAIN PLAN FOR\nSELECT * FROM orders \nWHERE order_date BETWEEN TO_DATE('2024-01-01') AND TO_DATE('2024-04-01')\n  AND status = 'completed';\n\nSELECT * FROM TABLE(DBMS_XPLAN.DISPLAY);\n```",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-12",
        "questionId": "q-21963",
        "question": "Scenario: A global marketplace uses Postgres with tables: sellers(id, region, created_at), users(id, city_id), cities(id, name), orders(id, seller_id, user_id, placed_at, status), order_items(id, order_id, product_id, quantity, unit_price), products(id, category, name), refunds(id, order_item_id, amount, refunded_at). For a given city name and month (YYYY-MM), return per-seller: total_revenue (sum of quantity*unit_price for paid orders in that month), total_orders, unique_buyers, avg_order_size (revenue/total_orders), and refund_rate (sum(refund_amount)/revenue). Provide the SQL and discuss index/normalization choices and how you’d scale with partitioning?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ensure partition pruning by bounding the partition key in WHERE (no function on date), then verify with EXPLAIN ANALYZE that only relevant monthly partitions are scanned. If pruning fails, redesign to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a three-way join (plans → subscriptions → invoices) filtered by issued_at in the month and an active window on subscriptions, then group by plan. Example: SELECT p.name, SUM(i.amount) AS total_inv",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Add a separate created_at index for analytics queries, consider partial indexes or BRIN indexes for time-series data, and implement index-only scans where possible.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single SQL with two CTEs: filter paid orders in the month for city via joins; second aggregate per seller_id computing revenue, orders, unique_buyers, refunds; apply COALESCE. Index strategy: pa",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical SQL composition across normalized tables, with multiple joins and aggregations, plus performance considerations for real-world scale.\n\n## Key Concepts\n- Joins across sellers, orders, order_items, refunds, users, cities\n- Aggregations: revenue, orders, unique_buyers, avg_order_size, refund_rate\n- Index design: composite and partial indexes; city lookups\n- Normalization trade-offs: avoiding data duplication vs. read efficiency\n- Partitioning: monthly partitions for large time-based queries\n\n## Code Example\n```javascript\nSELECT s.id AS seller_id,\n       SUM(oi.quantity * oi.unit_price) AS total_revenue,\n       COUNT(DISTINCT o.id) AS total_orders,\n       COUNT(DISTINCT o.user_id) AS unique_buyers,\n       SUM(oi.quantity * oi.unit_price) / NULLIF(COUNT(DISTINCT o.id), 0) AS avg_order_size,\n       SUM(COALESCE(r.amount, 0)) / NULLIF(SUM(oi.quantity * oi.unit_price), 0) AS refund_rate\nFROM sellers s\nJOIN orders o ON o.seller_id = s.id\nJOIN order_items oi ON oi.order_id = o.id\nLEFT JOIN refunds r ON r.order_item_id = oi.id\nJOIN users u ON u.id = o.user_id\nJOIN cities c ON c.id = u.city_id\nWHERE c.name = 'CityName'\n  AND date_trunc('month', o.placed_at) = DATE 'YYYY-MM-01'\nGROUP BY s.id;\n```\n\n## Follow-up Questions\n- How would you adjust indexes if city names are not unique? \n- How would you validate the plan across shards or with incremental refresh for a dashboard?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-13",
        "questionId": "q-16380",
        "question": "You're working with a large e-commerce database with a 'orders' table containing millions of rows. Queries filtering by customer_id and date_range are running slowly despite having separate indexes on both columns. What indexing strategy would you use to optimize these queries, and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a composite index with customer_id as the leading column followed by order_date. Place the most selective column first, and order columns so that equality conditions precede range conditions.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a global per-tenant API rate limiter using Redis as the hot cache, MongoDB for tenant configuration, Cassandra for event logs, and DynamoDB for daily summaries. Implement a token-bucket rate limiter with explicit…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed: composite B-tree index on (region, signup_year) INCLUDE (user_id, last_login, status) to cover range scans and point lookups. Skip the separate hash index on user_id since B-tree already handles point lookups…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "SQL plan (category and top products): SELECT pr.category AS category, SUM(oi.quantity*oi.unit_price) AS revenue FROM orders ord JOIN order_items oi ON oi.order_id = ord.id JOIN products pr ON oi.produ",
            "isCorrect": false
          }
        ],
        "explanation": "When separate indexes exist on customer_id and order_date, the database can only use one index effectively for a query that filters on both columns. A composite (multi-column) index solves this by creating a single sorted structure that supports both equality (customer_id = X) and range (order_date BETWEEN ...) conditions efficiently.\n\nThe key principle is column order: place equality conditions first (customer_id), then range conditions (order_date). This allows the index to immediately narrow results to a specific customer before scanning the relevant date range within that subset. Because the index entries are sorted by customer_id and then by order_date, the database can perform a tight index range scan rather than scanning large portions of the table.\n\nAdditionally, consider selectivity — customer_id typically has high cardinality in a large e-commerce database, meaning it filters out the vast majority of rows early. Placing it as the leading column maximizes the index's ability to prune irrelevant data. This composite indexing strategy minimizes disk I/O and dramatically improves query performance compared to relying on separate single-column indexes.",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-14",
        "questionId": "q-409",
        "question": "You're designing a database for an e-commerce platform with frequent queries on (user_id, order_date) and (product_id, category). How would you choose between B-tree and hash indexes, and what composite index strategy would optimize both query patterns?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use B-tree composite indexes: (user_id, order_date) for range queries and (product_id, category) for exact matches. Hash indexes are unsuitable for order_date range queries because they store data in hash buckets withou…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "SELECT c.name, SUM(oi.quantity * oi.unit_price) AS total_spent, MAX(o.placed_at) AS last_order_date FROM customers c JOIN orders o ON o.customer_id = c.id JOIN order_items oi ON oi.order_id = o.id WHE",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Redis for typing indicators: keys `typing:<conv_id>:<user_id>` with a 5s TTL; publish updates on `typing:<conv_id>` channel for real-time rendering. Record lightweight events in Cassandra for analytics.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a partial index on (customer_id, created_at) filtering on status='pending', and create a supporting index on customers(tier) for the subquery. Validate with EXPLAIN ANALYZE to confirm index-only scans are being used…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical index selection knowledge and understanding of real-world query optimization trade-offs that TCS engineers face daily.\n\n## Expected Answer\nStrong candidate explains B-tree handles range queries (order_date) and equality, while hash only supports equality. They'll recommend two composite indexes matching query patterns, discuss index ordering, and mention covering indexes to avoid table scans.\n\n## Index Selection Deep Dive\n**B-tree vs Hash for Range Queries:**\nHash indexes distribute values across buckets using hash functions, destroying any logical ordering. For queries like `WHERE order_date BETWEEN '2024-01-01' AND '2024-01-31'`, hash indexes must scan every bucket (O(n) complexity), while B-trees efficiently traverse adjacent leaf nodes (O(log n + k) complexity).\n\n**Composite Index Strategy:**\n- `(user_id, order_date)`: Enables efficient user order history with date filtering\n- `(product_id, category)`: Optimizes product category lookups and inventory queries\n\n**Covering Index Benefits:**\nA covering index like `(user_id, order_date, status, total)` can satisfy queries without accessing the table, reducing I/O by 60-80% for common reporting queries.\n\n## Code Example\n```sql\n-- Primary composite indexes\nCREATE INDEX idx_user_orders ON orders(user_id, order_date);\nCREATE INDEX idx_product_category ON products(product_id, category);\n\n-- Covering index for order summaries (avoids table lookups)\nCREATE INDEX idx_order_summary ON orders(user_id, order_date, status, total) \nINCLUDE (customer_name);\n\n-- Hash index would fail here (cannot optimize range):\n-- EXPLAIN ANALYZE SELECT * FROM orders \n-- WHERE order_date >= '2024-01-01' AND order_date <= '2024-01-31';\n```\n\n## Performance Impact\n- B-tree range queries: 10-100x faster than hash indexes\n- Index size: B-trees use 20-30% more storage but provide comprehensive optimization\n- Write overhead: B-trees have 15-25% slower inserts due to tree rebalancing\n\n## Follow-up Questions\n- How would you handle queries that only filter on the second column?\n- When would you consider a hash index instead?\n- How do you monitor index effectiveness in production?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-15",
        "questionId": "q-18569",
        "question": "You're optimizing a PostgreSQL workload with a 100B-row events table partitioned by RANGE (event_date) into monthly partitions. A 90-day window query aggregates counts by event_type and filters by user_id range. Current EXPLAIN ANALYZE shows scans of many partitions. Propose a concrete optimization plan: partitioning strategy, per-partition index design, pruning method, and a practical SQL rewrite plus expected EXPLAIN pattern. Include maintenance notes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: use monthly range partitions on event_date with constraint-based pruning; place a per-partition index on (user_id, event_type) and INCLUDE (event_date) as needed. Write the 90-day query with exp",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a single serializable transaction to move funds atomically. Schema: accounts(id PK, balance, version); transfers(id PK, from_id, to_id, amount, ts, status). Flow: BEGIN; SELECT balance FROM accoun",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a two‑phase commit (2PC) across shards: prepare phase locks and debits on A and credits on B, writing a durable prepare record; commit phase only if both prepares succeed, else abort both. Use",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Write path: Cassandra appends the notification per user_id; MongoDB stores user metadata; Redis updates unread_count and pushes the notification_id into a per-user list; DynamoDB updates a daily_diges",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of partition pruning, index design on partitioned tables, and how EXPLAIN ANALYZE reveals plan improvements in real workloads.\n\n## Key Concepts\n- Partition pruning and constraint exclusion\n- Per-partition indexes and INCLUDE columns\n- Partitionwise aggregation and parallelism\n- Maintenance: partition creation/retirement, vacuum, stats\n\n## Code Example\n```sql\nCREATE TABLE events (\n  id bigserial PRIMARY KEY,\n  user_id bigint NOT NULL,\n  event_type text NOT NULL,\n  event_date date NOT NULL,\n  payload jsonb\n) PARTITION BY RANGE (event_date);\n\nCREATE TABLE events_2026_03 PARTITION OF events FOR VALUES FROM ('2026-03-01') TO ('2026-04-01');\nCREATE INDEX ON events_2026_03 (user_id, event_type);\n```\n\n```sql\nSELECT event_type, COUNT(*)\nFROM events\nWHERE event_date >= '2026-03-01' AND event_date < '2026-06-01'\n  AND user_id BETWEEN 100000 AND 200000\nGROUP BY event_type;\n```\n\n## Follow-up Questions\n- How would you validate partition pruning in EXPLAIN ANALYZE?\n- What are trade-offs of global indexes vs per-partition indexes in this setup?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-16",
        "questionId": "q-27889",
        "question": "You have a PostgreSQL table events partitioned by month (range partitions) on created_at. A query filters created_at between 2026-01-01 and 2026-03-31 and status = 'active'. Explain how partition pruning works with this setup, what you expect from the query plan, and how you would verify with EXPLAIN ANALYZE that only relevant partitions are scanned. Then propose a minimal indexing/partitioning tweak to ensure efficient pruning and fast access for common status filters, including DDL snippets and a short test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "For Redis real-time caching, implement sorted sets (zset) for leaderboards with per-user session keys and TTL of 15-60 minutes based on data volatility. MongoDB should shard on user_id with compound indexes on (last_see…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Composite BTREE index on (user_id, event_type, created_at) for efficient per-user, per-type range scans; HASH index on (event_type) for rapid equality lookups and aggregations; partial BTREE index on created_at WHERE cr…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use inner joins to connect views to posts, then posts to users: v JOIN p ON p.id = v.post_id JOIN u ON u.id = p.author_id. Filter by u.region = 'EMEA' and v.viewed_at between date_trunc('month', current_date) - interval…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition pruning happens at plan time by pushing the created_at constraint into partition bounds; with monthly RANGE partitions, the planner prunes partitions outside the specified window. Verify with EXPLAIN ANALYZE t…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nUnderstanding partition pruning and explain plans is essential for real-world query performance at scale.\n\n## Key Concepts\n- Partition pruning\n- Explain plans and constraint exclusion\n- Composite indexes within partitions\n\n## Code Example\n\n```sql\nCREATE TABLE events (\n  id bigserial PRIMARY KEY,\n  created_at timestamptz NOT NULL,\n  status text NOT NULL\n) PARTITION BY RANGE (created_at);\n\nCREATE TABLE events_2026_01 PARTITION OF events FOR VALUES FROM ('2026-01-01') TO ('2026-02-01');\nCREATE TABLE events_2026_02 PARTITION OF events FOR VALUES FROM ('2026-02-01') TO ('2026-03-01');\nCREATE TABLE events_2026_03 PARTITION OF events FOR VALUES FROM ('2026-03-01') TO ('2026-04-01');\n\n-- Add composite indexes per partition\nCREATE INDEX ON events_2026_01 (status, created_at);\nCREATE INDEX ON events_2026_02 (status, created_at);\nCREATE INDEX ON events_2026_03 (status, created_at);\n\n-- Verify pruning behavior\nEXPLAIN ANALYZE\nSELECT * FROM events\nWHERE created_at BETWEEN '2026-01-01' AND '2026-03-31'\n  AND status = 'active';\n```",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-17",
        "questionId": "q-30482",
        "question": "Scenario: A PostgreSQL analytics database stores 500GB of e-commerce data with tables: orders(orderId, customerId, productId, quantity, price, status, createdAt), customers(customerId, region, tier, signupDate), and products(productId, categoryId, price, name). The application has these query patterns: Q1) SELECT * FROM orders WHERE customerId = ? AND createdAt > ? ORDER BY createdAt DESC; Q2) SELECT o.* FROM orders o JOIN customers c ON o.customerId = c.customerId WHERE c.region = ? AND o.createdAt > ? AND o.status = 'completed'; Q3) SELECT productId, COUNT(*), SUM(quantity) FROM orders WHERE createdAt > ? GROUP BY productId; Q4) SELECT customerId, SUM(price) FROM orders WHERE status = 'completed' AND createdAt > ? GROUP BY customerId HAVING SUM(price) > ?. Design index strategies for all four queries, explain trade-offs between indexes, and describe how you would validate that indexes are being used correctly by the query planner?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "- Under READ COMMITTED, if B is deleted mid-transaction, A can be debited while the credit to B never materializes, risking loss or orphaned state. Prevent by strict atomicity: - Use a single transaction with explicit r…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create: HASH index idx_visits_user_hash on (user_id); BTREE idx_visits_time on (visited_at); COMPOSITE BTREE idx_visits_user_time_page on (user_id, visited_at, page_id). Pattern 1 uses composite with",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage explain analyze to verify that only relevant daily partitions are scanned via partition pruning on sale_date and region. Use range partitions by date and hash partitions by region; add local",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design composite and covering indexes for a mixed read-write analytics workload, balancing query performance against index maintenance overhead.",
            "isCorrect": true
          }
        ],
        "explanation": "For Q1 (customer lookups by date), a composite index on (customerId, createdAt DESC) covers both filtering and sorting, eliminating the need for an explicit sort operation. For Q2 (regional joins with status filtering), a partial index on orders WHERE status = 'completed' paired with a composite index on (status, createdAt) efficiently supports the join, while the customers table requires an index on region. For Q3 (product aggregations), a composite index on (productId, quantity) with a partial index WHERE createdAt > ? covers the aggregation needs. For Q4 (high-value customer identification), a covering index on (customerId, price) WHERE status = 'completed' enables index-only scans. To validate index usage, examine EXPLAIN ANALYZE output to confirm the query planner selects the appropriate indexes, verify that index scans appear instead of sequential scans on large tables, and monitor query execution times before and after index creation to measure actual performance impact.",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-18",
        "questionId": "q-27261",
        "question": "Scenario: A fintech ledger uses Postgres as the single source of truth, Redis for fast balance reads, and an immutable audit log in S3. Design a cross-store transfer that debits one account and credits another under SERIALIZABLE isolation. Include concrete schemas, an atomic transfer flow (Outbox or 2PC), and a test plan for idempotency and failure recovery?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Choose structured data and exact match queries for PostgreSQL indexes, full-text search and complex filters for Elasticsearch, use partial indexes with tenant_id WHERE clauses, and invalidate Redis ca",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Question:** Design a transfer between accounts in a financial ledger service storing accounts and a daily aggregate. The transfer must guarantee no overdrafts and no phantom updates under MVCC. Accounts may reside on…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a SERIALIZABLE transaction in Postgres to debit and credit, and write a corresponding event into an outbox table within the same transaction. A separate worker streams the outbox to Kafka with a t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create Composite B-tree indexes: (exchange_id, user_id, ts) for A and (symbol, ts) for B; add a HASH index on (exchange_id) for exact lookups; implement partial index on ts >= current_timestamp - inte",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests cross-service transactional design, correctness under SERIALIZABLE isolation, and strategies to maintain consistency across a relational database, a cache, and an append-only audit log.\n\n## Key Concepts\n\n- SERIALIZABLE isolation with MVCC\n- Outbox pattern vs. 2PC\n- Idempotent consumers and exactly-once delivery\n- Cache invalidation strategies after commit\n\n## Code Example\n\n```sql\n-- within a single SERIALIZABLE transaction\nBEGIN;\nUPDATE accounts SET balance = balance - :amt WHERE id = :from AND balance >= :amt;\nUPDATE accounts SET balance = balance + :amt WHERE id = :to;\nINSERT INTO outbox_events (event_type, payload, processed) VALUES ('transfer', :payload, false);\nCOMMIT;\n```\n\n## Follow-up Questions\n\n- What failure modes could occur after debit but before commit, and how would you mitigate them?\n- How would you test idempotency and replay safety in the outbox-to-consumer path?\n- How would you handle Redis cache coherence with rapidly changing balances?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-19",
        "questionId": "q-15132",
        "question": "In a fintech workload, a large transactions table is range-partitioned by sale_date (monthly) with a hash subpartition on user_id. A query filters by user_id and a date range and joins to a small customers table. Explain how you would validate explain analyze plans to guarantee partition pruning and parallelism, and state concrete changes to partitioning, indexing, and planner settings to fix any mispruning?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "READ COMMITTED sees only committed data at query time, allowing non-repeatable reads. REPEATABLE READ uses MVCC snapshots to guarantee consistent reads within a transaction, preventing non-repeatable reads but still all…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use EXPLAIN ANALYZE to confirm only relevant partitions are scanned and the plan uses a feasible join with the small customers table. If pruning fails, switch to range partitioning by sale_date with h",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement declarative range partitions by month on events(event_date). Ensure WHERE clauses use tight, contiguous date ranges (avoid OR conditions) to guarantee partition pruning. Add a per-partition index on (dim_id, e…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "SELECT ar.name, SUM(s.duration_seconds) AS total_duration, COUNT(DISTINCT s.id) AS song_count FROM users u JOIN playlists p ON p.user_id = u.id JOIN playlist_songs ps ON ps.playlist_id = p.id JOIN songs s ON s.id = ps.s…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nInterview context explanation.\n\n## Key Concepts\n\n- Partition pruning validation with EXPLAIN ANALYZE\n- Composite per-partition indexing\n- Planner settings: enable_partition_pruning, constraint_exclusion\n- Join strategies with small dimension tables\n\n## Code Example\n\n```sql\nEXPLAIN ANALYZE\nSELECT t.*\nFROM transactions t\nJOIN customers c ON c.user_id = t.user_id\nWHERE t.user_id = ? AND t.sale_date BETWEEN ? AND ?;\n```\n\n## Follow-up Questions\n\n- How would you monitor pruning regressions over time?\n- What trade-offs arise from adding hash subpartitions on user_id?",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-20",
        "questionId": "q-6351",
        "question": "Design a four-store flow for a fintech app: Redis stores live sessions, MongoDB holds recent user actions, Cassandra retains long-term analytics, DynamoDB archives. Specify concrete schemas and TTLs: Redis keys like session:{user}:{sid} with 900s TTL; MongoDB documents with a ts field and a TTL index set to 30 days; Cassandra table with TTL 730 days; DynamoDB archives with expireAt attribute set 7 years ahead. Describe writes, movement rules, and cache coherence?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "READ COMMITTED sees only committed data at query time, allowing non-repeatable reads. REPEATABLE READ uses MVCC snapshots to guarantee consistent reads within a transaction, preventing non-repeatable reads but still all…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "For the events table with the given workload, here is a concrete indexing strategy: -- Primary key is already indexed (B-tree by default) -- 1. Composite B-tree for fetching latest 100 events per user in date range CREA…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run EXPLAIN ANALYZE to confirm partition pruning and index usage. Ensure predicates reference the partition key (event_date) and that partitions have CHECK constraints enabling pruning. If not, recrea",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On each event, write to Redis session:{user}:{sid} with 900s TTL and a MongoDB actions document ts:now. Nightly, move actions older than 7 days to Cassandra with TTL 730 days; export monthly chunks to",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to reason about cross-store TTLs, concrete data models, and practical data movement across Redis, MongoDB, Cassandra, and DynamoDB.\n\n## Key Concepts\n\n- TTL and expiry mechanisms across four stores\n- Cross-store data movement patterns and idempotence\n- Schema design: Redis keys, MongoDB TTL index, Cassandra TTL, DynamoDB expireAt\n\n## Code Example\n\n```javascript\n// Example write path sketch\nfunction writeEvent(user, sid, event) {\n  redis.setex(`session:${user}:${sid}`, 900, JSON.stringify(event))\n  mongo.updateOne({user}, {$push: {events: {ts: Date.now(), ...event}}})\n}\n```\n\n## Follow-up Questions\n\n- How would you test TTL cleanup and cross-store consistency?\n- How do you handle clock skew or retry semantics across stores?",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-21",
        "questionId": "q-29733",
        "question": "Scenario: A multi-tenant analytics app stores tenants, users, sessions, and events in Postgres with strict normalization. Events has a JSONB metadata column. For a given date, compute per-tenant daily active users (unique users who started sessions) using efficient joins and indexing. Propose tables, key constraints, and indexes (including partial and GIN on metadata). Provide the SQL to compute; discuss normalization trade-offs and how to handle data archival?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Under Read Committed, two concurrent transfers can race: each transaction reads the account balances, performs calculations, then writes updated values. This creates a write skew where both transfers may succeed, result…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "MongoDB: Update user document `_id=userId` with `display_name`, `bio`, and `last_modified` timestamp. Redis: Delete `user:<id>:profile` cache key (TTL 300s) to force fresh data on next read, also delete any related sess…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Normalize into tenants(id), users(id, tenant_id), sessions(id, user_id, started_at), events(id, session_id, type, created_at, metadata). Indexes: btree on sessions(tenant_id, started_at); partial inde",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a materialized Top-10 in Redis: a sorted set top24:YYYYMMDD keyed by sku with score=views_last24h and a DynamoDB table regional_availability: {region, sku, available}. On every view/purchase",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests normalization and indexing for analytic queries over multi-tenant data, with a JSONB field. Emphasizes real-world performance considerations and archival strategy.\n\n## Key Concepts\n\n- 3NF data modeling and foreign-key integrity\n- Indexing: composite, partial, and JSONB (GIN)\n- Efficient joins across tenants/users/sessions/events\n- Time-bucketed analytics and partitioning for archival\n\n## Code Example\n\n```javascript\n// SQL-like example for the per-tenant DAU query (pseudo)\nconst sql = `SELECT t.id, DATE(e.created_at) day, COUNT(DISTINCT s.user_id)\nFROM tenants t\nJOIN users u ON u.tenant_id = t.id\nJOIN sessions s ON s.user_id = u.id\nJOIN events e ON e.session_id = s.id\nWHERE e.type = 'login' AND e.created_at BETWEEN $1 AND $2\nGROUP BY t.id, day;`;\n```\n\n## Follow-up Questions\n\n- How would you adapt this for rolling windows (7-day DAU)?\n- How would you validate the index usage with EXPLAIN ANALYZE?",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-22",
        "questionId": "q-11316",
        "question": "In a payment system using PostgreSQL MVCC, two concurrent transactions operate on the accounts table: T1 transfers 100 from A to B; T2 computes daily risk as sum(balance) for all active accounts. Explain how write skew and phantom reads can manifest under READ COMMITTED, REPEATABLE READ, and SERIALIZABLE, and propose concrete strategies to guarantee correctness (isolation level choice, SELECT FOR UPDATE, constraints, or predicate locking). Include concrete SQL sequences to reproduce and to fix the anomaly?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use SERIALIZABLE for the risk path to prevent write skew and phantom reads. Lock the involved rows with SELECT FOR UPDATE in the transfer path; if SERIALIZABLE hurts throughput, use FOR UPDATE on affe",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a multi-layered indexing strategy: **1. Range Partitioning:** Partition the events table by createdAt (monthly), with tenantId as the distribution key to co-locate tenant data. This reduces the index footprint…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an explicit transfer transaction with row-level locking to prevent race conditions. Example: BEGIN; SELECT balance FROM accounts WHERE id=$src FOR UPDATE; SELECT balance FROM accounts WHERE id=$dst FOR UPDATE; IF ba…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "SQL: SELECT p.name AS patron, b.title AS book, a.name AS author, l.due_date FROM loans l JOIN patrons p ON p.id=l.patron_id JOIN books b ON b.id=l.book_id JOIN authors a ON a.id=b.author_id WHERE l.re",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of MVCC anomalies, their manifestation under common PostgreSQL isolation levels, and concrete strategies to guarantee correctness in financial style workloads.\n\n## Key Concepts\n- MVCC, phantom reads, write skew\n- Isolation levels: READ COMMITTED, REPEATABLE READ, SERIALIZABLE\n- Locking: SELECT FOR UPDATE, advisory locks\n- Integrity via constraints/triggers when needed\n\n## Code Example\n```sql\n-- Reproduce under READ COMMITTED\nBEGIN;\nUPDATE accounts SET balance = balance - 100 WHERE id = 'A';\n-- T2 runs concurrently\nSELECT SUM(balance) FROM accounts WHERE status='active';\nCOMMIT;\n```\n\n```sql\n-- Safe fix\nBEGIN;\nSELECT balance FROM accounts WHERE id = 'A' FOR UPDATE;\nUPDATE accounts SET balance = balance - 100 WHERE id = 'A';\nCOMMIT;\n```\n\n## Follow-up Questions\n- How would you measure SERIALIZABLE overhead in this workload?\n- How would you adapt to a sharded setup with cross-shard risk calculation?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-23",
        "questionId": "q-458",
        "question": "You have a PostgreSQL database with orders (10M rows) and customers (1M rows). A query joining these tables is slow. How would you optimize it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin a SERIALIZABLE transaction and coordinate across shards with either 2PC or a central lock. Lock the customer row (per-customer lock) with FOR UPDATE, read current_outstanding, and check cap. If",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To optimize the slow PostgreSQL query joining orders (10M rows) and customers (1M rows), add indexes on the customer_id foreign key in the orders table and analyze the query execution plan with EXPLAIN ANALYZE to identi…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement declarative range partitions by month on events(event_date). Ensure WHERE clauses use tight, contiguous date ranges (avoid OR conditions) to guarantee partition pruning. Add a per-partition index on (dim_id, e…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use SERIALIZABLE (or SSI). In one transaction, lock both accounts with FOR UPDATE, verify src balance >= amount, then debit/credit, and insert a ledger row, then COMMIT. On serialization_failure, retr",
            "isCorrect": false
          }
        ],
        "explanation": "## Key Optimization Strategies\n\n- **Indexing**: Create composite indexes on join columns and frequently filtered columns\n- **Query Analysis**: Use EXPLAIN ANALYZE to identify full table scans and expensive operations\n- **Join Strategy**: Ensure proper join order and consider hash joins for large datasets\n- **Partitioning**: Implement table partitioning by date or customer ranges for better performance\n\n## PostgreSQL Specific Techniques\n\n```sql\n-- Create composite index for join and filter\nCREATE INDEX idx_orders_customer_date \nON orders(customer_id, order_date);\n\n-- Analyze query performance\nEXPLAIN ANALYZE SELECT o.*, c.name \nFROM orders o JOIN customers c \nON o.customer_id = c.id \nWHERE o.order_date > '2023-01-01';\n```\n\n## Trade-offs\n\n- **Storage**: Indexes increase storage requirements\n- **Write Performance**: More indexes slow down INSERT/UPDATE operations\n- **Maintenance**: Regular VACUUM and ANALYZE needed for optimal performance",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-24",
        "questionId": "q-6974",
        "question": "You're designing a multi-tenant SaaS platform where each customer has their own isolated data but shares the same database infrastructure. How would you approach indexing strategy to ensure query performance while maintaining tenant isolation, and what trade-offs would you consider between composite indexes vs. tenant-prefixed indexes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-store write path: Redis key progress:u{userId}:v{videoId} with fields {watchPercent,lastViewedAt}, TTL 1h. MongoDB: users.{userId}.videos: {videoId,watchPercent,lastViewedAt}. Cassandra",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Under Read Committed, two concurrent transfers can race: each transaction reads the account balances, performs calculations, then writes updated values. This creates a write skew where both transfers may succeed, result…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use tenant_id as leading column in composite indexes for optimal isolation and performance, balancing query selectivity with index maintenance overhead across shared infrastructure.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use composite partition keys with sharding (product_id#hash) and adaptive capacity for hot items, trading some read latency for write scalability.",
            "isCorrect": false
          }
        ],
        "explanation": "For multi-tenant SaaS, the core indexing challenge is balancing performance with isolation. The optimal approach is making tenant_id the leading column in all composite indexes, ensuring queries are naturally filtered by tenant first. This prevents cross-tenant data leakage and allows the database to quickly narrow results to the relevant tenant's data subset. For example, in a customer orders table, an index on (tenant_id, customer_id, created_at) serves both tenant isolation and common query patterns like 'recent orders for specific customer'.\n\nThe trade-off between composite indexes and tenant-prefixed indexes involves maintenance overhead vs. query flexibility. Composite indexes with tenant_id as the prefix are more space-efficient and support multiple query patterns, but may require more indexes for different access patterns. Tenant-prefixed indexes (like creating separate index families per tenant) provide maximum isolation and can be tailored per tenant's usage patterns, but dramatically increase index count and maintenance complexity. Most SaaS platforms opt for composite indexes with tenant_id as the first column, supplemented by selective secondary indexes for high-frequency tenant-specific queries.\n\nReal-world implementation should also consider index cardinality - if tenant_id has low cardinality (few tenants), consider adding a high-cardinality column as the second index component to improve selectivity. Additionally, implement partial indexes for active tenants only, and use database features like PostgreSQL's partitioning or MongoDB's sharding with tenant_id as the partition/shard key for very large multi-tenant deployments.",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-20T11:09:02.754Z",
    "version": 1
  }
]