[
  {
    "id": "test-database",
    "channelId": "database",
    "channelName": "Database",
    "title": "Database Knowledge Test",
    "description": "Test your understanding of Database with 25 questions.",
    "questions": [
      {
        "id": "database-q-0",
        "questionId": "q-21483",
        "question": "You have a PostgreSQL table posts(id BIGINT, city TEXT, created_at TIMESTAMPTZ, content TEXT). A common query fetches the 20 most recent posts for a city: SELECT * FROM posts WHERE city = $1 ORDER BY created_at DESC LIMIT 20. Another query counts posts per city for a given day: SELECT count(*) FROM posts WHERE city = $1 AND created_at >= $start AND created_at < $end. Design an index strategy using B-tree and discuss a hash index for city equality. Provide concrete index definitions and explain expected EXPLAIN plans?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To optimize the slow PostgreSQL query joining orders (10M rows) and customers (1M rows), add indexes on the customer_id foreign key in the orders table and analyze the query execution plan with EXPLAIN ANALYZE to identi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a composite index on (status, created_at) with status as the leading column to leverage index equality matching before range filtering.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a composite B-tree index on (city, created_at DESC) to satisfy both: the top-20 recent posts per city and per-day counts with a time range. A separate hash index on (city) can speed equality l",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a single global_order_id and an idempotent write path: a Redis Lua script writes order:<id> with status and version, MongoDB upserts orders:<id> on a matching order_version, and publishes an event",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of composite B-tree indexes and their ability to handle both equality and range predicates, plus where a hash index fits in. Also probes knowledge of ordering semantics and index-only scans in a beginner-friendly setting.\n\n## Key Concepts\n\n- Composite B-tree indexes support leading-column equality and trailing-column ranges; (city, created_at DESC) can satisfy both queries.\n- Hash indexes excel at equality lookups but do not support ORDER BY or range predicates on created_at.\n- Index-only scans are possible if visibility maps allow; this avoids heap access entirely.\n\n## Code Example\n\n```sql\n-- Composite index to support both queries\nCREATE INDEX idx_posts_city_createdat ON posts (city, created_at DESC);\n\n-- Optional: hash index for city-only equality lookups\nCREATE INDEX idx_posts_city_hash ON posts USING HASH (city);\n```\n\n## Follow-up Questions\n\n- How would you adjust the indexing strategy if there are millions of cities?\n- When would partial indexes (WHERE city IS NOT NULL AND created_at >= now() - interval '7 days') help?\n- How do insert/update patterns impact index maintenance and write amplification?",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-1",
        "questionId": "q-11334",
        "question": "PostgreSQL advanced: you maintain a logs table partitioned by month on occurred_at (logs(id, tenant_id, event_type, occurred_at, details JSONB)); tenants(tenant_id, region). For a given region and date range, return top 3 event_type by total count where details->>'status' = 'completed'. Propose indexing strategy (partial/GIN), provide a single SQL query, and explain how partition pruning avoids scanning old partitions and how to add a new monthly partition without downtime?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Under Read Committed, a concurrent update can overwrite another’s change (lost update). Repeatable Read may prevent re-reading a changed balance but can still allow anomalies with certain patterns. Se",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "This document describes a Saga orchestrator that coordinates four data stores: MongoDB (inventory), Redis (hot-stock cache), DynamoDB (orders), and Cassandra (audit logs). The workflow ensures no overselling, supports i…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Write path: Cassandra appends the notification per user_id; MongoDB stores user metadata; Redis updates unread_count and pushes the notification_id into a per-user list; DynamoDB updates a daily_diges",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use partition pruning on occurred_at, join tenants for region, and filter via JSONB index on details. Per-partition index: CREATE INDEX logs_details_completed ON logs USING GIN (details) WHERE details",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThe scenario tests understanding of partition pruning, JSONB indexing, and multi-tenant query patterns in Postgres.\n\n## Key Concepts\n- Partition pruning across monthly shards\n- JSONB GIN and partial indexes for status filters\n- Joins to filter by region and avoid full scans\n\n## Code Example\n```sql\nCREATE INDEX logs_details_completed ON logs USING GIN (details) WHERE details @> '{\"status\":\"completed\"}';\nSELECT l.event_type, COUNT(*) AS total, COUNT(DISTINCT l.tenant_id) AS tenants\nFROM logs l JOIN tenants t ON l.tenant_id = t.tenant_id\nWHERE t.region = :region AND l.occurred_at >= :start AND l.occurred_at < :end\n  AND l.details @> '{\"status\":\"completed\"}'\nGROUP BY l.event_type\nORDER BY total DESC\nLIMIT 3;\n```\n\n## Follow-up Questions\n- How would you adapt for a rolling window and new partitions?\n- How would you validate partition pruning in production workloads?",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-2",
        "questionId": "q-15932",
        "question": "In a PostgreSQL e-commerce database with tables for orders (10M rows), order_items (50M rows), and products (500K rows), you notice slow queries for 'orders by customer with product category filter' and 'monthly sales by category'. Given the existing single-column indexes on order_date, customer_id, and product_id, design a comprehensive indexing strategy: which index types would you use (B-tree, GIN, BRIN, etc.), what should the composite index column order be, and how would you handle the trade-off between read performance and write overhead?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition events by day using range partitioning on `ts`. Create a multicolumn index on `devices(region, type)`. Create a covering partial index on `events(device_id, ts) INCLUDE (value) WHERE status <> 'error'`. Optimi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use composite B-tree indexes with high-selectivity (low-cardinality) columns first, followed by low-selectivity (high-cardinality) columns. For range predicates like order_date, position these after equality columns. Ad…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Clustered indexes physically reorder data (1 per table) - ideal for range queries and primary keys. Non-clustered indexes store pointers to data (many per table) - better for selective lookups and covering queries. Clus…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an explicit transfer transaction with row-level locking to prevent race conditions. Example: BEGIN; SELECT balance FROM accounts WHERE id=$src FOR UPDATE; SELECT balance FROM accounts WHERE id=$dst FOR UPDATE; IF ba…",
            "isCorrect": false
          }
        ],
        "explanation": "For the 'orders by customer with product category' query, create a composite index on (customer_id, order_date, category). Here, customer_id serves as an equality predicate, order_date as a range predicate, and category filters results. Placing equality columns first allows the index to narrow down the result set efficiently before scanning the date range.\n\nFor monthly sales by category, a materialized view with a composite index on (year, month, category) provides optimal performance for aggregation queries. Alternatively, a BRIN (Block Range Index) is well-suited for time-series data on the orders table, as it takes advantage of physical row ordering and offers minimal storage overhead with reasonable query performance for sequential scans.",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-3",
        "questionId": "q-12702",
        "question": "Design an indexing strategy for a PostgreSQL users table: columns id (PK), username TEXT, email TEXT, country TEXT, signup_date DATE, status TEXT. You need: (a) fast exact lookup by username, (b) fast range queries for signup_date within a given country and status, and (c) fast top-N by signup_date for a given country/status. Propose concrete index choices and CREATE INDEX statements, and explain trade-offs. What indexes would you create and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a BTREE index on lower(username) for case-insensitive exact lookups, and a composite BTREE index on (country, status, signup_date DESC) to support range queries and top-N by signup_date within",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Partition by month (range) on event_time. On each partition, create a composite index on (user_id, event_time) covering event_type. Write filters with event_time >= current_date - interval '90 days' a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create: HASH index idx_visits_user_hash on (user_id); BTREE idx_visits_time on (visited_at); COMPOSITE BTREE idx_visits_user_time_page on (user_id, visited_at, page_id). Pattern 1 uses composite with",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage EXPLAIN ANALYZE BUFFERS to confirm partition pruning and partition-wise aggregation; ensure the WHERE clause matches partition bounds (order_month for last 6 months); weigh daily vs monthly p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical indexing reasoning for a beginner, focusing on BTREE vs hash and composite indexes.\n\n## Key Concepts\n\n- BTREE for range and order\n- Hash index for exact lookups\n- Composite index (leading columns) to cover queries\n- DESC order for top-N\n\n## Code Example\n\n```javascript\n// SQL-like index definitions (PostgreSQL)\nCREATE INDEX idx_users_username ON users (lower(username));\nCREATE INDEX idx_users_country_status_signup ON users (country, status, signup_date DESC);\n```\n\n## Follow-up Questions\n\n- How would you adjust if queries often filter by country but not status?\n- What are the write-time trade-offs of adding the composite index?",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-4",
        "questionId": "q-20537",
        "question": "Scenario: A Postgres-like database backs a marketplace with table events(id, user_id, city, category, event_type, product_id, ts). Frequent queries: (a) last 20 events for a city/category by ts; (b) daily top-5 products by event_type='view' in a city; (c) most recent events per user across categories. Propose concrete indexing using BTREE, HASH, and composite indexes, with exact CREATE INDEX statements, key orders, and INCLUDE columns; discuss read/write trade-offs and maintenance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use BTREE composite indexes: 1. For last 20 events by city/category sorted by timestamp: ```sql CREATE INDEX idx_events_city_cat_ts ON events (city, category, ts DESC); ``` 2. For daily top-5 products by event_type='vie…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Hybrid: range-partition by created_at (daily) with subpartition by tenant_id using HASH to confine scans per tenant. Create indexes on (tenant_id, created_at, channel_id) and a covering (tenant_id, cr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: join three tables with LEFT JOINs to preserve regions with zero overdue, filter overdue in the invoice join, and aggregate by region. Use a partial index on invoices(due_at) WHERE paid_at IS",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Normalize into tenants(id), users(id, tenant_id), sessions(id, user_id, started_at), events(id, session_id, type, created_at, metadata). Indexes: btree on sessions(tenant_id, started_at); partial inde",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical indexing skills across BTREE, HASH, and composite indexes with concrete SQL. It requires understanding how to choose the correct left-to-right key order to satisfy multiple WHERE clauses and ORDER BY clauses, along with the use of covering indexes and INCLUDE columns.\n\n## Key Concepts\n- Composite BTREE index design for multi-column filters\n- Hash index usage for equality-only filters on high-cardinality columns\n- Covering indexes and INCLUDE to avoid unnecessary table lookups\n- Read versus write amplification and maintenance strategies\n\n## Code Example\n```sql\n-- Query (a): Last 20 events for a city/category\nSELECT * FROM events\nWHERE city = 'NYC' AND category = 'electronics'\nORDER BY ts DESC\nLIMIT 20;\n\n-- Query (b): Daily top-5 products by views in a city\nSELECT product_id, COUNT(*) as view_count\nFROM events\nWHERE city = 'NYC' AND event_type = 'view'\nAND ts >= CURRENT_DATE\nGROUP BY product_id\nORDER BY view_count DESC\nLIMIT 5;\n\n-- Query (c): Most recent events per user\nSELECT * FROM events\nWHERE user_id = 12345\nORDER BY ts DESC;\n```",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-5",
        "questionId": "q-4333",
        "question": "Given a PostgreSQL schema with tables: customers(id), orders(id, customer_id, product_id, placed_at, status, amount), products(id, category_id), categories(id, name). For every customer, return the latest completed order per product category. How would you write the query, what indexes (including partial or expression indices) would you create, and how would you verify the plan actually uses them under a 1TB data set?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Find posts in the date window that have the tag via joins to post_tags and tags, count the posts, sum their comments, and compute per-user post counts to average them. Use left join for comments, subq",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose: 1) B-tree composite index on (tenant_id, user_id, created_at DESC) for latest-events per user; 2) hash index on (tenant_id, user_id) for quick tenant lookups; 3) partial index on (tenant_id,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a single SQL query that returns, for each customer, the most recent completed order per product category, using a window function partitioned by (customer_id, category_id) ordered by placed_at",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "On each activity: Redis: ZINCRBY leaderboard:YYYY-MM-DD user_id delta, TTL 86400; MongoDB: update users last_seen; Cassandra: insert into activity_logs(user_id, ts, delta); DynamoDB: update weekly_tot",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a correct, efficient per-group query on large data, choose practical indexes (including partial and expression-based), and verify plans with EXPLAIN. Also checks normalization trade-offs for read-heavy workloads.\n\n## Key Concepts\n\n- Window functions for per-group latest row\n- Partial indexes to cover WHERE filters\n- Expression/covering indexes to avoid heap lookups\n- Join strategy across orders, products, categories\n- Plan verification and robustness to data skew\n\n## Code Example\n\n```sql\nSELECT o.customer_id, c.name AS category, o.id AS order_id, o.placed_at, o.amount\nFROM (\n  SELECT o.*, p.category_id,\n         ROW_NUMBER() OVER (PARTITION BY o.customer_id, p.category_id ORDER BY o.placed_at DESC) AS rn\n  FROM orders o\n  JOIN products p ON p.id = o.product_id\n  WHERE o.status = 'completed'\n) AS o\nJOIN categories c ON c.id = o.category_id\nWHERE o.rn = 1;\n```\n\n## Follow-up Questions\n\n- How would you handle ties when two orders share the same placed_at timestamp?\n- How would you adapt this for hot paths where a few customers generate most orders?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-6",
        "questionId": "q-30436",
        "question": "In a financial ledger service storing accounts and a daily aggregate, design a transfer from one account to another that guarantees no overdrafts and no phantom updates under MVCC. The accounts may reside on different shards. Provide concrete schemas, a cross-shard transactional flow (2PC or advisory locks), and a practical test plan to prove correctness under SERIALIZABLE isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Question:** Design a transfer between accounts in a financial ledger service storing accounts and a daily aggregate. The transfer must guarantee no overdrafts and no phantom updates under MVCC. Accounts may reside on…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create a composite index on (customer_id, status, created_at DESC) for query #1, a BRIN index on created_at for time-range queries, and a partial index for pending orders. Balance index cardinality, selectivity, and wri…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "On each view, Redis INCRBY articles:<id>:views; Cassandra insert into article_interactions (date, article_id, delta=1). Redis TTL set to 86400. A background job recomputes Top5 by score = 0.6*recency",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Query: SELECT p.category, SUM(oi.quantity*oi.unit_price) AS revenue, COUNT(DISTINCT o.id) AS orders FROM customers c JOIN orders o ON o.customer_id=c.id JOIN order_items oi ON oi.order_id=o.id JOIN pr",
            "isCorrect": false
          }
        ],
        "explanation": "**Why This Question Is Asked**\n\nThis question evaluates a candidate's understanding of distributed transactional correctness under MVCC, including isolation guarantees, cross-shard coordination mechanisms, and idempotent retry semantics.\n\n**Key Concepts**\n\n\n- **MVCC visibility guarantees** under SERIALIZABLE/SSI isolation.\n- **Cross-shard coordination** using 2PC or advisory locks.\n- **Idempotent retry semantics** via idempotency keys.\n- **Phantom avoidance** through serialization and predicate locking.\n- **Overdraft guarantees** via balance validation before commit.\n\n**Follow-Up Questions**\n\n\n- How do clock skew and distributed timestamps affect SSI correctness?\n- What trade-offs exist between pessimistic locking (2PC) and optimistic concurrency control?\n- How would you handle network partitions without a coordinator?\n- What are the performance implications of SERIALIZABLE vs. READ COMMITTED isolation for this use case?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-7",
        "questionId": "q-13865",
        "question": "Scenario: A wallet service stores balances in a Postgres table (accounts: id, balance). Implement a funds transfer from :from to :to as an atomic operation under high concurrency using MVCC. Provide exact SQL: row locks, balance checks, balance updates, and an audit log insert in one transaction. Explain how you test SERIALIZABLE vs REPEATABLE READ and guard against write skew without distributed transactions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin a transaction; SELECT balance FROM accounts WHERE id = :from FOR UPDATE; SELECT balance FROM accounts WHERE id = :to FOR UPDATE; IF from_balance < :amount THEN RAISE; UPDATE accounts SET balance",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a single SQL with three CTEs: recent as completed orders in last 90 days, per_user as SUM(qty*price) by user in region, and per_user_cat as SUM(qty*price) by user, region, category. Then apply ROW",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an explicit transfer transaction with row-level locking to prevent race conditions. Example: BEGIN; SELECT balance FROM accounts WHERE id=$src FOR UPDATE; SELECT balance FROM accounts WHERE id=$dst FOR UPDATE; IF ba…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition events by day with range partitions; if last two weeks are typical, switch to weekly partitions for pruning. Create a multicolumn B-tree index on (region, device_type, event_date) and a BRIN",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nInterprets practical MVCC usage and how serializable isolation prevents write skew in funds transfers. It also touches auditability and behavior under high contention.\n\n## Key Concepts\n- MVCC and row-level locking\n- SERIALIZABLE vs REPEATABLE READ\n- Write skew and anomaly scenarios\n- Atomicity with audit logging\n\n## Code Example\n\n```sql\nBEGIN;\nSELECT balance FROM accounts WHERE id = :from FOR UPDATE;\nSELECT balance FROM accounts WHERE id = :to FOR UPDATE;\nIF from_balance < :amount THEN\n  RAISE EXCEPTION 'insufficient funds';\nEND IF;\nUPDATE accounts SET balance = balance - :amount WHERE id = :from;\nUPDATE accounts SET balance = balance + :amount WHERE id = :to;\nINSERT INTO audit_log (from_id,to_id,amount,timestamp) VALUES (:from,:to,:amount, NOW());\nCOMMIT;\n```\n\n## Follow-up Questions\n- How would you implement idempotency for repeated transfer requests?\n- How would you detect and recover from a serialization failure in production?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-8",
        "questionId": "q-380",
        "question": "You're optimizing a query that's slow due to a large time-series table. The query filters by timestamp range and device_id. How would you analyze the query plan and what partitioning strategy would you recommend?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a composite B-Tree index on (tenant_id, event_type, ts) to support per-tenant time-range scans and per-type lookups. Use a separate hash index on (tenant_id, event_type) for exact lookups that don't require ti…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition the events table by RANGE on event_time using monthly partitions. For each partition, create a covering index that includes both the partition key and the filtering/aggregation columns: `CREATE INDEX ON events…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To optimize the slow PostgreSQL query joining orders (10M rows) and customers (1M rows), add indexes on the customer_id foreign key in the orders table and analyze the query execution plan with EXPLAIN ANALYZE to identi…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Analyze the EXPLAIN plan to identify full table scans, then implement timestamp-based partitioning with a composite index on (timestamp, device_id).",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTesla deals with massive time-series data from vehicles. This tests practical query optimization skills and understanding of how partitioning affects performance at scale.\n\n## Expected Answer\nA strong candidate would: 1) Use EXPLAIN ANALYZE to identify the bottleneck, 2) Notice the query isn't using the index effectively, 3) Recommend partitioning by timestamp ranges (daily/weekly), 4) Suggest a composite index, 5) Explain how partition pruning reduces scanned data.\n\n## Code Example\n```sql\n-- Analyze current performance\nEXPLAIN ANALYZE SELECT * FROM telemetry \nWHERE timestamp BETWEEN '2024-01-01' AND '2024-01-07'\n  AND device_id = 'vehicle_123';\n\n-- Create partitioned table\nCREATE TABLE telemetry (\n    id SERIAL,\n    device_id VARCHAR(50),\n    timestamp TIMESTAMP,\n    metrics JSONB,\n    PRIMARY KEY (id, timestamp)\n) PARTITION BY RANGE (timestamp);\n\n-- Create daily partitions\nCREATE TABLE telemetry_2024_01_01 PARTITION OF telemetry\n    FOR VALUES FROM ('2024-01-01') TO ('2024-01-02');\n\n-- Composite index for efficient filtering\nCREATE INDEX idx_telemetry_timestamp_device \n    ON telemetry (timestamp, device_id);\n```\n\n## Key Insights\n- Partition pruning reduces scanned data dramatically\n- Composite index order matters: timestamp first for partitioning, then device_id\n- Consider query patterns when choosing partition granularity",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-9",
        "questionId": "q-9724",
        "question": "In PostgreSQL with tables: users(id, region, created_at); orders(id, user_id, created_at, status); order_items(id, order_id, product_id, quantity, price); products(id, category, name, price). For each region, return top 5 customers by total_spent in the last 90 days, and for each customer list the top 3 product categories by spend. Propose an index strategy (multicolumn and partial indexes) and provide a single SQL query using CTEs and window functions to produce the result efficiently?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a single SQL with three CTEs: recent as completed orders in last 90 days, per_user as SUM(qty*price) by user in region, and per_user_cat as SUM(qty*price) by user, region, category. Then apply ROW",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Compute risk in Redis with a Lua script that uses tx_id, amount, and user history, then writes risk:tx:<id> with score and ts. Use DynamoDB TransactWriteItems to write region-scoped score and a summar",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To model and flow: Redis key stock:<productId> -> {qty, warehouse}, TTL 300s. MongoDB: products collection with productId, name, price, qty. Cassandra: table stock_events (product_id, ts, delta). Dyna",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use EXPLAIN ANALYZE to verify partition pruning for last 30 days on a range-partitioned events table. If pruning fails, check that the WHERE clause uses the partition key directly (no function on crea",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to write multi-join, aggregated queries with ranking and per-region limits, plus concrete indexing guidance.\n\n## Key Concepts\n\n- CTEs, window functions, and ROW_NUMBER for regional and per-user ranking\n- Multi-join path across users, orders, order_items, products\n- Index design: multicolumn and partial indexes to prune recent data\n\n## Code Example\n\n```sql\nWITH recent AS (\n  SELECT o.user_id, oi.product_id, oi.quantity, oi.price, u.region\n  FROM orders o\n  JOIN order_items oi ON o.id = oi.order_id\n  JOIN users u ON o.user_id = u.id\n  WHERE o.created_at >= now() - interval '90 days' AND o.status = 'completed'\n),\nper_user AS (\n  SELECT region, user_id, SUM(quantity*price) AS spend\n  FROM recent\n  GROUP BY region, user_id\n),\nper_cat AS (\n  SELECT region, user_id, category, SUM(quantity*price) AS spend\n  FROM recent r JOIN products p ON r.product_id = p.id\n  GROUP BY region, user_id, category\n)\nSELECT * FROM ...;\n```\n\n## Follow-up Questions\n\n- How would you test with explain analyze and verify index usage?\n- How to adjust top-N when data skew occurs?",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-10",
        "questionId": "q-17580",
        "question": "In a PostgreSQL e-commerce database with tables for orders (10M rows), order_items (50M rows), and products (500K rows), you notice slow queries for 'Find all orders from the last 30 days containing products from category X, sorted by total order value, limited to 100 results'. The current execution time is 8 seconds. Walk through your approach to analyze and optimize this query through indexing strategies. Discuss index types you'd consider (B-tree, GIN, BRIN, etc.), the trade-offs of each, how you'd order columns in composite indexes, and any application-level optimizations you'd recommend.",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a multi-layered indexing strategy: **1. Range Partitioning:** Partition the events table by createdAt (monthly), with tenantId as the distribution key to co-locate tenant data. This reduces the index footprint…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a transactional boundary with MVCC locking. Read and lock source and destination rows, validate balance, and apply debit/credit atomically. Prefer SELECT FOR UPDATE on both rows to serialize acces",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "First, I'd analyze the query execution plan using EXPLAIN ANALYZE to identify bottlenecks such as sequential scans on orders and order_items or inefficient joins. Then, I'd create B-tree composite indexes with proper co…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use SERIALIZABLE for the risk path to prevent write skew and phantom reads. Lock the involved rows with SELECT FOR UPDATE in the transfer path; if SERIALIZABLE hurts throughput, use FOR UPDATE on affe",
            "isCorrect": false
          }
        ],
        "explanation": "First, I'd run EXPLAIN ANALYZE to identify the bottleneck—likely sequential scans on orders and order_items along with inefficient joins. For this query, a composite index on orders(order_date DESC, total_value DESC) would help with date filtering and sorting. For order_items, a composite index on (order_id, product_id) combined with a partial index on products(category_id) would accelerate the join. If category filtering is frequent, a GIN index on an array of category_ids stored with each order could eliminate the join to products entirely. BRIN indexes might work well for the time-series nature of the orders table, offering a smaller footprint at the cost of less precise indexing. Additionally, I'd consider application-level optimizations such as implementing result caching, using pagination with keyset rather than offset-based limiting, and pre-aggregating data in materialized views if this query pattern runs frequently during reporting periods.",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-11",
        "questionId": "q-19324",
        "question": "You have a table events(event_id BIGINT PRIMARY KEY, user_id INT, event_type VARCHAR(16), created_at TIMESTAMPTZ, data JSONB). The workload is beginner-friendly: fetch the latest 100 events for a user in a date window; count events by user and type within a date window; and retrieve the most recent event per user. Design a concrete indexing strategy using B-tree, hash, and composite indexes (and optional partial indexes). Include concrete CREATE INDEX statements and explain tradeoffs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run EXPLAIN ANALYZE to verify pruning; check that only relevant monthly partitions are scanned and that aggregation is driven by a date-based grouping. If pruning is absent, add a composite index on (",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "For the events table with the given workload, here is a concrete indexing strategy: -- Primary key is already indexed (B-tree by default) -- 1. Composite B-tree for fetching latest 100 events per user in date range CREA…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement automated index monitoring using database statistics, create adaptive indexing policies that track query execution plans, and use partial or expression-based indexes to handle evolving patte",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a SERIALIZABLE transaction to perform both updates atomically. Steps: BEGIN; SET TRANSACTION ISOLATION LEVEL SERIALIZABLE; SELECT balance FROM accounts WHERE id = $1 FOR UPDATE; UPDATE accounts SE",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical indexing decisions for common read-heavy patterns, illustrating trade-offs between B-tree, hash, and composite indexes, plus the value of partial indexes for recent data.\n\n## Key Concepts\n- Composite B-tree indexes support multi-column range queries efficiently when columns are used in ORDER BY\n- Hash indexes work well for equality filters but don't support range queries\n- Partial indexes reduce storage and maintenance overhead by indexing only relevant data\n- Column order in composite indexes matters: equality conditions first, then range/sort columns\n\n## Trade-offs\n- Write amplification: more indexes slow down INSERT/UPDATE operations\n- Storage overhead: each index consumes additional disk space\n- Maintenance: indexes must be maintained via VACUUM to stay efficient\n- Partial indexes require careful predicate selection—too narrow limits usefulness, too wide defeats the purpose",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-12",
        "questionId": "q-8221",
        "question": "Given a PostgreSQL schema for a small publication system: authors(id, name), posts(id, author_id, created_at, title, content), tags(id, name), post_tags(post_id, tag_id). For a selected author_id and date range, return each tag name with the number of posts, the total word count across those posts, and the average words per post. Describe normalization, indexing strategy, and provide the SQL to compute it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On each event, write to Redis session:{user}:{sid} with 900s TTL and a MongoDB actions document ts:now. Nightly, move actions older than 7 days to Cassandra with TTL 730 days; export monthly chunks to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "B-trees provide O(log n) range scans with sorted data traversal; hash indexes offer O(1) equality lookups but degrade to O(n) for ranges and require collision handling strategies like chaining or open addressing.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Architecture Overview** **Redis (Hot Feature Cache)** - Key: `feat:{userId}:vector` (float32 array, 128–512 dimensions) - TTL: 5 minutes - Write-back policy: Asynchronous write on feature update via Kafka consumer; ba…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Join authors→posts on author_id, filter by author and date, then join post_tags→tags; group by tag.name to emit post_count, total_words, avg_words. Use composite indexes: posts(author_id, created_at)",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of multi-table normalization, efficient joins, and concrete indexing for common query shapes.\n\n## Key Concepts\n- Foreign-key joins across a many-to-many relation.\n- Composite indexes to support filtered range scans.\n- Aggregation by a dimension (tag) with derived metrics (words).\n\n## Code Example\n```sql\nSELECT t.name, COUNT(*) AS post_count,\n       SUM(char_length(p.content)-char_length(replace(p.content,' ',''))+1) AS total_words,\n       AVG(char_length(p.content)-char_length(replace(p.content,' ',''))+1) AS avg_words\nFROM authors a\nJOIN posts p ON p.author_id = a.id\nJOIN post_tags pt ON pt.post_id = p.id\nJOIN tags t ON t.id = pt.tag_id\nWHERE a.id = $1 AND p.created_at BETWEEN $2 AND $3\nGROUP BY t.name\nORDER BY t.name;\n```",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-13",
        "questionId": "q-16359",
        "question": "Given a PostgreSQL table events(user_id INT, event_type TEXT, created_at TIMESTAMP), design a beginner-friendly indexing plan to support: 1) fetch all events for a user within a date range, 2) retrieve the most recent event_type per user, 3) count daily events per user. Propose concrete index definitions (BTREE, composite and any HASH or partial indexes), explain column order choices, and how writes are affected by each index?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a single query with INNER JOINs across orders → order_items → products, grouping by products.category_id. Calculate revenue as SUM(oi.qty * oi.unit_price) and total_items as SUM(oi.qty). Filter on orders.customer_id…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a three-way join (plans → subscriptions → invoices) filtered by issued_at in the month and an active window on subscriptions, then group by plan. Example: SELECT p.name, SUM(i.amount) AS total_inv",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Add a composite BTREE index on (user_id, created_at) to support per-user date range queries and finding the most recent event. This column order is critical—user_id comes first because it's used for equality filtering,…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "To avoid inventory oversell, use MVCC with SERIALIZABLE or a FOR UPDATE lock. T1 reads stock, then inserts cart line; T2 checks out and decrements stock. With READ COMMITTED, phantom reads could allow",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests understanding of when composite BTREE indexes provide optimal performance, awareness of column ordering trade-offs, and knowledge of when alternative index types (HASH, partial) are appropriate. It also evaluates practical knowledge of how indexes impact write performance.\n\n## Key Concepts\n\n- Composite BTREE indexes: column order matters—equality conditions first, then range/sort columns\n- Hash indexes: only useful for equality lookups on large tables; rarely the right choice\n- Partial indexes: filter data at index time to reduce size and improve performance for specific query patterns\n- Write impact: each index adds overhead to INSERT, UPDATE, and DELETE operations\n\n## Code Example\n\n```sql\nCREATE TABLE events (\n    id BIGINT PRIMARY KEY,\n    user_id INT NOT NULL,\n    event_type VARCHAR(50) NOT NULL,\n    created_at TIMESTAMP NOT NULL\n);\n\n-- Primary index for user + date range queries\nCREATE INDEX idx_events_user_created ON events (user_id, created_at);\n\n-- Partial index for common event type filters\nCREATE INDEX idx_events_user_type_recent ON events (user_id, event_type, created_at)\nWHERE event_type IN ('login', 'purchase', 'logout');\n```",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-14",
        "questionId": "q-11507",
        "question": "A social media platform uses PostgreSQL for user data and posts, with a compound index on (user_id, created_at) for the posts table. As the platform grows, query performance degrades for two main patterns: fetching a user's recent posts and finding trending posts globally. How would you redesign the indexing strategy, what specific indexes would you create, and how would you handle index maintenance for high write throughput while avoiding index bloat?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create separate partial indexes for user-specific and global queries, implement composite indexes with selective columns, and adopt index maintenance strategies including concurrent index creation and periodic cleanup t…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "**Architecture Overview** **Redis (Hot Feature Cache)** - Key: `feat:{userId}:vector` (float32 array, 128–512 dimensions) - TTL: 5 minutes - Write-back policy: Asynchronous write on feature update via Kafka consumer; ba…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use the outbox pattern: within a single SERIALIZABLE Postgres transaction, insert both the event record and a corresponding outbox entry. A separate background worker polls the outbox, publishes the event to Redis via a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To guarantee at-most-one reservation per seat, use serializable transactions with row-level locking or a per-seat advisory lock. The pattern: BEGIN TRANSACTION, SELECT seat_id FROM seats WHERE seat_id = ? FOR UPDATE, ve…",
            "isCorrect": false
          }
        ],
        "explanation": "For user-specific queries, maintain the existing (user_id, created_at) index as a partial index that includes only active posts to reduce size. For trending posts, create a composite index on (engagement_score, created_at) where engagement_score is a computed column combining likes, comments, and shares with appropriate weighting. Consider implementing a BRIN index for the created_at column due to its natural ordering, which will improve time-range query efficiency. For global feed queries, create a covering index that includes commonly accessed columns to eliminate table lookups. All index maintenance should be performed using CREATE INDEX CONCURRENTLY to avoid blocking writes, and schedule regular cleanup operations to prevent index bloat and maintain optimal performance under high write throughput conditions.",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-15",
        "questionId": "q-26220",
        "question": "You have a normalized Postgres schema: customers(id PK, region, created_at), orders(id PK, customer_id FK, order_date, status), order_items(id PK, order_id FK, product_id, quantity, unit_price). For region='EMEA' and 2025-01-01..2025-12-31, return each product_id with total_revenue = sum(quantity*unit_price) and order_count = count(distinct orders.id). Provide the SQL, then propose an indexing strategy (composite/partial) to optimize this, and discuss normalization trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a composite B-tree index on (customerId, status, createdAt DESC) for the first query, and a BRIN index on createdAt for the time-series aggregation query, while considering the write overhead implications of each…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a time-aligned partitioning strategy that maps the 30-day window to a small set of partitions; add a BRIN index on log_ts to speed partition pruning and a composite BTREE index on (event_type, event_value DESC) INCL…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Redis-based advisory lock (SETNX with TTL) to gate a stock decrement in MongoDB, then append an event to Cassandra and a summary entry in DynamoDB, all guarded by idempotent writes and a compens",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "SQL: SELECT oi.product_id, SUM(oi.quantity*oi.unit_price) AS total_revenue, COUNT(DISTINCT o.id) AS order_count FROM customers c JOIN orders o ON o.customer_id=c.id JOIN order_items oi ON oi.order_id=",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to craft joined queries over normalized tables and design targeted indexes for aggregations, a common beginner task with real-world relevance.\n\n## Key Concepts\n- Joins across normalized tables\n- Composite and partial indexes to support filters and grouping\n- Normalization vs query performance trade-offs\n\n## Code Example\n```javascript\nSELECT oi.product_id, SUM(oi.quantity*oi.unit_price) AS total_revenue, COUNT(DISTINCT o.id) AS order_count\nFROM customers c\nJOIN orders o ON o.customer_id=c.id\nJOIN order_items oi ON oi.order_id=o.id\nWHERE c.region='EMEA' AND o.order_date BETWEEN '2025-01-01' AND '2025-12-31'\nGROUP BY oi.product_id;\n```\n\n## Follow-up Questions\n- How would the query change if region filtering moved to a separate denormalized summary table?\n- How would you verify correctness and performance using EXPLAIN ANALYZE on a realistic dataset?",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-16",
        "questionId": "q-9397",
        "question": "In a Delta Lake table on Databricks, partitioned by date and region with billions of rows, a read analytics query filters a date range and region and aggregates by product_id, joining a small dim table. Explain the physical plan focusing on partition pruning, predicate pushdown, and join strategy. Propose partitioning, plan verification, and skew handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a multi-layered indexing strategy that balances query performance with write overhead. Start with a composite index on the most selective attributes that are commonly used together—typically (category,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "The physical plan demonstrates effective partition pruning on both date and region columns, with predicate pushdown directly into Delta Lake's storage layer. The small dimension table triggers a BroadcastHashJoin to avo…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use the existing (user_id, created_at) composite index for user timeline queries, add a separate (event_type, created_at) composite index for analytics queries, and consider a (user_id, event_type, created_at) composite…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Composite indexes are optimal for queries that filter on multiple columns in a specific order, while single-column indexes work better for independent column queries.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical expertise in query optimization, partitioning strategy, and performance tuning within modern lakehouse architectures using Delta Lake and Databricks.\n\n## Key Concepts\n\n- Partition pruning and selective data elimination\n- Predicate pushdown to storage layer\n- Dynamic partition pruning for runtime optimizations\n- Broadcast join versus shuffle join strategies\n- Data clustering techniques (Z-ordering)\n- Skew detection and mitigation approaches\n\n## Code Example\n\n```javascript\n// Analyze and display the physical execution plan\nconst plan = spark.sql(\n  `SELECT f.product_id, SUM(f.amount) as total_amount\n   FROM facts f\n   JOIN dim_products d ON f.product_id = d.product_id\n   WHERE f.date BETWEEN '2024-01-01' AND '2024-01-31'\n     AND f.region = 'US'\n   GROUP BY f.product_id`\n).explain(true)\n```",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-17",
        "questionId": "q-397",
        "question": "In a high-transaction payment system using PostgreSQL, how would you design a transaction isolation strategy to prevent lost updates while maintaining high concurrency for account transfers?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use SERIALIZABLE for the risk path to prevent write skew and phantom reads. Lock the involved rows with SELECT FOR UPDATE in the transfer path; if SERIALIZABLE hurts throughput, use FOR UPDATE on affe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a combination of specialized indexes to address each query pattern. For full-text search on title and content with relevance ranking, create a tsvector column with a GIN index and use ts_rank_cd for relevance scorin…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use SERIALIZABLE isolation with explicit row-level locking and retry logic for account transfers to prevent lost updates while maintaining concurrency.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create a composite index on (customer_id, status, created_at) with columns in order of selectivity - most selective first - to allow the database to filter most effectively using the index.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nPayPal needs to ensure financial transaction integrity under high load. This tests understanding of ACID properties, isolation levels, and practical database design for financial systems.\n\n## Expected Answer\nStrong candidates will discuss:\n- SERIALIZABLE vs REPEATABLE READ trade-offs\n- Explicit row-level locking with SELECT FOR UPDATE\n- Deadlock detection and retry mechanisms\n- Connection pooling and transaction timeout handling\n- Monitoring for serialization failures\n\n## Code Example\n```typescript\nasync function transferFunds(fromId: number, toId: number, amount: number) {\n  const maxRetries = 3;\n  \n  for (let attempt = 1; attempt <= maxRetries; attempt++) {\n    const tx = await db.transaction({ isolationLevel: 'serializable' });\n    \n    try {\n      // Lock both accounts to prevent concurrent modifications\n      const [fromAccount, toAccount] = await Promise.all([\n        tx.query('SELECT balance FROM accounts WHERE id = $1 FOR UPDATE', [fromId]),\n        tx.query('SELECT balance FROM accounts WHERE id = $1 FOR UPDATE', [toId])\n      ]);\n      \n      if (fromAccount.balance < amount) {\n        await tx.rollback();\n        throw new Error('Insufficient funds');\n      }\n      \n      await Promise.all([\n        tx.query('UPDATE accounts SET balance = balance - $1 WHERE id = $2', [amount, fromId]),\n        tx.query('UPDATE accounts SET balance = balance + $1 WHERE id = $2', [amount, toId])\n      ]);\n      \n      await tx.commit();\n      return;\n    } catch (error) {\n      await tx.rollback();\n      if (error.code === '40001' && attempt < maxRetries) {\n        // Serialization failure, retry with exponential backoff\n        await new Promise(resolve => setTimeout(resolve, Math.pow(2, attempt) * 100));\n        continue;\n      }\n      throw error;\n    }\n  }\n}\n```\n\n## Follow-up Questions\n- How would you handle distributed transactions across multiple databases?\n- What monitoring metrics would you track for transaction performance?\n- How would you optimize this for thousands of concurrent transfers?",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-18",
        "questionId": "q-5486",
        "question": "Scenario: global messaging app uses MongoDB for profiles, Redis for presence and ephemeral typing indicators, Cassandra for chat history, and DynamoDB for indexing/archival. At 20k writes/sec with multi-region writes, design an ephemeral 'typing' indicator system: TTL ~5s, per-conversation visibility, idempotent writes, and failover handling. Include concrete data models, TTLs, invalidation strategy, and how to avoid cross-region staleness during outages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a BTREE index on lower(username) for case-insensitive exact lookups, and a composite BTREE index on (country, status, signup_date DESC) to support range queries and top-N by signup_date within",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition by month (range) on event_time. On each partition, create a composite index on (user_id, event_time) covering event_type. Write filters with event_time >= current_date - interval '90 days' a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "MongoDB stores flexible JSON-like documents allowing varying schemas per user, ideal for profiles with optional fields. SQL uses fixed table rows with predefined columns, ensuring consistency but requiring schema migrat…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Redis for typing indicators: keys `typing:<conv_id>:<user_id>` with a 5s TTL; publish updates on `typing:<conv_id>` channel for real-time rendering. Record lightweight events in Cassandra for analytics.",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis explores ephemeral real-time signals across multiple stores, with TTL, invalidation, and failover in a global, multi-region setup.\n\n## Key Concepts\n\n- Ephemeral TTL data in Redis\n- Cross-store data consistency and latency\n- Pub/Sub and per-conversation channels\n- Regional failover and reconciliation\n\n## Code Example\n\n```javascript\n// Redis: set typing with TTL\nSETEX typing:${convId}:${userId} 5 1\n// Notify listeners\nPUBLISH typing:${convId} \"${userId}:typing\"\n```\n\n## Follow-up Questions\n\n- How to handle muted users or offline clients?\n- How to reconcile typing state during region recovery?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-19",
        "questionId": "q-6459",
        "question": "You have a Postgres table 'sales' partitioned by month on sale_date. A query filters by a date range and region, but EXPLAIN shows a Seq Scan across all partitions. How would you diagnose and optimize for partition pruning and indexing to speed the aggregation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Redis: flag:<id>:live:<region> -> boolean, TTL 300s. MongoDB: flags { _id, name, defaultValue, strategy }. Cassandra: evals (user_id, flag_id, ts, result). DynamoDB: rollouts (user_id, flag_id, result",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use composite indexes with high-cardinality columns first (customer_id), include frequently accessed columns for covering indexes, and consider partial indexes for common query patterns. Avoid indexing low-cardinality c…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use partition pruning on occurred_at, join tenants for region, and filter via JSONB index on details. Per-partition index: CREATE INDEX logs_details_completed ON logs USING GIN (details) WHERE details",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use EXPLAIN (ANALYZE, BUFFERS) to confirm partition pruning via the sale_date predicate. If partitions aren't being pruned, ensure the partition key is used directly without functions and that the date bounds align with…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests your understanding of partition pruning and how predicates on the partition key influence which partitions are scanned. It also assesses your ability to verify execution plans and apply practical indexing strategies on partitioned tables.\n\n## Key Concepts\n\n- Partition pruning\n- Predicate pushdown on partition keys\n- EXPLAIN (ANALYZE, BUFFERS) for plan verification\n- Per-partition indexes in partitioned tables\n\n## Code Example\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT customer_id, SUM(amount) AS total_amount\nFROM sales\nWHERE sale_date >= DATE '2025-03-01' \n  AND sale_date < DATE '2025-04-01'\n  AND region = 'EMEA';\n```",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-20",
        "questionId": "q-28659",
        "question": "Scenario: A B2B SaaS platform stores transactional data in PostgreSQL with 10,000 tenants sharing the same schema. Each tenant can have 1M-100M rows, and queries always filter by tenant_id. You observe: some tenants run analytics on date ranges, others on status/enum columns, and some rarely query but need retention. Explain your indexing strategy: 1) How would you design composite indexes considering the tenant-first pattern? 2) When and how would you use partial indexes for sparse columns? 3) How would you handle index bloat from heavy UPDATE workloads on certain tables? 4) What monitoring would you implement to detect missing or unused indexes? 5) How would your strategy change if tenants could define custom fields (polymorphic data) stored as JSONB?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-store write path: Redis key progress:u{userId}:v{videoId} with fields {watchPercent,lastViewedAt}, TTL 1h. MongoDB: users.{userId}.videos: {videoId,watchPercent,lastViewedAt}. Cassandra",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest audit events into Redis with key `audit:{event_id}` for fast-path reads (60s TTL). Write full documents to MongoDB collection `audit` with TTL index on `ts` field (30-day TTL). In Cassandra, use `date` as partiti…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use tenant_id as the leading column in all composite indexes; employ partial indexes for sparse data patterns; manage index bloat with BRIN indexes for time-series data, fillfactor settings, and regul",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "SERIALIZABLE isolation; per-row versioning via a version column. Atomic reserve: BEGIN; SET TRANSACTION ISOLATION LEVEL SERIALIZABLE; UPDATE stock SET quantity = quantity - 1, version = version + 1 WH",
            "isCorrect": false
          }
        ],
        "explanation": "For a multi-tenant PostgreSQL schema, the tenant_id must be the leading column in all composite indexes to enable partition pruning and efficient index scans per tenant. For example, an index on (tenant_id, created_at) for date-range analytics, or (tenant_id, status) for enum-based filtering. The composite index order matters: equality conditions (tenant_id, status) should precede range conditions (created_at) only if you have queries filtering by tenant_id and status without a date range. If queries always include both tenant_id and created_at, a composite on (tenant_id, created_at) with INCLUDE for frequently selected columns can serve as a covering index, eliminating heap fetches.\n\nPartial indexes are ideal for sparse data where only a fraction of rows match a condition. For instance, if 5% of orders have status='disputed', a partial index on (tenant_id, created_at) WHERE status = 'disputed' is dramatically smaller than a full composite index. This reduces index maintenance overhead and improves cache efficiency. However, partial indexes require careful cardinality analysis per tenant—if different tenants have wildly different distributions, consider tenant-specific indexes or partition the data by tenant.\n\nIndex bloat from heavy UPDATE workloads (especially with ON CONFLICT DO UPDATE patterns) accumulates dead tuples in B-tree indexes. Mitigation strategies include: setting fillfactor=70-80 on frequently updated tables to leave space for in-place updates; using BRIN indexes for time-series data where physical row order correlates with created_at (BRIN indexes are tiny and self-maintaining); scheduling REINDEX CONCURRENTLY during low-traffic windows; and monitoring with pg_stat_user_indexes to identify indexes with high dead tuple ratios.\n\nFor monitoring, query pg_stat_user_indexes to identify never-used indexes (idx_scan = 0), which waste write overhead. Use pg_stat_statements to correlate high execution time with missing index opportunities. Track index size growth over time and alert on bloat >2x the expected size. For unused indexes on hot tables, measure the write overhead savings before dropping.\n\nFor polymorphic JSONB custom fields, avoid indexing JSONB paths directly on every row. Instead, create expression indexes only for known high-value paths (e.g., CREATE INDEX ON orders ((data->>'customRegion'))) filtered by tenant_id. For truly dynamic fields, consider GIN indexes with jsonb_path_ops for containment queries, but note they cannot use tenant_id as leading column. A hybrid approach: maintain a materialized view of commonly indexed custom fields with a tenant_id column, index that, and route queries through the view.",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-21",
        "questionId": "q-12368",
        "question": "PostgreSQL interview: given a 100M-row events table partitioned by month (range) joined to a large user_dim, a query filters the last 6 months and aggregates by event_type. EXPLAIN ANALYZE shows Seq Scan on events and a naive join. Propose a concrete partitioning and indexing plan to enable partition pruning and fast joins, plus a validation plan using EXPLAIN ANALYZE?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Write a query joining trips to regions, filter by pickup_time >= now() - interval '24 hours', and group by region name to compute counts, sums, and averages. Index suggestions: trips(pickup_time), trips(region_id), or a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition pruning scans only partitions whose date ranges overlap the 30‑day window. In EXPLAIN ANALYZE you should see a PartitionRangeScan or Bitmap Heap/Index scans limited to the relevant partition",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "The physical plan demonstrates effective partition pruning on both date and region columns, with predicate pushdown directly into Delta Lake's storage layer. The small dimension table triggers a BroadcastHashJoin to avo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition the events table by month using range/interval partitions to enable pruning for the last 6 months. Create a composite index on (user_id, event_ts, event_type) to support both the filter conditions and aggregat…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses your mastery of partition pruning and EXPLAIN ANALYZE interpretation in real-world workloads. It tests your ability to design effective partitioned layouts, select appropriate indexing strategies, and validate performance improvements systematically.\n\n## Key Concepts\n\n- EXPLAIN plans and partition pruning mechanisms\n- Range vs. interval partitioning strategies\n- Composite indexes optimizing filters and aggregations\n- Performance validation using EXPLAIN ANALYZE\n\n## Code Example\n\n```sql\n-- Create monthly partitioned table\nCREATE TABLE events (\n    id BIGSERIAL,\n    user_id BIGINT,\n    event_ts TIMESTAMP,\n    event_type VARCHAR(50),\n    event_data JSONB\n) PARTITION BY RANGE (event_ts);\n\n-- Create composite index on each partition\nCREATE INDEX CONCURRENTLY events_composite_idx \nON events (user_id, event_ts, event_type);\n```\n\n## Follow-up Questions\n\n- How would you handle skewed partition sizes and ongoing index maintenance?\n- What strategies would you use to measure and optimize planning time versus execution time in production?",
        "difficulty": "intermediate"
      },
      {
        "id": "database-q-22",
        "questionId": "q-21309",
        "question": "In a gaming app where Redis stores session keys (session:<user_id> with TTL), MongoDB stores user profiles, Cassandra holds hourly telemetry, and DynamoDB stores weekly summaries, design a cross-store delete flow for a user_id (GDPR-like delete): specify concrete delete/write paths, tombstone handling, TTL decisions, and how to ensure reads exclude deleted data while keeping operations idempotent and auditable?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Under READ COMMITTED isolation, both transactions can read the initial balance of 100 concurrently, leading to a lost update anomaly. Transaction T1 subtracts 60 (resulting balance: 40) and Transaction T2 subtracts 40 (…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Clustered indexes physically reorder data (1 per table) - ideal for range queries and primary keys. Non-clustered indexes store pointers to data (many per table) - better for selective lookups and covering queries. Clus…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "SQL: SELECT p.name AS patron, b.title AS book, a.name AS author, l.due_date FROM loans l JOIN patrons p ON p.id=l.patron_id JOIN books b ON b.id=l.book_id JOIN authors a ON a.id=b.author_id WHERE l.re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Execute an idempotent, cross-store delete flow for user_id across all four data stores: **Redis**: Issue `DEL session:<user_id>` to immediately invalidate the session key. Since Redis keys have TTL, the operation is sel…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates candidates on designing distributed data deletion across polyglot persistence architectures—a critical skill for compliance with GDPR, CCPA, and similar regulations. It tests understanding of eventual consistency, cross-store coordination, and the tradeoffs between soft deletes, tombstones, and hard deletes.\n\n## Key Concepts\n- **Cross-store consistency**: Coordinating deletes across heterogeneous data stores with different consistency models\n- **Idempotence**: Ensuring delete operations can be safely retried without side effects\n- **Soft delete vs. tombstones**: MongoDB uses soft deletes (timestamp-based), Cassandra uses tombstones (marked for garbage collection), Redis and DynamoDB use hard deletes\n- **Audit logging**: Maintaining an immutable trail of deletion requests for compliance\n- **Retention and purge**: Understanding TTL behavior and compaction cycles for data expiration\n\n## Code Example\n```javascript\nconst { v4: uuidv4 } = require('uuid');\n\nasync function deleteUser(userId, initiatedBy) {\n  const requestId = uuidv4();\n  const timestamp = new Date();\n\n  // Idempotency check\n  const existingRequest = await auditLog.findOne({ requestId });\n  if (existingRequest) {\n    return { status: 'already_completed', requestId };\n  }\n\n  // Parallel deletes across stores\n  const [redisResult, mongoResult, cassandraResult, dynamoResult] = await Promise.all([\n    redis.del(`session:${userId}`),\n    mongo.updateOne(\n      { userId },\n      { $set: { deleted_at: timestamp, private_fields: {}, deletion_request_id: requestId } }\n    ),\n    cassandra.execute('DELETE FROM telemetry WHERE user_id = ?', [userId]),\n    dynamodb.deleteItem({\n      TableName: 'WeeklySummaries',\n      Key: { userId: { S: userId } }\n    }).promise()\n  ]);\n\n  // Audit log (append-only)\n  await auditLog.insertOne({\n    requestId,\n    userId,\n    timestamp,\n    initiatedBy,\n    stores_affected: ['redis', 'mongodb', 'cassandra', 'dynamodb'],\n    status: 'completed'\n  });\n\n  return { status: 'completed', requestId };\n}\n\n// Read filter example\nasync function getUserProfile(userId) {\n  const user = await mongo.findOne({ userId, deleted_at: null });\n  if (!user) return null;\n  return user;\n}\n```\n\n## Additional Considerations\n- **Eventual consistency**: Cassandra and DynamoDB may have brief windows where deleted data is still readable. Implement read-your-writes consistency or delay verification.\n- **TTL coordination**: Set application-level TTLs that exceed the longest store's garbage collection period to ensure complete deletion.\n- **Failure handling**: Use a saga pattern or distributed transaction log to track partial failures and implement compensating actions.",
        "difficulty": "beginner"
      },
      {
        "id": "database-q-23",
        "questionId": "q-30303",
        "question": "Scenario: In a PostgreSQL-based payout pipeline using MVCC, nightly batches compute per-driver payouts from unsettled rides and update a driver balance and a payout_ledger. Batches run concurrently and can touch the same driver. How would you guarantee correctness with minimal aborts? Compare SERIALIZABLE vs SSI, when to use row-level SELECT FOR UPDATE, and propose concrete schemas and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Prefer Serializable Snapshot Isolation (SSI) to avoid full abort storms at high concurrency, or SERIALIZABLE when you need strict guarantees. Use per-driver row locking (SELECT FOR UPDATE) or an expli",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose: 1) B-tree index on (city, start_time) for per-city time windows; 2) B-tree on (driver_id, start_time) for driver-centric recent trips; 3) hash index on (status) for fast equality; 4) composit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a composite B-tree index on (event_type, user_id, created_at) with INCLUDE (resource_id) for covering reads, and a separate hash index on (user_id) for fast existence checks. Add a partial index o",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest audit events into Redis with key `audit:{event_id}` for fast-path reads (60s TTL). Write full documents to MongoDB collection `audit` with TTL index on `ts` field (30-day TTL). In Cassandra, use `date` as partiti…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nTests understanding of MVCC, anomalies, and cross-batch correctness in a high-concurrency payout workflow.\\n\\n## Key Concepts\\n- MVCC, SERIALIZABLE, SSI\\n- Row-level locking, SELECT FOR UPDATE\\n- Idempotent writes, upserts, optimistic locking, version/timestamp columns\\n- Long-running transactions and abort rates\\n\\n## Code Example\\n```sql\\nBEGIN;\\nSET TRANSACTION ISOLATION LEVEL SERIALIZABLE;\\nSELECT balance, version FROM drivers WHERE id = :driver_id FOR UPDATE;\\n-- compute new_balance using :amount and existing balance/version\\nUPDATE drivers SET balance = new_balance, version = version + 1 WHERE id = :driver_id;\\nINSERT INTO payout_ledger(driver_id, amount, tstamp) VALUES (:driver_id, :amount, NOW());\\nCOMMIT;\\n```\\n\\n## Follow-up Questions\\n- How would you test for write skew in this setup?\\n- When would you introduce compensating transactions if an abort occurs?",
        "difficulty": "advanced"
      },
      {
        "id": "database-q-24",
        "questionId": "q-23363",
        "question": "Scenario: A delivery service stores driver profiles in MongoDB, live location in Redis geospatial index, 24h trip logs in Cassandra, and daily performance summaries in DynamoDB. For a real-time 'Nearby Drivers' feature, design concrete write paths and keys, TTLs, and cross-store consistency rules. Explain how to query the nearest 10 drivers quickly and how to refresh Redis cache from MongoDB/Cassandra as needed?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Redis GEO index for live locations with TTL 30s. On update: GEOADD drivers:geo and write to MongoDB (drivers collection) as truth; Cassandra holds 24h trips; DynamoDB stores daily summaries. Rea",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use composite B-tree indexes with high-selectivity (low-cardinality) columns first, followed by low-selectivity (high-cardinality) columns. For range predicates like order_date, position these after equality columns. Ad…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To implement an atomic funds transfer from account A to account B in PostgreSQL under high concurrency, use a single transaction with row-level locking: 1. Begin transaction 2. Lock both account rows using SELECT ... FO…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "The physical plan demonstrates effective partition pruning on both date and region columns, with predicate pushdown directly into Delta Lake's storage layer. The small dimension table triggers a BroadcastHashJoin to avo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical familiarity with multi-datastore patterns, real-time geo queries, TTL-driven cache, and read-fallback strategies across systems.\n\n## Key Concepts\n\n- Redis GEO with TTLs for live proximity data\n- MongoDB as source of truth for profiles\n- Cassandra for 24h time-series traces\n- DynamoDB for daily summaries and analytics\n- Read-time fallback paths and cache refresh mechanics\n\n## Code Example\n\n```javascript\n// Redis write (pseudo)\nredisClient.geoadd('drivers:geo', longitude, latitude, driverId);\nredisClient.expire('drivers:geo', 30);\n\n// Read near drivers (pseudo)\nconst nearby = redisClient.georadius('drivers:geo', lon, lat, 5, 'km', 'WITHDIST', 'COUNT', 10);\n```\n\n## Follow-up Questions\n\n- How would you test correctness with synthetic location data and TTLs?\n- How would you scale Redis for high write throughput and geo queries across regions?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-11T10:03:53.450Z",
    "version": 1
  }
]