[
  {
    "id": "test-cnf-certification",
    "channelId": "cnf-certification",
    "channelName": "CNF Certification",
    "title": "CNF Certification Knowledge Test",
    "description": "Test your understanding of CNF Certification with 25 questions.",
    "questions": [
      {
        "id": "cnf-certification-q-0",
        "questionId": "q-22539",
        "question": "CNF certification: In a multi-tenant CNF gateway platform on Kubernetes, design a per-tenant canary-based upgrade workflow using Argo Rollouts. Each tenant defines its rollout steps, analysis window, and auto-promotion/rollback rules. Describe traffic isolation, per-tenant metrics collection, and policy enforcement. Include minimal CRD sketches for Rollout/AnalysisRun and a one-namespace test manifest to validate canary promotion without cross-tenant impact?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant canary by hashing the tenantID plus path to a 0–99 value; if cnf.canary.enabled is true and value < cnf.canary.percent, route to upstream-canary, else upstream-prod. Config via",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a per-tenant canary using Istio's VirtualService with tenant-scoped weights, split 5-10% to a canary deployment, while 90-95% traffic goes to prod. Enforce tenant isolation via SPIFFE IDs and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Argo Rollouts to manage tenant-scoped canaries by attaching a tenant label to each Rollout and routing traffic with per-tenant subsets via the service mesh. Define per-tenant AnalysisRun templates using Prometheus t…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant CA in each namespace, CSR in the tenant namespace, approve via Kubernetes CSR API, issue short-lived client cert, mount cert as secret into the gateway sidecar at /etc/certs, configure the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world upgrades in multi-tenant CNF platforms require safe, isolated releases. This tests your ability to design rollout strategies, implement metrics-based promotion decisions, and enforce cross-tenant risk controls.\n\n## Key Concepts\n- Argo Rollouts canary strategy scoped per tenant\n- Tenant isolation via labels and service mesh subsets\n- Per-tenant AnalysisRun with Prometheus-based SLAs\n- Rollback and auto-promotion guardrails\n- Minimal CRD sketches and namespace-scoped validation\n\n## Code Example\n```yaml\napiVersion: argoproj.io/v1alpha1\nkind: Rollout\nmetadata:\n  name: gateway-rollout-tenant-a\n  labels:\n    tenant: a\nspec:\n  replicas: 3\n  strategy:\n    canary:\n      steps:\n        - setWeight: 10\n        - analysis:\n            templates:\n              - templateName: tenant-a-analysis\n        - setWeight: 30\n        - analysis:\n            templates:\n              - templateName: tenant-a-analysis\n        - setWeight: 100\n      trafficRouting:\n        nginx:\n          stableIngress: gateway-ingress\n          additionalIngressAnnotations:\n            canary-by-header: X-Tenant-ID\n      analysis:\n        successfulRunHistoryLimit: 3\n        unsuccessfulRunHistoryLimit: 3\n```\n\n## Per-Tenant AnalysisRun CRD Sketch\n```yaml\napiVersion: argoproj.io/v1alpha1\nkind: AnalysisTemplate\nmetadata:\n  name: tenant-a-analysis\nspec:\n  args:\n    - name: tenant-id\n  metrics:\n    - name: latency-sli\n      interval: 1m\n      successCondition: result[0] <= 100\n      failureLimit: 3\n      provider:\n        prometheus:\n          address: http://prometheus:9090\n          query: |-\n            histogram_quantile(0.99,\n              sum(rate(gateway_request_duration_seconds_bucket{tenant=\"{{args.tenant-id}}\"}[5m]))\n              by (le))\n    - name: error-rate-sli\n      interval: 1m\n      successCondition: result[0] <= 0.01\n      failureLimit: 3\n      provider:\n        prometheus:\n          address: http://prometheus:9090\n          query: |-\n            sum(rate(gateway_requests_total{tenant=\"{{args.tenant-id}},status=~\"5..\"}[5m]))\n            /\n            sum(rate(gateway_requests_total{tenant=\"{{args.tenant-id}}\"}[5m]))\n```\n\n## One-Namespace Test Manifest\n```yaml\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: tenant-a-canary-test\n  labels:\n    tenant: a\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: gateway-stable\n  namespace: tenant-a-canary-test\nspec:\n  ports:\n    - port: 80\n      targetPort: 8080\n  selector:\n    app: gateway\n    role: stable\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: gateway-canary\n  namespace: tenant-a-canary-test\nspec:\n  ports:\n    - port: 80\n      targetPort: 8080\n  selector:\n    app: gateway\n    role: canary\n---\napiVersion: argoproj.io/v1alpha1\nkind: Rollout\nmetadata:\n  name: gateway-rollout\n  namespace: tenant-a-canary-test\n  labels:\n    tenant: a\nspec:\n  replicas: 2\n  selector:\n    matchLabels:\n      app: gateway\n  template:\n    metadata:\n      labels:\n        app: gateway\n    spec:\n      containers:\n        - name: gateway\n          image: gateway:v2\n          ports:\n            - containerPort: 8080\n  strategy:\n    canary:\n      canaryService: gateway-canary\n      stableService: gateway-stable\n      trafficRouting:\n        service:\n          canaryWeight: 10\n      steps:\n        - setWeight: 10\n        - pause: {duration: 5m}\n        - setWeight: 30\n        - pause: {duration: 5m}\n        - setWeight: 100\n```\n\n## Traffic Isolation Design\n- **Service Mesh Subsets**: Each tenant gets dedicated subsets (stable/canary) in the mesh, ensuring zero cross-tenant traffic bleed\n- **Tenant Labels**: All resources (Rollouts, Services, AnalysisRuns) are labeled with tenant ID for RBAC and policy enforcement\n- **Network Policies**: Kubernetes NetworkPolicies restrict pod-to-pod communication to same-tenant pods only\n- **Ingress Routing**: Use header-based routing (X-Tenant-ID) at the ingress to route to tenant-specific services\n\n## Per-Tenant Metrics Collection\n- Deploy tenant-scoped Prometheus instances or use Prometheus tenants\n- Configure recording rules per tenant for SLI computation\n- Metrics queries always include tenant filter to ensure isolation\n- Store metrics with tenant label for multi-tenant querying and alerting\n\n## Policy Enforcement\n- **OPA/Gatekeeper**: Enforce that Rollouts must have tenant label and cannot modify other tenants' resources\n- **Kubernetes RBAC**: Bind roles to tenant groups, limiting access to tenant-specific namespaces\n- **Argo Rollouts RBAC**: Restrict Rollout/AnalysisTemplate access to tenant admins only",
        "difficulty": "intermediate"
      },
      {
        "id": "cnf-certification-q-1",
        "questionId": "q-20119",
        "question": "In a multi-tenant CNF gateway platform on Kubernetes, design a tenant-scoped rolling canary upgrade for a data-plane CNF that preserves tenant isolation during upgrades. Describe how to route a per-tenant canary using a service mesh with weighted traffic to a new image tag, gate per-tenant features via a central config service, and rollback without cross-tenant impact. Include minimal CRD snippets and a one-namespace manifest to validate end-to-end and discuss observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline offline provenance: generate SBOM with Syft for the CNF image, sign with Cosign using offline KMS key, attach SBOM, create a portable manifest with image_digest, sbom_digest, nonce, build_id,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use OPA Gatekeeper: ConstraintTemplate 'CNFNamespaceSecurity' enforcing hostNetwork/hostPID disallowance, readOnlyRootFilesystem required, and non-privileged containers. Create a Constraint for namesp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define per-tenant destination subsets and virtual routes in Istio, keyed by X-TENANT-ID. Example: route 90/10 to tenant-a-stable and tenant-a-canary; feature flags fetched from a central config servic",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Vault PKI to issue per-tenant certs stored as Kubernetes Secrets; gateway TLS termination is SNI-based per tenant domain, with mTLS using a tenant-specific CA bundle. Route to tenant upstreams, en",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests ability to design safe, scalable per-tenant canaries in CNFs, integrating service mesh routing, feature flag gating, and rollback without affecting other tenants.\n\n## Key Concepts\n- Canary deployments per tenant using destination subsets\n- Per-tenant feature gating via central config service\n- Strict isolation and observability\n\n## Code Example\n```yaml\n# Minimal Istio VirtualService per tenant\napiVersion: networking.istio.io/v1beta1\nkind: VirtualService\nmetadata:\n  name: gateway-tenant-a\nspec:\n  hosts:\n  - gateway.example.com\n  http:\n  - route:\n    - destination:\n        host: gateway\n        subset: tenant-a-stable\n      weight: 90\n    - destination:\n        host: gateway\n        subset: tenant-a-canary\n      weight: 10\n```\n\n## Follow-up Questions\n- How would you automate per-tenant subset creation as new tenants join?\n- How would you validate no cross-tenant data exposure during the canary?",
        "difficulty": "advanced"
      },
      {
        "id": "cnf-certification-q-2",
        "questionId": "q-7539",
        "question": "Beginner CNF certification: In a multi-tenant CNF gateway cluster on Kubernetes, design a tamper-evident audit trail for per-tenant image provenance and config changes. Include (1) an append-only log sink for Cosign signatures and SBOMs, (2) an admission webhook emitting tenant-scoped events on image deploys and config updates, (3) a per-tenant verifier and read-only API, and (4) minimal manifests and verification steps to validate in one namespace before rollout. Provide concrete YAMLs and commands?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Store all provenance and config events as append-only JSONL in a tenant-scoped log store, each entry signed with the tenant key. Emit events via a small controller when image pulls or config changes o",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a sidecar log-router that reads TENANT_ID from the namespace, forwards logs to a TLS-secured sink at /logs/{TENANT_ID} using mTLS with a client cert from a Secret. Use a ConfigMap to set SINK_H",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use cert-manager to issue per-tenant TLS certs, driven by a TenantIssuer in each Namespace that chains to a central root CA. The CNF sidecar mounts the issued Secret as tls.crt and tls.key and configu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a runtime attestation gate for CNFs across core and edge by issuing TPM-backed, SPIFFE ID bound attest tokens that admission controllers verify before pod startup. Use a policy as code (Kyve",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design an auditable, tamper-evident workflow across tenants, focusing on practical components (append-only logs, signatures, per-tenant verifiers, read-only APIs) rather than theory.\n\n## Key Concepts\n- Append-only audit logs for provenance\n- Cosign signatures and SBOMs per tenant\n- Tenant-scoped verifiers and read-only audit API\n- Admission webhook triggering on deploys/config changes\n- Minimal manifests and concrete verification steps\n\n## Code Example\n```javascript\n// Example log entry (JSONL)\n{\"tenant\":\"tenant-a\",\"event\":\"image-pull\",\"image\":\"registry.example/tenant-a/app@sha256:abcdef...\",\"sig\":\"base64sig\",\"sbomDigest\":\"sha256:1234...\",\"ts\":\"2026-01-26T12:00:00Z\"}\n```\n\n## Follow-up Questions\n- How would you test log tampering resistance and key rotation strategies?\n- How would you scale the per-tenant audit API under high churn of tenants and images?",
        "difficulty": "beginner"
      },
      {
        "id": "cnf-certification-q-3",
        "questionId": "q-28966",
        "question": "In a multi-tenant CNF gateway platform on Kubernetes, design a per-tenant chaos testing framework that validates fault isolation and recovery guarantees. How would you implement tenant-scoped chaos experiments, choose tooling, metrics, and rollback strategy; include a minimal manifest example to run one tenant test?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Chaos Mesh to run tenant-scoped experiments in each CNF gateway namespace. Create a Canary ChaosExperiment targeting the tenant’s namespace via labels (app=cnf-gateway, tenant=<name>). Simulate po",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "After deployment, the CNF executes an attestation generator that creates a tenant-scoped signed attestation containing: tenant identifier, build-id, image-digest, SBOM digest, runtime-hash, and timestamp. The attestatio…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement runtime attestation: pull the image digest and Cosign-signed SBOM from provenance, verify the Cosign signature with a KMS-backed key, and validate the SBOM against the tenant's policy. Check",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Isolate the namespace and pause workload updates. Collect immutable provenance: Cosign signature, SBOM, TPM/IMA attestations, registry audit logs, and Kubernetes manifests. Reconstruct the incident ti",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical knowledge of chaos engineering in CNF gateways, focusing on per-tenant isolation, safe rollback, and production-readiness. It tests ability to select tooling, enforce namespace scoping, and define measurable SLIs.\n\n## Key Concepts\n- Tenant-scoped chaos experiments in Kubernetes namespaces\n- Chaos Mesh or similar tools for controlled fault injection\n- Per-tenant SLIs (latency, error rate, availability) and Prometheus-based dashboards\n- Namespace RBAC, CRD scoping, and blast-radius controls\n- Recovery strategy: automatic rollback or controlled progression via canary\n\n## Code Example\n```yaml\napiVersion: chaosmesh.org/v1alpha1\nkind: ChaosExperiment\nmetadata:\n  name: tenant-a-pod-chaos\n  namespace: tenant-a\nspec:\n  definitionRef:\n    name: pod-chaos\n  duration: 30s\n  selector:\n    namespaces:\n      - tenant-a\n    labelSelectors:\n      - app=cnf-gateway\n      - tenant=tenant-a\n```\n\n## Follow-up Questions\n- How would you scale this to 100+ tenants while keeping blast radius minimal?\n- How would you validate data-plane continuity during chaos events (packet loss, latency spikes)?\n- What metrics would you surface in Grafana, and how would you alert on SLA breaches?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnf-certification-q-4",
        "questionId": "q-2262",
        "question": "How would you implement an end-to-end runtime-provenance strategy for upgrades in a three-region, air-gapped CNF platform hosting stateful gateways across Kubernetes clusters, without pulling new images from the registry? Include image provenance checks, TPM-backed attestation via Sigstore, in-cluster drift detection with eBPF, and a safe, low-downtime rollback plan with minimal manifests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a four-layer flow: (1) require Cosign-signed CNF images with SBOMs and a rollback tag; (2) enforce TPM-backed attestation via Sigstore (Fulcio/ Rekor) inside each cluster; (3) run in-cluster dri",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-tenant canary deployment in a single namespace: deploy gateway-tenantA-canary (1 pod) alongside gateway-tenantA baseline (N pods), configure Istio VirtualService to route 10% traffic to canary and 90% to b…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a TenantAuthPolicy CRD with fields: allowedMethods, defaultMethod, methodOrder. Gateway sidecar caches policy per tenant, and executes auth attempts in sequence (OIDC -> APIKey -> MTLS). Use p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant rate limiting via a gateway-sidecar interceptor. Use a TenantPolicy CRD with fields: tenantID, rate, burst, window, penalty. A fast-path in-memory cache (TTL ~1s) wired to a central policy",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design for multi-region, air-gapped CNFs with strict provenance and runtime security, focusing on upgrade safety without image pulls.\n\n## Key Concepts\n- Runtime attestation with TPM-backed keys (Sigstore)\n- Image provenance with SBOMs and Cosign\n- In-cluster drift detection via eBPF\n- Policy-driven rollback (OPA/Gatekeeper)\n\n## Code Example\n```\napiVersion: constraints.gatekeeper.sh/v1beta1\nkind: K8sImageProvenance\nmetadata:\n  name: require-sbom-signature\nspec:\n  match:\n    kinds:\n    - apiVersion: apps/v1\n      kind: Deployment\n  parameters:\n    requireSBOM: true\n    requireSignature: true\n```\n\n## Follow-up Questions\n- How would you test drift detection thresholds?\n- How would you simulate network partitions across regions and verify rollback?",
        "difficulty": "advanced"
      },
      {
        "id": "cnf-certification-q-5",
        "questionId": "q-25946",
        "question": "Beginner CNF certification: In a multi-tenant CNF gateway on Kubernetes, implement per-tenant metrics collection in the sidecar. Expose Prometheus-compatible metrics including tenantID, path, status, and latency; control sampling with a Namespace ConfigMap cnf.metrics.enabled, cnf.metrics.endpoint, and cnf.metrics.sample-rate. Provide minimal YAML and a one-namespace manifest to validate end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a CI step that signs every config payload (CRD/ConfigMap/policy) with Cosign using a KMS-backed key, attach an SBOM, and publish to a signed config registry. Use an admission controller (Kyv",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a metrics sidecar that collects per-tenant observability data in a multi-tenant CNF gateway on Kubernetes. The sidecar should increment a Prometheus histogram and counter on each request, labeled by tenantID,…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a per-tenant CNFFeaturePolicy CRD in the control plane. Gatekeeper sidecars enroll at startup with SPIFFE SVIDs, open a bidirectional gRPC channel to negotiate enabled features per tenant (QUIC, m",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "OpenTelemetry tracing in a multi-tenant CNF gateway: instrument gateway sidecars to attach tenantID as a trace attribute; funnel to a single OTLP collector; implement per-tenant sampling via a Namespa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to add observability with minimal surface area using existing CNF sidecars and Kubernetes ConfigMaps. It assesses understanding of multi-tenant security boundaries and metrics cardinality management.\n\n## Key Concepts\n- Per-tenant labeling in Prometheus metrics\n- Prometheus text format exposition\n- ConfigMap-based feature flags and sampling control\n- High-cardinality label management\n\n## Code Example\n\n```yaml\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: cnf-metrics-config\n  namespace: tenant-namespace\ndata:\n  cnf.metrics.enabled: 'true'\n  cnf.metrics.endpoint: '/metrics'\n  cnf.metrics.sample-rate: '0.5'\n```\n\n```yaml\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: tenant-namespace\n---\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: cnf-metrics-config\n  namespace: tenant-namespace\ndata:\n  cnf.metrics.enabled: 'true'\n  cnf.metrics.endpoint: '/metrics'\n  cnf.metrics.sample-rate: '0.5'\n---\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: cnf-gateway\n  namespace: tenant-namespace\nspec:\n  template:\n    spec:\n      containers:\n      - name: gateway\n        image: cnf-gateway:latest\n      - name: metrics-sidecar\n        image: metrics-sidecar:latest\n        ports:\n        - containerPort: 8080\n          name: metrics\n```\n\n## Follow-up Questions\n- How would you handle high-cardinality label explosion with many tenants and paths?\n- How would you verify no tenant data cross-contamination occurs in metrics?\n- How does the sampling rate affect metric accuracy and Prometheus storage?",
        "difficulty": "beginner"
      },
      {
        "id": "cnf-certification-q-6",
        "questionId": "q-8020",
        "question": "Advanced CNF certification: In a multi-tenant CNF mesh on Kubernetes with autoscaling, design an end-to-end provenance flow for ephemeral pods. Each new pod must derive from a Cosign-signed image with a tenant KMS-backed key and a corresponding SBOM. Implement runtime policy (OPA/Kyverno) to reject deployments lacking provenance and surface a per-tenant badge on deployments. Provide concrete manifests and test steps in a single-namespace scenario?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a WASM-based Envoy edge filter that uses the tenantID from the TLS SPIFFE identity to query a central PolicyStore (CRD-backed, cached with TTL). Enforce per-tenant allowlists for destinations,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an end-to-end license-attestation flow for multi-tenant CNFs: (1) generate a tenant-scoped SBOM at build with a KMS key, (2) sign image with Cosign and attach SBOM, (3) publish a tenant-spec",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement: sign images with Cosign using tenant KMS, generate SBOMs via Syft, publish to Rekor, and annotate Deployments with a provenance badge. Enforce with an OPA/Kyverno policy requiring i",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Audit TLS usage per tenant; adopt a hybrid post-quantum TLS approach (Kyber/Dilithium) for session keys and per-tenant certificates; load per-tenant keys from an HSM via PKCS#11 with rotation primitiv",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates end-to-end provenance for ephemeral CNF pods under autoscaling, tenant isolation, and runtime policy.\n\n## Key Concepts\n- Cosign, SBOM (SPDX/CycloneDX), Rekor\n- OPA/Kyverno, per-tenant KMS keys\n- Kubernetes HPA, ephemeral pods\n\n## Code Example\n```yaml\n# Minimal Kyverno policy enforcing provenance presence on Deployments\napiVersion: kyverno.io/v1\nkind: ClusterPolicy\nmetadata:\n  name: require-provenance\nspec:\n  validationFailureAction: enforce\n  rules:\n  - name: assert-provenance\n    match:\n      resources:\n        kinds: [\"Deployment\"]\n    validate:\n      message: \"Provenance (digest + SBOM) required\"\n      pattern:\n        metadata:\n          annotations:\n            provenance/digest: ?\n```\n\n## Follow-up Questions\n- How would you test provenance during rapid scale-out events?\n- How would you adapt the flow for edge/air-gapped deployments?",
        "difficulty": "advanced"
      },
      {
        "id": "cnf-certification-q-7",
        "questionId": "q-6557",
        "question": "In a multi-tenant Kubernetes-based CNF gateway platform spanning multiple clusters, design an end-to-end disaster recovery workflow for both image provenance and runtime state. Requirements: sign CNF image updates with Cosign using a KMS-backed key and attach SBOMs; back up per-tenant CRDs/etcd to a central encrypted store; implement canary rollouts with Argo Rollouts and automatic rollback on test failure; enforce cross-tenant isolation with Kyverno; provide a minimal in-namespace validation test and one-namespace rollback test before cluster-wide rollout; include minimal config snippets and commands to test the DR flow?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a DR workflow: sign CNF image upgrades with Cosign using a KMS-backed key and attach SBOMs; back up per-tenant CRDs/etcd to a central, encrypted store; roll out with canaries per tenant using",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Deploy a telemetry sidecar that reads TENANT_ID from the CNF manifest, appends a tenant label to every metric, and writes to a per-tenant remote_write sink (URL provided via a ConfigMap). Include an I",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enable per-tenant tracing with OpenTelemetry. Use cnf.trace.enabled and cnf.trace.sample-rate to control sampling; set cnf.trace.collector for OTLP exporter URL. The sidecar propagates X-TENANT-ID in",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a watchdog sidecar that launches the CNF main binary; if the main process crashes, the sidecar restarts it with exponential backoff (1s, 2s, 4s, …) capped at 60s and a per-tenant crash limit. E",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical disaster recovery planning for a CNF platform that serves multiple tenants. Candidates must show hands-on familiarity with image provenance, per-tenant runtime state backups, controlled rollouts, and tenant isolation policies.\n\n## Key Concepts\n- Image signing with Cosign using a KMS-backed key and SBOMs\n- Encrypted, per-tenant backups of CRDs/etcd\n- Canary/rollback strategies with Argo Rollouts\n- Tenant isolation via Kyverno policies and RBAC\n- Minimal in-namespace DR validation tests and rollback tests\n\n## Code Example\n```yaml\napiVersion: argoproj.io/v1alpha1\nkind: Rollout\nmetadata:\n  name: cnf-rollout\nspec:\n  replicas: 2\n  strategy:\n    canary:\n      steps:\n      - setWeight: 10\n```\n\n## Follow-up Questions\n- How would you validate DR in a single namespace before promoting cluster-wide? \n- What monitoring and alerting would you add to detect and trigger automatic rollback?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnf-certification-q-8",
        "questionId": "q-29359",
        "question": "In a multi-tenant CNF gateway platform on Kubernetes, design a per-tenant canary upgrade workflow. Each tenant should be able to trigger a tenant-scoped canary rollout for a new CNF image, with traffic shifted gradually (e.g., 5% canary, 95% stable) using a service mesh or rollout tool. The plan must include: per-tenant routing rules, health monitoring (latency, errors), rollback conditions, and minimal YAML fragments plus a one-namespace test manifest. Provide concrete trade-offs and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sign each tenant's CNF image with Cosign using a tenant-scoped key, generate an SPDX SBOM, and attach it as an image annotation. Enforce with ImagePolicyWebhook and a Gatekeeper/OPA constraint that requires both valid s…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a tenant-scoped FeaturePolicy CRD and a Gatekeeper ConstraintTemplate that validates Deployment.annotations['cnf/feature'] against the tenant's allowedFlags. The CI gate publishes pe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-tenant canary strategy leveraging Argo Rollouts combined with Istio for traffic management. Implement namespace-scoped Rollout objects where each tenant owns their CNF workload, with routing rules based on the…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-tenant mTLS termination using cert-manager to issue TLS certificates in each tenant namespace. Configure the gateway sidecar to terminate mTLS using the tenant-specific TLS secret (tenant-<id>-tls), valida…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates a candidate's understanding of multi-tenant reliability patterns, specifically testing their ability to design upgrade workflows that maintain tenant isolation while ensuring system reliability.\n\n## Key Concepts\n- **Per-tenant canary deployments**: Isolated rollout strategies per tenant with independent upgrade timelines\n- **Traffic splitting**: Service mesh (Istio) or rollout tools (Argo Rollouts) enabling gradual traffic shifting\n- **SLIs for validation**: p95/p99 latency thresholds, error rate budgets, and time-bounded degradation windows\n- **Rollback semantics**: Automated vs. manual gates with clear failure criteria\n- **Namespace-scoped testing**: Isolated test environments that mirror production topology\n\n## Code Example\n```yaml\n# Minimal Rollout example (Argo Rollouts)\napiVersion: argoproj.io/v1alpha1\nkind: Rollout\nmetadata:\n  name: tenant-a-cnf-rollout\nspec:\n  replicas: 2\n  selector:\n    matchLabels:\n      app: cnf\n      tenant: a\n  strategy:\n    canary:\n      steps:\n      - setWeight: 5\n      - pause: {duration: 5m}\n      - setWeight: 25\n      - pause: {duration: 5m}\n      - setWeight: 50\n      - pause: {duration: 10m}\n      analysis:\n        templates:\n        - templateName: latency-error-analysis\n      canaryMetadata:\n        labels:\n          app: cnf\n          tenant: a\n          version: canary\n      stableMetadata:\n        labels:\n          app: cnf\n          tenant: a\n          version: stable\n  template:\n    metadata:\n      labels:\n        app: cnf\n        tenant: a\n    spec:\n      containers:\n      - name: cnf\n        image: cnf:v2.0.0\n```\n\n```yaml\n# One-Namespace Test Manifest\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: tenant-a-test\n  labels:\n    tenant: a\n    environment: test\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: cnf-stable\n  namespace: tenant-a-test\nspec:\n  ports:\n  - port: 8080\n  selector:\n    app: cnf\n    version: stable\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: cnf-canary\n  namespace: tenant-a-test\nspec:\n  ports:\n  - port: 8080\n  selector:\n    app: cnf\n    version: canary\n---\napiVersion: networking.istio.io/v1beta1\nkind: DestinationRule\nmetadata:\n  name: cnf-destination\n  namespace: tenant-a-test\nspec:\n  host: cnf-stable\n  subsets:\n  - name: stable\n    labels:\n      version: stable\n  - name: canary\n    labels:\n      version: canary\n```\n\n## Expected Answer Quality\nA strong response should demonstrate:\n1. **Clear tenant isolation**: Each tenant manages independent rollout configurations\n2. **Measurable criteria**: Concrete thresholds for latency, error rates, and duration\n3. **Tool agnosticism**: Understanding of trade-offs between Istio, Argo Rollouts, Flagger, or similar tools\n4. **Failure resilience**: Explicit handling of partial states, metric failures, and rollback triggers\n5. **Operational realism**: Consideration of operational complexity vs. reliability benefits",
        "difficulty": "intermediate"
      },
      {
        "id": "cnf-certification-q-9",
        "questionId": "q-29833",
        "question": "CNF gateway platform on Kubernetes, intermediate: design a per-tenant circuit breaker in the gateway sidecar. The circuit breaker must be keyed by X-TENANT-ID, observe a 60-second sliding window, and trip when the tenant's failure rate exceeds 50%; once tripped, open state lasts 60 seconds before a half-open probe. Provide a minimal per-tenant ConfigMap schema cnf.cb.state, API endpoints /cb/status and /cb/force-close, and minimal YAML to test inside a single namespace. Include testing steps with a simple failure generator?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a runtime attestation workflow using Linux IMA across clouds: collect per-container measurements, sign IMA attestations with an HSM via PKCS#11, publish to a central attestation service, and ga",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Redis rolling window per tenant/route to compute failure rate over last 50 requests in 30s. A gateway-side counter increments on each request; if failure_count/total ≥ 0.8, open circuit for that",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a CNFPolicy CRD (tenantID, pathHash, window=120s, trip=3). Sidecar polls the control plane over mTLS and caches policy; it maintains a per-tenantID:pathHash rolling 5xx counter. On 3 5xx in window",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant circuit breaker in a gateway sidecar keyed by X-TENANT-ID, using a 60s sliding window and a 50% failure-rate threshold. When tripped, the circuit stays OPEN for 60s before a half-open retry",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical reliability patterns under multi-tenant load, requiring understanding of circuit breakers, per-tenant isolation, and lightweight Kubernetes state management.\n\n## Key Concepts\n\n- Sliding window failure monitoring\n- Per-tenant isolation via header X-TENANT-ID\n- Lightweight per-tenant state in ConfigMaps\n- Health endpoints for status and manual override\n\n## Code Example\n\n```yaml\n# example per-tenant circuit-breaker state (illustrative)\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: cnf.cb.state\n  namespace: tenant-a\ndata:\n  tenant: tenant-a\n  state: CLOSED\n  openedAt: \"2026-04-11T12:00:00Z\"\n  failCount: \"0\"\n```\n\n## Follow-up Questions\n\n- How would you persist circuit state across pod restarts without losing history?\n- How would you simulate bursts to validate real-world behavior and measure latency impact?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnf-certification-q-10",
        "questionId": "q-12825",
        "question": "Beginner CNF certification: In a multi-tenant CNF gateway cluster on Kubernetes, implement a per-tenant rate limiter as a sidecar. The sidecar reads tenant rate settings from a Namespace ConfigMap cnf.tenantRateLimit (fields: tenantID, rps) and enforces the limit for /api/*. If a tenant config is missing, fall back to a global default. Provide minimal YAML for the ConfigMap, sidecar snippet, and a one-namespace manifest to validate throughput before rollout?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant rate limiter sidecar using a token-bucket per tenant. Sidecar reads cnf-tenantRateLimit.json from a Namespace ConfigMap (tenantID, rps) and enforces limits for /api/* using the request head",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use an eBPF program in the sidecar to enforce per-tenant domain-based rate limits. Maintain BPF maps keyed by tenantID+domain with a token bucket per domain; policy loaded from ConfigMap cnf.policy.{t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a tenant-scoped, zero-downtime certificate-rotation workflow for gateway clients authenticating to the control plane. Implement a TenantCertificateRotation CRD with a minimal in-cluster CA mani",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a sidecar policy cache keyed by tenant and version, fetch from /policies/{tenant} with X-TENANT-ID, cache for 30s, apply path regex allow/deny, emit audit logs, and return 503 on version drift.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design per-tenant controls using Kubernetes primitives and sidecar patterns.\n\n## Key Concepts\n- Per-tenant state with token bucket\n- Namespace ConfigMap watch / refresh\n- Sidecar deployment partnering with main gateway\n- Fallback defaults and safe testing in one namespace\n\n## Code Example\n```yaml\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: cnf-tenantRateLimit\n  namespace: cnf-system\ndata:\n  tenants.json: |-\n    {\n      \"tenants\": {\"tenantA\": 100, \"tenantB\": 50},\n      \"defaultRPS\": 20\n    }\n```\n\n```yaml\n# sidecar snippet: mount ConfigMap and read /etc/cnf/tenants.json\nvolumeMounts:\n  - name: tenant-rates\n    mountPath: /etc/cnf\nvolumes:\n  - name: tenant-rates\n    configMap:\n      name: cnf-tenantRateLimit\n```\n\n```yaml\n# one-namespace manifest (test-tenant)\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: test-tenant\n---\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: gateway\n  namespace: test-tenant\nspec:\n  replicas: 1\n  template:\n    metadata:\n      labels:\n        app: gateway\n    spec:\n      containers:\n        - name: gateway\n          image: cnf/gateway:latest\n        - name: rate-limit-sidecar\n          image: cnf/sidecar-rate-limit:latest\n          volumeMounts:\n            - name: tenant-rates\n              mountPath: /etc/cnf\n      volumes:\n        - name: tenant-rates\n          configMap:\n            name: cnf-tenantRateLimit\n```\n\n## Diagram\n```mermaid\nflowchart TD\n  A[Ingress] --> B[RateLimiter Sidecar]\n  B --> C[Gateway Backend]\n```",
        "difficulty": "beginner"
      },
      {
        "id": "cnf-certification-q-11",
        "questionId": "q-7182",
        "question": "Beginner CNF certification: In a multi-tenant Kubernetes cluster, design a gate that enforces both image provenance and per-tenant runtime config provenance in a single-namespace test. Outline concrete steps, and provide minimal manifests (Cosign signing with tenant KMS, SBOM attachment, ImagePolicyWebhook rule, signed per-tenant ConfigMap, and an admission webhook) plus the exact commands to validate end-to-end in isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sign CNF images with Cosign and SBOMs, publish to Rekor. Enforce using TPM 2.0 attestation bundles and an ImagePolicyWebhook policy so only attested images execute. Roll out region-by-region with traf",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a per-tenant gate enforcing both image provenance and config provenance in a single namespace test. Steps: (1) Cosign-sign CNF image with tenant KMS key and attach SPDX SBOM; (2) enforce with",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a per-tenant feature-flag system in the CNF sidecar. Flags are stored in Redis using a tenantID:path key format with a 60-second TTL. The sidecar evaluates flags before routing requests, falls back to a global de…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use SPIRE with per-tenant trust domains. Run a tenant-scoped SPIRE server/agent pair; gateways CSR for short-lived TLS certs, rotated every 15–30 minutes. A sidecar hot-reloads new certs without pod r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a practical, end-to-end CNF provenance gate involving both container images and runtime configuration. It assesses understanding of Cosign signing, SBOM requirements, admission controls, per-tenant isolation, and minimal test manifests.\n\n## Key Concepts\n- Image provenance and SBOM validation\n- Per-tenant configuration provenance via signed ConfigMaps\n- Admission controls tying image and config hashes to signed artifacts\n- Minimal in-namespace validation workflow\n\n## Code Example\n```yaml\n# Minimal admission webhook example (pseudo)\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: tenant-a-config-signature\ndata:\n  config-hash: \"sha256-...\"\n```\n\n```javascript\n// Pseudo flow for operator wiring\n// 1) Sign image with Cosign + SBOM\n// 2) Create ImagePolicyWebhook rule\n// 3) Sign runtime config hash in ConfigMap\n// 4) Admission webhook validates both artifacts\n```\n\n## Follow-up Questions\n- How would you test rollback after a failed provenance check?\n- What are the security trade-offs of storing per-tenant KMS keys in Kubernetes secrets?",
        "difficulty": "beginner"
      },
      {
        "id": "cnf-certification-q-12",
        "questionId": "q-10553",
        "question": "In a federated CNF gateway platform spanning a central cluster and edge sites, design an end-to-end image provenance and runtime attestation workflow that ensures only Cosign-signed images with SBOMs and TPM-attestation quotes from edge nodes are allowed to run, with admission and runtime policies, per-tenant visibility, and minimal manifests to validate in one edge site?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant canary rollout using two deployments per tenant (stable and canary) with Istio or Linkerd for traffic splitting. Start with 5% canary traffic, ramp incrementally in 10-minute windows, and promote to…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Cosign to sign images with a KMS-backed key and attach SPDX SBOMs. Enforce admission with Kyverno to require both a valid Cosign signature on the image and a non-empty SBOM, plus a tenant label. A",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement per-tenant network policy via a central Policy API. Gateways fetch tenant rules (CIDR, ports, protocols) and program per-tenant eBPF programs loaded by a sidecar. Use X-TENANT-ID to select p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-namespace runtime license gate in a multi-tenant cluster. Sign CNF images with Cosign and embed SBOMs with licenses; push to a central registry; enforce per-tenant licenses using an OPA G",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses end-to-end supply chain security, multi-tenant isolation, and edge/runtime attestation in CNF platforms.\n\n## Key Concepts\n- Image provenance with Cosign and SPDX SBOMs\n- Admission control vs runtime attestation (Kyverno/OPA)\n- TPM/edge hardware attestation\n- Per-tenant visibility via ConfigMap and namespace labels\n\n## Code Example\n```javascript\n// Pseudo-patch showing checks in policy engine\nfunction check(image, sbom, attestation){\n  return image.signed && sbom.present && attestation.good;\n}\n```\n\n## Follow-up Questions\n- How would you handle rotation of KMS keys across tenants?\n- What are trade-offs of using TPM attestation vs software attestation at scale?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnf-certification-q-13",
        "questionId": "q-7397",
        "question": "Advanced CNF certification: In a multi-cloud Kubernetes CNF platform, implement a zero-downtime upgrade gate that preserves runtime integrity without rebuilding images. Provide data flow and concrete manifests for: (1) per-CNF SPIFFE identity, (2) TPM/IMA attestation to a central verifier with a trust score, (3) an OPA policy that requires attestation+SBOM delta before upgrade, (4) tenant-scoped network policy via Cilium, (5) a minimal in-namespace test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "The candidate should demonstrate a lightweight sidecar application (Go/Python) that reads the tenant-specific ConfigMap from /etc/cnf/canary.json, validates the enabled flag, implements percentage-based traffic splittin…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed zero-downtime upgrade gate architecture: (1) Each CNF maintains a unique SPIFFE identity managed by SPIRE for cryptographically-secure service identification; (2) TPM/IMA attestation continuously validates runt…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a lightweight tenant-scoped audit trail that logs every API call with tenant ID, HTTP method, path, and timestamp. Each log entry is signed using HMAC with per-tenant secrets stored in Kubernetes Secrets, then sh…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe an upgrade-safe, tenant-isolated CNF gateway rollout using Argo Rollouts canary with per-tenant weights, a Gatekeeper/OPA constraint to enforce allowed upgrade windows and image approvals, an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design end-to-end supply chain security and runtime integrity for multi-cloud CNFs, including identity management, attestation mechanisms, policy-as-code enforcement, and network isolation during zero-downtime upgrades.\n\n## Key Concepts\n- SPIFFE/SPIRE identity management\n- TPM/IMA runtime attestation\n- SPDX SBOM delta validation\n- OPA/Kyverno policy enforcement\n- Cilium eBPF network policies\n- Canary upgrade strategies\n\n## Code Example\n```yaml\n# SPIFFE identity configuration\napiVersion: spiffe.example/v1\nkind: SPIFFEIdentity\nmetadata:\n  name: cnf-identity-01\nspec:\n  spiffeID: spiffe://cluster/ns/cnf-01\n  parentID: spiffe://cluster/ns/workload-scheduler\n  ttl: 1h\n```\n\n```yaml\n# OPA upgrade gate policy\npackage cnf.upgrade\ndefault allow = false\n\nallow {\n  input.attestation.trust_score >= 80\n  input.sbom.delta.additions == 0\n  input.network_policy.isolated == true\n}\n```\n\n## Implementation Approach\nThe solution integrates identity, attestation, and policy enforcement to ensure upgrades maintain security posture while achieving zero downtime through controlled canary deployments.",
        "difficulty": "advanced"
      },
      {
        "id": "cnf-certification-q-14",
        "questionId": "q-21459",
        "question": "Beginner CNF certification: In a multi-tenant CNF gateway platform on Kubernetes, implement per-tenant OpenTelemetry tracing with tenantID propagation and per-tenant sampling. The sidecar must propagate tenantID (X-TENANT-ID) and sampling, export traces via OTLP to a central collector, and honor a Namespace ConfigMap cnf.tracing.enabled, cnf.tracing.collector, cnf.tracing.sample-rate. Provide minimal YAML and a one-namespace manifest to validate end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant canary rollout using two deployments per tenant (stable and canary) with Istio or Linkerd for traffic splitting. Start with 5% canary traffic, ramp incrementally in 10-minute windows, and promote to…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up per-tenant OpenTelemetry tracing in the sidecar. Propagate tenantID in trace attributes and baggage (X-TENANT-ID header mapped to resource.attributes[tenant.id]). Configure sampling per-tenant",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Isolate the namespace and pause workload updates. Collect immutable provenance: Cosign signature, SBOM, TPM/IMA attestations, registry audit logs, and Kubernetes manifests. Reconstruct the incident ti",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant upstream failover in the sidecar: each tenant carries X-TENANT-ID; declare primary and mirror upstreams; sidecar performs health checks and switches to the mirror after 3 consecuti",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of distributed tracing, tenant isolation, and Kubernetes-ConfigMap driven configs in a CNF gateway.\n\n## Key Concepts\n- OpenTelemetry basics: propagation, resources, sampling\n- Tenant-aware baggage and attributes\n- OTLP exporter and central collector\n- Namespace ConfigMap driven per-namespace behavior\n\n## Code Example\n```yaml\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: cnf-tracing\ndata:\n  cnf.tracing.enabled: \"true\"\n  cnf.tracing.collector: \"otel-collector.observability.svc:4317\"\n  cnf.tracing.sample-rate: \"0.1\"\n```\n\n## Follow-up Questions\n- How would you test cross-tenant data leakage in traces?\n- How would you dynamically adjust per-tenant sampling without redeploy?",
        "difficulty": "beginner"
      },
      {
        "id": "cnf-certification-q-15",
        "questionId": "q-4636",
        "question": "Design an intermediate CNF certification scenario for a hybrid cloud service mesh: implement an end-to-end runtime attestation and upgrade gate for CNFs deployed across namespaces and clouds. Include (1) a lightweight in-pod sidecar that signs an attestation report with a KMS-backed key and posts to a central attestation service, (2) a trust policy validating image provenance and attestation against a root of trust, (3) a canary upgrade strategy with per-CNF rollback based on attestation and traffic signals, and (4) minimal config snippets to validate in one namespace before wider rollout?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant circuit breaker in a gateway sidecar keyed by X-TENANT-ID, using a 60s sliding window and a 50% failure-rate threshold. When tripped, the circuit stays OPEN for 60s before a half-open retry",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a TenantTLS CRD and a controller that issues per-tenant certs from Vault PKI, configures Envoy TLSContext with per-tenant minTLSVersion and cipherSuites, and updates xDS hotly without pod res",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant canary rollout: CRD TenantCanary{tenantID, canaryPercent, canaryService, prodService}. Sidecar reads X-TENANT-ID, hashes req.path, routes a tenant’s percent to canary; rest to prod. Test: i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement in-cluster runtime attestation: a lightweight sidecar signs an attestation report with a KMS-backed key and posts it to a central attestation service. Gate policies enforce image provenance",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nCNF reliability hinges on runtime trust alongside image provenance. This question tests practical knowledge of in-cluster attestation, trust roots, and controlled upgrades across clouds.\n\n## Key Concepts\n- Runtime attestation, KMS-backed keys, central attestation service\n- Image provenance and policy enforcement against a root of trust\n- Canary upgrades and per-CNF rollback in multi-namespace/multi-cloud\n- Minimal in-namespace validation and rollout gating\n\n## Code Example\n```yaml\n# Example policy (illustrative)\napiVersion: kyverno.io/v1\nkind: ClusterPolicy\nmetadata:\n  name: require-attestation\nspec:\n  validationFailureAction: enforce\n  rules:\n  - name: attestation-check\n    match:\n      resources:\n        kinds: [\"CNFDeployment\"]\n    validate:\n      message: \"Attestation must be present and signed\"\n      pattern:\n        metadata.attestationSigned: true\n```\n\n```yaml\n# Attestation report (illustrative)\nattestation:\n  signer: \"kms://arn:aws:kms:...\"\n  root: \"root-of-trust-xyz\"\n  status: \"valid\"\n```\n\n## Follow-up Questions\n- How would you handle attestation revocation and key rotation without downtime?\n- What are the failure modes if the central attestation service is unavailable, and how would you mitigate them?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnf-certification-q-16",
        "questionId": "q-18474",
        "question": "In a multi-tenant CNF gateway cluster, design a tenant-aware telemetry pipeline that guarantees per-tenant data isolation and PII masking across logs and traces. Outline components (OpenTelemetry collector, Fluent Bit/Loki, Jaeger), how tenant context is propagated, and provide a minimal one-namespace test manifest with a Lua filter to mask PII and a small OTEL collector config snippet. What concrete steps would you implement to validate end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a tenant-aware telemetry pipeline: inject tenantID as a structured attribute in every log and trace, route data via per-tenant pipelines in the OpenTelemetry collector, and mask PII with a Lua",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a tenant-aware Canary: per-namespace canaries with TrafficSplit routing 1-5% to a canary Deployment, a control-plane CRD to define upgrade windows and per-tenant SLOs, and Prometheus-based hea",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Cosign to sign images with a KMS-backed key and attach SPDX SBOMs. Enforce admission with Kyverno to require both a valid Cosign signature on the image and a non-empty SBOM, plus a tenant label. A",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A central Policy Service validates JWTs (tenantID) and consults a replicated TenantPolicy allowlist; returns allow/deny to Envoy ext-authz. Each cluster enforces via Istio's ext-authz on egress; deriv",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a compliant telemetry stack for CNF gateways with multi-tenant segregation and data privacy.\n\n## Key Concepts\n- Telemetry pipelines (OpenTelemetry, Fluent Bit) across logs and traces\n- Tenant context propagation and isolation\n- PII masking via in-line filters (e.g., Lua) before forwarding\n- Loki/Jaeger exporters and per-tenant pipelines\n\n## Code Example\n```yaml\n# Fluent Bit ConfigMap snippet (Lua filter used for masking)\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: fluent-bit-pp\ndata:\n  lua-filter.lua: |\n    function filter(tag, dt)\n      if dt[\"message\"] ~= nil then\n        dt[\"message\"] = (dt[\"message\"]):gsub(\"%w+@%w+%.%w+\", \"[REDACTED_EMAIL]\")\n      end\n      return dt\n    end\n```\n```yaml\n# OTEL collector snippet (simplified)\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: otel-collector-config\ndata:\n  collector.yaml: |-\n    receivers:\n      otlp:\n        protocols:\n          grpc: {}\n          http: {}\n    processors:\n      attributes:\n        actions: [insert]\n        keys: [tenant_id]\n        from_context: true\n    exporters:\n      jaeger: {}\n      loki: {}\n    service:\n      pipelines:\n        traces:\n          receivers: [otlp]\n          processors: [attributes]\n          exporters: [jaeger]\n        logs:\n          receivers: [otlp]\n          processors: [attributes]\n          exporters: [loki]\n```\n\n## Follow-up Questions\n- How would you test cross-tenant leakage in logs/traces?\n- What metrics would verify isolation and masking effectiveness?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnf-certification-q-17",
        "questionId": "q-9585",
        "question": "Beginner CNF certification: In a multi-tenant Kubernetes CNF gateway platform, design a per-tenant traffic shadowing feature: a sidecar that duplicates a configurable percentage of inbound requests for a tenant to a sandbox namespace for QA without affecting production. Store per-tenant settings in a ConfigMap mounted at /etc/cnf/shadow.json; provide minimal YAML for one tenant and a one-namespace test manifest to validate shadow traffic in isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a lightweight policy engine in the sidecar that loads JSON policies from the `cnf.acl.policy` ConfigMap in each tenant namespace. Each `tenantID` maps to permitted paths and their allowed HTTP methods. On each…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-tenant CanaryDeployment CRD and a central control-plane gate that progressively shifts traffic for feature X by tenant. Enforce tenant-scoped versioning and health checks, with automatic rol",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a sidecar that reads /etc/cnf/shadow.json (tenantID, percent, sandboxNamespace). It hashes the request's tenantID and samples by percent; matched requests are forwarded to a shadow proxy tha",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Sign a tenant baseline runtime configuration (environment variables, flags, tuning parameters) with Cosign using a KMS-backed key and publish an SBOM for the CNF image. Implement an admission webhook combined with a lig…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests design of a tenant-isolated shadow traffic feature, ensuring production safety and auditability.\n\n## Key Concepts\n- Sidecar pattern, ConfigMaps, per-tenant routing, traffic shadowing, sandbox namespace.\n- Hash-based sampling, header preservation, observability.\n\n## Code Example\n```yaml\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: shadow-config\n  namespace: tenant-a\ndata:\n  shadow.json: |\n    { tenantID: tenant-a, percent: 20, sandboxNamespace: shadow-tenant-a }\n```\n\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: gateway\n  namespace: tenant-a\nspec:\n  template:\n    spec:\n      containers:\n      - name: gateway\n        image: cnf/gateway:latest\n      - name: shadow-sidecar\n        image: cnf/shadow-sidecar:latest\n        volumeMounts:\n        - name: shadow-config\n          mountPath: /etc/cnf/shadow.json\n        env:\n        - name: SHADOW_CONFIG\n          value: /etc/cnf/shadow.json/shadow.json\n      volumes:\n      - name: shadow-config\n        configMap:\n          name: shadow-config\n```\n\n## Follow-up Questions\n- How would you verify shadow traffic without affecting production SLAs?\n- What are the edge cases if tenant churn or sandbox namespace cleanup?",
        "difficulty": "beginner"
      },
      {
        "id": "cnf-certification-q-18",
        "questionId": "q-3457",
        "question": "In a shared Kubernetes CNF certification setup with multi-tenant Gatekeeper policies, design an end-to-end flow to validate and enforce per-tenant feature flags before deployment: 1) define a tenant-scoped FeaturePolicy CRD, 2) implement a ConstraintTemplate (Rego) that reads Deployment.annotations['cnf/feature'] and checks against the tenant policy, 3) enforce with Gatekeeper so disallowed flags fail admission, 4) wire a minimal CI gate to publish tenant policies and test manifests, 5) add in-namespace tests that verify allowed vs disallowed cases. What would you implement and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a tenant-scoped FeaturePolicy CRD and a Gatekeeper ConstraintTemplate that validates Deployment.annotations['cnf/feature'] against the tenant's allowedFlags. The CI gate publishes pe",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use SPIRE to issue per-tenant SPIFFE IDs for gateway and control-plane pods, with short cert lifetimes (e.g., 6h) and in-place rotation via sidecar watcher. Central audit logs go to a sink (e.g., Loki",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an offline attestation flow: sign CNF images with Cosign using a TPM-backed key; publish SBOMs to a private catalog; enforce attestation via ImagePolicyWebhook with per-tenant OPA policies; deploy",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Define per-tenant quotas using a namespace-scoped ResourceQuota combined with a LimitRange. For namespace cnf-tenant-A: ResourceQuota cnf-tenant-A-quota with limits.cpu: 4, limits.memory: 16Gi, requests.cpu: 2, requests…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of policy-as-code, admission controls, and CNF feature gating in a real multi-tenant cluster.\n\n## Key Concepts\n- Tenant-scoped CRD for featurePolicy\n- Gatekeeper ConstraintTemplate with Rego logic\n- Per-tenant policy propagation in CI\n- In-namespace tests for allow/deny scenarios\n\n## Code Example\n```yaml\n# CRD: featurepolicies.example.com\napiVersion: apiextensions.k8s.io/v1\nkind: CustomResourceDefinition\nmetadata:\n  name: featurepolicies.example.com\nspec:\n  group: example.com\n  versions:\n  - name: v1\n    served: true\n    storage: true\n    schema:\n      openAPIV3Schema:\n        type: object\n        properties:\n          spec:\n            type: object\n            properties:\n              tenant:\n                type: string\n              allowedFlags:\n                type: array\n                items:\n                  type: string\n```\n```rego\npackage gatekeeper.featurepolicy\n\nviolation[{\"msg\": msg}] {\n  input.review.object.kind == \"Deployment\"\n  annotations := input.review.object.metadata.annotations\n  feature := annotations[\"cnf/feature\"]\n  policy := data.tenants[input.review.object.metadata.namespace]\n  not featureR in policy.allowedFlags\n  msg := sprintf(\"disallowed feature flag: %v for tenant: %v\", [feature, input.review.object.metadata.namespace])\n}\n```\n\n## Follow-up Questions\n- How would you handle evolving policies without downtime?\n- How would you test performance impact of policy checks at scale?",
        "difficulty": "beginner"
      },
      {
        "id": "cnf-certification-q-19",
        "questionId": "q-1347",
        "question": "Advanced CNF certification scenario: deploy a CNF gateway across three air‑gapped data centers. Design an end‑to‑end plan to ensure image provenance and runtime integrity for updates, covering (a) offline Cosign signing with a TPM‑backed key, (b) SBOM generation and private catalog, (c) per‑tenant policy enforcement via OPA/Gatekeeper, (d) edge‑site offline attestation broker, (e) drift detection with automated rollback, and (f) observability hooks. Include concrete config examples?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an offline attestation flow: sign CNF images with Cosign using a TPM-backed key; publish SBOMs to a private catalog; enforce attestation via ImagePolicyWebhook with per-tenant OPA policies; deploy",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect a ValidatingWebhook that, on image admission, fetches the provenance score from the central service and rejects if score < threshold. Implement: Cosign signing with a KMS key; SBOM via Syft",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose one VF per tenant via Multus + SR-IOV CNI, bind each VF to a dedicated CPU core and NUMA node, and place it behind a per-tenant OVS/VPP data plane. Use tc clsact with token-bucket policing on",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant rate limiter sidecar using a token-bucket per tenant. Sidecar reads cnf-tenantRateLimit.json from a Namespace ConfigMap (tenantID, rps) and enforces limits for /api/* using the request head",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes offline attestation, edge CNF deployment, and policy-driven updates in air‑gapped environments, ensuring provenance, integrity, and rollback safety.\n\n## Key Concepts\n\n- Offline attestation and TPM keys\n- SBOM generation and private catalogs\n- ImagePolicyWebhook + OPA/Gatekeeper policies\n- Edge/offline attestation brokers\n- Drift detection and automated rollback\n- Observability/telemetry for attestation and rollbacks\n\n## Code Example\n\n```javascript\n// Pseudo: Cosign signing with TPM key and SBOM publication\ncosign sign --key tpm-key.pem gcr.io/org/cnf-gateway:latest\ncosign generate sbom -o sbom.json gcr.io/org/cnf-gateway:latest\n```\n\n## Follow-up Questions\n\n- How would you handle clock skew between air‑gapped sites and the signing CA?\n- What metrics would you surface to detect attestation drift at scale?",
        "difficulty": "advanced"
      },
      {
        "id": "cnf-certification-q-20",
        "questionId": "q-19704",
        "question": "Design a tenant-aware, zero-downtime upgrade workflow for CNF gateways in a multi-tenant Kubernetes cluster. Your design must enforce image provenance and SBOM attestation, implement a canary rollout with controlled traffic across tenants, and auto-rollback on failure. Include minimal YAML snippets and testing approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-tenant canary rollout using Argo Rollouts in each CNF gateway namespace. Enforce Cosign-signed images and SBOM attestation with a Kyverno policy. Run 10-20% traffic in the canary, collec",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement image signing via Cosign with a KMS-backed key; attach SBOMs; generate per-tenant SBOM deltas and enforce gates with Kyverno so upgrades proceed only if attestation is valid and delta is all",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Store per-tenant canaryPercentage in a central ControlPlane and expose it to the sidecar via a lightweight REST/GRPC API. Sidecar derives a per-tenant seed from tenantID, uses a deterministic RNG to d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Redis-backed per-tenant QoS table where each tenant is mapped to a tier (bronze/silver/gold). The gateway sidecar tracks in-flight request counts using composite keys in the format tenant:tier:path. Gold tier perm…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design end-to-end upgrade governance, attestation, multi-tenant isolation, and observability.\n\n## Key Concepts\n\n- Canary deployments (Argo Rollouts)\n- Image provenance (Cosign) and SBOM attestation\n- Policy enforcement (Kyverno/OPA) at admission or gate\n- Per-tenant dashboards and Prometheus metrics\n- Auto-rollback strategies\n\n## Code Example\n\n```javascript\n// Minimal canary health check (pseudo)\nasync function canaryHealth(tenant, imageTag) {\n  const url = `/health/${tenant}?image=${imageTag}`;\n  const r = await fetch(url, {headers: {Authorization: `Bearer canary-token-${tenant}`}});\n  return r.ok;\n}\n```\n\n## Follow-up Questions\n\n- How would you handle SBOM drift across tenants?\n- How to scale the policy evaluation when tenants grow?\n- What are failure modes during canary and how to mitigate them?\n- How would you automate rollbacks across multiple tenants simultaneously?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnf-certification-q-21",
        "questionId": "q-25362",
        "question": "Advanced CNF certification: In a multi-tenant CNF gateway platform on Kubernetes, design a dynamic policy engine that updates per-tenant rate limits, circuit breakers, and log sampling at runtime via a central policy API (gRPC) and streams deltas to gateway sidecars without redeploys. Specify data model, delta delivery, conflict handling, and provide a CRD plus a one-namespace manifest and a canary rollout path for live policy updates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a canary rollout strategy using Argo Rollouts across both regions, gradually increasing tenant exposure in 5–10% increments. Route traffic through Istio with per-tenant headers for strict isolation, enforced b…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a tenant scoped feature flag system for CNF gateways: CRD FeatureFlagPolicy with perTenantFlags and a defaultEnabled; a policy service issues short lived tokens; gateway sidecar fetches flag",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a central gRPC policy service that streams per-tenant policy deltas to gateway sidecars. Data model includes TenantID, pathHash, rateLimit, window, circuitBreaker, logSampleRate, policyVersion. Ga",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Model tenants with SPIFFE IDs and workload selectors, enforce egress with Istio EgressGateway and per-tenant allowlists sourced from an OPA policy store, and surface a tenant badge on gateway status.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design runtime policy reconfiguration with multi-tenant separation and zero-downtime updates.\n\n## Key Concepts\n- Central policy streaming via gRPC\n- Per-tenant policy data model with versioning\n- In-memory delta application with TTL and idempotence\n- CRD design for policy declarations and rollout path\n\n## Code Example\n```proto\nsyntax = \"proto3\";\npackage policy;\nmessage PolicyDelta {\n  string tenant_id = 1;\n  string path_hash = 2;\n  uint32 rate_limit = 3;\n  uint32 window_seconds = 4;\n  uint32 circuit_failure_limit = 5;\n  uint32 circuit_reset_seconds = 6;\n  uint32 log_sample_rate = 7;\n  string policy_version = 8;\n}\nservice PolicyService {\n  rpc StreamPolicy(PolicyDelta) returns (stream PolicyDelta);\n}\n```\n\n```yaml\n# CRD sketch\napiVersion: apiextensions.k8s.io/v1\nkind: CustomResourceDefinition\nmetadata:\n  name: tenancypolicies.example.com\nspec:\n  group: examples.com\n  versions:\n  - name: v1\n    served: true\n    storage: true\n    schema:\n      openAPIV3Schema: {}\n  scope: Namespaced\n  names:\n    plural: tenancypolicies\n    singular: tenancypolicy\n    kind: TenancyPolicy\n    shortNames: [tp]\n```\n\n## Follow-up Questions\n- How is conflict resolved when two deltas collide? \n- How would you monitor delta latency and ensure strict ordering across replicas?",
        "difficulty": "advanced"
      },
      {
        "id": "cnf-certification-q-22",
        "questionId": "q-1914",
        "question": "In a multi-tenant CNF gateway managed by a central control plane, design an end-to-end upgrade workflow that uses per-tenant feature flags and a canary rollout with Istio, while enforcing runtime policy with OPA Gatekeeper and SPIRE-based identity. Include: (a) how CNF versions and flags are modeled in CRDs, (b) traffic splitting and health checks for safe canaries, (c) per-tenant policy evaluation, and (d) drift detection with automatic rollback and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant CNFUpgrade CRD describing tenantID, image, version, and featureFlags; implement a canary rollout via Istio with 10% traffic and progressive steps; encode policy decisions in OPA th",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a multi-cluster canary rollout controller with per-tenant upgrade windows, progressive traffic weights, and automatic rollback. Use Kyverno to enforce SBOM delta and image signing, Gatekeeper",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a FeatureGate CRD and per-tenant TenantFeatureGate, stored in a central config. Use an OPA Gatekeeper ConstraintTemplate and Constraint to require that requests to /v1/beta are allowed only wh",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose per-tenant TLS client certs issued via CSR/KMS, bound to a tenant SPIFFE ID, with a gateway-side controller that rotates short-lived SVIDs. Enforce egress using an OPA Gatekeeper rule keyed to",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical upgrade workflows for multi-tenant CNFs with policy checks, identity, and drift-driven rollback.\n\n## Key Concepts\n- per-tenant CRD design with versions and featureFlags\n- Istio canary traffic shifting and health checks\n- OPA Gatekeeper policy evaluation per tenant\n- SPIRE/SPIFFE identity for CNF components\n- drift detection and observability for rollback\n\n## Code Example\n```yaml\napiVersion: cnf.example/v1\nkind: CNFUpgrade\nmetadata:\n  name: tenant-a-upg-1\nspec:\n  tenantID: \"tenant-a\"\n  image: \"registry.example/cnf-gateway@sha256:abc123\"\n  version: \"v1.2.3\"\n  featureFlags:\n    newTLS: true\n```\n\n## Follow-up Questions\n- How would you test rollback triggers and thresholds?\n- How to scale canary to larger tenant fractions with safety gates?",
        "difficulty": "intermediate"
      },
      {
        "id": "cnf-certification-q-23",
        "questionId": "q-21232",
        "question": "Beginner CNF certification: In a multi-tenant Kubernetes CNF gateway platform, implement a per-tenant API allowlist using a sidecar and Redis. Each tenantID is read from header X-TENANT-ID; Redis stores an HSET tenantID:allowed with paths as fields. The sidecar returns 403 if path not listed, 200 otherwise. Provide minimal YAML and a one-namespace manifest to validate end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a metrics sidecar that collects per-tenant observability data in a multi-tenant CNF gateway on Kubernetes. The sidecar should increment a Prometheus histogram and counter on each request, labeled by tenantID,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an admission webhook backed by OPA. Tenants store a small Rego policy in a ConfigMap, keyed by namespace, e.g., constraints for node selectors and allowed egress CIDR. The webhook fetches po",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Sidecar logic: extract tenant from X-TENANT-ID, take request path, Redis HGET tenantID:allowed path; if missing, respond 403; else forward. Seed Redis with HSET tenantID:allowed '/api/v1/orders', '/ap",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a per-tenant gate enforcing both image provenance and config provenance in a single namespace test. Steps: (1) Cosign-sign CNF image with tenant KMS key and attach SPDX SBOM; (2) enforce with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a practical tenant-scoped access control pattern using existing CNF primitives (sidecars, Redis) without overengineering.\n\n## Key Concepts\n\n- Per-tenant identity via header\n- Redis-backed authorization cache\n- Safe proxying in a sidecar\n\n## Code Example\n\n```javascript\n// Pseudo: sidecar filter\nconst tenant = req.headers['x-tenant-id'];\nconst path = req.path;\nredis.hget(`${tenant}:allowed`, path, (err, ok) => {\n  if (!ok) res.status(403).send('forbidden');\n  else forward(req);\n});\n```\n\n## Follow-up Questions\n\n- How would you seed and rotate the allowlist safely?\n- How to handle missing tenant or Redis latency?",
        "difficulty": "beginner"
      },
      {
        "id": "cnf-certification-q-24",
        "questionId": "q-1674",
        "question": "Advanced CNF scenario: deploy a CNF API gateway across three geographies with a private registry and multi-tenancy. Propose an end-to-end plan to enforce per-tenant image provenance and runtime integrity, including (i) per-tenant Cosign signing keys stored in KMS/HSM, (ii) per-tenant SBOMs, (iii) admission controls using ImagePolicyWebhook/OPA, (iv) tenant-aware drift detection and rollback, and (v) observability for signing, attestation, and rollback signals. Provide concrete steps and minimal config examples?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant provenance uses tenant-scoped Cosign keys in KMS/HSM, SBOMs published to a tenant feed, and ImagePolicyWebhook + OPA to enforce signatures at admission. Drift detection compares in-cluster",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Cross-cluster CNF provenance and attestation: sign image with Cosign (KMS-backed key), attach SBOM, and emit a TPM/IMA attestation digest into a tamper-evident ledger shared across clusters. Enforce with Kyverno/OPA so…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant canary by hashing the tenantID plus path to a 0–99 value; if cnf.canary.enabled is true and value < cnf.canary.percent, route to upstream-canary, else upstream-prod. Config via",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement Cosign signing for plugin images using KMS-backed keys, generate and attach SPDX SBOMs to each plugin image, maintain per-tenant plugin allowlists in ConfigMaps, enforce admission-time validation through a web…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes multi-tenant CNF provenance at scale, including per-tenant keys, drift detection, rollback, and observability.\n\n## Key Concepts\n- Tenant-scoped key management\n- Per-tenant SBOMs and provenance enforcement\n- ImagePolicyWebhook + OPA constraints\n- Drift detection and tenant-aware rollback\n- Observability: metrics, traces, audit logs\n\n## Code Example\n```javascript\n// Pseudo-code: verify image provenance per tenant\nasync function verifyProvenance(imageRef, tenant) {\n  const sigOk = await cosign.verify(imageRef, { key: `kms://${tenant}-key` });\n  const sbom = await fetchSBOM(imageRef);\n  if (sbom.tenant !== tenant) throw new Error('Tenant SBOM mismatch');\n  return sigOk;\n}\n```\n\n## Follow-up Questions\n- How would you rotate tenant keys without downtime?\n- How would you test drift detection across regions?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-10T12:05:07.592Z",
    "version": 1
  }
]