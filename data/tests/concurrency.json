[
  {
    "id": "test-concurrency",
    "channelId": "concurrency",
    "channelName": "Concurrency",
    "title": "Concurrency Knowledge Test",
    "description": "Test your understanding of Concurrency with 25 questions.",
    "questions": [
      {
        "id": "concurrency-q-0",
        "questionId": "q-11065",
        "question": "Design a beginner-friendly concurrency controller for a real-time multiplayer game server's per-room event dispatcher. Use a bounded global queue and per-room FIFO buffers, with a small worker pool to apply updates in room order. Introduce a degradation path: when backlog crosses a threshold, debounce or drop non-critical events while preserving critical room state. Explain backpressure, fairness, and provide a concrete test plan for per-room ordering, degradation behavior, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a central dispatcher routing edits to per-user FIFOs backed by bounded buffers; map users to workers with consistent hashing; on rebalance transfer a user’s queue via an atomic hand-off token and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-endpoint token buckets (rate r, burst B) guarding a shared, work-stealing pool. Before enqueuing a request, check the endpoint’s bucket and only enqueue if a token is available. Use a global f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a bounded global queue fed by many producers. Implement a dynamic worker pool that scales from minW to maxW based on queue depth (high watermark triggers growth, low watermark triggers shrink).",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a bounded global queue (size N) with per-room FIFO buffers. A round-robin dispatcher drains buffers to a small worker pool, ensuring events for each room are processed in arrival order. When backlog crosses th…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical concurrency control with per-key ordering and degradation strategies under load.\n\n## Key Concepts\n- Bounded queues and backpressure mechanisms\n- Per-room FIFO semantics and ordering guarantees\n- Fair scheduling across multiple rooms\n- Graceful degradation when overwhelmed\n\n## Code Example\n```javascript\n// Pseudo-code for event dispatcher\nwhile (true) {\n  for (room of roundRobinSchedule()) {\n    if (room.buffer.hasEvents() && globalQueue.notFull()) {\n      globalQueue.enqueue(room.buffer.dequeue());\n    } else if (globalQueue.isFull()) {\n      applyBackpressure();\n    }\n  }\n}\n```\n\n## Test Plan\n1. **Per-room ordering**: Send concurrent events to same room, verify FIFO processing\n2. **Degradation behavior**: Flood system to trigger threshold, verify non-critical events are dropped/debounced\n3. **Graceful shutdown**: Stop accepting new events, process remaining queue, verify no data loss",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-1",
        "questionId": "q-7130",
        "question": "In a CI/CD runner, hundreds of jobs from different teams submit tasks that form a DAG of artifacts. Design a bounded, multi-tenant work queue with per-tenant FIFO buffers and a dependency scheduler that enforces in-degree readiness. Include backpressure signaling, cancellation, and a minimal runnable sketch in Go or Rust?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-type bounded channels to enforce backpressure, a dispatcher routing tasks to each type queue, and a central scaler that monitors tail latency and throughput to adjust worker counts via atomic",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a bounded, multi-tenant work queue with per-tenant FIFO buffers and a DAG scheduler. Use a global semaphore to cap in-flight tasks; track in-degree and a ready-set. Workers pull ready tasks fro",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a bounded global queue and per-tenant buffers; workers pull in round-robin across active tenants. Each tenant buffer preserves order; on overflow, apply backpressure to producers (block subm",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: a bounded global ring buffer with per-room FIFO queues inside workers; use CAS-based handoffs to avoid locks; maintain an LRU of active rooms to guide eviction. Overflow discards oldest mess",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a concurrent system with bounded resources, fairness across tenants, and dependency-aware scheduling. It probes practical reasoning about backpressure, cancellation, and testability in large-scale CI-like workloads.\n\n## Key Concepts\n- Bounded queues and semaphores for backpressure\n- DAG scheduling with in-degree tracking\n- Per-tenant FIFO buffers and fair work distribution\n- Task cancellation, timeouts, and test strategies\n\n## Code Example\n```javascript\n// Pseudo sketch: structures for Task, Scheduler, and Enqueue/Dequeue\n```\n\n## Follow-up Questions\n- How would you detect and mitigate starvation across tenants?\n- What metrics would you collect to diagnose stalls in the DAG?\n- How would you scale the design to thousands of tenants and millions of tasks?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-2",
        "questionId": "q-7831",
        "question": "Design an intermediate concurrency problem: a real-time telemetry processor with per-device time-windowed aggregates. N producers emit {device_id, ts, value} readings; readings may arrive out of order and late by up to L seconds. Implement a bounded per-device window manager that computes min, max, sum, count in fixed-size windows [start, end). Use M workers to maintain per-device buffers, advance a watermark, and emit aggregates when windows close. Enforce memory bounds, allow dynamic worker resizing, and define late-data policy with at-most-once vs at-least-once semantics. Provide data structures and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-tenant FIFO queue and a single bounded backlog. Each tenant gets a dynamic quota updated by a central scheduler (token buckets that adjust to burstiness). A dispatcher drains queues in weigh",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-device buffers keyed by device_id, each storing windows with running aggregates. Maintain a global watermark; when window_end <= watermark, flush and drop. Cap total buffered windows; on cap,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Approach: use a bounded global queue and per-sensor FIFO buffers; a worker pool processes events and pushes to a dedupe layer (map of last-seen timestamp per sensor). Keep a 1000ms sliding window; dro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-customer lanes with a central arbiter and per-customer credits to throttle producers when queues grow. Track a monotonic per-customer sequence to ensure stage boundaries do not reorder events. Implement a credit…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of event-time processing, bounded-memory windowing, and backpressure in a concurrent setting. It also probes dynamic worker resizing and late-data strategies.\n\n## Key Concepts\n- Event-time windowing with per-device state\n- Bounded buffers and memory accounting\n- Watermarks to trigger emission\n- Dynamic worker scaling and backpressure\n- Late-data handling and exactly-once vs at-least-once semantics\n\n## Code Example\n```javascript\n// Pseudo-structure: perDeviceWindows: Map<device_id, Map<WindowKey, WindowState>>\n// WindowKey could be (start, end); WindowState tracks sum, min, max, count, etc.\n```\n\n## Follow-up Questions\n- How would you validate correctness with late arrivals and clock skew?\n- What metrics would you expose to operators to tune window size and cap?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-3",
        "questionId": "q-8558",
        "question": "Design a per-customer ordered event processing pipeline for a real-time analytics service used by Robinhood/Instacart. Multiple producers emit events keyed by customer_id into a bounded shared buffer. The pipeline has stages ingest, transform, and emit. Guarantee that all events for the same customer are processed in order across stages, while different customers proceed in parallel. Implement per-customer FIFO subqueues, a global backpressure policy when the buffer fills, dynamic worker resizing, and a clear policy for late or out-of-order events, with an approach for exactly-once emission and checkpointing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a fixed-size circular ring buffer with atomic head/tail indices and per-partition sequencing. Use compare-and-swap (CAS) to advance tail on enqueue and head on dequeue; preserve per-partition order by embeddin…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a global bounded queue plus per-channel local deques. Workers drain local queues first, then fetch from the global queue with a rotating index and aging to prevent starvation. Block producers",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-key FIFO buffers coordinated by a global sequencer to maintain strict ordering across regions. Employ a bounded global queue with capacity Q and per-region credit allocation; producers block when credits a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-customer sequencer with per-customer FIFO queues, backed by a bounded shared ring buffer. Each worker handles a set of customers, preserving order by dispatching events with monotonically in",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess ability to reason about per-tenant ordering, backpressure, and fault-tolerant emission in a real-time analytics context with dynamic workers.\n\n## Key Concepts\n\n- Per-customer ordering across stages\n- Bounded buffers and backpressure\n- Checkpointing and exactly-once emission\n- Dynamic worker resizing and rebalancing\n\n## Code Example\n\n```javascript\n// Pseudo per-customer state\nclass CustomerState { constructor() { this.queue = []; this.lastSeq = 0; } }\n```\n\n## Follow-up Questions\n\n- How would you test late-arriving events and reordering?\n- How would you simulate worker failures and ensure progress with checkpoints?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-4",
        "questionId": "q-21585",
        "question": "Design a beginner-friendly concurrency exercise: 200 producers, each with a per-service FIFO buffer. A small worker pool drains a shared bounded queue to storage. Implement a per-service round-robin scheduler with a fixed time quantum to guarantee per-service ordering and prevent starvation. Backpressure: when backlog exceeds threshold, throttle by delaying new logs or dropping tail logs per policy. Test: per-service ordering, backlog behavior, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe a per-DAG, per-stage bounded queueing with path-specific sequencing. Each task carries path_id and sequence; a node forwards a task only after all its parents for that path have completed. Us",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a bounded global queue, a dict of per-tenant FIFO queues, and a dispatcher that routes by class priority while ensuring no starvation via round-robin scheduling across tenants. Workers drain ten",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-topic multi-stage streaming pipeline with dynamic worker pools and per-topic in-order guarantees. Producers publish topic-labeled messages to a bounded input queue. Workers route messages",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly concurrency exercise: 200 producers, each with a per-service FIFO buffer. A small worker pool drains a shared bounded queue to storage. Implement a per-service round-robin s",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nProbes practical time-slicing, ordering guarantees, and backpressure in bounded-queue settings. Requires translating scheduling ideas into code, reasoning about fairness, and designing tests.\n\n## Key Concepts\n\n- Per-service FIFO buffers\n- Bounded queues and backpressure\n- Round-robin scheduling with time quantum\n- Backpressure strategies and testing\n\n## Code Example\n\n```javascript\n// Pseudocode for scheduler loop\nwhile (running) {\n  const service = nextService();\n  const batch = service.dequeueUpTo(QUANTUM);\n  enqueueToWorkerPool(batch);\n}\n```\n\n## Follow-up Questions\n\n- How would you adjust the time quantum for bursty logs?\n- How would you extend to multiple storage backends?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-5",
        "questionId": "q-17307",
        "question": "Design a bounded, fairness-preserving dispatcher for a multi-tenant API gateway. Each tenant has an ordered queue; processing has two stages: authentication and business logic. Build a global scheduler that (1) preserves per-tenant order across stages, (2) enforces fair shares under bursts via virtual time, (3) supports dynamic worker resizing and upstream backpressure. Provide data structures, a runnable sketch, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a global bounded queue plus per-channel local deques. Workers drain local queues first, then fetch from the global queue with a rotating index and aging to prevent starvation. Block producers",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I’d implement per-device FIFO buffers feeding a bounded global queue with per-device token-bucket rate limiters. A fair scheduler preserves per-device order while enabling cross-device parallelism. On",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-entity FIFO queues keyed by entity_id and a global bounded buffer. Route each event to its entity queue with a per-entity sequence number to guarantee in-order processing, even as the worker pool dynamical…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose per-tenant FIFO queues feeding a global scheduler built on virtual time. Each tenant gets a fixed quantum; the scheduler advances to the head task of the tenant with the smallest virtual time",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design fair, bounded concurrency with multi-tenant isolation and backpressure in a gateway.\n\n## Key Concepts\n\n- Per-tenant FIFO queues\n- Virtual time fair scheduling\n- Two-stage processing with order preservation\n- Dynamic worker resizing and backpressure signaling\n\n## Code Example\n\n```go\n// skeleton data structures\ntype Task struct { tenantID string; payload []byte; enqueue time.Time }\ntype TenantState struct { queue chan Task; quota int; lastScheduled time.Time }\n```\n\n## Follow-up Questions\n\n- How would you adapt for arriving tenants vs existing ones? \n- How would you measure fairness under different traffic patterns?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-6",
        "questionId": "q-18467",
        "question": "Design a distributed task scheduler for a real-time analytics platform where 1k+ clients submit tasks that must be processed in per-client FIFO order, while the system stays under a globally bounded capacity. Build a scheduler that (a) preserves per-client ordering, (b) enforces backpressure, (c) supports cancellation, and (d) scales across multiple nodes. Provide a runnable minimal example in Rust (tokio) or Go and a test plan for ordering, fairness, burst latency, and failover?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-client FIFO scheduler with a global bounded frontier and a small, per-node arbiter. Use per-client queues and a central scheduler that grants a fixed number of slots to non-empty queues i",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use per-user queues (FIFO) fed into a bounded global worker pool. Each user channel feeds a non-blocking writer to a bounded arbiter that enforces global order, with a semaphore-based backpressure. Ga",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bounded global queue and three per-priority FIFO buffers, with a fixed pool of workers. Producers enqueue into the appropriate buffer; a dispatcher moves tasks to workers in priority order (high",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: per-instrument FIFO buffers, global bounded queue, worker pool updates price cache and notifies subscribers. Degradation: when backlog > threshold, keep only the latest tick per instrument,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis explores a scalable scheduling problem with strict per-client ordering under high concurrency, plus cross-node coordination, backpressure, and cancellation—areas often encountered in large-scale data platforms.\n\n## Key Concepts\n\n- Per-client FIFO queues to preserve order\n- Global bounded capacity and backpressure signaling\n- Central arbiter with fair slot distribution (round-robin or weighted)\n- Cancellations and fault tolerance across nodes\n- Test methodology: ordering guarantees, fairness under burst, tail latency, failover behavior\n\n## Code Example\n\n```rust\nuse tokio::sync::mpsc;\nuse std::collections::HashMap;\nuse std::collections::VecDeque;\n\ntype ClientId = u64;\nstruct Task { id: u64, payload: Vec<u8> }\n\n#[tokio::main]\nasync fn main() {\n  // Minimal scaffold: per-client queues and a bounded global queue\n  let (_tx, mut _rx) = mpsc::channel::<Task>(1024);\n  // Implementation would: enqueue into per-client VecDeque, arbiter grants slots, workers drain queues\n}\n```\n\n## Follow-up Questions\n\n- How would you test for starvation under varying client burst profiles?\n- How would you extend to multi-region failover while preserving per-client order?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-7",
        "questionId": "q-16921",
        "question": "Design a per-scene ordered video processing pipeline: N producers emit frames tagged with scene_id and frame_id; a pool of M workers runs a stateless transform; a final encoder aggregates per-scene progress. Guarantee per-scene in-order processing across all stages under backpressure, while allowing dynamic rebalancing of scenes to workers and a scene-level backlog allocator that lowers priority for scenes with long backlogs. Provide data structures, backpressure signaling, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two bounded queues (CPU-bound, IO-bound) fed from a single dispatcher; each job has a unique id and is enqueued only once. Two worker pools pull from their queues. When both queues have work, apply a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a durable in-flight map keyed by taskID to track status; mark inflight on enqueue, complete on success, and persist to a durable log for crash recovery. Workers pull from a bounded channel; before",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-scene FIFO buffers with bound K. A per-scene sequencer ensures frame_id order before transform; workers pull by scene, preserving order across stages. A dynamic scene-to-worker rebalancer migrates",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a single bounded ring buffer (size N) shared by all topics. Maintain per-topic consumer cursors and a single producer index with atomic enqueue. Producers await when full; workers poll per-topic o",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design per-scene ordered processing under backpressure, plus dynamic load balancing and fairness across concurrent producers, with concrete data structures and testability.\n\n## Key Concepts\n- Per-scene FIFO buffers, bounded queues\n- In-order processing across multiple stages\n- Backpressure signaling and flow control\n- Dynamic rebalancing of work assignments\n- Late/frame reordering safeguards\n\n## Code Example\n```javascript\n// Example structures\ntype Frame = { scene_id: string; frame_id: number; payload: any };\ntype SceneBuffer = { queue: Frame[]; head: number };\n```\n\n## Follow-up Questions\n- How would you prove liveness and bounded memory under bursty traffic?\n- What metrics would you monitor to tune K and B for fairness?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-8",
        "questionId": "q-28741",
        "question": "Design a concurrent document-edit pipeline: N editors publish edits as (doc_id, seq, op). A Transformer stage with M workers applies edits per document in increasing seq across documents, while a Merger with K workers emits final document states to subscribers in per-document order. Use a single bounded global queue for all edits, with backpressure when full. Support dynamic worker resizing and handle late edits (seq <= current) gracefully. Provide data structures, a runnable sketch, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use N per-client FIFO queues and a fixed worker pool. Each client has a credit counter; producers enqueue only when credits>0, decrementing atomically. (A) Lock-based: guard each queue with a mutex; a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a dependency counter per task and a bounded global ready queue; each worker pops from its local deque and steals from others when idle. On task completion, decrement indegree of dependents and enq",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed approach: allocate per-doc pending queues, a global distributor assigns edits to the corresponding doc queue; each doc tracks expected_seq; workers pull from a doc queue only when the next se",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a practical real-time CI task scheduler for a multi-repo codebase. Ingest build tasks from M repository streams, where each task has a repo_id and build_id. Guarantee per-repo in-order execution across a dynamic…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests per-document ordering under cross-document concurrency, dynamic worker changes, and backpressure.\n\n## Key Concepts\n- Per-document sequencing\n- Bounded buffers and backpressure\n- Dynamic worker resizing\n- Late-edit handling and idempotence\n\n## Code Example\n```javascript\n// Sketch: map doc_id->queue; worker pulls ready seqs; merger aggregates\n```\n\n## Follow-up Questions\n- How would you quantify backpressure impact on editors?  \n- How would you test with hot documents and synthetic late edits?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-9",
        "questionId": "q-4669",
        "question": "In a real-time ingestion service, dozens of producers emit per-tenant events into a fixed-size global queue. Implement a bounded, multi-tenant queue that guarantees per-tenant in-order processing and fair sharing while allowing dynamic partition rebalancing without pausing producers. Provide a runnable minimal sketch in Go or Rust showing enqueue/dequeue and a basic test plan for backpressure, ordering, and rebalancing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-room bounded queue with blocking semantics: producers block when the queue is full, consumers block when empty. In Go, use a buffered channel per room; in Python/Java, use a bounded queue with mutex/cond…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-room sequence numbers with bounded FIFO buffers (size B) managed by a central dispatcher feeding a global bounded queue (size Q). Workers pull from the global queue and route to room-specific deliverers th…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: use per-room bounded buffers and a dispatcher that polls buffers in a round-robin fashion, forwarding to a shared outbound queue with a fixed capacity. Guarantee room ordering by never reord",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-level queue: a bounded global ring buffer plus per-tenant FIFO subqueues. Each event includes tenantId and a monotonic sequence. Producers push to the ring; a dispatcher forwards to the tenant’s q",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a bounded, multi-tenant pipeline with ordering guarantees, backpressure, and dynamic rebalancing, reflecting real-world multi-tenant systems.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Per-tenant in-order processing\n- Dynamic partitioning and rebalancing\n- Memory visibility and race conditions in concurrent code\n\n## Code Example\n```go\n// Minimal sketch: global bounded channel + per-tenant queues\npackage main\nimport (\n  \"fmt\"\n)\n\ntype Event struct{ Tenant string; Seq uint64; Data string }\n\nfunc main() {\n  global := make(chan Event, 8) // bounded global queue\n  tenants := map[string]chan Event{\n    \"tA\": make(chan Event, 4),\n    \"tB\": make(chan Event, 4),\n  }\n  // enqueue example (producer)\n  go func(){ global <- Event{Tenant:\"tA\", Seq:1, Data:\"x\"} }()\n  // dispatcher (simplified)\n  go func(){ for e := range global { tenants[e.Tenant] <- e } }()\n  // worker (consumer)\n  go func(){ for e := range tenants[\"tA\"] { fmt.Println(\"work\", e) } }()\n}\n```\n\n## Follow-up Questions\n- How would you test for starvation during bursty tenant loads?\n- How would you ensure monotonic sequencing across partition rebalancing?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-10",
        "questionId": "q-16963",
        "question": "Design a dynamic, backpressure-aware DAG-based streaming pipeline: Ingest N producers into a runtime-configurable DAG of M operators connected via bounded buffers. Guarantee global causal ordering of records across operators under backpressure, support runtime DAG reconfiguration (add/remove operators, change connections) without halting, and provide a concrete test plan and data structures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a bounded global queue fed by many producers. Implement a dynamic worker pool that scales from minW to maxW based on queue depth (high watermark triggers growth, low watermark triggers shrink).",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a centralized DAG orchestrator with a globally increasing sequence per input, per-edge bounded queues, and per-node in-degree tracking. Each event carries a sequence and key; operators flush only",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-user sharded queues with a central sequencer. Each user tracks expected sequence; out-of-order events are buffered until the missing seq arrives, ensuring in-order processing. Use a bounded dispat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a fixed-size Go channel as the global queue, a mutex-protected map[string]bool for in-flight tracking, and per-ID coalescing buffers. On enqueue: if the ID is already in-flight, merge the payload with the exis…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design a mutable, backpressure-aware pipeline that maintains strong ordering guarantees across a DAG under runtime changes.\n\n## Key Concepts\n\n- Directed Acyclic Graph of operators with bounded buffers\n- Global causal ordering across multiple stages\n- Backpressure propagation and dynamic DAG reconfiguration\n- Test plan covering ordering, latency, failure modes\n\n## Code Example\n\n```javascript\n// Skeleton: per-edge buffer, sequence tracking, and gating\nclass EdgeBuffer { constructor(limit) { this.limit = limit; this.queue = []; } push(r) { if (this.queue.length < this.limit) this.queue.push(r); } available() { return this.queue.length; } }\n```\n\n## Follow-up Questions\n\n- How would you detect and recover from stalls due to skewed event arrival?\n- How would you implement safe in-flight data migration during reconfiguration?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-11",
        "questionId": "q-20934",
        "question": "Design a per-team log ingestion pipeline with a bounded global queue and per-team FIFO buffers. Each log item carries team_id and severity (INFO/WARN/ERROR). Downstream analyzers process per-team logs in arrival order, but a single bottleneck causes backlog; implement a graceful degradation path: when backlog threshold B is exceeded, drop non-critical logs (INFO/WARN) deterministically per team, while guaranteeing that all ERROR logs and the most recent per-team critical events are preserved and ordered. Provide data structures, backpressure signaling, test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-team log ingestion pipeline with a bounded global queue and per-team FIFO buffers. Each log item carries team_id and severity (INFO/WARN/ERROR). Downstream analyzers process per-team logs in arrival order,…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a per-user state with bounded FIFO buffers for A and B and a 60s watermark. Emit when both sides exist, preserving order by the latest timestamp. Drop/purge late records per policy to bound memory",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-user FIFO pipeline with a global bounded queue and per-user local buffers. Route events by user_id to dedicated worker queues, maintain per-user sequence ordering, support dynamic worker",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant FIFO queues and a shared bounded queue. Each tenant has a token-bucket budget to permit bursts; enqueue blocks when budget is exhausted to apply backpressure. Dispatcher uses a mi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical concurrency control under heavy load, enforcing per-team ordering with a global bottleneck and deterministic degradation. It assesses backpressure signaling, dynamic policy design, and test plan creation for real-world streaming workloads.\n\n## Key Concepts\n\n- Bounded global queue with capacity enforcement\n- Per-team FIFO buffers maintaining sequence order\n- Backpressure signaling mechanism to upstream producers\n- Deterministic drop policy by severity level\n- Ordering guarantees under graceful degradation\n- Comprehensive test strategy for correctness and resilience\n\n## Data Structures\n\n```javascript\n// Core types\ntype LogLevel = 'INFO' | 'WARN' | 'ERROR';\ntype Log = { teamId: string; severity: LogLevel; timestamp: number; payload: any; seq: number };\n\n// Per-team FIFO buffer with sequence tracking\nclass TeamBuffer {\n  constructor(teamId, capacity) {\n    this.teamId = teamId;\n    this.buffer = [];  // Circular buffer or ring buffer\n    this.seq = 0;       // Monotonic sequence number\n    this.criticalSeq = 0;  // Highest preserved ERROR/WARN seq\n  }\n  \n  push(log) {\n    log.seq = this.seq++;\n    this.buffer.push(log);\n  }\n  \n  // Deterministic drop: remove oldest non-critical when over capacity\n  prune(threshold) {\n    // Keep all ERROR, keep most recent WARN, drop oldest INFO\n  }\n}\n\n// Global bounded queue with backpressure signal\nclass LogIngestionPipeline {\n  constructor(globalCapacity, backlogThreshold) {\n    this.globalQueue = new Map();  // teamId -> TeamBuffer\n    this.capacity = globalCapacity;\n    this.backlogThreshold = backlogThreshold;\n    this.currentBacklog = 0;\n  }\n  \n  // Backpressure: signal when approaching threshold\n  isBacklogged() {\n    return this.currentBacklog >= this.backlogThreshold;\n  }\n  \n  ingest(log) {\n    if (!this.globalQueue.has(log.teamId)) {\n      this.globalQueue.set(log.teamId, new TeamBuffer(log.teamId));\n    }\n    \n    const buffer = this.globalQueue.get(log.teamId);\n    \n    // Always accept ERROR logs\n    if (log.severity === 'ERROR') {\n      buffer.push(log);\n      this.currentBacklog++;\n      return true;\n    }\n    \n    // Under backlog: accept all\n    if (!this.isBacklogged()) {\n      buffer.push(log);\n      this.currentBacklog++;\n      return true;\n    }\n    \n    // Over backlog: deterministic drop policy\n    return buffer.tryAppendWithPruning(log, this.backlogThreshold / this.globalQueue.size);\n  }\n}\n```\n\n## Backpressure Signaling\n\n- **Push-based**: Callback/EventEmitter to producers when backlog > 80% threshold\n- **Pull-based**: Expose `canAccept()` or `backlogPercentage()` for producers to poll\n- **HTTP headers**: X-RateLimit headers (Retry-After, X-Backlog-Level)\n- **Circuit breaker**: Temporarily reject with 503 Service Unavailable\n\n## Test Plan\n\n### Unit Tests\n1. **Ordering verification**: Confirm per-team logs maintain FIFO order under normal and degraded conditions\n2. **Drop policy correctness**: Verify ERROR logs never dropped; INFO/WARN dropped deterministically when over threshold\n3. **Backpressure triggers**: Validate signal fires at correct threshold\n4. **Sequence integrity**: Ensure seq numbers preserved after pruning\n\n### Integration Tests\n1. **High throughput**: Simulate 10x normal load, verify graceful degradation\n2. **Burst handling**: Rapid bursts per team, confirm fair buffer management\n3. **Recovery**: Backlog clears, verify no log loss or corruption\n\n### Chaos Tests\n1. **Producer failure**: Stop producers mid-stream, verify pipeline stability\n2. **Memory pressure**: Constrain heap, verify bounded behavior\n3. **Ordering stress**: Random severity injection, validate final order",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-12",
        "questionId": "q-8037",
        "question": "Design a beginner-friendly concurrency queue for a real-time chat moderation system: messages arrive concurrently from thousands of chat rooms. A bounded global queue feeds a small worker pool that runs moderation checks. Each chat room's messages must be processed in order, but messages across rooms may be interleaved. Propose a concrete design (data structures, routing, per-room buffers, backpressure) and outline a practical test plan to verify per-room ordering, room throttling, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a centralized DAG orchestrator with a globally increasing sequence per input, per-edge bounded queues, and per-node in-degree tracking. Each event carries a sequence and key; operators flush only",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: maintain a bounded global queue and per-client FIFO queues. Use a round-robin scheduler that selects the oldest non-empty client queue; workers pull and process. If the global queue is full,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bounded global queue (size Q) and per-room FIFO queues. A router assigns incoming messages to the corresponding room buffer; a small worker pool pulls from room buffers in round-robin, preservin",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-auction FIFO buffers with bounded size, a global bounded queue, and a sequencer to assign tasks to a scalable worker pool, guaranteeing per-auction in-order processing. Implement a soft latency bu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical concurrency design where per-key ordering matters, and backpressure affects producers and throughput.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Per-key (per-room) FIFO ordering\n- Work stealing vs round-robin drain\n- Per-room rate limiting and graceful shutdown testing\n\n## Code Example\n```python\nfrom collections import defaultdict, deque\nclass Router:\n    def __init__(self, global_limit):\n        self.global_q = deque()\n        self.room_buffers = defaultdict(deque)\n        self.rate = defaultdict(lambda: TokenBucket(allowance=1, bucket=5))\n    # methods to enqueue, route, and drain...\n```\n\n## Follow-up Questions\n- How would you scale to 100x load while preserving ordering guarantees per room?\n- How would you detect and recover from worker crashes without dropping messages?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-13",
        "questionId": "q-7348",
        "question": "Design a beginner-friendly concurrency task queue for a monitoring system: multiple agents publish health checks concurrently; a bounded global queue feeds a small worker pool that writes results to a local store. Implement per-agent rate-limiting and a graceful shutdown that drains in-flight tasks without losing results. Explain backpressure, ordering, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-key in-flight serialization with a map of keys to queues; a bounded global queue blocks producers when full; dynamic resizing via atomic counters and worker tickets; ensure in-order guarante",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two bounded queues (CPU-bound, IO-bound) fed from a single dispatcher; each job has a unique id and is enqueued only once. Two worker pools pull from their queues. When both queues have work, apply a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bounded global queue with per-agent rate-limiters (token bucket). Each agent maintains a small FIFO buffer and a dispatcher enqueues to the global queue; a small worker pool dequeues and writes",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "I would implement a per-host queueing layer atop a fixed worker pool. Each host maintains its own token bucket for rate limits and an in-flight linked list to preserve per-host order. The dispatcher r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical concurrency patterns that map to real-world monitoring systems: bounded queues, per-source rate limiting, graceful shutdowns, and recoverability.\n\n## Key Concepts\n\n- Bounded queues and backpressure\n- Per-agent FIFO buffers\n- Rate limiting and fairness\n- Graceful shutdown and recovery\n\n## Code Example\n\n```javascript\n// Pseudocode: dispatcher + workers + rate limiter\n```\n\n## Follow-up Questions\n\n- How would you modify for bursty health checks?\n- How would you simulate failures and ensure no data loss?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-14",
        "questionId": "q-12667",
        "question": "Design a concurrency-safe request coalescer for a high-throughput cache miss handler: multiple producers may request the same key concurrently; implement a global in-flight map with per-key single-flight semantics so the first fetch populates the result and remaining requests wait and reuse it. Support cancellation, per-key timeouts, and a backpressure throttling path that rejects new keys when inflight reaches threshold. Provide a runnable sketch and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a map of key to in-flight struct { done chan struct{}, result any, err error, ref int }. On miss, increment refs, start a single fetch, close done to release waiters, and cleanup. Each waiter read",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a three-stage pipeline with per-user session keys, bounded per-key buffers, and a watermark-based event-time model. Propagate backpressure with PAUSE/RESUME tokens from Aggregator to Transforme",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build per-service, per-event_type sliding window counts with bounded buffers. Ingest events with service_id, event_type, and timestamp; maintain a 10s window using per-service maps of event_type bucke",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: use per-user FIFO buffers fed from a bounded global queue; a sequencer enforces per-user order before dispatching to a fixed-size worker pool. Backpressure propagates from workers to produce",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of concurrency control, under load, with shared in-flight state and cancellation.\n\n## Key Concepts\n- Single-flight pattern per key to avoid duplicate work\n- In-flight tracking with per-key synchronization\n- Cancellation and per-key timeouts\n- Global backpressure via inflight cap\n\n## Code Example\n```go\n// sketch in Go-like pseudocode\ntype inflight struct {\n  done chan struct{}\n  val  any\n  err  error\n  ref  int\n}\n\nvar m = sync.Map{} // key -> *inflight\n\nfunc get(key string, fetch func() (any, error)) (any, error) {\n  if v, ok := m.Load(key); ok {\n    inf := v.(*inflight)\n    select {\n    case <-inf.done:\n      return inf.val, inf.err\n    default:\n      // wait for completion\n      <-inf.done\n      return inf.val, inf.err\n    }\n  }\n  inf := &inflight{done: make(chan struct{})}\n  actual, loaded := m.LoadOrStore(key, inf)\n  if loaded {\n    // another fetch started; wait\n    return get(key, fetch)\n  }\n  // this goroutine performs fetch\n  inf.ref++\n  val, err := fetch()\n  inf.val, inf.err = val, err\n  close(inf.done)\n  m.Delete(key)\n  return val, err\n}\n```\n\n## Follow-up Questions\n- How would you test for starvation under mixed-key traffic?\n- How would you adapt this for distributed workers across nodes?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-15",
        "questionId": "q-4482",
        "question": "In a real-time task processing system, multiple producers enqueue jobs into a global bounded queue with capacity C. A fixed pool of workers consumes jobs. Extend a beginner concurrency pattern to support two priority levels: high and normal. High-priority jobs must be served before normal ones, while preserving FIFO within each priority. Producers should block when the queue is full. Implement backpressure signaling and outline a concrete test plan to verify ordering and backpressure?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-topic FIFO queue and a global bounded capacity, e.g., a semaphore. A dispatcher pulls from non-empty topic queues and assigns to a small pool of workers. Topic creation/removal protected by",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a bounded, lock-free global ring buffer (size N) with per-client credit windows. Producers enqueue only if credits exist; else backpressure signals wait. Each client has a FIFO buffe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Python asyncio: give each producer a bounded queue, a central bounded queue collects coalesced updates, and a small worker pool applies updates in per-service order. A coalescer merges frequent up",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use two bounded queues (High and Normal) sharing capacity C. Enqueue blocks via a counting semaphore representing total items. A single dispatcher drains High first, then Normal, preserving FIFO withi",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical handling of multiple producers with prioritized fairness and backpressure in a simple, implementable pattern.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Priority with FIFO guarantees\n- Semaphore-based flow control\n- Testing ordering and deadlock avoidance\n\n## Code Example\n```javascript\n// Pseudo-code illustrating two queues with a dispatcher\nclass QueuePair {\n  constructor(capacity) { /* ... */ }\n  enqueueHigh(item) { /* block if full */ }\n  enqueueNormal(item) { /* block if full */ }\n  dispatch() { /* drain High then Normal */ }\n}\n```\n\n## Follow-up Questions\n- How would you adapt this for dynamic worker scaling?\n- How would you detect and recover from dispatcher starvation?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-16",
        "questionId": "q-30408",
        "question": "Design a real-time, multi-tenant event processor where events arrive tagged with tenant_id and event_type. Implement a central router that distributes events to per-tenant FIFO queues backed by a global bounded buffer, with a dynamic worker pool that can rebalance across tenants to maximize throughput. Ensure backpressure, strict per-tenant ordering, and exactly-once processing using per-tenant sequence numbers and a durable, replay-ready log, with cancellation and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Provide a Go sketch: a bounded global ring buffer feeding per-tenant FIFO queues; a dynamic worker pool that can rebalance across tenants. Enqueue blocks when the global buffer is full (backpressure).",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-user bounded queues (user_id -> ring buffer size B) with next_expected_seq[user]. A dispatcher marks ready users; M workers consume in-order sequences across keys. Support dynamic resizing by reba",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a Go-based solution: a fixed-size main queue (buffered channel) fed to a worker pool, plus per-market FIFO spill buffers. Backpressure blocks producers when the main queue is full; optional ex",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-stream bounded buffers keyed by partition, with a watermark-driven window and per-key queues for A and B. Use M workers to join when both sides have matching timestamps within window W;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests deep concurrency mastery: multi-tenant routing, dynamic rebalancing, and strong ordering guarantees under backpressure. Requires thinking about bounded buffers, fault tolerance, and replay semantics.\n\n## Key Concepts\n\n- Global bounded buffering and backpressure\n- Per-tenant FIFO ordering\n- Dynamic worker pool with rebalancing\n- Durable, sequence-numbered replay log for exactly-once semantics\n- Graceful shutdown and cancellation handling\n\n## Code Example\n\n```go\npackage main\n\ntype Event struct { Tenant string; Type string; ID int64; Payload []byte }\ntype Router struct { GlobalBuf chan Event; TenantQueues map[string]chan Event; Workers int; }\nfunc main() { /* sketch */ }\n```\n\n## Follow-up Questions\n\n- How would you test replay correctness after a crash?\n- How would you ensure fairness across tenants under bursty traffic?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-17",
        "questionId": "q-15543",
        "question": "Design a real-time telemetry concurrency controller for a multi-sensor platform. Ingest hundreds of sensor streams; each sensor's events must be processed in-order per arrival, even under bursts. Build per-sensor FIFO buffers feeding a dynamic worker pool that can resize at runtime. Implement global backpressure signaling when the total buffered workload approaches capacity, with a policy to throttle or drop non-critical data while preserving per-sensor order. Provide a runnable enqueue/dequeue sketch and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded global queue (maxsize N) and a per-topic deque for each topic. A dispatcher moves from per-topic buffers into the global queue; a small worker pool writes to the sink. When the global qu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Maintain per-tenant, per-session FIFO buffers and a global bounded queue. Each buffer holds events ordered by event_id; a scheduler ensures only the head of each buffer is dispatched, guaranteeing per",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-sensor FIFO queues and a central arbiter that assigns work to an elastic worker pool. Each sensor maintains in-order processing; global backpressure throttles producers via a bounded channel a",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a bounded worker pool with a fixed number of workers and a lock-free ring buffer. Block enqueue when full to enforce backpressure; use per-task cancellation via context and per-task timeouts",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a practical streaming control plane with per-source ordering, dynamic scaling, and backpressure in a high-throughput setting.\n\n## Key Concepts\n- Per-source FIFO buffers\n- Elastic worker pool with runtime resize\n- Global backpressure signaling to producers\n- Late-event handling and per-source budgets\n- Test plan coverage (ordering, throughput, resizes)\n\n## Code Example\n```javascript\n// Pseudocode enqueue sketch\nfunction enqueue(sensorId, event) {\n  // push to per-sensor queue, signal arbiter if needed\n}\n```\n\n## Follow-up Questions\n- How would you calibrate backpressure thresholds for burstiness?\n- How would you verify correctness under clock skew and dropped samples?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-18",
        "questionId": "q-4144",
        "question": "Design a beginner-friendly concurrency pattern for a bounded multi-producer, multi-consumer router: many producers publish tasks tagged by resource_id into a shared bounded global queue. Implement per-resource FIFO buffers and a small worker pool that processes tasks from those buffers, while guaranteeing per-resource order and fair progress between resources. Explain backpressure, starvation avoidance, and a simple test plan to verify ordering under bursty loads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-user FIFO queue system managed by a global bounded work queue with a supervisory coordinator ensuring ordering guarantees. Maintain a versioned atomic model reference that workers access via shared point…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a bounded global queue and per-resource FIFO buffers. A dispatcher uses round-robin to route incoming tasks by resource_id into small per-resource queues. A fixed-size worker pool drains the",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "An implementation uses per-app FIFOs with sequence numbers and a central watermark progress; each app gets a dedicated 2-phase queue path: ingress -> transformer -> reducer. Use bounded channels for e",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-entity FIFO queues keyed by entity_id and a global bounded buffer. Route each event to its entity queue with a per-entity sequence number to guarantee in-order processing, even as the worker pool dynamical…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis checks understanding of bounded queues, fairness, and per-key ordering with simple primitives.\n\n## Key Concepts\n\n- Bounded queues and backpressure\n- Per-resource FIFO ordering\n- Fair scheduling (round-robin)\n- Lightweight testing of ordering under concurrency\n\n## Code Example\n\n```javascript\n// Pseudo: dispatcher routes tasks to per-resource queues; workers drain in round-robin\n```\n\n## Follow-up Questions\n\n- How would you extend to dynamic resource sets?\n- How would you detect and mitigate starvation if a resource is rarely active?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-19",
        "questionId": "q-14201",
        "question": "Design a beginner-friendly concurrency exercise: a chat moderation pipeline where thousands of client events arrive concurrently; implement a bounded global queue feeding a small moderator pool, with per-chat-room FIFO buffers to preserve order, a per-room rate limiter to curb bursts, and a 'soft-drop' path that drops messages from least-active rooms when backlog exceeds a threshold while guaranteeing at least one message per active room. How would you approach testing and correctness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded global queue fed by a small moderator pool, with per-chat-room FIFO buffers to preserve order. Implement a per-room rate limiter (token bucket) and a soft-drop path that drops messages f",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a tri-stage, bounded-buffer pipeline that preserves per-device order across stages, supports dynamic worker resizing, and applies a latency-based late-data policy (drop or mark). Include per-de",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two per-stream input queues feed a bounded global queue. Each event carries stream_id and seq. A dispatcher assigns work to a dynamic pool, guaranteeing per-stream in-order processing and that HP even",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An ideal answer demonstrates a bounded queue plus a token bucket. A mutex guards an int tokens and a CV; a ticker periodically adds tokens (capped). Each producer blocks until tokens>0, then decrement",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of backpressure, fairness, and state management in a practical concurrency task, focusing on per-room buffering and degradation path in a streaming-like workload.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Per-room FIFO buffers\n- Per-room rate limiting\n- Soft-drop degradation\n- Graceful shutdown and in-flight handling\n\n## Code Example\n```javascript\n// Simple token bucket rate limiter\nclass TokenBucket {\n  constructor(rate, capacity) {\n    this.rate = rate;\n    this.capacity = capacity;\n    this.tokens = capacity;\n    this.last = Date.now();\n  }\n  allow() {\n    const now = Date.now();\n    const delta = (now - this.last) / 1000;\n    this.tokens = Math.min(this.capacity, this.tokens + delta * this.rate);\n    this.last = now;\n    if (this.tokens >= 1) {\n      this.tokens -= 1;\n      return true;\n    }\n    return false;\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test per-room ordering under concurrent producers?\n- How would you determine backlog thresholds and verify graceful shutdown?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-20",
        "questionId": "q-9513",
        "question": "You're building a cross-region real-time alerting service used by MongoDB, Plaid, and Discord. Events arrive from producers across regions and must be delivered to per-user consumers in strict per-user order, with at-least-once delivery and deduplication. Design a cross-region concurrency model: a bounded global queue, per-user buffers, and region-aware scheduling that migrates tasks to local workers. Explain backpressure, fault tolerance, and provide a minimal runnable enqueue/consume sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design and implement a global bounded-rate limiter using a central dispatcher with per-customer FIFO queues and a shared token-bucket pool. The dispatcher allocates tokens from a global capacity of R events/sec into per…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a two-layer scheduler: per-tenant FIFO queues guarded by a token-bucket quota, and a central arbiter that assigns M workers via deficit-round-robin to preserve per-tenant order while ensuring f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a cross-region concurrency model with a bounded global queue, per-user buffers, and region-aware scheduling that migrates tasks to local workers. Producers attach user_id, region, and seq; work",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a bounded worker pool with a fixed number of workers and a lock-free ring buffer. Block enqueue when full to enforce backpressure; use per-task cancellation via context and per-task timeouts",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess ability to architect cross-region concurrency with ordering guarantees, deduplication, and fault tolerance, beyond simple bounded queues.\n\n## Key Concepts\n\n- Cross-region task routing with locality hints\n- Per-user FIFO ordering and leasing\n- Backpressure and failover handling across regions\n\n## Code Example\n\n```javascript\n// Pseudocode: enqueue with region/user tags; worker leases\n```\n\n## Follow-up Questions\n\n- How would you test per-user ordering under bursty traffic and regional outages?\n- What data stores or queuing primitives would you choose and why?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-21",
        "questionId": "q-8949",
        "question": "Design a bounded, two-stage streaming pipeline with N producers and M workers per stage. Each record has a partition key and an event_time. Stage 1 applies stateless transforms; Stage 2 aggregates per key within tumbling windows of fixed length W. Guarantee per-key ordering across stages, while allowing parallelism across keys. Implement a watermark-based buffering strategy with bounded memory, handle late events, and specify the data structures and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant circular buffers and a sequencer to preserve per-tenant order. Maintain a global token bucket for backpressure; route to workers with a stable hash of tenant_id; lock-free structures (e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a two-tier scheduler with a compact fast-path queue and a larger slow-path buffer. Producers tag tasks by category; workers drain the fast path, and overflow is migrated to the slow path. Enfo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: maintain per-key FIFO buffers and a global watermark. Stage1 writes keyed records to per-key queues; Stage2 consumes in-key order to produce windowed aggregates. Use bounded ring buffers for",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a bounded global queue guarded by a counting semaphore to apply backpressure. Dequeue tasks into per-worker local deques; workers steal from neighbors to balance. Each task carries a deadline and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical concurrency patterns, including per-key ordering across stages, watermark management, bounded buffers, and late-data handling under backpressure.\n\n## Key Concepts\n\n- Watermarks and late data\n- Per-key ordering across stages\n- Bounded buffers and backpressure signaling\n- Tumbling window aggregation\n- Coordination between producers and workers\n\n## Code Example\n\n```javascript\n// Skeleton: per-key buffers and a simple watermark mechanism\nclass PerKeyBuffer {\n  constructor() { this.queues = new Map(); }\n  enqueue(key, item) { /* bounded enqueue per key */ }\n  dequeue(key) { /* return next item in-order if available */ }\n}\n```\n\n## Follow-up Questions\n\n- How would you test memory pressure and late-event handling?\n- How would you adapt to skewed keys or dynamic window sizes?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-22",
        "questionId": "q-2658",
        "question": "Design a bounded, fair, multi-producer/multi-consumer system for real-time order matching in a crypto exchange. Many producers submit limit orders; multiple workers attempt matches against an in-memory order book. Guarantee bounded memory, backpressure, and starvation-free fairness across producers (e.g., per-producer quotas or weighted fairness). Describe data structures, synchronization, and a minimal runnable Java snippet showing enqueue and a worker loop?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: implement per-agent FIFO buffers plus a bounded global queue feeding a small worker pool. Add a sampling mode activated when backlog exceeds a threshold: forward only every Kth event to the worker poo…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a bounded global queue (e.g., 1024) with per-match FIFOs. Use a 4-worker pool, each worker applying updates for a given match in sequence; track per-match sequencing to detect gaps. If backl",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-producer bounded queues fed into a rotating, weighted fair dispatcher that pushes into a fixed-size ring buffer consumed by worker threads. Use a single-order-book lock for matching, wit",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Proposed approach: allocate per-doc pending queues, a global distributor assigns edits to the corresponding doc queue; each doc tracks expected_seq; workers pull from a doc queue only when the next se",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design high-throughput, fair concurrency with bounded memory in a critical system like exchanges or dispatchers.\n\n## Key Concepts\n- Bounded queues with backpressure\n- Multi-producer/multi-consumer fairness\n- Dispatcher scheduling policies (weighted fair queuing)\n- Shared-order-book safety and lock granularity\n\n## Code Example\n```java\nimport java.util.concurrent.*;\nimport java.util.*;\n\npublic class BoundedFairDispatcher {\n  static class Order { final String id; Order(String id){ this.id = id; } }\n  private final int perProducerCap;\n  private final Map<String,BlockingQueue<Order>> queues = new ConcurrentHashMap<>();\n  private final BlockingQueue<Order> ring;\n  private volatile boolean shutdown = false;\n  public BoundedFairDispatcher(int perProducerCap, int ringCap){ this.perProducerCap = perProducerCap; this.ring = new ArrayBlockingQueue<>(ringCap); }\n  public void enqueue(String pid, Order o) throws InterruptedException {\n    BlockingQueue<Order> q = queues.computeIfAbsent(pid, k -> new ArrayBlockingQueue<>(perProducerCap));\n    q.put(o); // producer blocks if quota reached\n  }\n  public void dispatcherLoop() throws InterruptedException {\n    while (!shutdown) {\n      for (BlockingQueue<Order> q : queues.values()) {\n        Order o = q.poll();\n        if (o != null) ring.put(o);\n      }\n      // simple backoff to avoid busy spin\n      Thread.yield();\n    }\n  }\n  public void shutdown() { shutdown = true; }\n}\n```\n\n## Follow-up Questions\n- How would you scale to multiple dispatchers and partition rings?\n- How would you test fairness under highly skewed producer rates?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-23",
        "questionId": "q-13038",
        "question": "Design a beginner-friendly concurrency controller for real-time telemetry ingestion from a fleet of devices. Implement a global bounded queue with per-device FIFO buffers to preserve per-device order. Use a small worker pool to write to a time-series store. Add a degradation path: when backlog crosses a threshold, divert excess events to a latest-per-device sampler that drops others. Explain backpressure, fairness, and provide a test plan for per-device ordering, degradation behavior, and shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a telemetry ingest controller with a global bounded queue and per-device FIFO buffers to preserve per-device order. A small worker pool writes to a time-series store. When backlog hits a thr",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose per-vehicle bounded FIFOs of size B and a shared dispatcher. Producers append to the vehicle queue; workers fetch the heads, maintaining per-vehicle order with per-vehicle sequence numbers. Ba",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a map of key to in-flight struct { done chan struct{}, result any, err error, ref int }. On miss, increment refs, start a single fetch, close done to release waiters, and cleanup. Each waiter read",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a dict of per-project asyncio.Queues plus a global bounded worker pool guarded by a semaphore. A dispatcher routes to the right project queue; workers pull from queues in a round-robin fashion, en",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests handling of ingestion bottlenecks, per-source ordering, and safe degradation at the boundary. Requires concrete edge-case thinking rather than high-level concepts.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Per-device FIFO ordering\n- Graceful degradation via sampling\n- Race conditions and testing at the boundary\n\n## Code Example\n```javascript\n// Pseudocode sketch of enqueue path with degradation\nfunction enqueue(event){\n  if (globalQueue.size < MAX) push(event);\n  else degradeOrDrop(event); // either sample or drop\n}\n```\n\n## Follow-up Questions\n- How would you test per-device ordering under bursty traffic?\n- How would you verify the degradation sampler doesn't starve important devices?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-24",
        "questionId": "q-23269",
        "question": "Design a beginner-friendly concurrency exercise: a per-tenant event processor with per-tenant FIFO buffers and a global bounded queue consumed by a small worker pool. Implement a global rate limiter that guarantees total processing while preserving per-tenant ordering and preventing backlog from starving others. How would you test fairness, backpressure, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant FIFO buffers feeding a bounded global queue; a small worker pool drains to an external sink. Use a global token bucket (e.g., 100 ops/sec) with per-tenant credit, ensuring at leas",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a fixed-size Go channel as the global queue, a mutex-protected map[string]bool for in-flight tracking, and per-ID coalescing buffers. On enqueue: if the ID is already in-flight, merge the payload with the exis…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-client FIFO queues and a global bounded backlog. A single batcher selects the oldest requests across clients, fills batches of 32, and sends to the GPU; results route to the originating client",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-user FIFO buffers for both streams with a bounded size K. Use a pool of workers to pull from both buffers and emit joined pairs in ascending user_id order, coordinated by a central seque",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical concurrency design: per-tenant isolation, global pacing, and robust shutdown.\n\n## Key Concepts\n- Per-tenant FIFO and global bounded queue\n- Global rate limiter and fair credit allocation\n- Backpressure handling and graceful shutdown\n\n## Code Example\n```javascript\n// High-level sketch of token-bucket holder\nclass TokenBucket {\n  constructor(rate, capacity) { ... }\n  tryTake(n) { ... }\n}\n```\n\n## Follow-up Questions\n- How would you extend to N tenants with dynamic backlogs?\n- How to simulate fairness under varying burst patterns?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-27T12:23:55.783Z",
    "version": 1
  }
]