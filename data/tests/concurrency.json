[
  {
    "id": "test-concurrency",
    "channelId": "concurrency",
    "channelName": "Concurrency",
    "title": "Concurrency Knowledge Test",
    "description": "Test your understanding of Concurrency with 25 questions.",
    "questions": [
      {
        "id": "concurrency-q-0",
        "questionId": "q-10906",
        "question": "Design a beginner-friendly concurrency controller for an online gaming event processor: millions of clients publish match events; events must be processed in strict order per match, but events across matches may be handled in parallel. Build a global bounded queue with per-match FIFO buffers; implement a lightweight 'defer' path for low-priority events when backlog grows. Explain backpressure, fairness across matches, and provide a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route each event to its match-specific FIFO; a small worker pool processes one event at a time per match, preserving per-match order. Use a global bounded queue to cap inflight tasks; backlog triggers",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Two-level queue: a bounded global ring buffer plus per-tenant FIFO subqueues. Each event includes tenantId and a monotonic sequence. Producers push to the ring; a dispatcher forwards to the tenant’s q",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: implement per-aggregate FIFO buffers, a global sequencer to assign events to workers while preserving aggregate order, and a per-aggregate backpressure tag. Use bounded queue Q, a dynamic sh",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-partition bounded queues with backpressure signaling upstream; use 5-second tumbling windows with a monotonic watermark for time advancement; route late events to a buffer during the grace period; ensure e…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical concurrency control with per-key ordering, bounded buffers, and degradation paths in a beginner-friendly way.\n\n## Key Concepts\n\n- Per-match FIFO queues to preserve order\n- Global bounded queue to cap inflight tasks\n- Defer path for low-priority events during backlog\n- Fairness across matches via inflight caps and rotation\n- Graceful shutdown without losing in-flight events\n\n## Code Example\n\n```go\n// skeleton illustrating per-match channels and a bounded global queue\ntype Event struct{ MatchID string; Payload []byte; Priority int }\n\nvar globalQ = make(chan Event, 1024)\nvar perMatch = make(map[string]chan Event)\n// worker: serializes events per match while allowing parallelism across matches\n```\n\n## Follow-up Questions\n\n- How would you unit-test per-match ordering under burst traffic?\n- What thresholds would you tune to balance latency vs. throughput, and how would you detect unfairness across matches?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-1",
        "questionId": "q-20156",
        "question": "In a real-time analytics platform, thousands of producers publish events tagged by tenant IDs into a bounded central bus. Each tenant’s events must be FIFO, while a worker pool scales. Implement a dispatcher (Go or Rust) routing events to per-tenant queues, enforcing per-tenant backpressure with fair draining. Add a degradation path for hot tenants that samples. How would you structure tests to verify per-tenant order, backpressure, and degradation under burst?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a central bounded bus feeding a dispatcher that writes into per-tenant FIFO queues (ring buffers). Block enqueue when a tenant queue hits capacity; workers drain queues round-robin to preserve order.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Produce a bounded global queue with per-tenant FIFO buffers and a worker pool. Each tenant has a token-bucket limiter; producers await when their bucket is empty or the queue is full. Backpressure pro",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant FIFO buffers of size K feeding a global bounded queue Q. Each tenant gets a dynamic cadence window controlled by a timer-wheel, with tokens that throttle bursty tenants but allow fast t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly concurrency exercise: 200 producers, each with a per-service FIFO buffer. A small worker pool drains a shared bounded queue to storage. Implement a per-service round-robin s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to design multi-tenant, high-throughput concurrency systems with strict per-tenant ordering guarantees, bounded resource allocation, and controlled degradation under load. It emphasizes testing strategy alongside implementation details.\n\n## Key Concepts\n\n- **Bounded central bus** and per-tenant FIFO queues\n- **Dispatcher** with fair draining (round-robin or weighted)\n- **Per-tenant backpressure** and graceful degradation through sampling\n- **Observability**: ordering guarantees, latency under burst, degraded path metrics\n\n## Code Example\n\n```go\n// Minimal Go sketch showing structures (not a full solution)\npackage main\n\nimport (\n\t\"context\"\n\t\"sync\"\n\t\"time\"\n)\n\ntype Event struct {\n\tTenantID string\n\tPayload  []byte\n\tTimestamp time.Time\n}\n\ntype TenantQueue struct {\n\tID      string\n\tBuffer  chan *Event\n\tmu      sync.Mutex\n\tdropped int64\n}\n\nfunc NewTenantQueue(id string, cap int) *TenantQueue {\n\treturn &TenantQueue{\n\t\tID:     id,\n\t\tBuffer: make(chan *Event, cap),\n\t}\n}\n\nfunc (q *TenantQueue) Enqueue(e *Event) bool {\n\tselect {\n\tcase q.Buffer <- e:\n\t\treturn true\n\tdefault:\n\t\tatomic.AddInt64(&q.dropped, 1)\n\t\treturn false\n\t}\n}\n\ntype Dispatcher struct {\n\tqueues    map[string]*TenantQueue\n\tmu        sync.RWMutex\n\tworkQueue chan *Event\n}\n\nfunc NewDispatcher(bufferPerTenant int) *Dispatcher {\n\treturn &Dispatcher{\n\t\tqueues:    make(map[string]*TenantQueue),\n\t\tworkQueue: make(chan *Event, 10000),\n\t}\n}\n\nfunc (d *Dispatcher) GetQueue(tenantID string) *TenantQueue {\n\td.mu.RLock()\n\tq, ok := d.queues[tenantID]\n\td.mu.RUnlock()\n\t\n\tif ok {\n\t\treturn q\n\t}\n\t\n\td.mu.Lock()\n\tdefer d.mu.Unlock()\n\t// Double-check after acquiring write lock\n\tif q, ok := d.queues[tenantID]; ok {\n\t\treturn q\n\t}\n\tq = NewTenantQueue(tenantID, 1000)\n\td.queues[tenantID] = q\n\treturn q\n}\n\nfunc (d *Dispatcher) HandleEvent(e *Event) {\n\tqueue := d.GetQueue(e.TenantID)\n\tif !queue.Enqueue(e) {\n\t\t// Apply degradation: sample or drop\n\t\td.handleDegraded(e)\n\t}\n}\n\nfunc (d *Dispatcher) handleDegraded(e *Event) {\n\t// Implement sampling logic for hot tenants\n\tif time.Now().UnixNano()%10 == 0 {\n\t\td.workQueue <- e\n\t}\n}\n\nfunc (d *Dispatcher) StartWorkers(ctx context.Context, numWorkers int) {\n\tvar wg sync.WaitGroup\n\tfor i := 0; i < numWorkers; i++ {\n\t\twg.Add(1)\n\t\tgo func(workerID int) {\n\t\t\tdefer wg.Done()\n\t\t\tfor {\n\t\t\t\tselect {\n\t\t\t\tcase <-ctx.Done():\n\t\t\t\t\treturn\n\t\t\t\tcase e := <-d.workQueue:\n\t\t\t\t\t// Process event\n\t\t\t\t\t_ = e\n\t\t\t\t}\n\t\t\t}\n\t\t}(i)\n\t}\n\twg.Wait()\n}\n\nfunc main() {\n\td := NewDispatcher(1000)\n\tctx, cancel := context.WithCancel(context.Background())\n\tdefer cancel()\n\t\n\td.StartWorkers(ctx, 4)\n}\n```\n\n## Testing Strategy\n\n1. **Per-tenant order verification**: Inject ordered events per tenant, drain, and assert sequence integrity\n2. **Backpressure validation**: Fill tenant buffers, verify blocking behavior and caller notifications\n3. **Burst and degradation**: Simulate traffic spikes, measure sampling ratios and dropped event counts\n4. **Fairness testing**: Ensure no tenant starves regardless of event volume",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-2",
        "questionId": "q-24819",
        "question": "Design a bounded, multi-source stock feed processor: N producers emit Trade events {symbol, ts, seq}, into a central bounded queue. M workers apply trades to per-symbol state; global ordering must be by ts (not per symbol), and we must preserve per-symbol order for downstream outputs. Implement a mechanism to replay out-of-order trades when late events arrive, with a configurable canary window W. Support dynamic worker resizing and a backpressure policy (stall producers or drop oldest to make room). Provide data structures and a runnable sketch and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a central bounded channel for in-flight messages, a map of per-room FIFO buffers, and a small worker pool. Producers enqueue into the room buffer, a router forwards to the global bound, and wor",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a bounded global queue with per-region queues. Workers dequeue from region buffers in a round-robin fairness scheme. Use a semaphore to cap in-flight matches. When backlog exceeds high water",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose per-vehicle bounded FIFOs of size B and a shared dispatcher. Producers append to the vehicle queue; workers fetch the heads, maintaining per-vehicle order with per-vehicle sequence numbers. Ba",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a bounded reorder buffer using a global min-heap keyed by ts for ready Trade events, plus per-symbol FIFO queues to ensure in-symbol order. Use a watermark that advances as the earliest unpr",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of multi-source concurrency with global time-ordering, backpressure, and dynamic workers. It requires combining per-key ordering with global sequencing and late-event handling.\n\n## Key Concepts\n- Global time-ordering via ts and a watermark\n- Bounded central queue with per-symbol buffers\n- Reordering window and late-event replay\n- Dynamic worker resizing and backpressure policies\n- Testing with synthetic traces (late/out-of-order bursts)\n\n## Code Example\n```javascript\nclass Trade { constructor(symbol, ts, seq, payload) { this.symbol = symbol; this.ts = ts; this.seq = seq; this.payload = payload; } }\nclass ReorderBuffer {\n  constructor(limit) {\n    this.limit = limit; // bound\n    // min-heap by ts\n    this.heap = new MinHeap((a,b)=> a.ts-b.ts);\n    this.perSymbol = new Map();\n    this.watermark = -Infinity;\n  }\n  push(trade) { /* ... */ }\n  emitReady(upTo) { /* ... */ }\n}\n```\n\n## Follow-up Questions\n- How would you test late events and tie-breakers?\n- How would you adjust window size under bursty feeds?\n- How would you measure and enforce per-trade latency and ordering guarantees?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-3",
        "questionId": "q-22414",
        "question": "Design a real-time fraud-detection pipeline with three stages: Ingest (N producers) emits transactions: {user_id, txn_id, ts, amount}; Stage1 (M detector workers) runs stateless anomaly checks and yields candidate alerts; Stage2 (K aggregator workers) maintains per-user risk state and emits alerts if risk crosses threshold. Guarantee per-user in-order processing across stages under dynamic resizing and backpressure. Implement a soft backpressure: when downstream backlog exceeds B, throttle Ingest and Stage1; snapshot per-user checkpoints. Ensure at-least-once or exactly-once alert emission with dedup by (txn_id, user_id). Provide data structures, a runnable sketch, and a test plan for late events, restarts, and backpressure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-user sequence tokens and partitioned queues to keep order across stages. Route all events for a user to the same detector/aggregator path and bound buffers with a shared backpressure signal. F",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Partition events by app, each with a bounded per-app queue and a small worker pool. Producers backpressure when queues fill; a lag detector triggers dynamic rebalancing across partitions. Guarantee pe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a bounded global queue with per-region queues. Workers dequeue from region buffers in a round-robin fairness scheme. Use a semaphore to cap in-flight matches. When backlog exceeds high water",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Solution Design ### Architecture Overview A partitioned queue system with per-channel FIFO subqueues, a dispatcher pool, and SLA-based spill policies. ### Core Components 1. **Channel Queues**: Each channel maintains…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design multi-stage concurrency with ordering guarantees, backpressure, and restart-safe exactly-once semantics in a fraud-detection pipeline.\n\n## Key Concepts\n\n- Per-user ordering across stages\n- Bounded queues and cross-stage backpressure\n- Idempotent alert emission and per-user checkpoints\n- Dynamic worker resizing and fault tolerance\n\n## Code Example\n\n```javascript\n// Pseudo sketch: per-user partitioning, offsets, and commit points\n```\n\n## Follow-up Questions\n\n- How would you simulate late events and restarts in tests?\n- What metrics would you monitor to detect backpressure tipping points?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-4",
        "questionId": "q-6592",
        "question": "In a distributed telemetry pipeline for a fleet of delivery robots, producers emit events with robot_id, seq, and payload. Design a concurrency strategy to process events with per-robot in-order handling, a bounded in-flight cap, and exactly-once persistence to an external store. Requirements: 1) bounded global queue with capacity, 2) per-robot buffers to preserve order, 3) a small worker pool to apply side effects, 4) handle late/duplicate events, 5) provide a runnable sketch in Go or Rust and a test plan for ordering, liveness, and recovery. How would you implement this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-device FIFO buffers chained through a bounded global queue with a sequencer to assign monotonic epochs per device. Transformers use work-stealing with local queues to maintain per-device ordering while max…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design and implement a global bounded-rate limiter using a central dispatcher with per-customer FIFO queues and a shared token-bucket pool. The dispatcher allocates tokens from a global capacity of R events/sec into per…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a bounded global queue fed by many producers. Implement a dynamic worker pool that scales from minW to maxW based on queue depth (high watermark triggers growth, low watermark triggers shrink).",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a bounded global queue with capacity K and per-robot buffers. Incoming events route to per-robot buffers in sequence order; workers fetch in-order from each buffer. Track next_seq per robot and pe",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design scalable, low-latency ingestion with strict per-entity ordering and strong delivery guarantees, under memory pressure. It probes backpressure handling, out-of-order recycling, and failure recovery.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Per-entity in-order processing\n- Exactly-once semantics via commit logs and idempotent writes\n- Late/duplicate event handling and recovery strategies\n\n## Code Example\n```rust\n// Minimal sketch: per-robot buffers + next_seq tracking\n```\n\n## Follow-up Questions\n- How would you scale the worker pool under bursty traffic?\n- How would you test failure scenarios (partial outages, replay) and monitor liveness?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-5",
        "questionId": "q-10952",
        "question": "Describe a real-time multi-tenant event processor with a shared bounded queue. Each event has tenant_id and event_id. Guarantee per-tenant in-order processing using per-tenant FIFO buffers of size K, plus a credit-based scheduler that dynamically allocates tokens based on observed arrival rates and SLAs. Include a runnable enqueue/consume sketch and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant subqueues and a central arbiter with a credit/aging scheme. Enqueue grants a tenant credit; the scheduler selects the tenant by a best (credit, age) ratio to limit latency and avoid sta",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a global bounded queue and per-tenant FIFO buffers. A fixed worker pool drains the global queue and enqueues events to their tenant buffers, which are also FIFO. Aggregators read tenant buffers in",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Solution Design ### Architecture Overview A partitioned queue system with per-channel FIFO subqueues, a dispatcher pool, and SLA-based spill policies. ### Core Components 1. **Channel Queues**: Each channel maintains…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a shared bounded queue with per-tenant FIFO buffers. Each event has tenant_id and event_id. Guarantee per-tenant in-order processing via buffers of size K and a credit-based scheduler that allocat",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of per-tenant ordering, fairness, and dynamically adjusting backpressure in a bounded queue.\n\n## Key Concepts\n- per-tenant FIFO buffers\n- bounded global queue\n- credit-based scheduler\n- aging to prevent starvation\n- test plan and correctness\n\n## Code Example\n```javascript\n// Runnable enqueue/consume sketch (high-level)\nclass TenantBuffer {\n  constructor(K) { this.K = K; this.q = []; this.nextEvent = 0; }\n  enqueue(e) { if (this.q.length >= this.K) return false; this.q.push(e); return true; }\n  dequeue() { if (!this.q.length) return null; return this.q.shift(); }\n}\nfunction schedulerStep(state) {\n  // allocate tokens based on rates and SLA, age aging\n}\n```\n\n## Follow-up Questions\n- How would you calibrate K and token weights? How would you handle duplicates and resets? How would you simulate SLA drift and backpressure bursts?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-6",
        "questionId": "q-8993",
        "question": "Design a beginner-friendly concurrency controller for a service that calls three external APIs, each with its own rate limit. Many producers submit API-call tasks labeled with apiId; implement a bounded global queue and per-api token buckets. A distributor assigns tasks to a small worker pool only when a token is available. Include backpressure, fairness across APIs, and a concrete test plan for rate-limit adherence and retry behavior?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a two-path, bounded pipeline with per-key rate tracking via a sliding window; route spiraling keys to a hot path using per-key buffers and sequence numbers to guarantee in-order delivery acr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a bounded global queue and per-api token buckets. Before dispatch, a worker must acquire a token from that API’s bucket (refill rate equals the API limit). If tokens are exhausted, producers backp",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Produce a bounded global queue with per-tenant FIFO buffers and a worker pool. Each tenant has a token-bucket limiter; producers await when their bucket is empty or the queue is full. Backpressure pro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two approaches: (A) Locks/Cond: track count, target N, and a generation value. Each worker increments count and waits while count < N; the last increments generation and broadcasts; all wake, verify g",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of rate-limiting, backpressure, and fair scheduling across multiple external API endpoints, plus handling retries in a simple, beginner-friendly way.\n\n## Key Concepts\n- Bounded queues\n- Per-API token bucket rate limiter\n- Backpressure and fairness\n- Retry with exponential backoff\n\n## Code Example\n```javascript\n// Pseudo: token buckets and distributor sketch\n```\n\n## Follow-up Questions\n- How would you test bursty traffic across APIs and ensure no API starves?\n- How would you adapt for dynamic API limits or flaky endpoints?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-7",
        "questionId": "q-13339",
        "question": "Design a beginner-friendly concurrency scheduler for a live multiplayer lobby system: thousands of players (producers) submit match-requests tagged by region and skill tier. A global bounded queue feeds a small pool of match-makers (workers). Per-region FIFO buffers preserve regional ordering, while a lightweight fairness policy ensures no region starves. Add a degradation path: when backlog exceeds a threshold, divert excess requests to a 'best-effort' path that assigns provisional matches using a heuristic. Explain backpressure, fairness, and provide a pragmatic test plan for regional ordering, degradation behavior, and shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an asyncio.Queue(maxsize=128) with 4 worker coroutines. Producers await queue.put(task) to apply backpressure. Each worker pops tasks, processes image transforms, then calls queue.task_done(). On",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route by shard to per-shard fixed-size ring buffers; each shard has a single in-order consumer. Producers hash the shard key and enqueue; if any buffer is full, block until space frees (backpressure).",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a bounded global queue with per-region queues. Workers dequeue from region buffers in a round-robin fairness scheme. Use a semaphore to cap in-flight matches. When backlog exceeds high water",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-client bounded FIFO buffers (size K) + a global scheduler that orders by arrival time and tracks per-client progress. Dispatch the oldest ready event to any worker across stages ingest→filter→enco",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nIn live multiplayer matchmaking, concurrency and fairness matter. This question tests ability to design a simple scheduler that preserves per-region order while handling bursts and backpressure.\n\n## Key Concepts\n\n- Bounded queues and backpressure\n- Per-region FIFO buffers and fairness\n- Lightweight degradation path with best-effort matching\n- Graceful shutdown drained in-flight tasks\n\n## Code Example\n\n```javascript\n// Skeleton sketch of per-region buffers and dispatcher\nclass Scheduler {\n  constructor(maxGlobal, regions) { /* ... */ }\n  submit(req) { /* enqueue into global or region buffer */ }\n  dispatch() { /* rotate through regions and pop from buffers to workers */ }\n}\n```\n\n## Follow-up Questions\n\n- How to test starvation under bursty region loads?\n- How to tune high/low watermarks and observe metrics?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-8",
        "questionId": "q-15616",
        "question": "Design a beginner-friendly concurrency exercise for a plugin-based render queue in a video editor. Multiple plugins (producers) push render tasks into a bounded global queue; a pool of worker threads consumes tasks and outputs frames. Preserve per-plugin FIFO ordering, apply backpressure when full, and implement a degradation path that marks or drops non-critical tasks (e.g., preview frames) when backlog grows beyond a threshold. Explain backpressure, fairness, and provide a concrete test plan for per-plugin ordering, backlog behavior under bursts, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded global queue with per-plugin queues, and a worker pool consuming from all plugin queues in a round-robin or priority-aware fashion. Enforce backpressure by blocking producers when full;",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose: use a bounded global ring buffer with a per-document min-heap of in-flight edits keyed by a logical vector clock. Each document has a sequence frontier; producers enqueue edits; workers commi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest events with tenant_id, event_id, and ts; route to per-tenant bounded queues; maintain per-tenant sequence and in-order delivery by a central dispatcher that assigns ready events to a dynamic wo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a bounded global queue feeding per-topic FIFO queues. Producers tag events with topic IDs and block when full. A dispatcher assigns work to workers while preserving per-topic order, using a top",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of basic concurrency concepts in a realistic media pipeline, focusing on producer fairness, backpressure, and graceful shutdown.\n\n## Key Concepts\n\n- Bounded queues and backpressure\n- Per-plugin FIFO\n- Degradation path for non-critical tasks\n- Graceful shutdown and drain behavior\n\n## Code Example\n\n```javascript\n// Pseudo-code sketch\nclass BoundedQueue { constructor(limit){ this.limit=limit; this.q=[]; } push(t){ if(this.q.length>=this.limit) throw 'full'; this.q.push(t); } pop(){ return this.q.shift(); } }\n```\n\n## Follow-up Questions\n\n- How would you detect and mitigate producer starvation?\n- How would you test backlog thresholds and graceful shutdown?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-9",
        "questionId": "q-24238",
        "question": "Design a beginner-friendly concurrency exercise: a web analytics event router where dozens of agents produce events; use a bounded global queue and per-agent FIFO buffers to preserve per-agent ordering. Add a new 'sampling mode' when backlog crosses a threshold: forward only every Kth event to the worker pool, queue the remainder for later; ensure graceful fallback once backlog recedes. Explain backpressure and fairness, and provide a concrete test plan for per-agent ordering, sampling accuracy, and shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-user bounded input buffers, a dynamic work-stealing worker pool, and per-user sequence tracking. Out-of-order events are routed to a per-user gap buffer and replayed in-order once gaps are filled, with con…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A minimal Go or Python asyncio solution: implement a bounded queue with per-producer buffers; on enqueue, if capacity reached, stall new items until space frees up. Ensure workers dequeue in order of",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a global bounded queue with a pool of workers, plus per-tenant FIFO buffers and a distributor that enforces in-order processing for each tenant. Add aging to increase priority for stalled tenants",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed approach: implement per-agent FIFO buffers plus a bounded global queue feeding a small worker pool. Add a sampling mode activated when backlog exceeds a threshold: forward only every Kth event to the worker poo…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design a low-level concurrent router with per-producer ordering, a bounded queue, and a controlled degradation path using sampling. It demonstrates practical handling of backpressure and fairness without requiring large-scale infrastructure.\n\n## Key Concepts\n- **Per-agent FIFO buffers** preserve ordering for each producer\n- **Global bounded queue** with backpressure signaling to prevent memory exhaustion\n- **Sampling mode** to throttle backlog growth when system is overwhelmed\n- **Fairness**: round-robin or alternating drain across agents\n- **Graceful degradation and shutdown**: handling backlog when load normalizes and safely draining on shutdown\n\n## Code Example\n```javascript\n// Simplified architecture:\n// 1. Each agent has a dedicated FIFO buffer\n// 2. Global dispatcher reads from buffers in round-robin fashion\n// 3. When total backlog exceeds THRESHOLD, enable sampling mode\n\nclass EventRouter {\n  constructor(workerCount, samplingThreshold, sampleRate) {\n    this.agents = new Map();\n    this.globalQueue = new BoundedQueue(1000);\n    this.workers = new WorkerPool(workerCount);\n    this.samplingThreshold = samplingThreshold;\n    this.sampleRate = sampleRate; // K\n    this.samplingMode = false;\n  }\n\n  registerAgent(agentId) {\n    this.agents.set(agentId, { buffer: [], paused: false });\n  }\n\n  async submit(agentId, event) {\n    const agent = this.agents.get(agentId);\n    if (!agent) throw new Error(`Unknown agent: ${agentId}`);\n\n    const totalBacklog = this.calculateBacklog();\n    \n    if (totalBacklog > this.samplingThreshold && !this.samplingMode) {\n      this.samplingMode = true;\n    }\n\n    if (this.samplingMode) {\n      if (this.sampleCounter++ % this.sampleRate === 0) {\n        await this.globalQueue.offer(event);\n      } else {\n        agent.buffer.push(event);\n      }\n    } else {\n      agent.buffer.push(event);\n    }\n  }\n\n  calculateBacklog() {\n    return Array.from(this.agents.values())\n      .reduce((sum, a) => sum + a.buffer.length, 0);\n  }\n\n  // Graceful fallback: resume queued events when backlog recedes\n  maybeExitSamplingMode() {\n    if (this.samplingMode && this.calculateBacklog() < this.samplingThreshold * 0.5) {\n      this.samplingMode = false;\n      this.drainQueuedEvents();\n    }\n  }\n}\n```\n\n## Test Plan\n\n### 1. Per-Agent Ordering Test\n- Register 3 agents (A, B, C)\n- Submit events in order: A1, A2, A3, B1, B2, B3, C1, C2, C3\n- Verify output preserves per-agent sequence: A events arrive in order, same for B and C\n- Run 1000 iterations with different submission patterns\n\n### 2. Sampling Accuracy Test\n- Set sampleRate = 10 (forward every 10th event)\n- Submit 1000 events from a single agent\n- Verify ~100 events forwarded immediately, ~900 queued\n- Confirm queued events are forwarded after sampling mode exits\n\n### 3. Backpressure Test\n- Fill buffers to capacity\n- Verify submit() blocks or rejects when queue is full\n- Verify memory stays bounded under load\n\n### 4. Fairness Test\n- Submit 1000 events each from 5 agents\n- Verify no agent is starved (each gets processed)\n- Measure inter-arrival times between agents\n\n### 5. Shutdown Test\n- Start router with pending events in buffers\n- Initiate graceful shutdown\n- Verify all in-flight events complete\n- Verify no events are lost",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-10",
        "questionId": "q-30506",
        "question": "Design a beginner-friendly concurrency exercise: a multi-tenant serverless scheduler. Tenants (A-D) submit function invocations; a global bounded worker pool executes functions. Each tenant's invocations must be processed in arrival order (per-tenant FIFO), while invocations from different tenants are interleaved for parallelism. Implement per-tenant buffers, global backpressure, and a degradation path: when backlog grows, suspend low-priority tenants and let high-priority ones proceed with a best-effort policy. Provide a concrete test plan for per-tenant ordering, backpressure, and degradation fairness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a bounded, multi-tenant scheduler with per-tenant FIFO queues feeding a global ready-queue. Enforce per-tenant order while using a fairness policy (weighted max-min) to pick tenants for the next task dispatch. Su…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement with a bounded global asyncio.Queue(maxsize=1000), a dict tenant_queues[tenant] -> asyncio.Queue(). Dispatcher cycles tenants in RR and enqueues to their queues; workers (N) drain per-tenant",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bounded global queue (maxsize N) and a per-topic deque for each topic. A dispatcher moves from per-topic buffers into the global queue; a small worker pool writes to the sink. When the global qu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant FIFO buffers feed a fixed-size worker pool. Each tenant's invocations are serialized before enqueue, preserving order; a global scheduler interleaves across tenants to maximize throughput.",
            "isCorrect": true
          }
        ],
        "explanation": "Why This Is Asked\nTests understanding of per-group ordering within a shared pool, dynamic backpressure signaling to producers, and a degradation strategy that preserves essential throughput under load. The scenario mirrors real-world multi-tenant schedulers in serverless platforms.\n\nKey Concepts\n- Per-tenant FIFO buffers\n- Bounded global worker pool\n- Backpressure signaling and propagation\n- Degradation path and fairness under load\n\nCode Example\n```python\nfrom collections import deque\nfrom threading import Lock, Condition\n\nclass Scheduler:\n    pass  # skeleton to implement per-tenant buffers, enqueue, and dispatch\n```\n\nFollow-up Questions\n- How would you handle sudden tenant priority flips or orphaned futures?\n- How would you test starvation guarantees and shutdown behavior?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-11",
        "questionId": "q-21693",
        "question": "Design a bounded, per-customer FIFO dispatch system for a rideshare platform where thousands of drivers subscribe to their own queues; producers publish ride requests; the dispatcher must route to per-customer queues, ensure per-customer order, apply backpressure to producers when queues fill, support dynamic worker pools, and provide a runnable minimal Python asyncio sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage per-key sequence buffers in the Transformer: map key->ordered queue, one global bounded channel, and a speculative layer that routes to fast-path and slow-path with a commit/rollback protocol",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a central broker with a bounded global queue and per-customer FIFO queues. Producers enqueue ride requests to the global queue; a dispatcher assigns tasks to the corresponding per-customer q",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a bounded submission system using a dual-layer rate-limiting approach: per-key leaky buckets for API key limits (2 req/s with burst 4) combined with a global token bucket for the 40 req/s cap. Each client requ…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: use a bounded global queue and per-sensor FIFO buffers; a worker pool processes events and pushes to a dedupe layer (map of last-seen timestamp per sensor). Keep a 1000ms sliding window; dro",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests ability to design a scalable, low-latency dispatcher that preserves per-key ordering while handling bursts and worker pool dynamics. It also probes failure handling and backpressure reasoning in a distributed system.\n\n## Key Concepts\n- Bounded queues and backpressure propagation\n- Per-customer FIFO ordering (per-key sequencing)\n- Dispatcher design and worker pool fairness\n- Acks, retries, cancellation, and shutdown\n\n## Code Example\n```python\n# Minimal sketch of dispatcher and queues\nimport asyncio\n\nasync def demo():\n    global_q = asyncio.Queue(2)\n    per_customer = {}\n\n    async def producer(cid, item):\n        await global_q.put((cid, item))\n\n    async def dispatcher():\n        while True:\n            cid, item = await global_q.get()\n            q = per_customer.setdefault(cid, asyncio.Queue(4))\n            await q.put(item)\n            global_q.task_done()\n\n    async def worker(cid):\n        q = per_customer[cid]\n        while True:\n            it = await q.get()\n            # process(it)\n            q.task_done()\n\n    # startup omitted for brevity\n```\n\n## Follow-up Questions\n- How would you test per-customer ordering under bursty Producer load?\n- How would you handle graceful shutdown with in-flight tasks?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-12",
        "questionId": "q-735",
        "question": "Design a concurrency-safe per-channel message queue for a Discord-like chat service: multiple producers push messages, a single consumer persists them to storage; implement bounded capacity, preserve per-channel order, and handle backpressure. Which synchronization primitives would you use and how would you test edge cases?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-channel bounded queue (ring buffer) guarded by a mutex, with two condition variables: notFull and notEmpty. Producers block on full; a single consumer dequeues in arrival order and persists",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a bounded global Go channel combined with per-topic FIFO queues. Producers publish to topic-specific queues; a dispatcher goroutine enforces per-topic ordering while interleaving topics across the global chann…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design and implement a bounded ingress -> worker pool -> per-key in-order processor. Each event carries (key, seq, payload). A central scheduler assigns tasks to workers while each key has its own FIF",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a global bounded queue and per-tenant FIFO buffers. A fixed worker pool drains the global queue and enqueues events to their tenant buffers, which are also FIFO. Aggregators read tenant buffers in",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nConcurrency is central to both Discord-like chat and Snowflake-like storage. This question probes ability to map a real production pattern (per-channel queues) to concrete synchronization, backpressure, and ordering guarantees.\n\n## Key Concepts\n- Producer-consumer pattern\n- Bounded buffers and backpressure\n- Mutexes and condition variables\n- Per-channel ordering and fault handling\n\n## Code Example\n\n```javascript\nclass BoundedQueue {\n  constructor(capacity) {\n    this.capacity = capacity;\n    this.buf = new Array(capacity);\n    this.head = 0;\n    this.tail = 0;\n    this.size = 0;\n    this.mux = new Mutex();\n    this.notFull = new ConditionVariable();\n    this.notEmpty = new ConditionVariable();\n  }\n  enqueue(item) {\n    // acquire mutex; wait while size === capacity; store at tail; tail=(tail+1)%capacity; size++; notEmpty.signal();\n  }\n  dequeue() {\n    // acquire mutex; wait while size === 0; read from head; head=(head+1)%capacity; size--; notFull.signal();\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test backpressure and ordering across bursty producers?\n- How would you extend to multiple consumers while preserving per-channel order?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-13",
        "questionId": "q-25107",
        "question": "Design a bounded, multi-tenant task scheduler in Go for a real-time analytics pipeline. Thousands of producers push events labeled tenant_id and seq into a global bounded intake; a dispatcher must preserve per-tenant FIFO order while draining a fixed worker pool fairly across tenants. Implement dynamic backpressure when backlog grows, and provide a runnable sketch plus a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-client FIFO buffers feeding a bounded global queue, with a fixed worker pool processing tasks in arrival order per client. Enforce ordering by sequencing within each client and routing t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a two-stage log pipeline with N tenants feeding a bounded global queue; M workers parse/enrich; K workers perform per-tenant windowed aggregation in timestamp order. Preserve per-tenant order a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a global bounded intake channel feeding per-tenant FIFO queues. A dispatcher allocates work to a fixed worker pool using a round-robin over active tenants, preserving order via next_seq per tenant",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: implement N per-source FIFO buffers feeding a single bounded queue; a dispatcher with M workers runs a per-source fairness scheduler (weighted fair queuing) to select events and route them to si",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a scalable, fair scheduler under multi-tenant load with strict ordering and backpressure guarantees.\n\n## Key Concepts\n\n- Per-tenant FIFO ordering\n- Global bounded buffer and backpressure\n- Fair draining with round-robin across tenants\n- Graceful shutdown and error handling\n\n## Code Example\n\n```go\n// skeleton illustrating queues per tenant and a dispatcher loop\n```\n\n## Follow-up Questions\n\n- How would you measure and mitigate tail latency for lagging tenants?\n- What race conditions might arise and how would you test for them?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-14",
        "questionId": "q-17772",
        "question": "Design a beginner-friendly concurrency exercise: multiple clients publish video-encoding tasks labeled by account_id to a bounded global queue with per-account FIFO buffers. A small worker pool consumes tasks; implement fairness so no account starves by rotating accounts every N tasks. Add a degradation path: when backlog per account exceeds a threshold, divert tasks to a fast path that emits a low-resolution preview instead of full encoding. Include a concrete test plan covering per-account ordering, fairness under bursts, backlog-triggered degradation, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a bounded global queue with per-account FIFO buffers and a small round-robin scheduler across accounts. Each worker pulls from the current account buffer; after every K tasks, rotate",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I'd implement with Python asyncio. Use a bounded global queue (maxsize 1024) plus per-device FIFO queues. A central arbiter drains the global queue and forwards events to device queues in arrival orde",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a bounded, per-source-ordered event-time engine for a multi-tenant analytics platform. Producers push events with source_id and event_time; the system must preserve per-source order, handle bursts with bounded me…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a shared bounded queue with per-tenant FIFO buffers. Each event has tenant_id and event_id. Guarantee per-tenant in-order processing via buffers of size K and a credit-based scheduler that allocat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of per-group ordering, backpressure, and simple fairness in a beginner-friendly setting.\n\n## Key Concepts\n- Per-group FIFO and global bounded queue\n- Round-robin fairness and backpressure\n- Degradation path and graceful shutdown\n\n## Code Example\n```python\nfrom collections import deque, defaultdict\nclass Scheduler:\n    def __init__(self, gcap, per_cap):\n        self.global_q = deque()\n        self.buffers = defaultdict(deque)\n        self.gcap = gcap\n        self.per_cap = per_cap\n        self.account_order = deque()\n    def push(self, account, item):\n        q = self.buffers[account]\n        q.append(item)\n        if account not in self.account_order:\n            self.account_order.append(account)\n    def pop(self):\n        if not self.account_order:\n            return None\n        acc = self.account_order[0]\n        item = self.buffers[acc].popleft()\n        if not self.buffers[acc]:\n            self.account_order.popleft()\n        return acc, item\n```",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-15",
        "questionId": "q-18498",
        "question": "Design a beginner-friendly concurrency exercise: a log-ingest server collects logs from dozens of services. Each service has its own FIFO buffer to preserve order, and all buffers feed a single bounded global queue consumed by a small worker pool that writes to a sink. When backlog crosses a threshold, implement a degradation path that retains only high-priority logs (ERROR/WARN) and drops the rest with a clear policy. Explain backpressure, per-service fairness, and provide a concrete test plan for per-service ordering, degradation behavior, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: use a bounded global queue and per-sensor FIFO buffers; a worker pool processes events and pushes to a dedupe layer (map of last-seen timestamp per sensor). Keep a 1000ms sliding window; dro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-topic FIFO queue and a global bounded capacity, e.g., a semaphore. A dispatcher pulls from non-empty topic queues and assigns to a small pool of workers. Topic creation/removal protected by",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement with per-service buffered channels feeding a bounded global queue; producers block on full queues; a dispatcher routes items to a fixed worker pool. Preserve per-service FIFO, degrade by pro",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a global bounded memory pool with per-tenant FIFO queues and a credit ledger. Producers enqueue only when the tenant has credits; when credits are exhausted, non-critical events spill to a per-ten",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical understanding of concurrency basics (bounded queues, per-source FIFO), plus real-world concerns: backpressure, fairness, and degradation under load. It also probes testing discipline for ordering guarantees and shutdown behavior.\n\n## Key Concepts\n\n- Per-service FIFO buffers\n- Bounded global queue and backpressure\n- Degradation policy prioritizing high-severity logs\n- Fairness across services under contention\n- Graceful shutdown and error handling\n\n## Code Example\n\n```javascript\n// Skeleton: not full implementation\nclass Log {\n  constructor(service, level, message, time) {\n    this.service = service;\n    this.level = level; // 'ERROR','WARN','INFO'\n    this.message = message;\n    this.time = time;\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you tune the degradation policy if INFO logs still matter during bursts?\n- How would you test the system under simulated traffic spikes and verify ordering guarantees?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-16",
        "questionId": "q-9189",
        "question": "Design a beginner-friendly concurrency controller for a real-time multiplayer scoreboard updater: dozens of game servers publish per-match score updates concurrently. Build a bounded global queue and per-match FIFO buffers; a small worker pool applies updates in per-match order. Add a degradation path: when backlog nears capacity, switch to batched heartbeats and merge multiple updates into a single per-match publish. Explain backpressure, ordering guarantees, and a practical test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: per-instrument FIFO buffers, global bounded queue, worker pool updates price cache and notifies subscribers. Degradation: when backlog > threshold, keep only the latest tick per instrument,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-directory FIFO buffers and a bounded global queue; a small worker pool updates a mock index. Implement coalescing: for each file, merge rapid edits within 100ms into a single update before enq",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a bounded global queue (e.g., 1024) with per-match FIFOs. Use a 4-worker pool, each worker applying updates for a given match in sequence; track per-match sequencing to detect gaps. If backl",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "A per-device buffer architecture with per-stream sequencing and a 5-second watermark-driven windowed join. Maintain separate per-device queues for both streams A and B; a lightweight joiner drains each window when compl…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests understanding of per-match in-order processing under real-time load, plus a concrete degradation path and test plan.\n\n## Key Concepts\n- Bounded queue with backpressure\n- Per-match FIFO buffers\n- In-order update application\n- Degradation mode and batching\n- Graceful shutdown and testability\n\n## Code Example\n```javascript\nclass ScoreUpdater {\n  constructor(limit = 1024) {\n    this.global = new AsyncQueue(limit);\n    this.matches = new Map(); // matchId -> FIFO\n  }\n  enqueue(matchId, update) {\n    if (!this.matches.has(matchId)) this.matches.set(matchId, new FIFO());\n    this.matches.get(matchId).enqueue(update);\n  }\n  // simplified worker loop\n}\n```\n\n## Follow-up Questions\n- How to handle late updates arriving out of order for a match?\n- How to validate fairness when some matches are bursty?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-17",
        "questionId": "q-12232",
        "question": "Design a real-time per-tenant event processing pipeline with dozens to hundreds of producers. Implement a bounded global queue and per-tenant FIFO workers to guarantee per-tenant in-order processing, with backpressure to the producers when overloaded. Ensure exactly-once delivery to a sharded downstream sink, support late events with windowing and watermarks, and enable dynamic scaling of per-tenant workers based on backlog. Provide runnable skeleton pseudocode for the dispatcher, tenant workers, and sink, and discuss failure modes and test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: implement a global bounded queue feeding per-campaign FIFO buffers. Each campaign has a budget and a nextSeq; workers pull from the campaign buffer to preserve order, with dynamic resizing b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: partition by user_id into fixed shards; bounded per-shard queues; transformers emit to a per-user state store keyed by user_id to preserve order. Use sequence numbers and a barrier during re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: implement N per-source FIFO buffers feeding a single bounded queue; a dispatcher with M workers runs a per-source fairness scheduler (weighted fair queuing) to select events and route them to si",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a bounded global queue with per-tenant FIFO queues. Dispatcher routes events to the tenant worker; each tenant has a single serial processor to preserve order. Exactly-once achieved via idempotent",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a scalable, fault-tolerant per-tenant event processing pipeline with strong ordering guarantees and real-time backpressure.\n\n## Key Concepts\n- Per-tenant ordering and FIFO processing\n- Bounded global queues and backpressure\n- Exactly-once delivery with idempotent sinks and per-batch IDs\n- Late data handling with watermarks and bounded lateness\n- Dynamic scaling of workers based on backlog and throughput\n\n## Code Example\n```pseudo\n# Dispatcher: enqueues into global bounded queue; assigns tenant id\ndispatch(event): enqueue(global_queue, event)\n\n# Tenant worker: serially processes events for a tenant\ntenant_worker(tenant_id): while event=dequeue(tenant_queue[tenant_id]): process(event); ack(event)\n\n# Sink: transactional upserts with per-batch ID; dedupe on (tenant_id, event_id)\nsink(batch): begin; for e in batch: if not seen(e.id): upsert(e); mark_seen(e.id); commit\n```\n\n## Follow-up Questions\n- How to handle bursty tenants without starving others?\n- How would you test ordering guarantees and exactly-once under partial failures?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-18",
        "questionId": "q-13533",
        "question": "Design a distributed video transcoding pipeline with thousands of clips per minute. Build a bounded producer-consumer system that enforces per-tenant fairness, a global bounded queue, and dynamic backpressure to producers. Include degradation of non-urgent tasks to a low-priority path, graceful shutdown, and a concrete test plan for ordering, backpressure, and failure handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Produce a bounded global queue with per-tenant FIFO buffers and a worker pool. Each tenant has a token-bucket limiter; producers await when their bucket is empty or the queue is full. Backpressure pro",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a per-tenant FIFO queue and a single bounded backlog. Each tenant gets a dynamic quota updated by a central scheduler (token buckets that adjust to burstiness). A dispatcher drains queues in weigh",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a fixed 1024-slot circular buffer guarded by a ReentrantLock with two Conditions: notFull and notEmpty. Producers await notFull; consumers await notEmpty. Update head, tail, and count under lock;",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An IoT telemetry collector for 200 devices. Each device's readings must be processed in device-order; readings from different devices may be parallel. Implement per-device FIFO buffers feeding a bound",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests advanced concurrency design for real-time media pipelines, focusing on cross-tenant fairness, backpressure to producers, and degradation paths under load.\n\n## Key Concepts\n\n- Bounded queues, producer-consumer patterns, per-tenant FIFOs\n- Backpressure propagation and signaling across threads/processes\n- Dynamic degradation and prioritization\n- Graceful shutdown, failure handling, observability\n\n## Code Example\n\n```javascript\n// Pseudo: token bucket + bounded queue integration sketch\nclass TokenBucket { constructor(rate, burst) { … } consume(n=1){ … } }\nconst globalQueue = new BoundedQueue( MAX_CAP );\nfor (const tenant of tenants) {\n  const bucket = new TokenBucket(tenant.rate, tenant.burst);\n  // producers await bucket and queue space\n}\n// workers consume from globalQueue and process transcoding tasks\n```\n\n## Follow-up Questions\n\n- How would you measure and bound tail latency under bursty traffic?\n- How would you recover from worker failures without violating per-tenant fairness?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-19",
        "questionId": "q-748",
        "question": "You're building a real-time analytics pipeline with many shards. Propose a concurrent, bounded path that preserves per-shard in-order processing while enabling cross-shard parallelism. Design a data structure and protocol (enqueuing, routing, backpressure, and shutdown). Provide a runnable sketch in Go or Rust showing enqueue, per-shard consumer loop, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-auction FIFO buffers with bounded size, a global bounded queue, and a sequencer to assign tasks to a scalable worker pool, guaranteeing per-auction in-order processing. Implement a soft latency bu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route by shard to per-shard fixed-size ring buffers; each shard has a single in-order consumer. Producers hash the shard key and enqueue; if any buffer is full, block until space frees (backpressure).",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a telemetry ingest with per-sensor FIFO buffers feeding a bounded global worker pool. Preserve arrival order within each sensor; process sensors in parallel. When backlog crosses a threshold",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-directory FIFO buffers and a bounded global queue; a small worker pool updates a mock index. Implement coalescing: for each file, merge rapid edits within 100ms into a single update before enq",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design scalable, shard-aware, in-order processing with backpressure and clean shutdown in a high-throughput concurrency context.\n\n## Key Concepts\n- Bounded queues\n- Per-shard ordering\n- Backpressure\n- Lock-free synchronization\n- Graceful shutdown\n\n## Code Example\n```go\n// skeleton concurrent ring buffer for a shard\ntype Ring[T any] struct { /* ... */ }\nfunc (r *Ring[T]) Enqueue(v T) bool { /* ... */ }\nfunc (r *Ring[T]) Dequeue() (T, bool) { /* ... */ }\n```\n\n## Follow-up Questions\n- How would you handle dynamic shard rebalancing?\n- How to detect and recover from a stuck consumer?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-20",
        "questionId": "q-14616",
        "question": "Design a beginner-friendly concurrency exercise: multiple producers enqueue tasks into a bounded global queue where each task has a priority flag (high or normal). A small worker pool must always process high-priority tasks before normal ones, but avoid starving normal tasks. Describe your approach (dual-queue or aging), implement backpressure when full, and provide a concrete test plan for latency, starvation, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-stream bounded queues and a fixed worker pool. Producers enqueue into their stream's queue; if full, apply backpressure by blocking or signaling upstream. Use per-stream sequence numbers and a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-queue design: a high-priority FIFO queue and a normal-priority FIFO queue, both protected by a mutex and coordinated via condition variables. Workers first attempt to dequeue from the high-priority queue before fall…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a per-document ordered pipeline with a shared bounded queue and per-document buffers. Track per-document sequence numbers and fan-out across a worker pool to preserve document order. Implement",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use two queues: a global bounded high-priority queue and per-origin normal FIFOs. Dispatcher drains the high-priority queue first, then per-origin normal queues with a round-robin across origins. Back",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical concurrency control skills involving multiple producers, bounded buffers, and latency guarantees with a simple priority policy. The solution tests real-world challenges like starvation prevention and graceful shutdown coordination.\n\n## Key Concepts\n\n- Dual-queue architecture for priority separation\n- Mutual exclusion and condition synchronization\n- Backpressure mechanisms for flow control\n- Aging algorithms to prevent starvation\n- Graceful shutdown patterns with resource cleanup\n\n## Code Example\n\n```javascript\n// Pseudo sketch of data structures and worker loop\nclass Task { \n  constructor(priority) { \n    this.priority = priority; \n    this.timestamp = Date.now();\n  } \n}\n\nclass PriorityQueue {\n  constructor(capacity) {\n    this.capacity = capacity;\n    this.highQueue = [];\n    this.normalQueue = [];\n    this.mutex = new Mutex();\n    this.notEmpty = new ConditionVariable();\n    this.notFull = new ConditionVariable();\n    this.shutdown = false;\n    this.agingThreshold = 5000; // 5 seconds\n  }\n\n  async enqueue(task) {\n    await this.mutex.lock();\n    while (this.size() >= this.capacity && !this.shutdown) {\n      await this.notFull.wait();\n    }\n    \n    if (task.priority === 'high') {\n      this.highQueue.push(task);\n    } else {\n      this.normalQueue.push(task);\n    }\n    \n    this.notEmpty.signal();\n    this.mutex.unlock();\n  }\n\n  async dequeue() {\n    await this.mutex.lock();\n    while (this.size() === 0 && !this.shutdown) {\n      await this.notEmpty.wait();\n    }\n    \n    if (this.shutdown && this.size() === 0) {\n      this.mutex.unlock();\n      return null;\n    }\n\n    this.checkAging();\n    \n    let task;\n    if (this.highQueue.length > 0) {\n      task = this.highQueue.shift();\n    } else if (this.normalQueue.length > 0) {\n      task = this.normalQueue.shift();\n    }\n    \n    this.notFull.signal();\n    this.mutex.unlock();\n    return task;\n  }\n\n  checkAging() {\n    const now = Date.now();\n    this.normalQueue = this.normalQueue.filter(task => {\n      if (now - task.timestamp > this.agingThreshold) {\n        this.highQueue.push(task);\n        return false;\n      }\n      return true;\n    });\n  }\n\n  size() {\n    return this.highQueue.length + this.normalQueue.length;\n  }\n\n  async shutdown() {\n    await this.mutex.lock();\n    this.shutdown = true;\n    this.notEmpty.broadcast();\n    this.notFull.broadcast();\n    this.mutex.unlock();\n  }\n}\n```\n\n## Test Plan\n\n### Latency Testing\n- Measure end-to-end processing time for high vs. normal priority tasks\n- Verify high-priority tasks consistently achieve lower latency\n- Test with varying load levels to validate priority guarantees\n\n### Starvation Testing\n- Generate continuous high-priority load and monitor normal task processing\n- Verify aging mechanism promotes aged normal tasks\n- Measure maximum wait time for normal tasks under sustained high load\n\n### Graceful Shutdown Testing\n- Initiate shutdown while tasks are processing\n- Verify all in-flight tasks complete before worker termination\n- Test with producers blocked on backpressure\n- Confirm no resource leaks or deadlocks",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-21",
        "questionId": "q-24513",
        "question": "Design a beginner-friendly image ingestion pipeline: hundreds of clients concurrently upload images. Each client’s images must be processed in arrival order. Use a bounded global queue and per-client queues feeding a small worker pool that resizes and saves images. When backlog crosses a threshold, switch to a degradation path that processes at reduced resolution. Explain backpressure, fairness, and provide a test plan for per-client ordering, degradation behavior, and shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-module bounded queues (size K) with a global dispatcher over a shared worker pool. Route all tasks for a given module through a per-module sequence to guarantee in-order execution across bursts; dispatch tokens…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a router with per-client FIFO buffers fed into a bounded global queue, serviced by a small worker pool. Enforce ordering by dequeuing from the active client head, cycling for fairness. When ba",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design uses per-user FIFO queues feeding a shared dispatcher with a fixed-size worker pool. Each user has a bounded queue; producers enqueue non-blockingly and back off or drop messages when full. Dis",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant FIFO buffers feed a fixed-size worker pool. Each tenant's invocations are serialized before enqueue, preserving order; a global scheduler interleaves across tenants to maximize throughput.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExplores a fresh angle on concurrency: multi-tenant in-order processing with a degradation path that preserves correctness while boosting throughput under pressure.\n\n## Key Concepts\n\n- Per-client FIFO buffers\n- Global bounded queue and backpressure\n- Degradation path that preserves per-client order\n- Graceful shutdown and observability\n\n## Code Example\n\n```javascript\n// Pseudo-code sketch for routing with per-client ordering\nclass Router {\n  constructor() {\n    this.clients = new Map();\n    this.global = new BoundedQueue(100);\n    this.workers = new WorkerPool(4);\n  }\n\n  enqueue(clientId, item) {\n    let q = this.clients.get(clientId) ?? new Queue();\n    q.enqueue(item);\n    this.clients.set(clientId, q);\n    if (!this.global.isFull()) {\n      this.global.enqueue({ clientId, item: q.peek() });\n    }\n  }\n\n  dequeueForWorker() {\n    // pick from current head of a client, then next in round-robin\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test per-client ordering under bursty arrivals?\n- How would you verify that the degradation path does not violate cross-client fairness and supports graceful shutdown?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-22",
        "questionId": "q-26920",
        "question": "Design a concurrent cache-warming pipeline with per-key single-flight semantics. N producers enqueue (key, payload) into a bounded global queue; M workers perform an expensive warm-up to produce a cache value, but only one in-flight load per key may execute at a time. After warming, a sink pool of K workers writes results to a per-key store. Backpressure should delay enqueues when the global queue is full, while preserving per-key in-order processing across all stages and allowing dynamic resizing of both worker pools. Provide runnable sketch and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-key in-flight serialization with a map of keys to queues; a bounded global queue blocks producers when full; dynamic resizing via atomic counters and worker tickets; ensure in-order guarante",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement a bounded global queue with per-account FIFO buffers and a small round-robin scheduler across accounts. Each worker pulls from the current account buffer; after every K tasks, rotate",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant FIFO queues with a global bounded backlog B. Each tenant's producer feeds its queue; a fixed pool of workers pops from tenants in round-robin while preserving per-tenant order. Backpressure is applied to all…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a bounded global queue fed by a small moderator pool, with per-chat-room FIFO buffers to preserve order. Implement a per-room rate limiter (token bucket) and a soft-drop path that drops messages f",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design concurrency primitives with per-key coordination, backpressure, and dynamic scaling.\n\n## Key Concepts\n\n- per-key single-flight\n- bounded buffers\n- dynamic resizing\n- in-order guarantees\n- backpressure policies\n\n## Code Example\n\n```javascript\n// skeleton: perKeyLocks map, boundedQueue, worker pools\n```\n\n## Follow-up Questions\n\n- How would you handle cross-machine coordination for per-key guarantees?\n- How would you test for starvation and fairness under skewed heat?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-23",
        "questionId": "q-22235",
        "question": "Design an intermediate concurrency interview problem: Build a per-user streaming analytics pipeline with N producers feeding a bounded input buffer, M stateless transformers, and K per-user stateful operators. The system must preserve per-user event order across dynamic repartitioning and backpressure, compute per-user session length (first to last event within a session window) and maximum latency per event, support dynamic worker resizing, and handle late events with a watermark policy. Provide data structures, a runnable sketch, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a streaming dataflow with per-window bounded queues and a dynamic worker pool. Map events to fixed 1s windows; maintain a watermark. If a late event arrives past the watermark, cancel the corr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: partition by user_id into fixed shards; bounded per-shard queues; transformers emit to a per-user state store keyed by user_id to preserve order. Use sequence numbers and a barrier during re",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use asyncio with a fixed-size worker pool and a global semaphore for total in-flight bytes. Each chunk is enqueued with its index; workers fetch concurrently, storing results by index. An ordered writ",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-user rolling window guarded by a mutex with an in-memory map, plus a periodic sweeper to drop old events and emit counts. Alternatively funnel events through a single worker with a bounded c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of preserving per-key order during dynamic repartitioning, backpressure handling, and state migration in a streaming pipeline.\n\n## Key Concepts\n- Per-key ordering across rebalancing\n- Bounded buffers and backpressure\n- Stateful per-user aggregates with watermarks\n- Dynamic worker resizing and safe state migration\n- Exactly-once or at-least-once semantics trade-offs\n\n## Code Example\n```go\n// Pseudo sketch: shard map[int]Shard{...}; process events in-order per user\n```\n\n## Follow-up Questions\n- How would you verify correctness during bursts? \n- How would you extend to out-of-order arrivals within a session?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-24",
        "questionId": "q-7130",
        "question": "In a CI/CD runner, hundreds of jobs from different teams submit tasks that form a DAG of artifacts. Design a bounded, multi-tenant work queue with per-tenant FIFO buffers and a dependency scheduler that enforces in-degree readiness. Include backpressure signaling, cancellation, and a minimal runnable sketch in Go or Rust?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a bounded, multi-tenant work queue with per-tenant FIFO buffers and a DAG scheduler. Use a global semaphore to cap in-flight tasks; track in-degree and a ready-set. Workers pull ready tasks fro",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a router with per-client FIFO buffers fed into a bounded global queue, serviced by a small worker pool. Enforce ordering by dequeuing from the active client head, cycling for fairness. When ba",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-queue design: a high-priority FIFO queue and a normal-priority FIFO queue, both protected by a mutex and coordinated via condition variables. Workers first attempt to dequeue from the high-priority queue before fall…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design uses a bounded global queue with per-tenant FIFO buffers and a small worker pool. Schedule via round-robin across tenants with a per-tenant token bucket; throttle when latency rises by delaying token acquisition…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a concurrent system with bounded resources, fairness across tenants, and dependency-aware scheduling. It probes practical reasoning about backpressure, cancellation, and testability in large-scale CI-like workloads.\n\n## Key Concepts\n- Bounded queues and semaphores for backpressure\n- DAG scheduling with in-degree tracking\n- Per-tenant FIFO buffers and fair work distribution\n- Task cancellation, timeouts, and test strategies\n\n## Code Example\n```javascript\n// Pseudo sketch: structures for Task, Scheduler, and Enqueue/Dequeue\n```\n\n## Follow-up Questions\n- How would you detect and mitigate starvation across tenants?\n- What metrics would you collect to diagnose stalls in the DAG?\n- How would you scale the design to thousands of tenants and millions of tasks?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-11T13:15:23.023Z",
    "version": 1
  }
]