[
  {
    "id": "test-concurrency",
    "channelId": "concurrency",
    "channelName": "Concurrency",
    "title": "Concurrency Knowledge Test",
    "description": "Test your understanding of Concurrency with 25 questions.",
    "questions": [
      {
        "id": "concurrency-q-0",
        "questionId": "q-7692",
        "question": "Design a bounded, multi-tenant task router for a real-time telemetry ingest service. Producers submit per-tenant event batches; implement a global queue with max capacity N, plus per-tenant FIFO buffers. Build a fair, backpressure-aware scheduler that prevents starvation, supports dynamic tenant creation, and a small worker pool. Provide core data structures and a runnable minimal example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: use per-client in-order routing with bounded per-client buffers and a central sequencer. Upstream producers push into per-client queues; workers pull by client in round-robin to preserve ord",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a bounded, multi-tenant task router: a global queue with capacity N, plus per-tenant FIFO buffers. Implement a fair scheduler that prevents starvation under bursts by aging: waiting tenants ga",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a beginner-friendly concurrency exercise for a real-time analytics dashboard: dozens of producers publish user interaction events (clicks, scrolls) to a bounded global queue. Implement per-user FIFO buffers and a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-key ordered buffers with a central bounded queue, assign each key a sequence number, and maintain a moving watermark. Workers dequeue ready events by key priority, pushing late events to the c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical design of multi-tenant concurrency with backpressure and fairness under bursty loads.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Per-tenant FIFO and aging-based fairness\n- Deadlock avoidance and clean shutdown\n- Dynamic tenant creation and worker orchestration\n\n## Code Example\n```python\n# Skeleton\nclass Router:\n    pass\n```\n\n## Follow-up Questions\n- How would you adapt to heterogeneous task sizes?\n- How to measure and guarantee latency bounds?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-1",
        "questionId": "q-16043",
        "question": "Design a real-time heatmap feed: producers emit events with cell_id and timestamp; ensure per-cell in-order processing across a dynamic worker pool under burst traffic. Build per-cell bounded FIFOs, a dispatcher that routes cells to workers, and an adaptive in-flight limiter that preserves order while throttling producers. Include a runnable sketch and concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a fixed global worker pool with a bounded central queue to control concurrency. For each tenant, create a dedicated FIFO buffer that preserves arrival order for that tenant's events. Workers pull from tenant buffers…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-user FIFO buffers for both streams with a bounded size K. Use a pool of workers to pull from both buffers and emit joined pairs in ascending user_id order, coordinated by a central seque",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage a bounded global queue and per-image FIFO buffers. Each image_id has a small pending buffer for parts; decode workers assemble parts by part_idx, emitting complete images to analyze workers v",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-cell bounded FIFOs and a dispatcher using consistent hashing route events to workers. Each worker keeps a per-cell last_seq and only processes a message if its seq is last_seq+1; gaps are buffered",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nInterleaves real-world concurrency challenges: per-key in-order guarantees, bounded buffers, dynamic worker pools, and backpressure under bursts. Tests deep understanding of ordering, backpressure signaling, and load repartitioning.\n\n## Key Concepts\n- Per-key (cell) in-order processing\n- Bounded per-key FIFOs\n- Dispatcher with stable routing (consistent hashing)\n- Per-key reordering buffers\n- Global in-flight limiter with backpressure and dynamic rebalance\n\n## Code Example\n```javascript\nclass CellState {\n  constructor() {\n    this.lastSeq = 0;\n    thisbuffer = new Map(); // cell_id -> [msgs in order]\n  }\n  handle(msg){\n    const {cell_id, seq} = msg;\n    const s = this.state.get(cell_id) || {lastSeq:0, buf:[]};\n    if(seq === s.lastSeq+1){\n      s.lastSeq = seq;\n      this.emit(msg);\n      // drain buffered contiguous msgs\n    } else {\n      s.buf.push(msg);\n      // keep bounded and sort by seq for reordering\n    }\n    this.state.set(cell_id, s);\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test correctness when late events arrive after many already-processed messages?\n- What backpressure signals would you expose to upstream systems and how would you prevent starvation during rebalance?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-2",
        "questionId": "q-4482",
        "question": "In a real-time task processing system, multiple producers enqueue jobs into a global bounded queue with capacity C. A fixed pool of workers consumes jobs. Extend a beginner concurrency pattern to support two priority levels: high and normal. High-priority jobs must be served before normal ones, while preserving FIFO within each priority. Producers should block when the queue is full. Implement backpressure signaling and outline a concrete test plan to verify ordering and backpressure?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: per-instrument FIFO buffers, global bounded queue, worker pool updates price cache and notifies subscribers. Degradation: when backlog > threshold, keep only the latest tick per instrument,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement with a bounded global asyncio.Queue(maxsize=1000), a dict tenant_queues[tenant] -> asyncio.Queue(). Dispatcher cycles tenants in RR and enqueues to their queues; workers (N) drain per-tenant",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bounded global queue plus per-document FIFO queues. Edits carry document_id, user_id, op_id, and a vector clock. A dispatcher routes edits to the per-document queue only after all causal predece",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use two bounded queues (High and Normal) sharing capacity C. Enqueue blocks via a counting semaphore representing total items. A single dispatcher drains High first, then Normal, preserving FIFO withi",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical handling of multiple producers with prioritized fairness and backpressure in a simple, implementable pattern.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Priority with FIFO guarantees\n- Semaphore-based flow control\n- Testing ordering and deadlock avoidance\n\n## Code Example\n```javascript\n// Pseudo-code illustrating two queues with a dispatcher\nclass QueuePair {\n  constructor(capacity) { /* ... */ }\n  enqueueHigh(item) { /* block if full */ }\n  enqueueNormal(item) { /* block if full */ }\n  dispatch() { /* drain High then Normal */ }\n}\n```\n\n## Follow-up Questions\n- How would you adapt this for dynamic worker scaling?\n- How would you detect and recover from dispatcher starvation?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-3",
        "questionId": "q-8850",
        "question": "Design a real-time multi-tenant event processor: N producers emit events labeled with tenant_id and event_type into a global bounded queue of size C. Implement per-tenant FIFO lanes that preserve per-tenant event order across all producers, with dynamic worker resizing and per-tenant affinity. Enforce backpressure to keep latency bounded; define a late-event policy and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-user sharded queues with a central sequencer. Each user tracks expected sequence; out-of-order events are buffered until the missing seq arrives, ensuring in-order processing. Use a bounded dispat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a bounded global queue and per-api token buckets. Before dispatch, a worker must acquire a token from that API’s bucket (refill rate equals the API limit). If tokens are exhausted, producers backp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-client FIFO scheduler with a global bounded frontier and a small, per-node arbiter. Use per-client queues and a central scheduler that grants a fixed number of slots to non-empty queues i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant FIFO lanes using fixed-capacity lock-free queues. Route events from the global queue to corresponding tenant lanes via deterministic partitioning on tenant_id. Assign tenant shards to each worker fo…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design per-tenant ordering under backpressure, with dynamic workers and late-event handling.\n\n## Key Concepts\n- Per-tenant ordering and isolation\n- Bounded buffers and backpressure\n- Dynamic worker resizing and affinity\n- Late-event policies and testing\n\n## Code Example\n```javascript\n// Skeleton: per-tenant lane data structures\nclass TenantLane {\n  constructor(capacity) {\n    this.queue = new Array(capacity);\n    this.head = 0;\n    this.tail = 0;\n  }\n  enqueue(e) { /* lock-free insert */ }\n  dequeue() { /* lock-free remove */ }\n}\n```\n\n## Follow-up Question",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-4",
        "questionId": "q-4222",
        "question": "You're building a real-time analytics ingestion service for a multi-tenant SaaS platform. Each incoming event carries tenant_id and a local sequence number. You must preserve per-tenant in-order semantics across keys while allowing full parallelism across tenants. Design a bounded, multi-queue scheduler with per-tenant queues, a fixed-size worker pool, and backpressure when buffers saturate. Describe data structures, synchronization approach (locks vs lock-free), deadlock avoidance, and how to achieve exactly-once processing on failure. Provide runnable sketches in Go or Rust and outline a testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe a per-DAG, per-stage bounded queueing with path-specific sequencing. Each task carries path_id and sequence; a node forwards a task only after all its parents for that path have completed. Us",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Map tenant_id to a per-tenant queue and a fixed-size worker pool fed by a bounded global queue. Enqueue under a tenant lock to preserve order; workers pull events by tenant sequence to maintain per-te",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use N per-sensor queues feeding a bounded central queue; workers pop from central queue and emit in increasing timestamp order. Implement a late-data path by routing packets with timestamp older than",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a dict of per-project asyncio.Queues plus a global bounded worker pool guarded by a semaphore. A dispatcher routes to the right project queue; workers pull from queues in a round-robin fashion, en",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nReal-world ingestion needs per-tenant ordering with cross-tenant parallelism. Bounded buffers prevent unbounded memory growth and backpressure must be predictable under bursts. Candidate must reason about data structures, synchronization, and recovery guarantees.\n\n## Key Concepts\n\n- Per-tenant ordering with cross-tenant parallelism\n- Bounded, multi-queue scheduler and worker pool\n- Lock-based vs lock-free trade-offs\n- Deadlock avoidance and backpressure strategies\n- Exactly-once semantics via durable commits and idempotent handlers\n\n## Code Example\n\n```go\ntype Event struct {\n  TenantID string\n  Seq      int64\n  Payload  []byte\n}\n\ntype TenantQueue struct {\n  mu      sync.Mutex\n  nextSeq int64\n  q       []Event\n}\n\ntype Scheduler struct {\n  tenants map[string]*TenantQueue\n  global  chan *Event\n  workers int\n}\n```\n\n## Follow-up Questions\n\n- How would you test failure recovery and restart scenarios to guarantee exactly-once processing?\n- Discuss scaling the worker pool when tenants exhibit vastly different ingress rates and how to detect starvation.",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-5",
        "questionId": "q-27119",
        "question": "Design a bounded, three-stage streaming pipeline with per-key in-order delivery under dynamic worker resizing. Stage A: N producers emit (key, ts, value) into a bounded queue. Stage B: M stateless transformers apply CPU-intensive transforms. Stage C: K workers perform per-key tumbling-window aggregations (window size W, allowed lateness L). Extend with a speculative-execution lane where Stage B can duplicate work to prefetch future keys; Stage C must deduplicate and commit only the earliest per-key, in-window results to preserve order. Provide a runnable sketch (Go or Rust) and a concrete test plan including backpressure, late data, and dynamic resizing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-key sequencing with a monotonic sequence number, bounded queues between stages, and a central arbiter to commit in-order per key. Implement Stage B as stateless transforms with a speculative s",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a Go-based solution: a fixed-size main queue (buffered channel) fed to a worker pool, plus per-market FIFO spill buffers. Backpressure blocks producers when the main queue is full; optional ex",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant queues + a bounded global queue. A scheduler assigns tenants to workers; Stage 1 enriches with simulated latency; Stage 2 updates per-tenant aggregates. Preserve per-tenant in-order across",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a bounded global queue with per-region queues. Workers dequeue from region buffers in a round-robin fairness scheme. Use a semaphore to cap in-flight matches. When backlog exceeds high water",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a robust, low-flush concurrency pipeline with ordering guarantees, backpressure, and dynamic scaling. The speculative lane adds a realistic complication around mispredicted work and commit ordering.\n\n## Key Concepts\n- Per-key in-order delivery across stages\n- Bounded buffers and backpressure\n- Dynamic worker resizing\n- Speculative execution with deduplication and committed order\n- Late data handling via per-key lateness windows\n\n## Code Example\n```javascript\n// High-level sketch omitted for brevity\n```\n## Follow-up Questions\n- How would you prove correctness for out-of-order arrivals?\n- How would you tune the speculative budget under load?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-6",
        "questionId": "q-22858",
        "question": "Design a beginner-friendly concurrency exercise: multiple services publish cache invalidation requests for string keys. Build a local dedupe layer that batches duplicates within a 100ms window and forwards to a single invalidation sink (e.g., Redis). Use a bounded per-key buffer and a global batch writer. Describe backpressure, ensure per-key dedupe correctness, and provide a concrete test plan for dedupe accuracy, batch size, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-key FIFO buffers coordinated by a global sequencer to maintain strict ordering across regions. Employ a bounded global queue with capacity Q and per-region credit allocation; producers block when credits a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Sketch: per-tenant FIFO queues, a global bounded asyncio.Queue, and per-tenant token buckets. A central scheduler round-robins across tenants, decrementing budgets on dispatch. If a tenant exhausts to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-key dedupe with a 100ms batching window. Each publish enqueues (key, ts) to a bounded queue; a worker batches by key within the window and emits a single invalidation per key to the si",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-tenant FIFO lanes using fixed-capacity lock-free queues. Route events from the global queue to corresponding tenant lanes via deterministic partitioning on tenant_id. Assign tenant shards to each worker fo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEvaluates understanding of practical concurrency concerns: per-key deduplication, time-based batching, bounded buffers, and backpressure in a real-world microservice task like cache invalidation.\n\n## Key Concepts\n\n- per-key deduplication\n- time-based batching\n- bounded queues\n- backpressure handling\n- graceful shutdown\n\n## Code Example\n\n```javascript\nclass Deduper {\n  constructor(windowMs, maxBuffer) {\n    this.window = windowMs;\n    this.maxBuffer = maxBuffer;\n    this.queue = []; // bounded queue\n    this.map = new Map(); // key -> lastEmitTime\n  }\n  publish(key) {\n    // push to queue with bound checks\n  }\n  flush() {\n    // emit one invalidation per key within window\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you extend for multi-region deployments with clock skew?\n- How would you test bursty traffic and ensure no data loss during shutdown?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-7",
        "questionId": "q-29780",
        "question": "Design a bounded, multi-stage real-time ranking pipeline. N producers emit events as (user_id, item_id, score, ts). Stage 1 normalizes scores per user using a per-user accumulator with fixed memory. Stage 2 maintains a per-user top-K heap in bounded RAM, merged by M workers. Stage 3 periodically flushes global top-K to a sink. Guarantee per-user order and ranking correctness under backpressure and dynamic worker resizing; outline data structures, a runnable sketch, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-room bounded queue with blocking semantics: producers block when the queue is full, consumers block when empty. In Go, use a buffered channel per room; in Python/Java, use a bounded queue with mutex/cond…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a bounded global queue with per-tenant FIFO queues. Dispatcher routes events to the tenant worker; each tenant has a single serial processor to preserve order. Exactly-once achieved via idempotent",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: use a bounded global queue and per-sensor FIFO buffers; a worker pool processes events and pushes to a dedupe layer (map of last-seen timestamp per sensor). Keep a 1000ms sliding window; dro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: partition by user_id; Stage 1 normalizes scores per user with a fixed-size per-user accumulator. Stage 2 maintains a per-user top-K heap in bounded RAM, merged by M workers. Stage 3 periodic",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a bounded, multi-stage pipeline with per-user state, dynamic concurrency, and backpressure handling. It probes correctness guarantees (ordering, top-K accuracy) under scaling.\n\n## Key Concepts\n- Per-user partitioning and state management\n- Bounded memory top-K data structure per user\n- Backpressure-aware queues and dynamic worker resizing\n- Ordering guarantees across stages and safe flushing\n\n## Code Example\n```java\n// sketch: per-user Top-K holder\nclass PerUserTopK {\n  final int K;\n  PriorityQueue<Item> heap = new PriorityQueue<>(Comparator.comparingDouble(i -> i.score));\n  void consider(Item it) { /* insert/trim to K */ }\n}\n```\n\n## Follow-up Questions\n- How would you handle skewed user activity?\n- What testing strategy verifies ordering under concurrent updates?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-8",
        "questionId": "q-13228",
        "question": "Design an intermediate concurrency problem: implement a dynamic, lock-free bounded multi-producer multi-consumer scheduler for a streaming analytics pipeline. Each input event has stream_id and event_id. Guarantee per-stream in-order processing across a worker pool that can resize at runtime. Use per-stream bounded queues, a global balancer, and a backpressure signal when thresholds are hit. Provide a runnable sketch and concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-symbol partitioned, bounded ring-buffer with credit-based flow. Producers atomically decrement a per-partition credit counter to enqueue, avoiding locks; consumers advance a per-partitio",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a bounded worker pool with a fixed number of workers and a lock-free ring buffer. Block enqueue when full to enforce backpressure; use per-task cancellation via context and per-task timeouts",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: Implement per-stream FIFO ring buffers with configurable capacity (K) using stream_id sharding. A lock-free dispatcher distributes streams to workers via atomic steering tables; idle workers perform work steal…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a bounded reorder buffer using a min-heap keyed by event.TS. Ingested events are enqueued; producers block when buffer hits capacity to apply backpressure. A worker pool drains and emits onl",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests mastery of lock-free data structures, per-key ordering guarantees, and dynamic resizing under backpressure in realistic streaming scenarios.\n\n## Key Concepts\n- Lock-free MPMC queues\n- Per-stream ordering guarantees\n- Dynamic worker pool resizing\n- Backpressure signaling and spill-over mechanisms\n\n## Code Example\n```javascript\n// Pseudo-sketch: per-stream ring buffers, atomic counters, steering logic\n```\n\n## Follow-up Questions\n- How do you prevent livelock during extreme traffic bursts?\n- How would you extend this design for cross-node coordination and failure recovery?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-9",
        "questionId": "q-8235",
        "question": "Design a beginner-friendly concurrency scheduler for a log-forwarding service in a multi-tenant cloud app: dozens of services publish logs concurrently; a bounded global queue feeds a small worker pool, with per-tenant buffering and a simple fairness policy to avoid any single tenant starving others. Explain backpressure, fairness, and a test plan for per-tenant throughput, ordering within each tenant, and failure recovery?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a fixed global worker pool with a bounded central queue to control concurrency. For each tenant, create a dedicated FIFO buffer that preserves arrival order for that tenant's events. Workers pull from tenant buffers…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-level queue: a bounded global ring buffer plus per-tenant FIFO subqueues. Each event includes tenantId and a monotonic sequence. Producers push to the ring; a dispatcher forwards to the tenant’s q",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a telemetry ingest controller with a global bounded queue and per-device FIFO buffers to preserve per-device order. A small worker pool writes to a time-series store. When backlog hits a thr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a bounded global queue and per-tenant buffers; workers pull in round-robin across active tenants. Each tenant buffer preserves order; on overflow, apply backpressure to producers (block subm",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nThis angle tests practical, beginner-friendly concurrency design under multi-tenant fairness and backpressure.\\n\\n## Key Concepts\\n\\n- Bounded global queue and per-tenant buffers\\n- Round-robin fairness and backpressure\\n- Per-tenant ordering and crash recovery\\n\\n## Code Example\\n\\n```python\\n# Sketch\\nclass Scheduler:\\n    def __init__(self, global_cap, tenants):\\n        self.global = asyncio.Queue(maxsize=global_cap)\\n        self.tenant_bufs = {t: asyncio.Queue(maxsize=10) for t in tenants}\\n    async def schedule(self):\\n        while True:\\n            for t, buf in self.tenant_bufs.items():\\n                if not buf.empty():\\n                    job = await buf.get()\\n                    await self.global.put((t, job))\\n```\\n\\n## Follow-up Questions\\n\\n- How would you test per-tenant throughput and ordering under bursty tenants?\\n- How would you adapt if tenants have different SLA requirements?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-10",
        "questionId": "q-24577",
        "question": "Design a multi-tenant task scheduler with a single bounded backlog that preserves per-tenant FIFO order, applies dynamic quotas per tenant that adapt to observed burstiness, and introduces a global backpressure signal when backlog nears capacity. Build a runnable sketch (Go or Rust) showing per-tenant queues, a central dispatcher, and a graceful shutdown; explain how you prevent starvation and measure fairness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A multi-tenant bounded-memory scheduler with per-tenant credits, EDF within quotas, dynamic worker scaling, and backpressure signaling. Use per-tenant FIFO queues, a global arbiter allocating time sli",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-tenant FIFO queue and a single bounded backlog. Each tenant gets a dynamic quota updated by a central scheduler (token buckets that adjust to burstiness). A dispatcher drains queues in weigh",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a bounded global queue (size N) with per-room FIFO buffers. A round-robin dispatcher drains buffers to a small worker pool, ensuring events for each room are processed in arrival order. When backlog crosses th…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-layer: a global bounded queue (size Q) and per-key FIFO buffers with cap K. A dispatcher routes next event for a key to an available worker, preserving in-order delivery per key. Use a semaphore t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nDesigning a multi-tenant scheduler that preserves per-tenant ordering while adapting quotas to burstiness tests deep knowledge of backpressure, fairness, and liveness. It also challenges how to measure and guarantee fairness across tenants in a shared resource.\n\n## Key Concepts\n\n- Per-tenant FIFO queues\n- Global bounded backlog with backpressure signaling\n- Dynamic quotas via token buckets adjusted by observed burstiness\n- Scheduling policy (weighted RR) to prevent starvation\n- Observability: per-tenant throughput, latency, and starvation windows\n\n## Code Example\n\n```javascript\n// Skeleton: per-tenant queues and central dispatcher\nclass TokenBucket {\n  constructor(rate, burst){ this.rate = rate; this.burst = burst; this.tokens = burst; this.last = Date.now(); }\n  tryConsume(n=1){ const now = Date.now(); const elapsed = (now - this.last)/1000; this.tokens = Math.min(this.burst, this.tokens + elapsed*this.rate); this.last = now; if(this.tokens >= n){ this.tokens -= n; return true; } return false; }\n}\n\nclass TenantQueue {\n  constructor(id){ this.id = id; this.q = []; }\n  enqueue(item){ this.q.push(item); }\n  dequeue(){ return this.q.shift(); }\n}\n\n// Dispatcher sketch (async flow-control omitted for brevity)\n```\n\n## Follow-up Questions\n\n- How would you validate fairness under bursty workloads and detect starvation?\n- How would you scale this across processes or machines with consistent quotas?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-11",
        "questionId": "q-20156",
        "question": "In a real-time analytics platform, thousands of producers publish events tagged by tenant IDs into a bounded central bus. Each tenant’s events must be FIFO, while a worker pool scales. Implement a dispatcher (Go or Rust) routing events to per-tenant queues, enforcing per-tenant backpressure with fair draining. Add a degradation path for hot tenants that samples. How would you structure tests to verify per-tenant order, backpressure, and degradation under burst?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded global queue fed by a small moderator pool, with per-chat-room FIFO buffers to preserve order. Implement a per-room rate limiter (token bucket) and a soft-drop path that drops messages f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a central bounded bus feeding a dispatcher that writes into per-tenant FIFO queues (ring buffers). Block enqueue when a tenant queue hits capacity; workers drain queues round-robin to preserve order.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage a per-key in-flight coalescing map. On request(key): if in cache, return; if in-flight, await the same future; else start a single upstream fetch, then fulfill all waiters, cache the result,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a credit-based bounded pipeline. Each upstream stream maintains a local FIFO and a shared credit pool. Producers enqueue only when they hold a credit; decrement on enqueue, downstream replenishes",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to design multi-tenant, high-throughput concurrency systems with strict per-tenant ordering guarantees, bounded resource allocation, and controlled degradation under load. It emphasizes testing strategy alongside implementation details.\n\n## Key Concepts\n\n- **Bounded central bus** and per-tenant FIFO queues\n- **Dispatcher** with fair draining (round-robin or weighted)\n- **Per-tenant backpressure** and graceful degradation through sampling\n- **Observability**: ordering guarantees, latency under burst, degraded path metrics\n\n## Code Example\n\n```go\n// Minimal Go sketch showing structures (not a full solution)\npackage main\n\nimport (\n\t\"context\"\n\t\"sync\"\n\t\"time\"\n)\n\ntype Event struct {\n\tTenantID string\n\tPayload  []byte\n\tTimestamp time.Time\n}\n\ntype TenantQueue struct {\n\tID      string\n\tBuffer  chan *Event\n\tmu      sync.Mutex\n\tdropped int64\n}\n\nfunc NewTenantQueue(id string, cap int) *TenantQueue {\n\treturn &TenantQueue{\n\t\tID:     id,\n\t\tBuffer: make(chan *Event, cap),\n\t}\n}\n\nfunc (q *TenantQueue) Enqueue(e *Event) bool {\n\tselect {\n\tcase q.Buffer <- e:\n\t\treturn true\n\tdefault:\n\t\tatomic.AddInt64(&q.dropped, 1)\n\t\treturn false\n\t}\n}\n\ntype Dispatcher struct {\n\tqueues    map[string]*TenantQueue\n\tmu        sync.RWMutex\n\tworkQueue chan *Event\n}\n\nfunc NewDispatcher(bufferPerTenant int) *Dispatcher {\n\treturn &Dispatcher{\n\t\tqueues:    make(map[string]*TenantQueue),\n\t\tworkQueue: make(chan *Event, 10000),\n\t}\n}\n\nfunc (d *Dispatcher) GetQueue(tenantID string) *TenantQueue {\n\td.mu.RLock()\n\tq, ok := d.queues[tenantID]\n\td.mu.RUnlock()\n\t\n\tif ok {\n\t\treturn q\n\t}\n\t\n\td.mu.Lock()\n\tdefer d.mu.Unlock()\n\t// Double-check after acquiring write lock\n\tif q, ok := d.queues[tenantID]; ok {\n\t\treturn q\n\t}\n\tq = NewTenantQueue(tenantID, 1000)\n\td.queues[tenantID] = q\n\treturn q\n}\n\nfunc (d *Dispatcher) HandleEvent(e *Event) {\n\tqueue := d.GetQueue(e.TenantID)\n\tif !queue.Enqueue(e) {\n\t\t// Apply degradation: sample or drop\n\t\td.handleDegraded(e)\n\t}\n}\n\nfunc (d *Dispatcher) handleDegraded(e *Event) {\n\t// Implement sampling logic for hot tenants\n\tif time.Now().UnixNano()%10 == 0 {\n\t\td.workQueue <- e\n\t}\n}\n\nfunc (d *Dispatcher) StartWorkers(ctx context.Context, numWorkers int) {\n\tvar wg sync.WaitGroup\n\tfor i := 0; i < numWorkers; i++ {\n\t\twg.Add(1)\n\t\tgo func(workerID int) {\n\t\t\tdefer wg.Done()\n\t\t\tfor {\n\t\t\t\tselect {\n\t\t\t\tcase <-ctx.Done():\n\t\t\t\t\treturn\n\t\t\t\tcase e := <-d.workQueue:\n\t\t\t\t\t// Process event\n\t\t\t\t\t_ = e\n\t\t\t\t}\n\t\t\t}\n\t\t}(i)\n\t}\n\twg.Wait()\n}\n\nfunc main() {\n\td := NewDispatcher(1000)\n\tctx, cancel := context.WithCancel(context.Background())\n\tdefer cancel()\n\t\n\td.StartWorkers(ctx, 4)\n}\n```\n\n## Testing Strategy\n\n1. **Per-tenant order verification**: Inject ordered events per tenant, drain, and assert sequence integrity\n2. **Backpressure validation**: Fill tenant buffers, verify blocking behavior and caller notifications\n3. **Burst and degradation**: Simulate traffic spikes, measure sampling ratios and dropped event counts\n4. **Fairness testing**: Ensure no tenant starves regardless of event volume",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-12",
        "questionId": "q-773",
        "question": "In a Go HTTP server, you maintain a per-user quota counter in memory. Multiple requests for different users should advance concurrently, but updates to the same user must be serialized. Design a striped lock (a fixed set of mutexes) to guard a map[string]int, map userID to a bucket via hashing, and explain how you minimize contention, handle hash collisions, and ensure correctness when entries may be evicted?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a striped lock: a fixed-size slice of mutexes guards a map[string]int quotas. Hash userID to bucket index, lock bucket, read-modify-write, unlock. This lowers contention vs a global lock. Collisio",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a two-tier scheduler with a compact fast-path queue and a larger slow-path buffer. Producers tag tasks by category; workers drain the fast path, and overflow is migrated to the slow path. Enfo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-key token-bucket rate limiter with a global budget. Each service_key has its own bucket refilled at its rate; total tokens across keys capped by a shared budget. Use a concurrent map w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a central dispatcher routing edits to per-user FIFOs backed by bounded buffers; map users to workers with consistent hashing; on rebalance transfer a user’s queue via an atomic hand-off token and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of fine-grained synchronization and practical trade-offs for high-concurrency in-memory state.\n\n## Key Concepts\n\n- Striped locking for reduced contention\n- Lock granularity vs global lock\n- Handling hash collisions with buckets\n- Eviction strategies and correctness\n\n## Code Example\n\n```javascript\n// Go-like pseudocode demonstrating striped locks\ntype Striped struct {\n  locks []sync.Mutex\n  quotas map[string]int\n}\nfunc NewStriped(n int) *Striped { ... }\nfunc (s *Striped) Inc(id string) { idx := hash(id) % len(s.locks); s.locks[idx].Lock(); defer s.locks[idx].Unlock(); s.quotas[id]++ }\n```\n\n## Follow-up Questions\n\n- What happens if bucket count is too small? how to resize safely?\n- How would you extend to support read-heavy workloads (readers-writer locks)?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-13",
        "questionId": "q-11506",
        "question": "Design a three-stage real-time video pipeline (demux → encode → archive) using a per-video FIFO and a global per-user token bucket to backpressure producers. Build a bounded, multi-stage asyncio pipeline in Python where each video has its own per-video queue and a small worker pool per stage. Guarantee per-video in-order processing, implement backpressure, and provide graceful shutdown plus timeout cancellation if a video stalls longer than N seconds. Include a runnable minimal example showing setup, enqueue, worker loops, and cancellation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a global bounded queue (maxsize N) fed by per-feed FIFO buffers; a coordinator siphons from buffers into the global queue and applies backpressure to producers via await when full. 32 worker",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a global bounded queue with per-module FIFO buffers and a small worker pool. Preserve per-module order by routing tasks to module-specific queues fed in round-robin. If a module backlog exce",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a three-stage real-time video pipeline (demux → encode → archive) using per-video FIFO queues and a global per-user token bucket for backpressure. Implement bounded per-video asyncio.Queues with small worker pool…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a bounded global queue and three per-priority FIFO buffers, with a fixed pool of workers. Producers enqueue into the appropriate buffer; a dispatcher moves tasks to workers in priority order (high",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design multi-stage async pipelines with hard backpressure, order guarantees, and cancellation semantics at scale.\n\n## Key Concepts\n\n- Multi-stage async queues with per-video FIFO\n- Global per-user rate limiting using token bucket\n- Per-video ordering and backpressure propagation\n- Timeouts and graceful shutdown mechanisms\n- Observability hooks for monitoring and failure signaling\n\n## Code Example\n\n```python\n# Minimal runnable sketch of multi-stage bounded queues\nimport asyncio\nfrom collections import defaultdict\n\nasync def stage_worker(in_q, out_q, process_fn, timeout):\n    while True:\n        item = await in_q.get()\n        try:\n            result = await asyncio.wait_for(process_fn(item), timeout)\n            if out_q:\n                await out_q.put(result)\n        except asyncio.TimeoutError:\n            # Handle stalled video\n            pass\n        finally:\n            in_q.task_done()\n\n# Token bucket for per-user rate limiting\nclass TokenBucket:\n    def __init__(self, rate, capacity):\n        self.rate = rate\n        self.capacity = capacity\n        self.tokens = capacity\n        self.last_refill = asyncio.get_event_loop().time()\n    \n    async def acquire(self, tokens=1):\n        now = asyncio.get_event_loop().time()\n        elapsed = now - self.last_refill\n        self.tokens = min(self.capacity, self.tokens + elapsed * self.rate)\n        \n        if self.tokens >= tokens:\n            self.tokens -= tokens\n            return True\n        return False\n```",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-14",
        "questionId": "q-20934",
        "question": "Design a per-team log ingestion pipeline with a bounded global queue and per-team FIFO buffers. Each log item carries team_id and severity (INFO/WARN/ERROR). Downstream analyzers process per-team logs in arrival order, but a single bottleneck causes backlog; implement a graceful degradation path: when backlog threshold B is exceeded, drop non-critical logs (INFO/WARN) deterministically per team, while guaranteeing that all ERROR logs and the most recent per-team critical events are preserved and ordered. Provide data structures, backpressure signaling, test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement: a bounded global channel (size 200); domain queues protected by a mutex map; a round-robin dispatcher pulling one task from each domain queue and submitting to workers if the domain rat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Strategy: partition by key hash; per-partition workers hold per-key sliding windows in bounded maps. Join when both sides have a matching key within the window; emit with per-key sequence for in-order",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-team log ingestion pipeline with a bounded global queue and per-team FIFO buffers. Each log item carries team_id and severity (INFO/WARN/ERROR). Downstream analyzers process per-team logs in arrival order,…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a per-channel bounded queue (ring buffer) guarded by a mutex, with two condition variables: notFull and notEmpty. Producers block on full; a single consumer dequeues in arrival order and persists",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical concurrency control under heavy load, enforcing per-team ordering with a global bottleneck and deterministic degradation. It assesses backpressure signaling, dynamic policy design, and test plan creation for real-world streaming workloads.\n\n## Key Concepts\n\n- Bounded global queue with capacity enforcement\n- Per-team FIFO buffers maintaining sequence order\n- Backpressure signaling mechanism to upstream producers\n- Deterministic drop policy by severity level\n- Ordering guarantees under graceful degradation\n- Comprehensive test strategy for correctness and resilience\n\n## Data Structures\n\n```javascript\n// Core types\ntype LogLevel = 'INFO' | 'WARN' | 'ERROR';\ntype Log = { teamId: string; severity: LogLevel; timestamp: number; payload: any; seq: number };\n\n// Per-team FIFO buffer with sequence tracking\nclass TeamBuffer {\n  constructor(teamId, capacity) {\n    this.teamId = teamId;\n    this.buffer = [];  // Circular buffer or ring buffer\n    this.seq = 0;       // Monotonic sequence number\n    this.criticalSeq = 0;  // Highest preserved ERROR/WARN seq\n  }\n  \n  push(log) {\n    log.seq = this.seq++;\n    this.buffer.push(log);\n  }\n  \n  // Deterministic drop: remove oldest non-critical when over capacity\n  prune(threshold) {\n    // Keep all ERROR, keep most recent WARN, drop oldest INFO\n  }\n}\n\n// Global bounded queue with backpressure signal\nclass LogIngestionPipeline {\n  constructor(globalCapacity, backlogThreshold) {\n    this.globalQueue = new Map();  // teamId -> TeamBuffer\n    this.capacity = globalCapacity;\n    this.backlogThreshold = backlogThreshold;\n    this.currentBacklog = 0;\n  }\n  \n  // Backpressure: signal when approaching threshold\n  isBacklogged() {\n    return this.currentBacklog >= this.backlogThreshold;\n  }\n  \n  ingest(log) {\n    if (!this.globalQueue.has(log.teamId)) {\n      this.globalQueue.set(log.teamId, new TeamBuffer(log.teamId));\n    }\n    \n    const buffer = this.globalQueue.get(log.teamId);\n    \n    // Always accept ERROR logs\n    if (log.severity === 'ERROR') {\n      buffer.push(log);\n      this.currentBacklog++;\n      return true;\n    }\n    \n    // Under backlog: accept all\n    if (!this.isBacklogged()) {\n      buffer.push(log);\n      this.currentBacklog++;\n      return true;\n    }\n    \n    // Over backlog: deterministic drop policy\n    return buffer.tryAppendWithPruning(log, this.backlogThreshold / this.globalQueue.size);\n  }\n}\n```\n\n## Backpressure Signaling\n\n- **Push-based**: Callback/EventEmitter to producers when backlog > 80% threshold\n- **Pull-based**: Expose `canAccept()` or `backlogPercentage()` for producers to poll\n- **HTTP headers**: X-RateLimit headers (Retry-After, X-Backlog-Level)\n- **Circuit breaker**: Temporarily reject with 503 Service Unavailable\n\n## Test Plan\n\n### Unit Tests\n1. **Ordering verification**: Confirm per-team logs maintain FIFO order under normal and degraded conditions\n2. **Drop policy correctness**: Verify ERROR logs never dropped; INFO/WARN dropped deterministically when over threshold\n3. **Backpressure triggers**: Validate signal fires at correct threshold\n4. **Sequence integrity**: Ensure seq numbers preserved after pruning\n\n### Integration Tests\n1. **High throughput**: Simulate 10x normal load, verify graceful degradation\n2. **Burst handling**: Rapid bursts per team, confirm fair buffer management\n3. **Recovery**: Backlog clears, verify no log loss or corruption\n\n### Chaos Tests\n1. **Producer failure**: Stop producers mid-stream, verify pipeline stability\n2. **Memory pressure**: Constrain heap, verify bounded behavior\n3. **Ordering stress**: Random severity injection, validate final order",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-15",
        "questionId": "q-24171",
        "question": "Design a high-throughput concurrent task scheduler for global log aggregation. Thousands of producers emit per-tenant events; each tenant’s events must be processed in tenant order across dynamic worker pools. Build a two-level queue: a bounded global queue and per-tenant FIFO sub-queues, with backpressure to prevent backlog and with hot-swap capable workers. Explain failure modes, isolation, and provide a runnable sketch in Go or Rust showing enqueue and dispatch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two bounded queues (CPU-bound, IO-bound) fed from a single dispatcher; each job has a unique id and is enqueued only once. Two worker pools pull from their queues. When both queues have work, apply a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant FIFO queues backed by a lock-free ring and a bounded global queue feeding a dynamic worker pool. Each event carries tenant_id and seq; dispatcher enforces order via per-tenant cursors. Back",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a per-publisher FIFO translation pipeline with a bounded global queue. Publishers push posts with language and priority; M workers translate to a target language. Maintain per-publisher orde",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build per-tenant FIFO queues behind a bounded global queue. A dispatcher drains across tenants with fairness, while producers block when the global queue is full to apply backpressure. If backlog stay",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a scalable, fault-tolerant scheduler that preserves per-tenant order under dynamic workloads, while integrating backpressure and failover handling.\n\n## Key Concepts\n\n- Per-tenant FIFO queues\n- Bounded global queue with backpressure\n- Dynamic worker pool and hot-swapping\n- Atomic cursors to preserve order\n- Failure modes: partial failure, replay, and isolation guarantees\n\n## Code Example\n\n```go\n// A minimal sketch in Go illustrating the core idea\npackage main\n\ntype Event struct {\n  Tenant string\n  Seq    int64\n  Data   []byte\n}\n\n// Placeholder for enqueue/dispatch interfaces\nfunc Enqueue(e Event) { /* push to global queue; workers drain per-tenant subqueues */ }\nfunc Dispatch()       { /* route to per-tenant queues and workers, preserving order */ }\n```\n\n## Follow-up Questions\n\n- How would you test backpressure and per-tenant ordering under burst? \n- How would you extend to multi-region consistency and failure recovery?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-16",
        "questionId": "q-14715",
        "question": "Design a concurrent event processor for a real-time analytics service: events arrive from thousands of clients and must be processed in user-order, while the system scales horizontally. Build a hybrid scheduler using a bounded global queue and per-user FIFO queues. Implement a dispatcher that routes events to per-user queues and a fixed worker pool that drains them fairly (round-robin). Guarantee per-user order, implement backpressure, support cancellation, and provide a runnable minimal Python asyncio example showing enqueue, dispatch, and cancellation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Map tenant_id to a per-tenant queue and a fixed-size worker pool fed by a bounded global queue. Enqueue under a tenant lock to preserve order; workers pull events by tenant sequence to maintain per-te",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a bounded global queue G with maxsize N. A dispatcher pulls events and pushes them into per-user FIFO queues Q[u]. A fixed-size worker pool drains across queues in round-robin, guaranteeing per-us",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-user bounded queues (user_id -> ring buffer size B) with next_expected_seq[user]. A dispatcher marks ready users; M workers consume in-order sequences across keys. Support dynamic resizing by reba",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant token buckets and in-flight limits, with a central fairness-driven dispatcher. Each tenant has a bucket (capacity, refill rate) and a per-tenant in-flight cap. The dispatcher non-",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes designing a scalable, fair concurrency pattern that preserves per-user order under bursty loads, with explicit backpressure and cancellation semantics. It also tests practical implementation choices and debugging strategies for a real-time system.\n\n## Key Concepts\n\n- Bounded queues and backpressure\n- Per-user FIFO ordering\n- Fair scheduling (round-robin)\n- Cancellation and failure handling\n- Minimal runnable example in async I/O\n\n## Code Example\n\n```python\nimport asyncio\nfrom collections import defaultdict\n\ndef new_q():\n    return asyncio.Queue(maxsize=2)\n\nasync def worker(q, user):\n    while True:\n        ev = await q.get()\n        if ev is None:\n            break\n        await asyncio.sleep(0.05)  # simulate work\n        print(f\"Processed {ev} for user {user}\")\n        q.task_done()\n\nasync def dispatcher(global_q, per_user):\n    while True:\n        ev = await global_q.get()\n        if ev is None:\n            for q in per_user.values():\n                await q.put(None)\n            break\n        await per_user[ev['user']].put(ev)\n\nasync def main():\n    global_q = asyncio.Queue(maxsize=4)\n    per_user = defaultdict(new_q)\n    wA = asyncio.create_task(worker(per_user['A'], 'A'))\n    wB = asyncio.create_task(worker(per_user['B'], 'B'))\n    disp = asyncio.create_task(dispatcher(global_q, per_user))\n\n    for i in range(6):\n        await global_q.put({'user':'A','data':i})\n    await global_q.put({'user':'B','data':'x'})\n    await global_q.put(None)\n\n    await asyncio.gather(disp, wA, wB)\n\nasyncio.run(main())\n```\n\n## Follow-up Questions\n\n- How would you extend this to multi-node with cross-node fairness?\n- How to detect and recover from stuck per-user queues without starving others?\n- What tests verify ordering, backpressure, and cancellation guarantees?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-17",
        "questionId": "q-3797",
        "question": "In a GPU-accelerated image-processing pipeline, dozens of producers generate work units with different priorities and data dependencies, and a pool of workers dispatch kernels. Design a bounded, multi-producer multi-consumer queue with backpressure that supports per-item priority without starving high-priority tasks. Explain data structures, memory ordering, and how you avoid ABA and deadlocks. Include a runnable minimal sketch (Rust or C++) showing enqueue/dequeue semantics and a basic test strategy with synthetic load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a bounded worker pool with a fixed number of workers and a lock-free ring buffer. Block enqueue when full to enforce backpressure; use per-task cancellation via context and per-task timeouts",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a dynamic DAG scheduler with per component topological ordering. Each task stores in-degree and a dependency bitset. Ready tasks go to bounded per-subgraph queues; workers pull, execute, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a bounded MPMC queue built as a ring buffer with atomic head/tail indices and per-slot state. Use memory_order_release/acquire, padding to avoid false sharing, and a backoff strategy for conten",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Two-tier queue: each tenant has a bounded local queue (e.g., 4 in-flight). A global semaphore caps total in-flight jobs (e.g., 16). Producers enqueue to their tenant queue; a scheduler dispatches when capacity permits,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests advanced concurrency design for bounded queues, backpressure, and fairness in a GPU/telemetry-like pipeline.\n\n## Key Concepts\n\n- Lock-free MPMC queues\n- memory ordering\n- backpressure strategies\n- starvation prevention with priority lanes\n- deadlock-free shutdown and testing\n\n## Code Example\n\n```cpp\n// skeleton of bounded MPMC queue\n#include <atomic>\n#include <vector>\ntemplate<class T>\nclass BoundedMPMC {\n  size_t const CAP;\n  std::vector<std::atomic<T*>> slots;\n  std::atomic<size_t> head{0}, tail{0};\npublic:\n  bool enqueue(const T& v);\n  bool dequeue(T& v);\n};\n```\n\n## Follow-up Questions\n\n- How would you integrate per-item priority without starving low-priority tasks?\n- How would you test for memory ordering issues and race conditions at scale?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-18",
        "questionId": "q-18099",
        "question": "Design a CDN edge-logs streaming processor. Ingest logs from N edge servers; each log has edge_id, ts, size. Guarantee per-edge in-order processing across the entire pipeline under burst traffic and backpressure. Implement: (1) per-edge bounded FIFO buffers of size K; (2) a dynamic shard allocator mapping edge_ids to W workers with on-the-fly rebalancing; (3) a backpressure policy that slows upstream and optionally drops non-critical logs while preserving per-edge order. Provide a runnable enqueue/consume sketch and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-edge bounded FIFOs of size K, keyed by edge_id; a dynamic shard allocator maps edge_ids to W workers via consistent hashing and allows live rebalancing with minimal movement cost. Each edge tr",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-auction FIFO buffers with bounded size, a global bounded queue, and a sequencer to assign tasks to a scalable worker pool, guaranteeing per-auction in-order processing. Implement a soft latency bu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a dispatcher that feeds per-merchant FIFO queues from a global bounded buffer with dynamic backpressure propagation. Each merchant maintains a sequence number and a durable replay log to enable deterministic r…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a per-symbol partitioned, bounded ring-buffer with credit-based flow. Producers atomically decrement a per-partition credit counter to enqueue, avoiding locks; consumers advance a per-partitio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests knowledge of maintaining strict per-key ordering under high concurrency, plus dynamic rebalancing of workers and backpressure handling in a real-world CDN context.\n\n## Key Concepts\n\n- Per-key ordered buffers\n- Dynamic sharding and rebalancing\n- Backpressure policies and fairness\n- Test harness for burst, late logs, and drop policy\n\n## Code Example\n\n```go\n// sketch of per-edge bounded buffer and worker mapping\ntype Log struct{ EdgeID string; TS int64; Size int }\ntype EdgeBuf struct { mu sync.Mutex; q []Log; cap int; next *Log }\nfunc enqueue(buf *EdgeBuf, l Log) bool { /* drop if full */ ... }\n```\n\n## Follow-up Questions\n\n- How would you measure per-edge latency under backpressure?\n- How would you handle edge_ids joining later?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-19",
        "questionId": "q-28266",
        "question": "Design a bounded, concurrent event router for per-customer streams: N producers emit (customer_id, ts, payload) into a shared queue of size C. A pool of M workers handles two stages: (1) validation/enrichment, (2) per-customer aggregation. Guarantee per-customer in-order delivery across stages and apply backpressure to slow customers using per-customer credits; support dynamic worker resizing and a late-event policy. Provide a runnable sketch and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a bounded, multi-priority scheduler with three per-priority queues (0 high). Producers call offer with a timeout to apply backpressure. Workers poll queues in priority order (0, then 1, then",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a central dispatcher routing edits to per-user FIFOs backed by bounded buffers; map users to workers with consistent hashing; on rebalance transfer a user’s queue via an atomic hand-off token and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a map of key to in-flight struct { done chan struct{}, result any, err error, ref int }. On miss, increment refs, start a single fetch, close done to release waiters, and cleanup. Each waiter read",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-customer lanes with a central arbiter and per-customer credits to throttle producers when queues grow. Track a monotonic per-customer sequence to ensure stage boundaries do not reorder events. Implement a credit…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your ability to design systems that combine bounded buffering, per-customer ordering guarantees, and backpressure in a multi-stage pipeline. It also tests understanding of dynamic resource management and late-event policies—common requirements in production event-processing systems.\n\n## Key Concepts\n- Bounded buffering with per-customer lanes\n- Per-customer ordering across pipeline stages\n- Credit-based backpressure policies\n- Dynamic worker pool resizing\n- Late-event detection and handling\n\n## Code Example\n```javascript\n// Central Arbiter with Per-Customer Lanes and Credit-Based Backpressure\n\nclass EventRouter {\n  constructor(config) {\n    this.maxQueueSize = config.maxQueueSize || 10000;\n    this.creditBudget = config.creditBudget || 100;\n    this.lanes = new Map();\n    this.sequenceCounters = new Map();\n  }\n\n  // Acquire credit for a customer before enqueueing\n  acquireCredit(customerId) {\n    const credits = this.lanes.get(customerId)?.credits || this.creditBudget;\n    if (credits <= 0) {\n      return false; // Apply backpressure\n    }\n    if (this.lanes.has(customerId)) {\n      this.lanes.get(customerId).credits--;\n    } else {\n      this.lanes.set(customerId, { credits: credits - 1, queue: [] });\n      this.sequenceCounters.set(customerId, 0);\n    }\n    return true;\n  }\n\n  // Enqueue event with sequence tracking\n  enqueue(customerId, ts, payload) {\n    if (!this.acquireCredit(customerId)) {\n      throw new Error('Backpressure applied: insufficient credits');\n    }\n    const lane = this.lanes.get(customerId);\n    const seq = ++this.sequenceCounters.get(customerId);\n    lane.queue.push({ customerId, ts, payload, seq });\n  }\n\n  // Replenish credits after processing\n  replenishCredit(customerId, count = 1) {\n    const lane = this.lanes.get(customerId);\n    if (lane) {\n      lane.credits = Math.min(lane.credits + count, this.creditBudget);\n    }\n  }\n\n  // Process lane with in-order delivery guarantee\n  processLane(customerId, stage1Fn, stage2Fn) {\n    const lane = this.lanes.get(customerId);\n    if (!lane || lane.queue.length === 0) return;\n    \n    const event = lane.queue[0];\n    const enriched = stage1Fn(event); // Stage 1: Validation/Enrichment\n    const aggregated = stage2Fn(enriched); // Stage 2: Aggregation\n    lane.queue.shift();\n    this.replenishCredit(customerId);\n    return aggregated;\n  }\n}\n\n// Dynamic Worker Pool with Work-Stealing\nclass WorkerPool {\n  constructor(router, workerCount) {\n    this.router = router;\n    this.workers = [];\n    this.globalQueue = [];\n    this.epoch = 0;\n    this.resize(workerCount);\n  }\n\n  resize(newCount) {\n    this.epoch++; // Increment epoch for safe cleanup\n    while (this.workers.length < newCount) {\n      this.addWorker();\n    }\n    while (this.workers.length > newCount) {\n      this.removeWorker();\n    }\n  }\n\n  addWorker() {\n    const id = this.workers.length;\n    const worker = {\n      id,\n      localQueue: [],\n      running: true,\n      epoch: this.epoch\n    };\n    this.workers.push(worker);\n    this.runWorker(worker);\n  }\n\n  removeWorker() {\n    const worker = this.workers.pop();\n    worker.running = false;\n    // Migrate local queue to global queue (epoch-based cleanup)\n    this.globalQueue.push(...worker.localQueue.filter(e => e.epoch === this.epoch));\n  }\n\n  runWorker(worker) {\n    const process = () => {\n      if (!worker.running || worker.epoch !== this.epoch) return;\n      \n      let event = worker.localQueue.pop();\n      if (!event && this.globalQueue.length > 0) {\n        // Work-stealing from global queue\n        event = this.globalQueue.shift();\n      }\n      \n      if (event) {\n        this.router.processLane(event.customerId, \n          e => ({ ...e, validated: true }),\n          e => ({ ...e, aggregated: true })\n        );\n      }\n      \n      setImmediate(process);\n    };\n    process();\n  }\n}\n\n// Late Event Policy\nclass LateEventPolicy {\n  constructor(config) {\n    this.ttlMs = config.ttlMs || 300000; // 5 minutes default\n    this.policy = config.policy || 'reject'; // reject | log | deadLetter\n    this.deadLetterQueue = [];\n  }\n\n  isLate(ts) {\n    return Date.now() - ts > this.ttlMs;\n  }\n\n  handle(event) {\n    if (this.isLate(event.ts)) {\n      switch (this.policy) {\n        case 'log':\n          console.warn('Late event received:', event);\n          return null;\n        case 'deadLetter':\n          this.deadLetterQueue.push(event);\n          return null;\n        case 'reject':\n        default:\n          throw new Error(`Late event rejected: ${event.customerId}`);\n      }\n    }\n    return event;\n  }\n}\n```\n\n## Test Plan\n\n### Unit Tests\n1. **Credit Management**: Verify credits are acquired before enqueue and replenished after processing\n2. **Sequence Ordering**: Ensure monotonic sequence numbers and detect reordering at stage boundaries\n3. **Backpressure Activation**: Confirm producers are blocked when credits are exhausted\n4. **Late Event Detection**: Validate TTL-based rejection, logging, and dead-letter diversion\n\n### Integration Tests\n1. **Multi-Stage Pipeline**: End-to-end test with Stage 1 and Stage 2 processing\n2. **Dynamic Worker Resizing**: Add/remove workers and verify no in-flight events are lost\n3. **Per-Customer Isolation**: Verify events from different customers maintain independent ordering\n4. **Work-Stealing Balance**: Confirm workers steal from global queue when local queue is empty\n\n### Performance Tests\n1. **Throughput Under Load**: Measure events/sec with varying N producers and M workers\n2. **Backpressure Latency**: Verify backpressure response time under saturation\n3. **Memory Bounds**: Confirm queue sizes stay within configured limits\n4. **Worker Resize Impact**: Measure event processing disruption during pool resize",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-20",
        "questionId": "q-1080",
        "question": "In a log-processing pipeline, multiple producers enqueue log entries into a bounded, rate-limited work queue. Implement a beginner-friendly token-bucket rate limiter that allows enqueuing only when a token is available; a background timer refills tokens at a fixed rate up to a max. Producers block when tokens==0; workers process items from the queue. Provide a concrete Go/Python/Java solution and discuss testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An ideal answer demonstrates a bounded queue plus a token bucket. A mutex guards an int tokens and a CV; a ticker periodically adds tokens (capped). Each producer blocks until tokens>0, then decrement",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a bounded global backlog with per-topic FIFO buffers and a round-robin worker dispatcher. Producers enqueue into a shared bounded channel; a central dispatcher forwards items to per-topic queues, which are dra…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a real-time analytics pipeline that ingests concurrent streams from multiple sensors (sensor_id, ts, value) into a bounded central queue with a dynamic worker pool. Guarantee per-sensor in-o",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a NUMA-aware, sharded scheduler for region-based order processing where tasks carry region shard identifiers and DAG dependencies. Workers maintain preference for local shard processing with bounded queues to pre…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis explores practical synchronization for backpressure, a common real-world constraint in streaming services.\n\n## Key Concepts\n- Token bucket rate limiter with bounded queue\n- Mutexes, condition variables, and wakeups\n- Backpressure, fairness, and bounded memory\n\n## Code Example\n```javascript\n// Implementation code here\n```\n\n## Follow-up Questions\n- How would you test for bursty producers and ensure no starvation?\n- How would you adapt for multiple rate limits per producer or per client?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-21",
        "questionId": "q-19493",
        "question": "Design a multi-tenant CI task scheduler: thousands of jobs arrive labeled by tenant, each tenant must preserve FIFO order, but global fairness is required. Use a bounded global queue and per-tenant queues; implement a dispatcher that cycles tenants in round-robin and a worker pool that drains queues fairly. Add a speculative slow-path: when backlog crosses a high watermark, route some jobs to a lightweight 'lint/compile' path to reduce tail latency. Provide a runnable minimal Python asyncio example showing enqueue, dispatch, and cancellation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement with a bounded global asyncio.Queue(maxsize=1000), a dict tenant_queues[tenant] -> asyncio.Queue(). Dispatcher cycles tenants in RR and enqueues to their queues; workers (N) drain per-tenant",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a per-room bounded queue with blocking semantics: producers block when the queue is full, consumers block when empty. In Go, use a buffered channel per room; in Python/Java, use a bounded queue with mutex/cond…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a telemetry ingest with per-sensor FIFO buffers feeding a bounded global worker pool. Preserve arrival order within each sensor; process sensors in parallel. When backlog crosses a threshold",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-device FIFO queues buffer events; a central arbiter routes to a bounded worker pool. A single global channel enforces backpressure; producers block when full. Arbiter preserves device-order, scale",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests deep knowledge of fairness, backpressure, and breakdown of latency.\n\n## Key Concepts\n- Bounded queues and backpressure\n- per-tenant FIFO guarantees\n- round-robin fairness across tenants\n- speculative fast-path handling and cancellation\n\n## Code Example\n```javascript\n// Pseudo minimal asyncio dispatcher sketch\n```\n\n## Follow-up Questions\n- How would you test per-tenant FIFO under burst?\n- How would you model failure/retry and backoff?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-22",
        "questionId": "q-18778",
        "question": "Design an in-memory DAG-based function orchestrator for streaming tasks. Each DAG instance represents a pipeline of stages; stages may fan out; a task must preserve topological order along its path. Implement per-stage bounded FIFOs, per-path in-flight tracking, dynamic worker pools per stage, and a global backpressure policy that pauses sources when total in-flight tasks exceed a limit. Provide data structures, a runnable sketch (Go/Java/Python), and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-user bounded input buffers, a dynamic work-stealing worker pool, and per-user sequence tracking. Out-of-order events are routed to a per-user gap buffer and replayed in-order once gaps are filled, with con…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe a per-DAG, per-stage bounded queueing with path-specific sequencing. Each task carries path_id and sequence; a node forwards a task only after all its parents for that path have completed. Us",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a bounded global queue (size N) with per-room FIFO buffers. A round-robin dispatcher drains buffers to a small worker pool, ensuring events for each room are processed in arrival order. When backlog crosses th…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a bounded global queue with per-user FIFO buffers. A dispatcher mirrors each event into a per-user queue and a small worker pool drains buffers in a controlled round-robin. Enforce per-user",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design ordered, backpressure-aware DAG orchestration with dynamic scaling and per-path guarantees, a common pattern in real-time pipelines.\n\n## Key Concepts\n- DAG orchestration with per-path guarantees\n- Bounded per-stage FIFOs and per-path in-flight tracking\n- Dynamic worker pools and local backpressure signaling\n- Retry semantics and idempotence across stages\n- Deterministic test plans for ordering and shutdown\n\n## Code Example\n```python\nclass Task:\n    def __init__(self, path_id, seq, payload):\n        self.path_id = path_id\n        self.seq = seq\n        self.payload = payload\n```\n\n## Follow-up Questions\n- How would you test ordering when a stage fails and retries?\n- How would you detect and mitigate potential starvation under skewed DAG graphs?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-23",
        "questionId": "q-22414",
        "question": "Design a real-time fraud-detection pipeline with three stages: Ingest (N producers) emits transactions: {user_id, txn_id, ts, amount}; Stage1 (M detector workers) runs stateless anomaly checks and yields candidate alerts; Stage2 (K aggregator workers) maintains per-user risk state and emits alerts if risk crosses threshold. Guarantee per-user in-order processing across stages under dynamic resizing and backpressure. Implement a soft backpressure: when downstream backlog exceeds B, throttle Ingest and Stage1; snapshot per-user checkpoints. Ensure at-least-once or exactly-once alert emission with dedup by (txn_id, user_id). Provide data structures, a runnable sketch, and a test plan for late events, restarts, and backpressure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-user sequence tokens and partitioned queues to keep order across stages. Route all events for a user to the same detector/aggregator path and bound buffers with a shared backpressure signal. F",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use per-stream bounded queues and a fixed worker pool. Producers enqueue into their stream's queue; if full, apply backpressure by blocking or signaling upstream. Use per-stream sequence numbers and a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Python asyncio: give each producer a bounded queue, a central bounded queue collects coalesced updates, and a small worker pool applies updates in per-service order. A coalescer merges frequent up",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly concurrency exercise: 200 producers, each with a per-service FIFO buffer. A small worker pool drains a shared bounded queue to storage. Implement a per-service round-robin s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design multi-stage concurrency with ordering guarantees, backpressure, and restart-safe exactly-once semantics in a fraud-detection pipeline.\n\n## Key Concepts\n\n- Per-user ordering across stages\n- Bounded queues and cross-stage backpressure\n- Idempotent alert emission and per-user checkpoints\n- Dynamic worker resizing and fault tolerance\n\n## Code Example\n\n```javascript\n// Pseudo sketch: per-user partitioning, offsets, and commit points\n```\n\n## Follow-up Questions\n\n- How would you simulate late events and restarts in tests?\n- What metrics would you monitor to detect backpressure tipping points?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-24",
        "questionId": "q-18604",
        "question": "Design a beginner-friendly concurrency exercise: a chat server with many producers publishing messages to multiple chat rooms. Each room has its own FIFO buffer to preserve per-room ordering, and a central dispatcher drains these buffers into a bounded global outbound queue consumed by a notifier that pushes messages to clients. Implement per-room ordering, global backpressure, and a degradation path when backlog grows (e.g., drop or summarize low-priority messages). Explain backpressure, fairness, and provide a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: use per-room bounded buffers and a dispatcher that polls buffers in a round-robin fashion, forwarding to a shared outbound queue with a fixed capacity. Guarantee room ordering by never reord",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a dynamic, dependency-aware concurrency scheduler for a real-time ETL pipeline where tasks carry explicit dependencies forming a DAG. Each node has an estimated latency and a capacity limit. Build an in-process s…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a central bounded queue and per-key FIFO queues, with consistent-hash partitions so same key always goes to same worker. Maintain per-key sequence numbers to preserve order during replay. Backpres",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a global bounded queue (capacity N) for new join requests and per-region FIFO buffers. A worker pool dequeues from region buffers, batches 2–4 players, and dispatches to the game server. Enqueue b",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design simple, predictable concurrency patterns with room-level isolation and global contention management.\n\n## Key Concepts\n\n- Per-entity FIFO ordering\n- Bounded buffers and backpressure\n- Degradation strategies under load\n- Fairness across rooms\n- Observability and testability\n\n## Code Example\n\n```javascript\n// Pseudocode sketch of dispatcher and queues\n```\n\n## Follow-up Questions\n\n- How would you implement backpressure signaling to producers?\n- How would you stress-test fairness with bursty room activity?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-23T10:38:53.164Z",
    "version": 1
  }
]