[
  {
    "id": "test-concurrency",
    "channelId": "concurrency",
    "channelName": "Concurrency",
    "title": "Concurrency Knowledge Test",
    "description": "Test your understanding of Concurrency with 25 questions.",
    "questions": [
      {
        "id": "concurrency-q-0",
        "questionId": "q-21485",
        "question": "You operate a real-time streaming graph with three stages: Ingest from N sources, Branch by event.type into 3 bounded queues, then Merge. Upstream must backpressure if any branch is full. Guarantee per-source in-order delivery while branches can vary in latency, and support dynamic reconfiguration of branches (add/remove) with minimal disruption. Describe data structures, backpressure policy, and provide a runnable sketch plus a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-key in-memory queues with sequence numbers; a spill layer writes to an append-only disk log keyed by key and offset; a fetcher reads in-order chunks; a central orchestrator tracks per-key head",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a bounded reorder buffer using a min-heap keyed by event.TS. Ingested events are enqueued; producers block when buffer hits capacity to apply backpressure. A worker pool drains and emits onl",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-source sequence numbers and per-source FIFOs; central distributor with per-branch credits and a global in-flight cap; throttle sources when any branch is near capacity; deterministic routing b",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Adopt per-user FIFO queues backed by Redis Streams, with a dispatcher that assigns events to a fixed-size worker pool. Consumers read in-order per stream (XREADGROUP) to preserve user sequencing. Glob",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nNew angle: a branching streaming graph with dynamic reconfiguration and strict per-source ordering under backpressure.\n\n## Key Concepts\n- Branching graphs with bounded queues\n- Per-source in-order guarantees\n- Credit-based flow control and backpressure\n- Live reconfiguration of branches (add/remove) with minimal disruption\n\n## Code Example\n```javascript\n// Skeleton: data structures for per-source queues, branch queues, and a scheduler\nclass SourceQ { /* ... */ }\nclass BranchQ { /* ... */ }\nclass Scheduler { /* ... */ }\n```\n\n## Follow-up Questions\n- How would you handle branch failures or slow consumers affecting global latency?\n- What testing strategy would prove correctness under concurrent reconfigurations and bursts?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-1",
        "questionId": "q-12324",
        "question": "Design a NUMA-aware, sharded scheduler for region-based order processing: tasks carry region shard and DAG dependencies; workers prefer local shard with bounded queues; if a shard saturates, backpressure throttles enqueue via a global gate. Include deadlock avoidance, at-least-once retries with idempotent handlers, and a runnable Go or Rust sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a NUMA-aware, sharded scheduler for region-based order processing where tasks carry region shard identifiers and DAG dependencies. Workers maintain preference for local shard processing with bounded queues to pre…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-tenant token buckets and in-flight limits, with a central fairness-driven dispatcher. Each tenant has a bucket (capacity, refill rate) and a per-tenant in-flight cap. The dispatcher non-",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: per-team FIFO buffers, bounded global ready-queue; a dependency-tracker decrements on completion. Scheduler selects ready jobs by lowest numeric priority (0 highest), then oldest arrival, th",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Attach a cancellation token to each event; per-service FIFO buffers feed a fixed-size global work queue. Workers check the token and abort if canceled, freeing resources. Preserve per-service order fo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates distributed systems design skills at realistic scale: NUMA-aware scheduling, locality optimization, backpressure management, DAG dependency resolution, and fault tolerance mechanisms.\n\n## Key Concepts\n\n- **NUMA locality**: Worker affinity to memory regions for performance optimization\n- **Sharded bounded queues**: Isolated processing units with capacity limits\n- **Backpressure**: Global throttling mechanism when shards saturate\n- **DAG scheduling**: Dependency-aware task orchestration\n- **Idempotent retries**: Fault tolerance with guaranteed processing semantics\n- **Fairness**: Balanced load distribution across shards\n\n## Code Example\n\n```go\n// runnable sketch placeholder\n```\n\n## Follow-up Questions\n\n- How would you formally verify deadlock-freedom and prevent starvation?\n- What metrics would you use to measure locality benefits and queue saturation under realistic load patterns?\n- How would the design adapt to dynamic shard rebalancing?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-2",
        "questionId": "q-16043",
        "question": "Design a real-time heatmap feed: producers emit events with cell_id and timestamp; ensure per-cell in-order processing across a dynamic worker pool under burst traffic. Build per-cell bounded FIFOs, a dispatcher that routes cells to workers, and an adaptive in-flight limiter that preserves order while throttling producers. Include a runnable sketch and concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage per-device, per-minute lanes with a bounded input queue and a worker pool. Use a watermark that advances windows and routes late events to a DLQ or re-windowed bucket. Resize the pool atomica",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-cell bounded FIFOs and a dispatcher using consistent hashing route events to workers. Each worker keeps a per-cell last_seq and only processes a message if its seq is last_seq+1; gaps are buffered",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a bounded global queue fed by many producers. Implement a dynamic worker pool that scales from minW to maxW based on queue depth (high watermark triggers growth, low watermark triggers shrink).",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a global bounded queue (capacity N) for new join requests and per-region FIFO buffers. A worker pool dequeues from region buffers, batches 2–4 players, and dispatches to the game server. Enqueue b",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nInterleaves real-world concurrency challenges: per-key in-order guarantees, bounded buffers, dynamic worker pools, and backpressure under bursts. Tests deep understanding of ordering, backpressure signaling, and load repartitioning.\n\n## Key Concepts\n- Per-key (cell) in-order processing\n- Bounded per-key FIFOs\n- Dispatcher with stable routing (consistent hashing)\n- Per-key reordering buffers\n- Global in-flight limiter with backpressure and dynamic rebalance\n\n## Code Example\n```javascript\nclass CellState {\n  constructor() {\n    this.lastSeq = 0;\n    thisbuffer = new Map(); // cell_id -> [msgs in order]\n  }\n  handle(msg){\n    const {cell_id, seq} = msg;\n    const s = this.state.get(cell_id) || {lastSeq:0, buf:[]};\n    if(seq === s.lastSeq+1){\n      s.lastSeq = seq;\n      this.emit(msg);\n      // drain buffered contiguous msgs\n    } else {\n      s.buf.push(msg);\n      // keep bounded and sort by seq for reordering\n    }\n    this.state.set(cell_id, s);\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test correctness when late events arrive after many already-processed messages?\n- What backpressure signals would you expose to upstream systems and how would you prevent starvation during rebalance?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-3",
        "questionId": "q-713",
        "question": "Implement a thread-safe bounded queue using a fixed circular buffer with mutex protection and condition variables for coordinating producers and consumers. How would you handle enqueue/dequeue operations and implement graceful shutdown when the queue is no longer accepting new tasks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a mutex to protect shared state (head, tail, count) and two condition variables: not_full for producers to wait when the buffer is full, and not_empty for consumers to wait when empty. During shutdown, signal all wa…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Approach: partition by user_id; Stage 1 normalizes scores per user with a fixed-size per-user accumulator. Stage 2 maintains a per-user top-K heap in bounded RAM, merged by M workers. Stage 3 periodic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Concurrency Controller Design ### Core Architecture ```python from dataclasses import dataclass from collections import defaultdict from queue import Queue, PriorityQueue import threading import time import random @d…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-vehicle FIFO queues with sequence numbers ensure strict in-order delivery, while a dispatcher routes each vehicle's stream to a dedicated sub-pipeline. Each stage maintains per-vehicle state windows and uses per-veh…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests fundamental concurrency concepts including mutex synchronization, condition variables for thread coordination, and graceful shutdown patterns - all essential for building reliable concurrent systems.\n\n## Key Concepts\n- **Mutex**: Protects shared state (head, tail, count, buffer) from race conditions\n- **Condition Variables**: not_full blocks producers when buffer is full, not_empty blocks consumers when empty\n- **Circular Buffer**: Efficient fixed-size storage using modulo arithmetic for wrap-around\n- **Shutdown Handling**: Clean termination by signaling all waiting threads\n\n## Implementation Details\n```python\nimport threading\nimport time\n\nclass BoundedQueue:\n    def __init__(self, capacity):\n        self.capacity = capacity\n        self.buffer = [None] * capacity\n        self.head = 0  # dequeue position\n        self.tail = 0  # enqueue position\n        self.count = 0\n        self.mutex = threading.Lock()\n        self.not_full = threading.Condition(self.mutex)\n        self.not_empty = threading.Condition(self.mutex)\n        self.shutdown = False\n    \n    def enqueue(self, item):\n        with self.not_full:\n            while self.count == self.capacity and not self.shutdown:\n                self.not_full.wait()\n            \n            if self.shutdown:\n                return False  # Indicate shutdown\n            \n            self.buffer[self.tail] = item\n            self.tail = (self.tail + 1) % self.capacity\n            self.count += 1\n            \n            self.not_empty.notify()\n            return True\n    \n    def dequeue(self):\n        with self.not_empty:\n            while self.count == 0 and not self.shutdown:\n                self.not_empty.wait()\n            \n            if self.shutdown and self.count == 0:\n                return None  # Indicate shutdown\n            \n            item = self.buffer[self.head]\n            self.buffer[self.head] = None  # Clear reference\n            self.head = (self.head + 1) % self.capacity\n            self.count -= 1\n            \n            self.not_full.notify()\n            return item\n    \n    def graceful_shutdown(self):\n        with self.mutex:\n            self.shutdown = True\n            self.not_full.notify_all()\n            self.not_empty.notify_all()\n```\n\n## Shutdown Behavior\n- **Producers**: enqueue() returns False when shutdown is active\n- **Consumers**: dequeue() returns None when queue is empty AND shutdown is active\n- **Signal Broadcasting**: notify_all() wakes all waiting threads for clean exit\n- **Idempotent**: Multiple shutdown calls are safe\n\n## Usage Pattern\n```python\n# Producer thread\ndef producer(queue):\n    for i in range(100):\n        if not queue.enqueue(i):\n            print(\"Producer shutting down\")\n            break\n\n# Consumer thread  \ndef consumer(queue):\n    while True:\n        item = queue.dequeue()\n        if item is None:\n            print(\"Consumer shutting down\")\n            break\n        process(item)\n\n# Graceful shutdown\nqueue.graceful_shutdown()\n```",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-4",
        "questionId": "q-26221",
        "question": "Design a beginner-friendly concurrency dispatcher for a photo-processing service: dozens of clients upload images; each client's images must be processed in arrival order, but processing across clients can run in parallel. Use per-client FIFO buffers feeding a bounded global worker queue. When backlog crosses a threshold, switch to a degradation mode that skips non-critical transforms (resize/color-correct) and only emits thumbnails. Describe backpressure, per-client ordering, and a concrete test plan for ordering, degradation behavior, and shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a bounded global queue and a per-video sequencer. Use a small worker pool for transcoding. For each videoId maintain a dedicated channel; the router forwards jobs from the global queue to th",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant FIFO queues backed by a lock-free ring and a bounded global queue feeding a dynamic worker pool. Each event carries tenant_id and seq; dispatcher enforces order via per-tenant cursors. Back",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-type bounded channels to enforce backpressure, a dispatcher routing tasks to each type queue, and a central scaler that monitors tail latency and throughput to adjust worker counts via atomic",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-client FIFO buffers feeding a bounded global queue, with a fixed worker pool processing tasks in arrival order per client. Enforce ordering by sequencing within each client and routing t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical concurrency control: local per-client ordering, global bounded backpressure, and a simple degradation path that preserves core output while maintaining throughput.\n\n## Key Concepts\n\n- Per-client FIFO buffers\n- Global bounded queue and worker pool\n- Backpressure strategies (blocking enqueue, retry signals)\n- Degradation path that preserves essential results\n- End-to-end testing for ordering, backlog, and shutdown\n\n## Code Example\n\n```javascript\n// Pseudo: enqueue with backpressure and per-client sequencing\nclass Dispatcher {\n  constructor() { /* buffers, global queue, workers */ }\n  enqueue(clientId, item) {\n    // assign sequence, push to client's buffer, block if global full\n  }\n  process() {\n    // workers pull from buffers, respect per-client order\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you handle a client disconnecting during backlog?\n- How would you extend degradation to adjustable quality levels or dynamic backpressure thresholds?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-5",
        "questionId": "q-11159",
        "question": "Scenario: A city-wide fleet telemetry system ingests GPS updates from thousands of vehicles. Implement a bounded global queue and per-vehicle FIFO buffers to guarantee per-vehicle in-order processing across backpressure. Add a dynamic worker pool that scales based on per-vehicle backlog with a safe cap and cooldown. Define late/out-of-order handling, and provide a runnable enqueue/consume sketch plus a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-key sequence numbers and per-region FIFO buffers. Producers publish to a global shard keyed by hash(key); router prefers the current primary region but can failover. A cross-region checkpoint",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-module bounded queues (size K) with a global dispatcher over a shared worker pool. Route all tasks for a given module through a per-module sequence to guarantee in-order execution across bursts; dispatch tokens…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a bounded global queue with per-publisher buffers. In normal mode, drain per-publisher FIFOs in arrival order. When backlog exceeds a threshold, switch to a latency-first path that samples the",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose per-vehicle FIFO buffers, a bounded global queue, and a dynamic worker pool that scales with backlog up to a cap and cooldown. Use per-vehicle sequence numbers to retain order; handle late mes",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\\nThis question probes real-world concurrency control, per-vehicle ordering, dynamic scaling, and backpressure under bursty GPS data. It tests buffers, backpressure signals, and late data handling.\\n\\n## Key Concepts\\n- Per-key in-order processing\\n- Bounded buffers and backpressure\\n- Dynamic worker scaling and cooldown\\n- Late data handling and fault-tolerance\\n\\n## Code Example\\n```javascript\\n// sketch: enqueue/consume with per-vehicle queues\\n```\\n\\n## Follow-up Questions\\n- How would you test ordering guarantees under high churn?\\n- What metrics drive scaling decisions?\\n",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-6",
        "questionId": "q-12232",
        "question": "Design a real-time per-tenant event processing pipeline with dozens to hundreds of producers. Implement a bounded global queue and per-tenant FIFO workers to guarantee per-tenant in-order processing, with backpressure to the producers when overloaded. Ensure exactly-once delivery to a sharded downstream sink, support late events with windowing and watermarks, and enable dynamic scaling of per-tenant workers based on backlog. Provide runnable skeleton pseudocode for the dispatcher, tenant workers, and sink, and discuss failure modes and test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded global queue with per-tenant FIFO queues. Dispatcher routes events to the tenant worker; each tenant has a single serial processor to preserve order. Exactly-once achieved via idempotent",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Strategy: partition by key hash; per-partition workers hold per-key sliding windows in bounded maps. Join when both sides have a matching key within the window; emit with per-key sequence for in-order",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design uses per-user FIFO queues feeding a shared dispatcher with a fixed-size worker pool. Each user has a bounded queue; producers enqueue non-blockingly and back off or drop messages when full. Dis",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a bounded global queue feeding a small transcoder pool with per-camera FIFO buffers. When the queue fills, apply backpressure by delaying new frames or dropping with an increasing retry window",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a scalable, fault-tolerant per-tenant event processing pipeline with strong ordering guarantees and real-time backpressure.\n\n## Key Concepts\n- Per-tenant ordering and FIFO processing\n- Bounded global queues and backpressure\n- Exactly-once delivery with idempotent sinks and per-batch IDs\n- Late data handling with watermarks and bounded lateness\n- Dynamic scaling of workers based on backlog and throughput\n\n## Code Example\n```pseudo\n# Dispatcher: enqueues into global bounded queue; assigns tenant id\ndispatch(event): enqueue(global_queue, event)\n\n# Tenant worker: serially processes events for a tenant\ntenant_worker(tenant_id): while event=dequeue(tenant_queue[tenant_id]): process(event); ack(event)\n\n# Sink: transactional upserts with per-batch ID; dedupe on (tenant_id, event_id)\nsink(batch): begin; for e in batch: if not seen(e.id): upsert(e); mark_seen(e.id); commit\n```\n\n## Follow-up Questions\n- How to handle bursty tenants without starving others?\n- How would you test ordering guarantees and exactly-once under partial failures?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-7",
        "questionId": "q-22414",
        "question": "Design a real-time fraud-detection pipeline with three stages: Ingest (N producers) emits transactions: {user_id, txn_id, ts, amount}; Stage1 (M detector workers) runs stateless anomaly checks and yields candidate alerts; Stage2 (K aggregator workers) maintains per-user risk state and emits alerts if risk crosses threshold. Guarantee per-user in-order processing across stages under dynamic resizing and backpressure. Implement a soft backpressure: when downstream backlog exceeds B, throttle Ingest and Stage1; snapshot per-user checkpoints. Ensure at-least-once or exactly-once alert emission with dedup by (txn_id, user_id). Provide data structures, a runnable sketch, and a test plan for late events, restarts, and backpressure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a deadline-aware scheduler with a global bounded queue and per-account FIFO buffers; ensure high-SLA streams are prioritized, allow preemption or stall of lower-SLA tasks as deadlines near, and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-user sequence tokens and partitioned queues to keep order across stages. Route all events for a user to the same detector/aggregator path and bound buffers with a shared backpressure signal. F",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Approach: partition by user_id into fixed shards; bounded per-shard queues; transformers emit to a per-user state store keyed by user_id to preserve order. Use sequence numbers and a barrier during re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition events by shard; each shard maintains a bounded queue with a dedicated worker to preserve strict per-shard ordering. Map producers to shards using consistent hashing, and implement backpressure through per-sha…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design multi-stage concurrency with ordering guarantees, backpressure, and restart-safe exactly-once semantics in a fraud-detection pipeline.\n\n## Key Concepts\n\n- Per-user ordering across stages\n- Bounded queues and cross-stage backpressure\n- Idempotent alert emission and per-user checkpoints\n- Dynamic worker resizing and fault tolerance\n\n## Code Example\n\n```javascript\n// Pseudo sketch: per-user partitioning, offsets, and commit points\n```\n\n## Follow-up Questions\n\n- How would you simulate late events and restarts in tests?\n- What metrics would you monitor to detect backpressure tipping points?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-8",
        "questionId": "q-8826",
        "question": "Design a beginner-friendly concurrency pattern for a log-processing service: dozens of producers emit log events; a bounded global queue buffers events; a dynamic worker pool processes and writes to storage. Include per-source FIFO buffers, per-source rate limits, and a graceful shutdown that drains in-flight tasks and persists state. How would you test backpressure, dynamic scaling stability, and restart recovery?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a central bounded queue and per-key FIFO queues, with consistent-hash partitions so same key always goes to same worker. Maintain per-key sequence numbers to preserve order during replay. Backpres",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a map of per-client buffered channels feeding a single bounded global channel. A ticker drives a 16ms game loop that drains each client buffer in round-robin fashion, updating game state accordingly. Implement backp…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a bounded global queue fed by many producers. Implement a dynamic worker pool that scales from minW to maxW based on queue depth (high watermark triggers growth, low watermark triggers shrink).",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a per-customer sequencer with per-customer FIFO queues, backed by a bounded shared ring buffer. Each worker handles a set of customers, preserving order by dispatching events with monotonically in",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a system that adapts to load while preserving ordering and ensuring data integrity during shutdown.\n\n## Key Concepts\n\n- Bounded queue\n- Dynamic worker pool scaling\n- Per-source FIFO buffers\n- Per-source rate limiting\n- Graceful shutdown and state persistence\n\n## Code Example\n\n```python\n# Pseudo sketch for dynamic scaling (not complete)\nfrom threading import Thread, Lock\nfrom queue import Queue\n\nQ = Queue(maxsize=1000)\nminW, maxW = 2, 8\ncurrentW = minW\n```\n\n## Follow-up Questions\n\n- How would you implement per-source FIFO buffers efficiently?\n- How would you tune high/low watermarks for stable scaling?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-9",
        "questionId": "q-7400",
        "question": "Design a concurrency strategy for a real-time trading analytics engine that ingests streams from 10k tickers, computes moving-window aggregates, and emits per-ticker streams to downstream consumers. Propose a bounded work queue, per-ticker partitioned state, dynamic worker pool resizing, and robust backpressure signaling to prevent hot-ticker starvation. Include failure recovery, observability, and a minimal runnable example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a beginner-friendly concurrency exercise: a request router handles identical resource fetches from multiple clients. Implement deduplicated fetch: when multiple clients request the same URL concurrently, only one…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a bounded work queue with approximately 1,000 capacity, a dynamically resizable worker pool, and per-ticker partitioned state to compute moving-window aggregates. When the queue reaches capacity, producers blo…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a per-room bounded queue with blocking semantics: producers block when the queue is full, consumers block when empty. In Go, use a buffered channel per room; in Python/Java, use a bounded queue with mutex/cond…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a striped lock: a fixed-size slice of mutexes guards a map[string]int quotas. Hash userID to bucket index, lock bucket, read-modify-write, unlock. This lowers contention vs a global lock. Collisio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests the ability to design a low-latency, scalable streaming system that maintains strict ordering and fairness across thousands of concurrent data streams.\n\n## Key Concepts\n- Bounded queues for flow control and backpressure management\n- Dynamic worker pool sizing based on load metrics\n- Per-key state partitioning to isolate hot spots\n- Windowed aggregation with time-based sliding windows\n- Fault tolerance through checkpointing and idempotent updates\n- Observability via metrics, tracing, and SLO monitoring\n\n## Code Example\n```python\n# Skeleton: bounded queue + per-ti",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-10",
        "questionId": "q-3217",
        "question": "Design a per-session ordered event dispatcher for a real-time multiplayer game server. Each game session has its own FIFO queue; multiple producers can enqueue events across sessions, and a fixed-size pool of worker threads processes events. Requirements: (1) preserve strict per-session order; (2) allow cross-session work-stealing for load balancing; (3) enforce a global bounded queue with backpressure to producers; (4) support dynamic session creation/destruction with zero deadlock and graceful shutdown; (5) discuss memory reclamation and safety?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a global bounded queue (maxsize N) fed by per-feed FIFO buffers; a coordinator siphons from buffers into the global queue and applies backpressure to producers via await when full. 32 worker",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-partition bounded queues with backpressure signaling upstream; use 5-second tumbling windows with a monotonic watermark for time advancement; route late events to a buffer during the grace period; ensure e…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage a bounded global queue feeding a fixed worker pool, with per-session FIFOs. Producers enqueue to the global queue; workers pull sessions, draining in-order. For load balancing, allow stealing",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Approach: a bounded global input buffer fans frames to per-user in-order queues. Each frame carries user_id and seq. A per-user sequencer buffers out-of-order frames in a small heap until gaps close,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes the candidate's ability to design a concurrent, low-latency dispatcher that preserves per-session ordering while scaling across cores. It also exercises lifecycle management and safe shutdown.\n\n### Key Concepts\n- Bounded backpressure and global queues\n- Per-session FIFO with in-order processing\n- Work stealing across sessions without violating order\n- Dynamic session lifecycle and memory reclamation\n\n### Code Example\n```rust\n// Pseudo Rust sketch: session queues and stealing interface\nstruct SessionQueue { /* ... */ }\nimpl SessionQueue {\n  fn enqueue(&self, evt: Event);\n  fn dequeue(&self) -> Option<Event>;\n}\n```\n\n### Follow-up Questions\n- How would you extend to prioritize latency-sensitive events within a session?\n- How would you detect starvation and ensure fairness across sessions?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-11",
        "questionId": "q-9433",
        "question": "Design a concurrency-safe, backpressure-aware task scheduler for a real-time analytics pipeline with tasks from multiple clients, each with a deadline. Build a bounded global queue (preferably lock-free) and per-client buffers to preserve per-client ordering. Implement a small worker pool that consumes tasks and applies per-client deadlines, with a cancellation token. Explain how you avoid starvation, ensure progress, and outline a test plan and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-shard FIFO buffers feed a bounded global queue consumed by a small worker pool. A dedicated writer ensures per-shard order, while cross-shard work runs in parallel. Backpressure arises from the bo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant FIFO buffers on a bounded shared queue, plus a dynamic shard rebalancer that migrates tenants across workers to balance load. Preserve per-tenant order with epoch-based commits; on over",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-channel bounded queue (ring buffer) guarded by a mutex, with two condition variables: notFull and notEmpty. Producers block on full; a single consumer dequeues in arrival order and persists",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a bounded, lock-free global ring buffer (size N) with per-client credit windows. Producers enqueue only if credits exist; else backpressure signals wait. Each client has a FIFO buffe",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAdvanced systems need tight coupling of backpressure, fairness, and per-client deadlines in a shared pool. This tests ability to design lock-free queues, per-client buffers, and cancellation semantics.\n\n## Key Concepts\n- Backpressure and bounded queues\n- Per-client ordering and deadlines\n- Lock-free data structures and contention management\n- Cancellation tokens and failure modes\n\n## Code Example\n```rust\n// Skeleton: bounded queue with per-client sub-queues and worker pool\nuse std::collections::BinaryHeap;\nstruct Task { id: u64, client:u64, deadline: std::time::Instant }\n// ... omitted: lock-free ring, per-client buffers, worker loop\n```\n\n## Follow-up Questions\n- How would you test for starvation under bursty client traffic?\n- How would you adapt the design to multi-host clusters with replication and partition healing?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-12",
        "questionId": "q-21789",
        "question": "Design a bounded, partitioned queue system for a real-time channel moderation pipeline that must preserve per-channel order while scaling to millions of channels. Propose a bounded queue where each channel has its own FIFO subqueue and a fixed-size dispatcher/worker pool drains them fairly. Include per-channel backpressure, SLA-based spill policies, and cancellation support. Provide a runnable Go sketch and a testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a two-stage log pipeline with N tenants feeding a bounded global queue; M workers parse/enrich; K workers perform per-tenant windowed aggregation in timestamp order. Preserve per-tenant order a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a global bounded queue plus per-channel local deques. Workers drain local queues first, then fetch from the global queue with a rotating index and aging to prevent starvation. Block producers",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Solution Design ### Architecture Overview A partitioned queue system with per-channel FIFO subqueues, a dispatcher pool, and SLA-based spill policies. ### Core Components 1. **Channel Queues**: Each channel maintains…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Lock around the increment: with lock: counter += 1 in each thread. Spawn 4 threads, each loops 1000 times. Final value should be 4000. Without the lock, race conditions may yield a final value l",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates the ability to design scalable, fair, low-latency concurrency systems that must respect per-channel ordering while handling bursty traffic and backpressure in a real-time moderation pipeline.\n\n## Key Concepts\n\n- **Partitioned queues with per-channel FIFO**: Isolate channel traffic to guarantee ordering within each channel while enabling horizontal scaling\n- **Bounded capacity and global fairness**: Prevent memory exhaustion through global limits and ensure no channel starves others\n- **Per-channel credits and SLA-aware spill**: Implement backpressure signals and configurable spill policies based on message age or priority\n- **Context-based cancellation**: Support graceful shutdown and per-message cancellation for responsive system termination\n\n## What This Tests\n\n- System design under constraints\n- Concurrency patterns in Go (channels, mutexes, wait groups)\n- Understanding of tradeoffs between throughput, latency, and memory usage\n- Production thinking: graceful degradation, observability, and fault tolerance",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-13",
        "questionId": "q-12924",
        "question": "Design a concurrent event-processor for an event-sourced system with per-aggregate streams. Ingest domain events from many producers into a bounded global queue Q. Each event has aggregate_id, event_seq, and timestamp. Guarantee per-aggregate in-order processing across all workers under backpressure, with dynamic shard rebalancing that migrates aggregates across workers while preserving order. Include an overflow policy (spill to disk or drop non-critical events) and provide a runnable sketch and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-region concurrency model with a bounded global queue, per-user buffers, and region-aware scheduling that migrates tasks to local workers. Producers attach user_id, region, and seq; work",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: implement per-aggregate FIFO buffers, a global sequencer to assign events to workers while preserving aggregate order, and a per-aggregate backpressure tag. Use bounded queue Q, a dynamic sh",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage per-device, per-minute lanes with a bounded input queue and a worker pool. Use a watermark that advances windows and routes late events to a DLQ or re-windowed bucket. Resize the pool atomica",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant FIFO buffers and a global bounded queue; arbiter applies weighted fair queuing with per-tenant credits to schedule the next nonempty tenant, preserving in-order. Track per-tenant sequen",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to reason about complex concurrency constraints: per-aggregate ordering, backpressure propagation, dynamic rebalancing, and overflow handling in a realistic event-sourcing context.\n\n## Key Concepts\n\n- Per-aggregate FIFO buffers with bounded capacity\n- Global bounded queue and a sequencer that preserves per-aggregate order\n- Dynamic shard rebalancing without violating aggregate order\n- Overflow policy: spill to disk vs drop non-critical events; backpressure signaling\n\n## Code Example\n\n```javascript\n// Sketch: per-aggregate buffers and a simple sequencer\nclass AggBuffer {\n  constructor(K){ this.K=K; this.queues=new Map(); }\n  enqueue(evt){ /* maintain per-aggregate order */ }\n}\nclass Sequencer {\n  constructor(){ /* map agg -> worker */ }\n  assign(evt){ /* route to worker while preserving order */ }\n}\n```\n\n## Follow-up Questions\n\n- How would you verify order guarantees under burst and backpressure?\n- What metrics and tests would you include for spill-to-disk overflow scenarios?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-14",
        "questionId": "q-28713",
        "question": "Design a live feature-flag evaluation service for a global ride-hailing platform. Incoming requests carry region_id and user_id; you must guarantee per-region FIFO evaluation order under burst, with a central bounded dispatcher feeding per-region queues and a spill-to-disk policy for evicted events. Describe data structures, backpressure, and recovery with replay-safe idempotence; provide runnable Go or Python sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Python asyncio: give each producer a bounded queue, a central bounded queue collects coalesced updates, and a small worker pool applies updates in per-service order. A coalescer merges frequent up",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a telemetry ingest with per-sensor FIFO buffers feeding a bounded global worker pool. Preserve arrival order within each sensor; process sensors in parallel. When backlog crosses a threshold",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Central bounded dispatcher (size N) feeds per-region FIFO buffers. Maintain per-region sequence numbers to preserve order on replay. Evicted requests spill to a disk-backed LRU store. Backpressure is",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Begin by modeling each client with a per-client queue and a shared bounded ingest queue. A worker pool reads the shared queue, buffers lines per client until either 100 ms has elapsed or 128 bytes acc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests designing a per-key ordered, bounded, backpressured concurrency system with recovery guarantees in a real-world feature-flag workload.\n\n## Key Concepts\n\n- Bounded dispatcher with global and per-region buffers\n- Per-region FIFO ordering and sequence tracking\n- Disk-backed spill store (LRU) for evicted items\n- Dynamic backpressure and throttling to prevent bursts\n- Replay-safe, idempotent evaluations and graceful shutdown\n\n## Code Example\n\n```python\n# Minimal runnable sketch illustrating dispatcher and region queues\nimport asyncio\nfrom collections import defaultdict\n\nclass Dispatcher:\n    def __init__(self, cap):\n        self.global_q = asyncio.Queue(cap)\n        self.region_queues = defaultdict(lambda: asyncio.Queue(100))\n    async def enqueue(self, region, item):\n        await self.global_q.put((region, item))\n        # simplified routing\n```\n\n## Follow-up Questions\n\n- How would you test per-region order under burst and late arrivals?\n- How would you ensure exactly-once semantics for non-idempotent evaluations?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-15",
        "questionId": "q-28972",
        "question": "Design a beginner-friendly concurrency exercise: a distributed log-aggregation pipeline. Dozens of services emit logs tagged by service and level. Each service's logs must be consumed in arrival order, while across services processing can be parallel. Implement per-service FIFO buffers feeding a bounded global worker queue. When backlog hits a threshold, degrade by sampling to only ERROR logs, preserving per-service order for kept logs. Explain backpressure, per-service ordering, and a concrete test plan for ordering, degradation, and shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: allocate per-doc pending queues, a global distributor assigns edits to the corresponding doc queue; each doc tracks expected_seq; workers pull from a doc queue only when the next se",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a cross-region concurrency model with a bounded global queue, per-user buffers, and region-aware scheduling that migrates tasks to local workers. Producers attach user_id, region, and seq; work",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bounded global queue with per-agent rate-limiters (token bucket). Each agent maintains a small FIFO buffer and a dispatcher enqueues to the global queue; a small worker pool dequeues and writes",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "In a log-aggregation pipeline, implement per-service FIFO buffers feeding a bounded global worker pool. Each service's logs preserve arrival order; inter-service logs can interleave. On backlog, degra",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical concurrency skills: per-key ordering, bounded queues, and degradation policies in a streaming pipeline.\n\n## Key Concepts\n- Per-key FIFO ordering\n- Bounded queues and backpressure\n- Degradation via sampling while preserving order\n\n## Code Example\n```javascript\n// Pseudocode sketch\nfunction enqueue(log) {\n  if (globalQueue.isFull()) signalBackpressure();\n  perServiceBuffer[log.service].enqueue(log);\n  tryDispatch();\n}\n```\n\n## Follow-up Questions\n- How would you test per-service ordering under bursty traffic?\n- How would you extend to dynamic worker scaling and failover?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-16",
        "questionId": "q-21159",
        "question": "Design a per-tenant, ordered streaming router for a multi-tenant analytics platform. Ingest events with tenant_id, event_id, and ts. Route to a dynamic worker pool while preserving per-tenant order across producers and worker resizes. Implement bounded queues, backpressure, and a policy for late or out-of-order events. Provide data structures, a runnable sketch, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a bounded global queue feeding a small transcoder pool with per-camera FIFO buffers. When the queue fills, apply backpressure by delaying new frames or dropping with an increasing retry window",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a central bounded channel for in-flight messages, a map of per-room FIFO buffers, and a small worker pool. Producers enqueue into the room buffer, a router forwards to the global bound, and wor",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest events with tenant_id, event_id, and ts; route to per-tenant bounded queues; maintain per-tenant sequence and in-order delivery by a central dispatcher that assigns ready events to a dynamic wo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-session ordered processor with a global bounded queue and per-session FIFOs. Two enrichment paths: a fast path for low-latency services and a spill-backed path for slower ones. Enforce",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design a real-world concurrency system with multi-tenant fairness, ordering guarantees, and dynamic scaling under backpressure.\n\n## Key Concepts\n- Per-tenant ordering with bounded queues\n- Dynamic worker pool and fair dispatch\n- Backpressure signaling and dead-letter handling\n- Idempotent downstream processing and failure handling\n\n## Code Example\n```javascript\n// Skeleton for per-tenant router\ntype Event = {tenant_id:string, event_id:string, ts:number, payload:any};\nclass Router {\n  constructor(maxPerTenant){ this.queues=new Map(); this.inFlight=new Map(); this.max=maxPerTenant; }\n  enqueue(e){ /* per-tenant queue with backpressure */ }\n  dispatch(){ /* in-order dispatch to workers */ }\n}\n```\n\n## Follow-up Questions\n- How would you ensure fairness under bursty tenants?\n- How would you test ordering guarantees with backpressure and dynamic scaling?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-17",
        "questionId": "q-27973",
        "question": "Design an intermediate concurrency problem: implement a bounded, multi-tenant rate-limiter for a streaming ingestion path. N producers publish messages with tenant_id and priority; a global bounded queue feeds a dynamic pool of M workers. Guarantee per-tenant in-order processing across the pipeline, enforce backpressure when the queue fills, and support runtime reconfiguration of per-tenant tokens/priorities without dropping in-flight messages. Provide a runnable sketch and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a bounded global queue and per-resource FIFO buffers. A dispatcher uses round-robin to route incoming tasks by resource_id into small per-resource queues. A fixed-size worker pool drains the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a DAG scheduler with a global bounded queue and per-tenant fair queuing using weighted round-robin. Track in-degree for each node, release ready tasks to workers when all dependencies are satisfied, and enforc…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant circular buffers and a sequencer to preserve per-tenant order. Maintain a global token bucket for backpressure; route to workers with a stable hash of tenant_id; lock-free structures (e",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a global bounded queue (maxsize N) fed by per-feed FIFO buffers; a coordinator siphons from buffers into the global queue and applies backpressure to producers via await when full. 32 worker",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world streaming systems serve many tenants with varying priorities. This question tests per-tenant ordering, dynamic reconfiguration, and backpressure in a shared pool of workers.\n\n## Key Concepts\n- Per-tenant ordering across a multi-stage pool\n- Global bounded queue and backpressure signaling\n- Dynamic, runtime reconfiguration of per-tenant tokens and priorities\n- Idempotent processing and robust test plans\n\n## Code Example\n```javascript\n// Implementation sketch (types and core ideas)\ntype Message = { tenantId: string; priority: number; seq: number; payload: any };\nconst buffers = new Map<string, { nextSeq: number; queue: Message[] }>();\nlet tokenBucket = { tokens: 1000 };\nfunction route(msg) { const bucket = hash(msg.tenantId); return workers[bucket % workers.length]; }\n```\n\n## Follow-up Questions\n- How would you test per-tenant ordering under bursts and dynamic reconfig?\n- What backpressure strategies would you use to avoid starvation of low-priority tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-18",
        "questionId": "q-24904",
        "question": "Beginner concurrency exercise: implement a two-tier task scheduler with a small fast-path queue and a larger slow-path buffer. Multiple producers submit tasks tagged by category (billing, analytics, alerting). A small worker pool drains the fast path; when it overflows, an overflow path moves tasks to the slow path for a separate pool. Enforce per-category FIFO on both paths and specify a backpressure policy to prevent starvation. Include a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An IoT telemetry collector for 200 devices. Each device's readings must be processed in device-order; readings from different devices may be parallel. Implement per-device FIFO buffers feeding a bound",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a two-tier scheduler with a compact fast-path queue and a larger slow-path buffer. Producers tag tasks by category; workers drain the fast path, and overflow is migrated to the slow path. Enfo",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a central bounded queue with maxsize N and a dispatcher that routes frames to per-camera FIFO queues. Each camera has a dedicated worker set that preserves order per camera, while frames from diff",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-asset FIFO buffers managed by a bounded global queue, with per-asset sequence numbers and a watermark mechanism to enforce ordering. Workers pull updates in-order and emit the latest per-asset price to dow…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a two-tier queue, reason about backpressure, and preserve per-category ordering under constrained resources.\n\n## Key Concepts\n\n- Two-tier queues (fast-path and slow-path)\n- Backpressure and starvation avoidance\n- Per-category FIFO across multiple queues\n\n## Code Example\n\n```javascript\n// skeleton for scheduler representing two-tier flow\nclass Scheduler {\n  constructor() {\n    this.fastPath = new Map(); // category -> queue\n    this.slowPath = new Map(); // category -> queue\n  }\n  // enqueue, drain, and overflow logic to implement\n}\n```\n\n## Follow-up Questions\n\n- How would you measure throughput under bursty load?\n- How would you detect and recover from slow-path backlogs?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-19",
        "questionId": "q-10210",
        "question": "Design a per-app partitioned streaming processor for real-time analytics: dozens of mobile apps emit events at high velocity. Build a bounded, backpressured pipeline with per-app queues, a small worker pool, and dynamic rebalancing when a partition lags. Guarantee per-app event ordering, at-least-once ingestion, and exactly-once sinks with idempotent writes. Include a runnable scaffold in Go or Python and a testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition events by app, each with a bounded per-app queue and a small worker pool. Producers backpressure when queues fill; a lag detector triggers dynamic rebalancing across partitions. Guarantee pe",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use per-device FIFOs with a global bounded queue. Maintain a per-device sliding window (implemented as a deque of samples) and track each device's last-seen sequence number to enforce in-order processing across all prod…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a bounded, multi-tenant task router: a global queue with capacity N, plus per-tenant FIFO buffers. Implement a fair scheduler that prevents starvation under bursts by aging: waiting tenants ga",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a global bounded queue with a pool of workers, plus per-tenant FIFO buffers and a distributor that enforces in-order processing for each tenant. Add aging to increase priority for stalled tenants",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests expertise in building robust streaming pipelines with real-world constraints: per-partition ordering, bounded buffers, backpressure, dynamic rebalancing, and exactly-once sinks. It also probes fault tolerance, replay semantics, and test strategy.\n\n## Key Concepts\n\n- Per-app partitioning and ordering\n- Bounded buffers and backpressure\n- Lag detection and dynamic rebalancing\n- Exactly-once sinks and idempotence\n- Checkpointing and replay\n\n## Code Example\n\n```javascript\n// Pseudo-scaffold: bounded per-app queue with workers\nclass Partition {\n  constructor(id) { this.id=id; this.q=[]; this.workers=2; }\n  enqueue(ev){ if(this.q.length>MAX) throw 'backpressure'; this.q.push(ev); }\n  async run(){\n    while(this.q.length){ const e=this.q.shift(); await this.process(e); }\n  }\n  async process(e){ /* write to sink atomically; ensure idempotence */ }\n}\n```\n\n## Follow-up Questions\n\n- How would you test ordering guarantees during dynamic rebalancing?\n- How to pick MAX queue sizes for skewed apps?\n- How would you implement exactly-once with a non-transactional sink?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-20",
        "questionId": "q-28078",
        "question": "Design a beginner-friendly concurrency exercise: a multi-source index updater for a search service. Dozens of producers emit document-change events, each tagged by shard ID. Each shard’s events must be applied in arrival order, while updates across shards can be processed in parallel. Implement per-shard FIFO buffers feeding a bounded global worker queue that writes to a single in-memory index. When backlog crosses a threshold, degrade by coalescing updates per shard to the latest change within a time window and dropping non-critical fields. Explain backpressure, per-shard ordering, and a concrete test plan for correctness, degradation behavior, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Partition events by app, each with a bounded per-app queue and a small worker pool. Producers backpressure when queues fill; a lag detector triggers dynamic rebalancing across partitions. Guarantee pe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a bounded global queue with per-publisher buffers. In normal mode, drain per-publisher FIFOs in arrival order. When backlog exceeds a threshold, switch to a latency-first path that samples the",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-shard FIFO buffers feed a bounded global queue consumed by a small worker pool. A dedicated writer ensures per-shard order, while cross-shard work runs in parallel. Backpressure arises from the bo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use two queues: a global bounded high-priority queue and per-origin normal FIFOs. Dispatcher drains the high-priority queue first, then per-origin normal queues with a round-robin across origins. Back",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical concurrency in a real-world component: multiple producers, per-source ordering, bounded queues, and degradation under load. It also probes test planning and fault tolerance.\n\n## Key Concepts\n- Per-source FIFO buffers and a bounded global queue\n- Single writer per shard to preserve ordering; parallel across shards\n- Backpressure and degradation via update coalescing\n\n## Code Example\n```javascript\n// Pseudo outline: perShardBuffers, globalQueue, workers, degradationPolicy\n```\n\n## Follow-up Questions\n- How would you extend to multiple index writers while preserving ordering guarantees?\n- How do you ensure idempotence during degradation?\n- What metrics would you collect to monitor backlog and degradation triggers?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-21",
        "questionId": "q-16388",
        "question": "Design a **beginner-friendly** concurrency exercise: a **log-shipper** with many clients enqueuing logs into a bounded central queue. A small worker pool batches lines per client (preserving per-client order) into 128-byte blocks and writes to a sink. Implement per-client backpressure, a max batch debounce (100 ms), and a graceful shutdown that flushes in-flight batches. Explain test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a global bounded queue with per-module FIFO buffers and a small worker pool. Preserve per-module order by routing tasks to module-specific queues fed in round-robin. If a module backlog exce",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by modeling each client with a per-client queue and a shared bounded ingest queue. A worker pool reads the shared queue, buffers lines per client until either 100 ms has elapsed or 128 bytes acc",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-tenant FIFO queues with a global bounded backlog B. Each tenant's producer feeds its queue; a fixed pool of workers pops from tenants in round-robin while preserving per-tenant order. Backpressure is applied to all…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use language-supported atomic operations or a minimal lock to guard the counter. For Java: counter is AtomicLong; call incrementAndGet after each event. For Go: use atomic.AddUint64; for CPython: prot",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of per-client ordering, bounded queues, and graceful shutdown in a concurrent setting.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Per-client batching and ordering\n- Time-based batching (debounce)\n- Graceful shutdown and flush semantics\n\n## Code Example\n```javascript\n// Implementation sketch omitted for brevity\n```\n\n## Follow-up Questions\n- How would you adjust for variable batch sizes or different client priorities?\n- How would you test for starvation under heavy bursts?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-22",
        "questionId": "q-23585",
        "question": "Scenario: live multi-stream sensor ingestion into a bounded downstream processor updating a shared model. Preserve per-stream ordering, but prevent downstream overload with a credit-based flow control. Build a upstream-per-stream FIFO + a global bounded queue; producers enqueue only when they hold a credit, downstream replenishes credits after processing. Include cancellation and a policy to drop oldest when credits exhausted. Provide runnable sketches in Go or Rust and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-tenant FIFO queue and a single bounded backlog. Each tenant gets a dynamic quota updated by a central scheduler (token buckets that adjust to burstiness). A dispatcher drains queues in weigh",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a beginner-friendly concurrency exercise: dozens of producers publish events into a bounded global queue. Implement per-source FIFO buffers, a small worker pool to process events, and a per-source token-bucket ra…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-edge bounded FIFOs of size K, keyed by edge_id; a dynamic shard allocator maps edge_ids to W workers via consistent hashing and allows live rebalancing with minimal movement cost. Each edge tr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a credit-based bounded pipeline. Each upstream stream maintains a local FIFO and a shared credit pool. Producers enqueue only when they hold a credit; decrement on enqueue, downstream replenishes",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests mastery of backpressure, per-stream ordering, and pragmatic design choices in streaming systems. The answer should justify credit-based flow control vs blocking, identify edge cases (credit starvation, spillover, cancellation), and describe test strategies and instrumentation rather than abstract concepts.\n\n## Key Concepts\n\n- Per-stream ordering under bounded backpressure\n- Credit-based flow control between upstream and downstream\n- Bounded queues and dispatcher design\n- Cancellation and graceful shutdown in streaming paths\n- Testing strategies: ordering under bursts, latency budgets, throughput\n\n## Code Example\n\n```go\npackage main\nimport \"time\"\n\ntype Event struct{ Stream int; Ts time.Time; Payload []byte }\n\ntype Dispatcher struct {\n  credits map[int]int\n  streams map[int]chan Event\n  global  chan Event\n}\n\n// Skeleton enqueue / drain logic omitted for brevity\n```\n\n## Follow-up Questions\n\n- How would you extend to hundreds of streams with dynamic joining/leaving?\n- What metrics would you collect to detect backpressure-induced latency violations?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-23",
        "questionId": "q-27397",
        "question": "Design a bounded, concurrent feature-flag evaluation engine. Producers publish updates as (flag_id, version, payload, timestamp) into a global bounded queue. Evaluator workers handle read requests (client_id, flag_id, client_version) and must guarantee monotonic per-flag versions: a client should never observe an older version than its previous request. Implement per-flag in-order updates, backpressure when the queue is full (delay or drop low-priority updates), dynamic resizing of the evaluator pool at runtime, and a policy for late or out-of-order updates. Provide runnable sketches in Go and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a tri-stage, bounded-buffer pipeline that preserves per-device order across stages, supports dynamic worker resizing, and applies a latency-based late-data policy (drop or mark). Include per-de",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant FIFO queues backed by a lock-free ring and a bounded global queue feeding a dynamic worker pool. Each event carries tenant_id and seq; dispatcher enforces order via per-tenant cursors. Back",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-flag ordered broker with a bounded input queue and per-flag version tracking. Each flag maintains a `current_version` and a queue of pending updates. Clients waiting for a specific version receive updates i…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-device FIFOs with a global bounded queue. Maintain a per-device sliding window (implemented as a deque of samples) and track each device's last-seen sequence number to enforce in-order processing across all prod…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates a candidate's ability to design systems that handle distributed ordering, backpressure, and dynamic scaling—challenges commonly encountered in production environments. It assesses mastery of per-key ordering guarantees, bounded resource management, and late data handling in a realistic scenario.\n\n## Key Concepts\n\n- Per-flag ordered streams and version monotonicity guarantees\n- Bounded queues and backpressure policies for resource protection\n- Dynamic worker pool resizing and graceful coordination\n- Late and out-of-order data handling with fault tolerance\n\n## Code Example\n\nSee the implementation in the Answer section above.",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-24",
        "questionId": "q-16950",
        "question": "Design a beginner-friendly concurrency exercise: multiple microservices publish metric samples to a bounded global queue that feeds a pool of aggregators. Each service must preserve FIFO order, a backpressure policy kicks in when full, and a lightweight 'partial-aggregation' path yields provisional totals during backlog. Implement with Go channels or Python asyncio. Outline how you'd test per-service ordering, backpressure behavior, and accuracy under burst?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a bounded queue + per‑stream priority scheduler. Use a global maxsize with producers awaiting when full; a small worker pool drains by always preferring high, then medium, then low; implement",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "In Go, use a bounded global channel and per-service FIFOs feeding it. Backpressure by blocking producers when the global queue is full (or returning retriable errors). Implement a partial-aggregation",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design and implement a global bounded-rate limiter using a central dispatcher with per-customer FIFO queues and a shared token-bucket pool. The dispatcher allocates tokens from a global capacity of R events/sec into per…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a bounded global queue plus per-document FIFO queues. Edits carry document_id, user_id, op_id, and a vector clock. A dispatcher routes edits to the per-document queue only after all causal predece",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests understanding of per-source FIFO, backpressure, and graceful degradation in a metric-push pipeline that mirrors real telemetry systems.\n\n## Key Concepts\n\n- Bounded queues and backpressure\n- Per-service FIFO buffers\n- Partial/ provisional aggregation\n- Concurrency primitives in Go channels or Python asyncio\n\n## Code Example\n\n```javascript\n// Implementation sketch placeholder\n```\n\n## Follow-up Questions\n\n- How would you adapt for out-of-order samples or late arrivals?\n- How would you ensure shutdown without losing in-flight metrics?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-05T06:43:26.694Z",
    "version": 1
  }
]