[
  {
    "id": "test-concurrency",
    "channelId": "concurrency",
    "channelName": "Concurrency",
    "title": "Concurrency Knowledge Test",
    "description": "Test your understanding of Concurrency with 25 questions.",
    "questions": [
      {
        "id": "concurrency-q-0",
        "questionId": "q-24011",
        "question": "Design a beginner-friendly concurrency exercise: a real-time event aggregator that ingests updates from 20 microservices (producers), each with its own per-service FIFO queue. A bounded global worker queue feeds a small pool that applies updates to a shared read model. Implement per-service ordering, a coalescing pass for rapid same-key updates, and a backlog-pruning policy that drops oldest updates while preserving per-service order. End with a plan for testing backpressure and correctness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a fixed-size Go channel as the global queue, a mutex-protected map[string]bool for in-flight tracking, and per-ID coalescing buffers. On enqueue: if the ID is already in-flight, merge the payload with the exis…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage per-device, per-minute lanes with a bounded input queue and a worker pool. Use a watermark that advances windows and routes late events to a DLQ or re-windowed bucket. Resize the pool atomica",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed approach: allocate per-doc pending queues, a global distributor assigns edits to the corresponding doc queue; each doc tracks expected_seq; workers pull from a doc queue only when the next se",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Python asyncio: give each producer a bounded queue, a central bounded queue collects coalesced updates, and a small worker pool applies updates in per-service order. A coalescer merges frequent up",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis new angle tests per-service ordering with cross-service fairness, a coalescing optimization, and backlog pruning—common in real-time ingest pipelines but not covered by prior questions. It also exercises observable behavior under backpressure.\n\n## Key Concepts\n\n- Per-service FIFO combined with a global bounded queue\n- Coalescing updates by key to reduce load\n- Backpressure by blocking producers when full\n- Backlog pruning that preserves per-service order\n- Observability and testing strategies\n\n## Code Example\n\n```python\n# Skeleton of coalescing + bounded queues (simplified)\nfrom asyncio import Queue, create_task, gather\n\nasync def worker(in_q: Queue, model):\n    while True:\n        key, value = await in_q.get()\n        model[key] = value\n        in_q.task_done()\n```\n\n## Follow-up Questions\n\n- How would you test per-service ordering under bursts?\n- How would you adapt to many more producers without starving others?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-1",
        "questionId": "q-9398",
        "question": "Context: A real-time, multi-tenant order-processing service ingests events from many tenants. Implement a scheduler with a bounded global backlog B and per-tenant FIFO buffers. Ensure per-tenant order, cross-tenant parallelism, and backpressure to all tenants when B is reached. Provide idempotent processing, a dedup key, and a starvation-avoidance policy so no tenant stalls beyond threshold T. Include concrete test plans and data structures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant FIFO queues with a global bounded backlog B. Each tenant's producer feeds its queue; a fixed pool of workers pops from tenants in round-robin while preserving per-tenant order. Backpressure is applied to all…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a per-user FIFO pipeline with a global bounded queue and per-user local buffers. Route events by user_id to dedicated worker queues, maintain per-user sequence ordering, support dynamic worker",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed approach: allocate per-doc pending queues, a global distributor assigns edits to the corresponding doc queue; each doc tracks expected_seq; workers pull from a doc queue only when the next se",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-client FIFO buffers feeding a bounded global queue, with a fixed worker pool processing tasks in arrival order per client. Enforce ordering by sequencing within each client and routing t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates the ability to design a multi-tenant system that balances per-tenant ordering guarantees with global resource constraints. It tests understanding of concurrent scheduling, backpressure mechanisms, fault tolerance, and fairness policies in distributed systems.\n\n## Key Concepts\n\n- **Per-tenant FIFO buffers**: Maintain ordering within each tenant's event stream\n- **Global bounded backlog**: Enforce system-wide limits to prevent memory exhaustion\n- **Backpressure propagation**: Signal upstream producers when system capacity is reached\n- **Idempotent processing**: Handle duplicate events safely using deduplication keys\n- **Starvation avoidance**: Ensure fair access across all tenants with configurable thresholds\n- **Test validation**: Verify ordering, backpressure, and exactly-once semantics under load\n\n## Code Example\n\n```go\ntype Order struct {\n    TenantID    string\n    SequenceNum int64\n    Timestamp   time.Time\n    Payload     interface{}\n    DedupKey    string // TenantID + SequenceNum\n}\n\ntype TenantQueue struct {\n    mu       sync.Mutex\n    events   []Order\n    lastSeen time.Time\n}\n\ntype Scheduler struct {\n    tenants    map[string]*TenantQueue\n    backlog    chan Order  // Global bounded channel\n    dedupCache *ttlcache.Cache[string, bool]\n    workers    int\n    threshold  time.Duration\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-2",
        "questionId": "q-4669",
        "question": "In a real-time ingestion service, dozens of producers emit per-tenant events into a fixed-size global queue. Implement a bounded, multi-tenant queue that guarantees per-tenant in-order processing and fair sharing while allowing dynamic partition rebalancing without pausing producers. Provide a runnable minimal sketch in Go or Rust showing enqueue/dequeue and a basic test plan for backpressure, ordering, and rebalancing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-cell bounded FIFOs and a dispatcher using consistent hashing route events to workers. Each worker keeps a per-cell last_seq and only processes a message if its seq is last_seq+1; gaps are buffered",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: maintain per-drone bounded input queues for the three streams, a fusion stage that merges by per-drone timestamp using a small window and a min-heap keyed by timestamp across streams, all fe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-level queue: a bounded global ring buffer plus per-tenant FIFO subqueues. Each event includes tenantId and a monotonic sequence. Producers push to the ring; a dispatcher forwards to the tenant’s q",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-tenant FIFO buffers on a bounded shared queue, plus a dynamic shard rebalancer that migrates tenants across workers to balance load. Preserve per-tenant order with epoch-based commits; on over",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a bounded, multi-tenant pipeline with ordering guarantees, backpressure, and dynamic rebalancing, reflecting real-world multi-tenant systems.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Per-tenant in-order processing\n- Dynamic partitioning and rebalancing\n- Memory visibility and race conditions in concurrent code\n\n## Code Example\n```go\n// Minimal sketch: global bounded channel + per-tenant queues\npackage main\nimport (\n  \"fmt\"\n)\n\ntype Event struct{ Tenant string; Seq uint64; Data string }\n\nfunc main() {\n  global := make(chan Event, 8) // bounded global queue\n  tenants := map[string]chan Event{\n    \"tA\": make(chan Event, 4),\n    \"tB\": make(chan Event, 4),\n  }\n  // enqueue example (producer)\n  go func(){ global <- Event{Tenant:\"tA\", Seq:1, Data:\"x\"} }()\n  // dispatcher (simplified)\n  go func(){ for e := range global { tenants[e.Tenant] <- e } }()\n  // worker (consumer)\n  go func(){ for e := range tenants[\"tA\"] { fmt.Println(\"work\", e) } }()\n}\n```\n\n## Follow-up Questions\n- How would you test for starvation during bursty tenant loads?\n- How would you ensure monotonic sequencing across partition rebalancing?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-3",
        "questionId": "q-7805",
        "question": "Build a real-time per-user join between two streams. Stream A emits (user_id, tsA) and Stream B emits (user_id, tsB, amount). A bounded inbound queue feeds M workers that output joined records when both sides have arrived within a 60s watermark. Must preserve per-user in-order emission, handle late data with a policy, and support dynamic worker resizing with backpressure?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a three-stage real-time video pipeline (demux → encode → archive) using per-video FIFO queues and a global per-user token bucket for backpressure. Implement bounded per-video asyncio.Queues with small worker pool…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Three bounded queues by priority (high, normal, low). Scheduler pops from high if non-empty, else normal, else low; aging promotes waited items to the next higher priority after a fixed timeout (e.g.,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-user state with bounded FIFO buffers for A and B and a 60s watermark. Emit when both sides exist, preserving order by the latest timestamp. Drop/purge late records per policy to bound memory",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "A per-device buffer architecture with per-stream sequencing and a 5-second watermark-driven windowed join. Maintain separate per-device queues for both streams A and B; a lightweight joiner drains each window when compl…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProbe practical streaming joins, backpressure, and dynamic scaling under bounded resources.\n\n## Key Concepts\n- Per-key buffering, watermarks, and in-order guarantees\n- Join semantics in streaming under backpressure\n- Backpressure propagation and dynamic worker resizing\n\n## Code Example\n```javascript\n// Pseudo: per-user buffers, watermark, and join\n```\n\n## Follow-up Questions\n- How would you implement late data handling policies? \n- How would you test ordering under burst traffic and worker resizing?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-4",
        "questionId": "q-28895",
        "question": "Design an intermediate concurrency problem: implement a bounded, three-stage, multi-tenant change-stream processing pipeline from MongoDB to Snowflake. Ingest events from N tenants (each with multiple collections). Each event has tenant_id, collection, opTime, payload. Guarantee per-tenant in-order delivery across stages under dynamic worker resizing, with backpressure and fair scheduling. Include a late-event policy, per-tenant watermarking, and a runnable sketch with a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-region ride-matching system with two input streams: rider_requests and driver_availabilities. Each region has a bounded FIFO queue for requests. A central dispatcher assigns each rider in-region to an idl…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant semaphores to enforce C_t, per-tenant type FIFOs, and a global bounded queue. Enqueue blocks if global queue is full. Scheduler cycles tenants in a fairness loop, draining one task per",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a global bounded memory pool with per-tenant FIFO queues and a credit ledger. Producers enqueue only when the tenant has credits; when credits are exhausted, non-critical events spill to a per-ten",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a 3-stage bounded pipeline with per-tenant in-order guarantees using per-tenant sequence numbers and per-tenant buffers across stages. A single global bounded queue enforces backpressure while fair scheduling…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question tests candidates' practical concurrency design skills in a multi-tenant streaming context—directly applicable to real-world scenarios involving MongoDB change streams and Snowflake ingestion pipelines.\n\n## Key Concepts Demonstrated\n- **Per-tenant in-order delivery**: Sequence numbers and per-tenant buffers ensure events from each tenant are processed sequentially\n- **Bounded buffers and backpressure**: Global bounded queue with capacity limits prevents memory exhaustion and enables producer throttling\n- **Dynamic worker resizing**: Runtime scaling of the worker pool adapts processing parallelism to current throughput demands",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-5",
        "questionId": "q-22823",
        "question": "Design a beginner-friendly concurrency exercise: multiple services publish user events into a bounded global queue, each user has its own per-user FIFO buffer; a fixed worker pool drains events from all buffers while preserving per-user order and fairly interleaving across users. Implement backpressure, a starvation-free fairness policy, and a graceful shutdown; include a concrete test plan and workload example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a global dedupe map (task_id -> seen_at) with short TTL stored in a central store like Redis, per-region bounded queues with backpressure signaling, and a dispatcher that routes to per-task workers. Ensure idempoten…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose per-vehicle bounded FIFOs of size B and a shared dispatcher. Producers append to the vehicle queue; workers fetch the heads, maintaining per-vehicle order with per-vehicle sequence numbers. Ba",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bounded global queue plus per-user FIFO buffers. A fixed worker pool polls buffers in a round-robin with weight on occupancy to preserve per-user order while interleaving users. Backpressure: bl",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design uses per-user FIFO queues feeding a shared dispatcher with a fixed-size worker pool. Each user has a bounded queue; producers enqueue non-blockingly and back off or drop messages when full. Dis",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of per-user isolation, fairness, backpressure, and graceful shutdown in a multi-producer/concurrent-consumer setting.\n\n## Key Concepts\n- Bounded queues\n- Per-user FIFO buffers\n- Weighted round-robin interleaving\n- Backpressure strategies\n- Graceful shutdown\n\n## Code Example\n```javascript\nclass UserEventQueue {\n  constructor(cap){ this.cap=cap; this.global=[]; this.buffers=new Map(); }\n  enqueue(uid, item){ if(!this.buffers.has(uid)) this.buffers.set(uid, []); this.buffers.get(uid).push(item); if(this.global.length < this.cap) this._flush(); }\n  dequeue(){ /* pick from non-empty buffers while preserving per-user order */ }\n}\n```\n\n## Follow-up Questions\n- How would you handle dynamic user churn?\n- How would you test starvation guarantees and latency?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-6",
        "questionId": "q-2876",
        "question": "Design a dynamic DAG-based task scheduler with runtime-submitted tasks and dependencies; implement a fixed-size worker pool with local queues, support cross-worker work-stealing, and ensure dependents wake atomically when prerequisites finish; bound the global queue to enforce backpressure; discuss deadlock avoidance and fairness under high contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a global bounded memory pool with per-tenant FIFO queues and a credit ledger. Producers enqueue only when the tenant has credits; when credits are exhausted, non-critical events spill to a per-ten",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-publisher FIFO translation pipeline with a bounded global queue. Publishers push posts with language and priority; M workers translate to a target language. Maintain per-publisher orde",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Represent tasks as nodes with in-degrees and adjacency lists. Each worker owns a local queue; when a task’s in-degree becomes 0, enqueue to a worker. On completion, atomically decrement children and e",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Produce a bounded global queue with per-tenant FIFO buffers and a worker pool. Each tenant has a token-bucket limiter; producers await when their bucket is empty or the queue is full. Backpressure pro",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical concurrency design for dynamic workloads, runtime DAGs, backpressure, and fairness in a multi-threaded runtime common in real systems.\n\n## Key Concepts\n\n- DAG scheduling with in-degree tracking\n- Local queues and cross-worker work-stealing\n- Bounded global queue for backpressure\n- Deadlock and livelock avoidance, fairness under contention\n\n## Code Example\n\n```go\n// Skeleton illustrating the core ideas\ntype Task struct {\n  id   string\n  deps int32\n  // children references omitted for brevity\n}\n\n// On submit: if in-degree becomes 0 -> enqueue to a worker\n// On completion: atomically decrement children; enqueue when 0\n```\n\n## Follow-up Questions\n\n- How would you test for starvation or livelock in this scheduler?\n- How would you extend to support task priorities or IO-bound vs CPU-bound tasks?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-7",
        "questionId": "q-713",
        "question": "Implement a thread-safe bounded queue using a fixed circular buffer with mutex protection and condition variables for coordinating producers and consumers. How would you handle enqueue/dequeue operations and implement graceful shutdown when the queue is no longer accepting new tasks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a two-layer scheduler: per-tenant FIFO queues guarded by a token-bucket quota, and a central arbiter that assigns M workers via deficit-round-robin to preserve per-tenant order while ensuring f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant FIFO lanes using fixed-capacity lock-free queues. Route events from the global queue to corresponding tenant lanes via deterministic partitioning on tenant_id. Assign tenant shards to each worker fo…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a mutex to protect shared state (head, tail, count) and two condition variables: not_full for producers to wait when the buffer is full, and not_empty for consumers to wait when empty. During shutdown, signal all wa…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement: per-device FIFO buffers feeding a bounded global queue; producers block when full. Degrade when backlog exceeds threshold by dropping older items for low-priority devices and keeping only t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests fundamental concurrency concepts including mutex synchronization, condition variables for thread coordination, and graceful shutdown patterns - all essential for building reliable concurrent systems.\n\n## Key Concepts\n- **Mutex**: Protects shared state (head, tail, count, buffer) from race conditions\n- **Condition Variables**: not_full blocks producers when buffer is full, not_empty blocks consumers when empty\n- **Circular Buffer**: Efficient fixed-size storage using modulo arithmetic for wrap-around\n- **Shutdown Handling**: Clean termination by signaling all waiting threads\n\n## Implementation Details\n```python\nimport threading\nimport time\n\nclass BoundedQueue:\n    def __init__(self, capacity):\n        self.capacity = capacity\n        self.buffer = [None] * capacity\n        self.head = 0  # dequeue position\n        self.tail = 0  # enqueue position\n        self.count = 0\n        self.mutex = threading.Lock()\n        self.not_full = threading.Condition(self.mutex)\n        self.not_empty = threading.Condition(self.mutex)\n        self.shutdown = False\n    \n    def enqueue(self, item):\n        with self.not_full:\n            while self.count == self.capacity and not self.shutdown:\n                self.not_full.wait()\n            \n            if self.shutdown:\n                return False  # Indicate shutdown\n            \n            self.buffer[self.tail] = item\n            self.tail = (self.tail + 1) % self.capacity\n            self.count += 1\n            \n            self.not_empty.notify()\n            return True\n    \n    def dequeue(self):\n        with self.not_empty:\n            while self.count == 0 and not self.shutdown:\n                self.not_empty.wait()\n            \n            if self.shutdown and self.count == 0:\n                return None  # Indicate shutdown\n            \n            item = self.buffer[self.head]\n            self.buffer[self.head] = None  # Clear reference\n            self.head = (self.head + 1) % self.capacity\n            self.count -= 1\n            \n            self.not_full.notify()\n            return item\n    \n    def graceful_shutdown(self):\n        with self.mutex:\n            self.shutdown = True\n            self.not_full.notify_all()\n            self.not_empty.notify_all()\n```\n\n## Shutdown Behavior\n- **Producers**: enqueue() returns False when shutdown is active\n- **Consumers**: dequeue() returns None when queue is empty AND shutdown is active\n- **Signal Broadcasting**: notify_all() wakes all waiting threads for clean exit\n- **Idempotent**: Multiple shutdown calls are safe\n\n## Usage Pattern\n```python\n# Producer thread\ndef producer(queue):\n    for i in range(100):\n        if not queue.enqueue(i):\n            print(\"Producer shutting down\")\n            break\n\n# Consumer thread  \ndef consumer(queue):\n    while True:\n        item = queue.dequeue()\n        if item is None:\n            print(\"Consumer shutting down\")\n            break\n        process(item)\n\n# Graceful shutdown\nqueue.graceful_shutdown()\n```",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-8",
        "questionId": "q-3217",
        "question": "Design a per-session ordered event dispatcher for a real-time multiplayer game server. Each game session has its own FIFO queue; multiple producers can enqueue events across sessions, and a fixed-size pool of worker threads processes events. Requirements: (1) preserve strict per-session order; (2) allow cross-session work-stealing for load balancing; (3) enforce a global bounded queue with backpressure to producers; (4) support dynamic session creation/destruction with zero deadlock and graceful shutdown; (5) discuss memory reclamation and safety?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose per-vehicle FIFO buffers, a bounded global queue, and a dynamic worker pool that scales with backlog up to a cap and cooldown. Use per-vehicle sequence numbers to retain order; handle late mes",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a bounded global queue feeding a fixed worker pool, with per-session FIFOs. Producers enqueue to the global queue; workers pull sessions, draining in-order. For load balancing, allow stealing",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Approach: maintain per-drone bounded input queues for the three streams, a fusion stage that merges by per-drone timestamp using a small window and a min-heap keyed by timestamp across streams, all fe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A multi-tenant bounded-memory scheduler with per-tenant credits, EDF within quotas, dynamic worker scaling, and backpressure signaling. Use per-tenant FIFO queues, a global arbiter allocating time sli",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes the candidate's ability to design a concurrent, low-latency dispatcher that preserves per-session ordering while scaling across cores. It also exercises lifecycle management and safe shutdown.\n\n### Key Concepts\n- Bounded backpressure and global queues\n- Per-session FIFO with in-order processing\n- Work stealing across sessions without violating order\n- Dynamic session lifecycle and memory reclamation\n\n### Code Example\n```rust\n// Pseudo Rust sketch: session queues and stealing interface\nstruct SessionQueue { /* ... */ }\nimpl SessionQueue {\n  fn enqueue(&self, evt: Event);\n  fn dequeue(&self) -> Option<Event>;\n}\n```\n\n### Follow-up Questions\n- How would you extend to prioritize latency-sensitive events within a session?\n- How would you detect starvation and ensure fairness across sessions?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-9",
        "questionId": "q-16075",
        "question": "In a real-time analytics dashboard, dozens of client apps (producers) push metric events concurrently. Each event has a topic and a region. Build a beginner-friendly concurrency controller (Go or Python asyncio) with a bounded global queue and per-topic FIFO buffers. A worker pool consumes and writes to a sink. Implement a backpressure policy: when the global queue is full, drop the oldest events from the least recently active topic while preserving per-topic order. Ensure fair sharing to avoid region starvation. Provide a concrete test plan for per-topic ordering, backpressure under bursts, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: implement per-aggregate FIFO buffers, a global sequencer to assign events to workers while preserving aggregate order, and a per-aggregate backpressure tag. Use bounded queue Q, a dynamic sh",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-user buffers of size K, a central sequencer mapping user_id to a worker, and region-aware dispatch that routes new events to the correct worker while preserving order. Global queue is bounded;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bounded global queue (maxsize N) and a per-topic deque for each topic. A dispatcher moves from per-topic buffers into the global queue; a small worker pool writes to the sink. When the global qu",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Maintain per-room FIFO queues and a fixed-size global dispatcher. A central sequencer assigns tasks to workers ensuring per-room order by (room_id, seq). Global backpressure uses a bounded queue; over",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of per-topic FIFO ordering, bounded queues, backpressure, fairness, and graceful shutdown in a beginner-friendly setting.\n\n## Key Concepts\n- Bounded queues and per-topic buffers\n- Backpressure policies and fairness\n- Graceful shutdown with in-flight draining\n\n## Code Example\n```python\n# Skeleton demonstrating structure\nimport asyncio\nfrom collections import defaultdict, deque\nclass Controller:\n    def __init__(self, global_max=1024, per_topic_max=128):\n        self.global_q = asyncio.Queue(maxsize=global_max)\n        self.topic_buffers = defaultdict(lambda: deque(maxlen=per_topic_max))\n    async def submit(self, topic, item):\n        self.topic_buffers[topic].append(item)\n        await self._maybe_dispatch()\n    async def _maybe_dispatch(self):\n        for topic, buf in self.topic_buffers.items():\n            while buf and not self.global_q.full():\n                await self.global_q.put(buf.popleft())\n```",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-10",
        "questionId": "q-10445",
        "question": "Design a multi-tenant, rate-limited task scheduler for a live analytics pipeline. Each tenant t has a per-tenant concurrency cap C_t and within each tenant, tasks are categorized by type with per-type FIFO. All tasks arrive into a global bounded queue of size Q. Producers should block on enqueue when the global queue is full. When a tenant hits its cap, implement a fairness policy (round-robin across tenants or per-type priority) that avoids starving others. Support runtime changes to C_t without downtime and provide a minimal runnable enqueue/consume sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a durable in-flight map keyed by taskID to track status; mark inflight on enqueue, complete on success, and persist to a durable log for crash recovery. Workers pull from a bounded channel; before",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-session ordered processor with a global bounded queue and per-session FIFOs. Two enrichment paths: a fast path for low-latency services and a spill-backed path for slower ones. Enforce",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant semaphores to enforce C_t, per-tenant type FIFOs, and a global bounded queue. Enqueue blocks if global queue is full. Scheduler cycles tenants in a fairness loop, draining one task per",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a centralized DAG orchestrator with a globally increasing sequence per input, per-edge bounded queues, and per-node in-degree tracking. Each event carries a sequence and key; operators flush only",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical handling of dynamic per-tenant quotas, fairness under contention, and runtime reconfiguration in a streaming context.\n\n## Key Concepts\n\n- Per-tenant concurrency caps\n- Fair scheduling across tenants\n- Global bounded queues and backpressure\n- Runtime cap reconfiguration without downtime\n- Correctness: ordering within tenant and starvation avoidance\n\n## Code Example\n\n```javascript\n// Pseudo-code sketch: minimal enqueue/consume with per-tenant controls\nclass Scheduler {\n  constructor(Q, tenants) {\n    this.globalQ = new BoundedQueue(Q);\n    this.tenants = tenants; // map<tenantId, {C_t, sem, queues}>\n  }\n  enqueue(task) {\n    // block if globalQ full\n    this.globalQ.enqueue(task);\n  }\n  workerLoop() {\n    while (true) {\n      for (const t of this.tenants.values()) {\n        if (t.semaphore.tryAcquire()) {\n          const x = t.queues[type].dequeue();\n          if (x) process(x);\n        }\n      }\n      // optional backoff\n    }\n  }\n  updateCap(tenantId, newCap) {\n    const t = this.tenants.get(tenantId);\n    t.semaphore.updateLimit(newCap);\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test fairness under bursty arrivals?\n- How would you scale to thousands of tenants while keeping low latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-11",
        "questionId": "q-4923",
        "question": "In a high-throughput streaming service, multiple producers publish events across distinct topics to a shared bounded buffer. Design a dispatcher in Rust that routes events to per-topic worker pools using a single ring buffer. Include per-topic offsets, backpressure when full, fair scheduling, and a clean shutdown. Provide a runnable minimal sketch and testing guidance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-level queue: a bounded global ring buffer plus per-tenant FIFO subqueues. Each event includes tenantId and a monotonic sequence. Producers push to the ring; a dispatcher forwards to the tenant’s q",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a single bounded ring buffer (size N) shared by all topics. Maintain per-topic consumer cursors and a single producer index with atomic enqueue. Producers await when full; workers poll per-topic o",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use per-tenant bounded queues, fed by a central bounded dispatch buffer. Each tenant has a single consumer to preserve per-tenant order; cross-tenant work proceeds in parallel. Implement backpressure",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-key ordered buffers with a central bounded queue, assign each key a sequence number, and maintain a moving watermark. Workers dequeue ready events by key priority, pushing late events to the c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a low-latency, multi-topic dispatcher with bounded resources, backpressure, and fairness.\n\n## Key Concepts\n\n- Bounded buffers, ring buffers, per-topic cursors\n- Atomics, memory ordering, CAS\n- Backpressure, starvation avoidance, shutdown\n\n## Code Example\n\n```rust\n// Placeholder sketch\n```\n\n## Follow-up Questions\n\n- How would you handle dynamic topic addition?\n- How would you verify fairness under bursty load?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-12",
        "questionId": "q-19319",
        "question": "Design a beginner-friendly concurrency controller for a video ingest system with thousands of streams (producers) sending chunks tagged by stream-id into a bounded global queue consumed by a small worker pool. Preserve per-stream FIFO order, apply backpressure when full, and implement a compaction path that drops late chunks for inactive streams under backlog pressure. How would you test per-stream ordering, backpressure behavior, and compaction correctness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-user sequence tokens and partitioned queues to keep order across stages. Route all events for a user to the same detector/aggregator path and bound buffers with a shared backpressure signal. F",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-stream FIFO buffers keyed by stream-id, backed by a single bounded global queue consumed by a small worker pool. Producers enqueue chunks into per-stream buffers; a scheduler drains each stream's buffer in round…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a bounded global queue fed by a small moderator pool, with per-chat-room FIFO buffers to preserve order. Implement a per-room rate limiter (token bucket) and a soft-drop path that drops messages f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Central bounded dispatcher (size N) feeds per-region FIFO buffers. Maintain per-region sequence numbers to preserve order on replay. Evicted requests spill to a disk-backed LRU store. Backpressure is",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design a practical scheduler with per-stream ordering, backpressure, and a degradation path under backlog. It evaluates understanding of idleness handling and compaction strategies.\n\n## Key Concepts\n- **Per-stream FIFO buffers**: Maintain insertion order for each stream\n- **Global bounded queue**: Enforce capacity limits and trigger backpressure\n- **Scheduling policy**: Fair drainage across streams (e.g., round-robin, weighted fair queuing)\n- **Compaction/dropping policy**: Graceful degradation under backlog (e.g., drop late chunks for inactive streams)\n\n## Testing Approach\n- **Per-stream ordering**: Verify chunks from each stream-id are processed in insertion order using sequence numbers or timestamps\n- **Backpressure behavior**: Monitor producer blocking when the global queue reaches capacity; verify resumed processing after space frees\n- **Compaction correctness**: Simulate inactive streams under backlog; confirm late chunks are dropped while maintaining reasonable throughput for active streams",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-13",
        "questionId": "q-7308",
        "question": "You run a real-time data ingestion service. Many producers publish tasks with a deadline timestamp and a payload size. A bounded global queue holds tasks; a scheduler pops tasks by earliest deadline and assigns them to a small worker pool for processing. Design a beginner-friendly Go or Python asyncio solution that enforces backpressure, explores deadline fairness, and includes a concrete test plan for late tasks, jitter, and queue fullness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-key in-flight serialization with a map of keys to queues; a bounded global queue blocks producers when full; dynamic resizing via atomic counters and worker tickets; ensure in-order guarante",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant FIFO buffers of size K feeding a global bounded queue Q. Each tenant gets a dynamic cadence window controlled by a timer-wheel, with tokens that throttle bursty tenants but allow fast t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-producer bounded queues fed into a rotating, weighted fair dispatcher that pushes into a fixed-size ring buffer consumed by worker threads. Use a single-order-book lock for matching, wit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a bounded queue and a dedicated deadline scheduler (earliest-deadline-first) feeding a small worker pool; implement with Go channels or Python asyncio. Backpressure via bounded queue size; tie-break with FIFO within…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis asks for a practical scheduling pattern beyond per-domain or per-user queues, focusing on deadline-driven work with simple primitives. It tests understanding of concurrency primitives, backpressure mechanisms, and scheduler design.\n\n## Key Concepts\n\n- **Bounded queues**: Queue with max capacity; producers block or error when full, providing backpressure\n- **Deadline-aware scheduling (EDF)**: Always select task with earliest deadline first - optimal for minimizing missed deadlines\n- **Backpressure**: Flow control where downstream congestion propagates upstream to slow producers\n- **Fairness among tasks with equal deadlines**: Use FIFO ordering as tie-breaker to ensure predictable behavior\n\n## Code Example (Python asyncio)\n\n```python\nimport asyncio\nimport heapq\nimport time\nfrom dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass(order=True)\nclass Task:\n    deadline: float\n    payload_size: int = field(compare=False)\n    data: str = field(compare=False)\n    id: int = field(compare=False)\n\nclass DeadlineScheduler:\n    def __init__(self, max_queue_size: int, num_workers: int):\n        self.queue: asyncio.PriorityQueue = asyncio.PriorityQueue(maxsize=max_queue_size)\n        self.workers = []\n        self.num_workers = num_workers\n        self.running = True\n        self.processed = 0\n        self.missed_deadlines = 0\n        \n    async def producer(self, task_id: int, deadline_offset: float, payload_size: int):\n        deadline = time.time() + deadline_offset\n        task = Task(deadline=deadline, payload_size=payload_size, data=f\"task-{task_id}\", id=task_id)\n        try:\n            await asyncio.wait_for(self.queue.put(task), timeout=5.0)\n            print(f\"Producer {task_id}: queued (deadline={deadline:.2f}, size={payload_size})\")\n        except asyncio.TimeoutError:\n            print(f\"Producer {task_id}: BACKPRESSURE - queue full, dropping task\")\n            self.missed_deadlines += 1\n            \n    async def worker(self, worker_id: int):\n        while self.running:\n            try:\n                task = await asyncio.wait_for(self.queue.get(), timeout=1.0)\n                current_time = time.time()\n                latency = current_time - task.deadline\n                \n                if latency > 0:\n                    print(f\"Worker {worker_id}: LATE by {latency:.3f}s - {task.data}\")\n                    self.missed_deadlines += 1\n                else:\n                    print(f\"Worker {worker_id}: processed {task.data} (margin={-latency:.3f}s)\")\n                    \n                self.processed += 1\n                self.queue.task_done()\n                await asyncio.sleep(0.01)  # Simulate work\n                \n            except asyncio.TimeoutError:\n                continue\n                \n    async def run(self, producers: list, duration: float = 10):\n        # Start workers\n        self.workers = [asyncio.create_task(self.worker(i)) for i in range(self.num_workers)]\n        \n        # Run producers\n        await asyncio.gather(*producers)\n        \n        # Wait for queue to drain\n        await self.queue.join()\n        \n        # Shutdown\n        self.running = False\n        await asyncio.gather(*self.workers, return_exceptions=True)\n        \n        return {\"processed\": self.processed, \"missed\": self.missed_deadlines}\n\nasync def test_deadline_fairness():\n    scheduler = DeadlineScheduler(max_queue_size=5, num_workers=2)\n    \n    # Create tasks with same deadline (FIFO tie-break)\n    producers = [\n        scheduler.producer(1, deadline_offset=1.0, payload_size=100),\n        scheduler.producer(2, deadline_offset=1.0, payload_size=200),\n        scheduler.producer(3, deadline_offset=1.0, payload_size=150),\n    ]\n    \n    results = await scheduler.run(producers)\n    print(f\"Results: {results}\")\n\nasync def test_backpressure():\n    scheduler = DeadlineScheduler(max_queue_size=3, num_workers=1)\n    \n    # Submit more tasks than queue capacity\n    producers = [\n        scheduler.producer(i, deadline_offset=5.0, payload_size=100)\n        for i in range(10)\n    ]\n    \n    results = await scheduler.run(producers)\n    print(f\"Backpressure test: {results}\")\n\nasync def test_late_tasks():\n    scheduler = DeadlineScheduler(max_queue_size=10, num_workers=1)\n    \n    # First task with short deadline\n    # Second task with long deadline but submitted after\n    producers = [\n        scheduler.producer(1, deadline_offset=0.1, payload_size=1000),  # Very short deadline\n        scheduler.producer(2, deadline_offset=2.0, payload_size=100),  # Longer deadline\n    ]\n    \n    await asyncio.sleep(0.2)  # Let first task's deadline pass\n    results = await scheduler.run(producers)\n    print(f\"Late task test: {results}\")\n\nif __name__ == \"__main__\":\n    print(\"=== Test Deadline Fairness ===\")\n    asyncio.run(test_deadline_fairness())\n    \n    print(\"\\n=== Test Backpressure ===\")\n    asyncio.run(test_backpressure())\n    \n    print(\"\\n=== Test Late Tasks ===\")\n    asyncio.run(test_late_tasks())\n```\n\n## Follow-up Questions\n\n- **How to test fairness when deadlines tie?** Use tasks with identical deadlines and verify FIFO ordering via task IDs in output\n- **How adapt to clock drift or skew?** Use logical clocks or monotonic time; for distributed systems, use NTP synchronization or logical timestamps\n- **How would you extend for multiple priority levels?** Add priority field to Task, use composite sort key (priority, deadline), or use separate queues per priority level\n- **How to handle jitter?** Add random delays in producers, measure actual vs expected processing times, and monitor queue depth over time",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-14",
        "questionId": "q-19196",
        "question": "Design a real-time alert routing engine with N producers sending alerts tagged by source and severity into a shared bounded queue. Implement per-source FIFO buffers and a central dispatcher with M workers that route to sinks based on severity, preserving per-source order and guaranteeing bounded latency under bursts. Add a high-priority fast-path for critical alerts and a backpressure signaling mechanism to producers. Provide a runnable sketch and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a three-stage pipeline with per-user session keys, bounded per-key buffers, and a watermark-based event-time model. Propagate backpressure with PAUSE/RESUME tokens from Aggregator to Transforme",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two approaches: (A) Locks/Cond: track count, target N, and a generation value. Each worker increments count and waits while count < N; the last increments generation and broadcasts; all wake, verify g",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an asyncio.Queue(maxsize=128) with 4 worker coroutines. Producers await queue.put(task) to apply backpressure. Each worker pops tasks, processes image transforms, then calls queue.task_done(). On",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: implement N per-source FIFO buffers feeding a single bounded queue; a dispatcher with M workers runs a per-source fairness scheduler (weighted fair queuing) to select events and route them to si",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical concurrency skills: managing backpressure, per-source ordering, and prioritization under bursts in a real-time routing scenario.\n\n## Key Concepts\n- Bounded queues; per-source FIFO buffers\n- Fair scheduling across sources (weighted fair queuing)\n- Priority fast-paths and backpressure signaling\n- Latency budgets and late-event handling\n\n## Code Example\n```javascript\n// sketch of enqueue + dispatch logic\n```\n\n## Follow-up Questions\n- How would you measure tail latency under burst traffic?\n- How would you extend to multi-region alert routing with cross-region ordering?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-15",
        "questionId": "q-12277",
        "question": "You're building a real-time bidding platform's concurrency controller. Ingest bids from many services into a bounded global queue; each bid has auction_id and bid_id with a timestamp. Guarantee per-auction in-order processing across all workers under burst traffic. Add a soft latency budget L: if estimated time exceeds L, route to a fast-path or drop per policy, while preserving auction order. Include per-auction buffers, dynamic worker scaling, backpressure signaling; provide a runnable sketch and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: use per-room bounded buffers and a dispatcher that polls buffers in a round-robin fashion, forwarding to a shared outbound queue with a fixed capacity. Guarantee room ordering by never reord",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-vehicle FIFO queues with sequence numbers ensure strict in-order delivery, while a dispatcher routes each vehicle's stream to a dedicated sub-pipeline. Each stage maintains per-vehicle state windows and uses per-veh…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-auction FIFO buffers with bounded size, a global bounded queue, and a sequencer to assign tasks to a scalable worker pool, guaranteeing per-auction in-order processing. Implement a soft latency bu",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a centralized DAG orchestrator with a globally increasing sequence per input, per-edge bounded queues, and per-node in-degree tracking. Each event carries a sequence and key; operators flush only",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nRealistic concurrency with per-key ordering, backpressure, and latency budgets in a high-throughput domain.\n\n## Key Concepts\n- Per-auction ordering with bounded buffers\n- Soft latency budgets and conditional fast-paths\n- Dynamic worker scaling and backpressure signaling\n\n## Code Example\n```javascript\n// sketch omitted\n```\n\n## Follow-up Questions\n- How would you handle late arrivals and gap recovery?\n- What tests validate backpressure propagation and latency guarantees?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-16",
        "questionId": "q-9828",
        "question": "Design a per-topic constrained event bus in Go for a high-throughput analytics pipeline. Hundreds of producers publish events tagged by topic; a bounded global queue feeds a pool of workers that must preserve per-topic order while interleaving topics. Implement dynamic topic prioritization (hot-topic boosting) with backpressure and graceful shutdown. Provide a runnable skeleton showing enqueue, dispatch, and shutdown hooks, plus a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a global bounded intake channel feeding per-tenant FIFO queues. A dispatcher allocates work to a fixed worker pool using a round-robin over active tenants, preserving order via next_seq per tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build per-service, per-event_type sliding window counts with bounded buffers. Ingest events with service_id, event_type, and timestamp; maintain a 10s window using per-service maps of event_type bucke",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a bounded global Go channel combined with per-topic FIFO queues. Producers publish to topic-specific queues; a dispatcher goroutine enforces per-topic ordering while interleaving topics across the global chann…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a bounded, multi-priority scheduler with three per-priority queues (0 high). Producers call offer with a timeout to apply backpressure. Workers poll queues in priority order (0, then 1, then",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates advanced concurrency patterns and system design capabilities. It tests multi-producer multi-consumer coordination with per-topic ordering guarantees, dynamic prioritization mechanisms, and robust backpressure handling. The solution demonstrates understanding of real-world ingestion systems that must balance throughput with ordering constraints.\n\n## Key Concepts\n\n- Bounded concurrency and backpressure management\n- Per-topic FIFO ordering preservation\n- Dynamic topic prioritization using weighted round-robin\n- Graceful shutdown with proper resource cleanup",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-17",
        "questionId": "q-15853",
        "question": "Design a multi-region ride-matching system with two input streams: rider_requests and driver_availabilities. Each region has a bounded FIFO queue for requests. A central dispatcher assigns each rider in-region to an idle driver in the same region, preserving per-region order of requests. When a region backlog exceeds a threshold, divert new requests to a pending pool with a policy that preserves order. Support dynamic worker scaling across regions and a backpressure signaling mechanism. Provide data structures and a runnable test plan demonstrating correctness under burst traffic?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a global bounded queue and per-tenant FIFO buffers. A fixed worker pool drains the global queue and enqueues events to their tenant buffers, which are also FIFO. Aggregators read tenant buffers in",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-stream bounded queues and a fixed worker pool. Producers enqueue into their stream's queue; if full, apply backpressure by blocking or signaling upstream. Use per-stream sequence numbers and a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a multi-region ride-matching system with two input streams: rider_requests and driver_availabilities. Each region has a bounded FIFO queue for requests. A central dispatcher assigns each rider in-region to an idl…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a bounded queue channel and a scalable worker pool. Start with minWorkers; a scaler grows workers when queue length crosses a high watermark and shrinks when it falls below a low watermark, bounde",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates understanding of per-region concurrency, ordering guarantees, and backpressure handling under burst traffic. It exercises dynamic scaling and isolation between regions, mirroring real-world ride-matching workloads.\n\n## Key Concepts\n\n- Per-region FIFO ordering\n- Bounded queues and backpressure\n- Dynamic worker scaling\n- Pending pool policy and overflow handling\n\n## Solution Overview\n\nThe architecture consists of:\n1. **Region Queues**: Bounded FIFO queues per region for rider requests\n2. **Central Dispatcher**: Matches riders to drivers within the same region\n3. **Pending Pool**: Overflow mechanism when region backlog exceeds threshold\n4. **Worker Pool**: Dynamic scaling based on load\n5. **Backpressure Signal**: Alerts upstream systems when queues are saturated\n\n## Data Structures\n\n```go\npackage main\n\nimport (\n\t\"context\"\n\t\"sync\"\n\t\"time\"\n)\n\ntype RiderRequest struct {\n\tRegion string\n\tID     string\n\tTS     int64\n}\n\ntype DriverAvail struct {\n\tRegion string\n\tID     string\n\tTS     int64\n}\n\ntype RegionData struct {\n\tName             string\n\tRequestQueue     chan RiderRequest\n\tDriverQueue      chan DriverAvail\n\tPendingPool      *PendingPool\n\tBacklogThreshold int\n\tMaxQueueSize     int\n\tmu               sync.RWMutex\n\tworkerCount      int\n}\n\ntype PendingPool struct {\n\tmu      sync.Mutex\n\tqueue   []RiderRequest\n\tregions map[string]int\n}\n\ntype Dispatcher struct {\n\tregions      map[string]*RegionData\n\tpendingPool  *PendingPool\n\tworkerPool   *WorkerPool\n\tbackpressure chan struct{}\n\tmu           sync.RWMutex\n}\n\nfunc NewDispatcher(maxQueueSize, backlogThreshold int) *Dispatcher {\n\treturn &Dispatcher{\n\t\tregions:      make(map[string]*RegionData),\n\t\tpendingPool:  NewPendingPool(),\n\t\tworkerPool:   NewWorkerPool(),\n\t\tbackpressure: make(chan struct{}, 1),\n\t}\n}\n\nfunc NewPendingPool() *PendingPool {\n\treturn &PendingPool{\n\t\tqueue:   make([]RiderRequest, 0),\n\t\tregions: make(map[string]int),\n\t}\n}\n\nfunc (p *PendingPool) Add(req RiderRequest) {\n\tp.mu.Lock()\n\tdefer p.mu.Unlock()\n\tp.queue = append(p.queue, req)\n\tp.regions[req.Region]++\n}\n\nfunc (p *PendingPool) GetByRegion(region string) *RiderRequest {\n\tp.mu.Lock()\n\tdefer p.mu.Unlock()\n\tfor i, req := range p.queue {\n\t\tif req.Region == region {\n\t\t\tp.queue = append(p.queue[:i], p.queue[i+1:]...)\n\t\t\tp.regions[region]--\n\t\t\treturn &req\n\t\t}\n\t}\n\treturn nil\n}\n\nfunc (p *PendingPool) IsEmpty() bool {\n\tp.mu.Lock()\n\tdefer p.mu.Unlock()\n\treturn len(p.queue) == 0\n}\n\ntype WorkerPool struct {\n\twg         sync.WaitGroup\n\tworkers    map[string]int\n\tmu         sync.RWMutex\n\tctx        context.Context\n\tcancel     context.CancelFunc\n}\n\nfunc NewWorkerPool() *WorkerPool {\n\tctx, cancel := context.WithCancel(context.Background())\n\treturn &WorkerPool{\n\t\tworkers: make(map[string]int),\n\t\tctx:     ctx,\n\t\tcancel:  cancel,\n\t}\n}\n\nfunc (wp *WorkerPool) Scale(region string, count int) {\n\twp.mu.Lock()\n\tdefer wp.mu.Unlock()\n\tdiff := count - wp.workers[region]\n\tif diff > 0 {\n\t\tfor i := 0; i < diff; i++ {\n\t\t\twp.wg.Add(1)\n\t\t\tgo wp.worker(region)\n\t\t}\n\t} else if diff < 0 {\n\t\twp.workers[region] = count\n\t}\n\twp.workers[region] = count\n}\n\nfunc (wp *WorkerPool) worker(region string) {\n\tdefer wp.wg.Done()\n\t<-wp.ctx.Done()\n}\n\nfunc (d *Dispatcher) RegisterRegion(name string, maxQueueSize, backlogThreshold int) {\n\td.mu.Lock()\n\tdefer d.mu.Unlock()\n\td.regions[name] = &RegionData{\n\t\tName:             name,\n\t\tRequestQueue:     make(chan RiderRequest, maxQueueSize),\n\t\tDriverQueue:      make(chan DriverAvail, maxQueueSize),\n\t\tPendingPool:      d.pendingPool,\n\t\tBacklogThreshold: backlogThreshold,\n\t\tMaxQueueSize:     maxQueueSize,\n\t}\n}\n\nfunc (d *Dispatcher) HandleRiderRequest(req RiderRequest) bool {\n\td.mu.RLock()\n\tregion, ok := d.regions[req.Region]\n\td.mu.RUnlock()\n\n\tif !ok {\n\t\treturn false\n\t}\n\n\tselect {\n\tcase region.RequestQueue <- req:\n\t\treturn true\n\tdefault:\n\t\tif len(region.RequestQueue) >= region.BacklogThreshold {\n\t\t\tselect {\n\t\t\tcase d.backpressure <- struct{}{}:\n\t\t\tdefault:\n\t\t\t}\n\t\t\tregion.PendingPool.Add(req)\n\t\t\treturn true\n\t\t}\n\t\treturn false\n\t}\n}\n\nfunc (d *Dispatcher) HandleDriverAvail(avail DriverAvail) bool {\n\td.mu.RLock()\n\tregion, ok := d.regions[avail.Region]\n\td.mu.RUnlock()\n\n\tif !ok {\n\t\treturn false\n\t}\n\n\tselect {\n\tcase region.DriverQueue <- avail:\n\t\treturn true\n\tdefault:\n\t\treturn false\n\t}\n}\n\nfunc (d *Dispatcher) ProcessRegion(regionName string) {\n\td.mu.RLock()\n\tregion := d.regions[regionName]\n\td.mu.RUnlock()\n\n\tfor {\n\t\tselect {\n\t\tcase req := <-region.RequestQueue:\n\t\t\td.matchAndAssign(region, req)\n\t\tcase avail := <-region.DriverQueue:\n\t\t\td.matchAndAssignDriver(region, avail)\n\t\tcase req := region.PendingPool.GetByRegion(regionName):\n\t\t\tif req != nil {\n\t\t\t\td.matchAndAssign(region, *req)\n\t\t\t}\n\t\t}\n\t}\n}\n\nfunc (d *Dispatcher) matchAndAssign(region *RegionData, req RiderRequest) {\n\tselect {\n\tcase avail := <-region.DriverQueue:\n\t\td.assignDriver(req, avail)\n\tdefault:\n\t\tregion.RequestQueue <- req\n\t}\n}\n\nfunc (d *Dispatcher) matchAndAssignDriver(region *RegionData, avail DriverAvail) {\n\tselect {\n\tcase req := <-region.RequestQueue:\n\t\td.assignDriver(req, avail)\n\tdefault:\n\t\tregion.DriverQueue <- avail\n\t}\n}\n\nfunc (d *Dispatcher) assignDriver(req RiderRequest, avail DriverAvail) {\n\t// Assignment logic here\n}\n\nfunc (d *Dispatcher) GetBackpressureStatus() bool {\n\tselect {\n\tcase <-d.backpressure:\n\t\treturn true\n\tdefault:\n\t\treturn false\n\t}\n}\n```\n\n## Runnable Test Plan\n\n```go\npackage main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n\t\"time\"\n)\n\nfunc TestRegionFIFOOrdering(t *testing.T) {\n\td := NewDispatcher(10, 5)\n\td.RegisterRegion(\"us-east\", 10, 5)\n\n\tvar wg sync.WaitGroup\n\tresults := make(chan string, 3)\n\n\t// Submit requests in order\n\tfor i := 0; i < 3; i++ {\n\t\twg.Add(1)\n\t\tgo func(i int) {\n\t\t\tdefer wg.Done()\n\t\t\treq := RiderRequest{Region: \"us-east\", ID: fmt.Sprintf(\"r%d\", i), TS: time.Now().UnixNano()}\n\t\t\td.HandleRiderRequest(req)\n\t\t}(i)\n\t}\n\n\twg.Wait()\n\tclose(results)\n\n\t// Verify order preserved\n\tvar ids []string\n\tfor r := range results {\n\t\tids = append(ids, r)\n\t}\n\n\tif len(ids) != 3 {\n\t\tt.Errorf(\"Expected 3 results, got %d\", len(ids))\n\t}\n}\n\nfunc TestBackpressureThreshold(t *testing.T) {\n\td := NewDispatcher(5, 3)\n\td.RegisterRegion(\"us-east\", 5, 3)\n\n\t// Fill queue beyond threshold\n\tfor i := 0; i < 5; i++ {\n\t\treq := RiderRequest{Region: \"us-east\", ID: fmt.Sprintf(\"r%d\", i)}\n\t\tok := d.HandleRiderRequest(req)\n\t\tif !ok && i < 3 {\n\t\t\tt.Errorf(\"Request %d should succeed\", i)\n\t\t}\n\t}\n\n\t// Verify backpressure triggered\n\tif !d.GetBackpressureStatus() {\n\t\tt.Error(\"Backpressure should be triggered\")\n\t}\n}\n\nfunc TestPendingPoolOverflow(t *testing.T) {\n\td := NewDispatcher(3, 2)\n\td.RegisterRegion(\"us-east\", 3, 2)\n\n\t// Overflow queue\n\tfor i := 0; i < 4; i++ {\n\t\treq := RiderRequest{Region: \"us-east\", ID: fmt.Sprintf(\"r%d\", i)}\n\t\td.HandleRiderRequest(req)\n\t}\n\n\t// Verify pending pool has overflow\n\tif d.pendingPool.IsEmpty() {\n\t\tt.Error(\"Pending pool should not be empty\")\n\t}\n}\n\nfunc TestDynamicWorkerScaling(t *testing.T) {\n\twp := NewWorkerPool()\n\n\twp.Scale(\"us-east\", 3)\n\ttime.Sleep(100 * time.Millisecond)\n\n\twp.mu.RLock()\n\tcount := wp.workers[\"us-east\"]\n\twp.mu.RUnlock()\n\n\tif count != 3 {\n\t\tt.Errorf(\"Expected 3 workers, got %d\", count)\n\t}\n\n\twp.Scale(\"us-east\", 5)\n\ttime.Sleep(100 * time.Millisecond)\n\n\twp.mu.RLock()\n\tcount = wp.workers[\"us-east\"]\n\twp.mu.RUnlock()\n\n\tif count != 5 {\n\t\tt.Errorf(\"Expected 5 workers, got %d\", count)\n\t}\n\n\twp.cancel()\n\twp.wg.Wait()\n}\n\nfunc TestBurstTraffic(t *testing.T) {\n\td := NewDispatcher(100, 50)\n\td.RegisterRegion(\"us-east\", 100, 50)\n\n\tvar wg sync.WaitGroup\n\tburstSize := 200\n\n\t// Simulate burst\n\tfor i := 0; i < burstSize; i++ {\n\t\twg.Add(1)\n\t\tgo func(i int) {\n\t\t\tdefer wg.Done()\n\t\t\treq := RiderRequest{Region: \"us-east\", ID: fmt.Sprintf(\"r%d\", i)}\n\t\t\td.HandleRiderRequest(req)\n\t\t}(i)\n\t}\n\n\twg.Wait()\n\n\t// Verify system handled burst without crashing\n\td.mu.RLock()\n\tregion := d.regions[\"us-east\"]\n\td.mu.RUnlock()\n\n\tqueueLen := len(region.RequestQueue)\n\tpendingLen := len(region.PendingPool.queue)\n\n\tt.Logf(\"Queue length: %d, Pending pool length: %d\", queueLen, pendingLen)\n\n\tif queueLen+pendingLen != burstSize {\n\t\tt.Errorf(\"Expected %d total requests, got %d\", burstSize, queueLen+pendingLen)\n\t}\n}\n```\n\n## Summary\n\nThis design provides:\n- **Per-region isolation** with bounded FIFO queues\n- **Overflow handling** via pending pool preserving order\n- **Dynamic scaling** for worker pools\n- **Backpressure signaling** for upstream systems\n- **Comprehensive test coverage** for burst traffic scenarios",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-18",
        "questionId": "q-7101",
        "question": "Design a fault-tolerant, bounded telemetry router: partition by region, with per-device FIFO buffers feeding a small worker pool. Use a central bounded queue to backpressure producers; ensure in-order processing per device via sequence counters and per-device reorder buffers. On regional outage, spill to durable regional store and throttle producers with backpressure. Auto-scale workers from 1 to K based on queue depth and latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-region FIFO buffers feeding a bounded global queue consumed by a small worker pool. Enforce per-region order by a round-robin feeder and a non-blocking push when full; apply backpressure when",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use region-local per-device FIFOs feeding a small worker pool. A central bounded queue enforces backpressure; per-device order is preserved with sequence numbers and a local reorder buffer. Regional o",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design and implement a global bounded-rate limiter using a central dispatcher with per-customer FIFO queues and a shared token-bucket pool. The dispatcher allocates tokens from a global capacity of R events/sec into per…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a bounded queue channel and a scalable worker pool. Start with minWorkers; a scaler grows workers when queue length crosses a high watermark and shrinks when it falls below a low watermark, bounde",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses thinking about multi-region concurrency, ordering guarantees, fault tolerance, and dynamic scaling.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Per-device FIFO ordering with sequence counters\n- Regional spill-to-durable storage and failure handling\n- Auto-scaling and observability\n\n## Code Example\n```go\n// Skeleton\ntype Event struct { region string; device string; seq uint64; data []byte }\ntype Router struct {\n  central chan Event\n  regionBuffers map[string]map[string]chan Event\n  nextSeq map[string]uint64\n}\n```\n\n## Follow-up Questions\n- How would you test ordering under regional outages?\n- How would you ensure idempotence and exactly-once processing across regions?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-19",
        "questionId": "q-6884",
        "question": "In a real-time telemetry pipeline for multi-region IoT devices, design a bounded, region-scoped concurrency model that preserves per-region event order while capping memory usage. Describe data structures, backpressure signaling, and region lifecycle, and provide a runnable prototype outline in Go or Python showing enqueue, per-region dispatch, backpressure, and graceful shutdown. What tests would you add to verify ordering and backpressure?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use language-supported atomic operations or a minimal lock to guard the counter. For Java: counter is AtomicLong; call incrementAndGet after each event. For Go: use atomic.AddUint64; for CPython: prot",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-key sequence numbers and per-region FIFO buffers. Producers publish to a global shard keyed by hash(key); router prefers the current primary region but can failover. A cross-region checkpoint",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement region-scoped FIFO queues with a global bounded semaphore for memory control. Each region maintains its own ordered queue; a fixed worker pool drains region queues in round-robin fashion to preserve per-region…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a per-key in-flight serialization with a map of keys to queues; a bounded global queue blocks producers when full; dynamic resizing via atomic counters and worker tickets; ensure in-order guarante",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-region concurrency, backpressure propagation, and dynamic lifecycle management in realistic telemetry pipelines.\n\n## Key Concepts\n- Region-scoped FIFO queues maintaining strict ordering\n- Global bounded capacity with per-region quotas\n- Fairness across regions via round-robin draining\n- Backpressure signaling with exponential backoff\n- Idle-region retirement and automatic reactivation\n\n## Code Example\n```javascript\n// sketch: region queue management and worker coordination\n```\n\n## Follow-up Questions\n- How would you measure end-to-end latency under burst conditions?\n- What monitoring metrics would you track for system health?\n- How would you handle region-specific priority requirements?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-20",
        "questionId": "q-2080",
        "question": "In a real-time chat system, multiple producers enqueue messages into per-room bounded queues and multiple consumers deliver to connected clients. Design a beginner-friendly concurrency solution (Go, Java, or Python) that guarantees producers block when a room's queue is full and consumers block when the queue is empty, while preserving per-room isolation and simple backpressure. Compare a mutex/condition-based queue versus a channel-based approach for bounded queues?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To solve: use a bounded global queue with per-account FIFO buffers; a fixed worker pool processes items from each buffer, preserving account order. Implement backpressure by blocking enqueues when ful",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a per-host queueing layer atop a fixed worker pool. Each host maintains its own token bucket for rate limits and an in-flight linked list to preserve per-host order. The dispatcher r",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-tier scheduler: a global bounded queue feeding per-shard FIFO queues with a fixed worker pool. Producers backpressure when global is full. Scheduler uses weighted round-robin to allocate CPU fairl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-room bounded queue with blocking semantics: producers block when the queue is full, consumers block when empty. In Go, use a buffered channel per room; in Python/Java, use a bounded queue with mutex/cond…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical concurrency design for real-time systems, focusing on bounded queues, blocking behavior, and per-room isolation—core patterns at companies like HashiCorp and Discord.\n\n## Key Concepts\n\n- Bounded queues with blocking semantics\n- Per-room isolation and backpressure\n- Channel-based versus lock-based synchronization\n- Correct testing across producers/consumers\n\n## Code Example\n\n```go\n// Go sketch: per-room bounded channel\ntype Room struct {\n  Msgs chan Message\n}\nfunc NewRoom(cap int) *Room { return &Room{Msgs: make(chan Message, cap)} }\n```\n\n```python\n# Python sketch: per-room bounded queue with locks/conditions\nfrom threading import Condition, Lock\n\nclass Room:\n    def __init__(self, capacity):\n        self.capacity = capacity\n        self.queue = []\n        self.lock = Lock()\n        self.not_full = Condition(self.lock)\n        self.not_empty = Condition(self.lock)\n    \n    def enqueue(self, message):\n        with self.not_full:\n            while len(self.queue) >= self.capacity:\n                self.not_full.wait()\n            self.queue.append(message)\n            self.not_empty.notify()\n    \n    def dequeue(self):\n        with self.not_empty:\n            while not self.queue:\n                self.not_empty.wait()\n            message = self.queue.pop(0)\n            self.not_full.notify()\n            return message\n```\n\n## Comparison: Mutex/Condition vs Channel-Based\n\n**Mutex/Condition (Python/Java):**\n- More explicit control over queue state\n- Easier to implement custom behavior (timeouts, priorities)\n- Higher code complexity and error-prone\n- Manual management of condition variables\n\n**Channel-Based (Go):**\n- Simpler, idiomatic Go concurrency\n- Built-in blocking semantics\n- Less error-prone, compiler-enforced usage\n- Limited flexibility for custom queue behavior\n\nBoth approaches provide the same blocking guarantees, but channels offer cleaner code for standard bounded queue patterns.",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-21",
        "questionId": "q-788",
        "question": "In a streaming data pipeline, multiple producers generate frames and a bounded buffer sits between the producer stage and a consumer stage. Implement a backpressure-enabled producer-consumer pair in JavaScript (Node.js) using an async bounded queue that supports multiple producers and multiple consumers. It must guarantee no data loss, bounded memory, and graceful shutdown. How would you test under varying production/consumption rates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a central broker with a bounded global queue and per-customer FIFO queues. Producers enqueue ride requests to the global queue; a dispatcher assigns tasks to the corresponding per-customer q",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a beginner-friendly concurrency exercise for a real-time analytics dashboard: dozens of producers publish user interaction events (clicks, scrolls) to a bounded global queue. Implement per-user FIFO buffers and a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bounded queue with capacity N; producers await when full and consumers await when empty to enforce backpressure and bounded memory. For shutdown, push a sentinel or close the queue so workers ex",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a two-stage pipeline with per-user ordering and dynamic rebalancing. Map user_id to a worker using a consistent-hash ring; when load rises, rebalance a subset of users to less-busy workers whi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of backpressure, bounded buffers, and graceful shutdown in a concurrent environment using asynchronous primitives.\n\n## Key Concepts\n- Bounded buffers and backpressure\n- Multiple producers and multiple consumers\n- Graceful shutdown signaling\n- Correct testing under varying rates\n\n## Code Example\n```javascript\nclass BoundedQueue {\n  constructor(capacity) {\n    this.capacity = capacity\n    this.queue = []\n    this.waitPuts = []\n    this.waitGets = []\n  }\n  async push(item) {\n    if (this.queue.length < this.capacity) {\n      this.queue.push(item)\n      if (this.waitGets.length) this.waitGets.shift()(this.queue.shift())\n      return\n    }\n    await new Promise(res => this.waitPuts.push(res))\n    return this.push(item)\n  }\n  async pop() {\n    if (this.queue.length) {\n      const item = this.queue.shift()\n      if (this.waitPuts.length) this.waitPuts.shift()\n      return item\n    }\n    return new Promise(resolve => this.waitGets.push(resolve))\n  }\n}\n// Usage sketch: create producers/consumers using q.push(q.pop())\n```\n\n## Follow-up Questions\n- How would you adapt for a single consumer vs multiple consumers?\n- What strategies exist if you must drop data when the buffer is full, and how would you test them?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-22",
        "questionId": "q-10261",
        "question": "Design a cross-region streaming pipeline with per-key in-order guarantees across regions. Propose concrete data structures and a backpressure scheme (bounded global queue, per-region credits, dynamic worker resizing) and detail late-message handling, test plan, and failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-key FIFO buffers coordinated by a global sequencer to maintain strict ordering across regions. Employ a bounded global queue with capacity Q and per-region credit allocation; producers block when credits a…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use per-user buffers of size K, a central sequencer mapping user_id to a worker, and region-aware dispatch that routes new events to the correct worker while preserving order. Global queue is bounded;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a bounded global queue that feeds into separate per-type FIFO buffers. For types requiring system-wide in-order processing, enforce sequential processing within each type buffer and route to a shared worker po…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: maintain per-key FIFO buffers and a global watermark. Stage1 writes keyed records to per-key queues; Stage2 consumes in-key order to produce windowed aggregates. Use bounded ring buffers for",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates expertise in designing low-latency, globally distributed streaming pipelines with strict ordering guarantees and sophisticated backpressure mechanisms.\n\n## Key Concepts\n- Per-key ordering preservation across geographic regions\n- Credit-based cross-region backpressure implementation\n- Dynamic worker scaling with work stealing\n- Late-message handling and fault tolerance strategies\n\n## Code Example\n```javascript\n// Core interfaces for streaming pipeline components\ntype Message = { key: string; value: any; region: string; arrival: number; };\n```\n\n## Follow-up Questions",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-23",
        "questionId": "q-19180",
        "question": "You're building a real-time analytics join engine that correlates user events from two streams: Clicks and Purchases. Each event contains user_id and timestamp. Design a concurrent join with per-user in-order guarantees under burst traffic. Use bounded buffers for each input, a dynamic worker pool to compute matches, and a central coordinator to preserve per-user ordering across both streams. Define a late-event policy (e.g., drop, DLQ) with a concrete test plan and runnable sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant queues + a bounded global queue. A scheduler assigns tenants to workers; Stage 1 enriches with simulated latency; Stage 2 updates per-tenant aggregates. Preserve per-tenant in-order across",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-symbol FIFO queues, a bounded global work queue, and a small worker pool. Preserve per-symbol ordering, use round-robin across symbols for fairness, and backpressure: if the global queue",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-vehicle FIFO queues with sequence numbers ensure strict in-order delivery, while a dispatcher routes each vehicle's stream to a dedicated sub-pipeline. Each stage maintains per-vehicle state windows and uses per-veh…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-user FIFO buffers for both streams with a bounded size K. Use a pool of workers to pull from both buffers and emit joined pairs in ascending user_id order, coordinated by a central seque",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nReal-time joins across streams under backpressure test ordering guarantees and fault handling. This problem probes buffering strategies, coordinator design, backpressure signaling, and late-event handling, all critical in production-streams roles.\n\n## Key Concepts\n- Per-user FIFO buffers with bounds\n- Dynamic worker pool and coordination\n- Cross-stream join logic with ordering guarantees\n- Backpressure signaling and late-event policy (DLQ/drop)\n- Test plan for ordering, latency, and failure modes\n\n## Code Example\n```javascript\nclass PerUserJoiner {\n  constructor(bound) {\n    this.bound = bound;\n    this.bufA = new Map(); // user_id -> [A events]\n    this.bufB = new Map(); // user_id -> [B events]\n  }\n  enqueueA(evt) { /* bounded insert */ }\n  enqueueB(evt) { /* bounded insert */ }\n  tryJoin(): Array<any> { /* produce joined records while in order */ }\n}\n```\n\n## Follow-up Questions\n- How would you extend to N streams and ensure global fairness?\n- How would you test ordering under jitter and DLQ routing at scale?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-24",
        "questionId": "q-26708",
        "question": "Design and implement a real-time fraud-detection pipeline for a multi-tenant e-commerce platform. Each merchant's events must be processed strictly in arrival order, but the system must bound total memory with a global queue and apply dynamic backpressure. Build a central dispatcher that routes to per-merchant FIFO queues; implement an LRU spill store for evicted non-critical events; maintain a per-merchant sequence number for crash recovery and replay to a precise checkpoint. Add a fast-path for high-priority events, support cancellation and graceful shutdown. Provide runnable sketches in Rust (Tokio) or Java and a concrete test plan covering ordering, backpressure, spill behavior, and recovery?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-team log ingestion pipeline with a bounded global queue and per-team FIFO buffers. Each log item carries team_id and severity (INFO/WARN/ERROR). Downstream analyzers process per-team logs in arrival order,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a dispatcher that feeds per-merchant FIFO queues from a global bounded buffer with dynamic backpressure propagation. Each merchant maintains a sequence number and a durable replay log to enable deterministic r…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Partitioned ring buffers per video ID with one consumer per partition, managed by a global scheduler for hot partition balancing. Producers append with per-ID sequence numbers to preserve in-order delivery. Backpressure…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a fixed-size bounded ring buffer for the global queue and per-tenant FIFO subqueues. An arbiter assigns workers to the oldest event from a tenant, guaranteeing per-tenant order while preserving in",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates understanding of real-time multi-tenant concurrency, strict ordering guarantees, memory-bounded processing, spillover strategies, and crash recovery patterns—all essential skills for building high-throughput fraud detection systems.\n\n## Key Concepts\n\n- **Per-tenant in-order processing**: Each merchant's events must be processed in strict arrival order while maintaining isolation between tenants.\n- **Global bounded buffer with dynamic backpressure**: Prevents unbounded memory growth by applying backpressure when queue depth approaches limits.\n- **LRU spill store**: When the global buffer is full, non-critical events are evicted to secondary storage using least-recently-used semantics, ensuring critical events are always processed.\n- **Sequence numbers for recovery**: Each merchant maintains a monotonically increasing sequence number, enabling precise checkpointing and deterministic replay after failures.\n- **Fast-path for priority events**: Latency-sensitive events like fraud alerts bypass normal queuing to ensure immediate processing.\n\n## Design Trade-offs\n\nThe solution prioritizes ordering guarantees and memory safety over absolute throughput. The LRU spill store introduces a potential performance penalty for evicted events but ensures the system never drops events under load. The per-merchant FIFO queues guarantee ordering within a tenant while the global bounded buffer prevents system-wide memory exhaustion.\n\n## Test Plan\n\n1. **Ordering verification**: Submit events from multiple merchants and assert each merchant's events are processed in sequence.\n2. **Backpressure testing**: Fill the global buffer and verify rejection/spill behavior for non-critical events.\n3. **Spill recovery**: Verify evicted events are retrievable and reprocessed in correct order after spill.\n4. **Crash recovery**: Simulate failure mid-stream, restart, and confirm replay produces identical results.\n5. **Graceful shutdown**: Verify all in-flight events complete processing and no events are lost.\n6. **Fast-path latency**: Measure fraud alert processing time versus normal events to confirm prioritization.",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-20T07:30:29.357Z",
    "version": 1
  }
]