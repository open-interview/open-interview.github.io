[
  {
    "id": "test-concurrency",
    "channelId": "concurrency",
    "channelName": "Concurrency",
    "title": "Concurrency Knowledge Test",
    "description": "Test your understanding of Concurrency with 25 questions.",
    "questions": [
      {
        "id": "concurrency-q-0",
        "questionId": "q-23768",
        "question": "In a real-time data-processing platform, thousands of publishers send tasks tagged by tenant_id and priority. Build a bounded dispatcher with a global queue and per-tenant FIFO queues. Ensure per-tenant ordering, dynamic worker scaling, and backpressure; high-priority bursts must be throttled without starving low-priority tenants. Provide a runnable sketch and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-level bounded queue: a fixed-size global ring buffer for backpressure and per-topic FIFO queues feeding workers. Producers claim a slot, append to topic queue; a compact scheduler interleaves topi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a Go-based solution: a fixed-size main queue (buffered channel) fed to a worker pool, plus per-market FIFO spill buffers. Backpressure blocks producers when the main queue is full; optional ex",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a bounded, multi-tenant dispatcher: tasks arrive with tenant_id and priority. Maintain a global bounded queue and per-tenant FIFO queues. Ensure per-tenant ordering, dynamic worker scaling, and",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a deadline-aware bounded scheduler with per-producer rate limiting, earliest-deadline-first (EDF) scheduling, and global backpressure signals. Include a runnable minimal Go or Python asyncio ex",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes the ability to design a scalable, low-latency dispatcher with strict per-tenant ordering, dynamic worker scaling, and observable backpressure, plus auditable replay logs.\n\n## Key Concepts\n\n- Two-tier scheduling: global bounded queue + per-tenant FIFO queues\n- Backpressure: blocking enqueue and per-tenant throttling during bursts\n- Fairness vs priority: rotating tenant windows to avoid starvation\n- Fault tolerance and replay: deterministic logs for audits and replay\n- Cancellation and graceful shutdown\n\n## Code Example\n\n```python\nimport asyncio\n\nglobal_q = asyncio.Queue(maxsize=1024)\ntenant_queues = {}\n\nasync def enqueue(task):\n    tenant = task['tenant_id']\n    q = tenant_queues.setdefault(tenant, asyncio.Queue())\n    await q.put(task)\n    # optional: wake a central dispatcher that drains global_q\n```\n\n## Follow-up Questions\n\n- How would you test per-tenant ordering and backpressure under burst traffic?\n- How would you handle dynamic worker scaling and failure scenarios while preserving replayability?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-1",
        "questionId": "q-29780",
        "question": "Design a bounded, multi-stage real-time ranking pipeline. N producers emit events as (user_id, item_id, score, ts). Stage 1 normalizes scores per user using a per-user accumulator with fixed memory. Stage 2 maintains a per-user top-K heap in bounded RAM, merged by M workers. Stage 3 periodically flushes global top-K to a sink. Guarantee per-user order and ranking correctness under backpressure and dynamic worker resizing; outline data structures, a runnable sketch, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: partition by user_id; Stage 1 normalizes scores per user with a fixed-size per-user accumulator. Stage 2 maintains a per-user top-K heap in bounded RAM, merged by M workers. Stage 3 periodic",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a global bounded queue plus per-channel local deques. Workers drain local queues first, then fetch from the global queue with a rotating index and aging to prevent starvation. Block producers",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a global bounded queue and per-tenant FIFO buffers. A fixed worker pool drains the global queue and enqueues events to their tenant buffers, which are also FIFO. Aggregators read tenant buffers in",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a two-stage log pipeline with N tenants feeding a bounded global queue; M workers parse/enrich; K workers perform per-tenant windowed aggregation in timestamp order. Preserve per-tenant order a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a bounded, multi-stage pipeline with per-user state, dynamic concurrency, and backpressure handling. It probes correctness guarantees (ordering, top-K accuracy) under scaling.\n\n## Key Concepts\n- Per-user partitioning and state management\n- Bounded memory top-K data structure per user\n- Backpressure-aware queues and dynamic worker resizing\n- Ordering guarantees across stages and safe flushing\n\n## Code Example\n```java\n// sketch: per-user Top-K holder\nclass PerUserTopK {\n  final int K;\n  PriorityQueue<Item> heap = new PriorityQueue<>(Comparator.comparingDouble(i -> i.score));\n  void consider(Item it) { /* insert/trim to K */ }\n}\n```\n\n## Follow-up Questions\n- How would you handle skewed user activity?\n- What testing strategy verifies ordering under concurrent updates?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-2",
        "questionId": "q-2542",
        "question": "Design a two-stage, bounded-concurrency pipeline for real-time video frames from multiple cameras. Stage 1 decodes frames (CPU-bound) using a fixed thread pool; Stage 2 runs an AI-based enhancer asynchronously. Ensure per-frame ordering across all cameras, backpressure to keep queues bounded, support dynamic worker scaling and clean shutdown with cancellation, and provide a runnable minimal sketch in a language of your choice?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a fixed-size circular ring buffer with atomic head/tail indices and per-partition sequencing. Use compare-and-swap (CAS) to advance tail on enqueue and head on dequeue; preserve per-partition order by embeddin…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-publisher FIFO translation pipeline with a bounded global queue. Publishers push posts with language and priority; M workers translate to a target language. Maintain per-publisher orde",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use N per-sensor queues feeding a bounded central queue; workers pop from central queue and emit in increasing timestamp order. Implement a late-data path by routing packets with timestamp older than",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement two bounded queues: Q1 for decoding (N1 workers) and Q2 for enhancement (N2 workers). Attach per-frame sequence numbers to enforce global in-order delivery to Stage 2. Stage 1 runs on a fixed thread pool; Stag…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes real-world patterns for multi-stage pipelines, ordering guarantees, backpressure, dynamic scaling, and graceful shutdown across components. It highlights trade-offs between CPU-bound decoding and async I/O-bound processing, and tests ability to reason about cross-stage coordination.\n\n## Key Concepts\n\n- Bounded queues and backpressure\n- Ordering guarantees across concurrent stages\n- Thread pools vs async workers\n- Cancellation tokens and error propagation\n- Dynamic worker scaling\n\n## Code Example\n\n```javascript\n// Implementation code here\n```\n\n## Follow-up Considerations\n\nThe solution should address how to handle frame drops during overload scenarios, monitor queue depths for scaling decisions, and ensure proper resource cleanup during shutdown.",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-3",
        "questionId": "q-30408",
        "question": "Design a real-time, multi-tenant event processor where events arrive tagged with tenant_id and event_type. Implement a central router that distributes events to per-tenant FIFO queues backed by a global bounded buffer, with a dynamic worker pool that can rebalance across tenants to maximize throughput. Ensure backpressure, strict per-tenant ordering, and exactly-once processing using per-tenant sequence numbers and a durable, replay-ready log, with cancellation and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-edge bounded FIFOs of size K, keyed by edge_id; a dynamic shard allocator maps edge_ids to W workers via consistent hashing and allows live rebalancing with minimal movement cost. Each edge tr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant semaphores to enforce C_t, per-tenant type FIFOs, and a global bounded queue. Enqueue blocks if global queue is full. Scheduler cycles tenants in a fairness loop, draining one task per",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Provide a Go sketch: a bounded global ring buffer feeding per-tenant FIFO queues; a dynamic worker pool that can rebalance across tenants. Enqueue blocks when the global buffer is full (backpressure).",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use two queues: a global bounded high-priority queue and per-origin normal FIFOs. Dispatcher drains the high-priority queue first, then per-origin normal queues with a round-robin across origins. Back",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests deep concurrency mastery: multi-tenant routing, dynamic rebalancing, and strong ordering guarantees under backpressure. Requires thinking about bounded buffers, fault tolerance, and replay semantics.\n\n## Key Concepts\n\n- Global bounded buffering and backpressure\n- Per-tenant FIFO ordering\n- Dynamic worker pool with rebalancing\n- Durable, sequence-numbered replay log for exactly-once semantics\n- Graceful shutdown and cancellation handling\n\n## Code Example\n\n```go\npackage main\n\ntype Event struct { Tenant string; Type string; ID int64; Payload []byte }\ntype Router struct { GlobalBuf chan Event; TenantQueues map[string]chan Event; Workers int; }\nfunc main() { /* sketch */ }\n```\n\n## Follow-up Questions\n\n- How would you test replay correctness after a crash?\n- How would you ensure fairness across tenants under bursty traffic?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-4",
        "questionId": "q-29015",
        "question": "Design a per-service ordered task scheduler with a bounded global worker pool. Each service's requests must be executed in arrival order, but slow services trigger backpressure via per-service credits. Implement a central arbiter with per-service FIFOs, a global ready-queue, cancellation, and a fast-path for urgent tasks. Provide runnable sketch in Go or Rust and discuss correctness, memory bounds, and failure recovery?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-tenant FIFO queue and a single bounded backlog. Each tenant gets a dynamic quota updated by a central scheduler (token buckets that adjust to burstiness). A dispatcher drains queues in weigh",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a per-symbol partitioned, bounded ring-buffer with credit-based flow. Producers atomically decrement a per-partition credit counter to enqueue, avoiding locks; consumers advance a per-partitio",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a bounded global queue with per-service FIFO buffers, and a round-robin time-slice scheduler giving each service a fixed time budget per round. Enforce a per-round cap on processed work. Whe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-service FIFO queues and a central arbiter. Maintain a global worker semaphore for capacity. Each service has a credit counter; enqueue only promotes a task when credits > 0, then decrement cre",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a scalable, fair scheduler with per-source ordering, bounded resources, and failure handling across high-concurrency services.\n\n## Key Concepts\n- Per-service FIFO queues\n- Central arbiter with a global ready-queue\n- Credit-based backpressure and replenishment\n- Urgent fast-path and cancellation\n- Failure recovery and ordering guarantees\n\n## Code Example\n\n```javascript\n// Placeholder runnable sketch showing architecture\n```\n\n## Follow-up Questions\n- How would you adapt credit replenishment in bursty traffic? \n- How would you test for starvation and memory growth under skewed load?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-5",
        "questionId": "q-9346",
        "question": "Design a per-room fan-out dispatcher for a real-time chat app. N producers emit messages with room_id; a central dispatcher enforces per-room in-order processing using per-room FIFO buffers capped at B and a global bounded queue of size Q. M workers process messages; support dynamic resizing of workers and backpressure. Define late-message policy and provide a concrete test plan and edge-case handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-topic multi-stage streaming pipeline with dynamic worker pools and per-topic in-order guarantees. Producers publish topic-labeled messages to a bounded input queue. Workers route messages",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "In a log-aggregation pipeline, implement per-service FIFO buffers feeding a bounded global worker pool. Each service's logs preserve arrival order; inter-service logs can interleave. On backlog, degra",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a publish-subscribe event bus with a bounded global queue, topic-tagged messages, and a pool of broadcaster workers. Preserve per-topic FIFO in subscriber queues, enforce cross-topic fairness,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-room sequence numbers with bounded FIFO buffers (size B) managed by a central dispatcher feeding a global bounded queue (size Q). Workers pull from the global queue and route to room-specific deliverers th…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of per-tenant ordering under backpressure, dynamic scaling, and late message handling in real-time distributed systems.\n\n## Key Concepts\n- Per-room in-order delivery with concurrent producers\n- Bounded buffers and global queue backpressure management\n- Dynamic worker scaling with thread-safety guarantees\n- Late message policies and deduplication strategies\n\n## Code Example\n```javascript\n// Core structure for per-room buffers and dispatcher\nclass RoomBuffer {\n  constructor(limit) {\n    this.queue = [];\n    this.limit = limit;\n    this.nextSequence = 0;\n  }\n  \n  push(message) {\n    if (this.queue.length >= this.limit) return false;\n    this.queue.push(message);\n    return true;\n  }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-6",
        "questionId": "q-6750",
        "question": "Design a concurrent in-memory rate-limiter router for a fleet of microservices. N producers emit requests labeled by service_key; a bounded global queue feeds M workers. Implement per-key token buckets with independent refill rates and a global budget to cap total in-flight permits. Support dynamic key creation, backpressure signaling when the global queue is full, and a clear burst-idle policy. Provide concrete data structures and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a bounded global queue (e.g., 1024) with per-match FIFOs. Use a 4-worker pool, each worker applying updates for a given match in sequence; track per-match sequencing to detect gaps. If backl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: implement a global bounded queue feeding per-campaign FIFO buffers. Each campaign has a budget and a nextSeq; workers pull from the campaign buffer to preserve order, with dynamic resizing b",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bounded buffer API Put(item)/Get(). In Go, a buffered channel size N blocks producers when full and blocks consumers when empty. In Java, ArrayBlockingQueue<N> with put/take; Python: queue.Queue",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-key token-bucket rate limiter with a global budget. Each service_key has its own bucket refilled at its rate; total tokens across keys capped by a shared budget. Use a concurrent map w",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests integration of per-key throttling with system-wide backpressure, plus dynamic key handling under contention.\n\n## Key Concepts\n- Per-key rate limiting with independent refills\n- Global budget for overall control\n- Lock-free or low-contention data structures\n- Backpressure signaling and verification\n\n## Code Example\n```javascript\n// sketch\n```\n\n## Follow-up Questions\n- How would you adapt to multi-region deployments with shard budgets?\n- How would you validate fairness and monotonicity under bursty traffic?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-7",
        "questionId": "q-22966",
        "question": "Design a dynamic, per-user ordered analytics pipeline: N producers emit events with user_id, event_type, and ts into a bounded GlobalInputQueue. Run M Stage1 workers and K Stage2 workers. Preserve per-user order across all stages even when the key-to-worker mapping changes via a live rebalancer. Introduce a soft deadline D; events past D go to a slow-path that preserves per-user order but reduces priority. Provide runnable sketch and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant bounded queues, fed by a central bounded dispatch buffer. Each tenant has a single consumer to preserve per-tenant order; cross-tenant work proceeds in parallel. Implement backpressure",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement with a bounded global asyncio.Queue(maxsize=1000), a dict tenant_queues[tenant] -> asyncio.Queue(). Dispatcher cycles tenants in RR and enqueues to their queues; workers (N) drain per-tenant",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a two-stage pipeline with per-user ordering and dynamic rebalancing. Map user_id to a worker using a consistent-hash ring; when load rises, rebalance a subset of users to less-busy workers whi",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a fixed-size Go channel as the global queue, a mutex-protected map[string]bool for in-flight tracking, and per-ID coalescing buffers. On enqueue: if the ID is already in-flight, merge the payload with the exis…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a live, reconfigurable pipeline that preserves per-key order under dynamic worker reassignment, plus practical backpressure and latency control.\n\n## Key Concepts\n- Per-user ordering across dynamic mappings (consistent hashing, ring rebalancing)\n- Global bounded queues and backpressure signaling\n- Latency-aware routing with a slow-path for late events\n- Dynamic worker resizing and fairness\n\n## Code Example\n```javascript\n// Pseudo-code sketch: per-user buffers and ring rebalancing\nclass RingMap {\n  constructor(users, workers) { /* ... */ }\n  map(userId) { /* ... */ }\n}\nclass UserBuffers {\n  constructor() { this.buffers = new Map(); }\n  enqueue(userId, evt) { /* ... */ }\n}\nfunction rebalanceIfNeeded(state, metrics) { /* ... */ }\n```\n\n## Follow-up Questions\n- How would you test correctness under concurrent rebalances?\n- How would you bound memory for per-user buffers?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-8",
        "questionId": "q-773",
        "question": "In a Go HTTP server, you maintain a per-user quota counter in memory. Multiple requests for different users should advance concurrently, but updates to the same user must be serialized. Design a striped lock (a fixed set of mutexes) to guard a map[string]int, map userID to a bucket via hashing, and explain how you minimize contention, handle hash collisions, and ensure correctness when entries may be evicted?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a striped lock: a fixed-size slice of mutexes guards a map[string]int quotas. Hash userID to bucket index, lock bucket, read-modify-write, unlock. This lowers contention vs a global lock. Collisio",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a bounded global queue plus per-document FIFO queues. Edits carry document_id, user_id, op_id, and a vector clock. A dispatcher routes edits to the per-document queue only after all causal predece",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-stream bounded queues and a fixed worker pool. Producers enqueue into their stream's queue; if full, apply backpressure by blocking or signaling upstream. Use per-stream sequence numbers and a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Concurrency Controller Design ### Core Architecture ```python from dataclasses import dataclass from collections import defaultdict from queue import Queue, PriorityQueue import threading import time import random @d…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of fine-grained synchronization and practical trade-offs for high-concurrency in-memory state.\n\n## Key Concepts\n\n- Striped locking for reduced contention\n- Lock granularity vs global lock\n- Handling hash collisions with buckets\n- Eviction strategies and correctness\n\n## Code Example\n\n```javascript\n// Go-like pseudocode demonstrating striped locks\ntype Striped struct {\n  locks []sync.Mutex\n  quotas map[string]int\n}\nfunc NewStriped(n int) *Striped { ... }\nfunc (s *Striped) Inc(id string) { idx := hash(id) % len(s.locks); s.locks[idx].Lock(); defer s.locks[idx].Unlock(); s.quotas[id]++ }\n```\n\n## Follow-up Questions\n\n- What happens if bucket count is too small? how to resize safely?\n- How would you extend to support read-heavy workloads (readers-writer locks)?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-9",
        "questionId": "q-24577",
        "question": "Design a multi-tenant task scheduler with a single bounded backlog that preserves per-tenant FIFO order, applies dynamic quotas per tenant that adapt to observed burstiness, and introduces a global backpressure signal when backlog nears capacity. Build a runnable sketch (Go or Rust) showing per-tenant queues, a central dispatcher, and a graceful shutdown; explain how you prevent starvation and measure fairness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-tenant FIFO queue and a single bounded backlog. Each tenant gets a dynamic quota updated by a central scheduler (token buckets that adjust to burstiness). A dispatcher drains queues in weigh",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Python asyncio: give each producer a bounded queue, a central bounded queue collects coalesced updates, and a small worker pool applies updates in per-service order. A coalescer merges frequent up",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: maintain per-drone bounded input queues for the three streams, a fusion stage that merges by per-drone timestamp using a small window and a min-heap keyed by timestamp across streams, all fe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly concurrency exercise for a real-time analytics dashboard: dozens of producers publish user interaction events (clicks, scrolls) to a bounded global queue. Implement per-user FIFO buffers and a…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nDesigning a multi-tenant scheduler that preserves per-tenant ordering while adapting quotas to burstiness tests deep knowledge of backpressure, fairness, and liveness. It also challenges how to measure and guarantee fairness across tenants in a shared resource.\n\n## Key Concepts\n\n- Per-tenant FIFO queues\n- Global bounded backlog with backpressure signaling\n- Dynamic quotas via token buckets adjusted by observed burstiness\n- Scheduling policy (weighted RR) to prevent starvation\n- Observability: per-tenant throughput, latency, and starvation windows\n\n## Code Example\n\n```javascript\n// Skeleton: per-tenant queues and central dispatcher\nclass TokenBucket {\n  constructor(rate, burst){ this.rate = rate; this.burst = burst; this.tokens = burst; this.last = Date.now(); }\n  tryConsume(n=1){ const now = Date.now(); const elapsed = (now - this.last)/1000; this.tokens = Math.min(this.burst, this.tokens + elapsed*this.rate); this.last = now; if(this.tokens >= n){ this.tokens -= n; return true; } return false; }\n}\n\nclass TenantQueue {\n  constructor(id){ this.id = id; this.q = []; }\n  enqueue(item){ this.q.push(item); }\n  dequeue(){ return this.q.shift(); }\n}\n\n// Dispatcher sketch (async flow-control omitted for brevity)\n```\n\n## Follow-up Questions\n\n- How would you validate fairness under bursty workloads and detect starvation?\n- How would you scale this across processes or machines with consistent quotas?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-10",
        "questionId": "q-15442",
        "question": "Design and implement a per-topic bounded dispatcher for an IoT telemetry hub: thousands of devices publish events with a topic. Build a global bounded backlog, per-topic FIFO buffers, and a fixed worker pool that drains queues in round-robin fairness. Include dynamic topic creation, backpressure signaling, cancellation, and a graceful shutdown. Provide a runnable minimal example in Go or Python showing enqueue, dispatch, and shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a bounded global backlog with per-topic FIFO buffers and a round-robin worker dispatcher. Producers enqueue into a shared bounded channel; a central dispatcher forwards items to per-topic queues, which are dra…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a bounded global queue feeding per-topic FIFO queues. Producers tag events with topic IDs and block when full. A dispatcher assigns work to workers while preserving per-topic order, using a top",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A minimal Go or Python asyncio solution: implement a bounded queue with per-producer buffers; on enqueue, if capacity reached, stall new items until space frees up. Ensure workers dequeue in order of",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a bounded global queue (size Q) and per-room FIFO queues. A router assigns incoming messages to the corresponding room buffer; a small worker pool pulls from room buffers in round-robin, preservin",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis tests understanding of per-topic ordering, backpressure mechanisms, dynamic topic management, and safe shutdown in a concurrent dispatcher system.\n\n## Key Concepts\n- **Bounded backlog and backpressure**: Prevent memory exhaustion by limiting queue depth and signaling producers when buffers are full\n- **Per-topic FIFO with ordering guarantees**: Ensure events within each topic maintain publication order\n- **Fair scheduling (round-robin)**: Prevent a high-volume topic from starving others\n- **Dynamic topic management**: Support on-the-fly topic creation as new device types appear\n- **Cancellation and graceful shutdown**: Safely drain pending work without data loss\n\n## Code Example\n```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"sync\"\n\t\"time\"\n)\n\ntype Event struct {\n\tTopic   string\n\tPayload string\n}\n\ntype TopicQueue struct {\n\tch     chan Event\n\tmu     sync.Mutex\n\tclosed bool\n}\n\ntype Dispatcher struct {\n\tglobalBacklog chan Event\n\ttopicQueues  map[string]*TopicQueue\n\tworkers      int\n\twg           sync.WaitGroup\n\tmu           sync.RWMutex\n\tctx          context.Context\n\tcancel       context.CancelFunc\n}\n\nfunc NewDispatcher(backlogSize, workers int) *Dispatcher {\n\tctx, cancel := context.WithCancel(context.Background())\n\td := &Dispatcher{\n\t\tglobalBacklog: make(chan Event, backlogSize),\n\t\ttopicQueues:  make(map[string]*TopicQueue),\n\t\tworkers:      workers,\n\t\tctx:          ctx,\n\t\tcancel:       cancel,\n\t}\n\treturn d\n}\n\nfunc (d *Dispatcher) getOrCreateTopic(topic string) *TopicQueue {\n\td.mu.Lock()\n\tdefer d.mu.Unlock()\n\tif q, ok := d.topicQueues[topic]; ok {\n\t\treturn q\n\t}\n\tq := &TopicQueue{ch: make(chan Event, 100)}\n\td.topicQueues[topic] = q\n\treturn q\n}\n\nfunc (d *Dispatcher) Enqueue(e Event) error {\n\tselect {\n\tcase d.globalBacklog <- e:\n\t\treturn nil\n\tdefault:\n\t\treturn fmt.Errorf(\"backpressure: global backlog full\")\n\t}\n}\n\nfunc (d *Dispatcher) Start() {\n\tfor i := 0; i < d.workers; i++ {\n\t\td.wg.Add(1)\n\t\tgo d.worker(i)\n\t}\n\tgo d.dispatcher()\n}\n\nfunc (d *Dispatcher) dispatcher() {\n\tfor {\n\t\tselect {\n\t\tcase <-d.ctx.Done():\n\t\t\treturn\n\t\tcase e := <-d.globalBacklog:\n\t\t\tq := d.getOrCreateTopic(e.Topic)\n\t\t\tselect {\n\t\t\tcase q.ch <- e:\n\t\t\tdefault:\n\t\t\t\tfmt.Printf(\"backpressure: topic %s full\\n\", e.Topic)\n\t\t\t}\n\t\t}\n\t}\n}\n\nfunc (d *Dispatcher) worker(id int) {\n\tdefer d.wg.Done()\n\tfor {\n\t\tselect {\n\t\tcase <-d.ctx.Done():\n\t\t\treturn\n\t\tdefault:\n\t\t}\n\t\td.mu.RLock()\n\t\ttopics := make([]string, 0, len(d.topicQueues))\n\t\tfor t := range d.topicQueues {\n\t\t\ttopics = append(topics, t)\n\t\t}\n\t\td.mu.RUnlock()\n\t\tif len(topics) == 0 {\n\t\t\ttime.Sleep(time.Millisecond)\n\t\t\tcontinue\n\t\t}\n\t\tfor _, topic := range topics {\n\t\t\td.mu.RLock()\n\t\t\tq, ok := d.topicQueues[topic]\n\t\t\td.mu.RUnlock()\n\t\t\tif !ok {\n\t\t\t\tcontinue\n\t\t\t}\n\t\t\tselect {\n\t\t\tcase e := <-q.ch:\n\t\t\t\tfmt.Printf(\"worker %d processed: %s -> %s\\n\", id, e.Topic, e.Payload)\n\t\t\tdefault:\n\t\t\t}\n\t\t}\n\t}\n}\n\nfunc (d *Dispatcher) Shutdown() {\n\td.cancel()\n\td.wg.Wait()\n\td.mu.Lock()\n\tdefer d.mu.Unlock()\n\tfor _, q := range d.topicQueues {\n\t\tclose(q.ch)\n\t}\n}\n\nfunc main() {\n\td := NewDispatcher(1000, 3)\n\td.Start()\n\n\td.Enqueue(Event{Topic: \"temperature\", Payload: \"25.5\"})\n\td.Enqueue(Event{Topic: \"humidity\", Payload: \"60%\"})\n\td.Enqueue(Event{Topic: \"temperature\", Payload: \"26.0\"})\n\n\ttime.Sleep(100 * time.Millisecond)\n\td.Shutdown()\n\tfmt.Println(\"shutdown complete\")\n}\n```\n\n## Follow-up Questions\n- How would you detect and recover from a stuck topic queue without starving others?\n- How would you extend this to multi-process coordination with exactly-once semantics?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-11",
        "questionId": "q-28474",
        "question": "Design a live multiplayer game server event processor: for each room_id, events must be processed in strict per-room order, while rooms run in parallel. Build a bounded, real-time dispatcher with per-room FIFO queues, a global backpressure limit, and an LRU spill store for overflow. Include late-message reordering using per-room sequence numbers, cancellation, and graceful shutdown. Provide a runnable sketch in Go or Python?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose per-tenant FIFO queues feeding a global scheduler built on virtual time. Each tenant gets a fixed quantum; the scheduler advances to the head task of the tenant with the smallest virtual time",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a bounded queue + per‑stream priority scheduler. Use a global maxsize with producers awaiting when full; a small worker pool drains by always preferring high, then medium, then low; implement",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Maintain per-room FIFO queues and a fixed-size global dispatcher. A central sequencer assigns tasks to workers ensuring per-room order by (room_id, seq). Global backpressure uses a bounded queue; over",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a bounded global queue (size Q) and per-room FIFO queues. A router assigns incoming messages to the corresponding room buffer; a small worker pool pulls from room buffers in round-robin, preservin",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAdvanced per-room ordering with dynamic concurrency, bounded resources, spillover handling, and graceful shutdown in a real-time game server. \n\n## Key Concepts\n- Per-room FIFO isolation with a global bounded dispatcher\n- Backpressure and an LRU spill store for overflow\n- Late-message reordering using per-room sequence numbers\n- Graceful shutdown and cancellation\n\n## Code Sketch\n```go\n// high-level sketch: dispatcher, per-room queues, sequencer, spill store\n```\n\n## Follow-ups\n- How to test strict per-room ordering under burst\n- How to simulate node failure and recovery",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-12",
        "questionId": "q-13228",
        "question": "Design an intermediate concurrency problem: implement a dynamic, lock-free bounded multi-producer multi-consumer scheduler for a streaming analytics pipeline. Each input event has stream_id and event_id. Guarantee per-stream in-order processing across a worker pool that can resize at runtime. Use per-stream bounded queues, a global balancer, and a backpressure signal when thresholds are hit. Provide a runnable sketch and concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-user rolling window guarded by a mutex with an in-memory map, plus a periodic sweeper to drop old events and emit counts. Alternatively funnel events through a single worker with a bounded c",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-tier queue: each tenant has a bounded local queue (e.g., 4 in-flight). A global semaphore caps total in-flight jobs (e.g., 16). Producers enqueue to their tenant queue; a scheduler dispatches when capacity permits,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two bounded queues (CPU-bound, IO-bound) fed from a single dispatcher; each job has a unique id and is enqueued only once. Two worker pools pull from their queues. When both queues have work, apply a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: Implement per-stream FIFO ring buffers with configurable capacity (K) using stream_id sharding. A lock-free dispatcher distributes streams to workers via atomic steering tables; idle workers perform work steal…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests mastery of lock-free data structures, per-key ordering guarantees, and dynamic resizing under backpressure in realistic streaming scenarios.\n\n## Key Concepts\n- Lock-free MPMC queues\n- Per-stream ordering guarantees\n- Dynamic worker pool resizing\n- Backpressure signaling and spill-over mechanisms\n\n## Code Example\n```javascript\n// Pseudo-sketch: per-stream ring buffers, atomic counters, steering logic\n```\n\n## Follow-up Questions\n- How do you prevent livelock during extreme traffic bursts?\n- How would you extend this design for cross-node coordination and failure recovery?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-13",
        "questionId": "q-4482",
        "question": "In a real-time task processing system, multiple producers enqueue jobs into a global bounded queue with capacity C. A fixed pool of workers consumes jobs. Extend a beginner concurrency pattern to support two priority levels: high and normal. High-priority jobs must be served before normal ones, while preserving FIFO within each priority. Producers should block when the queue is full. Implement backpressure signaling and outline a concrete test plan to verify ordering and backpressure?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use two bounded queues (High and Normal) sharing capacity C. Enqueue blocks via a counting semaphore representing total items. A single dispatcher drains High first, then Normal, preserving FIFO withi",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Describe a per-DAG, per-stage bounded queueing with path-specific sequencing. Each task carries path_id and sequence; a node forwards a task only after all its parents for that path have completed. Us",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-key in-flight serialization with a map of keys to queues; a bounded global queue blocks producers when full; dynamic resizing via atomic counters and worker tickets; ensure in-order guarante",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant FIFO queues and a shared bounded queue. Each tenant has a token-bucket budget to permit bursts; enqueue blocks when budget is exhausted to apply backpressure. Dispatcher uses a mi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical handling of multiple producers with prioritized fairness and backpressure in a simple, implementable pattern.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Priority with FIFO guarantees\n- Semaphore-based flow control\n- Testing ordering and deadlock avoidance\n\n## Code Example\n```javascript\n// Pseudo-code illustrating two queues with a dispatcher\nclass QueuePair {\n  constructor(capacity) { /* ... */ }\n  enqueueHigh(item) { /* block if full */ }\n  enqueueNormal(item) { /* block if full */ }\n  dispatch() { /* drain High then Normal */ }\n}\n```\n\n## Follow-up Questions\n- How would you adapt this for dynamic worker scaling?\n- How would you detect and recover from dispatcher starvation?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-14",
        "questionId": "q-11673",
        "question": "Design a cross-region streaming engine with strict per-key ordering and failover. Producers in Region A publish keyed events to a global topic; Region B can become primary if Region A fails. Implement per-key FIFO buffers per region, region-aware routing, and a cross-region checkpointing protocol that guarantees monotonic per-key delivery within a region and consistent handover to the new primary. Include a runnable enqueue/consume sketch and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-key sequence numbers and per-region FIFO buffers. Producers publish to a global shard keyed by hash(key); router prefers the current primary region but can failover. A cross-region checkpoint",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a consistent-hash ring mapping tenant keys to a fixed worker, with a bounded global inflight budget. Each worker maintains per-key FIFO queues and a per-key sequence number. On migration, hand off",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a fixed-size Go channel as the global queue, a mutex-protected map[string]bool for in-flight tracking, and per-ID coalescing buffers. On enqueue: if the ID is already in-flight, merge the payload with the exis…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant subqueues and a central arbiter with a credit/aging scheme. Enqueue grants a tenant credit; the scheduler selects the tenant by a best (credit, age) ratio to limit latency and avoid sta",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates cross-region consistency, failover safety, and backpressure handling in a realistic streaming system.\n\n## Key Concepts\n- Per-key FIFO ordering across regions\n- Cross-region checkpointing and handoff\n- Region-aware routing and deduplication\n- Backpressure signaling and graceful degradation\n\n## Code Example\n```javascript\n// Sketch: enqueue with per-key seq, region-aware route\nfunction enqueue(key, value){ /* ... */ }\nfunction consume(){ /* ... */ }\n```\n\n## Follow-up Questions\n- How would you verify correctness under intermittent region outages?\n- What observability hooks would you add to diagnose drops or reordering?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-15",
        "questionId": "q-7624",
        "question": "Design a beginner-friendly concurrency pattern for a feature flag evaluation service: many microservices request flag values by key; a bounded global queue feeds a small worker pool that fetches from a remote flag store on a cache miss. Implement per-key request coalescing so concurrent requests for the same key trigger a single remote fetch; ensure fairness, backpressure, and a practical test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-session ordered processor with a global bounded queue and per-session FIFOs. Two enrichment paths: a fast path for low-latency services and a spill-backed path for slower ones. Enforce",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a publish-subscribe event bus with a bounded global queue, topic-tagged messages, and a pool of broadcaster workers. Preserve per-topic FIFO in subscriber queues, enforce cross-topic fairness,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route by shard to per-shard fixed-size ring buffers; each shard has a single in-order consumer. Producers hash the shard key and enqueue; if any buffer is full, block until space frees (backpressure).",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-key coalescing (singleflight). Maintain a map from key to an in-flight future. On a cache miss, if no in-flight for that key, start a remote fetch and store its future; otherwise await the exi",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical concurrency patterns: dedup across concurrent requests, bounded queues, and cache integration in a real-world service.\n\n## Key Concepts\n\n- Per-key deduplication (singleflight)\n- Bounded queues and backpressure\n- Cache coherence and eviction\n- Failure handling and retries\n\n## Code Example\n\n```javascript\nconst inflight = new Map();\nasync function fetchFlag(key){\n  if (inflight.has(key)) return inflight.get(key);\n  const p = remoteFetch(key).finally(()=>inflight.delete(key));\n  inflight.set(key,p);\n  return p;\n}\n```\n\n## Follow-up Questions\n\n- How would you handle remote fetch failures?\n- How would you extend to support priority keys or TTL-based eviction?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-16",
        "questionId": "q-8993",
        "question": "Design a beginner-friendly concurrency controller for a service that calls three external APIs, each with its own rate limit. Many producers submit API-call tasks labeled with apiId; implement a bounded global queue and per-api token buckets. A distributor assigns tasks to a small worker pool only when a token is available. Include backpressure, fairness across APIs, and a concrete test plan for rate-limit adherence and retry behavior?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design uses a central dispatcher hashing on user_id to route events into per-user queues stored in a durable, distributed log (e.g., Redis Streams). Each user has a single ordered queue; a fixed worke",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a bounded global queue and per-api token buckets. Before dispatch, a worker must acquire a token from that API’s bucket (refill rate equals the API limit). If tokens are exhausted, producers backp",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Proposed approach: assign each sensor its own bounded FIFO; a dispatcher drains into a global bounded queue consumed by a small worker pool. Enforce per-sensor order by never reordering items within a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use N per-sensor queues feeding a bounded central queue; workers pop from central queue and emit in increasing timestamp order. Implement a late-data path by routing packets with timestamp older than",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of rate-limiting, backpressure, and fair scheduling across multiple external API endpoints, plus handling retries in a simple, beginner-friendly way.\n\n## Key Concepts\n- Bounded queues\n- Per-API token bucket rate limiter\n- Backpressure and fairness\n- Retry with exponential backoff\n\n## Code Example\n```javascript\n// Pseudo: token buckets and distributor sketch\n```\n\n## Follow-up Questions\n- How would you test bursty traffic across APIs and ensure no API starves?\n- How would you adapt for dynamic API limits or flaky endpoints?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-17",
        "questionId": "q-15742",
        "question": "Design a concurrent, bounded event reordering service for real-time telemetry. Ingest events from many producers; each event has a timestamp. Ensure downstream sees events in non-decreasing timestamp order within a configurable lateness window. Implement a bounded min-heap reorder buffer, a worker pool that drains by timestamp, and backpressure when the buffer is full. Include cancellation and fault-recovery plan; provide a runnable minimal Go example showing enqueue, reorder, and flush?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt per-user FIFO queues backed by Redis Streams, with a dispatcher that assigns events to a fixed-size worker pool. Consumers read in-order per stream (XREADGROUP) to preserve user sequencing. Glob",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a dynamic DAG scheduler with per component topological ordering. Each task stores in-degree and a dependency bitset. Ready tasks go to bounded per-subgraph queues; workers pull, execute, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a mutex to protect shared state (head, tail, count) and two condition variables: not_full for producers to wait when the buffer is full, and not_empty for consumers to wait when empty. During shutdown, signal all wa…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a bounded reorder buffer using a min-heap keyed by event.TS. Ingested events are enqueued; producers block when buffer hits capacity to apply backpressure. A worker pool drains and emits onl",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nOut-of-order telemetry is common in distributed systems. This tests ability to design low-latency, memory-bounded reordering with backpressure, correctness under jitter, and fault handling.\n\n## Key Concepts\n\n- Bounded reorder buffer using a min-heap\n- Non-decreasing effective output with lateness window\n- Backpressure to producers\n- Cancellation and fault recovery\n- Test strategy for ordering, backpressure, and resilience\n\n## Code Example\n\n```go\npackage main\n\nimport (\n  \"container/heap\"\n  \"time\"\n)\n\ntype Event struct {\n  TS      time.Time\n  Payload []byte\n}\n\ntype MinHeap []Event\n\nfunc (h MinHeap) Len() int            { return len(h) }\nfunc (h MinHeap) Less(i, j int) bool  { return h[i].TS.Before(h[j].TS) }\nfunc (h MinHeap) Swap(i, j int)       { h[i], h[j] = h[j], h[i] }\nfunc (h *MinHeap) Push(x interface{}) { *h = append(*h, x.(Event)) }\nfunc (h *MinHeap) Pop() interface{} {\n  old := *h\n  n := len(old)\n  x := old[n-1]\n  *h = old[:n-1]\n  return x\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt this for multiple sinks with exactly-once semantics?\n- How would you instrument and test memory usage under burst load?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-18",
        "questionId": "q-10261",
        "question": "Design a cross-region streaming pipeline with per-key in-order guarantees across regions. Propose concrete data structures and a backpressure scheme (bounded global queue, per-region credits, dynamic worker resizing) and detail late-message handling, test plan, and failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a bounded global backlog with per-topic FIFO buffers and a round-robin worker dispatcher. Producers enqueue into a shared bounded channel; a central dispatcher forwards items to per-topic queues, which are dra…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a 3-stage bounded pipeline with per-tenant in-order guarantees using per-tenant sequence numbers and per-tenant buffers across stages. A single global bounded queue enforces backpressure while fair scheduling…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a two-tier model: a global bounded queue to backpressure producers and per-instrument FIFO queues consumed by a small fixed worker pool. Route each trade to its instrument queue, guarded by a mono",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-key FIFO buffers coordinated by a global sequencer to maintain strict ordering across regions. Employ a bounded global queue with capacity Q and per-region credit allocation; producers block when credits a…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates expertise in designing low-latency, globally distributed streaming pipelines with strict ordering guarantees and sophisticated backpressure mechanisms.\n\n## Key Concepts\n- Per-key ordering preservation across geographic regions\n- Credit-based cross-region backpressure implementation\n- Dynamic worker scaling with work stealing\n- Late-message handling and fault tolerance strategies\n\n## Code Example\n```javascript\n// Core interfaces for streaming pipeline components\ntype Message = { key: string; value: any; region: string; arrival: number; };\n```\n\n## Follow-up Questions",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-19",
        "questionId": "q-6884",
        "question": "In a real-time telemetry pipeline for multi-region IoT devices, design a bounded, region-scoped concurrency model that preserves per-region event order while capping memory usage. Describe data structures, backpressure signaling, and region lifecycle, and provide a runnable prototype outline in Go or Python showing enqueue, per-region dispatch, backpressure, and graceful shutdown. What tests would you add to verify ordering and backpressure?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a sensor-fusion scheduler with per-sensor buffers and a global bounded queue. A worker pool merges the latest readings within a 100ms window to produce fused outputs. When backlog grows, dro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-stream bounded queues and a fixed worker pool. Producers enqueue into their stream's queue; if full, apply backpressure by blocking or signaling upstream. Use per-stream sequence numbers and a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant circular buffers and a sequencer to preserve per-tenant order. Maintain a global token bucket for backpressure; route to workers with a stable hash of tenant_id; lock-free structures (e",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement region-scoped FIFO queues with a global bounded semaphore for memory control. Each region maintains its own ordered queue; a fixed worker pool drains region queues in round-robin fashion to preserve per-region…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-region concurrency, backpressure propagation, and dynamic lifecycle management in realistic telemetry pipelines.\n\n## Key Concepts\n- Region-scoped FIFO queues maintaining strict ordering\n- Global bounded capacity with per-region quotas\n- Fairness across regions via round-robin draining\n- Backpressure signaling with exponential backoff\n- Idle-region retirement and automatic reactivation\n\n## Code Example\n```javascript\n// sketch: region queue management and worker coordination\n```\n\n## Follow-up Questions\n- How would you measure end-to-end latency under burst conditions?\n- What monitoring metrics would you track for system health?\n- How would you handle region-specific priority requirements?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-20",
        "questionId": "q-24819",
        "question": "Design a bounded, multi-source stock feed processor: N producers emit Trade events {symbol, ts, seq}, into a central bounded queue. M workers apply trades to per-symbol state; global ordering must be by ts (not per symbol), and we must preserve per-symbol order for downstream outputs. Implement a mechanism to replay out-of-order trades when late events arrive, with a configurable canary window W. Support dynamic worker resizing and a backpressure policy (stall producers or drop oldest to make room). Provide data structures and a runnable sketch and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-feed asyncio.Queues with maxsize, a global bounded queue, and a small worker pool. Track per-feed sequence numbers to preserve order within a sliding window. Propagate backpressure via awaitin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a bounded reorder buffer using a global min-heap keyed by ts for ready Trade events, plus per-symbol FIFO queues to ensure in-symbol order. Use a watermark that advances as the earliest unpr",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I would implement a bounded, lock-free global ring buffer (size N) with per-client credit windows. Producers enqueue only if credits exist; else backpressure signals wait. Each client has a FIFO buffe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a bounded, multi-tenant scheduler with per-tenant FIFO queues feeding a global ready-queue. Enforce per-tenant order while using a fairness policy (weighted max-min) to pick tenants for the next task dispatch. Su…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of multi-source concurrency with global time-ordering, backpressure, and dynamic workers. It requires combining per-key ordering with global sequencing and late-event handling.\n\n## Key Concepts\n- Global time-ordering via ts and a watermark\n- Bounded central queue with per-symbol buffers\n- Reordering window and late-event replay\n- Dynamic worker resizing and backpressure policies\n- Testing with synthetic traces (late/out-of-order bursts)\n\n## Code Example\n```javascript\nclass Trade { constructor(symbol, ts, seq, payload) { this.symbol = symbol; this.ts = ts; this.seq = seq; this.payload = payload; } }\nclass ReorderBuffer {\n  constructor(limit) {\n    this.limit = limit; // bound\n    // min-heap by ts\n    this.heap = new MinHeap((a,b)=> a.ts-b.ts);\n    this.perSymbol = new Map();\n    this.watermark = -Infinity;\n  }\n  push(trade) { /* ... */ }\n  emitReady(upTo) { /* ... */ }\n}\n```\n\n## Follow-up Questions\n- How would you test late events and tie-breakers?\n- How would you adjust window size under bursty feeds?\n- How would you measure and enforce per-trade latency and ordering guarantees?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-21",
        "questionId": "q-26906",
        "question": "Design a dynamic DAG based task scheduler for a real time data pipeline. The graph can grow at runtime with new tasks and dependencies; tasks must execute only after all prerequisites in their subgraph complete, preserving per component topological order. The system should bound memory, apply backpressure to producers, support fast path cancellation, and allow graceful shutdown. Provide runnable sketch in Rust or Go and outline a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A beginner-friendly concurrency router for media encoding: producers publish jobs tagged by format (mp4, webm, gif) into a bounded global queue. A per-format dispatcher feeds dedicated worker pools wi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a dynamic DAG scheduler with per component topological ordering. Each task stores in-degree and a dependency bitset. Ready tasks go to bounded per-subgraph queues; workers pull, execute, and",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a central broker with a bounded global queue and per-customer FIFO queues. Producers enqueue ride requests to the global queue; a dispatcher assigns tasks to the corresponding per-customer q",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a bounded global queue with per-plugin queues, and a worker pool consuming from all plugin queues in a round-robin or priority-aware fashion. Enforce backpressure by blocking producers when full;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-time DAG scheduling with runtime graph growth tests deep understanding of ordering guarantees, backpressure, and shutdown semantics in concurrent systems.\n\n## Key Concepts\n- Dynamic graph with per-component topological order\n- Bounded memory, per-subgraph ready queues\n- Backpressure to producers via credits\n- Fast-path cancellation and graceful shutdown\n\n## Code Example\n```rust\n// Placeholder: sketch showing core structures (Task, InDegree, ReadyQueue) and enqueue/dequeue paths\n```\n\n## Follow-up Questions\n- How would you ensure exactly-once semantics for task results?\n- How would you test for starvation and deadlock under high churn?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-22",
        "questionId": "q-22206",
        "question": "You manage a multi-tenant analytics ingest service with a shared worker pool (8 workers) and a global bounded queue (max 1024). Build a fairness-aware scheduler that guarantees per-tenant latency <= 300ms during bursts (up to 100 tasks per tenant) and prevents starvation. Implement a credit-based aging policy with per-tenant FIFO and backpressure when the queue fills. Provide a runnable Go sketch using channels and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant subqueues and a central arbiter with a credit/aging scheme. Enqueue grants a tenant credit; the scheduler selects the tenant by a best (credit, age) ratio to limit latency and avoid sta",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use per-user queues (FIFO) fed into a bounded global worker pool. Each user channel feeds a non-blocking writer to a bounded arbiter that enforces global order, with a semaphore-based backpressure. Ga",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I'd implement with Python asyncio. Use a bounded global queue (maxsize 1024) plus per-device FIFO queues. A central arbiter drains the global queue and forwards events to device queues in arrival orde",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An ideal answer demonstrates a bounded queue plus a token bucket. A mutex guards an int tokens and a CV; a ticker periodically adds tokens (capped). Each producer blocks until tokens>0, then decrement",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExplores advanced fairness and backpressure in a bounded, multi-tenant pipeline. A realistic system must bound per-tenant latency under bursts while preventing starvation, requiring careful scheduling and observability.\n\n## Key Concepts\n\n- Per-tenant FIFO within a bounded global queue\n- Credit-based aging to enforce latency bounds while preventing starvation\n- Backpressure semantics when queue is full\n- Race-free dispatcher and worker coordination in Go or Python asyncio\n- Test plan: latency under burst, fairness across tenants, shutdown behavior\n\n## Code Example\n\n```go\npackage main\n\nimport (\n  \"sync\"\n  \"time\"\n)\n\ntype Task struct{ Tenant string; Run func() }\n\ntype Scheduler struct {\n  global chan Task\n  perTenant map[string]chan Task\n  mu sync.Mutex\n  // simplified: in production, add priority queue, aging metrics, and a watchdog\n}\n\nfunc main() {\n  // sketch: create scheduler, 8 workers, dispatch loop, aging ticker\n  // real implementation would handle enqueue, backpressure, and per-tenant fairness\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt this for dynamic worker counts or heterogeneous task costs?\n- What metrics and tracing would you add to verify latency guarantees in production?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-23",
        "questionId": "q-28078",
        "question": "Design a beginner-friendly concurrency exercise: a multi-source index updater for a search service. Dozens of producers emit document-change events, each tagged by shard ID. Each shard’s events must be applied in arrival order, while updates across shards can be processed in parallel. Implement per-shard FIFO buffers feeding a bounded global worker queue that writes to a single in-memory index. When backlog crosses a threshold, degrade by coalescing updates per shard to the latest change within a time window and dropping non-critical fields. Explain backpressure, per-shard ordering, and a concrete test plan for correctness, degradation behavior, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-session ordered processor with a global bounded queue and per-session FIFOs. Two enrichment paths: a fast path for low-latency services and a spill-backed path for slower ones. Enforce",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-room FIFO buffers feeding a bounded global worker queue to preserve per-room ordering while allowing parallelism across rooms. Add a backlog-based degradation path that coalesces rapid typing events per ro…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: implement N per-source FIFO buffers feeding a single bounded queue; a dispatcher with M workers runs a per-source fairness scheduler (weighted fair queuing) to select events and route them to si",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-shard FIFO buffers feed a bounded global queue consumed by a small worker pool. A dedicated writer ensures per-shard order, while cross-shard work runs in parallel. Backpressure arises from the bo",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical concurrency in a real-world component: multiple producers, per-source ordering, bounded queues, and degradation under load. It also probes test planning and fault tolerance.\n\n## Key Concepts\n- Per-source FIFO buffers and a bounded global queue\n- Single writer per shard to preserve ordering; parallel across shards\n- Backpressure and degradation via update coalescing\n\n## Code Example\n```javascript\n// Pseudo outline: perShardBuffers, globalQueue, workers, degradationPolicy\n```\n\n## Follow-up Questions\n- How would you extend to multiple index writers while preserving ordering guarantees?\n- How do you ensure idempotence during degradation?\n- What metrics would you collect to monitor backlog and degradation triggers?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-24",
        "questionId": "q-3453",
        "question": "Design a beginner-friendly concurrency setup in Go for a real-time notification service where many producers emit payloads that must be delivered to users in per-user order. Implement a global bounded queue and per-user FIFO channels so that each user's messages are processed in order, while a small worker pool handles delivery. Explain how you enforce backpressure, avoid starvation, and test ordering and race conditions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a global buffered channel as backpressure, and maintain a per-user FIFO channel to guarantee order. A single dispatcher reads from the global queue and forwards each payload to the user’s dedicate",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Approach: per-instrument FIFO buffers, global bounded queue, worker pool updates price cache and notifies subscribers. Degradation: when backlog > threshold, keep only the latest tick per instrument,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a two-stage pipeline with per-user ordering and dynamic rebalancing. Map user_id to a worker using a consistent-hash ring; when load rises, rebalance a subset of users to less-busy workers whi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-cell bounded FIFOs and a dispatcher using consistent hashing route events to workers. Each worker keeps a per-cell last_seq and only processes a message if its seq is last_seq+1; gaps are buffered",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical micro-concurrency patterns, including backpressure, per-key ordering, and fairness—common in large-scale services.\n\n## Key Concepts\n- Bounded queues\n- Per-key sequencing\n- Dispatcher + worker pool\n- Fairness and deadlock avoidance\n\n## Code Example\n```go\n// skeleton illustrating structures and flow (omitted for brevity)\n```\n\n## Follow-up Questions\n- How would you scale the worker pool if the per-user queues grow unbounded? \n- How would you test for starvation under skewed load?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-04T05:15:21.205Z",
    "version": 1
  }
]