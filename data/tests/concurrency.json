[
  {
    "id": "test-concurrency",
    "channelId": "concurrency",
    "channelName": "Concurrency",
    "title": "Concurrency Knowledge Test",
    "description": "Test your understanding of Concurrency with 25 questions.",
    "questions": [
      {
        "id": "concurrency-q-0",
        "questionId": "q-6247",
        "question": "In a streaming analytics system, dozens of data shards emit aggregation tasks that must be processed within strict latency budgets. Design a bounded, hierarchical queue: a global bounded backlog that feeds per-shard FIFO queues, with a fixed worker pool. Enforce backpressure to producers when full, isolate shards to prevent starvation, and implement a fairness policy (e.g., weighted round-robin or tokens) to allocate processing time fairly across shards. Include a runnable minimal sketch in Rust or C++ showing enqueue and dequeue semantics and a basic test strategy to verify latency and ordering?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-tier scheduler: a global bounded queue feeding per-shard FIFO queues with a fixed worker pool. Producers backpressure when global is full. Scheduler uses weighted round-robin to allocate CPU fairl",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a mutex to protect shared state (head, tail, count) and two condition variables: not_full for producers to wait when the buffer is full, and not_empty for consumers to wait when empty. During shutdown, signal all wa…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a map of per-client buffered channels feeding a single bounded global channel. A ticker drives a 16ms game loop that drains each client buffer in round-robin fashion, updating game state accordingly. Implement backp…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a central dispatcher routing edits to per-user FIFOs backed by bounded buffers; map users to workers with consistent hashing; on rebalance transfer a user’s queue via an atomic hand-off token and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a two-level concurrency control that scales, maintains per-shard ordering, and exposes backpressure and fairness under bursty traffic.\n\n## Key Concepts\n\n- Bounded global queue with per‑shard FIFO queues\n- Backpressure strategies and producer throttling\n- Fair scheduling across shards (weighted RR or token buckets)\n- Graceful shutdown, error propagation, testability\n\n## Code Example\n\n```rust\nuse std::sync::mpsc::{sync_channel, SyncSender, Receiver};\nuse std::thread;\n\nfn main() {\n  // Global bounded queue: (shard_id, value)\n  let (global_tx, global_rx) = sync_channel::<(usize, i32)>(8);\n\n  // Per-shard queues\n  let (shard1_tx, shard1_rx) = sync_channel::<i32>(4);\n  let (shard2_tx, shard2_rx) = sync_channel::<i32>(4);\n\n  // Dispatcher: routes by shard\n  thread::spawn(move || {\n    while let Ok((sid, v)) = global_rx.recv() {\n      if sid == 1 { shard1_tx.send(v).unwrap(); }\n      else { shard2_tx.send(v).unwrap(); }\n    }\n  });\n\n  // Simple workers: pull from shard queues\n  let _w1 = thread::spawn(move || {\n    while let Ok(x) = shard1_rx.recv() { /* process x preserving shard1 order */ }\n  });\n  let _w2 = thread::spawn(move || {\n    while let Ok(x) = shard2_rx.recv() { /* process x preserving shard2 order */ }\n  });\n\n  // enqueue example\n  global_tx.send((1, 123)).unwrap();\n  global_tx.send((2, 456)).unwrap();\n\n  // graceful shutdown omitted for brevity\n}\n```\n\n## Follow-up Questions\n\n- How to extend to N shards and ensure global fairness without starving any shard?\n- How to measure tail latency and verify ordering guarantees under burst?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-1",
        "questionId": "q-3217",
        "question": "Design a per-session ordered event dispatcher for a real-time multiplayer game server. Each game session has its own FIFO queue; multiple producers can enqueue events across sessions, and a fixed-size pool of worker threads processes events. Requirements: (1) preserve strict per-session order; (2) allow cross-session work-stealing for load balancing; (3) enforce a global bounded queue with backpressure to producers; (4) support dynamic session creation/destruction with zero deadlock and graceful shutdown; (5) discuss memory reclamation and safety?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-symbol FIFO buffers of size K, a bounded global queue, and per-symbol sequence numbers ensure ordering across venues under backpressure. A pool of M stateless risk-check workers consumes from each per-symbol buffer,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a centralized DAG orchestrator with a globally increasing sequence per input, per-edge bounded queues, and per-node in-degree tracking. Each event carries a sequence and key; operators flush only",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage a bounded global queue feeding a fixed worker pool, with per-session FIFOs. Producers enqueue to the global queue; workers pull sessions, draining in-order. For load balancing, allow stealing",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a two-layer scheduler: per-tenant FIFO queues guarded by a token-bucket quota, and a central arbiter that assigns M workers via deficit-round-robin to preserve per-tenant order while ensuring f",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes the candidate's ability to design a concurrent, low-latency dispatcher that preserves per-session ordering while scaling across cores. It also exercises lifecycle management and safe shutdown.\n\n### Key Concepts\n- Bounded backpressure and global queues\n- Per-session FIFO with in-order processing\n- Work stealing across sessions without violating order\n- Dynamic session lifecycle and memory reclamation\n\n### Code Example\n```rust\n// Pseudo Rust sketch: session queues and stealing interface\nstruct SessionQueue { /* ... */ }\nimpl SessionQueue {\n  fn enqueue(&self, evt: Event);\n  fn dequeue(&self) -> Option<Event>;\n}\n```\n\n### Follow-up Questions\n- How would you extend to prioritize latency-sensitive events within a session?\n- How would you detect starvation and ensure fairness across sessions?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-2",
        "questionId": "q-11821",
        "question": "Scenario: a log-aggregation service accepts events from dozens of publishers. Build a beginner-friendly concurrency controller in Go or Python asyncio using a bounded global queue and per-publisher buffers to preserve per-publisher order. In normal mode drain FIFO buffers; when backlog crosses a threshold switch to a latency-first path that samples oldest events across buffers to reduce tail latency while preserving fairness. Include backpressure policy and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a global buffered channel as backpressure, and maintain a per-user FIFO channel to guarantee order. A single dispatcher reads from the global queue and forwards each payload to the user’s dedicate",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a bounded buffer API Put(item)/Get(). In Go, a buffered channel size N blocks producers when full and blocks consumers when empty. In Java, ArrayBlockingQueue<N> with put/take; Python: queue.Queue",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a bounded global queue with per-publisher buffers. In normal mode, drain per-publisher FIFOs in arrival order. When backlog exceeds a threshold, switch to a latency-first path that samples the",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a fixed 1024-slot circular buffer guarded by a ReentrantLock with two Conditions: notFull and notEmpty. Producers await notFull; consumers await notEmpty. Update head, tail, and count under lock;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of practical concurrency control, backpressure, and fairness in a multi-producer setting using familiar primitives. The dual-path degradation mirrors real systems that must prioritize latency under pressure.\n\n## Key Concepts\n- Bounded queues and per-source buffering\n- Scheduling policies: FIFO vs latency-first sampling\n- Backpressure strategies: blocking vs drop-tail\n- Graceful shutdown and correctness guarantees\n\n## Code Example\n```go\n// Pseudo-Go skeleton:\ntype Task struct{ id string; publisher string; ts int64 }\nvar globalCh = make(chan Task, 1024)\nvar buffers map[string]chan Task // per-publisher\n// dispatcher selects oldest across buffers when backlog high\n```\n\n## Follow-up Questions\n- How to measure tail latency in production? \n- How to tune threshold for mode switching? \n- How would you test starvation under adversarial publishers?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-3",
        "questionId": "q-17503",
        "question": "Design a concurrent CI scheduler for a monorepo. Tasks arrive from N developers and are grouped by module. Guarantee per-module in-order execution across a shared worker pool under bursts. Implement per-module bounded queues (size K), a global dispatcher, and dynamic worker scaling. Add a speculative path: when backlog exceeds a threshold, run speculative tests for later commits in that module and deduplicate results. Provide a runnable sketch and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a telemetry ingest with per-sensor FIFO buffers feeding a bounded global worker pool. Preserve arrival order within each sensor; process sensors in parallel. When backlog crosses a threshold",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-module bounded queues (size K) with a global dispatcher over a shared worker pool. Route all tasks for a given module through a per-module sequence to guarantee in-order execution across bursts; dispatch tokens…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose per-vehicle bounded FIFOs of size B and a shared dispatcher. Producers append to the vehicle queue; workers fetch the heads, maintaining per-vehicle order with per-vehicle sequence numbers. Ba",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-cell bounded FIFOs and a dispatcher using consistent hashing route events to workers. Each worker keeps a per-cell last_seq and only processes a message if its seq is last_seq+1; gaps are buffered",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question demonstrates practical concurrency control under bursts, multi-tenant fairness, and recovery with speculative execution paths.\n\n## Key Concepts\n- Per-module sequencing to preserve order\n- Bounded queues and backpressure\n- Speculative execution with result deduplication\n- Dynamic worker scaling\n\n## Code Example\n```javascript\n// Skeleton scheduler illustrating per-module queues and speculative path\nclass Scheduler { /* ... */ }\n```\n\n## Follow-up Questions\n- How would you detect and handle late tasks that arrive after their module has advanced?\n- How would you test correctness under mixed latency and failure scenarios?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-4",
        "questionId": "q-18390",
        "question": "In a real-time ingest pipeline for a multi-region service, N producers publish events with fields region_id and event_id into a bounded queue Q of size B. Build a 3-stage pipeline (ingest -> compute -> sink) with region-scoped ordering guarantees and a regional fairness scheduler. Support dynamic worker resizing per region and provide a concrete test plan that covers bursts, late events, and backpressure behavior?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: maintain per-key FIFO buffers and a global watermark. Stage1 writes keyed records to per-key queues; Stage2 consumes in-key order to produce windowed aggregates. Use bounded ring buffers for",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-client bounded FIFO buffers (size K) + a global scheduler that orders by arrival time and tracks per-client progress. Dispatch the oldest ready event to any worker across stages ingest→filter→enco",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a dynamic DAG scheduler with per component topological ordering. Each task stores in-degree and a dependency bitset. Ready tasks go to bounded per-subgraph queues; workers pull, execute, and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-region FIFO buffers plus a bounded global queue; enforce region-local monotonic counters to preserve in-order delivery across all three pipeline stages. Implement a weighted round-robin fairness scheduler that alloc…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests real-world concurrency understanding: preserving per-region ordering under backpressure, implementing fairness across regions, and enabling dynamic worker resizing without breaking guarantees.\n\n## Key Concepts\n\n- **Per-region ordering**: Monotonic counters ensure events within each region maintain FIFO order across all pipeline stages\n- **Bounded queues and backpressure**: Backpressure signaling prevents memory exhaustion when producers outpace consumers\n- **Regional fairness scheduler**: Weighted round-robin allocates processing slots proportionally to region weights\n- **Dynamic worker resizing**: Per-region worker pools can scale independently without violating ordering guarantees\n- **Late/out-of-order event handling**: Watermark-based windowing and retries for events arriving after their expected timestamp\n\n## Code Example\n\n```javascript\nclass RegionBuffer {\n  constructor(limit) {\n    this.queue = [];\n    this.limit = limit;\n    this.nextSeq = 0;\n  }\n\n  enqueue(event) {\n    if (this.queue.length >= this.limit) {\n      throw new BackpressureError('region buffer full');\n    }\n    event.seq = this.nextSeq++;\n    this.queue.push(event);\n    return event;\n  }\n\n  dequeue() {\n    return this.queue.shift();\n  }\n\n  isFull() {\n    return this.queue.length >= this.limit;\n  }\n}\n\nclass RegionalFairnessScheduler {\n  constructor(regions, weights) {\n    this.regionBuffers = new Map();\n    this.weights = weights;\n    this.counters = new Map();\n    this.currentIndex = 0;\n\n    regions.forEach(region => {\n      this.regionBuffers.set(region, new RegionBuffer(1000));\n      this.counters.set(region, 0);\n    });\n  }\n\n  schedule() {\n    const regions = Array.from(this.regionBuffers.keys());\n    const weight = this.weights[regions[this.currentIndex]] || 1;\n\n    if (this.counters.get(regions[this.currentIndex]) < weight) {\n      const buffer = this.regionBuffers.get(regions[this.currentIndex]);\n      if (buffer.queue.length > 0) {\n        this.counters.set(regions[this.currentIndex], \n          this.counters.get(regions[this.currentIndex]) + 1);\n        return buffer.dequeue();\n      }\n    }\n\n    this.currentIndex = (this.currentIndex + 1) % regions.length;\n    if (this.counters.get(regions[this.currentIndex]) >= weight) {\n      this.counters.set(regions[this.currentIndex], 0);\n    }\n    return null;\n  }\n}\n```\n\n## Test Plan\n\n### 1. Burst Handling Test\n- Send 10x normal load from a single region\n- Verify other regions still receive processing slots\n- Confirm no starvation occurs\n\n### 2. Late Events Test\n- Introduce events with out-of-sequence timestamps\n- Configure late-tolerance window (e.g., 5 seconds)\n- Verify late events are handled according to policy (drop/retry/reject)\n\n### 3. Backpressure Test\n- Fill regional buffers to capacity\n- Verify backpressure signal is emitted to producers\n- Confirm producers throttle correctly\n- Test backpressure release when buffer drains\n\n### 4. Dynamic Worker Resizing Test\n- Increase worker count mid-operation for a region\n- Verify ordering maintained through the transition\n- Decrease workers and confirm graceful degradation\n\n### 5. Recovery Test\n- Simulate region failure and recovery\n- Confirm ordering guarantees restore after recovery",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-5",
        "questionId": "q-3855",
        "question": "Design a streaming dataflow for real-time analytics that processes events into fixed 1-second windows. Implement per-window bounded queues, a dynamic worker pool, and a watermark-driven cancellation protocol: late events crossing the watermark cancel the window and abort pending tasks. Guarantee in-window output order and no deadlocks under backpressure. Explain testing strategies and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a router with per-client FIFO buffers fed into a bounded global queue, serviced by a small worker pool. Enforce ordering by dequeuing from the active client head, cycling for fairness. When ba",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a bounded global queue that feeds into separate per-type FIFO buffers. For types requiring system-wide in-order processing, enforce sequential processing within each type buffer and route to a shared worker po…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a bounded queue + per‑stream priority scheduler. Use a global maxsize with producers awaiting when full; a small worker pool drains by always preferring high, then medium, then low; implement",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a streaming dataflow with per-window bounded queues and a dynamic worker pool. Map events to fixed 1s windows; maintain a watermark. If a late event arrives past the watermark, cancel the corr",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nInterviewers seek practical experience with streaming systems, time-windowing, and cancellation under backpressure. A watermark-driven cancellation tests correctness under out-of-order data and ensures resources aren’t wasted on irrelevant windows.\n\n## Key Concepts\n\n- Watermarks and late data handling\n- Per-window bounded queues and dynamic worker pools\n- Cancellation propagation and deadlock avoidance\n- Testing strategies for timing, backpressure, and failure modes\n\n## Code Example\n\n```javascript\n// Skeleton windowing structure\nclass Window {\n  constructor(ts) {\n    this.ts = ts;\n    this.tasks = [];\n    this.cancelled = false;\n  }\n  addTask(t) {\n    if (this.cancelled) throw new Error('window cancelled');\n    this.tasks.push(t);\n  }\n  complete() {\n    // emit aggregated result\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you measure fairness across windows under bursty input?\n- How would you test cancellation propagation and deadlock scenarios?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-6",
        "questionId": "q-25321",
        "question": "Design a beginner-friendly concurrency exercise: a file-watcher system where multiple directories emit file-change events concurrently. Build per-directory FIFO buffers and a bounded global queue consumed by a small worker pool that updates a mock index. Add a coalescing mechanism: merge rapid successive edits for the same file within a 100ms window into a single update. Enforce backpressure when the global queue is full, preserve per-directory ordering, and provide a test plan for ordering, coalescing, backlog handling, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-client FIFO scheduler with a global bounded frontier and a small, per-node arbiter. Use per-client queues and a central scheduler that grants a fixed number of slots to non-empty queues i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-directory FIFO buffers and a bounded global queue; a small worker pool updates a mock index. Implement coalescing: for each file, merge rapid edits within 100ms into a single update before enq",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Architect a dispatcher that feeds per-merchant FIFO queues from a global bounded buffer with dynamic backpressure propagation. Each merchant maintains a sequence number and a durable replay log to enable deterministic r…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a streaming dataflow with per-window bounded queues and a dynamic worker pool. Map events to fixed 1s windows; maintain a watermark. If a late event arrives past the watermark, cancel the corr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical concurrency patterns that resemble real-world pipelines: per-source buffering, bounded queues, worker pools, event coalescing, and backpressure. It also emphasizes testability and graceful shutdown.\n\n## Key Concepts\n\n- Per-directory FIFO buffers\n- Global bounded queue\n- Backpressure strategies\n- Coalescing window and deduping\n- Ordering guarantees\n- Graceful shutdown\n\n## Code Example\n\n```javascript\n// Minimal skeleton illustrating structure\nclass BoundedQueue { constructor(limit){ this.limit=limit; this.q=[]; } push(x){ if(this.q.length>=this.limit) return false; this.q.push(x); return true; } pop(){ return this.q.shift(); } }\n```\n\n```javascript\n// Pseudo flow: on file event\nfunction onEvent(dir, file, evt){\n  // add to per-dir buffer\n  // schedule coalescing timer for file\n  // when timer fires, enqueue a single update for file\n}\n```\n\n## Follow-up Questions\n\n- How would you test ordering guarantees across many directories?  \n- How would you measure and tune the coalescing window?  \n- What backpressure strategies would you choose in a production-like environment?  \n- How would you implement graceful shutdown without losing in-flight work?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-7",
        "questionId": "q-811",
        "question": "In a real-time analytics service, multiple producers enqueue data into a fixed-size circular buffer consumed by multiple workers. Design a beginner-friendly concurrency solution in Java, Go, or Python that guarantees producers block when the buffer is full and consumers block when empty, while maintaining correctness under concurrent access; compare lock-based vs channel-based approaches for this bounded buffer?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded buffer API Put(item)/Get(). In Go, a buffered channel size N blocks producers when full and blocks consumers when empty. In Java, ArrayBlockingQueue<N> with put/take; Python: queue.Queue",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use per-device buffers keyed by device_id, each storing windows with running aggregates. Maintain a global watermark; when window_end <= watermark, flush and drop. Cap total buffered windows; on cap,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-user queues (FIFO) fed into a bounded global worker pool. Each user channel feeds a non-blocking writer to a bounded arbiter that enforces global order, with a semaphore-based backpressure. Ga",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a bounded global queue and per-api token buckets. Before dispatch, a worker must acquire a token from that API’s bucket (refill rate equals the API limit). If tokens are exhausted, producers backp",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nInterviewers assess practical concurrency intuition with bounded buffers, a common real-time pattern.\n\n## Key Concepts\n- Bounded buffers\n- Producer-consumer synchronization\n- Blocking semantics\n- Trade-offs: channels vs locks vs lock-free\n\n## Code Example\n```java\nArrayBlockingQueue<Integer> q = new ArrayBlockingQueue<>(N);\nq.put(item); // blocks if full\nint x = q.take(); // blocks if empty\n```\n\n## Follow-up Questions\n- How would you handle graceful shutdown?\n- How would you introduce fairness guarantees or timeouts?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-8",
        "questionId": "q-825",
        "question": "In a real-time chat moderation pipeline for a platform like Discord/LinkedIn, design a dynamic Go worker pool that scales from minWorkers to maxWorkers based on a bounded task queue. The queue should backpressure producers, guarantee per-user fairness, support graceful shutdown, and tolerate occasional misbehaving workers (timeouts). Provide runnable code showing enqueue, worker loop, and scaler?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded queue channel and a scalable worker pool. Start with minWorkers; a scaler grows workers when queue length crosses a high watermark and shrinks when it falls below a low watermark, bounde",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I’d implement per-device FIFO buffers feeding a bounded global queue with per-device token-bucket rate limiters. A fair scheduler preserves per-device order while enabling cross-device parallelism. On",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a tri-stage, bounded-buffer pipeline that preserves per-device order across stages, supports dynamic worker resizing, and applies a latency-based late-data policy (drop or mark). Include per-de",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe a per-DAG, per-stage bounded queueing with path-specific sequencing. Each task carries path_id and sequence; a node forwards a task only after all its parents for that path have completed. Us",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a scalable, backpressured worker pool for real-time workloads, with fairness, fault tolerance, and clean shutdown—exactly the kind of pattern seen in large chat platforms.\n\n## Key Concepts\n\n- Bounded queue with backpressure\n- Dynamic worker scaling (min/max bounds)\n- Fairness across users (distribution by UserID)\n- Graceful shutdown and worker timeout handling\n\n## Code Example\n\n```go\npackage main\n\nimport (\n  \"context\"\n  \"fmt\"\n  \"hash/fnv\"\n  \"sync\"\n  \"time\"\n)\n\ntype Task struct {\n  UserID  string\n  Payload string\n}\n\nfunc worker(ctx context.Context, id int, tasks <-chan Task, wg *sync.WaitGroup) {\n  defer wg.Done()\n  for {\n    select {\n    case t := <-tasks:\n      // simulate processing\n      _ = t\n      time.Sleep(5 * time.Millisecond)\n      fmt.Printf(\"worker %d processed %s\\n\", id, t.Payload)\n    case <-ctx.Done():\n      return\n    }\n  }\n}\n\nfunc main() {\n  minW, maxW := 2, 6\n  queueCap := 64\n  queue := make(chan Task, queueCap)\n  var wg sync.WaitGroup\n  var cancels []context.CancelFunc\n\n  // start min workers\n  for i := 0; i < minW; i++ {\n    ctx, cancel := context.WithCancel(context.Background())\n    cancels = append(cancels, cancel)\n    wg.Add(1)\n    go worker(ctx, i, queue, &wg)\n  }\n\n  // scaler\n  go func() {\n    ticker := time.NewTicker(100 * time.Millisecond)\n    defer ticker.Stop()\n    for range ticker.C {\n      l := len(queue)\n      if l > 32 && len(cancels) < maxW {\n        // scale up\n        ctx, cancel := context.WithCancel(context.Background())\n        cancels = append(cancels, cancel)\n        w := len(cancels) - 1\n        wg.Add(1)\n        go worker(ctx, w, queue, &wg)\n      } else if l < 8 && len(cancels) > minW {\n        // scale down\n        cancel := cancels[len(cancels)-1]\n        cancels = cancels[:len(cancels)-1]\n        cancel()\n      }\n    }\n  }()\n\n  // producer: enqueue tasks\n  for i := 0; i < 1000; i++ {\n    t := Task{UserID: fmt.Sprintf(\"user-%d\", i%20), Payload: fmt.Sprintf(\"payload-%d\", i)}\n    queue <- t // blocks when full (backpressure)\n  }\n\n  // graceful shutdown\n  time.Sleep(1 * time.Second)\n  for _, c := range cancels {\n    c()\n  }\n  close(queue)\n  wg.Wait()\n}\n```\n\n## Follow-up Questions\n\n- How would you test scaling behavior under bursty traffic and ensure latency SLOs are met?\n- How would you extend fairness for thousands of users without starving rare users?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-9",
        "questionId": "q-15149",
        "question": "Design a hot-key routing pipeline with a bounded global queue and two processing paths: normal and hot. Track per-key arrival rate with a sliding window; when a key crosses a threshold, route its events to the hot path with per-key buffers and sequence counters to preserve in-order across paths. Use backpressure signaling on full queues and a fairness policy between hot/cold keys, plus a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-client FIFO buffers feeding a bounded global queue, with a fixed worker pool processing tasks in arrival order per client. Enforce ordering by sequencing within each client and routing t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-user state with bounded FIFO buffers for A and B and a 60s watermark. Emit when both sides exist, preserving order by the latest timestamp. Drop/purge late records per policy to bound memory",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage a bounded global queue feeding a fixed worker pool, with per-session FIFOs. Producers enqueue to the global queue; workers pull sessions, draining in-order. For load balancing, allow stealing",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a two-path, bounded pipeline with per-key rate tracking via a sliding window; route spiraling keys to a hot path using per-key buffers and sequence numbers to guarantee in-order delivery acr",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests a realistic, nuanced concurrency pattern: dynamic routing by per-key load, two distinct worker pools, and cross-path in-order guarantees under backpressure.\n\n## Key Concepts\n- Hot-key routing and dual-path processing\n- Per-key FIFO ordering across multiple workers\n- Sliding-window rate tracking and dynamic routing\n- Backpressure signaling and fairness policies\n- Dynamic scaling and fault tolerance\n\n## Code Example\n```javascript\n// Pseudo-types and routing scaffolding\ntype Event = { key:string, value:any, ts:number }\n// Data structures would include per-key buffers for normal/hot paths and seq counters\n```\n\n## Follow-up Questions\n- How to prevent starvation if hot keys dominate?\n- How to test ordering guarantees under clock skew or bursty traffic?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-10",
        "questionId": "q-19371",
        "question": "Design a beginner-friendly concurrency task for a video conferencing signaling service: many clients send room-tagged control messages (mute/unmute, join/leave). A bounded global queue feeds a pool of dispatchers that apply messages to per-room state in FIFO order. Add a degradation path: drop non-critical messages (typing indicators) when backlog grows. Provide a concrete test plan for per-room ordering, backpressure, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a bounded global queue with per-room FIFO buffers and a small dispatcher pool; dispatchers apply messages to room state while preserving per-room order. Prioritize critical actions (mute/unm",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "An IoT telemetry collector for 200 devices. Each device's readings must be processed in device-order; readings from different devices may be parallel. Implement per-device FIFO buffers feeding a bound",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant FIFO lanes using fixed-capacity lock-free queues. Route events from the global queue to corresponding tenant lanes via deterministic partitioning on tenant_id. Assign tenant shards to each worker fo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-client FIFO queues and a global bounded backlog. A single batcher selects the oldest requests across clients, fills batches of 32, and sends to the GPU; results route to the originating client",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nShows practical understanding of per-room concurrency, bounded queues, backpressure, and degradation in a realistic signaling path.\n\n## Key Concepts\n- Bounded queues and backpressure\n- Per-room FIFO buffers\n- Dispatcher pool and ordering guarantees\n- Degradation policies and graceful shutdown\n\n## Code Example\n\n```javascript\n// Skeleton illustrating a bounded per-room buffer and dispatcher loop\nclass RoomBuffer { constructor(roomId) { this.roomId = roomId; this.queue = []; } push(m) { this.queue.push(m); } }\n```\n\n## Follow-up Questions\n- How would you test ordering under burst traffic?\n- How would you tune backlog thresholds for latency vs. throughput?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-11",
        "questionId": "q-713",
        "question": "Implement a thread-safe bounded queue using a fixed circular buffer with mutex protection and condition variables for coordinating producers and consumers. How would you handle enqueue/dequeue operations and implement graceful shutdown when the queue is no longer accepting new tasks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded global queue with per-agent rate-limiters (token bucket). Each agent maintains a small FIFO buffer and a dispatcher enqueues to the global queue; a small worker pool dequeues and writes",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a mutex to protect shared state (head, tail, count) and two condition variables: not_full for producers to wait when the buffer is full, and not_empty for consumers to wait when empty. During shutdown, signal all wa…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a per-user FIFO queue system managed by a global bounded work queue with a supervisory coordinator ensuring ordering guarantees. Maintain a versioned atomic model reference that workers access via shared point…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly concurrency exercise: 200 producers, each with a per-service FIFO buffer. A small worker pool drains a shared bounded queue to storage. Implement a per-service round-robin s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests fundamental concurrency concepts including mutex synchronization, condition variables for thread coordination, and graceful shutdown patterns - all essential for building reliable concurrent systems.\n\n## Key Concepts\n- **Mutex**: Protects shared state (head, tail, count, buffer) from race conditions\n- **Condition Variables**: not_full blocks producers when buffer is full, not_empty blocks consumers when empty\n- **Circular Buffer**: Efficient fixed-size storage using modulo arithmetic for wrap-around\n- **Shutdown Handling**: Clean termination by signaling all waiting threads\n\n## Implementation Details\n```python\nimport threading\nimport time\n\nclass BoundedQueue:\n    def __init__(self, capacity):\n        self.capacity = capacity\n        self.buffer = [None] * capacity\n        self.head = 0  # dequeue position\n        self.tail = 0  # enqueue position\n        self.count = 0\n        self.mutex = threading.Lock()\n        self.not_full = threading.Condition(self.mutex)\n        self.not_empty = threading.Condition(self.mutex)\n        self.shutdown = False\n    \n    def enqueue(self, item):\n        with self.not_full:\n            while self.count == self.capacity and not self.shutdown:\n                self.not_full.wait()\n            \n            if self.shutdown:\n                return False  # Indicate shutdown\n            \n            self.buffer[self.tail] = item\n            self.tail = (self.tail + 1) % self.capacity\n            self.count += 1\n            \n            self.not_empty.notify()\n            return True\n    \n    def dequeue(self):\n        with self.not_empty:\n            while self.count == 0 and not self.shutdown:\n                self.not_empty.wait()\n            \n            if self.shutdown and self.count == 0:\n                return None  # Indicate shutdown\n            \n            item = self.buffer[self.head]\n            self.buffer[self.head] = None  # Clear reference\n            self.head = (self.head + 1) % self.capacity\n            self.count -= 1\n            \n            self.not_full.notify()\n            return item\n    \n    def graceful_shutdown(self):\n        with self.mutex:\n            self.shutdown = True\n            self.not_full.notify_all()\n            self.not_empty.notify_all()\n```\n\n## Shutdown Behavior\n- **Producers**: enqueue() returns False when shutdown is active\n- **Consumers**: dequeue() returns None when queue is empty AND shutdown is active\n- **Signal Broadcasting**: notify_all() wakes all waiting threads for clean exit\n- **Idempotent**: Multiple shutdown calls are safe\n\n## Usage Pattern\n```python\n# Producer thread\ndef producer(queue):\n    for i in range(100):\n        if not queue.enqueue(i):\n            print(\"Producer shutting down\")\n            break\n\n# Consumer thread  \ndef consumer(queue):\n    while True:\n        item = queue.dequeue()\n        if item is None:\n            print(\"Consumer shutting down\")\n            break\n        process(item)\n\n# Graceful shutdown\nqueue.graceful_shutdown()\n```",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-12",
        "questionId": "q-2658",
        "question": "Design a bounded, fair, multi-producer/multi-consumer system for real-time order matching in a crypto exchange. Many producers submit limit orders; multiple workers attempt matches against an in-memory order book. Guarantee bounded memory, backpressure, and starvation-free fairness across producers (e.g., per-producer quotas or weighted fairness). Describe data structures, synchronization, and a minimal runnable Java snippet showing enqueue and a worker loop?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-level bounded queue: a fixed-size global ring buffer for backpressure and per-topic FIFO queues feeding workers. Producers claim a slot, append to topic queue; a compact scheduler interleaves topi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use asyncio with a fixed-size worker pool and a global semaphore for total in-flight bytes. Each chunk is enqueued with its index; workers fetch concurrently, storing results by index. An ordered writ",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-producer bounded queues fed into a rotating, weighted fair dispatcher that pushes into a fixed-size ring buffer consumed by worker threads. Use a single-order-book lock for matching, wit",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Map tenant_id to a per-tenant queue and a fixed-size worker pool fed by a bounded global queue. Enqueue under a tenant lock to preserve order; workers pull events by tenant sequence to maintain per-te",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design high-throughput, fair concurrency with bounded memory in a critical system like exchanges or dispatchers.\n\n## Key Concepts\n- Bounded queues with backpressure\n- Multi-producer/multi-consumer fairness\n- Dispatcher scheduling policies (weighted fair queuing)\n- Shared-order-book safety and lock granularity\n\n## Code Example\n```java\nimport java.util.concurrent.*;\nimport java.util.*;\n\npublic class BoundedFairDispatcher {\n  static class Order { final String id; Order(String id){ this.id = id; } }\n  private final int perProducerCap;\n  private final Map<String,BlockingQueue<Order>> queues = new ConcurrentHashMap<>();\n  private final BlockingQueue<Order> ring;\n  private volatile boolean shutdown = false;\n  public BoundedFairDispatcher(int perProducerCap, int ringCap){ this.perProducerCap = perProducerCap; this.ring = new ArrayBlockingQueue<>(ringCap); }\n  public void enqueue(String pid, Order o) throws InterruptedException {\n    BlockingQueue<Order> q = queues.computeIfAbsent(pid, k -> new ArrayBlockingQueue<>(perProducerCap));\n    q.put(o); // producer blocks if quota reached\n  }\n  public void dispatcherLoop() throws InterruptedException {\n    while (!shutdown) {\n      for (BlockingQueue<Order> q : queues.values()) {\n        Order o = q.poll();\n        if (o != null) ring.put(o);\n      }\n      // simple backoff to avoid busy spin\n      Thread.yield();\n    }\n  }\n  public void shutdown() { shutdown = true; }\n}\n```\n\n## Follow-up Questions\n- How would you scale to multiple dispatchers and partition rings?\n- How would you test fairness under highly skewed producer rates?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-13",
        "questionId": "q-15543",
        "question": "Design a real-time telemetry concurrency controller for a multi-sensor platform. Ingest hundreds of sensor streams; each sensor's events must be processed in-order per arrival, even under bursts. Build per-sensor FIFO buffers feeding a dynamic worker pool that can resize at runtime. Implement global backpressure signaling when the total buffered workload approaches capacity, with a policy to throttle or drop non-critical data while preserving per-sensor order. Provide a runnable enqueue/dequeue sketch and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-sensor FIFO queues and a central arbiter that assigns work to an elastic worker pool. Each sensor maintains in-order processing; global backpressure throttles producers via a bounded channel a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-stream bounded buffers keyed by partition, with a watermark-driven window and per-key queues for A and B. Use M workers to join when both sides have matching timestamps within window W;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a sensor-fusion scheduler with per-sensor buffers and a global bounded queue. A worker pool merges the latest readings within a 100ms window to produce fused outputs. When backlog grows, dro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant FIFO lanes using fixed-capacity lock-free queues. Route events from the global queue to corresponding tenant lanes via deterministic partitioning on tenant_id. Assign tenant shards to each worker fo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a practical streaming control plane with per-source ordering, dynamic scaling, and backpressure in a high-throughput setting.\n\n## Key Concepts\n- Per-source FIFO buffers\n- Elastic worker pool with runtime resize\n- Global backpressure signaling to producers\n- Late-event handling and per-source budgets\n- Test plan coverage (ordering, throughput, resizes)\n\n## Code Example\n```javascript\n// Pseudocode enqueue sketch\nfunction enqueue(sensorId, event) {\n  // push to per-sensor queue, signal arbiter if needed\n}\n```\n\n## Follow-up Questions\n- How would you calibrate backpressure thresholds for burstiness?\n- How would you verify correctness under clock skew and dropped samples?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-14",
        "questionId": "q-12697",
        "question": "You operate a real-time risk engine ingesting events from hundreds of upstream feeds. You must deliver events to a 32-worker pool while preserving per-feed order and applying global backpressure so producers slow when the system saturates. Design a bounded, multi-queue architecture: a global bounded queue plus per-feed FIFO buffers; support cross-region failover, exactly-once delivery semantics, and clean shutdown. Explain trade-offs and provide a concrete test plan for ordering, backpressure, and fault tolerance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe a per-DAG, per-stage bounded queueing with path-specific sequencing. Each task carries path_id and sequence; a node forwards a task only after all its parents for that path have completed. Us",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-user queues (FIFO) fed into a bounded global worker pool. Each user channel feeds a non-blocking writer to a bounded arbiter that enforces global order, with a semaphore-based backpressure. Ga",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a global bounded queue (maxsize N) fed by per-feed FIFO buffers; a coordinator siphons from buffers into the global queue and applies backpressure to producers via await when full. 32 worker",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-device buffers keyed by device_id, each storing windows with running aggregates. Maintain a global watermark; when window_end <= watermark, flush and drop. Cap total buffered windows; on cap,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess system design for multi-queue concurrency, ordering guarantees, backpressure propagation, and cross-region fault tolerance in a realistic streaming engine.\n\n## Key Concepts\n\n- Bounded queues\n- Per-feed ordering and sequence tracking\n- Backpressure signaling\n- Durable checkpoints and cross-region failover\n- Graceful shutdown and idempotence\n\n## Code Example\n\n```javascript\n// Pseudocode sketch for a bounded global queue with per-feed buffers\n```\n\n## Follow-up Questions\n\n- How would you ensure exactly-once across failover?\n- How would you test ordering, backpressure propagation, and clean shutdown?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-15",
        "questionId": "q-26906",
        "question": "Design a dynamic DAG based task scheduler for a real time data pipeline. The graph can grow at runtime with new tasks and dependencies; tasks must execute only after all prerequisites in their subgraph complete, preserving per component topological order. The system should bound memory, apply backpressure to producers, support fast path cancellation, and allow graceful shutdown. Provide runnable sketch in Rust or Go and outline a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-tier scheduler: a global bounded queue feeding per-shard FIFO queues with a fixed worker pool. Producers backpressure when global is full. Scheduler uses weighted round-robin to allocate CPU fairl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement two bounded queues: Q1 for decoding (N1 workers) and Q2 for enhancement (N2 workers). Attach per-frame sequence numbers to enforce global in-order delivery to Stage 2. Stage 1 runs on a fixed thread pool; Stag…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a dynamic DAG scheduler with per component topological ordering. Each task stores in-degree and a dependency bitset. Ready tasks go to bounded per-subgraph queues; workers pull, execute, and",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a NUMA-aware concurrent task scheduler for a multi-socket server with a global bounded queue and local queues per NUMA node. Enqueue operations should apply backpressure when the global queue is full. A dispatche…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-time DAG scheduling with runtime graph growth tests deep understanding of ordering guarantees, backpressure, and shutdown semantics in concurrent systems.\n\n## Key Concepts\n- Dynamic graph with per-component topological order\n- Bounded memory, per-subgraph ready queues\n- Backpressure to producers via credits\n- Fast-path cancellation and graceful shutdown\n\n## Code Example\n```rust\n// Placeholder: sketch showing core structures (Task, InDegree, ReadyQueue) and enqueue/dequeue paths\n```\n\n## Follow-up Questions\n- How would you ensure exactly-once semantics for task results?\n- How would you test for starvation and deadlock under high churn?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-16",
        "questionId": "q-29628",
        "question": "Design a beginner-friendly concurrency exercise for a chat notification service: dozens of rooms produce events (messages and typing); ensure per-room arrival order via per-room FIFO buffers feeding a bounded global worker queue. Implement a degradation path that coalesces rapid typing events per room when backlog grows and drops non-critical metadata. Explain backpressure, per-room ordering, and provide a test plan for correctness and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I'd implement with Python asyncio. Use a bounded global queue (maxsize 1024) plus per-device FIFO queues. A central arbiter drains the global queue and forwards events to device queues in arrival orde",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a bounded queue with producers awaiting when full. Each task is {id, version, payload}. Workers perform speculative work and push provisional results to an in-flight map. A validator deterministic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "An IoT telemetry collector for 200 devices. Each device's readings must be processed in device-order; readings from different devices may be parallel. Implement per-device FIFO buffers feeding a bound",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-room FIFO buffers feeding a bounded global worker queue to preserve per-room ordering while allowing parallelism across rooms. Add a backlog-based degradation path that coalesces rapid typing events per ro…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates a candidate's practical understanding of per-source ordering with shared throughput, backpressure mechanisms, and graceful degradation in a realistic chat notification system.\n\n## Key Concepts\n\n- **Per-room FIFO buffers**: Ensure events within each room maintain insertion order\n- **Global bounded worker queue**: Limit total memory usage while enabling cross-room parallelism\n- **Backpressure thresholds**: Prevent producers from overwhelming the system\n- **Degradation via event coalescing**: Reduce load by merging rapid state changes (typing indicators) into single events\n- **Graceful shutdown**: Drain in-flight work before terminating without data loss\n\n## Code Example\n\n```javascript\nclass RoomBuffer {\n  constructor(maxBacklog = 1000) {\n    this.queues = new Map();\n    this.maxBacklog = maxBacklog;\n  }\n  \n  enqueue(room, event) {\n    if (!this.queues.has(room)) {\n      this.queues.set(room, []);\n    }\n    const queue = this.queues.get(room);\n    if (queue.length >= this.maxBacklog) {\n      if (event.type === 'typing') {\n        return this.coalesceTyping(room, event);\n      }\n      return false; // backpressure signal\n    }\n    queue.push({ ...event, seq: this.getNextSeq(room) });\n    return true;\n  }\n  \n  coalesceTyping(room, event) {\n    const queue = this.queues.get(room);\n    const last = queue[queue.length - 1];\n    if (last?.type === 'typing') {\n      last.count = (last.count || 1) + 1;\n      return true;\n    }\n    queue.push({ ...event, count: 1 });\n    return true;\n  }\n}\n```\n\nThis implementation provides per-room ordering via sequence numbers, backpressure via bounded queue depth, and automatic coalescing of typing events during high load.",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-17",
        "questionId": "q-17652",
        "question": "Design a beginner-friendly concurrency exercise: an event-processing pipeline for a real-time stock alert system. Producers generate price-change events tagged by market (NYSE, NASDAQ, LSE). A bounded shared queue feeds a small worker pool that executes analyzers. Preserve per-market FIFO order; implement backpressure to slow producers when the queue is full. Add a per-market spill path: excess events go to a per-market spill buffer with max depth; if overflow, drop oldest. Explain backpressure, fairness, and propose a pragmatic test plan for per-market ordering, spill behavior, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: partition by user_id into fixed shards; bounded per-shard queues; transformers emit to a per-user state store keyed by user_id to preserve order. Use sequence numbers and a barrier during re",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-document ordered pipeline with a shared bounded queue and per-document buffers. Track per-document sequence numbers and fan-out across a worker pool to preserve document order. Implement",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a Go-based solution: a fixed-size main queue (buffered channel) fed to a worker pool, plus per-market FIFO spill buffers. Backpressure blocks producers when the main queue is full; optional ex",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a two-tier design: per-client FIFO buffers and a bounded global dispatcher queue consumed by N workers. The dispatcher uses a deficit-round-robin scheduler over active client queues to preserve pe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical concurrency reasoning: bounded buffers, per-market ordering, and fallback paths under load. It also checks how candidates balance fairness and safety while avoiding starvation in multi-tenant streams.\n\n## Key Concepts\n\n- Bounded queues and backpressure\n- Per-market FIFO ordering\n- Spill buffers with eviction policy\n- Graceful shutdown and testability\n\n## Code Example\n\n```go\ntype Event struct { Market string; Seq int; Data any }\n```\n\n## Follow-up Questions\n\n- How would you simulate bursts and validate per-market ordering?\n- How would you extend to dynamic worker scaling or multiple brokers?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-18",
        "questionId": "q-22979",
        "question": "Design a per-customer real-time event pipeline with dynamic workers where updates arrive out of order. Ensure per-customer in-order processing across a bounded, sharded queue and backpressure from bursty customers. Specify data structures (per-customer sequence counters, per-customer buffers, min-heaps), a watermarking approach to advance progress, and a runtime policy for late events (delay, duplicate-suppress, or drop). Provide a runnable sketch and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-directory FIFO buffers and a bounded global queue; a small worker pool updates a mock index. Implement coalescing: for each file, merge rapid edits within 100ms into a single update before enq",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-customer real-time event pipeline with dynamic workers where updates arrive out of order. Ensure per-customer in-order processing across a bounded, sharded queue with backpressure from bursty customers. Spe…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a credit-based bounded pipeline. Each upstream stream maintains a local FIFO and a shared credit pool. Producers enqueue only when they hold a credit; decrement on enqueue, downstream replenishes",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a router with per-client FIFO buffers fed into a bounded global queue, serviced by a small worker pool. Enforce ordering by dequeuing from the active client head, cycling for fairness. When ba",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical handling of per-entity ordering in a concurrent, dynamic worker pool with backpressure. It mirrors real-world systems at scale, similar to those built at companies like Instacart and Cloudflare.\n\n## Key Concepts\n\n- **Per-partition state and in-order emission**: Each customer (partition) maintains its own sequence counter and buffer to ensure events are processed in order\n- **Watermarks for progress tracking**: Track the minimum sequence number across all active partitions to advance the global watermark and handle late events\n- **Dynamic rebalancing**: Workers can be added/removed dynamically; partitions must be redistributed without losing in-order guarantees\n- **Backpressure mechanisms**: Apply bounded queues and flow control for bursty customers to prevent memory exhaustion\n\n## Implementation Sketch\n\n```javascript\n// Per-customer state manager\nclass PerCustomerState {\n  constructor(customerId) {\n    this.customerId = customerId;\n    this.nextExpectedSeq = 0;\n    this.buffer = new Map(); // seq -> event\n    this.watermark = 0;\n  }\n\n  addEvent(event) {\n    this.buffer.set(event.seq, event);\n    this.advanceWatermark();\n  }\n\n  advanceWatermark() {\n    while (this.buffer.has(this.watermark)) {\n      this.watermark++;\n    }\n  }\n\n  getNextEvent() {\n    const event = this.buffer.get(this.nextExpectedSeq);\n    if (event) {\n      this.buffer.delete(this.nextExpectedSeq);\n      this.nextExpectedSeq++;\n    }\n    return event;\n  }\n}\n\n// Global event router with sharded queues\nclass EventPipeline {\n  constructor(options = {}) {\n    this.numShards = options.numShards || 64;\n    this.customerStates = new Map();\n    this.minHeap = new MinHeap(); // tracks min watermark per customer\n    this.maxBufferSize = options.maxBufferSize || 10000;\n    this.lateEventPolicy = options.lateEventPolicy || 'delay'; // 'delay', 'drop', 'suppress'\n    this.workers = [];\n  }\n\n  // Consistent hashing for customer -> shard assignment\n  getShard(customerId) {\n    return this.hash(customerId) % this.numShards;\n  }\n\n  // Route event to appropriate customer state\n  async ingest(event) {\n    const shard = this.getShard(event.customerId);\n    let state = this.customerStates.get(event.customerId);\n    \n    if (!state) {\n      state = new PerCustomerState(event.customerId);\n      this.customerStates.set(event.customerId, state);\n      this.minHeap.insert({ customerId: event.customerId, watermark: 0 });\n    }\n\n    // Backpressure: check buffer size\n    if (state.buffer.size > this.maxBufferSize) {\n      await this.applyBackpressure(event.customerId);\n    }\n\n    // Handle late/out-of-order events\n    return this.handleEvent(state, event);\n  }\n\n  handleEvent(state, event) {\n    if (event.seq < state.nextExpectedSeq) {\n      // Late event - apply policy\n      switch (this.lateEventPolicy) {\n        case 'drop':\n          return { status: 'dropped', reason: 'late' };\n        case 'suppress':\n          return state.buffer.has(event.seq) \n            ? { status: 'suppressed', reason: 'duplicate' }\n            : this.addAndProcess(state, event);\n        case 'delay':\n        default:\n          return this.addAndProcess(state, event);\n      }\n    }\n    return this.addAndProcess(state, event);\n  }\n\n  addAndProcess(state, event) {\n    state.addEvent(event);\n    this.minHeap.update(state.customerId, state.watermark);\n    return { status: 'buffered' };\n  }\n\n  // Global watermark = minimum across all customer watermarks\n  getGlobalWatermark() {\n    return this.minHeap.getMin()?.watermark || 0;\n  }\n\n  // Process ready events for a customer\n  processCustomer(customerId) {\n    const state = this.customerStates.get(customerId);\n    if (!state) return [];\n\n    const processed = [];\n    let event;\n    while ((event = state.getNextEvent()) !== undefined) {\n      processed.push(this.executeEvent(event));\n    }\n    return processed;\n  }\n\n  async executeEvent(event) {\n    // Worker processes the event\n    return { customerId: event.customerId, seq: event.seq, result: 'processed' };\n  }\n\n  async applyBackpressure(customerId) {\n    // Slow down ingestion for bursty customer\n    await new Promise(r => setTimeout(r, 100));\n  }\n}\n\n// Min-heap for tracking customer watermarks\nclass MinHeap {\n  constructor() {\n    this.data = [];\n    this.indexMap = new Map();\n  }\n\n  insert(item) {\n    this.data.push(item);\n    this.indexMap.set(item.customerId, this.data.length - 1);\n    this.bubbleUp(this.data.length - 1);\n  }\n\n  update(customerId, watermark) {\n    const idx = this.indexMap.get(customerId);\n    if (idx !== undefined) {\n      this.data[idx].watermark = watermark;\n      this.bubbleUp(idx);\n      this.bubbleDown(idx);\n    }\n  }\n\n  getMin() {\n    return this.data[0];\n  }\n\n  bubbleUp(idx) {\n    // ... standard heap operations\n  }\n\n  bubbleDown(idx) {\n    // ... standard heap operations\n  }\n}\n```\n\n## Runtime Policy for Late Events\n\n| Policy | Behavior | Use Case |\n|--------|----------|----------|\n| **Delay** | Buffer late events until sequence is complete | When all events must be processed (e.g., financial transactions) |\n| **Suppress** | Drop duplicates, keep first occurrence | When idempotency is guaranteed (e.g., cache updates) |\n| **Drop** | Immediately discard late events | When recent state is more important than completeness (e.g., real-time dashboards) |\n\n## Test Plan\n\n### Unit Tests\n1. **In-order processing**: Verify events with sequential sequence numbers are processed in order\n2. **Out-of-order handling**: Send events [3, 1, 2] and verify output order [1, 2, 3]\n3. **Late event policies**: Test each policy with events arriving after watermark advances\n4. **Backpressure**: Verify ingestion blocks when buffer exceeds max size\n5. **Watermark advancement**: Confirm global watermark moves with min(customer watermarks)\n\n### Integration Tests\n1. **Dynamic workers**: Add/remove workers and verify partition rebalancing\n2. **Bursty customers**: Simulate high-volume customer with backpressure enabled\n3. **Shard consistency**: Verify same customer always routes to same shard\n4. **Failure recovery**: Test state reconstruction after worker restart\n\n### Performance Tests\n1. **Throughput**: Measure events/second under load\n2. **Latency**: P50/P99 end-to-end latency for out-of-order events\n3. **Memory boundedness**: Verify buffer cleanup prevents unbounded growth",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-19",
        "questionId": "q-23768",
        "question": "In a real-time data-processing platform, thousands of publishers send tasks tagged by tenant_id and priority. Build a bounded dispatcher with a global queue and per-tenant FIFO queues. Ensure per-tenant ordering, dynamic worker scaling, and backpressure; high-priority bursts must be throttled without starving low-priority tenants. Provide a runnable sketch and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-user queues (FIFO) fed into a bounded global worker pool. Each user channel feeds a non-blocking writer to a bounded arbiter that enforces global order, with a semaphore-based backpressure. Ga",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a beginner-friendly concurrency exercise: hundreds of vehicles stream telemetry updates; each vehicle's updates must arrive in strict order, but overall processing runs in parallel. Use per-veh",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a deadline-aware scheduler with a global bounded queue and per-account FIFO buffers; ensure high-SLA streams are prioritized, allow preemption or stall of lower-SLA tasks as deadlines near, and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a bounded, multi-tenant dispatcher: tasks arrive with tenant_id and priority. Maintain a global bounded queue and per-tenant FIFO queues. Ensure per-tenant ordering, dynamic worker scaling, and",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes the ability to design a scalable, low-latency dispatcher with strict per-tenant ordering, dynamic worker scaling, and observable backpressure, plus auditable replay logs.\n\n## Key Concepts\n\n- Two-tier scheduling: global bounded queue + per-tenant FIFO queues\n- Backpressure: blocking enqueue and per-tenant throttling during bursts\n- Fairness vs priority: rotating tenant windows to avoid starvation\n- Fault tolerance and replay: deterministic logs for audits and replay\n- Cancellation and graceful shutdown\n\n## Code Example\n\n```python\nimport asyncio\n\nglobal_q = asyncio.Queue(maxsize=1024)\ntenant_queues = {}\n\nasync def enqueue(task):\n    tenant = task['tenant_id']\n    q = tenant_queues.setdefault(tenant, asyncio.Queue())\n    await q.put(task)\n    # optional: wake a central dispatcher that drains global_q\n```\n\n## Follow-up Questions\n\n- How would you test per-tenant ordering and backpressure under burst traffic?\n- How would you handle dynamic worker scaling and failure scenarios while preserving replayability?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-20",
        "questionId": "q-28078",
        "question": "Design a beginner-friendly concurrency exercise: a multi-source index updater for a search service. Dozens of producers emit document-change events, each tagged by shard ID. Each shard’s events must be applied in arrival order, while updates across shards can be processed in parallel. Implement per-shard FIFO buffers feeding a bounded global worker queue that writes to a single in-memory index. When backlog crosses a threshold, degrade by coalescing updates per shard to the latest change within a time window and dropping non-critical fields. Explain backpressure, per-shard ordering, and a concrete test plan for correctness, degradation behavior, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded global queue with per-sensor FIFO buffers. A small worker pool drains buffers in a round-robin fashion, preserving per-sensor order. Backpressure blocks producers when the global queue i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage per-device, per-minute lanes with a bounded input queue and a worker pool. Use a watermark that advances windows and routes late events to a DLQ or re-windowed bucket. Resize the pool atomica",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-shard FIFO buffers feed a bounded global queue consumed by a small worker pool. A dedicated writer ensures per-shard order, while cross-shard work runs in parallel. Backpressure arises from the bo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-tenant FIFO buffers feeding a bounded global queue; a small worker pool drains to an external sink. Use a global token bucket (e.g., 100 ops/sec) with per-tenant credit, ensuring at leas",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical concurrency in a real-world component: multiple producers, per-source ordering, bounded queues, and degradation under load. It also probes test planning and fault tolerance.\n\n## Key Concepts\n- Per-source FIFO buffers and a bounded global queue\n- Single writer per shard to preserve ordering; parallel across shards\n- Backpressure and degradation via update coalescing\n\n## Code Example\n```javascript\n// Pseudo outline: perShardBuffers, globalQueue, workers, degradationPolicy\n```\n\n## Follow-up Questions\n- How would you extend to multiple index writers while preserving ordering guarantees?\n- How do you ensure idempotence during degradation?\n- What metrics would you collect to monitor backlog and degradation triggers?",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-21",
        "questionId": "q-16075",
        "question": "In a real-time analytics dashboard, dozens of client apps (producers) push metric events concurrently. Each event has a topic and a region. Build a beginner-friendly concurrency controller (Go or Python asyncio) with a bounded global queue and per-topic FIFO buffers. A worker pool consumes and writes to a sink. Implement a backpressure policy: when the global queue is full, drop the oldest events from the least recently active topic while preserving per-topic order. Ensure fair sharing to avoid region starvation. Provide a concrete test plan for per-topic ordering, backpressure under bursts, and graceful shutdown?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded global queue (maxsize N) and a per-topic deque for each topic. A dispatcher moves from per-topic buffers into the global queue; a small worker pool writes to the sink. When the global qu",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a bounded global queue plus per-user FIFO buffers. A fixed worker pool polls buffers in a round-robin with weight on occupancy to preserve per-user order while interleaving users. Backpressure: bl",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Concurrency Controller Design ### Core Architecture ```python from dataclasses import dataclass from collections import defaultdict from queue import Queue, PriorityQueue import threading import time import random @d…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant FIFO queues backed by a lock-free ring and a bounded global queue feeding a dynamic worker pool. Each event carries tenant_id and seq; dispatcher enforces order via per-tenant cursors. Back",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of per-topic FIFO ordering, bounded queues, backpressure, fairness, and graceful shutdown in a beginner-friendly setting.\n\n## Key Concepts\n- Bounded queues and per-topic buffers\n- Backpressure policies and fairness\n- Graceful shutdown with in-flight draining\n\n## Code Example\n```python\n# Skeleton demonstrating structure\nimport asyncio\nfrom collections import defaultdict, deque\nclass Controller:\n    def __init__(self, global_max=1024, per_topic_max=128):\n        self.global_q = asyncio.Queue(maxsize=global_max)\n        self.topic_buffers = defaultdict(lambda: deque(maxlen=per_topic_max))\n    async def submit(self, topic, item):\n        self.topic_buffers[topic].append(item)\n        await self._maybe_dispatch()\n    async def _maybe_dispatch(self):\n        for topic, buf in self.topic_buffers.items():\n            while buf and not self.global_q.full():\n                await self.global_q.put(buf.popleft())\n```",
        "difficulty": "beginner"
      },
      {
        "id": "concurrency-q-22",
        "questionId": "q-21905",
        "question": "Telemetry ingestion scenario with thousands of devices. Each device's events must be processed in device-order, even as worker pool scales. A global bounded queue feeds workers; backpressure must prevent overload without breaking ordering. Design a concurrency strategy (Go or Rust) using per-device queues and a central arbiter, address failure, scaling, and provide a runnable minimal sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a beginner-friendly concurrency exercise: 200 producers, each with a per-service FIFO buffer. A small worker pool drains a shared bounded queue to storage. Implement a per-service round-robin s",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-device FIFO queues buffer events; a central arbiter routes to a bounded worker pool. A single global channel enforces backpressure; producers block when full. Arbiter preserves device-order, scale",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design and implement a global bounded-rate limiter using a central dispatcher with per-customer FIFO queues and a shared token-bucket pool. The dispatcher allocates tokens from a global capacity of R events/sec into per…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement: per-device FIFO buffers feeding a bounded global queue; producers block when full. Degrade when backlog exceeds threshold by dropping older items for low-priority devices and keeping only t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nRationale about cross-device ordering and backpressure in scalable systems.\n\n## Key Concepts\n- Per-device FIFO ordering\n- Central arbiter scheduling\n- Global bounded queues and backpressure\n- Dynamic scaling and failure handling\n\n## Code Example\n```go\n// Minimal sketch demonstrating per-device queues, arbiter, and worker pool\npackage main\n\nfunc main() {\n  // placeholder\n}\n```\n\n## Follow-up Questions\n- How to test ordering guarantees under burst?\n- How would you adapt for exactly-once delivery and replay-free restarts?",
        "difficulty": "advanced"
      },
      {
        "id": "concurrency-q-23",
        "questionId": "q-11234",
        "question": "Design a cross-region ride analytics pipeline where events from 40 regions arrive with user_id keys but may arrive out of order; implement per-user bounded FIFO buffers (size K), a region-aware dispatcher that assigns users to workers, and a dynamic rebalancer to balance load? Provide a runnable enqueue/consume sketch and a concrete test plan for per-user ordering, backpressure propagation, and regional failover?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-tenant FIFO queue and a single bounded backlog. Each tenant gets a dynamic quota updated by a central scheduler (token buckets that adjust to burstiness). A dispatcher drains queues in weigh",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a deadline-aware scheduler with a global bounded queue and per-account FIFO buffers; ensure high-SLA streams are prioritized, allow preemption or stall of lower-SLA tasks as deadlines near, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-user buffers of size K, a central sequencer mapping user_id to a worker, and region-aware dispatch that routes new events to the correct worker while preserving order. Global queue is bounded;",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly concurrency exercise: 200 producers, each with a per-service FIFO buffer. A small worker pool drains a shared bounded queue to storage. Implement a per-service round-robin s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world systems must preserve per-user order across regional boundaries under backpressure and partial outages. This question probes understanding of multi-tenant ordering, dynamic scaling, and failure handling in a distributed event pipeline.\n\n## Key Concepts\n- Per-user FIFO buffers with bounded capacity\n- Region-aware dispatch and dynamic rebalancing\n- Backpressure signaling and propagation across regions\n- Ordering guarantees under bursts and failover scenarios\n\n## Code Example\n```javascript\n// Skeleton enqueue/consume sketch\nclass Dispatcher {\n  enqueue(event) {\n    // if globalQueue is full, return false to indicate backpressure\n  }\n  route(event) { /* map user_id to worker, maintain order */ }\n}\nclass Worker {\n  constructor() { this.buffers = new Map(); }\n  process() { /* ensure per-user order when consuming */ }\n}\n```\n\n## Follow-up Questions\n- How would you test ordering guarantees under burst traffic and region failover?\n- What metrics would you monitor to detect imbalance or cascading backpressure?",
        "difficulty": "intermediate"
      },
      {
        "id": "concurrency-q-24",
        "questionId": "q-24171",
        "question": "Design a high-throughput concurrent task scheduler for global log aggregation. Thousands of producers emit per-tenant events; each tenant’s events must be processed in tenant order across dynamic worker pools. Build a two-level queue: a bounded global queue and per-tenant FIFO sub-queues, with backpressure to prevent backlog and with hot-swap capable workers. Explain failure modes, isolation, and provide a runnable sketch in Go or Rust showing enqueue and dispatch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Attach a cancellation token to each event; per-service FIFO buffers feed a fixed-size global work queue. Workers check the token and abort if canceled, freeing resources. Preserve per-service order fo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant FIFO queues backed by a lock-free ring and a bounded global queue feeding a dynamic worker pool. Each event carries tenant_id and seq; dispatcher enforces order via per-tenant cursors. Back",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a durable in-flight map keyed by taskID to track status; mark inflight on enqueue, complete on success, and persist to a durable log for crash recovery. Workers pull from a bounded channel; before",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: use per-room bounded buffers and a dispatcher that polls buffers in a round-robin fashion, forwarding to a shared outbound queue with a fixed capacity. Guarantee room ordering by never reord",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a scalable, fault-tolerant scheduler that preserves per-tenant order under dynamic workloads, while integrating backpressure and failover handling.\n\n## Key Concepts\n\n- Per-tenant FIFO queues\n- Bounded global queue with backpressure\n- Dynamic worker pool and hot-swapping\n- Atomic cursors to preserve order\n- Failure modes: partial failure, replay, and isolation guarantees\n\n## Code Example\n\n```go\n// A minimal sketch in Go illustrating the core idea\npackage main\n\ntype Event struct {\n  Tenant string\n  Seq    int64\n  Data   []byte\n}\n\n// Placeholder for enqueue/dispatch interfaces\nfunc Enqueue(e Event) { /* push to global queue; workers drain per-tenant subqueues */ }\nfunc Dispatch()       { /* route to per-tenant queues and workers, preserving order */ }\n```\n\n## Follow-up Questions\n\n- How would you test backpressure and per-tenant ordering under burst? \n- How would you extend to multi-region consistency and failure recovery?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-13T06:17:46.306Z",
    "version": 1
  }
]