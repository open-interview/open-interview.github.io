[
  {
    "id": "test-api-testing",
    "channelId": "api-testing",
    "channelName": "API Testing",
    "title": "API Testing Knowledge Test",
    "description": "Test your understanding of API Testing with 25 questions.",
    "questions": [
      {
        "id": "api-testing-q-0",
        "questionId": "q-23313",
        "question": "You manage an Order API for a multi-tenant marketplace. POST /orders creates an order and emits a Kafka event consumed by inventory, billing, and shipping. Design a pragmatic API testing plan covering idempotency, event-state consistency, per-tenant isolation on a shared bus, contract tests with downstream services, and resilience to broker outages and DLQ routing. Include concrete test cases, tooling, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design and implement a comprehensive end-to-end test strategy for an asynchronous bulk import API where POST /bulk/import accepts CSV uploads and returns a jobId, while GET /bulk/import/{jobId} provides real-time status…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a concrete test plan for GET /telemetry?vehicleId=... The endpoint reads real-time data from a Kafka feed and a batch Cassandra store. Validate cross-source consistency, ensure a defined stale",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test idempotency by resubmitting the same payment with identical payload and idempotency key across two regions; ensure ledger has exactly one entry per key. Simulate regional outages to verify retrie",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an API testing plan that enforces idempotency and end-to-end consistency for POST /orders in a multi-tenant marketplace. Include: 1) idempotency-key tests to prevent duplicates; 2) traceability",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTo assess depth in testing distributed, event-driven APIs with multi-tenancy and reliability requirements.\n\n## Key Concepts\n- Idempotency keys and deduplication\n- Event-driven consistency between REST and topics\n- Per-tenant data isolation on shared bus\n- Consumer-driven contract testing\n- Resilience tests: broker outages, lag, DLQ routing\n\n## Code Example\n```javascript\n// Pseudo-idempotency\nconst existing = cache.get(idKey);\nif (existing) return existing;\nconst order = saveOrder(payload);\ncache.set(idKey, order.id);\npublishEvent('order.created', order);\n```\n\n## Follow-up Questions\n- How would you test real-time latency under load?\n- How to ensure DLQ contains only unprocessable messages and how to replay safely?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-1",
        "questionId": "q-9774",
        "question": "Design an advanced test plan for a GraphQL API serving media data with nested fields series -> seasons -> episodes and role-based visibility (premiumNotes visible only to PRO users). Include deep-nested query validation, depth/complexity controls, multi-region caching with invalidation, token expiry mid-query handling with partial responses, and feature-flag driven schema drift tests. Provide concrete test cases and tooling recommendations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan focuses on GraphQL nested data, access control, and cross-region behavior. Validate deep queries, enforce depth and complexity limits, test cache invalidation after mutations, simulate token expi",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a deterministic base payload and an Idempotency-Key header. Send POST /orders with key k1; capture orderId1. Send the same request again with k1; expect 200/201 with the same orderId1 and no dupli",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an idempotent contract with Idempotency-Key for POST /audit-logs. Send a batch twice; verify only one run creates artifacts across ingestion DB, analytics warehouse, and search index. Check 202 an",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an elevated concurrency test: submit N parallel POST /orders calls with the same idempotency-key; verify only a single order is created in Postgres, a single Kafka event emitted, and the Redis cac",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design end-to-end GraphQL tests that cover nested data access, per-field permissions, cross-region caching, and schema drift, which are common in large platforms.\n\n## Key Concepts\n- GraphQL query depth/complexity controls\n- Field-level authorization and masking\n- Multi-region caching and invalidation strategies\n- Token lifecycle handling and partial responses\n- Feature-flag driven schema drift testing\n\n## Code Example\n\n```javascript\n// Example: basic structure for a GraphQL complexity check\nfunction limitQueryDepth(query) {\n  // analyze query depth and return boolean\n  return true;\n}\n```\n\n## Follow-up Questions\n- How would you measure per-tenant GraphQL query cost and enforce quotas?\n- How would you design contract tests to detect schema drift when feature flags change fields?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-2",
        "questionId": "q-6564",
        "question": "Design a beginner-friendly test plan for a long-running report-generation API where POST /reports starts a job and GET /reports/{jobId} polls status, and GET /reports/{jobId}/download returns the CSV when DONE. Include idempotency key handling for duplicate POSTs, timeouts, and failure modes (job crash), and provide concrete test cases and tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pact for consumer-driven contracts between the REST API, async worker, and the index layer. Validate both paths: synchronous small-queries and asynchronous large-exports. Include: 1) contract test",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan should verify: 1) POST /reports with idempotency key returns 202 and a jobId; duplicates yield same jobId. 2) Polling: status flows PENDING -> RUNNING -> DONE with progress ETA. 3) After DON",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Make a first call with key 'abc' returning 201 and a transaction_id. Replay with the same key; gateway mocks return 200 with the same id or 409; ensure no extra charges. Validate 400/422 for bad input",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate an ingest batch job end-to-end: per-record schema checks, required fields, and max sizes; confirm idempotency by re-sending with the same idempotency key; simulate partial failures to push ba",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses beginner API-testing skills for async job flows, including idempotency, polling, and robust download validation.\n\n## Key Concepts\n- Async job lifecycle\n- Idempotency keys\n- Polling and timeouts\n- Content-Type and error schemas\n- Instrumentation and test data\n\n## Code Example\n```javascript\n// Example test sketch in pseudo-code\n```\n\n## Follow-up Questions\n- How would you test for concurrent polls altering status?\n- How would you integrate this into CI with flaky queue simulation?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-3",
        "questionId": "q-14934",
        "question": "Design a beginner-friendly test plan for POST /orders that uses an Idempotency-Key header to avoid duplicates. Include: first request creates an order (201) and stores the key; replay with identical body returns same order_id; replay with a modified body returns 4xx; two parallel identical requests create a single order; handle transient 5xx retries resulting in one order; guidance for 429/Retry-After?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive test plan for POST /orders with Idempotency-Key header validation: 1. **First Request Creates Order**: Send an initial request with the Idempotency-Key header; verify a 201 Created response is ret…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Outline tenant isolation by issuing the same GraphQL query with different auth tokens and asserting tenant-scoped data and headers. Validate persisted queries by hashing a query and confirming the gat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Make a first call with key 'abc' returning 201 and a transaction_id. Replay with the same key; gateway mocks return 200 with the same id or 409; ensure no extra charges. Validate 400/422 for bad input",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan end-to-end tests for POST /payments with Idempotency-Key. Validate that repeated POSTs with the same key produce a single payment record and identical responses, while new keys create new records",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTesting idempotent POST endpoints is critical for preventing duplicate creates and handling race conditions in production traffic. This validates basic correctness, ensures no duplicate orders under concurrent load, and confirms proper retry behavior.\n\n## Key Concepts\n\n- **Idempotency Keys**: Unique identifiers that allow safe retry of requests\n- **Race Conditions**: Scenarios where concurrent requests may cause unintended side effects\n- **HTTP Status Codes**: 201 (Created), 200 (OK), 4xx (Client Error), 5xx (Server Error), 429 (Too Many Requests)\n- **Concurrency Testing**: Validating system behavior under parallel requests and retries\n\n## Code Example\n\n```javascript\n// First request creates the order\nconst response1 = await fetch('/orders', {\n  method: 'POST',\n  headers: {\n    'Idempotency-Key': 'unique-key-123',\n    'Content-Type': 'application/json'\n  },\n  body: JSON.stringify({ customer_id: 'cust-456', amount: 99.99 })\n});\n\n// Replay with identical body returns same order\nconst response2 = await fetch('/orders', {\n  method: 'POST',\n  headers: {\n    'Idempotency-Key': 'unique-key-123',\n    'Content-Type': 'application/json'\n  },\n  body: JSON.stringify({ customer_id: 'cust-456', amount: 99.99 })\n});\n```",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-4",
        "questionId": "q-14780",
        "question": "You're deploying a real-time pricing API with REST GET /prices and WebSocket /ws/prices. Design an end-to-end test plan that verifies REST snapshot accuracy and freshness, WebSocket streaming correctness under high throughput (in-order delivery, message deduplication, and robust reconnect with backoff), and cross-service consistency when upstream feeds diverge. Include outage fallback to cached data and mid-stream token rotation handling. Provide concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose resilience-focused tests: mock downstream latency and failures for /sales/daily and /customers/daily; assert 200 with partial payload and a warning header when one downstream times out; assert",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: test both REST snapshots and WebSocket streams. REST: validate schema, freshness window, symbol coverage, and delta semantics. WebSocket: assert in-order messages via sequenceId, deduplicate",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test the GET /users/{id} endpoint with three key scenarios: valid ID (expect 200 status with correct user data), invalid ID (expect 404 status), and malformed requests (expect 400 status). Verify response structure, dat…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan tests for POST /payments with Idempotency-Key: verify a single charge on first request; same key returns cached result without charging again; different keys create new charges. Test under concur",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design end-to-end tests for both REST and streaming paths, covering data correctness, ordering guarantees, resilience under failures, and secure token handling in real-time systems.\n\n## Key Concepts\n- End-to-end testing across REST and WebSocket paths\n- Message ordering and deduplication in streaming\n- Reconnection/backoff strategies under churn\n- Cross-service data consistency with upstream feeds\n- Cache fallback and mid-stream token rotation handling\n\n## Code Example\n\n```javascript\n// Pseudo-test skeleton\nasync function testPricesREST() {\n  const r = await fetch('/prices?symbols=AAPL,GOOG');\n  assert(r.status===200);\n  const data = await r.json();\n  assert(Array.isArray(data.symbols));\n}\nasync function testPricesWS(wsUrl) {\n  const ws = new WebSocket(wsUrl);\n  ws.onmessage = (ev) => {\n    const msg = JSON.parse(ev.data);\n    // validate sequenceId monotonicity and uniqueness\n  };\n  // connect, use short-lived tokens, simulate reconnections\n}\n``` \n\n## Follow-up Questions\n- How would you verify delta semantics across multiple upstreams?\n- How would you simulate network partitions and ensure no data loss during recovery?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-5",
        "questionId": "q-26535",
        "question": "Design a beginner-friendly API test for POST /payments, which accepts amount, currency, and source (card token). Include idempotency header X-Idempotency-Key, verify that retries with the same key do not double-charge, test 409 for duplicate keys, and cover validation errors (400/422) and gateway timeout handling with backoff. Specify tooling, test data, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Make a first call with key 'abc' returning 201 and a transaction_id. Replay with the same key; gateway mocks return 200 with the same id or 409; ensure no extra charges. Validate 400/422 for bad input",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Validate RBAC, per-tenant data isolation, dynamic rate limits, and canary flags in a multi-tenant gateway. Create tenants A/B, mint tokens, and exercise endpoints with cross-tenant access attempts; bu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a concrete test approach: validate multipart CSV content-type and max 5MB; use Idempotency-Key to prevent duplicates; expect 202 with a numeric jobId; poll GET /imports/{jobId} for progress un",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Verify 202 and trackingId on first call; simulate duplicates with the same payload and Idempotency-Key to ensure no duplicates in downstream stores; validate per-partition ordering by including a part",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for idempotency, retry behavior, and gateway interactions are critical in payments APIs. This question probes practical validation of idempotent semantics, duplicate handling, and robust error paths with realistic tooling.\n\n## Key Concepts\n\n- Idempotency keys and idempotent POSTs\n- Duplicate-key handling (201 vs 409) and charge integrity\n- Validation paths (400, 422) and gateway timeouts\n- Retry strategies with backoff and state isolation\n- Mocked external gateway and observability\n\n## Code Example\n\n```javascript\n// Example: using supertest + nock for idempotent POST /payments\nconst request = require('supertest');\nconst gateway = require('nock')('https://payments.example.com');\n\ndescribe('POST /payments idempotency', () => {\n  test('first call succeeds, retry reuses idempotency key without double charge', async () => {\n    gateway.post('/charge').reply(200, {transaction_id: 'tx-1'});\n    const first = await request(app)\n      .post('/payments')\n      .set('X-Idempotency-Key', 'abc')\n      .send({amount: 1000, currency: 'USD', source: 'tok_visa'});\n    expect(first.status).toBe(201);\n\n    gateway.post('/charge').reply(200, {transaction_id: 'tx-1'});\n    const second = await request(app)\n      .post('/payments')\n      .set('X-Idempotency-Key', 'abc')\n      .send({amount: 1000, currency: 'USD', source: 'tok_visa'});\n    expect(second.status).toBe(200);\n  });\n});\n```\n\n## Follow-up Questions\n\n- How would you adapt tests if X-Idempotency-Key is optional?\n- How would you validate concurrency with multiple clients submitting the same key simultaneously?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-6",
        "questionId": "q-21349",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /payments that charges a customer card and enforces idempotency with an Idempotency-Key header. Specify how you verify no duplicate charges when multiple identical requests arrive, how different keys yield separate charges, and how you validate failure modes (400, 401/403, 429, 5xx). Include concurrency tests, tooling, and per-tenant data isolation considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Key verification points: execute concurrent POST requests with the same idempotency key to confirm only a single charge occurs and the same transaction ID is returned; use distinct keys to verify separate charges are cr…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose an automated end-to-end plan validating idempotent ingestion with unique keys, strict CSV schema validation (types, nullability, field counts, enum constraints), downstream dispatch to Kafka with at-least-once s…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would validate end-to-end by using idempotency keys, simulating replays, verifying signatures, and ensuring duplicates land in a DLQ. Tests cover missing/invalid signatures, duplicate payloads, fiel",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Idempotent bulk import test: POST /imports/users with 100k records and an idempotency-key. Re-send with the same key to verify no duplicates. Inject partial downstream analytics failures to validate p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTesting idempotency in payment systems is critical to prevent duplicate charges, ensure proper auditability, and verify the idempotency store functions correctly under concurrent conditions.\n\n## Key Concepts\n- Idempotency keys, race conditions, and optimistic locking strategies\n- Concurrency testing and HTTP status code handling (400, 401/403, 429, 5xx)\n- Ledger reconciliation, retry logic, and idempotency store semantics\n- Per-tenant data isolation and key scoping\n\n## Test Approach\n**Duplicate Prevention**: Submit 5 concurrent requests with the same Idempotency-Key header. Assert only one charge is created and all responses return identical transaction IDs.\n\n**Distinct Keys**: Submit requests with different idempotency keys. Assert separate charges are created with unique transaction IDs.\n\n**Failure Mode Validation**:\n- 400: Invalid request body returns immediately without creating idempotency record\n- 401/403: Authentication failures create no charge; invalid credentials return error\n- 429: Implement exponential backoff with jitter; subsequent retries within window return cached response\n- 5xx: Transient failures should be retryable; idempotency key ensures no duplicate charge\n\n**Per-Tenant Isolation**: Append tenant ID to idempotency key scope to ensure keys are isolated per customer.\n\n## Code Example\n```javascript\n// Test concurrent requests with same idempotency key\nasync function testIdempotencySameKey() {\n  const idempotencyKey = 'key_' + Date.now();\n  const requests = Array(5).fill(null).map(() => \n    fetch('/payments', {\n      method: 'POST',\n      headers: {\n        'Idempotency-Key': idempotencyKey,\n        'Content-Type': 'application/json'\n      },\n      body: JSON.stringify({ amount: 1000, currency: 'USD' })\n    })\n  );\n  \n  const responses = await Promise.all(requests);\n  const transactionIds = await Promise.all(responses.map(r => r.json()));\n  \n  // Assert all return same transaction ID\n  const uniqueIds = new Set(transactionIds.map(r => r.transactionId));\n  console.assert(uniqueIds.size === 1, 'Should have one transaction');\n}\n\n// Test different keys create separate charges\nasync function testIdempotencyDifferentKeys() {\n  const responses = await Promise.all(\n    ['key1', 'key2', 'key3'].map(key => \n      fetch('/payments', {\n        method: 'POST',\n        headers: { 'Idempotency-Key': key, 'Content-Type': 'application/json' },\n        body: JSON.stringify({ amount: 1000, currency: 'USD' })\n      })\n    )\n  );\n  \n  const transactionIds = await Promise.all(responses.map(r => r.json()));\n  const uniqueIds = new Set(transactionIds.map(r => r.transactionId));\n  \n  console.assert(uniqueIds.size === 3, 'Should have three transactions');\n}\n```\n\n## Tooling Recommendations\n- Use k6 or Artillery for load testing\n- Use Redis or PostgreSQL for idempotency store\n- Implement TTL-based cleanup for idempotency keys (24-48 hour window)",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-7",
        "questionId": "q-3037",
        "question": "You're testing a public product catalog API: GET /products with pagination (page, limit), optional filters category and minPrice, and returns fields id, name, category, price, inStock. Draft a practical, beginner-friendly test plan: include concrete test cases for pagination boundaries, filter correctness, invalid inputs, and response headers (Cache-Control, ETag). Propose tooling (Postman/Newman or curl scripts) and sample asserts?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Test Plan for GET /products API ### Test Cases **Basic Pagination:** - GET /products?page=1&limit=10 → Expect 200 with ≤10 items containing id,name,category,price,inStock - GET /products?page=999 → Expect 200 with em…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage strict PATCH semantics: verify only provided fields merge into the resource; undefined fields remain untouched, null can delete. Validate 412 on stale If-Match, and 200 with a refreshed ETag",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design and implement a comprehensive end-to-end test strategy for an asynchronous bulk import API where POST /bulk/import accepts CSV uploads and returns a jobId, while GET /bulk/import/{jobId} provides real-time status…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ensure exactly-once semantics for POST /payments using an idempotency-key store; simulate concurrent duplicate requests and verify only a single ledger entry is created. Mock bank responses with varyi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTo evaluate practical API testing skills, focusing on pagination contracts, filtering logic, error handling, and caching header validation for real-world endpoints.\n\n## Key Concepts\n\n- **Pagination Contracts:** Page/limit parameters and boundary conditions\n- **Filtering Correctness:** Category and price filter validation\n- **HTTP Status Semantics:** Proper 400 errors for invalid inputs\n- **Caching Headers:** ETag and Cache-Control presence verification\n- **Test Automation:** Tooling selection and assertion strategies\n\n## Code Example\n\n```javascript\n// Postman test example\npm.test(\"Pagination limit respected\", function() {\n    const response = pm.response.json();\n    pm.expect(response.items.length).to.be.at most(parseInt(pm.request.url.query.get('limit')));\n});\n```\n\n## Follow-up Questions\n\n- How would you adapt tests if /products also supports sortBy and search parameters?\n- What approach would you take for testing rate limiting on this endpoint?\n- How would you handle testing when the API returns paginated metadata (total count, totalPages)?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-8",
        "questionId": "q-21756",
        "question": "Design an end-to-end test plan for a multi-tenant WebSocket live-feed API. Validate at-least-once delivery using messageId dedupe, per-tenant isolation (no cross-talk), reconnect handling (auto-reconnect with backoff), and backpressure (pause/resume). Simulate regional broker outages and verify resubscribe and replay of missed events. Use k6/ws, a per-tenant seed in Redis, and define SLA: p95 latency < 400ms?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use the Idempotency-Key header to guard POST /cart/items. Re-run identical requests with same key and confirm no duplicates; on transient 5xx, retry using same key and compare final item count; ensure",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a concrete test approach: validate multipart CSV content-type and max 5MB; use Idempotency-Key to prevent duplicates; expect 202 with a numeric jobId; poll GET /imports/{jobId} for progress un",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement idempotency via a write-ahead dedup table keyed by idempotency_key (resource_id, status, amount, charge_id). Retries return cached result unless the original failed irrecoverably. Test by re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Model 3 tenants in parallel, generate unique messageIds to verify deduplication and at-least-once delivery behavior at scale. Instrument per-tenant queues to confirm zero cross-tenant data leakage. Test client reconnect…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the candidate's ability to design complex end-to-end tests for a streaming API with multi-tenancy, delivery guarantees, and resilience under failure conditions.\n\n## Key Concepts\n- WebSocket streaming with per-tenant isolation and backpressure control\n- Message deduplication via unique messageIds for at-least-once delivery\n- Reconnection logic with exponential backoff and replay semantics\n- Fault injection for broker failover and regional outages\n- Latency and throughput SLA validation\n\n## Code Example\n```javascript\nconst WebSocket = require('ws');\nconst { Kafka } = require('kafkajs');\nconst Redis = require('ioredis');\n\nconst TENANTS = ['tenant-a', 'tenant-b', 'tenant-c'];\nconst SLA_P95_LATENCY_MS = 400;\nconst MAX_RECONNECT_ATTEMPTS = 10;\nconst BASE_BACKOFF_MS = 100;\n\nclass MultiTenantTest {\n  constructor() {\n    this.redis = new Redis();\n    this.connections = new Map();\n    this.messageIds = new Map();\n    this.latencies = [];\n  }\n\n  async setup() {\n    await this.redis.flushdb();\n    for (const tenant of TENANTS) {\n      await this.redis.set(`seed:${tenant}`, Math.random().toString(36));\n    }\n  }\n\n  async connectTenant(tenant) {\n    const ws = new WebSocket('wss://api.example.com/feed');\n    const clientId = `${tenant}-${Date.now()}`;\n    \n    ws.on('open', () => {\n      ws.send(JSON.stringify({ type: 'subscribe', tenant, clientId }));\n    });\n\n    ws.on('message', (data) => {\n      const msg = JSON.parse(data);\n      if (msg.tenant !== tenant) {\n        throw new Error(`CROSS-TENANT LEAK: ${tenant} received ${msg.tenant} data`);\n      }\n      const latency = Date.now() - msg.timestamp;\n      this.latencies.push(latency);\n      this.trackMessageId(msg.messageId, tenant);\n    });\n\n    this.connections.set(tenant, ws);\n    return ws;\n  }\n\n  trackMessageId(messageId, tenant) {\n    if (this.messageIds.has(messageId)) {\n      const existing = this.messageIds.get(messageId);\n      console.log(`DUPLICATE: ${messageId} from ${existing} to ${tenant}`);\n    }\n    this.messageIds.set(messageId, tenant);\n  }\n\n  async simulateBackpressure(tenant) {\n    const ws = this.connections.get(tenant);\n    ws.send(JSON.stringify({ type: 'pause' }));\n  }\n\n  async resumeFeed(tenant) {\n    const ws = this.connections.get(tenant);\n    ws.send(JSON.stringify({ type: 'resume' }));\n  }\n\n  async simulateBrokerOutage(region) {\n    console.log(`Simulating broker outage in ${region}`);\n  }\n\n  calculatePercentile(arr, p) {\n    const sorted = arr.slice().sort((a, b) => a - b);\n    const idx = Math.ceil((p / 100) * sorted.length) - 1;\n    return sorted[Math.max(0, idx)];\n  }\n\n  async validateSLA() {\n    const p95 = this.calculatePercentile(this.latencies, 95);\n    console.log(`P95 Latency: ${p95}ms`);\n    if (p95 > SLA_P95_LATENCY_MS) {\n      throw new Error(`SLA VIOLATION: p95 ${p95}ms exceeds ${SLA_P95_LATENCY_MS}ms`);\n    }\n  }\n}\n```\n\nThis test framework validates all key requirements: per-tenant isolation, at-least-once delivery through messageId deduplication, backpressure handling, and latency SLAs under simulated failure conditions.",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-9",
        "questionId": "q-10960",
        "question": "Design a beginner-friendly test plan for an API endpoint GET /api/items with pagination (page, size). Include per-user rate limiting: after 60 requests per minute, the API returns 429 with a Retry-After header. Provide concrete test cases for boundary conditions, verify pagination links stay correct during bursts, and outline how client-side backoff should respect Retry-After. Include tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose end-to-end tests for POST /reports with an idempotency-key. Verify that the first call enqueues one job and returns a job-id; a second call with the same idempotency-key while the first is run",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan should use a test runner (e.g., Postman or k6) to emit per-user bursts, verify 200s up to the limit and 429 with Retry-After thereafter; validate Link headers for next/prev pages remain correct d",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "First request for a tenant fetches the policy and caches it for 60s; subsequent requests reuse the cache. Change the tenant policy via admin and verify masking changes after expiry or on manual refres",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose end-to-end tests simulating 50k concurrent edits via REST for create/update and WebSocket deltas, asserting CRDT/OT convergence to a single canonical state. Verify idempotent delta application",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes pagination behavior, rate limiting, and backoff strategies in realistic API testing scenarios.\n\n## Key Concepts\n\n- Pagination semantics (page, size, next/prev links)\n- Per-user rate limits and Retry-After handling\n- Concurrency and client backoff\n\n## Code Example\n\n```javascript\nimport http from 'k6/http';\nimport { sleep, check } from 'k6';\nlet base = 'https://api.example.com';\nexport default function () {\n  let res = http.get(`${base}/api/items?page=1&size=50`);\n  check(res, { 'status is 200 or 429': (r) => r.status === 200 || r.status === 429 });\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt tests for a 3xx redirect or 500 error?\n- How would you modify for multi-region deployment?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-10",
        "questionId": "q-5096",
        "question": "Design a practical end-to-end test plan for a multi-tenant REST + WebSocket API with a rolling auth service; validate tenant isolation, token revocation propagation, streaming data integrity under bursts, and per-tenant quotas during a canary rollout; include concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Tests should cover idempotent behavior with a shared idempotency-key, verifying first call returns 201 and a job_id while duplicates return the same job_id. Validate retry semantics on transient netwo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an end-to-end test plan for a real-time notification API: REST /subscribe and WebSocket feed. Validate per-tenant isolation in a shared cluster, idempotent subscribe/unsubscribe, and in-order W",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose end-to-end latency tests (k6) targeting 95th percentile under peak load with <2s SLA; validate per-tenant isolation by using tenant_id headers and querying with mixed tenants to ensure no cros",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan end-to-end tests for a multi-tenant REST + WebSocket API with rolling auth service; validate tenant isolation, token revocation propagation, streaming data integrity under bursts, and per-tenant",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes your ability to craft end-to-end tests for multi-tenant APIs under live deployments, focusing on isolation, authorization refresh behavior, streaming reliability, and per-tenant resource controls.\n\n## Key Concepts\n\n- Tenant isolation across REST and WebSocket paths\n- Token revocation propagation and caching implications\n- Streaming data integrity: ordering, deduplication, replay safety\n- Rolling deploys, canary testing, and feature flags for auth\n- Per-tenant quotas and fault-injection strategies\n\n## Code Example\n\n```javascript\n// Example test scaffold (pseudo)\nimport { login, fetchWithToken, wsConnect } from './test-utils'\n\ntest('tenant isolation during canary rollout', async () => {\n  const tokenA = await login('tenantA')\n  const tokenB = await login('tenantB')\n\n  // REST: tenantA can access its data; tenantA cannot access tenantB data\n  const r1 = await fetchWithToken('/api/tenantA/data', tokenA)\n  const r2 = await fetchWithToken('/api/tenantB/data', tokenA)\n  expect(r1.ok).toBe(true)\n  expect(r2.status).toBe(403)\n\n  // WebSocket: subscribe as tenantA and ensure messages belong to tenantA\n  const wsA = await wsConnect('/ws/stream', tokenA)\n  const msgA = await wsA.next()\n  expect(msgA.tenant).toBe('tenantA')\n  wsA.close()\n\n  // Revocation: revoke tokenA and verify access denied\n  await revokeToken(tokenA)\n  const r3 = await fetchWithToken('/api/tenantA/data', tokenA)\n  expect(r3.status).toBe(401)\n})\n```\n\n## Follow-up Questions\n\n- How would you validate that revocation propagates across multiple auth nodes?\n- How do you measure coverage impact when enabling auth-canary for streaming endpoints?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-11",
        "questionId": "q-6682",
        "question": "Design a testing strategy for a real-time alert API used by three tenants, with REST GET /alerts?since= and SSE /alerts/stream. A per-tenant feature flag toggles fields (severity, source, batchId). Outline how you’d validate backward-compatible schema changes across REST and SSE payloads, enforce per-tenant RBAC under high concurrency, implement consumer-driven contracts between REST and stream, and simulate outages (admin service, message broker). Include concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a test plan that uses an Idempotency-Key on POST /checkout; simulate downstream faults via mocks for auth, inventory, and payment with a fault-injection tool (e.g., WireMock) to trigger 2s time",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an end-to-end test for POST /payments/charge with Idempotency-Key. Validate duplicate submissions reuse same chargeId, no double debiting, and idempotent retry behavior. Simulate bank outage",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose contract-driven tests (PACT) for REST and SSE payloads per tenant, validating ON/OFF flag states and optional fields. Use RBAC checks under bursts, ensure event ordering and dedup under SSE re",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly test plan for POST /tasks that enqueues a long-running job and notifies completion via a webhook to a registered URL. Include how you would verify webhook delivery guarantee",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests spanning REST and streaming with per-tenant flags and real-time guarantees are a practical, high-stakes problem in production systems. This probes cross-service contracts, schema evolution discipline, RBAC under load, and outage resilience.\n\n## Key Concepts\n- Consumer-driven contracts (PACT/OpenAPI)\n- SSE streaming semantics and ordering\n- Schema evolution with feature flags per tenant\n- Per-tenant RBAC under concurrency\n- Outage simulations (admin service, broker)\n\n## Code Example\n```javascript\n// Pact contract sketch for REST -> SSE payload validation\nconst { Pact } = require('@pact-foundation/pact');\n// define consumer/provider, interactions for ON/OFF flag states\n```\n\n## Follow-up Questions\n- How would you version and deprecate fields in contracts without breaking tenants?\n- How would you detect drift across tenants when flags change at runtime?\n\n## Diagram\nflowchart TD\n  REST[REST Call] --> CONTRACT{Contract Match}\n  CONTRACT --> SSE[Validate SSE payload]\n  SSE --> OUTCOME[Outcome]",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-12",
        "questionId": "q-20568",
        "question": "Design a practical API testing plan for a multi-tenant analytics API with two endpoints: GET /analytics/v2/usage?tenantId={id}&page={n}&size={s} (paginated) and GET /analytics/v2/usage/export?tenantId={id} (CSV). Data is cached per-tenant in Redis and refreshed by a background worker from a data warehouse. Outline concrete test cases to verify per-tenant isolation, pagination correctness under high concurrency, and export integrity during cache eviction and Redis outages. Include tooling and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use consumer-driven contracts between the gateway, adapters, and partner webhooks, with strictly versioned payload schemas. Implement contract tests (Pact) that cover both old and new schemas during transition periods,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a comprehensive API testing plan for a multi-tenant analytics API with the following test scenarios: **1. Per-Tenant Data Isolation** - Verify Redis keys follow the pattern `tenant:{id}:usage` - Cross-tenant requ…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test idempotent creation with repeated POST /payments using the same Idempotency-Key to ensure a single record is created and same Location is returned. Validate 400 on invalid payload, 401 on missing",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a comprehensive API testing plan that validates per-user RBAC enforcement across a distributed architecture consisting of a central policy decision point (PDP) and regional edge caches. The plan should cover thre…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the candidate's ability to design practical test strategies for complex, cache-driven multi-tenant systems. It tests understanding of data isolation, concurrency handling, and resilience patterns.\n\n## Key Concepts\n- Per-tenant data isolation in Redis cache\n- Pagination semantics under high concurrency\n- CSV export validity, encoding, and integrity\n- Cache eviction patterns and Redis outage handling\n- Background worker refresh logic and data consistency\n\n## Code Example\n```javascript\n// Pagination concurrency test (k6)\nexport default function () {\n  const tenantId = ['tenant-a', 'tenant-b'][Math.floor(Math.random() * 2)];\n  const page = Math.floor(Math.random() * 10) + 1;\n  const size = [10, 25, 50, 100][Math.floor(Math.random() * 4)];\n\n  http.get(\n    `https://api.example.com/analytics/v2/usage?tenantId=${tenantId}&page=${page}&size=${size}`,\n    { headers: { 'Authorization': `Bearer ${authToken}` } }\n  );\n\n  sleep(1);\n}\n```\n\n## What Distinguishes Strong Candidates\nStrong candidates demonstrate:\n- Understanding of Redis key patterns and tenant scoping\n- Knowledge of pagination edge cases (empty pages, boundary conditions)\n- Familiarity with cache-aside pattern and staleness handling\n- Ability to design tests for race conditions and concurrent access patterns\n- Awareness of CSV encoding issues and large file handling",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-13",
        "questionId": "q-29803",
        "question": "Design an advanced testing plan for a globally distributed, federated GraphQL API with per-field authorization and a shared cache. Include per-field contract tests for multiple roles, schema-drift checks across regions, latency targets, and cache invalidation during schema changes. Provide concrete tooling, data setup, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a testing strategy for a globally distributed, federated GraphQL API with per-field authorization and a shared cache. Include per-field contract tests for multiple roles, schema-drift checks ac",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Validate signature (401/403 for invalid), ensure idempotent handling by ignoring duplicate event_id, confirm successful processing once per id, and test retry behavior with 3 attempts and backoff on 5",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a concrete test approach: validate multipart CSV content-type and max 5MB; use Idempotency-Key to prevent duplicates; expect 202 with a numeric jobId; poll GET /imports/{jobId} for progress un",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate correctness under eventual consistency between the read-model and streaming data: verify read-your-writes within a defined drift window, ensure per-tenant isolation under concurrency, simulat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAddresses a new angle: federation, field-level access controls, and cross-region caching. Tests must verify no data leakage, consistent schemas, and performance under regional latency variations.\n\n## Key Concepts\n\n- GraphQL federation and schema drift\n- Field-level authorization per role\n- Cross-region latency and cache invalidation\n- Contract testing and end-to-end coverage\n\n## Code Example\n\n```javascript\n// Example contract test skeleton (pseudo)\ndescribe('Field-level auth contracts', () => {\n  it('returns permitted fields for role: user', async () => {\n    // query with user role; assert disallowed fields are absent\n  })\n  it('restricts sensitive fields for role: guest', async () => {\n    // query; assert sensitive fields are missing\n  })\n})\n```\n\n## Follow-up Questions\n\n- How would you simulate cross-region latency spikes and measure per-region drift?\n- What metrics define acceptable schema drift and cache coherence during hot schema changes?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-14",
        "questionId": "q-9531",
        "question": "Design a consumer-driven contract testing plan for a high-throughput user API gateway that forwards REST requests to user-service and emits user-change events to Kafka. Provide Pact-based contracts for both REST responses and Kafka events, ensure backward compatibility across API versions, simulate out-of-order event delivery, idempotent retries, and partial downstream outages; outline data generators, concrete test cases, and CI integration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose end-to-end tests for POST /reports with an idempotency-key. Verify that the first call enqueues one job and returns a job-id; a second call with the same idempotency-key while the first is run",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an idempotency key per /payments call to prevent duplicates; mock downstreams (fraud, bank, ledger) and verify a single transaction and consistent ledger for repeated requests with the same key. S",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would mock latency and failures in each downstream using WireMock, then validate end-to-end correctness by asserting total equals sum(prices)+tax+shipping, currency conversion per region, and timest",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt Pact contracts for REST and Kafka events, versioned per API version, and run consumer verifications in CI with a central broker and provider verifications per version. Simulate out-of-order even",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes ability to design cross-service contract testing for REST and event streams, with versioning, resilience, and CI integration.\n\n## Key Concepts\n- Consumer-driven contracts\n- Pact and contract brokers\n- REST and Kafka event contracts\n- Versioning and backward compatibility\n- Out-of-order delivery and idempotency\n- CI/CD integration\n\n## Code Example\n\n```javascript\n// Pact consumer test sketch for REST\nconst { Pact } = require('@pact-foundation/pact');\n// ... test configuration\n```\n\n## Follow-up Questions\n- How would you handle contract drift across versions?\n- How would you monitor for stale verifications in production?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-15",
        "questionId": "q-28652",
        "question": "For a multi-tenant anomaly-detection API with POST /ingest, GET /results/{id}, and GET /subscriptions, design an advanced API testing plan that validates per-tenant isolation, schema versioning, and canary rollouts of new model versions via a feature flag. Include latency targets under burst load, contract tests between ingest and downstream processors, idempotency checks, and resilience to downstream outages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use consumer-driven contracts (Pact) to lock gateway-downstream contracts per tenant, plus OpenAPI diff to detect drift. Add runtime shadow tests behind feature flags, and deprecation windows for endp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Tests should cover: (1) payload validation for title and dueDate; (2) auth and tenant scoping via X-API-KEY; (3) data isolation by creating two tenants and verifying no cross-tenant visibility; (4) ra",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a comprehensive test strategy that includes tenant-scoped test data generation to validate per-tenant isolation guarantees. Implement contract tests using Pact between the POST /ingest endpoint and downstream pro…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "## Test Plan for GET /products API ### Test Cases **Basic Pagination:** - GET /products?page=1&limit=10 → Expect 200 with ≤10 items containing id,name,category,price,inStock - GET /products?page=999 → Expect 200 with em…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates advanced API testing knowledge, specifically multitenant isolation, schema versioning, canary deployments, and production-like failure scenarios. It also probes understanding of contract testing discipline and observable latency behavior under burst traffic conditions.\n\n## Key Concepts\n- Contract testing with Pact between ingestion and downstream processing services\n- Per-tenant isolation and data residency guarantees across test scenarios\n- Schema versioning strategies with backward compatibility validation\n- Feature-flag-driven canary rollouts with quantitative acceptance criteria\n- Latency targets (p95/p99) under burst load and SLA alignment\n- Idempotency guarantees and DLQ/resilience patterns for downstream outages\n\n## Code Example\n```javascript\n// Pact contract test: consumer side (ingest service)\nconst { Pact } = require('@pact-foundation/pact');\nconst { like, eachLike } = require('pact-matchers');\n\nconst provider = new Pact({\n  consumer: 'anomaly-detect-api',\n  provider: 'downstream-processor',\n  port: 1234\n});\n\nbefore(() => provider.setup());\nafter(() => provider.finalize());\n\ndescribe('POST /ingest contract', () => {\n  it('validates anomaly payload schema', async () => {\n    await provider.addInteraction({\n      state: 'tenant abc-123 exists',\n      uponReceiving: 'a valid anomaly ingest request',\n      withRequest: {\n        method: 'POST',\n        path: '/ingest',\n        headers: { 'Content-Type': 'application/json', 'X-Tenant-ID': 'abc-123' },\n        body: { metrics: eachLike({ timestamp: 1700000000, value: 42.5 }) }\n      },\n      willRespondWith: {\n        status: 202,\n        body: like({ ingestionId: 'uuid-here' })\n      }\n    });\n  });\n});\n```",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-16",
        "questionId": "q-29914",
        "question": "Design an advanced API testing plan for a streaming price-notification API that adds a new /prices/stream SSE endpoint on top of GET /prices?symbols=... for multi-tenant workloads with per-tenant quotas. Validate streaming contract (schema, order, timestamps, replay protection), per-tenant isolation under burst traffic, 95th percentile latency, and graceful fallback to a batch export when the stream stalls or downstream latency spikes. Include seeds, tooling, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: verify streaming contract: JSON payload schema, sequence order, timestamps, replay protection; burst test for per-tenant isolation; latency checks 95th percentile; simulate downstream outage to",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: cache-aware, multi-tenant ML inference test. Verify keys use tenant_id + model_version and eviction on changes; simulate warm/cold starts with 1000 concurrent requests (k6); monitor latency, err",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Idempotent bulk import test: POST /imports/users with 100k records and an idempotency-key. Re-send with the same key to verify no duplicates. Inject partial downstream analytics failures to validate p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a bulk upsert test suite focusing on JSONL payloads up to 1000 records, validating per-record status, atomic batch vs partial commits, idempotent retries with the same idempotency-key, and ten",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nWhy: testing streaming APIs with multi-tenant quotas and graceful degradation is common in fintech and data platforms; it surfaces contract testing, latency guarantees, and resilience.\n\n## Key Concepts\n- SSE streaming contract, idempotency, replay protection\n- Tenant isolation under burst traffic\n- Latency percentiles and fallback paths\n- End-to-end tooling integration (contract tests, load tests, mocks)\n\n## Code Example\n```javascript\n// Example: k6-like pseudocode for SSE test (conceptual)\nimport http from 'k6/http';\nimport { check } from 'k6';\nexport default function () {\n  let r = http.get('https://api/.../prices/stream?symbols=AAPL');\n  check(r, { 'status 200': (r) => r.status === 200 });\n}\n```\n\n## Follow-up Questions\n- How would you extend tests to validate time-based quotas and sticky sessions across region failovers?\n- How would you integrate observed anomalies into a circuit-breaker dashboard for real-time remediation?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-17",
        "questionId": "q-1957",
        "question": "Design a concrete, end-to-end automated test plan for an asynchronous data-processing API: POST /data/process returns 202 with an operation-id; messages flow via Kafka to downstream workers; results exposed at GET /data/process/{operationId}/result. Include idempotency tests (Idempotency-Key), eventual consistency with versioning, and fault-injection scenarios (Kafka outage, consumer restart, DB outage) with concrete test cases and tooling recommendations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A comprehensive API testing plan for a gateway serving both REST and WebSocket streaming endpoints in a real-time telemetry system should cover the following areas: REST and WebSocket paths, per-tenant isolation, contra…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Develop an end-to-end test plan for an async /data/process flow: verify idempotent POSTs with Idempotency-Key, check that duplicates yield a single operation, poll results for eventual consistency and",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a comprehensive API test plan for a geo-distributed telemetry platform with the following test scenarios: **1. Exactly-Once Ingestion & Idempotency Testing** - Implement idempotent ingestion via the `Idempotency-…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement end-to-end tests that verify cache coherence and bus-backfill timing. Create a contract test for GET /prices/{symbol} returning price, timestamp, and etag; simulate rapid invalidations and b",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEnd-to-end testing of async pipelines with idempotency and fault tolerance is crucial at scale; this question probes API contracts, event delivery, deduplication, and failure modes.\n\n## Key Concepts\n- Idempotency-Key handling across retries\n- Async processing and at-least-once delivery\n- Event ordering and versioned results\n- Fault-injection and chaos testing (Kafka, DB, consumer restarts)\n- Tooling: Testcontainers, Kafka, Jaeger, backoff/retry strategies\n\n## Code Example\n```javascript\n// Pseudo-test: idempotent POST handler test\nconst res1 = await post('/data/process', {payload}, {headers: {'Idempotency-Key':'abc-123'}});\nconst res2 = await post('/data/process', {payload}, {headers: {'Idempotency-Key':'abc-123'}});\nexpect(res1.status).toBe(202);\nexpect(res2.status).toBe(202);\n```\n\n## Follow-up Questions\n- How would you validate deduplication at the consumer side?\n- What metrics would you collect to detect processing skew?\n- How would you implement replay protection and idempotent cleanup in the downstream store?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-18",
        "questionId": "q-16016",
        "question": "For a REST API POST /payments that initiates an asynchronous charge, returns 202 with Location: /payments/{id}, and supports Idempotency-Key header to prevent duplicate charges, design a beginner-friendly test plan. Include concrete test cases to verify: (a) same-idempotency requests yield same payment ID and no duplicate charge, (b) different keys create separate payments, (c) status polling to /payments/{id} eventually reaches completed or failed, (d) 400 on missing/invalid Idempotency-Key, (e) behavior under simulated downstream failure while preserving idempotency. Also mention tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Submit a POST /payments with Idempotency-Key: key1; expect 202 and a Location header for /payments/{id}. Resubmit with the same key; verify the response reuses the same id and that no extra charges we",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Validate idempotency by replaying the same POST with a single Idempotency-Key and confirm only one order is created and responses match; test different keys create separate orders; simulate out-of-sto",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test Plan for REST API POST /checkout with Idempotency-Key: 1. **Happy Path**: Valid cart payload returns 202 with unique jobId 2. **Idempotency**: Same Idempotency-Key returns 200 with identical jobId, no duplicate job…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I’d propose a PyTest-based plan for POST /payments with userId, amount, currency and an Idempotency-Key header. Test cases: 1) first submission returns 201 and an id; 2) same Idempotency-Key returns 2",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests an asynchronous, idempotent flow common in payments; exercises end-to-end behavior, including 202 semantics, Location header, polling, and fault tolerance.\n\n## Key Concepts\n- Idempotency keys prevent duplicate side effects\n- Asynchronous processing and status polling\n- Proper error handling for missing/invalid headers\n- Downstream failure resilience with preserved idempotency\n\n## Code Example\n```javascript\n// Jest-like sketch\ntest('idempotent POST /payments', async () => {\n  const key = 'idem-123';\n  const r1 = await post('/payments', { amount: 100 }, { 'Idempotency-Key': key });\n  const r2 = await post('/payments', { amount: 100 }, { 'Idempotency-Key': key });\n  expect(r1.status).toBe(202);\n  expect(r1.headers.Location).toContain('/payments/');\n  expect(r2.status).toBe(202);\n  expect(r2.body.id).toBe(r1.body.id);\n  // poll until final state\n  const id = r1.body.id;\n  let s = await get(`/payments/${id}/status`);\n  while (s.state === 'pending') s = await get(`/payments/${id}/status`);\n});\n```\n\n## Follow-up Questions\n- How would you extend tests for long-running payments and timeouts?\n- How would you test idempotency across multiple regions or microservices?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-19",
        "questionId": "q-17268",
        "question": "Design a beginner-friendly test plan for GET /profiles/{id} that supports conditional requests with ETag and If-None-Match. Outline concrete test cases for 200 with body, 304 Not Modified, 412 Precondition Failed, and post-update invalidation. Include tooling suggestions and how to verify caching behavior in a real-world app like a social profile service?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To test GET /profiles/{id} with conditional requests: fetch a fresh resource to capture ETag and Last-Modified. Re-request with If-None-Match and If-Modified-Since to expect 304. Update the resource a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Explain a plan to test idempotent job creation, concurrency, and DLQ: use an idempotency-key, verify only one job created and subsequent POST returns same job ID; simulate 10k parallel POSTs with same",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Draft a test plan: verify idempotency by sending duplicates for the same event_id and asserting a single aggregate in GET /events/{event_id}; simulate downstream outages to ensure at-least-once delive",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a contract-driven end-to-end suite with Pact between rides, drivers, and payments services, plus chaos testing. Verify idempotency on POST /rides, ensure eventual consistency via rea",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests basic HTTP caching semantics that many API failures hinge on. It introduces deterministic verification of status codes tied to caching headers.\n\n## Key Concepts\n\n- ETag, If-None-Match, Last-Modified, If-Modified-Since\n- 200 vs 304 vs 412 vs 404/410 handling\n- Test data isolation and cleanup\n\n## Code Example\n\n```javascript\n// Example test skeleton in Jest + supertest\n```\n\n## Follow-up Questions\n\n- How would you automate this in CI and handle flaky timestamps?\n- How does cache invalidation interact with bulk updates?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-20",
        "questionId": "q-8736",
        "question": "Design an end-to-end test plan for a multi-region balance service with REST GET /accounts/{id} and streaming /accounts/{id}/stream that emits balance updates. Include high-throughput scenarios, idempotent PATCH upserts, cross-region cache invalidation, and failure modes (regional blackout, streaming backpressure). Provide concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a concrete GraphQL test plan: validate tenant isolation via token scopes and resource filtering; enforce maximum query depth (e.g., 8 levels) and field whitelisting for persisted queries; test",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "End-to-end test plan for a real-time WebSocket inference API with canary rollout and per-tenant masking. Validate tenant-specific payloads, ensure canary routes correctly per region, test backpressure handling, verify m…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy three-region replicas; drive high update rates with k6 against PATCH and GET; verify GET returns latest balance after upserts and streaming delivers deduplicated, in-order events with sequence",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a distributed load test (e.g., k6) to saturate tenants with concurrent requests, asserting 429 responses with Retry-After. Verify admin-API quota updates propagate across nodes within a bounded wi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests multi-region data correctness, idempotent updates, and streaming reliability under outages—core in fintech-scale APIs.\n\n## Key Concepts\n\n- Cross-region consistency\n- Streaming deduplication\n- Cache invalidation\n- Failure injection\n\n## Code Example\n\n```javascript\n// Pseudocode: idempotent PATCH test\nasync function testIdempotentPatch(client, id, payload, repeats=3){\n  for(let i=0;i<repeats;i++){ await client.patch(`/accounts/${id}`, payload); }\n}\n\n// Pseudocode: streaming dedupe\nlet lastSeq = 0;\nstream.on('data', ev => { if (ev.seq>lastSeq){ lastSeq=ev.seq; process(ev); } });\n```\n\n## Follow-up Questions\n\n- How would you parameterize tests for different regional latencies?\n- How would you detect and recover from partial data divergence?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-21",
        "questionId": "q-17279",
        "question": "You have a REST API GET /accounts/{accountId}/balance that reads from a read-model updated by a separate event stream. Design an end-to-end test plan to validate eventual consistency, idempotent replays, and resilience under partial outages, including concrete test cases, tooling, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## End-to-End Test Plan for Asynchronous Job-Processing API ### API Endpoints - **POST /jobs** — Submit job with idempotency key - **GET /jobs/{jobId}/status** — Poll job status - **GET /jobs/{jobId}/result** — Retrieve…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose an end-to-end plan for a fleet telemetry API with REST history and a WebSocket real-time stream. Use a deterministic vehicle simulator (e.g., 1k msgs/min for 10m). Validate WS heartbeats, in-o",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a test suite using per-user keys, simulate burst and steady requests to GET /accounts/{accountId}/transactions?limit=50, verify 429 with Retry-After when limits exceed (100/h and 5/s), validat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To test a read-model based GET /accounts/{accountId}/balance backed by an event stream, verify eventual consistency and idempotency under delays. Use Kafka with testcontainers, inject delays in the co",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests for event-driven read models require understanding eventual consistency, replay safety, and resilience when streams lag or fail.\n\n## Key Concepts\n- Event-sourced writes vs read-models\n- Idempotent event replay handling\n- Latency bounds and time-to-consistency\n- Downstream outage simulation and recovery\n\n## Code Example\n```javascript\n// Pseudo: produce BalanceUpdated events and poll read-model balance\n```\n\n## Follow-up Questions\n- How would you quantify TTR and what instrumentation would you add?\n- How would you test cross-account causality when delays affect aggregates?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-22",
        "questionId": "q-4664",
        "question": "Design an automated end-to-end test plan for an API that exposes a REST endpoint GET /products/{id} and a persistent WebSocket stream at /ws/products/updates that pushes real-time delta updates (price, availability, promotions) per product across regions. The plan should test cross-service consistency, per-tenant isolation, reconnection/backoff, and failure modes (downstream cache or search index). Include concrete test cases, tooling suggestions, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan example: write end-to-end tests that fetch /products/{id} via REST and subscribe to /ws/products/updates for the same product, asserting REST data aligns with streamed deltas after each upda",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Outline an end-to-end plan for a high-volume asynchronous content-moderation API: multipart uploads, a background job, and /status /result endpoints; ensure per-tenant isolation and data masking. Incl",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an automated end-to-end plan: validate OpenAPI contracts with Pact; generate synthetic tenants and users; test cursor-based pagination edge cases; verify streaming delivers at-least-once updat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: use k6 to simulate 10k SSE clients on /prices/stream and concurrent GETs to /prices/{symbol}. Validate: 1) correct order and dedup by event_id; 2) backpressure and buffering under bursts; 3) con",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for REST accuracy and real-time streams, plus cross-service consistency, are critical for user-facing product data. This question probes end-to-end reliability under realistic failures and multi-tenant isolation.\n\n## Key Concepts\n\n- End-to-end testing of REST + streaming APIs\n- Data consistency between REST state and streaming deltas\n- Reconnect/backoff handling for WebSocket streams\n- Per-tenant isolation and security controls\n- Failure modes: downstream cache and search index outages\n- Tooling for orchestration, mocks, and load testing\n\n## Code Example\n\n```javascript\n// Node-like test sketch (using ws and axios)\nconst axios = require('axios');\nconst WebSocket = require('ws');\n\nasync function testProductSync(productId, tenantKey) {\n  const rest = await axios.get(`https://api.example.com/products/${productId}`, {\n    headers: { 'Authorization': `Bearer ${tenantKey}` }\n  });\n  const ws = new WebSocket(`wss://api.example.com/ws/products/updates?productId=${productId}`, {\n    headers: { 'Authorization': `Bearer ${tenantKey}` }\n  });\n  // Simplified: compare first REST payload with first delta, then on each delta\n  // close sockets, simulate reconnection delays, etc.\n}\n```\n\n## Follow-up Questions\n\n- How would you test late-arriving or out-of-order deltas?\n- How would you parameterize tests for multiple tenants and regions?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-23",
        "questionId": "q-27033",
        "question": "Design a beginner-friendly test plan for a REST API endpoint POST /bookings that creates a ride booking in a multi-tenant system. Each request must include a header X-API-KEY identifying the tenant and an idempotency-key. Validate input, per-tenant isolation, idempotency semantics (same key does not create duplicates), 429 Retry-After handling, and successful path. Include concrete test cases, tooling, and CI acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Test Cases** - **400 Bad Request**: Missing or invalid required fields (e.g., missing pickup_location or invalid passenger_count) - **401 Unauthorized / 403 Forbidden**: Missing or invalid X-API-KEY header; tenant not…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a test harness that exercises the REST enqueue endpoint and per-tenant WebSocket streaming. Verify: 1) strict per-tenant isolation ensuring tenants receive only their own events, 2) in-order delivery within sh…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose contract-driven tests (PACT) for REST and SSE payloads per tenant, validating ON/OFF flag states and optional fields. Use RBAC checks under bursts, ensure event ordering and dedup under SSE re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: test idempotency with the same Idempotency-Key across two rapid requests; expect a single charge and identical transaction_id. Test different keys yield separate charges. Simulate timeouts and r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates a candidate's understanding of idempotent POST semantics, per-tenant data isolation, and resilience testing—critical concepts in bookings and payments services.\n\n## Key Concepts\n\n- **Idempotency-key handling** prevents duplicate bookings when clients retry requests due to network failures or timeouts\n- **Per-tenant data isolation** is enforced via X-API-KEY validation, ensuring tenants cannot access each other's data\n- **API test coverage** includes input validation, authentication, rate limiting, and concurrent retry scenarios\n\n## Code Example\n\n```javascript\ndescribe('POST /bookings', () => {\n  it('creates booking with valid idempotency key', async () => {\n    const response1 = await api.post('/bookings', {\n      pickup_location: 'NYC',\n      passenger_count: 2\n    }, { headers: { 'X-API-KEY': 'tenant-a', 'idempotency-key': 'abc123' } });\n    \n    const response2 = await api.post('/bookings', {\n      pickup_location: 'NYC',\n      passenger_count: 2\n    }, { headers: { 'X-API-KEY': 'tenant-a', 'idempotency-key': 'abc123' } });\n\n    expect(response2.status).toBe(200);\n    expect(response2.body.booking_id).toBe(response1.body.booking_id);\n  });\n});\n```",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-24",
        "questionId": "q-29128",
        "question": "Design a practical API testing plan for a GraphQL Federation gateway that stitches multiple per-tenant subgraphs behind a single API endpoint. The system supports per-tenant schema variants, persisted queries, and a cache with TTL. Include concrete test cases for query complexity, data isolation, schema migration, token rotation during requests, and subgraph outage resilience. Specify tooling, acceptance criteria, and traffic simulation details?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create per-tenant mock listeners, replay the same eventId to verify idempotency (only one delivery). Inject outages to validate retries and exponential backoff; ensure no cross-tenant leakage. Verify",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## GraphQL Federation Gateway API Testing Plan ### 1. Testing Scope Overview **System Under Test:** - GraphQL Federation Gateway (single API endpoint) - Multiple per-tenant subgraphs with schema variants - Persisted que…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Outline a plan: for each tenant seed isolated data; use a mock feature-flag service to toggle per-tenant flags that alter the response payload schema. Validate data isolation by attempting cross-tenan",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Test Plan for POST /events Batch Ingestion API ### 1. Input Validation - Verify batch accepts maximum 100 events - Validate each event contains required fields: id, type, payload - Test payload size limits and type c…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nGraphQL federation with per-tenant architectures presents unique testing challenges that generic API testing approaches fail to address. This question evaluates the candidate's understanding of:\n\n- **Contract testing across federated services**: Ensuring subgraph schemas remain compatible with the gateway's composed schema\n- **Multi-tenant isolation verification**: Preventing data leakage between tenants sharing the same gateway\n- **Query complexity controls**: Protecting against malicious or expensive nested queries\n- **Resilience patterns**: Handling subgraph failures gracefully without exposing internal errors\n- **Security considerations**: Token rotation, persisted query validation, and authorization context preservation\n- **Operational concerns**: Cache invalidation, schema versioning, and monitoring during migrations\n\nA strong candidate will demonstrate:\n1. Familiarity with GraphQL-specific testing tools (not just generic REST testing approaches)\n2. Understanding of the security implications in multi-tenant GraphQL environments\n3. Practical experience with chaos engineering and resilience testing\n4. Awareness of the trade-offs between developer experience and production hardening",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-18T12:18:51.179Z",
    "version": 1
  }
]