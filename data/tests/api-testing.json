[
  {
    "id": "test-api-testing",
    "channelId": "api-testing",
    "channelName": "API Testing",
    "title": "API Testing Knowledge Test",
    "description": "Test your understanding of API Testing with 25 questions.",
    "questions": [
      {
        "id": "api-testing-q-0",
        "questionId": "q-2369",
        "question": "Design an automated test plan for a webhook listener at /webhooks/ci ingesting push events from a CI service. Each payload is signed with HMAC-SHA256 and delivered at-least-once to a Redis-backed event store. Outline concrete test cases for signature validation, replay/duplicate handling, event ordering or out-of-order resilience, retries/backoff, and failure modes (secret rotation, Redis outage). Include tooling suggestions and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test signature validation by sending tampered payloads (expect 401). Enforce idempotency by replaying the same event and ensuring no duplicate writes to Redis. Validate ordering vs. out-of-order event",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Validate signature (401/403 for invalid), ensure idempotent handling by ignoring duplicate event_id, confirm successful processing once per id, and test retry behavior with 3 attempts and backoff on 5",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design end-to-end tests for POST /rides, GET /rides/{id}, and /rides/stream. Use consumer-driven contracts and OpenAPI validation; simulate rotating tokens; verify per-tenant rate limits; test idempot",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Automated approach: seed tenants deterministically; mock the flag service to emit canary vs baseline values; run high-concurrency load tests (1k+ RPS) with k6 or Gatling; assert responses reflect the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nGauges ability to test security and reliability of inbound webhooks in a distributed system.\n\n## Key Concepts\n- Signature validation\n- Idempotency\n- Ordering vs concurrency\n- Retry/backoff strategies\n- Failure modes (secret rotation, Redis outage)\n\n## Code Example\n```javascript\n// Example test snippet for signature verification\nfunction verifySignature(payload, secret, signature) {\n  const h = crypto.createHmac('sha256', secret)\n  h.update(payload)\n  return `sha256=${h.digest('hex')}` === signature\n}\n```\n\n## Follow-up Questions\n- How would you test secret rotation without missing events?\n- How would you simulate Redis outage in CI?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-1",
        "questionId": "q-29914",
        "question": "Design an advanced API testing plan for a streaming price-notification API that adds a new /prices/stream SSE endpoint on top of GET /prices?symbols=... for multi-tenant workloads with per-tenant quotas. Validate streaming contract (schema, order, timestamps, replay protection), per-tenant isolation under burst traffic, 95th percentile latency, and graceful fallback to a batch export when the stream stalls or downstream latency spikes. Include seeds, tooling, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a contract-driven end-to-end suite with Pact between rides, drivers, and payments services, plus chaos testing. Verify idempotency on POST /rides, ensure eventual consistency via rea",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: verify streaming contract: JSON payload schema, sequence order, timestamps, replay protection; burst test for per-tenant isolation; latency checks 95th percentile; simulate downstream outage to",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Create a streaming test harness that subscribes to /stream with configurable backpressure, verifies resumption from a cursor after disconnect, injects downstream failures to test retry/backoff, and ch",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a comprehensive test matrix covering regions (US/UK) and locales (en-US/en-GB), with fallback validation for unsupported locales. Verify the presence and correctness of price, currency, and tax fields, ensuring c…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nWhy: testing streaming APIs with multi-tenant quotas and graceful degradation is common in fintech and data platforms; it surfaces contract testing, latency guarantees, and resilience.\n\n## Key Concepts\n- SSE streaming contract, idempotency, replay protection\n- Tenant isolation under burst traffic\n- Latency percentiles and fallback paths\n- End-to-end tooling integration (contract tests, load tests, mocks)\n\n## Code Example\n```javascript\n// Example: k6-like pseudocode for SSE test (conceptual)\nimport http from 'k6/http';\nimport { check } from 'k6';\nexport default function () {\n  let r = http.get('https://api/.../prices/stream?symbols=AAPL');\n  check(r, { 'status 200': (r) => r.status === 200 });\n}\n```\n\n## Follow-up Questions\n- How would you extend tests to validate time-based quotas and sticky sessions across region failovers?\n- How would you integrate observed anomalies into a circuit-breaker dashboard for real-time remediation?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-2",
        "questionId": "q-19410",
        "question": "You have a real-time analytics API that exposes a REST surface (GET /events/{id}) and a WebSocket streaming channel for live event delivery. Design a practical, end-to-end API testing plan that validates message ordering, at-least-once delivery semantics, client reconnection/backoff behavior, and correct backfill after downtime. Include tests for partial upstream outages, dynamic schema changes, and end-to-end observability using traces and logs. Specify tooling, data generation, and success criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan: POST /payments with Idempotency-Key to prevent duplicates. Use a mocked external gateway to capture charges. Steps: same key twice -> one charge; different keys -> separate charges; high-co",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan end-to-end tests for a REST + WebSocket analytics API: verify message ordering with sequence IDs, ensure at-least-once delivery and safe replay, validate reconnection/backoff under network faults",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test plan: Implement route routing using the X-API-Version header, with v1 as the default when the header is missing. For v1 endpoints, validate response contains {username,email}. For v2 endpoints, validate response co…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create per-tenant mock listeners, replay the same eventId to verify idempotency (only one delivery). Inject outages to validate retries and exponential backoff; ensure no cross-tenant leakage. Verify",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nStreaming APIs combined with REST surfaces are common in large-scale systems. This question probes testing of delivery guarantees, ordering, resilience under partial outages, and observability—areas critical for Snap, Microsoft, and Google-scale services.\n\n## Key Concepts\n\n- End-to-end testing across REST and WebSocket channels\n- Message ordering and sequencing guarantees\n- Delivery semantics (at-least-once vs exactly-once) and replay safety\n- Reconnection, backoff, and backfill strategies using durable offsets\n- Handling partial upstream outages and dynamic schema changes\n- Observability with tracing, logs, and metrics\n\n## Code Example\n\n```javascript\n// Example: basic WebSocket stream ordering test (conceptual)\nconst WebSocket = require('ws');\nconst ws = new WebSocket('wss://example.com/stream');\nlet lastSeq = -1;\nlet ok = true;\nlet count = 0;\nws.on('message', (data) => {\n  const msg = JSON.parse(data);\n  if (typeof msg.seq !== 'number' || msg.seq <= lastSeq) ok = false;\n  lastSeq = msg.seq;\n  count++;\n  if (count >= 100) ws.close();\n});\n```\n\n## Follow-up Questions\n\n- How would you simulate network partitions and validate reconnect/backoff behavior across regions?\n- How would you validate exactly-once semantics when an event is replayed during backfill?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-3",
        "questionId": "q-19855",
        "question": "Advanced: You operate a real-time event streaming service delivering to tens of thousands of tenants via WebSocket connections. Each tenant subscribes to channels with per-tenant access controls, rate limits, and feature flags to enable/disable event types. The system must handle client reconnections, backpressure, and occasional downstream outages in the broker. Design a concrete end-to-end API test plan to validate contract, data isolation, latency under bursts, reconnection semantics, and resilience?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design and implement a comprehensive end-to-end test strategy for an asynchronous bulk import API where POST /bulk/import accepts CSV uploads and returns a jobId, while GET /bulk/import/{jobId} provides real-time status…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an end-to-end tracing test plan for a multi-tenant API gateway using OpenTelemetry: verify trace context propagation across Auth, Gateway, and per-tenant services; test sampling rates, span generation, retries, a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: test idempotent POST /orders using a Redis-backed idempotency store TTL 3600s. 1) POST /orders with payload and Idempotency-Key A, expect 201 and orderId. 2) Parallel retry with same key; expect",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a complete end-to-end testing plan for a real-time WebSocket streaming service that serves multiple tenants with per-tenant channels, JWT-scoped access, and event-type flags. Include a WS load",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nReal-time streams require end-to-end orchestration, per-tenant isolation, and robust reconnection/backpressure handling. This question probes practical testing strategies and tooling choices for streaming APIs.\n\n## Key Concepts\n- WebSocket/streaming contracts and schemas\n- Per-tenant isolation and access control\n- Backpressure and latency under burst\n- Reconnection, idempotent replay, and exactly-once semantics\n- Resilience to broker outages and message redelivery\n- CI automation and observability\n\n## Code Example\n```javascript\n// Example test harness skeleton for WS clients\nasync function runWSTest() {\n  // establish multiple tenant connections, subscribe, measure RTT\n  // simulate bursts, reconnections, and partial outages\n}\n```\n\n## Follow-up Questions\n- How would you parameterize tests for different tenant loads?\n- What metrics and dashboards would you surface in Grafana?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-4",
        "questionId": "q-25447",
        "question": "Design an end-to-end tracing test plan for a multi-tenant API gateway using OpenTelemetry: verify trace context propagation across auth, gateway, and per-tenant services; test sampling rate, span generation, retries, and error cases; validate correlation with logs and metrics; include region-aware latency budgets and failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an end-to-end tracing test plan for a multi-tenant API gateway using OpenTelemetry: verify trace context propagation across Auth, Gateway, and per-tenant services; test sampling rates, span generation, retries, a…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Test input validation: invalid JSON, missing header, empty array -> 400. Idempotency: same X-Idempotency-Key returns same batchId; no duplicate processing on retry. Processing mode: verify all-or-noth",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a concrete test approach: validate multipart CSV content-type and max 5MB; use Idempotency-Key to prevent duplicates; expect 202 with a numeric jobId; poll GET /imports/{jobId} for progress un",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan focuses on GraphQL nested data, access control, and cross-region behavior. Validate deep queries, enforce depth and complexity limits, test cache invalidation after mutations, simulate token expi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses your ability to design comprehensive observability testing for distributed systems. It validates knowledge of OpenTelemetry standards, multi-tenant architecture patterns, and SLO validation under failure conditions.\n\n## Key Concepts\n- Distributed tracing across microservices\n- OpenTelemetry context propagation (W3C traceparent, baggage)\n- Sampling strategies and span generation\n- Retry handling and span drop mitigation\n- Region-aware latency budgets\n- Log and metrics correlation\n- Failure injection and SLO validation\n\n## Code Example\n```javascript\n// Comprehensive E2E tracing test suite\nimport { NodeSDK } from '@opentelemetry/sdk-node';\nimport { JaegerExporter } from '@opentelemetry/exporter-jaeger';\nimport { BatchSpanProcessor } from '@opentelemetry/sdk-trace-base';\nimport { trace, SpanStatusCode } from '@opentelemetry/api';\n\ndescribe('Multi-Tenant API Gateway Tracing', () => {\n  let sdk: NodeSDK;\n  let testTracer: any;\n\n  beforeAll(async () => {\n    sdk = new NodeSDK({\n      serviceName: 'e2e-tracing-test',\n      spanProcessor: new BatchSpanProcessor(new JaegerExporter()),\n    });\n    await sdk.start();\n    testTracer = trace.getTracer('e2e-test');\n  });\n\n  afterAll(async () => await sdk.shutdown());\n\n  describe('Trace Context Propagation', () => {\n    it('propagates traceparent across Auth, Gateway, and Tenant Services', async () => {\n      const span = testTracer.startSpan('test-request');\n      const context = span.spanContext();\n      const traceparent = `00-${context.traceId}-${context.spanId}-01`;\n\n      // Simulate request flow\n      const authResponse = await callAuthService({ traceparent });\n      expect(authResponse.headers['traceparent']).toBe(traceparent);\n\n      const gatewayResponse = await callGateway(authResponse.token, { traceparent });\n      expect(gatewayResponse.headers['traceparent']).toMatch(new RegExp(`^00-${context.traceId}`));\n\n      const tenantResponse = await callTenantService(gatewayResponse.token, { traceparent });\n      expect(tenantResponse.span.traceId).toBe(context.traceId);\n    });\n\n    it('propagates baggage across services', async () => {\n      const baggage = testTracer.startActiveSpan('test-baggage', (span) => {\n        span.setAttribute('tenant.id', 'tenant-123');\n        span.setAttribute('user.id', 'user-456');\n        return span;\n      });\n      // Verify baggage propagates through all services\n    });\n  });\n\n  describe('Sampling Configuration', () => {\n    it('respects configured sampling rate', async () => {\n      const sampledTraces = [];\n      for (let i = 0; i < 100; i++) {\n        const response = await makeRequest({ sampleRate: 0.1 });\n        sampledTraces.push(response.traceSampled);\n      }\n      const sampledCount = sampledTraces.filter(Boolean).length;\n      expect(sampledCount).toBeGreaterThan(5);\n      expect(sampledCount).toBeLessThan(25);\n    });\n\n    it('generates complete spans with required attributes', async () => {\n      const span = testTracer.startSpan('test-span');\n      span.setAttribute('http.method', 'GET');\n      span.setAttribute('http.url', '/api/tenant/users');\n      span.setAttribute('tenant.id', 'tenant-123');\n      span.end();\n\n      const exportedSpan = await getExportedSpan();\n      expect(exportedSpan.attributes['http.method']).toBe('GET');\n      expect(exportedSpan.attributes['tenant.id']).toBe('tenant-123');\n    });\n  });\n\n  describe('Retry and Error Handling', () => {\n    it('maintains trace continuity across retries', async () => {\n      const span = testTracer.startSpan('retry-test');\n      const context = span.spanContext();\n\n      let attempts = 0;\n      const response = await callServiceWithRetry({\n        onAttempt: () => attempts++,\n        traceId: context.traceId,\n      });\n\n      expect(attempts).toBe(3);\n      const spans = await getAllSpansForTrace(context.traceId);\n      expect(spans.length).toBe(3); // Parent + 3 retry spans\n    });\n\n    it('captures error spans correctly', async () => {\n      const span = testTracer.startSpan('error-test');\n      try {\n        await failingServiceCall();\n      } catch (err) {\n        span.setStatus({ code: SpanStatusCode.ERROR, message: err.message });\n        span.recordException(err);\n      } finally {\n        span.end();\n      }\n\n      const exportedSpan = await getExportedSpan();\n      expect(exportedSpan.status.code).toBe(SpanStatusCode.ERROR);\n      expect(exportedSpan.events).toContain(expect.objectContaining({ name: 'exception' }));\n    });\n  });\n\n  describe('Observability Correlation', () => {\n    it('correlates traces with logs', async () => {\n      const span = testTracer.startSpan('log-correlation');\n      const context = span.spanContext();\n\n      await logMessage('Processing request', {\n        traceId: context.traceId,\n        spanId: context.spanId,\n      });\n\n      const correlatedLogs = await queryLogs(context.traceId);\n      expect(correlatedLogs).toHaveLength(1);\n      expect(correlatedLogs[0].traceId).toBe(context.traceId);\n    });\n\n    it('correlates traces with metrics', async () => {\n      const span = testTracer.startSpan('metric-correlation');\n      span.end();\n\n      const metrics = await queryMetrics({\n        traceId: span.spanContext().traceId,\n      });\n      expect(metrics.durationHistogram).toBeDefined();\n    });\n  });\n\n  describe('Region-Aware Latency & Failure Scenarios', () => {\n    it('enforces latency budgets per region', async () => {\n      const regions = ['us-east-1', 'eu-west-1', 'ap-southeast-1'];\n      const budgets = { 'us-east-1': 200, 'eu-west-1': 300, 'ap-southeast-1': 400 };\n\n      for (const region of regions) {\n        const durations = [];\n        for (let i = 0; i < 10; i++) {\n          const duration = await measureRequest({ region });\n          durations.push(duration);\n        }\n        const p99 = percentile(durations, 99);\n        expect(p99).toBeLessThan(budgets[region]);\n      }\n    });\n\n    it('handles region failover gracefully', async () => {\n      const span = testTracer.startSpan('failover-test');\n      await simulateRegionFailure('us-east-1');\n\n      const response = await callWithFailover({ preferRegion: 'us-east-1' });\n      expect(response.region).not.toBe('us-east-1');\n      expect(span.spanContext().traceId).toBeDefined();\n    });\n\n    it('validates SLO under failure injection', async () => {\n      await injectFault('latency', { addedDelay: 5000 });\n\n      const results = await runLoadTest({ duration: '5m', targetSLO: 0.99 });\n      expect(results.availability).toBeGreaterThan(0.95);\n    });\n  });\n});\n```",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-5",
        "questionId": "q-14780",
        "question": "You're deploying a real-time pricing API with REST GET /prices and WebSocket /ws/prices. Design an end-to-end test plan that verifies REST snapshot accuracy and freshness, WebSocket streaming correctness under high throughput (in-order delivery, message deduplication, and robust reconnect with backoff), and cross-service consistency when upstream feeds diverge. Include outage fallback to cached data and mid-stream token rotation handling. Provide concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: test both REST snapshots and WebSocket streams. REST: validate schema, freshness window, symbol coverage, and delta semantics. WebSocket: assert in-order messages via sequenceId, deduplicate",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Test plan should verify: 1) POST /reports with idempotency key returns 202 and a jobId; duplicates yield same jobId. 2) Polling: status flows PENDING -> RUNNING -> DONE with progress ETA. 3) After DON",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Generate MSW handlers from OpenAPI using swagger-to-msw or openapi-msw-mock, validate responses against schema in tests, integrate contract tests in CI pipeline to detect API drift, implement version-controlled mock dat…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an end-to-end test plan for POST /rides in a multi-region orchestration with pricing and payment. Validate p95 latency and error rate under peak load, verify circuit breakers and exponential retries, and ensure g…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design end-to-end tests for both REST and streaming paths, covering data correctness, ordering guarantees, resilience under failures, and secure token handling in real-time systems.\n\n## Key Concepts\n- End-to-end testing across REST and WebSocket paths\n- Message ordering and deduplication in streaming\n- Reconnection/backoff strategies under churn\n- Cross-service data consistency with upstream feeds\n- Cache fallback and mid-stream token rotation handling\n\n## Code Example\n\n```javascript\n// Pseudo-test skeleton\nasync function testPricesREST() {\n  const r = await fetch('/prices?symbols=AAPL,GOOG');\n  assert(r.status===200);\n  const data = await r.json();\n  assert(Array.isArray(data.symbols));\n}\nasync function testPricesWS(wsUrl) {\n  const ws = new WebSocket(wsUrl);\n  ws.onmessage = (ev) => {\n    const msg = JSON.parse(ev.data);\n    // validate sequenceId monotonicity and uniqueness\n  };\n  // connect, use short-lived tokens, simulate reconnections\n}\n``` \n\n## Follow-up Questions\n- How would you verify delta semantics across multiple upstreams?\n- How would you simulate network partitions and ensure no data loss during recovery?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-6",
        "questionId": "q-29381",
        "question": "Design an automated end-to-end test plan for a real-time notification API that exposes REST POST /notify to enqueue events and a WebSocket /ws stream delivering per-tenant notifications. Validate per-tenant isolation, cross-shard ordering guarantees, client reconnection/backpressure behavior, and gateway fallback when downstream broker is unavailable. Include concrete test cases, tooling, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a plan: for each tenant seed isolated data; use a mock feature-flag service to toggle per-tenant flags that alter the response payload schema. Validate data isolation by attempting cross-tenan",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan should verify end-to-end exactly-once processing via idempotency keys and a transactional outbox. Include: duplicate event with same idempotency_key yields one charge and one outbox entry; h",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a test harness that exercises the REST enqueue endpoint and per-tenant WebSocket streaming. Verify: 1) strict per-tenant isolation ensuring tenants receive only their own events, 2) in-order delivery within sh…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Leverage an idempotency key on POST /orders to guarantee exactly-once semantics. Test retries by replaying the same key; assert a single order is created and no duplicates. Simulate Kafka broker outag",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design end-to-end tests spanning REST and WebSocket paths, reason about multi-tenant isolation, shard ordering, and cascading failures, and specify concrete metrics and tooling beyond unit tests.\n\n## Key Concepts\n- End-to-end testing across REST and WebSocket paths\n- Per-tenant isolation and cross-shard ordering guarantees\n- Streaming backpressure and client reconnection semantics\n- Broker outages, gateway fallback, and eventual consistency\n- Idempotency and replay protection\n\n## Code Example\n```javascript\n// Pseudo-test harness sketch\nconst { createTenantContext, enqueueEvent, streamEvents } = require('./test-harness');\n\ndescribe('Notification API E2E', () => {\n  it('enforces per-tenant isolation', async () => {\n    const [tenantA, tenantB] = await Promise.all([\n      createTenantContext('tenant-a'),\n      createTenantContext('tenant-b')\n    ]);\n    await enqueueEvent(tenantA, { type: 'order.placed', payload: { id: 'order-1' } });\n    await enqueueEvent(tenantB, { type: 'order.placed', payload: { id: 'order-2' } });\n    \n    const eventsA = await streamEvents(tenantA, { timeout: 2000 });\n    expect(eventsA.map(e => e.payload.id)).not.toContain('order-2');\n  });\n\n  it('guarantees in-order delivery within shards', async () => {\n    const ctx = await createTenantContext('tenant-a');\n    const sequence = ['a', 'b', 'c'];\n    for (const id of sequence) {\n      await enqueueEvent(ctx, { type: 'step', payload: { id }, shardKey: 'shard-1' });\n    }\n    const events = await streamEvents(ctx, { timeout: 2000 });\n    expect(events.map(e => e.payload.id)).toEqual(sequence);\n  });\n\n  it('handles backpressure during reconnection', async () => {\n    const ctx = await createTenantContext('tenant-a');\n    const events = streamEvents(ctx, { bufferLimit: 10 });\n    await enqueueBurst(ctx, 100);\n    expect(await ctx.getDroppedEventCount()).toBe(0);\n    expect(await ctx.getBackpressureMetric()).toBeGreaterThan(0);\n    await events.disconnect();\n    const reconnected = await streamEvents(ctx, { bufferLimit: 10 });\n    expect(await reconnected.getResumptionOffset()).toBeGreaterThan(0);\n  });\n\n  it('falls back gracefully when broker is unavailable', async () => {\n    const ctx = await createTenantContext('tenant-a');\n    await ctx.simulateBrokerOutage();\n    const result = await enqueueEvent(ctx, { type: 'alert' });\n    expect(result.status).toBe('queued');\n    expect(await ctx.getFallbackMetric()).toBe(1);\n    await ctx.restoreBroker();\n    const events = await streamEvents(ctx, { timeout: 5000 });\n    expect(events.map(e => e.type)).toContain('alert');\n  });\n});\n```\n\n## Concrete Test Cases\n\n| Test Case | Scope | Acceptance Criteria |\n|-----------|-------|---------------------|\n| Tenant Isolation | Cross-tenant | Tenant A never receives Tenant B events |\n| Shard Ordering | Intra-shard | Events within same shard maintain FIFO order |\n| Reconnection | Client | Client reconnects within 5s, resumes from last acknowledged offset |\n| Backpressure | Server | Buffer full triggers 503, client retries with exponential backoff |\n| Broker Fallback | Gateway | Enqueued events persist, delivery resumes within 30s of recovery |\n| Message Deduplication | Idempotency | Duplicate event IDs do not result in duplicate delivery |\n\n## Tooling Recommendations\n- **k6** or **Artillery**: Load testing and throughput validation\n- **MockWebSocket** or **Selenium**: WebSocket client simulation\n- **Toxiproxy** or **Chaos Mesh**: Network partition and broker failure injection\n- **Prometheus + Grafana**: Metrics collection (latency, throughput, drop rates)\n- **Postman/Newman**: REST endpoint validation",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-7",
        "questionId": "q-2585",
        "question": "Design a practical test plan for a WebSocket-based real-time update API at /ws/updates, where clients authenticate with JWT and receive per-tenant event streams. Include test cases for authentication failure, tenant isolation, message ordering and deduplication across reconnects, backpressure, and failure modes (broker outage, client disconnects). Recommend tooling and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: seed a deterministic set of idempotent events to POST /events, verify Redis-backed GET /analytics?window=1m reflects exact aggregates per window, and confirm Kafka offsets align with exactly",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement comprehensive end-to-end testing for the multi-tenant event publishing API through a structured approach: utilize idempotency keys to deduplicate POST /events requests, verify webhook delivery with exponential…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a comprehensive WebSocket test plan using a test client to simulate multiple tenants on /ws/updates. Validate: (1) JWT-based tenant authentication and isolation, (2) message ordering guarantees per tenant, (3)…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "End-to-end test plan for a multi-tenant payment API with async settlement and merchant webhooks. Validate idempotency across idempotency_key, verify webhook signatures, ensure tenant data isolation, a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical streaming API behavior under realistic conditions, including security, multi-tenant isolation, delivery guarantees, backpressure, and fault scenarios. It reveals depth in end-to-end testing of real-time systems.\n\n## Key Concepts\n- WebSocket end-to-end testing\n- JWT-based tenant isolation\n- Ordering, deduplication, and reconnect semantics\n- Backpressure and flow control\n- Fault injection and resiliency testing\n\n## Code Example\n```javascript\n// Example WebSocket test skeleton using ws library\nconst WebSocket = require('ws');\nconst jwt = require('jsonwebtoken');\n\n// Test client for WebSocket validation\nconst testWebSocketConnection = async (tenantId, token) => {\n  const ws = new WebSocket(`ws://localhost/ws/updates?token=${token}`);\n  \n  ws.on('open', () => {\n    console.log(`Connected for tenant: ${tenantId}`);\n  });\n  \n  ws.on('message', (data) => {\n    // Validate tenant isolation and message ordering\n    const message = JSON.parse(data);\n    console.log(`Received: ${message.sequenceId} for tenant: ${tenantId}`);\n  });\n  \n  return ws;\n};\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-8",
        "questionId": "q-5522",
        "question": "Design an advanced end-to-end test plan for a multi-tenant REST API that exposes GET /audits/{tenantId} and streams audit events to a real-time fan-out service via Kafka. The API enforces per-tenant privacy filters that can be toggled via admin API, and admin changes should propagate to consumers within a bounded lag. Include concrete test cases for privacy isolation, stream correctness, latency, and failure modes (Kafka outage, schema drift), and specify tooling and metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate RBAC, per-tenant data isolation, dynamic rate limits, and canary flags in a multi-tenant gateway. Create tenants A/B, mint tokens, and exercise endpoints with cross-tenant access attempts; bu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design tests to verify tenant isolation by polling /audits/{tenantId} while simulating cross-tenant requests, validate that Kafka streams only emit events for the correct tenant and that admin toggles",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test idempotent creation with repeated POST /payments using the same Idempotency-Key to ensure a single record is created and same Location is returned. Validate 400 on invalid payload, 401 on missing",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use end-to-end tests with real and mocked downstreams. Validate: 1) idempotency by resubmitting the same payload within TTL and ensuring a single job runs; 2) privacy by testing tenants with redaction",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes multi-tenant privacy guarantees, real-time streaming accuracy, and admin-driven policy propagation—crucial for platforms handling sensitive audits.\n\n## Key Concepts\n\n- Tenant isolation and access control\n- Streaming backfill and latency budgets\n- Admin-driven privacy filters and dynamic policy rollout\n- Failure modes: Kafka outage, schema drift, topic compaction, replay semantics\n\n## Code Example\n\n```javascript\n// Pseudo-test: verify tenant isolation in REST and Kafka stream\nconst tenant = 'tenantA';\nconst consumer = kafka.consumer({ groupId: 'audit-tenantA' });\nawait consumer.subscribe({ topic: 'audits', fromBeginning: true });\nlet received = [];\nfor await (const msg of consumer.consume()) {\n  const ev = JSON.parse(msg.value.toString());\n  if (ev.tenantId !== tenant) throw new Error('cross-tenant event detected');\n  received.push(ev);\n  if (received.length >= 5) break;\n}\n```\n\n## Follow-up Questions\n\n- How would you quantify end-to-end latency from REST GET to stream emission?\n- How would you test admin-flag propagation under high churn and network partitions?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-9",
        "questionId": "q-28220",
        "question": "Design a practical API testing strategy for a high-volume order service exposing REST POST /orders (with idempotency keys) and a companion WebSocket stream at /ws/orders for real-time updates. Include cross-contract tests between REST and WebSocket, per-tenant rate limits, backpressure handling in the ingestion pipeline, and a controlled schema evolution scenario with a deprecated field and a new nested field. Provide concrete test cases, tooling, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan a GraphQL API test for a gateway that composes data from products, pricing, and reviews. Enforce maxDepth 5 and a cost-based limit, profile N+1 issues with DataLoader, and validate caching behavi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan example: write end-to-end tests that fetch /products/{id} via REST and subscribe to /ws/products/updates for the same product, asserting REST data aligns with streamed deltas after each upda",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Pact to define cross-contract tests between REST /orders and WS /ws/orders with a versioned schema. Drive multi-tenant load with k6, validate per-tenant quotas and backpressure handling, and verif",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a durable idempotency store keyed by idempotency-key with a 24h TTL; on first POST /payments create the charge and return 201 with a charge_id, caching the payload. On subsequent requests with the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for cross-transport contracts and real-time ingestion with schema evolution are critical at scale. This angle ensures contract fidelity, idempotency under retries, and resilience to backpressure.\n\n## Key Concepts\n- Cross-transport contract testing (REST↔WebSocket)\n- Versioned, forward/backward-compatible schemas\n- Per-tenant rate limits and backpressure\n- Idempotency and DLQ routing in ingestion\n- Feature-flag-driven schema evolution\n\n## Code Example\n```javascript\n// Pact contract between REST /orders and WS /ws/orders\nconst pact = require('@pact-foundation/pact');\n// ... simplified snippet illustrating consumer/producer interactions\n```\n\n## Follow-up Questions\n- How would you detect drift between REST and WS payload schemas in CI?\n- What metrics and alerting would you add to catch backpressure-induced failures early?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-10",
        "questionId": "q-24356",
        "question": "Design a beginner-friendly test plan for a webhook endpoint POST /webhook/payment that a payment provider calls to notify about completed payments. The plan should verify the HMAC-SHA256 signature in the X-Signature header using a shared secret, enforce idempotent processing by event_id, and simulate retries on 5xx with exponential backoff. Include concrete test cases, tooling suggestions, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan for POST /webhooks/payment: verify HMAC-SHA256 signature with shared secret; enforce a 5-minute timestamp window; require idempotency-key to dedupe. Create cases: valid signature; invalid si",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Validate signature (401/403 for invalid), ensure idempotent handling by ignoring duplicate event_id, confirm successful processing once per id, and test retry behavior with 3 attempts and backoff on 5",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design end-to-end tests for POST /rides, GET /rides/{id}, and /rides/stream. Use consumer-driven contracts and OpenAPI validation; simulate rotating tokens; verify per-tenant rate limits; test idempot",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Focus on idempotent POSTs using Idempotency-Key, ensure retries return the same result, confirm 202 produces a Location for polling and 200/final state, verify /payments/{id} reflects consistent statu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nWebhook security and reliability are critical in payment flows. This question tests basic security validation, idempotency handling, and retry logic in a realistic, beginner-friendly way.\n\n## Key Concepts\n- Webhooks and callback semantics\n- HMAC verification with a shared secret\n- Idempotent processing using event_id\n- Retry strategies and backoff\n- Concrete tooling for API testing\n\n## Code Example\n```javascript\n// Signature helper (JS)\nconst crypto = require('crypto');\nfunction sign(payload, secret) {\n  return crypto.createHmac('sha256', secret).update(payload).digest('hex');\n}\n```\n\n## Follow-up Questions\n- How would you handle clock drift or replay protection?\n- How would you test for signature scheme upgrade or secret rotation?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-11",
        "questionId": "q-24116",
        "question": "Design a beginner-friendly test plan for POST /tasks that enqueues a long-running job and notifies completion via a webhook to a registered URL. Include how you would verify webhook delivery guarantees (at-least-once vs exactly-once), idempotency keys, handling duplicate deliveries, and retries with exponential backoff when the downstream consumer is outage-prone; include tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: use an idempotency-key header on POST /orders. First call creates the order and returns 201 with order_id. Subsequent calls with the same key return 200 with the same order_id and no duplicates.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a beginner-friendly test plan for POST /tasks that enqueues a long-running job and notifies completion via a webhook to a registered URL. Include how you would verify webhook delivery guarantee",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan end-to-end tests for POST /payments with Idempotency-Key. Validate that repeated POSTs with the same key produce a single payment record and identical responses, while new keys create new records",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a test plan for GET /composite/users/{id} that aggregates from three services in parallel and caches in Redis. Include: 1) stale-read detection by injecting latency on one backend and assert",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests asynchronous workflows and webhook reliability: requires thinking about delivery guarantees and failure modes beyond simple API calls.\n\n## Key Concepts\n\n- Asynchronous enqueues\n- Webhook semantics: at-least-once vs exactly-once\n- Idempotency keys and duplicate handling\n- Retry strategies and backoff\n- Observability with replay logs and dead-letter queues\n- Tooling: Postman, k6, WireMock, nock, Jest, VCR-like mocks\n\n## Code Example\n\n```javascript\n// Simple webhook sender with exponential backoff\nasync function sendWebhook(url, payload, attempt = 1) {\n  const max = 5;\n  try {\n    await fetch(url, { method: 'POST', body: JSON.stringify(payload), headers: { 'Content-Type': 'application/json' } });\n  } catch (e) {\n    if (attempt < max) {\n      const delay = Math.pow(2, attempt) * 100; // exponential backoff\n      await new Promise(r => setTimeout(r, delay));\n      return sendWebhook(url, payload, attempt + 1);\n    }\n    throw e;\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you verify duplicates are deduplicated in logs and metrics?\n- How would you simulate downstream outages and measure end-to-end latency?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-12",
        "questionId": "q-19579",
        "question": "Design a practical test plan for a REST API that supports asynchronous long-running jobs via POST /jobs, polls via GET /jobs/{id}, and a webhook callback to /webhooks/job-complete when finished. Include scenarios for duplicate submissions, webhook replay, out-of-order webhooks, backpressure, and regional failover?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan should validate idempotency by resubmitting the same idempotency-key and asserting a single export is created; simulate 10k concurrent POSTs to measure throughput and ensure duplicates aren’",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a beginner-friendly test plan for POST /webhook that validates an HMAC-SHA256 signature in X-Signature over the JSON payload with a shared secret. Include: happy-path with valid signature; mis",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a test plan that validates asynchronous job submission, idempotent behavior, polling semantics, and webhook integrity. Verify POST /jobs with idempotency-key yields single queued job; poll tra",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Leverage an idempotency key on POST /orders to guarantee exactly-once semantics. Test retries by replaying the same key; assert a single order is created and no duplicates. Simulate Kafka broker outag",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical testing of async APIs with callbacks, covering idempotency, event ordering, delivery guarantees, and resilience under failover.\n\n## Key Concepts\n\n- Async job lifecycle and polling\n- Webhook security (signatures/HMAC)\n- Idempotent operations and replay protection\n- Event ordering and out-of-order handling\n- Backpressure and rate limiting\n- Multi-region failover and redirection\n\n## Code Example\n\n```javascript\n// Example pseudo-test structure\nconst res1 = post('/jobs', { task: 'train', data:'s3://bucket' }, { idempotencyKey: 'abc-123' });\nconst status = get(`/jobs/${res1.id}`);\nassert(status.phase === 'completed' || status.phase === 'running');\n```\n\n## Follow-up Questions\n\n- How would you simulate out-of-order webhooks in a test suite?\n- How would you design webhook replay protection and state reconciliation?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-13",
        "questionId": "q-28811",
        "question": "Design an advanced API testing plan for a multi-tenant API gateway that supports dynamic schema stitching and per-tenant field-level permissions across REST and GraphQL endpoints, with daily blue/green schema deployments and mTLS between gateway and downstream services. Include contract testing between gateway and downstream services, schema compatibility checks, feature-flag evaluation, per-tenant isolation assertions, resilience to upstream outages, and end-to-end latency targets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan for per-tenant data masking with dynamic policies: verify policy propagation latency, correct masking per tenant, and cache invalidation. Include race conditions when a policy updates during",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deploy three-region replicas; drive high update rates with k6 against PATCH and GET; verify GET returns latest balance after upserts and streaming delivers deduplicated, in-order events with sequence",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design tests to validate edge caching correctness, purge propagation, and per-tenant isolation under concurrent traffic. Steps: run multi-region load tests, inspect X-Cache-Status and Age headers, ens",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a practical plan: 1) use Pact for REST and GraphQL contracts; 2) run schema compatibility checks for each stitch version against a registry; 3) implement per-tenant visibility tests and access",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests cross-cutting concerns like dynamic schemas, multi-tenant isolation, and secure, verifiable deployments under real-world ops.\n\n## Key Concepts\n- Contract testing across REST and GraphQL\n- Dynamic schema stitching and compatibility\n- Per-tenant authorization and field-level permissions\n- Blue/Green deployment and canary verification\n- mTLS and token lifecycle\n- Resilience under upstream outages and latency targets\n\n## Code Example\n```javascript\n// Example Pact contract test setup (REST)\n```\n\n## Follow-up Questions\n- How would you detect schema drift breaking existing clients in a blue/green rollout?\n- What metrics and dashboards would you expose for per-tenant SLAs?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-14",
        "questionId": "q-22880",
        "question": "Design an end-to-end test strategy for a multi-tenant REST API where per-tenant feature flags dynamically alter the response payload schema. Show how you'd validate data isolation, per-tenant privacy rules, runtime schema drift, contract tests with client SDKs, and admin-driven feature toggles. Include concrete tooling, test cases, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify 202 and trackingId on first call; simulate duplicates with the same payload and Idempotency-Key to ensure no duplicates in downstream stores; validate per-partition ordering by including a part",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a plan: for each tenant seed isolated data; use a mock feature-flag service to toggle per-tenant flags that alter the response payload schema. Validate data isolation by attempting cross-tenan",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "## Test Plan: PATCH /inventory/{sku} with Optimistic Concurrency Control ### Precondition Ensure GET /inventory/{sku} returns an ETag header in the response. ### Test Cases **TC1: Successful Update with Valid ETag** 1.…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a bulk upsert test suite focusing on JSONL payloads up to 1000 records, validating per-record status, atomic batch vs partial commits, idempotent retries with the same idempotency-key, and ten",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for multi-tenant APIs with dynamic schemas mirror real production risks: data isolation and contract drift.\n\n## Key Concepts\n\n- Multi-tenant isolation\n- Dynamic schema via feature flags\n- Contract testing\n- Admin-driven toggles\n\n## Code Example\n\n```javascript\n// Pseudo-test snippet showing schema validation\n```\n\n## Follow-up Questions\n\n- How would you automate drift checks over time?\n- How would you roll back a flag without breaking clients?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-15",
        "questionId": "q-14650",
        "question": "Design an end-to-end test strategy for an asynchronous bulk import API: POST /bulk/import uploads a CSV and returns a jobId; GET /bulk/import/{jobId} reports status, processed rows, and per-row errors. How would you validate idempotent submissions, per-tenant isolation, and resilience under queue outages and worker restarts with retries; and what concrete test cases and tooling would you use?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a test plan that uses an Idempotency-Key on POST /checkout; simulate downstream faults via mocks for auth, inventory, and payment with a fault-injection tool (e.g., WireMock) to trigger 2s time",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Test Plan for POST /payments/process Idempotency **Objective:** Verify that duplicate requests with the same X-Idempotency-Key result in a single charge, while different keys allow separate charges. ### Test Cases |…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test plan: Implement route routing using the X-API-Version header, with v1 as the default when the header is missing. For v1 endpoints, validate response contains {username,email}. For v2 endpoints, validate response co…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design and implement a comprehensive end-to-end test strategy for an asynchronous bulk import API where POST /bulk/import accepts CSV uploads and returns a jobId, while GET /bulk/import/{jobId} provides real-time status…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Matters\n\nTesting asynchronous, event-driven APIs requires deep understanding of eventual consistency, idempotency patterns, and multi-tenant data isolation. This question evaluates your ability to design comprehensive test coverage for complex failure scenarios including queue infrastructure downtime, worker process crashes, retry logic implementation, and maintaining data integrity across retry attempts.\n\n## Core Technical Concepts\n\n- Asynchronous processing patterns with polling-based status updates\n- Idempotent request handling and duplicate submission prevention\n- Multi-tenant data isolation and security boundaries\n- Resilience testing for queue failures and worker restart scenarios\n- Retry semantics with exponential backoff and circuit breaker patterns\n- Comprehensive observability through metrics, structured logging, and audit trails\n\n## Implementation Example\n\n```python\n# Example pytest framework skeleton for end-to-end testing\nimport pytest\nimport asyncio\nfrom aiohttp import ClientSession\nimport time\n\nclass TestBulkImportAPI:\n    async def test_idempotent_submissions(self):\n        # Verify duplicate submissions with same data\n        # produce identical jobIds or reference same processing\n        pass\n    \n    async def test_per_tenant_isolation(self):\n        # Ensure data segregation between tenants\n        # Validate cross-tenant data leakage prevention\n        pass\n    \n    async def test_queue_failure_resilience(self):\n        # Simulate queue downtime and worker crashes\n        # Verify graceful degradation and recovery\n        pass\n```\n\n## Key Test Scenarios\n\n1. **Idempotency Testing**: Duplicate CSV submissions, concurrent identical requests, and deduplication mechanisms\n2. **Isolation Validation**: Cross-tenant data boundaries, namespace separation, and security rule enforcement\n3. **Failure Resilience**: Queue outages, worker crashes, network partitions, and retry limit behaviors\n4. **Data Integrity**: Exactly-once processing, transaction consistency, and error recovery validation\n\n## Recommended Tooling\n\n- **Test Framework**: pytest with async support for concurrent testing\n- **API Testing**: aiohttp or httpx for async HTTP client operations\n- **Mock Services**: LocalStack for AWS queue simulation, Docker containers for service isolation\n- **Observability**: Prometheus metrics, ELK stack for log aggregation, Jaeger for distributed tracing\n- **Load Testing**: k6 or Locust for concurrency and stress testing scenarios\n\nThis comprehensive approach ensures robust validation of your asynchronous bulk import system under various operational conditions.",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-16",
        "questionId": "q-14820",
        "question": "Design a pragmatic test plan for an HTTP POST /payments/process in a multi-tenant platform used by high-traffic services. The endpoint must be idempotent, trigger asynchronous payout jobs, and emit per-tenant webhook events. Describe how you'd simulate peak concurrency, enforce idempotency keys, validate webhook retries with exponential backoff, and verify audit logs across services for eventual consistency. Provide concrete test cases and tooling recommendations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate HMAC-backed auth: (1) valid signature over method, path, timestamp, body returns 200; (2) tampered body or wrong secret returns 401; (3) timestamp outside +/-5 minutes returns 401; (4) missin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a deterministic base payload and an Idempotency-Key header. Send POST /orders with key k1; capture orderId1. Send the same request again with k1; expect 200/201 with the same orderId1 and no dupli",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an API testing plan that enforces idempotency and end-to-end consistency for POST /orders in a multi-tenant marketplace. Include: 1) idempotency-key tests to prevent duplicates; 2) traceability",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a dedupe store (Redis) keyed by Idempotency-Key; run parallel requests with the same key and assert a single payout created. Validate distinct keys create separate payouts. Simulate webhook delive",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests for idempotent, asynchronous payouts with webhook delivery under high concurrency are common in ride-hailing and edge networks. This checks correctness, fault-tolerance, and observability in distributed multi-tenant scenarios.\n\n## Key Concepts\n- Idempotency and idempotency-key\n- Async processing and eventual consistency\n- Webhook reliability with backoff and jitter\n- Audit logs and traceability across services\n- Tenant isolation and data integrity\n\n## Code Example\n\n```javascript\n// Pseudocode test sketch\nconst r1 = httpPost('/payments/process', {amount:100, currency:'USD'}, {headers: {'Idempotency-Key':'k-abc', 'Tenant':'t1'}});\nconst r2 = httpPost('/payments/process', {amount:100, currency:'USD'}, {headers: {'Idempotency-Key':'k-abc', 'Tenant':'t1'}});\nexpect(r1.body.payoutId).toBeDefined();\nexpect(r2.body.payoutId).toBe(r1.body.payoutId); // deduped\n```\n\n## Follow-up Questions\n- How would you verify audit log propagation across async boundaries during a webhook retry storm?\n- What production signals would you monitor to detect idempotency leaks?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-17",
        "questionId": "q-28061",
        "question": "Design a robust end-to-end test plan for a hybrid /search endpoint that serves small synchronous queries and queues large ones for asynchronous export. Backend uses Postgres + Elasticsearch with a reconciliation service migrating index versions v1→v2. Provide concrete test cases, cross-service contract tests, data-consistency checks during migration, per-tenant isolation, cancellation behavior, and SLA targets for both paths; include tooling and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create per-tenant mock listeners, replay the same eventId to verify idempotency (only one delivery). Inject outages to validate retries and exponential backoff; ensure no cross-tenant leakage. Verify",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pact for consumer-driven contracts between the REST API, async worker, and the index layer. Validate both paths: synchronous small-queries and asynchronous large-exports. Include: 1) contract test",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage Idempotency-Key header on POST /payments. The first call should create a payment and return 201 with a transaction_id. Repeating with the same key must return 200 with the same transaction_id",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To test GET /profiles/{id} with conditional requests: fetch a fresh resource to capture ETag and Last-Modified. Re-request with If-None-Match and If-Modified-Since to expect 304. Update the resource a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nInterview context explanation.\n\n## Key Concepts\n- Contract testing across services (Pact)\n- Hybrid sync/async flows and cancellation\n- Data migration and index reconciliation\n- Per-tenant isolation and security\n- Latency SLAs and tooling\n\n## Code Example\n```javascript\n// Pact contract between REST API and Worker example\nconst pact = {\n  consumer: 'SearchAPI',\n  provider: 'SearchWorker',\n  interactions: [/* ... */],\n}\n```\n\n## Follow-up Questions\n- How would you simulate index migration drift during tests?\n- How do you ensure idempotent retries for failed async exports?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-18",
        "questionId": "q-17696",
        "question": "Design a testing strategy for a webhook delivery API used by PayPal and Amazon that guarantees at-least-once delivery, enforces idempotency, and defends against replay attacks. Include test cases for missing/invalid signatures, duplicate payloads, DLQ routing, backpressure under peak load, and end-to-end latency budgets. Specify tooling, data generation, and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would validate end-to-end by using idempotency keys, simulating replays, verifying signatures, and ensuring duplicates land in a DLQ. Tests cover missing/invalid signatures, duplicate payloads, fiel",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Test plan for POST /v1/process/image in a multi-tenant service. Validate idempotency using Idempotency-Key, ensure large payloads stream correctly, confirm per-tenant routing meets SLA across backends",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement comprehensive end-to-end testing for the multi-tenant event publishing API through a structured approach: utilize idempotency keys to deduplicate POST /events requests, verify webhook delivery with exponential…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: test per-user rate limiting on POST /checkout. Step 1: send 5 rapid requests -> 200; Step 2: 6th request -> 429 with Retry-After header; Step 3: advance the clock to reset window and verify 200",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for webhook reliability, security, and observability in real-world traffic.\n\n## Key Concepts\n- At-least-once delivery\n- Idempotency and deduplication\n- Signature verification and replay protection\n- DLQ routing and dead-letter processing\n- Backpressure, retry policies, and latency budgets\n\n## Code Example\n```javascript\n// Example: simple signature verification (HMAC-SHA256)\nfunction verifySignature(payload, signature, secret) {\n  const crypto = require('crypto');\n  const hmac = crypto.createHmac('sha256', secret).update(payload, 'utf8').digest('hex');\n  return crypto.timingSafeEqual(Buffer.from(hmac), Buffer.from(signature));\n}\n```\n\n## Follow-up Questions\n- How would you test cross-region duplicate handling and idempotency key reuse?\n- How would you design a test to validate DLQ poison-message handling and alerting?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-19",
        "questionId": "q-26016",
        "question": "You have an API with POST /jobs to enqueue long-running tasks and GET /jobs/{id} to fetch status. Design a beginner-friendly test plan that verifies: 1) a valid enqueue returns 202 with Location header and jobId; 2) repeated POSTs within 2 seconds are idempotent and return the same jobId; 3) polling uses exponential backoff and completes within 5 minutes; 4) worker failures trigger up to 3 retries and final status 'failed' with error details?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a test matrix around per-tenant feature flags and edge routing. Include canary rollout: Tenant A flag on, Tenant B off; observe 0–30s propagation to edge caches. Validate responses reflect flag",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan should simulate multi-region uploads, sending chunks out-of-order and retrying duplicates; enforce per-user quotas under bursts; validate reassembly with SHA-256; verify final callback paylo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Valid payload → 202 with Location /jobs/{id} and body containing jobId. Replays within 2 seconds must be idempotent and return the same jobId. Implement polling with exponential backoff (1s, 2s, 4s) a",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement idempotent webhook processing by persisting provider+event_id and rejecting duplicates; verify HMAC signature first. Test cases: a) duplicate delivery, b) out-of-order events, c) transient d",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for asynchronous API flows are common in production; this checks idempotency, polling strategies, and retry behavior.\n\n## Key Concepts\n- Asynchronous processing\n- Idempotency\n- Polling with backoff\n- Failure/retry semantics\n- CI integration\n\n## Code Example\n```javascript\n// High-level test outline (pseudo)\ndescribe('POST /jobs and GET /jobs/{id}', () => {\n  it('enqueues and polls to completion', async () => {\n    const res = await post('/jobs', validPayload)\n    expect(res.status).toBe(202)\n    const id = res.body.jobId\n    // poll with backoff\n    // etc.\n  })\n})\n```\n\n## Follow-up Questions\n- How would you adapt tests if GET /jobs/{id} returns partial updates?\n- How would you simulate extremely long-running jobs in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-20",
        "questionId": "q-12604",
        "question": "Design a practical test plan for a high-stakes trading API exposed by a broker. It includes POST /orders with idempotency_key, GET /orders/{id}, and a real-time market data feed over WebSocket. Outline end-to-end idempotency validation, cross-service consistency using an outbox pattern, WS replay handling, and failure modes (broker outage, partial DB failure). Include concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Submit /orders with idempotency_key 'k1' twice; second response must be same order and no duplicates. Simulate broker outage during commit to verify compensating/rollback behavior and eventual retry.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Tests should cover idempotent behavior with a shared idempotency-key, verifying first call returns 201 and a job_id while duplicates return the same job_id. Validate retry semantics on transient netwo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan around an idempotent POST /orders with a unique idempotency key; verify a single committed order across Inventory, Payment, and Shipping. Simulate partial outages and replayed events; ensure retr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design tests to verify tenant isolation by polling /audits/{tenantId} while simulating cross-tenant requests, validate that Kafka streams only emit events for the correct tenant and that admin toggles",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests idempotency, cross-service consistency, and failure handling in a brokered trading API under real-world faults.\n\n## Key Concepts\n- Idempotency keys\n- Outbox pattern\n- WebSocket replay\n- Fault injection\n\n## Code Example\n```javascript\n// example pseudo-test\nconst res1 = post('/orders', {idempotency_key:'k1', symbol:'AAPL', qty:10, side:'buy'});\nconst res2 = post('/orders', {idempotency_key:'k1', symbol:'AAPL', qty:10, side:'buy'});\nassert(res1.id === res2.id);\n```\n\n## Follow-up Questions\n- How would you validate idempotency under partial system outages?\n- What metrics would you monitor for cross-service consistency?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-21",
        "questionId": "q-3290",
        "question": "Design an end-to-end API testing strategy for a real-time ride ETA service with POST /rides, GET /rides/{id}, and /rides/stream (SSE). Include contract tests, OpenAPI schema validation, rotating auth tokens, rate-limiting checks, idempotent POSTs with idempotency keys, retry semantics, and resilience under downstream outages. Also cover canary rollout and feature-flag driven behavior?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive test matrix covering regions (US/UK) and locales (en-US/en-GB), with fallback validation for unsupported locales. Verify the presence and correctness of price, currency, and tax fields, ensuring c…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use consumer-driven contracts between the gateway, adapters, and partner webhooks, with strictly versioned payload schemas. Implement contract tests (Pact) that cover both old and new schemas during transition periods,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Submit a POST /payments with Idempotency-Key: key1; expect 202 and a Location header for /payments/{id}. Resubmit with the same key; verify the response reuses the same id and that no extra charges we",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design end-to-end tests for POST /rides, GET /rides/{id}, and /rides/stream. Use consumer-driven contracts and OpenAPI validation; simulate rotating tokens; verify per-tenant rate limits; test idempot",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of end-to-end reliability for real-time APIs, including streaming and feature flags.\n\n## Key Concepts\n- Contract testing with Pact/OpenAPI-driven checks\n- SSE streaming reliability and dedup\n- Idempotency, retries, and backoff strategies\n- Token rotation, auth and per-tenant rate limits\n- Canary rollouts and feature-flag controlled behavior\n- Chaos testing for downstream outages\n\n## Code Example\n```javascript\n// Pseudo-contract test sketch\ndescribe('POST /rides contract', ()=>{ /* ... */ })\n```\n\n## Follow-up Questions\n- How would you validate schema drift across API versions without breaking clients?\n- How would you simulate a Redis outage affecting rate limits during a test run?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-22",
        "questionId": "q-18291",
        "question": "Design a practical API test plan for a high-throughput payment processing REST API that supports idempotent posting with idempotency-key headers, ensures exactly-once processing across retries, and guarantees ordered ledger events under partial upstream outages. Describe test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Tests should cover idempotent behavior with a shared idempotency-key, verifying first call returns 201 and a job_id while duplicates return the same job_id. Validate retry semantics on transient netwo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose end-to-end tests for a high-throughput payment API that uses idempotency-key headers to achieve exactly-once processing. Include cases for replay with same key, out-of-order deliveries, partia",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a test harness that runs Kafka (transactional producer), Redis, and Postgres in a sandbox. 1) Send POST /items twice with the same Idempotency-Key and expect one write and one effective update; 2)",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design tests for a streaming inference API delivering Server-Sent Events. Validate event order and IDs, heartbeat intervals, client backpressure (pause/resume) and precise replay on reconnect. Use a s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests distributed systems correctness under concurrency, idempotency, ordering, and partial outages; validates that the API and ledger service maintain exactly-once semantics and message ordering.\n\n## Key Concepts\n\n- Idempotency keys, exactly-once processing\n- Ordering guarantees in distributed ledger\n- Partial upstream outages and retry semantics\n- End-to-end testing with simulated upstream services and timeouts\n- Tooling: Postman/Newman, k6, WireMock, Kafka test doubles\n\n## Code Example\n\n```javascript\n// Example k6 test that repeats a payment request with same idempotency key\nimport http from 'k6/http';\nimport { check, sleep } from 'k6';\n\nexport default function () {\n  const payload = { amount: 1000, currency: 'USD', account: 'A1' };\n  const headers = { 'Content-Type': 'application/json', 'Idempotency-Key': 'abc-123' };\n  const res = http.post('https://api/payments', JSON.stringify(payload), { headers });\n  check(res, { 'status 200': (r) => r.status === 200 || r.status === 409 });\n\n  sleep(0.5);\n}\n```\n\nNote: The code snippet is minimal; ensure you adapt to your API.",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-23",
        "questionId": "q-7213",
        "question": "In a high-throughput payments API, you have POST /payments with Idempotency-Key and a GET /payments/{id} to poll status. The downstream gateway is flaky, returning intermittent 500s; design a concrete end-to-end test plan to validate idempotency, exactly-once processing semantics, retry/backoff with jitter, and webhook reconciliation, under simulated outages and concurrent requests. Include specific test cases, data, and tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Issue repeated POST /payments requests with the same Idempotency-Key and verify that only one charge is created in the ledger. Ensure the payment gateway is invoked exactly once, and confirm that subsequent calls return…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan end-to-end tests for POST /payments with Idempotency-Key. Validate that repeated POSTs with the same key produce a single payment record and identical responses, while new keys create new records",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test idempotent behavior by sending two identical POST /payments/charge requests with the same Idempotency-Key header and identical payload. Verify that only one charge is created by checking the response (looking for t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan emphasizes idempotent fetch and deduplication: the endpoint uses `since`, `limit`, and per-user `sequence_id`; duplicates are rejected within a deduplication window. Validate ordering per user, across partitions, v…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests must model real-world reliability challenges in payment flows, including idempotency guarantees, exact-once processing, and webhook reconciliation under outages and bursts.\n\n## Key Concepts\n- Idempotency keys and deduplication\n- Exactly-once processing in distributed systems\n- Retries with backoff and jitter\n- Webhook reconciliation and deduplication\n- Fault injection for flaky downstream systems\n\n## Code Example\n```javascript\n// k6 skeleton for idempotency testing\nimport http from 'k6/http';\nimport {check} from 'k6';\nexport default function () {\n  const key = __ENV.KEY;\n  const payload = {amount:100, currency:'USD'};\n  http.post('https://api/payments', JSON.stringify(payload), {\n    headers: { 'Content-Type': 'application/json', 'Idempotency-Key': key }\n  });\n  check(res, { 'status 200': (r) => r && r.status === 200 });\n}\n```\n\n## Follow-up Questions\n- How would you validate webhook dedup under bursty traffic and partial outages?\n- How would you ensure idempotency storage survives restarts and scales horizontally?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-24",
        "questionId": "q-21642",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /payments that creates a payment intent and returns 202 Accepted with a Location header to poll for status; ensure idempotency using Idempotency-Key header, validate retries, concurrent requests, and failure-to-poll scenarios. What concrete test cases, tooling, and acceptance criteria would you include?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test idempotent creation with repeated POST /payments using the same Idempotency-Key to ensure a single record is created and same Location is returned. Validate 400 on invalid payload, 401 on missing",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect a test plan for GET /composite/users/{id} that aggregates from three services in parallel and caches in Redis. Include: 1) stale-read detection by injecting latency on one backend and assert",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Issue repeated POST /payments requests with the same Idempotency-Key and verify that only one charge is created in the ledger. Ensure the payment gateway is invoked exactly once, and confirm that subsequent calls return…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a deterministic base payload and an Idempotency-Key header. Send POST /orders with key k1; capture orderId1. Send the same request again with k1; expect 200/201 with the same orderId1 and no dupli",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses understanding of idempotency, asynchronous processing, and polling reliability in a real API flow. It also probes concurrency handling, error scenarios, and test tooling choices.\n\n## Key Concepts\n- Idempotency keys and deduplication\n- Asynchronous processing with 202 and Location for polling\n- Polling strategy with timeout and backoff\n- Concurrency and race condition handling\n- Auth, validation, and rate-limiting tests\n\n## Code Example\n```javascript\n// Example: Jest + supertest snippet for idempotent create\nconst res1 = await request(app).post('/payments')\n  .set('Idempotency-Key', 'abc-123')\n  .set('Authorization', 'Bearer token')\n  .send({ amount: 1000, currency: 'USD' });\nexpect(res1.status).toBe(202);\nconst id = res1.body.paymentId;\nconst res2 = await request(app).post('/payments')\n  .set('Idempotency-Key', 'abc-123')\n  .set('Authorization', 'Bearer token')\n  .send({ amount: 1000, currency: 'USD' });\nexpect(res2.status).toBe(202);\n```\n\n## Follow-up Questions\n- How would you simulate high-concurrency in CI?\n- How would you extend tests for partial failures in polling or network hiccups?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-07T07:03:26.207Z",
    "version": 1
  }
]