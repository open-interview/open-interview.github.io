[
  {
    "id": "test-api-testing",
    "channelId": "api-testing",
    "channelName": "API Testing",
    "title": "API Testing Knowledge Test",
    "description": "Test your understanding of API Testing with 25 questions.",
    "questions": [
      {
        "id": "api-testing-q-0",
        "questionId": "q-18740",
        "question": "Design a practical test plan for an API endpoint POST /reports that receives a tenant-scoped request to generate a large analytics report. The endpoint should return 202 with a job-id and support idempotent retries via an idempotency-key, while guaranteeing that the report is produced exactly once and the status is observable via GET /reports/{job-id}. Include concrete test cases, failure modes, and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Draft a test plan: verify idempotency by sending duplicates for the same event_id and asserting a single aggregate in GET /events/{event_id}; simulate downstream outages to ensure at-least-once delive",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose end-to-end tests for POST /reports with an idempotency-key. Verify that the first call enqueues one job and returns a job-id; a second call with the same idempotency-key while the first is run",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test plan focuses on idempotent long-running search jobs. Send POST /api/v1/search with payload and X-Idempotency-Key; expect 202 and a Location header. Re-send with same key should return same jobId;",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a comprehensive high-concurrency test strategy across regions: generate N parallel /orders requests with dependent /inventory updates, inject configurable latency between regions using Chaos Mesh or similar, and…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for long-running, tenant-scoped APIs must guarantee exactly-once semantics while handling retries and high concurrency. This question probes idempotency handling, job deduplication, cross-tenant isolation, and failure resilience in a realistic pipeline.\n\n## Key Concepts\n\n- Idempotency keys and deduplication\n- Exactly-once vs at-least-once semantics\n- Background job orchestration and status visibility\n- Concurrency across tenants and race-condition handling\n- Failure modes and recovery (worker crash, DB outages)\n\n## Code Example\n\n```javascript\n// Example test snippet (Node.js with axios)\nconst axios = require('axios');\nasync function testIdempotentPost() {\n  const payload = { tenant: 'ace', params: { range: '30d' } };\n  const id = 'idt-guest-123';\n  const r1 = await axios.post('/reports', payload, { headers: { 'Idempotency-Key': id } });\n  const r2 = await axios.post('/reports', payload, { headers: { 'Idempotency-Key': id } });\n  console.assert(r1.data.jobId === r2.data.jobId, 'Dedup should return same job-id');\n}\n```\n\n## Follow-up Questions\n\n- How would you validate end-to-end latency distribution under increasing load?\n- How would you verify audit logs reflect idempotent operation and tenant boundaries?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-1",
        "questionId": "q-4273",
        "question": "Design an advanced test plan for a REST /checkout endpoint in a multi-region e-commerce platform where checkout triggers downstream services (inventory, payment, fulfillment) asynchronously via a message bus. Include idempotency with idempotencyKey, eventual consistency across regions, partial outages, and latency budgets. Provide concrete test cases, tooling, and validation for tombstones and reconciliation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an end-to-end test plan for a multi-region REST /checkout that triggers downstream services (inventory, payment, fulfillment) asynchronously via a message bus. Validate idempotency with idempot",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Test POST /payments with Idempotency-Key: verify that same key + same payload creates one payment and returns 200 with identical response; same key + different payload returns an error (conflict or ba",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: seed a deterministic set of idempotent events to POST /events, verify Redis-backed GET /analytics?window=1m reflects exact aggregates per window, and confirm Kafka offsets align with exactly",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a test plan for GET /composite/users/{id} that aggregates from three services in parallel and caches in Redis. Include: 1) stale-read detection by injecting latency on one backend and assert",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests must cover distributed transactions with asynchronous callbacks, idempotency keys to avoid duplicates across retries, and cross-region consistency. This mirrors real-world retail/back-office systems at Lyft/Goldman Sachs.\n\n## Key Concepts\n- Distributed async workflows\n- Idempotency, exactly-once vs at-least-once\n- Partial failure simulation (downstream outages)\n- Data reconciliation and tombstones\n- Latency budgets and observability (SLIs)\n\n## Code Example\n```javascript\n// Pseudo-idempotent processor\nfunction processCheckoutEvent(event, store) {\n  const key = event.idempotencyKey;\n  if (store.has(key)) return store.get(key); // idempotent\n  const res = doWork(event); // triggers downstream\n  store.set(key, res);\n  return res;\n}\n```\n\n## Follow-up Questions\n- How would you structure a test to verify exactly-once semantics when events can be delivered multiple times?\n- What metrics and dashboards would you use to detect latency regressions in the downstream flow during peak load?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-2",
        "questionId": "q-30242",
        "question": "Design a test plan for an HTTP API that ingests financial trades via POST /trades with an Idempotency-Key header. It returns 202 and a trade-id. Downstream writes to a transactional log and publishes to Kafka. Outline how you would validate idempotency and exactly-once processing under retries and broker outages, including time skew, multi-tenant isolation, and backoff strategies. Provide concrete test cases, tooling, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive automated test plan to validate the multi-tenant REST API's behavior under various conditions: 1. **Baseline Contract Testing**: Create contract tests for /api/v2/users/{id} covering both extended…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ensure idempotency by replaying the same payload with the same Idempotency-Key within the window; confirm only a single trade is persisted in the log and produced to Kafka. Validate dedup window bound",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a test plan that uses an Idempotency-Key on POST /checkout; simulate downstream faults via mocks for auth, inventory, and payment with a fault-injection tool (e.g., WireMock) to trigger 2s time",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate an ingest batch job end-to-end: per-record schema checks, required fields, and max sizes; confirm idempotency by re-sending with the same idempotency key; simulate partial failures to push ba",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical risks around idempotency and exactly-once guarantees in asynchronous pipelines.\n\n## Key Concepts\n- Idempotency windows; deduplication; exactly-once downstream writes; fault-injection; tenancy isolation.\n\n## Code Example\n```javascript\n// no code here in this example\n```\n\n## Follow-up Questions\n- How would you measure idempotency violation rates under load?\n- How would you adapt tests for a streaming sink instead of Kafka?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-3",
        "questionId": "q-1050",
        "question": "Design an automated test plan for a REST + streaming API: /inventory/{sku}/status returns current stock via a streaming endpoint /inventory/stream (Server-Sent Events). The plan should cover stream resilience, event deduplication, per-warehouse aggregation under bursts, and failure modes when downstream storage becomes partially unavailable. Provide concrete test cases, tooling suggestions, and expected outcomes, with emphasis on realism for high-scale retail backends?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose layered testing: unit tests for resolvers with mocked batch loaders to catch N+1; integration tests against staging with tenant tokens validating access control and data masking; contract test",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Validate HMAC-backed auth: (1) valid signature over method, path, timestamp, body returns 200; (2) tampered body or wrong secret returns 401; (3) timestamp outside +/-5 minutes returns 401; (4) missin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement an SSE client-based test suite in Node/Python using EventSource (or httpx), simulate stock deltas across warehouses, verify idempotent snapshots after reconnects, check per-warehouse",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a distributed load test (e.g., k6) to saturate tenants with concurrent requests, asserting 429 responses with Retry-After. Verify admin-API quota updates propagate across nodes within a bounded wi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes practical streaming API testing: reconnection, dedup, partial outages, and aggregation correctness in a retail-scale setting.\n\n## Key Concepts\n- Server-Sent Events testing\n- Reconnection/backoff strategy\n- Deduplication via eventId\n- Downstream outage handling and fallback\n- Cross-warehouse data consistency\n\n## Code Example\n```javascript\n// Node SSE client skeleton\nconst EventSource = require('eventsource');\nconst es = new EventSource('https://api/inventory/stream');\nes.onmessage = e => console.log('evt', JSON.parse(e.data));\nes.onerror = () => es.close();\n```\n\n## Follow-up Questions\n- How would you simulate a partial upstream outage and verify graceful degradation?\n- What metrics would you collect to evaluate burst handling and stability?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-4",
        "questionId": "q-26279",
        "question": "Design a testing plan for an API that ingests batched JSON records via POST /ingest, processes asynchronously, and exposes status and results endpoints; include per-record validation, idempotency, partial-failure handling with DLQ, ordering guarantees, and end-to-end latency targets, plus concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Idempotency-Key must guarantee a single charge per key. Test steps: 1) POST /payments with key → 202, paymentId; 2) retry with same key and identical payload → same paymentId; 3) run concurrent reques",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan end-to-end tests for a REST + WebSocket analytics API: verify message ordering with sequence IDs, ensure at-least-once delivery and safe replay, validate reconnection/backoff under network faults",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define a strict JSON schema for POST /search; validate invalid types, missing required fields, and oversized payloads return 400 with actionable messages. Simulate rate limiting to yield 429 and Retry",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate an ingest batch job end-to-end: per-record schema checks, required fields, and max sizes; confirm idempotency by re-sending with the same idempotency key; simulate partial failures to push ba",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests for asynchronous ingestion are common in production systems and tricky due to eventual consistency, DLQ behavior, and idempotency guarantees.\n\n## Key Concepts\n- Asynchronous processing with status polling\n- Per-record schema validation and evolution\n- Idempotency and batch-level deduplication\n- Dead-letter queue routing for failed records\n- End-to-end latency, retries, and backoff\n\n## Code Example\n```javascript\n// Pseudo-test skeleton for batched ingest\nasync function testIngestBatch() {\n  const batch = [{a:1},{a:\"x\"}];\n  // POST /ingest with batch and idempotency-key\n  // poll /status/{batchId} until DONE\n  // fetch /results/{batchId} and validate outcomes\n}\n```\n\n## Follow-up Questions\n- How would you test out-of-order processing and its impact on results?\n- How would you verify idempotency across regional deployments?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-5",
        "questionId": "q-17704",
        "question": "Design an end-to-end test plan for POST /batch/infer that queues a batch and returns a jobId, with results posted to /callbacks/{jobId}. Verify idempotent submissions (same idempotency key yields single job), webhook signature validation, and eventual results within SLA. Simulate partial failures (1/3 records fail), ensure retries/backfill, and correct per-record statuses. Use mocks for model service, a test webhook receiver, and contract checks (PACT)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Idempotency-Key must guarantee a single charge per key. Test steps: 1) POST /payments with key → 202, paymentId; 2) retry with same key and identical payload → same paymentId; 3) run concurrent reques",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test idempotent behavior for POST /payments/process by issuing concurrent requests with the same idempotency_key and asserting a single charge with a consistent transaction_id, while retries return th",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe how you would validate the end-to-end flow of POST /batch/infer, including idempotent batch submissions, webhook signature validation, partial-failure handling with per-record status, and ret",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Outline a concrete test plan for GET /telemetry?vehicleId=... The endpoint reads real-time data from a Kafka feed and a batch Cassandra store. Validate cross-source consistency, ensure a defined stale",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEnd-to-end tests for asynchronous batch APIs ensure reliability where results arrive via webhooks and not immediately. It tests idempotency, security of callbacks, partial successes, and retry/backfill strategies under realistic traffic.\n\n## Key Concepts\n\n- Idempotent batch submission\n- Webhook validation (signature)\n- Eventual consistency and SLA\n- Partial failures and per-record status transitions\n- Replay/backfill of results\n\n## Code Example\n\n```javascript\n// Example: validate webhook signature during callback\nconst crypto = require('crypto');\nfunction validWebhook(payload, sig, secret){\n  const h = crypto.createHmac('sha256', secret).update(payload).digest('hex');\n  return crypto.timingSafeEqual(Buffer.from(sig), Buffer.from(h));\n}\n```\n\n## Follow-up Questions\n\n- How would you simulate bursty webhook delivery and ensure ordering guarantees?\n- How would you test idempotency keys at scale with distributed queues?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-6",
        "questionId": "q-12879",
        "question": "Design a beginner-friendly test plan for a POST /payments endpoint that initiates a one-time charge via an external gateway. Use an Idempotency-Key header to prevent duplicates, simulate gateway failures (429/503) with exponential backoff, and verify that retries do not create duplicate charges. Include webhook callback POST /payments/webhook with HMAC signature verification and end-to-end coverage for success, declined, and timeout scenarios. Include tooling recommendations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan includes: isolate tenants A and B with distinct policies; simulate latency spikes and downstream failure; verify retry logic uses exponential backoff with cap and that circuit-breakers trip",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan outline: enforce idempotency with Idempotency-Key on POST /payments; mock external gateway to return 429/503 then 200, verify no double charges by inspecting DB and events; validate webhook",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Approach: Create a bulk payload with mixed valid and invalid users. Validate per-item results (success, validation error). Test idempotency via Idempotency-Key: re-sending with same key should not create duplicates. Ver…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use regional load tests with k6 to ensure p95 latency stays under SLA across regions. Validate idempotency by sending repeated POST /payments with the same idempotency key and assert a single charge.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical API testing skills for real-world payment flows, focusing on idempotency, webhook security, and resilience, rather than abstract concepts.\n\n## Key Concepts\n\n- Idempotency and Idempotency-Key usage to avoid duplicate charges\n- Webhook validation using HMAC with a shared secret\n- Retry logic with exponential backoff and idempotent side effects\n- End-to-end verification tying client request, gateway result, and webhook callback\n\n## Code Example\n\n```javascript\n// Skeleton test using Jest and nock\nconst request = require('supertest');\nconst nock = require('nock');\n```\n\n## Follow-up Questions\n\n- How would you validate webhook replay protection?\n- How would you simulate gateway outages spanning minutes and ensure client timeout handling?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-7",
        "questionId": "q-6564",
        "question": "Design a beginner-friendly test plan for a long-running report-generation API where POST /reports starts a job and GET /reports/{jobId} polls status, and GET /reports/{jobId}/download returns the CSV when DONE. Include idempotency key handling for duplicate POSTs, timeouts, and failure modes (job crash), and provide concrete test cases and tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To test GET /profiles/{id} with conditional requests: fetch a fresh resource to capture ETag and Last-Modified. Re-request with If-None-Match and If-Modified-Since to expect 304. Update the resource a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan around an idempotent POST /orders with a unique idempotency key; verify a single committed order across Inventory, Payment, and Shipping. Simulate partial outages and replayed events; ensure retr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test plan should verify: 1) POST /reports with idempotency key returns 202 and a jobId; duplicates yield same jobId. 2) Polling: status flows PENDING -> RUNNING -> DONE with progress ETA. 3) After DON",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Tests: 1) valid signature and payload -> 200 and idempotent processing; 2) invalid signature -> 401; 3) missing X-Signature -> 400; 4) replayed event_id within 10 minutes -> only one processing; 5) de",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses beginner API-testing skills for async job flows, including idempotency, polling, and robust download validation.\n\n## Key Concepts\n- Async job lifecycle\n- Idempotency keys\n- Polling and timeouts\n- Content-Type and error schemas\n- Instrumentation and test data\n\n## Code Example\n```javascript\n// Example test sketch in pseudo-code\n```\n\n## Follow-up Questions\n- How would you test for concurrent polls altering status?\n- How would you integrate this into CI with flaky queue simulation?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-8",
        "questionId": "q-21241",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /exports that creates a long-running export job per user (tenant). The request must include an Idempotency-Key header. Validate: idempotent behavior on repeated POSTs, immediate 202 response with job_id, status transitions via GET /exports/{job_id} from PENDING to RUNNING to COMPLETED/FAILED, handling background worker downtime, and eventual consistency across repeated polls. Include tooling suggestions and concrete test cases?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Idempotent bulk import test: POST /imports/users with 100k records and an idempotency-key. Re-send with the same key to verify no duplicates. Inject partial downstream analytics failures to validate p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan should verify end-to-end exactly-once processing via idempotency keys and a transactional outbox. Include: duplicate event with same idempotency_key yields one charge and one outbox entry; h",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test idempotency by sending identical POST requests with the same idempotency key. The first request returns 201 Created with a unique transaction ID. Subsequent requests with the same key return 200 OK with the identic…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test idempotency by sending multiple POST /exports with the same Idempotency-Key and verify only one job is created and subsequent responses return the same job_id. Assert 202 with job_id and a Locati",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nExplores a new angle on API testing: idempotent long-running operations and eventual consistency for per-tenant exports, a common real-world pattern.\n\n## Key Concepts\n\n- Idempotency-Key handling across retries\n- Long-running jobs with 202 acceptance and polling\n- Status lifecycle: PENDING -> RUNNING -> FINAL (COMPLETED/FAILED)\n- Worker downtime simulation and resilience\n- Per-tenant data isolation in test data setup\n\n## Code Example\n\n```javascript\n// Pseudo-test: ensure idempotency and status polling\nasync function testExportIdempotent(key){\n  const r1 = await fetch('/exports', {method:'POST', headers:{'Idempotency-Key': key}, body: JSON.stringify({format:'csv'})});\n  const j1 = await r1.json();\n  const r2 = await fetch('/exports', {method:'POST', headers:{'Idempotency-Key': key}, body: JSON.stringify({format:'csv'})});\n  const j2 = await r2.json();\n  console.assert(j1.job_id === j2.job_id, 'Idempotent key should return same job_id');\n  let status = 'PENDING';\n  const job = j1.job_id;\n  while(['PENDING','RUNNING'].includes(status)){\n    const s = await fetch(`/exports/${job}`); const js = await s.json(); status = js.status;\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you simulate worker downtime and verify fallback behavior?\n- What metrics and logs would you collect to confirm eventual consistency across multiple clients?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-9",
        "questionId": "q-29490",
        "question": "Design a beginner-friendly API testing plan for a POST /payments that requires an idempotency-key header to prevent duplicate charges. Include how you validate that a first call succeeds, a second identical call within TTL reuses the same charge, how TTL expiration is handled, and how you test concurrent requests with the same key; plus tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: create a streaming API test that validates per-tenant isolation, message order, and replay handling. Use synthetic tenants with isolated streams, drive concurrency with load tests, inject ne",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a durable idempotency store keyed by idempotency-key with a 24h TTL; on first POST /payments create the charge and return 201 with a charge_id, caching the payload. On subsequent requests with the",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test plan for POST /v1/process/image in a multi-tenant service. Validate idempotency using Idempotency-Key, ensure large payloads stream correctly, confirm per-tenant routing meets SLA across backends",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan tests for POST /payments with Idempotency-Key: verify a single charge on first request; same key returns cached result without charging again; different keys create new charges. Test under concur",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question tests practical API testing for idempotent operations, a common financial pattern.\n\n## Key Concepts\n- Idempotency keys, TTL, parallel requests, race conditions\n- Response consistency, deterministic outcomes, fixture data\n\n## Code Example\n```javascript\n// Pseudo-test: ensure idempotency returns cached response on repeat\n```\n\n## Follow-up Questions\n- How would you handle retrieval of the cached response for a high-priority webhook fallback?\n- How would you simulate network partitions to test idempotency under partial failures?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-10",
        "questionId": "q-13860",
        "question": "Design an end-to-end test strategy for a POST /audit-logs endpoint that accepts batched log entries and queues them for asynchronous processing by three downstream systems (ingestion DB, analytics warehouse, and search index). Include idempotency handling with an Idempotency-Key, retries with backoff, exactly-once processing guarantees, cross-system data consistency checks, and failure modes when any downstream service becomes unavailable. Provide concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an idempotent export system using export_id to deduplicate POST /exports requests; simulate 1000 concurrent submissions to ensure exactly one export is queued while others return appropriate responses. Validat…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an idempotent contract with Idempotency-Key for POST /audit-logs. Send a batch twice; verify only one run creates artifacts across ingestion DB, analytics warehouse, and search index. Check 202 an",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a test matrix around per-tenant feature flags and edge routing. Include canary rollout: Tenant A flag on, Tenant B off; observe 0–30s propagation to edge caches. Validate responses reflect flag",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a test harness with Locust for load and a lightweight SSE client to /reports/stream?tenant=. Run two tenants in parallel; assert tenant-scoped events and correct order; verify replay semantics on",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests that exercise asynchronous orchestration and cross-service consistency under failure require testers to reason about idempotency, DLQ, and eventual guarantees. This question probes practical skills in integration testing, retries/backoff, and observability.\n\n## Key Concepts\n\n- Idempotency with Idempotency-Key\n- Exactly-once processing across ingestion DB, analytics warehouse, and search index\n- Retries/backoff and circuit breakers\n- End-to-end data consistency checks and DLQ\n- Failure modes and monitoring\n\n## Code Example\n\n```javascript\n// Example test skeleton illustrating idempotent batch post\nasync function testIdempotentBatchPost(apiClient, batch, key) {\n  await apiClient.post('/audit-logs', { batch }, { headers: { 'Idempotency-Key': key } })\n  // repeat with same key and assert single downstream event\n}\n```\n\n## Follow-up Questions\n\n- How would you verify dedup when downstream stores have delayed visibility?\n- What metrics and logs would prove correct DLQ handling and retry behavior?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-11",
        "questionId": "q-23462",
        "question": "Design a beginner-friendly test plan for the GET /prices endpoint in a multi-region catalog. The endpoint uses Accept-Language and region query parameters to return locale-specific currency and tax rules. Include regional validation for en-US (USD) and en-GB (GBP), fallback to en-US for unsupported locales, verify fields (price, currency, tax), auth via X-API-KEY, rate limits, and cache-control headers. Provide tooling and concrete acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan tests for a REST API delivering per-tenant feature flags with dynamic rollout and A/B experiments. Verify per-user bucketing via a deterministic hash, check distribution fairness within tolerance",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a comprehensive test matrix covering regions (US/UK) and locales (en-US/en-GB), with fallback validation for unsupported locales. Verify the presence and correctness of price, currency, and tax fields, ensuring c…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "1) GET /articles/1 to obtain the initial ETag. 2) GET with If-None-Match set to that ETag; expect 304 Not Modified. 3) Update the article, then GET again; expect 200 with a new ETag. 4) Simulate concu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test plan should cover: verify ETag/If-None-Match returns 304 when data unchanged; check Cache-Control max-age and s-maxage; simulate first request populating cache, subsequent requests hit cache; str",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nTesting regionalization and locale-driven data represents a practical entry-level challenge that validates foundational API testing skills without requiring complex system design knowledge.\n\n## Key Concepts\n\n- Localization and region-specific testing\n- Region/locale fallback logic validation\n- Data correctness verification (price, currency, tax)\n- Authentication and rate-limiting checks\n- Cache headers and performance optimization basics\n\n## Code Example\n\n```javascript\nasync function testPrices(region, locale) {\n  const res = await fetch(`/prices?region=${region}&locale=${locale}`, {\n    headers: { 'X-API-KEY': 'test-api-key' }\n  });\n  \n  return {\n    status: res.status,\n    data: await res.json()\n  };\n}\n\n// Test cases\nconst tests = [\n  { region: 'US', locale: 'en-US', expectCurrency: 'USD' },\n  { region: 'UK', locale: 'en-GB', expectCurrency: 'GBP' },\n  { region: 'US', locale: 'de-DE', expectCurrency: 'USD' } // fallback\n];\n```",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-12",
        "questionId": "q-19579",
        "question": "Design a practical test plan for a REST API that supports asynchronous long-running jobs via POST /jobs, polls via GET /jobs/{id}, and a webhook callback to /webhooks/job-complete when finished. Include scenarios for duplicate submissions, webhook replay, out-of-order webhooks, backpressure, and regional failover?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Test Plan: GET /reports/{tenantId} ### 1. Functional Tests #### Input Validation (tenantId formats) | Test ID | Test Description | Input | Expected Result | |---------|------------------|-------|-----------------| |…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test idempotent creation with repeated POST /payments using the same Idempotency-Key to ensure a single record is created and same Location is returned. Validate 400 on invalid payload, 401 on missing",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test input validation: invalid JSON, missing header, empty array -> 400. Idempotency: same X-Idempotency-Key returns same batchId; no duplicate processing on retry. Processing mode: verify all-or-noth",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a test plan that validates asynchronous job submission, idempotent behavior, polling semantics, and webhook integrity. Verify POST /jobs with idempotency-key yields single queued job; poll tra",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical testing of async APIs with callbacks, covering idempotency, event ordering, delivery guarantees, and resilience under failover.\n\n## Key Concepts\n\n- Async job lifecycle and polling\n- Webhook security (signatures/HMAC)\n- Idempotent operations and replay protection\n- Event ordering and out-of-order handling\n- Backpressure and rate limiting\n- Multi-region failover and redirection\n\n## Code Example\n\n```javascript\n// Example pseudo-test structure\nconst res1 = post('/jobs', { task: 'train', data:'s3://bucket' }, { idempotencyKey: 'abc-123' });\nconst status = get(`/jobs/${res1.id}`);\nassert(status.phase === 'completed' || status.phase === 'running');\n```\n\n## Follow-up Questions\n\n- How would you simulate out-of-order webhooks in a test suite?\n- How would you design webhook replay protection and state reconciliation?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-13",
        "questionId": "q-28287",
        "question": "Design an API testing plan for a REST service enforcing per-user RBAC via a central policy engine with regional edge caches. The test should validate rapid policy propagation, race conditions during policy updates, and default-deny behavior for missing tokens. Include concrete test cases, tooling, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan end-to-end tests for a REST + WebSocket analytics API: verify message ordering with sequence IDs, ensure at-least-once delivery and safe replay, validate reconnection/backoff under network faults",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test GET /stream/alerts using Server-Sent Events. Verify 200 and Content-Type: text/event-stream. Capture the first N events and confirm each starts with data: and is valid JSON; ensure id values incr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a comprehensive API testing plan that validates per-user RBAC enforcement across a distributed architecture consisting of a central policy decision point (PDP) and regional edge caches. The plan should cover thre…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Valid login returns 200 with a non-empty token; then /me returns 200 when using that token. Test invalid credentials should yield 401; missing fields should yield 400. Example commands: curl -s -X POS",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question assesses understanding of real-time policy-driven access control in distributed systems with edge caching. It evaluates knowledge of propagation latency, consistency guarantees under concurrent updates, and secure default-deny behavior.\n\n## Key Concepts\n- RBAC enforcement via a central policy engine\n- Regional edge caching with eventual consistency\n- Policy propagation and cache invalidation strategies\n- Observability: metrics, TTLs, and cache refresh mechanisms\n- Contract testing between API gateway and policy engine",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-14",
        "questionId": "q-11702",
        "question": "Design a canary-flag test plan for a multi-region Content Recommendations API where feature flags come from a distributed config store (etcd). Validate per-tenant isolation, cache invalidation TTLs, and resilience during config outages. Include concurrent flag updates, cross-region propagation latency, rollback safety, and concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An effective plan should verify per-tenant data masking with explicit assertions and no cross-tenant leakage; use ETag/If-None-Match for optimistic concurrency during updates; test Content Negotiation",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Test Plan: PATCH /inventory/{sku} with Optimistic Concurrency Control ### Precondition Ensure GET /inventory/{sku} returns an ETag header in the response. ### Test Cases **TC1: Successful Update with Valid ETag** 1.…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a comprehensive multi-tenant streaming test framework with HTTP/2 SSE as the primary protocol and WebSocket as the fallback mechanism. Validate stream initialization by confirming 200 OK responses with appropriate C…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a canary-flag test suite: simulate flag flips in a distributed config store (etcd) and measure propagation latency across regions. Validate per-tenant isolation by querying tenants concurrently",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess ability to design end-to-end API tests for dynamic feature flags and multi-region consistency under outages.\n\n## Key Concepts\n\n- Distributed config propagation latency\n- Per-tenant isolation and cache invalidation\n- Canary rollouts and rollback safety\n- Observability and chaos testing\n\n## Code Example\n\n```javascript\n// Pseudo-test: verify flag flip propagates across regions within TTL\nasync function testFlagPropagation(regions, tenantId, flagKey, expected) {\n  const before = await fetchRecommendations(tenantId, regions[0]);\n  await flipFlag(flagKey, true); // in etcd\n  for (const r of regions) {\n    const v = await pollUntilChanged(() => fetchRecommendations(tenantId, r), old => old !== before, 60000);\n    if (v === before) throw new Error('Flag did not propagate in region ' + r);\n  }\n  return true;\n}\n```\n\n## Follow-up Questions\n\n- How would you measure TTL impact on user-perceived latency during flag flips?\n- How would you test partial outages where some regions see new flags while others don't?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-15",
        "questionId": "q-18291",
        "question": "Design a practical API test plan for a high-throughput payment processing REST API that supports idempotent posting with idempotency-key headers, ensures exactly-once processing across retries, and guarantees ordered ledger events under partial upstream outages. Describe test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate correctness under eventual consistency between the read-model and streaming data: verify read-your-writes within a defined drift window, ensure per-tenant isolation under concurrency, simulat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement signature verification with HMAC-SHA256 using a shared secret; store processed event_ids to enforce idempotency; route events per merchant into a per-merchant queue so FIFO is preserved; tes",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose end-to-end tests for a high-throughput payment API that uses idempotency-key headers to achieve exactly-once processing. Include cases for replay with same key, out-of-order deliveries, partia",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a test that sends 1000 events with a deterministic idempotency-key to POST /events, triggers retries, and confirms downstream analytics see exactly 1000 unique messages. Simulate broker outage",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests distributed systems correctness under concurrency, idempotency, ordering, and partial outages; validates that the API and ledger service maintain exactly-once semantics and message ordering.\n\n## Key Concepts\n\n- Idempotency keys, exactly-once processing\n- Ordering guarantees in distributed ledger\n- Partial upstream outages and retry semantics\n- End-to-end testing with simulated upstream services and timeouts\n- Tooling: Postman/Newman, k6, WireMock, Kafka test doubles\n\n## Code Example\n\n```javascript\n// Example k6 test that repeats a payment request with same idempotency key\nimport http from 'k6/http';\nimport { check, sleep } from 'k6';\n\nexport default function () {\n  const payload = { amount: 1000, currency: 'USD', account: 'A1' };\n  const headers = { 'Content-Type': 'application/json', 'Idempotency-Key': 'abc-123' };\n  const res = http.post('https://api/payments', JSON.stringify(payload), { headers });\n  check(res, { 'status 200': (r) => r.status === 200 || r.status === 409 });\n\n  sleep(0.5);\n}\n```\n\nNote: The code snippet is minimal; ensure you adapt to your API.",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-16",
        "questionId": "q-236",
        "question": "How would you implement a comprehensive contract testing strategy using MSW (Mock Service Worker) with OpenAPI to ensure frontend API mocks stay synchronized with backend specifications, including CI/CD integration and drift detection?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test idempotency by resubmitting identical payloads and asserting the same jobId and final status is returned. Verify state transitions flow correctly from PENDING → PROCESSING → COMPLETED (or FAILED) with appropriate t…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "An effective plan should verify per-tenant data masking with explicit assertions and no cross-tenant leakage; use ETag/If-None-Match for optimistic concurrency during updates; test Content Negotiation",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create per-tenant mock listeners, replay the same eventId to verify idempotency (only one delivery). Inject outages to validate retries and exponential backoff; ensure no cross-tenant leakage. Verify",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Generate MSW handlers from OpenAPI using swagger-to-msw or openapi-msw-mock, validate responses against schema in tests, integrate contract tests in CI pipeline to detect API drift, implement version-controlled mock dat…",
            "isCorrect": true
          }
        ],
        "explanation": "## Core Implementation\n\nGenerate MSW handlers programmatically from OpenAPI spec:\n\n```javascript\n// Generate handlers from OpenAPI\nimport { generateHandlers } from 'openapi-msw-mock';\nconst handlers = generateHandlers(openApiSpec);\n```\n\n## CI/CD Integration\n\nAdd contract tests to pipeline:\n\n```yaml\n# GitHub Actions\n- name: Contract Tests\n  run: npm run test:contract\n- name: API Drift Check\n  run: npm run check:api-drift\n```\n\n## Drift Detection\n\nImplement schema validation in tests:\n\n```javascript\nimport { validateResponse } from 'ajv';\n\nit('validates against OpenAPI schema', async () => {\n  const response = await client.get('/users');\n  expect(validateResponse(schema, response.data)).toBe(true);\n});\n```\n\n## Mock Data Management\n\nVersion mock data alongside API specs, use factories for realistic test data, and implement response caching for performance. This ensures frontend and backend contracts remain synchronized throughout development lifecycle.",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-17",
        "questionId": "q-4532",
        "question": "Design and implement a test strategy for a high-volume Order Management API used by a global fleet (Tesla, DoorDash, Citadel-scale) exposing POST /orders, GET /orders/{id}, and a real-time WebSocket stream at /orders/stream. Ensure idempotent order creation, exactly-once delivery for status events, out-of-order handling, and cross-region data consistency with Kafka and PostgreSQL; include concrete test cases, tooling, and failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a durable idempotency store keyed by idempotency-key with a 24h TTL; on first POST /payments create the charge and return 201 with a charge_id, caching the payload. On subsequent requests with the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement idempotency using an Idempotency-Key header on POST /orders with a Redis-backed map to prevent duplicate order creation. For the WebSocket stream at /orders/stream, include sequence numbers in events and imple…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Run parallel requests with the same Idempotency-Key to confirm a single mutation; ensure replay returns 200 with unchanged state and no duplicate side effects; simulate clock skew across nodes, crash",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Define a strict JSON schema for POST /search; validate invalid types, missing required fields, and oversized payloads return 400 with actionable messages. Simulate rate limiting to yield 429 and Retry",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTesting idempotency and reliable streaming under global scale is critical for high-stakes financial and logistics systems where duplicate orders or missed events can cause significant business impact.\n\n## Key Concepts\n- Idempotency keys with Redis storage for duplicate prevention\n- Exactly-once streaming using sequence IDs and consumer idempotence\n- Cross-region consistency with multi-DC PostgreSQL and Kafka replication\n- Fault injection testing for partial outages and latency budgets\n- Contract testing for API and streaming interfaces\n\n## Code Example\n```javascript\nasync function testIdempotentCreate(client, orderPayload, idempotencyKey) {\n  const res1 = await client.post('/orders', orderPayload, { \n    headers: { 'Idempotency-Key': idempotencyKey } \n  });\n  const res2 = await client.post('/orders', orderPayload, { \n    headers: { 'Idempotency-Key': idempotencyKey } \n  });\n  assert.equal(res1.status, 201);\n  assert.equal(res2.status, 200);\n  assert.equal(res1.body.id, res2.body.id);\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-18",
        "questionId": "q-6872",
        "question": "Design a realistic end-to-end test plan for GET /users/{uid}/transactions with cursor-based pagination and GET /users/{uid}/transactions/stream. Include data consistency across microservices, idempotent retries and stream dedup, simulated upstream outages (DB, fraud service), and strict multi-tenant data isolation. Provide concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use consumer-driven contracts (Pact). Consumers publish contracts to a broker; CI fetches latest contracts and runs provider verifications in a dedicated env per tenant/feature flag. Version contracts",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use the Idempotency-Key header to guard POST /cart/items. Re-run identical requests with same key and confirm no duplicates; on transient 5xx, retry using same key and compare final item count; ensure",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an automated end-to-end plan: validate OpenAPI contracts with Pact; generate synthetic tenants and users; test cursor-based pagination edge cases; verify streaming delivers at-least-once updat",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Test the POST /orders endpoint using the X-Customer-ID header to enforce per-customer data isolation. Validate all required fields and confirm the API returns 400 Bad Request for invalid or incomplete payloads. Verify i…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for REST + streaming under multi-tenant data PODs with failure modes are common in high-scale apps; this checks reliability, isolation, and observability.\n\n## Key Concepts\n- Contract testing (Pact/OpenAPI)\n- Cursor pagination correctness\n- Streaming dedup / backpressure\n- Fault injection / resilience\n- Multi-tenant data isolation and security\n- Observability and tracing\n\n## Code Example\n```javascript\n// Pseudo-test: subscribe to /stream and verify deduplicated events\nconst stream = fetchStream(`/users/${uid}/transactions/stream`);\nconst seen = new Set();\nfor await (const evt of stream) {\n  if (seen.has(evt.id)) throw new Error('Dedup');\n  seen.add(evt.id);\n}\n```\n\n## Follow-up Questions\n- How would you simulate partial upstream outages in CI vs. staging?\n- How would you verify per-tenant isolation with noisy neighbors?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-19",
        "questionId": "q-22573",
        "question": "Design a pragmatic end-to-end test plan for a multi-tenant REST API GET /orders/{id} that is evolving: add an optional shipEta field while deprecating customerNote; ensure backward compatibility and per-tenant isolation. Include contract testing (PACT/OpenAPI), drift checks, performance targets (95th percentile latency), and resilience to shipping-service outages with retries. Provide concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "First, retrieve the current ETag for /profiles/{id} via a GET request. Then perform a PUT request with a modified payload, including the If-Match header set to the retrieved ETag; expect a 200 OK response along with a n…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a test that sends 1000 events with a deterministic idempotency-key to POST /events, triggers retries, and confirms downstream analytics see exactly 1000 unique messages. Simulate broker outage",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A pragmatic end-to-end test plan for evolving the GET /orders/{id} endpoint: **1. Contract Testing (PACT & OpenAPI)** - Implement consumer-driven contracts with PACT between OrderService (provider) and consumers (OrderU…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a comprehensive test strategy that includes tenant-scoped test data generation to validate per-tenant isolation guarantees. Implement contract tests using Pact between the POST /ingest endpoint and downstream pro…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your understanding of API evolution strategies, contract testing at scale, and maintaining backward compatibility in multi-tenant systems. It tests practical knowledge of resilience patterns, performance validation, and coordination across service boundaries.\n\n## Key Concepts\n- Consumer-driven contracts (PACT/OpenAPI) for API versioning\n- Additive changes vs. breaking changes in REST APIs\n- Per-tenant data isolation and security validation\n- Latency SLAs and p95/percentile measurements\n- Circuit breakers, retries, and fallback strategies\n- Feature-flagged deprecations for safe rollouts\n\n## Code Example\n```javascript\n// PACT consumer test for GET /orders/{id}\nconst { Pact } = require('@pact-foundation/pact');\nconst provider = new Pact({\n  consumer: 'OrderUI',\n  provider: 'OrderService',\n  cors: true\n});\n\ndescribe('GET /orders/{id}', () => {\n  before(() => provider.setup());\n  \n  it('returns order with optional shipEta', async () => {\n    await provider.addInteraction({\n      state: 'an order exists with shipping info',\n      uponReceiving: 'a request for order details',\n      withRequest: { method: 'GET', path: '/orders/123' },\n      willRespondWith: {\n        status: 200,\n        body: {\n          id: '123',\n          customerNote: 'Please leave at door', // deprecated but present\n          shipEta: '2024-12-25T10:00:00Z' // new optional field\n        }\n      }\n    });\n    \n    const response = await axios.get(`${provider.mockService.baseUrl}/orders/123`);\n    expect(response.data).to.have.property('shipEta');\n    expect(response.data.customerNote).to.not.be.undefined; // backward compat\n  });\n});\n```\n\n## Follow-up Questions\n- How would you handle contract versioning when multiple client versions coexist in production?\n- What strategies would you use to validate tenant isolation under high concurrency?\n- How do you coordinate deprecation timelines across dependent services?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-20",
        "questionId": "q-14650",
        "question": "Design an end-to-end test strategy for an asynchronous bulk import API: POST /bulk/import uploads a CSV and returns a jobId; GET /bulk/import/{jobId} reports status, processed rows, and per-row errors. How would you validate idempotent submissions, per-tenant isolation, and resilience under queue outages and worker restarts with retries; and what concrete test cases and tooling would you use?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan an end-to-end test for a real-time, multi-tenant SaaS API spanning three regions with dynamic per-tenant feature flags. Seed regional shards, drive high-concurrency user lookups, verify per-tenan",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan tests for a REST API delivering per-tenant feature flags with dynamic rollout and A/B experiments. Verify per-user bucketing via a deterministic hash, check distribution fairness within tolerance",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end test plan for a multi-region REST /checkout that triggers downstream services (inventory, payment, fulfillment) asynchronously via a message bus. Validate idempotency with idempot",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design and implement a comprehensive end-to-end test strategy for an asynchronous bulk import API where POST /bulk/import accepts CSV uploads and returns a jobId, while GET /bulk/import/{jobId} provides real-time status…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Matters\n\nTesting asynchronous, event-driven APIs requires deep understanding of eventual consistency, idempotency patterns, and multi-tenant data isolation. This question evaluates your ability to design comprehensive test coverage for complex failure scenarios including queue infrastructure downtime, worker process crashes, retry logic implementation, and maintaining data integrity across retry attempts.\n\n## Core Technical Concepts\n\n- Asynchronous processing patterns with polling-based status updates\n- Idempotent request handling and duplicate submission prevention\n- Multi-tenant data isolation and security boundaries\n- Resilience testing for queue failures and worker restart scenarios\n- Retry semantics with exponential backoff and circuit breaker patterns\n- Comprehensive observability through metrics, structured logging, and audit trails\n\n## Implementation Example\n\n```python\n# Example pytest framework skeleton for end-to-end testing\nimport pytest\nimport asyncio\nfrom aiohttp import ClientSession\nimport time\n\nclass TestBulkImportAPI:\n    async def test_idempotent_submissions(self):\n        # Verify duplicate submissions with same data\n        # produce identical jobIds or reference same processing\n        pass\n    \n    async def test_per_tenant_isolation(self):\n        # Ensure data segregation between tenants\n        # Validate cross-tenant data leakage prevention\n        pass\n    \n    async def test_queue_failure_resilience(self):\n        # Simulate queue downtime and worker crashes\n        # Verify graceful degradation and recovery\n        pass\n```\n\n## Key Test Scenarios\n\n1. **Idempotency Testing**: Duplicate CSV submissions, concurrent identical requests, and deduplication mechanisms\n2. **Isolation Validation**: Cross-tenant data boundaries, namespace separation, and security rule enforcement\n3. **Failure Resilience**: Queue outages, worker crashes, network partitions, and retry limit behaviors\n4. **Data Integrity**: Exactly-once processing, transaction consistency, and error recovery validation\n\n## Recommended Tooling\n\n- **Test Framework**: pytest with async support for concurrent testing\n- **API Testing**: aiohttp or httpx for async HTTP client operations\n- **Mock Services**: LocalStack for AWS queue simulation, Docker containers for service isolation\n- **Observability**: Prometheus metrics, ELK stack for log aggregation, Jaeger for distributed tracing\n- **Load Testing**: k6 or Locust for concurrency and stress testing scenarios\n\nThis comprehensive approach ensures robust validation of your asynchronous bulk import system under various operational conditions.",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-21",
        "questionId": "q-453",
        "question": "You're testing a REST API that returns paginated results. The endpoint has a rate limit of 100 requests per minute and sometimes returns 500 errors under load. How would you design a comprehensive test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Draft a test plan: verify idempotency by sending duplicates for the same event_id and asserting a single aggregate in GET /events/{event_id}; simulate downstream outages to ensure at-least-once delive",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan for GET /weather?city={city} aggregating two providers with 60s cache and 5s per-provider timeouts. Verify: (a) both up returns average temperature; (b) one down still returns from the other",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement comprehensive load testing with gradual ramp-up using tools like k6 or JMeter to simulate realistic traffic patterns. Add retry logic with exponential backoff for 500 errors, implementing circuit breakers to p…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Open an SSE stream for a ticker and assert the first event payload contains {ticker, price, timestamp}. Validate each event's price is a number and timestamp is monotonically increasing. Simulate a di",
            "isCorrect": false
          }
        ],
        "explanation": "## Test Strategy Components\n\n- **Load Testing**: Simulate realistic user traffic patterns with gradual ramp-up\n- **Rate Limiting**: Implement request throttling and batching to respect API limits\n- **Error Handling**: Test retry mechanisms with exponential backoff and circuit breakers\n- **Contract Testing**: Validate API responses against schemas and documentation\n- **Monitoring**: Track performance metrics, error rates, and system resource utilization\n\n## Tools & Implementation\n\n```javascript\n// Example retry logic with exponential backoff\nconst retryRequest = async (url, retries = 3) => {\n  for (let i = 0; i < retries; i++) {\n    try {\n      const response = await fetch(url);\n      if (response.ok) return response;\n      \n      // Exponential backoff: 1s, 2s, 4s\n      const delay = Math.pow(2, i) * 1000;\n      await new Promise(resolve => setTimeout(resolve, delay));\n    } catch (error) {\n      if (i === retries - 1) throw error;\n    }\n  }\n};\n\n// Rate limiting implementation\nclass RateLimiter {\n  constructor(maxRequests = 100, timeWindow = 60000) {\n    this.maxRequests = maxRequests;\n    this.timeWindow = timeWindow;\n    this.requests = [];\n  }\n  \n  async makeRequest(requestFn) {\n    const now = Date.now();\n    this.requests = this.requests.filter(time => now - time < this.timeWindow);\n    \n    if (this.requests.length >= this.maxRequests) {\n      const waitTime = this.timeWindow - (now - this.requests[0]);\n      await new Promise(resolve => setTimeout(resolve, waitTime));\n    }\n    \n    this.requests.push(now);\n    return await requestFn();\n  }\n}\n```\n\n## Testing Phases\n\n1. **Baseline Testing**: Establish performance metrics under normal load\n2. **Stress Testing**: Identify breaking points and failure modes\n3. **Spike Testing**: Test sudden traffic increases and recovery\n4. **Endurance Testing**: Validate performance over extended periods\n5. **Rate Limit Testing**: Verify behavior at and beyond rate limits",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-22",
        "questionId": "q-11267",
        "question": "Design a pragmatic test plan for a multi-tenant GraphQL API with federation and per-tenant extensions, including: access control and data masking validation; detection of N+1 queries in nested resolvers using batched load patterns; per-tenant schema contract testing; performance testing with caching and batching; and resilience testing against partial upstream outages. Provide concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose layered testing: unit tests for resolvers with mocked batch loaders to catch N+1; integration tests against staging with tenant tokens validating access control and data masking; contract test",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Define a strict JSON schema for POST /search; validate invalid types, missing required fields, and oversized payloads return 400 with actionable messages. Simulate rate limiting to yield 429 and Retry",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use consumer-driven contracts (Pact). Consumers publish contracts to a broker; CI fetches latest contracts and runs provider verifications in a dedicated env per tenant/feature flag. Version contracts",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a contract-driven end-to-end suite with Pact between rides, drivers, and payments services, plus chaos testing. Verify idempotency on POST /rides, ensure eventual consistency via rea",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nIn multi-tenant GraphQL APIs with federation and dynamic extensions, testing must cover authorization, per-tenant schema correctness, N+1 risks, performance under batching, and resilience to partial failures. This question probes the ability to craft end-to-end test coverage that mirrors real-world deployment with isolation guarantees and evolving schemas.\n\n## Key Concepts\n- Multi-tenant GraphQL + federation + per-tenant extensions\n- Access control and data masking validation\n- N+1 detection via batched data loaders\n- Contract testing for per-tenant schema drift\n- Performance testing with caching and batching patterns\n- Resilience/chaos testing for partial upstream outages\n\n## Code Example\n```javascript\n// Example: unit test for a resolver with a batched loader\ntest('Orders field batches loads', async () => {\n  const loadOrders = jest.fn().mockResolvedValue([{id: '1'}, {id: '2'}]);\n  const result = await resolveOrders({ user: 'u1' }, { loaders: { orders: loadOrders } });\n  expect(loadOrders).toHaveBeenCalledTimes(1);\n  expect(result).toHaveLength(2);\n});\n```\n\n## Follow-up Questions\n- How would you detect N+1 in a live system using tracing tools like OpenTelemetry?\n- How would you evolve tests when per-tenant schemas drift or new extensions are rolled out?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-23",
        "questionId": "q-5216",
        "question": "Design an intermediate test plan for POST /webhooks/internal that uses HMAC signatures and a Redis-backed replay cache. How would you verify signature correctness and replay protection under high velocity, ensure idempotent downstream state updates for duplicate payloads, and simulate downstream outages (e.g., 503) and Redis failures? Include concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an end-to-end plan for POST /webhooks/internal that uses HMAC signatures and a Redis-backed replay cache. Verify signature correctness and replay protection under high velocity; ensure idempote",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Idempotent bulk import test: POST /imports/users with 100k records and an idempotency-key. Re-send with the same key to verify no duplicates. Inject partial downstream analytics failures to validate p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a comprehensive WebSocket test plan using a test client to simulate multiple tenants on /ws/updates. Validate: (1) JWT-based tenant authentication and isolation, (2) message ordering guarantees per tenant, (3)…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a durable idempotency store (Redis/DynamoDB) with a TTL. On first POST, create the order, publish to Kafka, then store idempotency-key -> orderId. Replays with same key return 200 and same orderId",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests must cover security, correctness, and resilience of webhook ingestion in real-time systems.\n\n## Key Concepts\n- HMAC verification and replay protection\n- Idempotent processing across at-least-once delivery\n- Redis-backed replay cache behavior\n- Downstream fault tolerance and retries\n- Contract testing for webhook contracts\n\n## Code Example\n```javascript\n// Example test harness outline\nconst payload = { event_id: 'evt_123', type: 'order.updated', data: { orderId: 'ord_1' } };\n// mock signature and send to webhook endpoint\n```\n\n## Follow-up Questions\n- How would you test duplicate event_id eviction in Redis?\n- How would you simulate canary/webhook replay?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-24",
        "questionId": "q-5298",
        "question": "Design an end-to-end test strategy for a batch enrichment API that orchestrates Customer, Orders, and Loyalty services. The POST /batch/enrich accepts {batchId, items:[{customerId, fields:[demographics,risk_score,loyalty_status]}]} and returns 202 with operation_id. A streaming endpoint GET /batch/enrich/{id}/events emits progress via SSE. Include concrete test cases, failure modes (queue outages, partial DB outages), and tooling to validate idempotency, ordering, and rollbacks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pact for consumer-driven contracts between the REST API, async worker, and the index layer. Validate both paths: synchronous small-queries and asynchronous large-exports. Include: 1) contract test",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: POST /batch/enrich with batchId and items; expect 202 and operation_id. Subscribe to /batch/enrich/{id}/events SSE to verify ordered progress and final enriched fields. Mock upstream via Wir",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design an end-to-end tracing test plan for a multi-tenant API gateway using OpenTelemetry: verify trace context propagation across Auth, Gateway, and per-tenant services; test sampling rates, span generation, retries, a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement idempotent webhook processing by persisting provider+event_id and rejecting duplicates; verify HMAC signature first. Test cases: a) duplicate delivery, b) out-of-order events, c) transient d",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end orchestration across multiple services and streaming updates; evaluates resilience, idempotency, and data integrity in async batch processing.\n\n## Key Concepts\n\n- Async batch orchestration across services\n- Idempotency and deduplication\n- SSE stream correctness and ordering\n- Partial failures and rollback/compensation\n- End-to-end tooling and observability\n\n## Code Example\n\n```javascript\n// Pseudo-test sketch (not executable here)\nasync function testBatchEnrich(batch) {\n  // 1) POST and capture operation_id\n  // 2) connect to SSE stream and validate events\n  // 3) fetch final state and compare enriched fields\n}\n```\n\n## Follow-up Questions\n\n- How would you validate event ordering under concurrency?\n- How would you model and test compensating actions after partial outages?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-14T07:37:15.004Z",
    "version": 1
  }
]