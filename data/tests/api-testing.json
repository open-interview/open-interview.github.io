[
  {
    "id": "test-api-testing",
    "channelId": "api-testing",
    "channelName": "API Testing",
    "title": "API Testing Knowledge Test",
    "description": "Test your understanding of API Testing with 25 questions.",
    "questions": [
      {
        "id": "api-testing-q-0",
        "questionId": "q-10129",
        "question": "Design a practical test plan for a high-volume batch API: POST /payments/batch accepts up to 500 payments per payload with a per-batch idempotency key and a downstream settlement service. Validate exactly-once semantics at the batch level, deterministic retries, and compensating transactions for partial failures under simulated network partitions. Include data, tooling, and observability requirements?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify that POST /events with the same Idempotency-Key yields a single created event (first call 201, retry returns 200) and duplicates are ignored; test 400/401/403 for invalid keys or payloads; ensu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: replay and dedup tests using idempotency keys per tenant; verify cross-tenant isolation by attempting cross-tenant data access. Validate retries with exponential backoff and max retry counts; si",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a test harness with Locust for load and a lightweight SSE client to /reports/stream?tenant=. Run two tenants in parallel; assert tenant-scoped events and correct order; verify replay semantics on",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan includes batch_id idempotency checks, ledger dedupe verification on replay, and compensating transactions for partial downstream failures. Simulate partial success/failure, partitioned network, a",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssess ability to design robust batch testing for financial-style batch processing where idempotency and exactly-once semantics matter, and safe recovery is required when downstream outcomes are partial.\n\n## Key Concepts\n- Batch-level idempotency keys and idempotent operations\n- Exactly-once vs at-least-once semantics\n- Downstream settlement integration and compensating actions\n- Observability: tracing, idempotency guardrails, dashboards\n- Failure modes: partial success, network partitions, timeouts, retries\n\n## Code Example\n```javascript\n// Pseudo test skeleton\nasync function testBatchIdempotentReplay() {\n  const batch = { batch_id: \"BATCH-001\", payments: [/* mock items */] };\n  await httpPost(\"/payments/batch\", batch);\n  await httpPost(\"/payments/batch\", batch); // replay\n  // Assert ledger contains single batch and no duplicate payments\n}\n```\n\n## Follow-up Questions\n- How would you test idempotency under high concurrency and varying batch_id scopes?\n- How would you verify eventual consistency between batch ledger and downstream settlement during failure/recovery?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-1",
        "questionId": "q-13334",
        "question": "Design a beginner-friendly test plan for a new endpoint POST /orders that accepts JSON {customerId, items: [{sku, qty}], address}. The API uses an Idempotency-Key header to ensure exactly-once processing. Provide concrete test cases to verify idempotent behavior, error handling, and edge cases (out-of-stock, payment failures, network retry). Include tooling suggestions and data setup?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Idempotency-Key must guarantee a single charge per key. Test steps: 1) POST /payments with key → 202, paymentId; 2) retry with same key and identical payload → same paymentId; 3) run concurrent reques",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## GraphQL Federation Gateway API Testing Plan ### 1. Testing Scope Overview **System Under Test:** - GraphQL Federation Gateway (single API endpoint) - Multiple per-tenant subgraphs with schema variants - Persisted que…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Submit /orders with idempotency_key 'k1' twice; second response must be same order and no duplicates. Simulate broker outage during commit to verify compensating/rollback behavior and eventual retry.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate idempotency by replaying the same POST with a single Idempotency-Key and confirm only one order is created and responses match; test different keys create separate orders; simulate out-of-sto",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests a realistic idempotent POST pattern and how to verify it under failure modes common in e-commerce APIs.\n\n## Key Concepts\n- Idempotency and idempotency keys\n- Exactly-once processing vs at-least-once semantics\n- Retries, backoff, and fault injection\n- Mocking external services (payments, inventory)\n\n## Code Example\n```javascript\n// Example test sketch (pseudo)\ndescribe('POST /orders idempotency', () => {\n  it('returns same order on replay with same key', async () => {\n    const key = 'abc-123'\n    const payload = {customerId: 'C1', items: [{sku: 'S1', qty: 2}], address: 'X'}\n    const r1 = await post('/orders', payload, {Idempotency-Key: key})\n    const r2 = await post('/orders', payload, {Idempotency-Key: key})\n    expect(r1.orderId).toBe(r2.orderId)\n  })\n})\n```\n\n## Follow-up Questions\n- How would you simulate inventory rollback after a failed payment?\n- What metrics would you collect to monitor idempotency correctness in production?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-2",
        "questionId": "q-209",
        "question": "How would you design a REST API testing framework that handles rate limiting, circuit breaking, and distributed tracing for microservices with 10,000+ concurrent requests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan should verify: valid multipart upload yields 202 with a jobId; 413 for images over 5MB; 415 for unsupported formats; 400 for missing image. Idempotency: same Idempotency-Key must not duplica",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a test plan for a multi-tenant chat API (REST + WebSocket) with 15-minute rotating tokens. Use contract tests between the REST gateway and WebSocket hub (Pact-style or OpenAPI). Validate per-t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an asynchronous request batching system with token bucket rate limiting, Hystrix circuit breaker patterns, and OpenTelemetry distributed tracing across test suites.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create a streaming test harness that subscribes to /stream with configurable backpressure, verifies resumption from a cursor after disconnect, injects downstream failures to test retry/backoff, and ch",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nProduction-scale REST API testing requires sophisticated concurrency control and observability. The framework must accurately simulate real-world load conditions while maintaining test reliability and providing comprehensive monitoring capabilities.\n\n## Implementation Details\n- **Rate Limiting**: Token bucket algorithm with distributed Redis counters for coordinated throttling across multiple test instances\n- **Circuit Breaking**: Hystrix-style failure threshold detection with exponential backoff and automatic recovery mechanisms\n- **Distributed Tracing**: OpenTelemetry span propagation across service boundaries for end-to-end request visibility\n- **Request Batching**: Asynchronous HTTP client pools with connection multiplexing to optimize resource utilization\n\n## Code Example\n```javascript\n// Rate-limited test executor\n```",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-3",
        "questionId": "q-542",
        "question": "You're testing a payment API that processes transactions. How would you design test cases to verify idempotency, and what specific HTTP status codes would you expect for duplicate requests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "1) GET /articles/1 to obtain the initial ETag. 2) GET with If-None-Match set to that ETag; expect 304 Not Modified. 3) Update the article, then GET again; expect 200 with a new ETag. 4) Simulate concu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use end-to-end tests with Postman/Newman or REST-assured. Confirm 200 JSON with fields id,title,price; 406 when Accept is application/xml; verify localized titles/descriptions for fr and es under Acce",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test idempotency by sending identical POST requests with the same idempotency key. The first request returns 201 Created with a unique transaction ID. Subsequent requests with the same key return 200 OK with the identic…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Outline a concrete test plan for GET /telemetry?vehicleId=... The endpoint reads real-time data from a Kafka feed and a batch Cassandra store. Validate cross-source consistency, ensure a defined stale",
            "isCorrect": false
          }
        ],
        "explanation": "## Key Testing Areas\n\n- **Idempotency Key Testing**: Same key → same result, different keys → new transactions\n- **Status Code Verification**: 201 for first request, 200 for duplicates, 400 for invalid keys\n- **Database State Validation**: Ensure no duplicate records or charges\n- **Edge Cases**: Expired keys, malformed requests, concurrent requests\n\n## Implementation Strategy\n\n```javascript\n// Test framework example\ndescribe('Payment API Idempotency', () => {\n  const idempotencyKey = uuidv4();\n  \n  it('should create transaction on first request', async () => {\n    const response = await request(app)\n      .post('/api/payments')\n      .set('Idempotency-Key', idempotencyKey)\n      .send(paymentData);\n    \n    expect(response.status).toBe(201);\n    expect(response.body.transactionId).toBeDefined();\n  });\n  \n  it('should return same response for duplicate requests', async () => {\n    const response = await request(app)\n      .post('/api/payments')\n      .set('Idempotency-Key', idempotencyKey)\n      .send(paymentData);\n    \n    expect(response.status).toBe(200);\n    expect(response.body).toEqual(originalResponse.body);\n  });\n});\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-4",
        "questionId": "q-12994",
        "question": "For a production streaming REST API GET /stream/marketdata delivering chunked JSON events, design an end-to-end test plan that validates (a) consumer-driven contract testing with a schema registry across versioned clients, (b) backward compatibility for added/removed fields with defaults, (c) chunk framing and reassembly, (d) backpressure and reconnect logic, and (e) resilience under partial upstream outages and network latency, with tooling and success criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Test Plan: GET /reports/{tenantId} ### 1. Functional Tests #### Input Validation (tenantId formats) | Test ID | Test Description | Input | Expected Result | |---------|------------------|-------|-----------------| |…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a streaming API test plan using consumer-driven contracts via a schema registry. Validate backward compatibility by simulating added/removed fields with defaults, ensure chunk boundaries are pr",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use consumer-driven contracts (Pact) for each client version, run CI triggered on schema updates, and validate responses against JSON Schemas validated with Ajv for v1–v3. Simulate Accept-Version, dep",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate idempotency by issuing POST /payments with the same idempotency key multiple times and asserting that only a single payout is created. Use a distributed ledger to reconcile events from the orchestrator and down…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess ability to test streaming APIs with schema evolution, contracts, and resilience.\n\n## Key Concepts\n- Streaming semantics, chunked transfer\n- Consumer-driven contracts, schema registry\n- Versioned payloads, backward compatibility\n- Backpressure, reconnection, fault injection\n- Observability and failure criteria\n\n## Code Example\n```javascript\n// placeholder snippet for contract test setup\n```\n\n## Follow-up Questions\n- How would you automate drift detection in the registry over time?\n- What failures would trigger rollback or feature-flagged fallbacks?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-5",
        "questionId": "q-27443",
        "question": "Design a test plan for an API that ingests events via POST /events and fans out to Kafka and Redis for real-time analytics. How would you validate idempotent processing, potential out-of-order delivery, and exactly-once semantics under at-least-once broker behavior and partial outages? Include concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test POST /webhooks/{provider}: verify signature with HMAC, ensure Idempotency-Key guarantees at-most-once processing, simulate exponential-backoff retries, check 2xx for valid payloads, 429 on rate l",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test idempotent creation with repeated POST /payments using the same Idempotency-Key to ensure a single record is created and same Location is returned. Validate 400 on invalid payload, 401 on missing",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan end-to-end tests for POST /payments with Idempotency-Key. Validate that repeated POSTs with the same key produce a single payment record and identical responses, while new keys create new records",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Key defenses: idempotent handling via per-event-ID keys in the storage layer; a deduplication path in the Kafka consumer to detect and discard duplicates; tracking the last-seen sequence number per tenant in Redis to de…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests must prove end-to-end guarantees in an event-driven API where duplicates, replays, and out-of-order delivery are common failure modes. In production, brokers may retry messages, caches may fail, and ordering directly impacts analytics accuracy.\n\n## Key Concepts\n\n- **Idempotency and deduplication**: Ensuring the same event processed multiple times produces the same result\n- **Exactly-once vs at-least-once semantics**: Understanding what guarantees the system actually provides and how to enforce stricter guarantees at the application layer\n- **Message ordering and sequence tracking**: Detecting gaps, reordering events, and maintaining tenant-specific state\n- **Cache invalidation and reconciliation**: Keeping Redis state consistent with Kafka's persistent log\n- **Tenant isolation**: Preventing cross-tenant contamination in shared infrastructure\n\n## Code Example\n\n```javascript\n// Pseudo idempotent handler sketch\nasync function handleEvent(event) {\n  const lastSeq = await getLastSeq(tenantId);\n  if (event.seq <= lastSeq) {\n    return; // Duplicate: discard\n  }\n  await storeLastSeq(tenantId, event.seq);\n  await publishToKafka(event);\n}\n```\n\n## Concrete Test Cases\n\n| Scenario | Test Case | Validation |\n|----------|-----------|------------|\n| Duplicate POST | Submit same event ID twice | Verify single Kafka message produced; Redis seq advances once |\n| Out-of-order delivery | Ingest seq 1, 3, 2 | Verify seq 2 is buffered or rejected; no stale analytics written |\n| Replay during outage | Kill Kafka producer mid-batch, retry | Verify no duplicates after recovery; seq integrity maintained |\n| Exactly-once verification | Run consumer twice against same offset | Verify no duplicate writes to analytics store |\n| Partial Redis failure | Redis unavailable during storeLastSeq | Verify fallback to Kafka header or database; no data loss |\n\n## Tooling Recommendations\n\n- **Kafka**: `kafkacat` or `kafka-console-producer` for manual event injection\n- **Redis**: `redis-cli` for inspecting sequence state; Redis Cluster for failure simulation\n- **Chaos testing**: Chaos Mesh or Pumba to inject broker/pod failures\n- **Contract testing**: Pact for producer-consumer contract validation\n- **Load testing**: k6 or Gatling to simulate high-throughput replay scenarios",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-6",
        "questionId": "q-16521",
        "question": "Design a targeted test plan for per-tenant data masking with dynamic policies: API returns /customers/{id} data masked or unmasked based on tenant policies refreshed from a central policy service. Validate policy propagation latency, cache invalidation, and race conditions when a policy updates during a request. Include concrete cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming API test plan using consumer-driven contracts via a schema registry. Validate backward compatibility by simulating added/removed fields with defaults, ensure chunk boundaries are pr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Idempotent bulk import test: POST /imports/users with 100k records and an idempotency-key. Re-send with the same key to verify no duplicates. Inject partial downstream analytics failures to validate p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: test per-user rate limiting on POST /checkout. Step 1: send 5 rapid requests -> 200; Step 2: 6th request -> 429 with Retry-After header; Step 3: advance the clock to reset window and verify 200",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test plan for per-tenant data masking with dynamic policies: verify policy propagation latency, correct masking per tenant, and cache invalidation. Include race conditions when a policy updates during",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to reason about dynamic access control policies, cache coherence, and fault tolerance in a multi-tenant API.\n\n## Key Concepts\n- Policy propagation latency and eventual consistency\n- Cache TTL and invalidation strategies\n- Race conditions between policy updates and requests\n- Fault injection: policy service outages, cache corruption\n- Multi-tenant data masking correctness\n\n## Code Example\n```javascript\n// Pseudo test: verify masking based on tenant policy refresh\nconst res = api.get(`/customers/123?tenant=tenantA`);\nassert(res.data.ssn === '***-**-6789');\n```\n\n## Follow-up Questions\n- How would you test policy rollout in a multi-region deployment?\n- What metrics would you monitor to detect masking misconfigurations?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-7",
        "questionId": "q-20114",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /webhook-events that ingests events from external publishers. Each publisher has a secret used to compute an HMAC-SHA256 signature over the request body, sent in X-Signature. Validate: (1) valid signature returns 202; (2) invalid signature returns 401; (3) Replay protection via a nonce in the body prevents processing same nonce within 24h; (4) idempotent retries with same payload and nonce processed once; (5) error handling for invalid JSON and missing header. Include tooling and a minimal mock setup for per-publisher secrets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an end-to-end tracing test plan for a multi-tenant API gateway using OpenTelemetry: verify trace context propagation across Auth, Gateway, and per-tenant services; test sampling rates, span generation, retries, a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Submit /orders with idempotency_key 'k1' twice; second response must be same order and no duplicates. Simulate broker outage during commit to verify compensating/rollback behavior and eventual retry.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a test plan that validates HMAC-based request signing, nonce replay protection, and idempotent handling for webhook ingestion. Include per-publisher secrets, signature generation, and error ha",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Test input validation: invalid JSON, missing header, empty array -> 400. Idempotency: same X-Idempotency-Key returns same batchId; no duplicate processing on retry. Processing mode: verify all-or-noth",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question adds a practical security angle to API testing by focusing on webhook ingestion, HMAC verification, and replay protection, which are common in real-world integrations.\n\n## Key Concepts\n- HMAC signature verification\n- Replay protection with nonces and TTL\n- Idempotent processing under retries\n- Robust error handling (missing header, invalid JSON)\n\n## Code Example\n```javascript\nconst crypto = require('crypto');\nfunction sign(payload, secret){\n  return crypto.createHmac('sha256', secret).update(payload, 'utf8').digest('hex');\n}\n```\n\n## Follow-up Questions\n- How would you scale nonce storage across services?\n- How would you test multiple publishers with rotating secrets?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-8",
        "questionId": "q-23232",
        "question": "Design a beginner-friendly test plan for an HTTP API that accepts analytics events and exposes per-user history. The API provides POST /events with an Idempotency-Key header for deduplication and GET /events/{userId} for paginated history. Include per-tenant isolation, idempotency and retries, rate-limit edges, and backpressure handling when the queue is full. Provide concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Issue repeated POST /payments requests with the same Idempotency-Key and verify that only one charge is created in the ledger. Ensure the payment gateway is invoked exactly once, and confirm that subsequent calls return…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To test the /events/stream endpoint, implement an automated SSE client that validates: 1) Content-Type header is correctly set to text/event-stream; 2) events are delivered sequentially with no gaps or duplicates within…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Verify that POST /events with the same Idempotency-Key yields a single created event (first call 201, retry returns 200) and duplicates are ignored; test 400/401/403 for invalid keys or payloads; ensu",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Define a strict JSON schema for POST /search; validate invalid types, missing required fields, and oversized payloads return 400 with actionable messages. Simulate rate limiting to yield 429 and Retry",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis angle tests fundamental API testing skills in a realistic scenario: idempotent writes, per-tenant isolation, and resiliency under backpressure, all with paginated reads.\n\n## Key Concepts\n\n- Idempotency with Idempotency-Key\n- Multi-tenant data isolation\n- Pagination semantics (pageSize, pageToken)\n- Backpressure handling and 503 with Retry-After\n- Test automation tooling (Postman/Newman, Supertest)\n\n## Code Example\n\n```javascript\n// Example using supertest-like syntax\nconst request = require('supertest')(BASE_URL);\nconst key = 'idemp-123';\nawait request.post('/events')\n  .set('Idempotency-Key', key)\n  .send({ userId: 'u1', action: 'click' })\n  .expect(201);\nawait request.post('/events')\n  .set('Idempotency-Key', key)\n  .send({ userId: 'u1', action: 'click' })\n  .expect(200);\n```\n\n## Follow-up Questions\n\n- How would you validate that a failed deduplication due to a transient error still preserves idempotency?\n- What metrics would you collect to monitor tail latency under backpressure, and how would you alert on them?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-9",
        "questionId": "q-29803",
        "question": "Design an advanced testing plan for a globally distributed, federated GraphQL API with per-field authorization and a shared cache. Include per-field contract tests for multiple roles, schema-drift checks across regions, latency targets, and cache invalidation during schema changes. Provide concrete tooling, data setup, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Initiate a refresh for tenant A and verify only A’s cache is rewritten; tenant B must remain unchanged. Send multiple identical refresh requests to prove idempotency. Query GET /tenants/A/users to con",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose end-to-end latency tests (k6) targeting 95th percentile under peak load with <2s SLA; validate per-tenant isolation by using tenant_id headers and querying with mixed tenants to ensure no cros",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a testing strategy for a globally distributed, federated GraphQL API with per-field authorization and a shared cache. Include per-field contract tests for multiple roles, schema-drift checks ac",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Test plan should cover: 1) obtain access_token via client_credentials and call GET /portfolio; 2) simulate expired token and expect 401; 3) refresh flow with offline_access and verify new token; 4) re",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAddresses a new angle: federation, field-level access controls, and cross-region caching. Tests must verify no data leakage, consistent schemas, and performance under regional latency variations.\n\n## Key Concepts\n\n- GraphQL federation and schema drift\n- Field-level authorization per role\n- Cross-region latency and cache invalidation\n- Contract testing and end-to-end coverage\n\n## Code Example\n\n```javascript\n// Example contract test skeleton (pseudo)\ndescribe('Field-level auth contracts', () => {\n  it('returns permitted fields for role: user', async () => {\n    // query with user role; assert disallowed fields are absent\n  })\n  it('restricts sensitive fields for role: guest', async () => {\n    // query; assert sensitive fields are missing\n  })\n})\n```\n\n## Follow-up Questions\n\n- How would you simulate cross-region latency spikes and measure per-region drift?\n- What metrics define acceptable schema drift and cache coherence during hot schema changes?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-10",
        "questionId": "q-2369",
        "question": "Design an automated test plan for a webhook listener at /webhooks/ci ingesting push events from a CI service. Each payload is signed with HMAC-SHA256 and delivered at-least-once to a Redis-backed event store. Outline concrete test cases for signature validation, replay/duplicate handling, event ordering or out-of-order resilience, retries/backoff, and failure modes (secret rotation, Redis outage). Include tooling suggestions and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test the POST /orders endpoint using the X-Customer-ID header to enforce per-customer data isolation. Validate all required fields and confirm the API returns 400 Bad Request for invalid or incomplete payloads. Verify i…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test signature validation by sending tampered payloads (expect 401). Enforce idempotency by replaying the same event and ensuring no duplicate writes to Redis. Validate ordering vs. out-of-order event",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test plan: Implement route routing using the X-API-Version header, with v1 as the default when the header is missing. For v1 endpoints, validate response contains {username,email}. For v2 endpoints, validate response co…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use idempotency keys on POST /payments; couple with OpenTelemetry tracing across API and Kafka to prove exactly-once semantics under retry storms. Validate per-tenant isolation via quotas and masked l",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nGauges ability to test security and reliability of inbound webhooks in a distributed system.\n\n## Key Concepts\n- Signature validation\n- Idempotency\n- Ordering vs concurrency\n- Retry/backoff strategies\n- Failure modes (secret rotation, Redis outage)\n\n## Code Example\n```javascript\n// Example test snippet for signature verification\nfunction verifySignature(payload, secret, signature) {\n  const h = crypto.createHmac('sha256', secret)\n  h.update(payload)\n  return `sha256=${h.digest('hex')}` === signature\n}\n```\n\n## Follow-up Questions\n- How would you test secret rotation without missing events?\n- How would you simulate Redis outage in CI?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-11",
        "questionId": "q-25780",
        "question": "Design an end-to-end API test plan for a multi-tenant gateway that fronts a write API /comments and an SSE stream; a central config service controls **per-tenant quotas** and purge policies with asynchronous refresh. Validate **per-tenant quotas**, **quota-change propagation** (<5s), correct **SSE event ordering** under throttling, and graceful degradation when the config service is unavailable. Include tooling and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design and implement an end-to-end test harness for POST /feeds/{id}/publish with outbox-based propagation to multiple regions. Verify idempotency with replayed requests, handle out-of-order events, s",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose an end-to-end plan using contract tests between the gateway and a central config service, simulating tenants with distinct quotas. Validate quota enforcement, quota-change propagation (<5s), S",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test POST /webhooks/{provider}: verify signature with HMAC, ensure Idempotency-Key guarantees at-most-once processing, simulate exponential-backoff retries, check 2xx for valid payloads, 429 on rate l",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose end-to-end tests for a high-throughput payment API that uses idempotency-key headers to achieve exactly-once processing. Include cases for replay with same key, out-of-order deliveries, partia",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes advanced API testing: dynamic per-tenant quotas controlled centrally, asynchronous config propagation, and streaming behavior under throttling. It evaluates contract testing, resilience, and end-to-end observability across gateway, write path, and SSE channel.\n\n## Key Concepts\n\n- Contract tests between gateway and config service\n- Token-bucket rate limiting with per-tenant isolation\n- SSE stream ordering under backpressure\n- Failure modes when config service is unavailable\n\n## Code Example\n\n```javascript\n// Example k6 snippet illustrating quota change and SSE validation\nimport http from 'k6/http';\n// ...\n```\n\n## Follow-up Questions\n\n- How would you model quota bursts and backoff strategies across tenants?\n- How would you verify SLA adherence under mixed latency conditions?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-12",
        "questionId": "q-453",
        "question": "You're testing a REST API that returns paginated results. The endpoint has a rate limit of 100 requests per minute and sometimes returns 500 errors under load. How would you design a comprehensive test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pact to define cross-contract tests between REST /orders and WS /ws/orders with a versioned schema. Drive multi-tenant load with k6, validate per-tenant quotas and backpressure handling, and verif",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use end-to-end tests with Postman/Newman or REST-assured. Confirm 200 JSON with fields id,title,price; 406 when Accept is application/xml; verify localized titles/descriptions for fr and es under Acce",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement comprehensive load testing with gradual ramp-up using tools like k6 or JMeter to simulate realistic traffic patterns. Add retry logic with exponential backoff for 500 errors, implementing circuit breakers to p…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a test plan that validates asynchronous job submission, idempotent behavior, polling semantics, and webhook integrity. Verify POST /jobs with idempotency-key yields single queued job; poll tra",
            "isCorrect": false
          }
        ],
        "explanation": "## Test Strategy Components\n\n- **Load Testing**: Simulate realistic user traffic patterns with gradual ramp-up\n- **Rate Limiting**: Implement request throttling and batching to respect API limits\n- **Error Handling**: Test retry mechanisms with exponential backoff and circuit breakers\n- **Contract Testing**: Validate API responses against schemas and documentation\n- **Monitoring**: Track performance metrics, error rates, and system resource utilization\n\n## Tools & Implementation\n\n```javascript\n// Example retry logic with exponential backoff\nconst retryRequest = async (url, retries = 3) => {\n  for (let i = 0; i < retries; i++) {\n    try {\n      const response = await fetch(url);\n      if (response.ok) return response;\n      \n      // Exponential backoff: 1s, 2s, 4s\n      const delay = Math.pow(2, i) * 1000;\n      await new Promise(resolve => setTimeout(resolve, delay));\n    } catch (error) {\n      if (i === retries - 1) throw error;\n    }\n  }\n};\n\n// Rate limiting implementation\nclass RateLimiter {\n  constructor(maxRequests = 100, timeWindow = 60000) {\n    this.maxRequests = maxRequests;\n    this.timeWindow = timeWindow;\n    this.requests = [];\n  }\n  \n  async makeRequest(requestFn) {\n    const now = Date.now();\n    this.requests = this.requests.filter(time => now - time < this.timeWindow);\n    \n    if (this.requests.length >= this.maxRequests) {\n      const waitTime = this.timeWindow - (now - this.requests[0]);\n      await new Promise(resolve => setTimeout(resolve, waitTime));\n    }\n    \n    this.requests.push(now);\n    return await requestFn();\n  }\n}\n```\n\n## Testing Phases\n\n1. **Baseline Testing**: Establish performance metrics under normal load\n2. **Stress Testing**: Identify breaking points and failure modes\n3. **Spike Testing**: Test sudden traffic increases and recovery\n4. **Endurance Testing**: Validate performance over extended periods\n5. **Rate Limit Testing**: Verify behavior at and beyond rate limits",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-13",
        "questionId": "q-17557",
        "question": "You operate a real-time telemetry API for a logistics platform with per-tenant quotas and a streaming endpoint /telemetry/stream. Design a practical test plan that verifies: stream resilience and dedup in bursts; per-tenant rate limits and quota propagation across nodes; idempotent retries for submitted events; partial downstream outages; end-to-end data correctness. Include concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Test Architecture - Deploy N concurrent SSE clients connecting to /stream/events - Set up a local Kafka cluster with a dedicated test topic - Implement a mock downstream consumer to verify message flow ## Test Cases…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan covers: input validation of tenantId/reportId formats; 403 when path tenantId differs from authenticated user; 404 for missing reports; cache behavior: cold fetch hits MongoDB, subsequent hi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test POST /payments with Idempotency-Key: verify that same key + same payload creates one payment and returns 200 with identical response; same key + different payload returns an error (conflict or ba",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would build an end-to-end test harness for the `/telemetry` and `/telemetry/stream` endpoints that validates real-time, multi-tenant behavior. The test plan covers: 1. **Stream resilience and deduplication in bursts**…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates the ability to translate real-time telemetry requirements into comprehensive test plans that address streaming, multi-tenancy, and resilience under failure conditions.\n\n## Key Concepts\n\n- End-to-end streaming validation (SSE/WebSocket) across multi-node deployments\n- Per-tenant quota propagation and burst handling\n- Idempotent retries and deduplication strategies\n- Downstream outage resilience with backoff and replay mechanisms\n- Observability and synthetic data management\n\n## Follow-Up Questions\n\n- How would you handle quota-exceeded scenarios while maintaining fair resource allocation across tenants?\n- What metrics would you monitor to detect quota bypass or drift in distributed systems?\n- How would you test deduplication window configuration under varying latency conditions?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-14",
        "questionId": "q-28349",
        "question": "Design a beginner-friendly API test plan for POST /orders with an Idempotency-Key header to prevent duplicate orders in a high-concurrency e-commerce flow. Validate: first call creates order; second call with same idempotency key returns 200 with same order id and idempotent behavior; ensure different keys produce new orders; test with concurrent retries; test across tenants (X-Tenant-ID) to ensure isolation; verify key TTL (1 hour) and cleanup; include tooling and CI checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "End-to-end test plan for a real-time WebSocket inference API with canary rollout and per-tenant masking. Validate tenant-specific payloads, ensure canary routes correctly per region, test backpressure handling, verify m…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: test idempotent POST /orders using a Redis-backed idempotency store TTL 3600s. 1) POST /orders with payload and Idempotency-Key A, expect 201 and orderId. 2) Parallel retry with same key; expect",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use an idempotent contract with Idempotency-Key for POST /audit-logs. Send a batch twice; verify only one run creates artifacts across ingestion DB, analytics warehouse, and search index. Check 202 an",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Key defenses: idempotent handling via per-event-ID keys in the storage layer; a deduplication path in the Kafka consumer to detect and discard duplicates; tracking the last-seen sequence number per tenant in Redis to de…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for idempotency are crucial in high-concurrency flows to prevent duplicate orders. This question probes reliability, concurrency handling, tenant isolation, and TTL-based cleanup.\n\n## Key Concepts\n\n- Idempotency keys and deduplication\n- Concurrency and race conditions\n- Multi-tenant isolation and header-based scoping\n- TTL-based cleanup and data retention\n- Error handling paths and resilience\n\n## Code Example\n\n```bash\ncurl -X POST https://api.example.com/orders \\\n  -H 'Content-Type: application/json' \\\n  -H 'Idempotency-Key: A' \\\n  -H 'X-Tenant-ID: t1' \\\n  -d '{\"item\":\"widget\",\"qty\":1}'\n```\n\n```javascript\n// Pseudo test sketch\nconst r1 = post('/orders', payload, { idKey: 'A', tenant: 't1' })\nconst r2 = post('/orders', payload, { idKey: 'A', tenant: 't1' })\nassert(r1.orderId === r2.orderId)\n```\n\n## Follow-up Questions\n\n- How would you detect flaky idempotency tests in CI?\n- How would you extend tests for eventual consistency or cross-region deployments?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-15",
        "questionId": "q-10375",
        "question": "Design an end-to-end API testing plan for a multi-tenant GraphQL gateway that supports persisted queries, per-tenant quotas updated via Admin API, and dynamic schema extensions. Include data isolation checks, query plan validation, caching behavior, retries and idempotency, and simulate partial backend outages with 5xx propagation. Provide concrete test cases, tooling, and success criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline tenant isolation by issuing the same GraphQL query with different auth tokens and asserting tenant-scoped data and headers. Validate persisted queries by hashing a query and confirming the gat",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Execute a per-user rate-limit test: 5 POST /checkout requests in 60s; 6th returns 429 with Retry-After 60s; verify that headers like Retry-After or X-RateLimit-Reset reflect 60s; run parallel bursts f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test idempotency by sending two POST /orders with the same Idempotency-Key and verify only one order is created and the second response reuses the first order_id; verify /orders/{id} status transition",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a comprehensive test strategy that includes tenant-scoped test data generation to validate per-tenant isolation guarantees. Implement contract tests using Pact between the POST /ingest endpoint and downstream pro…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests must cover multi-tenant data isolation, GraphQL features like persisted queries, dynamic schema changes, and fault injection at scale.\n\n## Key Concepts\n- Data isolation across tenants\n- Persisted queries and query plans\n- Admin API-driven quotas and per-tenant throttling\n- Retries, idempotency keys, and failure handling\n- Partial outages and 5xx propagation\n- Observability and dashboards\n\n## Code Example\n```javascript\nimport http from 'k6/http';\nimport { check } from 'k6';\n\nexport default function () {\n  const res = http.post('https://gateway/graphql', JSON.stringify({ query: '{ viewer { id } }' }), {\n    headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer tokenA' }\n  });\n  check(res, { 'status 200': (r) => r.status === 200 });\n}\n```\n\n## Follow-up Questions\n- How would you validate schema drift across dynamic extensions in a CI run?\n- What metrics and thresholds would you monitor to detect quota mispropagation early?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-16",
        "questionId": "q-13175",
        "question": "Design a beginner-friendly test plan for a POST /payments endpoint that processes credit-card charges via a sandboxed 3rd‑party gateway. Validate request schema, handling of valid/invalid cards, idempotencyKey behavior, 3DS-required flows, and webhook retry semantics. Include concrete test cases, data, and tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive test matrix covering regions (US/UK) and locales (en-US/en-GB), with fallback validation for unsupported locales. Verify the presence and correctness of price, currency, and tax fields, ensuring c…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pact to define cross-contract tests between REST /orders and WS /ws/orders with a versioned schema. Drive multi-tenant load with k6, validate per-tenant quotas and backpressure handling, and verif",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a plan that covers request/schema validation, positive/negative card scenarios, idempotency on repeated requests, 3DS fallback, and webhook retry handling. Include data models, expected status",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan a GraphQL API test for a gateway that composes data from products, pricing, and reviews. Enforce maxDepth 5 and a cost-based limit, profile N+1 issues with DataLoader, and validate caching behavi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical API testing skills around payment flows, common for enterprise platforms like Salesforce/IBM. It covers schema validation, idempotency, 3DS, and webhooks—areas often under-tested in beginner tasks.\n\n## Key Concepts\n\n- Request/response schema validation and error mapping\n- Idempotency keys to prevent duplicate charges\n- 3DS flow handling and fallbacks\n- Webhook retry semantics and idempotent handling\n\n## Code Example\n\n```javascript\n// Example test with jest + supertest\ntest('POST /payments success', async () => {\n  const res = await request(app)\n    .post('/payments')\n    .send({ amount: 1000, currency: 'USD', cardNumber: '4242424242424242', expiry: '12/28', cvc: '123', idempotencyKey: 'idemp-1' });\n  expect(res.status).toBe(201);\n  expect(res.body).toHaveProperty('transactionId');\n});\n```\n\n## Follow-up Questions\n\n- How would you instrument tests to validate webhook delivery retries without relying on the real gateway?\n- What changes to test data would help catch race conditions with concurrent charges?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-17",
        "questionId": "q-6682",
        "question": "Design a testing strategy for a real-time alert API used by three tenants, with REST GET /alerts?since= and SSE /alerts/stream. A per-tenant feature flag toggles fields (severity, source, batchId). Outline how you’d validate backward-compatible schema changes across REST and SSE payloads, enforce per-tenant RBAC under high concurrency, implement consumer-driven contracts between REST and stream, and simulate outages (admin service, message broker). Include concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan emphasizes idempotent fetch and deduplication: the endpoint uses `since`, `limit`, and per-user `sequence_id`; duplicates are rejected within a deduplication window. Validate ordering per user, across partitions, v…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose contract-driven tests (PACT) for REST and SSE payloads per tenant, validating ON/OFF flag states and optional fields. Use RBAC checks under bursts, ensure event ordering and dedup under SSE re",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "A pragmatic end-to-end test plan for evolving the GET /orders/{id} endpoint: **1. Contract Testing (PACT & OpenAPI)** - Implement consumer-driven contracts with PACT between OrderService (provider) and consumers (OrderU…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ensure idempotency by replaying the same payload with the same Idempotency-Key within the window; confirm only a single trade is persisted in the log and produced to Kafka. Validate dedup window bound",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests spanning REST and streaming with per-tenant flags and real-time guarantees are a practical, high-stakes problem in production systems. This probes cross-service contracts, schema evolution discipline, RBAC under load, and outage resilience.\n\n## Key Concepts\n- Consumer-driven contracts (PACT/OpenAPI)\n- SSE streaming semantics and ordering\n- Schema evolution with feature flags per tenant\n- Per-tenant RBAC under concurrency\n- Outage simulations (admin service, broker)\n\n## Code Example\n```javascript\n// Pact contract sketch for REST -> SSE payload validation\nconst { Pact } = require('@pact-foundation/pact');\n// define consumer/provider, interactions for ON/OFF flag states\n```\n\n## Follow-up Questions\n- How would you version and deprecate fields in contracts without breaking tenants?\n- How would you detect drift across tenants when flags change at runtime?\n\n## Diagram\nflowchart TD\n  REST[REST Call] --> CONTRACT{Contract Match}\n  CONTRACT --> SSE[Validate SSE payload]\n  SSE --> OUTCOME[Outcome]",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-18",
        "questionId": "q-21241",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /exports that creates a long-running export job per user (tenant). The request must include an Idempotency-Key header. Validate: idempotent behavior on repeated POSTs, immediate 202 response with job_id, status transitions via GET /exports/{job_id} from PENDING to RUNNING to COMPLETED/FAILED, handling background worker downtime, and eventual consistency across repeated polls. Include tooling suggestions and concrete test cases?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an asynchronous request batching system with token bucket rate limiting, Hystrix circuit breaker patterns, and OpenTelemetry distributed tracing across test suites.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test idempotency by sending multiple POST /exports with the same Idempotency-Key and verify only one job is created and subsequent responses return the same job_id. Assert 202 with job_id and a Locati",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement comprehensive end-to-end testing for the multi-tenant event publishing API through a structured approach: utilize idempotency keys to deduplicate POST /events requests, verify webhook delivery with exponential…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "First request for a tenant fetches the policy and caches it for 60s; subsequent requests reuse the cache. Change the tenant policy via admin and verify masking changes after expiry or on manual refres",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExplores a new angle on API testing: idempotent long-running operations and eventual consistency for per-tenant exports, a common real-world pattern.\n\n## Key Concepts\n\n- Idempotency-Key handling across retries\n- Long-running jobs with 202 acceptance and polling\n- Status lifecycle: PENDING -> RUNNING -> FINAL (COMPLETED/FAILED)\n- Worker downtime simulation and resilience\n- Per-tenant data isolation in test data setup\n\n## Code Example\n\n```javascript\n// Pseudo-test: ensure idempotency and status polling\nasync function testExportIdempotent(key){\n  const r1 = await fetch('/exports', {method:'POST', headers:{'Idempotency-Key': key}, body: JSON.stringify({format:'csv'})});\n  const j1 = await r1.json();\n  const r2 = await fetch('/exports', {method:'POST', headers:{'Idempotency-Key': key}, body: JSON.stringify({format:'csv'})});\n  const j2 = await r2.json();\n  console.assert(j1.job_id === j2.job_id, 'Idempotent key should return same job_id');\n  let status = 'PENDING';\n  const job = j1.job_id;\n  while(['PENDING','RUNNING'].includes(status)){\n    const s = await fetch(`/exports/${job}`); const js = await s.json(); status = js.status;\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you simulate worker downtime and verify fallback behavior?\n- What metrics and logs would you collect to confirm eventual consistency across multiple clients?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-19",
        "questionId": "q-28899",
        "question": "Design a pragmatic end-to-end test plan for a push-notification API that routes messages to APNs and FCM, enforcing per-tenant isolation and delivery guarantees. Include test cases for idempotency, token revocation, regional failover, deduplication on retries, and backoff behavior under partial outages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "End-to-end test plan for a push-notification API that routes messages to APNs and FCM, enforcing per-tenant isolation and delivery guarantees. Comprehensive test cases cover idempotency validation via Idempotency-Key he…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Define tests for GET /products/{id} with a real-time feature flag. Verify response includes fields 'discount' and 'promotionTag' when flag is ON and omits them when OFF. Simulate admin API downtime to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: create a layered end-to-end test plan for a multi-tenant time-series API. Use contract tests, load tests with k6, idempotent writes via keys, and streaming dedup using sequence IDs. Validate",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test GET /stream/alerts using Server-Sent Events. Verify 200 and Content-Type: text/event-stream. Capture the first N events and confirm each starts with data: and is valid JSON; ensure id values incr",
            "isCorrect": false
          }
        ],
        "explanation": "### Why This Question Is Asked\n\nThis question evaluates practical understanding of multi-channel message delivery with per-tenant isolation and delivery guarantees. It assesses the ability to design comprehensive test coverage for idempotency, credential revocation, and regional resilience under realistic failure modes.\n\n### Key Concepts\n\n- Multi-channel delivery (APNs, FCM)\n- Idempotency and replay safety\n- Token revocation and credential rotation\n- Regional failover and outage simulation\n- Telemetry, correlation IDs, and SLA verification\n\n### Code Example\n\n```javascript\n// Idempotency check via Idempotency-Key header\nasync function sendPushNotification(notification, idempotencyKey) {\n  const response = await fetch('/api/push/send', {\n    method: 'POST',\n    headers: {\n      'Content-Type': 'application/json',\n      'Idempotency-Key': idempotencyKey,\n      'X-Tenant-ID': notification.tenantId\n    },\n    body: JSON.stringify(notification)\n  });\n  return response.json();\n}\n\n// Test: Verify duplicate requests return cached response\nasync function testIdempotency() {\n  const key = `notif-${Date.now()}-${Math.random()}`;\n  const result1 = await sendPushNotification(notification, key);\n  const result2 = await sendPushNotification(notification, key);\n  assert(result1.messageId === result2.messageId);\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-20",
        "questionId": "q-13607",
        "question": "For a rideshare platform with REST endpoints /rides and /drivers and a partner webhook system, design an end-to-end API test plan to validate at-least-once webhook delivery, idempotent processing, retry/backoff behavior, signature verification (HMAC-SHA256) and replay protection, dead-letter routing, and contract validation with partner schemas. Include concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a bulk upsert test suite focusing on JSONL payloads up to 1000 records, validating per-record status, atomic batch vs partial commits, idempotent retries with the same idempotency-key, and ten",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan: for webhook deliveries in a rides platform, verify at-least-once delivery with idempotent handlers by resending the same payload; confirm retry/backoff behavior and DLQ after 3 failed attem",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test idempotency by resending requests with identical Idempotency-Key headers to verify single order creation; validate batch size limits and malformed order structures; confirm asynchronous processing returns 202 Accep…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a test plan for GET /composite/users/{id} that aggregates from three services in parallel and caches in Redis. Include: 1) stale-read detection by injecting latency on one backend and assert",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests webhook reliability and contract integrity in integration-heavy APIs, a common production risk.\n\n## Key Concepts\n- Webhook reliability (at-least-once)\n- Idempotence and state reconciliation\n- Retry/backoff and DLQ semantics\n- Signature verification and replay protection\n- Consumer contract testing across partners\n\n## Code Example\n\n```javascript\n// Placeholder example: simulate webhook post and idempotent check\n```\n\n## Follow-up Questions\n- How would you monitor and alert on DLQ bloat and failed deliveries?\n- How would you test with multiple partner signing keys rotation?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-21",
        "questionId": "q-30089",
        "question": "NEW ANGLE: Idempotent API testing for retries and deduplication. You have a POST /shipments endpoint that accepts JSON body {orderId, items, address}. The system uses an idempotency-key header X-Idempotency-Key to avoid duplicates if the client retries due to network failures. Design a beginner-friendly test plan to verify idempotency behavior, deduplication window (e.g., 5 minutes), behavior under concurrent identical requests, and a controlled failure in the downstream fulfillment service (simulate via mocks). Include tooling suggestions and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a comprehensive idempotent test plan: verify that identical X-Idempotency-Key values result in a single shipment record, even under retries and concurrent requests. **Test Scenarios:** 1. **Basic Idempotency**…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Test the GET /users/{id} endpoint with three key scenarios: valid ID (expect 200 status with correct user data), invalid ID (expect 404 status), and malformed requests (expect 400 status). Verify response structure, dat…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Validate HMAC-backed auth: (1) valid signature over method, path, timestamp, body returns 200; (2) tampered body or wrong secret returns 401; (3) timestamp outside +/-5 minutes returns 401; (4) missin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose layered testing: unit tests for resolvers with mocked batch loaders to catch N+1; integration tests against staging with tenant tokens validating access control and data masking; contract test",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of idempotent API design—a critical pattern for handling retries in distributed systems where network failures cause duplicate submissions. Shipment workflows are particularly high-risk since duplicates result in costly operational overhead.\n\n## Key Concepts\n\n- **Idempotency Key**: Client-generated unique identifier (UUID recommended) that allows the server to recognize and deduplicate retried requests.\n- **Deduplication Window**: Time-bounded storage of idempotency records (typically 5–24 hours) to balance memory costs against retry scenarios.\n- **Concurrent Request Handling**: Race conditions must be handled atomically—typically via database locking or distributed locks (Redis SETNX).\n- **Downstream Failure Resilience**: If the operation succeeds but the response fails to reach the client, the server must cache the result to return it on retry rather than reprocessing.\n\n## Code Example\n\n```javascript\nconst request = require('supertest')(app);\n\ndescribe('POST /shipments idempotency', () => {\n  test('same key yields same shipmentId', async () => {\n    const key = 'test-key-' + Date.now();\n    const payload = { orderId: 'ORD1', items: [{sku:'X',qty:1}], address: '123 Main St' };\n    \n    const r1 = await request.post('/shipments').set('X-Idempotency-Key', key).send(payload);\n    const r2 = await request.post('/shipments').set('X-Idempotency-Key', key).send(payload);\n    \n    expect(r1.status).toBe(201);\n    expect(r2.status).toBe(201);\n    expect(r1.body.shipmentId).toBe(r2.body.shipmentId);\n  });\n  \n  test('concurrent requests return same shipmentId', async () => {\n    const key = 'concurrent-key-' + Date.now();\n    const payload = { orderId: 'ORD2', items: [{sku:'Y',qty:2}], address: '456 Oak Ave' };\n    \n    const [r1, r2, r3] = await Promise.all([\n      request.post('/shipments').set('X-Idempotency-Key', key).send(payload),\n      request.post('/shipments').set('X-Idempotency-Key', key).send(payload),\n      request.post('/shipments').set('X-Idempotency-Key', key).send(payload),\n    ]);\n    \n    expect(new Set([r1.body.shipmentId, r2.body.shipmentId, r3.body.shipmentId]).size).toBe(1);\n  });\n});\n```\n\n## Common Pitfalls\n\n- Storing only the request hash instead of response body (breaks retry-after-failure scenarios)\n- Not handling key comparison case-sensitively\n- Missing TTL cleanup for idempotency records\n- Assuming idempotency keys are always present (should reject or generate if missing)",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-22",
        "questionId": "q-19538",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /transactions that creates a financial transaction. The endpoint enforces per-key rate-limiting and idempotency via a client-supplied Idempotency-Key header. Provide concrete test cases to verify duplicate prevention with the same key, distinct results with different keys, input validation, and proper 429 handling on rate-limit. Include concurrency tests and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a concrete test plan for GET /telemetry?vehicleId=... The endpoint reads real-time data from a Kafka feed and a batch Cassandra store. Validate cross-source consistency, ensure a defined stale",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a beginner-friendly test plan for POST /webhook that validates an HMAC-SHA256 signature in X-Signature over the JSON payload with a shared secret. Include: happy-path with valid signature; mis",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test plan for GET /weather?city={city} aggregating two providers with 60s cache and 5s per-provider timeouts. Verify: (a) both up returns average temperature; (b) one down still returns from the other",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "First POST /transactions with Idempotency-Key: A creates a txn id; repeat POST with key A returns same id (no duplicate). POST with new key B creates another txn. Missing or invalid Idempotency-Key re",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests idempotency and per-key rate-limiting in a concrete, beginner-friendly scenario. It checks understanding of dedup logic, error handling, and basic concurrency considerations without needing advanced infra.\n\n## Key Concepts\n\n- Idempotency-Key handling\n- Per-key rate limiting and Retry-After\n- Concurrency and race conditions\n- Input validation and schema checks\n- Observability via DB/state validation\n\n## Code Example\n\n```javascript\n// Example test using node-fetch and Mocha\nconst fetch = require('node-fetch');\ndescribe('idempotency tests', function() {\n  it('duplicates are prevented with same Idempotency-Key', async function() {\n    const url = 'https://api.example.com/transactions';\n    const payload = {amount: 10, currency: 'USD'};\n    const headers = {'Idempotency-Key': 'A'};\n    const r1 = await fetch(url, {method: 'POST', headers, body: JSON.stringify(payload)});\n    const r2 = await fetch(url, {method: 'POST', headers, body: JSON.stringify(payload)});\n    const d1 = await r1.json();\n    const d2 = await r2.json();\n    if (r1.status !== 200 || r2.status !== 200 || d1.id !== d2.id) throw new Error('idempotency failed');\n  });\n});\n``` \n\n## Follow-up Questions\n\n- How would you adapt tests for multi-region deployments with separate idempotency stores?\n- What changes would you make to test suite to cover retries with timeouts and jitter?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-23",
        "questionId": "q-7281",
        "question": "Design an end-to-end API testing plan for POST /orders that triggers a multi-service saga across inventory, payment, and shipping with asynchronous processing. The endpoint returns a sagaId immediately while processing continues via events and webhook callbacks. Outline concrete test cases for idempotency, exactly-once delivery where possible, webhook retries, compensating actions, and eventual consistency under partial outages. Include tooling and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: verify response fields are shaped by per-user feature flag and trait, using two synthetic users (premium_offers ON/OFF, region A/B). Expect 200 with premium fields when ON, 200 without premium f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "First POST /transactions with Idempotency-Key: A creates a txn id; repeat POST with key A returns same id (no duplicate). POST with new key B creates another txn. Missing or invalid Idempotency-Key re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Explain a practical end-to-end test plan for POST /orders that triggers a multi-service saga across inventory, payment, and shipping with asynchronous processing. Show how you'd verify idempotency, ex",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "## GraphQL Federation Gateway API Testing Plan ### 1. Testing Scope Overview **System Under Test:** - GraphQL Federation Gateway (single API endpoint) - Multiple per-tenant subgraphs with schema variants - Persisted que…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world distributed workflows require testing asynchronous sagas end-to-end, not just unit tests.\n\n## Key Concepts\n- End-to-end testing of asynchronous workflows\n- Idempotency and exactly-once delivery\n- Webhook retry logic and compensating actions\n- Eventual consistency under partial outages\n\n## Code Example\n```javascript\n// Pseudo-test: ensure same idempotency key yields single order\nconst res1 = await post('/orders', { items:[{id:1,qty:2}], idempotencyKey:'abc-123' });\nconst res2 = await post('/orders', { items:[{id:1,qty:2}], idempotencyKey:'abc-123' });\nassert(res1.data.orderId === res2.data.orderId);\n```\n\n## Follow-up Questions\n- How would you detect duplicate events in the event store?\n- How would you simulate webhook failures and recoveries?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-24",
        "questionId": "q-27033",
        "question": "Design a beginner-friendly test plan for a REST API endpoint POST /bookings that creates a ride booking in a multi-tenant system. Each request must include a header X-API-KEY identifying the tenant and an idempotency-key. Validate input, per-tenant isolation, idempotency semantics (same key does not create duplicates), 429 Retry-After handling, and successful path. Include concrete test cases, tooling, and CI acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan covers: input validation of tenantId/reportId formats; 403 when path tenantId differs from authenticated user; 404 for missing reports; cache behavior: cold fetch hits MongoDB, subsequent hi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test signature validation by sending tampered payloads (expect 401). Enforce idempotency by replaying the same event and ensuring no duplicate writes to Redis. Validate ordering vs. out-of-order event",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a comprehensive multi-tenant streaming test framework with HTTP/2 SSE as the primary protocol and WebSocket as the fallback mechanism. Validate stream initialization by confirming 200 OK responses with appropriate C…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Test Cases** - **400 Bad Request**: Missing or invalid required fields (e.g., missing pickup_location or invalid passenger_count) - **401 Unauthorized / 403 Forbidden**: Missing or invalid X-API-KEY header; tenant not…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates a candidate's understanding of idempotent POST semantics, per-tenant data isolation, and resilience testing—critical concepts in bookings and payments services.\n\n## Key Concepts\n\n- **Idempotency-key handling** prevents duplicate bookings when clients retry requests due to network failures or timeouts\n- **Per-tenant data isolation** is enforced via X-API-KEY validation, ensuring tenants cannot access each other's data\n- **API test coverage** includes input validation, authentication, rate limiting, and concurrent retry scenarios\n\n## Code Example\n\n```javascript\ndescribe('POST /bookings', () => {\n  it('creates booking with valid idempotency key', async () => {\n    const response1 = await api.post('/bookings', {\n      pickup_location: 'NYC',\n      passenger_count: 2\n    }, { headers: { 'X-API-KEY': 'tenant-a', 'idempotency-key': 'abc123' } });\n    \n    const response2 = await api.post('/bookings', {\n      pickup_location: 'NYC',\n      passenger_count: 2\n    }, { headers: { 'X-API-KEY': 'tenant-a', 'idempotency-key': 'abc123' } });\n\n    expect(response2.status).toBe(200);\n    expect(response2.body.booking_id).toBe(response1.body.booking_id);\n  });\n});\n```",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-20T02:56:28.563Z",
    "version": 1
  }
]