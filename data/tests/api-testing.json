[
  {
    "id": "test-api-testing",
    "channelId": "api-testing",
    "channelName": "API Testing",
    "title": "API Testing Knowledge Test",
    "description": "Test your understanding of API Testing with 25 questions.",
    "questions": [
      {
        "id": "api-testing-q-0",
        "questionId": "q-14650",
        "question": "Design an end-to-end test strategy for an asynchronous bulk import API: POST /bulk/import uploads a CSV and returns a jobId; GET /bulk/import/{jobId} reports status, processed rows, and per-row errors. How would you validate idempotent submissions, per-tenant isolation, and resilience under queue outages and worker restarts with retries; and what concrete test cases and tooling would you use?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## End-to-End Test Plan for Asynchronous Job-Processing API ### API Endpoints - **POST /jobs** — Submit job with idempotency key - **GET /jobs/{jobId}/status** — Poll job status - **GET /jobs/{jobId}/result** — Retrieve…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Model 3 tenants in parallel, generate unique messageIds to verify deduplication and at-least-once delivery behavior at scale. Instrument per-tenant queues to confirm zero cross-tenant data leakage. Test client reconnect…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design and implement a comprehensive end-to-end test strategy for an asynchronous bulk import API where POST /bulk/import accepts CSV uploads and returns a jobId, while GET /bulk/import/{jobId} provides real-time status…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Leverage an end-to-end test that simulates stock deltas via Kafka and validates /inventory/{sku} under eventual consistency. Use Testcontainers with Kafka and Redis, publish a delta, verify stock upda",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Matters\n\nTesting asynchronous, event-driven APIs requires deep understanding of eventual consistency, idempotency patterns, and multi-tenant data isolation. This question evaluates your ability to design comprehensive test coverage for complex failure scenarios including queue infrastructure downtime, worker process crashes, retry logic implementation, and maintaining data integrity across retry attempts.\n\n## Core Technical Concepts\n\n- Asynchronous processing patterns with polling-based status updates\n- Idempotent request handling and duplicate submission prevention\n- Multi-tenant data isolation and security boundaries\n- Resilience testing for queue failures and worker restart scenarios\n- Retry semantics with exponential backoff and circuit breaker patterns\n- Comprehensive observability through metrics, structured logging, and audit trails\n\n## Implementation Example\n\n```python\n# Example pytest framework skeleton for end-to-end testing\nimport pytest\nimport asyncio\nfrom aiohttp import ClientSession\nimport time\n\nclass TestBulkImportAPI:\n    async def test_idempotent_submissions(self):\n        # Verify duplicate submissions with same data\n        # produce identical jobIds or reference same processing\n        pass\n    \n    async def test_per_tenant_isolation(self):\n        # Ensure data segregation between tenants\n        # Validate cross-tenant data leakage prevention\n        pass\n    \n    async def test_queue_failure_resilience(self):\n        # Simulate queue downtime and worker crashes\n        # Verify graceful degradation and recovery\n        pass\n```\n\n## Key Test Scenarios\n\n1. **Idempotency Testing**: Duplicate CSV submissions, concurrent identical requests, and deduplication mechanisms\n2. **Isolation Validation**: Cross-tenant data boundaries, namespace separation, and security rule enforcement\n3. **Failure Resilience**: Queue outages, worker crashes, network partitions, and retry limit behaviors\n4. **Data Integrity**: Exactly-once processing, transaction consistency, and error recovery validation\n\n## Recommended Tooling\n\n- **Test Framework**: pytest with async support for concurrent testing\n- **API Testing**: aiohttp or httpx for async HTTP client operations\n- **Mock Services**: LocalStack for AWS queue simulation, Docker containers for service isolation\n- **Observability**: Prometheus metrics, ELK stack for log aggregation, Jaeger for distributed tracing\n- **Load Testing**: k6 or Locust for concurrency and stress testing scenarios\n\nThis comprehensive approach ensures robust validation of your asynchronous bulk import system under various operational conditions.",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-1",
        "questionId": "q-7966",
        "question": "Design a beginner-friendly end-to-end API test plan for a blog platform's post endpoints (GET /posts/{id}, POST /posts, PUT /posts/{id}, PATCH /posts/{id}, DELETE /posts/{id}) that uses ETag-based optimistic concurrency. Include: 1) how to validate that ETag changes after updates, 2) how to reproduce 412 on stale versions during concurrent edits, 3) how 304 Not Modified interacts with cached responses, 4) basic schema validation and error paths (missing fields, invalid JSON), 5) tooling suggestions and concrete test cases?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage an idempotency key on POST /orders to guarantee exactly-once semantics. Test retries by replaying the same key; assert a single order is created and no duplicates. Simulate Kafka broker outag",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use consumer-driven contracts (Pact) to lock API gateway–downstream schemas for both v1 and v2 payloads. Validate idempotency via idempotency-key on POST /ingest; test tenant isolation by using distin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan a concrete, beginner-friendly test flow: 1) GET /posts/{id} and capture ETag; 2) PUT /posts/{id} with If-Match: ETag to update title/content and verify 200 with new ETag; 3) simulate two clients with concurrent acc…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a test harness with Locust for load and a lightweight SSE client to /reports/stream?tenant=. Run two tenants in parallel; assert tenant-scoped events and correct order; verify replay semantics on",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of HTTP caching and optimistic concurrency using ETag, plus practical API validation basics.\n\n## Key Concepts\n- ETag, If-Match, If-None-Match headers for optimistic concurrency\n- HTTP 412 Precondition Failed for stale resource versions\n- HTTP 304 Not Modified for cache validation\n- Schema validation and error handling patterns\n- Test data management and appropriate tooling selection\n\n## Testing Strategy\nThe test plan validates the complete ETag lifecycle while covering edge cases and error conditions. Each test case isolates specific functionality while maintaining clear dependencies between steps. The workflow demonstrates proper concurrency handling, cache validation, and input validation scenarios that are essential for robust API testing.",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-2",
        "questionId": "q-14820",
        "question": "Design a pragmatic test plan for an HTTP POST /payments/process in a multi-tenant platform used by high-traffic services. The endpoint must be idempotent, trigger asynchronous payout jobs, and emit per-tenant webhook events. Describe how you'd simulate peak concurrency, enforce idempotency keys, validate webhook retries with exponential backoff, and verify audit logs across services for eventual consistency. Provide concrete test cases and tooling recommendations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Tests should cover idempotent behavior with a shared idempotency-key, verifying first call returns 201 and a job_id while duplicates return the same job_id. Validate retry semantics on transient netwo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a dedupe store (Redis) keyed by Idempotency-Key; run parallel requests with the same key and assert a single payout created. Validate distinct keys create separate payouts. Simulate webhook delive",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test GET /stream/alerts using Server-Sent Events. Verify 200 and Content-Type: text/event-stream. Capture the first N events and confirm each starts with data: and is valid JSON; ensure id values incr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a test plan that uses an Idempotency-Key on POST /checkout; simulate downstream faults via mocks for auth, inventory, and payment with a fault-injection tool (e.g., WireMock) to trigger 2s time",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for idempotent, asynchronous payouts with webhook delivery under high concurrency are common in ride-hailing and edge networks. This checks correctness, fault-tolerance, and observability in distributed multi-tenant scenarios.\n\n## Key Concepts\n- Idempotency and idempotency-key\n- Async processing and eventual consistency\n- Webhook reliability with backoff and jitter\n- Audit logs and traceability across services\n- Tenant isolation and data integrity\n\n## Code Example\n\n```javascript\n// Pseudocode test sketch\nconst r1 = httpPost('/payments/process', {amount:100, currency:'USD'}, {headers: {'Idempotency-Key':'k-abc', 'Tenant':'t1'}});\nconst r2 = httpPost('/payments/process', {amount:100, currency:'USD'}, {headers: {'Idempotency-Key':'k-abc', 'Tenant':'t1'}});\nexpect(r1.body.payoutId).toBeDefined();\nexpect(r2.body.payoutId).toBe(r1.body.payoutId); // deduped\n```\n\n## Follow-up Questions\n- How would you verify audit log propagation across async boundaries during a webhook retry storm?\n- What production signals would you monitor to detect idempotency leaks?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-3",
        "questionId": "q-22691",
        "question": "Design a beginner-friendly test plan for a content-negotiated API endpoint GET /articles that serves two representations based on the Accept header: application/json and application/vnd.company+json. Validate that each representation includes the correct fields, Content-Type matches, and 406 for unsupported Accept; include schema validation, regression checks after schema changes, and tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ensure exactly-once semantics for POST /payments using an idempotency-key store; simulate concurrent duplicate requests and verify only a single ledger entry is created. Mock bank responses with varyi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test GET /articles with Accept: application/json and Accept: application/vnd.company+json; validate each payload against its schema, verify Content-Type matches, and expect 406 for unsupported Accept",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To test GET /profiles/{id} with conditional requests: fetch a fresh resource to capture ETag and Last-Modified. Re-request with If-None-Match and If-Modified-Since to expect 304. Update the resource a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a comprehensive idempotent test plan: verify that identical X-Idempotency-Key values result in a single shipment record, even under retries and concurrent requests. **Test Scenarios:** 1. **Basic Idempotency**…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests content negotiation basics, ensuring two representations are served correctly and that improper Accept headers are handled gracefully. It also touches schema validation, regression checks, and lightweight tooling suitable for beginners.\n\n## Key Concepts\n\n- Content negotiation and media types\n- JSON Schema validation for each representation\n- 406 Not Acceptable for unsupported Accept\n- Regression testing for schema drift across representations\n- Simple automation with curl, jq, or Postman/Newman\n\n## Code Example\n\n```bash\ncurl -s -H \"Accept: application/json\" https://api.example.com/articles | jq .\ncurl -s -H \"Accept: application/vnd.company+json\" https://api.example.com/articles | jq .\n```\n```\n\n## Follow-up Questions\n\n- How would you add a default representation when Accept is missing?\n- How would you integrate these tests with an OpenAPI contract and CI parallelization?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-4",
        "questionId": "q-21756",
        "question": "Design an end-to-end test plan for a multi-tenant WebSocket live-feed API. Validate at-least-once delivery using messageId dedupe, per-tenant isolation (no cross-talk), reconnect handling (auto-reconnect with backoff), and backpressure (pause/resume). Simulate regional broker outages and verify resubscribe and replay of missed events. Use k6/ws, a per-tenant seed in Redis, and define SLA: p95 latency < 400ms?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a comprehensive multi-tenant streaming test framework with HTTP/2 SSE as the primary protocol and WebSocket as the fallback mechanism. Validate stream initialization by confirming 200 OK responses with appropriate C…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Model 3 tenants in parallel, generate unique messageIds to verify deduplication and at-least-once delivery behavior at scale. Instrument per-tenant queues to confirm zero cross-tenant data leakage. Test client reconnect…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test plan: POST /payments with Idempotency-Key to prevent duplicates. Use a mocked external gateway to capture charges. Steps: same key twice -> one charge; different keys -> separate charges; high-co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan to test /stream SSE: simulate 20–100 concurrent clients, emit events with strictly increasing ids, and verify Last-Event-ID reconnect delivers in-order, without duplicates, even after gaps. Inclu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the candidate's ability to design complex end-to-end tests for a streaming API with multi-tenancy, delivery guarantees, and resilience under failure conditions.\n\n## Key Concepts\n- WebSocket streaming with per-tenant isolation and backpressure control\n- Message deduplication via unique messageIds for at-least-once delivery\n- Reconnection logic with exponential backoff and replay semantics\n- Fault injection for broker failover and regional outages\n- Latency and throughput SLA validation\n\n## Code Example\n```javascript\nconst WebSocket = require('ws');\nconst { Kafka } = require('kafkajs');\nconst Redis = require('ioredis');\n\nconst TENANTS = ['tenant-a', 'tenant-b', 'tenant-c'];\nconst SLA_P95_LATENCY_MS = 400;\nconst MAX_RECONNECT_ATTEMPTS = 10;\nconst BASE_BACKOFF_MS = 100;\n\nclass MultiTenantTest {\n  constructor() {\n    this.redis = new Redis();\n    this.connections = new Map();\n    this.messageIds = new Map();\n    this.latencies = [];\n  }\n\n  async setup() {\n    await this.redis.flushdb();\n    for (const tenant of TENANTS) {\n      await this.redis.set(`seed:${tenant}`, Math.random().toString(36));\n    }\n  }\n\n  async connectTenant(tenant) {\n    const ws = new WebSocket('wss://api.example.com/feed');\n    const clientId = `${tenant}-${Date.now()}`;\n    \n    ws.on('open', () => {\n      ws.send(JSON.stringify({ type: 'subscribe', tenant, clientId }));\n    });\n\n    ws.on('message', (data) => {\n      const msg = JSON.parse(data);\n      if (msg.tenant !== tenant) {\n        throw new Error(`CROSS-TENANT LEAK: ${tenant} received ${msg.tenant} data`);\n      }\n      const latency = Date.now() - msg.timestamp;\n      this.latencies.push(latency);\n      this.trackMessageId(msg.messageId, tenant);\n    });\n\n    this.connections.set(tenant, ws);\n    return ws;\n  }\n\n  trackMessageId(messageId, tenant) {\n    if (this.messageIds.has(messageId)) {\n      const existing = this.messageIds.get(messageId);\n      console.log(`DUPLICATE: ${messageId} from ${existing} to ${tenant}`);\n    }\n    this.messageIds.set(messageId, tenant);\n  }\n\n  async simulateBackpressure(tenant) {\n    const ws = this.connections.get(tenant);\n    ws.send(JSON.stringify({ type: 'pause' }));\n  }\n\n  async resumeFeed(tenant) {\n    const ws = this.connections.get(tenant);\n    ws.send(JSON.stringify({ type: 'resume' }));\n  }\n\n  async simulateBrokerOutage(region) {\n    console.log(`Simulating broker outage in ${region}`);\n  }\n\n  calculatePercentile(arr, p) {\n    const sorted = arr.slice().sort((a, b) => a - b);\n    const idx = Math.ceil((p / 100) * sorted.length) - 1;\n    return sorted[Math.max(0, idx)];\n  }\n\n  async validateSLA() {\n    const p95 = this.calculatePercentile(this.latencies, 95);\n    console.log(`P95 Latency: ${p95}ms`);\n    if (p95 > SLA_P95_LATENCY_MS) {\n      throw new Error(`SLA VIOLATION: p95 ${p95}ms exceeds ${SLA_P95_LATENCY_MS}ms`);\n    }\n  }\n}\n```\n\nThis test framework validates all key requirements: per-tenant isolation, at-least-once delivery through messageId deduplication, backpressure handling, and latency SLAs under simulated failure conditions.",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-5",
        "questionId": "q-17777",
        "question": "Design an end-to-end test plan for a WebSocket-based per-user notification service used by an e-commerce platform that publishes events like order_updates and stock_alerts with per-tenant rate quotas and a downstream pipeline. Include guarantees for per-user message ordering and deduplication, backpressure handling for slow clients, robust reconnect and session resumption, burst testing under per-tenant quotas, and resilience to partial upstream outages. Provide concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a plan that validates end-to-end correctness when GET /accounts/{accountId}/transactions merges data from core-ledger, fx-rates, and merchant-meta. Include data freshness windows, cross-service",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a test harness that runs per-tenant WebSocket clients, sends events with monotonically increasing seqId, and asserts in-order delivery and deduplication per user. Simulate slow consumers to tr",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: simulate a 5-node deployment with Redis-backed per-minute quotas and an admin API. Use k6 to generate bursts on GET /quotes/{symbol} and WebSocket /ws/quotes from multiple tenants; verify 429s w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an end-to-end test for POST /payments/charge with Idempotency-Key. Validate duplicate submissions reuse same chargeId, no double debiting, and idempotent retry behavior. Simulate bank outage",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for a WebSocket-based notification service require understanding ordering, deduplication, and resilience under real-time load. This angle explores concurrency, per-tenant quotas, and failure modes not deeply exercised by REST checks.\n\n## Key Concepts\n\n- WebSocket delivery guarantees: in-order messages, at-least-once delivery\n- Per-tenant quotas and burst handling\n- Backpressure, buffering, and flow control\n- Reconnect logic and session resumption\n- Fault injection and partial upstream outages\n\n## Code Example\n\n```javascript\n// Pseudo-test skeleton using ws library\nconst WebSocket = require('ws');\nconst ws = new WebSocket('wss://example.com/notifications');\nws.on('open', () => ws.send(JSON.stringify({type:'subscribe', tenant:'t1'})));\nws.on('message', (data) => {\n  const msg = JSON.parse(data);\n  // verify seqId monotonic per tenant and dedupe\n});\n```\n\n## Follow-up Questions\n\n- How would you scale to 10k tenants with isolated streams?\n- How would you validate behavior if the WebSocket layer fails and you must fall back to long-polling?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-6",
        "questionId": "q-9866",
        "question": "Design a pragmatic test plan for a new asynchronous export API: POST /exports with a payload containing user_id, export_options, and an idempotent export_id which ensures deduplication. The API queues a batch export to an external analytics partner via a message broker. Status is retrievable via GET /exports/{export_id}/status. Outline concrete test cases for payload validation, idempotency, high-concurrency submissions, partial failures (broker or partner down), retries with exponential backoff, and observability checks. Include tooling, environment, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify 202 and trackingId on first call; simulate duplicates with the same payload and Idempotency-Key to ensure no duplicates in downstream stores; validate per-partition ordering by including a part",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test Plan for POST /payments Endpoint with Idempotency-Key **1. New Request (Happy Path)** Submit first request with valid Idempotency-Key → Expect 200/201 with charge created successfully **2. Duplicate Request (Idempo…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an idempotent export system using export_id to deduplicate POST /exports requests; simulate 1000 concurrent submissions to ensure exactly one export is queued while others return appropriate responses. Validat…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Test idempotency by resubmitting identical payloads and asserting the same jobId and final status is returned. Verify state transitions flow correctly from PENDING → PROCESSING → COMPLETED (or FAILED) with appropriate t…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a real-world asynchronous export flow requiring idempotency, retry logic, and observability. It validates high-concurrency behavior and failure modes across the message broker and downstream analytics partner.\n\n## Key Concepts\n\n- Idempotent POST endpoints with deduplication keys\n- At-least-once vs exactly-once processing semantics\n- End-to-end observability and monitoring\n- Exponential backoff retry strategies\n\n## Code Example\n\n```javascript\n// Example test skeleton for idempotent export\nasync function testIdempotentExport(exportId) {\n  const res1 = await post('/exports', {\n    user_id: 'user123',\n    export_options: { format: 'csv', date_range: '30d' },\n    export_id: exportId\n  });\n  \n  const res2 = await post('/exports', {\n    user_id: 'user123',\n    export_options: { format: 'csv', date_range: '30d' },\n    export_id: exportId\n  });\n  \n  // First request should queue the export\n  assert(res1.status === 202);\n  // Second request should return existing export\n  assert(res2.status === 200 && res2.data.export_id === exportId);\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-7",
        "questionId": "q-19309",
        "question": "Design a practical API testing plan for an asynchronous file-processing API where POST /process returns a jobId and updates arrive via SSE at /jobs/stream or GET /jobs/{id} for status—what concrete test cases, tooling, and acceptance criteria would you use to validate idempotency, state transitions, streaming vs polling semantics, failure modes (worker crash, broker outage), and per-tenant audit trails?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test idempotency by resubmitting identical payloads and asserting the same jobId and final status is returned. Verify state transitions flow correctly from PENDING → PROCESSING → COMPLETED (or FAILED) with appropriate t…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a practical plan to verify backward compatibility between v1 and v2 endpoints. Include concrete test cases for legacy clients calling v2, schema validation of both versions, OpenAPI contract",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define a strict JSON schema for POST /search; validate invalid types, missing required fields, and oversized payloads return 400 with actionable messages. Simulate rate limiting to yield 429 and Retry",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose contract-driven tests (PACT) for REST and SSE payloads per tenant, validating ON/OFF flag states and optional fields. Use RBAC checks under bursts, ensure event ordering and dedup under SSE re",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical understanding of asynchronous API design, focusing on idempotency guarantees, dual delivery mechanisms (SSE versus polling), and failure modes that require replay semantics and strong auditability.\n\n## Key Concepts\n\n- **Asynchronous processing and event delivery**: Understanding how to handle long-running operations and deliver status updates through multiple channels\n- **Idempotent API behavior**: Ensuring repeated requests produce the same result without side effects\n- **Streaming vs polling semantics**: Comparing Server-Sent Events (real-time push) versus polling (client-initiated retrieval) for consistency and completeness\n- **Failure modes and replay guarantees**: Handling worker crashes, broker outages, and network partitions gracefully\n- **Per-tenant isolation and audit trails**: Maintaining clear separation between tenants and comprehensive logging for compliance\n\n## Concrete Test Cases\n\n1. **Idempotency**: Submit identical payloads multiple times; verify the same jobId is returned and no duplicate processing occurs\n2. **State Transitions**: Assert PENDING → PROCESSING → COMPLETED/FAILED transitions occur within expected timeframes\n3. **SSE vs Polling**: Compare event sequence from /jobs/stream with GET /jobs/{id} responses for parity\n4. **Failure Modes**: Simulate worker crash mid-processing and broker outage; verify graceful degradation and replay capability\n5. **Audit Trails**: Validate per-tenant job history, ensuring complete traceability without cross-tenant data leakage\n\n## Recommended Tooling\n\n- **HTTP clients**: curl, Postman, or Rest Assured for API validation\n- **SSE clients**: Server-sent event consumers to verify stream behavior\n- **Chaos engineering**: Tools like Chaos Monkey or custom scripts to simulate failures\n- **Logging/Audit**: ELK Stack or similar for verifying audit trail completeness\n\n## Acceptance Criteria\n\n- Identical payloads return the same jobId within 100ms\n- State transitions complete within defined SLA timeframes\n- SSE and polling deliver identical final state\n- System recovers gracefully from worker/broker failures within retry limits\n- All job events are traceable per tenant with complete audit logs",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-8",
        "questionId": "q-27123",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /api/v1/search that starts a long-running search job. The request includes an optional X-Idempotency-Key header to prevent duplicates. The endpoint responds with 202 Accepted and a Location header pointing to /api/v1/search/{jobId}/status. Include tests for idempotent retries, concurrent identical requests with different keys, 429 rate limits, and correctness of eventual results via GET /api/v1/search/{jobId}/results. Use concrete tooling and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan focuses on idempotent long-running search jobs. Send POST /api/v1/search with payload and X-Idempotency-Key; expect 202 and a Location header. Re-send with same key should return same jobId;",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Test Plan: GET /reports/{tenantId} ### 1. Functional Tests #### Input Validation (tenantId formats) | Test ID | Test Description | Input | Expected Result | |---------|------------------|-------|-----------------| |…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Validate HMAC-backed auth: (1) valid signature over method, path, timestamp, body returns 200; (2) tampered body or wrong secret returns 401; (3) timestamp outside +/-5 minutes returns 401; (4) missin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Develop an end-to-end test plan for an async /data/process flow: verify idempotent POSTs with Idempotency-Key, check that duplicates yield a single operation, poll results for eventual consistency and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for idempotency and long-running behavior are common in search APIs and are easy to misconfigure.\n\n## Key Concepts\n\n- Idempotency keys prevent duplicates\n- 202 with Location header enables polling\n- Polling strategy and eventual consistency\n- Rate limiting and Retry-After handling\n- Concurrency: distinct keys yield separate jobs\n\n## Code Example\n\n```python\nimport httpx\n\ndef test_idempotent_search():\n    client = httpx.Client()\n    payload = {\"query\": \"cpu\"}\n    headers = {\"X-Idempotency-Key\": \"req-1\"}\n    r1 = client.post(\"https://api.example.com/api/v1/search\", json=payload, headers=headers)\n    assert r1.status_code == 202\n    location = r1.headers[\"Location\"]\n    r2 = client.post(\"https://api.example.com/api/v1/search\", json=payload, headers=headers)\n    assert r2.status_code == 202\n    assert r2.headers.get(\"Location\") == location\n```\n\n## Follow-up Questions\n\n- How would you test idempotency in a distributed env?\n- How to simulate high contention and verify race conditions?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-9",
        "questionId": "q-23111",
        "question": "Design a beginner-friendly test plan for POST /orders that creates a per-tenant order. Include: input validation, per-tenant isolation via X-ORG-ID, idempotency with Idempotency-Key, interaction with a pricing service (simulate slow or down), and eventual consistency with the inventory service under concurrent requests. Provide concrete test cases, tooling, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test approach: POST /orders with Idempotency-Key and X-ORG-ID per tenant; verify 201 created with order_id and idempotent retries return the same id without duplication. Simulate pricing service slow/",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Tests should cover: (1) payload validation for title and dueDate; (2) auth and tenant scoping via X-API-KEY; (3) data isolation by creating two tenants and verifying no cross-tenant visibility; (4) ra",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end tracing test plan for a multi-tenant API gateway using OpenTelemetry: verify trace context propagation across Auth, Gateway, and per-tenant services; test sampling rates, span generation, retries, a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan end-to-end tests for a partner webhook endpoint /webhooks/payments using HMAC-SHA256 with rotating keys. Verify idempotency with eventId, dedupe concurrent posts, simulate key rotation with zero",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests idempotency handling, inter-service resilience, and strict data isolation across tenants, all essential in multi-tenant API testing.\n\n## Key Concepts\n- Idempotency and Retry semantics\n- Inter-service dependencies (pricing, inventory)\n- Per-tenant isolation and header validation\n- Concurrency effects on data integrity\n\n## Code Example\n```javascript\n// Minimal test scaffold for idempotent order creation\nasync function testOrder(orgId, idKey, payload){\n  const res = await fetch('/orders', {method:'POST', headers:{'X-ORG-ID':orgId,'Idempotency-Key':idKey}, body: JSON.stringify(payload)});\n  return res.status;\n}\n```\n\n## Follow-up Questions\n- How would you test idempotency key collision handling?\n- How would you mirror pricing-service outages in CI/CD?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-10",
        "questionId": "q-24264",
        "question": "Design a beginner-friendly test plan for GET /reports?month=YYYY-MM that uses content negotiation (Accept: application/json or application/x-yaml) and permission checks. Provide concrete test cases for (a) content-type correctness and data shape in both JSON and YAML, (b) 403 when lacking permissions, (c) 304 Not Modified via ETag/If-None-Match, (d) 400 on invalid month, and (e) basic rate-limiting behavior?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a beginner-friendly test plan for POST /tasks that enqueues a long-running job and notifies completion via a webhook to a registered URL. Include how you would verify webhook delivery guarantee",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test Plan for GET /reports?month=YYYY-MM ## Overview Test the /reports endpoint focusing on content negotiation, permission checks, caching, input validation, and rate limiting. ## Test Cases ### (a) Content-Type Correc…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Outline an end-to-end plan for a high-volume asynchronous content-moderation API: multipart uploads, a background job, and /status /result endpoints; ensure per-tenant isolation and data masking. Incl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Define tests for GET /products/{id} with a real-time feature flag. Verify response includes fields 'discount' and 'promotionTag' when flag is ON and omits them when OFF. Simulate admin API downtime to",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical API surface skills, including content negotiation, authentication, caching, input validation, and resilience patterns — all essential for building production-ready APIs.\n\n## Key Concepts Covered\n- **Content Negotiation**: Proper use of Accept header to serve multiple formats\n- **Permission Checks**: 403 Forbidden for unauthorized access\n- **Caching**: ETag/If-None-Match for efficient 304 responses\n- **Input Validation**: 400 Bad Request for invalid parameters\n- **Rate Limiting**: 429 Too Many Requests for service protection\n\n## Code Example\n```javascript\nasync function testContentNegotiation() {\n  const response = await fetch('/reports?month=2024-01', {\n    headers: { 'Accept': 'application/json' }\n  });\n  \n  expect(response.status).toBe(200);\n  expect(response.headers.get('Content-Type')).toContain('application/json');\n  \n  const data = await response.json();\n  expect(data).toMatchSchema(reportSchema);\n}\n\nasync function testETagCaching() {\n  const initial = await fetch('/reports?month=2024-01');\n  const etag = initial.headers.get('ETag');\n  \n  const cached = await fetch('/reports?month=2024-01', {\n    headers: { 'If-None-Match': etag }\n  });\n  \n  expect(cached.status).toBe(304);\n}\n\nasync function testRateLimiting() {\n  const responses = [];\n  for (let i = 0; i < 101; i++) {\n    responses.push(fetch('/reports?month=2024-01'));\n  }\n  \n  const results = await Promise.allSettled(responses);\n  const429s = results.filter(r => r.status === 'fulfilled' && r.value.status === 429);\n  \n  expect(429s.length).toBeGreaterThan(0);\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-11",
        "questionId": "q-5978",
        "question": "For a REST endpoint PUT /profiles/{id} that uses ETag-based optimistic locking (If-Match). As a beginner, outline a test plan to cover: successful update with the correct ETag, 412 Precondition Failed on a stale ETag, concurrent updates causing a race and a deterministic final state, and that the server returns a new ETag after a successful update. Include tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage strict PATCH semantics: verify only provided fields merge into the resource; undefined fields remain untouched, null can delete. Validate 412 on stale If-Match, and 200 with a refreshed ETag",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "First, retrieve the current ETag for /profiles/{id} via a GET request. Then perform a PUT request with a modified payload, including the If-Match header set to the retrieved ETag; expect a 200 OK response along with a n…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a comprehensive test harness for the SSE streaming endpoint /telemetry/stream/{sensorId} with per-tenant feature flags and versioned payloads. The test plan validates: at-least-once delivery via Last-Event-ID rep…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test plan includes: 1) input validation for required fields (cartId, amount, currency, paymentMethod) and types; 2) idempotency-key behavior: same key returns same result without duplicate charges; 3)",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a fundamental API concurrency control pattern that frequently causes production bugs when multiple clients attempt to modify the same resource simultaneously.\n\n## Key Concepts\n\n- ETag header for resource versioning\n- If-Match header for conditional requests\n- 412 Precondition Failed response status\n- Optimistic locking mechanism\n- Race condition detection and prevention\n- Tooling: pytest+requests (Python) or REST-assured (Java)\n\n## Code Example\n\n```javascript\n// Pseudo-test for ETag-based optimistic locking\nasync function testETagLock(url, id) {\n  const res1 = await\n```",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-12",
        "questionId": "q-24101",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /orders that creates a new order per tenant. Each request must include X-API-KEY identifying the tenant and an Idempotency-Key header to prevent duplicates on retries. Outline concrete test cases to validate idempotency under retries and concurrent submissions, input validation, per-tenant isolation, and proper 429/5xx handling; include tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an end-to-end plan for a fleet telemetry API with REST history and a WebSocket real-time stream. Use a deterministic vehicle simulator (e.g., 1k msgs/min for 10m). Validate WS heartbeats, in-o",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a test plan that validates asynchronous job submission, idempotent behavior, polling semantics, and webhook integrity. Verify POST /jobs with idempotency-key yields single queued job; poll tra",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan should cover idempotent submission, resubmission handling, deterministic webhook retries, queue backoffs, partial downstream outages, and end-to-end trace correlation. Use mocks for downstream se",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose server-side idempotency using a per-tenant map of Idempotency-Key to response; persist only after a transactional check to avoid duplicates. On repeat, return the same orderId and status. Test",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world idempotency handling, crucial for payment-like APIs.\n\n## Key Concepts\n- Idempotency keys per tenant; per-tenant isolation; race-condition handling; retry semantics; error handling.\n\n## Code Example\n```javascript\n// pseudocode: idempotency guard\nif (exists(tenantId, idempotencyKey)) return cachedResponse;\nbeginTxn();\nif (exists(tenantId, idempotencyKey)) return cachedResponse; // double-check\norder = createOrder(payload);\ncache(tenantId, idempotencyKey, order);\ncommitTxn();\nreturn order;\n```\n\n## Follow-up Questions\n- How would you test with network hiccups causing duplicate Idempotency-Key submissions?\n- How would you monitor and alert on idempotency violations?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-13",
        "questionId": "q-27142",
        "question": "Design a test plan for an internal REST API that serves per-tenant product catalogs with v1 and v2 endpoints. The API introduces optional fields in v2, a deprecation timeline for v1, and per-tenant data masking. Outline concrete tests for contract validation (OpenAPI and Pact-style), backward/forward compatibility across versions, per-tenant access control, data masking verification, performance under 2k RPS, and failover behavior when the catalog service is temporarily unavailable?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: implement contract tests for v1 and v2 with OpenAPI specs and Pact-style consumer contracts; verify backward/forward compatibility via a compatibility matrix; enforce per-tenant data masking che",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a comprehensive test harness for the SSE streaming endpoint /telemetry/stream/{sensorId} with per-tenant feature flags and versioned payloads. The test plan validates: at-least-once delivery via Last-Event-ID rep…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose resilience-focused tests: mock downstream latency and failures for /sales/daily and /customers/daily; assert 200 with partial payload and a warning header when one downstream times out; assert",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test idempotency by sending identical POST requests with the same idempotency key. The first request returns 201 Created with a unique transaction ID. Subsequent requests with the same key return 200 OK with the identic…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical API-testing skills across contract testing, multi-version compatibility, data privacy, and reliability under failures.\n\n## Key Concepts\n- OpenAPI contract testing\n- Pact-style consumer-driven tests\n- Compatibility matrices across API versions\n- Per-tenant data masking and access control\n- Fault-injection and resilience testing\n\n## Code Example\n```python\n# pseudo compatibility check\ndef is_backward_compatible(old_schema, new_schema):\n    # ensure new fields are optional; existing fields preserved\n    return old_schema.fields.issubset(new_schema.fields) and all(f.optional for f in new_schema.fields if f.name in old_schema.fields)\n```\n\n## Follow-up Questions\n- How would you automate deprecation window validation in CI/CD?\n- What tooling would you use to simulate per-tenant data masking at scale?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-14",
        "questionId": "q-11267",
        "question": "Design a pragmatic test plan for a multi-tenant GraphQL API with federation and per-tenant extensions, including: access control and data masking validation; detection of N+1 queries in nested resolvers using batched load patterns; per-tenant schema contract testing; performance testing with caching and batching; and resilience testing against partial upstream outages. Provide concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Test Plan: POST /payments Idempotency ### Overview This test plan validates idempotency behavior for a payment endpoint accepting `{amount, currency, source, description}` with a required `X-Idempotency-Key` header.…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a test harness that runs per-tenant WebSocket clients, sends events with monotonically increasing seqId, and asserts in-order delivery and deduplication per user. Simulate slow consumers to tr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose layered testing: unit tests for resolvers with mocked batch loaders to catch N+1; integration tests against staging with tenant tokens validating access control and data masking; contract test",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Approach: create a layered end-to-end test plan for a multi-tenant time-series API. Use contract tests, load tests with k6, idempotent writes via keys, and streaming dedup using sequence IDs. Validate",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nIn multi-tenant GraphQL APIs with federation and dynamic extensions, testing must cover authorization, per-tenant schema correctness, N+1 risks, performance under batching, and resilience to partial failures. This question probes the ability to craft end-to-end test coverage that mirrors real-world deployment with isolation guarantees and evolving schemas.\n\n## Key Concepts\n- Multi-tenant GraphQL + federation + per-tenant extensions\n- Access control and data masking validation\n- N+1 detection via batched data loaders\n- Contract testing for per-tenant schema drift\n- Performance testing with caching and batching patterns\n- Resilience/chaos testing for partial upstream outages\n\n## Code Example\n```javascript\n// Example: unit test for a resolver with a batched loader\ntest('Orders field batches loads', async () => {\n  const loadOrders = jest.fn().mockResolvedValue([{id: '1'}, {id: '2'}]);\n  const result = await resolveOrders({ user: 'u1' }, { loaders: { orders: loadOrders } });\n  expect(loadOrders).toHaveBeenCalledTimes(1);\n  expect(result).toHaveLength(2);\n});\n```\n\n## Follow-up Questions\n- How would you detect N+1 in a live system using tracing tools like OpenTelemetry?\n- How would you evolve tests when per-tenant schemas drift or new extensions are rolled out?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-15",
        "questionId": "q-3633",
        "question": "Design a practical test plan for a payments API with POST /payments/process that uses an idempotency-key to guard against duplicates across ledger, order, and an event bus. Outline concrete test cases for high-concurrency identical keys, verify a single ledger write and a single order update, and validate eventual ledger consistency via polling. Include backoff/retry validation and simulate ledger outages or event-bus delays?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement idempotent POST /features using an idempotency-key header to ensure upsert semantics, where repeated requests return HTTP 200 without duplicate data ingestion. Validate eventual read consistency by confirming…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a comprehensive test plan for POST /payments/process using an idempotency-key to prevent duplicates across ledger, order, and event bus systems. Create high-concurrency tests with identical keys using tools like…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test POST /webhooks/{provider}: verify signature with HMAC, ensure Idempotency-Key guarantees at-most-once processing, simulate exponential-backoff retries, check 2xx for valid payloads, 429 on rate l",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Test Plan Overview** Use two roles: `user` and `admin`. Validate field-level access control by querying `{ user(id: 1) { name } }`. This should return only the `name` field for a standard user, while an admin should r…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates understanding of idempotency patterns and cross-service consistency in distributed payment systems. It tests knowledge of concurrency handling, retry mechanisms, and observability in mission-critical financial flows.\n\n## Key Concepts\n- Idempotency keys spanning multiple services (ledger, order, event bus)\n- Cross-service data consistency and eventual consistency windows\n- High-concurrency testing methodologies (k6, JMeter)\n- Failure scenario simulation: ledger outages, event-bus delays\n- Observability patterns: distributed tracing, metrics, and retry monitoring\n\n## Code Example\n```javascript\n// Pseudo-test using k6-like script\nimport http from 'k6/http';\nimport { check } from 'k6';\nexport default function () {\n  const url = 'https://api.example.com/payments/process';\n  const payload = {\n    amount: 100,\n    currency: 'USD',\n    'idempotency-key': 'test-key-123'\n  };\n  const params = {\n    headers: {\n      'Content-Type': 'application/json',\n      'Authorization': 'Bearer token'\n    }\n  };\n  \n  // Simulate high concurrency with identical keys\n  const responses = http.batch(url.map(url => \n    http.post(url, JSON.stringify(payload), params)\n  ));\n  \n  // Verify single processing\n  check(responses, {\n    'single successful response': (r) => r.filter(res => res.status === 200).length === 1,\n    'duplicate responses rejected': (r) => r.filter(res => res.status === 409).length > 0\n  });\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-16",
        "questionId": "q-6063",
        "question": "Design an end-to-end API test plan for a multi-tenant REST API that serves per-tenant resources with quotas. The endpoints are GET /tenants/{tenantId}/resources and POST /tenants/{tenantId}/transactions. Include how you'd validate per-tenant isolation under high concurrency, simulate quota changes via admin API, verify rate-limit headers and 429 responses, and test failure modes like Redis/cache outages across regions. Provide concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a tenant-scoped load tool (k6) to drive tenants A,B,C at varying rates against GET /tenants/{tenantId}/resources and POST /tenants/{tenantId}/transactions. Assert per-tenant data isolation; verify",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Test plan focuses on idempotent ingestion with a per-tenant schema registry, end-to-end validation from /ingest to the kafka topic to the consumer result, and handling duplicates under concurrency. Us",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a comprehensive test plan for POST /payments/batch: Verify 200 response with per-item success statuses when all charges process correctly; validate Idempotency-Key header prevents duplicate processing on retry sc…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Key defenses: idempotent handling via per-event-ID keys in the storage layer; a deduplication path in the Kafka consumer to detect and discard duplicates; tracking the last-seen sequence number per tenant in Redis to de…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nMulti-tenant quotas with dynamic admin updates and cross-region resilience test end-to-end behavior, reflecting production complexity.\n\n## Key Concepts\n- Multi-tenant isolation\n- Dynamic quotas propagation\n- Observability and headers\n- Failure modes and graceful degradation\n- Tooling: k6, Postman, Redis, etc.\n\n## Code Example\n```javascript\n// k6 skeleton for tenant traffic\nimport http from 'k6/http';\nimport { check } from 'k6';\nexport default function () {\n  const r = http.get('https://api.example.com/tenants/t1/resources');\n  check(r, { 'status is 200': (r) => r.status === 200 });\n}\n```\n\n## Follow-up Questions\n- How would you validate quota propagation latency across regions?\n- How would you incorporate chaos tests for cache/queue outages?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-17",
        "questionId": "q-18041",
        "question": "Design an end-to-end test plan for a partner webhook ingestion endpoint at /webhooks/payments that validates HMAC-SHA256 signatures with rotating keys. Include idempotency via eventId, concurrent delivery handling, key-rotation downtime, replay protection, and downstream retry/compensation semantics. Provide concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan end-to-end tests for a partner webhook endpoint /webhooks/payments using HMAC-SHA256 with rotating keys. Verify idempotency with eventId, dedupe concurrent posts, simulate key rotation with zero",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Outline a beginner-friendly test plan for POST /webhook that validates an HMAC-SHA256 signature in X-Signature over the JSON payload with a shared secret. Include: happy-path with valid signature; mis",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a dedupe store (Redis) keyed by Idempotency-Key; run parallel requests with the same key and assert a single payout created. Validate distinct keys create separate payouts. Simulate webhook delive",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Test Plan for POST /payments/process Idempotency **Objective:** Verify that duplicate requests with the same X-Idempotency-Key result in a single charge, while different keys allow separate charges. ### Test Cases |…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nReal-world webhook reliability and security require resilience to duplicate deliveries, key rotation, and downstream retries under load. This tests knowledge of idempotent processing, signature validation, and observability across services.\n\n## Key Concepts\n\n- Webhook reliability and exactly-once semantics\n- HMAC verification with rotating keys\n- Deduplication using eventId and race-condition handling\n- Replay protection and retry/backoff strategies\n- Observability, metrics, and tracing for webhook pipelines\n\n## Code Example\n\n```javascript\n// Pseudocode: verify signature and idempotency\nfunction handleWebhook(req) {\n  const key = getCurrentSigningKey();\n  const valid = verifyHmac(req.body, req.headers['X-Signature'], key);\n  if (!valid) return 401;\n  if (isEventProcessed(req.body.eventId)) return 200;\n  processEvent(req.body); // write upsert, emit downstream task\n  return 200;\n}\n```\n\n## Follow-up Questions\n\n- How would you test key-rotation during live traffic without downtime?\n- How would you simulate high-volume webhook bursts and validate backpressure?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-18",
        "questionId": "q-15198",
        "question": "Design a practical test plan for an API delivering real-time vehicle telematics via REST GET /vehicles/{id} and WebSocket /vehicles/{id}/stream. Include concrete test cases, tooling, and expected outcomes, covering data correctness, streaming ordering/dedup/reconnect/backpressure, multi-tenant isolation, failure modes, and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive test matrix covering regions (US/UK) and locales (en-US/en-GB), with fallback validation for unsupported locales. Verify the presence and correctness of price, currency, and tax fields, ensuring c…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a comprehensive automated test plan for the payments API by implementing an idempotent POST /payments endpoint that utilizes an idempotency-key header to prevent duplicate charge creation. Test duplicate submissi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "REST: 200 with schema, 404 not found, 429 for rate limiting. Streaming: ensure in-order delivery, dedupe, reconnect recovery, and backpressure signaling. Multi-tenant: simulate 5 tenants with scoped t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Outline a practical plan: 1) use Pact for REST and GraphQL contracts; 2) run schema compatibility checks for each stitch version against a registry; 3) implement per-tenant visibility tests and access",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes real-world API testing skills for REST and streaming endpoints, multi-tenant isolation, and resilience under failure.\n\n## Key Concepts\n- REST + streaming testing\n- Data correctness, order, dedupe\n- Reconnect, backpressure\n- Tenant isolation and security\n- Observability and test data management\n\n## Code Example\n```javascript\n// example test skeleton\n```\n\n## Follow-up Questions\n- How would you test clock skew or out-of-order events across streams?\n- How would you automate data masking in streamed payloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-19",
        "questionId": "q-7458",
        "question": "Design an automated test plan for a payments API: POST /payments creates a charge, followed by status updates via SSE/WebSocket on /payments/{id}/stream. Include idempotency handling, cross-service correlation with fraud-check and ledger writes, retries with backoff, and outage scenarios (DB failover, DNS hiccups). Provide concrete test cases, tooling, and success criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose resilience-focused tests: mock downstream latency and failures for /sales/daily and /customers/daily; assert 200 with partial payload and a warning header when one downstream times out; assert",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "End-to-end tests publish a deterministic sequence of shipment events with explicit sequence numbers and injected delays. Validate GET /shipments/{id} returns the latest state within 200 ms, and /shipm",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a comprehensive automated test plan for the payments API by implementing an idempotent POST /payments endpoint that utilizes an idempotency-key header to prevent duplicate charge creation. Test duplicate submissi…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose end-to-end tests that seed deterministic events, track ids for dedupe, simulate backpressure via a traffic injector, verify that SSE clients receive events in strict order with no duplicates,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your ability to design comprehensive test strategies for distributed payment systems that must handle real-world complexities including idempotency, cross-service coordination, and failure scenarios.\n\n## Key Concepts\n- Idempotency mechanisms for financial operations\n- Cross-service correlation and distributed transaction consistency\n- Real-time status delivery via SSE/WebSocket protocols\n- Resilience patterns including backoff retries and circuit breakers\n- Chaos engineering for downstream service failures\n\n## Code Example\n```javascript\n// Example test for idempotency handling\ndescribe('POST /payments idempotency', () => {\n  it('should return same chargeId for duplicate requests', async () => {\n    const idempotencyKey = 'test-key-123';\n    const request = { amount: 100, currency: 'USD' };\n    \n    const firstResponse = await request(app)\n      .post('/payments')\n      .set('Idempotency-Key', idempotencyKey)\n      .send(request);\n    \n    const secondResponse = await request(app)\n      .post('/payments')\n      .set('Idempotency-Key', idempotencyKey)\n      .send(request);\n    \n    expect(firstResponse.body.chargeId).toBe(secondResponse.body.chargeId);\n  });\n});\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-20",
        "questionId": "q-12267",
        "question": "Design an automated test plan for a bulk upsert API: a /api/v2/accounts/bulk endpoint that accepts a JSONL stream of up to 1000 records per request and returns per-record statuses. Validate partial failures, per-record error reporting, and strict tenant isolation in a multi-tenant SaaS. Include test data strategies, failure modes, and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test idempotent behavior for POST /payments/process by issuing concurrent requests with the same idempotency_key and asserting a single charge with a consistent transaction_id, while retries return th",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage per-record Id and transportId to achieve idempotency. Validate dedup across partitions by replaying identical batches and asserting a single write per record in the sink. Check eventual consi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Explain how you would verify idempotent behavior for POST /orders with Idempotency-Key, ensuring exactly-once order creation despite repeated requests at high concurrency; validate DLQ routing and bac",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a bulk upsert test suite focusing on JSONL payloads up to 1000 records, validating per-record status, atomic batch vs partial commits, idempotent retries with the same idempotency-key, and ten",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests for bulk upsert APIs are common but often miss partial-failure semantics and strict tenancy boundaries. This question probes practical test design, data strategies, and failure injection in a multi-tenant SaaS.\n\n## Key Concepts\n- Bulk operations and per-record results\n- Idempotency and safe retries\n- Partial failure semantics and rollback\n- Multi-tenant isolation and data masking\n- Downstream consistency and fault injection\n\n## Code Example\n```javascript\n// Pseudo-test illustration\nconst payload = generateBulkJsonl(1000, tenantId);\nconst res = await post(\"/api/v2/accounts/bulk\", payload, { headers: { \"Idempotency-Key\": key } });\nassert(res.status === 200);\nassert(res.body.length === 1000);\n```\n\n## Follow-up Questions\n- How would you model backoff for retries under surge?\n- How would you verify data consistency across downstream stores after bulk upserts?\n- How would you test schema evolution for bulk records?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-21",
        "questionId": "q-13930",
        "question": "You're exposing a REST POST /orders that uses an idempotency-key header to ensure at-least-once delivery. The service writes to a distributed DB and publishes an 'order_placed' event to Kafka. Design an automated test plan to verify idempotency: repeated identical requests, concurrent requests with the same key vs different keys, failure modes (DB/Kafka downtime), cleanup of old keys, and race conditions during retry/backoff. Include concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan focuses on idempotent export IDs, paged data integrity, and delta exports. Test cases: 1) retry with same export_id returns identical pages; 2) since-based delta yields correct additions/removals",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a comprehensive automated test plan for the payments API by implementing an idempotent POST /payments endpoint that utilizes an idempotency-key header to prevent duplicate charge creation. Test duplicate submissi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Validate RBAC, per-tenant data isolation, dynamic rate limits, and canary flags in a multi-tenant gateway. Create tenants A/B, mint tokens, and exercise endpoints with cross-tenant access attempts; bu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a durable idempotency store (Redis/DynamoDB) with a TTL. On first POST, create the order, publish to Kafka, then store idempotency-key -> orderId. Replays with same key return 200 and same orderId",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical idempotency testing in a distributed system, covering state stores, event emission, concurrency, and failure modes.\n\n## Key Concepts\n- Idempotent operations and deduplication windows\n- Durable storage guarantees and TTL management\n- Concurrency control and race conditions\n- Failure modes and graceful degradation\n\n## Code Example\n```javascript\n// Pseudo-handler: on POST /orders with idempotencyKey\nif (!store.has(key)) {\n  const order = createOrder(payload);\n  publishKafka(order);\n  store.set(key, order.id, ttl);\n  return { status: 200, body: { orderId: order.id } };\n} else {\n  return { status: 200, body: { orderId: store.get(key) } };\n}\n```\n\n## Follow-up Questions\n- How would you test TTL-based key reuse vs. hard deduplication windows?\n- How would you monitor and alert on rising duplicate requests or stuck idempotency keys?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-22",
        "questionId": "q-18415",
        "question": "You maintain a real-time telemetry ingestion API POST /telemetry/bulk that accepts arrays up to 1MB and uses per-batch idempotency keys. Downstream consumers read from a distributed log with at-least-once delivery. Design a practical API testing strategy to verify end-to-end exactly-once delivery, idempotency, and data ordering under peak load and regional outages. Include concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement comprehensive end-to-end testing for the multi-tenant event publishing API through a structured approach: utilize idempotency keys to deduplicate POST /events requests, verify webhook delivery with exponential…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a test harness that issues concurrent POST /telemetry/bulk calls with controlled idempotency keys, each carrying a monotonic sequence number. Validate no duplicates arrive in the downstream store, all payloads ar…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a streaming API test plan using consumer-driven contracts via a schema registry. Validate backward compatibility by simulating added/removed fields with defaults, ensure chunk boundaries are pr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan focuses on GraphQL nested data, access control, and cross-region behavior. Validate deep queries, enforce depth and complexity limits, test cache invalidation after mutations, simulate token expi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for high-throughput ingestion pipelines must verify end-to-end guarantees—exactly-once semantics, idempotency, and ordering—under realistic failure conditions, not just basic endpoint validation.\n\n## Key Concepts\n\n- **Exactly-once delivery**: Ensuring data is processed once despite at-least-once underlying transport\n- **Idempotency via batch keys**: Preventing duplicate processing when the same batch is submitted multiple times\n- **Ordering guarantees**: Maintaining sequence integrity in downstream consumers\n- **Fault injection**: Simulating regional outages and network partitions to verify resilience\n\n## Code Example\n\n```javascript\n// Test harness for exactly-once verification\nasync function testExactlyOnce() {\n  const idempotencyKey = uuidv4();\n  const payload = generateTelemetryBatch(1024 * 512); // 512KB\n  \n  // Send same request multiple times (simulating retries)\n  const results = await Promise.all([\n    postTelemetry(payload, idempotencyKey),\n    postTelemetry(payload, idempotencyKey),\n    postTelemetry(payload, idempotencyKey)\n  ]);\n  \n  // Verify: only ONE record in downstream store\n  const downstreamCount = await countRecords(idempotencyKey);\n  expect(downstreamCount).toBe(1);\n}\n\n// Test ordering under concurrent load\nasync function testOrdering() {\n  const sequences = Array.from({length: 1000}, (_, i) => i);\n  await Promise.all(sequences.map(seq => \n    postTelemetry({seq, data: `batch-${seq}`}, `key-${seq}`)\n  ));\n  \n  const stored = await getAllRecords();\n  const isOrdered = stored.every((r, i) => r.seq === i);\n  expect(isOrdered).toBe(true);\n}\n```\n\n## Test Cases\n\n| Scenario | Tooling | Expected Outcome |\n|----------|---------|------------------|\n| Duplicate submits with same idempotency key | Retry harness | Single record in sink |\n| Concurrent batch writes | k6/JMeter load test | All sequences preserved |\n| Regional failover | Chaos mesh injection | Zero data loss, correct ordering |\n| Network interruption during POST | WireMock fault simulation | Exactly-once after recovery |",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-23",
        "questionId": "q-9494",
        "question": "Design a beginner-friendly test plan for a per-user rate-limited POST /checkout endpoint that enforces 5 requests per minute per user. Validate 429 responses with Retry-After, ensure the limit resets after 60 seconds, test concurrent requests from multiple clients to confirm distribution, and ensure non-idempotent POSTs do not bypass quotas. Provide concrete test cases and tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "End-to-end test using a deterministic idempotency-key per event, a synthetic event generator, and a consumer that records to a durable ledger. Validate exactly-once by resending the same key and asser",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Test Plan: POST /jobs Asynchronous Enqueue ### 1. Happy Path — Single Submission Submit a valid job payload with a unique Idempotency-Key. Verify the response returns HTTP 202 Accepted with a Location header pointing…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a comprehensive API testing plan that validates per-user RBAC enforcement across a distributed architecture consisting of a central policy decision point (PDP) and regional edge caches. The plan should cover thre…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Execute a per-user rate-limit test: 5 POST /checkout requests in 60s; 6th returns 429 with Retry-After 60s; verify that headers like Retry-After or X-RateLimit-Reset reflect 60s; run parallel bursts f",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical API testing skills around rate limiting, a common production risk. It checks how a tester validates 429s, backoffs, and per-user isolation under concurrent load.\n\n## Key Concepts\n\n- HTTP rate limiting basics and 429s\n- Retry-After and rate-limit headers\n- Per-user isolation vs shared pools\n- Basic load testing with tools (k6, Postman/Newman)\n- Edge cases: bursts, clock drift\n\n## Code Example\n\n```javascript\nimport http from 'k6/http';\nimport { check, sleep } from 'k6';\n\nexport let options = { vus: 5, duration: '70s' };\n\nexport default function () {\n  let res = http.post('https://api.example.com/checkout', JSON.stringify({ items: [] }), {\n    headers: { 'Content-Type': 'application/json' }\n  });\n  check(res, { 'status is 200 or 429': (r) => r.status === 200 || r.status === 429 });\n  sleep(0.5);\n}\n```\n\n## Follow-up Questions\n\n- How would tests adapt if rate limiting is IP-based rather than per-user token-based?\n- How would you verify rate limit state is consistent across multiple API gateways or regions?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-24",
        "questionId": "q-7371",
        "question": "Design an advanced test plan for a REST API POST /orders that accepts idempotent submissions via an idempotency-key, with multi-region replicas and a separate read-model; verify exactly-once processing, deduplication across retries, latency effects on order status propagation, and schema evolution of the payload; include failure modes (primary failure, network partition), back-pressure, and admin override paths?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan should simulate multi-region uploads, sending chunks out-of-order and retrying duplicates; enforce per-user quotas under bursts; validate reassembly with SHA-256; verify final callback paylo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a testing matrix for tenants on mixed JSON Schemas. Validate core fields always present, new fields appear for newer tenants, and streaming reflects per-tenant schema in real time. Automate wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a plan that enforces exactly-once using a Redis-backed idempotency window and an outbox pattern for durable writes; simulate duplicate submissions under retries to ensure no additional orders a",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use end-to-end tests with Postman/Newman or REST-assured. Confirm 200 JSON with fields id,title,price; 406 when Accept is application/xml; verify localized titles/descriptions for fr and es under Acce",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests understanding of idempotent APIs, distributed write reliability, and multi-region read-model consistency under failure.\n\nKey Concepts\n- Idempotency keys and deduplication windows\n- Outbox/CDC patterns for exactly-once processing\n- Cross-region latency and eventual consistency\n- Schema evolution and backward compatibility\n- Failure modes: primary failover, network partitions, back-pressure\n\nCode Example\n```javascript\n// Example: idempotent POST /orders with dedupe check\nasync function submitOrder(key, payload) {\n  const exists = await redis.get(`order_idem:${key}`);\n  if (exists) return JSON.parse(exists);\n  const order = await db.createOrder(payload);\n  await redis.set(`order_idem:${key}`, JSON.stringify(order), 'EX', 3600);\n  await outbox.publish(order);\n  return order;\n}\n```\n\nFollow-up Questions\n- How would you verify that admin override paths do not bypass deduplication?\n- What metrics and dashboards would you expose to detect replication lag and dead-lettered events?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-11T13:15:22.766Z",
    "version": 1
  }
]