[
  {
    "id": "test-api-testing",
    "channelId": "api-testing",
    "channelName": "API Testing",
    "title": "API Testing Knowledge Test",
    "description": "Test your understanding of API Testing with 25 questions.",
    "questions": [
      {
        "id": "api-testing-q-0",
        "questionId": "q-7750",
        "question": "Design an end-to-end test plan for GET /composite/users/{id} that aggregates profile, preferences, and activities from three services in parallel and caches results in Redis. Include: stale-read detection under simulated backend latency, race-conditions with concurrent updates during fetch, and cache eviction/outage handling with fallback refresh. Provide tooling suggestions, data-generation approach, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a durable idempotency store keyed by idempotency-key with a 24h TTL; on first POST /payments create the charge and return 201 with a charge_id, caching the payload. On subsequent requests with the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a test plan for GET /composite/users/{id} that aggregates from three services in parallel and caches in Redis. Include: 1) stale-read detection by injecting latency on one backend and assert",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Approach: seed a deterministic set of idempotent events to POST /events, verify Redis-backed GET /analytics?window=1m reflects exact aggregates per window, and confirm Kafka offsets align with exactly",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Test Architecture - Deploy N concurrent SSE clients connecting to /stream/events - Set up a local Kafka cluster with a dedicated test topic - Implement a mock downstream consumer to verify message flow ## Test Cases…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for composite endpoints with distributed sources and caching expose subtle consistency, race conditions, and failure modes. This question assesses end-to-end orchestration, cache invalidation, and resilience in realistic multi-service setups.\n\n## Key Concepts\n\n- End-to-end integration testing across services\n- Cache invalidation and stale reads with TTL\n- Race conditions and idempotent fetches\n- Fault injection and chaos testing\n- Contract tests between services\n\n## Code Example\n\n```javascript\n// Example sketch: k6 script outline for parallel requests and validations\nimport http from 'k6/http';\nexport default function () {\n  const res = http.get('https://api/.../composite/users/123');\n  // validate fields, timestamps, and consistency\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt this plan for multi-region deployments?\n- How would you verify partial data consistency vs. full consistency during outages?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-1",
        "questionId": "q-24704",
        "question": "Design a practical interview question: For a high-volume asynchronous content-moderation API that accepts multipart uploads, starts a background job, and exposes /status and /result endpoints, outline a concrete end-to-end test plan that ensures per-tenant isolation, idempotency, and resilience to failures (upload error, queue backpressure, moderation-service outage) under burst load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a test plan that validates HMAC-based request signing, nonce replay protection, and idempotent handling for webhook ingestion. Include per-publisher secrets, signature generation, and error ha",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Tests should cover: (1) payload validation for title and dueDate; (2) auth and tenant scoping via X-API-KEY; (3) data isolation by creating two tenants and verifying no cross-tenant visibility; (4) ra",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline an end-to-end plan for a high-volume asynchronous content-moderation API: multipart uploads, a background job, and /status /result endpoints; ensure per-tenant isolation and data masking. Incl",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a testing strategy for a globally distributed, federated GraphQL API with per-field authorization and a shared cache. Include per-field contract tests for multiple roles, schema-drift checks ac",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests async workflows, data isolation, and resilience in a realistic API.\n\n## Key Concepts\n- Async job lifecycle\n- Idempotency and dedup\n- Data masking per tenant\n- End-to-end latency under burst\n- Failure injection and circuit breaking\n\n## Code Example\n```javascript\n// Example test sketch (pseudo)\ndescribe('upload -> status', () => {\n  it('uploads idempotently and polls until DONE', async () => { /* ... */ });\n});\n```\n\n## Follow-up Questions\n- How would you simulate moderation-service outage?\n- How would you measure end-to-end latency distribution?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-2",
        "questionId": "q-17072",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /payments/process that implements idempotency via the header X-Idempotency-Key. Include concrete test cases for duplicate requests with the same key, requests with different keys, missing keys, and high-concurrency access with the same key. What tooling and data setup would you use, and how would you verify a single charge?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: cache-aware, multi-tenant ML inference test. Verify keys use tenant_id + model_version and eviction on changes; simulate warm/cold starts with 1000 concurrent requests (k6); monitor latency, err",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Test Plan for POST /payments/process Idempotency **Objective:** Verify that duplicate requests with the same X-Idempotency-Key result in a single charge, while different keys allow separate charges. ### Test Cases |…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test idempotent behavior for POST /payments/process by issuing concurrent requests with the same idempotency_key and asserting a single charge with a consistent transaction_id, while retries return th",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Test Plan for POST /events Batch Ingestion API ### 1. Input Validation - Verify batch accepts maximum 100 events - Validate each event contains required fields: id, type, payload - Test payload size limits and type c…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nIdempotency is a critical pattern in payment systems. Interviewers ask this to assess whether a candidate understands:\n- How to prevent duplicate charges (a costly business problem)\n- The distinction between idempotent and non-idempotent operations\n- Proper handling of edge cases and failure scenarios\n\n## Key Concepts\n\n- **Idempotency Key:** A unique client-generated identifier (UUID recommended) that allows the server to recognize duplicate requests\n- **Idempotent Response:** Same response returned for retries, regardless of timing\n- **Concurrency:** Multiple simultaneous requests with the same key must be handled safely (typically via database locking)\n- **TTL (Time-to-Live):** Idempotency keys typically expire after 24-48 hours\n\n## Code Example\n\n```javascript\n// Idempotency test using Jest and Supertest\nconst request = require('supertest');\nconst { v4: uuidv4 } = require('uuid');\n\ndescribe('POST /payments/process idempotency', () => {\n  const idempotencyKey = uuidv4();\n  const paymentPayload = {\n    amount: 5000,\n    currency: 'USD',\n    card_token: 'test_tok_visa'\n  };\n\n  test('TC-01: duplicate request with same key charges once', async () => {\n    const firstResponse = await request(app)\n      .post('/payments/process')\n      .set('X-Idempotency-Key', idempotencyKey)\n      .send(paymentPayload);\n\n    const secondResponse = await request(app)\n      .post('/payments/process')\n      .set('X-Idempotency-Key', idempotencyKey)\n      .send(paymentPayload);\n\n    expect(firstResponse.status).toBe(200);\n    expect(secondResponse.status).toBe(200);\n    expect(firstResponse.body.transactionId).toBe(secondResponse.body.transactionId);\n    \n    // Verify single charge via external API or database\n    const chargeCount = await getChargeCount(idempotencyKey);\n    expect(chargeCount).toBe(1);\n  });\n\n  test('TC-04: high concurrency with same key', async () => {\n    const concurrentRequests = Array(10).fill(null).map(() =>\n      request(app)\n        .post('/payments/process')\n        .set('X-Idempotency-Key', idempotencyKey)\n        .send(paymentPayload)\n    );\n\n    const responses = await Promise.all(concurrentRequests);\n    const transactionIds = responses.map(r => r.body.transactionId);\n    const uniqueIds = [...new Set(transactionIds)];\n\n    expect(uniqueIds).toHaveLength(1);\n  });\n});\n```\n\n## Follow-up Discussion Points\n\n- **CI Automation:** Integrate these tests into CI pipelines using containerized test databases and mock payment gateways (e.g., Stripe Mock)\n- **Gateway Downtime:** Use tools like Chaos Mesh or Toxiproxy to simulate network failures and verify idempotency handles partial failures gracefully",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-3",
        "questionId": "q-2369",
        "question": "Design an automated test plan for a webhook listener at /webhooks/ci ingesting push events from a CI service. Each payload is signed with HMAC-SHA256 and delivered at-least-once to a Redis-backed event store. Outline concrete test cases for signature validation, replay/duplicate handling, event ordering or out-of-order resilience, retries/backoff, and failure modes (secret rotation, Redis outage). Include tooling suggestions and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Idempotent bulk import test: POST /imports/users with 100k records and an idempotency-key. Re-send with the same key to verify no duplicates. Inject partial downstream analytics failures to validate p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: verify response fields are shaped by per-user feature flag and trait, using two synthetic users (premium_offers ON/OFF, region A/B). Expect 200 with premium fields when ON, 200 without premium f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement idempotency using an Idempotency-Key header on POST /orders with a Redis-backed map to prevent duplicate order creation. For the WebSocket stream at /orders/stream, include sequence numbers in events and imple…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test signature validation by sending tampered payloads (expect 401). Enforce idempotency by replaying the same event and ensuring no duplicate writes to Redis. Validate ordering vs. out-of-order event",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nGauges ability to test security and reliability of inbound webhooks in a distributed system.\n\n## Key Concepts\n- Signature validation\n- Idempotency\n- Ordering vs concurrency\n- Retry/backoff strategies\n- Failure modes (secret rotation, Redis outage)\n\n## Code Example\n```javascript\n// Example test snippet for signature verification\nfunction verifySignature(payload, secret, signature) {\n  const h = crypto.createHmac('sha256', secret)\n  h.update(payload)\n  return `sha256=${h.digest('hex')}` === signature\n}\n```\n\n## Follow-up Questions\n- How would you test secret rotation without missing events?\n- How would you simulate Redis outage in CI?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-4",
        "questionId": "q-9101",
        "question": "Design an end-to-end test plan for a video-processing API used by Tesla/Uber: POST /videos submits a video file and returns a jobId; GET /videos/{id}/status streams progress via SSE. Validate idempotent submissions for identical file hashes, 100+ concurrent jobs, resilience when the transcoder restarts, and final artifact URL accuracy and retry behavior; include tooling and data-management details?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## GraphQL Federation Gateway API Testing Plan ### 1. Testing Scope Overview **System Under Test:** - GraphQL Federation Gateway (single API endpoint) - Multiple per-tenant subgraphs with schema variants - Persisted que…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an idempotency key per /payments call to prevent duplicates; mock downstreams (fraud, bank, ledger) and verify a single transaction and consistent ledger for repeated requests with the same key. S",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end automated test plan for a REST ingestion endpoint /telemetry/{tenantId}/ingest that accepts chunked JSON lines, enforces per-tenant quotas, deduplicates by message_id, validates s",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Idempotent submissions via file hash; ramp up to 100 concurrent jobs; verify SSE progress ordering; simulate transcoder crash to validate retries, backoff, and DLQ; assert final artifact URL/metadata",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design an end-to-end plan for asynchronous, stateful API flows with streaming updates, ensuring idempotency, high concurrency, and failure recovery—critical at scale.\n\n## Key Concepts\n\n- Idempotency and deduplication using file hashes\n- Server-Sent Events correctness and ordering\n- Concurrency and backpressure under high load\n- Failure modes: downstream crashes, retries, and DLQs\n- Observability: logs, traces, and archives\n\n## Code Example\n\n```javascript\n// Pseudo-test: submit video + listen to SSE, assert final state\nconst jobId = submitVideo(fileHash, fileBytes);\nconst stream = listenSSE(`/videos/${jobId}/status`);\nfor await (const evt of stream) {\n  if (evt.type === 'progress' && evt.complete) break;\n}\nverifyArtifactUrl(jobId);\n```\n\n## Follow-up Questions\n\n- How would you verify idempotency across retries with network jitter?\n- How would you adapt the plan for a multi-region deployment with eventual consistency?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-5",
        "questionId": "q-23686",
        "question": "Design a beginner-friendly test plan for GET /reports/{tenantId}/{reportId} serving analytics from a MongoDB-backed store with a Redis cache. Include input validation, per-tenant isolation (403 on cross-tenant access), cache warm-up and invalidation when underlying data changes, maintenance downtime resilience, and latency targets (cached <150ms, cold fetch <1.2s). Provide concrete test cases and tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test idempotent behavior for POST /payments/process by issuing concurrent requests with the same idempotency_key and asserting a single charge with a consistent transaction_id, while retries return th",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test POST /webhooks/{provider}: verify signature with HMAC, ensure Idempotency-Key guarantees at-most-once processing, simulate exponential-backoff retries, check 2xx for valid payloads, 429 on rate l",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test plan covers: input validation of tenantId/reportId formats; 403 when path tenantId differs from authenticated user; 404 for missing reports; cache behavior: cold fetch hits MongoDB, subsequent hi",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Idempotent bulk import test: POST /imports/users with 100k records and an idempotency-key. Re-send with the same key to verify no duplicates. Inject partial downstream analytics failures to validate p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for multi-tenant data delivery with cache layers and maintenance resilience are common in production APIs. This question checks practical testing habits for correctness, isolation, performance boundaries, and failure modes.\n\n## Key Concepts\n\n- Tenant isolation and authorization checks\n- Cache layering and eviction strategies\n- Data-change invalidation triggers\n- Failure mode handling and graceful degradation\n- Latency verification across hot/cold paths\n\n## Code Example\n\n```javascript\n// Jest + supertest example for tenant isolation\nconst request = require('supertest');\nconst app = require('../src/app');\n\ntest('403 when tenant mismatch', async () => {\n  const res = await request(app)\n    .get('/reports/tenantA/report1')\n    .set('Authorization', 'Bearer token-for-tenantB');\n  expect(res.status).toBe(403);\n});\n```\n\n## Follow-up Questions\n\n- How would you automate cache eviction verification after a data update?\n- How would you simulate Redis outages and verify fallback behavior in tests?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-6",
        "questionId": "q-4532",
        "question": "Design and implement a test strategy for a high-volume Order Management API used by a global fleet (Tesla, DoorDash, Citadel-scale) exposing POST /orders, GET /orders/{id}, and a real-time WebSocket stream at /orders/stream. Ensure idempotent order creation, exactly-once delivery for status events, out-of-order handling, and cross-region data consistency with Kafka and PostgreSQL; include concrete test cases, tooling, and failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: test idempotent POST /orders using a Redis-backed idempotency store TTL 3600s. 1) POST /orders with payload and Idempotency-Key A, expect 201 and orderId. 2) Parallel retry with same key; expect",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement idempotency using an Idempotency-Key header on POST /orders with a Redis-backed map to prevent duplicate order creation. For the WebSocket stream at /orders/stream, include sequence numbers in events and imple…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a dedupe store (Redis) keyed by Idempotency-Key; run parallel requests with the same key and assert a single payout created. Validate distinct keys create separate payouts. Simulate webhook delive",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test idempotency by sending two POST /orders with the same Idempotency-Key and verify only one order is created and the second response reuses the first order_id; verify /orders/{id} status transition",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTesting idempotency and reliable streaming under global scale is critical for high-stakes financial and logistics systems where duplicate orders or missed events can cause significant business impact.\n\n## Key Concepts\n- Idempotency keys with Redis storage for duplicate prevention\n- Exactly-once streaming using sequence IDs and consumer idempotence\n- Cross-region consistency with multi-DC PostgreSQL and Kafka replication\n- Fault injection testing for partial outages and latency budgets\n- Contract testing for API and streaming interfaces\n\n## Code Example\n```javascript\nasync function testIdempotentCreate(client, orderPayload, idempotencyKey) {\n  const res1 = await client.post('/orders', orderPayload, { \n    headers: { 'Idempotency-Key': idempotencyKey } \n  });\n  const res2 = await client.post('/orders', orderPayload, { \n    headers: { 'Idempotency-Key': idempotencyKey } \n  });\n  assert.equal(res1.status, 201);\n  assert.equal(res2.status, 200);\n  assert.equal(res1.body.id, res2.body.id);\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-7",
        "questionId": "q-1166",
        "question": "You manage a public REST API with versioning: /v1/... and /v2/... Design a practical, automated test plan to validate backward compatibility as v2 introduces a new field (tags) and changes a field type (price from int to decimal). Include concrete test cases, OpenAPI contract checks, cross-version schema validation, and how to verify deprecation behavior via a warnings header and 429s for old clients. Outline tooling and steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Explain how you would verify idempotent behavior for POST /orders with Idempotency-Key, ensuring exactly-once order creation despite repeated requests at high concurrency; validate DLQ routing and bac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a practical plan to verify backward compatibility between v1 and v2 endpoints. Include concrete test cases for legacy clients calling v2, schema validation of both versions, OpenAPI contract",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a test harness that issues concurrent POST /telemetry/bulk calls with controlled idempotency keys, each carrying a monotonic sequence number. Validate no duplicates arrive in the downstream store, all payloads ar…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test the GET /users/{id} endpoint with three key scenarios: valid ID (expect 200 status with correct user data), invalid ID (expect 404 status), and malformed requests (expect 400 status). Verify response structure, dat…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical understanding of API versioning, contract testing, and automated validation for real-world back-compat scenarios. It emphasizes concrete test cases, tooling, and CI integration.\n\n## Key Concepts\n- Contract testing across versions\n- Schema validation for v1 and v2\n- Deprecation signaling and rate-limiting for old clients\n- CI automation and stable fixtures\n\n## Code Example\n```javascript\n// Example: basic schema validation with Ajv for v1 vs v2\nconst Ajv = require('ajv');\nconst ajv = new Ajv();\nconst v1 = { type: 'object', properties: { id: {type:'string'}, price: {type:'number'} }, required: ['id','price'] };\nconst v2 = { type: 'object', properties: { id: {type:'string'}, price: {type:'number'}, tags: {type:'array', items:{type:'string'}} }, required: ['id','price'] };\nconsole.log(ajv.validate(v1, {id:'a', price:9}) );\nconsole.log(ajv.validate(v2, {id:'a', price:9, tags:['new']} ));\n```\n\n## Follow-up Questions\n- How would you phase in v2 while keeping v1 fully functional?\n- How would you automate schema drift alerts in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-8",
        "questionId": "q-10524",
        "question": "Design a practical test plan for a multi-tenant user-lookup API GET /tenants/{tenantId}/users/{userId} that enforces per-tenant RBAC/ABAC, uses a Redis cache for access decisions, and propagates policy changes from an Admin API within seconds. Include test cases for tenant isolation, dynamic policy updates, cache invalidation, downstream DB outages, and audit logging. Provide tooling and success criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a plan: for each tenant seed isolated data; use a mock feature-flag service to toggle per-tenant flags that alter the response payload schema. Validate data isolation by attempting cross-tenan",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan end-to-end tests for a partner webhook endpoint /webhooks/payments using HMAC-SHA256 with rotating keys. Verify idempotency with eventId, dedupe concurrent posts, simulate key rotation with zero",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "End-to-end tests ensure REST snapshots for GET /prices/{symbol} align with the latest WebSocket tick within one tick tolerance. Use consumer-driven contracts (Pact) between REST and WebSocket clients; simulate upstream…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan should verify: 1) tenant isolation for GET /tenants/{tid}/users/{uid} with valid/invalid tokens; 2) policy propagation from Admin API within seconds; 3) Redis cache invalidation and TTL behavior",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for multi-tenant access control require validating isolation, policy latency, and resilience under failure. This question probes practical thinking about RBAC/ABAC, caching, and auditability in production-like conditions.\n\n## Key Concepts\n\n- Multi-tenant isolation and ABAC/RBAC enforcement\n- Cache invalidation strategies and TTLs (Redis)\n- Admin API driven policy propagation latency\n- Failure handling: DB outages, cache misses, race conditions\n- Audit logging and traceability\n\n## Code Example\n\n```javascript\n// Pseudo-test: measure policy propagation latency after Admin API update\nasync function testPolicyPropagationLatency(api, adminApi, token) {\n  const t0 = Date.now();\n  await api.get(`/tenants/42/users/7`, { headers: { Authorization: `Bearer ${token}` } });\n  const t1 = Date.now();\n  const latency = t1 - t0;\n  console.log('propagation latency (ms):', latency);\n}\n```\n\n## Follow-up Questions\n\n- How would you measure cross-region propagation latency for policy changes?\n- How would you validate that policy changes cannot be bypassed via token tampering or cache poisoning?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-9",
        "questionId": "q-7665",
        "question": "Design an end-to-end API test strategy for a multi-region order service where POST /orders creates an order and emits events to downstream services via Kafka. The plan should cover idempotency, out-of-order event handling, cross-region data consistency, and failure modes under chaos engineering (network partitions, partial outages, retries with backoff). Include concrete test cases, tooling, and observability requirements?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a plan using a WebSocket tester (50 tenants) to subscribe to price_update and status_change, verify each message contains tenant_id and correct event type, measure 95th percentile latency unde",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline an end-to-end test plan focusing on idempotent /orders POST, deduplication of downstream events, cross-region consistency, and replay-safe state. Use a harness to induce region faults, validat",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Explain a practical end-to-end test plan for POST /orders that triggers a multi-service saga across inventory, payment, and shipping with asynchronous processing. Show how you'd verify idempotency, ex",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Verify 202 and trackingId on first call; simulate duplicates with the same payload and Idempotency-Key to ensure no duplicates in downstream stores; validate per-partition ordering by including a part",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests expertise in distributed API reliability, multi-region data consistency, and chaos engineering practices for production-like environments.\n\n## Key Concepts\n\n- End-to-end tests across regions\n- Idempotency keys, deduplication, exactly-once vs at-least-once\n- Kafka semantics: offsets, retries, replay protection\n- Chaos engineering: fault injection, partitions, partial outages\n- Observability: traces, metrics, logs, service mesh signals\n\n## Code Example\n\n```javascript\n// Pseudo-test harness for idempotent order creation and event replay\nasync function testIdempotentPostOrder(orderPayload) {\n  const id = await postOrder(orderPayload);\n  // retry with same payload\n  const id2 = await postOrder(orderPayload);\n  if (id !== id2) throw new Error('Idempotency key not preserved');\n  const state = await getOrder(id);\n  if (!state || state.status !== 'CONFIRMED') throw new Error('Order not final');\n}\n```\n\n## Follow-up Questions\n\n- How would you validate idempotency across retries and regions in CI/CD?\n- How would you test replay protection for downstream events across services and ensure no drift across regions?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-10",
        "questionId": "q-4738",
        "question": "You're adding a multi-tenant API that returns a paginated user list for a tenant via GET /tenants/{tenantId}/users. A separate POST /tenants/{tenantId}/users/refresh queues a refresh job in a downstream service to recompute each user's complianceScore. Design a concrete end-to-end test plan to validate correctness, idempotency of refresh, tenant isolation, cache invalidation latency, and failure handling when the downstream service is slow or unavailable. Include concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan a beginner-friendly contract test for a versioned API: GET /v1/items and GET /v2/items, where v2 adds a 'discount' field. Validate Content-Type, HTTP 200, and that v1 lacks 'discount' while v2 in",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: verify streaming contract: JSON payload schema, sequence order, timestamps, replay protection; burst test for per-tenant isolation; latency checks 95th percentile; simulate downstream outage to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Initiate a refresh for tenant A and verify only A’s cache is rewritten; tenant B must remain unchanged. Send multiple identical refresh requests to prove idempotency. Query GET /tenants/A/users to con",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose layered testing: unit tests for resolvers with mocked batch loaders to catch N+1; integration tests against staging with tenant tokens validating access control and data masking; contract test",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end data freshness across REST and async refresh, per-tenant isolation, and resilience to downstream slowdowns.\n\n## Key Concepts\n\n- End-to-end flow across REST and queue\n- Idempotency and race conditions\n- Cache invalidation timing and SLA\n- Downstream failure simulation and observability\n\n## Code Example\n\n```javascript\n// Example test sketch (pseudo)\nconst res1 = await callRefresh('tenantA');\nconst res2 = await callRefresh('tenantA'); // idempotent\nawait waitForCacheUpdate('tenantA');\nconst users = await getUsers('tenantA');\nexpect(users.find(u=>u.id==='target').complianceScore).toBeGreaterThan(0);\n```\n\n## Follow-up Questions\n\n- How would you test cross-region consistency?\n- How would you validate metrics and alerts for cache stampedes?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-11",
        "questionId": "q-26403",
        "question": "Design a test plan for a REST PATCH /resources/{id} that uses an Idempotency-Key header to guarantee exactly-once updates under high concurrency. Include concrete test cases for replay protection, clock skew, partial failures (node crash, network drop), cross-region consistency, and how you'd validate with tooling (k6, Postman, Redis)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test GET /articles with Accept: application/json and Accept: application/vnd.company+json; validate each payload against its schema, verify Content-Type matches, and expect 406 for unsupported Accept",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Idempotent submissions via file hash; ramp up to 100 concurrent jobs; verify SSE progress ordering; simulate transcoder crash to validate retries, backoff, and DLQ; assert final artifact URL/metadata",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run parallel requests with the same Idempotency-Key to confirm a single mutation; ensure replay returns 200 with unchanged state and no duplicate side effects; simulate clock skew across nodes, crash",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Approach: seed a deterministic set of idempotent events to POST /events, verify Redis-backed GET /analytics?window=1m reflects exact aggregates per window, and confirm Kafka offsets align with exactly",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests mastery of idempotent APIs, distributed state, and failure handling under load. It reveals how the candidate reasoned about exactly-once semantics and cross-region consistency.\n\n## Key Concepts\n\n- Idempotency keys and exactly-once updates\n- Distributed state storage (e.g., Redis) and TTLs\n- Concurrency testing, replay handling, and clock skew\n- Partial failures and cross-region validation\n- Tooling: k6, Postman, Redis mock\n\n## Code Example\n\n```javascript\n// Pseudo-test: replay should not mutate state\nimport http from 'k6/http';\nexport default function () {\n  http.patch('https://api.example.com/resources/1', { name: 'Alice' }, {\n    headers: { 'Idempotency-Key': 'abc-123' }\n  });\n}\n```\n\n## Follow-up Questions\n\n- How would you monitor idempotency store health and TTL cleanup?\n- How would you extend tests for multiple resources sharing the same key?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-12",
        "questionId": "q-27492",
        "question": "Design a testing plan for a payments API that uses idempotency keys to guarantee exactly-once processing across retries. Include how you'd simulate duplicate webhook events, verify idempotency across multi-region failover, handle key rotation edge-cases, and ensure retries never duplicate charges. Provide concrete test cases, tooling, and clear acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive test plan for POST /orders with Idempotency-Key header validation: 1. **First Request Creates Order**: Send an initial request with the Idempotency-Key header; verify a 201 Created response is ret…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement idempotency via a write-ahead dedup table keyed by idempotency_key (resource_id, status, amount, charge_id). Retries return cached result unless the original failed irrecoverably. Test by re",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test a batch import API with idempotency keys to enforce at-least-once processing and per-record status. Scenario: submit 10k rows; simulate downstream failure on a subset; re-submit with the same Ide",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a streaming API test plan using consumer-driven contracts via a schema registry. Validate backward compatibility by simulating added/removed fields with defaults, ensure chunk boundaries are pr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests around idempotency and webhook replay are critical for payments platforms. This question probes coverage of end-to-end flow, stateful guarantees, multi-region considerations, and operational runtimes (rotation, outages).\n\n## Key Concepts\n\n- Idempotency keys\n- Deduplication table with transactional guarantees\n- Webhook replay safety\n- Cross-region consistency and key rotation\n- Fault injection and test tooling\n\n## Code Example\n\n```javascript\n// Pseudo implementation sketch for idempotency handling\nasync function handleCharge(event) {\n  const key = event.idempotency_key;\n  const existing = await db.findIdempotent(key);\n  if (existing) return existing.response;\n  const result = await chargeProvider.charge(event);\n  await db.upsertIdempotent({ key, response: result, status: result.success ? 'completed' : 'failed' });\n  return result;\n}\n```\n\n## Follow-up Questions\n\n- How would you test idempotency under eventual consistency of reads? \n- How would you design metrics and alerts for idempotency-related failures?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-13",
        "questionId": "q-21756",
        "question": "Design an end-to-end test plan for a multi-tenant WebSocket live-feed API. Validate at-least-once delivery using messageId dedupe, per-tenant isolation (no cross-talk), reconnect handling (auto-reconnect with backoff), and backpressure (pause/resume). Simulate regional broker outages and verify resubscribe and replay of missed events. Use k6/ws, a per-tenant seed in Redis, and define SLA: p95 latency < 400ms?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Model 3 tenants in parallel, generate unique messageIds to verify deduplication and at-least-once delivery behavior at scale. Instrument per-tenant queues to confirm zero cross-tenant data leakage. Test client reconnect…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Outline a plan that covers request/schema validation, positive/negative card scenarios, idempotency on repeated requests, 3DS fallback, and webhook retry handling. Include data models, expected status",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan should validate JSON schemas for both modes, confirm internal fields appear only when authenticated and debug is on, run parallel requests with mixed ?debug values to detect leakage, and verify fallback behavior wh…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an elevated concurrency test: submit N parallel POST /orders calls with the same idempotency-key; verify only a single order is created in Postgres, a single Kafka event emitted, and the Redis cac",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the candidate's ability to design complex end-to-end tests for a streaming API with multi-tenancy, delivery guarantees, and resilience under failure conditions.\n\n## Key Concepts\n- WebSocket streaming with per-tenant isolation and backpressure control\n- Message deduplication via unique messageIds for at-least-once delivery\n- Reconnection logic with exponential backoff and replay semantics\n- Fault injection for broker failover and regional outages\n- Latency and throughput SLA validation\n\n## Code Example\n```javascript\nconst WebSocket = require('ws');\nconst { Kafka } = require('kafkajs');\nconst Redis = require('ioredis');\n\nconst TENANTS = ['tenant-a', 'tenant-b', 'tenant-c'];\nconst SLA_P95_LATENCY_MS = 400;\nconst MAX_RECONNECT_ATTEMPTS = 10;\nconst BASE_BACKOFF_MS = 100;\n\nclass MultiTenantTest {\n  constructor() {\n    this.redis = new Redis();\n    this.connections = new Map();\n    this.messageIds = new Map();\n    this.latencies = [];\n  }\n\n  async setup() {\n    await this.redis.flushdb();\n    for (const tenant of TENANTS) {\n      await this.redis.set(`seed:${tenant}`, Math.random().toString(36));\n    }\n  }\n\n  async connectTenant(tenant) {\n    const ws = new WebSocket('wss://api.example.com/feed');\n    const clientId = `${tenant}-${Date.now()}`;\n    \n    ws.on('open', () => {\n      ws.send(JSON.stringify({ type: 'subscribe', tenant, clientId }));\n    });\n\n    ws.on('message', (data) => {\n      const msg = JSON.parse(data);\n      if (msg.tenant !== tenant) {\n        throw new Error(`CROSS-TENANT LEAK: ${tenant} received ${msg.tenant} data`);\n      }\n      const latency = Date.now() - msg.timestamp;\n      this.latencies.push(latency);\n      this.trackMessageId(msg.messageId, tenant);\n    });\n\n    this.connections.set(tenant, ws);\n    return ws;\n  }\n\n  trackMessageId(messageId, tenant) {\n    if (this.messageIds.has(messageId)) {\n      const existing = this.messageIds.get(messageId);\n      console.log(`DUPLICATE: ${messageId} from ${existing} to ${tenant}`);\n    }\n    this.messageIds.set(messageId, tenant);\n  }\n\n  async simulateBackpressure(tenant) {\n    const ws = this.connections.get(tenant);\n    ws.send(JSON.stringify({ type: 'pause' }));\n  }\n\n  async resumeFeed(tenant) {\n    const ws = this.connections.get(tenant);\n    ws.send(JSON.stringify({ type: 'resume' }));\n  }\n\n  async simulateBrokerOutage(region) {\n    console.log(`Simulating broker outage in ${region}`);\n  }\n\n  calculatePercentile(arr, p) {\n    const sorted = arr.slice().sort((a, b) => a - b);\n    const idx = Math.ceil((p / 100) * sorted.length) - 1;\n    return sorted[Math.max(0, idx)];\n  }\n\n  async validateSLA() {\n    const p95 = this.calculatePercentile(this.latencies, 95);\n    console.log(`P95 Latency: ${p95}ms`);\n    if (p95 > SLA_P95_LATENCY_MS) {\n      throw new Error(`SLA VIOLATION: p95 ${p95}ms exceeds ${SLA_P95_LATENCY_MS}ms`);\n    }\n  }\n}\n```\n\nThis test framework validates all key requirements: per-tenant isolation, at-least-once delivery through messageId deduplication, backpressure handling, and latency SLAs under simulated failure conditions.",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-14",
        "questionId": "q-19949",
        "question": "Design a practical end-to-end testing plan for a high-volume trading API with POST /orders and a real-time fill WebSocket stream. Include per-tenant isolation, idempotent order submissions via Idempotency-Key, signed webhooks with replay protection, and WebSocket backpressure handling. Provide concrete test cases, tooling, and acceptance criteria, plus failure scenarios (gateway downtime, webhook outages) and latency targets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline: send two identical requests to /payments with same Idempotency-Key and payload; verify only one charge is created and response bodies share same transactionId; test missing fields and invalid",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement idempotency tests by replaying a POST /orders with the same Idempotency-Key across transient faults and ensure only one order is created per tenant. Validate webhook replay-safe by signing p",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To test the /events/stream endpoint, implement an automated SSE client that validates: 1) Content-Type header is correctly set to text/event-stream; 2) events are delivered sequentially with no gaps or duplicates within…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test the GET /users/{id} endpoint with three key scenarios: valid ID (expect 200 status with correct user data), invalid ID (expect 404 status), and malformed requests (expect 400 status). Verify response structure, dat…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nIn trading APIs, correctness under retries, webhooks, and streaming reliability are critical; many teams fail due to subtle idempotency/replay bugs.\n\n## Key Concepts\n- Idempotency and exactly-once semantics\n- Webhook replay protection\n- Streaming with WebSocket and backpressure\n- Tenant isolation and latency budgets\n- Fault injection and chaos testing\n\n## Code Example\n```javascript\n// Pseudo test snippet for idempotent order submit\nconst key = 'abc123';\nconst resp1 = await api.post('/orders', {symbol:'AAPL', qty:1}, {headers:{'Idempotency-Key': key}});\nconst resp2 = await api.post('/orders', {symbol:'AAPL', qty:1}, {headers:{'Idempotency-Key': key}});\nconsole.assert(resp2.status === 200 && resp2.body.id === resp1.body.id);\n```\n\n## Follow-up Questions\n- How would you verify exactly-once behavior under partial outages?\n- How would you test webhook signing and nonce replay windows?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-15",
        "questionId": "q-5743",
        "question": "Design a testing plan for a REST API /payments that orchestrates a multi-service distributed transaction (fraud check, bank transfer, ledger). The API returns 202 with a transactionId and a status polling endpoint /payments/{id}/status. Include idempotency handling, partial-failure compensation, eventual consistency verification, and retry/backoff strategies with concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an idempotency key per /payments call to prevent duplicates; mock downstreams (fraud, bank, ledger) and verify a single transaction and consistent ledger for repeated requests with the same key. S",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a practical plan to verify backward compatibility between v1 and v2 endpoints. Include concrete test cases for legacy clients calling v2, schema validation of both versions, OpenAPI contract",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: create a streaming API test that validates per-tenant isolation, message order, and replay handling. Use synthetic tenants with isolated streams, drive concurrency with load tests, inject ne",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement idempotent POST /features using an idempotency-key header to ensure upsert semantics, where repeated requests return HTTP 200 without duplicate data ingestion. Validate eventual read consistency by confirming…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes end-to-end testing of a distributed transaction orchestrator, focusing on idempotency, compensation, and eventual consistency—critical in payments-like flows.\n\n## Key Concepts\n- Idempotency keys and deduplication\n- Distributed orchestration and compensating actions\n- Partial-failure handling and rollback paths\n- End-to-end vs contract tests with downstream services\n- Observability and deterministic test data\n\n## Code Example\n```javascript\n// Example test skeleton for Node.js (mocha)\ndescribe('POST /payments idempotent flow', () => {\n  it('preserves a single transaction for repeated calls with same idempotency key', async () => {\n    const key = 'idem-123';\n    await postPayment({amount: 100}, key);\n    await postPayment({amount: 100}, key);\n    const txs = await listPaymentsByKey(key);\n    assert.strictEqual(txs.length, 1);\n  });\n});\n```\n\n## Follow-up Questions\n- How would you simulate a bank service outage during the transaction and verify idempotence?\n- How would you validate that compensation runs only when partial failure occurs?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-16",
        "questionId": "q-25080",
        "question": "You operate a real-time, per-tenant WebSocket order-stream API. Design a practical end-to-end test plan that validates per-tenant data isolation, message ordering, and resilience under failures while toggling streaming via admin API. Include concrete test cases, tooling, and acceptance criteria for latency, throughput, and correctness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an end-to-end test for POST /payments/charge with Idempotency-Key. Validate duplicate submissions reuse same chargeId, no double debiting, and idempotent retry behavior. Simulate bank outage",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Connect to wss://api.example.com/ws/{tenant}/orders; verify initial snapshot, push 1000 updates concurrently, and assert per-tenant messages arrive in order with no duplicates and latency under 200 ms",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan focuses on idempotent export IDs, paged data integrity, and delta exports. Test cases: 1) retry with same export_id returns identical pages; 2) since-based delta yields correct additions/removals",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test the POST /orders endpoint using the X-Customer-ID header to enforce per-customer data isolation. Validate all required fields and confirm the API returns 400 Bad Request for invalid or incomplete payloads. Verify i…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to validate real-time streaming APIs under multi-tenant constraints, including data isolation, ordering guarantees, and resilience to failures. It also covers dynamic feature flags via an admin API, which is common in production environments.\n\n## Key Concepts\n\n- WebSocket streaming and per-tenant scoping\n- Message ordering and deduplication in a high-throughput channel\n- Reconnection/backoff logic and backpressure handling\n- Dynamic feature flags via admin API and their impact on clients\n- Fault injection: broker outages, network partitions, and fallback behavior\n\n## Code Example\n\n```javascript\n// Minimal WebSocket test scaffold (conceptual)\nconst WebSocket = require('ws');\nconst ws = new WebSocket(`wss://api.example.com/ws/tenantA/orders`);\nlet lastTs = 0;\nws.on('message', (data) => {\n  const msg = JSON.parse(data);\n  // verify monotonic timestamp and non-duplication\n  if (msg.ts <= lastTs) throw new Error('Out-of-order or duplicate message');\n  lastTs = msg.ts;\n});\n```\n\n## Follow-up Questions\n\n- How would you simulate clock skew or message replays to test idempotency and replay protection?\n- How would you scale these tests to verify behavior across dozens of tenants concurrently while keeping test isolation?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-17",
        "questionId": "q-10548",
        "question": "Design an API test plan for a data-export service: POST /exports creates a high-volume export job; GET /exports/{id}/status reports progress; GET /exports/{id}/download retrieves the result. Include tests for idempotent POST via idempotency-key, high-concurrency submissions, webhook callbacks with retry/backoff, partial network failures, and data-integrity checks (checksum) under burst loads. Provide tooling and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An effective plan should verify per-tenant data masking with explicit assertions and no cross-tenant leakage; use ETag/If-None-Match for optimistic concurrency during updates; test Content Negotiation",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan should validate idempotency by resubmitting the same idempotency-key and asserting a single export is created; simulate 10k concurrent POSTs to measure throughput and ensure duplicates aren’",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Model 3 tenants in parallel, generate unique messageIds to verify deduplication and at-least-once delivery behavior at scale. Instrument per-tenant queues to confirm zero cross-tenant data leakage. Test client reconnect…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use end-to-end tests with Postman/Newman or REST-assured. Confirm 200 JSON with fields id,title,price; 406 when Accept is application/xml; verify localized titles/descriptions for fr and es under Acce",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests must cover asynchronous long-running tasks, replay-safe submissions, and reliable completion via callbacks. This ensures data integrity and resilience in real-world burst scenarios.\n\n## Key Concepts\n- Idempotency and duplicate suppression\n- Concurrency and throughput under burst\n- Webhook reliability and retry/backoff strategies\n- Data integrity checks for large exports\n\n## Code Example\n```javascript\n// Example: idempotent POST test skeleton\nconst primaryKey = 'export-key-123';\nconst res1 = await post('/exports', { options }, { Idempotency-Key: primaryKey });\nconst res2 = await post('/exports', { options }, { Idempotency-Key: primaryKey });\nassert(res1.id === res2.id);\n```\n\n## Follow-up Questions\n- How would you model webhook failure scenarios and verify idempotency across retries?\n- How would you ensure monotonic progress and guard against race conditions when multiple clients poll /exports/{id}/status?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-18",
        "questionId": "q-4366",
        "question": "You have REST endpoint POST /payments/events that ingests payment events and guarantees exactly-once processing using idempotency keys and a transactional outbox that publishes to Kafka. Design an automated test plan with at least 6 concrete test cases (duplicate events, concurrent retries, idempotency key reuse, broker outages, replay safety), including tooling and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a comprehensive multi-tenant streaming test framework with HTTP/2 SSE as the primary protocol and WebSocket as the fallback mechanism. Validate stream initialization by confirming 200 OK responses with appropriate C…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use end-to-end tests with Postman/Newman or REST-assured. Confirm 200 JSON with fields id,title,price; 406 when Accept is application/xml; verify localized titles/descriptions for fr and es under Acce",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a comprehensive idempotent test plan: verify that identical X-Idempotency-Key values result in a single shipment record, even under retries and concurrent requests. **Test Scenarios:** 1. **Basic Idempotency**…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test plan should verify end-to-end exactly-once processing via idempotency keys and a transactional outbox. Include: duplicate event with same idempotency_key yields one charge and one outbox entry; h",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests in asynchronous event pipelines with exactly-once guarantees are challenging. This checks understanding of idempotency, the outbox pattern, and end-to-end test coverage across DB and messaging systems.\n\n## Key Concepts\n- End-to-end testing of asynchronous pipelines\n- Idempotency keys and replay safety\n- Outbox pattern and broker failure modes\n- Observability and deterministic assertions\n\n## Code Example\n```javascript\n// Pseudo-test skeleton illustrating test name and assertions\nasync function testDuplicateEventIdempotent() {\n  const payload = { event_id: \"evt-1\", idempotency_key: \"ik-123\", type: \"charge.succeeded\", amount: 1000 };\n  await sendEvent(payload);\n  await sendEvent(payload); // duplicate\n  const charges = await db.query(\"SELECT COUNT(*) FROM charges WHERE event_id='evt-1'\");\n  // expect 1\n}\n```\n\n## Follow-up Questions\n- How would you simulate broker outages in integration tests?\n- How do you validate exactly-once when downstream services are slow?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-19",
        "questionId": "q-3743",
        "question": "Scenario: A public API exposes **POST /webhooks/{provider}** to receive events from external providers. Webhooks may be retried with exponential backoff, and the service must deduplicate using an **Idempotency-Key** header. Design a beginner-friendly test plan that validates **signature verification**, idempotent processing, retry/backoff behavior, and resilience to datastore outages. Include concrete cases and tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan for a gateway using a central policy service for per-tenant routing and canaries. Validate policy consistency across nodes within seconds, simulate concurrent policy reloads to expose race c",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test the POST /orders endpoint using the X-Customer-ID header to enforce per-customer data isolation. Validate all required fields and confirm the API returns 400 Bad Request for invalid or incomplete payloads. Verify i…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test POST /webhooks/{provider}: verify signature with HMAC, ensure Idempotency-Key guarantees at-most-once processing, simulate exponential-backoff retries, check 2xx for valid payloads, 429 on rate l",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose an automated end-to-end plan: validate OpenAPI contracts with Pact; generate synthetic tenants and users; test cursor-based pagination edge cases; verify streaming delivers at-least-once updat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess foundational webhook testing skills: security, idempotency, and resilience in the face of downstream failures.\n\n## Key Concepts\n\n- Webhook security (signature verification)\n- Idempotent processing with Idempotency-Key\n- Retry/backoff semantics and 429 handling\n- Fault tolerance with datastore outages\n\n## Code Example\n\n```javascript\n// Example webhook handler skeleton\napp.post('/webhooks/:provider', (req, res) => {\n  const key = req.headers['idempotency-key'];\n  // deduplicate, verify signature, process once\n  res.status(200).send('ok');\n});\n```\n\n## Follow-up Questions\n\n- How would you test replay attacks and clock drift?\n- How would you simulate provider retries and ensure ordering?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-20",
        "questionId": "q-5731",
        "question": "Design a pragmatic test plan for a GraphQL API that serves a multi-tenant analytics platform. The API supports complex nested queries, per-tenant authorization, persisted queries, and a streaming WebSocket feed for real-time updates. Provide concrete test cases for query depth limits, field-level access control, error handling, rate limiting, token refresh, caching behavior, and resilience when downstream services fail. Include tooling suggestions and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Test Plan: POST /upload Endpoint ### Overview This test plan covers the primary acceptance criteria and security validations for a multipart/form-data file upload endpoint with per-tenant isolation. --- ### 1. Valid…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan for POST /webhooks/payment: verify HMAC-SHA256 signature with shared secret; enforce a 5-minute timestamp window; require idempotency-key to dedupe. Create cases: valid signature; invalid si",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a concrete GraphQL test plan: validate tenant isolation via token scopes and resource filtering; enforce maximum query depth (e.g., 8 levels) and field whitelisting for persisted queries; test",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Test pagination comprehensively using parameterized tests for multiple page sizes, validate data consistency across pages, implement concurrent load testing, and verify error handling for invalid inputs.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess knowledge of GraphQL testing, multi-tenant security, complexity control, persisted queries, streaming endpoints, and resilience. It challenges to design end-to-end plan, tooling choices, and concrete test cases across auth, rate limits, caching, and failure modes.\n\n## Key Concepts\n\n- GraphQL query complexity and depth limits\n- Tenant isolation and field-level authorization\n- Persisted queries and query whitelisting\n- API gateway, rate limiting and retries\n- Testing streaming WebSocket with real-time data and fallback\n- End-to-end test data mocking and downstream failure simulation\n\n## Code Example\n\n```javascript\n// Example test outline (pseudo)\ndescribe(\"GraphQL API tests\", () => {\n  it(\"valid simple query returns expected fields\", async () => {\n    // ...\n  });\n  it(\"depth-limited nested query is rejected\", async () => {\n    // ...\n  });\n});\n```\n\n## Follow-up Questions\n\n- How would you simulate a malicious query that attempts to bypass authorization?\n- How would you verify persisted queries are truly whitelisted and not tunable at runtime?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-21",
        "questionId": "q-7403",
        "question": "Design a beginner-friendly test plan for GET /accounts/{accountId} with a per-request ?debug=true query param and a global /config toggle that adds internalNotes and auditTrail to the response, covering schema checks, auth leakage prevention, parallel mixed-debug requests, and fallback behavior when /config is down?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan should validate JSON schemas for both modes, confirm internal fields appear only when authenticated and debug is on, run parallel requests with mixed ?debug values to detect leakage, and verify fallback behavior wh…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Connect to wss://api.example.com/ws/{tenant}/orders; verify initial snapshot, push 1000 updates concurrently, and assert per-tenant messages arrive in order with no duplicates and latency under 200 ms",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a plan that covers request/schema validation, positive/negative card scenarios, idempotency on repeated requests, 3DS fallback, and webhook retry handling. Include data models, expected status",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Test Plan: GET /reports/{tenantId} ### 1. Functional Tests #### Input Validation (tenantId formats) | Test ID | Test Description | Input | Expected Result | |---------|------------------|-------|-----------------| |…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests concurrency, conditional fields, auth, and downtime handling; mirrors real-world API risks.\n\n## Key Concepts\n- Conditional fields and feature flags\n- Per-request vs global toggles\n- Concurrency and isolation\n- Downtime resilience and fallback\n- JSON schema validation\n\n## Code Example\n```javascript\n// Example test skeleton using Jest + Supertest\nconst request = require('supertest');\nconst app = require('./app');\n\ntest('accounts with debug false', async () => {\n  const res = await request(app).get('/accounts/1');\n  expect(res.status).toBe(200);\n  expect(res.body).toHave",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-22",
        "questionId": "q-6968",
        "question": "Design an end-to-end test plan for a high-throughput API used for a real-time feature store. The API supports POST /features (ingest), GET /features/{id} (read), and a streaming endpoint /features/stream. Validate idempotency with upsert semantics, ensure eventual consistency within a 0–5s window, and test resilience by injecting downstream outages (Kafka, Redis). Include concrete test cases, tooling, and success criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a distributed load test (e.g., k6) to saturate tenants with concurrent requests, asserting 429 responses with Retry-After. Verify admin-API quota updates propagate across nodes within a bounded wi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement idempotent POST /features using an idempotency-key header to ensure upsert semantics, where repeated requests return HTTP 200 without duplicate data ingestion. Validate eventual read consistency by confirming…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement an asynchronous request batching system with token bucket rate limiting, Hystrix circuit breaker patterns, and OpenTelemetry distributed tracing across test suites.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: test idempotency with the same Idempotency-Key across two rapid requests; expect a single charge and identical transaction_id. Test different keys yield separate charges. Simulate timeouts and r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end behavior across ingestion, storage, and streaming data paths under load, with emphasis on idempotency, eventual consistency windows, and resilience to downstream outages. This reflects real-world production constraints in feature store architectures.\n\n## Key Concepts\n\n- Idempotency and upsert semantics for POST endpoints\n- Eventual consistency windows and read-after-write guarantees\n- Fault injection and resilience testing across messaging systems and caches\n- End-to-end automation combining load testing with API validation\n\n## Code Example\n\n```python\n# example: basic idempotent ingest test\nimport requests\nimport time\nimport uuid\n\ndef test_idempotent_ingest():\n    feature_data = {\"feature_id\": \"test_123\", \"value\": 42}\n    idempotency_key = str(uuid.uuid4())\n    \n    headers = {\"Idempotency-Key\": idempotency_key}\n    \n    # First request should create\n    response1 = requests.post(\"/features\", json=feature_data, headers=headers)\n    assert response1.status_code == 201\n    \n    # Second request with same key should succeed without duplication\n    response2 = requests.post(\"/features\", json=feature_data, headers=headers)\n    assert response2.status_code == 200\n    \n    # Verify only one record exists\n    get_response = requests.get(f\"/features/{feature_data['feature_id']}\")\n    assert get_response.json()[\"value\"] == 42\n```",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-23",
        "questionId": "q-4341",
        "question": "Design an end-to-end test strategy for an asynchronous order placement API: POST /orders with Idempotency-Key, which queues the order to a downstream processor (Kafka) and updates status via GET /orders/{id}/status. Include tests for duplicate submissions, exactly-once processing, DLQ routing on consumer failure, backoff/retry behavior, and end-to-end visibility using tracing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a tiny test harness to hit GET /search?q=foo, simulating 100 requests per 15 minutes per user. Assert 200 with JSON matching the schema; when the limit is exceeded, expect 429 and a Retry-After he",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Test Plan: POST /upload Endpoint ### Overview This test plan covers the primary acceptance criteria and security validations for a multipart/form-data file upload endpoint with per-tenant isolation. --- ### 1. Valid…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Explain how you would verify idempotent behavior for POST /orders with Idempotency-Key, ensuring exactly-once order creation despite repeated requests at high concurrency; validate DLQ routing and bac",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a comprehensive idempotent test plan: verify that identical X-Idempotency-Key values result in a single shipment record, even under retries and concurrent requests. **Test Scenarios:** 1. **Basic Idempotency**…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAsynchronous order processing with idempotency is common in production; testing must prove duplicates don't create multiple orders and failures route to DLQs with retries, plus end-to-end visibility.\n\n## Key Concepts\n- Idempotency keys, exactly-once processing\n- Message queues (Kafka), DLQ design\n- Retry/backoff strategies, consumer failure modes\n- End-to-end tracing and correlation\n\n## Code Example\n```javascript\n// Example test (pseudo)\nconst request = require('supertest');\nconst api = require('supertest')('https://api.example.com');\nconst idKey = 'order-uniq-123';\n\ntest('duplicate submissions do not create duplicates', async () => {\n  const r1 = await api.post('/orders').set('Idempotency-Key', idKey).send({items:[{sku:'A1',qty:1}]});\n  const r2 = await api.post('/orders').set('Idempotency-Key', idKey).send({items:[{sku:'A1',qty:1}]});\n  expect(r1.status).toBe(202);\n  expect(r2.status).toBe(202);\n  // fetch status after processing\n});\n```\n\n## Follow-up Questions\n- How would you validate DLQ contents and retry semantics across multiple partitions?\n- What observability would you add to prove end-to-end visibility across services?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-24",
        "questionId": "q-2187",
        "question": "You add a new endpoint POST /imports/candidates to ingest candidate records from a CSV file for a recruitment platform. The endpoint returns 202 with a jobId and status 'accepted'; processing is asynchronous via a worker queue. Design a beginner-friendly test plan covering input validation (file type/size), idempotency with Idempotency-Key, correct 202 response and jobId, status polling with GET /imports/{jobId}, and failure modes (malformed CSV, partial failure, queue downtime). Include concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a concrete test approach: validate multipart CSV content-type and max 5MB; use Idempotency-Key to prevent duplicates; expect 202 with a numeric jobId; poll GET /imports/{jobId} for progress un",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Test plan: POST /payments with Idempotency-Key to prevent duplicates. Use a mocked external gateway to capture charges. Steps: same key twice -> one charge; different keys -> separate charges; high-co",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Test Plan: POST /payments Idempotency ### Overview This test plan validates idempotency behavior for a payment endpoint accepting `{amount, currency, source, description}` with a required `X-Idempotency-Key` header.…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline: send two identical requests to /payments with same Idempotency-Key and payload; verify only one charge is created and response bodies share same transactionId; test missing fields and invalid",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for async processing and idempotency are common in real systems, but beginners benefit from a concrete pattern that ties API responses to backend behavior.\n\n## Key Concepts\n- Async endpoints return 202 with a jobId and user-visible polling\n- Idempotency via Idempotency-Key to avoid duplicate work\n- Robust input validation for CSV uploads (type, size, schema)\n- End-to-end verification via a status endpoint\n- Clear failure modes and observability\n\n## Code Example\n\n```javascript\n// Example test skeleton\nconst fetch = require('node-fetch');\n\nasync function testImport() {\n  const res1 = await fetch('/imports/candidates', {method: 'POST', headers: {'Content-Type': 'multipart/form-data', 'Idempotency-Key': 'k1'}, body: fileBlob});\n  const res2 = await fetch('/imports/candidates', {method: 'POST', headers: {'Content-Type': 'multipart/form-data', 'Idempotency-Key': 'k1'}, body: fileBlob});\n  // assert both respond 202 and same jobId\n}\n```\n\n## Follow-up Questions\n- How would you simulate queue downtime and verify client-visible retry behavior?\n- How would you extend tests for concurrent submissions with different idempotency keys?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-12T10:11:20.398Z",
    "version": 1
  }
]