[
  {
    "id": "test-api-testing",
    "channelId": "api-testing",
    "channelName": "API Testing",
    "title": "API Testing Knowledge Test",
    "description": "Test your understanding of API Testing with 25 questions.",
    "questions": [
      {
        "id": "api-testing-q-0",
        "questionId": "q-28811",
        "question": "Design an advanced API testing plan for a multi-tenant API gateway that supports dynamic schema stitching and per-tenant field-level permissions across REST and GraphQL endpoints, with daily blue/green schema deployments and mTLS between gateway and downstream services. Include contract testing between gateway and downstream services, schema compatibility checks, feature-flag evaluation, per-tenant isolation assertions, resilience to upstream outages, and end-to-end latency targets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a bulk upsert test suite focusing on JSONL payloads up to 1000 records, validating per-record status, atomic batch vs partial commits, idempotent retries with the same idempotency-key, and ten",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a practical plan: 1) use Pact for REST and GraphQL contracts; 2) run schema compatibility checks for each stitch version against a registry; 3) implement per-tenant visibility tests and access",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "A comprehensive API testing plan for a gateway serving both REST and WebSocket streaming endpoints in a real-time telemetry system should cover the following areas: REST and WebSocket paths, per-tenant isolation, contra…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose end-to-end test plan focusing on exactly-once delivery for streaming /stream and idempotent replay via POST /replay. Use deterministic event IDs, a dedup cache, and a replay window to verify n",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests cross-cutting concerns like dynamic schemas, multi-tenant isolation, and secure, verifiable deployments under real-world ops.\n\n## Key Concepts\n- Contract testing across REST and GraphQL\n- Dynamic schema stitching and compatibility\n- Per-tenant authorization and field-level permissions\n- Blue/Green deployment and canary verification\n- mTLS and token lifecycle\n- Resilience under upstream outages and latency targets\n\n## Code Example\n```javascript\n// Example Pact contract test setup (REST)\n```\n\n## Follow-up Questions\n- How would you detect schema drift breaking existing clients in a blue/green rollout?\n- What metrics and dashboards would you expose for per-tenant SLAs?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-1",
        "questionId": "q-29948",
        "question": "Design a test plan for a streaming inference API that delivers Server-Sent Events for real-time model updates. The endpoint supports per-client backpressure, a heartbeat, and reconnect with idempotent replay of the last event. Provide concrete test cases, metrics, tooling, and failure modes (broker pause, partial delivery, reconnect storms)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: verify streaming contract: JSON payload schema, sequence order, timestamps, replay protection; burst test for per-tenant isolation; latency checks 95th percentile; simulate downstream outage to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design tests for a streaming inference API delivering Server-Sent Events. Validate event order and IDs, heartbeat intervals, client backpressure (pause/resume) and precise replay on reconnect. Use a s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan end-to-end tests for a REST + WebSocket analytics API: verify message ordering with sequence IDs, ensure at-least-once delivery and safe replay, validate reconnection/backoff under network faults",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design and implement a comprehensive end-to-end test strategy for an asynchronous bulk import API where POST /bulk/import accepts CSV uploads and returns a jobId, while GET /bulk/import/{jobId} provides real-time status…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question targets testing of streaming API semantics, backpressure handling, and reconnect safety—areas often under-tested in ML-service pipelines.\n\n## Key Concepts\n\n- Streaming correctness (event order, IDs, dedup)\n- Backpressure and flow control\n- Heartbeat and reconnect semantics\n- Fault injection and observability\n\n## Code Example\n\n```javascript\n// Minimal SSE client example used in tests\nconst EventSource = require('eventsource');\nconst es = new EventSource('https://api.example.com/stream');\nes.onmessage = (e) => console.log(JSON.parse(e.data));\nes.onerror = () => es.close();\n```\n\n## Follow-up Questions\n\n- How would you simulate network partitions to test backpressure behavior?\n- How would you validate replay idempotency across reconnects?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-2",
        "questionId": "q-1744",
        "question": "Design a test for a bulk user import API: POST /imports/users accepts up to 100k records, uses an idempotency-key, and publishes per-record results to a downstream analytics queue. Specify test data, idempotency scenarios, partial downstream failures, retry/backoff, and observability. Include concrete cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Idempotent bulk import test: POST /imports/users with 100k records and an idempotency-key. Re-send with the same key to verify no duplicates. Inject partial downstream analytics failures to validate p",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I’d propose a PyTest-based plan for POST /payments with userId, amount, currency and an Idempotency-Key header. Test cases: 1) first submission returns 201 and an id; 2) same Idempotency-Key returns 2",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test idempotency by resubmitting identical payloads and asserting the same jobId and final status is returned. Verify state transitions flow correctly from PENDING → PROCESSING → COMPLETED (or FAILED) with appropriate t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose end-to-end tests for a high-throughput payment API that uses idempotency-key headers to achieve exactly-once processing. Include cases for replay with same key, out-of-order deliveries, partia",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests a realistic bulk ingest with idempotency, partial failures, and observability in a distributed pipeline.\n\n## Key Concepts\n- Bulk ingestion, idempotency, per-record status\n- Asynchronous downstream processing and retries\n- Observability: metrics, logs, traces\n\n## Code Example\n```javascript\n// Pseudo-test skeleton showing idempotent POST and retry expectations\n```\n\n## Follow-up Questions\n- How would you simulate 100k records efficiently?\n- How would you verify no duplicates on retry?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-3",
        "questionId": "q-24101",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /orders that creates a new order per tenant. Each request must include X-API-KEY identifying the tenant and an Idempotency-Key header to prevent duplicates on retries. Outline concrete test cases to validate idempotency under retries and concurrent submissions, input validation, per-tenant isolation, and proper 429/5xx handling; include tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a test suite using per-user keys, simulate burst and steady requests to GET /accounts/{accountId}/transactions?limit=50, verify 429 with Retry-After when limits exceed (100/h and 5/s), validat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose server-side idempotency using a per-tenant map of Idempotency-Key to response; persist only after a transactional check to avoid duplicates. On repeat, return the same orderId and status. Test",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test idempotency by sending two POST /orders with the same Idempotency-Key and verify only one order is created and the second response reuses the first order_id; verify /orders/{id} status transition",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test plan for GET /weather?city={city} aggregating two providers with 60s cache and 5s per-provider timeouts. Verify: (a) both up returns average temperature; (b) one down still returns from the other",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world idempotency handling, crucial for payment-like APIs.\n\n## Key Concepts\n- Idempotency keys per tenant; per-tenant isolation; race-condition handling; retry semantics; error handling.\n\n## Code Example\n```javascript\n// pseudocode: idempotency guard\nif (exists(tenantId, idempotencyKey)) return cachedResponse;\nbeginTxn();\nif (exists(tenantId, idempotencyKey)) return cachedResponse; // double-check\norder = createOrder(payload);\ncache(tenantId, idempotencyKey, order);\ncommitTxn();\nreturn order;\n```\n\n## Follow-up Questions\n- How would you test with network hiccups causing duplicate Idempotency-Key submissions?\n- How would you monitor and alert on idempotency violations?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-4",
        "questionId": "q-19965",
        "question": "Design a beginner-friendly test plan for a real-time WebSocket endpoint at ws://api.example.com/real-time/updates?tenant_id=<id> that streams per-tenant events (price_update, status_change). Include:\n- a concrete multi-tenant load scenario (e.g., 50 concurrent tenants)\n- tenant isolation checks (no cross-tenant data)\n- latency targets (95th percentile under 200 ms)\n- resiliency tests (reconnect/backoff, in-flight buffering)\n- admin downtime fallback behavior. Suggest tooling and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a plan using a WebSocket tester (50 tenants) to subscribe to price_update and status_change, verify each message contains tenant_id and correct event type, measure 95th percentile latency unde",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use an idempotency key per /payments call to prevent duplicates; mock downstreams (fraud, bank, ledger) and verify a single transaction and consistent ledger for repeated requests with the same key. S",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a test harness that issues concurrent POST /telemetry/bulk calls with controlled idempotency keys, each carrying a monotonic sequence number. Validate no duplicates arrive in the downstream store, all payloads ar…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create per-tenant mock listeners, replay the same eventId to verify idempotency (only one delivery). Inject outages to validate retries and exponential backoff; ensure no cross-tenant leakage. Verify",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for real-time WebSocket flows with multi-tenant isolation, latency, and resiliency are a practical gap for beginners beyond REST basics.\n\n## Key Concepts\n\n- WebSocket lifecycle, framing, and per-tenant data boundaries\n- Concurrency planning and p95 latency measurement\n- Reconnect/backoff strategies and in-flight message handling\n- Admin downtime fallbacks and replay semantics\n- Tooling: k6, Artillery, or ws-based tests\n\n## Code Example\n\n```javascript\n// Example WebSocket client for a tenant\nconst WebSocket = require('ws');\nconst ws = new WebSocket('ws://api.example.com/real-time/updates?tenant_id=tenant1');\nws.on('message', (m) => console.log(JSON.parse(m)));\n```\n\n## Follow-up Questions\n\n- How would you adapt the plan for flaky networks or broker outages?\n- What metrics would you collect to detect cross-tenant data leakage?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-5",
        "questionId": "q-1166",
        "question": "You manage a public REST API with versioning: /v1/... and /v2/... Design a practical, automated test plan to validate backward compatibility as v2 introduces a new field (tags) and changes a field type (price from int to decimal). Include concrete test cases, OpenAPI contract checks, cross-version schema validation, and how to verify deprecation behavior via a warnings header and 429s for old clients. Outline tooling and steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe how you would validate the end-to-end flow of POST /batch/infer, including idempotent batch submissions, webhook signature validation, partial-failure handling with per-record status, and ret",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Provide steps to test: create payload with amount,currency,card fields; compute signature with shared secret; send with headers; verify 403 on bad signature, 401 for missing key; resend same Idempoten",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Focus on idempotent POSTs using Idempotency-Key, ensure retries return the same result, confirm 202 produces a Location for polling and 200/final state, verify /payments/{id} reflects consistent statu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a practical plan to verify backward compatibility between v1 and v2 endpoints. Include concrete test cases for legacy clients calling v2, schema validation of both versions, OpenAPI contract",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical understanding of API versioning, contract testing, and automated validation for real-world back-compat scenarios. It emphasizes concrete test cases, tooling, and CI integration.\n\n## Key Concepts\n- Contract testing across versions\n- Schema validation for v1 and v2\n- Deprecation signaling and rate-limiting for old clients\n- CI automation and stable fixtures\n\n## Code Example\n```javascript\n// Example: basic schema validation with Ajv for v1 vs v2\nconst Ajv = require('ajv');\nconst ajv = new Ajv();\nconst v1 = { type: 'object', properties: { id: {type:'string'}, price: {type:'number'} }, required: ['id','price'] };\nconst v2 = { type: 'object', properties: { id: {type:'string'}, price: {type:'number'}, tags: {type:'array', items:{type:'string'}} }, required: ['id','price'] };\nconsole.log(ajv.validate(v1, {id:'a', price:9}) );\nconsole.log(ajv.validate(v2, {id:'a', price:9, tags:['new']} ));\n```\n\n## Follow-up Questions\n- How would you phase in v2 while keeping v1 fully functional?\n- How would you automate schema drift alerts in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-6",
        "questionId": "q-22910",
        "question": "Design a beginner-friendly API testing plan for a POST /payments endpoint that processes charges via an external gateway. The endpoint requires an Idempotency-Key header to prevent duplicate charges. Create concrete test cases for idempotency (same key twice yields a single charge), concurrent requests with different keys, error handling (400/401/500), retry strategies with backoff, and latency targets; specify tooling, test data, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan for POST /webhooks/payment: verify HMAC-SHA256 signature with shared secret; enforce a 5-minute timestamp window; require idempotency-key to dedupe. Create cases: valid signature; invalid si",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Test Architecture - Deploy N concurrent SSE clients connecting to /stream/events - Set up a local Kafka cluster with a dedicated test topic - Implement a mock downstream consumer to verify message flow ## Test Cases…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test plan: POST /payments with Idempotency-Key to prevent duplicates. Use a mocked external gateway to capture charges. Steps: same key twice -> one charge; different keys -> separate charges; high-co",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Verify 202 and trackingId on first call; simulate duplicates with the same payload and Idempotency-Key to ensure no duplicates in downstream stores; validate per-partition ordering by including a part",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis checks practical API testing for idempotency, external gateway integration, concurrency, and resilience. It evaluates ability to design end-to-end tests with mocks, realistic data, and clear acceptance criteria.\n\n## Key Concepts\n\n- Idempotent endpoints\n- External gateway mocking\n- Concurrency and race conditions\n- Retry/backoff behavior\n- Latency SLAs\n\n## Code Example\n\n```javascript\n// Pseudo-test: idempotent POST /payments\nconst res1 = post('/payments', { amount: 100 }, { headers: { 'Idempotency-Key': 'abc123' }})\nconst res2 = post('/payments', { amount: 100 }, { headers: { 'Idempotency-Key': 'abc123' }})\nassert(res1.chargeId === res2.chargeId)\n```\n\n## Follow-up Questions\n\n- How would you automate the gateway mock to simulate intermittent failures and timeouts?\n- How would you measure end-to-end latency for the payment path under load?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-7",
        "questionId": "q-17087",
        "question": "Design an advanced API testing plan for a real-time telemetry streaming endpoint /telemetry/stream/{sensorId} that uses Server-Sent Events (SSE) and supports versioned payloads controlled by a tenant-scoped feature flag. Your plan must cover (a) at-least-once delivery under network instability, (b) idempotent handling and Last-Event-ID replay, (c) cross-version compatibility and field evolution across versions, (d) per-tenant isolation and rate-limiting interactions during stream bursts, (e) admin downtime and failover scenarios, (f) end-to-end testing with a mock event bus and real-time dashboards. Include concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive test harness for the SSE streaming endpoint /telemetry/stream/{sensorId} with per-tenant feature flags and versioned payloads. The test plan validates: at-least-once delivery via Last-Event-ID rep…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Test GET /stream/alerts using Server-Sent Events. Verify 200 and Content-Type: text/event-stream. Capture the first N events and confirm each starts with data: and is valid JSON; ensure id values incr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test Plan for REST API POST /checkout with Idempotency-Key: 1. **Happy Path**: Valid cart payload returns 202 with unique jobId 2. **Idempotency**: Same Idempotency-Key returns 200 with identical jobId, no duplicate job…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan tests for a REST API delivering per-tenant feature flags with dynamic rollout and A/B experiments. Verify per-user bucketing via a deterministic hash, check distribution fairness within tolerance",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for streaming APIs present unique challenges: stateful delivery, backpressure handling, and cross-tenant isolation. This question probes practical strategies for SSE streams with versioned payloads and feature flags, focusing on real-world reliability and compatibility.\n\n## Key Concepts\n\n- Server-Sent Events (SSE) streaming fundamentals\n- At-least-once delivery guarantees and Last-Event-ID replay mechanisms\n- Idempotent event processing and deduplication strategies\n- Schema/version evolution and feature flag semantics\n- Multi-tenant isolation and rate-limiting under burst conditions",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-8",
        "questionId": "q-24065",
        "question": "Design a pragmatic API testing plan for a multi-tenant gateway that enforces per-tenant RBAC, dynamic rate limits, and feature flags, with a canary rollout; include concrete test cases for auth, data isolation, rate-limit bursts, flag-driven field visibility, and rollback or canary failover, plus tooling and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate RBAC, per-tenant data isolation, dynamic rate limits, and canary flags in a multi-tenant gateway. Create tenants A/B, mint tokens, and exercise endpoints with cross-tenant access attempts; bu",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Test plan covers: input validation of tenantId/reportId formats; 403 when path tenantId differs from authenticated user; 404 for missing reports; cache behavior: cold fetch hits MongoDB, subsequent hi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose end-to-end latency tests (k6) targeting 95th percentile under peak load with <2s SLA; validate per-tenant isolation by using tenant_id headers and querying with mixed tenants to ensure no cros",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: replay and dedup tests using idempotency keys per tenant; verify cross-tenant isolation by attempting cross-tenant data access. Validate retries with exponential backoff and max retry counts; si",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical, scalable API-testing strategies for multi-tenant gateways with RBAC, rate limiting, and feature flag rollouts, ensuring isolation and resilience.\n\n## Key Concepts\n- RBAC and tenant isolation\n- Dynamic rate limits and burst handling\n- Feature flag visibility and rollback\n- Contract testing with OpenAPI, canaries, observability\n\n## Code Example\n```javascript\n// Pseudo-test skeleton (conceptual)\nfunction testTenantIsolation() { /* create tenants; assert cross-tenant data blocked */ }\n```\n\n## Follow-up Questions\n- How would you validate canary failover under partial outages?\n- What telemetry would you add to diagnose cross-tenant leakage?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-9",
        "questionId": "q-7665",
        "question": "Design an end-to-end API test strategy for a multi-region order service where POST /orders creates an order and emits events to downstream services via Kafka. The plan should cover idempotency, out-of-order event handling, cross-region data consistency, and failure modes under chaos engineering (network partitions, partial outages, retries with backoff). Include concrete test cases, tooling, and observability requirements?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: create a streaming API test that validates per-tenant isolation, message order, and replay handling. Use synthetic tenants with isolated streams, drive concurrency with load tests, inject ne",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement an SSE client-based test suite in Node/Python using EventSource (or httpx), simulate stock deltas across warehouses, verify idempotent snapshots after reconnects, check per-warehouse",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline an end-to-end test plan focusing on idempotent /orders POST, deduplication of downstream events, cross-region consistency, and replay-safe state. Use a harness to induce region faults, validat",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Outline a concrete GraphQL test plan: validate tenant isolation via token scopes and resource filtering; enforce maximum query depth (e.g., 8 levels) and field whitelisting for persisted queries; test",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests expertise in distributed API reliability, multi-region data consistency, and chaos engineering practices for production-like environments.\n\n## Key Concepts\n\n- End-to-end tests across regions\n- Idempotency keys, deduplication, exactly-once vs at-least-once\n- Kafka semantics: offsets, retries, replay protection\n- Chaos engineering: fault injection, partitions, partial outages\n- Observability: traces, metrics, logs, service mesh signals\n\n## Code Example\n\n```javascript\n// Pseudo-test harness for idempotent order creation and event replay\nasync function testIdempotentPostOrder(orderPayload) {\n  const id = await postOrder(orderPayload);\n  // retry with same payload\n  const id2 = await postOrder(orderPayload);\n  if (id !== id2) throw new Error('Idempotency key not preserved');\n  const state = await getOrder(id);\n  if (!state || state.status !== 'CONFIRMED') throw new Error('Order not final');\n}\n```\n\n## Follow-up Questions\n\n- How would you validate idempotency across retries and regions in CI/CD?\n- How would you test replay protection for downstream events across services and ensure no drift across regions?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-10",
        "questionId": "q-27033",
        "question": "Design a beginner-friendly test plan for a REST API endpoint POST /bookings that creates a ride booking in a multi-tenant system. Each request must include a header X-API-KEY identifying the tenant and an idempotency-key. Validate input, per-tenant isolation, idempotency semantics (same key does not create duplicates), 429 Retry-After handling, and successful path. Include concrete test cases, tooling, and CI acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose end-to-end tests for a high-throughput payment API that uses idempotency-key headers to achieve exactly-once processing. Include cases for replay with same key, out-of-order deliveries, partia",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: use an idempotency-key header on POST /orders. First call creates the order and returns 201 with order_id. Subsequent calls with the same key return 200 with the same order_id and no duplicates.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a comprehensive end-to-end test suite that validates the complete payments risk API workflow: ingest payment events via POST /payments, toggle per-tenant enrichment features through the admin API, and verify r…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Test Cases** - **400 Bad Request**: Missing or invalid required fields (e.g., missing pickup_location or invalid passenger_count) - **401 Unauthorized / 403 Forbidden**: Missing or invalid X-API-KEY header; tenant not…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates a candidate's understanding of idempotent POST semantics, per-tenant data isolation, and resilience testing—critical concepts in bookings and payments services.\n\n## Key Concepts\n\n- **Idempotency-key handling** prevents duplicate bookings when clients retry requests due to network failures or timeouts\n- **Per-tenant data isolation** is enforced via X-API-KEY validation, ensuring tenants cannot access each other's data\n- **API test coverage** includes input validation, authentication, rate limiting, and concurrent retry scenarios\n\n## Code Example\n\n```javascript\ndescribe('POST /bookings', () => {\n  it('creates booking with valid idempotency key', async () => {\n    const response1 = await api.post('/bookings', {\n      pickup_location: 'NYC',\n      passenger_count: 2\n    }, { headers: { 'X-API-KEY': 'tenant-a', 'idempotency-key': 'abc123' } });\n    \n    const response2 = await api.post('/bookings', {\n      pickup_location: 'NYC',\n      passenger_count: 2\n    }, { headers: { 'X-API-KEY': 'tenant-a', 'idempotency-key': 'abc123' } });\n\n    expect(response2.status).toBe(200);\n    expect(response2.body.booking_id).toBe(response1.body.booking_id);\n  });\n});\n```",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-11",
        "questionId": "q-17535",
        "question": "Design an end-to-end test plan for an asynchronous job-processing API: submit with POST /jobs (idempotency key), poll with GET /jobs/{jobId}/status, fetch result with GET /jobs/{jobId}/result. Validate at-least-once delivery, dedup on resubmission, DLQs, backoff/retry, and cross-service event-schema evolution (Kafka topics). Include concrete cases, tooling (Pact, k6, Testcontainers), and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a comprehensive idempotent test plan: verify that identical X-Idempotency-Key values result in a single shipment record, even under retries and concurrent requests. **Test Scenarios:** 1. **Basic Idempotency**…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement idempotent webhook processing by persisting provider+event_id and rejecting duplicates; verify HMAC signature first. Test cases: a) duplicate delivery, b) out-of-order events, c) transient d",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## End-to-End Test Plan for Asynchronous Job-Processing API ### API Endpoints - **POST /jobs** — Submit job with idempotency key - **GET /jobs/{jobId}/status** — Poll job status - **GET /jobs/{jobId}/result** — Retrieve…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a comprehensive WebSocket test plan using a test client to simulate multiple tenants on /ws/updates. Validate: (1) JWT-based tenant authentication and isolation, (2) message ordering guarantees per tenant, (3)…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates a candidate's ability to test asynchronous, event-driven systems with idempotency, resilience patterns, and cross-service contracts. It assesses understanding of production-grade reliability and distributed systems testing.\n\n## Key Concepts\n\n- Asynchronous job processing and polling patterns\n- Idempotency keys and deduplication strategies\n- Dead-letter queues (DLQs) for failed processing\n- Retry mechanisms with exponential backoff\n- Schema evolution in event-driven architectures (Kafka)\n- Contract testing and observability\n\n## Code Example\n\n```java\n@Test\nvoid testIdempotency() {\n    String idempotencyKey = UUID.randomUUID().toString();\n    \n    Response first = given()\n        .contentType(\"application/json\")\n        .body(jobPayload)\n        .header(\"Idempotency-Key\", idempotencyKey)\n        .post(\"/jobs\");\n    \n    Response second = given()\n        .contentType(\"application/json\")\n        .body(jobPayload)\n        .header(\"Idempotency-Key\", idempotencyKey)\n        .post(\"/jobs\");\n    \n    assertEquals(first.jsonPath().getString(\"jobId\"), \n                 second.jsonPath().getString(\"jobId\"));\n}\n\n@Test\nvoid testRetryWithBackoff() {\n    String jobId = submitJob();\n    \n    await().atMost(30, TimeUnit.SECONDS)\n           .until(() -> getStatus(jobId).equals(\"DONE\"));\n    \n    verify(retries, times(3));\n    verify(backoff, times(2)); // exponential: 1s, 2s\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-12",
        "questionId": "q-4738",
        "question": "You're adding a multi-tenant API that returns a paginated user list for a tenant via GET /tenants/{tenantId}/users. A separate POST /tenants/{tenantId}/users/refresh queues a refresh job in a downstream service to recompute each user's complianceScore. Design a concrete end-to-end test plan to validate correctness, idempotency of refresh, tenant isolation, cache invalidation latency, and failure handling when the downstream service is slow or unavailable. Include concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Explain a practical end-to-end test plan for POST /orders that triggers a multi-service saga across inventory, payment, and shipping with asynchronous processing. Show how you'd verify idempotency, ex",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Initiate a refresh for tenant A and verify only A’s cache is rewritten; tenant B must remain unchanged. Send multiple identical refresh requests to prove idempotency. Query GET /tenants/A/users to con",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-tenant namespaces, deterministic event IDs, and a strict sequence number ensure order. Test idempotent REST calls, dedup logic on the broker, and at-least-once delivery for both SSE and WebSocket.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: use an idempotency-key header on POST /orders. First call creates the order and returns 201 with order_id. Subsequent calls with the same key return 200 with the same order_id and no duplicates.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end data freshness across REST and async refresh, per-tenant isolation, and resilience to downstream slowdowns.\n\n## Key Concepts\n\n- End-to-end flow across REST and queue\n- Idempotency and race conditions\n- Cache invalidation timing and SLA\n- Downstream failure simulation and observability\n\n## Code Example\n\n```javascript\n// Example test sketch (pseudo)\nconst res1 = await callRefresh('tenantA');\nconst res2 = await callRefresh('tenantA'); // idempotent\nawait waitForCacheUpdate('tenantA');\nconst users = await getUsers('tenantA');\nexpect(users.find(u=>u.id==='target').complianceScore).toBeGreaterThan(0);\n```\n\n## Follow-up Questions\n\n- How would you test cross-region consistency?\n- How would you validate metrics and alerts for cache stampedes?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-13",
        "questionId": "q-21756",
        "question": "Design an end-to-end test plan for a multi-tenant WebSocket live-feed API. Validate at-least-once delivery using messageId dedupe, per-tenant isolation (no cross-talk), reconnect handling (auto-reconnect with backoff), and backpressure (pause/resume). Simulate regional broker outages and verify resubscribe and replay of missed events. Use k6/ws, a per-tenant seed in Redis, and define SLA: p95 latency < 400ms?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Model 3 tenants in parallel, generate unique messageIds to verify deduplication and at-least-once delivery behavior at scale. Instrument per-tenant queues to confirm zero cross-tenant data leakage. Test client reconnect…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Test approach: POST /orders with Idempotency-Key and X-ORG-ID per tenant; verify 201 created with order_id and idempotent retries return the same id without duplication. Simulate pricing service slow/",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use end-to-end tests with Postman/Newman or REST-assured. Confirm 200 JSON with fields id,title,price; 406 when Accept is application/xml; verify localized titles/descriptions for fr and es under Acce",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan focuses on: happy-path full data from both downstreams; partial-fallback 200 with partial_data=true when one downstream fails; per-tenant isolation checks with distinct tokens; 400/401/403 and 42",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the candidate's ability to design complex end-to-end tests for a streaming API with multi-tenancy, delivery guarantees, and resilience under failure conditions.\n\n## Key Concepts\n- WebSocket streaming with per-tenant isolation and backpressure control\n- Message deduplication via unique messageIds for at-least-once delivery\n- Reconnection logic with exponential backoff and replay semantics\n- Fault injection for broker failover and regional outages\n- Latency and throughput SLA validation\n\n## Code Example\n```javascript\nconst WebSocket = require('ws');\nconst { Kafka } = require('kafkajs');\nconst Redis = require('ioredis');\n\nconst TENANTS = ['tenant-a', 'tenant-b', 'tenant-c'];\nconst SLA_P95_LATENCY_MS = 400;\nconst MAX_RECONNECT_ATTEMPTS = 10;\nconst BASE_BACKOFF_MS = 100;\n\nclass MultiTenantTest {\n  constructor() {\n    this.redis = new Redis();\n    this.connections = new Map();\n    this.messageIds = new Map();\n    this.latencies = [];\n  }\n\n  async setup() {\n    await this.redis.flushdb();\n    for (const tenant of TENANTS) {\n      await this.redis.set(`seed:${tenant}`, Math.random().toString(36));\n    }\n  }\n\n  async connectTenant(tenant) {\n    const ws = new WebSocket('wss://api.example.com/feed');\n    const clientId = `${tenant}-${Date.now()}`;\n    \n    ws.on('open', () => {\n      ws.send(JSON.stringify({ type: 'subscribe', tenant, clientId }));\n    });\n\n    ws.on('message', (data) => {\n      const msg = JSON.parse(data);\n      if (msg.tenant !== tenant) {\n        throw new Error(`CROSS-TENANT LEAK: ${tenant} received ${msg.tenant} data`);\n      }\n      const latency = Date.now() - msg.timestamp;\n      this.latencies.push(latency);\n      this.trackMessageId(msg.messageId, tenant);\n    });\n\n    this.connections.set(tenant, ws);\n    return ws;\n  }\n\n  trackMessageId(messageId, tenant) {\n    if (this.messageIds.has(messageId)) {\n      const existing = this.messageIds.get(messageId);\n      console.log(`DUPLICATE: ${messageId} from ${existing} to ${tenant}`);\n    }\n    this.messageIds.set(messageId, tenant);\n  }\n\n  async simulateBackpressure(tenant) {\n    const ws = this.connections.get(tenant);\n    ws.send(JSON.stringify({ type: 'pause' }));\n  }\n\n  async resumeFeed(tenant) {\n    const ws = this.connections.get(tenant);\n    ws.send(JSON.stringify({ type: 'resume' }));\n  }\n\n  async simulateBrokerOutage(region) {\n    console.log(`Simulating broker outage in ${region}`);\n  }\n\n  calculatePercentile(arr, p) {\n    const sorted = arr.slice().sort((a, b) => a - b);\n    const idx = Math.ceil((p / 100) * sorted.length) - 1;\n    return sorted[Math.max(0, idx)];\n  }\n\n  async validateSLA() {\n    const p95 = this.calculatePercentile(this.latencies, 95);\n    console.log(`P95 Latency: ${p95}ms`);\n    if (p95 > SLA_P95_LATENCY_MS) {\n      throw new Error(`SLA VIOLATION: p95 ${p95}ms exceeds ${SLA_P95_LATENCY_MS}ms`);\n    }\n  }\n}\n```\n\nThis test framework validates all key requirements: per-tenant isolation, at-least-once delivery through messageId deduplication, backpressure handling, and latency SLAs under simulated failure conditions.",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-14",
        "questionId": "q-5731",
        "question": "Design a pragmatic test plan for a GraphQL API that serves a multi-tenant analytics platform. The API supports complex nested queries, per-tenant authorization, persisted queries, and a streaming WebSocket feed for real-time updates. Provide concrete test cases for query depth limits, field-level access control, error handling, rate limiting, token refresh, caching behavior, and resilience when downstream services fail. Include tooling suggestions and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Develop an end-to-end test plan for an async /data/process flow: verify idempotent POSTs with Idempotency-Key, check that duplicates yield a single operation, poll results for eventual consistency and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan includes ensuring deterministic per-user flag values across DCs, asserting cache invalidation within 5s after config changes, simulating regional outages and network partitions, and measuring end",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Focus on idempotent POSTs using Idempotency-Key, ensure retries return the same result, confirm 202 produces a Location for polling and 200/final state, verify /payments/{id} reflects consistent statu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a concrete GraphQL test plan: validate tenant isolation via token scopes and resource filtering; enforce maximum query depth (e.g., 8 levels) and field whitelisting for persisted queries; test",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess knowledge of GraphQL testing, multi-tenant security, complexity control, persisted queries, streaming endpoints, and resilience. It challenges to design end-to-end plan, tooling choices, and concrete test cases across auth, rate limits, caching, and failure modes.\n\n## Key Concepts\n\n- GraphQL query complexity and depth limits\n- Tenant isolation and field-level authorization\n- Persisted queries and query whitelisting\n- API gateway, rate limiting and retries\n- Testing streaming WebSocket with real-time data and fallback\n- End-to-end test data mocking and downstream failure simulation\n\n## Code Example\n\n```javascript\n// Example test outline (pseudo)\ndescribe(\"GraphQL API tests\", () => {\n  it(\"valid simple query returns expected fields\", async () => {\n    // ...\n  });\n  it(\"depth-limited nested query is rejected\", async () => {\n    // ...\n  });\n});\n```\n\n## Follow-up Questions\n\n- How would you simulate a malicious query that attempts to bypass authorization?\n- How would you verify persisted queries are truly whitelisted and not tunable at runtime?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-15",
        "questionId": "q-30046",
        "question": "Design a beginner-friendly test plan for POST /jobs that enqueues asynchronous work. The endpoint uses Idempotency-Key for duplicates and returns 202 with Location to GET /jobs/{id}. Include per-tenant isolation, retry/backoff, idempotent duplicate handling, worker timeouts (around 20s), and payload-schema validation. Provide concrete test cases and tooling recommendations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Test Plan: POST /jobs Asynchronous Enqueue ### 1. Happy Path — Single Submission Submit a valid job payload with a unique Idempotency-Key. Verify the response returns HTTP 202 Accepted with a Location header pointing…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement a contract-driven end-to-end suite with Pact between rides, drivers, and payments services, plus chaos testing. Verify idempotency on POST /rides, ensure eventual consistency via rea",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would design a comprehensive end-to-end test plan for multi-tenant field-level masking that systematically validates data protection across various scenarios. First, I'd verify correct masking behavior per tenant usin…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan a beginner-friendly contract test for a versioned API: GET /v1/items and GET /v2/items, where v2 adds a 'discount' field. Validate Content-Type, HTTP 200, and that v1 lacks 'discount' while v2 in",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates understanding of asynchronous API design patterns, idempotency mechanisms, and multi-tenant system testing—foundational skills for building reliable production APIs.\n\n## Key Concepts\n\n- **202 Accepted + Location Header** — Standard pattern for async operations, indicating the request has been accepted for processing but work occurs asynchronously.\n- **Idempotency Keys** — Prevent duplicate job creation when clients retry due to network issues; the server returns the original response for duplicate keys within the TTL window.\n- **Per-Tenant Isolation** — Ensures each tenant's jobs remain logically separated, typically through tenant-scoped queueing or database partitioning.\n- **Exponential Backoff** — Client-side retry strategy that increases delay between attempts to avoid overwhelming the server while giving transient failures time to resolve.\n- **Worker Timeouts** — Prevents jobs from running indefinitely; timed-out jobs should be marked as failed with appropriate error metadata.\n- **Schema Validation** — Catches invalid payloads early with clear, actionable error messages for API consumers.",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-16",
        "questionId": "q-27811",
        "question": "Design a beginner-friendly test plan for a REST API POST /cart/items that adds a product to a cart. Each request must include an Idempotency-Key header. Validate that repeated POSTs with the same key do not create duplicates, that retries after transient 5xx errors reuse the same result, and that different keys create separate items. Include concrete test cases, tooling, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement idempotency using an Idempotency-Key header on POST /orders with a Redis-backed map to prevent duplicate order creation. For the WebSocket stream at /orders/stream, include sequence numbers in events and imple…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use the Idempotency-Key header to guard POST /cart/items. Re-run identical requests with same key and confirm no duplicates; on transient 5xx, retry using same key and compare final item count; ensure",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement an idempotent webhook handler that uses id as the dedupe key, replies 200 on first attempt, and replays without side effects on duplicates. Tests: (1) same id sent twice yields a single proc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To test a read-model based GET /accounts/{accountId}/balance backed by an event stream, verify eventual consistency and idempotency under delays. Use Kafka with testcontainers, inject delays in the co",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes understanding of idempotent POST semantics, deduplication guarantees, and retry behavior. It requires concrete test cases and tooling choices, aligning with real-world API testing needs while staying accessible to beginners.\n\n## Key Concepts\n\n- Idempotency-Key semantics\n- Deduplication and idempotent operations\n- Retry strategies for 5xx and their impact on state\n- Conflict handling (409)\n- Tooling choices (Postman/Newman, pytest, httpx)\n\n## Code Example\n\n```javascript\n// Example test sketch (pseudo)\nasync function testIdempotentPost(client, key) {\n  const payload = { productId: 42, quantity: 1 };\n  const opts = { method: 'POST', headers: { 'Idempotency-Key': key, 'Content-Type': 'application/json' }, body: JSON.stringify(payload) };\n  const r1 = await fetch('/cart/items', opts);\n  const r2 = await fetch('/cart/items', opts);\n  // asserts...\n}\n```\n\n## Follow-up Questions\n\n- How would you test across distributed caching and possible duplicate deliveries?\n- How would you implement a contract test to ensure the idempotent endpoint returns identical payload across retries?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-17",
        "questionId": "q-9706",
        "question": "Design an end-to-end API testing plan for a real-time analytics service where clients POST /events (ingested into a Kafka stream) and GET /analytics?window=1m returns windowed aggregates from a Redis cache backed by the stream. Include how you'd verify exactly-once processing, idempotent event submission, late-arrival data, cache stampede protection, watermarking, and partial downstream warehouse outages. Provide concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: test idempotent POST /orders using a Redis-backed idempotency store TTL 3600s. 1) POST /orders with payload and Idempotency-Key A, expect 201 and orderId. 2) Parallel retry with same key; expect",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: seed a deterministic set of idempotent events to POST /events, verify Redis-backed GET /analytics?window=1m reflects exact aggregates per window, and confirm Kafka offsets align with exactly",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a tenant-aware contract suite around GET /products/{id} validating per-tenant field exposure under concurrent mid-request policy changes. Use Pact for consumer-driven contracts, a policy-change",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Tests should cover idempotent behavior with a shared idempotency-key, verifying first call returns 201 and a job_id while duplicates return the same job_id. Validate retry semantics on transient netwo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses end-to-end testing across streaming ingestion, caching, and downstream failover. Focuses on exactly-once semantics, idempotency, late data handling, cache resilience, and disaster recovery in a realistic real-time analytics workflow.\n\n## Key Concepts\n\n- End-to-end streaming tests across POST, stream, and GET paths\n- Exactly-once delivery and idempotent submissions\n- Watermarking and late-arrival handling\n- Cache stampede protection and eviction policies\n- Partial downstream outages and replay strategies\n\n## Code Example\n\n```javascript\n// Pseudo test harness outline for end-to-end flow\nasync function testEndToEnd() {\n  const events = generateEvents(seed, N);\n  await publishEvents('/events', events);\n  const aggs = await queryAnalytics('/analytics?window=1m');\n  assertAggsEqual(aggs, expected(events));\n}\n```\n\n## Follow-up Questions\n\n- How would you scale tests to 10k events/sec?\n- How would you validate cross-region consistency during failover?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-18",
        "questionId": "q-7371",
        "question": "Design an advanced test plan for a REST API POST /orders that accepts idempotent submissions via an idempotency-key, with multi-region replicas and a separate read-model; verify exactly-once processing, deduplication across retries, latency effects on order status propagation, and schema evolution of the payload; include failure modes (primary failure, network partition), back-pressure, and admin override paths?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate idempotency by replaying the same POST with a single Idempotency-Key and confirm only one order is created and responses match; test different keys create separate orders; simulate out-of-sto",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Key verification points: execute concurrent POST requests with the same idempotency key to confirm only a single charge occurs and the same transaction ID is returned; use distinct keys to verify separate charges are cr…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a plan that enforces exactly-once using a Redis-backed idempotency window and an outbox pattern for durable writes; simulate duplicate submissions under retries to ensure no additional orders a",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan an end-to-end test for a real-time, multi-tenant SaaS API spanning three regions with dynamic per-tenant feature flags. Seed regional shards, drive high-concurrency user lookups, verify per-tenan",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests understanding of idempotent APIs, distributed write reliability, and multi-region read-model consistency under failure.\n\nKey Concepts\n- Idempotency keys and deduplication windows\n- Outbox/CDC patterns for exactly-once processing\n- Cross-region latency and eventual consistency\n- Schema evolution and backward compatibility\n- Failure modes: primary failover, network partitions, back-pressure\n\nCode Example\n```javascript\n// Example: idempotent POST /orders with dedupe check\nasync function submitOrder(key, payload) {\n  const exists = await redis.get(`order_idem:${key}`);\n  if (exists) return JSON.parse(exists);\n  const order = await db.createOrder(payload);\n  await redis.set(`order_idem:${key}`, JSON.stringify(order), 'EX', 3600);\n  await outbox.publish(order);\n  return order;\n}\n```\n\nFollow-up Questions\n- How would you verify that admin override paths do not bypass deduplication?\n- What metrics and dashboards would you expose to detect replication lag and dead-lettered events?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-19",
        "questionId": "q-7100",
        "question": "Design a beginner-friendly test plan for an API endpoint GET /stream/alerts that uses Server-Sent Events (text/event-stream) to push real-time alerts. Include verification that the initial response is 200 with Content-Type text/event-stream, events arrive as distinct data lines, IDs increase monotonically, and reconnection with Last-Event-ID preserves continuity; test keepalive messages and concurrent clients receiving isolated streams; provide tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design end-to-end tests for POST /rides, GET /rides/{id}, and /rides/stream. Use consumer-driven contracts and OpenAPI validation; simulate rotating tokens; verify per-tenant rate limits; test idempot",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test signature validation by sending tampered payloads (expect 401). Enforce idempotency by replaying the same event and ensuring no duplicate writes to Redis. Validate ordering vs. out-of-order event",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test GET /stream/alerts using Server-Sent Events. Verify 200 and Content-Type: text/event-stream. Capture the first N events and confirm each starts with data: and is valid JSON; ensure id values incr",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Test idempotency by sending two POST /orders with the same Idempotency-Key and verify only one order is created and the second response reuses the first order_id; verify /orders/{id} status transition",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes hands-on SSE testing, reconnection semantics, and concurrency basics—common in real-time APIs used by dashboards or alerting.\n\n## Key Concepts\n- Server-Sent Events, text/event-stream\n- Event framing, data: lines, id:\n- Last-Event-ID, reconnection\n- Keepalive, multiple clients\n\n## Code Example\n```javascript\n// Simple client with EventSource\nconst es = new EventSource('/stream/alerts');\nes.onmessage = e => console.log(e.data);\nes.onerror = () => es.close();\n```\n\n## Follow-up Questions\n- How would you test error handling when the stream server restarts?\n- How would you simulate backpressure if events arrive faster than the client can process?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-20",
        "questionId": "q-21669",
        "question": "Design an end-to-end test strategy for a streaming REST endpoint that serves an NDJSON stream at GET /stream with client backpressure. Include test cases for stream resumption after disconnects, per-tenant data isolation, buffering and backpressure behavior, checkpointing and exactly-once processing guarantees, and contract validation against OpenAPI/async schemas. Outline tooling, acceptance criteria, and failure modes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe how you would validate the end-to-end flow of POST /batch/infer, including idempotent batch submissions, webhook signature validation, partial-failure handling with per-record status, and ret",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose contract-driven tests (PACT) for REST and SSE payloads per tenant, validating ON/OFF flag states and optional fields. Use RBAC checks under bursts, ensure event ordering and dedup under SSE re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a streaming test harness that subscribes to /stream with configurable backpressure, verifies resumption from a cursor after disconnect, injects downstream failures to test retry/backoff, and ch",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a tiny test harness to hit GET /search?q=foo, simulating 100 requests per 15 minutes per user. Assert 200 with JSON matching the schema; when the limit is exceeded, expect 429 and a Retry-After he",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTest a realistic streaming API to detect subtle issues around backpressure, fault tolerance, and multi-tenant isolation.\n\n## Key Concepts\n\n- Streaming contracts, backpressure, checkpointing\n- Tenant isolation, replay semantics\n- Consumer-driven contract testing, OpenAPI validation\n\n## Code Example\n\n```javascript\n// Pseudo test harness sketch\n```\n\n## Follow-up Questions\n\n- How would you validate cross-service ordering guarantees under high load?\n- How would you introduce drift detection between streaming contract and consumer expectations?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-21",
        "questionId": "q-5216",
        "question": "Design an intermediate test plan for POST /webhooks/internal that uses HMAC signatures and a Redis-backed replay cache. How would you verify signature correctness and replay protection under high velocity, ensure idempotent downstream state updates for duplicate payloads, and simulate downstream outages (e.g., 503) and Redis failures? Include concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan: POST /payments with Idempotency-Key to prevent duplicates. Use a mocked external gateway to capture charges. Steps: same key twice -> one charge; different keys -> separate charges; high-co",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a concrete test plan for GET /telemetry?vehicleId=... The endpoint reads real-time data from a Kafka feed and a batch Cassandra store. Validate cross-source consistency, ensure a defined stale",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt Pact contracts for REST and Kafka events, versioned per API version, and run consumer verifications in CI with a central broker and provider verifications per version. Simulate out-of-order even",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an end-to-end plan for POST /webhooks/internal that uses HMAC signatures and a Redis-backed replay cache. Verify signature correctness and replay protection under high velocity; ensure idempote",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests must cover security, correctness, and resilience of webhook ingestion in real-time systems.\n\n## Key Concepts\n- HMAC verification and replay protection\n- Idempotent processing across at-least-once delivery\n- Redis-backed replay cache behavior\n- Downstream fault tolerance and retries\n- Contract testing for webhook contracts\n\n## Code Example\n```javascript\n// Example test harness outline\nconst payload = { event_id: 'evt_123', type: 'order.updated', data: { orderId: 'ord_1' } };\n// mock signature and send to webhook endpoint\n```\n\n## Follow-up Questions\n- How would you test duplicate event_id eviction in Redis?\n- How would you simulate canary/webhook replay?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-22",
        "questionId": "q-25683",
        "question": "Design a beginner-friendly test plan for an API endpoint GET /weather?city={city} backed by a distributed cache. Validate caching headers (ETag, Cache-Control), ensure 304 Not Modified when data is unchanged, force cache miss to verify origin fetch and TTL behavior, test cache stampede mitigation and outage handling, and outline CI-ready tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Test Plan: POST /upload Endpoint ### Overview This test plan covers the primary acceptance criteria and security validations for a multipart/form-data file upload endpoint with per-tenant isolation. --- ### 1. Valid…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose end-to-end latency tests (k6) targeting 95th percentile under peak load with <2s SLA; validate per-tenant isolation by using tenant_id headers and querying with mixed tenants to ensure no cros",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test plan should cover: verify ETag/If-None-Match returns 304 when data unchanged; check Cache-Control max-age and s-maxage; simulate first request populating cache, subsequent requests hit cache; str",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "## Test Plan: PATCH /inventory/{sku} with Optimistic Concurrency Control ### Precondition Ensure GET /inventory/{sku} returns an ETag header in the response. ### Test Cases **TC1: Successful Update with Valid ETag** 1.…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical API testing with caching behavior, a common real-world pitfall.\n\n## Key Concepts\n- HTTP caching headers; ETag; conditional requests\n- Cache TTLs; cache stampede mitigation\n- Failover and graceful degradation; CI integration\n\n## Code Example\n```javascript\n// pseudo-test: check 304 when If-None-Match matches\n```\n\n## Follow-up Questions\n- How would you automate cache invalidation scenarios across multiple regions?\n- How would you monitor cache hit ratio in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-23",
        "questionId": "q-8115",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /checkout that enforces per-user rate limits (e.g., 5 requests per minute). Verify correct 429 responses with Retry-After, ensure counters reset, test concurrent clients, and observe behavior when downstream payment gateway times out (returning 503). Include tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use consumer-driven contracts (Pact). Consumers publish contracts to a broker; CI fetches latest contracts and runs provider verifications in a dedicated env per tenant/feature flag. Version contracts",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a comprehensive multi-tenant streaming test framework with HTTP/2 SSE as the primary protocol and WebSocket as the fallback mechanism. Validate stream initialization by confirming 200 OK responses with appropriate C…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: test per-user rate limiting on POST /checkout. Step 1: send 5 rapid requests -> 200; Step 2: 6th request -> 429 with Retry-After header; Step 3: advance the clock to reset window and verify 200",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-tenant tokens and a sandboxed tenant namespace; simulate bursty load with k6 targeting SSE endpoints, aiming for 99th percentile latency under SLA. Validate per-tenant event order via monotoni",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nRate limits are common and tricky for beginners; this tests understanding of quota windows, edge-cases, and downstream impacts.\n\n## Key Concepts\n- Per-user quotas, 429, Retry-After\n- Concurrency, distributed state, clock control\n- Downstream timeouts and 503 handling\n- Idempotent retries for POST\n- Tooling: Postman, k6, pytest/httpx\n\n## Code Example\n```javascript\n// Example test snippet\n```\n\n## Follow-up Questions\n- How would tests differ if quota is per IP?\n- How to verify Redis counters and TTLs in the implementation?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-24",
        "questionId": "q-25780",
        "question": "Design an end-to-end API test plan for a multi-tenant gateway that fronts a write API /comments and an SSE stream; a central config service controls **per-tenant quotas** and purge policies with asynchronous refresh. Validate **per-tenant quotas**, **quota-change propagation** (<5s), correct **SSE event ordering** under throttling, and graceful degradation when the config service is unavailable. Include tooling and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Valid payload → 202 with Location /jobs/{id} and body containing jobId. Replays within 2 seconds must be idempotent and return the same jobId. Implement polling with exponential backoff (1s, 2s, 4s) a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a test harness that runs Kafka (transactional producer), Redis, and Postgres in a sandbox. 1) Send POST /items twice with the same Idempotency-Key and expect one write and one effective update; 2)",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose end-to-end tests for POST /reports with an idempotency-key. Verify that the first call enqueues one job and returns a job-id; a second call with the same idempotency-key while the first is run",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose an end-to-end plan using contract tests between the gateway and a central config service, simulating tenants with distinct quotas. Validate quota enforcement, quota-change propagation (<5s), S",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes advanced API testing: dynamic per-tenant quotas controlled centrally, asynchronous config propagation, and streaming behavior under throttling. It evaluates contract testing, resilience, and end-to-end observability across gateway, write path, and SSE channel.\n\n## Key Concepts\n\n- Contract tests between gateway and config service\n- Token-bucket rate limiting with per-tenant isolation\n- SSE stream ordering under backpressure\n- Failure modes when config service is unavailable\n\n## Code Example\n\n```javascript\n// Example k6 snippet illustrating quota change and SSE validation\nimport http from 'k6/http';\n// ...\n```\n\n## Follow-up Questions\n\n- How would you model quota bursts and backoff strategies across tenants?\n- How would you verify SLA adherence under mixed latency conditions?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-12T06:42:43.238Z",
    "version": 1
  }
]