[
  {
    "id": "test-api-testing",
    "channelId": "api-testing",
    "channelName": "API Testing",
    "title": "API Testing Knowledge Test",
    "description": "Test your understanding of API Testing with 25 questions.",
    "questions": [
      {
        "id": "api-testing-q-0",
        "questionId": "q-9335",
        "question": "Design a beginner-friendly test plan for a REST API POST /checkout that accepts a cart payload and starts an asynchronous order processing job. The API uses an Idempotency-Key header: repeated requests with the same key must return 200 with the same jobId and not duplicate jobs. Include concrete test cases for: initial 202 with jobId, idempotent retries with same key, missing fields and invalid data, concurrent requests with different keys, and polling status until DONE. Also propose tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test Plan for REST API POST /checkout with Idempotency-Key: 1. **Happy Path**: Valid cart payload returns 202 with unique jobId 2. **Idempotency**: Same Idempotency-Key returns 200 with identical jobId, no duplicate job…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would build an end-to-end test harness for the `/telemetry` and `/telemetry/stream` endpoints that validates real-time, multi-tenant behavior. The test plan covers: 1. **Stream resilience and deduplication in bursts**…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Idempotency-Key must guarantee a single charge per key. Test steps: 1) POST /payments with key → 202, paymentId; 2) retry with same key and identical payload → same paymentId; 3) run concurrent reques",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Test Cases** - **400 Bad Request**: Missing or invalid required fields (e.g., missing pickup_location or invalid passenger_count) - **401 Unauthorized / 403 Forbidden**: Missing or invalid X-API-KEY header; tenant not…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis evaluates understanding of idempotent API design, asynchronous workflows, and comprehensive testing strategies in distributed systems.\n\n## Key Concepts\n- Idempotency keys prevent duplicate operations\n- Asynchronous job processing and status polling\n- Input validation and error response design\n- Concurrent request handling and race conditions\n- Test isolation and data management\n- Tooling selection for different testing phases\n\n## Code Example\n```javascript\n// Jest test example\nconst payload = {cart: [{id: 'p1', qty: 2}], currency: 'USD'}\nconst headers = {'Idempotency-Key': 'abc-123'}\n\nconst response1 = await request(app)\n  .post('/checkout')\n  .set(headers)\n  .send(payload)\n  .expect(202)\n\nconst response2 = await request(app)\n  .post('/checkout')\n  .set(headers)\n  .send(payload)\n  .expect(200)\n\nexpect(response1.body.jobId).toBe(response2.body.jobId)\n```",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-1",
        "questionId": "q-2017",
        "question": "Design a beginner-friendly test plan for an API endpoint GET /products/{id} that uses a real-time feature flag to toggle response fields. Validate that additional fields (e.g., discount, promotionTag) appear when the flag is ON and disappear when OFF; cover admin-API downtime fallback; and test mid-request toggles with concurrent requests to ensure each response matches its observed flag. Include tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan should generate a valid HMAC for the payload to simulate a real client, expect 200/202 for valid, 401 for invalid. Reuse same Idempotency-Key to prove idempotent behavior within 5-minute win",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan an end-to-end test for a real-time, multi-tenant SaaS API spanning three regions with dynamic per-tenant feature flags. Seed regional shards, drive high-concurrency user lookups, verify per-tenan",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Issue repeated POST /payments requests with the same Idempotency-Key and verify that only one charge is created in the ledger. Ensure the payment gateway is invoked exactly once, and confirm that subsequent calls return…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Define tests for GET /products/{id} with a real-time feature flag. Verify response includes fields 'discount' and 'promotionTag' when flag is ON and omits them when OFF. Simulate admin API downtime to",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests for dynamic response shapes via feature flags; ensures contract testing and resilience to admin API issues.\n\n## Key Concepts\n- Feature flags, API contracts, concurrent requests, admin downtime, fallback behavior.\n\n## Code Example\n```javascript\n// Example test skeleton (using Jest + supertest)\n```\n\n## Follow-up Questions\n- How would you test caching interactions with feature flags?\n- How would you handle A/B testing with user targeting?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-2",
        "questionId": "q-4785",
        "question": "Design a beginner-friendly test plan for GET /weather?city={city} that aggregates data from two third‑party providers. The service caches results for 60 seconds and imposes a 5-second timeout per provider. Include tests for provider outage, caching behavior, input validation, timeout handling, and per-IP rate limits returning 429. Propose concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan for GET /weather?city={city} aggregating two providers with 60s cache and 5s per-provider timeouts. Verify: (a) both up returns average temperature; (b) one down still returns from the other",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Open an SSE stream for a ticker and assert the first event payload contains {ticker, price, timestamp}. Validate each event's price is a number and timestamp is monotonically increasing. Simulate a di",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a WebSocket test harness simulating 1000 devices per tenant with JWT-scoped tokens. Verify isolation by asserting tenantId boundaries; ensure sequence-based ordering and dedupe on reconnect via la",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Verify 202 and trackingId on first call; simulate duplicates with the same payload and Idempotency-Key to ensure no duplicates in downstream stores; validate per-partition ordering by including a part",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests resilience of a composite API and introduces caching, partial failure handling, and integration with mocks. It reveals understanding of timeouts, fallbacks, and rate limiting.\n\n## Key Concepts\n- Parallel downstream calls\n- Caching TTL\n- Partial failure handling (fallback to available provider)\n- Proper status codes (400, 503, 429)\n- Tooling for mocks and assertions\n\n## Code Example\n```javascript\n// pseudo-test skeleton showing assertions\n```\n\n## Follow-up Questions\n- How would you measure cache coherence across multiple instances?\n- How would you handle provider secret rotation?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-3",
        "questionId": "q-26535",
        "question": "Design a beginner-friendly API test for POST /payments, which accepts amount, currency, and source (card token). Include idempotency header X-Idempotency-Key, verify that retries with the same key do not double-charge, test 409 for duplicate keys, and cover validation errors (400/422) and gateway timeout handling with backoff. Specify tooling, test data, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Make a first call with key 'abc' returning 201 and a transaction_id. Replay with the same key; gateway mocks return 200 with the same id or 409; ensure no extra charges. Validate 400/422 for bad input",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I’d propose a PyTest-based plan for POST /payments with userId, amount, currency and an Idempotency-Key header. Test cases: 1) first submission returns 201 and an id; 2) same Idempotency-Key returns 2",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Validate idempotency by issuing POST /payments with the same idempotency key multiple times and asserting that only a single payout is created. Use a distributed ledger to reconcile events from the orchestrator and down…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Provide steps to test: create payload with amount,currency,card fields; compute signature with shared secret; send with headers; verify 403 on bad signature, 401 for missing key; resend same Idempoten",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for idempotency, retry behavior, and gateway interactions are critical in payments APIs. This question probes practical validation of idempotent semantics, duplicate handling, and robust error paths with realistic tooling.\n\n## Key Concepts\n\n- Idempotency keys and idempotent POSTs\n- Duplicate-key handling (201 vs 409) and charge integrity\n- Validation paths (400, 422) and gateway timeouts\n- Retry strategies with backoff and state isolation\n- Mocked external gateway and observability\n\n## Code Example\n\n```javascript\n// Example: using supertest + nock for idempotent POST /payments\nconst request = require('supertest');\nconst gateway = require('nock')('https://payments.example.com');\n\ndescribe('POST /payments idempotency', () => {\n  test('first call succeeds, retry reuses idempotency key without double charge', async () => {\n    gateway.post('/charge').reply(200, {transaction_id: 'tx-1'});\n    const first = await request(app)\n      .post('/payments')\n      .set('X-Idempotency-Key', 'abc')\n      .send({amount: 1000, currency: 'USD', source: 'tok_visa'});\n    expect(first.status).toBe(201);\n\n    gateway.post('/charge').reply(200, {transaction_id: 'tx-1'});\n    const second = await request(app)\n      .post('/payments')\n      .set('X-Idempotency-Key', 'abc')\n      .send({amount: 1000, currency: 'USD', source: 'tok_visa'});\n    expect(second.status).toBe(200);\n  });\n});\n```\n\n## Follow-up Questions\n\n- How would you adapt tests if X-Idempotency-Key is optional?\n- How would you validate concurrency with multiple clients submitting the same key simultaneously?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-4",
        "questionId": "q-13607",
        "question": "For a rideshare platform with REST endpoints /rides and /drivers and a partner webhook system, design an end-to-end API test plan to validate at-least-once webhook delivery, idempotent processing, retry/backoff behavior, signature verification (HMAC-SHA256) and replay protection, dead-letter routing, and contract validation with partner schemas. Include concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan: for webhook deliveries in a rides platform, verify at-least-once delivery with idempotent handlers by resending the same payload; confirm retry/backoff behavior and DLQ after 3 failed attem",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage an idempotency key on POST /orders to guarantee exactly-once semantics. Test retries by replaying the same key; assert a single order is created and no duplicates. Simulate Kafka broker outag",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a concrete GraphQL test plan: validate tenant isolation via token scopes and resource filtering; enforce maximum query depth (e.g., 8 levels) and field whitelisting for persisted queries; test",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test plan should verify end-to-end exactly-once processing via idempotency keys and a transactional outbox. Include: duplicate event with same idempotency_key yields one charge and one outbox entry; h",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests webhook reliability and contract integrity in integration-heavy APIs, a common production risk.\n\n## Key Concepts\n- Webhook reliability (at-least-once)\n- Idempotence and state reconciliation\n- Retry/backoff and DLQ semantics\n- Signature verification and replay protection\n- Consumer contract testing across partners\n\n## Code Example\n\n```javascript\n// Placeholder example: simulate webhook post and idempotent check\n```\n\n## Follow-up Questions\n- How would you monitor and alert on DLQ bloat and failed deliveries?\n- How would you test with multiple partner signing keys rotation?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-5",
        "questionId": "q-8265",
        "question": "Design a beginner-friendly test plan for an API endpoint POST /search that accepts complex search parameters in JSON body (filters, sort, and pagination). Validate proper input handling (400 for invalid types or missing required fields), rate-limiting behavior (429 and Retry-After header), and pagination consistency across pages. Include concrete test cases, tooling suggestions, and edge-case handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan: Implement route routing using the X-API-Version header, with v1 as the default when the header is missing. For v1 endpoints, validate response contains {username,email}. For v2 endpoints, validate response co…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Define a strict JSON schema for POST /search; validate invalid types, missing required fields, and oversized payloads return 400 with actionable messages. Simulate rate limiting to yield 429 and Retry",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a canary-flag test suite: simulate flag flips in a distributed config store (etcd) and measure propagation latency across regions. Validate per-tenant isolation by querying tenants concurrently",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan end-to-end tests for a multi-tenant REST + WebSocket API with rolling auth service; validate tenant isolation, token revocation propagation, streaming data integrity under bursts, and per-tenant",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nIntroduces an angle not covered: rate-limiting, pagination and input validation in a single endpoint, a common real-world pattern not covered by existing questions.\n\n## Key Concepts\n- Input validation and schema contracts\n- Rate limiting and Retry-After semantics\n- Pagination tokens (cursor) semantics and consistency\n- Idempotency and concurrency\n- Tooling: Postman, Newman, pytest-requests, k6 for load; mocking rate limiter; contract tests with JSON Schema\n\n## Code Example\n```javascript\n// Example test: invalid input returns 400\nconst axios = require('axios');\ntest('POST /search invalid type returns 400', async () => {\n  const res = await axios.post('https://api.example.com/search', { filters: 1, sort: 'asc' }, { validateStatus: () => true });\n  expect(res.status).toBe(400);\n});\n```\n\n## Follow-up Questions\n- How would you test 429 handling in a flaky network and ensure Retry-After is respected across retries?\n- How could you automate growth tests for pagination under higher concurrency?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-6",
        "questionId": "q-7852",
        "question": "Advanced API testing challenge: In a real-time, multi-tenant SaaS API serving per-tenant user profiles across three regions, with a dynamic feature flag system that can toggle fields mid-request, design a practical end-to-end test plan. Include data seeding, concurrency, per-tenant isolation checks, mid-flight flag toggles, token rotation impact, and chaos-injected regional DB outages; specify tooling and success criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan includes: 1) contract tests for GET /inventory/{sku} and POST /inventory/{sku} using ETag (If-Match) semantics; ensure 412 on stale ETAG. 2) validate /inventory/updates delta events under simulat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a comprehensive WebSocket test plan using a test client to simulate multiple tenants on /ws/updates. Validate: (1) JWT-based tenant authentication and isolation, (2) message ordering guarantees per tenant, (3)…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan tests for a REST API delivering per-tenant feature flags with dynamic rollout and A/B experiments. Verify per-user bucketing via a deterministic hash, check distribution fairness within tolerance",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan an end-to-end test for a real-time, multi-tenant SaaS API spanning three regions with dynamic per-tenant feature flags. Seed regional shards, drive high-concurrency user lookups, verify per-tenan",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests multi-region, multi-tenant APIs with dynamic flags, token rotation, and chaos engineering, covering data isolation, race conditions, and resilience.\n\n## Key Concepts\n\n- Multi-tenant isolation across regions\n- Dynamic feature flags and mid-request toggles\n- Token rotation and auth resilience\n- Fault injection and regional outages\n- End-to-end test data management and observability\n\n## Code Example\n\n```javascript\n// Pseudo-test harness skeleton\nasync function runTest() {\n  // seed data per tenant\n  // fire concurrent requests across regions\n  // toggle flags mid-flight\n}\n```\n\n## Follow-up Questions\n\n- How would you measure data leakage across tenants under load?\n- What observability signals would you rely on for pass criteria?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-7",
        "questionId": "q-8736",
        "question": "Design an end-to-end test plan for a multi-region balance service with REST GET /accounts/{id} and streaming /accounts/{id}/stream that emits balance updates. Include high-throughput scenarios, idempotent PATCH upserts, cross-region cache invalidation, and failure modes (regional blackout, streaming backpressure). Provide concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan should verify: valid multipart upload yields 202 with a jobId; 413 for images over 5MB; 415 for unsupported formats; 400 for missing image. Idempotency: same Idempotency-Key must not duplica",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use consumer-driven contracts between the gateway, adapters, and partner webhooks, with strictly versioned payload schemas. Implement contract tests (Pact) that cover both old and new schemas during transition periods,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy three-region replicas; drive high update rates with k6 against PATCH and GET; verify GET returns latest balance after upserts and streaming delivers deduplicated, in-order events with sequence",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan focuses on: happy-path full data from both downstreams; partial-fallback 200 with partial_data=true when one downstream fails; per-tenant isolation checks with distinct tokens; 400/401/403 and 42",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests multi-region data correctness, idempotent updates, and streaming reliability under outages—core in fintech-scale APIs.\n\n## Key Concepts\n\n- Cross-region consistency\n- Streaming deduplication\n- Cache invalidation\n- Failure injection\n\n## Code Example\n\n```javascript\n// Pseudocode: idempotent PATCH test\nasync function testIdempotentPatch(client, id, payload, repeats=3){\n  for(let i=0;i<repeats;i++){ await client.patch(`/accounts/${id}`, payload); }\n}\n\n// Pseudocode: streaming dedupe\nlet lastSeq = 0;\nstream.on('data', ev => { if (ev.seq>lastSeq){ lastSeq=ev.seq; process(ev); } });\n```\n\n## Follow-up Questions\n\n- How would you parameterize tests for different regional latencies?\n- How would you detect and recover from partial data divergence?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-8",
        "questionId": "q-5743",
        "question": "Design a testing plan for a REST API /payments that orchestrates a multi-service distributed transaction (fraud check, bank transfer, ledger). The API returns 202 with a transactionId and a status polling endpoint /payments/{id}/status. Include idempotency handling, partial-failure compensation, eventual consistency verification, and retry/backoff strategies with concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline tenant isolation by issuing the same GraphQL query with different auth tokens and asserting tenant-scoped data and headers. Validate persisted queries by hashing a query and confirming the gat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a comprehensive API testing plan for a multi-tenant analytics API with the following test scenarios: **1. Per-Tenant Data Isolation** - Verify Redis keys follow the pattern `tenant:{id}:usage` - Cross-tenant requ…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: seed a deterministic set of idempotent events to POST /events, verify Redis-backed GET /analytics?window=1m reflects exact aggregates per window, and confirm Kafka offsets align with exactly",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an idempotency key per /payments call to prevent duplicates; mock downstreams (fraud, bank, ledger) and verify a single transaction and consistent ledger for repeated requests with the same key. S",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes end-to-end testing of a distributed transaction orchestrator, focusing on idempotency, compensation, and eventual consistency—critical in payments-like flows.\n\n## Key Concepts\n- Idempotency keys and deduplication\n- Distributed orchestration and compensating actions\n- Partial-failure handling and rollback paths\n- End-to-end vs contract tests with downstream services\n- Observability and deterministic test data\n\n## Code Example\n```javascript\n// Example test skeleton for Node.js (mocha)\ndescribe('POST /payments idempotent flow', () => {\n  it('preserves a single transaction for repeated calls with same idempotency key', async () => {\n    const key = 'idem-123';\n    await postPayment({amount: 100}, key);\n    await postPayment({amount: 100}, key);\n    const txs = await listPaymentsByKey(key);\n    assert.strictEqual(txs.length, 1);\n  });\n});\n```\n\n## Follow-up Questions\n- How would you simulate a bank service outage during the transaction and verify idempotence?\n- How would you validate that compensation runs only when partial failure occurs?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-9",
        "questionId": "q-4738",
        "question": "You're adding a multi-tenant API that returns a paginated user list for a tenant via GET /tenants/{tenantId}/users. A separate POST /tenants/{tenantId}/users/refresh queues a refresh job in a downstream service to recompute each user's complianceScore. Design a concrete end-to-end test plan to validate correctness, idempotency of refresh, tenant isolation, cache invalidation latency, and failure handling when the downstream service is slow or unavailable. Include concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Verify 202 and trackingId on first call; simulate duplicates with the same payload and Idempotency-Key to ensure no duplicates in downstream stores; validate per-partition ordering by including a part",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Initiate a refresh for tenant A and verify only A’s cache is rewritten; tenant B must remain unchanged. Send multiple identical refresh requests to prove idempotency. Query GET /tenants/A/users to con",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan: simulate a 5-node deployment with Redis-backed per-minute quotas and an admin API. Use k6 to generate bursts on GET /quotes/{symbol} and WebSocket /ws/quotes from multiple tenants; verify 429s w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Focus on idempotent POSTs using Idempotency-Key, ensure retries return the same result, confirm 202 produces a Location for polling and 200/final state, verify /payments/{id} reflects consistent statu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end data freshness across REST and async refresh, per-tenant isolation, and resilience to downstream slowdowns.\n\n## Key Concepts\n\n- End-to-end flow across REST and queue\n- Idempotency and race conditions\n- Cache invalidation timing and SLA\n- Downstream failure simulation and observability\n\n## Code Example\n\n```javascript\n// Example test sketch (pseudo)\nconst res1 = await callRefresh('tenantA');\nconst res2 = await callRefresh('tenantA'); // idempotent\nawait waitForCacheUpdate('tenantA');\nconst users = await getUsers('tenantA');\nexpect(users.find(u=>u.id==='target').complianceScore).toBeGreaterThan(0);\n```\n\n## Follow-up Questions\n\n- How would you test cross-region consistency?\n- How would you validate metrics and alerts for cache stampedes?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-10",
        "questionId": "q-15478",
        "question": "Suppose an API endpoint GET /articles/{id} supports ETag caching via the If-None-Match header. Design a beginner-friendly test plan to verify correct ETag handling, 304 responses, and cache invalidation after updates. Include test cases for initial fetch, cache hit, resource update causing 200 with new ETag, concurrent requests with different If-None-Match values, and a 404 for missing IDs. Include tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive API testing plan that validates per-user RBAC enforcement across a distributed architecture consisting of a central policy decision point (PDP) and regional edge caches. The plan should cover thre…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Execute a per-user rate-limit test: 5 POST /checkout requests in 60s; 6th returns 429 with Retry-After 60s; verify that headers like Retry-After or X-RateLimit-Reset reflect 60s; run parallel bursts f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a multi-layered testing approach: unit tests for individual API endpoints, integration tests for model inference workflows, contract testing using OpenAPI specs, load testing with tools like k6 or JMeter, and…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "1) GET /articles/1 to obtain the initial ETag. 2) GET with If-None-Match set to that ETag; expect 304 Not Modified. 3) Update the article, then GET again; expect 200 with a new ETag. 4) Simulate concu",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests here check understanding of HTTP caching and conditional requests, not just functional correctness.\n\n## Key Concepts\n- ETag headers and If-None-Match\n- 304 Not Modified behavior\n- Cache invalidation after updates\n- Concurrency in tests for shared caches\n- Basic tooling for APIs (curl, httpie)\n\n## Code Example\n```bash\ncurl -i http://api/articles/1\n```\n\n## Follow-up Questions\n- How would you automate these tests in a CI pipeline?\n- How would you test weak vs strong ETag comparisons?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-11",
        "questionId": "q-15198",
        "question": "Design a practical test plan for an API delivering real-time vehicle telematics via REST GET /vehicles/{id} and WebSocket /vehicles/{id}/stream. Include concrete test cases, tooling, and expected outcomes, covering data correctness, streaming ordering/dedup/reconnect/backpressure, multi-tenant isolation, failure modes, and observability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test plan should simulate multi-region uploads, sending chunks out-of-order and retrying duplicates; enforce per-user quotas under bursts; validate reassembly with SHA-256; verify final callback paylo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Develop an end-to-end test plan for an async /data/process flow: verify idempotent POSTs with Idempotency-Key, check that duplicates yield a single operation, poll results for eventual consistency and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Idempotency-Key must guarantee a single charge per key. Test steps: 1) POST /payments with key → 202, paymentId; 2) retry with same key and identical payload → same paymentId; 3) run concurrent reques",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "REST: 200 with schema, 404 not found, 429 for rate limiting. Streaming: ensure in-order delivery, dedupe, reconnect recovery, and backpressure signaling. Multi-tenant: simulate 5 tenants with scoped t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes real-world API testing skills for REST and streaming endpoints, multi-tenant isolation, and resilience under failure.\n\n## Key Concepts\n- REST + streaming testing\n- Data correctness, order, dedupe\n- Reconnect, backpressure\n- Tenant isolation and security\n- Observability and test data management\n\n## Code Example\n```javascript\n// example test skeleton\n```\n\n## Follow-up Questions\n- How would you test clock skew or out-of-order events across streams?\n- How would you automate data masking in streamed payloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-12",
        "questionId": "q-23787",
        "question": "Design a tenant-aware API test plan for GET /products/{id} where admin policy updates may change visible fields mid-request and must not leak across tenants. Include per-tenant field exposure, mid-request policy flips, concurrent requests, and rollback scenarios. Provide concrete test cases, tooling, and acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan should use a test runner (e.g., Postman or k6) to emit per-user bursts, verify 200s up to the limit and 429 with Retry-After thereafter; validate Link headers for next/prev pages remain correct d",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a tenant-aware contract suite around GET /products/{id} validating per-tenant field exposure under concurrent mid-request policy changes. Use Pact for consumer-driven contracts, a policy-change",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Test plan for POST /webhooks/payment: verify HMAC-SHA256 signature with shared secret; enforce a 5-minute timestamp window; require idempotency-key to dedupe. Create cases: valid signature; invalid si",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an end-to-end automated test plan for a REST ingestion endpoint /telemetry/{tenantId}/ingest that accepts chunked JSON lines, enforces per-tenant quotas, deduplicates by message_id, validates s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to validate complex, real-world guarantees when policy changes occur at runtime, potentially mid-request, and must not leak between tenants.\n\n## Key Concepts\n\n- Tenant-aware field exposure\n- Mid-request policy application\n- Contract testing with Pact\n- Concurrency and isolation\n- Rollback and audit trails\n\n## Code Example\n\n```javascript\n// Example: Pact consumer test skeleton for tenant-specific fields\nconst { Pact } = require('@pact-foundation/pact');\n// ... test setup demonstrating tenant-scoped expectations\n```\n\n## Follow-up Questions\n\n- How would you detect and prevent data leakage during policy rollbacks?\n- How would you simulate mid-request policy flips under high concurrency?\n- What metrics would you collect to prove SLA adherence during policy churn?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-13",
        "questionId": "q-453",
        "question": "You're testing a REST API that returns paginated results. The endpoint has a rate limit of 100 requests per minute and sometimes returns 500 errors under load. How would you design a comprehensive test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement idempotency tests by replaying a POST /orders with the same Idempotency-Key across transient faults and ensure only one order is created per tenant. Validate webhook replay-safe by signing p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: Create a bulk payload with mixed valid and invalid users. Validate per-item results (success, validation error). Test idempotency via Idempotency-Key: re-sending with same key should not create duplicates. Ver…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement comprehensive load testing with gradual ramp-up using tools like k6 or JMeter to simulate realistic traffic patterns. Add retry logic with exponential backoff for 500 errors, implementing circuit breakers to p…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose resilience-focused tests: mock downstream latency and failures for /sales/daily and /customers/daily; assert 200 with partial payload and a warning header when one downstream times out; assert",
            "isCorrect": false
          }
        ],
        "explanation": "## Test Strategy Components\n\n- **Load Testing**: Simulate realistic user traffic patterns with gradual ramp-up\n- **Rate Limiting**: Implement request throttling and batching to respect API limits\n- **Error Handling**: Test retry mechanisms with exponential backoff and circuit breakers\n- **Contract Testing**: Validate API responses against schemas and documentation\n- **Monitoring**: Track performance metrics, error rates, and system resource utilization\n\n## Tools & Implementation\n\n```javascript\n// Example retry logic with exponential backoff\nconst retryRequest = async (url, retries = 3) => {\n  for (let i = 0; i < retries; i++) {\n    try {\n      const response = await fetch(url);\n      if (response.ok) return response;\n      \n      // Exponential backoff: 1s, 2s, 4s\n      const delay = Math.pow(2, i) * 1000;\n      await new Promise(resolve => setTimeout(resolve, delay));\n    } catch (error) {\n      if (i === retries - 1) throw error;\n    }\n  }\n};\n\n// Rate limiting implementation\nclass RateLimiter {\n  constructor(maxRequests = 100, timeWindow = 60000) {\n    this.maxRequests = maxRequests;\n    this.timeWindow = timeWindow;\n    this.requests = [];\n  }\n  \n  async makeRequest(requestFn) {\n    const now = Date.now();\n    this.requests = this.requests.filter(time => now - time < this.timeWindow);\n    \n    if (this.requests.length >= this.maxRequests) {\n      const waitTime = this.timeWindow - (now - this.requests[0]);\n      await new Promise(resolve => setTimeout(resolve, waitTime));\n    }\n    \n    this.requests.push(now);\n    return await requestFn();\n  }\n}\n```\n\n## Testing Phases\n\n1. **Baseline Testing**: Establish performance metrics under normal load\n2. **Stress Testing**: Identify breaking points and failure modes\n3. **Spike Testing**: Test sudden traffic increases and recovery\n4. **Endurance Testing**: Validate performance over extended periods\n5. **Rate Limit Testing**: Verify behavior at and beyond rate limits",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-14",
        "questionId": "q-6968",
        "question": "Design an end-to-end test plan for a high-throughput API used for a real-time feature store. The API supports POST /features (ingest), GET /features/{id} (read), and a streaming endpoint /features/stream. Validate idempotency with upsert semantics, ensure eventual consistency within a 0–5s window, and test resilience by injecting downstream outages (Kafka, Redis). Include concrete test cases, tooling, and success criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy three-region replicas; drive high update rates with k6 against PATCH and GET; verify GET returns latest balance after upserts and streaming delivers deduplicated, in-order events with sequence",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a test plan for GET /composite/users/{id} that aggregates from three services in parallel and caches in Redis. Include: 1) stale-read detection by injecting latency on one backend and assert",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement idempotent POST /features using an idempotency-key header to ensure upsert semantics, where repeated requests return HTTP 200 without duplicate data ingestion. Validate eventual read consistency by confirming…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose end-to-end tests that seed deterministic events, track ids for dedupe, simulate backpressure via a traffic injector, verify that SSE clients receive events in strict order with no duplicates,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end behavior across ingestion, storage, and streaming data paths under load, with emphasis on idempotency, eventual consistency windows, and resilience to downstream outages. This reflects real-world production constraints in feature store architectures.\n\n## Key Concepts\n\n- Idempotency and upsert semantics for POST endpoints\n- Eventual consistency windows and read-after-write guarantees\n- Fault injection and resilience testing across messaging systems and caches\n- End-to-end automation combining load testing with API validation\n\n## Code Example\n\n```python\n# example: basic idempotent ingest test\nimport requests\nimport time\nimport uuid\n\ndef test_idempotent_ingest():\n    feature_data = {\"feature_id\": \"test_123\", \"value\": 42}\n    idempotency_key = str(uuid.uuid4())\n    \n    headers = {\"Idempotency-Key\": idempotency_key}\n    \n    # First request should create\n    response1 = requests.post(\"/features\", json=feature_data, headers=headers)\n    assert response1.status_code == 201\n    \n    # Second request with same key should succeed without duplication\n    response2 = requests.post(\"/features\", json=feature_data, headers=headers)\n    assert response2.status_code == 200\n    \n    # Verify only one record exists\n    get_response = requests.get(f\"/features/{feature_data['feature_id']}\")\n    assert get_response.json()[\"value\"] == 42\n```",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-15",
        "questionId": "q-27492",
        "question": "Design a testing plan for a payments API that uses idempotency keys to guarantee exactly-once processing across retries. Include how you'd simulate duplicate webhook events, verify idempotency across multi-region failover, handle key rotation edge-cases, and ensure retries never duplicate charges. Provide concrete test cases, tooling, and clear acceptance criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan includes batch_id idempotency checks, ledger dedupe verification on replay, and compensating transactions for partial downstream failures. Simulate partial success/failure, partitioned network, a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test idempotency by sending multiple POST /exports with the same Idempotency-Key and verify only one job is created and subsequent responses return the same job_id. Assert 202 with job_id and a Locati",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement idempotency via a write-ahead dedup table keyed by idempotency_key (resource_id, status, amount, charge_id). Retries return cached result unless the original failed irrecoverably. Test by re",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan around an idempotent POST /orders with a unique idempotency key; verify a single committed order across Inventory, Payment, and Shipping. Simulate partial outages and replayed events; ensure retr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests around idempotency and webhook replay are critical for payments platforms. This question probes coverage of end-to-end flow, stateful guarantees, multi-region considerations, and operational runtimes (rotation, outages).\n\n## Key Concepts\n\n- Idempotency keys\n- Deduplication table with transactional guarantees\n- Webhook replay safety\n- Cross-region consistency and key rotation\n- Fault injection and test tooling\n\n## Code Example\n\n```javascript\n// Pseudo implementation sketch for idempotency handling\nasync function handleCharge(event) {\n  const key = event.idempotency_key;\n  const existing = await db.findIdempotent(key);\n  if (existing) return existing.response;\n  const result = await chargeProvider.charge(event);\n  await db.upsertIdempotent({ key, response: result, status: result.success ? 'completed' : 'failed' });\n  return result;\n}\n```\n\n## Follow-up Questions\n\n- How would you test idempotency under eventual consistency of reads? \n- How would you design metrics and alerts for idempotency-related failures?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-16",
        "questionId": "q-17087",
        "question": "Design an advanced API testing plan for a real-time telemetry streaming endpoint /telemetry/stream/{sensorId} that uses Server-Sent Events (SSE) and supports versioned payloads controlled by a tenant-scoped feature flag. Your plan must cover (a) at-least-once delivery under network instability, (b) idempotent handling and Last-Event-ID replay, (c) cross-version compatibility and field evolution across versions, (d) per-tenant isolation and rate-limiting interactions during stream bursts, (e) admin downtime and failover scenarios, (f) end-to-end testing with a mock event bus and real-time dashboards. Include concrete test cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive test harness for the SSE streaming endpoint /telemetry/stream/{sensorId} with per-tenant feature flags and versioned payloads. The test plan validates: at-least-once delivery via Last-Event-ID rep…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Test Plan for POST /payments/process Idempotency **Objective:** Verify that duplicate requests with the same X-Idempotency-Key result in a single charge, while different keys allow separate charges. ### Test Cases |…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "First, retrieve the current ETag for /profiles/{id} via a GET request. Then perform a PUT request with a modified payload, including the If-Match header set to the retrieved ETag; expect a 200 OK response along with a n…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create canary and prod routes across regions, drive traffic while toggling flags ON/OFF, validate returned payloads, headers, and SLA, and confirm that when the flag service is unavailable, the API fa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for streaming APIs present unique challenges: stateful delivery, backpressure handling, and cross-tenant isolation. This question probes practical strategies for SSE streams with versioned payloads and feature flags, focusing on real-world reliability and compatibility.\n\n## Key Concepts\n\n- Server-Sent Events (SSE) streaming fundamentals\n- At-least-once delivery guarantees and Last-Event-ID replay mechanisms\n- Idempotent event processing and deduplication strategies\n- Schema/version evolution and feature flag semantics\n- Multi-tenant isolation and rate-limiting under burst conditions",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-17",
        "questionId": "q-7371",
        "question": "Design an advanced test plan for a REST API POST /orders that accepts idempotent submissions via an idempotency-key, with multi-region replicas and a separate read-model; verify exactly-once processing, deduplication across retries, latency effects on order status propagation, and schema evolution of the payload; include failure modes (primary failure, network partition), back-pressure, and admin override paths?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ensure exactly-once semantics for POST /payments using an idempotency-key store; simulate concurrent duplicate requests and verify only a single ledger entry is created. Mock bank responses with varyi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a plan that enforces exactly-once using a Redis-backed idempotency window and an outbox pattern for durable writes; simulate duplicate submissions under retries to ensure no additional orders a",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a test harness that exercises the REST enqueue endpoint and per-tenant WebSocket streaming. Verify: 1) strict per-tenant isolation ensuring tenants receive only their own events, 2) in-order delivery within sh…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a testing strategy for a globally distributed, federated GraphQL API with per-field authorization and a shared cache. Include per-field contract tests for multiple roles, schema-drift checks ac",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests understanding of idempotent APIs, distributed write reliability, and multi-region read-model consistency under failure.\n\nKey Concepts\n- Idempotency keys and deduplication windows\n- Outbox/CDC patterns for exactly-once processing\n- Cross-region latency and eventual consistency\n- Schema evolution and backward compatibility\n- Failure modes: primary failover, network partitions, back-pressure\n\nCode Example\n```javascript\n// Example: idempotent POST /orders with dedupe check\nasync function submitOrder(key, payload) {\n  const exists = await redis.get(`order_idem:${key}`);\n  if (exists) return JSON.parse(exists);\n  const order = await db.createOrder(payload);\n  await redis.set(`order_idem:${key}`, JSON.stringify(order), 'EX', 3600);\n  await outbox.publish(order);\n  return order;\n}\n```\n\nFollow-up Questions\n- How would you verify that admin override paths do not bypass deduplication?\n- What metrics and dashboards would you expose to detect replication lag and dead-lettered events?",
        "difficulty": "advanced"
      },
      {
        "id": "api-testing-q-18",
        "questionId": "q-10960",
        "question": "Design a beginner-friendly test plan for an API endpoint GET /api/items with pagination (page, size). Include per-user rate limiting: after 60 requests per minute, the API returns 429 with a Retry-After header. Provide concrete test cases for boundary conditions, verify pagination links stay correct during bursts, and outline how client-side backoff should respect Retry-After. Include tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan should use a test runner (e.g., Postman or k6) to emit per-user bursts, verify 200s up to the limit and 429 with Retry-After thereafter; validate Link headers for next/prev pages remain correct d",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan an end-to-end test for a real-time, multi-tenant SaaS API spanning three regions with dynamic per-tenant feature flags. Seed regional shards, drive high-concurrency user lookups, verify per-tenan",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "First request for a tenant fetches the policy and caches it for 60s; subsequent requests reuse the cache. Change the tenant policy via admin and verify masking changes after expiry or on manual refres",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose to apply idempotency keys stored in Redis, send two identical POST /orders with same idempotency-key and ensure a single order persists and one event emitted; test concurrent submissions with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes pagination behavior, rate limiting, and backoff strategies in realistic API testing scenarios.\n\n## Key Concepts\n\n- Pagination semantics (page, size, next/prev links)\n- Per-user rate limits and Retry-After handling\n- Concurrency and client backoff\n\n## Code Example\n\n```javascript\nimport http from 'k6/http';\nimport { sleep, check } from 'k6';\nlet base = 'https://api.example.com';\nexport default function () {\n  let res = http.get(`${base}/api/items?page=1&size=50`);\n  check(res, { 'status is 200 or 429': (r) => r.status === 200 || r.status === 429 });\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt tests for a 3xx redirect or 500 error?\n- How would you modify for multi-region deployment?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-19",
        "questionId": "q-1166",
        "question": "You manage a public REST API with versioning: /v1/... and /v2/... Design a practical, automated test plan to validate backward compatibility as v2 introduces a new field (tags) and changes a field type (price from int to decimal). Include concrete test cases, OpenAPI contract checks, cross-version schema validation, and how to verify deprecation behavior via a warnings header and 429s for old clients. Outline tooling and steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a practical plan to verify backward compatibility between v1 and v2 endpoints. Include concrete test cases for legacy clients calling v2, schema validation of both versions, OpenAPI contract",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Test Plan for GET /products API ### Test Cases **Basic Pagination:** - GET /products?page=1&limit=10 → Expect 200 with ≤10 items containing id,name,category,price,inStock - GET /products?page=999 → Expect 200 with em…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Execute a per-user rate-limit test: 5 POST /checkout requests in 60s; 6th returns 429 with Retry-After 60s; verify that headers like Retry-After or X-RateLimit-Reset reflect 60s; run parallel bursts f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test plan should validate idempotency by resubmitting the same idempotency-key and asserting a single export is created; simulate 10k concurrent POSTs to measure throughput and ensure duplicates aren’",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical understanding of API versioning, contract testing, and automated validation for real-world back-compat scenarios. It emphasizes concrete test cases, tooling, and CI integration.\n\n## Key Concepts\n- Contract testing across versions\n- Schema validation for v1 and v2\n- Deprecation signaling and rate-limiting for old clients\n- CI automation and stable fixtures\n\n## Code Example\n```javascript\n// Example: basic schema validation with Ajv for v1 vs v2\nconst Ajv = require('ajv');\nconst ajv = new Ajv();\nconst v1 = { type: 'object', properties: { id: {type:'string'}, price: {type:'number'} }, required: ['id','price'] };\nconst v2 = { type: 'object', properties: { id: {type:'string'}, price: {type:'number'}, tags: {type:'array', items:{type:'string'}} }, required: ['id','price'] };\nconsole.log(ajv.validate(v1, {id:'a', price:9}) );\nconsole.log(ajv.validate(v2, {id:'a', price:9, tags:['new']} ));\n```\n\n## Follow-up Questions\n- How would you phase in v2 while keeping v1 fully functional?\n- How would you automate schema drift alerts in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-20",
        "questionId": "q-14989",
        "question": "Design a beginner-friendly test plan for a REST API endpoint POST /events that ingests a batch of up to 100 events, each with id, type, and payload. After submission, the API returns 202 and a jobId; status can be polled at GET /events/{jobId}/status to reflect progress and partial results. Include validation for input schema, idempotency via eventId, concurrency handling, retry/backoff, and eventual consistency checks across retries; propose concrete test cases and tooling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan end-to-end tests for a partner webhook endpoint /webhooks/payments using HMAC-SHA256 with rotating keys. Verify idempotency with eventId, dedupe concurrent posts, simulate key rotation with zero",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant tokens and a sandboxed tenant namespace; simulate bursty load with k6 targeting SSE endpoints, aiming for 99th percentile latency under SLA. Validate per-tenant event order via monotoni",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test plan should generate a valid HMAC for the payload to simulate a real client, expect 200/202 for valid, 401 for invalid. Reuse same Idempotency-Key to prove idempotent behavior within 5-minute win",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Test Plan for POST /events Batch Ingestion API ### 1. Input Validation - Verify batch accepts maximum 100 events - Validate each event contains required fields: id, type, payload - Test payload size limits and type c…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question assesses practical API testing skills with emphasis on async processing patterns, idempotency considerations, and concurrency handling in real-world ingestion systems. It evaluates a candidate's understanding of resilience patterns and end-to-end validation strategies.\n\n## Key Concepts Covered\n- Input validation and schema enforcement\n- Idempotency implementation and verification\n- Async job management with status polling\n- Concurrency and race condition detection\n- Retry logic and exponential backoff\n- Eventual consistency verification\n- Test tooling selection and test data strategies\n\n## Code Example\n```javascript\n// Test skeleton using fetch\nconst submitBatch = async (events) => {\n  const response = await fetch('/events', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ events })\n  });\n  return { status: response.status, jobId: response.json().jobId };\n};\n\nconst pollStatus = async (jobId) => {\n  let status;\n  do {\n    const res = await fetch(`/events/${jobId}/status`);\n    status = await res.json();\n    await new Promise(r => setTimeout(r, 1000));\n  } while (status.state === 'processing');\n  return status;\n};\n\n// Test: idempotency\nconst result1 = await submitBatch([{ eventId: 'e1', type: 'click', payload: {} }]);\nconst result2 = await submitBatch([{ eventId: 'e1', type: 'click', payload: {} }]);\nassert(result1.jobId !== result2.jobId);\nconst finalStatus = await pollStatus(result2.jobId);\nassert(finalStatus.eventsProcessed === 1);\n```\n\n## Follow-up Questions\n- How would you simulate long-running jobs and timeouts in your tests?\n- How would you verify exactly-once delivery semantics for repeated event submissions?\n- What strategies would you use to test partial failure scenarios within a batch?\n- How would you handle rate limiting in your test suite?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-21",
        "questionId": "q-1744",
        "question": "Design a test for a bulk user import API: POST /imports/users accepts up to 100k records, uses an idempotency-key, and publishes per-record results to a downstream analytics queue. Specify test data, idempotency scenarios, partial downstream failures, retry/backoff, and observability. Include concrete cases, tooling, and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## GraphQL Federation Gateway API Testing Plan ### 1. Testing Scope Overview **System Under Test:** - GraphQL Federation Gateway (single API endpoint) - Multiple per-tenant subgraphs with schema variants - Persisted que…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Idempotent bulk import test: POST /imports/users with 100k records and an idempotency-key. Re-send with the same key to verify no duplicates. Inject partial downstream analytics failures to validate p",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "**Test Cases** - **400 Bad Request**: Missing or invalid required fields (e.g., missing pickup_location or invalid passenger_count) - **401 Unauthorized / 403 Forbidden**: Missing or invalid X-API-KEY header; tenant not…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a plan that uses OpenAPI-based contract validation for POST /videos payloads, replay-safe idempotent submit with idempotency keys, chaotic backpressure simulation via k6 or Locust, and concurre",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests a realistic bulk ingest with idempotency, partial failures, and observability in a distributed pipeline.\n\n## Key Concepts\n- Bulk ingestion, idempotency, per-record status\n- Asynchronous downstream processing and retries\n- Observability: metrics, logs, traces\n\n## Code Example\n```javascript\n// Pseudo-test skeleton showing idempotent POST and retry expectations\n```\n\n## Follow-up Questions\n- How would you simulate 100k records efficiently?\n- How would you verify no duplicates on retry?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-22",
        "questionId": "q-6474",
        "question": "Design a beginner-friendly, practical test plan for an API endpoint GET /reports/daily that aggregates data from two downstream services: /sales/daily and /customers/daily. Validate behavior under latency, partial failures, and retries: 200 with partial data and a warning header when one downstream times out, 503 when both fail, idempotent retries with backoff, and correct error payloads? Include tooling suggestions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test approach: POST /orders with Idempotency-Key and X-ORG-ID per tenant; verify 201 created with order_id and idempotent retries return the same id without duplication. Simulate pricing service slow/",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose contract-driven tests (PACT) for REST and SSE payloads per tenant, validating ON/OFF flag states and optional fields. Use RBAC checks under bursts, ensure event ordering and dedup under SSE re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose resilience-focused tests: mock downstream latency and failures for /sales/daily and /customers/daily; assert 200 with partial payload and a warning header when one downstream times out; assert",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement comprehensive load testing with gradual ramp-up using tools like k6 or JMeter to simulate realistic traffic patterns. Add retry logic with exponential backoff for 500 errors, implementing circuit breakers to p…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests reliability of composite APIs with dependent services.\n\n## Key Concepts\n- Downstream latency, timeouts, partial failures, circuit breakers\n- Idempotent retries, exponential backoff, observability\n- End-to-end timing and schema validation\n\n## Code Example\n```javascript\n// Pseudo test using jest + nock\nconst nock = require('nock');\nconst request = require('supertest');\nconst app = require('../app');\ntest('partial data when one downstream times out', async () => {\n  nock('http://sales').get('/daily').delay(3000).reply(200, {sales: 1000});\n  nock('http://customers').get('/daily').reply(200, {customers: 50});\n  const res = await request(app).get('/reports/daily');\n  expect(res.statusCode).toBe(200);\n  expect(res.body).toHaveProperty('sales');\n  expect(res.headers).toHaveProperty('x-downstream-warn');\n});\n```\n\n## Follow-up Questions\n- How would you test circuit-breaker behavior under sustained latency?\n- What metrics would you validate in your observability stack?",
        "difficulty": "beginner"
      },
      {
        "id": "api-testing-q-23",
        "questionId": "q-16041",
        "question": "Design an end-to-end automated test plan for a REST ingestion endpoint /telemetry/{tenantId}/ingest that accepts chunked JSON lines, enforces per-tenant quotas, deduplicates by message_id, validates schema, and writes to a distributed sink. Include burst traffic, duplicates, out-of-order arrivals, quota propagation, and downstream outages; specify tooling (k6, Locust, Postman, Kafka) and concrete test cases?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Submit /orders with idempotency_key 'k1' twice; second response must be same order and no duplicates. Simulate broker outage during commit to verify compensating/rollback behavior and eventual retry.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an end-to-end automated test plan for a REST ingestion endpoint /telemetry/{tenantId}/ingest that accepts chunked JSON lines, enforces per-tenant quotas, deduplicates by message_id, validates s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a plan using a WebSocket tester (50 tenants) to subscribe to price_update and status_change, verify each message contains tenant_id and correct event type, measure 95th percentile latency unde",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test signature validation by sending tampered payloads (expect 401). Enforce idempotency by replaying the same event and ensuring no duplicate writes to Redis. Validate ordering vs. out-of-order event",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests realism of streaming ingestion under multi-tenant quotas; validates idempotency, schema, and failure handling across a distributed sink.\n\n## Key Concepts\n\n- Per-tenant quotas and propagation\n- Deduplication by message_id\n- Chunked streaming ingestion\n- Exactly-once vs at-least-once semantics\n- Downstream outage resilience\n\n## Code Example\n\n```javascript\n// pseudo-test demonstrating deduplication check\nconst req1 = { tenantId: 't1', message_id: 'm1', data: { /* ... */ } }\nconst req2 = { tenantId: 't1', message_id: 'm1', data: { /* ... */ } }\nawait ingest('/telemetry/t1/ingest', [req1, req2])\n// assert only one write to sink\n```\n\n## Follow-up Questions\n\n- How would you parameterize tests for tenants with different quotas?\n- How to verify idempotent guarantees across retries?",
        "difficulty": "intermediate"
      },
      {
        "id": "api-testing-q-24",
        "questionId": "q-27321",
        "question": "Design a practical API-testing plan for GET /price/{productId} in a multi-region, multi-tenant pricing API. Prices come from regional feeds, support per-tenant currency, and a cache with TTL. Include tests for currency conversion accuracy using latest FX data, regional failover, per-tenant data isolation, cache invalidation on updates, and p95 latency under simulated outages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: test idempotency with the same Idempotency-Key across two rapid requests; expect a single charge and identical transaction_id. Test different keys yield separate charges. Simulate timeouts and r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Idempotency-Key header on POST /payments. The first call should create a payment and return 201 with a transaction_id. Repeating with the same key must return 200 with the same transaction_id",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: validate GET /price/{productId} across tenants and currencies using the latest FX data; simulate regional feed outages to exercise fallback pricing; verify per-tenant data isolation with paralle",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Tests should cover idempotent behavior with a shared idempotency-key, verifying first call returns 201 and a job_id while duplicates return the same job_id. Validate retry semantics on transient netwo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis scenario captures real-world complexity: multi-region data, currency conversion, caching, tenant isolation, and outages. It tests end-to-end thinking, data freshness, and reliability guarantees.\n\n## Key Concepts\n- Currency conversion accuracy with FX data and rounding\n- Cache invalidation and TTL semantics\n- Regional feed failover and deterministic fallback\n- Tenants isolation and data parity across regions\n- Latency measurements and SLA compliance under outages\n\n## Code Example\n```python\ndef test_price_endpoint_currency():\n    res = client.get(\"/price/123?tenant=A&currency=EUR\")\n    assert res.status_code == 200\n    assert isinstance(res.json()[\"price\"], float)\n```\n\n## Follow-up Questions\n- How would you simulate FX drift and measure impact on SLA?\n- How do you test cache stampedes and regional consistency during outages?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-11T12:42:08.425Z",
    "version": 1
  }
]