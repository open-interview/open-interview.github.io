[
  {
    "id": "test-gcp-data-engineer",
    "channelId": "gcp-data-engineer",
    "channelName": "GCP Data Engineer",
    "title": "GCP Data Engineer Knowledge Test",
    "description": "Test your understanding of GCP Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "gcp-data-engineer-q-0",
        "questionId": "q-6912",
        "question": "Design a real-time data quality and governance pipeline on GCP for multi-tenant IoT telemetry (1-2 TB/day) ingested from Pub/Sub into per-tenant BigQuery tables across two regions. Include ingestion-time anomaly detection, per-tenant quarantine, auto-remediation via rule engine, lineage to Data Catalog, and dashboards for data quality with rollback for quarantined data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route Pub/Sub streams through Dataflow streaming templates that shard by tenant and write to regional BigQuery datasets across two regions. Implement ingestion-time quality checks including schema drift validation, rang…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a GCP streaming pipeline that ingests 5 TB/day of JSON events from Pub/Sub, runs a Vertex AI anomaly model in real time, writes per-tenant enriched results to BigQuery, and publishes alerts bac",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Dataflow-based streaming pipeline: Pub/Sub → Dataflow, where each event undergoes real-time validation against a centralized schema registry. When schema drift is detected or anomaly scores exceed configured th…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant quality contracts in Data Catalog; Dataflow validates Pub/Sub events against a TDV-derived schema, emitting per-tenant quality metrics to Cloud Monitoring. Implement anomaly detection with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes end-to-end streaming data quality, per-tenant isolation, regional replication, and governance integration under real-world failure modes. It also tests operational thinking around dead-letter handling, auto-remediation, rollback, and observability.\n\n## Key Concepts\n\n- Streaming pipelines: Pub/Sub → Dataflow\n- Tenant-aware routing and regional BigQuery\n- Data quality gates: schema drift, range checks, null handling\n- Dead-letter queues and auto-remediation\n- Data Catalog lineage and access controls\n- Rollback and backfill strategies\n\n## Code Example\n\n```java\n// Dataflow pipeline with quality checks\nPipeline p = Pipeline.create(options);\n\np.apply(\"ReadFromPubSub\", PubsubIO.readStrings()\n    .fromTopic(\"projects/PROJECT/topics/iot-telemetry\"))\n .apply(\"ParseAndValidate\", ParDo.of(new ValidateAndEnrichFn()))\n .apply(\"TenantRouting\", ParDo.of(new TenantRouterFn()))\n .apply(\"QualityChecks\", ParDo.of(new DataQualityValidatorFn()))\n .apply(\"WriteToBigQuery\", BigQueryIO.writeTableRows()\n    .to(new DynamicTenantDestinations())\n    .withSchema(TELEMETRY_SCHEMA)\n    .withWriteDisposition(BigQueryIO.Write.WriteDisposition.WRITE_APPEND));\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-1",
        "questionId": "q-17880",
        "question": "On GCP, design a real-time analytics pipeline ingesting 5 TB/day of JSON telemetry from 1k tenants via Pub/Sub, with per-tenant isolation and privacy (masking at ingestion), end-to-end lineage in Data Catalog, data contracts, and a partner-facing aggregated feed via Data Exchange. Include multi-region resilience, 24h per-tenant rollback, schema evolution, and cost-optimized storage with partitioned BigQuery tables and BI Engine?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming Dataflow pipeline ingesting 2-5 TB/day from Pub/Sub, writing per-tenant, partitioned BigQuery tables and exposing dashboards in Looker. Use Data Catalog for lineage and Cloud Monito",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Dataflow streaming job to ingest 1.5 TB/day from Pub/Sub, apply per-tenant schema validation from a Registry, run quality gates (nulls, type, ranges), and write to per-tenant BigQuery datasets w",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route 5 TB/day via Pub/Sub into a streaming Dataflow pipeline that writes masked CLEAN data to per-tenant BigQuery datasets, with RAW kept regional for rollback. Use Authorized Views/row-level securit",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Architect a 3-region cross-tenant streaming pipeline: 4 TB/day of JSON clickstream from Pub/Sub, Dataflow processing with per-tenant masking via DLP; regional raw Parquet in Cloud Storage; per-tenant",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes real-world scale dataflow design with privacy, lineage, contracts, and partner data sharing. It tests cross-region resilience, rollback strategies, and cost controls.\n\n## Key Concepts\n- Pub/Sub streaming and Dataflow templates\n- Per-tenant isolation via datasets/authorized views\n- Data Catalog lineage and Data Exchange contracts\n- Multi-region storage, time travel, and GCS backups\n- Partitioned BigQuery tables and BI Engine\n\n## Code Example\n```javascript\n// Pseudo Beam mask example (high-level)\nPardo.of(element => maskPII(element));\n```\n\n## Follow-up Questions\n- How would you test schema evolution without breaking tenants?\n- How would you validate privacy masking at scale?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-2",
        "questionId": "q-6846",
        "question": "Design a cross-region data platform on GCP for two tenants ingesting 3 TB/day of JSON telemetry via Pub/Sub. Each tenant has isolated BigQuery datasets and Vertex AI Feature Stores. Include per-tenant privacy controls (masking/DP), end-to-end lineage via Data Catalog, backward-compatible schema evolution, and a 24h rollback plan with backfill and testing. Outline architecture, data models, and failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a real-time pipeline that ingests 10 TB/day of mixed JSON/Avro events from Pub/Sub for a multi-tenant SaaS. Use Dataflow templates with the Splittable DoFn optimization to process records in parallel. Implement p…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest data with tenant_id as the partitioning key; use Dataflow with a dynamic DoFn that validates each message against a per-tenant schema version stored in Data Catalog. On schema drift detection, automatically promo…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Beam pipeline in Python: read Pub/Sub JSON, flatten nested fields, enrich with a Cloud Bigtable user-tier lookup, validate required fields, and upsert to a daily-partitioned BigQuery table. U",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage Pub/Sub -> Dataflow pipelines to regional BigQuery datasets and per-tenant Vertex AI Feature Stores. Enforce isolation, per-tenant masking/DP, and end-to-end lineage via Data Catalog. Support",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests multi-tenant isolation, real-time streaming, and integration of Vertex AI Feature Stores with governance and privacy controls. Also probes rollback, testing, and failure handling across regions.\n\n## Key Concepts\n\n- Pub/Sub to Dataflow pipelines\n- Per-tenant BigQuery datasets and Vertex AI Feature Stores\n- Data Catalog lineage and masking/DP\n- Backward-compatible schema evolution\n- Point-in-time recovery and feature store snapshots for rollback\n\n## Code Example\n\n```yaml\n# high-level configuration sketch\ningest:\n  source: Pub/Sub\n  flow: Dataflow\ntenants:\n  - id: tenant1\n    bigquery: project/tenant1\n    feature_store: tenant1/fs\n  - id: tenant2\n    bigquery: project/tenant2\n    feature_store: tenant2/fs\nprivacy:\n  masking: true\n  differential_privacy: true\nlineage:\n  publisher: Data Catalog\n```\n\n## Follow-up Questions\n\n- How would you monitor per-tenant data freshness and latency?\n- How would you test schema drift across tenants and rollback safely?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-3",
        "questionId": "q-8696",
        "question": "In GCP, design a beginner-friendly pipeline to ingest 3-5 GB/day of JSON events across tenants, store in BigQuery with per-tenant CMEK encryption and daily partitions, Dataflow (Beam) masks/ redacts PII on the fly, and validates required fields; route malformed to a dead-letter Pub/Sub and archive originals to Cloud Storage; implement per-tenant 24h rollback and a basic end-to-end test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a 2 TB/day multi-tenant streaming pipeline on GCP (Pub/Sub -> Dataflow -> per-tenant BigQuery). Introduce a versioned schema registry in Data Catalog, enforce compatibility checks on changes",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant contracts in Data Catalog; Dataflow streaming with DoFn checks for schema drift, nulls, and value ranges; quarantine bad records; strict per-tenant BigQuery; lineage in Data Catalog; 24h ro",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Dataflow (Python) parses Pub/Sub JSON, validates fields, masks PII, and writes to a daily-partitioned BigQuery table keyed by tenant_id with insertId dedupe. Malformed events go to a dead-letter Pub/S",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Pub/Sub to stream tenant-scoped events (1-2 TB/day), Dataflow for real-time feature extraction, and write per-tenant features into Vertex AI Feature Store (namespaced by tenant). Enforce IAM/VPC p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses end-to-end pipeline design with tenant isolation, on-the-fly masking, and a basic rollback strategy.\n\n## Key Concepts\n- Pub/Sub ingestion; Dataflow (Beam) transforms; BigQuery partitioning by date and tenant.\n- CMEK per-tenant encryption; data masking on the fly; dedupe with insertId.\n- Dead-letter queues; Cloud Storage archiving; 24h rollback strategy.\n\n## Code Example\n```javascript\n// Placeholder: actual implementation would be in Beam Python, not shown here\n```\n\n## Follow-up Questions\n- How would you test masking rules and schema changes?\n- How would you scale Dataflow workers for peak days without breaking tenancy bounds?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-4",
        "questionId": "q-962",
        "question": "In a GCP streaming pipeline, Pub/Sub feeds millions of events into BigQuery via Dataflow. Out-of-order arrivals and duplicates occur. Design an idempotent sink using a staging table and a final partitioned table, leveraging insertId for dedup and a MERGE strategy to upsert into the final table. Outline concrete steps and trade-offs to implement?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a 1,000-tenant streaming analytics platform: tenants share a single BigQuery dataset secured with Row-Level Security by tenant_id. Ingest 5 TB/day of JSON via Pub/Sub, process with Dataflow to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a staging BigQuery table where Dataflow writes with insertId derived from a stable key (user_id + event_id + event_time). Periodically MERGE from staging into a final partitioned table, deduplicat",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Parse Pub/Sub messages with a simple Beam DoFn that validates required fields (user_id, event_type) and coerces amount to float. Valid records go to BigQuery; invalid ones emit to a dead-letter Pub/Su",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a Dataflow (Beam) pipeline that fetches Pub/Sub, validates each event against a JSON Schema stored in GCS (cached per bundle as a side input), routes valid events to a partitioned Bi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical data quality, idempotent sinks, and operational design using Dataflow + BigQuery features.\n\n## Key Concepts\n\n- Idempotent sink\n- insertId dedup\n- staging vs final table\n- MERGE in BigQuery\n- allowed lateness and partitioning\n- quotas and monitoring\n\n## Code Example\n\n```sql\nMERGE INTO dataset.final AS F\nUSING dataset.staging AS S\nON F.user_id = S.user_id AND F.event_time = S.event_time\nWHEN NOT MATCHED THEN INSERT (user_id, event_time, event_id, action, payload)\nWHEN MATCHED THEN UPDATE SET action = S.action, payload = S.payload;\n```\n\n## Follow-up Questions\n\n- How to handle tombstones?\n- How to scale MERGE with sharded partitions?\n- How would you instrument observability for dedup latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-5",
        "questionId": "q-3251",
        "question": "You operate a multi-tenant analytics service on GCP with 50 tenants. 5 GB/day of JSON events arrive via Pub/Sub. Design a beginner-friendly pipeline that ingests events with Dataflow, stores them in a single BigQuery table partitioned by ingestion_date, and includes tenant_id. Enforce per-tenant access using BigQuery authorized views (or per-tenant filters), support additive schema evolution, deduplicate by event_id, handle late data up to 1 hour, and capture end-to-end lineage in Data Catalog. Provide architecture sketch and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a Dataflow (Beam) pipeline that fetches Pub/Sub, validates each event against a JSON Schema stored in GCS (cached per bundle as a side input), routes valid events to a partitioned Bi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub to feed Dataflow streaming jobs that validate JSON schemas, mask PII via DLP, and emit per-tenant records to separate BigQuery datasets in two regions. Enforce IAM on a per-dataset basis,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest Pub/Sub JSON with Dataflow into a single BigQuery table partitioned by ingestion_date, including tenant_id. Enforce per-tenant access via authorized views filtering on tenant_id. Deduplicate by",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant Vertex AI Feature Store with isolated feature stores and versioned schemas. Ingest 1–2 TB/day from Pub/Sub via Dataflow, applying PII masking/tokenization with DLP, and write both raw and m",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a practical, beginner-friendly pipeline with tenant isolation, governance, and lineage on GCP.\n\n## Key Concepts\n\n- Dataflow streaming ingestion from Pub/Sub\n- BigQuery single table with partitioning by ingestion_date and tenant_id\n- Authorized views for per-tenant access control\n- Additive schema evolution with nullable fields\n- Deduplication by event_id\n- Late data handling (1 hour)\n- Data Catalog-based lineage\n\n## Code Example\n\n```javascript\n// Pseudo Dataflow-style transform (illustrative)\nfunction transform(eventJson){\n  const obj = JSON.parse(eventJson);\n  obj.ingestion_date = (obj.event_time ? obj.event_time : new Date()).toISOString().slice(0,10);\n  return obj;\n}\n```\n\n## Follow-up Questions\n\n- How would you test tenant isolation and access controls?\n- What are trade-offs between a single table vs per-tenant tables?\n- How would you roll out additive schema changes with minimal downtime?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-6",
        "questionId": "q-2855",
        "question": "Design a real-time cross-tenant data pipeline on GCP ingesting 1-2 TB/day of JSON events from Pub/Sub into two regions. Implement per-tenant privacy via field-level masking and tokenization using DLP and Cloud KMS, enforce data residency rules, provide end-to-end lineage with Data Catalog, support forward/backward schema evolution, and build a testing strategy with synthetic tenants. Include a rollback plan for privacy policy changes within 24 hours?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a telemetry pipeline with Pub/Sub, Dataflow, and BigQuery, enforcing per-service data contracts via Pub/Sub Schema Registry and Data Catalog tagging. Redact PII at ingest, store raw in Cloud S",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-tier streaming: a BigQuery hot table partitioned by ingestion_date with 90-day partition expiration for PII, plus a daily Cloud Storage Parquet archive for audits. Dataflow templates ingest Pub/Su",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a pipeline from Pub/Sub to per-tenant BigQuery datasets using Dataflow (Beam). Validate required fields, parse event_time to TIMESTAMP, and implement idempotent writes via MERGE with a tenant_",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A robust answer identifies Pub/Sub -> Dataflow with per-tenant masking using DLP and Cloud KMS, writes to regional BigQuery tables, ensures tenant-scoped lineage in Data Catalog, implements schema evo",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nEvaluates capability to design a scalable, privacy-conscious streaming pipeline across regions, with governance and rollback considerations.\n\n## Key Concepts\n\n- Real-time ingestion with Pub/Sub and Dataflow\n- Per-tenant privacy via DLP masking and Cloud KMS\n- Data residency controls and multi-region writes\n- End-to-end lineage in Data Catalog\n- Schema evolution and backward/forward compatibility\n- Synthetic data testing and 24-hour rollback planning\n\n## Code Example\n\n```javascript\n// Pseudo Dataflow sketch for masking a tenant field using DLP\n```\n\n## Follow-up Questions\n\n- How would you test privacy-policy rollbacks across tenants during peak load?\n- What monitoring would you add to detect masking failures or schema drift?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-7",
        "questionId": "q-20944",
        "question": "On GCP, design a privacy-preserving ML feature-sharing platform that enables cross-tenant training: ingest 10 TB/day of telemetry via Pub/Sub, materialize per-tenant features in a shared feature store with masking, expose only derived features to partners, enforce data contracts in Data Catalog, maintain end-to-end lineage from source to Vertex AI experiments, and ensure reproducibility with experiments, while meeting cost and security constraints across projects and VPCs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant feature store backed by BigQuery views with exchangeable derived features. Apply masking and tokenization at ingestion to protect raw data. Share only vetted, derived features with partners via Data…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Datastream to ingest PostgreSQL changes into Pub/Sub; a streaming Dataflow pipeline deduplicates and routes events to per-tenant BigQuery tables (partitioned by date and tenant_id); enforce per-te",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an automated GitOps workflow that provisions a dedicated GCP project for each tenant, creates a BigQuery dataset with per-tenant IAM policies, applies Data Catalog tags for governance enforcement, and bootstra…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Dataflow (Beam) in batch mode to read daily JSONL from Cloud Storage, parse and flatten, write to a staging table, then MERGE into a final partitioned table (load_date) clustered by record_id for",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates end-to-end ML feature sharing and governance across tenants on GCP, emphasizing privacy, lineage, and reproducibility rather than data ingestion alone.\n\n## Key Concepts\n- Vertex AI Experiments and Feature Store integration\n- Data Catalog, Data Exchange, and data contract enforcement\n- Per-tenant isolation with masking and tokenization\n- IAM Conditions and VPC Service Controls\n- End-to-end lineage, drift monitoring, and cost governance\n\n## Code Example\n```yaml\n# IAM policy snippet for partner access\nbindings:\n- role: roles/aiplatform.user\n  members: [\"group:partners@example.com\"]\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-8",
        "questionId": "q-19286",
        "question": "On GCP, design an advanced cross-cloud analytics pipeline that ingests 5 TB/day of JSON events from Pub/Sub and surfaces per-tenant aggregates in a multi-cloud data mesh using BigQuery Omni (GCP and AWS). Implement per-tenant row-level security, CMEK, Data Catalog lineage, and partner sharing via Data Exchange. Include 24h per-tenant rollback, data contracts, and a rigorous test/rollback plan with synthetic tenants?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a tenant-scoped streaming pipeline: Pub/Sub -> Dataflow streaming with per-tenant transforms, write to partitioned BigQuery tables (tenant_id as partition). Mask PII in a dedicated DoFn and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a cross-cloud, per-tenant analytics pipeline that ingests 5 TB/day of JSON events from Pub/Sub, processes them with Dataflow for real-time enrichment, and stores per-tenant aggregates using BigQuery Omni acros…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage Pub/Sub for ingest, Dataflow for streaming feature computation, and per-tenant BigQuery partitions with CMEK for raw data. Use Vertex AI Feature Store for online features, feeding a real-time",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a dual-region Dataflow streaming pipeline that reads from Pub/Sub, applies per-tenant tokenization and privacy masking using a dedicated key management service, and writes aggregated metrics to partitioned BigQuery…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to architect cross-cloud data meshes, enforce strict tenant isolation, and manage lineage, governance, and rollback procedures in production environments.\n\n## Key Concepts\n- BigQuery Omni and multi-cloud data mesh architecture\n- Row-level security and customer-managed encryption keys (CMEK)\n- Data Catalog lineage tracking and Data Exchange for partner sharing\n- End-to-end reliability: 24-hour rollback, testing, and recovery drills\n\n## Code Example\n```javascript\n// Pseudo Dataflow config for streaming enrichment\nconst pipeline = createDataflowPipeline({\n  inputSubscription: 'projects/PROJECT/subscriptions/events',\n  outputTable: 'omni:aws:us-east-1.dataset.table',\n  windowSize: '1m',\n  tenantIdField: 'tenant_id'\n});\n\npipeline.apply(ParDo.of(new EnrichmentFn()))\n  .apply(Window.into(FixedWindows.of(60)))\n  .apply(GroupByKey.create())\n  .apply(ParDo.of(new AggregateFn()));\n```",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-9",
        "questionId": "q-27358",
        "question": "On GCP, design a 5 TB/day Pub/Sub→BigQuery streaming pipeline with per-tenant isolation and privacy controls. New angle: integrate automated per-tenant data quality scoring (completeness, validity, dedup) and a remediation workflow that backfills the last 24 hours via a separate Beam backfill job. Include schema-drift detection against a canonical schema, publishing drift events to Data Catalog and Data Exchange. Ensure 24h rollback via BigQuery time travel and per-tenant snapshots, with CMEK and Authorized Views?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a two-layer scheme: a stable BigQuery table with known fields and a separate extra_json column to capture drift. The Dataflow job maps incoming JSON to the known schema; unknown fields are stored",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Pub/Sub → Dataflow streaming to per-tenant BigQuery partitions. Add a data-quality module that computes per-tenant scores (completeness, validity, dedup) and kicks off a 24h backfill job to remediate",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Two Dataflow pipelines: an ingress path that reads Pub/Sub, applies tenant-specific masking from a centralized policy engine, routes data to per-tenant BigQuery partitions in US/EU, and a governance versioned pipeline t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cost-aware, multi-tenant streaming pipeline: Pub/Sub feeds 1 TB/day of JSON events; Dataflow writes per-tenant, day-partitioned BigQuery tables (tenant_id, user_id). Run a shared Vertex AI an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to extend streaming pipelines with governance, quality, and rollback capabilities at scale. The new angle probes data quality automation, schema drift handling, and granular rollback while maintaining tenant isolation and data lineage.\n\n## Key Concepts\n- Per-tenant data quality scoring and remediation workflows\n- Streaming backfill for 24h window\n- Schema drift detection and drift event publishing\n- Data Catalog/Data Exchange integration for governance and sharing\n- Time travel rollback and per-tenant snapshots with CMEK and Authorized Views\n\n## Code Example\n```javascript\n// Pseudo-structure for quality scoring and backfill trigger\nconst tenantScore = computeQuality(event);\nif (tenantScore < threshold) triggerBackfill(event.tenantId, 24);\n```\n\n## Follow-up Questions\n- How would you test backfill latency and drift detection in CI/CD?\n- What metrics and alerting would ensure timely remediation without escalating noise?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-10",
        "questionId": "q-11060",
        "question": "On GCP, design a 5 TB/day multi-tenant streaming analytics pipeline that ingests JSON events via Pub/Sub. Use Dataflow for real-time processing, implement per-tenant field-level tokenization/masking, and apply per-tenant differential privacy on aggregates with budgets (epsilon ~1, delta 1e-5). Store per-tenant aggregates in BigQuery; expose to a partner via Data Exchange; maintain end-to-end lineage in Data Catalog. Include 24h per-tenant rollback, data contracts, and a testing/rollback strategy with concrete data models and failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming Dataflow pipeline to ingest 5 TB/day of multi-tenant JSON events via Pub/Sub, implementing field-level tokenization for sensitive data protection while computing per-tenant aggregates with differentia…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a Dataflow-based streaming pipeline ingesting 100 GB/day Pub/Sub JSON per 3 tenants, using a versioned event schema with a Data Catalog registry, canary rollout, and per-tenant views. Add DLP-b",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a Dataflow (Beam) pipeline that fetches Pub/Sub, validates each event against a JSON Schema stored in GCS (cached per bundle as a side input), routes valid events to a partitioned Bi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Dataflow streaming job that reads from Pub/Sub and writes to per-tenant BigQuery tables (using tenant_id as a table suffix), with insertId-based deduplication and required-field validation (tenant_id, event_…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEvaluates the design of privacy-preserving, scalable streaming architectures with cross-tenant governance, differential privacy budgeting, and comprehensive rollback capabilities.\n\n## Key Concepts\n\n- Dataflow streaming pipelines with per-tenant isolation and resource management\n- Differential privacy budget allocation, tracking, and enforcement mechanisms\n- Field-level tokenization and dynamic data masking strategies\n- BigQuery Data Exchange for secure partner data sharing with fine-grained controls\n- Data Catalog for comprehensive end-to-end lineage and metadata governance",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-11",
        "questionId": "q-19416",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. New angle: implement per-tenant data retention policies with automated archival to Cloud Storage after 30 days and removal from BigQuery, plus external table access for long-term analytics. Include test plan and rough cost estimate?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest 2 TB/day per tenant from Pub/Sub into a per-tenant Dataflow streaming job that writes to separate BigQuery datasets; mask PII on ingest using the DLP API; publish per-tenant lineage to Data Cat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant CMEK in Cloud KMS (one key per tenant, scoped to its project). Dataflow reads Pub/Sub, masks PII via a policy store, then writes to BigQuery tables encrypted with CMEK. Data Catalog tag",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed pipeline: a streaming Dataflow job reads Pub/Sub JSON events, validates a minimal schema, and writes to per-tenant partitioned BigQuery tables. Data older than 30 days is archived automatical",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose streaming ingestion from Pub/Sub into Dataflow; join with tenant data in BigQuery Data Clean Room to enable privacy-preserving cross-tenant joins with a partner; per-tenant isolation via separ",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a realistic, beginner-friendly data-pipeline design that includes governance (retention), cost awareness, and analytics access to archived data. It also evaluates ability to map tenant isolation to BigQuery partitions and to leverage external tables for archival analytics.\n\n## Key Concepts\n\n- Streaming ingestion with Pub/Sub and Dataflow\n- Tenant isolation with per-tenant BigQuery partitions\n- Automated archival to Cloud Storage after 30 days\n- External tables to query archived data without loading it back\n- Retention policies, scheduling, and cost estimation\n\n## Code Example\n\n```python\nimport apache_beam as beam\n# Pseudo snippet: validate schema, route per-tenant, and write to partitioned table\nclass RouteTenant(beam.DoFn):\n  def process(self, element):\n    # extract tenant_id, validate fields, emit to (tenant, record)\n    yield (tenant_id, record)\n```\n\n## Follow-up Questions\n\n- How would you test archival integrity and external table performance?\n- How would you handle tenants with different retention windows?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-12",
        "questionId": "q-16647",
        "question": "On GCP, design a beginner-friendly pipeline that ingests 100 GB/day of JSON clickstream events via Pub/Sub into BigQuery. Implement a hybrid approach: streaming from Pub/Sub plus nightly batch replay from Cloud Storage to backfill late events. Ensure idempotent writes using a stable event_id, per-tenant isolation (partitioned BigQuery tables), and a data contract stored in Data Catalog. Include testing for backfills and a simple rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Dataflow streaming reads Pub/Sub, parses JSON, and upserts into per-tenant BigQuery tables using MERGE on event_id. A nightly batch loads backfilled events from GCS and upserts, deduplicating by event",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a Dataflow streaming pipeline with Pub/Sub input, a dynamic per-tenant schema registry in Data Catalog, PII masking via Cloud DLP, field-level schema validation, and BigQuery streaming inserts for",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Validate each message against a fixed schema in a Dataflow custom DoFn. Valid records are written to the primary BigQuery table, while invalid records are routed to both a dedicated errors BigQuery table and Cloud Stora…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage Pub/Sub Schemas and a Cloud Spanner-backed schema registry; each event has a schema_version. Dataflow maps to a canonical row and writes to a tenant-partitioned BigQuery table. Enforce compat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to combine streaming and batch workflows, ensure idempotent writes, and manage per-tenant isolation with simple data contracts.\n\n## Key Concepts\n\n- Dataflow streaming + batch backfill\n- BigQuery MERGE on event_id\n- Per-tenant partitioning and data contracts in Data Catalog\n- Testing and rollback strategies\n\n## Code Example\n\n```javascript\n// SQL-like pseudo\nMERGE INTO `tenant.dataset.table` AS t\nUSING (SELECT @event_id AS event_id, ...) AS s\nON t.event_id = s.event_id\nWHEN MATCHED THEN UPDATE SET ...\nWHEN NOT MATCHED THEN INSERT (...);\n```\n\n## Follow-up Questions\n\n- How would you implement dedup across backfill batches?\n- How would you handle late-arriving events with order guarantees?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-13",
        "questionId": "q-22297",
        "question": "On GCP, design a real-time fintech analytics pipeline for 2,000 TPS across tenants. Ingest via Pub/Sub, process with Dataflow streaming with exactly-once semantics and tenant-id dedupe, handle late-arriving events with watermarking. Persist to Parquet in Cloud Storage, then incremental load to a BigQuery serving layer. Implement per-tenant access controls with row-level security, data contracts and lineage in Data Catalog, plus a 24h rollback, backfill, testing, and fault-injection plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming pipeline using Dataflow to parse JSON from Pub/Sub and write to a shared raw BigQuery table, exposing per-tenant views for isolation. Implement: 1) schema registry in Data Catalog; 2) per-tenant Autho…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a multi-region streaming pipeline with Pub/Sub ingestion, Dataflow processing across two regions, and per-tenant BigQuery tables for feature vectors. Compute real-time drift metrics including distribution shif…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose exactly-once streaming in Dataflow with a tenant-keyed dedupe (tenant_id, txn_id) and watermarks to handle late arrivals. Use Pub/Sub arrival order with idempotent sink to BigQuery via Parquet",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Architect a 3-region cross-tenant streaming pipeline: 4 TB/day of JSON clickstream from Pub/Sub, Dataflow processing with per-tenant masking via DLP; regional raw Parquet in Cloud Storage; per-tenant",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes real-world streaming semantics, multi-tenant isolation, and data contracts in GCP with practical rollback.\n\n## Key Concepts\n\n- Dataflow exactly-once processing\n- Tenant-level dedupe by (tenant_id, txn_id)\n- Watermarks for late events\n- Parquet in Cloud Storage + incremental BigQuery loading\n- Data Catalog contracts and lineage\n- 24h rollback and backfill test plan\n\n## Code Example\n\n```javascript\n// Pseudo Dataflow-like dedupe (illustrative)\nclass DedupFn {\n  process(element) {\n    const key = element.tenant_id + '|' + element.txn_id;\n    if (!state.has(key)) {\n      state.add(key);\n      return element;\n    }\n    return null;\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you verify exactly-once behavior under backpressure and late data?\n- How would you roll back a fraudulent day’s data within 24h?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-14",
        "questionId": "q-18811",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. NEW ANGLE: implement idempotent ingestion by deduplicating messages using a lightweight Dataflow DoFn keyed by (tenant_id, event_id) and a 60-second window to emit only the latest event before writing to a partitioned BigQuery table. Include data model changes, testing with synthetic duplicates, and monitoring of dedup latency and failure rates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming pipeline: Pub/Sub subscription → Dataflow (Beam) with a custom DoFn that deduplicates events by (tenant_id, event_id) using a 60-second fixed window → write to a time-partitioned BigQuery table (parti…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage Pub/Sub Schema Registry for drift alerts, Dataflow DoFns for validation and normalization, and tenant-partitioned BigQuery tables. Use Data Catalog for per-tenant data contracts and full line",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a per-tenant CMEK strategy: Ingest via Pub/Sub, Dataflow uses tenant-specific KMS keys to decrypt and mask fields on the fly, and writes to partitioned per-tenant BigQuery tables. Maintain lin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest JSON events through Pub/Sub and process with Dataflow using per-tenant schemas from a centralized registry. Quarantine malformed records to Cloud Storage, write validated data to tenant-specific BigQuery datasets…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical understanding of idempotent streaming ingestion—a common requirement when processing potentially duplicate events from Pub/Sub at scale.\n\n## Key Concepts\n- Idempotent streaming ingestion with Dataflow/Beam\n- Per-key deduplication using a composite key (tenant_id, event_id)\n- 60-second fixed window with in-flight state management\n- Partitioned BigQuery table design and schema evolution\n- Synthetic duplicate testing and observability for deduplication latency and failure rates\n\n## Data Model Changes\nAdd the following columns to support deduplication and auditing:\n- `event_id` (STRING, NOT NULL) — unique event identifier\n- `tenant_id` (STRING, NOT NULL) — tenant organization ID\n- `created_at` (TIMESTAMP) — event creation timestamp from source\n- `processing_timestamp` (TIMESTAMP, DEFAULT CURRENT_TIMESTAMP) — when the event was processed\n- `dedup_key` (STRING) — computed key for debugging (tenant_id + event_id)\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.transforms import window\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\nclass DeduplicateDoFn(beam.DoFn):\n    def __init__(self):\n        self.state_spec = beam.state_spec.Value('latest_event', coder=beam.coders.JsonCoder())\n    \n    def process(self, element, window=beam.DoFn.WindowParam):\n        key = (element['tenant_id'], element['event_id'])\n        state = self.state_spec\n        \n        # Store latest event per key in state\n        existing = self.latest_event.read()\n        if existing is None or element['created_at'] > existing['created_at']:\n            self.latest_event.write(element)\n            yield element\n\ndef run_pipeline():\n    options = PipelineOptions()\n    with beam.Pipeline(options=options) as p:\n        (p\n         | 'ReadFromPubSub' >> beam.io.ReadFromPubSub(topic='projects/my-project/topics/clickstream')\n         | 'ParseJSON' >> beam.Map(lambda x: beam.io.gcp.bigquery.parse_json_dict(x, {'tenant_id': 'STRING', 'event_id': 'STRING', 'created_at': 'TIMESTAMP', 'payload': 'STRING'}))\n         | 'AddFixedWindow' >> beam.WindowInto(window.FixedWindows(60))\n         | 'Deduplicate' >> beam.ParDo(DeduplicateDoFn())\n         | 'WriteToBigQuery' >> beam.io.WriteToBigQuery(\n             'my-project:dataset.events_table',\n             schema='event_id:STRING,tenant_id:STRING,created_at:TIMESTAMP,payload:STRING,processing_timestamp:TIMESTAMP',\n             create_disposition=beam.io.BigQueryDisposition.CREATE_IF_NEEDED,\n             write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND\n         ))\n```\n\n## Testing with Synthetic Duplicates\n```python\ndef generate_test_data():\n    base_event = {'tenant_id': 'tenant_1', 'event_id': 'event_123', 'created_at': '2024-01-15T10:00:00', 'payload': '{\"page\": \"/home\"}'}\n    \n    # Create 5 duplicates with slight timing variations\n    events = []\n    for i in range(5):\n        event = base_event.copy()\n        event['created_at'] = f'2024-01-15T10:00:0{i}'\n        events.append(event)\n    \n    return events\n\n# Test pipeline with duplicates\ndef test_deduplication():\n    test_events = generate_test_data()\n    p = beam.TestPipeline()\n    result = (p | beam.Create(test_events)\n                | beam.WindowInto(window.FixedWindows(60))\n                | beam.ParDo(DeduplicateDoFn())\n                | beam.io.WriteToBigQuery(...))\n    \n    # Assert only 1 event written\n    assert result | beam.globally() | beam.combiners.Count.Globally() == 1\n```\n\n## Monitoring Deduplication Latency and Failure Rates\nAdd custom metrics to the DoFn:\n\n```python\nclass DeduplicateDoFn(beam.DoFn):\n    def __init__(self):\n        self.dedup_latency = beam.metrics.Metrics.distribution(self.__class__, 'dedup_latency_ms')\n        self.dedup_failures = beam.metrics.Metrics.counter(self.__class__, 'dedup_failures')\n    \n    def process(self, element, window=beam.DoFn.WindowParam):\n        start_time = time.time()\n        try:\n            key = (element['tenant_id'], element['event_id'])\n            # deduplication logic\n            self.dedup_latency.update((time.time() - start_time) * 1000)\n            yield element\n        except Exception as e:\n            self.dedup_failures.inc()\n            raise\n```\n\n**Stackdriver/Dataplex Monitoring Dashboard:**\n- `dataflow.googleapis.com/job/streaming/pipeline/logical_bytes` — throughput\n- Custom metric: `dedup_latency_ms` — p50, p95, p99\n- Custom metric: `dedup_failures` — failure rate per window\n- BigQuery: `INFORMATION_SCHEMA.PARTITIONS` for table size and partition usage",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-15",
        "questionId": "q-9733",
        "question": "On GCP, design a beginner-friendly data ingestion pipeline: Pub/Sub delivers JSON events for multiple tenants; Dataflow (Beam) normalizes to a unified schema, validates required fields, and writes to a daily-partitioned BigQuery table. Implement per-tenant privacy with masking via BigQuery views, route invalid messages to a dead-letter Pub/Sub, and archive originals in Cloud Storage. Include basic monitoring and a 24h rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "In this pipeline, ingest Pub/Sub JSON per tenant, use Dataflow to normalize into a single BigQuery schema partitioned by day, validate required fields, and stream to BigQuery. Implement per-tenant pri",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Batch Dataflow (Beam) reads daily JSON lines from Cloud Storage, validates required fields (tenant_id, event_id, event_ts), filters invalid records, and deduplicates by (tenant_id, event_id). Valid ro",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a Dataflow streaming job keyed by tenant_id; redact PII fields via a DoFn using CMEK-protected keys, emitting masked JSON to a per-tenant partitioned BigQuery table. Enforce per-tenant access wi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a 2 TB/day multi-tenant streaming pipeline on GCP (Pub/Sub -> Dataflow -> per-tenant BigQuery). Introduce a versioned schema registry in Data Catalog, enforce compatibility checks on changes",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end practical skills for a beginner: streaming ingestion, basic data quality, privacy via per-tenant masking, and ops basics.\n\n## Key Concepts\n\n- Pub/Sub → Dataflow ingestion and normalization\n- Unified BigQuery schema with daily partitioning\n- Per-tenant privacy using BigQuery views and access controls\n- Dead-letter handling and archival storage\n- Basic rollback by replaying archives within 24h\n\n## Code Example\n\n```python\nfrom apache_beam.options.pipeline_options import PipelineOptions\nimport apache_beam as beam\nimport json\n\nclass ParseAndValidate(beam.DoFn):\n  def process(self, x):\n    data = json.loads(x)\n    if 'tenantId' not in data or 'payload' not in data:\n      raise ValueError('missing fields')\n    return [data]\n\ndef run():\n  opts = PipelineOptions(streaming=True)\n  with beam.Pipeline(options=opts) as p:\n    (p\n     | beam.io.ReadFromPubSub(topic='projects/PROJECT/topics/tenant-events')\n     | beam.ParDo(ParseAndValidate())\n     | beam.Map(lambda d: {'tenantId': d['tenantId'], 'payload': d['payload'], 'ts': d.get('ts')})\n     | beam.io.WriteToBigQuery('PROJECT.DATASET.TABLE', schema='SCHEMA', write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND))\n```\n\n## Follow-up Questions\n\n- How would you test schema evolution and backward compatibility?\n- How would you enforce per-tenant access in BigQuery beyond masking?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-16",
        "questionId": "q-15890",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 10 GB/day of IoT device telemetry from Pub/Sub into BigQuery in a single region, adding a lightweight streaming validation step that quarantines out-of-range or malformed records to Cloud Storage, while valid records go to a partitioned BigQuery table. Include a backward-compatible schema evolution (adding firmwareVersion) and a simple test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a pipeline that writes to BigQuery with a single VARIANT column for event payloads, plus a Firestore schema registry for field versions. Dataflow parses Pub/Sub JSON, validates against registry",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a streaming Dataflow pipeline that consumes Pub/Sub JSON events, applies a DoFn-based PII mask for fields like email/phone, and writes redacted records to a partitioned BigQuery table. Raw e",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I'd implement a per-tenant data quality framework: a Data Catalog-tagged schema registry, Dataflow validation templates that enforce tenant schemas and quality rules, with invalid records redirected to a quarantine Pub/…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Dataflow (Beam) to read from Pub/Sub, apply a DoFn to enforce value ranges for fields like temperature, gate invalid rows to Cloud Storage (quarantine bucket), and emit valid rows to a partitioned BigQuery table by…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses hands-on experience with streaming ingestion pipelines, Dataflow transforms, and end-to-end data quality management in a beginner-friendly manner. It also covers schema evolution and testing strategies.\n\n## Key Concepts\n- **Pub/Sub** for real-time IoT telemetry ingestion\n- **Cloud Dataflow/Beam** for scalable, serverless data processing\n- **BigQuery partitioning** by ingestion date for cost efficiency and query performance\n- **Cloud Storage** as a quarantine sink for invalid records\n- **Schema evolution** with backward compatibility\n- **Testing strategy**: unit tests for validation logic and end-to-end tests with sampled data\n\n## Code Example\n```python\n# Beam DoFn for validation\nclass ValidateTelemetry(beam.DoFn):\n    def process(self, element):\n        try:\n            event = json.loads(element)\n            # Check required field\n            if 'temperature' not in event:\n                yield beam.pvalue.TaggedOutput('quarantine', element)\n                return\n            \n            t = event['temperature']\n            if not (-40 <= t <= 85):\n                yield beam.pvalue.TaggedOutput('quarantine', element)\n                return\n            \n            yield event\n        except json.JSONDecodeError:\n            yield beam.pvalue.TaggedOutput('quarantine', element)\n\n# Pipeline construction\nwith beam.Pipeline() as p:\n    raw = p | 'ReadPubSub' >> beam.io.ReadFromPubSub(topic='projects/my-project/topics/iot-telemetry')\n    validated = raw | 'Validate' >> beam.ParDo(ValidateTelemetry()).with_outputs('quarantine', main='valid')\n    \n    validated.valid | 'WriteToBQ' >> beam.io.WriteToBigQuery(\n        'my-project:iot.telemetry',\n        schema='timestamp:TIMESTAMP,device_id:STRING,temperature:FLOAT,firmwareVersion:STRING',\n        create_disposition=beam.io.BigQueryDisposition.CREATE_IF_NEEDED,\n        write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND\n    )\n    \n    validated.quarantine | 'WriteToGCS' >> beam.io.WriteToText('gs://quarantine-bucket/invalid-records')\n```\n\n## Test Strategy\n- **Unit tests**: Test ValidateTelemetry DoFn with valid/invalid/malformed inputs\n- **End-to-end test**: Run pipeline with 1000 sample messages and verify data lands in correct sinks",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-17",
        "questionId": "q-5744",
        "question": "Process 300k daily JSON events from Pub/Sub with nested attributes (user: {id, tier}, event: {type, ts, meta}, device: {os}) by enriching with a user-tier lookup from Cloud Bigtable, flattening to a flat schema, and writing to a daily-partitioned BigQuery table in a single region. Include idempotent writes, dead-letter handling, archiving originals to Cloud Storage, a 24-hour rollback, and a lightweight data quality dashboard?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Beam pipeline in Python: read Pub/Sub JSON, flatten nested fields, enrich with a Cloud Bigtable user-tier lookup, validate required fields, and upsert to a daily-partitioned BigQuery table. U",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Pipeline Architecture **Ingestion Layer (Pub/Sub)** - Publish 50 GB/day of JSON clickstream events to a single regional Pub/Sub topic - Enable schema validation on the subscription to reject malformed messages early…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-tier streaming: a BigQuery hot table partitioned by ingestion_date with 90-day partition expiration for PII, plus a daily Cloud Storage Parquet archive for audits. Dataflow templates ingest Pub/Su",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Pub/Sub → Dataflow streaming: a DoFn validates events against a contract, routes valid data to per-device partitions in BigQuery. A policy engine auto-evolves schemas for new fields with versioning st",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design an end-to-end ingestion, enrichment, and quality pattern using familiar GCP primitives, with an approachable yet realistic complexity for a beginner.\n\n## Key Concepts\n\n- Dataflow/Beam transforms\n- Flattening nested JSON\n- Cloud Bigtable enrichment\n- Idempotent BigQuery writes (insertId / upserts)\n- Dead-lettering and archival\n- 24-hour rollback strategy\n- Lightweight data quality monitoring\n\n## Code Example\n\n```python\n# Skeleton Beam pipeline outline\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nimport json\n\nclass FlattenAndEnrich(beam.DoFn):\n    def process(self, elem):\n        # placeholder for flatten/enrich logic\n        return [elem]\n\ndef run():\n  with beam.Pipeline(options=PipelineOptions()) as p:\n      msgs = (\n        p\n        | \"Read\" >> beam.io.ReadFromPubSub(subscription=\"projects/PROJECT/subscriptions/SUB\")\n        | \"Parse\" >> beam.Map(lambda b: json.loads(b.decode(\"utf-8\")))\n        | \"Flatten\" >> beam.ParDo(FlattenAndEnrich())\n        | \"WriteBQ\" >> beam.io.WriteToBigQuery(\n            table=\"PROJECT:DATASET.TABLE\",\n            write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND,\n            create_disposition=beam.io.BigQueryDisposition.CREATE_IF_NEEDED\n        )\n      )\n\nif __name__ == \"__main__\":\n  run()\n```\n\n## Follow-up Questions\n\n- How would you test this with unit tests and integration tests?\n- How would you handle Bigtable lookup latency failures and retries?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-18",
        "questionId": "q-26883",
        "question": "On GCP, design an advanced, multi-region streaming pipeline ingesting 5 TB/day of JSON events from Pub/Sub. Enforce per-tenant data residency (EU/US), implement per-tenant row-level security in BigQuery, redact PII with Data Loss Prevention, and maintain end-to-end lineage in Data Catalog. Include 24h per-tenant rollback, data contracts, and a backfill path for new fields with schema evolution; test under burst and late data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest with Pub/Sub → Dataflow streaming with per-tenant DLP masking, keeping tenant isolation in BigQuery partitions. Use Data Catalog for per-tenant contracts; store raw Parquet in GCS and tenant-ag",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Pub/Sub -> Dataflow ingests 10 TB/day, computes per-tenant features, masks PII, and calls a Vertex AI Online Endpoint for real-time scoring. Scores are written to a partitioned BigQuery table (tenant_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant BigQuery datasets; Dataflow streaming pipeline masks PII (emails, phones) via DoFn; writes with insertId; malformed go to dead-letter Pub/Sub; raw payloads to Cloud Storage; Data Catalog ta",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a multi-region stream: Pub/Sub → Dataflow (Beam) → region-specific BigQuery datasets (EU/US). Enforce per-tenant residency with row-level security, redact PII via Data Loss Prevention, and c",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes architecting for data residency, real-time processing, and robust governance. It evaluates handling of per-tenant security at scale, PII masking, and lineage tracking across regions, plus practical recovery and schema evolution strategies.\n\n ## Key Concepts\n\n- Multi-region streaming architecture (EU/US)\n- Row-level security in BigQuery\n- Data Loss Prevention masking for PII\n- Data Catalog lineage integration\n- Data contracts and backfill/schema evolution\n-  burst traffic and late-arriving data testing\n\n ## Code Example\n\n```python\nimport apache_beam as beam\n\ndef enrich(e):\n  # placeholder enrichment logic\n  return e\n\ndef run():\n  with beam.Pipeline(...) as p:\n    events = p | beam.io.ReadFromPubSub(topic='projects/..../topics/events')\n    enriched = events | beam.Map(enrich)\n    enriched | beam.io.WriteToBigQuery('PROJECT:DATA.EU_EVENTS', schema='SCHEMA', insert_properties={'rowLevelSecurity': True})\n\nif __name__ == '__main__':\n  run()\n```\n\n ## Follow-up Questions\n\n- How would you implement per-tenant routing to EU vs US datasets in Dataflow?\n- How would you validate and rollback 24h of tenant data in case of contract violation?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-19",
        "questionId": "q-17308",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. Add a new angle: enable Customer-Managed Encryption Keys (CMEK) for the dataset and use per-tenant Authorized Views on a shared RAW table. Outline CMEK setup, key rotation, access auditing, and a simple test plan with synthetic tenants?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose per-tenant isolation with separate BigQuery datasets; Pub/Sub to Dataflow for masking and feature extraction; Vertex AI per-tenant models for online scoring; store results in BigQuery; tag con",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest 50 GB/day via Pub/Sub to a single-region BigQuery RAW table, with per-tenant access through Authorized Views. New angle: CMEK for the dataset via Cloud KMS and a 90-day key rotation; attach row",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Architect a 3-region cross-tenant streaming pipeline: 4 TB/day of JSON clickstream from Pub/Sub, Dataflow processing with per-tenant masking via DLP; regional raw Parquet in Cloud Storage; per-tenant",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant schema registry in Data Catalog, streaming via Pub/Sub into Dataflow, write raw JSON to Cloud Storage under tenant prefixes, materialize per-tenant aggregates in BigQuery, and surface",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of basic streaming ingestion, governance, and security via CMEK; introduces security policy and practical testing.\n\n## Key Concepts\n- Pub/Sub to BigQuery streaming\n- Authorized Views for per-tenant access\n- Customer-Managed Encryption Keys (CMEK) for BigQuery datasets\n- Key rotation and Cloud Audit Logs for access\n\n## Code Example\n```bash\n# Example: set CMEK for dataset\ngcloud kms keys create per_tenant_key --location=global --keyring=rg1 --purpose=encryption\ngcloud bigquery datasets update tenant_dataset --default-encryption_configuration=projects/PROJECT/locations/global/keyRings/rg1/cryptoKeys/per_tenant_key\n```\n\n## Follow-up Questions\n- How would you validate that key rotation maintains access without downtime?\n- What monitoring would you add to detect CMEK-related failures in ingestion?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-20",
        "questionId": "q-7422",
        "question": "Design a beginner-friendly GCP data pipeline to ingest 400 GB/day of JSON events via Pub/Sub into BigQuery in a single region. Include per-tenant data isolation using daily partitions, a simple field-level masking step, idempotent writes with MERGE, a 24h Cloud Storage backup for rollback, and a lightweight data lineage log exported to Cloud Storage. Provide a basic test plan with synthetic data and rollback validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a telemetry pipeline with Pub/Sub, Dataflow, and BigQuery, enforcing per-service data contracts via Pub/Sub Schema Registry and Data Catalog tagging. Redact PII at ingest, store raw in Cloud S",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design uses Pub/Sub -> Dataflow for ingestion, writes raw Parquet to Cloud Storage, exposes per-tenant views via BigLake external tables in BigQuery, and registers per-tenant schemas in Data Catalog.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a GCP design with 5k tenants, 2 GB/day of JSON via Pub/Sub. Dataflow performs de-dup, upserts to per-tenant BigQuery tables, with schema evolution support. Use Cloud Storage for archival tiers",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Dataflow streaming job that reads Pub/Sub JSON messages, applies field-level PII masking in a custom DoFn, and writes to daily-partitioned BigQuery tables using MERGE statements for idempotent upserts. Maint…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical familiarity with Pub/Sub, Dataflow, and BigQuery in a tenant-aware, beginner-friendly setup, plus rollback and lineage basics.\n\n## Key Concepts\n- Dataflow streaming pipelines and Beam transforms\n- BigQuery MERGE for idempotent upserts\n- Field-level masking in-flight\n- Daily partitions for tenant isolation\n- Cloud Storage backups for rollback\n- Lightweight lineage logs and Data Catalog basics\n\n## Code Example\n```javascript\nfunction maskPII(record) {\n  if (record.email) record.email = 'redacted@example.com';\n  if (record.phone) record.phone = '000-000-0000';\n}",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-21",
        "questionId": "q-11854",
        "question": "On GCP, design a cross-region, multi-tenant streaming pipeline ingesting 5 TB/day of JSON events via Pub/Sub. Enforce per-tenant isolation with CMEK encryption, mask PII in flight, compute per-tenant aggregates in BigQuery, and publish via Data Exchange. Include Data Catalog lineage, a 7-day per-tenant rollback, data contracts, and a staged testing/rollout plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pub/Sub -> Dataflow ingests 10 TB/day, computes per-tenant features, masks PII, and calls a Vertex AI Online Endpoint for real-time scoring. Scores are written to a partitioned BigQuery table (tenant_",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a pipeline: Pub/Sub to Dataflow for validation, then per-tenant BigQuery tables; store raw Parquet in Cloud Storage; maintain Data Catalog lineage; expose per-tenant quality scores to dashboar",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I propose a cross-region streaming architecture with separate Pub/Sub topics per region, Dataflow jobs that enforce tenant ID isolation and perform real-time PII masking through DoFn transformations, CMEK-encrypted BigQ…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a single-region Dataflow (Beam) job to parse Pub/Sub JSON; enforce the schema (user_id: string, event_time: string ISO8601, event_type: string) and drop extra fields. Write valid records to a dail",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests your ability to design secure, scalable streaming architectures with strict multi-tenancy requirements, regulatory compliance, and comprehensive data governance across distributed systems.\n\n## Key Concepts\n- Cross-region Pub/Sub with Dataflow orchestration for high availability\n- Per-tenant CMEK encryption ensuring data isolation and security\n- Real-time PII masking during stream processing\n- End-to-end data lineage through Data Catalog integration\n- Data Exchange publishing for secure data sharing\n- Schema validation and contract enforcement\n- Time travel capabilities for audit and rollback scenarios\n\n## Code Example\n```javascript\nfunction mask(record) {\n  // Real-time PII masking in Dataflow DoFn\n  if (record.ssn) record.ssn = '***-**-****';\n  if (record.email) record.email = record.email.replace(/(.{2}).*@/, '$1***@');\n  return record;\n}\n```\n\n## Follow-up Questions\n- How would you handle schema evolution while maintaining backward compatibility?\n- What strategies would you implement for monitoring and alerting on pipeline failures?\n- How would you optimize costs while maintaining the required security and isolation levels?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-22",
        "questionId": "q-24188",
        "question": "On GCP, design a real-time multi-tenant data quality and lineage pipeline: ingest 5 TB/day of JSON events via Pub/Sub into BigQuery; enforce per-tenant schema contracts and validate every event in Dataflow; emit only compliant rows to tenant-specific BigQuery partitions; detect drift (missing fields, type changes) and trigger per-tenant rollbacks with 24h retention; expose quality metrics via Data Exchange?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage per-tenant schema contracts tracked in Data Catalog, a Beam Dataflow streaming pipeline that validates events against the contract, routing only compliant records to tenant-segregated BigQuer",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement the pipeline with Pub/Sub -> Dataflow (Beam) -> BigQuery. Add a lightweight schema validator in a DoFn that checks for tenant_id, user_id, timestamp, event_type; on failure, publish to a Dea",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Pub/Sub → Dataflow for normalization; per-tenant IAM with Data Catalog tagging; DLP masking/tokenization at ingress; Vertex AI online anomaly detector on masked PII; auto-remediation via Cloud Functio",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a CI/CD-driven end-to-end test harness that generates deterministic synthetic events per tenant, publishes them to Pub/Sub, executes a Dataflow pipeline in staging, and validates idempotent upserts with exact-…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProbes ability to design robust, scalable data quality pipelines with strict per-tenant contracts, real-time validation, and automated rollback in GCP. It also tests use of Data Catalog for lineage, Data Exchange for sharing quality metrics, and safe drift handling in production.\n\n## Key Concepts\n- Per-tenant contracts and schema drift detection\n- Streaming validation in Dataflow/Beam\n- Tenant isolation and rollback strategies\n- Data Catalog lineage and Data Exchange integration\n\n## Code Example\n```python\nimport apache_beam as beam\nimport json\n\nclass ValidateEvent(beam.DoFn):\n  def __init__(self, contract):\n    self.contract = contract\n  def process(self, elem):\n    obj = json.loads(elem)\n    for k, v in self.contract.items():\n      if k not in obj:\n        return\n      t = v.get('type')\n      if t and not isinstance(obj[k], {'string': str, 'int': int}.get(t, object)):\n        return\n    yield json.dumps(obj)\n```\n\n## Follow-up Questions\n- How would you implement per-tenant rollback guarantees and data-evolution strategies?\n- How would you scale drift detection and decide when to trigger a rollback?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-23",
        "questionId": "q-27122",
        "question": "On GCP, design a robust ingestion and quality-control pipeline for 2-3 TB/day of IoT telemetry across 200 tenants, arriving via Pub/Sub and stored as Parquet in Cloud Storage. Implement per-tenant data contracts, idempotent ingestion, and drift/anomaly detection with Dataflow, publish clean data via per-tenant BigQuery tables through BigLake, and include late-arrival handling, a 24h rollback window, monitoring, and a concise testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest Pub/Sub messages by tenant, dedupe with a tenant-scoped sequence, validate payloads against per-tenant schema contracts, and apply anomaly scoring (e.g., z-score) in streaming Dataflow. Persist",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a GCP design with 5k tenants, 2 GB/day of JSON via Pub/Sub. Dataflow performs de-dup, upserts to per-tenant BigQuery tables, with schema evolution support. Use Cloud Storage for archival tiers",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a streaming guardrail that validates each incoming JSON event against the tenant's current schema version registered in Data Catalog, computes drift metrics in-flight, and quarantines nonconforming records to…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Pipeline Architecture **Ingestion Layer (Pub/Sub)** - Publish 50 GB/day of JSON clickstream events to a single regional Pub/Sub topic - Enable schema validation on the subscription to reject malformed messages early…",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests ability to integrate per-tenant data contracts with data quality, drift detection, and rollback in a real-world streaming pipeline. It also probes design for late data, backfills, and end-to-end testing.\n\nKey Concepts\n- Per-tenant data contracts and schema evolution\n- Pub/Sub ingestion and idempotent processing\n- Dataflow (Beam) streaming for validation and anomaly detection\n- Parquet storage in Cloud Storage and BigLake external tables for BigQuery access\n- Late-arrival handling, 24h rollback window, monitoring, and testing\n\nCode Example\n```python\n# Skeleton Apache Beam pipeline (Python) illustrating tenant-based dedupe and contract validation\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\nclass Deduplicate(beam.DoFn):\n  def process(self, element, *args, **kwargs):\n    tenant, record = element\n    # placeholder dedupe logic using a tenant-scoped seed/offset\n    yield (tenant, record)\n\ndef run():\n  opts = PipelineOptions()\n  with beam.Pipeline(options=opts) as p:\n    _ = (\n      p\n      | 'Read PubSub' >> beam.io.ReadFromPubSub(subscription='projects/..../subscriptions/telemetry')\n      | 'Parse' >> beam.Map(lambda b: json.loads(b))\n      | 'Key by Tenant' >> beam.Map(lambda x: (x['tenant_id'], x))\n      | 'Deduplicate' >> beam.ParDo(Deduplicate())\n      | 'Write Parquet' >> beam.io.WriteToParquet('gs://bucket/telemetry/', schema=...) \n    )\n```\n\nFollow-up Questions\n- How would you test data contract evolution across tenants without breaking existing data?\n- How would you tune anomaly thresholds to minimize false positives across diverse tenants?\n- How would you validate rollback correctness with backfills and audit trails?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-24",
        "questionId": "q-3093",
        "question": "Design a batch Dataflow pipeline that reads per-tenant JSON logs from Cloud Storage, validates against a schema.json in GCS, flattens nested fields, and writes to a BigQuery table partitioned by ingest_date with event_id dedup. Allow late-arriving files up to 24 hours via re-ingest, and emit an audit log to Cloud Storage plus a quick observability summary to Pub/Sub?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Pub/Sub → Dataflow validates against a registry in Data Catalog; when drift detected, bump contract version and tag affected partitions with tenant metadata; expose only compliant data via",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a batch Dataflow pipeline that reads per-tenant JSON logs from Cloud Storage, validates them against a schema.json file stored in GCS, flattens nested fields, and writes to a BigQuery table partitioned by ingest_d…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-tenant isolation via per-tenant BigQuery partitions and Dataflow triggers, plus a metadata-driven access layer: Data Catalog policy tags + Authorized Views so a partner can query only TenantA aggr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a Dataflow streaming job that reads Pub/Sub, validates required fields (app, timestamp, event), applies per-app schemas, writes to daily-partitioned per-app BigQuery tables using MERGE/insertI",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a scalable, tenant-aware batch pipeline that uses Dataflow, enforces schema validation at ingest, and handles late arrivals without complex orchestration.\n\n## Key Concepts\n- Dataflow batch pipelines and PTransforms\n- JSON schema validation stored in GCS\n- BigQuery partitioning by ingest_date and per-tenant isolation\n- Idempotent deduplication by event_id\n- Late-arrival handling with re-ingest and retries\n- Audit logs into Cloud Storage and Pub/Sub observability\n\n## Code Example\n```javascript\n// Pseudo Dataflow-like sketch: validate JSON against schema and flatten\nfunction processTenantLogs(pipeline) {\n  pipeline\n    .apply('ReadTenantLogs', GCSIO.read().from('gs://tenant-logs/*/*.json'))\n    .apply('ValidateSchema', ParDo.of(new SchemaValidator('gs://schemas/schema.json')))\n    .apply('FlattenFields', ParDo.of(new FieldFlattener()))\n    .apply('DeduplicateEvents', Distinct.create().withField('event_id'))\n    .apply('WriteToBigQuery', BigQueryIO.writeTable()\n      .to('project.dataset.events')\n      .withSchema(eventSchema)\n      .withWriteDisposition(BigQueryIO.Write.WriteDisposition.WRITE_APPEND)\n      .withCreateDisposition(BigQueryIO.Write.CreateDisposition.CREATE_IF_NEEDED)\n      .withTimePartitioning('DAY', 'ingest_date'))\n    .apply('GenerateAudit', ParDo.of(new AuditLogger('gs://audit-logs/')));\n}\n```",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-18T07:07:13.021Z",
    "version": 1
  }
]