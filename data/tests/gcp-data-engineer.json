[
  {
    "id": "test-gcp-data-engineer",
    "channelId": "gcp-data-engineer",
    "channelName": "GCP Data Engineer",
    "title": "GCP Data Engineer Knowledge Test",
    "description": "Test your understanding of GCP Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "gcp-data-engineer-q-0",
        "questionId": "q-15216",
        "question": "On GCP, design a beginner-friendly streaming pipeline to ingest 1-2 TB/month of JSON user events via Pub/Sub into BigQuery with per-tenant datasets, masking PII, and a lightweight Data Catalog-based data contract; include a quarantine path for invalid events and a rollback plan that reprocesses the last 24 hours from quarantine back into tenant tables?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pub/Sub → Dataflow streaming to parse JSON, apply ParDo masking (emails, phones), route valid events to per-tenant BigQuery datasets, and invalid to a quarantine table. Store contract in Data Cata",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a single-region Dataflow (Beam) job to parse Pub/Sub JSON; enforce the schema (user_id: string, event_time: string ISO8601, event_type: string) and drop extra fields. Write valid records to a dail",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a streaming Dataflow pipeline with a ParDo that explodes the events array, validates sub-field presence, and emits valid rows to a BigQuery streaming insert into a daily-partitioned table using th",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant isolation via per-tenant BigQuery datasets and Pub/Sub topics, a Dataflow streaming pipeline to both regions, and a concurrent Data Quality pipeline that validates schema conformance, null",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nUnderstanding end-to-end streaming ingestion, privacy handling, and basic rollback in GCP.\n\n## Key Concepts\n- Pub/Sub streaming\n- Dataflow DoFn masking\n- BigQuery per-tenant datasets\n- Data Catalog data contracts and lineage\n- Quarantine path and 24h rollback\n\n## Code Example\n```javascript\n// Pseudo Dataflow DoFn: mask PII before write\nclass MaskPIIFn {\n  process(element) {\n    // element is a JSON object\n    if (element.email) element.email = maskEmail(element.email);\n    if (element.phone) element.phone = maskPhone(element.phone);\n    return element;\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test this pipeline for schema drift and masking correctness?\n- How would you alert on quarantine volume spikes and auto-remediate?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-1",
        "questionId": "q-6723",
        "question": "Design a beginner-friendly GCP streaming pipeline for multi-tenant JSON activity ingested via Pub/Sub into per-tenant BigQuery datasets. Implement tenant-level masking for sensitive fields with DLP at ingest, enforce per-tenant quotas, and provide a 24h rollback that replays raw data from Cloud Storage to affected tenants only. Outline architecture, data models, and a basic testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest with Dataflow (Beam) reading Pub/Sub, route by tenant to per-tenant BigQuery datasets; create datasets on demand. Apply DLP masking for sensitive fields (email, phone) in the pipeline. Use a pe",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement Pub/Sub → Dataflow streaming with tenant isolation; write per-tenant BigQuery datasets and Parquet raw in regional Cloud Storage. Use Data Catalog policy tags for field masking, and a schema",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Dataflow Beam pipeline that reads Pub/Sub JSON telemetry, validates required fields, enriches with Firestore device metadata, writes to a daily-partitioned BigQuery table using MERGE for idempotency, routes…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant isolation: distinct Pub/Sub topics and BigQuery datasets per tenant; Dataflow pipelines with exactly-once writes; centralized schema registry with Data Catalog for evolution; QC checks (sch",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle explores multi-tenant isolation, masking, and rollback in a beginner-friendly way.\n\n## Key Concepts\n- Dataflow (Beam) streaming pipelines\n- Pub/Sub for ingestion\n- Per-tenant BigQuery datasets\n- Data Loss Prevention masking in ingest\n- Idempotent writes with MERGE\n- Dead-letter queues and raw data archiving\n- 24h rollback via replay from Cloud Storage\n- Observability through Dataflow metrics\n\n## Code Example\n```javascript\n// Dataflow masking sketch (pseudo)\nclass MaskPII {\n  process(element) {\n    if (element.email) element.email = '*****@*****.com';\n    if (element.phone) element.phone = '**********';\n    return element;\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test data quality across tenants? \n- How would you enforce per-tenant quotas in BigQuery and Dataflow?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-2",
        "questionId": "q-21402",
        "question": "On GCP, design a real-time feature store for a fraud-detection use case: ingest 2k events/sec via Pub/Sub; compute features in Dataflow; store online features in Vertex AI Feature Store with per-tenant isolation; archive batch data to BigQuery; aim for <10 ms online latency; include drift detection, data lineage via Data Catalog, PII masking, IAM/VPC controls; add 24h rollback of feature definitions and a robust test plan with synthetic drift scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: a Dataflow streaming pipeline reading tenant-scoped JSON from Pub/Sub, consulting per-tenant schemas from a registry stored in Cloud Storage and cataloged in Data Catalog, and mapping to a shared BigQuery sche…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Explain enabling Pub/Sub Schemas, a Python Dataflow DoFn that validates fields (tenantId, eventId, ts, url), types, and rejects invalid messages to a Cloud Storage manifest; discuss how to handle opti",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "On GCP, design a real-time feature store for fraud detection: ingest 2k events/sec from Pub/Sub; compute features in Dataflow; publish online features to Vertex AI Feature Store (per-tenant isolation)",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Pub/Sub and Dataflow for streaming, isolating tenants with per-tenant Cloud KMS keys and envelope encryption; store raw Parquet in regional Cloud Storage; materialize per-tenant aggregates in BigQ",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProbes production-grade ML feature management at scale: real-time and batch stores, tenant isolation, and governance.\n\n## Key Concepts\n- Vertex AI Feature Store: online/offline stores for low-latency serving and offline training\n- Pub/Sub + Dataflow: streaming ingestion and feature computation\n- Data Catalog lineage + data masking\n- IAM/VPC controls: per-tenant isolation and secure access\n- Drift detection and 24h rollback for feature definitions\n\n## Code Example\n```javascript\nconst spec = { tenantIsolation: true, onlineStore: 'VertexAI Feature Store', maxOnlineLatencyMs: 10 };\n```\n\n## Follow-up Questions\n- How would you validate drift and roll back feature definitions in production?\n- What monitoring and alerting would you implement for data skew between online/offline stores?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-3",
        "questionId": "q-23830",
        "question": "On GCP, design a real-time streaming pipeline ingesting 10 TB/day of mixed JSON/Avro events from Pub/Sub for a multi-tenant SaaS. Implement per-tenant isolation in BigQuery with partitioned datasets and a central Data Catalog schema registry for dynamic evolution. Ensure end-to-end lineage, data contracts, and 24h per-tenant rollback. Include cross-region replication to Snowflake via Snowpipe with a testing and rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Dataflow (Beam) streaming pipeline: Pub/Sub → enrichment via an external Redis-backed service (Memorystore) for country and account_tier, cached to meet latency. If Redis miss, route to a fal",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-region Dataflow pipelines ingest Pub/Sub into a shared RAW BigQuery table with per-tenant Authorized Views. Cloud DLP performs in-flight redaction before storage; aggregates written to region-loca",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest 1 TB/day of JSON events via Pub/Sub into a single-region BigQuery, with per-tenant schema evolution stored in Data Catalog. Use a Dataflow (Python Beam) pipeline that maps each tenant to its ex",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a real-time pipeline that ingests 10 TB/day of mixed JSON/Avro events from Pub/Sub for a multi-tenant SaaS. Use Dataflow templates with the Splittable DoFn optimization to process records in parallel. Implement p…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests end-to-end real-time data engineering competencies on GCP, including multi-tenant isolation, dynamic schema evolution, data contracts, and rollback semantics, plus cross-cloud replication and testing strategies.\n\n## Key Concepts\n\n- Real-time ingestion with Pub/Sub and Dataflow templates\n- Per-tenant isolation using partitioned BigQuery datasets\n- Central schema registry in Data Catalog for dynamic evolution\n- End-to-end lineage and data contracts\n- 24-hour per-tenant rollback via BigQuery time travel\n- Cross-region replication to Snowflake via Snowpipe\n\n## Code Example\n\n```java\n// Dataflow pipeline with tenant isolation and schema validation\npublic class TenantPipeline {\n    public static void main(String[] args) {\n        PipelineOptions options = PipelineOptionsFactory.fromArgs(args).create();\n        Pipeline pipeline = Pipeline.create(options);\n        \n        pipeline\n            .apply(\"Read from Pub/Sub\", PubsubIO.readMessagesWithAttributes()\n                .fromSubscription(options.getSubscription()))\n            .apply(\"Extract tenant ID\", ParDo.of(new ExtractTenantDoFn()))\n            .apply(\"Validate against schema\", ParDo.of(new SchemaValidationDoFn()))\n            .apply(\"Write to tenant BigQuery\", new TenantBigQueryWrite());\n        \n        pipeline.run();\n    }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-4",
        "questionId": "q-21434",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. New angle: incorporate a privacy-preserving stage using Cloud Data Loss Prevention (DLP) to redact PII fields (email, phone, and SSN) during Dataflow processing before storage. Outline DLP config, redaction templates, per-tenant access controls via Authorized Views, testing with synthetic PII, and monitoring of redaction latency and false positives?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage per-tenant datasets and partitions, using Pub/Sub → Dataflow → Parquet in Cloud Storage, BigLake external tables in BigQuery. Enforce per-tenant retention (7d/365d) via separate partitions an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a streaming pipeline: Pub/Sub -> Dataflow streaming templates -> per-tenant BigQuery in two regions with partitioned tables; tokenization/masking via Data Loss Prevention in the transform; e",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Dataflow streaming job that subscribes to Pub/Sub, parses JSON, applies DLP redaction for email, phone, and SSN, then writes de-identified data to a partitioned BigQuery table. Use DLP templa",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a per-tenant feature store backed by BigQuery views with exchangeable derived features. Apply masking and tokenization at ingestion to protect raw data. Share only vetted, derived features with partners via Data…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to add privacy controls in a real pipeline, a common beginner-friendly scenario in data engineering.\n\n## Key Concepts\n- GCP Dataflow, Pub/Sub, BigQuery, DLP, Authorized Views, data masking, testing with synthetic data.\n- Trade-offs: latency vs privacy, cost.\n\n## Code Example\n```javascript\n// Pseudo-Dataflow pseudocode for DLP redaction\n// Read Pub/Sub -> Parse JSON -> Apply DLP redaction -> Write to BigQuery\n```\n\n## Follow-up Questions\n- How would you validate that redacted fields cannot be reconstructed from metadata?\n- What metrics would you surface in Cloud Monitoring to detect privacy leaks or processing delays?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-5",
        "questionId": "q-19286",
        "question": "On GCP, design an advanced cross-cloud analytics pipeline that ingests 5 TB/day of JSON events from Pub/Sub and surfaces per-tenant aggregates in a multi-cloud data mesh using BigQuery Omni (GCP and AWS). Implement per-tenant row-level security, CMEK, Data Catalog lineage, and partner sharing via Data Exchange. Include 24h per-tenant rollback, data contracts, and a rigorous test/rollback plan with synthetic tenants?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest Pub/Sub JSON with Dataflow into a single BigQuery table partitioned by ingestion_date, including tenant_id. Enforce per-tenant access via authorized views filtering on tenant_id. Deduplicate by",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Dataflow pipeline that ingests Pub/Sub messages and validates each JSON event against a tenant-scoped, versioned schema retrieved from a schema catalog (e.g., Cloud Storage or a metadata store). Events that…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: Dataflow ingest with exactly-once; tenant quality gates (null rate, drift) as streaming metrics; snapshots in Cloud Storage for 24h rollback via replay; Data Catalog for contracts and lineag",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a cross-cloud, per-tenant analytics pipeline that ingests 5 TB/day of JSON events from Pub/Sub, processes them with Dataflow for real-time enrichment, and stores per-tenant aggregates using BigQuery Omni acros…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to architect cross-cloud data meshes, enforce strict tenant isolation, and manage lineage, governance, and rollback procedures in production environments.\n\n## Key Concepts\n- BigQuery Omni and multi-cloud data mesh architecture\n- Row-level security and customer-managed encryption keys (CMEK)\n- Data Catalog lineage tracking and Data Exchange for partner sharing\n- End-to-end reliability: 24-hour rollback, testing, and recovery drills\n\n## Code Example\n```javascript\n// Pseudo Dataflow config for streaming enrichment\nconst pipeline = createDataflowPipeline({\n  inputSubscription: 'projects/PROJECT/subscriptions/events',\n  outputTable: 'omni:aws:us-east-1.dataset.table',\n  windowSize: '1m',\n  tenantIdField: 'tenant_id'\n});\n\npipeline.apply(ParDo.of(new EnrichmentFn()))\n  .apply(Window.into(FixedWindows.of(60)))\n  .apply(GroupByKey.create())\n  .apply(ParDo.of(new AggregateFn()));\n```",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-6",
        "questionId": "q-2958",
        "question": "Design a real-time, cross-tenant analytics pipeline on GCP that ingests 4 TB/day of JSON clickstream from Pub/Sub across three regions, with per-tenant isolation: raw data stored regionally as Parquet, aggregated BI-ready tables in BigQuery; enforce field-level masking with DLP; automatic schema drift handling using Data Catalog and Dataplex; maintain end-to-end lineage; and implement a rollback plan for tenant-specific schema changes within 24 hours, including testing and failover playbooks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest 2 TB/day via Pub/Sub; use Dataflow streaming with per-tenant routing; apply DLP masking at ingest; write to per-tenant BigQuery datasets with Authorized Views; maintain end-to-end lineage in Da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage per-tenant Vertex AI Feature Store in their region, a Dataflow streaming pipeline feeding a Vertex AI Endpoint with autoscaling, and Pub/Sub ingestion. Emit only anomalies to a shared BigQuer",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a 3-region cross-tenant streaming pipeline: 4 TB/day of JSON clickstream from Pub/Sub, Dataflow processing with per-tenant masking via DLP; regional raw Parquet in Cloud Storage; per-tenant",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Validate each message against a fixed schema in a Dataflow custom DoFn. Valid records are written to the primary BigQuery table, while invalid records are routed to both a dedicated errors BigQuery table and Cloud Stora…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design real-time, multi-region data pipelines with privacy, governance, and operational resilience.\n\n## Key Concepts\n- Multi-region streaming and data locality\n- Tenant isolation and masking\n- Schema drift management with Data Catalog/Dataplex\n- Data lineage and rollback workflows\n- Canary testing and runbooks\n\n## Code Example\n```python\n# Minimal Apache Beam skeleton for Pub/Sub -> BigQuery with masking\nimport apache_beam as beam\nclass MaskFn(beam.DoFn):\n  def process(self, element):\n    # pseudocode: mask PII fields\n    return masked\nwith beam.Pipeline(options=... ) as p:\n  (p\n   | beam.io.ReadFromPubSub(subscription='projects/…/subscriptions/…')\n   | beam.ParDo(MaskFn())\n   | beam.io.WriteToBigQuery('project:dataset.table', write_disposition=beam.io.BigQueryDisposition.WRITE_TRUNCATE)\n  )\n```\n\n## Follow-up Questions\n- How would you implement per-tenant rollback isolation and testing without impacting other tenants?\n- What are your failover and data consistency guarantees across regions during a rollback?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-7",
        "questionId": "q-9733",
        "question": "On GCP, design a beginner-friendly data ingestion pipeline: Pub/Sub delivers JSON events for multiple tenants; Dataflow (Beam) normalizes to a unified schema, validates required fields, and writes to a daily-partitioned BigQuery table. Implement per-tenant privacy with masking via BigQuery views, route invalid messages to a dead-letter Pub/Sub, and archive originals in Cloud Storage. Include basic monitoring and a 24h rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "In this pipeline, ingest Pub/Sub JSON per tenant, use Dataflow to normalize into a single BigQuery schema partitioned by day, validate required fields, and stream to BigQuery. Implement per-tenant pri",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a Dataflow (Beam) streaming pipeline: Pub/Sub → enrichment via an external Redis-backed service (Memorystore) for country and account_tier, cached to meet latency. If Redis miss, route to a fal",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Pub/Sub for ingest, Dataflow for streaming feature computation, and per-tenant BigQuery partitions with CMEK for raw data. Use Vertex AI Feature Store for online features, feeding a real-time",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest JSON events through Pub/Sub and process with Dataflow using per-tenant schemas from a centralized registry. Quarantine malformed records to Cloud Storage, write validated data to tenant-specific BigQuery datasets…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end practical skills for a beginner: streaming ingestion, basic data quality, privacy via per-tenant masking, and ops basics.\n\n## Key Concepts\n\n- Pub/Sub → Dataflow ingestion and normalization\n- Unified BigQuery schema with daily partitioning\n- Per-tenant privacy using BigQuery views and access controls\n- Dead-letter handling and archival storage\n- Basic rollback by replaying archives within 24h\n\n## Code Example\n\n```python\nfrom apache_beam.options.pipeline_options import PipelineOptions\nimport apache_beam as beam\nimport json\n\nclass ParseAndValidate(beam.DoFn):\n  def process(self, x):\n    data = json.loads(x)\n    if 'tenantId' not in data or 'payload' not in data:\n      raise ValueError('missing fields')\n    return [data]\n\ndef run():\n  opts = PipelineOptions(streaming=True)\n  with beam.Pipeline(options=opts) as p:\n    (p\n     | beam.io.ReadFromPubSub(topic='projects/PROJECT/topics/tenant-events')\n     | beam.ParDo(ParseAndValidate())\n     | beam.Map(lambda d: {'tenantId': d['tenantId'], 'payload': d['payload'], 'ts': d.get('ts')})\n     | beam.io.WriteToBigQuery('PROJECT.DATASET.TABLE', schema='SCHEMA', write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND))\n```\n\n## Follow-up Questions\n\n- How would you test schema evolution and backward compatibility?\n- How would you enforce per-tenant access in BigQuery beyond masking?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-8",
        "questionId": "q-10147",
        "question": "On GCP, design a beginner-friendly streaming pipeline that ingests 200 GB/day of JSON events via Pub/Sub, validates the schema, applies per-tenant masking, and writes to a daily-partitioned BigQuery table. Include dead-letter handling, archival of originals, a 24h per-tenant rollback plan, data contracts, and a basic end-to-end data lineage using Data Catalog?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-tenant streaming pipeline on GCP that ingests 200 TB/day of telemetry data from Pub/Sub, processes it through Dataflow with per-tenant isolation and privacy masking, writes Parquet files to Cloud Storage…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest via Pub/Sub, stream with Dataflow to a daily-partitioned BigQuery table, validate fields, apply tenant-specific masking using a policy table, and enforce schema via Pub/Sub Schema Registry. Rou",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a Dataflow (Apache Beam) streaming job that reads JSON events from Pub/Sub, applies Cloud DLP masking to PHI fields (name, DOB, SSN), writes the redacted records to a BigQuery dataset, and emits audit logs to…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a per-tenant, schema-driven streaming solution: pull tenant schema from Data Catalog, validate with Dataflow, mask PII on the fly, write to tenant-specific BigQuery partitions, and surface met",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical streaming ingestion, per-tenant masking, basic schema validation, dead-letter handling, and rollback at a tenant granularity, aligned with real-world GCP usage for a beginner.\n\n## Key Concepts\n\n- Pub/Sub ingestion and Dataflow streaming\n- Per-tenant masking with policy lookup\n- Daily-partitioned BigQuery and schema validation\n- Dead-letter Pub/Sub and archival of originals\n- 24h per-tenant rollback\n- Data Catalog lineage\n\n## Code Example\n\n```python\n# Pseudo Beam snippet showing masking per-tenant\nclass MaskPerTenant(beam.DoFn):\n  def process(self, element, policies):\n    tenant = element['tenant_id']\n    mask = policies.get(tenant, {})\n    element['customer_email'] = mask_email(element['customer_email'], mask)\n    yield element\n```\n\n## Follow-up Questions\n\n- How would you test rollback per tenant end-to-end?\n- How would you validate schema evolution while retaining backward compatibility?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-9",
        "questionId": "q-18682",
        "question": "On GCP, design a streaming multi-tenant telemetry pipeline for a fleet-management platform: each tenant's vehicles emit JSON events with evolving schemas; ingest ~25 TB/day via Pub/Sub; route per-tenant data into separate BigQuery datasets/tables; enforce per-tenant privacy (mask/tokenize PII) in real time; maintain per-tenant schema evolution history with Data Catalog contracts; include a testing strategy, a 24h rollback plan, fault-injection scenarios, latency targets, and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create per-tenant datasets and Authorized Views; use CMEK keys for each tenant in Cloud KMS and policy-based key rotation. Ingest via Pub/Sub into Dataflow, write to partitioned BigQuery tables; share",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Dataflow (Beam) streaming pipeline ingesting 25 TB/day from Pub/Sub, routing per-tenant data to separate BigQuery datasets, enforcing real-time privacy (mask/tokenize PII) and per-tenant sche",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Ingest via Dataflow from Pub/Sub to two regional BigQuery datasets. Use ingestion-time partitioned tables, cluster by tenant_id, and apply row-level security so each tenant sees only their data. Keep",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant Vertex AI Feature Store with isolated feature stores and versioned schemas. Ingest 1–2 TB/day from Pub/Sub via Dataflow, applying PII masking/tokenization with DLP, and write both raw and m",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nNew angle focusing on per-tenant schema evolution, privacy at streaming scale, and contract-driven governance using Data Catalog.\n\n## Key Concepts\n- Streaming dataflow with per-tenant routing and isolation\n- Schema evolution governed by a central registry/contract in Data Catalog\n- Real-time privacy masking/tokenization in Beam\n- Per-tenant BigQuery datasets/tipelines and cost controls\n- 24h per-tenant rollback, testing, and fault-injection\n\n## Code Example\n```python\nimport apache_beam as beam\nimport json\n\nclass MaskPIIFn(beam.DoFn):\n  def process(self, element):\n    record = json.loads(element)\n    if 'ownerPhone' in record:\n      record['ownerPhone'] = 'XXX-XXX-' + str(record['ownerPhone'])[-4:]\n    yield json.dumps(record)\n```\n\n## Follow-up Questions\n- How would you validate backward compatibility of schemas across tenants during upgrades and rollbacks?\n- How would you detect and handle schema drift when tenants introduce new fields?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-10",
        "questionId": "q-12252",
        "question": "On GCP, implement a beginner-friendly streaming ETL: 500 GB/day of JSON events published to Pub/Sub. Build a Dataflow (Beam) job that validates required fields, masks PII, and writes per-tenant records to partitioned BigQuery tables using event_id for idempotent MERGE upserts. Malformed messages go to a dead-letter Pub/Sub topic and originals to Cloud Storage. Include a 24h per-tenant rollback and basic tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: Dataflow Python beam pipeline reads Pub/Sub JSON events, validates required fields, masks PII (SSN, email), and upserts into per-tenant partitioned BigQuery tables using event_id fo",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a Dataflow streaming job that ingests 50 GB/day of JSON events from Pub/Sub into a shared BigQuery raw table. Implement per-tenant isolation using Authorized Views that grant tenants access to only their specific…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a streaming Dataflow pipeline to ingest 5 TB/day of multi-tenant JSON events via Pub/Sub, implementing field-level tokenization for sensitive data protection while computing per-tenant aggregates with differentia…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose streaming ingestion from Pub/Sub into Dataflow; join with tenant data in BigQuery Data Clean Room to enable privacy-preserving cross-tenant joins with a partner; per-tenant isolation via separ",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests building a practical, beginner-friendly streaming ETL, focusing on Pub/Sub -> Dataflow -> BigQuery, basic data quality, privacy, idempotent writes, and rollback/testing—core skills for data engineers at scale.\n\n## Key Concepts\n- Pub/Sub streaming ingestion\n- Apache Beam (Dataflow) transformations\n- Field validation and PII masking\n- Idempotent upserts in BigQuery (MERGE)\n- Per-tenant partitioning strategy\n- Dead-letter handling and archival\n- Rollback and basic tests\n\n## Code Example\n```python\n# Minimal Dataflow pipeline sketch (Python)\nimport json\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\nclass MaskPII(beam.DoFn):\n  def process(self, x):\n    if 'ssn' in x:\n      x['ssn'] = None\n    if 'email' in x:\n      user, dom = x['email'].split('@', 1) if '@' in x['email'] else (x['email'], 'REDACTED')\n      x['email'] = f\"{user}@REDACTED\" if dom else 'REDACTED'\n    return [x]\n\ndef run():\n  opts = PipelineOptions()\n  with beam.Pipeline(options=opts) as p:\n    (p\n     | 'ReadPubSub' >> beam.io.ReadFromPubSub(subscription='projects/.../subscriptions/...', with_attributes=False)\n     | 'Parse' >> beam.Map(lambda b: json.loads(b))\n     | 'Mask' >> beam.ParDo(MaskPII())\n     | 'WriteToBQ' >> beam.io.WriteToBigQuery(\n         table='project.dataset.table$YYYYMMDD',\n         write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND)\n    )\n```\n\n## Follow-up Questions\n- How would you validate idempotency and handle event retries?\n- How would you adapt for new fields or schema evolution without breaking existing pipelines?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-11",
        "questionId": "q-17094",
        "question": "On GCP, design a cross-region streaming analytics pipeline that ingests 10 TB/day of JSON events via Pub/Sub, aggregates per-tenant metrics in BigQuery across us-central1 and europe-west1 with privacy masking, ensures exactly-once semantics and low-latency replication, preserves end-to-end lineage in Data Catalog, and implements a 24h per-tenant rollback, data contracts, and a robust testing/rollback strategy including canary rollout and fault injection?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-region Dataflow pipelines ingest Pub/Sub into a shared RAW BigQuery table with per-tenant Authorized Views. Cloud DLP performs in-flight redaction before storage; aggregates written to region-loca",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest 2–5 TB/day via Pub/Sub and Cloud Storage; Dataflow computes tenant-scoped features with event-time windows; write to BigQuery tenant datasets with IAM-based isolation; roll out Vertex AI Featur",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a dual-region Dataflow streaming pipeline that reads from Pub/Sub, applies per-tenant tokenization and privacy masking using a dedicated key management service, and writes aggregated metrics to partitioned BigQuery…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose per-tenant isolation with separate BigQuery datasets; Pub/Sub to Dataflow for masking and feature extraction; Vertex AI per-tenant models for online scoring; store results in BigQuery; tag con",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests your ability to design cross-region streaming architectures at scale while addressing data governance, consistency guarantees, and operational resilience.\n\n## Key Concepts\n\n- **Cross-region streaming and consistency**: Managing data flow across geographically distributed regions while maintaining low latency\n- **Exactly-once processing**: Ensuring no data loss or duplication through idempotent writes and checkpointing\n- **Per-tenant isolation and privacy**: Tokenization, masking, and access controls for multi-tenant data\n- **Lineage and contracts**: Tracking data provenance and enforcing schema validation\n- **Time travel and rollback**: Implementing point-in-time recovery capabilities\n\n## Code Example\n\n```java\n// Dataflow streaming pipeline for ingestion with privacy masking\npublic class StreamingPipeline {\n    public static void main(String[] args) {\n        PipelineOptions options = PipelineOptionsFactory.fromArgs(args).create();\n        Pipeline pipeline = Pipeline.create(options);\n        \n        pipeline.apply(\"Ingest from Pub/Sub\", PubsubIO.readMessagesJson()\n            .withTimestampAttribute(\"eventTime\")\n            .withSubscription(options.getSubscription()))\n            .apply(\"Window per Tenant\", Window.into(FixedWindows.of(Duration.standardMinutes(5))))\n            .apply(\"Mask PII\", ParDo.of(new MaskPIIFn()))\n            .apply(\"Write to BigQuery\", BigQueryIO.write()\n                .to(options.getOutputTable())\n                .withSchema(getSchema())\n                .withCreateDisposition(CreateDisposition.CREATE_NEVER)\n                .withWriteDisposition(WriteDisposition.WRITE_APPEND));\n        \n        pipeline.run();\n    }\n}\n```\n\n## Follow-up Questions\n\n- How would you handle schema drift in your pipeline?\n- What strategies would you use for canary rollouts and fault injection testing?\n- How do you ensure compliance with GDPR right-to-erasure requirements?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-12",
        "questionId": "q-22158",
        "question": "On GCP, design a scalable, tenant-aware streaming analytics platform that can onboard/offboard tenants at runtime without downtime. Ingest 5 TB/day of JSON events via Pub/Sub, route each tenant to isolated per-tenant BigQuery partitions, enforce privacy with DLP masking and per-tenant Authorized Views, maintain data lineage in Data Catalog, and share aggregated metrics via Data Exchange. Include 24h per-tenant rollback and a zero-downtime rollout plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant contracts in Data Catalog; Dataflow streaming with DoFn checks for schema drift, nulls, and value ranges; quarantine bad records; strict per-tenant BigQuery; lineage in Data Catalog; 24h ro",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub for event ingestion, then Dataflow with per-tenant routing to partitioned BigQuery tables keyed by tenant_id. Apply DLP masking on ingest, and expose per-tenant results via Authorized Views for strict tenant…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a streaming pipeline: Pub/Sub subscription → Dataflow (Beam) with a custom DoFn that deduplicates events by (tenant_id, event_id) using a 60-second fixed window → write to a time-partitioned BigQuery table (parti…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest via Pub/Sub, stream with Dataflow to a daily-partitioned BigQuery table, validate fields, apply tenant-specific masking using a policy table, and enforce schema via Pub/Sub Schema Registry. Rou",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design scalable multi-tenant streaming pipelines with runtime tenant lifecycle management, privacy controls, and governance.\n\n## Key Concepts\n- Tenant-aware routing and partitioning strategies\n- Dataflow/Beam streaming architecture patterns\n- DLP masking and Authorized Views for privacy enforcement\n- Data Catalog lineage tracking and Data Exchange for sharing\n- Rollback strategy using snapshots and time-travel\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.io.gcp.bigquery import WriteToBigQuery\n\nclass MaskAndRoute(beam.DoFn):\n  def process(self, element):\n    tenant_id = element.get('tenant_id')\n    masked = self._apply_dlp_masking(element)\n    yield beam.TaggedOutput(f'tenant_{tenant_id}', masked)\n  \n  def _apply_dlp_masking(self, record):\n    # Apply DLP transformations\n    return record\n\n# Streaming pipeline with side inputs for tenant config\nwith beam.Pipeline() as p:\n  events = p | 'ReadFromPubSub' >> beam.io.ReadFromPubSub(topic='projects/*/topics/events')\n  masked = events | 'MaskAndRoute' >> beam.ParDo(MaskAndRoute()).with_outputs()\n```",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-13",
        "questionId": "q-26699",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events from Pub/Sub into BigQuery in a single region. NEW ANGLE: optimize cost by comparing real-time streaming vs micro-batching with 60s windows, justify the chosen approach, and illustrate how to configure a Dataflow job to write to a time-partitioned BigQuery table with basic monitoring and error handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming pipeline: Pub/Sub subscription → Dataflow (Beam) with a custom DoFn that deduplicates events by (tenant_id, event_id) using a 60-second fixed window → write to a time-partitioned BigQuery table (parti…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Dataflow/Beam with a DoFn that validates each Pub/Sub JSON against a compact inline schema, coerces types, and emits invalid records to a quarantine sink with a reason. Route valid records to a pa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Architecture Decision: 60s Micro-Batching** For a 50 GB/day clickstream workload in a single region, choose **60s micro-batching** over pure streaming: | Factor | Real-Time Streaming | 60s Micro-Batching | |--------|-…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a Dataflow (Beam) streaming pipeline ingesting 25 TB/day from Pub/Sub, routing per-tenant data to separate BigQuery datasets, enforcing real-time privacy (mask/tokenize PII) and per-tenant sche",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical, cost-aware design decisions for GCP data pipelines. It tests understanding of:\n\n- When to prefer batched over streaming ingestion\n- Beam windowing strategies for micro-batching\n- BigQuery partitioning and clustering for query optimization\n- Cost optimization through infrastructure configuration\n- Monitoring and error handling for production workloads\n\n## Key Concepts\n- **Dataflow/Beam windowing:** Fixed windows enable micro-batching without complex triggering logic\n- **Pub/Sub:** Durable, scalable ingestion layer with at-least-once delivery\n- **BigQuery partitioning:** Date-based partitioning reduces scan costs; clustering on `user_id` optimizes user-scoped queries\n- **FILE_LOADS method:** Batches rows into GCS files before loading, dramatically reducing BigQuery costs compared to streaming inserts\n- **Dedup strategy:** Use `insertId` in streaming or rely on `FILE_LOADS` batch deduplication\n\n## Common Pitfalls\n- Using streaming inserts without cost analysis\n- Missing windowing configuration, causing unbounded groups\n- Not clustering tables for high-cardinality filter columns\n- Omitting dead-letter queue routing for failed records",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-14",
        "questionId": "q-6806",
        "question": "Design a beginner-friendly GCP data pipeline to ingest 100 MB/day of JSON logs from Pub/Sub into BigQuery using Dataflow. Flatten nested JSON, normalize timestamps, and enrich with a small country lookup table stored in BigQuery. Isolate tenants with separate datasets, implement idempotent writes, route malformed messages to a dead-letter topic, and implement a 24-hour rollback by re-ingesting archived JSON from Cloud Storage. Include architecture, data model, and testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-tenant streaming pipeline on GCP ingesting 5 TB/day of JSON events from Pub/Sub into BigQuery across two regions. Enforce per-tenant isolation with tenant-scoped datasets, a central sch",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Dataflow (Python) streaming pipeline: Pub/Sub -> Dataflow -> per-tenant BigQuery datasets. Flatten nested JSON, normalize event_time, and enrich with a country lookup in BigQuery via a side i",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a multi-tenant streaming pipeline on GCP that ingests 200 TB/day of telemetry data from Pub/Sub, processes it through Dataflow with per-tenant isolation and privacy masking, writes Parquet files to Cloud Storage…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest 15 TB/day via Pub/Sub, route per-tenant data using tenant_id partitions in Dataflow, and write to BigQuery with per-tenant Authorized Views and RLS. Apply DLP masking on PII, store contracts/li",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to translate a beginner scenario into a repeatable streaming pipeline with basic features: parsing, enrichment, per-tenant isolation, dedup, DLQ, and archival for rollback. Ensures familiarity with Dataflow, Pub/Sub, and BigQuery integration.\n\n## Key Concepts\n- Dataflow DoFn for flattening and enrichment\n- Pub/Sub schema and message validation\n- Per-tenant isolation via separate BigQuery datasets\n- Idempotent writes with insertId or MERGE\n- Dead-letter handling for bad messages\n- Cloud Storage archival for rollback\n- 24h rollback workflow by re-ingesting archived data\n\n## Code Example\n```python\nimport apache_beam as beam\nimport json\n\nclass FlattenEnrich(beam.DoFn):\n    def process(self, element, country_lookup):\n        record = json.loads(element)\n        data = {\n            \"tenant\": record.get(\"tenant\"),\n            \"event_time\": record.get(\"event_time\"),\n            \"user_id\": record.get(\"user\", {}).get(\"id\"),\n            \"country\": country_lookup.get(record.get(\"region\"), \"unknown\")\n        }\n        yield data\n```\n\n## Follow-up Questions\n- How would you test idempotency and message deduplication in this pipeline?\n- How would you handle late or out-of-order data while preserving correct event-time semantics?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-15",
        "questionId": "q-5297",
        "question": "On GCP, design a two-region, cross-tenant streaming analytics platform for 1 TB/day of JSON events ingested via Pub/Sub. Build per-tenant data contracts with drift detection and automatic schema evolution, plus per-tenant cost controls (BQ slot reservations) and SLA-bound latency. Include architecture, data models, rollback plan within 24h, and testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a two-region streaming pipeline: 1 TB/day of JSON events from Pub/Sub per tenant ingested via Dataflow into region-local storage and per-tenant BigQuery datasets with Authorized Views. Enforce",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-tenant isolation via per-tenant BigQuery datasets and Pub/Sub topics, a Dataflow streaming pipeline to both regions, and a concurrent Data Quality pipeline that validates schema conformance, null",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Dataflow streaming job that ingests Pub/Sub JSON events, deduplicates by event_id, and enriches with merchant/user lookups from BigQuery. Write per-tenant aggregates to partitioned BigQuery tables with strict tena…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest via Dataflow from Pub/Sub to two regional BigQuery datasets. Use ingestion-time partitioned tables, cluster by tenant_id, and apply row-level security so each tenant sees only their data. Keep",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design multi-region, cross-tenant contracts with automated drift detection, policy-driven masking, and cost controls, using core GCP services. Emphasizes production-readiness: rollback, testing, and governance.\n\n## Key Concepts\n- Data contracts per tenant and drift detection\n- Drift-aware schema evolution and automated upgrades\n- Cross-region replication, latency commitments, and late data handling\n- Per-tenant cost controls (BigQuery Reservations/slots) and SLA guarantees\n- Immutable audit logs and end-to-end data lineage via Data Catalog\n\n## Code Example\n```python\nimport apache_beam as beam\nclass MaskPII(beam.DoFn):\n  def process(self, element):\n    if isinstance(element, dict) and 'ssn' in element:\n      element['ssn'] = '***-**-****'\n    yield element\n```\n\n## Follow-up Questions\n- How would you detect and resolve schema drift across hundreds of tenants?\n- How would you model per-tenant cost budgets and alerting in real time?\n- What tests would you run to validate rollback correctness and data integrity?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-16",
        "questionId": "q-23146",
        "question": "On GCP, design a streaming pipeline that ingests 5 TB/day of JSON events from Pub/Sub into BigQuery with per-tenant isolation; describe how you would implement a versioned JSON schema registry, end-to-end lineage, automatic schema evolution, and a robust 30-day rollback, testing, and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a telemetry pipeline with Pub/Sub, Dataflow, and BigQuery, enforcing per-service data contracts via Pub/Sub Schema Registry and Data Catalog tagging. Redact PII at ingest, store raw in Cloud S",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub → Dataflow with a DoFn that validates each event against a versioned JSON schema stored in Cloud Storage; route valid events to a tenant-scoped, partitioned BigQuery table and invalid ones",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Streaming via Pub/Sub -> Dataflow templates; apply per-tenant masking with a DoFn and DLP; write to regionally replicated BigQuery datasets partitioned by ingest_date and clustered by tenant_id; Data",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest with Pub/Sub and Dataflow streaming into per-tenant BigQuery partitions; enforce retention with partition expiration and a Cloud Storage archival tier with lifecycle rules. Use a per-tenant lif",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end data governance: schema management, per-tenant isolation, lineage, and rollback in a real streaming context.\n\n## Key Concepts\n- Versioned JSON schema registry in Cloud Storage\n- Dataflow DoFn for per-record validation\n- Tenant-scoped partitioned BigQuery tables\n- End-to-end lineage via Data Catalog\n- Automated schema evolution with backfill and rollback window\n- Data quality quarantine, alerting, and monitoring\n\n## Code Example\n```python\n# Pseudo: Dataflow DoFn validating events against current schema version\nclass ValidateEvent(DoFn):\n    def process(self, element, schema_version):\n        schema = load_schema(schema_version)\n        if validate(element, schema):\n            yield element\n        else:\n            yield pvalue.TaggedOutput('invalid', element)\n```\n\n## Follow-up Questions\n- How would you implement canary rollout of a new schema version?\n- How do you handle late-arriving events referencing older schema versions?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-17",
        "questionId": "q-17679",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. New angle: implement end-to-end observability by emitting custom Dataflow metrics for per-tenant latency, in-flight window size, and failed records; export metrics to Cloud Monitoring, and create a lightweight dashboard. Also document how to validate alarms with synthetic tenants and ensure no data path leaks between tenants?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Dataflow pipeline that tags each event with tenant_id, writes to a partitioned RAW BigQuery table, and emits per-tenant metrics for latency, in-flight window, and failures to Cloud Monitoring",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Pub/Sub and Dataflow for streaming, isolating tenants with per-tenant Cloud KMS keys and envelope encryption; store raw Parquet in regional Cloud Storage; materialize per-tenant aggregates in BigQ",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a staging BigQuery table where Dataflow writes with insertId derived from a stable key (user_id + event_id + event_time). Periodically MERGE from staging into a final partitioned table, deduplicat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Dataflow streaming job to ingest 1.5 TB/day from Pub/Sub, apply per-tenant schema validation from a Registry, run quality gates (nulls, type, ranges), and write to per-tenant BigQuery datasets w",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAdds observability to a straightforward Pub/Sub to BigQuery ingestion, a practical yet beginner-accessible enhancement.\n\n## Key Concepts\n- Pub/Sub ingestion and partitioned BigQuery RAW/CLEAN tables\n- Dataflow per-tenant tagging and metrics\n- Cloud Monitoring dashboards and alerts\n- Dead-lettering and idempotent writes\n\n## Code Example\n```javascript\n// Pseudo Dataflow metric example (conceptual)\nfunction processElement(elem){\n  const tenant = elem.tenant_id;\n  Metrics.counter('ingest_latency_ms', {tenant_id: tenant}).inc(elem.latency_ms);\n}\n```\n\n## Follow-up Questions\n- How would you implement dead-letter processing and retries?\n- How would you validate alert thresholds with synthetic tenants?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-18",
        "questionId": "q-17732",
        "question": "On GCP, design a streaming analytics platform that ingests 5-10 TB/day of multi-tenant web-click events via Pub/Sub, processes with Dataflow, and writes per-tenant aggregates to BigQuery. Implement a metadata-driven access model: automatically grant a partner read access only to aggregated per-tenant data using Data Catalog policy tags and Authorized Views so a partner can query only TenantA aggregates, while tenant data remains isolated. Include a rollback strategy, schema evolution approach, and testing plan for drift and late-arriving data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant isolation via per-tenant BigQuery partitions and Dataflow triggers, plus a metadata-driven access layer: Data Catalog policy tags + Authorized Views so a partner can query only TenantA aggr",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a Batch Dataflow job reading gs://logs/YYYY/MM/DD/part-*.json, validate event_id, user_id, ts, action, and upsert into a daily-partitioned BigQuery table using MERGE on event_id. Push malfor",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a multi-region streaming pipeline with Pub/Sub ingestion, Dataflow processing across two regions, and per-tenant BigQuery tables for feature vectors. Compute real-time drift metrics including distribution shif…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Pub/Sub Schema Registry with per-tenant Avro schemas to enforce data contracts at ingestion. Dataflow validates and enriches events in streaming mode, then writes Parquet shards to Cloud Storage. Expose tenant datas…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design multi-tenant streaming pipelines with strict access control and rollback capabilities.\n\n## Key Concepts\n\n- Per-tenant isolation with BigQuery partitions and Dataflow\n- Data Catalog policy tags and Authorized Views for metadata-driven access\n- Disaster rollback window and schema evolution strategy\n- Testing for drift and late-arriving data\n\n## Code Example\n\n```sql\nCREATE VIEW `proj.ds.tenantA_aggregates` AS\nSELECT tenant_id, COUNT(*) AS evt, SUM(metric) AS total\nFROM `proj.ds.tenants_tenantA.events`\nGROUP BY tenant_id;\n```\n\n## Follow-up Questions\n\n- How would you propagate Data Catalog tags to new tenants during onboarding?\n- How would you test rollback across multiple tenants without affecting production?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-19",
        "questionId": "q-23789",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. NEW ANGLE: implement schema-on-read by storing events in a single VARIANT column and maintaining a lightweight Firestore-based schema registry to track current vs. deprecated fields. How would you evolve schemas, test compatibility, and query missing fields?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pub/Sub ingest 5 TB/day JSON events; Dataflow streaming with per-tenant windowing and idempotent writes keyed by (tenant_id, event_id); publish per-tenant scores to partitioned BigQuery; expose via Cl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant contracts in Data Catalog, stream 1-3 TB/day from Pub/Sub through Dataflow into per-tenant BigQuery datasets via BigLake external tables. Add automated data quality gates and sche",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a pipeline that writes to BigQuery with a single VARIANT column for event payloads, plus a Firestore schema registry for field versions. Dataflow parses Pub/Sub JSON, validates against registry",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Set up a data-product ecosystem using Pub/Sub -> Dataflow -> Parquet in GCS; register schemas in Data Catalog with policy tags; auto-apply DLP masking on PII; export per-tenant external tables via Big",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProbes schema evolution skills and practical data modeling in GCP.\n\n## Key Concepts\n- Schema-on-read, VARIANT in BigQuery\n- Lightweight schema registry (Firestore)\n- Backward/forward compatibility testing\n\n## Code Example\n```javascript\n// Example: reading a VARIANT payload and extracting fields with defaulting\nconst event = payload.payload; // VARIANT\nconst tenant = event.tenant ?? 'unknown';\nconst ts = (event.timestamp ?? Date.now())/1000;\n```\n```\n\n## Follow-up Questions\n- How would you automate registry updates across deployments?\n- How would you handle schema conflicts during high-velocity ingestion?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-20",
        "questionId": "q-15924",
        "question": "On GCP, design a multi-tenant streaming data lake handling 2-5 TB/day of JSON events. Ingest via Pub/Sub, enrich in Dataflow, and write to per-tenant BigQuery datasets. Add a live data-quality guardrail: detect schema drift and anomalous field distributions in near real time, quarantine nonconforming records to a separate sink, and support per-tenant rollback by reprocessing from the raw source. Use Data Catalog for schemas and contracts, include testing, fault-injection, latency targets, and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Cloud Dataflow (Python) batch pipeline to read GCS CSVs, validate fields, deduplicate by run_id, and upsert to a daily-partitioned BigQuery table via MERGE. Add optional device_type via a schema_v",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a streaming guardrail that validates each incoming JSON event against the tenant's current schema version registered in Data Catalog, computes drift metrics in-flight, and quarantines nonconforming records to…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a Dataflow streaming job that reads JSON messages from Pub/Sub, validates required fields and data types, and writes to a daily-partitioned BigQuery table using insertId for idempotent writes. Route invalid me…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Pub/Sub to feed Dataflow streaming jobs that validate JSON schemas, mask PII via DLP, and emit per-tenant records to separate BigQuery datasets in two regions. Enforce IAM on a per-dataset basis,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design real-time data quality guardrails at scale, integrating Data Catalog's schema lifecycle management with per-tenant isolation and robust rollback capabilities.\n\n## Key Concepts\n- Streaming validation against cataloged schemas with version control\n- Schema drift detection and automated quarantining of nonconforming records\n- Per-tenant rollback via replay from raw source (Pub/Sub retains messages for configurable duration)\n- Fault injection for chaos engineering\n- Latency target of <500ms p99 for end-to-end processing\n- Cost-aware design leveraging streaming inserts over batch loads\n\n## Code Example\n(Implementation code would demonstrate schema validation logic, drift metric computation, and quarantine routing using Dataflow)",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-21",
        "questionId": "q-24475",
        "question": "On GCP, design a tenant-aware data quality and lineage pipeline for 1-3 TB/day of device telemetry ingested via Pub/Sub. Implement per-tenant contracts, schema drift detection, automated anomaly alerts, and immutable audit logs via Data Catalog/Dataflow/BigQuery. Include 24-hour per-tenant rollback using BigQuery time travel and a changelog in Cloud Storage, plus testing and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Dataflow-based streaming pipeline ingesting 100 GB/day Pub/Sub JSON per 3 tenants, using a versioned event schema with a Data Catalog registry, canary rollout, and per-tenant views. Add DLP-b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a Dataflow streaming job with a typed Beam schema to parse JSON, emit valid events to BigQuery (WRITE_APPEND) and invalid to Cloud Storage as a manifest. Include schema-version in each record, s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Onboard 2–4 external partners ingesting 2–3 TB/day via Pub/Sub. Create per-tenant contracts in Data Catalog, stream through Dataflow into Cloud Storage Parquet files partitioned by tenant/date. Expose",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a streaming Dataflow job that validates messages against per-tenant contracts in Data Catalog, routes nonconforming events to a DLQ, and writes valid data to per-tenant partitioned BigQuery tables",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical data quality governance in a multi-tenant, high-volume pipeline, including contracts, drift detection, per-tenant rollback, and auditability across Data Catalog, Dataflow, and BigQuery. It tests integration choices, failure handling, and cost considerations.\n\n## Key Concepts\n\n- Per-tenant data contracts in Data Catalog\n- Data quality validation in Dataflow streaming pipelines\n- Schema drift detection and drift alerting\n- BigQuery time travel for 24h per-tenant rollback\n- Immutable audit logs and changelogs in Cloud Storage\n- Dead-letter queues and monitoring for reliability\n\n## Code Example\n\n```python\n# Pseudo validator for a tenant contract\nfrom typing import Dict\n\ndef validate_event(event: Dict, contract: Dict) -> bool:\n    required = contract.get('required_fields', [])\n    for field in required:\n        if field not in event:\n            return False\n    # basic type checks (example)\n    if 'device_id' in event and not isinstance(event['device_id'], str):\n        return False\n    return True\n```\n\n## Follow-up Questions\n\n- How would you handle contract evolution while preserving backward compatibility?\n- How would you set and tune drift thresholds and alerting in Cloud Monitoring for multi-tenant workloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-22",
        "questionId": "q-8052",
        "question": "On GCP, design a privacy-preserving, cross-tenant streaming analytics pipeline for 2 TB/day of JSON events arriving via Pub/Sub, targeting per-tenant BigQuery datasets. Implement per-tenant data contracts and field-level masking using DLP, maintain end-to-end lineage with Data Catalog, and support per-tenant 24h rollback. Include a real-time cross-tenant leakage detector, a testing strategy with synthetic tenants and canary rollouts, and plan for schema evolution and cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To guarantee idempotent ingestion, implement a streaming pipeline using Pub/Sub -> Dataflow (Beam) -> BigQuery, setting a per-record insertId derived from event_id and ts. Use WriteToBigQuery with ins",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I'd implement a per-tenant data quality framework: a Data Catalog-tagged schema registry, Dataflow validation templates that enforce tenant schemas and quality rules, with invalid records redirected to a quarantine Pub/…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest 2 TB/day via Pub/Sub; use Dataflow streaming with per-tenant routing; apply DLP masking at ingest; write to per-tenant BigQuery datasets with Authorized Views; maintain end-to-end lineage in Da",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest 2-5 TB/day of JSON via Pub/Sub; Dataflow runs per-tenant validity checks and sliding-window anomaly detection, emitting per-tenant flags. Store results in partitioned BigQuery tables keyed by t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question tests practical mastery of privacy, lineage, and multi-tenant isolation in GCP with real-world constraints like rollback and testing.\n\n## Key Concepts\n- Pub/Sub -> Dataflow per-tenant routing\n- DLP masking at ingest\n- Data Catalog lineage across sources and sinks\n- Per-tenant BigQuery datasets with fine-grained access\n- 24h tenant rollback and canary schema evolution\n- Cross-tenant leakage detector; synthetic tenants for testing\n\n## Code Example\n```python\n# Example: quick field masking scaffold\ndef mask_fields(record, fields_to_mask):\n    for f in fields_to_mask:\n        if f in record:\n            record[f] = 'REDACTED'\n    return record\n```\n\n## Follow-up Questions\n- How would you validate leakage detector accuracy and false positives?\n- What latency/throughput trade-offs exist with masking depth?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-23",
        "questionId": "q-27911",
        "question": "On GCP, design a multi-tenant telemetry pipeline that ingests 1-3 TB/day of device events via Pub/Sub and enforces per-tenant data contracts using Pub/Sub Schema Registry with Avro schemas. Validate in Dataflow, store Parquet in Cloud Storage, expose per-tenant datasets as BigQuery external tables with row-level security. Include schema evolution, canary promotions, automated contract tests, and per-tenant rollback across two regions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest with Dataflow (Beam) reading Pub/Sub, route by tenant to per-tenant BigQuery datasets; create datasets on demand. Apply DLP masking for sensitive fields (email, phone) in the pipeline. Use a pe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a per-tenant Dataflow streaming job that reads Pub/Sub, validates against a Data Catalog schema registry, enforces quality checks (null density, ranges, referential integrity), and writes to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Dataflow streaming job that ingests Pub/Sub JSON events, deduplicates by event_id, and enriches with merchant/user lookups from BigQuery. Write per-tenant aggregates to partitioned BigQuery tables with strict tena…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Pub/Sub Schema Registry with per-tenant Avro schemas to enforce data contracts at ingestion. Dataflow validates and enriches events in streaming mode, then writes Parquet shards to Cloud Storage. Expose tenant datas…",
            "isCorrect": true
          }
        ],
        "explanation": "This question assesses cross-service architecture design and operational maturity. Key components include Pub/Sub Schema Registry with Avro schemas for per-tenant contract enforcement, Dataflow for validation and enrichment, and Cloud Storage with Parquet for cost-efficient storage. BigQuery external tables with row-level security enable query isolation without data duplication. Critical production considerations include schema evolution with backward/forward compatibility, canary promotions for gradual rollouts, automated contract tests, and per-tenant rollback capabilities across two regions.\n\n```python\n# Dataflow pipeline skeleton with contract validation\n\nclass MultiTenantPipeline(Pipeline):\n    def __init__(self, project, region, schema_registry):\n        self.project = project\n        self.region = region\n        self.schema_registry = schema_registry\n\n    def build_pipeline(self, input_subscription, output_path):\n        return (\n            pipeline\n            | 'ReadFromPubSub' >> ReadFromPubSub(subscription=input_subscription)\n            | 'ValidateAgainstSchema' >> ParDo(SchemaValidationDoFn(self.schema_registry))\n            | 'EnrichWithTenantContext' >> ParDo(TenantEnrichmentDoFn())\n            | 'WriteToGCS' >> WriteToParquet(output_path)\n        )\n\nclass SchemaValidationDoFn(DoFn):\n    def __init__(self, schema_registry):\n        self.schema_registry = schema_registry\n\n    def process(self, element):\n        tenant_id = element.get('tenant_id')\n        schema = self.schema_registry.get_schema(tenant_id)\n        try:\n            validated = schema.validate(element)\n            yield pvalue.TaggedOutput('valid', validated)\n        except Exception as e:\n            yield pvalue.TaggedOutput('invalid', (element, str(e)))\n\nclass TenantEnrichmentDoFn(DoFn):\n    def process(self, element):\n        element['processed_timestamp'] = datetime.utcnow().isoformat()\n        element['region'] = self.region\n        yield element\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-24",
        "questionId": "q-24188",
        "question": "On GCP, design a real-time multi-tenant data quality and lineage pipeline: ingest 5 TB/day of JSON events via Pub/Sub into BigQuery; enforce per-tenant schema contracts and validate every event in Dataflow; emit only compliant rows to tenant-specific BigQuery partitions; detect drift (missing fields, type changes) and trigger per-tenant rollbacks with 24h retention; expose quality metrics via Data Exchange?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a streaming pipeline: Pub/Sub -> Dataflow streaming templates -> per-tenant BigQuery in two regions with partitioned tables; tokenization/masking via Data Loss Prevention in the transform; e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Dataflow-based streaming pipeline that ingests Pub/Sub JSON events, routes per-tenant data into dedicated BigQuery partitions, masks PII at ingest using Cloud DLP or custom UDFs, records end-to-end lineage in D…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a streaming quality framework: Pub/Sub Avro events flow into Dataflow templates that enforce per-tenant contracts (required fields, types, value ranges) and emit validated rows to tenant-specific BigQuery tabl…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage per-tenant schema contracts tracked in Data Catalog, a Beam Dataflow streaming pipeline that validates events against the contract, routing only compliant records to tenant-segregated BigQuer",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nProbes ability to design robust, scalable data quality pipelines with strict per-tenant contracts, real-time validation, and automated rollback in GCP. It also tests use of Data Catalog for lineage, Data Exchange for sharing quality metrics, and safe drift handling in production.\n\n## Key Concepts\n- Per-tenant contracts and schema drift detection\n- Streaming validation in Dataflow/Beam\n- Tenant isolation and rollback strategies\n- Data Catalog lineage and Data Exchange integration\n\n## Code Example\n```python\nimport apache_beam as beam\nimport json\n\nclass ValidateEvent(beam.DoFn):\n  def __init__(self, contract):\n    self.contract = contract\n  def process(self, elem):\n    obj = json.loads(elem)\n    for k, v in self.contract.items():\n      if k not in obj:\n        return\n      t = v.get('type')\n      if t and not isinstance(obj[k], {'string': str, 'int': int}.get(t, object)):\n        return\n    yield json.dumps(obj)\n```\n\n## Follow-up Questions\n- How would you implement per-tenant rollback guarantees and data-evolution strategies?\n- How would you scale drift detection and decide when to trigger a rollback?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-25T09:44:56.028Z",
    "version": 1
  }
]