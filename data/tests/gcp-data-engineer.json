[
  {
    "id": "test-gcp-data-engineer",
    "channelId": "gcp-data-engineer",
    "channelName": "GCP Data Engineer",
    "title": "GCP Data Engineer Knowledge Test",
    "description": "Test your understanding of GCP Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "gcp-data-engineer-q-0",
        "questionId": "q-26699",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events from Pub/Sub into BigQuery in a single region. NEW ANGLE: optimize cost by comparing real-time streaming vs micro-batching with 60s windows, justify the chosen approach, and illustrate how to configure a Dataflow job to write to a time-partitioned BigQuery table with basic monitoring and error handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a CI/CD-driven end-to-end test harness that generates deterministic synthetic events per tenant, publishes them to Pub/Sub, executes a Dataflow pipeline in staging, and validates idempotent upserts with exact-…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Architecture Decision: 60s Micro-Batching** For a 50 GB/day clickstream workload in a single region, choose **60s micro-batching** over pure streaming: | Factor | Real-Time Streaming | 60s Micro-Batching | |--------|-…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose per-tenant isolation with separate BigQuery datasets; Pub/Sub to Dataflow for masking and feature extraction; Vertex AI per-tenant models for online scoring; store results in BigQuery; tag con",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose exactly-once streaming in Dataflow with a tenant-keyed dedupe (tenant_id, txn_id) and watermarks to handle late arrivals. Use Pub/Sub arrival order with idempotent sink to BigQuery via Parquet",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical, cost-aware design decisions for GCP data pipelines. It tests understanding of:\n\n- When to prefer batched over streaming ingestion\n- Beam windowing strategies for micro-batching\n- BigQuery partitioning and clustering for query optimization\n- Cost optimization through infrastructure configuration\n- Monitoring and error handling for production workloads\n\n## Key Concepts\n- **Dataflow/Beam windowing:** Fixed windows enable micro-batching without complex triggering logic\n- **Pub/Sub:** Durable, scalable ingestion layer with at-least-once delivery\n- **BigQuery partitioning:** Date-based partitioning reduces scan costs; clustering on `user_id` optimizes user-scoped queries\n- **FILE_LOADS method:** Batches rows into GCS files before loading, dramatically reducing BigQuery costs compared to streaming inserts\n- **Dedup strategy:** Use `insertId` in streaming or rely on `FILE_LOADS` batch deduplication\n\n## Common Pitfalls\n- Using streaming inserts without cost analysis\n- Missing windowing configuration, causing unbounded groups\n- Not clustering tables for high-cardinality filter columns\n- Omitting dead-letter queue routing for failed records",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-1",
        "questionId": "q-8052",
        "question": "On GCP, design a privacy-preserving, cross-tenant streaming analytics pipeline for 2 TB/day of JSON events arriving via Pub/Sub, targeting per-tenant BigQuery datasets. Implement per-tenant data contracts and field-level masking using DLP, maintain end-to-end lineage with Data Catalog, and support per-tenant 24h rollback. Include a real-time cross-tenant leakage detector, a testing strategy with synthetic tenants and canary rollouts, and plan for schema evolution and cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest 2 TB/day via Pub/Sub; use Dataflow streaming with per-tenant routing; apply DLP masking at ingest; write to per-tenant BigQuery datasets with Authorized Views; maintain end-to-end lineage in Da",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Two-region Dataflow pipelines ingest Pub/Sub into a shared RAW BigQuery table with per-tenant Authorized Views. Cloud DLP performs in-flight redaction before storage; aggregates written to region-loca",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Explain enabling Pub/Sub Schemas, a Python Dataflow DoFn that validates fields (tenantId, eventId, ts, url), types, and rejects invalid messages to a Cloud Storage manifest; discuss how to handle opti",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate each message against a fixed schema in a Dataflow custom DoFn. Valid records are written to the primary BigQuery table, while invalid records are routed to both a dedicated errors BigQuery table and Cloud Stora…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question tests practical mastery of privacy, lineage, and multi-tenant isolation in GCP with real-world constraints like rollback and testing.\n\n## Key Concepts\n- Pub/Sub -> Dataflow per-tenant routing\n- DLP masking at ingest\n- Data Catalog lineage across sources and sinks\n- Per-tenant BigQuery datasets with fine-grained access\n- 24h tenant rollback and canary schema evolution\n- Cross-tenant leakage detector; synthetic tenants for testing\n\n## Code Example\n```python\n# Example: quick field masking scaffold\ndef mask_fields(record, fields_to_mask):\n    for f in fields_to_mask:\n        if f in record:\n            record[f] = 'REDACTED'\n    return record\n```\n\n## Follow-up Questions\n- How would you validate leakage detector accuracy and false positives?\n- What latency/throughput trade-offs exist with masking depth?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-2",
        "questionId": "q-10147",
        "question": "On GCP, design a beginner-friendly streaming pipeline that ingests 200 GB/day of JSON events via Pub/Sub, validates the schema, applies per-tenant masking, and writes to a daily-partitioned BigQuery table. Include dead-letter handling, archival of originals, a 24h per-tenant rollback plan, data contracts, and a basic end-to-end data lineage using Data Catalog?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-tenant streaming pipeline on GCP ingesting 5 TB/day of JSON events from Pub/Sub into BigQuery across two regions. Enforce per-tenant isolation with tenant-scoped datasets, a central sch",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest JSON events through Pub/Sub and process with Dataflow using per-tenant schemas from a centralized registry. Quarantine malformed records to Cloud Storage, write validated data to tenant-specific BigQuery datasets…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a cost-aware multi-tenant analytics platform for 20+ tenants on GCP. Ingest data via Pub/Sub, process it with Dataflow, and publish per-tenant results to individual BigQuery datasets. Implement Authorized Views t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest via Pub/Sub, stream with Dataflow to a daily-partitioned BigQuery table, validate fields, apply tenant-specific masking using a policy table, and enforce schema via Pub/Sub Schema Registry. Rou",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical streaming ingestion, per-tenant masking, basic schema validation, dead-letter handling, and rollback at a tenant granularity, aligned with real-world GCP usage for a beginner.\n\n## Key Concepts\n\n- Pub/Sub ingestion and Dataflow streaming\n- Per-tenant masking with policy lookup\n- Daily-partitioned BigQuery and schema validation\n- Dead-letter Pub/Sub and archival of originals\n- 24h per-tenant rollback\n- Data Catalog lineage\n\n## Code Example\n\n```python\n# Pseudo Beam snippet showing masking per-tenant\nclass MaskPerTenant(beam.DoFn):\n  def process(self, element, policies):\n    tenant = element['tenant_id']\n    mask = policies.get(tenant, {})\n    element['customer_email'] = mask_email(element['customer_email'], mask)\n    yield element\n```\n\n## Follow-up Questions\n\n- How would you test rollback per tenant end-to-end?\n- How would you validate schema evolution while retaining backward compatibility?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-3",
        "questionId": "q-9262",
        "question": "On GCP, design a per-tenant streaming data quality and schema-evolution pipeline for 5 TB/day of JSON events ingested via Pub/Sub. Ensure per-tenant isolation, additive/backward-compatible schema evolution, automated data quality checks (null density, value ranges, referential checks), and end-to-end lineage via Data Catalog. Include a rollback path to restore a tenant within 12 hours and a testing strategy with synthetic data and canary tenants?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant isolation via per-tenant BigQuery partitions and Dataflow triggers, plus a metadata-driven access layer: Data Catalog policy tags + Authorized Views so a partner can query only TenantA aggr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Pub/Sub Schema Registry for drift alerts, Dataflow DoFns for validation and normalization, and tenant-partitioned BigQuery tables. Use Data Catalog for per-tenant data contracts and full line",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant Vertex AI Feature Store with isolated feature stores and versioned schemas. Ingest 1–2 TB/day from Pub/Sub via Dataflow, applying PII masking/tokenization with DLP, and write both raw and m",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a per-tenant Dataflow streaming job that reads Pub/Sub, validates against a Data Catalog schema registry, enforces quality checks (null density, ranges, referential integrity), and writes to",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes design for per-tenant data quality and schema evolution in a large-scale streaming pipeline, focusing on data catalog lineage, backward-compatible changes, drift detection, and rollback – all essential for production data platforms.\n\n## Key Concepts\n\n- Streaming data quality checks (null density, ranges, referential integrity)\n- Per-tenant isolation in storage and compute\n- Schema evolution with backward compatibility\n- End-to-end lineage via Data Catalog\n- Drift detection and tenant-specific rollback\n\n## Code Example\n\n```javascript\n// Minimal validator against a registry schema (conceptual)\nfunction validateRecord(record, schema) {\n  // assume 'schema' is loaded from Data Catalog\n  const valid = ajv.validate(schema, record);\n  if (!valid) throw new Error('Validation error: ' + ajv.errorsText());\n  return true;\n}\n```\n\n## Follow-up Questions\n\n- How to test schema drift across tenants and automate remediation?\n- How to handle late-arriving data and replays without breaking lineage?\n- How to enforce tenant isolation at storage and compute layers?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-4",
        "questionId": "q-10327",
        "question": "Design a GCP streaming pipeline that ingests 200 GB/day of JSON from Pub/Sub into BigQuery in a single region using Dataflow. Implement basic data quality (required fields, types), idempotent writes (MERGE or insertId), route malformed events to a dead-letter Pub/Sub and archive originals to Cloud Storage. Provide a 24h rollback by deleting the affected daily BigQuery partition and outline a simple test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I propose a cross-region streaming architecture with separate Pub/Sub topics per region, Dataflow jobs that enforce tenant ID isolation and perform real-time PII masking through DoFn transformations, CMEK-encrypted BigQ…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "We implement tenant-aware drift detection and rollback: ingest Pub/Sub via Dataflow with a contract validator that compares each event against per-tenant schemas in Data Catalog; route valid records t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Dataflow streaming job that reads JSON messages from Pub/Sub, validates required fields and data types, and writes to a daily-partitioned BigQuery table using insertId for idempotent writes. Route invalid me…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest 2–5 TB/day via Pub/Sub and Cloud Storage; Dataflow computes tenant-scoped features with event-time windows; write to BigQuery tenant datasets with IAM-based isolation; roll out Vertex AI Featur",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical streaming ingestion fundamentals on GCP, including Pub/Sub integration with Dataflow, BigQuery loading strategies, and rollback procedures. It evaluates error handling capabilities through dead-letter queues and data archiving, along with basic testing approaches.\n\n## Key Concepts\n\n- Streaming ingestion architecture with Pub/Sub and Dataflow\n- Data quality validation using schema rules\n- Idempotent writes in BigQuery (insertId or MERGE operations)\n- Dead-letter queue patterns and Cloud Storage archival\n- Daily partition management and rollback strategies\n\n## Code Example\n\n```python\n# Pseudo Dataflow DoFn: validate and route good/bad records\nimport json, apache_beam as beam\n\nclass ValidateAndRoute(beam.DoFn):\n    def process(self, element):\n        try:\n            data = json.loads(element.decode('utf-8'))\n            # Validate required fields and types\n            if not all(field in data for field in ['id', 'timestamp', 'event_type']):\n                raise ValueError(\"Missing required fields\")\n            # Route to valid output\n            yield beam.TaggedOutput('valid', data)\n        except Exception as e:\n            # Route to dead-letter\n            yield beam.TaggedOutput('dead_letter', element)\n```",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-5",
        "questionId": "q-25857",
        "question": "On GCP, design a real-time compliance auditing pipeline for 5 TB/day of JSON events ingested via Pub/Sub, with tenant isolation, masking of PII using DLP before storage, and immutable audit logs via Cloud Logging and Data Catalog. Implement per-tenant retention policies, data contracts, and a 24h rollback window. Include testing and rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Dataflow streaming reads Pub/Sub JSON, partitions by tenant_id, and writes to a partitioned BigQuery table. Mask PII via DLP before insert; enable 24h rollback with BigQuery time travel and periodic s",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest Pub/Sub messages by tenant, dedupe with a tenant-scoped sequence, validate payloads against per-tenant schema contracts, and apply anomaly scoring (e.g., z-score) in streaming Dataflow. Persist",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest JSON events through Pub/Sub and process with Dataflow using per-tenant schemas from a centralized registry. Quarantine malformed records to Cloud Storage, write validated data to tenant-specific BigQuery datasets…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Dataflow (Python) parses Pub/Sub JSON, validates fields, masks PII, and writes to a daily-partitioned BigQuery table keyed by tenant_id with insertId dedupe. Malformed events go to a dead-letter Pub/S",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProbes ability to design real-time, compliant data pipelines with tenant isolation, privacy controls, and governance audits under SLAs.\n\n## Key Concepts\n- Streaming ingestion, partitioned tables, per-tenant governance\n- DLP masking, time travel rollback, schema evolution\n- Data Catalog lineage, Data Exchange access patterns, Authorized Views\n\n## Code Example\n```javascript\n// Pseudo mask function (illustrative)\nfunction mask(record){\n  const {pii, ...rest} = record;\n  return { ...rest, pii: hash(pii) };\n}\n```\n\n## Follow-up Questions\n- How would you test rollback consistency across tenants?\n- How would you handle schema changes without in-flight queries failing?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-6",
        "questionId": "q-1449",
        "question": "Design a streaming enrichment pipeline: Pub/Sub → Dataflow → BigQuery, with an inline governance layer enforcing per-record rules (required fields, value ranges, cross-field consistency) at ingestion. Records failing rules go to a quarantine Cloud Storage bucket; good records load to a partitioned BigQuery table. Describe data model, rule engine approach, idempotence, backpressure, and testing/monitoring plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a DoFn in Dataflow that validates each event against a schema (required fields, ranges, cross-field constraints). Use tagged outputs: main for valid records, quarantine for invalid. Write ma",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a Pub/Sub -> Dataflow streaming pipeline to BigQuery, routing 95% of events to a Main table and 5% to an Experiment table using a tenant_id hash for deterministic routing. Use separate datasets per tenant for iso…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Pub/Sub for ingestion, Dataflow streaming to compute per-tenant anomaly scores, and write top-k events to a per-tenant BigQuery dataset via Authorized Views on a shared RAW table. Enable CMEK on d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Architecture Decision: 60s Micro-Batching** For a 50 GB/day clickstream workload in a single region, choose **60s micro-batching** over pure streaming: | Factor | Real-Time Streaming | 60s Micro-Batching | |--------|-…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to embed governance in streaming, handling bad data without blocking analytics, and operationalize quarantine and observability.\n\n## Key Concepts\n\n- Ingestion-time data quality checks\n- Dataflow ParDo with side outputs for quarantines\n- Idempotent BigQuery writes and record versioning\n- Backpressure and dead-letter handling\n- Observability and testing strategy\n\n## Code Example\n\n```python\nimport apache_beam as beam\n\nclass Govern(beam.DoFn):\n    def process(self, e):\n        if not e.get('user_id') or e.get('age', 0) < 0:\n            yield beam.pvalue.TaggedOutput('quarantine', e)\n        else:\n            yield e\n\nwith beam.Pipeline(...) as p:\n    raw = p | 'Read' >> beam.io.ReadFromPubSub(topic=...)  \n    main, bad = (raw | 'Govern' >> beam.ParDo(Govern()).with_outputs('quarantine', main='main'))\n    main | 'BQ' >> beam.io.WriteToBigQuery(table=..., schema=...)\n    bad | 'Quarantine' >> beam.io.WriteToText('gs://bucket/quarantine/')\n```\n\n## Follow-up Questions\n\n- How would you handle schema evolution in the governance layer without breaking downstream?\n- How to quantify and alert on data quality drift over time?\n- What tests would you add for end-to-end validation and rollback scenarios?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-7",
        "questionId": "q-29440",
        "question": "On GCP, design a real-time telemetry analytics pipeline for a multi-tenant fleet of IoT devices delivering 10-20k devices, 100-200 MB/min, with two regional residency. Ingest via Pub/Sub, compute streaming features in Dataflow, persist raw Parquet in Cloud Storage, expose per-tenant aggregates through BigLake external tables to BigQuery. Include per-tenant isolation, data contracts in Data Catalog, 24h per-tenant rollback, late-arriving data handling, testing/chaos plan, and latency targets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Dataflow streaming pipeline: Pub/Sub -> Dataflow -> BigQuery partitioned table for valid events; invalid events go to a DEAD-LETTER BigQuery table. Add a data-quality layer that validates eac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub for ingestion, with Dataflow streaming pipelines for per-tenant feature computation. Store raw Parquet files in Cloud Storage organized by tenant and date; expose per-tenant aggregates through BigLake extern…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Proposed approach: Dataflow Python beam pipeline reads Pub/Sub JSON events, validates required fields, masks PII (SSN, email), and upserts into per-tenant partitioned BigQuery tables using event_id fo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Dataflow pipeline that tags each event with tenant_id, writes to a partitioned RAW BigQuery table, and emits per-tenant metrics for latency, in-flight window, and failures to Cloud Monitoring",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis design question evaluates your ability to architect real-time, multi-tenant streaming solutions on GCP. It tests understanding of data contracts, isolation guarantees, operational resilience, and latency optimization under production-scale workloads.\n\n## Key Concepts\n\n- **Streaming ingestion**: Pub/Sub for durable, ordered message delivery at scale\n- **Stream processing**: Dataflow for windowed aggregations, late-data handling, and feature computation\n- **Multi-tenant isolation**: IAM-based access controls, separate datasets/tables per tenant, and service account segregation\n- **BigLake external tables**: Query Parquet in Cloud Storage directly from BigQuery without data movement\n- **Data contracts**: Schema enforcement and lineage tracking via Data Catalog\n- **Rollback and backfill**: Time-travel queries, versioned storage, and replay pipelines\n- **Chaos testing**: Inject failures to validate resilience and SLA compliance\n\n## Code Example\n\n```python\n# Dataflow streaming pipeline skeleton\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\noptions = PipelineOptions()\nwith beam.Pipeline(options=options) as p:\n    (\n        p\n        | 'IngestFromPubSub'\n        >> beam.io.ReadFromPubSub(subscription='projects/my-project/subscriptions/iot-telemetry')\n        | 'ParseMessages'\n        >> beam.Map(lambda msg: parse_telemetry_event(msg, schema))\n        | 'AddTenantWindow'\n        >> beam.WindowInto(\n            windowfn=beam.window.SlidingWindows(60, 10),\n            trigger=beam.trigger.AfterWatermark(early=beam.trigger.Repeat(beam.trigger.AfterCount(100))),\n            accumulation_mode=beam.trigger.AccumulationMode.DISCARDING,\n        )\n        | 'PerTenantAggregation'\n        >> beam.GroupByKey()\n        | 'WriteAggregates'\n        >> beam.io.WriteToBigQuery(\n            table='project:dataset.tenant_aggregates',\n            schema='device_id:STRING, tenant_id:STRING, metric:STRING, value:FLOAT64, window_start:TIMESTAMP',\n            create_disposition=beam.io.BigQueryDisposition.CREATE_NEVER,\n            write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND,\n        )\n    )\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-8",
        "questionId": "q-1906",
        "question": "Ingest a daily batch of JSON records stored in Cloud Storage into BigQuery via a Dataflow (Beam) job. Build a beginner-friendly pipeline that validates a minimal schema (tenant_id, event_id, event_ts), filters out records missing required fields, and deduplicates by (tenant_id, event_id). Load valid data into a partitioned BigQuery table; write invalid records to a separate GCS errors bucket. Include basic health counters and a simple template?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: Dataflow ingest with exactly-once; tenant quality gates (null rate, drift) as streaming metrics; snapshots in Cloud Storage for 24h rollback via replay; Data Catalog for contracts and lineag",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Batch Dataflow (Beam) reads daily JSON lines from Cloud Storage, validates required fields (tenant_id, event_id, event_ts), filters invalid records, and deduplicates by (tenant_id, event_id). Valid ro",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a streaming Dataflow pipeline to ingest Pub/Sub events, consult Vertex AI Feature Store for per-tenant anomaly scores, and write results to per-tenant BigQuery tables. Enforce per-tenant access control via Authorize…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: ingest 2 TB/day via Pub/Sub, route per-tenant data to region-bound BigQuery datasets; use Dataflow for streaming transforms, Vertex AI for real-time enrichment, and Data Catalog for lineage.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n- Tests ability to implement a reliable, beginner-friendly batch ingestion with data quality checks, idempotent deduplication, and observable health signals in GCP.\n\n## Key Concepts\n- Dataflow/Beam batch pipelines, JSON parsing, and error routing\n- Simple schema validation and field presence checks\n- Deduplication strategy for (tenant_id, event_id)\n- BigQuery partitioning and error data landfill\n- Basic monitoring with counters\n\n## Code Example\n```javascript\n// Java-like pseudocode for Beam; real syntax depends on the SDK (Python/Java)\nclass ValidateAndKey(DoFn):\n  def process(self, elem):\n    required = ['tenant_id','event_id','event_ts']\n    if all(k in elem and elem[k] is not None for k in required):\n      yield ((elem['tenant_id'], elem['event_id']), elem)\n    else:\n      yield ('invalid', elem)\n```\n\n## Follow-up Questions\n- How would you scale this to 10x data and ensure low tail latency?\n- How would you adapt for evolving event schemas and backward compatibility?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-9",
        "questionId": "q-3251",
        "question": "You operate a multi-tenant analytics service on GCP with 50 tenants. 5 GB/day of JSON events arrive via Pub/Sub. Design a beginner-friendly pipeline that ingests events with Dataflow, stores them in a single BigQuery table partitioned by ingestion_date, and includes tenant_id. Enforce per-tenant access using BigQuery authorized views (or per-tenant filters), support additive schema evolution, deduplicate by event_id, handle late data up to 1 hour, and capture end-to-end lineage in Data Catalog. Provide architecture sketch and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Dataflow-based streaming pipeline: Pub/Sub → Dataflow, where each event undergoes real-time validation against a centralized schema registry. When schema drift is detected or anomaly scores exceed configured th…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest Pub/Sub JSON with Dataflow into a single BigQuery table partitioned by ingestion_date, including tenant_id. Enforce per-tenant access via authorized views filtering on tenant_id. Deduplicate by",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Pub/Sub -> Dataflow with per-tenant partitioning; regional Dataflow workers bound to tenant region; ingestion-time masking driven by per-tenant policies stored in Data Catalog (masking fields, redacti",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a schema registry in Data Catalog to serve as the single source of truth for all tenant schema definitions. Dataflow queries Data Catalog to apply tenant-specific schema validators, enforcing required fields,…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a practical, beginner-friendly pipeline with tenant isolation, governance, and lineage on GCP.\n\n## Key Concepts\n\n- Dataflow streaming ingestion from Pub/Sub\n- BigQuery single table with partitioning by ingestion_date and tenant_id\n- Authorized views for per-tenant access control\n- Additive schema evolution with nullable fields\n- Deduplication by event_id\n- Late data handling (1 hour)\n- Data Catalog-based lineage\n\n## Code Example\n\n```javascript\n// Pseudo Dataflow-style transform (illustrative)\nfunction transform(eventJson){\n  const obj = JSON.parse(eventJson);\n  obj.ingestion_date = (obj.event_time ? obj.event_time : new Date()).toISOString().slice(0,10);\n  return obj;\n}\n```\n\n## Follow-up Questions\n\n- How would you test tenant isolation and access controls?\n- What are trade-offs between a single table vs per-tenant tables?\n- How would you roll out additive schema changes with minimal downtime?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-10",
        "questionId": "q-20944",
        "question": "On GCP, design a privacy-preserving ML feature-sharing platform that enables cross-tenant training: ingest 10 TB/day of telemetry via Pub/Sub, materialize per-tenant features in a shared feature store with masking, expose only derived features to partners, enforce data contracts in Data Catalog, maintain end-to-end lineage from source to Vertex AI experiments, and ensure reproducibility with experiments, while meeting cost and security constraints across projects and VPCs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant feature store backed by BigQuery views with exchangeable derived features. Apply masking and tokenization at ingestion to protect raw data. Share only vetted, derived features with partners via Data…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a streaming path: Pub/Sub -> Dataflow -> BigQuery. Implement per-message trace_id; emit it to Cloud Logging at ingest, Dataflow steps log progress with metrics, and store a lineage entry in Da",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Pub/Sub Schema Registry with per-tenant Avro schemas to enforce data contracts at ingestion. Dataflow validates and enriches events in streaming mode, then writes Parquet shards to Cloud Storage. Expose tenant datas…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a multi-tenant streaming pipeline on GCP that ingests 200 TB/day of telemetry data from Pub/Sub, processes it through Dataflow with per-tenant isolation and privacy masking, writes Parquet files to Cloud Storage…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates end-to-end ML feature sharing and governance across tenants on GCP, emphasizing privacy, lineage, and reproducibility rather than data ingestion alone.\n\n## Key Concepts\n- Vertex AI Experiments and Feature Store integration\n- Data Catalog, Data Exchange, and data contract enforcement\n- Per-tenant isolation with masking and tokenization\n- IAM Conditions and VPC Service Controls\n- End-to-end lineage, drift monitoring, and cost governance\n\n## Code Example\n```yaml\n# IAM policy snippet for partner access\nbindings:\n- role: roles/aiplatform.user\n  members: [\"group:partners@example.com\"]\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-11",
        "questionId": "q-8162",
        "question": "On GCP, design a cost-aware, per-tenant analytics pipeline for 50 tenants ingesting 200 GB/day JSON events via Pub/Sub, with per-tenant quotas and cost attribution in Cloud Billing, and dynamic retention per tenant. Use Dataflow for streaming, BigQuery per-tenant data products with Authorized Views, Data Catalog tagging, and budget alarms in Cloud Monitoring. Include scaling and rollback strategies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Beam pipeline in Python: read Pub/Sub JSON, flatten nested fields, enrich with a Cloud Bigtable user-tier lookup, validate required fields, and upsert to a daily-partitioned BigQuery table. U",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Pub/Sub Schema Registry for drift alerts, Dataflow DoFns for validation and normalization, and tenant-partitioned BigQuery tables. Use Data Catalog for per-tenant data contracts and full line",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant data product: each tenant uses its own BigQuery dataset with Authorized Views; a single Dataflow streaming job writes to tenant tables; resources are tagged in Data Catalog; Cloud Billing e",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a Dataflow (Apache Beam) pipeline that reads Pub/Sub JSON messages, parses them with a DoFn that emits metrics for ingested, processed, and failed events, then writes to BigQuery via streaming inserts with ins…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes cost governance, per-tenant isolation, policy enforcement, and rollback controls in a real GCP data platform.\n\n## Key Concepts\n\n- Cloud Billing cost attribution with resource labels and Data Catalog tagging\n- BigQuery per-tenant datasets and Authorized Views for isolation\n- Dataflow autoscaling and per-tenant quotas\n- Data retention policies and 24h rollback via time travel\n- Monitoring alerts and rollback testing\n\n## Code Example\n\n```sql\n-- Pseudo BigQuery per-tenant dataset and view (illustrative)\nCREATE SCHEMA tenant_123_dataset;\nCREATE TABLE tenant_123_dataset.events AS\nSELECT * FROM raw_events WHERE tenant_id = 123;\n```\n\n## Follow-up Questions\n\n- How would you validate budget alert thresholds at scale?\n- How would you test rollback under production load and ensure data integrity?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-12",
        "questionId": "q-8892",
        "question": "On GCP, ingest 10 GB/day of JSON telemetry via Pub/Sub. Design a beginner-friendly Dataflow (Beam) pipeline that parses and validates required fields, enriches with device metadata from Firestore, writes to a daily-partitioned BigQuery table using MERGE for idempotence, routes malformed messages to a dead-letter Pub/Sub, and archives originals to Cloud Storage. Propose a 24-hour rollback plan via daily snapshots and a simple test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Pub/Sub Schema Registry for drift alerts, Dataflow DoFns for validation and normalization, and tenant-partitioned BigQuery tables. Use Data Catalog for per-tenant data contracts and full line",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Dataflow Beam pipeline that reads Pub/Sub JSON telemetry, validates required fields, enriches with Firestore device metadata, writes to a daily-partitioned BigQuery table using MERGE for idempotency, routes…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Dataflow (Beam) to read from Pub/Sub, apply a DoFn to enforce value ranges for fields like temperature, gate invalid rows to Cloud Storage (quarantine bucket), and emit valid rows to a partitioned BigQuery table by…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Onboard 2–4 external partners ingesting 2–3 TB/day via Pub/Sub. Create per-tenant contracts in Data Catalog, stream through Dataflow into Cloud Storage Parquet files partitioned by tenant/date. Expose",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical Dataflow ingestion patterns, field validation, data enrichment, and reliable write operations for beginners working with GCP data pipelines.\n\n## Key Concepts\n- Dataflow (Apache Beam) pipeline fundamentals\n- Pub/Sub message ingestion and processing\n- BigQuery partitioned tables and MERGE operations for idempotency\n- Firestore lookups for data enrichment\n- Dead-letter queue patterns and archival strategies\n- Rollback mechanisms via daily snapshots\n- Basic data quality validation and error handling\n\n## Code Example\n```javascript\n// Pseudo-code Beam pipeline skeleton:\n// Read Pub/Sub -> Parse/Validate -> Enrich with Firestore -> \n// Write to BigQuery (MERGE) -> DLQ/Archive\n```",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-13",
        "questionId": "q-27838",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. NEW ANGLE: compute per-tenant event_volume by counting events in a 5-minute sliding window with 20 minutes allowed lateness, and write results to a separate BigQuery analytics table; outline how to implement windowing and triggers in Dataflow, how to schema-match to BigQuery, testing with synthetic late events, and monitor watermark lag and late-data rate?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Pub/Sub -> Dataflow -> BigQuery in two regions; apply per-tenant masking for PII with DLP before load; tag lineage in Data Catalog for each field and table, and surface per-tenant access vi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a real-time anomaly-detection pipeline on GCP for 1M IoT devices emitting ~100 MB/s via Pub/Sub. Use Dataflow for streaming, Vertex AI Feature Store Online for per-tenant scoring, and BigQue",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy a streaming Dataflow job: read from Pub/Sub as JSON, extract tenant_id and event timestamp, assign event-time watermark based on ts, apply SlidingWindows.of(Duration.standardMinutes(5)).withAllowedLateness(Durati…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Set up a data-product ecosystem using Pub/Sub -> Dataflow -> Parquet in GCS; register schemas in Data Catalog with policy tags; auto-apply DLP masking on PII; export per-tenant external tables via Big",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of streaming windowing, late data handling, and integration between Pub/Sub, Dataflow, and BigQuery. It emphasizes concrete implementation choices over theoretical knowledge.\n\n## Key Concepts\n- **Dataflow streaming windowing**: SlidingWindows with configurable lateness tolerance\n- **Event-time processing**: Watermark-based progress tracking and late data handling\n- **BigQuery schema design**: Partitioned and clustered tables for analytics workloads\n- **Observability**: Monitoring watermark lag and late-data rates to ensure SLAs\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.transforms.window import SlidingWindows, Sessions\nfrom apache_beam.options.pipeline_options import StandardOptions\nfrom apache_beam.io.gcp.bigquery import WriteToBigQuery\n\nclass ParseEvent(beam.DoFn):\n    def process(self, element):\n        import json\n        record = json.loads(element.decode('utf-8'))\n        yield beam.window.TimestampedValue({\n            'tenant_id': record['tenant_id'],\n            'event_time': record['ts']\n        }, record['ts'])\n\nclass CountPerTenant(beam.DoFn):\n    def process(self, element, window=beam.DoFn.WindowParam):\n        tenant_id, events = element\n        yield {\n            'tenant_id': tenant_id,\n            'window_start': window.start.to_utc_datetime().isoformat(),\n            'window_end': window.end.to_utc_datetime().isoformat(),\n            'event_volume': sum(1 for _ in events)\n        }\n\npipeline_options = PipelineOptions(['--streaming'])\noptions = pipeline_options.view_as(StandardOptions)\n\nwith beam.Pipeline(options=options) as p:\n    (p\n        | 'ReadFromPubSub' >> beam.io.ReadFromPubSub(topic='projects/my-project/topics/clickstream')\n        | 'ParseEvents' >> beam.ParDo(ParseEvent())\n        | 'ExtractTenant' >> beam.Map(lambda x: (x['tenant_id'], x))\n        | 'SlidingWindow' >> beam.WindowInto(\n            SlidingWindows(5 * 60, 1 * 60),\n            allowed_lateness=20 * 60,\n            accumulation_mode=ACCUMULATING\n        )\n        | 'GroupByTenant' >> beam.GroupByKey()\n        | 'CountEvents' >> beam.ParDo(CountPerTenant())\n        | 'WriteToBigQuery' >> WriteToBigQuery(\n            'my-project:analytics.tenant_event_volume',\n            schema='tenant_id:STRING, window_start:TIMESTAMP, window_end:TIMESTAMP, event_volume:INTEGER'\n        )\n    )\n```\n\n## Schema Matching to BigQuery\n- Partition the analytics table by `window_start` for efficient time-range queries\n- Cluster by `tenant_id` for fast per-tenant aggregations\n- Use INT64 for event_volume; avoid FLOAT to prevent rounding issues\n\n## Testing with Synthetic Late Events\n```python\ndef test_late_data_handling():\n    import unittest\n    from apache_beam.testing.test_pipeline import TestPipeline\n    from apache_beam.testing.util import assert_that, equal_to\n    \n    events = [\n        {'tenant_id': 't1', 'ts': 0},      # on-time\n        {'tenant_id': 't1', 'ts': 100},    # on-time (within window)\n        {'tenant_id': 't1', 'ts': 1500},   # late (beyond 20-min window)\n    ]\n    \n    with TestPipeline() as p:\n        results = (\n            p\n            | beam.Create(events)\n            | 'AddTimestamp' >> beam.Map(\n                lambda x: beam.window.TimestampedValue(x, x['ts'])\n            )\n            | 'Window' >> beam.WindowInto(\n                SlidingWindows(5 * 60, 1 * 60),\n                allowed_lateness=20 * 60,\n                trigger=AfterWatermark(early=AfterCount(1)),\n                accumulation_mode=DISCARDING\n            )\n            | 'Count' >> beam.combiners.Count.Globally()\n        )\n        assert_that(results, equal_to([2]))  # Late event should be dropped\n```\n\n## Monitoring Watermark Lag and Late-Data Rate\n- **Cloud Monitoring metrics**: Track `dataflow.googleapis.com/job/streamgen/watermark_delay` per worker\n- **Late-data metrics**: Monitor `dataflow.googleapis.com/job/streamgen/elements_late`; aim for <5% late ratio\n- **Custom metrics**: Use `DataflowIO.write_statistics` to emit per-tenant late counts via `beam.Metrics.counter`\n- **Alerting**: Set alerts on watermark lag >15 minutes or late-data rate >10%\n\n## Follow-up Questions\n- How would you adjust the pipeline to handle variable event volumes per tenant using session windows instead of fixed sliding windows?\n- What trade-offs would you consider when choosing between accumulated and discarded window modes?\n- How would you implement exactly-once delivery semantics for the BigQuery writes?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-14",
        "questionId": "q-6201",
        "question": "Design a beginner-friendly GCP data pipeline to ingest 50 MB/day of JSON API logs via Pub/Sub into BigQuery. Use Dataflow (Beam) to parse and validate fields (timestamp, user_id, endpoint). Propose a simple schema-evolution approach (core schema in BigQuery plus an extras JSON field or per-day tables). Ensure idempotent writes (insertId/MERGE), route malformed messages to a dead-letter Pub/Sub topic and archive originals to Cloud Storage, and outline a 24-hour rollback and basic monitoring plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-tenant streaming data quality engine: tenants publish field-level rules (null rate, regex, value ranges) to a manifest. Dataflow consumes rules as side inputs, applies checks during Pub/Sub-to-BigQuery str…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a streaming Dataflow pipeline (Python) that reads Pub/Sub JSON logs, validates timestamp/user_id/endpoint, and writes to BigQuery with a fixed core schema; store extra fields in a JSON extras c",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a streaming Dataflow pipeline from Pub/Sub to a partitioned, clustered BigQuery table with schema registry in Data Catalog for forward/backward compatibility. Validate records, redact PII vi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement ingestion with Pub/Sub and a stateful Beam/Dataflow stage to deduplicate by event_id; write Parquet partitioned by tenant/date to Cloud Storage; expose per-tenant slices via BigLake external",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to implement a simple, reliable streaming ingest with validation, a practical approach to schema evolution, and basic operations like dead-lettering, archiving, rollback, and monitoring at beginner level.\n\n## Key Concepts\n\n- Pub/Sub as a source and dead-lettering for bad messages\n- Dataflow (Beam) for parsing/validation\n- BigQuery core schema + extras JSON field for evolution\n- Idempotent writes using insertId or MERGE\n- Rollback strategies using Pub/Sub replay or GCS archive\n- Basic monitoring and alerts for data quality\n\n## Code Example\n\n```javascript\n// Pseudo-beam style pseudocode\n// Read from Pub/Sub, parse JSON, validate required fields, emit rows to BigQuery\nclass ParseAndValidateDoFn extends DoFn {\n  processElement(ctx) {\n    const msg = JSON.parse(ctx.element);\n    if (!msg.timestamp || !msg.user_id || !msg.endpoint) return;\n    const row = {core_field1: msg.timestamp, core_field2: msg.user_id, core_field3: msg.endpoint, extras: JSON.stringify(msg.extras || {})};\n    yield row;\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test schema-evolution choices and measure their impact on query performance?\n- What would you monitor to detect data quality regressions in this pipeline?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-15",
        "questionId": "q-5984",
        "question": "Design a cross-region, cross-tenant analytics platform on GCP for 2 TB/day of JSON activity ingested via Pub/Sub. Implement per-tenant privacy with masking, share aggregated metrics with a partner via BigQuery Data Exchange, enforce data contracts in Data Catalog, and export end-to-end lineage and immutable audit logs to Cloud Storage. Include a 24h per-tenant rollback plan, testing strategy, and failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Pub/Sub for ingest, Dataflow for streaming feature computation, and per-tenant BigQuery partitions with CMEK for raw data. Use Vertex AI Feature Store for online features, feeding a real-time",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-region pipeline: Pub/Sub → Dataflow → regional BigQuery raw layer, with per-tenant masked analytics views. Leverage BigQuery Data Exchange to share aggregated metrics with partners, enforcing per-tenant contracts th…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Architect a tenant-scoped streaming pipeline: Pub/Sub -> Dataflow streaming with per-tenant transforms, write to partitioned BigQuery tables (tenant_id as partition). Mask PII in a dedicated DoFn and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Dataflow streaming job that reads Pub/Sub JSON messages, applies field-level PII masking in a custom DoFn, and writes to daily-partitioned BigQuery tables using MERGE statements for idempotent upserts. Maint…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates end-to-end design capabilities for cross-region, multi-tenant data sharing on GCP, emphasizing privacy controls, data lineage, and rollback mechanisms.\n\n## Key Concepts\n\n- Cross-region BigQuery governance\n- Data Exchange and partner sharing\n- Data masking and per-tenant isolation\n- Data lineage and testing frameworks\n- Rollback and resilience patterns\n\n## Code Example\n\n```javascript\n// Simple masking helper\nfunction mask(str) { return str ? '***' : null; }\n```\n\n## Follow-up Questions\n\n- How would you implement per-tenant rollbacks if a tenant's data exceeds the 24-hour time travel window?\n- What strategies would you use to optimize cross-region data transfer costs?\n- How would you handle schema evolution while maintaining data contracts?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-16",
        "questionId": "q-15415",
        "question": "On GCP, design a near real-time data quality and schema drift detection pipeline for 5+ tenants ingesting 2-10 TB/day of JSON events via Pub/Sub. Enforce per-tenant validators (schema, nulls, ranges) with a rules store in Data Catalog and BigQuery, quarantine bad records to a separate Pub/Sub topic, surface alerts, and allow tenant-specific backfills/rollbacks. Describe data model, tooling (Dataflow templates), testing, and rollback strategies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route Pub/Sub streams through Dataflow streaming templates that shard by tenant and write to regional BigQuery datasets across two regions. Implement ingestion-time quality checks including schema drift validation, rang…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Dataflow to normalize and schema-evolve events, write per-tenant Parquet prefixes to Cloud Storage, and expose per-tenant aggregates via BigQuery tables with Row-Level Security. Use Data Cata",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Pub/Sub Schemas and a Cloud Spanner-backed schema registry; each event has a schema_version. Dataflow maps to a canonical row and writes to a tenant-partitioned BigQuery table. Enforce compat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I'd implement a per-tenant data quality framework: a Data Catalog-tagged schema registry, Dataflow validation templates that enforce tenant schemas and quality rules, with invalid records redirected to a quarantine Pub/…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nExplores practical data quality at scale on GCP, focusing on schema drift detection, per-tenant isolation, and controlled backfills—eight key concerns in production data pipelines.\n\n## Key Concepts\n\n- Per-tenant schema governance and data quality rules stored in Data Catalog tags\n- Real-time validation using Dataflow streaming templates\n- Drift detection with field-level schema comparison\n- Quarantine processing for invalid records\n- Observability, alerting via Cloud Monitoring, and backfill/rollback mechanisms using BigQuery time-travel and Cloud Storage\n\n## Code Example\n\n```python\nfrom google.cloud import bigquery\nfrom google.cloud import datacatalog\n\nclass TenantValidator:\n    def __init__(self, tenant_id: str, schema_registry: datacatalog.DataCatalogClient):\n        self.tenant_id = tenant_id\n        self.schema_registry = schema_registry\n        self.client = bigquery.Client()\n        \n    def load_schema(self) -> dict:\n        scope = datacatalog.SearchCatalogRequest scope=datacatalog.EntryScope.GCP_RESOURCES)\n        entry = self.schema_registry.search_catalog(\n            request=datacatalog.SearchCatalogRequest(\n                query=f\"schema:{self.tenant_id}\",\n                scope=scope\n            )\n        )[0]\n        return self._parse_schema(entry.linked_resources[0])\n    \n    def validate(self, record: dict) -> tuple[bool, list[str]]:\n        schema = self.load_schema()\n        errors = []\n        for field, rules in schema.items():\n            value = record.get(field)\n            if rules.get('required') and value is None:\n                errors.append(f\"Missing required field: {field}\")\n            if value is not None and rules.get('type') == 'number':\n                if not isinstance(value, (int, float)):\n                    errors.append(f\"Invalid type for {field}\")\n            if 'min' in rules and value < rules['min']:\n                errors.append(f\"Value {value} below minimum {rules['min']}\")\n            if 'max' in rules and value > rules['max']:\n                errors.append(f\"Value {value} above maximum {rules['max']}\")\n        return len(errors) == 0, errors\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-17",
        "questionId": "q-29538",
        "question": "On GCP, design a two-region IoT telemetry data exchange (2-3 TB/day) that ingests via Pub/Sub, stores Parquet in Cloud Storage, and surfaces per-tenant analytics through BigQuery with a cross-tenant data catalog and DLP-based masking policies. Focus on privacy-by-design: per-tenant data contracts, dynamic masking rules, two-region residency, 24h per-tenant rollback, backfill, and chaos testing for contracts and masking failures. Provide concrete components and data models?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest 2-3 TB/day via Pub/Sub into two regions, write Parquet to Cloud Storage partitioned by tenant/date, and expose per-tenant analytics through BigQuery via BigLake external tables. Enforce per-ten",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage Pub/Sub for ingest, Dataflow for streaming feature computation, and per-tenant BigQuery partitions with CMEK for raw data. Use Vertex AI Feature Store for online features, feeding a real-time",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Dataflow in streaming mode with event-time windows, validate each event against a per-tenant schema catalog entry, normalize fields, and upsert into a partitioned BigQuery table using MERGE with a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest with Pub/Sub and Dataflow, deploy two regional pipelines feeding per-tenant BigQuery datasets. Use DLP + IAM for field-level masking, and BigQuery auto schema updates. Implement a tenant-scoped",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests privacy-by-design in data pipelines, with per-tenant contracts, masking via DLP, and robust rollback/backfill across regions.\n\n## Key Concepts\n- Privacy by design with DLP masking per tenant\n- BigLake external tables and Data Catalog contractual metadata\n- Two-region residency and cross-region replication\n- 24h per-tenant rollback, backfill strategies, and chaos testing for contract violations and masking failures\n\n## Code Example\n```javascript\n// Pseudocode: apply tenant-aware masking before write\nfunction maskForTenant(record, tenantId){\n  if(isSensitiveTenant(tenantId)){\n    record.pii = mask(record.pii)\n  }\n  return record\n}\n```\n\n## Follow-up Questions\n- How would you validate contracts under late-arriving data?\n- What are cost/latency trade-offs of two-region BigLake-external tables versus a single-region model?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-18",
        "questionId": "q-15216",
        "question": "On GCP, design a beginner-friendly streaming pipeline to ingest 1-2 TB/month of JSON user events via Pub/Sub into BigQuery with per-tenant datasets, masking PII, and a lightweight Data Catalog-based data contract; include a quarantine path for invalid events and a rollback plan that reprocesses the last 24 hours from quarantine back into tenant tables?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate each message against a fixed schema in a Dataflow custom DoFn. Valid records are written to the primary BigQuery table, while invalid records are routed to both a dedicated errors BigQuery table and Cloud Stora…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub → Dataflow streaming to parse JSON, apply ParDo masking (emails, phones), route valid events to per-tenant BigQuery datasets, and invalid to a quarantine table. Store contract in Data Cata",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Provide a concrete design: Pub/Sub schema enforcement, Dataflow DoFn for tenant routing, per-tenant datasets or views with access controls, Data Catalog lineage, CMEK on datasets, Vertex AI scoring, 2",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed pipeline: a streaming Dataflow job reads Pub/Sub JSON events, validates a minimal schema, and writes to per-tenant partitioned BigQuery tables. Data older than 30 days is archived automatical",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nUnderstanding end-to-end streaming ingestion, privacy handling, and basic rollback in GCP.\n\n## Key Concepts\n- Pub/Sub streaming\n- Dataflow DoFn masking\n- BigQuery per-tenant datasets\n- Data Catalog data contracts and lineage\n- Quarantine path and 24h rollback\n\n## Code Example\n```javascript\n// Pseudo Dataflow DoFn: mask PII before write\nclass MaskPIIFn {\n  process(element) {\n    // element is a JSON object\n    if (element.email) element.email = maskEmail(element.email);\n    if (element.phone) element.phone = maskPhone(element.phone);\n    return element;\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test this pipeline for schema drift and masking correctness?\n- How would you alert on quarantine volume spikes and auto-remediate?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-19",
        "questionId": "q-18408",
        "question": "On GCP, design a cross-tenant streaming analytics pipeline for 200 GB/day of JSON events published to Pub/Sub, enforcing per-tenant residency (US/EU), privacy masking via a centralized policy engine, and per-tenant aggregates in BigQuery. Include Data Catalog lineage, 24h per-tenant rollback, schema evolution, cross-region partner data exchange, and observability with retries and alerting. What is your concrete design?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Dataflow streaming pipeline reading Pub/Sub JSON, validate against a fixed schema, apply a lookup to enrich fields, and upsert into a daily-partitioned BigQuery table using insertId for deduplic",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a streaming pipeline (Pub/Sub -> Dataflow -> BigQuery) with per-tenant namespaces and dynamic schema evolution. Enforce data quality gates in Dataflow, track lineage in Data Catalog, and publis",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Dataflow streaming reads Pub/Sub JSON, partitions by tenant_id, and writes to a partitioned BigQuery table. Mask PII via DLP before insert; enable 24h rollback with BigQuery time travel and periodic s",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two Dataflow pipelines: an ingress path that reads Pub/Sub, applies tenant-specific masking from a centralized policy engine, routes data to per-tenant BigQuery partitions in US/EU, and a governance versioned pipeline t…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests your ability to design a multi-tenant streaming analytics pipeline that addresses several complex requirements: cross-region data residency enforcement, privacy masking, tenant isolation, lineage tracking, rollback capabilities, schema evolution, partner data sharing, and operational observability—all working together in a unified system.\n\n## Key Concepts\n- **Pub/Sub streaming** with Dataflow for real-time processing\n- **Per-tenant BigQuery** partitions with US/EU residency enforcement\n- **Centralized policy engine** for consistent masking across all tenants\n- **Data Catalog lineage** for governance and auditability\n- **BigQuery Data Exchange** for secure cross-region partner sharing\n- **24-hour rollback** via replay from Pub/Sub snapshots\n- **Schema evolution** with managed schema registry\n- **Observability** with retries, dead-letter queues, and alerting\n\n## Code Example\n```javascript\n// Pseudocode: policy-based masking\nfunction mask(record, policy) {\n  const r = {...record};\n  for (const field of policy.fields) {\n    if (field.mask) r[field.name] = maskValue(r[field.name], field.maskType);\n  }\n  return r;\n}\n\nfunction maskValue(value, type) {\n  switch (type) {\n    case 'hash': return crypto.createHash('sha256').update(value).digest('hex');\n    case 'redact': return '[REDACTED]';\n    case 'partial': return value.slice(0, 2) + '***';\n    default: return value;\n  }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-20",
        "questionId": "q-2364",
        "question": "Design a cross-tenant data exchange on GCP: ingest 1 TB/day of JSON telemetry from Pub/Sub into two BigQuery regions. Implement per-tenant isolation via dataset-level access controls and masked views, and use Data Catalog policy tags to enforce visibility. Model a pragmatic two-layer architecture: raw + curated; ensure end-to-end lineage; support controlled data sharing via explicit contracts. Provide a 24h per-tenant rollback plan and testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a data mesh architecture with domain-owned BigQuery datasets for Sales, Product, and Operations. Each domain manages its own data products using standardized schemas, while Data Catalog provides centralized me…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a real-time multi-tenant streaming pipeline on GCP: Pub/Sub -> per-tenant Dataflow pipelines -> BigQuery per-tenant tables. Enforce per-tenant quality gates (schema conformance, range checks",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route 1 TB/day via Dataflow to BigQuery in Region A and Region B. Implement per-tenant isolation with dataset-level access and masked views; enforce visibility with Data Catalog policy tags. Build raw",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest 5-10 GB/day via Pub/Sub. Dataflow parses JSON, normalizes timestamps, standardizes fields, and computes a per-tenant quality score (completeness, validity, consistency). Write normalized events",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nExplores data-contract-driven governance and cross-region sharing, a real-world need for large, multi-tenant analytics.\n\n## Key Concepts\n- Data contracts in Data Catalog\n- Per-tenant isolation via views/policies\n- End-to-end lineage\n- Two-region replication and cost-aware design\n- Rollback strategy with time travel and canaries\n\n## Code Example\n```python\n# pseudo-contract validator\ndef validate_contract(contract):\n  required_fields = {\"tenant_id\",\"schema\"}\n  return required_fields.issubset(contract.keys())\n```\n\n## Follow-up Questions\n- How would you test contract violations in CI/CD?\n- How would you handle schema evolution across tenants without breaking sharing contracts?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-21",
        "questionId": "q-14615",
        "question": "On GCP, design a multi-tenant CDC and schema-evolution pipeline that ingests 1-2 TB/day of JSON/Parquet events from Pub/Sub into per-tenant BigQuery datasets. Implement per-tenant schema drift detection, auto-generate backward-compatible migrations, and apply per-tenant schema changes using Dataflow templates. Store pre-migration snapshots for 24h rollback. Enforce contracts in Data Catalog, mask PII with DLP, and publish anonymized changes to partners via Data Exchange. Include 24h per-tenant rollback and testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route 1 TB/day via Dataflow to BigQuery in Region A and Region B. Implement per-tenant isolation with dataset-level access and masked views; enforce visibility with Data Catalog policy tags. Build raw",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Dataflow to read Pub/Sub JSON events, group by tenant, compute a 7-day rolling average, and write to a partitioned BigQuery table. Create per-tenant Authorized Views to restrict access. Rollback b",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a single Dataflow pipeline with per-tenant keys and stateful deduplication on (tenant_id, event_id). Ingest 5 TB/day via Pub/Sub, store raw events in Cloud Storage, and write per-tenant aggregates",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant Change Data Capture (CDC) pipeline using Pub/Sub to ingest JSON and Parquet events into tenant-specific BigQuery datasets. Deploy Apache Beam-based Dataflow templates with integrated schema drift…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses the ability to design a scalable, tenant-isolated CDC and schema evolution pipeline with robust governance, security, and rollback requirements.\n\n## Key Concepts\n\n- Change Data Capture (CDC) at cloud scale\n- Per-tenant data isolation and architecture\n- Schema drift detection and automated validation\n- Backward-compatible migration strategies\n- Data Catalog contract enforcement\n- DLP-based PII masking and data protection\n- Data Exchange for secure partner publishing\n\n## Code Example\n\n```javascript\n// Pseudo-logic: drift detection\nfunction driftCheck(incomingSchema, currentSchema) {\n  const changes = schemaDiff(incomingSchema, currentSchema);\n  \n  if (isBackwardCompatible(changes)) {\n    generateMigration(changes);\n    createSnapshot();\n    applyMigration(changes);\n  } else {\n    alertSchemaViolation(changes);\n  }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-22",
        "questionId": "q-29832",
        "question": "On GCP, design a two-region data fabric that enforces per-tenant differential privacy budgets for 2-5 TB/day of telemetry ingested via Pub/Sub. Implement a streaming Dataflow pipeline that computes DP-protected aggregates, stores Parquet in Cloud Storage, and exposes per-tenant data products as BigLake external tables in BigQuery. Include budget tracking, 24h per-tenant rollback, tests, and alerting?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Beam (Python) streaming pipeline that reads Pub/Sub JSON, validates required fields, and routes a configurable fraction of tenants to an EXPERIMENTAL BigQuery table while the rest goes to MAI",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a two-region GCP data fabric enforcing per-tenant differential privacy budgets for 2-5 TB/day telemetry via Pub/Sub. Streaming Dataflow computes DP-protected aggregates, writes Parquet to Cloud",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Pub/Sub → Dataflow Flex Template with per-tenant routing to BigQuery tables. Ingest masks PII using DLP in a streaming DoFn; write to partitioned, tenant-scoped BigQuery tables to ensure isolation",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Dataflow streaming pipeline with Pub/Sub input, a dynamic per-tenant schema registry in Data Catalog, PII masking via Cloud DLP, field-level schema validation, and BigQuery streaming inserts for",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design privacy-aware, multi-region data fabrics with DP budgets, streaming processing, and cross-region governance.\n\n## Key Concepts\n- Differential privacy budgets per tenant\n- Streaming Dataflow with DP aggregation\n- BigLake external tables for per-tenant data products\n- Budget tracking, rollback window, testing and alerts\n\n## Code Example\n```python\n# Pseudo DP budget enforcement\ndef process_event_batch(batch, tenant_id, budgets, cost_model):\n    budget = budgets.get(tenant_id)\n    if budget.remaining < batch.privacy_cost:\n        raise Exception(\"DP budget exhausted\")\n    # compute DP-protected aggregates\n    aggregates = compute_dp_aggregates(batch)\n    budgets.update(tenant_id, -batch.privacy_cost)\n    return aggregates\n```\n\n## Follow-up Questions\n- How would you test budget exhaustion scenarios without affecting production data?\n- How would you simulate cross-region replication delays on DP budget accounting?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-23",
        "questionId": "q-931",
        "question": "A GCP pipeline ingests 1 TB of JSON user activity daily from Pub/Sub into BigQuery via Dataflow. New fields appear over time; you must evolve the schema without downtime. What approach would you use for schema drift and nested fields, and outline concrete steps to implement it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Parse Pub/Sub messages with a simple Beam DoFn that validates required fields (user_id, event_type) and coerces amount to float. Valid records go to BigQuery; invalid ones emit to a dead-letter Pub/Su",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "On GCP, design a real-time feature store for fraud detection: ingest 2k events/sec from Pub/Sub; compute features in Dataflow; publish online features to Vertex AI Feature Store (per-tenant isolation)",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a two-layer scheme: a stable BigQuery table with known fields and a separate extra_json column to capture drift. The Dataflow job maps incoming JSON to the known schema; unknown fields are stored",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Dataflow streaming to ingest JSON from Pub/Sub to BigQuery. Export metrics to Cloud Monitoring; build dashboards for ingestion latency, throughput, and backlog. Set SLOs: 95th percentile latency <",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical handling of evolving data in a streaming pipeline, balancing query stability with schema growth.\n\n## Key Concepts\n- Schema evolution in BigQuery\n- Semi-structured data (JSON)\n- Dataflow transforms for parsing\n- Backward compatibility\n- Metadata and cataloging\n\n## Code Example\n```javascript\nfunction parseEvent(json) {\n  const data = JSON.parse(json);\n  const known = {\n    userId: data.user_id || null,\n    eventTime: data.event_time || null,\n    country: data.country || null\n  };\n  const extraKeys = Object.keys(data).filter(k => !(k in known));\n  const extra = JSON.stringify(extraKeys.reduce((acc, k) => { acc[k] = data[k]; return acc; }, {}));\n  return { ...known, extra_json: extra };\n}\n```\n\n## Follow-up Questions\n- How would you migrate existing data to the new table version with minimal downtime?\n- How would you monitor for schema drift and alert when new fields appear?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-24",
        "questionId": "q-3795",
        "question": "Design a GCP streaming ML feature pipeline for five tenants: ingest 250 GB/day of JSON events from Pub/Sub into Vertex AI Feature Store (online for real-time scoring and offline for batch) with per-tenant isolation, data contracts in Data Catalog, and PII masking at ingest. Include schema evolution, drift monitoring, cross-region replication, and a 24h rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming CDC pipeline that captures changes from a multi-tenant PostgreSQL database using Debezium's Kafka Connect connector (reading PostgreSQL's logical decoding WAL via pgoutput), publishes change events to…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Parse JSON events from Pub/Sub, validate required fields, enrich via a side-input mapping, and write to a daily-partitioned BigQuery table using insertId for idempotence. Route invalid messages to a d",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Datastream to ingest PostgreSQL changes into Pub/Sub; a streaming Dataflow pipeline deduplicates and routes events to per-tenant BigQuery tables (partitioned by date and tenant_id); enforce per-te",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Five-tenant streaming ML feature pipeline: Pub/Sub → Dataflow → Vertex AI Feature Store (online for real-time scoring and offline for batch) with per-tenant isolation, data contracts in Data Catalog,",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end streaming ML pipelines on GCP with tenants separation, data contracts, and governance.\n\n## Key Concepts\n- Vertex AI Feature Store (online/offline)\n- Pub/Sub + Dataflow streaming\n- Data Catalog data contracts and schema evolution\n- Per-tenant isolation via IAM\n- Drift monitoring and rollback plans\n\n## Code Example\n```javascript\n// PII masking example at ingest\nfunction maskPII(record){ if(record.email){ record.email = hash(record.email); } return record; }\n```\n\n## Follow-up Questions\n- How would you validate schema evolution without breaking tenants?\n- How would you design drift alerts and rollbacks across regions?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-25T07:11:08.408Z",
    "version": 1
  }
]