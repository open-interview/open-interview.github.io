[
  {
    "id": "test-gcp-data-engineer",
    "channelId": "gcp-data-engineer",
    "channelName": "GCP Data Engineer",
    "title": "GCP Data Engineer Knowledge Test",
    "description": "Test your understanding of GCP Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "gcp-data-engineer-q-0",
        "questionId": "q-13974",
        "question": "On GCP, design an intermediate multi-tenant streaming analytics pipeline for a food-delivery platform (Apple/ DoorDash). Ingest 3 TB/day of JSON events via Pub/Sub, deduplicate by event_id, enrich with merchant and user lookups, and emit per-tenant aggregates to BigQuery with strict isolation. Implement ingestion-time data quality gates: schema validation, field masking via DLP, and an anomaly score to gate writes. Use Dataflow templates, Data Catalog tagging, and handle late data with event-time windows and watermarking; guarantee exactly-once writes, 24h per-tenant rollback, and backfill capability. Include data models, failure modes, testing strategy, and cost considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a single Dataflow streaming job that ingests 5 TB/day of Pub/Sub JSON events, routes by tenant to dedicated BigQuery tables, and enforces real-time data quality: (1) schema drift check again",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: Dataflow Python beam pipeline reads Pub/Sub JSON events, validates required fields, masks PII (SSN, email), and upserts into per-tenant partitioned BigQuery tables using event_id fo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Dataflow streaming to ingest JSON from Pub/Sub to BigQuery. Export metrics to Cloud Monitoring; build dashboards for ingestion latency, throughput, and backlog. Set SLOs: 95th percentile latency <",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Dataflow streaming job that ingests Pub/Sub JSON events, deduplicates by event_id, and enriches with merchant/user lookups from BigQuery. Write per-tenant aggregates to partitioned BigQuery tables with strict tena…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates your ability to design a production-ready multi-tenant streaming analytics pipeline with data quality controls, privacy compliance, and operational resilience—critical gaps not addressed by previous questions.\n\n## Key Concepts\n- Dataflow streaming architecture with deduplication and side inputs\n- Exactly-once processing via insert_id and Pub/Sub message ordering\n- Ingestion-time data quality gates: schema validation, DLP field masking, anomaly scoring\n- Per-tenant isolation using partitioned BigQuery tables with row-level security\n- Late data handling through event-time windows and watermarking strategies\n- 24-hour rollback capability via reprocessing with Data Catalog metadata tagging\n- Comprehensive testing: unit tests, synthetic data generation, chaos engineering\n\n## Code Example\n```python\n# DoFn skeleton for data quality validation\nclass QualityGateDoFn(beam.DoFn):\n    def process(self, element):\n        # Schema validation, DLP masking, anomaly scoring\n        yield validated_element\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-1",
        "questionId": "q-15239",
        "question": "On GCP, design an advanced, privacy-preserving streaming anomaly-detection pipeline for a multi-tenant payments platform. Ingest 8-12 TB/day of JSON events via Pub/Sub, mask PII in-flight, extract features, and score with Vertex AI per-tenant models. Store per-tenant results in BigQuery, maintain end-to-end lineage via Data Catalog, and enforce per-tenant data contracts. Include 24h rollback per tenant, drift/QA testing strategy, latency targets, and cost considerations, plus schema evolution and failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Dataflow streaming reads Pub/Sub, parses JSON, and upserts into per-tenant BigQuery tables using MERGE on event_id. A nightly batch loads backfilled events from GCS and upserts, deduplicating by event",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a GCP streaming path with per-tenant onboarding, using Pub/Sub, Dataflow for enrichment, BigQuery per-tenant datasets for analytics, and Memorystore for online features. Enforce per-tenant cont",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose per-tenant isolation with separate BigQuery datasets; Pub/Sub to Dataflow for masking and feature extraction; Vertex AI per-tenant models for online scoring; store results in BigQuery; tag con",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Pub/Sub Schema Registry for per-tenant event shapes, Dataflow to route to tenant-specific BigQuery datasets, and per-tenant contracts in Data Catalog. Rollback is tenant-scoped via snapshot export",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests end-to-end design for multi-tenant data pipelines with privacy, real-time scoring, and governance across a large data volume.\n\n## Key Concepts\n- Tenant isolation, Data Contracts, Data Catalog tagging\n- Streaming masking, feature engineering in Dataflow\n- Vertex AI online scoring, per-tenant models\n- End-to-end lineage, rollback, schema evolution\n- Latency, cost optimization, failure testing\n\n## Code Example\n```python\n# example: Dataflow DoFn pseudocode for masking\nclass MaskPII(DoFn):\n  def process(self, element):\n    import json\n    rec = json.loads(element)\n    rec['email'] = mask_email(rec['email'])\n    yield json.dumps(rec).encode('utf-8')\n```\n\n## Follow-up Questions\n- How would you implement per-tenant feature stores and versioned models?\n- How do you test rollback and schema drift in production-safe ways?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-2",
        "questionId": "q-22297",
        "question": "On GCP, design a real-time fintech analytics pipeline for 2,000 TPS across tenants. Ingest via Pub/Sub, process with Dataflow streaming with exactly-once semantics and tenant-id dedupe, handle late-arriving events with watermarking. Persist to Parquet in Cloud Storage, then incremental load to a BigQuery serving layer. Implement per-tenant access controls with row-level security, data contracts and lineage in Data Catalog, plus a 24h rollback, backfill, testing, and fault-injection plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming pipeline (Pub/Sub -> Dataflow -> BigQuery) with per-tenant namespaces and dynamic schema evolution. Enforce data quality gates in Dataflow, track lineage in Data Catalog, and publis",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Dataflow batch job (Python) to parse nested JSON, flatten to a flat schema, and write to a staging BigQuery table partitioned by ingestion_date. Use a MERGE into production table by a stable id. Maintain schema files in…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose exactly-once streaming in Dataflow with a tenant-keyed dedupe (tenant_id, txn_id) and watermarks to handle late arrivals. Use Pub/Sub arrival order with idempotent sink to BigQuery via Parquet",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a canonical BigQuery schema and versioned Dataflow mappings keyed by tenant_id and contract_version. Contract updates trigger routing to the new mapping and per-tenant Authorized Views via Data Ca",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes real-world streaming semantics, multi-tenant isolation, and data contracts in GCP with practical rollback.\n\n## Key Concepts\n\n- Dataflow exactly-once processing\n- Tenant-level dedupe by (tenant_id, txn_id)\n- Watermarks for late events\n- Parquet in Cloud Storage + incremental BigQuery loading\n- Data Catalog contracts and lineage\n- 24h rollback and backfill test plan\n\n## Code Example\n\n```javascript\n// Pseudo Dataflow-like dedupe (illustrative)\nclass DedupFn {\n  process(element) {\n    const key = element.tenant_id + '|' + element.txn_id;\n    if (!state.has(key)) {\n      state.add(key);\n      return element;\n    }\n    return null;\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you verify exactly-once behavior under backpressure and late data?\n- How would you roll back a fraudulent day’s data within 24h?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-3",
        "questionId": "q-10100",
        "question": "On GCP, design a two-region streaming analytics pipeline for 4 TB/day of JSON events via Pub/Sub where each tenant's data sits in a separate BigQuery dataset with strict IAM, enforces per-tenant data quality checks at ingest, produces per-tenant aggregates, and provides end-to-end lineage in Data Catalog; include a 24h per-tenant rollback, schema drift handling, and automated data contracts across tenants?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pub/Sub to feed Dataflow streaming jobs that validate JSON schemas, mask PII via DLP, and emit per-tenant records to separate BigQuery datasets in two regions. Enforce IAM on a per-dataset basis,",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a three-tier design: raw/curated/analytics with per-tenant schemas in BigQuery, regionally stored data in Cloud Storage, streamed via Dataflow. Enforce privacy with per-tenant masking, encryption/",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant contracts in Data Catalog; Dataflow streaming with DoFn checks for schema drift, nulls, and value ranges; quarantine bad records; strict per-tenant BigQuery; lineage in Data Catalog; 24h ro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage Dataflow (Beam) with a Python DoFn validating against an external Avro/JSON schema; route valid events to BigQuery staging and malformed ones to a quarantine table with error codes. Enforce per-field checks usi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-region tenancy, data quality, masking, and lineage with rollback guarantees.\n\n## Key Concepts\n- Pub/Sub ingestion\n- Dataflow streaming\n- Cloud DLP masking\n- BigQuery per-tenant datasets (regional)\n- Data Catalog lineage and contracts\n- Time-travel rollback and checkpoints\n- Schema drift testing\n\n## Code Example\n```python\nimport apache_beam as beam\nclass ValidateSchema(beam.DoFn):\n  def process(self, element):\n    # placeholder for schema validation\n    yield element\n```\n\n## Follow-up Questions\n- How would you test schema drift in production?\n- What failure modes require manual intervention and how to mitigate?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-4",
        "questionId": "q-26593",
        "question": "On GCP, design a 3 TB/day multi-tenant data ingestion and governance pipeline for JSON events from a retail platform. Ingest via Pub/Sub into Dataflow, store raw Parquet in Cloud Storage, expose per-tenant data products via BigLake external tables to BigQuery with per-tenant schemas registered in Data Catalog. Implement automated schema drift detection, field-level masking for PII, two-region residency, 24h per-tenant rollback with backfill, testing/chaos plan, and cost targets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest 2 TB/day via Pub/Sub; use Dataflow streaming with per-tenant routing; apply DLP masking at ingest; write to per-tenant BigQuery datasets with Authorized Views; maintain end-to-end lineage in Da",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage per-tenant datasets and partitions, using Pub/Sub → Dataflow → Parquet in Cloud Storage, BigLake external tables in BigQuery. Enforce per-tenant retention (7d/365d) via separate partitions an",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a cross-cloud, per-tenant analytics pipeline that ingests 5 TB/day of JSON events from Pub/Sub, processes them with Dataflow for real-time enrichment, and stores per-tenant aggregates using BigQuery Omni acros…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design uses Pub/Sub -> Dataflow for ingestion, writes raw Parquet to Cloud Storage, exposes per-tenant views via BigLake external tables in BigQuery, and registers per-tenant schemas in Data Catalog.",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical data governance in GCP: multi-tenant isolation, schema drift handling, and per-tenant rollback mechanisms, plus end-to-end lineage and cost visibility.\n\n## Key Concepts\n\n- Multi-tenant governance with per-tenant contracts in Data Catalog\n- Automated schema drift detection and versioned backfill\n- BigLake external tables and per-tenant isolation in BigQuery\n- Field-level masking for PII at ingest\n- 24h per-tenant rollback and chaos testing\n\n## Code Example\n\n```python\n# Pseudocode: drift-detection sketch in a Dataflow template\nclass DriftCheckDoFn(beam.DoFn):\n  def process(self, element, registry):\n    # validate element against registry schema\n    yield element\n```\n\n## Follow-up Questions\n\n- How would you test drift remediation performance for large backfills?\n- How would schema evolution be managed when tenants introduce new fields selectively?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-5",
        "questionId": "q-6319",
        "question": "Ingest multi-app telemetry via Pub/Sub into per-app BigQuery datasets: design a beginner-friendly GCP streaming pipeline using Dataflow that validates required fields, writes to daily-partitioned per-app tables, uses idempotent upserts, routes malformed messages to a dead-letter Pub/Sub topic, archives originals to Cloud Storage, enforces per-app quotas via slot reservations, and supports a 24h rollback by replaying archived data. Outline architecture, schemas, and testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a Dataflow streaming job that reads Pub/Sub, validates required fields (app, timestamp, event), applies per-app schemas, writes to daily-partitioned per-app BigQuery tables using MERGE/insertI",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest 2 TB/day per tenant from Pub/Sub into a per-tenant Dataflow streaming job that writes to separate BigQuery datasets; mask PII on ingest using the DLP API; publish per-tenant lineage to Data Cat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Pub/Sub → Dataflow streaming to per-tenant BigQuery partitions. Add a data-quality module that computes per-tenant scores (completeness, validity, dedup) and kicks off a 24h backfill job to remediate",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage per-tenant datasets and partitions, using Pub/Sub → Dataflow → Parquet in Cloud Storage, BigLake external tables in BigQuery. Enforce per-tenant retention (7d/365d) via separate partitions an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a practical, cost-aware multi-app ingest with isolation and rollback.\n\n## Key Concepts\n- Dataflow streaming from Pub/Sub\n- Per-app BigQuery schemas and datasets\n- Idempotent writes (MERGE/insertId)\n- Dead-letter Pub/Sub and archival\n- Per-app quotas and slot reservations\n- 24h rollback via replay from Cloud Storage\n\n## Code Example\n```javascript\n// Pseudo-Beam skeleton for Dataflow pipeline in JS-like syntax\nimport beamsdk from 'beam-js';\nconst p = new beamsdk.Pipeline();\n// ... validation, routing, and write steps\n```\n\n## Follow-up Questions\n- How would you test rollback without affecting other apps?\n- How would you monitor ingestion latency and quota usage?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-6",
        "questionId": "q-28727",
        "question": "On GCP, design a streaming pipeline ingesting 200 GB/day of JSON IoT events via Pub/Sub, computing per-device aggregates in BigQuery. NEW ANGLE: implement a policy-driven schema evolution and data-quality layer that enforces a contract, auto-evolves BigQuery schemas for new fields, and rolls back to the previous schema within 24h if quality drift thresholds are breached. Include synthetic-data tests and drift dashboards?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Dataflow (Beam) streaming job (Python) to parse Pub/Sub JSON click events, validate required fields, and normalize country codes to ISO 3166-1 alpha-2. Write to a daily-partitioned BigQuery t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Pub/Sub → Dataflow streaming: a DoFn validates events against a contract, routes valid data to per-device partitions in BigQuery. A policy engine auto-evolves schemas for new fields with versioning st",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a pipeline: Pub/Sub to Dataflow for validation, then per-tenant BigQuery tables; store raw Parquet in Cloud Storage; maintain Data Catalog lineage; expose per-tenant quality scores to dashboar",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a multi-tenant Dataflow pipeline architecture using Pub/Sub for ingestion and dedicated BigQuery datasets per tenant to ensure strict data isolation. Implement dead-letter topics for failed messages, configurable…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design governance-driven streaming pipelines with schema evolution, rollback, data quality, and observability. Emphasizes reliability, experimentation, and production-readiness.\n\n## Key Concepts\n- Policy-driven schema evolution\n- Data contracts and validation\n- Drift detection and rollback\n- Synthetic data testing\n- Replayable partitioned writes\n\n## Code Example\n```python\n# Pseudo-code: validate event against contract\ndef validate(event, schema_version):\n    contract = load_contract(schema_version)\n    return jsonschema.validate(instance=event, schema=contract)\n```\n\n## Follow-up Questions\n- How would you define drift thresholds and alerting for schema changes?\n- What testing strategy ensures rollback replay doesn't violate data freshness guarantees?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-7",
        "questionId": "q-12252",
        "question": "On GCP, implement a beginner-friendly streaming ETL: 500 GB/day of JSON events published to Pub/Sub. Build a Dataflow (Beam) job that validates required fields, masks PII, and writes per-tenant records to partitioned BigQuery tables using event_id for idempotent MERGE upserts. Malformed messages go to a dead-letter Pub/Sub topic and originals to Cloud Storage. Include a 24h per-tenant rollback and basic tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a two-region streaming pipeline: 1 TB/day of JSON events from Pub/Sub per tenant ingested via Dataflow into region-local storage and per-tenant BigQuery datasets with Authorized Views. Enforce",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a CI/CD-driven end-to-end test harness that generates deterministic synthetic events per tenant, publishes them to Pub/Sub, executes a Dataflow pipeline in staging, and validates idempotent upserts with exact-…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed approach: Dataflow Python beam pipeline reads Pub/Sub JSON events, validates required fields, masks PII (SSN, email), and upserts into per-tenant partitioned BigQuery tables using event_id fo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a per-tenant schema registry in Data Catalog, streaming via Pub/Sub into Dataflow, write raw JSON to Cloud Storage under tenant prefixes, materialize per-tenant aggregates in BigQuery, and surface",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests building a practical, beginner-friendly streaming ETL, focusing on Pub/Sub -> Dataflow -> BigQuery, basic data quality, privacy, idempotent writes, and rollback/testing—core skills for data engineers at scale.\n\n## Key Concepts\n- Pub/Sub streaming ingestion\n- Apache Beam (Dataflow) transformations\n- Field validation and PII masking\n- Idempotent upserts in BigQuery (MERGE)\n- Per-tenant partitioning strategy\n- Dead-letter handling and archival\n- Rollback and basic tests\n\n## Code Example\n```python\n# Minimal Dataflow pipeline sketch (Python)\nimport json\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\nclass MaskPII(beam.DoFn):\n  def process(self, x):\n    if 'ssn' in x:\n      x['ssn'] = None\n    if 'email' in x:\n      user, dom = x['email'].split('@', 1) if '@' in x['email'] else (x['email'], 'REDACTED')\n      x['email'] = f\"{user}@REDACTED\" if dom else 'REDACTED'\n    return [x]\n\ndef run():\n  opts = PipelineOptions()\n  with beam.Pipeline(options=opts) as p:\n    (p\n     | 'ReadPubSub' >> beam.io.ReadFromPubSub(subscription='projects/.../subscriptions/...', with_attributes=False)\n     | 'Parse' >> beam.Map(lambda b: json.loads(b))\n     | 'Mask' >> beam.ParDo(MaskPII())\n     | 'WriteToBQ' >> beam.io.WriteToBigQuery(\n         table='project.dataset.table$YYYYMMDD',\n         write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND)\n    )\n```\n\n## Follow-up Questions\n- How would you validate idempotency and handle event retries?\n- How would you adapt for new fields or schema evolution without breaking existing pipelines?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-8",
        "questionId": "q-3251",
        "question": "You operate a multi-tenant analytics service on GCP with 50 tenants. 5 GB/day of JSON events arrive via Pub/Sub. Design a beginner-friendly pipeline that ingests events with Dataflow, stores them in a single BigQuery table partitioned by ingestion_date, and includes tenant_id. Enforce per-tenant access using BigQuery authorized views (or per-tenant filters), support additive schema evolution, deduplicate by event_id, handle late data up to 1 hour, and capture end-to-end lineage in Data Catalog. Provide architecture sketch and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pub/Sub events feed a Dataflow pipeline that computes per-tenant features (amounts, velocity, device, geo) and streams them to Vertex AI Feature Store. A per-tenant Online Predictor serves real-time f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a multi-tenant streaming pipeline on GCP that ingests 200 TB/day of telemetry data from Pub/Sub, processes it through Dataflow with per-tenant isolation and privacy masking, writes Parquet files to Cloud Storage…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest Pub/Sub JSON with Dataflow into a single BigQuery table partitioned by ingestion_date, including tenant_id. Enforce per-tenant access via authorized views filtering on tenant_id. Deduplicate by",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a Dataflow streaming pipeline that subscribes to Pub/Sub and writes to a partitioned BigQuery table per tenant. Add a DoFn to validate required fields and route invalid records to a Cloud Storage dead-letter b…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a practical, beginner-friendly pipeline with tenant isolation, governance, and lineage on GCP.\n\n## Key Concepts\n\n- Dataflow streaming ingestion from Pub/Sub\n- BigQuery single table with partitioning by ingestion_date and tenant_id\n- Authorized views for per-tenant access control\n- Additive schema evolution with nullable fields\n- Deduplication by event_id\n- Late data handling (1 hour)\n- Data Catalog-based lineage\n\n## Code Example\n\n```javascript\n// Pseudo Dataflow-style transform (illustrative)\nfunction transform(eventJson){\n  const obj = JSON.parse(eventJson);\n  obj.ingestion_date = (obj.event_time ? obj.event_time : new Date()).toISOString().slice(0,10);\n  return obj;\n}\n```\n\n## Follow-up Questions\n\n- How would you test tenant isolation and access controls?\n- What are trade-offs between a single table vs per-tenant tables?\n- How would you roll out additive schema changes with minimal downtime?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-9",
        "questionId": "q-5875",
        "question": "Ingest 50 GB/day of JSON logs via Pub/Sub and route two event types to separate BigQuery tables (clicks and impressions) with distinct schemas. Build a beginner-friendly Dataflow (Beam) pipeline that parses messages, validates required fields, normalizes timestamps, and writes to daily-partitioned tables. Use insertId for idempotency, a dead-letter Pub/Sub for malformed messages, and archive originals to Cloud Storage. Propose a 24h rollback and a simple schema-evolution approach for adding fields to clicks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Dataflow streaming pipeline: Pub/Sub -> Dataflow -> BigQuery. Add event-time processing with watermarks and 10-minute lateness to handle late data; partition BigQuery tables by day; fan-out t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a GCP streaming path with per-tenant onboarding, using Pub/Sub, Dataflow for enrichment, BigQuery per-tenant datasets for analytics, and Memorystore for online features. Enforce per-tenant cont",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a single Dataflow (Beam) job that parses Pub/Sub JSON, validates required fields (userId, timestamp, eventType), and routes events to two BigQuery tables (clicks and impressions) with separate",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Cloud Dataflow (Python) batch pipeline to read GCS CSVs, validate fields, deduplicate by run_id, and upsert to a daily-partitioned BigQuery table via MERGE. Add optional device_type via a schema_v",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical Dataflow design for multi-table ingestion, basic validation, and idempotent writes in a beginner context. Includes fault handling (DLQ) and a rollback strategy to backfill.\n\n## Key Concepts\n\n- Dataflow (Beam) fundamentals\n- Pub/Sub ingestion and DLQ routing\n- BigQuery daily partitions and separate schemas\n- Idempotent writes with insertId\n- Simple schema evolution and archival\n\n## Code Example\n\n```javascript\n// Pseudo-beam DoFn skeleton for parsing and routing\nclass RouteEventFn extends DoFn {\n  processElement(ctx) {\n    // parse JSON, validate fields, emit to per-type PCollections\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test idempotency and DLQ handling?\n- How would you monitor data quality and backlog in production?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-10",
        "questionId": "q-21310",
        "question": "On GCP, design a multi-tenant streaming analytics pipeline ingesting 2-5 TB/day of JSON events via Pub/Sub for two regions, enforcing per-tenant data quality gates (schema validity, value ranges, and referential integrity) before writing to a shared BigQuery lake with per-tenant partitions. Describe Dataflow design, governance with Data Catalog, automated remediation for bad records, late-arriving data handling, testing and backfill strategy, and cost/SLA targets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a streaming Dataflow template: Pub/Sub ingests JSON events; apply per-field masking/tokenization for PII, then write to tenant-partitioned BigQuery tables. Use per-tenant Authorized Views on a sha",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a GCP streaming pipeline to ingest 2-5 TB/day of JSON events via Pub/Sub across two regions, enforcing per-tenant data quality gates—including schema validity, value range validation, and referential integrity ch…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a scalable multi-tenant data-as-a-product platform where 200 tenants publish event streams to Pub/Sub, processing through Bronze/Silver/Gold layers in BigQuery. Implement per-tenant feature flags, cost controls v…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Pub/Sub -> Dataflow ingests 15 TB/day of JSON events; route to a shared RAW BigQuery dataset partitioned by tenant_id. Use per-tenant Authorized Views on a CLEAN dataset for isolation; redact PII with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses the ability to architect enterprise-grade, multi-tenant data pipelines with built-in quality controls, governance, and real-time processing at scale.\n\n## Key Concepts\n- **Streaming Ingestion**: Pub/Sub with Dataflow for near real-time processing\n- **Per-Tenant Quality Gates**: Schema validation, value range enforcement, referential integrity checks\n- **Data Governance**: Data Catalog for metadata management, lineage tracking, and per-tenant access controls\n- **Data Lake Architecture**: Shared BigQuery with logical tenant partitioning via dataset-level isolation or row-level security\n- **Error Handling**: Dead-letter topics for failed records with automated remediation workflows\n- **Late-Arriving Data**: Watermark-based windowing with allowed lateness configuration\n- **Testing & Backfill**: Unit tests for validation logic, integration tests for pipeline flow, and replay mechanisms for historical data\n- **Cost & SLA**: Optimized pipeline design balancing throughput, latency, and operational cost\n\n## Code Example\n```python\nimport json\nfrom apache_beam import DoFn, ParDo\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\nclass ValidateEventDoFn(DoFn):\n    def __init__(self, tenant_config):\n        self.tenant_config = tenant_config\n    \n    def process(self, element):\n        try:\n            event = json.loads(element)\n            tenant_id = event.get('tenant_id')\n            \n            schema = self.tenant_config.get(tenant_id)\n            if not schema:\n                raise ValueError(f\"Unknown tenant: {tenant_id}\")\n            \n            self._validate_schema(event, schema)\n            self._validate_ranges(event, schema)\n            self._validate_references(event, schema)\n            \n            yield event\n        except Exception as e:\n            yield apache_beam.pvalue.TaggedOutput('bad_records', {\n                'event': element,\n                'error': str(e),\n                'timestamp': datetime.utcnow().isoformat()\n            })\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-11",
        "questionId": "q-18185",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. New angle: implement per-tenant experimental routing by directing a configurable fraction of events to a separate BigQuery table for A/B testing, controlled by a message attribute or a small Dataflow step; ensure 24h per-tenant rollback and Data Catalog registration; include a simple test plan with synthetic tenants?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design uses Pub/Sub -> Dataflow for ingestion, writes raw Parquet to Cloud Storage, exposes per-tenant views via BigLake external tables in BigQuery, and registers per-tenant schemas in Data Catalog.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Beam pipeline in Dataflow to validate inbound Pub/Sub messages against a schema registry, compute per-tenant quality metrics (null rates, outliers, drift), and write valid data to BigQuery and r",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a streaming telemetry pipeline on GCP ingesting 5-10 TB/day of JSON events via Pub/Sub into BigQuery with per-tenant isolation and privacy masking. Integrate Vertex AI Feature Store for online/",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Beam (Python) streaming pipeline that reads Pub/Sub JSON, validates required fields, and routes a configurable fraction of tenants to an EXPERIMENTAL BigQuery table while the rest goes to MAI",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical routing and rollback in a real GCP dataflow pipeline without overengineering.\n\n## Key Concepts\n- Beam dataflow routing, Pub/Sub, BigQueryIO\n- per-tenant feature flag routing, ingestion-time partitioning, rollback\n\n## Code Example\n```python\n# routing example: 80/20 split\ndef route(tenant_id):\n    return 'MAIN' if (hash(tenant_id) % 5) != 0 else 'EXPERIMENT'\n```\n\n## Follow-up Questions\n- How would you monitor drift between MAIN and EXPERIMENT tables?\n- How would you handle skewed tenants affecting backfill latency?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-12",
        "questionId": "q-24772",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. NEW ANGLE: add a data-quality gate at ingestion validating each event against a lightweight evolving schema registry (e.g., stored in Cloud Firestore), routing malformed or drifted records to a Dead-Letter Pub/Sub topic for review, while valid events proceed to BigQuery. Include how to model optional/required fields, drift detection, testing with synthetic drift, and dashboards for validation latency and DLQ counts?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Pub/Sub to BigQuery, validate each JSON against a lightweight schema registry stored in Cloud Firestore, route invalid or drifted events to a Dead-Letter Pub/Sub topic, and write valid even",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a streaming pipeline: Pub/Sub subscription → Dataflow (Beam) with a custom DoFn that deduplicates events by (tenant_id, event_id) using a 60-second fixed window → write to a time-partitioned BigQuery table (parti…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest data with tenant_id as the partitioning key; use Dataflow with a dynamic DoFn that validates each message against a per-tenant schema version stored in Data Catalog. On schema drift detection, automatically promo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed pipeline: a streaming Dataflow job reads Pub/Sub JSON events, validates a minimal schema, and writes to per-tenant partitioned BigQuery tables. Data older than 30 days is archived automatical",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to embed a data-quality gate at ingestion, a common real-world requirement. It exercises schema registry usage, drift detection, DLQ routing, and end-user visibility via dashboards. This angle complements prior questions by focusing on data quality and governance early in the pipeline.\n\n## Key Concepts\n- Ingest-time data quality gates and lightweight schema registry\n- Drift detection and backward-compatible schema evolution\n- Dead-letter routing for malformed or drifted events\n- Cloud Firestore as a lightweight registry; Dataflow/Beam integration\n- Validation latency, DLQ metrics, and streaming dashboards\n\n## Code Example\n```python\n# Pseudo-code: validate against registry-driven schema\ndef validate_event(event, registry_ref):\n    schema = registry_ref.get_schema(event[\"tenant_id\"])\n    for field, spec in schema.items():\n        if spec[\"required\"] and field not in event:\n            return False\n        if field in event and not isinstance(event[field], spec[\"type\"]):\n            return False\n    return True\n```\n\n## Follow-up Questions\n- How would you test drift detection with synthetic registry updates?\n- How would you monitor and alert on DLQ saturation and validation latency?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-13",
        "questionId": "q-22803",
        "question": "On GCP, design a beginner-friendly streaming pipeline to ingest 50 GB/day of JSON clickstream events from Pub/Sub into BigQuery in a single region. NEW ANGLE: add a dedicated automated data-quality layer that validates each event against a contract stored in Data Catalog, and routes invalid events to a DEAD-LETTER BigQuery table while valid ones go to a partitioned destination table. Include how you store and evolve the contract, test with synthetic bad events, and monitor quality metrics and lateness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Dataflow streaming pipeline: Pub/Sub -> Dataflow -> BigQuery partitioned table for valid events; invalid events go to a DEAD-LETTER BigQuery table. Add a data-quality layer that validates eac",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a Dataflow (Apache Beam) streaming job that reads JSON events from Pub/Sub, applies Cloud DLP masking to PHI fields (name, DOB, SSN), writes the redacted records to a BigQuery dataset, and emits audit logs to…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Dataflow to normalize and schema-evolve events, write per-tenant Parquet prefixes to Cloud Storage, and expose per-tenant aggregates via BigQuery tables with Row-Level Security. Use Data Cata",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Dataflow streaming pipeline: Pub/Sub -> Dataflow -> BigQuery. Add event-time processing with watermarks and 10-minute lateness to handle late data; partition BigQuery tables by day; fan-out t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical data quality controls in a streaming pipeline, leveraging Data Catalog for contracts and Dataflow for validation, plus testing and monitoring.\n\n## Key Concepts\n- Dataflow streaming with Pub/Sub\n- Data quality validation against a contract\n- Data Catalog as contract repository\n- Dead-letter handling and partitioned BigQuery writes\n- Synthetic data testing and monitoring\n\n## Code Example\n```python\n# Minimal skeleton of the validation DoFn in Apache Beam (Python)\nclass ValidateEventFn(DoFn):\n    def __init__(self, contract):\n        self.contract = contract\n    def process(self, element, *args, **kwargs):\n        try:\n            event = json.loads(element)\n            if self.is_valid(event, self.contract):\n                yield element\n            else:\n                yield beam.pvalue.TaggedOutput('invalid', element)\n        except Exception:\n            yield beam.pvalue.TaggedOutput('invalid', element)\n```\n\n## Follow-up Questions\n- How would you test contract evolution with backward compatibility?\n- How would you monitor and alert on invalid event rate and processing latency?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-14",
        "questionId": "q-27911",
        "question": "On GCP, design a multi-tenant telemetry pipeline that ingests 1-3 TB/day of device events via Pub/Sub and enforces per-tenant data contracts using Pub/Sub Schema Registry with Avro schemas. Validate in Dataflow, store Parquet in Cloud Storage, expose per-tenant datasets as BigQuery external tables with row-level security. Include schema evolution, canary promotions, automated contract tests, and per-tenant rollback across two regions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pub/Sub Schema Registry with per-tenant Avro schemas to enforce data contracts at ingestion. Dataflow validates and enriches events in streaming mode, then writes Parquet shards to Cloud Storage. Expose tenant datas…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "In this pipeline, ingest Pub/Sub JSON per tenant, use Dataflow to normalize into a single BigQuery schema partitioned by day, validate required fields, and stream to BigQuery. Implement per-tenant pri",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Pub/Sub + Dataflow (Beam) to stream 3 TB/day, isolating tenants by dataset per-tenant, apply differential privacy on aggregates with per-tenant budgets, store budgets and noise params in Fire",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest via Dataflow from Pub/Sub to two regional BigQuery datasets. Use ingestion-time partitioned tables, cluster by tenant_id, and apply row-level security so each tenant sees only their data. Keep",
            "isCorrect": false
          }
        ],
        "explanation": "This question assesses cross-service architecture design and operational maturity. Key components include Pub/Sub Schema Registry with Avro schemas for per-tenant contract enforcement, Dataflow for validation and enrichment, and Cloud Storage with Parquet for cost-efficient storage. BigQuery external tables with row-level security enable query isolation without data duplication. Critical production considerations include schema evolution with backward/forward compatibility, canary promotions for gradual rollouts, automated contract tests, and per-tenant rollback capabilities across two regions.\n\n```python\n# Dataflow pipeline skeleton with contract validation\n\nclass MultiTenantPipeline(Pipeline):\n    def __init__(self, project, region, schema_registry):\n        self.project = project\n        self.region = region\n        self.schema_registry = schema_registry\n\n    def build_pipeline(self, input_subscription, output_path):\n        return (\n            pipeline\n            | 'ReadFromPubSub' >> ReadFromPubSub(subscription=input_subscription)\n            | 'ValidateAgainstSchema' >> ParDo(SchemaValidationDoFn(self.schema_registry))\n            | 'EnrichWithTenantContext' >> ParDo(TenantEnrichmentDoFn())\n            | 'WriteToGCS' >> WriteToParquet(output_path)\n        )\n\nclass SchemaValidationDoFn(DoFn):\n    def __init__(self, schema_registry):\n        self.schema_registry = schema_registry\n\n    def process(self, element):\n        tenant_id = element.get('tenant_id')\n        schema = self.schema_registry.get_schema(tenant_id)\n        try:\n            validated = schema.validate(element)\n            yield pvalue.TaggedOutput('valid', validated)\n        except Exception as e:\n            yield pvalue.TaggedOutput('invalid', (element, str(e)))\n\nclass TenantEnrichmentDoFn(DoFn):\n    def process(self, element):\n        element['processed_timestamp'] = datetime.utcnow().isoformat()\n        element['region'] = self.region\n        yield element\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-15",
        "questionId": "q-2410",
        "question": "Design an end-to-end GCP data ingestion pipeline for 1-2 TB/day of JSON telemetry streamed from Pub/Sub into BigQuery with per-tenant isolation and strict data residency policies. The solution must mask PII per tenant at ingestion (field-level), use CMEK, and employ DLP where needed. Provide two-region architecture, automatic schema evolution, late data handling, and a rollback/backfill plan, plus end-to-end data lineage in Data Catalog and audit exports. Include architecture, data model, testing, and runbook details?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pub/Sub → Dataflow streaming; per-tenant isolation via separate datasets and partitioned tables; field-level masking at parse time driven by a central policy store; CMEK encryption on BigQuery; DLP fo",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a single-region pipeline: Pub/Sub → Dataflow → BigQuery raw table; per-tenant isolation via Authorized Views on a shared dataset. Data quality: enforce required fields, drop rows with key-null,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a streaming pipeline: Pub/Sub -> Dataflow streaming templates -> per-tenant BigQuery in two regions with partitioned tables; tokenization/masking via Data Loss Prevention in the transform; e",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Pub/Sub to feed Dataflow streaming jobs that validate JSON schemas, mask PII via DLP, and emit per-tenant records to separate BigQuery datasets in two regions. Enforce IAM on a per-dataset basis,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a privacy-aware, multi-region data pipeline with governance and testing in a realistic setting.\n\n## Key Concepts\n- Pub/Sub streaming\n- Multi-tenant masking and isolation\n- CMEK and DLP integration\n- Data Catalog lineage\n- Schema evolution and backfills\n- Two-region resilience and audits\n\n## Code Example\n```javascript\n// Dataflow DoFn: mask PII based on tenant policy\nclass MaskPII {\n  process(record, ctx) {\n    const policy = getPolicy(ctx.tenantId);\n    return maskRecord(record, policy);\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test end-to-end lineage accuracy? \n- How would you handle policy updates without downtime?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-16",
        "questionId": "q-23613",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. New angle: implement a dead-letter queue (DLQ) and automatic retry with exponential backoff using Dataflow templates, plus a simple schema validation step that routes malformed messages to DLQ and stores a summary in BigQuery for quality monitoring. Include testing with synthetic bad messages and a rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a Dataflow (Beam) streaming job: Pub/Sub -> Dataflow -> BigQuery (partitioned by day). A validation DoFn checks required fields/types; valid events land in BigQuery, invalid ones are",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest via Pub/Sub, stream with Dataflow to a daily-partitioned BigQuery table, validate fields, apply tenant-specific masking using a policy table, and enforce schema via Pub/Sub Schema Registry. Rou",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a batch Dataflow pipeline that reads per-tenant JSON logs from Cloud Storage, validates them against a schema.json file stored in GCS, flattens nested fields, and writes to a BigQuery table partitioned by ingest_d…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a Dataflow streaming job keyed by tenant_id; redact PII fields via a DoFn using CMEK-protected keys, emitting masked JSON to a per-tenant partitioned BigQuery table. Enforce per-tenant access wi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end streaming with basic validation, error handling, and observability in GCP.\n\n## Key Concepts\n- Dataflow/Beam streaming, Pub/Sub DLQ, JSON schema validation, BigQuery partitioning, test strategy.\n\n## Code Example\n```javascript\n// Pseudo Beam DoFn snippet for validation\n```\n\n## Follow-up Questions\n- How would you monitor DLQ growth and set alerts?\n- How do you test schema evolution while preserving backward compatibility?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-17",
        "questionId": "q-27611",
        "question": "On GCP, design a streaming data quality observability framework for a 20-30 TB/day IoT telemetry pipeline ingested via Pub/Sub, processed with Dataflow, stored as Parquet in Cloud Storage and surfaced to BigQuery via BigLake external tables. Build per-tenant quality contracts in Data Catalog, real-time anomaly detection using TensorFlow Data Validation in Dataflow, and end-to-end lineage. Include automated rollback for schema drift and late-arriving data, alerting, testing, and runbooks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a per-tenant streaming pipeline: Ingest 2 TB/day via Pub/Sub, normalize divergent schemas into a conformed schema in Dataflow, and write to per-tenant BigQuery datasets with IAM-based isolat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a real-time multi-tenant streaming pipeline on GCP: Pub/Sub -> per-tenant Dataflow pipelines -> BigQuery per-tenant tables. Enforce per-tenant quality gates (schema conformance, range checks",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant isolation via per-tenant BigQuery datasets and Pub/Sub topics, a Dataflow streaming pipeline to both regions, and a concurrent Data Quality pipeline that validates schema conformance, null",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant quality contracts in Data Catalog; Dataflow validates Pub/Sub events against a TDV-derived schema, emitting per-tenant quality metrics to Cloud Monitoring. Implement anomaly detection with",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests the candidate's ability to design robust data quality observability for a large-scale streaming pipeline, focusing on per-tenant governance, real-time anomaly detection, and reliable rollback.\n\n## Key Concepts\n- Data quality observability in streaming ETL\n- TensorFlow Data Validation integration with Dataflow\n- Per-tenant contracts via Data Catalog\n- End-to-end lineage and governance\n- Schema drift handling and rollback strategies\n\n## Code Example\n\n```python\n# Pseudo TDV validation in a Dataflow-like transform\nfrom tdv import Schema, validate\nschema = Schema({\"device_id\": \"string\", \"temp\": \"float\"})\ndef process(event):\n  validate(event, schema)\n  # enrichment and routing\n```\n\n## Follow-up Questions\n- How would you test the rollback mechanism under late-arriving data?\n- What metrics and alert thresholds would you establish for drift detection?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-18",
        "questionId": "q-21856",
        "question": "On GCP, design an active-active streaming ingestion pipeline across two regions to process 200 GB/day of JSON events from Pub/Sub, delivering per-tenant aggregates to region-local BigQuery, masking sensitive fields with per-tenant KMS keys, and preserving end-to-end lineage in Data Catalog—while guaranteeing exactly-once semantics and cross-region consistency. How would you implement data flow, testing, failover, and 24h per-tenant rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two region-local Dataflow streaming pipelines ingest Pub/Sub, emitting per-tenant aggregates into BigQuery in their region. Exactly-once via (tenant_id, event_id) dedupe and idempotent writes. Mask PI",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a multi-tenant streaming quality and anomaly pipeline on GCP. Ingest 2-5 TB/day via Pub/Sub; Dataflow enforces per-tenant schema and quality gates; emit anomaly signals; write clean data as Pa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: ingest 2 TB/day via Pub/Sub, route per-tenant data to region-bound BigQuery datasets; use Dataflow for streaming transforms, Vertex AI for real-time enrichment, and Data Catalog for lineage.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a per-tenant streaming pipeline: Ingest 2 TB/day via Pub/Sub, normalize divergent schemas into a conformed schema in Dataflow, and write to per-tenant BigQuery datasets with IAM-based isolat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests DR across regions, privacy, and data governance in GCP.\n\n## Key Concepts\n- active-active DR across regions\n- exactly-once streaming ingestion\n- per-tenant masking with Cloud KMS\n- Data Catalog lineage and Data Exchange contracts\n\n## Code Example\n```python\n# dedupe sketch\ndef process(event):\n  key = (event['tenant_id'], event['event_id'])\n  if seen(key): return\n  emit(event)\n```\n\n## Follow-up Questions\n- How would you monitor cross-region data drift and SLA compliance?\n- What are failure scenarios and how would you test them?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-19",
        "questionId": "q-27392",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. NEW ANGLE: add end-to-end observability by emitting custom metrics from the Dataflow pipeline (ingest_latency_ms, per_tenant_bad_ratio, per_tenant_lag) and a Cloud Function to push metrics to Cloud Monitoring; propose dashboards and an alert rule that fires when bad_ratio exceeds 1% for 10 minutes. Include a small synthetic spike test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Pub/Sub Schema Registry for drift alerts, Dataflow DoFns for validation and normalization, and tenant-partitioned BigQuery tables. Use Data Catalog for per-tenant data contracts and full line",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Dataflow streaming pipeline that subscribes to Pub/Sub and writes to a partitioned BigQuery table per tenant. Add a DoFn to validate required fields and route invalid records to a Cloud Storage dead-letter b…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a two-region GCP data fabric enforcing per-tenant differential privacy budgets for 2-5 TB/day telemetry via Pub/Sub. Streaming Dataflow computes DP-protected aggregates, writes Parquet to Cloud",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest with Dataflow (Beam) reading Pub/Sub, route by tenant to per-tenant BigQuery datasets; create datasets on demand. Apply DLP masking for sensitive fields (email, phone) in the pipeline. Use a pe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question evaluates practical observability skills, including dashboard design, alerting configuration, and data quality enforcement in a real-world GCP pipeline. It also tests familiarity with synthetic testing methodologies to validate alerting behavior before production incidents occur.\n\n## Key Concepts\n- **Dataflow streaming pipeline**: Continuous ingestion from Pub/Sub to BigQuery with windowed processing\n- **Custom Cloud Monitoring metrics**: Per-tenant granularity enables tenant-specific SLA tracking and anomaly detection\n- **Dead-letter routing**: Data quality enforcement without data loss; invalid records are preserved for debugging\n- **Synthetic testing**: Proactive validation of alerting thresholds under load conditions\n\n## Code Example\n```java\npublic class ValidateAndEmitMetricsDoFn extends DoFn<String, TableRow> {\n  private final Counter validRecords;\n  private final Counter invalidRecords;\n  private final Distribution latencyDistribution;\n\n  public ValidateAndEmitMetricsDoFn(String tenantId) {\n    this.validRecords = Metrics.counter(ValidateAndEmitMetricsDoFn.class, \"valid_records_\" + tenantId);\n    this.invalidRecords = Metrics.counter(ValidateAndEmitMetricsDoFn.class, \"invalid_records_\" + tenantId);\n    this.latencyDistribution = Metrics.distribution(ValidateAndEmitMetricsDoFn.class, \"ingest_latency_ms\");\n  }\n\n  @ProcessElement\n  public void processElement(@Element String json, OutputReceiver<TableRow> out, ProcessContext c) {\n    long startTime = System.currentTimeMillis();\n    try {\n      JSONObject record = new JSONObject(json);\n      if (isValid(record)) {\n        out.output(new TableRow().set(\"data\", record.toString()).set(\"tenant_id\", c.sideInput(tenantId)));\n        validRecords.inc();\n      } else {\n        invalidRecords.inc();\n        emitBadRatioMetric(c);\n      }\n    } catch (Exception e) {\n      invalidRecords.inc();\n      emitBadRatioMetric(c);\n    } finally {\n      latencyDistribution.update(System.currentTimeMillis() - startTime);\n    }\n  }\n\n  private void emitBadRatioMetric(ProcessContext c) {\n    Metrics.gauge(ValidateAndEmitMetricsDoFn.class, \"per_tenant_bad_ratio_\" + c.sideInput(tenantId),\n        () -> (double) invalidRecords.getCount() / (validRecords.getCount() + invalidRecords.getCount()) * 100);\n  }\n}\n```\n\n## Dashboard Recommendations\nGroup metrics by tenant using the **Group By** feature in Cloud Monitoring. Include panels for:\n1. Ingest latency distribution (p50, p95, p99)\n2. Bad ratio trend per tenant\n3. Pipeline lag by tenant with threshold lines\n4. Dead-letter bucket size as a data quality indicator\n\n## Alert Configuration\n- **Metric**: per_tenant_bad_ratio\n- **Condition**: > 1% for 10 consecutive minutes\n- **Notification**: Email/Slack/PagerDuty based on severity",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-20",
        "questionId": "q-24053",
        "question": "On GCP, design a streaming pipeline ingesting 100 GB/day of JSON events from Pub/Sub into BigQuery for multi-tenant workloads. Implement a data quality layer using a JSON Schema validation in Dataflow (Beam) and route failing records to Cloud Storage for audits. Enforce per-tenant isolation with partitioned BigQuery tables and per-tenant Authorized Views; capture lineage in Data Catalog; use a canary rollout and 24h rollback window if schema or quality rules drift. How would you implement and test this end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Dataflow in streaming mode parses Pub/Sub JSON, validates each event against a JSON Schema in a DoFn, and emits valid records to a tenant-scoped, partitioned BigQuery table; invalid records are writte",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a dual-region Dataflow streaming pipeline that reads from Pub/Sub, applies per-tenant tokenization and privacy masking using a dedicated key management service, and writes aggregated metrics to partitioned BigQuery…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Pub/Sub for ingestion, with Dataflow streaming pipelines for per-tenant feature computation. Store raw Parquet files in Cloud Storage organized by tenant and date; expose per-tenant aggregates through BigLake extern…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate each message against a fixed schema in a Dataflow custom DoFn. Valid records are written to the primary BigQuery table, while invalid records are routed to both a dedicated errors BigQuery table and Cloud Stora…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProbes advanced data engineering: streaming ingestion, per-tenant isolation, data quality governance, lineage, and rollback under schema drift.\n\n## Key Concepts\n- Streaming pipelines with Dataflow/Beam\n- JSON Schema validation in Beam\n- Data quality governance and bad-record routing\n- Per-tenant isolation via partitioned tables and views\n- Data Catalog lineage and data contracts\n- Canary rollouts and 24h rollback for drift\n\n## Code Example\n```python\nimport json\nclass ValidateJSON(beam.DoFn):\n    def process(self, element):\n        data = json.loads(element)\n        if 'tenant_id' in data and 'event' in data:\n            yield data\n```\n\n## Follow-up Questions\n- How would you implement a canary rollback if the JSON schema changes?\n- What metrics and dashboards would you monitor for data quality drift?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-21",
        "questionId": "q-20200",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. NEW ANGLE: observability and data-quality gates. Ingest only well-formed events; quarantine malformed messages; emit per-tenant quality metrics to a dedicated table and Cloud Monitoring; outline dataflow steps, schema checks, and a test plan with synthetic noisy data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Dataflow streaming pipeline: Pub/Sub → DoFn that validates JSON against a per-tenant schema contract using JSON Schema or Pub/Sub Schema Registry; valid events write to a time-partitioned BigQuery table (par…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a streaming pipeline (Pub/Sub -> Dataflow -> BigQuery) with per-tenant namespaces and dynamic schema evolution. Enforce data quality gates in Dataflow, track lineage in Data Catalog, and publis",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Pub/Sub → Dataflow streaming; per-tenant isolation via separate datasets and partitioned tables; field-level masking at parse time driven by a central policy store; CMEK encryption on BigQuery; DLP fo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design outline: ingest 5 TB/day of JSON via Pub/Sub into a multi-region Dataflow pipeline. Enforce per-tenant isolation with tenant_id keys and Authorized Views in BigQuery, plus Data Catalog lineage.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical streaming design with real-world data quality and observability requirements, including schema validation, error handling, and metrics emission - critical skills for production data pipelines.\n\n## Key Concepts\n- Pub/Sub streaming ingestion with timestamp handling\n- Dataflow DoFn validation with side inputs for tenant schemas\n- BigQuery time-partitioning (by timestamp column, not just tenant_id) with clustering\n- Dead-letter routing (separate table or Pub/Sub DLQ)\n- Cloud Monitoring custom metrics per tenant\n- Schema evolution using Pub/Sub Schema Registry or JSON Schema\n\n## Code Example\n```python\nclass ValidateEvent(beam.DoFn):\n    def process(self, element, tenant_schemas):\n        try:\n            event = json.loads(element)\n            tenant = event.get('tenant_id')\n            schema = tenant_schemas.get(tenant)\n            \n            if not schema:\n                yield beam.pvalue.TaggedOutput('quarantine', {\n                    'raw': element, 'error': 'unknown_tenant', \n                    'tenant_id': tenant, 'ts': datetime.utcnow()\n                })\n                return\n            \n            jsonschema.validate(event, schema)\n            yield beam.pvalue.TaggedOutput('valid', event)\n        except json.JSONDecodeError as e:\n            yield beam.pvalue.TaggedOutput('quarantine', {\n                'raw': element, 'error': f'invalid_json: {e}',\n                'tenant_id': None, 'ts': datetime.utcnow()\n            })\n        except ValidationError as e:\n            yield beam.pvalue.TaggedOutput('quarantine', {\n                'raw': element, 'error': f'schema_violation: {e.message}',\n                'tenant_id': tenant, 'ts': datetime.utcnow()\n            })\n```\n\n## Follow-up Questions\n- **Quarantine retention**: Store in BigQuery with 30-day partition expiration or Pub/Sub DLQ with 7-day message retention; re-ingest via corrected Dataflow job or BigQuery export→import\n- **Schema evolution**: Use additive changes only (new optional fields), never remove required fields; version schemas in registry; test with dual-producer pattern in staging",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-22",
        "questionId": "q-25922",
        "question": "On GCP, design a per-tenant streaming telemetry ingestion path with a tenant-scoped schema registry and policy engine. Ingest 2-4 TB/day via Pub/Sub, enforce per-tenant schema drift policies using Dataflow with a dynamic DoFn that validates against a per-tenant schema version stored in Data Catalog; route to tenant-specific Parquet partitions in Cloud Storage. Expose via BigLake external tables to BigQuery; retain 24h rollback, DLP masking, late data handling, and a test/chaos plan for contracts and lineage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Pub/Sub, Dataflow streaming, keyed by tenant_id; event_id dedupe with stateful processing; per-tenant windowed aggregates for latency and error rate; write to partitioned BigQuery table per",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: Dataflow ingest with exactly-once; tenant quality gates (null rate, drift) as streaming metrics; snapshots in Cloud Storage for 24h rollback via replay; Data Catalog for contracts and lineag",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a pipeline that writes to BigQuery with a single VARIANT column for event payloads, plus a Firestore schema registry for field versions. Dataflow parses Pub/Sub JSON, validates against registry",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest data with tenant_id as the partitioning key; use Dataflow with a dynamic DoFn that validates each message against a per-tenant schema version stored in Data Catalog. On schema drift detection, automatically promo…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses the ability to design a tenant-aware streaming data pipeline with real-time schema governance, data contracts, and rollback capabilities—skills critical for multi-tenant SaaS platforms requiring strong data isolation and compliance.\n\n## Key Concepts\n- **Tenant-scoped schema registry**: Store and version schemas per tenant in Data Catalog with semantic versioning and deprecation policies\n- **Dynamic Dataflow DoFns**: Implement runtime schema validation that loads tenant-specific schemas dynamically without pipeline redeployment\n- **Schema drift handling**: Detect schema changes in real-time, validate incoming records against the registered version, and trigger alerts or reject non-conformant data per tenant-specific policies\n- **Per-tenant data isolation**: Route and partition data by tenant_id to ensure strict logical separation in Cloud Storage while maintaining cost efficiency\n- **BigLake + BigQuery integration**: Leverage BigLake external tables for unified analytics access with fine-grained access controls and schema evolution support\n\n## Critical Considerations\n- **24-hour rollback capability**: Implement versioned snapshots or time-travel queries in Cloud Storage with automated retention policies\n- **DLP masking**: Apply Data Loss Prevention API to redact sensitive fields (PII, PHI, PCI) before storage based on tenant-specific data classification policies\n- **Late data handling**: Configure watermark policies and allow out-of-order ingestion with configurable lag tolerances (e.g., up to 24 hours) using Pub/Sub ordering keys\n- **Chaos engineering**: Design failure injection tests (network partitions, Pub/Sub latency, Dataflow worker restarts) and maintain data lineage tracking via Cloud Logging and Data Catalog entry links",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-23",
        "questionId": "q-22325",
        "question": "On GCP, design an advanced, multi-tenant streaming pipeline that ingests 5 TB/day of JSON events from Pub/Sub into BigQuery. Each tenant has a live data contract stored in Data Catalog and a canonical schema with versioning; events include tenant_id and contract_version. Explain how you handle (1) schema evolution per contract, (2) per-tenant isolation and access, (3) real-time data quality and drift detection, and (4) a 24h per-tenant rollback strategy with testing and governance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate each message against a fixed schema in a Dataflow custom DoFn. Valid records are written to the primary BigQuery table, while invalid records are routed to both a dedicated errors BigQuery table and Cloud Stora…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a Dataflow streaming job that ingests 50 GB/day of JSON events from Pub/Sub into a shared BigQuery raw table. Implement per-tenant isolation using Authorized Views that grant tenants access to only their specific…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a streaming Dataflow pipeline to ingest Pub/Sub events, consult Vertex AI Feature Store for per-tenant anomaly scores, and write results to per-tenant BigQuery tables. Enforce per-tenant access control via Authorize…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a canonical BigQuery schema and versioned Dataflow mappings keyed by tenant_id and contract_version. Contract updates trigger routing to the new mapping and per-tenant Authorized Views via Data Ca",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests schema evolution, tenancy, data contracts, drift detection, rollback and governance in a real streaming pipeline.\n\n## Key Concepts\n\n- Multi-tenant data modeling in BigQuery\n- Versioned data contracts in Data Catalog\n- Schema evolution without breaking consumers\n- Real-time drift detection and per-tenant isolation\n- 24h rollback using BigQuery time travel and raw backups\n\n## Code Example\n\n```sql\n-- Canonical table for events\nCREATE TABLE project.dataset.events_v1 (\n  tenant_id STRING,\n  event_ts TIMESTAMP,\n  contract_version STRING,\n  payload_json STRING\n) PARTITION BY DATE(event_ts);\n```\n\n```python\n# Pseudo mapping by contract version\ndef route(record):\n  if record.contract_version == 'v2':\n    map_fields_v2(record)\n  else:\n    map_fields_v1(record)\n```\n\n## Follow-up Questions\n\n- How would you test contract migrations with zero downtime?\n- What monitoring/alerts ensure per-tenant drift is caught early?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-24",
        "questionId": "q-10544",
        "question": "On GCP, ingest 100 GB/day of JSON events via Pub/Sub into a single-region BigQuery dataset. Build a beginner-friendly Dataflow pipeline that computes per-tenant 7-day rolling averages, stores daily partitions by ingestion date, and exposes per-tenant results to each tenant via BigQuery Authorized Views (no direct table access). Include a 24h per-tenant rollback plan, a basic data quality pass, and a test strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest 30 GB/day via Pub/Sub → Dataflow (Beam) with 5-minute micro-batches to a partitioned raw BigQuery table. Use tenant_id clustering and Authorized Views for per-tenant access. Archive older data",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement Pub/Sub → Dataflow streaming with tenant isolation; write per-tenant BigQuery datasets and Parquet raw in regional Cloud Storage. Use Data Catalog policy tags for field masking, and a schema",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Dataflow to read Pub/Sub JSON events, group by tenant, compute a 7-day rolling average, and write to a partitioned BigQuery table. Create per-tenant Authorized Views to restrict access. Rollback b",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest with Pub/Sub and Dataflow, deploy two regional pipelines feeding per-tenant BigQuery datasets. Use DLP + IAM for field-level masking, and BigQuery auto schema updates. Implement a tenant-scoped",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical streaming ingestion basics, tenant isolation, and rollback discipline with familiar GCP tools. The twist is using BigQuery Authorized Views for per-tenant isolation, which is a common real-world pattern that beginners should understand.\n\n## Key Concepts\n- Pub/Sub ingestion\n- Dataflow (Beam) windowed aggregations\n- BigQuery partitioned tables\n- Authorized Views for tenant isolation\n- Rollback/backfill strategy and data quality checks\n\n## Code Example\n```python\n# Pseudocode: per-tenant 7-day rolling average in Dataflow\nclass AvgPerTenant(DoFn):\n    def process(self, element, window=beam.DoFn.WindowParam):\n        tenant = element['tenant']\n        value = element['metric']\n        # accumulate and emit (tenant, window_end, avg)\n        yield (tenant, window.end, compute_avg(...))\n```\n\n## Follow-up Questions\n- How would you test the Authorized Views isolation in CI?\n- How would you monitor ingestion latency and window correctness in production?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-03T15:51:48.952Z",
    "version": 1
  }
]