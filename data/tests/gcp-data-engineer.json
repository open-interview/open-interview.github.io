[
  {
    "id": "test-gcp-data-engineer",
    "channelId": "gcp-data-engineer",
    "channelName": "GCP Data Engineer",
    "title": "GCP Data Engineer Knowledge Test",
    "description": "Test your understanding of GCP Data Engineer with 25 questions.",
    "questions": [
      {
        "id": "gcp-data-engineer-q-0",
        "questionId": "q-23040",
        "question": "On GCP, design a data mesh for a multi-tenant SaaS ingesting 1-3 TB/day of device telemetry via Pub/Sub. Persist as Parquet in Cloud Storage; expose per-tenant data products via BigLake external tables to BigQuery. Include per-tenant data contracts in Data Catalog, masking/privacy, two-region residency, 24h per-tenant rollback, backfill strategy, and a testing/chaos plan for data contracts and lineage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Parse JSON events from Pub/Sub, validate required fields, enrich via a side-input mapping, and write to a daily-partitioned BigQuery table using insertId for idempotence. Route invalid messages to a d",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a data mesh architecture with tenant-owned data products. Ingest telemetry via Pub/Sub into Dataflow, which writes partitioned Parquet files to Cloud Storage, segmented by tenant and date. Register each data prod…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a streaming pipeline with Pub/Sub → Dataflow (Beam) → BigQuery, featuring a dynamic data quality gate using a Firestore-stored policy engine for field/type validation at ingest. Invalid records are serialized to…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Dataflow (Python) streaming pipeline: Pub/Sub -> Dataflow -> per-tenant BigQuery datasets. Flatten nested JSON, normalize event_time, and enrich with a country lookup in BigQuery via a side i",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates the ability to architect a scalable, tenant-owned data mesh on GCP while balancing data product boundaries, governance, and multi-region requirements.\n\n## Key Concepts\n- Data mesh principles with tenant-owned data products\n- BigLake external tables with Data Catalog for governance and discovery\n- Per-tenant IAM, row-level security, and DLP masking\n- Two-region residency via cross-region replication\n- 24-hour per-tenant rollback using object versioning and namespace restore\n- Backfill strategy leveraging CDC and time-travel queries\n- Chaos engineering for data contracts and lineage validation",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-1",
        "questionId": "q-5875",
        "question": "Ingest 50 GB/day of JSON logs via Pub/Sub and route two event types to separate BigQuery tables (clicks and impressions) with distinct schemas. Build a beginner-friendly Dataflow (Beam) pipeline that parses messages, validates required fields, normalizes timestamps, and writes to daily-partitioned tables. Use insertId for idempotency, a dead-letter Pub/Sub for malformed messages, and archive originals to Cloud Storage. Propose a 24h rollback and a simple schema-evolution approach for adding fields to clicks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant isolation with IAM, Authorized Views, and separate feature stores; process Pub/Sub events in Dataflow, compute features in real-time, score with Vertex AI hosted model, and push r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub for ingestion, with Dataflow streaming pipelines for per-tenant feature computation. Store raw Parquet files in Cloud Storage organized by tenant and date; expose per-tenant aggregates through BigLake extern…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Pub/Sub to feed Dataflow streaming jobs that validate JSON schemas, mask PII via DLP, and emit per-tenant records to separate BigQuery datasets in two regions. Enforce IAM on a per-dataset basis,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a single Dataflow (Beam) job that parses Pub/Sub JSON, validates required fields (userId, timestamp, eventType), and routes events to two BigQuery tables (clicks and impressions) with separate",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical Dataflow design for multi-table ingestion, basic validation, and idempotent writes in a beginner context. Includes fault handling (DLQ) and a rollback strategy to backfill.\n\n## Key Concepts\n\n- Dataflow (Beam) fundamentals\n- Pub/Sub ingestion and DLQ routing\n- BigQuery daily partitions and separate schemas\n- Idempotent writes with insertId\n- Simple schema evolution and archival\n\n## Code Example\n\n```javascript\n// Pseudo-beam DoFn skeleton for parsing and routing\nclass RouteEventFn extends DoFn {\n  processElement(ctx) {\n    // parse JSON, validate fields, emit to per-type PCollections\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test idempotency and DLQ handling?\n- How would you monitor data quality and backlog in production?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-2",
        "questionId": "q-3753",
        "question": "Design a cross-tenant analytics platform on GCP that ingests 2 TB/day of JSON events per tenant via Pub/Sub, stores raw Parquet in regional Cloud Storage, and serves per-tenant aggregated views in BigQuery with strict isolation. Implement per-tenant differential privacy budgets at query time using DP libraries and UDFs, track budgets in Data Catalog, and enable a 24h rollback of privacy budgets. Include data models, lineage, testing strategy, and rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a data mesh architecture with domain-owned BigQuery datasets for Sales, Product, and Operations. Each domain manages its own data products using standardized schemas, while Data Catalog provides centralized me…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a two-layer scheme: a stable BigQuery table with known fields and a separate extra_json column to capture drift. The Dataflow job maps incoming JSON to the known schema; unknown fields are stored",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "In-depth approach: ingest 2 TB/day via Pub/Sub, land raw Parquet in regional GCS, and publish per-tenant views in BigQuery with row-level isolation. Enforce differential privacy at query time using DP",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Ingest 15 TB/day via Pub/Sub, route per-tenant data using tenant_id partitions in Dataflow, and write to BigQuery with per-tenant Authorized Views and RLS. Apply DLP masking on PII, store contracts/li",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis new angle tests privacy-aware analytics design across GCP services, tenant isolation, and governance.\n\n## Key Concepts\n- Differential privacy budgets per tenant\n- BigQuery UDFs for DP noise\n- Dataflow DP transforms and lineage\n- Data Catalog metadata for budget tracking\n- Regional storage and per-tenant isolation\n- Rollback strategies and testing\n\n## Code Example\n```javascript\n// DP budget checkpoint (pseudo)\nfunction applyDP(value, epsilonBudget) {\n  const noise = sampleLaplace(1/epsilonBudget);\n  return value + noise;\n}\n```\n\n## Follow-up Questions\n- How would you test budget exhaustion and prevent cross-tenant leakage?\n- How would you handle late data affecting DP guarantees and rollback?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-3",
        "questionId": "q-26192",
        "question": "On GCP, design a data-pipeline for a SaaS product ingesting 200 GB/day of JSON events per tenant via Pub/Sub, streaming into BigQuery with per-tenant isolation. Implement data quality gates: embed per-tenant validation using Great Expectations in Dataflow to validate schema, ranges, and nulls; route bad records to a DLQ; surface validation metrics. Include schema evolution, per-tenant retention, and rollback/testing strategy. What is your end-to-end approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Provide a concrete design: Pub/Sub schema enforcement, Dataflow DoFn for tenant routing, per-tenant datasets or views with access controls, Data Catalog lineage, CMEK on datasets, Vertex AI scoring, 2",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a schema registry in Data Catalog to serve as the single source of truth for all tenant schema definitions. Dataflow queries Data Catalog to apply tenant-specific schema validators, enforcing required fields,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a GCP streaming pipeline ingesting 200 GB/day per tenant via Pub/Sub, writing to per-tenant BigQuery partitions. Implement data quality gates with Great Expectations in Dataflow (validate schem",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Dataflow in streaming mode parses Pub/Sub JSON, validates each event against a JSON Schema in a DoFn, and emits valid records to a tenant-scoped, partitioned BigQuery table; invalid records are writte",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to add data quality at streaming scale with per-tenant isolation and governance.\n\n## Key Concepts\n- Streaming validation with Great Expectations in Dataflow\n- Per-tenant BigQuery partitions and access controls\n- DLQ handling, metrics, and rollback tests\n- Schema evolution and retention policies\n\n## Code Example\n```javascript\n// Pseudo-code: Dataflow DoFn for validation using GE-like rules\nclass ValidateFn extends DoFn {\n  processElement(ctx){\n    const rec=ctx.element;\n    if(!valid(rec)){ ctx.output(badTag, rec); } else { ctx.output(goodTag, rec); }\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test validating rules with synthetic tenants?\n- How would you monitor prod quality drift across tenants and trigger rollbacks?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-4",
        "questionId": "q-1310",
        "question": "You operate a streaming ingestion pipeline: Pub/Sub -> Dataflow -> BigQuery. Daily 50k events with fields user_id, event_type, amount (which can be string). Propose a simple ingestion-time data quality plan that validates JSON schema, coerces types, and routes invalid records to a dead-letter Pub/Sub topic without stopping the pipeline. Include testing with a small sample and how you monitor invalid counts?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: per-tenant namespaces in Vertex AI Feature Store, a Dataflow streaming pipeline from Pub/Sub feeding both offline and online feature stores, with IAM-based isolation and tenant-scop",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Parse Pub/Sub messages with a simple Beam DoFn that validates required fields (user_id, event_type) and coerces amount to float. Valid records go to BigQuery; invalid ones emit to a dead-letter Pub/Su",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a streaming pipeline with Pub/Sub to Dataflow, routing each tenant's data into a separate, partitioned BigQuery dataset. Apply tenant-specific masking in Dataflow via policy-driven UDFs, store",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a dual-region Dataflow streaming pipeline that reads from Pub/Sub, applies per-tenant tokenization and privacy masking using a dedicated key management service, and writes aggregated metrics to partitioned BigQuery…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess data quality at ingestion and the practicality of dead-letter routing in a real pipeline.\n\n## Key Concepts\n\n- Data quality at ingestion\n- Side outputs / dead-letter routing\n- Type coercion and validation\n- Beam/Dataflow to BigQuery integration\n- Monitoring\n\n## Code Example\n\n```javascript\n// Pseudo Beam DoFn for ingestion validation\nfunction processElement(element) {\n  let obj;\n  try { obj = JSON.parse(element); } catch (e) { return {error: 'invalid_json'}; }\n  if (!obj.user_id || !obj.event_type) return {error: 'missing_fields'};\n  const amount = parseFloat(obj.amount);\n  if (Number.isNaN(amount)) return {error: 'invalid_amount'};\n  return {user_id: obj.user_id, event_type: obj.event_type, amount: amount};\n}\n```\n\n## Follow-up Questions\n\n- How would you test dead-letter routing under load?\n- How would you tune retry/backoff for DLQ messages?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-5",
        "questionId": "q-24216",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. NEW ANGLE: implement out-of-order tolerance by using event_timestamp with a 5-minute allowed lateness, computing per-tenant aggregates (count, min_event_ts, max_event_ts) in a rolling per-tenant table, and routing late events to a dead-letter topic. Outline data model changes, Dataflow windowing, and validation steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a three-tier design: raw/curated/analytics with per-tenant schemas in BigQuery, regionally stored data in Cloud Storage, streamed via Dataflow. Enforce privacy with per-tenant masking, encryption/",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Dataflow streaming reads Pub/Sub, parses JSON, and upserts into per-tenant BigQuery tables using MERGE on event_id. A nightly batch loads backfilled events from GCS and upserts, deduplicating by event",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement per-tenant tombstones and a purge workflow: raw events go into a partitioned BigQuery table scoped by tenant; on erase, emit a tombstone record and run a purge pipeline within 24h th",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Dataflow (Beam) to ingest from Pub/Sub, validate JSON schema, and write to a partitioned BigQuery table with tenant_id and event_timestamp fields. Apply a 5-minute allowed lateness window using event_timestamp to ha…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the candidate's ability to handle real-world data quality challenges, including out-of-order event arrival and tenant-level aggregation in a serverless GCP stack. It also tests familiarity with dead-letter patterns for operational monitoring.\n\n## Key Concepts\n- Pub/Sub to BigQuery streaming ingestion\n- Dataflow/Beam windowing with allowed lateness\n- Per-tenant rolling aggregations\n- Dead-letter routing for failed events\n- Schema evolution and validation\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.transforms.window import FixedWindows\nfrom apache_beam.transforms.trigger import AfterWatermark, AfterProcessingTime, AccumulationMode\n\nclass ProcessEvent(beam.DoFn):\n    def process(self, element):\n        import json\n        try:\n            data = json.loads(element)\n            yield beam.pvalue.TaggedOutput('valid', data)\n        except:\n            yield beam.pvalue.TaggedOutput('invalid', element)\n\npipeline | 'ReadFromPubSub' >> beam.io.ReadFromPubSub(topic='projects/my-project/topics/clickstream')\n         | 'ParseAndValidate' >> beam.ParDo(ProcessEvent()).with_outputs('invalid', main='valid')\n         | 'WindowInto' >> beam.WindowInto(\n             FixedWindows(60),\n             trigger=AfterWatermark(early=AfterProcessingTime(30)),\n             allowed_lateness=300,\n             accumulation_mode=AccumulationMode.DISCARDING\n         )\n         | 'AggregatePerTenant' >> beam.CombinePerKey(\n             beam.CombineFn(lambda xs: {\n                 'count': len(xs),\n                 'min_event_ts': min(x['event_timestamp'] for x in xs),\n                 'max_event_ts': max(x['event_timestamp'] for x in xs)\n             })\n         )\n```",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-6",
        "questionId": "q-3415",
        "question": "New streaming data: 150 MB/day of JSON click events arrive via Pub/Sub and must be ingested into BigQuery in a single region. Design a beginner-friendly GCP pipeline using Dataflow (Beam) to parse, validate required fields, and enrich with a userId lookup; store results in a daily-partitioned BigQuery table; implement idempotent writes using insertId or MERGE; route malformed messages to a dead-letter Pub/Sub topic and archive originals to Cloud Storage; propose a 24-hour rollback plan and a basic data quality/monitoring approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Dataflow streaming pipeline: Pub/Sub -> Dataflow -> BigQuery partitioned table for valid events; invalid events go to a DEAD-LETTER BigQuery table. Add a data-quality layer that validates eac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest 15 TB/day via Pub/Sub, route per-tenant data using tenant_id partitions in Dataflow, and write to BigQuery with per-tenant Authorized Views and RLS. Apply DLP masking on PII, store contracts/li",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Dataflow pipeline that tags each event with tenant_id, writes to a partitioned RAW BigQuery table, and emits per-tenant metrics for latency, in-flight window, and failures to Cloud Monitoring",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design Dataflow (Beam) to parse JSON, validate fields (event_id, user_id, ts), and enrich with a simple userId lookup. Write to a daily-partitioned BigQuery table using BigQueryIO with insertId on eve",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to craft a practical streaming pipeline on GCP with beginner-friendly components: Pub/Sub ingestion, Dataflow parsing/validation, BigQuery partitioning, idempotent writes, dead-letter handling, and archival. Also touches data quality checks and rollback planning.\n\n## Key Concepts\n\n- Dataflow Beam parsing and validation\n- BigQuery streaming inserts with insertId or MERGE\n- Dead-letter queues and archival storage\n- Schema evolution, basic data quality checks, monitoring\n\n## Code Example\n\n```javascript\n// Example: pseudo-beam snippet illustrating insertId usage\n```\n\n## Follow-up Questions\n\n- How would you test the pipeline end-to-end?\n- What data quality metrics would you monitor?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-7",
        "questionId": "q-3879",
        "question": "Design a cost-aware, two-region multi-tenant analytics pipeline on GCP that ingests 1 TB/day of JSON events via Pub/Sub into BigQuery. Add tenant-scoped data quality scoring at ingest, per-tenant row-level isolation, and automated schema drift rollback with canary deployments. Include data models, partitioning, testing plan, and rollback strategy per tenant?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant, two-region pipeline: Pub/Sub -> Dataflow -> BigQuery per-tenant datasets. Enforce isolation with Row-Level Security and Authorized Views; compute tenant-scoped data quality sco",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect a per-tenant Vertex AI Feature Store with isolated online/offline stores, streaming updates from Pub/Sub to per-tenant feature rows, and data contracts enforced by Data Catalog. Include per-",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a Dataflow (Beam) pipeline that ingests 50 GB/day of JSON clickstream events from Pub/Sub to BigQuery in one region, plus a data-drift harness. Implement a DoFn that samples records, validates",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: ingest 2 TB/day via Pub/Sub, route per-tenant data to region-bound BigQuery datasets; use Dataflow for streaming transforms, Vertex AI for real-time enrichment, and Data Catalog for lineage.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a cost-aware, two-region, multi-tenant pipeline with scalable data quality, robust isolation, and a clear rollback strategy. Requires practical knowledge of GCP services and governance.\n\n## Key Concepts\n- Two-region BigQuery with per-tenant datasets and isolation via RLS/Authorized Views\n- Ingest-time data quality scoring (conformance, completeness, duplicates)\n- Canary schema changes and per-tenant rollback within 24h\n- Data Catalog for lineage; Dataflow for streaming transformation\n\n## Code Example\n```python\n# Pseudo Dataflow transform: extract tenant, score quality, route to ok/bad lanes\nclass ProcessEvent(DoFn):\n    def process(self, element):\n        evt = json.loads(element)\n        tenant = evt['tenant_id']\n        score = score_quality(evt)\n        if score['ok']:\n            yield beam.window.TimestampedValue((tenant, evt), evt['ts'])\n        else:\n            yield beam.pvalue.TaggedOutput('bad', (tenant, evt, score))\n```\n\n## Follow-up Questions\n- How would you validate per-tenant rollback across all tenants without impacting live queries?\n- What metrics and alerts would you surface to detect schema drift per tenant?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-8",
        "questionId": "q-1953",
        "question": "Design a GCP data pipeline for a multinational SaaS product that ingests 200 GB/day of JSON telemetry from Pub/Sub into BigQuery across two regions. Requirements: multi-tenant isolation, automatic schema evolution, per-tenant TTL, data quality checks (schema conformance, nulls, duplicates), and an automated rollback path for schema changes. Include data model sketches, partitioning strategy, testing, and rollback steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a pipeline from Pub/Sub to per-tenant BigQuery datasets using Dataflow (Beam). Validate required fields, parse event_time to TIMESTAMP, and implement idempotent writes via MERGE with a tenant_",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a Dataflow streaming job (Python) that ingests 50 GB/day from Pub/Sub into per-tenant BigQuery datasets in one region. Validation uses a Cloud Storage JSON manifest: required fields, type check",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant datasets in BigQuery and two-region Dataflow pipelines with a unified Avro/JSON schema registry. Ingest with event_id for dedup, partition by ingestion_date, TTL per tenant (30/90 days)",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a Dataflow streaming job that subscribes to Pub/Sub, parses JSON, applies DLP redaction for email, phone, and SSN, then writes de-identified data to a partitioned BigQuery table. Use DLP templa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design a compliant, scalable, two-region data pipeline with strong data quality and schema governance, plus a robust rollback path.\n\n## Key Concepts\n- Multi-tenant isolation with per-tenant datasets\n- Two-region active-active ingestion\n- Schema registry and automatic evolution\n- TTL/retention controls per tenant\n- Data quality: schema conformance, null checks, dedup\n- Rollback strategy: versioned schemas and reprocessing window\n- Metadata and lineage via Data Catalog\n\n## Code Example\n```python\n# Pseudocode: Beam DoFn for basic validation and dedup tagging\nclass ValidateEventDoFn(beam.DoFn):\n    def process(self, element):\n        data = json.loads(element)\n        if not data.get('tenant_id') or not data.get('event_id'):\n            yield beam.pvalue.TaggedOutput('invalid', data)\n            return\n        # type checks and masking placeholders\n        yield data\n```\n\n## Follow-up Questions\n- How would you test schema evolution across regions without downtime?\n- What metrics and alerts would you set for quality/rollback events?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-9",
        "questionId": "q-3512",
        "question": "On GCP, design a two-tenant data platform for an advertising analytics service ingesting 800 GB/day of JSON events via Pub/Sub. Each tenant has isolated dashboards and ML models. Build versioned data contracts in Data Catalog, enforce them via a registry, route data to per-tenant BigQuery tables and to Vertex AI Feature Store for ML, apply per-tenant PII masking, and maintain end-to-end lineage. Include testing, a canary rollout, and a per-tenant rollback window (6 hours)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed solution: a Dataflow (Beam) pipeline that consumes Pub/Sub JSON, applies a simple schema map in a DoFn, writes to a partitioned BigQuery table (ingestion_time) and, in parallel, a nightly bat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Ingest via Pub/Sub to Dataflow (Apache Beam) deployed across two regions, writing to region-partitioned BigQuery tables with tenant isolation. Implement drift detection by comparing incoming event schemas against a cano…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "8 TB/day per-tenant; Pub/Sub to per-tenant BigQuery via streaming Dataflow templates; enforce access with Data Catalog policy tags and IAM at the dataset level; define per-tenant data contracts in a c",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Establish per-tenant data contracts in Data Catalog with versioned schemas, enforce via Dataflow templates that validate JSON against the registry, mask PII with DLP before storage, route to tenant-sc",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world pattern combining data contracts, lineage, privacy, and ML integration; tests ability to design governance and practical rollback\n\n## Key Concepts\n- Versioned data contracts in Data Catalog\n- Dataflow-based validation against registry\n- PII masking via DLP and per-tenant isolation\n- BigQuery + Vertex AI Feature Store integration\n- End-to-end lineage and canary rollouts\n\n## Code Example\n```yaml\ncontracts:\n  - tenant: \"tenantA\"\n    schema_version: \"v1\"\n    fields:\n      - name: \"user_id\"\n        type: \"STRING\"\n      - name: \"event_type\"\n        type: \"STRING\"\n      - name: \"timestamp\"\n        type: \"TIMESTAMP\"\n    privacy:\n      mask_fields:\n        - \"user_id\"\n```\n\n## Follow-up Questions\n- How would you test schema evolution for existing tenants?\n- How would you implement canary rollout and rollback for contracts?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-10",
        "questionId": "q-22325",
        "question": "On GCP, design an advanced, multi-tenant streaming pipeline that ingests 5 TB/day of JSON events from Pub/Sub into BigQuery. Each tenant has a live data contract stored in Data Catalog and a canonical schema with versioning; events include tenant_id and contract_version. Explain how you handle (1) schema evolution per contract, (2) per-tenant isolation and access, (3) real-time data quality and drift detection, and (4) a 24h per-tenant rollback strategy with testing and governance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest 5-10 GB/day via Pub/Sub. Dataflow parses JSON, normalizes timestamps, standardizes fields, and computes a per-tenant quality score (completeness, validity, consistency). Write normalized events",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a streaming quality framework: Pub/Sub Avro events flow into Dataflow templates that enforce per-tenant contracts (required fields, types, value ranges) and emit validated rows to tenant-specific BigQuery tabl…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Dataflow (Apache Beam) streaming job that reads JSON events from Pub/Sub, applies Cloud DLP masking to PHI fields (name, DOB, SSN), writes the redacted records to a BigQuery dataset, and emits audit logs to…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a canonical BigQuery schema and versioned Dataflow mappings keyed by tenant_id and contract_version. Contract updates trigger routing to the new mapping and per-tenant Authorized Views via Data Ca",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests schema evolution, tenancy, data contracts, drift detection, rollback and governance in a real streaming pipeline.\n\n## Key Concepts\n\n- Multi-tenant data modeling in BigQuery\n- Versioned data contracts in Data Catalog\n- Schema evolution without breaking consumers\n- Real-time drift detection and per-tenant isolation\n- 24h rollback using BigQuery time travel and raw backups\n\n## Code Example\n\n```sql\n-- Canonical table for events\nCREATE TABLE project.dataset.events_v1 (\n  tenant_id STRING,\n  event_ts TIMESTAMP,\n  contract_version STRING,\n  payload_json STRING\n) PARTITION BY DATE(event_ts);\n```\n\n```python\n# Pseudo mapping by contract version\ndef route(record):\n  if record.contract_version == 'v2':\n    map_fields_v2(record)\n  else:\n    map_fields_v1(record)\n```\n\n## Follow-up Questions\n\n- How would you test contract migrations with zero downtime?\n- What monitoring/alerts ensure per-tenant drift is caught early?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-11",
        "questionId": "q-23146",
        "question": "On GCP, design a streaming pipeline that ingests 5 TB/day of JSON events from Pub/Sub into BigQuery with per-tenant isolation; describe how you would implement a versioned JSON schema registry, end-to-end lineage, automatic schema evolution, and a robust 30-day rollback, testing, and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a two-stage pipeline: stage1 normalizes Parquet(JSON/Avro) to a canonical Parquet schema, stage2 loads to a partitioned BigQuery table. Use a central schema registry and field-level nulls fo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Pub/Sub → Dataflow with a DoFn that validates each event against a versioned JSON schema stored in Cloud Storage; route valid events to a tenant-scoped, partitioned BigQuery table and invalid ones",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Leverage Pub/Sub + Dataflow (Beam) to stream 3 TB/day, isolating tenants by dataset per-tenant, apply differential privacy on aggregates with per-tenant budgets, store budgets and noise params in Fire",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Cloud Composer to orchestrate a Dataflow (Beam) job that ingests 100 GB/day of newline-delimited NDJSON from Cloud Storage and writes to per-tenant BigQuery datasets. Use a shared raw table struct",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end data governance: schema management, per-tenant isolation, lineage, and rollback in a real streaming context.\n\n## Key Concepts\n- Versioned JSON schema registry in Cloud Storage\n- Dataflow DoFn for per-record validation\n- Tenant-scoped partitioned BigQuery tables\n- End-to-end lineage via Data Catalog\n- Automated schema evolution with backfill and rollback window\n- Data quality quarantine, alerting, and monitoring\n\n## Code Example\n```python\n# Pseudo: Dataflow DoFn validating events against current schema version\nclass ValidateEvent(DoFn):\n    def process(self, element, schema_version):\n        schema = load_schema(schema_version)\n        if validate(element, schema):\n            yield element\n        else:\n            yield pvalue.TaggedOutput('invalid', element)\n```\n\n## Follow-up Questions\n- How would you implement canary rollout of a new schema version?\n- How do you handle late-arriving events referencing older schema versions?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-12",
        "questionId": "q-13290",
        "question": "On GCP, design a 1,000-tenant streaming analytics platform: tenants share a single BigQuery dataset secured with Row-Level Security by tenant_id. Ingest 5 TB/day of JSON via Pub/Sub, run a Dataflow pipeline to enrich, window, and write per-tenant partitions; publish aggregates to Data Exchange. Include 24h per-tenant rollback, data contracts, and a schema-evolution/testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Pub/Sub for ingestion, with Dataflow streaming pipelines for per-tenant feature computation. Store raw Parquet files in Cloud Storage organized by tenant and date; expose per-tenant aggregates through BigLake extern…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Pub/Sub → Dataflow → BigQuery pipeline, with per-tenant anomaly scoring via Vertex AI online endpoint. Store results in partitioned per-tenant BigQuery tables and publish aggregates throug",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a 1,000-tenant streaming analytics platform: tenants share a single BigQuery dataset secured with Row-Level Security by tenant_id. Ingest 5 TB/day of JSON via Pub/Sub, process with Dataflow to",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a Dataflow (Beam) streaming job (Python) to parse Pub/Sub JSON click events, validate required fields, and normalize country codes to ISO 3166-1 alpha-2. Write to a daily-partitioned BigQuery t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design scalable, secure multi-tenant pipelines using modern GCP services (Pub/Sub, Dataflow, BigQuery with RLS, Data Exchange) and to articulate rollback, data contracts, and schema evolution.\n\n## Key Concepts\n- BigQuery Row-Level Security for multi-tenant isolation\n- Dataflow streaming pipelines with windowing and late data handling\n- Data contracts and Data Exchange for external consumption\n- Per-tenant rollback strategy and point-in-time data restoration\n- Schema evolution with backward compatibility and testing\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\nimport json\n\ndef enrich(e):\n  # example enrichment\n  e['processed_at'] = 'now'\n  return e\n\nwith beam.Pipeline(options=PipelineOptions()) as p:\n  (p\n   | 'ReadPubSub' >> beam.io.ReadFromPubSub(topic='projects/PROJECT/topics/tenant-events')\n   | 'Parse' >> beam.Map(lambda b: json.loads(b.decode('utf-8')))\n   | 'Enrich' >> beam.Map(enrich)\n   | 'WriteBQ' >> beam.io.WriteToBigQuery(\n       table='shared_dataset.tenant_events',\n       schema='SCHEMA_AUTODETECT',\n       write_disposition=beam.io.BigQueryDisposition.WRITE_APPEND)\n  )\n```\n\n## Follow-up Questions\n- How would you validate 24h rollback with minimal data loss and cost?\n- How would you handle tenant churn and schema drift while keeping a shared dataset compliant?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-13",
        "questionId": "q-14615",
        "question": "On GCP, design a multi-tenant CDC and schema-evolution pipeline that ingests 1-2 TB/day of JSON/Parquet events from Pub/Sub into per-tenant BigQuery datasets. Implement per-tenant schema drift detection, auto-generate backward-compatible migrations, and apply per-tenant schema changes using Dataflow templates. Store pre-migration snapshots for 24h rollback. Enforce contracts in Data Catalog, mask PII with DLP, and publish anonymized changes to partners via Data Exchange. Include 24h per-tenant rollback and testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage per-tenant schema contracts tracked in Data Catalog, a Beam Dataflow streaming pipeline that validates events against the contract, routing only compliant records to tenant-segregated BigQuer",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant Change Data Capture (CDC) pipeline using Pub/Sub to ingest JSON and Parquet events into tenant-specific BigQuery datasets. Deploy Apache Beam-based Dataflow templates with integrated schema drift…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Architect a 3-region cross-tenant streaming pipeline: 4 TB/day of JSON clickstream from Pub/Sub, Dataflow processing with per-tenant masking via DLP; regional raw Parquet in Cloud Storage; per-tenant",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest 1 TB/day of JSON events via Pub/Sub into a single-region BigQuery, with per-tenant schema evolution stored in Data Catalog. Use a Dataflow (Python Beam) pipeline that maps each tenant to its ex",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses the ability to design a scalable, tenant-isolated CDC and schema evolution pipeline with robust governance, security, and rollback requirements.\n\n## Key Concepts\n\n- Change Data Capture (CDC) at cloud scale\n- Per-tenant data isolation and architecture\n- Schema drift detection and automated validation\n- Backward-compatible migration strategies\n- Data Catalog contract enforcement\n- DLP-based PII masking and data protection\n- Data Exchange for secure partner publishing\n\n## Code Example\n\n```javascript\n// Pseudo-logic: drift detection\nfunction driftCheck(incomingSchema, currentSchema) {\n  const changes = schemaDiff(incomingSchema, currentSchema);\n  \n  if (isBackwardCompatible(changes)) {\n    generateMigration(changes);\n    createSnapshot();\n    applyMigration(changes);\n  } else {\n    alertSchemaViolation(changes);\n  }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-14",
        "questionId": "q-4022",
        "question": "Design a two-region streaming pipeline on GCP for multi-tenant analytics (Pub/Sub → Dataflow → BigQuery). Enforce per-tenant isolation (Authorized Views), two-region active-active replication, exactly-once processing, late data handling, and per-tenant TTL (90 days). Support automatic schema evolution and provide a rollback plan using versioned tables and canary validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-tenant schema registry in Data Catalog, streaming via Pub/Sub into Dataflow, write raw JSON to Cloud Storage under tenant prefixes, materialize per-tenant aggregates in BigQuery, and surface",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I propose a cross-region streaming architecture with separate Pub/Sub topics per region, Dataflow jobs that enforce tenant ID isolation and perform real-time PII masking through DoFn transformations, CMEK-encrypted BigQ…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Dataflow (Apache Beam) streaming job that reads JSON events from Pub/Sub, applies Cloud DLP masking to PHI fields (name, DOB, SSN), writes the redacted records to a BigQuery dataset, and emits audit logs to…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Configure a Dataflow streaming pipeline (Pub/Sub → Dataflow → BigQuery) with per-tenant datasets and Authorized Views, two-region replication, and exactly-once processing. Use TTL via time-partitioned",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nTests practical mastery of streaming pipelines, cross-region consistency, and tenant isolation with BigQuery, Dataflow, Pub/Sub. It also probes ability to design rollback and testing.\\n\\n## Key Concepts\\n- Streaming exactly-once semantics in Dataflow\\n- Tenant isolation with Authorized Views/BQ datasets\\n- Two-region replication and failover\\n- TTL and schema evolution strategies\\n- Canary rollbacks and testing\\n\\n## Code Example\\n```javascript\\n// Pseudo-Dataflow config (illustrative)\\nconst opts = {\\n  streaming: true,\\n  region: 'us-central1'\\n};\\n```\\n\\n## Follow-up Questions\\n- How would you test TTL compliance and canary rollback?\\n- How would you handle schema drift across regions?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-15",
        "questionId": "q-3795",
        "question": "Design a GCP streaming ML feature pipeline for five tenants: ingest 250 GB/day of JSON events from Pub/Sub into Vertex AI Feature Store (online for real-time scoring and offline for batch) with per-tenant isolation, data contracts in Data Catalog, and PII masking at ingest. Include schema evolution, drift monitoring, cross-region replication, and a 24h rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a three-tier design: raw/curated/analytics with per-tenant schemas in BigQuery, regionally stored data in Cloud Storage, streamed via Dataflow. Enforce privacy with per-tenant masking, encryption/",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Five-tenant streaming ML feature pipeline: Pub/Sub → Dataflow → Vertex AI Feature Store (online for real-time scoring and offline for batch) with per-tenant isolation, data contracts in Data Catalog,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Parse JSON events from Pub/Sub, validate required fields, enrich via a side-input mapping, and write to a daily-partitioned BigQuery table using insertId for idempotence. Route invalid messages to a d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a streaming Dataflow pipeline to ingest Pub/Sub events, consult Vertex AI Feature Store for per-tenant anomaly scores, and write results to per-tenant BigQuery tables. Enforce per-tenant access control via Authorize…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end streaming ML pipelines on GCP with tenants separation, data contracts, and governance.\n\n## Key Concepts\n- Vertex AI Feature Store (online/offline)\n- Pub/Sub + Dataflow streaming\n- Data Catalog data contracts and schema evolution\n- Per-tenant isolation via IAM\n- Drift monitoring and rollback plans\n\n## Code Example\n```javascript\n// PII masking example at ingest\nfunction maskPII(record){ if(record.email){ record.email = hash(record.email); } return record; }\n```\n\n## Follow-up Questions\n- How would you validate schema evolution without breaking tenants?\n- How would you design drift alerts and rollbacks across regions?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-16",
        "questionId": "q-23587",
        "question": "On GCP, design a beginner-friendly streaming pipeline to ingest 40 GB/day of JSON events from Pub/Sub into BigQuery in a single region. New angle: implement event-time processing with watermarks and a 10-minute late-arrival window to ensure late data is captured; partition BigQuery tables by day and fan-out to a derived per-session_aggregates table. Use a stateful, keyed DoFn (tenant_id, event_id) for idempotent ingestion. Include tests with synthetic late data and instrumentation of backlog, lag, and dedup rate?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Load per-tenant events from Pub/Sub into dedicated BigQuery datasets via a Dataflow streaming template; each tenant uses a separate table with backward-compatible schema evolution managed by a central",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a cost-aware, multi-tenant streaming pipeline: Pub/Sub feeds 1 TB/day of JSON events; Dataflow writes per-tenant, day-partitioned BigQuery tables (tenant_id, user_id). Run a shared Vertex AI an",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a Pub/Sub -> Dataflow -> BigQuery pipeline with separate partitioned tables per tenant, and a Cloud Run API exposing tenant-scoped datasets via authorized views. Enforce per-tenant IAM roles",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Dataflow streaming pipeline: Pub/Sub -> Dataflow -> BigQuery. Add event-time processing with watermarks and 10-minute lateness to handle late data; partition BigQuery tables by day; fan-out t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of streaming semantics (event-time, watermarks, late data) and how to combine ingestion with reliable analytics tables. It also probes stateful processing for idempotency and practical testing/observability.\n\n## Key Concepts\n- Event-time processing and watermarks\n- Late data handling with a bounded lateness window\n- Stateful DoFn for idempotent deduplication\n- BigQuery partitioning by day and derived tables for analytics\n- End-to-end testing with synthetic late data and metrics\n\n## Code Example\n```python\n# Skeleton: DoFn that deduplicates by (tenant_id, event_id)\nimport apache_beam as beam\n\nclass DedupDoFn(beam.DoFn):\n  def process(self, element, tenant_id=beam.DOUBLE, event_id=beam.DOUBLE, state=beam.DoFn.StateParam):\n    key = (tenant_id, event_id)\n    seen = state.read() or set()\n    if key not in seen:\n      seen.add(key)\n      state.write(seen)\n      yield element\n```\n\n## Follow-up Questions\n- How would you monitor late data volume and adjust the lateness window at run-time?\n- How would you test idempotency with out-of-order Pub/Sub messages in a staging environment?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-17",
        "questionId": "q-25063",
        "question": "On GCP, design a streaming ingestion pipeline for 2-5 TB/day of JSON user activity across tenants via Pub/Sub, validating against per-tenant dynamic schemas stored in Data Catalog, and materializing features into a tenant-scoped Vertex AI Feature Store. Expose sanitized aggregates to BigQuery for analytics while enforcing tenant isolation and data contracts; include schema evolution, drift detection, automated per-tenant rollback, and cost controls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest 50 GB/day via Pub/Sub to a single-region BigQuery RAW table, with per-tenant access through Authorized Views. New angle: CMEK for the dataset via Cloud KMS and a 90-day key rotation; attach row",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Dataflow streaming reads Pub/Sub, parses JSON, and upserts into per-tenant BigQuery tables using MERGE on event_id. A nightly batch loads backfilled events from GCS and upserts, deduplicating by event",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design the pipeline with Pub/Sub ingestion, Dataflow for streaming processing, dynamic per-tenant schemas registered in Data Catalog, and Vertex AI Feature Store for tenant-scoped features. Use BigQuery for sanitized ag…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Leverage Pub/Sub Schemas and a Cloud Spanner-backed schema registry; each event has a schema_version. Dataflow maps to a canonical row and writes to a tenant-partitioned BigQuery table. Enforce compat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes building a scalable, governed streaming pipeline with per-tenant schemas and ML-ready features, including drift detection and rollback capabilities.\n\n## Key Concepts\n- Pub/Sub, Dataflow, Apache Beam for streaming ingestion\n- Vertex AI Feature Store with per-tenant isolation\n- BigQuery access controls and BigLake external tables\n- Data Catalog for dynamic tenant schemas and data contracts\n- Schema evolution, data drift detection, automated rollback, and cost controls\n\n## Code Example\n```python\n# Example: Minimal Beam DoFn for dynamic schema validation per-tenant\nimport apache_beam as beam\nfrom google.cloud import datacatalog_v1\n\nclass ValidateAgainstTenantSchema(beam.DoFn):\n    def __init__(self, datacatalog_project):\n        self.datacatalog_project = datacatalog_project\n        self._schema_cache = {}\n    \n    def process(self, element, tenant_id):\n        schema = self._get_tenant_schema(tenant_id)\n        validated = self._validate(element, schema)\n        yield validated\n    \n    def _get_tenant_schema(self, tenant_id):\n        if tenant_id not in self._schema_cache:\n            lookup_client = datacatalog_v1.LookupEntryClient()\n            entry = lookup_client.lookup_entry(\n                linked_resource=f\"//datacatalog.googleapis.com/projects/{self.datacatalog_project}/locations/*/entryGroups/tenant_schemas/entries/{tenant_id}\"\n            )\n            self._schema_cache[tenant_id] = json.loads(entry.user_specified_system.metadata)\n        return self._schema_cache[tenant_id]\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-18",
        "questionId": "q-7824",
        "question": "On GCP, design a streaming data quality and observability pipeline for 5 TB/day of JSON events ingested from Pub/Sub across 3 regions. Build per-tenant quality gates, schema drift detection, and automated remediation triggers before data lands in BigQuery. Include how Data Catalog lineage, DLP masking, and monitoring integrate, and outline a rollback plan for corrupted batches?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a streaming quality and observability layer: ingest 5 TB/day from Pub/Sub into Dataflow, route by tenant to per-tenant BigQuery partitions, apply per-tenant schema validation and quality gates",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement a Dataflow (Beam) streaming job: Pub/Sub -> Dataflow -> BigQuery (partitioned by day). A validation DoFn checks required fields/types; valid events land in BigQuery, invalid ones are",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant datasets in BigQuery and two-region Dataflow pipelines with a unified Avro/JSON schema registry. Ingest with event_id for dedup, partition by ingestion_date, TTL per tenant (30/90 days)",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Dataflow streaming pipeline: Pub/Sub -> Dataflow -> BigQuery partitioned table for valid events; invalid events go to a DEAD-LETTER BigQuery table. Add a data-quality layer that validates eac",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical design of a streaming quality and observability layer, cross-region tenant isolation, and integration with Data Catalog and DLP, plus rollback semantics.\n\n## Key Concepts\n\n- Real-time data quality gates\n- Schema drift detection\n- Data masking and privacy\n- Data lineage and observability\n- Per-tenant backfill and rollback\n\n## Code Example\n\n```python\nfrom apache_beam import DoFn\n\nclass QualityGate(DoFn):\n    def process(self, element, *args, **kwargs):\n        tenant = element.get('tenant_id')\n        if not tenant:\n            return\n        if element.get('field_x') is None:\n            return\n        yield element\n```\n\n## Follow-up Questions\n\n- How would you test drift detection without impacting production data?\n- How would you scale the backfill across regions while ensuring exactly-once semantics?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-19",
        "questionId": "q-5605",
        "question": "In a multi-tenant, GCP-based data platform, each tenant's PII must be encrypted with a tenant-specific CMEK and only accessible within its project. Describe an end-to-end design using Pub/Sub ingestion, Dataflow streaming, BigQuery, Cloud KMS, and Data Catalog that enforces per-tenant encryption, field-level masking, and per-tenant access controls. Include data models, key management strategy, end-to-end lineage, testing, and a 24-hour rollback plan per tenant?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a schema registry in Data Catalog to serve as the single source of truth for all tenant schema definitions. Dataflow queries Data Catalog to apply tenant-specific schema validators, enforcing required fields,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Streaming pipeline: Pub/Sub → Dataflow → BigQuery across two regions with per-tenant isolation and field-level masking. Enable tenant-scoped schema evolution via versioned schemas in Data Catalog, plu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant CMEK in Cloud KMS (one key per tenant, scoped to its project). Dataflow reads Pub/Sub, masks PII via a policy store, then writes to BigQuery tables encrypted with CMEK. Data Catalog tag",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Set up a data-product ecosystem using Pub/Sub -> Dataflow -> Parquet in GCS; register schemas in Data Catalog with policy tags; auto-apply DLP masking on PII; export per-tenant external tables via Big",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of CMEK-per-tenant, streaming masking, lineage, and rollback.\n\n## Key Concepts\n- Tenant-level KMS keys and project scoping\n- Field-level masking in streaming\n- Data Catalog lineage and access governance\n- Point-in-time rollback for multi-tenant data\n\n## Code Example\n```python\n# Pseudo Dataflow-like sketch: read Pub/Sub, mask, write to CMEK-protected BigQuery\n```\n\n## Follow-up Questions\n- How to rotate tenant keys without downtime?\n- How to validate end-to-end privacy guarantees in production?\n- How would you enforce per-tenant isolation across Pub/Sub topics and BigQuery datasets?",
        "difficulty": "advanced"
      },
      {
        "id": "gcp-data-engineer-q-20",
        "questionId": "q-5281",
        "question": "Ingest 150 MB/day of JSON events via Pub/Sub into BigQuery in a single region. Build a beginner-friendly Dataflow pipeline that parses and validates required fields, enriches via a side-input mapping, and writes to a daily-partitioned BigQuery table using insertId for idempotence. Route invalid messages to a dead-letter Pub/Sub topic and archive originals to Cloud Storage. Maintain a streaming-aggregation table for daily active users and provide a 24-hour rollback plus basic monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Parse JSON events from Pub/Sub, validate required fields, enrich via a side-input mapping, and write to a daily-partitioned BigQuery table using insertId for idempotence. Route invalid messages to a d",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Pub/Sub to feed Dataflow streaming jobs that validate JSON schemas, mask PII via DLP, and emit per-tenant records to separate BigQuery datasets in two regions. Enforce IAM on a per-dataset basis,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Pub/Sub -> Dataflow for real-time feature extraction and windowing; query Vertex AI Online for per-event fraud risk; write results to per-tenant BigQuery datasets. IAM isolates tenants; maintain a mod",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement Dataflow with tenant-based routing, a Data Catalog-backed schema registry, per-tenant BigQuery partitions, and a separate aggregated view for Data Exchange. Enforce schemas at ingest, keep b",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to build a resilient, beginner-friendly streaming pipeline using common GCP primitives, with explicit data quality, enrichment, and observability requirements. It emphasizes idempotent writes, dead-letter handling, and rollback strategies—key for real-world data work.\n\n## Key Concepts\n- Dataflow (Beam) basic transforms: ParDo, DoFn, side inputs\n- Pub/Sub as source, BigQuery as sink (daily partitions)\n- Idempotent writes via insertId\n- Side-input enrichment\n- Dead-letter handling and Cloud Storage archival\n- Streaming aggregation in BigQuery\n- Rollback: replay from archive or Pub/Sub snapshot\n- Monitoring with Cloud Monitoring and Logging\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.options.pipeline_options import PipelineOptions\n\ndef parse_validate(elem):\n    # simple example\n    return {'user_id': elem['user_id'], 'event': elem['event']}\n\nclass AddTier(beam.DoFn):\n    def __init__(self, tier_map):\n        self.tier_map = tier_map\n    def process(self, elem):\n        user = elem['user_id']\n        elem['tier'] = self.tier_map.get(user, 'unknown')\n        yield elem\n\ndef run():\n    options = PipelineOptions(...)\n    tier_map = {'u1':'gold','u2':'silver'}\n    with beam.Pipeline(options=options) as p:\n        (p\n         | 'Read PubSub' >> beam.io.ReadFromPubSub(topic='projects/.../topics/events')\n         | 'Decode' >> beam.Map(lambda b: json.loads(b.decode('utf-8')))\n         | 'Validate' >> beam.Filter(lambda r: 'user_id' in r and 'event' in r)\n         | 'Enrich' >> beam.ParDo(AddTier(tier_map))\n         | 'WriteBigQuery' >> beam.io.WriteToBigQuery(...)\n        )\n```\n\n## Follow-up Questions\n- How to test idempotence across restarts?\n- How to handle late data and out-of-order events?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-21",
        "questionId": "q-27931",
        "question": "On GCP, design a streaming observability and governance stack for 2-4 TB/day of device telemetry ingested via Pub/Sub. Implement per-tenant quality gates, schema drift detection, and automated impact analysis for downstream data products in BigQuery and Parquet in Cloud Storage, with per-tenant contracts in Data Catalog and controlled sharing. Include 24h per-tenant rollback, testing/chaos plan, and alerting in Cloud Monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: Dataflow ingest with exactly-once; tenant quality gates (null rate, drift) as streaming metrics; snapshots in Cloud Storage for 24h rollback via replay; Data Catalog for contracts and lineag",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a single-region Dataflow (Beam) job to parse Pub/Sub JSON; enforce the schema (user_id: string, event_time: string ISO8601, event_type: string) and drop extra fields. Write valid records to a dail",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest JSON events through Pub/Sub and process with Dataflow using per-tenant schemas from a centralized registry. Quarantine malformed records to Cloud Storage, write validated data to tenant-specific BigQuery datasets…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage per-tenant Vertex AI Feature Store in their region, a Dataflow streaming pipeline feeding a Vertex AI Endpoint with autoscaling, and Pub/Sub ingestion. Emit only anomalies to a shared BigQuer",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes practical GCP data governance, observability, and rollback strategies across multi-tenant pipelines, balancing safety, cost, and latency.\n\n## Key Concepts\n- Streaming ETL with Dataflow and Pub/Sub\n- Per-tenant governance: Data Catalog contracts, lineage, row-level security\n- Schema drift detection and quality gates\n- Rollback via point-in-time snapshots and replay\n- Observability with Cloud Monitoring and chaos testing\n\n## Code Example\n```javascript\n// Not required in interview; concept spot\n```\n\n## Follow-up Questions\n- How would you implement automated contract updates without breaking tenants?\n- How would you scale quality gates as tenants grow?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-22",
        "questionId": "q-19956",
        "question": "On GCP, design a real-time fraud-detection pipeline for merchant events streaming via Pub/Sub, using Dataflow for feature extraction and windowing, Vertex AI online prediction for inference, and per-tenant storage in BigQuery with dataset-level isolation. Describe latency targets, feature contracts, model versioning, drift monitoring, backfill, testing, and rollback strategies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest 5 TB/day of JSON events via Pub/Sub into Dataflow, applying per-tenant windows for isolation. Materialize features into Vertex AI Feature Store, serve online predictions using tenant-scoped model shards in Vertex…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Pub/Sub -> Dataflow for real-time feature extraction and windowing; query Vertex AI Online for per-event fraud risk; write results to per-tenant BigQuery datasets. IAM isolates tenants; maintain a mod",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Approach: Dataflow ingest with exactly-once; tenant quality gates (null rate, drift) as streaming metrics; snapshots in Cloud Storage for 24h rollback via replay; Data Catalog for contracts and lineag",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a two-region GCP data fabric enforcing per-tenant differential privacy budgets for 2-5 TB/day telemetry via Pub/Sub. Streaming Dataflow computes DP-protected aggregates, writes Parquet to Cloud",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests end-to-end streaming design, tenant isolation, and ML model integration in production.\n\n## Key Concepts\n- Real-time streaming: Pub/Sub, Dataflow, windowing\n- ML inference: Vertex AI Online predictions\n- Multi-tenant data governance: per-tenant BigQuery datasets and IAM\n- ML ops: model registry, versioning, drift detection, rollback, backfill\n\n## Code Example\n```python\n# pseudo drift monitor sketch\nclass DriftMonitor:\n    def update(self, features):\n        pass\n```\n\n## Follow-up Questions\n- How would you validate latency under burst traffic? \n- How would you rollback a model version without data loss?",
        "difficulty": "intermediate"
      },
      {
        "id": "gcp-data-engineer-q-23",
        "questionId": "q-27838",
        "question": "On GCP, design a beginner-friendly pipeline to ingest 50 GB/day of JSON clickstream events via Pub/Sub into BigQuery in a single region. NEW ANGLE: compute per-tenant event_volume by counting events in a 5-minute sliding window with 20 minutes allowed lateness, and write results to a separate BigQuery analytics table; outline how to implement windowing and triggers in Dataflow, how to schema-match to BigQuery, testing with synthetic late events, and monitor watermark lag and late-data rate?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Explain enabling Pub/Sub Schemas, a Python Dataflow DoFn that validates fields (tenantId, eventId, ts, url), types, and rejects invalid messages to a Cloud Storage manifest; discuss how to handle opti",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a Dataflow streaming job keyed by tenant_id; redact PII fields via a DoFn using CMEK-protected keys, emitting masked JSON to a per-tenant partitioned BigQuery table. Enforce per-tenant access wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed solution: a Dataflow (Beam) pipeline that consumes Pub/Sub JSON, applies a simple schema map in a DoFn, writes to a partitioned BigQuery table (ingestion_time) and, in parallel, a nightly bat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy a streaming Dataflow job: read from Pub/Sub as JSON, extract tenant_id and event timestamp, assign event-time watermark based on ts, apply SlidingWindows.of(Duration.standardMinutes(5)).withAllowedLateness(Durati…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of streaming windowing, late data handling, and integration between Pub/Sub, Dataflow, and BigQuery. It emphasizes concrete implementation choices over theoretical knowledge.\n\n## Key Concepts\n- **Dataflow streaming windowing**: SlidingWindows with configurable lateness tolerance\n- **Event-time processing**: Watermark-based progress tracking and late data handling\n- **BigQuery schema design**: Partitioned and clustered tables for analytics workloads\n- **Observability**: Monitoring watermark lag and late-data rates to ensure SLAs\n\n## Code Example\n```python\nimport apache_beam as beam\nfrom apache_beam.transforms.window import SlidingWindows, Sessions\nfrom apache_beam.options.pipeline_options import StandardOptions\nfrom apache_beam.io.gcp.bigquery import WriteToBigQuery\n\nclass ParseEvent(beam.DoFn):\n    def process(self, element):\n        import json\n        record = json.loads(element.decode('utf-8'))\n        yield beam.window.TimestampedValue({\n            'tenant_id': record['tenant_id'],\n            'event_time': record['ts']\n        }, record['ts'])\n\nclass CountPerTenant(beam.DoFn):\n    def process(self, element, window=beam.DoFn.WindowParam):\n        tenant_id, events = element\n        yield {\n            'tenant_id': tenant_id,\n            'window_start': window.start.to_utc_datetime().isoformat(),\n            'window_end': window.end.to_utc_datetime().isoformat(),\n            'event_volume': sum(1 for _ in events)\n        }\n\npipeline_options = PipelineOptions(['--streaming'])\noptions = pipeline_options.view_as(StandardOptions)\n\nwith beam.Pipeline(options=options) as p:\n    (p\n        | 'ReadFromPubSub' >> beam.io.ReadFromPubSub(topic='projects/my-project/topics/clickstream')\n        | 'ParseEvents' >> beam.ParDo(ParseEvent())\n        | 'ExtractTenant' >> beam.Map(lambda x: (x['tenant_id'], x))\n        | 'SlidingWindow' >> beam.WindowInto(\n            SlidingWindows(5 * 60, 1 * 60),\n            allowed_lateness=20 * 60,\n            accumulation_mode=ACCUMULATING\n        )\n        | 'GroupByTenant' >> beam.GroupByKey()\n        | 'CountEvents' >> beam.ParDo(CountPerTenant())\n        | 'WriteToBigQuery' >> WriteToBigQuery(\n            'my-project:analytics.tenant_event_volume',\n            schema='tenant_id:STRING, window_start:TIMESTAMP, window_end:TIMESTAMP, event_volume:INTEGER'\n        )\n    )\n```\n\n## Schema Matching to BigQuery\n- Partition the analytics table by `window_start` for efficient time-range queries\n- Cluster by `tenant_id` for fast per-tenant aggregations\n- Use INT64 for event_volume; avoid FLOAT to prevent rounding issues\n\n## Testing with Synthetic Late Events\n```python\ndef test_late_data_handling():\n    import unittest\n    from apache_beam.testing.test_pipeline import TestPipeline\n    from apache_beam.testing.util import assert_that, equal_to\n    \n    events = [\n        {'tenant_id': 't1', 'ts': 0},      # on-time\n        {'tenant_id': 't1', 'ts': 100},    # on-time (within window)\n        {'tenant_id': 't1', 'ts': 1500},   # late (beyond 20-min window)\n    ]\n    \n    with TestPipeline() as p:\n        results = (\n            p\n            | beam.Create(events)\n            | 'AddTimestamp' >> beam.Map(\n                lambda x: beam.window.TimestampedValue(x, x['ts'])\n            )\n            | 'Window' >> beam.WindowInto(\n                SlidingWindows(5 * 60, 1 * 60),\n                allowed_lateness=20 * 60,\n                trigger=AfterWatermark(early=AfterCount(1)),\n                accumulation_mode=DISCARDING\n            )\n            | 'Count' >> beam.combiners.Count.Globally()\n        )\n        assert_that(results, equal_to([2]))  # Late event should be dropped\n```\n\n## Monitoring Watermark Lag and Late-Data Rate\n- **Cloud Monitoring metrics**: Track `dataflow.googleapis.com/job/streamgen/watermark_delay` per worker\n- **Late-data metrics**: Monitor `dataflow.googleapis.com/job/streamgen/elements_late`; aim for <5% late ratio\n- **Custom metrics**: Use `DataflowIO.write_statistics` to emit per-tenant late counts via `beam.Metrics.counter`\n- **Alerting**: Set alerts on watermark lag >15 minutes or late-data rate >10%\n\n## Follow-up Questions\n- How would you adjust the pipeline to handle variable event volumes per tenant using session windows instead of fixed sliding windows?\n- What trade-offs would you consider when choosing between accumulated and discarded window modes?\n- How would you implement exactly-once delivery semantics for the BigQuery writes?",
        "difficulty": "beginner"
      },
      {
        "id": "gcp-data-engineer-q-24",
        "questionId": "q-19017",
        "question": "On GCP, design a streaming analytics pipeline to ingest 5-10 TB/day of JSON events from Pub/Sub, routing per-tenant data into partitioned BigQuery datasets. Implement tenant-specific data masking, policy-driven by Data Catalog tags, and maintain per-tenant isolation. Include per-tenant 24h rollback via BigQuery time travel, per-tenant lifecycle (TTL/table expiration), and an automated test and rollback plan for data drift and failures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a single Dataflow (Beam) job that parses Pub/Sub JSON, validates required fields (userId, timestamp, eventType), and routes events to two BigQuery tables (clicks and impressions) with separate",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a streaming Dataflow pipeline to ingest Pub/Sub events, consult Vertex AI Feature Store for per-tenant anomaly scores, and write results to per-tenant BigQuery tables. Enforce per-tenant access control via Authorize…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a GCP pipeline with per-tenant isolation (device_group) and <5s end-to-end latency: Pub/Sub ingestion, Dataflow (Beam) enrichment, Vertex AI online inference for scoring, and write to BigQuery",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a streaming pipeline with Pub/Sub to Dataflow, routing each tenant's data into a separate, partitioned BigQuery dataset. Apply tenant-specific masking in Dataflow via policy-driven UDFs, store",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes real-world multi-tenant streaming design, privacy controls, data contracts, and rollback strategies on GCP. It tests understanding of per-tenant isolation, lifecycle management, and operational testing.\n\n## Key Concepts\n- Pub/Sub streaming, Dataflow, BigQuery partitioning\n- Data masking policies and Data Catalog\n- Time travel / TTLs for tenant data\n- Testing, rollback, drift handling\n\n## Code Example\n```javascript\n// Example Dataflow mask function sketch\nfunction maskFields(record, policy){ /*...*/ }\n```\n\n## Follow-up Questions\n- How would you test per-tenant masking without leaking data?\n- How would you automate TTL changes per tenant as their contracts evolve?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-12T10:11:20.756Z",
    "version": 1
  }
]