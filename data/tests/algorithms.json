[
  {
    "id": "test-algorithms",
    "channelId": "algorithms",
    "channelName": "Algorithms",
    "title": "Algorithms Knowledge Test",
    "description": "Test your understanding of Algorithms with 25 questions.",
    "questions": [
      {
        "id": "algorithms-q-0",
        "questionId": "q-23007",
        "question": "Given a directed, non-negative weighted graph G modeling a road network, implement routePlanner(G, s, t) that returns: shortestTime via Dijkstra from s to t; minHops via BFS from s to t on the unweighted graph; if G is a DAG, earliestArrival times from s to all nodes using topological order; if a cycle exists, report it and skip the DAG part?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Trie where each node tracks endCount (words ending here) and subCount (words in its subtree). On insert, increment subCount along the path and endCount at the last node. On delete, decrement. co",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Tarjan's algorithm to compute strongly connected components and condense the graph into a DAG. For each SCC, run Dijkstra from all boundary vertices (those with edges entering or exiting the component) to compute op…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Dijkstra's algorithm to find the shortest weighted path from s to t. Use BFS on the unweighted graph to find the minimum hop path from s to t. Use DFS to detect cycles; if no cycle exists, perform topological sortin…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a self-balancing AVL tree storing integers with per-node count and subtree sum. The insert(x) operation either increments cnt if the key exists, or inserts a new node. The erase(x) operation either decrements…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests proficiency with fundamental graph algorithms—BFS, DFS, Dijkstra's algorithm, and topological sort-based dynamic programming—in a single cohesive task. It also evaluates understanding of when to apply each algorithm based on graph properties.\n\n## Key Concepts\n- **BFS**: Shortest path in unweighted graphs (minimum hops)\n- **DFS**: Cycle detection\n- **Dijkstra's algorithm**: Weighted shortest path\n- **Topological sort**: DAG-based DP for earliest arrival computation\n\n## Solution Approach\n1. Run Dijkstra's algorithm from s to compute shortestTime to t\n2. Run BFS from s on the unweighted graph to compute minHops to t\n3. Run DFS to detect cycles; if found, report and skip DAG computation\n4. If acyclic, compute topological order and calculate earliestArrival using DP along that order",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-1",
        "questionId": "q-18308",
        "question": "Given a string s and a dictionary of words dict, count how many different ways to segment s into a sequence of dictionary words. Return the count modulo 1e9+7. Provide a top-down memoized solution and a bottom-up tabulation sketch. Assume dict lookup is O(1) via a hash set and that n = len(s) up to 500?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Bottom-up: dp[i][k] = min_{j< i} dp[j][k-1] + max(a[j+1..i]), base dp[0][0]=0, others INF. Top-down: f(i,k) memoized, iterates j from k-1 to i-1 while tracking max(a[j+1..i]). Edge cases: if N==0 retu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "We maintain two DP layers: dp0[i][j] represents the minimum path sum to cell (i,j) without using the cell removal, while dp1[i][j] represents the minimum path sum when the removal has already been used. Initialize dp0[0…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define dp[0]=1. For i from 1..n, maintain a left index so that sum(a[left..i-1]) ≤ B; then dp[i] = sum(dp[left..i-1]). Use a sliding window to update both sum and left, yielding O(n) time and O(n) spa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Top-down: define dfs(i) as ways to segment s[i:]; if i==n return 1; otherwise sum dfs(j) for every j>i with s[i:j] in dict; use memo[i] to cache. Bottom-up: dp[n]=1; for i from n-1 to 0, for j from i+",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThe problem tests basic DP on strings, counting segmentations, reinforcing memoization vs tabulation, handling modulo, and performance with s length up to 500. It also touches dictionary lookup optimization.\n\n## Key Concepts\n\n- Top-down memoization with index-based DP\n- Bottom-up tabulation\n- Modulo arithmetic and time/space complexity\n\n## Code Example\n\n```python\nMOD = 10**9+7\n\ndef count_ways(s, words):\n    wordset = set(words)\n    n = len(s)\n    memo = [-1]*(n+1)\n    def dfs(i):\n        if i==n: return 1\n        if memo[i]!=-1: return memo[i]\n        res = 0\n        for j in range(i+1, n+1):\n            if s[i:j] in wordset:\n                res = (res + dfs(j)) % MOD\n        memo[i] = res\n        return res\n    return dfs(0)\n\ndef count_ways_tab(s, words):\n    wordset = set(words)\n    n = len(s)\n    dp = [0]*(n+1)\n    dp[n] = 1\n    for i in range(n-1, -1, -1):\n        for j in range(i+1, n+1):\n            if s[i:j] in wordset:\n                dp[i] = (dp[i] + dp[j]) % MOD\n    return dp[0]\n```\n\n## Follow-up Questions\n\n- How would you optimize for very large dictionaries?\n- Extend to return all segmentations or limit by number of words?",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-2",
        "questionId": "q-2121",
        "question": "How would you implement a load balancer that uses predictive scaling based on request patterns and historical data, and what machine learning techniques would you use to forecast traffic spikes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a predictive load balancer using time series forecasting models like ARIMA or LSTM to analyze historical request patterns, then proactively scale server capacity and adjust distribution algorithms.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a hash map mapping `player_id` to a per-player record containing `{sum, deque of (ts, delta)}`. On `addEvent(pid, d, ts)`: push `(ts, d)` onto the deque, remove expired entries where `front.ts < ts - W`, adjust `sum…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A practical hybrid in-place sort would use 3-way partition quicksort with median-of-three pivot, and a low insertion-sort threshold. Implement iteratively using a manual stack to avoid recursive depth",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Fibonacci-like DP with obstacles: dp[i] = 0 if step i is blocked; otherwise dp[i] = dp[i-1] + dp[i-2] (mod 1e9+7), with dp[0]=1. Base cases: dp[0]=1 (one way to stay at start), dp[1]=0 if blocked else 1. Time O(N)…",
            "isCorrect": false
          }
        ],
        "explanation": "To implement a predictive load balancer, I would first collect historical metrics including request rates, response times, server utilization, and seasonal patterns. The system would use time series forecasting models like ARIMA for simpler patterns or LSTM neural networks for complex, non-linear traffic patterns. These models would predict traffic volumes for the next 15-60 minutes with confidence intervals.\n\nThe load balancer would then adjust its distribution strategy based on these predictions. For predicted high-traffic periods, it would pre-warm additional servers and shift from round-robin to weighted distribution algorithms that account for predicted server load. During low-traffic periods, it would consolidate requests to fewer servers to optimize resource utilization.\n\nKey machine learning techniques would include:\n\n1. **Time Series Analysis**: ARIMA models for capturing trend and seasonality in predictable traffic patterns\n2. **Deep Learning**: LSTM networks for modeling complex, non-linear relationships in traffic data\n3. **Anomaly Detection**: Isolation Forest or Autoencoders to identify unusual traffic spikes that deviate from patterns\n4. **Ensemble Methods**: Combining multiple models to improve prediction accuracy\n\nThe system would continuously retrain models with new data and implement a feedback loop where actual traffic patterns are compared against predictions to improve model accuracy over time.",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-3",
        "questionId": "q-21140",
        "question": "Count the number of distinct shortest paths from s to t in a directed weighted graph with non-negative weights. Describe and implement an algorithm that uses Dijkstra to compute dist[], then builds a shortest-path DAG (edge (u,v) included if dist[u] + w(u,v) == dist[v]), and finally uses DP in topological order to count paths from s to t modulo 1000000007. Include complexity and edge cases?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: per-user deque of (ts, score) plus a running sum; a hashtable maps user_id -> {queue, sum}. addEvent pushes to queue and increments sum. expire as currentTime advances: pop front while ts <=",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Compute prefix sums to allow O(1) range sums. Let dp[i][j] be the minimum cost to merge stones[i..j] into one pile. Base: if i == j, cost 0. For length L from 2 to n, for all i with j = i+L-1, set dp[",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Dijkstra from s to compute dist[]. Build a DAG with edges (u,v) where dist[u] + w == dist[v]. Then DP in topological order: ways[s]=1; for nodes in nondecreasing dist, propagate to DAG neighbors:",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a topological order to linearize the DAG. dist[s]=0, dist[others]=INF. For u in topo order, for each edge (u,v) with weight w, if dist[u]+w < dist[v], update dist[v]. The result is the earliest in",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to combine weighted shortest paths with counting paths on a DAG derived from shortest edges, ensuring correctness with multiple shortest routes.\n\n## Key Concepts\n- Shortest-path DAG\n- Topological order derived from dist[]\n- Modulo path counting\n- Edge cases: unreachable t, multiple edges\n\n## Code Example\n```python\nimport heapq\n\ndef count_shortest_paths(n, adj, s, t):\n    MOD = 10**9+7\n    INF = 10**18\n    dist = [INF]*n\n    dist[s] = 0\n    pq = [(0, s)]\n    while pq:\n        d,u = heapq.heappop(pq)\n        if d != dist[u]:\n            continue\n        for v,w in adj[u]:\n            nd = d + w\n            if nd < dist[v]:\n                dist[v] = nd\n                heapq.heappush(pq, (nd, v))\n    dag = [[] for _ in range(n)]\n    for u in range(n):\n        if dist[u] == INF: continue\n        for v,w in adj[u]:\n            if dist[u] + w == dist[v]:\n                dag[u].append(v)\n    order = sorted(range(n), key=lambda x: dist[x])\n    ways = [0]*n\n    ways[s] = 1\n    for u in order:\n        for v in dag[u]:\n            ways[v] = (ways[v] + ways[u]) % MOD\n    return ways[t]\n```\n\n## Follow-up Questions\n- How would you reconstruct a concrete shortest path after this DP?\n- How would you adapt for counting from multiple sources to a single sink?",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-4",
        "questionId": "q-10744",
        "question": "Design a mutable sequence analytics structure SequenceAnalytics with operations:\n- append(v)\n- insertAt(i, v)\n- removeAt(i)\n- topKByFrequency(k)\n- topKInLastWindow(k, w)\nYour design should use an array for the sequence, a doubly linked list to support efficient insertions/removals, a hashtable mapping value->frequency, and a max-heap to extract top-K values. Ensure updates keep O(log n) and handle expirations for the last-window queries as new elements arrive?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Compute in a single pass over a topological order: dist[u] = shortest distance from s; cnt[u] = number of shortest s→u paths; par[u] = one predecessor for path reconstruction. Relax edges in topo orde",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Compute a topological order for the A-edges and perform DAG DP to obtain distA. Construct a layered graph with K+1 layers. For each A-edge u→v, add (u,k)→(v,k) with cost 1; for each B-edge u→v,w, add (u,k)→(v,k+1) with…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "The SequenceAnalytics structure employs a hybrid approach: an array provides indexed storage, a doubly linked list enables O(1) insertions and removals, a hashmap maintains value-to-frequency mappings, and a max-heap st…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "On a weighted DAG, compute shortest paths from s by topological sorting the nodes and relaxing outgoing edges in that order. Initialize dist[s]=0, dist[others]=INF. For each u in topo order, if dist[u",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to combine multiple data structures (arrays, linked lists, hashmaps, and heaps) to build an efficient mutable sequence analytics system with top-K queries and sliding-window expiration management.\n\n## Key Concepts\n- Hybrid data structure design combining array storage with linked list operations\n- Hashmap-based frequency tracking for O(1) count updates\n- Max-heap implementation with lazy deletion for amortized O(log n) top-K operations\n- Sliding window management with timestamp-based expiration\n\n## Code Example\n```javascript\nclass SequenceAnalytics {\n  append(v) { /* implementation */ }\n  insertAt(i, v) { /* implementation */ }\n  removeAt(i) { /* implementation */ }\n  topKByFrequency(k) { /* implementation */ }\n  topKInLastWindow(k, w) { /* implementation */ }\n}\n```\n\n## Follow-up Questions\n- How would you adapt the structure for dynamic k values in top-K queries?\n- What are the memory usage implications and what optimization strategies would you consider?\n- How would the design change if you needed to support range queries in addition to top-K operations?",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-5",
        "questionId": "q-8860",
        "question": "Design a data structure to process a stream of events with fields (user_id, action, score, ts). Requirements: (1) deduplicate the latest (user_id, action) within a sliding window W seconds, (2) maintain each user's total score from non-expired events, (3) support retrieving the top-K users by current total score in O(log K) time per update, with efficient expirations. Specify data structures (arrays, linked lists, hashtables, heap) and analyze time/memory?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a hashmap to track the latest timestamp for each (user_id, action) pair to deduplicate within window W. Maintain per-user total scores in another hashmap and store per-user event lists (linked lists) for efficient e…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a Trie where terminal nodes store each word's weight. insertWord(word, weight) traverses/creates nodes along the character path and sets endWeight = weight at the terminal node; deleteWord(word) traverses to the ter…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use topological sort with BFS to process tasks level by level. The minimum time equals the length of the longest path (critical path) in the DAG. O(V+E) time, O(V) space. Track completion times using DP: dp[node] = max(…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use BFS with a queue to explore the graph level by level, tracking visited nodes and distances from the start. Return the distance when the target node is found, or -1 if all reachable nodes are exhausted without findin…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis problem tests the integration of fundamental data structures—arrays, linked lists, hashmaps, and heaps—to solve a streaming scenario with sliding window deduplication and real-time ranking.\n\n## Key Concepts\n- Sliding window deduplication per (user_id, action) using a hashmap with timestamp tracking\n- Per-user score aggregation maintained in a hashmap for O(1) updates\n- Global top-K ranking via max-heap with O(log K) insertion/deletion and lazy invalidation\n- Efficient expiration handling using linked lists and amortized cleanup strategies\n\n## Code Example\n```javascript\nclass EventProcessor {\n  constructor(W) {\n    this.W = W;\n    this.latestByUserAction = new Map(); // (user_id,action) -> timestamp\n    this.userTotals = new Map(); // user_id -> total_score\n    this.userEvents = new Map(); // user_id -> linked list of (timestamp, score)\n    this.maxHeap = new MaxHeap(); // (total_score, user_id)\n  }\n  \n  addEvent(event) {\n    const key = `${event.user_id}:${event.action}`;\n    const latest = this.latestByUserAction.get(key);\n    \n    if (!latest || event.ts - latest > this.W) {\n      // Update dedup map\n      this.latestByUserAction.set(key, event.ts);\n      \n      // Update user total\n      const currentTotal = this.userTotals.get(event.user_id) || 0;\n      const newTotal = currentTotal + event.score;\n      this.userTotals.set(event.user_id, newTotal);\n      \n      // Add to user's event list\n      if (!this.userEvents.has(event.user_id)) {\n        this.userEvents.set(event.user_id, new LinkedList());\n      }\n      this.userEvents.get(event.user_id).push({ts: event.ts, score: event.score});\n      \n      // Update heap\n      this.maxHeap.insert({score: newTotal, user: event.user_id});\n    }\n  }\n  \n  getTopK(k, now) {\n    this.expire(now);\n    return this.maxHeap.getTopK(k);\n  }\n  \n  expire(now) {\n    for (const [userId, events] of this.userEvents) {\n      while (events.length > 0 && now - events.head.ts > this.W) {\n        const expired = events.shift();\n        const currentTotal = this.userTotals.get(userId);\n        const newTotal = currentTotal - expired.score;\n        this.userTotals.set(userId, newTotal);\n        \n        if (newTotal > 0) {\n          this.maxHeap.insert({score: newTotal, user: userId});\n        }\n      }\n    }\n  }\n}\n```\n\n## Complexity Analysis\n**Time:** O(1) amortized per event for updates, O(log K) for top-K retrieval\n**Space:** O(N) where N is number of active users plus events within window W",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-6",
        "questionId": "q-627",
        "question": "Explain the difference between round-robin and weighted round-robin load balancing algorithms. When would you choose one over the other?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Round-robin distributes requests evenly across servers, while weighted round-robin assigns more requests to servers with higher capacity based on configured weights.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Expanded-state Dijkstra on (node, used). For each edge (u→v,w), relax to (v,used) with cost +w; if used < K, also relax to (v,used+1) with cost +0. Use a min-heap priority queue and store parent pointers for path recons…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an adaptive load balancer that monitors server metrics like CPU, memory, and response times, then uses a weighted algorithm that dynamically updates server weights based on performance trend",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Compute prefix sums to allow O(1) range sums. Let dp[i][j] be the minimum cost to merge stones[i..j] into one pile. Base: if i == j, cost 0. For length L from 2 to n, for all i with j = i+L-1, set dp[",
            "isCorrect": false
          }
        ],
        "explanation": "Round-robin load balancing is the simplest algorithm where requests are distributed sequentially to each server in the pool, cycling back to the first server after reaching the last. This works well when all servers have equal capacity and processing power. However, in real-world scenarios, servers often have different specifications - some might have more CPU, RAM, or faster network connections.\n\nWeighted round-robin addresses this limitation by assigning a weight to each server, representing its relative capacity. A server with weight 3 receives three times more requests than a server with weight 1. For example, if you have three servers with weights 5, 3, and 2, out of every 10 requests, 5 go to the first server, 3 to the second, and 2 to the third.\n\nChoose round-robin for homogeneous server environments where all nodes have identical hardware and performance characteristics. Use weighted round-robin in heterogeneous environments with varying server capacities, when upgrading infrastructure gradually, or when you want to control traffic distribution during maintenance or testing phases. Major cloud providers like AWS, Google Cloud, and Azure implement both algorithms in their load balancers.",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-7",
        "questionId": "q-20028",
        "question": "Design a dynamic autocomplete engine using a Trie where each node stores the top-K most frequent words sharing that prefix. Implement addWord(word, delta) to adjust a word’s frequency (delta may be negative) and getTopK(prefix, K) to return up to K words with highest frequency for that prefix, breaking ties lexicographically. Handle deletions when freq reaches 0 and discuss time/memory trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an in-place, stable sort with O(1) extra space and O(n log n) worst-case time. Implement a bottom-up in-place mergesort that uses a rotation-based merge to combine adjacent runs. Use block rot",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Trie where each node stores a min-heap of up to K (frequency, word) pairs, maintaining efficient top-K retrieval by negating frequencies. Keep a global frequency map for all words. On addWord(word, delta), update…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a hash ring with TreeMap/SortedMap for O(log n) lookups, implement virtual nodes to distribute load evenly, and only remap keys between adjacent servers when topology changes.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run Dijkstra's algorithm from s to compute dist[], the shortest distance to all nodes. Build a shortest-path DAG H consisting of edges (u, v) where dist[u] + w(u, v) == dist[v]. Since all weights are positive, this form…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical skills in per-prefix top-K maintenance using a Trie with bounded heaps and dynamic frequency updates, revealing trade-offs between speed and memory.\n\n## Key Concepts\n- Trie with per-node top-K storage using a min-heap with bounded size K\n- Min-heap with tie-breaking by word for stable ordering\n- Dynamic frequency updates with addWord(word, delta) supporting negative deltas\n- Automatic deletion when frequency reaches zero\n\n## Time and Memory Trade-offs\n- **Time Complexity**: addWord runs in O(L × log K) where L is word length, as we traverse the path and perform heap operations. getTopK runs in O(K × log K) for heap extraction plus O(K log K) for sorting.\n- **Space Complexity**: O(N × L + N × K) where N is the total number of unique words—the Trie itself costs O(N × L), and each node stores up to K heap entries.\n- **Trade-off**: Increasing K improves result quality but increases memory usage and slows updates. A smaller K is faster but returns fewer suggestions.\n\n## Code Example\n```javascript\nclass TrieNode {\n  constructor() {\n    this.children = new Map();\n    this.topK = new MinHeap((a, b) => {\n      if (a.freq !== b.freq) return a.freq - b.freq;\n      return a.word.localeCompare(b.word);\n    });\n  }\n}\n\nclass AutocompleteEngine {\n  constructor(K = 3) {\n    this.root = new TrieNode();\n    this.freqMap = new Map();\n    this.K = K;\n  }\n\n  addWord(word, delta) {\n    const newFreq = (this.freqMap.get(word) || 0) + delta;\n    if (newFreq <= 0) {\n      this.freqMap.delete(word);\n    } else {\n      this.freqMap.set(word, newFreq);\n    }\n\n    let node = this.root;\n    for (const char of word) {\n      if (!node.children.has(char)) {\n        node.children.set(char, new TrieNode());\n      }\n      node = node.children.get(char);\n      node.topK.upsert({ word, freq: newFreq }, this.K);\n    }\n  }\n\n  getTopK(prefix, K) {\n    let node = this.root;\n    for (const char of prefix) {\n      if (!node.children.has(char)) return [];\n      node = node.children.get(char);\n    }\n    return node.topK.getTop(K);\n  }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-8",
        "questionId": "al-3",
        "question": "What is Dynamic Programming and how does it differ from plain recursion? When would you choose one over the other?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a 3-way partition scheme with three pointers: lt (less-than boundary), i (current element), and gt (greater-than boundary). First, select a pivot using either random selection or median-of-three. Then, while i <= gt…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Maintain a circular array of W buckets; each bucket stores per-symbol sums for events in that second. A hashmap maps symbol -> {totPV, totV, VWAP}. On addEvent(symbol, price, volume, ts): update bucke",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Perform BFS from s on unblocked nodes. Maintain dist[] initialized to INF and ways[] initialized to 0. Set dist[s]=0, ways[s]=1. For each edge v->u: skip if u blocked. If dist[u] is INF, set dist[u]=d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "- Dynamic Programming (DP) caches subproblem results to avoid recomputation, turning exponential recursion into polynomial time. - Styles: top-down memoization and bottom-up tabulation. - Core idea: DP trades extra memo…",
            "isCorrect": true
          }
        ],
        "explanation": "DP reduces time by caching subproblem results; however, it uses extra memory for the cache or table. Top-down memoization is quick to adapt to existing recursive solutions, while bottom-up tabulation often yields better cache locality and avoids recursion overhead. Edge cases: very large n with tight memory may require space optimization or even choosing plain recursion if depth is small and overhead cancels benefits. Space-time trade-offs are problem-specific; for some recurrences you can achieve O(n) time and O(1) space with clever iteration.",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-9",
        "questionId": "q-11121",
        "question": "Scenario: DNA sequence S and motif T. How many distinct ways can T occur as a subsequence of S, modulo 1e9+7? Provide a bottom-up DP using O(|S|·|T|) time and O(|T|) space plus a memoized top-down variant. Consider empty T, empty S, and repeated nucleotides in T?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Kahn’s topological sort to obtain an order. Build adjacency and in-degree, set finish[i] = durations[i] for all i. For each node u in topo order, for each v in adj[u], set finish[v] = max(finish[v",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Trie: each node has 26 children and an isWord flag. insert(w) walks/creates nodes for each char and marks the last node. search(w) returns true if every node exists and the final node isWord. st",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an introsort-style hybrid: quicksort with median-of-three pivot and a recursion depth limit D = 2*floor(log2(n)). If depth exceeds D on a subarray, switch to an in-place mergesort for that r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bottom-up: maintain a 1D dp of length m+1 with dp[0]=1. For each char c in S, iterate j from m down to 1 and if c==T[j-1], set dp[j]=(dp[j]+dp[j-1])%MOD. Return dp[m]. Time O(nm), space O(m). Top-down",
            "isCorrect": true
          }
        ],
        "explanation": "Two valid DP strategies for counting subsequences with modulo arithmetic. Bottom-up uses a rolling array to save space; top-down demonstrates memoization and base cases. Edge cases: repeated chars and empty strings are handled by modulo and base rules.",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-10",
        "questionId": "q-13021",
        "question": "Given an array a1..an (n up to 2e5) and an integer k, partition into exactly k non-empty contiguous segments. The cost of a segment [l..r] is (sum(l..r))^2. Design an O(k n log C) DP using a Li Chao segment tree to minimize total cost. Explain the initialization, recurrence dp[k][j] = min_i dp[k-1][i] + (S[j]-S[i])^2, how lines L_i(x) = (dp[k-1][i]+S[i]^2) + (-2 S[i]) x are managed, and discuss potential pitfalls in practice?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a hashmap gid -> {dq: Deque of (ts, value), sum, count, ver}; a max-heap stores entries {avg, gid, ver}. On addEvent(ts, gid, val): push (ts,val) into dq, update sum/count, evict old events (ts <",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design and implement a hybrid sorter for an array of N integers with many duplicates. Start with 3-way partition quicksort to minimize swaps and efficiently handle repeated values. Track recursion depth and partition si…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Consistent hashing maps both servers and requests to a circular hash space, minimizing key remapping. It provides better distribution and reduces disruption when scaling compared to modulo hashing.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Formulate dp[k][j] = min_{i<j} dp[k-1][i] + (S[j]-S[i])^2, where S is prefix sums. Use a Li Chao segment tree to maintain lines L_i(x) = (dp[k-1][i] + S[i]^2) + (-2 S[i]) x and query at x = S[j]. This",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis explores advanced DP with a non-linear cost, requiring optimization beyond naive O(k n^2). It demonstrates how to transform the cost into a linear form and employ Li Chao trees for efficient queries.\n\n## Key Concepts\n\n- DP over partitions with a convex/quadrangle cost\n- Prefix sums and squared segment costs\n- Li Chao segment tree for dynamic line insertion and min queries\n- Correct base cases and 64-bit arithmetic\n\n## Code Example\n\n```javascript\n// Pseudocode sketch\nfunction minCostPartition(a, k) {\n  // compute prefix sums S[0..n]\n  // dpPrev and dpCur arrays length n+1 with INF\n  // initialize dpPrev[0] = 0\n  // for t = 1..k:\n  //   LiChaoTree cht = new LiChaoTree(xMin, xMax)\n  //   for j = t..n:\n  //     if dpPrev[j-1] < INF:\n  //       cht.addLine(-2 * S[j-1], dpPrev[j-1] + S[j-1]*S[j-1])\n  //     dpCur[j] = cht.query(S[j]) + S[j]*S[j]\n  //   swap dpPrev, dpCur; reset dpCur\n  // return dpPrev[n]\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt for at-most k segments?\n- What changes if the segment cost is (sum(l..r))^2 + lambda per segment, and how to choose lambda efficiently?",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-11",
        "questionId": "q-10499",
        "question": "Implement a dynamic multiset of integers with operations insert(x), delete(x) (one occurrence), and range queries querySum(L,R) and queryCount(L,R) in O(log N). Explain how you'd implement it using an AVL tree augmented with per-node fields: count (duplicates), subtree_size, subtree_sum, and rotations to maintain balance. Provide interface sketches and complexities?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An AVL-based multiset stores each value once, with a count for duplicates. Each node tracks count, subtree size, and subtree sum. Insert increments count or creates a node; delete decrements or remove",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "• Optimal DP uses deletions only; insertion cost 2 is never cheaper than deleting one end (cost 1) for any mismatch. • Let dp[i][j] be the min cost to turn s[i..j] into a palindrome. Base: i>=j → 0. • If s[i]==s[j], dp[…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an in-place, stable bottom-up merge sort. For each merge of two consecutive runs, rotate to interleave elements with O(1) extra space. Repeatedly double the block size until the entire array is sorted. The rotation…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a 4D DP: dp[i][j][t][d] stores the maximum sum reaching cell (i,j) with exactly t turns completed, where d indicates the direction of the last move (0=right, 1=down). For each cell, we transition from either the lef…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests augmenting BSTs with order-statistics and range queries, handling duplicates, and rotations for rebalancing.\n\n## Key Concepts\n- AVL balancing and rotations\n- Node augmentation: count, size, sum\n- Prefix sums for range queries\n- Duplicates in BST via count fields\n\n## Code Example\n```javascript\nclass AVLNode { constructor(key){ this.key=key; this.cnt=1; this.left=null; this.right=null; this.h=0; this.sz=1; this.sm=key; } }\n```\n\n## Follow-up Questions\n- How would you support k-th smallest in O(log N)?\n- How would you adapt to dynamic key changes without deletion?",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-12",
        "questionId": "q-350",
        "question": "Given a directed graph representing city intersections and one-way streets, implement a function to find if there's a valid route from point A to point B using BFS. Return the shortest path distance or -1 if no route exists?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use DP with tabulation: O(mn) time, O(mn) space. Optimize to O(n) space using rolling array. For path reconstruction, maintain parent pointers or backtrack from DP table. Handle edge cases like empty grid, single cell,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Bottom-up: use a hashmap mapping prefix sums to total ways up to the current index. Initialize map[0]=1, prefix=0. For i from 1..N, prefix+=A[i-1]; dp[i] = map[prefix-K] ?? 0; map[prefix] = (map[prefi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use BFS with a queue to explore the graph level by level, tracking visited nodes and distances from the start. Return the distance when the target node is found, or -1 if all reachable nodes are exhausted without findin…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Top‑down: dfs(i) = max(nums[i] + dfs(i+2), dfs(i+1)); base i>=n => 0; memoize by i. Bottom‑up: iteratively compute from n-1 down to 0 using two vars prev2, prev1; cur = max(prev1, nums[i] + prev2); sh",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests fundamental graph traversal algorithms, BFS implementation, and pathfinding skills - critical for autonomous vehicle navigation systems and route planning applications.\n\n## Expected Answer\nThe candidate should implement BFS using a queue, maintain a visited set to avoid cycles, track distances from the start node, and handle edge cases such as when start equals target, disconnected graphs, or empty input. They should also explain why BFS guarantees the shortest path in unweighted graphs.\n\n## Code Example\n```python\nfrom collections import deque\n\ndef shortest_path(graph, start, end):\n    if start == end:\n        return 0\n    \n    queue = deque([(start, 0)])\n    visited = {start}\n    \n    while queue:\n        node, distance = queue.popleft()\n        \n        for neighbor in graph.get(node, []):\n            if neighbor == end:\n                return distance + 1\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append((neighbor, distance + 1))\n    \n    return -1\n```",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-13",
        "questionId": "q-29273",
        "question": "In a workload-balancing context, given an array loads of length n and an integer k, partition the array into exactly k non-empty contiguous blocks. The cost is the maximum sum among blocks. Return the minimum possible cost and the number of partitions achieving it. Provide a memoized top-down DP and a bottom-up DP, and discuss edge cases (n < k, zeros, large sums)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a top-down memoized solution: countWays(i) returns the number of segmentations of s[i:] using dict. Iterate lengths up to maxWordLen and check substrings against dict, caching result",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a topological DP approach. Build an adjacency list and indegree array from the edges. Initialize finish[u] = duration[u] for all nodes with indegree 0, and add them to a queue. Process nodes in topological order; fo…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Perform BFS from s on unblocked nodes. Maintain dist[] initialized to INF and ways[] initialized to 0. Set dist[s]=0, ways[s]=1. For each edge v->u: skip if u blocked. If dist[u] is INF, set dist[u]=d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Let f(i,j) return {best, ways} for first i elements partitioned into j blocks. For last block t+1..i, S = prefix[i]-prefix[t], candidate = max(f(t,j-1).best, S). Update best to smallest candidate; acc",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question adds counting of optimal partitions to a standard min-max DP, testing both correctness and edge-case handling in a realistic workload-balancing scenario.\n\n## Key Concepts\n\n- DP with a value-cost pair (minCost, count) and prefix sums for O(1) subarray sums\n- Top-down memoization and bottom-up tabulation with identical transitions\n- Edge-case handling: n<k invalid, zeros, large sums\n\n## Code Example\n\n```javascript\nfunction minCostPartitions(nums, k){\n  const n = nums.length;\n  const pref = new Array(n+1).fill(0);\n  for(let i=0;i<n;i++) pref[i+1]=pref[i]+nums[i];\n  const dp = Array.from({length:n+1},()=>Array.from({length:k+1},()=>null));\n  for(let i=1;i<=n;i++) dp[i][1] = {best: pref[i], count:1};\n  for(let i=1;i<=n;i++){\n    for(let j=2;j<=Math.min(i,k);j++){\n      let best = Infinity, cnt = 0;\n      for(let t=j-1;t<=i-1;t++){\n        const last = pref[i]-pref[t];\n        const prev = dp[t][j-1];\n        if(!prev) continue;\n        const cand = Math.max(prev.best, last);\n        if(cand < best){ best = cand; cnt = prev.count; }\n        else if(cand === best){ cnt += prev.count; }\n      }\n      dp[i][j] = {best, count: cnt};\n    }\n  }\n  const res = dp[n][k];\n  return res ? {cost: res.best, ways: res.count} : null;\n}\n```\n\n## Follow-up Questions\n\n- How would you modify to return all partition indices achieving the min cost?\n- Could you optimize to O(nk) time with a narrowed search if nums are non-decreasing?",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-14",
        "questionId": "q-27145",
        "question": "Given an array of n integers where every element is in the inclusive range 0 to 9, sort the array in O(n) time with O(1) extra space. Explain the counting sort approach for fixed-range digits, outline the two passes (count and write-back), and show a concise in-place implementation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use in-place cyclic sort: while A[i] in [1,n] and A[A[i]-1] != A[i], swap A[i] and A[A[i]-1]. After the loop, scan: the first i with A[i] != i+1 gives missing = i+1 and duplicate = A[i]. Complexity: O",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-user map: user_id -> { categories: map<category, last_ts>, count, expireQueue }. On event (user, category, ts): if ts - last_ts(category) > 30s or category new, and within 30s window, update",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use counting sort: with digits 0-9, allocate a 10-slot count array. First pass: tally counts of each digit. Second pass: overwrite the input array by emitting digit i count[i] times, incrementing an i",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use topological sort with BFS to process tasks level by level. The minimum time equals the length of the longest path (critical path) in the DAG. O(V+E) time, O(V) space. Track completion times using DP: dp[node] = max(…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nInterview context explanation.\n\n## Key Concepts\n\n- Counting sort for fixed-range keys\n- In-place reconstruction using a separate count array\n- Time complexity O(n + k), with k = 10\n- Space complexity O(1) aside from the small counts array\n- Tradeoffs between stability and in-place requirements\n\n## Code Example\n\n```javascript\nfunction sortDigitsInPlace(arr){\n  const count = new Array(10).fill(0);\n  for (let v of arr) count[v]++;\n  let i = 0;\n  for (let d = 0; d <= 9; d++){\n    for (let c = 0; c < count[d]; c++) arr[i++] = d;\n  }\n  return arr;\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt if the range were 0..1e6 but n is 1e5?\n- Can you make the sort stable without using a second array, and what are the tradeoffs?",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-15",
        "questionId": "q-30529",
        "question": "Given a 32-bit unsigned integer, how would you efficiently count the number of set bits (1s) in its binary representation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Define dp[i][j] as the minimum number of removals to delete s[i..j]. Base: dp[i][i] = 1. Recurrence: dp[i][j] = 1 + dp[i+1][j], and for any k in (i+1..j) with s[i] == s[k], dp[i][j] = min(dp[i][j], dp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Brian Kernighan's algorithm: repeatedly clear the lowest set bit using n & (n-1). This runs in O(k) time where k is the number of set bits, making it more efficient than naive O(n) bit-by-bit chec",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Memoized DFS and bottom-up DP both operate on substrings (l,r). If s[l]==s[r], recurse on (l+1,r-1) with no deletion. Otherwise, take min of deleting left (l+1,r) plus delCost[l] and deleting right (l",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Top-down: dp(i, j) = min insertions to make s[i..j] palindrome. Base i>=j -> 0. If s[i]==s[j], dp(i+1, j-1); else 1+min(dp(i+1, j), dp(i, j-1)). Bottom-up: fill by length with a 2D table. Complexity:",
            "isCorrect": false
          }
        ],
        "explanation": "The most efficient approach for counting set bits is Brian Kernighan's algorithm, which leverages a clever mathematical identity: n & (n-1) clears the lowest set bit from n. This works because subtracting 1 from n flips all bits from the rightmost 1 bit onwards, so when you AND the result with n, that bit becomes 0.\n\nTo implement this, you repeatedly execute n = n & (n-1) and increment a counter until n becomes 0. Each iteration clears exactly one set bit, so the algorithm runs in O(k) time where k is the number of set bits—optimal when k is much less than 32.\n\nFor example, with n=12 (binary 1100), the first iteration gives n & (n-1) = 12 & 11 = 8 (1000), second iteration gives 8 & 7 = 0. So the count is 2.\n\nIn real-world applications, population count (popcount) is used in hash tables for computing hash bucket indices, in JPEG compression for DC coefficient encoding, in network routing for subnet mask calculations, and in bioinformatics for DNA sequence analysis. Most modern CPUs provide hardware popcount via instructions like POPCNT.",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-16",
        "questionId": "q-22465",
        "question": "Design a TimeWindowTopK data structure that ingests events (user_id, tag, delta, ts). For a sliding window of width W seconds ending at currentTime, maintain per-user totalDelta across all tags. Provide operations: 1) addEvent(ts, user_id, tag, delta) in O(log N) or amortized O(1); 2) topK(currentTime, K) returning top-K user_ids by totalDelta within [currentTime-W+1, currentTime]. Use a hash map, a per-user linked list of tag sums, a ring of time buckets, and a max-heap with lazy expirations. Explain expirations and memory usage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run a DFS to detect a directed cycle; if found, report cycle nodes. If no cycle, topologically sort and DP: finish[s] = d[s], others = -inf; in topo order, finish[v] = max over preds u of finish[u] +",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Insert: create/extend nodes and increment a passCount at each node. Delete: if word exists, walk path and decrement passCount; prune nodes with zero. LCP query: walk query, stop when next child missin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a hash map from user_id to a per-user ledger (a small doubly linked list of (tag, sum) nodes) plus a ring buffer of time buckets for the last W seconds and a max-heap of (totalSum, user_id, lastTs",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Top-down: dp[i][j] stores minimum cost to triangulate the chain from i to j (i+1 < j). For each k in (i+1..j-1), update best = min(dp[i][k] + dp[k][j] + w[i]*w[k]*w[j]); track count[i][j] for equal be",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests ability to combine hash maps, linked structures, and a heap to maintain a sliding-window top-K with expirations in real-time.\n\nKey Concepts\n- Sliding window with time-bucket decomposition\n- Hash map for O(1) user access\n- Per-user linked ledger for per-tag sums\n- Max-heap with lazy expiration for top-K\n- Lazy cleanup to bound memory\n\nCode Example\n```javascript\nclass TimeWindowTopK {\n  constructor(W) {\n    this.W = W;\n    // maps etc. (simplified skeleton)\n  }\n  addEvent(ts, user_id, tag, delta) {\n    // update bucket, per-tag sum, and totalSum; push to heap as needed\n  }\n  topK(currentTime, K) {\n    // pop from heap while expired; collect top K\n  }\n}\n```\n\nFollow-up Questions\n- How would you handle out-of-order arrivals and late events beyond the tolerance?\n- What optimizations reduce memory when the user base is large but activity is sparse?",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-17",
        "questionId": "q-24987",
        "question": "Given a directed weighted graph with non-negative weights, moving inside any strongly connected component costs 0; crossing components costs the edge weight. Compute the min cost from s to t. Provide: (1) SCC condensation plus topological DP on the DAG; (2) Dijkstra on the condensed DAG. Explain correctness, complexities, and edge cases (e.g., same SCC, no path, multiple edges).?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use m buckets for remainders 0..m-1 with a compact index map. First scan to count bucket sizes, then compute start positions. In a second pass, place each A[i] into its bucket at the next available sl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Compress the graph into strongly connected components (SCCs). Since movement within any SCC costs 0, build a condensation DAG where each node represents an SCC. For each pair of components A → B, create a directed edge…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a topological DP approach. Build an adjacency list and indegree array from the edges. Initialize finish[u] = duration[u] for all nodes with indegree 0, and add them to a queue. Process nodes in topological order; fo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: Utilize a hash map for frequency tracking and a max-heap containing pairs of (count, -value) with lazy deletions. For the add(x) operation: increment counts[x] and push (counts[x], -x) onto the heap. For getMa…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to combine SCC condensation with shortest-path algorithms, leveraging the insight that intra-component travel is free while inter-component traversal costs the edge weight.\n\n## Key Concepts\n- **SCC Detection**: Tarjan's or Kosaraju's algorithm\n- **Condensation DAG**: The SCC graph where each node is a component\n- **Edge Aggregation**: For each component pair (A, B), keep the minimum crossing edge weight\n- **Shortest Path**: Topological DP for DAGs or Dijkstra for non-negative weights\n- **Edge Cases**: Same SCC as source/target, disconnected components, multiple edges between components\n\n## Correctness\n- Inside an SCC, any node can reach any other node at zero cost by definition\n- The minimum crossing edge captures the cheapest way to leave one component and enter another\n- The condensation graph is a DAG (SCCs form a partial order)\n- Shortest path on this DAG yields the minimum cost from s to t\n\n## Complexity\n- SCC detection: O(V + E)\n- DAG construction: O(V + E) with edge aggregation\n- Shortest path on DAG: O(V + E) or O((V + E) log V) for Dijkstra\n\n## Edge Cases\n- **s and t in same SCC**: Answer is 0 (free movement)\n- **No path exists**: Return infinity or indicate unreachable\n- **Multiple edges between components**: Use minimum weight edge\n- **Self-loops**: Ignore (already handled by zero-cost SCC property)\n\n## Code Example\n```javascript\nfunction minCost(s, t, graph, weights) {\n  // 1. Find SCCs using Kosaraju or Tarjan\n  const sccs = findSCCs(graph);\n  \n  // 2. Build condensation DAG with min edge weights\n  const dag = buildCondensationDAG(graph, weights, sccs);\n  \n  // 3. Find source and target components\n  const srcComp = sccs[s], dstComp = sccs[t];\n  \n  // 4. If same component, cost is 0\n  if (srcComp === dstComp) return 0;\n  \n  // 5. Run Dijkstra on DAG from srcComp to dstComp\n  return dijkstra(dag, srcComp, dstComp);\n}\n```\n\n## Follow-up Questions\n- How would you handle multiple crossover edges between the same components?\n- What if edge weights can be negative?\n- How does this approach compare to running Dijkstra on the original graph directly?",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-18",
        "questionId": "q-14379",
        "question": "Given an array of objects {id, priority (1-3), time}, write a stable sort that orders first by priority ascending and, within the same priority, by time ascending. Implement in JavaScript without relying on any built-in multi-key sort utilities and explain the time/space trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: per-user deque of (ts, score) plus a running sum; a hashtable maps user_id -> {queue, sum}. addEvent pushes to queue and increments sum. expire as currentTime advances: pop front while ts <=",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-pass stable sort: first sort by time, then stably sort by priority. This yields final (priority, time) order. A stable merge sort gives O(n log n) time and O(n) extra space; using a non-stable in-",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "An in-place three-way quicksort (Dutch national flag) sorts an int array with duplicates by partitioning around a pivot into <, ==, > regions and recursing only on the < and > parts. Use three pointer",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run a DFS to detect a directed cycle; if found, report cycle nodes. If no cycle, topologically sort and DP: finish[s] = d[s], others = -inf; in topo order, finish[v] = max over preds u of finish[u] +",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to reason about multi-key sorting, stability, and practical trade-offs in beginner-friendly tasks. It also aligns with real-world data processing where groupings matter.\n\n## Key Concepts\n\n- Sorting stability and its impact on multi-key ordering\n- Two-pass stable sorting technique\n- Time complexity: O(n log n) for mergesort\n- Space complexity: O(n) auxiliary memory\n\n## Code Example\n\n```javascript\nfunction stableMergeSortBy(arr, key) {\n  if (arr.length <= 1) return arr\n  const mid = Math.floor(arr.length / 2)\n  const left = stableMergeSortBy(arr.slice(0, mid), key)\n  const right = stableMergeSortBy(arr.slice(mid), key)\n  const out = []\n  let i = 0, j = 0\n  while (i < left.length && j < right.length) {\n    if (left[i][key] <= right[j][key]) out.push(left[i++])\n    else out.push(right[j++])\n  }\n  return out.concat(left.slice(i)).concat(right.slice(j))\n}\n\nfunction sortRecords(arr) {\n  const byTime = stableMergeSortBy(arr, 'time')\n  return stableMergeSortBy(byTime, 'priority')\n}\n```\n\n## Follow-up Questions\n\n- How would you modify if you must sort in place and the library sort is not stable?\n- If time values span a large range, how could you optimize space or use counting/radix techniques to reduce complexity?",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-19",
        "questionId": "q-30023",
        "question": "Design a hybrid in-place sorting algorithm that uses quicksort with median-of-three pivot and a depth limit of 2*log2(n). If recursion depth for a subarray exceeds the limit, switch to an in-place mergesort for that subarray to avoid worst-case behavior. Explain pivot choice, switching threshold, and the overall time and space bounds, including how you handle small partitions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Bottom-up: maintain a 1D dp of length m+1 with dp[0]=1. For each char c in S, iterate j from m down to 1 and if c==T[j-1], set dp[j]=(dp[j]+dp[j-1])%MOD. Return dp[m]. Time O(nm), space O(m). Top-down",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-room doubly linked list of sender entries, a hash map (room_id, sender_id) -> node for dedup, per-room total, a max-heap of rooms by total with lazy updates, and a time-bucket queue for expi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an algorithm that computes earliest finish times using Dijkstra on a transformed graph: for each edge (u,v) cost is w(u,v)+d(v). Initialize dist[s]=d(s) for s in S, dist[other]=INF. The finish",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an introsort-style hybrid: quicksort with median-of-three pivot and a recursion depth limit D = 2*floor(log2(n)). If depth exceeds D on a subarray, switch to an in-place mergesort for that r",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of sorting algorithm trade-offs, pivot strategies, and handling pathological inputs by switching strategies.\n\n## Key Concepts\n- Hybrid algorithms\n- Median-of-three pivot\n- Recursion depth control (introsort)\n- In-place merge techniques with rotation\n- Time and space analysis (O(n log n) time, O(log n) stack, O(1) extra space)\n\n## Code Example\n\n```javascript\n// Implementation sketch: not provided here\n```\n\n## Follow-up Questions\n- How would you tune D for different data distributions?\n- Compare with std::sort and discuss practical memory trade-offs.",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-20",
        "questionId": "q-22835",
        "question": "Given a directed weighted graph with positive weights, implement a function shortestPathWithOneFreeEdge(graph, src, dst) that returns the minimum travel time from src to dst if you may set the cost of at most one edge on the path to zero. Describe your approach, provide a code sketch, and analyze complexity?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Trie: each node has 26 children and an isWord flag. insert(w) walks/creates nodes for each char and marks the last node. search(w) returns true if every node exists and the final node isWord. st",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "3-way partitioning groups duplicates into <, =, > to minimize swaps; median-of-three pivots reduces degenerate pivots; use tail recursion elimination to reduce stack usage; track depth and switch to s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-layer Dijkstra: dist[v][f] for f in {0,1} tracks shortest distance to v with f indicating whether the one zero-cost edge has been used. Initialize dist[src][0]=0, push (0,src,0). For each edge u->",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "First, compute a topological order of the graph and verify reachability: check that S can reach the first mandatory edge's source, that the last mandatory edge's target can reach T, and that each mandatory edge's target…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests adapting Dijkstra to a stateful constraint and reasoning about path modifications.\n\n## Key Concepts\n- Layered shortest path with two states (free edge used vs not used)\n- Dijkstra with expanded state space, vertex duplication\n- Correct edge relaxations for both states and path reconstruction considerations\n\n## Code Example\n```python\nimport heapq\n\ndef shortest_with_one_free_edge(graph, src, dst):\n    n = len(graph)\n    INF = float('inf')\n    dist = [[INF]*2 for _ in range(n)]\n    dist[src][0] = 0\n    pq = [(0, src, 0)]  # (d, node, used_free)\n    while pq:\n        d, u, f = heapq.heappop(pq)\n        if d != dist[u][f]:\n            continue\n        for v, w in graph[u]:\n            if dist[v][f] > d + w:\n                dist[v][f] = d + w\n                heapq.heappush(pq, (dist[v][f], v, f))\n            if f == 0 and dist[v][1] > d:\n                dist[v][1] = d\n                heapq.heappush(pq, (d, v, 1))\n    return min(dist[dst][0], dist[dst][1])\n```\n\n## Follow-up Questions\n- How would you modify for multiple free edges?\n- How would you reconstruct the actual path taken?",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-21",
        "questionId": "q-9656",
        "question": "Design a data structure ExpiringLeaderboard with operations: add(key, score, ts, ttl), getCurrentScore(key, ts), getTopK(ts, K). The entry for a key expires ttl seconds after ts. Use arrays, a hashtable, a doubly linked list for LRU-like recency, and a max-heap to support getTopK efficiently. Explain how you'd handle expirations and stale heap entries, and analyze time/space?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a SlidingTopK path using: a fixed-size window (linked list) for expirations, a hashmap for frequencies, and a min-heap of size K for top-K with lazy deletions. On add and expiration, adjust co",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a hashtable mapping each key to a structure containing {sum, queue of (ts, value)} entries, combined with a min-heap of (sum, key) entries using lazy validity. On addEvent(key, value, ts): push (ts, value) to the ke…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Maintain a hashmap key -> {score, tsLast, ttl, ver}. On add, bump ver and push (score, -key, ver, tsLast) into a max-heap. getCurrentScore(ts) checks expiration: if ts >= tsLast + ttl, remove or mark",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a deque for windowed events, a hashmap for recent (driver_id,trip_id)->ts to deduplicate with 30s expiry, a hashmap cityTotal[city]->sumDistance, and a max-heap of (total,city) with lazy validatio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests managing per-key scores with time-based expirations and top-K queries using several data structures.\n\n## Key Concepts\n- Expiration and lazy deletion\n- Hash map with versioning\n- Max-heap for top-K with lazy validation\n- O(1) updates via map + heap amortized\n\n## Code Example\n```javascript\nclass ExpiringLeaderboard { constructor(){...} add(key, score, ts, ttl){...} getCurrentScore(key, ts){...} getTopK(ts, K){...}}\n```\n\n## Follow-up Questions\n- How would you handle high churn with memory pressure?\n- How would you support decrementing scores and tie-breakers?",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-22",
        "questionId": "q-28897",
        "question": "Design a real-time streaming leaderboard: events arrive as (user_id, category, value, ts). For a sliding window W, maintain per-user totals across all categories within [currentTime-W, currentTime). Provide operations: 1) addEvent(user_id, category, value, ts) in O(log n) amortized, 2) topKUsers(K, currentTime) returning the K users with the largest total within the window in O(K log n). Explain the data structures, how expirations are handled, and edge cases like ties?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed design: a Trie where each node stores a balanced BST mapping phrase to score, enabling O(log n) updates; each node also maintains a top-k structure (a max-heap or segment tree over its subtree) to extract top-k…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a hashmap to track the latest timestamp for each (user_id, action) pair to deduplicate within window W. Maintain per-user total scores in another hashmap and store per-user event lists (linked lists) for efficient e…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use in-place cyclic sort: while A[i] in [1,n] and A[A[i]-1] != A[i], swap A[i] and A[A[i]-1]. After the loop, scan: the first i with A[i] != i+1 gives missing = i+1 and duplicate = A[i]. Complexity: O",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a global max-heap keyed by user total, a hash map from `user_id` to a struct containing `{total, perCategorySums, events, version}`. Each user also maintains a doubly linked list of events sorted by timestamp to sup…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design a streaming, multi-structure solution combining arrays, linked lists, hash tables, and heaps. Evaluates understanding of sliding-window aggregation, lazy expiration, and tie handling across categories.\n\n## Key Concepts\n- Sliding-window aggregation across multiple categories\n- Per-user maps with per-category substructures\n- Global heap with lazy validation for top-K queries\n- Doubly linked lists for per-user event expiration\n\n## Data Structures\n\n| Structure | Purpose | Complexity |\n|-----------|---------|------------|\n| Max-heap | Global ranking by user total | O(K log n) for top-K |\n| Hash map | User lookup and metadata storage | O(1) average |\n| Doubly linked list | Per-user event ordering for expiration | O(1) insert, O(1) expire |\n| Version counter | Lazy validation of stale heap entries | O(1) |\n\n## Edge Cases\n\n**Ties:** When users have equal totals, break ties by `user_id` or insertion order to ensure deterministic ordering.\n\n**Expiration:** Events automatically expire when they fall outside the sliding window. Periodically check the head of each user's linked list, remove expired events, update totals, and reheap as needed.\n\n**Concurrency:** Version counters enable thread-safe updates; heap operations use locks or atomic primitives to prevent race conditions.",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-23",
        "questionId": "q-22078",
        "question": "Design a dynamic string dictionary that supports: insert(s, freqDelta), delete(s) (when freq becomes <= 0 remove), sumPrefix(p) = total frequency of all strings starting with p, and kthPrefix(p, k) = the k-th string in lexicographic order among strings starting with p. Strings up to length L, total distinct strings up to 2e5. Achieve amortized O(log n) updates and O(log n) prefix sums; propose a hybrid data structure combining a Trie and per-node balanced BST storing keys in lex order with subtree sums. Provide pseudo-interface and complexity analysis. Edge cases: absent prefix, duplicates, etc?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a hashmap for counts and a size-K min-heap of [count, value]. On add(x): freq[x]++, if x exists in the heap adjust its key; else if heap size < K insert; else if freq[x] > min or freq[x] == min an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: a Trie where each node holds an augmented ordered set of terminal strings in its subtree and a subtree sum of frequencies. insert/delete updates along the path in O(L log N). sumPre",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Introsort guarantees O(n log n) worst-case performance by monitoring recursion depth and switching to heapsort when quicksort depth exceeds 2*log₂(n). The implementation uses 3-way partitioning for efficient duplicate h…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Define dp[0]=1. For i from 1..n, maintain a left index so that sum(a[left..i-1]) ≤ B; then dp[i] = sum(dp[left..i-1]). Use a sliding window to update both sum and left, yielding O(n) time and O(n) spa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to blend trie augmentation with balanced BSTs to support fast prefix sums and kth selection.\n\n## Key Concepts\nTrie augmentation, order-statistics tree, range sums.\n\n## Code Example\n```cpp\nstruct Node { int sum; map<string,int> *sub; /* keyed by full string */ };\n```\n\n## Follow-up Questions\n- How would you support updates to frequencies in bulk?\n- Analyze memory and worst-case height implications.",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-24",
        "questionId": "q-23732",
        "question": "Implement a data structure PrefixTopK that stores strings with non-negative scores and supports dynamic updates and prefix-based top-K queries. It should use a Trie for fast prefix navigation and per-node max-heaps with lazy validation to return the top-K words starting with a given prefix in decreasing score (ties broken lexicographically). It must support: insert/update (add(word, delta)); remove(word); query(prefix, K)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Compute shortest distances and path counts on a weighted DAG using a single topological order. Initialize dist[0]=0, ways[0]=1, others INF and 0. For each u in topo, relax every edge u->v with weight",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Maintain a map of (service_id, endpoint_id) to a fixed-size linked-list window of the last W seconds of latency samples (ts, latency). For each service_id, track totalLatency and totalCount across all its endpoints. A m…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Trie where each node holds a max-heap of (−score, word) entries for all words in its subtree. Maintain a global map word→score. On add/remove, traverse the word’s path and push (−newScore, word)",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Trie with per-node counters: `pass` (words passing through) and `end` (words ending here). Insert(word) increments `pass` at each node traversed and `end` at the terminal node. CountPrefix(prefix) returns the `pas…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests prefix search, dynamic ranking, and practical autocomplete logic. Uses a Trie for O(length) prefix lookup, and a heap with lazy deletion to keep updates efficient without heavy per-node restructuring.\n\n## Key Concepts\n- Trie for prefix navigation\n- Lazy deletion in heaps to avoid global rebuilds\n- Trade-offs between update cost and query latency\n- Edge cases: zero scores, deletions, unknown words\n\n## Code Example\n```javascript\n// Pseudo implementation sketch (not runnable here)\n```\n\n## Follow-up Questions\n- How would you handle duplicate insertions with different deltas?\n- How would you scale to support multiple alphabets or memory constraints?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-03T06:55:11.803Z",
    "version": 1
  }
]