[
  {
    "id": "test-algorithms",
    "channelId": "algorithms",
    "channelName": "Algorithms",
    "title": "Algorithms Knowledge Test",
    "description": "Test your understanding of Algorithms with 25 questions.",
    "questions": [
      {
        "id": "algorithms-q-0",
        "questionId": "q-5422",
        "question": "Design a data structure to support an auto-complete dictionary with dynamic phrase scores using a Trie for prefix search and a balanced BST at each node, augmented with a segment-tree layer to extract top-k results for a given prefix efficiently. The structure must support insertPhrase(phrase, score), updateScore(phrase, newScore), deletePhrase(phrase), and queryTopK(prefix, k). Analyze time and memory, discuss edge cases, and compare with alternatives?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a comprehensive load balancer with configurable health check monitoring, dynamic server state management, consistent hashing for session affinity, and circuit breaker patterns for graceful degradation.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use prefix sums to get block sums in O(1). Let dp[i][k] be the min cost for the first i items partitioned into k blocks. Then dp[i][k] = min over t in [0..i-1] of dp[t][k-1] + (prefix[i]-prefix[t])^2.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed design: a Trie where each node stores a balanced BST mapping phrase to score, enabling O(log n) updates; each node also maintains a top-k structure (a max-heap or segment tree over its subtree) to extract top-k…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a sliding window with per-(user_id,item_id) pair counts. Use a global queue of events in ts order; a map pairCount[(u,i)] -> int; a map itemCount[item] -> int; a max-heap of (count,item). On",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses the ability to blend Trie prefix search with balanced BSTs for dynamic scores, plus a top-k retrieval strategy that scales efficiently. It tests practical update propagation and memory trade-offs in a high-throughput autocomplete system.\n\n## Key Concepts\n- Trie for prefix lookup\n- AVL/balanced BST per node for phrase-score storage\n- Top-k extraction via a front-to-back heap or subtree aggregation\n- Update propagation costs and memory considerations\n\n## Code Example\n```javascript\nclass TrieNode {\n  constructor() {\n    this.next = new Map();\n    this.words = new AVLTree(); // key: phrase, value: score\n    this.topK = new MaxHeap(k); // top-k scores for subtree\n  }\n}\n\nclass AutoCompleteDictionary {\n  constructor(k = 10) {\n    this.root = new TrieNode();\n    this.k = k;\n  }\n\n  insertPhrase(phrase, score) {\n    let node = this.root;\n    for (let char of phrase) {\n      if (!node.next.has(char)) {\n        node.next.set(char, new TrieNode());\n      }\n      node = node.next.get(char);\n      node.words.insert(phrase, score);\n      node.topK.insert(score);\n    }\n  }\n\n  queryTopK(prefix, k) {\n    let node = this.root;\n    for (let char of prefix) {\n      if (!node.next.has(char)) return [];\n      node = node.next.get(char);\n    }\n    return node.topK.getTopK(k);\n  }\n}\n```\n\n## Time Complexity Analysis\n- insertPhrase: O(L log n) where L is phrase length, n is phrases per prefix\n- updateScore: O(L log n) for BST updates plus O(L) for heap propagation\n- deletePhrase: O(L log n) for BST removal and heap cleanup\n- queryTopK: O(L + k log k) for traversal plus heap extraction\n\n## Memory Trade-offs\n- Trie structure: O(N × L) where N is total phrases, L is average length\n- BST per node: O(N) total across all nodes\n- Top-k heaps: O(N × k) worst-case, but typically O(N) with pruning\n\n## Edge Cases\n- Duplicate phrases: update existing entry instead of inserting\n- Score ties: use lexicographic ordering as tiebreaker\n- Empty prefix: return global top-k from root node\n- Large k: consider segment tree for better range queries\n\n## Comparison with Alternatives\n- Hash map + sorting: O(1) insert but O(n log n) prefix queries\n- Suffix tree: better substring search but higher memory overhead\n- Pure Trie with scores: O(L) queries but O(n) updates for score changes\n- Hybrid approach (proposed): balances prefix efficiency with dynamic updates",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-1",
        "questionId": "q-8204",
        "question": "Given a string s of length n, what is the minimum number of insertions required to transform s into a palindrome, and provide a bottom-up dynamic programming approach to compute this value in O(n^2) time and space? You may insert any character at any position. For example, 'abca' requires 1 insertion?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a 2D DP where dp[i][j] is the minimum insertions to make s[i..j] a palindrome. If s[i] == s[j], dp[i][j] = dp[i+1][j-1]; else dp[i][j] = 1 + min(dp[i+1][j], dp[i][j-1]). Initialize dp[i][i] = 0 an",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a load balancer with active and passive health checks, maintain server state machines, and use request buffering or graceful drain to handle transitions without dropping requests.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Solution:** Perform a topological sort of the DAG and use DP over the state `(node, lastColor)`. Maintain `dist[v][c]` = minimum color changes to reach node `v` ending with an edge of color `c`, and `ways[v][c]` = num…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Memoized DFS and bottom-up DP both operate on substrings (l,r). If s[l]==s[r], recurse on (l+1,r-1) with no deletion. Otherwise, take min of deleting left (l+1,r) plus delCost[l] and deleting right (l",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes translating a string problem into substring subproblems and requires a clear bottom-up DP setup.\n\n## Key Concepts\n- Substring DP: dp[i][j] for i..j\n- Base cases: i >= j -> 0 insertions\n- Bottom-up vs memoization: space/time trade-offs\n\n## Code Example\n```javascript\nfunction minInsertions(s){\n  const n = s.length;\n  const dp = Array.from({length:n}, ()=> Array(n).fill(0));\n  for(let i = n-2; i >= 0; i--){\n    for(let j = i+1; j < n; j++){\n      if(s[i] === s[j]) dp[i][j] = dp[i+1][j-1];\n      else dp[i][j] = 1 + Math.min(dp[i+1][j], dp[i][j-1]);\n    }\n  }\n  return n ? dp[0][n-1] : 0;\n}\n```\n\n## Follow-up Questions\n- How would you reconstruct one palindrome achieving the minimum insertions?\n- How would you adapt for streaming input or very large strings with limited memory?",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-2",
        "questionId": "q-2617",
        "question": "How would you implement a consistent hashing load balancer that minimizes server remapping when adding or removing nodes, and what data structures would you use to achieve O(log n) lookup time?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Trie where every node stores a BST keyed by (frequency, word). Maintain a global map word->freq. insert(word, delta): update freq, propagate along the path; at each node remove the old (freq, wo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a predictive load balancer using time series forecasting models like ARIMA or LSTM to analyze historical request patterns, then proactively scale server capacity and adjust distribution algorithms.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To design SlidingTagTopK: maintain a hash map from tag to TagBucket(sum, deque of (ts, value)); a global max-heap of (sum, tag, ver). On addEvent, push to deque; pop expired items ts < currentTime-W,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a consistent hashing load balancer using a hash ring with virtual nodes and a balanced tree structure (such as TreeMap in Java or std::map in C++) to map request hashes to server nodes, achieving minimal remap…",
            "isCorrect": true
          }
        ],
        "explanation": "Consistent hashing addresses the major drawback of traditional hash-based load balancing, where adding or removing servers requires remapping the entire key space. The solution involves mapping both servers and requests onto a circular hash space (typically 0 to 2^32-1). Each request is routed to the first server encountered when moving clockwise from the request's hash position. When servers are added or removed, only requests in the adjacent hash segments require remapping, significantly minimizing disruption.\n\nFor implementation, I would use a balanced tree data structure (TreeMap in Java, std::map in C++, or similar structures) to maintain sorted server positions on the ring. This enables O(log n) lookup time for finding the next server using tree traversal operations. To ensure even distribution and handle load imbalances, I would implement virtual nodes—multiple hash positions per physical server—reducing the impact of uneven hash distribution and providing better load balancing across the cluster.",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-3",
        "questionId": "q-5774",
        "question": "You're given a DAG representing tasks with dependencies and durations. Each node has a positive duration. Implement earliestFinishTimes(n, edges, durations) that returns the earliest finish time for every node assuming all prerequisites finish before a task starts. Use a topological sort to process tasks in dependency order. Provide a signature, outline the algorithm, and its time complexity. Realistic scenario: building a data pipeline?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a load balancer with active and passive health checks, maintain server state machines, and use request buffering or graceful drain to handle transitions without dropping requests.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Kahn’s topological sort to obtain an order. Build adjacency and in-degree, set finish[i] = durations[i] for all i. For each node u in topo order, for each v in adj[u], set finish[v] = max(finish[v",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Tarjan's algorithm to compute strongly connected components and condense the graph into a DAG. For each SCC, run Dijkstra from all boundary vertices (those with edges entering or exiting the component) to compute op…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Top-down: minCost(n) = min_s (s + minCost(n-s)) with base minCost(0)=0; ignore n<0 as Infinity. Memoize in a map. Bottom-up: dp[0]=0; for i=1..n, dp[i] = min_{s in S, i>=s} (s + dp[i-s]); if dp[n] is",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to convert dependency graphs into scheduling via topological order; checks understanding of longest-path DP on DAG, cycle detection, and clear complexity analysis.\n\n## Key Concepts\n- Topological sort (Kahn / DFS)\n- DAG longest path DP\n- Cycle detection\n- Complexity analysis\n\n## Code Example\n\n```javascript\nfunction earliestFinishTimes(n, edges, durations) {\n  const adj = Array.from({ length: n }, () => []);\n  const indeg = Array(n).fill(0);\n  for (const [u, v] of edges) { adj[u].push(v); indeg[v]++; }\n  const q = [];\n  for (let i = 0; i < n; i++) if (indeg[i] === 0) q.push(i);\n  const finish = durations.slice();\n  let seen = 0;\n  while (q.length) {\n    const u = q.shift();\n    seen++;\n    for (const v of adj[u]) {\n      finish[v] = Math.max(finish[v], finish[u] + durations[v]);\n      if (--indeg[v] === 0) q.push(v);\n    }\n  }\n  if (seen !== n) throw new Error('Cycle detected');\n  return finish;\n}\n```\n\n## Follow-up Questions\n- How would you adapt for multiple start times or optional prerequisites?\n- How does the solution change if edge weights represent communication delay instead of task duration?",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-4",
        "questionId": "q-18439",
        "question": "Design a data structure BurstyRoomWindow that processes streaming events (room_id, sender_id, len, ts) with a sliding window W seconds. For each room, deduplicate multiple events from the same sender within the window, keeping only the latest len, and maintain per-room total length. Support getTopKRooms(K) by total length in O(log K) per update, and expire old events as time advances in O(1) amortized per expiration. Explain data structures and complexity?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a health checking system with circuit breaker pattern, maintain an active server pool, and use request buffering or connection draining for seamless failover.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Sliding window of W seconds. For each post, keep a map user_id -> deque of their like timestamps. On a new like, push ts; if the user’s deque was empty, increment post’s distinct-count. Expire old lik",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a hash map that stores task_id mapping to {score, timestamp}. Maintain a max-heap containing (score, task_id) pairs with lazy deletion. For each delta event, update the task's score and timestamp in the hash map, th…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-room doubly linked list of sender entries, a hash map (room_id, sender_id) -> node for dedup, per-room total, a max-heap of rooms by total with lazy updates, and a time-bucket queue for expi",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical use of multiple data structures to handle streaming data: linked lists for O(1) removal, hash tables for dedup, a heap for top-K queries, and time-based expirations.\n\n## Key Concepts\n- Doubly linked list per room for fast eviction\n- Hash map for (room_id, sender_id) to node\n- Per-room totals and a global max-heap with lazy updates\n- Time-bucket expirations to achieve amortized O(1) handling\n\n## Code Example\n```javascript\nclass Node { constructor(sender, len, ts){...} }\nclass BurstyRoomWindow {\n  // maps and structures\n  onEvent(room, sender, len, ts){ /* dedup, update totals, heap push */ }\n  expire(now){ /* remove old nodes, adjust totals */ }\n  topK(K){ /* pop from heap with lazy cleanup */ }\n}\n```\n\n## Follow-up Questions\n- How would you adapt for variable window sizes per room? \n- What are spillover risks with extreme burstiness and how to mitigate them?",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-5",
        "questionId": "q-9656",
        "question": "Design a data structure ExpiringLeaderboard with operations: add(key, score, ts, ttl), getCurrentScore(key, ts), getTopK(ts, K). The entry for a key expires ttl seconds after ts. Use arrays, a hashtable, a doubly linked list for LRU-like recency, and a max-heap to support getTopK efficiently. Explain how you'd handle expirations and stale heap entries, and analyze time/space?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Quicksort is an efficient divide-and-conquer algorithm that partitions an array around a pivot element, achieving O(n log n) average time complexity. It's preferable over mergesort when in-place sorting is required, as…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Maintain a hashmap key -> {score, tsLast, ttl, ver}. On add, bump ver and push (score, -key, ver, tsLast) into a max-heap. getCurrentScore(ts) checks expiration: if ts >= tsLast + ttl, remove or mark",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Compute topological order (Kahn or DFS). For each node v, est[v] = max(est[u] + dur[u] for all u -> v); ef[v] = est[v] + dur[v]. Roots have est=0. The project duration is max ef[v], and the path is re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Compute in a single pass over a topological order: dist[u] = shortest distance from s; cnt[u] = number of shortest s→u paths; par[u] = one predecessor for path reconstruction. Relax edges in topo orde",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests managing per-key scores with time-based expirations and top-K queries using several data structures.\n\n## Key Concepts\n- Expiration and lazy deletion\n- Hash map with versioning\n- Max-heap for top-K with lazy validation\n- O(1) updates via map + heap amortized\n\n## Code Example\n```javascript\nclass ExpiringLeaderboard { constructor(){...} add(key, score, ts, ttl){...} getCurrentScore(key, ts){...} getTopK(ts, K){...}}\n```\n\n## Follow-up Questions\n- How would you handle high churn with memory pressure?\n- How would you support decrementing scores and tie-breakers?",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-6",
        "questionId": "q-24643",
        "question": "Given an array nums of positive integers and a target sum S, count the number of subsets that sum exactly to S. Each element can be chosen at most once. Return the answer modulo 1_000_000_007. Provide both a top-down memoized solution and a bottom-up tabulation solution with O(N*S) time; explain edge cases like empty subset and S=0?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Dijkstra on augmented state (node, hops). Maintain dist[node][k] as best time to reach node in exactly k hops. Start dist[S][0]=0; push (0,S,0). For each edge u→v with weight w, if k+1≤H and dist[",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use median-of-three to pick pivot, in-place partition, and recurse on subranges; track depth as you go. If depth > 2*log2(n), switch to heapsort on the current range; for small ranges (e.g., <=16), us",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Top-down: f(i, s) = f(i-1, s) + f(i-1, s-nums[i-1]); base case f(0,0)=1, f(0,s>0)=0; memoize by (i,s); return f(n,S) mod 1e9+7. Bottom-up: 1D dp array of size S+1; initialize dp[0]=1; for each num in nums, iterate s fro…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "I would implement a top-down memoized solution: countWays(i) returns the number of segmentations of s[i:] using dict. Iterate lengths up to maxWordLen and check substrings against dict, caching result",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests your understanding of counting DP, covering both memoization (top-down) and tabulation (bottom-up) approaches, along with space optimization and edge-case handling.\n\n## Key Concepts\n\n- Subset sum counting with each element used at most once\n- Memoization (top-down) with recursive DFS and memo table\n- Tabulation (bottom-up) with 1D space optimization to O(S)\n- Modulo arithmetic to prevent integer overflow\n\n## Edge Cases\n\n- **Empty subset (N=0)**: If N=0 and S=0, return 1 (one valid subset - empty set). If S>0, return 0.\n- **Target S=0**: Always return 1 (the empty subset counts as one valid subset).\n- **All elements greater than S**: No valid subsets exist, return 0.\n\n## Code Example\n\n```javascript\nfunction countSubsets(nums, target) {\n  const mod = 1_000_000_007;\n  const n = nums.length;\n  \n  // Top-down with memoization\n  const memo = Array.from({ length: n + 1 }, () => Array(target + 1).fill(-1));\n  function dfs(i, s) {\n    if (s === 0) return 1;\n    if (i === 0) return 0;\n    if (memo[i][s] !== -1) return memo[i][s];\n    memo[i][s] = dfs(i - 1, s) % mod;\n    if (s >= nums[i - 1]) {\n      memo[i][s] = (memo[i][s] + dfs(i - 1, s - nums[i - 1])) % mod;\n    }\n    return memo[i][s];\n  }\n  return dfs(n, target);\n}\n\n// Bottom-up with space optimization\nfunction countSubsetsBU(nums, target) {\n  const mod = 1_000_000_007;\n  const dp = Array(target + 1).fill(0);\n  dp[0] = 1;\n  \n  for (const num of nums) {\n    for (let s = target; s >= num; s--) {\n      dp[s] = (dp[s] + dp[s - num]) % mod;\n    }\n  }\n  return dp[target];\n}\n```\n\nBoth solutions run in O(N×S) time with the bottom-up approach using O(S) space.",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-7",
        "questionId": "al-1",
        "question": "When would you choose a Linked List over an Array and what are the key trade-offs for each data structure?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Trie with 26 children per node and an end flag. insert(word) creates nodes and marks end. contains(word) traverses and checks end. kthWord(k) does a lexicographic DFS: when a node is an en",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a topological order to linearize the DAG. dist[s]=0, dist[others]=INF. For u in topo order, for each edge (u,v) with weight w, if dist[u]+w < dist[v], update dist[v]. The result is the earliest in",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Dijkstra's algorithm on an augmented two-layer graph with states (u, 0) and (u, 1), where the second component indicates whether the coupon has been used. From state (u, 0), traverse each edge (u, v) with weight w t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "- When to choose Linked List: - - Frequent insertions/deletions at known positions: O(1) if you have a node pointer (doubly linked list is typical); avoids shifting. - - Dynamic/unknown size with many reallocations: per…",
            "isCorrect": true
          }
        ],
        "explanation": "Linked lists excel in scenarios with many inserts/deletes at known nodes and with uncertain length, since they avoid expensive reallocations and shifting. However, random access is linear and memory per element is higher due to next/prev pointers, hurting cache locality and increasing fragmentation risk. Arrays give O(1) indexing and excellent spatial locality, but insertions/deletions in the middle cost O(n) due to shifts and potential reallocation penalties. In practice, hybrid structures (e.g., LRU using a hashmap + doubly linked list) maximize the strengths of both patterns.",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-8",
        "questionId": "q-16679",
        "question": "Scenario: You receive a batch of N integers that is almost sorted—each element is at most k positions away from its final place. Implement a stable sort in O(n log k) time using a min-heap of size k+1 and O(k) extra space. Include lightweight test data and justify when this beats standard O(n log n) sorts in practice?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "First, compute a topological ordering of the DAG. Then, for each landmark in {s} ∪ L ∪ {t}, run a single-source shortest-path DP in topological order to compute distances to all other nodes, along with predecessor infor…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**Approach:** Use a min-heap to repeatedly extract the smallest element from the first k+1 items, maintaining sorted order. ```javascript class MinHeap { constructor() { this.a = []; } push(x) { this.a.push(x); this.bub…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "- State of the art: O(n) time, O(1) space via a 6-slot rolling window. - Recurrence: dp[i] = dp[i-1] + ... + dp[i-6], with dp[0]=1 and dp[i<0]=0. - Data: buf[6] stores the last six dp values; window = sum(buf). - Init:…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Problem: Deadline-Constrained Task Scheduling Given N tasks where each task i has: - Duration `t[i]` (time needed to complete) - Deadline `d[i]` (must finish by this time) - Value `v[i]` (benefit earned) Schedule tas…",
            "isCorrect": false
          }
        ],
        "explanation": "## What Is an Almost-Sorted Array?\nAn array where each element is at most **k positions** from its final sorted position. Examples:\n- `k=1`: Only adjacent elements may need swapping (insertion sort territory)\n- `k=3`: Each element is within 3 spots of where it belongs\n- `k=n`: Unsorted—reverts to standard heap sort\n\n---\n\n## Why Use a Min-Heap?\n\nThe heap always gives you the smallest element from the **window of k+1 candidates**. This works because:\n1. The smallest correct element **must** be in the first k+1 positions (guaranteed by the k-sorted property)\n2. The heap extracts it in O(log k) time\n3. We refill the window with the next input element, maintaining the invariant\n\n---\n\n## Step-by-Step Algorithm\n\n1. **Initialize:** Push first k+1 elements into heap\n2. **Iterate:** For each remaining element:\n   - Pop smallest from heap → add to output\n   - Push next input element into heap\n3. **Finish:** Pop all remaining elements from heap in sorted order\n\n---\n\n## Correctness Proof Sketch\n\n**Invariant:** After processing position `i`, the heap contains elements from indices `[i, i+k]`.\n- Base case: Initially holds `[0, k]` ✓\n- Inductive step: Pop at `i` removes oldest; push at `i+k+1` adds newest → heap now holds `[i+1, i+k+1]` ✓\n- Termination: After last element, heap drains completely, producing fully sorted output ✓\n\n---\n\n## Test Data Walkthrough\n\nInput: `[3, 2, 1, 6, 5, 4, 9, 8, 7]`, k=3\n\n| Step | Heap (values) | Output | Action |\n|------|---------------|--------|--------|\n| Init | [3, 2, 1] | [] | Push 3,2,1 |\n| i=3 | [2, 3, 1] | [1] | Pop 1, push 6 |\n| i=4 | [2, 6, 3] | [1, 2] | Pop 2, push 5 |\n| i=5 | [3, 6, 5] | [1, 2, 3] | Pop 3, push 4 |\n| i=6 | [4, 6, 5] | [1,2,3,4] | Pop 4, push 9 |\n| i=7 | [5, 6, 9] | [1,2,3,4,5] | Pop 5, push 8 |\n| i=8 | [6, 8, 9] | [1,2,3,4,5,6] | Pop 6, push 7 |\n| Drain | [] | [1,2,3,4,5,6,7,8,9] | Pop 7,8,9 |\n\n---\n\n## Key Interview Takeaways\n\n1. **Identify the constraint:** k limits disorder, not input size\n2. **Choose the right structure:** Min-heap finds smallest in O(log k)\n3. **Analyze correctly:** O(n log k) depends on k, not n\n4. **Know the tradeoffs:** Better for small k, streaming, memory-constrained, cache-sensitive scenarios",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-9",
        "questionId": "q-362",
        "question": "Given an array of integers, implement quicksort with proper partitioning, explain its O(n log n) average vs O(n²) worst-case complexity, and compare with mergesort in terms of stability, space usage, and practical performance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Maintain a map of (service_id, endpoint_id) to a fixed-size linked-list window of the last W seconds of latency samples (ts, latency). For each service_id, track totalLatency and totalCount across all its endpoints. A m…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a load balancer with active and passive health checks, maintain server state machines, and use request buffering or graceful drain to handle transitions without dropping requests.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Quicksort uses partitioning around a pivot. Average O(n log n) due to balanced splits, worst O(n²) with poor pivots (sorted data). Mergesort guarantees O(n log n) and is stable but needs O(n) extra space. Use quicksort…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "**Approach:** Use a min-heap to repeatedly extract the smallest element from the first k+1 items, maintaining sorted order. ```javascript class MinHeap { constructor() { this.a = []; } push(x) { this.a.push(x); this.bub…",
            "isCorrect": false
          }
        ],
        "explanation": "## Quicksort Implementation\n\n```python\ndef quicksort(arr, low=0, high=None):\n    if high is None: high = len(arr) - 1\n    if low < high:\n        pivot_idx = partition(arr, low, high)\n        quicksort(arr, low, pivot_idx - 1)\n        quicksort(arr, pivot_idx + 1, high)\n\ndef partition(arr, low, high):\n    # Lomuto partition scheme\n    pivot = arr[high]  # Choose last element as pivot\n    i = low - 1\n    for j in range(low, high):\n        if arr[j] <= pivot:\n            i += 1\n            arr[i], arr[j] = arr[j], arr[i]\n    arr[i + 1], arr[high] = arr[high], arr[i + 1]\n    return i + 1\n```\n\n## Pivot Selection Strategies\n\n- **Last element**: Simple but O(n²) worst case on sorted data\n- **Random element**: Good average case, minimal overhead\n- **Median-of-three**: Reduces probability of worst case\n- **Introsort**: Switch to heapsort when recursion depth exceeds 2log₂n\n\n## Complexity Analysis\n\n**Average Case O(n log n)**: Each partition splits array roughly in half, creating log n levels with O(n) work per level.\n\n**Worst Case O(n²)**: Consistently unbalanced partitions (already sorted array with poor pivot choice) create n levels of O(n) work.\n\n**Space Complexity**: O(log n) average (recursion stack), O(n) worst case.\n\n## Quicksort vs Mergesort\n\n| Factor | Quicksort | Mergesort |\n|--------|-----------|-----------|\n| Time Complexity | O(n log n) avg, O(n²) worst | O(n log n) guaranteed |\n| Space | O(log n) avg, O(n) worst | O(n) auxiliary |\n| Stability | Not stable | Stable |\n| Cache Performance | Excellent (in-place) | Poor (sequential access) |\n| Adaptivity | No | Partially adaptive |\n\n## Practical Considerations\n\n- Use **quicksort** for general-purpose sorting where average performance matters\n- Choose **mergesort** for external sorting, linked lists, or when stability required\n- **Timsort** (Python's default) combines mergesort with insertion sort for real-world data\n- Consider **introsort** (C++ std::sort) for guaranteed O(n log n) with fast average case",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-10",
        "questionId": "q-29060",
        "question": "You are given an R x C grid with non-negative integers and -1 representing blocked cells. Start at (0,0) and end at (R-1,C-1). You may move only right or down. The score of a path is the sum of visited cell values. You are allowed to make at most T turns (a direction change). Compute the maximum possible score under this constraint. If no valid path exists within T turns, return negative infinity or a sentinel value. Provide a memoized top-down solution and a bottom-up DP, with time/space complexities and edge cases?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Insert: create/extend nodes and increment a passCount at each node. Delete: if word exists, walk path and decrement passCount; prune nodes with zero. LCP query: walk query, stop when next child missin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Memoized top-down: state (i,j,t,last) with last in {R,D,None}. Recurse to (i+1,j) and (i,j+1) updating t when last != next. Skip blocked cells, use -INF for invalid. Cache results. Bottom-up: fill dp[",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a SlidingTopK path using: a fixed-size window (linked list) for expirations, a hashmap for frequencies, and a min-heap of size K for top-K with lazy deletions. On add and expiration, adjust co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use inorder traversal which visits nodes in sorted order. For O(h) space, use recursive DFS with counter. For O(1) space, use Morris traversal with threading. Both run in O(k) time average, O(n) worst case. Morris avoid…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests DP with path constraints: limited turns, two-move directions, and blocked cells.\n\n## Key Concepts\n\n- DP with states (i,j,t,dir)\n- transitions add value and optionally incur a turn\n- memoization versus tabulation with space optimizations\n\n## Code Example\n\n```javascript\nfunction maxPathWithTurns(grid, maxTurns){\n  // Outline: implement top-down with memo {(i,j,t,dir): best}\n  // and bottom-up DP filling dp[i][j][t][dir]\n  // Treat -1 as blocked (skip)\n}\n```\n\n## Follow-up Questions\n\n- How to extend to allow moves in four directions while counting turns to delta between consecutive moves?\n- How would you optimize for very large grids with small maxTurns, using a sparse DP representation?",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-11",
        "questionId": "q-17860",
        "question": "Design a sorting routine that guarantees O(n log n) worst-case in-place while efficiently handling duplicates. Implement a 3-way quicksort using a deterministic median-of-medians pivot and partition into <, =, > without extra memory. For subarrays larger than a threshold (e.g., 64), switch to an in-place bottom-up merge pass that merges adjacent runs. Provide core functions and analyze trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: maintain a ring buffer of 60 buckets; each bucket stores the set of (user_id, page_id) pairs seen in that second. A global map stores last_seen_ts per pair and a per-page total. On",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "External merge sort with a k-way merge using a min-heap. First, read 32 GB chunks into memory, sort each chunk using quicksort (O(n log n) in-memory), and write back 800+ sorted runs to disk. Then per",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a 3-way QuickSort with a deterministic median-of-medians pivot to bound partitions. In-place 3-way partitioning separates <, =, >, handling duplicates efficiently. For subarrays larger than a thre",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Start with QuickSort with depth limit based on log2(n) and a robust pivot like median-of-three. Use 3-way partitioning to handle duplicates. Monitor partition balance and run-lengths; switch to MergeS",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nExplain why a 3-way partition reduces comparisons on duplicates and how median-of-medians guarantees bound. \n\n## Key Concepts\n- 3-way partitioning, median-of-medians pivot, in-place merge of runs, space O(1).\n\n## Code Example\n````javascript\nfunction partition3(a, lo, hi){/*...*/}\n````\n\n## Follow-up Questions\n- How does choosing threshold affect cache locality and constants in practice?\n- Compare worst-case vs average-case costs for this hybrid approach.",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-12",
        "questionId": "q-23190",
        "question": "Implement a Trie-based autocomplete structure where each word has a mutable weight. Provide operations: insertWord(word, weight), updateWord(word, delta), and queryPrefix(prefix, k) to return the top-k words starting with prefix by weight (highest first), breaking ties lexicographically. Assume up to 2e5 words and total length up to 2e6. Describe data layouts, update mechanics, and complexities?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Introsort hybrid: implement quicksort with 3-way partition and a depth limit d = floor(2*log2(n)). If recursion depth exceeds d for a subarray, switch to heapsort on that subarray to guarantee O(n log",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "One effective approach is a Trie with per-node top-k structures and a word→weight map. insertWord traverses the word, creating nodes and updating each node's top-k; updateWord adjusts weights along th",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "HybridTopK uses a hash table mapping (id, category) → {ts, score} for O(1) lookup and update; a min-heap to track expiration times for lazy removal; a max-heap with lazy invalidation to maintain per-category totals; and…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Starting from the origin service, execute Dijkstra's algorithm on the weighted directed graph using a min-heap priority queue to compute the earliest arrival time for all services. The relaxation step follows: dist[v] =…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes designing a dynamic weighted autocomplete using a Trie with per-node top-k maintenance and efficient updates.\n\n## Key Concepts\n\n- Trie with per-node top-k structures to enable fast prefix queries\n- Mutable weights with insert and delta updates\n- Lexicographic tie-breakers for stable ordering\n- Time/space: insert/update O(L log M); query O(K log M) in practice; memory ~ O(totalChars + totalWords)\n\n## Code Example\n\n```javascript\nclass TrieNode {\n  constructor() {\n    this.next = new Map();\n    this.topK = []; // list of [weight, word], sorted\n  }\n}\n\nclass AutocompleteTrie {\n  constructor() {\n    this.root = new TrieNode();\n    this.words = new Map(); // word -> weight\n  }\n  insertWord(word, weight) {\n    // build path, update per-node topK along the way\n  }\n  updateWord(word, delta) {\n    // adjust weight, propagate changes; remove if non-positive\n  }\n  queryPrefix(prefix, k) {\n    // traverse to prefix node, return top k words by weight, tie-break by word\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you manage memory and time for 2e5 words with long prefixes?\n- How would you handle concurrent updates in a multi-threaded environment?",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-13",
        "questionId": "q-14794",
        "question": "Scenario: a manufacturing pipeline is a DAG of tasks. Each node i has duration d[i], each edge (u,v) has a setup s[u,v]. A task can start when all prerequisites finish and the maximum EFT[u]+s[u,v] over its incoming edges is achieved. EFT[v] = ES[v]+d[v], ES[v] = max_u(EFT[u]+s[u,v]); sources have EFT = d[i]. The project finish time is max EFT. Compute EFT for all nodes and the number of distinct critical schedules (mod 1e9+7), i.e., number of longest paths achieving the project finish time. Provide an O(N+M) DP?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "3-way partitioning groups duplicates into <, =, > to minimize swaps; median-of-three pivots reduces degenerate pivots; use tail recursion elimination to reduce stack usage; track depth and switch to s",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline a topo-sort based DP: compute EFT[v] = d[v] + max_{(u,v) in incoming}(EFT[u] + s[u,v]) (or d[v] if no incoming edges); count[v] = sum count[u] over all u where EFT[u] + s[u,v] equals the max; base: EFT[v]=d[v],…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a hash map from user_id to a per-user ledger (a small doubly linked list of (tag, sum) nodes) plus a ring buffer of time buckets for the last W seconds and a max-heap of (totalSum, user_id, lastTs",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement an in-place hybrid: quicksort with median-of-three and 3-way partitioning, track recursion depth, switch to heapsort when depth > 2 log2(n), and use insertion sort for small subarray",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to blend topological ordering with DP to compute the longest-path in a weighted DAG and count the number of critical paths, including multiple sources and modulo arithmetic.\n\n## Key Concepts\n- Longest-path in DAG with node durations and edge setup weights\n- Topological sort (Kahn) to process in dependency order\n- DP for counting number of longest paths with modulo\n\n## Code Example\n```javascript\nfunction countCriticalPaths(N, edges, dur) {\n  const MOD = 1000000007;\n  const incoming = Array.from({length: N}, () => []);\n  const indeg = Array(N).fill(0);\n  \n  for (const [u, v, w] of edges) {\n    incoming[v].push([u, w]);\n    indeg[v]++;\n  }\n  \n  const EFT = dur.slice();\n  const count = Array(N).fill(0);\n  const q = [];\n  \n  for (let i = 0; i < N; i++) {\n    if (indeg[i] === 0) {\n      count[i] = 1;\n      q.push(i);\n    }\n  }\n  \n  while (q.length) {\n    const u = q.shift();\n    for (const [v, w] of incoming[u]) {\n      const newEFT = EFT[u] + w + dur[v];\n      if (newEFT > EFT[v]) {\n        EFT[v] = newEFT;\n        count[v] = count[u];\n      } else if (newEFT === EFT[v]) {\n        count[v] = (count[v] + count[u]) % MOD;\n      }\n      indeg[u]--;\n      if (indeg[u] === 0) q.push(u);\n    }\n  }\n  \n  let maxEFT = 0, totalCritical = 0;\n  for (let i = 0; i < N; i++) {\n    if (EFT[i] > maxEFT) {\n      maxEFT = EFT[i];\n      totalCritical = count[i];\n    } else if (EFT[i] === maxEFT) {\n      totalCritical = (totalCritical + count[i]) % MOD;\n    }\n  }\n  return { EFT, maxEFT, criticalPaths: totalCritical };\n}\n```\n\n## Complexity\n- Time: O(N + M) for topological sort and DP\n- Space: O(N + M) for adjacency and arrays",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-14",
        "questionId": "q-22666",
        "question": "In a streaming analytics scenario, you have an array a[0..n-1] of non‑negative costs and a per‑session budget B. Count the number of ways to partition the entire sequence into contiguous sessions so that each session’s sum ≤ B. Return the count mod 1e9+7. Implement both bottom‑up DP (O(n) space) and a top‑down memoized version; discuss edge cases like zeros and very large B?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Define dp[0]=1. For i from 1..n, maintain a left index so that sum(a[left..i-1]) ≤ B; then dp[i] = sum(dp[left..i-1]). Use a sliding window to update both sum and left, yielding O(n) time and O(n) spa",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a dynamic Dijkstra variant with incremental updates and hierarchical decomposition, maintaining O(log n) per update and query.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-room doubly linked list of sender entries, a hash map (room_id, sender_id) -> node for dedup, per-room total, a max-heap of rooms by total with lazy updates, and a time-bucket queue for expi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a hashmap gid -> {dq: Deque of (ts, value), sum, count, ver}; a max-heap stores entries {avg, gid, ver}. On addEvent(ts, gid, val): push (ts,val) into dq, update sum/count, evict old events (ts <",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of partition DP with sliding window optimization and modular arithmetic, plus dual implementation styles.\n\n## Key Concepts\n\n- Contiguous partitions\n- Sliding window to enforce per-session bound\n- Prefix-sum style accumulation for O(n) time\n\n## Code Example\n\n```javascript\nfunction bottomUpCount(a, B) {\n  const n = a.length, MOD = 1000000007;\n  const dp = new Array(n+1).fill(0);\n  dp[0] = 1;\n  let left = 0, window = 0, acc = 0;\n  for (let i = 1; i <= n; i++) {\n    window += a[i-1];\n    while (window > B) {\n      window -= a[left];\n      acc = (acc - dp[left] + MOD) % MOD;\n      left++;\n    }\n    acc = (acc + dp[i-1]) % MOD;\n    dp[i] = acc;\n  }\n  return dp[n];\n}\n```\n\n## Follow-up Questions\n\n- How would you modify for 64‑bit sums to avoid overflow in JS/TS?\n- How does this pattern relate to monotone queue optimizations in similar counting problems?",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-15",
        "questionId": "q-18308",
        "question": "Given a string s and a dictionary of words dict, count how many different ways to segment s into a sequence of dictionary words. Return the count modulo 1e9+7. Provide a top-down memoized solution and a bottom-up tabulation sketch. Assume dict lookup is O(1) via a hash set and that n = len(s) up to 500?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Top-down: define dfs(i) as ways to segment s[i:]; if i==n return 1; otherwise sum dfs(j) for every j>i with s[i:j] in dict; use memo[i] to cache. Bottom-up: dp[n]=1; for i from n-1 to 0, for j from i+",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use DFS from s to prune to unblocked nodes, then Dijkstra on that subgraph to get dist[]. Build the SP DAG with edges (u,v) where dist[u]+w(u,v)=dist[v], topologically sort it (positive weights guaran",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Both operations run in O(length) time. Each trie node stores `subtreeSum` and `subtreeBest = (word, weight)`. For `add(word, delta)`: traverse the characters, creating nodes as needed, increment `subtreeSum` by delta at…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Compute a topological order of the DAG, then relax edges to propagate earliest start/finish times. Initialize earliestStart[node] = 0 and finish[node] = 0. For each u in topo: finish[u] = earliestStar",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThe problem tests basic DP on strings, counting segmentations, reinforcing memoization vs tabulation, handling modulo, and performance with s length up to 500. It also touches dictionary lookup optimization.\n\n## Key Concepts\n\n- Top-down memoization with index-based DP\n- Bottom-up tabulation\n- Modulo arithmetic and time/space complexity\n\n## Code Example\n\n```python\nMOD = 10**9+7\n\ndef count_ways(s, words):\n    wordset = set(words)\n    n = len(s)\n    memo = [-1]*(n+1)\n    def dfs(i):\n        if i==n: return 1\n        if memo[i]!=-1: return memo[i]\n        res = 0\n        for j in range(i+1, n+1):\n            if s[i:j] in wordset:\n                res = (res + dfs(j)) % MOD\n        memo[i] = res\n        return res\n    return dfs(0)\n\ndef count_ways_tab(s, words):\n    wordset = set(words)\n    n = len(s)\n    dp = [0]*(n+1)\n    dp[n] = 1\n    for i in range(n-1, -1, -1):\n        for j in range(i+1, n+1):\n            if s[i:j] in wordset:\n                dp[i] = (dp[i] + dp[j]) % MOD\n    return dp[0]\n```\n\n## Follow-up Questions\n\n- How would you optimize for very large dictionaries?\n- Extend to return all segmentations or limit by number of words?",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-16",
        "questionId": "q-596",
        "question": "Explain the differences between round-robin, least connections, and IP hash load balancing algorithms. When would you choose each one?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run Dijkstra from s to obtain dist[]. Build a DAG of edges (u,v) where dist[u] + w(u,v) = dist[v]. Topologically process nodes in increasing dist; initialize ways[s]=1 and propagate: ways[v]+=ways[u]",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use array with parent at i, children at 2i+1/2i+2. Insert: add to end, bubble up O(log n). extractMin: swap root with last, remove, bubble down O(log n). peek O(1). Space O(n). Handle empty heap, duplicate values, array…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Round-robin distributes requests evenly, least connections sends traffic to the server with fewest active connections, and IP hash routes clients to the same server based on their IP address.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a dynamic Dijkstra variant with incremental updates and hierarchical decomposition, maintaining O(log n) per update and query.",
            "isCorrect": false
          }
        ],
        "explanation": "Load balancing algorithms are crucial for distributing traffic across multiple servers efficiently. Round-robin is the simplest approach, cycling through servers sequentially. It works well for identical servers with similar processing capabilities and stateless applications. However, it doesn't account for varying server loads or request complexities.\n\nLeast connections algorithm monitors active connections and routes new requests to the server with the fewest current connections. This is ideal when requests have varying processing times or when servers have different capabilities. For example, in a microservices architecture where some endpoints are more resource-intensive, this prevents overloading a single server.\n\nIP hash uses the client's IP address to determine which server handles the request, ensuring session persistence. This is essential for applications requiring stateful connections, like shopping carts or user sessions. The hash function consistently maps the same IP to the same server, preventing session loss during load balancing.\n\nChoose round-robin for simple, stateless applications with uniform servers. Use least connections for heterogeneous environments or varying request complexities. Select IP hash when session persistence is critical, such as e-commerce platforms or authentication systems.",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-17",
        "questionId": "q-27404",
        "question": "Given an array cost of length n with non-negative integers and a budget B, you start at a virtual before-first-step and may jump to i+1 or i+2, paying cost at the landing step. Count the number of distinct paths that end at index n-1 with total cost exactly B, modulo 1e9+7. Implement bottom-up DP with O(n*B) time and O(B) space and a memoized DFS variant; discuss edge cases?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a cache-aware hybrid sort: partition the array into fixed-size blocks, sort each block in place with quicksort, then perform a bottom-up in-place merge across neighboring blocks using a small",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a sliding window with per-(user_id,item_id) pair counts. Use a global queue of events in ts order; a map pairCount[(u,i)] -> int; a map itemCount[item] -> int; a max-heap of (count,item). On",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run Dijkstra's algorithm from s to compute dist[], the shortest distance to all nodes. Build a shortest-path DAG H consisting of edges (u, v) where dist[u] + w(u, v) == dist[v]. Since all weights are positive, this form…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Bottom-up DP:** Use two rolling arrays of size B+1 to track the number of ways to reach each step with a given accumulated cost. Initialize: `cur[0][cost[0]] = 1` (from virtual position -1 to step 0), and if n>1, `cur…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis problem evaluates the ability to extend classic DP patterns (like the staircase problem) with an additional resource dimension, while also testing space optimization skills and recursive thinking via memoization. It distinguishes candidates who understand both bottom-up and top-down approaches, and who reason through edge cases systematically.\n\n## Key Concepts\n- DP with an extra budget constraint (two-dimensional state: position + accumulated cost)\n- Space optimization using rolling arrays to reduce O(n·B) space to O(B)\n- Memoization on composite state (i, b) for the recursive variant\n- Correct base case handling when starting from a virtual position before index 0\n\n## Code Example\n```javascript\nconst MOD = 1_000_000_007n;\n\nfunction countWays(cost, B) {\n  const n = cost.length;\n  if (n === 0) return 0;\n\n  // bottom-up with rolling arrays\n  let prev2 = new Array(B + 1).fill(0);\n  let prev1 = new Array(B + 1).fill(0);\n\n  // base: step 0\n  if (cost[0] <= B) prev2[cost[0]] = 1;\n\n  // base: step 1\n  if (n > 1 && cost[1] <= B) prev1[cost[1]] = 1;\n\n  // step 2 onwards\n  for (let i = 2; i < n; i++) {\n    const cur = new Array(B + 1).fill(0);\n    for (let b = cost[i]; b <= B; b++) {\n      cur[b] = (prev1[b - cost[i]] + prev2[b - cost[i]]) % MOD;\n    }\n    prev2 = prev1;\n    prev1 = cur;\n  }\n\n  return n === 1 ? prev2[B] : prev1[B];\n}\n\n// memoized DFS variant\nfunction countWaysDFS(cost, B) {\n  const n = cost.length;\n  const memo = new Map();\n\n  function dfs(i, b) {\n    if (b < 0) return 0;\n    if (i < 0) return b === 0 ? 1 : 0;\n    const key = `${i},${b}`;\n    if (memo.has(key)) return memo.get(key);\n    const result = (dfs(i - 1, b - cost[i]) + dfs(i - 2, b - cost[i])) % MOD;\n    memo.set(key, result);\n    return result;\n  }\n\n  return dfs(n - 1, B);\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-18",
        "questionId": "q-23485",
        "question": "Scenario: A DAG-structured transport network has non-negative costs on edges. From a source S, compute the minimum cost to reach every node using at most K edges. Propose an algorithm that uses a topological order and dynamic programming with a state per number of edges, derive the recurrence, show how to reconstruct the path, and discuss complexity. How would you adapt if cycles exist (SCC condensation and DP on the DAG or alternative methods)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Model as an augmented graph with states (node, k). From (u, k) you can traverse (u→v) with cost w(u,v); if k < K, also go to (v, k+1) with cost floor(w/2). Run Dijkstra on VK nodes, keep predecessors,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a hybrid sort that combines 3-way quicksort for large partitions with in-place stable merge sort for small partitions. For partitions larger than 64 elements, I'd use 3-way quicksort with median-of-thr…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Insert: create/extend nodes and increment a passCount at each node. Delete: if word exists, walk path and decrement passCount; prune nodes with zero. LCP query: walk query, stop when next child missin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Given a source S in a DAG with non-negative edge costs, compute the minimum cost to reach every node using at most K edges using topological DP: initialize dist[S][0] = 0 and dist[v][0] = ∞ for all v ≠ S. For each t fro…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to combine multiple graph algorithms: topological ordering, dynamic programming on DAGs, and hop-constrained shortest path computation. It also probes understanding of handling cycles through SCC condensation and requires consideration of time/space trade-offs.\n\n## Key Concepts\n\n- **Topological sort**: Enables processing DAG nodes in a valid order where all predecessors come before successors\n- **DP over edge count**: State dist[v][t] represents minimum cost to reach v using exactly t edges; the \"at most K\" variant takes the minimum across t = 0..K\n- **Path reconstruction**: Maintain predecessor[v][t] during computation to trace back the optimal path\n\n## Complexity\n\n- Time: O(K · (V + E)) — K DP layers, each processed in topological order O(V + E)\n- Space: O(K · V) for the DP table, plus O(K · V) for predecessor tracking\n\n## Handling Cycles (SCC Condensation)\n\nIf cycles exist, first condense the graph into SCCs, producing a DAG. Apply the same DP on this DAG, where each SCC becomes a node with internal shortest paths precomputed (e.g., via Dijkstra since internal costs are non-negative). Alternatively, use modified Bellman-Ford (running for K iterations) which handles cycles but runs in O(K · E) time.",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-19",
        "questionId": "q-653",
        "question": "How would you implement a consistent hashing load balancer that handles server additions and removals with minimal key remapping? What data structures would you use and how would you handle virtual nodes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bottom-up merge sort on the array of {score, time} objects, merging by score and taking the left element on ties to preserve stability. Allocate one auxiliary array of size n and reuse it per pa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Kahn’s topological sort to obtain an order. Build adjacency and in-degree, set finish[i] = durations[i] for all i. For each node u in topo order, for each v in adj[u], set finish[v] = max(finish[v",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use topological dynamic programming: compute earliest finish times by processing nodes in topological order, where each node's earliest finish equals its duration plus the maximum earliest finish of all predecessors. So…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a hash ring with TreeMap/SortedMap for O(log n) lookups, implement virtual nodes to distribute load evenly, and only remap keys between adjacent servers when topology changes.",
            "isCorrect": true
          }
        ],
        "explanation": "Consistent hashing solves the hot-spot problem of traditional hash-based load balancing by distributing keys across a circular hash space. The core implementation uses a sorted data structure (like TreeMap in Java or std::map in C++) to maintain the ring, allowing O(log n) lookup for which server should handle a given key. Each server is placed on the ring at multiple virtual node positions to ensure more uniform distribution.\n\nWhen a server is added, you only need to remap keys that would have gone to the next server clockwise on the ring. Similarly, when a server fails, its keys are redistributed to the next server clockwise. This minimizes the disruption compared to traditional hashing where adding/removing a server requires remapping all keys. Virtual nodes (typically 100-200 per physical server) help ensure even distribution and handle scenarios where physical servers have different capacities.\n\nIn practice, you'd hash both the server identifier and a virtual node index (like 'server1-vnode-0', 'server1-vnode-1') to place multiple points for each server. The choice of hash function (CRC32, MurmurHash, FNV) affects the distribution quality. For production systems, you'd also need to handle server health checks, weighted distribution for heterogeneous hardware, and potentially implement bounded loads to prevent any single server from becoming overloaded.",
        "difficulty": "intermediate"
      },
      {
        "id": "algorithms-q-20",
        "questionId": "q-10258",
        "question": "In a service-call graph for a distributed system (nodes are services, directed edges carry propagation latency; scenario inspired by internal deployments at MongoDB/Zoom), an origin service issues a config change. Design an algorithm to compute the earliest arrival time for all services, assuming immediate forwarding on receipt and respecting dependency constraints. Explain how you'd use BFS/DFS to explore reachability, Dijkstra for shortest propagation times, and topological sort if the graph is a DAG. Include data structures and complexity?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Starting from the origin service, execute Dijkstra's algorithm on the weighted directed graph using a min-heap priority queue to compute the earliest arrival time for all services. The relaxation step follows: dist[v] =…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Problem: Deadline-Constrained Task Scheduling Given N tasks where each task i has: - Duration `t[i]` (time needed to complete) - Deadline `d[i]` (must finish by this time) - Value `v[i]` (benefit earned) Schedule tas…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Bottom-up: use a hashmap mapping prefix sums to total ways up to the current index. Initialize map[0]=1, prefix=0. For i from 1..N, prefix+=A[i-1]; dp[i] = map[prefix-K] ?? 0; map[prefix] = (map[prefi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design and implement a hybrid sorter that starts with quicksort but switches to mergesort when the recursion depth exceeds 2 log2 n or a partition is imbalanced. Guarantee worst-case O(n log n) time a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical graph propagation logic under latency constraints, combining reachability (BFS/DFS), shortest-path (Dijkstra), and DAG optimizations (topological order). Mirrors real-world config propagation in large services (MongoDB/Zoom).\n\n## Key Concepts\n\n- Reachability via BFS/DFS\n- Dijkstra for earliest arrival with positive weights\n- Topological sort for DAGs\n- Cycle detection and handling in general graphs\n- Data structures: adjacency list, min-heap, dist/prev arrays\n\n## Code Example\n\n```javascript\nfunction propagate(graph, start){\n  // Dijkstra-like relaxation on weighted graph\n}",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-21",
        "questionId": "q-8878",
        "question": "You're given an array of N integers that may contain many duplicates. Implement an in-place quicksort variant with 3-way partitioning (Dutch national flag style) to sort ascending. Use a randomized pivot to avoid worst-case. Explain how you partition into <, ==, > and why this reduces comparisons for duplicates. Provide a short, correct implementation in a language of your choice?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Compute prefix sums to allow O(1) range sums. Let dp[i][j] be the minimum cost to merge stones[i..j] into one pile. Base: if i == j, cost 0. For length L from 2 to n, for all i with j = i+L-1, set dp[",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Trie node with a map of children and a boolean word-end flag. Insertion is O(len(word)). For autocomplete, traverse to the prefix in O(len(prefix)) and DFS over children in lexicographic order,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a depth-limited quicksort with a cache-aware partitioning threshold, switching to a single-pass bottom-up mergesort for large partitions. Mergesort maintains stability by merging equal keys in ori",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Select a random pivot and partition the array into three regions: elements less than the pivot, elements equal to the pivot, and elements greater than the pivot. Recursively sort the less-than and greater-than regions w…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nDuplicate values are common in real-world data sets. Traditional 2-way quicksort performs poorly with many duplicates, leading to unnecessary comparisons and degraded performance. 3-way partitioning addresses this by grouping equal elements together, significantly reducing the algorithm's work.\n\n## Key Concepts\n\n- **3-way partitioning (Dutch national flag algorithm)**: Separates array into < pivot, == pivot, and > pivot regions\n- **Randomized pivot selection**: Prevents worst-case O(n²) scenarios on already sorted or adversarial input\n- **In-place sorting**: Operates with O(1) additional space, using only O(log n) expected stack depth\n- **Performance optimization**: Reduces comparisons from O(n log n) to O(n) when many duplicates exist\n\n## Code Example\n\n```javascript\nfunction quicksort3(a, lo = 0, hi = a.length - 1) {\n  if (lo >= hi) return;\n  \n  // Random pivot selection\n  const pivot = a[Math.floor(Math.random() * (hi - lo + 1)) + lo];\n  let lt = lo, i = lo, gt = hi;\n  \n  // 3-way partitioning\n  while (i <= gt) {\n    if (a[i] < pivot) {\n      [a[lt], a[i]] = [a[i], a[lt]];\n      lt++;\n      i++;\n    } else if (a[i] > pivot) {\n      [a[i], a[gt]] = [a[gt], a[i]];\n      gt--;\n    } else {\n      i++; // a[i] === pivot\n    }\n  }\n  \n  // Recursively sort subarrays\n  quicksort3(a, lo, lt - 1);\n  quicksort3(a, gt + 1, hi);\n}\n```\n\n## Partitioning Strategy\n\nThe algorithm maintains three regions during partitioning:\n- `[lo, lt-1]`: elements < pivot\n- `[lt, gt]`: elements === pivot (being processed)\n- `[gt+1, hi]`: elements > pivot\n\nAs we iterate, elements are swapped into their appropriate region, with equal elements naturally forming the middle section that needs no further processing.",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-22",
        "questionId": "q-28291",
        "question": "You are building a streaming analytics engine. Each event has (user_id: string, category: string, value: number, ts: number). Implement a data structure WindowedTopK with a sliding window of W seconds that supports: insertEvent(ts: number, user_id: string, category: string, value: number); advanceTime(now: number) to expire events older than now-W; topK(now: number, K: number) returning the top-K user_ids by total value across all categories in the window (tie-break by userid). Design using only arrays, linked lists, hashtables, and a heap. Describe operations, edge cases, and time/space complexity?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement 3-way quicksort to sort A with many duplicates. Use pivot p and partition into <p, =p, >p; cluster equals in the middle, avoiding extra passes. This reduces expected time to O(n log n) even",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a sweep line algorithm: collect all start and end times, sort them chronologically, then iterate through the timeline while maintaining a count of active events. Increment the count for each start time and decrement…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design WindowedTopK using a circular array of W buckets, where each bucket holds a linked list of events. Maintain two supporting structures: a hashmap `totalsPerId` that maps each user_id to its total value across all…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement an exponential weighted moving average (EWMA) algorithm to track server response times, calculate dynamic weights for traffic distribution based on these metrics, and apply gradual traffic ramp-up strategies f…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis problem tests your ability to combine multiple data structures to solve a real-world analytics challenge: maintaining sliding window aggregations with fast top-K retrieval under tight latency constraints.\n\n## Key Concepts\n\n1. **Circular bucket array** enables O(1) bucketing and automatic expiration of old events\n2. **Hashmap aggregation** provides O(1) lookup and update for per-user totals across categories\n3. **Max-heap with lazy deletion** avoids the overhead of rebuilding the heap on every expiration; stale entries are filtered on-demand during `topK`\n4. **Lazy expiration** ensures we never scan individual events during query time—only expired buckets trigger cleanup\n\n## Code Example\n\n```javascript\nclass WindowedTopK {\n  constructor(W) {\n    this.W = W;\n    this.buckets = new Array(W).fill(null).map(() => ({ head: null, tail: null, ts: null }));\n    this.totals = new Map();\n    this.heap = [];\n    this.head = 0;\n  }\n\n  insertEvent(ts, userId, category, value) {\n    const idx = ts % this.W;\n    const bucket = this.buckets[idx];\n    const event = { userId, category, value, ts, next: null };\n    if (!bucket.head) bucket.head = bucket.tail = event;\n    else bucket.tail = bucket.tail.next = event;\n    bucket.ts = ts;\n\n    this.totals.set(userId, (this.totals.get(userId) || 0) + value);\n    this.heap.push({ total: this.totals.get(userId), userId, ts });\n  }\n\n  advanceTime(now) {\n    const cutoff = now - this.W;\n    while (this.buckets[this.head].ts !== null && this.buckets[this.head].ts < cutoff) {\n      let event = this.buckets[this.head].head;\n      while (event) {\n        this.totals.set(event.userId, (this.totals.get(event.userId) || 0) - event.value);\n        event = event.next;\n      }\n      this.buckets[this.head] = { head: null, tail: null, ts: null };\n      this.head = (this.head + 1) % this.W;\n    }\n  }\n\n  topK(now, K) {\n    this.advanceTime(now);\n    const result = [];\n    let i = 0;\n    while (result.length < K && i < this.heap.length) {\n      const entry = this.heap[i++];\n      if (this.totals.get(entry.userId) === entry.total) {\n        result.push(entry.userId);\n      }\n    }\n    return result;\n  }\n}\n```\n\n## Follow-up Questions\n\n- **Per-category top-K**: Maintain separate totals per `(userId, category)` and separate heaps for each category\n- **Bounded memory**: Add a periodic rebuild phase or use LRU eviction for rarely-active user_ids\n- **Distributed extension**: Partition by user_id hash across nodes, aggregate partial results",
        "difficulty": "advanced"
      },
      {
        "id": "algorithms-q-23",
        "questionId": "q-350",
        "question": "Given a directed graph representing city intersections and one-way streets, implement a function to find if there's a valid route from point A to point B using BFS. Return the shortest path distance or -1 if no route exists?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use dp[i] = number of ways to reach step i. Base dp[0] = 1. If step i is blocked, dp[i] = 0. Otherwise, dp[i] = sum of dp[i - t] for t in 1..maxJump with i - t >= 0. Memoized: count(i) caches results;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use BFS with a queue to explore the graph level by level, tracking visited nodes and distances from the start. Return the distance when the target node is found, or -1 if all reachable nodes are exhausted without findin…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Count paths from index 0 to n-1 in a linear array where each cell is either free (0) or blocked (1). You may move 1 or 2 steps at a time. Return the number of distinct paths modulo 1e9+7. **Memoized DFS:** Define f(i) a…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a top-down memoized solution: countWays(i) returns the number of segmentations of s[i:] using dict. Iterate lengths up to maxWordLen and check substrings against dict, caching result",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests fundamental graph traversal algorithms, BFS implementation, and pathfinding skills - critical for autonomous vehicle navigation systems and route planning applications.\n\n## Expected Answer\nThe candidate should implement BFS using a queue, maintain a visited set to avoid cycles, track distances from the start node, and handle edge cases such as when start equals target, disconnected graphs, or empty input. They should also explain why BFS guarantees the shortest path in unweighted graphs.\n\n## Code Example\n```python\nfrom collections import deque\n\ndef shortest_path(graph, start, end):\n    if start == end:\n        return 0\n    \n    queue = deque([(start, 0)])\n    visited = {start}\n    \n    while queue:\n        node, distance = queue.popleft()\n        \n        for neighbor in graph.get(node, []):\n            if neighbor == end:\n                return distance + 1\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append((neighbor, distance + 1))\n    \n    return -1\n```",
        "difficulty": "beginner"
      },
      {
        "id": "algorithms-q-24",
        "questionId": "q-10499",
        "question": "Implement a dynamic multiset of integers with operations insert(x), delete(x) (one occurrence), and range queries querySum(L,R) and queryCount(L,R) in O(log N). Explain how you'd implement it using an AVL tree augmented with per-node fields: count (duplicates), subtree_size, subtree_sum, and rotations to maintain balance. Provide interface sketches and complexities?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Consistent hashing maps servers and keys to a virtual ring, ensuring only 1/n of data moves when servers change. Trade-offs include larger rings for better distribution vs. slower lookups.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "One effective approach is a Trie with per-node top-k structures and a word→weight map. insertWord traverses the word, creating nodes and updating each node's top-k; updateWord adjusts weights along th",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Dijkstra on a 2-layer graph: each node v has (v,0) before visiting any landmark and (v,1) after visiting one. From (u,0) to (v,0) if v is not a landmark; to (v,1) if v is a landmark; from (u,1) to (v,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An AVL-based multiset stores each value once, with a count for duplicates. Each node tracks count, subtree size, and subtree sum. Insert increments count or creates a node; delete decrements or remove",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests augmenting BSTs with order-statistics and range queries, handling duplicates, and rotations for rebalancing.\n\n## Key Concepts\n- AVL balancing and rotations\n- Node augmentation: count, size, sum\n- Prefix sums for range queries\n- Duplicates in BST via count fields\n\n## Code Example\n```javascript\nclass AVLNode { constructor(key){ this.key=key; this.cnt=1; this.left=null; this.right=null; this.h=0; this.sz=1; this.sm=key; } }\n```\n\n## Follow-up Questions\n- How would you support k-th smallest in O(log N)?\n- How would you adapt to dynamic key changes without deletion?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-17T13:24:44.590Z",
    "version": 1
  }
]