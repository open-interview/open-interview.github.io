[
  {
    "id": "test-operating-systems",
    "channelId": "operating-systems",
    "channelName": "Operating Systems",
    "title": "Operating Systems Knowledge Test",
    "description": "Test your understanding of Operating Systems with 25 questions.",
    "questions": [
      {
        "id": "operating-systems-q-0",
        "questionId": "q-29459",
        "question": "In a Linux-like kernel running on a NUMA system with heterogeneous cores and per-core memory domains, design a scalable, NUMA-aware lazy paging fault handler for a shared anonymous mapping accessed by many processes across nodes. The handler should serialize duplicates, leverage per-node IO queues, and implement per-page soft states to avoid redundant fetches. Describe end-to-end fault flow, per-page state machine, race conditions, source selection (IO vs zero-fill vs read-ahead), and a minimal C-like sketch of the fault path and per-page state transitions. How would you validate correctness and performance under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Interleaving: V0 = 0x00000000_00000000. A writes high 32 bits to 0x00000001; B reads; low 32 still 0, so B sees 0x00000001_00000000—a torn value. Minimal fixes: (1) atomic 64-bit stores on 64-bit CPUs",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "On a miss, acquire a per-page lock; if another fault wins, wait for present flag. If not present, allocate a frame, read from backing store or zero-fill, map it, set present, and return. Use a CAS/ato",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Process scheduling manages CPU allocation among processes by determining which process gets CPU time and when. For real-time systems, I would choose **Rate Monotonic Scheduling (RMS)** because it's predictable, has boun…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On a fault, acquire a per-page lock; CAS-state NONE->PENDING and enqueue an IO task to the local NUMA node. If data exists, map; if not, zero-fill while IO in flight. On IO completion, flip state to V",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical NUMA-aware memory management, concurrency, and IO paths under contention. It requires precise state machines and race-free synchronization.\n\n## Key Concepts\n\n- NUMA-aware allocation\n- per-page state machine NONE/PENDING/VALID\n- CAS-based serialization to avoid duplicate fetch\n- cross-node IO queues and read-ahead\n\n## Code Example\n\n```c\n// Minimal sketch\ntypedef enum { NONE, PENDING, VALID } page_state_t;\nstruct page { atomic_int state; void *frame; int io_inflight; };\nvoid handle_fault(struct page *p) { if (cas(&p->state, NONE, PENDING)) { enqueue_io(p); } else { wait_on_page(p); } }\n```\n\n## Follow-up Questions\n\n- How would you test contention and ensure liveness?\n- How to extend for write-back coherency and cache eviction?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-1",
        "questionId": "q-12641",
        "question": "Design a NUMA-aware, kernel-assisted I/O scheduler for a two-socket system. The workload mixes streaming video reads (high bandwidth, low latency) and metadata writes (lower bandwidth, stricter latency). Propose per-NUMA data structures, cross-CPU sharing, and a deadline-based policy to meet both QoS targets. Describe fault-tolerance, preemption, and provide a minimal C-like enqueue/dispatch sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a high-performance, zero-copy IPC channel using a lock-free ring buffer backed by huge pages, optimized for NUMA-aware systems with container namespace support. Each slot is aligned to a cache line (64 bytes) to…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-NUMA I/O queues with a global deadline scheduler. Route streaming reads to the nearest NUMA node and bound latency with service envelopes; run metadata writes in the adjacent node with b",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a per-tenant, per-queue path: map flows to fixed TX queues with a hash; preserve per-flow order using monotonic sequence numbers and per-flow credits; enforce tenant rate limits via a token-buc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a NUMA-aware page cache eviction policy with per-container quotas. On a page fault, look up local node cache; if miss, fetch from disk and assign to container quota. Eviction uses a global LRU",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDesign a NUMA-aware I/O scheduler for mixed workloads; focuses on latency, throughput, and per-NUMA fairness. \n\n## Key Concepts\n- NUMA-aware scheduling\n- Deadline-based QoS\n- Read-ahead tuning and backpressure\n- Lock-free counters and back-end IO paths\n\n## Code Example\n```javascript\n// Pseudo sketch: per-NUMA enqueue and dispatch\nfunction enqueue(ioReq) {\n  // place into per-numa queue; update atomic counters\n}\nfunction pickNext(numa) {\n  // select highest-priority by deadline from ready queue\n}\n```\n\n## Follow-up Questions\n- How would you test QoS guarantees under contention?\n- How to adapt to dynamic NUMA topology changes and device hot-plug?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-2",
        "questionId": "q-24825",
        "question": "In a toy OS with a fixed-size per-process page cache, describe the end-to-end path of a page fault when a non-resident page is accessed for the first time. Include fault detection, frame allocation, IO path from swap/file or zero-fill, and how two concurrent faults avoid duplicative IO, ensuring correctness and throughput under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "End-to-end: a fault occurs, non-present PTE detected; acquire per-page lock and allocate a frame. If backed by swap/file, issue IO to fetch it; else zero-fill. After IO completes, update the PTE, set",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-size bins with per-CPU freelists; local fast path first, then refill from global pool; on free, return to local if space, else push to global; quarantine for recently freed objects; use CAS-based",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a circular timer wheel with B buckets. Each timer holds expiry_tick, task, and next. On insert, compute delta = expiry - now; bucket[(now+delta) % B].push(timer). On each timer tick, process bucke",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-tenant memory pressure notifier: enforce per-container quotas (cgroups) with a NUMA hint. On pressure threshold, enqueue work into per-CPU batch queues and reclaim or migrate pages in bulk. Apply",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of page fault handling, page cache coordination, IO paths, and race condition handling under contention.\n\n## Key Concepts\n- Page fault handling workflow: detection, frame allocation, IO vs zero-fill\n- Per-page locking and CAS-based inflight signaling to serialize IO\n- PTE updates, TLB invalidation, and waking waiters\n- Throughput considerations under concurrent faults\n\n## Code Example\n```c\n// Minimal sketch of per-page fault handling (pseudo-C)\ntypedef struct { int inflight; void* frame; int swapped; } Page;\nvoid handle_fault(Page* p) {\n  if (atomic_compare_and_swap(&p->inflight, 0, 1) == 0) {\n    p->frame = allocate_frame();\n    if (p->swapped) read_from_storage(p->frame);\n    else zero_fill(p->frame);\n    update_pte(p, p->frame);\n    invalidate_tlb();\n    wake_waiters(p);\n    p->inflight = 0;\n  } else {\n    wait_for_completion(p);\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test correctness with multiple concurrent faulting threads?\n- What are potential race conditions besides inflight handling, and how would you mitigate them?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-3",
        "questionId": "q-22033",
        "question": "Implement a beginner sleep(ms) system call in a tiny OS. Describe how you track wake times (timer queue, min-heap, or wheel), how the timer interrupt wakes sleeping processes, and how to avoid races between concurrent sleepers and wakeups. Provide a minimal pseudo-code sketch for sleep() and the timer handler, and outline a simple test plan to verify wakeups occur on time under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a Round-Robin cooperative scheduler with a fixed-size ready queue, per-task context, and explicit yield points. Implement enqueue/dequeue, a yield that saves context and re-enables the next ta",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement sleep with a wake_time field and a timer queue (min-heap). sleep(ms) computes wake_time = now() + ms and inserts a (pid, wake_time) entry. The timer interrupt pops all entries with wake_time",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a two-phase, sense-reversing barrier with a shared arrival counter and a global sense flag. Each thread maintains a local sense bit initialized to false. On `barrier_wait()`, a thread atomically increments the arriv…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On a MAP_SHARED mmap write, the kernel first handles a page fault if the page isn't present, loading it from the backing file or device. The kernel then maps the page into the process's address space, updates the page t…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of kernel scheduling, timer hardware, and concurrency.\n\n## Key Concepts\n\n- Timer queues and wakeups\n- Concurrency and synchronization\n- Testing timing guarantees\n\n## Code Example\n\n```javascript\n// Implementation code here\n```\n\n## Follow-up Questions\n\n- How would clock drift affect wakeups?\n- How would you test fairness with many sleepers?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-4",
        "questionId": "q-6184",
        "question": "Design and implement a per-cgroup aware page cache eviction policy for a shared file-backed cache in a Linux-like kernel. Each cgroup has a memory cap for cached pages; the system must still support read-ahead and multi-thread contention, and must avoid starvation of small cgroups. Describe end-to-end workflow from a cache miss on a process within a cgroup to victim selection and eviction, including per-page state, synchronization, how to account charges, and testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On a miss, acquire a per-page lock; if another fault wins, wait for present flag. If not present, allocate a frame, read from backing store or zero-fill, map it, set present, and return. Use a CAS/ato",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Allocate 8 MB per thread with a guard page below the stack. A touch on the guard triggers a page fault; runtime unwinds thread-local state, marks the thread dead, cleans resources, and yields to the s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "On a first-touch fault, the handler CASes the per-page slot from EMPTY to LOADING, wakes waiters, and fetches the page from the remote cache or disk with optional read-ahead. After a successful fetch,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On a miss, allocate a page, attach it to the requesting cgroup, and charge that cgroup. Track per-page flags (ref, dirty, exclusive) and per-cgroup watermarks. Eviction uses a hierarchical LRU with pe",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design memory accounting and eviction under multi-tenant workloads, a common production pressure point. It evaluates correctness, performance trade-offs, and how to prevent starvation while honoring isolation guarantees.\n\n## Key Concepts\n- Per-cgroup memory accounting and quotas\n- Shared file-backed page cache eviction\n- Per-page state and synchronization\n- Read-ahead interactions and fairness under contention\n- Testing under memory pressure and mixed workloads\n\n## Code Example\n```javascript\n// Pseudo sketch: decide_eviction_target(cgroup, cache)\nfunction decide_eviction_target(cgroup, cache){\n  // choose victim from cgroup's pages with lowest refcount and clean state\n  let candidates = cache.pages.filter(p => p.owner === cgroup && p.refcount <= 1 && !p.dirty);\n  return candidates.sort((a,b)=> a.lastUsed - b.lastUsed)[0];\n}\n```\n\n## Follow-up Questions\n- How would you handle page-dirty handling and write-back races during eviction?\n- How would you extend the policy to prioritize hot pages across multiple cgroups while enforcing caps?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-5",
        "questionId": "q-5001",
        "question": "Two worker threads share a single io_uring instance and submit I/O to the same file descriptor concurrently. Describe a concrete interleaving where two threads call io_uring_get_sqe() and io_uring_submit() in overlapping fashion, racing the tail pointer and potentially losing a submission or delivering out-of-order completions. What exact synchronization and memory-barrier guarantees are required to ensure atomic reservation of SQEs, and how would you structure code to guarantee in-order completions for logging while preserving high throughput?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "One robust approach is to use a per-thread pending signal flag and a wake mechanism. On a signal, atomically set pending and wake the blocked read. The read loop checks for pending signals after each",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two worker threads contend for the submission queue tail pointer. Thread A obtains an SQE slot and writes its submission entry, while Thread B races to advance the kernel tail pointer and submit. Without proper memory b…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "End-to-end path: on first touch, verify container quota; if quota available, pick destination node (least-loaded with spare capacity and lowest migration cost); use CAS to atomically claim migration ownership; allocate…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A per-process token bucket with atomic ops. Refill happens on a per-second timer; tokens = min(tokens + refill, bucket_size). On IO submission, if tokens >= size, consume tokens and submit; else enque",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nRaces in io_uring multi-producer submission and memory ordering are subtle in real systems; candidates demonstrate understanding of ring buffers, memory barriers, and ordering guarantees.\n\n## Key Concepts\n- io_uring submission and completion rings (SQ/CQ)\n- Multi-producer correctness in shared ring buffers\n- Memory barriers (write/read, acquire/release semantics)\n- In-order completion guarantees and request tagging\n\n## Code Example\n```c\n// Example: multi-producer submission with atomic SQE reservation\nstruct io_uring ring;\npthread_mutex_t sqe_mutex = PTHREAD_MUTEX_INITIALIZER;\n\n// Atomic SQE reservation with sequence numbers\nstatic atomic_uint_fast64_t seq_counter = ATOMIC_VAR_INIT(0);\n\nint submit_io_safe(int fd, void *buf, size_t len, uint64_t *seq_out) {\n    // Reserve SQE atomically\n    pthread_mutex_lock(&sqe_mutex);\n    struct io_uring_sqe *sqe = io_uring_get_sqe(&ring);\n    if (!sqe) {\n        pthread_mutex_unlock(&sqe_mutex);\n        return -EBUSY;\n    }\n    \n    // Assign sequence number for ordering\n    uint64_t seq = atomic_fetch_add_explicit(&seq_counter, 1, \n                                            memory_order_relaxed);\n    *seq_out = seq;\n    \n    // Prepare submission with sequence tag\n    io_uring_prep_write(sqe, fd, buf, len, 0);\n    sqe->user_data = seq;\n    \n    // Release barrier before submit\n    atomic_thread_fence(memory_order_release);\n    int ret = io_uring_submit(&ring);\n    pthread_mutex_unlock(&sqe_mutex);\n    \n    return ret;\n}\n\n// Completion processing with ordering\nvoid process_completions(void) {\n    struct io_uring_cqe *cqe;\n    \n    while (io_uring_peek_cqe(&ring, &cqe) == 0) {\n        // Acquire barrier before reading completion\n        atomic_thread_fence(memory_order_acquire);\n        \n        uint64_t seq = cqe->user_data;\n        // Process completion in sequence order...\n        \n        io_uring_cqe_seen(&ring, cqe);\n    }\n}\n```\n\n## Implementation Strategy\nFor high-throughput ordered logging, implement per-thread submission queues with a single consumer thread that preserves order while allowing parallel I/O preparation.",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-6",
        "questionId": "q-1759",
        "question": "In a multithreaded server, each worker maintains a per-thread stats counter in an array of N 64-byte structs (one per thread). A separate thread periodically sums these counters every second. Without padding, explain a concrete interleaving that leads to cache line false sharing and degraded throughput, and propose a fix (padding, alignas cache-line, or per-thread local counters plus a reduction) that preserves correctness and improves performance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "False sharing occurs when multiple threads write to nearby fields within a single cache line, causing repeated invalidations and cross-core traffic. If thread i updates its own counter in a shared lin",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "An interleaving: a writer performs a compare-and-swap to publish a new node pointer before fully initializing its fields; a reader loads the pointer and dereferences it before initialization completes, reading partially…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-page state: is_compressed, compressed_size, density. On eviction or lazy compression, CAS-mark the page, compress with a fast algorithm (e.g., LZ4), store data in swap cache, set flags. On fault,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an MCS-style queue lock with per-thread stack-allocated nodes, linking to the predecessor and spinning on a private flag. Place each node in the thread’s NUMA domain and pad to a cache line to red",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes practical understanding of cache coherence and false sharing in real multithreaded code.\n\n## Key Concepts\n- False sharing\n- Cache coherence protocols (MESI)\n- Data padding and alignment\n- Reduction patterns across threads\n\n## Code Example\n```c\ntypedef struct {\n  long long count;\n} PerThreadStat;\n```\n\n```c\ntypedef struct {\n  long long count;\n  char pad[64 - sizeof(long long)];\n} PerThreadStatPadded;\n```\n\n## Follow-up Questions\n- How would you measure throughput improvements after padding?\n- What changes if thread count isn’t a multiple of 64?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-7",
        "questionId": "q-10531",
        "question": "In a toy OS with a single filesystem driver and multiple block devices, design a prefetch-aware I/O scheduler that serves disk requests with locality-based read-ahead. Outline data structures to track recent access per device, a policy to kick off prefetches without starving other I/Os, and a minimal C-like sketch for enqueue, dequeue, and dispatch that demonstrates how prefetch decisions integrate with the main path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a circular timer wheel with B buckets. Each timer holds expiry_tick, task, and next. On insert, compute delta = expiry - now; bucket[(now+delta) % B].push(timer). On each timer tick, process bucke",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-CPU write-back cache with a small on-disk journal. Implement: 1) block-tagged cache lines with dirty bits; 2) a write-back queue flushed in batches; 3) two-phase commit via a journal.bin on disk,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-core energy budgeting using PMU counters; track energy per task in a quanta window. On dispatch, increment by energy delta; if over quota, throttle (short yield or suspend) until reset. Use per-co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose per-device queues with a small read-ahead window (e.g., 4–8 blocks) driven by locality. Use per-device counters to cap prefetches and a global limiter to prevent starvation. Provide a minimal",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nDesigning a prefetch-aware I/O scheduler tests practical trade-offs between locality and fairness in a concrete, testable path from submission to completion.\n\n## Key Concepts\n- I/O scheduling and read-ahead\n- locality-aware prefetch policies\n- starvation prevention and fairness\n- per-device queues and a simple ring-buffer implementation\n\n## Code Example\n```javascript\n// Minimal sketch for a per-device ring queue\ntypedef struct { int block; int next; } BlockNode;\ntypedef struct { BlockNode ring[64]; int head; int tail; } DeviceQueue;\n```\n\n## Follow-up Questions\n- How would you extend the policy for multiple devices with shared bandwidth?\n- How would you evaluate latency, throughput, and starvation in a test harness?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-8",
        "questionId": "q-26318",
        "question": "Design and implement a reusable barrier primitive for a two-core SMP in a toy OS. It must support multiple rounds, use a sense-reversing approach, rely on atomic operations, and ensure all participating CPUs complete each round before any proceed. Provide a minimal C-like init and wait() sketch and discuss correctness under memory ordering on x86 vs ARM?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-CPU hierarchical timer wheel with 1024 slots and a global rollover bucket; timers install into the current CPU's slot and slip into higher granularity as time advances. Use per-timer gen",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Answer: Build a NUMA-aware global hash cache keyed by (dataset_id, page) with per-page refcounts and per-service quotas. First-touch uses CAS on a 'claimed' flag to serialize IO. If quota is full, evi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-page state machine with an atomic state (INVALID, LOADING, LOADED) and a tiny per-page lock plus a wait queue. On a fault, a thread wins the lock, sets LOADING, issues IO, fills the page, th",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a two-phase, sense-reversing barrier with a shared arrival counter and a global sense flag. Each thread maintains a local sense bit initialized to false. On `barrier_wait()`, a thread atomically increments the arriv…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates understanding of fundamental synchronization primitives, the challenges of implementing reusable barriers across multiple rounds, and critical memory ordering implications on different CPU architectures—topics essential for embedded systems and operating system development.\n\n## Key Concepts\n\n- **Barrier synchronization**: Ensures all threads pause at a synchronization point until every participant has arrived, preventing any thread from proceeding until all have reached the barrier.\n- **Sense-reversing technique**: Alternates a global flag between true and false across barrier rounds. Each thread tracks its own local sense and flips it after each wait, allowing the barrier to be reused without explicit reset logic.",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-9",
        "questionId": "q-26388",
        "question": "Design and implement an in-kernel adaptive file prefetcher that switches between sequential read-ahead and random-access patterns based on observed stride and hit rates. Provide data structures to track per-file access patterns, a policy for adjusting prefetch distance, and a test plan showing correctness and performance under mixed workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a 64-slot timer wheel with per-slot lists for sleeping threads. On sleep_ms(ms), place the thread into slot (current_tick + ms) % 64. timer_tick() advances current_tick, wakes all threads in th",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "End-to-end: a fault occurs, non-present PTE detected; acquire per-page lock and allocate a frame. If backed by swap/file, issue IO to fetch it; else zero-fill. After IO completes, update the PTE, set",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an adaptive file prefetcher using per-file state tracking with a pattern classification engine that distinguishes between sequential, strided, and random access patterns based on offset deltas and cache hit ra…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "On first write fault to a hugepage, serialize with per-region lock; split the 2MB page into 4KiB frames by materializing new PTEs and updating the page-table tree. If shared, perform Copy-On-Write and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests candidates' practical skills in I/O optimization by combining read-ahead mechanisms with workload-adaptive strategies—a common challenge at scale.\n\n## Key Concepts\n- Per-file I/O pattern tracking via offset delta analysis\n- Adaptive prefetch distance based on cache hit/miss feedback\n- Concurrency control using RCU and per-CPU data structures\n- Avoiding cache pollution from misdirected prefetch requests\n\n## Implementation Considerations\n- **Stride confidence**: Require N consecutive consistent deltas before classifying as strided\n- **Prefetch distance**: Start conservative (e.g., 4 pages) and adjust based on observed hit rates; increase distance for sequential workloads, decrease or disable for random access\n- **Pattern transitions**: Allow graceful switching between patterns as workload characteristics evolve over time",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-10",
        "questionId": "q-22866",
        "question": "Design a preemptible page-fault service path in a Linux-like kernel to guarantee bounded latency for real-time tasks under memory pressure. Propose per-task fault budgets, per-CPU fault workers, and a deadline-aware queuing mechanism. Describe end-to-end flow from a TLB miss to fault completion, how to handle concurrent faults with CAS, and how you would test worst-case latency and throughput in a multi-core, NUMA system?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-page state: is_compressed, compressed_size, density. On eviction or lazy compression, CAS-mark the page, compress with a fast algorithm (e.g., LZ4), store data in swap cache, set flags. On fault,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Maintain per-container vruntime and a burst credit bucket. Each task increments its container's vruntime; when quota is exceeded, suspend dispatch for that container until vruntime lags the global fro",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-task fault budgets cap latency and a per-CPU worker pool ensures predictability. On a TLB miss, an atomic CAS claims a fault slot; a granted worker performs the page-in with strict deadline, other",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "On a first-touch fault, the handler CASes the per-page slot from EMPTY to LOADING, wakes waiters, and fetches the page from the remote cache or disk with optional read-ahead. After a successful fetch,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of paging, real-time constraints, and low-latency synchronization in a kernel context, including race conditions and cross-core coordination.\n\n## Key Concepts\n- Real-time guarantees in memory management\n- CAS-based fault claiming and queueing\n- Per-task budgets and per-CPU fault workers\n- NUMA-aware placement and TLB shootdowns\n\n## Code Example\n```javascript\n// Minimal pseudo-code illustrating CAS-based fault claim\ntypedef struct { volatile int claimed; int page; int task_id; } FaultReq;\nvoid handle_fault(FaultReq* fr) {\n  if (__sync_bool_compare_and_swap(&fr->claimed, 0, 1)) {\n    // perform page-in (IO or zero-fill)\n    io_submit(fr);\n    complete_fault(fr);\n  } else {\n    enqueue_fault(fr);\n  }\n}\n```\n\n## Follow-up Questions\n- How would you measure and bound tail latency under memory pressure?\n- How would you scale the fault workers across cores and NUMA nodes?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-11",
        "questionId": "q-23379",
        "question": "Design a kernel mechanism to handle asynchronous I/O completions for a shared block device in a multi-tenant Linux-like OS. It must avoid thundering herd, support per-tenant quotas, and scale to thousands of concurrent I/Os. Describe the data structures, the completion path, per-I/O context, how you wake waiters, how you ensure fairness across tenants, and provide a minimal enqueuing and completing code sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On first touch, an atomic CAS marks the page as claimed by the requester; pages are placed on the requester’s local NUMA node with per-service quotas. Misses fetch backing data, optional read-ahead, a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant IO queues with a global dispatcher, per-CPU workers, and token-bucket quotas to cap bursts. Each IO carries tenant_id, seq, and a completion callback. Completions are batched, written t",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-thread atomic credits with per-core run queues. On each tick, increment credits up to a cap; CPU work spends 1 credit; I/O spends 2 credits and blocks if balance would go negative. A tick handler",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Thread A loads a block from its per-core freelist and begins use; Thread B, holding the pool lock, frees the same block into the global pool. A's subsequent access may hit freed memory or see partially corrupted data, c…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of scalable async IO completion, tenant isolation, and fairness in a container-dense OS.\n\n## Key Concepts\n- Asynchronous IO completion\n- Per-tenant quotas and throttling\n- Per-CPU workers and batching\n- Lock-free rings and memory order\n- Futex/eventfd wakeups and wake ordering\n\n## Code Example\n```javascript\n// Sketch: per-tenant queue and enqueue/complete paths\ntypedef struct io_ctx { unsigned long id; unsigned int tenant; void (*cb)(void*); } io_ctx;\ntypedef struct tenant_q { atomic_uint head, tail; io_ctx **ring; unsigned long quota; } tenant_q;\n\nvoid enqueue_io(tenant_q *q, io_ctx *io) { /* atomically push; respect quota */ }\nvoid complete_io(io_ctx *io) { /* push to tenant completion ring; wake waiter */ }\n```\n\n## Follow-up Questions\n- How would you test fairness under bursty tenants and high IO contention?\n- How would you ensure correctness if a tenant’s IO is canceled or errors out?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-12",
        "questionId": "q-15342",
        "question": "Extend a toy OS IO subsystem: implement per-device submission queues, a scalable worker pool, and a shared completion path. Describe how submissions map to devices, how completions are posted and delivered to the originating process, and how to avoid deadlocks under concurrent submissions. Provide a minimal C-like sketch for enqueue, dequeue, and completion handling, plus a test plan with burst and contention scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a zero-copy, per-connection RX path for a high‑throughput chat server using AF_XDP on Linux. With 100k concurrent connections on a single host, describe how packets are enqueued into per‑connection rings, how seq…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a seqlock: writer increments seq to odd, writes value, uses a memory barrier, then increments seq to even. Reader samples seq, reads value, re-samples seq, and repeats if seq changed or is odd. Us",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-device submission queues and a shared completion path. Submissions enqueue atomically into a device queue; a per-core worker pool dequeues and issues I/O. Upon completion, an entry is po",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Serialization is required. If truncation and access race, the mapping may serve stale data or fault with SIGBUS. A safe pattern is to lock around both operations, unmap the region before truncation an",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests lock-free data structures, cross-core coordination, and end-to-end IO flow. It gauges ability to design scalable, low-latency paths with correct completion routing.\n\nKey Concepts\n- Per-device queues and shared completion path\n- Lock-free enqueue/dequeue via CAS\n- Correct delivery to originating process\n- Backpressure, burst handling, and deadlock avoidance\n\nCode Example\n```c\n// Pseudo-C: per-device ring enqueue\ntypedef struct req { void* buf; size_t len; int op; } req_t;\ntypedef struct ring { _Atomic(int) head, tail; req_t* buf[N]; } ring_t;\nint enqueue(ring_t* r, req_t* e){\n  int t = atomic_load(&r->tail);\n  int next = (t + 1) % N;\n  if (next == atomic_load(&r->head)) return -1; // full\n  r->buf[t] = e;\n  atomic_store(&r->tail, next);\n  return 0;\n}\nreq_t* dequeue(ring_t* r){\n  int h = atomic_load(&r->head);\n  int t = atomic_load(&r->tail);\n  if (h == t) return NULL; // empty\n  req_t* e = r->buf[h];\n  atomic_store(&r->head, (h + 1) % N);\n  return e;\n}\n``` \n\nFollow-up Questions\n- How to scale with multiple devices sharing completion rings?\n- How to handle out-of-order completions and per-process cancellation?\n- What tests validate fairness under mixed device load?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-13",
        "questionId": "q-11003",
        "question": "Design a kernel-level cross-process deduplicated, read-only code cache where identical pages across processes share a single physical frame. Describe the end-to-end fault path, including fingerprinting, atomic hash-table insertion, CAS-based claim, single IO on miss, per-page refcounts, eviction, and NUMA awareness; explain how you avoid duplicate IO under concurrent faults?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "After fork, mark shared pages read-only and increment per-page refcounts. On a write fault, allocate a new frame, copy the source data, update the faulting process’s PTE to the new frame with write pe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-page state machine with states: RESIDENT, INFLIGHT, EVICTED, and NEEDS_WRITEBACK. On fault, use CAS to atomically claim the fault (transition from RESIDENT to INFLIGHT); if another thread already claimed…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a fingerprinted global page cache. On first touch, compute a 64-bit hash of page contents; atomically insert into a global hash by fingerprint. If hit, CAS-claim the existing frame and install a s",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use two atomic indices head and tail on a fixed array buf[N]. Enqueue next=(tail+1)%N; if next==head, full. Otherwise store buf[tail]=val; tail=next with memory_order_release. Dequeue if head==tail ->",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design cross-process memory sharing with minimal IO and correct concurrency.\n\n## Key Concepts\n- Page fingerprinting and deduplication\n- Lock-free CAS paths and race handling\n- Global per-page metadata and eviction\n- NUMA-aware placement and TLB coherence\n\n## Code Example\n```javascript\n// Pseudo kernel snippet for hash-lookup and mapping\n```\n\n## Follow-up Questions\n- How would you test correctness under concurrent faults and eviction pressure?\n- What are the security implications of sharing read-only code pages across userspace mappings?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-14",
        "questionId": "q-448",
        "question": "Explain how process scheduling works in an operating system. Which scheduling algorithm would you choose for a real-time system and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Process scheduling manages CPU allocation among processes by determining which process gets CPU time and when. For real-time systems, I would choose **Rate Monotonic Scheduling (RMS)** because it's predictable, has boun…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement using atomic head and tail pointers with compare-and-swap operations. Use hazard pointers or versioned references to solve the ABA problem. Trade-offs: hazard pointers have higher memory overhead but simpler i…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-CPU FD caches to reduce system-call overhead in I/O-heavy multi-tenant workloads. Store recently opened FDs per process on its current CPU; use a tiny, lock-free hash of (pid, fd) into a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a kernel IPC channel with bounded per-channel memory and multiple producers/consumers. Use a ring buffer with per-message header (len, pri, ts) and include backpressure signals. Implement a loc",
            "isCorrect": false
          }
        ],
        "explanation": "## Key Concepts\n- **Process scheduling**: Determines CPU allocation order among competing processes\n- **Scheduling algorithms**: FCFS, SJF, Priority, Round Robin, Rate Monotonic Scheduling\n- **Real-time requirements**: Deterministic timing behavior and deadline guarantees\n\n## Real-time Considerations\n- **Predictability**: Must guarantee task completion before deadlines\n- **Priority assignment**: Shorter periods receive higher priorities in RMS\n- **CPU utilization**: RMS can safely utilize up to 69% of CPU capacity\n\n## Implementation\n```c\n// RMS priority calculation\npriority = 1000 / period; // Higher values for shorter periods\n```\n\n## Trade-offs\n- RMS offers simplicity and predictability but is less flexible compared to dynamic scheduling algorithms\n- Optimal for fixed-priority systems but may require careful task design",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-15",
        "questionId": "q-3892",
        "question": "Two processes write concurrently to the same file opened with O_APPEND. Describe end-to-end how the kernel ensures atomic appends, what can cause atomicity to fail (buffered I/O, network FS), and how you would guarantee visibility and ordering in practice?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On sleep(n): wake_tick = current_tick + n; disable_interrupts(); insert task into a single, ordered sleep_list by wake_tick; suspend task. Timer ISR: current_tick++; while (sleep_list.head && sleep_li",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Copy-On-Write (COW) fork mechanism in a toy OS. After fork, parent and child processes share physical pages marked as COW. On a write fault, allocate a new page, copy the contents, update both processes' pag…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "On a Linux-like kernel, O_APPEND makes each write execute atomically with respect to other O_APPEND writes by updating the per-file offset in an atomic operation and appending the data to the inode bu",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Introduce acquire/release ordering in a ring buffer. Producer writes data[tail] then tail.store(next, memory_order_release). Consumer loads tail with memory_order_acquire and reads data[head] if head",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of atomic file semantics, VFS buffering, and real-world filesystem behavior under concurrent writes.\n\n## Key Concepts\n- Atomic appends with O_APPEND\n- VFS inode buffering and writeback\n- fsync/fdatasync for visibility guarantees\n- Atomicity caveats on network/remote FS\n\n## Code Example\n```c\nint fd = open(\"data.log\", O_WRONLY | O_APPEND);\nssize_t n = write(fd, buf, len);\n// Expect write to append atomically with respect to other O_APPEND writes\n```\n\n## Follow-up Questions\n- How would you test atomicity across a network filesystem? \n- What changes when using O_SYNC or writing via a dedicated append-logging mechanism?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-16",
        "questionId": "q-28325",
        "question": "In a Linux-like kernel on a multi-tenant NUMA system, implement a scalable per-device I/O budgeter that enforces bounded tail latency for high-priority disks under bursty load. Describe end-to-end from interrupt arrival to task reschedule, including per-device credits, cross-NUMA coordination, and policing across cgroups. Provide a minimal C-like sketch for budget debit and interrupt path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Concrete interleaving: two 16-bit stores to a single 32-bit word write high half 0xAABB then low half 0xCCDD. Reader may observe 0xAABB0000 or 0x0000CCDD, i.e., a torn read since writes aren’t atomic.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-process ready queue and a global file descriptor table that maps each descriptor to a list of waiting tasks. Mark all descriptors as non-blocking; registration occurs through a poll(fd, events, task) interface…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-task fault budgets cap latency and a per-CPU worker pool ensures predictability. On a TLB miss, an atomic CAS claims a fault slot; a granted worker performs the page-in with strict deadline, other",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-device I/O credits with NUMA-aware local pools and a global limiter. In the ISR, atomically debit the device's credit; if exhausted, enqueue requests to a throttle queue and defer completion. Credit replen…",
            "isCorrect": true
          }
        ],
        "explanation": "This question evaluates deep understanding of real-time-like guarantees in a scalable kernel I/O path. Strong candidates should articulate: per-device credit data structures with cache-line isolation, a fast path for normal I/O with minimal overhead, a slow path for throttling with appropriate backpressure, NUMA-aware coordination to avoid cross-node contention, and correctness under burst and contended I/O scenarios. Candidates must address critical edge cases including credit underflow, refill races between timer ticks and concurrent requests, and QoS isolation across cgroups.",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-17",
        "questionId": "q-6524",
        "question": "Design a simple 3-level MLFQ scheduler for a toy OS. Levels have quantum 4, 8, and 16 ticks; aging promotes waiting tasks to higher levels after a threshold to prevent starvation. Given two processes arriving together—P1 CPU-bound, P2 I/O-bound—describe their scheduling order, how aging affects priorities, and provide a minimal C-like sketch for enqueue, dequeue, and choosing the next task. How would you test fairness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Fault flow: if a free frame exists, bind it to the faulting page, set the PTE valid, and load the page; else choose the oldest frame as victim, write back if dirty, invalidate victim PTE, assign victi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Three ready queues Q0–Q2 with quanta 4, 8, 16. Aging promotes waiting tasks by one level after A ticks to prevent starvation. On quantum expiry, move the running task to the next lower level; on IO co",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "A strong response describes: per-file FIFO queues; a global per-process token bucket; aging to boost long-waiting requests; submit_aio() enqueues and returns a handle; worker drains tokens and issues",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-core energy budgeting using PMU counters; track energy per task in a quanta window. On dispatch, increment by energy delta; if over quota, throttle (short yield or suspend) until reset. Use per-co",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of practical scheduling design, aging to avoid starvation, and handling preemption in a simple OS.\n\n## Key Concepts\n- Multi-level feedback queue structure\n- Quantum slicing and preemption\n- Aging to prevent starvation\n- Correctness under concurrent events and IO wakeups\n\n## Code Example\n```javascript\n// Minimal MLFQ skeleton\nconst QUANTUM = [4,8,16];\nlet queues = [[], [], []];\nfunction enqueue(t){ queues[t.level].push(t); }\nfunction dequeue(){ for(let i=0;i<3;i++) if(queues[i].length) return queues[i].shift(); return null; }\nfunction pick(){ let t = dequeue(); if(!t) return null; t.remaining = QUANTUM[t.level]; return t; }\n```\n\n## Follow-up Questions\n- How would you test aging effectiveness and starvation resistance?\n- What edge cases (burstiness, bursty IO) would you simulate to validate correctness?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-18",
        "questionId": "q-12248",
        "question": "Design a zero-copy IPC channel between two user-space processes in a minimal OS: a shared memory ring buffer with per-slot credits, wakeup semantics, and a file-backed backing store to survive restarts. Ensure correctness under concurrent producers/consumers, handle buffer full, and minimize copies. Provide a minimal C sketch for init, enqueue, and dequeue, plus a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "One robust approach is to use a per-thread pending signal flag and a wake mechanism. On a signal, atomically set pending and wake the blocked read. The read loop checks for pending signals after each",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Zero-copy IPC channel using a shared memory ring buffer with per-slot credits. Producer/consumer use atomic CAS on tail/head plus memory fences for visibility. Pin pages, back with a file to survive r",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a shared ring buffer in shm: atomic head/tail and an array slots[]. Full when (head+1)%CAP==tail; empty when head==tail. Enqueue: while full, futex_wait(&state,0); slots[head]=v; head=(head+1)%CAP",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On a fault, acquire a per-page lock; CAS-state NONE->PENDING and enqueue an IO task to the local NUMA node. If data exists, map; if not, zero-fill while IO in flight. On IO completion, flip state to V",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of low-level IPC, synchronized ring buffers, and memory-ordering guarantees in a shared-memory setup. Expected to justify CAS-based coordination, fence usage, and restart-safe backing.\n\n## Key Concepts\n\n- Shared memory synchronization and cache coherence\n- Atomic CAS, memory ordering, and fences\n- Zero-copy data transfer and page pinning\n- Backpressure and restart-safety with file-backed backing\n- Race conditions in producer/consumer handshakes\n\n## Code Example\n\n```c\ntypedef struct {\n  volatile uint64_t head;\n  volatile uint64_t tail;\n  size_t capacity;\n  void* slots[]; // flexible array\n} ring_t;\n\n// Init, enqueue, dequeue sketches illustrating CAS and fences\n```\n\n## Follow-up Questions\n\n- How would you test under bursty producers with multi-core contention?\n- What memory-ordering guarantees are essential on ARM vs x86, and how would you adapt the fences?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-19",
        "questionId": "q-5291",
        "question": "In a tiny preemptive OS with per-CPU run queues and a single disk I/O device, describe the end-to-end flow when a thread calls read() on a file not yet in memory. From syscall entry to user-space return, explain blocking, how the I/O request is queued, how the interrupt signals completion, how the waking thread is re-queued and scheduled, and how to avoid starving other ready tasks. Include minimal pseudo-code for the blocking path and the wakeup path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CFS uses a red-black tree ordered by vruntime; the task with the smallest vruntime runs next. CPU-bound tasks accumulate vruntime quickly, while I/O-bound tasks sleep, so their vruntime grows slowly, giving them priorit…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a hierarchical timer wheel: per-core buckets for fast timers, a global wheel for long timers, and a central clock source. Each timer holds expires, cb, and arg; insertion uses a core-local hash wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "read() blocks the thread, moves it to a blocked state, queues a disk-read request, and yields the CPU. The I/O subsystem issues the request; on completion, the IRQ marks the request done, wakes the bl",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a NUMA-aware shared buffer pool with per-VM quotas, ref-counted buffers, and per-NUMA pools. Include a fast path for in-quota allocations, a fair eviction policy, and CAS-based quota updates. P",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of blocking I/O, interrupts, wakeups, and basic scheduler interaction in a simple OS.\n\n## Key Concepts\n- Blocking I/O lifecycle and task states\n- ISR vs thread wakeup and race avoidance\n- Run queues and fairness to prevent starvation\n- Memory ordering and visibility on wakeup\n\n## Code Example\n```javascript\n// Pseudo blocking path\nfunction do_read(fd, buf, len){\n  if (not_in_memory(fd)) {\n    req = new_io_request(fd, buf, len);\n    enqueue_io(req);\n    block_current_task();\n  }\n  return_read_to_user(buf, len);\n}\n```\n\n## Follow-up Questions\n- How would you test starvation between CPU-bound and I/O-bound threads?\n- What race conditions might occur between interrupt and scheduler, and how would you prevent them?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-20",
        "questionId": "q-27660",
        "question": "Design a NUMA-aware page cache eviction policy for a memory-heavy containerized workload. The OS runs multiple containers with per-container quotas and non-uniform memory access. Describe end-to-end flow from a page fault to eviction, including per-page metadata, concurrency control (CAS/locks), and how to guarantee fairness across containers under pressure. Provide minimal pseudocode for victim_selection() and reclaim_page()?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A strong response describes: per-file FIFO queues; a global per-process token bucket; aging to boost long-waiting requests; submit_aio() enqueues and returns a handle; worker drains tokens and issues",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a NUMA-aware page cache eviction policy with per-container quotas. On a page fault, look up local node cache; if miss, fetch from disk and assign to container quota. Eviction uses a global LRU",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-thread token bucket: each task has tokens and a budget B; tokens refill at R MB/s. On IO submit, if tokens < size, stall or queue; on completion, refund. Attach tokens to the submitting task in th",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On a touch fault for a 4KB page not resident and without a backing file, the handler first checks page presence and performs an atomic test-and-set operation to reserve the page. A 4KB page is allocated from the buddy a…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests OS memory management under NUMA, container isolation, and memory pressure. Evaluates per-page state design, concurrency (CAS/locking), and fairness guarantees across tenants under contention.\n\n## Key Concepts\n\n- NUMA locality and memory bandwidth\n- Per-container quotas and accounting\n- Page cache metadata (state, owner, node, refcount)\n- Concurrency control (CAS, locks, pinning)\n- Fairness under pressure and aging\n\n## Code Example\n\n```c\n// Minimal pseudocode illustrating key steps\nPage* victim_selection(Cache* cache, Container* containers) {\n  // select victim with debt, age, and locality considerations\n  // return pointer to Page to reclaim or NULL if none\n}\nvoid reclaim_page(Page* p) {\n  // write back if dirty, remove from cache, update mappings\n}\n```\n\n## Follow-up Questions\n\n- How would you instrument tests to validate cross-container fairness under synthetic pressure?\n- How would topology changes (NUMA node hot-spots) affect eviction decisions?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-21",
        "questionId": "q-11893",
        "question": "Implement a bounded producer-consumer queue between two threads sharing a fixed circular buffer. Describe push and pop with a mutex and two condition variables (not_full, not_empty). Explain handling of spurious wakeups, race conditions with multiple producers/consumers, and how to avoid deadlocks and guarantee progress under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Compute per-page scores from hotness and per-tenant quotas; eviction favors local NUMA pages first. Under node A pressure, move candidates to per-node queues, batch-evict, and synchronize with a light",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "On a write fault to a CoW page, trap to fault handler. If page is marked Copy-On-Write, allocate a new page, copy the contents, install the new PTE with write permission, and atomically swap it in. De",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a bounded producer-consumer queue using a fixed circular buffer shared between threads. Each push operation waits while count == size, and each pop operation waits while count == 0, both protected by a mutex.…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a circular buffer, a mutex, and two condition variables: not_full and not_empty. try_send locks, checks full, returns false if full; otherwise enqueues and signals not_empty. blocking_recv waits o",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of inter-thread communication, synchronization primitives, and robust edge-case handling in concurrent programming scenarios.\n\n## Key Concepts\n- Bounded buffer with circular indexing\n- Mutual exclusion via mutex and condition variable coordination\n- Spurious wakeup handling and invariant maintenance\n- Deadlock prevention strategies and progress guarantees\n\n## Code Example\n```c\n// Minimal implementation using pthreads\ntypedef struct {\n    int *buf;\n    int size;\n    int head, tail, count;\n} circbuf;\npthread_mutex_t m = PTHREAD_MUTEX_INITIALIZER;\npthread_",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-22",
        "questionId": "q-19955",
        "question": "In a Linux-like kernel, implement per-process I/O quotas across multiple block devices using a token-bucket model. Each process has a quota (bytes/sec) that applies to all I/O across devices; allow bursts up to a configurable bucket size. Describe the end-to-end flow from an application's write through the block-layer throttle, including how to handle cross-device I/O, refill semantics, and fairness. Provide a minimal C-like sketch for replenishing tokens and deciding whether to submit an I/O, and a test scenario showing bursty I/O from multiple processes with different quotas?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A per-process token bucket with atomic ops. Refill happens on a per-second timer; tokens = min(tokens + refill, bucket_size). On IO submission, if tokens >= size, consume tokens and submit; else enque",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "A reusable barrier for N threads using the sense-reversing technique: ```c #include <stdatomic.h> typedef struct { atomic_int count; atomic_int sense; int N; } barrier_t; void barrier_init(barrier_t* b, int N) { b->N =…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "On a TLB miss, the hardware or software walks the two-level page table to locate the corresponding page table entry (PTE). First, the page directory (PD) index selects the appropriate page directory entry (PDE), which p…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Interleaving: A reads 0, B reads 0, A increments to 1 and writes 1, B logs 0. The write may not be visible immediately due to per-core caches and cache-coherence delays, so B observes 0. Fix with atom",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical I/O throttling in the kernel, including how to enforce per-process quotas across devices, handle bursts, and integrate with the block layer without starving others.\n\n## Key Concepts\n\n- Token bucket per process\n- Refill cadence and burst handling\n- Cross-device fairness (cgroups/weights)\n- Enqueue/wake paths in kernel I/O\n- Atomic synchronization across CPUs\n\n## Code Example\n\n```javascript\n// Minimal token bucket sketch (pseudo-C style in JS-like syntax for readability)\ntypedef struct { atomic_uint tokens; uint32_t bucket; uint32_t rate; spinlock_t lock; wait_queue_t wq; } tb_t;\nbool io_submit(tb_t *tb, size_t size) {\n  if (atomic_read(&tb->tokens) >= size) {\n    atomic_sub(&tb->tokens, size);\n    submit_to_device(size);\n    return true;\n  }\n  enqueue_io(size);\n  return false;\n}\nvoid refill(tb_t *tb) {\n  uint32_t t = atomic_read(&tb->tokens) + tb->rate;\n  if (t > tb->bucket) t = tb->bucket;\n  atomic_write(&tb->tokens, t);\n  wake_up(&tb->wq);\n}\n```\n\n## Follow-up Questions\n\n- How would you test latency vs throughput for mixed burst patterns?\n- How to ensure fairness when devices have different speeds and tenants with varying quotas?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-23",
        "questionId": "q-30276",
        "question": "In a minimal Linux-like kernel, implement a per-CPU page-cache eviction policy that prioritizes keeping hot pages on the local CPU and minimizes cross-CPU traffic. Describe the data structures (per-CPU LRU, global clock), the eviction protocol, and how you migrate ownership on access. Provide a compact C sketch for choosing a victim from the per-CPU cache and a test plan using mixed CPU-bound and memory-bound workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-CPU page-cache eviction: each CPU maintains an LRU, aided by a global clock for rare cleanups and per-page hotness counters. On fault, insert into local cache, migrate ownership on access",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Allocate by: (1) try per-CPU freelist; if empty, atomically refill from shared global pool via CAS; (2) free to local cache; if full, spill to global. Use per-list CAS to avoid locks; include a lightw",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe an asynchronous I/O completion pipeline: devices enqueue events into a lock-free ring; a reactor dequeues and wakes waiters via per-task queues. Explain end-to-end flow, how to avoid loss und",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a fixed-size cache with a hash table for O(1) lookups and an LRU list for eviction. Each entry tracks block_id, data_ptr, flags (valid, dirty, in_flight). On read_block: if present, return; else i",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical design of per-CPU memory caches and cross-CPU coherence under contention, emphasizing low latency and minimal traffic.\n\n## Key Concepts\n- Per-CPU caches\n- Eviction policies (LRU, hotness)\n- Ownership migration and races\n- Concurrency control and perf measurement\n\n## Code Example\n```c\n// compact victim selection sketch\nstatic struct page *select_victim(struct cpu_cache *cc) {\n    // iterate LRU list, skip pinned pages, pick first unreferenced\n    struct page *p = cc->lru_tail; // tail is oldest\n    while (p && is_pinned(p)) p = p->prev;\n    return p;\n}\n```\n\n## Follow-up Questions\n- How would NUMA affect this design? How would you adapt the policy?\n- How would you validate reduced cross-CPU traffic with measurements?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-24",
        "questionId": "q-7878",
        "question": "Design and implement a kernel-space robust futex primitive to synchronize user-space threads on a shared address, addressing termination safety, spurious wakeups, and wake-order guarantees under preemption. Provide end-to-end flow: user-space futex_wait on addr; kernel enqueues and blocks per-address; futex_wake wakes one/all waiters; how the robust list ensures safety when threads exit while blocked; memory barriers; and a minimal C-like sketch of fast-path, slow-path, and a test matrix for races (exit during wait, wake before wait, requeue)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design and implement a kernel-space robust futex primitive to synchronize user-space threads on a shared address, addressing termination safety, spurious wakeups, and wake-order guarantees under preemption. Provide end-…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Fault flow uses a per-page lock: first thread allocates a frame, zero-fills it, updates the PTE to present, invalidates the TLB, and wakes waiters; then releases the lock. If a second thread faults on",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Detect sequential access by tracking the previous block number and stride; when a run of 3+ consecutive accesses is observed, prefetch a window of the next 4–8 blocks asynchronously. Mark prefetched b",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-CPU timer affinity using hrtimers and per-CPU queues. Each timer selects a target CPU; on expiry, atomically mark fired and dispatch the callback to that CPU's runqueue, with a fallback to the nea",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of low-level synchronization, race conditions with thread lifecycle, and memory-ordering guarantees in kernel-space futex design.\n\n## Key Concepts\n- Futex fast vs slow path\n- Per-address wait queues\n- Robust futex list and exit handling\n- Race conditions during thread termination\n- Memory barriers and wake ordering\n\n## Code Example\n```c\n// Minimal sketch of fast-path vs slow-path for futex_wait\ntypedef struct { atomic_int *addr; wait_queue_t *wq; robust_list_t *robust; } futex_t;\nint futex_wait(int *addr, int expected) {\n  // Fast path: if *addr != expected, return immediately\n  if (atomic_load(addr) != expected) return EAGAIN;\n  \n  // Slow path: enqueue and block\n  futex_t f = {addr, get_wait_queue(addr), current->robust_list};\n  add_to_robust_list(&f);\n  smp_mb(); // Memory barrier before sleep\n  return wait_event_interruptible(f.wq, atomic_load(addr) != expected);\n}\n```\n\n## Test Matrix\n| Race Condition | Expected Behavior |\n|----------------|------------------|\n| Thread exits during wait | Robust list cleanup wakes waiters |\n| Wake before wait | Waiter sees updated value, returns immediately |\n| Requeue race | Waiter moves to new queue without lost wakeups |",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-27T12:23:55.970Z",
    "version": 1
  }
]