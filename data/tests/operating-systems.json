[
  {
    "id": "test-operating-systems",
    "channelId": "operating-systems",
    "channelName": "Operating Systems",
    "title": "Operating Systems Knowledge Test",
    "description": "Test your understanding of Operating Systems with 25 questions.",
    "questions": [
      {
        "id": "operating-systems-q-0",
        "questionId": "q-5997",
        "question": "Design and implement a kernel-level page cache eviction strategy for a shared memory region mapped by multiple processes from a single file. Enforce per-process memory caps, enable adaptive read-ahead, and prevent starvation under contention. Describe end-to-end flow from first touch to eviction, including per-page state, synchronization, and test plans?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose per-page metadata including last_access, refcount, and owner_mask; impose per-process quotas; use an aging score combining recency and per-process fairness to pick eviction candidates. On faul",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Two worker threads contend for the submission queue tail pointer. Thread A obtains an SQE slot and writes its submission entry, while Thread B races to advance the kernel tail pointer and submit. Without proper memory b…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A kernel API exposes prefetch hints via ioctl; a per-page state machine tracks HOLE, PENDING, and LOADED states. Hints enqueue fetch tasks, and CAS (compare-and-swap) operations prevent duplicate I/O when multiple proce…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A concrete race: a reader faults in a mmap’d page while a writer’s writeback evicts that same page, yielding a stale read. Coherence relies on page dirty flags, writeback, and cache invalidation on fa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nExplores practical OS page cache design in multi-process environments, emphasizing correctness, fairness, and performance under contention.\n\n## Key Concepts\n- Shared memory and page cache dynamics\n- Per-page metadata and per-process quotas\n- Synchronization primitives (CAS, memory barriers)\n- Adaptive read-ahead and starvation prevention\n\n## Code Example\n\n```c\ntypedef struct {\n  uint64_t last_access;\n  uint32_t refcount;\n  uint16_t owner_mask;\n  uint16_t flags;\n} PageMeta;\n```\n\n## Follow-up Questions\n- How would you extend for NUMA-aware eviction decisions?\n- How would you verify eviction correctness under heavy write contention?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-1",
        "questionId": "q-16927",
        "question": "In a toy OS with a simple two-level page table and Copy-On-Write forks, describe and implement the minimal COW fault path after fork: pages are initially shared and marked read-only; when a process writes, a page fault occurs, a private copy is allocated, data copied, and both page tables updated to point to private writable frames. Provide a small C-like sketch of the PTE structure and a fault handler?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-device deadline-based I/O scheduler with per-process QoS and NUMA locality. On request arrival, assign a deadline and weight, enqueue to a device-local queue. The next I/O is chosen by earliest de",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Under memory pressure, the kernel detects the pressure condition through signals from the page allocator (e.g., allocation failures) or from the memory cgroup itself. The reclaim loop then computes per-cgroup memory def…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "After fork, mark shared pages read-only and increment per-page refcounts. On a write fault, allocate a new frame, copy the source data, update the faulting process’s PTE to the new frame with write pe",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Translate VA 0x1234ABCD using 2-level paging: L1 = (va>>22)&0x3FF; L2 = (va>>12)&0x3FF; offset = va&0xFFF. If L1 entry present, fetch L2; if L2 entry present, PPN = PTE & ~0xFFF; phys = (PPN<<12) | of",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of Copy-On-Write semantics, page table entries, and race-free memory sharing during fork.\n\n## Key Concepts\n\n- Copy-On-Write, PTE fields (present, rw, user, cow flag), refcounts\n- Fault path orchestration: validation, allocation, copy, update\n- Synchronization and TLB flush considerations\n\n## Code Example\n\n```c\ntypedef struct {\n  unsigned int frame;\n  unsigned int present:1;\n  unsigned int rw:1;\n  unsigned int user:1;\n  unsigned int cow:1;\n  unsigned int refcount:28;\n} PTE;\n\nvoid pagefault_handler(int pid, unsigned long addr){\n  // locate PTE for addr; if cow, allocate new frame, copy, remap with rw=1\n  // atomically update refcount; free old frame if refcount == 0\n  // flush relevant TLBs then resume\n}\n```\n\n## Follow-up Questions\n\n- How would you test correctness and performance?\n- How would you extend to multi-core with per-page locks?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-2",
        "questionId": "q-5375",
        "question": "In a tiny OS kernel, implement a preemptive Round-Robin scheduler with a fixed time quantum of 4 ticks. Describe the end-to-end flow from timer interrupt to context switch in this scheduler, including ready-queue management, tick handling, and a scheme to prevent starvation via aging?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement preemptive RR with quantum=4 ticks. On each timer tick: decrement running.remaining_quantum; if zero then enqueue the running process at tail and pop next from the ready queue; perform a con",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "A per-process token bucket with atomic ops. Refill happens on a per-second timer; tokens = min(tokens + refill, bucket_size). On IO submission, if tokens >= size, consume tokens and submit; else enque",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a kernel-level deduplicated shared page cache keyed by page content hash, featuring a global page table with compare-and-swap (CAS) based claiming on page faults, lazy loading from the backing file, per-page refe…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On a fault, acquire a per-page lock; CAS-state NONE->PENDING and enqueue an IO task to the local NUMA node. If data exists, map; if not, zero-fill while IO in flight. On IO completion, flip state to V",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of timer interrupts, context switches, and simple schedulers at kernel level. It also probes how aging prevents starvation.\n\n## Key Concepts\n- Preemptive scheduling and round-robin\n- Timer interrupt handling and context-switch protocol\n- Per-process state and a ready-queue data structure\n- Aging to prevent starvation under load\n\n## Code Example\n```javascript\n// Pseudo-kernel RR tick\nfunction timerTick(running, ready) {\n  running.quantum--;\n  if (running.quantum <= 0) {\n    ready.enqueue(running);\n    const next = ready.dequeue();\n    contextSwitch(running, next);\n  }\n}\n```\n\n## Follow-up Questions\n- How to implement tickless mode with RR?\n- How would you test aging efficacy under bursty workloads?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-3",
        "questionId": "q-12998",
        "question": "In a tiny kernel with demand paging for a shared file-backed region, how can multiple threads faulting on the same page cooperate to load it exactly once? Describe a race-free protocol using per-page states (invalid/loading/loaded), a small per-page lock, and a wait-queue to wake waiters after IO completes. Include a concise C-like snippet implementing the fault path and the loader, showing lock/protection and page-in decision?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Multi-Queue I/O Scheduler Design ### Core Design This scheduler organizes I/O requests into per-NUMA-node, per-process queues. Each NUMA node maintains a separate queue for every process, allowing both NUMA locality…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a tickless per-CPU timer system using per-CPU deadline queues for local timer management and a global min-heap for cross-CPU timer coordination. On CPU idle, disable the hardware timer interrupt to conserve po…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-page state machine with an atomic state (INVALID, LOADING, LOADED) and a tiny per-page lock plus a wait queue. On a fault, a thread wins the lock, sets LOADING, issues IO, fills the page, th",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create two cgroups: foreground (io.weight=200) and background (io.weight=10); move short I/O threads into foreground. Run fio with randread 4k, and record p95/p99 latency; monitor throughput with iost",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of race-free demand paging, per-page synchronization, and wakeup of threads on IO completion; practical for kernels sharing memory.\n\n## Key Concepts\n- Per-page state machine\n- Tiny per-page locking\n- Wait queues and wakeups\n- Visibility and memory ordering\n\n## Code Example\n```c\n// Minimal sketch\nenum PageState { INVALID, LOADING, LOADED };\nstruct Page {\n  atomic_int state;\n  mutex lock;\n  char data[PAGE_SIZE];\n  wait_queue wq;\n  // ... io handle\n};\n```\n\n## Follow-up Questions\n- How would you handle IO errors and retries?\n- How would you scale this to many pages with fair waiting threads?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-4",
        "questionId": "q-15790",
        "question": "In a Linux-like kernel with persistent memory (PMEM) available, design a crash-consistent allocator and in-memory key-value store shared across processes via mmap. It must support atomic allocate/free, durable metadata in NVRAM, per-process access control, and a single-log redo for recovery. Describe end-to-end path for put(key,value) and recovery steps, including ordering, flush semantics, and testing plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A reusable barrier for N threads using the sense-reversing technique: ```c #include <stdatomic.h> typedef struct { atomic_int count; atomic_int sense; int N; } barrier_t; void barrier_init(barrier_t* b, int N) { b->N =…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a PMEM-backed allocator with crash-consistent single-log redo for a shared mmap key-value store. Use per-object headers in NVRAM, atomic allocate/free, and a durable root pointer. Describe end-",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a NUMA-aware migrator that uses per-node free lists plus per-VM quotas, with a central migration pool and per-core work-stealers. On memory pressure, pick cold pages, compute target node by acc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a 4-entry software TLB tagged by ASID. VA splits into PDE, PTE, offset. Translate(vaddr): check TLB; on miss, walk PD/PT; if page present, build PA and insert into TLB with LRU. If absent, raise a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes crash-consistency and recovery for a shared PMEM data store, a real-world R&D concern at scale.\n\n## Key Concepts\n- Persistent memory semantics: flush/barrier ordering, cache-line persistence\n- Crash-consistent redo logging and recovery path\n- Cross-process mmap synchronization, per-object headers, and durable root pointer\n- Atomic operations for allocate/free and security/isolation guarantees\n\n## Code Example\n```c\n// sketch: allocate, log, flush, and commit root\n```\n\n## Follow-up Questions\n- How would you test mid-put crashes and recovery?\n- How would you scale to multiple PMEM pools and concurrent writers?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-5",
        "questionId": "q-8113",
        "question": "In a tiny preemptive OS, implement a sleep(n) API and wakeup mechanism. Describe the data structures for a per-task sleep queue, how the timer interrupt wakes tasks, and provide minimal C-like pseudo-code for enqueuing sleeping tasks and handling ticks to wake them, including race-condition avoidance between enqueue and tick?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a hybrid memory page migration policy for DRAM+PMEM. On first touch, compute a score from recency, frequency, and NUMA distance; migrate hot pages to PMEM if the score exceeds a threshold, els",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a NUMA-aware page migration policy with per-page affinity state tracking (owner_node, migrating flag, access_frequency) and a cost-based decision model. The policy compares the cumulative penalty of re…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Interleaving: A reads 0, B reads 0, A increments to 1 and writes 1, B logs 0. The write may not be visible immediately due to per-core caches and cache-coherence delays, so B observes 0. Fix with atom",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On sleep(n): wake_tick = current_tick + n; disable_interrupts(); insert task into a single, ordered sleep_list by wake_tick; suspend task. Timer ISR: current_tick++; while (sleep_list.head && sleep_li",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical skills for implementing sleep/wake in a preemptive kernel, focusing on data structures, ISR interaction, and race conditions.\n\n## Key Concepts\n- Sleep list ordered by wake_tick\n- Per-task wake_tick and state fields\n- Interrupt masking or locks to avoid races between enqueue and timer\n- Immediate wakeup and re-scheduling to ready queue\n\n## Code Example\n```javascript\n// Pseudo C-like snippet\ntypedef struct Task { int wake_tick; int state; struct Task* next; } Task;\nTask* sleep_list = NULL;\nvoid sleep(int n) {\n  int w = current_tick + n;\n  task->wake_tick = w;\n  disable_interrupts();\n  insert_ordered(&sleep_list, task, w);\n  task->state = SLEEPING;\n  schedule();\n  enable_interrupts();\n}\nvoid timer_handler() {\n  current_tick++;\n  while (sleep_list && sleep_list->wake_tick <= current_tick) {\n    Task* t = pop_head(&sleep_list);\n    t->state = READY;\n    enqueue_ready(t);\n  }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-6",
        "questionId": "q-10867",
        "question": "In a Linux-like host, design a kernel-assisted per-page versioning protocol for mmap’d data shared across containers. Each page carries a version tag; writers publish a new version via an atomic swap and readers re-check the tag on access. On fault, readers retry with the new mapping. Use per-page CAS to claim fault and minimize TLB flushes. Outline data structures, end-to-end fault flow, and tests for latency and fairness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "End-to-end: fault triggers the path; identify tenant and region; check per-tenant credits atomically; if credits remain, debit and service the fault (IO/zero-fill); if zero, enqueue fault in a per-ten",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "After fork, mark shared pages read-only and increment per-page refcounts. On a write fault, allocate a new frame, copy the source data, update the faulting process’s PTE to the new frame with write pe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a kernel-assisted per-page versioning scheme for mmap’ed data shared across containers. Each page stores a version stamp; writers publish a new version by atomically swapping the backing and b",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-core lock-free deques; local producers push/pop; idle cores steal from the least-local non-empty deque via CAS; a central pool provides fallbacks; track per-task affinity and epoch for fairness; t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes a deep, practical OS concept: coordinating readers and writers on shared memory with minimal stalls and strong consistency across NUMA/container boundaries.\n\n## Key Concepts\n- Per-page versioning in the page cache\n- Lock-free CAS-based fault claiming\n- TLB invalidation strategies and cost control\n- Read-mostly shared data and versioned updates\n- NUMA-affinity and cross-container coherence\n\n## Code Example\n```javascript\n// Pseudo\ntypedef struct {\n  unsigned long version;\n  void *backing;\n} page_t;\n\nint claim_fault(page_t *p){\n  unsigned long v = p->version;\n  return __atomic_compare_exchange_n(&p->version, &v, v+1, 0, __ATOMIC_SEQ_CST, __ATOMIC_SEQ_CST);\n}\n```\n\n## Follow-up Questions\n- How would you test for stale reads under high write concurrency? what metrics and workloads?\n- How would you integrate with existing page cache eviction policies without regressing latency?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-7",
        "questionId": "q-1354",
        "question": "You implement a lock-free ring buffer with two atomics (head, tail) and a data array for inter-thread communication. Describe a concrete interleaving on a weak memory model (e.g., ARM64) where the consumer observes a stale value or an invalid read due to missing ordering. Propose a minimal fix using memory_order_release on the data write/tail update and memory_order_acquire on the consumer read, and sketch a safe patch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Introduce acquire/release ordering in a ring buffer. Producer writes data[tail] then tail.store(next, memory_order_release). Consumer loads tail with memory_order_acquire and reads data[head] if head",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a shared ring buffer in shm: atomic head/tail and an array slots[]. Full when (head+1)%CAP==tail; empty when head==tail. Enqueue: while full, futex_wait(&state,0); slots[head]=v; head=(head+1)%CAP",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe how to implement a shared file-block cache for a toy OS. When a thread requests a block not cached, ensure only one disk read occurs and all waiters receive the block once loaded. Provide a m",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-slot sequence counter and a single atomic tail. Producer CASes to reserve a slot, writes the event pointer, then releases with a memory barrier. Consumers read with acquire, verify slot.seq",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of memory ordering, lock-free data structures, and weak memory models in OS CPUs. It probes ability to identify a real race and reason about inter-thread visibility across architectures.\n\n## Key Concepts\n\n- Lock-free synchronization\n- memory_order_release\n- memory_order_acquire\n- Data visibility before signaling availability\n\n## Code Example\n\n```cpp\n// Pseudo-C++ lock-free sketch\nstd::atomic<size_t> head{0}, tail{0};\nint data[N];\n\nvoid produce(int v){\n  auto t = tail.load(std::memory_order_relaxed);\n  data[t % N] = v;\n  tail.store((t+1) % N, std::memory_order_release);\n}\n\nint consume(){\n  auto t = tail.load(std::memory_order_acquire);\n  auto h = head.load(std::memory_order_relaxed);\n  if (h == t) return -1;\n  int v = data[h % N];\n  head.store((h+1) % N, std::memory_order_release);\n  return v;\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt this pattern for multiple producers/consumers?\n- What tests would you add to validate correctness across ARM/x86?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-8",
        "questionId": "q-6216",
        "question": "Design a per-cgroup memory-aware page reclaim policy in a Linux-like kernel. Given two services on the same host—A is memory-hungry and latency-sensitive, B is cache-heavy—describe the end-to-end path for reclaim: how per-cgroup accounting influences decisions; how kswapd and direct reclaim interact; how to pick candidates (anon vs file-backed); how to avoid starvation and thrash; and how to guarantee fairness and latency under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a lock-free MPMC-like scheme with a per-slot sequence and a shared head counter updated via CAS. Producers obtain an index, write data, then publish by updating the slot’s sequence with a release-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage memory.cgroups to track per-page charges and extend the per-cgroup LRU with separate anon and file-backed lists. On reclaim, kswapd runs per‑cgroup shrinkers, prioritizing anon pages from the",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Interleaving: A reads 0, B reads 0, A increments to 1 and writes 1, B logs 0. The write may not be visible immediately due to per-core caches and cache-coherence delays, so B observes 0. Fix with atom",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-page lock and an in-flight flag. The first thread CASes the state from Invalid to Loading and starts the disk read; it registers waiters on a per-page wait-queue. Others seeing Loading join",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical OS skills: per-cgroup accounting, page reclaim flow, and the interaction between kswapd and direct reclaim under contention.\n\n## Key Concepts\n- memory.cgroups per-page accounting\n- per-cgroup LRU separation for anon vs file-backed\n- reclaim coordination between kswapd and direct reclaim\n- starvation avoidance and fairness across cgroups\n- measuring latency and throughput under load\n\n## Code Example\n```c\n// Pseudo data-structures for per-cgroup page lists\nstruct memcg_page {\n  struct list_head anon_list;\n  struct list_head file_list;\n  unsigned long charge;\n};\n```\n\n## Follow-up Questions\n- How would you test per-cgroup reclaim fairness on a mixed workload?\n- What metrics would you expose to operators to detect thrash vs healthy reclamation?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-9",
        "questionId": "q-7088",
        "question": "In a toy OS kernel, two threads contend for a single device using a FIFO ticket lock paired with a per-thread wait queue. Describe end-to-end how acquire and release work, how a thread blocks, and how wakeups wake the next waiter. Provide a minimal C-like sketch for the data structures and the acquire/release paths, including how to avoid deadlock and minimize spinning under contention. Include how you would test for fairness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On first write to a COW page, the kernel traps the fault, allocates a private page for the child, copies data from the shared page, updates the child's PTE to the new page with write permission, clear",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a FIFO ticket lock with a wait queue. Acquire: ticket = fetch_add(&lk->next, 1); spin until nowServing == ticket; Enter critical section; Release: nowServing++ and wake the next waiter via the que",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "On a faulting address, verify it's within the allowed stack growth window. Atomically claim the next stack page (CAS on a per-thread growth flag). Allocate a new physical page, map it at the correct p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a 16-byte circular buffer with head and tail indices. In the ISR, compute next=(head+1)&15; if next==tail, overflow; else buf[head]=byte; head=next; memory barriers; wake reader. In read(), block",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked: tests understanding of synchronization primitives, fairness, and memory ordering. Key Concepts: ticket locks, wake queues, avoiding starvation, spurious wakeups, memory barriers. Code Example: see below. Follow-up: discuss adaptation to multicore/NUMA, impact of preemption, and testing strategies.\n\nCode Example:\n```c\n#include <stdatomic.h>\n\ntypedef struct {\n  atomic_uint next;\n  atomic_uint nowServing;\n} ticket_lock_t;\n\nvoid ticket_lock_init(ticket_lock_t *lk){\n  atomic_store(&lk->next, 0);\n  atomic_store(&lk->nowServing, 0);\n}\n\nunsigned ticket_lock_acquire(ticket_lock_t *lk){\n  unsigned ticket = atomic_fetch_add_explicit(&lk->next, 1, memory_order_relaxed);\n  while (atomic_load_explicit(&lk->nowServing, memory_order_acquire) != ticket) {\n    _mm_pause();\n  }\n  return ticket;\n}\n\nvoid ticket_lock_release(ticket_lock_t *lk){\n  atomic_fetch_add_explicit(&lk->nowServing, 1, memory_order_release);\n}\n```\n\nFollow-up Questions:\n- How would you test fairness under heavy contention?\n- How would you extend to multiple locks and prevent deadlock?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-10",
        "questionId": "q-17570",
        "question": "Design a scalable multi-tenant QoS component for a NUMA server that guarantees latency-sensitive tenants a bounded CPU, memory bandwidth, and cache usage. Propose concrete mechanisms using Linux-like tools (cgroups v2, memcg, NUMA policies) and a practical API. Include data structures, enforcement paths, and a test plan for contention and sudden workload shifts?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Allocate 8 MB per thread with a guard page below the stack. A touch on the guard triggers a page fault; runtime unwinds thread-local state, marks the thread dead, cleans resources, and yields to the s",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a seqlock: writer increments seq to odd, writes value, uses a memory barrier, then increments seq to even. Reader samples seq, reads value, re-samples seq, and repeats if seq changed or is odd. Us",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Solution Overview A multi-tenant QoS subsystem for NUMA servers requires coordinated control across CPU scheduling, memory bandwidth, and cache allocation. This design leverages cgroups v2 for CPU isolation, memcg fo…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design per-process I/O queues and a single disk worker thread. Each process has its own FIFO queue protected by a spinlock. The disk thread cycles through queues in round-robin order, serving up to three requests per cy…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates the candidate's ability to design cross-layer system solutions that integrate OS-level primitives with hardware constraints. It tests understanding of:\n\n- **Linux resource controllers**: cgroups v2, memcg, and their interaction patterns\n- **Hardware topology**: NUMA architecture, memory bandwidth, cache hierarchy\n- **System design**: API design, data structure choices, enforcement mechanisms\n- **Operational concerns**: Testing strategies for multi-tenant environments\n\n## Key Concepts Covered\n\n1. **Per-tenant isolation and fairness**: Ensuring latency-sensitive workloads receive guaranteed resources while best-effort tenants consume residual capacity\n2. **cgroups v2 and memcg integration**: Using modern cgroups interface for CPU and memory enforcement\n3. **NUMA-aware placement**: Binding memory and CPU to minimize cross-node access latency\n4. **Dynamic rebalancing**: Adapting quotas under changing load conditions\n5. **Measurement and monitoring**: Extracting telemetry from kernel interfaces for observability",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-11",
        "questionId": "q-9952",
        "question": "In a toy Linux-like kernel with NUMA nodes, implement a NUMA-aware allocator that prefers local pages and migrates a bounded number per tick from remote to local under pressure. Describe per-node freelists, ownership flags, and provide a minimal C sketch for alloc() and migrate_one_page() using CAS to move ownership. Include a simple test scenario with two threads on different nodes to illustrate improvement and contention handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-VM memory credits and a global pressure estimator. The ballooner tracks a deficit for each VM; when host pressure rises, it reclaim increasingly from the VM's local free list, marking pages re",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a hybrid memory page migration policy for DRAM+PMEM. On first touch, compute a score from recency, frequency, and NUMA distance; migrate hot pages to PMEM if the score exceeds a threshold, els",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "On a TLB miss, trap to the kernel; walk the page table root-to-leaf using the ASID. If PTE present, update the TLB and resume. If not, pick a victim frame (LRU/clock), allocate, zero-fill or fetch fro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe per-node freelists and a page ownership flag; alloc() should fetch local pages first, then remote under pressure. Implement migrate_one_page() with CAS to update ownership, plus barriers to m",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests NUMA locality understanding, memory allocation strategy, and safe cross-node migration under contention.\n\n## Key Concepts\n- NUMA locality and cross-node migration\n- Per-node freelists and ownership flags\n- Atomic operations and memory barriers\n- Contention control and starvation avoidance\n\n## Code Example\n```javascript\n// Pseudo-implementation\nfunction alloc(local) {\n  let p = localFree[local].pop();\n  if (p) return p;\n  migrate_batch(local);\n  return localFree[local].pop();\n}\nfunction migrate_one_page(to) {\n  let from = remoteNodeWithPage();\n  let p = remoteFree[from].pop();\n  if (!p) return null;\n  if (CAS(p.owner, from, to)) {\n     push(localFree[to], p);\n     return p;\n  } else {\n     push(remoteFree[from], p);\n     return null;\n  }\n}\n```\n\n## Follow-up Questions\n- How would you measure NUMA locality improvements with perf counters?\n- How to tune cap and migrate strategy under interconnect bandwidth spikes?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-12",
        "questionId": "q-6406",
        "question": "You're adding a transparent compressed, demand-paged anonymous memory cache. On a touch fault for a 4KB page not resident and without a backing file, describe the end-to-end flow: allocate a page, decide fill source (zero-fill, decompress from a shared compressed cache, or swap-in), update per-page state, handle concurrent faults across CPUs to avoid duplicate work, and ensure fairness under memory pressure. Include TLB handling and prefetch hints as needed?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "CFS uses a red-black tree ordered by vruntime; the task with the smallest vruntime runs next. CPU-bound tasks accumulate vruntime quickly, while I/O-bound tasks sleep, so their vruntime grows slowly, giving them priorit…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-container credits (nano-seconds) and per-task accounting. Extend task struct with container_id and rem_credits; on each schedule, deduct rem_credits by the runtime slice; if rem_credits < 0, t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Maintain a min-heap of sleeping threads by wake_time. Each thread stores wake_time and a per-thread semaphore. sleep(ms) computes wake = now + ms, inserts thread into the heap, blocks on its semaphore",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On a touch fault for a 4KB page not resident and without a backing file, the handler first checks page presence and performs an atomic test-and-set operation to reserve the page. A 4KB page is allocated from the buddy a…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests reasoning about a modern OS feature (compressed memory) integrated with the fault path, under contention and memory pressure.\n\n## Key Concepts\n\n- Demand paging, per-page state, and a shared compressed cache\n- Concurrency control for faults across CPUs (CAS, in-flight markers)\n- Cache coherence, TLB shootdowns, and NUMA-aware placement\n\n## Code Example\n\n```c\n// Sketch of fault handler (not complete)\nint handle_fault(page_t *p, vm_area_t *v) {\n  if (p->present) return 0;\n  if (!atomic_try_lock(&p->lock)) return retry;\n  if (p->present) { unlock(p); return 0; }\n  \n  //\n```",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-13",
        "questionId": "q-4410",
        "question": "Scenario: a Linux server uses an anonymous mmap buffer as a circular IPC queue shared by 8 workers. Each slot contains an 8-byte header (seq, done) and a 256-byte payload. Writers update header before/after writing payload; readers rely on header to detect complete entries. Propose a precise, lock-free protocol that guarantees readers never observe partially updated entries, including the exact write/read order, required memory barriers, and a concrete contention test on real hardware?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a hierarchical I/O scheduler with per-container credits, per-device queues, and adaptive budgets that allow bursting within safe limits. Use per-core run queues combined with token buckets and a global budget all…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-slot seqlock-like protocol. Writer: publish 0 to seq, write payload, then set seq to 1 (commit). Reader: read seq; if 0 or if seq changes before reading payload, retry; otherwise read payloa",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "A per-core circular deque with a head/tail index and a small version stamp to guard against ABA; local enqueue/dequeue use atomic fetch_add/ncas with release/acquire semantics; other cores steal by CA",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a lock-free MPMC ring with per-slot sequence counters and atomic head/tail indices in a page-aligned shared memory region. Each slot is cache-line padded (~64 bytes) and contains a sequence number initialized to its…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests lock-free IPC design, precise memory ordering, and verification under contention.\n\n## Key Concepts\n\n- lock-free primitives, per-slot sequencing, memory barriers, mmap-based IPC, cache coherence.\n- detection of partial writes and ensuring visibility under contention.\n\n## Code Example\n\n```c\ntypedef struct { uint64_t seq; char payload[256]; } slot_t;\n\nvoid write_slot(slot_t *s, void *data, size_t n){\n  __atomic_store_n(&s->seq, 0, __ATOMIC_RELEASE);\n  memcpy(s->payload, data, n);\n  __atomic_store_n(&s->seq, 1, __ATOMIC_RELEASE);\n}\n\nbool read_slot(slot_t *s, void *out){\n  uint64_t a = __atomic_load_n(&s->seq, __ATOMIC_ACQUIRE);\n  if (a != 1) return false;\n  memcpy(out, s->payload, 256);\n  uint64_t b = __atomic_load_n(&s->seq, __ATOMIC_ACQUIRE);\n  return (a == b && a == 1);\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-14",
        "questionId": "q-11218",
        "question": "Design and implement a Linux-like page cache eviction policy for a multi-tenant system with per-tenant quotas and NUMA awareness. Include data structures, eviction criteria (hotness, recency, writeback), and race-free reclamation under memory pressure. Provide steps to test fairness and performance under mixed workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "At fork, copy the parent’s page table entries but mark all pages as read-only and increment per-page reference counts so pages can be shared. On a write fault, allocate a private page, copy the old co",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "When a blocking read() encounters an empty pipe, the kernel blocks the current task by enqueuing it on the pipe's wait queue and putting it to sleep. When a writer appends data, the pipe buffer length becomes non-zero,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a per-tenant quotaed page cache with per-NUMA hot/cold lists and an LRU eviction that prioritizes frequently-hit, clean pages. Maintain atomic per-tenant counters and a central reclaim daemon;",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a per-container RSS quota using per-page reference counts and a global quota tree. Track shared pages and COW pages separately, with a soft quota and proportional eviction across containers. Us",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nMemory pressure in multi-tenant systems requires isolated, fair cache management.\n\n## Key Concepts\n- Per-tenant accounting, NUMA-aware data placement\n- LRU-based eviction with hot/cold separation\n- Concurrency control: CAS/state transitions, non-blocking updates\n- Reclaim daemon analogous to kswapd\n\n## Code Example\n```c\ntypedef struct {\n  int quota_pages;\n  atomic_int hits;\n} tenant_t;\n```\n\n## Follow-up Questions\n- How would you prove correctness under concurrent eviction?\n- How would you adapt for writeback-intensive workloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-15",
        "questionId": "q-14162",
        "question": "Design a block IO scheduler for a single NVMe device that supports three request classes: latency-critical (deadline), throughput-oriented (bulk), and speculative prefetch. Each request carries size, optional deadline timestamp, and class. Describe enqueue/dequeue, reordering across classes, aging to prevent starvation, and how to guarantee fairness under mixed workloads (e.g., fast DB lookups vs large sequential backups). Provide a minimal C-like sketch for enqueue, dequeue, and next-request selection?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Thread A loads a block from its per-core freelist and begins use; Thread B, holding the pool lock, frees the same block into the global pool. A's subsequent access may hit freed memory or see partially corrupted data, c…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "On fault below stack base, perform an atomic CAS to subtract one page from the thread's stack-base. If CAS wins, allocate a new page, zero-fill or load from swap, map it with user permissions, and flu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Under memory pressure, the kernel detects the pressure condition through signals from the page allocator (e.g., allocation failures) or from the memory cgroup itself. The reclaim loop then computes per-cgroup memory def…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Three queues: latency (EDF), bulk, prefetch. Each IO carries size, arrival, deadline, class. Enqueue into its queue; pick_next prefers the earliest deadline in latency; if none, service bulk within a",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical OS IO scheduling with real-time constraints, QoS, and fairness in a multiclass path from submission to hardware.\n\n## Key Concepts\n\n- EDF scheduling within a latency class\n- Multiclass IO QoS and quotas\n- Aging to prevent starvation\n- Concurrency-safe enqueue/dequeue\n- Fairness under mixed workloads\n\n## Code Example\n\n```c\ntypedef struct {\n  int cls;            // 0=LAT,1=BULK,2=PREF\n  size_t size;\n  uint64_t arrival;\n  uint64_t deadline;    // valid if LAT\n} rq_t;\n\n// enqueue, dequeue, next_request sketches (conceptual)\n```\n\n## Follow-up Questions\n\n- How would you extend to multiple channels or per-queue throttling?\n- How would you validate tail latency under concurrent DB lookups and large backups?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-16",
        "questionId": "q-5255",
        "question": "In a multi-core OS scheduler, design a scalable per-core work queue that supports work-stealing across cores. Implement as a lock-free double-ended queue used by local producers and the local dequeuer, with a theft path from other cores. Describe data structures, memory ordering (acquire/release), ABA avoidance, and how you ensure correctness under high contention and preemption. Provide pseudo-code for enqueue, dequeue, and steal and discuss testing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On a fault, acquire a per-page lock; CAS-state NONE->PENDING and enqueue an IO task to the local NUMA node. If data exists, map; if not, zero-fill while IO in flight. On IO completion, flip state to V",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A per-core circular deque with a head/tail index and a small version stamp to guard against ABA; local enqueue/dequeue use atomic fetch_add/ncas with release/acquire semantics; other cores steal by CA",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use per-CPU circular buffers with atomic head/tail and a small version tag to avoid ABA. Local enqueue uses MPSC CAS on tail; steal uses CAS to pop from the victim's head, with a random neighbor and a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-CPU write-back cache with a small on-disk journal. Implement: 1) block-tagged cache lines with dirty bits; 2) a write-back queue flushed in batches; 3) two-phase commit via a journal.bin on disk,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes lock-free concurrency, memory ordering, and real-world scheduler design under high contention across many cores.\n\n## Key Concepts\n\n- Lock-free deques and work-stealing semantics (Chase-Lev style)\n- Acquire/release semantics and memory barriers\n- ABA avoidance (version stamps, pointer tagging)\n- Safe memory reclamation (hazard pointers, epochs)\n- Correctness under preemption and high thread counts\n\n## Code Example\n\n```c\n// Pseudo: Chase-Lev style per-core deque (simplified)\ntypedef struct {\n  void* slots[DEQUE_SIZE];\n  _Atomic size_t head; // stealing/read by others\n  _Atomic size_t tail; // local producer/consumer index\n  // versioning to mitigate ABA\n  _Atomic size_t stamp;\n} Deque;\n\nvoid enqueue_bottom(Deque* d, void* v){\n  size_t t = atomic_load_explicit(&d->tail, memory_order_relaxed);\n  d->slots[t & MASK] = v;\n  atomic_store_explicit(&d->tail, t+1, memory_order_release);\n}\n\nvoid* dequeue_bottom(Deque* d){\n  size_t t = atomic_load_explicit(&d->tail, memory_order_relaxed);\n  if (t == atomic_load_explicit(&d->head, memory_order_acquire)) return NULL;\n  void* v = d->slots[(t-1) & MASK];\n  atomic_store_explicit(&d->tail, t-1, memory_order_relaxed);\n  return v;\n}\n\nvoid* steal_top(Deque* d){\n  size_t h = atomic_load_explicit(&d->head, memory_order_acquire);\n  size_t t = atomic_load_explicit(&d->tail, memory_order_acquire);\n  if (t <= h) return NULL;\n  void* v = d->slots[h & MASK];\n  if (atomic_compare_exchange_weak_explicit(&d->head, &h, h+1, memory_order_acquire, memory_order_relaxed))\n    return v;\n  return NULL;\n}\n```\n\n## Follow-up Questions\n\n- How would you extend for priority tasks and dynamic resizing?\n- How would you validate absence of ABA, memory leaks, and task loss under stress?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-17",
        "questionId": "q-29208",
        "question": "In a Linux-like host with a three-tenant NUMA pool backed by a shared file cache, design a cross-tenant, deduplicated memory allocator. On a page fault, decide whether to allocate, fetch, or share a page, while enforcing per-tenant quotas, maintaining isolation, and avoiding double IO with CAS. Describe end-to-end fault path, per-page state machine, eviction, and a minimal C-like patch sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On a page fault: CAS-claim the page state; check per-tenant quota; if a shared mapping exists, map it with correct permissions; else fetch from backing cache, zero-fill if cold, and install mapping; u",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design and implement a kernel-space robust futex primitive to synchronize user-space threads on a shared address, addressing termination safety, spurious wakeups, and wake-order guarantees under preemption. Provide end-…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a kernel IPC channel with bounded per-channel memory and multiple producers/consumers. Use a ring buffer with per-message header (len, pri, ts) and include backpressure signals. Implement a loc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Concrete interleaving: two 16-bit stores to a single 32-bit word write high half 0xAABB then low half 0xCCDD. Reader may observe 0xAABB0000 or 0x0000CCDD, i.e., a torn read since writes aren’t atomic.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-tenant memory sharing, isolation, and performance on a NUMA system with a deduplicated cache. Requires robust race handling and quota enforcement.\n\n## Key Concepts\n- CAS-based per-page state transitions\n- Per-tenant quotas and privacy\n- Shared vs private mappings and reference counting\n- Eviction strategies with NUMA locality\n\n## Code Example\n```c\n// Minimal fault path sketch\nint page_fault_handler(...) {\n  // CAS on page->state, quota checks, map or fetch\n  return 0;\n}\n```\n\n## Follow-ups\n- How to test starvation under concurrent faults?\n- How to extend for live migrations and cache invalidation?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-18",
        "questionId": "q-5075",
        "question": "On a Linux server using blk-mq with bfq, a worker pool handles both small random I/O (4 KB) and large sequential I/O (1 MB). Under peak load, tail latency for small I/O degrades unpredictably. Provide a concrete diagnostic plan to attribute latency to the IO scheduler, and outline a fix using per-cgroup IO weights or deadlines, including exact commands and measurement criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Page Fault to Eviction Sequence **1. User-space Page Fault** - `do_user_addr_fault()` → `handle_mm_fault()` → `__handle_mm_fault()` - Page walk via `walk_page_range()` to find PTE - If PTE not present: `do_fault()` →…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-slot seqlock-like protocol. Writer: publish 0 to seq, write payload, then set seq to 1 (commit). Reader: read seq; if 0 or if seq changes before reading payload, retry; otherwise read payloa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Explain a NUMA-aware page migration under memory pressure in a Linux-like kernel. Include trigger, candidate selection, source/dest node choice, per-page state, PTE/TLB updates (including lazy invalid",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create two cgroups: foreground (io.weight=200) and background (io.weight=10); move short I/O threads into foreground. Run fio with randread 4k, and record p95/p99 latency; monitor throughput with iost",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical OS skills: IO scheduling, cgroups, and performance measurement under mixed workloads. It asks for a reproducible diagnostic protocol and a concrete fix that respects production constraints.\n\n## Key Concepts\n\n- Block IO schedulers (bfq/mq-deadline) and their fairness\n- cgroup-based QoS (io.weight, io.max)\n- Telemetry: p95/p99 latency, IOPS, throughput\n- Benchmarking with fio and iostat\n\n## Code Example\n\n```bash\n# Example: create cgroups (v2)\nmkdir -p /sys/fs/cgroup/io/foreground\necho 200 > /sys/fs/cgroup/io/foreground/io.weight\n# move pid 1234 to foreground\necho 1234 > /sys/fs/cgroup/io/foreground/cgroup.procs\n```\n\n## Follow-up Questions\n\n- How would you extend this to multi-tenant backends?\n- What if the device uses network-backed storage?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-19",
        "questionId": "q-2477",
        "question": "Explain how Linux handles a write to a 2MB Transparent Huge Page (THP) that only partially overlaps with a 4KB region. Describe the fault path, the split_huge_page path (and khugepaged if applicable), PTE updates, TLB shootdowns, and the performance implications vs pre-splitting THPs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a NUMA-aware cross-VM shared page cache with per-VM quotas on a Linux-like host. Pages load lazily on first touch and are cached globally, guarded by per-VM credits to throttle prefetches and prevent cache-floodi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-CPU circular buffers with atomic head/tail and a small version tag to avoid ABA. Local enqueue uses MPSC CAS on tail; steal uses CAS to pop from the victim's head, with a random neighbor and a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "On a write, a page fault splits the 2MB THP into 512 4KB pages via split_huge_page, coordinated by khugepaged or on-demand. The kernel copies the write into the new 4KB page, updates the PTEs to point",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "When a guest G touches a page P that is on node N, collect per-node access counters for G and estimate migration cost; select target node T with max(G-locality - cost) within budget; schedule migratio",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of THP in Linux, on-demand page splitting, and the impact on performance when only a small region is written.\n\n## Key Concepts\n- Transparent Huge Pages (THP) and 4KB subpage mapping\n- split_huge_page and khugepaged coordination\n- Page Table Entries (PTEs) updates and COW semantics\n- TLB shootdowns across CPUs and performance impact\n\n## Code Example\n```c\n// high-level sketch of handling a THP write fault\nint handle_thp_write(struct vm_area_struct *vma, unsigned long addr) {\n  if (is_hugepage(addr)) {\n    split_huge_page(vma, addr);\n    // fault restarts, now maps 4KB pages\n  }\n  // mark dirty, proceed with normal write\n  return 0;\n}\n```\n\n## Follow-up Questions\n- How does THP splitting interact with NUMA memory policies?\n- What are the trade-offs of aggressive THP splitting vs keeping large pages for workload with mixed write sizes?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-20",
        "questionId": "q-26388",
        "question": "Design and implement an in-kernel adaptive file prefetcher that switches between sequential read-ahead and random-access patterns based on observed stride and hit rates. Provide data structures to track per-file access patterns, a policy for adjusting prefetch distance, and a test plan showing correctness and performance under mixed workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an adaptive file prefetcher using per-file state tracking with a pattern classification engine that distinguishes between sequential, strided, and random access patterns based on offset deltas and cache hit ra…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "On first write fault to a hugepage, serialize with per-region lock; split the 2MB page into 4KiB frames by materializing new PTEs and updating the page-table tree. If shared, perform Copy-On-Write and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Pin the latency-sensitive task to a dedicated CPU core using taskset, isolate CPUs via the isolcpus kernel parameter, and redirect device interrupts away using smp_affinity. Employ ftrace with trace_irq_handler_entry/ex…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a balanced interval tree (such as a red-black tree or AVL tree) where each node represents a locked byte range [start, end). Each node contains a mutex and a wait-queue for threads blocked on that range. To acquire…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests candidates' practical skills in I/O optimization by combining read-ahead mechanisms with workload-adaptive strategies—a common challenge at scale.\n\n## Key Concepts\n- Per-file I/O pattern tracking via offset delta analysis\n- Adaptive prefetch distance based on cache hit/miss feedback\n- Concurrency control using RCU and per-CPU data structures\n- Avoiding cache pollution from misdirected prefetch requests\n\n## Implementation Considerations\n- **Stride confidence**: Require N consecutive consistent deltas before classifying as strided\n- **Prefetch distance**: Start conservative (e.g., 4 pages) and adjust based on observed hit rates; increase distance for sequential workloads, decrease or disable for random access\n- **Pattern transitions**: Allow graceful switching between patterns as workload characteristics evolve over time",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-21",
        "questionId": "q-22059",
        "question": "Explain a concrete priority inversion scenario in a one-core, preemptive OS where a high-priority thread waits on a semaphore guarded by a low-priority thread while a middle-priority thread runs. Propose a basic priority-inheritance scheme: when a waiter arrives, elevate the holder’s priority; how to propagate to chained waiters; how and when to restore priorities on release; and what per-thread data to track. Provide a concise, language-agnostic outline of wait and signal behavior?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Writer blocks when the buffer is full; reader blocks when empty. Use a fixed circular buffer, a mutex, and two condition variables not_full and not_empty. After enqueue/dequeue, signal the opposite si",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "RCU per bucket: readers run non-blocking; writers replace a bucket by allocating a fresh node, copying data, linking into the bucket list, and swapping the head with a release store, followed by synch",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Track per-task memory usage and quota in a per-task struct; on page fault, atomically update usage. If usage exceeds quota, throttle the task by reducing its remaining timeslice and moving it to a low-priority queue unt…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Priority inversion occurs when a high-priority thread H waits on a semaphore held by a low-priority L while a mid-priority M runs. Inheritance raises L.priority to max(L.orig, H.priority). On release,",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of priority inversion and inheritance, critical in latency-sensitive systems.\n\n## Key Concepts\n\n- Priority inversion\n- Priority inheritance scheme\n- Per-thread metadata (orig_priority, current_holder)\n- Wait queues and wake-up policy\n\n## Code Example\n\n```javascript\n// Implementation sketch (language-agnostic)\n```\n\n## Follow-up Questions\n\n- How to extend to multiple resources and avoid deadlocks?\n- How to handle nested inheritance and timeouts?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-22",
        "questionId": "q-471",
        "question": "You're designing a GPU memory manager for CUDA applications. How would you implement a memory allocator that handles both unified memory and explicit device memory, considering fragmentation, coalescing, and the 48-bit address space limitations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Thread A loads a block from its per-core freelist and begins use; Thread B, holding the pool lock, frees the same block into the global pool. A's subsequent access may hit freed memory or see partially corrupted data, c…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Three ready queues Q0–Q2 with quanta 4, 8, 16. Aging promotes waiting tasks by one level after A ticks to prevent starvation. On quantum expiry, move the running task to the next lower level; on IO co",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Only one thread performs the IO: acquire per-page lock, set state LOADING, issue disk read, then set READY and release lock. Other threads wait on a condition/wait-queue; on wakeup they re-check, map",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a hybrid memory allocator combining segregated free lists for different size classes with a buddy system for large allocations, utilizing virtual memory techniques to efficiently manage the 48-bit addr…",
            "isCorrect": true
          }
        ],
        "explanation": "## Memory Management Architecture\n\n### Allocation Strategies\n- **Segregated Lists**: Separate free lists optimized for small (4KB-64KB), medium (64KB-1MB), and large (>1MB) allocations to minimize search time\n- **Buddy System**: Power-of-2 allocation for large blocks enabling efficient coalescing and reducing external fragmentation\n- **Slab Allocation**: Specialized pools for frequently used small objects to minimize internal fragmentation\n\n### Address Space Management\n```c\n// 48-bit virtual address layout\ntypedef struct {\n    uint64_t prefix : 16;  // Reserved for future expansion\n    uint64_t vaddr   : 48;  // Actual virtual address\n} gpu_vaddr_t;\n```\n\n### Unified Memory Optimization\n- **Migration Policies**: Implement adaptive page migration based on access patterns and locality\n- **Prefetching**: Hardware-guided prefetching for anticipated memory accesses\n- **Page Fault Handling**: Efficient page fault resolution with minimal CPU intervention",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-23",
        "questionId": "q-21493",
        "question": "Design a lock-free per-core work-stealing deque for a multi-core Linux-like scheduler. Each core maintains a local circular buffer; the core pushes/pops at the tail, while other cores steal from the head. Specify the algorithm, memory ordering, ABA handling, and starvation avoidance. Provide a minimal C-like patch for push/pop/steal and a microbenchmark plan under heavy contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Concrete interleaving: two 16-bit stores to a single 32-bit word write high half 0xAABB then low half 0xCCDD. Reader may observe 0xAABB0000 or 0x0000CCDD, i.e., a torn read since writes aren’t atomic.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-page state machine with an atomic state (INVALID, LOADING, LOADED) and a tiny per-page lock plus a wait queue. On a fault, a thread wins the lock, sets LOADING, issues IO, fills the page, th",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-container RSS quota using per-page reference counts and a global quota tree. Track shared pages and COW pages separately, with a soft quota and proportional eviction across containers. Us",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-core deque with atomic head/tail; local push/pop on tail; steals from head by other cores; ABA protection via version counters or tagged pointers; memory fences around updates; bounded buffer to s",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nExplain real-world scheduler contention with many cores and cache locality. This tests lock-free design, memory ordering, and correctness under races.\n\n## Key Concepts\n- Lock-free data structures\n- Memory ordering (acquire/release)\n- ABA problem and version counters\n- Work-stealing guarantees and starvation avoidance\n\n## Code Example\n```c\n// sketch\ntypedef struct {\n  _Atomic int head;\n  _Atomic int tail;\n  void *buf[DEQUE_SIZE];\n} Deque;\n\nstatic inline void push(Deque *d, void *task) {\n  int t = atomic_fetch_add_explicit(&d->tail, 1, memory_order_relaxed);\n  d->buf[t & (DEQUE_SIZE-1)] = task;\n}\n\nstatic inline void *pop(Deque *d) {\n  int t = atomic_fetch_sub_explicit(&d->tail, 1, memory_order_acq_rel);\n  int h = atomic_load_explicit(&d->head, memory_order_acquire);\n  if (t <= h) return NULL;\n  return d->buf[(t-1) & (DEQUE_SIZE-1)];\n}\n\nstatic inline int steal(Deque *d, void **out) {\n  int h = atomic_load_explicit(&d->head, memory_order_acquire);\n  int t = atomic_load_explicit(&d->tail, memory_order_acquire);\n  if (h >= t) return 0;\n  void *candidate = d->buf[h & (DEQUE_SIZE-1)];\n  if (atomic_compare_exchange_weak_explicit(&d->head, &h, h+1, memory_order_acq_rel, memory_order_acquire)) {\n    *out = candidate;\n    return 1;\n  }\n  return 0;\n}\n```\n\n## Follow-up Questions\n- How would you prove starvation-freedom and bound the latency under contention?\n- What benchmarks test locality and contention across 64+ cores?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-24",
        "questionId": "q-24160",
        "question": "Design a per-tenant I/O throttling mechanism integrated into a block device scheduler for a container-heavy service. Map each tenant to its own I/O queue, implement a credit-based throttling scheme with time-based credit replenishment, and handle bursty writes without starving others. Provide a minimal C-like sketch for enqueue, dequeue, and next-request selection, and a test scenario (A: many small reads; B: large writes) that demonstrates fairness vs naive FIFO?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Demand paging loads pages only on first access, triggering page faults handled by the OS. Page replacement algorithms like LRU, FIFO, or Clock evict pages when memory is full. The TLB caches recent translations to reduc…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Assign each tenant a dedicated I/O queue with a credit balance. Refill credits at a rate aligned with target latency; enqueue only if credits remain, else enter overdraft with decay. Dequeue selects t",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a NUMA-aware memory allocator extension for a 4-socket server running latency-sensitive trading workloads alongside background analytics. The allocator should prefer local memory pages, track per-node pressure me…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a hash map for O(1) lookup and a doubly linked LRU list to order blocks. Each block has a simple spinlock or mutex and a per-block state: valid, dirty, refcount. On miss, atomically acquire the bl",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nMulti-tenant I/O contention tests practical OS scheduling design, fairness guarantees, and real workload handling on a block device.\n\n## Key Concepts\n\n- per-tenant queues\n- credit-based throttling\n- burst handling\n- fairness metrics\n\n## Code Example\n\n```javascript\n// Minimal sketch: enqueue/dequeue with per-tenant credits (pseudo-C)\nclass TenantQueue {\n  constructor(creditLimit) {\n    this.credits = creditLimit;\n    this.queue = [];\n  }\n  enqueue(req) { this.queue.push(req); }\n  dequeue() { return this.queue.shift(); }\n}\nfunction chooseNext(tenants) {\n  // pick queue with max deficit ratio: (creditUsed / max(credit,1))\n  let best = null; let bestScore = -Infinity;\n  for (const t of tenants) {\n    const score = t.credits <= 0 ? -Infinity : 1 - t.credits / t.creditLimit;\n    if (score > bestScore) { bestScore = score; best = t; }\n  }\n  return best && best.queue.length ? best.dequeue() : null;\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt for non-uniform I/O sizes?\n- How would you test for tail-latency regression under burst bursts?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-24T07:16:51.848Z",
    "version": 1
  }
]