[
  {
    "id": "test-operating-systems",
    "channelId": "operating-systems",
    "channelName": "Operating Systems",
    "title": "Operating Systems Knowledge Test",
    "description": "Test your understanding of Operating Systems with 25 questions.",
    "questions": [
      {
        "id": "operating-systems-q-0",
        "questionId": "q-3625",
        "question": "In a Linux kernel driver for a PCIe DMA device, the host uses a shared descriptor ring with the device. A producer thread writes descriptors and updates a head pointer; the device consumes descriptors and raises an interrupt on completion. Describe a concrete interleaving that could yield a lost or out-of-order completion if there is no proper synchronization between producer writes and device processing. Propose a minimal fix using per-descriptor sequence numbers and memory barriers (e.g., smp_wmb, dma_wmb) and explain how this guarantees in-order, at-least-once completion, including cache coherence and DMA semantics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Interleaving: producer writes desc0, then desc1, then updates head to 1; device may observe head=1 and fetch desc1 before desc0 is visible due to missing barriers. Fix: add a per-descriptor seq field; device validates s…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-node reclaim with per-node LRU, per-VM quotas, and a cross-node coordinator for steals. Each node tracks active/inactive lists and per-page scores (recency, dirty flag, NUMA distance). Under press",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-slot sequence numbers and release/acquire semantics. Producer writes data, then performs a release write of slot.seq to signal readiness; consumer reads slot.seq with acquire, validates, then",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-VM I/O budget tracked across NUMA nodes. Each submission consumes tokens; on exhaustion, requests join a per-CPU throttle queue. Refill tokens periodically and coordinate cross-CPU operations with IPI-driven b…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests concrete reasoning about DMA rings, ordering guarantees, and barrier semantics in drivers.\n\n## Key Concepts\n- PCIe DMA descriptor rings, producer/consumer synchronization\n- Per-descriptor sequence numbers, publish semantics\n- Memory barriers (dma_wmb, smp_wmb) and DMA coherence\n- Cache lines and visibility between CPU and device\n\n## Code Example\n```c\n// pseudo-C like\nstruct desc { uint64_t addr; uint32_t len; uint32_t flags; uint64_t seq; };\ndesc[i].addr = data; \ndesc[i].len = n; \ndma_wmb();\ndesc[i].seq = i; \nsmp_wmb();\nhead = i; // publish\n```\n\n## Follow-up Questions\n- How would you handle sequence number wraparound?\n- What if the device doesn't support sequence numbers?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-1",
        "questionId": "q-8994",
        "question": "Scenario: a web-scale host maintains thousands of per-connection timeouts (1 ms to 2 s). Design a scalable, NUMA-aware timer subsystem using a two-tier timer wheel (coarse 1 ms buckets with fine sub-queues). Describe data structures, insertion, expiration, race conditions, and cancellation. Provide minimal C sketches for add_timer and tick, and a test plan to measure tail latency and fairness under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a hash map for O(1) lookup and a doubly linked LRU list to order blocks. Each block has a simple spinlock or mutex and a per-block state: valid, dirty, refcount. On miss, atomically acquire the bl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-thread atomic credits with per-core run queues. On each tick, increment credits up to a cap; CPU work spends 1 credit; I/O spends 2 credits and blocks if balance would go negative. A tick handler",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a two-tier timer wheel: a coarse wheel with 1024 buckets at 1 ms granularity, and per-bucket sub-queues for sub-ms expirations. Maintain per-NUMA CPU timer lists to minimize cross-node traffic",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-core deque with atomic head/tail; local push/pop on tail; steals from head by other cores; ABA protection via version counters or tagged pointers; memory fences around updates; bounded buffer to s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests knowledge of kernel timing facilities, NUMA awareness, and lock-free structures under high timer density.\n\n## Key Concepts\n- Timer wheels\n- Concurrency and CAS\n- NUMA locality\n- Test planning and jitter analysis\n\n## Code Example\n```c\n// add_timer, tick skeleton\ntypedef struct timer { volatile uint64_t expires; struct timer *next; void (*cb)(void*); void *arg; } timer_t;\nvoid add_timer(timer_t **buckets, timer_t *t, uint64_t now_ms){ /* ... */ }\nvoid tick(timer_t **buckets, uint64_t now_ms){ /* ... */ }\n```\n\n## Follow-up Questions\n- How does this scale with 10k+ timers per core? How would you measure worst-case latency?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-2",
        "questionId": "q-1354",
        "question": "You implement a lock-free ring buffer with two atomics (head, tail) and a data array for inter-thread communication. Describe a concrete interleaving on a weak memory model (e.g., ARM64) where the consumer observes a stale value or an invalid read due to missing ordering. Propose a minimal fix using memory_order_release on the data write/tail update and memory_order_acquire on the consumer read, and sketch a safe patch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Introduce acquire/release ordering in a ring buffer. Producer writes data[tail] then tail.store(next, memory_order_release). Consumer loads tail with memory_order_acquire and reads data[head] if head",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Trap page faults via userfaultfd and implement a per-page guard to serialize concurrent faults. When a fault occurs, one thread fills the 4 KiB page from the backing file (pread) or zeros it, then use",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Allocate from the local node freelist first; if empty, atomically steal a batch from a remote node or global pool, then return to the local node. Maintain per-object headers with node id and size clas",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Pin threads to sockets, allocate per‑socket queues and data, and ensure memory allocations stay on the same node via numa_bind/numa_alloc_onnode. Favor per‑socket pools to avoid remote RAM. Validate w",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of memory ordering, lock-free data structures, and weak memory models in OS CPUs. It probes ability to identify a real race and reason about inter-thread visibility across architectures.\n\n## Key Concepts\n\n- Lock-free synchronization\n- memory_order_release\n- memory_order_acquire\n- Data visibility before signaling availability\n\n## Code Example\n\n```cpp\n// Pseudo-C++ lock-free sketch\nstd::atomic<size_t> head{0}, tail{0};\nint data[N];\n\nvoid produce(int v){\n  auto t = tail.load(std::memory_order_relaxed);\n  data[t % N] = v;\n  tail.store((t+1) % N, std::memory_order_release);\n}\n\nint consume(){\n  auto t = tail.load(std::memory_order_acquire);\n  auto h = head.load(std::memory_order_relaxed);\n  if (h == t) return -1;\n  int v = data[h % N];\n  head.store((h+1) % N, std::memory_order_release);\n  return v;\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt this pattern for multiple producers/consumers?\n- What tests would you add to validate correctness across ARM/x86?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-3",
        "questionId": "q-11532",
        "question": "Design a scalable 1ms-granularity timer subsystem for a Linux-like kernel with per-CPU worker pools and a shared high-resolution timer wheel. Explain timer insertion, cancellation, and expiration, how to avoid wakeup storms under bursty timer workloads, and how hotplug/migration affects correctness. Include a minimal C-like sketch for timer_insert and timer_expire and describe a validation approach to meet 1ms SLA under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-CPU hierarchical timer wheel with 1024 slots and a global rollover bucket; timers install into the current CPU's slot and slip into higher granularity as time advances. Use per-timer gen",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "On cache miss, lock the block entry, mark inflight, initiate disk IO for the 4KB block, copy data into a shared buffer, set valid, wake waiting readers, and decrement inflight. Use per-block refcounts",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a per-slot seqlock-like protocol. Writer: publish 0 to seq, write payload, then set seq to 1 (commit). Reader: read seq; if 0 or if seq changes before reading payload, retry; otherwise read payloa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe the fault path: the trap handler checks the fault address against the process's stack growth policy and RLIMIT_STACK; if allowed, allocate a 4KB page, zero it, map it at the next lower addres",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluate practical timer subsystem design under latency, contention, and migration constraints. This tests real-world readiness beyond theory.\n\n## Key Concepts\n- High-resolution timer wheels\n- Per-CPU queues and locking discipline\n- Cancellation races and generation counters\n- Burst resilience and wakeup storm avoidance\n- CPU hotplug and cross-CPU migration semantics\n- NUMA locality considerations\n\n## Code Example\n```javascript\n// Minimal sketch (C-like)\ntypedef struct timer {\n  unsigned long expires;\n  void (*cb)(void*);\n  void *arg;\n  int cpu_hint;\n  unsigned int gen;\n  struct timer *next;\n} timer_t;\n\nstatic inline void timer_insert(timer_t *t) {\n  // naive per-CPU bucket insertion by expires\n}\nstatic inline void timer_expire(timer_t *t) {\n  t->cb(t->arg);\n}\n```\n\n## Follow-up Questions\n- How would you validate a 1ms SLA under concurrent timer insertions and expirations?\n- How would you adapt the design for CPU hotplug and cross-NUMA migrations?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-4",
        "questionId": "q-29593",
        "question": "Design a per-process I/O budget QoS scheduler for a Linux-like kernel with NVMe devices. Given a latency-sensitive browser tab issuing many small I/Os and a background updater emitting large sequential I/Os, specify data structures, budgeting policy, and core algorithms to bound bandwidth per process, prevent starvation, and adapt budgets across bursty workloads; provide enqueue/dequeue and decision pseudocode?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On a first-touch fault, the handler CASes the per-page slot from EMPTY to LOADING, wakes waiters, and fetches the page from the remote cache or disk with optional read-ahead. After a successful fetch,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-process I/O budget QoS scheduler: each process has credits; I/Os consume credits; when credits run out, requests are delayed until replenished. Use per-device WFQ-like queues and a glo",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a per-tenant quotaed page cache with per-NUMA hot/cold lists and an LRU eviction that prioritizes frequently-hit, clean pages. Maintain atomic per-tenant counters and a central reclaim daemon;",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Priority inversion occurs when a high-priority thread H waits on a semaphore held by a low-priority L while a mid-priority M runs. Inheritance raises L.priority to max(L.orig, H.priority). On release,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design OS I/O QoS with per-process quotas, fairness, and dynamic adaptation under bursty workloads.\n\n## Key Concepts\n- I/O scheduling, per-process budgets, credits, bursting, starvation avoidance, latency vs throughput\n- Workload characterization (latency-sensitive vs bulk I/O)\n- Data structures: per-process queues, global budget allocator, device queues\n- Interaction with cgroups and NVMe I/O path\n\n## Code Example\n```javascript\n// Pseudocode: per-process I/O budget scheduler (high level)\nclass BudgetScheduler {\n  constructor() {\n    this.queues = new Map(); // pid -> queue of requests\n  }\n  enqueue(req, pid) { /* allocate/consume credits, push to per-pid queue */ }\n  pickNext() {\n    // choose next request from queues with positive credits,\n    // possibly boost foreground processes during bursts\n  }\n}\n```\n\n## Follow-up Questions\n- How would you tune credits to adapt to changing workloads?\n- How would you test fairness and latency under mixed workloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-5",
        "questionId": "q-26610",
        "question": "In a Linux-like OS running thousands of containers sharing a deduplicated page cache, design a NUMA-aware cross-node eviction policy. When node A experiences memory pressure while tenants X and Y touch various pages, explain end-to-end how you classify pages (hot/cold), select victims, coordinate eviction across CPUs, and handle dirty or pinned pages while preserving fairness. Provide a minimal C-like eviction path sketch and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Compute per-page scores from hotness and per-tenant quotas; eviction favors local NUMA pages first. Under node A pressure, move candidates to per-node queues, batch-evict, and synchronize with a light",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a hierarchical I/O scheduler with per-container credits, per-device queues, and adaptive budgets that allow bursting within safe limits. Use per-core run queues combined with token buckets and a global budget all…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Decrement current->time_slice on each timer tick; if it hits zero, push current to the end of the ready queue and switch to the next ready task. Use a brief interrupt lock around the scheduler state.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Signals target a single thread; if that thread holds a mutex, the handler runs with the mutex still held, risking deadlock or data corruption unless the handler uses only async-signal-safe calls. Prac",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests NUMA-awareness, cross-node coordination, fairness, and correctness under contention for a shared cache.\n\n## Key Concepts\n\n- NUMA-aware eviction\n- per-tenant quotas\n- per-node queues and batched eviction\n- cross-node synchronization to minimize IPC overhead\n- handling dirty/pinned pages and fairness guarantees\n\n## Code Example\n\n```c\n// sketch: per-node eviction path\nstruct Page { unsigned long hot; int tenant_id; int pinned; int dirty; int node; };\nvoid evict_batch(Node *n, int limit) { /* score, filter, pick, drain */ }\n```\n\n## Follow-up Questions\n\n- How would you test scalability and fairness under multi-tenant load?\n- Which data structures minimize contention and ensure progress across thousands of cores?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-6",
        "questionId": "q-12396",
        "question": "In a dual-process setup, implement a fixed-size ring buffer in POSIX shared memory for IPC. Use atomic head/tail indices and a simple futex-like wake (spin-then-sleep) to block when full/empty without busy-waiting. Provide the memory layout, enqueue/dequeue pseudocode, race-avoidance guarantees, and a minimal test scenario demonstrating both blocking and non-blocking behavior?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "The kernel-assisted paging policy utilizes a per-page state machine with FREE, PENDING, VALID, and EVICTED states to manage file-backed shared memory across NUMA nodes. Fault claiming employs compare-and-swap operations…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A candidate would describe: set both PTEs to read-only with a refcount; on write fault, allocate a new page, copy data, update both mappings to point to the private page, atomically clear RO flag, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a shared ring buffer in shm: atomic head/tail and an array slots[]. Full when (head+1)%CAP==tail; empty when head==tail. Enqueue: while full, futex_wait(&state,0); slots[head]=v; head=(head+1)%CAP",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-page state: owner (CPU/GPU/shared), dirty, migrating, last_access, and per-node quotas. Coherence via lightweight version tags and batched invalidations on cross-access. Migrate on demand: CPU ref",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests basic IPC primitives, atomic ordering, and wakeup correctness without busy-waiting.\n\n## Key Concepts\n- POSIX shared memory layout for a ring buffer\n- Atomic head/tail indices and modulo arithmetic\n- Lightweight futex-like waiting to avoid busy-waiting\n- Correct handling of full vs empty races\n\n## Code Example\n```javascript\n// Minimal C-like pseudocode\ntypedef struct {\n  _Atomic uint32_t head;\n  _Atomic uint32_t tail;\n  uint64_t slots[CAP];\n  _Atomic int state; // 0 empty, 1 has item\n} Ring;\n```\n```javascript\n// Enqueue (producer)\nwhile (((head+1)%CAP) == tail) futex_wait(&state,0);\nslots[head] = v;\nhead = (head+1)%CAP;\nfutex_wake(&state,1);\n```\n```javascript\n// Dequeue (consumer)\nwhile (head == tail) futex_wait(&state,1);\nv = slots[tail];\ntail = (tail+1)%CAP;\nfutex_wake(&state,0);\n```\n\n## Follow-up Questions\n- How would you extend to multiple producers/consumers safely?\n- What failure modes exist if wakeups are lost and how would you mitigate them?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-7",
        "questionId": "q-9649",
        "question": "In a Linux-like kernel on a 4-socket NUMA system, design and implement a NUMA-aware anonymous page cache for memory mapped regions. Describe per-page state, how a page fault selects an allocation node, when and how to migrate pages to the nearest node, and how to prevent cross-node traffic under contention. Include a lightweight test workload and outline correctness guarantees?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On first touch, an atomic CAS marks the page as claimed by the requester; pages are placed on the requester’s local NUMA node with per-service quotas. Misses fetch backing data, optional read-ahead, a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt per-object MVCC with 64-bit version counters and a publish-on-commit scheme. Readers capture the version, read the data, then re-check the version to confirm no concurrent modification occurred. Writers prepare a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Maintain per-page: owner_node, refcount, and hot_counter. On fault, allocate from the local node; if cache miss persists, assign to local node first and mark for possible migration. A background migra",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "On first write fault to a hugepage, serialize with per-region lock; split the 2MB page into 4KiB frames by materializing new PTEs and updating the page-table tree. If shared, perform Copy-On-Write and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess NUMA awareness, per-page state management, and safe migration under contention. It probes lock ordering, race-free page fault handling, and practical testing strategies. The scenario mirrors real cross-socket workloads.\n\n## Key Concepts\n\n- NUMA-aware allocation\n- Page migration and hotness tracking\n- Concurrency control with per-page locks and CAS\n- Testing cross-node traffic and correctness\n\n## Code Example\n\n```javascript\n// illustrative structure\nstruct page_cache_entry {\n  int owner_node;\n  atomic_int refcount;\n  atomic_int hot_count;\n  spinlock_t lock;\n  bool migratable;\n};\n```\n\n## Follow-up Questions\n\n- How would you adapt this for huge pages and memory pressure?\n- How would you handle memory-backed devices vs anonymous memory in this cache?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-8",
        "questionId": "q-23681",
        "question": "Design a tiny I/O scheduler for a toy OS with a single block device and a fixed 8-slot request queue. Each request has fields: pid, lba, size, is_read. Implement fairness by per-process credit and aging; ensure no starvation for CPU-bound vs IO-bound processes. Provide minimal C-like code for enqueue and pick_next, and outline a test plan with at least P1 CPU-bound and P2 IO-bound to show aging keeps both serviced?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a two-tier scheduler with per-core queues for fast and slow cores. Each thread tracks recent_burst, last_core, and a migration_count. Enqueue prefers fast cores when burst > threshol",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a lock-free MPMC ring with per-slot sequence counters and atomic head/tail indices in a page-aligned shared memory region. Each slot is cache-line padded (~64 bytes) and contains a sequence number initialized to its…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an 8-slot fixed queue with per-process credits and aging. Each IOReq has pid, lba, size, read, plus dynamic fields: credit and age. Enqueue: drop oldest if full; pick_next scans queue to prefer re",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a quota-aware eviction path for a shared per-process page cache backed by a remote store. Track per-process credit, per-page flags (clean/dirty, claimed, evicted), and select candidates from a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of practical I/O scheduling fairness in a constrained, beginner-friendly setting.\n\n## Key Concepts\n- I/O scheduling fairness and starvation avoidance\n- Per-process credit system and aging\n- Fixed-size request queue management\n- Enqueue/dequeue logic and safe selection\n\n## Code Example\n```c\ntypedef struct IOReq {\n  int pid;\n  uint64_t lba;\n  int size;\n  int read; // 1 = read, 0 = write\n  int credit; // dynamic, per-process\n  int age;    // waiting cycles\n  int arrival;\n} IOReq;\n\nint pick_next(IOReq q[], int n) {\n  int best = -1;\n  int best_prio = -1;\n  for (int i = 0; i < n; i++) {\n    if (q[i].arrival) {\n      int prio = q[i].credit + (q[i].age > AGE_THRESHOLD ? 1 : 0);\n      if (prio > best_prio) {\n        best_prio = prio;\n        best = i;\n      }\n    }\n  }\n  return best;\n}\n```\n\n## Follow-up Questions\n- How would you test starvation under a mixed workload (CPU-bound vs IO-bound)?\n- How would you handle I/O errors and partial completions in this scheduler?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-9",
        "questionId": "q-23109",
        "question": "Design a scalable per-core I/O scheduler for a single block device that handles both random and sequential requests. Propose a two-queue design (hot for random, cold for sequential) with a merge window to coalesce adjacent blocks, use lock-free enqueues via atomic CAS, and implement a fairness policy to prevent starvation. Provide a minimal C-like sketch for enqueue, dequeue, and merging, plus a test plan with mixed workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A scalable per-core I/O scheduler with two queues (hot for random, cold for sequential), a merge window to coalesce adjacent requests, a lock-free enqueue using atomic CAS, and per-request deadlines t",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-core lock-free deques using atomic operations for push and pop operations. Monitor queue lengths through per-core counters; when imbalance is detected (local queue exceeds threshold while neighboring cores…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-CPU FD caches to reduce system-call overhead in I/O-heavy multi-tenant workloads. Store recently opened FDs per process on its current CPU; use a tiny, lock-free hash of (pid, fd) into a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a FIFO ticket lock with a wait queue. Acquire: ticket = fetch_add(&lk->next, 1); spin until nowServing == ticket; Enter critical section; Release: nowServing++ and wake the next waiter via the que",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of high-performance I/O scheduling, lock-free data structures, and fairness under mixed workloads, mirroring real kernel-path concerns in storage stacks.\n\n## Key Concepts\n- I/O scheduling and per-core queues\n- Lock-free enqueue/dequeue via CAS\n- Request merging for sequential bursts\n- Fairness and starvation mitigation via deadlines\n- Realistic testing with mixed workloads and tail latency focus\n\n## Code Example\n```javascript\n// Minimal C-like sketch (conceptual)\ntypedef struct io_req { uint64_t lba; uint32_t len; int id; struct io_req *next; } io_req;\ntypedef struct { io_req *head, *tail; } ring_t;\nstatic inline void io_enqueue(ring_t *q, io_req *r) { r->next = NULL; /* CAS-based tail append */ }\nstatic inline io_req* io_dequeue(ring_t *q) { /* pop head */ return NULL; }\nstatic inline int can_merge(io_req *a, io_req *b) { return a->lba + a->len == b->lba; }\nstatic inline io_req* merge_adjacent(io_req *a, io_req *b) {\n  if (!can_merge(a,b)) return NULL; a->len += b->len; return a;\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-10",
        "questionId": "q-25121",
        "question": "Design an NVMe I/O scheduler for a Linux-like kernel that enforces per-container IO quotas (IOPS and bandwidth) while preserving low tail latency for latency-sensitive services. Include a data locality tag to favor intra-CPU submissions, a starvation-prevention mechanism, and burst handling. Describe end-to-end from submission through completion, including per-container accounting, queue selection, and a minimal C-like sketch for enqueue, dequeue, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "End-to-end: Process A writes to a MAP_SHARED mmap page; the update propagates to the page cache and marks the page dirty, then the kernel may write it back during writeback or page eviction. CPU cache coherence ensures…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Concrete interleaving: two 16-bit stores to a single 32-bit word write high half 0xAABB then low half 0xCCDD. Reader may observe 0xAABB0000 or 0x0000CCDD, i.e., a torn read since writes aren’t atomic.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Three ready queues Q0–Q2 with quanta 4, 8, 16. Aging promotes waiting tasks by one level after A ticks to prevent starvation. On quantum expiry, move the running task to the next lower level; on IO co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-container quotas via token buckets for bandwidth and IOPS; latency-sensitive streams get a fast path; data locality tag guides dispatch to local NUMA queues; CAS-based credit claims prevent races;",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nReal workloads are multi-tenant; tail latency for latency-sensitive apps matters; this tests practical scheduling, fairness, and locality.\n\n## Key Concepts\n\n- I/O scheduling and fairness\n- Per-container quotas and tokens\n- Data locality and NUMA-aware dispatch\n- Lock-free queues and CAS for race-free updates\n- Burst handling and starvation prevention\n\n## Code Example\n\n```javascript\n// Minimal enqueue sketch\ntypedef struct {\n  int container_id;\n  int bytes;\n  int latency_sensitive;\n} io_req_t;\n\ntypedef struct {\n  volatile int iops_credits;\n  volatile int bw_credits;\n} quota_t;\n\nquota_t quotas[MAX_CONTAINERS];\nqueue_t *queues[MAX_CONTAINERS];\n\nvoid enqueue_io(io_req_t *r){\n  int c = r->latency_sensitive ? 1 : 0; // quick path flag\n  // naive atomic update for illustration\n  if (__sync_bool_compare_and_swap(&quotas[r->container_id].iops_credits, c, c-1)) {\n    push(&queues[r->container_id], r);\n  } else {\n    // fall back to slow path\n    enqueue_slow(r);\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you validate fairness under bursty traffic?\n- What metrics would you monitor in production?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-11",
        "questionId": "q-18297",
        "question": "In a Linux-like kernel running on a system with NVIDIA GPUs sharing unified memory, design a page-management policy that automatically migrates pages between CPU RAM and GPU VRAM based on access affinity. Describe per-page lifecycle (state, flags), coherence protocol between CPU and GPU, eviction criteria under memory pressure, and a minimal C-like sketch of the migration decision function and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-page state: owner (CPU/GPU/shared), dirty, migrating, last_access, and per-node quotas. Coherence via lightweight version tags and batched invalidations on cross-access. Migrate on demand: CPU ref",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "On a touch of a page on a non-local node, update a per-page heat counter and, if heat exceeds a threshold and the local node cap permits, enqueue a migration to the thread’s local NUMA node. Use an at",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose per-device queues with a small read-ahead window (e.g., 4–8 blocks) driven by locality. Use per-device counters to cap prefetches and a global limiter to prevent starvation. Provide a minimal",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Track per-page heat with a simple recency-frequency score and a per-zone migrator. On fault, if heat below threshold, enqueue a migration to PMEM and resume with a zero- or read‑daemon upon completion",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes OS-level memory management with GPU unified memory, a real-world challenge in high-performance workloads. It tests concurrency control, cross-device coherence, and migration policies under contention.\n\n## Key Concepts\n- Unified CPU/GPU memory management and migration\n- Per-page state with quotas and ownership\n- Lightweight coherence with versioning and batched invalidations\n- Async DMA, read-ahead, and eviction under pressure\n\n## Code Example\n```c\n// Pseudo migration decision\nstatic bool should_migrate_to_gpu(page_t *p, perf_t *perf){\n  if (p->owner == GPU) return false;\n  if (perf->gpu_access_hot) return true; // example heuristic\n  return p->refcount > THRESHOLD;\n}\n```\n\n## Follow-up Questions\n- How would you test fairness across tenants and ensure low tail latency?\n- What race conditions can occur during concurrent migrations and how to solve them?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-12",
        "questionId": "q-9952",
        "question": "In a toy Linux-like kernel with NUMA nodes, implement a NUMA-aware allocator that prefers local pages and migrates a bounded number per tick from remote to local under pressure. Describe per-node freelists, ownership flags, and provide a minimal C sketch for alloc() and migrate_one_page() using CAS to move ownership. Include a simple test scenario with two threads on different nodes to illustrate improvement and contention handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an SPSC ring buffer in a shared memory page with cacheline-aligned head and tail indices to prevent false sharing. Enqueue uses CAS on the head pointer with a release store after writing the item; dequeue uses C…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe per-node freelists and a page ownership flag; alloc() should fetch local pages first, then remote under pressure. Implement migrate_one_page() with CAS to update ownership, plus barriers to m",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "On a MAP_SHARED mmap write, the kernel first handles a page fault if the page isn't present, loading it from the backing file or device. The kernel then maps the page into the process's address space, updates the page t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "On a first-touch fault, the handler CASes the per-page slot from EMPTY to LOADING, wakes waiters, and fetches the page from the remote cache or disk with optional read-ahead. After a successful fetch,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests NUMA locality understanding, memory allocation strategy, and safe cross-node migration under contention.\n\n## Key Concepts\n- NUMA locality and cross-node migration\n- Per-node freelists and ownership flags\n- Atomic operations and memory barriers\n- Contention control and starvation avoidance\n\n## Code Example\n```javascript\n// Pseudo-implementation\nfunction alloc(local) {\n  let p = localFree[local].pop();\n  if (p) return p;\n  migrate_batch(local);\n  return localFree[local].pop();\n}\nfunction migrate_one_page(to) {\n  let from = remoteNodeWithPage();\n  let p = remoteFree[from].pop();\n  if (!p) return null;\n  if (CAS(p.owner, from, to)) {\n     push(localFree[to], p);\n     return p;\n  } else {\n     push(remoteFree[from], p);\n     return null;\n  }\n}\n```\n\n## Follow-up Questions\n- How would you measure NUMA locality improvements with perf counters?\n- How to tune cap and migrate strategy under interconnect bandwidth spikes?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-13",
        "questionId": "q-20967",
        "question": "Implement a 3-thread barrier primitive using only atomics. Each thread calls barrier_wait(N) and blocks until all N threads arrive, then all resume. Use an atomic counter and a generation counter to avoid wakeup races. Last thread flips the generation and resets the counter; others spin on the new generation with a short pause. Include a test where one thread is slow to prove reuse?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose per-page metadata including last_access, refcount, and owner_mask; impose per-process quotas; use an aging score combining recency and per-process fairness to pick eviction candidates. On faul",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a 3-thread barrier primitive using only atomics. Each thread calls `barrier_wait(N)` and blocks until all N threads arrive, then all resume simultaneously. Use an atomic counter and a generation counter to avo…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Two-tier invalidation: per-CPU queues plus a global batched shootdown. On PTE change, mark the VPN range dirty, enqueue the local CPU shootdown, and periodically drain to broadcast an IPI to other CPU",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a two-tier timer wheel: a coarse wheel with 1024 buckets at 1 ms granularity, and per-bucket sub-queues for sub-ms expirations. Maintain per-NUMA CPU timer lists to minimize cross-node traffic",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests fundamental understanding of atomic synchronization and barrier reuse in operating systems or concurrent programming. It highlights common race conditions and memory ordering considerations that arise when implementing synchronization primitives.\n\n## Key Concepts\n\n- **Atomic operations and memory ordering**: Using `memory_order_acq_rel`, `memory_order_release`, and `memory_order_acquire` to ensure proper visibility of updates across threads\n- **Generation counter**: Prevents race conditions when reusing a barrier—threads must not proceed until all waiting threads have been woken\n- **Busy-wait with backoff**: A short pause in the spin loop reduces CPU contention while waiting\n\n## Code Example\n\n```c\n#include <stdatomic.h>\n#include <sched.h>\n\ntypedef struct {\n    _Atomic int count;\n    _Atomic int generation;\n    int n;\n} barrier_t;\n\nvoid barrier_init(barrier_t *b, int n) {\n    atomic_init(&b->count, 0);\n    atomic_init(&b->generation, 0);\n    b->n = n;\n}\n\nvoid barrier_wait(barrier_t *b) {\n    int gen = atomic_load_explicit(&b->generation, memory_order_acquire);\n    int cnt = atomic_fetch_add_explicit(&b->count, 1, memory_order_acq_rel);\n    \n    if (cnt == b->n - 1) {\n        atomic_store_explicit(&b->count, 0, memory_order_release);\n        atomic_store_explicit(&b->generation, gen + 1, memory_order_release);\n    } else {\n        while (atomic_load_explicit(&b->generation, memory_order_acquire) == gen) {\n            sched_yield();\n        }\n    }\n}\n```\n\n## Key Implementation Details\n\n1. The **last thread** (when `cnt == n-1`) resets the counter and increments the generation, releasing all waiting threads\n2. Other threads **spin** until the generation changes, ensuring they wait for all threads to arrive\n3. Memory ordering (`acq_rel`, `release`, `acquire`) ensures proper synchronization without data races\n4. The barrier can be **reused**—after all threads pass through, the next call to `barrier_wait` will work correctly\n\n## Test for Reuse\n\n```c\nvoid *thread_func(void *arg) {\n    barrier_t *b = (barrier_t *)arg;\n    printf(\"Thread %lu entering barrier (round 1)\\n\", pthread_self());\n    barrier_wait(b);\n    printf(\"Thread %lu exited barrier (round 1)\\n\", pthread_self());\n    \n    printf(\"Thread %lu entering barrier (round 2)\\n\", pthread_self());\n    barrier_wait(b);\n    printf(\"Thread %lu exited barrier (round 2)\\n\", pthread_self());\n    return NULL;\n}\n```\n\nTo prove reuse, add a delay in one thread between the two rounds—this demonstrates the barrier correctly blocks until all N threads arrive again.",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-14",
        "questionId": "q-30366",
        "question": "In a NUMA-enabled Linux-like hypervisor hosting dozens of KVM guests across many CPU sockets, implement a scalable, low-latency page-migration mechanism to rebalance memory across NUMA nodes under sustained memory pressure. Describe data structures (per-node free lists, per-VM quotas, migration work-stealers), synchronization, and when to trigger migrations. Provide a minimal path sketch for moving a page from source to target node and updating mappings safely?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-slot seqlock-like protocol. Writer: publish 0 to seq, write payload, then set seq to 1 (commit). Reader: read seq; if 0 or if seq changes before reading payload, retry; otherwise read payloa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "On fault below stack base, perform an atomic CAS to subtract one page from the thread's stack-base. If CAS wins, allocate a new page, zero-fill or load from swap, map it with user permissions, and flu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a NUMA-aware migrator that uses per-node free lists plus per-VM quotas, with a central migration pool and per-core work-stealers. On memory pressure, pick cold pages, compute target node by acc",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Allocate from the local node freelist first; if empty, atomically steal a batch from a remote node or global pool, then return to the local node. Maintain per-object headers with node id and size clas",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design NUMA-aware memory management at scale. Evaluates page migration planning, coherence, and interaction with TLB shootdowns.\n\n## Key Concepts\n- NUMA-aware balancing and migration policies\n- Per-node freelists and per-VM quotas\n- Synchronization and per-core work-stealing\n- PTE updates, TLB coherence, and IPI coordination\n- Contention, thrash, and batch processing\n\n## Code Example\n```c\n// Pseudo-path sketch\nvoid migrate_page(page_t *p, node_t *target) {\n  lock(p->lock);\n  if (p->state != PAGE_ONLINE) return;\n  move_to_node(p, target);\n  update_pte(p);\n  barrier();\n  tlb_shootdown(p);\n  unlock(p->lock);\n}\n```\n\n## Follow-up Questions\n- How would you test under sustained memory pressure and multiple VM instances?\n- How would you ensure fairness and prevent migration thrash?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-15",
        "questionId": "q-12248",
        "question": "Design a zero-copy IPC channel between two user-space processes in a minimal OS: a shared memory ring buffer with per-slot credits, wakeup semantics, and a file-backed backing store to survive restarts. Ensure correctness under concurrent producers/consumers, handle buffer full, and minimize copies. Provide a minimal C sketch for init, enqueue, and dequeue, plus a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Compute per-page scores from hotness and per-tenant quotas; eviction favors local NUMA pages first. Under node A pressure, move candidates to per-node queues, batch-evict, and synchronize with a light",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Zero-copy IPC channel using a shared memory ring buffer with per-slot credits. Producer/consumer use atomic CAS on tail/head plus memory fences for visibility. Pin pages, back with a file to survive r",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Thread A loads a block from its per-core freelist and begins use; Thread B, holding the pool lock, frees the same block into the global pool. A's subsequent access may hit freed memory or see partially corrupted data, c…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce a single mmap ring with per-slot sequence numbers; producers reserve a slot via atomic fetch-and-add, write data, then publish by bumping sequence. Consumers spin on the expected sequence, rea",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of low-level IPC, synchronized ring buffers, and memory-ordering guarantees in a shared-memory setup. Expected to justify CAS-based coordination, fence usage, and restart-safe backing.\n\n## Key Concepts\n\n- Shared memory synchronization and cache coherence\n- Atomic CAS, memory ordering, and fences\n- Zero-copy data transfer and page pinning\n- Backpressure and restart-safety with file-backed backing\n- Race conditions in producer/consumer handshakes\n\n## Code Example\n\n```c\ntypedef struct {\n  volatile uint64_t head;\n  volatile uint64_t tail;\n  size_t capacity;\n  void* slots[]; // flexible array\n} ring_t;\n\n// Init, enqueue, dequeue sketches illustrating CAS and fences\n```\n\n## Follow-up Questions\n\n- How would you test under bursty producers with multi-core contention?\n- What memory-ordering guarantees are essential on ARM vs x86, and how would you adapt the fences?",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-16",
        "questionId": "q-28623",
        "question": "In a tiny OS on a 2-core system, implement a lock-free bounded circular buffer for IPC with a single producer on core A and a single consumer on core B. Describe the correctness guarantees, memory ordering, and wrap-around handling; provide a minimal C-like enqueue/dequeue and outline a test interleaving that could break it and how acquire/release semantics prevent it?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per-tenant token-bucket I/O throttling mechanism integrated with the OS I/O scheduler. Each tenant i has a target bandwidth b_i (bytes/sec) and a burst tolerance B_i (max bucket size). On an I/O request of size…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "On fault, kernel checks if the address is in the thread's stack VMA and if growth is allowed. If yes, allocate a zero-filled page, map it into the user page table, and perform a TLB shootdown. The fau",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a power-of-two circular buffer with atomic indices. The producer on core A increments head using fetch-and-add, writes data to the corresponding slot, then publishes via a release fence. The consumer on core B incre…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "End-to-end: Process A writes to a MAP_SHARED mmap page; the update propagates to the page cache and marks the page dirty, then the kernel may write it back during writeback or page eviction. CPU cache coherence ensures…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates understanding of lock-free inter-process communication under simple multicore constraints and cache coherence protocols. It distinguishes candidates who know the mechanics of atomics from those who understand visibility and ordering guarantees.\n\n## Key Concepts\n- Lock-free ring buffers with atomic indices\n- Acquire/release semantics for safe cross-core visibility\n- Wrap-around via power-of-two masking\n- Contention handling and backoff strategies\n\n## Code Example\n```c\n#define SIZE 1024  // power of two\n#define MASK (SIZE - 1)\n\n_Atomic size_t head = 0;\n_Atomic size_t tail = 0;\ntype_t buffer[SIZE];\n\nvoid enqueue(type_t value) {\n    size_t h = atomic_fetch_add(&head, 1, memory_order_relaxed);\n    buffer[h & MASK] = value;\n    atomic_thread_fence(memory_order_release);\n}\n\ntype_t dequeue(void) {\n    size_t t = atomic_load(&tail, memory_order_acquire);\n    type_t value = buffer[t & MASK];\n    atomic_fetch_add(&tail, 1, memory_order_relaxed);\n    return value;\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-17",
        "questionId": "q-13959",
        "question": "Design a hierarchical, per-CPU timer wheel to support 100k timers per process on a multi-core OS; specify data structures for a coarse global wheel and per-core micro-wheels, enqueue rules, tick advancement, and expiration handling, including timer coalescing. How would you validate scalability under mixed long and short workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An I/O scheduler per cgroup with quotas. Track per-cgroup bytes and requests, plus a global credit pool. Enqueue checks quota; if exhausted, block or delay until credit refresh. choose_next picks the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-tier per-CPU timer wheel: a coarse global wheel for far expirations and per-core micro-wheels for hot timers. Timers are placed into buckets keyed by their expiration modulo wheel size; enqueue uses atomic operation…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "On fault: lock the page; if present return; if loading flag is set, wait; else set loading, allocate page, unlock, read backing file into page or zero-fill, then lock again to clear loading, mark pres",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an atomic counter `remain` initialized to N and a generation counter `gen`. When a thread calls `barrier_wait`, it atomically decrements `remain`. If the result is 0, this is the last thread: reset `remain` to N, in…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of scalable timer design, cache locality, and concurrency under load. It also probes practical considerations like coalescing wakeups and per-core data locality.\n\n## Key Concepts\n- Hierarchical timer wheel (coarse + per-core wheels)\n- Lock-free/atomic enqueue into per-core structures\n- Tick progression and expiration emission\n- Timer coalescing to reduce wakeups\n\n## Code Example\n```c\n// Pseudo-structs illustrating the approach\ntypedef struct Timer { \n    uint64_t expires; \n    void (*cb)(void*); \n    void *arg; \n    struct Timer *next; \n} Timer;\n\ntypedef struct TimerWheel {\n    Timer *buckets[WHEEL_SIZE];\n    uint64_t current_tick;\n    atomic_int pending_count;\n} TimerWheel;\n\n// Global coarse wheel for far expirations\nTimerWheel global_wheel;\n\n// Per-core micro-wheels for hot timers\nTimerWheel per_core_wheels[MAX_CPUS];\n\n// Lock-free enqueue using atomic operations\nvoid enqueue_timer(Timer *timer, int core_id) {\n    uint64_t expires = timer->expires;\n    TimerWheel *wheel;\n    \n    if (expires - current_time > COARSE_THRESHOLD) {\n        wheel = &global_wheel;\n    } else {\n        wheel = &per_core_wheels[core_id];\n    }\n    \n    uint64_t bucket_idx = expires % WHEEL_SIZE;\n    \n    // Atomic insertion into bucket\n    do {\n        timer->next = wheel->buckets[bucket_idx];\n    } while (!atomic_compare_exchange_weak(\n        &wheel->buckets[bucket_idx], \n        &timer->next, \n        timer\n    ));\n    \n    atomic_fetch_add(&wheel->pending_count, 1);\n}\n\n// Tick advancement with cascading\nvoid advance_tick(uint64_t new_tick) {\n    // Advance global wheel first\n    process_wheel_expirations(&global_wheel, new_tick);\n    \n    // Cascade to per-core wheels\n    for (int i = 0; i < MAX_CPUS; i++) {\n        process_wheel_expirations(&per_core_wheels[i], new_tick);\n    }\n}\n\n// Timer coalescing during expiration\nvoid process_wheel_expirations(TimerWheel *wheel, uint64_t tick) {\n    uint64_t bucket_idx = tick % WHEEL_SIZE;\n    Timer *timer = wheel->buckets[bucket_idx];\n    Timer *coalesced_group = NULL;\n    \n    // Group timers with similar expirations\n    while (timer) {\n        Timer *next = timer->next;\n        if (timer->expires <= tick) {\n            // Add to coalesced group\n            timer->next = coalesced_group;\n            coalesced_group = timer;\n        }\n        timer = next;\n    }\n    \n    // Execute coalesced group in single wakeup\n    if (coalesced_group) {\n        execute_coalesced_timers(coalesced_group);\n        wheel->buckets[bucket_idx] = NULL;\n        atomic_fetch_sub(&wheel->pending_count, \n                         count_timers(coalesced_group));\n    }\n}\n```\n\n## Validation Strategy\n- **Micro-benchmarks**: Measure timer enqueue/dequeue latency under varying loads\n- **Scalability testing**: Simulate 100k+ timers with mixed expiration patterns\n- **Cache analysis**: Profile per-core cache hit rates and memory access patterns\n- **Concurrency stress**: Test with high-frequency timer creation/cancellation on multiple cores\n- **Coalescing effectiveness**: Measure wakeup reduction vs timer accuracy trade-offs",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-18",
        "questionId": "q-29416",
        "question": "In a Linux-like kernel, design a scalable futex wait/wake path that minimizes contention and thundering herd under very high wakeups (e.g., IO completion bursts). Describe end-to-end flow from a user-space futex wait to kernel queueing, waiter struct management, cross-CPU batching, and wake path, including fairness, handling of spurious wakeups, and how to test correctness under load. Provide a minimal C-like sketch for enqueue and batch_wake?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Waiters are enqueued in a per-futex bucket using a compact lock-free queue. A per-CPU wake-batch aggregates sleepers and issues a single IPI to wake target CPUs where the predicate may hold. Wakes drain the queue in bat…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a circular buffer with head, tail, and count trackers. Use a mutex for synchronization and two wait queues for readers and writers. Reads block when count==0 (unless O_NONBLOCK is set, returning -EAGAIN); writes…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "One robust approach is to use a per-thread pending signal flag and a wake mechanism. On a signal, atomically set pending and wake the blocked read. The read loop checks for pending signals after each",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-page hotness tracking uses recency and frequency metrics to drive tiering decisions. Hot pages remain in DRAM while cold pages are demoted to PMEM. On first access (touch) of a page, migrate it to DRAM asynchronousl…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question tests understanding of kernel synchronization primitives, specifically futex internals, batching strategies, cross-CPU signaling, and fairness guarantees under high wakeup loads.\n\n## Key Concepts\n- **Per-futex bucket hashing**: Distributes contention by partitioning waiters across hash buckets.\n- **Lock-free queueing**: Enables enqueue operations without global locks, critical for scalability.\n- **Per-CPU wake batching**: Aggregates wakeups to amortize IPI overhead and reduce thundering herd.\n- **Predicate re-check**: Ensures correctness by validating the wait condition after waking before returning to userspace.",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-19",
        "questionId": "q-28896",
        "question": "Design a live memory migration protocol for a Linux-like hypervisor with pre-copy rounds. Each VM has a per-page dirty bitmap; in every round copy pages not yet dirtied by the guest, then briefly quiesce to copy remaining pages. Maintain per-page bitmaps via CPU-local page-table walks with barrier synchronization. Coordinate rounds with a global sequence and IPIs; finalize when bitmap empty. Provide minimal pseudocode for the dirty bitmap update and the pre-copy loop?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Interleaving: V0 = 0x00000000_00000000. A writes high 32 bits to 0x00000001; B reads; low 32 still 0, so B sees 0x00000001_00000000—a torn value. Minimal fixes: (1) atomic 64-bit stores on 64-bit CPUs",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Page Fault to Eviction Sequence **1. User-space Page Fault** - `do_user_addr_fault()` → `handle_mm_fault()` → `__handle_mm_fault()` - Page walk via `walk_page_range()` to find PTE - If PTE not present: `do_fault()` →…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an iterative pre-copy migration protocol using per-VM, per-page dirty bitmaps. In each round, copy all pages not yet marked dirty while the guest continues executing. Then briefly quiesce the VM to capture any…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-tenant memory pressure notifier: enforce per-container quotas (cgroups) with a NUMA hint. On pressure threshold, enqueue work into per-CPU batch queues and reclaim or migrate pages in bulk. Apply",
            "isCorrect": false
          }
        ],
        "explanation": "This question evaluates understanding of live migration correctness under production workloads. Key concepts include pre-copy algorithms for minimizing downtime, dirty page tracking via bitmaps, cross-CPU synchronization using memory barriers and IPIs, and managing the quiesce window during which the VM is paused. The pseudocode demonstrates bitmap update logic and the iterative pre-copy loop structure. Follow-up discussion can address failure recovery, handling memory pressure during migration, and calculating downtime bounds.",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-20",
        "questionId": "q-29862",
        "question": "Design a NUMA-aware, hierarchical software page cache layered over RAM and NVMe for a read-heavy dataset accessed by dozens of services. Each cache line is keyed by (dataset_id, page). On first touch, a per-node CAS serializes the page fetch. Implement cross-node eviction credits to prevent thrash, adaptive prefetch across NUMA distances, and fairness under contention. Provide an end-to-end path from fault to cache insertion, plus a minimal C-like sketch of eviction and page-in logic. How would you validate performance at scale?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-slot seqlock-like protocol. Writer: publish 0 to seq, write payload, then set seq to 1 (commit). Reader: read seq; if 0 or if seq changes before reading payload, retry; otherwise read payloa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "On a write, a page fault splits the 2MB THP into 512 4KB pages via split_huge_page, coordinated by khugepaged or on-demand. The kernel copies the write into the new 4KB page, updates the PTEs to point",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-level per-CPU timer wheel: near is 0–1 ms with 64 slots, far is 1–32 ms with 64 slots. Timer struct has deadline, callback, and waiter. Creation inserts into current CPU’s near wheel; on tick, adv",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a NUMA-aware, per-node cache with per-dataset index. On fault, CAS to claim a page, allocate locally, issue IO from NVMe, insert into per-node LRU, and update cross-node credits to throttle ev",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design scalable, NUMA-aware caches with cross-node synchronization, eviction policies, and prefetch heuristics under real workloads.\n\n## Key Concepts\n- NUMA locality, per-node memory pools, cross-node credits\n- CAS-based page claim, zero/fault paths, async IO\n- Adaptive prefetch/read-ahead and fairness under contention\n\n## Code Example\n```c\n// Pseudo-code sketch for eviction/page-in\n```\n\n## Follow-up Questions\n- How would you handle bursty traffic that exceeds per-node quotas?\n- What metrics would you track to detect thrashing and adjust credits dynamically?",
        "difficulty": "advanced"
      },
      {
        "id": "operating-systems-q-21",
        "questionId": "q-7968",
        "question": "In a preemptive kernel running on 4 CPUs, design and implement a mutex with priority inheritance to fix priority inversion when a high-priority task waits for a mutex held by an I/O-bound task. Provide a minimal C-like API: mutex_lock, mutex_unlock, and data structures to track base and current priorities. Sketch how to update priorities on lock/unlock, show a concrete interleaving where inheritance prevents starvation, and discuss multicore pitfalls and timer preemption?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Elevate the holder's priority to the maximum priority among all waiting tasks when acquiring the lock. Track both base and inflated priorities per task. On unlock, restore the holder to its base priority or the next hig…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "When a blocking read() encounters an empty pipe, the kernel blocks the current task by enqueuing it on the pipe's wait queue and putting it to sleep. When a writer appends data, the pipe buffer length becomes non-zero,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a two-stage ISR: a fast top-half updates tick and flags, queues heavy work to a per-CPU workqueue managed by a softirq/bottom-half thread. Disk IO ISR records completion and pushes an event. Botto",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Signals target a single thread; if that thread holds a mutex, the handler runs with the mutex still held, risking deadlock or data corruption unless the handler uses only async-signal-safe calls. Prac",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of real-time constraints, priority inheritance mechanisms, and multicore synchronization challenges. Requires concrete data structures and reasoning about nested locking, IRQ interactions, and race conditions.\n\n## Key Concepts\n- Priority Inheritance Protocol\n- Mutex design with priority tracking\n- Multicore scheduling interactions\n- Deadlock and priority inversion edge cases\n\n## Code Example\n\n```c\n// Minimal sketch of priority inheritance mutex\n// (conceptual C-like pseudocode for interview setting)\n\ntypedef struct task {\n    int base_prio;\n    int cur_prio;\n    // ... other fields\n} task_t;\n\ntypedef struct mutex {\n    task_t *holder;\n    // queue of waiting tasks, sorted by priority\n    struct list_head wait_queue;\n} mutex_t;\n\nvoid mutex_lock(mutex_t *m, task_t *t) {\n    if (m->holder == NULL) {\n        m->holder = t;\n        return;\n    }\n    \n    // Add to wait queue\n    enqueue_by_prio(&m->wait_queue, t);\n    \n    // Priority inheritance: boost holder if needed\n    if (t->cur_prio > m->holder->cur_prio) {\n        m->holder->cur_prio = t->cur_prio;\n        // Reschedule holder to run immediately\n        schedule_task(m->holder);\n    }\n    \n    // Block current task\n    block_task(t);\n}\n\nvoid mutex_unlock(mutex_t *m, task_t *t) {\n    if (m->holder != t) return;\n    \n    // Find highest priority waiter\n    task_t *next_waiter = peek_highest_prio(&m->wait_queue);\n    \n    if (next_waiter) {\n        // Transfer lock to next waiter\n        m->holder = next_waiter;\n        dequeue(&m->wait_queue, next_waiter);\n        unblock_task(next_waiter);\n    } else {\n        m->holder = NULL;\n    }\n    \n    // Restore holder's priority\n    t->cur_prio = t->base_prio;\n}\n```\n\n## Priority Update Logic\n- **Lock**: When task T waits for mutex held by H, set H.cur_prio = max(H.cur_prio, T.cur_prio)\n- **Unlock**: Restore holder to base_prio or highest waiting task's priority\n- **Chain Propagation**: Handle nested mutexes by recursively updating priority chains\n\n## Concrete Interleaving Example\n``nTime  High(T1)  Medium(T2)  Low(T3)  Mutex Action\n0     -         -           Run     T3 acquires M1\n1     -         Run         -       \n2     Run       Blocked     -       T1 tries M1, blocks\n3     -         -           Elevated T3 priority = max(T3.base, T1.cur) = HIGH\n4     -         -           Run     T3 releases M1, T1 acquires it\n```\nWithout inheritance: T1 starves while T3 performs I/O.\nWith inheritance: T3 gets temporary boost to complete critical section quickly.\n\n## Multicore Pitfalls\n- **Race Conditions**: Priority updates must be atomic across cores\n- **Cache Coherence**: Priority fields need proper memory barriers\n- **Timer Preemption**: Inherited priorities should survive timer interrupts\n- **Scheduler Migration**: Tasks may migrate between CPUs during priority boost\n- **Nested Locking**: Complex inheritance chains with multiple mutexes\n\n## Timer Preemption Considerations\n- Inherited priorities override timer tick rescheduling\n- Boost duration should be limited to critical section execution\n- Timer interrupts should not demote boosted tasks prematurely\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "operating-systems-q-22",
        "questionId": "q-8917",
        "question": "In a tiny Linux-like OS, after fork, pages are shared as read-only via Copy-On-Write. When a write occurs to a shared page, describe the exact page-fault path to create a private copy: how to allocate the new page, copy data, update the per-process page table, adjust per-page reference counts, and invalidate the TLB. How do you ensure race-freedom when two threads fault on the same page concurrently?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Fault flow uses a per-page lock: first thread allocates a frame, zero-fills it, updates the PTE to present, invalidates the TLB, and wakes waiters; then releases the lock. If a second thread faults on",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "On a miss, allocate a page, attach it to the requesting cgroup, and charge that cgroup. Track per-page flags (ref, dirty, exclusive) and per-cgroup watermarks. Eviction uses a hierarchical LRU with pe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement Copy-On-Write with per-page reference counting. After fork, pages are mapped read-only while sharing physical frames. When a write fault occurs, allocate a new physical page, copy the contents from the origina…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a per-thread wake_time and a global sleep-queue ordered by wake_time (min-heap). sleep(d): wake_time = now() + d; insert thread into queue; block. timer_tick(): while queue.min.wake_time <= now, p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical virtual memory implementation skills including COW semantics, page table management, reference counting, synchronization, and TLB consistency. It evaluates the ability to handle concurrent page faults without memory leaks or race conditions.\n\n## Key Concepts\n- Copy-On-Write (COW) optimization\n- Page table updates and TLB invalidation\n- Race condition handling and synchronization primitives\n- Concurrent memory management\n\n## Code Example\n```c\nvoid handle_write_fault(PTE *pte) {\n  acquire(pte->lock);\n  if (pte->refcount > 1) {\n    Page *newpage = alloc_page();\n    memcpy(newpage->addr, pte->frame->addr, PAGE_SIZE);\n    pte->frame = newpage;\n    pte->writable = 1;\n    pte->refcount--;\n    invalidate_tlb(pte->vaddr);\n  } else {\n    pte->writable = 1;\n  }\n  release(pte->lock);\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-23",
        "questionId": "q-23656",
        "question": "Design a minimal guard-page based stack overflow protection for a tiny threading library: allocate 8 MB per thread with a guard page just below the stack. Describe the fault path from touching the guard page to safe thread termination, and outline how to test overflow under concurrent thread creation and yield. Include a brief C-like skeleton for thread_init and fault_handler?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Allocate 8 MB per thread with a guard page below the stack. A touch on the guard triggers a page fault; runtime unwinds thread-local state, marks the thread dead, cleans resources, and yields to the s",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a per-CPU run queue architecture with a global I/O-priority bitmap. Each task tracks wait time and recent I/O latency; boost I/O-bound tasks when wait time exceeds threshold W, and demote CPU-bound tasks when lat…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-task I/O quota in task_struct, a per-NUMA IO queue, and a token-bucket throttler that refills tokens at the assigned bandwidth. On IO submit, atomically deduct tokens; if insufficient,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement preemptive RR with quantum=4 ticks. On each timer tick: decrement running.remaining_quantum; if zero then enqueue the running process at tail and pop next from the ready queue; perform a con",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nBeginners should reason about virtual memory protection, per-thread isolation, and fault handling without requiring full OS support. Guard pages are a simple, practical mechanism to detect overflows early.\n\n## Key Concepts\n\n- Virtual memory protection and per-thread stacks\n- Guard page placement and page faults\n- Graceful thread termination and resource cleanup\n- Basic testing for concurrency and fault paths\n\n## Code Example\n\n```c\n// Minimal skeleton\ntypedef struct { void *stack; void *guard; } Thread;\n\nvoid thread_init(Thread *t) {\n  t->stack = mmap(NULL, 8*1024*1024, PROT_READ|PROT_WRITE,\n                  MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);\n  t->guard = (char*)t->stack - PAGE_SIZE;\n  mprotect(t->guard, PAGE_SIZE, PROT_NONE);\n}\n\nvoid fault_handler(void *addr, Thread *t) {\n  if (addr >= t->guard && addr < (char*)t->guard + PAGE_SIZE) {\n    terminate_thread(t);\n  }\n}\n``` \n\n## Follow-up Questions\n\n- How would you extend to optionally grow the stack on demand?\n- What testing strategy ensures no collateral damage when multiple threads fault simultaneously?",
        "difficulty": "beginner"
      },
      {
        "id": "operating-systems-q-24",
        "questionId": "q-9742",
        "question": "In a tiny OS with a blocking read(fd, buf, n) on a file descriptor, design a safe interrupt mechanism via signals. Explain how a signal interrupts the read, how to avoid data corruption when a signal arrives mid-read, and provide a minimal C-like sketch for read_with_signal that returns -EINTR when interrupted or otherwise the bytes read, while ensuring thread safety and wake-up of the waiting read?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-container token bucket throttling with per-NUMA node refilling. IO requests check tokens; if enough, grant a ticket to the block layer; else enqueue in the container’s wait queue. Per-node timers",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a barrier with a mutex, a condition variable, a counter, and a generation id. barrier_init(b,n) sets count=0, needed=n, gen=0. barrier_wait(b) locks; if (++count==needed){ count=0; gen++; wake all",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "On fault, kernel checks if the address is in the thread's stack VMA and if growth is allowed. If yes, allocate a zero-filled page, map it into the user page table, and perform a TLB shootdown. The fau",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "One robust approach is to use a per-thread pending signal flag and a wake mechanism. On a signal, atomically set pending and wake the blocked read. The read loop checks for pending signals after each",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nSignals interrupting kernel I/O are a core but subtle area; beginners must reason about races and correctness.\n\n## Key Concepts\n- Blocking I/O, signals, race conditions, atomic flags, wakeups\n- EINTR semantics and partial reads\n- Memory visibility and CAS-based state updates\n\n## Code Example\n```javascript\n// Pseudo-C implementation sketch for read_with_signal\nint read_with_signal(int fd, void *buf, int n, int *interrupted){\n  int total = 0;\n  while (total < n){\n    int got = kernel_read(fd, (char*)buf + total, n - total);\n    if (got > 0){ total += got; continue; }\n    if (pending_signal(fd_thread_id)){ *interrupted = 1; return -1; }\n    // block until wake\n  }\n  return total;\n}\n```\n\n## Follow-up Questions\n- How would you extend this to handle multiple signals, and nesting of signals?\n- How would you test race conditions between signal delivery and I/O completion?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-02T05:28:05.666Z",
    "version": 1
  }
]