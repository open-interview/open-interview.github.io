[
  {
    "id": "test-docker-dca",
    "channelId": "docker-dca",
    "channelName": "Docker DCA",
    "title": "Docker DCA Knowledge Test",
    "description": "Test your understanding of Docker DCA with 25 questions.",
    "questions": [
      {
        "id": "docker-dca-q-0",
        "questionId": "q-30174",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement a per-tenant token-bucket rate limiter using Redis with keys derived from X-Tenant and a Lua script for atomic token fetch/consume; expose an admin endpoint to adjust caps without redeploy. Bootstrap initial quotas via a Docker Config and load into both DCs. Ensure cross-DC routing with zero-downtime quota updates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Vault to issue per-tenant signing keys and rotate via leases. Implement a log- producer sidecar that signs each log line with the tenant’s key and publishes to a tenant-specific Kafka topic.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant token-bucket rate limiter using Redis with keys derived from X-Tenant. Use a Lua script for atomic token fetch/consume; expose an admin endpoint to adjust caps without redeploy.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Outline a three-node Swarm deployment using TLS secrets, a config for endpoints, an Nginx proxy, an API service, and a lightweight compliance sidecar that UDPs audit data to a collector with no measur",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument requests with OpenTelemetry: edge proxy, service, and sidecar emit OTLP traces to a central Collector. Use W3C TraceContext; propagate tenant_id via trace attributes. Enforce mTLS with SPIF",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world QoS control across DCs, using per-tenant keys, Redis Lua scripting for atomicity, and zero-downtime quota updates via Docker Config.\n\n## Key Concepts\n- Token bucket rate limiting per tenant\n- Redis Lua scripting for atomic operations\n- Docker Config bootstrap for quotas\n- Traefik TLS termination and multi-DC routing\n- Live quota adjustments without redeploys\n\n## Code Example\n```lua\n-- Redis Lua script (simplified) to atomically fetch/consume a token\nlocal key = KEYS[1]\nlocal rate = tonumber(ARGV[1])\nlocal burst = tonumber(ARGV[2])\nlocal now = tonumber(ARGV[3])\nlocal tokens = tonumber(redis.call('GET', key) or burst)\nif tokens < 1 then\n  return -1\nend\nredis.call('DECR', key)\nredis.call('EXPIREAT', key, now + rate)\nreturn tokens - 1\n```\n\n## Follow-up Questions\n- How would you handle clock skew across DCs?\n- What observability would you add for per-tenant quotas and violations?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-1",
        "questionId": "q-1443",
        "question": "In a 3-node Swarm across DC-A and DC-B, deploy a stateless API via overlay api-net with DC-affinity (2 replicas in DC-A and 1 in DC-B). Outline the exact CLI steps to init/join, create the overlay, and deploy two services with update_config (order: start-first, parallelism: 1). Describe a canary upgrade process that gradually updates replicas across DCs and validates with health checks. Include a minimal docker-compose snippet showing the two services, the overlay network, and update_config?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Init Swarm on DC-A; join DC-B; create overlay api-net; deploy two services: api-dcA with 2 replicas constrained to DC-A and api-dcB with 1 replica constrained to DC-B, both on api-net and update_confi",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Set up two app variants (blue/green) behind Traefik with a single dynamic config indicating active_variant. Default blue; a small sidecar reads active_variant from Docker Config and updates Traefik's",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure a small policy layer in the API to filter responses by tenant. Mount Docker Config tenant-schemas.json at /config/tenant-schemas.json, parse into map[string][]string, and on each request rea",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Solution Overview Per-tenant routing means each tenant gets routed to either v1 or v2 API based on the X-Tenant header. Traefik reads the tenant-canary config to decide which backend to use. ### Architecture - Traefi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-DC orchestration, DC-aware placement, and canary upgrades using Docker Swarm update_config.\n\n## Key Concepts\n- Overlay networks spanning DCs\n- Placement constraints and spread/preference for DC distribution\n- update_config for staged upgrades\n- Canary rollout and health-check driven promotion\n\n## Code Example\n```javascript\nversion: \"3.8\"\nservices:\n  api-dcA:\n    image: myapi:latest\n    deploy:\n      replicas: 2\n      update_config:\n        order: start-first\n        parallelism: 1\n      placement:\n        constraints:\n          - node.labels.dc == DC-A\n      networks:\n        - api-net\n  api-dcB:\n    image: myapi:latest\n    deploy:\n      replicas: 1\n      update_config:\n        order: start-first\n        parallelism: 1\n      placement:\n        constraints:\n          - node.labels.dc == DC-B\n      networks:\n        - api-net\nnetworks:\n  api-net:\n```\n\n## Follow-up Questions\n- How would you monitor canary success across DCs and automate rollback if latency spikes? \n- How would you adapt this for a rolling upgrade with multiple versions concurrently?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-2",
        "questionId": "q-3119",
        "question": "In a two-node docker-dca Swarm spanning two data centers, deploy a TLS-enabled stateless API behind an Nginx edge proxy. Use Docker secrets for TLS certs and a Docker config for API endpoints. Attach both services to a single overlay network. Add an Envoy sidecar to the API service to perform region-aware traffic-splitting driven by a global feature flag stored in Consul KV at /features/new-api. Implement a progressive canary: 20% traffic to v2 until the flag is 1, with health checks and automatic rollback on failure. Provide exact commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Traefik as the TLS edge; per-tenant flags live in Docker Config; a lightweight sidecar serves a /flags endpoint and writes updates to an in‑memory cache shared with the API. The API reads from the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a central tenant-version map in Consul KV (or Redis), a per-tenant sidecar that reads X-Tenant and routes to blue or green through Traefik, enabling on-the-fly tenant pinning without redeploys",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Envoy as a per-service sidecar to implement traffic-splitting, controlled by Consul KV flag /features/new-api. Nginx edge handles TLS via Docker secrets; services join one overlay network. St",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a Python controller in Swarm that reads p95 latency from a Prometheus-compatible endpoint and gradually shifts Nginx upstream weights from 10/90 to 0/100 in 15s steps. Use Docker secrets for",
            "isCorrect": false
          }
        ],
        "explanation": "Why this is asked\n- Tests practical canary via a sidecar proxy and a feature flag, without a full service mesh\n- Validates multi-site deployment, TLS handling, and dynamic traffic control\n\nKey Concepts\n- Envoy as a sidecar for per-service traffic shaping\n- Consul KV feature flags for cross-service rollout control\n- Docker secrets/config for TLS and API metadata\n- Canary progression and rollback based on health/latency metrics\n\nCode Example\n```yaml\nversion: '3.8'\nservices:\n  edge-nginx:\n    image: nginx:alpine\n    secrets:\n      - tls_cert\n      - tls_key\n    networks:\n      - overlay\n  api:\n    image: myapi:latest\n    depends_on:\n      - envoy\n    networks:\n      - overlay\n  envoy:\n    image: envoyproxy/envoy:v1.25.0\n    volumes:\n      - ./envoy.yaml:/etc/envoy/envoy.yaml:ro\n    networks:\n      - overlay\n\ndevices: []\nsecrets:\n  tls_cert:\n    file: ./certs/server.crt\n  tls_key:\n    file: ./certs/server.key\nnetworks:\n  overlay:\n    driver: overlay\n```\n\nFollow-up Questions\n- How would you monitor and alert on canary drift vs. full traffic?\n- How would you handle certificate rotation without downtime in Swarm?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-3",
        "questionId": "q-24983",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant rate limiting using a Redis-backed token bucket, with a per-tenant namespace (tenant:<X-Tenant>), and synchronize counters across DCs via Redis Pub/Sub. Add a sidecar middleware that enforces the limit (reads X-Tenant, returns 429 with Retry-After when exhausted). Bootstrap config via a Docker Config named rate-config and provide a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sign events in a dedicated audit sidecar using Vault Transit per-tenant keys, rotate automatically, forward to a TLS-enabled SIEM via a lightweight broker, and store immutable digests in a WORM bucket",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument the API with OpenTelemetry, using a per-tenant sampler read from a Docker Config (one per-tenant sampling rate). Spans go to a central OTLP collector in each DC. Traefik handles TLS and ten",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a tenant-scoped feature-flag pipeline at the edge: fetch from central config, propagate flags with a CRDT gossip mesh across DCs, apply per-tenant OPA rules, and tie canaries to per-tenant togg",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy a Redis-backed per-tenant token bucket with keys in the format tenant:<X-Tenant>:bucket. Configure token refill at a defined interval, and implement a sidecar middleware that validates the X-Tenant header, decrem…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical multi-DC rate limiting with per-tenant isolation using Redis as shared state and a sidecar pattern. It validates understanding of token bucket algorithms, cross-DC consistency via Redis Pub/Sub, runtime configuration through Docker Configs, and Traefik TLS routing.\n\n## Key Concepts\n- Token bucket per tenant stored in Redis with namespace tenant:<id>:bucket\n- Sidecar middleware for enforcement, handling the X-Tenant header\n- Redis Pub/Sub for cross-DC counter synchronization\n- Docker Config for per-tenant rate limit parameters\n- Traefik as edge proxy with TLS termination\n\n## Code Example\n```python\n# Pseudo: token bucket check in sidecar\nkey = f\"tenant:{tenant_id}:bucket\"\ncurrent = redis.incr(key)\nif current > limit:\n    redis.decr(key)\n    return 429, {\"Retry-After\": retry_after}\n# Allow request through\n```\n\n## docker-compose.yml Skeleton\n```yaml\nservices:\n  traefik:\n    image: traefik:v3\n    volumes:\n      - /var/run/docker.sock:/var/run/docker.sock\n    ports:\n      - \"443:443\"\n    command:\n      - --providers.docker\n      - --entrypoints.websecure.address=:443\n      - --tls\n\n  api:\n    build: ./api\n    labels:\n      - \"traefik.enable=true\"\n      - \"traefik.http.routers.api.tls=true\"\n      - \"traefik.http.routers.api.rule=PathPrefix(/api)\"\n\n  rate-limiter:\n    image: rate-limiter-sidecar\n    depends_on:\n      - redis\n\n  redis:\n    image: redis:7\n    volumes:\n      - redis-data:/data\n\nconfigs:\n  rate-config:\n    file: ./rate-config.json\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-4",
        "questionId": "q-2346",
        "question": "In a 3-node docker-dca cluster spanning two data centers, deploy a TLS-secured stateless API behind an Nginx proxy. Use Docker secrets for TLS certs and a Docker config for API endpoints. Add a shadow sidecar that mirrors 5% of production traffic to a canary version. Implement a canary rollout by adjusting weights in 10% increments using update_config, with health checks and automatic rollback on failure. Provide exact commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Solution Overview Per-tenant routing means each tenant gets routed to either v1 or v2 API based on the X-Tenant header. Traefik reads the tenant-canary config to decide which backend to use. ### Architecture - Traefi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement dynamic traffic shadow: deploy a main API and a canary, with a shadow Envoy sidecar mirroring ~5% of traffic to the canary; TLS via Docker secrets; Nginx routes 95% to main and 5% to shadow;",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Bootstrap a lightweight OpenSSL CA, issue certificates for Traefik and the API, and load them as Docker Secrets. Configure Traefik to enforce mTLS with clientAuth and TLS settings, route via TLS to the API, add a canary…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a zero-downtime SPIFFE SVID rotation using Vault with short TTLs; deploy per-tenant Envoy sidecars that enforce Consul KV-stored data residency policies and route to tenant-scoped overlay networks; configure t…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to orchestrate multi-DC deployments, secret-driven TLS, and traffic shaping with live canary upgrades. It also probes rollback discipline and the interaction between edge proxies, sidecars, and the orchestrator.\n\n## Key Concepts\n- Docker secrets and configs for TLS and endpoints\n- Nginx/Envoy traffic routing and shadow/canary pattern\n- Canary rollout via update_config with health checks\n- Multi-DC networking and rollback strategies\n\n## Code Example\n```yaml\nversion: '3.8'\nservices:\n  nginx:\n    image: nginx:stable\n    secrets:\n      - tls_cert\n    configs:\n      - source: api_endpoints\n        target: /etc/api/endpoints.conf\n  api-main:\n    image: myapi:latest\n    secrets:\n      - tls_cert\n  api-canary:\n    image: myapi:canary\n    secrets:\n      - tls_cert\n    deploy:\n      update_config:\n        parallelism: 1\n        order: start-first\n        failure_action: rollback\nsecrets:\n  tls_cert:\n    file: ./certs/tls_cert.pem\nconfigs:\n  api_endpoints:\n    file: ./configs/endpoints.conf\n```\n\n## Follow-up Questions\n- How would you monitor canary health and automate rollback decisions?\n- How would you secure inter-service communication and ensure TLS mutual authentication across DCs?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-5",
        "questionId": "q-27733",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement tenant-aware canary deployments: an orchestrator reads per-tenant rollout plans from Consul KV and Docker Config, applies Traefik weighted routing to progressively shift traffic for each tenant, and uses Redis pub/sub signals to trigger automatic rollback on failures; provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe tenant-aware canary deployment in two DCs: an orchestrator reads per-tenant rollout plans from Consul KV and Docker Config, applies Traefik weighted routes to gradually shift traffic per tena",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Deploy a Redis-backed routing map storing tenant_id → api_version mappings. Implement a lightweight sidecar that monitors Redis for version changes and pushes dynamic Traefik configuration updates through the Docker API…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a zero-trust path: in a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy. Use Vault to issue SPIFFE SVIDs, and a per-app sidecar that",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To solve this, terminate TLS at the edge, enable mTLS inside the cluster via Vault-issued SPIFFE SVIDs, and run a cert-verifying sidecar next to each service. The OPA policy, stored in a Docker Config",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests real-world, cross-DC tenant canary design with traffic control and rollback signals via established components: Traefik routing, Consul KV, Docker Config, and Redis Pub/Sub.\n\n## Key Concepts\n- Tenant-aware canary routing via Traefik\n- Per-tenant rollout plans in Consul KV and Docker Config\n- Robust rollback on failure signals via Redis Pub/Sub\n- Idempotent promotions and cross-DC safety\n- Observability per tenant\n\n## Code Example\n```yaml\n# minimal docker-compose skeleton\nversion: '3.8'\nservices:\n  traefik:\n    image: traefik:v2.x\n    ports:\n      - '443:443'\n  api:\n    image: tenant/api:latest\n    labels:\n      - 'traefik.enable=true'\n      - 'traefik.http.routers.api.rule=Host(`api.example.com`)' \n  canary-orchestrator:\n    image: canary-orch:latest\n    environment:\n      - CONSUL_ADDRESS=consul:8500\n      - REDIS_ADDR=redis:6379\n```\n\n## Follow-up Questions\n- How ensure per-tenant isolation during canary?\n- How to design idempotent canary promotions and safe rollback?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-6",
        "questionId": "q-6133",
        "question": "In a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy. Implement a lightweight per-key rate limiter as a sidecar to enforce 60 requests/min per API key and a 15% canary to route a subset of keys to a new version; provide exact bootstrap commands and a docker-compose skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instrument the API to emit structured JSON logs including tenant_id extracted from request headers like X-Tenant. Deploy a lightweight sidecar log collector that tails API logs, redacts PII data, writes per-tenant log f…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an eBPF anomaly-detection sidecar that monitors per-tenant CPU, memory, and I/O, exporting metrics to an OPA-based policy service. On anomaly, quarantine the tenant by redirecting to a canar",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Prototype a per-key rate limiter as a Docker sidecar in Python. The sidecar reads the X-API-Key header, tracks windowed counts in a tiny JSON file mounted as a volume, and returns 429 when the count e",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Leverage Traefik's WeightedRoundRobin: two backends app-v1 and app-v2; route by X-Tenant header and per-tenant weights from Docker Config tenant-canary.yaml. Example: {tenantA:90, tenantB:10}. Bootstr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical setup: TLS Terminations via Nginx, a lightweight sidecar pattern, and a beginner-friendly canary workflow without external policy engines.\n\n## Key Concepts\n\n- Docker Compose wiring for multi-node, multi-DC setups\n- TLS termination at Nginx and service-to-edge security\n- Sidecar pattern to implement per-key rate limiting\n- Canary rollout basics with URL/param-based routing\n\n## Code Example\n\n```python\n# rate_limiter.py\nimport time, json\nSTATE_PATH = \"/shared/state.json\"\n\ndef load():\n    try:\n        with open(STATE_PATH) as f: return json.load(f)\n    except Exception:\n        return {}\n\ndef save(state):\n    with open(STATE_PATH, \"w\") as f: json.dump(state, f)\n\n\ndef allow(key, window=60, limit=60, state=None):\n    if state is None: state = load()\n    now = int(time.time())\n    bucket = state.get(key, [])\n    bucket = [t for t in bucket if now - t < window]\n    if len(bucket) >= limit: return False\n    bucket.append(now)\n    state[key] = bucket\n    save(state)\n    return True\n```\n\n## Follow-up Questions\n\n- How would clock drift affect accuracy and how to mitigate it?\n- How would you scale the limiter across many nodes?\n- What tests would you add to validate 15% canary behavior?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-7",
        "questionId": "q-18047",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement a tenant-based access allowlist stored in a Docker Secret named tenant-allow that maps X-Tenant values to true/false, and enforce per-tenant route access (eg /data) only when allowed; return 403 otherwise. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would sign images with Docker Content Trust, add a lightweight 'sigver' sidecar that validates signatures against Notary on container startup and before traffic; issue SVIDs with Vault for mTLS; fet",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Store a Docker Secret named tenant-allow mounted into the API container, parse at startup, and cache in memory. API reads X-Tenant, validates against the secret, and serves /data only if allowed; othe",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Configure per-tenant observability across two DCs: TLS API behind Traefik, OpenTelemetry with a Jaeger backend as a central collector; per-tenant sampling rates controlled via Docker Config (tenant-sa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant token-bucket rate limiter using Redis with keys derived from X-Tenant. Use a Lua script for atomic token fetch/consume; expose an admin endpoint to adjust caps without redeploy.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of Docker Secrets, multi-DC deployments, and simple per-tenant access control.\n\n## Key Concepts\n- Docker Secrets and secret mounting in services\n- Traefik TLS termination and service wiring\n- Runtime tenant lookup and 403 handling\n- Secret rotation and service reloads\n\n## Code Example\n```javascript\n// Pseudo: startup path reads secret/tenant-allow.json and caches in memory\nconst allow = JSON.parse(fs.readFileSync('/run/secrets/tenant-allow', 'utf8'));\nconst tenant = req.header('X-Tenant');\nif (!allow[tenant]) return res.status(403).send('Forbidden');\n```\n\n## Follow-up Questions\n- How would you implement secret rotation with zero-downtime reloads?\n- How would you test correctness across DCs?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-8",
        "questionId": "q-26305",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant envelope encryption using Vault Transit: each tenant has a Transit key named tenant-<id> to encrypt data keys that guard tenant data at rest. Rotate keys on a 24h cadence with zero downtime, evicting cached tokens via a rewrap strategy; ensure X-Tenant selects tenant key. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a tenant-scoped feature-flag pipeline at the edge: fetch from central config, propagate flags with a CRDT gossip mesh across DCs, apply per-tenant OPA rules, and tie canaries to per-tenant togg",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant envelope encryption using Vault Transit, where each tenant possesses a Transit key named tenant-<id>. This key wraps a data key that encrypts tenant data at rest. A sidecar container fetches the act…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Create two service versions (stable and canary) behind Traefik TLS. Add a Docker Config canary-tenant-map that maps X-Tenant values to 'stable' or 'canary'. Route with Traefik rule on Headers 'X-Tenan",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Route per-tenant traffic using a canary weight fetched from Consul KV, mapped via X-Tenant header in Traefik. Implement a shadow destination mirroring a percentage of requests to the canary API while",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of cross-datacenter security architecture, key management lifecycle, and zero-downtime key rotation strategies in distributed systems.\n\n## Key Concepts\n- Per-tenant envelope encryption using Vault Transit\n- Zero-downtime key rotation via rewrap strategy\n- mTLS between services and Vault for secure communication\n- Docker Config secrets for tenant key provisioning\n- Traefik for TLS termination and tenant routing\n\n## Bootstrap Commands\n```bash\n# Enable Vault Transit\nvault secrets enable transit\n\n# Create per-tenant keys\nfor id in tenant-a tenant-b; do\n  vault write transit/keys/$id rotation_period=\"24h\"\ndone\n\n# Configure key rotation policy\nvault write transit/keys/tenant-a/config rotation_period=\"24h\"\n\n# Create rewrap policy\nvault policy write rewrap-policy - <<EOF\npath \"transit/rewrap/*\" { capabilities = [\"update\"] }\npath \"transit/keys/*\" { capabilities = [\"read\"] }\nEOF\n\n# Generate Vault token for rewrap\nvault token create -policy=rewrap-policy\n```\n\n## Minimal docker-compose.yml Skeleton\n```yaml\nversion: '3.8'\nservices:\n  traefik:\n    image: traefik:v2.10\n    ports:\n      - \"443:443\"\n    volumes:\n      - /var/run/docker.sock:/var/run/docker.sock:ro\n    command:\n      - --providers.docker\n      - --entrypoints.websecure.address=:443\n      - --tls.certResolver=letsencrypt\n\n  vault:\n    image: hashicorp/vault:1.15\n    environment:\n      VAULT_ADDR: http://vault:8200\n    cap_add:\n      - IPC_LOCK\n    volumes:\n      - vault-data:/vault/file\n\n  api:\n    image: your-api-image\n    environment:\n      VAULT_ADDR: http://vault:8200\n      VAULT_TOKEN_FILE: /run/secrets/vault_token\n    secrets:\n      - vault_token\n    labels:\n      - \"traefik.enable=true\"\n      - \"traefik.http.routers.api.rule=PathPrefix(`/api`)\"\n      - \"traefik.http.routers.api.tls=true\"\n\n  key-rewrapper:\n    image: your-rewrap-image\n    environment:\n      VAULT_ADDR: http://vault:8200\n    depends_on:\n      - vault\n    restart: unless-stopped\n    cron: \"0 * * * *\"  # Hourly rewrap check\n\nvolumes:\n  vault-data:\n\nsecrets:\n  vault_token:\n    file: ./vault-token.txt\n```\n\n## X-Tenant Header Flow\n1. Client sends request with X-Tenant: tenant-a header\n2. Traefik routes to appropriate API instance\n3. API extracts tenant ID and retrieves tenant-a Transit key\n4. API encrypts/decrypts data using the tenant-specific data key\n5. Cached keys are evicted and refreshed on rotation via rewrap strategy",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-9",
        "questionId": "q-18250",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant access control using an OPA policy engine as a sidecar that reads policies from a Docker Config named tenant-policies and enforces access to the /admin and /data endpoints. Support runtime policy updates with zero downtime and per-tenant policy versioning; provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Docker Config tenant-canaries to mark canary tenants and Traefik routers to direct X-Tenant==canary to api-v2 while others go to api-v1. Implement per-tenant health checks and a lightweight moni",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an OPA sidecar per API replica enforcing per-tenant rules loaded from Docker Config tenant-policies. Traefik forwards requests with X-Tenant; OPA evaluates against the tenant’s policy bundle and r",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Enable Docker Content Trust and cosign signing on all nodes (DOCKER_CONTENT_TRUST=1; cosign sign). Deploy api-v1 and a canary api-v2-canary behind Traefik with a 10% canary route. Use update_config {",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Solution Overview Per-tenant routing means each tenant gets routed to either v1 or v2 API based on the X-Tenant header. Traefik reads the tenant-canary config to decide which backend to use. ### Architecture - Traefi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design policy-as-code, cross-DC deployment, dynamic updates, and zero-downtime rotation, using OPA, Docker Configs, Traefik, and TLS.\n\n## Key Concepts\n\n- OPA as a sidecar for per-tenant access control\n- Docker Configs as policy bundles with versioning\n- Zero-downtime policy updates and live reloads\n- Traefik integration to surface tenant context\n- Multi-DC consistency and rollback considerations\n\n## Code Example\n\n```yaml\n# docker-compose.yml skeleton\nversion: '3.8'\nservices:\n  api:\n    image: myapi:latest\n    ports:\n      - \"443:443\"\n    environment:\n      - OPA_ADDRESS=http://opa:8181/v1/data/\n    depends_on:\n      - opa\n  opa:\n    image: openpolicyagent/opa:latest\n    volumes:\n      - tenant-policies:/policies\n    command: [\"run\", \"--server\", \"--config-file\", \"/policies/opa-config.yaml\"]\nvolumes:\n  tenant-policies:\n```\n\n## Follow-up Questions\n\n- How would you orchestrate policy versioning and migrations across DCs without breaking in-flight requests?\n- What monitoring would you add to detect policy reload issues and enforce SLA guarantees?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-10",
        "questionId": "q-17611",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant A/B routing: tenants are mapped in a Docker Config tenant-backends.json to either backend-v1 or backend-v2. Traefik should dynamically apply routing without container redeploys as the config updates. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant mTLS by issuing tenant certs from Vault, rotate automatically with short-lived leases, and revoke compromised certs via per-tenant CRLs loaded by sidecars. Traefik enforces client",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Bootstrap Vault for SPIFFE SVID issuance, configure Nginx to terminate TLS using SVIDs, attach a verifier sidecar that validates SVIDs, and a policy sidecar that queries an external OPA-like service p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe a runtime workload attestation flow: each service fetches a short-lived attestation token from a central Attestation Service tied to its SPIFFE ID; a separate verification sidecar validates t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bootstrap two backends (backend-v1, backend-v2) and a Traefik edge with TLS. Maintain a Docker Config tenant-backends.json that maps each tenant_id to v1 or v2. A lightweight bootstrap script generate",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a resilient, multi-DC TLS deployment with dynamic per-tenant routing and zero-downtime config updates.\n\n## Key Concepts\n- Traefik FileProvider dynamic config\n- Docker Configs for runtime data\n- Dynamic per-tenant routing without redeploys\n- Simple A/B routing using tenant-to-version mapping\n\n## Code Example\n```bash\n#!/bin/bash\n# bootstrap per-tenant routers from tenant-backends.json\n# (pseudo-implementation to illustrate flow)\nEOF\n```\n\n## Follow-up Questions\n- How would you validate per-tenant routing during rollout and roll back safely?\n- What observability would you add to verify tenant traffic splits and detect misrouting?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-11",
        "questionId": "q-4507",
        "question": "In a two-node docker-dca Swarm, deploy a TLS-enabled API behind an Nginx edge proxy and enforce mutual TLS (mTLS) with client certificates issued by a private CA. Store server and CA certs as Docker secrets, configure Nginx to require and validate client certs, and ensure the API receives the client identity via a header. Provide exact docker-compose.yml skeleton and the certificate generation steps, plus test commands?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a tenant-scoped audit path: a sidecar publishes per-tenant events to Kafka with idempotency keys to guard against duplicates. Ensure at-least-once delivery, dedupe in the consumer for exact-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deploy a TLS-enabled API behind Traefik in a Docker DCA cluster across two data centers. Implement per-tenant rate limiting using a Redis-backed token bucket: a Redis counter per tenant with a 60-second window. Tenant I…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy two backend services (api-v1 and api-v2) behind Traefik as the TLS edge proxy. By default, all traffic routes to api-v1. A Docker Config named tenant-canary contains a list of tenants permitted to receive api-v2…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Generate a private CA, issue server and client certificates, and store them as Docker secrets. Configure Nginx with `ssl_verify_client on; ssl_client_certificate /certs/ca.pem; proxy_set_header X-Client-Subject $ssl_cli…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical mTLS implementation, Docker secrets management, and edge proxy configuration in a Swarm environment. Evaluates operational skills for secure service-to-service communication.\n\n## Key Concepts\n- Docker secrets for TLS certificate management\n- Nginx mutual TLS verification and client certificate validation\n- Client identity propagation to backend services via HTTP headers\n\n## Code Example\n```yaml\n# skeleton docker-compose.yml\nversion: '3.9'\nservices:\n  edge:\n    image: nginx:alpine\n    ports:\n      - '443:443'\n    secrets:\n      - source: nginx_server_cert\n        target: server.crt\n      - source: nginx_server_key\n        target: server.key\n      - source: nginx_ca_cert\n        target: ca.pem\n    configs:\n      - source: nginx_config\n        target: /etc/nginx/nginx.conf\n  api:\n    image: your-api:latest\n    deploy:\n      replicas: 2\nsecrets:\n  nginx_server_cert:\n    external: true\n  nginx_server_key:\n    external: true\n  nginx_ca_cert:\n    external: true\nconfigs:\n  nginx_config:\n    external: true\n```\n\n## Certificate Generation\n```bash\n# Create private CA\nopenssl genrsa -out ca.key 4096\nopenssl req -new -x509 -days 365 -key ca.key -out ca.pem\n\n# Generate server certificate\nopenssl genrsa -out server.key 2048\nopenssl req -new -key server.key -out server.csr\nopenssl x509 -req -in server.csr -CA ca.pem -CAkey ca.key -CAcreateserial -out server.crt -days 365\n\n# Generate client certificate\nopenssl genrsa -out client.key 2048\nopenssl req -new -key client.key -out client.csr\nopenssl x509 -req -in client.csr -CA ca.pem -CAkey ca.key -CAcreateserial -out client.crt -days 365\n\n# Create Docker secrets\ndocker secret create nginx_server_cert server.crt\ndocker secret create nginx_server_key server.key\ndocker secret create nginx_ca_cert ca.pem\n```\n\n## Testing Commands\n```bash\n# Test with client certificate\ncurl --cert client.crt --key client.key --cacert ca.pem https://your-service:443/api\n\n# Test without client certificate (should fail)\ncurl --cacert ca.pem https://your-service:443/api\n\n# Verify client identity header\ncurl --cert client.crt --key client.key --cacert ca.pem https://your-service:443/api -v | grep X-Client-Subject\n```",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-12",
        "questionId": "q-23129",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant TLS cert provisioning via Vault PKI, with Traefik performing SNI-based routing by X-Tenant; certificates must reload without downtime. Maintain a Consul KV revocation list; revocation triggers an immediate Traefik reload and client disconnects. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant TLS certs issued by Vault PKI, Traefik configured for SNI routing by X-Tenant; certificates mounted per tenant and reloaded dynamically via Traefik's file provider. Maintain a Consul KV rev",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create a tiny Python sidecar 'audit-shipper.py' that tails /var/log/api/access.jsonl (produced by the API) in real-time and writes per-tenant JSON entries to /audit/logs/audit.jsonl. Each line include",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Store a Docker Secret named tenant-allow mounted into the API container, parse at startup, and cache in memory. API reads X-Tenant, validates against the secret, and serves /data only if allowed; othe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument the API with OpenTelemetry, using a per-tenant sampler read from a Docker Config (one per-tenant sampling rate). Spans go to a central OTLP collector in each DC. Traefik handles TLS and ten",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests knowledge of dynamic TLS provisioning, zero-downtime reloads, and cross-DC coordination using Vault, Traefik, and Consul in docker-dca.\n\n## Key Concepts\n\n- Vault PKI per-tenant certificates\n- Traefik SNI routing and dynamic reloads\n- Consul KV revocation mechanisms\n- Zero-downtime config reloads across DCs\n- docker-compose-based skeletons for multi-service trust\n\n## Code Example\n\n```bash\n# Issue a new per-tenant certificate\nvault write pki/issue/tenant-123 common_name=\"tenant-123.example.com\" ttl=\"24h\"\n```\n\n## Follow-up Questions\n\n- How would you handle issuance failures and retries?\n- How would you test revocation propagation across DCs?\n- What are the security implications of mounting tenant certs in containers?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-13",
        "questionId": "q-12534",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster. Implement per-tenant rate limiting using Nginx limit_req, with a small JSON config loaded into the edge to map Tenant to a rate; identify tenant via the X-Tenant header and return 429 when over limit. Provide a minimal docker-compose.yml skeleton with api, nginx, and a config loader; bootstrap two tenants and test with curl -H 'X-Tenant: A' and -H 'X-Tenant: B'?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy a TLS API behind Nginx in docker-dca and add per-tenant rate limiting using Nginx limit_req with a small JSON file mounted into the edge to map Tenant to rate. Tenant is identified by X-Tenant;",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage Traefik's dynamic config via a file provider loaded from the shadow-tenants Docker Config. Create a router for the API with a mirror to the shadow service, conditioned by a header check X-Ten",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a rate-limiter sidecar that sits in front of the API, using a Redis cluster shared across both DCs. On each request, extract X-Api-Key, run a Lua script to atomically INCR a 60s bucket key (api",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Configure Traefik with a dynamic config loaded from ab-split that maps each tenant to weights for api-v1 and api-v2. Deploy api-v1 and api-v2 behind Traefik in both DCs. TLS terminates at the edge; Tr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests edge proxy TLS termination, simple per-tenant rate limiting, and Docker Compose orchestration across DCs. It stays beginner-friendly while touching operational concerns.\n\n## Key Concepts\n- TLS termination at the edge\n- Nginx limit_req for per-tenant rates\n- Tenant discovery via HTTP header\n- Docker Compose skeleton for cross-DC deployments\n\n## Code Example\n```nginx\n# nginx.conf\nhttp {\n  map $http_x_tenant $limit {\n    default 1r/s;\n    'tenantA' 5r/s;\n    'tenantB' 2r/s;\n  }\n  limit_req_zone $binary_remote_addr zone=per_tenant:10m rate=$limit;\n  server {\n    listen 443 ssl;\n    location /api { limit_req zone=per_tenant burst=10; proxy_pass http://api:8080; }\n  }\n}\n```\n\n```yaml\n# docker-compose.yml skeleton\nversion: 3.8\nservices:\n  nginx:\n    image: nginx:latest\n    ports: [\"443:443\"]\n    volumes:\n      - ./nginx.conf:/etc/nginx/nginx.conf:ro\n      - ./certs:/etc/ssl/certs:ro\n  api:\n    image: your-api\n  loader:\n    image: alpine:latest\n    command: sh -c 'cp /config/tenant_limits.json /nginx/tenant_limits.json'\n    volumes:\n      - ./config/tenant_limits.json:/config/tenant_limits.json:ro\n```\n\n## Follow-up Questions\n- How would you validate 429 responses per tenant in tests?\n- How would you handle a tenant with burst needs without impacting others?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-14",
        "questionId": "q-20668",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant traffic mirroring (shadow testing) to a feature-branch backend without affecting production responses; tenants identified via X-Tenant; shadow URL per-tenant provided via a Docker Config tenant-shadow that maps tenant IDs to shadow service coordinates. Bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: deploy a TLS-enabled API behind Envoy edge across DCs with mTLS; add an eBPF-based limiter sidecar that enforces per-tenant quotas stored in Vault and rotated via SPIFFE SVIDs; support HTTP",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deploy a TLS-enabled API behind Traefik in a Docker DCA cluster across two data centers. Implement per-tenant rate limiting using a Redis-backed token bucket: a Redis counter per tenant with a 60-second window. Tenant I…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route per-tenant production traffic through Traefik TLS edge and mirror to a shadow backend for canary testing. Identify tenants with X-Tenant, fetch shadow endpoints from Docker Config tenant-shadow,",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Describe the end-to-end setup: Vault-issued SPIFFE SVIDs for all tenants, edge verifier sidecar validating SVIDs, WASM policy engine hot-loading tenant modules from a broker, blue/green with 25% canar",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nDemonstrates configuring a TLS edge, per-tenant routing, and production-safe shadow testing using Traefik's mirror feature with Docker Config-based targets; validates behavior across two data centers.\n\n## Key Concepts\n- Traefik TLS edge termination and per-tenant routing\n- traffic mirroring to a shadow backend for canary testing\n- Docker Config for tenant-shadow mappings\n- multi-DC deployment considerations and zero-downtime updates\n\n## Code Example\n```javascript\n// Traefik mirror configuration sketch (illustrative)\n{\n  routers: { apiProd: { rule: 'Host(`api.local`)', service: 'api-prod' } },\n  middlewares: { shadow: { mirror: { service: 'shadow-api' } } }\n}\n```\n\n## Follow-up Questions\n- How would you observe shadow vs production traffic without affecting user metrics?\n- How would you safely rotate shadow mappings per-tenant without downtime?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-15",
        "questionId": "q-3052",
        "question": "In a two-node docker-dca Swarm, deploy a TLS-enabled stateless API behind Nginx. Use Docker secrets for TLS certs and a Docker Config named flag.json mounted at /config/flag.json. Implement a simple feature toggle in the API that reads the flag from /config/flag.json and supports hot reload via SIGHUP without restarting. Add a progressive canary rollout: enable the feature in 0%, 25%, 50%, 75%, 100% in 20-second steps with health checks and automatic rollback on failure. Provide exact docker commands and a docker-compose.yml skeleton that demonstrates the rollout. Also show how to verify latency impact during each step?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Store a Docker Secret named tenant-allow mounted into the API container, parse at startup, and cache in memory. API reads X-Tenant, validates against the secret, and serves /data only if allowed; othe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deploy an Envoy sidecar alongside your API across two data centers, behind a Traefik edge proxy. Configure it to read a Docker Config named `tenant_region` that maps tenant IDs to their preferred data center, then rewri…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a 5-node Swarm across DCs, TLS via Docker secrets, Nginx edge proxy, and a per-service Envoy sidecar for canary routing. Routing rules pulled from a central HTTP API rendered into a Docker c",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy a Flask API that reads /config/flag.json to toggle GET /v1/feature, with SIGUSR1 handling for hot reload. TLS termination via Nginx using Docker Secrets, running 2 replicas on an overlay network. Implement canary…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests dynamic configuration reload, feature toggling, and safe canary rollout capabilities in a Docker Swarm environment.\n\n## Key Concepts\n- Docker Configs and Secrets management\n- TLS termination with Nginx reverse proxy\n- Signal-based hot reload in Python applications\n- Progressive canary deployment with health monitoring\n- Automated rollback mechanisms\n\n## Code Example\n```python\nfrom flask import Flask, jsonify\nimport json, signal\napp = Flask(__name__)\nflag = False\n\ndef load_flag():\n    global flag\n    try:\n        with open('/config/flag.json') as f:\n            data = json.load(f)\n            flag = data.get('feature_x', False)\n    except Exception:\n        flag = False\n\ndef reload_handler(signum, frame):\n    load_flag()\n    \nsignal.signal(signal.SIGUSR1, reload_handler)\nload_flag()\n\n@app.route('/v1/feature')\ndef feature():\n    return jsonify({'enabled': flag})\n\nif __name__ == '__main__':\n    app.run(host='0.0.0.0', port=8080)\n```\n\n## Implementation\n- Docker Config mounts flag.json at /config/flag.json\n- Docker Secrets store TLS certificates for Nginx\n- SIGUSR1 triggers config reload without container restart\n- Canary rollout uses service update with rollback on health check failure",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-16",
        "questionId": "q-20939",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement a canary deployment: run two API versions (v1 and v2) and configure Traefik to route 15% of traffic to v2 while 85% to v1. Expose per-tenant routing via X-Tenant header and rotate the canary weight via a Docker Config named canary-weight. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-stage canary: deploy auth-v2 with 1 replica and route 5% of traffic to it; ensure health checks pass; then roll the main stack with update_config start-first, parallelism 1. Commands: docker swarm",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement dynamic traffic shadow: deploy a main API and a canary, with a shadow Envoy sidecar mirroring ~5% of traffic to the canary; TLS via Docker secrets; Nginx routes 95% to main and 5% to shadow;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two API versions (v1 and v2) sit behind Traefik with TLS termination. Traefik uses a weighted router to send 15% of traffic to v2 and 85% to v1, with weight distribution controlled by a Docker Config named canary-weight…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Describe a runtime workload attestation flow: each service fetches a short-lived attestation token from a central Attestation Service tied to its SPIFFE ID; a separate verification sidecar validates t",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked: This question tests practical canary routing with Traefik, Docker Configs, and multi-data center deployment under TLS. Key Concepts: Traefik weighted routing, Docker Config integration, TLS edge termination, canary deployments, and multi-DC reliability. Code Example: A minimal docker-compose.yml snippet demonstrating Traefik labels for v1/v2 routing, plus a Docker Config for canary weight control. Follow-up questions typically explore monitoring strategies, automated rollback procedures, and observability during traffic shifts.",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-17",
        "questionId": "q-19264",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement a per-tenant daily quota system: quotas live in a Docker Config (quota-map.json) and are loaded by the API at boot, refreshed on updates. For each /import request, check the tenant’s remaining quota, perform the import if allowed, then decrement and publish a Redis PUB/SUB quota update so peers across DCs stay in sync. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a zero-downtime cross-DC Postgres migration: add a nullable new_col, perform a rolling backfill, and route tenants through a feature-gated read/write path via a lightweight sidecar that rewr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant daily quota system using Docker Config to store quota limits in quota-map.json. The API loads these quotas at startup and watches for configuration updates. For each /import request, the system ch…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Instrument the API with OpenTelemetry, using a per-tenant sampler read from a Docker Config (one per-tenant sampling rate). Spans go to a central OTLP collector in each DC. Traefik handles TLS and ten",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a policy-driven canary with OPA: per-tenant rules live in Docker Config; a sidecar queries OPA to decide if a tenant should hit the new path. Canary traffic is 20% for eligible tenants, acti",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical skills in cross-DC coordination, configuration management, and state synchronization using Docker Config and Redis Pub/Sub. It also covers rate limiting and error handling under load.\n\n## Key Concepts\n- Docker Config for storing per-tenant quota limits\n- Redis PUB/SUB for cross-DC quota propagation\n- Quota checks with eventual consistency across peers\n- TLS edge routing with Traefik\n\n## Code Example\n```javascript\nconst quota = getQuota(tenantId);\nif (quota <= 0) throw new Error('Quota exceeded');\n\nawait performImport(data);\ndecrementQuota(tenantId);\npublishQuotaUpdate(tenantId, getQuota(tenantId));\n```",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-18",
        "questionId": "q-14841",
        "question": "In a three-node docker-dca cluster across two data centers, implement a cross-DC trust bootstrap workflow: when a new DC joins, automatically fetch a Vault-stored CA bundle and SPIFFE trust anchors, rotate SPIFFE SVIDs for all services, and hot-load edge proxies with the updated trust material. Include idempotent checks, rollback, and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a bootstrap agent that activates on DC-join: pull Vault-provided CA bundles and SPIFFE trust anchors, rotate all SPIFFE SVIDs, and hot-reload edge proxies with the new material. Use",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Load a Wasmtime WASM policy module per tenant from a central broker. Before hot-loading, validate the module against a signed policy schema and API version; propagate a version stamp via SVIDs. Use a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "The candidate should outline: two backends (api and opa) behind Traefik; API calls OPA at http://opa:8181/v1/data/tenant/feature with input {tenant:\"...\"}; OPA policy checks tenant against tenant-flag",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a logging-sidecar approach: each service runs a small sidecar that outputs JSON logs with tenant, path, status, and latency to a shared log volume. A Fluent Bit container tails the logs and sh",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests automation, Vault + SPIFFE integration, and zero-downtime trust reconfiguration across DCs.\n\n## Key Concepts\n- Vault CA bundles and SPIFFE trust anchors\n- SPIFFE SVID rotation and edge-proxy hot-reload\n- Idempotence, rollback, and verification across DCs\n\n## Code Example\n```javascript\n// bootstrap trust distribution (pseudocode)\nasync function bootstrapCrossDC(dc) {\n  await fetchTrustAnchors(dc);\n  await rotateSVIDs(dc);\n  await hotReloadProxies(dc);\n}\n```\n\n## Follow-up Questions\n- How would you test drift between trust material and service SVIDs?\n- How do you handle partial failures during bootstrap (e.g., one DC unreachable)?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-19",
        "questionId": "q-27421",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy. Implement tenant-aware TLS termination with SNI routing for two domains, automate certificate renewal with a mock ACME server, and verify that each tenant's traffic uses its own certificate; provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Configure Traefik with two TLS routers using HostSNI rules for tenantA.example.com and tenantB.example.com, each routing to the same backend API service. Set up a mock ACME server to automate per-tenant certificate issu…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Route per-tenant production traffic through Traefik TLS edge and mirror to a shadow backend for canary testing. Identify tenants with X-Tenant, fetch shadow endpoints from Docker Config tenant-shadow,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant dynamic feature-flag system: a watcher sidecar polls Consul KV for tenant flags and updates Traefik's dynamic REST config (providers.rest) to toggle a tenant-specific rate limit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument the API with OpenTelemetry (Go) and export traces to a replicated OTLP collector across both DCs; propagate W3C TraceContext and tag traces with X-Tenant. Drive per-tenant sampling via a Do",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical knowledge of TLS termination, SNI routing, and certificate automation in a multi-tenant Docker Compose environment—areas often overlooked in production deployments.\n\n## Key Concepts\n\n- **Traefik TLS Configuration**: Use SNI-based routing to direct traffic to the appropriate tenant certificate\n- **Docker Secrets**: Securely store per-tenant certificates within Docker Swarm or Compose\n- **Mock ACME Server**: Simulate automated certificate issuance and renewal workflows\n- **Multi-DC Deployment**: Considerations for certificate synchronization across data centers and proper SNI hostname validation",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-20",
        "questionId": "q-28077",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Add tenant-specific request tracing using OpenTelemetry: deploy a per-tenant tracer with sampling rates controlled by a Docker Config (per-tenant), ship spans to a central OTLP collector deployed in each DC; ensure updates to per-tenant sampling rates via the Docker Config propagate without restarting services. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a four-node docker-dca canary with Envoy sidecars, edge TLS via Docker secrets, and a Consul KV flag to drive 10%→100% traffic steps every 30s. Health checks gate progress; rollback triggers on tw",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route per-tenant traffic via Traefik dynamic configuration updated by a central policy service. In the canary deployment, tenants in the canary set receive 10-20% traffic to v2, while remaining tenants continue using v1…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument the API with OpenTelemetry, using a per-tenant sampler read from a Docker Config (one per-tenant sampling rate). Spans go to a central OTLP collector in each DC. Traefik handles TLS and ten",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use edge TLS termination, Nginx proxy, and two Redis instances in each DC with Redis Sentinel for failover. Store sessions keyed by user_id in Redis; issue signed cookies to pin users to a DC; TTL 15–",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design tenant-scoped observability with minimal downtime, combining OpenTelemetry, Docker Config, and multi-DC deployment.\n\n## Key Concepts\n- OpenTelemetry, OTLP exporter, per-tenant sampling, dynamic reconfiguration, Docker Config integration, hot-reload without restart, Traefik TLS edge, multi-DC data path.\n\n## Code Example\n```yaml\n# sample otel collector and service config\notel:\n  receivers:\n    otlp:\n      protocols: { grpc: {}, http: {}}\n  processors:\n    batch: {}\n  exporters:\n    otlp:\n      endpoint: otlp-collector:4317\n      tls:\n        insecure: true\n```\n\n## Follow-up Questions\n- How would you test per-tenant sampling changes without restarting services?\n- How would you monitor collector backpressure across DCs?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-21",
        "questionId": "q-4237",
        "question": "Three-node docker-dca Swarm across two data centers; deploy a TLS-enabled API behind an Nginx edge proxy. Enforce a Cosign-based supply-chain policy: only attested images for API and sidecars; add a gate sidecar that verifies attestations at startup with Vault keys. Attach to a single overlay network. Implement a 10% canary rollout with health checks and automatic rollback. Provide exact commands and docker-compose skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Cosign attestations for the API and all sidecars; store keys in Vault and provide a gate sidecar that runs cosign verify on startup to reject non-attested images. Route through TLS-enabled Nginx e",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a per-tenant canary controller that writes status to Redis (key: canary:tenant:<id>, fields enabled, percentage). Traefik’s dynamic config is updated by a small sidecar reading Redis and rou",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Solution Overview Create a Go sidecar that watches Consul KV at `/tenants/{tenant_id}/region` and updates Nginx upstream servers dynamically. ### For open-source Nginx: - Sidecar writes to `/etc/nginx/conf.d/upstream…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant token-bucket rate limiter using Redis with keys derived from X-Tenant. Use a Lua script for atomic token fetch/consume; expose an admin endpoint to adjust caps without redeploy.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world supply-chain security in container deployments, using Cosign attestations, Vault-managed keys, and a gate sidecar to enforce policy before service start. Combines Swarm networking, TLS, and canary rollback under load.\n\n## Key Concepts\n- Image attestation with Cosign\n- Vault for key distribution and rotation\n- Gate sidecar for startup verification\n- Nginx TLS edge proxy integration\n- Canary rollout with health checks and rollback\n- Overlay network in Swarm\n\n## Code Example\n```javascript\n// gate sidecar startup check (pseudo)\nasync function verify(image){/* cosign verify against Vault key */}\nverify(process.env.IMAGE);\n```\n\n## Follow-up Questions\n- How would you test attestation failure scenarios in CI/CD?\n- How would you rotate Cosign keys without downtime?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-22",
        "questionId": "q-16942",
        "question": "In a two-data-center docker-dca deployment, place a TLS-enabled API behind an Envoy edge proxy and enable tenant-scoped A/B testing between API versions v1 and v2. Use a Redis-backed policy store for per-tenant routing decisions, validate tenants via JWT, route 25% of eligible tenants to v2, collect latency and error-rate, and automatically rollback if SLA breaches persist >5 minutes. Provide bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Configure Traefik with TLS termination and two routers, tenantA.api.example.org and tenantB.api.example.org, pointing to the same API. Load per-tenant credentials from a Docker Config named tenant-aut",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a two-DC deployment where TLS-terminated API behind an Envoy edge routes tenants for A/B testing between v1 and v2. Use Redis as a policy store to decide per-tenant routing, verify JWT-provi",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a Consul Connect-based service mesh with per-tenant service identities. Enable mTLS between Traefik and backend services using Envoy sidecars. Store per-tenant authorization policies in Consul KV and leverage…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy per-tenant eBPF programs attached to cgroups for outbound isolation, with Nginx providing TLS termination at the edge and tenant-scoped overlay networking routes. A lightweight policy daemon fetches rules from Va…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical edge routing with real-world constraints: per-tenant A/B testing, cross-DC consistency, and observable SLAs.\n\n## Key Concepts\n- Envoy edge TLS termination\n- Redis-backed per-tenant policy store\n- JWT-based tenant identification and authorization\n- Canary rollout with per-tenant routing\n- Automation of rollback under SLA breach across DCs\n\n## Code Example\n```yaml\n# Minimal bootstrap for envoy + redis (illustrative)\nversion: \"3\"\nservices:\n  envoy:\n    image: envoyproxy/envoy:v1.28.0\n    ports:\n      - \"443:443\"\n    volumes:\n      - ./envoy.yaml:/etc/envoy/envoy.yaml\n  redis:\n    image: redis:7-alpine\n```\n\n## Follow-up Questions\n- How would you test cross-DC latency and canary safety?\n- How would you handle Redis cluster failure scenarios?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-23",
        "questionId": "q-25825",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement tenant-aware rate limiting with a Redis-backed limiter service, keyed by X-Tenant, enforcing per-tenant quotas (RPS and burst). Use a central Docker Config per-tenant quotas that hot-reloads without restart, and add a circuit-breaker when Redis is unavailable. Include bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Redis-backed token-bucket limiter as a per-tenant sidecar. It reads per-tenant quotas from a Docker Config (quotas.json) keyed by X-Tenant, and enforces RPS/burst in real time. A watcher h",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a Redis-backed quota per-tenant. A lightweight sidecar in the request path increments tenant:window in Redis with a 60s TTL and allows if count <= 60; otherwise returns 429. Use X-Tenant to se",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy Calico NetworkPolicy to segment tenants by namespace labels within the docker-dca cluster. Expose TLS via Traefik with mutual TLS (mTLS) termination, store per-tenant policies in Consul KV, and trigger automatic…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Onboard a tenant by provisioning an envelope key and a short-lived TLS cert in Vault, creating a tenant-scoped overlay network, and pushing a dynamic Nginx config via a watcher sidecar. Implement 24h",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical latency and resilience patterns in multi-dc deployments: per-tenant rate control, config hot-reload, and failure handling.\n\n## Key Concepts\n- Tenant-aware rate limiting with Redis-backed token bucket\n- Docker Config as per-tenant quotas and hot reload\n- Edge proxy integration and circuit-breaker on Redis outage\n\n## Code Example\n```javascript\n// Pseudo: limiter reads quotas.json, applies token bucket logic per tenant\n```\n\n## Follow-up Questions\n- How would you observe/alert on quota breaches and Redis outages? \n- How would you test hot-reload behavior in CI?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-24",
        "questionId": "q-20572",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant logging: use a Docker Config tenant-logging.json mapping tenant IDs to log levels (debug/info/warn/error). The API should emit JSON logs including tenant_id, method, path, status, and timestamp; a lightweight sidecar log-shipper writes per-tenant logs to local files. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Init Swarm on DC-A; join DC-B; create overlay api-net; deploy two services: api-dcA with 2 replicas constrained to DC-A and api-dcB with 1 replica constrained to DC-B, both on api-net and update_confi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route tenants based on Redis-backed feature flags using Nginx with Lua scripting. Extract the X-Tenant-Id header from incoming requests, query Redis for that tenant's flag value, and dynamically route to /api/v2 when th…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure Traefik with two TLS routers, one for /v1 and one for /v2, routing decisions based on the X-Tenant header using a small middleware that rewrites the request path to the correct version; defa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy a TLS-enabled API behind Traefik in a Docker DCA cluster across two data centers. Implement per-tenant logging using a Docker Config (tenant-logging.json) mapping tenant IDs to log levels (debug/info/warn/error).…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical Docker DCA logging discipline, runtime configuration management, and multi-data center edge routing with a focus on per-tenant logging control. It evaluates the candidate's ability to handle multi-tenancy, secure communications, and observability in a distributed system.\n\n## Key Concepts\n\n- **Docker Configs**: Store runtime per-tenant logging configurations as secrets/configs that can be mounted without baking into images\n- **Traefik**: TLS termination, load balancing, and routing at the edge\n- **JSON Structured Logs**: Machine-readable logs with tenant context for filtering and analysis\n- **Live Config Reload**: Updating log levels without downtime\n- **Sidecar Pattern**: Lightweight log shipper writing per-tenant logs to local files\n\n## Code Example\n\n```javascript\n// Tenant-aware logger\nconst fs = require('fs');\nlet cfg = JSON.parse(fs.readFileSync('/config/tenant-logging.json','utf8'));\n\nfunction log(tenant, level, entry) {\n  const logLevel = cfg[tenant] || 'info';\n  if (shouldLog(level, logLevel)) {\n    console.log(JSON.stringify({\n      tenant_id: tenant,\n      level: level,\n      timestamp: new Date().toISOString(),\n      ...entry\n    }));\n  }\n}\n\nfunction shouldLog(requested, configured) {\n  const levels = { debug: 0, info: 1, warn: 2, error: 3 };\n  return levels[requested] >= levels[configured];\n}\n```",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-15T14:18:05.213Z",
    "version": 1
  }
]