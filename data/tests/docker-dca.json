[
  {
    "id": "test-docker-dca",
    "channelId": "docker-dca",
    "channelName": "Docker DCA",
    "title": "Docker DCA Knowledge Test",
    "description": "Test your understanding of Docker DCA with 25 questions.",
    "questions": [
      {
        "id": "docker-dca-q-0",
        "questionId": "q-13732",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster. Extend with mTLS to Postgres: a sidecar fetches short-lived client certificates and CA certs from Vault every 12 minutes and updates an in-memory PG connection pool without restarting the API. Describe how to implement zero-downtime cert rotation, bootstrap commands, and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe wiring TLS mutual authentication to identify tenants from the client CN, Traefik per-tenant weights (0-100) driven by a Docker Config for canary deployment across DCs, with automatic rollback",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Vault Agent sidecar that auto-renews and writes certs to a shared /certs volume. Configure the API and PG client to reference these certs on startup and reload the pool with SIGHUP when Vault si",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a central tenant-version map in Consul KV (or Redis), a per-tenant sidecar that reads X-Tenant and routes to blue or green through Traefik, enabling on-the-fly tenant pinning without redeploys",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Vault PKI to issue per-tenant client certs with TTL around 60 minutes, leverage Traefik to terminate TLS and enforce mTLS to a tenant-scoped backend. A sidecar fetches/refreshes certs and reloads",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical mTLS cert rotation and cross-DC reliability in docker-dca, not just TLS setup.\n\n## Key Concepts\n- mTLS with Postgres\n- Vault Agent and auto-renewal\n- Sidecar pattern sharing /certs\n- In-process pool reload on cert rotation\n\n## Code Example\n```yaml\n# docker-compose.yml (excerpt)\nversion: '3.8'\nservices:\n  api:\n    image: myapi:latest\n    volumes:\n      - certs:/certs\n      - ./config:/config\n  vault-agent:\n    image: hashicorp/vault:1.14\n    volumes:\n      - certs:/certs\n      - ./vault/config:/vault/config\nvolumes:\n  certs:\n```\n\n```yaml\n# vault-agent config snippet (config.hcl) — simplified\nauto_auth {\n  method \"approle\" {\n    config = {\n      role_id_file_path = \"/vault/config/role_id.secret\"\n      secret_id_file_path = \"/vault/config/secret_id.secret\"\n    }\n  }\n  sink \"file\" { config = {\n    path = \"/certs/tls.pem\" \n  } }\n}\n```\n\n## Follow-up Questions\n- How would you validate cert rotation impact on active connections during peak load?\n- What are the trade-offs of using Vault vs a local CA for this pattern?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-1",
        "questionId": "q-23417",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Add per-tenant distributed tracing: propagate X-Tenant through all services, emit OpenTelemetry traces, and route them to a central Jaeger backend with per-tenant namespaces; configure per-tenant sampling via a Docker Config. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a tenant-aware OpenTelemetry pipeline: TLS at Traefik, forward X-Tenant to services; each service emits OTLP traces to a collector, which exports to Jaeger with per-tenant namespaces. Drive",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Store a Docker Secret named tenant-allow mounted into the API container, parse at startup, and cache in memory. API reads X-Tenant, validates against the secret, and serves /data only if allowed; othe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: Bring up both DCs as a single swarm, create an attachable overlay, deploy Traefik with TLS via Docker secrets, create a Swarm Config with the feature flag, launch two API stacks (prod and canary",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Docker Config tenant-canaries to mark canary tenants and Traefik routers to direct X-Tenant==canary to api-v2 while others go to api-v1. Implement per-tenant health checks and a lightweight moni",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design an end-to-end observability pipeline across DCs, with per-tenant isolation and dynamic sampling. Requires integrating edge routing, trace propagation, and a centralized backend.\n\n## Key Concepts\n\n- OpenTelemetry OTLP pipeline and per-tenant attributes\n- Traefik TLS termination and header propagation of X-Tenant\n- Docker Config for per-tenant sampling configuration\n- Jaeger (or Tempo) backend with tenant-scoped namespaces\n- Cross-datacenter observability and basic resilience checks\n\n## Code Example\n\n```yaml\n# Minimal docker-compose skeleton (excerpt)\nversion: '3.8'\nservices:\n  traefik:\n    image: traefik:latest\n    ports:\n      - \"443:443\"\n    volumes:\n      - ./traefik.yaml:/etc/traefik/traefik.yaml\n  api:\n    image: myapi:latest\n    environment:\n      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317\n    depends_on:\n      - otel-collector\n  otel-collector:\n    image: otel/opentelemetry-collector\n    volumes:\n      - ./otel-config.yaml:/etc/otel/config.yaml\n    command: [\"--config=/etc/otel/config.yaml\"]\n```\n\n## Follow-up Questions\n\n- How would you validate tenant isolation in traces and prevent leakage?\n- What failure scenarios would change your sampling strategy across DCs?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-2",
        "questionId": "q-29187",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant request tracing with OpenTelemetry: include X-Tenant in trace attributes, and drive per-tenant sampling using a Docker Config trace-config.json. Deploy an OTEL Collector per DC and a Jaeger backend. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Vault to issue per-tenant signing keys and rotate via leases. Implement a log- producer sidecar that signs each log line with the tenant’s key and publishes to a tenant-specific Kafka topic.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument the API with OpenTelemetry and route traces to a per-datacenter OTEL Collector with a Jaeger backend. Attach X-Tenant as a trace attribute via middleware and implement per-tenant sampling through a Docker Con…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I would deploy a Traefik edge and a tenant-aware API behind TLS. Store per-tenant flags in a Docker Config named tenant-flags (JSON). Bootstrap flags for alpha/beta with a script and bootstrap manifes",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a tiny Python sidecar 'audit-shipper.py' that tails /var/log/api/access.jsonl (produced by the API) in real-time and writes per-tenant JSON entries to /audit/logs/audit.jsonl. Each line include",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to implement observable distributed tracing across data centers using docker-dca patterns and TLS edge routing. It also validates per-tenant observability controls and configuration management via Docker Config.\n\n## Key Concepts\n- OpenTelemetry for distributed tracing\n- Traefik TLS termination and routing\n- Docker Config for per-tenant sampling configuration\n- Multi-datacenter trace collection architecture\n\n## Code Example\n```json\n{ \"traceConfig\": { \"tenantA\": 0.5, \"tenantB\": 1.0 } }\n```\n\n## Follow-up Questions\n- How would you validate traces per tenant in Jaeger?\n- What if a tenant has multiple API instances?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-3",
        "questionId": "q-28320",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant canary rollouts with live-weighted routing: store per-tenant weights in Consul KV and configure Traefik to route a portion of each tenant’s traffic to a canary backend, with zero-downtime reloads driven by a Docker Config update. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "The candidate should outline: two backends (api and opa) behind Traefik; API calls OPA at http://opa:8181/v1/data/tenant/feature with input {tenant:\"...\"}; OPA policy checks tenant against tenant-flag",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deploy an admission sidecar that calls an OPA server before launching the API container. Policies validate allowed base images per tenant, allowed tags, and required licenses; violations exit non-zero",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure two backends per tenant (stable and canary) behind Traefik. Read per-tenant weights from Consul KV at the path tenants/{tenant}/weight, then inject these weights into Traefik's dynamic configuration via per-te…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Configure Traefik with TLS termination and two routers, tenantA.api.example.org and tenantB.api.example.org, pointing to the same API. Load per-tenant credentials from a Docker Config named tenant-aut",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis tests a candidate's ability to design and implement per-tenant canary rollouts with dynamic configuration across a docker-dca cluster spanning multiple data centers. The solution requires integrating Traefik, Consul KV, and Docker Config while maintaining zero-downtime deployments.\n\n## Key Concepts\n\n- Per-tenant weighted routing via Traefik's dynamic configuration\n- Cross-datacenter deployment with zero-downtime reloads\n- Consul KV-backed weight storage for real-time traffic shifting\n- Docker Config-driven reconfiguration for automated deployments\n- Observability and rollback strategies for safe canary releases\n\n## Code Example\n\n```yaml\nhttp:\n  routers:\n    tenant1:\n      rule: \"Host(`tenant1.example.com`)\"\n      tls: {}\n      service: tenant1\n  services:\n    tenant1:\n      weighted:\n        services:\n          - name: tenant1-stable@file\n            weight: 90\n          - name: tenant1-canary@file\n            weight: 10\n```",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-4",
        "questionId": "q-19316",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant feature flags: tenant flags are stored in a Docker Config mapping tenantId to featureName->enabled. The API should load flags per-tenant, cache them with a per-tenant TTL, invalidate on update via a Redis pub/sub channel 'tenant-flags'. Changes from the central policy store must propagate within 2 seconds. Bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Generate a private certificate authority, create and sign certificates for both edge and API services, configure Nginx as a TLS termination point with two upstreams (api_v1, api_v2). Implement Nginx's split_clients modu…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a tiny Python sidecar 'audit-shipper.py' that tails /var/log/api/access.jsonl (produced by the API) in real-time and writes per-tenant JSON entries to /audit/logs/audit.jsonl. Each line include",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To solve this, terminate TLS at the edge, enable mTLS inside the cluster via Vault-issued SPIFFE SVIDs, and run a cert-verifying sidecar next to each service. The OPA policy, stored in a Docker Config",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a per-tenant feature-flag store in Docker Config (tenantId → feature → bool) plus an in-process per-tenant LRU cache with 2–5 minute TTL in the API. A lightweight Flag/Policy service reads the Docker Config, pu…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design distributed feature-flag propagation with per-tenant isolation across data centers. It covers Docker Config for tenant flags, per-tenant in-process caching with TTL, Redis Pub/Sub for invalidation and cross-DC propagation, and edge security with TLS termination at Traefik.\n\n## Key Concepts\n- Docker Config for tenant-specific flag storage\n- Per-tenant in-process cache with configurable TTL\n- Redis Pub/Sub for cache invalidation and propagation\n- Lightweight policy/flag service coordinating across DCs\n- TLS termination at Traefik edge proxy\n\n## Code Example\n```javascript\nclass FlagCache {\n  constructor(redis, ttlSeconds = 120) {\n    this.cache = new Map();\n    this.ttlSeconds = ttlSeconds;\n    this.redis = redis;\n    \n    redis.subscribe('tenant-flags', (err, count) => {\n      if (err) console.error('Subscribe error:', err);\n    });\n    \n    redis.on('message', (channel, message) => {\n      if (channel === 'tenant-flags') {\n        const { tenantId } = JSON.parse(message);\n        this.cache.delete(tenantId);\n      }\n    });\n  }\n\n  async getFlag(tenantId, feature) {\n    const key = `${tenantId}:${feature}`;\n    const entry = this.cache.get(key);\n    \n    if (entry && Date.now() < entry.expiresAt) {\n      return entry.value;\n    }\n    \n    const value = await this.redis.hget(`flags:${tenantId}`, feature);\n    this.cache.set(key, { value: JSON.parse(value), expiresAt: Date.now() + this.ttlSeconds * 1000 });\n    return JSON.parse(value);\n  }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-5",
        "questionId": "q-26597",
        "question": "Across two data centers in a docker-dca cluster, deploy a TLS-enabled API behind a Traefik edge proxy and implement tenant-aware traffic shadow and canary deployments. Use a central policy service (e.g., Consul KV) to drive per-tenant canary weights and shadow destinations, with updates propagating via Docker Config without restarts. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Explain how you would implement tenant-specific A/B routing with Traefik and a Redis-backed Experiment service, enabling per-tenant config updates without redeploys, including canary thresholds, healt",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument the API with OpenTelemetry (Node/Python). Export traces via OTLP to an otel-collector sidecar, which forwards to Jaeger. Propagate trace-context through Nginx by enabling W3C traceparent he",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route per-tenant traffic using a canary weight fetched from Consul KV, mapped via X-Tenant header in Traefik. Implement a shadow destination mirroring a percentage of requests to the canary API while",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a 3-node docker-dca with TLS in front, plus an anomaly-edge sidecar. It loads tenant models from a TLS registry, refreshes every 5m, scores each request, and rejects outliers. Uses Vault SPIFFE",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design nuanced canary and shadow deployments for multi-DC, with dynamic policy updates.\n\n## Key Concepts\n- Traefik per-tenant weighted routing\n- Tenant-aware shadow/canary deployment\n- Central policy store (Consul KV)\n- Docker Config-based dynamic updates\n- Mutual TLS and security considerations\n\n## Code Example\n```yaml\n# Traefik dynamic router example (pseudo)\nhttp:\n  routers:\n    tenant-router:\n      rule: \"Headers(\\\"X-Tenant\\\")\"\n      service: tenant-api\n      tls: true\n```\n\n```bash\n# bootstrap (pseudo)\nCONSUL_KEY=\"tenants/$TENANT/canary_weight\"\nWEIGHT=$(consul kv get $CONSUL_KEY)\n```\n\n```\n\n## Follow-up Questions\n- How would you monitor and rollback if canary latency spikes?\n- How to ensure data isolation during shadow traffic?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-6",
        "questionId": "q-10551",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Envoy edge proxy in a docker-dca cluster. Implement per-tenant data residency: tenants labeled residency=local must route to DC1; others may be served from DC1 or DC2. Use a central OPA policy store, Consul for service discovery, and a lightweight routing sidecar that enforces policy at runtime and logs decisions. Provide bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe implementing per-tenant residency with Envoy as edge proxy and OPA-based routing. Tenants carry residency label in JWT; Consul registers services in DC1 and DC2; a sidecar queries OPA to deci",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Configure TLS Termination at Traefik and route to two DC API instances. Add an OpenTelemetry Collector as a sidecar or central collector; drive tenant-specific sampling via a Docker Config tenant_samp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy plan: create TLS secret and API endpoints config, run a two-variant stack (blue/green) behind Nginx, wire a sidecar log-shipper, attach to overlay net, and use health checks with automatic roll",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage Vault to issue per-tenant signing keys and rotate via leases. Implement a log- producer sidecar that signs each log line with the tenant’s key and publishes to a tenant-specific Kafka topic.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to combine edge proxy routing, policy-as-code, and multi-DC placement.\n\n## Key Concepts\n- Envoy edge proxy, mTLS, JWT-tenant passthrough\n- OPA policy store, rego-based routing rules\n- Consul service discovery, docker-dca deployment\n- Cross-DC routing decisions and auditing\n\n## Code Example\n```yaml\n# docker-compose skeleton snippet (conceptual)\nversion: '3.8'\nservices:\n  edge:\n    image: envoyproxy/envoy:v1.25\n    ports:\n      - '443:443'\n    networks: [default]\n    # TLS, ext_authz to OPA, etc.\n```\n\n## Follow-up Questions\n- How would you test residency policy rollback?\n- How handle policy cache invalidation across DCs?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-7",
        "questionId": "q-2918",
        "question": "In a two-node docker-dca Swarm, deploy a TLS-enabled stateless API behind an Nginx edge proxy. Use Docker secrets for TLS certs and a Docker config to store per-tenant ACLs. Add an Open Policy Agent (OPA) sidecar to enforce per-request authorization, with policy loaded from a Docker config. Include a separate log-collector sidecar that writes audit logs to a shared volume. Implement a 20% canary rollout every 15 seconds with health checks and automatic rollback on policy violation. Provide exact commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant daily quota system using Docker Config to store quota limits in quota-map.json. The API loads these quotas at startup and watches for configuration updates. For each /import request, the system ch…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Configure Traefik TLS with mTLS; implement a tenant router middleware that reads X-Tenant and consults a Docker Config tenant-region-map {tenantA:dc1, tenantB:dc2}. Forward to the home DC backend; if",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy a TLS-enabled API behind Nginx with OPA policy enforcement and canary rollouts. Use Docker secrets for certs, configs for ACLs, and sidecar containers for authorization and logging. Implement 20% canary deploymen…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design uses Traefik middleware to route by X-Tenant, fetch policy from Consul KV at /policies/{tenant} for log level and sink URL, and retrieve per-tenant credentials from Vault with short-lived token",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to implement zero-trust security, multi-tenant isolation, and safe deployment patterns in Docker Swarm with policy-as-code enforcement.\n\n## Key Concepts\n- Swarm secrets/configs for secure data management\n- Sidecar pattern for policy enforcement and logging\n- Canary deployments with automatic rollback\n- TLS termination at edge proxy\n- OPA for real-time authorization decisions\n\n## Code Example\n```yaml\nversion: '3.8'\nservices:\n  nginx:\n    image: nginx:alpine\n    ports: ['443:443']\n    secrets:\n      - source: tls-cert\n        target: /etc/nginx/ssl/tls.crt\n      - source: tls-key\n        target: /etc/nginx/ssl/tls.key\n    configs:\n      - source: nginx-conf\n        target: /etc/nginx/nginx.conf\n    deploy:\n      replicas: 1\n      restart_policy:\n        condition: on-failure\n        delay: 5s\n        max_attempts: 3\n    networks:\n      - frontend\n\n  api:\n    image: my-api:latest\n    environment:\n      - TENANT_CONFIGS=/configs/acls.json\n    configs:\n      - source: tenant-acls\n        target: /configs/acls.json\n    deploy:\n      replicas: 5\n      update_config:\n        parallelism: 1\n        delay: 15s\n        failure_action: rollback\n        monitor: 30s\n        max_failure_ratio: 0.2\n      rollback_config:\n        parallelism: 1\n        delay: 10s\n      healthcheck:\n        test: [\"CMD\", \"curl\", \"-f\", \"http://localhost:8080/health\"]\n        interval: 10s\n        timeout: 5s\n        retries: 3\n        start_period: 30s\n    networks:\n      - frontend\n      - backend\n    volumes:\n      - audit-logs:/var/log/audit\n\n  opa:\n    image: openpolicyagent/opa:latest\n    command: [\"run\", \"--server\", \"/policies/policy.rego\"]\n    configs:\n      - source: opa-policy\n        target: /policies/policy.rego\n    environment:\n      - OPA_LOG_LEVEL=info\n    networks:\n      - backend\n    healthcheck:\n      test: [\"CMD\", \"curl\", \"-f\", \"http://localhost:8181/v1/data\"]\n      interval: 30s\n      timeout: 5s\n      retries: 3\n\n  log-collector:\n    image: fluent/fluent-bit:latest\n    command: [\"/fluent-bit/bin/fluent-bit\", \"--config\", \"/fluent-bit/etc/fluent-bit.conf\"]\n    volumes:\n      - audit-logs:/var/log/audit\n      - ./fluent-bit.conf:/fluent-bit/etc/fluent-bit.conf:ro\n    networks:\n      - backend\n    deploy:\n      replicas: 1\n      restart_policy:\n        condition: on-failure\n\nsecrets:\n  tls-cert:\n    external: true\n  tls-key:\n    external: true\n\nconfigs:\n  nginx-conf:\n    external: true\n  opa-policy:\n    external: true\n  tenant-acls:\n    external: true\n\nvolumes:\n  audit-logs:\n    driver: local\n\nnetworks:\n  frontend:\n    driver: overlay\n  backend:\n    driver: overlay\n    internal: true\n```\n\n## fluent-bit.conf\n```ini\n[SERVICE]\n    Flush         5\n    Log_Level     info\n    Daemon        off\n\n[INPUT]\n    Name          tail\n    Path          /var/log/audit/*.log\n    Tag           audit.*\n    Refresh_Interval 10\n\n[OUTPUT]\n    Name          file\n    Match         audit.*\n    Path          /var/log/processed\n    File          audit.log\n```\n\n## Implementation Details\n\n**OPA Integration**: OPA sidecar loads policies from Docker config and exposes authorization endpoint at `http://localhost:8181/v1/data`. API checks authorization before processing requests.\n\n**Canary Strategy**: 20% rollout (1 out of 5 replicas) every 15 seconds with 30-second health monitoring. Automatic rollback on >20% failures.\n\n**Security**: TLS termination at Nginx edge, secrets management for certificates, internal backend network isolation, per-tenant ACL enforcement.\n\n**Logging**: Audit logs written to shared volume, processed by fluent-bit sidecar for aggregation and forwarding.",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-8",
        "questionId": "q-4345",
        "question": "In a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy. Enforce image provenance with Docker Content Trust (Notary) by signing base images and validating signatures in a dedicated 'sigver' sidecar before the request reaches the app. Use Vault to issue SPIFFE SVIDs; enforce an OPA policy loaded via a Docker Config to gate access. Implement a 20% canary rollout with latency and error-rate thresholds and automatic rollback. Provide exact commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Explain how you would implement tenant-specific A/B routing with Traefik and a Redis-backed Experiment service, enabling per-tenant config updates without redeploys, including canary thresholds, healt",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two API versions v1 and v2 behind Traefik in two-DC docker-dca. Rollout-config.json in a Docker Config maps tenant to canary%.\\nA lightweight canary-decider reads this per-tenant map and routes to v2",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would sign images with Docker Content Trust, add a lightweight 'sigver' sidecar that validates signatures against Notary on container startup and before traffic; issue SVIDs with Vault for mTLS; fet",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create two service versions (stable and canary) behind Traefik TLS. Add a Docker Config canary-tenant-map that maps X-Tenant values to 'stable' or 'canary'. Route with Traefik rule on Headers 'X-Tenan",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end trust from image provenance to runtime policy enforcement, plus canary control and rollback.\n\n## Key Concepts\n- Docker Content Trust/Notary for image provenance\n- SPIFFE/SVIDs via Vault for mTLS\n- OPA policies loaded via Docker Config\n- Sigver sidecar for per-request validation\n- Canary rollout with latency/error thresholds and rollback\n\n## Code Example\n```javascript\n// Pseudo example: verify image signature before starting app\nasync function verifySignature(image) {\n  const sig = await notary.verify(image);\n  if (!sig || !sig.valid) throw new Error('invalid image');\n  return true;\n}\n```\n\n## Follow-up Questions\n- How would you handle signature revocation and key rotation in this setup?\n- How would you scale the sigver and policy sidecars across nodes?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-9",
        "questionId": "q-4640",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster. Enforce mTLS between services with SPIFFE SVIDs issued by Vault, and attach a sidecar that validates the SVIDs and enforces an OPA policy loaded via Docker Config for per-tenant feature gates. Implement a canary rollout with traffic shifting and automatic rollback. Include exact bootstrap commands and a minimal docker-compose skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a chaos-driven ingress: a controller fetches per-tenant fault rules from Vault, applies latency, jitter, or drops via Nginx Lua per tenant, and triggers automatic rollback if an SLA latency",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a sidecar validator that reads tenant CN from the MTLS certificate, fetches the tenant schema from the tenant-schemas Docker Config, caches it, and validates POST /data bodies before deliver",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument API with OpenTelemetry; export OTLP traces to a central collector (Tempo/Jaeger) across DCs; tag spans with X-Tenant; drive per-tenant sampling via Docker Config sampling-config (JSON) read",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To solve this, terminate TLS at the edge, enable mTLS inside the cluster via Vault-issued SPIFFE SVIDs, and run a cert-verifying sidecar next to each service. The OPA policy, stored in a Docker Config",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design multi-DC, mTLS, SPIFFE, Vault, OPA, canary strategy in docker-dca, with real-world constraints and measurable rollouts.\n\n## Key Concepts\n- mTLS inside cluster, SPIFFE SVIDs, Vault integration\n- Sidecar pattern for security and policy enforcement\n- Docker Config-based policies and per-tenant gates\n- Canary rollouts and automatic rollback using traffic shaping\n- Observability thresholds and rollback criteria\n\n## Code Example\n```bash\n# bootstrap sketch\ndocker swarm init\nvault server -config=config.hcl\nvault write ... # issue SVIDs\n```\n```\n\n## Follow-up Questions\n- How to test canary path under latency spikes?\n- How to rotate SVIDs without downtime?\n```",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-10",
        "questionId": "q-5175",
        "question": "In a two-node docker-dca cluster, deploy a TLS-enabled API behind an Nginx edge proxy. Extend with OpenTelemetry tracing: instrument the API, add an otel-collector sidecar, and export traces to Jaeger; ensure trace context propagates through Nginx to the API; provide a docker-compose.yml skeleton and bootstrap commands; include a quick validation by hitting an endpoint and checking Jaeger UI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Sigstore cosign to sign images; deploy a verifier sidecar on each node and an edge-verifier at the gateway. Enable mTLS via SPIFFE/SVIDs and rotate keys with Vault or a similar CA. Isolate tenants",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe a two-DC deployment where a TLS API behind Traefik uses Vault for dynamic per-tenant DB credentials. The API authenticates with AppRole, fetches tenant creds with a short lease, renews proact",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument the API with OpenTelemetry (Node/Python). Export traces via OTLP to an otel-collector sidecar, which forwards to Jaeger. Propagate trace-context through Nginx by enabling W3C traceparent he",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Proposed approach: 2-node docker-dca Swarm with an Nginx TLS edge proxy and TLS certs in Secrets. Deploy Redis as the feature-flag store; canary routing sends 20% to /api/v2 when feature.canary.v2=1.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to add observability with OpenTelemetry in a docker-dca context, ensuring end-to-end trace propagation from edge to service and basic validation.\n\n## Key Concepts\n- OpenTelemetry instrumentation and OTLP exporter\n- otel-collector as a sidecar and Jaeger backend\n- Trace context propagation (traceparent/tracestate)\n- docker-compose scaffolding for api, nginx, collector\n- Basic end-to-end validation via Jaeger UI\n\n## Code Example\n```javascript\n// Node.js OpenTelemetry setup (simplified)\nconst { NodeTracerProvider } = require('@opentelemetry/sdk-trace-node');\nconst { OTLPTraceExporter } = require('@opentelemetry/exporter-otlp-http');\nconst { SimpleSpanProcessor } = require('@opentelemetry/tracing');\n\nconst provider = new NodeTracerProvider();\nprovider.addSpanProcessor(new SimpleSpanProcessor(\n  new OTLPTraceExporter({ url: 'http://otel-collector:4318/v1/traces' })\n));\nprovider.register();\n```\n\n## Follow-up Questions\n- How would you adjust sampling and metric correlation to avoid perf impact on a busy API?\n- How would you test trace failures or collector downtime without losing observability?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-11",
        "questionId": "q-14210",
        "question": "In a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy and attach an OpenTelemetry tracing pipeline that propagates SPIFFE IDs across services. Use Vault to issue SPIFFE SVIDs and configure per-tenant tracing/feature flags. Implement a 25% canary of a new API version with latency and error-rate thresholds and automatic rollback. Provide exact bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Parse X-Tenant, fetch limits from Docker Config tenant-rate-limits.json (tenantId: rpm). Implement in-memory per-tenant token bucket with 1-minute refill; if tokens available, process and decrement; i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: use Traefik mTLS (TLSOptions) with an internal CA that signs per-tenant certs on demand. A sidecar validates the client cert, extracts the tenant from CN, and injects X-Tenant for upstream polic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lightweight per-tenant policy engine in the API that loads policies.json from a Docker Config, extracts the tenant from the X-Tenant header, validates the request path against the allowedPaths for that tenan…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Set up TLS with Nginx in front, Vault-issued SPIFFE SVIDs for all services, and an OpenTelemetry collector to export traces to a central backend. Use per-tenant feature flags stored in Vault with a 25",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cross-DC TLS, identity, tracing, and controlled rollouts with real-world constraints.\n\n## Key Concepts\n\n- End-to-end TLS and Nginx edge proxy\n- SPIFFE SVID issuance via Vault\n- OpenTelemetry trace propagation across services\n- Per-tenant feature flags and canary rollout\n- Automatic rollback based on latency and error-rate SLAs\n\n## Code Example\n\n```yaml\nversion: \"3.8\"\nservices:\n  edge:\n    image: nginx:stable\n    ports: [\"80:80\",\"443:443\"]\n    networks: [\"overlay\"]\n  api:\n    image: myorg/api:latest\n    networks: [\"overlay\"]\n    deploy:\n      replicas: 2\n  collector:\n    image: otel/collector\n    networks: [\"overlay\"]\n```\n\n## Follow-up Questions\n\n- How would you implement per-tenant routing logic to span across data centers?\n- How would you handle Vault rotation and SVID renewal without downtime?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-12",
        "questionId": "q-26305",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant envelope encryption using Vault Transit: each tenant has a Transit key named tenant-<id> to encrypt data keys that guard tenant data at rest. Rotate keys on a 24h cadence with zero downtime, evicting cached tokens via a rewrap strategy; ensure X-Tenant selects tenant key. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant envelope encryption using Vault Transit, where each tenant possesses a Transit key named tenant-<id>. This key wraps a data key that encrypts tenant data at rest. A sidecar container fetches the act…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Instrument the API and edge proxy with OpenTelemetry (OTLP exporter to Jaeger), initialize a tracer in each service, and propagate traceparent headers through Nginx and the Verifier sidecar. Run Jaege",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument the API with OpenTelemetry (Node/Python). Export traces via OTLP to an otel-collector sidecar, which forwards to Jaeger. Propagate trace-context through Nginx by enabling W3C traceparent he",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a small Node.js Express API behind Nginx TLS. Load a per-tenant feature map from a Docker Config (flags.json) and poll/reload every 30s. Gate GET /payments by checking X-Tenant-ID against the ma",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of cross-datacenter security architecture, key management lifecycle, and zero-downtime key rotation strategies in distributed systems.\n\n## Key Concepts\n- Per-tenant envelope encryption using Vault Transit\n- Zero-downtime key rotation via rewrap strategy\n- mTLS between services and Vault for secure communication\n- Docker Config secrets for tenant key provisioning\n- Traefik for TLS termination and tenant routing\n\n## Bootstrap Commands\n```bash\n# Enable Vault Transit\nvault secrets enable transit\n\n# Create per-tenant keys\nfor id in tenant-a tenant-b; do\n  vault write transit/keys/$id rotation_period=\"24h\"\ndone\n\n# Configure key rotation policy\nvault write transit/keys/tenant-a/config rotation_period=\"24h\"\n\n# Create rewrap policy\nvault policy write rewrap-policy - <<EOF\npath \"transit/rewrap/*\" { capabilities = [\"update\"] }\npath \"transit/keys/*\" { capabilities = [\"read\"] }\nEOF\n\n# Generate Vault token for rewrap\nvault token create -policy=rewrap-policy\n```\n\n## Minimal docker-compose.yml Skeleton\n```yaml\nversion: '3.8'\nservices:\n  traefik:\n    image: traefik:v2.10\n    ports:\n      - \"443:443\"\n    volumes:\n      - /var/run/docker.sock:/var/run/docker.sock:ro\n    command:\n      - --providers.docker\n      - --entrypoints.websecure.address=:443\n      - --tls.certResolver=letsencrypt\n\n  vault:\n    image: hashicorp/vault:1.15\n    environment:\n      VAULT_ADDR: http://vault:8200\n    cap_add:\n      - IPC_LOCK\n    volumes:\n      - vault-data:/vault/file\n\n  api:\n    image: your-api-image\n    environment:\n      VAULT_ADDR: http://vault:8200\n      VAULT_TOKEN_FILE: /run/secrets/vault_token\n    secrets:\n      - vault_token\n    labels:\n      - \"traefik.enable=true\"\n      - \"traefik.http.routers.api.rule=PathPrefix(`/api`)\"\n      - \"traefik.http.routers.api.tls=true\"\n\n  key-rewrapper:\n    image: your-rewrap-image\n    environment:\n      VAULT_ADDR: http://vault:8200\n    depends_on:\n      - vault\n    restart: unless-stopped\n    cron: \"0 * * * *\"  # Hourly rewrap check\n\nvolumes:\n  vault-data:\n\nsecrets:\n  vault_token:\n    file: ./vault-token.txt\n```\n\n## X-Tenant Header Flow\n1. Client sends request with X-Tenant: tenant-a header\n2. Traefik routes to appropriate API instance\n3. API extracts tenant ID and retrieves tenant-a Transit key\n4. API encrypts/decrypts data using the tenant-specific data key\n5. Cached keys are evicted and refreshed on rotation via rewrap strategy",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-13",
        "questionId": "q-29106",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant HTTP basic auth using Traefik dynamic configuration loaded from a Docker Config named tenant-auth, with a separate htpasswd file per tenant. Route tenantA and tenantB to the same API but under different hostnames (tenantA.api.example.org, tenantB.api.example.org). Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant rate limiting with Redis-backed counters: a lightweight middleware reads quota from rate-config (tenant→limit) via X-Tenant, increments a Redis key per tenant with a 60s window, a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage the NVIDIA runtime with a per-tenant quota allocator sidecar that retrieves GPU tokens from a Vault-backed central policy service and configures CUDA_VISIBLE_DEVICES accordingly. A device plugin enforces quota…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure Traefik with TLS termination and two routers, tenantA.api.example.org and tenantB.api.example.org, pointing to the same API. Load per-tenant credentials from a Docker Config named tenant-aut",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a DLP sidecar that validates outbound JSON against a tenant-specific schema pulled from Vault at /secret/tenants/{tenant}/schema. Identify tenants by X-Tenant header; redact or block non-com",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to wire multi-tenant routing with TLS, Traefik, and Docker Configs across two DCs while using a simple, industry-standard auth mechanism.\n\n## Key Concepts\n- Traefik TLS edge routing\n- Docker Configs for tenant credentials\n- Per-tenant htpasswd-based basic auth\n- Dynamic routing by host and tenant isolation\n- Minimal docker-compose.yml across DCs\n\n## Code Example\n```yaml\n# docker-compose.yml skeleton\nversion: '3.9'\nservices:\n  traefik:\n    image: traefik:v2.12\n    ports:\n      - \"80:80\"\n      - \"443:443\"\n    volumes:\n      - ./dynamic:/dynamic\n    command:\n      - --providers.file.directory=/dynamic\n      - --entrypoints.websecure.address=:443\n  api:\n    image: yourorg/api:latest\n    deploy:\n      replicas: 2\n```\n\n```yaml\n# dynamic tenant-auth.yaml (simplified)\nhttp:\n  routers:\n    tenantA:\n      rule: Host(`tenantA.api.example.org`)\n      service: api\n      middlewares: [tenantA-auth]\n    tenantB:\n      rule: Host(`tenantB.api.example.org`)\n      service: api\n      middlewares: [tenantB-auth]\n  middlewares:\n    tenantA-auth:\n      basicAuth:\n        usersFile: /config/tenant-auth/tenantA.htpasswd\n    tenantB-auth:\n      basicAuth:\n        usersFile: /config/tenant-auth/tenantB.htpasswd\n```\n\n## Follow-up Questions\n- How would you rotate tenant credentials without restarting Traefik? \n- What testing steps would verify tenant isolation and TLS correctness across DCs?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-14",
        "questionId": "q-28040",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant canary rollouts: route a configurable percentage of TenantID requests to API v2 while the rest go to v1; store per-tenant rollout quotas in a Docker Config named rollout-config (JSON mapping tenant->canaryPct) and ensure updates propagate without restarts. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two API versions v1 and v2 behind Traefik in two-DC docker-dca. Rollout-config.json in a Docker Config maps tenant to canary%.\\nA lightweight canary-decider reads this per-tenant map and routes to v2",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Fetch tenant flags from per-tenant Docker Configs named feature-flags-<tenant>.json using X-Tenant in requests, expose a small in-memory cache with TTL, and fallback to global defaults if missing. Boo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe issuing tenant-scoped short-lived client certificates from Vault PKI, configuring Traefik with mTLS and a Vault-backed certresolver, wiring per-tenant policies via Docker Config, and propagat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Redis-backed per-tenant semaphore (tenant:<id>) and a Traefik middleware that acquires/releases permits. Forward if acquired; otherwise return 429 and optionally route to a canary backend.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to implement per-tenant canary rollouts with dynamic config in a multi-DC Docker flow.\n\n## Key Concepts\n- Traefik dynamic routing and per-tenant weights\n- Docker Config for tenant-tier rollout policy\n- Canary logic with stateless decider and no restarts\n\n## Code Example\n```javascript\n// Pseudo: canary-decider reads rollout-config.json and returns targetVersion=v1|v2\n```\n\n## Follow-up Questions\n- How would you roll back a failed canary without affecting tenants? \n- How would you monitor per-tenant canary metrics and alerting?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-15",
        "questionId": "q-25092",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement tenant-scoped rate limiting via a central RateLimit service backed by Redis, with per-tenant quotas provided through a Docker Config and enforced by a Traefik middleware that can hot-reload policy without restarts. Validate by simulating traffic bursts from multiple tenants and proving isolation. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a GPU-enabled TorchServe deployment on a 3-node Swarm across 2 DCs. Use NVIDIA runtime and a service with GPU constraint (nvidia.com/gpu=1). Implement canary upgrades: deploy 1 new replica, ru",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-tenant canary controller that writes status to Redis (key: canary:tenant:<id>, fields enabled, percentage). Traefik’s dynamic config is updated by a small sidecar reading Redis and rou",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe the end-to-end flow: a sidecar reads X-Tenant, consults a Redis-based per-tenant token bucket (Lua script for atomic grant/deny), and trips a circuit-breaker to route to a degraded path when",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Configure Traefik rateLimit middleware with Redis as the store, quotas loaded from a Docker Config rate-config.json keyed by X-Tenant. Attach X-Tenant to all requests and enable policy hot-reload via",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design per-tenant QoS with dynamic config in a multi-DC setup.\n\n## Key Concepts\n- Traefik RateLimit middleware, Redis store, per-tenant quotas, Docker Config\n- TLS termination, hot-reload without restart, cross-DC coordination\n- Request tagging with X-Tenant and validation of isolation\n\n## Code Example\n```javascript\n// Pseudo middleware sketch: extract tenant and enforce quota from Redis\nasync function enforceTenantRate(req, res, next){\n  const tenant = req.headers['x-tenant'];\n  const quota = await redisGetQuota(tenant);\n  if (quota.allow(req)) return next();\n  res.status(429).send('Too Many Requests');\n}\n```\n\n## Follow-up Questions\n- How would you audit cross-DC quota drift and recover from Redis replication lag?\n- How would you test hot-reload behavior under config churn?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-16",
        "questionId": "q-12262",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster. Implement a per-tenant feature-flag system as a sidecar that reads flags from a Docker Config, caches in-memory with a 60s TTL, and routes requests to either /v1 or /v2 based on the flag. Include bootstrap commands and a minimal docker-compose.yml skeleton; ensure graceful fallback if the flag store is unavailable?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instrument the API to emit structured JSON logs including tenant_id extracted from request headers like X-Tenant. Deploy a lightweight sidecar log collector that tails API logs, redacts PII data, writes per-tenant log f…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would deploy a Traefik edge and a tenant-aware API behind TLS. Store per-tenant flags in a Docker Config named tenant-flags (JSON). Bootstrap flags for alpha/beta with a script and bootstrap manifes",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy a three-node Docker Swarm cluster with 2 manager nodes in DC1 and 1 manager node in DC2 using node labels for DC-aware placement. Configure Envoy edge proxies with WASM filters for per-tenant rate limiting and he…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a sidecar that reads per-tenant flags from a Docker Config, caches in-memory with a 60s TTL, and writes a small routing hint to a shared volume consumed by Nginx to route to /v1 or /v2. Bootstrap",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a beginner-friendly yet realistic multi-DC deployment with TLS, an edge proxy, and a tenant-aware feature-flag system using the sidecar pattern. It emphasizes resilience when a config store is unavailable and practical bootstrap steps.\n\n## Key Concepts\n- Docker Configs for per-tenant settings\n- Sidecar pattern for feature flags\n- Nginx dynamic routing to API versions\n- In-memory cache with TTL\n- Fault-tolerance and graceful fallback\n\n## Code Example\n```yaml\n# docker-compose.yml (skeleton)\nversion: '3.8'\nservices:\n  edge:\n    image: nginx:latest\n  apiV1:\n    image: myapi:v1\n  apiV2:\n    image: myapi:v2\n  sidecar-flag:\n    image: flag-reader:latest\n```\n\n## Follow-up Questions\n- How would you test TTL eviction and flag refresh in a CI pipeline?\n- How would you handle a tenant migration where a flag suddenly flips mid-request?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-17",
        "questionId": "q-12372",
        "question": "In a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy. Add a provenance guard sidecar that uses Cosign to validate the running image against a per-tenant allowlist fetched from a central policy broker; Vault issues per-tenant signing keys and SPIFFE SVID rotation; enable a 20% canary rollout with SLA latency targets and automatic rollback. Provide exact bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Store a Docker Secret named tenant-allow mounted into the API container, parse at startup, and cache in memory. API reads X-Tenant, validates against the secret, and serves /data only if allowed; othe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe per-tenant token-bucket rate limiting in a two-DC docker-dca deployment behind Traefik. A sidecar enforces a Redis-backed token bucket per tenant (namespace per X-Tenant), with quotas updated",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant image provenance guard as a sidecar that uses Cosign to verify the running image against a Vault-backed per-tenant digest list from a central policy broker, with automatic SPIFFE SVID rotation for…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Deploy a TLS-enabled API behind Traefik in a Docker DCA cluster across two data centers. Implement per-tenant rate limiting using a Redis-backed token bucket: a Redis counter per tenant with a 60-second window. Tenant I…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical integration of image provenance, per-tenant isolation, and production-safe canaries in docker-dca.\n\n## Key Concepts\n- Cosign image signing/verification\n- Vault per-tenant keys and SPIFFE SVID rotation\n- Central policy broker for per-tenant allowlists\n- Canary deployments with SLA-based rollback\n\n## Code Example\n```javascript\nasync function verifyRunningImage(tenantId, digest) {\n  const allow = await fetchAllowlist(tenantId); // mTLS\n  if (!allow.includes(digest)) throw new Error('Untrusted image');\n  await cosignVerify(digest, vaultKeyForTenant(tenantId));\n}",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-18",
        "questionId": "q-8769",
        "question": "Across two DCs in a docker-dca cluster, implement strict per-tenant data isolation by routing all DB queries through a policy-enabled proxy that rewrites queries and enforces row-level security, with centralized audit logging; describe architecture, data models, and validation strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a per-tenant isolation path using Envoy as a policy-enabled proxy, a lightweight query-rewrite sidecar, and RFC-compliant tenant_id enforcement in Postgres (ROW/CLAUSE level). Emit structure",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create two service versions (stable and canary) behind Traefik TLS. Add a Docker Config canary-tenant-map that maps X-Tenant values to 'stable' or 'canary'. Route with Traefik rule on Headers 'X-Tenan",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lightweight Go sidecar that caches GET responses per tenant using an in-memory map; TTL 60s; cache key = tenant|path|query; on 5xx, invalidate and bypass; edge uses X-Tenant header for rou",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe a two-DC deployment where a TLS API behind Traefik uses Vault for dynamic per-tenant DB credentials. The API authenticates with AppRole, fetches tenant creds with a short lease, renews proact",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design policy-driven isolation across DCs, with practical use of proxy, policy engine, query rewrite, and audit.\n\n## Key Concepts\n\n- Policy as code (OPA/REGO)\n- Per-tenant data isolation (row-level security or tenant_id)\n- Query rewriting sidecar\n- Centralized audit/logging (OTel, Elastic)\n- Cross-DC latency implications and consistency\n\n## Code Example\n\n```javascript\n// OPA policy (conceptual)\npackage dca.tenant\ndefault allow = false\nallow {\n  input.tenant_id == input.resource.tenant_id\n}\n```\n\n## Follow-up Questions\n\n- How would you test isolation with adversarial queries?\n- How to handle schema changes affecting RLS or tenant_id mappings?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-19",
        "questionId": "q-17690",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Introduce per-tenant logging configuration stored in a Docker Config named tenant-logging that maps tenant IDs to log levels (debug/info/warn/error). The API reads X-Tenant to apply the appropriate level when emitting logs to stdout, with a lightweight log-collector container aggregating logs per tenant to a central endpoint. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a 4-node Swarm across two DCs, with a 10% canary of the worker upgraded to the new proto-format, while 90% stay on the legacy image. Deploy update_config: order: start-first; parallelism: 1. Route",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a tenant-scoped feature flag gate using a sidecar container that reads the X-Tenant header from incoming requests, consults a Docker Config (tenant_flags.json mapping tenant_id to a boolean), and hashes the te…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a Node.js API image with a Dockerfile, and a start script wait-for-redis.sh that pings redis:6379 until ready, then runs node index.js. In docker-compose.yml, define services api and redis on a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant log levels via a Docker Config tenant-logging that maps tenant IDs to levels (e.g., info, debug). API reads X-Tenant, applies the configured level to the logger at runtime (e.g.,",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to read per-tenant config from Docker Configs and apply runtime behavior in a simple TLS/gateway setup.\n\n## Key Concepts\n- Docker Configs for tenant-specific config\n- Per-tenant log level mapping\n- Traefik TLS termination and middleware routing\n- Light-weight log collection pipeline\n\n## Code Example\n```javascript\n// Pseudocode: load tenant config, set logger level accordingly\nconst lvl = tenantConfig[X_TENANT] ?? 'info';\nlogger.setLevel(lvl);\n```\n\n## Follow-up Questions\n- How would you handle missing tenant config gracefully?\n- How would you test log level changes without redeploying?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-20",
        "questionId": "q-20234",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant request concurrency quotas enforced at the edge using a Redis-backed semaphore with per-tenant namespaces; when a tenant exceeds its quota, route extra requests to a degraded canary backend and return 429 for the rest; include bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Vault Transit to issue per-tenant signing keys; Nginx edge terminates TLS and signs responses; a sidecar signs logs and streams to a WORM store with Merkle proofs; enable automatic key rotation an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a solution where Nginx terminates TLS at the edge, Envoy enforces per-tenant routing to local region databases, and a sidecar calls a policy service to authorize requests. Vault Transit perf",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Redis-backed per-tenant semaphore (tenant:<id>) and a Traefik middleware that acquires/releases permits. Forward if acquired; otherwise return 429 and optionally route to a canary backend.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Instrument the API to emit structured JSON logs including tenant_id extracted from request headers like X-Tenant. Deploy a lightweight sidecar log collector that tails API logs, redacts PII data, writes per-tenant log f…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests edge-traffic control, cross-DC consistency, and per-tenant quota design at the proxy layer with bootstrapability.\n\n## Key Concepts\n- Per-tenant concurrency control\n- Redis-backed semaphores\n- Traefik edge routing and dynamic config\n- Canary/backends and 429 handling\n- Docker Config for quotas\n\n## Code Example\n```yaml\n# quotas.yaml (example)\nversion: \"3.8\"\nservices:\n  quotas:\n    image: alpine:latest\n    command: \"/bin/sh -c 'echo quotas'\"\n```\n\n```go\n// sampleAcquire.go\npackage main\nfunc acquire(tenant string) bool { /* acquire permit from Redis */ }\n```\n\n## Follow-up Questions\n- How would you handle quota drift during DC partition?\n- What tests validate correctness under burst traffic?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-21",
        "questionId": "q-3933",
        "question": "In a two-node docker-dca Swarm, deploy a TLS-enabled REST API behind Traefik as the edge proxy. Configure Traefik's ACME to obtain TLS certificates from Let's Encrypt in staging. Attach all services to a single overlay network. Implement a 15% canary rollout of a new /status/v2 endpoint (v1 remains) using Traefik's weighted routing with health checks, automatic rollback on failures. Add a minimal OpenTelemetry tracing sidecar exporting to stdout; ensure trace headers propagate?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set up a two-node docker-dca Swarm with Traefik as the edge proxy, TLS via Let’s Encrypt staging, and a single overlay network. Deploy api-v1 and a canary api-v2; steer 15% traffic to v2 using Traefik",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Envoy as a per-tenant rate limiter sidecar tied to the API service. Validate JWT via Vault OIDC, fetch quotas from Vault KV on cache miss, store per-tenant state in Redis with short TTL, and enfor",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument the API with OpenTelemetry and route traces to a per-datacenter OTEL Collector with a Jaeger backend. Attach X-Tenant as a trace attribute via middleware and implement per-tenant sampling through a Docker Con…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant log levels via a Docker Config tenant-logging that maps tenant IDs to levels (e.g., info, debug). API reads X-Tenant, applies the configured level to the logger at runtime (e.g.,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to configure a lightweight canary in a real swarm, using Traefik as a modern edge proxy, and integrate observability with OpenTelemetry. It checks practical networking, TLS setup, and traffic shaping without requiring complex multi-cluster setups.\n\n## Key Concepts\n\n- Docker Swarm overlay networks and service deployment\n- Traefik ACME TLS with staging\n- Weighted canary routing and health-check-based rollback\n- Lightweight OpenTelemetry tracing (export to stdout) and header propagation\n\n## Code Example\n\n```javascript\n// Example: simple trace header propagation (conceptual)\nconst http = require('http');\nhttp.get({ hostname: 'api.example', path: '/status', headers: { 'Trace-Id': 'abc123' } }, (res) => {\n  // handle response\n});\n```\n\n## Follow-up Questions\n\n- How would you monitor the canary rollout with metrics and logs?\n- How to handle TLS certificate rotation during canary without downtime?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-22",
        "questionId": "q-17611",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant A/B routing: tenants are mapped in a Docker Config tenant-backends.json to either backend-v1 or backend-v2. Traefik should dynamically apply routing without container redeploys as the config updates. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy Calico NetworkPolicy to segment tenants by namespace labels within the docker-dca cluster. Expose TLS via Traefik with mutual TLS (mTLS) termination, store per-tenant policies in Consul KV, and trigger automatic…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a tenant-scoped audit path: a sidecar publishes per-tenant events to Kafka with idempotency keys to guard against duplicates. Ensure at-least-once delivery, dedupe in the consumer for exact-",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set up two backends (primary and canary) behind Traefik TLS. Use a file-based dynamic config sourced from Docker Config canary-config.json that maps each X-Tenant to canary: true/false. Traefik router",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bootstrap two backends (backend-v1, backend-v2) and a Traefik edge with TLS. Maintain a Docker Config tenant-backends.json that maps each tenant_id to v1 or v2. A lightweight bootstrap script generate",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a resilient, multi-DC TLS deployment with dynamic per-tenant routing and zero-downtime config updates.\n\n## Key Concepts\n- Traefik FileProvider dynamic config\n- Docker Configs for runtime data\n- Dynamic per-tenant routing without redeploys\n- Simple A/B routing using tenant-to-version mapping\n\n## Code Example\n```bash\n#!/bin/bash\n# bootstrap per-tenant routers from tenant-backends.json\n# (pseudo-implementation to illustrate flow)\nEOF\n```\n\n## Follow-up Questions\n- How would you validate per-tenant routing during rollout and roll back safely?\n- What observability would you add to verify tenant traffic splits and detect misrouting?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-23",
        "questionId": "q-9187",
        "question": "In a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy. Add a dynamic per-tenant eviction and migration feature: a tenant-migration sidecar watches a central tenant-mapping service and, on command, migrates a tenant from DC-A to DC-B by updating tenant-scoped overlays, draining in-flight requests, and rotating TLS certs with Vault SPIFFE SVIDs. Provide exact bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant tracing isolation using OpenTelemetry across two data centers behind a TLS-terminated edge. Add a tenant-scoped sampling sidecar that reads per-tenant rates from Consul KV and tag",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Traefik's dynamic config via a file provider loaded from the shadow-tenants Docker Config. Create a router for the API with a mirror to the shadow service, conditioned by a header check X-Ten",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a tenant-migration workflow with a dedicated sidecar that subscribes to the tenant-mapping service, updates the tenant overlay network, performs graceful drain, triggers Vault SPIFFE SVID ro",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Instrument the API with OpenTelemetry, add HTTP trace context propagation, and export to Jaeger via OTLP. Use a Docker Config named tenant-tracing to configure per-tenant sampling rates (e.g., alpha=0.3, beta=0.7). Depl…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests dynamic tenant mobility, overlay routing, and zero-downtime TLS rotation in multi-DC.\n\n## Key Concepts\n- dynamic tenant migration across DCs\n- per-tenant overlay updates\n- graceful drain and cutover\n- Vault SPIFFE SVID rotation\n- edge-sidecar verification and audit\n\n## Code Example\n```yaml\n# skeleton docker-compose excerpt\nversion: '3.8'\nservices:\n  edge:\n    image: nginx:stable\n  tenant-migration:\n    image: docker/dca-migration\n```\n\n## Follow-up Questions\n- How would you test migrations in staging with rollback?\n- How ensure idempotent migrations and data residency guarantees?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-24",
        "questionId": "q-30174",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement a per-tenant token-bucket rate limiter using Redis with keys derived from X-Tenant and a Lua script for atomic token fetch/consume; expose an admin endpoint to adjust caps without redeploy. Bootstrap initial quotas via a Docker Config and load into both DCs. Ensure cross-DC routing with zero-downtime quota updates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant token-bucket rate limiter using Redis with keys derived from X-Tenant. Use a Lua script for atomic token fetch/consume; expose an admin endpoint to adjust caps without redeploy.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Configure Traefik with TLS termination and two routers, tenantA.api.example.org and tenantB.api.example.org, pointing to the same API. Load per-tenant credentials from a Docker Config named tenant-aut",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure Traefik with two TLS routers using HostSNI rules for tenantA.example.com and tenantB.example.com, each routing to the same backend API service. Set up a mock ACME server to automate per-tenant certificate issu…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a lightweight per-tenant policy engine in the API that loads policies.json from a Docker Config, extracts the tenant from the X-Tenant header, validates the request path against the allowedPaths for that tenan…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world QoS control across DCs, using per-tenant keys, Redis Lua scripting for atomicity, and zero-downtime quota updates via Docker Config.\n\n## Key Concepts\n- Token bucket rate limiting per tenant\n- Redis Lua scripting for atomic operations\n- Docker Config bootstrap for quotas\n- Traefik TLS termination and multi-DC routing\n- Live quota adjustments without redeploys\n\n## Code Example\n```lua\n-- Redis Lua script (simplified) to atomically fetch/consume a token\nlocal key = KEYS[1]\nlocal rate = tonumber(ARGV[1])\nlocal burst = tonumber(ARGV[2])\nlocal now = tonumber(ARGV[3])\nlocal tokens = tonumber(redis.call('GET', key) or burst)\nif tokens < 1 then\n  return -1\nend\nredis.call('DECR', key)\nredis.call('EXPIREAT', key, now + rate)\nreturn tokens - 1\n```\n\n## Follow-up Questions\n- How would you handle clock skew across DCs?\n- What observability would you add for per-tenant quotas and violations?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-16T10:34:13.453Z",
    "version": 1
  }
]