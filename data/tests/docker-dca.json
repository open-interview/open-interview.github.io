[
  {
    "id": "test-docker-dca",
    "channelId": "docker-dca",
    "channelName": "Docker DCA",
    "title": "Docker DCA Knowledge Test",
    "description": "Test your understanding of Docker DCA with 25 questions.",
    "questions": [
      {
        "id": "docker-dca-q-0",
        "questionId": "q-26474",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Add per-tenant distributed tracing with OpenTelemetry: instrument the API to emit traces that include X-Tenant, configure an OpenTelemetry Collector to receive traces and export to Jaeger, and provide a minimal docker-compose.yml skeleton and bootstrap commands to run the collector and Jaeger locally for validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Solution Overview Deploy a TLS-protected API behind Traefik with per-tenant distributed tracing via OpenTelemetry to Jaeger. ### 1. Project Structure ``` project/ ├── docker-compose.yml ├── otel-collector-config.yaml…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-tenant health checks behind Traefik: a lightweight readiness sidecar uses X-Tenant to query tenant_primary vs tenant_backup endpoints defined in Docker Config tenant_dependency_map (tenant_id -> p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure a small policy layer in the API to filter responses by tenant. Mount Docker Config tenant-schemas.json at /config/tenant-schemas.json, parse into map[string][]string, and on each request rea",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement tenant-scoped data encryption using Vault Transit with per-tenant data keys that rotate every 24 hours. Deploy a sidecar that fetches the appropriate data key based on the tenant-id HTTP header, caches it with…",
            "isCorrect": false
          }
        ],
        "explanation": "## Architecture Overview\n\n```\n┌────────────────────────────────────────────────────────────────────┐\n│                        Data Center 1 & 2                          │\n├────────────────────────────────────────────────────────────────────┤\n│                                                                    │\n│  ┌──────────┐   TLS/HTTPS    ┌─────────┐   HTTP    ┌─────────┐   │\n│  │  Client  │ ─────────────► │ Traefik │ ─────────► │   API   │   │\n│  │          │   :443         │  Edge   │   :3000    │ Tenant  │   │\n│  └──────────┘                │ Proxy   │            │ Service │   │\n│                               └─────────┘            └────┬────┘   │\n│                                  │                       │       │\n└──────────────────────────────────┼───────────────────────┼───────┘\n                                   │                       │\n                                   │              OTLP :4317\n                                   │                       │\n                                   ▼                       ▼\n                           ┌─────────────────────────────────────┐\n                           │      OpenTelemetry Collector        │\n                           │  ┌─────────┐  ┌──────────────────┐  │\n                           │  │  OTLP   │  │    Processors    │  │\n                           │  │Receiver │──│  batch/memory    │  │\n                           │  └─────────┘  └────────┬─────────┘  │\n                           └─────────────────────────┼───────────┘\n                                                     │\n                                   ┌─────────────────┼───────────┐\n                                   │                 │           │\n                                   ▼                 ▼           │\n                           ┌─────────────┐   ┌──────────────────┐ │\n                           │   Jaeger    │   │  Loki/Prometheus │ │\n                           │  Storage    │   │  (optional)      │ │\n                           │  :16686 UI  │   └──────────────────┘ │\n                           └─────────────┘                         │\n                                                                    │\n                           Legend:                                  │\n                           ─── HTTP/TLS Traffic                     │\n                           === OTLP Trace Export                   │\n```\n\n## Key Implementation Details\n\n| Component | Purpose | Key Config |\n|-----------|---------|------------|\n| **Traefik** | TLS termination, routing | File-based certs, label-based service discovery |\n| **API** | Business logic, tenant extraction | OTel SDK auto-instrumentation, manual span attributes |\n| **OTel Collector** | Trace aggregation, processing | OTLP receiver, batch processor, Jaeger exporter |\n| **Jaeger** | Trace storage, visualization | In-memory storage for local dev, query API on :16686 |\n\n## Per-Tenant Tracing Flow\n\n1. Client sends request with `X-Tenant: <tenant-id>` header\n2. Traefik terminates TLS, routes to API service\n3. API middleware extracts `X-Tenant`, adds to OpenTelemetry span as `x-tenant` attribute\n4. OTel SDK exports trace via OTLP to Collector\n5. Collector batches and forwards to Jaeger\n6. Jaeger stores trace with tenant attribute for filtering\n\n## Multi-Tenant Filtering in Jaeger\n\n```\nSearch traces by:\n- service.name = tenant-api\n- x-tenant = acme-corp\n- x-tenant = globex-inc\n```\n\n## Validation Checklist\n\n- [ ] All containers running (`docker-compose ps`)\n- [ ] TLS certs present in `certs/` directory\n- [ ] Jaeger UI accessible at http://localhost:16686\n- [ ] API responds to health check\n- [ ] API traces appear in Jaeger with `x-tenant` attribute\n- [ ] Different tenants show separate trace views\n- [ ] OTel Collector metrics available at :8888",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-1",
        "questionId": "q-7662",
        "question": "In a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind an edge proxy. Implement image provenance with Sigstore cosign attestations verified by a dedicated edge verifier and per-node sidecar; route tenants to tenant-scoped service groups on a shared overlay and gate access with tenant-specific OPA policies loaded from a Docker Config. Provide exact bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Sigstore cosign to sign images; deploy a verifier sidecar on each node and an edge-verifier at the gateway. Enable mTLS via SPIFFE/SVIDs and rotate keys with Vault or a similar CA. Isolate tenants",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Run: `docker swarm init`; `docker network create -d overlay appnet`; `docker secret create tls.pem cert.pem`; `docker secret create tls.key key.pem`; `docker config create api-endpoints /path/api.json`; `docker stack de…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Sign all base images with cosign and verify in a dedicated 'sigver' sidecar before the app runs. Use Vault to issue SPIFFE SVIDs for services, and enforce an OPA policy loaded via Docker Config to gat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument the API with OpenTelemetry, deploy a sidecar collector per replica, propagate W3C trace context across services, and export traces via OTLP to Jaeger. Configure sampling through Docker configs, ensure cross-d…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests deep understanding of runtime image provenance, edge verification, and tenant isolation in a multi‑DC docker-dca setup. Evaluates how to integrate Sigstore attestations, per‑tenant routing, and policy enforcement with operational bootstrap reproducibility.\n\n## Key Concepts\n\n- Sigstore cosign attestations for image provenance\n- Edge and node‑level verifiers with mTLS\n- Tenant-scoped routing on a shared overlay\n- OPA policies loaded via Docker Config\n- Bootstrap reproducibility with docker-compose\n\n## Code Example\n\n```bash\n# sign and verify images with cosign\ncosign sign --key cosign.key repo/app:tag\ncosign verify --certificate-identity-regexp 'edge-registry.*' repo/app:tag\n```\n\n## Follow-up Questions\n\n- How would you handle cross‑DC key rotation without downtime?\n- How would you test provenance enforcement under a canary rollout?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-2",
        "questionId": "q-2016",
        "question": "In a three-node Docker Swarm spanning two data centers, deploy a stateless API behind a Traefik ingress with a Redis-backed cache layer and mutual TLS between services. Use Docker secrets/configs for TLS certs and cache creds. Apply zero-downtime upgrades via update_config (start-first, parallelism 1) and implement a canary upgrade path with health checks and automatic rollback. Outline the exact init/join commands, overlay creation, stack file, and upgrade sequence?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would front the two API versions with TLS-terminated Nginx. A central policy engine (OPA) evaluates per-tenant flags and returns allowed routes. Requests with the flag go to api-v2; others go to api",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Initialize a three-node Swarm across two DCs, join the remaining nodes with generated tokens, create an overlay network, and deploy a Traefik-based API with a Redis cache layer. Use Docker secrets for",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Deploy a TLS-enabled API behind Nginx with Fluent Bit sidecar for log forwarding to Elasticsearch across two data centers. ## Bootstrap Commands ```bash # Create Docker networks for multi-container communication docker…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy a three-node Docker Swarm cluster across two data centers with TLS-terminated API traffic behind an Nginx edge proxy. Utilize HashiCorp Vault to issue SPIFFE SVIDs for workload identity, and attach a Verifier sid…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests orchestration across DCs, zero-downtime upgrades, and secure service-to-service traffic with mTLS. It also probes secret/config management, canary rollout, and rollback handling.\n\n## Key Concepts\n- Swarm multi-DC deployment and node join tokens\n- Overlay networks and Traefik ingress with mTLS\n- Docker secrets/configs for TLS and credentials\n- update_config with start_first and parallelism, canary deployments\n\n## Code Example\n```yaml\n# docker-stack.yml (excerpt)\nversion: '3.8'\nservices:\n  api:\n    image: myapi:v2\n    deploy:\n      replicas: 3\n      update_config:\n        parallelism: 1\n        order: start-first\n    secrets:\n      - redis-creds\n      - tls-cert\n    networks:\n      - app-net\n  cache:\n    image: redis:6\n    deploy:\n      replicas: 3\n    secrets:\n      - redis-creds\n    networks:\n      - app-net\nnetworks:\n  app-net:\n    external: true\nsecrets:\n  tls-cert:\n    external: true\n  redis-creds:\n    external: true\n```\n\n## Follow-up Questions\n- How would you implement automatic rollback on canary failure?\n- How do you measure canary success beyond HTTP 200s (latency, error rate)?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-3",
        "questionId": "q-26114",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Add tenant-aware observability by shipping per-tenant traces with an OpenTelemetry Collector sidecar per API container that exports via OTLP to a central Jaeger backend; derive per-tenant attributes from X-Tenant and a Docker Config named otel-config, enabling per-tenant pipelines and sampling. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a tiny Python sidecar 'audit-shipper.py' that tails /var/log/api/access.jsonl (produced by the API) in real-time and writes per-tenant JSON entries to /audit/logs/audit.jsonl. Each line include",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Attach an OpenTelemetry Collector as a per-tenant sidecar to the API containers, wiring traces via OTLP to a central Jaeger backend. Propagate X-Tenant into span attributes, and configure a tenant-sco",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Explain how you would implement tenant-specific A/B routing with Traefik and a Redis-backed Experiment service, enabling per-tenant config updates without redeploys, including canary thresholds, healt",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement Vault envelope encryption with per-tenant data keys: Vault generates DEKs wrapped by KEKs for each tenant, a data-crypto sidecar fetches and decrypts payloads at the API boundary with 24-hour rotation and zero…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical tenant-scoped observability in a Docker Data Center Architecture with cross-DC traffic. It covers sidecar augmentation, OTLP export, and per-tenant configuration without downtime.\n\n## Key Concepts\n- Docker Config for per-tenant OTEL settings\n- OpenTelemetry Collector as sidecar per tenant\n- OTLP export to Jaeger; X-Tenant propagation\n- Traefik TLS termination and cross-DC routing\n\n## Code Example\n```yaml\n# otel-collector-config.yaml (per-tenant)\nreceivers:\n  otlp:\n    protocols:\n      grpc: {}\nexporters:\n  jaeger:\n    endpoint: jaeger-collector:4317\nservice:\n  pipelines:\n    traces:\n      receivers: [otlp]\n      exporters: [jaeger]\n```\n\n## Follow-up Questions\n- How would you adjust sampling per tenant to protect hot tenants?\n- How would you validate that per-tenant attributes are consistently propagated across DCs?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-4",
        "questionId": "q-21605",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant rate limiting with a distributed token bucket stored in Redis, driven by an in-cluster sidecar that uses the X-Tenant header to fetch and consume tokens. Add per-tenant circuit-breaker rules and a degraded path when limits are exceeded. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy a TLS-enabled API behind an Nginx edge proxy in a Docker cluster across two data centers. Implement a zero-trust mTLS mesh using SPIFFE/SPIRE with automatic certificate rotation and SPIFFE-ID propagation to servi…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe the end-to-end flow: a sidecar reads X-Tenant, consults a Redis-based per-tenant token bucket (Lua script for atomic grant/deny), and trips a circuit-breaker to route to a degraded path when",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Sigstore cosign to sign images; deploy a verifier sidecar on each node and an edge-verifier at the gateway. Enable mTLS via SPIFFE/SVIDs and rotate keys with Vault or a similar CA. Isolate tenants",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a tenant-scoped feature flag gate using a sidecar container that reads the X-Tenant header from incoming requests, consults a Docker Config (tenant_flags.json mapping tenant_id to a boolean), and hashes the te…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design distributed per-tenant rate limiting and failure handling across DCs, including proxy integration, sidecar vs. proxy extension, and degraded path strategy.\n\n## Key Concepts\n- Per-tenant token bucket in Redis with atomic Lua script\n- Sidecar enforcement using X-Tenant header\n- Circuit-breaker logic per tenant with a degraded path\n- TLS termination at Traefik and cross-DC traffic routing\n- Bootstrap and minimal docker-compose skeleton for two DCs\n\n## Code Example\n```lua\n-- Redis Lua token bucket (per-tenant)\nlocal key = KEYS[1]\nlocal now = tonumber(ARGV[1])\nlocal rate = tonumber(ARGV[2])\nlocal cap = tonumber(ARGV[3])\nlocal tokens = tonumber(redis.call('GET', key) or cap)\nlocal new_tokens = math.min(cap, tokens + rate - 1)\nif new_tokens < 0 then\n  return 0\nelse\n  redis.call('SET', key, new_tokens, 'PX', 60000)\n  return 1\nend\n```\n\n## Follow-up Questions\n- How would you scale Redis for tenant-level state across DCs and handle failover?\n- How would you test race conditions and ensure accurate per-tenant quotas under bursty traffic?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-5",
        "questionId": "q-20557",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Introduce OpenTelemetry tracing with a Jaeger backend replicated across DCs. Trace must include X-Tenant header as tenant attribute; enable per-tenant sampling via a Docker Config named tenant-sampling (e.g., 0.1 for low-volume tenants, 1.0 for critical tenants). Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Configure Traefik with two TLS routers using HostSNI rules for tenantA.example.com and tenantB.example.com, each routing to the same backend API service. Set up a mock ACME server to automate per-tenant certificate issu…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement mTLS: run a lightweight internal CA container to issue per-tenant client certs; configure Nginx to require and verify client certs against the CA; API reads the certificate's CommonName to d",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant log levels via a Docker Config tenant-logging that maps tenant IDs to levels (e.g., info, debug). API reads X-Tenant, applies the configured level to the logger at runtime (e.g.,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument the API with OpenTelemetry, exporting traces to a Jaeger backend replicated across both data centers. Configure per-tenant sampling policies loaded from a Docker Config named tenant-sampling (e.g., {tenantA:…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question validates the ability to implement observability in a Docker DCA (Docker Certified Associate) multi-data center environment. It tests practical knowledge of wiring OpenTelemetry instrumentation into an API, configuring Jaeger as a distributed tracing backend, and coordinating per-tenant sampling through Docker Configs—all while operating behind a TLS-terminating edge proxy.\n\n## Key Concepts\n\n- OpenTelemetry SDK integration within a service API\n- Jaeger as a distributed tracing backend with cross-DC replication\n- Docker Config for centralized, runtime-based parameter management\n- Per-tenant sampling strategies for cost-effective observability\n- X-Tenant header propagation for multi-tenant trace correlation\n\n## Minimal docker-compose.yml Skeleton\n\n```yaml\nservices:\n  traefik:\n    image: traefik:v3.0\n    command:\n      - --providers.docker\n      - --providers.docker.swarmMode\n      - --entrypoints.websecure Address::443\n      - --entrypoints.websecure.tls\n    ports:\n      - \"443:443\"\n    volumes:\n      - /var/run/docker.sock:/var/run/docker.sock\n\n  api:\n    image: your-api:latest\n    deploy:\n      replicas: 2\n    environment:\n      - OTEL_EXPORTER_JAEGER_ENDPOINT=jaeger:14250\n      - OTEL_SERVICE_NAME=api-service\n    configs:\n      - source: tenant-sampling\n        target: /etc/sampling.json\n\n  jaeger:\n    image: jaegertracing/all-in-one:1.50\n    environment:\n      - COLLECTOR_REPLICATION_FACTOR=2\n    ports:\n      - \"16686:16686\"\n\nconfigs:\n  tenant-sampling:\n    external: true\n```",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-6",
        "questionId": "q-24128",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement tenant-aware rate limiting: Redis-backed per-tenant namespaces with per-tenant limits in a Docker Config named rate-config.json; enforce via a lightweight limiter middleware behind Traefik; use Redis Pub/Sub to broadcast abuse events for cross-DC revocation; add a per-tenant circuit-breaker that trips after N 429s. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instrument the API with OpenTelemetry and route traces to a per-datacenter OTEL Collector with a Jaeger backend. Attach X-Tenant as a trace attribute via middleware and implement per-tenant sampling through a Docker Con…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a per-tenant feature-flag store in Docker Config (tenantId → feature → bool) plus an in-process per-tenant LRU cache with 2–5 minute TTL in the API. A lightweight Flag/Policy service reads the Docker Config, pu…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure a Redis-backed per-tenant rate limiter integrated with Traefik middleware; per-tenant limits loaded from rate-config.json Docker Config; abuse events published via Redis Pub/Sub trigger cros",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-tenant circuit breaker with a Redis-backed state (tenant:<id>:cb) and a Traefik middleware that 503s when open. Seed tenants from Docker Config tenant-registry, monitor latency and err",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design cross-DC rate limiting with tenant isolation and fast failover. It checks integration between Traefik, Redis-based state, and a circuit-breaker pattern under operational constraints.\n\n## Key Concepts\n- Tenant isolation via Redis namespaces\n- Cross-DC event propagation with Redis Pub/Sub\n- Circuit-breaker semantics and cooldown windows\n- Docker Config-driven runtime configuration\n\n## Code Example\n\n```yaml\n# docker-compose skeleton (excerpt)\nservices:\n  api:\n    image: myapi:latest\n    depends_on:\n      - traefik\n    environment:\n      - RDB_ENDPOINT=${REDIS}\n    deploy:\n      labels:\n        - traefik.http.routers.api.rule=Host(`api.example.org`)\n```\n\n## Follow-up Questions\n- How would you test cross-DC circuit-breaker behavior?\n- How handle Redis outage during bursts?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-7",
        "questionId": "q-17637",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement tenant-scoped shadow replicas: when a new tenant appears via X-Tenant, route a small percentage of traffic to a shadow replica spun up by a Tenant Registry and seeded via Docker Config. Warm, then migrate traffic or rollback on health. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create two service versions (stable and canary) behind Traefik TLS. Add a Docker Config canary-tenant-map that maps X-Tenant values to 'stable' or 'canary'. Route with Traefik rule on Headers 'X-Tenan",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose tenant-scoped shadow replicas: TLS API behind Traefik in two data centers; when a new tenant is detected via X-Tenant, route a small canary fraction to a shadow replica spun up by a Tenant Reg",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Prometheus-driven canary. 25% traffic to v2 behind Nginx; SLI: p99 latency <200ms and error rate <1% for 5m. A traffic-sidecar polls metrics, updates Nginx weights via API, and triggers automatic roll",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would deploy a Traefik edge and a tenant-aware API behind TLS. Store per-tenant flags in a Docker Config named tenant-flags (JSON). Bootstrap flags for alpha/beta with a script and bootstrap manifes",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design dynamic tenant onboarding with canary rollout across DCs, using docker-dca tooling.\n\n## Key Concepts\n- Per-tenant shadow replicas\n- Canary traffic shifting\n- Tenant Registry service\n- Docker Config-driven seed data\n- Zero-downtime rollout across DCs\n\n## Code Example\n```javascript\n// pseudo: edge config chooses canary weight based on tenant registry\n```\n\n## Follow-up Questions\n- How would you monitor canary health and auto-rollback?\n- How do you ensure data isolation during warmup?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-8",
        "questionId": "q-7699",
        "question": "In a two-datacenter docker-dca cluster, deploy a TLS-enabled API behind an Nginx edge proxy. Implement per-tenant resource isolation with a gatekeeper container that uses CGroups to cap CPU/memory for each tenant's app container, with quotas stored in Redis and enforced by a lightweight sidecar. Add automatic throttling and a feedback loop to throttle or drop requests when quotas are exceeded, and ensure latency SLOs during a simulated burst. Provide exact bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a two-DC deployment with a TLS API behind Nginx. Implement a per-tenant gatekeeper container that reads quotas from Redis and enforces CPU/memory limits via CGroups, throttling requests when a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Describe wiring TLS mutual authentication to identify tenants from the client CN, Traefik per-tenant weights (0-100) driven by a Docker Config for canary deployment across DCs, with automatic rollback",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a lightweight quota sidecar (Go/JS) that loads per-tenant quotas from YAML via Docker Config, watches for changes, and enforces per-tenant token bucket. Read tenant from header X-Tenant, return",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a lightweight Go sidecar that caches GET responses per tenant using an in-memory map; TTL 60s; cache key = tenant|path|query; on 5xx, invalidate and bypass; edge uses X-Tenant header for rou",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design per-tenant resource governance in a multi-DC docker-dca stack, balancing isolation and performance under bursty load.\n\n## Key Concepts\n\n- CGroups per-tenant, Redis as quota store, edge proxy TLS, sidecar enforcement, backpressure, cross-DC consistency.\n\n## Code Example\n\n```go\n// Pseudo-code: gatekeeper reads quota and applies cgroup limits\n```\n\n## Follow-up Questions\n\n- How would you test quota violations? How would you handle Redis unavailability without starving tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-9",
        "questionId": "q-13519",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster. Add a lightweight per-tenant idempotency guard as an in-memory sidecar that caches (tenant, id) with a 30s TTL and returns 409 on duplicates. The API should read X-Tenant and X-Idempotency-Key and the sidecar should log decisions. Provide bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a lightweight per-tenant idempotency guard as an in-memory sidecar between Nginx and the API. Cache (tenant, id) with a 30s TTL; read X-Tenant and X-Idempotency-Key; on first hit forward to API",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Configure TLS Termination at Traefik and route to two DC API instances. Add an OpenTelemetry Collector as a sidecar or central collector; drive tenant-specific sampling via a Docker Config tenant_samp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Generate a private certificate authority, create and sign certificates for both edge and API services, configure Nginx as a TLS termination point with two upstreams (api_v1, api_v2). Implement Nginx's split_clients modu…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant token-bucket rate limiter using Redis with keys derived from X-Tenant. Use a Lua script for atomic token fetch/consume; expose an admin endpoint to adjust caps without redeploy.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess understanding of per-tenant isolation, sidecar middleware placement, and idempotency semantics in docker-dca. The scenario mirrors real-world issues with retry storms and duplicate requests.\n\n## Key Concepts\n- In-memory idempotency cache scoped by (tenant, id) with TTL\n- Sidecar pattern between edge proxy and API\n- Header extraction (X-Tenant, X-Idempotency-Key) and forward logic\n- docker-compose skeleton and bootstrap workflow\n- Basic validation tests for idempotency behavior\n\n## Code Example\n```python\ncache = {}\ndef handle(req):\n  tenant = req.headers['X-Tenant']\n  key = req.headers['X-Idempotency-Key']\n  now = time.time()\n  if (tenant, key) in cache and cache[(tenant, key)] > now:\n    return 409\n  cache[(tenant, key)] = now + 30\n  forward_to_api(req)\n```\n\n## Follow-up Questions\n- How would you scale the cache across multiple sidecar instances?\n- What failure modes can break idempotency and how to mitigate (clock skew, TTL drift)?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-10",
        "questionId": "q-2295",
        "question": "In a 5-node Docker Swarm spanning two data centers, deploy a TLS-secured stateless API behind an Nginx edge proxy. Use Docker secrets for TLS certs and a Docker config for per-tenant routing templates, consuming routing directives from a central HTTP API. Attach a per-service Envoy sidecar to drive traffic-splitting with a progressive canary (25% steps every 20s). Ship logs via a separate sidecar to a Loki stack without blocking requests. Implement a zero-downtime upgrade using update_config (start-first, parallelism 1). Provide exact commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe the design for per-tenant data-plane quotas using a quota-agent sidecar that configures host cgroups for CPU/memory, reads policies from Vault, supports live updates, and rebalances on tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a feature-flag rollout controlled by a central policy service. Requests carry Tenant-ID header; compute canary bucket via hash(Tenant-ID) % 100 < 25 to map 25% tenants to canary backend. Ngi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy a TLS-enabled API behind Traefik in a Docker Swarm cluster spanning two data centers. Implement per-tenant rate limiting using Redis: each incoming request includes an X-Tenant header. Enforce a maximum of 100 re…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a 5-node Swarm across DCs, TLS via Docker secrets, Nginx edge proxy, and a per-service Envoy sidecar for canary routing. Routing rules pulled from a central HTTP API rendered into a Docker c",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design multi-DC Swarm, TLS secrets, dynamic routing, and Canary with sidecars and observability. Checks how routing rules are distributed and how upgrades remain zero-downtime.\n\n## Key Concepts\n\n- Docker Swarm across data centers\n- Secrets and Configs for TLS and routing\n- Nginx edge proxy with per-service Envoy sidecar\n- Canary rollout with health checks and rollback\n- Sidecar logging to Loki without blocking requests\n\n## Code Example\n\n```yaml\nversion: \"3.8\"\nservices:\n  edge-nginx:\n    image: nginx:alpine\n    secrets: [ tls_cert ]\n    configs: [ routing_tpl ]\n  api:\n    image: my/api:latest\n    deploy:\n      replicas: 3\n      update_config:\n        parallelism: 1\n        order: start-first\n```\n\n## Follow-up Questions\n\n- How would you validate canary health signals and rollback criteria?\n- How would you handle TLS certificate rotation without downtime?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-11",
        "questionId": "q-18557",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement tenant-aware feature rollouts using a lightweight in-edge feature flag service that serves per-tenant flag sets from Docker Config. The API should fetch flags per tenant (X-Tenant header) and switch on/off a new endpoint version for some tenants without redeploying. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Bootstrap Vault for SPIFFE SVID issuance, configure Nginx to terminate TLS using SVIDs, attach a verifier sidecar that validates SVIDs, and a policy sidecar that queries an external OPA-like service p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a tenant-scoped chaos testing sidecar that reads per-tenant chaos policies from Consul KV, uses a deterministic hash of the X-Tenant-ID header to selectively inject network faults (100–400ms latency, 0.5–2% pa…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Envoy sidecar routes each API call through a remote policy service over gRPC, authenticated with Vault SPIFFE SVIDs for mTLS. Cache decisions with 30s TTL and invalidate on policy reload via Redis pub",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant feature flag service injected via Docker Config, read with tenant header X-Tenant; Traefik routes to API behind TLS. The API queries the flag service for the current tenant and decide",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to introduce per-tenant dynamic behavior with minimal ops.\n\n## Key Concepts\n- Tenant-scoped feature flags via Docker Config\n- Edge TLS termination with Traefik\n- Canary-like routing without redeploys\n- Cross-DC consistency and health checks\n\n## Code Example\n```javascript\n// Pseudo: api.ts would call /flags/{tenant}.json and switch logic\n```\n\n## Follow-up Questions\n- How would you handle flag drift between DCs?\n- How to rollback if v2 causes errors?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-12",
        "questionId": "q-11432",
        "question": "In a three-node docker-dca cluster across two data centers, implement tenant-scoped data encryption using Vault Transit with per-tenant data keys, automatically rotated every 24 hours without downtime. Build a sidecar that fetches and caches the data key for each request based on HTTP header tenant-id, integrates with the edge TLS terminator, and logs key-management events to a central SIEM. Provide bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement tenant-scoped data encryption using Vault Transit with per-tenant data keys that rotate every 24 hours. Deploy a sidecar that fetches the appropriate data key based on the tenant-id HTTP header, caches it with…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Configure a per-tenant observability policy via a Docker Config otel-policy that maps each X-Tenant to a sampling rate and log level. Run an OpenTelemetry collector as a sidecar per API container expo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy Cilium+Hubble in docker-dca to enforce tenant-scoped NetworkPolicy with per-tenant labels and eBPF enforcement. Use Vault to mint SPIFFE SVIDs for all services and a sidecar to validate SVIDs a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy two API variants (v1 and v2) in a Docker DCA cluster. Configure Nginx as an edge proxy with TLS termination, implementing a 25% canary traffic split based on the X-Canary header. Instrument both services with Ope…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a scalable, tenant-isolated encryption architecture that integrates Vault Transit with a containerized multi-datacenter deployment, while addressing performance optimization, zero-downtime rotation, and comprehensive auditing requirements.\n\n## Key Concepts\n\n- Vault Transit for centralized key management\n- Envelope encryption with per-tenant data keys\n- Sidecar pattern with intelligent key caching and TTL management\n- Zero-downtime key rotation using data re-wrapping\n- Edge TLS termination integration\n- Centralized SIEM logging for audit trails\n\n## Code Example\n\n```javascript\n// Pseudo-code: fetch per-tenant data key from Vault Transit and encrypt\nasync function encryptTenantData(tenantId, plaintext){\n  const key = await cacheOrFetchKey(tenantId);\n  // Implement encryption logic using the tenant-specific key\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-13",
        "questionId": "q-7445",
        "question": "Across two data centers, deploy a TLS-enabled API behind Nginx. Implement a tenant-aware service mesh created on demand by a policy controller, isolating each tenant on ephemeral overlay networks and routing to their backends. Use Vault for per-tenant short-lived certs with zero-downtime rotation; edge verifier validates; and a dedicated SIEM pipeline for tenant events. Provide exact bootstrap commands and a docker-compose skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy a policy-driven tenant service mesh created on demand. For each tenant, provision an ephemeral overlay network with sidecar proxies, route through Nginx with TLS termination, and issue per-tenant TLS certificates…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect a tenant-scoped rate limiter using Redis sliding-window counters keyed by X-Tenant-ID, wired into Traefik 2.x as a custom middleware that supports hot-reload without restart. A sidecar feeds quota metrics to P…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Envoy as a per-tenant rate limiter sidecar tied to the API service. Validate JWT via Vault OIDC, fetch quotas from Vault KV on cache miss, store per-tenant state in Redis with short TTL, and enfor",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Mint SVIDs via Vault for all services; edge Nginx terminates TLS; ship an audit-proxy sidecar that forwards 15% of tenant events to the tenant-audit Kafka topic; enforce per-tenant rate limits via an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design on-demand, tenant-scoped service meshes with dynamic networking, secure edge routing, and automated certificate management.\n\n## Key Concepts\n- On-demand tenant service meshes and ephemeral overlay networks\n- Policy-driven orchestration for per-tenant isolation\n- Vault-based TLS certificates with TTL rotation and zero-downtime rekeying\n- Edge certificate validation and centralized SIEM pipeline for tenant events\n- Canary rollout and rollback strategies\n\n## Code Example\n```javascript\n// illustrative bootstrap\nconsole.log('deploy tenant mesh bootstrap');\n```\n\n## Follow-up Questions\n- How would you migrate a single tenant to another data center with zero downtime?\n- What metrics would you monitor to ensure mesh health and performance?\n- How do you handle certificate revocation for terminated tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-14",
        "questionId": "q-2432",
        "question": "In a 4-node docker-dca Swarm, deploy a TLS-secured event-processing pipeline behind an Nginx edge proxy. Use Docker secrets for TLS certs, a Docker config for routing rules, and implement a 5% canary upgrade for the Transform service controlled by a Consul KV flag. Provide exact commands and a docker-compose.yml skeleton that demonstrates canary traffic, health checks, and zero-downtime upgrades?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Configure Nginx at the edge for TLS termination using Docker secrets (edge.crt, edge.key), proxying to the API, and apply a per-tenant rate limit based on X-Org-Id. Include a /health probe and a reque",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up a 4-node Swarm with an overlay network, create TLS secrets tls.crt and tls.key, and a routing config. Deploy ingest, transform-prod, transform-canary, and sink behind edge-nginx. Route 95% to p",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Deploy an Envoy sidecar alongside your API across two data centers, behind a Traefik edge proxy. Configure it to read a Docker Config named `tenant_region` that maps tenant IDs to their preferred data center, then rewri…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To achieve sub-50ms per-tenant decision latency, implement an LRU cache with TTL in the Wasmtime sidecar, preload policy modules from the policy broker, and maintain a fast-path default policy for broker timeout scenari…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to orchestrate secrets/configs with a canary using a proxy. Requires Swarm networking, overlay, health checks, and rollback semantics.\n\n## Key Concepts\n\n- Docker secrets/configs\n- Swarm services and update_config\n- Overlay networks and edge proxy traffic-splitting\n- Consul KV flag-based traffic routing\n\n## Code Example\n\n```yaml\nversion: '3.8'\nservices:\n  ingest:\n    image: myreg/ingest:latest\n    deploy:\n      replicas: 1\n      restart_policy:\n        condition: on-failure\n  transform-prod:\n    image: myreg/transform-prod:latest\n    deploy:\n      replicas: 1\n      update_config:\n        parallelism: 1\n        order: start-first\n  transform-canary:\n    image: myreg/transform-canary:latest\n    deploy:\n      replicas: 1\n  sink:\n    image: myreg/sink:latest\n    deploy:\n      replicas: 1\n  edge-nginx:\n    image: nginx:alpine\n    secrets:\n      - tls_crt\n      - tls_key\n    configs:\n      - source: routing_conf\n        target: /etc/nginx/conf.d/routing.conf\nnetworks:\n  pipeline:\n    driver: overlay\nsecrets:\n  tls_crt:\n    file: ./certs/tls.crt\n  tls_key:\n    file: ./certs/tls.key\nconfigs:\n  routing_conf:\n    file: ./routing/nginx.conf\n```\n\n## Follow-up Questions\n\n- How would you test the canary flag in production-like load?\n- How would you roll back if the canary shows degradation?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-15",
        "questionId": "q-10877",
        "question": "In a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy. Add a new requirement: implement tenant-scoped immutable log streams by signing logs at generation and routing them to a tenant-specific Kafka topic, with per-tenant keys stored in Vault and rotated automatically. Ensure a verifier service checks signatures at ingestion, and enforce cross-DC replication and access control so only edge nodes can publish to tenant topics. Provide exact bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe how to implement tenant-scoped rate limiting in a two-DC docker-dca setup using Traefik's RateLimit middleware with Redis as the backend. Explain how per-tenant keys are composed from X-Tenan",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Vault to issue per-tenant signing keys and rotate via leases. Implement a log- producer sidecar that signs each log line with the tenant’s key and publishes to a tenant-specific Kafka topic.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Traefik as the TLS edge; per-tenant flags live in Docker Config; a lightweight sidecar serves a /flags endpoint and writes updates to an in‑memory cache shared with the API. The API reads from the",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bind each tenant to a versioned model from a central registry, loading on first use. Use Traefik weighted routing for per-tenant canaries, with health checks to promote/demote versions. Enforce per-te",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical mastery of cross-DC trust, logging integrity, and key management. It tests designing a scalable tenant-isolated log pipeline, safe key rotation, and enforcement at the edge via SPIFFE identities. It also evaluates operational considerations for verifier deployment, topic ACLs, and data residency.\n\n## Key Concepts\n- Tenant-scoped signing keys and Vault lease rotation\n- Immutable log generation with per-tenant Kafka topics\n- Signature verification at ingestion via a verifier sidecar\n- SPIFFE-based edge-origin enforcement\n- Cross-DC Kafka replication and ACLs\n\n## Code Example\n```yaml\nversion: '3.8'\nservices:\n  edge-nginx:\n    image: nginx:latest\n  api:\n    image: myorg/api:latest\n  log-producer:\n    image: myorg/log-producer:latest\n  log-verifier:\n    image: myorg/log-verifier:latest\n  kafka:\n    image: confluentinc/cp-kafka:7.0.0\n  zookeeper:\n    image: confluentinc/cp-zookeeper:7.0.0\n```\n\n## Follow-up Questions\n- How would you test key rotation impact on in-flight logs?\n- How would you handle tenant onboarding/offboarding and revocation of access to topics?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-16",
        "questionId": "q-23756",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant API versioning: clients specify X-API-Version; the gateway must route to tenant-specific versioned backends (v1, v2) per tenant, with missing version defaulting to the latest. Ensure safe migration paths and compatibility checks, plus a bootstrap flow and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Route tenants to versioned backends using Traefik v2 with header-based routing: X-API-Version selects v1 or v2; a validation middleware checks per-tenant supported-versions from Docker Config tenant_v",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-tenant log levels via a Docker Config tenant-logging that maps tenant IDs to levels (e.g., info, debug). API reads X-Tenant, applies the configured level to the logger at runtime (e.g.,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure Nginx as TLS termination proxy and host multi-tenant vhosts via Docker Configs. Steps: 1) create TLS secret, 2) create a tenant-nginx.conf as a templated vhost map, 3) docker config create t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a Redis-backed per-tenant semaphore (tenant:<id>) and a Traefik middleware that acquires/releases permits. Forward if acquired; otherwise return 429 and optionally route to a canary backend.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design per-tenant versioning and dynamic routing across DCs under TLS, using Traefik and docker-dca patterns. It also exercises config-driven routing, safe migration, and minimal bootstrap orchestration.\n\n## Key Concepts\n- Traefik v2 header-based routing\n- Docker Config for tenant_version_map\n- Per-tenant versioned backends with default to latest\n- Migration safety checks and compatibility validation\n\n## Code Example\n```javascript\n// Example Traefik dynamic config pseudo\n// router.tls=true; rule: Host(`api.example`); headers.CustomRequestHeaders.X-API-Version = {tenantVersion}\n```\n\n## Follow-up Questions\n- How would you implement deprecation and rollback windows for older versions?\n- How would you test cross-DC routing and failover for versioned endpoints?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-17",
        "questionId": "q-13676",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster. Define a new angle: implement a per-tenant data residency policy where tenants map to DCs; a policy-driven sidecar reads rules from a Docker Config and rewrites upstream targets in real time without reloading Nginx. Bootstrap commands and a docker-compose skeleton required?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy a TLS-enabled API behind a Traefik edge proxy in a Docker DCA cluster across two data centers. Implement per-tenant dynamic feature flags using a central Feature Flag service backed by Redis shards per DC, with p…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a sidecar that watches Docker Config for a tenant->DC map and residency rules. The sidecar updates Nginx upstreams in place (via Lua/OpenResty) to avoid reloads, enabling hot-reload of policies wi",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster with a per-tenant rate-limiting sidecar using in-memory token buckets (10 RPS, burst 20). Quotas load from Consul KV at s",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a solution where Nginx terminates TLS at the edge, Envoy enforces per-tenant routing to local region databases, and a sidecar calls a policy service to authorize requests. Vault Transit perf",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design policy-driven edge routing and hot-reload without reloading proxies; explores Docker Config, multi-DC deployment, and data residency.\n\n## Key Concepts\n- Docker Config-driven dynamic routing\n- Nginx upstream reconfiguration without reload (Lua/OpenResty)\n- Per-tenant residency policies and failure handling\n\n## Code Example\n```lua\n-- Pseudo Lua snippet illustrating dynamic upstream switch based on tenant\n-- This would live in the init_by_lua_block or content_by_lua_block\nupstreams = {\n  [\"tenantA\"] = \"dc1.example.com\",\n  [\"tenantB\"] = \"dc2.example.com\",\n}\n\n-- function to route request based on header tenant id\nfunction route()\n  local tenant = ngx.req.get_headers()[\"X-Tenant\"]\n  if tenant and upstreams[tenant] then\n    ngx.var.upstream = upstreams[tenant]\n  end\nend\n\nroute()\n```\n\n## Follow-up Questions\n- How would you test residency policy changes under traffic bursts?\n- How would you handle certificate rotation for TLS across DCs?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-18",
        "questionId": "q-18488",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant data residency: a Docker Config tenant-region-map maps tenant IDs to a preferred DC (dc1 or dc2). API should route reads/writes by X-Tenant to the tenant's home DC, with automatic failover to the other DC if the residency DC is down. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a central tenant-version map in Consul KV (or Redis), a per-tenant sidecar that reads X-Tenant and routes to blue or green through Traefik, enabling on-the-fly tenant pinning without redeploys",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deploy an admission sidecar that calls an OPA server before launching the API container. Policies validate allowed base images per tenant, allowed tags, and required licenses; violations exit non-zero",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure Traefik TLS with mTLS; implement a tenant router middleware that reads X-Tenant and consults a Docker Config tenant-region-map {tenantA:dc1, tenantB:dc2}. Forward to the home DC backend; if",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-tenant canary controller that writes status to Redis (key: canary:tenant:<id>, fields enabled, percentage). Traefik’s dynamic config is updated by a small sidecar reading Redis and rou",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical multi-DC data residency, simulating real-world constraints where tenants must stay in their home data center unless failure occurs. Evaluates how a reverse proxy can enforce policy and how to architect graceful failover without data leakage or inconsistent routing.\n\n## Key Concepts\n\n- docker-dca multi-DC deployment\n- Traefik TLS and mTLS configuration\n- Docker Configs for tenant-region-maps\n- Per-tenant routing logic based on X-Tenant\n- Health checks and automatic failover across DCs\n\n## Code Example\n\n```yaml\n# tenant-region-map example\ntenant-region-map:\n  tenantA: dc1\n  tenantB: dc2\n```\n\n## Follow-up Questions\n\n- How would you test failover under simulated DC outages?\n- What are the latency implications for cross-DC routing and how would you mitigate them?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-19",
        "questionId": "q-22000",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Expose a per-tenant Server-Sent Events stream at /stream. Implement dynamic per-tenant concurrency limits loaded from a Docker Config named tenant-streams, with quotas hot-reloaded at runtime. Reject new streams with 429 when at limit; expose per-tenant metrics at /metrics; provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a TLS-terminated edge using Nginx, fronting a primary API and a shadow version. Add Nginx mirror directive to send 5% of requests to the shadow service while the primary path remains unchang",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use SPIRE to issue per-service SVIDs in both DCs; deploy SPIRE server+agents, run Traefik and API with SPIRE-validated mTLS; store CA bundle in a Docker Config and rotate via a rolling secret update;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Solution Overview Create a Go sidecar that watches Consul KV at `/tenants/{tenant_id}/region` and updates Nginx upstream servers dynamically. ### For open-source Nginx: - Sidecar writes to `/etc/nginx/conf.d/upstream…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-tenant streaming gate backed by Redis for current counts, with a small in-process limiter that loads the tenant-streams config from Docker Config and hot-reloads on change. Validate on ea",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design dynamic per-tenant streaming quotas, runtime-config reloading, SSE handling, and multi-DC deployment with Traefik TLS.\n\n## Key Concepts\n\n- Server-Sent Events, per-tenant concurrency limits, Docker Config, Redis-backed counters, hot-reload config, OpenTelemetry tracing, TLS at edge.\n\n## Code Example\n\n```javascript\n// sample Lua script for atomic check/incr\nlocal tenant = KEYS[1]\nlocal limit = tonumber(ARGV[1])\nlocal active = redis.call('GET', 'streams:' .. tenant) or 0\nif tonumber(active) >= limit then\n  return 0\nelse\n  redis.call('INCR', 'streams:' .. tenant)\n  redis.call('EXPIRE', 'streams:' .. tenant, 60)\n  return 1\nend\n```\n\n## Follow-up Questions\n\n- How would you test hot-reload correctness under rapid config changes?\n- What are failure modes if Redis is partitioned or slow to respond, and how would you mitigate them?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-20",
        "questionId": "q-6673",
        "question": "In a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy. Implement end-to-end distributed tracing with OpenTelemetry: run an OpenTelemetry collector as a sidecar on each node, instrument services to emit traces, export via OTLP to a central Jaeger backend over TLS, and apply sampling (baseline 10%, canary 25% for a new version). Provide exact bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instrument a three-node docker-dca cluster with TLS behind Nginx and end-to-end tracing via OpenTelemetry. Run an OpenTelemetry Collector as a sidecar on each node, emit traces from the API, export to",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a Vault Agent sidecar that auto-renews and writes certs to a shared /certs volume. Configure the API and PG client to reference these certs on startup and reload the pool with SIGHUP when Vault si",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument the API with OpenTelemetry, exporting traces to a Jaeger backend replicated across both data centers. Configure per-tenant sampling policies loaded from a Docker Config named tenant-sampling (e.g., {tenantA:…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a Node.js API image with a Dockerfile, and a start script wait-for-redis.sh that pings redis:6379 until ready, then runs node index.js. In docker-compose.yml, define services api and redis on a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of distributed tracing integration in a multi‑DC Docker context, including sidecar patterns, TLS OTLP transport, sampling strategies, and a practical docker-compose setup.\n\n## Key Concepts\n- OpenTelemetry collector as a per-node sidecar\n- OTLP over TLS to Jaeger\n- W3C trace context propagation\n- Canary sampling and observability buy-in\n\n## Code Example\n```yaml\n# docker-compose skeleton\nversion: '3.8'\nservices:\n  nginx-edge:\n    image: nginx:latest\n  api:\n    image: myorg/api:latest\n  otel:\n    image: otel/opentelemetry-collector:latest\n  jaeger:\n    image: jaegertracing/all-in-one:1.33\n```\n\n## Follow-up Questions\n- How would you validate tracing data quality under network hiccups?\n- How would you handle TLS certificate rotation with OTLP endpoints?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-21",
        "questionId": "q-1832",
        "question": "Design and implement a zero-downtime upgrade for a 4-node Docker Swarm across two data centers hosting a stateful Redis queue and a stateless worker service. The worker consumes messages encoded in a new proto format; the cluster uses TLS mutual authentication with Vault for cert rotation and Docker Secrets for credentials. Outline exact upgrade steps, a 10% canary rollout, health checks, and a rollback plan, and include a minimal docker-compose snippet showing update_config(order: start-first, parallelism: 1)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a 4-node Swarm across two DCs, with a 10% canary of the worker upgraded to the new proto-format, while 90% stay on the legacy image. Deploy update_config: order: start-first; parallelism: 1. Route",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Describe configuring Traefik with two services (baseline and canary) and a per-tenant weight map in Docker Config. Explain wiring a tenant-aware router that uses Headers to route to the correct servic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a Redis-backed per-tenant token-bucket rate limiter. Each tenant uses a Redis namespace; tokens are decremented atomically via a Lua script. Middleware reads X-Tenant, enforces the remaining",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Vault PKI to issue per-tenant client certs with TTL around 60 minutes, leverage Traefik to terminate TLS and enforce mTLS to a tenant-scoped backend. A sidecar fetches/refreshes certs and reloads",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world upgrade workflows in a distributed Swarm with cross-DC considerations, TLS cert rotation, and in-flight data.\n\n## Key Concepts\n- Swarm update_config with start-first and parallelism\n- Canary rollouts and rollback triggers\n- TLS mutual authentication, Vault cert rotation, Docker secrets\n- In-flight data migration and zero-downtime guarantees\n\n## Code Example\n```yaml\nversion: '3.8'\nservices:\n  worker:\n    image: myorg/worker:upgrade\n    deploy:\n      replicas: 4\n      update_config:\n        order: start-first\n        parallelism: 1\n    secrets:\n      - tls_cert\n      - db_creds\n    environment:\n      - PROTO_VERSION=v2\n```\n\n## Follow-up Questions\n- How would you validate canary health beyond a simple Liveness probe?\n- What rollback automation would you add to rapidly revert if metrics deteriorate?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-22",
        "questionId": "q-16942",
        "question": "In a two-data-center docker-dca deployment, place a TLS-enabled API behind an Envoy edge proxy and enable tenant-scoped A/B testing between API versions v1 and v2. Use a Redis-backed policy store for per-tenant routing decisions, validate tenants via JWT, route 25% of eligible tenants to v2, collect latency and error-rate, and automatically rollback if SLA breaches persist >5 minutes. Provide bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe a two-DC deployment where a TLS API behind Traefik uses Vault for dynamic per-tenant DB credentials. The API authenticates with AppRole, fetches tenant creds with a short lease, renews proact",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To solve this, terminate TLS at the edge, enable mTLS inside the cluster via Vault-issued SPIFFE SVIDs, and run a cert-verifying sidecar next to each service. The OPA policy, stored in a Docker Config",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy a Redis-backed routing map storing tenant_id → api_version mappings. Implement a lightweight sidecar that monitors Redis for version changes and pushes dynamic Traefik configuration updates through the Docker API…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a two-DC deployment where TLS-terminated API behind an Envoy edge routes tenants for A/B testing between v1 and v2. Use Redis as a policy store to decide per-tenant routing, verify JWT-provi",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical edge routing with real-world constraints: per-tenant A/B testing, cross-DC consistency, and observable SLAs.\n\n## Key Concepts\n- Envoy edge TLS termination\n- Redis-backed per-tenant policy store\n- JWT-based tenant identification and authorization\n- Canary rollout with per-tenant routing\n- Automation of rollback under SLA breach across DCs\n\n## Code Example\n```yaml\n# Minimal bootstrap for envoy + redis (illustrative)\nversion: \"3\"\nservices:\n  envoy:\n    image: envoyproxy/envoy:v1.28.0\n    ports:\n      - \"443:443\"\n    volumes:\n      - ./envoy.yaml:/etc/envoy/envoy.yaml\n  redis:\n    image: redis:7-alpine\n```\n\n## Follow-up Questions\n- How would you test cross-DC latency and canary safety?\n- How would you handle Redis cluster failure scenarios?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-23",
        "questionId": "q-21729",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant rate limiting using a Redis-backed token bucket: a Redis counter per tenant with a 60-second window. Tenant IDs are sent in the X-Tenant header. Rate limits are defined in a Docker Config named rate-config (tenant_id -> limit). Update limits without service restarts via in-process watcher. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy a TLS-enabled API behind Traefik in a Docker DCA cluster across two data centers. Implement per-tenant rate limiting using a Redis-backed token bucket: a Redis counter per tenant with a 60-second window. Tenant I…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Sign events in a dedicated audit sidecar using Vault Transit per-tenant keys, rotate automatically, forward to a TLS-enabled SIEM via a lightweight broker, and store immutable digests in a WORM bucket",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would deploy a Traefik edge and a tenant-aware API behind TLS. Store per-tenant flags in a Docker Config named tenant-flags (JSON). Bootstrap flags for alpha/beta with a script and bootstrap manifes",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a feature-flag rollout controlled by a central policy service. Requests carry Tenant-ID header; compute canary bucket via hash(Tenant-ID) % 100 < 25 to map 25% tenants to canary backend. Ngi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to implement per-tenant QoS in a multi-DC setup using dynamic configuration updates without service restarts.\n\n## Key Concepts\n- Per-tenant rate limiting with Redis-backed counters (token bucket algorithm)\n- Docker Config as a runtime parameter store with hot-reload capability\n- Traefik edge routing and TLS termination across data centers\n- In-process configuration watchers for zero-downtime updates\n\n## Code Example\n```javascript\n// Token bucket implementation per tenant\nasync function allow(tenantId, limit) {\n  const window = Math.floor(Date.now() / 60000);\n  const key = `ratelimit:${tenantId}:${window}`;\n  \n  const current = await redis.incr(key);\n  if (current === 1) {\n    await redis.expire(key, 60);\n  }\n  \n  return current <= limit;\n}\n\n// Rate limit middleware with dynamic config reload\nconst ratelimit = {\n  scheme: 'tokenbucket',\n  store: 'redis',\n  key: '${header:X-Tenant}',\n  burst: 100,\n  rate: '60s'\n};\n\n// In-process config watcher\nfunction watchRateConfig(path, onUpdate) {\n  const watcher = chokidar.watch(path, { persistent: true });\n  watcher.on('change', async () => {\n    const config = await loadRateConfig();\n    traefik.middlewares.ratelimit = buildMiddleware(config);\n  });\n}\n```\n\n## Architecture\n- **Traefik**: Edge proxy handling TLS termination and rate limiting\n- **Redis**: Shared counter store across data centers for consistency\n- **Docker Config**: Stores rate limit mappings, mounted for watcher access\n- **API Service**: Backend handling tenant-specific business logic\n\n## Bootstrap Commands\n```bash\n# Create rate limit config\necho '{\"tenant-a\": 100, \"tenant-b\": 50}' | docker config create rate-config -\n\n# Deploy stack\ndocker stack deploy -c docker-compose.yml api-stack\n```",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-24",
        "questionId": "q-26901",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Add a Redis-backed per-tenant token-bucket rate limiter. Each tenant has a namespace; tokens consumed atomically via Lua. The API middleware reads X-Tenant, enforces the remaining tokens, and returns 429 when exhausted. Bootstrap tenant_limits.json with per-tenant bursts via Docker Config mounted into the limiter. TLS via Traefik; provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe the end-to-end flow: a sidecar reads X-Tenant, consults a Redis-based per-tenant token bucket (Lua script for atomic grant/deny), and trips a circuit-breaker to route to a degraded path when",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Redis-backed per-tenant token-bucket rate limiter. Each tenant uses a Redis namespace; tokens are decremented atomically via a Lua script. Middleware reads X-Tenant, enforces the remaining",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a Redis-backed quota per-tenant. A lightweight sidecar in the request path increments tenant:window in Redis with a 60s TTL and allows if count <= 60; otherwise returns 429. Use X-Tenant to se",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Parse X-Tenant, fetch limits from Docker Config tenant-rate-limits.json (tenantId: rpm). Implement in-memory per-tenant token bucket with 1-minute refill; if tokens available, process and decrement; i",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests practical rate limiting in a multi-tenant, multi-DC setup with container orchestration.\n\nKey Concepts\n- Token bucket per-tenant with Redis namespaces\n- Atomic Lua script for refill/consume in Redis\n- Docker Config for per-tenant limits and mounting into services\n- TLS termination via Traefik in a docker-dca cluster\n\nCode Example\n```javascript\n// Example Redis-Lua snippet (high level, client calls EVALSHA in production)\nconst key = KEYS[1]\nconst limit = tonumber(ARGV[1])\nconst now = tonumber(ARGV[2])\n// refill tokens, decrement, return remaining tokens\n```\n\nFollow-up Questions\n- How to handle Redis outage and retry strategies?\n- How to test rate limits locally and in CI without a full DC setup?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-27T05:51:23.533Z",
    "version": 1
  }
]