[
  {
    "id": "test-docker-dca",
    "channelId": "docker-dca",
    "channelName": "Docker DCA",
    "title": "Docker DCA Knowledge Test",
    "description": "Test your understanding of Docker DCA with 25 questions.",
    "questions": [
      {
        "id": "docker-dca-q-0",
        "questionId": "q-27519",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement a tenant-aware log redaction layer: a middleware that redacts PII fields (email, phone, ssn) from JSON responses according to a per-tenant policy stored in a Docker Config named log-policy. The policy supports per-tenant allow/deny lists and hot-reloads without downtime. Route by tenant via X-Tenant; ship redacted logs to a central Loki with per-tenant retention. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a deploy-time gate as a sidecar: run 'trivy image $IMAGE', parse CVEs, block with non-zero exit if any CVSS >= 7.0. Query Consul KV at tenants/$TENANT/policy (tenant from X-Tenant) to allow",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Python controller in Swarm that reads p95 latency from a Prometheus-compatible endpoint and gradually shifts Nginx upstream weights from 10/90 to 0/100 in 15s steps. Use Docker secrets for",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a tenant-aware redaction middleware at the edge that, using a per-tenant policy in a Docker Config named log-policy, redacts PII fields (email, phone, ssn) from JSON responses. Policies supp",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Redis-backed per-tenant token-bucket with a Lua script for atomic refill/consume; enforce via a Traefik middleware that checks the bucket before routing. Manage per-tenant rates and pause via Do",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to implement data governance and privacy in a multi-DC setup: edge TLS, per-tenant policy, hot-reload, and observability with Loki, all integrated via docker-dca.\n\n## Key Concepts\n\n- Traefik edge TLS termination across DCs\n- Middleware-based response redaction per-tenant\n- Docker Config hot-reload and policy delivery\n- Loki-based centralized logs with per-tenant retention\n- X-Tenant routing and impact on latency\n\n## Code Example\n\n```javascript\n// Express-style middleware skeleton for redacting response payloads per-tenant policy\nfunction redactor(policy) {\n  return (req, res, next) => {\n    const oldSend = res.send;\n    res.send = function (body) {\n      let payload = typeof body === 'string' ? JSON.parse(body) : body;\n      const tenant = req.headers['x-tenant'];\n      const p = policy[tenant] || { redact: [] };\n      if (payload && typeof payload === 'object') {\n        for (const key of p.redact) {\n          if (payload.hasOwnProperty(key)) payload[key] = 'REDACTED';\n        }\n      }\n      oldSend.call(this, JSON.stringify(payload));\n    };\n    next();\n  };\n}\n```\n\n## Follow-up Questions\n\n- How would you test hot-reload correctness and no-downtime guarantees for policy updates?\n- How would you measure and mitigate the performance impact of redaction on large payloads?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-1",
        "questionId": "q-15710",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster. Add a simple rate-limiter sidecar that enforces 100 requests/min per api_key using a Redis cluster shared across both DCs. If the limit is exceeded, return 429 with a Retry-After header. Provide bootstrap commands and a docker-compose.yml skeleton wiring nginx, api, limiter, and Redis, with a shared Redis endpoint and health checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a rate-limiter sidecar that sits in front of the API, using a Redis cluster shared across both DCs. On each request, extract X-Api-Key, run a Lua script to atomically INCR a 60s bucket key (api",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would sign images with Docker Content Trust, add a lightweight 'sigver' sidecar that validates signatures against Notary on container startup and before traffic; issue SVIDs with Vault for mTLS; fet",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan to instrument all service calls with OpenTelemetry: set up a tenant-scoped tracer with W3C trace context, annotate spans with tenant.id, propagate via X-Tenant header, and export via OTLP to a pe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant rate limiting with Redis-backed counters: a lightweight middleware reads quota from rate-config (tenant→limit) via X-Tenant, increments a Redis key per tenant with a 60s window, a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nNew angle on cross-DC coordination of a gating control in edge + service.\n\n## Key Concepts\n\n- TLS termination at Nginx edge\n- Sidecar pattern in docker-dca\n- Redis Lua script for atomic rate-limiting\n- Shared Redis across data centers\n\n## Code Example\n\n```javascript\n// Pseudo Redis Lua script concept\nconst bucket = `rate:${apiKey}:60s`\nconst count = await redis.incr(bucket)\nif (count === 1) await redis.expire(bucket, 60)\nreturn count <= 100\n```\n\n## Follow-up Questions\n\n- How would you test cross-DC failover for the limiter?\n- How would clock skew affect buckets and how would you mitigate?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-2",
        "questionId": "q-25994",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant rate limiting using a Redis-backed token bucket; quotas defined via a Docker Config quotas-config.json and applied per-tenant by the X-Tenant header; support bursts with a fixed refill rate and return 429 when the quota is exceeded. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe implementing per-tenant rate limiting with a Redis-backed token bucket. Quotas loaded from quotas-config.json via Docker Config; X-Tenant selects tenant. TLS API behind Traefik in docker-dca",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Instrument the API with OpenTelemetry: add the SDK and OTLP exporter, tag traces and metrics with X-Tenant, and emit a spans-per-request plus a per-tenant counter. Route traces to a central OTLP colle",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a tenant-scoped rate-limiter sidecar that reads quotas from rate-config Docker Config (JSON keyed by X-Tenant). The sidecar uses a Redis cluster with a Lua script to atomically decrement tokens fo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument API with OpenTelemetry; export OTLP traces to a central collector (Tempo/Jaeger) across DCs; tag spans with X-Tenant; drive per-tenant sampling via Docker Config sampling-config (JSON) read",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\\n\\nTests practical per-tenant QoS in multi-DC Docker setups.\\n\\nKey Concepts\\n- Traefik TLS in docker-dca\\n- Redis-backed per-tenant token bucket\\n- Docker Config for quotas\\n- X-Tenant-based routing and 429 responses\\n\\nCode Example\\n```json\\n{ \"tenants\": { \"TenantA\": {\"rate\": 10, \"burst\": 20} } }\\n```\\n\\nFollow-up Questions\\n- How would you validate burst vs. sustained rate under test load?\\n- How would you monitor and alert on rate-limit breaches?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-3",
        "questionId": "q-2346",
        "question": "In a 3-node docker-dca cluster spanning two data centers, deploy a TLS-secured stateless API behind an Nginx proxy. Use Docker secrets for TLS certs and a Docker config for API endpoints. Add a shadow sidecar that mirrors 5% of production traffic to a canary version. Implement a canary rollout by adjusting weights in 10% increments using update_config, with health checks and automatic rollback on failure. Provide exact commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instrument the API and edge proxy with OpenTelemetry (OTLP exporter to Jaeger), initialize a tracer in each service, and propagate traceparent headers through Nginx and the Verifier sidecar. Run Jaege",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe implementing per-tenant residency with Envoy as edge proxy and OPA-based routing. Tenants carry residency label in JWT; Consul registers services in DC1 and DC2; a sidecar queries OPA to deci",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument the API with OpenTelemetry, run a per-task OTEL Collector sidecar, and export traces to Jaeger via OTLP. TLS via Docker secrets; TLS termination at Nginx. All services on a single overlay n",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement dynamic traffic shadow: deploy a main API and a canary, with a shadow Envoy sidecar mirroring ~5% of traffic to the canary; TLS via Docker secrets; Nginx routes 95% to main and 5% to shadow;",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to orchestrate multi-DC deployments, secret-driven TLS, and traffic shaping with live canary upgrades. It also probes rollback discipline and the interaction between edge proxies, sidecars, and the orchestrator.\n\n## Key Concepts\n- Docker secrets and configs for TLS and endpoints\n- Nginx/Envoy traffic routing and shadow/canary pattern\n- Canary rollout via update_config with health checks\n- Multi-DC networking and rollback strategies\n\n## Code Example\n```yaml\nversion: '3.8'\nservices:\n  nginx:\n    image: nginx:stable\n    secrets:\n      - tls_cert\n    configs:\n      - source: api_endpoints\n        target: /etc/api/endpoints.conf\n  api-main:\n    image: myapi:latest\n    secrets:\n      - tls_cert\n  api-canary:\n    image: myapi:canary\n    secrets:\n      - tls_cert\n    deploy:\n      update_config:\n        parallelism: 1\n        order: start-first\n        failure_action: rollback\nsecrets:\n  tls_cert:\n    file: ./certs/tls_cert.pem\nconfigs:\n  api_endpoints:\n    file: ./configs/endpoints.conf\n```\n\n## Follow-up Questions\n- How would you monitor canary health and automate rollback decisions?\n- How would you secure inter-service communication and ensure TLS mutual authentication across DCs?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-4",
        "questionId": "q-5857",
        "question": "In a two-data-center docker-dca cluster, deploy a TLS-enabled API behind an Nginx edge proxy. Add per-tenant data residency by routing tenant data to dedicated overlay volumes and enforce locality with a sidecar policy engine that consults a central policy store. Use Vault SPIFFE SVIDs, and implement a 15% canary with rollbacks. Provide exact bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy a Go-based edge-cache sidecar alongside the API in each data center. The sidecar caches GET responses keyed by TenantID+URL path, with per-tenant TTLs sourced from a central config service. Use a lightweight pub/…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant canary: Traefik routes to v1 and v2. Canary_pct per tenant stored in Redis (tenant_id -> int). Traefik uses X-Tenant to split traffic with weighted routing. Update canary_pct via",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Route tenants based on Redis-backed feature flags using Nginx with Lua scripting. Extract the X-Tenant-Id header from incoming requests, query Redis for that tenant's flag value, and dynamically route to /api/v2 when th…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Vault to mint SPIFFE SVIDs for all services and terminate TLS at Nginx with mTLS. Enforce per-tenant data residency by binding tenant data to overlay volumes and a sidecar loc-enforcer that valida",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design tenant-aware data residency and policy enforcement in docker-dca, including edge TLS termination, overlay data paths, and canary safety net.\n\n## Key Concepts\n- docker-dca multi-DC deployment\n- SPIFFE/SVID rotation with Vault\n- data residency via overlay networks/volumes\n- per-tenant policy with OPA\n- canary rollout and rollback criteria\n\n## Code Example\n```javascript\n// Pseudo-code illustrating policy check\nconst tenant = req.headers['x-tenant'];\nif (allowedTenants[tenant]?.includes(req.path)) {\n  next();\n} else {\n  res.status(403).send('forbidden');\n}\n```\n\n## Follow-up Questions\n- How would you monitor cross-tenant data access to ensure residency is respected?\n- How would you test the canary rollback under sudden quota breach?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-5",
        "questionId": "q-4996",
        "question": "In a two-node docker-dca cluster, deploy a TLS-enabled API behind a Traefik edge proxy. Use a lightweight in-cluster CA (OpenSSL) to issue and rotate certificates for both the API and the proxy; store certs in Docker Secrets and configure mTLS between Traefik and the API. Add a tiny feature flag service via Docker Config that toggles a field in the API response per-tenant. Implement a 10% canary routing to a forked backend; provide a docker-compose skeleton and exact bootstrap steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement dynamic traffic shadow: deploy a main API and a canary, with a shadow Envoy sidecar mirroring ~5% of traffic to the canary; TLS via Docker secrets; Nginx routes 95% to main and 5% to shadow;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Bootstrap a lightweight OpenSSL CA, issue certificates for Traefik and the API, and load them as Docker Secrets. Configure Traefik to enforce mTLS with clientAuth and TLS settings, route via TLS to the API, add a canary…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Deploy a Redis-backed routing map storing tenant_id → api_version mappings. Implement a lightweight sidecar that monitors Redis for version changes and pushes dynamic Traefik configuration updates through the Docker API…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Configure Traefik rateLimit middleware with Redis as the store, quotas loaded from a Docker Config rate-config.json keyed by X-Tenant. Attach X-Tenant to all requests and enable policy hot-reload via",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests comprehensive TLS setup, mTLS implementation without external tools like Vault, feature flag management with Docker Config, and canary deployment strategies in a beginner-friendly Docker Swarm scenario.\n\n## Key Concepts\n\n- **TLS mutual authentication** with a lightweight in-cluster CA using OpenSSL\n- **Docker Secrets** for secure certificate storage and management\n- **Docker Config** for per-tenant feature flag configuration\n- **Traefik edge proxy** dynamic routing and weighted canary deployments\n- **Certificate rotation** strategies and automated renewal processes\n- **Simple bootstrap steps** with a minimal docker-compose skeleton\n\n## Code Example\n\n```bash\n# Bootstrap OpenSSL CA\n#!/bin/bash\n# Create CA\nopenssl req -new -x509 -days 365 -nodes -out ca.crt -keyout ca.key -subj \"/CN=My-CA\"\n\n# Create Traefik certs\nopenssl req -new -nodes -out traefik.csr -keyout traefik.key -subj \"/CN=traefik\"\nopenssl x509 -req -in traefik.csr -CA ca.crt -CAkey ca.key -CAcreateserial -out traefik.crt -days 365\n\n# Create API certs\nopenssl req -new -nodes -out api.csr -keyout api.key -subj \"/CN=api\"\nopenssl x509 -req -in api.csr -CA ca.crt -CAkey ca.key -CAcreateserial -out api.crt -days 365\n\n# Create Docker Secrets\ndocker secret create ca.crt ca.crt\ndocker secret create traefik.key traefik.key\ndocker secret create traefik.crt traefik.crt\ndocker secret create api.key api.key\ndocker secret create api.crt api.crt\n\n# Create feature flag config\necho '{\"tenant_a\": {\"premium_features\": true}, \"tenant_b\": {\"premium_features\": false}}' > feature-flags.json\ndocker config create feature-flags feature-flags.json\n```\n\n```yaml\n# docker-compose.yml\nversion: '3.8'\n\nservices:\n  traefik:\n    image: traefik:v2.10\n    command:\n      - --api.insecure=true\n      - --providers.docker=true\n      - --entrypoints.websecure.address=:443\n      - --entrypoints.websecure.http.tls=true\n      - --entrypoints.websecure.http.tls.options=default\n      - --entrypoints.websecure.http.tls.certResolver=myresolver\n      - --certificatesresolvers.myresolver.acme.tlschallenge=true\n      - --serversTransport.insecureSkipVerify=false\n    ports:\n      - \"443:443\"\n      - \"8080:8080\"\n    secrets:\n      - ca.crt\n      - traefik.key\n      - traefik.crt\n    volumes:\n      - /var/run/docker.sock:/var/run/docker.sock:ro\n    deploy:\n      replicas: 1\n\n  api:\n    image: my-api:latest\n    environment:\n      - TLS_CERT_PATH=/run/secrets/api.crt\n      - TLS_KEY_PATH=/run/secrets/api.key\n      - CA_CERT_PATH=/run/secrets/ca.crt\n    secrets:\n      - api.crt\n      - api.key\n      - ca.crt\n    configs:\n      - feature-flags\n    deploy:\n      replicas: 2\n      labels:\n        - \"traefik.enable=true\"\n        - \"traefik.http.routers.api.rule=Host(`api.example.com`)\"\n        - \"traefik.http.routers.api.entrypoints=websecure\"\n        - \"traefik.http.routers.api.tls=true\"\n        - \"traefik.http.routers.api.tls.options=default\"\n        - \"traefik.http.services.api.loadbalancer.server.port=8443\"\n\n  api-canary:\n    image: my-api:canary\n    environment:\n      - TLS_CERT_PATH=/run/secrets/api.crt\n      - TLS_KEY_PATH=/run/secrets/api.key\n      - CA_CERT_PATH=/run/secrets/ca.crt\n    secrets:\n      - api.crt\n      - api.key\n      - ca.crt\n    configs:\n      - feature-flags\n    deploy:\n      replicas: 1\n      labels:\n        - \"traefik.enable=true\"\n        - \"traefik.http.routers.api-canary.rule=Host(`api.example.com`)\"\n        - \"traefik.http.routers.api-canary.entrypoints=websecure\"\n        - \"traefik.http.routers.api-canary.tls=true\"\n        - \"traefik.http.services.api-canary.loadbalancer.server.port=8443\"\n        - \"traefik.http.services.api.loadbalancer.mirrors.api-canary.weight=10\"\n\nsecrets:\n  ca.crt:\n    external: true\n  traefik.key:\n    external: true\n  traefik.crt:\n    external: true\n  api.key:\n    external: true\n  api.crt:\n    external: true\n\nconfigs:\n  feature-flags:\n    external: true\n```\n\n## Implementation Steps\n\n1. **Initialize CA infrastructure** using OpenSSL commands\n2. **Generate and sign certificates** for both Traefik and API services\n3. **Create Docker Secrets** for secure certificate distribution\n4. **Configure Traefik** with TLS and mTLS settings\n5. **Set up canary deployment** with 10% weighted routing\n6. **Implement feature flags** using Docker Config for tenant-specific toggles\n7. **Deploy stack** and verify TLS handshake and mTLS enforcement",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-6",
        "questionId": "q-6133",
        "question": "In a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy. Implement a lightweight per-key rate limiter as a sidecar to enforce 60 requests/min per API key and a 15% canary to route a subset of keys to a new version; provide exact bootstrap commands and a docker-compose skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a bootstrap agent that activates on DC-join: pull Vault-provided CA bundles and SPIFFE trust anchors, rotate all SPIFFE SVIDs, and hot-reload edge proxies with the new material. Use",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two API versions (v1 and v2) sit behind Traefik with TLS termination. Traefik uses a weighted router to send 15% of traffic to v2 and 85% to v1, with weight distribution controlled by a Docker Config named canary-weight…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure a small policy layer in the API to filter responses by tenant. Mount Docker Config tenant-schemas.json at /config/tenant-schemas.json, parse into map[string][]string, and on each request rea",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Prototype a per-key rate limiter as a Docker sidecar in Python. The sidecar reads the X-API-Key header, tracks windowed counts in a tiny JSON file mounted as a volume, and returns 429 when the count e",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical setup: TLS Terminations via Nginx, a lightweight sidecar pattern, and a beginner-friendly canary workflow without external policy engines.\n\n## Key Concepts\n\n- Docker Compose wiring for multi-node, multi-DC setups\n- TLS termination at Nginx and service-to-edge security\n- Sidecar pattern to implement per-key rate limiting\n- Canary rollout basics with URL/param-based routing\n\n## Code Example\n\n```python\n# rate_limiter.py\nimport time, json\nSTATE_PATH = \"/shared/state.json\"\n\ndef load():\n    try:\n        with open(STATE_PATH) as f: return json.load(f)\n    except Exception:\n        return {}\n\ndef save(state):\n    with open(STATE_PATH, \"w\") as f: json.dump(state, f)\n\n\ndef allow(key, window=60, limit=60, state=None):\n    if state is None: state = load()\n    now = int(time.time())\n    bucket = state.get(key, [])\n    bucket = [t for t in bucket if now - t < window]\n    if len(bucket) >= limit: return False\n    bucket.append(now)\n    state[key] = bucket\n    save(state)\n    return True\n```\n\n## Follow-up Questions\n\n- How would clock drift affect accuracy and how to mitigate it?\n- How would you scale the limiter across many nodes?\n- What tests would you add to validate 15% canary behavior?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-7",
        "questionId": "q-2254",
        "question": "Create a local Docker Compose setup with two Python services: web (Flask API) and cache (Redis). Duplicate web as web_canary with CANARY=true. Add an Nginx container as a reverse proxy routing /api to web and /canary to web_canary. Use a named volume for Redis data. Add healthchecks for all three containers and provide a docker-compose.yml skeleton plus exact bootstrap commands to seed data and verify endpoints?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Sign all base images with cosign and verify in a dedicated 'sigver' sidecar before the app runs. Use Vault to issue SPIFFE SVIDs for services, and enforce an OPA policy loaded via Docker Config to gat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Four services: web, web_canary, cache, and nginx. web and web_canary expose ports and use CANARY to differentiate behavior. Redis uses a named volume redis-data for persistence. Nginx routes /api to w",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Initialize a 4-node Docker Swarm across two data centers and create an overlay network for cross-DC communication. Deploy the API stack with 3 production replicas and 1 canary replica, both configured with update_config…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Configure Traefik with two TLS routers, one for /v1 and one for /v2, routing decisions based on the X-Tenant header using a small middleware that rewrites the request path to the correct version; defa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a practical local deployment with a stable/canary pattern, data persistence, and a proxy layer.\n\n## Key Concepts\n\n- Docker Compose service composition\n- Health checks and readiness\n- Canary routing via a dedicated proxy\n- Volume for data persistence\n\n## Code Example\n\n```javascript\n// Placeholder illustrating structure for health checks\n```\n\n## Follow-up Questions\n\n- How would you promote canary to stable with zero downtime?\n- How would you extend to multiple canaries and metrics-based routing?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-8",
        "questionId": "q-3162",
        "question": "In a two-node docker-dca Swarm, deploy a TLS-enabled stateless API behind an Nginx edge proxy. Use Docker secrets for TLS certs, a Docker config for API endpoints, and a lightweight traffic-controller container that adjusts canary weights by updating a shared Nginx config mounted as a Docker config. Route 10% traffic to v2 initially, then scale to 100% per 15-second steps based on p95 latency measured via a small health-check endpoint; auto rollback to previous weight if latency exceeds 250ms for two consecutive checks. Provide exact docker commands and a docker-compose.yml skeleton, plus how to measure latency and trigger rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Python controller in Swarm that reads p95 latency from a Prometheus-compatible endpoint and gradually shifts Nginx upstream weights from 10/90 to 0/100 in 15s steps. Use Docker secrets for",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Mint per-tenant client certs via Vault PKI; store a per-tenant CA in a Docker Config. Traefik enforces mTLS with tenant CA; the API caches certs and hot-reloads on Vault webhook or short poll, avoidin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Vault PKI to mint tenant-specific TLS certs and a Vault Agent sidecar in each API container to fetch/renew certs with a short TTL (e.g., 1h). Traefik uses a Vault-backed certResolver, keyed b",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bootstrap a Traefik TLS setup with docker-dca; create a small Express API that reads X-Tenant, loads tenant-flags from a Docker Config (JSON: {tenantId: true|false}), and exposes /feature only if the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design dynamic traffic routing with strict latency guardrails in a small Swarm, plus integration of Docker secrets/configs and live config reloads.\n\n## Key Concepts\n- Swarm-based Canary with external controller\n- Nginx upstream weight management via Docker Configs\n- TLS secrets for mTLS-like security\n- Latency-driven rollback and health checks\n\n## Code Example\n```bash\n# example commands showing secret/config creation and stack deploy\ndocker swarm init\ndocker secret create tls_api path/to/tls.pem\ndocker secret create tls_key path/to/key.pem\ndocker config create upstreams.yaml /dev/null\n# deploy stack with nginx, api-v1, api-v2, traffic-controller\n```\n\n## Follow-up Questions\n- How would you ensure idempotent config updates?\n- How to validate rollback safety under burst traffic?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-9",
        "questionId": "q-11889",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster. Implement a cross-DC, per-tenant Redis cache with 60s TTL and stale-while-revalidate behavior; cache keys are namespaced by tenant, and a central events service publishes cache-invalidation events when tenant data changes. Ensure zero-downtime cache swaps and proper TLS handling. Provide bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Redis Cluster with per-tenant namespaces using the pattern 'tenant:<id>', configure 60-second TTL with stale-while-revalidate behavior via Lua scripts that serve stale data while asynchronously refreshing on…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-tenant TLS certs issued by Vault PKI, Traefik configured for SNI routing by X-Tenant; certificates mounted per tenant and reloaded dynamically via Traefik's file provider. Maintain a Consul KV rev",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an edge-driven per-tenant quota system using a programmable data plane (eBPF/Cilium) within a three-node Docker-DCA cluster spanning two data centers. Enforce granular CPU, memory, and network budgets at ingre…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement Vault envelope encryption with per-tenant data keys: Vault generates DEKs wrapped by KEKs for each tenant, a data-crypto sidecar fetches and decrypts payloads at the API boundary with 24-hour rotation and zero…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests distributed caching strategy across data centers, evaluating consistency, failover handling, and operational reliability under TLS constraints.\n\n## Key Concepts\n- Cross-data center Redis clustering with per-tenant key namespacing\n- Stale-while-revalidate semantics implemented through Lua scripts\n- Cache invalidation via central events service and Redis pub/sub\n- TLS/mTLS configuration with Nginx edge proxy in docker-dca environment\n- Bootstrap orchestration and docker-compose infrastructure scaffolding\n\n## Code Example\n```lua\n-- Stale-while-revalidate function for cache refresh\nlocal key = KEYS[1]\nlocal ttl = ARGV[1]\nlocal current = redis.call('GET', key)\nif current then\n  return current\nelse\n  local stale_key = key .. ':stale'\n  local stale = redis.call('GET', stale_key)\n  if stale then\n    redis.call('SETEX', key, ttl, stale)\n    return stale\n  end\n  return nil\nend\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-10",
        "questionId": "q-19316",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant feature flags: tenant flags are stored in a Docker Config mapping tenantId to featureName->enabled. The API should load flags per-tenant, cache them with a per-tenant TTL, invalidate on update via a Redis pub/sub channel 'tenant-flags'. Changes from the central policy store must propagate within 2 seconds. Bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant canary controller that writes status to Redis (key: canary:tenant:<id>, fields enabled, percentage). Traefik’s dynamic config is updated by a small sidecar reading Redis and rou",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Instrument a three-node docker-dca cluster with TLS behind Nginx and end-to-end tracing via OpenTelemetry. Run an OpenTelemetry Collector as a sidecar on each node, emit traces from the API, export to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a three-node Swarm deployment using TLS secrets, a config for endpoints, an Nginx proxy, an API service, and a lightweight compliance sidecar that UDPs audit data to a collector with no measur",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a per-tenant feature-flag store in Docker Config (tenantId → feature → bool) plus an in-process per-tenant LRU cache with 2–5 minute TTL in the API. A lightweight Flag/Policy service reads the Docker Config, pu…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design distributed feature-flag propagation with per-tenant isolation across data centers. It covers Docker Config for tenant flags, per-tenant in-process caching with TTL, Redis Pub/Sub for invalidation and cross-DC propagation, and edge security with TLS termination at Traefik.\n\n## Key Concepts\n- Docker Config for tenant-specific flag storage\n- Per-tenant in-process cache with configurable TTL\n- Redis Pub/Sub for cache invalidation and propagation\n- Lightweight policy/flag service coordinating across DCs\n- TLS termination at Traefik edge proxy\n\n## Code Example\n```javascript\nclass FlagCache {\n  constructor(redis, ttlSeconds = 120) {\n    this.cache = new Map();\n    this.ttlSeconds = ttlSeconds;\n    this.redis = redis;\n    \n    redis.subscribe('tenant-flags', (err, count) => {\n      if (err) console.error('Subscribe error:', err);\n    });\n    \n    redis.on('message', (channel, message) => {\n      if (channel === 'tenant-flags') {\n        const { tenantId } = JSON.parse(message);\n        this.cache.delete(tenantId);\n      }\n    });\n  }\n\n  async getFlag(tenantId, feature) {\n    const key = `${tenantId}:${feature}`;\n    const entry = this.cache.get(key);\n    \n    if (entry && Date.now() < entry.expiresAt) {\n      return entry.value;\n    }\n    \n    const value = await this.redis.hget(`flags:${tenantId}`, feature);\n    this.cache.set(key, { value: JSON.parse(value), expiresAt: Date.now() + this.ttlSeconds * 1000 });\n    return JSON.parse(value);\n  }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-11",
        "questionId": "q-30174",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement a per-tenant token-bucket rate limiter using Redis with keys derived from X-Tenant and a Lua script for atomic token fetch/consume; expose an admin endpoint to adjust caps without redeploy. Bootstrap initial quotas via a Docker Config and load into both DCs. Ensure cross-DC routing with zero-downtime quota updates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Init Swarm on DC-A; join DC-B; create overlay api-net; deploy two services: api-dcA with 2 replicas constrained to DC-A and api-dcB with 1 replica constrained to DC-B, both on api-net and update_confi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Deploy a TLS-enabled API behind Traefik in a Docker DCA cluster, instrument all services with OpenTelemetry, add a central OTLP collector, configure OTLP exporters to point to the collector, propagate trace context via…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant token-bucket rate limiter using Redis with keys derived from X-Tenant. Use a Lua script for atomic token fetch/consume; expose an admin endpoint to adjust caps without redeploy.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Deploy a per-service OpenTelemetry sidecar in each DC that exports OTLP spans to a central TLS-enabled Jaeger collector. Drive per-tenant sampling from Docker Config trace-config and inject X-Tenant a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world QoS control across DCs, using per-tenant keys, Redis Lua scripting for atomicity, and zero-downtime quota updates via Docker Config.\n\n## Key Concepts\n- Token bucket rate limiting per tenant\n- Redis Lua scripting for atomic operations\n- Docker Config bootstrap for quotas\n- Traefik TLS termination and multi-DC routing\n- Live quota adjustments without redeploys\n\n## Code Example\n```lua\n-- Redis Lua script (simplified) to atomically fetch/consume a token\nlocal key = KEYS[1]\nlocal rate = tonumber(ARGV[1])\nlocal burst = tonumber(ARGV[2])\nlocal now = tonumber(ARGV[3])\nlocal tokens = tonumber(redis.call('GET', key) or burst)\nif tokens < 1 then\n  return -1\nend\nredis.call('DECR', key)\nredis.call('EXPIREAT', key, now + rate)\nreturn tokens - 1\n```\n\n## Follow-up Questions\n- How would you handle clock skew across DCs?\n- What observability would you add for per-tenant quotas and violations?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-12",
        "questionId": "q-20346",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant rate-limiting via a distributed token-bucket using Redis per-tenant namespaces, with burst handling and automatic eviction. Expose an admin endpoint to pause/resume tenants and adjust rates via Docker Config in real time. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Init swarm on node A: docker swarm init --advertise-addr <A>; join B/C: docker swarm join --token <tok> <A>:2377; overlay: docker network create -d overlay --attachable app-net; deploy: docker service",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Redis-backed per-tenant token-bucket with a Lua script for atomic refill/consume; enforce via a Traefik middleware that checks the bucket before routing. Manage per-tenant rates and pause via Do",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a central tenant-version map in Consul KV (or Redis), a per-tenant sidecar that reads X-Tenant and routes to blue or green through Traefik, enabling on-the-fly tenant pinning without redeploys",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Vault Agent sidecar that auto-renews and writes certs to a shared /certs volume. Configure the API and PG client to reference these certs on startup and reload the pool with SIGHUP when Vault si",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design distributed rate limiting across DCs with per-tenant controls and edge enforcement. Highlights trade-offs: Redis replication latency, Lua script atomics, and failure handling.\n\n## Key Concepts\n- Per-tenant Redis namespaces and Lua-based token bucket\n- Traefik middleware edge enforcement\n- Docker Config for dynamic per-tenant rates and operators\n- Cross-DC replication, failover, TLS/mTLS\n\n## Code Example\n```lua\n-- Redis Lua script (atomic token bucket)\nlocal bucket = KEYS[1]\nlocal now = tonumber(ARGV[1])\nlocal rate = tonumber(ARGV[2])\nlocal capacity = tonumber(ARGV[3])\nlocal tokens = tonumber(redis.call('GET', bucket) or capacity)\ntokens = math.min(capacity, tokens + rate)\nif tokens >= 1 then\n  redis.call('SET', bucket, tokens-1, 'PX', 60000)\n  return 1\nelse\n  return 0\nend\n```\n\n## Follow-up Questions\n- How would you test bursty traffic and DC failover under load?\n- How would you handle Redis unavailability or clock skew between DCs?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-13",
        "questionId": "q-3283",
        "question": "In a three-node docker-dca cluster, deploy a TLS-enabled API behind an Nginx gateway using mutual TLS. Secrets: CA and API certs as Docker secrets; a Vault-backed secret-provider sidecar rotates short-lived MTLS creds with zero latency. Add a lightweight tracing sidecar to ship spans non-blockingly. All services on one overlay network. Implement zero-downtime rotation with health checks and automatic rollback on failure. Provide docker-compose skeleton and bootstrap steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an etcd-backed per-tenant routing map. A sidecar watches etcd, regenerates an nginx fragment for the tenant, writes it to a shared mount, and issues a graceful nginx reload. Ensure raft-backed etc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Mutual TLS between Nginx and the API using CA and service certs as Docker secrets (ca.pem, api.crt/api.key). Add a Vault-backed secret-provider sidecar to rotate short-lived MTLS creds with zero laten",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement dynamic traffic shadow: deploy a main API and a canary, with a shadow Envoy sidecar mirroring ~5% of traffic to the canary; TLS via Docker secrets; Nginx routes 95% to main and 5% to shadow;",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a tenant-scoped in-process cache complemented by a Redis namespace per tenant. Cache keys: tenant:path. On mutating requests, publish an invalidation message on a tenant-specific channel and have",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests mastery of secure service-to-service communication, secret rotation, and non-disruptive deployments in docker-dca. Emphasizes Safer-by-design patterns and observable behavior under rotation.\n\n## Key Concepts\n- Mutual TLS between gateway and API with Docker secrets\n- Vault-based secret provisioning for dynamic cert rotation\n- Sidecar patterns: tracing and credential rotation with no request latency\n- Overlay networking, zero-downtime updates, health-check driven rollback\n- Bootstrap steps: swarm init/join, secret/config creation, compose deploy\n\n## Code Example\n```yaml\nversion: \"3.8\"\nservices:\n  nginx:\n    image: nginx:stable-alpine\n    ports:\n      - \"443:443\"\n    secrets:\n      - source: ca.pem\n        target: ca.pem\n    configs:\n      - source: api-endpoints\n        target: /etc/nginx/conf.d/api-endpoints.conf\n  api:\n    image: myorg/api:latest\n    secrets:\n      - source: ca.pem\n      - source: api.crt\n      - source: api.key\n    deploy:\n      replicas: 2\n  secret-provider:\n    image: vault-bridge:latest\n    environment:\n      - VAULT_ADDR=http://vault:8200\n    depends_on:\n      - vault\n  tracing:\n    image: tracing-collector:latest\n    depends_on:\n      - api\n```\n\n## Follow-up Questions\n- How would you verify rotation safety under peak traffic?\n- What metrics would you collect to detect failed rotations early?\n- How would you handle secret revocation if a node goes offline?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-14",
        "questionId": "q-10398",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster. Implement a health-based rollback: if latency 95th percentile > 300ms or error rate > 2% for 3 consecutive minutes, automatically roll back to the previous image version; provide bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A migrator serializes per-tenant migrations using a Redis lock with key `tenant:<id>:migrate`, applies the migration to the tenant database in the home DC, and records the applied version. Migrations are stored in a cen…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Describe configuring Traefik with two services (baseline and canary) and a per-tenant weight map in Docker Config. Explain wiring a tenant-aware router that uses Headers to route to the correct servic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: Vault issues new TLS certificate and database credential; create new Swarm secrets (e.g., api-tls-v2, db-cred-v2); perform service updates with `docker service update --secret-add` then `--secret-rm`, using update…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a rollback controller that polls Prometheus every 30s for p95 latency and error rate. If p95>300ms or errors>2% for 3 consecutive windows, roll back to the previous image tag and restart ngi",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to instrument simple rollback logic using existing observability (Prometheus) and container orchestration in a docker-dca setting. It verifies practical rollout safety without requiring full K8s.\n\n## Key Concepts\n\n- Basic metrics-driven rollbacks\n- Prometheus querying and thresholds\n- docker-compose as a skeleton for multi-DC setups\n- Reproducible image tagging and restart procedures\n\n## Code Example\n\n```yaml\nversion: '3.8'\nservices:\n  api:\n    image: myapi:stable\n  nginx:\n    image: nginx:latest\n  rollback-svc:\n    image: rollback-controller:latest\n  prometheus:\n    image: prom/prometheus:latest\n```\n\n## Follow-up Questions\n\n- How would you alert on rollback events?\n- How would you adapt thresholds for traffic bursts?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-15",
        "questionId": "q-21558",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement per-tenant observability: enable OpenTelemetry with a Jaeger backend and per-tenant sampling rates configurable via Docker Config; ensure traces are correlated across DCs and visible in a central UI; provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy a Redis-backed per-tenant token bucket with keys in the format tenant:<X-Tenant>:bucket. Configure token refill at a defined interval, and implement a sidecar middleware that validates the X-Tenant header, decrem…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Configure per-tenant observability across two DCs: TLS API behind Traefik, OpenTelemetry with a Jaeger backend as a central collector; per-tenant sampling rates controlled via Docker Config (tenant-sa",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Fetch tenant flags from per-tenant Docker Configs named feature-flags-<tenant>.json using X-Tenant in requests, expose a small in-memory cache with TTL, and fallback to global defaults if missing. Boo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant feature flag service injected via Docker Config, read with tenant header X-Tenant; Traefik routes to API behind TLS. The API queries the flag service for the current tenant and decide",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cross-DC observability with real-world constraints: TLS termination at the edge, per-tenant sampling control, and trace correlation across environments. Requires understanding of OpenTelemetry, Jaeger, Docker Config, and traffic routing implications.\n\n## Key Concepts\n\n- OpenTelemetry and Jaeger integration across multiple data centers\n- Docker Config for per-tenant configuration\n- Traefik TLS edge with correct header propagation for tracing\n- Cross-DC trace correlation using tenant identifiers (X-Tenant)\n\n## Code Example\n\n```yaml\n# Minimal docker-compose skeleton (example)\nversion: '3.8'\nservices:\n  api:\n    image: myapi:latest\n  jaeger:\n    image: jaegertracing/all-in-one\n    environment:\n      COLLECTOR_OTLP_ENABLED: 'true'\n```\n\n## Follow-up Questions\n\n- How would you handle dynamic changes to per-tenant sampling without restart?\n- How do you prevent trace data from tenants with strict residency from leaking between DCs?",
        "difficulty": "intermediate"
      },
      {
        "id": "docker-dca-q-16",
        "questionId": "q-16843",
        "question": "In a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy. Implement per-tenant feature flags using Open Policy Agent (OPA) with policies stored in Vault-backed policy store; cache decisions in Redis with per-tenant TTL, and provide a canary rollout of a new feature gated by latency targets and automatic rollback. Provide exact bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Wasmtime-based policy sidecar attached to each API pod, loading per-tenant policies from Vault and rotating SPIFFE SVIDs. Enforce: block DC-B egress unless tenant opt-in exists, redact PII in ou",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two API versions v1 and v2 behind Traefik in two-DC docker-dca. Rollout-config.json in a Docker Config maps tenant to canary%.\\nA lightweight canary-decider reads this per-tenant map and routes to v2",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a zero-downtime SPIFFE SVID rotation using Vault with short TTLs; deploy per-tenant Envoy sidecars that enforce Consul KV-stored data residency policies and route to tenant-scoped overlay networks; configure t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Traefik terminates TLS with certs rotated by Vault. API loads OPA policies from a Vault-backed policy store; decisions cached in Redis per-tenant with short TTL. Canary deploys v2 to 25% traffic; moni",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nTests ability to integrate policy-as-code with runtime feature flags across DCs, using Vault for secrets/policies, OPA for decisions, and Redis for caching. Also exercises canary strategies, monitoring, and rollback in a docker-dca context.\\n\\n## Key Concepts\\n- Policy-as-code with OPA (Vault-backed policies) \\n- Per-tenant caching and TTLs in Redis\\n- Canary rollout controls and automatic rollback\\n- TLS management with Vault; multi-DC routing with Traefik\\n\\n## Code Example\\n```rego\\npackage features\\n\\ndefault allow = false\\n\\n# allow if tenant has 'new_v2' feature flag enabled\\nallow = data.tenants[tenant].features[_] == \\\"new_v2\\\"\\n```\\n\\n## Follow-up Questions\\n- How would you handle policy cache invalidation on policy updates?\\n- How to test canary rollback under synthetic load?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-17",
        "questionId": "q-28833",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement tenant-scoped distributed tracing: run an OpenTelemetry Collector per DC, propagate a per-tenant tag via the trace context, route traces to a central OTLP endpoint, and apply per-tenant sampling rates controlled by a Docker Config. Include bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Bootstrap a Traefik TLS setup with docker-dca; create a small Express API that reads X-Tenant, loads tenant-flags from a Docker Config (JSON: {tenantId: true|false}), and exposes /feature only if the",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Consul Connect-based service mesh with per-tenant service identities. Enable mTLS between Traefik and backend services using Envoy sidecars. Store per-tenant authorization policies in Consul KV and leverage…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy an Envoy sidecar alongside your API across two data centers, behind a Traefik edge proxy. Configure it to read a Docker Config named `tenant_region` that maps tenant IDs to their preferred data center, then rewri…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Instrument OpenTelemetry in both DCs, propagate tenant as a trace attribute via X-Tenant, and configure per-tenant sampling (1.0 for critical tenants, 0.1 otherwise) in a Docker Config. Run an OTLP co",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design cross-DC observability with tenant isolation and controlled overhead. Focus on tracing integration, context propagation, and secure transport in a docker-dca surface.\n\n## Key Concepts\n- OpenTelemetry per-tenant tracing\n- Trace attributes vs baggage\n- Per-tenant sampling policy\n- Central OTLP/Jaeger backend\n- TLS in cross-DC pipelines\n\n## Code Example\n```yaml\nservice:\n  pipelines:\n    traces:\n      receivers: [otlp]\n      exporters: [jaeger]\n```\n\n## Follow-up Questions\n- How would you secure the OTLP data in transit?\n- How would you handle tenant churn affecting traces?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-18",
        "questionId": "q-862",
        "question": "In a Docker Swarm with a stateful web app that uses Postgres, you must roll out version 3.2 with a DB schema migration and zero downtime. Propose a concrete upgrade plan that uses a start-first, one-task-at-a-time update, a separate migration container, and post-migration validation. Include exact Swarm commands and a minimal docker-compose snippet showing update_config?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a zero-downtime SPIFFE SVID rotation using Vault with short TTLs; deploy per-tenant Envoy sidecars that enforce Consul KV-stored data residency policies and route to tenant-scoped overlay networks; configure t…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a tenant-aware OpenTelemetry pipeline: TLS at Traefik, forward X-Tenant to services; each service emits OTLP traces to a collector, which exports to Jaeger with per-tenant namespaces. Drive",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: run DB migration first in a dedicated one-off container, then perform a start-first, one-task-at-a-time web upgrade. Commands: docker run --rm --network swarm_net migrate:3.2; docker service upd",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Leverage Vault PKI to mint tenant-specific TLS certs and a Vault Agent sidecar in each API container to fetch/renew certs with a short TTL (e.g., 1h). Traefik uses a Vault-backed certResolver, keyed b",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to coordinate schema migrations with zero downtime, manage Swarm update strategies, and handle rollback safely.\n\n## Key Concepts\n- Start-first updates and parallelism control\n- Separate migration container orchestration\n- Post-migration validation and rollback\n- Health checks and observability during upgrade\n\n## Code Example\n```yaml\nversion: '3.8'\nservices:\n  web:\n    image: myapp:3.2\n    deploy:\n      update_config:\n        parallelism: 1\n        delay: 15s\n        order: start-first\n```\n\n## Follow-up Questions\n- How would you handle long-running migrations?\n- How ensure data consistency with read replicas during upgrade?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-19",
        "questionId": "q-29216",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Add an OPA-based per-tenant authorization layer that pulls policy from a central store and supports live updates without restarts. Integrate Vault for dynamic tenant creds and Redis-backed per-tenant rate limiting. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Explain how you would implement tenant-specific A/B routing with Traefik and a Redis-backed Experiment service, enabling per-tenant config updates without redeploys, including canary thresholds, healt",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Vault PKI solution integrated with HSM via PKCS#11 to issue per-tenant SPIFFE SVIDs. Deploy edge verification using a verifier sidecar to validate SVIDs before requests reach the application. Implement envel…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an OPA-powered per-tenant authorization layer in a two-DC docker-dca setup. TLS API behind Traefik; OPA sidecar loads policy from Consul KV, watches for changes, and hot-reloads without rest",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Build a lightweight quota sidecar (Go/JS) that loads per-tenant quotas from YAML via Docker Config, watches for changes, and enforces per-tenant token bucket. Read tenant from header X-Tenant, return",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests integration of policy-as-code, live config reload, and secure secret management across DCs. It also evaluates how candidates design to avoid restarts while updating policies and credentials.\n\n## Key Concepts\n\n- OPA policy management and hot-reload\n- Traefik TLS termination\n- Consul KV integration for policies\n- Vault dynamic credentials per tenant\n- Redis per-tenant rate limits\n\n## Code Example\n\n```javascript\n// Placeholder for candidate's approach overview\n```\n\n## Follow-up Questions\n\n- How would you test policy hot-reload correctness across DCs?\n- What are failure modes for Vault credential rotation affecting ongoing requests?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-20",
        "questionId": "q-12372",
        "question": "In a three-node docker-dca cluster across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy. Add a provenance guard sidecar that uses Cosign to validate the running image against a per-tenant allowlist fetched from a central policy broker; Vault issues per-tenant signing keys and SPIFFE SVID rotation; enable a 20% canary rollout with SLA latency targets and automatic rollback. Provide exact bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-tenant image provenance guard as a sidecar that uses Cosign to verify the running image against a Vault-backed per-tenant digest list from a central policy broker, with automatic SPIFFE SVID rotation for…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "A possible answer would outline: init a swarm across both data centers, create an overlay network, load TLS secrets and API configs, deploy the app with a sidecar SPIRE agent container, configure SPIR",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a tenant-migration workflow with a dedicated sidecar that subscribes to the tenant-mapping service, updates the tenant overlay network, performs graceful drain, triggers Vault SPIFFE SVID ro",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To solve this, terminate TLS at the edge, enable mTLS inside the cluster via Vault-issued SPIFFE SVIDs, and run a cert-verifying sidecar next to each service. The OPA policy, stored in a Docker Config",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical integration of image provenance, per-tenant isolation, and production-safe canaries in docker-dca.\n\n## Key Concepts\n- Cosign image signing/verification\n- Vault per-tenant keys and SPIFFE SVID rotation\n- Central policy broker for per-tenant allowlists\n- Canary deployments with SLA-based rollback\n\n## Code Example\n```javascript\nasync function verifyRunningImage(tenantId, digest) {\n  const allow = await fetchAllowlist(tenantId); // mTLS\n  if (!allow.includes(digest)) throw new Error('Untrusted image');\n  await cosignVerify(digest, vaultKeyForTenant(tenantId));\n}",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-21",
        "questionId": "q-10313",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster. Implement a per-tenant feature flag that enables a new /api/v2 endpoint for a subset of tenants. Store flags in Redis (one key per tenant). Use Nginx + Lua to query Redis for tenant flag (from header X-Tenant-Id) and route to /v2 when enabled, else /v1. Provide bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an OpenTelemetry setup where each API pod includes an OTLP exporter pointing to a central collector, with a middleware that adds X-Tenant to trace attributes; use a Docker Config per-tenant sa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Route tenants based on Redis-backed feature flags using Nginx with Lua scripting. Extract the X-Tenant-Id header from incoming requests, query Redis for that tenant's flag value, and dynamically route to /api/v2 when th…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Onboard a tenant by provisioning an envelope key and a short-lived TLS cert in Vault, creating a tenant-scoped overlay network, and pushing a dynamic Nginx config via a watcher sidecar. Implement 24h",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy a Redis-backed per-tenant token bucket with keys in the format tenant:<X-Tenant>:bucket. Configure token refill at a defined interval, and implement a sidecar middleware that validates the X-Tenant header, decrem…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests your ability to implement dynamic routing capabilities, configure edge proxies, and maintain cross-data center consistency using Docker containerization concepts—all critical skills for modern distributed systems.\n\n## Key Concepts\n- **Nginx with Lua scripting**: Enables runtime routing decisions based on dynamic conditions\n- **Per-tenant feature flags**: Stored in Redis with one key per tenant for granular control\n- **Cross-data center TLS termination**: Ensures secure communication with shared certificate management\n- **Docker Compose orchestration**: Provides a skeleton for multi-service deployment across data centers\n\n## Code Example\n\n```nginx\nhttp {\n  lua_shared_dict flags 1m;\n  server {\n    location /api/ {\n      access_by_lua_block {\n        local tenant = ngx.var.http_x_tenant_id\n        local redis = require \"resty.redis\"\n        local red = redis:new()\n        \n        red:set_timeout(1000)\n        local ok, err = red:connect(\"redis\", 6379)\n        if not ok then\n          ngx.log(ngx.ERR, \"failed to connect to redis: \", err)\n          return ngx.exit(500)\n        end\n        \n        local flag, err = red:get(\"flag:\" .. tenant)\n        if flag == \"1\" then\n          ngx.req.set_uri(\"/api/v2\" .. ngx.var.uri:sub(5))\n        else\n          ngx.req.set_uri(\"/api/v1\" .. ngx.var.uri:sub(5))\n        end\n      }\n    }\n  }\n}\n```\n\nThis implementation demonstrates enterprise-grade routing with feature flag management, ensuring zero-downtime deployments and tenant-specific feature rollouts.",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-22",
        "questionId": "q-25092",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Implement tenant-scoped rate limiting via a central RateLimit service backed by Redis, with per-tenant quotas provided through a Docker Config and enforced by a Traefik middleware that can hot-reload policy without restarts. Validate by simulating traffic bursts from multiple tenants and proving isolation. Provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy a TLS API behind Nginx in docker-dca and add per-tenant rate limiting using Nginx limit_req with a small JSON file mounted into the edge to map Tenant to rate. Tenant is identified by X-Tenant;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Redis-backed token bucket per tenant with Lua scripting to atomically check and consume tokens; deploy Redis Cluster across DCs for availability; read quotas from docker-config tenant-quota on s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure Traefik rateLimit middleware with Redis as the store, quotas loaded from a Docker Config rate-config.json keyed by X-Tenant. Attach X-Tenant to all requests and enable policy hot-reload via",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create a docker-compose.yml with api, redis, and fluent-bit services on one host, plus a shared logs volume. The API writes to /var/log/app/app.log; Fluent Bit tails that file and forwards to stdout.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design per-tenant QoS with dynamic config in a multi-DC setup.\n\n## Key Concepts\n- Traefik RateLimit middleware, Redis store, per-tenant quotas, Docker Config\n- TLS termination, hot-reload without restart, cross-DC coordination\n- Request tagging with X-Tenant and validation of isolation\n\n## Code Example\n```javascript\n// Pseudo middleware sketch: extract tenant and enforce quota from Redis\nasync function enforceTenantRate(req, res, next){\n  const tenant = req.headers['x-tenant'];\n  const quota = await redisGetQuota(tenant);\n  if (quota.allow(req)) return next();\n  res.status(429).send('Too Many Requests');\n}\n```\n\n## Follow-up Questions\n- How would you audit cross-DC quota drift and recover from Redis replication lag?\n- How would you test hot-reload behavior under config churn?",
        "difficulty": "advanced"
      },
      {
        "id": "docker-dca-q-23",
        "questionId": "q-13519",
        "question": "Across two data centers, deploy a TLS-enabled API behind an Nginx edge proxy in a docker-dca cluster. Add a lightweight per-tenant idempotency guard as an in-memory sidecar that caches (tenant, id) with a 30s TTL and returns 409 on duplicates. The API should read X-Tenant and X-Idempotency-Key and the sidecar should log decisions. Provide bootstrap commands and a docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deploy TLS-enabled API behind Nginx edge proxy with a second shadow API. Mirror 5% of requests to the shadow via Nginx mirror, keeping live path on the primary. Use Docker secrets for TLS certs and a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a lightweight per-tenant idempotency guard as an in-memory sidecar between Nginx and the API. Cache (tenant, id) with a 30s TTL; read X-Tenant and X-Idempotency-Key; on first hit forward to API",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Two API versions v1 and v2 behind Traefik in two-DC docker-dca. Rollout-config.json in a Docker Config maps tenant to canary%.\\nA lightweight canary-decider reads this per-tenant map and routes to v2",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a TLS-terminated edge using Nginx, fronting a primary API and a shadow version. Add Nginx mirror directive to send 5% of requests to the shadow service while the primary path remains unchang",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess understanding of per-tenant isolation, sidecar middleware placement, and idempotency semantics in docker-dca. The scenario mirrors real-world issues with retry storms and duplicate requests.\n\n## Key Concepts\n- In-memory idempotency cache scoped by (tenant, id) with TTL\n- Sidecar pattern between edge proxy and API\n- Header extraction (X-Tenant, X-Idempotency-Key) and forward logic\n- docker-compose skeleton and bootstrap workflow\n- Basic validation tests for idempotency behavior\n\n## Code Example\n```python\ncache = {}\ndef handle(req):\n  tenant = req.headers['X-Tenant']\n  key = req.headers['X-Idempotency-Key']\n  now = time.time()\n  if (tenant, key) in cache and cache[(tenant, key)] > now:\n    return 409\n  cache[(tenant, key)] = now + 30\n  forward_to_api(req)\n```\n\n## Follow-up Questions\n- How would you scale the cache across multiple sidecar instances?\n- What failure modes can break idempotency and how to mitigate (clock skew, TTL drift)?",
        "difficulty": "beginner"
      },
      {
        "id": "docker-dca-q-24",
        "questionId": "q-22000",
        "question": "Across two data centers, deploy a TLS-enabled API behind a Traefik edge proxy in a docker-dca cluster. Expose a per-tenant Server-Sent Events stream at /stream. Implement dynamic per-tenant concurrency limits loaded from a Docker Config named tenant-streams, with quotas hot-reloaded at runtime. Reject new streams with 429 when at limit; expose per-tenant metrics at /metrics; provide bootstrap commands and a minimal docker-compose.yml skeleton?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Show a plan to deploy Vault Transit with per-tenant envelope keys, a crypto sidecar that decrypts/encrypts payloads at the edge, and key rotation with zero downtime. Nginx validates SPIFFE SVIDs via a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-tenant streaming gate backed by Redis for current counts, with a small in-process limiter that loads the tenant-streams config from Docker Config and hot-reloads on change. Validate on ea",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I would front the two API versions with TLS-terminated Nginx. A central policy engine (OPA) evaluates per-tenant flags and returns allowed routes. Requests with the flag go to api-v2; others go to api",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bootstrap two backends (backend-v1, backend-v2) and a Traefik edge with TLS. Maintain a Docker Config tenant-backends.json that maps each tenant_id to v1 or v2. A lightweight bootstrap script generate",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design dynamic per-tenant streaming quotas, runtime-config reloading, SSE handling, and multi-DC deployment with Traefik TLS.\n\n## Key Concepts\n\n- Server-Sent Events, per-tenant concurrency limits, Docker Config, Redis-backed counters, hot-reload config, OpenTelemetry tracing, TLS at edge.\n\n## Code Example\n\n```javascript\n// sample Lua script for atomic check/incr\nlocal tenant = KEYS[1]\nlocal limit = tonumber(ARGV[1])\nlocal active = redis.call('GET', 'streams:' .. tenant) or 0\nif tonumber(active) >= limit then\n  return 0\nelse\n  redis.call('INCR', 'streams:' .. tenant)\n  redis.call('EXPIRE', 'streams:' .. tenant, 60)\n  return 1\nend\n```\n\n## Follow-up Questions\n\n- How would you test hot-reload correctness under rapid config changes?\n- What are failure modes if Redis is partitioned or slow to respond, and how would you mitigate them?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-07T09:27:45.404Z",
    "version": 1
  }
]