[
  {
    "id": "test-machine-learning",
    "channelId": "machine-learning",
    "channelName": "Machine Learning",
    "title": "Machine Learning Knowledge Test",
    "description": "Test your understanding of Machine Learning with 25 questions.",
    "questions": [
      {
        "id": "machine-learning-q-0",
        "questionId": "q-26244",
        "question": "Design an end-to-end real-time pricing and availability forecasting system for a cloud marketplace handling 1M users and 1000 features, with 120 ms CPU latency, online learning, drift detection, calibration, feature-store versioning, and regional fairness constraints; outline architecture, data splits, metrics, and rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-stage real-time pricing: Stage 1 a fast per-item predictor (quantized features, LightGBM/GBDT) meeting 120 ms CPU latency. Stage 2 region-aware calibration and fairness enforcement via constrained",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Split by time (80/10/10) to mimic streaming. Use TF-IDF with unigrams+bigrams; train Logistic Regression (C=1.0, liblinear). Calibrate with Platt scaling; select threshold to maximize F1 at target pre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a compact two-tower on-device recommender: 32–64 item embeddings, a small user embedding, 8-bit quantization, and a light MLP. Target sub-5 ms on modern mobile CPUs; weights under ~300 KB. Federated learning with…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Encode user_id with a hashing trick to a fixed number of bins (e.g., 2^20); one-hot or target-encode device_type; numeric features kept as-is after imputation; split by time to avoid leakage across se",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design latency-aware, multi-objective ML systems with fairness and drift handling in a production-like setting.\n\n## Key Concepts\n- Real-time inference latency constraints\n- Multi-objective optimization and fairness constraints\n- Online learning with partial_fit\n- Concept drift detection (ADWIN)\n- Feature stores and versioning\n- Guardrails for live experimentation\n\n## Code Example\n```javascript\nclass OnlinePricingModel {\n  constructor() { /* init */ }\n  partialFit(X, y) { /* update with streaming samples */ }\n  predict(X) { /* fast inference */ }\n}\n``` \n\n## Follow-up Questions\n- How would you monitor model drift in real time?\n- How would you design rollback and latency-violation safeguards?",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-1",
        "questionId": "q-11815",
        "question": "Design a privacy-preserving, real-time fraud-detection system for a mobile banking app that runs partly on-device (per-transaction <30 ms on mid-range CPUs) and partly in the cloud for refinement. Use feature hashing and a small on-device neural model (~1M params), plus a lightweight cloud gradient-boosted tree for refinement. Include data schema, model choices, latency verification, deployment, and drift handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a federated fraud detector with per-region CPU models meeting sub-150ms latency. Each region trains locally on its data and periodically contributes encrypted model updates to a central coordinator. The global ag…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a two-tier streaming pipeline: extract lightweight features from metrics, logs, and traces; fuse with a compact attention head and allow early exits. Implement ONNX/torchscript runtimes with mixed",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "On-device first: privacy-preserving and low latency; target <30 ms per transaction on mid-range CPUs. Use feature hashing for high-cardinality features, a small on-device neural model (~1M params), an",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Hybrid: compute lightweight features on-device (device fingerprint, recent auth failures, tap cadence, location jitter), hash/mask and send to server; run a compact, quantized LightGBM model (e.g., 10",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to architect a privacy-preserving edge-cloud ML system with strict latency, and to reason about model partitioning, feature design, and drift handling.\n\n## Key Concepts\n\n- Edge-cloud partitioning, feature hashing, tiny on-device models, GBM refinement, secure aggregation, federated updates, drift detectors, calibration, rollout strategy.\n\n## Code Example\n\n```python\n# Implementation sketch: on-device + cloud refinement\ndef infer(transaction, device_state, model_on_device, remote_model):\n    feats = feature_hash(transaction, device_state)\n    local = model_on_device.predict(feats)\n    if latency_budget_ok():\n        remote = remote_model.predict(feats)\n        score = 0.5 * local + 0.5 * remote\n    else:\n        score = local\n    return score\n```\n\n## Follow-up Questions\n\n- How would you validate drift detectors and privacy budgets in production?\n- What monitoring would you put in place to catch latency regressions?",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-2",
        "questionId": "q-29345",
        "question": "Advanced ML interview: design a real-time, privacy-preserving recommender for a streaming platform with 1k features per user and 1M daily sessions. Target latency: sub-25 ms CPU per inference. Propose a two-tier pipeline: a fast online learner for latency, and a slower offline model for accuracy, with a privacy layer (federated learning or DP), drift/fairness handling, retraining cadence, and evaluation setup. Include data splits and metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-stage online recommender: fast online learner (SGD logistic regression or shallow tree) for ~25 ms latency, plus a deep offline model updated nightly. Use feature hashing for 1k features, privacy",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Baseline approach: standardize numeric features, bucketize time_of_day, one-hot device/region; use logistic regression with L2; implement partial_fit with mini-batches (~128) to update online; drift d",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Baseline: standardize/one-hot 25 features; try logistic regression with L2 0.01 or a tiny tree (max depth 3). Use 8-bit quantization and on-device feature hashing to fit memory. Update with partial_fi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A two-tower edge architecture featuring a lightweight stereo depth branch paired with an image backbone, fused through attention-based cross-modal fusion. The 3D detector head achieves the ~60 ms latency target on embed…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a production-ready, privacy-conscious online learning system with realistic latency, delayed feedback, and fairness constraints. Requires balancing latency-accuracy trade-offs and privacy guarantees at scale.\n\n## Key Concepts\n\n- Two-tier online/offline inference architecture\n- Privacy: federated learning or DP with secure aggregation\n- Drift detection (ADWIN), calibration strategies, fairness constraints\n- Evaluation: time-based splits, AUC, NDCG, calibration error, latency benchmarks\n\n## Code Example\n\n```javascript\n// Pseudo: partial_fit for online LR\nconst lr = new SGDClassifier({ loss: 'log', penalty: 'l2' });\nlr.partial_fit(X_batch, y_batch, classes=[0,1]);\n```\n\n## Follow-up Questions\n\n- How would you validate privacy guarantees under active user opt-out?\n- How would you handle non-stationary feature distributions across cohorts?",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-3",
        "questionId": "q-21778",
        "question": "You are deploying a real-time cross-lingual content moderation system for a messaging app. Incoming text in 30 languages must be flagged for toxicity with latency under 60 ms on CPU. Propose a practical pipeline: embedding strategy (tiny multilingual model vs lexical features), classifier, online/update cadence, drift handling, calibration, and a minimal labeling loop with weak supervision. Include evaluation metrics and CPU latency verification?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Contextual bandit with per-user context; actions: 4 bitrate levels × 3 thumbnails (12 arms). Inference <20 ms on CPU using online Thompson sampling with a lightweight linear model. Feedback (watch tim",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a policy-driven governance layer: version models with MLFlow, track datasets with lineage, generate per-model SHAP/CF explanations, expose counterfactuals via an API, and store explanations",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement domain adaptation with weather-specific batch normalization, synthetic data augmentation, and multi-task learning. Use domain adversarial training, model pruning for real-time performance, and knowledge distil…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a compact multilingual encoder (e.g., MiniLM-Lite or distilled mBERT) with a linear classifier, exported to ONNX and INT8-quantized for CPU inference. Target ≤60 ms per input at batch size 1, and calibrate via tempe…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nDesigning a low-latency, multilingual content moderation system requires balancing model size, inference speed, and accuracy—all while handling real-world constraints like label drift and limited annotation budgets.\n\n## Key Concepts\n- **Embedding Strategy**: Lightweight multilingual encoders (e.g., MiniLM-Lite, distilled mBERT) outperform lexical features by capturing contextual nuance while still meeting strict latency constraints.\n- **Classifier**: A simple linear head atop frozen embeddings enables fast inference and straightforward retraining.\n- **Quantization**: INT8 quantization significantly reduces inference latency and memory footprint with minimal accuracy loss, making it ideal for CPU-bound real-time applications.\n- **Online Updates**: Implement weekly retraining cycles with a staging environment to validate model changes before production deployment.\n- **Drift Handling**: Monitor prediction confidence distributions and keyword/emoji frequency shifts to detect concept drift early.\n- **Calibration**: Use temperature scaling post-training to ensure probability outputs reflect true toxicity likelihood.\n- **Labeling Loop**: Employ weak supervision with heuristic rules (keyword blacklists, language-specific patterns) combined with active learning to efficiently label ambiguous cases.",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-4",
        "questionId": "q-16643",
        "question": "Design a production governance and explainability framework for a live fintech risk scoring model (Robinhood-like credit decisions) that requires auditability, versioning, and privacy. Include data lineage, model card style explanations, counterfactuals at user-level, automated canary deployments with safety triggers, drift monitoring, retraining policies, and regulatory reporting artifacts. Outline data flows, required artifacts, and rollback criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a CPU-friendly time-series encoder (lightweight Transformer or TCN) with self-supervised pretraining: masked autoencoding and temporal contrast across sensors. Fine-tune on limited fault labels. I",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement MLflow Model Registry with canary deployment using Kubernetes traffic splitting. Configure Istio service mesh to route 95% traffic to stable model and 5% to canary version. Set up Prometheus monitoring for lat…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use 8×8 HOG features to reduce input from 784 dimensions to approximately 60–100 dimensions. Preprocess images with normalization (scale pixel values to [0,1]) and optional contrast enhancement. Train a logistic regress…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a policy-driven governance layer: version models with MLFlow, track datasets with lineage, generate per-model SHAP/CF explanations, expose counterfactuals via an API, and store explanations",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThe candidate demonstrates end-to-end governance: explainability, data lineage, and compliant deployment in regulated fintech.\n\n## Key Concepts\n- Model governance and versioning\n- Data lineage and audit trails\n- Explainability (SHAP, counterfactuals)\n- Canary deployments and rollback\n- Drift detection and retraining triggers\n- regulatory reporting artifacts\n\n## Code Example\n```javascript\n// Pseudo snippet: trigger retraining when drift metric exceeds threshold\nfunction shouldRetrain(driftStats, thresh=0.05){\n  return driftStats.kl > thresh || driftStats.pvalue < 0.05\n}\n```\n\n## Follow-up Questions\n- How would you validate explanations for auditors?\n- What data lineage artifacts would you store and where?",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-5",
        "questionId": "q-13185",
        "question": "Scenario: A mobile health app collects 7 days of daily steps, sleep quality, and mood scores for 20k users across 3 countries. Design a beginner baseline to predict if a user will report high mood the next day. Include data ingestion/cleaning, feature engineering (rolling mood mean, day-of-week, country encoding), model choice (logistic regression vs gradient boosting), calibration, a time-aware train/test split to prevent leakage, class-imbalance handling, and a simple retraining trigger with new labels?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline: use a 5-minute rolling window with per-event features (timestamp bucket, log_level one-hot, msg_len) plus small aggregates. Model: IsolationForest; fallback to LogisticRegression on engineer",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a multi-metric streaming pipeline with adaptive weighting, using precomputed confusion matrices and metric caching for real-time evaluation.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a two-tower architecture: a shared quantized encoder for metadata and chat text, with modality-specific heads; fuse with a light attention layer. Target ~25 ms on mid-range GPUs. Use DP-SGD an",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan a chronologically split baseline: per-user train/test split by date, simple imputation, features like rolling mood mean (past 3 days), day-of-week, country one-hot; compare Logistic Regression an",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nGauges ability to build a practical end-to-end baseline, considering time-series leakage, simple feature engineering, and model calibration in a beginner-friendly setting.\n\n## Key Concepts\n- Time-aware train/test split to avoid leakage and drift.\n- Feature engineering: rolling statistics, temporal features, and categorical encoding.\n- Model comparisons: linear vs tree-based with calibration.\n- Handling class imbalance and calibration quality.\n- Retraining triggers and drift monitoring.\n\n## Code Example\n```python\n# Pseudo: create rolling mood feature and time-based split\nimport pandas as pd\n\n# df has: user_id, date, steps, sleep_quality, mood, country\ndf['date'] = pd.to_datetime(df['date'])\ndf = df.sort_values(['date'])\n\n# rolling mood feature per user\ndf['mood_roll3'] = df.groupby('user_id')['mood'].rolling(window=3, min_periods=1).mean().reset_index(level=0, drop=True)\n\n# time-based split (cutoff date)\ncutoff = df['date'].quantile(0.8)\ntrain = df[df['date'] < cutoff]\ntest  = df[df['date'] >= cutoff]\n\n# encode country, train a model, calibrate probabilities in a separate step\n```\n\n## Follow-up Questions\n- How would you handle missing mood scores? \n- How would you monitor model drift and automate retraining?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-6",
        "questionId": "q-1290",
        "question": "You’re training a binary classifier for signup conversion on a dataset with numeric features (age, session_time) and categorical features (device, country). A logistic regression baseline yields high AUROC but poor calibration on holdout. Outline a practical plan to diagnose and fix calibration, comparing Platt scaling and isotonic regression, data preprocessing tweaks, and how you’d validate the fix with a minimal code sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Step 1: Diagnose the calibration problem** - Plot a reliability diagram showing predicted probability vs actual success rate - Calculate Brier score and Expected Calibration Error (ECE) on holdout data **Step 2: Choos…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan a chronologically split baseline: per-user train/test split by date, simple imputation, features like rolling mood mean (past 3 days), day-of-week, country one-hot; compare Logistic Regression an",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement MLflow for experiment tracking/model registry with MLflow Server, Kubeflow Pipelines for orchestration using Argo workflows, and include automated data validation with Great Expectations. Set up monitoring wit…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a sklearn Pipeline: impute numeric features with median, scale them; encode the country category with OneHotEncoder; fit LogisticRegression with class_weight='balanced' as baseline, and compar",
            "isCorrect": false
          }
        ],
        "explanation": "**Why the original was unclear:**\n1. The explanation was cut off mid-sentence\n2. Key concepts were mixed together without clear structure\n3. Important details about Platt scaling (use logits, not probabilities) were buried\n4. The validation strategy needed clearer emphasis\n\n**Key corrections made:**\n- **Platt scaling** applies to logits (raw model outputs), not probabilities. Using probabilities directly can hurt calibration.\n- **Isotonic regression** needs enough calibration data (1000+ samples recommended) to work reliably—otherwise it overfits.\n- **Separate calibration set** is essential to avoid data leakage. Never use test data to fit the calibrator.\n- **Brier score and ECE** are standard metrics—Brier measures overall accuracy of probabilistic predictions, ECE measures how far off the calibration curve is from perfect.\n\n**Structure improvement:**\nThe answer now flows in 4 clear steps: diagnose → choose method → preprocess → validate. Each calibration method has its trade-offs explained simply. The code uses proper data splitting (train/calibrate/test) instead of incorrectly using test data for calibration.",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-7",
        "questionId": "q-26270",
        "question": "Design a beginner baseline for real-time anomaly detection on an industrial IoT sensor stream (25 features: temp, vibration, pressure, etc.). Anomaly labels arrive 2–5 minutes later. Output per-event anomaly score within 50 ms CPU. Include feature preprocessing, model choice (logistic regression with partial_fit or a small tree), online learning, calibration, drift handling, retraining cadence, and data splits with metrics and latency validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline: 25 features from sensors, compute rolling means/stds, scale; use SGDClassifier with log loss and partial_fit; initialize with [0,1], option for a tiny tree for nonlinear residuals. Calibrate",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a two-tier approach: an on-device encoder (128–256k params) producing a compact embedding, and a lightweight head for recommendation scores. Use NAS-lite to search under 50 MB model size and 1",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a lightweight text representation (TF-IDF or 32–64-d fastText) fused with 4–6 metadata features. Choose SGDClassifier with partial_fit or a small tree, train with mini-batches, and calibrate via i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-tier: on-device lightweight code-completion model producing token predictions within 40 ms, plus a cloud-based reranker with the full context. Use per-tenant feature stores, privacy via DP-SGD and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a practical, low-latency online baseline with calibration and drift handling for streaming data.\n\n## Key Concepts\n- Online learning with partial_fit\n- Score calibration (Platt)\n- Concept drift detection (ADWIN)\n- Latency budgeting and feature engineering with rolling stats\n\n## Code Example\n```python\nfrom sklearn.linear_model import SGDClassifier\nfrom sklearn.calibration import CalibratedClassifierCV\n\nmodel = SGDClassifier(loss='log', max_iter=5)\n# initial train with both classes\nmodel.partial_fit(X_batch, y_batch, classes=[0,1])\n# calibration step (pseudo)\ncal = CalibratedClassifierCV(model, method='sigmoid')\n```\n\n## Follow-up Questions\n- How would you monitor latency across devices and during updates?\n- How would you handle bursts of data affecting drift detection timings?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-8",
        "questionId": "q-201",
        "question": "How does an LSTM cell's forget gate regulate information flow compared to a simple RNN?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline: two logistic heads sharing a common hashed feature map from 12 fields (ids hashed to 2^20, numerics standardized). Use online SGD with partial_fit to update both tasks per event. Calibrate w",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "On-device: a tiny gated fusion model (e.g., 8k–20k params quantized) combining device fingerprint features and lightweight transaction descriptors; server: an ensemble with longer-horizon features. Us",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "LSTM forget gate uses sigmoid activation to selectively retain or discard previous cell state information, preventing vanishing gradients that plague simple RNNs. Unlike simple RNNs which compute h_t = tanh(W·x_t + U·h_…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Parameters are learned during training, hyperparameters are set before training. Cross-validation prevents overfitting by testing hyperparameters on multiple data splits.",
            "isCorrect": false
          }
        ],
        "explanation": "## LSTM Forget Gate Overview\nThe forget gate is a critical component that controls what information from the previous cell state should be retained or discarded.\n\n## Simple RNN vs LSTM Comparison\n- **Simple RNN**: h_t = tanh(W·x_t + U·h_{t-1}) — no gates, no selective information control\n- **LSTM**: Uses gates to selectively preserve or inhibit information at each time step\n\n## Implementation Details\n- **Input**: Previous hidden state (h_{t-1}) and current input (x_t)\n- **Activation**: Sigmoid function outputs values between 0-1\n- **Operation**: Element-wise multiplication with previous cell state\n- **Output**: Filtered cell state passed to next time step\n- **Key insight**: Values close to 1 mean \"keep this information\"; values close to 0 mean \"discard this information\"\n\n## Code Example\n```python\n# Forget gate computation\nf_t = sigmoid(W_f @ h_{t-1} + U_f @ x_t + b_f)\n# Apply to cell state - what to DISCARD from previous state\nC_t = f_t * C_{t-1} + i_t * C_tilde\n```\n\n## Common Pitfalls\n- Sigmoid saturation can cause gradients to vanish\n- Improper weight initialization may lead to poor learning\n- Over-reliance on forget gate can cause information loss",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-9",
        "questionId": "q-17414",
        "question": "Design a privacy-preserving, cross-tenant anomaly detector on a cloud billing and usage graph. The system must support sub-30 ms edge inference with quantization, federated updates to a central model, handle highly skewed, non-IID tenant data, and return calibrated anomaly scores with explainability. Describe data schema, graph-based model choice (e.g., hetero-GNN), training/aggregation, deployment, and drift mitigation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a hetero-graph neural network operating on a tenant billing graph. Enable 8-bit quantized edge/node features for edge inference (<30 ms) with DP-friendly aggregation at the server. Use FedAvg-s",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a CPU-friendly linear ranking model with online updates. Hash 30 features into a fixed-size sparse vector using feature hashing; scale numeric features; compute score w^T x; update via FTRL or SGD",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-stage: real-time retrieval with multilingual embeddings (FAISS) followed by a compact ranker (GBDT/RankNet) on per-item features. Feature store uses versioned schemas and tenant isolation. Drift s",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A retrieval-augmented architecture featuring a fast approximate nearest neighbor (ANN) index over item embeddings stored in a Delta Lake feature store. User events stream via Kafka into the feature store, triggering rea…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests privacy-preserving, scalable graph ML across tenants with real-time latency, explainability, and drift handling.\n\n## Key Concepts\n- Hetero-graph neural networks and cross-tenant feature integration\n- Federated learning with privacy guarantees\n- Calibration of anomaly scores and explainability\n- Drift detection and robust rollout strategies\n\n## Code Example\n```python\n# Pseudo forward pass (high level)\ndef forward(graph, features, model):\n    z = model.encode(graph, features)  # hetero-GNN encoding\n    scores = calibrator.fit_transform(z)\n    return scores\n```\n\n## Follow-up Questions\n- How would you evaluate calibration quality across tenants?\n- What drift signals would you monitor and how would you respond?",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-10",
        "questionId": "q-30124",
        "question": "Design a streaming, privacy-preserving routing model for a global live-chat platform. Each message with metadata must be routed to the best agent tier (bot, junior, or senior) with a 15 ms CPU latency target. Labels for session quality arrive 2–5 minutes later. Propose features (text + context), a lightweight online model (logistic regression with partial_fit or small tree), online calibration, drift detection, fairness checks across regions, and a safe rollout plan with latency validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline: for multilingual, emoji-heavy chat, use lightweight features (character n-grams 2-5, emoji counts, language tag, hashed user/room). Model: SGDClassifier with log loss using partial_fit; onli",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Monitor feature distribution drift with KS statistic per feature and AUC on a rolling window. Trigger retraining when drift > threshold or AUC drops beyond a tolerance. Simulate drift offline by time-",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Train a lightweight SGDClassifier with logistic loss on 12 features after simple normalization (fit on-device with streaming partial_fit). Use feature selection to keep 10–12 features. Process in mini",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement on-device streaming inference using hashed text features and session context, paired with a small logistic regression model (leveraging partial_fit) or a lightweight decision tree. Route incoming messages to t…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates your understanding of real-time streaming inference, privacy-conscious feature engineering, and the practical trade-offs between model size, latency, and accuracy. It also probes your knowledge of drift detection, fairness considerations, and rollout strategies across diverse regions.\n\n## Key Concepts\n\n- **Online learning**: Using partial_fit with logistic regression or incremental decision trees for streaming data\n- **Feature hashing**: Transforming text and session metadata into fixed-length vectors without storing raw data\n- **Latency budgets**: End-to-end validation to meet the 15ms CPU target\n- **Drift detection**: Algorithms such as ADWIN or DDM to identify distribution shifts in incoming data\n- **Model calibration**: Techniques like Platt scaling to ensure confidence scores reflect true probabilities\n- **Privacy by design**: On-device preprocessing ensures no raw text leaves the client\n- **Fairness checks**: Stratified monitoring across regions during rollout to detect disparate impact\n\n## Code Example\n\n```python\nclass OnlineRoutingModel:\n    def __init__(self, latency_budget_ms=15):\n        self.hash_vectorizer = FeatureHasher(n_features=1024, input_type='dict')\n        self.model = SGDClassifier(\n            loss='log_loss',\n            warm_start=True,\n            random_state=42\n        )\n        self.latency_budget = latency_budget_ms\n        self.calibrator = CalibratedClassifierCV(self.model, method='sigmoid')\n        self.drift_detector = ADWIN()\n        self.fairness_monitor = FairnessMonitor()\n\n    def extract_features(self, message: dict) -> scipy.sparse.csr_matrix:\n        feature_dict = {\n            'text_hash': hash(message['text']) % 1024,\n            'hour': message['timestamp'].hour,\n            'region': message['region'],\n            'session_length': message.get('session_len', 0),\n            'is_returning': message.get('returning_user', False),\n            **message.get('context', {})\n        }\n        return self.hash_vectorizer.fit_transform([feature_dict])\n\n    def predict(self, message: dict) -> dict:\n        start = time.perf_counter()\n        X = self.extract_features(message)\n        proba = self.model.predict_proba(X)[0]\n        latency_ms = (time.perf_counter() - start) * 1000\n\n        if latency_ms > self.latency_budget:\n            self._log_latency_violation(latency_ms, message['region'])\n\n        return {\n            'tier': ['bot', 'junior', 'senior'][proba.argmax()],\n            'confidence': proba.max(),\n            'latency_ms': latency_ms\n        }\n\n    def partial_fit(self, X, y, classes):\n        self.model.partial_fit(X, y, classes=classes)\n        self.fairness_monitor.update(X, y)\n\n    def detect_drift(self, X) -> bool:\n        prediction = self.model.predict(X)\n        return self.drift_detector.detect_change(prediction)\n```\n\n## Rollout Plan\n\n1. **Shadow mode**: Deploy the model alongside the existing system, logging predictions without acting on them for 2 weeks\n2. **Canary release**: Route 5% of live traffic through the new model; compare outcomes against the control group\n3. **Latency validation**: Continuously monitor P99 latency per region; automatically fall back to heuristics if thresholds are breached\n4. **Fairness audit**: After 2 weeks, review false positive and false negative rates stratified by region; halt rollout if disparities exceed 5%\n5. **Full deployment**: Gradually increase traffic to 50%, then 100% over 4 weeks with daily drift and latency reports",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-11",
        "questionId": "q-2530",
        "question": "Design a production-ready calibration-and-drift pipeline for a binary classifier deployed in a high-stakes domain (e.g., loan approvals). The system must calibrate probabilities in real time as data drifts occur, detect distribution shift, trigger targeted retraining with limited labels, and provide explainability and auditing. Describe architecture, data flow, concrete metrics, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement online continual learning with a two-tower embedding model (driver, rider). Update via a streaming replay buffer; store features in Delta Lake. Use drift detection (ADWIN, KS) and a feature-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a production-grade calibration-and-drift-aware scoring stack. Calibrate probabilities using isotonic regression on recent labeled data; detect distribution shift with Population Stability Index (PSI) on inputs…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Baseline: Use `HashingVectorizer(n_features=2000)` to convert text to numeric features, followed by an `SGDClassifier(loss='log_loss', max_iter=5, tol=1e-3)` trained via `partial_fit` for online updates. Calibrate with…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Split by time (80/10/10) to mimic streaming. Use TF-IDF with unigrams+bigrams; train Logistic Regression (C=1.0, liblinear). Calibrate with Platt scaling; select threshold to maximize F1 at target pre",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates the ability to design production-grade ML systems addressing calibration, drift, data efficiency, and explainability—key competencies for IBM/Google ML roles.\n\n## Key Concepts\n- Calibration metrics (ECE, Brier score)\n- Distribution drift detection (PSI, KL divergence)\n- Rolling calibration windows and retraining triggers\n- Active learning with limited labels\n- Explainability and auditing frameworks\n\n## Code Example\n```python\nfrom sklearn.isotonic import IsotonicRegression\ncal = IsotonicRegression(out_of_bounds='clip').fit(probs_last_week, labels_last_week)\ncalibrated_probs = cal.transform(current_probs)\n```",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-12",
        "questionId": "q-21749",
        "question": "Design a beginner baseline for an intent classifier used in a chat support tool. Data: text messages; 20-30 intents; 10k labeled examples; latency target 50 ms per inference on a single CPU core; labels arrive in batch with delay for some intents. Propose a concrete pipeline: preprocessing, model choice, online updating, drift detection, evaluation, and retraining cadence. Include data schema and latency checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Tokenize text, use hashing-based features (2,048 buckets), and train a logistic regression model with partial_fit for online updates on CPU. Process in micro-batches (32 samples). Implement drift checks using population…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use feature hashing to reduce 40 features to ~128 dims; train logistic regression with SGD on-device; apply 8-bit quantization and prune small weights to stay under 1 MB. Calibrate with Platt scaling",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A privacy-preserving edge-first design: on-device ranking model (tiny transformer or dense retriever) with a server-side federated updater. Use post-training weight quantization to INT8, prune aggress",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a streaming feature extractor using 1-second windows to compute FFT magnitudes, RMS, and crest factor; an online autoencoder provides anomaly scores updated incrementally, with a gradient-boos",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical competency in building a production-ready text classification system under constraints. It covers selecting CPU-efficient models for strict latency requirements, designing for online/streaming updates, and planning for model maintenance—all essential skills for deploying ML in production environments.\n\n## Key Concepts\n- **Text preprocessing and hashing vectorization**: Hashing vectorizer provides constant-time vocabulary lookup, ideal for latency-sensitive deployments\n- **Online learning with partial_fit**: Enables incremental model updates without full retraining\n- **Latency-aware model choices on CPU**: Logistic regression offers fast inference due to simple matrix operations\n- **Drift detection and model maintenance**: Population stability index (PSI) and confidence monitoring detect distribution shifts\n- **Evaluation metrics for imbalanced intents**: Use macro F1-score and per-class precision/recall to account for imbalanced classes\n\n## Data Schema\n```\n{\n  \"text\": \"string\",           // Raw message text\n  \"intent\": \"string\",       // Labeled intent (e.g., \"refund_request\")\n  \"timestamp\": \"datetime\",  // Message timestamp for drift analysis\n  \"confidence\": \"float\"     // Model prediction confidence\n}\n```\n\n## Latency Budget (50ms target)\n| Stage | Estimated Time |\n|-------|----------------|\n| Preprocessing + tokenization | 5-10ms |\n| Hashing vectorization | 2-5ms |\n| Model inference | 10-20ms |\n| Post-processing | 2-5ms |\n| **Total** | **~25-40ms** |\n\n## Code Example\n```python\n# Online learning pipeline with drift detection\nfrom sklearn.feature_extraction.text import HashingVectorizer\nfrom sklearn.linear_model import SGDClassifier\nimport numpy as np\n\nvectorizer = HashingVectorizer(n_features=2048, ngram_range=(1, 2))\nmodel = SGDClassifier(loss='log_loss', warm_start=True)\n\n# Micro-batch processing\ndef process_batch(texts, labels=None):\n    X = vectorizer.transform(texts)\n    \n    if labels is not None:\n        model.partial_fit(X, labels, classes=all_intents)\n    \n    predictions = model.predict(X)\n    confidences = model.predict_proba(X).max(axis=1)\n    return predictions, confidences\n\n# Drift detection\ndef compute_psi(baseline, current, bins=10):\n    # Population Stability Index calculation\n    breakpoints = np.percentile(baseline, np.linspace(0, 100, bins + 1))\n    baseline_hist = np.histogram(baseline, bins=breakpoints)[0] / len(baseline)\n    current_hist = np.histogram(current, bins=breakpoints)[0] / len(current)\n    psi = np.sum((current_hist - baseline_hist) * np.log(current_hist / baseline_hist))\n    return psi\n```",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-13",
        "questionId": "q-18528",
        "question": "You operate a live customer-support system that assigns urgent vs non-urgent to incoming emails. Data arrives at ~50 emails/min, and training labels arrive with a 30–60 minute delay. Propose a beginner-friendly CPU pipeline: text preprocessing (TF-IDF vs bag-of-words), model choices (Logistic Regression vs small linear SVM), online learning (partial_fit or warm-start), an active-learning loop for high-uncertainty emails, latency targets (<100 ms per inference), evaluation metrics, and a simple retraining cadence. Include data splits and latency validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline: text preprocessing with lightweight TF‑IDF or bag‑of‑words; compare Logistic Regression vs small linear SVM; enable online learning via partial_fit or warm-start; add active learning to samp",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Candidate should outline an on-device feature extractor (e.g., PCA/autoencoder), an online learner ( SGD/streaming MLP), a drift detector (ADWIN) with windowing, a lightweight update protocol over sec",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a modular stack: image backbone (CNN/ViT) + LiDAR voxel encoder, followed by transformer-based fusion and an adaptive early-exit gate to meet 20 ms. Use FP16/INT8, on-device DP for updates, an",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use isotonic regression or temperature scaling to recalibrate probabilities. Build a small, recent calibration set from live predictions; fit the calibrator on scores vs true outcomes; monitor calibra",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests a practical NLP streaming setup with real-time latency and delayed supervision. It probes feature choice, online updating, active learning, and retraining cadence under CPU limits.\n\n## Key Concepts\n- Text preprocessing choices (TF-IDF vs bag-of-words)\n- Online learning for text data (partial_fit, SGDClassifier)\n- Active learning (uncertainty sampling) for label economy\n- Latency budgeting on CPU and feature/vector sizes\n- Evaluation in streaming (rolling splits, AUC, F1, calibration)\n\n## Code Example\n```python\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.linear_model import SGDClassifier\n\nvectorizer = TfidfVectorizer(max_features=5000, ngram_range=(1,2))\nclf = SGDClassifier(loss='log', max_iter=1, tol=1e-3)\n# initial fit with both classes\nX0 = vectorizer.fit_transform([\"sample email text\"])\ny0 = [0]\nclf.partial_fit(X0, y0, classes=[0,1])\n# online loop\n# for batch in streaming_batches:\n#     X = vectorizer.transform(batch_texts)\n#     y_pred = clf.predict(X)\n#     clf.partial_fit(X, batch_labels)\n```\n\n## Follow-up Questions\n- How would you measure latency under bursty load?\n- How would label delay affect evaluation and retraining cadence?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-14",
        "questionId": "q-9486",
        "question": "You're building a real-time credit-risk scoring system for a global fintech with multi-geo data streams. Design an end-to-end pipeline that (a) ingests streaming features with latency under 20 ms, (b) detects feature drift and data leakage, (c) provides per-prediction explanations that satisfy regulators, (d) calibrates risk scores for rare events, (e) supports blue/green rollout and audit logging, and (f) outlines retraining, monitoring, and governance. What choices drive performance and compliance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline: TF-IDF with logistic regression. Multilingual: lowercase, simple tokenization; avoid heavy preprocessing. Splits: language-stratified train/val/test. Metrics: macro F1 and per-language recal",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a two-stage model: a lightweight gradient-boosted detector for fast scoring plus a differentiable rule engine for regulatory constraints; train with federated updates using DP-SGD and secure",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-stage real-time pricing: Stage 1 a fast per-item predictor (quantized features, LightGBM/GBDT) meeting 120 ms CPU latency. Stage 2 region-aware calibration and fairness enforcement via constrained",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a streaming feature store (Feast) with point-in-time joins to keep latency under 20 ms, a gradient-boosted model with monotone constraints, drift detection per feature (KS/ADWIN) and leakage check",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess end-to-end ML systems: streaming latency, drift detection, calibration for rare events, and regulator-friendly explanations with governance and rollouts.\n\n## Key Concepts\n\n- Streaming feature store with point-in-time joins\n- Per-feature drift detection and leakage checks\n- Model calibration for imbalanced events\n- Explainability regulators trust (local/global)\n- Canary/blue-green rollout and auditability\n\n## Code Example\n\n```javascript\n// Simple drift check skeleton\nfunction driftCheck(oldDist, newDist, pThresh=0.05){\n  const p = kolmogorovSmirnovTest(oldDist, newDist);\n  return p < pThresh;\n}\n```\n\n## Follow-up Questions\n\n- How would you handle label latency and delayed feedback in retraining?\n- How would you audit feature leakage during deployment?",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-15",
        "questionId": "q-29012",
        "question": "Design a real-time item-ranking pipeline for a professional social network feed that surfaces personalized job posts. Data stream: 40k–80k events/hour; each post has 30 features; user interactions (clicks) arrive 2–5 minutes later. Output per-post ranking score within 15 ms on CPU. Describe preprocessing (feature hashing, normalization), model choice (linear ranking with FTRL or SGD), online learning, calibration, drift handling, and evaluation plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-tower ranking with per-user embeddings cached at the edge; a lightweight ranking head on-device. Use feature hashing to cap feature space, and differential privacy to protect data. Offline trainin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a multimodal backbone with text embeddings and structured features, fused via cross-attention. Process streams with a feature store and drift detectors; keep latency under 200 ms with on-device or",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a CPU-friendly linear ranking model with online updates. Hash 30 features into a fixed-size sparse vector using feature hashing; scale numeric features; compute score w^T x; update via FTRL or SGD",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a two-stage on-device model: a lightweight CNN+MLP architecture that processes 2-second frame sequences and audio features to generate a scalar engagement score. Apply int8 quantization to achieve sub-1 millis…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design an online, latency-constrained ranking system with delayed feedback and drift across regions.\n\n## Key Concepts\n- Real-time CPU inference with feature hashing\n- Online learning (FTRL/SGD) and incremental updates\n- Calibration (Platt/isotonic) and drift detection (ADWIN)\n- Region adapters for non-stationarity and latency validation\n\n## Code Example\n```javascript\n// Pseudo update sketch (not production-ready)\nlet x = hashFeatures(features); // fixed-size sparse vector\nlet yPred = dotProduct(w, x);\nlet error = label - yPred;\nw = w.map((wi, i) => wi + eta * error * x[i]);\n```\n\n## Follow-up Questions\n- How would you validate drift robustness across regions?\n- What latency testing strategy ensures end-to-end SLA compliance?",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-16",
        "questionId": "q-25528",
        "question": "Design a privacy-preserving, cross-tenant fraud-detection pipeline for a payments platform serving Snowflake, Zoom, and PayPal. Tenants keep data isolated; labels arrive with 1–2 hour delay; per-event risk score must be produced in under 50 ms on CPU. Propose architecture (feature hashing, DP-SGD or FedAvg, online calibration, drift monitoring), data partitioning, evaluation metrics, deployment strategy, and how you guarantee privacy and prevent cross-tenant leakage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Hybrid: compute lightweight features on-device (device fingerprint, recent auth failures, tap cadence, location jitter), hash/mask and send to server; run a compact, quantized LightGBM model (e.g., 10",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant feature hashing with a small shared model, privacy via DP-SGD or FedAvg, online calibration, and drift monitoring. Partition data by tenant and time window; train on delayed labels with rol",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a two-tower model: a log/metric encoder and a time-context encoder, trained with self-supervised tasks (masked prediction, sequence shuffling). Use lightweight quantized embeddings and an earl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a real-time feature-quality gate for a multi-tenant inference service. Validate each incoming feature vector with per-feature range checks, basic z-scores, and monotonicity tests; monitor drift",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design privacy-preserving, cross-tenant ML at scale under strict latency. Requires understanding of differential privacy, federated learning, calibration, and drift monitoring in a production fraud context.\n\n## Key Concepts\n\n- Cross-tenant privacy and isolation\n- DP-SGD or federated averaging (FedAvg) for privacy-preserving training\n- Online calibration and drift detection\n- Data partitioning by tenant and time windows\n- Latency constraints and CPU-friendly inference\n\n## Code Example\n\n```javascript\n// Pseudocode: FedAvg-style update for cross-tenant model\nfor each tenant in tenants:\n  local_model = train(tenant_data[tenant])\n  send_to_server(local_model, weight=|tenant_data[tenant]|)\nglobal_model = aggregate_models(tenant_models, weights)\n```\n\n## Follow-up Questions\n\n- How would you audit for cross-tenant leakage in logs and features?\n- Which metrics and thresholds would trigger retraining or model a/b tests across tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-17",
        "questionId": "q-7765",
        "question": "Design an end-to-end real-time content ranking system for a social feed (Twitter-scale) that must deliver sub-50 ms latency for 99th percentile queries, support multilingual content, handle concept drift, privacy constraints (DP/FL options), and provide explanations for top-ranked items without leaking business strategies. Include data ingestion, feature store, model choice, training, evaluation, rollout, and governance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Split data by time to mimic future data; one-hot encode device_type; normalize session_duration and pages_viewed; train Logistic Regression with L2; calibrate scores with isotonic calibration; monitor",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-stage: real-time retrieval with multilingual embeddings (FAISS) followed by a compact ranker (GBDT/RankNet) on per-item features. Feature store uses versioned schemas and tenant isolation. Drift s",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use hashing trick for categorical features; scale numerics. Start with logistic regression via partial_fit for online updates; add a tiny tree if needed for nonlinearity. Track drift with ADWIN; calib",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use hashed character n-grams (2,000 features) with an SGDClassifier (loss='log'), update the model via partial_fit in small batches to achieve <150 ms CPU latency, calibrate using Platt scaling on a rolling window, moni…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests orchestration of ML at social-scale, combining retrieval, ranking, privacy, explainability, drift, and governance. The candidate must justify system choices and trade-offs at Twitter/MongoDB/Two Sigma scale.\n\n## Key Concepts\n\n- Two-stage retrieval and ranking\n- Multilingual embeddings\n- Versioned feature store and tenant isolation\n- Drift detection and privacy\n- Explainability in ranking\n- Latency budgets and observability\n\n## Code Example\n\n```javascript\n// Drift check helper: return true if distributions differ beyond threshold\nfunction hasDrift(pValue, alpha=0.05) {\n  return pValue < alpha;\n}\n```\n\n## Follow-up Questions\n\n- How would you validate latency under traffic spikes without degrading quality?\n- What instrumentation would you add for privacy and governance in production?",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-18",
        "questionId": "q-227",
        "question": "How would you implement dynamic quantization-aware training with mixed-precision to optimize inference latency while maintaining model accuracy across varying hardware constraints?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline: text + numeric features via a ColumnTransformer; apply TfidfVectorizer to 'summary', StandardScaler to numeric features; combine with a LogisticRegression (class_weight='balanced') and a sma",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-layer mixed-precision quantization with hardware-aware calibration and accuracy-aware layer selection to balance latency and accuracy.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "On-device: a tiny gated fusion model (e.g., 8k–20k params quantized) combining device fingerprint features and lightweight transaction descriptors; server: an ensemble with longer-horizon features. Us",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a lightweight multi-modal model: text encoder (quantized small transformer), video encoder (MobileNetV3), audio encoder (tiny spectrogram CNN), and late fusion via a compact MLP. Streaming use",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\nDynamic quantization-aware training (QAT) with mixed-precision combines the benefits of quantization and precision optimization by selectively applying different bit-widths to different layers based on their sensitivity and hardware capabilities.\n\n## Implementation Details\n- **Per-layer sensitivity analysis**: Measure accuracy impact of quantizing each layer\n- **Hardware profiling**: Determine optimal precision for target hardware\n- **Dynamic precision selection**: Runtime adaptation based on device constraints\n- **Accuracy-aware optimization**: Maintain model performance within acceptable thresholds\n\n## Code Example\n```python\nclass MixedPrecisionQAT:\n    def __init__(self, model, hardware_profile):\n        self.sensitivity_scores = self.analyze_sensitivity(model)\n        self.precision_map = self.optimize_precision(\n            model, hardware_profile, self.sensitivity_scores\n        )\n    \n    def quantize_layer(self, layer, target_precision):\n        if target_precision == 'int8':\n            return torch.quantization.prepare_qat(layer)\n        elif target_precision == 'fp16':\n            return layer.half()\n        return layer\n```\n\n## Common Pitfalls\n- **Over-aggressive quantization**: Losing accuracy on sensitive layers\n- **Hardware mismatch**: Optimizing for wrong target hardware\n- **Calibration data bias**: Using unrepresentative calibration datasets\n- **Precision inconsistency**: Mixed precision causing numerical instability",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-19",
        "questionId": "q-17864",
        "question": "Design a real-time anomaly detection system for cross-border financial transactions at scale (billions/day) with <60 ms latency per inference on mixed hardware (data-center CPUs and edge devices). Include streaming features, model choice and fusion, calibration for low FP rate, drift detection, privacy-preserving updates (DP/FL), and a rollout with A/B testing. Outline data schema, training pipeline, and rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-layer approach: a lightweight on-device detector (tree-based or quantized model) for sub-60 ms inference, plus a centralized deep model for complex signals. Use streaming features with a rolling w",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Two-tier privacy-preserving fraud detector: on-device anomaly detectors for user content and a server streaming model with federated updates and differential privacy. Use a fast routing classifier and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant uplift modeling with grouped A/B tests, compute conditional treatment effects via causal forest on a shared feature space, isolating tenants with tenant-id sharding and DP-friendly aggr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use hashed bag-of-words on the streaming email text, train a logistic regression model for CPU latency, apply Platt scaling for calibration, and implement an active-learning loop that prioritizes unce",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses real-time, scalable anomaly detection under latency, drift, and privacy constraints while requiring a practical rollout strategy.\n\n## Key Concepts\n- Real-time streaming features and windowing\n- Two-layer model fusion (on-device + centralized)\n- Score calibration and adaptive thresholds\n- Drift detection and monitoring\n- Privacy-preserving updates (DP/FL) and secure aggregation\n- Rollout, canary testing, and rollback\n\n## Code Example\n```python\n# Pseudocode: simple on-device detector with calibration hook\ndef infer_on_device(x):\n    y = fast_model.predict(x)\n    s = calibrator.predict_proba(y)  # calibrated score\n    if s > threshold:\n        return 1  # anomaly\n    return 0\n```\n\n## Follow-up Questions\n- How would you validate calibration stability under distribution shift?\n- What metrics and experiments would you use for canary rollout and rollback safety?",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-20",
        "questionId": "q-11596",
        "question": "Build a beginner-friendly baseline to forecast churn from a small tabular dataset (demographics + app usage) with on-device inference on a mid-range mobile CPU, aiming under 15 ms. Propose a concrete pipeline: encoding choices (categorical with hashing), model (logistic regression with L1), calibration, and a drift-monitoring/retraining plan with concrete steps and a minimal code sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Hash-based encoding for categoricals, numeric scaling; train a logistic regression with L1 to keep size small; apply Platt scaling for calibration; drift detect by KS test over 1k recent samples for e",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage a lightweight CNN for image, a compact text encoder for captions, and an efficient audio feature extractor. Fuse with gated multimodal attention. Use self-supervised pretraining across modali",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Classification predicts discrete categories such as spam/not spam or image classes, typically using algorithms like logistic regression or decision trees. Regression predicts continuous numerical values such as house pr…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-tier: on-device lightweight code-completion model producing token predictions within 40 ms, plus a cloud-based reranker with the full context. Use per-tenant feature stores, privacy via DP-SGD and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical baseline ML for on-device churn prediction under tight latency and data constraints. It emphasizes simple, interpretable models, small-footprint features, and low-friction maintenance.\n\n## Key Concepts\n- On-device ML constraints and feature hashing\n- Calibrated probabilities with minimal overhead\n- Drift detection and lightweight retraining triggers\n- Model size budgeting and latency considerations\n\n## Code Example\n```javascript\n// Pseudo-code: fit logistic regression with L1, hashed features\n```\n\n## Follow-up Questions\n- How would you adjust if latency tightens to 5 ms?\n- How would you validate calibration in production?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-21",
        "questionId": "q-23614",
        "question": "Design a real-time drift-aware feature quality gate for a multi-tenant inference service. Validate each incoming feature vector with per-feature range checks, basic z-scores, and monotonicity tests; monitor drift using ADWIN and a lightweight percentile tracker; on drift or anomaly, soft-fail to a safe default and flag for review. Latency target: <50 ms per vector on mid-range CPUs; canary rollout (5%); tenant isolation; online calibration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-platform content ranking system for a Netflix-like service using video embeddings, audio cues, text metadata, and user signals, with a target latency under 60 ms on edge GPUs. Propose a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a CPU-friendly time-series encoder (lightweight Transformer or TCN) with self-supervised pretraining: masked autoencoding and temporal contrast across sensors. Fine-tune on limited fault labels. I",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a real-time feature-quality gate for a multi-tenant inference service. Validate each incoming feature vector with per-feature range checks, basic z-scores, and monotonicity tests; monitor drift",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a 2-stage approach: 1) lightweight feature hashing + online feature scaling; 2) logistic regression with SGD partial_fit. Latency target 150 ms on CPU. Data splits: 70/15/15; metrics: AUROC, F1, c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design robust data-quality gates, latency constraints, drift detection, and deployment safety for multi-tenant models.\n\n## Key Concepts\n\n- Real-time validation\n- Drift detection (ADWIN)\n- Soft-fail strategy\n- Canary rollout\n- Multi-tenant isolation\n\n## Code Example\n\n```python\ndef validate_features(x, mins, maxs, means, stds, eps=3.0):\n    for i, v in enumerate(x):\n        if v < mins[i] or v > maxs[i]:\n            return False\n        z = (v - means[i]) / (max(stds[i], 1e-6))\n        if abs(z) > eps:\n            return False\n    return True\n```\n\n## Follow-up Questions\n\n- How to adapt thresholds per-tenant?\n- How to measure impact on latency and uptime?",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-22",
        "questionId": "q-20972",
        "question": "Edge-device image classifier for safety signage on a production floor. 10 classes; input 32x32 grayscale; CPU-only; latency target 50 ms per image. Propose a beginner baseline pipeline: feature preprocessing, model choice (tiny CNN vs flat MLP), quantization, calibration, and a simple retraining cadence. Include evaluation plan and memory constraints?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A practical baseline: implement a compact three-layer CNN (8–16 channels) accepting 32x32 grayscale inputs. For extremely constrained hardware, fall back to a simple two-layer MLP. Apply 8-bit post-training quantization…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Two-tier privacy-preserving fraud detector: on-device anomaly detectors for user content and a server streaming model with federated updates and differential privacy. Use a fast routing classifier and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a 5-way classifier using hashed text features plus 4 numeric features; train with SGDClassifier(loss='log', max_iter=1) using partial_fit; keep feature space under ~1500 dims to meet 40 ms lat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan a chronologically split baseline: per-user train/test split by date, simple imputation, features like rolling mood mean (past 3 days), day-of-week, country one-hot; compare Logistic Regression an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses the ability to design lightweight edge ML solutions under realistic constraints—latency, memory, and model robustness in safety-critical production environments.\n\n## Key Concepts\n- Edge inference with model width/depth trade-offs\n- Quantization, calibration, and latency optimization\n- Drift monitoring and retraining cadence\n\n## Code Example\n```python\n# Example pseudo-pipeline outline (PyTorch)\nimport torch\nimport torch.nn as nn\n\nclass TinyCNN(nn.Module):\n    def __init__(self, num_classes=10):\n        super().__init__()\n        self.features = nn.Sequential(\n            nn.Conv2d(1, 8, 3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n            nn.Conv2d(8, 16, 3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n            nn.Conv2d(16, 16, 3, padding=1),\n            nn.ReLU(),\n            nn.AdaptiveAvgPool2d(1)\n        )\n        self.classifier = nn.Linear(16, num_classes)\n    \n    def forward(self, x):\n        x = self.features(x)\n        x = x.view(x.size(0), -1)\n        return self.classifier(x)\n```",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-23",
        "questionId": "q-7677",
        "question": "You're coordinating ML across Snowflake and Microsoft Fabric for a global enterprise churn model. Data spans CRM, billing, support, and telemetry with strict governance and privacy rules. Design the end-to-end system: data schema/versioning, feature store and lineage, privacy-preserving training and inference, model lifecycle (training, evaluation, drift detection, rollout), monitoring/alerts/rollback, and cost/latency constraints. Be concrete about tech choices and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed approach: streaming encoder-decoder with lightweight language adapters, 8-bit quantization, and monotonic attention to meet 150 ms on mobile GPUs; shared base model with language-local adapte",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Contextual bandit with per-user context; actions: 4 bitrate levels × 3 thumbnails (12 arms). Inference <20 ms on CPU using online Thompson sampling with a lightweight linear model. Feedback (watch tim",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a feature-store backed churn model with schema-versioned contracts in Snowflake, and lineage tracked in a data catalog; real-time features computed via Fabric pipelines. Train with DP-SGD for pr",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a CPU-friendly time-series encoder (lightweight Transformer or TCN) with self-supervised pretraining: masked autoencoding and temporal contrast across sensors. Fine-tune on limited fault labels. I",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design enterprise-grade ML pipelines that integrate cross-cloud data, enforce governance, and balance privacy with performance.\n\n## Key Concepts\n\n- Data schema versioning and contracts\n- Feature store governance and lineage\n- Privacy-preserving training (DP-SGD) and constrained inference\n- Drift detection and continuous evaluation\n- Rollout strategies (canary/blue-green) and cost-aware scaling\n- Explainability (SHAP) for compliance\n\n## Code Example\n\n```python\n# DP-SGD skeleton\nfor batch in dataloader:\n    grad = model.compute_grad(batch)\n    grad = dp_clip(grad, C)\n    grad = add_noise(grad, epsilon)\n    optimizer.step(grad)\n```\n\n## Follow-up Questions\n\n- How would you handle schema evolution across multiple data sources?\n- How would you quantify and enforce explainability requirements?",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-24",
        "questionId": "q-6119",
        "question": "Given a 10k-row tabular dataset for churn prediction with numeric features (e.g., user_age, session_count) and a categorical feature (country), design a practical baseline ML pipeline in Python (scikit-learn) to maximize ROC-AUC in stratified cross-validation. Describe preprocessing, encoding, model choice, evaluation strategy, and a simple deployment check?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a sklearn Pipeline: impute numeric features with median, scale them; encode the country category with OneHotEncoder; fit LogisticRegression with class_weight='balanced' as baseline, and compar",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect a streaming edge pipeline with per-request features (TLS fingerprint, anonymized IP bucket, headers) plus 1–5s windowed stats; run an online one-class detector at the edge with a fast calibr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a lightweight multilingual model (DistilBERT with adapters or a CNN-based text encoder) for on-device inference. Use self-trainingabels on unlabeled reviews with confident pseudo-l combined with a small labeled…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a two-layer feature store: Redis-backed hot cache per tenant plus durable store (S3/Parquet). Use versioned feature schemas and per-tenant namespaces to enforce isolation. Target sub-5 ms P95",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical end-to-end thinking for tabular data, focusing on beginner-friendly, reproducible preprocessing, model selection, evaluation, and deployment sanity checks.\n\n## Key Concepts\n\n- sklearn Pipeline\n- ColumnTransformer\n- OneHotEncoder\n- SimpleImputer\n- StratifiedKFold\n- ROC-AUC\n- Leakage prevention\n- Model selection basics\n\n## Code Example\n\n```javascript\n# Python-like pseudocode\npreprocess = ColumnTransformer([\n  ('num', Pipeline([('imputer', SimpleImputer(strategy='median')),\n                    ('scaler', StandardScaler())]), ['user_age','session_count']),\n  ('cat', Pipeline([('imputer', SimpleImputer(strategy='most_frequent')),\n                    ('onehot', OneHotEncoder(handle_unknown='ignore'))]), ['country'])\n])\nmodel = LogisticRegression(max_iter=1000, class_weight='balanced')\npipe = Pipeline([('preprocess', preprocess), ('model', model)])\n```\n\n## Follow-up Questions\n\n- How would you detect and prevent time-based leakage?\n\n- How would you explain a low ROC-AUC and what quick experiments would you run?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-05T05:15:14.744Z",
    "version": 1
  }
]