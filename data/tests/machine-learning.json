[
  {
    "id": "test-machine-learning",
    "channelId": "machine-learning",
    "channelName": "Machine Learning",
    "title": "Machine Learning Knowledge Test",
    "description": "Test your understanding of Machine Learning with 25 questions.",
    "questions": [
      {
        "id": "machine-learning-q-0",
        "questionId": "q-15374",
        "question": "You're given 2,000 labeled vehicle images across 4 classes (car, truck, bus, motorcycle) and 10,000 unlabeled images from the same distribution. Build a beginner-friendly CPU-based baseline: a small CNN with data augmentation, plus a rotation self-supervised task to leverage unlabeled data. Describe splits, model, training, evaluation (top-1, calibration), and an incremental labeling plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Train a small CNN with 4 convolutional blocks on the 2,000 labeled images using standard data augmentation (random flips, rotations, and color jitter). For the 10,000 unlabeled images, employ a rotation-prediction self-…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "A cascaded, streaming speech-to-text architecture comprising lightweight language-specific encoders fed by a shared front-end, leveraging 8-bit quantization and operator fusion to achieve sub-250ms latency targets on mi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a 5-way classifier using hashed text features plus 4 numeric features; train with SGDClassifier(loss='log', max_iter=1) using partial_fit; keep feature space under ~1500 dims to meet 40 ms lat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a streaming pipeline using Apache Flink for real-time feature extraction with tumbling windows (5-minute intervals) to compute transaction aggregates including velocity metrics and amount statistics. Deploy a hyb…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical semi-supervised learning reasoning, CPU-friendly architecture design, and model calibration under data scarcity.\n\n## Key Concepts\n- Self-supervised learning via rotation prediction\n- Lightweight CNN architectures for CPU inference\n- Data augmentation and model calibration (ECE, temperature scaling)\n- Incremental labeling strategy for active learning\n\n## Code Example\n```python\nimport torch\nimport torch.nn as nn\n\nclass SimpleCNN(nn.Module):\n    def __init__(self, num_classes=4):\n        super().__init__()\n        self.features = nn.Sequential(\n            nn.Conv2d(3, 32, 3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n            nn.Conv2d(32, 64, 3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n            nn.Conv2d(64, 128, 3, padding=1),\n            nn.ReLU(),\n            nn.MaxPool2d(2),\n            nn.Conv2d(128, 256, 3, padding=1),\n            nn.ReLU(),\n            nn.AdaptiveAvgPool2d(1)\n        )\n        self.classifier = nn.Linear(256, num_classes)\n        self.rotation_head = nn.Linear(256, 4)\n    \n    def forward(self, x, task='classify'):\n        x = self.features(x)\n        x = x.view(x.size(0), -1)\n        if task == 'rotate':\n            return self.rotation_head(x)\n        return self.classifier(x)\n```\n\n## Data Splits\n- Train: 1,400 labeled images (350 per class)\n- Validation: 300 labeled images\n- Test: 300 labeled images\n- Unlabeled pool: 10,000 images\n\n## Training Pipeline\n1. **Supervised baseline**: Train on labeled data with augmentation for 20 epochs\n2. **SSL joint training**: Combine labeled cross-entropy loss with rotation prediction loss (weight=0.5)\n3. **Evaluation**: Report top-1 accuracy on test set; compute ECE with temperature scaling for calibration\n\n## Incremental Labeling Plan\n1. Use uncertainty sampling to select most confident predictions from the unlabeled pool\n2. Label top 500 uncertain samples (entropy-based selection)\n3. Retrain model with expanded labeled set\n4. Repeat until performance plateaus or budget exhausted",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-1",
        "questionId": "q-2783",
        "question": "You're designing a real-time video ranking model for a global streaming platform. Data is highly skewed, frequent updates, and strict latency (≤20 ms) on edge devices; privacy constraints prevent raw data leaving devices. How would you architect a solution that personalizes rankings, preserves privacy, and remains production-safe? Be concrete about models, training, and evaluation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a compact on-device encoder (quantized Transformer) paired with a retrieval module to fetch relevant policy templates. A region adapter gates outputs by language and locale to enforce region-specific policies. Model…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-tower ranking with per-user embeddings cached at the edge; a lightweight ranking head on-device. Use feature hashing to cap feature space, and differential privacy to protect data. Offline trainin",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a cascade: Stage 1 uses a lightweight logistic regression with SGD on engineered features; Stage 2 triggers for high-uncertainty cases using a compact tree ensemble. Calibrate online (isoton",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage a streaming feature store (Kafka + Flink) with incremental SGDClassifier/Partial Fit, per-region isotonic calibration, and ADWIN drift detection. Enforce region-aware fairness constraints (counterfactual or gro…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses designing scalable, privacy-aware personalized ranking with edge latency guarantees and production safety.\n\n## Key Concepts\n- Two-tower architectures\n- Edge caching and latency budgets\n- Feature hashing and DP\n- Offline+online training and counterfactual evaluation\n- Drift monitoring and privacy constraints\n\n## Code Example\n```javascript\n// Pseudo-code for edge ranking with offline+online learning\nfunction offlineTrain(dataset) {\n  // train on logged data\n}\nfunction onlineInfer(userEvent) {\n  // fetch per-user embedding, rank candidates\n}\n```\n\n## Follow-up Questions\n- How would you validate offline metrics to predict online performance?\n- How would you handle cold-start users and new items in this setup?",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-2",
        "questionId": "q-403",
        "question": "You're training a large language model for Notion's AI features. Your model is overfitting on the training data but underperforming on validation. Design a comprehensive regularization strategy that addresses both L1/L2 regularization and more advanced techniques like dropout and early stopping. How would you implement cross-validation to ensure your hyperparameters generalize across different user data distributions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a production-grade calibration-and-drift-aware scoring stack. Calibrate probabilities using isotonic regression on recent labeled data; detect distribution shift with Population Stability Index (PSI) on inputs…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use HOG features extracted from 28x28 grayscale images, fed into an SGDClassifier with loss='log_loss' for online partial_fit updates. Balance classes using sample_weight. Validate sub-60ms latency per image on target C…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement L2 regularization with weight decay 0.01, dropout layers with 0.3 rate, early stopping with patience 5 epochs, and use stratified k-fold cross-validation to validate hyperparameters across user data distributi…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "A practical baseline: implement a compact three-layer CNN (8–16 channels) accepting 32x32 grayscale inputs. For extremely constrained hardware, fall back to a simple two-layer MLP. Apply 8-bit post-training quantization…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical ML engineering skills for production AI systems. Notion needs candidates who can handle real-world model training issues like overfitting and generalization across diverse user data.\n\n## Expected Answer\nStrong candidates will discuss: 1) L2 regularization for weight decay, 2) Dropout implementation specifics (rates, layer placement), 3) Early stopping criteria and validation monitoring, 4) Cross-validation strategy for user data heterogeneity, 5) Hyperparameter tuning approach with grid/random search.\n\n## Code Example\n```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nfrom sklearn.model_selection import StratifiedKFold\n\nclass NotionAIModel(nn.Module):\n    def __init__(self, input_dim, hidden_dim, output_dim, dropout_rate=0.3):\n        super().__init__()\n        self.layers = nn.Sequential(\n            nn.Linear(input_dim, hidden_dim),\n            nn.ReLU(),\n            nn.Dropout(dropout_rate),\n            nn.Linear(hidden_dim, hidden_dim),\n            nn.ReLU(),\n            nn.Dropout(dropout_rate),\n            nn.Linear(hidden_dim, output_dim)\n        )\n    \n    def forward(self, x):\n        return self.layers(x)\n\ndef train_with_regularization(X, y, cv_folds=5):\n    skf = StratifiedKFold(n_splits=cv_folds, shuffle=True, random_state=42)\n    best_val_loss = float('inf')\n    patience_counter = 0\n    max_patience = 5\n    \n    for fold, (train_idx, val_idx) in enumerate(skf.split(X, y)):\n        X_train, X_val = X[train_idx], X[val_idx]\n        y_train, y_val = y[train_idx], y[val_idx]\n        \n        model = NotionAIModel(input_dim=X.shape[1], hidden_dim=256, output_dim=len(set(y)))\n        optimizer = optim.Adam(model.parameters(), lr=0.001, weight_decay=0.01)  # L2 regularization\n        criterion = nn.CrossEntropyLoss()\n        \n        for epoch in range(100):\n            model.train()\n            optimizer.zero_grad()\n            outputs = model(X_train)\n            loss = criterion(outputs, y_train)\n            loss.backward()\n            optimizer.step()\n            \n            # Early stopping validation\n            model.eval()\n            with torch.no_grad():\n                val_outputs = model(X_val)\n                val_loss = criterion(val_outputs, y_val)\n            \n            if val_loss < best_val_loss:\n                best_val_loss = val_loss\n                patience_counter = 0\n                torch.save(model.state_dict(), f'best_model_fold_{fold}.pth')\n            else:\n                patience_counter += 1\n                if patience_counter >= max_patience:\n                    print(f'Early stopping at epoch {epoch} for fold {fold}')\n                    break\n```\n\n## Implementation Strategy\n1. **L2 Regularization**: Apply weight decay 0.01 through optimizer to penalize large weights\n2. **Dropout**: Use 0.3 dropout rate after hidden layers to prevent co-adaptation\n3. **Early Stopping**: Monitor validation loss with patience 5 epochs to prevent overfitting\n4. **Cross-Validation**: Use stratified k-fold (k=5) to ensure representation across user segments\n5. **Hyperparameter Tuning**: Grid search across dropout rates (0.2-0.5) and weight decay (0.001-0.1)\n6. **User Data Stratification**: Group by user behavior patterns to ensure distribution coverage",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-3",
        "questionId": "q-26001",
        "question": "Design a real-time on-device speech-to-text translation app that must run under 150 ms per utterance on mid-range mobile GPUs, across four languages, with privacy-preserving on-device adaptation. Include a streaming encoder-decoder with language adapters, a quantization plan, and a safe rollout with latency and quality metrics, drift checks, and rollback triggers?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-language drift detectors: compute streaming distribution stats (means, variances, quantiles) with DP on aggregates; share via secure aggregation. Trigger staged federated updates: shadow mode → li",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: streaming encoder-decoder with lightweight language adapters, 8-bit quantization, and monotonic attention to meet 150 ms on mobile GPUs; shared base model with language-local adapte",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "On-device: a tiny gated fusion model (e.g., 8k–20k params quantized) combining device fingerprint features and lightweight transaction descriptors; server: an ensemble with longer-horizon features. Us",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Start with exploratory data analysis to identify missing values and outliers. Handle categorical features with one-hot encoding or target encoding. Split data using stratified sampling to maintain churn distribution, ad…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests edge ML design, multilingual streaming, privacy constraints, and rollout safety.\n\n## Key Concepts\n- On-device streaming ASR/MT, quantization, adapters, safe rollout, drift detection, private updates.\n\n## Code Example\n```javascript\n// Pseudo: streaming inference skeleton\nfunction infer(state, chunk){ /* maintain streaming state */ }\n```\n\n## Follow-up Questions\n- How to validate latency under thermal throttling?\n- How to measure translation quality across languages on-device?",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-4",
        "questionId": "q-23003",
        "question": "Mobile app digit recognizer: you have 1k labeled 28x28 grayscale images of digits 0–9. Propose a beginner CPU-friendly baseline that runs under 50 ms per inference on a mid-range device. Describe preprocessing, a simple feature extractor (flatten vs HOG), model choice (logistic regression or small SVM), training with mini-batches, data splits, and how you'd measure on-device latency and memory; include a quantization plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use 8×8 HOG features to reduce input from 784 dimensions to approximately 60–100 dimensions. Preprocess images with normalization (scale pixel values to [0,1]) and optional contrast enhancement. Train a logistic regress…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Adopt a two-pronged approach: (1) parameter-efficient fine-tuning with adapters/LoRA on both vision and text heads while freezing the backbone; train using supervised contrastive loss with 1k–2k fashion catalog examples…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Layered drift monitoring with budgeted retraining. Detect feature drift per feature using ADWIN and KL divergence; flag label drift via counterfactual evaluation. Use temperature scaling for drift-aware calibration. Tra…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Baseline: a compact multilingual encoder with a shared base and per-language adapters, eight languages, distillation to INT8, ~1.5B params total. Decoder with constrained beam search to meet 100 ms on",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical on-device ML design with tight latency and memory budgets—a common constraint at mobile-first companies.\n\n## Key Concepts\n- **Feature extraction**: HOG captures edge orientations and is more robust than raw pixels for digit recognition while remaining CPU-friendly\n- **Classifier choice**: Logistic regression with L2 regularization provides good generalization on small datasets with minimal memory footprint\n- **Training strategy**: Mini-batch SGD converges quickly on small datasets and reduces memory requirements\n- **Quantization**: Post-training quantization (INT8) reduces model size by ~4× and improves inference speed with minimal accuracy loss\n\n## Missing from Original Answer\nThe original answer omitted preprocessing details, on-device measurement methodology, and quantization plan—these are essential for a complete production-ready solution.",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-5",
        "questionId": "q-26943",
        "question": "Design a real-time cross-border payments fraud detection system with streaming transactions (up to 2k/sec) where labels arrive 5–15 minutes after events. Propose an online learning pipeline that handles concept drift, data imbalance, and privacy constraints (differential privacy or federated learning). Specify feature engineering (transaction metadata, geolocation, device fingerprints), model choice (SGD Logistic Regression or lightweight boosted trees streaming), calibration strategy, evaluation plan, and rollback/monitoring procedures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Recommend a streaming tiny conformer (12–15M) or wav2vec 2.0-small with 20–24 ms frames. Use streaming CTC with a compact finance lexicon and optional lightweight LM. On-device int8 quantization; fede",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "An online SGD-Logistic Regression with adaptive learning rates can handle streaming data; use windowed partial_fit to incorporate delayed labels. Features: amount, merchant, geolocation, device finger",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Baseline: standardize/one-hot 25 features; try logistic regression with L2 0.01 or a tiny tree (max depth 3). Use 8-bit quantization and on-device feature hashing to fit memory. Update with partial_fi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-tier approach: on-device lightweight classifier for immediate pass/fail signals (privacy-preserving, ~40 ms/frame) and a server-side multi-modal model using a video CNN, audio transformer, and tex",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design streaming ML with delays, privacy, and operational controls across regions. Requires concrete feature choices, online update strategy, calibration, and rollback.\n\n## Key Concepts\n- Online learning with delayed labels\n- Concept drift detection in high-volume streams\n- Privacy: DP or FL in production\n- Calibration and evaluation under streaming constraints\n\n## Code Example\n```javascript\n// Pseudocode: partial_fit loop\n```\n\n## Follow-up Questions\n- How would you handle variable label latency?\n- How would you validate performance during a rollout?",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-6",
        "questionId": "q-28614",
        "question": "Design a real-time fraud-detection pipeline for a global payments platform. Events stream at high TPS; fraud labels arrive with 3–7 minutes latency. Output a per-transaction fraud probability within 25 ms on CPU. Include data schema, feature hashing, region-aware routing, a hybrid inference stack (fast base model with a regional calibrated head), online learning, drift detection, fairness constraints across country groups, and privacy-preserving update protocol (federated-like)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a streaming, CPU-friendly architecture: 1. **Data Schema**: Capture transaction features (amount, merchant category, timestamp, device fingerprint) alongside region/country identifiers and a hashed user ID for p…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "An online SGD-Logistic Regression with adaptive learning rates can handle streaming data; use windowed partial_fit to incorporate delayed labels. Features: amount, merchant, geolocation, device finger",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Baseline approach: detect language; translate to English and train a Logistic Regression with TF‑IDF features, or use a small multilingual embedding with a linear head. Evaluate with stratified K‑fold",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Baseline: two logistic heads sharing a common hashed feature map from 12 fields (ids hashed to 2^20, numerics standardized). Use online SGD with partial_fit to update both tasks per event. Calibrate w",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design an end-to-end streaming ML system with strict latency constraints, cross-region fairness, privacy considerations, and online adaptation. It probes understanding of model architecture choices, data pipeline design, and model governance under real-world conditions.\n\n## Key Concepts\n- **Streaming Inference**: Processing high-TPS events with strict latency budgets\n- **Online Learning**: Continual model updates as fraud labels arrive with delayed feedback\n- **Calibration**: Ensuring predicted probabilities are reliable across regions\n- **Region-Aware Routing**: Directing traffic to region-specific model instances\n- **Fairness Constraints**: Maintaining demographic parity across country groups\n- **Privacy-Preserving Updates**: Federated-like protocols that protect user data\n\n## Code Example\n```javascript\n// Streaming fraud detection pipeline outline\nconst express = require('express');\nconst { pipeline, InferenceEngine } = require('./fraud-ml');\n\nconst app = express();\napp.use(express.json());\n\nconst engine = new InferenceEngine({\n  baseModelPath: './models/base.onnx',\n  maxLatencyMs: 25,\n  featureHashDim: 256\n});\n\nengine.registerRegion('NA', './models/na-head.onnx');\nengine.registerRegion('EU', './models/eu-head.onnx');\n\napp.post('/predict', async (req, res) => {\n  const { transaction, region } = req.body;\n  \n  const features = hashFeatures(transaction, 256);\n  const baseScore = await engine.predictBase(features);\n  const calibratedScore = await engine.applyRegionalHead(region, baseScore);\n  \n  res.json({ fraudProbability: calibratedScore, latencyMs: engine.lastLatency });\n});\n\n// Online learning endpoint\napp.post('/label', async (req, res) => {\n  const { transactionId, fraudLabel, region } = req.body;\n  \n  await engine.bufferLabel({ transactionId, fraudLabel, region });\n  \n  if (engine.shouldRetrain()) {\n    await engine.updateRegionalHead(region);\n  }\n  \n  res.json({ status: 'acknowledged' });\n});\n\napp.listen(3000);\n```\n\n## Follow-Up Questions\n- How would you measure calibration stability across regions over time?\n- What would you use as a fallback if a regional model goes offline?\n- How do you handle transactions that span multiple regions?\n- What privacy guarantees does your federated-like protocol provide?\n- How would you debug a sudden spike in fraud predictions for a specific region?",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-7",
        "questionId": "q-28986",
        "question": "Edge-on-device health monitoring: 12 sensor signals and 6 rolling window stats per event, running with a 25 ms CPU budget on a mobile device. Labels (anomaly) arrive 5–8 minutes later from the cloud. Propose a beginner baseline pipeline: feature hashing to a fixed-size vector, model choice (logistic regression with SGD or a tiny tree), 8-bit quantization with on-device calibration, drift detection, retraining cadence, data splits, metrics, and latency verification. Include memory constraints?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a hashing-based TF-IDF limiter to stay under memory (HashingVectorizer with n_features ~ 2^18), train logistic regression with L2 and class_weight='balanced'. Normalize text; consider character n-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a privacy-preserving, multi-tenant RAG assistant for a payments platform with the following specifications: latency target of 120 ms end-to-end for short replies on CPU; encrypted vector store; a 6-8B parameter…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by generating a precision-recall curve to visualize the trade-off between precision and recall across different classification thresholds. Identify the specific threshold range where precision drops significantly…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed baseline: on-device, fixed-size feature vector via hashing (2^20 buckets) for 18 features; use SGD Logistic Regression with partial_fit for online updates; apply 8-bit quantization and Platt-",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nProbes practical edge ML constraints: latency, memory, and delayed supervision shape a sane baseline. Tests knowledge of feature hashing, online learning, calibration, and drift handling in constrained environments.\n\n## Key Concepts\n- On-device ML with tight latency\n- Feature hashing and fixed-size vectors\n- Online learning with partial_fit\n- Model calibration and drift detection\n- Memory and power constraints in mobile devices\n\n## Code Example\n```python\nfrom sklearn.linear_model import SGDClassifier\n# x is a fixed-size hashed feature vector, y is 0/1 label\nclf = SGDClassifier(loss='log', max_iter=1000)\nclf.partial_fit(x, y, classes=[0,1])\n```\n\n## Follow-up Questions\n- How would you extend to handle concept drift without frequent offline retraining?\n- What are alternatives to hashing to reduce collisions and improve accuracy?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-8",
        "questionId": "q-9224",
        "question": "You're given 50k customer support emails labeled into 6 classes with severe class imbalance. Design a beginner-friendly baseline pipeline that outputs calibrated probabilities within 1s latency, including: text preprocessing, feature extraction, model choice (logistic regression vs shallow NN), calibration, evaluation set to avoid leakage, and a simple retraining plan on new labels?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline: 25 features from sensors, compute rolling means/stds, scale; use SGDClassifier with log loss and partial_fit; initialize with [0,1], option for a tiny tree for nonlinear residuals. Calibrate",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "This is overfitting. The model memorizes training data but fails to generalize. Diagnose by plotting train/val loss, checking learning curves. Fix with regularization (dropout, L2), early stopping, data augmentation, or…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I'd propose a shared encoder (XLM-R) with language-conditioned heads. Calibrate per language via temperature scaling on held-out data to equalize FPRs across languages within plus/minus 0.5%. Monitor",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly baseline: text preprocessing (lowercasing, punctuation removal), feature extraction (TF‑IDF), model choice (logistic regression with L2), calibration (Platt scaling), evalua",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses end-to-end practicality of building a text classification baseline with calibrated outputs under latency constraints, suitable for beginners yet showing depth in choices and trade-offs.\n\n## Key Concepts\n\n- Text preprocessing and feature extraction\n- Calibrated probability estimates\n- Lightweight models and latency targets\n- Leakage-free evaluation and simple retraining cadence\n\n## Code Example\n\n```python\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.calibration import CalibratedClassifierCV\nfrom sklearn.pipeline import Pipeline\n\npipe = Pipeline([\n  ('tfidf', TfidfVectorizer(stop_words='english', max_df=0.95)),\n  ('clf', CalibratedClassifierCV(LogisticRegression(max_iter=1000), method='sigmoid', cv=5))\n])\n```\n\n## Follow-up Questions\n\n- How would you handle severe class imbalance beyond basic class weights?\n- What changes if latency target drops to 100ms or scales to 3x classes?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-9",
        "questionId": "q-9333",
        "question": "DESIGN a real-time ML system for fraud detection in a cross-platform digital wallet and merchant checkout flow used by a PayPal/Delivery ecosystem. The system must preserve user privacy (DP/Secure Aggregation), operate with sub-10 ms latency per event, adapt to non-stationary fraud patterns via online retraining and drift detectors, and support compliant explainability. Provide data schemas, feature extraction, model architecture (tabular + graph features), evaluation, and staged rollout plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a hybrid architecture: edge-optimized language detector + multilingual toxicity classifier (quantized MobileBERT/DistilBERT via ONNX Runtime) + server-side heavier multitask model for ambiguou",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a versioned feature store with offline feature computation and online serving cache. Enforce per-user DP budgets and explore federated updates to minimize data transfer. Add drift detection",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a lightweight multilingual model (DistilBERT with adapters or a CNN-based text encoder) for on-device inference. Use self-trainingabels on unlabeled reviews with confident pseudo-l combined with a small labeled…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a two-tier architecture: (1) a lightweight client-side model for immediate on-device screening and (2) a server-side graph-augmented model for comprehensive fraud analysis. Deploy differential privacy during t…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates practical ML system design expertise for fintech, balancing privacy, latency, adaptability, and compliance across digital platforms.\n\n## Key Concepts\n- Privacy-preserving training (DP-SGD, secure aggregation)\n- Real-time streaming with sub-10ms latency constraints\n- Online drift detection and adaptive model retraining\n- Regulatory-compliant explainability mechanisms\n- Scalable data schemas and staged deployment strategies\n\n## Code Example\n```python\n# Simplified drift monitoring implementation\nclass DriftMonitor:\n    def __init__(self, window=1000, alpha=0.01):\n        self.window = window\n        self.alpha = alpha\n        self.baseline_stats = None\n        \n    def monitor_drift(self, current_data):\n        # Compute statistical distance between baseline and current\n        if self.baseline_stats is not None:\n            drift_score = self._compute_drift_score(current_data)\n            return drift_score > self.alpha\n        return False\n```\n\n## Implementation Considerations\n- Schema design for real-time transaction events\n- Feature engineering combining tabular and graph embeddings\n- Model architecture balancing accuracy and inference speed\n- Comprehensive evaluation metrics beyond accuracy (precision, recall, F1, AUC)\n- Gradual rollout strategy with A/B testing and shadow mode deployment",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-10",
        "questionId": "q-26466",
        "question": "Design a privacy-preserving, on-device multimodal recommender for a mobile app (text reviews + product images) that delivers top-5 results in ~70 ms on mid-range hardware. Use a two-tower encoder with quantization, cross-modal fusion, and an on-device ANN; describe training with federated averaging + secure aggregation, DP, drift detection, and update cadence?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-tower on-device multimodal recommender: text encoder (distilBERT-tiny) and image encoder (MobileNetV3). Quantize to int8; project to shared embedding; fuse with a lightweight cross-attention head.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Proposed system: a detector-lite (MobileNetV3-SSD) plus a Kalman-filter tracker with Hungarian data association; ONNX Runtime with INT8/FP16; hardware-aware pruning to hit sub-30 ms. On-device drift c",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set up per-region drift detectors (distributional shifts via Population Stability Index and KL divergence on features) and per-region calibrators (isotonic regression or temperature scaling). Use a st",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a streaming, CPU-friendly architecture: 1. **Data Schema**: Capture transaction features (amount, merchant category, timestamp, device fingerprint) alongside region/country identifiers and a hashed user ID for p…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests privacy-preserving on-device ML, multimodal fusion, and efficient latency. Evaluates understanding of FL/DP, quantization, and on-device retrieval with drift handling.\n\n## Key Concepts\n- On-device inference and latency budgeting\n- Multimodal embedding fusion\n- Federated learning with secure aggregation and differential privacy\n- Approximate nearest neighbors for retrieval\n\n## Code Example\n```javascript\n// Pseudo on-device inference sketch\nfunction inference(text, image) {\n  const t = textEncoder.encode(text);\n  const i = imageEncoder.encode(image);\n  const z = fuse(t, i);\n  return topK(z, 5);\n}\n```\n\n## Follow-up Questions\n- How would you monitor and respond to drift in user preferences?\n- How would you handle new modalities or cold-start users without central data access?",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-11",
        "questionId": "q-19027",
        "question": "You’re building an on-device content recommender for a mobile streaming app. The model must run under 50 ms on a CPU in a browser and use only 1k labeled interactions across 25 features. Propose a beginner baseline pipeline: feature preprocessing, model choice (logistic regression vs small tree), quantization, incremental updates, evaluation metrics (latency, AUC, calibration), and a simple retraining cadence. Include data splits and latency validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed baseline: enforce time-ordered splits, drop or lag any future-looking features, compute features with rolling means and lags, use linear regression or a small tree, apply rolling updates, dri",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Parameters are learned during training, hyperparameters are set before training. Cross-validation prevents overfitting by testing hyperparameters on multiple data splits.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-tower architecture: a device-side classifier leverages features from device telemetry, network context, and order metadata to produce a sub-50ms risk score. A server-side DP-enabled ensemble trains on federated aggr…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Baseline: standardize/one-hot 25 features; try logistic regression with L2 0.01 or a tiny tree (max depth 3). Use 8-bit quantization and on-device feature hashing to fit memory. Update with partial_fi",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests on-device inference, lightweight models, and latency accounting in a realistic mobile setting.\n\n## Key Concepts\n- On-device ML, model selection, quantization, partial_fit, latency validation, calibration, data splits.\n\n## Code Example\n```python\n# Pseudo: normalize, train small LR, quantize weights\n```\n\n## Follow-up Questions\n- How would drift affect AUC and how would you detect it?\n- What changes if 10x labeled data becomes available?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-12",
        "questionId": "q-23954",
        "question": "Design a real-time root-cause localization system for cloud workloads using multi-source telemetry (metrics, logs, traces). Target: 200 ms latency at the 95th percentile on a CPU+GPU heterogeneous cluster; support missing modalities, online drift adaptation, and safe rollback. Include feature extraction, fusion strategy, evaluation plan, and deployment approach?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Hybrid: compute lightweight features on-device (device fingerprint, recent auth failures, tap cadence, location jitter), hash/mask and send to server; run a compact, quantized LightGBM model (e.g., 10",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a dual-model approach: fast online features with a latency-targeted model and a drift-aware ensemble updated via canary rollout. Detect drift with KL divergence and CUSUM; auto-rollback if drift p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a two-tier streaming pipeline: extract lightweight features from metrics, logs, and traces; fuse with a compact attention head and allow early exits. Implement ONNX/torchscript runtimes with mixed",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Baseline: concatenate subject and body, TF-IDF with 1-2 word and character n-grams, train logistic regression with class_weight='balanced'. Evaluate with stratified 5-fold CV using ROC-AUC and Brier s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nCloud platforms demand rapid pinpointing of issues as telemetry changes across modalities. This question probes practical system design, real-time constraints, and robust handling of missing data and drift.\n\n## Key Concepts\n\n- Real-time streaming architectures and latency budgeting\n- Multi-modal feature extraction and fusion\n- Drift detection (ADWIN, CUSUM) and safe rollback\n- Hardware-aware inference (mixed precision, ONNX/torchscript)\n\n## Code Example\n\n```javascript\n// Pseudo skeleton for fusion with early exit\nfunction fuseFeatures(features) {\n  // lightweight attention-based fusion\n  // decide if early exit is feasible\n  return { score, exitPoint };\n}\n```\n\n## Follow-up Questions\n\n- How would you validate latency percentile targets under bursty traffic?\n- What failure modes require automatic rollback and how would you test them?",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-13",
        "questionId": "q-22124",
        "question": "Design a beginner baseline for real-time fault probability in a vehicle fleet: streaming telemetry with 120 numeric features and a 0–15 word diagnostic log per event. Labels arrive 5–10 minutes later. Target latency: 80 ms per event on CPU. Propose a CPU-friendly baseline pipeline using numeric standardization, a text hashing-based feature for the log, two logistic regression streams (numeric and text) with partial_fit, a simple ensemble, drift detection, and calibration. Include data splits, metrics, and a latency validation plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a two-tier design: on-device lightweight model (e.g., boosted trees) using minimal features (amount, time, device_id, geolocation delta) with secure enclaves, and a cloud model for richer cont",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-stream baseline: numeric features standardized; diagnostic log text hashed to 2^20 bins; train separate LogisticRegression models using partial_fit for online updates; ensemble by averaging probabilities; add drift…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Two-tier privacy-preserving anomaly detector. Edge: per-tenant lightweight autoencoder or one-class model for <30 ms inference, flag potential fraud. Server: DP-FL trained ensemble that refines decisi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a baseline with features: recency (days since last impression), dwell_time, slot_position, device_type; hashed user proxy; model: logistic regression with L2; on-device training via partial_fi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical, CPU-friendly baseline skills for streaming ML with mixed data types (tabular and text) and delayed labels. It evaluates understanding of online learning, feature engineering for text, calibration, and drift handling in production systems.\n\n## Key Concepts\n\n- Multimodal features (numeric telemetry + text diagnostic logs)\n- Online learning via partial_fit for streaming updates\n- Feature hashing (hashing trick) for text data\n- Drift detection using KL divergence\n- Probability calibration for reliable confidence scores\n- Ensemble methods for combining model streams\n\n## Code Example\n\n```javascript\nclass BaselineModel {\n  constructor() {\n    this.scaler = new StandardScaler();\n    this.numericModel = new LogisticRegression();\n    this.textModel = new LogisticRegression();\n  }\n\n  partialFitNumeric(X, y) {\n    const Xscaled = this.scaler.partialFit(X);\n    this.numericModel.partialFit(Xscaled, y);\n  }\n\n  partialFitText(textData, y) {\n    const Xhashed = this.hashText(textData);\n    this.textModel.partialFit(Xhashed, y);\n  }\n\n  predict(probaNumeric, probaText) {\n    return (probaNumeric + probaText) / 2;\n  }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-14",
        "questionId": "q-18200",
        "question": "Design a multi-tenant real-time recommendation system for video thumbnails on a streaming platform where latency target is 25 ms per inference on Nvidia GPUs. Propose a hardware-aware inference stack with early exit, mixed precision, and per-tenant personalization, while providing privacy guarantees via DP or FL. Include data schema, feature store, model architecture, deployment, and monitoring plan, plus a rollout strategy for gradual tenant onboarding?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A hybrid offline-online feature store that ingests client and edge telemetry, delivers sub-60ms latency for anomaly scoring and content personalization, and supports versioned features stored in MongoDB with hot caching…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a lightweight CNN for image, a compact text encoder for captions, and an efficient audio feature extractor. Fuse with gated multimodal attention. Use self-supervised pretraining across modali",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-tower architecture: a shared backbone for feature extraction and per-tenant embeddings for personalization. The inference path uses an early-exit shallow head to meet 25 ms on Nvidia GPUs, with dy",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Two-tower architecture: a device-side classifier leverages features from device telemetry, network context, and order metadata to produce a sub-50ms risk score. A server-side DP-enabled ensemble trains on federated aggr…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-time multi-tenant deployment, latency budgets, privacy guarantees, and drift monitoring in production-like settings.\n\n## Key Concepts\n\n- Real-time multi-tenant inference\n- Early-exit and mixed-precision optimization\n- DP/FL privacy guarantees and secure aggregation\n- Drift detection and rollout strategy\n\n## Code Example\n\n```javascript\n// Pseudo-inference skeleton for two-tower model\nfunction twoTowerInfer(input, tenantId, model) {\n  // shared backbone\n  const shared = model.backbone.extract(input.features)\n  // tenant-specific embedding\n  const emb = model.tenantEmbeddings[tenantId](input.userId)\n  // combine and head with early exit\n  const fused = concatenate(shared, emb)\n  return model.heads[0](fused) // early exit path\n}\n```\n\n## Follow-up Questions\n\n- How to handle new tenants with cold-start data? \n- How to quantify privacy-utility trade-offs with DP/Secure Aggregation?",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-15",
        "questionId": "q-7159",
        "question": "Deploy a lightweight plant-leaf disease classifier on a Raspberry Pi (CPU-only) processing 720p video at 30 FPS. What concrete steps would you take to meet a 50 ms inference budget? Specify dataset, model family (e.g., MobileNetV2), quantization strategy, pruning, preprocessing, on-device runtime (TensorFlow Lite/ONNX Runtime), and a rollout plan. Provide a minimal code snippet showing TFLite conversion with quantization?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a modular design: a lightweight on-device branch for tabular + text features using a two-tower setup, fusion via cross-attention, early exit with gates to hit 50 ms on edge, and DP-SGD federat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Logistic regression with hashed feature vectors to control dimensionality, combined with min-max scaling for numeric features and lightweight one-hot encoding for key categorical variables. Apply L2 regularization for g…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Hybrid edge-cloud design: on-device temporal autoencoder (1D CNN + lightweight LSTM) flags anomalies by reconstruction error; a central lightweight classifier refines decisions using aggregated summar",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Choose MobileNetV2, train on a 224x224 leaf-disease dataset, then apply post-training int8 quantization with a representative dataset. Deploy with TensorFlow Lite on CPU, verify sub-50 ms per frame fo",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical on-device ML deployment, edge latency, and model optimization—core skills for real-world ML engineers.\n\n## Key Concepts\n- On-device inference, quantization, representative dataset, latency benchmarking, edge deployment pipelines, drift monitoring.\n\n## Code Example\n```python\nimport tensorflow as tf\n# assume `model` is a pretrained Keras model\nconverter = tf.keras.TFLiteConverter.from_keras_model(model)\nconverter.optimizations = [tf.lite.Optimize.DEFAULT]\n# representative dataset generator\ndef representative():\n  for data in dataset_gen():\n    yield [data]\nconverter.representative_dataset = representative\ntflite_model = converter.convert()\nopen(\"model.tflite\",\"wb\").write(tflite_model)\n```\n\n## Follow-up Questions\n- How would you validate latency across different Pi models or boards?\n- How would you handle model updates and offline drift detection?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-16",
        "questionId": "q-1220",
        "question": "You're deploying a single on-device model for real-time video analytics on an edge device with 12 ms per frame latency and 200 MB RAM. The model must perform both object detection and semantic segmentation. Describe a concrete plan to meet latency while preserving accuracy: architecture choices (shared backbone, task heads, feature pyramids), training strategies (loss weighting, distillation, data augmentation), deployment optimizations (quantization, operator fusion, memory layout, early exits), and validation strategy (latency budgets, mAP, mIoU, robustness across weather)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a lightweight shared backbone (e.g., MobileNetV3 or ConvNeXt-tiny) with two compact heads and a small feature pyramid. Use a balanced multi-task loss with gradient normalization; distill from a",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a two-path system: an ultra-fast on-device multi-modal classifier (text + image previews) under 50 ms, plus a privacy-preserving explainer module that produces concise, policy-aligned justifica",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a privacy-preserving, multi-tenant RAG assistant for a payments platform with the following specifications: latency target of 120 ms end-to-end for short replies on CPU; encrypted vector store; a 6-8B parameter…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a two-tier design: on-device lightweight model (e.g., boosted trees) using minimal features (amount, time, device_id, geolocation delta) with secure enclaves, and a cloud model for richer cont",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess ability to design on-device, multi-task ML under tight latency and memory constraints, with cross-task interactions and robustness to weather.\n\n## Key Concepts\n- Edge latency budgets, memory constraints, and hardware-aware design\n- Shared trunk vs task-specific heads; feature pyramids\n- Multi-task loss, distillation, augmentation\n- Quantization, operator fusion, memory layout, early exits\n- Validation across varying conditions (rain/night) and latency\n\n## Code Example\n```javascript\n// Simple fused loss schematic\nfunction multiTaskLoss(detLoss, segLoss, wDet=0.5, wSeg=0.5){\n  return wDet*detLoss + wSeg*segLoss;\n}\n```\n\n## Follow-up Questions\n- How would you measure and guarantee latency with bursty input?\n- What are the failure modes when weather shifts distribution and how would you mitigate?",
        "difficulty": "advanced"
      },
      {
        "id": "machine-learning-q-17",
        "questionId": "q-2682",
        "question": "Describe a minimal, end-to-end churn-prediction pipeline for 25k records with numeric features (tenure, spend), categorical features (region, device_type, plan) and a high-cardinality feature like 'customer_segment_id'. Data drift occurs monthly; outline preprocessing (encode high-cardinality features, handle missing), model choice (logistic vs tree-based) and rationale, evaluation (AUC, calibration), and drift monitoring/retraining cadence with concrete steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a 30-feature hashing-based representation combining char n-grams and hashed word features. Train a logistic regression model with SGD and partial_fit for online updates on mini-batches. Preprocess",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Hybrid real-time fraud score: fast structured-model (LightGBM) for amount/time/device; compact text encoder (TinyBERT) for notes; on-device quantized inference; late fusion with a calibrated sigmoid;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Avoid using customer_segment_id directly; engineer time-based features (months since signup, recency). For high-cardinality region, use target encoding or hashing; one-hot small categories. Scale nume",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Two-tower architecture: a shared backbone for feature extraction and per-tenant embeddings for personalization. The inference path uses an early-exit shallow head to meet 25 ms on Nvidia GPUs, with dy",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes the ability to design robust, production-ready ML pipelines that handle common data issues like high-cardinality features, missing data, and concept drift, plus actionable modeling/evaluation choices.\n\n## Key Concepts\n- End-to-end pipeline design\n- High-cardinality encoding strategies\n- Drift detection and retraining cadence\n- Calibration and KS/PSI monitoring\n\n## Code Example\n```javascript\n// Pseudocode for pipeline\nfunction buildPipeline() {\n  // 1. split data\n  // 2. apply target/hash encoding for high-cardinality cols\n  // 3. assemble model (XGBoost)\n  // 4. evaluate with AUC and calibration curves\n}\n```\n\n## Follow-up Questions\n- How would you implement drift detection in production?\n- What are trade-offs between target encoding and hashing for high-cardinality features?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-18",
        "questionId": "q-28366",
        "question": "In a smart-city traffic sensing system, you stream 40 features per vehicle pass (speed, heading, lane, location_bucket, weather, time_of_day, sensor_id, etc.). Labels indicate risk of near-future congestion in the next 2 minutes and arrive after 3-5 minutes due to aggregation. Output a per-event risk score within 30 ms on CPU. Propose a beginner baseline pipeline focusing on feature hashing for high-cardinality fields, a logistic regression with online updates, and a simple drift detector on residuals. Include data splits, metrics, and latency validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "The pipeline uses a tiered architecture with on-device early-exit classifiers for immediate decisions and server-side fusion for ambiguous cases. On-device text moderation employs a distilled multilingual Transformer (e…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Split chronologically to avoid leakage (train on oldest 80%, test on most recent 20%). Start with logistic regression with L2 regularization, standardize numeric features using training set statistics, and one-hot encod…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Provide a beginner baseline that uses feature hashing for sensor_id and other high-cardinality fields, a logistic regression with partial_fit for online updates, and a simple drift detector on residua",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a real-time feature-quality gate for a multi-tenant inference service. Validate each incoming feature vector with per-feature range checks, basic z-scores, and monotonicity tests; monitor drift",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a practical online baseline for streaming, with high-cardinality features and delayed labels, while emphasizing latency and drift handling.\n\n## Key Concepts\n- Online learning with partial_fit\n- Feature hashing to handle cardinality\n- Drift detection from residuals\n- Latency and calibration-focused evaluation\n\n## Code Example\n```python\n# sketch: online_logistic_regression with partial_fit\nfrom sklearn.linear_model import SGDClassifier\nclf = SGDClassifier(loss='log', max_iter=1, tol=1e-3)\n# x_batch hashed_features, y_batch available later\nclf.partial_fit(X_batch, y_batch, classes=np.unique(y))\n```\n\n## Follow-up Questions\n- How would you handle new sensors appearing over time?\n- How would you validate latency on CPU under load?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-19",
        "questionId": "q-12581",
        "question": "Design a privacy-preserving, real-time content ranking system for a global video platform that must produce top-5 recommendations in under 25 ms on edge GPUs, supports cross-user fairness constraints without exposing user data, uses feature-level DP, and enables counterfactual explanations. Describe model architecture, latency budgeting, privacy/fairness checks, drift handling, and rollout?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On-device first: privacy-preserving and low latency; target <30 ms per transaction on mid-range CPUs. Use feature hashing for high-cardinality features, a small on-device neural model (~1M params), an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-tower on-device ranking: user embedding computed on-device; prefetch item embeddings on edge; ranker selects top-5 in under 25 ms on mid-range GPUs. Use per-user fairness constraints via a post-ho",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "An end-to-end fraud detection system for a global fintech platform. Ingest streaming events via Kafka into a per-tenant feature store (Feast) with online features cached in Redis for sub-15ms latency. Deploy quantized t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Baseline plan: normalize 40 features; compare logistic regression (L2 1e-4) and a small depth-4 tree; use partial_fit on streaming batches (1k events). Include an unsupervised isolation forest as a sa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design privacy-preserving, low-latency systems at the edge with fairness and explainability, plus practical rollout.\n\n## Key Concepts\n- Two-tower on-device ranking\n- Feature-level DP and DP-SGD\n- Fairness constraints in ranking with post-hoc adjustments\n- Drift detection and incremental rollout\n\n## Code Example\n```javascript\n// Pseudo-code sketch for scoring function\nfunction score(userEmbed, itemEmbeds){ /* ... */ }\n```\n\n## Follow-up Questions\n- How would you generate counterfactual explanations for the top-5 items?\n- What metrics and rollback criteria would you use during canary rollout?",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-20",
        "questionId": "q-28194",
        "question": "Streaming e-commerce telemetry: 15 features per event (session_id, user_id, device, referrer, page_type, time_on_page, scroll_depth, clicks_per_min, etc.). Predict purchase probability in the next 2 minutes. Labels arrive with a 4-6 minute delay. Output a per-event score within 25 ms on CPU. Propose a beginner baseline pipeline: preprocessing, model choice (SGD logistic regression or small tree), online learning, calibration, drift detection, retraining cadence, data splits, metrics, latency validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a MobileNetV3-like backbone, train on-device with DP-SGD (privacy budget ~1–2), local epochs 1–3, and send only clipped deltas via secure aggregation. Central server runs FedAvg with momentum, plu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Split by time (80/10/10) to mimic streaming. Use TF-IDF with unigrams+bigrams; train Logistic Regression (C=1.0, liblinear). Calibrate with Platt scaling; select threshold to maximize F1 at target pre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Baseline: use lightweight online logistic regression with SGD, one-hot encode categorical features via hashing trick, scale numeric features, use partial_fit for streaming updates, simple calibration",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement domain adaptation with weather-specific batch normalization, synthetic data augmentation, and multi-task learning. Use domain adversarial training, model pruning for real-time performance, and knowledge distil…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a beginner real-time ML baseline under tight latency and delayed supervision, with online learning, drift handling, and evaluation.\n\n## Key Concepts\n- Online learning with partial_fit\n- Feature encoding for mixed data\n- Simple calibration and drift checks\n- Latency accounting and CPU constraints\n\n## Code Example\n```javascript\n// Pseudocode for streaming SGD+partial_fit\n```\n\n## Follow-up Questions\n- How would you adjust if label delay extended to 15 minutes?\n- What monitoring would you add for calibration drift?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-21",
        "questionId": "q-4767",
        "question": "You’re building a lightweight classifier to predict whether a user taps a new AR lens in their first session after install. Dataset: per-session features include time_of_day (0-23), device_type (categorical), app_version (string), lens_popularity (float), user_id (high-cardinality), and target_tapped (0/1). Propose a concrete baseline ML pipeline that handles high-cardinality IDs without leakage, chooses an encoding, uses a simple model, and evaluates with AUROC and PR-AUC. Include a minimal implementation plan and a short code snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Encode user_id with a hashing trick to a fixed number of bins (e.g., 2^20); one-hot or target-encode device_type; numeric features kept as-is after imputation; split by time to avoid leakage across se",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Edge pipeline: 8 features (binary keywords, length bin, sentiment). Model: logistic regression with partial_fit or a small tree; employ quantization-aware training for 8-bit inference; online updates",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement domain adaptation with weather-specific batch normalization, synthetic data augmentation, and multi-task learning. Use domain adversarial training, model pruning for real-time performance, and knowledge distil…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A two-tower edge architecture featuring a lightweight stereo depth branch paired with an image backbone, fused through attention-based cross-modal fusion. The 3D detector head achieves the ~60 ms latency target on embed…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical encoding of high-cardinality features, leakage-aware split strategy, and a quick baseline with clear evaluation.\n\n## Key Concepts\n- High-cardinality encoding ( hashing, target encoding )\n- Leakage-free train/val/test splits by time and user\n- Simple models ( logistic regression, small boosted trees )\n- Calibration and evaluation metrics AUROC/PR-AUC\n\n## Code Example\n```javascript\n// Pseudo-pipeline sketch\nconst features = preprocessRow(row); // hash user_id to int, encode device_type, passthrough numerics\nconst model = trainLogisticRegression(features, label);\n```\n\n## Follow-up Questions\n- How would you compare hashing vs target encoding for user_id?\n- How would you monitor model drift for lens popularity over time?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-22",
        "questionId": "q-254",
        "question": "When implementing a bidirectional GRU vs LSTM for sequence labeling, how do gradient clipping thresholds and batch size affect convergence and what are the memory trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant feature hashing with a small shared model, privacy via DP-SGD or FedAvg, online calibration, and drift monitoring. Partition data by tenant and time window; train on delayed labels with rol",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Bidirectional GRU needs lower clipping thresholds (1.0-5.0) than LSTM (5.0-10.0) due to fewer parameters, with optimal batch sizes 32-64 for GRU vs 16-32 for LSTM to balance convergence speed and memo",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "A compact video encoder (e.g., MobileViT-Lite) fused with a lightweight audio network, featuring a two-stage early-exit architecture to achieve sub-40ms latency per frame. The model uses int8 quantization and operator-l…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An end-to-end fraud detection system for a global fintech platform. Ingest streaming events via Kafka into a per-tenant feature store (Feast) with online features cached in Redis for sub-15ms latency. Deploy quantized t…",
            "isCorrect": false
          }
        ],
        "explanation": "## Concept Overview\n\nBidirectional sequence models process data in both forward and backward directions, concatenating hidden states for each timestep. GRU uses 2 gates (reset, update) while LSTM uses 3 gates (input, forget, output) plus a cell state, affecting parameter count and memory requirements.\n\n## Implementation Details\n\n### Gradient Clipping Differences\n- **GRU**: More sensitive to exploding gradients due to simpler gating, requires lower clipping threshold\n- **LSTM**: More stable with cell state, tolerates higher clipping values\n- **Bidirectional**: Doubles gradient flow, making clipping critical\n\n### Batch Size Trade-offs\n- **GRU**: Larger batches (32-64) work well due to faster computation\n- **LSTM**: Smaller batches (16-32) preferred to manage memory overhead\n- **Bidirectional**: Memory usage doubles with sequence length\n\n### Memory Considerations\n```python\n# GRU vs LSTM memory comparison per timestep\ndef model_memory(batch_size, seq_len, hidden_dim):\n    # GRU: (reset_gate + update_gate + candidate) * 3\n    gru_params = 3 * hidden_dim * hidden_dim * 3\n    \n    # LSTM: (input_gate + forget_gate + output_gate + candidate) * 4 + cell_state\n    lstm_params = 4 * hidden_dim * hidden_dim * 4 + hidden_dim\n    \n    # Bidirectional doubles memory requirements\n    bidirectional_factor = 2\n    \n    return {\n        'gru': gru_params * batch_size * seq_len * bidirectional_factor,\n        'lstm': lstm_params * batch_size * seq_len * bidirectional_factor\n    }\n```\n\n## Common Pitfalls\n\n1. **Over-clipping GRU**: Setting threshold too low (<1.0) causes underfitting\n2. **Batch size too large for LSTM**: Leads to OOM errors with bidirectional processing\n3. **Ignoring sequence padding**: Variable-length sequences waste memory\n4. **Not using gradient checkpointing**: Critical for long sequences with bidirectional models\n\n## Performance Trade-offs\n\n- **GRU**: 15-25% faster training, 20% less memory, slightly lower accuracy on complex tasks\n- **LSTM**: Better long-term dependency capture, higher memory usage, slower convergence\n- **Choice**: GRU for real-time applications, LSTM for tasks requiring deep memory",
        "difficulty": "intermediate"
      },
      {
        "id": "machine-learning-q-23",
        "questionId": "q-2954",
        "question": "In a beginner-friendly setting, you have a small tabular dataset with features a, b, c and target y. Propose a concrete approach to prevent data leakage during evaluation, choose a baseline model, and explain how you would validate performance using a robust cross-validation strategy, including data preprocessing and evaluation metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline: one-hot encode hour_of_day, day_of_week; keep weather as categories; scale not required. Start with Logistic Regression; compare with LightGBM/GBM if allowed. Use a time-based split (train 7",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a hybrid pipeline: on-device distilled multilingual transformer (e.g., TinyBERT with language adapters) for 200 ms inference, plus a server classifier for refinement. Use active learning to re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a practical pipeline using adapter modules on a shared base language model (e.g., T5 or OPT), with each tenant receiving a lightweight adapter trained on their local data. Federated updates aggregate adapter weig…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use stratified k-fold CV on a small tabular dataset to preserve class balance. Build a pipeline with SimpleImputer, StandardScaler, and LogisticRegression; test a tree-based baseline too. Guard agains",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis checks practical CV setup, leakage awareness, and model selection on small data.\n\n## Key Concepts\n- Data leakage prevention\n- Stratified cross-validation\n- Pipeline preprocessing\n- Model comparison metrics (AUROC, calibration)\n\n## Code Example\n```javascript\n// Pseudo-code: stratified K-fold CV in JS-like syntax\nconst k = 5;\nconst folds = stratifiedKFoldSplit(labels, k);\nfor (const [trainIdx, testIdx] of folds) {\n  const model = trainLogisticRegression(features[trainIdx], labels[trainIdx]);\n  const preds = model.predictProba(features[testIdx]);\n  console.log(rocAuc(labels[testIdx], preds));\n}\n```\n\n## Follow-up Questions\n- How would you handle class imbalance if AUROC is similar across models?\n- How could you incorporate feature engineering without leaking future information?",
        "difficulty": "beginner"
      },
      {
        "id": "machine-learning-q-24",
        "questionId": "q-16455",
        "question": "Edge gesture classifier on wearable: design a beginner-friendly baseline pipeline to classify 3D accelerometer gestures on-device with <200 ms latency and a model size under 256 KB. Use streaming 0.3 s windows with 50% overlap; features per window: mean, std, min, max, entropy, zero-crossings for each axis. Choose logistic regression vs tiny neural net; include on-device calibration, incremental updates plan, and energy budget checks. Include data splits and latency verification?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a real-time fraud risk scoring system for a global payments platform ingesting streaming events from issuers/merchants. Achieve sub-60 ms inference latency per event, support on-device scori",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Baseline: 0.3s sliding window with 50% overlap; features per axis: mean, std, min, max, entropy, zero-crossings. Use logistic regression with L2 to stay under ~256 KB; optionally a tiny MLP if feature",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a two-tier explainability architecture: a compact, privacy-preserving attribution vector generated offline for common feature templates, paired with a lightweight surrogate model that renders faithful explanatio…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Prototype a streaming drift-monitoring stack for real-time fraud detection with evolving user behavior. Track data drift with KS/JSD between recent feature distributions and the historical baseline; m",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical edge deployment, beginner feature design, and simple online/offline updates.\n\n## Key Concepts\n- On-device ML constraints, feature extraction, model size\n- Windowing and latency budgeting\n- Calibration, data splits, evaluation\n\n## Code Example\n```javascript\n// pseudocode for feature extraction and logistic regression inference\n```\n\n## Follow-up Questions\n- How would you reduce memory footprint further? \n- How would you validate latency on-device across different phones?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-14T09:34:30.241Z",
    "version": 1
  }
]