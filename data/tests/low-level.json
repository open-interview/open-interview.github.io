[
  {
    "id": "test-low-level",
    "channelId": "low-level",
    "channelName": "LOW Level",
    "title": "LOW Level Knowledge Test",
    "description": "Test your understanding of LOW Level with 25 questions.",
    "questions": [
      {
        "id": "low-level-q-0",
        "questionId": "q-3485",
        "question": "Design a crash‑consistent, lock‑free, bounded ring buffer in shared memory that acts as a persistent queue across restarts. It must be backed by a memory‑mapped file, use per‑slot CRCs, and a two‑phase commit so writers can resume after power loss. Describe enqueue/dequeue algorithms, restoration after crash, and how you handle memory ordering, reclamation of retired descriptors, and backpressure. Include a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a crash‑consistent, lock‑free bounded ring in shared memory backed by a mmapped file. Use per‑slot CRCs and a two‑phase commit so writers can resume after power loss; dequeue under reader prog",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a NUMA-aware allocator with per-thread caches for <=128-byte objects, backed by a central pool on a designated NUMA node. API: alloc/free; layout: per-thread freelists + central pool; sync via",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a lock-free free-list encoded in a 64-bit head: lower 32 bits for the head block index, upper 32 bits for a version counter to defeat ABA. Initialize the arena into a singly linked free list. allo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two writers and one reader share a shm ring buffer with N=1<<16; each slot has a 64-bit seq and 128-byte payload. Producers CAS head, write payload, bump seq, release-store head. Consumer uses acquire",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests crash‑recovery, memory ordering, and lock‑free design for production‑like persistent queues.\n\n## Key Concepts\n- Crash consistency, memory‑mapped persistence\n- Lock‑free enqueue/dequeue with two‑phase commit\n- Reclamation (hazard pointers/epochs) and memory fences\n- Backpressure, recovery after crash, NUMA considerations\n\n## Code Example\n```javascript\n// Pseudo enqueue/dequeue framework for crash‑consistent queue\nfunction enqueue(queue, item) {\n  // reserve slot, write, commit tail, flush\n}\nfunction dequeue(queue) {\n  // read, validate, advance head, reclaim if needed\n}\n```\n\n## Follow-up Questions\n- How would you simulate power loss during commit and validate recovery?\n- How would you extend to multiple readers and ensure fairness?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-1",
        "questionId": "q-7240",
        "question": "Design and implement a tiny bitmap-based memory allocator in C for a 1KB arena that serves 8-byte blocks. Provide API: void mem8_init(void* arena, size_t size); void* mem8_alloc(); void mem8_free(void* ptr). Use a bitmap to track free blocks; ensure alignment and avoid double-free; include a minimal test snippet and discuss race conditions in a single-threaded context?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-core arena with 4 KB slab of 64B slots; 64 slots per cache-line; lock-free free-list per core using atomic head with ABA counter. Allocate: fetch and CAS on head, grab slot, advance head; Dealloca",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a 1KB bitmap-based allocator for 8-byte blocks. Arena: 16-byte bitmap header, then data; up to 128 blocks. Use two uint64_t words for the bitmap. mem8_init clears the bitmap; mem8_alloc sear",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "A lock-free, bounded task queue with work-stealing for a 2-socket x86-64 server. Each worker maintains a private deque with local push and shared steal endpoints. Producers push to their local queue; if full, they fallb…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a small harness: two threads increment their own 64-bit counters for 10M iterations. Provide two layouts: Unpadded with a single 64-bit field and Padded with an extra 64-byte pad. Ti",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests fundamental low-level memory management, bit-twiddling, and safety in a confined arena.\n\n## Key Concepts\n- Bitmap-based allocators\n- Alignment and bounds checks\n- Safety against double-free\n\n## Code Example\n```c\n#include <stdint.h>\n#include <stddef.h>\n\ntypedef struct { uint64_t m0, m1; unsigned char* data; size_t blocks; } mem8_pool;\n\nvoid mem8_init(mem8_pool* p, void* arena, size_t size){ p->data=(unsigned char*)arena+16; p->blocks=(size-16)/8; p->m0=0; p->m1=0; }\n\nvoid* mem8_alloc(mem8_pool* p){ for(int i=0;i<128;i++){ int word = i/64; int bit = i%64; uint64_t mask = (1ULL<<bit); uint64_t* w = (word==0)? &p->m0 : &p->m1; if(!(*w & mask)){ *w |= mask; return p->data + i*8; } } return NULL; }\n\nvoid mem8_free(mem8_pool* p, void* ptr){ size_t idx = ((unsigned char*)ptr - p->data)/8; if(idx< p->blocks){ int word = idx/64; int bit = idx%64; uint64_t mask = (1ULL<<bit); uint64_t* w = (word==0)? &p->m0 : &p->m1; *w &= ~mask; } }\n```\n\n## Follow-up Questions\n- How to extend to dynamic arena growth?\n- How to add thread-safety or locking strategies?\n- Compare space efficiency and fragmentation to a free-list allocator.",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-2",
        "questionId": "q-29688",
        "question": "Design a NUMA-aware per-core memory pool allocator for a high-throughput NIC path. Provide API allocate(size) and free(ptr), ensure 64-byte block alignment, per-core caches, and lock-free cross-core freelists; include a strategy to rebalance arenas under contention and to reclaim remotely freed blocks. Give minimal pseudo-code for allocate and free and outline a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a sense-reversing barrier. Maintain an atomic int count and a generation bit sense. Each thread stores a local copy of sense. In wait(), atomically increment count; if count == N, reset count to 0",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-core arenas isolate allocations to minimize cross-NUMA memory traffic. Each core maintains a private 64-byte block freelist; when empty, it atomically fetches a batch from a global pool using CAS, then refills its l…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Two atomics: next_ticket and now_serving. Each thread gets t = fetch_add(&next_ticket, 1) and spins until now_serving == t. Unlock sets now_serving = t+1 with release semantics and a thread-local tick",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed answer: two rings (requests/responses) in DMA-safe mem, per-slot gen counters to avoid ABA, and fetch_add/CAS for head/tail with seq_cst fences. NIC updates status; host polls a completion ri",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests NUMA locality awareness, per-core caches, lock-free freelists, and safe cross-core reclamation in a realistic high-performance allocator.\n\n## Key Concepts\n\n- NUMA topology and memory locality optimization\n- Per-core slab caches for minimal synchronization overhead\n- Lock-free freelists using CAS operations\n- Epoch-based reclamation for safe memory reuse\n- Slab design and fragmentation management\n- Thread/core pinning strategies for consistent affinity\n\n## Code Example\n\n```javascript\nclass Arena {\n  constructor() {\n    this.head = null;\n  }\n}\n\nfunction allocate(size) {\n  // Atomically pop from per-core freelist\n}\n\nfunction free(ptr) {\n  // Enqueue to owning core's arena\n}\n```\n\n## Microbenchmark Plan\n\n1. **Baseline metrics**: Measure raw allocation latency per thread with no contention\n2. **NUMA scalability test**: Vary thread count across sockets, measure remote vs. local access latency\n3. **Rebalancing test**: Inject high contention, measure throughput recovery after rebalance\n4. **Remote reclamation test**: Free from non-owning cores, measure reclaim latency and correctness\n5. **Cache line bouncing**: Instrument false sharing to detect cross-core cache line migration",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-3",
        "questionId": "q-14216",
        "question": "Design and implement a NUMA-aware, per-core slab allocator for fixed-size objects in user space. Each core keeps a local free-list aligned to a cache line; allocate from the local list; on depletion, steal a batch (e.g., 128 objects) from a shared pool using lock-free CAS or fetch_and_add. Include APIs alloc()/free(), alignment guarantees, and discuss fragmentation, cross-socket latency, and benchmarking strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Answer sketch: implement a 256-slot software timer wheel in C. Each slot contains an atomic lock-free list of Timer nodes. Compute target_slot = (delay_ms / granularity) & 0xFF and push the timer onto",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-thread cache allocator with NUMA awareness for hot-path allocations on a 2-socket server. Use 4KB slabs (aligned to page size) with small size classes (64/128/256/512 bytes) to keep allocations thread-local…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a 32-bit atomic flag. In lock(), spin with atomic_flag_test_and_set_explicit(&lock, memory_order_acquire) until it succeeds; on miss, perform exponential backoff with _mm_pause() (cap at 1024). un",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-core slab allocator: each core maintains a cache-line-aligned freelist for fixed-size objects. Alloc uses local list; if empty, steal a batch (128 objs) from a shared pool using a lock-free CAS/fe",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of NUMA locality, cache-line effects, and lock-free cross-core pooling. Candidates must design per-core data structures, safe cross-core stealing, and benchmarking plans.\n\n## Key Concepts\n\n- NUMA-aware memory locality\n- Cache-line padding to avoid false sharing\n- Lock-free freelists and batch stealing\n- Cross-socket latency and fragmentation analysis\n\n## Code Example\n\n```c\ntypedef struct Obj {\n  struct Obj *next;\n} Obj;\ntypedef struct Slab {\n  Obj *free;\n  // cache-line padding to avoid false sharing\n  char pad[64 - sizeof(void*)];\n} Slab;\n```\n\n## Follow-up Questions\n\n- How would you tune batch size for different workloads?\n- How would you extend to variable object sizes and minimize fragmentation?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-4",
        "questionId": "q-17451",
        "question": "Implement a cache-friendly memmove-like routine in C that safely handles arbitrary overlapping src/dst ranges for N bytes. Use AVX2 to copy in 32/64-byte chunks, apply non-temporal stores for large blocks to avoid cache pollution, and choose copy direction based on overlap (src < dst => backward). Provide prototypes, a simple overlap-safe path, and a microbenchmark plan comparing to libc memmove on a NUMA system?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Provide a 1<<16, 128-byte per-slot bounded MPMC ring. Slot header: 16B seq/len plus 112B payload. Producers publish with store-release, then advance via CAS; consumers use load-acquire, process, and r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Data Layout The queue uses a ring buffer structure with N slots, where each slot contains: - **Data payload**: A fixed-size region (or pointer to variable-sized data) for zero-copy access - **Sequence counter**: A 64…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a fixed-size ring with per-slot sequence counters and 64-byte frames. Enqueue reserves via CAS on tail, writes frame, bumps seq, and uses a release store for tail. Dequeue reads head, validates seq, copies frame, an…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Detect overlap and copy direction (src < dst ? backward : forward). Use AVX2 to copy 32/64-byte chunks, aligning when possible. For blocks above 256B, employ non-temporal stores to minimize cache poll",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests overlap handling, SIMD usage, and cache behavior in practical memory movement code on real hardware.\n\n## Key Concepts\n- Overlap-safe copy\n- Directional copy for correctness\n- SIMD data movement (AVX2)\n- Non-temporal stores to reduce cache pollution\n- Alignment and tail handling\n\n## Code Example\n```c\nvoid* memmove_simd(void* dst, const void* src, size_t n){\n  // outline: determine direction, process aligned chunks with AVX2, handle tail\n  return dst;\n}\n```\n\n## Follow-up Questions\n- How would you extend to AVX-512 or adapt non-temporal hints for different CPUs?\n- How would you test correctness with adversarial overlaps and misalignment scenarios?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-5",
        "questionId": "q-5284",
        "question": "Design and implement a tiny software transactional memory (STM) in C11 that supports two concurrent transactions operating on a shared array of 64-bit integers. Provide begin(), read(addr), write(addr, val), commit(), and abort(). Use per-slot versioning to detect conflicts, a read/write set, and simple rollback. Explain how you avoid ABA, ensure serializability, and validate with a minimal two-transaction test under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin with per-slot version counters and per-transaction read/write logs. On read, snapshot value and ver; on write, stash in the log. Commit validates all prior versions unchanged using CAS to advanc",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-thread arenas with size-class bins and per-socket freelists; small allocations served from thread cache; large aligned blocks drawn from a pooled arena with 64B cacheline alignment. Use epoch-base",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a bounded lock-free pool: arena of N blocks (128B each). Free-list head is a 64-bit atomic: upper bits = index, lower bits = version. Each block stores next index. alloc_block uses atomic_co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a bounded, lock-free WSQ with per-thread deques. Push to local tail; pop from local tail; thieves steal from heads. Use versioned pointers or tagged CAS to avoid ABA; apply hazard pointers or",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of memory ordering, non-blocking progress, and correctness of transactional memory in a tight two-thread scenario.\n\n## Key Concepts\n\n- Per-slot version counters to detect conflicts\n- Read/write logs and commit protocol\n- ABA avoidance with version increments and monotonic counters\n- Memory_order semantics and atomic CAS\n\n## Code Example\n\n```c\ntypedef struct { atomic_uint64_t ver; uint64_t val; } slot_t;\ntypedef struct { slot_t *slots; size_t n; log_t read, write; } tx_t;\n\n// begin, read, write, commit skeletons...\n```\n\n## Follow-up Questions\n\n- How would you scale to many concurrent transactions and nested transactions?\n- How would you detect starvation and unfairness?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-6",
        "questionId": "q-18385",
        "question": "Design a fixed-size object memory pool in C for an embedded device with 1024 blocks of 64 bytes. Implement init_pool, allocate_block, and free_block using an in-block free-list (next index). Explain alignment, fragmentation, and how you handle multi-thread safety on a multi-core MCU. Provide concise pseudo-code for the three functions and a basic validation plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement slab_alloc() and slab_free(ptr). The pool has 256 blocks of 64 bytes; the free list uses a 32-bit index stored at the start of each free block. Keep a head index and a used counter. slab_all",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Initialize a Block array where each block stores a next index (free-list pointer). Set free_head to 0 and link all blocks in a chain during init_pool. The allocate_block function atomically pops the head using CAS and r…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a crash‑consistent, multi‑producer/multi‑consumer queue in persistent memory (NVM) using a fixed ring with per‑slot sequence counters. API: pmq_create, pmq_open, pmq_enqueue, pmq_dequeue, pm",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a fixed-capacity MPMC ring with 128-byte slots, cache-line aligned, and per-slot 64-bit version counters. Enqueue reserves a slot by atomically bumping tail, writes data, updates the version, flus",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests memory management discipline, low-level data structures, and thread-safety in constrained environments. The scenario mirrors embedded or driver code where malloc is banned due to determinism and memory overhead concerns.\n\n## Key Concepts\n- **Fixed-size pool design**: Pre-allocated array eliminates fragmentation and provides O(1) allocation.\n- **In-block free-list**: Each block embeds the next free index, using only 4 bytes overhead per block.\n- **Lock-free allocation**: CAS (Compare-And-Swap) enables thread-safe operations without locks on multi-core MCUs.\n- **Alignment**: Use 8-byte or 16-byte alignment matching the native CAS width to ensure atomicity.\n- **Fragmentation**: Fixed-size pools have zero external fragmentation; internal fragmentation is minimal since all blocks are identical.\n- **Multi-thread safety on multi-core MCU**: Use atomic operations (CAS) rather than critical sections to avoid priority inversion and improve real-time responsiveness. Disable interrupts for brief critical sections if CAS is unavailable.\n\n## Code Example\n```c\ntypedef struct Block { uint32_t next; uint8_t payload[64]; } Block;\nBlock blocks[1024];\natomic_uint head_free;\n\nvoid init_pool() {\n    for (int i = 0; i < 1023; i++) blocks[i].next = i + 1;\n    blocks[1023].next = -1;\n    head_free = 0;\n}\n\nvoid* allocate_block() {\n    uint32_t old_head = head_free;\n    do { \n        if (old_head == -1) return NULL;\n    } while (!CAS(&head_free, old_head, blocks[old_head].next));\n    return blocks[old_head].payload;\n}\n\nvoid free_block(void* ptr) {\n    Block* b = container_of(ptr, Block, payload);\n    uint32_t old_head;\n    do { old_head = head_free; }\n    while (!CAS(&head_free, old_head, b - blocks));\n    b->next = old_head;\n}\n```\n\n## Validation Plan\n1. **Functional tests**: Verify allocate returns valid pointers, free reclaims blocks, and exhausted pool returns NULL.\n2. **Boundary tests**: Verify behavior at 0, 1, and 1024 allocations.\n3. **Thread-safety tests**: Run concurrent allocate/free from multiple threads and check for leaks or corruption.\n4. **Memory corruption tests**: Use MPU/MMU to detect buffer overflows into block metadata.",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-7",
        "questionId": "q-1177",
        "question": "Scenario: a high-throughput edge service must enforce precise timeouts for thousands of connections. Design a lock-free, per-core timer wheel that manages up to 1,000,000 timers with microsecond granularity on a 4-socket server. API: add_timer(id, due_us, cb, ctx), cancel_timer(id), tick(). Requirements: no global locks, handle cancellation safely, cache-friendly layout, and crash-safe recovery. Include pseudo-code for add_timer, cancel_timer, and tick, plus a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a two-tier, per-core timer wheel with microsecond granularity: a local wheel for near-term timers and a shared overflow queue feeding a global wheel. Timers are stored in per-core buckets to minim",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Two-path memmove: if dst <= src or no overlap, copy forward in 64-byte steps; if dst > src with overlap, copy backward (start from end) to avoid clobbering source data. Handle tail bytes after 64-byte",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I’d implement per-core 4KB slabs with 64B blocks, a lock-free freelist using atomic CAS on a per-slab head, and 64B padding to avoid false sharing. Allocation uses the local arena; frees stay local if",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a crash-durable, zero-copy IPC path using a shared ring with per-slot sequence counters and a compact durability log. Enqueue writes 256-byte messages, writes a durability record, then uses a r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design lock-free, NUMA-friendly timeouts with tight latency budgets and predictable memory access patterns. The answer demonstrates practical trade-offs between per-core locality and a minimal global coordination path.\n\n## Key Concepts\n- Lock-free data structures and ABA avoidance\n- Per-core timer buckets and cache-line alignment\n- Two-tier timer wheels for short vs long timeouts\n- Safe cancellation via id-to-timer mapping and sequence counters\n- Crash-safe recovery implications (logs, id replay)\n\n## Code Example\n```cpp\n// skeleton structures (conceptual)\ntypedef struct timer {\n  uint64_t due;        // absolute microsecond timestamp\n  uint64_t id;         // unique identifier\n  void (*cb)(void*);   // callback\n  void *ctx;             // user data\n  struct timer *next;    // linked list within bucket\n} timer_t;\n\ntypedef struct timer_wheel {\n  // per-core buckets, each bucket is a lock-free list\n  bucket_t *buckets; // sized per wheel\n  // mapping from id -> timer for cancellation (lock-free)\n  atomic<uint64_t> active_id; // simplified placeholder\n} timer_wheel;\n\nvoid add_timer(timer_wheel* w, uint64_t due, uint64_t id, void (*cb)(void*), void* ctx);\nvoid cancel_timer(timer_wheel* w, uint64_t id);\nvoid tick(timer_wheel* w);\n```\n\n## Follow-up Questions\n- How would you verify correctness and latency under burst traffic and CPU contention?\n- How do you handle timer overflow, rebalancing across cores, and NUMA effects?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-8",
        "questionId": "q-16449",
        "question": "Implement a lightweight rendezvous for one-time initialization in a 3-thread scenario. Thread A performs init and then exposes a flag; threads B and C must wait until initialization completes before proceeding. Use C11 atomic variables; ensure memory visibility with proper orderings, avoid busy-wait; provide a minimal API and sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Cross-NUMA, two-process ring for 40 Gbps on a dual-socket server. Slot: 128 bytes; capacity 1<<18; 64-byte alignment; per-slot 64-bit sequence for ABA-avoidance. NUMA-aware allocation on local node. ## Slot Layout ```c…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a NUMA-aware event bus with 4 producer rings and 4 consumer rings on a 2-socket system. Each ring holds 128-byte, cache-line-aligned events in a circular buffer. Producers advance tail atomical",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a circular log with atomic head/tail, 128-byte slots, and per-slot sequence counters. Producers CAS-tail to reserve, write, then advance seq. Consumers CAS-head, copy, advance. Use memory_orde",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a global atomic_bool init_done = false. Thread A runs init, then atomic_store(&init_done, true, memory_order_release). Threads B/C wait with a acquire load: while (!atomic_load(&init_done, memory_",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of memory ordering, visibility, and simple rendezvous in a beginner-friendly but realistic context.\n\n## Key Concepts\n- C11 atomics, memory_order_release/acquire\n- Visibility guarantees and simple spin-wait vs condvar\n- Data races and safe initialization sequencing\n\n## Code Example\n```javascript\n#include <stdatomic.h>\n#include <pthread.h>\n\nstatic atomic_bool init_done{false};\n\nvoid init_thread() {\n  // perform init\n  atomic_store(&init_done, true, memory_order_release);\n}\n\nvoid wait_for_init() {\n  while (!atomic_load(&init_done, memory_order_acquire)) {\n    // spin or yield\n  }\n}\n```\n\n## Follow-up Questions\n- How would you generalize to N waiters with a barrier?\n- How would you test correctness under weak memory models and ensure no reordering issues?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-9",
        "questionId": "q-22750",
        "question": "On a 2-socket x86-64 NUMA server, implement a fast, lock-free per-thread slab allocator for short-lived objects with size classes {64,128,256,512,1024}. Each thread maintains a local cache; a central heap handles misses with minimal cross-socket traffic. Use epoch-based reclamation to avoid cross-thread frees and stall. Ensure cache-line alignment and minimize false sharing; provide C API: void* alloc(size_t sz); void free(void* p); plus pseudo-code for alloc/free and a microbenchmark plan to measure cross-socket traffic, fragmentation, and sustained throughput under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use two fixed-size rings, per-slot 64-byte payload and 64-bit sequence counters; producers reserve a slot by atomically incrementing tail, write data, then publish with release; consumer uses acquire to read the sequenc…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a durable, bounded, lock-free queue that resides in persistent memory (PMEM) supporting multiple producers and a single consumer. Use per-slot sequence counters to avoid ABA problems, a fixed capacity N, and cras…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-thread caches per size class, a NUMA-aware central pool, and epoch-based reclamation to avoid cross-thread frees. Slabs align to 64-byte lines; pad to prevent false sharing. Lock-free central pool",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Initialize a Block array where each block stores a next index (free-list pointer). Set free_head to 0 and link all blocks in a chain during init_pool. The allocate_block function atomically pops the head using CAS and r…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical NUMA-aware allocator design, concurrency, and fragmentation trade-offs in real workloads that push memory systems.\n\n## Key Concepts\n- Per-thread caches per size class\n- Lock-free freelists with CAS\n- Central heap with NUMA-awareness\n- Epoch-based reclamation to avoid cross-thread frees\n- Cache-line alignment and padding to prevent false sharing\n\n## Code Example\n```c\ntypedef struct { void* head; } FreeList;\ntypedef struct { void* cache[5]; } ThreadCache;\n```\n\n## Follow-up Questions\n- How would you validate hot-path performance under multi-threaded load?\n- What fragmentation bounds would you expect and how would you mitigate them?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-10",
        "questionId": "q-14164",
        "question": "Design a cross-process, zero-copy shared hashmap in a fixed-size shared memory region (shm) to support concurrent GET/PUT/DEL across multiple processes on a 2-socket x86-64 server. Use open addressing with linear probing, per-slot sequence counters to detect ABA, and epoch-based reclamation for deletes. Provide API prototypes and minimal pseudo-code, discuss memory ordering, cache-line padding, and NUMA awareness; outline a microbenchmark vs a mutex-protected hashmap?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a fixed-size block pool within the 64KB heap, serving 32-byte blocks aligned to 8 bytes. Maintain a singly-linked free-list stored in the first 4 bytes of each free block as the offset to the next",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A practical approach: shared hashmap in shm with fixed capacity; hash to bucket, linear probe; per-slot 64-bit sequence counter to detect ABA; CAS-based updates with strong memory ordering; epoch-base",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Alloc: acquire spinlock; pop head from free-list; return that block. Free: push block back; release lock. Init: chain all 1024 blocks into the free-list. Test: two threads allocate/free in tight loop;",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Allocate first from per-thread cache, then per-NUMA node pool, then global heap. Implement 5 size classes: 8,16,32,64,128. Lock-free free lists per class using atomic head with a tag to avoid ABA; cro",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExamines cross-process synchronization, lock-free design, and crash-safe memory reclamation in a shared memory layout, directly relevant to ultra-low-latency data stores and high-throughput services.\n\n## Key Concepts\n\n- Shared memory IPC and fixed-capacity hashmaps\n- Open addressing with linear probing\n- ABA avoidance via per-slot sequence counters\n- Epoch-based reclamation for deletes\n- Memory ordering, cache-line padding, and NUMA-awareness\n\n## Code Example\n\n```c\n// Prototype definitions\ntypedef struct { uint64_t key; uint64_t value_offset; uint64_t seq; } slot_t;\ntypedef struct { slot_t* slots; size_t capacity; size_t mask; void* shm_base; } shm_hashmap_t;\n\nint shm_hash_put(shm_hashmap_t* h, uint64_t key, const void* data, size_t len);\nint shm_hash_get(shm_hashmap_t* h, uint64_t key, void** data, size_t* len);\nint shm_hash_del(shm_hashmap_t* h, uint64_t key);\n```\n\n## Follow-up Questions\n\n- How would you resize or migrate to a larger shm region without blocking clients?\n- How do you prove correctness under out-of-order memory writes and cross-core updates?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-11",
        "questionId": "q-2688",
        "question": "Design and implement a per-thread arena allocator for 16-byte blocks from a fixed 256KB pool. Provide API: init_arena(void* arena, size_t size), void* alloc16(), void free16(void*). Use a global bitmap to track 16,384 blocks and a thread-local cache of 8 blocks to reduce contention. Explain alignment, fragmentation, and how this approach compares to malloc under multi-core contention. Include a minimal test harness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a sense-reversal barrier with two atomics: `count` and `generation`. Each arriving thread performs an atomic fetch-and-increment on `count`. When the count reaches N, the last thread resets `count` to 0, toggles the…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-core slab allocator: each core maintains a cache-line-aligned freelist for fixed-size objects. Alloc uses local list; if empty, steal a batch (128 objs) from a shared pool using a lock-free CAS/fe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a crash-durable, zero-copy IPC path using a shared ring with per-slot sequence counters and a compact durability log. Enqueue writes 256-byte messages, writes a durability record, then uses a r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "```c #include <stdint.h> #include <string.h> #include <stdatomic.h> #include <pthread.h> #include <stdio.h> #include <time.h> #include <stdlib.h> #define ARENA_SIZE (256 * 1024) #define BLOCK_SIZE 16 #define NUM_BLOCKS…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nDemonstrates practical low-level memory management, fixed-size allocators, thread-local caches, and fragmentation trade-offs. A bitmap-based free-tracking scheme avoids pointer-chasing and supports fast scans on aligned blocks.\n\n## Key Concepts\n- Fixed-size block allocator with 16-byte alignment guarantees\n- Bitmap-based free tracking using bit manipulation for O(1) allocation\n- Thread-local caches to reduce contention under multi-core workloads\n- Fragmentation analysis: zero external fragmentation, bounded internal fragmentation\n- Performance comparison with system malloc under contention\n\n## Implementation Highlights\n- **Bitmap operations**: Uses `__builtin_ctzll` for O(1) first-free-bit finding\n- **Thread cache**: 8-block LIFO cache reduces mutex acquisitions significantly\n- **Safety checks**: Validates pointers in free() to prevent corruption\n- **Benchmark**: Includes performance comparison showing speedup under various loads\n\n## Follow-up Questions\n- How would you handle arena resizing or varying block sizes?\n- How would you ensure NUMA-awareness and fault containment?\n- What strategies would you use for garbage collection when thread caches become full?\n- How could this design be extended for larger allocations or mixed-size workloads?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-12",
        "questionId": "q-30495",
        "question": "On a 2-socket x86-64 server, design a lock-free, multi-producer multi-consumer shared-ring queue that supports two priority classes (high and normal). Implement two fixed rings sharing a 1<<18-slot slab, with per-slot sequence counters to avoid ABA. Provide slot layout, enqueue/dequeue pseudo-code, memory fences discussion, backpressure handling, and a microbenchmark plan under mixed priorities?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Envision per-thread ring with capacity N; threads CAS‑take a slot to enqueue; flush_to_global batches slots into a global sink via an atomic swap of a filled block. Use per-slot sequence counters to a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a NUMA-aware bounded scheduler with per-thread local queues and a shared steal pool. Local rings: 1<<14 slots, 128-byte entries and a 64-bit sequence counter to prevent ABA. Enqueue/dequeue an",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two fixed rings for high and normal priority share a preallocated slab of N=1<<18 slots. Each slot holds seq, pri, payload. Enqueue high/normal CAS onto their ring tails; Dequeue preferentially sample",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-thread caches per size class, a NUMA-aware central pool, and epoch-based reclamation to avoid cross-thread frees. Slabs align to 64-byte lines; pad to prevent false sharing. Lock-free central pool",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a low-latency, lock-free queue with priority handling, addressing ABA, memory reclamation, and cache effects in a NUMA setting.\n\n## Key Concepts\n\n- Lock-free multi-producer multi-consumer queues\n- Priority-aware two-ring design\n- ABA avoidance with per-slot sequence counters\n- Epoch-based reclamation for memory safety\n- Cache-line alignment and false sharing considerations\n- Backpressure and starvation avoidance\n- Microbenchmark design for mixed-priority workloads\n\n## Code Example\n\n```cpp\nstruct Slot {\n  uint64_t seq;\n  uint32_t pri; // 0=normal, 1=high\n  uint8_t payload[64];\n};\n```\n\n## Follow-up Questions\n\n- How would you extend to more than two priorities?\n- How does this design handle sudden producer/consumer bursts and eviction of stalled threads?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-13",
        "questionId": "q-6272",
        "question": "Design a durable, crash-safe, lock-free queue for a 2-socket server using non-volatile memory (PMEM) that guarantees correct enqueue/dequeue across power failures. Provide a bounded, multi-producer, single-consumer ring with wrap-around, per-entry header (len, seq, committed), and a recovery procedure after crash. Include pseudo-code for enqueue/dequeue, a test plan, and a micro-benchmark?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a circular log in PMEM with per-entry header {len, seq, committed}. Enqueue writes payload, then header; flush order with clwb/siws and mfence; consumer validates header and reads payload un",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "## Data Layout The queue uses a ring buffer structure with N slots, where each slot contains: - **Data payload**: A fixed-size region (or pointer to variable-sized data) for zero-copy access - **Sequence counter**: A 64…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a tiny fixed-size block allocator (128B) for a high-throughput logging path. Each thread has a private freelist; when empty, fetch a slab from a global pool. Use a lock-free freelist with atomic operations to avo…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a two-counter ticket lock: next_ticket and now_serving as atomic integers. Lock: my = fetch_add(&next_ticket, 1); while (atomic_load_explicit(&now_serving, memory_order_acquire) != my); Unlock: at",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes crash-safe, low-latency data paths in persistent memory architectures.\n\n## Key Concepts\n- Non-volatile memory ordering (clwb/flush, sfence).\n- Crash-consistent ring buffers and per-entry headers.\n- Epoch-based reclamation and GC for reuse.\n- Testing crash-recovery and power-failure scenarios.\n\n## Code Example\n```c\ntypedef struct { uint32_t len; uint64_t seq; uint8_t committed; } entry_hdr_t;\n```\n\n## Follow-up Questions\n- How would you benchmark recovery latency and correctness after abrupt power loss?\n- What would you change for multi-consumer support?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-14",
        "questionId": "q-20793",
        "question": "Design a tiny fixed-size memory pool allocator for an embedded system with a 4 KB buffer. Implement pool_alloc(size_t size) and pool_free(void* ptr) using an in-block free-list (each free block stores a pointer to the next free block). Enforce 8-byte alignment, a minimum 8-byte block, and no external allocators. Provide initialization and allocation pseudo-code in C, and discuss testing for fragmentation and worst-case failure on constrained hardware?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Lockless Cross-Thread Config Exchange Design ### Data Structures ```c #define BUFFER_SIZE 64 typedef struct { uint8_t data[BUFFER_SIZE]; uint32_t crc; } ConfigBuffer; typedef struct { ConfigBuffer buffers[2]; _Atomic…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Provide a tiny fixed-block pool: 4KB, block size 32B, alignment 8. Each free block's header stores next pointer. Init builds the free list by linking all blocks. pool_alloc(size) rounds up to 32, pops",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Key approach: per-slot fields: seq, data. Prods reserve a slot via CAS on tail, write data, flush data cache line, then publish by updating seq and tail with a durable fence. Consumer reads by parity-",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-thread cache of 32 slots; on alloc, pop from local cache; if empty, atomically refill 32 items from a global lock-free free-list using CAS. On free, push to local cache; if local is full, flush the batch to the glob…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests low-level memory management, determinism, and fragmentation handling in tight constraints.\n\n## Key Concepts\n\n- Fixed-size pool vs variable allocations\n- In-block free-list approach\n- Alignment and fragmentation\n- Initialization, allocation, freeing, and testing\n\n## Code Example\n\n```c\n/* Initialization and simple allocator skeleton */\ntypedef struct Block { struct Block* next; } Block;\n\nstatic uint8_t pool[4096];\nstatic Block* free_list;\n\nvoid pool_init(void){\n  free_list = (Block*)pool;\n  Block* cur = free_list;\n  for (size_t i=0; i< (4096/32)-1; ++i){\n    cur->next = (Block*)((uint8_t*)cur + 32);\n    cur = cur->next;\n  }\n  cur->next = NULL;\n}\nvoid* pool_alloc(size_t s){\n  if (s>32) return NULL;\n  if (!free_list) return NULL;\n  Block* b = free_list;\n  free_list = b->next;\n  return (void*)b;\n}\nvoid pool_free(void* p){\n  ((Block*)p)->next = free_list;\n  free_list = (Block*)p;\n}\n```\n\n## Follow-up Questions\n\n- How would you add support for variable-sized allocations with splitting and coalescing?\n- How would you verify correctness with a tiny unit test harness on a microcontroller?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-15",
        "questionId": "q-26919",
        "question": "Design a lock-free, bounded MPMC queue implemented as a singly linked list with hazard-pointer-based memory reclamation. Capacity N = 1<<16; provide node/queue structs and enqueue/dequeue pseudo-code using CAS with seq-counters. Explain how hazard pointers guard dequeued nodes, retire-list lifecycle, and how ABA/false sharing are mitigated under heavy contention. Include a microbenchmark plan versus a classic lock-free queue?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a 64-slot ring with per-slot lock-free stacks. Enqueue computes slot = (current_tick + delay_ms) & 63, pushes a timer node with due_tick and callback. Tick advances every ms, drains current sl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a lock-free, bounded MPMC queue using a linked list with hazard pointers for safe reclamation. Include node/queue structs, CAS-based enqueue/dequeue with seq counters, hazard-pointer life cycle",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Initialize a Pool struct with a 64-byte block size, a head pointer, a mutex, and a preallocated buffer. Init links all blocks into a free-list. AllocBlock locks, pops head, unlocks, returns block; Fre",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: Implement a fixed-size ring buffer in persistent memory with per-slot metadata. Each slot contains a 64-byte payload, an 8-byte sequence counter, and an 8-byte ready flag. The writer writes the payload, advanc…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests expertise in safe memory reclamation (hazard pointers) within a lock-free, high-contention queue, a common real-world primitive in network servers and latency-sensitive services. It also probes understanding of ABA, retirement, and cache locality under load.\n\n## Key Concepts\n- Hazard pointers and retire lists for safe reclamation\n- Lock-free CAS-based updates and memory ordering\n- ABA avoidance and false sharing considerations\n- Throughput under contention and reclamation overhead\n\n## Code Example\n```c\n// Pseudo: node struct, queue with atomic head/tail, enqueue/dequeue with CAS\n```\n\n## Follow-up Questions\n- How would you adapt this for NUMA-aware memory placement?\n- How does hazard-pointer-based reclamation compare to epoch-based schemes in this workload?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-16",
        "questionId": "q-11129",
        "question": "Implement a durable, lock-free PMEM queue in C: a circular ring of N slots in persistent memory. Each slot stores a 16-byte payload and a 64-bit sequence. Writers write payload, then publish a new sequence and persist (pmem_persist, sfence). Readers check sequence, dequeue when matches, advance tail and persist. Include recovery steps and a minimal API?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-thread fixed-size object pool in C for 64-byte objects. Each thread maintains a local free-list; when empty, refills from a global pool via a lock-free stack or a light spin-lock. APIs",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I’d implement thread-local freelists with 64-byte blocks, aligned to 64 bytes. Each thread has a private pool; on empty, refill from a shared global pool via a lock-free CAS on the head. Allocation po",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a circular PMEM ring with N slots; writer writes payload, then updates and persists seq to publish the item; reader polls slots, verifies seq, copies payload, then advances a persistent tail and p",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Two-generation barrier: each thread increments an atomic arrivals counter and writes its slot, then spins on its per-thread flag for the current generation. When the last thread arrives, flip the gene",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests crash-consistency with persistent memory, memory ordering, and minimal synchronization in a high-throughput queue. Emphasizes per-slot sequencing, proper flush order, and recovery logic after power loss.\n\n## Key Concepts\n- Persistent memory semantics and crash-consistency\n- Per-slot sequence counters to avoid ABA\n- Flush/fence strategies (pmem_persist, sfence, clflushopt)\n- Recovery protocol after crash or reset\n\n## Code Example\n```c\ntypedef struct {\n  uint8_t payload[16];\n  uint64_t seq;\n} slot_t;\n\ntypedef struct {\n  slot_t *slots;\n  uint64_t head; // producer position (mod N)\n  uint64_t tail; // consumer position (mod N)\n  uint64_t mask; // N-1\n} pmem_queue_t;\n\nint enq(pmem_queue_t *q, const void *data);\nint deq(pmem_queue_t *q, void *out);\n```\n\n## Follow-up Questions\n- How would you extend to multi-producer/multi-consumer while preserving crash-consistency?\n- How would you adapt the design for different PMEM persistence APIs and cache-line sizes?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-17",
        "questionId": "q-16074",
        "question": "Design and implement a lock-free, per-thread allocator for small objects (64-128 bytes) in C/C++. Each thread maintains a private arena (256 slots) and refills from a shared central cache when needed. Describe the data structures (arena/slab, per-thread freelists), how to avoid ABA with tagged CAS, how cross-thread frees are handled, alignment to 64 bytes, and necessary memory fences. Provide minimal pseudo-code for allocate/free and a microbenchmark plan vs malloc in a multi-threaded workload?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Crash-consistent NVM ring: use a redo log and per-slot sequence counters. Enqueue: write payload+header, flush header (clwb/flushopt), then advance tail with a release store; Dequeue: fetch, advance h",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-thread arenas with a private 256-slot cache and a global central cache. Implement a lock-free freelist using CAS with a tag (ABA prevention). Allocate from thread cache; refill from central ca",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a lock-free, bounded MPMC queue using a linked list with hazard pointers for safe reclamation. Include node/queue structs, CAS-based enqueue/dequeue with seq counters, hazard-pointer life cycle",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a 4-way set-associative software TLB for a CPU emulator on a 2-socket server: N=1<<16 entries, 64B per entry (PFN, ASID, flags). Lock-free lookup: set = hash(VPN, ASID) mod Nset; probe 4 ways;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests low-level memory management, lock-free data structures, and cache-conscious design in high-performance systems.\n\n## Key Concepts\n- Thread-local caching minimizes contention\n- Central cache enables cross-thread reuse\n- ABA avoidance with tagged CAS\n- Cache-line alignment/padding to prevent false sharing\n- Memory ordering (acquire/release, fences)\n- Safe cross-thread deallocation paths\n\n## Code Example\n```javascript\n// Pseudo-code: allocate\nfunction alloc(): Object {\n  if (threadLocal.freeList != null) return threadLocal.freeList.pop()\n  refillFromCentral()\n  return threadLocal.freeList.pop()\n}\n\nfunction free(obj): void {\n  if (belongsToCurrentThread(obj)) threadLocal.freeList.push(obj)\n  else centralCache.push(obj)\n}\n```\n\n## Follow-up Questions\n- How would you profile fragmentation vs throughput?\n- How would you adapt for varying object sizes or NUMA awareness?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-18",
        "questionId": "q-5524",
        "question": "Design a lock-free, NUMA-aware concurrent hash map in C11 that supports inserts and lookups with bounded rehashing. Use epoch-based reclamation to retire nodes and avoid ABA. Describe per-bucket structure, CAS-based insert, and a safe resize strategy. Provide pseudo-code for insert and lookup, and a microbenchmark plan focusing on locality and contention across NUMA nodes. How would you validate correctness and performance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a fixed-capacity MPMC ring with 128-byte slots, cache-line aligned, and per-slot 64-bit version counters. Enqueue reserves a slot by atomically bumping tail, writes data, updates the version, flus",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a crash‑consistent, lock‑free bounded ring in shared memory backed by a mmapped file. Use per‑slot CRCs and a two‑phase commit so writers can resume after power loss; dequeue under reader prog",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-bucket head pointers with atomic CAS for inserts; each node holds key, value, next; pad nodes to cache-lines. Use epoch-based reclamation to retire removed nodes to prevent ABA. Resize by doub",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "To stream descriptors efficiently, implement per-NUMA freelists with 64-byte alignment, batch reclamation, and a tiny allocator header per socket. Pin arenas to sockets, avoid cross-socket frees, and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nConcrete, end-to-end lock-free structure tailored for NUMA-aware workloads.\n\n## Key Concepts\n- Lock-free data structures\n- Epoch-based reclamation (EBR)\n- NUMA locality and cache-line padding\n- Safe dynamic resizing of hash tables\n\n## Code Example\n```c\n// Pseudocode for insert and lookup with CAS and EBR\n```\n\n## Follow-up Questions\n- How would you verify correctness under heavy contention?\n- What failure scenarios would you test (power loss, crashes) and how ensure crash resilience?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-19",
        "questionId": "q-28632",
        "question": "Implement a scalable, lock-free barrier for N threads on a 2-socket NUMA system using sense-reversal. Each thread signals arrival and waits for all to arrive, then proceeds with the release phase. Provide a concrete data layout and minimal pseudo-code for barrier_enter and barrier_wait; discuss memory ordering, cache-line alignment, false sharing, and propose a microbenchmark to measure latency and scalability as N grows from 8 to 1024?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement two variants using pthreads. Variant A uses a shared counters array where thread 0 updates counters[0] and thread 1 updates counters[1] in a tight loop, causing false sharing. Variant B pads each counter to 64…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-generation barrier: each thread increments an atomic arrivals counter and writes its slot, then spins on its per-thread flag for the current generation. When the last thread arrives, flip the gene",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a sense-reversal barrier with 64-byte-aligned per-thread slots and a shared generation counter. **barrier_enter:** each thread writes its sense bit to its slot and performs an atomic fetch_add on the arrivals counte…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a bounded lock-free pool: arena of N blocks (128B each). Free-list head is a 64-bit atomic: upper bits = index, lower bits = version. Each block stores next index. alloc_block uses atomic_co",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical low-level synchronization skills, including non-blocking algorithm design, explicit memory ordering, cache-line padding strategies, and NUMA-aware thread placement.\n\n## Key Concepts\n- **Sense-reversal barrier:** Uses a per-thread local sense flag paired with a global generation counter to avoid thundering-herd on wake-up.\n- **64-byte alignment:** Prevents false sharing by ensuring each thread's slot occupies its own cache line.\n- **Atomic semantics and memory orders:** Acquire/release semantics ensure proper visibility ordering without unnecessary fence overhead.",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-20",
        "questionId": "q-25186",
        "question": "Design a low-latency, lock-free inter-actor ring for a PCIe DMA engine on a 2-socket x86_64 server. The system has N producers and M consumers sharing a fixed-size, cache-line-aligned ring in unified memory. Use per-slot 64-bit sequence counters to avoid ABA, support wrap-around, and ensure progress under high contention. Provide slot layout, enqueue/dequeue pseudo-code with memory fences, discuss backpressure and crash-recovery in power-failure scenarios, and outline a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan to implement per-slot sequence counters, cache-line aligned slots, and CAS-based enqueues/dequeues with acquire/release semantics. Use a 128-byte descriptor, precompute masks for wrap-around, and",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-core logs minimize contention; mutations append to local segments, then a durable epoch fence persists metadata. Recovery replays per-core segments to rebuild the index; Bloom filters speed lookup",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a bounded MPMC queue on a power-of-two ring, per-slot length field and a sequence number to avoid ABA. Pad indices to cache lines; publish slot content with release stores and consume with acquire",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-thread caches organized by size class (8, 16, 32, and 64 bytes), each cache line-aligned to prevent false sharing. The allocation path first checks the thread-local cache; on a miss, refill from a central…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests mastery of lock-free design with real hardware constraints (PCIe DMA, NUMA) and recovery.\n\n## Key Concepts\n- Lock-free queues with ABA avoidance\n- Per-slot sequencing, memory ordering, cache-line optimization\n- Crash-recovery semantics for non-volatile memory\n\n## Code Example\n```c\n// Enqueue pseudo\nbool enqueue(Ring* r, Descriptor* d) {\n  // CAS-based path with per-slot sequence counters\n  // acquire memory ordering, publish descriptor, advance tail\n  return true;\n}\n```\n\n## Follow-up Questions\n- How would you dimension N, M for burst traffic?\n- How would you validate crash-recovery correctness under power loss?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-21",
        "questionId": "q-4008",
        "question": "Design a per-core, bounded publish-subscribe queue for a NUMA multi-socket server. Producers write to local rings and publish to a shared log with per-slot sequence numbers. Provide data structures, enqueue/dequeue pseudo-code, handle wrap-around, memory ordering on x86-64, and validation plan for throughput and message ordering under cross-core contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-core, bounded publish–subscribe queue: each producer writes to a local ring buffer and publishes to a shared log with per-slot sequence numbers. Use per-slot 64-byte, cache-aligned slots",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a ring with per-slot versioned headers and a pool of fixed-size 256B buffers. Enqueue reserves a slot with CAS on head, copies into a free buffer, writes size and buffer pointer, then publishes wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lock-free free-list allocator: keep a head pointer to a singly linked list of free blocks stored in the pool; allocate by CAS-swapping head with head->next, freeing by pushing the block on",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-bucket head pointers with atomic CAS for inserts; each node holds key, value, next; pad nodes to cache-lines. Use epoch-based reclamation to retire removed nodes to prevent ABA. Resize by doub",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of low-latency IPC, NUMA locality, and cache-coherence with careful memory ordering. It also probes design trade-offs for per-core buffering vs a centralized log.\n\n## Key Concepts\n- Per-core buffering and cross-core publication\n- Sequence-numbered slots and wrap-around handling\n- Memory ordering on x86-64 (relaxed/upholds with fences)\n- False sharing avoidance and cacheline padding\n- Validation: throughput, ordering, and backpressure under contention\n\n## Code Example\n```c\n// Pseudo-enqueue\nbool enqueue(int core_id, void* msg) {\n  uint64_t pos = atomic_fetch_add(&tail[core_id], 1);\n  slot_t* s = &ring[core_id][pos & (N-1)];\n  if (s->seq != pos) return false; // consumer hasn't advanced\n  s->payload = msg;\n  s->seq = pos + 1;\n  return true;\n}\n```\n\n## Follow-up Questions\n- How would you extend this to support dynamic growth of the log while preserving wait-freedom?\n- How would you test correctness with synthetic workloads that stress cross-core contention?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-22",
        "questionId": "q-26214",
        "question": "In a 2-socket NUMA server, implement a NUMA-aware slab allocator for fixed-size objects (e.g., 64B). Use per-thread freelists with a global pool, ensure thread safety, minimize cross-socket traffic, and provide a minimal API (alloc/free). Discuss data structures, alignment, false sharing, memory barriers, and a microbenchmark plan to measure throughput and fragmentation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "NUMA-aware slab allocator with per-thread caches and a small global pool. Each thread maintains a 4KB slab of 64B objects (64 slots) aligned to 64B. Freelist entries are 64B with a CAS-based push/pop",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a fixed-size ring with slots each containing a 64-bit payload and a 64-bit sequence, padded to avoid false sharing. Enqueue: read head, compute next=head+1; if slot[next].seq==head, write pa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a sequence-lock guarded read path for a 128-bit config updated by a single writer. Use a 64-bit version counter V. Writer: V++ (odd), write cfg, V++ (even). Reader: loop: s = V; if (s&1) ret",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A lock-free fixed-size pool using a single ring: 128-byte cells; per-slot 64-bit sequence counters guard ABA; producers reserve slots with atomic fetch_add, write data, then publish with release; cons",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests NUMA locality, per-thread caches, and lock-free freelists; checks understanding of false sharing and ABA avoidance.\n\n## Key Concepts\n- NUMA locality\n- Per-thread caches\n- Lock-free freelists\n- ABA avoidance with version counters\n- Cache-line padding and fragmentation\n\n## Code Example\n```c\ntypedef struct Node { struct Node *next; unsigned long tag; } Node;\ntypedef struct Slab { void *base; _Atomic size_t free; Node *freelist; } Slab;\n```\n\n## Follow-up Questions\n- How would you validate scalability beyond 2 sockets?\n- How would you adapt for variable object sizes?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-23",
        "questionId": "q-15816",
        "question": "Implement a fair ticket spinlock in C11. Use two atomic integers: next_ticket and serve. Provide lock() and unlock() with proper memory ordering. Then create a small test with 3 threads, each performing 1000 entries into a critical section guarded by the lock and printing their thread id and a per-thread counter in CS to demonstrate FIFO fairness. Explain padding to avoid false sharing and any potential starvation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a ticket lock: two atomics, next and serve. lock(): my = atomic_fetch_add(&next,1); while(atomic_load(&serve) != my); unlock(): atomic_fetch_add(&serve,1); pad to 64B to avoid false sharing. Test:",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "A lock-free, bounded task queue with work-stealing for a 2-socket x86-64 server. Each worker maintains a private deque with local push and shared steal endpoints. Producers push to their local queue; if full, they fallb…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a circular ring in NVDIMM with per-slot: seq (64-bit), payload, and a small metadata region. Writers CAS-tail to reserve, write payload, then publish by bumping seq. Readers CAS-head, verify seq,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Provide a 1<<16, 128-byte per-slot bounded MPMC ring. Slot header: 16B seq/len plus 112B payload. Producers publish with store-release, then advance via CAS; consumers use load-acquire, process, and r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of atomic operations, memory ordering, and cache behavior. A ticket lock is simple yet reveals reasoning about spin-waiting and fairness.\n\n## Key Concepts\n\n- Atomic operations and memory_order semantics\n- FIFO fairness and starvation risk\n- Cache-line padding to avoid false sharing\n- Busy-wait vs back-off strategies\n\n## Code Example\n\n```c\n#include <stdatomic.h>\ntypedef struct {\n  _Atomic_uint next;\n  _Atomic_uint serve;\n} ticket_lock_t;\n\nvoid lock(ticket_lock_t *t){\n  unsigned my = atomic_fetch_add(&t->next, 1);\n  while(atomic_load_explicit(&t->serve, memory_order_acquire) != my) {\n    // optional pause\n  }\n}\n\nvoid unlock(ticket_lock_t *t){\n  atomic_fetch_add(&t->serve, 1);\n}\n```\n\n## Follow-up Questions\n\n- How would you modify to reduce tail latency under high contention?\n- How would you adapt for 64-bit counters or platforms with weak memory models?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-24",
        "questionId": "q-8072",
        "question": "In a latency-sensitive service on a 2-socket server, design a NUMA-aware allocator for 64-byte small objects. Use per-node free lists, ABA-free cross-node steals, and a global refill policy. Provide API prototypes and pseudo-code for the fast path, and outline a microbenchmark plan to compare throughput and fragmentation against malloc under high contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A robust solution uses per-thread arenas, fixed-size pools (8–256 bytes), lock-free freelists with ABA-safe CAS, and epoch-based cross-thread reclamation. Allocate from local arena; defer frees by epo",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-node pools of 64-byte blocks with cache-line alignment; lock-free free lists using atomic pointers and pointer tagging to prevent ABA; local fast path, cross-node steal queues, and batch refills t",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose per-thread deques with a bottom push/pop and a shared top for steals. Local path uses a fixed-size, cache-aligned ring; steals CAS on top with a version tag to prevent ABA; pad structures to p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a fixed-size ring with atomic head and tail indices. Enqueue: spin until (tail-head) < cap; write item to buf[tail%cap]; tail = tail+1 with memory_order_release. Dequeue: spin until (tail-he",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical NUMA-aware memory management, per-node caching, and ABA-safe freelists under high concurrency.\n\n## Key Concepts\n\n- NUMA affinity and cache locality\n- Lock-free free lists and ABA avoidance\n- Cross-node stealing and refill policy\n- Cache-line padding and fragmentation\n\n## Code Example\n\n```c\ntypedef struct Node { void* next; } Node;\nvoid* sa_alloc(void);\nvoid sa_free(void*);\n```\n\n## Follow-up Questions\n\n- How scale to 4+ sockets?\n- How would you measure long-term fragmentation and allocator throughput?",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-05T10:42:16.554Z",
    "version": 1
  }
]