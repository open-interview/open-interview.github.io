[
  {
    "id": "test-low-level",
    "channelId": "low-level",
    "channelName": "LOW Level",
    "title": "LOW Level Knowledge Test",
    "description": "Test your understanding of LOW Level with 25 questions.",
    "questions": [
      {
        "id": "low-level-q-0",
        "questionId": "q-17451",
        "question": "Implement a cache-friendly memmove-like routine in C that safely handles arbitrary overlapping src/dst ranges for N bytes. Use AVX2 to copy in 32/64-byte chunks, apply non-temporal stores for large blocks to avoid cache pollution, and choose copy direction based on overlap (src < dst => backward). Provide prototypes, a simple overlap-safe path, and a microbenchmark plan comparing to libc memmove on a NUMA system?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a bounded MPSC queue using a fixed-size circular buffer of 1<<12 64-bit slots. Use an atomic tail for producers to reserve a slot with fetch_add; store the value and publish with memory_order_r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-thread fixed-size object pool in C for 64-byte objects. Each thread maintains a local free-list; when empty, refills from a global pool via a lock-free stack or a light spin-lock. APIs",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement two variants using pthreads. Variant A uses a shared counters array where thread 0 updates counters[0] and thread 1 updates counters[1] in a tight loop, causing false sharing. Variant B pads each counter to 64…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Detect overlap and copy direction (src < dst ? backward : forward). Use AVX2 to copy 32/64-byte chunks, aligning when possible. For blocks above 256B, employ non-temporal stores to minimize cache poll",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests overlap handling, SIMD usage, and cache behavior in practical memory movement code on real hardware.\n\n## Key Concepts\n- Overlap-safe copy\n- Directional copy for correctness\n- SIMD data movement (AVX2)\n- Non-temporal stores to reduce cache pollution\n- Alignment and tail handling\n\n## Code Example\n```c\nvoid* memmove_simd(void* dst, const void* src, size_t n){\n  // outline: determine direction, process aligned chunks with AVX2, handle tail\n  return dst;\n}\n```\n\n## Follow-up Questions\n- How would you extend to AVX-512 or adapt non-temporal hints for different CPUs?\n- How would you test correctness with adversarial overlaps and misalignment scenarios?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-1",
        "questionId": "q-8104",
        "question": "Design a minimal 1-producer-1-consumer mailbox using a single shared 64-bit slot and a 1-bit ready flag. Provide init, send(value), and recv() in C11-style pseudocode. Explain how to encode a sequence to avoid ABA, how memory order ensures visibility, and outline a microbenchmark on a 4-core system to validate latency and throughput?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Durable, lock-free SPMC queue backed by persistent memory. Use per-slot sequence counters, cache-line-aligned slots, and a two-phase commit: write payload, flush fence, then publish sequence. Consumer",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two 32-bit atomics: next and serve. Init next=serve=0. Lock: my = atomic_fetch_add(&next,1); while (atomic_load(&serve) != my) { pause(); } Unlock: atomic_store_explicit(&serve, my+1, memory_order_rel",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Encode a single 64-bit slot: high 32 bits = sequence, low 32 bits = payload. Init seq=0, payload=0. Sender CASes to publish (seq+1, payload) when current seq matches and payload is 0. Receiver uses ac",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Two writers and one reader share a shm ring buffer with N=1<<16; each slot has a 64-bit seq and 128-byte payload. Producers CAS head, write payload, bump seq, release-store head. Consumer uses acquire",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests low-level synchronization primitives, memory ordering, and ABA avoidance in a tiny mailbox primitive.\n\n## Key Concepts\n- Atomic CAS on a 64-bit slot; sequence counters prevent ABA.\n- Memory_order_release on publish, memory_order_acquire on receive for visibility.\n- Fill-empty state via payload==0 heuristic for simplicity; robust designs can reserve a dedicated marker.\n\n## Code Example\n```c\ntypedef struct { _Atomic uint64_t slot; } Mailbox;\nvoid mailbox_init(Mailbox *m){ atomic_init(&m->slot, 0); }\nbool mailbox_send(Mailbox *m, uint32_t payload){\n  uint64_t cur = atomic_load_explicit(&m->slot, memory_order_relaxed);\n  uint32_t seq = (uint32_t)(cur>>32);\n  uint64_t want = ((uint64_t)seq<<32);\n  uint64_t next = (((uint64_t)(seq+1))<<32) | payload;\n  return atomic_compare_exchange_strong(&m->slot, &want, next);\n}\nbool mailbox_recv(Mailbox *m, uint32_t *out){\n  uint64_t cur = atomic_load_explicit(&m->slot, memory_order_acquire);\n  uint32_t seq = (uint32_t)(cur>>32);\n  uint32_t payload = (uint32_t)(cur & 0xFFFFFFFF);\n  if (payload == 0) return false;\n  uint64_t next = (((uint64_t)(seq+1))<<32) | 0u;\n  if (atomic_compare_exchange_strong(&m->slot, &cur, next)){\n    *out = payload; return true;\n  }\n  return false;\n}\n```\n\n## Follow-up Questions\n- How would you extend to multiple producers while keeping SPSC guarantees?\n- What microarchitectural pitfalls exist (false sharing, cache lines) and how would you mitigate them?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-2",
        "questionId": "q-23461",
        "question": "Design a per-core work-stealing deque for a real-time scheduler on a 2-socket NUMA server; ensure deterministic latency with a fixed budget per task, implement a cache-line-aligned local ring with 64-byte slots, per-slot sequence counters to avoid ABA, and a lock-free steal path from a neighbor's front using CAS; outline a microbenchmark to validate tail latency under mixed load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use stdatomic.h atomic_flag for a spinlock. Acquire: while (atomic_flag_test_and_set_explicit(&lock->flag, memory_order_acquire)) { _mm_pause(); for (int b=1; b<1024; b<<=1) _mm_pause(); }. Release: a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a lock-free free list with an atomic head. Preallocate N=2^16 blocks with the following layout: ```c typedef struct Block { struct Block* next; // 8 bytes unsigned char payload[56]; // 56 bytes } Block; // 64 bytes…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A local per-core deque implementing lock-free push and pop on a 64-byte cache-line-aligned ring buffer; per-slot sequence counters to prevent ABA problems; a lock-free steal operation from a neighbor's queue front using…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a ring with 1<<16 slots, 128B cache-line aligned. Each slot holds a 64-bit version, 64-bit pointer, 32-byte data, and a 16-byte header. Enqueue uses atomic fetch_add on tail with acquire, writ",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical low-level concurrent data structure design with real-time constraints on NUMA systems.\n\n## Key Concepts\n\n- Per-core queues and work-stealing parallelism\n- Lock-free synchronization, CAS operations, and sequence counters\n- Cache-line padding to eliminate false sharing\n- Deterministic latency budgeting and microbenchmark design\n\n## Code Example\n\n```c\n// Sketch of push/pop/steal operations (omitted for brevity)\n```\n\n## Follow-up Questions\n\n- How would you extend this design to multi-level backpressure?\n- How would you measure and mitigate jitter in a real-time system?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-3",
        "questionId": "q-13989",
        "question": "Design a small fixed-size object pool for 64-byte blocks in C. Preallocate an array of blocks and a free-list that uses indices to link free blocks. Implement init_pool, pool_alloc, pool_free with an atomic CAS on a head index. Explain how you handle ABA (with a stamp) and discuss cache-line alignment and testing under multi-threaded access?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a durable, bounded, lock-free queue that resides in persistent memory (PMEM) supporting multiple producers and a single consumer. Use per-slot sequence counters to avoid ABA problems, a fixed capacity N, and cras…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a preallocated array of blocks[N], each aligned to 64-byte boundaries. Maintain a lock-free free list with the head represented as an atomic structure containing {int idx; uint32_t stamp}. During initialization, set…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use per-thread slabs with a shared central pool for overflow. Implement a lock-free free-list using atomic pointers and ABA-safe counters; thread-local caches serve fast paths. For cross-thread frees,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a lock-free, bounded object pool for 128-byte objects on a 2-socket system. API: pool_create(capacity), pool_alloc(), pool_free(). Use per-thread caches, 64-byte aligned blocks, hazard pointers",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates understanding of lock-free data structures, fixed-size memory pools, and efficient memory reuse in concurrent environments.\n\n## Key Concepts\n- Lock-free free list implementation using atomic Compare-And-Swap (CAS)\n- ABA problem mitigation through stamping/counter techniques\n- Memory alignment strategies and false sharing prevention\n- Performance validation under multi-threaded contention\n\n## Code Example\n```c\n// Implementation sketches for init_pool, pool_alloc, pool_free\n```\n\n## Follow-up Questions\n- How would you safely extend the pool to support variable block sizes?\n- What strategies would you use for pool expansion when exhausted?\n- How would you integrate this pool with existing allocators like malloc?\n- What instrumentation would you add to measure pool efficiency and contention?\n- How would you handle backpressure when the pool is under high load?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-4",
        "questionId": "q-725",
        "question": "Design a crash-consistent, in-memory index for a 4-byte key, 8-byte value store on an 8-core Linux machine. Use per-core log-structured segments and a Bloom filter; describe durable append ordering, startup recovery by replaying per-core logs, and validation under power-loss scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-core logs minimize contention; mutations append to local segments, then a durable epoch fence persists metadata. Recovery replays per-core segments to rebuild the index; Bloom filters speed lookup",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a two-counter ticket lock: next_ticket and now_serving as atomic integers. Lock: my = fetch_add(&next_ticket, 1); while (atomic_load_explicit(&now_serving, memory_order_acquire) != my); Unlock: at",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a cache-friendly tiled matmul: 32x32 tiles, 4-wide inner-loop unrolling, and first-touch NUMA affinity to localize A, B, C. Use restrict pointers, align data to 64-byte cache lines, prefetch",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a NUMA-aware, per-worker lock-free deque scheduler in C++. Each worker has a 64-byte-aligned local queue; idle workers steal from the least-loaded neighbor using tagged indices to avoid ABA. Us",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes experience with crash-consistent in-memory structures, per-core data locality, and durable metadata synchronization under power failures—critical in low-latency financial/ads platforms.\n\n## Key Concepts\n\n- Per-core log-structured index and Bloom filter for fast lookups\n- Durable append order via epoch fences and fsync/msync semantics\n- Recovery by replaying per-core logs in a deterministic order\n- False sharing minimization and memory-mapped file durability\n\n## Code Example\n\n```javascript\n// Pseudo replay of per-core logs to rebuild index\nfunction replay_logs(coreLogs) {\n  for (const core of coreLogs) {\n    for (const entry of core.segments) {\n      apply(entry);\n    }\n  }\n}\n```\n\n## Follow-up Questions\n\n- How do you handle Bloom filter false positives during recovery?\n- What are the failure modes during corruption of a core log segment, and how do you detect them?\n- How would you validate performance under steady-state and crash-recovery cycles?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-5",
        "questionId": "q-27625",
        "question": "Design a crash-consistent, lock-free, inter-process ring buffer that uses both DRAM and PMEM for durable IPC. Assume a single producer and multiple consumers, N = 1<<20 slots, 128-byte payload, and per-slot sequence counters to avoid ABA. Describe the slot layout, atomic enqueue/dequeue operations with memory order and flush barriers, and a microbenchmark plan that stresses durability under simulated power loss?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt per-slot 64-bit sequence, 128-byte payload, and a tail pointer in PMEM. Producer reserves a slot with CAS, writes payload, updates its slot sequence, and CASes the tail with release semantics. E",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-node slab caches for 32–128B and 1–4KB object buckets; lock-free singly-linked free lists per bucket; local allocation from per-node cache; on miss, fetch from per-node central pool; on cross-node free, enqueue to t…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a 64-slot SPSC ring buffer using per-slot 8-bit flags. Producer waits for flag==0, writes the 64-byte payload into slot i = tail & (N-1), then stores flag=1 with release semantics. C",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Init: pool = 1MB; blocks = 4096; bitmap = 64 64-bit words (one bit per block), all bits 0 (free). alloc(): lock; scan words for a zero bit, set it, unlock; return pool + idx*256. free(p): idx = (p - p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes low-level queues that must survive power loss, combining DRAM and PMEM. It tests understanding of crash-consistency, memory fencing, and practical IPC design.\n\n## Key Concepts\n\n- Crash-consistency with per-slot sequencing\n- PMEM flush semantics (clwb/pc flushes, sfence)\n- Lock-free progress and ABA avoidance\n- Memory ordering for inter-process communication\n- Microbenchmark design for durability under fault injections\n\n## Code Example\n\n```javascript\n// Implementation code here\n```\n\n## Follow-up Questions\n\n- How would you optimize for batch enqueues/dequeues to improve throughput?\n- What strategies handle wraparound and backpressure among multiple consumers?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-6",
        "questionId": "q-26387",
        "question": "Design a lock-free, bounded, multi-producer/multi-consumer ring buffer for real-time audio frame exchange between an ISR-like audio producer and a lower-priority worker consumer. Use per-slot sequence counters to avoid ABA, guarantee deterministic latency, and implement backpressure without dynamic allocation. Provide API prototypes and minimal pseudo-code for enqueue/dequeue with correct memory ordering, and outline a microbenchmark to validate max latency and jitter under mixed-priority traffic on a 2-socket server?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a fixed-size ring with per-slot sequence counters and 64-byte frames. Enqueue reserves via CAS on tail, writes frame, bumps seq, and uses a release store for tail. Dequeue reads head, validates seq, copies frame, an…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a NUMA-aware bounded scheduler with per-thread local queues and a shared steal pool. Local rings: 1<<14 slots, 128-byte entries and a 64-bit sequence counter to prevent ABA. Enqueue/dequeue an",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lock-free ring with N=1<<18, each slot holding a 128-byte payload and a 64-bit seq. Producers write data, publish with a release-store to the slot and advance a head index; the consumer re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a fixed-size, open-addressing hash table with per-slot version counters and a tombstone flag. Use CAS to insert, validate version on finds, and mark tombstones on delete, incrementing the ve",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nReal-time audio paths require deterministic latency and predictable backpressure under concurrent production and consumption. This question probes lock-free design, ABA avoidance, memory ordering, and cache-awareness in a practical, high-frequency context.\n\n## Key Concepts\n\n- Lock-free ring with per-slot sequence counters to avoid ABA problems\n- Deterministic latency guarantees for real-time audio frame processing\n- Memory ordering: acquire/release semantics with proper memory fences\n- Cache-line alignment and false sharing mitigation\n- Backpressure without dynamic memory allocation\n\n## Code Example\n\n```c\n// Ring buffer with per-slot sequence counters\n#define SLOT_COUNT 256\n#define FRAME_SIZE 64\n\ntypedef struct {\n    volatile uint64_t seq;\n    char data[FRAME_SIZE];\n} slot_t;\n\ntypedef struct {\n    slot_t slots[SLOT_COUNT];\n    volatile uint64_t head;  // consumer-controlled\n    volatile uint64_t tail;  // producer-controlled\n    char pad[PADSIZE];\n} ring_t;\n\n// Producer: enqueue frame\nint enqueue(ring_t* rb, const char* frame) {\n    uint64_t tail = __atomic_load_n(&rb->tail, Order::Relaxed);\n    while (1) {\n        uint64_t seq = __atomic_load_n(&rb->slots[tail % SLOT_COUNT].seq, Order::Acquire);\n        intptr_t diff = (intptr_t)tail - (intptr_t)seq;\n        if (diff < SLOT_COUNT - 1) {\n            // slot available\n            memcpy(rb->slots[tail % SLOT_COUNT].data, frame, FRAME_SIZE);\n            __atomic_store_n(&rb->slots[tail % SLOT_COUNT].seq, tail + 1, Order::Release);\n            if (__atomic_compare_exchange_n(&rb->tail, &tail, tail + 1, false, Order::Relaxed, Order::Relaxed)) {\n                return 0; // success\n            }\n        } else {\n            // backpressure: pause and retry\n            cpu_pause();\n        }\n    }\n}\n\n// Consumer: dequeue frame\nint dequeue(ring_t* rb, char* frame) {\n    uint64_t head = __atomic_load_n(&rb->head, Order::Relaxed);\n    while (1) {\n        uint64_t seq = __atomic_load_n(&rb->slots[head % SLOT_COUNT].seq, Order::Acquire);\n        if (seq > head) {\n            memcpy(frame, rb->slots[head % SLOT_COUNT].data, FRAME_SIZE);\n            __atomic_store_n(&rb->head, head + 1, Order::Release);\n            return 0; // success\n        } else if (seq == 0) {\n            // buffer empty: block or return\n            return -1;\n        }\n    }\n}\n```\n\n## Microbenchmark Outline\n\n1. Spawn one high-priority producer thread (audio ISR simulation) and one low-priority consumer thread\n2. Generate synthetic audio frames at a fixed rate (e.g., 48kHz, 256 samples per buffer)\n3. Measure enqueue and dequeue latency per frame using `clock_gettime(CLOCK_MONOTONIC)`\n4. Inject cross-traffic interference on a 2-socket server using `stress-ng` or custom load to stress memory controller\n5. Record tail latencies (p99, p99.9) and jitter (max - min) over 10M+ iterations\n6. Verify no lockups or data corruption under sustained 100% producer fill\n\n## Expected Validation Criteria\n\n- P99 latency < 1 μs under no-load, < 5 μs under cross-traffic\n- Zero missed frames under backpressure conditions\n- No ABA corruption across 1B+ enqueue/dequeue cycles\n- Deterministic behavior across NUMA sockets",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-7",
        "questionId": "q-22333",
        "question": "Design and implement a fixed-size memory pool for 64-byte blocks within a 4KB arena. Provide C functions allocate_block() and free_block(void*). Use a 64-bit bitmap to track block usage and ensure thread-safety for a multi-threaded program. Include the struct layout, allocation/free pseudo-code (with atomic operations), and a short plan to verify correctness under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a fixed-size block pool within the 64KB heap, serving 32-byte blocks aligned to 8 bytes. Maintain a singly-linked free-list stored in the first 4 bytes of each free block as the offset to the next",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a 64-bit atomic bitmap to track 64 blocks in a 4KB arena. Allocate by reading the bitmap, compute free = ~bitmap, pick idx = __builtin_ctzll(free), and CAS to set that bit. Return arena + idx*64 o",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a sense-reversing barrier with per-thread slots padded to 64 bytes to avoid false sharing. Each thread stores its arrival flag in a unique slot; the last arriving thread flips a generation counter. Threads spin on t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a fixed-capacity MPMC ring with 128-byte slots, cache-line aligned, and per-slot 64-bit version counters. Enqueue reserves a slot by atomically bumping tail, writes data, updates the version, flus",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical low-level memory management with thread-safety using atomic bit ops on a compact bitmap. A beginner-friendly yet realistic task for systems work.\n\n## Key Concepts\n\n- Fixed-size block pools\n- Bitmap-based tracking\n- Atomic CAS and memory order\n- Alignment and cache-friendly layout\n\n## Code Example\n\n```c\n#include <stdatomic.h>\n#include <stddef.h>\n\ntypedef struct {\n  alignas(64) unsigned char arena[4096];\n  atomic_uint64_t used;\n} Pool;\n\nvoid* allocate_block(Pool* p) {\n  uint64_t oldv = atomic_load_explicit(&p->used, memory_order_relaxed);\n  while (1) {\n    uint64_t free = ~oldv;\n    if (!free) return NULL;\n    int idx = __builtin_ctzll(free);\n    uint64_t want = oldv | (1ull << idx);\n    if (atomic_compare_exchange_weak(&p->used, &oldv, want))\n      return (void*)(p->arena + idx * 64);\n  }\n}\n\nvoid free_block(Pool* p, void* ptr) {\n  size_t idx = ((unsigned char*)ptr - p->arena) / 64;\n  uint64_t mask = ~(1ull << idx);\n  atomic_fetch_and_explicit(&p->used, mask, memory_order_release);\n}\n``` \n\n## Follow-up Questions\n\n- How would you detect double-free? \n- How would you extend to multiple arenas or resizing?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-8",
        "questionId": "q-10234",
        "question": "Design and implement a reusable user-space barrier for N threads using only atomic primitives (no pthread barriers). Provide C-like pseudo-code for barrier_enter(barrier, id) that returns when all threads reach the barrier, using a generation counter to avoid ABA. Explain memory_order implications and how you would test correctness with 4 threads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use 512-bit SIMD to load 64-byte blocks, broadcast the 16–32 byte pattern across vector lanes, and compare using 8–16 8-byte lanes per cycle. Build an index mask from comparison results and extract match positions. Main…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a lock-free, crash-consistent ring buffer in NVRAM. Use MPMC with power-of-two size; per-slot 64-bit sequence counters; atomic head/tail. Enqueue: reserve tail via fetch_add, write data, flu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-core caches hold 64 objects each, cache-line aligned to 64 bytes; a global lock-free freelist handles cross-core refills in batches (e.g., 64). Fast path alloc/free uses local cache with relaxed a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implementation uses two atomics: a 'count' (current thread count) and a 'generation' counter to avoid ABA. In barrier_enter(barrier, id), each thread atomically increments count with fetch_add; the last thread (when cou…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nTests understanding of inter-thread synchronization without kernel primitives, and memory ordering.\\\\n\\n## Key Concepts\\n- Atomic operations, memory_order semantics\\\\n- Barrier synchronization, generation counters\\\\n- Spin-wait considerations and pause\\n\\n## Code Example\\n```c\ntypedef struct {\n  atomic_int count;\n  atomic_uint64_t gen;\n  int N;\n} barrier_t;\n\nvoid barrier_enter(barrier_t* b, int id) {\n  uint64_t g = atomic_load_explicit(&b->gen, memory_order_relaxed);\n  if (atomic_fetch_add_explicit(&b->count, 1, memory_order_acq_rel) == b->N - 1) {\n    atomic_store_explicit(&b->count, 0, memory_order_relaxed);\n    atomic_fetch_add_explicit(&b->gen, 1, memory_order_release);\n  } else {\n    while (atomic_load_explicit(&b->gen, memory_order_acquire) == g) {\n      _mm_pause();\n    }\n  }\n}\n```\n\\n## Follow-up Questions\\n- How would you ensure fairness if threads stall? \\n- How would you extend for multiple, consecutive barrier phases?\\n",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-9",
        "questionId": "q-22129",
        "question": "Design a tiny, fixed-size memory allocator for 64-byte blocks in a multithreaded C program. The allocator should support allocate() and free() with a global pool of N = 1<<16 blocks and per-thread caches to reduce contention. Describe data structures, synchronization strategy (lock-free vs. lock-backed), and a minimal API. Provide pseudo-code for allocate and free and discuss correctness, fragmentation, and worst-case latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-core slab allocator for 64-byte messages on a 2-socket host. Use per-core caches, a lock-free central freelist, and batch refills to minimize cross-CPU traffic. Provide APIs: void* alloc(size_t),",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a ring with 1<<16 slots, 128B cache-line aligned. Each slot holds a 64-bit version, 64-bit pointer, 32-byte data, and a 16-byte header. Enqueue uses atomic fetch_add on tail with acquire, writ",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-thread caches fed from a global pool. Each block holds a next pointer; the global free list head is atomic. **allocate()**: if the local cache is non-empty, pop and return; otherwise, lock the global pool, refill th…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a fixed-size circular buffer with head, tail, and count variables to track queue state. Protect all access with a single mutex and use two condition variables: `not_full` to signal space available and `not_empty` to…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design a memory pool that balances locality, contention, and simplicity for low-latency code.\n\n## Key Concepts\n- Thread-local caches to reduce lock contention\n- Global pool synchronization (lock-free vs. lock-backed)\n- Fixed-size blocks using free lists\n- Fragmentation and worst-case latency considerations\n\n## Data Structures\n```c\ntypedef struct Block { struct Block* next; } Block;\n\ntypedef struct {\n    Block* local_cache;\n    Block* global_head;\n    size_t local_count;\n    size_t local_capacity;\n    pthread_mutex_t global_lock;\n} Allocator;\n```\n\n## API\n```c\nvoid  init_allocator(Allocator* a, Block* pool, size_t n);\nvoid* allocate(Allocator* a);\nvoid  free(Allocator* a, void* ptr);\nvoid  destroy_allocator(Allocator* a);\n```\n\n## Synchronization Strategy\n**Lock-backed approach** (simpler, sufficient for most cases):\n- Use a single mutex protecting the global free list\n- Per-thread caches are lock-free (each thread has exclusive access)\n- Global lock only acquired during cache refill/drain operations\n\n**Lock-free alternative**:\n- Use atomic compare-and-swap (CAS) on the global head pointer\n- Higher complexity but eliminates global lock contention entirely\n- Suitable for extremely high contention scenarios\n\n## Pseudo-code\n\n### allocate()\n```\nfunction allocate(a):\n    if a.local_cache not empty:\n        block = pop(a.local_cache)\n        a.local_count--\n        return block\n    \n    lock(a.global_lock)\n    while a.local_count < CACHE_SIZE and a.global_head not null:\n        block = pop(a.global_head)\n        push(a.local_cache, block)\n        a.local_count++\n    unlock(a.global_lock)\n    \n    if a.local_cache not empty:\n        block = pop(a.local_cache)\n        a.local_count--\n        return block\n    \n    return NULL  // out of memory\n```\n\n### free()\n```\nfunction free(a, block):\n    push(a.local_cache, block)\n    a.local_count++\n    \n    if a.local_count >= CACHE_SIZE:\n        lock(a.global_lock)\n        for i = 0 to CACHE_SIZE/2:\n            block = pop(a.local_cache)\n            push(a.global_head, block)\n        a.local_count -= CACHE_SIZE/2\n        unlock(a.global_lock)\n```\n\n## Correctness\n- **No double-frees**: Each block has single owner at any time\n- **No memory leaks**: All freed blocks eventually return to global pool\n- **Bounds checking**: Fixed 64-byte blocks eliminate size tracking overhead\n- **Thread safety**: Local caches provide thread isolation; global pool synchronized\n\n## Fragmentation\n- **Internal fragmentation**: Zero (fixed 64-byte blocks)\n- **External fragmentation**: Minimal, since all blocks are identical size and fully managed via free lists\n- **Local cache retention**: Blocks may sit idle in thread caches temporarily, but this is bounded and acceptable\n\n## Worst-Case Latency\n- **allocate()**: O(CACHE_SIZE) when local cache is empty and global pool must be drained; bounded by cache refill batch size\n- **free()**: O(1) amortized; O(CACHE_SIZE) only when local cache drains to global pool\n- **Contention**: Reduced significantly by per-thread caches; global lock held only during batch transfers",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-10",
        "questionId": "q-23691",
        "question": "Design a per-core arena lock-free allocator for 64-byte objects in a high-throughput packet processing path. It must be zero-copy, support bulk free, avoid false sharing, and work across a 2-socket NUMA system. Provide data structures, allocation/deallocation pseudo-code with memory fences, and discuss how to measure latency and memory-bandwidth impact under 10 Gbps traffic?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-core arena with 4 KB slab of 64B slots; 64 slots per cache-line; lock-free free-list per core using atomic head with ABA counter. Allocate: fetch and CAS on head, grab slot, advance head; Dealloca",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Proposed answer: API prototypes for enqueue/dequeue; use per-slot sequence counters to detect ABA; lock-free tail/head CAS with memory_order_acq_rel and a guarded fallback path for full/empty; pad to",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-core slab allocator: each core maintains a cache-line-aligned freelist for fixed-size objects. Alloc uses local list; if empty, steal a batch (128 objs) from a shared pool using a lock-free CAS/fe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a fixed-size allocator using a block header structure with size, next pointer, and free flag. Maintain a global free list head; allocate by searching for the first sufficiently large block, splitting when poss…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of cache-friendly memory allocators and NUMA-aware design under high contention.\n\n## Key Concepts\n- Per-core memory arenas\n- Lock-free free-list with ABA counters\n- Cache-line padding to avoid false sharing\n- Recovery/crash behavior\n\n## Code Example\n```c\n// Allocation pseudo code here\n```\n\n## Follow-up Questions\n- How would you extend this to support object lifetimes with epoch-based reclamation?\n- What happens under CPU frequency throttling and NUMA imbalance?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-11",
        "questionId": "q-25166",
        "question": "Design a small fixed-size SPSC ring buffer in C for a logging subsystem where a single producer thread emits 64-byte messages and a single consumer thread writes them to disk. Use N as a power of two (e.g., 64 slots). Provide init, enqueue, and dequeue, with per-slot occupancy flags to avoid races. Explain wrap-around, memory ordering, and a simple correctness test under high-throughput?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Initialize a 64KB arena with an 8-byte head at offset 0 and 32-byte blocks starting at offset 8. Each block uses an 8-byte header to store the next-free index and 24 bytes for payload (8-byte alignmen",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a bounded single-producer single-consumer (SPSC) ring buffer in C for an event logger on a two-core system. Use N = 1<<12 slots, 16-byte payloads, and a per-slot 64-bit sequence counter padded to 64 bytes to a…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a cache-friendly transpose with 16x16 tiling: for r,c in steps, copy A[i][j] to B[j][i] within each tile, clipping bounds at edges. This increases L1 reuse and reduces cache misses; final co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a 64-slot SPSC ring buffer using per-slot 8-bit flags. Producer waits for flag==0, writes the 64-byte payload into slot i = tail & (N-1), then stores flag=1 with release semantics. C",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of a minimal SPSC design, cache-line effects, and correct memory ordering in a low-level context.\n\n## Key Concepts\n- SPSC ring with power-of-two capacity\n- Per-slot flags to avoid ABA and stale data\n- Release/acquire semantics for producer/consumer visibility\n- Cache-line padding to avoid false sharing\n\n## Code Example\n```javascript\n/* C-like pseudocode illustrating data, flags, and indices */\ntypedef struct {\n  uint8_t payload[64];\n  uint8_t flag; // 0 empty, 1 full\n} slot_t;\n\ntypedef struct {\n  slot_t buf[64];\n  size_t head; // consumer index\n  size_t tail; // producer index\n} spsc_ring_t;\n```\n\n## Follow-up Questions\n- How would you adapt this to multiple producers or consumers?\n- How would you benchmark latency and throughput under contention?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-12",
        "questionId": "q-7550",
        "question": "Design a crash-consistent, lock-free persistent ring buffer in non-volatile memory (NVRAM) for inter-process communication. It must be MPMC with bounded capacity, use per-slot sequence counters to avoid ABA, and support crash recovery without data loss. Provide API prototypes, discuss memory ordering and flush strategy (e.g., clflushopt/mfence or PMDK), and outline a minimal recovery procedure. How would you validate under power loss?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design and implement a 128 MB userfaultfd-backed ring buffer for high-throughput NIC traffic on a 2-socket server. Each slot contains a 64-byte payload plus an 8-byte sequence number, padded to 128 bytes. Producers rese…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Local per-socket 128KB pools with per-socket freelists; a shared pool; alloc128() prefers local freelist, then pulls from shared using atomic operations; free128() returns to the local pool if the obj",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a log-structured allocator with per-thread arenas and a persistent undo log in NVM. On allocation, reserve from a thread’s arena, emit a compact WAL entry, then update metadata with proper memory",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a lock-free, crash-consistent ring buffer in NVRAM. Use MPMC with power-of-two size; per-slot 64-bit sequence counters; atomic head/tail. Enqueue: reserve tail via fetch_add, write data, flu",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests persistent memory concepts, crash-consistency, and lock-free design under power loss.\n\n## Key Concepts\n- Persistent memory flush semantics and fencing\n- Per-slot version counters to prevent ABA\n- MPMC invariants and crash recovery\n\n## Code Example\n```c\n// API sketch and structures\n```\n\n## Follow-up Questions\n- How would you validate crash recovery under power loss?\n- How does batch flushing impact latency and throughput?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-13",
        "questionId": "q-8539",
        "question": "Design and implement a fixed-size 64-byte block memory allocator in C that preallocates a 64KB arena and supports concurrent allocation/free without using malloc. Use a free-list embedded in blocks and an atomic head pointer; provide prototypes and a minimal implementation sketch. How would you test it under multi-threaded allocation and freeing for use-after-free or double-free issues?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-thread arenas + global quarantine with epoch-based reclamation. Use versioned pointers on free-list heads to prevent ABA; allocate from local arena, thread-local cache; on free, push to quarantine",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt per-slot 64-bit sequence, 128-byte payload, and a tail pointer in PMEM. Producer reserves a slot with CAS, writes payload, updates its slot sequence, and CASes the tail with release semantics. E",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a tiny-object allocator with per-thread caches and a scalable global free-list. Each thread allocates from its local cache (e.g., 64 blocks of 128B); on miss, fetch a batch from a lock-free glo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a fixed-size block allocator: preallocate arena (64KB) of 64-byte blocks. Use the head of a free-list stored in the arena; each free block stores a next pointer. Allocation uses lock-free CA",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a safe, efficient memory primitive from scratch, focusing on low-level concurrency and memory layout rather than library calls.\n\n## Key Concepts\n\n- Fixed-size allocators and arena preallocation\n- Free-list embedded in blocks\n- Lock-free allocation/free with atomic CAS\n- Memory alignment and fragmentation basics\n- Basic multi-threaded testing strategies\n\n## Code Example\n\n```javascript\n// Sketch in C-like pseudocode\n#include <stdatomic.h>\ntypedef struct Block { struct Block* next; unsigned char data[64]; } Block;\nstatic unsigned char arena[64*1024];\n_Atomic(Block*) head;\nvoid init_pool() { head = NULL; /* link blocks in arena */ }\nvoid* alloc_block() { Block* b; while ((b = atomic_load(&head)) && !atomic_compare_exchange_weak(&head, (Block**)&head, (Block*)b->next)); return b ? b->data : NULL; }\nvoid free_block(void* p) { Block* b = (Block*)((char*)p - offsetof(Block, data)); b->next = atomic_load(&head); while (!atomic_compare_exchange_weak(&head, (Block**)&head, b)); }\n```\n\n## Follow-up Questions\n\n- How would you detect double-free or use-after-free in this scheme?\n- How would you extend to multiple block sizes or a scalable pool?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-14",
        "questionId": "q-778",
        "question": "Write a C function sum_prefetch that sums N 64-bit integers from an aligned array of length n using software prefetching to hide memory latency. Use 4-way unrolling and __builtin_prefetch to bring data 256 elements ahead. Ensure correctness for any length. Propose a microbenchmark plan to compare with a naive loop and discuss cache-line utilization and false sharing concerns?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use four accumulators (a,b,c,d). Loop i+=4; prefetch arr[i+256]; accumulate a+=arr[i], b+=arr[i+1], c+=arr[i+2], d+=arr[i+3]. Finalize by adding a,b,c,d. Ensure arr is 64-byte aligned and compile with",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-NUMA freelists with versioned heads to avoid ABA. Allocation: try local list CAS on (head, version); on empty, fetch a batch from a central pool, refill local, retry. Free: push to local; if local",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-phase, durable enqueue/dequeue using a circular buffer in NVRAM. On enqueue: write payload, publish via a release-store to head and flush the payload region once; on dequeue: read data, advance ta",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A ticket spinlock uses a `next` counter to assign tickets and a `serve` counter to indicate which ticket currently holds the lock. The acquire path atomically fetches the next ticket, then spins until `serve` matches it…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical low-level optimization skills: cache behavior, prefetching, and reliable counting.\n\n## Key Concepts\n- Cache lines and spatial locality\n- Software prefetching with __builtin_prefetch\n- Loop unrolling and multiple accumulators\n- Alignment and portability\n\n## Code Example\n```c\nuint64_t sum_prefetch(const uint64_t *arr, size_t n){\n    uint64_t a=0,b=0,c=0,d=0;\n    size_t i=0;\n    for(; i+3<n; i+=4){\n        __builtin_prefetch(&arr[i+256],0,1);\n        a += arr[i];\n        b += arr[i+1];\n        c += arr[i+2];\n        d += arr[i+3];\n    }\n    for(; i<n; ++i) a += arr[i];\n    return a+b+c+d;\n}\n``` \n\n## Follow-up Questions\n- How would you adapt this for streaming data vs. random access?\n- What changes if array length is not multiple of four?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-15",
        "questionId": "q-19900",
        "question": "Implement a tiny fixed-size slab allocator in C for 128-byte blocks drawn from a 256 KB pre-allocated pool. The allocator must be thread-safe for multiple producers/consumers, using a single global spinlock for simplicity. Provide init_pool(), alloc_block(), and free_block(). Describe block meta-placement (e.g., store next pointer in freed blocks), alignment guarantees, fragmentation risks, and a minimal stress test plan showing throughput under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a 256 KB pool, 2048 blocks of 128 bytes. Implement a global spinlock (atomic_flag) to guard alloc/free. Free blocks link via first pointer word; on alloc, pop from head; on free, push. Align t",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a fixed-size, thread-safe allocator for 32-byte blocks from a pre-allocated arena (1–2 MB). Use a lock-free free-list updated via CAS, plus per-thread caches to reduce contention. Expose voi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Question:** Implement a scalable sense-reversing barrier in C11 for N threads using only atomic primitives. Each thread must call `barrier_wait()`, which returns only after all N threads have arrived, then reuses the…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed approach: a 16-slot wheel with a shared mutex protecting slot lists for simplicity. add_timer locks the target slot (slot_index = (current_tick + delay_ms) % 16), appends a timer with expiry_",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests low-level memory management and basic concurrency without overwhelming complexity.\n\n## Key Concepts\n- Slab allocator basics\n- Thread-safety with a spinlock\n- Simple free-list management and alignment\n- Fragmentation control and sanity checks\n\n## Code Example\n```c\n// minimal sketch\ntypedef struct Block { struct Block* next; } Block;\n#define BLOCK_SIZE 128\n#define POOL_SIZE (256*1024)\nstatic uint8_t pool[POOL_SIZE];\nstatic Block* free_list; static _Atomic int lock = ATOMIC_VAR_INIT(0);\nvoid init_pool(void){ /* link blocks */ }\nvoid* alloc_block(void){ /* lock, pop, return */ }\nvoid free_block(void* p){ /* push back */ }\n```\n\n## Follow-up Questions\n- How would you extend to support variable-sized allocations without fragmentation? \n- What tests ensure correctness under high contention?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-16",
        "questionId": "q-826",
        "question": "Design and implement a tiny spinlock in C11 for a shared memory region. Provide lock() and unlock() using stdatomic.h primitives, ensuring memory_order_acquire on successful lock and memory_order_release on unlock. Include a minimal two-thread test contending for the lock and explain your backoff/yield strategy and fairness limitations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a NUMA-aware slab allocator with per-thread caches to optimize memory locality on multi-socket systems. The design maintains separate slab pools for each NUMA node, with thread-local free lists providing fast…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a 256 KB pool, 2048 blocks of 128 bytes. Implement a global spinlock (atomic_flag) to guard alloc/free. Free blocks link via first pointer word; on alloc, pop from head; on free, push. Align t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "```c #include <stdatomic.h> #include <sched.h> typedef struct { atomic_flag f; } spinlock_t; #define SPINLOCK_INIT { ATOMIC_FLAG_INIT } static inline void spin_lock(spinlock_t *s){ while (atomic_f",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Approach: Implement a fixed-size ring buffer in persistent memory with per-slot metadata. Each slot contains a 64-byte payload, an 8-byte sequence counter, and an 8-byte ready flag. The writer writes the payload, advanc…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical use of C11 atomics, memory ordering, and contention handling in low-level code. ## Key Concepts\n- C11 atomic primitives (atomic_flag)\n- memory_order_acquire/release semantics\n- busy-wait with backoff/yield to reduce bus traffic\n- fairness and starvation considerations ## Code Example\n```c\n// above code snippet\n```\n## Follow-up Questions\n- How would you extend this to a fair queueing spinlock?\n- What benchmarks would you run to profile contention impact?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-17",
        "questionId": "q-14250",
        "question": "Design a fixed-size memory pool for 64-byte blocks to be used by a multithreaded logging path. It must support many threads acquiring and releasing blocks with minimal latency, no dynamic allocations after startup, and 64-byte alignment. Provide C-like prototypes and a minimal outline for implementation, including per-thread caches, a global atomic free-list, and strategy for refill/spill. Explain how you would test correctness and throughput under bursty traffic?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a slab allocator with per-thread caches. Each block is 64-byte aligned. Global free-list uses an atomic stack of indices; each thread keeps a local cache of 4 blocks. Acquire: if cache empty, p",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Three free lists (head16, head32, head64) with the next pointer stored at the start of each free block. init_pool() creates 8 blocks per size and threads them into the appropriate list. alloc(size) fi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a fixed-size block allocator: preallocate arena (64KB) of 64-byte blocks. Use the head of a free-list stored in the arena; each free block stores a next pointer. Allocation uses lock-free CA",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a NUMA-aware slab allocator with per-thread caches to optimize memory locality on multi-socket systems. The design maintains separate slab pools for each NUMA node, with thread-local free lists providing fast…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis task probes practical low-level memory management, thread-locality, and lock-free coordination under concurrency.\n\n## Key Concepts\n- Fixed-size slab pool with thread-local caches to reduce contention\n- 64-byte alignment and cache-friendly layout\n- Global atomic free-list for cross-thread reuse\n- Bounded pool leading to deterministic latency and predictable fragmentation\n- Simple stress test plan to validate throughput and correctness\n\n## Code Example\n```javascript\n// Minimal API (C-like)\ntypedef struct { void* blocks; size_t block_size; _Atomic(size_t) *free_indices; size_t total; } pool_t;\nvoid pool_init(pool_t*, size_t blocks, size_t block_size);\nvoid* pool_acquire(pool_t*);\nvoid pool_release(pool_t*, void*);\n```\n\n## Follow-up Questions\n- How would you extend to multiple pools or varying block sizes?\n- How would you detect and mitigate fragmentation during long-running runs?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-18",
        "questionId": "q-12038",
        "question": "Design a durable, lock-free queue for a 2-socket x86 server backed by persistent memory (Optane DC PM). It should be single producer, multiple consumers (SPMC) with a fixed capacity N=1<<20 and per-slot sequence counters to avoid ABA. Ensure crash-consistency with minimal flushes, choose between epoch-based reclamation and persistent hazard pointers, and outline a microbenchmark simulating power loss at enqueue/dequeue moments?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a fixed-size, thread-local arena allocator with per-thread pools for small size classes (8,16,32,64,128,256). Allocate by rounding up to class, pop from thread pool, else fall back to a lock-fr",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use stdatomic.h atomic_flag for a spinlock. Acquire: while (atomic_flag_test_and_set_explicit(&lock->flag, memory_order_acquire)) { _mm_pause(); for (int b=1; b<1024; b<<=1) _mm_pause(); }. Release: a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Durable, lock-free SPMC queue backed by persistent memory. Use per-slot sequence counters, cache-line-aligned slots, and a two-phase commit: write payload, flush fence, then publish sequence. Consumer",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use atomic_fetch_add_explicit for a per-thread ticket; spin on owner until it equals my_ticket; store my_ticket in thread-local storage; on unlock, atomic_store_explicit(&owner, my_ticket+1, memory_or",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nUnderstanding crash-consistent data structures in PM requires knowledge of memory ordering, flushing, and reclamation.\n\n## Key Concepts\n- Durable queues, PM semantics, CLFLUSH, SFENCE\n- Lock-free SPMC design with per-slot seq counters\n- Reclamation: epoch vs persistent hazards\n- Crash-injection testing and recovery planning\n\n## Code Example\n```c\ntypedef struct { /* slot */ } slot_t;\ntypedef struct { /* queue */ } dqueue_t;\nvoid enq(dqueue_t *q, void *data);\nint deq(dqueue_t *q, void **out);\n```\n\n## Follow-up Questions\n- How would you validate crash-consistency under power loss?\n- Compare epoch-based reclamation vs persistent hazard pointers in this context.",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-19",
        "questionId": "q-18620",
        "question": "Design a fixed-size-block allocator for 32-byte blocks in a pre-allocated arena (1–2 MB) that supports thread-safe malloc/free without coarse locks. Provide API prototypes, minimal pseudo-code for init/malloc/free using a lock-free free-list (CAS) and thread-local caches, and discuss alignment, fragmentation, and how you would validate it under multithreaded stress?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a per-core, bounded publish–subscribe queue: each producer writes to a local ring buffer and publishes to a shared log with per-slot sequence numbers. Use per-slot 64-byte, cache-aligned slots",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a fixed-size, thread-safe allocator for 32-byte blocks from a pre-allocated arena (1–2 MB). Use a lock-free free-list updated via CAS, plus per-thread caches to reduce contention. Expose voi",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-core arenas with a lock-free local free-list and a shared global pool refilling via CAS in batches. Allocation uses local list pop; if empty, atomically steal a batch from the global pool; dealloc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a tiny fixed-size block allocator (128B) for a high-throughput logging path. Each thread has a private freelist; when empty, fetch a slab from a global pool. Use a lock-free freelist with atomic operations to avo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess practical low-level memory management skills, lock-free primitives, and performance considerations for multithreaded C/C++ code.\n\n## Key Concepts\n\n- Fixed-size allocators\n- Lock-free free-list with CAS\n- Thread-local caches\n- Alignment and fragmentation\n\n## Code Example\n\n```javascript\n// Pseudo-code for init, malloc, and free\ntypedef struct Node { void* next; } Node;\nstatic void* head;\n\nvoid init_arena(void* arena_base, size_t size) {\n  // split arena into 32-byte blocks and link on free-list\n}\n\nvoid* my_malloc() {\n  void* old = atomic_load(&head);\n  void* next = ((Node*)old)->next;\n  if (atomic_compare_exchange_weak(&head, &old, next)) return old;\n  // retry loop omitted for brevity\n}\n\nvoid my_free(void* p) {\n  Node* node = (Node*)p;\n  void* old;\n  do {\n    old = atomic_load(&head);\n    node->next = old;\n  } while (!atomic_compare_exchange_weak(&head, &old, node));\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-20",
        "questionId": "q-14363",
        "question": "Design a minimal software timer wheel for a user-space program that schedules callbacks at millisecond granularity. Use fixed 2048 buckets and a dedicated 1 kHz tick thread. Provide pseudo-code for add_timer(callback, delay_ms) and tick() that fires due timers. Discuss jitter, locking strategy (per-bucket vs global), and a test plan to validate correctness under high scheduling load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A minimal software timer wheel with 2048 buckets and 1 kHz tick thread provides efficient millisecond-precision scheduling. Each bucket maintains a linked list of timers; `add_timer(callback, delay_ms)` calculates expir…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement two variants using pthreads. Variant A uses a shared counters array where thread 0 updates counters[0] and thread 1 updates counters[1] in a tight loop, causing false sharing. Variant B pads each counter to 64…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a ring with per-slot 64-bit sequence and a persistent head/tail. On enqueue: write payload, write seq+1, flush, advance tail with a fence. On dequeue: read seq, verify, claim, advance head, flush.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a bounded MPMC queue that avoids contention by partitioning slots into per-producer stripes (e.g., 8 stripes, 1<<10 slots each) and a single shared drain. Each producer atomically reserves a s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates understanding of practical, low-level timing data structures for efficiently managing many timers with bounded latency and predictable jitter, contrasting with heap-based approaches.\n\n## Key Concepts\n- Fixed 2048-bucket wheel with modulo mapping for O(1) timer insertion\n- 1 ms granularity from 1 kHz tick thread providing time base\n- Jitter characteristics: bounded to ±1ms, predictable execution patterns\n- Locking strategies: per-bucket spinlocks for reduced contention vs global mutex for simplicity\n- Cache locality benefits of bucket organization vs random heap access\n- Timer lifecycle management: insertion, expiry handling, cancellation support\n- High-load testing methodology with concurrency and stress scenarios\n\n## Code Example\n```c\n// Pseudo implementation sketch\n#define BUCKETS 2048\ntypedef struct Timer {\n    void (*cb)(void*);\n    void* arg;\n    uint32_t expiry;\n    struct Timer* next;\n} Timer;\n\nstatic Timer* buckets[BUCKETS];\nstatic pthread_mutex_t locks[BUCKETS];\nstatic volatile uint32_t current_tick = 0;\n\nvoid add_timer(void (*cb)(void*), void* arg, uint32_t delay_ms) {\n    Timer* t = malloc(sizeof(Timer));\n    t->cb = cb;\n    t->arg = arg;\n    t->expiry = current_tick + delay_ms;\n    \n    uint32_t bucket_idx = t->expiry % BUCKETS;\n    pthread_mutex_lock(&locks[bucket_idx]);\n    t->next = buckets[bucket_idx];\n    buckets[bucket_idx] = t;\n    pthread_mutex_unlock(&locks[bucket_idx]);\n}\n\nvoid tick() {\n    uint32_t bucket_idx = current_tick % BUCKETS;\n    pthread_mutex_lock(&locks[bucket_idx]);\n    Timer* t = buckets[bucket_idx];\n    buckets[bucket_idx] = NULL;\n    pthread_mutex_unlock(&locks[bucket_idx]);\n    \n    while (t) {\n        Timer* next = t->next;\n        if (t->expiry <= current_tick) {\n            t->cb(t->arg);\n            free(t);\n        }\n        t = next;\n    }\n    current_tick++;\n}\n```\n\n## Locking Strategy\n**Per-bucket locking** reduces contention by allowing concurrent access to different buckets, improving scalability under high timer load. **Global locking** is simpler but becomes a bottleneck with many threads adding timers. Per-bucket spinlocks work well given minimal critical sections and good cache locality.\n\n## Test Plan\n1. **Functional correctness**: Schedule timers with varying delays, verify exact callback execution times\n2. **Jitter analysis**: Measure actual vs expected execution times to validate ±1ms bounds\n3. **Concurrency stress**: Multiple threads adding timers while tick thread processes\n4. **Load testing**: Schedule thousands of concurrent timers to validate O(1) performance\n5. **Edge cases**: Test with delay = 0, maximum delay, and rapid timer cancellation\n6. **Memory validation**: Ensure no timer leaks under high scheduling/cancellation rates",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-21",
        "questionId": "q-11221",
        "question": "On a 2-socket Linux server with a high-rate NIC, design a lock-free per-core slab allocator for fixed-size blocks (256B) used for DMA descriptors. Blocks are cached per-core; a global pool is touched when a core runs dry. Use epoch-based reclamation to retire blocks, ensure O(1) allocate/free, cache-aligned structures, and minimize cross-NUMA traffic. Provide data structures, pseudo-code for allocate() and free(), and a microbenchmark plan focusing on cross-core throughput and latency under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a minimal spinlock in C using atomic_flag with lock, unlock, and trylock APIs. The lock function should use `while (atomic_flag_test_and_set(&flag)) { pause; }`, unlock should use `atomic_flag_clear(&flag)`, w…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Crash-consistent PMEM queue: ring with N=1<<16 slots; each slot 128B: 8B seq, 4B len, 8B id, 108B payload. Enqueue writes payload, flushes, updates len, then CAS on tail; Dequeue validates seq/len, co",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lock-free MPSC ring with N=1<<18 slots, each 64 bytes and a 64-bit per-slot sequence counter. Producers atomically fetch_tail, compute s = tail & (N-1), spin until slots[s].seq == tail, wr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-core free lists with a shared global pool of 256B blocks; blocks and queues cache-line aligned (64B). allocate() pops from local list; if empty, batch-refill from global pool; free() pushes to loc",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical memory allocator design in a NUMA, high-throughput setting with DMA. It probes lock-free data structures, memory alignment, and cross-core traffic considerations.\n\n## Key Concepts\n- Lock-free per-core allocators\n- Global pool interactions and batching\n- Epoch-based reclamation\n- NUMA-aware cache-line placement\n- DMA descriptor lifetimes\n\n## Code Example\n```c\nstruct Block { alignas(64) uint8_t data[256]; uint64_t next; };\n// Pseudo: per-core caches, batch refill from global pool, epoch GC hooks\n```\n\n## Follow-up Questions\n- How would you test false sharing? How does block size affect occupancy? What happens under CPU hot-plug or NUMA rebalancing?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-22",
        "questionId": "q-18431",
        "question": "Design a tiny fixed-size block allocator (128B) for a high-throughput logging path. Each thread has a private freelist; when empty, fetch a slab from a global pool; use a lock-free freelist with atomic ops to avoid locks. Provide alloc/free pseudo-code, explain memory order, and discuss fragmentation and NUMA considerations. Include a simple stress test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An implementation would use per-thread free lists and an epoch-based reclamation scheme. Allocate from the thread-local cache; on empty, refill from a central pool with a batch fetch using CAS, taggin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: pre-allocate a 256KB arena of 64-byte blocks; each thread maintains a lock-free free-list (stack) of blocks. allocate pops a block atomically; free pushes it back. Cross-thread frees land in",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a tiny fixed-size block allocator (128B) for a high-throughput logging path. Each thread has a private freelist; when empty, fetch a slab from a global pool. Use a lock-free freelist with atomic operations to avo…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan to implement per-slot sequence counters, cache-line aligned slots, and CAS-based enqueues/dequeues with acquire/release semantics. Use a 128-byte descriptor, precompute masks for wrap-around, and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests understanding of low-level memory management, per-thread caching strategies, lock-free data structures, memory ordering semantics, and NUMA awareness in practical allocator design.\n\n## Key Concepts\n- Fixed-size block allocators\n- Thread-local storage (TLS) freelists with global slab pool\n- Lock-free freelist with ABA avoidance using atomic compare-exchange\n- Memory ordering: acquire/release semantics\n- NUMA locality considerations and fragmentation\n\n## Code Example\n\n```javascript\n// Pseudo-code: alloc/free for fixed-size blocks\ntypedef struct Block { struct Block* next; } Block;\nBlock* tls_freelist_head;\nBlock* global_pool_pull();\n\nBlock* alloc() {\n  Block* b = tls_freelist_head;\n  if (!b) {\n    b = global_pool_pull();\n    if (!b) return NULL; // OOM\n  }\n  tls_freelist_head = b->next;\n  return b;\n}\n\nvoid free(Block* b) {\n  b->next = tls_freelist_head;\n  tls_freelist_head = b;\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-23",
        "questionId": "q-28852",
        "question": "Design a durable, lock-free inter-process queue on non-volatile RAM. Use a log-structured append design with per-producer segments and a shared recovered tail; ensure enqueues are flushed before advancing the durable tail descriptor and that dequeues are safe under crash recovery. Provide API prototypes (init, enqueue, dequeue, recover) and minimal pseudo-code; discuss ordering barriers, cache-line alignment, failure-safety, and a microbenchmark plan under mixed contention and simulated power loss?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a durable, lock-free inter-process queue backed by non-volatile RAM using a log-structured append architecture with per-producer segments. Enqueue operations flush writes to NVRAM before advancing a shared durabl…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Detect overlap and copy direction (src < dst ? backward : forward). Use AVX2 to copy 32/64-byte chunks, aligning when possible. For blocks above 256B, employ non-temporal stores to minimize cache poll",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "NUMA-aware slab allocator with per-thread caches and a small global pool. Each thread maintains a 4KB slab of 64B objects (64 slots) aligned to 64B. Freelist entries are 64B with a CAS-based push/pop",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a 64-bit atomic bitmap to track 64 blocks in a 4KB arena. Allocate by reading the bitmap, compute free = ~bitmap, pick idx = __builtin_ctzll(free), and CAS to set that bit. Return arena + idx*64 o",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates the ability to design crash-safe, lock-free data structures for non-volatile memory systems. It tests understanding of durability guarantees, memory ordering semantics, and recovery mechanisms.\n\n## Key Concepts\n\n- **Durable memory semantics** in NVRAM environments\n- **Log-structured queue design** for write optimization\n- **Per-producer segmentation** to reduce contention\n- **Atomic tail descriptors** and sequence counters to mitigate ABA problems\n- **Recovery replay** mechanisms and correctness guarantees\n\n## Code Example\n\n```c\ntypedef struct {\n    volatile uint64_t head;\n    volatile uint64_t tail;\n    _Atomic uint64_t durable_tail;\n    void *segments[NUM_SEGMENTS];\n} NVRAMQueue;\n\n// Pseudo-code for enqueue\nint enqueue(NVRAMQueue *q, void *data) {\n    uint64_t segment = get_segment_id();\n    uint64_t idx = atomic_fetch_add(&q->tail, 1) % SEGMENT_SIZE;\n    \n    // Write data and flush to NVRAM\n    write_and_flush(&q->segments[segment][idx], data);\n    \n    // Memory barrier before advancing durable tail\n    atomic_thread_fence(memory_order_release);\n    \n    // Atomically advance durable tail\n    uint64_t old = atomic_load(&q->durable_tail);\n    atomic_compare_exchange_strong(&q->durable_tail, &old, old + 1);\n    return 0;\n}\n\n// Pseudo-code for dequeue with recovery safety\nvoid *dequeue(NVRAMQueue *q) {\n    uint64_t durable = atomic_load(&q->durable_tail);\n    uint64_t current = atomic_load(&q->head);\n    \n    if (current >= durable) return NULL; // Nothing to dequeue\n    \n    // Read with acquire barrier\n    atomic_thread_fence(memory_order_acquire);\n    \n    return read_and_invalidate(&q->segments[current % SEGMENT_SIZE]);\n}\n\n// Recovery procedure\nint recover(NVRAMQueue *q) {\n    uint64_t durable = atomic_load(&q->durable_tail);\n    uint64_t head = atomic_load(&q->head);\n    \n    // Replay log to find last consistent state\n    while (head < durable) {\n        validate_and_replay_entry(q, head);\n        head++;\n    }\n    \n    atomic_store(&q->head, durable);\n    return 0;\n}\n```\n\n## Discussion Points\n\n### Memory Ordering Barriers\n- Use `memory_order_release` before advancing the durable tail to ensure all writes are visible\n- Use `memory_order_acquire` when reading the tail to synchronize with previous producers\n\n### Cache-Line Alignment\n- Align each segment to cache-line boundaries (typically 64 bytes) to prevent false sharing\n- Pad metadata structures to avoid contention between producers\n\n### Failure Safety\n- The durable tail only advances after NVRAM flushes complete\n- Recovery replays from the last known head position to the durable tail\n- Checkpoint periodically to bound recovery time\n\n### Microbenchmark Plan\n1. **Mixed contention**: Run multiple producers/consumers with varying workloads\n2. **Simulated power loss**: Use fsync/fence operations to model durability\n3. **Metrics**: Measure throughput, latency, and recovery time after crash\n4. **Test scenarios**: Single producer, multiple producers, high contention, and crash-recovery sequences",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-24",
        "questionId": "q-12682",
        "question": "Design a fixed-size block pool allocator in C: pre-allocate a 1MB pool, support thread-safe allocate/free for 64-byte blocks using a lock-free free-list. Use an atomic head pointer; free blocks store the next pointer in the first word. Provide allocate/free pseudocode, ensure alignment, and outline a minimal stress test harness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a 4-level, 64-slot-per-level lock-free timer wheel. schedule_timer(delta_ns, cb) maps to the target slot at the appropriate level; enqueue a descriptor in a per-level slab using a per-slot sequ",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-thread caches store fixed-size slabs; central free-list per size class is a lock-free ring; on allocate, try thread cache, else fetch from central, and on free, push to thread cache or central if",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a 1MB fixed-size pool of 64-byte blocks with a lock-free freelist. Each free block reuses its first word as the next pointer; the pool head is an atomic pointer. Allocate pops the head via CAS",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Initialize a 64KB arena with an 8-byte head at offset 0 and 32-byte blocks starting at offset 8. Each block uses an 8-byte header to store the next-free index and 24 bytes for payload (8-byte alignmen",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical memory management, lock-free data structures, and safe object reuse under contention. A beginner-friendly allocator emphasizes alignment and simple reclaim strategies.\n\n## Key Concepts\n- Lock-free freelist using CAS\n- ABA avoidance (tagged pointers or hazard pointers)\n- Slab-style pool with 64-byte blocks\n- Cache-line awareness and fragmentation\n\n## Code Example\n```c\n// Pseudo-code for allocate/free\ntypedef struct Block { struct Block* next; } Block;\ntypedef struct Pool { Block* head; void* pool; size_t block_size; } Pool;\n\nBlock* allocate(Pool* p){\n  Block* old = atomic_load(&p->head);\n  while (old){\n    Block* next = old->next;\n    if (atomic_compare_exchange_weak(&p->head, &old, next)){\n      return old;\n    }\n  }\n  return NULL;\n}\nvoid deallocate(Pool* p, Block* b){\n  Block* head = atomic_load(&p->head);\n  do {\n    b->next = head;\n  } while (!atomic_compare_exchange_weak(&p->head, &head, b));\n}\n```\n\n## Follow-up Questions\n- How would you detect pool exhaustion and recover?\n- How would you extend to variable-size objects or per-object headers?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-01T11:54:31.368Z",
    "version": 1
  }
]