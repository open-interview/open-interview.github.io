[
  {
    "id": "test-low-level",
    "channelId": "low-level",
    "channelName": "LOW Level",
    "title": "LOW Level Knowledge Test",
    "description": "Test your understanding of LOW Level with 25 questions.",
    "questions": [
      {
        "id": "low-level-q-0",
        "questionId": "q-12682",
        "question": "Design a fixed-size block pool allocator in C: pre-allocate a 1MB pool, support thread-safe allocate/free for 64-byte blocks using a lock-free free-list. Use an atomic head pointer; free blocks store the next pointer in the first word. Provide allocate/free pseudocode, ensure alignment, and outline a minimal stress test harness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement per-core 4KB slabs with 64B blocks, a lock-free freelist using atomic CAS on a per-slab head, and 64B padding to avoid false sharing. Allocation uses the local arena; frees stay local if",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a circular log in PMEM with per-entry header {len, seq, committed}. Enqueue writes payload, then header; flush order with clwb/siws and mfence; consumer validates header and reads payload un",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a 1MB fixed-size pool of 64-byte blocks with a lock-free freelist. Each free block reuses its first word as the next pointer; the pool head is an atomic pointer. Allocate pops the head via CAS",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Two-phase, durable enqueue/dequeue using a circular buffer in NVRAM. On enqueue: write payload, publish via a release-store to head and flush the payload region once; on dequeue: read data, advance ta",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical memory management, lock-free data structures, and safe object reuse under contention. A beginner-friendly allocator emphasizes alignment and simple reclaim strategies.\n\n## Key Concepts\n- Lock-free freelist using CAS\n- ABA avoidance (tagged pointers or hazard pointers)\n- Slab-style pool with 64-byte blocks\n- Cache-line awareness and fragmentation\n\n## Code Example\n```c\n// Pseudo-code for allocate/free\ntypedef struct Block { struct Block* next; } Block;\ntypedef struct Pool { Block* head; void* pool; size_t block_size; } Pool;\n\nBlock* allocate(Pool* p){\n  Block* old = atomic_load(&p->head);\n  while (old){\n    Block* next = old->next;\n    if (atomic_compare_exchange_weak(&p->head, &old, next)){\n      return old;\n    }\n  }\n  return NULL;\n}\nvoid deallocate(Pool* p, Block* b){\n  Block* head = atomic_load(&p->head);\n  do {\n    b->next = head;\n  } while (!atomic_compare_exchange_weak(&p->head, &head, b));\n}\n```\n\n## Follow-up Questions\n- How would you detect pool exhaustion and recover?\n- How would you extend to variable-size objects or per-object headers?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-1",
        "questionId": "q-2688",
        "question": "Design and implement a per-thread arena allocator for 16-byte blocks from a fixed 256KB pool. Provide API: init_arena(void* arena, size_t size), void* alloc16(), void free16(void*). Use a global bitmap to track 16,384 blocks and a thread-local cache of 8 blocks to reduce contention. Explain alignment, fragmentation, and how this approach compares to malloc under multi-core contention. Include a minimal test harness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a NUMA-aware slab allocator using 4KB blocks with per-thread caches of 128 blocks each, backed by central memory pools per NUMA node. The metadata structures would be cache-line aligned and padded to p…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-thread arenas with size-class bins and per-socket freelists; small allocations served from thread cache; large aligned blocks drawn from a pooled arena with 64B cacheline alignment. Use epoch-base",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a NUMA-aware event bus with 4 producer rings and 4 consumer rings on a 2-socket system. Each ring holds 128-byte, cache-line-aligned events in a circular buffer. Producers advance tail atomical",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "```c #include <stdint.h> #include <string.h> #include <stdatomic.h> #include <pthread.h> #include <stdio.h> #include <time.h> #include <stdlib.h> #define ARENA_SIZE (256 * 1024) #define BLOCK_SIZE 16 #define NUM_BLOCKS…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nDemonstrates practical low-level memory management, fixed-size allocators, thread-local caches, and fragmentation trade-offs. A bitmap-based free-tracking scheme avoids pointer-chasing and supports fast scans on aligned blocks.\n\n## Key Concepts\n- Fixed-size block allocator with 16-byte alignment guarantees\n- Bitmap-based free tracking using bit manipulation for O(1) allocation\n- Thread-local caches to reduce contention under multi-core workloads\n- Fragmentation analysis: zero external fragmentation, bounded internal fragmentation\n- Performance comparison with system malloc under contention\n\n## Implementation Highlights\n- **Bitmap operations**: Uses `__builtin_ctzll` for O(1) first-free-bit finding\n- **Thread cache**: 8-block LIFO cache reduces mutex acquisitions significantly\n- **Safety checks**: Validates pointers in free() to prevent corruption\n- **Benchmark**: Includes performance comparison showing speedup under various loads\n\n## Follow-up Questions\n- How would you handle arena resizing or varying block sizes?\n- How would you ensure NUMA-awareness and fault containment?\n- What strategies would you use for garbage collection when thread caches become full?\n- How could this design be extended for larger allocations or mixed-size workloads?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-2",
        "questionId": "q-13823",
        "question": "Design a crash-consistent, lock-free ring buffer backed by persistent memory (PMEM) for multi-producer/multi-consumer traffic. API: init(capacity), enqueue(item*), dequeue(item*). Explain how you ensure durability after power loss using per-slot sequence counters and a global commit counter, plus recovery steps. Include concise enqueue/dequeue pseudo-code with CAS on head/tail, seq counters, and cache-line alignment. Outline a microbenchmark plan comparing latency and durability impact vs an in-RAM ring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-core arena with 4 KB slab of 64B slots; 64 slots per cache-line; lock-free free-list per core using atomic head with ABA counter. Allocate: fetch and CAS on head, grab slot, advance head; Dealloca",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a ring of N slots (128B each) with per-slot sequence counters and a global commit counter persisted after each successful operation. Enqueue: CAS on tail, write item, flip sequence, flush/persist,",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a fixed-size ring with per-slot sequence counters and 64-byte frames. Enqueue reserves via CAS on tail, writes frame, bumps seq, and uses a release store for tail. Dequeue reads head, validates seq, copies frame, an…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose per-thread deques with a bottom push/pop and a shared top for steals. Local path uses a fixed-size, cache-aligned ring; steals CAS on top with a version tag to prevent ABA; pad structures to p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes crash-consistency, PMEM durability, and lock-free coordination across NUMA nodes. It also tests API boundary definition and recovery semantics.\n\n## Key Concepts\n- persistent memory semantics (flush/fence), CAS on head/tail, per-slot sequence counters, global commit counter\n- crash recovery, torn write handling, wrap-around, cache-line alignment\n- microbenchmark design to measure latency and durability impact\n\n## Code Example\n```javascript\n// Pseudo-C\nfunction enqueue(r, item){ /* CAS tail, write, flush, commit */ }\n```\n\n## Follow-up Questions\n- How would you test power-fail recovery? \n- How to tune flush granularity and durability guarantees?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-3",
        "questionId": "q-24980",
        "question": "Design a beginner-friendly per-thread fixed-size object pool in C for 128-byte blocks to reduce malloc churn in a multi-threaded server. Provide API prototypes pool_init, pool_alloc, pool_free; implement a thread-local cache and a small global pool as a fallback; ensure 128-byte alignment and explain how you'd validate correctness and leak-free behavior with a simple multi-thread microbenchmark?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-thread TLS cache of 8 blocks; pool_alloc retrieves from TLS cache; on underflow, refills from a global lock-free stack using CAS; pool_free returns to TLS cache or to global pool if TLS is full. All blocks are 128-b…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Adopt per-slot 64-bit sequence, 128-byte payload, and a tail pointer in PMEM. Producer reserves a slot with CAS, writes payload, updates its slot sequence, and CASes the tail with release semantics. E",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-core, lock-free work queues on a NUMA machine. Each core owns a circular queue; producers enqueue to local tail with CAS, publish payload, then update header via release; workers dequeue from loca",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a lock-free, bounded object pool for 128-byte objects on a 2-socket system. API: pool_create(capacity), pool_alloc(), pool_free(). Use per-thread caches, 64-byte aligned blocks, hazard pointers",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses understanding of thread-local caches, lock-free fallbacks, memory alignment, and ABA problem mitigation in a practical systems programming context.\n\n## Key Concepts\n- **Thread-local storage (TLS)**: Provides low-latency allocation by keeping memory local to each thread, avoiding contention\n- **Lock-free data structures**: Use compare-and-swap (CAS) operations to safely manipulate shared state without locks\n- **Memory alignment**: Ensures 128-byte aligned blocks; cache line alignment prevents false sharing between threads\n- **ABA avoidance**: Tagged or generational pointers track state changes to prevent the ABA problem in lock-free algorithms\n\n## Code Example\n```c\n// C structures and API prototypes\ntypedef struct block_header {\n    struct block_header *next;\n    uint64_t tag;\n} block_header_t;\n\ntypedef struct pool {\n    block_header_t *global_stack;\n    uint64_t global_tag;\n} pool_t;\n\nint pool_init(pool_t *pool, size_t count);\nvoid *pool_alloc(pool_t *pool);\nvoid pool_free(pool_t *pool, void *ptr);\nvoid pool_destroy(pool_t *pool);\n```",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-4",
        "questionId": "q-27767",
        "question": "Design a minimal fixed-size per-thread memory pool for fast allocations in a multithreaded program. Each thread uses a per-thread free-list drawn from a shared arena of blocks. Provide API prototypes allocate(size) and free(ptr) with alignment guarantees, address cross-thread frees via a quarantine, and explain how you would test fragmentation and latency under 4-core contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: pre-allocate a 256KB arena of 64-byte blocks; each thread maintains a lock-free free-list (stack) of blocks. allocate pops a block atomically; free pushes it back. Cross-thread frees land in",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a crash‑safe PMEM ring buffer with two‑phase publish: write the data to the next slot, flush the data, then publish by updating a durable tail with a release store and a fence (sfence/clwb). Use p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Key design highlights: per-thread arenas with local caches, size-class slabs, a central lock-free free-list for cross-thread returns, and NUMA-aware local allocations by preferring local nodes. Mainta",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a NUMA-aware slab allocator with per-thread caches to optimize memory locality on multi-socket systems. The design maintains separate slab pools for each NUMA node, with thread-local free lists providing fast…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess practical low-level memory management skills: per-thread caches, lock-free freelists, and cross-thread safety without heavyweight locks.\n\n## Key Concepts\n\n- Per-thread caches to reduce contention\n- Lock-free free-list with atomic ops\n- Cross-thread frees via quarantine\n- Fragmentation control with fixed-size blocks\n\n## Code Example\n\n```c\n// Minimal API surface for reference\nvoid* perthread_alloc(size_t size);\nvoid perthread_free(void* ptr);\n```\n\n```c\ntypedef struct Block { struct Block* next; } Block;\n```\n\n## Follow-up Questions\n\n- How would you extend to variable-size allocations?\n- How would you quantify fragmentation under churn, and how would you tune ARENA_SIZE/BLOCK_SIZE?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-5",
        "questionId": "q-27143",
        "question": "Design a NUMA-aware, lock-free slab allocator for fixed-size objects (128B) on a 2-socket Linux server. Each object occupies a slot within per-NUMA slabs; implement per-core free lists (lock-free stacks) that refill from a global pool via atomic ops. Ensure minimal fragmentation and no cross-socket cache thrash. Provide API prototypes and minimal C-like pseudo-code, and outline a microbenchmark to compare throughput against a naive malloc under varying thread counts and memory locality?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a crash-consistent, lock-free skip list in non-volatile memory (NVRAM) that supports concurrent inserts and lookups across multiple threads while surviving power loss. Implement copy-on-write semantics: allocate…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-NUMA pools with 128B slots in 4K slabs; maintain a lock-free per-core freelist, refilling from a global pool via CAS when empty; alloc by popping from local freelist, free by pushing back. Bin",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use stdatomic.h atomic_flag for a spinlock. Acquire: while (atomic_flag_test_and_set_explicit(&lock->flag, memory_order_acquire)) { _mm_pause(); for (int b=1; b<1024; b<<=1) _mm_pause(); }. Release: a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Thread-local histograms: each thread maintains a 256-bin buffer aligned to 64 bytes. add_sample increments local bin non-atomically. Periodically merge: atomically add each bin to a global 256-bin his",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked: Tests practical, low-level allocator design with real-world constraints (NUMA, lock-free structures, fragmentation). Key trade-offs include locality vs global refill, cache-line alignment, and fast-path vs fallback paths.\n\nKey Concepts:\n- NUMA locality and first-touch bindings\n- Lock-free freelists with CAS\n- Slab allocation and fixed-size object management\n- Cache-line alignment and false sharing prevention\n- Fragmentation control and slab reclamation\n\nCode Example\n```c\ntypedef struct Block { struct Block* next; } Block;\ntypedef struct Slab { void* base; Block* freelist; struct Slab* next; } Slab;\n\n// Prototypes (illustrative)\nvoid* slab_alloc(void);\nvoid  slab_free(void* obj);\n```\n\nFollow-up Questions\n- How would you extend to multiple object sizes with per-size pools?\n- How would you simulate and measure worst-case allocator latency under contention?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-6",
        "questionId": "q-2361",
        "question": "Design a NUMA-aware fixed-size allocator in C++ for objects of 64 bytes, with per-NUMA-node free lists and a lightweight cross-node migration policy; describe structure, allocation/free, and how to avoid inter-node contention and false sharing; provide a small code sketch and a validation plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a two-tier, per-core timer wheel with microsecond granularity: a local wheel for near-term timers and a shared overflow queue feeding a global wheel. Timers are stored in per-core buckets to minim",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-NUMA allocator with per-node free lists (lock-free or atomics), each block stores next pointer; allocate uses local node list via CAS; if empty, steal from a remote node or fall back to a central",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Initialize a Block array where each block stores a next index (free-list pointer). Set free_head to 0 and link all blocks in a chain during init_pool. The allocate_block function atomically pops the head using CAS and r…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-thread arenas + global quarantine with epoch-based reclamation. Use versioned pointers on free-list heads to prevent ABA; allocate from local arena, thread-local cache; on free, push to quarantine",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical low-level memory management in NUMA systems, emphasizing locality, contention, and false sharing—key for latency-sensitive trading/analytics workloads.\n\n## Key Concepts\n\n- NUMA locality and per-node free lists\n- Lock-free/atomic free-list operations and ABA avoidance\n- Cross-node migration policies and migration costs\n- Cache-line alignment and false sharing avoidance\n- Validation: microbenchmarks with mixed-local/global allocations, perf measurements\n\n## Code Example\n\n```cpp\n// simplified per-node free-list CAS skeleton\n#include <atomic>\n\nstruct Node {\n  std::atomic<void*> head{nullptr};\n};\n\nvoid* alloc(Node& node) {\n  void* h = node.head.load(std::memory_order_acquire);\n  if (h) {\n    void* next = *(void**)h;\n    if (node.head.compare_exchange_weak(h, next, std::memory_order_acquire, std::memory_order_relaxed))\n      return h;\n  }\n  // fallback: steal from other node or central pool\n  return nullptr;\n}\n```\n\n## Follow-up Questions\n\n- How would you implement ABA protection and reclaim memory safely across NUMA nodes?\n- How would you measure cross-node traffic impact and optimize for a workload with hot/cold object lifetimes?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-7",
        "questionId": "q-25481",
        "question": "Implement a thread-local object pool in C for 64-byte objects. Each thread maintains a local freelist; when it runs dry, refills from a global pool by taking a batch of 128 objects. The global pool is a lock-free stack using CAS. Provide API prototypes: void* pool_alloc(void); void pool_free(void* obj); Ensure 64-byte alignment, no ABA, and describe a minimal test harness to compare throughput against malloc/free at 32 threads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a thread-local object pool in C for 64-byte objects. Each thread maintains a local freelist; when it runs dry, it refills from a global pool by taking a batch of 128 objects. The global pool is a lock-free sta…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-thread caches per size class, a NUMA-aware central pool, and epoch-based reclamation to avoid cross-thread frees. Slabs align to 64-byte lines; pad to prevent false sharing. Lock-free central pool",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use two stdatomic: arrivals and generation. barrier_enter() atomically increments arrivals; if it reaches N, reset to 0 and increment generation. Each thread spins on generation until it changes, then",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Provide a fixed-size pool with 512 blocks of 64B. Use a separate free-list array next[512], initialized to 1.., last=0xFFFF. pool_alloc disables interrupts, pops head, returns base+idx*64. pool_free c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates candidates on practical memory management under contention, focusing on per-thread caches and lock-free synchronization paths.\n\n## Key Concepts\n- **Thread-local freelists** minimize lock contention by allowing each thread to allocate and free objects without synchronization\n- **Lock-free global pool** using CAS (Compare-And-Swap) enables safe concurrent access without blocking\n- **Batch refills** (128 objects) amortize synchronization costs across multiple allocations\n- **64-byte alignment** and padding help prevent false sharing between cores\n- **ABA prevention** techniques such as tagged pointers or hazard pointers ensure safe memory reclamation\n\n## Code Example\n```c\n// API Prototypes\nvoid* pool_alloc(void);\nvoid pool_free(void* obj);\n```\n\n## Follow-up Questions\n- How would you handle memory pressure when the global pool is exhausted?\n- What strategies would you use to detect and recover from memory leaks in this design?\n- How does cache line alignment affect performance in high-contention scenarios?\n- Could you implement this using a different lock-free data structure, such as a Michael-Scott queue?\n\n## Evaluation Criteria\n- Correct implementation of thread-local storage with efficient refill logic\n- Proper CAS usage without introducing race conditions\n- Understanding of memory ordering and visibility guarantees\n- Ability to explain tradeoffs between complexity and performance gains",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-8",
        "questionId": "q-26387",
        "question": "Design a lock-free, bounded, multi-producer/multi-consumer ring buffer for real-time audio frame exchange between an ISR-like audio producer and a lower-priority worker consumer. Use per-slot sequence counters to avoid ABA, guarantee deterministic latency, and implement backpressure without dynamic allocation. Provide API prototypes and minimal pseudo-code for enqueue/dequeue with correct memory ordering, and outline a microbenchmark to validate max latency and jitter under mixed-priority traffic on a 2-socket server?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a cross-process IPC ring buffer backed by a memory-mapped file with multiple producers and a single consumer. Each 64-byte slot contains an 8-byte atomic sequence number followed by a 56-byte payload. Writers res…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-path memmove: if dst <= src or no overlap, copy forward in 64-byte steps; if dst > src with overlap, copy backward (start from end) to avoid clobbering source data. Handle tail bytes after 64-byte",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a thread-local object pool in C for 64-byte objects. Each thread maintains a local freelist; when it runs dry, it refills from a global pool by taking a batch of 128 objects. The global pool is a lock-free sta…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a fixed-size ring with per-slot sequence counters and 64-byte frames. Enqueue reserves via CAS on tail, writes frame, bumps seq, and uses a release store for tail. Dequeue reads head, validates seq, copies frame, an…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nReal-time audio paths require deterministic latency and predictable backpressure under concurrent production and consumption. This question probes lock-free design, ABA avoidance, memory ordering, and cache-awareness in a practical, high-frequency context.\n\n## Key Concepts\n\n- Lock-free ring with per-slot sequence counters to avoid ABA problems\n- Deterministic latency guarantees for real-time audio frame processing\n- Memory ordering: acquire/release semantics with proper memory fences\n- Cache-line alignment and false sharing mitigation\n- Backpressure without dynamic memory allocation\n\n## Code Example\n\n```c\n// Ring buffer with per-slot sequence counters\n#define SLOT_COUNT 256\n#define FRAME_SIZE 64\n\ntypedef struct {\n    volatile uint64_t seq;\n    char data[FRAME_SIZE];\n} slot_t;\n\ntypedef struct {\n    slot_t slots[SLOT_COUNT];\n    volatile uint64_t head;  // consumer-controlled\n    volatile uint64_t tail;  // producer-controlled\n    char pad[PADSIZE];\n} ring_t;\n\n// Producer: enqueue frame\nint enqueue(ring_t* rb, const char* frame) {\n    uint64_t tail = __atomic_load_n(&rb->tail, Order::Relaxed);\n    while (1) {\n        uint64_t seq = __atomic_load_n(&rb->slots[tail % SLOT_COUNT].seq, Order::Acquire);\n        intptr_t diff = (intptr_t)tail - (intptr_t)seq;\n        if (diff < SLOT_COUNT - 1) {\n            // slot available\n            memcpy(rb->slots[tail % SLOT_COUNT].data, frame, FRAME_SIZE);\n            __atomic_store_n(&rb->slots[tail % SLOT_COUNT].seq, tail + 1, Order::Release);\n            if (__atomic_compare_exchange_n(&rb->tail, &tail, tail + 1, false, Order::Relaxed, Order::Relaxed)) {\n                return 0; // success\n            }\n        } else {\n            // backpressure: pause and retry\n            cpu_pause();\n        }\n    }\n}\n\n// Consumer: dequeue frame\nint dequeue(ring_t* rb, char* frame) {\n    uint64_t head = __atomic_load_n(&rb->head, Order::Relaxed);\n    while (1) {\n        uint64_t seq = __atomic_load_n(&rb->slots[head % SLOT_COUNT].seq, Order::Acquire);\n        if (seq > head) {\n            memcpy(frame, rb->slots[head % SLOT_COUNT].data, FRAME_SIZE);\n            __atomic_store_n(&rb->head, head + 1, Order::Release);\n            return 0; // success\n        } else if (seq == 0) {\n            // buffer empty: block or return\n            return -1;\n        }\n    }\n}\n```\n\n## Microbenchmark Outline\n\n1. Spawn one high-priority producer thread (audio ISR simulation) and one low-priority consumer thread\n2. Generate synthetic audio frames at a fixed rate (e.g., 48kHz, 256 samples per buffer)\n3. Measure enqueue and dequeue latency per frame using `clock_gettime(CLOCK_MONOTONIC)`\n4. Inject cross-traffic interference on a 2-socket server using `stress-ng` or custom load to stress memory controller\n5. Record tail latencies (p99, p99.9) and jitter (max - min) over 10M+ iterations\n6. Verify no lockups or data corruption under sustained 100% producer fill\n\n## Expected Validation Criteria\n\n- P99 latency < 1 μs under no-load, < 5 μs under cross-traffic\n- Zero missed frames under backpressure conditions\n- No ABA corruption across 1B+ enqueue/dequeue cycles\n- Deterministic behavior across NUMA sockets",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-9",
        "questionId": "q-27625",
        "question": "Design a crash-consistent, lock-free, inter-process ring buffer that uses both DRAM and PMEM for durable IPC. Assume a single producer and multiple consumers, N = 1<<20 slots, 128-byte payload, and per-slot sequence counters to avoid ABA. Describe the slot layout, atomic enqueue/dequeue operations with memory order and flush barriers, and a microbenchmark plan that stresses durability under simulated power loss?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt per-slot 64-bit sequence, 128-byte payload, and a tail pointer in PMEM. Producer reserves a slot with CAS, writes payload, updates its slot sequence, and CASes the tail with release semantics. E",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "```c #include <stdint.h> #include <string.h> #include <stdatomic.h> #include <pthread.h> #include <stdio.h> #include <time.h> #include <stdlib.h> #define ARENA_SIZE (256 * 1024) #define BLOCK_SIZE 16 #define NUM_BLOCKS…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lock-free ring with N=1<<18, each slot holding a 128-byte payload and a 64-bit seq. Producers write data, publish with a release-store to the slot and advance a head index; the consumer re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "To stream descriptors efficiently, implement per-NUMA freelists with 64-byte alignment, batch reclamation, and a tiny allocator header per socket. Pin arenas to sockets, avoid cross-socket frees, and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes low-level queues that must survive power loss, combining DRAM and PMEM. It tests understanding of crash-consistency, memory fencing, and practical IPC design.\n\n## Key Concepts\n\n- Crash-consistency with per-slot sequencing\n- PMEM flush semantics (clwb/pc flushes, sfence)\n- Lock-free progress and ABA avoidance\n- Memory ordering for inter-process communication\n- Microbenchmark design for durability under fault injections\n\n## Code Example\n\n```javascript\n// Implementation code here\n```\n\n## Follow-up Questions\n\n- How would you optimize for batch enqueues/dequeues to improve throughput?\n- What strategies handle wraparound and backpressure among multiple consumers?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-10",
        "questionId": "q-21600",
        "question": "Scenario: design a crash‑consistent, lock‑free ring buffer in persistent memory for a high‑throughput NIC DMA path on a 2‑socket x86‑64 server. Use N = 1<<16 slots, 64‑byte payloads. Implement a two‑phase durable commit: 1) write payload data to a slot and flush with CLWB/CLFLUSHOPT, 2) publish a durable sequence counter and SFENCE. Provide API prototypes, minimal pseudo‑code, and recovery steps after power loss?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A lock-free, bounded task queue with work-stealing for a 2-socket x86-64 server. Each worker maintains a private deque with local push and shared steal endpoints. Producers push to their local queue; if full, they fallb…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enqueue writes to a slot and publishes a release-store to the slot’s seq; consumer uses an acquire-load to validate slot and then advances head. For crash-resilience, flush the slot data and its seq w",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a crash‑consistent PMem ring: N=1<<16 slots, 64‑byte payloads. Two‑phase commit: (1) persist payload with CLWB/CLFLUSHOPT, (2) publish new tail by updating the durable sequence and issuing S",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-thread slabs mapped to local NUMA nodes, with 64-byte aligned 128B bins. Allocate from local cache; on miss refill from a global lock-free free-list via CAS. Cross-socket frees enqueue into th",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of crash‑consistent data structures in persistent memory, including proper ordering, flush/fence semantics, and recovery guarantees in a high‑throughput path.\n\n## Key Concepts\n- Persistent memory semantics and flush instructions (CLWB, CLFLUSHOPT, SFENCE)\n- Two‑phase commit for durability\n- Ring buffer invariants and recovery in power loss scenarios\n\n## Code Example\n```c\n// Pseudo-code: writers publish after durable tail update\nslot = tail & (N-1);\nmemcpy(slot_data(slot), payload, 64);\nclwb(slot_data(slot));\nclwb(&slot_seq(slot)); // update after data\nsfence();\nslot_seq(slot) = new_seq; // durable publish\n```\n\n## Follow-up Questions\n- How would you extend to multiple producers and a multi‑consumer model with minimal contention?\n- How would you validate durability guarantees under sudden power loss and after hot restarts?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-11",
        "questionId": "q-17741",
        "question": "Design a lock-free, cache-friendly object pool on a 2-socket NUMA server that uses tagged pointers to detect ABA in the freelist. The pool must support O(1) allocate/free under high contention, store 64-byte objects, and keep per-slot data aligned to 64 bytes. Provide the slot layout, pseudo-code for allocate/free using a 128-bit atomic CAS on a combined head pointer+tag, and discuss correctness proofs and a microbenchmark plan to measure throughput and latency under concurrent access?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "use a ticket lock: two 64-bit atomics; next_ticket incremented by each thread; thread spins on now_serving until its ticket matches. Pseudo: lock: t = atomic_fetch_add(&next_ticket,1); while (atomic_l",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-NUMA freelists with versioned heads to avoid ABA. Allocation: try local list CAS on (head, version); on empty, fetch a batch from a central pool, refill local, retry. Free: push to local; if local",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Crash-consistent NVM ring: use a redo log and per-slot sequence counters. Enqueue: write payload+header, flush header (clwb/flushopt), then advance tail with a release store; Dequeue: fetch, advance h",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An ideal solution uses a 128-bit head state (pointer+tag) for the freelist and 64-byte aligned slots. Allocate: load head, read slot, CAS head to head.next with a bumped tag; on success, place the obj",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ABA avoidance, 128-bit CAS feasibility, memory ordering, and NUMA-friendly cache alignment in a practical pool design.\n\n## Key Concepts\n\n- ABA problem and tag-based solution\n- 128-bit CAS support and atomicity\n- Cache-line alignment to avoid false sharing\n- Memory orders (acquire/release) in lock-free paths\n\n## Code Example\n\n```c\ntypedef struct { void* ptr; uint64_t tag; } head_t;\ntypedef struct { uint8_t data[64]; uint64_t next; } slot_t;\n// slot array+head management omitted for brevity\n```\n\n## Follow-up Questions\n\n- How would you resize or shrink the pool under pressure without global pauses?\n- How would you validate absence of ABA in a production-grade microbenchmark?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-12",
        "questionId": "q-20964",
        "question": "Design and implement a 128 MB userfaultfd-backed ring buffer for high-throughput NIC traffic on a 2-socket server. Each slot contains a 64B payload plus 8B sequence, padded to 128B. Producers reserve head with atomic fetch_add, write payload, then publish via tail CAS. Consumer drains by checking per-slot sequence; on-demand paging via userfaultfd pages data. Explain wrap-around handling and false-sharing mitigation. Provide enqueue/dequeue pseudo-code and a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a sense-reversing barrier using a shared generation counter and per-thread flags padded to 64 bytes to prevent false sharing. Each thread toggles its local sense, sets its arrival flag, and spins on the genera…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-core counters: allocate an array blocks[NUM_CORES][4], padded to 64 bytes per core. record_event(core_id, e) { counters[core_id][e]++; } read_total() { sum over all cores and events. Padding p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design and implement a 128 MB userfaultfd-backed ring buffer for high-throughput NIC traffic on a 2-socket server. Each slot contains a 64-byte payload plus an 8-byte sequence number, padded to 128 bytes. Producers rese…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-thread arenas hold a small freelist of 256 blocks, each 128B aligned to 64B. Allocation pops from the thread-local freelist; if empty, refill from a shared global pool via CAS-based lock-free stac",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the design of a low-latency, memory-centric IPC path using userfaultfd, evaluating understanding of memory ordering, cache-line padding, and wrap-around logic in a NUMA environment.\n\n## Key Concepts\n\n- **userfaultfd lazy loading**: On-demand paging of memory pages for large ring buffers\n- **Pinned 128 MB shared memory region**: Prevents swapping; memory locked for DMA\n- **Per-slot sequencing**: Prevents ABA problems without heavy locking\n- **64B payload + 8B sequence, 128B slot padding**: Aligns to cache lines; eliminates false sharing\n- **Atomic operations**: Memory ordering semantics for producer-consumer synchronization\n- **NUMA awareness**: Optimized placement for 2-socket server topology\n\n## Implementation Details\n\n### Enqueue (Producer)\n```\nhead = atomic_fetch_add(&ring->head, 1) % RING_SIZE\n// Write payload and sequence\nring->slots[head].seq = head\nmemcpy(ring->slots[head].data, payload, 64)\n// Publish: update tail with CAS\nwhile (!atomic_compare_exchange(&ring->tail, &expected, head + 1)) {\n    expected = ring->tail\n}\n```\n\n### Dequeue (Consumer)\n```\ntail = ring->tail  // local snapshot\nif (ring->slots[tail % RING_SIZE].seq == tail) {\n    // Read payload\n    memcpy(output, ring->slots[tail % RING_SIZE].data, 64)\n    atomic_increment(&ring->tail)\n} else {\n    // Trigger page fault via userfaultfd if needed\n}\n```\n\n### Wrap-Around Handling\n- Use modulo arithmetic on the logical sequence number, not the array index\n- Sequence numbers grow monotonically without reset\n- Array index = sequence_number % RING_SIZE\n- This avoids ABA issues entirely\n\n### False-Sharing Mitigation\n- Each slot is exactly 128 bytes (2 cache lines)\n- No two slots share a cache line\n- Separate head/tail variables from slot data\n\n## Microbenchmark Plan\n\n1. **Throughput test**: Measure enqueue/dequeue ops/sec at various batch sizes\n2. **Latency test**: Measure per-operation latency at head vs tail of ring\n3. **NUMA test**: Compare same-socket vs cross-socket producer/consumer\n4. **Page fault test**: Measure fault handling overhead during cold start\n5. **Cache miss profiling**: Use VTune/Perf to identify cache-line conflicts",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-13",
        "questionId": "q-760",
        "question": "On a dual-socket NUMA server, design a cross-NUMA, zero-copy ring buffer for a 40 Gbps path between two processes where producers live on socket A and a consumer on socket B. Use per-slot 64-bit sequence numbers to prevent ABA, 128-byte payload slots, and capacity 1<<18 with 64-byte alignment. Provide slot layout, enqueue/dequeue pseudo-code with memory fences and cache-line padding, wrap-around and backpressure handling, and outline a minimal microbenchmark to validate throughput and data integrity under cross-socket contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Cross-NUMA, two-process ring for 40 Gbps on a dual-socket server. Slot: 128 bytes; capacity 1<<18; 64-byte alignment; per-slot 64-bit sequence for ABA-avoidance. NUMA-aware allocation on local node. ## Slot Layout ```c…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a ring with per-slot 64-bit sequence and a persistent head/tail. On enqueue: write payload, write seq+1, flush, advance tail with a fence. On dequeue: read seq, verify, claim, advance head, flush.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a fixed-size ring with N=256 slots. Each slot stores a 64-byte payload plus an atomic 64-bit sequence. Maintain a shared atomic tail for producers and a per-slot seq. Enqueue: i = atomic_fetch_add",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a 256-slot timer wheel; each slot holds a linked list of timers with fields id, expire_at, cb, arg, canceled, and next. timer_create(delay, cb, arg) assigns id and inserts into slot (now + dela",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-NUMA IPC design with ABA avoidance, memory ordering, and cache-line discipline for senior systems positions.\n\n## Key Concepts\n- Cross-NUMA memory allocation and cache coherence\n- Per-slot sequencing to prevent ABA problems\n- Acquire/release semantics for proper memory ordering\n- Cache-line padding to avoid false sharing\n- Backpressure handling and wrap-around logic\n- NUMA-aware process binding and memory pinning\n\n## Technical Accuracy\n- 40 Gbps requires ~5GB/s throughput, achievable with this design\n- 64-bit sequence numbers prevent ABA even with wrap-around\n- Cache-line alignment (64 bytes) prevents false sharing\n- Memory fences ensure proper ordering across NUMA domains\n- Fixed 128-byte slots with 112-byte payload meet requirements\n- Capacity 1<<18 (262144 slots) provides 32MB buffer space\n\n## Follow-up Questions\n- How would you extend to N producers and M consumers with fairness guarantees?\n- What failure modes matter under NUMA remapping or node failure?\n- How would you handle variable-sized payloads efficiently?\n- What optimizations for specific CPU architectures (Intel vs AMD)?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-14",
        "questionId": "q-29688",
        "question": "Design a NUMA-aware per-core memory pool allocator for a high-throughput NIC path. Provide API allocate(size) and free(ptr), ensure 64-byte block alignment, per-core caches, and lock-free cross-core freelists; include a strategy to rebalance arenas under contention and to reclaim remotely freed blocks. Give minimal pseudo-code for allocate and free and outline a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a lock-free, crash-consistent ring buffer in NVRAM. Use MPMC with power-of-two size; per-slot 64-bit sequence counters; atomic head/tail. Enqueue: reserve tail via fetch_add, write data, flu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a fixed-size ring with slots each containing a 64-bit payload and a 64-bit sequence, padded to avoid false sharing. Enqueue: read head, compute next=head+1; if slot[next].seq==head, write pa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-core arenas isolate allocations to minimize cross-NUMA memory traffic. Each core maintains a private 64-byte block freelist; when empty, it atomically fetches a batch from a global pool using CAS, then refills its l…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a crash‑consistent, lock‑free object pool using per‑thread local caches backed by a central persistent free list. The central pool maintains a CAS‑based head pointer to a singly linked free list stored in pers…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests NUMA locality awareness, per-core caches, lock-free freelists, and safe cross-core reclamation in a realistic high-performance allocator.\n\n## Key Concepts\n\n- NUMA topology and memory locality optimization\n- Per-core slab caches for minimal synchronization overhead\n- Lock-free freelists using CAS operations\n- Epoch-based reclamation for safe memory reuse\n- Slab design and fragmentation management\n- Thread/core pinning strategies for consistent affinity\n\n## Code Example\n\n```javascript\nclass Arena {\n  constructor() {\n    this.head = null;\n  }\n}\n\nfunction allocate(size) {\n  // Atomically pop from per-core freelist\n}\n\nfunction free(ptr) {\n  // Enqueue to owning core's arena\n}\n```\n\n## Microbenchmark Plan\n\n1. **Baseline metrics**: Measure raw allocation latency per thread with no contention\n2. **NUMA scalability test**: Vary thread count across sockets, measure remote vs. local access latency\n3. **Rebalancing test**: Inject high contention, measure throughput recovery after rebalance\n4. **Remote reclamation test**: Free from non-owning cores, measure reclaim latency and correctness\n5. **Cache line bouncing**: Instrument false sharing to detect cross-core cache line migration",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-15",
        "questionId": "q-30538",
        "question": "Implement a lock-free slab allocator on a fixed 2MB arena consisting of 64-byte blocks. Use a single atomic 64-bit head where lower 32 bits hold the index of the first free block and upper 32 bits hold a version counter to mitigate ABA. Each free block stores the next-free index. Provide C11 code for init, alloc, and free, including alignment and error handling, and a small test where 8 threads perform interleaved alloc/free of blocks. Explain memory ordering choices and ABA mitigation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-thread fixed-size object pool in C for 64-byte objects. Each thread maintains a local free-list; when empty, refills from a global pool via a lock-free stack or a light spin-lock. APIs",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a lock-free free-list encoded in a 64-bit head: lower 32 bits for the head block index, upper 32 bits for a version counter to defeat ABA. Initialize the arena into a singly linked free list. allo",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a fixed-size, open-addressing hash table with per-slot version counters and a tombstone flag. Use CAS to insert, validate version on finds, and mark tombstones on delete, incrementing the ve",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a NUMA-aware allocator with per-thread caches for <=128-byte objects, backed by a central pool on a designated NUMA node. API: alloc/free; layout: per-thread freelists + central pool; sync via",
            "isCorrect": false
          }
        ],
        "explanation": "## How It Works\n\nA 2MB arena is divided into 64-byte blocks, yielding 32768 blocks. The free list is a single atomic head packed into a 64-bit value: [ version (upper 32 bits) | head index (lower 32 bits) ]. A free block stores the index of the next free block. On init, all blocks form a chain starting at 0, ending with a sentinel (0xFFFFFFFF).\n\nThe allocator uses a lock-free approach:\n- Alloc: read the head with acquire, compute the next pointer, CAS to update head to next and increment version. Return the block corresponding to the popped index.\n- Free: write the freed block’s next pointer to current head, CAS to push it back and increment version.\n\nABA is mitigated by the version counter; each successful CAS increments the version, so a stale head cannot be mistaken for a newer one.\n\nMemory order choices:\n- Loads use memory_order_acquire to synchronize with the producer/consumer.\n- Stores use memory_order_release or memory_order_acq_rel to ensure visibility of the updated head and block linkage.\n\n### Key Points\n- **64-bit head encoding**: version guards against ABA by differentiating sequential head values.\n- **Block linkage stored in arena**: next indices reside in the block headers themselves.\n- **Initialization**: pre-link 32768 blocks and set head to 0 with version 1.\n- **Threading model**: works with many producers/consumers with minimal contention due to CAS on a single head.\n\n### Example\n```c\n#include <stdatomic.h>\n#include <stdint.h>\n#include <stddef.h>\n\n#define ARENA_SIZE (2*1024*1024)\n#define BLOCK_SIZE 64\n#define BLOCKS (ARENA_SIZE / BLOCK_SIZE)\n\ntypedef struct { uint32_t next; } Block;\nstatic uint8_t arena[ARENA_SIZE];\nstatic _Atomic uint64_t head; // [version:upper32 | index:lower32]\n\nstatic inline void init_slab(void) {\n  for (uint32_t i = 0; i < BLOCKS-1; ++i) ((Block*)arena)[i].next = i+1;\n  ((Block*)arena)[BLOCKS-1].next = 0xFFFFFFFFu;\n  atomic_store_explicit(&head, ((uint64_t)1 << 32) | 0u, memory_order_relaxed);\n}\n\nvoid* slab_alloc(void) {\n  uint64_t oldv, newv;\n  uint32_t idx, ver, next;\n  while (1) {\n    oldv = atomic_load_explicit(&head, memory_order_acquire);\n    idx = (uint32_t)(oldv & 0xFFFFFFFFu);\n    ver = (uint32_t)(oldv >> 32);\n    if (idx == 0xFFFFFFFFu) return NULL;\n    next = ((Block*)arena)[idx].next;\n    newv = ((uint64_t)(ver+1) << 32) | (uint64_t)next;\n    if (atomic_compare_exchange_weak_explicit(&head, &oldv, newv,\n                                            memory_order_acquire, memory_order_relaxed))\n      return (void*)(arena + (size_t)idx * BLOCK_SIZE);\n  }\n}\n\nvoid slab_free(void* p) {\n  if (!p) return;\n  uint32_t idx = ((uint8_t*)p - arena) / BLOCK_SIZE;\n  uint64_t oldv, newv;\n  while (1) {\n    oldv = atomic_load_explicit(&head, memory_order_acquire);\n    uint32_t ver = (uint32_t)(oldv >> 32);\n    ((Block*)arena)[idx].next = (uint32_t)(oldv & 0xFFFFFFFFu);\n    newv = ((uint64_t)(ver+1) << 32) | idx;\n    if (atomic_compare_exchange_weak_explicit(&head, &oldv, newv,\n                                            memory_order_release, memory_order_relaxed))\n      break;\n  }\n}\n\n/* Simple multithreaded test omitted for brevity; see test harness in real usage */\n```\n\n## Diagram\n\nflowchart TD\n  A[Initialize arena] --> B[Link free blocks]\n  B --> C[Alloc path: CAS pop head]\n  C --> D[Return pointer]\n  B --> E[Free path: CAS push block onto head]\n  E --> F[Head updated]",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-16",
        "questionId": "q-8072",
        "question": "In a latency-sensitive service on a 2-socket server, design a NUMA-aware allocator for 64-byte small objects. Use per-node free lists, ABA-free cross-node steals, and a global refill policy. Provide API prototypes and pseudo-code for the fast path, and outline a microbenchmark plan to compare throughput and fragmentation against malloc under high contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a fixed-size block allocator: preallocate arena (64KB) of 64-byte blocks. Use the head of a free-list stored in the arena; each free block stores a next pointer. Allocation uses lock-free CA",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-thread caches per size class, a NUMA-aware central pool, and epoch-based reclamation to avoid cross-thread frees. Slabs align to 64-byte lines; pad to prevent false sharing. Lock-free central pool",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Lockless Cross-Thread Config Exchange Design ### Data Structures ```c #define BUFFER_SIZE 64 typedef struct { uint8_t data[BUFFER_SIZE]; uint32_t crc; } ConfigBuffer; typedef struct { ConfigBuffer buffers[2]; _Atomic…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-node pools of 64-byte blocks with cache-line alignment; lock-free free lists using atomic pointers and pointer tagging to prevent ABA; local fast path, cross-node steal queues, and batch refills t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical NUMA-aware memory management, per-node caching, and ABA-safe freelists under high concurrency.\n\n## Key Concepts\n\n- NUMA affinity and cache locality\n- Lock-free free lists and ABA avoidance\n- Cross-node stealing and refill policy\n- Cache-line padding and fragmentation\n\n## Code Example\n\n```c\ntypedef struct Node { void* next; } Node;\nvoid* sa_alloc(void);\nvoid sa_free(void*);\n```\n\n## Follow-up Questions\n\n- How scale to 4+ sockets?\n- How would you measure long-term fragmentation and allocator throughput?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-17",
        "questionId": "q-826",
        "question": "Design and implement a tiny spinlock in C11 for a shared memory region. Provide lock() and unlock() using stdatomic.h primitives, ensuring memory_order_acquire on successful lock and memory_order_release on unlock. Include a minimal two-thread test contending for the lock and explain your backoff/yield strategy and fairness limitations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a NUMA-aware slab allocator with per-thread caches to optimize memory locality on multi-socket systems. The design maintains separate slab pools for each NUMA node, with thread-local free lists providing fast…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "```c #include <stdatomic.h> #include <sched.h> typedef struct { atomic_flag f; } spinlock_t; #define SPINLOCK_INIT { ATOMIC_FLAG_INIT } static inline void spin_lock(spinlock_t *s){ while (atomic_f",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use per-core local queues (128-byte slots, size 1<<12). Producers write data to per-core buffers, then publish a compact descriptor to a shared ring of N=1<<18 guarded with atomic tail/head counters that increment monot…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement EBR with a global epoch, per-thread observed epochs, and per-thread retire lists. retire_node enqueues to a thread-local list tagged with retirement_epoch; advance_epoch advances global_epoc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical use of C11 atomics, memory ordering, and contention handling in low-level code. ## Key Concepts\n- C11 atomic primitives (atomic_flag)\n- memory_order_acquire/release semantics\n- busy-wait with backoff/yield to reduce bus traffic\n- fairness and starvation considerations ## Code Example\n```c\n// above code snippet\n```\n## Follow-up Questions\n- How would you extend this to a fair queueing spinlock?\n- What benchmarks would you run to profile contention impact?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-18",
        "questionId": "q-19250",
        "question": "Design a lock-free, bounded, cross-thread memory allocator for small objects (sizes 16/32/64 bytes) in a shared 2-socket NUMA region. Use per-thread local caches and a global pool; implement allocate(size) and free(ptr) with a CAS-based refill and hazard-pointer-based reclamation to avoid ABA and use-after-free. Describe data structures (free lists, slab descriptors), fragmentation handling, and a microbenchmark plan comparing against a naive allocator under high-concurrency workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-thread caches store fixed-size slabs; central free-list per size class is a lock-free ring; on allocate, try thread cache, else fetch from central, and on free, push to thread cache or central if",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose and implement a lock-free bounded allocator with multiple size classes (16/32/64 bytes). Each thread maintains a local free-list; a shared global pool refills via CAS when local is empty. Haza",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use an atomic_flag-based spinlock. init: atomic_flag_clear(lock); lock: while (atomic_flag_test_and_set_explicit(lock, memory_order_acquire)) { _mm_pause(); } unlock: atomic_flag_clear_explicit(lock,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I’d implement thread-local freelists with 64-byte blocks, aligned to 64 bytes. Each thread has a private pool; on empty, refill from a shared global pool via a lock-free CAS on the head. Allocation po",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests hands-on understanding of low-level memory management under contention: lock-free design, ABA-free reclamation, and NUMA-aware locality.\n\n## Key Concepts\n- Hazard pointers or epoch-based reclamation for UAF\n- Size-class free lists and per-thread caches\n- ABA avoidance in lock-free paths\n- CAS-based global pool refills\n- NUMA locality and cache-line alignment to minimize false sharing\n\n## Code Example\n```cpp\n// Pseudo-code: allocate and free with size classes 16/32/64\nstruct Block { Block* next; };\nclass Allocator {\n  std::atomic<Block*> globalFree[3];\n  thread_local Block* localFree[3];\n  int size_to_index(size_t s);\n  void* allocate(size_t sz); // uses localFree, spills to globalFree with CAS\n  void free(void* p, size_t sz); // pushes to localFree, retires via hazard pointers\n};\n``` \n\n## Follow-up Questions\n- How would you measure fragmentation and reclaim efficiency under churn?\n- How would you adapt this for true multi-socket coherence with high core counts?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-19",
        "questionId": "q-20425",
        "question": "On a 2-socket NUMA server, design a scalable, lock-free per-core object pool for fixed-size blocks (64B) that supports fast thread-local allocations and cross-thread reclamation without global locks. Provide API prototypes, minimal C-like pseudo-code, an ABA-avoidance strategy, and a plan to benchmark latency under mixed contention. How would you handle reclamation across cores?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "An ideal solution uses a 128-bit head state (pointer+tag) for the freelist and 64-byte aligned slots. Allocate: load head, read slot, CAS head to head.next with a bumped tag; on success, place the obj",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Allocate per-core, cache-aligned freelists of 64B blocks; keep a small per-core cache (e.g., 128 blocks). On allocate, pop from local freelist; on miss, batch-steal from a neighboring core or fetch fr",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a bounded MPMC queue on a power-of-two ring, per-slot length field and a sequence number to avoid ABA. Pad indices to cache lines; publish slot content with release stores and consume with acquire",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a PMEM-backed MPSC (Multi-Producer Single-Consumer) log with per-record headers containing: a monotonically increasing sequence number (seq), payload length (len), and a CRC-32C checksum. Producers atomically inc…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design lock-free, cache-friendly allocators with cross-core reclamation and NUMA awareness, plus practical benchmarking plans.\n\n## Key Concepts\n\n- Lock-free local freelists with cross-core drain\n- ABA avoidance via pointer tagging or tagged pointers\n- Epoch-based reclamation vs hazard pointers\n- Cache-line alignment and false sharing avoidance\n- NUMA locality and cross-socket contention\n\n## Code Example\n\n```javascript\n// Pseudo-C like structures for illustration\nclass Block {\n  constructor() { this.next = null; this.data = new Uint8Array(64); }\n}\nclass LocalPool {\n  constructor() { this.head = null; this.size = 0; }\n  allocate() { /* pop from local freelist atomically */ }\n  free(b) { /* push back, with local buffering */ }\n}\n```\n\n## Follow-up Questions\n\n- How would you prove liveness and bounded wait under high contention?\n- What are NUMA-aware strategies for cross-core reclamation and memory bandwidth pressure?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-20",
        "questionId": "q-19664",
        "question": "Design and implement a simple reusable barrier for N threads in C11. The barrier should block arriving threads until all N threads have reached it, then release them all to continue. Provide init and wait APIs, and discuss memory ordering and performance on cache-coherent CPUs. Include timeout handling and fairness considerations. Show your approach in pseudo-code?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a sense-reversal barrier with two atomics: `count` and `generation`. Each arriving thread performs an atomic fetch-and-increment on `count`. When the count reaches N, the last thread resets `count` to 0, toggles the…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a slab allocator with per-thread caches. Each block is 64-byte aligned. Global free-list uses an atomic stack of indices; each thread keeps a local cache of 4 blocks. Acquire: if cache empty, p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a 1MB fixed-size pool of 64-byte blocks with a lock-free freelist. Each free block reuses its first word as the next pointer; the pool head is an atomic pointer. Allocate pops the head via CAS",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use atomic_fetch_add_explicit for a per-thread ticket; spin on owner until it equals my_ticket; store my_ticket in thread-local storage; on unlock, atomic_store_explicit(&owner, my_ticket+1, memory_or",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests understanding of low-level synchronization primitives, atomic memory ordering semantics, and how barriers affect performance on modern cache-coherent CPUs.\n\n## Key Concepts\n\n- **Sense-reversal technique**: Allows barrier reuse by toggling a generation counter instead of creating new barrier objects\n- **Atomic operations**: Use C11 `_Atomic` types with appropriate memory order primitives (`memory_order_acquire`, `memory_order_release`)\n- **Spin-wait optimization**: Use CPU pause hints (`_mm_pause()`) to reduce memory contention and power consumption during busy-waiting\n- **Memory ordering**: The arriving thread must use `memory_order_release` when updating the generation to ensure all prior writes are visible to waking threads; waiting threads use `memory_order_acquire` to synchronize with the releaser\n- **Cache coherency**: On cache-coherent CPUs, the coherence protocol (e.g., MESI) ensures atomic updates propagate across cores, though false sharing can degrade performance—padding the atomic variables helps\n- **Timeout handling**: Use `memory_order_relaxed` for the counter increment, then check timeout while spinning on generation; return an error code if timeout expires before the barrier releases\n- **Fairness**: Threads are released simultaneously but may resume execution at slightly different times due to scheduling; no strict ordering is guaranteed\n\n## Pseudo-Code Implementation\n\n```c\ntypedef struct {\n    _Atomic int count;\n    _Atomic int generation;\n    int N;\n} barrier_t;\n\nvoid barrier_init(barrier_t *b, int N) {\n    b->N = N;\n    atomic_init(&b->count, 0);\n    atomic_init(&b->generation, 0);\n}\n\nint barrier_wait(barrier_t *b) {\n    int my_gen = atomic_load_explicit(&b->generation, memory_order_relaxed);\n    if (atomic_fetch_add_explicit(&b->count, 1, memory_order_acq_rel) == b->N - 1) {\n        atomic_store_explicit(&b->count, 0, memory_order_release);\n        atomic_store_explicit(&b->generation, my_gen + 1, memory_order_release);\n        return 1; // last thread\n    }\n    while (atomic_load_explicit(&b->generation, memory_order_acquire) == my_gen) {\n        _mm_pause(); // avoid memory contention\n    }\n    return 0;\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-21",
        "questionId": "q-3978",
        "question": "Write a cooperative user-space scheduler that preempts green threads using a timer signal. In a single process, implement N coroutines with contexts (ucontext or setjmp/longjmp), maintain a ready queue, and use a per-thread timer (ITIMER_REAL) to trigger SIGALRM, performing yield by swapping to the next context. Include stack guard pages, an async-signal-safe enqueue/dequeue, and discuss race conditions, reentrancy, and performance implications?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a crash-safe, persistent ring buffer in NVRAM for a 2-socket NUMA system with a PCIe NIC. N=1<<18, 128-byte payloads, per-slot 64-bit sequence to avoid ABA. Slot: payload|seq|meta. Enqueue: pub",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a crash-consistent, lock-free queue for persistent memory (NVRAM) with multiple producers and a single consumer. Use per-slot sequence counters, a log-structured commit path, and cache-line al",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Initialize a 64KB arena with an 8-byte head at offset 0 and 32-byte blocks starting at offset 8. Each block uses an 8-byte header to store the next-free index and 24 bytes for payload (8-byte alignmen",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implementation approach: use ucontext to create N contexts with stacks aligned to 16/64 bytes; install a single signal handler for SIGALRM that is async-signal-safe; on timer expiry, save current cont",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical low-level skills: user-space scheduling, context switching, and signal safety. It probes how candidates manage cross-core timing, layout stacks, and handle preemption without kernel help.\n\n## Key Concepts\n- Async-signal-safety and reentrancy in a signal handler\n- Context switching APIs (ucontext vs setjmp/longjmp)\n- Stack guards and guard pages to detect overflows\n- Preemption cost and jitter; worst-case latency\n\n## Code Example\n```javascript\n// Minimal skeleton (C-like) for a preemptive user-space scheduler\n#include <signal.h>\n#include <ucontext.h>\n#include <sys/mman.h>\n#include <unistd.h>\n\n#define N 4\n\nucontext_t ctxs[N], main_ctx;\nint current = 0;\nvoid scheduler_tick(int sig){ /* async-signal-safe yield to next */ }\n```\n\n## Follow-up Questions\n- How would you extend to handle more coroutines than hardware threads while keeping latency predictable?\n- How would you measure and minimize preemption jitter across cores in a dense workload?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-22",
        "questionId": "q-6278",
        "question": "Design a two-level, lock-free timer wheel in C for a high-throughput event scheduler across 8 cores. Outer wheel: milliseconds; inner wheel: microseconds. Each timer stores deadline, callback, and linkage. Enqueue uses thread-local slots and atomic stores; tick advances outer wheel, moves due timers to inner wheel for immediate execution, with epoch-based reclamation. Compare latency and jitter vs a heap queue?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-level timer wheel with outer 1ms buckets and inner 1µs buckets. Timers hold deadline, cb_id, and next pointer. Enqueue places the timer in the target outer bucket via per-thread slot with atomic s",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "A bounded, NVRAM-resident hash map using per-slot version counters to prevent ABA problems, coupled with a compact write-ahead log (WAL) to guarantee durability after power loss. On put, the operation is logged first, f…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Key ideas: a skip-list map with lock-free inserts/finds/deletes. Use hazard pointers for reclamation and marked next pointers for logical deletion. Nodes carry per-level forward pointers, and new node",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a ring with per-slot 64-bit sequence and a persistent head/tail. On enqueue: write payload, write seq+1, flush, advance tail with a fence. On dequeue: read seq, verify, claim, advance head, flush.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a deterministic low-latency timer structure with lock-free synchronization and practical reclamation in a multi-core environment.\n\n## Key Concepts\n- Timer wheels, two-level design\n- Lock-free synchronization, atomic ops\n- Epoch-based reclamation and hazard pointers equivalent\n- Cache-line and false sharing considerations\n- Microbenchmark methodology\n\n## Code Example\n```c\n// Skeleton definitions\ntypedef struct timer {\n  uint64_t deadline;\n  void (*cb)(void*);\n  struct timer *next;\n} timer_t;\n```\n\n## Follow-up Questions\n- How would you scale to dynamic bucket counts?  \n- How would you support timer cancellation efficiently?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-23",
        "questionId": "q-5058",
        "question": "Design a NUMA-aware slab allocator for 64-byte objects on a 4-core, 2-socket system. Each core maintains a private freelist; allocations preferentially use local slabs; deallocations migrate cross-socket via a per-node buffer for batch transfer. Provide data structures, allocate/free pseudo-code, discuss locking-free design, cache-line alignment, and a microbenchmark plan comparing local vs cross-socket allocations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-core slab allocator: each core maintains a cache-line-aligned freelist for fixed-size objects. Alloc uses local list; if empty, steal a batch (128 objs) from a shared pool using a lock-free CAS/fe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a thread-local freelist backed by a NUMA-aware shared central pool. On allocation, first attempt to pop from the thread-local cache; on a cache miss, fetch a batch of blocks from the central pool (preferably f…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-core freelists with a lock-free stack; slabs 4KB, 64-byte slots; hot path uses thread-local cache; misses refill from NUMA-local pool; remote frees buffered per-node and migrated in bulk to minimi",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Init: pool = 1MB; blocks = 4096; bitmap = 64 64-bit words (one bit per block), all bits 0 (free). alloc(): lock; scan words for a zero bit, set it, unlock; return pool + idx*256. free(p): idx = (p - p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical NUMA locality, lock-free data structures, and cross-socket migration decisions under realistic constraints. It probes memory ordering, cache-line contention, and batch reclamation strategies.\n\n## Key Concepts\n- NUMA locality and cross-socket traffic\n- Lock-free freelists and per-core caches\n- Cache-line padding to avoid false sharing\n- Batch migration of remote frees\n- Memory ordering in hot paths\n\n## Code Example\n```c\n// Pseudo-structure for per-core cache (simplified)\ntypedef struct core_cache {\n  alignas(64) void *head; // atomic head of freelist\n  char padding[60];\n} core_cache_t;\n```\n\n## Follow-up Questions\n- How would you extend for variable-sized objects?\n- How would you measure scalability across more sockets and cores?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-24",
        "questionId": "q-10783",
        "question": "Design a crash-consistent, lock-free ring buffer for a high-throughput network path using non-volatile memory (NVRAM). Implement 1<<20 64-byte slots; use per-slot sequence counters and a separate commit log in a memory-mapped file to survive power loss. Provide slot layout, enqueue/dequeue protocols with fences and flushes, handle wrap-around and backpressure, and outline a crash-injection validation plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Intel TSX to wrap enqueue/dequeue in a small transaction: validate capacity, write payload, advance tail, and commit with _xend. If TSX aborts or capacity constraints are hit, fall back to a lock-free CAS-based path…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-slot sequence counter approach with a crash-consistent protocol. The producer writes payload data, updates the descriptor, sets the sequence counter to ready, then flushes to NVRAM using CLFLUSHOPT follo…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "**One Producer, Multiple Consumers: Fixed Circular Buffer in Shared Memory** A circular buffer for 1:N producer-consumer patterns requires careful synchronization to ensure data integrity. Below is the complete design,…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-core rings: 512 slots, 64 bytes each, with per-slot 64-bit seq; central Aggregator ring drains into a single Consumer. Enqueue uses CAS on seq, then stores payload and updates seq with a release s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nUnderstanding crash-consistent, low-latency data paths is critical for persistent networking infrastructure.\n\n## Key Concepts\n- Non-volatile memory semantics and flush ordering\n- Per-slot sequencing to prevent ABA problems\n- Separate commit log for crash recovery mechanisms\n- Cache-line alignment, CLFLUSHOPT, and SFENCE usage\n- Wrap-around handling and backpressure management\n\n## Code Example\n```c\ntypedef struct {\n  uint64_t seq;\n  uint8_t payload[64];\n} slot_t;\n\ntypedef struct {\n  slot_t slots[1<<20];\n  uint64_t head, tail;\n} ring_t;\n```\n\n## Follow-up Questions\n- How would",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-01T10:40:50.473Z",
    "version": 1
  }
]