[
  {
    "id": "test-low-level",
    "channelId": "low-level",
    "channelName": "LOW Level",
    "title": "LOW Level Knowledge Test",
    "description": "Test your understanding of LOW Level with 25 questions.",
    "questions": [
      {
        "id": "low-level-q-0",
        "questionId": "q-26808",
        "question": "Implement a per-thread fixed-size object pool in C for 64-byte objects. Each thread maintains a local free-list; when empty, refill from a global pool via a lock-free stack or lightweight spin-lock. Provide APIs pool_init(size), pool_alloc(), pool_free(). Ensure 64-byte alignment and a batch refill (e.g., 64 blocks). Explain cross-thread reclamation and how per-thread caches reduce contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a crash‑consistent, multi‑consumer queue on NVM with per‑slot sequence counters. Enqueue: write payload, publish seq=tail, mfence/flush, then tail++. Dequeue: read head; if slot.seq==head, r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-thread fixed-size object pool in C for 64-byte objects. Each thread maintains a local free-list; when empty, refills from a global pool via a lock-free stack or a light spin-lock. APIs",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-thread arenas with size-class bins and per-socket freelists; small allocations served from thread cache; large aligned blocks drawn from a pooled arena with 64B cacheline alignment. Use epoch-base",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "FAST PATH: thread-local freelists for 8/16/32/64 B; allocate from cache, push to freelist on free. SLOW PATH: central pool with lock-free free-list per size class. Use tagged pointers to avoid ABA; al",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of per-thread data locality and simple pool design, avoiding heavy allocators.\n\n## Key Concepts\n- Thread-local storage, free-list, batch refill\n- Global pool with lightweight synchronization\n- Alignment and cache-friendly layouts\n\n## Code Example\n```c\ntypedef struct Block { struct Block *next; char data[64]; } Block;\ntypedef struct Pool { Block *local; _Atomic(Block*) global_head; } Pool;\nvoid pool_init(Pool *p, size_t blocks); \nvoid *pool_alloc(Pool *p); \nvoid pool_free(Pool *p, void *obj);\n```\n\n## Follow-up Questions\n- How would you handle thread termination and reclamation of objects?\n- How would you tune batch size for different workloads?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-1",
        "questionId": "q-800",
        "question": "Design a per-core, lock-free memory allocator for a shared-memory, NUMA-aware object store. Each core maintains a 2 MB local heap; allocations first attempt local allocation, with a fast cross-core path using atomic hand-offs; reclaimed memory is managed via hazard pointers and epoch-based reclamation. Provide allocate/free APIs, a sketch of the free-list structure, and a microbenchmark plan that shows fragmentation under steady-state load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a two-counter ticket lock: next_ticket and now_serving as atomic integers. Lock: my = fetch_add(&next_ticket, 1); while (atomic_load_explicit(&now_serving, memory_order_acquire) != my); Unlock: at",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-core free lists with a bump-pointer local allocator inside a 2 MB heap per core. Align to 64 bytes; local allocations are fast paths, cross-core requests use an atomic hand-off ring to a target co",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Provide a fixed-size pool with 512 blocks of 64B. Use a separate free-list array next[512], initialized to 1.., last=0xFFFF. pool_alloc disables interrupts, pops head, returns base+idx*64. pool_free c",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-thread arenas with size-class bins and per-socket freelists; small allocations served from thread cache; large aligned blocks drawn from a pooled arena with 64B cacheline alignment. Use epoch-base",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests core memory management, concurrency primitives, NUMA awareness, and safe reclamation in high-throughput systems.\n\n## Key Concepts\n- Per-core locality\n- Lock-free reclaim (hazard pointers, epochs)\n- Cross-core handoff patterns\n- Fragmentation and cache-line alignment\n\n## Code Example\n```c\n// Pseudo allocator core logic\ntypedef struct Block Block;\nvoid* alloc(size_t n);\nvoid free(void* p);\n```\n\n## Follow-up Questions\n- How would you measure false sharing?\n- How would you adapt if cores may be hot-swapped or deactivated?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-2",
        "questionId": "q-28101",
        "question": "Design a tiny fixed-size block allocator for a 256-block pool of 64-byte blocks in a shared memory region. Implement thread-safe allocate and free using a lock-free free-list (CAS-based) and a per-block free flag. Explain how you avoid ABA (tag or epoch), how you recycle metadata safely, and provide a minimal multi-thread test plan that detects double-free and corruption?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-core counters: allocate an array blocks[NUM_CORES][4], padded to 64 bytes per core. record_event(core_id, e) { counters[core_id][e]++; } read_total() { sum over all cores and events. Padding p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: crash-consistent KV store with NVRAM write-ahead log and concurrent in-memory index. For put operations, append a log entry with the key-value pair, persist using clwb followed by sfence to ensure durability,…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a PMEM ring buffer with N fixed-size slots stored in persistent memory. Each slot contains a sequence counter and payload. Head and tail pointers are stored in DRAM with periodic durable checkpoints to persistent…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a lock-free free-list allocator: keep a head pointer to a singly linked list of free blocks stored in the pool; allocate by CAS-swapping head with head->next, freeing by pushing the block on",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nA practical test of low-level concurrency in a tiny allocator, exposing understanding of lock-free data structures, ABA avoidance, memory reclamation, and correctness across threads.\n\n## Key Concepts\n\n- Lock-free free-list using CAS\n- ABA avoidance (tag counters or epoch)\n- Fixed-size pool management and memory reclamation\n- Debug/test patterns for multithreaded correctness\n\n## Code Example\n\n```c\ntypedef struct Block { struct Block *next; } Block;\n\ntypedef struct Pool {\n  Block *head;\n  unsigned int tag;\n} Pool;\n\nBlock *allocate(Pool *p) {\n  Block *old = p->head;\n  while (old) {\n    Block *next = old->next;\n    if (__atomic_compare_exchange_n(&p->head, &old, next, 0, __ATOMIC_SEQ_CST, __ATOMIC_SEQ_CST)) {\n      return old;\n    }\n  }\n  return NULL;\n}\nvoid free_block(Pool *p, Block *b) {\n  Block *old;\n  do {\n    old = p->head;\n    b->next = old;\n  } while (!__atomic_compare_exchange_n(&p->head, &old, b, 0, __ATOMIC_SEQ_CST, __ATOMIC_SEQ_CST));\n}\n```\n\n## Follow-up Questions\n\n- How would you extend to variable-sized blocks or per-thread caches?\n- How would you verify correctness with sanitizers and stress tests?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-3",
        "questionId": "q-9638",
        "question": "Design a high-resolution, lock-free event timer for a multithreaded profiler on x86-64. API: timer_start(id) and timer_stop(id) for up to 256 concurrent events. Use RDTSCP with serialization to read stable timestamps, maintain a 256-entry table with total_ns, start_ts, and last_cpu hints. Support CPU migrations and concurrent starts/stops without locks. Provide prototypes and a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a ring of 4096 slots; each slot has a 64-byte payload and a 1-bit ready flag. The producer atomically increments a head counter, computes index = head & (N-1), writes the payload, then sets READY",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Read timestamps with RDTSCP after a CPUID serialize; at timer_start, store start_ts in a per-id slot atomically and mark active; on timer_stop, read end_ts, delta = end - start, add to total_ns using",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I’d implement thread-local freelists with 64-byte blocks, aligned to 64 bytes. Each thread has a private pool; on empty, refill from a shared global pool via a lock-free CAS on the head. Allocation po",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Envision per-thread ring with capacity N; threads CAS‑take a slot to enqueue; flush_to_global batches slots into a global sink via an atomic swap of a filled block. Use per-slot sequence counters to a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a low-overhead, lock-free timing primitive, handle CPU migration, and reason about timing accuracy in profiling workloads.\n\n## Key Concepts\n- RDTSCP serialization\n- Lock-free per-id timers\n- TSC frequency calibration\n- Migration awareness\n\n## Code Example\n```c\nstatic inline uint64_t read_tsc(void) { /* implementation depends on arch */ }\n```\n\n## Follow-up Questions\n- How would you extend for nested timers or different clock frequencies?\n- How would you ensure correctness under hyper-threading and CPU migration?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-4",
        "questionId": "q-18120",
        "question": "Design a per-core, lock-free work-dispatch table on a NUMA machine: each core owns a fixed-size circular queue; producers enqueue to their local queue via a CAS on tail, publish a 64-byte payload, then publish a header update; workers dequeue from local head using an acquire, and when empty steal a slot from a random core using a versioned header to prevent ABA. Provide data structures and enqueue/dequeue prototypes in C, ensure correctness under contention and NUMA effects, and propose a microbenchmark that scales with core count and measures throughput and tail latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-core freelists with 128-byte alignment and a shared pool. Allocate from the local list; on miss, pop a batch (e.g., 64 objects) from a global lock-free stack using a tagged CAS to avoid ABA; p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-core, lock-free work queues on a NUMA machine. Each core owns a circular queue; producers enqueue to local tail with CAS, publish payload, then update header via release; workers dequeue from loca",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a crash-consistent, persistent ring buffer for NVRAM with multi-producer, multi-consumer access. Use 1<<18 slots, 128-byte payloads. ABA avoided via per-slot sequence numbers; two-phase publis",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Detect overlap and copy direction (src < dst ? backward : forward). Use AVX2 to copy 32/64-byte chunks, aligning when possible. For blocks above 256B, employ non-temporal stores to minimize cache poll",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests lock-free data structures, ABA avoidance, NUMA locality, and memory-order reasoning under contention.\n\n## Key Concepts\n\n- Lock-free CAS and memory_order semantics\n- ABA prevention with versioned headers\n- NUMA-aware design and cache-line padding\n- False sharing and backoff strategies\n\n## Code Example\n\n```c\ntypedef struct {\n  uint32_t head;\n  uint32_t tail;\n  // payload slot aligned to cache line, plus header\n} ring_t;\n\n// Prototypes\nvoid enqueue(ring_t *r, void *payload);\nvoid *dequeue(ring_t *r);\n```\n\n## Follow-up Questions\n\n- How would you prove correctness formally or with tests?\n- How would you adapt the design for hundreds of cores?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-5",
        "questionId": "q-10783",
        "question": "Design a crash-consistent, lock-free ring buffer for a high-throughput network path using non-volatile memory (NVRAM). Implement 1<<20 64-byte slots; use per-slot sequence counters and a separate commit log in a memory-mapped file to survive power loss. Provide slot layout, enqueue/dequeue protocols with fences and flushes, handle wrap-around and backpressure, and outline a crash-injection validation plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-slot sequence counter approach with a crash-consistent protocol. The producer writes payload data, updates the descriptor, sets the sequence counter to ready, then flushes to NVRAM using CLFLUSHOPT follo…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a fixed-size, thread-local arena allocator with per-thread pools for small size classes (8,16,32,64,128,256). Allocate by rounding up to class, pop from thread pool, else fall back to a lock-fr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Key design highlights: per-thread arenas with local caches, size-class slabs, a central lock-free free-list for cross-thread returns, and NUMA-aware local allocations by preferring local nodes. Mainta",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a sequence-lock guarded read path for a 128-bit config updated by a single writer. Use a 64-bit version counter V. Writer: V++ (odd), write cfg, V++ (even). Reader: loop: s = V; if (s&1) ret",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nUnderstanding crash-consistent, low-latency data paths is critical for persistent networking infrastructure.\n\n## Key Concepts\n- Non-volatile memory semantics and flush ordering\n- Per-slot sequencing to prevent ABA problems\n- Separate commit log for crash recovery mechanisms\n- Cache-line alignment, CLFLUSHOPT, and SFENCE usage\n- Wrap-around handling and backpressure management\n\n## Code Example\n```c\ntypedef struct {\n  uint64_t seq;\n  uint8_t payload[64];\n} slot_t;\n\ntypedef struct {\n  slot_t slots[1<<20];\n  uint64_t head, tail;\n} ring_t;\n```\n\n## Follow-up Questions\n- How would",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-6",
        "questionId": "q-21713",
        "question": "Design a per-thread, lock-free slab allocator for fixed-size objects (32/64/128/256 bytes) on a 2-socket x86-64 system. Requirements: per-thread caches with a free-list per size, a global lock-free pool for cross-thread transfers, ABA-free allocation via pointer tagging, and epoch-based reclamation for safe reuse. Provide API prototypes and minimal pseudo-code, discuss fragmentation, cache alignment, and propose a microbenchmark to measure latency under mixed allocation/free patterns and NUMA skew?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-thread fast path for Pin/Unpin, a global atomic refcount, and an epoch-based retire queue with hazard pointers. Pin increments a per-thread local counter and a global refcount; Unpin decrements an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a four-size-class slab allocator supporting 32, 64, 128, and 256-byte objects. Each thread maintains per-size free lists. On a cache miss, fetch a full slab from a global lock-free pool using a compare-and-swa…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-core freelists with 4KB slabs; objects ≤64B aligned to 8B; alloc pops from local freelist via CAS; on empty, fetch a slab from a global lock-free pool. Free pushes into per-core cache, periodicall",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a thread-local freelist backed by a NUMA-aware shared central pool. On allocation, first attempt to pop from the thread-local cache; on a cache miss, fetch a batch of blocks from the central pool (preferably f…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question assesses the ability to design a low-latency, cache-friendly memory allocator with cross-thread reclamation. It evaluates understanding of ABA avoidance, per-size caches, and NUMA implications on modern multi-socket systems.\n\n## Key Concepts\n\n- **Lock-free, per-size-class slab allocator** with thread-local caches to minimize synchronization overhead\n- **Cross-thread transfer** via a global lock-free pool enabling efficient memory sharing between threads\n- **ABA avoidance** using pointer tagging (e.g., utilizing low bits as a collision counter)\n- **Epoch-based reclamation** versus hazard pointers for safe memory reuse\n- **Cache-line alignment** to prevent false sharing and control fragmentation\n\n## Code Example\n\n```c\ntypedef struct {\n    void *next;           // Free list pointer\n    uintptr_t tag;        // ABA prevention counter\n} slab_object_t;\n\ntypedef struct {\n    void *slabs;          // Pointer to slab pages\n    _Atomic(slab_object_t *) free_list[4];  // Per-size free lists\n    _Atomic(uintptr_t) tag;                // Global ABA tag\n} thread_cache_t;\n\ntypedef struct {\n    void *slab;\n    size_t size_class;\n    _Atomic(struct global_pool *) next;\n} global_pool_t;\n\n// Thread-local cache declaration\nthread_local thread_cache_t tls_cache;\n\n// Allocation API\nvoid *slab_alloc(size_t size) {\n    int idx = get_size_class_index(size);\n    slab_object_t *obj = atomic_load(&tls_cache.free_list[idx], memory_order_acquire);\n    \n    while (obj) {\n        slab_object_t *next = obj->next;\n        uintptr_t expected_tag = obj->tag;\n        \n        if (atomic_compare_exchange_weak(&tls_cache.free_list[idx], &next,\n            (slab_object_t *)((uintptr_t)obj->next | ((expected_tag + 1) << 2)),\n            memory_order_release, memory_order_relaxed)) {\n            return obj;\n        }\n    }\n    \n    // Cache miss: fetch from global pool\n    return fetch_from_global_pool(idx);\n}\n\n// Free API\nvoid slab_free(void *ptr, size_t size) {\n    int idx = get_size_class_index(size);\n    slab_object_t *obj = (slab_object_t *)ptr;\n    obj->tag = atomic_fetch_add(&tls_cache.tag, 1) + 1;\n    \n    do {\n        obj->next = tls_cache.free_list[idx];\n    } while (!atomic_compare_exchange_weak(&tls_cache.free_list[idx], &obj->next,\n        obj, memory_order_release, memory_order_relaxed));\n}\n\n// Epoch-based reclamation (pseudocode)\nvoid epoch_reclaim(slab_object_t *obj) {\n    atomic_fetch_add(&current_epoch, 1);\n    if (atomic_load(&retired_count) > THRESHOLD) {\n        // Safely free objects from previous epoch\n        sweep_retired_objects();\n    }\n}\n```\n\n## Performance Considerations\n\n### Fragmentation\n- Internal fragmentation is bounded by the per-size-class design (worst case: ~50% for objects slightly larger than half the slab size)\n- External fragmentation remains minimal since slabs are fixed-size and预先 allocated\n\n### Cache Alignment\n- Align each object to cache line size (64 bytes) or use padding to eliminate false sharing between threads\n- Global pool structures should occupy separate cache lines from thread-local data\n\n### NUMA Awareness\n- On a 2-socket x86-64 system, each socket should maintain a local slab pool\n- Affinitize threads to sockets using `sched_setaffinity()` or platform-specific NUMA APIs\n- Prefetch memory from the local NUMA node to minimize cross-socket memory access latency\n\n## Proposed Microbenchmark\n\n1. **Workload Configuration**: Run 4 producer-consumer thread pairs, each producing and consuming objects of varying sizes (32/64/128/256 bytes) at configurable rates\n\n2. **Metrics to Measure**:\n   - Allocation latency (p50, p99, p999 percentiles)\n   - Free latency\n   - Cache miss rate via performance counters\n   - Cross-NUMA memory transfers\n\n3. **Test Patterns**:\n   - Uniform random allocation/free\n   - Bursty workload (bulk allocations followed by bulk frees)\n   - Strided access patterns to stress NUMA skew\n\n4. **Example Implementation**:\n   ```c\n   void run_benchmark(int num_threads, size_t ops_per_thread) {\n       for (int i = 0; i < num_threads; i++) {\n           pthread_create(&threads[i], NULL, worker, &params[i]);\n       }\n       // Measure latencies using rdtsc() or clock_gettime()\n   }\n   ```",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-7",
        "questionId": "q-10993",
        "question": "Design a NUMA-aware, zero-copy, multi-producer single-consumer logger in user space. Producers enqueue variable-length records into per-core buffers; a single consumer flushes to a file with direct I/O. Use tagged pointers or per-slot sequence counters to avoid ABA, implement epoch-based reclamation for record buffers, and describe a microbenchmark plan that measures throughput and tail latency for small (64B) vs large (4KB) records on a 2-socket server. Provide minimal C-like prototypes and pseudo-code for enqueue/dequeue?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-core ring buffers, fixed-size slots, variable payloads in a shared pool. Enqueue uses CAS on tail; ABA avoided via per-slot sequence counters. Reclaim with epoch-based GC tied to quiescent state.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Crash-consistent NVM ring: use a redo log and per-slot sequence counters. Enqueue: write payload+header, flush header (clwb/flushopt), then advance tail with a release store; Dequeue: fetch, advance h",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I’d implement per-core 4KB slabs with 64B blocks, a lock-free freelist using atomic CAS on a per-slab head, and 64B padding to avoid false sharing. Allocation uses the local arena; frees stay local if",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a lock-free ring with N=1<<18, each slot holding a 128-byte payload and a 64-bit seq. Producers write data, publish with a release-store to the slot and advance a head index; the consumer re",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests low-level synchronization, non-blocking design, and memory reclamation in a realistic IO path.\n\n## Key Concepts\n- Lock-free queues, ABA avoidance, epoch-based reclamation, NUMA awareness, zero-copy paths, O_DIRECT.\n\n## Code Example\n```c\ntypedef struct { /* fields */ } slot_t;\ntypedef struct { /* ring, pool, etc */ } logger_t;\nint enqueue(logger_t*, const void* data, size_t len);\nint dequeue(logger_t*, void** out, size_t* len);\n```\n\n## Follow-up Questions\n- How would you adapt this for multiple consumers?\n- What diagnostics would you add to verify reclamation safety under abrupt termination?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-8",
        "questionId": "q-18973",
        "question": "Design a per-core, small-object slab allocator for an ARM64 server with 6 cores, targeting allocations under 256B. Provide core data structures, alignment to 64 bytes, per-core lock-free free-lists, and a global pool as fallback. Present allocate/free pseudocode using CAS and memory barriers, with size-class bins (32B, 64B, 128B, 256B). Discuss fragmentation, cache aliasing, TLB pressure, and backpressure if the global pool stalls. Include a microbenchmark plan comparing to a naive malloc and measuring latency, throughput, and fragmentation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use 32×32 tiling to keep working set small. For N×N float matrices A,B, loop br/bc in steps of 32 and copy B[c*N + r] = A[r*N + c] with r=br+i, c=bc+j. Add bounds checks for N not multiple of 32, use",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Encode a single 64-bit slot: high 32 bits = sequence, low 32 bits = payload. Init seq=0, payload=0. Sender CASes to publish (seq+1, payload) when current seq matches and payload is 0. Receiver uses ac",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-core caches with 64-byte-aligned blocks in bins 32/64/128/256B. Each core holds an atomic freelist; a global pool refills via CAS on misses. Allocate from the core cache; if empty, refill from glo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a power-of-two ring buffer with per-slot sequence numbers and separate atomic head/tail indices. Each slot has a 64-bit sequence: low 32 bits = turn number, high 32 bits = slot index. Producers: read tail, compute s…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical low-level allocator design, cache locality, and lock-free data structures under NUMA/ARM constraints.\n\n## Key Concepts\n- Per-core freelists and global pool coordination\n- Lock-free CAS-based enqueue/dequeue\n- Size-class design and fragmentation trade-offs\n- Cache-line alignment and NUMA considerations\n\n## Code Example\n```javascript\n// Pseudo-code for allocate\n```\n\n## Follow-up Questions\n- How would you adapt for larger allocations and allocator poisoning protection?\n- What tests would you run to ensure no memory leaks under pressure?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-9",
        "questionId": "q-5466",
        "question": "You are given a microcontroller without an FPU. Implement a tiny 16.16 fixed-point math library for non-negative numbers. Provide fixed_mul and fixed_div that take 32-bit inputs in 16.16 form, perform correct rounding, guard divide-by-zero, and document overflow handling. Include a small test harness and discuss performance on flash/RAM?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-thread fast path for Pin/Unpin, a global atomic refcount, and an epoch-based retire queue with hazard pointers. Pin increments a per-thread local counter and a global refcount; Unpin decrements an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-thread cache of 32 slots; on alloc, pop from local cache; if empty, atomically refill 32 items from a global lock-free free-list using CAS. On free, push to local cache; if local is full, flush the batch to the glob…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a sense-reversal barrier with 64-byte-aligned per-thread slots and a shared generation counter. **barrier_enter:** each thread writes its sense bit to its slot and performs an atomic fetch_add on the arrivals counte…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Multiply: use uint64_t t = (uint64_t)a * (uint64_t)b; return (uint32_t)(t >> 16); Divide: if (b==0) return MAX; uint64_t t = ((uint64_t)a << 16) / b; return (uint32_t)t; Guard overflow by saturating t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests fixed-point fundamentals, overflow/rounding, and testing on resource-constrained MCUs.\n\n## Key Concepts\n- 16.16 representation, 64‑bit intermediates, rounding, saturation.\n- Edge cases: division by zero, overflow, sign handling.\n- Minimal test harness design for corner cases.\n\n## Code Example\n```c\nstatic inline uint32_t fixed_mul(uint32_t a, uint32_t b) {\n  uint64_t t = (uint64_t)a * (uint64_t)b;\n  return (uint32_t)(t >> 16);\n}\nstatic inline uint32_t fixed_div(uint32_t a, uint32_t b) {\n  if (b == 0) return 0xFFFFFFFFu; // saturate\n  uint64_t t = ((uint64_t)a << 16) /",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-10",
        "questionId": "q-22333",
        "question": "Design and implement a fixed-size memory pool for 64-byte blocks within a 4KB arena. Provide C functions allocate_block() and free_block(void*). Use a 64-bit bitmap to track block usage and ensure thread-safety for a multi-threaded program. Include the struct layout, allocation/free pseudo-code (with atomic operations), and a short plan to verify correctness under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a 64-bit atomic bitmap to track 64 blocks in a 4KB arena. Allocate by reading the bitmap, compute free = ~bitmap, pick idx = __builtin_ctzll(free), and CAS to set that bit. Return arena + idx*64 o",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "**Question:** Implement a scalable sense-reversing barrier in C11 for N threads using only atomic primitives. Each thread must call `barrier_wait()`, which returns only after all N threads have arrived, then reuses the…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-thread arenas hold a small freelist of 256 blocks, each 128B aligned to 64B. Allocation pops from the thread-local freelist; if empty, refill from a shared global pool via CAS-based lock-free stac",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a spinlock with atomic_flag. Acquire via while (atomic_flag_test_and_set_explicit(&lock, memory_order_acquire)) { _mm_pause(); } Release with atomic_flag_clear_explicit(&lock, memory_order_r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical low-level memory management with thread-safety using atomic bit ops on a compact bitmap. A beginner-friendly yet realistic task for systems work.\n\n## Key Concepts\n\n- Fixed-size block pools\n- Bitmap-based tracking\n- Atomic CAS and memory order\n- Alignment and cache-friendly layout\n\n## Code Example\n\n```c\n#include <stdatomic.h>\n#include <stddef.h>\n\ntypedef struct {\n  alignas(64) unsigned char arena[4096];\n  atomic_uint64_t used;\n} Pool;\n\nvoid* allocate_block(Pool* p) {\n  uint64_t oldv = atomic_load_explicit(&p->used, memory_order_relaxed);\n  while (1) {\n    uint64_t free = ~oldv;\n    if (!free) return NULL;\n    int idx = __builtin_ctzll(free);\n    uint64_t want = oldv | (1ull << idx);\n    if (atomic_compare_exchange_weak(&p->used, &oldv, want))\n      return (void*)(p->arena + idx * 64);\n  }\n}\n\nvoid free_block(Pool* p, void* ptr) {\n  size_t idx = ((unsigned char*)ptr - p->arena) / 64;\n  uint64_t mask = ~(1ull << idx);\n  atomic_fetch_and_explicit(&p->used, mask, memory_order_release);\n}\n``` \n\n## Follow-up Questions\n\n- How would you detect double-free? \n- How would you extend to multiple arenas or resizing?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-11",
        "questionId": "q-760",
        "question": "On a dual-socket NUMA server, design a cross-NUMA, zero-copy ring buffer for a 40 Gbps path between two processes where producers live on socket A and a consumer on socket B. Use per-slot 64-bit sequence numbers to prevent ABA, 128-byte payload slots, and capacity 1<<18 with 64-byte alignment. Provide slot layout, enqueue/dequeue pseudo-code with memory fences and cache-line padding, wrap-around and backpressure handling, and outline a minimal microbenchmark to validate throughput and data integrity under cross-socket contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a sense-reversing barrier. Maintain an atomic int count and a generation bit sense. Each thread stores a local copy of sense. In wait(), atomically increment count; if count == N, reset count to 0",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed approach: a 16-slot wheel with a shared mutex protecting slot lists for simplicity. add_timer locks the target slot (slot_index = (current_tick + delay_ms) % 16), appends a timer with expiry_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Cross-NUMA, two-process ring for 40 Gbps on a dual-socket server. Slot: 128 bytes; capacity 1<<18; 64-byte alignment; per-slot 64-bit sequence for ABA-avoidance. NUMA-aware allocation on local node. ## Slot Layout ```c…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use four accumulators (a,b,c,d). Loop i+=4; prefetch arr[i+256]; accumulate a+=arr[i], b+=arr[i+1], c+=arr[i+2], d+=arr[i+3]. Finalize by adding a,b,c,d. Ensure arr is 64-byte aligned and compile with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-NUMA IPC design with ABA avoidance, memory ordering, and cache-line discipline for senior systems positions.\n\n## Key Concepts\n- Cross-NUMA memory allocation and cache coherence\n- Per-slot sequencing to prevent ABA problems\n- Acquire/release semantics for proper memory ordering\n- Cache-line padding to avoid false sharing\n- Backpressure handling and wrap-around logic\n- NUMA-aware process binding and memory pinning\n\n## Technical Accuracy\n- 40 Gbps requires ~5GB/s throughput, achievable with this design\n- 64-bit sequence numbers prevent ABA even with wrap-around\n- Cache-line alignment (64 bytes) prevents false sharing\n- Memory fences ensure proper ordering across NUMA domains\n- Fixed 128-byte slots with 112-byte payload meet requirements\n- Capacity 1<<18 (262144 slots) provides 32MB buffer space\n\n## Follow-up Questions\n- How would you extend to N producers and M consumers with fairness guarantees?\n- What failure modes matter under NUMA remapping or node failure?\n- How would you handle variable-sized payloads efficiently?\n- What optimizations for specific CPU architectures (Intel vs AMD)?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-12",
        "questionId": "q-19900",
        "question": "Implement a tiny fixed-size slab allocator in C for 128-byte blocks drawn from a 256 KB pre-allocated pool. The allocator must be thread-safe for multiple producers/consumers, using a single global spinlock for simplicity. Provide init_pool(), alloc_block(), and free_block(). Describe block meta-placement (e.g., store next pointer in freed blocks), alignment guarantees, fragmentation risks, and a minimal stress test plan showing throughput under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a crash-consistent PMEM B+tree with a per-operation redo log. Data layout: persistent nodes with keys and child offsets; a per-thread log tail for redo records. On mutating: append log (op, ke",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a PMEM-backed MPSC (Multi-Producer Single-Consumer) log with per-record headers containing: a monotonically increasing sequence number (seq), payload length (len), and a CRC-32C checksum. Producers atomically inc…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Cross-NUMA, two-process ring for 40 Gbps on a dual-socket server. Slot: 128 bytes; capacity 1<<18; 64-byte alignment; per-slot 64-bit sequence for ABA-avoidance. NUMA-aware allocation on local node. ## Slot Layout ```c…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a 256 KB pool, 2048 blocks of 128 bytes. Implement a global spinlock (atomic_flag) to guard alloc/free. Free blocks link via first pointer word; on alloc, pop from head; on free, push. Align t",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests low-level memory management and basic concurrency without overwhelming complexity.\n\n## Key Concepts\n- Slab allocator basics\n- Thread-safety with a spinlock\n- Simple free-list management and alignment\n- Fragmentation control and sanity checks\n\n## Code Example\n```c\n// minimal sketch\ntypedef struct Block { struct Block* next; } Block;\n#define BLOCK_SIZE 128\n#define POOL_SIZE (256*1024)\nstatic uint8_t pool[POOL_SIZE];\nstatic Block* free_list; static _Atomic int lock = ATOMIC_VAR_INIT(0);\nvoid init_pool(void){ /* link blocks */ }\nvoid* alloc_block(void){ /* lock, pop, return */ }\nvoid free_block(void* p){ /* push back */ }\n```\n\n## Follow-up Questions\n- How would you extend to support variable-sized allocations without fragmentation? \n- What tests ensure correctness under high contention?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-13",
        "questionId": "q-29223",
        "question": "Design a bounded, lock-free ring buffer that supports many producers and a single consumer. Each slot stores a 64-bit payload and a per-slot 64-bit sequence counter to prevent ABA. Provide enqueue() and dequeue() pseudocode, specify memory_order semantics, and explain wrap-around handling and how you avoid false sharing. Include a microbenchmark plan to compare throughput with 2, 4, and 8 producers on a 4-core system?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a fixed-size ring with slots each containing a 64-bit payload and a 64-bit sequence, padded to avoid false sharing. Enqueue: read head, compute next=head+1; if slot[next].seq==head, write pa",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a fixed-size circular buffer with head, tail, and count variables to track queue state. Protect all access with a single mutex and use two condition variables: `not_full` to signal space available and `not_empty` to…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: pre-allocate a 256KB arena of 64-byte blocks; each thread maintains a lock-free free-list (stack) of blocks. allocate pops a block atomically; free pushes it back. Cross-thread frees land in",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a crash-consistent, lock-free queue for persistent memory (NVRAM) with multiple producers and a single consumer. Use per-slot sequence counters, a log-structured commit path, and cache-line al",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of practical lock-free design, memory ordering, and false sharing in a bounded, multi-producer single-consumer context.\n\n## Key Concepts\n- lock-free ring with many producers and a single consumer\n- per-slot sequence counters to prevent ABA\n- cache padding to prevent false sharing\n- memory_order semantics for producer and consumer paths\n\n## Code Example\n```javascript\n// Pseudo-code for enqueue and dequeue\n```\n\n## Follow-up Questions\n- How would you extend to multiple consumers without breaking correctness?\n- How would you validate with microbenchmarks and detect starvation?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-14",
        "questionId": "q-29688",
        "question": "Design a NUMA-aware per-core memory pool allocator for a high-throughput NIC path. Provide API allocate(size) and free(ptr), ensure 64-byte block alignment, per-core caches, and lock-free cross-core freelists; include a strategy to rebalance arenas under contention and to reclaim remotely freed blocks. Give minimal pseudo-code for allocate and free and outline a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a lock-free, bounded hash map with 1<<16 buckets, 64-byte buckets, and per-bucket sequence counters to avoid ABA. Use linear probing and CAS on (seq, key) to insert/find/delete; handle wrap-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose per-core local queues with a producer index in each core, and a central drain queue. Enqueue uses CAS on the local head with a per-slot sequence counter; dequeues use acquire on the drain when",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Explain a per-CPU, lock-free ring buffer design with NUMA awareness. Each CPU i owns ring[i] of N=1<<16, 64B slots and a per-slot 64-bit sequence counter to avoid ABA. Enqueue: atomic fetch_add on hea",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-core arenas isolate allocations to minimize cross-NUMA memory traffic. Each core maintains a private 64-byte block freelist; when empty, it atomically fetches a batch from a global pool using CAS, then refills its l…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests NUMA locality awareness, per-core caches, lock-free freelists, and safe cross-core reclamation in a realistic high-performance allocator.\n\n## Key Concepts\n\n- NUMA topology and memory locality optimization\n- Per-core slab caches for minimal synchronization overhead\n- Lock-free freelists using CAS operations\n- Epoch-based reclamation for safe memory reuse\n- Slab design and fragmentation management\n- Thread/core pinning strategies for consistent affinity\n\n## Code Example\n\n```javascript\nclass Arena {\n  constructor() {\n    this.head = null;\n  }\n}\n\nfunction allocate(size) {\n  // Atomically pop from per-core freelist\n}\n\nfunction free(ptr) {\n  // Enqueue to owning core's arena\n}\n```\n\n## Microbenchmark Plan\n\n1. **Baseline metrics**: Measure raw allocation latency per thread with no contention\n2. **NUMA scalability test**: Vary thread count across sockets, measure remote vs. local access latency\n3. **Rebalancing test**: Inject high contention, measure throughput recovery after rebalance\n4. **Remote reclamation test**: Free from non-owning cores, measure reclaim latency and correctness\n5. **Cache line bouncing**: Instrument false sharing to detect cross-core cache line migration",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-15",
        "questionId": "q-6125",
        "question": "Design a two-level lock-free telemetry pipeline for a multi-socket server: each core has a 512-slot, 64-byte ring buffer with per-slot sequence counters; a central Aggregator ring drains from all cores to a single Consumer that feeds a PCIe DMA buffer. Provide slot layout, enqueue/dequeue pseudo-code, memory fences, wrap-around handling, and backpressure. Explain memory reclamation (epoch/hazard pointers) and discuss cache-line pitfalls. Include a microbenchmark plan for 10+ Gbps bursts?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-core rings: 512 slots, 64 bytes each, with per-slot 64-bit seq; central Aggregator ring drains into a single Consumer. Enqueue uses CAS on seq, then stores payload and updates seq with a release s",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a fixed-size ring (N=1<<16) in shared memory. Each slot stores a 64-bit seq and 64-byte payload. Producers write payload, increment seq, mfence, then CAS head. Consumer spins on tail, valida",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a crash-consistent PMEM allocator for a fixed 1MB arena with 128-byte blocks. Each block includes a header storing `next_offset` and `in_use` metadata. The allocator maintains a persistent global head pointer. Al…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed solution sketch: maintain a ring of N=1<<20 slots, each with a 128-byte payload and a 64-bit sequence. Use atomic head/tail indices and per-slot seq to guard ABA. Enqueue: reserve B slots via",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests two-level lock-free design, cross-socket propagation, and memory reclamation under real-time loads.\n\n## Key Concepts\n- Lock-free rings with per-slot sequence counters\n- Two-level buffering: per-core + aggregator\n- Memory ordering: acquire/release fences, CAS\n- Reclamation: epoch-based or hazard pointers\n- Cache-line locality and false sharing awareness\n- Backpressure handling and wrap-around correctness\n\n## Code Example\n```c\ntypedef struct { uint64_t seq; uint8_t payload[56]; } slot_t;\ntypedef struct { slot_t slots[512]; uint64_t head, tail; } ring_t;\n``` \n\n## Follow-up Questions\n- How would you scale to 1024+ cores while keeping fairness?\n- What padding strategy prevents false sharing in the two-level design?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-16",
        "questionId": "q-3478",
        "question": "Design a small, beginner-friendly spinlock in C to protect a shared 64-bit counter on a multi-core system. Implement init(), lock(), unlock() using C11 atomics (memory_order_acquire for lock, memory_order_release for unlock). Provide a minimal test plan with 8 threads incrementing the counter and a short fairness note. How would you verify correctness under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an atomic_flag-based spinlock. init: atomic_flag_clear(lock); lock: while (atomic_flag_test_and_set_explicit(lock, memory_order_acquire)) { _mm_pause(); } unlock: atomic_flag_clear_explicit(lock,",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I’d implement per-core 4KB slabs with 64B blocks, a lock-free freelist using atomic CAS on a per-slab head, and 64B padding to avoid false sharing. Allocation uses the local arena; frees stay local if",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a fixed N=1<<16, slots with (key, val, seq). Hash(key) -> i = h & (N-1); probe linearly. Insert: CAS on slot.key from EMPTY to key, then store val and bump seq with release. Lookup: read key and s",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Lockless Cross-Thread Config Exchange Design ### Data Structures ```c #define BUFFER_SIZE 64 typedef struct { uint8_t data[BUFFER_SIZE]; uint32_t crc; } ConfigBuffer; typedef struct { ConfigBuffer buffers[2]; _Atomic…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests fundamental low-level synchronization and memory ordering concepts in a realistic micro-optimization context.\n\n## Key Concepts\n\n- Atomic flags and test_and_set semantics\n- memory_order_acquire vs memory_order_release\n- Busy-wait with cpu pause for reduced contention\n- Backoff strategies to avoid cache-line thrashing\n\n## Code Example\n\n```javascript\n#include <stdatomic.h>\n#include <immintrin.h>\n\ntypedef atomic_flag spinlock_t;\nvoid spinlock_init(spinlock_t* s) { atomic_flag_clear(s); }\nvoid spinlock_lock(spinlock_t* s) { while (atomic_flag_test_and_set_explicit(s, memory_order_acquire)) { _mm_pause(); } }\nvoid spinlock_unlock(spinlock_t* s) { atomic_flag_clear_explicit(s, memory_order_release); }\n```\n\n## Follow-up Questions\n\n- How would you modify to minimize starvation?\n- How would you test for fairness across threads under high contention?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-17",
        "questionId": "q-5123",
        "question": "Implement a simple 256-slot software timer wheel in C for a single-threaded event loop. Each slot stores a list of tasks: struct Task { void (*cb)(void*); void* ctx; struct Task* next; }. API: void init_wheel(); void schedule(void (*cb)(void*), void* ctx, uint32_t delay_ms); void tick_one_ms(); The wheel uses wrap-around, fixed pool of 512 nodes, and a per-slot head pointer. Provide enqueue/dequeue pseudo-code, wrap-around handling, and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a 256-slot ring; keep current_slot. schedule maps to slot = (current_slot + delay_ms) & 0xFF; pull a node from a preallocated pool, set cb/ctx, push to front of slot's list. tick executes all node",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan to implement per-slot sequence counters, cache-line aligned slots, and CAS-based enqueues/dequeues with acquire/release semantics. Use a 128-byte descriptor, precompute masks for wrap-around, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-thread bump allocator: store base and offset; alignment: off = (offset + 7) & ~7; if off + sz <= ARENA_SIZE then return (void*)(base + off); offset = off + sz; else NULL. reset() sets offset=0. No",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a PMEM-backed circular log with per-slot sequence counters to avoid ABA. Each slot: header{seq,len,ts} + payload. Producers CAS on next index, write payload, then publish by increasing seq and flu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nLow-level timer logic is common in network and game loops; beginner-friendly but tests memory layout, wrap-around, and basic lock-free thinking without heavy concurrency.\n\n## Key Concepts\n- Ring buffers and wrap-around\n- Pre-allocated object pools\n- Scheduling by slot arithmetic and locality\n- Minimal locking assumptions in single-threaded loop\n\n## Code Example\n```c\ntypedef struct Task { void (*cb)(void*); void* ctx; struct Task* next; } Task;\n#define WHEEL_SIZE 256\nstatic Task* wheel[WHEEL_SIZE];\nstatic int cur = 0;\nstatic Task pool[512]; static int pool_free = 512;\nvoid init_wheel(){ for(int i=0;i<WHEEL_SIZE;i++) wheel[i]=NULL; cur=0; pool_free=512; }\nstatic Task* alloc_task(){ return &pool[--pool_free]; }\nvoid schedule(void (*cb)(void*), void* ctx, uint32_t delay_ms){ int slot = (cur + delay_ms) & (WHEEL_SIZE-1); Task* t = alloc_task(); t->cb=cb; t->ctx=ctx; t->next=wheel[slot]; wheel[slot]=t; }\nvoid tick_one_ms(){ Task* t = wheel[cur]; wheel[cur]=NULL; while(t){ Task* next=t->next; t->cb(t->ctx); t = next; } cur=(cur+1)&(WHEEL_SIZE-1); }\n```\n\n## Follow-up Questions\n- How would you adapt for multiple producers? \n- How would you test with bursty traffic and verify timing guarantees?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-18",
        "questionId": "q-16143",
        "question": "Design a 64-slot ring where each slot is a lock-free stack of timer nodes. Enqueue uses target = (current_tick + delay_ms) & 63 and pushes a node with due_tick; the ticker advances every ms, pops due timers in the current slot, runs callbacks outside the lock, and recycles nodes via an epoch-based pool. If a callback schedules another timer, defer it to the next tick to avoid reentrancy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: Implement a 4KB ring buffer with head and tail indices to track read and write positions. Write incoming data at the tail position, advance the tail pointer, and use newline delimiters to segment complete line…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a crash‑consistent, lock‑free object pool using per‑thread local caches backed by a central persistent free list. The central pool maintains a CAS‑based head pointer to a singly linked free list stored in pers…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a 64-slot ring with per-slot lock-free stacks. Enqueue computes slot = (current_tick + delay_ms) & 63, pushes a timer node with due_tick and callback. Tick advances every ms, drains current sl",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "A two-tier lock-free queue architecture featuring per-core private bounded ring buffers for fast local operations, complemented by a shared global ring buffer for cross-core transfers. Local rings provide the fast path…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes a candidate's grasp of timer wheels, lock-free data structures, and memory reclamation, plus reentrancy handling in callbacks.\n\n## Key Concepts\n- Lock-free stacks and epoch reclamation\n- Slotization and modulo arithmetic\n- Reentrancy safety when scheduling within callbacks\n\n## Code Example\n```pseudo\n// enqueue_timer: compute target slot and push node atomically\n// tick: advance to next slot, execute and reclaim\n```\n\n## Follow-up Questions\n- How would you scale to multiple tick rates? \n- How would you measure jitter and latency under load?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-19",
        "questionId": "q-11726",
        "question": "Design a NUMA-aware, lock-free hash map for a 2-socket system to cache 64-byte records with ultra-low latency. Use open addressing with a power-of-two capacity, 64-byte slots, and per-slot atomic states to avoid ABA. Provide slot layout, insert and get pseudocode with CAS loops, discuss wrap-around, cache-line alignment, and a simple cross-socket reclamation strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Key approach: align each slot to a cache line (64B), store 64-bit key and 64-bit value, plus a 64-bit sequence counter and a flag in a 64-bit header, all padded to 64B. Use CAS-based insert/get with l",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-core free lists with a shared global pool of 256B blocks; blocks and queues cache-line aligned (64B). allocate() pops from local list; if empty, batch-refill from global pool; free() pushes to loc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a bounded lock-free pool: arena of N blocks (128B each). Free-list head is a 64-bit atomic: upper bits = index, lower bits = version. Each block stores next index. alloc_block uses atomic_co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-phase, durable enqueue/dequeue using a circular buffer in NVRAM. On enqueue: write payload, publish via a release-store to head and flush the payload region once; on dequeue: read data, advance ta",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses low-latency concurrency, NUMA awareness, and cache-friendly data structures.\n\n## Key Concepts\n- NUMA locality and cache-line alignment\n- Lock-free open addressing with CAS\n- ABA avoidance via version counters\n- Safe memory reclamation (epoch-based)\n\n## Code Example\n```javascript\n// Pseudocode for a slot\nclass Slot {\n  constructor() {\n    this.key = 0n; // BigInt for 64-bit\n    this.val = 0n;\n    this.seq = 0n;\n  }\n}\n\nfunction slot_insert(slot, k, v) {\n  const cur = atomicLoad(slot.seq);\n  if (cur !== 0n) return false;\n  if (atomicCompareExchange(slot.seq, cur, 1n)) {\n    slot.key = k;\n    slot.val = v;\n    atomicStore(slot.seq, cur + 1n);\n    return true;\n  }\n  return false;\n}\n\nfunction slot_get(slot, k) {\n  const cur = atomicLoad(slot.seq);\n  if ((cur & 1n) === 0n) return { found: false };\n  if (slot.key === k) return { found: true, val: slot.val };\n  return { found: false };\n}\n```\n\n## Follow-up Questions\n- How would you extend to 3 sockets and handle memory bandwidth saturation?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-20",
        "questionId": "q-15233",
        "question": "Design a lock-free, bounded ring buffer for large payloads (256-byte records) shared across two threads on a 2-socket system. Use per-slot 64-bit sequence counters and non-temporal stores for producers; readers use streaming loads and cache-line padding to avoid false sharing. Provide slot layout, push/pop pseudo-code with memory fences, and a microbenchmark plan to measure throughput and latency under mixed contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A lock-free, bounded task queue with work-stealing for a 2-socket x86-64 server. Each worker maintains a private deque with local push and shared steal endpoints. Producers push to their local queue; if full, they fallb…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Each thread maintains thread-local pointers (local_ptr and local_end) to track allocation within its current block. Allocations are performed by incrementing local_ptr within the block boundaries with proper alignment.…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a ring of N=1<<16 slots, each 256 bytes, padded to 256B. Each slot holds a 64-bit seq and a 256-byte payload. Producers write the payload with non-temporal stores, then publish by updating seq wit",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-socket cache-line rings (N=1<<16) for two queues: high and normal. Each slot stores (gen, pri, data_ptr). Producers reserve via atomic fetch_add, publish with release; consumers read with acqu",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests practical low-level concurrency skills: non-temporal stores, false sharing avoidance, NUMA awareness, and correct lock-free sequencing.\n\nKey Concepts\n- Lock-free ring buffers\n- Non-temporal stores\n- Cache-line padding to prevent false sharing\n- Memory ordering: release/acquire, CAS\n- NUMA pinning and microbenchmarking\n\nCode Example\n```cpp\n// Slot and ring sketch (conceptual)\ntypedef struct alignas(256) Slot {\n  uint64_t seq;\n  uint8_t payload[256];\n} Slot;\n\ntypedef struct {\n  Slot *slots;\n  _Atomic uint32_t head, tail;\n} Ring;\n\nbool ring_push(Ring *r, const void *src){\n  uint32_t h = atomic_load_explicit(&r->head, memory_order_relaxed);\n  Slot *s = &r->slots[h & (N-1)];\n  uint64_t seq = atomic_load_explicit(&s->seq, memory_order_acquire);\n  if (seq != h) return false; // full\n  // non-temporal payload write path placeholder\n  memcpy(s->payload, src, 256);\n  atomic_store_explicit(&s->seq, h+1, memory_order_release);\n  atomic_store_explicit(&r->head, h+1, memory_order_relaxed);\n  return true;\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-21",
        "questionId": "q-10583",
        "question": "In a 2-socket x86-64 server, implement a lock-free, bounded MPMC ring that supports online resizing without pausing traffic. Use per-slot sequence counters to avoid ABA and hazard pointers for reclamation. Provide API prototypes (create, enqueue, dequeue, resize) and minimal pseudo-code for enqueue/dequeue; discuss linearizability and contention hazards. Include a microbenchmark plan to test latency during growth/shrink?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan a lock-free bounded MPMC ring with per-slot sequence counters and hazard-pointer reclamation, plus an online resize protocol. Allocate a new table, migrate in-flight ops, publish swap atomically,",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a per-thread allocator for fixed-size 64-byte objects. Each thread stores a local freelist aligned to 64 bytes; allocate pops from local free list, refills from a global lock-free stack when",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a preallocated pool with a head index to the free-list. Initialize by chaining all blocks. Allocate by CAS-pop from head, returning the block; free by writing the block's next index and CAS-",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Initialize a buffer of N blocks (64 bytes each). The first 8 bytes store a next pointer; the remaining 56 bytes are payload. Head points to first free block. allocate() pops head, returns block+8; fre",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nGrowing/shrinking a lock-free ring is a hard correctness challenge. Online resizing tests ability to reason about safety margins, memory reclamation, and progress under contention.\n\n## Key Concepts\n- MPMC lock-free data structure\n- Per-slot sequence counters to avoid ABA\n- Hazard pointers / epoch-based reclamation\n- Online resizing without stopping producers/consumers\n- Linearizability and safe memory ordering\n\n## Code Example\n```javascript\n// Pseudo prototypes\ntypedef struct ring ring_t;\nring_t* ring_create(size_t n);\nbool ring_enqueue(ring_t* r, void* item);\nbool ring_dequeue(ring_t* r, void** item);\nbool ring_resize(ring_t* r, size_t new_n);\n```\n\n## Follow-up Questions\n- How would you detect and recover from mid-operation resize?\n- How would you validate linearizability with randomized workloads?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-22",
        "questionId": "q-21072",
        "question": "Design a cache-friendly memmove variant for 256-byte-aligned buffers that may overlap, handling sizes up to 4096 bytes. Copy in 64-byte chunks, using a forward pass when dst <= src or no overlap and a backward pass when dst > src with overlap. Fall back to memcpy for small or non-critical paths. Provide C code and explain correctness, alignment, and how you'd microbenchmark throughput on a modern x86-64 system?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-path memmove: if dst <= src or no overlap, copy forward in 64-byte steps; if dst > src with overlap, copy backward (start from end) to avoid clobbering source data. Handle tail bytes after 64-byte",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a bounded MPMC queue that avoids contention by partitioning slots into per-producer stripes (e.g., 8 stripes, 1<<10 slots each) and a single shared drain. Each producer atomically reserves a s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a NUMA-aware, lock-free work-stealing deque: each worker has a local circular buffer; thieves steal from tails via CAS on a shared top; per-slot sequence counters avoid ABA; padding to avoid fa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Durable, lock-free SPMC queue backed by persistent memory. Use per-slot sequence counters, cache-line-aligned slots, and a two-phase commit: write payload, flush fence, then publish sequence. Consumer",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of memory layout, overlap handling, and cache-friendly copy strategies in tight, real-world paths.\n\n## Key Concepts\n- Overlap-safe memmove\n- Cache-friendly 64-byte blocks\n- Alignment assumptions and tail handling\n- Fallback to memcpy for tiny or unaligned data\n- Microbenchmark plan to measure throughput vs standard memcpy\n\n## Code Example\n```c\n#include <stddef.h>\n#include <string.h>\n\nvoid *memmove64(void *dst, const void *src, size_t n){\n  unsigned char *d = (unsigned char*)dst;\n  const unsigned char *s = (const unsigned char*)src;\n  const size_t BLOCK = 64;\n\n  if ((size_t)(d) <= (size_t)(s) || d >= s + n){\n    while (n >= BLOCK){\n      memcpy(d, s, BLOCK);\n      d += BLOCK; s += BLOCK; n -= BLOCK;\n    }\n    if (n) memcpy(d, s, n);\n  } else {\n    d += n; s += n;\n    while (n >= BLOCK){\n      d -= BLOCK; s -= BLOCK;\n      memcpy(d, s, BLOCK);\n      n -= BLOCK;\n    }\n    if (n) memcpy(d - n, s - n, n);\n  }\n  return dst;\n}\n```\n\n## Follow-up Questions\n- How would you adapt this for arbitrary alignment and larger data sizes?\n- Compare performance to builtin memmove on x86-64 with different runtimes.\n- What edge cases or architectures could break this approach?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-23",
        "questionId": "q-7711",
        "question": "Design and implement a tiny fixed-size-block memory allocator over a 64KB preallocated pool. Use 64-byte blocks; store the next-free block index in the first 4 bytes of a free block. Provide init_pool(), my_malloc(), and my_free(ptr) in C or C-like syntax. Explain fragmentation handling, alignment guarantees, and behavior when the pool is exhausted. Include a minimal stress test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a tiny fixed-size allocator over 64KB with 1024 blocks of 64 bytes. Use the first 4 bytes of a free block to store the next-free index. init_pool() chains all blocks; my_malloc() pops the he",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a deterministic, lock-free object pool allocator for fixed-size 128-byte objects on a 2-socket x86-64 system with per-thread fast-path free lists, central cross-thread pool, and hazard-pointer-based reclamation w…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Key approach: align each slot to a cache line (64B), store 64-bit key and 64-bit value, plus a 64-bit sequence counter and a flag in a 64-bit header, all padded to 64B. Use CAS-based insert/get with l",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement EBR with a global epoch, per-thread observed epochs, and per-thread retire lists. retire_node enqueues to a thread-local list tagged with retirement_epoch; advance_epoch advances global_epoc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of low‑level memory management, freelists, and fixed-size allocators.\n\n## Key Concepts\n- Fixed-size blocks and simple freelist\n- In-block metadata for next pointer\n- Alignment guarantees and fragmentation considerations\n\n## Code Example\n```javascript\n// JavaScript-simulated fixed-size allocator on an ArrayBuffer\nconst POOL_SIZE = 64 * 1024;\nconst BLOCK_SIZE = 64;\nconst pool = new ArrayBuffer(POOL_SIZE);\nconst nextFree = new Int32Array(pool);\nlet head = 0;\nfunction init_pool(){ const blocks = POOL_SIZE / BLOCK_SIZE; for(let i=0;i<blocks-1;i++){ nextFree[i] = i+1; } nextFree[blocks-1] = -1; head = 0; }\nfunction my_malloc(){ if(head === -1) return null; const idx = head; head = nextFree[idx]; return new DataView(pool, idx*BLOCK_SIZE, BLOCK_SIZE); }\nfunction my_free(ptr){ // ptr is DataView; compute index and push back to head\n  const idx = Math.floor(ptr.byteOffset / BLOCK_SIZE);\n  nextFree[idx] = head; head = idx;\n}\n```\n\n## Follow-up Questions\n- How would you extend to variable sizes? trade-offs?\n- What are the impacts of contention in multi-thread setups?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-24",
        "questionId": "q-14164",
        "question": "Design a cross-process, zero-copy shared hashmap in a fixed-size shared memory region (shm) to support concurrent GET/PUT/DEL across multiple processes on a 2-socket x86-64 server. Use open addressing with linear probing, per-slot sequence counters to detect ABA, and epoch-based reclamation for deletes. Provide API prototypes and minimal pseudo-code, discuss memory ordering, cache-line padding, and NUMA awareness; outline a microbenchmark vs a mutex-protected hashmap?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a bounded, lock-free hash map with N=1<<16 buckets, 64-byte slots, and per-bucket sequence counters to avoid ABA. Use open addressing with linear probing: PUT CAS's an empty slot, GET validates the sequence co…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a bounded MPMC queue on a power-of-two ring, per-slot length field and a sequence number to avoid ABA. Pad indices to cache lines; publish slot content with release stores and consume with acquire",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A practical approach: shared hashmap in shm with fixed capacity; hash to bucket, linear probe; per-slot 64-bit sequence counter to detect ABA; CAS-based updates with strong memory ordering; epoch-base",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a fixed-size allocator using a block header structure with size, next pointer, and free flag. Maintain a global free list head; allocate by searching for the first sufficiently large block, splitting when poss…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExamines cross-process synchronization, lock-free design, and crash-safe memory reclamation in a shared memory layout, directly relevant to ultra-low-latency data stores and high-throughput services.\n\n## Key Concepts\n\n- Shared memory IPC and fixed-capacity hashmaps\n- Open addressing with linear probing\n- ABA avoidance via per-slot sequence counters\n- Epoch-based reclamation for deletes\n- Memory ordering, cache-line padding, and NUMA-awareness\n\n## Code Example\n\n```c\n// Prototype definitions\ntypedef struct { uint64_t key; uint64_t value_offset; uint64_t seq; } slot_t;\ntypedef struct { slot_t* slots; size_t capacity; size_t mask; void* shm_base; } shm_hashmap_t;\n\nint shm_hash_put(shm_hashmap_t* h, uint64_t key, const void* data, size_t len);\nint shm_hash_get(shm_hashmap_t* h, uint64_t key, void** data, size_t* len);\nint shm_hash_del(shm_hashmap_t* h, uint64_t key);\n```\n\n## Follow-up Questions\n\n- How would you resize or migrate to a larger shm region without blocking clients?\n- How do you prove correctness under out-of-order memory writes and cross-core updates?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-15T08:57:01.322Z",
    "version": 1
  }
]