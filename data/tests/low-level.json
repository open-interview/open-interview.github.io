[
  {
    "id": "test-low-level",
    "channelId": "low-level",
    "channelName": "LOW Level",
    "title": "LOW Level Knowledge Test",
    "description": "Test your understanding of LOW Level with 25 questions.",
    "questions": [
      {
        "id": "low-level-q-0",
        "questionId": "q-24980",
        "question": "Design a beginner-friendly per-thread fixed-size object pool in C for 128-byte blocks to reduce malloc churn in a multi-threaded server. Provide API prototypes pool_init, pool_alloc, pool_free; implement a thread-local cache and a small global pool as a fallback; ensure 128-byte alignment and explain how you'd validate correctness and leak-free behavior with a simple multi-thread microbenchmark?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a 4KB circular buffer with per-slot header {uint16_t len; uint64_t seq;}. Maintain atomic head and tail. Writer reserves a slot via CAS on head, copies payload, sets len, and bumps seq to signal d",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: Implement a fixed-size ring buffer in persistent memory with per-slot metadata. Each slot contains a 64-byte payload, an 8-byte sequence counter, and an 8-byte ready flag. The writer writes the payload, advanc…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a crash‑consistent, multi‑producer/multi‑consumer queue in persistent memory (NVM) using a fixed ring with per‑slot sequence counters. API: pmq_create, pmq_open, pmq_enqueue, pmq_dequeue, pm",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-thread TLS cache of 8 blocks; pool_alloc retrieves from TLS cache; on underflow, refills from a global lock-free stack using CAS; pool_free returns to TLS cache or to global pool if TLS is full. All blocks are 128-b…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses understanding of thread-local caches, lock-free fallbacks, memory alignment, and ABA problem mitigation in a practical systems programming context.\n\n## Key Concepts\n- **Thread-local storage (TLS)**: Provides low-latency allocation by keeping memory local to each thread, avoiding contention\n- **Lock-free data structures**: Use compare-and-swap (CAS) operations to safely manipulate shared state without locks\n- **Memory alignment**: Ensures 128-byte aligned blocks; cache line alignment prevents false sharing between threads\n- **ABA avoidance**: Tagged or generational pointers track state changes to prevent the ABA problem in lock-free algorithms\n\n## Code Example\n```c\n// C structures and API prototypes\ntypedef struct block_header {\n    struct block_header *next;\n    uint64_t tag;\n} block_header_t;\n\ntypedef struct pool {\n    block_header_t *global_stack;\n    uint64_t global_tag;\n} pool_t;\n\nint pool_init(pool_t *pool, size_t count);\nvoid *pool_alloc(pool_t *pool);\nvoid pool_free(pool_t *pool, void *ptr);\nvoid pool_destroy(pool_t *pool);\n```",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-1",
        "questionId": "q-29254",
        "question": "Design a zero-copy, lock-free, multi-producer/multi-consumer inter-thread queue for dynamic frames up to 256 bytes on a NUMA system. It must be truly zero-copy by using a pool of preallocated buffers and a shared ring with per-slot headers (size, version, flags). Enqueue reserves a slot with a CAS on head, writes into a free buffer, stores meta, and publishes with release; Dequeue claims via CAS on tail, validates, returns buffer to consumer, and recycles slot. Include API prototypes and minimal pseudo-code; discuss backpressure, memory ordering, and safe reclamation without hazard pointers; propose a microbenchmark?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a ring with per-slot 64-bit sequence and a persistent head/tail. On enqueue: write payload, write seq+1, flush, advance tail with a fence. On dequeue: read seq, verify, claim, advance head, flush.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a log-structured PMEM allocator: append allocations to a durable log, then atomically update a per-block header (version, CRC). Maintain a free-block bitmap and a per-thread cache. Recovery re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a cross-process IPC ring buffer backed by a memory-mapped file with multiple producers and a single consumer. Each 64-byte slot contains an 8-byte atomic sequence number followed by a 56-byte payload. Writers res…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a ring with per-slot versioned headers and a pool of fixed-size 256B buffers. Enqueue reserves a slot with CAS on head, copies into a free buffer, writes size and buffer pointer, then publishes wi",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\\nThis question probes low-level queue design under contention, focusing on zero-copy inter-thread communication and safe memory reclamation on NUMA.\\n\\n## Key Concepts\\n- Zero-copy inter-thread transfers with a fixed buffer pool\\n- Lock-free CAS-based ring with per-slot headers and versioning\\n- Backpressure via header flags and available-slot accounting\\n- Epoch-based reclamation instead of hazard pointers\\n\\n## Code Example\\n```c\\n// Minimal API prototypes\\ntypedef struct { uint64_t head, tail; } mq_t;\\nint mq_enqueue(mq_t* q, void* buf, size_t size);\\nint mq_dequeue(mq_t* q, void** buf, size_t* size);\\n```\\n\\n## Follow-up Questions\\n- How would you implement the epoch-based reclamation in this setting?\\n- How would you microbenchmark latency under varying frame sizes?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-2",
        "questionId": "q-24286",
        "question": "Design a SIMD-accelerated substring search kernel in C that scans a 10 GB log stream on a 2-socket x86-64 server. Use AVX-512 (or AVX2 fallback) to search for a short pattern (16–32 bytes). Handle cross-chunk boundary matches, alignment, and boundary tails. Provide a minimal code skeleton and discuss vector width, misalignment handling, prefetching, and a microbenchmark plan comparing to a scalar baseline under varying pattern locations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use 512-bit SIMD to load 64-byte blocks, broadcast the 16–32 byte pattern across vector lanes, and compare using 8–16 8-byte lanes per cycle. Build an index mask from comparison results and extract match positions. Main…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use an atomic_flag-based spinlock. init: atomic_flag_clear(lock); lock: while (atomic_flag_test_and_set_explicit(lock, memory_order_acquire)) { _mm_pause(); } unlock: atomic_flag_clear_explicit(lock,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Provide a 1<<16, 128-byte per-slot bounded MPMC ring. Slot header: 16B seq/len plus 112B payload. Producers publish with store-release, then advance via CAS; consumers use load-acquire, process, and r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a per-core, bounded publish–subscribe queue: each producer writes to a local ring buffer and publishes to a shared log with per-slot sequence numbers. Use per-slot 64-byte, cache-aligned slots",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests proficiency with vectorized string search, boundary handling for streaming data, and realistic benchmarking on multi-socket servers. It assesses understanding of low-level performance optimization, SIMD instruction sets, and the practical challenges of processing large-scale data streams.\n\n## Key Concepts\n- SIMD vectorization (AVX-512 with AVX2 fallback for older hardware)\n- Cross-chunk boundary handling and tail management for streaming data\n- Mask extraction and index reporting for match positions\n- Cache behavior analysis and prefetching strategy design\n- Multi-socket NUMA awareness and memory bandwidth considerations\n\n## Code Example\n```c\n#include <immintrin.h>\n#include <stdint.h>\n#include <stdlib.h>\n#include <string.h>\n\n#define CHUNK_SIZE 64\n#define TAIL_SIZE 31\n\ntypedef struct {\n    size_t* positions;\n    size_t count;\n    size_t capacity;\n} MatchResults;\n\nstatic inline __m512i broadcast_pattern(const unsigned char* pat, size_t pat_len) {\n    __m512i v = _mm512_setzero_si512();\n    for (size_t i = 0; i < pat_len && i < 64; i++) {\n        v = _mm512_set1_epi8(pat[i]);\n    }\n    return v;\n}\n\nvoid search_simd(const unsigned char* data, size_t n,\n                 const unsigned char* pat, size_t pat_len,\n                 MatchResults* results) {\n    const size_t vec_width = 64;\n    unsigned char tail[TAIL_SIZE] = {0};\n    size_t tail_len = 0;\n    \n    size_t i = 0;\n    for (; i + vec_width <= n; i += vec_width) {\n        __m512i v = _mm512_loadu_si512((__m512i*)(data + i));\n        __m512i p = _mm512_set1_epi8(pat[0]);\n        __m512i cmp = _mm512_cmpeq_epi8(v, p);\n        uint64_t mask = _mm512_mask_reduce_u64(cmp);\n        \n        while (mask) {\n            uint64_t bit = __builtin_ctzll(mask);\n            size_t pos = i + bit;\n            if (pos + pat_len <= n && \n                memcmp(data + pos, pat, pat_len) == 0) {\n                if (results->count < results->capacity) {\n                    results->positions[results->count++] = pos;\n                }\n            }\n            mask &= mask - 1;\n        }\n    }\n    \n    tail_len = n - i;\n    if (tail_len > 0) {\n        memcpy(tail, data + i, tail_len);\n    }\n    for (size_t j = 0; j < pat_len && tail_len + j < CHUNK_SIZE; j++) {\n        tail[tail_len + j] = pat[j];\n    }\n    \n    for (size_t j = 0; j < tail_len; j++) {\n        if (j + pat_len <= tail_len + pat_len &&\n            memcmp(tail + j, pat, pat_len) == 0) {\n            if (results->count < results->capacity) {\n                results->positions[results->count++] = i + j;\n            }\n        }\n    }\n}\n\ndouble benchmark_search(const unsigned char* data, size_t n,\n                       const unsigned char* pat, size_t pat_len,\n                       int use_simd, size_t iterations) {\n    struct timespec start, end;\n    MatchResults results = {\n        .positions = malloc(n * sizeof(size_t)),\n        .count = 0,\n        .capacity = n\n    };\n    \n    clock_gettime(CLOCK_MONOTONIC, &start);\n    for (size_t iter = 0; iter < iterations; iter++) {\n        results.count = 0;\n        if (use_simd) {\n            search_simd(data, n, pat, pat_len, &results);\n        } else {\n            for (size_t i = 0; i + pat_len <= n; i++) {\n                if (memcmp(data + i, pat, pat_len) == 0) {\n                    if (results.count < results.capacity) {\n                        results.positions[results.count++] = i;\n                    }\n                }\n            }\n        }\n    }\n    clock_gettime(CLOCK_MONOTONIC, &end);\n    \n    free(results.positions);\n    return (end.tv_sec - start.tv_sec) + \n           (end.tv_nsec - start.tv_nsec) / 1e9;\n}\n```\n\n## Microbenchmark Plan\n1. **Baseline**: Scalar `memcmp`/byte-by-byte search across full dataset\n2. **Test Cases**:\n   - Pattern at stream start (best case for early exit)\n   - Pattern at stream end (worst case, full scan)\n   - Pattern distributed uniformly (average case)\n   - Pattern absent (negative case)\n3. **Metrics**: Throughput (GB/s), latency per match, cache miss rate\n4. **Variables**: Pattern length (16/32 bytes), server topology effects",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-3",
        "questionId": "q-9638",
        "question": "Design a high-resolution, lock-free event timer for a multithreaded profiler on x86-64. API: timer_start(id) and timer_stop(id) for up to 256 concurrent events. Use RDTSCP with serialization to read stable timestamps, maintain a 256-entry table with total_ns, start_ts, and last_cpu hints. Support CPU migrations and concurrent starts/stops without locks. Provide prototypes and a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Initialize a Block array where each block stores a next index (free-list pointer). Set free_head to 0 and link all blocks in a chain during init_pool. The allocate_block function atomically pops the head using CAS and r…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a PMEM-backed circular log with per-slot sequence counters to avoid ABA. Each slot: header{seq,len,ts} + payload. Producers CAS on next index, write payload, then publish by increasing seq and flu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Read timestamps with RDTSCP after a CPUID serialize; at timer_start, store start_ts in a per-id slot atomically and mark active; on timer_stop, read end_ts, delta = end - start, add to total_ns using",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use atomic_fetch_add_explicit for a per-thread ticket; spin on owner until it equals my_ticket; store my_ticket in thread-local storage; on unlock, atomic_store_explicit(&owner, my_ticket+1, memory_or",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a low-overhead, lock-free timing primitive, handle CPU migration, and reason about timing accuracy in profiling workloads.\n\n## Key Concepts\n- RDTSCP serialization\n- Lock-free per-id timers\n- TSC frequency calibration\n- Migration awareness\n\n## Code Example\n```c\nstatic inline uint64_t read_tsc(void) { /* implementation depends on arch */ }\n```\n\n## Follow-up Questions\n- How would you extend for nested timers or different clock frequencies?\n- How would you ensure correctness under hyper-threading and CPU migration?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-4",
        "questionId": "q-28101",
        "question": "Design a tiny fixed-size block allocator for a 256-block pool of 64-byte blocks in a shared memory region. Implement thread-safe allocate and free using a lock-free free-list (CAS-based) and a per-block free flag. Explain how you avoid ABA (tag or epoch), how you recycle metadata safely, and provide a minimal multi-thread test plan that detects double-free and corruption?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a circular log in PMEM with per-entry header {len, seq, committed}. Enqueue writes payload, then header; flush order with clwb/siws and mfence; consumer validates header and reads payload un",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a lock-free free-list allocator: keep a head pointer to a singly linked list of free blocks stored in the pool; allocate by CAS-swapping head with head->next, freeing by pushing the block on",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Key approach: per-slot fields: seq, data. Prods reserve a slot via CAS on tail, write data, flush data cache line, then publish by updating seq and tail with a durable fence. Consumer reads by parity-",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two writers and one reader share a shm ring buffer with N=1<<16; each slot has a 64-bit seq and 128-byte payload. Producers CAS head, write payload, bump seq, release-store head. Consumer uses acquire",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nA practical test of low-level concurrency in a tiny allocator, exposing understanding of lock-free data structures, ABA avoidance, memory reclamation, and correctness across threads.\n\n## Key Concepts\n\n- Lock-free free-list using CAS\n- ABA avoidance (tag counters or epoch)\n- Fixed-size pool management and memory reclamation\n- Debug/test patterns for multithreaded correctness\n\n## Code Example\n\n```c\ntypedef struct Block { struct Block *next; } Block;\n\ntypedef struct Pool {\n  Block *head;\n  unsigned int tag;\n} Pool;\n\nBlock *allocate(Pool *p) {\n  Block *old = p->head;\n  while (old) {\n    Block *next = old->next;\n    if (__atomic_compare_exchange_n(&p->head, &old, next, 0, __ATOMIC_SEQ_CST, __ATOMIC_SEQ_CST)) {\n      return old;\n    }\n  }\n  return NULL;\n}\nvoid free_block(Pool *p, Block *b) {\n  Block *old;\n  do {\n    old = p->head;\n    b->next = old;\n  } while (!__atomic_compare_exchange_n(&p->head, &old, b, 0, __ATOMIC_SEQ_CST, __ATOMIC_SEQ_CST));\n}\n```\n\n## Follow-up Questions\n\n- How would you extend to variable-sized blocks or per-thread caches?\n- How would you verify correctness with sanitizers and stress tests?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-5",
        "questionId": "q-17536",
        "question": "Design a NUMA-friendly, lock-free allocator for tiny allocations 8-64 bytes on a 4-socket server. Each thread maintains a small, cache-line-aligned per-size cache; a central pool refills caches in batches. Provide API prototypes, discuss avoiding cross-core cache thrashing, memory ordering, and thread-exit cleanup. Include pseudo-code for allocate/deallocate paths and a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use atomic_fetch_add_explicit for a per-thread ticket; spin on owner until it equals my_ticket; store my_ticket in thread-local storage; on unlock, atomic_store_explicit(&owner, my_ticket+1, memory_or",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two atomics: next_ticket and now_serving. Each thread gets t = fetch_add(&next_ticket, 1) and spins until now_serving == t. Unlock sets now_serving = t+1 with release semantics and a thread-local tick",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed solution: four-level hierarchical timer wheel with 64K buckets per level, 128-byte per-slot records {expiry:8, id:8, seq:8, payload:104}. Enqueue computes target level/slot, uses CAS to insta",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-thread caches organized by size class (8, 16, 32, and 64 bytes), each cache line-aligned to prevent false sharing. The allocation path first checks the thread-local cache; on a miss, refill from a central…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates understanding of NUMA-aware memory management, lock-free concurrency patterns, and performance optimization for high-throughput systems. It requires reasoning about cache-line tiling, per-thread caches, batch transfers, memory ordering guarantees, and thread lifecycle management.\n\n## Key Concepts\n\n- **NUMA locality**: Ensuring memory allocations reside on the same NUMA node as the requesting thread to minimize remote memory access latency\n- **Cache-line alignment**: Aligning per-thread data structures to 64-byte cache lines to prevent false sharing between cores\n- **Per-thread caches by size class**: Maintaining separate caches for each allocation size (8, 16, 32, 64 bytes) to reduce fragmentation and improve allocation speed\n- **Lock-free freelists**: Using compare-and-swap (CAS) operations to safely manipulate global free lists without locks\n- **Batch refills**: Transferring multiple blocks at once to amortize synchronization overhead\n- **Memory ordering**: Applying appropriate atomic operations (acquire/release semantics) to ensure visibility across cores\n- **Thread-exit cleanup**: Properly reclaiming thread-local allocations when threads terminate to prevent leaks\n\n## Code Example\n\n```c\n// C-like pseudocode for clarity\ntypedef struct Block {\n    struct Block* next;\n} Block;\n\ntypedef struct PerThreadCache {\n    Block* pools[4];        // 8, 16, 32, 64 byte caches\n    _Atomic int counts[4];  // Cache fill levels\n} PerThreadCache;\n\n// Cache-line-aligned to prevent false sharing\nalignas(64) PerThreadCache thread_caches[MAX_THREADS];\n```\n\n## Allocation Path (Pseudo-code)\n\n```c\nvoid* allocate(size_t size) {\n    int idx = size_to_index(size);\n    PerThreadCache* tc = get_thread_cache();\n    \n    if (tc->counts[idx] > 0) {\n        tc->counts[idx]--;\n        return tc->pools[idx];\n    }\n    // Cache miss: refill from global pool\n    return refill_and_allocate(tc, idx);\n}\n\nvoid* refill_and_allocate(PerThreadCache* tc, int idx) {\n    Block* batch = global_pool_extract(BATCH_SIZE);\n    if (batch == NULL) return NULL;\n    \n    // Push remaining to thread cache\n    tc->pools[idx] = batch->next;\n    tc->counts[idx] = BATCH_SIZE - 1;\n    return batch;\n}\n```\n\n## Deallocation Path (Pseudo-code)\n\n```c\nvoid deallocate(void* ptr, size_t size) {\n    int idx = size_to_index(size);\n    PerThreadCache* tc = get_thread_cache();\n    \n    Block* block = (Block*)ptr;\n    \n    if (tc->counts[idx] < CACHE_CAPACITY) {\n        block->next = tc->pools[idx];\n        tc->pools[idx] = block;\n        tc->counts[idx]++;\n    } else {\n        // Cache full: return excess to global pool\n        return_excess_to_pool(tc, idx);\n        block->next = tc->pools[idx];\n        tc->pools[idx] = block;\n        tc->counts[idx] = 1;\n    }\n}\n```\n\n## Avoiding Cross-Core Cache Thrashing\n\n1. **False sharing prevention**: Each per-thread cache is aligned to a full cache line (64 bytes), ensuring updates from different threads do not share the same cache line\n2. **NUMA-aware allocation**: The central pool maintains per-NUMA-node sub-pools; threads refill from their local node's pool\n3. **Batch transfers**: Moving blocks in bulk reduces the frequency of CAS operations and cache-line traffic\n4. **Thread affinity**: Binding threads to specific cores reduces migration and improves cache locality\n\n## Memory Ordering Considerations\n\n- Use `memory_order_acquire` when reading from the freelist to ensure visibility of the latest data\n- Use `memory_order_release` when returning blocks to the freelist\n- Use `memory_order_relaxed` for simple counters where ordering is not critical\n- The lock-free CAS loop provides implicit ordering through its retry mechanism\n\n## Thread-Exit Cleanup Strategy\n\n1. On thread exit, scan the thread's cache for non-empty pools\n2. Return all available blocks to the global pool using CAS\n3. Use a reference-counting scheme or epoch-based reclamation to safely free blocks that may still be in use by other threads\n4. Register a thread-local destructor that performs cleanup automatically\n\n## Microbenchmark Plan\n\n1. **Baseline comparison**: Measure `malloc`/`free` vs. custom allocator throughput\n2. **Single-threaded throughput**: Allocate/deallocate in a tight loop, measure cycles per operation\n3. **Multi-threaded contention**: Run N threads concurrently, measure throughput and scalability\n4. **NUMA locality test**: Compare performance when threads are bound to local vs. remote NUMA nodes\n5. **Cache behavior analysis**: Use hardware performance counters (L3 misses, false sharing events)\n6. **Allocation size distribution**: Test with varying proportions of each size class\n7. **Thread migration impact**: Measure performance when threads move between cores\n8. **Warmup vs. steady-state**: Compare cold-start performance against warmed caches",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-6",
        "questionId": "q-707",
        "question": "Design a crash‑consistent, multi‑producer/multi‑consumer ring buffer backed by persistent memory PMEM. How would you ensure last enqueued item durability across power loss, implement recovery, and validate correctness? Provide concise enqueue/dequeue pseudocode with proper flush/fence ordering and discuss failure scenarios?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a crash‑safe PMEM ring buffer with two‑phase publish: write the data to the next slot, flush the data, then publish by updating a durable tail with a release store and a fence (sfence/clwb). Use p",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a fixed-size ring with per-slot sequence counters and 64-byte frames. Enqueue reserves via CAS on tail, writes frame, bumps seq, and uses a release store for tail. Dequeue reads head, validates seq, copies frame, an…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two writers and one reader share a shm ring buffer with N=1<<16; each slot has a 64-bit seq and 128-byte payload. Producers CAS head, write payload, bump seq, release-store head. Consumer uses acquire",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "## Data Layout The queue uses a ring buffer structure with N slots, where each slot contains: - **Data payload**: A fixed-size region (or pointer to variable-sized data) for zero-copy access - **Sequence counter**: A 64…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design crash‑consistent data paths in persistent memory, a common production issue when power failures occur during in‑flight operations.\n\n## Key Concepts\n\n- Durability guarantees for PMEM writes\n- Memory ordering: release/acquire, fences, cacheline flushes\n- Recovery via redo log and slot sequence counters\n- ABA avoidance in a concurrent ring\n- Testing with fault injection and crash replay\n\n## Code Example\n\n```c\n// Pseudo-code: enqueue/dequeue with PMEM flush/fence\n// Slot: { data, seq, valid }\nvoid enqueue(T v){\n  size_t t = tail.fetch_add(1);\n  Slot *s = &ring[t % CAP];\n  s->data = v; // write data\n  clwb(&s->data, sizeof(T));\n  fence(); // ensure data persists\n  s->seq = t; // publish with durable tail update (release)\n  clwb(&s->seq, sizeof(size_t));\n  fence();\n  // publish tail already happens via atomic tail update\n}\n\nbool dequeue(T *out){\n  size_t h0 = head.load();\n  Slot *s = &ring[h0 % CAP];\n  if (s->seq != h0) return false;\n  *out = s->data;\n  s->valid = false;\n  clwb(&s->valid, sizeof(bool));\n  fence();\n  head.fetch_add(1);\n  return true;\n}\n```\n\n## Follow-up Questions\n\n- How would you handle power loss during the publish vs data write steps?\n- How would you validate recovery correctness across multiple restart scenarios?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-7",
        "questionId": "q-6946",
        "question": "Design a crash-consistent PMEM allocator for a fixed 1MB arena with 128-byte blocks. Provide API prototypes, a compact layout, and pseudo-code for alloc/free/recover, ensuring crash durability and correct recovery. Discuss a small commit journal, per-block headers, and how to enforce ordering with flushes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a hierarchical NUMA-aware allocator with per-process caches and global coordination. The design consists of three tiers: per-process freelists for small allocations (<128 bytes), per-NUMA-node pools for medium…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a crash-consistent PMEM allocator for a fixed 1MB arena with 128-byte blocks. Each block includes a header storing `next_offset` and `in_use` metadata. The allocator maintains a persistent global head pointer. Al…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use per-core freelists with 128-byte alignment and a shared pool. Allocate from the local list; on miss, pop a batch (e.g., 64 objects) from a global lock-free stack using a tagged CAS to avoid ABA; p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-core freelists with a lock-free stack; slabs 4KB, 64-byte slots; hot path uses thread-local cache; misses refill from NUMA-local pool; remote frees buffered per-node and migrated in bulk to minimi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of crash-safe memory allocators for persistent memory, including durable data structures, memory ordering, and recovery mechanisms.\n\n## Key Concepts\n\n- Persistent memory semantics and flush/barrier instructions\n- Atomic operations (CAS) on a shared free list\n- Crash-recovery journaling and idempotent replay\n- Per-thread caches to reduce contention\n- Memory ordering constraints and cache line alignment\n\n## Code Example\n\n```javascript\n// API usage example demonstrates practical implementation\nconst allocator = new PMEMAllocator(arena);\nconst block = allocator.alloc();  // Returns 128-byte block\nallocator.free(block);           // Returns block to free list\n```\n\n## Follow-up Questions\n\n- How would you extend this design to support variable block sizes?\n- What optimizations would you add for high-concurrency scenarios?\n- How would you handle fragmentation over time?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-8",
        "questionId": "q-19900",
        "question": "Implement a tiny fixed-size slab allocator in C for 128-byte blocks drawn from a 256 KB pre-allocated pool. The allocator must be thread-safe for multiple producers/consumers, using a single global spinlock for simplicity. Provide init_pool(), alloc_block(), and free_block(). Describe block meta-placement (e.g., store next pointer in freed blocks), alignment guarantees, fragmentation risks, and a minimal stress test plan showing throughput under contention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a NUMA-aware slab allocator using 4KB blocks with per-thread caches of 128 blocks each, backed by central memory pools per NUMA node. The metadata structures would be cache-line aligned and padded to p…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a 256 KB pool, 2048 blocks of 128 bytes. Implement a global spinlock (atomic_flag) to guard alloc/free. Free blocks link via first pointer word; on alloc, pop from head; on free, push. Align t",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a NUMA-aware, lock-free allocator optimized for 64-byte objects. Use per-thread freelists to satisfy allocations from a thread-local cache; on cache miss, fetch a slab from a central NUMA-local pool and refill th…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a bounded MPMC queue that avoids contention by partitioning slots into per-producer stripes (e.g., 8 stripes, 1<<10 slots each) and a single shared drain. Each producer atomically reserves a s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests low-level memory management and basic concurrency without overwhelming complexity.\n\n## Key Concepts\n- Slab allocator basics\n- Thread-safety with a spinlock\n- Simple free-list management and alignment\n- Fragmentation control and sanity checks\n\n## Code Example\n```c\n// minimal sketch\ntypedef struct Block { struct Block* next; } Block;\n#define BLOCK_SIZE 128\n#define POOL_SIZE (256*1024)\nstatic uint8_t pool[POOL_SIZE];\nstatic Block* free_list; static _Atomic int lock = ATOMIC_VAR_INIT(0);\nvoid init_pool(void){ /* link blocks */ }\nvoid* alloc_block(void){ /* lock, pop, return */ }\nvoid free_block(void* p){ /* push back */ }\n```\n\n## Follow-up Questions\n- How would you extend to support variable-sized allocations without fragmentation? \n- What tests ensure correctness under high contention?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-9",
        "questionId": "q-8213",
        "question": "Design a NUMA-aware, lock-free memory allocator for 128-byte messages used by a high-throughput inter-thread bus on a 4-socket server. Provide API prototypes, per-node caches, cross-node reclamation, and a plan to measure tail latency under contention. Include a concrete object layout (128-byte, 8-byte header, 112-byte payload), fragmentation mitigation, and a minimal stress harness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a 4KB circular buffer with per-slot header {uint16_t len; uint64_t seq;}. Maintain atomic head and tail. Writer reserves a slot via CAS on head, copies payload, sets len, and bumps seq to signal d",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a 32-byte ring with mask=31. Maintain volatile head and tail. Enqueue: next=(tail+1)&mask; if next==head drop; else buf[tail]=b; tail=next. Dequeue: if head==tail return EMPTY; else *out=buf[head]",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a crash-consistent, persistent ring buffer for NVRAM with multi-producer, multi-consumer access. Use 1<<18 slots, 128-byte payloads. ABA avoided via per-slot sequence numbers; two-phase publis",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-node arenas with thread-local freelists and a global pool. Object layout: 8-byte epoch, 8-byte next, 112-byte payload. Alloc uses per-node cache first; bulk-replenish from remote nodes via work-st",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of NUMA sensitivity, lock-free structures, and cross-node reclamation under real workloads.\n\n## Key Concepts\n\n- NUMA-aware memory pools and per-node caching\n- Lock-free allocation paths with minimal contention\n- Epoch-based reclamation to avoid ABA issues\n- Fragmentation mitigation and cache-line alignment\n\n## Code Example\n\n```c\ntypedef struct {\n  uint64_t epoch;      // reclamation epoch\n  uint64_t next;       // free list linkage\n  uint8_t  payload[112];\n} mem128_t; // total 128 bytes\n\nvoid* alloc128_on_node(int node);\nvoid  free128_on_node(void* p);\n```\n\n## Follow-up Questions\n\n- How would you extend to support object truncation or compaction under pressure?\n- How would you validate memory safety with weak memory ordering on modern CPUs?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-10",
        "questionId": "q-21381",
        "question": "**DESIGN CHALLENGE**: Build a NUMA-aware, kernel-bypass, zero-copy producer-consumer queue for a high-throughput network stack across two sockets. Support many producers/consumers with bounded capacity, per-slot sequence counters to avoid ABA, and a lock-free path with a small fallback. Provide API prototypes and minimal pseudo-code for enqueue/dequeue, and discuss cache-line alignment and memory ordering?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed answer: API prototypes for enqueue/dequeue; use per-slot sequence counters to detect ABA; lock-free tail/head CAS with memory_order_acq_rel and a guarded fallback path for full/empty; pad to",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "NUMA-aware slab allocator with per-thread caches and a small global pool. Each thread maintains a 4KB slab of 64B objects (64 slots) aligned to 64B. Freelist entries are 64B with a CAS-based push/pop",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "FAST PATH: thread-local freelists for 8/16/32/64 B; allocate from cache, push to freelist on free. SLOW PATH: central pool with lock-free free-list per size class. Use tagged pointers to avoid ABA; al",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Leverage per-size-class slabs of 64-byte-aligned blocks, with lock-free free-lists and per-thread caches to minimize cross-NUMA traffic. Use atomic CAS on a head-pointer for pop/push; implement ABA-fr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real‑world, low‑level queue design under NUMA and kernel-bypass constraints. Evaluates API clarity, memory ordering discipline, and practical safeguards against ABA and false sharing.\n\n## Key Concepts\n\n- NUMA affinity and cross-socket latency\n- Kernel-bypass, zero-copy semantics\n- Per-slot sequence counters to prevent ABA\n- Cache-line alignment and padding to avoid false sharing\n- Memory ordering: CAS on tail/head with acq_rel fences\n\n## Code Example\n\n```c\n// Prototype definitions\ntypedef struct { void* data; unsigned long seq; } slot_t;\ntypedef struct {\n  slot_t* slots;\n  size_t cap;\n  _Atomic size_t head;\n  _Atomic size_t tail;\n} queue_t;\n\nbool enqueue(queue_t* q, void* item);\nbool dequeue(queue_t* q, void** item);\n```\n\n```c\n// Minimal pseudo-code (high level)\nsize_t t = atomic_load_explicit(&q->tail, memory_order_acquire);\nsize_t h = atomic_load_explicit(&q->head, memory_order_relaxed);\nif (t - h >= q->cap) return false; // full\nslot_t* s = &q->slots[t & (q->cap-1)];\nunsigned long expected = s->seq;\nif (CAS(&s->seq, expected, expected+1)) {\n  s->data = item;\n  atomic_store_explicit(&q->tail, t+1, memory_order_release);\n  return true;\n}\n```\n\n## Follow-up Questions\n\n- How would you test determinism under sustained load and cross-socket traffic?\n- What backpressure strategy would you apply when producers outpace consumers?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-11",
        "questionId": "q-6278",
        "question": "Design a two-level, lock-free timer wheel in C for a high-throughput event scheduler across 8 cores. Outer wheel: milliseconds; inner wheel: microseconds. Each timer stores deadline, callback, and linkage. Enqueue uses thread-local slots and atomic stores; tick advances outer wheel, moves due timers to inner wheel for immediate execution, with epoch-based reclamation. Compare latency and jitter vs a heap queue?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-level timer wheel with outer 1ms buckets and inner 1µs buckets. Timers hold deadline, cb_id, and next pointer. Enqueue places the timer in the target outer bucket via per-thread slot with atomic s",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Leverage per-slot sequence numbers and a compact PMEM log. Enqueue: write data to PMEM, flush, publish tail with a release store and memory fence; dequeue validates sequence, advances head with atomic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Init: pool = 1MB; blocks = 4096; bitmap = 64 64-bit words (one bit per block), all bits 0 (free). alloc(): lock; scan words for a zero bit, set it, unlock; return pool + idx*256. free(p): idx = (p - p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a 64-slot ring with per-slot lock-free stacks. Enqueue computes slot = (current_tick + delay_ms) & 63, pushes a timer node with due_tick and callback. Tick advances every ms, drains current sl",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a deterministic low-latency timer structure with lock-free synchronization and practical reclamation in a multi-core environment.\n\n## Key Concepts\n- Timer wheels, two-level design\n- Lock-free synchronization, atomic ops\n- Epoch-based reclamation and hazard pointers equivalent\n- Cache-line and false sharing considerations\n- Microbenchmark methodology\n\n## Code Example\n```c\n// Skeleton definitions\ntypedef struct timer {\n  uint64_t deadline;\n  void (*cb)(void*);\n  struct timer *next;\n} timer_t;\n```\n\n## Follow-up Questions\n- How would you scale to dynamic bucket counts?  \n- How would you support timer cancellation efficiently?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-12",
        "questionId": "q-8104",
        "question": "Design a minimal 1-producer-1-consumer mailbox using a single shared 64-bit slot and a 1-bit ready flag. Provide init, send(value), and recv() in C11-style pseudocode. Explain how to encode a sequence to avoid ABA, how memory order ensures visibility, and outline a microbenchmark on a 4-core system to validate latency and throughput?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use thread-local storage to hold a pool of 1024 blocks (128 bytes each). Each block has a next pointer; a per-thread free-list is formed. allocate() pops from the list, free() pushes back. If the thre",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a 4-level, 64-slot-per-level lock-free timer wheel. schedule_timer(delta_ns, cb) maps to the target slot at the appropriate level; enqueue a descriptor in a per-level slab using a per-slot sequ",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Encode a single 64-bit slot: high 32 bits = sequence, low 32 bits = payload. Init seq=0, payload=0. Sender CASes to publish (seq+1, payload) when current seq matches and payload is 0. Receiver uses ac",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Local-first, NUMA-aware allocator for 64-byte objects on a 2-socket server. Each node hosts slabs and a per-thread fast-path cache; allocation pops from the thread cache, then the node free-list; if e",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests low-level synchronization primitives, memory ordering, and ABA avoidance in a tiny mailbox primitive.\n\n## Key Concepts\n- Atomic CAS on a 64-bit slot; sequence counters prevent ABA.\n- Memory_order_release on publish, memory_order_acquire on receive for visibility.\n- Fill-empty state via payload==0 heuristic for simplicity; robust designs can reserve a dedicated marker.\n\n## Code Example\n```c\ntypedef struct { _Atomic uint64_t slot; } Mailbox;\nvoid mailbox_init(Mailbox *m){ atomic_init(&m->slot, 0); }\nbool mailbox_send(Mailbox *m, uint32_t payload){\n  uint64_t cur = atomic_load_explicit(&m->slot, memory_order_relaxed);\n  uint32_t seq = (uint32_t)(cur>>32);\n  uint64_t want = ((uint64_t)seq<<32);\n  uint64_t next = (((uint64_t)(seq+1))<<32) | payload;\n  return atomic_compare_exchange_strong(&m->slot, &want, next);\n}\nbool mailbox_recv(Mailbox *m, uint32_t *out){\n  uint64_t cur = atomic_load_explicit(&m->slot, memory_order_acquire);\n  uint32_t seq = (uint32_t)(cur>>32);\n  uint32_t payload = (uint32_t)(cur & 0xFFFFFFFF);\n  if (payload == 0) return false;\n  uint64_t next = (((uint64_t)(seq+1))<<32) | 0u;\n  if (atomic_compare_exchange_strong(&m->slot, &cur, next)){\n    *out = payload; return true;\n  }\n  return false;\n}\n```\n\n## Follow-up Questions\n- How would you extend to multiple producers while keeping SPSC guarantees?\n- What microarchitectural pitfalls exist (false sharing, cache lines) and how would you mitigate them?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-13",
        "questionId": "q-26044",
        "question": "Design a lock-free open-addressing hash map for a 2-socket x86-64 server that supports concurrent inserts, lookups, and deletes with dynamic, lock-free resizing. Use per-slot sequence counters to avoid ABA, and ensure safe memory reclamation via epoch-based reclamation or hazard pointers. Provide API prototypes and minimal pseudo-code for insert/find/delete; outline a microbenchmark plan to validate latency and throughput under high contention and resize events?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two 32-bit atomics: next and serve. Init next=serve=0. Lock: my = atomic_fetch_add(&next,1); while (atomic_load(&serve) != my) { pause(); } Unlock: atomic_store_explicit(&serve, my+1, memory_order_rel",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I'd implement a free-list allocator over a 256 KiB pool divided into 2048 blocks (256*1024 / 128). Each free block stores a pointer or index to the next free block in its first 8 bytes. The allocate operation atomically…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lock-free open-addressing hash map for two sockets. Use 64-bit keys/values, per-slot sequence counters to detect tombstones and avoid ABA, CAS-based inserts, and epoch-based reclamation. R",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Per-core freelists with a lock-free stack; slabs 4KB, 64-byte slots; hot path uses thread-local cache; misses refill from NUMA-local pool; remote frees buffered per-node and migrated in bulk to minimi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a core, real-world lock-free data structure under NUMA pressure with dynamic resizing, focusing on correctness, memory reclamation, and performance under contention.\n\n## Key Concepts\n\n- Lock-free hashing\n- Open addressing with tombstones\n- ABA avoidance via per-slot sequence counters\n- Epoch-based reclamation or hazard pointers\n- NUMA-aware allocation and phased resizing\n- Linearizability guarantees\n\n## Code Example\n\n```c\ntypedef struct Slot {\n  uint64_t key;\n  uint64_t value;\n  uint64_t seq;\n  atomic_bool used;\n} Slot;\n\ntypedef struct HashMap {\n  size_t capacity;\n  Slot* table;\n  atomic_size_t size;\n} HashMap;\n\n// Prototypes\nHashMap* hm_create(size_t cap);\nbool hm_put(HashMap* hm, uint64_t key, uint64_t value);\nbool hm_get(HashMap* hm, uint64_t key, uint64_t* out);\nbool hm_del(HashMap* hm, uint64_t key);\n```\n\n```",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-14",
        "questionId": "q-22031",
        "question": "Write a C function crc32c_stream that computes CRC32c for a data stream at line-rate on a 2-socket server using AVX2; process 64-byte blocks with _mm_crc32_u64, unrolled 4x, align input to 64 bytes, and fallback tail path for leftovers. Include a microbenchmark plan, and discuss cache-line reuse and latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a spinlock with atomic_flag. Acquire via while (atomic_flag_test_and_set_explicit(&lock, memory_order_acquire)) { _mm_pause(); } Release with atomic_flag_clear_explicit(&lock, memory_order_r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-node shards with a global coordinator. Lock-free hot path: atomic upserts into per-node arrays using CAS, with 64-byte padding; memory order: acquire/release fences around reads/writes; cross-node",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use AVX2 with _mm_crc32_u64 to accumulate 8-byte chunks; align input to 64 bytes, unroll the loop 4x, and update crc = _mm_crc32_u64(crc, data64) for every word. Handle remaining bytes with a scalar p",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Local-first, NUMA-aware allocator for 64-byte objects on a 2-socket server. Each node hosts slabs and a per-thread fast-path cache; allocation pops from the thread cache, then the node free-list; if e",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world requirement: sustaining line-rate CRC32c on multi-socket servers with hardware acceleration, and ensuring portability with a clean tail-path and proper alignment.\n\n## Key Concepts\n- AVX2 CRC32 intrinsics\n- 64-byte block processing and loop unrolling\n- Input alignment, endianness, and tail handling\n- Cache-line locality and memory bandwidth\n- Benchmark methodology for throughput and latency\n\n## Code Example\n```c\n#include <nmmintrin.h>\nuint32_t crc32c_stream(const uint8_t *data, size_t len, uint32_t crc){\n  // align and process 64-byte chunks using _mm_crc32_u64\n  // unrolled loop and tail handling\n  return ~crc;\n}\n```\n\n## Follow-up Questions\n- How would you adapt for non-64-byte alignment without losing throughput?\n- How would you verify correctness under endian mismatches and partial packets?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-15",
        "questionId": "q-21953",
        "question": "You have a fixed memory pool of 1MB partitioned into 256-byte blocks. Implement a tiny allocator with two operations: alloc() that returns a 256-byte block or NULL if exhausted, and free(void*) that reclaims a block. Use a bitmap to track free blocks and protect concurrent alloc() calls from two threads with a simple mutex around the bitmap. Provide init, alloc, and free in pseudo-C?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-thread arenas hold a small freelist of 256 blocks, each 128B aligned to 64B. Allocation pops from the thread-local freelist; if empty, refill from a shared global pool via CAS-based lock-free stac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Init: pool = 1MB; blocks = 4096; bitmap = 64 64-bit words (one bit per block), all bits 0 (free). alloc(): lock; scan words for a zero bit, set it, unlock; return pool + idx*256. free(p): idx = (p - p",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a two-counter ticket lock: next_ticket and now_serving as atomic integers. Lock: my = fetch_add(&next_ticket, 1); while (atomic_load_explicit(&now_serving, memory_order_acquire) != my); Unlock: at",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a 256-slot ring; keep current_slot. schedule maps to slot = (current_slot + delay_ms) & 0xFF; pull a node from a preallocated pool, set cb/ctx, push to front of slot's list. tick executes all node",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests memory pool basics, bitmap tracking, and simple synchronization in a realistic but tiny allocator. Beginner-friendly yet checks discipline in safe memory handling.\n\n## Key Concepts\n- Fixed-size pool allocation\n- Bitmap-based free-list\n- Lightweight locking around shared state\n\n## Code Example\n```javascript\n// pseudo-C\nvoid init_pool(); void* alloc(); void free(void*);\n```\n\n## Follow-up Questions\n- How would you extend to handle different block sizes without a major redesign?\n- How would per-thread caches affect contention and correctness?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-16",
        "questionId": "q-18973",
        "question": "Design a per-core, small-object slab allocator for an ARM64 server with 6 cores, targeting allocations under 256B. Provide core data structures, alignment to 64 bytes, per-core lock-free free-lists, and a global pool as fallback. Present allocate/free pseudocode using CAS and memory barriers, with size-class bins (32B, 64B, 128B, 256B). Discuss fragmentation, cache aliasing, TLB pressure, and backpressure if the global pool stalls. Include a microbenchmark plan comparing to a naive malloc and measuring latency, throughput, and fragmentation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "This SP MC queue uses per-slot seq and a single tail. Producer does tail = fetch_add(1) to pick slot i, writes payload, then stores seq = i+1. Each consumer has a local read index r and spins until sl",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-core caches with 64-byte-aligned blocks in bins 32/64/128/256B. Each core holds an atomic freelist; a global pool refills via CAS on misses. Allocate from the core cache; if empty, refill from glo",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Init: pool = 1MB; blocks = 4096; bitmap = 64 64-bit words (one bit per block), all bits 0 (free). alloc(): lock; scan words for a zero bit, set it, unlock; return pool + idx*256. free(p): idx = (p - p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-thread 128-byte blocks; each thread keeps a lock-free freelist; when empty, replenish from a central arena via a lock-free pool using CAS. allocate() pops from thread list or fetches a batch, free",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical low-level allocator design, cache locality, and lock-free data structures under NUMA/ARM constraints.\n\n## Key Concepts\n- Per-core freelists and global pool coordination\n- Lock-free CAS-based enqueue/dequeue\n- Size-class design and fragmentation trade-offs\n- Cache-line alignment and NUMA considerations\n\n## Code Example\n```javascript\n// Pseudo-code for allocate\n```\n\n## Follow-up Questions\n- How would you adapt for larger allocations and allocator poisoning protection?\n- What tests would you run to ensure no memory leaks under pressure?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-17",
        "questionId": "q-8279",
        "question": "Design a tiny fixed-size block memory pool: arena is 1MB, consisting of 64-byte blocks. Implement pool_init(), pool_alloc(), pool_free(void*). Use a bitmap to track free blocks and ensure O(1) allocation and deallocation. Include 8-byte alignment checks and bounds validation. Provide minimal C-like pseudocode and explain how you would extend it to a multi-threaded environment?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a NUMA-aware, lock-free allocator optimized for 64-byte objects. Use per-thread freelists to satisfy allocations from a thread-local cache; on cache miss, fetch a slab from a central NUMA-local pool and refill th…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a 1MB arena partitioned into 64-byte blocks (16384 blocks). A 256-entry bitmap tracks free blocks; 0=free,1=used. pool_init() allocates arena and clears bitmap. pool_alloc() finds first 0 bit, set",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To stream descriptors efficiently, implement per-NUMA freelists with 64-byte alignment, batch reclamation, and a tiny allocator header per socket. Pin arenas to sockets, avoid cross-socket frees, and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enqueue writes to a slot and publishes a release-store to the slot’s seq; consumer uses an acquire-load to validate slot and then advances head. For crash-resilience, flush the slot data and its seq w",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical memory pool design basics: arena layout, bitmap free list, O(1) ops, alignment, safety, and extension to concurrency.\n\n## Key Concepts\n- Fixed-size allocators\n- Bitmap free-list\n- Pointer arithmetic\n- Alignment and bounds checking\n- Concurrency extension strategies\n\n## Code Example\n```javascript\ntypedef struct {\n  unsigned char *arena;\n  uint32_t nblocks;\n  uint64_t *bitmap; // bitmask\n} Pool;\n\nvoid pool_init(Pool *p, void *buffer, size_t size);\nvoid *pool_alloc(Pool *p);\nvoid pool_free(Pool *p, void *ptr);\n```\n\n## Follow-up Questions\n- How would you test for fragmentation and reuse patterns?\n- Which synchronization primitive would you add to support multi-threaded access and why?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-18",
        "questionId": "q-27143",
        "question": "Design a NUMA-aware, lock-free slab allocator for fixed-size objects (128B) on a 2-socket Linux server. Each object occupies a slot within per-NUMA slabs; implement per-core free lists (lock-free stacks) that refill from a global pool via atomic ops. Ensure minimal fragmentation and no cross-socket cache thrash. Provide API prototypes and minimal C-like pseudo-code, and outline a microbenchmark to compare throughput against a naive malloc under varying thread counts and memory locality?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-NUMA pools with 128B slots in 4K slabs; maintain a lock-free per-core freelist, refilling from a global pool via CAS when empty; alloc by popping from local freelist, free by pushing back. Bin",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Lock-free graph: each node has an atomic head pointer to a singly linked list of edges; Edge {int to; atomic next}. add_edge uses CAS to prepend; add_node allocates a node and links it atomically. rem",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-thread 128-byte blocks; each thread keeps a lock-free freelist; when empty, replenish from a central arena via a lock-free pool using CAS. allocate() pops from thread list or fetches a batch, free",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a hierarchical NUMA-aware allocator with per-process caches and global coordination. The design consists of three tiers: per-process freelists for small allocations (<128 bytes), per-NUMA-node pools for medium…",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked: Tests practical, low-level allocator design with real-world constraints (NUMA, lock-free structures, fragmentation). Key trade-offs include locality vs global refill, cache-line alignment, and fast-path vs fallback paths.\n\nKey Concepts:\n- NUMA locality and first-touch bindings\n- Lock-free freelists with CAS\n- Slab allocation and fixed-size object management\n- Cache-line alignment and false sharing prevention\n- Fragmentation control and slab reclamation\n\nCode Example\n```c\ntypedef struct Block { struct Block* next; } Block;\ntypedef struct Slab { void* base; Block* freelist; struct Slab* next; } Slab;\n\n// Prototypes (illustrative)\nvoid* slab_alloc(void);\nvoid  slab_free(void* obj);\n```\n\nFollow-up Questions\n- How would you extend to multiple object sizes with per-size pools?\n- How would you simulate and measure worst-case allocator latency under contention?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-19",
        "questionId": "q-29688",
        "question": "Design a NUMA-aware per-core memory pool allocator for a high-throughput NIC path. Provide API allocate(size) and free(ptr), ensure 64-byte block alignment, per-core caches, and lock-free cross-core freelists; include a strategy to rebalance arenas under contention and to reclaim remotely freed blocks. Give minimal pseudo-code for allocate and free and outline a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a 4-level, 64-slot-per-level lock-free timer wheel. schedule_timer(delta_ns, cb) maps to the target slot at the appropriate level; enqueue a descriptor in a per-level slab using a per-slot sequ",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a two-counter ticket lock: next_ticket and now_serving as atomic integers. Lock: my = fetch_add(&next_ticket, 1); while (atomic_load_explicit(&now_serving, memory_order_acquire) != my); Unlock: at",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a fixed-size ring with per-slot sequence counters and 64-byte frames. Enqueue reserves via CAS on tail, writes frame, bumps seq, and uses a release store for tail. Dequeue reads head, validates seq, copies frame, an…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-core arenas isolate allocations to minimize cross-NUMA memory traffic. Each core maintains a private 64-byte block freelist; when empty, it atomically fetches a batch from a global pool using CAS, then refills its l…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests NUMA locality awareness, per-core caches, lock-free freelists, and safe cross-core reclamation in a realistic high-performance allocator.\n\n## Key Concepts\n\n- NUMA topology and memory locality optimization\n- Per-core slab caches for minimal synchronization overhead\n- Lock-free freelists using CAS operations\n- Epoch-based reclamation for safe memory reuse\n- Slab design and fragmentation management\n- Thread/core pinning strategies for consistent affinity\n\n## Code Example\n\n```javascript\nclass Arena {\n  constructor() {\n    this.head = null;\n  }\n}\n\nfunction allocate(size) {\n  // Atomically pop from per-core freelist\n}\n\nfunction free(ptr) {\n  // Enqueue to owning core's arena\n}\n```\n\n## Microbenchmark Plan\n\n1. **Baseline metrics**: Measure raw allocation latency per thread with no contention\n2. **NUMA scalability test**: Vary thread count across sockets, measure remote vs. local access latency\n3. **Rebalancing test**: Inject high contention, measure throughput recovery after rebalance\n4. **Remote reclamation test**: Free from non-owning cores, measure reclaim latency and correctness\n5. **Cache line bouncing**: Instrument false sharing to detect cross-core cache line migration",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-20",
        "questionId": "q-26214",
        "question": "In a 2-socket NUMA server, implement a NUMA-aware slab allocator for fixed-size objects (e.g., 64B). Use per-thread freelists with a global pool, ensure thread safety, minimize cross-socket traffic, and provide a minimal API (alloc/free). Discuss data structures, alignment, false sharing, memory barriers, and a microbenchmark plan to measure throughput and fragmentation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a sense-reversal barrier with 64-byte-aligned per-thread slots and a shared generation counter. **barrier_enter:** each thread writes its sense bit to its slot and performs an atomic fetch_add on the arrivals counte…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "NUMA-aware slab allocator with per-thread caches and a small global pool. Each thread maintains a 4KB slab of 64B objects (64 slots) aligned to 64B. Freelist entries are 64B with a CAS-based push/pop",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Provide a 1<<16, 128-byte per-slot bounded MPMC ring. Slot header: 16B seq/len plus 112B payload. Producers publish with store-release, then advance via CAS; consumers use load-acquire, process, and r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a spinlock with atomic_flag. Acquire via while (atomic_flag_test_and_set_explicit(&lock, memory_order_acquire)) { _mm_pause(); } Release with atomic_flag_clear_explicit(&lock, memory_order_r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests NUMA locality, per-thread caches, and lock-free freelists; checks understanding of false sharing and ABA avoidance.\n\n## Key Concepts\n- NUMA locality\n- Per-thread caches\n- Lock-free freelists\n- ABA avoidance with version counters\n- Cache-line padding and fragmentation\n\n## Code Example\n```c\ntypedef struct Node { struct Node *next; unsigned long tag; } Node;\ntypedef struct Slab { void *base; _Atomic size_t free; Node *freelist; } Slab;\n```\n\n## Follow-up Questions\n- How would you validate scalability beyond 2 sockets?\n- How would you adapt for variable object sizes?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-21",
        "questionId": "q-6272",
        "question": "Design a durable, crash-safe, lock-free queue for a 2-socket server using non-volatile memory (PMEM) that guarantees correct enqueue/dequeue across power failures. Provide a bounded, multi-producer, single-consumer ring with wrap-around, per-entry header (len, seq, committed), and a recovery procedure after crash. Include pseudo-code for enqueue/dequeue, a test plan, and a micro-benchmark?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed solution: four-level hierarchical timer wheel with 64K buckets per level, 128-byte per-slot records {expiry:8, id:8, seq:8, payload:104}. Enqueue computes target level/slot, uses CAS to insta",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a circular log in PMEM with per-entry header {len, seq, committed}. Enqueue writes payload, then header; flush order with clwb/siws and mfence; consumer validates header and reads payload un",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a four-size-class slab allocator supporting 32, 64, 128, and 256-byte objects. Each thread maintains per-size free lists. On a cache miss, fetch a full slab from a global lock-free pool using a compare-and-swa…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a fixed-size ring with per-slot sequence counters and 64-byte frames. Enqueue reserves via CAS on tail, writes frame, bumps seq, and uses a release store for tail. Dequeue reads head, validates seq, copies frame, an…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes crash-safe, low-latency data paths in persistent memory architectures.\n\n## Key Concepts\n- Non-volatile memory ordering (clwb/flush, sfence).\n- Crash-consistent ring buffers and per-entry headers.\n- Epoch-based reclamation and GC for reuse.\n- Testing crash-recovery and power-failure scenarios.\n\n## Code Example\n```c\ntypedef struct { uint32_t len; uint64_t seq; uint8_t committed; } entry_hdr_t;\n```\n\n## Follow-up Questions\n- How would you benchmark recovery latency and correctness after abrupt power loss?\n- What would you change for multi-consumer support?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-22",
        "questionId": "q-10870",
        "question": "Design a NUMA-aware, lock-free per-thread object pool allocator for a 2-socket x86_64 server. Provide API prototypes allocate/free, describe per-thread caches, a bounded global pool, fast-path local allocations, slow-path remote fetches, and safe cross-thread deallocation. Explain reclamation with hazard pointers or epochs, avoid ABA with pointer tagging, ensure 64-byte cache-line alignment, and outline a microbenchmark plan to compare against malloc with bursty allocations and cross-socket frees?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-core arenas with a lock-free local free-list and a shared global pool refilling via CAS in batches. Allocation uses local list pop; if empty, atomically steal a batch from the global pool; dealloc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "An effective answer outlines: API: void* alloc(size_t); void free(void*); with per-thread caches, a bounded global pool, fast-path local allocation, slow-path remote fetch, and safe cross-thread deall",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Read timestamps with RDTSCP after a CPUID serialize; at timer_start, store start_ts in a per-id slot atomically and mark active; on timer_stop, read end_ts, delta = end - start, add to total_ns using",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use epoch-based reclamation (EBR). Keep a global epoch and per-thread local retire lists with retire_epoch. On free, push to the thread’s retire list; periodically advance the global epoch after a qui",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests NUMA awareness, lock-free design, and memory reclamation in a realistic allocator.\n\n## Key Concepts\n- NUMA locality and cache-line alignment\n- Lock-free per-thread caches and shared pools\n- Hazard pointers or epoch-based reclamation\n- ABA avoidance via pointer tagging\n- Fragmentation control and bounded pools\n\n## Code Example\n```c\ntypedef struct object { alignas(64) void* data; } object_t;\n// API:\nvoid* pool_alloc(size_t n);\nvoid pool_free(void* p);\n```\n\n## Follow-up Questions\n- How would you validate reclamation correctness under stress?\n- What metrics and microbenchmarks would you use to compare against malloc?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-23",
        "questionId": "q-20136",
        "question": "Design a tiny, lockless cross-thread config exchange using two 64-byte buffers. A writer updates config in buffer A, computes a CRC, and publishes a version counter via atomic_store_release (toggle parity to indicate ready). A reader loops: read version with acquire, copy to local, verify CRC, re-check version; accept only if unchanged and even. Provide C-like pseudo-code and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a deterministic, lock-free object pool allocator for fixed-size 128-byte objects on a 2-socket x86-64 system with per-thread fast-path free lists, central cross-thread pool, and hazard-pointer-based reclamation w…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-NUMA allocator with per-node free lists (lock-free or atomics), each block stores next pointer; allocate uses local node list via CAS; if empty, steal from a remote node or fall back to a central",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Lockless Cross-Thread Config Exchange Design ### Data Structures ```c #define BUFFER_SIZE 64 typedef struct { uint8_t data[BUFFER_SIZE]; uint32_t crc; } ConfigBuffer; typedef struct { ConfigBuffer buffers[2]; _Atomic…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a sense-reversing barrier with N threads and C11 atomics. Each thread signals arrival via a shared counter (fetch_add); the last thread resets the counter and toggles a shared sense flag. Other th",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates understanding of lock-free synchronization patterns and memory ordering semantics—critical skills for high-performance concurrent systems. It tests the ability to design data structures that operate correctly without traditional locking mechanisms, which is essential for building low-latency systems in domains such as real-time trading, embedded systems, and kernel development.\n\n## Key Concepts\n\n- **Atomic Release/Acquire Semantics**: Release semantics ensure all prior writes are visible before the atomic operation completes, while acquire semantics guarantee the reader sees all writes published by the writer. This eliminates the need for explicit memory barriers.\n\n- **Double-Buffering Technique**: Using two alternating buffers allows the producer to write to one buffer while the consumer reads from the other, eliminating contention and enabling lockless operation.\n\n- **CRC Validation**: A cyclic redundancy check provides data integrity verification, detecting torn reads caused by partial writes or memory ordering anomalies.\n\n- **Version Parity Pattern**: Toggling a version counter's least-significant bit (parity) signals buffer availability. Even values indicate one buffer is ready; odd values indicate the other.\n\n- **Retry Loop with Re-validation**: The reader's check-then-check-again pattern prevents stale data acceptance, addressing the ABA problem common in lock-free designs.\n\n## What Success Looks Like\n\nA strong candidate will explain the memory ordering implications, recognize the version check as a form of guard against torn reads, and understand why the retry loop is necessary. They should also discuss test scenarios that verify correctness under race conditions and memory ordering violations.",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-24",
        "questionId": "q-14848",
        "question": "On a two-socket NUMA server, design a scalable, lock-free, multi-producer multi-consumer priority queue with a high-priority ring that bypasses the normal path. Provide data structures, enqueue/dequeue pseudocode with C11 atomics, and a plan to measure cross-socket traffic, fairness, and latency under mixed workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-thread fast path for Pin/Unpin, a global atomic refcount, and an epoch-based retire queue with hazard pointers. Pin increments a per-thread local counter and a global refcount; Unpin decrements an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-path memmove: if dst <= src or no overlap, copy forward in 64-byte steps; if dst > src with overlap, copy backward (start from end) to avoid clobbering source data. Handle tail bytes after 64-byte",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a four-size-class slab allocator supporting 32, 64, 128, and 256-byte objects. Each thread maintains per-size free lists. On a cache miss, fetch a full slab from a global lock-free pool using a compare-and-swa…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-socket cache-line rings (N=1<<16) for two queues: high and normal. Each slot stores (gen, pri, data_ptr). Producers reserve via atomic fetch_add, publish with release; consumers read with acqu",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical understanding of NUMA-awareness, lock-free queues, and cross-socket traffic with priority cues. It probes ability to design data structures, pick atomics, and reason about fairness.\n\n## Key Concepts\n- NUMA-aware design and local queues\n- Lock-free synchronization with C11 atomics and memory order\n- Priority queuing and drain path to minimize cross-socket traffic\n- Fairness and backpressure in multi-core environments\n\n## Code Example\n```javascript\n// Pseudo-code: enqueue/dequeue\ntypedef struct { uint64_t gen; uint16_t pri; void *ptr; } slot_t;\ntypedef struct { slot_t *slots; _Atomic uint32_t head, tail; } ring_t;\n\n// Enqueue pseudocode\nvoid enqueue(ring_t *r, void *p, uint16_t pri) {\n  uint32_t idx = atomic_fetch_add_explicit(&r->head, 1, memory_order_relaxed);\n  slot_t *s = &r->slots[idx & (N-1)];\n  s->ptr = p;\n  s->pri = pri;\n  atomic_thread_fence(memory_order_release);\n  // publish idx\n}\n```",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-31T11:11:19.917Z",
    "version": 1
  }
]