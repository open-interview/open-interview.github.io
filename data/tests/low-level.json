[
  {
    "id": "test-low-level",
    "channelId": "low-level",
    "channelName": "LOW Level",
    "title": "LOW Level Knowledge Test",
    "description": "Test your understanding of LOW Level with 25 questions.",
    "questions": [
      {
        "id": "low-level-q-0",
        "questionId": "q-6278",
        "question": "Design a two-level, lock-free timer wheel in C for a high-throughput event scheduler across 8 cores. Outer wheel: milliseconds; inner wheel: microseconds. Each timer stores deadline, callback, and linkage. Enqueue uses thread-local slots and atomic stores; tick advances outer wheel, moves due timers to inner wheel for immediate execution, with epoch-based reclamation. Compare latency and jitter vs a heap queue?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a fixed-size, thread-safe allocator for 32-byte blocks from a pre-allocated arena (1–2 MB). Use a lock-free free-list updated via CAS, plus per-thread caches to reduce contention. Expose voi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A lock-free, bounded task queue with work-stealing for a 2-socket x86-64 server. Each worker maintains a private deque with local push and shared steal endpoints. Producers push to their local queue; if full, they fallb…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "```c #include <stdatomic.h> #include <sched.h> typedef struct { atomic_flag f; } spinlock_t; #define SPINLOCK_INIT { ATOMIC_FLAG_INIT } static inline void spin_lock(spinlock_t *s){ while (atomic_f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-level timer wheel with outer 1ms buckets and inner 1µs buckets. Timers hold deadline, cb_id, and next pointer. Enqueue places the timer in the target outer bucket via per-thread slot with atomic s",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a deterministic low-latency timer structure with lock-free synchronization and practical reclamation in a multi-core environment.\n\n## Key Concepts\n- Timer wheels, two-level design\n- Lock-free synchronization, atomic ops\n- Epoch-based reclamation and hazard pointers equivalent\n- Cache-line and false sharing considerations\n- Microbenchmark methodology\n\n## Code Example\n```c\n// Skeleton definitions\ntypedef struct timer {\n  uint64_t deadline;\n  void (*cb)(void*);\n  struct timer *next;\n} timer_t;\n```\n\n## Follow-up Questions\n- How would you scale to dynamic bucket counts?  \n- How would you support timer cancellation efficiently?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-1",
        "questionId": "q-723",
        "question": "Design a software-based **TLB** for a 4-core 64-bit system with 4KiB pages. Each core has a private 128-entry **TLB** and a global page-table invalidation path. Provide lookup/refill pseudo-code, discuss eviction strategy (LRU vs. random), synchronization via **memory fences**, and cross-core shootdowns. Include a test under memory pressure and explain validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design and implement a NUMA-aware per-thread arena allocator in C for a 2-socket server. Each thread utilizes a cache-line-aligned pool with 128-byte padding to prevent false sharing, optimized fast paths for 8/16/32-by…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a circular buffer of N slots, each 64B with a 64-bit sequence counter. Initialize each slot's sequence to its index. The producer writes payload data, then updates the tail pointer and increments the slot's sequence…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Private per-core 128-entry TLB, 4KiB pages, 4-way set-associative. On miss, walk multi-level page tables, then install using atomic ops. Use a scalable eviction (pseudo-LRU). Invalidate entries across",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "An effective answer outlines: API: void* alloc(size_t); void free(void*); with per-thread caches, a bounded global pool, fast-path local allocation, slow-path remote fetch, and safe cross-thread deall",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nA software TLB with per-core caches and cross-core invalidations tests low-level thinking about coherence, consistency, and performance under contention. It also probes trade-offs between eviction, barriers, and hardware hints.\n\n## Key Concepts\n\n- TLB structure: per-core caches, set-associative layout\n- Page-table walk costs and caching gains\n- Cross-core invalidation/shootdown mechanisms\n- Memory fences and atomic updates\n- Validation under memory pressure and realistic workloads\n\n## Code Example\n\n```javascript\n// Pseudo-code: TLB lookup and refill\nfunction tlbLookup(vpn){\n  const idx = vpn % 128;\n  const e = tlb[idx];\n  if (e && e.vpn === vpn && e.valid) return e.pte;\n  return null;\n}\nfunction tlbRefill(vpn, pte){\n  const idx = vpn % 128;\n  tlb[idx] = { vpn, pte, valid: true, ts: Date.now() };\n}\n```\n\n## Follow-up Questions\n\n- How would you scale to larger pages or userfaults?\n- How would you validate correctness under concurrent refills and shootdowns?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-2",
        "questionId": "q-19250",
        "question": "Design a lock-free, bounded, cross-thread memory allocator for small objects (sizes 16/32/64 bytes) in a shared 2-socket NUMA region. Use per-thread local caches and a global pool; implement allocate(size) and free(ptr) with a CAS-based refill and hazard-pointer-based reclamation to avoid ABA and use-after-free. Describe data structures (free lists, slab descriptors), fragmentation handling, and a microbenchmark plan comparing against a naive allocator under high-concurrency workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose and implement a lock-free bounded allocator with multiple size classes (16/32/64 bytes). Each thread maintains a local free-list; a shared global pool refills via CAS when local is empty. Haza",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a fixed-size ring with slots each containing a 64-bit payload and a 64-bit sequence, padded to avoid false sharing. Enqueue: read head, compute next=head+1; if slot[next].seq==head, write pa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a crash‑consistent, lock‑free object pool using per‑thread local caches backed by a central persistent free list. The central pool maintains a CAS‑based head pointer to a singly linked free list stored in pers…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a circular PMEM ring with N slots; writer writes payload, then updates and persists seq to publish the item; reader polls slots, verifies seq, copies payload, then advances a persistent tail and p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests hands-on understanding of low-level memory management under contention: lock-free design, ABA-free reclamation, and NUMA-aware locality.\n\n## Key Concepts\n- Hazard pointers or epoch-based reclamation for UAF\n- Size-class free lists and per-thread caches\n- ABA avoidance in lock-free paths\n- CAS-based global pool refills\n- NUMA locality and cache-line alignment to minimize false sharing\n\n## Code Example\n```cpp\n// Pseudo-code: allocate and free with size classes 16/32/64\nstruct Block { Block* next; };\nclass Allocator {\n  std::atomic<Block*> globalFree[3];\n  thread_local Block* localFree[3];\n  int size_to_index(size_t s);\n  void* allocate(size_t sz); // uses localFree, spills to globalFree with CAS\n  void free(void* p, size_t sz); // pushes to localFree, retires via hazard pointers\n};\n``` \n\n## Follow-up Questions\n- How would you measure fragmentation and reclaim efficiency under churn?\n- How would you adapt this for true multi-socket coherence with high core counts?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-3",
        "questionId": "q-12160",
        "question": "Design a crash-consistent in-memory B+tree intended to live on persistent memory (PMEM/NVMe) with a write-ahead log. Provide data layout, mutation protocol, and a recovery procedure after power loss. Explain how you ensure durability with minimal flush traffic, and outline a micro-benchmark to measure recovery time and throughput under mixed workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a cache-friendly transpose with 16x16 tiling: for r,c in steps, copy A[i][j] to B[j][i] within each tile, clipping bounds at edges. This increases L1 reuse and reduces cache misses; final co",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a circular log in PMEM with per-entry header {len, seq, committed}. Enqueue writes payload, then header; flush order with clwb/siws and mfence; consumer validates header and reads payload un",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Detect overlap and copy direction (src < dst ? backward : forward). Use AVX2 to copy 32/64-byte chunks, aligning when possible. For blocks above 256B, employ non-temporal stores to minimize cache poll",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a crash-consistent PMEM B+tree with a per-operation redo log. Data layout: persistent nodes with keys and child offsets; a per-thread log tail for redo records. On mutating: append log (op, ke",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests real-world crisis-safe data structures on PMEM and requires a durable protocol, recovery, and low flush overhead.\n\n## Key Concepts\n- Crash consistency on persistent memory\n- Write-ahead logging and durable pointers\n- Minimal flush traffic via in-log commits\n- Recovery by replaying a redo log\n- Atomicity with per-thread logs and commit tokens\n\n## Code Example\n```javascript\n// Simplified redo entry and apply flow (high level)\nstruct Redo { uint64_t op; uint64_t key; uint64_t old_ptr; uint64_t new_ptr; }\nfunction mutate(node, redo) {\n  // append redo, flush, then apply and commit\n}\n```\n\n## Follow-up Questions\n- How would you recover if a crash occurs during log tail flush?\n- How would you validate durability guarantees with power-failure tests?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-4",
        "questionId": "q-18829",
        "question": "Implement a lock-free, multi-thread barrier for a 2-socket NUMA server that minimizes stall time; use per-thread flags and a generation counter, leveraging a sense-reversing pattern; provide wait() pseudo-code with memory order hints and a microbenchmark plan comparing against pthread_barrier; discuss NUMA effects and cache-pauses?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a crash‑safe PMEM ring buffer with two‑phase publish: write the data to the next slot, flush the data, then publish by updating a durable tail with a release store and a fence (sfence/clwb). Use p",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-thread bump allocator: store base and offset; alignment: off = (offset + 7) & ~7; if off + sz <= ARENA_SIZE then return (void*)(base + off); offset = off + sz; else NULL. reset() sets offset=0. No",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a sense-reversing barrier using a shared generation counter and per-thread flags padded to 64 bytes to prevent false sharing. Each thread toggles its local sense, sets its arrival flag, and spins on the genera…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a per-slot sequence counter approach with a crash-consistent protocol. The producer writes payload data, updates the descriptor, sets the sequence counter to ready, then flushes to NVRAM using CLFLUSHOPT follo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates low-level synchronization mastery and NUMA awareness critical for high-performance computing and latency-sensitive systems.\n\n## Key Concepts\n- **Lock-free barriers**: Provide wait-free guarantees using atomic operations instead of locks, eliminating lock contention\n- **Sense-reversing pattern**: Each thread tracks its own sense (0/1) and waits for the generation counter to match, eliminating the need to reset counts between barrier phases\n- **Memory ordering**: Proper __ATOMIC_ACQUIRE/__ATOMIC_RELEASE semantics ensure visibility across CPU sockets without unnecessary memory fences\n- **NUMA effects**: Cross-socket memory access introduces significant latency (100-200ns) due to QPI/Infinity Fabric interconnects. Padding flags to cache line size (64 bytes) prevents false sharing, which would cause cache line ping-ponging between sockets and destroy performance\n- **Cache coherency pauses**: On MESI-based systems, spinning on a shared counter can cause cache line migrations; the sense-reversing pattern localizes spinning to per-thread state where possible\n\n## Microbenchmark Plan\n1. Measure latency per thread across varying thread counts (2-64) on a 2-socket NUMA system\n2. Compare against pthread_barrier using thread_getcpu() to verify cross-socket placement\n3. Profile cache misses and QPI traffic via perf stat\n4. Measure barrier synchronization time variance across multiple iterations",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-5",
        "questionId": "q-5625",
        "question": "Design a lock-free, bounded object pool for fixed-size 128-byte objects to be used in a high-throughput network path on a 2-socket x86-64 server. Provide APIs pool_create(capacity), pool_alloc(), pool_free(). Use hazard pointers or epoch-based reclamation, per-thread caches, and cacheline-aligned blocks. Explain ABA avoidance, memory ordering, and how you would test under contention and backpressure?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a minimal spinlock in C using atomic_flag with lock, unlock, and trylock APIs. The lock function should use `while (atomic_flag_test_and_set(&flag)) { pause; }`, unlock should use `atomic_flag_clear(&flag)`, w…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-core ring buffers, fixed-size slots, variable payloads in a shared pool. Enqueue uses CAS on tail; ABA avoided via per-slot sequence counters. Reclaim with epoch-based GC tied to quiescent state.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a lock-free, bounded object pool for 128-byte objects on a 2-socket system. API: pool_create(capacity), pool_alloc(), pool_free(). Use per-thread caches, 64-byte aligned blocks, hazard pointers",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a crash-consistent PMEM allocator for a fixed 1MB arena with 128-byte blocks. Each block includes a header storing `next_offset` and `in_use` metadata. The allocator maintains a persistent global head pointer. Al…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical mastery of non-blocking memory reclamation, ABA avoidance, and performance on multi-socket systems.\n\n## Key Concepts\n\n- Lock-free pools with per-thread caches\n- Hazard pointers vs epoch-based reclamation\n- Cacheline alignment and false sharing\n- NUMA/2-socket contention and memory ordering\n\n## Code Example\n\n```javascript\n// Pseudo API\nfunction pool_create(capacity) { /*...*/ }\nfunction pool_alloc() { /*...*/ }\nfunction pool_free(obj) { /*...*/ }\n```\n\n## Follow-up Questions\n\n- How would you resize the pool at runtime?\n- Compare hazard pointers and epochs for this workload.",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-6",
        "questionId": "q-6125",
        "question": "Design a two-level lock-free telemetry pipeline for a multi-socket server: each core has a 512-slot, 64-byte ring buffer with per-slot sequence counters; a central Aggregator ring drains from all cores to a single Consumer that feeds a PCIe DMA buffer. Provide slot layout, enqueue/dequeue pseudo-code, memory fences, wrap-around handling, and backpressure. Explain memory reclamation (epoch/hazard pointers) and discuss cache-line pitfalls. Include a microbenchmark plan for 10+ Gbps bursts?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Intel TSX to wrap enqueue/dequeue in a small transaction: validate capacity, write payload, advance tail, and commit with _xend. If TSX aborts or capacity constraints are hit, fall back to a lock-free CAS-based path…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "An implementation would use per-thread free lists and an epoch-based reclamation scheme. Allocate from the thread-local cache; on empty, refill from a central pool with a batch fetch using CAS, taggin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Private per-core 128-entry TLB, 4KiB pages, 4-way set-associative. On miss, walk multi-level page tables, then install using atomic ops. Use a scalable eviction (pseudo-LRU). Invalidate entries across",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-core rings: 512 slots, 64 bytes each, with per-slot 64-bit seq; central Aggregator ring drains into a single Consumer. Enqueue uses CAS on seq, then stores payload and updates seq with a release s",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests two-level lock-free design, cross-socket propagation, and memory reclamation under real-time loads.\n\n## Key Concepts\n- Lock-free rings with per-slot sequence counters\n- Two-level buffering: per-core + aggregator\n- Memory ordering: acquire/release fences, CAS\n- Reclamation: epoch-based or hazard pointers\n- Cache-line locality and false sharing awareness\n- Backpressure handling and wrap-around correctness\n\n## Code Example\n```c\ntypedef struct { uint64_t seq; uint8_t payload[56]; } slot_t;\ntypedef struct { slot_t slots[512]; uint64_t head, tail; } ring_t;\n``` \n\n## Follow-up Questions\n- How would you scale to 1024+ cores while keeping fairness?\n- What padding strategy prevents false sharing in the two-level design?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-7",
        "questionId": "q-21381",
        "question": "**DESIGN CHALLENGE**: Build a NUMA-aware, kernel-bypass, zero-copy producer-consumer queue for a high-throughput network stack across two sockets. Support many producers/consumers with bounded capacity, per-slot sequence counters to avoid ABA, and a lock-free path with a small fallback. Provide API prototypes and minimal pseudo-code for enqueue/dequeue, and discuss cache-line alignment and memory ordering?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage per-slot sequence numbers and a compact PMEM log. Enqueue: write data to PMEM, flush, publish tail with a release store and memory fence; dequeue validates sequence, advances head with atomic",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a crash‑consistent, lock‑free object pool using per‑thread local caches backed by a central persistent free list. The central pool maintains a CAS‑based head pointer to a singly linked free list stored in pers…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Allocate per-core, cache-aligned freelists of 64B blocks; keep a small per-core cache (e.g., 128 blocks). On allocate, pop from local freelist; on miss, batch-steal from a neighboring core or fetch fr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed answer: API prototypes for enqueue/dequeue; use per-slot sequence counters to detect ABA; lock-free tail/head CAS with memory_order_acq_rel and a guarded fallback path for full/empty; pad to",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real‑world, low‑level queue design under NUMA and kernel-bypass constraints. Evaluates API clarity, memory ordering discipline, and practical safeguards against ABA and false sharing.\n\n## Key Concepts\n\n- NUMA affinity and cross-socket latency\n- Kernel-bypass, zero-copy semantics\n- Per-slot sequence counters to prevent ABA\n- Cache-line alignment and padding to avoid false sharing\n- Memory ordering: CAS on tail/head with acq_rel fences\n\n## Code Example\n\n```c\n// Prototype definitions\ntypedef struct { void* data; unsigned long seq; } slot_t;\ntypedef struct {\n  slot_t* slots;\n  size_t cap;\n  _Atomic size_t head;\n  _Atomic size_t tail;\n} queue_t;\n\nbool enqueue(queue_t* q, void* item);\nbool dequeue(queue_t* q, void** item);\n```\n\n```c\n// Minimal pseudo-code (high level)\nsize_t t = atomic_load_explicit(&q->tail, memory_order_acquire);\nsize_t h = atomic_load_explicit(&q->head, memory_order_relaxed);\nif (t - h >= q->cap) return false; // full\nslot_t* s = &q->slots[t & (q->cap-1)];\nunsigned long expected = s->seq;\nif (CAS(&s->seq, expected, expected+1)) {\n  s->data = item;\n  atomic_store_explicit(&q->tail, t+1, memory_order_release);\n  return true;\n}\n```\n\n## Follow-up Questions\n\n- How would you test determinism under sustained load and cross-socket traffic?\n- What backpressure strategy would you apply when producers outpace consumers?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-8",
        "questionId": "q-4241",
        "question": "Design a fixed-size-object allocator for a 4-core NUMA machine that minimizes inter-core traffic. Implement per-core caches, a small global cache, non-blocking refill of core caches, and safe bulk release during teardown. Explain cache-line alignment and false sharing avoidance, how you reclaim freed objects, and how you'd benchmark latency under bursty allocations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-core slab allocator for 64-byte messages on a 2-socket host. Use per-core caches, a lock-free central freelist, and batch refills to minimize cross-CPU traffic. Provide APIs: void* alloc(size_t),",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-thread arenas with a private 256-slot cache and a global central cache. Implement a lock-free freelist using CAS with a tag (ABA prevention). Allocate from thread cache; refill from central ca",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-core caches hold 64 objects each, cache-line aligned to 64 bytes; a global lock-free freelist handles cross-core refills in batches (e.g., 64). Fast path alloc/free uses local cache with relaxed a",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "A lock-free, bounded task queue with work-stealing for a 2-socket x86-64 server. Each worker maintains a private deque with local push and shared steal endpoints. Producers push to their local queue; if full, they fallb…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical design choices for low-latency allocators in multi-core, NUMA contexts, including cache locality, lock-free synchronization, and teardown safety.\n\n## Key Concepts\n\n- Per-core caches for locality\n- Global lock-free freelist for cross-core refills\n- Batch operations to amortize synchronization cost\n- Cache-line padding to avoid false sharing\n\n## Code Example\n\n```c\n// Pseudo: per-core cache node and refill logic\ntypedef struct Object { struct Object *next; } Object;\ntypedef struct { _Atomic(Object*) head; } FreeList;\n\n#define BATCH 64\n\nObject* allocate(FreeList *local, FreeList *global){\n    Object* head = atomic_load_explicit(&local->head, memory_order_acquire);\n    if(head){\n        local->head = atomic_fetch_sub_explicit(&local->head, NULL, memory_order_acquire);\n        return head;\n    }\n    // refill from global (simplified)\n    for(int i=0;i<BATCH;i++){\n        Object* o = pop_global(global);\n        if(!o) break;\n        push_local(local, o);\n    }\n    return allocate(local, global);\n}\n\nvoid deallocate(FreeList *local, Object *o){\n    push_local(local, o);\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt this for varying object sizes and allocator fragmentation?\n- What microbenchmarks would validate latency under high contention and NUMA effects?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-9",
        "questionId": "q-694",
        "question": "Design a cache-friendly, per-thread deque work-stealer for a multi-core task executor. Each worker maintains a fixed-size ring buffer for bottom push/pop; thieves steal from the top of other workers via CAS on a top index with a version counter. Explain ABA avoidance, memory ordering, and padding to avoid false sharing. Provide precise pseudo-code and a realistic burst workload scenario?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose per-thread deques with a bottom push/pop and a shared top for steals. Local path uses a fixed-size, cache-aligned ring; steals CAS on top with a version tag to prevent ABA; pad structures to p",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "An implementation would use per-thread free lists and an epoch-based reclamation scheme. Allocate from the thread-local cache; on empty, refill from a central pool with a batch fetch using CAS, taggin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Private per-core 128-entry TLB, 4KiB pages, 4-way set-associative. On miss, walk multi-level page tables, then install using atomic ops. Use a scalable eviction (pseudo-LRU). Invalidate entries across",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Initialize a Block array where each block stores a next index (free-list pointer). Set free_head to 0 and link all blocks in a chain during init_pool. The allocate_block function atomically pops the head using CAS and r…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design scalable, non-blocking schedulers and reason about ABA, memory ordering, and cache effects.\n\n## Key Concepts\n- Work-stealing deques\n- ABA avoidance with version counters\n- Cache-line padding to avoid false sharing\n- Memory order: acquire/release\n- Backoff and fairness under bursty loads\n\n## Code Example\n```javascript\n// Pseudo-code: per-thread deque with local and steal paths\nclass Deque {\n  constructor(cap) { /* ... */ }\n  pushBottom(x) { /* O(1) with cache-friendly indices */ }\n  popBottom() { /* ... */ }\n  stealTop(owner) { /* CAS on top with version, then claim slot */ }\n}\n```\n\n## Follow-up Questions\n- How would you test tail latency under bursty arrival rates?\n- How would you adapt the design for multi-socket NUMA and cache-coherent interconnects?\n- How would you extend to prioritized tasks or dynamic resizing of ring buffers?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-10",
        "questionId": "q-15193",
        "question": "Design a lightweight Single-Producer Single-Consumer (SPSC) queue using a fixed-size circular buffer for a high-frequency sensor data path. Provide enqueue/dequeue pseudocode, ensure wrap-around is correct, and explain why minimal synchronization with atomic head/tail suffices without locks. Include a minimal test plan to verify correctness under bursty input and drift between producer and consumer?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a deterministic, lock-free object pool allocator for fixed-size 128-byte objects on a 2-socket x86-64 system with per-thread fast-path free lists, central cross-thread pool, and hazard-pointer-based reclamation w…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a fixed-size ring with atomic head and tail indices. Enqueue: spin until (tail-head) < cap; write item to buf[tail%cap]; tail = tail+1 with memory_order_release. Dequeue: spin until (tail-he",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-core logs minimize contention; mutations append to local segments, then a durable epoch fence persists metadata. Recovery replays per-core segments to rebuild the index; Bloom filters speed lookup",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use four accumulators (a,b,c,d). Loop i+=4; prefetch arr[i+256]; accumulate a+=arr[i], b+=arr[i+1], c+=arr[i+2], d+=arr[i+3]. Finalize by adding a,b,c,d. Ensure arr is 64-byte aligned and compile with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of a practical low-level concurrency primitive (SPSC) and memory ordering without locks in a real data-path scenario.\n\n## Key Concepts\n- Ring buffer wrap-around and capacity checks\n- SPSC invariants: single producer advances tail, single consumer advances head\n- Atomic head/tail with acquire/release semantics\n- Correct visibility guarantees without heavy synchronization\n\n## Code Example\n```javascript\n// Pseudo-code for SPSC queue\ntypedef struct { int data; } Item;\n#define CAP 1024\nItem buf[CAP];\nstd::atomic<size_t> head(0), tail(0);\n\nbool enqueue(Item it){\n  size_t h = head.load(std::memory_order_acquire);\n  size_t t = tail.load(std::memory_order_relaxed);\n  if (t - h >= CAP) return false; // full\n  buf[t % CAP] = it;\n  tail.store(t+1, std::memory_order_release);\n  return true;\n}\n\nbool dequeue(Item* out){\n  size_t h = head.load(std::memory_order_relaxed);\n  size_t t = tail.load(std::memory_order_acquire);\n  if (t - h == 0) return false; // empty\n  *out = buf[h % CAP];\n  head.store(h+1, std::memory_order_release);\n  return true;\n}\n```\n\n## Follow-up Questions\n- How would you extend to multiple producers/consumers without stalls?\n- What tests would you add to detect drift or data loss under bursty input?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-11",
        "questionId": "q-684",
        "question": "Design a fixed-size ring buffer in C that stores bytes. Capacity N is a power of two (e.g., 1024). Show how to compute the next index using a mask (idx & (N-1)), and explain full vs empty detection using only head and tail counters. Provide enqueue and dequeue logic for a single-producer/single-consumer scenario?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Durable, lock-free SPMC queue backed by persistent memory. Use per-slot sequence counters, cache-line-aligned slots, and a two-phase commit: write payload, flush fence, then publish sequence. Consumer",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a durable, lock-free inter-process queue backed by non-volatile RAM using a log-structured append architecture with per-producer segments. Enqueue operations flush writes to NVRAM before advancing a shared durabl…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use two indices: head and tail. Let mask = N-1. Enqueue: if ((head - tail) == N) return 0; data[head & mask] = value; head++; Dequeue: if (head == tail) return 0; *out = data[tail & mask]; tail++; Thi",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a lock-free free-list encoded in a 64-bit head: lower 32 bits for the head block index, upper 32 bits for a version counter to defeat ABA. Initialize the arena into a singly linked free list. allo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of a practical, low-level data structure and its wrap-around behavior, plus efficient empty/full checks in a simple producer/consumer path.\n\n## Key Concepts\n\n- Ring buffers\n- Power-of-two masking\n- Empty/full checks with head/tail without extra state\n- SPSC memory ordering\n\n## Code Example\n\n```c\ntypedef struct {\n  size_t head, tail;\n  unsigned char data[N];\n} Ring;\n\nint enqueue(Ring *r, unsigned char value) {\n  if ((r->head - r->tail) == N) return 0; // full\n  r->data[r->head & (N - 1)] = value;\n  r->head++;\n  return 1;\n}\nint dequeue(Ring *r, unsigned char *out) {\n  if (r->head == r->tail) return 0; // empty\n  *out = r->data[r->tail & (N - 1)];\n  r->tail++;\n  return 1;\n}\n```\n\n## Follow-up Questions\n\n- How would you modify for multi-producer/multi-consumer?\n- What memory-ordering considerations arise on weakly-ordered architectures?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-12",
        "questionId": "q-27625",
        "question": "Design a crash-consistent, lock-free, inter-process ring buffer that uses both DRAM and PMEM for durable IPC. Assume a single producer and multiple consumers, N = 1<<20 slots, 128-byte payload, and per-slot sequence counters to avoid ABA. Describe the slot layout, atomic enqueue/dequeue operations with memory order and flush barriers, and a microbenchmark plan that stresses durability under simulated power loss?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt per-slot 64-bit sequence, 128-byte payload, and a tail pointer in PMEM. Producer reserves a slot with CAS, writes payload, updates its slot sequence, and CASes the tail with release semantics. E",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a crash-consistent lock-free MPSC (multiple producer, single consumer) queue for persistent memory using a ring buffer with N fixed-size slots. Each slot contains a 64-bit sequence number for ordering and a 56-by…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "A lock-free, bounded task queue with work-stealing for a 2-socket x86-64 server. Each worker maintains a private deque with local push and shared steal endpoints. Producers push to their local queue; if full, they fallb…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a spinlock with atomic_flag. Acquire via while (atomic_flag_test_and_set_explicit(&lock, memory_order_acquire)) { _mm_pause(); } Release with atomic_flag_clear_explicit(&lock, memory_order_r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes low-level queues that must survive power loss, combining DRAM and PMEM. It tests understanding of crash-consistency, memory fencing, and practical IPC design.\n\n## Key Concepts\n\n- Crash-consistency with per-slot sequencing\n- PMEM flush semantics (clwb/pc flushes, sfence)\n- Lock-free progress and ABA avoidance\n- Memory ordering for inter-process communication\n- Microbenchmark design for durability under fault injections\n\n## Code Example\n\n```javascript\n// Implementation code here\n```\n\n## Follow-up Questions\n\n- How would you optimize for batch enqueues/dequeues to improve throughput?\n- What strategies handle wraparound and backpressure among multiple consumers?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-13",
        "questionId": "q-21419",
        "question": "In C, implement a one-producer/one-consumer fixed-size circular buffer for 64-byte messages. Use N = 1<<12 slots. Provide enqueue and dequeue pseudocode, including how to handle full/empty without locks; discuss memory ordering and how you would validate correctness with a simple multi-iteration microbenchmark measuring latency and any dropped messages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-thread arenas with a private 256-slot cache and a global central cache. Implement a lock-free freelist using CAS with a tag (ABA prevention). Allocate from thread cache; refill from central ca",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Key approach: per-thread caches of 4 slots for 64-byte objects; on miss, bulk refill 16 objects from the global pool with a single CAS; on free, push to local cache and refill global in batches when f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a 1MB fixed-size pool of 64-byte blocks with a lock-free freelist. Each free block reuses its first word as the next pointer; the pool head is an atomic pointer. Allocate pops the head via CAS",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "One-producer/one-consumer circular buffer for 64-byte messages in C. N = 1<<12 slots; head and tail in separate cache lines. Enqueue: if ((head+1)%N == tail) return FULL; copy to buf[head]; head=(head",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of a simple lock-free pattern (SPSC), cache locality, and correct full/empty handling without locks. It also probes basic validation strategies.\n\n## Key Concepts\n- Circular buffers\n- Single-producer/single-consumer synchronization\n- Memory ordering and barriers\n\n## Code Example\n```javascript\n// Pseudocode enqueue/dequeue for SPSC circular buffer\n```\n\n## Follow-up Questions\n- How would you extend to multiple producers/consumers?\n- What platform-specific optimizations would help on modern CPUs?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-14",
        "questionId": "q-10234",
        "question": "Design and implement a reusable user-space barrier for N threads using only atomic primitives (no pthread barriers). Provide C-like pseudo-code for barrier_enter(barrier, id) that returns when all threads reach the barrier, using a generation counter to avoid ABA. Explain memory_order implications and how you would test correctness with 4 threads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implementation uses two atomics: a 'count' (current thread count) and a 'generation' counter to avoid ABA. In barrier_enter(barrier, id), each thread atomically increments count with fetch_add; the last thread (when cou…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a bounded MPMC queue that avoids contention by partitioning slots into per-producer stripes (e.g., 8 stripes, 1<<10 slots each) and a single shared drain. Each producer atomically reserves a s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a 1MB arena partitioned into 64-byte blocks (16384 blocks). A 256-entry bitmap tracks free blocks; 0=free,1=used. pool_init() allocates arena and clears bitmap. pool_alloc() finds first 0 bit, set",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Encode a single 64-bit slot: high 32 bits = sequence, low 32 bits = payload. Init seq=0, payload=0. Sender CASes to publish (seq+1, payload) when current seq matches and payload is 0. Receiver uses ac",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nTests understanding of inter-thread synchronization without kernel primitives, and memory ordering.\\\\n\\n## Key Concepts\\n- Atomic operations, memory_order semantics\\\\n- Barrier synchronization, generation counters\\\\n- Spin-wait considerations and pause\\n\\n## Code Example\\n```c\ntypedef struct {\n  atomic_int count;\n  atomic_uint64_t gen;\n  int N;\n} barrier_t;\n\nvoid barrier_enter(barrier_t* b, int id) {\n  uint64_t g = atomic_load_explicit(&b->gen, memory_order_relaxed);\n  if (atomic_fetch_add_explicit(&b->count, 1, memory_order_acq_rel) == b->N - 1) {\n    atomic_store_explicit(&b->count, 0, memory_order_relaxed);\n    atomic_fetch_add_explicit(&b->gen, 1, memory_order_release);\n  } else {\n    while (atomic_load_explicit(&b->gen, memory_order_acquire) == g) {\n      _mm_pause();\n    }\n  }\n}\n```\n\\n## Follow-up Questions\\n- How would you ensure fairness if threads stall? \\n- How would you extend for multiple, consecutive barrier phases?\\n",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-15",
        "questionId": "q-13300",
        "question": "Design a persistent, lock-free, multi-producer, single-consumer ring buffer in shared memory (mapped file) that survives power loss. Each enqueue must ensure the slot is durably persisted before signaling success, using cache-line flush instructions and a per-slot sequence counter to avoid ABA. Describe slot layout, enqueue/dequeue pseudo-code with fences, and recovery steps after restart?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Key approach: per-slot fields: seq, data. Prods reserve a slot via CAS on tail, write data, flush data cache line, then publish by updating seq and tail with a durable fence. Consumer reads by parity-",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Initialize a buffer of N blocks (64 bytes each). The first 8 bytes store a next pointer; the remaining 56 bytes are payload. Head points to first free block. allocate() pops head, returns block+8; fre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enqueue writes to a slot and publishes a release-store to the slot’s seq; consumer uses an acquire-load to validate slot and then advances head. For crash-resilience, flush the slot data and its seq w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement a small harness: two threads increment their own 64-bit counters for 10M iterations. Provide two layouts: Unpadded with a single 64-bit field and Padded with an extra 64-byte pad. Ti",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests durability and cross-process coordination under crash scenarios using low-level memory hygiene.\n\n## Key Concepts\n- Durable memory ordering with CLWB/CLFLUSHOPT and sfence\n- Per-slot sequencing to avoid ABA\n- Shared memory persistence across crashes\n- Recovery algorithm from persistent metadata\n\n## Code Example\n```c\n// Minimal sketch\ntypedef struct { uint64_t seq; char data[256]; } slot_t;\ntypedef struct { slot_t slots[N]; uint64_t head, tail; } ring_t;\n\nuint64_t pos = __sync_fetch_and_add(&ring->tail, 1);\nslot_t *s = &ring->slots[pos % N];\nwhile (s->seq != pos) {}\nmemcpy(s->data, payload, len);\npersist(&s->data, len);\n__sync_synchronize();\ns->seq = pos + 1;\npersist(&s->seq, sizeof(uint64_t));\n```\n\n## Follow-up Questions\n- How would you adapt for variable-sized payloads?\n- How would you test durability under power loss and crash scenarios?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-16",
        "questionId": "q-5058",
        "question": "Design a NUMA-aware slab allocator for 64-byte objects on a 4-core, 2-socket system. Each core maintains a private freelist; allocations preferentially use local slabs; deallocations migrate cross-socket via a per-node buffer for batch transfer. Provide data structures, allocate/free pseudo-code, discuss locking-free design, cache-line alignment, and a microbenchmark plan comparing local vs cross-socket allocations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a four-size-class slab allocator supporting 32, 64, 128, and 256-byte objects. Each thread maintains per-size free lists. On a cache miss, fetch a full slab from a global lock-free pool using a compare-and-swa…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-core freelists with a lock-free stack; slabs 4KB, 64-byte slots; hot path uses thread-local cache; misses refill from NUMA-local pool; remote frees buffered per-node and migrated in bulk to minimi",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Two writers and one reader share a shm ring buffer with N=1<<16; each slot has a 64-bit seq and 128-byte payload. Producers CAS head, write payload, bump seq, release-store head. Consumer uses acquire",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-thread fast path for Pin/Unpin, a global atomic refcount, and an epoch-based retire queue with hazard pointers. Pin increments a per-thread local counter and a global refcount; Unpin decrements an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical NUMA locality, lock-free data structures, and cross-socket migration decisions under realistic constraints. It probes memory ordering, cache-line contention, and batch reclamation strategies.\n\n## Key Concepts\n- NUMA locality and cross-socket traffic\n- Lock-free freelists and per-core caches\n- Cache-line padding to avoid false sharing\n- Batch migration of remote frees\n- Memory ordering in hot paths\n\n## Code Example\n```c\n// Pseudo-structure for per-core cache (simplified)\ntypedef struct core_cache {\n  alignas(64) void *head; // atomic head of freelist\n  char padding[60];\n} core_cache_t;\n```\n\n## Follow-up Questions\n- How would you extend for variable-sized objects?\n- How would you measure scalability across more sockets and cores?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-17",
        "questionId": "q-20884",
        "question": "Implement a reusable spin barrier for N threads in C without relying on pthread_barrier. Provide a minimal API barrier_enter() and barrier_wait(), using atomics to coordinate arrivals with a generation counter, ensuring the barrier can be reused. Include memory-order details and a plan to avoid false sharing, plus a simple stress-test idea?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use two stdatomic: arrivals and generation. barrier_enter() atomically increments arrivals; if it reaches N, reset to 0 and increment generation. Each thread spins on generation until it changes, then",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use per-core freelists with 128-byte alignment and a shared pool. Allocate from the local list; on miss, pop a batch (e.g., 64 objects) from a global lock-free stack using a tagged CAS to avoid ABA; p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Lockless Cross-Thread Config Exchange Design ### Data Structures ```c #define BUFFER_SIZE 64 typedef struct { uint8_t data[BUFFER_SIZE]; uint32_t crc; } ConfigBuffer; typedef struct { ConfigBuffer buffers[2]; _Atomic…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a bounded lock-free pool: arena of N blocks (128B each). Free-list head is a 64-bit atomic: upper bits = index, lower bits = version. Each block stores next index. alloc_block uses atomic_co",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe barrier tests fundamental atomic sequencing and memory-order choices without OS primitives.\n\n## Key Concepts\n- Barrier reuse with generation counter\n- Atomic increments and acquire/release semantics\n- False sharing avoidance via padding\n- Stress testing for thread skew and latency\n\n## Code Example\n```javascript\n// Pseudo:\nvolatile int arrivals; // atomic\nvolatile int generation; // atomic\n\nvoid barrier_enter(int N) {\n  int a = atomic_fetch_add_explicit(&arrivals, 1, memory_order_acq_rel);\n  if (a + 1 == N) {\n    atomic_store_explicit(&arrivals, 0, memory_order_relaxed);\n    atomic_fetch_add_explicit(&generation, 1, memory_order_release);\n  }\n  int g = atomic_load_explicit(&generation, memory_order_acquire);\n  while (g == atomic_load_explicit(&generation, memory_order_acquire)) { ; }\n}\n```\n\n## Follow-up Questions\n- How would you extend for dynamic N?  - How to measure contention and fairness?",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-18",
        "questionId": "q-11305",
        "question": "Design and implement a lock-free, bounded MPSC ring for high-rate inter-thread messaging on a 2-socket NUMA server. Use N=1<<18, 64-byte slots, and a per-slot 64-bit sequence counter to avoid ABA. Producers fetch tail, derive slot s = tail & (N-1), spin until slots[s].seq == tail, write payload, then publish by setting slots[s].seq = tail+1. Consumer reads when slots[head&(N-1)].seq == head+1, then advances head. Propose a minimal C-like API and a microbenchmark plan to validate throughput and latency under 4/8/16 producers and varying NUMA distances?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two-phase, durable enqueue/dequeue using a circular buffer in NVRAM. On enqueue: write payload, publish via a release-store to head and flush the payload region once; on dequeue: read data, advance ta",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a lock-free free-list encoded in a 64-bit head: lower 32 bits for the head block index, upper 32 bits for a version counter to defeat ABA. Initialize the arena into a singly linked free list. allo",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage per-size-class slabs of 64-byte-aligned blocks, with lock-free free-lists and per-thread caches to minimize cross-NUMA traffic. Use atomic CAS on a head-pointer for pop/push; implement ABA-fr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a lock-free MPSC ring with N=1<<18 slots, each 64 bytes and a 64-bit per-slot sequence counter. Producers atomically fetch_tail, compute s = tail & (N-1), spin until slots[s].seq == tail, wr",
            "isCorrect": true
          }
        ],
        "explanation": "Why This Is Asked\n- Tests mastery of lock-free design, memory ordering, and NUMA-awareness in practical inter-thread communication.\n\nKey Concepts\n- Lock-free MPSC rings with per-slot sequencing to prevent ABA\n- Cache-line alignment and padding to avoid false sharing\n- Correct wrap/threshold handling and epoch-based wraparound\n- Memory_order_acquire/release semantics on producers and consumer\n\nCode Example\n```c\ntypedef struct {\n  uint64_t seq;         // per-slot sequence counter\n  uint8_t  payload[64]; // cache-line aligned payload\n} slot_t;\n\ntypedef struct {\n  slot_t *slots;\n  size_t N;               // power-of-two\n  _Atomic uint64_t tail;    // producer index\n  _Atomic uint64_t head;    // consumer index\n} pcq_t;\n\nint pcq_push(pcq_t *q, const void *payload);\nint pcq_pop(pcq_t *q, void *out);\n```\n\nFollow-up Questions\n- How would you extend to multiple consumers?\n- How would you measure tail latency and backpressure under bursty producers?\n- What are the failure modes if the cache-line is misaligned and how do you fix them?",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-19",
        "questionId": "q-8922",
        "question": "Design a bounded, single-producer single-consumer queue for a real-time video path. Each slot is 64B; use a per-slot 64-bit sequence counter to avoid ABA, with N=1<<12. Provide init, enqueue, dequeue in C-like pseudocode (memory_order_release/acquire as appropriate) and explain how you prevent false sharing and validate correctness under bursty traffic?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "```c #include <stdint.h> #include <string.h> #include <stdatomic.h> #include <pthread.h> #include <stdio.h> #include <time.h> #include <stdlib.h> #define ARENA_SIZE (256 * 1024) #define BLOCK_SIZE 16 #define NUM_BLOCKS…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a durable, lock-free inter-process queue backed by non-volatile RAM using a log-structured append architecture with per-producer segments. Enqueue operations flush writes to NVRAM before advancing a shared durabl…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a circular buffer of N slots, each 64B with a 64-bit sequence counter. Initialize each slot's sequence to its index. The producer writes payload data, then updates the tail pointer and increments the slot's sequence…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Answer sketch: implement a 256-slot software timer wheel in C. Each slot contains an atomic lock-free list of Timer nodes. Compute target_slot = (delay_ms / granularity) & 0xFF and push the timer onto",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical knowledge of low-level concurrency beyond mutexes, requiring precise memory ordering, ABA avoidance, and cache-line awareness.\n\n## Key Concepts\n\n- SPSC queues, ring buffers, per-slot sequence counters\n- Memory ordering: acquire/release semantics\n- False sharing and padding\n- Correctness under wrap-around and burst traffic\n\n## Code Example\n\n```c\ntypedef struct { uint64_t seq; uint8_t data[64]; } Slot;\n#define N (1<<12)\nSlot q[N];\nvolatile uint64_t head=0, tail=0;\n\nvoid enqueue(void* payload){ /* producer path with release store to slot and tail update */}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "low-level-q-20",
        "questionId": "q-29052",
        "question": "Design a NUMA-aware, deterministic memory allocator for a long-running service. It should use per-thread arenas with thread-local caches, a size-class pool, and guarantee bounded worst-case allocation latency under contention. Provide API prototypes and minimal pseudo-code for allocate/free, discuss fragmentation, cross-socket penalties, and a microbenchmark to measure tail latency as thread count scales?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement slab_alloc() and slab_free(ptr). The pool has 256 blocks of 64 bytes; the free list uses a 32-bit index stored at the start of each free block. Keep a head index and a used counter. slab_all",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a fixed-capacity MPMC ring with 128-byte slots, cache-line aligned, and per-slot 64-bit version counters. Enqueue reserves a slot by atomically bumping tail, writes data, updates the version, flus",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a lock-free MPSC ring with N=1<<18 slots, each 64 bytes and a 64-bit per-slot sequence counter. Producers atomically fetch_tail, compute s = tail & (N-1), spin until slots[s].seq == tail, wr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Key design highlights: per-thread arenas with local caches, size-class slabs, a central lock-free free-list for cross-thread returns, and NUMA-aware local allocations by preferring local nodes. Mainta",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssess low-level memory allocator design under NUMA, focusing on determinism, cache locality, and latency guarantees.\n\n## Key Concepts\n- NUMA locality, per-thread arenas, size classes\n- Lock-free free lists, slow path, fragmentation control\n- Deterministic latency and microbench measurement\n\n## Code Example\n```c\n// Pseudo API\nvoid* allocate(size_t size);\nvoid deallocate(void*ptr);\n```\n\n## Follow-up Questions\n- How would you measure tail latency under varied thread counts and allocation sizes?\n- How would you prevent fragmentation over long runtimes?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-21",
        "questionId": "q-17536",
        "question": "Design a NUMA-friendly, lock-free allocator for tiny allocations 8-64 bytes on a 4-socket server. Each thread maintains a small, cache-line-aligned per-size cache; a central pool refills caches in batches. Provide API prototypes, discuss avoiding cross-core cache thrashing, memory ordering, and thread-exit cleanup. Include pseudo-code for allocate/deallocate paths and a microbenchmark plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-thread caches organized by size class (8, 16, 32, and 64 bytes), each cache line-aligned to prevent false sharing. The allocation path first checks the thread-local cache; on a miss, refill from a central…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use thread-local storage to hold a pool of 1024 blocks (128 bytes each). Each block has a next pointer; a per-thread free-list is formed. allocate() pops from the list, free() pushes back. If the thre",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "FAST PATH: thread-local freelists for 8/16/32/64 B; allocate from cache, push to freelist on free. SLOW PATH: central pool with lock-free free-list per size class. Use tagged pointers to avoid ABA; al",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a sense-reversal barrier with 64-byte-aligned per-thread slots and a shared generation counter. **barrier_enter:** each thread writes its sense bit to its slot and performs an atomic fetch_add on the arrivals counte…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates understanding of NUMA-aware memory management, lock-free concurrency patterns, and performance optimization for high-throughput systems. It requires reasoning about cache-line tiling, per-thread caches, batch transfers, memory ordering guarantees, and thread lifecycle management.\n\n## Key Concepts\n\n- **NUMA locality**: Ensuring memory allocations reside on the same NUMA node as the requesting thread to minimize remote memory access latency\n- **Cache-line alignment**: Aligning per-thread data structures to 64-byte cache lines to prevent false sharing between cores\n- **Per-thread caches by size class**: Maintaining separate caches for each allocation size (8, 16, 32, 64 bytes) to reduce fragmentation and improve allocation speed\n- **Lock-free freelists**: Using compare-and-swap (CAS) operations to safely manipulate global free lists without locks\n- **Batch refills**: Transferring multiple blocks at once to amortize synchronization overhead\n- **Memory ordering**: Applying appropriate atomic operations (acquire/release semantics) to ensure visibility across cores\n- **Thread-exit cleanup**: Properly reclaiming thread-local allocations when threads terminate to prevent leaks\n\n## Code Example\n\n```c\n// C-like pseudocode for clarity\ntypedef struct Block {\n    struct Block* next;\n} Block;\n\ntypedef struct PerThreadCache {\n    Block* pools[4];        // 8, 16, 32, 64 byte caches\n    _Atomic int counts[4];  // Cache fill levels\n} PerThreadCache;\n\n// Cache-line-aligned to prevent false sharing\nalignas(64) PerThreadCache thread_caches[MAX_THREADS];\n```\n\n## Allocation Path (Pseudo-code)\n\n```c\nvoid* allocate(size_t size) {\n    int idx = size_to_index(size);\n    PerThreadCache* tc = get_thread_cache();\n    \n    if (tc->counts[idx] > 0) {\n        tc->counts[idx]--;\n        return tc->pools[idx];\n    }\n    // Cache miss: refill from global pool\n    return refill_and_allocate(tc, idx);\n}\n\nvoid* refill_and_allocate(PerThreadCache* tc, int idx) {\n    Block* batch = global_pool_extract(BATCH_SIZE);\n    if (batch == NULL) return NULL;\n    \n    // Push remaining to thread cache\n    tc->pools[idx] = batch->next;\n    tc->counts[idx] = BATCH_SIZE - 1;\n    return batch;\n}\n```\n\n## Deallocation Path (Pseudo-code)\n\n```c\nvoid deallocate(void* ptr, size_t size) {\n    int idx = size_to_index(size);\n    PerThreadCache* tc = get_thread_cache();\n    \n    Block* block = (Block*)ptr;\n    \n    if (tc->counts[idx] < CACHE_CAPACITY) {\n        block->next = tc->pools[idx];\n        tc->pools[idx] = block;\n        tc->counts[idx]++;\n    } else {\n        // Cache full: return excess to global pool\n        return_excess_to_pool(tc, idx);\n        block->next = tc->pools[idx];\n        tc->pools[idx] = block;\n        tc->counts[idx] = 1;\n    }\n}\n```\n\n## Avoiding Cross-Core Cache Thrashing\n\n1. **False sharing prevention**: Each per-thread cache is aligned to a full cache line (64 bytes), ensuring updates from different threads do not share the same cache line\n2. **NUMA-aware allocation**: The central pool maintains per-NUMA-node sub-pools; threads refill from their local node's pool\n3. **Batch transfers**: Moving blocks in bulk reduces the frequency of CAS operations and cache-line traffic\n4. **Thread affinity**: Binding threads to specific cores reduces migration and improves cache locality\n\n## Memory Ordering Considerations\n\n- Use `memory_order_acquire` when reading from the freelist to ensure visibility of the latest data\n- Use `memory_order_release` when returning blocks to the freelist\n- Use `memory_order_relaxed` for simple counters where ordering is not critical\n- The lock-free CAS loop provides implicit ordering through its retry mechanism\n\n## Thread-Exit Cleanup Strategy\n\n1. On thread exit, scan the thread's cache for non-empty pools\n2. Return all available blocks to the global pool using CAS\n3. Use a reference-counting scheme or epoch-based reclamation to safely free blocks that may still be in use by other threads\n4. Register a thread-local destructor that performs cleanup automatically\n\n## Microbenchmark Plan\n\n1. **Baseline comparison**: Measure `malloc`/`free` vs. custom allocator throughput\n2. **Single-threaded throughput**: Allocate/deallocate in a tight loop, measure cycles per operation\n3. **Multi-threaded contention**: Run N threads concurrently, measure throughput and scalability\n4. **NUMA locality test**: Compare performance when threads are bound to local vs. remote NUMA nodes\n5. **Cache behavior analysis**: Use hardware performance counters (L3 misses, false sharing events)\n6. **Allocation size distribution**: Test with varying proportions of each size class\n7. **Thread migration impact**: Measure performance when threads move between cores\n8. **Warmup vs. steady-state**: Compare cold-start performance against warmed caches",
        "difficulty": "intermediate"
      },
      {
        "id": "low-level-q-22",
        "questionId": "q-13768",
        "question": "Design a crash-consistent, lock-free, multi-producer single-consumer queue for persistent memory (NVRAM). It must support bounded capacity N (power of two), allow multiple producers and a single consumer, and survive power loss with no data corruption. Propose the on-NVRAM layout, API prototypes (init, enqueue, dequeue, recover), and minimal pseudocode using CLWB/CLFLUSHOPT or msync barriers. Explain ABA avoidance, durability guarantees, and recovery steps. Outline a microbenchmark comparing crash-free vs crash-injected latency and throughput?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-thread cache of 32 slots; on alloc, pop from local cache; if empty, atomically refill 32 items from a global lock-free free-list using CAS. On free, push to local cache; if local is full, flush the batch to the glob…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two-phase, durable enqueue/dequeue using a circular buffer in NVRAM. On enqueue: write payload, publish via a release-store to head and flush the payload region once; on dequeue: read data, advance ta",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a log-structured PMEM allocator: append allocations to a durable log, then atomically update a per-block header (version, CRC). Maintain a free-block bitmap and a per-thread cache. Recovery re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-thread fast path for Pin/Unpin, a global atomic refcount, and an epoch-based retire queue with hazard pointers. Pin increments a per-thread local counter and a global refcount; Unpin decrements an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess practical crash-consistency and persistent memory design in high-concurrency paths.\n\n## Key Concepts\n\n- Persistent memory semantics and durability\n- Lock-free progress with ABA avoidance\n- Recovery semantics and journaling\n\n## Code Example\n\n```pseudo\n// Pseudocode: enqueue\nslot = head & (N-1)\nwrite payload to slot\nwrite seq = head_seq+1\nflush(slot)\nhead = head+1\nflush(head)\n```\n\n## Follow-up Questions\n\n- How would you validate recovery correctness after power loss?\n- What are trade-offs of batching flushes vs per-operation flushes?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-23",
        "questionId": "q-5215",
        "question": "Design a bounded, lock-free work-stealing queue (WSQ) for a fixed thread pool of N workers. Each worker has a local deque; producers push to their own tail; workers pop from their own tail; idle workers steal from other heads. Provide pseudocode for push, pop, steal; discuss ABA avoidance, memory reclamation (hazard pointers or epochs), and cache-line layout to minimize false sharing. Include a concrete stress-validation plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a 64-slot SPSC ring buffer using per-slot 8-bit flags. Producer waits for flag==0, writes the 64-byte payload into slot i = tail & (N-1), then stores flag=1 with release semantics. C",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a fixed-size pool with a free-list embedded in the blocks. pool_init links blocks[0..N-1]; pool_alloc pops the head and returns the payload; pool_free pushes the block back. Align to 8 bytes; avoi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Envision per-thread ring with capacity N; threads CAS‑take a slot to enqueue; flush_to_global batches slots into a global sink via an atomic swap of a filled block. Use per-slot sequence counters to a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a bounded, lock-free WSQ with per-thread deques. Push to local tail; pop from local tail; thieves steal from heads. Use versioned pointers or tagged CAS to avoid ABA; apply hazard pointers or",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design high-throughput concurrent queues under contention, covering memory reclamation, false sharing, and backpressure—critical for scalable data platforms at large-scale storage and analytics workloads.\n\n## Key Concepts\n- Lock-free queue basics, per-thread locality, and work-stealing semantics\n- ABA avoidance via versioned pointers or sequence counters\n- Hazard pointers or epoch-based reclamation for memory safety\n- Cache-line alignment and padding to reduce false sharing\n- Bounded capacity and spill-over strategies for backpressure\n\n## Code Example\n```c\ntypedef struct node {\n  void* data;\n  _Atomic(struct node*) next;\n  int stamp; // version counter\n} node_t;\n\ntypedef struct wsq {\n  _Atomic(node_t*) head;\n  _Atomic(node_t*) tail;\n  // per-thread deques omitted for brevity\n} wsq_t;\n```\n\n## Follow-up Questions\n- How would you adapt WSQ design for NUMA and high-core-count machines?\n- What metrics would you collect to detect bottlenecks and starvation, and how would you validate scalability?",
        "difficulty": "advanced"
      },
      {
        "id": "low-level-q-24",
        "questionId": "q-20161",
        "question": "Design a cache-friendly, lock-free barrier for N threads on a 4-core system. Each thread writes its ID to a unique 64-byte-aligned slot and spins until a global generation counter indicates all threads have arrived. Provide pseudo-code for barrier_enter and barrier_wait, discuss memory_order choices and how to avoid false sharing, and outline a microbenchmark plan to measure scalability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a fixed-size ring with per-slot sequence counters and 64-byte frames. Enqueue reserves via CAS on tail, writes frame, bumps seq, and uses a release store for tail. Dequeue reads head, validates seq, copies frame, an…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-node shards with a global coordinator. Lock-free hot path: atomic upserts into per-node arrays using CAS, with 64-byte padding; memory order: acquire/release fences around reads/writes; cross-node",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a sense-reversing barrier with per-thread slots padded to 64 bytes to avoid false sharing. Each thread stores its arrival flag in a unique slot; the last arriving thread flips a generation counter. Threads spin on t…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Two writers and one reader share a shm ring buffer with N=1<<16; each slot has a 64-bit seq and 128-byte payload. Producers CAS head, write payload, bump seq, release-store head. Consumer uses acquire",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses the candidate's understanding of lightweight synchronization primitives, cache coherency effects, and memory ordering in concurrent programming.\n\n## Key Concepts\n- **Sense-reversing barrier**: A centralized synchronization mechanism where threads signal arrival by setting a flag, and the last thread flips a generation counter to release waiters\n- **False sharing prevention**: Padding each thread's slot to 64 bytes (cache line size) ensures threads do not contend on the same cache line\n- **Memory ordering**: Use relaxed ordering for per-thread arrival flags, and acquire/release semantics for the generation counter to ensure proper synchronization\n\n## Code Example\n```c\n// Pseudo-code for a cache-friendly, lock-free barrier\ntypedef struct { alignas(64) volatile int ready; } slot_t;\n\nslot_t slots[N];\nvolatile int generation = 0;\n\nvoid barrier_enter(int tid) {\n    slots[tid].ready = 1;  // Signal arrival with relaxed memory order\n    int gen = generation;  // Read current generation\n    \n    // Spin until all threads arrive and generation changes\n    while (generation == gen) { /* spin */ }\n}\n\nvoid barrier_wait(int tid) {\n    // Check if this is the last arriving thread\n    int arrived = 0;\n    for (int i = 0; i < N; i++) {\n        arrived += slots[i].ready;\n    }\n    \n    if (arrived == N) {\n        // Last thread: flip generation and reset slots\n        for (int i = 0; i < N; i++) slots[i].ready = 0;\n        generation++;\n    }\n}\n```\n\n## Follow-up Questions\n- How would you support dynamic N (varying thread counts)?\n- How do you handle potential spinning overhead on hyper-threaded cores?\n- How does this scale beyond 4 cores?\n- What are the trade-offs compared to hardware barriers like `__sync_synchronize()`?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-12T12:10:59.807Z",
    "version": 1
  }
]