[
  {
    "id": "test-ios",
    "channelId": "ios",
    "channelName": "IOS",
    "title": "IOS Knowledge Test",
    "description": "Test your understanding of IOS with 25 questions.",
    "questions": [
      {
        "id": "ios-q-0",
        "questionId": "q-10326",
        "question": "Design a beginner iOS SwiftUI app named 'PlantDiary' that logs plant care events (plantName, date, eventType: water/fertilize/prune), optional photo, and notes. Persist locally via Codable to a single JSON file in the Documents directory. Provide a weekly calendar-like overview showing per-day counts; ensure accessibility and Dynamic Type; include a minimal test plan and explain data model and persistence strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline: data model PlantCareEvent {id: UUID, plantName: String, date: Date, type: EventType, photoFilename: String?, notes: String?}; persist via Codable to PlantDiary.json in Documents directory; WeekView grid display…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Approach: local SQLite for Draft/Post; device key pair via CryptoKit; AES-GCM for encryption; Envelope with signature; offline queue and deterministic replay log; publish to a lightweight relay when o",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a BLE gateway to stream data into a thread-safe circular buffer; on sample burst, flush to SQLite via GRDB with LZ4 compression; store a replay log to reconstruct in crash; schedule BGTaskSchedule",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-document envelope encryption: each document has a symmetric key wrapped with the recipient's public keys; derive keys via HKDF-SHA256; store keys in Keychain. Encrypt annotations with the docu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates a candidate's ability to design a complete iOS app with local data persistence, SwiftUI fundamentals, and accessibility considerations at a beginner level.\n\n## Key Concepts\n- Codable data model with FileManager JSON persistence\n- MVVM architecture using @Published state management\n- SwiftUI List views and custom WeekView calendar component\n- Accessibility implementation with VoiceOver labels and Dynamic Type support\n\n## Code Example\n```swift\nstruct PlantCareEvent: Codable, Identifiable {\n  let id: UUID\n  let plantName: String\n  let date: Date\n  let type: EventType\n  let photoFilename: String?\n  let notes: String?\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-1",
        "questionId": "q-8092",
        "question": "Design and implement an offline-first iOS media sync feature: users capture photos in an album on one device and expect them to appear on all devices with conflict-aware CloudKit sync. Outline the Core Data model, CloudKit integration, conflict resolution (per-record versioning), and a robust background transfer plan (URLSession background tasks or BGTaskScheduler) that supports partial uploads and offline queuing. Include a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a tiny DSL parser and evaluator for feature flags. Use a rule like IF user.role == 'admin' AND appVersion >= 3 THEN enableNewUI. Store versioned rules locally (Realm/SQLite), stream patches",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a modular SyncEngine with per-anchor CRDT state and a compact transform pipeline; use WebRTC data channels for near-real-time sync with a relay fallback to a REST endpoint for outages; log every state mutation t…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use MVVM with a Recipe model, decode JSON into Recipe structs, and store favorites as a Set of ids in @AppStorage (UserDefaults). Bind the list to an @Published filtered array driven by a search strin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Core Data to model Album and Photo with id, assetURL, timestamp, isSynced, cloudRecordID, and version. Sync via CloudKit with per-record versioning; apply last-write-wins on conflicts but preserve",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests skills in offline-first design, distributed data sync, and iOS background processing across devices using Core Data and CloudKit. It checks conflict handling, performance, and test coverage.\n\n## Key Concepts\n- Core Data relational modeling for albums and photos\n- CloudKit integration patterns and per-record versioning\n- Conflict resolution strategies with user-visible edits preserved\n- Background transfers with URLSession/BGTaskScheduler and offline queues\n- Observability and test plans for multi-device sync\n\n## Code Example\n```swift\nstruct PhotoVersioned {\n  let id: UUID\n  let version: Int\n  let timestamp: Date\n}\n```\n\n## Follow-up Questions\n- How would you handle very large assets with partial transfers?\n- How would you measure and optimize cross-device sync latency?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-2",
        "questionId": "q-20838",
        "question": "Design a beginner iOS SwiftUI app named 'SnippetShelf' that fetches a list of code snippets from a REST API, caches results to disk with Codable in the app's Documents folder, supports pull-to-refresh and search by title or language, and displays each snippet with monospaced code and minimal syntax highlighting for common Swift keywords. Provide a minimal SwiftUI sketch for the list and detail views?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Uses a TelemetryPayload (timestamp, IMU, altitude, battery, health) and a Session. A 2s buffer feeds a Processor running a tiny CoreML detector plus a heuristic health rule. Persist via Core Data (SQL",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a Codable Expense struct with id, amount, category, date, receiptURL; persist via JSON to Documents with FileManager, load on startup; build a simple MVVM with @Published expenses and a weeklyT",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To implement: define a Codable Task { let id: UUID; var title: String; var completed: Bool }. Persist via JSON in UserDefaults using Data, encode/decode on save and on app launch. Use a SwiftUI List w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a SwiftUI List bound to a SnippetViewModel. Use URLSession to fetch JSON from /api/snippets, decode to Snippet{id,title,language,code}. Persist via Codable in Documents; refresh with a pull-",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of basic iOS data flow: networking, local persistence, and simple UI in SwiftUI. It exercises error handling, search, and a tiny code-highlighting feature without external deps.\n\n## Key Concepts\n- SwiftUI, Combine/async-await\n- URLSession, Codable\n- File-based caching (Documents), TTL\n- Simple text highlighting with attributed strings\n- Basic test plan\n\n## Code Example\n```swift\nstruct Snippet: Codable { let id: String; let title: String; let language: String; let code: String }\n```\n\n## Follow-up Questions\n- How would you scale to large code blocks and multiple languages?\n- How would you test the caching layer and search performance?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-3",
        "questionId": "q-6261",
        "question": "Build a beginner iOS SwiftUI app called QuickNotes that lets users create notes (title and body), saves them locally using a lightweight SQLite store via GRDB, and displays a searchable list with date filtering. Add accessibility: Dynamic Type, VoiceOver labels, and high-contrast mode. Outline data migrations and provide a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a client-side audit chain: Observation records signed with a device key, each with timestamp, location, notes; Evidence includes file hashes; Construct a Merkle tree over entries; store encryp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Persist notes using GRDB with a DatabaseQueue. Model NoteRecord(id, title, body, date). Expose a NotesViewModel with @Published notes, searchQuery, and dateFilter. Use a SwiftUI List with .searchable;",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Proposed answer: Use a modular MVP: ReplayKit for capture, WebRTC data channels with AES-GCM per-frame encryption via CryptoKit; rotate keys every few seconds and fall back to a relay server if direct",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt MVVM architecture: a Codable `Note` model, a `NotesStore` that persists to the Documents directory, and a SwiftUI `List` view. Each row displays a title and a color tag badge. For accessibility, set `accessibility…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical iOS basics: SwiftUI data flow, SQLite persistence with GRDB, and accessibility concerns. It also touches migrations and basic testing, which beginners can implement with guidance.\n\n## Key Concepts\n\n- SwiftUI data binding and state management\n- GRDB/SQLite data persistence and migrations\n- Searchable lists and date filtering\n- Accessibility: Dynamic Type, VoiceOver, high-contrast\n- Lightweight unit/UI testing strategies\n\n## Code Example\n\n```swift\nstruct Note: Identifiable {\n  let id: UUID\n  var title: String\n  var body: String\n  var date: Date\n}\n\nfunc notesForQuery(_ query: String, in notes: [Note]) -> [Note] {\n  let q = query.lowercased()\n  return notes.filter { $0.title.lowercased().contains(q) || $0.body.lowercased().contains(q) }\n}\n```\n\n## Follow-up Questions\n\n- How would you implement migrations and versioning in GRDB for evolving schemas?\n- How would you optimize search performance for large note datasets on-device?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-4",
        "questionId": "q-29152",
        "question": "In an iOS courier app for Instacart, design an on-device routing optimizer that uses a preloaded map graph and incremental traffic signals pushed from the cloud. Requirements: 1) a graph-based routing engine (A*), 2) Combine-based ingestion of streaming traffic events, 3) offline operation with crash-safe resume via checkpoints, 4) map updates signed with CryptoKit, 5) a SwiftUI dashboard showing current route, ETA, and confidence, 6) a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "The solution should wire an AVCaptureSession with metadataOutput for EAN/UPC, parse it into a Core Data Item (id, name, barcode, createdAt), and save on add. Use Combine to debounce a search publisher",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a directed graph stored locally in SQLite to represent the map. Run A* with a lightweight heuristic, incrementally updating edge weights as Combine streams traffic events. When weights are invalidated, trigger…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Core Data with entities Event and DailyBucket, storing per-event consent and a payloadHash. Persist lightweight events locally, accumulate into daily buckets, and upload in a background URLSession",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Record audio memos using AVAudioRecorder, save to Documents as a unique file, and write to a temp path first then move for atomic save. Build a Codable Memo struct with id, date, duration, path; list",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates offline-first routing with on-device compute, real-time edge-weight updates, crash safety, and cryptographic authenticity. It also tests SwiftUI/Combine integration and mobile performance optimization.\n\n## Key Concepts\n- **Graph algorithms**: A* with incremental weight updates\n- **Offline-first data store**: SQLite with write-ahead logging for durability\n- **Streaming ingestion**: Combine for processing traffic events\n- **Crash safety**: Checkpoints and state persistence for reliable resume\n- **Cryptographic verification**: CryptoKit for signed map updates\n- **SwiftUI dashboard**: Route visualization with ETA and confidence metrics\n\n## Code Example\n```swift\n// Graph structure\nstruct Node: Hashable { let id: String; let coordinate: CLLocationCoordinate2D }\nstruct Edge { let from: Node.ID; let to: Node.ID; var weight: Double }\n\nclass RoutingEngine: ObservableObject {\n    @Published var currentRoute: [Node] = []\n    @Published var eta: TimeInterval = 0\n    @Published var confidence: Double = 0\n\n    private var graph: AdjacencyList<Node.ID, Edge>\n    private var checkpoint: RouteCheckpoint?\n\n    // MARK: - A* with Checkpoint Resume\n    func computeRoute(from start: Node.ID, to end: Node.ID) {\n        let checkpoint = RouteCheckpoint(currentNode: start)\n        currentRoute = aStar(graph: graph, start: start, goal: end, checkpoint: checkpoint)\n        self.checkpoint = checkpoint\n        updateETA()\n    }\n\n    func invalidateEdges(_ edges: [Edge.ID]) {\n        guard let cp = checkpoint else { return }\n        currentRoute = aStar(graph: graph, start: cp.currentNode, goal: goal, checkpoint: cp)\n        updateConfidence()\n    }\n\n    // MARK: - Combine Traffic Ingestion\n    private var cancellables = Set<AnyCancellable>()\n\n    func subscribe(to publisher: AnyPublisher<TrafficEvent, Never>) {\n        publisher\n            .filter { $0.isSignificant }\n            .sink { [weak self] event in\n                self?.applyTrafficEvent(event)\n            }\n            .store(in: &cancellables)\n    }\n\n    private func applyTrafficEvent(_ event: TrafficEvent) {\n        graph.updateWeight(edgeId: event.edgeId, weight: event.delay)\n        invalidateEdges([event.edgeId])\n    }\n\n    // MARK: - Map Update Verification\n    func verifyAndApplyMapUpdate(_ update: MapUpdate) throws {\n        try CryptoKit.verify(update.signature, for: update.data)\n        try applyMapUpdate(update)\n        checkpoint = nil // Invalidate checkpoint after structural changes\n    }\n}\n```\n\n## Test Plan\n\n| Category | Test Case | Method |\n|----------|-----------|--------|\n| Routing | A* finds optimal path on static graph | XCTest with known graph |\n| Routing | Returns to checkpoint on weight update | Mock weight change, verify partial recomputation |\n| Streaming | Traffic events update weights correctly | Combine test publisher, assert graph state |\n| Offline | Route computed without network | Disable network, run computeRoute |\n| Crash Safety | Resume from checkpoint after simulated crash | Inject crash state, verify recovery |\n| Crypto | Reject tampered map updates | Modify signature, expect CryptoKit error |\n| UI | Dashboard reflects route/ETA/confidence | ViewInspector snapshot tests |",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-5",
        "questionId": "q-19146",
        "question": "In a beginner iOS SwiftUI app named 'ListForge', implement a local-first to-do management screen where tasks have title, dueDate, and priority, grouped by priority into sections. Support drag-and-drop reordering within and across sections, mark tasks complete, and persist all tasks via Codable to the Documents directory. Include a minimal SwiftUI view sketch and a basic test plan that covers encode/decode, persistence, and UI drag behavior?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use MVVM with SwiftUI. Model: Task with id, title, dueDate, priority, completed. Persist all tasks as JSON via Codable to the Documents directory; load on launch and save on change. UI: Sectioned List",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "To implement FocusFlow, create a FocusSession model with duration, remaining, isRunning, and lastUpdated; encode/decode to UserDefaults. Use a Circle progress view driven by (duration-remaining)/durat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a tick-based deterministic engine: every tick uses a shared RNG seed, applies an ordered list of player actions to arrive at identical world state. Messages include Tick, Actions, and a per-tic",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an append-only Event log with events: AnchorCreated, AnchorMoved, AnnotationAdded, AnnotationEdited. Persist events and a light Snapshot in Core Data; on relaunch replay the last 100 events to reb",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical iOS fundamentals: SwiftUI, MVVM, sectioned lists, drag-and-drop, and local persistence with Codable in a beginner context.\n\n## Key Concepts\n- MVVM data flow in SwiftUI\n- Sectioned List and onMove for drag-and-drop\n- Codable persistence to Documents directory\n- Local-first resiliency and a basic test plan\n- Accessibility basics for list items\n\n## Code Example\n```javascript\nstruct Task: Codable {\n  var id: UUID\n  var title: String\n  var dueDate: Date\n  var priority: Priority\n  var completed: Bool\n}\n```\n\n## Follow-up Questions\n- How would you handle a schema change for Task without breaking existing data?\n- How would you test drag-and-drop across sections in a UI test?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-6",
        "questionId": "q-27201",
        "question": "In an iOS enterprise app used by Cloudflare, Bloomberg, and Stripe, design a client-side policy engine that governs which API endpoints are exposed to the UI and how often calls can be made. Requirements: 1) a tiny DSL supporting patterns like 'IF endpoint == \"/payments\" AND role == \"manager\" THEN ALLOW' or 'IF endpoint == \"/payments\" THEN LIMIT 100/hour', 2) route all URLSession requests through a custom URLProtocol to enforce policy, 3) Combine-based stream of policy-violation events to a local dashboard, 4) crash-safe resume with checkpointing so policy state persists across relaunches, 5) secure policy updates via CryptoKit signatures, 6) a SwiftUI dashboard showing active policies and latest evaluation results. Provide data models, evaluation algorithm, persistence options, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an on-device pipeline: feed camera frames through a Metal preprocessor, run a Vision/CoreML defect model, maintain a rolling 1s anomaly window, and overlay a heatmap in SwiftUI. Include energy-",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a CRDT-based on-device whiteboard document. Each client issues unique op IDs (peerid-seq) for add/modify/delete of drawing objects; store ops in an append-only log; use tombstones for deletions",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a compact DSL with AST nodes for Endpoint, Condition, Action; parse with a hand-written recursive descent; persist policies and counters in SQLite; intercept via URLProtocol to enforce on ev",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use an append-only Event log with events: AnchorCreated, AnchorMoved, AnnotationAdded, AnnotationEdited. Persist events and a light Snapshot in Core Data; on relaunch replay the last 100 events to reb",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nExplores on-device policy evaluation, secure updates, and crash resilience in a realistic enterprise app.\n\n## Key Concepts\n- Tiny DSL parsing and evaluation\n- URLProtocol interception and request routing\n- Crash-safe persistence and checkpointing\n- CryptoKit-based policy verification\n- Combine-based event streaming and SwiftUI dashboards\n\n## Code Example\n```swift\n// Tiny DSL AST and evaluator sketch\nstruct Policy { let endpoint: String; let condition: (User) -> Bool; let action: Action }\nenum Action { case allow, deny, limit(Int) }\nclass Evaluator { func eval(_ p: Policy, for req: Request) -> Bool { /* ... */ } }\n```\n\n## Follow-up Questions\n- How would you test violation surge under bursty traffic?\n- How do you handle conflicting policies from multiple tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-7",
        "questionId": "q-6904",
        "question": "In an iOS trading app, implement a price ticker module that subscribes to a live WebSocket feed for multiple assets. Requirements: per-asset state managed by actors; cap UI updates to 10 Hz; use a small in-memory cache with on-disk persistence; ensure crash-safe resume; handle network interruptions with exponential backoff; provide a test plan and discuss trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a data pipeline: SensorFeed emits Data chunks; a Batcher groups into fixed-size batches with a batch nonce. UploadService uses URLSession with a background session, writes a persistent queue to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Define an OverlayEffect protocol with id, latencyBudget, and render(frame:). Effects plug into a fixed-priority per-frame scheduler (30fps target); capture frame times; render via Metal/CoreGraphics i",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "For each asset, create a PriceActor to serialize updates. Buffer bursts and emit UI changes at 10 Hz. Maintain a lightweight in-memory cache plus on-disk persistence of the last price and timestamp using Codable.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Store two Codable models: Habit(id, name, color) and DayLog(habitId, date, completed). Persist all habits and logs to a JSON file in Documents via FileManager. In SwiftUI, render a 7-day grid with tog",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes knowledge of Swift concurrency, actors, and robust networking for live data.\n\n## Key Concepts\n- Swift Concurrency, actors for per-item isolation\n- WebSocket resilience and backoff\n- Local persistence and crash recovery\n- UI throttling and data consistency\n\n## Code Example\n```swift\nactor PriceActor {\n    private var price: Double = 0\n    private var timestamp: Date = Date()\n    \n    func update(price: Double, timestamp: Date) {\n        self.price = price\n        self.timestamp = timestamp\n    }\n}\n```\n\n## Follow-up Questions\n- How would you test throttling behavior?\n- What strategies would you use for memory management with thousands of assets?\n- How would you handle schema evolution for persisted data?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-8",
        "questionId": "q-525",
        "question": "You're building a food delivery app like DoorDash. How would you implement background location updates to track delivery drivers while balancing battery life and accuracy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use CLLocationManager with significantLocationChangeMonitoring for battery efficiency, switching to standard location updates during active deliveries. Implement region-based geofencing for restaurant and destination zo…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create Core Data models Habit and Entry; pair a main context with a background context for writes. On toggle, set today’s Entry and debounce UI refresh ~500ms via a Debouncer. Load today’s state on la",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a CRDT-based collaborative notes engine. Each client stores a per-document CRDT in SQLite, writes edits to a durable log, and applies merges via Swift Actors. Sync uses delta diffs w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a privacy-preserving on-device analytics pipeline for a trading app that captures user interactions (taps, swipes, menu opens), applies differential privacy with per-feature epsilon budgets, uses deterministic ra…",
            "isCorrect": false
          }
        ],
        "explanation": "## Background Location Strategy\n\n- **significantLocationChangeMonitoring**: Battery-efficient for idle drivers\n- **Standard location updates**: High accuracy during active deliveries\n- **Geofencing**: CLRegion for restaurant/destination zones\n- **Deferred updates**: Batch location data to reduce wake-ups\n\n## Implementation Details\n\n```swift\nlet locationManager = CLLocationManager()\nlocationManager.allowsBackgroundLocationUpdates = true\nlocationManager.pausesLocationUpdatesAutomatically = true\nlocationManager.activityType = .otherNavigation\n```\n\n## Battery Optimization\n\n- Use desiredAccuracy = kCLLocationAccuracyHundredMeters for monitoring\n- Switch to kCLLocationAccuracyBestForNavigation during deliveries\n- Implement adaptive update frequency based on speed and proximity\n- Leverage iOS motion detection to pause updates when stationary",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-9",
        "questionId": "q-11782",
        "question": "Design a beginner iOS SwiftUI app named ExpenseTrace that lets users log an expense with amount, category, date, and an optional receipt image. Persist data locally using Codable to the Documents directory, implement a weekly summary view, and ensure data persists across launches. Include a minimal test plan accessibility considerations, and describe how you'd handle data migrations if the model changes between versions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a ModelManager that stores a manifest with modelName, version, sha256, size, and URL. Download to a temp directory in the background, verify the hash, then atomically swap by updating a versioned",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a Codable Expense struct with id, amount, category, date, receiptURL; persist via JSON to Documents with FileManager, load on startup; build a simple MVVM with @Published expenses and a weeklyT",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a CRDT-based offline-first sync for a collaborative Checklist. Use an OR-Set per item, per-device tombstones, and signed ops with CryptoKit. Persist locally in SQLite with encryption at rest; u",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a secure, offline-first iOS client for VaultNote. Implement per-document keys (CryptoKit, AES-GCM) with Secure Enclave-wrapped key material and periodic rotation. Drive low-latency sync via",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical iOS skills in data modeling, local persistence, and basic MVVM, plus test planning and accessibility. It also touches on data migrations that beginner apps should eventually handle.\n\n## Key Concepts\n\n- Codable persistence to disk with FileManager and JSON\n- Local data modeling and a lightweight MVVM pattern\n- Handling dates, time zones, and weekly summaries\n- Testing strategy for encoding/decoding and persistence\n- Accessibility: Dynamic Type and VoiceOver\n\n## Code Example\n\n```swift\nstruct Expense: Codable { var id: UUID; var amount: Double; var category: String; var date: Date; var receiptURL: URL? }\n```\n\n## Follow-up Questions\n\n- How would you validate inputs and handle currency formatting?\n- How would you implement a migration if fields change?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-10",
        "questionId": "q-27341",
        "question": "Design a beginner iOS app named 'ModelShelf' that lists ML models from a public catalog API (Think Hugging Face-like). Build a SwiftUI List showing model name, short description, and size; create Codable data models; implement local caching of the catalog to disk; provide offline fallback when offline; include a detail view with model metadata and a simple name-based search. Keep data fetch simple and testable?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a small Habit model with id, name, color, and completedDates [Date]. Use a single habits.json file in Documents. Compute the current streak by counting consecutive days with completion up to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a minimal, offline-first ModelShelf: fetch catalog from API, map to Codable ModelItem, store in a JSON file under the app's documents directory, read from disk when offline, display in a Swift",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Model Note: id: UUID, text: String, date: Date, latitude: Double, longitude: Double. Persist to Documents/TrailNotes.json with JSONEncoder/Decoder. UI: Map with annotations for notes and a searchable",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Securely store access and refresh tokens in Keychain; implement a URLSession wrapper that intercepts 401 responses, triggers a single token refresh guarded by a synchronization mechanism, and replays queued requests wit…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests SwiftUI data flow, Codable parsing, and light offline caching.\n\n## Key Concepts\n- SwiftUI List and Navigation\n- Codable models and API parsing\n- FileManager-based JSON cache in Documents\n- Simple search and detail view\n- Basic XCTest coverage for parsing and caching\n\n## Code Example\n```swift\nstruct ModelItem: Codable {\n  let id: String\n  let name: String\n  let description: String\n  let sizeMB: Int\n}\n```\n\n## Follow-up Questions\n- How would you add lightweight cache invalidation and a refresh trigger?\n- How would you test offline fallback behavior in UI tests?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-11",
        "questionId": "q-25959",
        "question": "In a beginner iOS SwiftUI app named 'MoodGraph', implement a local mood tracker: MoodEntry { date: Date, mood: Int (1-5), note: String? }. Build a UI to pick mood with a 5-point control and optional note, persist entries to a JSON file in the app's Documents directory, and compute daily average mood to render a simple line chart with SwiftUI Canvas. Ensure crash-safe resume by loading on launch and persisting after each change. Include a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a MoodEntry model conforming to Codable, store the array in a single JSON file within the app's Documents directory using FileManager, and atomically persist changes by writing to a temporary file before repla…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Architect a secure, offline-first iOS client for VaultNote. Implement per-document keys (CryptoKit, AES-GCM) with Secure Enclave-wrapped key material and periodic rotation. Drive low-latency sync via",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Note model: id, title, content, date, latitude, longitude. Persist as Codable to notes.json in Documents; support append/update with atomic write. Show MapKit annotations for each note in SwiftUI; tap",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use MVVM with SwiftUI and Core Data. Define a Task entity with attributes: id (UUID), title (String), dueDate (Date?), priority (String), and tag (String?) in a single persistent store. Load tasks via @FetchRequest and…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question validates core competencies in data modeling, local persistence, data aggregation, SwiftUI visualization, and crash-safe resume behavior, along with the candidate's ability to plan basic testing.\n\n## Key Concepts\n\n- Codable and FileManager for JSON persistence\n- SwiftUI Canvas for custom chart rendering\n- Date normalization to day granularity for aggregation\n- Atomic file writes for crash safety\n- Lightweight test planning\n\n## Code Example\n\n```swift\nstruct MoodEntry: Codable, Identifiable {\n    let id: UUID\n    let date: Date\n    let mood: Int // 1-5 scale\n    let note: String?\n}\n```\n\n```swift\nclass MoodStore: ObservableObject {\n    @Published var entries: [MoodEntry] = []\n    \n    private let url: URL\n    private let fileManager = FileManager.default\n    private let queue = DispatchQueue(label: \"com.moodgraph.persistence\")\n\n    init() {\n        let documentsURL = fileManager.urls(for: .documentDirectory, in: .userDomainMask)[0]\n        self.url = documentsURL.appendingPathComponent(\"mood_entries.json\")\n        load()\n    }\n\n    func addEntry(mood: Int, note: String?) {\n        let entry = MoodEntry(id: UUID(), date: Date(), mood: mood, note: note)\n        entries.append(entry)\n        save()\n    }\n\n    private func save() {\n        queue.async { [weak self] in\n            guard let self = self else { return }\n            do {\n                let data = try JSONEncoder().encode(self.entries)\n                let tempURL = self.url.deletingLastPathComponent()\n                    .appendingPathComponent(\"mood_entries_temp.json\")\n                try data.write(to: tempURL, options: .atomic)\n                try fileManager.moveItem(at: tempURL, to: self.url)\n            } catch {\n                print(\"Failed to save: \\(error)\")\n            }\n        }\n    }\n\n    private func load() {\n        guard fileManager.fileExists(atPath: url.path) else { return }\n        do {\n            let data = try Data(contentsOf: url)\n            entries = try JSONDecoder().decode([MoodEntry].self, from: data)\n        } catch {\n            print(\"Failed to load: \\(error)\")\n        }\n    }\n\n    func dailyAverages() -> [(date: Date, average: Double)] {\n        let calendar = Calendar.current\n        let grouped = Dictionary(grouping: entries) { entry in\n            calendar.startOfDay(for: entry.date)\n        }\n        return grouped.map { (date, entries) in\n            let avg = Double(entries.reduce(0) { $0 + $1.mood }) / Double(entries.count)\n            return (date: date, average: avg)\n        }.sorted { $0.date < $1.date }\n    }\n}\n```\n\n```swift\nstruct MoodChartView: View {\n    let data: [(date: Date, average: Double)]\n    \n    var body: some View {\n        Canvas { context, size in\n            guard data.count > 1 else { return }\n            let maxMood: Double = 5.0\n            let stepX = size.width / CGFloat(data.count - 1)\n            \n            var path = Path()\n            for (index, point) in data.enumerated() {\n                let x = CGFloat(index) * stepX\n                let y = size.height - (CGFloat(point.average / maxMood) * size.height)\n                if index == 0 {\n                    path.move(to: CGPoint(x: x, y: y))\n                } else {\n                    path.addLine(to: CGPoint(x: x, y: y))\n                }\n            }\n            \n            context.stroke(path, with: .color(.blue), lineWidth: 2)\n        }\n        .frame(height: 200)\n    }\n}\n```\n\n## Test Plan\n\n1. **Unit Tests**\n   - Verify MoodEntry encoding/decoding roundtrip\n   - Test dailyAverages() with known input data\n   - Validate mood range enforcement (1-5)\n\n2. **Integration Tests**\n   - Add entry, verify file exists in Documents\n   - Restart app, verify entries persist\n   - Add multiple entries same day, verify correct average\n\n3. **UI Tests**\n   - Verify mood picker limits selection to 1-5\n   - Confirm note field is optional\n   - Validate chart renders non-empty data",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-12",
        "questionId": "q-2859",
        "question": "Design and implement a high-throughput annotation queue in an iOS app used for labeling image datasets. Build a per-image task manager using Swift Concurrency (actors, AsyncSequence) to assign image slices to workers, enforce in-order UI updates, backpressure when the back-end lags, and automatic retries with exponential backoff. Provide API surface, a minimal SwiftUI view demo, and explain trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed answer: Use a modular MVP: ReplayKit for capture, WebRTC data channels with AES-GCM per-frame encryption via CryptoKit; rotate keys every few seconds and fall back to a relay server if direct",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-note CRDT (sequence CRDT) with vector clocks and an append-only log. Each edit is signed with CryptoKit and wrapped in a Merkle-proof block for cloud verification. Use Combine to strea",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage a per-image annotation queue implemented as an Actor to serialize state, with a TaskGroup dispatching annotation tasks to workers and streaming results via an AsyncSequence. UI updates on Mai",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a CRDT-based scene graph with Scene, Anchor, and Entity records persisted in CoreData. Implement delta-based synchronization over WebSocket or CloudKit, leveraging vector clocks for causality tracking and tombsto…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis probes Swift Concurrency mastery (actors, TaskGroups), deterministic UI ordering, and robust back-end latency handling in a data-labeling workflow.\n\n## Key Concepts\n- Swift Concurrency: actors, TaskGroup\n- AsyncSequence and backpressure\n- MainActor UI updates\n- Local persistence (SQLite/GRDB)\n- Exponential backoff and cancellation\n\n## Code Example\n```javascript\n// Swift-concurrency sketch (illustrative)\nactor AnnotationQueue {\n  private var queue: [ImageTask] = []\n  func enqueue(_ t: ImageTask) { queue.append(t) }\n  func run() async {\n    for await task in tasks() {\n      // process\n    }\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test under memory pressure and flaky network?\n- How do you ensure deterministic UI ordering if tasks complete out of order?\n- How would you swap in a different persistence backend without changing the API surface?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-13",
        "questionId": "q-28604",
        "question": "In a beginner iOS app named 'PhotoTagger', design a SwiftUI gallery that loads a local JSON catalog (name, filename, tags) from the bundle, shows images in a LazyVGrid, supports filtering by tag via a segmented control, and persists per-image user-added tags in UserDefaults. Use MVVM with a ViewModel exposing @Published properties, and ensure performance on older devices with lazy loading and image caching. Include a minimal SwiftUI view sketch and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a compact DSL parser for Rule{ id, condition, action } to drive an in-memory graph of AVAudioUnitEffects. Process frames via AVAudioEngine taps, evaluating rules per frame and applying matching",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement PhotoTagger using MVVM architecture: parse the local catalog.json file into ImageItem models, cache loaded UIImage instances in NSCache for performance, drive the grid with @Published properties in the ViewMod…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I would implement an on-device event-sourced pipeline: model Command, Batch, Session; persist as an append-only log with sequence numbers; sign each entry with CryptoKit using a device key; offline qu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed approach: define PhotoNote with id UUID, imageData Data, note String, tag Tag (Work|Personal|Travel), date. Persist as an array [PhotoNote] encoded with JSONEncoder to a file in the app's Doc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis exercise validates understanding of MVVM architecture, SwiftUI reactive data flow, and practical local persistence in a beginner-appropriate context. It also tests knowledge of efficient image loading patterns, caching strategies, and the ability to structure a straightforward test plan.\n\n## Key Concepts\n\n- **MVVM architecture** with @Published properties and Combine basics\n- **SwiftUI LazyVGrid** layout, segmented controls, and two-way data binding\n- **Local JSON parsing** using the Codable protocol\n- **NSCache** for memory-based image caching\n- **UserDefaults** for lightweight persistence of user-added tags",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-14",
        "questionId": "q-25043",
        "question": "In an iOS enterprise telemetry app used by IBM/Scale AI, design a privacy-preserving, offline-first telemetry collector that isolates data per tenant, aggregates locally before upload, and signs data before transmission. Requirements: 1) per-tenant keys stored in the Secure Enclave via CryptoKit, 2) a tiny DSL to express metrics like SUM(events) WHERE tenant='t1', 3) Combine-based ingestion with a durable on-disk queue, 4) crash-safe resume after relaunch with a deterministic replay, 5) a SwiftUI dashboard showing per-tenant aggregates and upload status, 6) a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-device offline-first ingestion: models Event, Device, Session in Core Data; a small in-memory ring buffer for high-rate streams; deduplicate by (deviceID,timestamp,type); compute 1s and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a lightweight data layer with Habit and DayLog models, Codable conformance, and a HabitStore using @Published with a small Combine pipeline to refresh the SwiftUI list on changes. Pe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use per-tenant CryptoKit keys stored in the Secure Enclave to sign per-tenant aggregates; a durable on-disk queue (SQLite) fed by a Combine ingestion pipeline; a tiny DSL for metrics like SUM(events) WHERE tenant='t1';…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Describe an offline-first map tile cache for an iOS app: use on-disk compressed tiles, a SQLite index, and an LRU eviction policy. Implement viewport-based prefetch with a background queue and URLSess",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design a privacy-focused, offline-first telemetry system with multi-tenant isolation, cryptographic signing, and robust persistence. Demonstrates proficiency in Swift, Combine, CryptoKit, and UI integration.\n\n## Key Concepts\n- **Privacy by design**: Per-tenant keys in Secure Enclave for cryptographic signing.\n- **Offline-first**: Durable on-disk queue with crash-recovery and deterministic replay.\n- **Data modeling**: Tenant, Event, Aggregate, Batch entities.\n- **DSL**: Tiny expression language for on-device metrics computation.\n- **Architecture**: Combine pipeline feeding a local processor and background uploader.\n\n## Code Example\n```swift\n// Metrics DSL\nfunc metric(_ expr: String) -> Metric {\n    let tokens = expr.split(separator: \" \")\n    // Parse: SUM(events) WHERE tenant='t1'\n    guard tokens.count >= 4, tokens[0] == \"SUM\",\n          tokens[2] == \"WHERE\", tokens[3].starts(with: \"tenant=\") else {\n        throw DSLError.invalidExpression\n    }\n    return Metric(aggregation: .sum, field: String(tokens[1]), \n                 filter: parseFilter(tokens[3...]))\n}\n\n// Secure Enclave key management\nstruct TenantKeyManager {\n    func generateKey(for tenantId: String) throws -> SecKey {\n        var error: Unmanaged<CFError>?\n        let access = SecAccessControlCreateWithFlags(\n            kCFAllocatorDefault,\n            kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly,\n            .privateKeyUsage, &error\n        )!\n        let attributes: [String: Any] = [\n            kSecAttrKeyType as String: kSecAttrKeyTypeECSECPrimeRandom,\n            kSecAttrKeySizeInBits as String: 256,\n            kSecAttrTokenID as String: kSecAttrTokenIDSecureEnclave,\n            kSecPrivateKeyAttrs as String: [\n                kSecAttrIsPermanent as String: true,\n                kSecAttrApplicationTag as String: tenantId.data(using: .utf8)!,\n                kSecAttrAccessControl as String: access\n            ]\n        ]\n        return SecKeyCreateRandomKey(attributes as CFDictionary, &error)!\n    }\n}\n\n// Combine ingestion pipeline\nclass TelemetryCollector {\n    private let queue = PassthroughSubject<TelemetryEvent, Never>()\n    private var subscriptions = Set<AnyCancellable>()\n    \n    init() {\n        queue\n            .collect(.byTime(DispatchQueue.global(), .seconds(1)))\n            .sink { events in self.persist(events) }\n            .store(in: &subscriptions)\n    }\n    \n    func ingest(_ event: TelemetryEvent) {\n        queue.send(event)\n    }\n    \n    private func persist(_ events: [TelemetryEvent]) {\n        // Write to SQLite with transaction\n    }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-15",
        "questionId": "q-14938",
        "question": "Design a beginner iOS SwiftUI app named StudyLog that lets users log daily study sessions with date, subject, duration (minutes), and optional notes. Persist locally as a single JSON array in the app's Documents directory using Codable; display a weekly view and a date-grouped list; ensure accessibility and Dynamic Type. Include a minimal test plan and data model rationale?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-device Actor streams to serialize inbound readings, buffer in-memory, batch SQLite writes, and schedule low-power background flushes. Encrypt data at rest with CryptoKit, sign entries with a d",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "The Stroke model includes: id (UUID), color (RGBA), points: [CGPoint], timestamp, author, and a per-stroke version vector. The CanvasActor maintains application state and serializes mutations; on each stroke change, the…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Sample answer: Build an AVAudioEngine pipeline with a mic tap feeding a Core ML noise-suppression model, then push to an Opus encoder; stream via WebRTC. Use sequence numbers and a small jitter buffer",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "StudyLog models a StudySession with id, date, subject, duration, and notes. It encodes and decodes an array to Documents/studylog.json via JSONEncoder and JSONDecoder, writing atomically on each update. On launch, it lo…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes beginner iOS SwiftUI fundamentals: simple data modeling, JSON-based persistence, and UI composition. It also introduces accessibility and performance considerations early in the design process.\n\n## Key Concepts\n- Codable persistence to the Documents directory\n- FileManager writes with atomic safety for data integrity\n- SwiftUI List with date grouping and a lightweight WeekView\n- Accessibility labels and Dynamic Type support for inclusive design\n\n## Data Model\n```swift\nstruct StudySession: Identifiable, Codable {\n    let id: UUID\n    let date: Date\n    let subject: String\n    let duration: Int\n    let notes: String?\n}\n```\n\n## Data Model Rationale\n- `id`: UUID ensures unique identification for list performance\n- `date`: Date type enables calendar math and grouping\n- `subject`: String for flexible categorization\n- `duration`: Int (minutes) keeps storage simple\n- `notes`: Optional String accommodates optional entry data\n\n## Test Plan\n1. Add session and verify file creation in Documents\n2. Launch app with existing data and confirm loading\n3. Test date grouping and week view rendering\n4. Verify Dynamic Type scaling across text elements\n5. Test VoiceOver labels for all interactive elements",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-16",
        "questionId": "q-15830",
        "question": "In an iOS app named RouteDash for delivery drivers, design a client-side architecture that enables offline-first map caching, real-time ETA sharing with minimal latency, CRDT-like conflict resolution for route edits, and crash-safe resume after termination. Specify data models (Tile, Route, ETA), a local SQLite cache, an op-log, transport via WebSocket with HTTP fallback, and a test plan; include a minimal SwiftUI view sketch showing an ETA badge and driver avatars?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "```swift import Foundation import SQLite4 // MARK: - Data Models struct Tile: Codable, Identifiable { let id: String let x: Int, y: Int, z: Int let data: Data let expiresAt: Date? } struct Route: Codable, Identifiable {…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a SwiftUI note editor that shows a live word count. Drive it with Combine: feed the field into a CurrentValueSubject<String,Never>, debounce 300ms on RunLoop.main, then update a @Published w",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a MoodEntry model conforming to Codable, store the array in a single JSON file within the app's Documents directory using FileManager, and atomically persist changes by writing to a temporary file before repla…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Store Message as encrypted blob in Core Data; use CryptoKit AES.GCM with a per-user key in the Keychain; encrypt on send, decrypt on fetch. Local queue for outgoing messages; on network, post to REST",
            "isCorrect": false
          }
        ],
        "explanation": "## Technical Review\n\n**Issues Fixed:**\n1. **Truncated answer** → Complete architecture with all requested components\n2. **Wrong language** → Changed JS pseudo-code to proper Swift/SwiftUI\n3. **Missing data models** → Added Tile (with coordinates and expiry), Route (with version for CRDT), ETA, Waypoint\n4. **Missing SwiftUI view** → Added ETABadge with capsule styling and DriverAvatarStack with overlap and +N indicator\n5. **Missing crash-safe resume** → Added atomic write with WAL pattern via `persistOpLog()`\n\n**Correct Patterns Used:**\n- **Offline cache**: SQLite4.swift with LRU eviction and replace-on-conflict\n- **CRDT ops**: Lamport timestamps for causal ordering; ops are append-only and commutative\n- **Transport**: WebSocket primary with HTTP fallback (standard mobile resilience pattern)\n- **Crash-safe**: Atomic file writes (`.atomic` option) for op-log persistence\n- **SwiftUI**: Modern iOS 17+ patterns with `some View`, `AsyncImage`, and `Capsule` shape\n\n**Test Plan (should include):**\n- Unit: CRDT convergence with 3-way concurrent edits\n- Integration: Offline queue replay after simulated crash\n- UI: SwiftUI preview for ETABadge states (updating, stale, error)",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-17",
        "questionId": "q-16911",
        "question": "In an iOS general app for ride-sharing, design a secure, privacy-preserving client-side session management architecture that stores user auth tokens, location, and ride data offline with data minimization, uses Secure Enclave for key storage, implements automatic key rotation via remote config, and ensures crash-safe resume after termination. Include data models, encryption scheme, and a test plan. How would you implement and verify this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a directed graph stored locally in SQLite to represent the map. Run A* with a lightweight heuristic, incrementally updating edge weights as Combine streams traffic events. When weights are invalidated, trigger…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A suitable answer encodes models for Habit and DayEntry, uses FileManager to read/write a single JSON file in Documents, and computes current streak by counting consecutive days from today backward un",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a SwiftUI app called TranslationsLab featuring a TranslationEntry model with id, key, baseString, and a translations dictionary for two target languages. Implement Codable persistence to the app's Documents direc…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use CryptoKit with a per-device key stored in the Keychain and rotated via remote config; encrypt tokens, location, and ride state with AES-GCM using random IVs; store only essential fields to minimiz",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssess secure, offline-capable client architectures that respect privacy, including key management, crash safety, and testability in a realistic ride-sharing scenario.\n\n## Key Concepts\n- Secure Enclave / Keychain-backed keys\n- CryptoKit AES-GCM encryption with random IVs\n- Remote config-driven key rotation\n- Data minimization and on-device privacy\n- Crash-safe resume via append-only log + snapshot\n\n## Code Example\n```swift\nimport Security\nimport CryptoKit\n\nfunc fetchKeyFromKeychain() throws -> Data {\n  // Secure retrieval placeholder\n  throw NSError(domain: \"\", code: -1, userInfo: nil)\n}\nfunc encryptPayload(_ payload: Data) throws -> Data {\n  let keyData = try fetchKeyFromKeychain()\n  let key = SymmetricKey(data: keyData)\n  let sealed = try AES.GCM.seal(payload, using: key)\n  return sealed.combined!\n}\n```\n\n## Follow-up Questions\n- How would you test key rotation with zero downtime?\n- How would you detect and recover from rotation-induced data corruption?\n- What edge cases exist when resuming after a crash and how would you mitigate them?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-18",
        "questionId": "q-9332",
        "question": "In an iOS healthcare app that monitors sensors via Bluetooth LE, design a robust local data pipeline that ingests streaming measurements, validates and de-duplicates data, stores 1-minute per-sensor aggregates in SQLite, and exposes a deterministic API for the UI to render in real time while handling out-of-order arrivals and app restarts. Include a crash-safe resume strategy and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a small Habit model with id, name, color, and completedDates [Date]. Use a single habits.json file in Documents. Compute the current streak by counting consecutive days with completion up to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an offline-first form data capture module for field agents. Use Core Data for local persistence, per-record versioning, and JSON deltas for REST sync. Implement optimistic UI, batched uploads,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a producer–consumer on‑device: a WAL SQLite table records raw events (session_id, type, ts). A background worker updates per‑type histograms and assembles a deterministic JSON export (versio",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Swift Actor to model the ingestion pipeline, parse BLE packets into immutable SensorReading values, deduplicate using a per-device sequence number, compute 1-minute averages in a sliding window, and store results…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a resilient, real-time data path on iOS with real hardware constraints (BLE, offline resumes), plus data integrity, concurrency, and testing philosophy.\n\n## Key Concepts\n\n- Swift Actors and async/await for isolated ingestion\n- BLE packet parsing and validation\n- Per-device de-duplication using sequence numbers\n- Sliding-window 1-minute aggregates stored in SQLite (GRDB)\n- Crash-safe resume via persistent checkpoints and WAL\n\n## Code Example\n\n```swift\n// Example: skeleton of the ingestion actor\nactor Ingestor {\n  private var lastSeqPerDevice: [String",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-19",
        "questionId": "q-18855",
        "question": "In an iOS AR collaboration app using ARKit that lets multiple users manipulate virtual anchors anchored to a shared space, design a client-side architecture to keep anchors and state in sync with sub-50ms latency, crash-safe resume, and offline-first operation. Requirements: 1) CRDT/OT-based state convergence; 2) WebRTC data channels with a server relay fallback; 3) on-device rendering optimizations (Metal) with a tight GPU budget; 4) local persistence of world map and anchors (SQLite/Realm) with a replay log; 5) a minimal SwiftUI overlay showing collaborators and anchors; 6) a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Optional chaining (?.) safely unwraps optionals, returning nil if any link fails instead of throwing runtime errors. Unlike force unwrapping (!), it prevents crashes. It's more concise than if-let/guard-let for single a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To implement: define a Codable Task { let id: UUID; var title: String; var completed: Bool }. Persist via JSON in UserDefaults using Data, encode/decode on save and on app launch. Use a SwiftUI List w",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Model a Draft entity (id: UUID, title, content, lastEdited: Date, status: Draft/Published) stored in Core Data with NSPersistentContainer. Offline-first. Sync via URLSession to a REST endpoint; implem",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a modular SyncEngine with per-anchor CRDT state and a compact transform pipeline; use WebRTC data channels for near-real-time sync with a relay fallback to a REST endpoint for outages; log every state mutation t…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis assesses real-time AR collaboration design under constrained mobile resources, focusing on latency, crash recovery, and offline resilience.\n\n## Key Concepts\n- ARKit anchors synchronization across distributed clients\n- CRDT/OT for convergent state without central coordination\n- WebRTC data channels with relay fallback for network variability\n- On-device Metal rendering under tight GPU budget\n- Local persistence with SQLite and append-only replay logs\n- Crash-safe resume via deterministic state replay\n- SwiftUI overlays for collaborator presence and anchor visualization\n\n## Code Example\n```swift\n// Pseudo Swift: Snapshot of SyncEngine interface\nclass SyncEngine {\n    func applyLocalAnchor(_ a: Anchor)\n    func applyRemoteOperation(_ op: CRDTOperation)\n    func sync() async throws\n    func loadPersistedState() async throws\n}\n\nclass AnchorCRDT {\n    private var state: [UUID: LWWRegister<AnchorData>]\n    func merge(_ other: AnchorCRDT) -> AnchorCRDT\n    func addAnchor(_ anchor: AnchorData) \n    func removeAnchor(id: UUID)\n}\n\nclass ReplayLog {\n    private let db: SQLite\n    func append(_ mutation: Mutation) throws\n    func replay() throws -> [Mutation]\n}\n```\n\n## Test Plan\n1. **Latency Benchmarks**: Measure end-to-end anchor sync latency across 2-5 clients; assert <50ms on local network\n2. **Crash Recovery**: Simulate app termination mid-sync; verify state consistency after relaunch via replay log\n3. **Offline Operation**: Disable all network; queue operations locally; confirm convergence after reconnection\n4. **Race Condition Stress**: Rapid concurrent edits from multiple clients; validate CRDT merge correctness\n5. **GPU Performance**: Instrument Metal render loop; ensure frame budget adherence under anchor load\n6. **World Map Integrity**: Persist ARWorldMap; verify anchor alignment accuracy after app restart",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-20",
        "questionId": "q-15384",
        "question": "In an iOS app used by field technicians to collect sensor telemetry from a BLE device, design a high-frequency ingestion pipeline that reads data at 100-200 Hz, buffers locally with a circular buffer, and performs on-device fusion (Kalman-like) to produce a clean telemetry stream. Persist as time-series in SQLite with compression, ensure crash-safe resume, and present a live SwiftUI chart with per-sample timestamps. Address backpressure, data integrity, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "BLE (100-200 Hz) → SensorStreamBuffer (lock-free ring buffer) → KalmanFusion (on-device sensor fusion) → TelemetryStream (per-sample timestamps) → SQLite (WAL mode, compressed time-series) + write-ahead log for crash-sa…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a PhotoUploader in Swift that uses URLSessionBackground with a persistent queue stored in GRDB. Each photo is enqueued with a UUID, computes a SHA-256 digest before upload for integrity, and u",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a CRDT-based scene graph with Scene, Anchor, and Entity records persisted in CoreData. Implement delta-based synchronization over WebSocket or CloudKit, leveraging vector clocks for causality tracking and tombsto…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe a modular pipeline using AsyncSequence/Combine for per-sensor streams, a ring buffer to compress readings, SQLite or Realm for time-series storage, UI throttling to 20fps, and crash-safe resu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design an end-to-end real-time data pipeline spanning BLE ingestion to persistent storage, incorporating crash resilience, on-device signal processing, and live visualization—key competencies for embedded and mobile engineering roles working with sensor data.\n\n## Key Concepts\n- **High-frequency BLE ingestion** (100-200 Hz) requires efficient data capture without dropped samples\n- **Lock-free circular (ring) buffer** provides bounded memory usage with O(1) enqueue/dequeue operations\n- **On-device sensor fusion** (Kalman-like filtering) reduces noise and produces clean telemetry\n- **Time-series persistence** with compression optimizes storage; WAL mode ensures durability\n- **Crash-safe resume** via write-ahead log enables transaction replay on app relaunch\n- **Backpressure handling** prevents memory exhaustion when processing cannot keep pace with ingestion\n- **Data integrity** requires checksums and atomic writes\n\n## Test Plan\n1. **Unit tests**: Kalman filter accuracy, buffer overflow/underflow handling\n2. **Integration tests**: End-to-end data flow from BLE mock to SQLite\n3. **Stress tests**: Sustained 200 Hz for 30+ minutes, memory profiling\n4. **Crash recovery tests**: Simulate app termination mid-write, verify no data loss\n5. **UI tests**: Chart rendering performance with high-frequency updates",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-21",
        "questionId": "q-11174",
        "question": "In an iOS live-collaboration app with real-time audio, design a low-latency pipeline that captures mic input, runs on-device noise suppression, encodes with Opus, and transmits via WebRTC. Explain the data flow (AVAudioEngine taps, Core ML model, Opus payload), latency targets (<100 ms), jitter/out-of-order handling, crash-safe resume, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement a CounterVM with @Published value and methods increment/decrement that persist to Documents/counter.json via Codable. On init, load value from disk (default 0). The SwiftUI view binds to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design for an offline-first PDF annotation flow: render PDFs with PDFKit, persist Annotations in Core Data with documentVersion; sync via REST using a SyncManager that handles idempotent PUTs and delt",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Sample answer: Build an AVAudioEngine pipeline with a mic tap feeding a Core ML noise-suppression model, then push to an Opus encoder; stream via WebRTC. Use sequence numbers and a small jitter buffer",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Sketch per-session tables: Sessions(id, startedAt), Telemetry(id, sessionId, ts, kind, payload). Ingest on a background queue, batch to N records, compress with zstd, and POST to /telemetry with an id",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a tight, on-device audio pipeline with real-time constraints, cross‑component data formats, crash recovery, and rigorous testing.\n\n## Key Concepts\n- AVAudioEngine taps for low-latency capture\n- Core ML integration for real-time denoising\n- Opus encoding for compact, packetized audio\n- WebRTC transport and audio track synchronization\n- End-to-end latency budgeting and jitter handling\n- Crash-safe state persistence and resume\n- Targeted test plan: latency, packet loss, power\n\n## Code Example\n```javascript\n// Swift-like pseudo\nlet engine = AVAudioEngine()\nlet input = engine.inputNode\ninput.installTap(onBus: 0, bufferSize: 1024, format: input.inputFormat(forBus: 0)) { buffer, time in\n  // route to CoreML model and Opus encoder\n}\n```\n\n## Follow-up Questions\n- How would you measure end-to-end latency in production and isolate components?\n- What fallback paths exist if WebRTC negotiation stalls or the device sleeps?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-22",
        "questionId": "q-25618",
        "question": "Design a beginner iOS SwiftUI feature: a simple TransactionEntry app. Build a form to add transactions (amount, category, date, optional note). Persist locally with Core Data. Show a SwiftUI list with totals by category and a search bar filtering by note or category. Include a minimal SwiftUI sketch and a basic validation rule (amount > 0)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Load initial items from bundled JSON into a published array and display them in a SwiftUI List. Add items with a debounced tap to prevent duplicates. Persist updates with FileManager.default.write(to:",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To ingest high-rate BLE telemetry in an on-device pipeline, implement a Swift Actor-based ingestion path with a ring buffer and a 2-second backpressure window. Deduplicate by (sensorID, timestamp) and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a Core Data entity Transaction with amount (Decimal), category (String, enum-like), date (Date), note (String). Build a SwiftUI form bound to @State, validate amount > 0 and non-empty category,",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a content-addressable SQLite cache of tiles: tile_id, x/y/z, checksum, lastAccess, size. Use NSCache for in‑memory hits. Prefetch along planned route within a radius; also fetch adjacent til",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses foundational iOS skills: data modeling with Core Data, SwiftUI form bindings, basic validation, and a live-updating list with category totals. It mirrors real apps and allows quick evaluation of MVVM patterns.\n\n## Key Concepts\n- Core Data modeling\n- SwiftUI MVVM bindings\n- Simple input validation\n- List with computed totals and search\n\n## Code Example\n```swift\nimport CoreData\n@objc(Transaction)\npublic class Transaction: NSManagedObject {\n  @NSManaged public var amount: NSDecimalNumber\n  @NSManaged public var category: String?\n  @NSManaged public var date: Date?\n  @NSManaged public var note: String?\n}\n```\n\n## Follow-up Questions\n- How would you add unit tests for validation?\n- How would you extend to grouping by date category or export data?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-23",
        "questionId": "q-14116",
        "question": "In an iOS general warehouse-control app, design a modular ingestion and command-pipeline for real-time telemetry and robot commands. Requirements: bounded in-memory buffer and on-disk persistence for crash-safety; per-zone occupancy heatmap with sliding window; backpressure-aware command dispatch with retries; crash-safe resume after relaunch; deterministic testability. Include data models (Beacon, Telemetry, Zone, CommandBatch), persistence choice (SQLite/Core Data), and a minimal SwiftUI view sketch showing zone statuses and last command outcome?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded in-memory ring buffer (size 1024) backed by SQLite; parse BLE beacon broadcasts into Telemetry (timestamp, deviceId, value); aggregate per-zone occupancy with a 15‑min window; dispatch C",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a tiny DSL parser and evaluator for feature flags. Use a rule like IF user.role == 'admin' AND appVersion >= 3 THEN enableNewUI. Store versioned rules locally (Realm/SQLite), stream patches",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Proposed solution: local SQLite with AuditRecord (id UUID, version Int, timestamp Date, geo String, note String, photoPath String, uploaded Bool) and PhotoAsset (path String, checksum String) tables. DeltaSet of changed…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement LearnWords with a Word struct: id, word, definition, example, partOfSpeech, imageData, nextReview. Persist as a JSON array in Documents/words.json via FileManager; load on startup. UI: searc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess handling of real-time mobile data pipelines, crash safety, and testability in iOS for a logistics scenario.\n\n## Key Concepts\n- Bounded buffers and backpressure\n- On-disk + in-memory persistence\n- Per-zone aggregation and heatmap logic\n- Crash-safe resume and deterministic tests\n- Data models: Beacon, Telemetry, Zone, CommandBatch\n- SwiftUI view reflecting live state\n\n## Code Example\n```swift\nstruct Telemetry {\n  let timestamp: Date\n  let deviceId: String\n  let value: Double\n}\n```\n\n## Follow-up Questions\n- How would you test event reordering and data loss?\n- What strategies ensure deterministic replay for tests and CI?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-24",
        "questionId": "q-17944",
        "question": "In a beginner iOS SwiftUI app named 'Transactly', implement a screen that fetches a JSON array of transactions from a mock REST API, displays each as title and amount in a List, caches the response to disk with Codable via FileManager for offline access, and shows a retry button on failure. Include data models, persistence strategy, and a minimal SwiftUI view sketch and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would start by creating a Core Data model with a Note entity (title: String, body: String, createdAt: Date), then build a SwiftUI List to display notes with a detail view for viewing and editing. For localization, I w…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Transaction model with id, title, amount, and date properties conforming to both Codable and Identifiable. Create a NetworkService using URLSession to fetch an array of transactions, decode them with JSONDec…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I'd implement a two-process sandbox: a host app plus a dedicated sandboxed runner for each snippet. Run code via XPC with strict time and memory quotas and a watchdog that terminates overruns. Persist",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an append-only local ledger of OrderEvent records (containing id, symbol, side, qty, price, status, seq, and timestamp fields) stored in SQLite. On app boot, replay the ledger starting from the last known sequence n…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical skills in building data-driven iOS apps, including asynchronous networking, local persistence for offline support, and error handling with user experience considerations. It demonstrates proficiency with SwiftUI's state management via @Published and @StateObject, Codable for data serialization, and FileManager for disk-based caching.\n\n## Key Concepts\n- **URLSession**: Performing asynchronous network requests with proper error handling\n- **Codable**: JSON encoding and decoding for data persistence\n- **FileManager**: Reading from and writing to the documents directory for offline access\n- **SwiftUI State**: Using @Published, @StateObject, and @ObservableObject for reactive UI updates\n- **Error Handling**: Graceful failure states with retry functionality",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-17T14:01:33.929Z",
    "version": 1
  }
]