[
  {
    "id": "test-ios",
    "channelId": "ios",
    "channelName": "IOS",
    "title": "IOS Knowledge Test",
    "description": "Test your understanding of IOS with 25 questions.",
    "questions": [
      {
        "id": "ios-q-0",
        "questionId": "q-23439",
        "question": "Context: iOS app in manufacturing. Task: design a real-time on-device defect-detection pipeline that ingests camera frames, uses a custom Metal kernel for pre-processing, runs a Vision/CoreML model, maintains a rolling 1-second window of anomaly scores, and overlays a heatmap in SwiftUI. Ensure energy-aware frame throttling, crash-safe resume with deterministic replay, and a test plan with CVPixelBuffer unit tests and latency targets. Provide a minimal SwiftUI view sketch showing overlay?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant on-device data explorer: namespaces per operator; crypto-at-rest with per-tenant keys (CryptoKit); streaming telemetry via Combine with 5s windows for averages; durable write-ahead log for",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an on-device pipeline: feed camera frames through a Metal preprocessor, run a Vision/CoreML defect model, maintain a rolling 1s anomaly window, and overlay a heatmap in SwiftUI. Include energy-",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a client CRDT-based delta engine for real-time document collaboration. Use a sequence CRDT to encode edits locally, persist as an append-only log, and maintain a Merkle tree root for integrity.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Store Message as encrypted blob in Core Data; use CryptoKit AES.GCM with a per-user key in the Keychain; encrypt on send, decrypt on fetch. Local queue for outgoing messages; on network, post to REST",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests building a real-time on-device vision pipeline, integrating Metal, Vision, and CoreML, while ensuring power efficiency and crash-safe resume for production reliability.\n\n## Key Concepts\n\n- On-device low-latency ML integration\n- Metal-based pre-processing kernel design\n- Rolling window anomaly scoring\n- Energy-aware frame throttling\n- Deterministic replay for crash recovery\n\n## Code Example\n\n```swift\nclass FrameBuffer {\n  var buffer: [CVPixelBuffer] = []\n  let maxSize = 60 // 1s at 60fps\n  func push(_ p: CVPixelBuffer) {\n    buffer.append(p)\n    if buffer.count > maxSize { buffer.removeFirst() }\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test latency and determinism across device types?\n- How would you adapt when lighting changes affect pre-processing?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-1",
        "questionId": "q-464",
        "question": "How would you implement a custom UICollectionViewFlowLayout that supports dynamic cell heights and sticky headers while maintaining smooth scrolling performance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Override `prepare()` to pre-calculate and cache layout attributes in a dictionary for efficient access. Implement `shouldInvalidateLayout(forBoundsChange:)` to handle sticky header positioning, and use `estimatedItemSiz…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I'd implement a bounded in-memory ring per device as the intake buffer, a streaming layer using AsyncSequence and Combine to publish rolling 1s/5s aggregates, and a SQLite WAL store with a replay log",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a Codable Expense struct with id, amount, category, date, receiptURL; persist via JSON to Documents with FileManager, load on startup; build a simple MVVM with @Published expenses and a weeklyT",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use MVVM with Core Data. Model: Article(id: String, title: String, body: String, updatedAt: Date, isRead: Bool). Persistence: Core Data stack; on launch, fetch cached articles; if online, fetch from A",
            "isCorrect": false
          }
        ],
        "explanation": "## Key Implementation Steps\n\n- Override `prepare()` to calculate and cache layout attributes for all elements\n- Implement `layoutAttributesForElements(in:)` to return cached attributes for visible elements\n- Use `shouldInvalidateLayout(forBoundsChange:)` to manage sticky header behavior during scroll\n- Set `estimatedItemSize` to enable dynamic height calculation for variable content\n\n## Performance Optimizations\n\n- Cache calculations in memory to avoid repeated expensive operations\n- Use `UICollectionViewFlowLayoutInvalidationContext` for targeted layout updates instead of full invalidation\n- Implement `targetContentOffset(forProposedContentOffset:)` to ensure smooth scrolling behavior\n\n## Common Pitfalls\n\n- Avoid performing expensive calculations in `layoutAttributesForElements(in:)` as it's called frequently\n- Don't invalidate the entire layout unnecessarily; use targeted invalidation contexts instead\n- Be careful with memory usage when caching large amounts of layout attributes",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-2",
        "questionId": "q-525",
        "question": "You're building a food delivery app like DoorDash. How would you implement background location updates to track delivery drivers while balancing battery life and accuracy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "TaskFlow stores tasks with properties for title, optional dueDate, and category using Codable, persisting to the Documents directory and loading on app launch. It supports English and Spanish localization, Dynamic Type…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a CRDT-based collaborative notes engine. Each client stores a per-document CRDT in SQLite, writes edits to a durable log, and applies merges via Swift Actors. Sync uses delta diffs w",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Record audio memos using AVAudioRecorder, save to Documents as a unique file, and write to a temp path first then move for atomic save. Build a Codable Memo struct with id, date, duration, path; list",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use CLLocationManager with significantLocationChangeMonitoring for battery efficiency, switching to standard location updates during active deliveries. Implement region-based geofencing for restaurant and destination zo…",
            "isCorrect": true
          }
        ],
        "explanation": "## Background Location Strategy\n\n- **significantLocationChangeMonitoring**: Battery-efficient for idle drivers\n- **Standard location updates**: High accuracy during active deliveries\n- **Geofencing**: CLRegion for restaurant/destination zones\n- **Deferred updates**: Batch location data to reduce wake-ups\n\n## Implementation Details\n\n```swift\nlet locationManager = CLLocationManager()\nlocationManager.allowsBackgroundLocationUpdates = true\nlocationManager.pausesLocationUpdatesAutomatically = true\nlocationManager.activityType = .otherNavigation\n```\n\n## Battery Optimization\n\n- Use desiredAccuracy = kCLLocationAccuracyHundredMeters for monitoring\n- Switch to kCLLocationAccuracyBestForNavigation during deliveries\n- Implement adaptive update frequency based on speed and proximity\n- Leverage iOS motion detection to pause updates when stationary",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-3",
        "questionId": "q-204",
        "question": "How would you optimize a UITableView with 10,000+ complex cells using Auto Layout while maintaining 60fps scrolling and memory efficiency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a CRDT-like log (vector clocks) to merge HealthKit samples and BLE scale readings stored in Core Data. Schedule BGProcessingTask for Wi‑Fi uploads; batch diffs to cloud with on-device differential",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use CoreData to model LocationPin, Polyline, and PhotoAttachment entities with a version field for conflict detection. Store media files in the Documents directory while maintaining metadata references in CoreData. Impl…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-note CRDT: each edit is an op with opId, author, and vector timestamp. Persist notes and a compact OpLog in Core Data. Merge by applying vector clocks to deterministically integrate remo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use cell reuse, pre-calculate heights, implement heightForRowAt caching, and optimize Auto Layout constraints with manual layout when needed.",
            "isCorrect": true
          }
        ],
        "explanation": "## Concept Overview\nOptimizing large UITableViews requires balancing memory usage, rendering performance, and smooth scrolling. Auto Layout adds computational overhead that becomes critical at scale.\n\n## Implementation Details\n\n### Cell Reuse Strategy\n```swift\noverride func prepareForReuse() {\n    super.prepareForReuse()\n    // Reset expensive operations\n    imageView.image = nil\n    complexView.resetContent()\n}\n```\n\n### Height Caching System\n```swift\nprivate var heightCache: [IndexPath: CGFloat] = [:]\n\nfunc tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {\n    if let cached = heightCache[indexPath] {\n        return cached\n    }\n    let height = calculateHeight(for: indexPath)\n    heightCache[indexPath] = height\n    return height\n}\n```\n\n### Auto Layout Optimization\n- Use `setNeedsLayout()` instead of `layoutIfNeeded()`\n- Prefer `intrinsicContentSize` over complex constraints\n- Implement `systemLayoutSizeFitting` for height calculation\n\n### Memory Management\n- Use weak references for cell closures\n- Implement `didEndDisplaying` for cleanup\n- Consider async image loading with placeholder\n\n## Common Pitfalls\n- Not caching heights leads to expensive recalculations\n- Complex Auto Layout in cell configuration blocks\n- Memory leaks from strong reference cycles\n- Ignoring cell reuse lifecycle",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-4",
        "questionId": "q-6409",
        "question": "In an iOS app for streaming video, implement an offline-first download queue: allow selecting multiple videos for download in parallel with multiple resolutions; ensure atomic file writes, resume after relaunch, support pause/resume and cancel, and report per-video progress. Outline data models (download record, video metadata, progress), caching strategy (disk storage + NSCache), background transfers (URLSession with background configuration), crash-safe resume logic, and a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: implement a Swift-based on-device telemetry stack with Swift Concurrency, Core Data, and an append-only log. Track per-tenant epsilon budgets, inject Laplace noise into counts before storage",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a robust offline-first download queue using Swift concurrency with a DownloadManager actor coordinating a TaskGroup of URLSessionBackgroundDownloadTasks. Write files to temporary locations, then atomically mov…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose an Event model: struct Event: Identifiable, Codable { var id: UUID; var title: String; var date: Date; var location: String; var url: URL? }. Persist [Event] to Documents with JSONEncoder/Deco",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: Create a Card model Codable to parse; build a SwiftUI CardView with 3D flip animation; load questions from bundled JSON into a @Published array; track currentIndex in @State and persist to U",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess mastery of offline-first design, concurrency, persistence, and robust background transfers in iOS at scale.\n\n## Key Concepts\n- Offline-first downloads with background URLSession\n- Data modeling with CoreData and per-video metadata\n- Concurrency safety via actors and TaskGroup\n- Crash-safe resume and cancellation\n- Testing strategy for persistence and race conditions\n\n## Code Example\n```swift\n// Swift data models\nclass DownloadRecord {\n    let id: UUID\n    let videoId: String\n    let resolution: VideoResolution\n    var status: DownloadStatus\n    var progress: Double\n    var localPath: String?\n}\n\nactor DownloadManager {\n    private let session: URLSession\n    private let cache = NSCache<NSString, NSNumber>()\n    \n    func startDownload(videoId: String, resolution: VideoResolution) async {\n        // Implementation details\n    }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-5",
        "questionId": "q-8145",
        "question": "Design an iOS feature: a client-side event tracer for SwiftUI that records per-view render timings and potential bottlenecks locally, stores them in secure SQLite with deterministic IDs, and batches uploads on background tasks when on Wi‑Fi and battery above threshold; ensure privacy by redacting PII and provide a deterministic QA replay payload. Explain architecture, data model, concurrency, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Decode FAQs with Codable from bundled JSON into FAQ models. Show a SwiftUI List with expandable rows. Add a debounced search using Combine that filters by question or answer. Persist last search query",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a client-side tracer for SwiftUI that records viewName, timestamp, renderMs, memory delta, and a privacyMask to a secure SQLite store (GRDB). Use Actors to serialize writes, and a background",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Architecture: capture frames at 15fps with AVFoundation; route through a ring buffer to a tiny Core ML model (Vision) on-device; persist Frame(id,timestamp,hash), AnalysisResult(frameId, score, decisi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Core Data models: Device(id, name) and TelemetryReading(id, deviceId, timestamp, temperature, battery, status, topic). Buffer MQTT messages in a small in‑memory queue and persist on write, deduplicati",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAdvanced iOS systems require robust in‑app telemetry with privacy controls and production‑grade reliability. This question probes data modeling, concurrency, and background behavior under real constraints.\n\n## Key Concepts\n- Swift Concurrency, Actors for serialized writes\n- SQLite-based event store with deterministic IDs\n- Background uploads with BGTaskScheduler and batching\n- Privacy: redaction rules and deterministic replay payloads\n\n## Code Example\n```javascript\n// Implementation sketch: data model and writer outline\nstruct TracerEvent {\n  let id: String\n  let viewName: String\n  let timestamp: TimeInterval\n  let renderMs: Double\n  let memoryDelta: Int64\n  let privacyMask: String\n}\n```\n\n## Follow-up Questions\n- How would you test battery/network constraints in CI?\n- How would you handle data schema migrations without breaking native clients?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-6",
        "questionId": "q-28279",
        "question": "Design a client-side data-transform sandbox in an iOS app for enterprise workflows. Create a tiny DSL for transforms (map, filter, join) over local data before upload; execute safely in a sandbox via Swift actors with per-user isolation. Use MVCC append-only SQLite log for crash-safe persistence and implement checkpoints. Provide data models, DSL semantics, safety guarantees, a live SwiftUI editor with real-time preview, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a compact DSL with three primitives: `map(field, expr)` to transform field values, `filter(pred)` to select records, and `join(other, key)` to combine datasets. Parse expressions into an AST, then execute the…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a two-tier cache and cancellable loading. Use NSCache for memory and a disk cache via FileManager. For each cell, spawn a Task tied to the cell's lifecycle; cancel on prepareForReuse. Load with UR",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a privacy-preserving on-device analytics pipeline for a trading app that captures user interactions (taps, swipes, menu opens), applies differential privacy with per-feature epsilon budgets, uses deterministic ra…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use the Secure Enclave to generate a P-256 private key, store it in the Keychain with biometric protection (kSecAttrAccessibleWhenUnlockedThisDeviceOnly and SecAccessControl with biometrics). Sign mes",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThese questions evaluate whether candidates can design a safe, auditable, offline-capable data-transformation subsystem with clear data modeling, persistence, and UX integration for iOS enterprise workflows.\n\n## Key Concepts\n- DSL design for transforms (map, filter, join)\n- Sandbox execution with Swift actors for isolation\n- MVCC append-only logs in SQLite for crash safety\n- Checkpointing and startup replay\n- Live SwiftUI editor with real-time preview\n\n## Code Example\n```swift\nenum Expr {\n  case literal(Int)\n  case field(String)\n  case call(name: String, args: [Expr])\n}\n\nstruct Transform: Codable {\n  let id: UUID\n  let op: Operation\n  enum Operation: Codable {\n    case map(field: String, expr: Expr)\n    case filter(pred: Expr)\n    case join(other: String, key: String)\n  }\n}\n\nactor TransformSandbox {\n  private var state: Dataset\n  \n  func execute(_ ops: [Transform]) async throws -> Dataset {\n    for op in ops {\n      state = try await apply(op, to: state)\n    }\n    return state\n  }\n}\n\nactor MVCCLog {\n  private var snapshot: MVCCSnapshot\n  \n  func append(_ entry: LogEntry) async throws {\n    snapshot = snapshot.append(entry)\n    try await persist(snapshot)\n  }\n  \n  func checkpoint() async throws {\n    try await persist(snapshot.compact())\n  }\n}\n```\n\n## Implementation Details\n\n### Data Models\n- **Dataset**: In-memory collection of typed records (`[[String: Any]]` or `[Codable]`)\n- **Transform**: Serialized operation wrapper with unique ID and version\n- **LogEntry**: Append-only record containing operation, timestamp, and checksum\n\n### DSL Semantics\n- `map(field, expr)`: Evaluates `expr` for each row, assigns result to `field`\n- `filter(pred)`: Retains rows where `pred` evaluates to `true`\n- `join(other, key)`: Left-outer join with `other` dataset on matching `key`\n\n### Safety Guarantees\n- **Actor isolation**: Each user's sandbox runs in an isolated Swift actor, preventing cross-contamination\n- **Deterministic execution**: No side effects; all operations are pure functions over the current state\n- **MVCC consistency**: Readers never block; writers append atomically\n- **Crash recovery**: On restart, replay log entries in order to reconstruct last known state\n\n### SwiftUI Editor\n- Syntax-highlighted text editor with autocomplete\n- Real-time preview pane executing transforms on sample data\n- Validation errors shown inline; transform state reflected immediately\n\n### Test Plan\n1. **Unit tests**: AST parsing, expression evaluation, transform correctness\n2. **Isolation tests**: Concurrent sandbox execution across actors\n3. **Persistence tests**: Crash simulation; verify log replay produces identical state\n4. **Integration tests**: End-to-end with SwiftUI editor, ensuring preview reflects DSL changes",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-7",
        "questionId": "q-11644",
        "question": "In an iOS field-data entry app for offline capture of customer details, design an encrypted, offline-first submission flow: data model, per-field encryption strategy, and a robust sync mechanism using Core Data and URLSession background tasks. Include conflict resolution, crash-safe resume, and validation rules. How would you implement this and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a lightweight Core ML model (e.g., temporal conv or small LSTM) trained on-device with heart-rate, HRV, and motion features. Apply differential privacy-free? No, use secure aggregation to share on",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Store customer records in Core Data with per-field encryption via CryptoKit AES-GCM, keys in the Keychain. A SyncEngine queues pending changes and uses a background URLSession to post when online; on",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a non-destructive FilterGraph using Swift actors: each filter is a node with input/output CIImage; edits propagate asynchronously via async/await; base image plus ordered passes render the f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a SwiftUI List bound to a SnippetViewModel. Use URLSession to fetch JSON from /api/snippets, decode to Snippet{id,title,language,code}. Persist via Codable in Documents; refresh with a pull-",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design robust offline-then-online data sync with encryption and crash resilience in a mobile enterprise context.\n\n## Key Concepts\n- Core Data persistence strategy for offline-first apps\n- CryptoKit AES-GCM per-field encryption and Keychain key management\n- Background URLSession for reliable syncing and conflict resolution\n\n## Code Example\n\n```swift\n// Pseudo Swift model sketch for encrypted fields\nimport Foundation\nimport CryptoKit\n\nstruct EncryptedField: Codable {\n  let ciphertext: Data\n  let tag: Data\n}\n```\n\n## Follow-up Questions\n- How would you test crash recovery and queue replay?\n- How would you rotate encryption keys without invalidating existing data?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-8",
        "questionId": "q-27201",
        "question": "In an iOS enterprise app used by Cloudflare, Bloomberg, and Stripe, design a client-side policy engine that governs which API endpoints are exposed to the UI and how often calls can be made. Requirements: 1) a tiny DSL supporting patterns like 'IF endpoint == \"/payments\" AND role == \"manager\" THEN ALLOW' or 'IF endpoint == \"/payments\" THEN LIMIT 100/hour', 2) route all URLSession requests through a custom URLProtocol to enforce policy, 3) Combine-based stream of policy-violation events to a local dashboard, 4) crash-safe resume with checkpointing so policy state persists across relaunches, 5) secure policy updates via CryptoKit signatures, 6) a SwiftUI dashboard showing active policies and latest evaluation results. Provide data models, evaluation algorithm, persistence options, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an UploadItem model with properties for id, localPath, remoteURL, totalBytes, uploadedBytes, checksum, and upload state. Persist items as JSON in Library/Application Support. Use BGTaskScheduler with URLSessio…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement LearnWords with a Word struct: id, word, definition, example, partOfSpeech, imageData, nextReview. Persist as a JSON array in Documents/words.json via FileManager; load on startup. UI: searc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a compact DSL with AST nodes for Endpoint, Condition, Action; parse with a hand-written recursive descent; persist policies and counters in SQLite; intercept via URLProtocol to enforce on ev",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Core Data with an Expense entity (title: String, amount: Double, date: Date, category: String). Create an NSPersistentContainer, save on changes, and load on launch. Bind a @FetchRequest to a List",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nExplores on-device policy evaluation, secure updates, and crash resilience in a realistic enterprise app.\n\n## Key Concepts\n- Tiny DSL parsing and evaluation\n- URLProtocol interception and request routing\n- Crash-safe persistence and checkpointing\n- CryptoKit-based policy verification\n- Combine-based event streaming and SwiftUI dashboards\n\n## Code Example\n```swift\n// Tiny DSL AST and evaluator sketch\nstruct Policy { let endpoint: String; let condition: (User) -> Bool; let action: Action }\nenum Action { case allow, deny, limit(Int) }\nclass Evaluator { func eval(_ p: Policy, for req: Request) -> Bool { /* ... */ } }\n```\n\n## Follow-up Questions\n- How would you test violation surge under bursty traffic?\n- How do you handle conflicting policies from multiple tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-9",
        "questionId": "q-11450",
        "question": "In an iOS app that reads BLE broadcasts from 20 environmental sensors (PM2.5, CO2, RH) for a live dashboard, design a backpressure-aware ingestion pipeline that deduplicates, preserves out-of-order arrivals, stores compact updates locally, and debounces UI updates to ~30fps; include data models, caching strategy, and a test plan; provide a minimal SwiftUI/Combine sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Persist notes using GRDB with a DatabaseQueue. Model NoteRecord(id, title, body, date). Expose a NotesViewModel with @Published notes, searchQuery, and dateFilter. Use a SwiftUI List with .searchable;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a Vehicle model with id, lastLocation, lastSeq, and lastTs; a Location struct with latitude/longitude; and an UpdateEnvelope with seq, vehicleId, location, ts. Use a bounded disk cache (SQLite/C",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a WebSocketManager using URLSessionWebSocketTask that maintains a single live connection, deduplicates incoming events by eventId, and persists them as Order entities in Core Data. U",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-sensor bounded in-memory queue with a reordering buffer to handle backpressure. Deduplicate incoming data using the composite key (sensorId, sequenceNumber) and reorder by timestamp before persistence. S…",
            "isCorrect": true
          }
        ],
        "explanation": "## Technical Corrections\n\n1. **Throttle vs Debounce (Critical)**: The original answer incorrectly stated \"debounced Combine publishers\" but for a live dashboard at ~30fps, you must use **throttle** (with `latest: true`). Debouncing waits for a pause in events before emitting—introducing artificial delay unsuitable for real-time data. Throttling enforces a consistent rate without initial delay.\n\n2. **Answer truncation**: The original answer was cut off mid-code. This version provides the complete implementation.\n\n## Key Implementation Details\n\n- **Bounded Queue**: Uses an actor-based `SensorDataPipeline` with per-sensor queues and a global reorder buffer. Returns `false` when buffer exceeds 1000 entries, signaling backpressure to BLE layer.\n\n- **Deduplication**: Composite key `(sensorId, sequenceNumber)` ensures each unique reading is processed exactly once, even with retransmissions.\n\n- **Reordering Buffer**: Collects readings from all sensors, sorts by timestamp, and only emits those with incrementing sequence numbers per sensor.\n\n- **SQLite WAL Mode**: Enables concurrent reads during writes and provides crash recovery.\n\n- **Throttled UI Updates**: Uses `collect(.byTime(_, .milliseconds(33)))` ≈ 30fps, combined with `@Published` to drive SwiftUI updates. The `latest: true` behavior ensures most recent value is always shown.\n\n## Test Plan Coverage\n\n- **Deduplication test**: Verify duplicates are rejected\n- **Backpressure test**: Verify buffer overflow handling\n- **Reordering test**: Verify out-of-order arrival handling\n- **Throttling test**: Verify render frequency\n- **Recovery test**: Verify WAL persistence after crash",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-10",
        "questionId": "q-9803",
        "question": "Design a beginner iOS SwiftUI app named RemindMood that lets a user set a daily mood reminder time via a local notification. Persist the reminder and mood logs in the Documents directory using Codable; handle time zones and permission flow. Include a minimal test plan and accessibility considerations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a beginner iOS SwiftUI app named RemindMood that allows users to set a daily mood reminder via local notifications. Implement Codable models for persistence—Prefs (enabled: Bool, time: Date) and MoodLog (date: Da…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Proposed answer: Use a modular MVP: ReplayKit for capture, WebRTC data channels with AES-GCM per-frame encryption via CryptoKit; rotate keys every few seconds and fall back to a relay server if direct",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a on-device telemetry pipeline with per-session buffering in CoreData, apply differential privacy (epsilon 1–2) to LocationEvent and SensorEvent before encryption, then stream via WebSocket to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Core Data with per-record versioning and an immutable audit log; encrypt the SQLite store with a hardware-backed key via Secure Enclave/Keychain. Model documents as CRDT-like OR-Set objects and me",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a small, real feature integrating UI, persistence, and system APIs, without heavy dependencies.\n\n## Key Concepts\n- Local notifications and permission flow\n- Codable persistence to the Documents directory\n- Time zone handling and daily scheduling\n- MVVM basics in SwiftUI and accessibility considerations\n\n## Code Example\n```swift\nimport Foundation\n\nstruct Prefs: Codable {\n  var enabled: Bool\n  var time: Date\n}\n```\n\n```swift\nimport UserNotifications\n\nfunc scheduleDaily(at date: Date) {\n  // Build a daily trigger for the given time\n}\n```\n\n## Follow-up Q",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-11",
        "questionId": "q-14367",
        "question": "In an iOS note-taking app named SyncNotes that must work offline and sync across devices with minimal data usage, design a delta-based sync architecture using a CRDT-like data model for notes and tags. Define data models: Note, Revision, Tag; persist with Core Data/SQLite; delta ops (addNote, updateContent, addTag, delTag) plus per-note vector clocks; sync over REST delta endpoints or WebSocket streams; conflict resolution by last-writer-wins with causal ordering; crash-safe resume via durable op queue and checkpoints; test offline edits, concurrent edits, out-of-order delivery, and end-to-end sync. Include a SwiftUI live-sync indicator sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use SQLite for messages with indices chatId/timestamp; table fields: id, chatId, senderId, body, timestamp, editedAt, deleted. Track receipts and per-chat rotation keys. Sync via push/pull; merges wit",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a CRDT-based annotation model with per-annotation IDs, version vectors, and a merge function. Use WebRTC data channels for peer sync, AES-GCM via CryptoKit for end-to-end encryption, and Core",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I'll design a CRDT-like delta sync architecture for SyncNotes with the following components: **Data Models:** ```swift struct Note { let id: UUID var content: String var tags: Set<String> var vectorClock: [String: Int]…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Approach: implement a SwiftUI view PINKitView with a separate KeypadView emitting digits to a 4-char @State. Mask with •, enable submit when length is 4. Store a hashed PIN in UserDefaults (hashing re",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates expertise in offline-first mobile applications, CRDT implementations, and robust synchronization patterns essential for modern collaborative apps.\n\n## Key Concepts\n- CRDT (Conflict-free Replicated Data Types) for eventual consistency\n- Vector clocks establishing causal relationships between operations\n- Delta-based synchronization minimizing bandwidth usage\n- Durable operation queues ensuring crash-safe operations\n- Mobile-specific challenges like network instability and battery constraints\n\n## Implementation Considerations\n- Core Data's migration capabilities for schema evolution\n- Efficient SQLite indexing for sync operation queries\n- Memory management for large note datasets\n- Background sync respecting iOS battery optimization\n\n## Code Example\n```swift\n// Simplified CRDT operation application\nfunc applyOperation(to note: Note, operation: SyncOperation) -> Note {\n    guard canApplyOperation(operation, to: note) else { return note }\n    \n    switch operation.type {\n    case .updateContent:\n        note.content = decodeContent(operation.payload)\n        note.vectorClock.merge(operation.vectorClock) { max($0, $1) }\n        note.modifiedAt = operation.timestamp\n    // Handle other operation types...\n    }\n    return note\n}\n```\n\n## Follow-up Questions\n- How would you optimize vector clocks for thousands of devices?\n- What approach would you use for binary attachment synchronization?\n- How would you handle data model migrations across app versions?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-12",
        "questionId": "q-2570",
        "question": "**iOS Beginner Challenge:** Build a minimal journaling screen where a user types notes, autosaves to a local file atomically, and displays a list of notes. Ensure data is preserved across app restarts, support background/foreground transitions, and debounce saves to avoid thrashing. Use Swift with SwiftUI, Codable Note, and FileManager in the Documents directory. Include a simple test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Swift Actor to model the ingestion pipeline, parse BLE packets into immutable SensorReading values, deduplicate using a per-device sequence number, compute 1-minute averages in a sliding window, and store results…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement LearnWords with a Word struct: id, word, definition, example, partOfSpeech, imageData, nextReview. Persist as a JSON array in Documents/words.json via FileManager; load on startup. UI: searc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "CoreData entities Ride, Driver, Route; local cache of ride history; a SwiftUI List driven by a fetched results controller; a Combine debounce on the search field (300ms); filter with NSPredicate like",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a 300ms debounced autosave mechanism that triggers on text changes, serialize the Note model using Codable, perform atomic writes by first saving to a temporary file then replacing the target file, and load ex…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical iOS development skills including data persistence, SwiftUI fundamentals, and robust file handling—essential competencies for junior iOS developers.\n\n## Key Concepts\n- SwiftUI state management and data binding\n- Codable protocol for data serialization\n- FileManager atomic operations in Documents directory\n- ScenePhase environment for app lifecycle management\n- Debounce pattern to optimize I/O operations\n\n## Code Example\n```swift\nimport SwiftUI\nstruct Note: Codable { let id: UUID; var text: String; let date: Date }\n```\n\n## Follow-up Questions",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-13",
        "questionId": "q-8391",
        "question": "Design an offline-capable live comment feed for a streaming app: implement a WebSocket-based live feed that delivers comments in-order, caches them locally in SQLite (GRDB) with a per-comment sequence ID, and persists on app restart. Support offline mode by serving cached comments, handle reconnection to fetch missed messages, deduplicate, and ensure atomic writes. Outline data models, persistence schema, and a practical test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an on-device wallet seed management and transaction signing system using Secure Enclave keys (via CryptoKit) with Shamir’s secret sharing backups (threshold t of n) encrypted and stored off-dev",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "In the pipeline, sample IMU at 100 Hz and camera at 30 Hz, feed to a quantized CoreML model for biomechanics, fuse with GPS via a lightweight Kalman filter, and emit KPI bundles at 30fps. Persist comp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a WebSocket-based live comment feed using URLSessionWebSocketTask for real-time communication, with SQLite persistence via GRDB. Design a Comment table with schema: `id TEXT PRIMARY KEY, seq INTEGER NOT NULL,…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a log of operations (opId, actor, timestamp, opType, fieldPath, value) with pii redaction; use a sequence-CRDT for edits so merges converge; store locally in Realm; sync via MultipeerConnect",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design a robust real-time feature with offline resilience, data integrity, and testability in iOS, leveraging URLSessionWebSocketTask, GRDB, and atomic writes. Includes reconnect, deduplication, and replay logic.\n\n## Key Concepts\n- WebSocket lifecycle and backpressure management\n- SQLite/GRDB transactions and schema design\n- In-order delivery via sequence IDs\n- Crash-safe resume and offline fallback\n- Testing strategies for network faults and edge cases\n\n## Code Example\n```swift\n// Sketch: minimal types and persistence\nstruct Comment: Codable {\n    let id: String\n    let seq: Int\n    let author: String\n    let content: String\n    let createdAt: Date\n}\n\nclass CommentFeed {\n    private let db: DatabaseQueue\n    private var socket: URLSessionWebSocketTask?\n    private var lastSeq: Int = 0\n    \n    func connect() {\n        socket = URLSession.shared.webSocketTask(with: URL(string: \"wss://api.example.com/comments\")!)\n        socket?.resume()\n        receiveMessages()\n    }\n    \n    private func receiveMessages() {\n        socket?.receive { [weak self] result in\n            guard let self = self else { return }\n            \n            switch result {\n            case .success(let message):\n                self.handleMessage(message)\n                self.receiveMessages()\n            case .failure(let error):\n                self.handleReconnection()\n            }\n        }\n    }\n    \n    private func handleMessage(_ message: URLSessionWebSocketTask.Message) {\n        // Parse and persist comment\n    }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-14",
        "questionId": "q-24878",
        "question": "In a beginner iOS fintech app, design a practical biometric authentication flow for approving high-value transfers. Use LAContext for biometrics, fall back to a secure passcode if biometrics are unavailable, and store a short-lived access token in the Keychain protected by the device. Describe flow, data models, and a minimal SwiftUI view showing auth status. Include a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-document envelope encryption: each document has a symmetric key wrapped with the recipient's public keys; derive keys via HKDF-SHA256; store keys in Keychain. Encrypt annotations with the docu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Sketch: per-note AES-GCM encryption with a per-note envelope key derived from a user password via HKDF; keys stored in Keychain. Persist encrypted notes in SQLite (GRDB); offline edits queued in a dur",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement biometric authentication for sensitive transfers with a biometric prompt (LAContext) and a Keychain-stored access token. On transfer, prompt device biometrics; if success, authorize using th",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design an offline-first posting system for PocketTasks. When a user creates a task, the app attempts to POST to the backend immediately if connectivity is available. If the device is offline, serialize a QueueItem using…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests knowledge of iOS security primitives, token storage, and crash-safe resume in fintech.\n\n## Key Concepts\n\n- LocalAuthentication LAContext\n- Keychain secure storage and AccessControl\n- Biometric types (FaceID/TouchID)\n- Fallback to passcode\n- State persistence and relaunch resume\n\n## Code Example\n\n```javascript\nimport LocalAuthentication\nlet ctx = LAContext()\nvar e: NSError?\nif ctx.canEvaluatePolicy(.deviceOwnerAuthenticationWithBiometrics, error: &e) {\n  ctx.evaluatePolicy(.deviceOwnerAuthenticationWithBiometrics, localizedReason: \\\"AUTH_TRANSFER\\\") { ok, _ in\n    // on success: read token from Keychain and authorize\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test edge cases (biometric changes, passcode policy, keychain access)?\n- How would you handle cross-device transfers and token revocation?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-15",
        "questionId": "q-22148",
        "question": "In a beginner iOS SwiftUI app named 'QuickCook' for offline recipe storage, design a local SQLite-backed data model (Recipe: id, title, ingredients, steps), implement an FTS-powered search, debounce the search input to ~200ms, and ensure accessibility via VoiceOver and Dynamic Type. Include a minimal SwiftUI view sketch and a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "SQLite-backed data model using a Recipe table with columns (id, title, ingredients, steps) paired with an FTS5 virtual table for full-text search on title, ingredients, and steps—keeping all data stored locally for offl…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a per-event vector clock and a CRDT-like merge. Event: id, title, start, end, attendees; each device tracks a vector clock. Edits enqueue as deltas in a local store (Core Data). On reconnect, merg",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe a practical, on-device sensor-fusion approach: combine accel, gyro, and HR via Combine; implement a compact Kalman filter with a sliding window; persist latest state and a replay log in SQLit",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Describe a modular pipeline using AsyncSequence/Combine for per-sensor streams, a ring buffer to compress readings, SQLite or Realm for time-series storage, UI throttling to 20fps, and crash-safe resu",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\n\nThis question evaluates practical skills in offline-first iOS development, covering persistent storage, search indexing, and accessibility implementation in a beginner-friendly SwiftUI context. It assesses candidate knowledge of SQLite integration with FTS5 for efficient text search, event debouncing techniques, and native accessibility features—without requiring enterprise-scale infrastructure experience.\n\nKey Concepts\n\n- SQLite with FTS5 virtual tables for fast, indexed full-text search\n- Combine framework for debouncing search input (200ms threshold)\n- Async/await for non-blocking data loading and pagination\n- Accessibility: VoiceOver labels, Dynamic Type support, and semantic reading order\n- Unit and UI test planning for local data and view behavior\n\nCode Example\n\n```swift\nclass RecipeViewModel: ObservableObject {\n    @Published var searchText = \"\"\n    @Published var results: [Recipe] = []\n    private var cancellables = Set<AnyCancellable>()\n    \n    init() {\n        $searchText\n            .debounce(for: .milliseconds(200), scheduler: RunLoop.main)\n            .removeDuplicates()\n            .sink { [weak self] text in\n                Task { await self?.performSearch(query: text) }\n            }\n            .store(in: &cancellables)\n    }\n    \n    func performSearch(query: String) async {\n        // Query FTS5 index via SQLite wrapper\n    }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-16",
        "questionId": "q-24812",
        "question": "In an iOS live-streaming app for creators, design a modular on-device overlay engine that supports pluggable effects (text, stickers, AR decals) with per-effect latency budgets and a central frame scheduler. Requirements: 1) a small Swift protocol API for effects with start/stop/updateFrame, 2) per-frame pipeline maintaining a 30fps target with backpressure and priority hints, 3) crash-safe resume with checkpointing, 4) deterministic replay log for debugging overlays, 5) a SwiftUI dashboard showing active effects, per-effect latency, and frames rendered in the last second, 6) a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use MVVM with SwiftUI. Model: Task with id, title, dueDate, priority, completed. Persist all tasks as JSON via Codable to the Documents directory; load on launch and save on change. UI: Sectioned List",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a tick-based deterministic engine: every tick uses a shared RNG seed, applies an ordered list of player actions to arrive at identical world state. Messages include Tick, Actions, and a per-tic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Core Data to model Album and Photo with id, assetURL, timestamp, isSynced, cloudRecordID, and version. Sync via CloudKit with per-record versioning; apply last-write-wins on conflicts but preserve",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Define an OverlayEffect protocol with id, latencyBudget, and render(frame:). Effects plug into a fixed-priority per-frame scheduler (30fps target); capture frame times; render via Metal/CoreGraphics i",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world tradeoffs for low-latency, modular live-video overlays, including testing for deterministic debugging and crash safety.\n\n## Key Concepts\n- Protocol-based plugin API for effects\n- Fixed-priority scheduler with backpressure\n- Crash-safe checkpointing and deterministic replay logs\n- On-device rendering path via Metal/CoreGraphics\n- SwiftUI diagnostics panel\n\n## Code Example\n```swift\nprotocol OverlayEffect {\n  var id: String { get }\n  var latencyBudgetMs: Int { get }\n  func render(frame: CVPixelBuffer) -> CVPixelBuffer\n}\n```\n\n## Follow-up Questions\n- How would you structure the replay log to be compact yet deterministic?\n- How would you test latency budgets across diverse devices?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-17",
        "questionId": "q-19864",
        "question": "Design a beginner iOS SwiftUI app named TrailNote that lets hikers log trail entries: trail name, distance, rating, date, optional photo, and GPS coordinates captured via CoreLocation; persist locally with Codable to the app's Documents directory; implement a filter by date and rating and a simple map view showing logged trails; include a minimal SwiftUI view sketch and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Bind a SwiftUI TextEditor to @State text. Implement onChange(text) to cancel a previous DispatchWorkItem and schedule a new one that calls saveDraft after 1 second. saveDraft writes to UserDefaults wi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Uses a TelemetryPayload (timestamp, IMU, altitude, battery, health) and a Session. A 2s buffer feeds a Processor running a tiny CoreML detector plus a heuristic health rule. Persist via Core Data (SQL",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Store `Task` as a Codable struct (id: UUID, title: String, dueDate: Date, completed: Bool). Persist to Documents/taskflow_tasks.json using FileManager, writing on every change to ensure crash-safe persistence. Search is…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a TrailEntry Codable struct with id, name, distance, rating, date, photoPath, latitude, longitude. Save/load as JSON in the app's Documents directory via FileManager. Acquire coordinates with Core",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThe prompt tests practical iOS data modeling, offline persistence, and UI composition for a real world hobby app.\n\n## Key Concepts\n- SwiftUI data flow and @ObservedObject/@State\n- Codable persistence to the Documents directory\n- CoreLocation for coordinates and MapKit/SwiftUI Map integration\n- Simple yet testable filtering by date and rating\n- Basic test strategy: encode/decode, persistence, map data\n\n## Code Example\n```swift\nstruct TrailEntry: Codable {\n  let id: UUID\n  let name: String\n  let distance: Double\n  let rating: Int\n  let date: Date\n  let photoPath: String?\n  let latitude: Double\n  let longitude: Double\n}\n```\n\n## Follow-up Questions\n- How would you handle photo storage and memory optimization?\n- How would you extend to sync with iCloud or a backend later?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-18",
        "questionId": "q-6435",
        "question": "Design and implement an advanced iOS offline-first chat component: end-to-end encrypted messages via CryptoKit, local storage with SQLite (GRDB), and CloudKit synchronization with per-device conflict resolution. Use Swift Concurrency (actors) to serialize state; outline data models, key management, offline queue, and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Auto Layout resolves constraints by calculating required cell heights based on content, then UITableView applies these heights during layout.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a high-throughput camera pipeline: configure an AVCaptureSession with video data output, process frames through Vision OCR on dedicated queues, maintain a circular buffer to bound memory under 200MB, export se…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement a CRDT-based collaborative notes engine. Each client stores a per-document CRDT in SQLite, writes edits to a durable log, and applies merges via Swift Actors. Sync uses delta diffs w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I'd implement an offline-first chat component using GRDB for local SQLite storage and CloudKit for synchronization. The data model would include: Message(id, conversationId, senderId, timestamp, nonce, ciphertext). End-…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates advanced iOS development skills including concurrency, security, offline-first architecture, and CloudKit data synchronization.\n\n## Key Concepts\n\n- End-to-end encryption using CryptoKit\n- Local persistence with SQLite via GRDB\n- Swift actors for race-free state management\n- CloudKit conflict resolution using per-device logical clocks\n- Secure key management in the Secure Enclave\n- Offline message queuing and sync orchestration\n\n## Code Example\n\n```swift\n// Swift data model\nstruct Message {\n    let id: UUID\n    let conversationId: UUID\n    let senderId: UUID\n    let timestamp: Date\n    let nonce: Data\n    let ciphertext: Data\n}\n```\n\n## Follow-up Questions\n\n- How would you handle key rotation for existing conversations?\n- What strategies would you use for efficient CloudKit batch operations?\n- How would you implement message deletion policies across devices?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-19",
        "questionId": "q-6904",
        "question": "In an iOS trading app, implement a price ticker module that subscribes to a live WebSocket feed for multiple assets. Requirements: per-asset state managed by actors; cap UI updates to 10 Hz; use a small in-memory cache with on-disk persistence; ensure crash-safe resume; handle network interruptions with exponential backoff; provide a test plan and discuss trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement a CRDT-based op log (Event: id, actor, op, ts) with per-document state and causal vectors. Use WebRTC data channels for sub-50ms updates; fallback to a relay server with reliable channel",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "For each asset, create a PriceActor to serialize updates. Buffer bursts and emit UI changes at 10 Hz. Maintain a lightweight in-memory cache plus on-disk persistence of the last price and timestamp using Codable.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a minimal, offline-first ModelShelf: fetch catalog from API, map to Codable ModelItem, store in a JSON file under the app's documents directory, read from disk when offline, display in a Swift",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a layered pipeline architecture: CoreBluetooth collects TelemetrySample streams from multiple BLE sensors into a Core Data buffer with three interconnected entities—Sensor (device metadata), TelemetrySample (time…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes knowledge of Swift concurrency, actors, and robust networking for live data.\n\n## Key Concepts\n- Swift Concurrency, actors for per-item isolation\n- WebSocket resilience and backoff\n- Local persistence and crash recovery\n- UI throttling and data consistency\n\n## Code Example\n```swift\nactor PriceActor {\n    private var price: Double = 0\n    private var timestamp: Date = Date()\n    \n    func update(price: Double, timestamp: Date) {\n        self.price = price\n        self.timestamp = timestamp\n    }\n}\n```\n\n## Follow-up Questions\n- How would you test throttling behavior?\n- What strategies would you use for memory management with thousands of assets?\n- How would you handle schema evolution for persisted data?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-20",
        "questionId": "q-28790",
        "question": "In a beginner iOS SwiftUI app named 'Quoter', fetch a list of inspirational quotes from a public REST API, display in a list, and show detail view on tap. Persist fetched quotes to a JSON file in the app’s documents directory. Add search by author, and pull-to-refresh to re-fetch. Include a minimal SwiftUI view sketch and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a modular on-device translation + diarization pipeline for an iOS video call app. Stream mic audio with AVAudioEngine + Combine; run a tiny CoreML translator (quantized) with server fallback; i",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a SwiftUI list bound to Quote models; use URLSession to fetch from quotable.io, decode with Codable, and save to Documents/quotes.json. Implement a SearchBar filtering by author and .refreshable",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use CryptoKit to symmetrically encrypt notes with a key stored in the Keychain. Notes modeled as Note(id, title, body, createdAt, isLocked). Persist encrypted data to the app's documents directory; lo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would start by creating a Core Data model with a Note entity (title: String, body: String, createdAt: Date), then build a SwiftUI List to display notes with a detail view for viewing and editing. For localization, I w…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical basics: networking, local persistence, search, and UI in SwiftUI.\n\n## Key Concepts\n\n- SwiftUI data flow, Codable parsing, file I/O in Documents, search filtration, pull-to-refresh.\n- Basic testing for parsing and UI state.\n\n## Code Example\n\n```swift\nstruct Quote: Codable, Identifiable { let id: String; let text: String; let author: String }\n```\n\n## Follow-up Questions\n\n- How would you handle API errors and retry logic in this flow?\n- How would you add lightweight caching invalidation when the API data changes?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-21",
        "questionId": "q-26984",
        "question": "Design a beginner iOS SwiftUI app named 'MoodNote' that lets users log daily mood entries with a short text body. Persist locally using Core Data with entities Entry (id, timestamp, mood, text). Add a feature to export all entries as a single JSON file encrypted with AES-GCM via CryptoKit, saved to the app's Documents directory; implement a one-click export button and a basic test plan. How would you implement this end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I'll design a CRDT-like delta sync architecture for SyncNotes with the following components: **Data Models:** ```swift struct Note { let id: UUID var content: String var tags: Set<String> var vectorClock: [String: Int]…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a WebSocket (URLSessionWebSocketTask) with automatic reconnect using exponential backoff, a background worker queue for message handling, and Idempotent application of presence events via sequence",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define a Core Data entity `Entry` with attributes `id` (UUID), `timestamp` (Date), `mood` (Int16), and `text` (String). Display entries in a SwiftUI List using `@FetchRequest`, and provide a form for creating new mood e…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Adopt MVVM architecture: a Codable `Note` model, a `NotesStore` that persists to the Documents directory, and a SwiftUI `List` view. Each row displays a title and a color tag badge. For accessibility, set `accessibility…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical iOS development skills, including Core Data modeling, SwiftUI data binding, secure encryption with CryptoKit, and fundamental testing practices.\n\n## Key Concepts\n- Core Data entity modeling and persistence\n- SwiftUI reactive data flow with `@FetchRequest`\n- Codable JSON serialization and file I/O\n- CryptoKit AES-GCM symmetric encryption\n- Secure storage for encryption keys (Keychain)\n- Unit testing for persistence, encoding, and encryption\n\n## Code Example\n```swift\nstruct Entry: Codable {\n    let id: UUID\n    let timestamp: Date\n    let mood: Int16\n    let text: String\n}\n\nclass EntryStore: ObservableObject {\n    @Published var entries: [Entry] = []\n\n    func exportToEncryptedJSON(key: SymmetricKey) throws -> URL {\n        let encoder = JSONEncoder()\n        encoder.dateEncodingStrategy = .iso8601\n        let data = try encoder.encode(entries)\n        let sealedBox = try AES.GCM.seal(data, using: key)\n        let encryptedData = sealedBox.combined!\n        let url = FileManager.default.urls(for: .documentDirectory, in: .userDomainMask)[0]\n            .appendingPathComponent(\"mood_export.enc\")\n        try encryptedData.write(to: url)\n        return url\n    }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-22",
        "questionId": "q-28390",
        "question": "In a beginner iOS SwiftUI app named 'SecureNotes', design a notes feature where each Note has id, title, body, createdAt. Encrypt on-disk storage with CryptoKit AES-GCM using a user-supplied passphrase derived via a KDF, and store as encrypted JSON in the documents directory. Add a URL scheme to share notes by id. Provide a minimal SwiftUI list/search view and outline a test plan that demonstrates encryption correctness and data integrity?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use CryptoKit AES-GCM to encrypt a JSON-encoded array of Note in the app's Documents directory, deriving a symmetric key from a user-entered passphrase with a KDF (e.g., HKDF). Note model: id, title,",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use an append-only Event log with events: AnchorCreated, AnchorMoved, AnnotationAdded, AnnotationEdited. Persist events and a light Snapshot in Core Data; on relaunch replay the last 100 events to reb",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a secure, offline-first iOS client for VaultNote. Implement per-document keys (CryptoKit, AES-GCM) with Secure Enclave-wrapped key material and periodic rotation. Drive low-latency sync via",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Decode FAQs with Codable from bundled JSON into FAQ models. Show a SwiftUI List with expandable rows. Add a debounced search using Combine that filters by question or answer. Persist last search query",
            "isCorrect": false
          }
        ],
        "explanation": "Why This Is Asked\nTests secure client-side persistence, key derivation, and SwiftUI data flow in a beginner context. It also checks the candidate’s ability to design a minimal sharing flow via URL schemes and verify data integrity after encryption/decryption.\n\nKey Concepts\n- CryptoKit AES-GCM for on-device encryption\n- Password-derived keys via a KDF (HKDF/PBKDF2)\n- Encrypted JSON persistence in the Documents directory\n- SwiftUI List + Search and basic data editing\n- URL schemes for sharing notes\n- Basic tests for encryption correctness, persistence, and search\n\nCode Example\n```swift\n// Pseudo-code: derive key and encrypt/decrypt notes\nimport CryptoKit\nlet passphrase = \"user-provided\"\nlet key = SymmetricKey(size: .bits256) // derived from passphrase in real code\n// Encrypt/Decrypt functions would go here\n```\n\nFollow-up Questions\n- How would you handle key rotation and re-encryption of existing notes?\n- How would you test resilience to file corruption or partial writes?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-23",
        "questionId": "q-578",
        "question": "What's the difference between weak and unowned references in Swift, and when would you use each?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt an offline-first CRDT-like approach: define vector-graphics operations (add/move/resize/delete) with per-annotation versions and tombstones, persist deltas in SQLite, and replay on startup. Use",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Bind a SwiftUI TextEditor to @State text. Implement onChange(text) to cancel a previous DispatchWorkItem and schedule a new one that calls saveDraft after 1 second. saveDraft writes to UserDefaults wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a chain of signed entries: each ledger row includes id, prevHash, ts, uid, did, nonce, payload, signature (Ed25519 via Secure Enclave). Store in SQLite WAL; queue unsynced entries in a transient t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Weak references are optional and automatically become nil when the referenced object is deallocated. Unowned references are non-optional and assume the referenced object will always exist. Use weak for optional relation…",
            "isCorrect": true
          }
        ],
        "explanation": "## Memory Management\n\nSwift uses ARC (Automatic Reference Counting) to manage memory automatically. Strong references can create retain cycles, so we use weak and unowned references to break them.\n\n## Weak References\n\n- Always optional (automatically set to nil on deallocation)\n- Use for optional relationships\n- Common patterns: delegates, outlets\n\n```swift\nweak var delegate: MyDelegate?\n```\n\n## Unowned References\n\n- Non-optional (assumes reference always exists)\n- Use when you know the reference will outlive the holder\n- Common patterns: parent-child relationships\n\n```swift\nunowned let parent: ParentClass\n```\n\n## Key Differences\n\nChoose weak when the reference might become nil during execution. Choose unowned when you're certain the reference will always exist throughout the object's lifetime.",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-24",
        "questionId": "q-9377",
        "question": "Design a beginner iOS SwiftUI app named PalettePolish that lets a user create color swatches with a name, hex value, and optional notes; provide a live color preview, validate the hex input, and persist locally using Codable to a file in Documents; include a searchable list with color previews and accessibility labels?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant CryptoKit keys stored in the Secure Enclave to sign per-tenant aggregates; a durable on-disk queue (SQLite) fed by a Combine ingestion pipeline; a tiny DSL for metrics like SUM(events) WHERE tenant='t1';…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use SwiftUI with two @AppStorage keys: one for themeColor (store as a hex or string) and one for fontSize (Double). Build a palette of color options; render them as tappable chips and reflect the sele",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define a Core Data entity `Entry` with attributes `id` (UUID), `timestamp` (Date), `mood` (Int16), and `text` (String). Display entries in a SwiftUI List using `@FetchRequest`, and provide a form for creating new mood e…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "PalettePolish is a beginner-friendly SwiftUI app that enables users to create and manage color swatches with custom names, hex values, and optional notes. The app features live color preview during input, hex validation…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates practical SwiftUI development skills beyond basic UI construction. It tests the ability to implement color input validation, live preview functionality, and accessibility features in a compact offline application. The assessment covers state management, regex validation, Codable persistence, and UI accessibility while maintaining an approachable complexity level for beginners.\n\n## Key Concepts\n\n- SwiftUI fundamentals and reactive state management\n- Regular expressions for hex color validation\n- Codable protocol and FileManager for local data persistence\n- Accessibility implementation and Dynamic Type support\n- Search functionality and data filtering techniques\n\n## Code Example\n\n```swift\n// Hex color validator\nfunc isValidHex(_ h",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-16T05:11:05.075Z",
    "version": 1
  }
]