[
  {
    "id": "test-ios",
    "channelId": "ios",
    "channelName": "IOS",
    "title": "IOS Knowledge Test",
    "description": "Test your understanding of IOS with 25 questions.",
    "questions": [
      {
        "id": "ios-q-0",
        "questionId": "q-6739",
        "question": "In an iOS video streaming app, design a resilient offline preview cache that supports seamless retry, prefetching, and resume after relaunch. You will prefetch upcoming segments, implement TTL eviction, cache keyframes, and resume playback after relaunch. Use AVPlayer with a custom resource loader, URLSession background downloads, and a manifest with segment offsets. Outline data models, caching strategy, testing plan, and trade-offs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an UploadItem model with properties for id, localPath, remoteURL, totalBytes, uploadedBytes, checksum, and upload state. Persist items as JSON in Library/Application Support. Use BGTaskScheduler with URLSessio…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a streaming, event-sourced client-side ML tagging pipeline: When photos are captured, a lightweight Core ML tagger runs in real time on-device, publishing TagEvent objects via Combine to a local replay log stored…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design and implement an offline preview cache for a live-streaming iOS app. Prefetch upcoming segments, cache keyframes with a TTL-based eviction policy, and resume playback after relaunch. Implement",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a pluggable analytics module that logs events to an append-only store using the following EventLog model: id (UUID), ts (timestamp), type (screen_view, user_action, or error), metadata (dictionary), appVersion, d…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a robust offline cache around streaming, focusing on prefetching, eviction, and crash-safe resume.\n\n## Key Concepts\n\n- AVPlayer and custom resource loading\n- Disk caching with TTL\n- Background downloads (URLSession)\n- State persistence for resume\n\n## Code Example\n\n```javascript\n// pseudo-code: prefetch loop outline\nfunction prefetchNextSegments(manifest, currentIndex) {\n  // fetch next N segments to disk cache\n}\n```\n\n## Follow-up Questions\n\n- How would you secure the cache against tampering?\n- How would you measure perf and cache hit rate in production?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-1",
        "questionId": "q-22819",
        "question": "In an iOS field-diagnostics app used by IBM and Databricks engineers, design a client-side data provenance system that yields a cryptographically verifiable audit trail for sensor readings and user actions, stored offline via an append-only ledger with Merkle chaining, and batch-uploaded when online. Requirements: 1) on-device signing with CryptoKit; 2) a tiny DSL for provenance queries; 3) Combine-based ingestion; 4) crash-safe resume with a replay log; 5) a SwiftUI dashboard showing latest provenance blocks; 6) a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: encode flags as a map of id and a compiled DSL predicate over inputs (imageQuality, GPSMode, batteryLevel, ambientLight). Use Combine to re-evaluate on input changes; persist flags and a cra",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "BLE (100-200 Hz) → SensorStreamBuffer (lock-free ring buffer) → KalmanFusion (on-device sensor fusion) → TelemetryStream (per-sample timestamps) → SQLite (WAL mode, compressed time-series) + write-ahead log for crash-sa…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an append-only ledger schema: Block(id, prevHash, ts, dataHash, signature). Store blocks in SQLite; sign each with CryptoKit P-256 and verify the chain on startup. Ingest sensor readings thr",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-user envelope encryption: a per-user AES-256-GCM key is wrapped by a Keychain key; transcripts are stored as encrypted binary blobs in Core Data. Downloads run via URLSessionBackgroundTask; on",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cryptographically verifiable on-device provenance with offline resilience in iOS using Swift, CryptoKit, and Combine. It also exercises data modeling for an append-only ledger, Merkle chaining, and a crash-safe replay mechanism within a mobile product.\n\n## Key Concepts\n\n- Append-only ledger with Merkle chaining\n- CryptoKit signatures and verification\n- Offline-first ingestion with Combine\n- Crash-safe resume via replay log\n- Tiny DSL for provenance queries\n- SwiftUI integration\n\n## Code Example\n\n```swift\nstruct LedgerBlock {\n  let id: UUID\n  let prevHash: String\n  let timestamp: Date\n  let dataHash: String\n  let signature: Data\n}\n```\n\n## Follow-up Questions\n\n- How would clock skew across devices affect provenance integrity, and how would you mitigate?\n- What strategies would you use to scale the ledger for millions of blocks and ensure fast verification?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-2",
        "questionId": "q-25959",
        "question": "In a beginner iOS SwiftUI app named 'MoodGraph', implement a local mood tracker: MoodEntry { date: Date, mood: Int (1-5), note: String? }. Build a UI to pick mood with a 5-point control and optional note, persist entries to a JSON file in the app's Documents directory, and compute daily average mood to render a simple line chart with SwiftUI Canvas. Ensure crash-safe resume by loading on launch and persisting after each change. Include a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Optional chaining (?.) safely unwraps optionals, returning nil if any link fails instead of throwing runtime errors. Unlike force unwrapping (!), it prevents crashes. It's more concise than if-let/guard-let for single a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a UICollectionView grid for episodes using a compositional layout and a single diffable data source. Use NSCache to cache episode artwork, set up prefetching to load next pages, and implemen",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Store notes locally in SQLite with per-note revision tracking and a change log; edits increment revision and emit deltas. Sync via WebSocket streaming with note ID, revision, delta, device ID, and timestamp. Resolve con…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a MoodEntry model conforming to Codable, store the array in a single JSON file within the app's Documents directory using FileManager, and atomically persist changes by writing to a temporary file before repla…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question validates core competencies in data modeling, local persistence, data aggregation, SwiftUI visualization, and crash-safe resume behavior, along with the candidate's ability to plan basic testing.\n\n## Key Concepts\n\n- Codable and FileManager for JSON persistence\n- SwiftUI Canvas for custom chart rendering\n- Date normalization to day granularity for aggregation\n- Atomic file writes for crash safety\n- Lightweight test planning\n\n## Code Example\n\n```swift\nstruct MoodEntry: Codable, Identifiable {\n    let id: UUID\n    let date: Date\n    let mood: Int // 1-5 scale\n    let note: String?\n}\n```\n\n```swift\nclass MoodStore: ObservableObject {\n    @Published var entries: [MoodEntry] = []\n    \n    private let url: URL\n    private let fileManager = FileManager.default\n    private let queue = DispatchQueue(label: \"com.moodgraph.persistence\")\n\n    init() {\n        let documentsURL = fileManager.urls(for: .documentDirectory, in: .userDomainMask)[0]\n        self.url = documentsURL.appendingPathComponent(\"mood_entries.json\")\n        load()\n    }\n\n    func addEntry(mood: Int, note: String?) {\n        let entry = MoodEntry(id: UUID(), date: Date(), mood: mood, note: note)\n        entries.append(entry)\n        save()\n    }\n\n    private func save() {\n        queue.async { [weak self] in\n            guard let self = self else { return }\n            do {\n                let data = try JSONEncoder().encode(self.entries)\n                let tempURL = self.url.deletingLastPathComponent()\n                    .appendingPathComponent(\"mood_entries_temp.json\")\n                try data.write(to: tempURL, options: .atomic)\n                try fileManager.moveItem(at: tempURL, to: self.url)\n            } catch {\n                print(\"Failed to save: \\(error)\")\n            }\n        }\n    }\n\n    private func load() {\n        guard fileManager.fileExists(atPath: url.path) else { return }\n        do {\n            let data = try Data(contentsOf: url)\n            entries = try JSONDecoder().decode([MoodEntry].self, from: data)\n        } catch {\n            print(\"Failed to load: \\(error)\")\n        }\n    }\n\n    func dailyAverages() -> [(date: Date, average: Double)] {\n        let calendar = Calendar.current\n        let grouped = Dictionary(grouping: entries) { entry in\n            calendar.startOfDay(for: entry.date)\n        }\n        return grouped.map { (date, entries) in\n            let avg = Double(entries.reduce(0) { $0 + $1.mood }) / Double(entries.count)\n            return (date: date, average: avg)\n        }.sorted { $0.date < $1.date }\n    }\n}\n```\n\n```swift\nstruct MoodChartView: View {\n    let data: [(date: Date, average: Double)]\n    \n    var body: some View {\n        Canvas { context, size in\n            guard data.count > 1 else { return }\n            let maxMood: Double = 5.0\n            let stepX = size.width / CGFloat(data.count - 1)\n            \n            var path = Path()\n            for (index, point) in data.enumerated() {\n                let x = CGFloat(index) * stepX\n                let y = size.height - (CGFloat(point.average / maxMood) * size.height)\n                if index == 0 {\n                    path.move(to: CGPoint(x: x, y: y))\n                } else {\n                    path.addLine(to: CGPoint(x: x, y: y))\n                }\n            }\n            \n            context.stroke(path, with: .color(.blue), lineWidth: 2)\n        }\n        .frame(height: 200)\n    }\n}\n```\n\n## Test Plan\n\n1. **Unit Tests**\n   - Verify MoodEntry encoding/decoding roundtrip\n   - Test dailyAverages() with known input data\n   - Validate mood range enforcement (1-5)\n\n2. **Integration Tests**\n   - Add entry, verify file exists in Documents\n   - Restart app, verify entries persist\n   - Add multiple entries same day, verify correct average\n\n3. **UI Tests**\n   - Verify mood picker limits selection to 1-5\n   - Confirm note field is optional\n   - Validate chart renders non-empty data",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-3",
        "questionId": "q-24640",
        "question": "In an iOS medical imaging review app, design an offline-first annotation system for DICOM images that supports concurrent edits across devices via a CRDT-like stroke model, with per-stroke provenance and cryptographic signing. Include data models, persistence, merge strategy, and a minimal SwiftUI overlay; describe a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an offline-first annotation system for DICOM images with concurrent edits across devices using a CRDT-like stroke model. Each stroke carries a unique ID, layer identifier, and timestamp. Use Combine to stream…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a SwiftUI app QuickChecklist: a Checklist with id, title, dueDate, priority, and items (title, done). Persist with Codable to Documents/quickchecklists.json, load on launch, and watch for chang",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Model a Draft entity (id: UUID, title, content, lastEdited: Date, status: Draft/Published) stored in Core Data with NSPersistentContainer. Offline-first. Sync via URLSession to a REST endpoint; implem",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architecture: capture frames at 15fps with AVFoundation; route through a ring buffer to a tiny Core ML model (Vision) on-device; persist Frame(id,timestamp,hash), AnalysisResult(frameId, score, decisi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses the ability to design offline-first collaboration systems with conflict resolution and security guarantees for medical data on iOS.\n\n## Key Concepts\n\n- **CRDT-like data model**: Operations as commutative replicas with unique stroke IDs\n- **Append-only log**: Immutable event store for crash recovery and replay\n- **Cryptographic signing**: Per-stroke signatures using CryptoKit for integrity\n- **Merkle-based provenance**: Hash-linked stroke chain enabling audit trails\n- **Combine**: Reactive event streaming for local state propagation\n- **SwiftUI overlay**: Lightweight canvas for rendering annotations over DICOM\n\n## Code Example\n\n```swift\nimport Foundation\nimport CoreGraphics\n\nstruct Stroke: Identifiable, Codable {\n  let id: UUID\n  let layer: String\n  let points: [CGPoint]\n  let colorHex: String\n  let width: CGFloat\n  let timestamp: Date\n  let signature: Data\n}\n```\n\n## Test Plan\n\n1. **Unit Tests**: Verify CRDT merge logic produces consistent results regardless of operation order\n2. **Integration Tests**: Test offline-to-online sync with conflict scenarios\n3. **Security Tests**: Validate cryptographic signatures and tamper detection\n4. **Performance Tests**: Measure annotation latency and sync throughput with large datasets\n5. **UI Tests**: Confirm SwiftUI overlay renders correctly across device sizes",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-4",
        "questionId": "q-28117",
        "question": "Context: iOS AR collaboration app for maintenance teams. Task: design a client-side CRDT-based editor enabling concurrent AR annotations and notes across devices via CloudKit. Requirements: 1) data model: Document, Annotation, Operation; 2) a CRDT type (e.g., Sequence CRDT) with tombstones for offline edits; 3) crash-safe resume with deterministic replay; 4) offline-first, batched, idempotent sync; 5) local persistence (Realm/CoreData); 6) minimal SwiftUI AR view sketch showing annotations and sync status?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use async/await URLSession to fetch a 7-day forecast JSON, decode into ForecastDay(date: Date, high: Double, low: Double, condition: String). Cache the raw JSON to the Caches directory and load from c",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: implement a Sequence CRDT for AR annotations, modeling edits as per-item ops (insert/delete/move) with unique IDs and causal metadata. Persist locally with Realm, including tombstones. Batch",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a streaming client for a real‑time analytics iOS app: use a WebSocket feed with per‑tick sequence numbers, a ring buffer for in‑memory deltas, and per‑symbol SQLite persistence of the last 1000",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a ModelManager that stores a manifest with modelName, version, sha256, size, and URL. Download to a temp directory in the background, verify the hash, then atomically swap by updating a versioned",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses capability to design advanced, crash-resilient, offline-first synchronization for real-time AR collaboration on iOS using CRDTs, CloudKit, and robust local persistence.\n\n## Key Concepts\n- Sequence CRDTs for ordered annotations\n- Offline-first with batched CloudKit sync\n- Tombstones and causal metadata for merges\n- Crash-safe resume via deterministic replay\n- ARKit + SwiftUI integration and persistence strategy\n\n## Code Example\n```javascript\n// Minimal CRDT operation example (pseudo)\ntype Op = { type: 'insert'|'delete'|'move', id: string, at?: number, item?: any, ts: number }\n```\n\n## Follow-up Questions\n- How would you test out-of-order deliveries and late joins?\n- How would you handle data privacy and access control in synced documents?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-5",
        "questionId": "q-28837",
        "question": "Design a beginner iOS SwiftUI app named 'PulseNote' that emphasizes accessibility and localization. The app stores notes locally (title, body) using Core Data, formats timestamps with Locale, supports Dynamic Type, VoiceOver labels, and high-contrast mode. Provide a minimal SwiftUI list sketch, a data model plan, and a basic test plan focusing on accessibility and localization coverage. What would you implement first?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-device offline-first ingestion: models Event, Device, Session in Core Data; a small in-memory ring buffer for high-rate streams; deduplicate by (deviceID,timestamp,type); compute 1s and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would start by creating a Core Data model with a Note entity (title: String, body: String, createdAt: Date), then build a SwiftUI List to display notes with a detail view for viewing and editing. For localization, I w…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Adopt an offline-first CRDT-like approach: define vector-graphics operations (add/move/resize/delete) with per-annotation versions and tombstones, persist deltas in SQLite, and replay on startup. Use",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-device Actor streams to serialize inbound readings, buffer in-memory, batch SQLite writes, and schedule low-power background flushes. Encrypt data at rest with CryptoKit, sign entries with a d",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates a candidate's understanding of real-world iOS development concerns. Accessibility and localization are essential for reaching diverse users, making this a practical assessment of foundational skills.\n\n## Key Concepts\n- Core Data modeling and persistence\n- SwiftUI List and navigation patterns\n- Localization with Localizable.strings and Locale-aware date formatting\n- Dynamic Type support for scalable text\n- VoiceOver labels and accessibility identifiers\n- High-contrast mode support\n- UIKit/SwiftUI testing strategies\n\n## Code Example\n```swift\nstruct Note: Identifiable {\n    let id: UUID\n    let title: String\n    let body: String\n    let createdAt: Date\n}\n\nstruct NoteListView: View {\n    @FetchRequest(sortDescriptors: [SortDescriptor(\\.createdAt, order: .reverse)])\n    var notes: FetchedResults<Note>\n\n    var body: some View {\n        List(notes) { note in\n            NavigationLink(destination: NoteDetailView(note: note)) {\n                VStack(alignment: .leading, spacing: 4) {\n                    Text(note.title)\n                        .font(.headline)\n                        .accessibilityLabel(\"Note title: \\(note.title)\")\n                    \n                    Text(note.createdAt, style: .date)\n                        .font(.subheadline)\n                        .foregroundColor(.secondary)\n                        .accessibilityLabel(\"Created on \\(note.createdAt.formatted(date: .long, time: .omitted))\")\n                }\n            }\n        }\n    }\n}\n```\n\n## Data Model Plan\n| Field | Type | Purpose |\n|-------|------|---------|\n| id | UUID | Unique identifier |\n| title | String | Note title |\n| body | String | Note content |\n| createdAt | Date | Creation timestamp |\n\n## Test Plan\n| Area | Coverage |\n|------|----------|\n| Dynamic Type | Verify layout adapts from AX1 to AX7 sizes |\n| VoiceOver | Test all elements with `accessibilityLabel` and `accessibilityValue` |\n| Localization | Test date formatting across en_US, de_DE, ja_JP locales |\n| High Contrast | Validate WCAG 2.1 AA compliance |\n| Localization | Test string translations load correctly for supported locales |",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-6",
        "questionId": "q-5124",
        "question": "iOS Beginner Challenge: Build a SwiftUI settings screen that lets a user choose a theme color from a small palette and adjust font size with a slider. Persist selections with @AppStorage so they survive app restarts; apply changes in real time to a live text preview; ensure accessibility labels for both color choices and the font size slider; include a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use SwiftUI with two @AppStorage keys: one for themeColor (store as a hex or string) and one for fontSize (Double). Build a palette of color options; render them as tappable chips and reflect the sele",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use MapKit for live routing, CoreData for offline persistence, and a CloudKit sync pipeline. An actor-isolated fetcher batches ETA/route updates, applies optimistic UI, and resolves conflicts with tim",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Swift Actor to model the ingestion pipeline, parse BLE packets into immutable SensorReading values, deduplicate using a per-device sequence number, compute 1-minute averages in a sliding window, and store results…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bind a SwiftUI TextEditor to @State text. Implement onChange(text) to cancel a previous DispatchWorkItem and schedule a new one that calls saveDraft after 1 second. saveDraft writes to UserDefaults wi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests basic SwiftUI state management, on-device persistence, live UI updates, and accessibility considerations in a beginner-friendly setting.\n\n## Key Concepts\n- SwiftUI @AppStorage for lightweight persistence\n- Color theming and dynamic UI updates\n- Accessibility labeling and dynamic type considerations\n- Basic test planning: persistence, accessibility, UI consistency\n\n## Code Example\n```swift\n@AppStorage(\"themeColor\") private var themeColorHex: String = \"#4285F4\"\n@AppStorage(\"fontSize\") private var fontSize: Double = 14.0\n\nvar body: some View {\n  VStack {\n    // color palette and font size slider UI here\n    Text(\"Preview text\").font(.system(size: CGFloat(fontSize)))\n      .foregroundColor(Color(hex: themeColorHex))\n  }\n}\n```\n\n## Follow-up Questions\n- How would you validate edge cases like invalid saved color values?\n- How would you extend this to support dark mode vs light mode themes?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-7",
        "questionId": "q-11450",
        "question": "In an iOS app that reads BLE broadcasts from 20 environmental sensors (PM2.5, CO2, RH) for a live dashboard, design a backpressure-aware ingestion pipeline that deduplicates, preserves out-of-order arrivals, stores compact updates locally, and debounces UI updates to ~30fps; include data models, caching strategy, and a test plan; provide a minimal SwiftUI/Combine sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use CryptoKit with a per-device key stored in the Keychain and rotated via remote config; encrypt tokens, location, and ride state with AES-GCM using random IVs; store only essential fields to minimiz",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a per-sensor bounded in-memory queue with a reordering buffer to handle backpressure. Deduplicate incoming data using the composite key (sensorId, sequenceNumber) and reorder by timestamp before persistence. S…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Offline-first album sync using a CRDT (OR-Set) for PhotoMetadata (id, albumId, caption, tags, location, timestamp). Each device applies local edits, tombstones, and vector-clock-based merges. Enforce per-album ACLs duri…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Decode a bundled JSON into Article structs, toggle read on tap, and persist the updated array to UserDefaults via JSONEncoder/Decoder so state survives restarts. Implement a 300ms debounce on taps and",
            "isCorrect": false
          }
        ],
        "explanation": "## Technical Corrections\n\n1. **Throttle vs Debounce (Critical)**: The original answer incorrectly stated \"debounced Combine publishers\" but for a live dashboard at ~30fps, you must use **throttle** (with `latest: true`). Debouncing waits for a pause in events before emitting—introducing artificial delay unsuitable for real-time data. Throttling enforces a consistent rate without initial delay.\n\n2. **Answer truncation**: The original answer was cut off mid-code. This version provides the complete implementation.\n\n## Key Implementation Details\n\n- **Bounded Queue**: Uses an actor-based `SensorDataPipeline` with per-sensor queues and a global reorder buffer. Returns `false` when buffer exceeds 1000 entries, signaling backpressure to BLE layer.\n\n- **Deduplication**: Composite key `(sensorId, sequenceNumber)` ensures each unique reading is processed exactly once, even with retransmissions.\n\n- **Reordering Buffer**: Collects readings from all sensors, sorts by timestamp, and only emits those with incrementing sequence numbers per sensor.\n\n- **SQLite WAL Mode**: Enables concurrent reads during writes and provides crash recovery.\n\n- **Throttled UI Updates**: Uses `collect(.byTime(_, .milliseconds(33)))` ≈ 30fps, combined with `@Published` to drive SwiftUI updates. The `latest: true` behavior ensures most recent value is always shown.\n\n## Test Plan Coverage\n\n- **Deduplication test**: Verify duplicates are rejected\n- **Backpressure test**: Verify buffer overflow handling\n- **Reordering test**: Verify out-of-order arrival handling\n- **Throttling test**: Verify render frequency\n- **Recovery test**: Verify WAL persistence after crash",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-8",
        "questionId": "q-20491",
        "question": "Design an on-device provenance engine for a personal finance iOS app that logs all user actions (expense create/edit, category change) as an append-only log, signs each entry with a device key via CryptoKit, and exports cryptographic proofs to the cloud for audit. Requirements: crash-safe replay log on relaunch; offline operation with eventual sync; data models: Action, Entry, Signature; a SwiftUI dashboard showing last 5 entries and verification status; test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Core Data with an immutable Entry entity: id, timestamp, actionType, payload, signature, previousHash. Maintain a replay log for crash-safe resume. Generate per-device ECDSA P-256 keys in Secure E",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a content-addressable SQLite cache of tiles: tile_id, x/y/z, checksum, lastAccess, size. Use NSCache for in‑memory hits. Prefetch along planned route within a radius; also fetch adjacent til",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deterministic on-device A/B with hashed userId for sampling; store Assignment(experimentId, variant, validUntil) in SQLite; replay log for crash-safe resume. Server sends signed JSON config; verify vi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an offline-first image gallery cache: an LRU disk cache plus in-memory NSCache, with a manifest table storing URL, etag, version, and local path. Use URLSession background downloads to fetch",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests on-device provenance, cryptographic integrity, and offline resiliency in privacy-conscious apps. It requires experience with CryptoKit, Secure Enclave, Core Data modeling, and robust replay logs.\n\n## Key Concepts\n\n- Append-only logs\n- Device-bound signing with CryptoKit\n- Crash-safe resume and replay\n- Proof export and auditability\n\n## Code Example\n\n```swift\n// Signing example (pseudo)\nlet payload = entry.payloadData\nlet signature = try deviceKey.sign(data: payload)\n```\n\n## Follow-up Questions\n\n- How would you rotate keys without breaking proofs?\n- How would you test offline proof export and verification?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-9",
        "questionId": "q-2391",
        "question": "Design a high-performance, offline-first photo feed in an iOS app. Render a UICollectionView that shows square image tiles with captions, support infinite scrolling, and prioritize visible cells for image downloads. Implement disk caching (LRU), memory pressure eviction, and offline fallback when network is unavailable. Ensure VoiceOver and Dynamic Type. Provide data model, caching strategy, testing plan, and a concise code sketch for an ImageLoader using async/await?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To implement offline-first AR collaboration, store edits as events: {type:add/move/edit/delete, id, payload, author, ts}. Use an OR-Set CRDT to resolve concurrent adds/deletes; replay the log on start",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Note model: id, title, content, date, latitude, longitude. Persist as Codable to notes.json in Documents; support append/update with atomic write. Show MapKit annotations for each note in SwiftUI; tap",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a two-tier image cache (NSCache + disk) and a Swift async/await loader. Cache key from the URL hash; store images in Caches/Photos. Prefetch upcoming cells, cancel in-flight fetches on reuse, and",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-document envelope encryption: each document has a symmetric key wrapped with the recipient's public keys; derive keys via HKDF-SHA256; store keys in Keychain. Encrypt annotations with the docu",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design an offline-first image pipeline with robust caching, memory management, and accessibility in a real UI scenario.\n\n## Key Concepts\n- Two-tier caching (memory + disk)\n- LRU eviction under memory pressure\n- Prefetching and cancellation tied to visible cells\n- Accessibility: Dynamic Type and VoiceOver labels\n- Async/await data loading and main-thread UI updates\n\n## Code Example\n```swift\nimport UIKit\n\nactor ImageLoader {\n    private let memCache = NSCache<NSString, UIImage>()\n    private let fileManager = FileManager.default\n    private let diskDir: URL\n\n    init(diskFolder: URL) { self.diskDir = diskFolder }\n\n    func load(from url: URL) async -> UIImage? {\n        let key = url.absoluteString as NSString\n        if let image = memCache.object(forKey: key) { return image }\n        let path = diskDir.appendingPathComponent(url.lastPathComponent)\n        if let data = try? Data(contentsOf: path), let image = UIImage(data: data) {\n            memCache.setObject(image, forKey: key)\n            return image\n        }\n        guard let (data, _) = try? await URLSession.shared.data(from: url),\n              let image = UIImage(data: data) else { return nil }\n        memCache.setObject(image, forKey: key)\n        try? data.write(to: path)\n        return image\n    }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-10",
        "questionId": "q-19463",
        "question": "Design a beginner iOS SwiftUI app named 'TaskGlide' that lets users create daily tasks with fields: title, dueDate, priority, and isDone. Persist data locally via Codable to the app's Documents directory. Provide a search bar that debounces input (300ms) before filtering the list; include a minimal SwiftUI view sketch showing task rows and overdue highlighting. Include a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a three-tier cache: in-memory NSCache for hot images, disk cache in the Caches dir for cold ones, and a URLSession-based fetch with an in-flight map. Use a bounded OperationQueue for prefetc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Weak references are optional and automatically become nil when the referenced object is deallocated. Unowned references are non-optional and assume the referenced object will always exist. Use weak for optional relation…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a modular on-device pipeline: encrypted SQLite buffers for tick data, Actors-based windowed aggregation, and a UI-friendly signal layer that avoids raw ticks. Implement crash-safe resume via a",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Model: Task(id: UUID, title: String, dueDate: Date, priority: Priority, isDone: Bool). Persist as JSON via Codable to Documents/taskglide/tasks.json using FileManager; load on launch, save on changes.",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a lightweight local data model, simple persistence, a SwiftUI UI, and a debounced search flow.\n\n## Key Concepts\n- Codable persistence in the Documents directory\n- FileManager-based read/write of a single JSON file\n- SwiftUI List rendering with overdue highlighting\n- Combine debounce for search input\n- Basic XCTest coverage for persistence and timing\n\n## Code Example\n```swift\nstruct Task: Codable {\n  let id: UUID\n  let title: String\n  let dueDate: Date\n  let priority: Priority\n  var isDone: Bool\n}\nenum Priority: String, Codable { case low, medium, high }\n```\n\n## Follow-up Questions\n- How to migrate when Task changes shape?\n- How to test concurrent saves and resume after app termination?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-11",
        "questionId": "q-29842",
        "question": "In an iOS live-audio app for podcasting, design a real-time on-device audio processing pipeline that uses a tiny DSL to describe per-stream effects (e.g., 'IF level>0.6 THEN compress 2:1'), streams audio frames from AVAudioEngine, applies chains with sub-10ms latency, and persists rules with crash-safe checkpoints. Provide data models, evaluation strategy, persistence (SQLite/Realm), test plan, and a SwiftUI dashboard showing active chain and last applied settings. How would you implement this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an offline-first draft store using a Swift actor per draft, with a tiny DSL parsed into a deterministic state machine (DRAFT, AWAITING_REVIEW, APPROVED, REJECTED, SYNCED). Use a Combine pipe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a compact DSL parser for Rule{ id, condition, action } to drive an in-memory graph of AVAudioUnitEffects. Process frames via AVAudioEngine taps, evaluating rules per frame and applying matching",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a JSON-driven dynamic form renderer for iOS: fetch a schema, render fields at runtime, validate per field type, persist partial responses in CoreData, and sync via a background URLSession when online. Include sch…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a WebSocket (URLSessionWebSocketTask) with automatic reconnect using exponential backoff, a background worker queue for message handling, and Idempotent application of presence events via sequence",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nExplores on-device real-time DSP, DSL-driven control, crash-safe persistence, and iOS audio architecture.\n\n## Key Concepts\n- Tiny DSL parsing and per-frame evaluation\n- AVAudioEngine taps and low-latency processing\n- Crash-safe persistence with replay logs\n- SQLite data models and SwiftUI dashboard\n\n## Code Example\n```swift\nstruct Rule { let id: String; let condition: String; let action: String }\n```\n\n## Follow-up Questions\n- How to ensure thread-safety in DSL evaluation across audio/render threads?\n- How would you test resilience to long audio sessions with many rules?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-12",
        "questionId": "q-2355",
        "question": "Design a live-map ETA/route update system for a rideshare app with intermittent connectivity. Describe an architecture using MapKit, CoreData offline persistence, and CloudKit sync with Swift concurrency. Include conflict resolution strategy, data integrity guarantees, and test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Store `Task` as a Codable struct (id: UUID, title: String, dueDate: Date, completed: Bool). Persist to Documents/taskflow_tasks.json using FileManager, writing on every change to ensure crash-safe persistence. Search is…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a modular SyncEngine with per-anchor CRDT state and a compact transform pipeline; use WebRTC data channels for near-real-time sync with a relay fallback to a REST endpoint for outages; log every state mutation t…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a WebSocket (URLSessionWebSocketTask) with automatic reconnect using exponential backoff, a background worker queue for message handling, and Idempotent application of presence events via sequence",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use MapKit for live routing, CoreData for offline persistence, and a CloudKit sync pipeline. An actor-isolated fetcher batches ETA/route updates, applies optimistic UI, and resolves conflicts with tim",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes how a senior iOS engineer designs a resilient, offline-capable live-mapping feature.\n\n## Key Concepts\n- MapKit live updates\n- CoreData offline store\n- CloudKit sync and Swift concurrency\n- Conflict resolution and data integrity\n- Test strategies for flaky networks\n\n## Code Example\n```javascript\n// Swift-like pseudocode illustrating an actor-based sync pipeline\nactor RouteSync {\n  func apply(_ updates: [RouteUpdate]) {\n    // merge with local store, resolve conflicts by timestamp\n  }\n}\n```\n\n## Follow-up Questions\n- How would you measure latency vs. consistency in this system?\n- How would you test conflict scenarios with deterministic replay?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-13",
        "questionId": "q-6004",
        "question": "Design a beginner iOS SwiftUI app named TagNote that lets users create notes with a title, body, and optional tag. Persist locally with Core Data; implement tag-based filtering and a debounced search bar. Ensure data remains across restarts, and handle app lifecycle events (foreground/background). Include a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build a beginner iOS SwiftUI app called TagNote: create notes with title, body, and an optional tag. Persist via Core Data locally; implement tag-based filtering and a debounced search. Use MVVM, fetc",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a non-destructive FilterGraph using Swift actors: each filter is a node with input/output CIImage; edits propagate asynchronously via async/await; base image plus ordered passes render the f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I'd implement a bounded in-memory ring per device as the intake buffer, a streaming layer using AsyncSequence and Combine to publish rolling 1s/5s aggregates, and a SQLite WAL store with a replay log",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Record audio memos using AVAudioRecorder, save to Documents as a unique file, and write to a temp path first then move for atomic save. Build a Codable Memo struct with id, date, duration, path; list",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical iOS data modeling, SwiftUI wiring, and offline persistence with Core Data.\n\n## Key Concepts\n- SwiftUI MVVM data flow\n- Core Data persistence\n- @FetchRequest and predicates for filtering\n- Debounced search with Combine/DispatchQueue\n- Background vs main thread work and lifecycle handling\n- Accessibility and basic testing\n\n## Code Example\n```swift\n// Minimal Core Data save helper\nfunc saveNote(context: NSManagedObjectContext, title: String, body: String, tag: String?) {\n  let note = NoteEntity(context: context)\n  note.id = UUID()\n  note.title = title\n  note.body = body\n  note.tag = tag\n  try? context.save()\n}\n```\n\n## Follow-up Questions\n- How would you add full-text search across title and body?\n- How would you test debounced search behavior in unit tests?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-14",
        "questionId": "q-3796",
        "question": "Design a beginner-friendly iOS SwiftUI screen: a note editor with a live word count. Debounce user input with Combine (300ms) before updating the count, and persist the last note to disk across restarts. Include handling of background/foreground transitions and a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement an on-device event-sourced pipeline: model Command, Batch, Session; persist as an append-only log with sequence numbers; sign each entry with CryptoKit using a device key; offline qu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use async/await URLSession to fetch the API and decode with Codable into WeatherResponse and DayForecast. Cache the latest JSON on disk via FileManager. Bind a SwiftUI List to the forecast array and p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a SwiftUI note editor that shows a live word count. Drive it with Combine: feed the field into a CurrentValueSubject<String,Never>, debounce 300ms on RunLoop.main, then update a @Published w",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Core Data models for Document and Page entities, PDFKit for PDF export, and on-device Vision OCR via VNRecognizeTextRequest. Implement a per-document state machine (idle → capturing → processing → done) to manage th…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests SwiftUI, Combine, and simple disk persistence in a beginner-friendly way, plus lifecycle handling.\n\n## Key Concepts\n- SwiftUI state management and data binding\n- Combine debounce to throttle rapid changes\n- Lightweight persistence to disk (Documents) with Codable or plain text\n- Lifecycle considerations (background/foreground)\n\n## Code Example\n```swift\nimport SwiftUI\nimport Combine\n\nclass NoteViewModel: ObservableObject {\n  @Published var text: String = \"\"\n  @Published var wordCount: Int = 0\n\n  private var cancellables = Set<AnyCancellable>()\n\n  init() {\n    $text\n      .debounce(for: .milliseconds(300), scheduler: RunLoop.main)\n      .map { $0.split(whereSeparator: { $0 == \" \" || $0 == \"\\n\" }).count }\n      .assign(to: \\.wordCount, on: self)\n      .store(in: &cancellables)\n  }\n\n  func saveToDisk() {\n    // write to Documents/notes.txt\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test that debounce correctly throttles rapid input?\n- How would you handle a save failure and retry strategy?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-15",
        "questionId": "q-7408",
        "question": "Design a dynamic form engine for iOS that renders UI from a JSON schema, including field types, constraints, conditional visibility, live validation, offline save (Core Data), and batch REST submission with idempotent retries. How would you implement this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a SwiftUI-based dynamic form engine that consumes a JSON schema to render adaptive forms with real-time validation, conditional field visibility, and robust offline persistence using Core Data.",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I'd implement an offline-first chat component using GRDB for local SQLite storage and CloudKit for synchronization. The data model would include: Message(id, conversationId, senderId, timestamp, nonce, ciphertext). End-…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a streaming client for a real‑time analytics iOS app: use a WebSocket feed with per‑tick sequence numbers, a ring buffer for in‑memory deltas, and per‑symbol SQLite persistence of the last 1000",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a CRDT-based offline-first collaboration layer: each Note contains ordered Fragment objects and uses an OR-Set style tombstone mechanism for deletions. Store data locally with Core Data and sync via CloudKit usin…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates your ability to translate a data-driven schema into a production-ready iOS application with offline resilience, dynamic UI generation, and enterprise-grade networking patterns.\n\n## Key Concepts\n\n- Dynamic SwiftUI UI generation from JSON schema definitions\n- Reactive state management with Combine for live validation\n- Offline-first architecture using Core Data with conflict resolution\n- Idempotent REST operations with exponential backoff retry logic\n- Field-level dependency management for conditional visibility\n\n## Code Example\n\n```swift\nstruct FieldDefinition: Codable {\n    let id: String\n    let type: FieldType\n    let label: String\n    let validation: ValidationRules?\n    let visibility: VisibilityRules?\n    let dependencies: [String]?\n}\n\nenum FieldType: String, Codable {\n    case text, email, number, date, picker, toggle\n}\n\nstruct ValidationRules: Codable {\n    let required: Bool?\n    let minLength: Int?\n    let maxLength: Int?\n    let pattern: String?\n    let customRules: [String]?\n}\n\nclass DynamicFormEngine: ObservableObject {\n    @Published var formState: [String: Any]\n    @Published var validationErrors: [String: String]\n    @Published var visibleFields: Set<String>\n    \n    private let schema: FormSchema\n    private let persistenceManager: CoreDataManager\n    private let networkManager: NetworkManager\n    \n    init(schema: FormSchema) {\n        self.schema = schema\n        self.formState = [:]\n        self.validationErrors = [:]\n        self.visibleFields = Set(schema.fields.map { $0.id })\n        self.persistenceManager = CoreDataManager()\n        self.networkManager = NetworkManager()\n        \n        setupFieldDependencies()\n        loadOfflineData()\n    }\n    \n    func updateValue(for fieldId: String, value: Any) {\n        formState[fieldId] = value\n        validateField(fieldId)\n        updateFieldVisibility()\n        saveOffline()\n    }\n    \n    private func validateField(_ fieldId: String) {\n        guard let field = schema.fields.first(where: { $0.id == fieldId }),\n              let validation = field.validation else {\n            validationErrors.removeValue(forKey: fieldId)\n            return\n        }\n        \n        let value = formState[fieldId]\n        var errors: [String] = []\n        \n        if validation.required == true && (value == nil || value as? String == \"\") {\n            errors.append(\"This field is required\")\n        }\n        \n        if let stringValue = value as? String {\n            if let minLength = validation.minLength, stringValue.count < minLength {\n                errors.append(\"Minimum length is \\(minLength) characters\")\n            }\n            \n            if let maxLength = validation.maxLength, stringValue.count > maxLength {\n                errors.append(\"Maximum length is \\(maxLength) characters\")\n            }\n            \n            if let pattern = validation.pattern {\n                let regex = try? NSRegularExpression(pattern: pattern)\n                let range = NSRange(location: 0, length: stringValue.utf16.count)\n                if regex?.firstMatch(in: stringValue, options: [], range: range) == nil {\n                    errors.append(\"Invalid format\")\n                }\n            }\n        }\n        \n        validationErrors[fieldId] = errors.isEmpty ? nil : errors.first\n    }\n    \n    private func updateFieldVisibility() {\n        var newVisibleFields = Set<String>()\n        \n        for field in schema.fields {\n            if let visibility = field.visibility {\n                if shouldShowField(field: field, rules: visibility) {\n                    newVisibleFields.insert(field.id)\n                }\n            } else {\n                newVisibleFields.insert(field.id)\n            }\n        }\n        \n        visibleFields = newVisibleFields\n    }\n    \n    private func shouldShowField(field: FieldDefinition, rules: VisibilityRules) -> Bool {\n        guard let conditions = rules.conditions else { return true }\n        \n        for condition in conditions {\n            let dependentValue = formState[condition.fieldId]\n            let expectedValue = condition.value\n            \n            switch condition.operator {\n            case .equals:\n                if \"\\(dependentValue ?? \"\")\" != \"\\(expectedValue)\" {\n                    return false\n                }\n            case .notEquals:\n                if \"\\(dependentValue ?? \"\")\" == \"\\(expectedValue)\" {\n                    return false\n                }\n            case .contains:\n                if let stringValue = dependentValue as? String {\n                    if !stringValue.contains(expectedValue) {\n                        return false\n                    }\n                }\n            }\n        }\n        \n        return true\n    }\n    \n    func submitForm() async throws {\n        let submissionData = FormSubmission(\n            id: UUID().uuidString,\n            timestamp: Date(),\n            formData: formState,\n            schemaVersion: schema.version\n        )\n        \n        try await networkManager.submitForm(submissionData)\n        persistenceManager.markAsSubmitted(submissionData.id)\n    }\n}\n\nclass CoreDataManager {\n    func saveOffline(_ data: FormSubmission) {\n        let context = persistentContainer.viewContext\n        let entity = FormSubmissionEntity(context: context)\n        entity.id = data.id\n        entity.timestamp = data.timestamp\n        entity.formData = try? JSONEncoder().encode(data.formData)\n        entity.isSubmitted = false\n        \n        try? context.save()\n    }\n    \n    func getPendingSubmissions() -> [FormSubmission] {\n        let context = persistentContainer.viewContext\n        let request: NSFetchRequest<FormSubmissionEntity> = FormSubmissionEntity.fetchRequest()\n        request.predicate = NSPredicate(format: \"isSubmitted == false\")\n        \n        return (try? context.fetch(request))?.compactMap { entity in\n            guard let data = entity.formData,\n                  let formData = try? JSONDecoder().decode([String: Any].self, from: data) else {\n                return nil\n            }\n            \n            return FormSubmission(\n                id: entity.id ?? \"\",\n                timestamp: entity.timestamp ?? Date(),\n                formData: formData,\n                schemaVersion: \"1.0\"\n            )\n        } ?? []\n    }\n}\n\nclass NetworkManager {\n    func submitForm(_ data: FormSubmission) async throws {\n        let url = URL(string: \"https://api.example.com/forms\")!\n        var request = URLRequest(url: url)\n        request.httpMethod = \"POST\"\n        request.setValue(\"application/json\", forHTTPHeaderField: \"Content-Type\")\n        request.setValue(data.id, forHTTPHeaderField: \"X-Idempotency-Key\")\n        \n        let jsonData = try JSONEncoder().encode(data)\n        request.httpBody = jsonData\n        \n        try await executeWithRetry(request: request)\n    }\n    \n    private func executeWithRetry(request: URLRequest, maxRetries: Int = 3) async throws {\n        var delay: TimeInterval = 1.0\n        \n        for attempt in 0...maxRetries {\n            do {\n                let (_, response) = try await URLSession.shared.data(for: request)\n                \n                if let httpResponse = response as? HTTPURLResponse {\n                    if httpResponse.statusCode == 200 || httpResponse.statusCode == 201 {\n                        return\n                    } else if httpResponse.statusCode == 409 {\n                        return\n                    } else if httpResponse.statusCode >= 500 {\n                        throw NetworkError.serverError\n                    } else {\n                        throw NetworkError.clientError(httpResponse.statusCode)\n                    }\n                }\n            } catch {\n                if attempt == maxRetries {\n                    throw error\n                }\n                \n                try await Task.sleep(nanoseconds: UInt64(delay * 1_000_000_000))\n                delay *= 2\n            }\n        }\n    }\n}\n```\n\n## Architecture Benefits\n\n- **Schema-driven**: UI adapts automatically to schema changes without code modifications\n- **Offline-first**: Core Data ensures data persistence and sync when connectivity restores\n- **Reactive validation**: Combine framework provides real-time feedback as users type\n- **Idempotent operations**: Retry logic prevents duplicate submissions during network issues\n- **Conditional rendering**: Complex field dependencies handled through declarative rules",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-16",
        "questionId": "q-18748",
        "question": "Design a beginner iOS SwiftUI feature named NearbyShops that uses CoreLocation to fetch the user’s coordinates, loads a bundled JSON list of stores, computes distance with CLLocation.distance(from:), and displays a sorted list with distance in a SwiftUI List. Include permission handling (denied/undetermined) with a fallback to cached results in Documents via Codable, plus a minimal SwiftUI sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Fetch user location with CLLocationManager, load stores from bundled JSON via Codable, compute distance with CLLocation.distance(from:), and display a sorted list with distances in SwiftUI. If permiss",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create a local-first queue in SQLite/Realm with a durable replay log for order events. Resolve conflicts using per-record version vectors (or LWW with timestamps). Background-sync batched diffs via UR",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an append-only Event log with events: AnchorCreated, AnchorMoved, AnnotationAdded, AnnotationEdited. Persist events and a light Snapshot in Core Data; on relaunch replay the last 100 events to reb",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a DiagramStore as an Actor with per-diagram queues; use CloudKit sharing for offline objects and a WebSocket stream for live edits; cache locally in CoreData and apply updates via a versione",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests CoreLocation basics, permission handling, local persistence, and SwiftUI data display with offline fallback.\n\n## Key Concepts\n- CoreLocation authorization flows\n- CLLocation math and distance calculations\n- Codable persistence to the app's Documents\n- SwiftUI List rendering and sorting\n- Offline fallback and error handling\n\n## Code Example\n```javascript\nimport CoreLocation\n\nstruct Store: Codable { let id: String; let name: String; let latitude: Double; let longitude: Double }\n\nfunc distance(from user: CLLocation, to store: Store) -> Double {\n  let loc = CLLocation(latitude: store.latitude, longitude: store.longitude)\n  return user.distance(from: loc)\n}\n```\n\n## Follow-up Questions\n- How would you test location permission flows on device and simulator?\n- How would you handle large store lists and memory constraints?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-17",
        "questionId": "q-20341",
        "question": "Design a secure, offline-first collaboration client for an iOS app named VaultNote that enables real-time co-editing on documents with end-to-end encryption. Your task: (1) implement per-document keys with CryptoKit and rotate them safely, (2) achieve low-latency sync via WebRTC data channels with a relay fallback, (3) store edits in an append-only log on-device and replay to rebuild state after relaunch, (4) manage crash-safe resume and provide thorough audit logging, (5) outline a practical test plan and a minimal SwiftUI sketch for status UI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a secure, offline-first iOS client for VaultNote. Implement per-document keys (CryptoKit, AES-GCM) with Secure Enclave-wrapped key material and periodic rotation. Drive low-latency sync via",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Per-drone UDP listener with sequence validation; per-drone ring buffers; a global monotonic clock for alignment; merge streams by timestamp; SQLite storage with telemetry and sessions, WAL; crash-safe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect an offline-first pipeline: local EventStore (Device, Reading, Session, Alert) in SQLite with append-only logs; ARKit anchors bind Alerts to device IDs and render overlays in RealityKit; run",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use CryptoKit to symmetrically encrypt notes with a key stored in the Keychain. Notes modeled as Note(id, title, body, createdAt, isLocked). Persist encrypted data to the app's documents directory; lo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes encryption key lifecycle, offline-first data integrity, and low-latency sync, which are critical in finance+mobile.\n\n## Key Concepts\n- End-to-end encryption with CryptoKit and Secure Enclave\n- Per-document keys and rotation strategy\n- Offline-first append-only log with replay\n- WebRTC data channels with relay fallback\n- Crash-safe resume and audit logging\n- SwiftUI UI considerations\n\n## Code Example\n\n```swift\n// Pseudo code: rotate document key\nimport CryptoKit\nfunc rotateKey(forDocument docID: String, oldKey: SymmetricKey) -> SymmetricKey {\n  // derive new key material\n  let newKeyMaterial = Data((0..<32).map{ _ in UInt8.random(in: 0...255) })\n  let newKey = SymmetricKey(data: newKeyMaterial)\n  // wrap with old key or wrap in Keychain; simplified\n  return newKey\n}\n```\n\n## Follow-up Questions\n- How would you test key rotation without exposing user data?\n- How do you handle offline edits when a conflicting new version exists remotely?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-18",
        "questionId": "q-5519",
        "question": "Design a beginner iOS SwiftUI app named FocusTimer that lets a user create 3 Pomodoro-style timers (25/5/15 minutes). It must be accessible (VoiceOver labels, Dynamic Type, color contrast) and fit on a single screen: a timer list, a prominent Start/Pause button, and a simple progress ring. Implement the timer with Timer.publish, persist remaining seconds and active timer to UserDefaults, debounce taps, and provide a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a modular SyncEngine with per-anchor CRDT state and a compact transform pipeline; use WebRTC data channels for near-real-time sync with a relay fallback to a REST endpoint for outages; log every state mutation t…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "An ideal answer outlines a small MVVM SwiftUI app with three presets (25, 5, 15). Timer uses Timer.publish(every: 1). The active timer updates a ProgressRing; accessibility: labels for timer, Dynamic",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a CRDT-like offline-first chat engine: immutable Message records and per-message tombstones; Edits/Reactions encoded as delta Events; Core Data with an append-only Log; vector clocks for con",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement TaskPulse with a SwiftUI List of Task items (title, dueDate, isDone). Persist tasks as JSON in the app's Documents directory using Codable, load on launch, save on changes. Add a overdue bad",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests familiarity with SwiftUI basics, timer handling, accessibility, and simple persistence in a beginner-friendly scenario.\n\n## Key Concepts\n- SwiftUI TimerPublisher and state management\n- MVVM pattern with @ObservableObject and @Published\n- UserDefaults for minimal persistence\n- Accessibility: VoiceOver labels, Dynamic Type, color contrast\n- Debouncing user input to prevent thrashing\n\n## Code Example\n```swift\nclass FocusTimerViewModel: ObservableObject {\n  @Published var remaining: Int = 0\n  @Published var isRunning: Bool = false\n  private var cancellable: AnyCancellable?\n  func start(duration: Int) {\n    remaining = duration * 60\n    isRunning = true\n    cancellable = Timer.publish(every: 1, on: .main, in: .common)\n      .autoconnect()\n      .sink { [weak self] _ in\n        guard let self = self else { return }\n        if self.remaining > 0 { self.remaining -= 1 } else { self.isRunning = false }\n      }\n  }\n}\n```\n\n## Follow-up Questions\n- How would you handle timer accuracy when the app is backgrounded?\n- How would you scale to more than 3 presets and persist per-preset state?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-19",
        "questionId": "q-8092",
        "question": "Design and implement an offline-first iOS media sync feature: users capture photos in an album on one device and expect them to appear on all devices with conflict-aware CloudKit sync. Outline the Core Data model, CloudKit integration, conflict resolution (per-record versioning), and a robust background transfer plan (URLSession background tasks or BGTaskScheduler) that supports partial uploads and offline queuing. Include a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement BookmarkSketch with a Bookmark struct containing id, title, url, note, tags, and date. Persist via Codable to a single file in Documents; read/write with FileManager and atomic write",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Core Data to model Album and Photo with id, assetURL, timestamp, isSynced, cloudRecordID, and version. Sync via CloudKit with per-record versioning; apply last-write-wins on conflicts but preserve",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a high-throughput camera pipeline: configure an AVCaptureSession with video data output, process frames through Vision OCR on dedicated queues, maintain a circular buffer to bound memory under 200MB, export se…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: Create a Card model Codable to parse; build a SwiftUI CardView with 3D flip animation; load questions from bundled JSON into a @Published array; track currentIndex in @State and persist to U",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests skills in offline-first design, distributed data sync, and iOS background processing across devices using Core Data and CloudKit. It checks conflict handling, performance, and test coverage.\n\n## Key Concepts\n- Core Data relational modeling for albums and photos\n- CloudKit integration patterns and per-record versioning\n- Conflict resolution strategies with user-visible edits preserved\n- Background transfers with URLSession/BGTaskScheduler and offline queues\n- Observability and test plans for multi-device sync\n\n## Code Example\n```swift\nstruct PhotoVersioned {\n  let id: UUID\n  let version: Int\n  let timestamp: Date\n}\n```\n\n## Follow-up Questions\n- How would you handle very large assets with partial transfers?\n- How would you measure and optimize cross-device sync latency?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-20",
        "questionId": "q-26984",
        "question": "Design a beginner iOS SwiftUI app named 'MoodNote' that lets users log daily mood entries with a short text body. Persist locally using Core Data with entities Entry (id, timestamp, mood, text). Add a feature to export all entries as a single JSON file encrypted with AES-GCM via CryptoKit, saved to the app's Documents directory; implement a one-click export button and a basic test plan. How would you implement this end-to-end?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design offline-first chat: store drafts and sent messages in CoreData with fields id (UUID), convoId (UUID), localSeq (Int64), serverId (String?), status (pending/sent/delivered), timestamp, and conte",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "```swift import Foundation import SQLite4 // MARK: - Data Models struct Tile: Codable, Identifiable { let id: String let x: Int, y: Int, z: Int let data: Data let expiresAt: Date? } struct Route: Codable, Identifiable {…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design for an offline-first PDF annotation flow: render PDFs with PDFKit, persist Annotations in Core Data with documentVersion; sync via REST using a SyncManager that handles idempotent PUTs and delt",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Define a Core Data entity `Entry` with attributes `id` (UUID), `timestamp` (Date), `mood` (Int16), and `text` (String). Display entries in a SwiftUI List using `@FetchRequest`, and provide a form for creating new mood e…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical iOS development skills, including Core Data modeling, SwiftUI data binding, secure encryption with CryptoKit, and fundamental testing practices.\n\n## Key Concepts\n- Core Data entity modeling and persistence\n- SwiftUI reactive data flow with `@FetchRequest`\n- Codable JSON serialization and file I/O\n- CryptoKit AES-GCM symmetric encryption\n- Secure storage for encryption keys (Keychain)\n- Unit testing for persistence, encoding, and encryption\n\n## Code Example\n```swift\nstruct Entry: Codable {\n    let id: UUID\n    let timestamp: Date\n    let mood: Int16\n    let text: String\n}\n\nclass EntryStore: ObservableObject {\n    @Published var entries: [Entry] = []\n\n    func exportToEncryptedJSON(key: SymmetricKey) throws -> URL {\n        let encoder = JSONEncoder()\n        encoder.dateEncodingStrategy = .iso8601\n        let data = try encoder.encode(entries)\n        let sealedBox = try AES.GCM.seal(data, using: key)\n        let encryptedData = sealedBox.combined!\n        let url = FileManager.default.urls(for: .documentDirectory, in: .userDomainMask)[0]\n            .appendingPathComponent(\"mood_export.enc\")\n        try encryptedData.write(to: url)\n        return url\n    }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-21",
        "questionId": "q-6169",
        "question": "Create an offline-first iOS chat client: messages stored in Core Data, encrypted with CryptoKit before persistence, and decrypted in memory for display. When online, sync with REST backend; ensure idempotent delivery, respect message order within a thread, handle edits/deletes with conflict resolution, and implement exponential backoff retries. Outline data model, key management, and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an offline-first form data capture module for field agents. Use Core Data for local persistence, per-record versioning, and JSON deltas for REST sync. Implement optimistic UI, batched uploads,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a lightweight Core ML model (e.g., temporal conv or small LSTM) trained on-device with heart-rate, HRV, and motion features. Apply differential privacy-free? No, use secure aggregation to share on",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Store Message as encrypted blob in Core Data; use CryptoKit AES.GCM with a per-user key in the Keychain; encrypt on send, decrypt on fetch. Local queue for outgoing messages; on network, post to REST",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Sketch: per-note AES-GCM encryption with a per-note envelope key derived from a user password via HKDF; keys stored in Keychain. Persist encrypted notes in SQLite (GRDB); offline edits queued in a dur",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluates practical offline-first sync, encryption, and data integrity under flaky networks.\n\n## Key Concepts\n- Core Data persistence of encrypted blobs\n- CryptoKit AES-GCM key management in Keychain\n- Idempotent REST sync with delivery markers\n- Conflict resolution with per-message versioning\n- Robust test strategies for offline/online transitions\n\n## Code Example\n```swift\nimport CryptoKit\nlet key = SymmetricKey(size: .bits256)\nlet data = Data(\"hello\".utf8)\nlet sealed = try AES.GCM.seal(data, using: key)\n```\n\n## Follow-up Questions\n- How would you rotate keys with minimal user impact?\n- How to test race conditions in concurrent UI sends?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-22",
        "questionId": "q-23439",
        "question": "Context: iOS app in manufacturing. Task: design a real-time on-device defect-detection pipeline that ingests camera frames, uses a custom Metal kernel for pre-processing, runs a Vision/CoreML model, maintains a rolling 1-second window of anomaly scores, and overlays a heatmap in SwiftUI. Ensure energy-aware frame throttling, crash-safe resume with deterministic replay, and a test plan with CVPixelBuffer unit tests and latency targets. Provide a minimal SwiftUI view sketch showing overlay?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an on-device pipeline: feed camera frames through a Metal preprocessor, run a Vision/CoreML defect model, maintain a rolling 1s anomaly window, and overlay a heatmap in SwiftUI. Include energy-",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I'd build HabitKit using Core Data for persistent storage. The data model would include two entities: Habit(id: UUID, title: String, colorHex: String, notes: String?, photo: Data?) and Completion(date: Date, habit: Habi…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: implement a Swift-based on-device telemetry stack with Swift Concurrency, Core Data, and an append-only log. Track per-tenant epsilon budgets, inject Laplace noise into counts before storage",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I'd implement a bounded in-memory ring per device as the intake buffer, a streaming layer using AsyncSequence and Combine to publish rolling 1s/5s aggregates, and a SQLite WAL store with a replay log",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests building a real-time on-device vision pipeline, integrating Metal, Vision, and CoreML, while ensuring power efficiency and crash-safe resume for production reliability.\n\n## Key Concepts\n\n- On-device low-latency ML integration\n- Metal-based pre-processing kernel design\n- Rolling window anomaly scoring\n- Energy-aware frame throttling\n- Deterministic replay for crash recovery\n\n## Code Example\n\n```swift\nclass FrameBuffer {\n  var buffer: [CVPixelBuffer] = []\n  let maxSize = 60 // 1s at 60fps\n  func push(_ p: CVPixelBuffer) {\n    buffer.append(p)\n    if buffer.count > maxSize { buffer.removeFirst() }\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you test latency and determinism across device types?\n- How would you adapt when lighting changes affect pre-processing?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-23",
        "questionId": "q-26340",
        "question": "In an iOS note-collaboration app used by field engineers at Apple/Stripe, design a CRDT-powered offline-first collaboration layer that enables concurrent edits to notes across devices, stores locally with Core Data, and syncs via CloudKit. Include data models (Note, Fragment, Collaborator), a crash-safe resume strategy (append-only log with deterministic replay), and a minimal SwiftUI multi-user note view sketch showing live cursors and status indicators? Outline a practical test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a non-destructive FilterGraph using Swift actors: each filter is a node with input/output CIImage; edits propagate asynchronously via async/await; base image plus ordered passes render the f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "SQLite-backed data model using a Recipe table with columns (id, title, ingredients, steps) paired with an FTS5 virtual table for full-text search on title, ingredients, and steps—keeping all data stored locally for offl…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a CRDT-based offline-first collaboration layer: each Note contains ordered Fragment objects and uses an OR-Set style tombstone mechanism for deletions. Store data locally with Core Data and sync via CloudKit usin…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement WordEntry: id, word, definition, partOfSpeech, imageData?; persist [WordEntry] via Codable to a JSON file in Documents; load examples from a bundled lookup dictionary. UI: SwiftUI List with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses knowledge of CRDTs, offline-first synchronization patterns, and iOS platform specifics (Core Data, CloudKit) under realistic engineering constraints.\n\n## Key Concepts\n- **CRDTs**: OR-Set structures and tombstones for handling concurrent edits without conflict resolution\n- **CloudKit Sync**: Per-note version vectors to track changes across distributed clients\n- **Core Data Schema**: Note, Fragment, and Collaborator entities with appropriate relationships\n- **Crash-Safe Resume**: Append-only operation log combined with deterministic replay to recover state reliably\n- **SwiftUI View**: Multi-user interface supporting live cursors and presence indicators\n\n## Code Example\n```swift\n// CRDT fragment merge (high level)\nstruct Fragment: Identifiable {\n    let id: UUID\n    var content: String\n    var tombstoned: Bool\n}\n\n// Merge rule: tombstoned copies take precedence\nfunc merge(fragments: [Fragment]) -> [Fragment] {\n    return fragments.reduce(into: [:]) { merged, fragment in\n        if fragment.tombstoned {\n            merged[fragment.id] = fragment\n        } else if merged[fragment.id] == nil {\n            merged[fragment.id] = fragment\n        }\n    }.values.sorted { $0.id < $1.id }\n}\n```\n\n## Test Plan\n1. **Unit Tests**: Verify CRDT merge logic handles concurrent inserts, deletes, and conflicts correctly\n2. **Integration Tests**: Test Core Data persistence and CloudKit sync under varying network conditions\n3. **Crash Recovery Tests**: Simulate app termination mid-sync; verify deterministic replay restores consistent state\n4. **UI Tests**: Validate live cursor rendering and presence indicator updates across multiple simulated users\n5. **Edge Case Tests**: Clock skew, clock drift, offline-to-online transitions, and rapid concurrent edits",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-24",
        "questionId": "q-11782",
        "question": "Design a beginner iOS SwiftUI app named ExpenseTrace that lets users log an expense with amount, category, date, and an optional receipt image. Persist data locally using Codable to the Documents directory, implement a weekly summary view, and ensure data persists across launches. Include a minimal test plan accessibility considerations, and describe how you'd handle data migrations if the model changes between versions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement BookmarkSketch with a Bookmark struct containing id, title, url, note, tags, and date. Persist via Codable to a single file in Documents; read/write with FileManager and atomic write",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a CRDT-like log (vector clocks) to merge HealthKit samples and BLE scale readings stored in Core Data. Schedule BGProcessingTask for Wi‑Fi uploads; batch diffs to cloud with on-device differential",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a prune module that keeps 60 days of minute-price history per symbol in SQLite, archives tick events in a separate table, and offers user-export. Ensure crash-safe resume by writing durable mar",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a Codable Expense struct with id, amount, category, date, receiptURL; persist via JSON to Documents with FileManager, load on startup; build a simple MVVM with @Published expenses and a weeklyT",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical iOS skills in data modeling, local persistence, and basic MVVM, plus test planning and accessibility. It also touches on data migrations that beginner apps should eventually handle.\n\n## Key Concepts\n\n- Codable persistence to disk with FileManager and JSON\n- Local data modeling and a lightweight MVVM pattern\n- Handling dates, time zones, and weekly summaries\n- Testing strategy for encoding/decoding and persistence\n- Accessibility: Dynamic Type and VoiceOver\n\n## Code Example\n\n```swift\nstruct Expense: Codable { var id: UUID; var amount: Double; var category: String; var date: Date; var receiptURL: URL? }\n```\n\n## Follow-up Questions\n\n- How would you validate inputs and handle currency formatting?\n- How would you implement a migration if fields change?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-11T12:42:08.818Z",
    "version": 1
  }
]