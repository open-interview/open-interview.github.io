[
  {
    "id": "test-ios",
    "channelId": "ios",
    "channelName": "IOS",
    "title": "IOS Knowledge Test",
    "description": "Test your understanding of IOS with 25 questions.",
    "questions": [
      {
        "id": "ios-q-0",
        "questionId": "q-21653",
        "question": "In an iOS AR-guided maintenance app used by field technicians, design an offline-first data pipeline that ingests high-frequency device readings, overlays alerts via ARKit, debounces UI to 25–30fps, runs a tiny on-device CoreML anomaly detector, and ensures crash-safe resume with deterministic replay for audit. Specify data models (Device, Reading, Session, Alert), persistence (SQLite/Realm), and a minimal SwiftUI AR view plus a robust test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement BookmarkSketch with a Bookmark struct containing id, title, url, note, tags, and date. Persist via Codable to a single file in Documents; read/write with FileManager and atomic write",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Core Data Note entity with id, text, updatedAt, isSynced. Add a disk-persisted queue for outgoing changes, replaying it on launch. Build a SyncManager around URLSession: fetch /notes to up",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect an offline-first pipeline: local EventStore (Device, Reading, Session, Alert) in SQLite with append-only logs; ARKit anchors bind Alerts to device IDs and render overlays in RealityKit; run",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "I would implement a Codable Note type and a NotesStore class that loads/saves an array of notes to Documents/notes.json using an atomic write (temp file + swap). UI uses SwiftUI with @Published notes",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes end-to-end iOS architecture skills: AR overlays, offline-first persistence, ML integration, crash resilience, and auditability, all realistic at Lyft/Snowflake scale.\n\n## Key Concepts\n- ARKit RealityKit integration with anchors\n- Append-only EventStore for replay\n- CoreML tiny model and debouncing\n- Crash-safe resume and deterministic replay\n- Secure sync and conflict handling\n\n## Code Example\n\n```swift\nstruct Device { let id: String; let name: String }\nstruct Reading { let deviceId: String; let ts: Date; let value: Double }\n```\n\n## Follow-up Questions\n- How to test AR alignment offline and after relaunch?\n- How would you scale the LocalStore for thousands of devices?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-1",
        "questionId": "q-28604",
        "question": "In a beginner iOS app named 'PhotoTagger', design a SwiftUI gallery that loads a local JSON catalog (name, filename, tags) from the bundle, shows images in a LazyVGrid, supports filtering by tag via a segmented control, and persists per-image user-added tags in UserDefaults. Use MVVM with a ViewModel exposing @Published properties, and ensure performance on older devices with lazy loading and image caching. Include a minimal SwiftUI view sketch and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a on-device telemetry pipeline with per-session buffering in CoreData, apply differential privacy (epsilon 1–2) to LocationEvent and SensorEvent before encryption, then stream via WebSocket to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement PhotoTagger using MVVM architecture: parse the local catalog.json file into ImageItem models, cache loaded UIImage instances in NSCache for performance, drive the grid with @Published properties in the ViewMod…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use the Secure Enclave to generate a P-256 private key, store it in the Keychain with biometric protection (kSecAttrAccessibleWhenUnlockedThisDeviceOnly and SecAccessControl with biometrics). Sign mes",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an append-only Event log with events: AnchorCreated, AnchorMoved, AnnotationAdded, AnnotationEdited. Persist events and a light Snapshot in Core Data; on relaunch replay the last 100 events to reb",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis exercise validates understanding of MVVM architecture, SwiftUI reactive data flow, and practical local persistence in a beginner-appropriate context. It also tests knowledge of efficient image loading patterns, caching strategies, and the ability to structure a straightforward test plan.\n\n## Key Concepts\n\n- **MVVM architecture** with @Published properties and Combine basics\n- **SwiftUI LazyVGrid** layout, segmented controls, and two-way data binding\n- **Local JSON parsing** using the Codable protocol\n- **NSCache** for memory-based image caching\n- **UserDefaults** for lightweight persistence of user-added tags",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-2",
        "questionId": "q-12638",
        "question": "In an iOS app that captures and analyzes live athlete training data, design an on-device processing pipeline that ingests high-frequency IMU data and camera frames, runs a quantized CoreML model for biomechanics analysis, fuses outputs with GPS, debounces UI updates to 30fps, stores compact results locally, preserves per-run order across relaunch, and never uploads raw data. Outline data models, threading, caching, and a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "In the pipeline, sample IMU at 100 Hz and camera at 30 Hz, feed to a quantized CoreML model for biomechanics, fuse with GPS via a lightweight Kalman filter, and emit KPI bundles at 30fps. Persist comp",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use SQLite for messages with indices chatId/timestamp; table fields: id, chatId, senderId, body, timestamp, editedAt, deleted. Track receipts and per-chat rotation keys. Sync via push/pull; merges wit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "**Data Model:** ```swift struct CheckEvent: Codable, Identifiable { let id: UUID let inspectorId: String let timestamp: Date let latitude: Double let longitude: Double let photosHash: String // SHA-256 hash of photo ass…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a 3-5 item speculative prefetch queue backed by SQLite for durable cache and a small in-memory map for fast checks. Use URLSession background fetch to retrieve articles, deduplicate by articleI",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes ability to design an end-to-end on-device processing pipeline with multiple data sources (IMU, camera, GPS), real-time processing constraints, and strict data privacy (no raw uploads). It also tests persistence and crash-resilience across app relaunch, plus testability.\n\n## Key Concepts\n\n- On-device CoreML with quantization for low-latency inference\n- Sensor fusion (Kalman) and 30fps UI debouncing\n- Local persistence (SQLite) with per-run identifiers and write-ahead logs\n- Data minimization and deterministic tests for reproducibility\n\n## Code Example\n\n```swift\nstruct RunSample {\n  let id: UUID\n  let timestamp: TimeInterval\n  let imu: [Double]\n  let imageFeature: [Double]\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt the pipeline for varying device capabilities and energy budgets?\n- How would you validate latency and accuracy across iPhone generations?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-3",
        "questionId": "q-11088",
        "question": "In an iOS SwiftUI app used by field technicians to annotate a map of a construction site, design an offline-first collaboration module. Users can drop pins with metadata, draw polylines, and attach photos. Implement local storage with CoreData, store media in the file system, and synchronize with a REST backend using delta patches and per-record versioning. Explain conflict resolution, UI flow, and a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Securely store access and refresh tokens in Keychain; implement a URLSession wrapper that intercepts 401 responses, triggers a single token refresh guarded by a synchronization mechanism, and replays queued requests wit…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use CoreData to model LocationPin, Polyline, and PhotoAttachment entities with a version field for conflict detection. Store media files in the Documents directory while maintaining metadata references in CoreData. Impl…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Persist notes using GRDB with a DatabaseQueue. Model NoteRecord(id, title, body, date). Expose a NotesViewModel with @Published notes, searchQuery, and dateFilter. Use a SwiftUI List with .searchable;",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Codable Note model with id, title, content, unlockDate, opened. Save as a JSON array in Documents; load on launch; register for notifications; schedule a notification via UNCalendarNotificationT",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses on-device data modeling, offline-first synchronization, and conflict resolution under concurrent edits. Requires concrete trade-offs between CoreData vs CloudKit, delta-based sync vs full state sync, and UX considerations for conflict scenarios.\n\n## Key Concepts\n\n- CoreData schema design for pins, polylines, and attachments with optimistic UI patterns\n- Delta-based synchronization with per-record versioning and tombstone records for deletions\n- Local media storage management with reference integrity\n- Conflict resolution strategies and user experience cues",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-4",
        "questionId": "q-26095",
        "question": "In an iOS real-time video conferencing app, design an on-device translation and speaker diarization pipeline that runs offline to preserve privacy while delivering low-latency captions? Requirements: streaming mic audio via Combine; a tiny CoreML translator with server fallback; on-device diarization; crash-safe resume with an append-only log and checkpoints; SQLite persistence; a SwiftUI status view; and a concrete test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a WebSocket (URLSessionWebSocketTask) with automatic reconnect using exponential backoff, a background worker queue for message handling, and Idempotent application of presence events via sequence",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a modular SyncEngine with per-anchor CRDT state and a compact transform pipeline; use WebRTC data channels for near-real-time sync with a relay fallback to a REST endpoint for outages; log every state mutation t…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I’d implement a CRDT-based op log (Event: id, actor, op, ts) with per-document state and causal vectors. Use WebRTC data channels for sub-50ms updates; fallback to a relay server with reliable channel",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a modular on-device translation + diarization pipeline for an iOS video call app. Stream mic audio with AVAudioEngine + Combine; run a tiny CoreML translator (quantized) with server fallback; i",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end on-device ML for real-time tasks, privacy, and crash resilience in a video-call context.\n\n## Key Concepts\n- On-device ML with CoreML\n- Audio streaming via AVAudioEngine + Combine\n- Diarization\n- Crash-safe persistence (append-only logs)\n- SQLite persistence\n- Performance testing (latency, memory) and privacy guarantees\n\n## Code Example\n```swift\n// Pseudo-setup illustrating components\nimport AVFoundation\nimport Combine\n```\n\n## Follow-up Questions\n- What latency targets would you set for translation?\n- How would you validate diarization accuracy in live calls?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-5",
        "questionId": "q-19864",
        "question": "Design a beginner iOS SwiftUI app named TrailNote that lets hikers log trail entries: trail name, distance, rating, date, optional photo, and GPS coordinates captured via CoreLocation; persist locally with Codable to the app's Documents directory; implement a filter by date and rating and a simple map view showing logged trails; include a minimal SwiftUI view sketch and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a TrailEntry Codable struct with id, name, distance, rating, date, photoPath, latitude, longitude. Save/load as JSON in the app's Documents directory via FileManager. Acquire coordinates with Core",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "End-to-end encrypted, offline-first document collaboration. Per-document keys, ephemeral DH exchange, and signatures via CryptoKit; store keys in Secure Enclave. Data model: Document, EditOp CRDT, Acc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Securely store access and refresh tokens in Keychain; implement a URLSession wrapper that intercepts 401 responses, triggers a single token refresh guarded by a synchronization mechanism, and replays queued requests wit…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement offline-first notes with CoreData storage and a sequence CRDT for text edits, each operation carrying a vector timestamp. Sync via CloudKit records; apply deterministic merges based on vecto",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe prompt tests practical iOS data modeling, offline persistence, and UI composition for a real world hobby app.\n\n## Key Concepts\n- SwiftUI data flow and @ObservedObject/@State\n- Codable persistence to the Documents directory\n- CoreLocation for coordinates and MapKit/SwiftUI Map integration\n- Simple yet testable filtering by date and rating\n- Basic test strategy: encode/decode, persistence, map data\n\n## Code Example\n```swift\nstruct TrailEntry: Codable {\n  let id: UUID\n  let name: String\n  let distance: Double\n  let rating: Int\n  let date: Date\n  let photoPath: String?\n  let latitude: Double\n  let longitude: Double\n}\n```\n\n## Follow-up Questions\n- How would you handle photo storage and memory optimization?\n- How would you extend to sync with iCloud or a backend later?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-6",
        "questionId": "q-257",
        "question": "What is optional chaining in Swift and how does it compare to force unwrapping, optional binding, and optional chaining with method calls when accessing nested optional properties?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a Core Data-backed Change Journal to track inserts/edits/deletes for Message(id, content, author, createdAt, updatedAt, localVersion). Sync loop pulls /messages?since=serverMax and pushes loc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a directed graph stored locally in SQLite to represent the map. Run A* with a lightweight heuristic, incrementally updating edge weights as Combine streams traffic events. When weights are invalidated, trigger…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a beginner iOS SwiftUI app called TagNote: create notes with title, body, and an optional tag. Persist via Core Data locally; implement tag-based filtering and a debounced search. Use MVVM, fetc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Optional chaining (?.) safely unwraps optionals, returning nil if any link fails instead of throwing runtime errors. Unlike force unwrapping (!), it prevents crashes. It's more concise than if-let/guard-let for single a…",
            "isCorrect": true
          }
        ],
        "explanation": "## Core Concept\nOptional chaining (?.) provides a safe way to access properties, methods, and subscripts of optional values, returning nil if any part of the chain is nil.\n\n## Key Differences\n- **Force unwrapping (!)**: Crashes if nil\n- **Optional binding**: Requires separate if-let/guard-let blocks\n- **Optional chaining**: Single-line safe access\n\n## Code Examples\n```swift\n// Nested optionals\nlet streetCount = person?.address?.street?.count\n\n// Method calls with optional chaining\nlet result = optionalObject?.method()?.property\n\n// Subscript access\nlet value = dictionary?[key]?.nestedValue\n```\n\n## Performance & Best Practices\n- Slight overhead vs force unwrapping due to nil checks\n- Use when you need single access to deeply nested optionals\n- Prefer optional binding for multiple operations on the same value\n- Avoid excessive chaining that reduces code readability\n\n## Common Pitfalls\n- Forgetting that the result is always optional\n- Mixing with force unwrapping in the same chain\n- Not handling the nil case appropriately",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-7",
        "questionId": "q-22509",
        "question": "In an iOS field-survey app used offline in remote areas, design an offline-first data pipeline for multi-modal responses (text, GPS, photos) that must merge across devices when connectivity returns. Use a CRDT (OR-Set) for responses, an append-only log, and deterministic conflict resolution. Specify data models (Survey, Response, Media), persistence (Realm/SQLite), a crash-safe resume with replay, and a minimal SwiftUI view sketch showing sync status?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a CRDT-based offline-first sync for a collaborative Checklist. Use an OR-Set per item, per-device tombstones, and signed ops with CryptoKit. Persist locally in SQLite with encryption at rest; u",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would start by creating a Core Data model with a Note entity (title: String, body: String, createdAt: Date), then build a SwiftUI List to display notes with a detail view for viewing and editing. For localization, I w…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "The Stroke model includes: id (UUID), color (RGBA), points: [CGPoint], timestamp, author, and a per-stroke version vector. The CanvasActor maintains application state and serializes mutations; on each stroke change, the…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a per-survey OR-Set CRDT to handle mergeable responses (text, GPS, photo references). Store media as files with references managed in Realm or SQLite. Implement an append-only event log with replay on app launch…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests advanced offline-first architecture, CRDT-based conflict resolution, and iOS persistence strategies that scale across devices operating in disconnected environments.\n\n## Key Concepts\n- **CRDT OR-Set**: Enables automatic merging of concurrent offline edits without requiring central coordination\n- **Append-only event log**: Provides crash-safe durability and enables idempotent replay on startup\n- **Multi-modal data modeling**: Survey (container), Response (user input), Media (file references with metadata)\n- **Deterministic conflict resolution**: Timestamp-based last-write-wins with user-facing conflict flags for manual resolution when needed\n- **Durable persistence**: Realm or SQLite for structured data, with file-backed storage for media\n- **Crash-safe resume**: Replay log on launch to reconstruct state and resume interrupted operations\n\n## Data Models\n```swift\nstruct Survey {\n    let id: UUID\n    let title: String\n    let questions: [Question]\n    var version: Int\n}\n\nstruct Response {\n    let id: UUID\n    let surveyId: UUID\n    let questionId: UUID\n    var text: String?\n    var location: Coordinate?\n    var mediaRefs: [Media]\n    var orSet: ORSet<ResponseElement>\n    var updatedAt: Date\n}\n\nstruct Media {\n    let id: UUID\n    var localPath: String\n    var remotePath: String?\n    var mimeType: String\n    var syncStatus: SyncStatus\n}\n```\n\n## OR-Set Implementation Sketch\n```swift\nstruct ORSet<T: Hashable> {\n    private var adds: [T: VectorClock] = [:]\n    private var removes: [T: VectorClock] = [:]\n    \n    mutating func add(_ element: T, clock: VectorClock) {\n        adds[element] = clock\n    }\n    \n    mutating func remove(_ element: T, clock: VectorClock) {\n        removes[element] = clock\n    }\n    \n    func lookup(_ element: T) -> Bool {\n        guard let addClock = adds[element] else { return false }\n        guard let removeClock = removes[element] else { return true }\n        return addClock > removeClock\n    }\n    \n    func merge(with other: ORSet<T>) -> ORSet<T> {\n        var result = self\n        for (elem, clock) in other.adds {\n            result.adds[elem] = max(result.adds[elem] ?? [:], clock)\n        }\n        for (elem, clock) in other.removes {\n            result.removes[elem] = max(result.removes[elem] ?? [:], clock)\n        }\n        return result\n    }\n}\n```\n\n## Sync Status View (SwiftUI)\n```swift\nstruct SyncStatusView: View {\n    @ObservedObject var syncManager: SyncManager\n    \n    var body: some View {\n        HStack {\n            Image(systemName: syncManager.isOnline ? \"wifi\" : \"wifi.slash\")\n                .foregroundColor(syncManager.isOnline ? .green : .orange)\n            \n            Text(syncManager.isOnline ? \"Online\" : \"Offline\")\n            \n            Spacer()\n            \n            if syncManager.pendingChanges > 0 {\n                ProgressView()\n                    .scaleEffect(0.8)\n                Text(\"\\(syncManager.pendingChanges) pending\")\n                    .font(.caption)\n            } else {\n                Image(systemName: \"checkmark.circle.fill\")\n                    .foregroundColor(.green)\n            }\n        }\n        .padding()\n    }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-8",
        "questionId": "q-25043",
        "question": "In an iOS enterprise telemetry app used by IBM/Scale AI, design a privacy-preserving, offline-first telemetry collector that isolates data per tenant, aggregates locally before upload, and signs data before transmission. Requirements: 1) per-tenant keys stored in the Secure Enclave via CryptoKit, 2) a tiny DSL to express metrics like SUM(events) WHERE tenant='t1', 3) Combine-based ingestion with a durable on-disk queue, 4) crash-safe resume after relaunch with a deterministic replay, 5) a SwiftUI dashboard showing per-tenant aggregates and upload status, 6) a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use cell reuse, pre-calculate heights, implement heightForRowAt caching, and optimize Auto Layout constraints with manual layout when needed.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant CryptoKit keys stored in the Secure Enclave to sign per-tenant aggregates; a durable on-disk queue (SQLite) fed by a Combine ingestion pipeline; a tiny DSL for metrics like SUM(events) WHERE tenant='t1';…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "GestureLab stores Gesture with id, name, startTime, and an array of GestureEvent {type: pan|pinch|rotate, x, y, delta, timestamp}. Persist via Codable to Documents, load on launch, and cache in memory",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an offline-first analytics pipeline: collect events, persist to a durable on-disk queue (CoreData/SQLite), batch them and upload via a URLSession background task, and ensure idempotent processi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design a privacy-focused, offline-first telemetry system with multi-tenant isolation, cryptographic signing, and robust persistence. Demonstrates proficiency in Swift, Combine, CryptoKit, and UI integration.\n\n## Key Concepts\n- **Privacy by design**: Per-tenant keys in Secure Enclave for cryptographic signing.\n- **Offline-first**: Durable on-disk queue with crash-recovery and deterministic replay.\n- **Data modeling**: Tenant, Event, Aggregate, Batch entities.\n- **DSL**: Tiny expression language for on-device metrics computation.\n- **Architecture**: Combine pipeline feeding a local processor and background uploader.\n\n## Code Example\n```swift\n// Metrics DSL\nfunc metric(_ expr: String) -> Metric {\n    let tokens = expr.split(separator: \" \")\n    // Parse: SUM(events) WHERE tenant='t1'\n    guard tokens.count >= 4, tokens[0] == \"SUM\",\n          tokens[2] == \"WHERE\", tokens[3].starts(with: \"tenant=\") else {\n        throw DSLError.invalidExpression\n    }\n    return Metric(aggregation: .sum, field: String(tokens[1]), \n                 filter: parseFilter(tokens[3...]))\n}\n\n// Secure Enclave key management\nstruct TenantKeyManager {\n    func generateKey(for tenantId: String) throws -> SecKey {\n        var error: Unmanaged<CFError>?\n        let access = SecAccessControlCreateWithFlags(\n            kCFAllocatorDefault,\n            kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly,\n            .privateKeyUsage, &error\n        )!\n        let attributes: [String: Any] = [\n            kSecAttrKeyType as String: kSecAttrKeyTypeECSECPrimeRandom,\n            kSecAttrKeySizeInBits as String: 256,\n            kSecAttrTokenID as String: kSecAttrTokenIDSecureEnclave,\n            kSecPrivateKeyAttrs as String: [\n                kSecAttrIsPermanent as String: true,\n                kSecAttrApplicationTag as String: tenantId.data(using: .utf8)!,\n                kSecAttrAccessControl as String: access\n            ]\n        ]\n        return SecKeyCreateRandomKey(attributes as CFDictionary, &error)!\n    }\n}\n\n// Combine ingestion pipeline\nclass TelemetryCollector {\n    private let queue = PassthroughSubject<TelemetryEvent, Never>()\n    private var subscriptions = Set<AnyCancellable>()\n    \n    init() {\n        queue\n            .collect(.byTime(DispatchQueue.global(), .seconds(1)))\n            .sink { events in self.persist(events) }\n            .store(in: &subscriptions)\n    }\n    \n    func ingest(_ event: TelemetryEvent) {\n        queue.send(event)\n    }\n    \n    private func persist(_ events: [TelemetryEvent]) {\n        // Write to SQLite with transaction\n    }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-9",
        "questionId": "q-21350",
        "question": "In an iOS logistics app used for last‑mile deliveries, design a privacy‑preserving on‑device route prefetch system that fetches routes from a central server but never stores exact stop locations in plaintext. Provide data models, on‑device encryption with CryptoKit, a persistence strategy (Core Data or SQLite), a background fetch plan with BGTaskScheduler, and a test plan. Include a minimal SwiftUI dashboard showing fetch status and privacy indicators?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use CryptoKit for on-device encryption with a root key stored securely in the Keychain. Derive per-record encryption keys via HKDF for per-stop protection. Store encrypted route data in Core Data or SQLite using a consi…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I'd implement a bounded in-memory ring per device as the intake buffer, a streaming layer using AsyncSequence and Combine to publish rolling 1s/5s aggregates, and a SQLite WAL store with a replay log",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I'd build HabitKit using Core Data for persistent storage. The data model would include two entities: Habit(id: UUID, title: String, colorHex: String, notes: String?, photo: Data?) and Completion(date: Date, habit: Habi…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed approach: stream line-delimited JSON via URLSession, parse each line, and apply updates on a private Core Data queue. Use a per-record version for conflict resolution and optimistic UI update",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses the ability to design on-device privacy solutions, implement encryption mechanisms, handle offline data, and manage background tasks in iOS. It evaluates practical data modeling, security implementation, and test planning skills.\n\n## Key Concepts\n\n- **CryptoKit encryption** with Keychain storage for root keys\n- **Core Data or SQLite persistence** with encrypted data storage\n- **BGTaskScheduler** for reliable background route prefetching\n- **HKDF-based key derivation** for per-record encryption keys\n- **Privacy indicators** to provide user visibility into data protection measures\n\n## Architecture Overview\n\nThe system encrypts all route and stop data on-device before storage, ensuring that exact location data never exists in plaintext on disk. The Keychain stores the master encryption key, while HKDF derives unique per-record keys, providing defense-in-depth if the master key were ever compromised.",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-10",
        "questionId": "q-22920",
        "question": "Context: iOS enterprise payments app used by Stripe and Salesforce customers. Task: design an offline-first payment draft system with an on-device state machine and crash-safe resume, enabling creation/editing/submission without network and syncing when online. Requirements: 1) a tiny DSL for state transitions (e.g., 'DRAFT -> AWAITING_REVIEW if amount > 1000'), 2) a Combine-based event stream for draft mutations, 3) CryptoKit-based end-to-end protection of drafts before syncing, 4) an append-only replay log to rebuild state after a crash, 5) a SwiftUI dashboard showing current drafts and their sync status, 6) a practical test plan focusing on correctness, race conditions, and security?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Core Data Note entity with id, text, updatedAt, isSynced. Add a disk-persisted queue for outgoing changes, replaying it on launch. Build a SyncManager around URLSession: fetch /notes to up",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an offline-first draft store using a Swift actor per draft, with a tiny DSL parsed into a deterministic state machine (DRAFT, AWAITING_REVIEW, APPROVED, REJECTED, SYNCED). Use a Combine pipe",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a CRDT-based on-device whiteboard document. Each client issues unique op IDs (peerid-seq) for add/modify/delete of drawing objects; store ops in an append-only log; use tombstones for deletions",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-event vector clock and a CRDT-like merge. Event: id, title, start, end, attendees; each device tracks a vector clock. Edits enqueue as deltas in a local store (Core Data). On reconnect, merg",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world enterprise payment apps demand correctness when network is flaky, strong security, and transparent audit trails. This task probes state-machine design, tiny DSL interpretation, secure on-device crypto, and crash-resilient recovery, all within iOS patterns.\n\n## Key Concepts\n- Offline-first state machine with crash-safe replay\n- Tiny DSL for transitions\n- Combine-based event streams\n- CryptoKit signing/encryption before sync\n- Append-only replay log for rebuilds\n- SwiftUI dashboard for live status\n\n## Code Example\n```swift\nenum DraftState { case draft, awaitingReview, approved, rejected, synced }\nstruct Transition { let from: DraftState; let to: DraftState; let condition: String? }\nclass DraftManager {\n  // Actor/serial isolation to ensure determinism\n}\n```\n\n## Follow-up Questions\n- How would you verify replay correctness after a crash?\n- How would you handle conflicting edits across devices during sync?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-11",
        "questionId": "q-6300",
        "question": "Design and implement a real-time stock watchlist screen that consumes a simulated WebSocket price feed, renders a grid with per-item sparkline charts, persists last prices locally (CoreData) and recovers on restart, and uses SwiftUI with Async/Await and a dedicated Actor to serialize updates. Describe data flow, error handling, and a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use URLSessionWebSocketTask to connect to a mock feed delivering symbol, price, and timestamp. Implement a PriceFeedActor that parses updates and updates a StockStore (@MainActor) which writes to Core",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use CoreData to model LocationPin, Polyline, and PhotoAttachment entities with a version field for conflict detection. Store media files in the Documents directory while maintaining metadata references in CoreData. Impl…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a fixed-rate 120 Hz control loop with a primary Wi‑Fi transport and BLE fallback. Use a ring buffer for CommandFrame/TelemetryFrame, track RTT/jitter/loss, and halt motion if latency exceeds th",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Securely store access and refresh tokens in Keychain; implement a URLSession wrapper that intercepts 401 responses, triggers a single token refresh guarded by a synchronization mechanism, and replays queued requests wit…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses real-time data streaming, concurrency with Swift actors, persistent state recovery, and UI performance under frequent updates—common in finance apps.\n\n## Key Concepts\n\n- WebSocket streaming with URLSessionWebSocketTask\n- Swift concurrency: actors and Async/Await\n- Local persistence: CoreData with recovery on restart\n- SwiftUI: grid layouts and sparkline visualizations\n- Testing: WS mocks, latency/ordering, persistence checks\n\n## Code Example\n\n```swift\nactor PriceFeedActor {\n  func receive(_ update: PriceUpdate) {\n    // parse and forward to StockStore\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you handle reconnection backoff and out-of-order messages?\n- What strategies ensure deterministic UI rendering during burst updates?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-12",
        "questionId": "q-12156",
        "question": "In an iOS whiteboard collaboration app, design a CRDT-based on-device document model to support offline edits from multiple devices, seamless merge on reconnect, presence, and tombstone-based deletions; outline data structures, merge rules, sync protocol, and a minimal SwiftUI/Combine sketch using Core Data or SwiftData?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Deduplicate by (symbol, seq); persist last acknowledged seq per symbol. Use WAL SQLite (GRDB) to store ticks and 1s/1m aggregates. Implement deterministic replay on boot by replaying from the last che",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a CRDT-based on-device whiteboard document. Each client issues unique op IDs (peerid-seq) for add/modify/delete of drawing objects; store ops in an append-only log; use tombstones for deletions",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a small MVVM SwiftUI gallery. Load image metadata from a bundled JSON into a model, then for each item check a disk cache dir for a local file; if missing, fetch with URLSession, write to a temp f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-device ingest actors serialize events, deduplicate by (deviceID, eventID), and feed a central router that writes to a WAL SQLite store (GRDB). Maintain 1s and 1min aggregates (speed, distance, sto",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates understanding of CRDTs, offline-first collaboration, and practical iOS persistence with SwiftUI/Combine. It requires concrete data modeling and a deterministic merge strategy.\n\n## Key Concepts\n- CRDT/OR-Set style operation logs\n- Append-only log with per-op IDs, tombstones for deletes\n- Deterministic merge by id, eventual consistency\n- Crash-safe persistence and offline-first UX\n\n## Code Example\n```swift\nstruct Op: Identifiable, Codable {\n  let id: String // peerid-seq\n  let type: String\n  let payload: Data\n  let timestamp: Date\n}\n```\n\n## Follow-up Questions\n- How would you test convergence under concurrent edits and network partitions?\n- How would you expose presence and per-peer cursors for live cursors on the canvas?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-13",
        "questionId": "q-28117",
        "question": "Context: iOS AR collaboration app for maintenance teams. Task: design a client-side CRDT-based editor enabling concurrent AR annotations and notes across devices via CloudKit. Requirements: 1) data model: Document, Annotation, Operation; 2) a CRDT type (e.g., Sequence CRDT) with tombstones for offline edits; 3) crash-safe resume with deterministic replay; 4) offline-first, batched, idempotent sync; 5) local persistence (Realm/CoreData); 6) minimal SwiftUI AR view sketch showing annotations and sync status?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed solution: local SQLite with AuditRecord (id UUID, version Int, timestamp Date, geo String, note String, photoPath String, uploaded Bool) and PhotoAsset (path String, checksum String) tables. DeltaSet of changed…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: implement a Sequence CRDT for AR annotations, modeling edits as per-item ops (insert/delete/move) with unique IDs and causal metadata. Persist locally with Realm, including tombstones. Batch",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a TrailEntry Codable struct with id, name, distance, rating, date, photoPath, latitude, longitude. Save/load as JSON in the app's Documents directory via FileManager. Acquire coordinates with Core",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Swift concurrency: on text change, cancel any active search Task, start a new one with a 300ms delay, then filter the dataset on a background queue using case-insensitive contains; update the UI o",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses capability to design advanced, crash-resilient, offline-first synchronization for real-time AR collaboration on iOS using CRDTs, CloudKit, and robust local persistence.\n\n## Key Concepts\n- Sequence CRDTs for ordered annotations\n- Offline-first with batched CloudKit sync\n- Tombstones and causal metadata for merges\n- Crash-safe resume via deterministic replay\n- ARKit + SwiftUI integration and persistence strategy\n\n## Code Example\n```javascript\n// Minimal CRDT operation example (pseudo)\ntype Op = { type: 'insert'|'delete'|'move', id: string, at?: number, item?: any, ts: number }\n```\n\n## Follow-up Questions\n- How would you test out-of-order deliveries and late joins?\n- How would you handle data privacy and access control in synced documents?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-14",
        "questionId": "q-10371",
        "question": "Design a Shared AR collaboration feature for an iOS app using ARKit that lets multiple devices place and persist real-world anchors while offline, then sync anchors and transforms when online. Outline data models, offline queue, merge strategy (CRDT vs OT), conflict resolution (timestamps, device priority), re-localization after relaunch, and a minimal SwiftUI/RealityKit sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a Codable Note type and a NotesStore class that loads/saves an array of notes to Documents/notes.json using an atomic write (temp file + swap). UI uses SwiftUI with @Published notes",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an AnchorStore with AnchorID, localTransform, confidence, and optional reference image; persist anchors and ARWorldMap snapshots to Core Data. Sync uses a CRDT-like delta merge: edits versio",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a compact DSL with AST nodes for Endpoint, Condition, Action; parse with a hand-written recursive descent; persist policies and counters in SQLite; intercept via URLProtocol to enforce on ev",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use cell reuse, pre-calculate heights, implement heightForRowAt caching, and optimize Auto Layout constraints with manual layout when needed.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-world AR collaboration concepts: offline-first design, anchor persistence, and conflict resolution in a multi-device setting.\n\n## Key Concepts\n\n- ARKit/RealityKit anchor lifecycle and re-localization\n- offline-first synchronization and delta queues\n- CRDT vs OT merge strategies for transforms\n- local persistence with ARWorldMap or anchors in Core Data\n- testing under latency, clock skew, and drift\n\n## Code Example\n\n```swift\nstruct AnchorRecord: Codable {\n  let id: String\n  let transform: simd_float4x4\n  let confidence: Float\n  let timestamp: Date\n}\n```\n\n## Follow-up Questions\n\n- How would you choose between CRDT and OT in this context?\n- How to ensure privacy/compression of anchor data across devices?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-15",
        "questionId": "q-8529",
        "question": "Design a beginner iOS SwiftUI screen named Checkpoint that lets users capture a short video (max 10s) from the device camera, automatically generate a thumbnail, store both files in the app's Documents directory using FileManager, and present a searchable grid listing with date filter. Persist metadata with Codable to a local JSON; include accessibility labels and a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a per-note CRDT (sequence CRDT) with vector clocks and an append-only log. Each edit is signed with CryptoKit and wrapped in a Merkle-proof block for cloud verification. Use Combine to strea",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a per-note CRDT for offline edits, encrypt note bodies with AES-GCM via CryptoKit, and rotate per-note keys using Keychain/Secure Enclave. Persist edits in SQLite with FTS for search; log edits",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a Checkpoint SwiftUI screen using PHPickerViewController wrapped in UIViewControllerRepresentable for 10-second video capture, AVAssetImageGenerator for thumbnail extraction, FileManager to save .mov and .png fil…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a client-side tracer for SwiftUI that records viewName, timestamp, renderMs, memory delta, and a privacyMask to a secure SQLite store (GRDB). Use Actors to serialize writes, and a background",
            "isCorrect": false
          }
        ],
        "explanation": "## Implementation\n- PHPickerViewController (iOS 14+) via UIViewControllerRepresentable for SwiftUI video capture with 10s limit\n- AVAssetImageGenerator extracts thumbnails from captured video\n- FileManager.documentDirectory stores app-managed .mov and .png files\n- SwiftUI LazyVGrid displays searchable grid with TextField search and DatePicker date filter\n- @State manages Codable metadata JSON persistence\n- All controls need .accessibilityLabel() for VoiceOver\n\n## Key Code\n```swift\nstruct VideoPicker: UIViewControllerRepresentable {\n    @Binding var videoURL: URL?\n    func makeUIViewController() -> PHPickerViewController {\n        var config = PHPickerConfiguration()\n        config.filter = .videos\n        config.selectionLimit = 1\n        let picker = PHPickerViewController(configuration: config)\n        return picker\n    }\n}\n\nstruct CheckpointView: View {\n    @State private var videos: [VideoMetadata] = []\n    @State private var searchText = \"\"\n    @State private var selectedDate = Date()\n\n    var filteredVideos: [VideoMetadata] {\n        videos.filter { video in\n            video.title.localizedCaseInsensitiveContains(searchText) &&\n            Calendar.current.isDate(video.date, inSameDayAs: selectedDate)\n        }\n    }\n\n    var body: some View {\n        LazyVGrid(columns: [GridItem(.adaptive(minimum: 100))]) {\n            ForEach(filteredVideos) { video in\n                VideoCell(video: video)\n                    .accessibilityLabel(\"Video \\(video.title)\")\n            }\n        }\n    }\n}\n```\n\n## Test Plan\n1. Record 10s video - verify file saved to Documents\n2. Check thumbnail auto-generated\n3. Search by title - verify filtering works\n4. Filter by date - verify correct results\n5. VoiceOver reads all labels correctly",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-16",
        "questionId": "q-13172",
        "question": "In a finance-focused iOS app, design a battery-conscious, offline-first price-alert system: users configure per-symbol price thresholds; a WebSocket feed streams high-frequency ticks; implement deduping, reordering out-of-order ticks, and a crash-resilient resume after relaunch. Store last prices and thresholds in SQLite, coalesce rapid threshold crosses into a single notification, and only notify when device is charging or battery > 25%. Outline data models, persistence, and a test plan; include a minimal SwiftUI/Combine sketch of the notification UI?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an Event model: struct Event: Identifiable, Codable { var id: UUID; var title: String; var date: Date; var location: String; var url: URL? }. Persist [Event] to Documents with JSONEncoder/Deco",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Bundle.main to load a bundled quiz.json into [Question], shuffle, and present one per screen with a SwiftUI ProgressView. Persist responses via Codable to Documents/QuizBites/responses.json; model",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "PriceAlertManager should be an async actor that consumes a WebSocket tick stream via URLSessionWebSocketTask, persists last prices and thresholds in SQLite (GRDB), and evaluates per-symbol alerts. Deb",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design an offline-first posting system for PocketTasks. When a user creates a task, the app attempts to POST to the backend immediately if connectivity is available. If the device is offline, serialize a QueueItem using…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests system design for streaming data with offline persistence, power-aware notifications, and crash-safe resume, a realistic fintech requirement.\n\n## Key Concepts\n- Async/await and actors\n- WebSocket streaming and out-of-order handling\n- SQLite persistence and per-symbol thresholds\n- Battery-aware notification gating\n- Debounce and crash-resume\n\n## Code Example\n```swift\n// Minimal placeholder for debounce wiring in Swift\nfunc debounce(_ delay: TimeInterval, action: @escaping () -> Void) {\n    // Implementation omitted\n}\n```\n\n## Follow-up Questions\n- How would you test debounce under bursty ticks?\n- How would you handle symbol delisting or threshold changes mid-flight?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-17",
        "questionId": "q-6409",
        "question": "In an iOS app for streaming video, implement an offline-first download queue: allow selecting multiple videos for download in parallel with multiple resolutions; ensure atomic file writes, resume after relaunch, support pause/resume and cancel, and report per-video progress. Outline data models (download record, video metadata, progress), caching strategy (disk storage + NSCache), background transfers (URLSession with background configuration), crash-safe resume logic, and a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I'll design a CRDT-like delta sync architecture for SyncNotes with the following components: **Data Models:** ```swift struct Note { let id: UUID var content: String var tags: Set<String> var vectorClock: [String: Int]…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Swift Concurrency with a per-chat ChatManager actor, a local MessageStore in SQLite with partitioned tables, and an OfflineQueue for retries. Encrypt payloads with CryptoKit AES-GCM using per",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a beginner iOS SwiftUI app MoodHeat that lets a user record a daily mood with a color tag and optional photo, persists locally via Codable in the Documents directory, shows a calendar heatmap b",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a robust offline-first download queue using Swift concurrency with a DownloadManager actor coordinating a TaskGroup of URLSessionBackgroundDownloadTasks. Write files to temporary locations, then atomically mov…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssess mastery of offline-first design, concurrency, persistence, and robust background transfers in iOS at scale.\n\n## Key Concepts\n- Offline-first downloads with background URLSession\n- Data modeling with CoreData and per-video metadata\n- Concurrency safety via actors and TaskGroup\n- Crash-safe resume and cancellation\n- Testing strategy for persistence and race conditions\n\n## Code Example\n```swift\n// Swift data models\nclass DownloadRecord {\n    let id: UUID\n    let videoId: String\n    let resolution: VideoResolution\n    var status: DownloadStatus\n    var progress: Double\n    var localPath: String?\n}\n\nactor DownloadManager {\n    private let session: URLSession\n    private let cache = NSCache<NSString, NSNumber>()\n    \n    func startDownload(videoId: String, resolution: VideoResolution) async {\n        // Implementation details\n    }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-18",
        "questionId": "q-5544",
        "question": "Design a beginner iOS SwiftUI app named FocusFlow that starts a 25-minute focus timer, shows a circular progress indicator, and saves remaining seconds to UserDefaults on every app lifecycle change. On relaunch, restore the previous state (remaining time and running/paused). The app should resume automatically if it was running. Include a minimal test plan for persistence and state restoration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a structured concurrency pipeline with an actor-owned buffer and a separate I/O layer. Run the WebSocket on a background Task, decode to Event with JSONDecoder, push to an in-memory ring buffer (m",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a MoodEntry model conforming to Codable, store the array in a single JSON file within the app's Documents directory using FileManager, and atomically persist changes by writing to a temporary file before repla…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To implement FocusFlow, create a FocusSession model with duration, remaining, isRunning, and lastUpdated; encode/decode to UserDefaults. Use a Circle progress view driven by (duration-remaining)/durat",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "**Data Model:** ```swift struct CheckEvent: Codable, Identifiable { let id: UUID let inspectorId: String let timestamp: Date let latitude: Double let longitude: Double let photosHash: String // SHA-256 hash of photo ass…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical SwiftUI state management, lifecycle handling, and simple persistence for a timer use case.\n\n## Key Concepts\n\n- SwiftUI state and ObservableObject patterns\n- App lifecycle (scenePhase) and backgrounding\n- Codable persistence to UserDefaults\n- Timer/Combine integration for UI updates\n\n## Code Example\n\n```swift\nimport Foundation\n\nstruct FocusSession: Codable {\n    var duration: Int // seconds\n    var remaining: Int // seconds\n    var isRunning: Bool\n    var lastUpdated: Date?\n}\n```\n\n## Follow-up Questions\n\n- How would you handle timer accuracy across background/foreground transitions?\n- How would you unit test persistence and relaunch restoration without UI tests?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-19",
        "questionId": "q-26657",
        "question": "In a beginner iOS SwiftUI app named 'HabitFlow', implement a local habit tracker that stores daily entries in a JSON file under the app's Documents directory, tracks streaks per habit (consecutive days with an entry), supports add/edit/delete of habits, shows a minimal SwiftUI list with today's streak, and includes a basic test plan for persistence and streak calculation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A suitable answer encodes models for Habit and DayEntry, uses FileManager to read/write a single JSON file in Documents, and computes current streak by counting consecutive days from today backward un",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "The solution should wire an AVCaptureSession with metadataOutput for EAN/UPC, parse it into a Core Data Item (id, name, barcode, createdAt), and save on add. Use Combine to debounce a search publisher",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a client CRDT-based delta engine for real-time document collaboration. Use a sequence CRDT to encode edits locally, persist as an append-only log, and maintain a Merkle tree root for integrity.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: implement a privacy-preserving storage layer using per-record symmetric keys (32-byte random values) encrypted with a master key protected in the Secure Enclave and stored in the Keychain. Rotate per-record ke…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to implement a simple, offline-first persistence layer with Codable, basic CRUD in a SwiftUI app, and derive a user-facing metric (streak) from daily entries. It avoids external dependencies and targets beginner-friendly data modeling and UI work.\n\n## Key Concepts\n- SwiftUI, Codable, FileManager and Documents directory usage\n- Data model design for Habit and DayEntry\n- Derived metrics: computing daily streaks from consecutive entries\n- Basic CRUD operations and crash-safe resume through persistent storage\n- Lightweight test plan focusing on persistence and streak logic\n\n## Code Example\n```swift\nstruct DayEntry: Codable { let date: Date; let completed: Bool }\nstruct Habit: Codable { let id: UUID; var name: String; var colorHex: String; var entries: [DayEntry] }\n\nlet url = FileManager.default.urls(for: .documentDirectory, in: .userDomainMask).first!.appendingPathComponent(\"habits.json\")\nfunc save(_ habits: [Habit]) throws {\n  let data = try JSONEncoder().encode(habits)\n  try data.write(to: url)\n}\nfunc load() -> [Habit] {\n  guard let data = try? Data(contentsOf: url) else { return [] }\n  return (try? JSONDecoder().decode([Habit].self, from: data)) ?? []\n}\n```\n\n## Follow-up Questions\n- How would you handle time zones and daylight saving when computing streaks?\n- How would you extend this to sync across devices, while preserving offline-first behavior?",
        "difficulty": "beginner"
      },
      {
        "id": "ios-q-20",
        "questionId": "q-22400",
        "question": "In an iOS multiplayer trivia game, design a deterministic lockstep networking layer using WebRTC DataChannels for low-latency play, with a server relay fallback. Requirements: 1) tick-based state sync with deterministic RNG and op-lists so all clients reach identical game state; 2) on-device replay log and crash-safe resume on relaunch; 3) compact, verifiable messages (sequence numbers, hashes, and optional per-tick proofs) to deter cheating; 4) a SwiftUI scoreboard showing live scores and per-player latency; 5) a minimal data model and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a FeatureFlagService in Swift using an Actor to serialize access. Fetch remote flags via URLSession, cache as Codable to disk with per-flag TTL (e.g., 24h). Evaluate per-user targeting using",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a tick-based deterministic engine: every tick uses a shared RNG seed, applies an ordered list of player actions to arrive at identical world state. Messages include Tick, Actions, and a per-tic",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement TaskPulse with a SwiftUI List of Task items (title, dueDate, isDone). Persist tasks as JSON in the app's Documents directory using Codable, load on launch, save on changes. Add a overdue bad",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt MVVM architecture: a Codable `Note` model, a `NotesStore` that persists to the Documents directory, and a SwiftUI `List` view. Each row displays a title and a color tag badge. For accessibility, set `accessibility…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess understanding of deterministic lockstep networking on mobile, recovery from interruptions, and cheat-resistance in a latency-sensitive multiplayer context.\n\n## Key Concepts\n\n- Deterministic game loop and tick synchronization\n- Shared RNG seeds and action ordering for identical state\n- Compact message framing: Tick, Actions, Hash\n- WebRTC DataChannels with server relay fallback\n- On-device replay log for crash-safe resume\n- SwiftUI scoreboard and latency metrics\n\n## Code Example\n\n```swift\nstruct GameTick: Codable {\n  let tick: UInt32\n  let actions: [PlayerAction]\n  let seed: UInt64\n  func hashValue() -> String {\n    // lightweight hash computation for per-tick verification\n    return String((tick ^ seed) &+ actions.reduce(0) { $0 ^ $0 + $1.hashValue })\n  }\n}\n\nstruct PlayerAction: Codable {\n  let playerId: String\n  let actionType: String\n  let payload: Data?\n}\n```\n\n## Follow-up Questions\n\n- How would you handle late joiners or players dropping out mid-game?\n- What tests would you run to verify cheat-resistance and determinism across devices with varying hardware?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-21",
        "questionId": "q-14885",
        "question": "In an iOS general app named LiveCollab that enables real-time team collaboration via shared whiteboard and live captions, design a client-side architecture to minimize latency and ensure crash-safe resume. Requirements: 1) offline-first, local event queue with CRDT-like convergence for concurrent edits; 2) low-latency data transport using WebRTC data channels with a server relay fallback; 3) on-device live transcription for captions using Apple's Speech framework, with on-device model; 4) persistence with SQLite/Realm and a replayable event log to rebuild state after relaunch; 5) a minimal SwiftUI view sketch showing captions and remote cursors; 6) a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I’d implement a CRDT-based op log (Event: id, actor, op, ts) with per-document state and causal vectors. Use WebRTC data channels for sub-50ms updates; fallback to a relay server with reliable channel",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would implement BookmarkSketch with a Bookmark struct containing id, title, url, note, tags, and date. Persist via Codable to a single file in Documents; read/write with FileManager and atomic write",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Core Data for offline-first persistence. Model Inspection and RelatedReading with a one-to-many relationship; expose them via @FetchRequest in SwiftUI. Validate required fields (equipmentId,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a privacy-preserving analytics pipeline: on-device Core ML inference drives summaries; events are validated, deduplicated, and stored in SQLite with per-user encryption via AES-GCM using a K",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses ability to design for real-time collaboration with latency constraints, offline resilience, and crash safety in a single iOS client. Requires practical choices about data modeling, transport, on-device ML, and persistence.\n\n## Key Concepts\n- CRDT-like op logs for concurrent edits\n- WebRTC data channels with server relay fallback\n- On-device speech-to-text with Apple frameworks\n- Realm/SQLite persistence and log replay on launch\n\n## Code Example\n```swift\nstruct Op { let id: String; let actor: String; let op: String; let ts: TimeInterval }\n```\n\n## Follow-up Questions\n- How would you test CRDT convergence under high churn?\n- How would you handle late-arriving edits and clock skew in the UI?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-22",
        "questionId": "q-22696",
        "question": "In an iOS fleet-management app for EVs, design a privacy-preserving on-device data explorer that lets operators run pre-defined analytics on streaming telemetry (speed, battery, location) entirely on-device. Requirements: 1) multi-tenant namespaces with per-operator access controls; 2) streaming ingestion via Combine with windowed aggregates (e.g., avg speed over rolling 5s); 3) crash-safe resume using a durable write-ahead log; 4) on-disk encryption using CryptoKit with per-tenant keys; 5) a SwiftUI dashboard showing live analytics and provenance; 6) a test plan and trade-offs between client vs server analytics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Swift concurrency: on text change, cancel any active search Task, start a new one with a 300ms delay, then filter the dataset on a background queue using case-insensitive contains; update the UI o",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-tenant on-device data explorer: namespaces per operator; crypto-at-rest with per-tenant keys (CryptoKit); streaming telemetry via Combine with 5s windows for averages; durable write-ahead log for",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a WebSocket-based live comment feed using URLSessionWebSocketTask for real-time communication, with SQLite persistence via GRDB. Design a Comment table with schema: `id TEXT PRIMARY KEY, seq INTEGER NOT NULL,…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a streaming, event-sourced client-side ML tagging pipeline: When photos are captured, a lightweight Core ML tagger runs in real time on-device, publishing TagEvent objects via Combine to a local replay log stored…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes on-device analytics, privacy safeguards, and offline resilience in a fleet context. It blends Combine-based streaming, per-tenant data segregation, and cryptographic guarantees, plus a practical UI.\n\n## Key Concepts\n- Multi-tenant data isolation\n- On-device streaming and windowed aggregations\n- Durable logging for crash-safe resume\n- CryptoKit-based encryption with per-tenant keys\n- SwiftUI dashboards and provenance tracking\n\n## Code Example\n```javascript\n// Placeholder example: actual implementation would be Swift\n```\n\n## Follow-up Questions\n- How would you extend this to support differential privacy for aggregates? \n- What performance bottlenecks might you hit on older devices and how to mitigate?",
        "difficulty": "intermediate"
      },
      {
        "id": "ios-q-23",
        "questionId": "q-29016",
        "question": "In an iOS app for field inspectors that collects BLE sensor readings from IoT devices, design an offline-capable data pipeline: local buffering, on-device anomaly detection with CoreML, crash-safe resume, and energy-aware background sync. Outline data models (Sensor, Reading, Session), persistence, ML integration, and a minimal SwiftUI view with live anomaly indicators?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a bounded in-memory ring buffer (size 1024) backed by SQLite; parse BLE beacon broadcasts into Telemetry (timestamp, deviceId, value); aggregate per-zone occupancy with a 15‑min window; dispatch C",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: Use Core Data with entities Sensor, Reading, Session; BLE central collects readings, buffered in a write-ahead log. Tiny CoreML model runs on-device for anomaly scores; crash-safe resume via",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a 300ms debounced autosave mechanism that triggers on text changes, serialize the Note model using Codable, perform atomic writes by first saving to a temporary file then replacing the target file, and load ex…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a client-side audit chain: Observation records signed with a device key, each with timestamp, location, notes; Evidence includes file hashes; Construct a Merkle tree over entries; store encryp",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question assesses the ability to design a robust offline-capable data pipeline around BLE IoT data, on-device ML, and crash-safe relaunch in a scale-out enterprise iOS app.\n\n## Key Concepts\n\n- Core Data schemas: Sensor, Reading, Session\n- BLE data collection and write-ahead buffering\n- On-device CoreML integration and inference isolation\n- Crash-safe resume via deterministic replay log\n- Energy-aware background tasks (BGProcessingTask) and Network.framework\n- CryptoKit-based per-field encryption\n\n## Code Example\n\n```swift\n// Minimal BLE + WAL sketch\nclass BLEManager { /* ... */ }\n```\n\n## Follow-up Questions\n\n- How would you test crash-recovery and replay determinism?\n- How would you scale the WAL to handle bursty BLE data without blocking the UI?",
        "difficulty": "advanced"
      },
      {
        "id": "ios-q-24",
        "questionId": "q-15493",
        "question": "In an iOS app MarketPulse used by traders to monitor streaming feeds (price ticks, news sentiment, social signals) with 1-second real-time updates, design a client-side pipeline that ingests feeds via WebSocket, buffers locally with bounded ring buffers, debounces UI updates to ~15fps for charts, runs on-device anomaly detection with a tiny CoreML model, and supports crash-safe resume via an event log replay into SQLite WAL. Include CloudKit multi-device sync with optimistic concurrency and clear conflict resolution. Provide a SwiftUI dashboard sketch and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Offline-first album sync using a CRDT (OR-Set) for PhotoMetadata (id, albumId, caption, tags, location, timestamp). Each device applies local edits, tombstones, and vector-clock-based merges. Enforce per-album ACLs duri…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a SwiftUI NotesEditor with a @Published note. Debounce saves to disk at 500ms using Combine: $note.debounce(for: .milliseconds(500), scheduler: RunLoop.main).sink { save(note) }. Persist by",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a streaming path: WebSocket ingest of ticks/news, per-feed bounded ring buffers, debounced UI updates to ~15fps, on-device anomaly detection with a tiny CoreML model, and crash-safe resume v",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use CoreData to model LocationPin, Polyline, and PhotoAttachment entities with a version field for conflict detection. Store media files in the Documents directory while maintaining metadata references in CoreData. Impl…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes end-to-end client-side streaming in a high-frequency finance context, including network ingestion, in-memory buffering, UI cadence control, on-device ML, crash-safe persistence, and multi-device sync with conflict resolution.\n\n## Key Concepts\n- WebSocket ingestion and reconnect\n- In-memory ring buffers and debouncing\n- CoreML anomaly detection\n- SQLite WAL and event replay\n- CloudKit optimistic concurrency\n\n## Code Example\n```swift\n// Minimal ring buffer sketch\nstruct RingBuffer<T> {\n  var buffer: [T] = []\n  mutating func append(_ v: T) {\n    // naive bounded append\n  }\n}\n```\n\n## Follow-up Questions\n- How would you measure UI latency end-to-end?\n- How would you simulate CloudKit conflicts in tests?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-24T03:02:49.766Z",
    "version": 1
  }
]