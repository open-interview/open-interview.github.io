[
  {
    "id": "test-data-structures",
    "channelId": "data-structures",
    "channelName": "Data Structures",
    "title": "Data Structures Knowledge Test",
    "description": "Test your understanding of Data Structures with 25 questions.",
    "questions": [
      {
        "id": "data-structures-q-0",
        "questionId": "q-15044",
        "question": "Design a data structure to process a stream of integers with three operations: add(x), remove(x) (remove one occurrence if present), and getMedian() which returns the current median value (or the average of the two middle values if even). Duplicates allowed. Explain how you maintain the two halves and how you rebalance on insert and delete; discuss time complexities?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an order-statistics tree (such as a treap or red-black tree) where each node stores the value, a count field for handling duplicates, and a size field representing the total count of elements in its subtree. The ins…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use two heaps: a max-heap for the lower half and a min-heap for the upper half, plus a hashmap for lazy deletions. On add(x), place x in the appropriate half and rebalance so sizes differ by at most 1",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "To set(i, j, v): update a 2D segment tree where each node stores a balanced BST of non-zero values with counts. sumSubmatrix uses standard 2D range sum with those trees. kthLargestInSubmatrix runs a v",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a dynamic MST approach with Link-Cut Trees. Maintain a MST forest and a global pool of non-tree edges by id. On addEdge: if separate components, link and add; else if w < maxEdgeOnPath(u,v), repla",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nStreaming data often requires maintaining a running statistic under insertions and deletions. The median is robust and a common interview target for dynamic data structures.\n\n## Key Concepts\n- Two-heap approach: keep lower half in a max-heap and upper half in a min-heap.\n- Duplicates supported; lazy deletion via a delay map.\n- Rebalancing invariant: size difference at most 1; median derived from heap tops.\n\n## Code Example\n```javascript\nclass MedianDS {\n  constructor(){\n    this.lo = new MaxHeap(); // lower half\n    this.hi = new MinHeap(); // upper half\n    this.delayed = new Map();\n    this.sizeLo = 0;\n    this.sizeHi = 0;\n  }\n  _cleanTop(h) {\n    const top = h.peek();\n    while (top !== null && this.delayed.get(top) > 0) {\n      h.pop();\n      this.delayed.set(top, this.delayed.get(top) - 1);\n    }\n  }\n  add(x){\n    if (this.sizeLo === 0 || x <= this.lo.peek()) {\n      this.lo.push(x); this.sizeLo++;\n    } else { this.hi.push(x); this.sizeHi++; }\n    this._balance();\n  }\n  remove(x){\n    const loTop = this.lo.peek();\n    if (loTop !== null && x <= loTop) {\n      this.delayed.set(x, (this.delayed.get(x) || 0) + 1);\n      this.sizeLo--;\n      if (x === loTop) this._cleanTop(this.lo);\n    } else {\n      this.delayed.set(x, (this.delayed.get(x) || 0) + 1);\n      this.sizeHi--;\n      this._cleanTop(this.hi);\n    }\n    this._balance();\n  }\n  _balance(){\n    if (this.sizeLo > this.sizeHi + 1) {\n      this.hi.push(this.lo.pop()); this.sizeLo--; this.sizeHi++;\n    } else if (this.sizeHi > this.sizeLo) {\n      this.lo.push(this.hi.pop()); this.sizeHi--; this.sizeLo++;\n    }\n    this._cleanTop(this.lo); this._cleanTop(this.hi);\n  }\n  getMedian(){\n    if (this.sizeLo === 0) return null;\n    if (this.sizeLo > this.sizeHi) return this.lo.peek();\n    return (this.lo.peek() + this.hi.peek()) / 2;\n  }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "data-structures-q-1",
        "questionId": "q-12039",
        "question": "Design a dynamic time-window data structure for a multiset of weighted events, each with a timestamp. Support insertEvent(t, w) and eraseEvent(t, w) (duplicates allowed). Implement queryMaxSubarraySumInWindow(t1, t2) that returns the maximum subarray sum over any contiguous sequence of events with increasing timestamps within [t1, t2], along with the start and end timestamps achieving it. Target amortized O(log n) updates and O(log n) queries. Explain your data layout and combination rules?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a Link-Cut Tree (Sleator–Tarjan). Represent edges as nodes carrying the weight; maintain path-sum as a segment-aggregate on the preferred paths. For pathKth, maintain an order-statistics struc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Trie Design for Strings with Duplicates ### Node Structure Each trie node contains: - `children`: map from character to child node - `subCount`: total number of strings in the subtree (including duplicates) - `endCou…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a treap keyed by interval start. Each node stores [l, r, w, id] along with augmented data: subtreeWeightSum. Maintain a map from id to node for O(1) lookups. insertInterval(l, r, w) splits existing intervals at boun…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a balanced BST keyed by timestamp; to support duplicates, store at each timestamp the total weight and event count. Each node also maintains Kadane-style aggregates for its subtree: sum, max prefi",
            "isCorrect": true
          }
        ],
        "explanation": "Why This Is Asked\n\nTests ability to design a time-ordered data structure with dynamic updates and range Kadane queries, useful for analytics with streaming events.\n\nKey Concepts\n\n- Kadane’s algorithm generalized to segments via a monoid: sum, pref, suff, best.\n- Handling duplicates by aggregating per timestamp.\n- Range queries using split/merge or traversal to collect O(log n) aggregates.\n\nCode Example\n\n```javascript\n// Pseudocode sketch of node structure and combine(a,b)\nclass Node{ constructor(t, w){ this.t = t; this.cnt = 1; this.sum = w; this.pref = Math.max(0, w); this.suff = Math.max(0, w); this.best = w; } }\nfunction combine(left, right){ /* merge Kadane aggregates */ }\n```\n\nFollow-up Questions\n\n- How would you extend to support deletions by weight and query the exact subarray indices when multiple subarrays tie for best sum?\n- How would you adapt for 2D time-series (two timestamps) or streaming without offline compression?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-structures-q-2",
        "questionId": "q-26370",
        "question": "Design a dynamic set of lines y = m x + b supporting addLine(m,b,id), removeLine(id), and queryMin(x) returning the minimum y across all active lines for a given x. Duplicates allowed. The structure must handle online interleaving of adds/removes up to N operations and supports queries in O(log^2 N) expected time. Explain lifetime interval assignment, per-node Li Chao maintenance, and edge cases with identical slopes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a two-heap design (lower half in a max-heap, upper half in a min-heap) with a hashmap for lazy deletions to support erase(x) (one occurrence). Balance so the sizes differ by at most 1; prune l",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Persist a segment tree over coordinate-compressed x. Each interval [L,R] adds weight w to every node covering [L,R]. Use a persistent multiset (treap) per node to track weights with duplicates. Each i",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Link-Cut Tree (LCT) augmented with per-node multisets of incident edge labels and a path-aggregated order-statistics structure. Expose path(u,v) to merge labels along the path; kthSmallestOnPath",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "This problem combines offline lifetime decomposition with per-node Li Chao trees. Each line's lifetime is represented as an interval [insertTime, removeTime) in a segment tree built over time. When adding a line, record…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests expertise in fully dynamic Li Chao trees, offline lifetime decomposition, and practical handling of duplicates in production settings.\n\n## Key Concepts\n- **Segment tree over time** for lifetime interval decomposition\n- **Per-node Li Chao trees** for minimum query aggregation\n- **Path-query traversal** across O(log N) nodes\n- Memory-time trade-offs for handling large N and duplicates\n\n## Code Example\n```javascript\nclass LiChao {\n  insert(line) { /* insert line into this node's Li Chao tree */ }\n  query(x) { /* return minimum y at x across stored lines */ }\n}\n\nclass DynamicLineSet {\n  constructor(N) {\n    this.segmentTree = new SegmentTree(N);\n  }\n\n  addLine(m, b, id, insertTime) {\n    const interval = [insertTime, Infinity];\n    this.segmentTree.addInterval(interval, { m, b, id });\n  }\n\n  removeLine(id, removeTime) {\n    // Update the line's interval end time in the segment tree\n    this.segmentTree.closeInterval(id, removeTime);\n  }\n\n  queryMin(x, queryTime) {\n    return this.segmentTree.queryPath(queryTime)\n      .reduce((min, lichao) => Math.min(min, lichao.query(x)), Infinity);\n  }\n}\n```\n\n## Complexity Analysis\n| Operation | Time | Space |\n|-----------|------|-------|\n| addLine | O(log N) | O(log N) per line |\n| removeLine | O(log N) | - |\n| queryMin | O(log² N) | - |\n\n## Edge Cases\n- **Identical slopes**: Stored separately with unique ids; Li Chao tree handles min correctly\n- **Immediate removal**: Assigned to leaf node only\n- **Large N**: Memory scales with O(N log N) total line-node assignments",
        "difficulty": "advanced"
      },
      {
        "id": "data-structures-q-3",
        "questionId": "q-13712",
        "question": "Design a dynamic sequence data structure that stores integers and supports insertAt(index, value), eraseAt(index), and rangeMedian(l, r). Duplicates allowed. Values are 32-bit integers. Achieve O(log V) per operation and O(log V) for rangeMedian, where V is the value range, using a Dynamic Wavelet Tree or an equivalent structure. Explain the data layout, how inserts/erases update the structure, and how rangeMedian is computed?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a segment-tree on coordinate-compressed axis. Each node maintains a Fenwick over weight ranks to count occurrences and a per-node sum of weights. An insert/erase touches O(log N) nodes, adjusting",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Insert and erase affect counts and subtree sizes; kthSmallest uses cumulative counts; countLessThan uses in-order traversal via sizes. Choose a Treap or AVL with a count field for duplicates and a siz",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Dynamic Wavelet Tree over the 32-bit value range [0, 2^31-1]. Each level maintains a dynamic bitvector that tracks whether symbols go to the left or right subtree; support insertAt and eraseAt by updating these bi…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "We use a segment tree where each node stores a Fenwick tree (BIT) indexed by compressed weights. This gives O(log² N) for updates and O(log N * log W) for queries. **Data Structure**: - Segment tree built on compressed…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests dynamic order-statistics techniques on mutable sequences, extending beyond static array approaches. It evaluates understanding of efficient range-quantile queries using dynamic wavelet trees and the propagation of updates through multi-level index structures.\n\n## Key Concepts\n\n- Dynamic order statistics in sequences\n- Wavelet tree variants and their dynamic implementations\n- Range median/quantile queries via value-domain binary search\n- Insertion/deletion operations in hierarchical index structures\n\n## Code Example\n\n```javascript\n// Minimal skeleton demonstrating dynamic wavelet tree operations\nclass DynamicWaveletTree {\n  constructor(valueRange = Math.pow(2, 31)) {\n    this.root = this.buildNode(0, valueRange - 1);\n  }\n  \n  insertAt(index, value) {\n    this.updateAt(index, value, true);\n  }\n  \n  eraseAt(index) {\n    this.updateAt(index, null, false);\n  }\n  \n  rangeMedian(l, r) {\n    return this.binarySearchMedian(l, r);\n  }\n  \n  // Core operations implemented across tree levels\n  updateAt(index, value, isInsert) { /* implementation */ }\n  binarySearchMedian(l, r) { /* implementation */ }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "data-structures-q-4",
        "questionId": "q-9182",
        "question": "Design a dynamic interval set on the number line with operations: addInterval(l, r, w, id), removeInterval(id, w), and queries at coordinate x: sumWeightsAt(x) and kthLargestWeightAt(x, k). Duplicates allowed. Achieve amortized O(log^2 n) updates and O(log^2 n) queries. Explain data structure, coordinate compression, and how to retrieve the kth largest weight among overlapping intervals?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a segment-tree over compressed coordinates. Each node holds a multiset of active weights and a Fenwick over the weights for fast sums and k-th selection. To addInterval, update O(log N) nodes",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Persist a segment tree over coordinate-compressed x. Each interval [L,R] adds weight w to every node covering [L,R]. Use a persistent multiset (treap) per node to track weights with duplicates. Each i",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a dynamic multiset with insert(x), erase(x) (one occurrence), and kthSmallest(k) (1-based). Use coordinate compression to map values to indices, and a Fenwick tree (or Segment Tree) to store c",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Maintain two complementary structures: a hash map `playerToScore` mapping `playerId -> score`, and a tree map `scoreToPlayers` mapping `score -> ordered set of players`. For `addOrUpdatePlayer`: remove the player from t…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests dynamic interval management, coordinate compression, and order-statistics in a segment-tree setting.\n\n## Key Concepts\n- Segment tree over coordinates\n- Per-node multiset with frequencies\n- Coordinate compression; lazy deletion\n\n## Code Example\n```javascript\n// Pseudo: segment tree with addInterval/removeInterval and queryAt\nclass IntervalDS {\n  constructor(coords) {\n    // build segment tree on compressed coords\n  }\n  addInterval(l, r, w, id) {\n    // update O(log N) nodes by inserting w\n  }\n  removeInterval(id, w) {\n    // update O(log N) nodes by deleting w\n  }\n  sumAt(x) {\n    // accumulate sums along path to x\n  }\n  kthLargestAt(x, k) {\n    // binary search over weights using per-node counts\n  }\n}\n```\n\n## Follow-up Questions\n- How to handle removing a specific weight when duplicates exist? \n- Memory optimizations for dense coordinate ranges? \n- How would you adapt to dynamic coordinate insertion without re-building compression?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-structures-q-5",
        "questionId": "q-14487",
        "question": "Design a data structure to implement a queue that supports enqueue, dequeue, and getMin in O(1) time. Explain the data layout, how each operation maintains the minimum, and how duplicates are handled. Provide an implementation plan and edge-case behavior for empty states, in a language of your choice?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a MinQueue using two stacks. Each stack stores pairs (val, min). Enqueue x: push (x, min(x, top.min)) to inbox. Dequeue: if outbox empty, move all inbox to outbox, recomputing min for each p",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a segment tree over the time axis. Each node stores a multiset of weights (allowing duplicates) and its aggregated sum. For addInterval(id, l, r, w), removeInterval(id), and updateInterval(id, newW), we traverse the…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an implicit treap (rope) where each node stores its subtree length and a 64-bit rolling hash; leaves hold text chunks. Operations: split by position, insert by merging a treap built from the inser",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Persist a segment tree over coordinate-compressed x. Each interval [L,R] adds weight w to every node covering [L,R]. Use a persistent multiset (treap) per node to track weights with duplicates. Each i",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of practical queue augmentations and min-tracking with guaranteed O(1) per operation.\n\n## Key Concepts\n- Min-tracking with stack-based queues\n- Amortized analysis of transfers between stacks\n- Edge-case handling for empty states and duplicates\n\n## Code Example\n```javascript\nclass MinQueue {\n  constructor(){ this.inbox = []; this.outbox = [] }\n  _push(stack, val){ const m = stack.length ? Math.min(val, stack[stack.length - 1].min) : val; stack.push({ val, min: m }) }\n  enqueue(x){ this._push(this.inbox, x) }\n  dequeue(){ if (!this.outbox.length){ while (this.inbox.length){ const el = this.inbox.pop(); const m = this.outbox.length ? Math.min(el.val, this.outbox[this.outbox.length - 1].min) : el.val; this.outbox.push({ val: el.val, min: m }) } }\n    return this.outbox.pop().val }\n  getMin(){ const mins = []; if (this.inbox.length) mins.push(this.inbox[this.inbox.length - 1].min); if (this.outbox.length) mins.push(this.outbox[this.outbox.length - 1].min); return mins.length ? Math.min(...mins) : undefined }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "data-structures-q-6",
        "questionId": "q-727",
        "question": "Design a dynamic data structure for weighted 3D points (x,y,t) with weight w. Support insertPoint(x,y,t,w) and erasePoint(x,y,t,w) (duplicates allowed), rangeSum3D(x1,x2,y1,y2,t1,t2) for total weight in the 3D box, and findKthLargestInRange(x1,x2,y1,y2,t1,t2,k) for the k-th largest weight inside the box. Target O(log^3 n) per op; O(log W * log^3 n) for kth; discuss coordinate compression, memory trade-offs, and handling duplicates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Trie with 26 children per node; each terminal node stores a frequency. insert(word): walk/create nodes; freq++ at terminal. erase(word): if freq>0, freq--, prune nodes with no children and freq=",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed solution: implement a persistent implicit treap; each insert/erase copies O(log n) nodes along the access path and updates subtree sums. Get and rangeSum use implicit indices, traversing by r",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an implicit treap as a rope where each node stores a string chunk, its length, and a pair of rolling hashes. Maintain subtree length and a hash pair. Insert/erase split by position, adjust/split c",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use coordinate compression on x, y, and t, and implement a 3D segment-tree (nested trees) to support dynamic inserts/deletes with duplicates. Each node stores aggregated weight sums and counts. rangeS",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of multi-dimensional data structures, dynamic updates with duplicates, and how to support order-statistics within spatial ranges. It also probes trade-offs between dense 3D structures vs sparse representations and the practicality of coordinate compression.\n\n## Key Concepts\n- 3D range queries with dynamic updates\n- Coordinate compression and nested segment trees\n- Order statistics within geometric ranges and handling duplicates\n- Trade-offs: memory vs time, online vs offline approaches\n\n## Code Example\n```javascript\n// Skeleton prototype for 3D segment tree with insert/erase and range query\nclass Node3D {\n  constructor() { /* ... */ }\n  update(x, y, t, w, delta) { /* delta = +w or -w */ }\n  query(x1, x2, y1, y2, t1, t2) { /* returns sum of weights */ }\n  kthInRange(x1,x2,y1,y2,t1,t2,k){ /* via weight-binary-search on tree */ }\n}\n```\n\n## Follow-up Questions\n- How would you optimize memory for sparse data?\n- How would you extend to support dynamic weight updates (change weight w for an existing point)?",
        "difficulty": "advanced"
      },
      {
        "id": "data-structures-q-7",
        "questionId": "q-5902",
        "question": "Design a dynamic sequence data structure that supports insertAt(index, value), eraseAt(index), rangeCountLE(l, r, x) and rangeKth(l, r, k). Duplicates allowed. Target average O(log sigma * log n) per operation (≈ O(log^2 n) when sigma ≈ n). Explain using a dynamic Wavelet Tree, how insert/erase propagate counts along the path, and how rank/kth are computed?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Dynamic Wavelet Tree over the value domain. The sequence is indexed by position; each node stores the count of elements that go to its left. For insertAt, follow the path from root to leaf updating counts along th…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use an ordered map keyed by interval start to store non-overlapping, merged intervals. addInterval merges all overlapping neighbors, removing their entries and extending the new end; removeInterval su",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a dynamic MST approach with Link-Cut Trees. Maintain a MST forest and a global pool of non-tree edges by id. On addEdge: if separate components, link and add; else if w < maxEdgeOnPath(u,v), repla",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use coordinate compression of scores and a Fenwick tree of counts. Build a sorted list of all possible scores ahead of time; map each score to an index. On insert: add 1 at idx; on erase: add -1. On c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests knowledge of dynamic order-statistics in a sequence, combining updates with range queries. It requires choosing a structure that supports positional edits and value-based queries efficiently.\n\n## Key Concepts\n- Dynamic Wavelet Tree for range counting and kth in a mutable sequence\n- Handling duplicates and pointer-free traversal on value domain\n- Complexity: O(log sigma * log n) per operation; near O(log^2 n) when sigma ~ n\n\n## Code Example\n```javascript\nclass DynamicWaveletTree {\n  constructor(minV, maxV) { /* ... */ }\n  insertAt(i, v) { /* update path counts */ }\n  eraseAt(i, v) { /* decrement path counts */ }\n  rangeCountLE(l, r, x) { /* rank queries at each level */ }\n  rangeKth(l, r, k) { /* binary search with rank */ }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "data-structures-q-8",
        "questionId": "q-4015",
        "question": "Design a queue data structure that supports: push(x) to back, pop() from front, max() returning the current maximum element, and sum() returning the sum of all elements in the queue. All four operations must be amortized O(1). Provide a concrete implementation plan (two stacks with per-element max, and a running sum) and a short code sketch in a language of your choice?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To support weighted strings with duplicates, build a trie over inserted words. Each node stores a multiset of weights for all words having that node’s prefix. On insertWord(word,w): traverse the path;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Trie with per-node pass-through counts. insertWord(word) traverses/creates nodes, incrementing counts; eraseWord(word) traverses and decrements, removing nodes only if counts reach zero; countWordsWithPrefix…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two-stack queue. Maintain two stacks of objects {val, max}. push(x) adds to the in-stack with max = max(x, in.top?.max). pop() moves items to the out-stack when needed and pops from it. max() returns",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use coordinate compression for all l, r+1 values and maintain two Fenwick trees (B1, B2) to support rangeAdd and prefixSum. insertInterval(l,r) updates B1 at l +1 and r+1 -1; B2 at l +1*(l-1) and r+1",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical DS reasoning for streams where both max and total are needed efficiently, using a familiar two-stack queue pattern.\n\n## Key Concepts\n- Two-stack queue with per-element max\n- Running sum maintenance for O(1) sum queries\n- Amortized analysis of transfer between stacks\n\n## Code Example\n```javascript\nclass MaxQueue {\n  constructor() {\n    this.in = [];\n    this.out = [];\n    this.total = 0;\n  }\n  push(x) {\n    const newMax = this.in.length === 0 ? x : Math.max(x, this.in[this.in.length - 1].max);\n    this.in.push({ val: x, max: newMax });\n    this.total += x;\n  }\n  pop() {\n    if (this.out.length === 0) {\n      while (this.in.length > 0) {\n        const v = this.in.pop().val;\n        const newMax = this.out.length === 0 ? v : Math.max(v, this.out[this.out.length - 1].max);\n        this.out.push({ val: v, max: newMax });\n      }\n    }\n    if (this.out.length === 0) return undefined;\n    const popped = this.out.pop();\n    this.total -= popped.val;\n    return popped.val;\n  }\n  max() {\n    const maxIn = this.in.length === 0 ? -Infinity : this.in[this.in.length - 1].max;\n    const maxOut = this.out.length === 0 ? -Infinity : this.out[this.out.length - 1].max;\n    if (maxIn === -Infinity && maxOut === -Infinity) return undefined;\n    return Math.max(maxIn, maxOut);\n  }\n  sum() {\n    return this.total;\n  }\n}\n``` \n\n## Follow-up Questions\n- How would you adapt this to support a sliding window of last N elements for max and sum?\n- How would you extend to also provide a min() with the same complexity guarantees?",
        "difficulty": "beginner"
      },
      {
        "id": "data-structures-q-9",
        "questionId": "q-5791",
        "question": "Design a dynamic multiset of integers with operations insert(x), erase(x), kthSmallest(k), and rangeCount(l, r) and rangeSum(l, r). Duplicates allowed. Implement a treap-based structure storing per-node: key, priority, cnt, sz, sm. Explain update rules, how duplicates are handled, and how to implement kthSmallest and range queries in O(log n) on average?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a time-segment tree over operation times. Each insertLine creates an active interval [start, end). Store (m,b,id) in every node covering its interval. Each node maintains a Li Chao tree for its li",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two balanced heaps with lazy deletion. Maintain maxHeap for lower half and minHeap for upper half. On insert, push to appropriate heap, then rebalance so their sizes differ by at most 1. On erase, mar",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an implicit treap (rope) where each node stores val, priority, size, and sum. The implicit key is the position (index), not an explicit value. To support moveSegment(l, r, dest): 1. Split the treap into three parts:…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a treap keyed by value; each node stores key, priority, cnt, sz, sum. insert(x) increments cnt at node or inserts new; erase(x) decrements cnt or removes node; after changes update sz and sum. kth",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests augmented BST design for duplicates and range queries on a dynamic multiset, focusing on balance, memory, and correctness.\n\n## Key Concepts\n- Treap with duplicates via per-node cnt\n- Subtree size and sum augmentation\n- kth smallest by counts; range queries via splits or traversal\n\n## Code Example\n```javascript\nclass Node{constructor(key,prio){this.key=key;this.prio=prio;this.cnt=1;this.l=null;this.r=null;this.sz=1;this.sum=key}};\nfunction upd(n){if(!n)return; n.sz=n.cnt+(n.l?.sz||0)+(n.r?.sz||0); n.sum=n.key*n.cnt+(n.l?.sum||0)+(n.r?.sum||0)}\nfunction rotateRight(y){const x=y.l; y.l=x.r; x.r=y; upd(y); upd(x); return x}\nfunction rotateLeft(x){const y=x.r; x.r=y.l; y.l=x; upd(x); upd(y); return y}\nfunction insert(n, key){if(!n)return new Node(key,Math.random()); if(key===n.key){n.cnt++; upd(n);return n}; if(key<n.key){n.l=insert(n.l,key); if(n.l.prio>n.prio) n=rotateRight(n);} else {n.r=insert(n.r,key); if(n.r.prio>n.prio) n=rotateLeft(n);} upd(n); return n}\nfunction kth(n,k){if(!n) return null; const leftSize=n.l?.sz||0; if(k<=leftSize) return kth(n.l,k); if(k>leftSize+n.cnt) return kth(n.r,k-leftSize-n.cnt); return n.key;}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "data-structures-q-10",
        "questionId": "q-17380",
        "question": "Design a dynamic 1D interval multiset on a number line with insertInterval(l,r,w,id) and eraseInterval(id). Queries: sumWeightsCoveringPoint(p) and kthLargestWeightCoveringPoint(p,k). Use coordinate compression and a segment tree; each node holds a multiset of weights for intervals fully covering that node. Insert/erase add/remove w on O(log n) nodes; sum is path-sum; kth uses per-node order statistics with a global binary search on weights?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "One viable approach uses a 2D segment tree with per-node color-weight maps. On insert/erase, update O(log n) nodes in each dimension, adjusting the color sums. For topKColorsInRectangle, pull O(log^2",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "```javascript class TrieNode { constructor() { this.children = new Map(); this.weightMultiset = new Map(); this.subtreeTotal = 0; } } class DynamicPrefixDictionary { constructor() { this.root = new TrieNode(); } insertW…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a segment tree over compressed coordinates. Each node stores a multiset of weights for intervals that fully cover that node. Maintain a map from id to (l,r,w). On insertInterval, update O(lo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Two-heap median structure: maintain a max-heap for the lower half (low) and a min-heap for the upper half (high), keeping their sizes balanced (within 1 of each other). Use a hash map for lazy deletion since actual heap…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests designing 1D interval multisets with duplicates, real-time updates, and point queries. Also exercises combining coordinate compression, segment trees, and order statistics.\n\n## Key Concepts\n- Segment tree on compressed coordinates\n- Per-node multisets for duplicates\n- Erase via id-tracking map\n- Path-sum and binary-search-based kth\n\n## Code Example\n```javascript\nclass IntervalDS { /* skeleton demonstrates API */ }\n```\n\n## Follow-up Questions\n- How would you scale to extremely large weight magnitudes?\n- How would you adapt if queries must run in parallel threads?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-structures-q-11",
        "questionId": "q-17779",
        "question": "Design a dynamic multiset of strings (duplicates allowed) that supports insert(s), erase(s) (one occurrence), and queries: countInPrefix(p) and kthStringWithPrefix(p, k). The kthStringWithPrefix should return the actual string, not just its index. Explain the data structure and update/query procedures, with expected time bounds?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed solution: a trie where each node stores the total strings in its subtree and a map of children. insert/erase adjust counts along the path; countInPrefix(p) returns the subtree count at the no",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Map -1000..1000 to 1..2001 in a Fenwick tree. On insertValue/eraseValue update index i = x + 1001. Maintain totalCount. findQuantile(p): if totalCount==0 return null; k = ceil(p * totalCount); idx = l",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a segment tree over the coordinate space, where each node maintains a multiset of weights for intervals that fully cover that node's segment. For insertInterval(l, r, w), traverse the segment tree and add weight w t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Proposed approach: index per category with a dynamic segment tree over compressed timestamps; each node stores total weight and a multiset of weights to enable kthLargest within a range. Insert/erase",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests combining a prefix-optimized search structure with dynamic multiplicities and order statistics for practical search features.\n\n## Key Concepts\n- Trie augmentation with per-prefix counts\n- Lexicographic order navigation for kth selection\n- Multiset with duplicates and dynamic updates\n\n## Code Example\n```javascript\nclass Node { constructor(){ this.next = new Map(); this.cnt = 0; this.isEnd = 0; } }\nclass PrefixTrie { /* insert/erase/kth implemented via counts */ }\n```\n\n## Follow-up Questions\n- How to scale memory for large alphabets?\n- How to efficiently support full deletion of a string and node pruning?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-structures-q-12",
        "questionId": "q-19509",
        "question": "Design a dynamic, duplicate-tolerant priority queue that supports: insert(key, id), erase(id), decreaseKey(id, newKey), and kthLargest(k). Duplicates allowed. Aim for amortized O(log n) per operation and O(log n) for kthLargest. Explain a concrete data structure (e.g., treap or balanced BST) with (price, id) ordering to disambiguate equal keys, how erase by id works, how decreaseKey is implemented, and how kthLargest finds the k-th largest when duplicates exist. Context: high-volume order book or job scheduler?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a binary IP trie. Each node stores subWeightSum, and a weight→count map (or a balanced BST) for its subtree. insertAccess/removeAccess update the path to the leaf, adjusting sums and counts.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a treap keyed by (price, id) with id ensuring unique ordering and subtree sizes. Maintain price[id]. insert(key,id): insert (price[id], id). erase(id): delete (price[id], id). decreaseKey(id,newKe",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use coordinate compression for latency values and a Fenwick tree of counts. insertLatency(x) increments count at idx(x); eraseLatency(x) decrements if present. percentileQuery(p) sets k = ceil(p/100 *",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a balanced BST (e.g., treap) where each node stores: key, count (for duplicates), and size (subtree total counts). insert(x): +1 count or create node; erase(x): -1 and prune if count=0; kthSmalles",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests mastery of augmented BSTs, duplicates handling, and order-statistics under dynamic updates, with real-world relevance to streaming data structures used in trading or scheduling.\n\n## Key Concepts\n\n- Augmented BST (treap) with key = (price, id) for unique ordering\n- Subtree size to support kthLargest in O(log n)\n- id->price map to locate keys for erase/decreaseKey\n- Erase by exact key; decreaseKey = erase+reinsert\n- Tradeoffs: randomness in treap vs. strict balancing, memory use\n\n## Code Example\n\n```javascript\n// Pseudo: insert and kthLargest on treap\n```\n\n## Follow-up Questions\n\n- How would you support bulk updates or persistence?\n- How would you handle concurrent updates in a lock-free style?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-structures-q-13",
        "questionId": "q-15819",
        "question": "Design a dynamic multiset of integers supporting insert(x), erase(x) (duplicates allowed), and querySumInRange(l, r) which returns the sum of all elements with values in the inclusive range [l, r]. Explain the data structure you would use, how you handle duplicates, and outline how insert/erase adjust subtree sums to enable range queries in O(log n) time on average?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a balanced BST keyed by timestamp; to support duplicates, store at each timestamp the total weight and event count. Each node also maintains Kadane-style aggregates for its subtree: sum, max prefi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "An augmented balanced BST (e.g., treap/AVL) where each node stores: value, count, left, right, and subtreeSum. On insert, increment count if value exists; otherwise create node. On erase, decrement co",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a circular buffer of size k to store the last k values, along with a running sum. On add(v): if the window is full (size == k), subtract the oldest value from the running sum to evict it. Store v at the current inde…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a splay-based Link-Cut Tree. Represent each edge as an edge node connected to its two endpoints; vertex nodes carry 0. Each node stores sum and max on its splay-subtree with a reverse flag. link/c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to augment BSTs for range queries and handle duplicates cleanly.\n\n## Key Concepts\n- Augmented BST with subtree sums\n- Duplicates via per-node counts\n- Efficient range queries with pruning\n\n## Code Example\n```javascript\n// Pseudocode sketch omitted for brevity\n```\n\n## Follow-up Questions\n- How would you optimize for memory?\n- How would you handle negative values or huge ranges?",
        "difficulty": "beginner"
      },
      {
        "id": "data-structures-q-14",
        "questionId": "q-3061",
        "question": "Design a dynamic data structure for a set of 2D colored points that supports: insertPoint(x,y,color), erasePoint(x,y,color) (duplicates allowed), and countDistinctColorsInRect(x1,y1,x2,y2) returning how many unique colors appear in the rectangle. Optimize for average O(log^2 n) per op; discuss coordinate compression, per-node color maps, and duplicate handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a two-level dynamic range structure: a segment tree over x-coordinates, where each node stores a balanced BST keyed by y-coordinates with a multiset of colors. Insert/erase operations update O(log n) x-nodes and O(l…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a run-length encoded set of disjoint intervals with bits. Maintain an ordered map of [L, R, bit]. For set(l,r), split at l and r+1, erase overlapping blocks, insert [l,r,1], and merge neighbors wi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a binary IP trie. Each node stores subWeightSum, and a weight→count map (or a balanced BST) for its subtree. insertAccess/removeAccess update the path to the leaf, adjusting sums and counts.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement an implicit treap where each node stores value, priority, size, and sum. To insertAt(i,x), split by i, merge left + new node + right. eraseAt(i) splits into left, mid, right, drops m",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates a candidate's ability to design hybrid data structures that support dynamic 2D range queries with an additional color dimension, a common requirement in spatial analytics and geographic information systems.\n\n## Key Concepts\n- Dynamic 2D range structures (segment trees, range trees)\n- Per-node multisets for color tracking and distinct counting\n- Coordinate compression for memory efficiency\n- Duplicate handling via color frequency counters\n- Trade-offs between memory usage and query performance\n- Lazy rebuilding strategies for coordinate space optimization\n\n## Code Example\n```javascript\n// Sketch: insertPoint updates across O(log n) x-nodes,\n// adjusting per-node y-color maps and distinct color counters\nfunction insertPoint(x, y, color) {\n  const compressedX = compressCoordinate(x);\n  updateSegmentTree(compressedX, y, color, 1);\n}\n```\n\n## Follow-up Considerations\n- How would you handle range queries for specific colors?\n- What optimizations could reduce the O(log² n) complexity?\n- How would you implement batch operations efficiently?",
        "difficulty": "advanced"
      },
      {
        "id": "data-structures-q-15",
        "questionId": "q-20883",
        "question": "Design a persistent, immutable priority queue that supports: insert(x) -> id, min(), popMin() -> (id, x) and a new version, decreaseKey(id, newX), plus versioning operations forkVersion() and checkoutVersion(v). How would you implement O(log n) per operation with O(log n) new nodes per update and multiple versions coexisting?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a sqrt-decomposition sequence: split the dynamic array into blocks of size ≈√n. Each block stores its elements and a cached sum. On insertAt/eraseAt, locate the containing block, modify the block'",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement as a persistent binary heap with path copying. Each insert, popMin, or decreaseKey creates a new root and O(log n) new nodes along the path, while reusing the rest of the tree. Keep a map id",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a persistent implicit treap to model the sequence, where each node stores value, size, and a persistent segment-tree over value domain to maintain a multiset of its subtree. For rangeKth(l,r,k,t),",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a dynamic suffix automaton with reversibility. Each append adds at most one state and O(1) link adjustments; pops restore a prior version via a checkpoint stack. countOccurrences(P) follows P",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nPersistent data structures enable undo and branching; a persistent priority queue tests functional design, memory reuse, and versioning.\n\n## Key Concepts\n- Persistence via structural sharing\n- Path copying on updates\n- Versioning model with fork and checkout\n\n## Code Example\n```javascript\nclass Node { constructor(val,left,right){ this.val=val; this.left=left; this.right=right } }\nfunction insert(root, x){ /* ... */ return newRoot; }\n```\n\n## Follow-up Questions\n- How to reclaim memory across versions\n- How to handle many versions efficiently",
        "difficulty": "intermediate"
      },
      {
        "id": "data-structures-q-16",
        "questionId": "q-11004",
        "question": "Design a dynamic forest data structure that supports linking two trees with a weighted edge, cutting an existing edge, updating an edge weight, and querying the sum of edge weights on the unique path between any two nodes. Provide amortized O(log n) per operation and explain how you prevent cycles?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Maintain three stacks: a main stack for values, a minStack to track the current minimum, and a maxStack to track the current maximum. **Push(x):** - Push x to the main stack - Push min(x, top of minStack) to minStack -…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a segment-tree over compressed coordinates. Each node holds a multiset of active weights and a Fenwick over the weights for fast sums and k-th selection. To addInterval, update O(log N) nodes",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Link-Cut Tree (LCT). Model each edge as a dedicated node containing its weight. Augment each splay subtree to track the maximum weight within that subtree. For **link(u, v, weight)**: first confirm that u and v be…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Link-Cut Tree with edge-nodes. Represent each edge as a dedicated node holding its weight; link(u,v,w) creates edge node e, then link u-e and e-v. cut(u,v) removes the two links to the edge node",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses mastery of dynamic trees, edge augmentation, and path-aggregate queries under frequent updates, aligned with scalable backends.\n\n## Key Concepts\n- Link-Cut Tree with edge-nodes\n- makeRoot, access, splay, findRoot\n- Path aggregates and edge-weight updates\n\n## Code Example\n```javascript\n// Skeleton: Edge-nodes with Link-Cut Tree operations (makeRoot, access, link, cut, pathSum)\n```\n\n## Follow-up Questions\n- How would you extend to support dynamic connectivity checks under concurrent updates?\n- What are memory considerations for graphs with billions of edges?",
        "difficulty": "advanced"
      },
      {
        "id": "data-structures-q-17",
        "questionId": "q-20952",
        "question": "Design a dynamic interval map: maintain a set of disjoint intervals [l, r] each with a weight w. Support insertInterval(l, r, w) that adds the range, merging overlaps by splitting existing intervals so the final structure remains disjoint; eraseInterval(id); updateWeight(id, w). Queries: sumWeightsInRange(a, b) and kthLargestWeightInRange(a, b, k). Strive for amortized O(log n) updates and O(log n) queries using a treap of endpoints and augmented weights?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a treap keyed by interval start. Each node stores [l, r, w, id] along with augmented data: subtreeWeightSum. Maintain a map from id to node for O(1) lookups. insertInterval(l, r, w) splits existing intervals at boun…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose an implicit-treap rope. Each node stores a chunk, its length, and a 64-bit rolling hash; maintain subtree length and hash. Split/merge by position for edits; substringHash(l,r) aggregated from",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a 2D segment tree over left and right indices. Each node stores: a weight-sum and an order-statistics structure (weighted multiset) over edge weights to answer kthLargest via binary search on a co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a singly linked-node approach where each node stores value and a pointer to the previous top. Each version stores a pointer to its top node. push creates a new node pointing to the old top; pop re",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design a dynamic, overlap-aware interval map with efficient range queries and top-k queries within a range. It mirrors real-world workloads like geo-fencing, ad targeting, or resource allocation systems where intervals represent regions, audiences, or allocated resources.\n\n## Key Concepts\n- Disjoint interval maintenance using a treap or balanced BST\n- Overlap splitting and merging to preserve the disjoint invariant\n- Range aggregation with subtree sums for efficient querying\n- Order-statistics handling for top-k queries within a range\n\n## Code Example\n```javascript\nclass Node {\n  constructor(l, r, w, id) {\n    this.l = l;\n    this.r = r;\n    this.w = w;\n    this.id = id;\n    this.prior = Math.random();\n    this.left = null;\n    this.right = null;\n    this.subtreeWeightSum = w;\n  }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "data-structures-q-18",
        "questionId": "q-7190",
        "question": "Design a dynamic multiset of integers that supports insert(x), erase(x) (duplicates allowed), and findMedian() after each operation in O(log n). Propose a solution using two heaps with lazy deletion, explain how to lazily delete elements, how to rebalance on insert/erase, and how median is computed for even/odd sizes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use two heaps: a max-heap L for the lower half and a min-heap R for the upper half. Maintain |L| >= |R| and |L| - |R| <= 1. For erase(x), mark x as deleted in a hashmap and lazily remove when it reach",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use coordinate compression on all endpoints and a Fenwick tree to support range-add / point-query. addInterval(l,r): bit.add(idx(l), +1); bit.add(idx(r+1), -1); removeInterval(l,r): bit.add(idx(l), -1",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Trie plus a hashmap of word->count. addWord(word, delta) updates the word’s count in the map and increments counts along the word’s path in the Trie. queryTopK(prefix, k) locates the prefix node",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a segment tree over compressed coordinates. Each node maintains the total weight of active intervals covering that node and an order-statistics structure of their weights. On insertInterval(l,r,w,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical median maintenance on a dynamic multiset with duplicates, using a robust technique (two heaps + lazy deletion).\n\n## Key Concepts\n\n- Median maintenance with two heaps\n- Lazy deletion via a hashmap\n- Invariant balancing between heaps\n- Handling duplicates consistently\n\n## Code Example\n\n```javascript\nclass MedianMultiset {\n  constructor() {\n    this.low = new MaxHeap(); // max-heap for lower half\n    this.high = new MinHeap(); // min-heap for upper half\n    this.delayed = new Map(); // value -> count to delete lazily\n    this.sizeLow = 0;\n    this.sizeHigh = 0;\n  }\n  // insert, erase, pruneTop, balance, and findMedian implementations omitted for brevity\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt for batch deletions or streaming inputs?\n- How would you extend to support getKthSmallest(k) with duplicates?",
        "difficulty": "beginner"
      },
      {
        "id": "data-structures-q-19",
        "questionId": "q-11110",
        "question": "Design a dynamic sequence data structure that supports insertAt(index, value), eraseAt(index), and rangeGCD(l, r) with 1-based indices and duplicates allowed. Achieve amortized O(log n) per operation. Explain the data structure (e.g., implicit treap or rope) and how gcd is maintained under split/merge, including edge cases and how you handle non-overlapping gcd reuse?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an order-statistics tree keyed by (dur, id) plus a FIFO queue of events to support expirations and duplicates. Maintain a running sum of durations in the window. On insert(ts,dur): push (ts,dur,id",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a rollback-capable DSU and a segment tree over the timeline. Map each edge’s active intervals by pairing add and subsequent remove; for edges never removed, use [tAdd, Q]. Build a segment tree whe",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "An implicit treap with each node storing val, size, and gcd of its subtree. Maintain gcd = gcd(left.gcd, val, right.gcd). For insertAt(i, v): split at i-1, merge(left, newNode(v), right). For eraseAt(",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a Link-Cut Tree. Represent each edge as an edge-node with weight w and connect to its endpoints. Each splay node stores the max weight on its path. link(u,v,w): create edge-node e, link to u and v",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests knowledge of advanced balanced BST variants and in-place sequence augmentation. It also checks correctness of gcd propagation through splits and merges and edge handling for empty or single-element ranges.\n\n## Key Concepts\n\n- Implicit indexing via size augmentation\n- Subtree gcd maintenance and gcd associative property\n- Safe split/merge to isolate segments\n- Handling duplicates and non-contiguous edits efficiently\n\n## Code Example\n\n```javascript\nclass Node { constructor(val){ this.val=val; this.prio=Math.random(); this.l=null; this.r=null; this.sz=1; this.gcd=val; } }\nfunction sz(n){return n? n.sz:0}\nfunction gcd(a,b){ while(b){ let t=a%b; a=b; b=t;} return Math.abs(a); }\nfunction upd(n){ if(!n) return; n.sz = 1 + sz(n.l) + sz(n.r); const gl = n.l? n.l.gcd:0; const gr = n.r? n.r.gcd:0; n.gcd = gcd(gcd(gl, n.val), gr); }\nfunction split(n, k){ if(!n) return [null, null]; if(sz(n.l) >= k){ const [a,b] = split(n.l, k); n.l=b; upd(n); return [a, n]; } else { const [a,b] = split(n.r, k - sz(n.l) -1); n.r=a; upd(n); return [n, b]; } }\nfunction merge(a,b){ if(!a||!b) return a||b; if(a.prio < b.prio){ a.r = merge(a.r, b); upd(a); return a; } else { b.l = merge(a, b.l); upd(b); return b; } }\n// Insert, erase, rangeGCD usage explained in prose\n```\n\n## Follow-up Questions\n\n- How would you support rangeGCD in a persistent version? \n- How does performance change with degenerate priorities?",
        "difficulty": "advanced"
      },
      {
        "id": "data-structures-q-20",
        "questionId": "q-25189",
        "question": "Design a dynamic multiset to back a live leaderboard: implement insertScore(playerId, score), eraseScore(playerId, score) (duplicates allowed per user over time), and getRank(score) which returns how many scores are <= score across all players. Use a treap augmented with subtree sizes; explain how duplicates are distinguished by playerId and how getRank works. How would you implement this and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a Link-Cut Tree (Sleator–Tarjan). Represent edges as nodes carrying the weight; maintain path-sum as a segment-aggregate on the preferred paths. For pathKth, maintain an order-statistics struc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a dynamic forest using a Link-Cut Tree where each edge is represented as an edge-node connected to its two endpoints. link inserts an edge-node, cut removes it, updateWeight updates the edge-n",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Treap keyed by (score, playerId) so each entry is unique. Each node stores its subtree size; insert/erase adjust pointers and sizes. getRank(score) traverses: if node.key.score <= score, count l",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use an ordered set of disjoint intervals keyed by start. Maintain totalCovered as the sum of (r-l+1) over the intervals. For addInterval, locate the first overlapping interval, merge all overlaps into",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical use of an augmented BST (treap) to support ranking with duplicates in a live leaderboard setting.\n\n## Key Concepts\n- Treap with random priorities for balancing\n- Node augmentation: subtree size for rank queries\n- Key design: (score, playerId) to distinguish duplicates\n- Rank query logic traversing by score threshold\n- Handling time-evolving duplicates safely\n\n## Code Example\n```javascript\nclass LeaderboardTreap {\n  constructor() { this.root = null; }\n  _cmp(a, b) {\n    if (a.score !== b.score) return a.score - b.score;\n    return a.playerId.localeCompare(b.playerId);\n  }\n  _size(n) { return n ? n.size : 0; }\n  _update(n) { if (n) n.size = 1 + this._size(n.left) + this._size(n.right); }\n  _rotateLeft(p) {\n    const r = p.right; p.right = r.left; r.left = p; this._update(p); this._update(r); return r;\n  }\n  _rotateRight(p) {\n    const l = p.left; p.left = l.right; l.right = p; this._update(p); this._update(l); return l;\n  }\n  insertScore(playerId, score) {\n    const key = { playerId, score, __prio: Math.random() };\n    this.root = this._insert(this.root, key);\n  }\n  _insert(n, key) {\n    if (!n) return { key, priority: key.__prio, left: null, right: null, size: 1 };\n    if (this._cmp(key, n.key) < 0) {\n      n.left = this._insert(n.left, key);\n      if (n.left.priority > n.priority) n = this._rotateRight(n);\n    } else {\n      n.right = this._insert(n.right, key);\n      if (n.right.priority > n.priority) n = this._rotateLeft(n);\n    }\n    this._update(n); return n;\n  }\n  eraseScore(playerId, score) {\n    const key = { playerId, score };\n    this.root = this._erase(this.root, key);\n  }\n  _erase(n, key) {\n    if (!n) return null;\n    const c = this._cmp(key, n.key);\n    if (c < 0) { n.left = this._erase(n.left, key); }\n    else if (c > 0) { n.right = this._erase(n.right, key); }\n    else {\n      if (!n.left) return n.right;\n      if (!n.right) return n.left;\n      if (n.left.priority > n.right.priority) { n = this._rotateRight(n); n.right = this._erase(n.right, key); }\n      else { n = this._rotateLeft(n); n.left = this._erase(n.left, key); }\n    }\n    this._update(n); return n;\n  }\n  getRank(score) {\n    const dfs = (n) => {\n      if (!n) return 0;\n      if (n.key.score <= score) return this._size(n.left) + 1 + dfs(n.right);\n      return dfs(n.left);\n    };\n    return dfs(this.root);\n  }\n}\n```\n\n## Follow-up Questions\n- How would you adapt this to support getKRank(score, k) for the top-k ranks?\n- What are trade-offs compared to using a Fenwick tree with coordinate compression for fixed score ranges?",
        "difficulty": "beginner"
      },
      {
        "id": "data-structures-q-21",
        "questionId": "q-28592",
        "question": "Design a dynamic array of integers that supports insertAt(index, value), eraseAt(index), and rangeSum(l, r) using sqrt decomposition. Explain how you partition into fixed-size blocks, maintain per-block sums, and rebalance after edits when a block overflows (split) or underflows (merge). Provide a concrete example with initial [3,7,2,9], then insertAt(2,5), eraseAt(3), and rangeSum(1,4)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a hash map key→count and recompute top-k on demand by sorting entries in descending order (tie-break by key). On add, adjust the counter and delete if zero. getTopK(k) runs in O(n log n). This kee",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use sqrt decomposition with blocks of approximately √n elements each. Each block stores its elements and maintains a blockSum for O(1) range queries. For insertAt/eraseAt, update only the affected block and its sum. If…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "A Trie with per-node counters. On insertWord, traverse/extend the path incrementing prefixCount at each node and endCount at the final node. On eraseWord, ensure endCount>0, then decrement counts alon",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I propose a 3D range tree with a segment tree on the x-dimension, where each node maintains a nested 2D range tree on (y,z) coordinates that stores a weight multiset. First compress weights to discrete indices; in each…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design a practical data structure and reason about block balancing in edit-heavy scenarios.\n\n## Key Concepts\n- **Sqrt decomposition**: Partition data into fixed-size blocks for efficient range queries\n- **Per-block sums**: Enable O(1) block-level aggregation during rangeSum operations\n- **Amortized balance**: Split/merge operations maintain block sizes within bounds, ensuring O(√n) amortized time per update\n- **Edge-case handling**: Proper merge/split logic prevents pathological degradation\n\n## Code Example\n```javascript\nclass SqrtArray {\n  constructor() {\n    this.blocks = [];\n    this.B = 2;\n  }\n\n  _getBlock(index) {\n    let count = 0;\n    for (const block of this.blocks) {\n      if (index < count + block.length) return block;\n      count += block.length;\n    }\n    throw new Error('Index out of bounds');\n  }\n\n  insertAt(index, value) {\n    const block = this._getBlock(index);\n    block.push(value);\n    block.sum += value;\n    if (block.length > 2 * this.B) this._split(block);\n  }\n\n  eraseAt(index) {\n    const block = this._getBlock(index);\n    block.splice(index, 1);\n    block.sum -= value;\n    if (block.length < this.B / 2) this._merge(block);\n  }\n\n  rangeSum(l, r) {\n    let sum = 0;\n    let count = 0;\n    for (const block of this.blocks) {\n      const blockStart = count;\n      const blockEnd = count + block.length - 1;\n      if (r < blockStart || l > blockEnd) { count += block.length; continue; }\n      const start = Math.max(l, blockStart);\n      const end = Math.min(r, blockEnd);\n      if (start === blockStart && end === blockEnd) sum += block.sum;\n      else for (let i = start; i <= end; i++) sum += block[i];\n      count += block.length;\n    }\n    return sum;\n  }\n\n  _split(block) {\n    const mid = Math.floor(block.length / 2);\n    const newBlock = { arr: block.arr.splice(mid), sum: newBlock.arr.reduce((a,b)=>a+b,0) };\n    block.sum = block.arr.reduce((a,b)=>a+b,0);\n    const idx = this.blocks.indexOf(block);\n    this.blocks.splice(idx + 1, 0, newBlock);\n  }\n\n  _merge(block) {\n    const idx = this.blocks.indexOf(block);\n    const neighbor = idx > 0 ? this.blocks[idx - 1] : this.blocks[idx + 1];\n    const [small, big] = neighbor.length > block.length ? [block, neighbor] : [neighbor, block];\n    big.arr.push(...small.arr);\n    big.sum += small.sum;\n    this.blocks.splice(this.blocks.indexOf(small), 1);\n  }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "data-structures-q-22",
        "questionId": "q-16019",
        "question": "Design a dynamic rope data structure for a mutable string supporting insert(pos, s), delete(pos, len), and substringHash(l, r) in O(log n) time. Use an implicit treap where leaves store string chunks; each node tracks subtree length and a rolling hash. Explain split/merge, hash maintenance, and collision handling, plus how to answer substringHash efficiently?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed data structure: a segment tree on compressed coordinates; each node stores a multiset of (weight, id) for intervals assigned to that node. Add [l,r,w,id] by visiting O(log N) nodes and insert",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use an implicit treap with chunked leaves. Each node stores len(subtree) and two rolling hashes (mod p1, p2) from children and its chunk. Split by position, possibly splitting a leaf chunk; merge upda",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a balanced BST (e.g., treap or AVL tree) where each node stores the value, count (for duplicates), and subtreeSize. Insert(x): locate the node for x—if found, increment its count; otherwise create a new node. Update…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a two-heap design (lower half in a max-heap, upper half in a min-heap) with a hashmap for lazy deletions to support erase(x) (one occurrence). Balance so the sizes differ by at most 1; prune l",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nRope data structures enable fast edits and substring queries in large texts, a core need in code editors and high-throughput log systems. This tests balancing, augmentation, and hash-based queries under dynamic edits.\n\n## Key Concepts\n- Implicit treap with chunked leaves for memory-local operations\n- Dual rolling hash (two moduli) with precomputed powers\n- Node augmentation: subtree length and two hashes\n- Split/merge correctness when crossing chunk boundaries\n- Collision handling via dual hashes and rehash checks\n\n## Code Example\n```javascript\nclass Node {\n  constructor(chunk) {\n    this.chunk = chunk; // string\n    this.left = null;\n    this.right = null;\n    this.prio = Math.random();\n    this.len = chunk.length; // subtree length will be updated\n    this.h1 = hash(chunk, MOD1);\n    this.h2 = hash(chunk, MOD2);\n  }\n}\nfunction split(root, pos) { /* splits at pos; may cut a chunk */ }\nfunction merge(a, b) { /* standard treap merge updating len/hashes */ }\n```\n\n## Follow-up Questions\n- How would you add persistence, so old versions remain accessible? \n- How to optimize for very large chunks to minimize splits? \n- How to extend to Unicode and memory pool allocators?",
        "difficulty": "advanced"
      },
      {
        "id": "data-structures-q-23",
        "questionId": "q-8061",
        "question": "You’re building an autocomplete feature for a messaging app. Design a dynamic dictionary that supports insertWord(word), deleteWord(word) (duplicates allowed), and queryPrefix(prefix) returning the total number of inserted words starting with that prefix. Ensure deletions decrease counts and duplicates are counted. Describe the data structure, time complexity, and how you handle edge cases?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a segment-tree-over-time where each node stores an ordered multiset of weights. Maintain id -> (t, w). insertEvent updates O(log N) nodes; eraseEvent removes w from those nodes. sumWeightsInWindow",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a time-segment tree over operation times. Each insertLine creates an active interval [start, end). Store (m,b,id) in every node covering its interval. Each node maintains a Li Chao tree for its li",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a treap keyed by interval start. Each node stores [l, r, w, id] along with augmented data: subtreeWeightSum. Maintain a map from id to node for O(1) lookups. insertInterval(l, r, w) splits existing intervals at boun…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Trie with two counters per node: subCount (how many words pass through) and endCount (how many times a word ends here). insertWord increments subCount on each traversed node and increases endCou",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAutocomplete is common in apps; tests the ability to manage dynamic counts with duplicates and prefix queries, a practical real-world pattern.\n\n## Key Concepts\n- Trie with per-node counts\n- Duplicate handling via endCount and node subCount\n- Efficient prefix queries in O(length(prefix))\n\n## Code Example\n```javascript\nclass TrieNode { constructor(){ this.next=new Map(); this.subCount=0; this.endCount=0; } }\n```\n\n## Follow-up Questions\n- How would you support listing top k prefixes by frequency?\n- How would you handle memory constraints with large vocabularies?",
        "difficulty": "beginner"
      },
      {
        "id": "data-structures-q-24",
        "questionId": "q-28523",
        "question": "Design a dynamic command palette data structure that supports insert(cmd), erase(cmd) (one occurrence), and autocomplete(prefix, limit) returning up to limit commands starting with prefix in lexicographic order. Duplicates allowed. Implement with a Trie, justify endCount/subCount, and outline time complexities?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Trie where each node maintains two counters: a pass count (how many words pass through this node) and an end count (how many words end at this node). **insertWord(word)** traverses or creates nodes along the path,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a segment tree over compressed coordinates; each node holds a multiset of weights for intervals that fully cover the node’s segment and a running sum. Insertion/erasure/update touches O(log N) n",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a Trie-based dynamic multiset of strings. Each node tracks children, endCount (duplicate endings) and subCount (strings in subtree). insert/erase update counts along the path. autocomplete(prefix,",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "A coordinate-segment-tree with per-node multisets. Compress endpoints, build a segment tree over gaps. For insertInterval(id,L,R,w), push w into every node fully covered by [L,R] and update a node tot",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a compact, practical data structure for command autocompletion with duplicates and deletions.\n\n## Key Concepts\n- Trie with multiplicity\n- subtree counts for pruning\n- Prefix search and bounded DFS\n\n## Code Example\n```javascript\n// minimal sketch\n```\n\n## Follow-up Questions\n- How would you handle memory limits for large vocabularies?\n- How would you extend to case-insensitive prefixes?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-07T06:08:58.651Z",
    "version": 1
  }
]