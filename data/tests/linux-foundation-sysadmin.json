[
  {
    "id": "test-linux-foundation-sysadmin",
    "channelId": "linux-foundation-sysadmin",
    "channelName": "Linux Foundation Sysadmin",
    "title": "Linux Foundation Sysadmin Knowledge Test",
    "description": "Test your understanding of Linux Foundation Sysadmin with 25 questions.",
    "questions": [
      {
        "id": "linux-foundation-sysadmin-q-0",
        "questionId": "q-14997",
        "question": "On a multi-tenant Linux host handling a high-throughput log ingestion pipeline writing to a shared journal, tail latency spikes occur under bursty writes. Design a production plan to guarantee per-tenant I/O isolation while preserving throughput. Specify storage topology (NVMe pools or separate namespaces), I/O scheduler and per-tenant throttling, per-tenant cgroups (blkio/mq) with quotas, barrier/flush tuning, and a test plan using fio patterns and a safe rollback path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Configure tc with HTB: set a root qdisc on eth0, create 4 classes (1:10–1:40) each with a rate of 200 Mbps. Attach fq_codel to each class for low-latency packet scheduling. Use net_cls cgroup-based tc filters to map ten…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "To reduce overhead, isolate policies per-tenant with a safe whitelist of syscalls, enable a cached BPF-based policy with a per-tenant map, prewarm at startup, and roll out gradually. Instrument with e",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Isolate I/O per tenant using dedicated NVMe namespaces or logical volumes, apply per-tenant blkio/cgroup v2 I/O throttling and weights, and configure an I/O scheduler with per-tenant weighted fairness (BFQ or mq-deadlin…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Diagnose using BPF programs tracing kmem:kmalloc and kmem:mm_page_alloc_extended_zone failures, combined with analysis of /proc/slabinfo and /proc/buddyinfo for fragmentation metrics. Apply SLUB tuning parameters: set s…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses understanding of I/O isolation in multi-tenant log ingestion environments, focusing on storage architecture, I/O schedulers, cgroups, and journal tuning to ensure fair resource distribution under bursty workloads.\n\n## Key Concepts\n- I/O isolation in multi-tenant environments\n- NVMe namespaces and logical volume partitioning\n- BFQ and mq-deadline I/O schedulers\n- cgroups v2 blkio controller for per-tenant quotas\n- Journal barrier and flush tuning for write durability\n\n## Production Architecture\n\n### Storage Topology\n- **Dedicated NVMe Namespaces**: Assign each tenant a separate NVMe namespace or partition for complete physical isolation\n- **Logical Volumes (LVM)**: Use LVM to create per-tenant thin-provisioned volumes with guaranteed I/O limits\n- **Separate Journal Devices**: Isolate journal writes to dedicated NVMe devices or namespaces to prevent journal contention\n\n### I/O Scheduler Configuration\n- **BFQ (Budget Fair Queuing)**: Best for latency-sensitive workloads; provides per-entity weighted fairness\n- **mq-deadline**: Simpler alternative with lower overhead; configure read/write batch sizes appropriately\n\n### Per-Tenant Cgroup Configuration\n```bash\n# Create per-tenant cgroups\nmkdir -p /sys/fs/cgroup/tenant-{1..N}\n\n# Set I/O throttling (bytes per second)\necho 100000000 > /sys/fs/cgroup/tenant-1/io.max\n\n# Set I/O weight (cgroup v2)\necho 100 > /sys/fs/cgroup/tenant-1/io.weight\n\n# Add tenant processes to cgroup\necho $(cat /proc/$(pgrep -f tenant1)/pid) > /sys/fs/cgroup/tenant-1/cgroup.procs\n```\n\n### Barrier and Flush Tuning\n- **Disable barriers** for battery-backed write cache (BBU) or disable for non-critical tenants\n- **Tune flush intervals** based on durability requirements (e.g., `echo 500 > /proc/sys/vm/dirty_writeback_centisecs`)\n- **Use fdatasync** instead of fsync for lower latency with acceptable durability\n\n## Test Plan\n\n### fio Patterns for Validation\n```bash\n# Burst write test per tenant\nfio --name=tenant1_burst --rw=write --bs=4k --iodepth=32 --numjobs=1 --direct=1 --filename=/dev/nvme0n1\n\n# Latency percentile test\nfio --name=latency_test --rw=write --bs=4k --iodepth=1 --numjobs=1 --direct=1 --runtime=60 --time_based --percentile=99\n\n# Mixed workload simulation\nfio --name=mixed_workload --rw=randwrite --bs=4k --iodepth=16 --numjobs=4 --direct=1\n```\n\n### Validation Metrics\n- p99 tail latency < 10ms under burst loads\n- No tenant experiences > 20% latency degradation during neighbor's peak\n- Journal write latency remains stable regardless of tenant I/O patterns\n\n## Rollback Path\n1. Revert cgroup settings to default (unlimited)\n2. Restore original I/O scheduler (`echo mq-deadline > /sys/block/nvme0n1/queue/scheduler`)\n3. Remount filesystems with original barrier settings\n4. Revert LVM volume allocations to shared pool\n\n## Key Takeaways\n1. **Physical isolation** via NVMe namespaces provides the strongest tenant separation\n2. **cgroup v2 blkio** enables fine-grained throttling without kernel modifications\n3. **BFQ scheduler** prioritizes latency-sensitive tenants during contention\n4. **Journal isolation** prevents noisy neighbor problems from log append operations\n5. **Always test** under realistic burst conditions before production deployment",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-1",
        "questionId": "q-14678",
        "question": "On a Linux host running a multi-tenant networking stack with a custom kernel module for XDP-based shaping, a CVE forces a live patch with no reboot. Design a production plan to apply a safe live patch to the running kernel while preserving tenant QoS. Include: patch strategy (kpatch/livepatch), ABI compatibility checks, canary rollout, per-tenant isolation (cpuset/v2), IRQ affinity, observability (perf/pmu/ftrace), staged test and rollback, and validation metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a live kernel patch using kpatch or livepatch modules; verify ABI compatibility against the running kernel; execute a canary deployment on a spare host, migrate tenants using cpuset/v2 isolation with fixed IRQ…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Bound CPU for CI work using cgroup v2 and systemd. Create a dedicated CI.slice and move CI containers/services into it. Set cpu.max to 50% (period 100000) and restrict to a subset of CPUs with Allowed",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure logrotate with copytruncate (or a postrotate that signals the process to reopen logs) and rotate daily or at 100M, keeping 7 archives. Move logs to a dedicated partition; enable Restart=on-f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Isolate tenant UDP log ingress by routing flows to per-tenant cores via NIC RSS/XPS configuration, pin per-tenant listeners to dedicated CPU sets using cpuset cgroups, and enforce CPU and memory quotas with cgroups v2.…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical capability to patch running kernels in multi-tenant environments without downtime, balancing safety, observability, and rollback procedures.\n\n## Key Concepts\n- Live patching mechanisms (kpatch, livepatch)\n- ABI compatibility and module interface validation\n- Canary rollout and staged deployment strategies\n- Tenant isolation (cpuset/v2) and IRQ affinity management\n- Observability tools (perf, PMU, ftrace) and validation metrics\n\n## Code Example\n```bash\n# Build patch for current kernel\nmake -C /lib/modules/$(uname -r)/build M=$(pwd) modules\n# Load patch\nsudo insmod patch_module.ko\n```\n\n## Implementation Approach\nThe solution demonstrates production-grade live patching by combining proven kernel mechanisms with systematic validation. kpatch/livepatch provides the fundamental patching framework, while ABI compatibility checks ensure module interface stability. The canary deployment strategy minimizes risk by validating on isolated hosts before production rollout.\n\nTenant isolation through cpuset/v2 preserves service boundaries during the patching process, and fixed IRQ affinity maintains network performance characteristics. Comprehensive observability via perf, PMU, and ftrace enables real-time monitoring of system behavior and QoS metrics.\n\nThe staged rollback capability provides a safety net for unexpected issues, ensuring service continuity while maintaining strict tenant QoS guarantees throughout the patching lifecycle.",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-2",
        "questionId": "q-17660",
        "question": "On a Linux host running a multi-tenant data ingestion pipeline backed by NVMe storage and io_uring, tail latency spikes per tenant occur during burst windows. Design a production plan to isolate and guarantee per-tenant I/O latency without sacrificing throughput: specify per-tenant cgroup v2 I/O and CPU controls, NUMA-aware data path binding, per-tenant I/O thread affinity, kernel IO scheduler tuning (mq-deadline, blk-mq), observability (blktrace, iostat, perf), and a staged load-test with rollback. What exact steps would you take and how would you verify?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable AppArmor confinement for two services: nginx and a custom worker. Commands: sudo apt-get update && sudo apt-get install apparmor apparmor-utils; sudo systemctl enable apparmor; sudo aa-status;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create per-tenant cgroup v2 trees and assign all tenant processes there. Enforce io.max and cpu.max to cap bandwidth and CPU share; pin I/O workers and memory to dedicated NUMA nodes; use mq-deadline",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-process and system-wide FD hardening plan: set LimitNOFILE high on each unit, raise fs.file-max, enable systemd TasksMax, isolate services with dedicated cgroups, add a watchdog that restarts on q",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Develop a low-overhead eBPF tracing plan to profile container lifecycle events (namespace creation, cgroup setup, mounts, network namespace) during startup bursts. Tag events with per-tenant identifie",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to implement per-tenant I/O isolation in a realistic multi-tenant ingestion pipeline; requires concrete steps, stable policy changes, and rollback.\n\n## Key Concepts\n\n- cgroup v2 unified hierarchy and per-tenant resource controls (io.max, cpu.max)\n- NUMA-aware binding and mq-deadline/blk-mq tuning\n- per-tenant I/O queueing and io_uring worker affinity\n- observability: blktrace, iostat, perf, ftrace\n- staged load testing and safe rollback\n\n## Code Example\n\n```bash\n# Create tenant subtree and apply limits\nmkdir -p /sys/fs/cgroup/tenant-A\necho \"10485760\" > /sys/fs/cgroup/tenant-A/io.max\n# (example) apply CPU limit for a tenant\n```\n\n## Follow-up Questions\n\n- Trade-offs of strict bandwidth caps vs latency\n- How to migrate tenants with zero downtime",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-3",
        "questionId": "q-18508",
        "question": "On a Linux host running hundreds of systemd user services under cgroup v2, a few services intermittently spawn bursts of short‑lived processes, consuming CPU and I/O and causing global latency spikes. Propose a beginner‑friendly plan to diagnose and contain runaway processes without touching application code. Include per‑service CPU quotas, memory.max/memory.high, a lightweight watchdog, and a safe rollback/test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create per-tenant cgroup v2 trees and assign all tenant processes there. Enforce io.max and cpu.max to cap bandwidth and CPU share; pin I/O workers and memory to dedicated NUMA nodes; use mq-deadline",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a systemd unit with Type=forking, PIDFile, and ExecStartPre that checks and clears a stale lock: if /var/lib/backup/backup.lock exists and its PID is not running, delete it. Stop uses KillMode=con",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Identify offenders with systemd-cgtop and systemd-cgls to pinpoint which service.slice is hogging resources. Throttle by applying quotas on the offending unit.slice using systemctl set-property to enf",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Apply per-service memory controls and OOM protection. Set MemoryLimit=2G, MemorySwapMax=2G, OOMScoreAdjust=-100, and Restart=on-failure with a 5s backoff. Tune vm.swappiness=10 and ensure swap is enab",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of Linux resource isolation via cgroup v2 and systemd, as well as practical troubleshooting without app changes.\n\n## Key Concepts\n- systemd slices/scopes and cgroup v2 resource controls\n- cpu.max, memory.max, memory.high as throttling/guard rails\n- lightweight watchdog for runaway processes\n- safe rollback procedures and validation tests\n\n## Code Example\n```javascript\n# Identify offenders\nsystemd-cgtop -n 5\nsystemd-cgls --root\n\n# Apply quotas (example)\nsystemctl set-property offending.slice CPUMax=50% MemoryMax=1G\n```\n\n## Follow-up Questions\n- How would you handle multi-tenant fairness if bursts are sporadic across tenants?\n- What monitoring would you add to catch regressions after rollback?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-4",
        "questionId": "q-2223",
        "question": "On a Linux host running multiple CI jobs in containers, CPU contention causes builds to time out. Propose a production-ready plan to bound CPU for CI workloads using cgroup v2 and systemd slices, including concrete unit settings and a test plan with a CPU-bound load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Isolate tenants with per-tenant cpuset and memory.limit_in_bytes, enable eBPF tracepoints and perf/bpftrace to map tail latency to tenants, implement per-tenant tc/qos and XDP for fast paths, and run",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Begin by establishing a baseline using mpstat, pidstat, and perf to identify which cores are being throttled and which tenants are causing the highest CPU contention. Once you have this data, enforce per-tenant CPU quot…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a dedicated ci.slice with CPUQuota=50%, CPUQuotaPeriodSec=100ms, MemoryLimit=2G, and TasksMax=200. Use a service template ci-job@.service with Slice=ci.slice and per-job limits. Test by launchi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Bound CPU for CI work using cgroup v2 and systemd. Create a dedicated CI.slice and move CI containers/services into it. Set cpu.max to 50% (period 100000) and restrict to a subset of CPUs with Allowed",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical CPU isolation using cgroup v2 and systemd, not theory. It also checks how to validate under load, monitor, and adjust configurations for fairness.\n\n## Key Concepts\n\n- cgroup v2 CPU controller (cpu.max)\n- systemd Slice and resource controls (Slice=, CPUQuota, AllowedCPUs)\n- container isolation in CI workloads (Docker/K8s)\n- Observability (systemd-cgtop, pidstat, stress-ng)\n\n## Code Example\n\n```bash\n# Example setup (illustrative)\n# Create and configure a dedicated CI.slice with CPU limit\nsudo mkdir -p /sys/fs/cgroup/CI.slice\necho \"50000000 100000000\" | sudo tee /sys/fs/cgroup/CI.slice/cpu.max\n# Or via systemd properties for the slice\nsudo systemctl set-property CI.slice CPUQuota=50% CPUQuotaPeriodSec=1\n```\n\n## Follow-up Questions\n\n- How would you adapt for bursty CI workloads?\n- How do you monitor and auto-tune CPU limits as workloads evolve?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-5",
        "questionId": "q-15654",
        "question": "On a Linux host running a multi-tenant data pipeline with SELinux enforcing, tail latency spikes 50–200 ms during bursts due to policy checks. Design a production plan to diagnose and reduce SELinux overhead without compromising security. Include policy granularity changes (types/domains), per-tenant labeling, audit2allow-driven refinements, decision caching (libselinux), and a staged load-test with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Baseline AVC audit collection (ausearch) to identify hot checks; instrument with perf/ftrace to quantify policy overhead; refactor by introducing per-tenant domains/types, label data paths with tenant",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-tenant I/O quotas with cgroup v2 io.max, add per-tenant tc shaping and optional IO-priority, pin tenants to dedicated CPUs and NUMA nodes, and tune blk-mq and the IO scheduler (bfq/kyber",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Diagnose contention points using perf and bpftrace. Implement per-tenant tc queues with HTB for bandwidth allocation and fq_codel for flow pacing. Redirect packets to per-tenant receive rings via XDP_REDIRECT, leveragin…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant L3 cache partitions with CAT, enable MBM to cap per-tenant bandwidth, pin workers to NUMA-affinitized CPUs, set IRQ affinity, measure with perf, pmu, turbostat, and mem bandwidth;",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nSecurity policy performance is a real friction point in multi-tenant Linux systems. This question probes how you balance strict containment with low overhead.\n\n## Key Concepts\n- SELinux policy granularity and domain modeling\n- Auditing and iterative policy refinement\n- Per-tenant labeling and isolation\n- Policy decision caching and runtime observability\n- Safe staging and rollback\n\n## Code Example\n```javascript\n// Suggested commands (not run here):\n// 1) collect avc: ausearch -m avc -ts today\n// 2) generate module: sepolicy generate tenant_t\n// 3) load module: semodule -i tenant_t.pp\n```\n\n## Follow-up Questions\n- How would you verify there is no security regression after policy changes?\n- What monitoring would you keep long-term to detect policy overhead drift?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-6",
        "questionId": "q-22867",
        "question": "On a Linux host serving a large, multi-tenant CI/workload farm, bursts trigger thermal throttling and CPU frequency scaling that inflate tail latency. Propose a production plan to guarantee performance isolation and stable latency. Include per-tenant CPU/memory isolation (cpuset/v2, memory.max), NUMA binding, CPU frequency policy (schedutil with min/max and no_turbo), IRQ affinity, thermal management (thermald), observability, and a staged load-test with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pin tenants to dedicated cpusets and memory pools (cpuset v2, memory.max). Bind workloads to a NUMA node, cap CPU frequency with schedutil min/max and disable turbo to prevent spikes. Bind IRQs away f",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Diagnose ephemeral-port exhaustion during bursts by tracing SSH connections per tenant (ss -tanp, lsof). Check and tune kernel defaults: net.ipv4.ip_local_port_range, net.core.somaxconn, net.ipv4.tcp_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant CPU limits with systemd slices. For each container, create tenantX.slice and apply CPUQuota=20000 CPUQuotaPeriodUS=100000 to cap at 20% per CPU. Add a monitor that watches systemd",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "IOMMU groups per tenant; bind each tenant to dedicated VFIO PCI device(s). Pin DMA and memory to tenant NUMA nodes. Use MSI-X steering with per-tenant smp_affinity. Enforce per-tenant cgroups (io/memo",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical mastery of Linux resource isolation, CPU frequency behavior, IRQ affinity, and thermal management under bursty multi-tenant workloads.\n\n## Key Concepts\n- cpuset/v2 isolation and memory.max\n- NUMA binding and memory placement\n- CPU frequency policy (schedutil) and turbo control\n- IRQ affinity and interrupt steering\n- Thermal management (thermald) and sensors\n- Observability (turbostat, pidstat, perf)\n- Staged load-testing and rollback\n\n## Code Example\n```javascript\n# Example: create per-tenant cpuset and limit memory (illustrative)\nmkdir -p /sys/fs/cgroup/cpuset/tenantA\necho 0-3 > /sys/fs/cgroup/cpuset/tenantA/cpuset.cpus\necho 0 > /sys/fs/cgroup/cpuset/tenantA/cpuset.mems\n```\n\n## Follow-up Questions\n- How would you validate tail-latency improvement post-change?\n- What metrics would trigger a rollback and how would you execute it?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-7",
        "questionId": "q-20859",
        "question": "On a Linux host running multiple tenants via containers, bursts of CPU-bound work from one tenant cause latency spikes for others. Design a beginner-friendly plan to enforce per-tenant CPU quotas using cgroups v2 and systemd slices, without modifying applications. Include how to map tenants to cgroups, set CPU.max, baseline tests (latency under load), monitoring, and rollback steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: isolate tenants with cpuset on dedicated CPUs, enforce per-tenant IO limits via cgroup v2 io.max on the NVMe device, assign a separate io_uring context per tenant, lock required buffers with mem",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose per-tenant v2 cgroups under /sys/fs/cgroup, map each tenant to a dedicated slice (tenantA.slice), and set CPU.max so total across tenants uses host capacity (e.g., 1-2 CPUs on a 4-core host).",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Enable the cpu controller, create per-tenant slices (e.g., container-tenantA.slice). Set cpu.max values (e.g., 250000 for 25% of a CPU). Validate with a controlled load (stress-ng) and monitor latency",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan includes: identify hot paths with eBPF tracepoints and bpftool; enforce per-tenant QoS with tc and net_cls labeling and per-tenant socket buffers; pin IRQs and bind NIC RSS to CPU cores; enable m",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical knowledge of CPU isolation in Linux with cgroups v2 and systemd slices, plus light monitoring and rollback.\n\n## Key Concepts\n- cgroups v2 and CPU.max/CPUQuota\n- systemd.slice, container runtimes, non-invasive isolation\n- baseline benchmarking and rollback strategy\n\n## Code Example\n```bash\n# per-tenant isolation example\nsudo mkdir -p /sys/fs/cgroup/tenantA.slice\necho \"50%\" > /sys/fs/cgroup/tenantA.slice/cpu.max\nsystemd-run --slice=tenantA.slice --unit=tenantA-test /usr/bin/stress --cpu 1 --timeout 60\n```\n\n## Follow-up Questions\n- How would you scale to dozens of tenants?\n- How would you verify no regressions for high-priority tasks?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-8",
        "questionId": "q-4919",
        "question": "On a Linux host running a stateful daemon that handles financial transaction streams, design a zero-downtime in-place upgrade plan for a new binary and on-disk format change. The system uses systemd, a local WAL, and in-memory indices; describe upgrade steps, migration strategy, feature flags, testing plan, and rollback criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a rolling, in-place upgrade using two binaries and feature flags. Use systemd with a canary path: gradually shift traffic to v2 while the old process migrates its on-disk format via an atomi",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: IEEE 1588v2 with hardware timestamping. Use PHC on NICs, map to /dev/ptp0, run linuxptp ptp4l as a boundary clock with a robust grandmaster, and chronyd in PT mode for precise discipline. Prefer",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement strict isolation: run each tenant in a per-tenant user namespace with rootless containers, drop all capabilities, enforce a seccomp-bpf filter and AppArmor/SELinux profile, and place each te",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "**Question:** On a Linux host with multiple backup jobs triggered by systemd timers running concurrently, one backup occasionally stalls during bursts. Design a beginner-friendly plan to diagnose whether systemd cgroups…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess the ability to design safe, production-grade upgrades for stateful services with strict uptime requirements.\n\n## Key Concepts\n\n- Zero-downtime, in-place upgrade patterns\n- WAL-driven on-disk format migration\n- Feature flags and canary rollout strategy\n- Rollback criteria, data integrity checks, and observability\n\n## Code Example\n\n```bash\n# Pseudocode for upgrade flow\n# 1) Build v2\n# 2) Prepare migration (WAL replay, schema changes)\n# 3) Swap binaries behind a feature flag\n# 4) Incrementally route traffic to v2\n# 5) Validate metrics; if OK, complete rollout\n```\n\n## Follow-up Questions\n\n- How would you guarantee no active connections are dropped during the swap?\n- What metrics and thresholds signal a safe rollback, and how would you automate it?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-9",
        "questionId": "q-20835",
        "question": "On a Linux host hosting a distributed multi-tenant web service using a Redis cluster as a shared in-memory cache, hot keys cause cross-node memory pressure and tail latency spikes during bursts. Design a practical plan to diagnose and mitigate cache contention while preserving data consistency and throughput. Include per-tenant cache partitioning, NUMA binding, memory cgroups, Redis clustering, and staged test/rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On the host, ensure chrony is syncing with low drift (chronyc tracking; sources -v). Prohibit NTP daemons inside containers; containers share host time. Verify with 'date' in 5 tenants during bursts.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Isolate NIC IRQs to dedicated CPU cores away from tenant workloads using IRQ affinity masks. Enable multiqueue NIC support and configure RPS (Receive Packet Steering) and XPS (Transmit Packet Steering) to distribute RX/…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Start with per-tenant cache segmentation (namespace keys or Redis hash tags) and pin Redis processes to NUMA nodes; enable memory cgroups v2 to cap per-tenant RSS. Use perf/pmu to trace cache misses a",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a production-grade live kernel patching strategy using kpatch to maintain zero uptime. Validate patch compatibility against the active kernel configuration and ABI, build the patch module targeting the exact r…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests advanced Linux sysadmin skills in real-world multi-tenant caching: NUMA-aware memory pressure, per-tenant isolation, and production testing with safe rollback.\n\n## Key Concepts\n\n- NUMA binding and memory policy\n- Per-tenant isolation and quotas\n- Cache contention and eviction strategies\n- Redis clustering and shard placement\n- Observability with perf/pmu/pidstat\n\n## Code Example\n\n```bash\n# Example: pin Redis to NUMA node 0 and limit per-tenant memory\nnumactl --cpunodebind=0 --membind=0 redis-server --port 6379 --cluster-enabled yes\n```\n\n## Follow-up Questions\n\n- How would you measure per-tenant eviction latency under bursts?\n- How would you adapt if a tenant’s data must stay in L1 cache for SLA?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-10",
        "questionId": "q-26612",
        "question": "On a Linux host running multiple tenants in containers, memory pressure triggers swapping that degrades all tenants during CI bursts. Design a beginner-friendly plan to diagnose and mitigate swap thrash without changing tenant apps. Include: per-tenant memory caps via cgroups v2, a simple swap-aware monitoring setup (vmstat, smem, free), a rollback path, and a minimal test plan using a synthetic workload?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Profile per-tenant memory with smem/top and vmstat; create per-tenant cgroups v2 and set memory.max; use memory.high for bursts; lower vm.swappiness to 10-20; run a synthetic memory load within quotas",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use per-tenant GPU isolation (MIG or per-GPU slices), CPU pinning, and container runtimes with strict cgroups v2 quotas. Run GPUs through MPS to reduce cross-tenant queueing. Instrument with eBPF trac",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Choose a patching method (kpatch or KSPP-based) and limit live patch scope to the running kernel. Validate NVIDIA driver compatibility in a test build and module signing. Run a staged rollout with a h",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant I/O ceilings using cgroup v2 `io.max` to enforce bandwidth and IOPS limits, paired with a custom eBPF throttle for tenant-tagged I/O at the block layer. Track IOPS, latencies, and queue depths using…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical memory isolation for multi-tenant Linux hosts and a beginner-friendly path to reduce swap thrash without touching application code.\n\n## Key Concepts\n- cgroups v2 memory controller\n- vm.swappiness tuning\n- per-tenant isolation\n- swap monitoring\n- safe rollback\n\n## Code Example\n```bash\n# Example: per-tenant memory cap (1G) in cgroup v2\nsudo mkdir -p /sys/fs/cgroup/tenants/tenantA\necho $((1<<30)) | sudo tee /sys/fs/cgroup/tenants/tenantA/memory.max\n```\n\n## Follow-up Questions\n- How would you verify that quotas are respected under a bursty load?\n- What signals indicate failure during rollback and how would you recover?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-11",
        "questionId": "q-24155",
        "question": "On a Linux host running a multi-tenant analytics pipeline with dozens of containers on a single NUMA node, bursts trigger sporadic failures to allocate large contiguous buffers, elevating latency. Propose a concrete production plan to diagnose and mitigate memory fragmentation and allocator pressure while preserving throughput. Include per-tenant memcg v2 quotas, cpuset/NUMA bindings, hugepages/THP strategy, per-tenant allocator hints (jemalloc arenas), kernel memory compaction tuning, and a staged load-test with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To quickly verify ARP traffic and identify abnormal ARP activity on interface eth1, run 'tcpdump -i eth1 arp'. This command captures and displays ARP packets specifically on the eth1 interface, allowing you to monitor f…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "**TCP Tuning:** ```bash # Socket buffers - allow larger buffers for high-throughput gateways sysctl -w net.core.rmem_max=16777216 sysctl -w net.core.wmem_max=16777216 sysctl -w net.ipv4.tcp_rmem='4096 87380 167772ysctl…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant memcg v2 quotas and cpuset/NUMA bindings; reserve HugePages/THP where appropriate for large buffers; isolate allocators per tenant (jemalloc arenas via MALLOC_CONF) to limit cross",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Begin by profiling with bpftool and perf to locate hot BPF maps and contention per tenant. Increase per-tenant map sizes and optimize access patterns. Pin BPF programs to dedicated CPUs, isolate traci",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical mastery of Linux memory isolation, NUMA binding, and low-latency multitenant workloads under fragmentation pressure.\n\n## Key Concepts\n- memcg v2 quotas, cpuset, NUMA bindings\n- HugePages/THP strategy; allocator isolation (jemalloc arenas)\n- kernel memory.compaction tuning; vmscan knobs\n- per-tenant BPF observability; staged rollouts\n\n## Code Example\n```c\n// Minimal eBPF: count major page allocations per cgroup id\n#include <uapi/linux/ptrace.h>\nBPF_HASH(page_allocs, u64, u64);\nint trace_alloc(struct trace_event_raw_alloc *e) {\n  u64 cid = bpf_get_current_uid_gid();\n  page_allocs.increment(cid);\n  return 0;\n}\n```\n\n## Follow-up Questions\n- How would you quantify fragmentation impact and rollback criteria?\n- Which tenants get priority during compaction?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-12",
        "questionId": "q-17172",
        "question": "On a Linux host running a multi-tenant CI build farm where tenants submit parallel compile jobs, all tenants share an Intel Xeon platform supporting RDT (CAT/MBA). Tail build times spike under peak loads due to cache and memory bandwidth contention. Design a production plan to guarantee deterministic builds and prevent starvation. Include hardware isolation with CAT/MBA (per-tenant LLC and bandwidth quotas), per-tenant cgroups v2 (memory.max, io.max), CPU/NUMA pinning, cache-aware scheduling, observability with pqos, perf, pidstat, and a staged canary rollout with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Intel RDT to carve LLC with per-tenant classes and MBA budgets, pin builds to reserved cores and NUMA nodes, enforce memory/io quotas via cgroup v2, and attach a cache-aware scheduler hint. Monito",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Pin each tenant to dedicated CPU cores and map NIC RX queues to those cores; disable irqbalance; configure per-IRQ affinity, and tune RPS/RFS so flows steer to the tenant’s CPUs; preserve memory isola",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Identify offenders with systemd-cgtop and systemd-cgls to pinpoint which service.slice is hogging resources. Throttle by applying quotas on the offending unit.slice using systemctl set-property to enf",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Isolate replication and audit traffic to dedicated NICs/queues; enable mqprio on both NICs; tag replication/audit with net_cls and pin their threads to isolated CPUs; apply tc clsact for upstream/down",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design deterministic performance boundaries using hardware and OS isolation techniques in a realistic multi-tenant CI environment.\n\n## Key Concepts\n\n- Intel RDT CAT and MBA for cache and memory bandwidth control\n- cgroup v2 memory.max and io.max for per-tenant quotas\n- CPU/NUMA pinning and cache locality considerations\n- Observability with pqos, perf, pidstat, and synthetic workloads\n- Canary rollout and rollback strategies\n\n## Code Example\n\n```bash\n# Enable RDT and set per-tenant budgets (example commands)\npqos -R\npqos -e 'L3:0=25;L3:1=25;L3:2=25;L3:3=25' # LLC partitions per tenant\n# Apply quotas via cgroups v2 (pseudo-commands)\nmkdir /sys/fs/cgroup/mytenant\necho 4G > /sys/fs/cgroup/mytenant/memory.max\necho 20:0-7 > /sys/fs/cgroup/mytenant/cgroup.procs\n```\n\n## Follow-up Questions\n\n- How would you validate the budgeting under a realistic CI workload mix?\n- What rollback criteria would you automate if latency budgets are breached?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-13",
        "questionId": "q-20290",
        "question": "On a Linux host running a shared CI runner farm for multiple teams, aggressive CPU bursts from one tenant occasionally cause latency spikes for others. Design a beginner-friendly plan to diagnose and mitigate CPU contention without modifying tenant jobs. Include steps for measuring CPU steal, isolating tenants with cgroups v2/systemd, configuring per-tenant CPU quotas, baseline tests, and a rollback path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Isolate the RT path on NUMA node0 and bind CPUs/memory tightly. Boot with isolcpus=0-7 nohz_full=0-7; route IRQ to node0; disable irqbalance. Put RT tasks in rt.slice with SCHED_FIFO 99. Bind with num",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Enable the cpu controller, create per-tenant slices (e.g., container-tenantA.slice). Set cpu.max values (e.g., 250000 for 25% of a CPU). Validate with a controlled load (stress-ng) and monitor latency",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Partition NUMA node 0 for the critical path; pin the UI worker to CPUs 0–3; boot with isolcpus=0-3, nohz_full=0-3, rcu_nocb; use SCHED_DEADLINE on rt.slice with period=5ms, runtime=4ms, slack=1ms; bin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Baseline with mpstat -P ALL 1 5 and iostat to confirm steal and I/O contention. Isolate tenants with cgroups v2: create per-tenant slices (tenant-a.slice, tenant-b.slice) and assign cpu.max or CPUQuot",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests basic performance diagnosis, tooling, and practical containment without touching tenant apps.\n\n## Key Concepts\n\n- Linux CPU contention and CPU steal\n- cgroups v2 and per-tenant isolation\n- systemd.slice and CPU quotas\n- baseline benchmarking and rollback\n\n## Code Example\n\n```javascript\n// Node.js snippet to read tenant quotas\nconst fs = require('fs');\n['tenant-a.slice/cpu.max','tenant-b.slice/cpu.max'].forEach(p=>{\n  try { console.log(p, fs.readFileSync(`/sys/fs/cgroup/${p}`, 'utf8').trim()); } catch{}\n});\n```\n\n## Follow-up Questions\n\n- How would you validate tests without impacting production workloads?\n- What metrics would you alert on to catch regressions?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-14",
        "questionId": "q-20302",
        "question": "On a Linux host running a shared CI build farm with containerized tenants, bursts of builds cause CPU contention and higher latency. Design a beginner-friendly plan to enforce per-tenant CPU quotas using systemd.slice and cpuset, plus a simple monitoring/rollback approach. Include concrete commands, baseline tests, and rollback steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create per-tenant slices (tenantA.slice) and assign each container to its slice. Set CPUQuota=50% and CPUQuotaPeriod=100000, optionally pin to CPUs with CPUAffinity. Add a lightweight monitor (pidstat",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create per-tenant log sinks at /var/log/tenant-logs/<tenant>, implement a simple log-router (Node/Python) to route writes to tenant-specific files, and configure per-tenant logrotate. Apply IO schedul",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create per-tenant log dirs under /var/log/tenant-<id> and enable filesystem quotas to cap each tenant’s log footprint. Route logs with rsyslog using loginuid so tenant logs land in tenant-specific fil",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enable per-tenant kmem.limit_in_bytes to cap slab usage, and tune vm.vfs_cache_pressure to balance cache. Use memcg/kmem accounting to isolate caches, pin tenants to NUMA nodes if feasible, and monito",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical, beginner-friendly familiarity with Linux resource isolation and systemd. It focuses on multi-tenant concurrency control without complex infra.\n\n## Key Concepts\n\n- systemd.slice and per-tenant isolation\n- CPUQuota/CPUQuotaPeriod and CPUAffinity\n- cpuset basics\n- lightweight monitoring and rollback steps\n\n## Code Example\n\n```javascript\n// Example: basic per-tenant quota commands\nsystemctl set-property tenantA.slice CPUQuota=50%\nsystemctl set-property tenantA.slice CPUQuotaPeriodUS=100000\n```\n\n## Follow-up Questions\n\n- How would you extend this with cpuset or cgroup v2 autobalancing?\n- What tests would validate fairness during mixed-tenant bursts?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-15",
        "questionId": "q-19399",
        "question": "On a Linux host running multiple tenants via Docker, CPU bursts cause CI jobs to starve other tenants. Design a beginner-friendly plan to isolate CPU resources and enforce fair scheduling using cgroups v2: create per-tenant cpusets, set cpu.max, and place tenants in a shared slice for fairness. Include concrete test steps, metrics (e.g., per-tenant latency), and rollback instructions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Configure per-tenant CPU isolation with cgroups v2: create per-tenant cpusets, assign CPUs via cpu.cpus, enforce quotas with cpu.max, and group tenants in a shared slice for fair scheduling. Run a sim",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Measure with perf, ftrace, and slabtop to see THP pauses during bursts; check /sys/kernel/mm/transparent_hugepage/enabled. If THP stalls are real, disable THP (echo never > enabled). Enforce per-tenan",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant I/O quotas with io.max on cgroup v2, assign each tenant to dedicated NVMe queues, and use BFQ or Kyber scheduler with NUMA binding. Instrument with iostat, blktrace, perf, and BPF tracing t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create per-tenant cgroup v2 trees and assign all tenant processes there. Enforce io.max and cpu.max to cap bandwidth and CPU share; pin I/O workers and memory to dedicated NUMA nodes; use mq-deadline",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical OS-level resource isolation fundamentals and how to implement fair CPU sharing in a real multi-tenant environment without complex orchestration.\n\n## Key Concepts\n\n- cgroups v2 and the cpu controller\n- cpuset vs unified hierarchy\n- cpu.max and cpu.cpus for quotas and pinning\n- latency-based benchmarking for fairness\n\n## Code Example\n\n```javascript\n// Example: pseudo-commands to create and apply a cpuset (not run in shell directly)\n``` \n\n## Follow-up Questions\n\n- How would you monitor per-tenant CPU usage in production with low overhead?\n- How would you handle a tenant requiring burst beyond its quota without impacting others?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-16",
        "questionId": "q-2167",
        "question": "On a KVM host running dozens of VMs with memory ballooning enabled, a burst in guest memory growth triggers host memory pressure and occasional OOM events. Propose a production-safe plan to cap balloon aggressiveness, enforce host backpressure, and prevent thrash, using libvirt XML (memory.size/currentMemory, memoryBacking, balloon device) and cgroup v2 memory.max/memory.high, plus a test plan with a reproducible spike and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use iostat -xd 1 to identify device waits and iotop to spot heavy hitters; switch to bfq or deadline on HDDs; tune per-disk nr_requests (128–256) and set a small queue depth; implement a simple blkio",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Per-VM caps and headroom via cgroup v2: memory.max and memory.high per VM plus a low vm.swappiness to delay swap pressure. Keep ballooning enabled but hard-cap host memory pressure so reclamation happ",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement per-tenant cgroup v2 hierarchies under /sys/fs/cgroup: tenant-A, tenant-B with io.max set to a fixed bandwidth and cpu.max to bound CPU. Switch the disk scheduler to BFQ and pin tenants to d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Isolate tenants with memory.max and memory.high in a cgroup v2, pin processes to NUMA nodes, tune vfs_cache_pressure and min_free_kbytes, set per-tenant IO priorities with BFQ/Deadline, and hint page",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nProbe for practical knowledge of KVM memory management, cgroup v2, ballooning trade-offs, and test scaffolding under real production-like conditions.\n\n## Key Concepts\n- KVM memory ballooning and how guest memory maps to host resources\n- cgroup v2 limits (memory.max, memory.high) for backpressure\n- libvirt XML configuration for per-VM memory caps\n- Observability and test plan for reproducible spikes\n\n## Code Example\n```xml\n<domain type='kvm'>\n  <name>vm1</name>\n  <memory unit='KiB'>4294967296</memory>\n  <currentMemory unit='KiB'>4294967296</currentMemory>\n  <memoryBacking>\n    <hugepages/>\n  </memoryBacking>\n  <devices>\n    <memballoon model='virtio'/>\n  </devices>\n</domain>\n```\n\n## Follow-up Questions\n- How would you monitor and alert on host memory pressure events?\n- What are the risks of disabling ballooning on some VMs?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-17",
        "questionId": "q-1230",
        "question": "On a Linux host with a multi-tenant workload sharing a single 10GbE NIC configured with multiple receive queues, one tenant bursts UDP traffic and starves others, causing increased latency and packet loss. Propose a production plan to enforce tenant isolation and fairness using NIC multi-queue, RSS/XPS mapping, IRQ affinity, and per-tenant cgroups (v2) with io.max and tc shaping. Include concrete steps and a test plan with realistic traffic?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan to isolate tenants by binding NIC RX queues to per-tenant CPUs, use RSS/XPS to map flows, create per-tenant systemd slices with per-tenant CPU budgets, apply io.max quotas, and shape egress with",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-tenant IO control with cgroup v2, assign each tenant its own overlayfs chain and a storage stripe, and choose BFQ as the block scheduler (mq-deadline if BFQ unavailable). Bind containers",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by profiling with bpftool and perf to locate hot BPF maps and contention per tenant. Increase per-tenant map sizes and optimize access patterns. Pin BPF programs to dedicated CPUs, isolate traci",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run df -i /scratch to confirm inode saturation; create per-tenant subdirs under /scratch; enable quotas (edquota -u tenant) and quotaon; set block and inode limits; add a lightweight cleanup script; s",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical network isolation skills in a multi-tenant Linux environment, focusing on NIC multi-queue tuning, IRQ affinity, and per-tenant resource containers to prevent bursts from one tenant affecting others.\n\n## Key Concepts\n- NIC multi-queue, RSS, XPS\n- IRQ affinity per-tenant isolation\n- cgroups v2 with io.max and per-tenant slices\n- tc shaping and fq_codel for fairness\n- Observability with iperf3/pktgen, iostat\n\n## Code Example\n```bash\n# Enable 4 RX queues\nethtool -L eth0 rx 4\n# Bind a few IRQs to CPUs (example)\necho 2-3,6-7 > /proc/irq/46/smp_affinity_list\n```\n\n## Follow-up Questions\n- How would you validate fairness under concurrent tenants?\n- What are NUMA pitfalls and how would you mitigate them?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-18",
        "questionId": "q-4264",
        "question": "On a fleet of Linux CI workers, bursts from a single job spike CPU and memory, starving other jobs. Propose a beginner-friendly plan to isolate each job using a dedicated systemd slice and cgroup v2, enforce per-job CPU quotas, limit PIDs, and ensure quick reversion if a burst is detected. Include concrete unit settings, a test plan with a 5-minute burst, and commands to verify outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant namespaces with VRF-style routing, dedicated CPUs, and XDP/BPF-based per-tenant ACLs and egress shaping. Use tc HTB/fq_codel with per-tenant queues, bind NIC TX/RX rings to the same NUMA no",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Configure per-worker io_uring rings with fixed SQ depth (e.g., 256), pin workers to dedicated CPUs via cpuset, set per-cgroup io.max (e.g., 8MB/s), implement pacing in ingestion to cap outstanding I/O",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a dedicated ci.slice with CPUQuota=50%, CPUQuotaPeriodSec=100ms, MemoryLimit=2G, and TasksMax=200. Use a service template ci-job@.service with Slice=ci.slice and per-job limits. Test by launchi",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Isolate VMs to fixed NUMA nodes, preallocate hugepages per guest, and enforce per-VM memory limits via cgroups. Disable or throttle KSM for priority tenants; tune virtio-balloon to avoid oscillations.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical, beginner-friendly systemd/cgroup isolation for CI workers.\n\n## Key Concepts\n- systemd slices and resource controls (CPUQuota, MemoryLimit, TasksMax)\n- cgroup v2 awareness and per-job isolation\n- test harness for bursts and verification\n\n## Code Example\n```ini\n# /etc/systemd/system/ci.slice\n[Unit]\nDescription=CI Job Sandbox\n\n[Slice]\nCPUAccounting=true\nCPUQuota=50%\nCPUQuotaPeriodSec=100ms\nMemoryLimit=2G\nTasksMax=200\n```\n```ini\n# /etc/systemd/system/ci-job@.service\n[Unit]\nDescription=CI Job %I\n\n[Service]\nSlice=ci.slice\nExecStart=/bin/sleep infinity\n```\n\n## Follow-up Questions\n- How would you extend this for GPU-accelerated jobs?\n- How to monitor and automatically eject misbehaving jobs between bursts?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-19",
        "questionId": "q-5030",
        "question": "On a Linux host acting as a multi-tenant CI runner farm (Docker/K8s), one project triggers heavy memory pressure causing host swapping and spillover latency. Design a production plan to enforce per-tenant memory quotas and isolation: cgroups v2 memory.max/memory.swap.max, memory.high defaults, OOMScoreAdjust, page-cache pressure, and fair reclaim; include test plan with synthetic tenants, burst scenarios, and a rollback path if latency degrades?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a logrotate config at /etc/logrotate.d/app for /srv/app/logs/*.log with daily, size 100M, rotate 7, compress, delaycompress, missingok, notifempty, and a postrotate block that reloads the servi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Patch kernel live with Kpatch/KSPP. Build patch, deploy to 1 canary host, run representative load for 2–4 hours, monitor PMU counters, dmesg, and latency; if clean, expand to 25%, then full fleet with",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant I/O quotas with cgroup v2 io.max, add per-tenant tc shaping and optional IO-priority, pin tenants to dedicated CPUs and NUMA nodes, and tune blk-mq and the IO scheduler (bfq/kyber",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement cgroups v2 with per-tenant slices, configure MemoryMax and MemorySwapMax for hard limits, set MemoryHigh for soft limits, pin tenants to dedicated CPUs, and configure io.max and cpu.max for fair resource distr…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical resource isolation under multi-tenant load using modern cgroup2 controls and robust rollback strategies.\n\n## Key Concepts\n- cgroups v2 memory controls: memory.max, memory.swap.max, memory.high\n- Per-tenant slices with CPU pinning and I/O throttling\n- OOMScoreAdjust tuning, swap minimization, burst handling\n- Fair reclaim algorithms and page-cache pressure management\n\n## Code Example\n```bash\n# Create per-tenant slice with memory limits\nsystemd-run --slice=tenant-A.slice \\\n  --property=MemoryMax=2G \\\n  --property=MemorySwapMax=1G \\\n  --property=MemoryHigh=1.8G \\\n  --property=CPUQuota=50% \\\n  --property=IOReadBandwidthMax=\"/dev/sda 10M\" \\\n  --property=IOWriteBandwidthMax=\"/dev/sda 10M\" \\\n  /usr/bin/docker run --rm tenant-workload\n\n# Configure OOM score adjustment\necho 500 > /proc/$(pidof tenant-workload)/oom_score_adj\necho -1000 > /proc/$(pidof systemd)/oom_score_adj\n```\n\n## Test Plan\n- Deploy synthetic tenants with varying memory profiles\n- Simulate burst scenarios with stress testing tools\n- Monitor swap usage, OOM events, and latency metrics\n- Validate rollback path via systemd slice removal",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-20",
        "questionId": "q-27125",
        "question": "On a Linux host running a multi-tenant data ingestion service writing to a shared NVMe pool, tail I/O latency spikes during bursts due to per-tenant I/O contention. Design a production plan to enforce fair I/O and prevent starvation while preserving throughput. Include per-tenant cgroup v2 io.weight and io.max configurations, storage QoS strategy with device binding, NUMA considerations, observability, and a staged load-test with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant cgroup v2 I/O quotas using io.weight and io.max, pin tenants to dedicated NVMe queues, and bind their devices to per-tenant cgroups. Use a storage QoS approach with a suitable sch",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan to diagnose: enable isolcpus and shielding, assign tenants to dedicated CPUs with cpuset v2, isolate and pin NIC interrupts to shield CPUs, bind VMs/containers accordingly, instrument with BPF tr",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant memory.high and memory.max with cpuset bindings; bind RX/TX NIC queues per tenant; deploy per-tenant XDP/eBPF rate limiting and per-tenant io.max; disable THP, set vm.min_free_kbytes and pe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant I/O limits using cgroups v2 blkio.weight, assigning each container a proportional share of disk bandwidth on the shared NVMe SSD. Establish baseline performance metrics using fio (random and sequent…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical ability to prevent I/O starvation in multi-tenant Linux environments by enforcing concrete, per-tenant controls at the block layer, with measurable validation.\n\n## Key Concepts\n\n- cgroup v2 io.weight and io.max throttling\n- per-tenant device binding and NUMA awareness\n- storage QoS and block schedulers (BFQ) for fairness\n- observability: iostat, blktrace, bpftrace\n- staged load-testing and rollback procedures\n\n## Code Example\n\n```bash\nfio --name=tenantA --rw=randrw --size=1G --runtime=60 --time_based --group_reporting\n```\n\n## Follow-up Questions\n\n- How would you verify improvements and identify edge cases (bursty tenants, bursty IO bursts)?\n- What change control and rollback considerations would you employ during a production rollout?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-21",
        "questionId": "q-13240",
        "question": "On a Linux host running a multi-tenant analytics platform with in-memory stores, a critical kernel CVE requires patching without reboot. Design a production plan to apply a live kernel patch (kgraft/kpatch) while preserving per-tenant SLAs, including patch preparation, per-tenant validation, XDP/BPF state handling, observability, and a staged rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant I/O limits using cgroups v2 blkio.weight, assigning each container a proportional share of disk bandwidth on the shared NVMe SSD. Establish baseline performance metrics using fio (random and sequent…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Execute a zero-downtime kernel patch via kGraft/kpatch with canary rollout during a maintenance window. Key steps: prepare and sign the patch, deploy to canary nodes, verify patch activation (uname -r, /proc/version, kp…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use systemd ExecReload to send SIGHUP to the daemon and a small validator script. Create /usr/local/bin/myapp_reload to validate /etc/myapp/config.yaml (e.g., ensure .port and .log_level exist) and th",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant WAL streams with isolated io_uring queues and dedicated WAL directories; pin WAL I/O to the local NUMA node to minimize cross-NUMA access latency; leverage cgroupv2 IO throttling to prevent any sing…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-world discipline for zero-downtime patching in multi-tenant environments, balancing security requirements with SLA guarantees.\n\n## Key Concepts\n- Live kernel patching (kGraft, kpatch)\n- Canary rollout and rollback strategies\n- Per-tenant SLA awareness during updates\n- XDP/BPF state preservation across patches\n- Observability: perf, ftrace, bpftrace, metrics\n\n## Code Example\n```bash\n# Example: verify patch applied (conceptual)\nuname -r\n# Load patch (conceptual)\nkpatch load patchfile.ko\n```\n\n## Follow-up Questions\n- How would you validate no regression in tail latencies during patching?\n- What specific metrics would you monitor for XDP/BPF program integrity?\n- How would you coordinate with tenant stakeholders during the maintenance window?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-22",
        "questionId": "q-13134",
        "question": "On a Linux host hosting a small dev-environment for multiple teams, each team runs a subset of containers on the same host. A sudden burst from one team monopolizes CPU, causing others to slow. Propose a beginner-friendly plan to enforce per-team CPU quotas using cgroups v2 and lightweight labeling, including a test plan and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use systemd ExecReload to send SIGHUP to the daemon and a small validator script. Create /usr/local/bin/myapp_reload to validate /etc/myapp/config.yaml (e.g., ensure .port and .log_level exist) and th",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Bind each GPU service to a dedicated CPU set and a separate systemd slice; pin IRQs and NIC interrupts; enable isolcpus on cores; cap CPU usage with cpu.max; tune I/O scheduler to mq-deadline with per",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: create per-team CPU quotas using cgroups v2 under a dedicated tree, set cpu.max with a safe quota/period (e.g., 20000 100000 for 0.2 CPU), and assign each team’s containers by moving their PIDs",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Deploy a lightweight monitor per container that checks /var/log/<ct> size every 5 minutes and alerts on threshold. Enforce quotas via per-dir quotas on the filesystem (or systemd.slice if available) t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests a beginner’s grasp of basic resource isolation on Linux using modern cgroups, plus practical steps to verify behavior and rollback safely.\n\n## Key Concepts\n- cgroups v2 and cpu.max\n- pid assignment to cgroups\n- simple per-team labeling/organization\n- verification with monitoring tools and lightweight load testing\n\n## Code Example\n```bash\n# Create per-team cgroup\nsudo mkdir -p /sys/fs/cgroup/teams/teamA\n# Set 0.2 CPU quota (quota period in microseconds)\necho \"20000 100000\" | sudo tee /sys/fs/cgroup/teams/teamA/cpu.max\n# Move a container's PIDs into the teamA cgroup\nfor PID in $(pidof containerA); do\n  echo $PID | sudo tee /sys/fs/cgroup/teams/teamA/cgroup.procs\ndone\n```\n\n## Follow-up Questions\n- How would you scale this to 10+ teams and multi-core hosts?\n- What metrics indicate a successful isolation and when would you roll back?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-23",
        "questionId": "q-26143",
        "question": "On a Linux host with multiple tenants writing to a shared log area, bursts cause IO contention and latency spikes. Design a beginner-friendly plan to isolate log traffic and reduce contention without changing tenant apps. Include per-tenant log directories, a rotating centralized writer, IO scheduling hints, baseline tests, and a rollback path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Proposed answer: Use per-tenant arenas in jemalloc or tcmalloc to eliminate cross-tenant contention; pin worker threads and malloc metadata to specific NUMA nodes; align allocations to cache lines and",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan to diagnose CPU/memory unfairness on a NUMA host serving a multi-tenant streaming service. Implement per-tenant cpuset and cpu.max, per-tenant memory.min/memory.high, and strict NUMA binding. Ena",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create per-tenant log sinks at /var/log/tenant-logs/<tenant>, implement a simple log-router (Node/Python) to route writes to tenant-specific files, and configure per-tenant logrotate. Apply IO schedul",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Isolate WAL on a dedicated NVMe, use a quiet IO scheduler (mq-deadline), and bind WAL writer to dedicated CPUs. Pin IRQs to those CPUs, NUMA-bind WAL memory, and assign per-tenant io.weight via cgroup",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nIn multi-tenant environments, bursty logging can saturate a single disk. This question probes concrete steps to isolate traffic without changing tenant apps, including file-system layout, a router, and rollback.\n\n## Key Concepts\n\n- per-tenant isolation\n- log rotation\n- IO scheduling\n- benchmarking and rollback\n\n## Code Example\n\n```javascript\n// Minimal log-router sketch\nconst fs = require('fs');\nconst tenant = process.env.TENANT || 'default';\nconst stream = fs.createWriteStream(`/var/log/tenant-logs/${tenant}/latest.log`, { flags: 'a' });\nstream.write(`[${new Date().toISOString()}] ${process.argv[2] || ''}\\n`);\n```\n\n## Follow-up Questions\n\n- How would you measure latency changes during bursts?\n- What fail-safes ensure log data isn't lost on router crash?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-24",
        "questionId": "q-21542",
        "question": "On a Linux host hosting multiple tenants via a shared /scratch directory, bursts of tiny file creates trigger inode pressure and directory churn, causing builds to stall even when space is available. Design a beginner-friendly plan to diagnose and mitigate this without changing tenant apps. Include per-tenant isolation (simple quotas or directories), a plan to enable quotas, a basic test plan, and rollback steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Run df -i /scratch to confirm inode saturation; create per-tenant subdirs under /scratch; enable quotas (edquota -u tenant) and quotaon; set block and inode limits; add a lightweight cleanup script; s",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan to diagnose and reduce tail latency on a low-latency ingest host using AF_XDP: baseline with bpftool, iostat, and perf; isolate NIC IRQs to dedicated CPUs; allocate per-tenant AF_XDP sockets with",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run host perf tools to locate IRQ/NIC bottlenecks (perf stat, mpstat, sar), verify multi-queue and RSS with ethtool -L/-C and ethtool -x; enable per-VM NIC multi-queue and bind each VM thread to dedic",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create per-tenant scopes: tenant-foo.scope and tenant-bar.scope; assign CPUQuota=50% and MemoryLimit=1024M, and BlockIOWeight=500 as a starting point. Use cgroup v2 with systemd. Add a simple monitori",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nInodes and quotas are a common beginner pain point in multi-tenant shared spaces. This tests practical diagnosis, isolation, and rollback skills.\n\n## Key Concepts\n- Inodes and directory churn\n- Per-tenant isolation via quotas or subdirs\n- Enabling quotas and safe rollback\n- Basic test strategy and metrics\n\n## Code Example\n```bash\n# Basic quota preparation (example placeholder commands)\nsudo apt-get install quota -y\nsudo quotacheck -cum /scratch\nsudo quotaon /scratch\n# Create tenant dirs and set limits (example)\n# sudo setquota -u tenant1 10000 12000 0 0 /dev/sdXN\n```\n\n## Follow-up Questions\n- How would you adapt if /scratch is NFS? \n- What metrics indicate success post-implementation?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-16T07:27:52.280Z",
    "version": 1
  }
]