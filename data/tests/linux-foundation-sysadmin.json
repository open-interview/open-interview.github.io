[
  {
    "id": "test-linux-foundation-sysadmin",
    "channelId": "linux-foundation-sysadmin",
    "channelName": "Linux Foundation Sysadmin",
    "title": "Linux Foundation Sysadmin Knowledge Test",
    "description": "Test your understanding of Linux Foundation Sysadmin with 25 questions.",
    "questions": [
      {
        "id": "linux-foundation-sysadmin-q-0",
        "questionId": "q-4752",
        "question": "On a Linux host serving a GPU-accelerated real-time inference pipeline, multiple workloads share a single 2-socket system. Occasional stalls increase tail latency of the critical path under peak load. Design a production plan to guarantee deterministic latency for the critical path. Include hardware topology (NUMA binding), kernel/driver knobs (nohz_full, isolcpus, IRQ affinity), per-workload scheduling (SCHED_DEADLINE with deadlines), cgroups v2 (io.max, memory.min), and a validation plan with synthetic bursts. What exact steps would you take and how would you verify?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Isolate tenants with cgroups v2 IO controllers (io.weight/io.max) and assign the critical build path to a dedicated CPU set. Use per-tenant devices or namespaces on NVMe, and the mq-deadline or bfq sc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Profile IO with iostat -x 1 and iotop to identify offenders. Apply per-container limits via a simple cgroup blkio policy (blkio.weight) or systemd IOWeight mapped to the container slice. Reproduce bur",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt per-tenant isolation: assign each tenant its own BPF map namespace (or tenant_id keys), run a dedicated BPF program instance per tenant, and pin maps to dedicated CPUs to reduce cross-tenant chu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Isolate a NUMA node for the critical CUDA workers; pin them to CPUs 4–7; enable nohz_full and isolcpus on those CPUs; bind the GPU PCIe device to that node via IOMMU; set IRQ affinity away from isolat",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests hands-on ability to guarantee deterministic latency in GPU-backed real-time pipelines by combining OS-level isolation (NUMA, CPU pinning, IRQ affinity) with schedulers and cgroups, plus a practical validation path.\n\n## Key Concepts\n- NUMA binding and CPU isolation (isolcpus, nohz_full)\n- IRQ affinity and PCIe device binding to a dedicated node\n- SCHED_DEADLINE and rt.slice usage for hard real-time tasks\n- cgroups v2 controls (io.max, memory.min, cpuset) for resource guarantees\n- Validation with synthetic bursts and tail-latency measurements\n\n## Code Example\n```bash\n# Example: isolate CPUs 4-7 and bind GPU to NUMA node 0 (conceptual)\nnumactl --cpunodebind=0 --physcpubind=4-7 <your-infer-program>\n```\n\n## Follow-up Questions\n- How would you monitor real-time latency and detect deadlocks?\n- What failure modes would you test, and how would you revert changes if needed?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-1",
        "questionId": "q-8443",
        "question": "On a Linux host acting as a public gateway behind a reverse proxy, a surge of short-lived TLS connections fills nf_conntrack, causing new connections to be dropped. Design a beginner plan to diagnose and mitigate connection tracking pressure without touching the app. Include diagnostic commands, concrete sysctl changes, and a rollback/test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Profile per-tenant memory with smem/top and vmstat; create per-tenant cgroups v2 and set memory.max; use memory.high for bursts; lower vm.swappiness to 10-20; run a synthetic memory load within quotas",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Production Plan: Diagnosing and Eliminating I/O Starvation in Multi-Tenant CI/CD Farm ### Phase 1: Baseline Observability with BPF Deploy BPF programs to capture per-cgroup I/O latency before applying controls: ```c…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Increase nf_conntrack capacity and apply per-source limits to relieve pressure. Steps: 1) Monitor current usage: `cat /proc/net/nf_conntrack | wc -l`; 2) Scale capacity: `sysctl -w net.netfilter.nf_conntrack_max=524288`…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Compute per-tenant I/O budgets using cgroup v2 `io.max` and `io.weight`, assign proportional bandwidth shares to each tenant via BFQ, and apply device-level quotas to cap peak throughput. Instrument with `iostat`, `iopi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests knowledge of nf_conntrack management, netfilter tuning, and safe operational practices without application changes.\n\n## Key Concepts\n\n- nf_conntrack capacity and monitoring\n- Per-source rate limiting with iptables connlimit\n- Sysctl parameter tuning\n- Operational rollback and testing procedures\n\n## Code Example\n\n```javascript\n// Simple Node.js snippet to monitor nf_conntrack status\nconst fs = require('fs');\nconst conntrackCount = fs.readFileSync('/proc/net/nf_conntrack','utf8').split('\\n').length - 1;\nconsole.log(`Current nf_conntrack entries: ${conntrackCount}`);\n```\n\n## Follow-up Questions\n\n- How would this approach change if TLS termination occurs at the reverse proxy?\n- What monitoring strategy would you implement to track the long-term impact of these changes?\n- How would you verify that legitimate connections aren't being dropped by the connlimit rule?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-2",
        "questionId": "q-18735",
        "question": "On a Linux host running a multi-tenant build/CI environment with hundreds of worker processes sharing a single allocator, tail latency spikes occur under load due to allocator contention. Design a practical production plan to diagnose and mitigate allocator-induced tail latency while preserving throughput. Include per-tenant arenas (jemalloc/tcmalloc), NUMA-aware binding, cache-alignment, false-sharing mitigations, observability (perf/pmu/pidstat/jemalloc), and staged load-testing with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Attach eBPF probes at net_dev_queue, udp_recvmsg, and inet_csk_receive, collect per-CPU latency histograms with BPF maps, sample modestly (e.g., 1k samples/sec), and aggregate 95th/99th percentiles. U",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed answer: Use per-tenant arenas in jemalloc or tcmalloc to eliminate cross-tenant contention; pin worker threads and malloc metadata to specific NUMA nodes; align allocations to cache lines and",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "On the host, ensure chrony is syncing with low drift (chronyc tracking; sources -v). Prohibit NTP daemons inside containers; containers share host time. Verify with 'date' in 5 tenants during bursts.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: enforce per-tenant memcg quotas on v2, pin tenants to specific NUMA nodes, disable THP for predictable latency, align hot paths to 64-byte cachelines, assign IRQ affinity per-tenant, instrument",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTail latency spikes from allocator contention in a multi-tenant environment are a realistic, high-impact issue that tests understanding of allocator internals, NUMA locality, and end-to-end observability.\n\n## Key Concepts\n- per-tenant arenas in allocators\n- NUMA-aware binding of threads and memory\n- cache-line padding to avoid false sharing\n- per-tenant isolation vs shared pools\n- jemalloc/tcmalloc stats and perf/pidstat observability\n\n## Code Example\n```javascript\n// pseudo config snippet\nconst allocator = {\n  perTenantArenas: true,\n  numaBinding: true\n}\n```\n\n## Follow-up Questions\n- How would you validate tail latency changes with staging workloads?\n- How would you roll back allocator changes if regression occurs?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-3",
        "questionId": "q-14198",
        "question": "On a Linux host that runs a multi-tenant CI cache, the system clock occasionally jumps backwards by 1-2 seconds under peak I/O, breaking time-based eviction and flaky builds. Create a beginner-friendly diagnostic plan to identify and fix time synchronization without touching tenant applications. Include checks for chrony/NTP, kernel timekeeping (clocksource), and a rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pin each tenant to dedicated CPUs and a NUMA node via cpuset/v2, assign memory.min/max, and bound I/O interrupts to that node. Enable NUMA-aware scheduling and CPU affinity, and adjust IRQ/softirq aff",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Identify offenders with systemd-cgtop and systemd-cgls to pinpoint which service.slice is hogging resources. Throttle by applying quotas on the offending unit.slice using systemctl set-property to enf",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: enforce per-tenant memcg quotas on v2, pin tenants to specific NUMA nodes, disable THP for predictable latency, align hot paths to 64-byte cachelines, assign IRQ affinity per-tenant, instrument",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: confirm a time sync service is active (chrony or systemd-timesyncd), verify drift with chronyc tracking and sources, ensure NTP servers are reachable, then resync and revalidate. Inspect current",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical time-sync troubleshooting in a multi-tenant context where clock drift hurts caches and logs.\n\n## Key Concepts\n- Time synchronization with chrony/systemd-timesyncd\n- Clock source stability (TSCT/HPET) vs TSC\n- Reproducible rollback of config changes\n\n## Code Example\n```javascript\n# example commands (bash)\nsystemctl is-active chronyd\nchronyc tracking\nchronyc sources\ncat /sys/devices/system/clocksource/clocksource0/current_clocksource\nsudo chronyc makestep\n```\n\n## Follow-up Questions\n- How would you automate drift monitoring across tenants?\n- How do you validate rollback safety during a maintenance window?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-4",
        "questionId": "q-19399",
        "question": "On a Linux host running multiple tenants via Docker, CPU bursts cause CI jobs to starve other tenants. Design a beginner-friendly plan to isolate CPU resources and enforce fair scheduling using cgroups v2: create per-tenant cpusets, set cpu.max, and place tenants in a shared slice for fairness. Include concrete test steps, metrics (e.g., per-tenant latency), and rollback instructions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose separate per-tenant log directories on a dedicated log volume (e.g., XFS) with bind mounts or subvols, enable per-tenant quotas, use logrotate with tenant-based rotation sizes, and monitor IO",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run host perf tools to locate IRQ/NIC bottlenecks (perf stat, mpstat, sar), verify multi-queue and RSS with ethtool -L/-C and ethtool -x; enable per-VM NIC multi-queue and bind each VM thread to dedic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure per-tenant CPU isolation with cgroups v2: create per-tenant cpusets, assign CPUs via cpu.cpus, enforce quotas with cpu.max, and group tenants in a shared slice for fair scheduling. Run a sim",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Baseline AVC audit collection (ausearch) to identify hot checks; instrument with perf/ftrace to quantify policy overhead; refactor by introducing per-tenant domains/types, label data paths with tenant",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical OS-level resource isolation fundamentals and how to implement fair CPU sharing in a real multi-tenant environment without complex orchestration.\n\n## Key Concepts\n\n- cgroups v2 and the cpu controller\n- cpuset vs unified hierarchy\n- cpu.max and cpu.cpus for quotas and pinning\n- latency-based benchmarking for fairness\n\n## Code Example\n\n```javascript\n// Example: pseudo-commands to create and apply a cpuset (not run in shell directly)\n``` \n\n## Follow-up Questions\n\n- How would you monitor per-tenant CPU usage in production with low overhead?\n- How would you handle a tenant requiring burst beyond its quota without impacting others?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-5",
        "questionId": "q-8210",
        "question": "On a Linux host running several containerized services using a shared block device, one container suddenly drives a burst of I/O that degrades others; propose a beginner-friendly, production-ready plan to diagnose and mitigate per-container I/O bottlenecks using cgroup v2 IO throttling. Include baseline metrics, configuration changes (io.max/io.weight or systemd.slice), test plan, and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: use per-tenant cgroup v2 io.max to cap disk writes, create tenant.slice with PrivateMounts for isolation, assign each service to a sub-cgroup, attach per-tenant tc qdisc to shape writes, and iso",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Diagnose using BPF programs tracing kmem:kmalloc and kmem:mm_page_alloc_extended_zone failures, combined with analysis of /proc/slabinfo and /proc/buddyinfo for fragmentation metrics. Apply SLUB tuning parameters: set s…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Audit inode usage with df -i /var/log/app and du -a /var/log/app | wc -l. Immediate fix: enable daily logrotate with compress, maxsize, and maxage; ensure /var/log is on a partition with ample inodes.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use cgroup v2 IO throttling to cap per-container I/O. Baseline with iostat -dx 1, iotop, pidstat to identify the offender. Apply per-container io.max (and optional io.weight) in its cgroup or systemd.",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to use low-level IO controls for container isolation and performance debugging in production.\n\n## Key Concepts\n\n- cgroup v2 IO throttling (io.max, io.weight)\n- baseline I/O metrics (iostat, iotop)\n- rollback procedures and safe testing\n\n## Code Example\n\n```bash\n# Pseudo: per-container I/O cap (device 8:0)\necho \"8:0 10485760\" > /sys/fs/cgroup/io.max/container-123/io.max\n```\n\n## Follow-up Questions\n\n- How would you automate detection of noisy tenants?\n- How would you scale this across many containers?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-6",
        "questionId": "q-21796",
        "question": "On a Linux host running a multi-tenant data-processing cluster, tenants share a pooled NVMe-backed storage backend (iSCSI) and tail latency spikes occur during bursts. Design a production plan to diagnose and reduce storage I/O tail latency while preserving tenant isolation and throughput. Include per-tenant I/O quotas (CGroups v2 blkio or BFQ), NUMA-aware IO paths, measurement with blktrace/iostat/iometer, a staged rollout with rollback, and concrete commands or steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: identify bursts with iostat and blktrace; test IO schedulers BFQ and mq-deadline with tuned queue_depth; enforce per-tenant I/O via cgroup v2 io.max/io.weight; pin I/O and workers to NUMA nodes;",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Hardening focuses on deterministic cadence and isolation: use OnCalendar with a fixed cadence and Persistent=true; AccuracySec=1s; RandomizedDelaySec=30s. Enforce CPUQuota and LimitNOFILE; PrivateTmp;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Isolate and shape I/O per tenant on the NVMe device using cgroup v2 io.max/io.weight, and implement per-tenant quotas. Tune blk-mq scheduler (mq-deadline/bfq), set per-tenant queue affinities for NVMe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Configure per-tenant I/O quotas using cgroups v2 with the BFQ scheduler, bind heavy tenants to dedicated NUMA nodes, and map devices to per-tenant cgroups. Instrument with blktrace, iostat, and fio for comprehensive obs…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates practical I/O isolation and performance tuning in multi-tenant Linux environments, focusing on tail latency reduction while maintaining isolation guarantees under bursty workloads.\n\n## Key Concepts\n- **Per-tenant I/O throttling**: cgroups v2 io.max for bandwidth limits or BFQ for latency-aware scheduling\n- **NUMA-aware I/O paths**: Binding tenant workloads to NUMA-local storage controllers to minimize cross-node traffic\n- **Observability**: blktrace for block layer tracing, iostat for throughput and latency metrics, fio for benchmark validation\n- **Staged rollout**: Deploy changes incrementally with automated rollback triggers based on predefined latency thresholds\n\n## Diagnosis Approach\n1. **Baseline measurement**: Use iostat and blktrace to establish current latency distribution and identify peak contributors\n2. **Tenant isolation verification**: Confirm cgroup boundaries are properly enforced and no cross-tenant I/O interference exists\n3. **NUMA topology analysis**: Map storage devices to NUMA nodes and identify misaligned workloads\n\n## Implementation Plan\n1. Create per-tenant cgroups and assign I/O weight limits using BFQ\n2. Pin tenant workloads to NUMA-local CPUs and storage paths\n3. Deploy incremental changes with continuous monitoring\n4. Roll back automatically if tail latency exceeds thresholds",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-7",
        "questionId": "q-30032",
        "question": "On a Linux host running a multi-tenant data-ingest service in containers, tail latency spikes under bursty traffic are traced to per-tenant BPF map contention in an eBPF observability stack. Design a production plan to diagnose and reduce BPF overhead without disabling tracing. Include per-tenant map sizing and access patterns, per-tenant pinning, map lifecycle management, tainting tenants, staged load tests with rollback, and verification strategies using bpftool, perf, and tc?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Intel RDT to carve LLC with per-tenant classes and MBA budgets, pin builds to reserved cores and NUMA nodes, enforce memory/io quotas via cgroup v2, and attach a cache-aware scheduler hint. Monito",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create per-tenant cgroup v2 layout for cpu, memory, and io; pin the artifact uploader to dedicated CPUs via a per-tenant cpuset; bind uploader and builds to a specific NUMA node; configure IO quotas (",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin by profiling with bpftool and perf to locate hot BPF maps and contention per tenant. Increase per-tenant map sizes and optimize access patterns. Pin BPF programs to dedicated CPUs, isolate traci",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Begin by instrumenting ARC/L2ARC with kstat to track hits, misses, and per tenant memory usage. Then tune zfs_arc_min and zfs_arc_max to a safe fraction of RAM, cap L2ARC size, and consider disabling",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to diagnose low-level kernel tracing overhead in a real multi-tenant production system, balancing observability with performance.\n\n## Key Concepts\n- BPF map contention in multi-tenant workloads\n- Per-tenant isolation and lifecycle of BPF resources\n- CPU pinning, cpuset, and tracing impact\n- Verification with bpftool, perf, and tc\n\n## Code Example\n```bash\n# snapshot current maps and programs\nbpftool map show\nbpftool prog show\n# monitor performance counters during burst\nperf stat -e cycles,instructions -p $(pidof <trace-worker>)\n```\n\n## Follow-up Questions\n- How would you adjust if tenants have unpredictable burst patterns?\n- What trade-offs exist between per-tenant maps vs a shared map in terms of complexity and performance?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-8",
        "questionId": "q-17816",
        "question": "On a Linux host serving multiple tenants via systemd-nspawn containers, a single tenant's cronjobs spawn thousands of processes, pushing the system toward the per-tenant PID limit and delaying logins for others. Design a beginner-friendly plan to enforce per-tenant process limits using cgroups v2 and the pids controller, without modifying tenant applications. Include tenant isolation method, configuration steps, a simple test plan, and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create per-tenant systemd.slice (tenant-A.slice) and run each tenant's container/service under that slice. Set a finite per-tenant pid limit (pids.max) e.g., 200. Add a simple monitor script that chec",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Isolate clocks per tenant via Linux time namespaces; pin containers to a PHC-backed clock from the NIC; run per-tenant ptp4l with its own grandmaster and hardware timestamping; use chrony in bounded-s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant Intel CAT cache partitions with NUMA-aware CPU pinning; bind each tenant's build workers to dedicated cores; enforce per-tenant memory and I/O limits via cgroup v2 controllers (memory.max, io.max, p…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Begin diagnosis by collecting CPU behavior data using turbostat, mpstat, and cpupower frequency-info to confirm the active governor and identify frequency scaling patterns. Once the baseline is established, create per-t…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical use of the cgroups v2 pid controller for per-tenant isolation, a common operational risk in multi-tenant Linux hosts. It checks understanding of systemd slice assignment and safe rollback.\n\n## Key Concepts\n- cgroups v2 unified hierarchy\n- pid controller (pids.max)\n- systemd.slice and container ownership\n- basic monitoring and rollback strategies\n\n## Code Example\n```bash\n# Create and configure a per-tenant slice (focus: pid limit)\nsudo mkdir -p /sys/fs/cgroup/system.slice/tenant-A.slice\necho 200 | sudo tee /sys/fs/cgroup/system.slice/tenant-A.slice/pids.max\n# Run container under the slice (example using systemd-run)\nsystemd-run --slice=tenant-A.slice --unit tenant-A-container -p MemoryLimit=256M /bin/bash -lc 'sleep 1000'\n```\n\n## Follow-up Questions\n- How would you handle tenants that need occasional bursts without permanent throttling?\n- How would you integrate this with existing monitoring to auto-rollback after a threshold breach?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-9",
        "questionId": "q-20418",
        "question": "On a Linux host running a multi-tenant streaming event processor with dozens of containers, time synchronization events (NTP/PTP) occasionally cause clock drift within tenants, breaking ordering guarantees. Design a production plan to bound per-tenant latency and preserve event ordering during time-sync adjustments. Include hardware clock setup (PHC), per-tenant time namespaces or clock isolation, ptp4l/chrony configuration, kernel time discipline, observability, and a staged rollout with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Isolate clocks per tenant via Linux time namespaces; pin containers to a PHC-backed clock from the NIC; run per-tenant ptp4l with its own grandmaster and hardware timestamping; use chrony in bounded-s",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Outline a concrete plan to mitigate memory pressure and THP fragmentation causing tail latency in a multi-tenant container stack. Include: per-tenant memory limits (memory.max, memory.high), explicit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enable AppArmor confinement for two services: nginx and a custom worker. Commands: sudo apt-get update && sudo apt-get install apparmor apparmor-utils; sudo systemctl enable apparmor; sudo aa-status;",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a zero-downtime kernel update plan using live patching (kpatch/kgraft or distro Livepatch). Include patch discovery, staged canary, automated validation (crash-free boot, perf tests), rolling r",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTime-sync-induced drift can break event ordering in multi-tenant workloads. The plan tests practical low-level clock isolation and production-safe rollout strategies.\n\n## Key Concepts\n- Time namespaces and per-tenant clock isolation\n- Hardware clock (PHC) and PCIe NIC timestamping\n- ptp4l and chrony configuration for bounded steps\n- Kernel time discipline and clocksource visibility\n- Observability hooks: phc drift, TSC, clockstats\n- Canary rollout and rollback safety\n\n## Code Example\n```bash\n# Illustrative: start per-tenant PTProc and PHC-bound clock (conceptual)\nptp4l -i eth0 -m -s 1\nchronyd -f /etc/chrony.d/tenant1.conf\n```\n\n## Follow-up Questions\n- How would you simulate misordering to validate the rollback plan?\n- What metrics signal PHC failure and trigger fallback to host-wide clock?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-10",
        "questionId": "q-29800",
        "question": "On a Linux host with a single service writing to /srv/app/logs, implement a practical log rotation plan using logrotate with daily rotation, size 100M, gzip compression, keep last 7 rotations, create new log file if missing, and reload the service after rotation. Provide exact config, testing steps, and rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "On a Linux host serving a shared FUSE-based artifact cache mounted at /cache, hundreds of tenants fetch small files in bursts, causing latency spikes. Design a beginner-friendly plan to diagnose and m",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Increase nf_conntrack capacity and apply per-source limits to relieve pressure. Steps: 1) Monitor current usage: `cat /proc/net/nf_conntrack | wc -l`; 2) Scale capacity: `sysctl -w net.netfilter.nf_conntrack_max=524288`…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a logrotate config at /etc/logrotate.d/app for /srv/app/logs/*.log with daily, size 100M, rotate 7, compress, delaycompress, missingok, notifempty, and a postrotate block that reloads the servi",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "On the host, ensure chrony is syncing with low drift (chronyc tracking; sources -v). Prohibit NTP daemons inside containers; containers share host time. Verify with 'date' in 5 tenants during bursts.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical log rotation planning, reliability during rotation, and rollback capability.\n\n## Key Concepts\n- logrotate syntax and directives\n- postrotate/endscript for service reload\n- dry-run (-d) and force rotate (-f) testing\n- rollback by config restoration and log state\n\n## Code Example\n```bash\n/srv/app/logs/*.log {\n  daily\n  size 100M\n  rotate 7\n  compress\n  delaycompress\n  missingok\n  notifempty\n  create 0640 root root\n  postrotate\n    systemctl reload your-service >/dev/null 2>&1 || true\n  endscript\n}\n```\n\n## Follow-up Questions\n- How would you extend to multi-tenant logs?\n- How would you monitor disk usage and alert on nearing quota?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-11",
        "questionId": "q-22716",
        "question": "On a Linux host running a multi-tenant CI/artifact-build fleet, dozens of containers share a single NVMe pool. Bursts cause tail latency spikes in artifact upload/download due to IO contention. Design a production plan to guarantee fair I/O and maintain throughput per tenant. Include per-tenant cgroup v2 io.max quotas, tc shaping, per-tenant CPU pinning and NUMA isolation, blk-mq tuning, instrumentation (iostat, blktrace, pidstat), and a staged load-test with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant I/O quotas with cgroup v2 io.max, add per-tenant tc shaping and optional IO-priority, pin tenants to dedicated CPUs and NUMA nodes, and tune blk-mq and the IO scheduler (bfq/kyber",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Configure logrotate with copytruncate (or a postrotate that signals the process to reopen logs) and rotate daily or at 100M, keeping 7 archives. Move logs to a dedicated partition; enable Restart=on-f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Bind each tenant’s containers to a dedicated cpuset tied to its NUMA node, and constrain memory with cgroups v2. Add a lightweight watchdog (pidstat/mpstat) that detects sustained >80% CPU for 60s and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Per-VM caps and headroom via cgroup v2: memory.max and memory.high per VM plus a low vm.swappiness to delay swap pressure. Keep ballooning enabled but hard-cap host memory pressure so reclamation happ",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical mastery of I/O fairness in a real multi-tenant setting, including unified cgroups, network shaping, and NUMA considerations. It also probes observability, testing discipline, and rollback discipline.\n\n## Key Concepts\n- cgroup v2 io.max and io.prio for per-tenant quotas\n- tc shaping and IO scheduler tuning (bfq/kyber)\n- CPU pinning and NUMA-aware layout\n- blk-mq tuning and per-tenant isolation\n- Observability: iostat, blktrace, pidstat, fio/ioping\n\n## Code Example\n```bash\n# Create per-tenant cgroup and set IO max (reads/writes per second)\nmkdir -p /sys/fs/cgroup/tenant-A\necho \"read_iops=3000 write_iops=2000\" > /sys/fs/cgroup/tenant-A/io.max\n# Pin a process (PID 12345) to a dedicated CPU and NUMA node\necho 2 > /sys/fs/cgroup/tenant-A/cpuset.cpus\necho 0 > /sys/fs/cgroup/tenant-A/cpuset.mems\necho 12345 > /sys/fs/cgroup/tenant-A/tasks\n```\n\n## Follow-up Questions\n- How would you monitor for IO starvation and which metrics would trigger rollback?\n- What are the risks of overly aggressive quotas and how would you mitigate them during rollout?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-12",
        "questionId": "q-10093",
        "question": "On a Linux host with two NUMA sockets running a multi-tenant microservice mesh, tail RPC latency spikes occur when tenants compete for CPU due to scheduler wakeups from C-states. Design a production plan to minimize jitter while preserving throughput. Include CPU isolation (nohz_full, isolcpus), Turbo control, CPU frequency governor, scheduler tuning, observability, and a staged test/rollback with synthetic and real traffic?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant Intel CAT cache partitions with NUMA-aware CPU pinning; bind each tenant's build workers to dedicated cores; enforce per-tenant memory and I/O limits via cgroup v2 controllers (memory.max, io.max, p…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Apply per-service memory controls and OOM protection. Set MemoryLimit=2G, MemorySwapMax=2G, OOMScoreAdjust=-100, and Restart=on-failure with a 5s backoff. Tune vm.swappiness=10 and ensure swap is enab",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Isolate latency-sensitive tenants to dedicated CPUs using nohz_full and isolcpus, pin workloads with taskset, keep background services on the rest; disable Turbo, set performance governor, and tune th",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Configure tc with HTB: set a root qdisc on eth0, create 4 classes (1:10–1:40) each with a rate of 200 Mbps. Attach fq_codel to each class for low-latency packet scheduling. Use net_cls cgroup-based tc filters to map ten…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nExamines practical mastery of Linux scheduling at scale, NUMA awareness, and production-readiness practices for latency-sensitive tenants.\n\n## Key Concepts\n\n- CPU isolation: nohz_full, isolcpus, cpuset binding\n- Power tuning: disable Turbo, governor=performance\n- Scheduler and RCU: CFS tunables, RCU tuning for reduced wakeups\n- Observability: perf, ftrace, fstrim traces, wakeup latencies\n- Validation: staged load tests with synthetic and real traffic; rollback by reverting CPU sets and governor\n\n## Code Example\n\n```javascript\nfunction plan() {\n  return [\n    'isolate_latency_tenants()',\n    'bind_workloads()',\n    'set_governor(\"performance\")',\n    'disable_turbo()',\n    'tune_scheduler()',\n    'start_perf_trace()',\n    'run_staged_tests()',\n    'rollback_if_needed()'\n  ];\n}\n```\n\n## Follow-up Questions\n\n- How would you verify no regressions in throughput after isolation?\n- What metrics and thresholds would trigger rollback without impacting tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-13",
        "questionId": "q-18832",
        "question": "On a Linux host running a real-time data pipeline using SCHED_DEADLINE to guarantee per-tenant latency, tail latency spikes appear under peak load due to mis-tuned deadline parameters and per-tenant CPU quotas. Design a production plan to ensure deterministic latency while preserving throughput. Include: per-tenant CPU pinning (cpuset) and cgroup v2 quotas, NUMA bindings, tuning sched_latency_ns and sched_min_granularity_ns, CPUfreq policy, IRQ affinity, observability (perf, turbostat, bpftrace), and a staged load-test with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Isolate I/O per tenant using dedicated NVMe namespaces or logical volumes, apply per-tenant blkio/cgroup v2 I/O throttling and weights, and configure an I/O scheduler with per-tenant weighted fairness (BFQ or mq-deadlin…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose isolating tenants with cgroup v2, pinning CPU and memory resources, and comparing ext4 (with dir_index) vs XFS metadata performance under mixed workloads. Tune directory indexing and inode/dentry cache parameter…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: confirm a time sync service is active (chrony or systemd-timesyncd), verify drift with chronyc tracking and sources, ensure NTP servers are reachable, then resync and revalidate. Inspect current",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A strong answer notes per-tenant CPU pinning via cpuset, strict cgroup v2 quotas, and NUMA-aware memory policies; tunes sched_latency_ns and sched_min_granularity_ns to the worst-case deadline across tenants; sets CPUfr…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to reason about real-time Linux scheduling, tenant isolation, and observability under pressure. It probes SCHED_DEADLINE parameter understanding, cpuset/cgroup v2 usage, NUMA-awareness, I/O/power considerations, and phased validation with rollback.\n\n## Key Concepts\n- SCHED_DEADLINE real-time guarantees\n- Per-tenant isolation (cpuset, cgroup v2)\n- CPU frequency/power policy\n- IRQ affinity and NUMA bindings\n- Observability (perf, turbostat, bpftrace)\n\n## Production Implementation Plan\n\n### Phase 1: Isolation Setup\n1. **CPU Pinning**: Use cpuset cgroups to dedicate exclusive CPU cores to each tenant, preventing cross-tenant preemption. Configure cgroup v2 CPU quotas (cpu.max) to enforce per-tenant CPU limits, ensuring no tenant can monopolize available cycles.\n2. **NUMA Binding**: Pin tenant memory allocations to NUMA nodes local to their assigned CPUs using numactl or cpuset memory policies, minimizing remote memory access latency.\n\n### Phase 2: Scheduler Tuning\n1. **sched_latency_ns**: Set to the worst-case deadline across all tenants (e.g., 10ms for a 100Hz deadline). This ensures the scheduler period accommodates all deadline tasks.\n2. **sched_min_granularity_ns**: Set equal to or slightly below the smallest tenant deadline to prevent finer-grained slicing that could cause deadline misses.\n3. **SCHED_DEADLINE Parameters**: Configure each tenant's Runtime (runtime), Period (period), and Deadline (dl_period) to match their latency SLOs using chrt or systemd annotations.\n\n### Phase 3: CPU and IRQ Configuration\n1. **CPUfreq Policy**: Set cpufreq to performance mode on isolated cores to eliminate frequency scaling latency. Disable turboboost if deterministic latency is prioritized over peak performance.\n2. **IRQ Affinity**: Pin network and storage IRQs to dedicated housekeeping cores, keeping tenant workloads free from IRQ preemption. Use irqbalance with masking or manual smp_affinity writes.\n\n### Phase 4: Observability Stack\n1. **perf**: Profile CPU cycles and identify scheduling delays with `perf sched latency` and `perf record -g` for flame graphs.\n2. **turbostat**: Monitor CPU frequency, C-state residency, and package power to detect frequency scaling events correlating with latency spikes.\n3. **bpftrace**: Instrument scheduler hooks (sched_switch, sched_wakeup) to trace per-tenant latency distributions and identify offending workloads.\n\n### Phase 5: Staged Load Test with Rollback\n1. **Baseline**: Measure latency percentiles (p50, p99, p99.9) under controlled load before changes.\n2. **Staged Rollout**: Increment load in stages (25%, 50%, 75%, 100%), monitoring latency histograms and error rates at each step.\n3. **Rollback Triggers**: Define thresholds (e.g., p99.9 exceeds 2x SLO) to automatically revert configuration changes via versioned Ansible/Terraform state.\n4. **Post-Mortem**: Analyze bpftrace/perf data to identify root causes before re-attempting tuning.",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-14",
        "questionId": "q-20290",
        "question": "On a Linux host running a shared CI runner farm for multiple teams, aggressive CPU bursts from one tenant occasionally cause latency spikes for others. Design a beginner-friendly plan to diagnose and mitigate CPU contention without modifying tenant jobs. Include steps for measuring CPU steal, isolating tenants with cgroups v2/systemd, configuring per-tenant CPU quotas, baseline tests, and a rollback path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Isolate tenants with per-tenant io_uring contexts or shard SQ/CQ; tailor SQ/CQ depths to burst profiles; pin worker threads to dedicated CPU rings; cap bandwidth with cgroup v2 io.max; align IRQs to t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Map tenants to separate cgroup v2 slices (e.g., tenantA.slice). First, establish a baseline of normal CPU usage using standard Linux tools such as top, pidstat, and sar. Then apply per-slice CPU controls: use cpu.max fo…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-agent isolation by running each CI agent in its own systemd unit with PrivateDevices, PrivateTmp, and ReadOnlyPaths; mount /var/log/ci as a dedicated volume with quotas; use logrotate pe",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Baseline with mpstat -P ALL 1 5 and iostat to confirm steal and I/O contention. Isolate tenants with cgroups v2: create per-tenant slices (tenant-a.slice, tenant-b.slice) and assign cpu.max or CPUQuot",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests basic performance diagnosis, tooling, and practical containment without touching tenant apps.\n\n## Key Concepts\n\n- Linux CPU contention and CPU steal\n- cgroups v2 and per-tenant isolation\n- systemd.slice and CPU quotas\n- baseline benchmarking and rollback\n\n## Code Example\n\n```javascript\n// Node.js snippet to read tenant quotas\nconst fs = require('fs');\n['tenant-a.slice/cpu.max','tenant-b.slice/cpu.max'].forEach(p=>{\n  try { console.log(p, fs.readFileSync(`/sys/fs/cgroup/${p}`, 'utf8').trim()); } catch{}\n});\n```\n\n## Follow-up Questions\n\n- How would you validate tests without impacting production workloads?\n- What metrics would you alert on to catch regressions?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-15",
        "questionId": "q-15497",
        "question": "On a Linux host running dozens of containerized data-processing jobs sharing a single NVMe pool, tail I/O latency spikes during bursts. Design a production plan to guarantee per-tenant I/O latency isolation for a critical tenant: specify per-tenant cgroup v2 io.max and io.weight, per-device throttling, and I/O scheduler tuning (mq-deadline vs bfq); consider per-NUMA binding if available. Include observability (iostat, blktrace, perf) and a testing plan using fio bursts plus a rollback path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use XDP in native mode at ingress with a per-tenant BPF hash map (tenant_id -> token bucket). Forward if bucket>0 (atomically decremented); otherwise XDP_DROP or rate-limit. Update quotas live from us",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Apply per-tenant I/O quotas with cgroup v2 io.max/io.weight on the shared NVMe, assign the critical tenant to its own IO queue and optionally a dedicated NUMA node. Use per-device throttling to cap ot",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Run iostat -dx 1 to identify high await and queue depth, and use blktrace/blkparse to attribute latency to devices. Switch I/O scheduler between mq-deadline and kyber by writing to /sys/block/<dev>/qu",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Configure logrotate with copytruncate (or a postrotate that signals the process to reopen logs) and rotate daily or at 100M, keeping 7 archives. Move logs to a dedicated partition; enable Restart=on-f",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess practical I/O QoS design in multi-tenant Linux environments, focusing on storage path isolation, what to measure, and how to rollback safely.\n\n## Key Concepts\n- cgroups v2 IO controller: io.max and io.weight for per-tenant limits\n- Block IO schedulers: mq-deadline vs bfq for latency-sensitive workloads\n- Per-device throttling: isolating tenants on a shared NVMe\n- NUMA binding: potential performance gains for I/O paths\n- Observability: iostat, blktrace, perf to trace latency and queueing\n\n## Code Example\n```bash\n# Illustrative commands (adjust per kernel version and device)\n# Create per-tenant IO controller\nsudo mkdir -p /sys/fs/cgroup/io/tenant-critical\n# Bind device 8:0 and set limits (example values)\n# echo \"8:0 rbps=10485760 wbps=10485760\" | sudo tee /sys/fs/cgroup/io/tenant-critical/io.max\n# Attach container to the cgroup (pseudo)\nsudo cgclassify -g io:tenant-critical <pid>\n```\n\n## Follow-up Questions\n- How would you validate latency isolation under burst traffic and rolling failover?\n- What are the trade-offs of using bfq vs mq-deadline in this scenario?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-16",
        "questionId": "q-29172",
        "question": "On a Linux host serving a shared data lake, hundreds of tenants perform file operations in /data/tenants/*; security auditing via auditd must log critical actions (create, delete, chown) but keep CPU overhead under 3-5% during peak bursts. Propose concrete audit rules, per-tenant rate limits, log rotation, and a rollback/test plan using ausearch, aureport, and fio/iostat to validate performance impact?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement targeted audit rules for critical file operations under `/data/tenants/*` using the watch directive to minimize overhead: ```bash auditctl -w /data/tenants -p wa -k tenant_changes ``` Configure a global rate l…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Set up a root HTB qdisc on the interface; create per-tenant classes with fixed rates (e.g., 5mbit, 3mbit, and 1mbit for management) and attach filters by source IP or netns. Enable basic ingress polic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set a per-tenant swap max using cgroups v2 memory.swap.max; lower vm.swappiness to 10; enable zram (2–4GB) to absorb bursts; run a controlled stress test per tenant and monitor swap in/out and latency",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Label tenants with net_cls and apply per-tenant HTB qdiscs on the egress interface, capping rate and setting a latency budget per class. Deploy an XDP program to monitor per-tenant queue depths and re",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question evaluates practical auditing configuration on multi-tenant Linux hosts with strict performance constraints. It tests understanding of auditd internals, rate limiting strategies, and empirical validation methods.\n\n## Key Concepts\n- **Targeted audit rules**: The watch directive (`-w path -p permissions`) applies rules only to specific paths, minimizing CPU overhead compared to broad syscall monitoring\n- **Key-based filtering**: Key prefixes (`-k`) enable efficient post-hoc filtering with `ausearch` and `aureport` without kernel-level per-tenant rules\n- **Rate limiting**: The `rate_limit` setting in `auditd.conf` caps kernel-to-user-space event delivery, preventing auditd from becoming a bottleneck during burst activity\n- **Disk protection**: The `admin_space_left_action = suspend` setting ensures the system remains operational even if audit logging consumes all available disk space\n\n## Evaluation Criteria\nStrong answers demonstrate:\n1. Knowledge of `-w` and `-k` flags for efficient rule construction\n2. Understanding of auditd rate limiting and queue behavior\n3. Ability to validate performance impact using standard Linux monitoring tools\n4. Evidence of operational safety via rollback procedures",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-17",
        "questionId": "q-11630",
        "question": "On a Linux host running a multi-tenant streaming analytics pipeline that uses io_uring for asynchronous file I/O to a single NVMe pool, tail latency spikes under micro-bursts due to per-tenant starvation in submission/completion queues. Design a production plan to diagnose and reduce tail latency while preserving throughput. Include per-tenant io_uring rings, submission/completion ring sizing, I/O bandwidth quotas, filesystem considerations, observability, and a staged test/rollback plan with realistic micro-bursts?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect baseline latency and per-tenant traffic using ss, iostat, and tcpdump. Implement per-tenant HTB with fq_codel on the gateway’s egress to cap bandwidth and reduce queue buildup, ensuring no app",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant io_uring rings with isolated submission/completion queues, bound to tenant cgroups v2 IO limits, and fixed ring sizes to prevent starvation. Enforce per-tenant bandwidth quotas on",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Patch kernel live with Kpatch/KSPP. Build patch, deploy to 1 canary host, run representative load for 2–4 hours, monitor PMU counters, dmesg, and latency; if clean, expand to 25%, then full fleet with",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Isolate latency-sensitive tenants to dedicated CPUs using nohz_full and isolcpus, pin workloads with taskset, keep background services on the rest; disable Turbo, set performance governor, and tune th",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical mastery of io_uring in multi-tenant settings, including isolation, QoS, and observability under bursty workloads. It probes the ability to translate theory into a concrete production plan with testing and rollback.\n\n## Key Concepts\n\n- io_uring architecture: SQ/CQ rings, submission/completion paths\n- Per-tenant isolation: separate rings, cgroups v2 io.max\n- Bandwidth QoS on NVMe and IRQ isolation\n- Observability: latency percentiles, CQ occupancy, queue depth, IRQ counts\n- Validation: realistic micro-bursts with fio; staged rollouts\n\n## Code Example\n\n```bash\n# Example: create per-tenant IO cgroup and limit throughput\nsudo mkdir -p /sys/fs/cgroup/io/tenant01\necho \"8:0 50M\" > /sys/fs/cgroup/io/tenant01/io.max\n```\n\n## Follow-up Questions\n\n- How would you adapt if multiple NVMe namespaces are shared across tenants?\n- What changes if tenant workloads skew toward metadata vs. streaming data?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-18",
        "questionId": "q-22848",
        "question": "On a Linux host running a multi-tenant CI/CD farm with hundreds of containers sharing a single NVMe SSD, tail latency spikes occur during simultaneous artifact packaging tasks. Design a production plan to diagnose and eliminate I/O starvation while preserving throughput. Specifically address: per-tenant IO prioritization using cgroups v2 io.weight and io.max, NVMe multi-queue depth tuning, BPF-based visibility into block I/O latency per cgroup, per-tenant cache and bandwidth isolation, and a staged load-test with rollback capability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Production plan to guarantee deterministic timekeeping and per-tenant deadline isolation on a Linux single-node control plane: Configure stable clocksource (prefer invariant TSC, fallback to HPET), implement PTP for pre…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "## Production Plan: Diagnosing and Eliminating I/O Starvation in Multi-Tenant CI/CD Farm ### Phase 1: Baseline Observability with BPF Deploy BPF programs to capture per-cgroup I/O latency before applying controls: ```c…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Develop a low-overhead eBPF tracing plan to profile container lifecycle events (namespace creation, cgroup setup, mounts, network namespace) during startup bursts. Tag events with per-tenant identifie",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: IEEE 1588v2 with hardware timestamping. Use PHC on NICs, map to /dev/ptp0, run linuxptp ptp4l as a boundary clock with a robust grandmaster, and chronyd in PT mode for precise discipline. Prefer",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Matters\n\nThis tests deep Linux I/O subsystem expertise: cgroups v2 controllers, block layer internals, BPF observability, and safe deployment practices. Multi-tenant CI/CD farms are increasingly common, making I/O isolation a critical production skill.\n\n## Key Concepts Demonstrated\n\n### cgroups v2 I/O Controllers\ncgroups v2 provides two complementary mechanisms:\n\n**io.weight** (`/sys/fs/cgroup/.../io.weight`): Proportional bandwidth sharing. Weight 300 means 3x more I/O opportunity than weight 100. Unlike io.bfq.weight in cgroups v1, io.weight applies to all devices in a unified hierarchy.\n\n**io.max** (`/sys/fs/cgroup/.../io.max`): Absolute limits in IOPS and bytes/sec. Format: `<major:minor> <IOPS> <bandwidth>`. Exceeding io.max causes throttling (not rejection), maintaining fairness under contention.\n\n### NVMe Queue Tuning\nModern NVMe SSDs support thousands of parallel queues. Key parameters:\n- `nr_requests`: Controls how many requests queue per block device\n- `scheduler`: Should be \"none\" for NVMe (no seek time optimization)\n- `write_cache_threshold`: Controls when writes flush to media\n\n### BPF Observability\nBPF programs attached to block layer tracepoints capture per-cgroup I/O patterns without the overhead of full blktrace. The histogram approach efficiently buckets latencies for quick P50/P95/P99/P999 analysis.\n\n### Staged Rollout Pattern\nProduction changes to I/O limits require careful validation:\n1. **Backup**: Preserve current state before any change\n2. **Canary**: Apply to subset of tenants first\n3. **Monitor**: Measure actual latency during realistic load\n4. **Validate**: Compare against SLOs before full rollout\n5. **Rollback**: Automated reversion if thresholds exceeded\n\n## Common Mistakes to Avoid\n\n1. **Confusing io.weight with io.max**: io.weight is proportional (relative), io.max is absolute. Use both together—io.max prevents runaway tenants, io.weight distributes remaining capacity fairly.\n\n2. **Forgetting device major:minor**: io.max requires the actual device identifier, not just the device name.\n\n3. **Setting nr_requests too high**: Excessive queue depth increases latency without throughput gains. Profile empirically.\n\n4. **Ignoring memory pressure**: I/O throttling under memory pressure differs from pure I/O limits. Monitor memory.low settings.\n\n5. **Rollback without measurement**: Always validate that rollback actually improved latency, not just that you restored old settings.",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-19",
        "questionId": "q-7702",
        "question": "A multi-tenant Linux node runs a shared NFSv4.1 export consumed by hundreds of pods. During peak load, file operations on the export stall 2–5 seconds sporadically, while network and CPU show no obvious bottlenecks. Design a production plan to diagnose and mitigate NFS stall latency without sacrificing consistency. Include tracing plan, mount/fs options, caching/isolation strategies, per-tenant QoS, and a test and rollback plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use nfsiostat and perf plus eBPF to trace stall sources and confirm if NFS lease contention or server I/O is the culprit. Mitigations: split into per-tenant subexports to isolate locks; upgrade to NFS",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-tenant io_uring rings with isolated submission/completion queues, bound to tenant cgroups v2 IO limits, and fixed ring sizes to prevent starvation. Enforce per-tenant bandwidth quotas on",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement targeted audit rules for critical file operations under `/data/tenants/*` using the watch directive to minimize overhead: ```bash auditctl -w /data/tenants -p wa -k tenant_changes ``` Configure a global rate l…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Isolate replication and audit traffic to dedicated NICs/queues; enable mqprio on both NICs; tag replication/audit with net_cls and pin their threads to isolated CPUs; apply tc clsact for upstream/down",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical diagnosis of production NFS latency in a multi-tenant Kubernetes context, focusing on real observability and safe isolation without breaking data correctness.\n\n## Key Concepts\n- NFSv4.1 leases, locking, and per-export isolation\n- Observability: nfsiostat, iostat, perf, and eBPF tracing\n- Tenant QoS via per-export subtrees and server quotas\n- Safe rollback and staged testing with realistic bursts\n\n## Code Example\n```javascript\nnfsiostat -m -d /mnt/nfs-export\n```\n\n## Follow-up Questions\n- How would you extend this to a mixed on-prem/cloud environment?\n- What metrics would you alert on to catch regressions early?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-20",
        "questionId": "q-10699",
        "question": "On a Linux host serving a multi-tenant in-memory cache gateway with thousands of tenants, tail latency spikes to 200 ms during micro-bursts when tenants share a single NIC path. Design a production plan to diagnose and eliminate unfairness while preserving throughput. Include: NUMA topology adjustments, per-tenant memory and CPU isolation (cgroups v2), per-tenant XDP/BPF rate limiting, observability, and a staged test/rollback plan with synthetic workloads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Collect metrics (iostat, sar, and ifstat) during bursts to identify the tenant. Tag containers with net_cls, create a root qdisc and per-container classes using tc with ht b or cbq, cap bandwidth per",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Run df -i /scratch to confirm inode saturation; create per-tenant subdirs under /scratch; enable quotas (edquota -u tenant) and quotaon; set block and inode limits; add a lightweight cleanup script; s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "My approach combines NUMA-aware tenant isolation with CPU pinning, per-tenant memory constraints using cgroups v2's memory.max and cpu.max controllers, and XDP/BPF-driven rate limiting at the NIC layer to eliminate unfa…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Isolate the critical I/O on dedicated CPUs and NVMe channel. Bind ingest threads to rt cpus via cpuset; boot with isolcpus,nohz_full; pin NVMe IRQs to those CPUs; use cgroup v2 io.max to cap tenants a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes a production-grade fairness problem in shared NIC environments, requiring concrete, testable mitigations across memory, CPU, and network layers.\n\n## Key Concepts\n\n- NUMA-aware tenant isolation\n- cgroups v2 memory.max and cpu.max\n- Per-tenant XDP/BPF rate limiting\n- Observability: perf, bpftool, iostat, sar\n- Canary rollout and rollback strategy\n\n## Code Example\n\n```javascript\n// Pseudo eBPF skeleton for per-tenant rate limiting\n// Note: real BPF code would use kernel headers and maps\n// This is illustrative for interview context\n```\n\n## Follow-up Questions",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-21",
        "questionId": "q-4020",
        "question": "On a Linux host running a multi-tenant data-collection service, tail latency spikes appear under 99th percentile during bursts. Outline a production plan to diagnose and mitigate root causes using eBPF tracing (bpftrace), per-tenant metrics, and strict QoS isolation. Include steps to install tracing, collect sched/net/block latency per tenant, enforce per-tenant cgroups v2 with cpu.max and io.max, and validate with synthetic bursts. What exact steps would you take and how would you revert if latency worsens?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-process and system-wide FD hardening plan: set LimitNOFILE high on each unit, raise fs.file-max, enable systemd TasksMax, isolate services with dedicated cgroups, add a watchdog that restarts on q",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: IEEE 1588v2 with hardware timestamping. Use PHC on NICs, map to /dev/ptp0, run linuxptp ptp4l as a boundary clock with a robust grandmaster, and chronyd in PT mode for precise discipline. Prefer",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument with eBPF (bpftrace) to trace per-tenant sched_latency, net_xmit, and block IO; collect p95/p99 latency per tenant; enforce per-tenant cgroups v2 with cpu.max and io.max; throttle noisy ten",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Pin tenants to dedicated CPU cores and NUMA nodes; deploy XDP/eBPF ingress with per-tenant UMEM rings; assign per-tenant NIC queues and bind IRQs to those cores; disable global irqbalance; enable nohz",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nThis tests real-world ability to diagnose tail latency in multi-tenant Linux environments using advanced tracing, QoS, and safe production changes.\\n\\n## Key Concepts\\n- eBPF tracing (bpftrace)\\n- per-tenant QoS with cgroups v2\\n- latency metrics (p95/p99) and synthetic workloads\\n\\n## Code Example\\n```javascript\\n// illustrative bpftrace snippet (not runnable)\\nBEGIN { /* setup */ }\\nEND { /* teardown */ }\\n```\\n\\n## Follow-up Questions\\n- How would you adapt if tenants share a single NVMe?\\n- How would you calibrate p99 thresholds and alerting?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-22",
        "questionId": "q-6257",
        "question": "On a Linux host running a multi-tenant CI build farm using ZFS on Linux, builds intermittently pause for 50-200ms during peak bursts. The culprit appears to be ARC and L2ARC pressure from aggressive metadata caching. Design a production plan to diagnose and mitigate caching bottlenecks without compromising data integrity. Include ZFS tunables, per-tenant memory isolation, test plan, and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Begin by instrumenting ARC/L2ARC with kstat to track hits, misses, and per tenant memory usage. Then tune zfs_arc_min and zfs_arc_max to a safe fraction of RAM, cap L2ARC size, and consider disabling",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan to diagnose I/O contention: assign per-tenant io_uring queues and cgroup v2 io.max limits; bind tenants to dedicated NUMA nodes; tune blk-mq with per-tenant weights; isolate page cache with zcach",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan uses eBPF: attach probes to blk_start_request, blk_mq_request_issue, and iowait; tag events by cgroup v2 tenant IDs; emit per-tenant latency, stall duration, and queue depth to Prometheus. Enforc",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt per-tenant memory isolation using cgroup v2: set memory.max and memory.high for each tenant’s TLS workers, disable swap (vm.swappiness=0), and pin workers to NUMA nodes. Limit per-tenant page ca",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThe question probes practical cache tuning under multi-tenant load, not basic disk I/O.\n\n## Key Concepts\n- ARC/L2ARC behavior in ZoL\n- tunables: zfs_arc_min/max, zfs_prefetch, vfs_cache_pressure\n- per-tenant isolation via cgroups\n- test plan with synthetic workloads and rollback\n\n## Code Example\n```javascript\n// Example approach to adjust ARC settings\n// (run with caution on live systems)\n```\n\n## Follow-up Questions\n- How would you validate no data loss during rollback?\n- What metrics decide success of tuning?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-23",
        "questionId": "q-26718",
        "question": "On a Linux host running dozens of tenant containers that share a PMEM-backed storage pool, tail latency spikes during coordinated WAL-like writes to a shared directory. Design a production plan to diagnose and reduce tail latency while preserving throughput. Include per-tenant I/O isolation (cgroup v2 I/O, quotas), filesystem tuning (XFS/ext4 with PMEM), memory/CPU pinning, and a staged test/rollback with observability (iostat, blktrace, perf)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Apply per-tenant I/O quotas with cgroup v2 io.max/io.weight on the shared NVMe, assign the critical tenant to its own IO queue and optionally a dedicated NUMA node. Use per-device throttling to cap ot",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan using cgroup v2: create ci.slice with per-service sub-slices (ci-runner-A/B/C). Assign cpu.max and io.max per service; pin CPUs via cpuset and set IRQ affinity. Use BFQ for per-service I/O schedu",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Enable per-tenant quotas on the shared filesystem (ext4/XFS). Add usrquota,grpquota in /etc/fstab for /shared and remount; run quotacheck -cug /shared and quotaon /shared. Use setquota -u tenantA 5000",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A comprehensive approach includes: 1) Apply per-tenant cgroup v2 I/O isolation with `io.max` throttling and blkio quotas to prevent any single tenant from monopolizing the shared PMEM pool; 2) Pin tenant containers to d…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical expertise in storage isolation and virtualization with shared PMEM, requiring concrete, actionable solutions rather than abstract concepts.\n\n## Key Concepts\n- Per-tenant I/O control with cgroup v2 and quotas\n- PMEM and NUMA awareness for latency-sensitive workloads\n- Filesystem tuning for high-throughput shared storage\n- Observability tooling: iostat, blktrace, perf\n- Canary rollout and rollback procedures\n\n## Code Example\n```bash\n# Example baseline commands\n# Set per-tenant I/O throttling (cgroup v2)\necho \"8:0 rbps=max wbps=max riops=max wiops=max\" > /sys/fs/cgroup/<tenant>/io.max\n\n# Pin container to CPU cores and NUMA node\necho \"0-7\" > /sys/fs/cgroup/<tenant>/cpuset.cpus\necho \"0\" > /sys/fs/cgroup/<tenant>/cpuset.mems\n\n# XFS filesystem tuning for PMEM\nmkfs.xfs -d su=2m,sw=1 -m reflink=0,dax /dev/pmem0\nmount -o dax,noatime,nodiratime /dev/pmem0 /mnt/pmem\n```\n\n## Follow-up Questions\n- How would you validate no regressions across tenant workloads after implementing I/O limits?\n- What monitoring alerts would indicate impending tail latency issues?\n- How do you handle tenant churn (container creation/destruction) with cgroup v2 policies?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-24",
        "questionId": "q-6244",
        "question": "On a Linux host serving a multi-tenant FaaS platform where tenants execute untrusted code in lightweight sandboxes, design a production plan to enforce strict per-tenant filesystem and process isolation while preserving latency. Specify: 1) namespace strategy (user, mount, pid, network), 2) per-tenant root layout (chroot/bind mounts/overlay), 3) sandbox escape mitigations (disable setns, tighten CAPs, seccomp-bpf), 4) auditing with auditd and eBPF, 5) test plan including a brute-force escape attempt and rollback, 6) a concrete per-tenant policy example and a runbook. How would you implement this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Instrument with nfsiostat and iostat during backups to separate locking from I/O. Increase NFS server threads (nfsd) and tweak lockd retry behavior; adjust vfs cache parameters and enable noatime on t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant user, mount, pid, and network namespaces; mount a private root via a read-only bind of a tenant base plus an overlay for writable state. Drop capabilities, enable seccomp-bpf, and",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "**TCP Tuning:** ```bash # Socket buffers - allow larger buffers for high-throughput gateways sysctl -w net.core.rmem_max=16777216 sysctl -w net.core.wmem_max=16777216 sysctl -w net.ipv4.tcp_rmem='4096 87380 167772ysctl…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Isolate per-tenant I/O and CPU on a shared NFS-backed build cache: create a cgroup v2 tree for tenants, set io.max to throttle each, assign CI workers to a dedicated CPU set, pin processes with a syst",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical, scalable tenant isolation on Linux hosts, balancing security with latency. It demands concrete, implementable namespace layouts, filesystem strategies, and verification steps.\n\n## Key Concepts\n- Namespaces: user, mount, pid, net\n- Filesystem isolation: per-tenant roots via bind mounts/overlay\n- Security: restricted CAPs, seccomp-bpf, disable setns, module signing\n- Observability: auditd, BPF tracing, runbooks\n\n## Code Example\n```javascript\n// Placeholder: concept sketch only\n```\n\n## Follow-up Questions\n- How would you adapt this for a mixed multi-tenant and dedicated-tenant model? \n- What metrics indicate isolation integrity is compromised, and how would you trigger a rollback?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-04T06:59:08.508Z",
    "version": 1
  }
]