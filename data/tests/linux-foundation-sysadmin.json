[
  {
    "id": "test-linux-foundation-sysadmin",
    "channelId": "linux-foundation-sysadmin",
    "channelName": "Linux Foundation Sysadmin",
    "title": "Linux Foundation Sysadmin Knowledge Test",
    "description": "Test your understanding of Linux Foundation Sysadmin with 25 questions.",
    "questions": [
      {
        "id": "linux-foundation-sysadmin-q-0",
        "questionId": "q-2538",
        "question": "On a Linux host running a multi-tenant data ingestion pipeline and a separate real-time alerting service, ingestion bursts cause alert latency. Propose a production plan to guarantee low-latency alerts while preserving ingestion throughput using: (a) per-service CPU isolation with systemd slices and cpuset, (b) RT scheduling for the alerting process, (c) CPU affinity and NUMA bindings, (d) a validation plan with burst traffic and SLA checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use low-overhead eBPF tracing (BCC/BPFtrace) to instrument the critical latency path. Attach kprobes/tracepoints to scheduler, block IO, and network receive paths; filter by cgroup/docker container ID",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition CPU resources using CGroups v2: allocate cores 0-3 for data ingestion and cores 4-5 for alerting; configure alerting within a dedicated systemd.slice with CPUAffinity=4-5 and CPUQuota=60%; execute alerting wit…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-VM caps and headroom via cgroup v2: memory.max and memory.high per VM plus a low vm.swappiness to delay swap pressure. Keep ballooning enabled but hard-cap host memory pressure so reclamation happ",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Engineer a per-tenant network control path using XDP/eBPF on the host NIC with maps keyed by tenant_id to enforce rate limits and drop policy; pair each tenant to dedicated veth pairs and attach a cls",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical CPU isolation and real-time scheduling implementation in multi-tenant environments.\n\n## Key Concepts\n- CGroups v2 CPU isolation and per-service systemd slices\n- Real-Time scheduling (SCHED_FIFO) for latency-critical workloads\n- CPU affinity and NUMA binding for deterministic performance\n- Validation methodology using burst traffic patterns and SLA compliance\n\n## Code Example\n```bash\n# Conceptual: RT scheduling and CPU isolation for alerting\nsudo systemctl edit alerting.service << 'EOF'\n[Service]\nCPUAffinity=4-5\nCPUQuota=60%\nEOF\n```\n\n## Follow-up Questions\n-",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-1",
        "questionId": "q-22848",
        "question": "On a Linux host running a multi-tenant CI/CD farm with hundreds of containers sharing a single NVMe SSD, tail latency spikes occur during simultaneous artifact packaging tasks. Design a production plan to diagnose and eliminate I/O starvation while preserving throughput. Specifically address: per-tenant IO prioritization using cgroups v2 io.weight and io.max, NVMe multi-queue depth tuning, BPF-based visibility into block I/O latency per cgroup, per-tenant cache and bandwidth isolation, and a staged load-test with rollback capability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt per-tenant isolation: assign each tenant its own BPF map namespace (or tenant_id keys), run a dedicated BPF program instance per tenant, and pin maps to dedicated CPUs to reduce cross-tenant chu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement fixed-core data path: dedicate 6–8 cores on a NUMA node for capture and detection; isolate NIC IRQs and set RPS/RFS to those CPUs; pin XDP/BPF processing to this CPU set; run the critical pa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Production Plan: Diagnosing and Eliminating I/O Starvation in Multi-Tenant CI/CD Farm ### Phase 1: Baseline Observability with BPF Deploy BPF programs to capture per-cgroup I/O latency before applying controls: ```c…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose separate per-tenant log directories on a dedicated log volume (e.g., XFS) with bind mounts or subvols, enable per-tenant quotas, use logrotate with tenant-based rotation sizes, and monitor IO",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Matters\n\nThis tests deep Linux I/O subsystem expertise: cgroups v2 controllers, block layer internals, BPF observability, and safe deployment practices. Multi-tenant CI/CD farms are increasingly common, making I/O isolation a critical production skill.\n\n## Key Concepts Demonstrated\n\n### cgroups v2 I/O Controllers\ncgroups v2 provides two complementary mechanisms:\n\n**io.weight** (`/sys/fs/cgroup/.../io.weight`): Proportional bandwidth sharing. Weight 300 means 3x more I/O opportunity than weight 100. Unlike io.bfq.weight in cgroups v1, io.weight applies to all devices in a unified hierarchy.\n\n**io.max** (`/sys/fs/cgroup/.../io.max`): Absolute limits in IOPS and bytes/sec. Format: `<major:minor> <IOPS> <bandwidth>`. Exceeding io.max causes throttling (not rejection), maintaining fairness under contention.\n\n### NVMe Queue Tuning\nModern NVMe SSDs support thousands of parallel queues. Key parameters:\n- `nr_requests`: Controls how many requests queue per block device\n- `scheduler`: Should be \"none\" for NVMe (no seek time optimization)\n- `write_cache_threshold`: Controls when writes flush to media\n\n### BPF Observability\nBPF programs attached to block layer tracepoints capture per-cgroup I/O patterns without the overhead of full blktrace. The histogram approach efficiently buckets latencies for quick P50/P95/P99/P999 analysis.\n\n### Staged Rollout Pattern\nProduction changes to I/O limits require careful validation:\n1. **Backup**: Preserve current state before any change\n2. **Canary**: Apply to subset of tenants first\n3. **Monitor**: Measure actual latency during realistic load\n4. **Validate**: Compare against SLOs before full rollout\n5. **Rollback**: Automated reversion if thresholds exceeded\n\n## Common Mistakes to Avoid\n\n1. **Confusing io.weight with io.max**: io.weight is proportional (relative), io.max is absolute. Use both together—io.max prevents runaway tenants, io.weight distributes remaining capacity fairly.\n\n2. **Forgetting device major:minor**: io.max requires the actual device identifier, not just the device name.\n\n3. **Setting nr_requests too high**: Excessive queue depth increases latency without throughput gains. Profile empirically.\n\n4. **Ignoring memory pressure**: I/O throttling under memory pressure differs from pure I/O limits. Monitor memory.low settings.\n\n5. **Rollback without measurement**: Always validate that rollback actually improved latency, not just that you restored old settings.",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-2",
        "questionId": "q-4400",
        "question": "On a Linux host, a periodic cleanup script used by a systemd timer occasionally fails because it relies on a writable /tmp that can be cleaned out during bursts. Propose a production-safe migration to a systemd-timer+service with isolation and idempotence. Include concrete unit settings (RuntimeDirectory, PrivateTmp, User/Group, WorkingDirectory, Restart), a minimal script pattern, and a test plan to reproduce and validate reliability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a zero-downtime TLS cert rotation using systemd.path to trigger on an updated certificate symlink, plus a dedicated systemd service that ExecReloads the proxy. Use atomic cert updates (place n",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Pin virtio-blk to dedicated cores, pin QEMU processes with a fixed CPU set, enable NUMA awareness and large pages for guests, isolate devices with vfio-pci, assign IRQs to those CPUs, carve per-guest",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use XDP in native mode at ingress with a per-tenant BPF hash map (tenant_id -> token bucket). Forward if bucket>0 (atomically decremented); otherwise XDP_DROP or rate-limit. Update quotas live from us",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement timer+service as a dedicated cleanup user with PrivateTmp and RuntimeDirectory=cleanup, set WorkingDirectory=/var/lib/cleanup, and Restart=on-failure. The script uses a /var/lib/cleanup/.don",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical systemd isolation, idempotence, and testability for regular tasks.\n\n## Key Concepts\n- systemd timer and service\n- RuntimeDirectory and PrivateTmp\n- Idempotent scripts with marker files\n- Journald-based verification and dry-run testing\n\n## Code Example\n```ini\n[Unit]\nDescription=Periodic cleanup service\n\n[Service]\nUser=cleanup\nGroup=cleanup\nRuntimeDirectory=cleanup\nPrivateTmp=true\nWorkingDirectory=/var/lib/cleanup\nExecStart=/usr/local/bin/cleanup.sh --marker /var/lib/cleanup/.done\nRestart=on-failure\nRemainAfterExit=yes\n```\n\n```ini\n[Timer]\nOnBootSec=5min\nOnUnitActiveSec=1h\nUnit=cleanup.service\n[Install]\nWantedBy=timers.target\n```\n\n## Follow-up Questions\n- How would you audit this over time?\n- What if the cleanup must run as root?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-3",
        "questionId": "q-13985",
        "question": "On a Linux host running a small web stack (Nginx front end, Node.js backend) that serves multiple tenants, bursts of traffic trigger intermittent 'Too many open files' errors and latency spikes. Design a beginner-friendly plan to diagnose and mitigate file descriptor exhaustion without code changes. Include per-tenant quotas via pam_limits, a system-wide file-max tune, systemd resource limits, a simple test plan (lsof, stress tests), and rollback steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Production plan to guarantee deterministic timekeeping and per-tenant deadline isolation on a Linux single-node control plane: Configure stable clocksource (prefer invariant TSC, fallback to HPET), implement PTP for pre…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Audit current file descriptor usage with `lsof` and `/proc/sys/fs/file-nr` to identify usage patterns and baseline consumption. Increase limits systematically: 1. **System-wide limit**: Set `fs.file-max=100000` in `/etc…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Isolate per-tenant I/O and CPU on a shared NFS-backed build cache: create a cgroup v2 tree for tenants, set io.max to throttle each, assign CI workers to a dedicated CPU set, pin processes with a syst",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Pin tenants to dedicated NUMA nodes via cpuset and cgroup v2, set CPU governor to performance, and enforce per-tenant memory.max quotas (memory.max) with memory.low and memory.high. Route IRQs to the",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nFile descriptor exhaustion in multi-tenant Linux hosts represents a common production issue that tests practical operational knowledge. This scenario requires understanding system limits, proper configuration management, and the ability to implement safe changes without application modifications.\n\n## Key Concepts\n\n- **File descriptors**: Kernel-managed integers that reference open files, sockets, or pipes. Each connection consumes at least one FD.\n- **System-wide vs per-process limits**: `fs.file-max` controls kernel-level capacity across all processes; `nofile` controls per-user limits via PAM.\n- **PAM limits**: The `pam_limits` module applies `/etc/security/limits.conf` settings at login, affecting all user sessions.\n- **Systemd resource controls**: `LimitNOFILE` in service unit files constrains FDs for forked processes, independent of user limits.\n- **Diagnostic tools**: `lsof` reveals per-process FD usage; `/proc/sys/fs/file-nr` shows allocated vs. free system-wide descriptors.\n\n## Common Pitfalls\n\n- Setting `fs.file-max` too low blocks all processes from opening files\n- Per-tenant limits must be below system-wide maximum\n- Systemd limits override PAM limits for services but not for user sessions\n- Transient surges may still exceed quotas under extreme load\n\n## Verification Approach\n\nApply changes incrementally: adjust system limit first, then per-tenant, then service-level. Use load testing to validate before production traffic resumes.",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-4",
        "questionId": "q-20835",
        "question": "On a Linux host hosting a distributed multi-tenant web service using a Redis cluster as a shared in-memory cache, hot keys cause cross-node memory pressure and tail latency spikes during bursts. Design a practical plan to diagnose and mitigate cache contention while preserving data consistency and throughput. Include per-tenant cache partitioning, NUMA binding, memory cgroups, Redis clustering, and staged test/rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Start by baseline with mpstat -P ALL 1, pidstat -p ALL 1 and iostat. Use systemd-cgls to identify bottlenecked services. Apply CPU isolation via per-service CPUQuota and, if available, cpuset for crit",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Baseline: run systemd-analyze blame and systemd-analyze critical-chain to identify slow units. Then optimize: set DefaultDependencies=no for independent services; adjust After/Wants to reduce unnecess",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Baseline AVC audit collection (ausearch) to identify hot checks; instrument with perf/ftrace to quantify policy overhead; refactor by introducing per-tenant domains/types, label data paths with tenant",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Start with per-tenant cache segmentation (namespace keys or Redis hash tags) and pin Redis processes to NUMA nodes; enable memory cgroups v2 to cap per-tenant RSS. Use perf/pmu to trace cache misses a",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests advanced Linux sysadmin skills in real-world multi-tenant caching: NUMA-aware memory pressure, per-tenant isolation, and production testing with safe rollback.\n\n## Key Concepts\n\n- NUMA binding and memory policy\n- Per-tenant isolation and quotas\n- Cache contention and eviction strategies\n- Redis clustering and shard placement\n- Observability with perf/pmu/pidstat\n\n## Code Example\n\n```bash\n# Example: pin Redis to NUMA node 0 and limit per-tenant memory\nnumactl --cpunodebind=0 --membind=0 redis-server --port 6379 --cluster-enabled yes\n```\n\n## Follow-up Questions\n\n- How would you measure per-tenant eviction latency under bursts?\n- How would you adapt if a tenant’s data must stay in L1 cache for SLA?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-5",
        "questionId": "q-1954",
        "question": "On a Linux host running three CI runners as systemd services sharing a single NVMe and a 10 Gb NIC, a long-running build on one runner starves CPU and I/O, delaying others. Propose a production plan to guarantee fair CPU and I/O while preserving peak throughput: (a) per-service CPU limits via cgroup v2 and CPU affinity; (b) per-service I/O throttling with io.max and a suitable I/O scheduler (BFQ/mq-deadline); (c) CPU pinning and IRQ isolation; (d) validation with bursts and latency metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a systemd unit with Type=forking, PIDFile, and ExecStartPre that checks and clears a stale lock: if /var/lib/backup/backup.lock exists and its PID is not running, delete it. Stop uses KillMode=con",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "SELinux context mismatches on the shared landing directory are causing AVC denials during burst periods. Plan: 1) Verify SELinux status: `getenforce`, `ls -Z /data/landing`. 2) Analyze audit logs: `ausearch -m avc -ts t…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a fair IO scheduler (BFQ or Kyber) for NVMe; cap per-tenant I/O with cgroup2 io.max/io.weight; pin each tenant to a dedicated CPU set and bind its memory to a NUMA node; tune min_free_kbytes and v",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan using cgroup v2: create ci.slice with per-service sub-slices (ci-runner-A/B/C). Assign cpu.max and io.max per service; pin CPUs via cpuset and set IRQ affinity. Use BFQ for per-service I/O schedu",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical mastery of isolating CPU and I/O in a multi-tenant CI environment under hardware contention, a common production challenge.\n\n## Key Concepts\n- cgroup v2: cpu and io controllers for per-service quotas.\n- systemd slices: grouping services under a parent for unified controls.\n- CPU affinity and cpuset: bind runners to dedicated CPUs.\n- I/O schedulers: BFQ or mq-deadline for per-service throttling.\n- Observability: latency, throughput, tail latency, and backlog.\n\n## Code Example\n```javascript\n# Systemd override example (pseudo)\n[Slice]\nCPUAccounting=true\n\n[Service]\nSlice=ci.slice\nCPUQuota=33%\n```\n```javascript\n# cgroup v2 quota (pseudo)\necho 0.33 > /sys/fs/cgroup/ci.slice/ci-runner-A/cpu.max\n```\n\n## Follow-up Questions\n- How would you monitor and adapt quotas during a bursty workload?\n- What rollback strategy would you use if a quota regresses build reliability?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-6",
        "questionId": "q-1385",
        "question": "On a Linux host running ephemeral build jobs managed by systemd, a rogue job floods /tmp with tiny files, exhausting inodes and delaying builds. Propose a production plan to isolate and bound /tmp usage and auto-clean stale files. Include: (a) per-service PrivateTmp and tmpfs /tmp with a size cap; (b) systemd-tmpfiles cleanup rules; (c) inode usage monitoring; (d) a reproducible test that proves isolation and cleanup. How would you implement this?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pin tenants to dedicated cpusets and memory pools (cpuset v2, memory.max). Bind workloads to a NUMA node, cap CPU frequency with schedutil min/max and disable turbo to prevent spikes. Bind IRQs away f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Baseline latency metrics first; profile XDP vs legacy paths with bpftool or bpftrace to locate hot-tenant map accesses. Implement per-tenant BPF maps, isolate with per-tenant tc qdiscs, and enable fq_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure per-service isolation: set PrivateTmp=true and PrivateDevices=true for build services; mount /tmp as tmpfs with size=128M and mode=1777; add systemd-tmpfiles cleanup rules to prune stale fil",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Create per-tenant tmpfs workspaces mounted under /mnt/tenant-XX/tmp with 128-256MB quotas; auto-create via systemd-tmpfiles and bind-mount into each tenant's workspace. Enforce memory limits with a pe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical isolation of temporary storage for multi-tenant build workloads and how to enforce cleanups under bursty IO.\n\n## Key Concepts\n- systemd PrivateTmp and PrivateDevices\n- tmpfs sizing and mounting options\n- inode monitoring and alerting (df -i, thresholds)\n- systemd-tmpfiles cleanup rules and scheduling\n\n## Code Example\n```javascript\n// Example pseudo-configuration snippets for illustration\n// systemd unit: PrivateTmp=true\n// /etc/fstab: tmpfs /tmp tmpfs size=128M,mode=1777 0 0\n```\n\n## Follow-up Questions\n- How would you scale this across a cluster of CI workers?\n- What are potential edge cases with cleanup timing and race conditions?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-7",
        "questionId": "q-7450",
        "question": "On a Linux host running a production multi-tenant data-collection stack, a critical kernel vulnerability patch must be applied with zero downtime. Propose a concrete production plan to deploy a live patch (kgraft/kpatch/Ksplice) without reboot, covering patch selection, staging tests, compatibility checks with glibc, CPU/NUMA considerations, monitoring, and rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Patch plan: Utilize a distribution-supported live patching tool (kpatch/kgraft/Ksplice) that maintains compatibility with the exact kernel version and glibc. Execute initial deployment on a staging environment replicati…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "**Approach:** 1. **Quantify tail latency per tenant** using fio-based burst simulations with per-tenant job files, capturing P99/P999 latencies via iostat and blktrace. 2. **Enforce I/O isolation** via the cgroup v2 IO…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement strict isolation: run each tenant in a per-tenant user namespace with rootless containers, drop all capabilities, enforce a seccomp-bpf filter and AppArmor/SELinux profile, and place each te",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A strong answer notes per-tenant CPU pinning via cpuset, strict cgroup v2 quotas, and NUMA-aware memory policies; tunes sched_latency_ns and sched_min_granularity_ns to the worst-case deadline across tenants; sets CPUfr…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to plan zero-downtime kernel patching with reproducible validation and safe rollback.\n\n## Key Concepts\n\n- Live patching tools (kpatch/kgraft/Ksplice)\n- Kernel-glibc compatibility and symbol versioning\n- Staging environments mirroring production workload\n- Rollback strategies and monitoring\n\n## Code Example\n\n```bash\n# Example patch sequence\nkpatch apply /patches/critical.patch\nkpatch status\njournalctl -k -b | tail -n 20\n```\n\n## Follow-up Questions\n\n- How would you validate patch under peak load without impacting users?\n- What failure modes require immediate rollback?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-8",
        "questionId": "q-4752",
        "question": "On a Linux host serving a GPU-accelerated real-time inference pipeline, multiple workloads share a single 2-socket system. Occasional stalls increase tail latency of the critical path under peak load. Design a production plan to guarantee deterministic latency for the critical path. Include hardware topology (NUMA binding), kernel/driver knobs (nohz_full, isolcpus, IRQ affinity), per-workload scheduling (SCHED_DEADLINE with deadlines), cgroups v2 (io.max, memory.min), and a validation plan with synthetic bursts. What exact steps would you take and how would you verify?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Isolate and shape I/O per tenant on the NVMe device using cgroup v2 io.max/io.weight, and implement per-tenant quotas. Tune blk-mq scheduler (mq-deadline/bfq), set per-tenant queue affinities for NVMe",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use XDP in native mode at ingress with a per-tenant BPF hash map (tenant_id -> token bucket). Forward if bucket>0 (atomically decremented); otherwise XDP_DROP or rate-limit. Update quotas live from us",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Isolate a NUMA node for the critical CUDA workers; pin them to CPUs 4–7; enable nohz_full and isolcpus on those CPUs; bind the GPU PCIe device to that node via IOMMU; set IRQ affinity away from isolat",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Pin ingestion workers to a dedicated NUMA node, isolate CPUs with isolcpus/nohz_full, bind IRQs to that node, and place the process in a v2 cgroup (critical.slice) with cpu.max, io.max, and memlock, p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests hands-on ability to guarantee deterministic latency in GPU-backed real-time pipelines by combining OS-level isolation (NUMA, CPU pinning, IRQ affinity) with schedulers and cgroups, plus a practical validation path.\n\n## Key Concepts\n- NUMA binding and CPU isolation (isolcpus, nohz_full)\n- IRQ affinity and PCIe device binding to a dedicated node\n- SCHED_DEADLINE and rt.slice usage for hard real-time tasks\n- cgroups v2 controls (io.max, memory.min, cpuset) for resource guarantees\n- Validation with synthetic bursts and tail-latency measurements\n\n## Code Example\n```bash\n# Example: isolate CPUs 4-7 and bind GPU to NUMA node 0 (conceptual)\nnumactl --cpunodebind=0 --physcpubind=4-7 <your-infer-program>\n```\n\n## Follow-up Questions\n- How would you monitor real-time latency and detect deadlocks?\n- What failure modes would you test, and how would you revert changes if needed?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-9",
        "questionId": "q-26143",
        "question": "On a Linux host with multiple tenants writing to a shared log area, bursts cause IO contention and latency spikes. Design a beginner-friendly plan to isolate log traffic and reduce contention without changing tenant apps. Include per-tenant log directories, a rotating centralized writer, IO scheduling hints, baseline tests, and a rollback path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create per-tenant log sinks at /var/log/tenant-logs/<tenant>, implement a simple log-router (Node/Python) to route writes to tenant-specific files, and configure per-tenant logrotate. Apply IO schedul",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Instrument with eBPF (bpftrace) to trace per-tenant sched_latency, net_xmit, and block IO; collect p95/p99 latency per tenant; enforce per-tenant cgroups v2 with cpu.max and io.max; throttle noisy ten",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument with nfsiostat and iostat during backups to separate locking from I/O. Increase NFS server threads (nfsd) and tweak lockd retry behavior; adjust vfs cache parameters and enable noatime on t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-tenant CPU isolation using cgroups v2 on your Linux host with these steps. **1. Verify cgroups v2** Check that cgroups v2 is active: ``` cat /sys/fs/cgroup/cgroup.controllers ``` **2. Create slice hierarch…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nIn multi-tenant environments, bursty logging can saturate a single disk. This question probes concrete steps to isolate traffic without changing tenant apps, including file-system layout, a router, and rollback.\n\n## Key Concepts\n\n- per-tenant isolation\n- log rotation\n- IO scheduling\n- benchmarking and rollback\n\n## Code Example\n\n```javascript\n// Minimal log-router sketch\nconst fs = require('fs');\nconst tenant = process.env.TENANT || 'default';\nconst stream = fs.createWriteStream(`/var/log/tenant-logs/${tenant}/latest.log`, { flags: 'a' });\nstream.write(`[${new Date().toISOString()}] ${process.argv[2] || ''}\\n`);\n```\n\n## Follow-up Questions\n\n- How would you measure latency changes during bursts?\n- What fail-safes ensure log data isn't lost on router crash?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-10",
        "questionId": "q-22005",
        "question": "On a Linux host running dozens of KVM VMs sharing a 40 Gbps NIC, guest VMs have intermittent 100–300 ms network latency during peak bursts. Propose a beginner-friendly plan to diagnose and mitigate host-side NIC queue contention and interrupt routing without changing guest NICs or apps. Include measurement steps, NIC offload/config tweaks, IRQ/SMPNUMA isolation, and a safe rollback/test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Diagnosis and Mitigation Plan for Page Cache Contention ### 1. Baseline Metrics Collection Establish a performance baseline using standard Linux monitoring tools: - **vmstat 1**: Monitor context switches, page faults…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement per-tenant L3 cache partitions with CAT, enable MBM to cap per-tenant bandwidth, pin workers to NUMA-affinitized CPUs, set IRQ affinity, measure with perf, pmu, turbostat, and mem bandwidth;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Run host perf tools to locate IRQ/NIC bottlenecks (perf stat, mpstat, sar), verify multi-queue and RSS with ethtool -L/-C and ethtool -x; enable per-VM NIC multi-queue and bind each VM thread to dedic",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "To quickly verify ARP traffic and identify abnormal ARP activity on interface eth1, run 'tcpdump -i eth1 arp'. This command captures and displays ARP packets specifically on the eth1 interface, allowing you to monitor f…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nMulti-tenant VMs share a NIC; latency spikes often come from queue contention or misrouted interrupts. Beginner focus: practical measurement and safe tuning without touching VM apps.\n\n## Key Concepts\n- NIC multi-queue, RSS, and offloads\n- IRQ affinity and CPU NUMA isolation\n- VM CPU pinning and vhost/net tuning\n- Safe rollback and test methodology\n\n## Code Example\n```bash\n# Example: pin IRQs to a dedicated core\necho 28 > /proc/irq/123/smp_affinity\n```\n\n## Follow-up Questions\n- How would you verify the impact of each change?\n- What are risks if misconfigured IRQ affinity?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-11",
        "questionId": "q-26875",
        "question": "On a Linux host with multi-tenant workloads contending on a shared NVMe backend, latency spikes to 150–350 ms during burst I/O. Provide a production plan to diagnose and reduce I/O latency while preserving tenant isolation: specify kernel/FS tunables, I/O scheduler choices, per-tenant QoS with cgroups v2, PCIe/NUMA affinities, and a staged test/rollback plan with measurable SLAs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use systemd ExecReload to send SIGHUP to the daemon and a small validator script. Create /usr/local/bin/myapp_reload to validate /etc/myapp/config.yaml (e.g., ensure .port and .log_level exist) and th",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt per-tenant memory isolation using cgroup v2: set memory.max and memory.high for each tenant’s TLS workers, disable swap (vm.swappiness=0), and pin workers to NUMA nodes. Limit per-tenant page ca",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Configure per-tenant IO QoS using cgroups v2 blkio and per-tenant quotas, assign dedicated NVMe queues, set io.scheduler to bfq or deadline, enable io_uring for async IO, tune vm.dirty_bytes and vm.di",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-tenant user, mount, pid, and network namespaces; mount a private root via a read-only bind of a tenant base plus an overlay for writable state. Drop capabilities, enable seccomp-bpf, and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical I/O isolation, QoS, and safe tuning under multi-tenant workloads in production.\n\n## Key Concepts\n\n- per-tenant IO QoS using cgroups v2 blkio and per-tenant quotas\n- I/O schedulers and device queue tuning\n- NUMA affinity and PCIe queueing\n- Observability: iostat, blktrace, perf\n\n## Code Example\n\n```bash\n# Example validation commands\nlsblk\niostat -xd 1\n```\n\n## Follow-up Questions\n\n- How would you detect if a change impacts one tenant more than others?\n- What rollback criteria would you implement if SLA violations occur?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-12",
        "questionId": "q-28251",
        "question": "On a Linux host running a multi-tenant data ingestion service writing to a shared NVMe pool, tail I/O latency spikes during bursts due to IO scheduler contention on block layers. Design a production plan to diagnose and minimize tail latency while preserving throughput. Include: IO schedulers (mq-deadline, kyber, bfq), per-tenant QoS via cgroups or blk QoS, per-device virtualization (NVMe sharing, IO queues), driver tuning (nomerges, elevator hints), and a staged load-test with perf, iostat, and blktrace; rollback steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: validate patch in CI against prod-like workloads; deploy to a canary group (5%), run automated smoke and latency tests for several hours; then roll to 20%, 50%, and 100%. Monitor patch state, SL",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: IEEE 1588v2 with hardware timestamping. Use PHC on NICs, map to /dev/ptp0, run linuxptp ptp4l as a boundary clock with a robust grandmaster, and chronyd in PT mode for precise discipline. Prefer",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-VM caps and headroom via cgroup v2: memory.max and memory.high per VM plus a low vm.swappiness to delay swap pressure. Keep ballooning enabled but hard-cap host memory pressure so reclamation happ",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A comprehensive plan to diagnose and minimize tail I/O latency in a multi-tenant Linux environment with a shared NVMe pool: **Diagnosis Phase:** Instrument the I/O path with blktrace, iostat, and perf to identify delays…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the practical ability to diagnose I/O path bottlenecks in a multi-tenant environment and to select appropriate kernel features for workload isolation without sacrificing throughput.\n\n## Key Concepts\n\n- **I/O Schedulers:** mq-deadline, kyber, and bfq each offer distinct trade-offs between latency and throughput\n- **cgroup v2 per-tenant I/O QoS:** io.max provides hard limits and guaranteed bandwidth allocation\n- **NVMe multi-queue tuning:** Driver parameters like nomerges and elevator hints reduce submission path overhead\n- **Observability stack:** blktrace, iostat, and perf provide complementary views of block layer performance",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-13",
        "questionId": "q-27326",
        "question": "On a Linux host running a multi-tenant data-processing cluster, dozens of containers share a single NVMe-backed filesystem; bursty writes from low-priority tenants trigger I/O contention and tail latency spikes. Design a production plan to diagnose and mitigate storage I/O contention while preserving throughput. Include per-tenant I/O throttling (cgroup v2 io.max), filesystem layout considerations, device-tuning (mq, elevator), observability (iostat, blktrace, bpftrace), and a staged test/rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Bound CPU for CI work using cgroup v2 and systemd. Create a dedicated CI.slice and move CI containers/services into it. Set cpu.max to 50% (period 100000) and restrict to a subset of CPUs with Allowed",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up a root HTB qdisc on the interface; create per-tenant classes with fixed rates (e.g., 5mbit, 3mbit, and 1mbit for management) and attach filters by source IP or netns. Enable basic ingress polic",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: instrument with iostat, blktrace, and bpftrace to confirm per-tenant I/O bottlenecks on the shared NVMe path. Implement per-tenant throttling via cgroup v2 io.max/io.weight, and isolate tenants",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement per-tenant memcg v2 quotas and cpuset/NUMA bindings; reserve HugePages/THP where appropriate for large buffers; isolate allocators per tenant (jemalloc arenas via MALLOC_CONF) to limit cross",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a candidate’s ability to diagnose cross-tenant resource contention in a realistic Linux storage stack, spanning kernel I/O paths, cgroup controls, and storage layout.\n\n## Key Concepts\n\n- cgroup v2 IO controllers and io.max\n- per-tenant storage isolation and mount strategy\n- MQ/block layer tuning and elevator choices\n- observability with iostat, blktrace, bpftrace\n\n## Code Example\n\n```javascript\n// Lightweight snippet to compute per-tenant IOPS targets\nfunction targetIops(totalIops, tenants, idx){\n  const share = totalIops / tenants;\n  return Math.max(1, Math.floor(share));\n}\n```\n\n## Follow-up Questions\n\n- How would you verify no hidden performance regression after rollout?\n- How would you roll back if tail latency worsens on one tenant?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-14",
        "questionId": "q-11346",
        "question": "On a NUMA Linux host serving a multi-tenant streaming service with hundreds of tenants, tail latency spikes when one tenant dominates CPU and memory. Design a production plan to diagnose and eliminate unfairness without sacrificing throughput. Include per-tenant cpuset and cpu.max, memory.min/memory.high, strict NUMA binding, uclamp, IRQ isolation, observability, and a staged test/rollback with fixed workloads and canaries?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant IO quotas (io.max/io.weight) in cgroup v2, dedicate the critical build path to a fast IO group (BFQ/Kyber), isolate storage per tenant, NUMA-bind devices, and per-tenant tmpfs scratch s",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan to diagnose CPU/memory unfairness on a NUMA host serving a multi-tenant streaming service. Implement per-tenant cpuset and cpu.max, per-tenant memory.min/memory.high, and strict NUMA binding. Ena",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Per-tenant namespaces with VRF-style routing, dedicated CPUs, and XDP/BPF-based per-tenant ACLs and egress shaping. Use tc HTB/fq_codel with per-tenant queues, bind NIC TX/RX rings to the same NUMA no",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: dedicate a NIC with hardware PPS to the PHC, bind the PTP stack to isolated CPUs using isolcpus and nohz_full, pin IRQs for PTP queues, lock memory for ptp4l, configure phc2sys to synchronize CL",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests advanced Linux resource isolation: NUMA awareness, per-tenant controls, and production-grade observability under multi-tenant load.\n\n## Key Concepts\n- NUMA affinity and memcg controls (memory.min/high)\n- cpuset and cpu.max in cgroup v2\n- per-tenant NUMA binding and memory guarantees\n- uclamp for latency predictability\n- IRQ isolation and per-tenant interrupt handling\n- Observability with bpftrace, perf, iostat\n\n## Code Example\n```javascript\n// Pseudocode: per-tenant quotas setup\nconst tenants = getTenants();\nfor (const t of tenants) {\n  configureCgroup(t.id, {\n    cpuset: t.cpus,\n    cpuMax: t.max,\n    memoryMin: 256 * 1024 * 1024,\n    memoryHigh: 512 * 1024 * 1024\n  });\n}\n```\n\n## Follow-up Questions\n- How would you validate with canaries and rollback safely?\n- What metrics define success (latency percentile, throughput, SLA adherence)?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-15",
        "questionId": "q-6743",
        "question": "On a Linux host running a multi-tenant artifact/cache service backed by a single NVMe pool, peak bursts from one tenant trigger heavy small writes that push the critical artifact path into tail latency spikes. Design a production plan to guarantee deterministic write latency for the critical path while preserving tenant isolation and data correctness. Include storage topology (per-tenant namespaces, pools, or separate mounts), I/O policy (io.max, per-tenant queues), CPU/memory isolation (cgroups v2, memory.policy), and a test plan with synthetic tenants and rollback/roll-forward criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Pin tenants to dedicated CPU cores and NUMA nodes; deploy XDP/eBPF ingress with per-tenant UMEM rings; assign per-tenant NIC queues and bind IRQs to those cores; disable global irqbalance; enable nohz",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create a dedicated NVMe namespace or pool for the critical artifact path; assign each tenant a fixed io.max in a separate cgroup2 subtree; pin the critical write processes to a reserved CPU core set;",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "My approach combines NUMA-aware tenant isolation with CPU pinning, per-tenant memory constraints using cgroups v2's memory.max and cpu.max controllers, and XDP/BPF-driven rate limiting at the NIC layer to eliminate unfa…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Pin ingestion workers to a dedicated NUMA node, isolate CPUs with isolcpus/nohz_full, bind IRQs to that node, and place the process in a v2 cgroup (critical.slice) with cpu.max, io.max, and memlock, p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design precise storage isolation and per-tenant QoS for high-throughput multi-tenant workloads, with concrete controls (NVMe namespaces, cgroupv2 io.max, CPU pinning, and I/O schedulers) and a realistic rollback path.\n\n## Key Concepts\n- Per-tenant I/O QoS using cgroup v2 io.max\n- Storage topology choices for isolation (NVMe namespaces, pools, separate mounts)\n- Latency measurement and rollback strategies\n\n## Code Example\n\n```bash\n# sample per-tenant io.max configuration (pseudo-example)\nmkdir -p /sys/fs/cgroup/io/tenant-a\necho 8:0 1048576 > /sys/fs/cgroup/io/tenant-a/io.max\n# attach critical writer to tenant-a cgroup\necho <pid> > /sys/fs/cgroup/io/tenant-a/cgroup.procs\n```\n\n## Follow-up Questions\n- How would you monitor and alert on tail latency spikes?\n- What changes would you make to handle multi-tenant bursts dynamically without manual intervention?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-16",
        "questionId": "q-16399",
        "question": "On a Linux host running a multi-tenant data pipeline in containers, bursts trigger disk I/O latency spikes due to shared storage contention. Design a production plan to guarantee per-tenant I/O isolation without sacrificing throughput. Include per-tenant IO control (cgroups v2 io.max and io.weight on devices), filesystem/topology choices (XFS with striped pools or ext4), NUMA-aware layout, observability (iostat, blktrace, perf), and a staged load-test with rollback?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose isolating tenants with cgroup v2, pinning CPU and memory resources, and comparing ext4 (with dir_index) vs XFS metadata performance under mixed workloads. Tune directory indexing and inode/dentry cache parameter…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Plan: enforce per-tenant memcg quotas on v2, pin tenants to specific NUMA nodes, disable THP for predictable latency, align hot paths to 64-byte cachelines, assign IRQ affinity per-tenant, instrument",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy per-tenant DNS forwarders (e.g., Unbound) inside isolated net namespaces and route each tenant’s containers to its local resolver at 127.0.0.1:5353. Enforce nftables rules to allow DNS queries",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Set per-tenant IO limits with cgroups v2 io.max and io.weight on the relevant devices, isolate tenants to dedicated disks or striped pools, and apply NUMA-aware bindings. Validate with iostat, blktrac",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nProbes practical per-tenant I/O isolation in multi-tenant pipelines, requiring concrete controls (cgroups v2 IO, per-device limits), filesystem/topology choices, NUMA awareness, observability, and a rollback plan.\n\n## Key Concepts\n- cgroups v2 IO controls (io.max, io.weight)\n- per-device quotas and isolation (io.max per tenant)\n- filesystem selection and striping strategy\n- NUMA-aware memory/disk allocation\n- observability primitives (iostat, blktrace, perf)\n- safe rollback and staged load tests\n\n## Code Example\n```bash\n# Example: create tenant1 cgroup and bound device 8:0\nsudo mkdir -p /sys/fs/cgroup/tenant1\necho \"8:0 50M\" | sudo tee /sys/fs/cgroup/tenant1/io.max\n```\n\n## Follow-up Questions\n- How would you measure success and what metrics?\n- How would you handle a tenant that consistently bursts beyond quota?",
        "difficulty": "intermediate"
      },
      {
        "id": "linux-foundation-sysadmin-q-17",
        "questionId": "q-19322",
        "question": "On a Linux host serving a shared artifact cache used by dozens of tenants, bursts of tiny builds trigger EMFILE/ENFILE errors and lower throughput due to per-tenant FD exhaustion. Design a beginner-friendly plan to diagnose and mitigate FD pressure without changing tenant apps. Include per-tenant limits (nofile), lightweight per-tenant FD watchers, a wrapper to cap FD usage, and a rollback/test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Raise nf_conntrack_max and nf_conntrack_buckets; enable per-namespace conntrack tables; deploy a small eBPF/XDP program to pre-filter invalid/duplicate connections; pin NAT/conntrack processing to ded",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Isolate tenants by giving each a dedicated io_uring instance and, where feasible, enable per-tenant SQPOLL to reduce cross-tenant queue contention. Bind workers to per-tenant CPUs, pre-register buffer",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan to diagnose and mitigate file descriptor pressure: 1. **Diagnose**: Use `lsof` or `/proc/<pid>/fd` to identify which tenants are hitting FD limits. Monitor with inotify-watch or simple `ls /proc/<pid>/fd | wc -l` s…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Baseline per-tenant page-cache pressure using perf counters and slabinfo; identify hot mmap patterns. Apply mitigations: MADV_DONTNEED after bursts, FADV_NOREUSE, adjust vfs_cache_pressure and min_fre",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests understanding of per-tenant resource isolation, file descriptor pressure, and practical beginner-friendly mitigations that don't require modifying tenant applications.\n\n## Key Concepts\n- File descriptor limits (NOFILE) and system-wide limits\n- Per-tenant isolation via `/etc/security/limits.conf` or systemd slices\n- Lightweight monitoring and alerting strategies\n- Safe rollback and test procedures\n\n## Code Example\n```bash\n# Set per-tenant nofile limit (per-user)\ncat > /etc/security/limits.d/tenant.conf << 'EOF'\ntenant1 soft nofile 2048\ntenant1 hard nofile 4096\ntenant2 soft nofile 2048\ntenant2 hard nofile 4096\nEOF\n```\n\n```bash\n# Wrapper script to cap FD usage per tenant build\n#!/bin/bash\n# fdcap.sh - Launch process with FD limit\nulimit -n \"$1\"  # Set max file descriptors\nshift\nexec \"$@\"\n```\n\n```bash\n# Usage: ./fdcap.sh 1024 build-command arg1 arg2\n```\n\n## Rollback Plan\n1. Keep backup of original `/etc/security/limits.conf`\n2. Test in staging with synthetic load before production\n3. Monitor build success rate and FD usage metrics\n4. If issues occur, remove `/etc/security/limits.d/tenant.conf` to revert",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-18",
        "questionId": "q-6020",
        "question": "On a production fleet of Linux servers, apply a critical security kernel patch without reboot. Design a zero-downtime rollout plan using live patching across 100+ hosts. Describe validation, canary strategy, phased rollout, monitoring, and rollback. Include module compatibility checks and SLA impact for latency-sensitive services?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Identify offenders with systemd-cgtop and systemd-cgls to pinpoint which service.slice is hogging resources. Throttle by applying quotas on the offending unit.slice using systemctl set-property to enf",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Proposed answer: Use per-tenant arenas in jemalloc or tcmalloc to eliminate cross-tenant contention; pin worker threads and malloc metadata to specific NUMA nodes; align allocations to cache lines and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Increase inotify capacity and harden limits; implement a polling fallback for missed events; add a watchdog health check to detect lag and restart gracefully. Steps: 1) set fs.inotify.max_user_watches",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: validate patch in CI against prod-like workloads; deploy to a canary group (5%), run automated smoke and latency tests for several hours; then roll to 20%, 50%, and 100%. Monitor patch state, SL",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to plan production-grade live kernel patch rollouts without downtime and with measurable SLAs.\n\n## Key Concepts\n\n- Live patching tooling (kpatch, kernel livepatch)\n- Canary deployments and phased rollouts\n- Validation, monitoring, and automated rollback\n- Module compatibility and reboot safety\n\n## Code Example\n\n```bash\n# illustrative commands\nkpatch load /patches/patch-2026-01-01.ko\n```\n\n## Follow-up Questions\n\n- How would you handle heterogeneous kernel versions? \n- What metrics define a safe rollback threshold?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-19",
        "questionId": "q-17499",
        "question": "On a Linux host with multiple backup jobs triggered by systemd timers running concurrently, one backup occasionally stalls during bursts. Design a beginner-friendly plan to diagnose whether systemd cgroups throttling is the cause and implement a safe mitigation by creating a dedicated backup.slice with explicit CPU and memory limits, plus a simple test and rollback path. Include commands to verify and monitor?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "**Question:** On a Linux host with multiple backup jobs triggered by systemd timers running concurrently, one backup occasionally stalls during bursts. Design a beginner-friendly plan to diagnose whether systemd cgroups…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create per-tenant log dirs under /var/log/tenant-<id> and enable filesystem quotas to cap each tenant’s log footprint. Route logs with rsyslog using loginuid so tenant logs land in tenant-specific fil",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use systemd ExecReload to send SIGHUP to the daemon and a small validator script. Create /usr/local/bin/myapp_reload to validate /etc/myapp/config.yaml (e.g., ensure .port and .log_level exist) and th",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Deploy a lightweight monitor per container that checks /var/log/<ct> size every 5 minutes and alerts on threshold. Enforce quotas via per-dir quotas on the filesystem (or systemd.slice if available) t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical debugging of resource isolation using systemd slices—a foundational skill for production Linux administrators. Backup jobs competing for system resources is a common real-world issue that can cause unpredictable failures during peak usage periods.\n\n## Key Concepts\n- **systemd slices**: Control groups (cgroup v2) that isolate and limit resources for related processes, preventing noisy neighbors from impacting critical workloads\n- **CPUQuota**: Caps CPU usage as a percentage of one CPU core (50% = half a core; 200% = two full cores)\n- **MemoryMax**: Hard limit on memory consumption; processes exceeding this limit are terminated or throttled\n- **systemd-run**: Spawns a transient unit in a specific slice without modifying existing service files\n\n## When to Use Slices\n- When batch jobs (backups, data processing) interfere with interactive services\n- To guarantee minimum resources for critical workloads\n- To prevent runaway processes from exhausting system resources\n\n## Verification Commands\n```\n# Check slice status and resource usage\nsystemctl status backup.slice\n\n# View real-time resource consumption\nsystemd-cgtop\n\n# Inspect applied cgroup limits\ncat /sys/fs/cgroup/system.slice/backup.slice/cpu.max\ncat /sys/fs/cgroup/system.slice/backup.slice/memory.max\n\n# Check for throttling events in kernel logs\njournalctl -k | grep -i throttle\n```",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-20",
        "questionId": "q-16868",
        "question": "On a Linux host running a multi-tenant container platform (CRI-O with Kubernetes) across NUMA nodes and SR-IOV NICs, a critical CVE prompts a kernel livepatch deployment. Design a production plan to validate, deploy, and rollback with zero downtime. Include: patch validation, module compatibility with 3rd-party drivers, per-tenant isolation during patch window, PCI binding, observability (perf, bpftool, pidstat), staged rollouts, and rollback criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant memcg v2 quotas and cpuset/NUMA bindings; reserve HugePages/THP where appropriate for large buffers; isolate allocators per tenant (jemalloc arenas via MALLOC_CONF) to limit cross",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a staged livepatch rollout: build patch in staging; verify 3rd-party driver compatibility and kmod/ufw interactions; run canary tenants in isolated namespaces seeded with representative worklo",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Enable XFS project quotas and map each tenant to a project ID; create per-tenant subdirectories under /data and assign them to their projects with xfs_quota. Set per-project inode/block limits. Use bf",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use an HTB-based qdisc on the host's outbound interface. Create per-tenant classes (e.g., 1:10, 1:20, 1:30) under a root qdisc, then map each container's IP address to its corresponding class using tc filters. Configure…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEvaluates livepatch planning, vendor-driver compatibility, and multi-tenant safety in production.\n\n## Key Concepts\n\n- Kernel livepatch techniques (kpatch/kgraft)\n- NUMA-aware scheduling and PCI binding\n- Observability (perf, pidstat, bpftool)\n- Safe rollback windows and metrics\n\n## Code Example\n\n```javascript\nfunction checkCompatibility() {\n  // Placeholder: verify driver ABI compatibility\n}\n```\n\n## Follow-up Questions\n\n- How would you automate canary promotion and rollback triggers?\n- What are the pitfalls of livepatching in a NUMA/SR-IOV environment and how would you mitigate them?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-21",
        "questionId": "q-28723",
        "question": "On a Linux host running a multi-tenant data lake with dozens of tenants sharing a fast NVMe backend and a common page cache, tail I/O latency spikes during cold-cache bursts. Design a production plan to enforce per-tenant storage I/O fairness and predictable tail latency using cgroup v2 IO controllers (io.max, io.weight), per-tenant device scoping, and eBPF-based telemetry for end-to-end observability. Include policy design, canary rollout, rollback criteria, and success metrics?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant IO control using cgroup v2 io.max and io.weight on the NVMe path, with per-tenant device scoping. Instrument with eBPF tracing to measure queue depth and latency. Run a staged can",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-tenant WAL streams with isolated io_uring queues and dedicated WAL directories; pin WAL I/O to the local NUMA node to minimize cross-NUMA access latency; leverage cgroupv2 IO throttling to prevent any sing…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Instrument tail latency with perf/pidstat and bpftrace to locate hot paths; enforce per-tenant memcg quotas and swap limits; pin tenants to dedicated NUMA nodes; isolate GPUs via MIG or per-tenant CUD",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement per-service memory quotas via cgroup v2: create dedicated slices for each service; enforce memory.max as a hard cap, memory.high as a soft target, and memory.swap.max to limit swap usage. Bind processes to spe…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes practical, scalable I/O fairness in multi-tenant Linux systems with high-speed storage, including observability and rollback plans.\n\n## Key Concepts\n- cgroup v2 IO controllers (io.max, io.weight)\n- per-tenant device scoping and isolation\n- eBPF tracing for latency and queue depth\n- Canary rollout and rollback criteria\n- Observability: iostat, blktrace, bpftools, perf\n\n## Code Example\n```bash\n# Create tenant IO group and apply limits (example)\nmkdir -p /sys/fs/cgroup/io/tenant-A\necho \"8:0 rbps=10485760 wbps=10485760\" > /sys/fs/cgroup/io/tenant-A/io.max\n```\n\n## Follow-up Questions\n- How would you map tenants to devices if multiple NVMe namespaces exist?\n- How would you detect and mitigate pathological tenants that exceed their fair share without starving others?",
        "difficulty": "advanced"
      },
      {
        "id": "linux-foundation-sysadmin-q-22",
        "questionId": "q-881",
        "question": "On a Linux host, a long-running daemon writes to `/var/log/myapp.log` and is managed by systemd, but log rotation occasionally causes logging to stop after rotation. Propose a practical fix to ensure logging continues after rotation without restarting the daemon. Include the exact approach and a sample logrotate config snippet and testing steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement per-tenant I/O isolation with a v2 cgroup: assign each tenant to /sys/fs/cgroup/tenantX and enforce io.max limits for read/write. Test with fio bursts; compare bfq vs mq-deadline tuning; con",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Configure logrotate to reopen the log file after rotation instead of copying or truncating. After rotating, signal the daemon to reopen logs (e.g., SIGHUP). The config should include a postrotate that",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement per-tenant I/O limits using cgroups v2 blkio.weight, assigning each container a proportional share of disk bandwidth on the shared NVMe SSD. Establish baseline performance metrics using fio (random and sequent…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition CPU resources using CGroups v2: allocate cores 0-3 for data ingestion and cores 4-5 for alerting; configure alerting within a dedicated systemd.slice with CPUAffinity=4-5 and CPUQuota=60%; execute alerting wit…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical handling of log rotation, signals, and ensuring service continuity without downtime. It checks knowledge of logrotate postrotate scripts, choosing the right approach (signal vs copytruncate), and how to validate in a controlled test.\n\n## Key Concepts\n- logrotate configuration fields and postrotate scripts\n- signaling daemons (SIGHUP) to reopen logs\n- copytruncate vs signaling trade-offs\n- testing routine for rotation\n\n## Code Example\n```bash\n/var/log/myapp.log {\n  rotate 5\n  weekly\n  missingok\n  notifempty\n  postrotate\n    kill -HUP `cat /var/run/myapp.pid` 2>/dev/null || true\n  endscript\n}\n```\n\n## Follow-up Questions\n- What are the trade-offs of copytruncate vs signaling?\n- How would you monitor and alert if log rotation fails to reopen logs?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-23",
        "questionId": "q-24690",
        "question": "On a Linux server hosting multiple services that write logs into a shared /var/log, bursts in log activity cause elevated I/O wait, affecting user-facing latency. Design a beginner-friendly plan to diagnose and mitigate log I/O contention without touching application code. Include filesystem isolation, logrotate tuning, journald/rsyslog buffering, and safe rollback steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant quotas via cgroup v2: cpu.max, memory.max, memory.high, pids.max, io.max; place each tenant in its own systemd.slice. Use tc clsact with per-tenant egress queues and BPF-based enforcement.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Baseline IO metrics with iostat and iotop. Move /var/log to its own partition on a dedicated disk with ext4, noatime. Tighten logrotate: smaller max size, more frequent rotations. Enable rsyslog/journ",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Increase inotify capacity and harden limits; implement a polling fallback for missed events; add a watchdog health check to detect lag and restart gracefully. Steps: 1) set fs.inotify.max_user_watches",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant GPU isolation (MIG or per-GPU slices), CPU pinning, and container runtimes with strict cgroups v2 quotas. Run GPUs through MPS to reduce cross-tenant queueing. Instrument with eBPF trac",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical, beginner-friendly plan for log-induced I/O contention without code changes, including observability, isolation, and rollback.\n\n## Key Concepts\n\n- IO isolation and per-partition mitigation\n- Log management: logrotate, rsyslog, journald\n- Mount options: noatime\n- Baseline benchmarking: iostat, iotop\n\n## Code Example\n\n```bash\n# Example: move /var/log to new partition and update fstab\nmkfs.ext4 /dev/sdb1\nmkdir -p /var/log\nmount /dev/sdb1 /var/log\n# Update /etc/fstab for persistence\n``` \n\n## Follow-up Questions\n\n- How would you validate rollback can be performed?\n- How would you handle high-priority logs during bursts?",
        "difficulty": "beginner"
      },
      {
        "id": "linux-foundation-sysadmin-q-24",
        "questionId": "q-19399",
        "question": "On a Linux host running multiple tenants via Docker, CPU bursts cause CI jobs to starve other tenants. Design a beginner-friendly plan to isolate CPU resources and enforce fair scheduling using cgroups v2: create per-tenant cpusets, set cpu.max, and place tenants in a shared slice for fairness. Include concrete test steps, metrics (e.g., per-tenant latency), and rollback instructions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan to guarantee deterministic scheduling on a multi-tenant build host by enforcing per-tenant cpuset and memory quotas, pinning build tasks with SCHED_DEADLINE, isolating CPUs with isolcpus and nohz",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a production-grade live kernel patching strategy using kpatch to maintain zero uptime. Validate patch compatibility against the active kernel configuration and ABI, build the patch module targeting the exact r…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Deploy kpatch to apply live kernel patches with zero downtime. Start with a 5% canary group (2-3 non-critical hosts) and verify stability over 12-24 hours. Then scale to 20% of the fleet, followed by staged rollouts inc…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Configure per-tenant CPU isolation with cgroups v2: create per-tenant cpusets, assign CPUs via cpu.cpus, enforce quotas with cpu.max, and group tenants in a shared slice for fair scheduling. Run a sim",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical OS-level resource isolation fundamentals and how to implement fair CPU sharing in a real multi-tenant environment without complex orchestration.\n\n## Key Concepts\n\n- cgroups v2 and the cpu controller\n- cpuset vs unified hierarchy\n- cpu.max and cpu.cpus for quotas and pinning\n- latency-based benchmarking for fairness\n\n## Code Example\n\n```javascript\n// Example: pseudo-commands to create and apply a cpuset (not run in shell directly)\n``` \n\n## Follow-up Questions\n\n- How would you monitor per-tenant CPU usage in production with low overhead?\n- How would you handle a tenant requiring burst beyond its quota without impacting others?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-13T11:23:31.463Z",
    "version": 1
  }
]