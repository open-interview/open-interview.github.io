[
  {
    "id": "test-e2e-testing",
    "channelId": "e2e-testing",
    "channelName": "E2E Testing",
    "title": "E2E Testing Knowledge Test",
    "description": "Test your understanding of E2E Testing with 25 questions.",
    "questions": [
      {
        "id": "e2e-testing-q-0",
        "questionId": "q-2526",
        "question": "Design an advanced E2E test for a real-time order-status pipeline built on a pub/sub backbone (Kafka-like) with a downstream fan-out service. Validate exactly-once delivery, out-of-order processing, regional outages, and per-tenant data isolation; include data generation, replay safety, and latency/throughput metrics under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an advanced end-to-end test for a real-time order-status pipeline using a pub/sub backbone (Kafka-like) with a downstream fan-out service. Validate exactly-once delivery, out-of-order processing, regional outages…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Seed a user and cart in the test DB, stub the feature flag service to return true then false, and navigate to checkout. Verify the UI shows Fast Checkout when the flag is ON and Standard Checkout when",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a E2E test for a CMS publish flow: author drafts, autosaves every 30s, submits for review; a separate reviewer approves in another session; on publish, confirm the article appears in the live",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright with a fresh test DB. Generate a unique email like user+<ts>@example.com. Mock the email service to intercept the verification link with a short-lived token. Visit the link to activate,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of streaming end-to-end guarantees, including idempotency, ordering, fault injection, and multi-tenant isolation across regions.\n\n## Key Concepts\n- Exactly-once semantics in pub/sub and fan-out architectures\n- Event ordering and out-of-order handling mechanisms\n- Fault injection for regional outages and replay safety validation\n- Test data isolation per tenant and end-to-end tracing\n\n## Code Example\n```javascript\n// Pseudo-test harness outline (Node.js)\nasync function runE2ETest({ tenants, events, durationMs }) {\n  // Generate per-tenant events in order",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-1",
        "questionId": "q-20132",
        "question": "Design an end-to-end test for an offline-first note app that syncs across devices. The app creates notes offline, then reconciles on reconnect; conflicts on concurrent edits must be resolved. Outline a deterministic test using Playwright or Cypress, test data isolation, network shaping, and a conflict-resolution policy (merge or last-write-wins). Provide concrete steps and expected outcomes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright to create two browser contexts simulating Device A and Device B. The test proceeds as follows: (1) Start both devices offline using network interception to block requests; (2) On Device A, create a note w…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Playwright with a mocked WebSocket server to drive status events deterministically. Seed a unique test order (e.g., `order-${Date.now()}`) via the admin API, then simulate the status sequence: Processing → Out for D…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an end-to-end test that seeds per-tenant loan data via API, navigates a mobile web loan flow in the frontend, simulates credit check latency with network throttling, and triggers a partial o",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed per-tenant datasets with representative samples; drive the ML suggestion module with drift scenarios including concept drift, data drift, and model degradation; verify the complete end-to-end task lifecycle: create…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTesting offline-first sync across devices is a realistic challenge for consumer apps with data integrity concerns, multi-device usage, and eventual consistency.\n\n## Key Concepts\n- Offline-first architecture: Data is persisted locally and synced when connectivity is restored\n- Conflict resolution strategy: Merge (combine changes) vs. Last-Write-Wins (most recent timestamp wins)\n- Test isolation across devices: Each Playwright context maintains separate local storage and cookies\n- Deterministic network shaping: Use Playwright's `route` or Cypress's `server` to intercept and delay requests\n\n## Code Example\n```javascript\nconst { test, expect } = require('@playwright/test');\n\ntest('offline-first sync with conflict resolution', async ({ browser }) => {\n  // Create two isolated device contexts\n  const ctxA = await browser.newContext();\n  const ctxB = await browser.newContext();\n  \n  const pageA = await ctxA.newPage();\n  const pageB = await ctxB.newPage();\n\n  // Simulate offline on both devices\n  await pageA.route('**/api/**', route => route.abort('failed'));\n  await pageB.route('**/api/**', route => route.abort('failed'));\n\n  // Device A creates a note offline\n  await pageA.goto('/app');\n  await pageA.fill('[data-testid=\"note-input\"]', 'Note from A');\n  await pageA.click('[data-testid=\"save\"]');\n\n  // Device B creates a note offline\n  await pageB.goto('/app');\n  await pageB.fill('[data-testid=\"note-input\"]', 'Note from B');\n  await pageB.click('[data-testid=\"save\"]');\n\n  // Restore connectivity\n  await pageA.unrouteAll();\n  await pageB.unrouteAll();\n\n  // Wait for sync and verify\n  await pageA.reload();\n  await pageB.reload();\n  \n  await expect(pageA.locator('[data-testid=\"notes\"]')).toContainText('Note from A');\n  await expect(pageA.locator('[data-testid=\"notes\"]')).toContainText('Note from B');\n});\n```\n\n## Follow-up Questions\n- How would you scale this to 5+ devices?\n- How would you verify conflict resolution for concurrent edits to the same note?\n- How would you test partial sync failures?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-2",
        "questionId": "q-18993",
        "question": "Design an end-to-end test for a multi-region, PCI-compliant payments platform that uses tokenized PANs, authorization, and settlement across two regions with Kafka as the event bus and a separate fraud service. Include seeds, idempotency handling, partial failure simulations (gateway timeout, bank delay), and a reconciliation check that settlements match ledger entries within SLA?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright to seed data via API: POST /seed with 7 days of events; open /analytics, set date range, click Export CSV; intercept POST /exports to return {id}; poll GET /exports/{id}/status every 50",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a mock flag service to control region-specific rollout percentages (US: 50%, EU: 0%, APAC: 100%), route requests through region-scoped tenants, and validate that the UI accurately reflects each region's flag s…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a test that seeds tokenized PANs, executes authorization, capture, and settlement across two regions using Kafka events and a fraud service, then validates ledger reconciliation within SLA. Inc",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a test harness that seeds two regions with drivers, a rider, and a surge window. Use Playwright to drive rider and driver UIs, enable geolocation drift and network throttling, and drive a We",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests must cover cross-region payment flows, tokenization, and reconciliation under real-world failures. It emphasizes deterministic seeds, idempotency, and end-to-end integrity in a PCI context.\n\n## Key Concepts\n- Cross-region E2E flows with tokenized PANs\n- Idempotency keys and deduplication guarantees\n- Kafka-based event-driven orchestration\n- Fraud service integration and its impact on flow\n- Partial failure injection and resilience\n\n## Code Example\n```javascript\n// Pseudo seeds and flow control for the test\nconst seeds = {\n  panToken: 'tok_TEST_4242',\n  merchantId: 'merch_001',\n  regionA: 'us-east-1',\n  regionB: 'eu-west-1'\n};\n```\n\n## Follow-up Questions\n- How would you measure end-to-end settlement latency across regions?\n- How would you test idempotency under webhook retries and out-of-order events?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-3",
        "questionId": "q-16312",
        "question": "Design an end-to-end test for a collaborative document editor that supports real-time presence via WebSockets and autosave. The test should cover two users editing the same document, presence propagation, autosave timing, offline reconnection, and conflict resolution to ensure data integrity. Specify tooling (Playwright or Cypress), test data isolation, determinism via a mock clock, and how you verify eventual consistency across clients and the data store?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan to mock the remote flag service to inject per-user segments. Use two test users: ON and OFF. Verify that with ON the checkout totals show the discount and the promo banner is visible; with OFF no",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed a deterministic user in the test database, mock email delivery to expose the verification URL, and mock OIDC so sign-in resolves locally. Use Playwright to drive: signup, click the verification link, complete the p…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright with two isolated browser contexts editing the same document. Stub autosave to a fixed 2-second interval, mock the server clock, and simulate WebSocket presence events. Validate that live edits propagate…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a seeded, versioned data pipeline: ingest v1, train on Cluster-A, publish artifacts to the registry, then mutate seed to v2 and retrain. Assert v1 artifacts immutable, v2 artifacts produced, metri",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question probes end-to-end orchestration of real-time collaboration, including WebSocket presence, autosave determinism, offline recovery, and conflict resolution. It also assesses test isolation strategies and how to model eventual consistency in tests.\n\n## Key Concepts\n- Real-time collaboration, presence, and WebSocket event handling\n- Autosave semantics and determinism via clock mocks\n- Offline/online recovery and conflict resolution strategies\n- Test data isolation and environment management\n- Observability: asserting store state and client views align\n\n## Code Example\n```javascript\n// Playwright test setup with two isolated contexts\nconst { test, expect } = require('@playwright/test');\n\ntest.describe('Collaborative Document Editor', () => {\n  let docId;\n\n  test.beforeEach(async ({ request }) => {\n    docId = `test-doc-${Date.now()}-${Math.random().toString(36).slice(2)}`;\n  });\n\n  test('real-time presence and autosave sync between two users', async ({ browser }) => {\n    const userA = await browser.newContext();\n    const userB = await browser.newContext();\n\n    const pageA = await userA.newPage();\n    const pageB = await userB.newPage();\n\n    await pageA.goto(`/doc/${docId}`);\n    await pageB.goto(`/doc/${docId}`);\n\n    // Verify presence indicators show both users\n    await expect(pageA.locator('.presence')).toContainText('User B');\n    await expect(pageB.locator('.presence')).toContainText('User A');\n\n    // User A types; User B should see it within 1-2 seconds\n    await pageA.locator('.editor').fill('Hello from User A');\n    await expect(pageB.locator('.editor')).toHaveValue('Hello from User A', { timeout: 3000 });\n\n    // Verify autosave to data store (mocked interval: 2s)\n    await pageA.waitForTimeout(2500);\n    const storedDoc = await request.get(`/api/docs/${docId}`);\n    expect((await storedDoc.json()).content).toBe('Hello from User A');\n  });\n\n  test('offline reconnection and conflict resolution', async ({ browser }) => {\n    const userA = await browser.newContext();\n    const userB = await browser.newContext();\n\n    const pageA = await userA.newPage();\n    const pageB = await userB.newPage();\n\n    await pageA.goto(`/doc/${docId}`);\n    await pageB.goto(`/doc/${docId}`);\n\n    // Simulate offline for User B\n    await pageB.context().setOffline(true);\n    await pageA.locator('.editor').fill('User A edit');\n    await pageB.waitForTimeout(1000);\n\n    // Reconnect User B\n    await pageB.context().setOffline(false);\n    await expect(pageB.locator('.editor')).toHaveValue('User A edit', { timeout: 5000 });\n\n    // Concurrent edits - last-write-wins conflict resolution\n    await pageB.locator('.editor').fill('User B edit');\n    await pageA.locator('.editor').fill('User A edit');\n    await pageB.waitForTimeout(2500); // Wait for autosave\n\n    const finalDoc = await request.get(`/api/docs/${docId}`);\n    expect((await finalDoc.json()).content).toMatch(/User (A|B) edit/);\n  });\n});\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-4",
        "questionId": "q-17512",
        "question": "Design an end-to-end test for a real-time market data platform that ingests tick data over WebSocket, normalizes to a canonical schema, and fans to three downstream analytics services via a message bus, with backpressure and alerting on anomalies. Include data seeds, reconnection and missed-tick handling, burst load, and end-to-end latency verification under SLA targets?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an end-to-end test for a real-time market data platform that ingests tick data over WebSocket, normalizes to a canonical schema, and fans out to three downstream analytics services via a message bus, with backpre…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a test harness that seeds two regions with drivers, a rider, and a surge window. Use Playwright to drive rider and driver UIs, enable geolocation drift and network throttling, and drive a We",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I'd implement a multi-session Playwright test that opens two pages simulating two users editing in parallel, use a mock WebSocket server to deterministically replay edits, fixture per-test doc, introd",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Locust for 2000 concurrent actions across 3 regions; seed MongoDB with 1000 users and 5000 tweets; prepopulate Redis caches as cold; publish actions to an event queue to trigger the indexer; instr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time streaming paths, backpressure management, reconnection handling, and cross-service visibility in a realistic production-like setup.\n\n## Key Concepts\n- Real-time data ingestion over WebSocket\n- Normalization to canonical schema\n- Downstream fan-out via message bus with backpressure\n- Burst load simulation, tick miss detection, and reconnection handling\n- End-to-end latency SLA verification and audit logging\n\n## Code Example\n```javascript\n// Synthetic tick generator\nconst ws = new WebSocket('wss://tdp.example');\nws.onopen = () => {\n  for (let i = 0; i < 100000; i++) {\n    ws.send(JSON.stringify({ symbol: 'AAPL', price: 150 + Math.random(), ts: Date.now() }));\n  }\n};\n\n// Verify end-to-end latency\nconst tickSent = Date.now();\nws.onmessage = (msg) => {\n  const latency = Date.now() - JSON.parse(msg.data).sentAt;\n  if (latency > SLA_THRESHOLD_MS) alert(`SLA breach: ${latency}ms`);\n};\n\n// Reconnection with missed-tick detection\nws.onclose = () => reconnect(lastSequenceId);\n```",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-5",
        "questionId": "q-24203",
        "question": "Design an end-to-end test for a Kubernetes-deployed, multi-tenant e-commerce platform with per-tenant discounts and feature flags. The test should simulate 1000 concurrent shoppers applying different coupons, ensure tenant data isolation, verify discount correctness, and validate disaster recovery by injecting a transient Redis cache miss and a DB failover, verifying order integrity and audit logs within 30 seconds?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright for end-to-end testing. The test uploads a 10,000-row CSV file, seeds required test data, and mocks the message bus to simulate partial failures with deterministic retries. Assert that the progress bar re…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed tenants, products, coupons, and feature flags in the test database. Use a load testing tool such as k6 to drive 1000 concurrent sessions through the frontend and API gateway, each randomly selecting a tenant, produ…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Seed a small dataset (5 tasks) and use dockerized Kafka topics for tasks and audits, UI automation with Playwright, and a Kubernetes namespace per run. Simulate two annotators claiming the same task t",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright for this E2E test. First, upload a fixture CSV containing 500 rows. Then intercept the POST request to /import and mock the job queue with a deterministic response to simulate asynchronous processing. See…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTesting at scale with multi-tenant isolation and controlled chaos reveals critical gaps in data separation, retry policies, observability, and disaster recovery mechanisms. This question assesses a candidate's understanding of end-to-end reliability, fault tolerance, and security in distributed systems.\n\n## Key Concepts\n\n- **End-to-end testing at scale**: Validating system behavior under realistic concurrent load\n- **Multi-tenant data isolation**: Ensuring tenants cannot access or view each other's data\n- **Fault injection and chaos engineering**: Deliberately introducing failures to test system resilience\n- **Auditability and data integrity**: Verifying that orders remain consistent and all actions are logged\n- **Time-bounded verification**: Confirming the system recovers within acceptable SLA windows\n\n## Code Example\n\n```javascript\n// k6 load test with fault injection\nimport http from 'k6/http';\nimport { check, sleep } from 'k6';\n\nexport const options = {\n  vus: 1000,\n  duration: '30s',\n  thresholds: {\n    http_req_duration: ['p(95)<500'],\n  },\n};\n\nconst tenants = ['tenant-a', 'tenant-b', 'tenant-c'];\nconst coupons = { 'tenant-a': 'DISC10', 'tenant-b': 'SAVE20', 'tenant-c': 'FREESHIP' };\n\nexport default function () {\n  const tenant = tenants[Math.floor(Math.random() * tenants.length)];\n  const coupon = coupons[tenant];\n  \n  // Apply coupon to cart\n  const cartRes = http.post('https://gateway/api/cart', \n    JSON.stringify({ tenant, productId: 'prod-123', coupon }),\n    { headers: { 'Content-Type': 'application/json' } }\n  );\n  \n  check(cartRes, {\n    'tenant isolation': (r) => r.json('tenant') === tenant,\n    'discount applied': (r) => r.json('discount') > 0,\n    'status is 200': (r) => r.status === 200,\n  });\n  \n  // Place order\n  const orderRes = http.post('https://gateway/api/orders',\n    JSON.stringify({ tenant, cartId: cartRes.json('cartId') }),\n    { headers: { 'Content-Type': 'application/json' } }\n  );\n  \n  check(orderRes, {\n    'order created': (r) => r.status === 201,\n    'order integrity': (r) => r.json('orderId') !== undefined,\n  });\n  \n  sleep(1);\n}\n\n// Fault injection via Kubernetes API:\n// 1. Redis cache miss: kubectl exec redis-0 -- redis-cli FLUSHDB\n// 2. DB failover: kubectl delete pod postgres-primary-0\n```",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-6",
        "questionId": "q-10715",
        "question": "Design an end-to-end test for a Zoom-like app that supports live captions and post-meeting transcription. The test should verify real-time caption accuracy, post-session ingestion into a data lake, and indexing for search, including handling transient transcription-service failures with retries and ensuring idempotent transcript writes across regional indices?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement a comprehensive end-to-end test for a Zoom-like application with live captions and transcription capabilities. Using Playwright with a controllable mock transcription API, the test would follow these s…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Seed three tenants with distinct user rows in a per-tenant data store. Trigger a tenant-scoped export via API, including a full export and then incremental delta export. Use a workflow orchestrator (T",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright to drive two browser contexts within the same tenant but different users. Seed a known document, simulate offline on one context, perform edits in both, reconnect, and verify final cont",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed a synthetic feature set into the feature store, drive 1000 req/min through canary/shadow paths, compare A vs B predictions within tolerance, ensure latency stays under 200ms avg, 95th percentile",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design comprehensive end-to-end tests for real-time features involving complex data flows, fault tolerance, and distributed system consistency.\n\n## Key Concepts\n- Real-time UI testing with latency constraints\n- Service mocking for deterministic test outcomes\n- Retry mechanisms with exponential backoff\n- Idempotency in distributed write operations\n- Eventual consistency across regional data stores\n- Test isolation and cleanup strategies\n\n## Code Example\n```javascript\n// Playwright test framework demonstrating mock transcription service",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-7",
        "questionId": "q-28172",
        "question": "Design an E2E test for a multi-region data ingestion pipeline using Kafka with Avro schemas and a registry. Simulate events across 3 regions, perform a schema evolution by adding an optional field, and verify compatibility across producers and consumers. Validate idempotent writes, out-of-order delivery, and dashboard correctness under burst load and partial outages; specify seeds, verifications, and rollback checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a beginner-friendly E2E test for an email verification flow in a fintech web app: signup, a one-time verification link, expiry handling, activation via the link, and preventing token reuse. Sp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage a multi-region test harness that seeds 1M events into Region A, with Kafka replication to Regions B and C and a Registry-backed Avro schema. Validate backward compatibility by running produce",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a beginner-friendly end-to-end test for a Databricks data ingestion pipeline: seed S3 with a small CSV dataset, trigger the batch job, validate the Delta Lake table schema and row count, enforce data quality rule…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed 3 tenants and test users; seed wallets and tokens; run a checkout path frontend -> auth -> wallet -> card gateway (3DS) -> ledger (async) with concurrent tenants. Inject gateway timeout and a led",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for cross-region data integrity with evolving schemas are critical for streaming platforms; this task probes end-to-end coverage of compatibility, idempotency, resilience under bursts, and dashboard fidelity.\n\n## Key Concepts\n- Multi-region Kafka replication and failover\n- Avro schema evolution and registry compatibility\n- Idempotent processing and replay handling\n- End-to-end validation of dashboards against raw data\n\n## Code Example\n```javascript\n// Seed payloads\nconst v1 = { userId: \"u1\", event: \"click\", ts: 12345 }\nconst v2 = { userId: \"u1\", event: \"click\", ts: 12346, browser: \"Chrome\" }\n```\n\n## Follow-up Questions\n- How would you detect schema drift in production?\n- How would you simulate network partitions to test replication lag?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-8",
        "questionId": "q-1335",
        "question": "Design an end-to-end test strategy for a multi-tenant SaaS app with per-tenant data isolation, dynamic feature flags, and role-based access controls. Outline how you seed tenants, run parallel tests across tenants and roles, validate feature gating, and verify audit logs across services. Include tooling choices, data isolation strategy, and cleanup?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright for end-to-end testing. The test uploads a 10,000-row CSV file, seeds required test data, and mocks the message bus to simulate partial failures with deterministic retries. Assert that the progress bar re…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed tenants and data via admin API using unique suffixes. Run parallel Playwright tests across tenants with roles admin, editor, and viewer. Validate feature flags by toggling at runtime and assertin",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a comprehensive end-to-end test plan leveraging a dedicated test harness that seeds 60 tenant contexts and executes concurrent onboarding workflows alongside production-simulated live usage actions. Include cross…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright for end-to-end dashboard verification, integrated with a test harness that seeds Kafka topics with test events, stubs external data feeds with deterministic mocks, and preloads Redis with isolated test st…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for multi-tenant SaaS with RBAC and feature flags are common in large orgs. This probes data isolation, dynamic config handling, and auditability under parallel execution.\n\n## Key Concepts\n- Multi-tenant data isolation and namespace scoping\n- Dynamic feature flags and runtime gating\n- RBAC coverage and cross-tenant access risk\n- Test data seeding, cleanup, and idempotence\n- Audit/log verification across services\n\n## Code Example\n```javascript\n// Example: seedTenant utility outline\nasync function seedTenant(client, tenantId) {\n  await client.post('/admin/tenants', { id: tenantId, name: `Tenant-${tenantId}` });\n  await client.post(`/admin/tenants/${tenantId}/seed`, { seedData: 'sample' });\n}\n```\n\n## Follow-up Questions\n- How would you prevent cross-tenant data leaks in flaky tests?\n- How do you scale seeds generation in CI for 100+ tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-9",
        "questionId": "q-2302",
        "question": "Design an end-to-end test for a real-time collaboration feature in a chat app with offline support where 3 clients edit the same message concurrently. Include how you simulate network partitions and reconciliations, verify offline queues, message ordering, conflict resolution, and eventual consistency across all clients?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Drive the flow with Playwright or Cypress in a dedicated test env; stub bank OAuth endpoints and data fetch; seed test users and synthetic accounts; use an idempotency key on token exchanges; assert l",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright to test a SaaS data export flow: seed test data (a user and dataset), apply date and status filters via the UI, click the Export button, mock the export service to resolve quickly, and stub email delivery…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Three parallel browser contexts connect to a test channel. Simulate offline by toggling network for one client. Have A edit the message, then with network down, B edits the same message. Restore netwo",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Approach: Use Playwright with two isolated browser sessions (UserA and UserB) to test offline-first real-time chat functionality. Steps: 1) Initialize both users and establish baseline connection; 2) Simulate network lo…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests must verify real-time collaboration correctness under imperfect networks, including offline queues and reconciliation, which is critical for user trust in chat apps.\n\n## Key Concepts\n- Real-time collaboration and optimistic updates\n- Offline support and reconciliation\n- Event propagation order and eventual consistency\n- Idempotency and conflict resolution strategies\n\n## Code Example\n```javascript\n// Playwright skeleton for multi-context test\nconst c1 = await browser.newContext();\nconst c2 = await browser.newContext();\nconst c3 = await browser.newContext();\nconst page1 = await c1.newPage();\nconst page2 = await c2.newPage();\nconst page3 = await c3.newPage();\n// Connect to channel, perform edits, toggle offline, then reconcile\n```\n\n## Follow-up Questions\n- How would you adapt if conflict resolution uses CRDT merging?\n- How would you scale this pattern to 100 concurrent clients and many channels?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-10",
        "questionId": "q-11082",
        "question": "Design a beginner-friendly end-to-end test for a feature-flag gated onboarding flow in a web app: after login, a user sees either a new multi-step onboarding or a quick tour based on an admin-toggle flag. Include data seeding, deterministic mocks for the flag, per-test isolation, and assertions that persist after refresh. Use Cypress or Playwright and outline concrete steps and verifications?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create two isolated test users; mock the flag service to return enabled for user A and disabled for user B. Write two E2E tests: (1) enabled path asserts the onboarding tour renders all steps and comp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a practical E2E test for an event-driven checkout with idempotent dedup: seed a fixed payment_intent_id, mock Stripe webhook twice (same id), ensure the warehouse writes one row with status pai",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed 50 drivers and 200 riders across three zones; trigger 120 concurrent ride requests per zone; publish a demand-spike event to the pricing engine (Kafka) and assert that ETA-based estimates and fin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a comprehensive end-to-end test for a feature-flag gated onboarding flow. Seed test user data with consistent identifiers, mock the admin API endpoints to deterministically control the onboarding flag state, a…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTesting feature-flag gated workflows ensures comprehensive coverage of user journeys across different configurations and prevents onboarding experience regressions when toggling between variants.\n\n## Key Concepts\n\n- Feature flag management and deterministic mocking\n- End-to-end test isolation and data seeding strategies\n- State persistence validation across browser refresh cycles\n- API mocking for controlled test scenarios\n\n## Code Example\n\n```javascript\n// Cypress implementation example\ndescribe('Feature-Flag Gated Onboarding Flow', () => {\n  beforeEach(() => {\n    cy.task('seedTestData', { userId: 'test-user-123' });\n    cy.intercept('GET', '/api/admin/flags*', { body: { onboardingEnabled: true } });\n  });\n\n  it('displays multi-step onboarding when flag is enabled', () => {\n    cy.login('test-user-123');\n    cy.get('[data-testid=\"onboarding-step-1\"]').should('be.visible');\n    cy.reload();\n    cy.get('[data-testid=\"onboarding-step-1\"]').should('be.visible');\n  });\n\n  afterEach(() => {\n    cy.task('cleanupTestData', { userId: 'test-user-123' });\n  });\n});\n```\n\n## Follow-up Questions\n\n- How would you handle flaky",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-11",
        "questionId": "q-9611",
        "question": "Design an E2E test for a multi-tenant real-time analytics dashboard that ingests events through a streaming pipeline (e.g., Kafka/Kinesis) and serves per-tenant dashboards with dynamic feature flags. Include data isolation, tenant bursts, flag toggling, and simulated outages; verify isolation, durability, and eventual consistency within 5 seconds. Provide tooling choices, seed data, and verification methods (latency, correctness, idempotency)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a 3-tenant test suite using a sandbox streaming cluster, seed distinct tenants, toggle feature flags per tenant, inject bursty events, simulate broker outages and replay, and verify per-tenant",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "I would drive an end-to-end flow with Playwright, seed two tenants, create a project in tenant A, invite a collaborator, and upload a blob via a mocked storage layer. Then trigger the create event twi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create three Playwright contexts simulating users A, B, C editing the same doc (docId=<UNIQUE_DOC_ID>) with WebSocket sync. Validate live presence cursors propagate in near real time, edits merge dete",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Orchestrate 6-8 synthetic participants across 3 regions via Playwright with mock media streams; seed a 500-participant meeting in a test env; emulate network conditions using tc/netem; capture WebRTC",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for streaming data at scale across multiple tenants, including feature flag effects, outages, and replay semantics, are critical for reliability.\n\n## Key Concepts\n- Streaming pipelines, exactly-once processing, per-tenant isolation\n- Dynamic feature flags, rollback paths, targeted rollouts\n- Outage simulation, event replay, idempotent handling\n\n## Code Example\n```javascript\n// Pseudo test: seed tenants, publish events, toggle flags, simulate outage, assert per-tenant results\n```\n\n## Follow-up Questions\n- How would you measure per-tenant convergence latency under backpressure?\n- How would you validate deterministic replay across a rolling upgrade?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-12",
        "questionId": "q-22417",
        "question": "Design an end-to-end test for a distributed, AI-assisted document collaboration tool (desktop/mobile) that integrates an LLM for real-time suggestions. Include multi-client edits, offline scenarios, model version rollout with feature flags, prompt safety guardrails, audit log integrity, and deterministic replay seeds for reproducibility. Specify tooling, data seeds, and verification methods?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright with a dedicated test tenant; seed tenant data; mock WebSocket match messages; stub the payment gateway to return a fixed token; trigger a trip request and advance simulated time to verify trip progressio…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test an end-to-end flow for a Salesforce-like storefront built with micro-frontends (Catalog, Cart, Checkout) controlled by feature flags. Run parallel across 3 regions with per-region data isolation.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an E2E matrix with 3 clients editing the same document, one offline; use CRDT-based sync for convergence; rollout two model versions behind a feature flag; mock LLM outputs to enforce safety c",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Provision a new tenant, seed tenant_id, enable the feature flag for that tenant, and drive UI flows with Playwright plus API calls scoped to that tenant. Verify isolation by attempting cross-tenant ac",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for AI-assisted real-time collaboration demand end-to-end coverage of multi-client sync, offline resilience, model versioning, guardrails, and auditability. This ensures the system remains correct, observable, and reproducible during complex deployments.\n\n## Key Concepts\n- AI-assisted real-time collaboration\n- End-to-end test matrix with offline scenarios\n- CRDT-based synchronization and conflict resolution\n- Feature flags and model version rollout\n- Prompt safety guardrails and audit log integrity\n- Deterministic replay seeds for reproducibility\n\n## Code Example\n\n```javascript\n// Pseudo test scaffold integrating Playwright + a mock LLM\ndescribe('AI-assisted doc E2E', () => {\n  it('validates offline-online converge and audit replay', async () => {\n    // setup seeds, mocks, and env\n  })\n})\n```\n\n## Follow-up Questions\n- How would you ensure deterministic LLM outputs in tests?\n- What instrumentation would you add to observability for failures?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-13",
        "questionId": "q-979",
        "question": "Design an end-to-end test for a multi-tenant content platform that serves regionally personalized content with feature flags and A/B tests. The platform must ensure per-tenant data isolation, correct content personalization, flag-driven UI, and during regional outages. Outline the test scope, data management, tooling, and steps to verify end-to-end delivery across tenants and regions without flakiness?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "getByRole() locates elements by their ARIA role and accessible name, making it more resilient to UI text changes and better for accessibility testing. getByText() matches visible text content directly, which can be brit…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a test plan using Playwright for UI, API mocks for personalization, and a seeded tenant registry. Verify per-tenant data isolation, region-specific content, feature-flag-driven UI, and A/B pat",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design an end-to-end test for a multi-tenant streaming analytics platform where tenants ingest real-time events, dashboards update over WebSocket, and data must be strictly isolated per tenant across cache and storage.…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright with per-tenant seeds and region-scoped data stores. Create three tenants, each with distinct tax rules and promos, enable flags, and drive the end-to-end checkout: cart total, discount",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests at scale must validate multi-tenant data isolation, region-resident content, and flag-driven UX under outages. This probes orchestration, data management, and resilience.\n\n## Key Concepts\n- Multi-tenancy isolation and data residency\n- Content personalization pipelines and identity graphs\n- Feature flags and A/B test routing\n- Regional outages and retry strategies\n- Test data seeding and reproducibility\n\n## Code Example\n```javascript\nimport { test, expect } from '@playwright/test';\n\ntest('e2e: multi-tenant personalization with feature flags', async ({ page }) => {\n  const tenant = 'tenantA';\n  await page.route('https://content.example.com/api/personalize', route =>\n    route.fulfill({ status: 200, body: JSON.stringify({ banner: 'A', content: 'X' }) })\n  );\n  await page.goto(`https://content.example.com/${tenant}?region=eu`);\n  await expect(page.locator('#banner')).toHaveText('A');\n});\n```\n\n## Follow-up Questions\n- How would you validate data isolation with real data vs. mock data across tenants?\n- How do you scale tests across regions without flakiness and ensure coverage for outage scenarios?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-14",
        "questionId": "q-208",
        "question": "What is the difference between Selenium WebDriver and Selenium Grid, and when would you use each in your testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a E2E test for a CMS publish flow: author drafts, autosaves every 30s, submits for review; a separate reviewer approves in another session; on publish, confirm the article appears in the live",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "WebDriver automates individual browsers locally, while Selenium Grid orchestrates distributed test execution across multiple machines and browser configurations through a hub-node architecture.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright or Cypress to verify cross-origin iframe behavior: load host app, access the iframe via frameLocator, simulate postMessage exchanges, trigger autosave by typing, inject a flaky network",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Provide a test harness that seeds per-tenant workloads, uses k6 for REST, a WebSocket client for streaming, and a multi-tenant simulator to enforce quotas. Verify strict tenant isolation, test hot-swa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why Asked\nInterview context: Tests understanding of scalable test infrastructure design and architectural trade-offs in enterprise testing environments.\n\n## Key Concepts\nCore knowledge: WebDriver for single browser automation, Grid's hub-node topology for distributed testing, session management, resource allocation, parallel execution strategies.\n\n## Architecture Deep Dive\nSelenium Grid uses a centralized hub that manages test sessions and routes requests to distributed nodes. The hub acts as a load balancer and session manager, while nodes register their capabilities (browser types, versions, platforms) and execute actual browser sessions. Each node can handle multiple concurrent sessions based on available resources, typically 4-6 sessions per 8GB RAM.\n\n## Large-Scale Use Cases\nNetflix runs 50,000+ daily parallel tests using multi-region hub architecture to reduce latency and improve fault tolerance. E-commerce platforms leverage Grid for Black Friday readiness testing, simulating thousands of concurrent users across Chrome, Firefox, Safari, and mobile browsers. CI/CD pipelines integrate Grid for comprehensive cross-browser validation before deployments.\n\n## Code Example\n```java\n// Grid configuration for large-scale testing\nDesiredCapabilities caps = new DesiredCapabilities();\ncaps.setBrowserName(\"chrome\");\ncaps.setPlatform(Platform.WINDOWS_10);\ncaps.setVersion(\"latest\");\n\n// Connect to load-balanced hub\nURL hubUrl = new URL(\"https://grid.company.com:4444/wd/hub\");\nWebDriver driver = new RemoteWebDriver(hubUrl, caps);\n\n// Configure session timeout and retry logic\ndriver.manage().timeouts().pageLoadTimeout(30, TimeUnit.SECONDS);\n```\n\n## Performance Optimization\nImplement session pooling with pre-warmed browsers, set aggressive 300-second idle timeouts, and monitor memory usage (1.5GB per session). Use health checks every 30 seconds to detect zombie nodes, and configure auto-scaling based on queue depth. Always call driver.quit() instead of driver.close() to ensure proper cleanup and prevent memory leaks.",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-15",
        "questionId": "q-30369",
        "question": "Design an E2E test for a feature-flag gated onboarding flow: a new funnel shows to a 25% user cohort controlled by a server flag. Include: (a) verification that flagged users see the new flow and unflagged see the old, (b) mid-session flag flip does not break UX, (c) deterministic data seeding and API mocks, (d) desktop and mobile coverage, (e) Cypress or Playwright choice with seed identities and isolated mocks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed a fresh user, clear storage, and isolate data per run. Mock geolocation to a fixed point, throttle network to 3G, and stub maps autocomplete responses. Verify pickup/dropoff fields populate, ride",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: seed test tenant, mock Jira and Slack endpoints, enqueue a new approval pipeline via REST, verify Jira issue is created, Slack notification posts in channel with a link to the issue, and an",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan to mock the remote flag service to inject per-user segments. Use two test users: ON and OFF. Verify that with ON the checkout totals show the discount and the promo banner is visible; with OFF no",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use server flag fixtures to drive two cohorts: flagged users see the new onboarding, others see the old flow. In Playwright, route /api/flags to return true/false per test, seed user_id in localStorag",
            "isCorrect": true
          }
        ],
        "explanation": "This question evaluates practical E2E test design for feature flags, including determinism, data isolation, cross-device coverage, and handling mid-session flag changes. It requires concrete tooling choices, mocking strategies, and robust assertions to prevent flaky tests while validating user-journeys under real-world conditions.",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-16",
        "questionId": "q-22692",
        "question": "Design an end-to-end test for a real-time collaborative document editor where two users edit a shared document; verify live updates via WebSocket between two browser sessions, simulate offline edits and reconciliation when reconnecting, and ensure final content matches the chosen conflict-resolution policy. Specify tooling (Playwright or Cypress), data seeds, latency/mode mocks, and eventual consistency checks within 30s?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose an E2E matrix with 3 clients editing the same document, one offline; use CRDT-based sync for convergence; rollout two model versions behind a feature flag; mock LLM outputs to enforce safety c",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright to create two isolated browser contexts (A and B) connected to the same shared document via WebSocket. **Setup:** 1. Seed a test document via API with known initial content 2. Launch both browser contexts…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Seed 3 tenants and emit trades with varied latency to Kafka; simulate regional partition, then resume and verify deduplicated, consistent state across dashboards within SLA. Validate per-tenant isolat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: create a dedicated test tenant; seed 1 product; mock payment gateway with idempotency keys; drive through checkout via API/UI; assert that the payment is authorized once, invoice created, an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Test Matters\nThis E2E test verifies your collaborative editor handles:\n- Real-time synchronization between multiple concurrent users\n- Offline editing with local queuing\n- Conflict resolution when connections resume\n- Eventual consistency guarantees\n\n## Key Components\n\n**Tooling: Playwright over Cypress**\n- Native multi-context isolation for true multi-user simulation\n- Built-in WebSocket inspection via `page.on('websocket')`\n- First-class offline simulation via `context.setOffline()`\n- Better control over network conditions with `route()` and `setOffline()`\n\n**Test Data Setup**\n- Seed document via REST API in `beforeAll` hook\n- Ensures deterministic starting state for both browser contexts\n- Document ID shared between contexts guarantees same collaborative session\n\n**Network Simulation**\n| Scenario | Method |\n|----------|--------|\n| Offline mode | `context.setOffline(true/false)` |\n| Latency | `route.continue({ delay: ms })` |\n| Packet loss | `server.setRealtimeEnabled(false)` (server-side) |\n| WebSocket intercept | `page.on('websocket')` + frame handlers |\n\n**Conflict Resolution Policies Tested**\n1. **CRDT (Conflict-free Replicated Data Type):** All edits merge automatically; use when partial edits must be preserved\n2. **Last-Writer-Wins (LWW):** Most recent timestamp wins; simpler but may lose edits\n3. **Operational Transformation (OT):** Transform operations against each other; common in Google Docs-style editors\n\n**Eventual Consistency Verification**\n- Use `expect.poll()` for async assertions within timeout windows\n- Compare content strings between contexts\n- Verify WebSocket message ordering and deduplication\n- Assert final state matches expected conflict-resolution outcome\n\n**Assertions Checklist**\n- [ ] Real-time sync: User B sees User A's edit within 5s\n- [ ] Offline indicator: UI reflects disconnected state\n- [ ] Reconnection: `setOffline(false)` triggers sync\n- [ ] Content merge: All edits present (CRDT) or winner selected (LWW)\n- [ ] Eventual consistency: Both contexts identical within 30s\n- [ ] WebSocket frames: Messages logged for debugging",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-17",
        "questionId": "q-9804",
        "question": "Design an end-to-end test for a real-time collaborative document editor embedded in a SaaS app. Use two browser sessions to simulate concurrent edits on the same document, verify live updates under injected latency, CRDT/OT reconciliation, and persistence after save; cover offline/reconnect paths, data isolation, and deterministic ordering; specify tooling (Playwright or Cypress) and how you orchestrate test data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Two Playwright contexts simulate concurrent edits on the same document. Inject latency to test real-time delta propagation, then validate CRDT reconciliation yields final content that includes both users' changes with p…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design an end-to-end test using Playwright for web and Appium for mobile. Seed 3 time zones with recurring events; run 4 clients, simulate offline edits and a concurrent change; verify convergence wit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Three parallel browser contexts connect to a test channel. Simulate offline by toggling network for one client. Have A edit the message, then with network down, B edits the same message. Restore netwo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Mock the external KYC API and the event bus to deterministic endpoints; isolate data per test tenant; verify idempotent signup with the same user_id; test eventual consistency across regional replicas",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes proficiency with real-time systems in E2E tests, including multi-user concurrency, latency handling, offline scenarios, and data isolation. It reveals how you design deterministic, reproducible tests for complex client-server interactions.\n\n## Key Concepts\n- Real-time collaboration testing\n- CRDT/OT reconciliation validation\n- Multiple browser contexts orchestration\n- Latency injection and offline/reconnect paths\n- Data isolation and deterministic ordering\n- Test data management and cleanup\n\n## Code Example\n```javascript\n// Playwright scaffold (pseudo)\nconst docId = `test_${Date.now()}`\nconst context1 = await browser.newContext()\nconst page1 = await context1.newPage()\nconst context2 = await browser.newContext()\nconst page2 = await context2.newPage()\n\n// Setup: both users open same document\nawait page1.goto(`/editor/${docId}`)\nawait page2.goto(`/editor/${docId}`)\n\n// Concurrent edits with latency injection\nawait page1.route('**/socket.io/**', route => {\n  setTimeout(() => route.continue(), 100) // Inject 100ms latency\n})\n\nawait page1.type('[contenteditable]', 'User 1 text')\nawait page2.type('[contenteditable]', 'User 2 text')\n\n// Verify real-time updates and CRDT reconciliation\nconst finalContent1 = await page1.textContent('[contenteditable]')\nconst finalContent2 = await page2.textContent('[contenteditable]')\nexpect(finalContent1).toBe(finalContent2) // Deterministic final state\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-18",
        "questionId": "q-3873",
        "question": "Design an E2E test for a Salesforce-like storefront built with micro-frontends (Catalog, Cart, Checkout) controlled by feature flags. Run parallel across 3 regions with per-region data isolation. Validate flag rollout/rollback, end-to-end flow cart -> checkout -> payment, and cross-service eventual consistency among order, inventory, and payment services. Specify tooling, data isolation, idempotent retries, and flaky-test mitigation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright for this E2E test. First, upload a fixture CSV containing 500 rows. Then intercept the POST request to /import and mock the job queue with a deterministic response to simulate asynchronous processing. See…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Conduct an offline-first E2E test across two devices and two tenants. Seed accounts A1, B1 with notes. On Device 1 offline, create/edit note N; On Device 2 offline, apply a conflicting change to N. Re",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test an end-to-end flow for a Salesforce-like storefront built with micro-frontends (Catalog, Cart, Checkout) controlled by feature flags. Run parallel across 3 regions with per-region data isolation.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design an E2E for a Slack-like chat: a user sends a message in a channel; verify real-time delivery to all connected clients via WebSocket; simulate an offline client that reconnects later and ensures",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nInterview context explanation.\n\n## Key Concepts\n\n- Micro-frontend testing strategy\n- Feature flag validation across regions\n- End-to-end across order, inventory, payment\n- Test isolation, idempotent retries, flaky-test detection\n- Orchestration with parallel regions\n\n## Code Example\n\n```javascript\n// Example test scaffold\nconst regions = ['us-east', 'eu-west', 'ap-south'];\nregions.forEach(region => {\n  it(`e2e region ${region} with flag X`, async () => {\n    // setup region data scoped by testId\n    // enable flagX (A/B)\n    // perform cart -> checkout -> payment\n    // verify inventory updated, order created, payment recorded\n  });\n});\n```\n\n## Follow-up Questions\n\n- How would you implement region data cleanup after tests?\n- How would you measure test flakiness and decide when to rerun?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-19",
        "questionId": "q-4930",
        "question": "You’re testing an admin-driven per-user feature flag in a SaaS app. The API toggles a 'CompactDashboard' flag for a specific user; on login, the UI should render the compact dashboard when the flag is on and the full dashboard when off. Design a beginner-friendly E2E test that seeds a test user, enables the flag via API, logs in, and asserts the compact UI; then disables and re-asserts the full UI. Include data isolation and reset steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright with two isolated browser contexts editing the same document. Stub autosave to a fixed 2-second interval, mock the server clock, and simulate WebSocket presence events. Validate that live edits propagate…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline approach: seed tenants/channels; spin up 3 regions with presence and messaging services; simulate 1000 clients joining a channel; enable moderation flag in region A and gradually roll to B/C;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed a test user, then call the flag API to enable CompactDashboard for that user. Log in as that user and verify the UI renders the compact dashboard (check for a specific element). Reload to confirm",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "I would implement a Playwright test that seeds two tenants: one with the data-sharing feature flag enabled, one disabled. The test visits /dashboard for each tenant, mocks the feature-flag and consent APIs, and asserts…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for per-user feature flags are common in SaaS apps. This question checks the ability to seed data, perform API-driven state changes, and verify UI behavior across login and page reloads in a beginner-friendly way.\n\n## Key Concepts\n- Per-user feature flags and API seed/reset\n- End-to-end UI verification of dynamic layouts\n- Test isolation and deterministic CI runs\n\n## Code Example\n```javascript\n// Example using Playwright-like syntax (pseudocode)\n// seedUser(), setFlag(), login() are helper utilities\ntest('compact dashboard toggling', async ({ page, request }) => {\n  const { userId, email, password } = await seedUser(request);\n  await setFlag(request, userId, 'CompactDashboard', true);\n  await login(page, email, password);\n  await expect(page.locator('.dashboard-compact')).toBeVisible();\n  await page.reload();\n  await expect(page.locator('.dashboard-compact')).toBeVisible();\n  await setFlag(request, userId, 'CompactDashboard', false);\n  await page.reload();\n  await expect(page.locator('.dashboard-full')).toBeVisible();\n});\n```\n\n## Follow-up Questions\n- How would you guard against flaky UI assertions caused by caching?\n- How would you extend the test to cover multiple user segments (roles/regions) and parallel execution?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-20",
        "questionId": "q-4497",
        "question": "Design a GDPR 'right to be forgotten' E2E test for a ride-booking platform: deleting a user should redact personal data across auth, profiles, payments, messages, and audit logs, emit webhook deletion events, and purge data in backups within 24 hours. Explain test data isolation, region-aware backfills, and idempotent retries under simulated outages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Enable the feature flag for a test tenant via API, seed a test user with the required role, launch the app, and open a channel. Assert the emoji picker is visible and introduces a reaction event acros",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright with three isolated pages to simulate participants. Mock navigator.mediaDevices.getUserMedia to provide fake streams and stub the signaling channel to a local mock server. Use unique me",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline an E2E flow that seeds a synthetic user across regions, triggers account deletion, and asserts redaction in all services (auth, profile, payments, messages, logs). Verify deletion webhooks fir",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Seed realistic portfolios and liquidity pools; execute parallel end-to-end flows across multiple regions with mixed order types (market, limit, stop), simulating partial fills and network partitions. Validate matching e…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests privacy, data integrity, and cross-service deletion in distributed systems, ensuring redaction, reliable webhook signaling, and region-specific isolation under failures.\n\n## Key Concepts\n\n- GDPR data deletion across microservices\n- Data redaction and audit trails\n- Webhook semantics and idempotency\n- Regional data isolation and backup purge timing\n\n## Code Example\n\n```javascript\n// Pseudo-test outline (conceptual)\ndescribe('GDPR delete flow', ()=> {\n  it('redacts across services and emits webhooks', async ()=> {\n    // seed user in regions\n    // trigger delete\n    // assert redacted fields in auth/profile/payments/messages/logs\n    // assert webhooks emitted once per region\n  })\n})\n```\n\n## Follow-up Questions\n\n- How would you verify purge timing within 24h across regions?\n- How would you simulate partial cloud outages during purge?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-21",
        "questionId": "q-9303",
        "question": "Design a beginner-friendly end-to-end test for a feature-flagged 'Express Checkout' flow in a shopping app. The flag is toggled by an API and cached client-side. With flag off, ensure standard checkout; with flag on, verify Express Checkout appears, applies a 5% discount, and completes in one click. Include data seeding, test isolation, mid-session flag toggle, and verification of totals and retries. Use Cypress or Playwright; provide concrete steps and checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Provision a new tenant, seed tenant_id, enable the feature flag for that tenant, and drive UI flows with Playwright plus API calls scoped to that tenant. Verify isolation by attempting cross-tenant ac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use two Playwright browser contexts to simulate two users simultaneously; seed a shared note in both contexts; put one context offline using network throttling, then make edits; have the other user edit the same note wh…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed test user/cart via API. First run: flag off, complete standard checkout; Second run: enable flag via API, reload, verify Express Checkout appears, 5% discount applies, total matches expectation,",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Cypress with cy.clock to advance time, fill first/last name, trigger autosave via a timer, then verify localStorage has the expected JSON under a key like 'profile_wizard'. Reload and confirm fiel",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests involving feature flags validate UI correctness, data isolation, and resilience to dynamic config changes. The mid-session toggle mirrors real-world deployments where flags flip without full reloads.\n\n## Key Concepts\n- Feature flags and caching behavior\n- Test data seeding and isolation\n- Network route interception and deterministic mocks\n- Client-side cache invalidation\n- Idempotency and retry handling\n\n## Code Example\n```javascript\n// Playwright sketch: mock flag endpoint and seed data\npage.route('**/api/feature-flags**', route =>\n  route.fulfill({ status: 200, body: JSON.stringify({ expressCheckout: false }) })\n);\n```\n\n## Follow-up Questions\n- How would you parameterize tests for multiple flag states?\n- How would you handle race conditions if the flag flips during checkout?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-22",
        "questionId": "q-8063",
        "question": "Design an E2E test for a dynamic ride-request flow where ETA, route optimization, and pricing update in real time from an external traffic feed. The system is multi-region and supports 1k+ concurrent rides. Include test data seeding, traffic outage simulation, per-region isolation, failover behavior, and idempotent retries in booking and payment sync?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design dual, canary-enabled flows: seed deterministic synthetic transaction streams; generate 100k events/sec; verify fraud scores and decisions match canonical rules; assert ledger immutability and t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Approach: Use Playwright with two isolated browser sessions (UserA and UserB) to test offline-first real-time chat functionality. Steps: 1) Initialize both users and establish baseline connection; 2) Simulate network lo…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed per-region drivers and trips; mock traffic feed with outages; run 1k+ concurrent ride requests across regions; verify ETA, routing, and pricing converge within 2–5s per region; test degraded beha",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Choose Cypress for UI checks, a Kafka test harness for event streams, and a mock feature-flag service to simulate A/B groups. Seed 2 cohorts; inject network latency and a model outage to verify fallba",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for real-time, multi-region flows with external feeds expose timing, data consistency, and failover edge cases often missed by unit tests. This question probes data seeding, concurrency, region isolation, cache/failover, and idempotent retries.\n\n## Key Concepts\n\n- End-to-end testing with streaming data\n- Multi-region data isolation\n- Realistic load and latency budgets\n- Cache, failover, and degraded mode\n- Idempotency and retry semantics\n\n## Code Example\n\n```javascript\n// Pseudo-test harness sketch\n```\n\n## Follow-up Questions\n\n- How would you instrument observability for this test?\n- How would you handle flaky results due to timing?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-23",
        "questionId": "q-27922",
        "question": "Design an E2E test for a multi-tenant marketplace with per-tenant feature flags and blue/green deployments. Validate correct feature visibility, data isolation, and rollback safety across 100 tenants during phased rollout. Include propagation of flag state through API, WebSocket, and cache layers, deterministic seeds, and failure injections to prove no cross-tenant leakage and rollback within 60 seconds?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright's test runner to create a test that navigates to the login page, fills credentials using `page.fill()`, submits the form with `page.click()`, and verifies successful login by checking the URL or page cont…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use 100 tenants, staggered flag rollout, and blue/green environments. Verify per-tenant flag visibility across API, WebSocket, and caches; ensure data isolation by tenancy keys; inject deterministic f",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright to drive the onboarding flow with a unique idempotency-key header. Mock identity, provisioning, and email services with deterministic stubs (WireMock/MSW). Simulate a regional email out",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two scenarios: flag off and on. Seed a test user with role analyst and enable the feature flag for that user. Mock the export service to return a small CSV. Verify: button hidden when off; a single CS",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes complex multi-tenant, feature-flag, and deployment safety concerns that are realistic in large marketplaces.\n\n## Key Concepts\n\n- Multi-tenant data isolation\n- Feature flag propagation through API, WebSocket, and caches\n- Blue/green deployment and rollback semantics\n- Deterministic seeding and fault injection\n- End-to-end verification across layers\n\n## Code Example\n\n```javascript\n// Pseudo-test outline showing setup and verifications\n```\n\n## Follow-up Questions\n\n- How would you scale the tenant pool beyond 100?\n- How would you monitor rollback health in production?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-24",
        "questionId": "q-26666",
        "question": "Design a beginner-friendly E2E test for a feature-flag gated UI in a Twitter-like app. Include seeding two users with cohorts 0 and 1, mocking a remote config rollout at 10% for cohort 1, verifying cohort 0 sees the old composer and cohort 1 sees the new composer, both able to post and see posts, and a mid-session flip to ensure UI updates without breaking?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright with separate browser contexts for Tenant A and Tenant B. Seed deterministic analytics data and mock external APIs. Enable the 'newAnalyticsPanel' feature flag for Tenant A via the admin API, authenticate…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright. Seed User A with cohort 0 and User B with cohort 1. Mock the remote config API to return a 10% rollout for cohort 1. Load the home page and verify that cohort 0 sees the old composer while cohort 1 sees…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a comprehensive end-to-end test suite for a serverless extensions marketplace that validates the following: Create 3 distinct tenants and seed per-tenant reference data (users, channels, settings). Deploy 4 sandb…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed a doc; open on web-online and mobile-offline. Make conflicting edits offline and online; reconnect mobile to trigger sync. Validate the merge outcome matches the policy (CRDT/OT or last-writer-wi",
            "isCorrect": false
          }
        ],
        "explanation": "## Purpose\n\nThis question evaluates practical E2E testing skills, specifically around runtime feature flags, test data seeding, and UI assertions in a social application context.\n\n## Key Concepts\n\n- Feature flag rollout strategies\n- Test data seeding for user cohorts\n- Mocking remote config services\n- UI assertions for different feature states\n- Stability under mid-flight config changes\n\n## Code Example\n\n```javascript\nconst { test, expect } = require('@playwright/test');\n\ntest('feature-flag gated composer', async ({ page, context }) => {\n  // Seed users and mock remote config\n  const userA = await seedUser({ cohort: 0 });\n  const userB = await seedUser({ cohort: 1 });\n  \n  await context.addCookies([\n    { name: 'user_id', value: userA.id, domain: 'localhost' },\n    { name: 'cohort', value: '0', domain: 'localhost' }\n  ]);\n  \n  // Mock remote config for 10% rollout to cohort 1\n  await page.route('**/remote-config', async (route) => {\n    route.fulfill({ json: { composer_rollout: { cohort: 1, percentage: 10 } } });\n  });\n  \n  // Verify cohort 0 sees old composer\n  await page.goto('/home');\n  await expect(page.locator('.old-composer')).toBeVisible();\n  await expect(page.locator('.new-composer')).not.toBeVisible();\n  \n  // Post and verify\n  await page.fill('.tweet-input', 'Tweet from cohort 0');\n  await page.click('.submit-tweet');\n  await expect(page.locator('.tweet:has-text(\"Tweet from cohort 0\")')).toBeVisible();\n});\n\ntest('mid-session config flip', async ({ page }) => {\n  await page.addInitScript(() => {\n    window.__INITIAL_CONFIG__ = { composer_rollout: { cohort: 1, percentage: 0 } };\n  });\n  \n  await page.goto('/home');\n  await expect(page.locator('.old-composer')).toBeVisible();\n  \n  // Flip config mid-session\n  await page.addInitScript(() => {\n    window.__REMOTE_CONFIG__ = { composer_rollout: { cohort: 1, percentage: 100 } };\n  });\n  await page.reload();\n  \n  await expect(page.locator('.new-composer')).toBeVisible();\n});\n```\n\n## What Good Answers Demonstrate\n\n- Understanding of Playwright fundamentals (page objects, context, routing)\n- Knowledge of feature flag architecture and cohort-based rollouts\n- Ability to mock external services for deterministic testing\n- Consideration of edge cases like mid-flight config changes\n- Clean, maintainable test structure with clear assertions",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-05T05:15:14.627Z",
    "version": 1
  }
]