[
  {
    "id": "test-e2e-testing",
    "channelId": "e2e-testing",
    "channelName": "E2E Testing",
    "title": "E2E Testing Knowledge Test",
    "description": "Test your understanding of E2E Testing with 25 questions.",
    "questions": [
      {
        "id": "e2e-testing-q-0",
        "questionId": "q-208",
        "question": "What is the difference between Selenium WebDriver and Selenium Grid, and when would you use each in your testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright with a dedicated test tenant; seed tenant data; mock WebSocket match messages; stub the payment gateway to return a fixed token; trigger a trip request and advance simulated time to verify trip progressio…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an end-to-end test that seeds per-tenant loan data via API, navigates a mobile web loan flow in the frontend, simulates credit check latency with network throttling, and triggers a partial o",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "WebDriver automates individual browsers locally, while Selenium Grid orchestrates distributed test execution across multiple machines and browser configurations through a hub-node architecture.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Mock the external KYC API and the event bus to deterministic endpoints; isolate data per test tenant; verify idempotent signup with the same user_id; test eventual consistency across regional replicas",
            "isCorrect": false
          }
        ],
        "explanation": "## Why Asked\nInterview context: Tests understanding of scalable test infrastructure design and architectural trade-offs in enterprise testing environments.\n\n## Key Concepts\nCore knowledge: WebDriver for single browser automation, Grid's hub-node topology for distributed testing, session management, resource allocation, parallel execution strategies.\n\n## Architecture Deep Dive\nSelenium Grid uses a centralized hub that manages test sessions and routes requests to distributed nodes. The hub acts as a load balancer and session manager, while nodes register their capabilities (browser types, versions, platforms) and execute actual browser sessions. Each node can handle multiple concurrent sessions based on available resources, typically 4-6 sessions per 8GB RAM.\n\n## Large-Scale Use Cases\nNetflix runs 50,000+ daily parallel tests using multi-region hub architecture to reduce latency and improve fault tolerance. E-commerce platforms leverage Grid for Black Friday readiness testing, simulating thousands of concurrent users across Chrome, Firefox, Safari, and mobile browsers. CI/CD pipelines integrate Grid for comprehensive cross-browser validation before deployments.\n\n## Code Example\n```java\n// Grid configuration for large-scale testing\nDesiredCapabilities caps = new DesiredCapabilities();\ncaps.setBrowserName(\"chrome\");\ncaps.setPlatform(Platform.WINDOWS_10);\ncaps.setVersion(\"latest\");\n\n// Connect to load-balanced hub\nURL hubUrl = new URL(\"https://grid.company.com:4444/wd/hub\");\nWebDriver driver = new RemoteWebDriver(hubUrl, caps);\n\n// Configure session timeout and retry logic\ndriver.manage().timeouts().pageLoadTimeout(30, TimeUnit.SECONDS);\n```\n\n## Performance Optimization\nImplement session pooling with pre-warmed browsers, set aggressive 300-second idle timeouts, and monitor memory usage (1.5GB per session). Use health checks every 30 seconds to detect zombie nodes, and configure auto-scaling based on queue depth. Always call driver.quit() instead of driver.close() to ensure proper cleanup and prevent memory leaks.",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-1",
        "questionId": "q-7714",
        "question": "Design an E2E test for a data-sharing platform that enforces RBAC and DLP across a multi-tenant environment. Include roles (data owner, editor, auditor, admin), policy-driven access checks, audit log integrity, and data export redaction. Show seeds, mocks, and verification of tamper-evident logs, plus handling of flaky external services and time-based retention?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger DSAR for a test user; confirm removal from primary DBs, propagate deletion events to the event store, invalidate caches, and scrub materialized views after TTL; verify soft-delete tombstones a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed tenants with deterministic RBAC data; create users for owner, editor, auditor, and admin; drive API/UI flows to enforce permissions and verify both allowed and denied actions. Validate audit logs",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I would implement a Playwright test: log in as a trial user, create a new org, and mock /api/flags to toggle guided-tour on or off. When on, verify the tour overlay renders its steps; when off, verify",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: simulate real traffic with two models, route by tenant metadata and feature flags, implement a canary ramp (0→50→100%), verify latency targets and accuracy SLAs, and ensure non-canary reques",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests critical authorization paths, data-loss prevention, and audit integrity in multi-tenant setups, plus resilience to flaky services and retention policies.\n\n## Key Concepts\n\n- RBAC and DLP enforcement across tenants\n- Tamper-evident audit logs (sequencing, signatures/HMAC)\n- End-to-end data export redaction\n- Seeds and mocks for deterministic, isolated tests\n- Flaky service handling and time-based retention checks\n\n## Code Example\n\n```javascript\n// Pseudo end-to-end test sketch (Playwright/Cypress-agnostic)\nasync function testRBACAndDLP() {\n  const t = seedTenant('tenantA');\n  const uOwner = seedUser(t, 'owner');\n  const uEditor = seedUser(t, 'editor');\n  const uAuditor = seedUser(t, 'auditor');\n  const admin = seedUser(t, 'admin');\n\n  // Owner creates a share with PII\n  await login(uOwner);\n  await createShare({ tenant: t.id, data: 'PII' });\n\n  // Editor attempts restricted action\n  await login(uEditor);\n  assertAccessDenied(editShare(t.id));\n\n  // Auditor reads audit trail\n  await login(uAuditor);\n  assertAuditIncludes(t.id, 'READ', uAuditor.id);\n\n  // Admin exports with redaction\n  await login(admin);\n  const exportPayload = await exportData(t.id);\n  assertRedacts(exportPayload, ['PII']);\n  // Verify tamper-evident log\n  assertTamperEvidence(t.id);\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-2",
        "questionId": "q-5280",
        "question": "Design an E2E test for a Discord-like real-time chat service with 3 regional shards, 5k channels, and 20k users. Validate message ordering, edits, deletions, reactions, and presence during simulated network partitions and reconnections. Seed per-region data, inject outages, and verify eventual consistency across shards within 5 seconds. Specify tooling, data seeds, and verification approaches (causal/Lamport ordering)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a mock flag store with per-tenant precedence (override > regional default > global default). Seed 200 tenants across 3 regions, perform concurrent toggles and simulate propagation latency, verify",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed 3 tenants with isolated topics, stream deterministic events via Kafka into Spark then into the warehouse, and validate dashboards per tenant only show their data. Include backfill replay, tenant-",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed regional customers, inventory, and payments. Publish an OrderRequested event to checkout.topic, enable a feature flag, and verify exactly-once writes to the order_ledger by replaying the event wi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use 3 region-local test runners simulating 20 users per region across 5k channels. Seed per-region users and channels, then drive message sends, edits, deletions, and reactions while toggling a region",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests end-to-end coverage for real-time cross-region chat at scale, including partition tolerance and causal consistency, which are critical in services like Discord.\n\n## Key Concepts\n\n- Multi-region E2E orchestration\n- Presence, edits, deletions, reactions\n- Network partitions and reconciliations\n- Causal ordering (Lamport/vector clocks)\n- Data seeding and per-region isolation\n\n## Code Example\n\n```javascript\n// Pseudo seed and partition simulation snippet\nasync function seedRegion(region, count) { /* ... */ }\n```\n\n## Follow-up Questions\n\n- How would you measure convergence latency?\n- How would you adapt when adding a new region?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-3",
        "questionId": "q-20317",
        "question": "Design a beginner-friendly E2E test for an admin data export in a Salesforce-like app: a user requests an export of selected contact fields; export is processed asynchronously via a job queue and emailed as a CSV attachment. Include data seeding, mocking the email service, verifying the CSV contents (headers and record count), and idempotent behavior (re-requesting yields the same file without creating duplicate export jobs). Specify tooling (Cypress or Playwright) and a 2-minute verification window?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed 100 contacts via API; use Playwright to log in as admin, open Export panel, select fields (Name, Email, Company), click Export; intercept the email service and capture the CSV; poll a status endp",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Seed tenants A and B with isolated datasets. Install a widget from the remote registry for tenant A and verify it renders correctly in the dashboard. Drive per-widget WebSocket updates and confirm tenant A receives live…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt a centralized flag service with per-shard seeds; run parallel E2E runs across web/mobile. Validate 0/5/25/100% canaries by asserting UI states, auth validity, cart totals, checkout flows, and an",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed per-tenant datasets with representative samples; drive the ML suggestion module with drift scenarios including concept drift, data drift, and model degradation; verify the complete end-to-end task lifecycle: create…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAsynchronous exports and external services are common in enterprise apps; this tests timing, determinism, and data isolation.\n\n## Key Concepts\n\n- Asynchronous job processing\n- Data seeding and isolation\n- Mocking external services (email)\n- Idempotent operations\n\n## Code Example\n\n```javascript\n// Playwright pseudo-test for idempotent export\ntest('export is idempotent and emailed', async ({ page, request }) => {\n  // seed data\n  // trigger export with fields\n  // poll status until completed\n  // intercept and verify CSV attachment\n  // resubmit export and verify same file is delivered\n});\n```\n\n## Follow-up Questions\n\n- How would you handle large exports and streaming?\n- How would you ensure security and access control for exports?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-4",
        "questionId": "q-26497",
        "question": "Design a beginner-friendly E2E test for a feature-flag gated onboarding flow in a fintech web app. The onboarding has two variants controlled by a remote flag service. Include: seeding a new user, mocking the flag service, verifying variant-specific UI steps render, navigating between steps, and ensuring enabling/disabling the flag mid-session does not replay onboarding events. Specify tooling (Cypress or Playwright), data seeding, and assertions that onboarding events are idempotent (only once per user)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement with Cypress. Seed a new user in the test DB, stub the flag service to return Variant A or B, assert variant-specific onboarding steps render, navigate through steps, and flip the flag mid-s",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Playwright. Simulate offline conditions by blocking network requests during draft creation, then verify the note is persisted in localStorage or IndexedDB. Re-enable the network and mock the server PUT endpoint to r…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright with API fixtures to seed a single paid order for isolation; mock the invoice endpoint to produce a deterministic PDF; click Generate Invoice, verify a download starts, parse the PDF to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Create a Playwright or Cypress test that seeds a fresh user with the flag ON, navigates to /dashboard, asserts the onboarding panel and four steps are visible, completes step 1, refreshes to verify st",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical E2E testing skills for beginners: data seeding, mocking remote services (feature flags), validating UI variants, and ensuring idempotent events within a fintech onboarding flow.\n\n## Key Concepts\n\n- Feature flags and remote config\n- Test data seeding and isolation\n- API interception/mocking\n- Idempotent event verification\n- Variant-driven UI testing\n\n## Code Example\n\n```javascript\n// Cypress skeleton for onboarding with feature flag\ndescribe('Onboarding feature flag', () => {\n  const userId = 'test-user-001';\n  beforeEach(() => {\n    cy.request('POST', '/test/setup-user', { userId });\n  });\n\n  it('Variant A renders steps A1/A2', () => {\n    cy.intercept('/api/flags', { variant: 'A' }).as('flagA');\n    cy.visit('/onboarding');\n    cy.wait('@flagA');\n    cy.get('.step1').should('be.visible');\n  });\n\n  it('Idempotent event when flag toggled', () => {\n    cy.intercept('/api/flags', { variant: 'A' }).as('flagA');\n    cy.visit('/onboarding');\n    // complete some steps\n    cy.reload(); // simulate flag refresh\n    cy.intercept('/api/flags', { variant: 'B' }).as('flagB');\n    cy.wait('@flagB');\n    cy.get('@onlyOnboardingEvent').should('have.length', 1);\n  });\n});\n```\n\n## Follow-up Questions\n\n- How would you extend this to more than two variants and ensure coverage of each path?\n- How would you integrate this with CI to catch flakey flag-based flows?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-5",
        "questionId": "q-10245",
        "question": "Design an E2E test for a real-time, multi-tenant collaborative document editor: two users edit the same document in parallel sessions; verify tenant isolation, deterministic autosave, and eventual convergence with OT/CRDT conflict resolution, under simulated network latency and intermittent disconnects. Outline tooling (Playwright or Cypress), data seeding, and idempotent publish of final content?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed deterministic inputs to produce both approved and flagged transactions; mock RiskEngine with fixed outputs and inject latency to create backpressure. Verify quarantined events do not reach Ledger",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Cypress to seed two tenants (A: flag ON, B: OFF). Log in as each tenant, complete checkout, and assert presence of NewFlow for A and OldFlow for B. Simulate flag change mid-checkout and verify gra",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline an E2E flow that seeds a synthetic user across regions, triggers account deletion, and asserts redaction in all services (auth, profile, payments, messages, logs). Verify deletion webhooks fir",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use two parallel Playwright pages seeded with the same document under one tenant. Mock autosave to deterministic intervals, inject latency to trigger OT/CRDT conflicts, and verify final content conver",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-time collaboration and multi-tenant data isolation under flaky networks, a common production risk.\n\n## Key Concepts\n\n- End-to-end testing with concurrent sessions\n- Real-time collaboration conflict resolution (OT/CRDT)\n- Data isolation per tenant\n- Deterministic test scaffolding and latency injection\n- Idempotent save semantics\n\n## Code Example\n\n```javascript\n// Pseudo Playwright setup illustrating two pages\nconst [pA, pB] = await Promise.all([context.newPage(), context.newPage()]);\nawait pA.goto('/doc/123');\nawait pB.goto('/doc/123');\nawait pA.fill('#cell', 'Hello');\nawait pB.fill('#cell', 'World');\n```\n\n## Follow-up Questions\n\n- How would you adapt this test for a transient outage where the doc becomes temporarily unavailable?\n- What metrics would you collect to validate convergence guarantees?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-6",
        "questionId": "q-5200",
        "question": "Design an E2E test for a note-taking app's autosave/drafts: frontend React autosaves every 30s to /api/drafts. Simulate online -> edits, go offline, make edits, go online, verify queued changes upload in order without duplicates, and if two devices edit, ensure merge by latest timestamp. Use Playwright with API mock, seed user/note, and assert draft content correctness and idempotency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an E2E test for a note-taking app's autosave/drafts: frontend React autosaves every 30s to /api/drafts. Simulate online -> edits, go offline, make edits, go online, verify queued changes upload",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Playwright with API mocks to deterministically drive outcomes: simulate drag-and-drop of a small CSV, verify the preview renders, trigger Import, and assert progress reaches 100% with a success ba",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design two E2E tests in Playwright to cover both flag states. Seed the cart and user via API before each test. Toggle the feature flag using a POST request to /api/feature-flags. Mock the tax service to return 8.5% tax…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed a user with onboarding incomplete. Mock the feature flag config API to return false, load the onboarding page and verify restricted UI. Then flip the flag to true via API, resume and complete onb",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for autosave/drafts cover offline-first UX, idempotent writes, and cross-device conflicts, which are common pain points in real apps.\n\n## Key Concepts\n- Offline-first data flows and write queuing\n- Idempotent REST writes and ordering guarantees\n- Test data isolation via seeds and reset hooks\n\n## Code Example\n```javascript\n// Playwright skeleton (pseudo)\ntest('autosave offline queue and merge', async ({ page, request }) => {\n  await page.goto('/editor');\n  await page.fill('#note', 'Initial');\n  await page.waitForTimeout(31000); // wait for autosave\n  // simulate offline\n  await page.route('POST', '/api/drafts', route => route.abort());\n  await page.fill('#note', 'Offline edit');\n  await page.setOfflineMode(true);\n  await page.fill('#note', 'More offline edits');\n  await page.setOfflineMode(false);\n  // verify upload order and no duplicates\n  // reload or inspect draft store\n});\n```\n\n## Follow-up Questions\n- How would you adapt this for WebSocket-based drafting?\n- How would you test long offline periods and partial merges?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-7",
        "questionId": "q-23017",
        "question": "Design an end-to-end test for a serverless extensions marketplace where third-party extensions run in tenant-isolated sandboxes and respond to events from a multi-tenant chat app. Include data seeds, concurrency burst, per-tenant isolation with quotas, and auditing; verify no cross-tenant data leakage, at-least-once event delivery, and cleanup after uninstall?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "To design a comprehensive end-to-end test for real-time collaborative documents, I would implement the following approach: First, seed a test document with deterministic content across multiple regions. Then initialize…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a comprehensive end-to-end test suite for a serverless extensions marketplace that validates the following: Create 3 distinct tenants and seed per-tenant reference data (users, channels, settings). Deploy 4 sandb…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright to run two variants: (1) mock /api/flags?TutorialEnabled=true; signup; assert the onboarding carousel appears and first slide text; (2) mock /api/flags?TutorialEnabled=false; signup; as",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an E2E test for a real-time editor using CRDT (Y.js) with WebSocket sync. Launch 3 browser contexts, simulate concurrent edits to distinct regions, verify final document state converges identic",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for serverless, multi-tenant extension sandboxes require robust isolation guarantees, quota enforcement under burst loads, and reliable event delivery semantics. This question evaluates your ability to architect end-to-end test flows that encompass test data seeding, observability instrumentation, concurrency handling, and comprehensive failure mode validation across tenant boundaries.\n\n## Key Concepts\n\n- **Tenant Isolation**: Strict data separation at database, memory, and network levels\n- **Per-Tenant Quotas**: Resource limits enforced under burst conditions\n- **Event Delivery Semantics**: At-least-once guarantees with idempotent processing\n- **Sandbox Enforcement**: Extension runtime boundaries and permission scoping\n- **Audit Trails**: Complete visibility into extension lifecycle and data access\n- **Cleanup Verification**: Guaranteed resource teardown after uninstallation\n\n## Code Example\n\n```javascript\n// Test fixture: tenant event flow setup\nasync function seedTenantData(tenantId, db) {\n  await db.users.insertMany([\n    { id: `u1_${tenantId}`, tenantId, name: 'Admin' },\n    { id: `u2_${tenantId}`, tenantId, name: 'Member' }\n  ]);\n  await db.channels.insertMany([\n    { id: `ch1_${tenantId}`, tenantId, name: 'general' }\n  ]);\n}\n\nasync function deployExtension(tenantId, extensionId, permissions) {\n  const sandbox = await createIsolatedSandbox({ tenantId, permissions });\n  await sandbox.deploy(extensionId);\n  return sandbox;\n}\n\nasync function fireBurstEvents(eventBus, tenants, count) {\n  const events = [];\n  for (let i = 0; i < count; i++) {\n    const tenant = tenants[i % tenants.length];\n    events.push(eventBus.publish({\n      tenantId: tenant.id,\n      type: 'message.created',\n      payload: { id: `msg_${i}`, channelId: `ch1_${tenant.id}` }\n    }));\n  }\n  await Promise.all(events);\n}\n\n// Validation assertions\nasync function validateIsolation(db, tenants) {\n  for (const tenant of tenants) {\n    const otherData = await db.events.find({ tenantId: { $ne: tenant.id } }).toArray();\n    const tenantData = await db.events.find({ tenantId: tenant.id }).toArray();\n    \n    for (const event of tenantData) {\n      expect(event.payload.channelId).toContain(tenant.id);\n    }\n  }\n}\n\nasync function validateDelivery(eventBus, expectedCounts) {\n  for (const [tenantId, expected] of Object.entries(expectedCounts)) {\n    const processed = await eventBus.getProcessedCount(tenantId);\n    expect(processed).toBeGreaterThanOrEqual(expected);\n  }\n}\n\nasync function validateCleanup(sandbox, db) {\n  await sandbox.uninstall();\n  const webhooks = await db.webhooks.find({ sandboxId: sandbox.id }).toArray();\n  const auditLogs = await db.auditLogs.find({ resourceId: sandbox.id }).toArray();\n  \n  expect(webhooks).toHaveLength(0);\n  expect(auditLogs).toMatchObject([{ action: 'uninstall', status: 'success' }]);\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-8",
        "questionId": "q-24954",
        "question": "Design an end-to-end test for a multi-tenant SaaS with per-tenant data segregation in sharded PostgreSQL and a Redis cache. Validate data isolation under concurrent writes/reads across tenants, ensure cache invalidation on tenant data changes and proper propagation to reads, and verify audit logs show tenant-scoped activity. Include seeds, burst traffic, and observability checks; how would you implement?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a mock flag service to control region-specific rollout percentages (US: 50%, EU: 0%, APAC: 100%), route requests through region-scoped tenants, and validate that the UI accurately reflects each region's flag s…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed synthetic customers; drive an end-to-end flow through the portal to trigger a delete request; verify removal from primary tables, anonymization/pseudonymization in analytics marts, and purge job",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed tenants A and B with distinct data; in parallel, perform writes/reads for both tenants under burst load; verify queries return only per-tenant data and no leakage; ensure Redis keys are tenant-pr",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Drive the flow with Playwright or Cypress in a dedicated test env; stub bank OAuth endpoints and data fetch; seed test users and synthetic accounts; use an idempotency key on token exchanges; assert l",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests multi-tenant data isolation under burst scenarios, ensuring per-tenant data separation across DB sharding and caching layers. It also checks cache invalidation, cross-tenant performance isolation, and end-to-end observability via audit logs.\n\n## Key Concepts\n\n- Multi-tenant data isolation\n- Burst/load isolation\n- Cache invalidation and propagation\n- Audit log integrity with tenant IDs\n- Data seeding and fixtures\n\n## Code Example\n\n```javascript\n// Pseudo-test seed-and-assert\nconst seedTenant = (tenantId, data) => { /* create per-tenant rows */ }\nconst burstTraffic = async (tenants) => { /* parallel writes/reads per tenant */ }\nconst assertIsolation = () => { /* ensure per-tenant queries don't leak */ }\n```\n\n## Follow-up Questions\n\n- How would you validate shard rebalancing affects tenant data during migration?\n- How would you test GDPR DSAR deletion across tenants without cross-tenant leakage?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-9",
        "questionId": "q-21235",
        "question": "Design an end-to-end test for a fintech onboarding flow where a user signs up, passes KYC via an external service, and an eligibility event is published to a multi-region event bus. The UI must reflect live status across devices. Include test data isolation, deterministic external API mocks, idempotency checks on signup, eventual consistency verification, and regional outage handling. Use Cypress or Playwright and outline orchestration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a beginner-friendly E2E test for onboarding with permission prompts and role-based UI gates in a fintech web app. Use Cypress or Playwright, seed guest and registered users, mock external servi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test an end-to-end flow for a Salesforce-like storefront built with micro-frontends (Catalog, Cart, Checkout) controlled by feature flags. Run parallel across 3 regions with per-region data isolation.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Mock the external KYC API and the event bus to deterministic endpoints; isolate data per test tenant; verify idempotent signup with the same user_id; test eventual consistency across regional replicas",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Playwright with a dedicated test environment, mock real-time streams via a WebSocket server, seed deterministic test data, and stub network calls for payment and kitchen APIs. Verify end-to-end state transitions by…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for onboarding across regions and async services are common in fintech; this question probes end-to-end reliability, data isolation, and failure handling.\n\n## Key Concepts\n- End-to-end testing with external dependencies\n- Event-driven architecture and eventual consistency\n- Data isolation and idempotency\n- Regional failure scenarios and recovery\n- Test determinism via mocks\n\n## Code Example\n```javascript\n// Playwright skeleton: intercept KYC and mock responses; drive onboarding flow\nimport { test, expect } from '@playwright/test';\ntest('onboarding E2E with mocked KYC and event bus', async ({ page }) => {\n  await page.route('**/kyc/*', r => r.fulfill({ status: 200, body: JSON.stringify({ ok: true }) }));\n  await page.route('**/event-bus/*', r => r.fulfill({ status: 202 }));\n  await page.goto('https://fintech.example/onboard');\n  await page.fill('#email','user@example.com');\n  await page.click('#submit');\n  await expect(page.locator('#status')).toHaveText(/pending|kyc_passed|approved/);\n});\n```\n\n## Follow-up Questions\n- How would you scale this to thousands of concurrent onboarding flows?\n- How would you validate idempotency across multiple regions and tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-10",
        "questionId": "q-27141",
        "question": "Design an end-to-end test for a video streaming platform that supports ABR playback across multiple CDNs and regional proxies. Create scenarios with four network profiles (5, 10, 20 Mbps; high latency; jitter) and two manifests (HD and 4K). Validate manifest fetch, startup buffering, adaptive bitrate transitions, and cross-browser consistency (Chrome, Safari, Edge). Include seeds, tooling choices, and verification methods?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Playwright-driven test that simulates Chrome, Safari, and Edge with network throttling and jitter; seed 4 network profiles; mock 2 ABR manifests (HD/4K) across 2 regional CDNs. Verify manifest f",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design and implement a two-client Playwright test that opens the same document, performs concurrent edits, simulates disconnects, and verifies that all clients converge to the same document state with",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end test for a Ride-Share platform's dynamic pricing service in a multi-region deployment: simulate surge events, rider and driver applications (mobile/web), network jitter, and region-specific promotio…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an offline-first E2E test for a Progressive Web App that utilizes IndexedDB and a Service Worker queue to synchronize edits when connectivity is restored. Begin by seeding a clean test tenant, execute the test wi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ABR across networks and CDNs in real-world conditions, ensuring playback stability and cross-browser reliability at scale.\n\n## Key Concepts\n- ABR and manifest handling\n- Multi-CDN failover and proxies\n- Network emulation (bandwidth, latency, jitter)\n- Cross-browser determinism and data seeding\n- End-to-end metrics collection\n\n## Code Example\n```javascript\n// Playwright skeleton for ABR test\n// - Setup browser contexts per platform\n// - Apply network throttling\n// - Load page and assert initial bitrate and buffering events\n```\n\n## Follow-up Questions\n- How would you isolate test data and seeds for parallel runs?\n- How would you measure startup time vs buffering across browsers and CDNs?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-11",
        "questionId": "q-17907",
        "question": "Design an end-to-end test for a new-tenant onboarding flow in a multi-region SaaS app: a user signs up, selects a plan, and is provisioned across Auth, Billing, and CRM services. Include deterministic mocks, per-tenant data isolation, a feature-flag path (onboarding-v2), idempotent provisioning events, and validation of eventual consistency across regions with retry/backoff?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright to drive a multi-region onboarding flow across Auth, Billing, and CRM services. Create per-tenant IDs to ensure data isolation and stub all external service calls deterministically. Toggle the onboarding-…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Seed tenants A and B with isolated datasets. Install a widget from the remote registry for tenant A and verify it renders correctly in the dashboard. Drive per-widget WebSocket updates and confirm tenant A receives live…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright for end-to-end dashboard verification, integrated with a test harness that seeds Kafka topics with test events, stubs external data feeds with deterministic mocks, and preloads Redis with isolated test st…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a test that seeds tokenized PANs, executes authorization, capture, and settlement across two regions using Kafka events and a fraud service, then validates ledger reconciliation within SLA. Inc",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for distributed onboarding require cross-service orchestration, feature flags, and eventual consistency verification beyond typical single-service flows.\n\n## Key Concepts\n- End-to-end orchestration across Auth, Billing, and CRM services\n- Multi-region latency handling and retry mechanisms\n- Data isolation via per-tenant identifiers\n- Deterministic mocks for reliable, reproducible tests\n- Feature flags to validate multiple code paths in production-like conditions\n- Idempotent provisioning to safely handle duplicate events\n- Eventual consistency verification across regions\n\n## Code Example\n```javascript\n// Deterministic mock setup for Playwright routing\npage.route('https://auth.example.com/**', route => {\n  const tenantId = 'tenant-' + Date.now();\n  route.fulfill({\n    status: 200,\n    body: JSON.stringify({ tenantId, status: 'provisioned' })\n  });\n});\n\n// Feature flag toggle for onboarding-v2\nawait context.addInitScript(() => {\n  window.__FEATURE_FLAGS__ = { 'onboarding-v2': true };\n});\n\n// Retry with exponential backoff for eventual consistency\nasync function waitForRegionSync(tenantId, region, maxRetries = 5) {\n  for (let i = 0; i < maxRetries; i++) {\n    const response = await api.get(`/tenants/${tenantId}/status?region=${region}`);\n    if (response.status === 'synced') return true;\n    await sleep(Math.pow(2, i) * 1000);\n  }\n  throw new Error('Region sync timeout');\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-12",
        "questionId": "q-5091",
        "question": "Design an end-to-end test plan for a privacy-focused onboarding and data-deletion flow in a social platform where user data is partitioned by region and deletion requests must erase data across 50+ services within strict SLAs. Include consent capture, regional data isolation, data erasure propagation, audit trails, and rollback scenarios; specify tooling, data seeds, and verification approaches?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright to seed the catalog and cart, drive a single checkout path, and toggle per-widget flags via a feature-flag service. Load 120 widgets through module federation in isolated frames, simula",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed two tenants in a dedicated test DB; run two isolated browser contexts with per-tenant headers; perform identical dashboard queries in each tenant; attempt cross-tenant API access by swapping toke",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed per-region drivers and trips; mock traffic feed with outages; run 1k+ concurrent ride requests across regions; verify ETA, routing, and pricing converge within 2–5s per region; test degraded beha",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline an end-to-end privacy flow test: seed region-bound user data; simulate consent capture; trigger regional data deletion (GDPR-like erasure) and verify data is removed across 50+ services within",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests end-to-end privacy workflows, cross-service propagation, and regional data governance under compliant SLAs.\n\n## Key Concepts\n\n- Regional data partitioning\n- Erasure requests and eventual consistency\n- Audit trails and tamper evidence\n- Data seeding and rollback strategies\n- Mocking external dependencies with feature flags\n\n## Code Example\n\n```javascript\n// Skeleton test for regional deletion propagation\n```\n\n## Follow-up Questions\n\n- How would you validate audit log integrity under partial failures?\n- How would you handle services with different deletion latencies?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-13",
        "questionId": "q-19471",
        "question": "Design an end-to-end test plan for a multi-tenant real-time analytics dashboard (web app) that relies on WebSocket live updates from a shared event bus and enforces strict per-tenant data isolation in a shared data store. Include scenarios for token expiry/refresh, concurrent tenants (20+ dashboards), network instability, and verifying no cross-tenant data leakage. Specify seeds, tooling, and verification methods?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed a user with onboarding incomplete. Mock the feature flag config API to return false, load the onboarding page and verify restricted UI. Then flip the flag to true via API, resume and complete onb",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed two tenants with distinct data; run two parallel Playwright contexts wired to a mock WebSocket bus publishing tenant-specific events; simulate token expiry with a short-lived JWT and refresh; inj",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Seed a fresh user, enable onboarding_v2 via API, launch the app, verify a 3-step wizard renders in order with Next/Back/Finish, complete it and verify a profile flag is set; then turn onboarding_v2 of",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An E2E test for a multi-tenant SaaS dashboard with per-tenant data isolation and RBAC. Seed tenants A,B,C with isolated datasets; enable a feature flag on A; verify via API and UI that Admin can creat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssess the ability to design robust E2E tests for multi-tenant, real-time apps, covering authentication flows, data isolation, and fault tolerance, not just happy-path scripts.\n\n## Key Concepts\n\n- E2E testing with WebSocket streams\n- Multi-tenant data isolation\n- Token refresh and session management\n- Fault injection and network topology\n- Deterministic seeds and observability\n\n## Code Example\n\n```javascript\n// Skeleton Playwright test outline for multi-tenant real-time dashboard\nimport { test, expect } from '@playwright/test'\n\ntest('tenant isolation and real-time updates', async ({ page, context }) => {\n  // Seeds and setup would run outside the browser context\n  // Open two dashboards for tenants A and B\n  await page.goto('https://app.example.com/dashboard?tenant=A')\n  // Connect to mock WS and verify events\n  // Token refresh and reconnection handled\n  // Assertions on UI showing only tenant A data\n})\n```\n\n## Follow-up Questions\n\n- How would you scale this to 100 tenants while keeping determinism?\n- How would you detect flaky network simulations vs genuine failures?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-14",
        "questionId": "q-30318",
        "question": "Design a beginner-friendly E2E test for a 'team invitation' flow in a SaaS app: an existing user invites a new user by email, the recipient accepts the invite via a link, the system creates the new user under the inviter's organization, and an audit log records the invite status. Include tooling (Cypress or Playwright), data seeding, mock the email service, and assertions that exactly one user is created, the invite is marked accepted, and reusing the link does not create duplicates?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an E2E test for live pricing via WebSocket across 3 regions, including a regional flag to disable surge. Seed region pricing, trigger a surge, verify live price updates, then booking locks t",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed an existing org and inviter; mock email service to capture the invite link; simulate inviter sending the invite and recipient opening the link; complete signup; assert that exactly one user is cr",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright to test a SaaS data export flow: seed test data (a user and dataset), apply date and status filters via the UI, click the Export button, mock the export service to resolve quickly, and stub email delivery…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce a beginner-friendly E2E for a data export: user submits a date-range export, a background job is enqueued, a mock 10s processing delay, UI progress turns to complete, and a download link appea",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nInvites touch data integrity, seeding, and idempotency—core E2E concerns for onboarding flows in SaaS apps.\n\n## Key Concepts\n\n- Test data isolation and seeding for org + inviter\n- Mocking external services (email)\n- Idempotency and duplicate prevention\n- End-to-end verification of user creation and audit trails\n\n## Code Example\n\n```javascript\n// Cypress skeleton\ndescribe('Team invite flow', ()=> {\n  it('creates a single user on accepting invite', ()=> {\n    // seed org/inviter\n    // intercept email, capture invite link\n    // navigate invite URL, complete signup\n    // assert 1 user in DB; invite 'accepted'\n  })\n})\n```\n\n## Follow-up Questions\n\n- How would you test failures when invite link is expired?\n- How would you adapt for multiple concurrent invites to same org?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-15",
        "questionId": "q-16768",
        "question": "Design a beginner-friendly E2E test for a local-first note app that caches drafts with a service worker and syncs to a server. The test should cover creating a note offline (verify local cache), going online to sync via an idempotent PUT (verify server state), and a simple cross-tab conflict resolution when two tabs edit the same note. Specify tooling (Cypress or Playwright), offline simulation, and deterministic data seeds?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Provide a concrete E2E test plan that covers: onboarding a new tenant with per-tenant feature flags, provisioning a dedicated data mart in Region A, gating API endpoints behind the flag, and ensuring complete tenant iso…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright. Simulate offline conditions by blocking network requests during draft creation, then verify the note is persisted in localStorage or IndexedDB. Re-enable the network and mock the server PUT endpoint to r…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design an E2E test for a Zoom-like call with meeting creation, host/participants join via WebRTC, screen share, captions, and chat. Use Playwright to drive UI, a mock signaling server, and synthetic W",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed tenants and regions; simulate an offline rider initiating a ride request, establish a live driver WebSocket stream for real-time location updates, and configure surge pricing thresholds. Introduce a region partitio…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for offline-first applications with service workers and cross-tab interactions are common in modern web development. This question evaluates practical E2E thinking around offline caching, network replay, idempotent API design, and conflict resolution at a beginner level.\n\n## Key Concepts\n- Offline-first UX and service workers\n- Local storage and IndexedDB persistence\n- Idempotent server updates with PUT\n- Cross-tab concurrency and conflict handling\n- Deterministic data seeds for repeatable tests\n\n## Code Example\n```javascript\n// Playwright E2E test for offline-first note app\nimport { test, expect } from '@playwright/test';\n\ntest.describe('Offline-first note app', () => {\n  test('should create note offline, sync when online, and handle idempotency', async ({ page }) => {\n    // Set up deterministic seed for reproducible data\n    const noteId = 'test-note-001';\n    const noteContent = 'Test note content';\n\n    // Simulate offline by intercepting network requests\n    await page.route('**/api/**', (route) => route.abort('failed'));\n\n    // Create note while offline\n    await page.goto('/app');\n    await page.fill('[data-testid=\"note-editor\"]', noteContent);\n    await page.click('[data-testid=\"save-button\"]');\n\n    // Verify note is cached locally\n    const localNote = await page.evaluate(() => localStorage.getItem('draft'));\n    expect(localNote).toContain(noteContent);\n\n    // Restore network and mock PUT endpoint\n    await page.unroute('**/api/**');\n    await page.route('**/api/notes/**', (route) => {\n      route.fulfill({ status: 200, body: JSON.stringify({ revision: 'rev-123' }) });\n    });\n\n    // Trigger sync\n    await page.click('[data-testid=\"sync-button\"]');\n    await expect(page.locator('[data-testid=\"sync-status\"]')).toHaveText('Synced');\n\n    // Verify idempotent behavior - re-submit should not create duplicate\n    const initialRequestCount = 1;\n    await page.click('[data-testid=\"sync-button\"]');\n    // Assert no duplicate entries on server\n  });\n});\n```",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-16",
        "questionId": "q-4138",
        "question": "Design an end-to-end test for a real-time collaborative document editor (like Google Docs) integrated into a messaging app. The editor uses CRDTs over WebSockets to sync edits among N clients. Outline how you would implement an E2E test that isolates documents, simulates latency and network partitions, handles offline edits, and validates CRDT convergence after reconnect. Include tooling choices and steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use 3 isolated browser contexts connected to the same docId via WebSocket CRDT Sync. Simulate latency and disconnects with network throttling. Have A,B,C perform concurrent edits, then reconnect and v",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a test that seeds tokenized PANs, executes authorization, capture, and settlement across two regions using Kafka events and a fraud service, then validates ledger reconciliation within SLA. Inc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed per-tenant datasets with representative samples; drive the ML suggestion module with drift scenarios including concept drift, data drift, and model degradation; verify the complete end-to-end task lifecycle: create…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an advanced end-to-end test for a real-time order-status pipeline using a pub/sub backbone (Kafka-like) with a downstream fan-out service. Validate exactly-once delivery, out-of-order processing, regional outages…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for real-time collaboration require validating convergence under latency, partitions, and offline edits. This checks CRDT correctness, replay safety, and end-to-end data integrity across multiple clients.\n\n## Key Concepts\n\n- Real-time collaboration and CRDT state convergence\n- Network conditioning (latency, partitions, offline mode)\n- Test data isolation and deterministic replay\n- Multi-client orchestration in E2E tests\n\n## Code Example\n\n```javascript\n// Pseudo-code: setup 3 contexts, isolate doc, throttle networks, perform concurrent edits, validate convergence\n```\n\n## Follow-up Questions\n\n- How would you extend to 5+ clients with random network conditions and still ensure deterministic outcomes?\n- How would you verify operation idempotence and replay safety across restarts?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-17",
        "questionId": "q-23431",
        "question": "Design an end-to-end test plan for a Zoom-like video meeting platform that scales to 500 participants across 3 regions. Validate the join/start flows, end-to-end audio/video synchronization, screen sharing, live captions, and per-region chat. Under network jitter and packet loss, simulate participant churn and rejoin, and verify global view convergence within 5 seconds. Include tooling, seeds, and deterministic checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright to simulate a 6 MB file upload in 1 MB chunks; seed a per-test bucket; throttle network to drop after 2 chunks, then restore; verify the final file exists and its MD5 hash on the client",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an E2E test for a multi-tenant feature-flag platform with canary rollout, regional replication, and webhook delivery to external systems. Seed tenants, assign rollout percentages (0, 25, 50, 10",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright to create two isolated browser contexts (A and B) connected to the same shared document via WebSocket. **Setup:** 1. Seed a test document via API with known initial content 2. Launch both browser contexts…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Orchestrate 6-8 synthetic participants across 3 regions via Playwright with mock media streams; seed a 500-participant meeting in a test env; emulate network conditions using tc/netem; capture WebRTC",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\\n\\nTests at scale for real-time media pose unique challenges: synchronization, latency budgets, and state convergence across regions.\\n\\n## Key Concepts\\n\\n- Real-time media testing at scale (WebRTC stats, ICE, RTT)\\n- Network simulation (jitter, loss, partitions)\\n- Deterministic seeds and parallel execution\\n\\n## Code Example\\n\\n```javascript\\n// Pseudo test skeleton using Playwright\\n```\\n\\n## Follow-up Questions\\n\\n- How would you isolate tests to avoid cross-region flakiness?\\n- What metrics would you publish to SRE for this test plan?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-18",
        "questionId": "q-18570",
        "question": "Design a beginner-friendly E2E test for a data export flow on a cloud analytics dashboard: user selects a 7-day date range on /analytics, clicks Export CSV, backend creates a background export job, UI polls /exports/{id}/status, then provides a download link; seed 7 days of events, mock the export API to respond with a job id and success after a short delay, and verify the downloaded CSV contains the seeded rows in expected order; simulate a flaky network during export and verify retry works; finish within 30s?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright or Cypress to run a mobile-first E2E test at 375x812. Seed per-run test data for a new user and catalog via API, isolate data by unique userId, then navigate to home, search lamp, apply",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Trigger a purchase in the UI, then simulate a regional outage to force event replay. Validate eventual consistency by asserting invoice status transitions (pending→paid), ensure idempotent CreateInvoice operations, and…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright to seed data via API: POST /seed with 7 days of events; open /analytics, set date range, click Export CSV; intercept POST /exports to return {id}; poll GET /exports/{id}/status every 50",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan two Playwright browser contexts simulating different regions, seed a document, perform concurrent edits, and assert convergence to the same CRDT state. Simulate offline by disconnecting one clien",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical E2E skills for data-export flows with background jobs, polling, and flaky networks.\n\n## Key Concepts\n- End-to-end flow with background tasks\n- API seeding and response mocking\n- Polling and timing resilience\n- File download and content validation\n\n## Code Example\n```javascript\n// Example Playwright snippet (not a full test)\n```\n\n## Follow-up Questions\n- How would you handle large exports and timeouts?\n- How would you verify idempotency if export is triggered twice?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-19",
        "questionId": "q-16990",
        "question": "Design an E2E test plan for a multi-tenant AI model hosting platform that supports real-time WebSocket streaming inference and REST batch scoring. Include per-tenant quotas, model versioning, and guardrails. Validate tenant isolation, hot-swapping models under load, burst traffic with cold starts, drift alarms, end-to-end telemetry correlation, auth, and failure modes (downtime, partition). Specify seeds, tooling, and verification methods?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Create a Playwright or Cypress test that seeds a fresh user with the flag ON, navigates to /dashboard, asserts the onboarding panel and four steps are visible, completes step 1, refreshes to verify st",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an end-to-end test for a Ride-Share platform's dynamic pricing service in a multi-region deployment: simulate surge events, rider and driver applications (mobile/web), network jitter, and region-specific promotio…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Provide a test harness that seeds per-tenant workloads, uses k6 for REST, a WebSocket client for streaming, and a multi-tenant simulator to enforce quotas. Verify strict tenant isolation, test hot-swa",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement a cross-device test using a shared central session store. Start playback on Smart TV at 120s, switch to mobile with the same session token, and simulate network handoffs (WiFi to cellular) a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes end-to-end testing across real-time and batch paths, multi-tenancy, and model lifecycle.\n\n## Key Concepts\n\n- Multi-tenant isolation and quotas\n- Real-time streaming + REST paths\n- Model versioning and hot-swapping\n- Drift detection and guardrails\n- Telemetry, tracing, and correlation\n- Failover, partition tolerance, and recovery\n\n## Code Example\n\n```javascript\n// Seed example (pseudo)\nconst seeds = {\n  tenantA: { quota: 1000, model: 'v2.0' },\n  tenantB: { quota: 500, model: 'v1.5' }\n};\n```\n\n## Follow-up Questions\n\n- How would you verify fairness under quota limits during burst?\n- How would you simulate deterministic drift in a local test environment?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-20",
        "questionId": "q-3104",
        "question": "Design a beginner-friendly E2E test for a passwordless login using a magic link in a fintech app. Include: simulating email delivery and token extraction, token validation and expiry, rate-limiting on requests, test data isolation across parallel runs, and ensuring idempotent redirects/navigation after login?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement using Playwright or Cypress with a test mailbox API to capture magic links and extract tokens. Navigate to the magic link URL and verify redirection to the dashboard. Mock email delivery via a test email servi…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use two Playwright browser contexts to simulate two users editing the same document. Seed a base document with known content, enable offline mode to queue local edits, then reconnect to trigger the CRDT merge. Validate…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright with route interception to seed the chat state, post a new message, delete it, and confirm the UI hides the item while an Undo toast appears. Trigger Undo to restore the original messag",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed 3 tenants with isolated topics, stream deterministic events via Kafka into Spark then into the warehouse, and validate dashboards per tenant only show their data. Include backfill replay, tenant-",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests passwordless authentication flows commonly used in fintech applications. It evaluates handling of email delivery, token lifecycle management, expiry scenarios, rate limiting, and parallel test data isolation. It also verifies idempotent navigation behavior after successful login.\n\n## Key Concepts\n- Passwordless authentication and token lifecycle management\n- Email interception techniques and token extraction in test environments\n- System time manipulation for testing token expiration scenarios\n- Rate limiting implementation and parallel test data isolation strategies\n- Idempotent navigation patterns and redirect consistency validation\n\n## Code Example\n```javascript\n// Example using Playwright\nconst link = await getMagicLinkFromInbox(userEmail);\nawait page.goto(link);\nawait expect(page).toHaveURL(/dashboard/);\n```",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-21",
        "questionId": "q-24674",
        "question": "Design a beginner-friendly E2E test for a data-import workflow in a cloud storage web app: a user uploads a CSV with 500 rows, backend enqueues a processing job, and the UI shows live progress with invalid rows highlighted and a downloadable summary. Specify tooling (Cypress or Playwright), seed data, mock the processing service, and verify the final report matches expected counts and that re-upload is idempotent?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright for this E2E test. First, upload a fixture CSV containing 500 rows. Then intercept the POST request to /import and mock the job queue with a deterministic response to simulate asynchronous processing. See…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Playwright with API mocks to deterministically drive outcomes: simulate drag-and-drop of a small CSV, verify the preview renders, trigger Import, and assert progress reaches 100% with a success ba",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright to drive three browser contexts; mock data channels with a WebRTC shim; seed board state; verify near real-time sync (<200ms) via observed DOM events; simulate disconnects and online re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use two isolated browser contexts (A and B) in Playwright. Seed a document, perform concurrent edits from both sessions simultaneously, and verify the final merged content reflects proper CRDT resolution. Then simulate…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for data-import flows are common in cloud applications. This question evaluates practical E2E thinking at a beginner level, covering end-to-end flow, UI feedback, and data integrity.\n\n## Key Concepts\n\n- End-to-end flow of uploading data and handling background processing\n- Mocking backend services (job queue) for deterministic, reliable tests\n- Data seeding to ensure predictable idempotent behavior\n- Verifying UI progress indicators and downloadable summary reports\n\n## Code Example\n\n```javascript\n// Example Playwright snippet illustrating route mocking and upload\nconst { test, expect } = require('@playwright/test');\n\ntest('data import workflow with progress and idempotency', async ({ page }) => {\n  // Seed database with existing records for idempotency testing\n  await seedTestData();\n\n  // Mock the job queue response\n  await page.route('**/api/import', async (route) => {\n    await route.fulfill({\n      status: 200,\n      body: JSON.stringify({ jobId: 'test-job-123', status: 'processing' })\n    });\n  });\n\n  // Upload CSV fixture\n  await page.setInputFiles('input[type=\"file\"]', 'fixtures/test-data-500-rows.csv');\n\n  // Verify live progress updates\n  await expect(page.locator('.progress-bar')).toBeVisible();\n  await expect(page.locator('.progress-text')).toContainText('100%');\n\n  // Verify invalid rows are highlighted\n  await expect(page.locator('.invalid-row')).toHaveCount(5);\n\n  // Download and verify summary report\n  const [download] = await Promise.all([\n    page.waitForEvent('download'),\n    page.click('.download-summary')\n  ]);\n\n  const report = await download.text();\n  expect(report).toContain('Total: 500');\n  expect(report).toContain('Valid: 495');\n  expect(report).toContain('Invalid: 5');\n\n  // Test idempotency - re-upload same file\n  await page.setInputFiles('input[type=\"file\"]', 'fixtures/test-data-500-rows.csv');\n  await page.waitForSelector('.upload-complete');\n\n  // Verify no duplicates were created\n  const recordCount = await getRecordCount();\n  expect(recordCount).toBe(500);\n});\n```",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-22",
        "questionId": "q-22162",
        "question": "Design an end‑to‑end test for an offline‑first, real‑time collaborative notes app. A user creates a note online, then edits offline; a second user edits the same note while the first is offline; when connectivity returns, changes sync. Validate deterministic conflict resolution (CRDT or last‑writer‑wins), per‑note sharing permissions, and UI updates (content, history, presence). Outline tooling, data isolation, network simulation, and timing expectations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a test harness with three simulated clients backed by a CRDT doc (e.g., Y.js). Steps: create a unique doc id, connect all clients, perform interleaved edits with offline periods, inject latency an",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use two Playwright browser contexts to simulate two users simultaneously; seed a shared note in both contexts; put one context offline using network throttling, then make edits; have the other user edit the same note wh…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright to drive three browser contexts; mock data channels with a WebRTC shim; seed board state; verify near real-time sync (<200ms) via observed DOM events; simulate disconnects and online re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "getByRole() locates elements by their ARIA role and accessible name, making it more resilient to UI text changes and better for accessibility testing. getByText() matches visible text content directly, which can be brit…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests your ability to design end-to-end tests for offline-first collaborative applications—a common pattern in modern productivity tools. It evaluates understanding of multi-client synchronization, conflict resolution strategies, and proper test isolation.\n\n## Key Concepts\n- **Offline-first syncing and eventual consistency**: Understanding how data propagates when clients reconnect\n- **Conflict resolution strategies**: CRDT (Conflict-free Replicated Data Types) ensures deterministic merging; last-writer-wins uses timestamps\n- **Multi-client test isolation**: Using separate browser contexts to simulate concurrent users\n- **Network simulation**: Leveraging tools like Playwright's network throttling to emulate offline/online transitions\n- **Per-note permissions and presence**: Validating access control and real-time user indicators\n\n## Code Example\n```javascript\n// Playwright setup with two isolated contexts\nconst { chromium } = require('playwright');\n\nconst browser = await chromium.launch();\nconst context1 = await browser.newContext();\nconst context2 = await browser.newContext();\n\nconst user1 = await context1.newPage();\nconst user2 = await context2.newPage();\n\n// Seed shared note\nconst noteId = await createSharedNote(user1, 'Test Note');\nawait user2.goto(`/notes/${noteId}`);\n\n// Take user1 offline\nawait context1.setOffline(true);\n\n// User1 edits offline\nawait user1.fill('.editor', 'User1 offline edit');\n\n// User2 edits online\nawait user2.fill('.editor', 'User2 online edit');\n\n// Restore connectivity\nawait context1.setOffline(false);\n\n// Verify sync and conflict resolution\nconst finalContent = await user1.textContent('.editor');\nconsole.log('Final content:', finalContent);\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-23",
        "questionId": "q-16369",
        "question": "Design an E2E test for a WebRTC-based video conferencing platform supporting 1000+ concurrent rooms with 50 participants per room. Include synthetic clients, mixed network conditions, room join/leave, screen share, and recording. Specify test harness, metrics (latency, jitter, packet loss, AV sync), data seeds, and verification that recording and live streams remain in sync across servers under burst load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use either Playwright or Cypress. Seed product, promo, and user fixtures. Mock the payment gateway with a sandbox API returning success, and drive a flaky network via throttling or offline/retry. Use",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright or Cypress. Begin by seeding test data: products, locales, promos, and a mock payment gateway. Steps: 1) Add an item to cart, apply a promo code, then verify the updated total, tax, and currency display c…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed a sheet with 3 clients editing overlapping cells (with dependent formulas) across web and mobile. Use CRDT‑style syncing over WebSocket. Inject partitions, force reconnect, and verify all clients",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would implement an automated test harness that instantiates N concurrent rooms with 50 synthetic participants per room, under varied network profiles including latency, jitter, and packet loss. The harness captures We…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates understanding of end-to-end testing for real-time media systems at scale, with emphasis on synchronization verification, fault injection, and observability under production-like conditions.\n\n## Key Concepts\n\n- Large-scale WebRTC testing with synthetic clients\n- Network emulation including latency, jitter, and packet loss profiles\n- Audio-video synchronization verification\n- Recording integrity and consistency across media servers\n- Burst load testing and stress testing methodologies\n- Signaling and media path coordination\n\n## Code Example\n\n```javascript\n// Pseudocode for WebRTC E2E test harness\nclass WebRTCE2EHarness {\n  constructor(config) {\n    this.rooms = [];\n    this.networkProfiles = config.networkProfiles;\n    this.statsInterval = config.statsIntervalMs || 1000;\n  }\n\n  async createRoom(participants, networkProfile) {\n    const room = new Room(participants, networkProfile);\n    await room.initialize();\n    this.rooms.push(room);\n    return room;\n  }\n\n  async runBurstLoadTest(options) {\n    const { roomCount, participantsPerRoom, staggerDelay } = options;\n    for (let i = 0; i < roomCount; i++) {\n      await this.createRoom(participantsPerRoom, this.getRandomProfile());\n      await this.sleep(staggerDelay);\n    }\n  }\n\n  collectMetrics() {\n    return this.rooms.flatMap(room => room.getRTCStats());\n  }\n\n  verifyRecordingSync(recording1, recording2) {\n    const drift = Math.abs(recording1.timestamp - recording2.timestamp);\n    return drift < 50; // 50ms tolerance\n  }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-24",
        "questionId": "q-16736",
        "question": "Design a thorough E2E test for a live food-delivery flow: customer places an order, restaurant accepts, driver is assigned, and driver location/ETA updates stream, payment is processed, and delivery completes. Include how you would simulate real-time driver status, ensure test data isolation, verify idempotent events and eventual consistency across services, and handle a driver disconnect scenario?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan a layered test: seeds per tenant, a canary flag toggling schema changes, deterministic event generation at 1M/min, and replayable duplicates. Validate Spark Structured Streaming reads from Kafka,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright with a dedicated test environment, mock real-time streams via a WebSocket server, seed deterministic test data, and stub network calls for payment and kitchen APIs. Verify end-to-end state transitions by…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright to emulate offline mode, add items to the cart, then re-enable network to trigger background sync; assert a single order is created with a stable id and no duplicates on retry. Seed cat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright with a dedicated test tenant and seed Vendors A and B. Intercept the payment gateway to fail once, then succeed on retry using an idempotency key. Validate inventory is reserved for bot",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design realistic cross-service end-to-end tests with real-time updates, failure modes, and proper data isolation.\n\n## Key Concepts\n- Real-time event streams (WebSocket/SSE) and state machine validation\n- Test isolation with per-tenant data and fresh environments\n- Deterministic inputs via seed data and mocked external services\n- Idempotency and eventual consistency verification across services\n- Failure injection testing (disconnects, latency, partial outages)\n\n## Code Example\n```javascript\n// Pseudo-test sketch using Playwright\nconst { test, expect } = require('@playwright/test');\n\ntest.describe('E2E Food Delivery Flow', () => {\n  let wsServer, testData;\n\n  test.beforeAll(async () => {\n    wsServer = await startMockWebSocketServer();\n    testData = await seedDeterministicTestData();\n  });\n\n  test('complete delivery flow with driver tracking', async ({ page }) => {\n    const driverUpdates = [];\n    \n    // Simulate real-time driver location stream\n    wsServer.on('driver-location', (data) => driverUpdates.push(data));\n    \n    // 1. Place order\n    await page.goto('/order');\n    await page.fill('#address', testData.address);\n    await page.click('#place-order');\n    const orderId = await expectOrderConfirmed(page);\n    \n    // 2. Restaurant accepts\n    await wsServer.emit('order-status', { orderId, status: 'accepted' });\n    await expectOrderStatus(page, 'accepted');\n    \n    // 3. Driver assigned\n    await wsServer.emit('driver-assigned', { orderId, driver: testData.driver });\n    await expectDriverAssigned(page, testData.driver);\n    \n    // 4. Verify location/ETA streaming\n    await wsServer.emit('driver-location', { lat: 40.7128, lng: -74.0060, eta: 10 });\n    await expectETA(page, '10 min');\n    \n    // 5. Payment processed\n    await stubPaymentAPI({ orderId, status: 'success' });\n    await expectPaymentStatus(page, 'paid');\n    \n    // 6. Delivery completes\n    await wsServer.emit('order-status', { orderId, status: 'delivered' });\n    await expectOrderStatus(page, 'delivered');\n    \n    // Verify idempotent event handling\n    await wsServer.emit('order-status', { orderId, status: 'delivered' });\n    expect(driverUpdates).toHaveLength(1); // No duplicate processing\n    \n    // Verify eventual consistency\n    await expectExternalServiceSynced(orderId);\n  });\n\n  test('handles driver disconnect gracefully', async ({ page }) => {\n    const orderId = await placeOrder(page);\n    await wsServer.simulateDisconnect();\n    \n    await expectReconnectionLogic(page);\n    await expectDriverReassigned(page);\n  });\n});\n```",
        "difficulty": "intermediate"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-19T12:37:24.157Z",
    "version": 1
  }
]