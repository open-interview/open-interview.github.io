[
  {
    "id": "test-e2e-testing",
    "channelId": "e2e-testing",
    "channelName": "E2E Testing",
    "title": "E2E Testing Knowledge Test",
    "description": "Test your understanding of E2E Testing with 25 questions.",
    "questions": [
      {
        "id": "e2e-testing-q-0",
        "questionId": "q-17375",
        "question": "Design a beginner-friendly E2E test for a mobile checkout flow in a Square-like web app. Include: entering card data, applying a promo code, submitting payment over a flaky network, and verifying that an order is created exactly once with a confirmed payment. Specify tooling (Cypress or Playwright), how to mock the payment gateway, how to seed data, and how to assert idempotent results within 30 seconds?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger DSAR for a test user; confirm removal from primary DBs, propagate deletion events to the event store, invalidate caches, and scrub materialized views after TTL; verify soft-delete tombstones a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed 3 tenants and emit trades with varied latency to Kafka; simulate regional partition, then resume and verify deduplicated, consistent state across dashboards within SLA. Validate per-tenant isolat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use either Playwright or Cypress. Seed product, promo, and user fixtures. Mock the payment gateway with a sandbox API returning success, and drive a flaky network via throttling or offline/retry. Use",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Playwright to sign in, open a title, and start playback; throttle network to 600kbps then 4Mbps to trigger ABR changes; verify bitrate transitions via a client telemetry hook window.__playerTeleme",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical E2E thinking for flaky networks, payments, and data isolation at a beginner level. Tests must cover real user actions with resilience against transient failures.\n\n## Key Concepts\n\n- End-to-end testing with flaky networks\n- Mocking external payment gateways\n- Data seeding and test isolation\n- Idempotency and retry/backoff patterns\n\n## Code Example\n\n```javascript\n// Example skeleton (pseudo)\n```\n\n## Follow-up Questions\n\n- How would you validate behavior with an invalid promo code?\n- How would you extend the test to verify multiple concurrent checkout attempts?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-1",
        "questionId": "q-9804",
        "question": "Design an end-to-end test for a real-time collaborative document editor embedded in a SaaS app. Use two browser sessions to simulate concurrent edits on the same document, verify live updates under injected latency, CRDT/OT reconciliation, and persistence after save; cover offline/reconnect paths, data isolation, and deterministic ordering; specify tooling (Playwright or Cypress) and how you orchestrate test data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use two Playwright browser contexts to simulate different regions, seed the test channel with isolated data, and implement a messageId to deduplicate. Connect to a test WebSocket server, throttle late",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Two Playwright contexts simulate concurrent edits on the same document. Inject latency to test real-time delta propagation, then validate CRDT reconciliation yields final content that includes both users' changes with p…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright with isolated test contexts per run. Seed a catalog of 150 deterministic products and mock the feature flag service to enable ranking_v2 for 20% of sessions. Validate that, when enabled",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: Use Playwright for deterministic UI flows; seed a dataset in MongoDB; mock the processing service with a local API stub; upload the same CSV twice to verify only one job is enqueued; simulat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes proficiency with real-time systems in E2E tests, including multi-user concurrency, latency handling, offline scenarios, and data isolation. It reveals how you design deterministic, reproducible tests for complex client-server interactions.\n\n## Key Concepts\n- Real-time collaboration testing\n- CRDT/OT reconciliation validation\n- Multiple browser contexts orchestration\n- Latency injection and offline/reconnect paths\n- Data isolation and deterministic ordering\n- Test data management and cleanup\n\n## Code Example\n```javascript\n// Playwright scaffold (pseudo)\nconst docId = `test_${Date.now()}`\nconst context1 = await browser.newContext()\nconst page1 = await context1.newPage()\nconst context2 = await browser.newContext()\nconst page2 = await context2.newPage()\n\n// Setup: both users open same document\nawait page1.goto(`/editor/${docId}`)\nawait page2.goto(`/editor/${docId}`)\n\n// Concurrent edits with latency injection\nawait page1.route('**/socket.io/**', route => {\n  setTimeout(() => route.continue(), 100) // Inject 100ms latency\n})\n\nawait page1.type('[contenteditable]', 'User 1 text')\nawait page2.type('[contenteditable]', 'User 2 text')\n\n// Verify real-time updates and CRDT reconciliation\nconst finalContent1 = await page1.textContent('[contenteditable]')\nconst finalContent2 = await page2.textContent('[contenteditable]')\nexpect(finalContent1).toBe(finalContent2) // Deterministic final state\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-2",
        "questionId": "q-7936",
        "question": "Design an end-to-end test for a real-time collaborative document editor (CRDT-based) with offline support and multi-device sync. Include how you simulate two devices with network latency, offline periods, and reconnection; verify eventual consistency of edits, conflict resolution, autosave, and version history; test export/import and a graceful recovery after a client crash. Please specify tooling choices, data seeding, and cleanup steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Cypress with cy.clock to advance time, fill first/last name, trigger autosave via a timer, then verify localStorage has the expected JSON under a key like 'profile_wizard'. Reload and confirm fiel",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test a real-time collaborative editor with 3 browser contexts editing one document via WebSocket. Emit concurrent edits, verify CRDT/OT conflict resolution, autosave cadence to the durable store, and",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Two scenarios: flag off and on. Seed a test user with role analyst and enable the feature flag for that user. Mock the export service to return a small CSV. Verify: button hidden when off; a single CS",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use two Playwright browser contexts to simulate collaborative editing of the same document with offline-first support. Implement network conditioning to introduce latency and simulate offline periods, then restore conne…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses practical end-to-end testing skills for complex real-time collaborative applications, specifically focusing on offline-first behavior, CRDT convergence properties, multi-device synchronization, and system resilience during network partitions.\n\n## Key Concepts\n\n- CRDT convergence under network latency and partition conditions\n- Offline-first user experience and autosave timing strategies\n- Multi-browser orchestration, network conditioning, and state reconciliation\n- Data seeding methodologies, deterministic merge validation, and cleanup procedures\n\n## Code Example\n\n```javascript\n// Pseudo-test: two contexts editing, simulate offline, then reconnect\n```\n\n## Follow-up Questions\n\n- How would you detect and resolve non-deterministic merge behaviors?\n- What metrics would you monitor to validate system performance during stress testing?\n- How would you automate the verification of version history accuracy across concurrent edits?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-3",
        "questionId": "q-2526",
        "question": "Design an advanced E2E test for a real-time order-status pipeline built on a pub/sub backbone (Kafka-like) with a downstream fan-out service. Validate exactly-once delivery, out-of-order processing, regional outages, and per-tenant data isolation; include data generation, replay safety, and latency/throughput metrics under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an advanced end-to-end test for a real-time order-status pipeline using a pub/sub backbone (Kafka-like) with a downstream fan-out service. Validate exactly-once delivery, out-of-order processing, regional outages…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use 3 region-local test runners simulating 20 users per region across 5k channels. Seed per-region users and channels, then drive message sends, edits, deletions, and reactions while toggling a region",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an E2E test using Playwright. Isolate tenants via a dedicated header, stub REST API with fixed responses, and feed a stable CSV fixture. Create a nightly pipeline through the UI or API, verify deterministic outp…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright to run two isolated browser contexts. Seed a list with 3 items. Put both offline, perform edits A and B (rename an item, add an item) then go back online; trigger sync. Validate the mer",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of streaming end-to-end guarantees, including idempotency, ordering, fault injection, and multi-tenant isolation across regions.\n\n## Key Concepts\n- Exactly-once semantics in pub/sub and fan-out architectures\n- Event ordering and out-of-order handling mechanisms\n- Fault injection for regional outages and replay safety validation\n- Test data isolation per tenant and end-to-end tracing\n\n## Code Example\n```javascript\n// Pseudo-test harness outline (Node.js)\nasync function runE2ETest({ tenants, events, durationMs }) {\n  // Generate per-tenant events in order",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-4",
        "questionId": "q-3308",
        "question": "Design a beginner-level end-to-end test for a Lyft-like ride-booking web flow (pickup → drop-off → ride type → confirm) using Playwright or Cypress. Include map auto-complete for addresses, ETA display, and handling the browser's GPS permission prompt; simulate a slow network and a short GPS outage. Describe test data isolation per run, how to seed a clean user, and ensure no session leakage between tests?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright to drive two browser contexts within the same tenant but different users. Seed a known document, simulate offline on one context, perform edits in both, reconnect, and verify final cont",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed a fresh user, clear storage, and isolate data per run. Mock geolocation to a fixed point, throttle network to 3G, and stub maps autocomplete responses. Verify pickup/dropoff fields populate, ride",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Seed a user and cart in the test DB, stub the feature flag service to return true then false, and navigate to checkout. Verify the UI shows Fast Checkout when the flag is ON and Standard Checkout when",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an E2E test for a checkout with a dynamic promo service. The user adds items, applies a promo, then checks out. The code may be revoked mid-flow and must apply only once. Validate: promo app",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests beginners learn to control external dependencies (maps, geolocation) and test isolation. This prompts demonstration of end-to-end coverage for critical user journeys while addressing flakiness through retries and data cleanup.\n\n## Key Concepts\n\n- Test data isolation per run\n- Geolocation and permission prompts\n- Network throttling and flakiness handling\n- Stubbing external APIs (maps)\n- Session cleanup to prevent leakage\n\n## Code Example\n\n```javascript\n// Playwright example: mock geolocation and slow network\nconst context = await browser.newContext({\n  geolocation: { latitude: 37.7749, longitude: -122.4194 }\n});\nconst page = await context.newPage();\nawait context.overridePermissions('https://ride.example.com', ['geolocation']);\nawait page.route('**/maps/api/**', route => route.fulfill({ status: 200, body: JSON.stringify({ predictions: [] }) }));\n```\n\n## Follow-up Questions\n\n- How would you adapt this test to run in CI with parallel isolation?\n- How would you diagnose a flaky GPS prompt in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-5",
        "questionId": "q-8958",
        "question": "Design a beginner-friendly E2E test for a CMS publish workflow: author drafts an article, autosaves every 30s, submits for review; a separate reviewer approves in another session; after publish, verify the article appears in the live feed within 60s. Include tooling choices (Cypress or Playwright), test data isolation, mocking of autosave for determinism, and checks for idempotent publish events and eventual consistency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Coordinate tests with three mock users in separate contexts; simulate offline edits via network throttling; verify live cursors and edits reflect in real time; test conflict resolution by merging conc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a E2E test for a CMS publish flow: author drafts, autosaves every 30s, submits for review; a separate reviewer approves in another session; on publish, confirm the article appears in the live",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Seed tenants t1 and t2, simulate mobile and web event streams, and verify strict per-tenant isolation across raw lake storage and transformed views. Confirm redacted PII in dashboards and caches, validate retention purg…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright to drive a multi-region onboarding flow across Auth, Billing, and CRM services. Create per-tenant IDs to ensure data isolation and stub all external service calls deterministically. Toggle the onboarding-…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests a realistic CMS multi-role workflow with autosave, highlighting practical E2E concerns for beginners: data isolation, determinism via mocks, and eventual consistency in live updates.\n\n## Key Concepts\n- End-to-end journeys with multiple user roles\n- Test data isolation for parallel runs\n- Autosave timing and deterministic testing via mocks\n- Idempotent publish operations and eventual consistency\n\n## Code Example\n```javascript\n// Example skeleton (Cypress)\ndescribe('CMS Publish Flow', () => {\n  it('draft autosaves and publishes with review', () => {\n    // login as author\n    // create draft, wait for autosave\n    // submit for review\n    // login as reviewer in new session, approve\n    // verify live feed shows article within 60s\n  });\n});\n```\n\n## Follow-up Questions\n- How would you simulate network latency affecting autosave?\n- How would you verify publish idempotency across retries?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-6",
        "questionId": "q-22507",
        "question": "Design an end-to-end test plan for a multi-tenant SaaS platform with 60 microservices that proves strict tenant isolation during peak concurrent onboarding and live usage. Include per-tenant feature flags, cross-tenant leakage checks, cache/CDN boundaries, data purge compliance, and accurate audit trails. Specify seeds, mocks, and verification steps, and name the tooling and metrics you would use?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive end-to-end test plan leveraging a dedicated test harness that seeds 60 tenant contexts and executes concurrent onboarding workflows alongside production-simulated live usage actions. Include cross…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Seed two tenants in isolated data stores; enable a canary feature flag gating the ingestion path. Ingest identical payloads via API for both tenants, verify streaming writes land only in each tenant’s",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed a sheet with 3 clients editing overlapping cells (with dependent formulas) across web and mobile. Use CRDT‑style syncing over WebSocket. Inject partitions, force reconnect, and verify all clients",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a comprehensive end-to-end test for an event-sourced payment flow that validates data consistency across distributed components while simulating real-world failure scenarios. The test orchestrates a complete paym…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses the ability to design scalable, isolation-driven E2E tests for multi-tenant SaaS platforms at scale, covering onboarding flows, live usage scenarios, and purge/audit compliance.\n\n## Key Concepts\n\n- Tenant isolation across services, data stores, and network boundaries\n- Per-tenant feature flags and gradual rollout semantics\n- Deterministic seeds, mocks, and observability mechanisms\n- Data purge compliance, audit trail integrity, and cross-region considerations\n- Cache and CDN boundary validation\n\n## Code Example\n\n```javascript\n// Pseudo-harness seed function\nasync function seedTenantContext(tenantId, config) {\n  const context = await testHarness.createContext({\n    tenantId,\n    featureFlags: config.flags,\n    seedData: generateDeterministicSeed(tenantId)\n  });\n  return context;\n}\n\n// Cross-tenant leakage check\nasync function verifyTenantIsolation(tenantId, otherTenantId) {\n  const tenantData = await db.query(`SELECT * FROM tenant_${tenantId}...`);\n  const otherData = await db.query(`SELECT * FROM tenant_${otherTenantId}...`);\n  expect(tenantData).not.toContain(otherData);\n}\n```\n\n## Tooling & Metrics\n\n| Category | Tool | Purpose |\n|----------|------|---------|\n| E2E UI Testing | Playwright | Browser-based tenant workflows |\n| API Validation | REST/GraphQL Test Suites | Service-level isolation |\n| Load Testing | k6 | Concurrent onboarding stress tests |\n| Data Seeding | Custom Harness | Deterministic tenant data |\n| Observability | Datadog/Prometheus | Isolation violation metrics |\n| Audit Logging | ELK Stack | Trail integrity verification |\n\n**Key Metrics:**\n- Cross-tenant data access attempts (should be 0)\n- Cache hit/miss ratios per tenant\n- Audit log completeness\n- Data purge completion times",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-7",
        "questionId": "q-10878",
        "question": "Design an end-to-end test for a feature-flagged checkout flow where a new payment UI is progressively rolled out behind a flag that can flip mid-session; validate correct routing to old vs new UI across 3 regions, ensure data isolation, simulate propagation delay of the flag, mock the feature-flag service for determinism, and verify idempotent checkout events and eventual consistency between regional order services; specify tooling (Cypress or Playwright) and test data strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright with two isolated browser contexts editing the same document. Stub autosave to a fixed 2-second interval, mock the server clock, and simulate WebSocket presence events. Validate that live edits propagate…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright or Cypress. Begin by seeding test data: products, locales, promos, and a mock payment gateway. Steps: 1) Add an item to cart, apply a promo code, then verify the updated total, tax, and currency display c…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design two parallel tests for old and new checkout behind a feature flag, running across three regions with a mocked, deterministic flag service and a simulated propagation delay. Validate correct UI",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Playwright to automate a mobile viewport; mock geolocation and throttle network; stub /estimate and /driver endpoints with deterministic responses; simulate intermittent connectivity by toggling f",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTesting feature flag routing under real user journeys, including mid-session flips and cross-region reconciliation, reveals flakiness and data drift.\n\n## Key Concepts\n- Feature flag determinism\n- Mid-session flip handling\n- Cross-region eventual consistency\n- Idempotent event design\n\n## Code Example\n```javascript\nimport { test, expect } from '@playwright/test';\n\n// simple flag stub\ntest('checkout respects flag', async ({ page }) => {\n  await page.route('https://flags.example.com/**', route => route.fulfill({ status: 200, body: JSON.stringify({ useNewCheckout: true }) }));\n  await page.goto('https://shop.example.com/checkout');\n  await expect(page.locator('#new-checkout')).toBeVisible();\n});\n```\n\n## Follow-up Questions\n- How would you simulate flaky flag propagation and recoveries?\n- What strategies ensure idempotency across retries and regional stores?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-8",
        "questionId": "q-19137",
        "question": "Design an end-to-end test plan for a real-time collaborative document editor (multi-user, presence, offline edits, CRDT convergence). Outline how you'd simulate multiple clients in parallel, network partitions, offline mode, and eventual consistency checks; specify tooling (Playwright), test data isolation, and how to validate conflict resolution and deterministic merge?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use 3 region-local test runners simulating 20 users per region across 5k channels. Seed per-region users and channels, then drive message sends, edits, deletions, and reactions while toggling a region",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright with two isolated browser contexts, mock WebSocket streams, and a controllable offline toggle. Create a tenant-scoped document, perform an offline edit in one context then reconnect; ve",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end test for a fintech payments platform that handles card-on-file payments, 3DS authentication, and settlement across multi-region microservices. Include scenarios for gateway outages, partial response…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a Playwright-based E2E with three browser contexts (A,B,C) editing the same document. Use a mocked presence/CRDT service to simulate joins, offline edits, and merges. Steps: open doc in all co",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time collaboration, presence, offline resilience, and CRDT/OT convergence across clients.\n\n## Key Concepts\n- Real-time collaboration, presence, CRDT/OT, offline mode\n- Parallel browser contexts and deterministic test data\n- Network partition handling and merge correctness\n\n## Code Example\n```javascript\n// Playwright sketch\nconst { chromium } = require('playwright');\n// Create 3 contexts, navigate to doc, perform edits, simulate offline, validate convergence\n```\n\n## Follow-up Questions\n- How would you isolate test data to avoid cross-test interference?\n- How would you handle flaky timing in presence updates?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-9",
        "questionId": "q-4930",
        "question": "You’re testing an admin-driven per-user feature flag in a SaaS app. The API toggles a 'CompactDashboard' flag for a specific user; on login, the UI should render the compact dashboard when the flag is on and the full dashboard when off. Design a beginner-friendly E2E test that seeds a test user, enables the flag via API, logs in, and asserts the compact UI; then disables and re-asserts the full UI. Include data isolation and reset steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed deterministic data, freeze time, then inject drift and schema changes. Verify online features reflect drift within SLA while offline training uses updated schema. Test cache invalidation/rehydrat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed a test user, then call the flag API to enable CompactDashboard for that user. Log in as that user and verify the UI renders the compact dashboard (check for a specific element). Reload to confirm",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use 100 tenants, staggered flag rollout, and blue/green environments. Verify per-tenant flag visibility across API, WebSocket, and caches; ensure data isolation by tenancy keys; inject deterministic f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Run a Cypress/Playwright test that triggers Delete My Data in the UI, then assert API calls across Auth, CRM, analytics, and backups show tenant-scoped deletion. Re-submit to prove idempotency. Simula",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for per-user feature flags are common in SaaS apps. This question checks the ability to seed data, perform API-driven state changes, and verify UI behavior across login and page reloads in a beginner-friendly way.\n\n## Key Concepts\n- Per-user feature flags and API seed/reset\n- End-to-end UI verification of dynamic layouts\n- Test isolation and deterministic CI runs\n\n## Code Example\n```javascript\n// Example using Playwright-like syntax (pseudocode)\n// seedUser(), setFlag(), login() are helper utilities\ntest('compact dashboard toggling', async ({ page, request }) => {\n  const { userId, email, password } = await seedUser(request);\n  await setFlag(request, userId, 'CompactDashboard', true);\n  await login(page, email, password);\n  await expect(page.locator('.dashboard-compact')).toBeVisible();\n  await page.reload();\n  await expect(page.locator('.dashboard-compact')).toBeVisible();\n  await setFlag(request, userId, 'CompactDashboard', false);\n  await page.reload();\n  await expect(page.locator('.dashboard-full')).toBeVisible();\n});\n```\n\n## Follow-up Questions\n- How would you guard against flaky UI assertions caused by caching?\n- How would you extend the test to cover multiple user segments (roles/regions) and parallel execution?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-10",
        "questionId": "q-7682",
        "question": "Design an end-to-end test for a multi-tenant SaaS checkout with per-tenant feature flags (promo codes and tax rules) that must work identically across three regions. Validate price accuracy, promo stacking, tax calculation, payment flow, and idempotent retries under flaky network, while guaranteeing test isolation and parallel execution. Include tooling, data seeds, and verification methods?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed tenant A in US with synthetic accounts and assets; run test that logs in as tenant A, places an order, asserts risk checks, matching, and ledger entries include tenant_id; simulate cross-region r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed a test user, then call the flag API to enable CompactDashboard for that user. Log in as that user and verify the UI renders the compact dashboard (check for a specific element). Reload to confirm",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright with per-tenant seeds and region-scoped data stores. Create three tenants, each with distinct tax rules and promos, enable flags, and drive the end-to-end checkout: cart total, discount",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Launch three browser contexts in parallel, connect to the same document, perform interleaved edits, then simulate disconnects and reconnections. Seed initial content and user IDs; verify final document equality across a…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests must handle multi-tenant isolation, flag-driven behavior, and cross-region consistency. It evaluates ability to seed realistic data, mock payment providers with idempotent keys, and design parallel, flaky-network aware tests. It also probes how candidates enforce test determinism and rollback.\n\n## Key Concepts\n\n- Multi-tenant data isolation (schemas or per-tenant seeds)\n- Region-specific tax rules and promo feature flags\n- End-to-end checkout flow with payment gateway and ledger\n- Idempotent retries and flaky network handling\n- Data seeds and parallel test execution by region\n- Observability: verifications via UI, API, and logs\n\n## Code Example\n\n```javascript\nimport { test, expect } from '@playwright/test';\n\ntest.describe('checkout multi-tenant', () => {\n  test('region-1 checkout validates price', async ({ page }) => {\n    // seed tenants via API\n    // select region/tenant, enable flags\n    // perform checkout: add to cart, apply promo, calc tax, pay\n    // assert final price and ledger entry\n  });\n});\n```\n\n## Follow-up Questions\n\n- How would you ensure cross-region data isolation in CI?\n- What strategies stabilize flaky payments in E2E tests?\n- How would you measure and reduce test flakiness over time?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-11",
        "questionId": "q-18240",
        "question": "Design an E2E test plan for a data labeling platform with human-in-the-loop workflows, asynchronous task queues, and per-task audit logs. Include multi-user workspaces, race conditions when annotators claim the same task, retries, and latency variation across regions. Provide seeds, tooling choices, and verification methods to prove end-to-end correctness and auditability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright to emulate offline mode, add items to the cart, then re-enable network to trigger background sync; assert a single order is created with a stable id and no duplicates on retry. Seed cat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement an E2E test in Playwright for a grocery app. Steps: sign in, add two items to cart, proceed to checkout; during payment, trigger a price change and stock-out for one item. Verify car",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed a small dataset (5 tasks) and use dockerized Kafka topics for tasks and audits, UI automation with Playwright, and a Kubernetes namespace per run. Simulate two annotators claiming the same task t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement test retries with exponential backoff, use test isolation with proper cleanup, add explicit waits instead of sleep, run tests in parallel with proper resource allocation, and use test data f",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests real-world E2E reliability under human-in-the-loop and regional latency, not just API success.\n\n## Key Concepts\n- End-to-end workflow tracing across UI, queues, and data stores\n- Race conditions and idempotency in task claiming\n- Auditability and regional latency testing\n\n## Code Example\n```javascript\n// Pseudo setup for deterministic clock and queues\n```\n\n## Follow-up Questions\n- How would you measure latency budgets per region and alert on SLA drift?\n- How would you adapt the plan for a multi-tenant labeling platform with strict data isolation?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-12",
        "questionId": "q-8645",
        "question": "Design an E2E test for a multi-tenant analytics pipeline (Kafka -> processor -> warehouse). Use two tenants with isolated schemas. Seed and publish events to the streaming topic; verify per-tenant isolation in the warehouse and dashboards; exercise a replay API to re-ingest a window; simulate burst traffic to test backpressure; verify exactly-once processing and eventual consistency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright to set the flag via API for a test user, launch in a dedicated test env, seed user data, navigate to onboarding, assert the new 4-step wizard renders, complete steps, verify final dashb",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up two tenants A and B with isolated schemas. Seed synthetic events, publish to the streaming topic, and trace through the processor to the warehouse. Validate per-tenant isolation in queries and",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement an E2E test for a feature-flag gated checkout with three cohorts: canary (5%), regional (20%), full. Use LaunchDarkly for flag evaluation, k6 for traffic injection, and seed PostgreSQL with",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan to mock the remote flag service to inject per-user segments. Use two test users: ON and OFF. Verify that with ON the checkout totals show the discount and the promo banner is visible; with OFF no",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a complex data path with tenancy, streaming, replay semantics, and backpressure. Probes data isolation, idempotency, and eventual consistency under failure modes.\n\n## Key Concepts\n\n- Data isolation across tenants\n- Exactly-once or idempotent processing\n- Replay/re-ingestion guarantees\n- Backpressure and burst handling in streaming stacks\n- End-to-end verification across warehouse and dashboards\n\n## Code Example\n\n```javascript\n// Example test hook outline\n```\n\n## Follow-up Questions\n\n- How would you validate schema evolution without cross-tenant impact?\n- How would you extend to more tenants and regional data residency?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-13",
        "questionId": "q-13050",
        "question": "Design a beginner-friendly E2E test for a team chat application's message deletion flow that uses optimistic UI: post a message, delete it, verify it disappears, a toast Undo appears, and Undo restores the message with original metadata. Also simulate a brief network outage during deletion and verify that after reconnection the backend reflects the deletion and the UI remains consistent. Include tooling choices, test data isolation, and deterministic seeds?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed a fresh user, clear storage, and isolate data per run. Mock geolocation to a fixed point, throttle network to 3G, and stub maps autocomplete responses. Verify pickup/dropoff fields populate, ride",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Outline an end-to-end privacy flow test: seed region-bound user data; simulate consent capture; trigger regional data deletion (GDPR-like erasure) and verify data is removed across 50+ services within",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright with route interception to seed the chat state, post a new message, delete it, and confirm the UI hides the item while an Undo toast appears. Trigger Undo to restore the original messag",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Seed a deterministic US-East order via test fixture; place order through the UI; publish OrderPlaced, DriverAssigned, and ETAUpdated events to domain Kafka topics; verify real-time WebSocket updates reach both rider and…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for optimistic UI and offline resilience are common in messaging apps. This question probes ability to design deterministic E2E tests that cover front-end and back-end coordination, error paths, and data isolation.\n\n## Key Concepts\n\n- Optimistic UI updates\n- Network fault simulation and retry handling\n- Test data isolation and deterministic seeding\n- Front-end/back-end state reconciliation\n\n## Code Example\n\n```javascript\n// Playwright test snippet (simplified)\nimport { test, expect } from '@playwright/test';\n\ntest('delete with optimistic update and Undo', async ({ page }) => {\n  await page.goto('https://chat.app');\n  await page.fill('#newMessage', 'hello world');\n  await page.click('#send');\n  const msg = page.locator('.message').last();\n  await expect(msg).toHaveText('hello world');\n\n  await msg.click('.delete');\n  await expect(page.locator('.toast-undo')).toBeVisible();\n  await expect(page.locator('.message')).not.toContainText('hello world');\n\n  await page.click('.toast-undo');\n  await expect(page.locator('.message')).toContainText('hello world');\n});\n```\n\n## Follow-up Questions\n\n- How would you extend this to handle multiple concurrent deletions in a single channel?\n- How would you verify backend state during intermittent connectivity without flaky tests?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-14",
        "questionId": "q-24674",
        "question": "Design a beginner-friendly E2E test for a data-import workflow in a cloud storage web app: a user uploads a CSV with 500 rows, backend enqueues a processing job, and the UI shows live progress with invalid rows highlighted and a downloadable summary. Specify tooling (Cypress or Playwright), seed data, mock the processing service, and verify the final report matches expected counts and that re-upload is idempotent?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright for this E2E test. First, upload a fixture CSV containing 500 rows. Then intercept the POST request to /import and mock the job queue with a deterministic response to simulate asynchronous processing. See…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "An E2E test for a multi-tenant SaaS dashboard with per-tenant data isolation and RBAC. Seed tenants A,B,C with isolated datasets; enable a feature flag on A; verify via API and UI that Admin can creat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed a fresh user, enable onboarding_v2 via API, launch the app, verify a 3-step wizard renders in order with Next/Back/Finish, complete it and verify a profile flag is set; then turn onboarding_v2 of",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed per-region drivers and trips; mock traffic feed with outages; run 1k+ concurrent ride requests across regions; verify ETA, routing, and pricing converge within 2–5s per region; test degraded beha",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for data-import flows are common in cloud applications. This question evaluates practical E2E thinking at a beginner level, covering end-to-end flow, UI feedback, and data integrity.\n\n## Key Concepts\n\n- End-to-end flow of uploading data and handling background processing\n- Mocking backend services (job queue) for deterministic, reliable tests\n- Data seeding to ensure predictable idempotent behavior\n- Verifying UI progress indicators and downloadable summary reports\n\n## Code Example\n\n```javascript\n// Example Playwright snippet illustrating route mocking and upload\nconst { test, expect } = require('@playwright/test');\n\ntest('data import workflow with progress and idempotency', async ({ page }) => {\n  // Seed database with existing records for idempotency testing\n  await seedTestData();\n\n  // Mock the job queue response\n  await page.route('**/api/import', async (route) => {\n    await route.fulfill({\n      status: 200,\n      body: JSON.stringify({ jobId: 'test-job-123', status: 'processing' })\n    });\n  });\n\n  // Upload CSV fixture\n  await page.setInputFiles('input[type=\"file\"]', 'fixtures/test-data-500-rows.csv');\n\n  // Verify live progress updates\n  await expect(page.locator('.progress-bar')).toBeVisible();\n  await expect(page.locator('.progress-text')).toContainText('100%');\n\n  // Verify invalid rows are highlighted\n  await expect(page.locator('.invalid-row')).toHaveCount(5);\n\n  // Download and verify summary report\n  const [download] = await Promise.all([\n    page.waitForEvent('download'),\n    page.click('.download-summary')\n  ]);\n\n  const report = await download.text();\n  expect(report).toContain('Total: 500');\n  expect(report).toContain('Valid: 495');\n  expect(report).toContain('Invalid: 5');\n\n  // Test idempotency - re-upload same file\n  await page.setInputFiles('input[type=\"file\"]', 'fixtures/test-data-500-rows.csv');\n  await page.waitForSelector('.upload-complete');\n\n  // Verify no duplicates were created\n  const recordCount = await getRecordCount();\n  expect(recordCount).toBe(500);\n});\n```",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-15",
        "questionId": "q-11368",
        "question": "Design a beginner-friendly E2E test for a 4-step fintech onboarding wizard with auto-save every 20s, offline support, and a single final submit. Include tooling (Playwright), data isolation, mocked identity API, and a determinism strategy for retries?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: Use Playwright with two isolated browser sessions (UserA and UserB) to test offline-first real-time chat functionality. Steps: 1) Initialize both users and establish baseline connection; 2) Simulate network lo…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Set up a comprehensive E2E test using Cypress or Playwright with a mock backend service. Begin by seeding the server with initial test data, then simulate offline mode by setting `navigator.onLine = false`. Create a new…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed messages with increasing sequence IDs; burst 1k msgs/s for 15 minutes; run Flink with 60s checkpoints and Kafka offsets committed after sink ack to enforce exactly-once; simulate consumer rebalan",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright to automate a 4-step fintech onboarding wizard with auto-save every 20s. Isolate data with a unique test user and mocked identity API. Simulate offline after step completion (context.se",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests at the beginner level often miss realistic offline scenarios and deterministic retries. This question probes practical E2E thinking: isolation, network control, and idempotent submission.\n\n## Key Concepts\n\n- End-to-end orchestration of multi-step flows\n- Auto-save timing and state persistence (e.g., localStorage/IndexedDB)\n- Offline/online transition handling and reconciling state\n- Mocking external APIs (identity) for determinism\n- Ensuring single, idempotent final submit\n\n## Code Example\n\n```javascript\n// Playwright sketch (context-level offline, data isolation, mocked API)\nconst context = await browser.newContext({ storageState: 'test-user.json' });\nconst page = await context.newPage();\nawait context.setOffline(true);\nawait page.goto('https://finapp/onboarding');\n// perform step interactions, wait for auto-save marker, then go online\nawait context.setOffline(false);\n// verify single submit\n```\n\n## Follow-up Questions\n\n- How would you extend this to mobile vs desktop layouts?\n- How would you validate that auto-save writes to the correct storage (localStorage vs IndexedDB) across browsers?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-16",
        "questionId": "q-12326",
        "question": "Design a realistic E2E test for a multi-tenant SaaS product that supports per-tenant feature flags and regional data residency. When onboarding a new tenant, a separate data mart is provisioned in Region A, an API route is gated by the flag, and user actions flow through a shared service mesh with Redis cache and CDN assets. Validate tenant isolation, flag-based UI behavior, regional data residency, cache invalidation, and end-to-end observability. Include data seeds, mock services, rollback/kill switches, and observability assertions (traces, logs, metrics) to prove isolation, correctness, and performance under flag toggles?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright with a per-run test DB and mocked transcoding endpoints. Upload a short video; intercept transcoding calls to return ready states for 1080p/720p/480p. Poll both regional feeds for the a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Provide a concrete E2E test plan that covers: onboarding a new tenant with per-tenant feature flags, provisioning a dedicated data mart in Region A, gating API endpoints behind the flag, and ensuring complete tenant iso…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose a Playwright-based E2E with three browser contexts (A,B,C) editing the same document. Use a mocked presence/CRDT service to simulate joins, offline edits, and merges. Steps: open doc in all co",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an E2E test suite for the 'NewUserTour' feature-flag gated onboarding flow using Playwright. The test should: 1. **Mock the Feature Flag Service**: Intercept API calls to return controlled flag states. 2. **Seed…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the integration of multi-tenant isolation, per-tenant feature flags, regional data residency, and cross-service observability. It requires a concrete plan around provisioning, gating, cache coherence, rollback mechanisms, and telemetry across regions.\n\n## Key Concepts\n- Multi-tenant isolation and data segregation\n- Feature flags and rollout strategies\n- Regional data residency provisioning\n- Cache invalidation and CDN coherence\n- Observability across traces, metrics, and logs\n- Service mesh communication patterns\n- Kill switches and rollback procedures\n\n## Code Example\n```javascript\n// Pseudo-setup for E2E test scaffolding\nconst tenant = createTenantSeed({region: 'RegionA', flags: {newFeature: true}});\nconst testSuite = new E2ETestSuite({tenant, observability: true});\n\n// Test tenant isolation and data residency\ntestSuite.assert('Data resides in Region A', () => {\n  return verifyDataLocation(tenant.id, 'RegionA');\n});\n\n// Test feature flag gating\ntestSuite.assert('API respects feature flag', () => {\n  return testEndpointAccess(tenant.id, '/new-feature', true);\n});\n```",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-17",
        "questionId": "q-11964",
        "question": "Design an E2E test for a feature-flag gated checkout: verify both flag states render the correct UI, ensure purchases use an idempotent key, and confirm analytics events reconcile across environments. Include tooling choice (Cypress or Playwright), how to mock the flag service, data isolation per tenant, and tests that cover cross-UI parity and eventual consistency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement an E2E test in Playwright for a grocery app. Steps: sign in, add two items to cart, proceed to checkout; during payment, trigger a price change and stock-out for one item. Verify car",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed a fresh user, mock GET /flags? onboarding=true. Open app, assert onboarding modal appears, complete it, and verify localStorage.onboarding_complete = 'true'. Reload; modal should be skipped. Then",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an E2E test for a feature-flag gated checkout: verify both flag states render the correct UI, ensure purchases use an idempotent key, and confirm analytics events reconcile across environments.",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement an E2E test for a feature-flag gated checkout with three cohorts: canary (5%), regional (20%), full. Use LaunchDarkly for flag evaluation, k6 for traffic injection, and seed PostgreSQL with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests around feature flags in critical flows like checkout are common in large apps. This question probes how to manage multiple UI branches, data isolation per tenant, and end-to-end integrity, including idempotency and analytics reconciliation.\n\n## Key Concepts\n\n- Feature flags and environment isolation\n- Deterministic testing with mocks and fake timers\n- Idempotent purchases and event reconciliation\n- Analytics/instrumentation validation across retries\n\n## Code Example\n\n```javascript\n// Playwright test skeleton for flag-gated checkout\nimport { test, expect } from '@playwright/test';\n\ntest.describe('Flag-gated checkout', () => {\n  test('flag ON renders new UI and processes purchase', async ({ page }) => {\n    // mock flag service to ON for tenant A\n    await page.route('**/flags/**', route =>\n      route.fulfill({ status: 200, body: JSON.stringify({ flag: 'on' }) })\n    );\n    // proceed with purchase flow, using idempotent key\n    // assert UI contains new checkout and purchase succeeds\n  });\n\n  test('flag OFF renders legacy UI and processes same purchase', async ({ page }) => {\n    // mock flag service to OFF for tenant A\n    await page.route('**/flags/**', route =>\n      route.fulfill({ status: 200, body: JSON.stringify({ flag: 'off' }) })\n    );\n    // proceed with same purchase flow, ensure success and analytics\n  });\n});\n```\n\n## Follow-up Questions\n\n- How would you scale this to many tenants and multiple flag states in parallel?\n- How would you verify that analytics events are eventually ingested and deduplicated?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-18",
        "questionId": "q-18993",
        "question": "Design an end-to-end test for a multi-region, PCI-compliant payments platform that uses tokenized PANs, authorization, and settlement across two regions with Kafka as the event bus and a separate fraud service. Include seeds, idempotency handling, partial failure simulations (gateway timeout, bank delay), and a reconciliation check that settlements match ledger entries within SLA?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright with two isolated contexts for tenants A and B. Seed tenants with the same flag schema. Update the flag for A via API; verify admin UI shows the new value and a WebSocket event propagat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an E2E test in Playwright that seeds Accounts, Contacts, Opportunities; triggers a background export for a user with Export permission; asserts progress via WebSocket (Queued → Processing →",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an E2E test for a multi-tenant feature-flag platform with canary rollout, regional replication, and webhook delivery to external systems. Seed tenants, assign rollout percentages (0, 25, 50, 10",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a test that seeds tokenized PANs, executes authorization, capture, and settlement across two regions using Kafka events and a fraud service, then validates ledger reconciliation within SLA. Inc",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests must cover cross-region payment flows, tokenization, and reconciliation under real-world failures. It emphasizes deterministic seeds, idempotency, and end-to-end integrity in a PCI context.\n\n## Key Concepts\n- Cross-region E2E flows with tokenized PANs\n- Idempotency keys and deduplication guarantees\n- Kafka-based event-driven orchestration\n- Fraud service integration and its impact on flow\n- Partial failure injection and resilience\n\n## Code Example\n```javascript\n// Pseudo seeds and flow control for the test\nconst seeds = {\n  panToken: 'tok_TEST_4242',\n  merchantId: 'merch_001',\n  regionA: 'us-east-1',\n  regionB: 'eu-west-1'\n};\n```\n\n## Follow-up Questions\n- How would you measure end-to-end settlement latency across regions?\n- How would you test idempotency under webhook retries and out-of-order events?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-19",
        "questionId": "q-26371",
        "question": "Design an E2E test for a bulk data import in a Salesforce-like CRM: a user uploads a CSV of 10k records; the system enqueues jobs across microservices, updates a live progress bar, and writes an auditable per-record log. Ensure idempotent re-upload (no duplicates), partial failures with deterministic retries, and eventual consistency where created records appear in UI and API within a defined window. Provide tooling choice (Cypress or Playwright), data seeding, and mocks for the message bus and downstream services; specify concrete UI/API assertions and performance targets (e.g., complete in 15 minutes)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright for end-to-end testing. The test uploads a 10,000-row CSV file, seeds required test data, and mocks the message bus to simulate partial failures with deterministic retries. Assert that the progress bar re…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Coordinate three simulated clients editing a single document concurrently via Automerge CRDT, with latency injection and offline periods across two regions. After reconnection, verify the final docume",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design two E2E tests in Playwright to cover both flag states. Seed the cart and user via API before each test. Toggle the feature flag using a POST request to /api/feature-flags. Mock the tax service to return 8.5% tax…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "A robust beginner answer would outline: seed translations and mock translation API, load /?locale=es and assert UI strings are Spanish, verify Intl date and currency formatting, switch locale without",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates a candidate's ability to design end-to-end tests for asynchronous bulk import workflows in a distributed system. It probes understanding of idempotency handling, auditability, and resilience testing across multiple microservices.\n\n## Key Concepts\n- End-to-end testing across UI, backend, and message bus\n- Batch processing with idempotency and retry semantics\n- Partial failure handling with deterministic retries\n- Eventual consistency verification\n- Test data isolation and deterministic test design\n\n## Code Example\n```javascript\n// playwright/bulk-import.spec.js\nconst { test, expect } = require('@playwright/test');\n\ntest.describe('Bulk Data Import', () => {\n  test('handles 10k records with idempotency, retries, and eventual consistency', async ({ page }) => {\n    // Seed test data\n    await seedTestEnvironment();\n    \n    // Mock message bus for partial failure simulation\n    await mockMessageBus({\n      failProbability: 0.1,\n      deterministicSeed: 42\n    });\n    \n    // Mock downstream services\n    await mockDownstreamServices();\n    \n    // Upload CSV\n    const fileChooserPromise = page.waitForEvent('filechooser');\n    await page.click('[data-testid=\"upload-button\"]');\n    const fileChooser = await fileChooserPromise;\n    await fileChooser.setFiles('./test-data/10k-records.csv');\n    \n    // Verify progress bar updates\n    await expect(page.locator('[data-testid=\"progress-bar\"]')).toBeVisible();\n    const progressText = page.locator('[data-testid=\"progress-text\"]');\n    \n    // Wait for completion with 15-minute timeout\n    await expect(progressText).toHaveText('100%', { timeout: 900000 });\n    \n    // Assert per-record audit logs\n    const auditLogs = await fetchAuditLogs();\n    expect(auditLogs).toHaveLength(10000);\n    expect(auditLogs.filter(log => log.status === 'success')).toHaveLength(9000);\n    expect(auditLogs.filter(log => log.status === 'retried')).toHaveLength(1000);\n    \n    // Test idempotency - re-upload same file\n    await fileChooserPromise;\n    await page.click('[data-testid=\"upload-button\"]');\n    const secondFile = await fileChooserPromise;\n    await secondFile.setFiles('./test-data/10k-records.csv');\n    \n    // Verify no duplicates created\n    const recordCount = await fetchRecordCount();\n    expect(recordCount).toBe(10000);\n    \n    // Verify eventual consistency - records appear in UI and API\n    await page.waitForFunction(\n      () => fetchRecordCount() === 10000,\n      { timeout: 300000, polling: 2000 }\n    );\n    \n    // UI assertion\n    await expect(page.locator('[data-testid=\"record-count\"]')).toContainText('10,000');\n    \n    // API assertion\n    const response = await page.request.get('/api/records');\n    expect(response.status()).toBe(200);\n    expect(response.json()).toHaveLength(10000);\n  });\n});\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-20",
        "questionId": "q-23017",
        "question": "Design an end-to-end test for a serverless extensions marketplace where third-party extensions run in tenant-isolated sandboxes and respond to events from a multi-tenant chat app. Include data seeds, concurrency burst, per-tenant isolation with quotas, and auditing; verify no cross-tenant data leakage, at-least-once event delivery, and cleanup after uninstall?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed two tenants with model versions v1 and v2 and a canary rule. Run parallel inferences across tenants; assert responses originate from the intended version via a version header, and that tenant dat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a comprehensive end-to-end test suite for a serverless extensions marketplace that validates the following: Create 3 distinct tenants and seed per-tenant reference data (users, channels, settings). Deploy 4 sandb…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement a 10% canary with a mock flag service to control exposure per user segment; drive end-to-end paths across Go services, Node.js, and React UI with Playwright and API mocks; inject flag-servic",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed deterministic inputs to produce both approved and flagged transactions; mock RiskEngine with fixed outputs and inject latency to create backpressure. Verify quarantined events do not reach Ledger",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for serverless, multi-tenant extension sandboxes require robust isolation guarantees, quota enforcement under burst loads, and reliable event delivery semantics. This question evaluates your ability to architect end-to-end test flows that encompass test data seeding, observability instrumentation, concurrency handling, and comprehensive failure mode validation across tenant boundaries.\n\n## Key Concepts\n\n- **Tenant Isolation**: Strict data separation at database, memory, and network levels\n- **Per-Tenant Quotas**: Resource limits enforced under burst conditions\n- **Event Delivery Semantics**: At-least-once guarantees with idempotent processing\n- **Sandbox Enforcement**: Extension runtime boundaries and permission scoping\n- **Audit Trails**: Complete visibility into extension lifecycle and data access\n- **Cleanup Verification**: Guaranteed resource teardown after uninstallation\n\n## Code Example\n\n```javascript\n// Test fixture: tenant event flow setup\nasync function seedTenantData(tenantId, db) {\n  await db.users.insertMany([\n    { id: `u1_${tenantId}`, tenantId, name: 'Admin' },\n    { id: `u2_${tenantId}`, tenantId, name: 'Member' }\n  ]);\n  await db.channels.insertMany([\n    { id: `ch1_${tenantId}`, tenantId, name: 'general' }\n  ]);\n}\n\nasync function deployExtension(tenantId, extensionId, permissions) {\n  const sandbox = await createIsolatedSandbox({ tenantId, permissions });\n  await sandbox.deploy(extensionId);\n  return sandbox;\n}\n\nasync function fireBurstEvents(eventBus, tenants, count) {\n  const events = [];\n  for (let i = 0; i < count; i++) {\n    const tenant = tenants[i % tenants.length];\n    events.push(eventBus.publish({\n      tenantId: tenant.id,\n      type: 'message.created',\n      payload: { id: `msg_${i}`, channelId: `ch1_${tenant.id}` }\n    }));\n  }\n  await Promise.all(events);\n}\n\n// Validation assertions\nasync function validateIsolation(db, tenants) {\n  for (const tenant of tenants) {\n    const otherData = await db.events.find({ tenantId: { $ne: tenant.id } }).toArray();\n    const tenantData = await db.events.find({ tenantId: tenant.id }).toArray();\n    \n    for (const event of tenantData) {\n      expect(event.payload.channelId).toContain(tenant.id);\n    }\n  }\n}\n\nasync function validateDelivery(eventBus, expectedCounts) {\n  for (const [tenantId, expected] of Object.entries(expectedCounts)) {\n    const processed = await eventBus.getProcessedCount(tenantId);\n    expect(processed).toBeGreaterThanOrEqual(expected);\n  }\n}\n\nasync function validateCleanup(sandbox, db) {\n  await sandbox.uninstall();\n  const webhooks = await db.webhooks.find({ sandboxId: sandbox.id }).toArray();\n  const auditLogs = await db.auditLogs.find({ resourceId: sandbox.id }).toArray();\n  \n  expect(webhooks).toHaveLength(0);\n  expect(auditLogs).toMatchObject([{ action: 'uninstall', status: 'success' }]);\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-21",
        "questionId": "q-18211",
        "question": "Design an E2E test for a Slack-like chat: a user sends a message in a channel; verify real-time delivery to all connected clients via WebSocket; simulate an offline client that reconnects later and ensures the message appears; test idempotency by resending the same message and ensuring no duplicates; verify read receipts propagate across clients; isolate test data; use Playwright with network emulation and WebSocket mocks for determinism?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an E2E for a Slack-like chat: a user sends a message in a channel; verify real-time delivery to all connected clients via WebSocket; simulate an offline client that reconnects later and ensures",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Playwright with three isolated pages to simulate participants. Mock navigator.mediaDevices.getUserMedia to provide fake streams and stub the signaling channel to a local mock server. Use unique me",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright with API fixtures to seed a single paid order for isolation; mock the invoice endpoint to produce a deterministic PDF; click Generate Invoice, verify a download starts, parse the PDF to",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Approach: parameterize tests by locale, seed catalog and prices per locale, and drive a full path from product listing to order, with a locale switch mid-flight. Use Playwright with locale-aware conte",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTesting real-time messaging with offline recovery and read receipts is a common production pain point. This question probes WebSocket choreography, event-driven consistency, and deduplication under partitions.\n\n## Key Concepts\n- Real-time WebSocket delivery\n- Offline/online transitions\n- Eventual consistency\n- Idempotency and deduplication\n- Test data isolation\n- Deterministic mocks\n\n## Code Example\n```javascript\n// Playwright-like pseudo\nasync function testChatRealTime() { /* ... */ }\n```\n\n## Follow-up Questions\n- How would you scale this across many channels?\n- How would you handle multiple brokers and namespaces?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-22",
        "questionId": "q-13324",
        "question": "Design an E2E test for a multi-tenant SaaS CRM that uses per-tenant data isolation, feature flags, and a server-side data reconciliation job that periodically computes a single source of truth across microservices (Contacts, Activities, Deals). The test should simulate 100 tenants with varying flag states, random long-running reconciliations, and ensure global consistency within 60 seconds after reconciliation completes, including automated verification of data parity across services and the audit trail. Include seeds, mocks, and verification steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a deterministic test plan: seed three tenants with distinct data residency rules; trigger a canary rollout followed by a 50% rollout; verify that within 60 seconds all 120 services observe the new flag state and…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright with separate browser contexts for Tenant A and Tenant B. Seed deterministic analytics data and mock external APIs. Enable the 'newAnalyticsPanel' feature flag for Tenant A via the admin API, authenticate…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed a test user, mock the email provider to capture the magic link, request the login, click the link, and confirm a session starts and lands on the dashboard. Validate token single-use, 15-minute ex",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed 100 tenants with varied feature flags and data; trigger a background reconciliation job that folds Contacts, Activities, and Deals into a single source of truth per tenant. Randomize writes durin",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis task tests end-to-end capabilities for complex, multi-tenant data flows, cross-service parity, and auditability under concurrency.\n\n## Key Concepts\n- Multi-tenant data isolation\n- Background reconciliation jobs\n- Cross-service consistency checks\n- Feature flag variability\n- Concurrency and race conditions\n- Audit logs and retention\n\n## Code Example\n```javascript\n// Pseudo-test asserting per-tenant SOT parity\nconst tenants = seedTenants(100);\nfor (const t of tenants) {\n  const hashA = hash(fetch(t, 'Contacts'));\n  const hashB = hash(fetch(t, 'Activities'));\n  const hashC = hash(fetch(t, 'Deals'));\n  assert(hashA === hashB && hashB === hashC, `Pariy fail for ${t.id}`);\n}\n```\n\n## Follow-up Questions\n- How would you verify parity without hashes?\n- How would you detect and recover from reconciliation drift in production?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-23",
        "questionId": "q-19952",
        "question": "Design an E2E test for a passwordless login flow: user inputs email, backend enqueues a one-time magic link, user clicks it to authenticate and lands on the dashboard. Include expiry (5 minutes), single-use tokens, and cross-device session creation; verify logout on one device revokes tokens on others; use Playwright with network mocks for the email provider to ensure determinism?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline an E2E for passwordless login: user enters email, system enqueues a one-time magic link; test that link logs in, creates a session, and redirects to dashboard; enforce 5-minute expiry, one-tim",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Provide a concrete E2E test plan that covers: onboarding a new tenant with per-tenant feature flags, provisioning a dedicated data mart in Region A, gating API endpoints behind the flag, and ensuring complete tenant iso…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use 100 tenants, staggered flag rollout, and blue/green environments. Verify per-tenant flag visibility across API, WebSocket, and caches; ensure data isolation by tenancy keys; inject deterministic f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an offline-first E2E test for a Progressive Web App that utilizes IndexedDB and a Service Worker queue to synchronize edits when connectivity is restored. Begin by seeding a clean test tenant, execute the test wi…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests that simulate real user journeys in security-sensitive flows, like passwordless login, reveal how teams handle token security, expiry, and multi-device sessions under deterministic conditions.\n\n## Key Concepts\n- Passwordless authentication flows\n- One-time tokens and expiry windows\n- Session management across devices\n- Test isolation with mocked email provider\n- Deterministic timing for flaky-edge cases\n\n## Code Example\n```javascript\n// Playwright sketch\ntest('passwordless login end-to-end', async ({ page, request }) => {\n  await page.goto('/login');\n  await page.fill('#email', 'user@example.com');\n  await page.click('#send-link');\n  // intercept and extract magic link from mocked email API\n  const link = await getMagicLinkFromEmail('user@example.com');\n  await page.goto(link);\n  await expect(page).toHaveURL('/dashboard');\n});\n```\n\n## Follow-up Questions\n- How would you test token revocation across multiple browsers and devices?\n- How would you simulate clock skew or expiry boundary conditions in CI?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-24",
        "questionId": "q-11650",
        "question": "Design a beginner-friendly end-to-end test for a mobile web vacation rental search and booking flow: city search, filters, listing, 2-night booking, and checkout with a mocked payment; ensure UI data reflects price and taxes, and booking shows in Upcoming trips. Include tooling, data seeding, and flaky-network retry strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed a sheet with 3 clients editing overlapping cells (with dependent formulas) across web and mobile. Use CRDT‑style syncing over WebSocket. Inject partitions, force reconnect, and verify all clients",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed a fresh user, mock GET /flags? onboarding=true. Open app, assert onboarding modal appears, complete it, and verify localStorage.onboarding_complete = 'true'. Reload; modal should be skipped. Then",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set up a comprehensive E2E test using Cypress or Playwright with a mock backend service. Begin by seeding the server with initial test data, then simulate offline mode by setting `navigator.onLine = false`. Create a new…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Set up a Cypress test that runs on a mobile viewport (375x812). Seed test data for a city with 3 listings, mock the payment gateway, and stub external APIs. Steps: search city, apply price/filter, pic",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests at beginner level need practical E2E coverage for mobile, data isolation, and flaky networks in beginner-friendly scope.\n\n## Key Concepts\n- Test data seeding and isolation\n- End-to-end booking flow\n- External service mocking (payments)\n- Retry and flakiness handling\n\n## Code Example\n```javascript\n// Example Cypress sketch (conceptual)\ndescribe('Booking flow end-to-end', () => {\n  it('searches, books, and confirms a booking', () => {\n    cy.viewport(375, 812)\n    cy.exec('node scripts/seed_test_data.js city listings=3')\n    cy.intercept('POST', '/payments/*', { statusCode: 200, body: { status: 'success' } }).as('pay')\n    cy.visit('/search?city=SampleCity')\n    cy.get('#filters').select('Under $200')\n    cy.get('.listing').first().click()\n    cy.get('#book').click()\n    cy.get('#checkout').type('4242424242424242')\n    cy.get('#pay').click()\n    cy.wait('@pay')\n    cy.get('.upcoming-trips').should('contain', 'SampleCity')\n  })\n})\n```\n\n## Follow-up Questions\n- How would you adapt for a non-PCI DSS environment?\n- How would you validate data consistency if the payment gateway is slow or retries?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-23T03:02:33.140Z",
    "version": 1
  }
]