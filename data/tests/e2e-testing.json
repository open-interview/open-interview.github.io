[
  {
    "id": "test-e2e-testing",
    "channelId": "e2e-testing",
    "channelName": "E2E Testing",
    "title": "E2E Testing Knowledge Test",
    "description": "Test your understanding of E2E Testing with 25 questions.",
    "questions": [
      {
        "id": "e2e-testing-q-0",
        "questionId": "q-10386",
        "question": "Design an end-to-end test plan for a feature-flag driven rollout of a critical UI revamp across web and mobile clients in a multi-service ecommerce platform. Include progressive rollout steps (0%, 5%, 25%, 100%), data isolation for test shards, cross-service consistency checks (auth, cart, checkout, analytics), randomized user segmentation, and automated rollback verification with drift detection when metrics diverge?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a centralized flag service with per-shard seeds; run parallel E2E runs across web/mobile. Validate 0/5/25/100% canaries by asserting UI states, auth validity, cart totals, checkout flows, and an",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Outline a beginner-friendly E2E test for an email verification flow in a fintech web app: signup, a one-time verification link, expiry handling, activation via the link, and preventing token reuse. Sp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a 10% canary with a mock flag service to control exposure per user segment; drive end-to-end paths across Go services, Node.js, and React UI with Playwright and API mocks; inject flag-servic",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly E2E test for onboarding with permission prompts and role-based UI gates in a fintech web app. Use Cypress or Playwright, seed guest and registered users, mock external servi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for feature-flag rollouts across services are common in large orgs; this checks end-to-end coverage, data isolation, and rollback control.\n\n## Key Concepts\n\n- Feature flag governance in distributed systems\n- Test shard data isolation\n- Consistency invariants across auth/cart/checkout/analytics\n- Drift detection and automated rollback\n\n## Code Example\n\n```javascript\n// Skeleton: Playwright test connecting to a feature flag service and asserting UI\nimport { test, expect } from '@playwright/test';\n\ntest('flagged UI renders consistently across shards', async ({ page, request }) => {\n  // setup: seed shard id and user\n  // enable flag for shard\n  // navigate and assert critical UI elements render\n});\n```\n\n## Follow-up Questions\n\n- How would you measure drift thresholds and alerting?\n- How do you handle flaky tests during rolled-out flags?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-1",
        "questionId": "q-14042",
        "question": "Design an end-to-end test for a multi-tenant ride-hailing web app: a rider requests a trip, a real-time driver match is created via WebSocket, trip progresses, payment is charged through a sandbox gateway, and post-ride analytics are enqueued. Include test data isolation per tenant, deterministic mocks for live events, handling of network partitions, and verification of eventual consistency across the ledger and analytics store. Specify tooling and steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright with a dedicated test tenant; seed tenant data; mock WebSocket match messages; stub the payment gateway to return a fixed token; trigger a trip request and advance simulated time to verify trip progressio…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Playwright to seed data via API: POST /seed with 7 days of events; open /analytics, set date range, click Export CSV; intercept POST /exports to return {id}; poll GET /exports/{id}/status every 50",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed three tenants via API with distinct regional tax configs. Run an E2E flow inside one tenant: signup, add to cart, checkout, tax calc, and ledger entry. Verify per-tenant data isolation, audit log",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Test a real-time collaborative editor with 3 browser contexts editing one document via WebSocket. Emit concurrent edits, verify CRDT/OT conflict resolution, autosave cadence to the durable store, and",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for real-time, multi-tenant flows with external systems are common and tricky. This question probes data isolation, deterministic mocks, and eventual consistency guarantees under failure.\n\n## Key Concepts\n- Multi-tenant data isolation\n- Real-time messaging (WebSocket)\n- External system stubs (payment gateway)\n- Idempotency and eventual consistency\n- Fault injection (network partitions)\n\n## Code Example\n```javascript\n// Pseudo Playwright snippet illustrating tenant-scoped seeding and WebSocket mock\n```\n\n## Follow-up Questions\n- How would you surface flaky WebSocket events?\n- What metrics would you track for test reliability?\n- How do you handle test data cleanup between runs?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-2",
        "questionId": "q-2367",
        "question": "Design an end-to-end test plan for a multi-tenant, localization-aware onboarding flow in a SaaS app. The flow includes marketing landing in the user's locale, signup that provisions a new workspace, email verification, in-app onboarding tour, and role-based access setup across three regions. Include how you isolate test tenants, seed data, simulate slow networks, verify asynchronous provisioning, and ensure no cross-tenant data leakage. Specify tooling (Playwright or Cypress), data seeding strategies, and validation hooks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed tenant A in US with synthetic accounts and assets; run test that logs in as tenant A, places an order, asserts risk checks, matching, and ledger entries include tenant_id; simulate cross-region r",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright or Cypress. Begin by seeding test data: products, locales, promos, and a mock payment gateway. Steps: 1) Add an item to cart, apply a promo code, then verify the updated total, tax, and currency display c…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright to automate a new-tenant onboarding across three regions. Steps: create tenant via API, seed locale data, sign up and verify email, launch onboarding tour, confirm default templates cre",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Playwright with per-tenant seeds and region-scoped data stores. Create three tenants, each with distinct tax rules and promos, enable flags, and drive the end-to-end checkout: cart total, discount",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests multi-tenant onboarding with localization, async provisioning, and strict data isolation across regions.\n\n## Key Concepts\n- Multi-tenant onboarding and workspace provisioning\n- Localization data and locale fallbacks\n- Async provisioning and eventual consistency across regions\n- Data isolation and cross-tenant leakage prevention\n- Data seeding via API/fixtures and feature flags\n\n## Code Example\n```javascript\n// Playwright test skeleton (pseudo)\ntest('onboard new tenant across regions', async ({page, request}) => {\n  const tenant = await signupTenant(request, {locale:'en-US', regions:['us','eu','ap']});\n  // further steps: sign-up, verify, tour, roles, validation\n});\n```\n\n## Follow-up Questions\n- How would you simulate locale-specific content failures?\n- How would you test concurrent onboarding to ensure isolation at scale?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-3",
        "questionId": "q-1603",
        "question": "Design an E2E test for a real-time collaborative post editor behind a region-specific feature flag (US vs EU) in a Twitter/Airbnb-like product. The test should cover flag gating, optimistic UI updates, WebSocket propagation across regions, eventual consistency after reconnections, and a rollback path when the flag is disabled. Which tooling and concrete steps would you use?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright with two parallel browser contexts simulating editors in US and EU regions. For flag gating, integrate with LaunchDarkly API via SDK client to toggle flags programmatically per test, or use test-specific…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Seed a document and initial CRDT state; open two Playwright contexts for User A and User B with configurable latency; perform concurrent edits, toggle network offline, then reconnect; assert final doc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Describe a deterministic E2E scenario that floods the API with 200 orders, validates inventory decrement and order creation across eventual-consistency windows (≤60s), and ensures idempotent retries f",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Trigger a GDPR deletion for a synthetic user across services; verify purge of PII in cart, catalog, checkout, loyalty, and analytics; validate DBs, caches, and search indexes show no PII; confirm audi",
            "isCorrect": false
          }
        ],
        "explanation": "This question evaluates E2E testing expertise for complex real-time, multi-region systems with feature flags—critical at scale in companies like Twitter, Airbnb, and Meta. Key concepts tested include:\n\n**Feature Flag Integration (LaunchDarkly/Optimizely):** Use SDK client for programmatic control rather than query params to ensure tests mirror production behavior. Configure targeting rules per user key to enable flags contextually.\n\n**WebSocket Lifecycle Testing:** Monitor connection events via Playwright's `page.on('websocket')`, validate message frames with `ws.on('message')`, and test reconnection with exponential backoff using `context.setOffline(true/false)`.\n\n**Optimistic UI Patterns:** Intercept local state updates immediately via `page.evaluate()`, verify content renders before server ACK using `waitForFunction()`, then confirm WebSocket propagation to other regions.\n\n**Region-Specific Routing:** Use `timezoneId` and `locale` context options to simulate US/EU geolocation. Set custom headers (`X-User-Key`, `X-Region`) to target appropriate backends.\n\n**Eventual Consistency:** Test after network drops by verifying local queue (`__pendingChanges`) accumulates edits, then validate sync completes after reconnection with ordered acknowledgment.\n\n**Rollback Safety:** Disable flag, verify graceful degradation to read-only UI, and ensure pending changes trigger appropriate alerts or persistence.\n\nThis comprehensive test suite validates the full stack: feature flag integration, real-time sync, network resilience, and safe rollout/deployment patterns—skills essential for senior SDET and test automation roles at big-tech companies.",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-4",
        "questionId": "q-22417",
        "question": "Design an end-to-end test for a distributed, AI-assisted document collaboration tool (desktop/mobile) that integrates an LLM for real-time suggestions. Include multi-client edits, offline scenarios, model version rollout with feature flags, prompt safety guardrails, audit log integrity, and deterministic replay seeds for reproducibility. Specify tooling, data seeds, and verification methods?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement with Playwright: three separate browser contexts for users A,B,C editing a shared doc. Simulate sequential and overlapping edits, check presence updates via the presence channel. Put one con",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose an E2E matrix with 3 clients editing the same document, one offline; use CRDT-based sync for convergence; rollout two model versions behind a feature flag; mock LLM outputs to enforce safety c",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a comprehensive end-to-end test suite for a serverless extensions marketplace that validates the following: Create 3 distinct tenants and seed per-tenant reference data (users, channels, settings). Deploy 4 sandb…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed messages with increasing sequence IDs; burst 1k msgs/s for 15 minutes; run Flink with 60s checkpoints and Kafka offsets committed after sink ack to enforce exactly-once; simulate consumer rebalan",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for AI-assisted real-time collaboration demand end-to-end coverage of multi-client sync, offline resilience, model versioning, guardrails, and auditability. This ensures the system remains correct, observable, and reproducible during complex deployments.\n\n## Key Concepts\n- AI-assisted real-time collaboration\n- End-to-end test matrix with offline scenarios\n- CRDT-based synchronization and conflict resolution\n- Feature flags and model version rollout\n- Prompt safety guardrails and audit log integrity\n- Deterministic replay seeds for reproducibility\n\n## Code Example\n\n```javascript\n// Pseudo test scaffold integrating Playwright + a mock LLM\ndescribe('AI-assisted doc E2E', () => {\n  it('validates offline-online converge and audit replay', async () => {\n    // setup seeds, mocks, and env\n  })\n})\n```\n\n## Follow-up Questions\n- How would you ensure deterministic LLM outputs in tests?\n- What instrumentation would you add to observability for failures?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-5",
        "questionId": "q-491",
        "question": "How would you set up a basic E2E test for a login form using Playwright?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: use Playwright with network mocks. Seed Region A inventory and a deterministic mock payment gateway. Test steps: create cart in Region A, go offline, add items, go online, place order; verify pa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright's test runner to create a test that navigates to the login page, fills credentials using `page.fill()`, submits the form with `page.click()`, and verifies successful login by checking the URL or page cont…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright's test() function with page.locator() to find elements. Fill credentials with fill(), click submit, then waitForURL() or expect(page.url()).toContain('/dashboard'). Add assertions for dashboard elements.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed realistic portfolios and liquidity pools; execute parallel end-to-end flows across multiple regions with mixed order types (market, limit, stop), simulating partial fills and network partitions. Validate matching e…",
            "isCorrect": false
          }
        ],
        "explanation": "## Test Setup\n- Install Playwright: `npm i @playwright/test`\n- Create test file: `login.spec.ts`\n\n## Key Steps\n- Navigate: `await page.goto('/login')`\n- Fill form: `await page.fill('#email', 'user@test.com')`\n- Submit: `await page.click('#submit')`\n- Assert: `await expect(page).toHaveURL('/dashboard')`\n\n## Best Practices\n- Use `test.describe()` for grouping related tests\n- Add `test.beforeEach()` for common setup logic\n- Use data-testid selectors for improved test stability",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-6",
        "questionId": "q-8645",
        "question": "Design an E2E test for a multi-tenant analytics pipeline (Kafka -> processor -> warehouse). Use two tenants with isolated schemas. Seed and publish events to the streaming topic; verify per-tenant isolation in the warehouse and dashboards; exercise a replay API to re-ingest a window; simulate burst traffic to test backpressure; verify exactly-once processing and eventual consistency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set up two tenants A and B with isolated schemas. Seed synthetic events, publish to the streaming topic, and trace through the processor to the warehouse. Validate per-tenant isolation in queries and",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Mock the WebSocket server to control message order and simulate network conditions; seed localStorage with initial chat history. Steps: 1) Load the dashboard page and chat widget; 2) Send messages through the UI interfa…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed two tenants with model versions v1 and v2 and a canary rule. Run parallel inferences across tenants; assert responses originate from the intended version via a version header, and that tenant dat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright. Seed a test user via API (POST /test/setup) with email tester@example.com and a password. Mock the email gateway by intercepting the send-email call and capture the 2FA code from the p",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a complex data path with tenancy, streaming, replay semantics, and backpressure. Probes data isolation, idempotency, and eventual consistency under failure modes.\n\n## Key Concepts\n\n- Data isolation across tenants\n- Exactly-once or idempotent processing\n- Replay/re-ingestion guarantees\n- Backpressure and burst handling in streaming stacks\n- End-to-end verification across warehouse and dashboards\n\n## Code Example\n\n```javascript\n// Example test hook outline\n```\n\n## Follow-up Questions\n\n- How would you validate schema evolution without cross-tenant impact?\n- How would you extend to more tenants and regional data residency?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-7",
        "questionId": "q-10171",
        "question": "Design a beginner-friendly E2E test for a wizard-style onboarding flow in a web app that collects a user name and email, persists progress in localStorage, and shows a final summary with a generated user ID. Use Cypress or Playwright, mock the account-creation API for determinism, ensure keyboard navigation works, test isolation, and verify the final screen shows the seeded ID?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Drive a 3-step onboarding wizard with Cypress. Before each run clear localStorage, seed test data, and intercept POST /api/create-account to return a deterministic id (user-123). Validate required fie",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Seed three tenants with distinct user rows in a per-tenant data store. Trigger a tenant-scoped export via API, including a full export and then incremental delta export. Use a workflow orchestrator (T",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would implement an automated test harness that instantiates N concurrent rooms with 50 synthetic participants per room, under varied network profiles including latency, jitter, and packet loss. The harness captures We…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright to drive two browser contexts within the same tenant but different users. Seed a known document, simulate offline on one context, perform edits in both, reconnect, and verify final cont",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests basic E2E skills on a realistic, client-side onboarding flow, including localStorage persistence, keyboard accessibility, and deterministic API mocking for repeatable results.\n\n## Key Concepts\n\n- E2E test isolation and deterministic mocks\n- LocalStorage state persistence across refresh\n- Keyboard navigation and accessibility\n- Test data seeding and cleanup\n- Cross-browser considerations for beginner level\n\n## Code Example\n\n```javascript\n// Example Cypress test\ndescribe('Onboarding wizard', () => {\n  beforeEach(() => {\n    cy.clearLocalStorage();\n    cy.viewport(1024, 768);\n  });\n\n  it('persists progress and uses deterministic API', () => {\n    cy.intercept('POST', '/api/create-account', { id: 'user-123', status: 'ok' }).as('create');\n    cy.visit('/onboarding');\n    cy.get('[data-testid=\"step1-name\"]').type('Alex');\n    cy.get('[data-testid=\"step1-name\"]').should('have.value', 'Alex');\n    cy.get('[data-testid=\"next\"]').click();\n    cy.get('[data-testid=\"step2-email\"]').type('alex@example.com');\n    cy.get('[data-testid=\"submit\"]').click();\n    cy.wait('@create');\n    cy.get('[data-testid=\"final-title\"]').should('contain', 'Welcome, Alex');\n  });\n});\n```\n\n## Follow-up Questions\n\n- How would you extend this to verify persistence across browser restarts in multiple browsers?\n- How would you test accessibility and keyboard-only navigation for all steps?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-8",
        "questionId": "q-25982",
        "question": "Design an E2E plan for a micro-frontend app using Module Federation where 20 SPAs share a single auth session and a global cart. Navigate across 8 federated apps, inject federated module load failures and network jitter, verify shared state consistency, and ensure graceful fallback without data loss. Include seeds, tooling (Playwright + MSW), and clear pass criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Drive the flow with Playwright or Cypress in a dedicated test env; stub bank OAuth endpoints and data fetch; seed test users and synthetic accounts; use an idempotency key on token exchanges; assert l",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an E2E plan for a micro-frontend app using Module Federation where 20 SPAs share a single auth session and a global cart. Navigate across 8 federated apps, inject federated module load failures",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright to emulate offline mode, add items to the cart, then re-enable network to trigger background sync; assert a single order is created with a stable id and no duplicates on retry. Seed cat",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright for end-to-end testing. The test uploads a 10,000-row CSV file, seeds required test data, and mocks the message bus to simulate partial failures with deterministic retries. Assert that the progress bar re…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests integration of multiple independently deployed frontends that must share auth and critical state across boundaries, including failure modes. It also probes testing tooling for complex module loading and network instability.\n\n## Key Concepts\n\n- Module Federation and micro-frontends\n- Shared state across independent apps\n- Fault injection: module load failures, network jitter\n- Deterministic seeds and test isolation\n\n## Code Example\n\n```javascript\n// Example Playwright stub for federated module load interception\nasync function testModuleLoad(page, mfName) {\n  await page.route('**/remoteEntry.js', route => route.continue());\n  // simulate load failure condition\n  // this is a placeholder to show intent\n}\n```\n\n## Follow-up Questions\n\n- How would you validate cross-origin isolation and secure token handling across federated modules?\n- What metrics would define a successful fallback without user-visible data loss?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-9",
        "questionId": "q-2388",
        "question": "Describe an E2E test for a multi-tenant onboarding flow where a new tenant is provisioned via API, then UI steps (profile, plan, webhooks) must complete across auth, billing, and notification services. Include per-tenant isolation (unique tenantId, separate DB/schema), data lifecycle, cross-service verification via logs/events, and cleanup. Use Playwright, REST, and DB probes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I'd implement a multi-session Playwright test that opens two pages simulating two users editing in parallel, use a mock WebSocket server to deterministically replay edits, fixture per-test doc, introd",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement with Playwright for UI, REST calls to provision tenant, and DB probes for isolation. Steps: (1) create tenant via API with unique tenantId; (2) launch onboarding UI scoped to tenant; (3) sim",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright with a mocked WebSocket price feed. Seed product data with fixed base prices and quantities. As the cart renders, emit price updates that adjust line item subtotals and the cart total i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed 100k synthetic fraud events into Kafka; run a Flink streaming job with a canary model; simulate downstream backpressure; hot-swap the model via feature-flag without downtime; validate latency bud",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests spanning provisioning, data isolation, and cross-service consistency are common in multi-tenant platforms. This question probes practical orchestration, data lifecycle, and observability.\n\n## Key Concepts\n- Multi-tenant provisioning via API and on-boarding UI\n- Per-tenant data isolation (dedicated schema/tenantId)\n- End-to-end across auth, billing, webhooks\n- Observability via logs, events, and DB probes\n- Cleanup and idempotent teardown\n\n## Code Example\n```javascript\n// Provision tenant\nconst tenant = await api.post('/tenants', {name:'onboard-test', region:'us-east'});\nawait page.goto(`/onboard?tenantId=${tenant.id}`);\n// complete steps\nawait page.fill('#name','Test User');\nawait page.click('#submit');\n```\n\n## Follow-up Questions\n- How would you test partial provisioning failures?\n- How ensure no cross-tenant data leakage in logs?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-10",
        "questionId": "q-4357",
        "question": "Design an advanced E2E test for a real-time analytics dashboard that consumes events from multiple producers via Kafka, handles schema evolution, late-arriving data, and backfills. Include end-to-end data path (producer -> Kafka -> stream processor -> sink -> UI), idempotent replay safety, regional multi-cluster considerations, fault injection (latency, downstream outage), and concrete verification criteria?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan an end-to-end test with a multi-producer harness emitting events to Kafka; validate schema evolution and late-arriving data by injecting field changes and backfills. Ensure idempotent sinks and e",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Seed a deterministic event set with known user_ids, actions, and timestamps. Publish an initial batch containing both on-time events and intentionally backdated events to simulate late arrivals. Validate that Snowflake…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright to drive a multi-region onboarding flow across Auth, Billing, and CRM services. Create per-tenant IDs to ensure data isolation and stub all external service calls deterministically. Toggle the onboarding-…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Join a live stream; mock Ad Server to return deterministic payloads; verify all connected clients display the same ad within the same break; simulate regional CDN latency and transient failures; ensur",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time analytics pipelines and cross-region consistency, focusing on schema evolution, late-arriving data, and backfills.\n\n## Key Concepts\n- Real-time data pipelines\n- Schema evolution and backfill\n- Idempotent processing\n- Multi-region data integrity\n- Fault injection and chaos testing\n\n## Code Example\n\n```javascript\n// Pseudo test harness sketch for event generation\n```\n\n## Follow-up Questions\n- How would you validate consumer lag impact on UI freshness?\n- How would you isolate tests when backfills run?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-11",
        "questionId": "q-7351",
        "question": "Design an E2E test for a Uber-like ride-booking flow focusing on per-tenant data isolation. Seed Tenant A with Rider R1 and Driver D1; seed Tenant B with Rider R2. Mock the driver-matching service to always assign D1 for Tenant A. Run parallel web and mobile flows, asserting dashboards and trip histories show only Tenant A data and that Tenant B data remains isolated. Include tooling choices, data seeds, and verification methods across UI and API?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed deterministic data, freeze time, then inject drift and schema changes. Verify online features reflect drift within SLA while offline training uses updated schema. Test cache invalidation/rehydrat",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an end-to-end test for a real-time market data platform that ingests tick data over WebSocket, normalizes to a canonical schema, and fans out to three downstream analytics services via a message bus, with backpre…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Leverage Cypress with tenant-scoped fixtures. Seed Tenant A with Rider R1 and Driver D1; Tenant B with Rider R2. Stub the matching service to always return D1 for Tenant A. Run parallel web and mobile",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design an end-to-end test for a fintech payments platform that handles card-on-file payments, 3DS authentication, and settlement across multi-region microservices. Include scenarios for gateway outages, partial response…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis tests understanding of multi-tenant data isolation in E2E tests, data seeding, and API/UI mocking in a ride-hailing context. It also checks cross-platform consistency (web/mobile) and end-to-end verification.\n\n## Key Concepts\n\n- Tenant isolation\n- Fixtures and mocks\n- Parallel E2E execution\n- UI + API verification\n- Test data lifecycle and cleanup\n\n## Code Example\n\n```javascript\n// Example Cypress setup (pseudo)\ncy.fixture('tenants/tenantA.json').then(...)\ncy.route('POST', '/match', { driverId: 'D1' }).as('matchTenantA')\n```\n\n## Follow-up Questions\n\n- How would you scale this to 100+ tenants without slowing tests?\n- How would you detect cross-tenant data leakage in logs or analytics dashboards?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-12",
        "questionId": "q-10543",
        "question": "Design an end-to-end test for a real-time collaborative editor (CRDT-based) where two users edit the same document concurrently, autosave every 2s, and presence updates arrive via WebSocket. Include how to simulate network partitions, offline mode, and reconnection, verify eventual consistency across all clients within 5s after reconnection, and ensure deterministic conflict resolution and test data isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design and implement a two-client Playwright test that opens the same document, performs concurrent edits, simulates disconnects, and verifies that all clients converge to the same document state with",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Playwright. Seed a test user via API (POST /test/setup) with email tester@example.com and a password. Mock the email gateway by intercepting the send-email call and capture the 2FA code from the p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end test for a real-time collaborative document editor that uses CRDTs. Simulate 4 clients editing concurrently, introduce a network partition, then reconnect and verify eventual convergence across all…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright to set the flag via API for a test user, launch in a dedicated test env, seed user data, navigate to onboarding, assert the new 4-step wizard renders, complete steps, verify final dashb",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-time collaboration, concurrency control, and fault tolerance in a CRDT-based editor; validates end-to-end state convergence under flaky networks.\n\n## Key Concepts\n\n- Real-time sync via WebSocket\n- CRDT conflict resolution and deterministic patches\n- Test data isolation across parallel clients\n- Network fault injection and offline/online transitions\n- Eventual consistency guarantees and timing windows\n\n## Code Example\n\n```javascript\n// Pseudo-outline for two-client Playwright test\nconst { chromium } = require('playwright');\n(async () => {\n  const browser = await chromium.launch();\n  const c1 = await browser.newContext();\n  const c2 = await browser.newContext();\n  const p1 = await c1.newPage();\n  const p2 = await c2.newPage();\n  // Open same doc, apply edits concurrently, simulate disconnects, assert convergence\n})();\n```\n\n## Follow-up Questions\n\n- How would you adapt this to support more than two concurrent editors?\n- What metrics would you collect to detect flakiness across runs?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-13",
        "questionId": "q-13679",
        "question": "Design an offline-first E2E test for a PWA using IndexedDB and a Service Worker queue to sync edits when connectivity returns; specify setup, offline/online transitions, conflict handling, idempotency checks, and how you would verify eventual server state with Playwright, including test data isolation and network mocking?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Outline a beginner-friendly E2E test for an email verification flow in a fintech web app: signup, a one-time verification link, expiry handling, activation via the link, and preventing token reuse. Sp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed two test users: one with the Notifications Center feature flag enabled, the other disabled. Sign in as each user and run E2E tests to verify the enabled user can see the Notifications panel and receives a sample no…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an offline-first E2E test for a Progressive Web App that utilizes IndexedDB and a Service Worker queue to synchronize edits when connectivity is restored. Begin by seeding a clean test tenant, execute the test wi…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement retry logic with exponential backoff, use test fixtures to mock slow responses, add explicit waits for network idle, and use test hooks to set up consistent test data. Combine with proper test isolation and co…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTesting offline-first behavior is essential for mobile-first applications and large-scale systems operating with intermittent connectivity. This question evaluates understanding of data queuing mechanisms, conflict resolution strategies, and eventual consistency in real-world network conditions.\n\n## Key Concepts\n\n- Offline-first architecture and conflict resolution strategies\n- IndexedDB for local data storage and Service Worker for background synchronization\n- Playwright network control capabilities and route interception techniques\n- Idempotency principles, replay safety mechanisms, and data isolation in test environments\n\n## Code Example\n\n```javascript\n// Playwright implementation: simulate offline conditions, perform user edits,\n// then restore connectivity and verify synchronization behavior",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-14",
        "questionId": "q-28467",
        "question": "Design a beginner-friendly E2E test for an offline-first note-taking PWA that supports creating notes offline, syncing to a backend when online, and resolving simple conflicts deterministically. Use Cypress or Playwright, simulate offline/online transitions, mock the backend sync API, seed initial notes, verify offline-created note uploads on connect and that a conflicting edit prompts a deterministic resolution?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a comprehensive end-to-end test plan leveraging a dedicated test harness that seeds 60 tenant contexts and executes concurrent onboarding workflows alongside production-simulated live usage actions. Include cross…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright. Seed two notes. Switch to offline, create a new note; ensure no POST to /api/notes occur. Go online, mock POST /notes to return 201 with serverId; verify the new note appears and shows",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright. Seed User A with cohort 0 and User B with cohort 1. Mock the remote config API to return a 10% rollout for cohort 1. Load the home page and verify that cohort 0 sees the old composer while cohort 1 sees…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed three tenants via API with distinct regional tax configs. Run an E2E flow inside one tenant: signup, add to cart, checkout, tax calc, and ledger entry. Verify per-tenant data isolation, audit log",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for offline-first flows are common in modern web apps and require practical use of network emulation, event queues, and deterministic conflict handling. This question probes hands-on ability to design reliable E2E tests that stay robust under connectivity changes.\n\n## Key Concepts\n\n- Offline-first UX and service workers\n- Network emulation in Playwright or Cypress\n- Backend API mocking and idempotent sync\n- Deterministic conflict resolution policy\n- Data seeding for repeatable tests\n\n## Code Example\n\n```javascript\n// Playwright test skeleton\nimport { test, expect } from '@playwright/test';\n\ntest('offline create and sync with deterministic merge', async ({ browser }) => {\n  const context = await browser.newContext();\n  const page = await context.newPage();\n  // seed data via API (omitted for brevity)\n  // go offline\n  await context.setOffline(true);\n  await page.goto('https://app.example');\n  await page.fill('#title', 'Offline note');\n  await page.fill('#body', 'content');\n  await page.click('#save');\n  await expect(page.locator('#status')).toHaveText('Queued');\n  // go online and sync\n  await context.setOffline(false);\n  const resp = await page.waitForResponse('/api/notes');\n  // assume server returns created note with serverId\n  await expect(resp.status()).toBe(201);\n  // verify note appears and is synced\n  await expect(page.locator('.note-title').last()).toHaveText('Offline note');\n  // optional conflict scenario in a second tab would be tested similarly\n});\n```\n\n## Follow-up Questions\n\n- How would you adapt this test for multiple concurrent offline edits across tabs?\n- How would you ensure test reliability when the backend response latency varies?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-15",
        "questionId": "q-25866",
        "question": "Design an end-to-end test for a feature-flagged product search ranking experiment on a large retail site. The flag ranking_v2 is rolled out to 20% of users via a remote flags service. Create deterministic seed data for products; assert that when ranking_v2 is enabled, search results reflect the new ranking; verify analytics events are emitted; ensure disabling ranking_v2 reverts results and events without duplicates across desktop and mobile, in multiple sessions. Specify tooling (Cypress or Playwright), data isolation, and how you mock the flag service?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use either Playwright or Cypress. Seed product, promo, and user fixtures. Mock the payment gateway with a sandbox API returning success, and drive a flaky network via throttling or offline/retry. Use",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "getByRole() locates elements by their ARIA role and accessible name, making it more resilient to UI text changes and better for accessibility testing. getByText() matches visible text content directly, which can be brit…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: Use Playwright (or Cypress) to run cross-browser E2E tests. Seed regional cohorts and mock the feature-flag service to control rollout. Validate that web and mobile UIs reflect region flag withi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright with isolated test contexts per run. Seed a catalog of 150 deterministic products and mock the feature flag service to enable ranking_v2 for 20% of sessions. Validate that, when enabled",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical E2E testing skills around feature flags, data seeding, cross-device validation, and deterministic mocks. It also evaluates how candidates handle paralellization, event deduplication, and rollback correctness in a live-like rollout.\n\n## Key Concepts\n\n- Feature flags and gradual rollouts\n- Deterministic data seeding for stable tests\n- Mocking external services (flag service, analytics)\n- Cross-device (desktop/mobile) parity and isolation\n- Event deduplication and correct rollback behavior\n\n## Code Example\n\n```javascript\n// Playwright skeleton for ranking_v2 on/off\nimport { test, expect } from '@playwright/test';\n\ntest('ranking_v2 enabled shows new ranking and analytics', async ({ page, request }) => {\n  // seed data and mock flag service to return true for ranking_v2\n  // navigate to search and verify ordering matches ranking_v2\n  // assert analytics endpoint received a single event per click\n});\n```\n\n## Follow-up Questions\n\n- How would you validate rollout skew and handle hotfix rollbacks?\n- How would you scale this test to 1000+ products with performance constraints?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-16",
        "questionId": "q-10715",
        "question": "Design an end-to-end test for a Zoom-like app that supports live captions and post-meeting transcription. The test should verify real-time caption accuracy, post-session ingestion into a data lake, and indexing for search, including handling transient transcription-service failures with retries and ensuring idempotent transcript writes across regional indices?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright with three isolated pages to simulate participants. Mock navigator.mediaDevices.getUserMedia to provide fake streams and stub the signaling channel to a local mock server. Use unique me",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement a comprehensive end-to-end test for a Zoom-like application with live captions and transcription capabilities. Using Playwright with a controllable mock transcription API, the test would follow these s…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Seed tenants in isolated namespaces with synthetic customers and invoices, perform checkout via Playwright, verify invoice creation and ledger reconciliation across billing, tax, and journal services,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan seeds for regions, drivers, and surge rules; run a multi-region end-to-end with Playwright/Cypress across web/mobile; simulate flaky networks and offline states; verify driver assignment respects",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design comprehensive end-to-end tests for real-time features involving complex data flows, fault tolerance, and distributed system consistency.\n\n## Key Concepts\n- Real-time UI testing with latency constraints\n- Service mocking for deterministic test outcomes\n- Retry mechanisms with exponential backoff\n- Idempotency in distributed write operations\n- Eventual consistency across regional data stores\n- Test isolation and cleanup strategies\n\n## Code Example\n```javascript\n// Playwright test framework demonstrating mock transcription service",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-17",
        "questionId": "q-30369",
        "question": "Design an E2E test for a feature-flag gated onboarding flow: a new funnel shows to a 25% user cohort controlled by a server flag. Include: (a) verification that flagged users see the new flow and unflagged see the old, (b) mid-session flag flip does not break UX, (c) deterministic data seeding and API mocks, (d) desktop and mobile coverage, (e) Cypress or Playwright choice with seed identities and isolated mocks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use server flag fixtures to drive two cohorts: flagged users see the new onboarding, others see the old flow. In Playwright, route /api/flags to return true/false per test, seed user_id in localStorag",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a test that seeds tokenized PANs, executes authorization, capture, and settlement across two regions using Kafka events and a fraud service, then validates ledger reconciliation within SLA. Inc",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Begin with Playwright. Seed per-tenant data, mock /api/data for filter/sort/paginate, and test both enabled/disabled views. Verify virtualization by asserting that only viewport rows exist in the DOM",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design and implement a two-client Playwright test that opens the same document, performs concurrent edits, simulates disconnects, and verifies that all clients converge to the same document state with",
            "isCorrect": false
          }
        ],
        "explanation": "This question evaluates practical E2E test design for feature flags, including determinism, data isolation, cross-device coverage, and handling mid-session flag changes. It requires concrete tooling choices, mocking strategies, and robust assertions to prevent flaky tests while validating user-journeys under real-world conditions.",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-18",
        "questionId": "q-29131",
        "question": "Design an end-to-end test for a multi-tenant streaming analytics platform where tenants ingest real-time events, dashboards update over WebSocket, and data must be strictly isolated per tenant across cache and storage. Provide seeds, traffic patterns (5 tenants, 20k events/s each), fault injections (broker drop, cache eviction), and verification that cross-tenant data leakage never occurs and latency stays under 2 seconds at peak?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an advanced end-to-end test for a real-time order-status pipeline using a pub/sub backbone (Kafka-like) with a downstream fan-out service. Validate exactly-once delivery, out-of-order processing, regional outages…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed two notes in localStorage, load the app, and navigate entirely with the keyboard: Tab through the notes list, press Enter to open the Create Note modal, type a title and body, then Save. Tab to the first note and p…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Set up a Cypress test that runs on a mobile viewport (375x812). Seed test data for a city with 3 listings, mock the payment gateway, and stub external APIs. Steps: search city, apply price/filter, pic",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an end-to-end test for a multi-tenant streaming analytics platform where tenants ingest real-time events, dashboards update over WebSocket, and data must be strictly isolated per tenant across cache and storage.…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question evaluates the ability to design reproducible, scalable end-to-end tests that validate strict data isolation in a real-time, tenant-separated system. It also tests understanding of failure scenarios that expose flaky caching or routing logic.\n\n## Key Concepts\n- End-to-end test orchestration across ingestion, broker, cache, storage, and UI\n- Per-tenant data isolation and security boundaries\n- Real-time WebSocket delivery and latency budgets\n- Fault injection and resilience under peak load\n- Telemetry, tracing, and reproducibility\n\n## Code Example\n\n```javascript\n// Seed data and tenant configuration\nconst tenants = [\n  { id: 'tenant-a', name: 'Acme Corp', apiKey: 'key_a_xxx' },\n  { id: 'tenant-b', name: 'Beta Inc', apiKey: 'key_b_xxx' },\n  { id: 'tenant-c', name: 'Gamma LLC', apiKey: 'key_c_xxx' },\n  { id: 'tenant-d', name: 'Delta Co', apiKey: 'key_d_xxx' },\n  { id: 'tenant-e', name: 'Epsilon Ltd', apiKey: 'key_e_xxx' },\n];\n\n// Traffic pattern: 20k events/second per tenant\nconst EVENTS_PER_SECOND = 20000;\nconst DURATION_SECONDS = 60;\n\nasync function ingestEvents(tenant, emitFn) {\n  const interval = 1000 / EVENTS_PER_SECOND;\n  for (let i = 0; i < DURATION_SECONDS * EVENTS_PER_SECOND; i++) {\n    emitFn({\n      tenantId: tenant.id,\n      eventId: uuid(),\n      timestamp: Date.now(),\n      payload: { value: Math.random(), tag: tenant.id },\n    });\n    await sleep(interval);\n  }\n}\n\n// Fault injection: broker drop\nasync function injectBrokerDrop(broker, dropPercent = 5) {\n  const originalPublish = broker.publish.bind(broker);\n  broker.publish = async (topic, msg) => {\n    if (Math.random() * 100 < dropPercent) return; // drop\n    return originalPublish(topic, msg);\n  };\n}\n\n// Fault injection: cache eviction\nfunction evictCacheKey(cache, keyPattern) {\n  const keys = cache.keys().filter(k => keyPattern.test(k));\n  keys.forEach(k => cache.delete(k));\n}\n\n// Verification: detect cross-tenant leakage\nasync function verifyIsolation(dashboard, expectedTenantId) {\n  const messages = await dashboard.getReceivedMessages();\n  for (const msg of messages) {\n    if (msg.tenantId !== expectedTenantId) {\n      throw new Error(`CROSS-TENANT LEAK: ${msg.tenantId} seen on ${expectedTenantId}'s dashboard`);\n    }\n  }\n}\n\n// Latency verification\nfunction verifyLatency(msgTimestamp, receiveTimestamp) {\n  const latency = receiveTimestamp - msgTimestamp;\n  if (latency > 2000) {\n    throw new Error(`LATENCY BREACH: ${latency}ms exceeds 2s threshold`);\n  }\n  return latency;\n}\n\n// Main test orchestration\nasync function runE2ETest() {\n  const broker = new MessageBroker();\n  const cache = new TenantCache();\n  const storage = new TenantStorage();\n  const dashboards = {};\n\n  // Setup WebSocket dashboards per tenant\n  for (const tenant of tenants) {\n    dashboards[tenant.id] = new WebSocketDashboard({\n      authToken: tenant.apiKey,\n      onMessage: (msg, ts) => {\n        verifyIsolation(dashboards[tenant.id], tenant.id);\n        verifyLatency(msg.timestamp, ts);\n      },\n    });\n  }\n\n  // Inject faults mid-test\n  setTimeout(() => injectBrokerDrop(broker, 5), 30000);\n  setTimeout(() => evictCacheKey(cache, /^tenant-a/), 45000);\n\n  // Run traffic for all tenants in parallel\n  await Promise.all(\n    tenants.map(tenant => ingestEvents(tenant, msg => broker.publish(`${tenant.id}/events`, msg)))\n  );\n\n  // Final assertions\n  console.log('All events processed. Verifying final state...');\n  for (const tenant of tenants) {\n    const storageData = await storage.getAll(tenant.id);\n    assert.strictEqual(storageData.length, EVENTS_PER_SECOND * DURATION_SECONDS);\n    console.log(`✓ ${tenant.name}: ${storageData.length} events stored`);\n  }\n}\n```\n\n## What to Look For in a Strong Answer\n1. Clear test structure covering setup, execution, and assertions\n2. Proper isolation verification at each layer (cache, broker, storage)\n3. Realistic fault injection with measurable impact\n4. Latency assertions tied to actual message timestamps\n5. Reproducibility through seed data and deterministic traffic",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-19",
        "questionId": "q-13180",
        "question": "You're building an Instacart-like checkout where a cart supports a dynamic promo code provided by a separate promo service. The code can be revoked by admin at any moment and must apply only once. Design an end-to-end test that checks: promo applies when active, not after revocation before checkout, retries are idempotent, and the billing system sees consistent state across cart, promo, and payment. Include tooling choices, data isolation, and how to simulate revocation and network delays?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement an E2E test for a checkout with a dynamic promo service. The user adds items, applies a promo, then checks out. The code may be revoked mid-flow and must apply only once. Validate: promo app",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Playwright with a per-run test DB and mocked transcoding endpoints. Upload a short video; intercept transcoding calls to return ready states for 1080p/720p/480p. Poll both regional feeds for the a",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed per-region drivers and trips; mock traffic feed with outages; run 1k+ concurrent ride requests across regions; verify ETA, routing, and pricing converge within 2–5s per region; test degraded beha",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I would drive an end-to-end flow with Playwright, seed two tenants, create a project in tenant A, invite a collaborator, and upload a blob via a mocked storage layer. Then trigger the create event twi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-service end-to-end logic with dynamic state changes and revocation. It verifies idempotent checkout and eventual consistency using mocks and isolated data. It also exercises real latency and failure scenarios that mimic production environments.\n\n## Key Concepts\n- E2E with external service mocks\n- Dynamic feature state and revocation handling\n- Idempotency and cross-service consistency\n- Test data isolation and tenant scoping\n\n## Code Example\n```javascript\n// Playwright example snippet for mocking promo API\n```\n\n## Follow-up Questions\n- How would you stress test this with high promo churn?\n- What metrics indicate this flow is flaky and how would you address it?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-20",
        "questionId": "q-22346",
        "question": "Design a beginner-friendly E2E test for an email verification flow in a fintech web app. Cover signup, sending a one-time verification link, link expiry handling, activation by clicking the link, and preventing token reuse. Specify tooling (Cypress or Playwright), test data seeding, mock the email service, and assertions that the user becomes Active once and no duplicate accounts are created?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan an end-to-end test with a multi-producer harness emitting events to Kafka; validate schema evolution and late-arriving data by injecting field changes and backfills. Ensure idempotent sinks and e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use 3 isolated browser contexts connected to the same docId via WebSocket CRDT Sync. Simulate latency and disconnects with network throttling. Have A,B,C perform concurrent edits, then reconnect and v",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline a beginner-friendly E2E test for an email verification flow in a fintech web app: signup, a one-time verification link, expiry handling, activation via the link, and preventing token reuse. Sp",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement an E2E test for live pricing via WebSocket across 3 regions, including a regional flag to disable surge. Seed region pricing, trigger a surge, verify live price updates, then booking locks t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes practical E2E test craft for user onboarding, focusing on external service mocks, data isolation, and idempotent behavior—key in fintech onboarding.\n\n## Key Concepts\n\n- E2E testing of authentication flows\n- Mocking external email gateways\n- Test data seeding and isolation\n- Token expiry and single-use guarantees\n- Deterministic assertions across browsers\n\n## Code Example\n\n```javascript\n// Example (Cypress)\ndescribe('Email verification onboarding', () => {\n  it('activates user via one-time link and prevents reuse', () => {\n    cy.request('POST', '/api/signup', { email: 'a@example.com', password: 'P@ssw0rd' })\n    .then(() => {\n      cy.task('getVerificationLink', 'a@example.com').then(link => {\n        cy.visit(link);\n        cy.get('#status').should('contain', 'Active');\n        cy.visit(link); // reuse attempt\n        cy.get('#error').should('exist');\n      });\n    });\n  });\n});\n```\n\n## Follow-up Questions\n\n- How would you simulate email expiry deterministically in a CI environment?\n- How would you verify that signing up with the same email twice does not create duplicates?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-21",
        "questionId": "q-13392",
        "question": "Design an E2E test for a multi-tenant analytics dashboard: when a user creates a new dataset, a scheduled ETL runs in the background and transformed results appear on the dashboard. Ensure tenant data isolation, mock external data sources, test idempotent dataset creation, and verify eventual consistency across regional replicas; specify tooling (Cypress or Playwright) and determinism strategies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a comprehensive end-to-end test for a feature-flag gated onboarding flow. Seed test user data with consistent identifiers, mock the admin API endpoints to deterministically control the onboarding flag state, a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design two E2E tests in Playwright to cover both flag states. Seed the cart and user via API before each test. Toggle the feature flag using a POST request to /api/feature-flags. Mock the tax service to return 8.5% tax…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed tenants A and B with isolated datasets. Install a widget from the remote registry for tenant A and verify it renders correctly in the dashboard. Drive per-widget WebSocket updates and confirm tenant A receives live…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright to run a three-phase test: (1) create a dataset with a unique tenant-scoped ID and verify isolation by querying tenant-scoped metadata; (2) intercept external data calls and return dete",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests data isolation, asynchronous ETL, eventual consistency, and idempotency in a real-world multi-tenant dashboard.\n\n## Key Concepts\n- Tenant isolation\n- Mocking external data sources\n- Idempotent operations\n- Eventual consistency and latency\n- Regional replication and failover\n\n## Code Example\n```javascript\n// Playwright-like sketch\ntest('dataset ETL end-to-end with tenant isolation', async ({page}) => {\n  const tenant = await createTenant();\n  const dsId = `ds-${Date.now()}`;\n  await loginAsTenantUser(tenant);\n  await page.goto('/datasets');\n  await page.fill('#name', dsId);\n  await page.click('#create');\n  // mock external sources and trigger ETL via API\n  await mockExternalDataForTenant(tenant, dsId);\n  await triggerETL(dsId);\n  await waitForDashboardData(tenant, dsId);\n  // idempotency check\n  await page.click('#create', {force:true});\n  await waitForDashboardData(tenant, dsId);\n});\n```\n\n## Follow-up Questions\n- How would you validate data correctness if ETL shows partial results due to a region outage?\n- What metrics would you collect to monitor test determinism across runs?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-22",
        "questionId": "q-16008",
        "question": "Design an end-to-end test for a model-serving platform that routes REST/gRPC requests to different ML models based on metadata and feature flags. Include canary shifts, cold starts, model warmup, accuracy/latency SLAs, rolling back on data drift, and end-user impact checks across multi-tenant namespaces. Specify data seeds, traffic profiles, observability assertions, and failure injections?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: simulate real traffic with two models, route by tenant metadata and feature flags, implement a canary ramp (0→50→100%), verify latency targets and accuracy SLAs, and ensure non-canary reques",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create two isolated test users; mock the flag service to return enabled for user A and disabled for user B. Write two E2E tests: (1) enabled path asserts the onboarding tour renders all steps and comp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed a user progress on mobile, trigger the migration to the new schema, resume on web, and assert the latest progress is preserved. Then replay the same events during a simulated regional outage to p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement with Playwright for UI, REST calls to provision tenant, and DB probes for isolation. Steps: (1) create tenant via API with unique tenantId; (2) launch onboarding UI scoped to tenant; (3) sim",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests end-to-end validation of a ML model-serving platform, including routing decisions by metadata/flags, canary deployments, cross-tenant isolation, and failure scenarios, ensuring performance, accuracy, and rollback safety in production-like conditions.\n\n## Key Concepts\n\n- E2E validation of routing logic with feature flags\n- Canary rollout and rollback strategies\n- Multi-tenant data isolation and seed generation\n- Observability: metrics, traces, logs, drift/latency checks\n- Fault injection: cold starts, network delays, partial outages\n\n## Code Example\n\n```javascript\n// Pseudo-test outline (high level)\nasync function testCanaryRouting() {\n  // seed data per tenant\n  // send mixed traffic with flag-enabled/disabled\n  // assert model A receives X% of traffic, latency < Y\n  // trigger drift condition and verify rollback\n}\n```\n\n## Follow-up Questions\n\n- How to parameterize across tenants and regions?\n- How to validate drift thresholds without exposing PII?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-23",
        "questionId": "q-28731",
        "question": "Design an E2E test for a multi-tenant image-labelling workspace where tenants A and B upload image sets; a background ML service labels images asynchronously. The UI shows per-tenant projects with status, labels, and audit logs. Your test should verify tenant isolation (A cannot see B's data), asynchronous processing with deterministic ML responses, access control (admin can cancel jobs while regular users cannot), and idempotent re-upload does not spawn duplicates. Propose tooling (Playwright or Cypress), data seeding, network mocks, and assertions; show how to implement cross-tenant session isolation and eventual consistency checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement the test with Playwright, driving a single-page checkout. Seed test users with a canary segment, mock the admin API to toggle newCheckout and canaryPercent, and intercept requests to",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright with two browser contexts (Tenant A, Tenant B). Seed tenants with distinct projects. Mock ML service to return fixed labels; poll project status until 'Completed' within timeout. Verify",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Seed two tenants with distinct data; run two parallel Playwright contexts wired to a mock WebSocket bus publishing tenant-specific events; simulate token expiry with a short-lived JWT and refresh; inj",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement an end-to-end test that seeds per-tenant loan data via API, navigates a mobile web loan flow in the frontend, simulates credit check latency with network throttling, and triggers a partial o",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests cross-tenant isolation and asynchronous processing in a data-heavy UI, a realistic pattern for enterprise platforms.\n\n## Key Concepts\n\n- Tenant data isolation across sessions\n- Asynchronous job guarantees with deterministic mocks\n- Role-based access control and audit trail\n- Idempotent operations and data seeding\n\n## Code Example\n\n```javascript\n// Example of Playwright setup highlights\n```\n\n## Follow-up Questions\n\n- How would you adapt this to mass dataset uploads?\n- How would you test partial failures of the ML service?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-24",
        "questionId": "q-25438",
        "question": "Design a beginner-friendly E2E test for a 'Drafts and Publish' flow in a social media web app. The user writes a post in a rich text editor, autosaves to localStorage every 15 seconds, reload preserves the draft, and publishing pushes to the profile and main feed with a timestamp. Specify tooling (Cypress or Playwright), seed a test user, mock the publish API, ensure test isolation, and assert that after publish the draft is cleared and the new post appears in both feed and profile?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Describe a deterministic E2E scenario that floods the API with 200 orders, validates inventory decrement and order creation across eventual-consistency windows (≤60s), and ensures idempotent retries f",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Create three Playwright contexts simulating users A, B, C editing the same doc (docId=<UNIQUE_DOC_ID>) with WebSocket sync. Validate live presence cursors propagate in near real time, edits merge dete",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright to drive three browser contexts; mock data channels with a WebRTC shim; seed board state; verify near real-time sync (<200ms) via observed DOM events; simulate disconnects and online re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed a test user, mock POST /api/posts, compose a draft in the rich text editor, wait 15 seconds to confirm localStorage autosave persistence, reload the page to verify draft restoration, click publish, then assert the…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes a beginner-friendly yet realistic E2E scenario: draft persistence, UI interactions, and end-to-end verification across multiple pages (feed and profile), with API mocking and test data isolation.\n\n## Key Concepts\n\n- LocalStorage persistence and autosave behavior\n- End-to-end API mocking with intercept/stub\n- Test data seeding and isolation\n- Cross-page assertions and state verification\n\n## Code Example\n\n```javascript\n// Cypress example\ndescribe('Drafts and Publish', () => {\n  beforeEach(() => {\n    // Seed test user\n    cy.request('POST', '/api/test/setup', { user: 'test-user' }).then(() => {\n      // Mock the publish API\n      cy.intercept('POST', '/api/posts', {\n        statusCode: 201,\n        body: { id: '123', status: 'published' }\n      }).as('publishPost');\n    });\n    cy.visit('/compose');\n  });\n\n  it('autosaves draft, preserves on reload, and publishes to feed and profile', () => {\n    // Compose a draft\n    cy.get('.rich-text-editor').type('My new post draft');\n    \n    // Wait for autosave (15 seconds)\n    cy.wait(15000);\n    cy.window().then(win => {\n      expect(win.localStorage.getItem('draft')).to.exist;\n    });\n\n    // Reload and verify draft restoration\n    cy.reload();\n    cy.get('.rich-text-editor').should('contain', 'My new post draft');\n\n    // Publish the post\n    cy.get('[data-testid=\"publish-btn\"]').click();\n    cy.wait('@publishPost');\n\n    // Verify draft is cleared\n    cy.window().then(win => {\n      expect(win.localStorage.getItem('draft')).to.be.null;\n    });\n\n    // Verify post appears in main feed\n    cy.visit('/feed');\n    cy.contains('My new post draft').should('be.visible');\n\n    // Verify post appears on profile\n    cy.visit('/profile/test-user');\n    cy.contains('My new post draft').should('be.visible');\n  });\n});\n```\n\n## Tool Selection\n\n**Cypress** is recommended for beginners due to its intuitive API, excellent debugging tools, and automatic waiting for elements and commands.\n\n## Test Isolation\n\nEach test runs in a fresh browser context with a clean localStorage state, ensuring no data leaks between tests.",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-06-24T06:07:43.102Z",
    "version": 1
  }
]