[
  {
    "id": "test-e2e-testing",
    "channelId": "e2e-testing",
    "channelName": "E2E Testing",
    "title": "E2E Testing Knowledge Test",
    "description": "Test your understanding of E2E Testing with 25 questions.",
    "questions": [
      {
        "id": "e2e-testing-q-0",
        "questionId": "q-460",
        "question": "You're testing a login form with Playwright. The form has email and password fields, and a submit button. How would you write a basic E2E test to verify successful login and redirect to dashboard?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use two parallel Playwright pages seeded with the same document under one tenant. Mock autosave to deterministic intervals, inject latency to trigger OT/CRDT conflicts, and verify final content conver",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright's test() function with page.locator() to find elements. Fill credentials with fill(), click submit, then waitForURL() or expect(page.url()).toContain('/dashboard'). Add assertions for dashboard elements.",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I'd implement a multi-session Playwright test that opens two pages simulating two users editing in parallel, use a mock WebSocket server to deterministically replay edits, fixture per-test doc, introd",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a test that opens the booking flow, selects a map pin, searches an address, completes multi-step checkout, and asserts a single booking is created. Then simulate offline, trigger retries on re",
            "isCorrect": false
          }
        ],
        "explanation": "## Key Concepts\n- E2E testing simulates real user interactions\n- Playwright provides cross-browser automation\n- Test structure: Arrange-Act-Assert pattern\n\n## Implementation Steps\n- Navigate to login page\n- Locate form elements using selectors\n- Fill input fields with test data\n- Submit form and wait for response\n- Verify redirect and dashboard content\n\n## Best Practices\n- Use data-testid attributes for stable selectors\n- Implement proper waiting strategies\n- Handle async operations with await\n- Clean up test data after execution",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-1",
        "questionId": "q-27490",
        "question": "Design a beginner-friendly E2E test for a feature-flag gated onboarding flow where a 'NewOnboard' flag controls whether users see the new onboarding steps or the legacy steps. Include how to seed the flag, mock the flag service, select tooling (Cypress or Playwright), verify both paths, ensure progress persists across navigation, and test isolation between runs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed a test flag in config to toggle NewOnboard on and off; mock the feature-flag service so both paths can be tested deterministically. Use Playwright or Cypress to drive signup and onboarding; verif",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Outline a deterministic test plan: seed three tenants with distinct data residency rules; trigger a canary rollout followed by a 50% rollout; verify that within 60 seconds all 120 services observe the new flag state and…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright to run two isolated browser contexts. Seed a list with 3 items. Put both offline, perform edits A and B (rename an item, add an item) then go back online; trigger sync. Validate the mer",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Playwright with route interception to seed the chat state, post a new message, delete it, and confirm the UI hides the item while an Undo toast appears. Trigger Undo to restore the original messag",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for feature flags and onboarding flows are common and brittle; this question checks planning for deterministic paths and test isolation.\n\n## Key Concepts\n- Feature flags and deterministic seeding\n- Mocking external flag services\n- UI path validation for multiple branches\n- Test isolation and data reset between runs\n\n## Code Example\n```javascript\n// Example: stub flag service in Playwright tests\n// await page.route('**/flag-service**', req => req.respond({ status: 200, body: JSON.stringify({ NewOnboard: true }) }));\n```\n\n## Follow-up Questions\n- How would you measure flakiness and retry strategies for this test?\n- How would you extend this to add a third path for another experiment flag?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-2",
        "questionId": "q-18153",
        "question": "Design a beginner-friendly E2E test for an offline-first note-taking web app that syncs across devices when connectivity is restored. Include: simulate offline mode, create and update notes while offline, then restore connectivity and trigger a sync, resolve conflicts with a simple last-writer-wins strategy, ensure no duplicate notes, and verify cross-device consistency within 15 seconds. Specify tooling (Playwright or Cypress), data seeding, and how you would verify success?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright. Seed two devices with the same notebook. Simulate offline on both pages, create/update notes while offline, then restore network and trigger a sync. Assert a single version per note an",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Drive a 3-step onboarding wizard with Cypress. Before each run clear localStorage, seed test data, and intercept POST /api/create-account to return a deterministic id (user-123). Validate required fie",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Outline approach: seed tenants/channels; spin up 3 regions with presence and messaging services; simulate 1000 clients joining a channel; enable moderation flag in region A and gradually roll to B/C;",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed a test user, mock the email provider to capture the magic link, request the login, click the link, and confirm a session starts and lands on the dashboard. Validate token single-use, 15-minute ex",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for offline-first apps cover real-world reliability with intermittent connectivity. A beginner-friendly task focuses on practical steps rather than abstract concepts.\n\n## Key Concepts\n- Offline simulation and network throttling\n- Data seeding across devices\n- Conflict resolution (last-writer-wins)\n- Idempotent, duplicate-free sync\n- Timing constraints for visible convergence\n\n## Code Example\n```javascript\n// Playwright sketch (pseudo)\n// seedNotebook(); page.setOffline(true); await createNoteOffline('A');\n// page.setOffline(false); await triggerSync();\n```\n\n## Follow-up Questions\n- How would you scale to 3+ devices?\n- How would you verify conflicts when edits occur almost simultaneously?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-3",
        "questionId": "q-8935",
        "question": "Design an E2E test for a multi-tenant analytics platform where dashboards pull data from a Kafka streaming pipeline with a real-time processor that materializes per-tenant views. The test must verify strict tenant data isolation, seedable data, simulate Kafka partition rebalances and consumer restarts, guarantee exactly-once processing for late events, and validate dashboard stability across time windows. Include tooling choices, seeds, and verification methods?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: seed per-tenant events in Kafka, run the real-time processor with a deterministic clock, trigger partition rebalances and consumer restarts, and assert exactly-once writes to per-tenant materialized views while va…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Seed a Lead in Salesforce; trigger create/update; verify CDC event propagates to Kafka and the Oracle upsert applies with correct field mappings. Check dedup on replay and that PII is masked in audit",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright. Seed User A with cohort 0 and User B with cohort 1. Mock the remote config API to return a 10% rollout for cohort 1. Load the home page and verify that cohort 0 sees the old composer while cohort 1 sees…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed Tenant A with a user u1 as viewer and verify access to /dashboard; ensure /user-management and /billing are blocked (403 or hidden). Upgrade u1 to editor via admin UI, reload, and confirm access",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests ability to design E2E tests for streaming, multi-tenant isolation, and data correctness under dynamic conditions like rebalances and late events. It also probes how the candidate picks tools, seeds data, and defines verifications across event store and UI.\n\n## Key Concepts\n\n- End-to-end testing of event-driven, multi-tenant systems\n- Exactly-once processing, deduplication, late events\n- Kafka partition rebalances, consumer restarts\n- Per-tenant data isolation and dashboard verification\n- Test data seeding and deterministic time windows\n\n## Code Example",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-4",
        "questionId": "q-9804",
        "question": "Design an end-to-end test for a real-time collaborative document editor embedded in a SaaS app. Use two browser sessions to simulate concurrent edits on the same document, verify live updates under injected latency, CRDT/OT reconciliation, and persistence after save; cover offline/reconnect paths, data isolation, and deterministic ordering; specify tooling (Playwright or Cypress) and how you orchestrate test data?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design an E2E test suite for the 'NewUserTour' feature-flag gated onboarding flow using Playwright. The test should: 1. **Mock the Feature Flag Service**: Intercept API calls to return controlled flag states. 2. **Seed…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test plan for streaming inference: seed deterministic inputs; run three deployment variants (streaming on, off, rolling). Connect a client via WebSocket, verify in-order chunk delivery, end-to-end lat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed a user and cart in the test DB, stub the feature flag service to return true then false, and navigate to checkout. Verify the UI shows Fast Checkout when the flag is ON and Standard Checkout when",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two Playwright contexts simulate concurrent edits on the same document. Inject latency to test real-time delta propagation, then validate CRDT reconciliation yields final content that includes both users' changes with p…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes proficiency with real-time systems in E2E tests, including multi-user concurrency, latency handling, offline scenarios, and data isolation. It reveals how you design deterministic, reproducible tests for complex client-server interactions.\n\n## Key Concepts\n- Real-time collaboration testing\n- CRDT/OT reconciliation validation\n- Multiple browser contexts orchestration\n- Latency injection and offline/reconnect paths\n- Data isolation and deterministic ordering\n- Test data management and cleanup\n\n## Code Example\n```javascript\n// Playwright scaffold (pseudo)\nconst docId = `test_${Date.now()}`\nconst context1 = await browser.newContext()\nconst page1 = await context1.newPage()\nconst context2 = await browser.newContext()\nconst page2 = await context2.newPage()\n\n// Setup: both users open same document\nawait page1.goto(`/editor/${docId}`)\nawait page2.goto(`/editor/${docId}`)\n\n// Concurrent edits with latency injection\nawait page1.route('**/socket.io/**', route => {\n  setTimeout(() => route.continue(), 100) // Inject 100ms latency\n})\n\nawait page1.type('[contenteditable]', 'User 1 text')\nawait page2.type('[contenteditable]', 'User 2 text')\n\n// Verify real-time updates and CRDT reconciliation\nconst finalContent1 = await page1.textContent('[contenteditable]')\nconst finalContent2 = await page2.textContent('[contenteditable]')\nexpect(finalContent1).toBe(finalContent2) // Deterministic final state\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-5",
        "questionId": "q-19514",
        "question": "Design an end-to-end test for a geo-distributed, offline-first food delivery app. Validate cart persistence and order submission when device is offline, then returns online, verify real-time driver updates via WebSocket, handle intermittent connectivity with service workers and background sync, ensure idempotent order placement and proper retry/backoff, and confirm end-to-end ledger reconciliation. Include seeds, mocks, and determinism?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set up a Cypress test that runs on a mobile viewport (375x812). Seed test data for a city with 3 listings, mock the payment gateway, and stub external APIs. Steps: search city, apply price/filter, pic",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a Playwright-based E2E harness with mobile emulation and an offline/online toggle. Seed catalog and region with known drivers. Simulate offline cart edits, then go online to submit. Verify final c",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright with two tenant seeds: one flag on, one flag off. Intercept flag service and analytics calls; assert the onboarding UI appears only for the on-flag tenant. Validate backend routing and",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a centralized flag service with per-shard seeds; run parallel E2E runs across web/mobile. Validate 0/5/25/100% canaries by asserting UI states, auth validity, cart totals, checkout flows, and an",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests offline-first UX, real-time updates, and end-to-end data integrity in a geo-distributed delivery platform. It probes how you model network partitions, state synchronization, and failure handling across services.\n\n## Key Concepts\n- Offline-first: service workers, background sync, localStorage/IndexedDB\n- End-to-end data integrity: idempotency, retries, reconciliation\n- Real-time: WebSocket/MQTT driver streams\n- determinism: mocks, seeds, time control\n\n## Code Example\n```javascript\n// Playwright sketch to toggle offline and submit\nconst context = await browser.newContext({ viewport: { width: 360, height: 800 } });\nconst page = await context.newPage();\nawait page.goto('https://example.app');\nawait context.setOffline(true);\nawait page.click('#add-to-cart');\nawait context.setOffline(false);\nawait page.click('#checkout-button');\nawait page.waitForSelector('#order-confirmation');\n```\n\n## Follow-up Questions\n- How would you validate exact ledger reconciliation across regional databases?\n- How would you model flaky network conditions to ensure deterministic tests?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-6",
        "questionId": "q-19137",
        "question": "Design an end-to-end test plan for a real-time collaborative document editor (multi-user, presence, offline edits, CRDT convergence). Outline how you'd simulate multiple clients in parallel, network partitions, offline mode, and eventual consistency checks; specify tooling (Playwright), test data isolation, and how to validate conflict resolution and deterministic merge?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design two E2E tests in Playwright to cover both flag states. Seed the cart and user via API before each test. Toggle the feature flag using a POST request to /api/feature-flags. Mock the tax service to return 8.5% tax…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed tenants A and B with isolated datasets. Install a widget from the remote registry for tenant A and verify it renders correctly in the dashboard. Drive per-widget WebSocket updates and confirm tenant A receives live…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a Playwright-based E2E with three browser contexts (A,B,C) editing the same document. Use a mocked presence/CRDT service to simulate joins, offline edits, and merges. Steps: open doc in all co",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Seed 100 riders/drivers; publish events RideRequested, DriverAssigned, InTransit, Arrived, Paid to Kafka with intentional reordering and delays. Validate idempotent handlers and exactly-once ledger wr",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests real-time collaboration, presence, offline resilience, and CRDT/OT convergence across clients.\n\n## Key Concepts\n- Real-time collaboration, presence, CRDT/OT, offline mode\n- Parallel browser contexts and deterministic test data\n- Network partition handling and merge correctness\n\n## Code Example\n```javascript\n// Playwright sketch\nconst { chromium } = require('playwright');\n// Create 3 contexts, navigate to doc, perform edits, simulate offline, validate convergence\n```\n\n## Follow-up Questions\n- How would you isolate test data to avoid cross-test interference?\n- How would you handle flaky timing in presence updates?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-7",
        "questionId": "q-24729",
        "question": "Design an end-to-end test for a multi-tenant feature-flag system with global defaults, per-tenant overrides, and rapid toggling. Test across UI, API, and background workers, simulating tenants in 3 regions and concurrent toggles. Specify seeds, test isolation, propagation delay checks, idempotency, and verification of correct states under load?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use two Playwright browser contexts to simulate collaborative editing of the same document with offline-first support. Implement network conditioning to introduce latency and simulate offline periods, then restore conne…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement an E2E test in Playwright that seeds Accounts, Contacts, Opportunities; triggers a background export for a user with Export permission; asserts progress via WebSocket (Queued → Processing →",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright to drive three browser contexts; mock data channels with a WebRTC shim; seed board state; verify near real-time sync (<200ms) via observed DOM events; simulate disconnects and online re",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a mock flag store with per-tenant precedence (override > regional default > global default). Seed 200 tenants across 3 regions, perform concurrent toggles and simulate propagation latency, verify",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design end-to-end tests for multi-region, multi-tenant feature flags, covering UI, API, and background workers, with rapid toggling and race conditions. Demonstrates seeds, isolation strategies, and SLA verification.\n\n## Key Concepts\n\n- Feature flag precedence and propagation across layers\n- Cross-region consistency (UI, API, workers)\n- Concurrency, load testing, and idempotency\n\n## Code Example\n\n```javascript\n// Pseudo-test scaffold for E2E feature-flag toggles\nasync function testFlagToggle(tenantId, flagKey, value) {\n  await api.setFlag(tenantId, flagKey, value);\n  await wait(50);\n  const uiState = await ui.getFlagState(tenantId, flagKey);\n  const workerState = await worker.getFlagState(tenantId, flagKey);\n  if (uiState !== value || workerState !== value) throw new Error('Inconsistent state');\n}\n```\n\n## Follow-up Questions\n\n- How would you measure cross-region propagation latency? \n- How do you handle partial failures (region down) and ensure eventual consistency?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-8",
        "questionId": "q-17118",
        "question": "Design a beginner-friendly E2E test for an offline-first e-commerce checkout flow: a user adds items to the cart, proceeds to checkout, loses network during payment, service worker caches cart and shows an offline state, then regains connectivity and completes payment. Include tooling (Cypress or Playwright), network throttling, data isolation, and how you verify persistence and idempotent order creation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Seed 100 events to a Kafka topic, execute the Flink streaming job in a test cluster, and verify Delta Lake on S3 contains 100 validated records. Call the analytics API and confirm the expected aggrega",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "In your E2E test, simulate a user adding items to the cart, proceeding to checkout, initiating payment, then simulating a network failure to verify the cart persists in localStorage and the offline UI displays correctly…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Seed tenants A and B with distinct data; in parallel, perform writes/reads for both tenants under burst load; verify queries return only per-tenant data and no leakage; ensure Redis keys are tenant-pr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed a document with initial content, launch two Playwright browser contexts in parallel to simulate distinct users, implement offline mode by disabling network connectivity to allow isolated edits, reconnect clients to…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nOffline-first reliability with service workers is a critical requirement in modern e-commerce applications. This question evaluates whether a candidate understands how to test caching behavior, connectivity state changes, and retry logic in end-to-end tests.\n\n## Key Concepts\n- Offline-first UX patterns with service workers\n- Network throttling and failure simulation (Cypress or Playwright network interception)\n- Test data isolation with fixtures or unique identifiers\n- Idempotent order placement to prevent duplicate charges\n\n## Code Example\n```javascript\n// Example: Intercept network requests to simulate offline behavior\ncy.intercept('POST', '/api/payment', (req) => {\n  req.reply({ statusCode: 503, body: { error: 'Network error' } });\n}).as('paymentRequest');\n\n// Simulate going offline\ncy.networkIdle(); // or use Cypress offline plugin\ncy.window().then((win) => {\n  win.navigator.onLine = false;\n});\n\n// Verify offline UI and localStorage persistence\ncy.get('[data-testid=offline-banner]').should('be.visible');\ncy.getLocalStorage('cart').should('exist');\n\n// Restore network and retry\ncy.networkOn();\ncy.wait('@paymentRequest').then(() => {\n  cy.get('[data-testid=order-confirmation]').should('be.visible');\n});\n\n// Verify idempotent order creation\ncy.get('@paymentRequest').its('response.statusCode').should('eq', 200);\n```",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-9",
        "questionId": "q-21235",
        "question": "Design an end-to-end test for a fintech onboarding flow where a user signs up, passes KYC via an external service, and an eligibility event is published to a multi-region event bus. The UI must reflect live status across devices. Include test data isolation, deterministic external API mocks, idempotency checks on signup, eventual consistency verification, and regional outage handling. Use Cypress or Playwright and outline orchestration?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Mock the external KYC API and the event bus to deterministic endpoints; isolate data per test tenant; verify idempotent signup with the same user_id; test eventual consistency across regional replicas",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Trigger the flow via Salesforce REST API to simulate lead conversion, emit a webhook to a test endpoint, and verify Snowflake incremental load using CDC streams and a stable timestamp. Use per-tenant",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "To design a comprehensive end-to-end test for real-time collaborative documents, I would implement the following approach: First, seed a test document with deterministic content across multiple regions. Then initialize…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed a user with onboarding incomplete. Mock the feature flag config API to return false, load the onboarding page and verify restricted UI. Then flip the flag to true via API, resume and complete onb",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for onboarding across regions and async services are common in fintech; this question probes end-to-end reliability, data isolation, and failure handling.\n\n## Key Concepts\n- End-to-end testing with external dependencies\n- Event-driven architecture and eventual consistency\n- Data isolation and idempotency\n- Regional failure scenarios and recovery\n- Test determinism via mocks\n\n## Code Example\n```javascript\n// Playwright skeleton: intercept KYC and mock responses; drive onboarding flow\nimport { test, expect } from '@playwright/test';\ntest('onboarding E2E with mocked KYC and event bus', async ({ page }) => {\n  await page.route('**/kyc/*', r => r.fulfill({ status: 200, body: JSON.stringify({ ok: true }) }));\n  await page.route('**/event-bus/*', r => r.fulfill({ status: 202 }));\n  await page.goto('https://fintech.example/onboard');\n  await page.fill('#email','user@example.com');\n  await page.click('#submit');\n  await expect(page.locator('#status')).toHaveText(/pending|kyc_passed|approved/);\n});\n```\n\n## Follow-up Questions\n- How would you scale this to thousands of concurrent onboarding flows?\n- How would you validate idempotency across multiple regions and tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-10",
        "questionId": "q-1893",
        "question": "Design an end-to-end test for a multi-tenant data-export feature: an admin triggers an export via the UI; the export runs in the background, reads tenant-scoped data from multiple services, writes to blob storage, and emails a download link. How would you verify tenant isolation, idempotent retries, and resilience to blob outages without cross-tenant leakage? Include concrete tooling choices and test data strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger export as admin; poll status via API; verify blob contains only data for the tenant and matches expected row counts; then re-run to confirm idempotent results; simulate blob outage to test ret",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Seed three tenants via API with distinct regional tax configs. Run an E2E flow inside one tenant: signup, add to cart, checkout, tax calc, and ledger entry. Verify per-tenant data isolation, audit log",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement an E2E test in Playwright that seeds Accounts, Contacts, Opportunities; triggers a background export for a user with Export permission; asserts progress via WebSocket (Queued → Processing →",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a cross-platform suite (Playwright + Appium) with seeded content and a deterministic playback validator. Use ABR traces, captions timing checks, and ad-insertion alignment across clients; run",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests spanning multi-tenant boundaries and asynchronous export flows surface data isolation and resilience issues. This question probes end-to-end coverage for real-world SaaS data export use cases like Airbnb and Microsoft enterprise products.\n\n## Key Concepts\n- Multi-tenant data isolation across services\n- Async background jobs and eventual consistency\n- End-to-end validation of blob storage and email delivery\n- Idempotent retries and fault injection\n- Test data strategy and environment isolation\n\n## Code Example\n```javascript\n// Example test outline (Playwright + API stubs)\ntest('tenant-scoped data export end-to-end', async ({ page, api, storage }) => {\n  await loginAsAdmin(page);\n  await page.click('#start-export');\n  await page.waitForResponse(r => r.url.includes('/exports/').and(r.status() === 200));\n  const status = await api.get(`/exports/${tenantId}/status`);\n  expect(status).toHaveProperty('state', 'COMPLETED');\n  const blob = await storage.getBlob(`exports/${tenantId}/data.json`);\n  expect(blob).toContain(`\"tenantId\":\"${tenantId}\"`);\n  // idempotence\n  await page.click('#start-export');\n  const status2 = await api.get(`/exports/${tenantId}/status`);\n  expect(status2).toHaveProperty('state', 'COMPLETED');\n});\n```\n\n## Follow-up Questions\n- How would you parameterize test data for tenants with different data shapes?\n- How would you enforce data privacy in the test environment while validating exports?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-11",
        "questionId": "q-10245",
        "question": "Design an E2E test for a real-time, multi-tenant collaborative document editor: two users edit the same document in parallel sessions; verify tenant isolation, deterministic autosave, and eventual convergence with OT/CRDT conflict resolution, under simulated network latency and intermittent disconnects. Outline tooling (Playwright or Cypress), data seeding, and idempotent publish of final content?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would implement an automated test harness that instantiates N concurrent rooms with 50 synthetic participants per room, under varied network profiles including latency, jitter, and packet loss. The harness captures We…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use two parallel Playwright pages seeded with the same document under one tenant. Mock autosave to deterministic intervals, inject latency to trigger OT/CRDT conflicts, and verify final content conver",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright. Seed 12-20 transactions via API before test. Go to Transactions -> Export, start CSV export; mock backend job so it completes quickly or provide a status endpoint. Intercept the downlo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an end-to-end test for a multi-tenant streaming analytics platform where tenants ingest real-time events, dashboards update over WebSocket, and data must be strictly isolated per tenant across cache and storage.…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-time collaboration and multi-tenant data isolation under flaky networks, a common production risk.\n\n## Key Concepts\n\n- End-to-end testing with concurrent sessions\n- Real-time collaboration conflict resolution (OT/CRDT)\n- Data isolation per tenant\n- Deterministic test scaffolding and latency injection\n- Idempotent save semantics\n\n## Code Example\n\n```javascript\n// Pseudo Playwright setup illustrating two pages\nconst [pA, pB] = await Promise.all([context.newPage(), context.newPage()]);\nawait pA.goto('/doc/123');\nawait pB.goto('/doc/123');\nawait pA.fill('#cell', 'Hello');\nawait pB.fill('#cell', 'World');\n```\n\n## Follow-up Questions\n\n- How would you adapt this test for a transient outage where the doc becomes temporarily unavailable?\n- What metrics would you collect to validate convergence guarantees?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-12",
        "questionId": "q-8132",
        "question": "Design an E2E test for a real-time collaborative editor in a SaaS app where 3-5 users concurrently edit a shared document over WebSocket. Include how you verify CRDT/OT conflict resolution, autosave to a durable store, latency measurements, and test data isolation/cleanup per document?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a comprehensive end-to-end test for a feature-flag gated onboarding flow. Seed test user data with consistent identifiers, mock the admin API endpoints to deterministically control the onboarding flag state, a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Trigger a GDPR deletion for a synthetic user across services; verify purge of PII in cart, catalog, checkout, loyalty, and analytics; validate DBs, caches, and search indexes show no PII; confirm audi",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Test a real-time collaborative editor with 3 browser contexts editing one document via WebSocket. Emit concurrent edits, verify CRDT/OT conflict resolution, autosave cadence to the durable store, and",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Seed a test user, then call the flag API to enable CompactDashboard for that user. Log in as that user and verify the UI renders the compact dashboard (check for a specific element). Reload to confirm",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for real-time collaboration across multiple clients require reasoning about concurrency, conflict resolution, and fault handling in production-like conditions.\n\n## Key Concepts\n\n- WebSocket end-to-end flows across clients\n- CRDT vs OT conflict resolution guarantees\n- Autosave cadence and durability in a shared store\n- Test data isolation and per-document teardown\n- Latency and network fault injection\n\n## Code Example\n\n```javascript\n// Pseudo-test harness for 3 clients editing same doc\nasync function testConcurrentEdits(docId){\n  const [c1,c2,c3] = await Promise.all([openClient(docId), openClient(docId), openClient(docId)]);\n  await c1.edit('A');\n  await c2.edit('B');\n  await c3.edit('C');\n  await wait(500);\n  const finalContent = await getDocContent(docId);\n  // expect CRDT-merge result reflects A,B,C edits\n}\n```\n\n## Follow-up Questions\n\n- How would you extend for offline-first users?\n- How would you validate autosave durability after a server restart?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-13",
        "questionId": "q-10292",
        "question": "Design an end-to-end test for an offline-first real-time chat in a Slack-like app. The test should cover optimistic UI, offline message queuing, retry on reconnect, cross-device presence, and message ordering persistence. Specify tooling (Playwright or Cypress), test data isolation, network virtualization, and deterministic backend mocks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: Use Playwright with two isolated browser sessions (UserA and UserB) to test offline-first real-time chat functionality. Steps: 1) Initialize both users and establish baseline connection; 2) Simulate network lo…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Playwright or Cypress. Begin by seeding test data: products, locales, promos, and a mock payment gateway. Steps: 1) Add an item to cart, apply a promo code, then verify the updated total, tax, and currency display c…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a test harness with three simulated clients backed by a CRDT doc (e.g., Y.js). Steps: create a unique doc id, connect all clients, perform interleaved edits with offline periods, inject latency an",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design an end-to-end test using Playwright for web and Appium for mobile. Seed 3 time zones with recurring events; run 4 clients, simulate offline edits and a concurrent change; verify convergence wit",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for offline-first, real-time chat systems with presence and ordering demonstrate expertise in handling real-world distributed system challenges where network reliability cannot be guaranteed.\n\n## Key Concepts\n\n- Offline-first user experience with optimistic updates\n- Network virtualization for simulating connectivity scenarios\n- Eventual consistency and message deduplication\n- Cross-device state synchronization\n- Message ordering and persistence guarantees\n- Deterministic backend mocking for reproducible test conditions\n\n## Code Example\n\n```javascript\n// Example tes",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-14",
        "questionId": "q-28644",
        "question": "Design a beginner-friendly E2E test for a feature-flag gated onboarding in a SaaS web app. The onboarding path is chosen by a remote 'NewUserTour' flag; tests must cover OFF and ON states, regional variations, data seeding, mocked feature-flag service, UI-step assertions, and analytics events. Verify mid-session flag toggling doesn't regress and onboarding completes only when ON?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright to drive the flow. Seed a user with first_login and an injectable feature flag endpoint returning onboarding_tour=true. Verify the 5-step tour renders modals, highlights elements, and N",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an E2E test suite for the 'NewUserTour' feature-flag gated onboarding flow using Playwright. The test should: 1. **Mock the Feature Flag Service**: Intercept API calls to return controlled flag states. 2. **Seed…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Seed 100 riders/drivers; publish events RideRequested, DriverAssigned, InTransit, Arrived, Paid to Kafka with intentional reordering and delays. Validate idempotent handlers and exactly-once ledger wr",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed tenants in isolated namespaces with synthetic customers and invoices, perform checkout via Playwright, verify invoice creation and ledger reconciliation across billing, tax, and journal services,",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates a candidate's ability to design deterministic end-to-end tests for complex user flows controlled by remote configuration. It demonstrates understanding of feature flag architecture, test data management, and handling asynchronous state changes. The question also probes awareness of telemetry validation and regression risks when configuration changes mid-session.\n\n## Key Concepts\n\n- **Feature Flags**: Remote configuration controlling feature availability without code deployment.\n- **Test Data Seeding**: Creating reproducible user states and regional variants.\n- **State Validation**: Verifying correct UI renders based on flag state.\n- **Analytics Integration**: Ensuring telemetry events fire accurately per business requirements.\n- **Regression Testing**: Confirming mid-session flag changes don't corrupt user state.\n\n## Code Example\n\n```javascript\n// Playwright E2E test for NewUserTour feature flag\ndescribe('NewUserTour Feature Flag', () => {\n  let mockFlagService;\n\n  beforeEach(() => {\n    mockFlagService = new MockFeatureFlagService();\n    mockFlagService.setFlag('NewUserTour', false);\n  });\n\n  test('OFF state: user bypasses onboarding', async ({ page }) => {\n    await page.goto('/dashboard');\n    await expect(page.locator('#onboarding-modal')).not.toBeVisible();\n  });\n\n  test('ON state: onboarding completes successfully', async ({ page }) => {\n    mockFlagService.setFlag('NewUserTour', true);\n    await page.goto('/dashboard');\n    await expect(page.locator('.onboarding-step-1')).toBeVisible();\n    await page.click('[data-testid=\"next-step\"]');\n    await expect(page.locator('.onboarding-step-2')).toBeVisible();\n    await page.click('[data-testid=\"complete-onboarding\"]');\n    await expect(page.locator('#onboarding-modal')).not.toBeVisible();\n  });\n\n  test('mid-session toggle: onboarding aborts when flag turns OFF', async ({ page }) => {\n    mockFlagService.setFlag('NewUserTour', true);\n    await page.goto('/dashboard');\n    await expect(page.locator('.onboarding-step-1')).toBeVisible();\n    mockFlagService.setFlag('NewUserTour', false);\n    await page.reload();\n    await expect(page.locator('#onboarding-modal')).not.toBeVisible();\n  });\n\n  test('regional variation: EU users see GDPR consent step', async ({ page }) => {\n    seedUser({ region: 'EU' });\n    mockFlagService.setFlag('NewUserTour', true);\n    await page.goto('/dashboard');\n    await expect(page.locator('.gdpr-consent-step')).toBeVisible();\n  });\n\n  test('analytics: events fire on step completion', async ({ page }) => {\n    mockFlagService.setFlag('NewUserTour', true);\n    const analytics = new MockAnalytics();\n    await page.goto('/dashboard');\n    await page.click('[data-testid=\"next-step\"]');\n    expect(analytics.events).toContainEqual(\n      expect.objectContaining({ name: 'onboarding_step_completed', step: 1 })\n    );\n  });\n});\n```\n\n## Evaluation Criteria\n\n- Correctly mocks the remote flag service\n- Covers both ON and OFF paths\n- Tests at least one regional variation\n- Validates UI state at each step\n- Confirms analytics events fire appropriately\n- Demonstrates handling of mid-session flag changes",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-15",
        "questionId": "q-10386",
        "question": "Design an end-to-end test plan for a feature-flag driven rollout of a critical UI revamp across web and mobile clients in a multi-service ecommerce platform. Include progressive rollout steps (0%, 5%, 25%, 100%), data isolation for test shards, cross-service consistency checks (auth, cart, checkout, analytics), randomized user segmentation, and automated rollback verification with drift detection when metrics diverge?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design two parallel tests for old and new checkout behind a feature flag, running across three regions with a mocked, deterministic flag service and a simulated propagation delay. Validate correct UI",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Adopt a centralized flag service with per-shard seeds; run parallel E2E runs across web/mobile. Validate 0/5/25/100% canaries by asserting UI states, auth validity, cart totals, checkout flows, and an",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright for end-to-end testing. The test uploads a 10,000-row CSV file, seeds required test data, and mocks the message bus to simulate partial failures with deterministic retries. Assert that the progress bar re…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Trigger DSAR for a test user; confirm removal from primary DBs, propagate deletion events to the event store, invalidate caches, and scrub materialized views after TTL; verify soft-delete tombstones a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests for feature-flag rollouts across services are common in large orgs; this checks end-to-end coverage, data isolation, and rollback control.\n\n## Key Concepts\n\n- Feature flag governance in distributed systems\n- Test shard data isolation\n- Consistency invariants across auth/cart/checkout/analytics\n- Drift detection and automated rollback\n\n## Code Example\n\n```javascript\n// Skeleton: Playwright test connecting to a feature flag service and asserting UI\nimport { test, expect } from '@playwright/test';\n\ntest('flagged UI renders consistently across shards', async ({ page, request }) => {\n  // setup: seed shard id and user\n  // enable flag for shard\n  // navigate and assert critical UI elements render\n});\n```\n\n## Follow-up Questions\n\n- How would you measure drift thresholds and alerting?\n- How do you handle flaky tests during rolled-out flags?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-16",
        "questionId": "q-8645",
        "question": "Design an E2E test for a multi-tenant analytics pipeline (Kafka -> processor -> warehouse). Use two tenants with isolated schemas. Seed and publish events to the streaming topic; verify per-tenant isolation in the warehouse and dashboards; exercise a replay API to re-ingest a window; simulate burst traffic to test backpressure; verify exactly-once processing and eventual consistency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Set up two tenants A and B with isolated schemas. Seed synthetic events, publish to the streaming topic, and trace through the processor to the warehouse. Validate per-tenant isolation in queries and",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement retry logic with exponential backoff, use test fixtures to mock slow responses, add explicit waits for network idle, and use test hooks to set up consistent test data. Combine with proper test isolation and co…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright to drive three headless browser clients simultaneously on the same document. Mock the WebSocket stream to inject deterministic real-time events and simulate offline edits with controlled timing. Isolate t…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Seed a test user, stub the export API to return a deterministic URL, simulate network latency in the wizard, verify autosave stores two-step progress in localStorage, trigger export once, intercept th",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a complex data path with tenancy, streaming, replay semantics, and backpressure. Probes data isolation, idempotency, and eventual consistency under failure modes.\n\n## Key Concepts\n\n- Data isolation across tenants\n- Exactly-once or idempotent processing\n- Replay/re-ingestion guarantees\n- Backpressure and burst handling in streaming stacks\n- End-to-end verification across warehouse and dashboards\n\n## Code Example\n\n```javascript\n// Example test hook outline\n```\n\n## Follow-up Questions\n\n- How would you validate schema evolution without cross-tenant impact?\n- How would you extend to more tenants and regional data residency?",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-17",
        "questionId": "q-10071",
        "question": "Design a beginner-friendly E2E test for a SaaS app with per-tenant feature flags that switch a checkout revamp flow on or off. Include how you seed tenants, how you toggle the flag via admin API, how you verify each tenant sees the correct flow, and how you detect and handle a flag change during an in-flight checkout. Propose tooling, data isolation, and determinism strategies?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Mock the external KYC API and the event bus to deterministic endpoints; isolate data per test tenant; verify idempotent signup with the same user_id; test eventual consistency across regional replicas",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Toggle payout feature flag to canary in region A, simulate payout request, and verify routing to region A services and final ledger entry. Promote to region B and repeat. Inject gateway timeouts and p",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Cypress to seed two tenants (A: flag ON, B: OFF). Log in as each tenant, complete checkout, and assert presence of NewFlow for A and OldFlow for B. Simulate flag change mid-checkout and verify gra",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a comprehensive end-to-end test plan leveraging a dedicated test harness that seeds 60 tenant contexts and executes concurrent onboarding workflows alongside production-simulated live usage actions. Include cross…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis asks for a practical, beginner-friendly E2E that exercises per-tenant feature flags, isolation, and race handling—core but often overlooked in onboarding. It tests determinism through API mocks and clean data seeding.\n\n## Key Concepts\n- Feature flags in E2E tests\n- Tenant data isolation\n- Deterministic test seeds and API mocks\n- Race conditions during in-flight flows\n- Audit/logging of flag state\n\n## Code Example\n```javascript\n// Example Cypress test skeleton\ndescribe('Checkout flag per tenant', () => {\n  it('verifies per-tenant flows', () => {\n    // seed tenants, toggle flags, run checkout, assert flows\n  })\n})\n```\n\n## Follow-up Questions\n- How would you scale this to many tenants and flags?\n- How would you handle non-deterministic third-party calls in tests?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-18",
        "questionId": "q-29961",
        "question": "Design a beginner-friendly E2E test for a delivery app where a runtime feature flag toggles a new checkout flow. When enabled, the checkout shows a 'Fast Checkout' path with a saved address and a one-click tip option; when disabled, the standard checkout is used. Use Cypress or Playwright, seed a user and cart, mock the feature flag service, verify the UI updates without a full page reload when the flag toggles, ensure cart persistence, and prevent duplicate orders if checkout is attempted under both paths?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test an end-to-end flow for a Salesforce-like storefront built with micro-frontends (Catalog, Cart, Checkout) controlled by feature flags. Run parallel across 3 regions with per-region data isolation.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright with API mocks: place a market order via UI; attach an idempotency-key header; mock broker to respond 429 then 200; assert only one broker call per key; verify UI shows PENDING then FIL",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed a user and cart in the test DB, stub the feature flag service to return true then false, and navigate to checkout. Verify the UI shows Fast Checkout when the flag is ON and Standard Checkout when",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Playwright or Cypress. Begin by seeding test data: products, locales, promos, and a mock payment gateway. Steps: 1) Add an item to cart, apply a promo code, then verify the updated total, tax, and currency display c…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests practical E2E with feature flags, UI path toggling, data persistence, and idempotency in a real-world delivery app context. It requires mocking a flag service and ensuring no duplicate orders when toggling mid-flow.\n\n## Key Concepts\n- Feature flags in E2E tests\n- Test data seeding for user/cart\n- Mocking external services\n- State persistence across navigation\n- Idempotency of order creation\n\n## Code Example\n\n```javascript\n// Example Playwright snippet\n// Setup: mock feature flag service to toggle\n```\n\n## Follow-up Questions\n\n- How would you extend the test to verify performance impact when the flag flips mid-flow?\n- What are potential flakiness risks with mid-flow toggling and how would you mitigate them?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-19",
        "questionId": "q-15864",
        "question": "Design an end-to-end test for an AI-powered data labeling platform with multi-tenant projects, where a live ML model provides label suggestions and human annotators can approve or override. Include data seeds, drift scenarios for the model, real-time collaboration on labeling tasks, audit logs, and verification of output integrity under high concurrency (e.g., 200 concurrent annotators)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright to seed the catalog and cart, drive a single checkout path, and toggle per-widget flags via a feature-flag service. Load 120 widgets through module federation in isolated frames, simula",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed per-tenant datasets with representative samples; drive the ML suggestion module with drift scenarios including concept drift, data drift, and model degradation; verify the complete end-to-end task lifecycle: create…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Implement with Playwright: three separate browser contexts for users A,B,C editing a shared doc. Simulate sequential and overlapping edits, check presence updates via the presence channel. Put one con",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly end-to-end test for a Databricks data ingestion pipeline: seed S3 with a small CSV dataset, trigger the batch job, validate the Delta Lake table schema and row count, enforce data quality rule…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question validates the candidate's ability to design comprehensive end-to-end tests for complex ML-in-the-loop systems. It tests understanding of multi-tenant isolation, data seeding strategies, ML model drift handling, real-time collaboration under load, and audit integrity.\n\n## Key Concepts\n- End-to-end testing of AI-assisted labeling workflows\n- Multi-tenant data isolation and access control verification\n- Model drift injection, detection, and graceful degradation\n- Audit log integrity, sequencing, and tamper-evidence\n- Concurrency control and conflict resolution in collaborative labeling\n\n## Code Example\n```javascript\n// Pseudo e2e test outline for AI labeling workflow\ndescribe('AI labeling E2E', () => {\n  it('handles full labeling lifecycle with ML suggestions', async () => {\n    // Seed tenant data\n    const project = await createProject({ tenantId: 'tenant-1' });\n    await seedLabelingData(project.id, SAMPLE_DATASET);\n    \n    // Inject model drift\n    await simulateModelDrift({ driftType: 'concept', severity: 'high' });\n    \n    // Create and process labeling task\n    const task = await createLabelingTask(project.id);\n    const suggestions = await getMLSuggestions(task.id);\n    \n    // Annotator workflow\n    const annotation = await approveSuggestion(task.id, suggestions[0].id);\n    expect(annotation.status).toBe('approved');\n    \n    // Verify audit integrity\n    const auditLog = await getAuditLog(task.id);\n    expect(auditLog).toBeSequenced();\n  });\n  \n  it('handles 200 concurrent annotators', async () => {\n    const annotators = await createAnnotators(200);\n    const results = await Promise.all(\n      annotators.map(a => a.submitAnnotation())\n    );\n    expect(results.conflictRate).toBeLessThan(0.01);\n  });\n});\n```",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-20",
        "questionId": "q-4930",
        "question": "You’re testing an admin-driven per-user feature flag in a SaaS app. The API toggles a 'CompactDashboard' flag for a specific user; on login, the UI should render the compact dashboard when the flag is on and the full dashboard when off. Design a beginner-friendly E2E test that seeds a test user, enables the flag via API, logs in, and asserts the compact UI; then disables and re-asserts the full UI. Include data isolation and reset steps?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a cross-device test using a shared central session store. Start playback on Smart TV at 120s, switch to mobile with the same session token, and simulate network handoffs (WiFi to cellular) a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Seed a test user, then call the flag API to enable CompactDashboard for that user. Log in as that user and verify the UI renders the compact dashboard (check for a specific element). Reload to confirm",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright with a mocked email service and a test inbox. Seed a test user, request a magic login link, intercept the outbound email, extract the URL, open it in the same browser context, verify su",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design two parallel tests for old and new checkout behind a feature flag, running across three regions with a mocked, deterministic flag service and a simulated propagation delay. Validate correct UI",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests for per-user feature flags are common in SaaS apps. This question checks the ability to seed data, perform API-driven state changes, and verify UI behavior across login and page reloads in a beginner-friendly way.\n\n## Key Concepts\n- Per-user feature flags and API seed/reset\n- End-to-end UI verification of dynamic layouts\n- Test isolation and deterministic CI runs\n\n## Code Example\n```javascript\n// Example using Playwright-like syntax (pseudocode)\n// seedUser(), setFlag(), login() are helper utilities\ntest('compact dashboard toggling', async ({ page, request }) => {\n  const { userId, email, password } = await seedUser(request);\n  await setFlag(request, userId, 'CompactDashboard', true);\n  await login(page, email, password);\n  await expect(page.locator('.dashboard-compact')).toBeVisible();\n  await page.reload();\n  await expect(page.locator('.dashboard-compact')).toBeVisible();\n  await setFlag(request, userId, 'CompactDashboard', false);\n  await page.reload();\n  await expect(page.locator('.dashboard-full')).toBeVisible();\n});\n```\n\n## Follow-up Questions\n- How would you guard against flaky UI assertions caused by caching?\n- How would you extend the test to cover multiple user segments (roles/regions) and parallel execution?",
        "difficulty": "beginner"
      },
      {
        "id": "e2e-testing-q-21",
        "questionId": "q-26298",
        "question": "Design an E2E test for a dynamic feature-flag rollout in a PayPal-like payments app: toggle a new instant-onboarding flow via API, verify only flagged tenants load the new UI, ensure backend routing and analytics events fire, simulate canary vs control, and guarantee rollback within 60 seconds across desktop and mobile?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright. Seed server with 2 notes. Set offline, create and edit a note, then go online. Assert local state matches server after sync and the UI shows a 'Synced' badge within 15s. Seed data, moc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use server flag fixtures to drive two cohorts: flagged users see the new onboarding, others see the old flow. In Playwright, route /api/flags to return true/false per test, seed user_id in localStorag",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Playwright with two tenant seeds: one flag on, one flag off. Intercept flag service and analytics calls; assert the onboarding UI appears only for the on-flag tenant. Validate backend routing and",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use two Playwright browser contexts to simulate collaborative editing of the same document with offline-first support. Implement network conditioning to introduce latency and simulate offline periods, then restore conne…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes concrete E2E strategies for feature flags in multi-tenant, latency-sensitive systems, ensuring isolation, observability, and fast rollback.\n\n## Key Concepts\n- Feature flags, canary rollout, rollback semantics\n- Multi-tenant data isolation and tenant-scoped routing\n- End-to-end with API mocks, network latency, and analytics verification\n- Cross-browser/mobile coverage\n\n## Code Example\n```javascript\n// Playwright snippet (conceptual)\ntest('feature-flag canary rollout', async ({ page }) => {\n  await page.route('**/api/flags/**', route => route.fulfill({ status: 200, body: JSON.stringify({ flag: true }) }));\n  // navigate to onboarding\n  await page.goto('/onboarding');\n  // assert canary UI visible only when flag on\n  const el = page.locator('#instant-onboard');\n  await expect(el).toBeVisible();\n});\n```\n\n## Follow-up Questions\n- How would you expand this to test concurrent flag changes during peak load?\n- How do you ensure no cross-tenant data leakage when the flag toggles?",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-22",
        "questionId": "q-449",
        "question": "How would you design an E2E testing strategy for a distributed edge computing platform that needs to validate functionality across 100+ global data centers with varying network conditions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a hierarchical testing approach with regional test orchestration hubs. Use Playwright with custom browser contexts to simulate different network conditions and geographic locations. Deploy test agents in major…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Seed 100 events to a Kafka topic, execute the Flink streaming job in a test cluster, and verify Delta Lake on S3 contains 100 validated records. Call the analytics API and confirm the expected aggrega",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Seed a fresh user, enable onboarding_v2 via API, launch the app, verify a 3-step wizard renders in order with Next/Back/Finish, complete it and verify a profile flag is set; then turn onboarding_v2 of",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use two isolated browser contexts (A and B) in Playwright. Seed a document, perform concurrent edits from both sessions simultaneously, and verify the final merged content reflects proper CRDT resolution. Then simulate…",
            "isCorrect": false
          }
        ],
        "explanation": "## Architecture\n- **Regional Test Hubs**: Deploy test orchestration in major geographic regions to minimize latency and maximize coverage\n- **Network Simulation**: Leverage browser APIs to dynamically throttle bandwidth and increase latency, accurately simulating real-world network conditions\n- **Parallel Execution**: Execute tests concurrently across regions with centralized coordination for efficient resource utilization\n\n## Key Components\n- **Test Agents**: Lightweight services that execute Playwright tests locally in each data center\n- **Result Aggregation**: Centralized service that collects, normalizes, and correlates test results from all regions\n- **Health Monitoring**: Real-time dashboard providing visibility into test coverage metrics and failure rates across the distributed infrastructure\n\n## Implementation Strategy\n```typescript\n// Regional test orchestration with network simulation\n```",
        "difficulty": "advanced"
      },
      {
        "id": "e2e-testing-q-23",
        "questionId": "q-16312",
        "question": "Design an end-to-end test for a collaborative document editor that supports real-time presence via WebSockets and autosave. The test should cover two users editing the same document, presence propagation, autosave timing, offline reconnection, and conflict resolution to ensure data integrity. Specify tooling (Playwright or Cypress), test data isolation, determinism via a mock clock, and how you verify eventual consistency across clients and the data store?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Trigger the flow via Salesforce REST API to simulate lead conversion, emit a webhook to a test endpoint, and verify Snowflake incremental load using CDC streams and a stable timestamp. Use per-tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Playwright with two isolated browser contexts editing the same document. Stub autosave to a fixed 2-second interval, mock the server clock, and simulate WebSocket presence events. Validate that live edits propagate…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Set up a Kafka test cluster using Testcontainers with per-tenant topics, seed 1,000 trades per minute with deterministic data, implement replay scripts to simulate late and malformed events, and use Playwright to verify…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan to mock the remote flag service to inject per-user segments. Use two test users: ON and OFF. Verify that with ON the checkout totals show the discount and the promo banner is visible; with OFF no",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\nThis question probes end-to-end orchestration of real-time collaboration, including WebSocket presence, autosave determinism, offline recovery, and conflict resolution. It also assesses test isolation strategies and how to model eventual consistency in tests.\n\n## Key Concepts\n- Real-time collaboration, presence, and WebSocket event handling\n- Autosave semantics and determinism via clock mocks\n- Offline/online recovery and conflict resolution strategies\n- Test data isolation and environment management\n- Observability: asserting store state and client views align\n\n## Code Example\n```javascript\n// Playwright test setup with two isolated contexts\nconst { test, expect } = require('@playwright/test');\n\ntest.describe('Collaborative Document Editor', () => {\n  let docId;\n\n  test.beforeEach(async ({ request }) => {\n    docId = `test-doc-${Date.now()}-${Math.random().toString(36).slice(2)}`;\n  });\n\n  test('real-time presence and autosave sync between two users', async ({ browser }) => {\n    const userA = await browser.newContext();\n    const userB = await browser.newContext();\n\n    const pageA = await userA.newPage();\n    const pageB = await userB.newPage();\n\n    await pageA.goto(`/doc/${docId}`);\n    await pageB.goto(`/doc/${docId}`);\n\n    // Verify presence indicators show both users\n    await expect(pageA.locator('.presence')).toContainText('User B');\n    await expect(pageB.locator('.presence')).toContainText('User A');\n\n    // User A types; User B should see it within 1-2 seconds\n    await pageA.locator('.editor').fill('Hello from User A');\n    await expect(pageB.locator('.editor')).toHaveValue('Hello from User A', { timeout: 3000 });\n\n    // Verify autosave to data store (mocked interval: 2s)\n    await pageA.waitForTimeout(2500);\n    const storedDoc = await request.get(`/api/docs/${docId}`);\n    expect((await storedDoc.json()).content).toBe('Hello from User A');\n  });\n\n  test('offline reconnection and conflict resolution', async ({ browser }) => {\n    const userA = await browser.newContext();\n    const userB = await browser.newContext();\n\n    const pageA = await userA.newPage();\n    const pageB = await userB.newPage();\n\n    await pageA.goto(`/doc/${docId}`);\n    await pageB.goto(`/doc/${docId}`);\n\n    // Simulate offline for User B\n    await pageB.context().setOffline(true);\n    await pageA.locator('.editor').fill('User A edit');\n    await pageB.waitForTimeout(1000);\n\n    // Reconnect User B\n    await pageB.context().setOffline(false);\n    await expect(pageB.locator('.editor')).toHaveValue('User A edit', { timeout: 5000 });\n\n    // Concurrent edits - last-write-wins conflict resolution\n    await pageB.locator('.editor').fill('User B edit');\n    await pageA.locator('.editor').fill('User A edit');\n    await pageB.waitForTimeout(2500); // Wait for autosave\n\n    const finalDoc = await request.get(`/api/docs/${docId}`);\n    expect((await finalDoc.json()).content).toMatch(/User (A|B) edit/);\n  });\n});\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "e2e-testing-q-24",
        "questionId": "q-17339",
        "question": "Design a beginner-friendly E2E test for an OAuth login flow (Google) in a fintech analytics dashboard. Include: mocking the Google OAuth server, a successful login flow, token expiry/refresh handling, session persistence across page reloads, and automatic sign-out on 401. Specify tooling (Cypress or Playwright), data isolation, and how you simulate OAuth responses to verify end-to-end behavior within 30 seconds?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Playwright to create two isolated browser contexts (A and B) connected to the same shared document via WebSocket. **Setup:** 1. Seed a test document via API with known initial content 2. Launch both browser contexts…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would implement with Playwright. Mock Google OAuth endpoints with a local server, seed a test user, trigger login, capture the redirect and exchange for a mock access/refresh token, store tokens in",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Playwright to sign in, open a title, and start playback; throttle network to 600kbps then 4Mbps to trigger ABR changes; verify bitrate transitions via a client telemetry hook window.__playerTeleme",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a cross-platform suite (Playwright + Appium) with seeded content and a deterministic playback validator. Use ABR traces, captions timing checks, and ad-insertion alignment across clients; run",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests involving external OAuth prove handling of redirects, token storage, and session behavior; mocking keeps tests reliable and fast for beginners.\n\n## Key Concepts\n\n- OAuth mocking and redirects\n- Token storage (sessionStorage/localStorage)\n- Session persistence across reloads\n- Token expiry/refresh flow\n- 401-triggered sign-out\n\n## Code Example\n\n```javascript\n// Playwright pseudo-setup\nimport { test, expect } from '@playwright/test';\ntest('oauth login end-to-end', async ({ page }) => {\n  // mock Google oauth endpoints and token exchange\n  // navigate to login, complete redirect, store tokens\n  // assert tokens present and persisted after reload\n  // simulate 401 on API call and verify redirect to login\n});\n```\n\n## Follow-up Questions\n\n- How would you extend to supporting multiple providers?\n- How would you test token refresh under network latency?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-14T15:44:02.152Z",
    "version": 1
  }
]