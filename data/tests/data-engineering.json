[
  {
    "id": "test-data-engineering",
    "channelId": "data-engineering",
    "channelName": "Data Engineering",
    "title": "Data Engineering Knowledge Test",
    "description": "Test your understanding of Data Engineering with 25 questions.",
    "questions": [
      {
        "id": "data-engineering-q-0",
        "questionId": "q-17540",
        "question": "Two regional CSV feeds (region A, region B) provide user_id, event_ts, action, and region, with possible extra columns and header drift. Design a beginner batch pipeline (Spark or Pandas) to ingest both feeds, enforce a per-record contract (user_id non-null, event_ts non-null, action non-null), normalize to a canonical schema (user_id, event_ts, action, region, ip_address), deduplicate by (user_id, event_ts), and append to a Parquet lakehouse. Support schema evolution by making ip_address optional and outline a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a two-tier feature store: offline in Iceberg, online in Redis. Ingest streaming events from Kafka plus batch catalog refreshes; implement semantic versioning for features, idempotent upserts,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Spark batch to read both CSVs with a permissive schema, then map to canonical fields (user_id STRING, event_ts TIMESTAMP, action STRING, region STRING, ip_address STRING). Filter rows where user_id, event_ts, or act…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use Spark Structured Streaming to ingest both regional JSON feeds, validate each record with a small function (event_id, ts, device_id non-null; metrics numeric). Conforming records map to a canonical",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a streaming data contracts-driven ingestion for three tenants (Tesla, Instacart, Hugging Face) into a lakehouse. Include per-tenant privacy masking, dynamic schema evolution, idempotent upsert",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses entry-level batch data ingestion, schema normalization, and deduplication skills in a real-world context, along with basic schema evolution handling. It aligns with data engineering interview expectations at companies like Google, Lyft, and Snowflake.\n\n## Key Concepts\n- Batch ingestion and canonicalization\n- Per-record data contracts and validation\n- Schema evolution with optional fields\n- Deduplication strategies in Spark\n- Lightweight testing for contracts and schema drift\n\n## Code Example\n\n```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, to_timestamp, row_number, when, coalesce, lit\nfrom pyspark.sql.window import Window\n\nspark = SparkSession.builder.appName(\"RegionalFeedsIngestion\").getOrCreate()\n\n# Define canonical schema\ncanonical_schema = \"user_id STRING, event_ts TIMESTAMP, action STRING, region STRING, ip_address STRING\"\n\n# Read both CSV feeds with permissive schema and header\ndf_a = spark.read.option(\"header\", \"true\").option(\"inferSchema\", \"true\").csv(\"s3://feeds/region_a/\")\ndf_b = spark.read.option(\"header\", \"true\").option(\"inferSchema\", \"true\").csv(\"s3://feeds/region_b/\")\n\n# Union both feeds\ndf = df_a.unionByName(df_b, allowMissingColumns=True)\n\n# Normalize and enforce contract\ndf_normalized = df.select(\n    col(\"user_id\"),\n    to_timestamp(col(\"event_ts\")).alias(\"event_ts\"),\n    col(\"action\"),\n    col(\"region\"),\n    coalesce(col(\"ip_address\"), lit(None)).alias(\"ip_address\")\n)\n\n# Filter invalid records (contract enforcement)\ndf_valid = df_normalized.filter(\n    col(\"user_id\").isNotNull() & \n    col(\"event_ts\").isNotNull() & \n    col(\"action\").isNotNull()\n)\n\n# Deduplicate by (user_id, event_ts), keeping latest\nwindow = Window.partitionBy(\"user_id\", \"event_ts\").orderBy(col(\"event_ts\").desc())\ndf_dedup = df_valid.withColumn(\"row_num\", row_number().over(window)).filter(col(\"row_num\") == 1).drop(\"row_num\")\n\n# Append to Parquet lakehouse partitioned by region\ndf_dedup.write.mode(\"append\").partitionBy(\"region\").parquet(\"s3://lakehouse/events/\")\n```\n\n## Minimal Test Plan\n\n| Test Case | Description |\n|-----------|-------------|\n| Schema Drift | Feed CSV with extra/missing columns; verify unionByName with allowMissingColumns succeeds |\n| Null Contract | Insert rows with null user_id, event_ts, or action; verify filtered out |\n| Deduplication | Feed duplicate (user_id, event_ts) pairs; verify only latest retained |\n| Schema Evolution | Feed CSV without ip_address; verify nulls populated correctly |\n| Partitioning | Verify output Parquet partitioned correctly by region |",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-1",
        "questionId": "q-20061",
        "question": "You have 3 regional CSV feeds with varying columns and optional fields. Design a beginner batch pipeline to ingest nightly into a lakehouse (Parquet/Iceberg) using a canonical schema: user_id string, event_ts timestamp, action string, region string, payload JSON. Enforce per-record contracts, route invalid rows to bad_data, handle drift with a simple mapping layer, and implement idempotent upserts into a central table using (region, user_id, event_ts). Include a minimal test plan and a MERGE example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Attach trace context in the producer, use Flink for streaming with idempotent MERGE into Iceberg, validate events against a JSON schema, and write a lineage row per record. Propagate trace IDs through",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a dual online/offline feature store: online store for sub-10ms lookups, offline Iceberg for batch recomputation. Enforce data contracts via a schema registry; version features and enable TTL pruni",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a multi-tenant validation layer backed by a per-tenant schema registry. Ingest via Kafka with idempotent producers and write to Iceberg using MERGE operations keyed by (tenant_id, event_ts, event_id) to ensure…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a PySpark batch job to read three regional CSV files, apply a column mapping layer to transform regional columns into a canonical schema, and enforce per-record contracts (user_id, event_ts, and action must be no…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses practical batch data ingestion, schema evolution handling, and upsert patterns in a lakehouse environment—core skills for entry-level data engineering roles.\n\n## Key Concepts\n- **Batch ingestion with regional variance:** Handling CSV files with different schemas from multiple sources\n- **Canonical schema design:** Establishing a unified structure (user_id, event_ts, action, region, payload) across all regions\n- **Per-record contracts and bad data handling:** Enforcing required fields and isolating invalid records for debugging\n- **Idempotent upserts:** Using MERGE operations with composite keys to ensure exactly-once semantics on repeated runs",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-2",
        "questionId": "q-3441",
        "question": "Ingest 2 TB/day mobile app events in JSON with nested optional fields into an Iceberg lakehouse. Timestamps arrive in multiple time zones. Design end-to-end pipeline that normalizes times to UTC, flattens key nested fields into a flat schema, and upserts with per-record lineage. Include schema validation, handling of missing fields, and monitoring; compare Spark Structured Streaming vs Flink for this workload?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Approach: validate each JSON row against a strict contract; mask PII in payload unless allowed; route non-conforming rows to bad_data; upsert conformant rows into the Iceberg-backed star schema with a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage Debezium CDC to stream changes into Kafka, process with Spark or Flink, and write to Iceberg with tenant_id isolation. Attach a stable record_id for per-record lineage; implement SCD2 using MERGE semantics for…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a central schema registry with per-tenant namespaces and contract validation; tag each record with tenant_id and trace_id; write to Iceberg lakehouse partitioned by tenant/date; perform idempotent",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design end-to-end: read 2 TB/day JSON events, normalize timestamps to UTC, flatten selected nested fields, and upsert into Iceberg with per-record lineage. Use a data contract to validate optional fie",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis angle tests time zone normalization, per-record lineage, and practical upsert challenges not covered by prior questions.\n\n## Key Concepts\n- Iceberg upserts, schema evolution\n- Nested JSON flattening, data contracts\n- Timezone normalization, idempotency, lineage\n\n## Code Example\n```javascript\n// Placeholder illustrative example: normalize timestamp and flatten nested fields\nfunction normalize(record) {\n  // pseudo-code\n}\n```\n\n## Follow-up Questions\n- How would you implement per-record lineage storage? \n- How do you validate schema evolution and backward compatibility? \n- How would you monitor data quality and alert on schema drift?",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-3",
        "questionId": "q-24409",
        "question": "Ingest a daily 500GB of region-tagged JSON events into an Iceberg lakehouse. Each event must contain region, session_id, event_ts, user_id, action; metadata is optional. Describe a beginner-friendly pipeline that enforces per-record contracts, performs idempotent upserts into a canonical Iceberg table partitioned by region/date, and detects schema drift within a 24-hour window. When drift is detected, route affected records to a quarantine table for review and outline a minimal test plan with a sample MERGE statement?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a streaming data contracts-driven ingestion for three tenants (Tesla, Instacart, Hugging Face) into a lakehouse. Include per-tenant privacy masking, dynamic schema evolution, idempotent upsert",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a streaming pipeline using Kafka -> Spark Structured Streaming with watermarking for 2-hour late data tolerance, Iceberg table with device_id partitioning and dynamic schema evolution, PII masking via Spark UDFs…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Spark Structured Streaming to read both JSON streams into Iceberg tables. Validate contracts with a simple schema enforcement (device_id: string, event_ts: timestamp, metric: double, non-null). Im",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Spark Structured Streaming to read the JSON, validate required fields (region, session_id, event_ts, user_id, action), route non-conforming rows to bad_data, then upsert into an Iceberg table part",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end data ingestion with contracts, idempotent upserts, drift handling, and minimal testing.\n\n## Key Concepts\n- Per-record contracts and bad_data routing\n- Iceberg upserts and region/date partitioning\n- Lightweight schema registry for canonical schema\n- Schema drift detection and quarantining drifted data\n\n## Code Example\n```sql\nMERGE INTO iceberg_db.events AS t\nUSING staging AS s\nON t.region = s.region AND t.session_id = s.session_id\nWHEN MATCHED THEN UPDATE SET t.event_ts = s.event_ts, t.user_id = s.user_id, t.action = s.action, t.metadata = s.metadata\nWHEN NOT MATCHED THEN INSERT (region, session_id, event_ts, user_id, action, metadata) VALUES (s.region, s.session_id, s.event_ts, s.user_id, s.action, s.metadata);\n```\n\n## Follow-up Questions\n- How would you test drift detection reliability with synthetic data?\n- What metrics would you expose for monitoring the pipeline health?",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-4",
        "questionId": "q-28462",
        "question": "Design an end-to-end pipeline for a multi-tenant SaaS analytics lakehouse where each tenant emits events and CSV exports with evolving schemas. Use a central schema registry to enforce per-record contracts, isolate data per tenant in Iceberg, and perform idempotent upserts into a canonical table. Support late data up to 6 hours, automated contract tests, and provide a concrete MERGE example plus a monitoring plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Canonical schema + per-tenant policy registry. Ingest CSV/JSON/Parquet via Flink into a lakehouse (Iceberg), normalizing to the canonical schema. Enforce per-tenant masking at read-time with tenant-scoped views or dynam…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Architect a per-plant namespace telemetry pipeline with a schema registry enforcing per-record contracts. Ingest via Pulsar or Kinesis with exactly-once guarantees, buffer late data to 30s, and upsert",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a central per-tenant contract registry and a canonical schema; stream with Flink for exactly-once, writing to Iceberg with tenant/region partitions; MERGE upserts into a canonical table; enforce r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant canonical schema, a central schema registry to validate every record, and per-tenant staging Iceberg tables. MERGE into a canonical table on (tenant_id, record_id) with updates for ch",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThe scenario tests ability to enforce cross-tenant data contracts while maintaining isolation, scalability, and correct upserts in a lakehouse. It also probes handling of late data and end-to-end testing.\n\n## Key Concepts\n- Central schema registry for per-record contracts\n- Iceberg per-tenant staging and canonical table\n- Idempotent upserts with (tenant_id, record_id)\n- Late data handling via watermarking (6 hours)\n- Contract tests and lineage monitoring\n\n## Code Example\n```javascript\n// MERGE sketch (pseudo-SQL in a JS block for readability)\nMERGE INTO canonical_table AS t\nUSING staging_table AS s\nON t.tenant_id = s.tenant_id AND t.record_id = s.record_id\nWHEN MATCHED THEN UPDATE SET t.fieldA = s.fieldA, t.fieldB = s.fieldB, t.updated_at = CURRENT_TIMESTAMP\nWHEN NOT MATCHED THEN INSERT (tenant_id, record_id, fieldA, fieldB, updated_at) VALUES (s.tenant_id, s.record_id, s.fieldA, s.fieldB, CURRENT_TIMESTAMP);\n```\n\n## Follow-up Questions\n- How would you test the registry schema evolution without breaking existing tenants?\n- What metrics and alerts would you configure for drift and quality monitoring?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-5",
        "questionId": "q-26434",
        "question": "Design an end-to-end data-sharing pipeline for multi-tenant fintech partners ingesting mixed-format feeds (CSV, JSON, Parquet) into a centralized lakehouse (Iceberg). Requirements: per-record contracts via a schema registry, canonical schema with drift handling, dynamic PII masking and row-level security, idempotent upserts, late-arriving data up to 24 hours, isolation by partner and region, and a contract-oriented testing strategy including end-to-end privacy tests. Provide a concrete MERGE example and discuss monitoring and data contracts validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-tenant schema registry with contract validation at ingest, store data in Iceberg with per-tenant partitioning, and perform idempotent MERGE UPSERTs. Enable dynamic schema evolution with comp",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Capture with streaming CDC, map to a canonical schema via a registry, and MERGE into Iceberg using a deterministic key (source_system,event_id) to ensure idempotency. Maintain region-scoped catalogs;",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a privacy-aware ingestion pipeline: ingest 3 regional CSV feeds, validate against a versioned schema registry, tokenize PII, map to a canonical schema, and upsert into Iceberg with idempotent MERGE statements.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a central Avro/Protobuf schema registry to map all feeds to a canonical schema. Upsert into Iceberg using per-tenant partitions (partner/region), apply row-level security and PII masking at the column level vi…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design cross-tenant data contracts, privacy controls, and scalable lakehouse ingestion with schema drift handling.\n\n## Key Concepts\n- **Schema Registry and Canonical Schema**: Enforce per-record contracts via Avro/Protobuf, normalize all feed formats to a unified canonical model with backward/forward compatibility policies.\n- **Iceberg Upserts and Partitioning**: Use MERGE statements partitioned by partner and region for isolation, auditability, and query performance.\n- **Row-Level Security and PII Masking**: Apply masking policies (e.g., SHA-256 hashing, tokenization) at ingestion or query time; enforce RLS filters based on tenant context.\n- **Late Data Handling**: Watermark-based event-time processing tolerates up to 24-hour late arrivals; out-of-window records trigger reprocessing or dead-letter handling.\n- **Data Contracts and Contract Testing**: Validate schemas, freshness, and distribution metrics using tools like Great Expectations or dbt contracts; integrate into CI/CD pipelines.\n- **Observability and Lineage**: Emit DataHub/OpenLineage metadata; track schema drift, ingestion latency, and contract violations.\n\n## Code Example\n```sql\nMERGE INTO fintech_schema.partner_activity AS target\nUSING (\n  SELECT \n    partner_id,\n    region,\n    event_id,\n    canonical_timestamp,\n    masked_email,\n    encrypted_ssn,\n    amount,\n    _source_feed,\n    _processed_at\n  FROM canonical_activity\n  WHERE _ingestion_time >= WATERMARK('event_time', INTERVAL '24' HOUR)\n) AS source\nON target.partner_id = source.partner_id \n   AND target.region = source.region \n   AND target.event_id = source.event_id\nWHEN MATCHED AND source._processed_at > target._processed_at THEN\n  UPDATE SET \n    canonical_timestamp = source.canonical_timestamp,\n    masked_email = source.masked_email,\n    encrypted_ssn = source.encrypted_ssn,\n    amount = source.amount,\n    _processed_at = source._processed_at,\n    _version = target._version + 1\nWHEN NOT MATCHED THEN\n  INSERT (partner_id, region, event_id, canonical_timestamp, masked_email, encrypted_ssn, amount, _source_feed, _processed_at, _version)\n  VALUES (source.partner_id, source.region, source.event_id, source.canonical_timestamp, source.masked_email, source.encrypted_ssn, source.amount, source._source_feed, source._processed_at, 1);\n```\n\n## Monitoring and Data Contract Validation\n- **Schema Drift Detection**: Alert when inbound schema deviates from registered canonical schema (added/removed/missing fields).\n- **Contract Assertions**: Run nightly Great Expectations suites validating nulls, ranges, uniqueness, and freshness SLAs per partner.\n- **Iceberg Metrics**: Track merge latency, file compaction rates, and snapshot age per partition.\n- **PII Compliance Tests**: End-to-end privacy tests asserting that masked columns contain no raw PII and RLS filters block cross-tenant access.\n- **Data Lineage**: Emit lineage events to track record origin through schema registry → ingestion → lakehouse → downstream consumers.",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-6",
        "questionId": "q-24571",
        "question": "Design a beginner-friendly pipeline to ingest a JSON feed of user events from three regions into Snowflake, mapping a nested payload to a canonical schema (users dim and events fact). Enforce per-record contracts (device_id, event_ts non-null); route non-conforming rows to a bad_data sink; support late events up to 5 minutes with watermarking; implement a simple idempotent MERGE into the fact table and keep data isolated by region?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose Spark Structured Streaming + Iceberg with a central schema registry (Avro/JSON) for 20 event types across 50 streams. Normalize to a canonical fact table, key on advertiser_id+event_id, and up",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Build a batch Spark pipeline that ingests three regional CSV feeds into Iceberg, enforces per-record contracts (id, ts, user_id non-null; others optional), auto-evolves schema for new columns, and ups",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Snowflake Streams and Tasks to ingest region-partitioned JSON, flatten payload into USER_DIM and EVENTS_FACT. Enforce per-record contracts: device_id and event_ts non-null. Route non-conforming ro",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a canonical Iceberg table with record_id as primary key. Validate: user_id (string, not null), event_time (timestamp), event_type (string). Normalize to canonical columns; optional payload mapped",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design an end-to-end, beginner-friendly pipeline with per-record contracts, late-data handling, and idempotent upserts in Snowflake. Emphasizes data hygiene and practical transforms.\n\n## Key Concepts\n- Snowflake Streams/Tasks for incremental loads\n- Flattening nested JSON to canonical schema\n- MERGE-based upserts and bad_data routing\n\n## Code Example\n```javascript\nfunction flattenEvent(raw) {\n  const { region, device_id, event_ts, payload } = raw;\n  const { user_id, action } = payload;\n  return { region, device_id, event_ts, user_id, action };\n}\n```\n\n## Follow-up Questions\n- How would you validate the 5-minute watermark and late-arrival behavior?\n- How would you monitor bad_data rate and schema drift in Snowflake?",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-7",
        "questionId": "q-25201",
        "question": "Ingest two regional JSON streams of app telemetry into a lakehouse with a canonical schema. Enforce per-record contracts (required: user_id, event_ts, action; optional: metadata), redact PII by hashing emails and masking phones at ingest, route non-conforming rows to bad_data, and perform idempotent upserts into Iceberg supporting schema evolution. Handle late data up to 60 minutes with watermark. Compare Spark Structured Streaming vs Flink and sketch a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming path using Spark Structured Streaming or Flink to read both regional JSON streams, enforce per-record contracts, redact PII (hash emails with SHA-256, mask phones), and upsert into",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Ingest 12 regional Kafka streams into Iceberg with idempotent upserts, driven by a central schema registry enforcing per-record contracts. Handle late data with watermarks and a configurable lateness",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a multi-region ingest pipeline: regional Kafka clusters → Protobuf serialization with a central Confluent/Glue schema registry → Spark Structured Streaming (or Flink) for processing → Iceberg lakehouse with idemp…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a batch Spark job: read customers.csv and orders.csv with an explicit schema, enforce contracts via filters (customer_id, order_id non-null; signup_ts/order_ts parseable; region in allowed set).",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a privacy-conscious streaming ingestion with per-record contracts, schema evolution, and idempotent upserts into a lakehouse. It also probes engine choice (Spark vs Flink) and a pragmatic testing plan.\n\n## Key Concepts\n\n- Per-record contracts and data contracts\n- In- ingest PII redaction (hashing emails, masking phones)\n- Canonical schema and schema evolution\n- Iceberg upserts with deterministic keys\n- Late data handling with watermarking\n- Spark Structured Streaming vs Flink trade-offs\n\n## Code Example\n\n```python\n# Pseudocode for redacting PII during ingest\ndef redact(record):\n  if 'email' in record:\n     record['email_hash'] = sha256(record['email'].lower().strip()).hexdigest()\n     del record['email']\n  if 'phone' in record and record['phone']:\n     record['phone'] = 'REDACTED'\n  return record\n```\n\n## Follow-up Questions\n\n- How would you test redaction correctness and performance?\n- How would you handle evolving required fields without breaking downstream sinks?",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-8",
        "questionId": "q-13405",
        "question": "Ingest streaming changes from a PostgreSQL source using Debezium into a multi-region Iceberg lakehouse. Include per-record data contracts, idempotent upserts, and per-record lineage, but add field-level privacy: PII must be hashed or tokenized at ingest with policy-driven re-identification offline. How would you enforce privacy-compliant schema evolution, validate data quality, and prove lineage across regions? Provide testing and a concrete MERGE example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Debezium CDC to stream changes into Kafka, process with Spark or Flink, and write to Iceberg with tenant_id isolation. Attach a stable record_id for per-record lineage; implement SCD2 using MERGE semantics for…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a CDC pipeline with Debezium from PostgreSQL to Spark Structured Streaming feeding Iceberg. Enforce per-record contracts via a schema registry, implement idempotent upserts with MERGE, and stor",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Propose Spark Structured Streaming reading three regional CSVs, mapping to a canonical Parquet schema, validating per-record contracts with a simple JSON schema registry. Write good data to the lake w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a deterministic masking approach using HMAC-SHA256 keyed hashing for email and phone fields, storing results in masked_email and masked_phone columns. Manage encryption keys securely in a vault (e.g., AWS KMS or…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical CDC design under privacy, lineage, and multi-region constraints.\n\n## Key Concepts\n- CDC ingestion and idempotent upserts\n- Per-record data contracts and schema evolution\n- Field-level privacy (hash/tokenize) at ingest\n- Per-record lineage and tombstones across regions\n\n## Code Example\n```javascript\n// Pseudocode: MERGE to apply upserts with lineage and privacy checks\nMERGE INTO iceberg_table AS t\nUSING staged_changes AS s\nON t.record_id = s.record_id\nWHEN MATCHED THEN UPDATE SET\n  t.payload = s.payload,\n  t.privacy_hash = s.privacy_hash,\n  t.op_ts = s.op_ts\nWHEN NOT MATCHED THEN INSERT (record_id, payload, privacy_hash, op_ts) VALUES (s.record_id, s.payload, s.privacy_hash, s.op_ts);\n```\n\n## Follow-up Questions\n- How would you test lineage accuracy and privacy guarantees end-to-end?\n- What strategies ensure GDPR deletions propagate across regions without leaking history?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-9",
        "questionId": "q-16321",
        "question": "Design a cross-region telemetry ingestion pipeline: 8 partners emit JSON/Avro with evolving schemas. Enforce per-record contracts via a canonical registry, buffer late events (15 minutes), and perform idempotent upserts into Iceberg lakehouse partitioned by partner-region. Store per-record lineage and an immutable audit log. Include a MERGE example and testing plan (contract, drift, lineage). Compare Spark vs Flink and justify your choice?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a streaming data contracts-driven ingestion for three tenants (Tesla, Instacart, Hugging Face) into a lakehouse. Include per-tenant privacy masking, dynamic schema evolution, idempotent upsert",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a multi-tenant pipeline with per-region ingestion, central schema registry enforcing a canonical schema, and per-tenant MERGE upserts into the lakehouse. Use Flink for exactly-once processin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant contract registry, canonical schema, and tenant-scoped Iceberg tables. Use event-time processing with watermarks and 6-hour lateness handling. Idempotent upserts via MERGE into Iceberg; per-record lineage and…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cross-region telemetry ingestion pipeline: eight partners emit JSON/Avro with evolving schemas. Enforce per-record contracts via a canonical schema registry, buffer late events for 15 minutes using watermarks,…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to engineer end-to-end data contracts, schema evolution, and lakehouse upserts at scale with lineage and auditability. Also probes tooling choice (Spark vs Flink) under late-arriving data and multi-tenant isolation.\n\n## Key Concepts\n- Central schema/contract registry with canonical schema enforcement\n- Idempotent upserts into Iceberg with exactly-once semantics\n- Late data handling via watermarks and buffering\n- Per-record lineage tracking and immutable audit logging\n- Contract and drift testing; lineage verification\n- Trade-offs: Spark vs Flink in latency, state management, and processing models\n\n## Code Example\n```sql\n-- Iceberg MERGE for idempotent upserts\nMERGE INTO telemetry_table T\nUSING staging_streams S\nON T.partner_id = S.partner_id \n   AND T.region = S.region \n   AND T.event_id = S.event_id\nWHEN MATCHED THEN UPDATE SET\n    payload = S.payload,\n    processed_at = CURRENT_TIMESTAMP,\n    version = T.version + 1\nWHEN NOT MATCHED THEN INSERT (\n    partner_id, region, event_id, payload, \n    ingested_at, processed_at, version, lineage\n) VALUES (\n    S.partner_id, S.region, S.event_id, S.payload,\n    S.ingested_at, CURRENT_TIMESTAMP, 1,\n    to_json(struct(S.*))\n);\n```\n\n```python\n# Testing Plan\n\n# 1. Contract Testing\ndef test_contract_enforcement():\n    schema = registry.get_schema(\"telemetry\")\n    for record in incoming_stream:\n        validation_result = schema.validate(record)\n        assert validation_result.is_valid, f\"Contract violation: {validation_result.errors}\"\n\n# 2. Schema Drift Detection\ndef test_schema_drift():\n    canonical_schema = registry.get_canonical()\n    incoming_schema = parse_schema(record)\n    drift = schema_diff(canonical_schema, incoming_schema)\n    assert drift.is_compatible, f\"Schema drift detected: {drift.breaking_changes}\"\n\n# 3. Lineage Verification\ndef test_lineage_tracking():\n    record = process_event(event)\n    lineage = lineage_tracker.get(record.event_id)\n    assert lineage.source == \"partner_stream\"\n    assert lineage.transformations == [\"validation\", \"enrichment\"]\n\n# 4. Idempotency Test\ndef test_idempotent_upsert():\n    event = create_test_event()\n    result1 = merge_to_iceberg(event)\n    result2 = merge_to_iceberg(event)\n    assert result1.updated == result2.updated\n    assert get_record_count() == 1  # No duplicates\n```\n\n## Spark vs Flink Comparison\n\n| Aspect | Spark Streaming | Flink |\n|--------|----------------|-------|\n| **Latency** | ~100ms (micro-batch) | ~1ms (true streaming) |\n| **State Management** | Checkpoint-based | Managed state with rocksDB |\n| **Late Data** | Watermarks + windows | Sophisticated watermarks |\n| **Exactly-Once** | Limited | Native support |\n| **Learning Curve** | Lower | Higher |\n| **Ecosystem** | Mature, wide adoption | Growing rapidly |\n\n**Recommendation**: Choose **Flink** for this use case because:\n1. True streaming handles late-arriving data more elegantly with sophisticated watermark mechanisms\n2. Native exactly-once semantics ensure correct upsert behavior\n3. Fine-grained state management suits the 15-minute buffer window requirement\n4. Better handling of evolving schemas during runtime\n\nUse **Spark** if: team expertise is limited, batch-heavy hybrid workloads exist, or latency requirements are moderate (>1 second).",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-10",
        "questionId": "q-18312",
        "question": "Design a unified ingestion and governance pipeline for real-time telemetry and nightly enrichment feeds across 10+ regions with these requirements: (1) per-tenant data contracts enforced via a central schema registry; (2) idempotent upserts into Iceberg with per-tenant partitioning; (3) dynamic schema evolution with compatibility rules; (4) per-record lineage and GDPR deletes via tombstones; (5) drift detection to quarantine bad data; (6) compare Spark Structured Streaming vs Flink and justify; (7) tests and monitoring plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose Spark Structured Streaming + Iceberg with a central schema registry (Avro/JSON) for 20 event types across 50 streams. Normalize to a canonical fact table, key on advertiser_id+event_id, and up",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a concrete end-to-end approach: choose between MongoDB Change Streams and Debezium, design per-region tenants with isolated streams, and upsert into an Iceberg lakehouse. Handle tombstone dele",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end streaming pipeline using Flink for strict event-time processing with 1-hour watermark handling. Implement a central Avro/Protobuf schema registry to enforce per-tenant contracts and validate schemas…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a per-tenant schema registry with contract validation at ingest, store data in Iceberg with per-tenant partitioning, and perform idempotent MERGE UPSERTs. Enable dynamic schema evolution with comp",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests cross-region governance, per-tenant contracts, and streaming vs batch tradeoffs in real-world scale.\n\n## Key Concepts\n- Data contracts, schema registry, idempotent upserts\n- Per-tenant isolation and partitioning in Iceberg\n- Schema evolution and data quality drift detection\n- GDPR deletion and lineage\n- Engine tradeoffs: Spark vs Flink\n\n## Code Example\n```sql\n-- simplified Iceberg MERGE example\nMERGE INTO lake.tennant_table AS t\nUSING staged.spark AS s\nON t.id = s.id\nWHEN MATCHED THEN UPDATE SET t.value = s.value\nWHEN NOT MATCHED THEN INSERT (id, value) VALUES (s.id, s.value);\n```\n\n## Follow-up Questions\n- How would you test schema drift and automatically promote compatible changes?\n- How would you implement per-record lineage in analytics tooling?",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-11",
        "questionId": "q-29434",
        "question": "Design an end-to-end, multi-tenant telemetry pipeline for ride-hailing data: ingest 100 TB/day of JSON events from 12 microservices across 3 regions via Kafka. Enforce strict region isolation, per-record contracts, and PII masking for analytics. Compare per-region catalogs vs a shared catalog with ACLs, plan idempotent MERGE upserts into Iceberg, and dynamic schema evolution. Include testing, monitoring, and lineage; end with a concrete MERGE example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Decode Protobuf messages using a schema registry, enforce per-record data contracts, and route by region. Ingest into Iceberg using MERGE-based idempotent upserts keyed on (region, gateway_id, event_time), maintaining p…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design an end-to-end multi-tenant telemetry pipeline for ride-hailing data that ingests 100 TB/day of JSON events from 12 microservices across 3 regions via Kafka. The solution must enforce strict region isolation, per-…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "In a Spark batch job, read the daily JSON, enforce contract: user_id string not null, event_ts long not null, action string not null; optional details as JSON. Normalize to canonical schema: user_id,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Model per-record consent and a consent_delta table. Use Iceberg MERGE to retroactively DELETE or MASK sensitive fields when consent_revoked is true, preserving lineage via a lineage_id and audit table",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates readiness for designing scalable, governed lakehouse data products with multi-tenancy, dynamic schemas, and privacy constraints at ride-hailing scale. It encompasses Iceberg, Kafka, ACLs, data contracts, and testability.\n\n## Key Concepts\n- Multi-tenant isolation strategies (separate catalogs per region vs. shared catalog with ACLs)\n- Per-record contracts and schema evolution\n- PII masking and data governance with lineage tracking\n- Idempotent upserts using MERGE operations\n- End-to-end testing and monitoring\n\n## Code Example\n```sql\n-- Idempotent MERGE upsert into Iceberg (using partition by date)\nMERGE INTO analytics.trips_merged t\nUSING (SELECT \n    event_id,\n    driver_id,\n    rider_id,\n    region,\n    pickup_lat,\n    pickup_lon,\n    dropoff_lat,\n    dropoff_lon,\n    fare_amount,\n    event_timestamp,\n    CURRENT_TIMESTAMP() AS updated_at\n  FROM staging.trips_staging) s\nON t.event_id = s.event_id \n   AND t.region = s.region\nWHEN MATCHED THEN UPDATE SET\n    driver_id = s.driver_id,\n    fare_amount = s.fare_amount,\n    dropoff_lat = s.dropoff_lat,\n    dropoff_lon = s.dropoff_lon,\n    updated_at = s.updated_at\nWHEN NOT MATCHED THEN INSERT (\n    event_id, driver_id, rider_id, region,\n    pickup_lat, pickup_lon, dropoff_lat, dropoff_lon,\n    fare_amount, event_timestamp, updated_at\n) VALUES (\n    s.event_id, s.driver_id, s.rider_id, s.region,\n    s.pickup_lat, s.pickup_lat, s.dropoff_lat, s.dropoff_lon,\n    s.fare_amount, s.event_timestamp, s.updated_at\n);\n```\n\n**Strong answer indicators:** Proposes region-scoped Kafka topics, enforces JSON schema validation per microservice, and selects catalog strategy with explicit trade-offs. Demonstrates familiarity with Iceberg's write isolation, hidden partitioning, and time-travel capabilities. Includes PII handling via column-level masking or field hashing with lineage capture via OpenLineage.",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-12",
        "questionId": "q-15838",
        "question": "Design a batch ingest that reads two regional JSON feeds into an Iceberg lakehouse. Normalize event_ts to UTC using region timezone, enforce per-record contracts (device_id non-null, event_ts sane, region valid), map event_type to a canonical set, and route bad records to bad_data. Upsert into a canonical star schema and support Iceberg schema evolution?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design and implement a batch ingest pipeline that reads two regional JSON feeds into an Iceberg lakehouse. The pipeline should normalize event_ts to UTC using the corresponding region timezone, enforce per-record data c…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Spark Structured Streaming to read Kafka JSON, validate with a lightweight contract (event_id, user_id, ts, event_type required; attrs optional), and MERGE into an Iceberg table on event_id to ups",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a feature registry to validate feature_name and type per event, partition by tenant, and apply event-time processing with a small watermark in Flink. Upsert into Iceberg via MERGE INTO on (tenant_",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a concrete end-to-end approach: choose between MongoDB Change Streams and Debezium, design per-region tenants with isolated streams, and upsert into an Iceberg lakehouse. Handle tombstone dele",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests the ability to design region-aware time normalization, implement data contracts with invalid-record routing, and work with Iceberg's schema evolution and upsert capabilities. It is beginner-friendly yet demonstrates practical thinking about data quality and lakehouse architecture.\n\n## Key Concepts\n\n- Timezone normalization and UTC conversion using region-specific mappings\n- Per-record data validation contracts with bad record segregation\n- Iceberg schema evolution for backward-compatible schema changes\n- Idempotent upserts using merge-on-read and primary key deduplication\n- Star schema design for analytical workloads\n\n## Code Example\n\n```python\nfrom datetime import datetime\nfrom dateutil import tz\n\ndef to_utc(event_ts: str, region_tz: str) -> datetime:\n    local = datetime.fromisoformat(event_ts.replace('Z', '+00:00'))\n    timezone = tz.gettz(region_tz)\n    if timezone is None:\n        raise ValueError(f\"Invalid timezone: {region_tz}\")\n    local = local.replace(tzinfo=timezone)\n    return local.astimezone(tz.UTC)\n\ndef validate_record(record: dict) -> tuple[bool, str]:\n    if not record.get('device_id'):\n        return False, \"device_id is null or missing\"\n    \n    try:\n        ts = to_utc(record['event_ts'], record['region'])\n        if ts.year < 2000 or ts.year > 2100:\n            return False, \"event_ts out of reasonable range\"\n    except (ValueError, KeyError) as e:\n        return False, f\"invalid event_ts or region: {e}\"\n    \n    valid_regions = {'US', 'EU', 'APAC'}\n    if record.get('region') not in valid_regions:\n        return False, f\"invalid region: {record.get('region')}\"\n    \n    return True, \"\"\n\nCANONICAL_EVENT_TYPE_MAP = {\n    'page_view': 'PAGE_VIEW',\n    'click': 'CLICK',\n    'purchase': 'PURCHASE',\n    'signup': 'SIGN_UP'\n}\n\ndef map_event_type(event_type: str) -> str:\n    return CANONICAL_EVENT_TYPE_MAP.get(event_type, 'UNKNOWN')\n\ndef process_batch(input_df, bad_data_writer):\n    valid_records = []\n    bad_records = []\n    \n    for record in input_df.collect():\n        is_valid, error_msg = validate_record.asDict(record)\n        if is_valid:\n            record['event_ts'] = to_utc(record['event_ts'], record['region'])\n            record['event_type'] = map_event_type(record['event_type'])\n            valid_records.append(record)\n        else:\n            record['error_reason'] = error_msg\n            bad_records.append(record)\n    \n    bad_data_writer.write(bad_data_writer, bad_records)\n    return valid_records\n```",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-13",
        "questionId": "q-16762",
        "question": "Design a cross-tenant data mesh for ML feature sharing across regions with a global feature store: require per-tenant data contracts, masking of PII, both online (low latency) and offline (lakehouse) feature views, schema evolution, and end-to-end lineage. Describe data contracts, storage layout, versioning, access control, failure modes, testing, and a concrete merge example for a customer_score feature?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Cross-Tenant Data Mesh for ML Feature Sharing ### Data Contracts Per-tenant contracts define schemas, SLAs, and access policies. Each tenant receives a validated contract ID and schema registry entry specifying: - Fe…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Prefer Flink: ingest from Kafka with event-time processing and 2h lateness. Validate against a schema registry, parse nested JSON, upsert into Iceberg star schema using (device_id, event_id) as PK. Em",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Spark to read regional CSVs, map to a canonical schema, cast event_ts to UTC, deduplicate on (region,user_id,event_ts), validate user_id exists in users_dim via left join, then MERGE INTO lake.can",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Unity Catalog + Delta Lake with region-scoped catalogs; define column-level masking and access policies. Example: mask customer_id by salted hash for non-privileged roles; redact amount for non-fi",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses multi-tenant governance, data mesh principles, and feature store design at scale—critical competencies for financial technology roles where privacy, lineage, and compliance are paramount.\n\n## Key Concepts\n- Cross-tenant contracts and policy-driven PII masking\n- Online versus offline feature store architectures\n- Canonical versioned schemas and drift detection\n- Data lineage, access control, and privacy-preserving deletes\n- Streaming updates and failure mode handling\n\n## Evaluation Criteria\nStrong candidates demonstrate:\n1. Understanding of tenant isolation and data contract governance\n2. Knowledge of feature store trade-offs between online and offline serving\n3. Awareness of schema evolution challenges and backward compatibility\n4. Familiarity with lineage tracking and regulatory compliance requirements",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-14",
        "questionId": "q-1140",
        "question": "Given daily 1 GB of web logs in JSON lines stored on S3 with fields user_id, timestamp, path, status, and optional referrer, design a beginner-friendly pipeline (Python or Node.js) that deduplicates by timestamp+user_id+path, validates required fields, normalizes timestamp to UTC, and writes date-partitioned Parquet to a data lake; include basic tests and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest via Kafka into a Spark Structured Streaming job or Flink, write features to an Iceberg-backed feature store partitioned by city. Provide online features in Redis for low latency; offline featur",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose Spark Structured Streaming reading three regional CSVs, mapping to a canonical Parquet schema, validating per-record contracts with a simple JSON schema registry. Write good data to the lake w",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a small streaming script (Python or Node.js) to read daily JSONL from S3, deduplicate by (timestamp, user_id, path), validate required fields (user_id, timestamp, path, status), coerce timestamp t",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a metric-versioning layer: augment each row with tenant_id, product_id, event_ts, and metric_version; keep historical rows untouched. Maintain a per-tenant pricing_rules table (tenant_id, prod",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical, beginner-friendly construction of a data pipeline with common tasks: deduplication, validation, timestamp handling, and partitioned Parquet, plus testing and monitoring.\n\n## Key Concepts\n- JSON Lines parsing\n- Idempotent deduplication\n- Data quality checks (required fields, types)\n- Timestamp normalization to UTC\n- Parquet writing and date partitioning\n- Basic testing and observability\n\n## Code Example\n```javascript\nconst fs = require('fs');\nconst readline = require('readline');\n(async () => {\n  const rl = readline.createInterface({ input: fs.createReadStream('logs.jsonl'), crlfDelay: Infinity });\n  const seen = new Set();\n  for await (const line of rl) {\n    const obj = JSON.parse(line);\n    if (!obj.user_id || !obj.timestamp || !obj.path || !obj.status) continue;\n    const key = `${obj.timestamp}|${obj.user_id}|${obj.path}`;\n    if (seen.has(key)) continue;\n    seen.add(key);\n    // normalization and persistence would occur here\n  }\n})();\n```\n\n## Follow-up Questions\n- How would you scale this for multi-region logs?\n- What metrics and alerts would you add for production reliability?",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-15",
        "questionId": "q-19428",
        "question": "In a multi-region real-time analytics pipeline for a global payments platform, ingest 80k events/sec of transaction deltas from five PSP partners. Each event carries fields: transaction_id, user_id, region, event_ts, amount, currency, and optional risk_flags. Schema evolves often. Requirements: per-record contracts via a central schema registry, canonical schema, idempotent upserts into Iceberg, dynamic schema evolution, region isolation, late data up to 10 minutes, per-record lineage, and robust data-quality gates. Design end-to-end pipeline: streaming inlet, lakehouse, partitioning by region, and MERGE example; include monitoring and testing strategy, compare Spark Structured Streaming vs Flink for latency and fault-tolerance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Ingest daily JSON lines, deduplicate by event_id, hash user_id with SHA-256, validate required fields, and write Parquet data partitioned by date in a data lake. Include a simple schema-drift strategy",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a versioned canonical schema in a registry; create per-tenant Iceberg tables for raw, canonical, and materialized views. Stream sources into staging with event-time watermarks and a 60-minute lateness tolerance, the…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Spark to read the JSON batch, enforce contracts (non-null user_id,event_time,event_type). Apply a UDF to redact email and phone when consent=false. Write raw batch to Raw Iceberg table for auditin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Central schema registry defines canonical payload (transaction_id PK). Ingest from five PSPs by region using Spark Structured Streaming with 10-minute watermark and 10-minute lateness. Iceberg per-reg",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nRenders a realistic, scalable data-contract driven pipeline with evolving schemas, requiring robust idempotent upserts and per-record lineage across regions.\n\n## Key Concepts\n- Data contracts and central schema registry\n- Iceberg upserts and MERGE semantics\n- Watermarks, late data handling\n- Per-region data isolation and lineage\n- Spark vs Flink trade-offs\n\n## Code Example\n```sql\nMERGE INTO iceberg.region_sales t\nUSING staged.region_sales s\nON t.transaction_id = s.transaction_id\nWHEN MATCHED THEN UPDATE SET ...\nWHEN NOT MATCHED THEN INSERT (...)\n```\n\n## Follow-up Questions\n- How would you test contract drift across schema evolution?\n- How would you implement monitoring for data quality gates and lineage?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-16",
        "questionId": "q-22513",
        "question": "You're operating a real-time user-event feed feeding a centralized ML feature store across 3 regions. Features are added weekly, with evolving schemas. Design an end-to-end pipeline that (1) enforces per-feature contracts via a central registry, (2) supports multi-version routing so models read the correct feature version, (3) handles schema evolution with zero-downtime writes, (4) maintains per-record lineage and data quality, (5) enables canary rollouts with rollback and monitoring. Include concrete components and a small contract example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a per-tenant ingestion path with tenant_id in every event, stream via Kafka to a lakehouse where raw JSON lands in a bronze Iceberg table partitioned by country/tenant. Normalize to a canonical sc",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Flink to stream JSON events from Kafka, apply per-device YAML contracts from a contracts store, validate, and project to Iceberg. Emit per-record lineage to a lineage table (source_id, device_id,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a central versioned feature registry with per-feature contracts and lineage. Persist features to a lakehouse (Iceberg) with version tags; route reads via a feature-lookup service that binds model requests to t…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a GDPR-compliant, multi-tenant lakehouse ingestion for 20 regional JSON streams. Use a tenant-scoped contract registry, immutable lineage, and per-record op (upsert/delete). Ingest to Iceberg w",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nNew feature variants across regions require strict contracts, versioned data, and safe deployments. This question tests practical knowledge of feature registries, version tagging, lineage tracking, and canary deployment strategies.\n\n## Key Concepts\n- Feature registry with versioning and contracts\n- Schema evolution guards with compatibility checks\n- Per-record lineage and data quality gates\n- Multi-version routing for model-feature alignment\n- Canary rollouts with monitoring and rollback\n\n## Code Example\n```javascript\n// Contract enforcement example\nconst contract = { \n  version: '1.0.0',\n  feature_name: 'user_engagement_score',\n  schema: {\n    user_id: { type: 'string', required: true },\n    engagement_score: { type: 'float', min: 0, max: 100 },\n    computed_at: { type: 'timestamp', required: true }\n  },\n  quality_rules: [\n    { field: 'engagement_score', rule: 'not_null' },\n    { field: 'user_id', rule: 'not_empty' }\n  ]\n};\n\nfunction validateFeature(record, contract) {\n  for (const rule of contract.quality_rules) {\n    if (rule.rule === 'not_null' && record[rule.field] === null) {\n      throw new Error(`Contract violation: ${rule.field} cannot be null`);\n    }\n  }\n  return true;\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-17",
        "questionId": "q-22977",
        "question": "Design a multi-tenant, region-aware streaming pipeline: per-region Kafka -> Flink with exactly-once, a central schema registry for a canonical schema, and per-tenant lakehouse partitions using MERGE upserts. Validate records against contracts, support late events via watermarking, and ensure tenant isolation and audit trails. Compare Flink vs Spark for latency and fault-tolerance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Canary-driven rollout approach: central registry, staging for new fields, 1–2% canaries to production, fail-fast QA, then gradual promotion. Ensure per-record lineage, idempotent upserts into Iceberg,",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a multi-tenant pipeline with per-region ingestion, central schema registry enforcing a canonical schema, and per-tenant MERGE upserts into the lakehouse. Use Flink for exactly-once processin",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Architect BU-isolated Iceberg tables; contracts bound to a central schema registry; enforce masking and retention with a policy engine; stream in Flink (exactly-once) and upsert into Iceberg; support",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a multi-tenant ingestion: enforce per-tenant contracts via a central Schema Registry (Avro) with backward/forward compatibility, publish to dedicated Kafka topics per stream, process with Flink",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design scalable, tenant-isolated streaming pipelines, contract management, late data handling, and trade-offs between Flink and Spark. It also touches governance, lineage, and monitoring.\n\n## Key Concepts\n- Multi-tenant isolation\n- Central schema registry\n- Canonical schema\n- MERGE upserts in lakehouse\n- Event-time watermarking\n- Per-tenant lineage and auditing\n- Exactly-once processing\n- Latency vs fault-tolerance trade-offs\n\n## Code Example\n```sql\nMERGE INTO analytics.tenant_events AS T\nUSING staging_events AS S\nON T.tenant_id = S.tenant_id AND T.event_id = S.event_id\nWHEN MATCHED THEN UPDATE SET T.payload = S.payload, T.event_time = S.event_time\nWHEN NOT MATCHED THEN INSERT (tenant_id, event_id, payload, event_time) VALUES (S.tenant_id, S.event_id, S.payload, S.event_time);\n```\n\n## Follow-up Questions\n- How would you test schema evolution without breaking older tenants?\n- What metrics indicate SLA breach and how would you alert?",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-18",
        "questionId": "q-21537",
        "question": "Ingest daily regional JSON feeds into a Parquet lake with a canonical schema. Validate per-record contracts (record_id, ingest_ts, region, payload fields with types), route non-conforming rows to a bad_data sink, and deduplicate by record_id before upserting into the canonical table partitioned by ingest_date. Describe a PySpark batch solution, a simple versioned schema registry to handle drift, and a concise test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a batch pipeline: use Spark to read daily 2GB JSON, apply per-record contracts, normalize to a canonical schema, and explode nested arrays into dimension rows; write Parquet in a lakehouse, pa",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a canonical Iceberg table with record_id as primary key. Validate: user_id (string, not null), event_time (timestamp), event_type (string). Normalize to canonical columns; optional payload mapped",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a multi-tenant streaming pipeline that ingests events from 12 SaaS customers, applies per-tenant field-level redaction based on configurable data policies—including PII handling and retention periods—stores both…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Read regional JSON with a strict Spark StructType; split conforming vs bad rows using from_json with the schema, pushing conforming rows to the canonical Parquet/Delta table partitioned by ingest_date",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nRationale: assess practical batch data validation, lineage, and drift handling in a beginner-friendly pipeline.\n\n## Key Concepts\n- Batch validation with Spark\n- Canonical schema and per-record contracts\n- Bad data quarantine\n- Upserts via deduplication and partitioning\n- Lightweight schema registry and drift tests\n\n## Code Example\n```python\nfrom pyspark.sql.types import StructType, StructField, StringType, TimestampType\nschema = StructType([\n  StructField(\"record_id\", StringType(), False),\n  StructField(\"ingest_ts\", TimestampType(), False),\n  StructField(\"region\", StringType(), False),\n  StructField(\"payload\", StringType(), True)\n])\ndf = spark.read.json(\"s3://regions/**/data.json\", schema=schema)\n# good rows satisfy contract; bad rows collected elsewhere\ngood = df.filter(\"record_id IS NOT NULL AND ingest_ts IS NOT NULL AND region IS NOT NULL\")\nbad = df.subtract(good)\ngood.write.partitionBy(\"ingest_date\").mode(\"append\").parquet(\"s3://lake/canonical\")\n```\n\n## Follow-up Questions\n- How would you extend to more regions or evolving fields?\n- How do you automate schema drift detection and tests in CI?",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-19",
        "questionId": "q-27648",
        "question": "Two regional JSON feeds must land in a lakehouse via a manifest-driven incremental loader that guarantees exactly-once semantics using a record_id primary key; handle out-of-order arrivals up to 15 minutes with a re-ingest window and tombstones for deletes; describe implementation details, an observable data contract, and a minimal test plan. Compare batch vs streaming approaches for this workload?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per‑vendor schema registry to map evolving JSON to a canonical flat schema, then upsert into Iceberg using a MERGE on a canonical primary key. Support late events up to 6 hours with watermark",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Flink with a staging area and Iceberg target. Validate each record against a central schema registry; partition by region; implement 24h late-data handling with reprocessing; apply idempotent upse",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a multi-tenant Iceberg-based lakehouse with per-tenant namespaces, a central schema registry for per-record contracts, and a 60-minute late-data window via watermarking. Upsert into a star sch",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a manifest-driven loader using a record_id key for de-dup, writing to a write-ahead manifest and committing atomically. Use idempotent upserts into a star schema; support re-ingest window for",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design a deterministic ingestion approach that stays beginner-friendly while addressing out-of-order data, deduplication, and lineage without diving into heavy streaming complexity.\n\n## Key Concepts\n- Manifest-driven loader for exactly-once semantics\n- Primary-key deduplication and upserts into a lakehouse\n- Late-arrival handling with a re-ingest window and tombstones\n- Basic data contract validation and lightweight QA checks\n\n## Code Example\n```javascript\n// Pseudo: update manifest after consuming record_id; upsert into star schema; re-ingest window logic\n```\n\n## Follow-up Questions\n- How would you validate end-to-end correctness? \n- What monitoring would you add for failure scenarios and recovery?",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-20",
        "questionId": "q-25430",
        "question": "Design a beginner-friendly batch pipeline to ingest a daily 120MB regional CSV feed into a data lake. The CSV has fields: user_id, event_ts (ISO 8601), action, region, and optional metadata. Enforce per-record contracts: user_id non-empty, event_ts parseable, action non-null; region and metadata optional. Normalize to a canonical schema, deduplicate by (user_id, event_ts), and write Parquet to a lakehouse partitioned by date/region. Include a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A beginner-friendly batch pipeline for ingesting a daily 120MB CSV feed into a data lake involves the following components: **1. Schema Definition & Validation** Define a canonical schema enforcing contracts: - user_id:…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Create a lightweight Python data validator using pandas: read the CSV, filter with conditions (order_id notnull, region in ['NA','EU','APAC'], order_ts parseable, total >= 0). Write valid rows to the",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a concrete end-to-end approach: choose between MongoDB Change Streams and Debezium, design per-region tenants with isolated streams, and upsert into an Iceberg lakehouse. Handle tombstone dele",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a regional-first ingest into Iceberg lakehouses partitioned by country/date, using exactly-once streaming (Flink preferred; Spark for batch). Achieve idempotent upserts via MERGE INTO, support",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses fundamental batch ETL skills, data quality enforcement, and practical thinking for modern lakehouse storage architectures. It tests whether a candidate can handle end-to-end data pipeline design with proper validation and error handling.\n\n## Key Concepts\n- **Batch ETL**: Processing data in scheduled batches rather than streaming\n- **Schema Enforcement**: Validating per-record contracts to ensure data quality\n- **Canonical Schema Normalization**: Standardizing field names and types across sources\n- **Deduplication Strategies**: Using composite keys (user_id, event_ts) to identify and remove duplicates\n- **Data Quality & Bad Record Routing**: Separating valid records from invalid ones for downstream investigation\n- **Lakehouse Partitioning**: Organizing Parquet files by date and region for efficient queries\n- **Parquet Format**: Columnar storage with built-in compression and schema evolution support\n\n## Expected Answer Elements\nA strong response should include: schema definition with field-level contracts, pipeline stages (read → validate → normalize → dedupe → write), handling of invalid records, partitioning strategy, and basic test coverage. The code example demonstrates these principles using PySpark.",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-21",
        "questionId": "q-18873",
        "question": "Design a streaming + batch pipeline for a multi-tenant analytics lakehouse. Ingest from 20 regions with evolving schemas; enforce per-row contracts via a central schema registry; mask PII at ingest; write to Iceberg partitioned by tenant and region. Use idempotent MERGE upserts, support dynamic schema evolution, and late data windows. Define data-quality gates and a cross-tenant sharing policy that preserves isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a streaming + batch pipeline for a multi-tenant analytics lakehouse that ingests from 20 regions with evolving schemas. Enforce per-row contracts via a central schema registry, mask PII at ingest, and write to Ic…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Load nightly customers.csv into a staging table, then MERGE into customers_dim on customer_id using the latest row per id (based on signup_date or an updated_at column). Update non-key fields; insert",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose Pub/Sub protobuf events into a Spark pipeline writing to Iceberg lakehouse. Use idempotent upserts with key (user_id, event_id), MERGE into lake. Implement GDPR deletes with tombstones and a p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a PySpark batch job to read three regional CSV files, apply a column mapping layer to transform regional columns into a canonical schema, and enforce per-record contracts (user_id, event_ts, and action must be no…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests your ability to design complex multi-tenant data systems at scale, covering governance, streaming and batch processing, schema evolution, and privacy compliance.\n\n## Key Concepts\n- **Multi-tenant isolation** with cross-tenant sharing policies that preserve data isolation\n- **Schema registry** with per-row contracts and dynamic schema evolution\n- **Iceberg partitioning** by tenant and region for efficient queries\n- **Idempotent MERGE upserts** handling late-arriving data within configurable windows\n- **PII masking** applied at ingest using transformation functions\n- **Data-quality gates** using frameworks like Great Expectations or Deequ\n\n## Code Example\n```sql\nMERGE INTO lake.tenants.t_events AS t\nUSING staged_events AS s\nON t.tenant_id = s.tenant_id \n  AND t.event_id = s.event_id \n  AND t.region = s.region\nWHEN MATCHED THEN UPDATE SET *\nWHEN NOT MATCHED THEN INSERT *\n```\n\n## Follow-up Questions\n- How would you handle schema conflicts between tenants with overlapping field names?\n- What strategies would you use for cost optimization across tenants with varying data volumes?\n- How do you implement row-level security for cross-tenant analytics queries?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-22",
        "questionId": "q-23556",
        "question": "Design a beginner-friendly batch pipeline to ingest 4 regional CSV feeds with varying column orders into a canonical Parquet lakehouse, enforcing per-record contracts (user_id and event_time non-null; event_type in {click, view, purchase}); route non-conforming rows to bad_data; normalize to canonical schema: (user_id, event_time, event_type, region, channel, metadata). Address schema drift when a new optional column appears and sketch a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a versioned canonical schema in a registry; create per-tenant Iceberg tables for raw, canonical, and materialized views. Stream sources into staging with event-time watermarks and a 60-minute lateness tolerance, the…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a per-tenant schema registry with contract validation at ingest, store data in Iceberg with per-tenant partitioning, and perform idempotent MERGE UPSERTs. Enable dynamic schema evolution with comp",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Ingest 4 regional CSVs, standardize columns to a canonical schema, validate per-record contracts (user_id/event_time non-null, event_type in allowed set), and write to Parquet lakehouse. Send non-conf",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement Kafka transactions with idempotent producers (enable.idempotence=true), use database transaction IDs for deduplication, commit offsets only after successful DB commit, and configure EOS=ALWAYS for exactly-once…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests batch ingestion, per-record validation, and schema normalization while handling drift and bad data routing. Encourages practical thinking about test strategies early.\n\n## Key Concepts\n- Batch data ingestion from multiple regions\n- Canonical schema design\n- Per-record data contracts and validation\n- Schema drift handling with optional fields\n- Bad data routing and observability\n\n## Code Example\n```python\n# Pseudo-code for mapping and validating a row\ndef map_row(row, region):\n  user_id = row.get('user_id') or row.get('uid')\n  event_time = parse_datetime(row.get('event_time'))\n  event_type = row.get('event_type')\n  if not user_id or not event_time or event_type not in {'click','view','purchase'}:\n    return None\n  channel = row.get('channel')\n  metadata = row.get('metadata', {})\n  return {'user_id': user_id, 'event_time': event_time, 'event_type': event_type,\n          'region': region, 'channel': channel, 'metadata': json.dumps(metadata)}\n```\n\n## Follow-up Questions\n- How would you implement and monitor the bad_data sink in your chosen framework?\n- How would you design unit tests and an end-to-end test to cover drift scenarios?",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-23",
        "questionId": "q-488",
        "question": "You're building a real-time analytics pipeline for a food delivery app. How would you design a data pipeline to process 1M events/day with 5-minute latency, considering data quality, schema evolution, and cost optimization?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a lightweight schema registry to enforce per-record contracts on each feed: required partner_id, event_ts, user_id; optional region, device. Ingest into Iceberg tables per feed, emit a lineage row",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a batch processing pipeline using Python with PySpark to ingest regional JSON logs. The pipeline reads JSON files from the input directory, validates each record against a contract requiring service_id, timest…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Architect a streaming to lakehouse with per-tenant contracts: enforce a canonical schema per tenant via a registry, apply real-time PII redaction at ingest, partition Iceberg by tenant_id, and impleme",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Kafka + Flink for stream processing with exactly-once semantics. Implement schema registry for evolution, use Debezium CDC for change capture. Store raw events in S3, processed data in Redshift. Use schema validatio…",
            "isCorrect": true
          }
        ],
        "explanation": "## Architecture\n- **Ingestion**: Apache Kafka with 3 partitions, replication factor 3\n- **Processing**: Apache Flink for windowed aggregations and joins with exactly-once semantics\n- **Storage**: Raw events in S3 (Parquet format), processed data in Redshift\n\n## Data Quality & Schema Evolution\n- Schema validation using Confluent Schema Registry\n- Data quality checks with Great Expectations\n- Dead letter queue for failed events\n- Debezium CDC for reliable change data capture\n\n## Cost Optimization\n- Spot instances for Flink cluster\n- S3 Intelligent Tiering for raw data\n- Redshift RA3 nodes for compute-storage separation\n\n## Monitoring\n- CloudWatch metrics for pipeline health\n- Data freshness alerts",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-24",
        "questionId": "q-27159",
        "question": "Beginner-friendly pipeline: ingest two regional JSON feeds into an Iceberg lakehouse. Implement lightweight data quality with a small per-record contract (event_id, event_ts, device_id non-null; metric optional), validate with run-time checks (1% sample) using Great Expectations; route non-conforming rows to bad_data. Normalize to a canonical Iceberg schema with evolution, handle late data up to 2 hours via watermarking. Idempotent upserts into a star schema. Compare Spark Structured Streaming vs Flink and provide a minimal test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement Apache Iceberg tables as the target with a centralized schema registry (Avro/JSON) and versioned contracts. Ingest through a micro-batch layer that validates against the current contract, masks PII, and emits…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a streaming pipeline with a contract-validated sink: register schemas in a registry, validate against per-record contracts, stage in region-specific micro-batches, and apply idempotent MERGE in",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Spark Structured Streaming to read both regional JSON feeds. Enforce a small per-record contract (event_id string, event_ts timestamp, device_id string non-null). Run lightweight runtime checks (1",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use Spark Structured Streaming to read both JSON streams into Iceberg tables. Validate contracts with a simple schema enforcement (device_id: string, event_ts: timestamp, metric: double, non-null). Im",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical ingestion with data quality, schema evolution, and upsert basics. It also introduces a beginner-friendly comparison of two popular engines.\n\n## Key Concepts\n- Lightweight data contracts at ingest\n- Data quality with Great Expectations\n- Iceberg canonical schema and evolution\n- Watermarking for late data\n- Idempotent upserts into a star schema\n- Observability and drift detection basics\n- Spark vs Flink trade-offs\n\n## Code Example\n```javascript\n// Pseudo: validate and upsert steps\n```\n\n## Follow-up Questions\n- How would you extend checks for schema drift over time?\n- What metrics would you surface for reliability?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-07-23T05:25:28.082Z",
    "version": 1
  }
]