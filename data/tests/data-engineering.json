[
  {
    "id": "test-data-engineering",
    "channelId": "data-engineering",
    "channelName": "Data Engineering",
    "title": "Data Engineering Knowledge Test",
    "description": "Test your understanding of Data Engineering with 25 questions.",
    "questions": [
      {
        "id": "data-engineering-q-0",
        "questionId": "q-14509",
        "question": "Design a cryptographically verifiable data lineage system for multi-region lakehouse ingestion. Describe per-record lineage hashes, immutable audit logs, region isolation, and idempotent upserts into Iceberg under dynamic schema evolution. Include a MERGE example and a testing plan for end-to-end verification?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: stream from Kafka; enforce per-tenant contracts via a central registry; on ingest, fetch attachment metadata, compute/verify checksums, and store in a side table. Merge into Iceberg on (tenant,e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a manifest-driven loader using a record_id key for de-dup, writing to a write-ahead manifest and committing atomically. Use idempotent upserts into a star schema; support re-ingest window for",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Flink to stream JSON events from Kafka, apply per-device YAML contracts from a contracts store, validate, and project to Iceberg. Emit per-record lineage to a lineage table (source_id, device_id,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cryptographically verifiable lineage system for multi-region lakehouse ingestion. Describe per-record lineage hashes, immutable audit logs, region isolation, and idempotent upserts into Icebe",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nUnderstanding data lineage, integrity, and governance in scalable pipelines across regions is critical for audits and reliability. This question probes how to implement verifiable lineage while accommodating schema drift and performance.\n\n## Key Concepts\n- cryptographic lineage hashes\n- immutable audit logs\n- per-region isolation\n- idempotent upserts and MERGE patterns\n- dynamic schema evolution and registry\n- end-to-end verification testing\n\n## Code Example\n```sql\nMERGE INTO lakehouse.main AS m\nUSING (SELECT tenant_id, record_id, payload, version FROM staging) AS s\nON m.tenant_id = s.tenant_id AND m.record_id = s.record_id\nWHEN MATCHED AND m.version < s.version THEN\n  UPDATE SET payload = s.payload, version = s.version, hash = s.hash\nWHEN NOT MATCHED THEN\n  INSERT (tenant_id, record_id, payload, version, hash) VALUES (s.tenant_id, s.record_id, s.payload, s.version, s.hash);\n```\n\n## Follow-up Questions\n- How would you securely manage region salts and the registry across teams?\n- How would you test end-to-end verification at scale across 10s of regions?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-1",
        "questionId": "q-19215",
        "question": "You operate a nationwide real-time sensor network emitting JSON events with fields sensor_id, ts, region, metrics (nested with optional fields). Ingest into a lakehouse (Iceberg) with per-region isolation and dynamic metric schema. Must support idempotent upserts, late data up to 6 minutes, and per-record data contracts via a registry. Design end-to-end pipeline, plus a simple MERGE example and monitoring plan; compare Spark Structured Streaming vs Flink for processing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "I would architect a multi-tenant ML feature store with per-tenant Iceberg namespaces for strict isolation, implementing a unified ingestion layer that handles both batch CSVs and streaming Kafka topics through a common…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a comprehensive pipeline that unifies data warehouse and ML feature store capabilities using Apache Iceberg as the storage layer. Implement a versioned feature registry with schema evolution controls, storing bot…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt a canonical schema in a central registry and tag each event with experiment_id, variation_id, region, and event_time. Use idempotent MERGE into Iceberg; partition by region/date; buffer late eve",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Central schema registry with versioned Avro/Protobuf; region-isolated Iceberg tables; idempotent upserts via MERGE INTO on (sensor_id, ts) with a contract_id for dedupe. Ingest with 6-minute lateness",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design streaming data contracts, dynamic schemas, multi-region isolation, and late data handling with idempotent upserts to Iceberg, plus practical trade-offs between Spark and Flink.\n\n## Key Concepts\n- Data contracts and schema evolution\n- Idempotent upserts in Iceberg\n- Per-region isolation and lineage\n- Watermarks and late data handling\n- Monitoring and testing\n\n## Code Example\n```javascript\n// Pseudo-SQL for Iceberg MERGE (illustrative)\nMERGE INTO iceberg.sensor_events AS t\nUSING staged_sensor_events AS s\nON t.sensor_id = s.sensor_id AND t.ts = s.ts\nWHEN MATCHED THEN UPDATE SET t.metrics = s.metrics\nWHEN NOT MATCHED THEN INSERT (sensor_id, ts, region, metrics) VALUES (s.sensor_id, s.ts, s.region, s.metrics);\n```\n\n## Follow-up Questions\n- How would you test contract upgrades without downtime?\n- How to handle clock skew across regions?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-2",
        "questionId": "q-22157",
        "question": "Design a multi-tenant streaming ingest to a lakehouse and feature store for a global e-commerce platform ingesting 1M JSON events/sec with nested fields and PII. Specify per-tenant isolation, GDPR purge, schema evolution, and ML-friendly feature extraction. Choose between Flink and Spark for processing and outline end-to-end testing, monitoring, data quality checks, and data lineage?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a multi-tenant streaming ingest to a lakehouse and feature store for a global e-commerce platform ingesting 1M JSON events/sec with nested fields and PII. Specify per-tenant isolation, GDPR purge, schema evolutio…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a daily Python streaming job that ingests JSONL files containing user records with PII fields, redacts sensitive data (email, phone) using salted hash tokenization, persists both redacted fields and tokens for…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a hybrid pipeline: streaming with Flink writing to Iceberg, using idempotent upserts on (region, event_id), watermarking for late data, and Iceberg schema evolution; batch reconciliation with",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a beginner pipeline ingesting 2 regional JSON streams into Iceberg. Enforce per-record contract: device_id, event_ts, metric non-null; optional fields allowed. Route bad records to a separate",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes candidates on enterprise-scale distributed systems design, specifically: (1) handling high-throughput streaming at scale, (2) implementing multi-tenant security and isolation, (3) complying with data privacy regulations like GDPR, (4) integrating with ML infrastructure via feature stores, and (5) operational concerns including testing, monitoring, and data governance.\n\n## Key Concepts\n\n- **Scale**: Streaming 1M events/sec with deeply nested JSON payloads\n- **Schema Management**: Canonical schema registry with evolution support\n- **Isolation**: Strict per-tenant isolation with logical or physical separation\n- **Compliance**: GDPR purge capabilities with cascading deletes across lakehouse and feature store\n- **Storage**: Iceberg lakehouse format enabling ACID transactions, time travel, and schema evolution\n- **ML Integration**: Feature store for serving fresh and batch features to ML models\n- **Engine Choice**: Flink for low-latency event processing vs. Spark for throughput-heavy batch micro-batching\n- **Operations**: Comprehensive testing, observability, data quality, and lineage tracking\n\n## Code Example\n\n```java\n// Flink-based processing with validation and upsert\nDataStream<Event> events = env.addSource(new KafkaSource<>(\"ecommerce-events\"))\n    .process(new PiiRedactionProcessor())\n    .keyBy(event -> event.getTenantId())\n    .process(new TenantIsolatedProcessor())\n    .map(event -> {\n        Schema schema = schemaRegistry.get(event.getType(), event.getSchemaVersion());\n        return schema.validate(event)\n            ? transformToFeature(event, schema)\n            : null;\n    })\n    .sinkTo(IcebergSink.forRow(data -> new PartitionSpec(...)));\n\n// GDPR purge via batch job\nspark.read().load(\"s3://lakehouse/tenants/{tenantId}\")\n    .filter(row => !row.col(\"deleted_at\").isNull)\n    .write()\n    .mode(\"overwrite\")\n    .save(\"s3://lakehouse/tenants/{tenantId}_purged\");\n```",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-3",
        "questionId": "q-13210",
        "question": "CDC pipeline from Postgres using Debezium to a lakehouse: stream changes for 20 tables into Kafka; data volumes 5-10M rows/day; rows contain required fields plus nested payloads. Ensure per-record lineage, tenant isolation, dynamic schema evolution, and support for deletes. Design end-to-end architecture using Spark Structured Streaming or Flink with Iceberg; ensure idempotent upserts, correct MERGE semantics for SCD Type 2, and testing/monitoring plan. Provide a concrete MERGE example for a user_dim table?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage Debezium CDC to stream changes into Kafka, process with Spark or Flink, and write to Iceberg with tenant_id isolation. Attach a stable record_id for per-record lineage; implement SCD2 using MERGE semantics for…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a streaming per-region lake with staging and Iceberg. Validate each event against a central contract, upsert with MERGE INTO region USING staging ON id WHEN MATCHED THEN UPDATE WHEN NOT MATC",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a data pipeline using Apache Kafka to ingest 1M ride events per minute with partitioning by ride_id, Apache Flink for real-time stream processing with windowed aggregations, and write optimized Parquet files to S…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Architect a streaming IoT telemetry pipeline: decode optional base64 payloads, apply schema evolution via a registry, and upsert into Iceberg; enforce region-based masking and GDPR delete hooks; imple",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests end-to-end CDC ingestion, lakehouse storage, and lineage with real-world schema drift and deletes.\n\n## Key Concepts\n- Debezium CDC, Kafka integration\n- Iceberg MERGE for SCD2 and deletes\n- Per-record lineage via record_id and source metadata\n- Tenant isolation and nested payload handling\n- Schema evolution and data contracts\n\n## Code Example\n```sql\nMERGE INTO lakehouse.public.user_dim AS t\nUSING staged_user_dim AS s\nON t.user_id = s.user_id\nWHEN MATCHED AND (t.name <> s.name OR t.email <> s.email) THEN\n  UPDATE SET t.name = s.name, t.email = s.email, t.valid_to = s.valid_from\nWHEN MATCHED AND s.__deleted = true THEN\n  UPDATE SET t.valid_to = s.valid_from\nWHEN NOT MATCHED THEN\n  INSERT (user_id, name, email, tenant_id, record_id, valid_from, valid_to)\n  VALUES (s.user_id, s.name, s.email, s.tenant_id, s.record_id, s.valid_from, NULL)\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-4",
        "questionId": "q-13621",
        "question": "Ingest streaming JSON events from a fleet of edge devices via MQTT bridge to Kafka; events have dynamic nested fields and a company-specific 'extras' map, with per-device contracts defined in YAML. You need to materialize into a lakehouse (Iceberg) with per-record lineage, idempotent upserts, and late data up to 2 hours. How would you design: (1) runtime data contracts loaded from YAML, (2) validation/transform pipeline, (3) lineage capture across stages, (4) a concrete MERGE that upserts a device_state table from incoming events, including handling of new/removed fields? Also discuss testing/monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Canonical schema via registry; region-scoped staging in object storage; per-record contracts with validation; deduplicate; GPU-accelerated feature extraction (embeddings) on streaming data to a centra",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose a multi-tenant Iceberg-based lakehouse with per-tenant namespaces, a central schema registry for per-record contracts, and a 60-minute late-data window via watermarking. Upsert into a star sch",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "I would architect a multi-tenant ML feature store with per-tenant Iceberg namespaces for strict isolation, implementing a unified ingestion layer that handles both batch CSVs and streaming Kafka topics through a common…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Flink to stream JSON events from Kafka, apply per-device YAML contracts from a contracts store, validate, and project to Iceberg. Emit per-record lineage to a lineage table (source_id, device_id,",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests a new angle: runtime per-device contracts, end-to-end governance, and lineage in a scalable streaming pipeline.\n\n## Key Concepts\n- Runtime data contracts (YAML) per device\n- Idempotent upserts into Iceberg\n- Per-record lineage across pipeline stages\n- Late data handling with watermarking\n- Testing: contract tests, end-to-end replays, monitoring dashboards\n\n## Code Example\n```javascript\n// Pseudo: load contract, validate, map, emit lineage, upsert via MERGE\n```\n\n## Follow-up Questions\n- How would you handle contract version migrations?\n- How to verify lineage accuracy at scale?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-5",
        "questionId": "q-24409",
        "question": "Ingest a daily 500GB of region-tagged JSON events into an Iceberg lakehouse. Each event must contain region, session_id, event_ts, user_id, action; metadata is optional. Describe a beginner-friendly pipeline that enforces per-record contracts, performs idempotent upserts into a canonical Iceberg table partitioned by region/date, and detects schema drift within a 24-hour window. When drift is detected, route affected records to a quarantine table for review and outline a minimal test plan with a sample MERGE statement?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Spark Structured Streaming to read the JSON, validate required fields (region, session_id, event_ts, user_id, action), route non-conforming rows to bad_data, then upsert into an Iceberg table part",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Build a policy-driven masking layer in the streaming path, sourcing consent and region from a policy service. Apply per-record field masking before writing to Iceberg, tag each row with policy_id for",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a central per-tenant contract registry and a canonical schema; stream with Flink for exactly-once, writing to Iceberg with tenant/region partitions; MERGE upserts into a canonical table; enforce r",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Enforce per-region data contracts via a schema registry and pre-ingest validation; buffer late rows for up to 12h; use Iceberg MERGE for idempotent upserts and region-tenant deletes to honor GDPR, tri",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design end-to-end data ingestion with contracts, idempotent upserts, drift handling, and minimal testing.\n\n## Key Concepts\n- Per-record contracts and bad_data routing\n- Iceberg upserts and region/date partitioning\n- Lightweight schema registry for canonical schema\n- Schema drift detection and quarantining drifted data\n\n## Code Example\n```sql\nMERGE INTO iceberg_db.events AS t\nUSING staging AS s\nON t.region = s.region AND t.session_id = s.session_id\nWHEN MATCHED THEN UPDATE SET t.event_ts = s.event_ts, t.user_id = s.user_id, t.action = s.action, t.metadata = s.metadata\nWHEN NOT MATCHED THEN INSERT (region, session_id, event_ts, user_id, action, metadata) VALUES (s.region, s.session_id, s.event_ts, s.user_id, s.action, s.metadata);\n```\n\n## Follow-up Questions\n- How would you test drift detection reliability with synthetic data?\n- What metrics would you expose for monitoring the pipeline health?",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-6",
        "questionId": "q-8996",
        "question": "Design a nightly ingest for 4 regional JSON feeds into an Iceberg lakehouse. Enforce per-record contracts: required fields user_id, event_ts, event_type; optional fields allowed; implement a privacy masking policy for payload fields (mask PII unless whitelisted). Route non-conforming rows to a bad_data sink, support schema evolution for new attributes, and perform idempotent upserts into a star schema. Include a minimal code snippet showing contract check and a MERGE-based upsert, and outline a basic test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a real-time feature store pipeline fed by 3 market feeds (tick, quote, event) at ~15k events/sec. Each event carries symbol, ts, and features. Emit per-record lineage to a metadata store; upser",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a canonical Iceberg table with record_id as primary key. Validate: user_id (string, not null), event_time (timestamp), event_type (string). Normalize to canonical columns; optional payload mapped",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Approach: validate each JSON row against a strict contract; mask PII in payload unless allowed; route non-conforming rows to bad_data; upsert conformant rows into the Iceberg-backed star schema with a",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Design a dual-path pipeline: streaming ingestion into an Iceberg lakehouse for offline features and a low-latency online store (per-tenant). Normalize nested JSON to a shared schema, enforce per-recor",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests practical batch ingestion, per-record contracts, privacy masking, and idempotent upserts in a common lakehouse pattern.\n\n## Key Concepts\n\n- Data contracts, masking, bad_data routing\n- Iceberg schema evolution, upserts, unique keys\n- Test plans for duplicates, new fields, and schema drift\n\n## Code Example\n\n```javascript\nfunction validate(record) {\n  const required = [\"user_id\",\"event_ts\",\"event_type\"];\n  for (const k of required) if (!(k in record)) return false;\n  // simple mask\n  if (record.payload) record.payload.pii = null;\n  return true;\n}\n```\n\n```javascript\n// MERGE-like upsert (pseudocode)\nMERGE INTO iceberg.fact AS t\nUSING staging AS s\nON t.user_id = s.user_id AND t.event_ts = s.event_ts\nWHEN MATCHED THEN UPDATE SET ...\nWHEN NOT MATCHED THEN INSERT ...\n```",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-7",
        "questionId": "q-17591",
        "question": "Ingest partner-specific JSON logs from multiple SaaS vendors into a lakehouse. Each vendor uses a distinct and evolving JSON schema. Design a per-vendor schema registry, a canonical flattened schema, and an idempotent upsert into Iceberg. Include late-arriving events up to 6 hours, per-record lineage, and a concrete MERGE example. Compare Spark Structured Streaming vs Flink for this workload and outline testing and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a per‑vendor schema registry to map evolving JSON to a canonical flat schema, then upsert into Iceberg using a MERGE on a canonical primary key. Support late events up to 6 hours with watermark",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a batch pipeline: use Spark to read daily 2GB JSON, apply per-record contracts, normalize to a canonical schema, and explode nested arrays into dimension rows; write Parquet in a lakehouse, pa",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a streaming path using Spark Structured Streaming or Flink to read both regional JSON streams, enforce per-record contracts, redact PII (hash emails with SHA-256, mask phones), and upsert into",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "I propose a batch Spark job (Spark 3.x) over Iceberg that reads nightly regional JSON files, enforces data contracts (required fields: user_id, event_ts; metric must be non-null; other fields optional), detects schema d…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to manage heterogeneous, evolving schemas across tenants, enforce contracts, and implement robust upserts with lineage and late data handling. \n\n## Key Concepts\n- Per‑vendor schema registry and dynamic mapping \n- Canonical schema and flattening \n- Idempotent upserts into Iceberg \n- Late data handling with watermarking \n- Per‑record lineage and provenance \n- Spark vs Flink trade-offs in this pattern \n\n## Code Example\n```javascript\n// Pseudo-code: map vendor JSON to canonical and MERGE into Iceberg\nconst canonical = mapToCanonical(vendorJson, vendorId);\nMERGE INTO partner_events AS t\nUSING staged_partner_events AS s\nON t.id = s.id\nWHEN MATCHED THEN UPDATE SET t.payload = s.payload\nWHEN NOT MATCHED THEN INSERT VALUES (...);\n```\n\n## Follow-up Questions\n- How would you version and evolve the canonical schema without breaking existing pipelines? \n- How would you monitor schema drift and automatically quarantine failing records?",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-8",
        "questionId": "q-12108",
        "question": "Ingest a daily CSV feed of product data (product_id, price, timestamp, category, plus optional fields) into a lakehouse. Build a tiny declarative data-quality engine (YAML) to enforce per-record contracts (product_id and timestamp non-null, price >= 0; optional fields allowed). Deduplicate on (product_id, timestamp). Support late data up to 6 hours with watermark and upsert into Iceberg. Route bad rows to bad_data. Provide a minimal test plan and compare Spark vs Flink?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: stream from Kafka; enforce per-tenant contracts via a central registry; on ingest, fetch attachment metadata, compute/verify checksums, and store in a side table. Merge into Iceberg on (tenant,e",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Spark-based ingestion: read regional CSVs, write to Iceberg per-region partitions, enforce per-record contracts (vendor_id, event_time, amount non-null; optional fields allowed). Route non",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a daily CSV-to-lakehouse ingest using a YAML-driven rule engine for per-record contracts: ensure product_id and timestamp non-null, price >= 0, with optional fields allowed. Deduplicate on (pro",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose a fault-tolerant streaming quality layer that computes per-tenant scores in real time (completeness, validity, timeliness), routes records below threshold to a bad_data sink, and preserves lin",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nA practical test of turning data quality and dedup into a maintainable pipeline with a declarative rule layer.\n\n## Key Concepts\n- Declarative data-quality rules (YAML)\n- Idempotent upserts into Iceberg\n- Dedup by composite key\n- Watermarks for late data\n- Observability and tests\n\n## Code Example\n```javascript\n// Pseudo: load rules, validate row, apply MERGE into Iceberg\n```\n\n## Follow-up Questions\n- How would you evolve rules for nested/optional fields?\n- How would you measure backlog and alert on failures?",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-9",
        "questionId": "q-11694",
        "question": "Design a real-time feature store pipeline to support a 1B-event/day user activity dataset for a recommender system. Requirements: 1s online latency, daily offline batch refresh, per-record data contracts, schema drift handling, GDPR delete requests, and event-time semantics. Propose end-to-end architecture using Iceberg for offline, Flink for streaming, Redis as online store, plus data governance and a test plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Central schema registry defines canonical payload (transaction_id PK). Ingest from five PSPs by region using Spark Structured Streaming with 10-minute watermark and 10-minute lateness. Iceberg per-reg",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Iceberg for offline feature storage; Flink streaming computes features with event-time and watermarks; write latest feature values to Redis for near-instant inference. Enforce per-record contracts",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "I would implement a batch ETL that reads daily regional JSONs, validates each record against a canonical schema (user_id: string, event_ts: timestamp, action: enum, context: map<string, any> | null),",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Iceberg partition pruning and metadata caching to read only relevant partitions; drive tiering with a policy engine (OPA) tagging data by tenant and age, moving files to hot S3 for recent data, wa",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nEvaluate ability to design a scalable online/offline feature store with strict data contracts, drift governance, and privacy controls, plus testability at scale.\n\n## Key Concepts\n- Real-time feature store\n- Iceberg offline, Redis online\n- Flink event-time processing and watermarks\n- Schema drift handling and per-record contracts\n- GDPR tombstones and backfill strategy\n\n## Code Example\n```yaml\nfeature_store:\n  offline: iceberg\n  online: redis\n  compute:\n    - flink\n  contracts:\n    mode: per-record\n```\n\n## Follow-up Questions\n- How would you verify feature drift across offline/online stores?\n- What tests would you automate for GDPR deletions and backfills?",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-10",
        "questionId": "q-16943",
        "question": "Design a practical end-to-end pipeline for a multi-tenant data platform ingesting 60 regional JSON feeds (total ~15TB/day), each with evolving schemas, enforcing per-record contracts, tenant isolation, and time-travel analytics. Describe data validation, canonicalization, upserts, schema evolution, testing, and rollback strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a data-mesh architecture where each of the six services publishes its events as data products. Events flow through a schema registry that enforces canonical schemas and handles evolution. Route these events to a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Spark Structured Streaming to read both JSON streams into Iceberg tables. Validate contracts with a simple schema enforcement (device_id: string, event_ts: timestamp, metric: double, non-null). Im",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a multi-tenant pipeline with per-region ingestion, central schema registry enforcing a canonical schema, and per-tenant MERGE upserts into the lakehouse. Use Flink for exactly-once processin",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a per-tenant canonical schema with a schema registry and a validation stage that gates raw events into a tenant-scoped Iceberg (or Delta) table via idempotent upserts. Normalize vendor JSON",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests a multi-tenant governance model, quality contracts, and production safety in evolving data feeds.\n\n## Key Concepts\n\n- Schema Registry, per-tenant isolation, canonical schema, data contracts\n- Lakehouse storage with time travel (Iceberg/Delta), idempotent upserts\n- Regression tests, Canary data, rollback strategies\n\n## Code Example\n```javascript\n// pseudo validation against canonical JSON schema\nfunction validate(event, schema){\n  // jsonschema-like library\n  return true;\n}\n```\n\n## Follow-up Questions\n\n- How would you handle cross-tenant quota bursts and late-arriving data?\n- What testing strategy ensures contract drift is caught before load?",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-11",
        "questionId": "q-457",
        "question": "You need to process 10GB of CSV files daily and load them into a PostgreSQL database. The files contain user activity logs with timestamps, user IDs, and event types. How would you design an efficient ETL pipeline using Python?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use pandas with chunking for memory efficiency: `pd.read_csv('file.csv', chunksize=10000)`. Process each chunk, validate data types, convert timestamps with `pd.to_datetime()`, and use `psycopg2.extras.execute_batch()`…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use Spark Structured Streaming with an explicit schema: event_id, user_id, event_ts, event_type, payload (map<string,string>). Ingest from three regional folders, apply a 30-minute watermark on event_",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design an end-to-end multi-tenant telemetry pipeline for ride-hailing data that ingests 100 TB/day of JSON events from 12 microservices across 3 regions via Kafka. The solution must enforce strict region isolation, per-…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Ingest two regional CSV feeds into a Parquet lake using a canonical schema (region,id,event_ts,metric and optional fields). Implement a lightweight validator enforcing per-record contracts; non-confor",
            "isCorrect": false
          }
        ],
        "explanation": "## Key Components\n\n- **Memory Management**: Chunk large files to avoid memory issues\n- **Data Validation**: Check for missing values, correct data types\n- **Bulk Operations**: Use batch inserts instead of row-by-row\n- **Error Handling**: Log failed records for retry\n\n## Implementation Strategy\n\n```python\nimport pandas as pd\nimport psycopg2\nfrom psycopg2.extras import execute_batch\n\ndef process_csv_chunk(chunk):\n    # Clean and validate data\n    chunk['timestamp'] = pd.to_datetime(chunk['timestamp'])\n    chunk.dropna(inplace=True)\n    return chunk\n\ndef load_to_db(chunks):\n    conn = psycopg2.connect(database_url)\n    cursor = conn.cursor()\n    \n    for chunk in chunks:\n        processed_chunk = process_csv_chunk(chunk)\n        execute_batch(cursor, INSERT_QUERY, processed_chunk.values.tolist())\n    \n    conn.commit()\n    cursor.close()\n    conn.close()\n```",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-12",
        "questionId": "q-21312",
        "question": "You operate a global analytics platform ingesting streaming events from 12 SaaS customers. Each tenant has data policies (PII, retention) and needs field-level redaction in analytics. Design an end-to-end pipeline that enforces tenant policies, applies dynamic redaction rules, stores raw and redacted views in per-tenant Iceberg tables, supports retroactive policy updates, and provides audit lineage. Compare Spark Structured Streaming vs Flink for policy evaluation, and outline tests and governance checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a multi-tenant pipeline with per-region ingestion, central schema registry enforcing a canonical schema, and per-tenant MERGE upserts into the lakehouse. Use Flink for exactly-once processin",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a multi-tenant streaming pipeline that ingests events from 12 SaaS customers, applies per-tenant field-level redaction based on configurable data policies—including PII handling and retention periods—stores both…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Canary-driven rollout approach: central registry, staging for new fields, 1–2% canaries to production, fail-fast QA, then gradual promotion. Ensure per-record lineage, idempotent upserts into Iceberg,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a cryptographically verifiable lineage system for multi-region lakehouse ingestion. Describe per-record lineage hashes, immutable audit logs, region isolation, and idempotent upserts into Icebe",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question assesses the ability to design enterprise-grade multi-tenant data governance systems with dynamic policy enforcement, retroactive redaction capabilities, and regulatory compliance tracking in a lakehouse architecture.\n\n## Key Concepts\n- Multi-tenant isolation with per-tenant security boundaries\n- Policy engine with retroactive redaction via replay\n- Lakehouse storage architecture using Apache Iceberg\n- Exactly-once processing for audit integrity\n- Dynamic field-level redaction rules\n- Event sourcing for policy lineage\n- Spark Structured Streaming vs. Flink trade-offs\n\n## Common Pitfalls\n- Treating all tenants identically rather than respecting unique data policies\n- Overlooking retroactive policy enforcement mechanisms\n- Failing to maintain audit lineage for compliance requirements\n- Choosing a streaming framework without evaluating workload-specific needs\n\n## Evaluation Criteria\n- Clear multi-tenant isolation design with policy enforcement points\n- Solution for retroactive policy application via replay\n- Appropriate storage architecture supporting both raw and processed data\n- Framework selection rationale based on latency, state management, and processing guarantees\n- Comprehensive audit and lineage strategy",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-13",
        "questionId": "q-6549",
        "question": "In a multi-region telemetry pipeline ingesting 40k events/sec from Kafka into an Iceberg lakehouse, enforce per-record contracts, region isolation, and schema evolution with late data up to 8 minutes. Design end-to-end using a streaming engine, idempotent upserts, per-record lineage, a schema registry, and a testing/rollback plan. What would you implement and why?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a global real-time feature store for ML models across 3 regions with data residency. Use Iceberg for offline features and a per‑region online store for sub‑100ms reads. Implement feature versio",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-region Kafka topics, a Flink streaming job with exactly-once checkpoints, and Iceberg table upserts for idempotent writes. Centralize per-record contracts via a Schema Registry; encode lineage",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Adopt a canonical feature schema: tenant_id, feature_name, value, timestamp, and metadata. Isolate tenants using schema separation or row-level security (RLS). Implement incremental upserts into Apache Iceberg using MER…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a beginner-friendly pipeline that ingests 18 regional CSV feeds daily (≈200k rows/region) with fields user_id, event_ts, region, action, plus optional metadata. Enforce per-row contracts, supp",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nEvaluates practical capability to design scalable, region-isolated streaming pipelines with strict data contracts, lineage, and robust handling of late and potentially corrupt data.\n\n## Key Concepts\n\n- Exactly-once streaming (Flink/Kafka)\n- Iceberg upserts and schema evolution\n- Schema Registry and per-record contracts\n- Per-record lineage tracking\n- Late-arrival handling and data-quality gates\n\n## Code Example\n\n```python\ndef validate_event(event, contract):\n    required = contract['required']\n    for f in required:\n        if f not in event or event[f] is None:\n            return False\n    for k, t in contract['types'].items():\n        if k in event and not isinstance(event[k], t):\n            return False\n    return True\n```\n\n## Follow-up Questions\n\n- How would you implement per-record lineage across regional partitions?\n- What are the failure modes and how would you test rollback by time travel?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-14",
        "questionId": "q-4979",
        "question": "Ingest daily JSONL batches of video view events from a CDN into a lakehouse. Each event has required fields: view_id, user_id, video_id, timestamp; optional: device_type, country. Design a beginner-friendly end-to-end ETL to validate per-record contracts, perform idempotent upserts by view_id, and support schema evolution for new fields. Partition by date, handle late-arriving data via staging, and include basic tests and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "This pipeline performs batch ingestion of three regional JSON files, enforcing a strict per-record contract where region_id, event_id, ts, and metric are required fields—all other fields are optional. Records that fail…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Spark Structured Streaming with a strict JSON schema to ingest 60k/min across regions, enforce per-record contracts, and upsert into Iceberg via MERGE on event_id. Flatten payloads, apply a 15-min",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a 6-region streaming pipeline using Kafka with enforced contracts via a central JSON Schema Registry, storing data in an Iceberg lakehouse with region-based namespaces. Process late data up to 60 minutes using wa…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Spark batch ETL that reads daily JSONL batches from S3, enforces a per-record contract (view_id, user_id, video_id, timestamp required; device_type, country optional), deduplicates by view_id, and performs idempot…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical batch ETL design with data contracts, idempotent operations, and Iceberg schema evolution in a realistic, beginner-friendly scenario.\n\n## Key Concepts\n- JSONL batch ingestion and per-record validation\n- Deduplication by natural key with idempotent upserts\n- Iceberg table schema evolution capabilities\n- Date-based partitioning and staging for data safety\n- Essential testing patterns and monitoring metrics\n\n## Code Example\n```python\n# PySpark implementation sketch\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, to_date\n\nspark = SparkSession.builder.getOrCreate()\n\n# Read daily batch\ndf = spark.read.json(\"s3://cdn-batch/video-views/20260101/*.jsonl\")\n\n# Validate required fields\nvalidated = df.filter(\n    col(\"view_id\").isNotNull() & \n    col(\"user_id\").isNotNull() & \n    col(\"video_id\").isNotNull() & \n    col(\"timestamp\").isNotNull()\n)\n\n# Add date partition and deduplicate\nfinal_df = validated.withColumn(\"date\", to_date(col(\"timestamp\")))\\\n    .dropDuplicates([\"view_id\"])\n```",
        "difficulty": "beginner"
      },
      {
        "id": "data-engineering-q-15",
        "questionId": "q-19066",
        "question": "You operate a multi-tenant feature-usage telemetry pipeline (mobile apps) delivering 150k-300k events/sec. Each event includes tenant_id, feature_id, timestamp, and a nested payload of environment and rollout metadata. Design an end-to-end system that isolates tenant data, upserts into a lakehouse (Iceberg), handles dynamic schema evolution, detects feature drift and data quality issues, and honors GDPR delete requests. Compare Spark Structured Streaming vs Flink for the streaming path and outline testing and monitoring?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant, streaming-first telemetry path using Iceberg. Ingest events with tenant_id, feature_id, timestamp; canonicalize via schema registry; upsert using MERGE by (tenant_id, feature_id, timestamp",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a pipeline ingesting 50 TB/day of clickstream data from Kafka topics and batch feeds into a lakehouse. Use an idempotent upsert into Iceberg/Delta, with watermarking for late events, and schema",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Create a lightweight Python data validator using pandas: read the CSV, filter with conditions (order_id notnull, region in ['NA','EU','APAC'], order_ts parseable, total >= 0). Write valid rows to the",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a hybrid pipeline: streaming with Flink writing to Iceberg, using idempotent upserts on (region, event_id), watermarking for late data, and Iceberg schema evolution; batch reconciliation with",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a scalable, multi-tenant telemetry pipeline with evolving schemas, data contracts, and privacy controls. It probes upsert correctness, late data handling, and governance decisions under high-throughput conditions.\n\n## Key Concepts\n\n- Multi-tenant data isolation\n- Iceberg upserts and partitioning by tenant/date\n- Schema registry and dynamic evolution\n- Late data handling and windowing\n- GDPR delete semantics and purge workflows\n- Drift detection and data-quality checks\n- Streaming engine trade-offs (Spark vs Flink) and monitoring\n\n## Code Example\n\n```sql\nMERGE INTO lakehouse.feature_usage t\nUSING staged_batch s\nON t.tenant_id = s.tenant_id AND t.feature_id = s.feature_id AND t.timestamp = s.timestamp\nWHEN MATCHED THEN UPDATE SET t.env = s.env, t.meta = s.meta\nWHEN NOT MATCHED THEN INSERT (tenant_id, feature_id, timestamp, env, meta) VALUES (s.tenant_id, s.feature_id, s.timestamp, s.env, s.meta);\n```\n\n## Follow-up Questions\n\n- How would you ensure exactly-once semantics across restarts and retries?\n- How would you implement drift detection and alerting for feature usage patterns?\n- How would you test GDPR delete propagation end-to-end across storage and analytics layers?",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-16",
        "questionId": "q-19428",
        "question": "In a multi-region real-time analytics pipeline for a global payments platform, ingest 80k events/sec of transaction deltas from five PSP partners. Each event carries fields: transaction_id, user_id, region, event_ts, amount, currency, and optional risk_flags. Schema evolves often. Requirements: per-record contracts via a central schema registry, canonical schema, idempotent upserts into Iceberg, dynamic schema evolution, region isolation, late data up to 10 minutes, per-record lineage, and robust data-quality gates. Design end-to-end pipeline: streaming inlet, lakehouse, partitioning by region, and MERGE example; include monitoring and testing strategy, compare Spark Structured Streaming vs Flink for latency and fault-tolerance?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Central schema registry defines canonical payload (transaction_id PK). Ingest from five PSPs by region using Spark Structured Streaming with 10-minute watermark and 10-minute lateness. Iceberg per-reg",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a per-tenant schema registry with versioned contracts and a streaming processor that validates each event against its tenant’s current contract, routing to tenant-specific Snowflake partitions",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a privacy-preserving, multi-region data ingestion and analytics pipeline for a fintech platform that processes 200k nested JSON events/sec from 12 regional gateways. Ingest into a lakehouse (Iceberg) with per-rec…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a centralized JSON schema registry to enforce per-record contracts; partition Iceberg by tenant_id and device_id; upsert with MERGE INTO target USING source ON (tenant_id, device_id, event_id) WHE",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nRenders a realistic, scalable data-contract driven pipeline with evolving schemas, requiring robust idempotent upserts and per-record lineage across regions.\n\n## Key Concepts\n- Data contracts and central schema registry\n- Iceberg upserts and MERGE semantics\n- Watermarks, late data handling\n- Per-region data isolation and lineage\n- Spark vs Flink trade-offs\n\n## Code Example\n```sql\nMERGE INTO iceberg.region_sales t\nUSING staged.region_sales s\nON t.transaction_id = s.transaction_id\nWHEN MATCHED THEN UPDATE SET ...\nWHEN NOT MATCHED THEN INSERT (...)\n```\n\n## Follow-up Questions\n- How would you test contract drift across schema evolution?\n- How would you implement monitoring for data quality gates and lineage?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-17",
        "questionId": "q-30077",
        "question": "Ingesting device telemetry (GPS, user_id, device_id, timestamp) from 100k devices per second across regions. PII must be redacted at ingest while downstream analytics and models require per-record lineage and reidentification only with explicit consent. Design an end-to-end pipeline (streaming processor, central contract registry, Iceberg lakehouse) with row-level masking, tokenization keys, schema evolution, and late data handling up to 1 hour. Show a concrete MERGE example and a testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a central Iceberg lakehouse with regional staging and a schema registry to enforce per-record contracts. Ingest JSON via Kafka with exactly-once, upsert into Iceberg using MERGE; handle late arriv",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a multi-source ingestion pipeline that processes five regional CSV feeds from partner banks into an Iceberg lakehouse. The pipeline enforces per-record data contracts through a centralized JSON schema registry…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Tokenize PII at ingest using deterministic keys backed by a KMS, replacing user_id and device_id with pseudonyms while maintaining a separate lineage channel. Store pseudonym mappings and lineage records in Iceberg, ena…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Adopt a canonical feature schema: tenant_id, feature_name, value, timestamp, and metadata. Isolate tenants using schema separation or row-level security (RLS). Implement incremental upserts into Apache Iceberg using MER…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design privacy-preserving end-to-end pipelines with data contracts, lineage tracking, and controlled reidentification, along with appropriate testing strategies.\n\n## Key Concepts\n- PII tokenization at ingest using deterministic keys with KMS backing\n- Separation of pseudonymized data and lineage/identity records\n- Central schema registry with contract versioning\n- Upserts into Iceberg using MERGE semantics for deduplication\n- Late data handling with configurable backfill windows (up to 1 hour)\n- Contract testing across schema evolution\n\n## Code Example\n\n```sql\nMERGE INTO lakehouse.telemetry AS t\nUSING (\n  SELECT \n    event_id,\n    event_time,\n    event_source_region,\n    user_pseudonym,\n    device_pseudonym,\n    latitude,\n    longitude,\n    payload_signature\n  FROM staging.telemetry_staged\n) AS s\nON t.event_id = s.event_id\nWHEN MATCHED THEN UPDATE SET\n  t.user_pseudonym = s.user_pseudonym,\n  t.device_pseudonym = s.device_pseudonym,\n  t.latitude = s.latitude,\n  t.longitude = s.longitude,\n  t.last_updated = CURRENT_TIMESTAMP(),\n  t._processing_version = t._processing_version + 1\nWHEN NOT MATCHED THEN INSERT (\n  event_id, event_time, event_source_region,\n  user_pseudonym, device_pseudonym,\n  latitude, longitude, payload_signature,\n  ingested_at, last_updated, _processing_version\n) VALUES (\n  s.event_id, s.event_time, s.event_source_region,\n  s.user_pseudonym, s.device_pseudonym,\n  s.latitude, s.longitude, s.payload_signature,\n  CURRENT_TIMESTAMP(), CURRENT_TIMESTAMP(), 1\n);\n```\n\n## Testing Strategy\n- **Contract Tests**: Validate schema evolution by testing producer/consumer compatibility—p新老 consumers can read old data, new producers write valid schemas\n- **PII Redaction Tests**: Verify tokenization coverage with golden datasets containing known PII, asserting zero raw PII in downstream tables\n- **Lineage Tests**: End-to-end tests that ingest records, verify lineage channel completeness, and validate reidentification-only-with-consent constraints\n- **Late Data Tests**: Inject records with timestamps up to 1 hour old, asserting correct MERGE behavior and watermark handling\n- **Chaos Tests**: Simulate regional failures to validate exactly-once semantics and cross-region deduplication",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-18",
        "questionId": "q-20576",
        "question": "Context: Global fintech analytics, 2 TB/day across 6 regions. Streams: JSON events for transactions, user_activity, and device_meta. Must enforce per-record contracts via a central schema registry, support dynamic schema evolution, isolate data by region in Iceberg, and handle late data up to 60 minutes. Implement privacy masking/tokenization with consent, idempotent upserts with MERGE, and per-record lineage. Provide end-to-end design, include a concrete MERGE example, and compare Spark vs Flink plus testing/monitoring strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design end-to-end: read 2 TB/day JSON events, normalize timestamps to UTC, flatten selected nested fields, and upsert into Iceberg with per-record lineage. Use a data contract to validate optional fie",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a policy engine integrated with a regional consent store to redact or exclude PII fields at ingestion time. Ingest events via Kafka, validate against a dynamic schema registry, and transform to a canonical Ice…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Adopt a canonical schema in a central registry and tag each event with experiment_id, variation_id, region, and event_time. Use idempotent MERGE into Iceberg; partition by region/date; buffer late eve",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a 6-region streaming pipeline using Kafka with enforced contracts via a central JSON Schema Registry, storing data in an Iceberg lakehouse with region-based namespaces. Process late data up to 60 minutes using wa…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question evaluates the ability to design a privacy-conscious, scalable data pipeline with per-record contracts, dynamic schema evolution, and lineage across multiple regions.\n\n## Key Concepts\n- **Central schema registry**: Enforces contracts and manages schema evolution\n- **Region isolation**: Iceberg namespaces provide logical data partitioning\n- **Late data handling**: Watermarks and windowing accommodate data arriving up to 60 minutes late\n- **Idempotent upserts**: MERGE statements ensure exactly-once processing semantics\n- **Privacy enforcement**: Policy-driven masking and tokenization respect consent management\n- **Lineage tracking**: Metadata columns capture per-record provenance\n\n## Code Example\n```sql\nMERGE INTO iceberg_db.transactions t\nUSING staging_transactions s\nON t.transaction_id = s.transaction_id AND t.region = s.region\nWHEN MATCHED THEN UPDATE SET\n    t.amount = s.amount,\n    t.status = s.status,\n    t.updated_at = s.event_time,\n    t.lineage_source = s.lineage_source\nWHEN NOT MATCHED THEN INSERT (\n    transaction_id, region, user_id, amount, status,\n    event_time, processed_at, lineage_source\n) VALUES (\n    s.transaction_id, s.region, s.user_id, s.amount,\n    s.status, s.event_time, CURRENT_TIMESTAMP, s.lineage_source\n);\n```\n\n## Technology Comparison: Spark vs Flink\n\n| Aspect | Spark Streaming | Flink |\n|--------|-----------------|-------|\n| **Processing Model** | Micro-batching (~100ms latency) | True streaming (sub-second latency) |\n| **State Management** | Checkpoint-based | RocksDB state backend with incremental checkpoints |\n| **Event-time Processing** | Watermarks supported | First-class support with watermarks and windows |\n| **Schema Evolution** | Limited native support | Schema registry integration |\n| **Operational Complexity** | Lower (mature ecosystem) | Higher (specialized expertise) |\n| **Use Case Fit** | Batch-oriented with streaming overlay | Low-latency streaming requirements |\n\n**Recommendation**: Use **Flink** for sub-second latency requirements and complex event-time windowing; use **Spark Structured Streaming** for higher throughput with acceptable latency and teams more familiar with Spark.\n\n## Testing Strategy\n- **Unit tests**: Schema validation, masking logic, MERGE deduplication\n- **Integration tests**: End-to-end pipeline with sample data, late data scenarios\n- **Contract tests**: Validate schema registry compatibility before deployment\n- **Chaos testing**: Simulate region failures, schema evolution conflicts\n\n## Monitoring Strategy\n- **Data quality**: Great Expectations or custom validations at ingestion gates\n- **Pipeline health**: Prometheus/Grafana for throughput, latency, backpressure metrics\n- **Schema alerts**: Track schema version mismatches and evolution violations\n- **Lineage completeness**: Audit trail verification for compliance",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-19",
        "questionId": "q-8000",
        "question": "Design a multi-tenant event ingestion pipeline where 1000 tenants emit JSON events with evolving schemas (tenant_id required, event_ts required). Ingest into a lakehouse (Iceberg) with per-tenant isolation, idempotent upserts, and late data handling. Enforce data contracts via a schema registry, validate at ingress, and route nonconforming records to a quarantine sink. Include GDPR delete handling per tenant and cost/latency trade-offs; compare Spark Structured Streaming vs Flink for this scenario and outline testing strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a Schema Registry to enforce tenant_id and event_ts at ingress; Flink processes with event-time watermarks and 15-minute late data; write to Iceberg with per-tenant namespaces; upserts via MERGE I",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design an end-to-end streaming pipeline using Flink for strict event-time processing with 1-hour watermark handling. Implement a central Avro/Protobuf schema registry to enforce per-tenant contracts and validate schemas…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a central policy registry that stores per-tenant contracts and RLS rules. Tag each event with tenant_id and apply Iceberg predicate filters at read time via Spark/Flink to enforce access. Upsert w",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Spark Structured Streaming with an explicit schema: event_id, user_id, event_ts, event_type, payload (map<string,string>). Ingest from three regional folders, apply a 30-minute watermark on event_",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design scalable, compliant multi-tenant data pipelines with evolving schemas, strict contracts, and governance.\n\n## Key Concepts\n- Schema Registry for contract enforcement\n- Per-tenant isolation in a lakehouse\n- Idempotent upserts and late-data handling\n- GDPR delete and data sovereignty\n- Streaming engines trade-offs (Flink vs Spark) and observability\n\n## Code Example\n```sql\n-- Pseudo MERGE snippet for upsert into Iceberg per-tenant\nMERGE INTO iceberg.tenant_db.tenant_table AS t\nUSING staging AS s\nON t.tenant_id = s.tenant_id AND t.event_ts = s.event_ts\nWHEN MATCHED THEN UPDATE SET ...\nWHEN NOT MATCHED THEN INSERT ...;\n```\n\n## Follow-up Questions\n- How would you validate per-tenant purge latency end-to-end?\n- How would schema drift be detected and rolled out safely across tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-20",
        "questionId": "q-10097",
        "question": "Design a multi-region streaming pipeline ingesting 8 regional JSON telemetry streams (up to 60 MB/s total). Load to an Iceberg lakehouse and expose a real-time feature store for ML models. Requirements: 1) versioned feature schemas via a schema registry; 2) per-record lineage from source to feature; 3) region-aware routing and data sovereignty; 4) late data handling with configurable watermark; 5) automated drift detection and feature deprecation with a policy-as-code approach. Describe architecture, data contracts, and provide a minimal Spark SQL MERGE example for upserting a new feature version?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "We ingest 8 regional JSON telemetry streams (combined up to 60 MB/s) into an Iceberg lakehouse and expose a real-time feature store for ML. Here's the comprehensive approach: **Architecture:** - **Ingestion Layer:** 8 r…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a multi-tenant pipeline with per-region ingestion, central schema registry enforcing a canonical schema, and per-tenant MERGE upserts into the lakehouse. Use Flink for exactly-once processin",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a real-time feature store pipeline fed by 3 market feeds (tick, quote, event) at ~15k events/sec. Each event carries symbol, ts, and features. Emit per-record lineage to a metadata store; upser",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a Spark PyJob that ingests a daily 10GB JSON feed into Iceberg. Enforce per-record contracts: user_id non-null, event_ts parseable ISO-8601, action in {'click','view','purchase'}, attributes as",
            "isCorrect": false
          }
        ],
        "explanation": "This design addresses all five requirements comprehensively:\n\n1. **Versioned Schemas via Registry:** AWS Glue/Confluent stores Avro schemas with version history; each feature references schema_version in metadata, enabling reproducible ML training\n\n2. **Per-Record Lineage:** lineage_id generated at ingestion links every feature row to exact Kafka partition/offset, enabling full audit trail from source telemetry to served feature\n\n3. **Region-Aware Routing + Data Sovereignty:** Iceberg partitioned by region_id + date ensures data never leaves its region; cross-region replication is explicit and auditable\n\n4. **Late Data Handling:** 15-minute watermark with 5-minute allowed lateness; records within window are correctly joined; late records routed to dead letter queue for manual review\n\n5. **Drift Detection + Policy-as-Code:** Great Expectations runs statistical checks on each batch; OPA policies enforce automated deprecation only after 30-day notification period and drift threshold exceeded\n\nThe streaming MERGE uses foreachBatch since Spark Structured Streaming doesn't support native MERGE INTO. The staging table approach with version tracking enables proper SCD Type 2 semantics for feature versioning. The watermark configuration ensures correct event-time processing while the checkpoint location enables exactly-once processing semantics.",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-21",
        "questionId": "q-8493",
        "question": "You're maintaining a lakehouse (Iceberg) ingesting 100B events daily plus user profiles. A regulatory requirement: when a user revokes consent, past records must be masked or retroactively deleted, while preserving auditable lineage. Design an end-to-end solution that models per-record consent, propagates updates efficiently, supports retroactive masking vs deletion, handles late data, and includes data contracts and testing. Include concrete Iceberg MERGE patterns and lineage validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "A batch ETL in PySpark that: 1. **Ingestion**: Read CSV with schema (user_id STRING, event_ts STRING, action STRING, product_id STRING, ip STRING). Add a monotonically_increasing_id() as row_id and compute file-wise has…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a central schema registry and per-feed contracts. Ingest JSON/Parquet from 200 feeds, validate against feed-specific schemas, map to a canonical schema, and preserve per-record lineage. Buffer lat",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: read two regional JSON streams into a Delta Lake; validators enforce required user_id, event_ts, event_type; optional payload. Non-conforming rows go to bad_records. Normalize to a canonical sch",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Model per-record consent and a consent_delta table. Use Iceberg MERGE to retroactively DELETE or MASK sensitive fields when consent_revoked is true, preserving lineage via a lineage_id and audit table",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design consent-driven data governance across a lakehouse with retroactive changes, lineage, and testing.\n\n## Key Concepts\n- Per-record consent contracts\n- Retroactive masking vs deletion\n- Iceberg MERGE patterns\n- Per-record lineage and audit trails\n- End-to-end data contracts and testing\n\n## Code Example\n```sql\nMERGE INTO lakehouse.events AS t\nUSING consent_updates AS s\nON t.record_id = s.record_id\nWHEN MATCHED AND s.consent_revoked = true THEN DELETE\nWHEN MATCHED AND s.mask_values = true THEN UPDATE SET\n  t.user_id = NULL, t.email = NULL, t.phone = NULL;\n```\n\n## Follow-up Questions\n- How would you validate lineage and ensure performance at 100B events/day?\n- What testing strategy would you use to simulate revocation and verify results in CI/CD?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-22",
        "questionId": "q-18893",
        "question": "You're building a global ML feature store for an e-commerce platform with 12 regional tenants. Describe an end-to-end design that supports fresh features, incremental materializations, per-tenant isolation, and schema drift handling. Include a canonical schema, a hybrid streaming/batch pipeline, rollback-safe materializations, and validation/testing strategy comparing Airflow vs Dagster for scheduling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt a canonical feature schema: tenant_id, feature_name, value, timestamp, and metadata. Isolate tenants using schema separation or row-level security (RLS). Implement incremental upserts into Apache Iceberg using MER…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Design a PySpark batch job to read three regional CSV files, apply a column mapping layer to transform regional columns into a canonical schema, and enforce per-record contracts (user_id, event_ts, and action must be no…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Batch workflow: Load JSON into a Snowflake staging table, enforce per-record contracts (customer_id, event_ts, action non-null). Use MERGE into the canonical fact table on (customer_id, event_ts) for",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Read regional JSON with Spark, convert event_ts from its tz to UTC, enforce per-record contract (device_id, event_ts, metric non-null). Route non-conforming rows to bad_data. Write to Iceberg via upse",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests end-to-end feature store design for multi-tenant global platforms, focusing on data contracts, drift handling, and safe evolution under production constraints.\n\n## Key Concepts\n- Canonical schema and per-tenant isolation\n- Incremental upserts using MERGE into Iceberg\n- Hybrid streaming/batch with watermark-based windowing\n- Schema drift detection and safe evolution\n- Scheduling and observability trade-offs: Airflow vs Dagster\n\n## Code Example\n```sql\nMERGE INTO feature_store AS t\nUSING staged_features AS s\nON t.tenant_id = s.tenant_id \n  AND t.feature_name = s.feature_name \n  AND t.timestamp = s.timestamp\nWHEN MATCHED THEN UPDATE SET t.value = s.value, t.metadata = s.metadata\nWHEN NOT MATCHED THEN INSERT (tenant_id, feature_name, value, timestamp, metadata)\nVALUES (s.tenant_id, s.feature_name, s.value, s.timestamp, s.metadata);\n```",
        "difficulty": "advanced"
      },
      {
        "id": "data-engineering-q-23",
        "questionId": "q-18873",
        "question": "Design a streaming + batch pipeline for a multi-tenant analytics lakehouse. Ingest from 20 regions with evolving schemas; enforce per-row contracts via a central schema registry; mask PII at ingest; write to Iceberg partitioned by tenant and region. Use idempotent MERGE upserts, support dynamic schema evolution, and late data windows. Define data-quality gates and a cross-tenant sharing policy that preserves isolation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Leverage a dual-path: near-real-time processing with Flink streaming into Iceberg with exactly-once semantics and a canonical schema from a central registry; a batch path for audits. Enforce per-recor",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a streaming + batch pipeline for a multi-tenant analytics lakehouse that ingests from 20 regions with evolving schemas. Enforce per-row contracts via a central schema registry, mask PII at ingest, and write to Ic…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Ingest CDC from OLTP and batch feeds into an immutable lake with per-tenant isolation and a metadata catalog for lineage. Redact PII and implement differential privacy for aggregates; store raw Parque",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Decode Protobuf messages using a schema registry, enforce per-record data contracts, and route by region. Ingest into Iceberg using MERGE-based idempotent upserts keyed on (region, gateway_id, event_time), maintaining p…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests your ability to design complex multi-tenant data systems at scale, covering governance, streaming and batch processing, schema evolution, and privacy compliance.\n\n## Key Concepts\n- **Multi-tenant isolation** with cross-tenant sharing policies that preserve data isolation\n- **Schema registry** with per-row contracts and dynamic schema evolution\n- **Iceberg partitioning** by tenant and region for efficient queries\n- **Idempotent MERGE upserts** handling late-arriving data within configurable windows\n- **PII masking** applied at ingest using transformation functions\n- **Data-quality gates** using frameworks like Great Expectations or Deequ\n\n## Code Example\n```sql\nMERGE INTO lake.tenants.t_events AS t\nUSING staged_events AS s\nON t.tenant_id = s.tenant_id \n  AND t.event_id = s.event_id \n  AND t.region = s.region\nWHEN MATCHED THEN UPDATE SET *\nWHEN NOT MATCHED THEN INSERT *\n```\n\n## Follow-up Questions\n- How would you handle schema conflicts between tenants with overlapping field names?\n- What strategies would you use for cost optimization across tenants with varying data volumes?\n- How do you implement row-level security for cross-tenant analytics queries?",
        "difficulty": "intermediate"
      },
      {
        "id": "data-engineering-q-24",
        "questionId": "q-1077",
        "question": "Design a data ingestion and processing pipeline for a global ride-hailing platform that ingests 5 TB/day of operational events from multiple regional Kafka topics and batch feeds. Requirements: idempotent upserts into a table (Iceberg/Delta), handle late-arriving events, schema evolution, and partition pruning by country/date. Compare Flink vs Spark for streaming, and outline testing, monitoring, and data quality checks?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a multi-tenant validation layer backed by a per-tenant schema registry. Ingest via Kafka with idempotent producers and write to Iceberg using MERGE operations keyed by (tenant_id, event_ts, event_id) to ensure…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a dual online/offline feature store: online store for sub-10ms lookups, offline Iceberg for batch recomputation. Enforce data contracts via a schema registry; version features and enable TTL pruni",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement Apache Iceberg tables as the target with a centralized schema registry (Avro/JSON) and versioned contracts. Ingest through a micro-batch layer that validates against the current contract, masks PII, and emits…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Propose a hybrid pipeline: streaming with Flink writing to Iceberg, using idempotent upserts on (region, event_id), watermarking for late data, and Iceberg schema evolution; batch reconciliation with",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis question probes end-to-end data engineering design, covering multi-source ingest, schema evolution, late data handling, and cost-aware architecture at scale.\n\n## Key Concepts\n- Multi-source ingestion, idempotent upserts, upsert semantics\n- Schema evolution, partition pruning, Iceberg/Delta\n- Late data handling, watermarking, out-of-order events\n- Streaming vs batch trade-offs, observability\n\n## Code Example\n```javascript\n// Example pseudo outline (not executable)\nfunction ingest(){ /* ... */ }\n```\n\n## Follow-up Questions\n- How would you test idempotence across regional sources?\n- What metrics and dashboards would you establish for data quality and SLA.",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-01T10:24:01.537Z",
    "version": 1
  }
]