[
  {
    "id": "test-backend",
    "channelId": "backend",
    "channelName": "Backend",
    "title": "Backend Knowledge Test",
    "description": "Test your understanding of Backend with 25 questions.",
    "questions": [
      {
        "id": "backend-q-0",
        "questionId": "q-4589",
        "question": "You’re designing a globally scaled social feed where user timelines are partitioned by user_id across shards with replication for durability. Explain your approach to: a) shard key choice and routing for reads/writes, b) dynamic shard rebalancing to handle hotspots with minimal downtime, c) read-after-write guarantees vs. eventual consistency, and d) failure handling with replica promotion. End with ?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use Redis as the canonical source for feed keys and a separate Memcached layer for hot reads. Invalidate on write by publishing to a channel with the affected feed keys; each reader subscribes and inv",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "A concrete plan for per-tenant cache invalidation in a multi-tenant SaaS environment: 1. **Key Naming Strategy**: Use tenant-scoped keys with a clear hierarchy, such as `tenant:{tenant_id}:cache:{resource}:{entity_id}`.…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "This plan focuses on per-tenant policy enforcement at the gateway. Store TenantPolicy in a central registry and cache per request; enforce quotas with a Redis-backed token bucket keyed by tenant and protocol; implement…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Route writes to the shard primary and distribute read requests across replicas with eventual consistency. Implement a stable shard map using consistent hashing for deterministic routing, combined with a shard relocation…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical understanding of distributed systems design, specifically shard management, routing strategies, and maintaining availability during infrastructure changes for global services.\n\n## Key Concepts\n\n- Shard key strategy and request routing\n- Data replication and failover mechanisms\n- Online rebalancing and hotspot mitigation\n- Consistency models and trade-offs\n\n## Code Example\n\n```javascript\nfunction route(userId, shardCount) {\n  const idx = hash(userId) % shardCount;\n  return idx;\n}\n```\n\n## Follow-up Questions\n\n- How would you detect hotspots and trigger rebalancing automatically?\n- How would you ensure idempotent writes across replicas?\n- What metrics would you monitor to determine rebalancing thresholds?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-1",
        "questionId": "q-17017",
        "question": "Channel: backend/authentication. Difficulty: intermediate. You operate a multi-tenant gateway that accepts OIDC ID tokens, SAML assertions, and OAuth 2.0 Token Exchange, and issues per-tenant, short-lived internal JWTs. A new requirement adds TLS-bound token binding using cnf/jwk in the JWT. Describe a practical plan to implement per-tenant binding keys, rotation, and verification: (1) where to store keys and how to version them, (2) how to bind incoming tokens to the tenant key during issuance and verification, (3) how to propagate the binding to downstream services, (4) a minimal code snippet showing issuing a bound JWT and validating the binding, (5) how to audit and recover from binding key compromise?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Cache the top-N leaderboard as Redis key leaderboard:top:{limit} with a short TTL (e.g., 60s). On score update: 1) update the authoritative DB; 2) delete leaderboard:top entry; 3) optionally publish a",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Store a per-tenant binding key in a KMS with versioned KIDs. Issue internal JWTs with cnf.jwk bound to the tenant’s current key and a kid header. On verification, fetch the tenant key version and ensu",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Adopt a cache-aside pattern with per-user locks. On read, fetch sum:{userId}; if missing, acquire a Redis lock, load from DB, repopulate cache with a short TTL (60s), then release the lock. On writes,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a practical approach: a per-tenant policy catalog (tenantId, roles, fieldMasks, masking rules) as source of truth; generate REST OpenAPI per tenant and stitch GraphQL from REST/gRPC; a unified",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of token binding and multi-idp flows in production.\n\n## Key Concepts\n- Token binding with cnf\n- per-tenant key rotation\n- KMS/JWK management\n- auditing and revocation\n\n## Code Example\n```javascript\n// Issuer (simplified)\nconst tenantKwk = getTenantKey(tenantId); // {kid, jwk}\nconst token = jwt.sign({sub, iss: 'gateway', aud: tenantAud}, tenantPrivateKey, { algorithm:'ES256', header: { kid: tenantKwk.kid }, nonce: crypto.randomBytes(16).toString('hex'), cnf: { jwk: tenantKwk.jwk } });\n\n// Verifier\nconst decoded = jwt.verify(token, tenantPublicKey, { algorithms:['ES256'] });\nif (!compareJwk(decoded.cnf.jwk, getTenantBoundJwk(tenantId))) throw new Error('binding mismatch');\n```\n\n## Follow-up Questions\n- How would you test rotation without breaking active sessions?\n- What disaster recovery steps for key compromise?",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-2",
        "questionId": "q-10468",
        "question": "You're building a product catalog API backed by Redis cache and a MySQL store. Admins can update price and stock via an admin panel. Design a beginner-friendly, concrete cache-invalidation plan that evicts or refreshes the relevant cache within 200 ms of DB updates, without polling. Include (a) invalidation mechanism (Redis keyspace notifications or pub/sub), (b) keys affected (product:ID, category:index), (c) trade-offs and a minimal code snippet for the update path that triggers invalidation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a client credentials flow to issue an internal JWT bound to tenant X (audience: tenant:X). Validate issuer, exp, and audience, and consult a TTL-based revocation list before allowing access.",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a lightweight saga orchestrator for a CMS workflow coordinating Article draft → License reserve → Payment → Publish. Use event-sourcing: publish DraftCreated, LicenseReserved, PaymentProcessed,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Projection plan: maintain a read-model Orders(id, status, total, version, deleted). Apply events in order; on OrderCreated insert (status=Created, total=0, version); ItemAdded/Removed adjust total; Pa",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use Redis keyspace notifications to detect cache invalidation and a dedicated pub/sub channel to broadcast invalidations. Evict product:ID, category:ID, and search_index:term keys on DB updates; then",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical cache invalidation strategies combining Redis and Memcached, emphasizing low-latency invalidation and stampede avoidance.\n\n## Key Concepts\n- Redis keyspace notifications\n- Pub/Sub fan-out for invalidations\n- Cache stampede prevention (debounce, read-through)\n- Multivalued keys: product:ID, category:ID, search_index\n\n## Code Example\n```javascript\nconst redis = require('redis');\nconst client = redis.createClient();\n\nfunction updateProduct(id, data){\n  // update DB... \n  client.del(`product:${id}`);\n  client.publish('cache_invalidate', `product:${id}`);\n}\n```\n\n## Follow-up Questions\n- How would you scale invalidation across Redis clusters?\n- How would you handle dropped invalidation messages and ensure eventual consistency?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-3",
        "questionId": "q-14371",
        "question": "How would you design an adaptive rate limiting system that automatically adjusts limits based on system load and user behavior patterns while maintaining fairness during traffic spikes?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan a Saga orchestrated across Inventory, Kitchen, Delivery, and Payment. On order placement, emit ReserveInventory, StartKitchen, ScheduleDelivery, and InitiatePayment. If Payment fails after reserv",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use per-tenant TTLs; on cache miss, acquire a Redis lock with SETNX and short expiry; refresh from DB after validating a version; write back with the new value and TTL; release the lock safely (e.g.,",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a dynamic rate limiter using sliding windows with load-based thresholds and machine learning for behavior analysis, adjusting limits in real-time while preserving fairness through weighted allocation and prior…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use consistent-hash sharding on tenant_id to map to shards; each shard owns an immutable per-tenant log and a write-ahead log for ordering. Replicate WALs to 3 regions with Raft-like consensus locally",
            "isCorrect": false
          }
        ],
        "explanation": "An adaptive rate limiting system combines traditional algorithms with dynamic adjustments based on real-time metrics. Start with a sliding window algorithm as the foundation, tracking requests per time window for each user and the overall system. Monitor key metrics including CPU usage, memory consumption, database connection pool utilization, and response times. When system load exceeds predefined thresholds (e.g., CPU > 80%), automatically reduce rate limits proportionally across all users, with higher priority tiers receiving smaller reductions.\n\nImplement behavior analysis using statistical models to identify patterns such as traffic spikes, unusual request patterns, or potential abuse. Use machine learning algorithms to classify user behavior and adjust individual rate limits accordingly. For fairness during traffic spikes, implement weighted allocation where new users receive initial baseline limits that gradually increase based on their behavior patterns, while established users maintain their proven limits.\n\nPriority queues ensure critical requests bypass restrictions during emergencies, and circuit breakers prevent cascade failures. The system should continuously learn and adapt, maintaining detailed metrics for optimization while providing administrators with override capabilities for manual adjustments when needed.",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-4",
        "questionId": "q-11438",
        "question": "Channel: backend/cache. You run Redis as the primary cache and Memcached as a secondary read layer for article pages in a global news platform. On article updates, design a concrete, scalable cross-cache invalidation plan: (1) event schema and per-tenant namespaces, (2) publish/subscribe flow from Redis to Memcached, (3) stampede protection and eventual consistency tactics, (4) recovery when an invalidation is missed. Include data structures and a minimal code snippet showing the invalidation flow?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Scale horizontally using hash-based sharding by user_id. Map each user to a shard using: shard = hash(user_id) % NUM_SHARDS. Route all reads and writes for that user to their assigned shard; replicate each shard synchro…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement tenant-scoped policies in the gateway: load per-tenant OpenAPI contracts from a central registry, validate requests, apply per-tenant rate limits and feature flags before protocol translatio",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a central Saga orchestrator that coordinates Content, Billing, and Access services. Implement idempotent handlers and per-step compensations. Use the Outbox to ensure durable events, and versioned",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Publish an invalidation event containing tenantId, articleId, and newVersion. Redis listeners delete keys formatted as article:tenant:articleId and increment a per-tenant version counter in a fast shard; a Memcached wor…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cross-cache invalidation for multi-tenant scales with Redis as the source of truth and Memcached as a secondary layer. Requires concrete event schemas, per-tenant isolation, and practical guards against stale reads.\n\n## Key Concepts\n\n- Cache-invalidation workflow across Redis and Memcached with tenant isolation\n- Event schema design (tenantId, articleId, version) and idempotent handling\n- Stampede protection (hot-key guards, probabilistic refresh) and recovery strategies\n\n## Code Example\n\n```javascript\n// Redis publish invalidation event\nfunction publishInvalidation(tenantId, articleId, newVersion) {\n  const event = { tenantId, articleId, version: newVersion, timestamp: Date.now() };\n  redis.publish('cache:invalidate', JSON.stringify(event));\n}\n\n// Redis listener and Memcached worker\nfunction handleInvalidation(event) {\n  const key = `article:${event.tenantId}:${event.articleId}`;\n  redis.del(key);\n  redis.incr(`version:${event.tenantId}`);\n  \n  // Memcached CAS invalidation\n  memcached.gets(key, (err, data, cas) => {\n    if (data) memcached.cas(key, '', cas, 0, () => {});\n  });\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-5",
        "questionId": "q-15511",
        "question": "Context: You're building a ride-hailing backend with RideService, DriverService, and PaymentService. Implement a beginner-friendly saga using event-sourcing for a ride flow: RideRequested -> DriverAllocated -> PaymentCharged -> RideCompleted, with compensations on failures. Outline event contracts, a minimal in-process orchestrator, and an Outbox for commands. Include data structures and a small code snippet mapping RideRequested to AllocateDriver and ChargePayment, plus compensation paths?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a centralized authentication service with JWT access tokens (15-minute expiry) paired with refresh tokens (7-day expiry). Utilize Redis for session state management, token blacklisting, and rate limiting. For…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "REST: ~1-2ms latency, suitable for public APIs. GraphQL: ~2-4ms with query complexity overhead, ideal for mobile clients needing flexible data fetching. gRPC: ~0.1-0.5ms with HTTP/2 multiplexing, perfect for internal se…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a RideSaga: persist events by rideId in an EventStore (RideRequested, DriverAllocated, PaymentCharged, RideCompleted, RideCancelled). Use a lightweight in-process orchestrator + Outbox for c",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-tenant cache keys including a product version, e.g., cache:prod:{id}:tenant:{tid}:v{ver}. Store ver in product:version in Redis. On update, incr product:version; publish on channel prod_update",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses practical experience with sagas, event sourcing, and simple orchestration in a beginner-friendly scenario.\n\n## Key Concepts\n- Saga state machine per rideId\n- EventStore schema and outbox pattern\n- Idempotent handlers via correlationId/eventId\n- Compensation paths and failure handling\n\n## Code Example\n```javascript\n// minimal sketch\nclass Orchestrator {\n  handle(event){ /* map to next commands */ }\n}\n```\n\n## Follow-up Questions\n- How would you test idempotency and failure paths?\n- How would you place a simple retry vs. dead-letter strategy?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-6",
        "questionId": "q-11074",
        "question": "You're serving product detail pages from a Redis cache; underlying DB updates price/stock asynchronously. Design a per-tenant cache-invalidation strategy that avoids stampede, supports TTLs, and ensures readers see fresh data within a bounded time. Include data structures, a refresh-trigger, and a minimal Node.js snippet using Redis to cache, lock, and refresh?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a write-through cache with per-tenant TTL; invalidate on database updates via Redis Pub/Sub; attach version stamps to cache and database records. On cache miss, acquire a Redis-based lock (SETNX), refresh if v…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Propose a gateway that derives REST endpoints from OpenAPI specifications, stitches a unified GraphQL schema from microservices, and forwards GraphQL field resolvers to corresponding gRPC methods; enforce per-tenant aut…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Per-tenant Saga with a correlated state machine and idempotent handlers. Maintain a tenant-scoped outbox and a correlation ID for each job. Compensations run in reverse order: Ingest, Transcode, Water",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a Redis-backed cache-aside pattern with a single Lua script to atomically invalidate both the product cache and all dependent caches. Define keys as: `product:<id>`, `category:<cat>`, `search:*`. On update, the Lua…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates cache invalidation strategies at scale, per-tenant data isolation, and stampede protection mechanisms in Redis-based systems. It also tests understanding of Pub/Sub invalidation patterns and lightweight locking approaches.\n\n## Key Concepts\n\n- Write-through cache architecture\n- Per-tenant TTL management and version stamping\n- Distributed locking to prevent cache stampede\n- Pub/Sub-based invalidation and out-of-band refresh\n- Performance trade-offs: data freshness vs. system load\n\n## Code Example\n\n```javascript\nasync function getProduct(id, tenant) {\n  const cacheKey = `prod:${tenant}:${id}`;\n  let data = await redis.get(cacheKey);\n  if (data) return JSON.parse(data);\n  \n  const lockKey = `${cacheKey}:lock`;\n  const lock = await redis.set(lockKey, '1', 'PX', 5000, 'NX');\n  \n  if (lock) {\n    try {\n      const freshData = await fetchFromDB(id, tenant);\n      await redis.setex(cacheKey, 300, JSON.stringify(freshData));\n      return freshData;\n    } finally {\n      await redis.del(lockKey);\n    }\n  }\n  \n  await new Promise(resolve => setTimeout(resolve, 100));\n  return getProduct(id, tenant);\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-7",
        "questionId": "q-3349",
        "question": "Design a beginner-friendly exercise for a microservices backend role: You have an Order service (creates orders), an Inventory service (manages stock), and a Payment service. Implement a simple saga using events and an orchestrator: when an order is placed, reserve inventory, then process payment; if payment fails, release inventory; if inventory is insufficient, cancel order. How would you model the event stream and state transitions?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "We partition data by tenant ID using consistent hashing with virtual nodes (typically 150-200 virtual nodes per physical node) to evenly distribute load and prevent hot spots. Each tenant's data maps to a single primary…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Leverage RFC 8693 Token Exchange: map OIDC claims to RBAC, issue per-service access tokens with tight audiences, cache JWKS per tenant with proactive rotation, add cross-region revocation, and nonce-b",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use an orchestrated saga with an Event Store and CQRS read model. On order placement, emit OrderCreated, reserve inventory, then emit InventoryReserved; proceed to PaymentRequested. If payment succeed",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Implement compensating transactions with idempotent operations: release inventory via distributed lock using Redis or Zookeeper, rollback payment state using ACID-compliant transaction queue with Kafka Streams, publish…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests familiarity with distributed transaction patterns in a practical, beginner-friendly way: translating a business flow into saga steps, using events to communicate between services, and handling compensation and failure paths.\n\n## Key Concepts\n\n- Saga choreography vs orchestration\n- Event Sourcing for an audit trail\n- CQRS read/write separation\n- Idempotency and retry semantics\n- Compensation patterns for failures\n\n## Code Example\n\n```javascript\n// Minimal sketch of saga orchestration (pseudo)\n```\n\n## Follow-up Questions\n\n- How would you test idempotent handlers?\n- How would you handle out-of-order events?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-8",
        "questionId": "q-8581",
        "question": "You're building a globally distributed event store for a multi-tenant analytics platform. Ingest requires 1M events/sec per region, tenants are bursty, and data must be replayable across regions. Design the storage architecture with sharding, replication, and failure handling. Include partitioning strategy (tenant_id) and replication factor, consistency model, resharding plan, exactly-once processing, and recoverability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Versioned keys with cross-region invalidation. Use product:{id}:v{ver} with 120s TTL and store per-product versions in a Redis hash. On price/stock updates, atomically increment the version with INCR product:{id}:ver an…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Partition by tenant_id with virtual shards to handle hotspots; replicate each shard to at least 3 regions with W=2, R=2 for availability. Use per-shard leader for writes and a cross-region transaction",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Plan to scope each region with a RegionAwareEventEnvelope that carries regionId, eventVersion, and unique sagaId; use a per-region outbox and commit-log that prevents cross-region leakage. Implement d",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a two-tier cache architecture with Redis as the authoritative source and Memcached as the distributed cache layer. On any product update, increment the version key (product:{id}:ver), write the new data to Redis (pr…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to reason about multi-region sharding, replication, and rebalancing with online migration and failure handling, a realistic production concern.\n\n## Key Concepts\n- Global sharding and hotspot mitigation\n- Cross-region replication and quorum writes\n- Exactly-once ingest and idempotent producers\n- Online resharding and consistent hashing\n- CQRS for cross-tenant queries\n\n## Code Example\n```javascript\nconst shardConfig = {\n  partitionKey: 'tenant_id',\n  replication: { regions: ['us-east', 'eu-west', 'ap-south'], factor: 3, writeQuorum: 2 },\n  resharding: { strategy: 'online', trigger: 'load', msBetweenChecks: 60000 },\n  processing: { exactlyOnce: true, idempotentProducer: true }\n};\n```\n\n## Follow-up Questions\n- How would you detect and handle hotspot migrations when a tenant bursts beyond its shard's capacity?\n- How would you measure and prove the system meets RPO/RTO across regions?",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-9",
        "questionId": "q-26739",
        "question": "You're designing a global time-series store for multi-tenant SaaS. Data is written in 1-hour shards per tenant and must survive regional outages. Design a scalable sharding and replication strategy across 3 regions that isolates tenants, supports hot tenants, and controls storage costs. Compare fixed-size vs sliding-window shards, decide on replication factor, and outline a test plan for cross-region failover and data consistency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Region-aware hash sharding: map conversation_id to N shards; place primary in the local region and replicate to two other regions. Use a per-conversation sequencer to assign monotonically increasing s",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a globally distributed messaging system with per-region shards providing local strong consistency, a multi-master architecture within each region, and asynchronous cross-region replication. Implement region-based…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Normalize both OIDC and SAML tokens into an internal JWT that carries tenant and scopes. Implement a tiny per-tenant policy language (tenant+scope checks) and cache rules in memory with TTL. Example:",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement 1-hour rolling shards per tenant using a composite key (tenantId + timestamp). Replicate each shard across all three regions with quorum-based consistency (W=2, R=2) to ensure durability and read availability…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates the ability to design scalable, resilient multi-region systems with per-tenant isolation, hot-tenant mitigation, and cost control—core competencies for distributed systems engineers at senior levels.\n\n## Key Concepts\n\n- Time-based shard design and tenant isolation strategies\n- Multi-region replication with quorum-based consistency\n- Hot-tenant mitigation and dynamic shard rebalancing\n- Data lifecycle management: tiered storage, retention, compaction, and archival\n\n## Code Example\n\n```javascript\nfunction shardKey(tenantId, timestamp) {\n  const hourBucket = Math.floor(timestamp / 3600000);\n  return `${tenantId}:${hourBucket}`;\n}\n\nclass TimeSeriesStore {\n  constructor(regions, replicationFactor = 3) {\n    this.regions = regions;\n    this.replicationFactor = replicationFactor;\n    this.quorum = Math.floor(replicationFactor / 2) + 1;\n  }\n\n  async write(tenantId, timestamp, data) {\n    const key = shardKey(tenantId, timestamp);\n    const replicas = this.selectReplicas(key);\n    const writes = replicas.map(region => this.writeToRegion(region, key, data));\n    await Promise.all(writes);\n    return this.verifyQuorum(writes);\n  }\n\n  selectReplicas(shardKey) {\n    return this.regions.slice(0, this.replicationFactor);\n  }\n\n  async read(tenantId, timestamp) {\n    const key = shardKey(tenantId, timestamp);\n    const replicas = this.selectReplicas(key);\n    const reads = replicas.map(region => this.readFromRegion(region, key));\n    return this.mergeResults(await Promise.all(reads));\n  }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-10",
        "questionId": "q-23119",
        "question": "Channel: backend/authentication. Intermediate. You’re building a multi-tenant API gateway that must support OAuth 2.0/OIDC tokens plus DPoP-bound access tokens for service-to-service calls across regions. Describe a concrete plan to: (1) bind access tokens to per-tenant client certificates or mTLS fingerprints using DPoP and a replay-protection cache, (2) implement per-tenant JWKS rotation with efficient validation, (3) enforce per-tenant audience binding and scope mapping, (4) add a /validate endpoint that returns binding status for downstream services, (5) provide a minimal snippet showing DPoP validation and issuing a bound JWT?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Bind access tokens to DPoP proofs per tenant, using a replay cache keyed by DPoP nonce plus token to prevent reuse. Rotate per-tenant JWKS and validate the DPoP proof against the tenant key and the ac",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement per-tenant CQRS with an event-sourced Billing domain and an in-process Saga orchestrator. When PricingChanged for a tenant, emit VersionedEventEnvelope<Tenant, V, PriceChanged>; Saga steps B",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a per-tenant orchestrated OrderSaga with a state machine and an outbox. Key events: OrderPlaced, InventoryReserved, PaymentAuthorized, ShippingBooked, CancelRequested, SagaCompleted. Command",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a single, versioned write contract (OpenAPI/SDL/Proto) in a central registry and route REST POST /items, GraphQL addItem(input: AddItemInput), and gRPC AddItemRequest to a single write path. Imple",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world need to ensure token binding prevents replay across tenants and regions.\n\n## Key Concepts\n- DPoP proof binding\n- per-tenant JWKS rotation\n- replay protection\n- token audience and scope mapping\n\n## Code Example\n```javascript\n// Node-ish sketch: validate DPoP and issue bound JWT\nasync function issueBoundJwt(accessToken, dpopHeader, tenant){\n  const jwk = await fetchTenantJwks(tenant);\n  const valid = await verifyDPoP(dpopHeader, jwk, accessToken);\n  if(!valid) throw new Error('DPoP invalid');\n  const payload = mapToInternalClaims(parseToken(accessToken), tenant);\n  return signJwt(payload, {aud: tenant.aud, iss: 'gateway'});\n}\n```\n\n## Follow-up Questions\n- How to handle cross-tenant revocation in a global cache?\n- What are the failure modes for DPoP binding in roaming clients?",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-11",
        "questionId": "q-11085",
        "question": "Design a globally distributed messaging system for a live video conferencing platform that uses per-region shards, multi-master writes, and cross-region replication. Explain shard key strategy, conflict resolution, and write/read latencies under failover during regional outages?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a globally distributed messaging system with per-region shards providing local strong consistency, a multi-master architecture within each region, and asynchronous cross-region replication. Implement region-based…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a unified architecture by establishing a single canonical domain model and creating protocol-specific adapters. Start with a canonical Book model (id, title, author, stock) and implement three endpoints: REST…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a per-tenant Saga orchestrator coordinating PaymentService, LedgerService, and ReconciliationService. Use event-sourcing: events (PaymentInitiated, LedgerDebited, LedgerCredited, Reconciled) an",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "This plan focuses on per-tenant policy enforcement at the gateway. Store TenantPolicy in a central registry and cache per request; enforce quotas with a Redis-backed token bucket keyed by tenant and protocol; implement…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests the ability to design scalable, globally distributed systems with sophisticated sharding strategies and multi-region replication patterns. It evaluates understanding of consistency trade-offs, conflict resolution mechanisms, and system behavior during partial failures—critical skills for architecting large-scale SaaS platforms.\n\n## Key Concepts\n- Global sharding by region with data locality optimization\n- Multi-master versus master-slave replication trade-offs\n- Conflict resolution strategies: last-writer-wins, causal ordering, CRDTs\n- Consistency levels and availability guarantees",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-12",
        "questionId": "q-22254",
        "question": "Channel: backend/server-architecture. Scenario: a global chat service stores per-tenant session state in a distributed key-value store and must scale to tens of millions of tenants across two data centers. Propose a beginner-friendly plan that shards by tenantId, ensures cross-dc replication, and supports safe rebalancing with minimal disruption. Include concrete code inspiration: 1) a ShardRouter interface, 2) a simple hash-based tenant→shard mapper, 3) a basic CrossDCReplicator outline. End with the key trade-offs between consistency and latency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a Redis cache-aside pattern using tenant-scoped keys formatted as `tenant:{tid}:profile:{uid}` with appropriate TTLs. Establish per-tenant invalidation via Redis pub/sub channels `tenant:{tid}:invalidate` wher…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a Saga with OrderService and InventoryService using an in-process orchestrator. Event contracts: OrderPlaced, InventoryReserved, PaymentCaptured, plus compensations: InventoryReleased, Order",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use tenantId-based consistent hashing to map tenants to shards, with replication factor 2 across DCs. Writes go to the local shard's primary; replicate asynchronously to the other DC. Rebalance by vir",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Propose an orchestrated OrderSaga with per-order state (INIT, RESERVING, CHARGING, PREPARING, DONE, CANCELLED), using an outbox for commands and versioned events. Idempotent handlers keyed by correlat",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of practical sharding, replication, and safe rebalance in a multi-DC setup for a multi-tenant store.\n\n## Key Concepts\n\n- Consistent hashing with virtual nodes\n- Cross-DC replication and eventual consistency\n- Safe rebalancing and handoff windows\n- Tenant isolation via key prefixes\n\n## Code Example\n\n```javascript\ninterface ShardRouter {\n  getShard(key: string): string;\n  addShard(id: string, weight: number): void;\n  rebalance(): Promise<void>;\n}\n```\n\n## Follow-up Questions\n\n- How would you monitor and detect shard hotspots and rebalance safely?\n- What tests would you add to validate zero-downtime migrations?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-13",
        "questionId": "q-29535",
        "question": "Channel: backend/apis Difficulty: intermediate You're building a multi-protocol item ingestion API: REST POST /items, GraphQL addItem(input: AddItemInput), and gRPC AddItemRequest. Describe a plan to implement cross-protocol, idempotent writes using a single versioned contract in a central registry and a unified write path, with deduplication, transactional outbox, and end-to-end observability. Provide concrete mappings?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: per-user feed cache with short TTLs; leverage Redis keyspace notifications to trigger refresh on invalidation; apply singleflight to coalesce concurrent refreshes; maintain a separate invalidati",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a stable, deterministic hash of the deviceId to map devices to shards, leveraging a virtual node ring to tolerate node movement. For roaming devices, route new events to the target shard while maintaining a short re…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a single, versioned write contract (OpenAPI/SDL/Proto) in a central registry and route REST POST /items, GraphQL addItem(input: AddItemInput), and gRPC AddItemRequest to a single write path. Imple",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Plan: gate Task.priority behind a per-tenant feature flag across REST, GraphQL, and gRPC. Keep a single source of truth by aligning OpenAPI, SDL, and proto definitions with Priority optional. Implemen",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to unify multi-protocol writes, enforce contract versioning, and implement idempotency with observable cross-protocol behavior.\n\n## Key Concepts\n\n- Multi-protocol routing and normalization\n- Versioned contract governance (OpenAPI/SDL/Proto)\n- Idempotency key strategy and deduplication\n- Transactional outbox / eventual consistency\n- Cross-protocol observability and tracing\n\n## Code Example\n\n```javascript\n// Example: mapping REST body to a write event (simplified)\nfunction toWriteEventFromRest(rest) {\n  return { type: 'ItemCreated', payload: rest };\n}\n```\n\n## Follow-up Questions\n\n- How would you test idempotency across REST, GraphQL, and gRPC?\n- How would you roll out contract changes with zero-downtime and deprecation?",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-14",
        "questionId": "q-27089",
        "question": "You're building a globally distributed ride-hailing platform's transaction ledger. Data must be partitioned by city (shard) with per-city replication across regions to support low-latency reads and strict isolation. As traffic grows, design a plan for: (a) shard layout and dynamic rebalancing between cities, (b) cross-region replication strategy and consistency model, (c) failure handling and automatic recovery, (d) testing plan for data integrity and SLA adherence, including hot-city mitigation. Compare hash-based vs range partitioning and explain practical mitigations?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt city-based sharding with virtual shards; replicate each city shard across three regions using a Raft-backed log for per-city strong consistency, while allowing eventual global analytics. Failove",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a token-exchange flow (RFC 8693) to mint service_tokens with a short TTL, binding them to a per-tenant aud/iss and a rotating 'kid' for JWKS. Bind tokens to TLS/mTLS with a nonce to prevent",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose an Order aggregate that records: OrderPlaced, RestaurantReserved, DeliveryScheduled, OrderCancelled. Use an event store per order and a read-model projection updating a Status table. The saga",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: on OrderPlaced, a tiny in-process saga orchestrator emits InventoryReserve and PaymentCharge commands, storing them in an Outbox with a correlationId. If InventoryReserve fails, issue CancelOrde",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nAssesses ability to design geo-distributed, partitioned data stores with clear SLAs, isolation guarantees, and resilient recovery paths. Also tests handling of hot spots and partitioning trade-offs in production.\n\n## Key Concepts\n\n- Sharding strategy: hash-based vs range-based and per-city isolation\n- Cross-region replication and consistency model (RAFT, CDC logs, eventual vs strong)\n- Failure handling, leader election, WAL/redo recovery\n- Hot-city mitigation: dynamic rebalancing, shard splitting, pre-splitting strategies\n- Testing: chaos testing, data integrity, SLA verification\n\n## Code Example\n\n```javascript\n// Pseudo-structure: routing config and per-city replication policy\nconst routing = { city: 'cityId', shardKey: 'cityId' };\nconst policy = { regions: ['us-east', 'eu-west', 'ap-south'], replication: 'raft-logs' };\n```\n\n## Follow-up Questions\n\n- How would you monitor per-city latency vs global analytics latency and automatically rebalance?\n- What data model would you use to preserve write-order guarantees across regions, and how would you handle cross-city transactions?",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-15",
        "questionId": "q-25567",
        "question": "You're building a multi-tenant content platform using CQRS, Event Sourcing, and a Saga orchestrator. A content publish flow involves (1) licensing check with an external rights provider, (2) provisioning access in the delivery service, and (3) invoicing. If the licensing check times out after provisioning and billing, design a robust compensation strategy that guarantees idempotent replays, per-tenant isolation, and safe late-arriving events. Include a minimal orchestrator sketch and a test scaffold?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a saga state per order: fields orderId, status, correlationId, inventoryReserved, paymentCaptured. Events: OrderPlaced, InventoryReserved, InventoryReservationFailed, PaymentCaptured, PaymentF",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test empty body (400), missing required fields (422), invalid data types (400), negative quantities (422), excessive quantities (400), duplicate orders (409), and valid requests (201).",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement per-tenant JWKS caches with a short TTL (5–10 min) and background refresh; use tenant-scoped trust anchors for both OIDC and SAML; enforce audience binding per tenant and map external claims",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a central Saga orchestrator that coordinates Content, Billing, and Access services. Implement idempotent handlers and per-step compensations. Use the Outbox to ensure durable events, and versioned",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests real-world saga orchestration across multiple bounded contexts with external dependencies, focusing on failure modes, retries, and compensations.\n\n## Key Concepts\n\n- Saga orchestration across CQRS and Event Sourcing\n- Outbox pattern and idempotent event handlers\n- Replay-safe read models with per-tenant isolation\n\n## Code Example\n\n```javascript\n// Pseudo orchestrator sketch\nasync function runPublishSaga(ctx) {\n  await beginStep(ctx, 'licenseCheck');\n  await beginStep(ctx, 'provisionAccess');\n  await beginStep(ctx, 'billing');\n  // on failure: compensationChain(ctx)\n}\n```\n\n## Follow-up Questions\n\n- How would you test compensations under chaos conditions?\n- How would you evolve events and projections without breaking tenants?",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-16",
        "questionId": "q-14626",
        "question": "Channel: backend/microservices. Difficulty: beginner. Scenario: You’re building a small order-processing system using CQRS and event-sourcing. When an OrderPlaced event is received, a Saga coordinates Inventory and Payment. Describe a minimal implementation: an append-only event store per order, a simple in-process orchestrator, and a read model OrderStatus. Include data contracts (Event/Command), the flow, and a tiny code snippet showing persisting OrderPlaced and projecting OrderStatus, plus a compensation path for PaymentFailed or InventoryRejected?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Structure cache keys as `tenant:{tid}:user:{uid}:profile` with TTL 3600s. Publish invalidation messages on `invalidate:tenant:{tid}:profile` channel when profiles update. On cache miss, reload from database and repopula…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a minimal CQRS/Event-Sourcing flow for orders with a Saga coordinating Inventory and Payment services. Use an append-only EventStore per order aggregate, an in-process orchestrator for saga coordination, and a…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a central Saga orchestrator that coordinates Content, Billing, and Access services. Implement idempotent handlers and per-step compensations. Use the Outbox to ensure durable events, and versioned",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a centralized rate limiter using Redis with distributed token bucket or sliding window algorithms, synchronized across all API servers to maintain consistent limits.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests understanding of a beginner-friendly CQRS/event-sourcing workflow with cross-service coordination, compensation patterns, and concrete read model projections.\n\n## Key Concepts\n\n- Append-only EventStore per aggregate root\n- Simple saga orchestration with compensation logic\n- Read model projection from event streams\n- Event-driven communication between bounded contexts\n\n## Code Example\n\n```javascript\n// Data contracts\nclass OrderPlaced { constructor(orderId, items, amount) { this.orderId = orderId; this.items = items; this.amount = amount; } }\nclass InventoryReserved { constructor(orderId) { this.orderId = orderId; } }\nclass PaymentAuthorized { constructor(orderId) { this.orderId = orderId; } }\nclass PaymentFailed { constructor(orderId, reason) { this.orderId = orderId; this.reason = reason; } }\nclass InventoryRejected { constructor(orderId, reason) { this.orderId = orderId; this.reason = reason; } }\n\n// Append-only EventStore per order\nclass EventStore {\n  constructor() { this.events = new Map(); }\n  append(orderId, event) {\n    if (!this.events.has(orderId)) this.events.set(orderId, []);\n    this.events.get(orderId).push(event);\n  }\n  getEvents(orderId) { return this.events.get(orderId) || []; }\n}\n\n// Saga Orchestrator\nclass OrderSaga {\n  constructor(eventStore, inventoryService, paymentService) {\n    this.eventStore = eventStore;\n    this.inventory = inventoryService;\n    this.payment = paymentService;\n  }\n  async handle(event) {\n    if (event instanceof OrderPlaced) {\n      this.eventStore.append(event.orderId, event);\n      const inventoryResult = await this.inventory.reserve(event.orderId, event.items);\n      if (inventoryResult.success) {\n        this.eventStore.append(event.orderId, new InventoryReserved(event.orderId));\n        const paymentResult = await this.payment.authorize(event.orderId, event.amount);\n        if (paymentResult.success) {\n          this.eventStore.append(event.orderId, new PaymentAuthorized(event.orderId));\n        } else {\n          this.eventStore.append(event.orderId, new PaymentFailed(event.orderId, paymentResult.reason));\n          this.compensate(event.orderId);\n        }\n      } else {\n        this.eventStore.append(event.orderId, new InventoryRejected(event.orderId, inventoryResult.reason));\n      }\n    }\n  }\n  compensate(orderId) {\n    const events = this.eventStore.getEvents(orderId);\n    events.reverse().forEach(event => {\n      if (event instanceof InventoryReserved) {\n        this.inventory.release(orderId);\n      }\n    });\n  }\n}\n\n// Read Model Projection\nclass OrderStatusProjection {\n  constructor(eventStore) {\n    this.eventStore = eventStore;\n    this.orderStatus = new Map();\n  }\n  project(orderId) {\n    const events = this.eventStore.getEvents(orderId);\n    let status = 'Pending';\n    events.forEach(event => {\n      if (event instanceof PaymentAuthorized) status = 'Completed';\n      else if (event instanceof PaymentFailed || event instanceof InventoryRejected) status = 'Failed';\n    });\n    this.orderStatus.set(orderId, status);\n    return status;\n  }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-17",
        "questionId": "q-6944",
        "question": "In a backend system with microservices, implement a Notification service using event-sourcing and a simple CQRS read model. The service subscribes to events: OrderCreated, OrderCancelled, PaymentFailed, InventoryLow and updates a per-user notification feed. Describe your event schema, how you build the read model by replaying events, and how you achieve idempotent processing and deduplication across at-least-once delivery. Include sample event definitions and a minimal code sketch showing event handler, replay, and a read model projection?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Design a Notification service using event-sourcing and CQRS patterns. The service subscribes to domain events—OrderCreated, OrderCancelled, PaymentFailed, InventoryLow—and maintains a per-user notification feed through…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "A concrete plan for per-tenant cache invalidation in a multi-tenant SaaS environment: 1. **Key Naming Strategy**: Use tenant-scoped keys with a clear hierarchy, such as `tenant:{tenant_id}:cache:{resource}:{entity_id}`.…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Validate tokens by verifying the signature with per-provider JWKS, check exp, and enforce replay protection by storing used jti in a distributed cache (e.g., Redis) with TTL equal to token lifetime. B",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Partition by tenant_id with virtual shards to handle hotspots; replicate each shard to at least 3 regions with W=2, R=2 for availability. Use per-shard leader for writes and a cross-region transaction",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design an event-driven notification system with proper event-sourcing patterns, ensuring candidates understand CQRS separation, idempotent processing, and reliable event replay—critical skills for distributed microservices architectures.\n\n## Key Concepts\n- Event Sourcing with immutable events\n- CQRS read model separation\n- Idempotent event processing\n- Event replay mechanisms\n- At-least-once delivery handling\n- Deduplication strategies\n\n## Code Example\n```typescript\ninterface Event {\n  event_id: string\n  user_id: string\n  stream_id: string\n  order_id?: string\n  type: string\n  payload: any\n  version: number\n  timestamp: Date\n}\n\ninterface Notification {\n  user_id: string\n  notification_id: string\n  type: string\n  message: string\n  created_at: Date\n  read: boolean\n}\n\nclass NotificationService {\n  async handleEvent(event: Event) {\n    if (await this.isProcessed(event.event_id)) return\n    \n    switch(event.type) {\n      case 'OrderCreated':\n        await this.createNotification(event.user_id, 'order_created', `Order ${event.order_id} created`)\n        break\n      case 'OrderCancelled':\n        await this.createNotification(event.user_id, 'order_cancelled', `Order ${event.order_id} cancelled`)\n        break\n      // Additional event handlers...\n    }\n    \n    await this.markProcessed(event.event_id)\n  }\n}\n```",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-18",
        "questionId": "q-15210",
        "question": "Channel: backend/apis Advanced. You are building a unified API platform that must expose REST, GraphQL, and gRPC for a 'Review' domain. Describe a production-ready flow that ensures parity across protocols, using a single OpenAPI source, per-protocol adapters, and a shared idempotency strategy. Include explicit mappings for REST POST /reviews, GraphQL mutation addReview(input: ReviewInput), and gRPC CreateReview; cover error envelopes, tracing, rate limiting, and safe schema evolution without breaking existing clients?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a hash-based shard map with a fixed shard count (N) and replicate each shard to 2–3 nodes. Map user_id to shard = hash(user_id) mod N. Route reads/writes to the shard's primary, with quorum reads",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a minimal in-process saga orchestrator backed by an Event Store and an Outbox. Define events: OrderPlaced, PaymentApproved/Declined, InventoryReserved/Failed, OrderCompleted/Cancelled. Track",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## Solution Overview Shard by `tenantId` using a hash-based `TenantShardMapper`, with a `ShardRouter` that writes to a local primary and asynchronously replicates to a secondary DC. The `CrossDCReplicator` ensures event…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a single write path with an idempotency key stored in Redis keyed by client+request_id; REST POST /reviews uses body and Idempotency-Key header; GraphQL addReview(input: ReviewInput, extensi",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design cross-protocol consistency, idempotency, and contract-driven evolution at scale across REST, GraphQL, and gRPC.\n\n## Key Concepts\n\n- Unified, idempotent write path across REST/GraphQL/gRPC\n- Centralized idempotency store (e.g., Redis) keyed by client+request_id\n- Event-sourced write model with an outbox for eventual consistency\n- Per-protocol adapters and a single source of truth OpenAPI/SDL/proto\n- Observability: tracing, rate limiting, and safe schema evolution\n\n## Code Example\n\n```javascript\n// Pseudo: idempotent write handler sketch\nasync function createReview(req, res) {\n  const idKey = req.headers['Idempotency-Key'] || req.body.idempotencyKey;\n  const key = `idemp:reviews:${idKey}`;\n  const cached = await redis.get(key);\n  if (cached) return res.status(200).json(JSON.parse(cached));\n\n  // validate and emit event\n  const event = { id: generateId(), type: 'ReviewCreated', payload: req.body };\n  eventBus.publish(event);\n\n  const created = { id: event.id, ...req.body };\n  await redis.set(key, JSON.stringify(created), 'EX', 3600);\n  res.status(201).json(created);\n}\n```\n\n## Follow-up Questions\n\n- How would you test idempotency across REST/GraphQL/gRPC?\n- How do you handle conflicting content with the same idempotencyKey?",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-19",
        "questionId": "q-25309",
        "question": "Channel: backend/microservices\nDifficulty: advanced\nScenario: A B2B ecommerce platform uses CQRS + Event Sourcing. An OrderSaga orchestrates across four services: OrderService, PaymentService, InventoryService, and ShippingService. The flow is: PlaceOrder -> ReserveInventory -> AuthorizePayment -> CreateShipment; on any failure, trigger compensating actions (ReleaseInventory, RefundPayment, CancelShipment). Design a minimal in-process orchestrator with event contracts, idempotent handlers via correlationId, and an outbox for commands. Provide concrete events/commands, a per-tenant state machine sketch, and a small code snippet mapping events to commands and performing dedupe?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a minimal CQRS/Event-Sourcing flow for orders with a Saga coordinating Inventory and Payment services. Use an append-only EventStore per order aggregate, an in-process orchestrator for saga coordination, and a…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use cache-aside with per-user versioning. Persist keys user:<id>:data and user:<id>:ver in Redis; Memcached holds hot data. On write, increment ver and publish a cache_invalidation message. On read, f",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a cache-aside plan with per-city TTLs in Redis. On read, try GET; on miss, fetch from primary, then SETEX with city TTL. Invalidate on driver/status/location updates by deleting the city key and p",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Start a Saga on OrderPlaced. Maintain per-correlationId saga state in an event store and publish commands via an outbox. Use idempotent handlers keyed by (correlationId, event.id). Orchestrator flow:",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design distributed workflow with real systems concepts: saga orchestration, event sourcing, outbox pattern, and idempotent handlers under failure. Focuses on practical correctness and maintainability at scale.\n\n## Key Concepts\n- Saga orchestration across multiple services\n- Event sourcing and outbox integration\n- Idempotent handlers using correlationId and event.id\n- Compensating actions for partial failures\n\n## Code Example\n```javascript\n// Pseudo orchestrator sketch\nfunction onEvent(evt) {\n  const key = `${evt.correlationId}:${evt.id}`;\n  if (dedupe(key)) return;\n  markHandled(key);\n  switch (evt.type) {\n    case 'OrderPlaced':\n      enqueue('ReserveInventory', { orderId: evt.orderId, items: evt.items }, evt.correlationId);\n      break;\n    case 'InventoryReserved':\n      enqueue('AuthorizePayment', { orderId: evt.orderId, amount: evt.total }, evt.correlationId);\n      break;\n    case 'PaymentAuthorized':\n      enqueue('ShipOrder', { orderId: evt.orderId }, evt.correlationId);\n      break;\n    case 'OrderFailed':\n      enqueue('ReleaseInventory', { orderId: evt.orderId }, evt.correlationId);\n      enqueue('RefundPayment', { orderId: evt.orderId }, evt.correlationId);\n      break;\n  }\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-20",
        "questionId": "q-17722",
        "question": "You're building a globally distributed event store used by 10k tenants with variable ingest rates. Design a sharded architecture that guarantees tenant isolation, supports cross-region replication across 3 regions, and allows online shard rebalancing with zero downtime. Specify shard key (tenant_id), replication factor, consistency levels, region-failover handling, and a concrete read/write path from client to storage. Include trade-offs and an operability plan?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a single OpenTelemetry tracer across REST, GraphQL, and gRPC. Emit spans per layer (NotesService.GetNote, GraphQL.GetNote, REST.GetNote). Propagate trace context via HTTP traceparent and for",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a consistent-hash ring mapping tenant_id to shards, replicate each shard to three regions (RF=3) with W=2/R=2. Ingest: client → API gateway → shard leader → WAL → storage; Reads: nearest region →",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a composite shard key (region + user_id hash) to distribute writes and preserve locality; enable chunk pre-splitting and a small max chunk size with a balancer to mitigate hot shards. Each shard i",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a RideSaga: persist events by rideId in an EventStore (RideRequested, DriverAllocated, PaymentCharged, RideCompleted, RideCancelled). Use a lightweight in-process orchestrator + Outbox for c",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design geo-distributed, multi-region sharded stores with online rebalancing, clear read/write paths, and failure handling.\n\n## Key Concepts\n\n- Consistent hashing for shard mapping\n- Tenant isolation and data locality\n- Cross-region replication and latency trade-offs\n- Online shard rebalancing with minimal downtime\n- Read/write path and quorum settings under regional outages\n\n## Code Example\n\n```javascript\n// Pseudo-config for shard routing\nconst SHARD_COUNT = 200;\nfunction shardForTenant(tenantId) {\n  // hash to shard index\n  return hash(tenantId) % SHARD_COUNT;\n}\n```\n\n## Follow-up Questions\n\n- How would you monitor tail latency during rebalancing?\n- How would you validate failover in staging without impacting tenants?",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-21",
        "questionId": "q-19104",
        "question": "In a multi-region ecommerce backend using CQRS and event-sourcing, design a retroactive promotion saga spanning OrderService, BillingService, and InventoryService. Define event contracts, an in-process orchestrator, idempotent handlers (correlationId, eventId), and an outbox for commands. Explain per-tenant read-model migration, and include a minimal mapping from PromotionUpdated to BillingAdjust and InventoryAdjust and a dedupe check?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Store a per-tenant binding key in a KMS with versioned KIDs. Issue internal JWTs with cnf.jwk bound to the tenant’s current key and a kid header. On verification, fetch the tenant key version and ensu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a versioned PromotionUpdatedEvent per tenant and an orchestrator that emits BillingAdjust and InventoryAdjust via an outbox. Handlers must be idempotent with correlationId and eventId. Add per-ten",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a hash-based per-tenant shard ring with 3-region replication. Each shard maintains a replicated log (Raft-like) for intra-region consistency and applies cross-region eventual consistency. Enabl",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design a beginner-friendly /token-exchange flow: accept an OIDC or SAML subject token, validate it, and issue a short-lived internal JWT for a downstream service. Bind to tenant, enforce per-tenant au",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests cross-service saga design in an event-sourced, CQRS system with retroactive business rules. Assesses orchestrator logic, idempotent handlers, outbox reliability, and safe, tenant-aware migrations.\n\n## Key Concepts\n\n- Saga orchestration across Order, Billing, Inventory\n- Versioned event contracts and retroactive rules\n- Outbox patterns and idempotent command handling\n- Tenant-scoped read-model migrations and replay safety\n\n## Code Example\n\n```javascript\n// Implementation sketch: map PromotionUpdated to commands\nfunction mapPromotionUpdatedToCommands(evt) {\n  const c1 = { type: 'BillingAdjust', payload: { orderId: evt.data.orderId, delta: evt.data.promoDelta }, correlationId: evt.correlationId };\n  const c2 = { type: 'InventoryAdjust', payload: { productId: evt.data.productId, delta: evt.data.promoUnitsDelta }, correlationId: evt.correlationId };\n  return [c1, c2];\n}\n\n// simple dedupe check\nif (dedupeTable.has(evt.correlationId, evt.eventId)) {\n  return;\n}\ndedupeTable.insert(evt.correlationId, evt.eventId);\n```\n\n## Follow-up Questions\n\n- How would you rollout the migration with zero downtime?\n- How would you test idempotent replay for large event histories?",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-22",
        "questionId": "q-11879",
        "question": "Design a scalable backend for a global multi-tenant analytics service: data is ingested per-tenant, you shard by tenant_id, replicate writes to a hot replica, and must online-rebalance without downtime. Explain shard layout, replication factor, cross-tenant isolation, and live migration strategy?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a centralized versioned invalidation: store content_version in Redis as content:<id>:ver. Cache keys include the version, feed:<user>:v:<ver>. On item update, INCR content:<id>:ver and XADD to cac",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use region-based shards with 3-replica Raft groups. Rebalance via a two-phase migration: prep copy on target shard (backfill), then coordinated cutover with a lease epoch barrier. Tombstones for delet",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: Build a contract-first gateway serving per-tenant OpenAPI specifications from a central registry with versioning and deprecation support; implement a GraphQL layer that stitches REST and gRPC endpoints using data…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a hash-based range sharding strategy with tenant_id as the partition key, using consistent hashing to distribute data across shards. Each shard maintains 3-way replication (primary + two secondaries) within th…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nThis evaluates your ability to design scalable, multi-tenant storage systems with zero-downtime operational capabilities.\n\n## Key Concepts\n- **Sharding Strategy**: Tenant_id-based consistent hashing with range partitions to ensure even distribution and minimize hotspots\n- **Replication Topology**: Multi-region 3-way replication with leader-follower consensus for durability and availability\n- **Online Rebalancing**: Versioned migration system with dual-write periods and gradual traffic shifting\n- **Isolation & Security**: Per-tenant namespaces with dedicated access controls and resource quotas\n\n## Code Example\n```javascript\n// Pseudo: migration plan snippet\nasync function migrateShard(oldShard, newShard) {\n  await startTransfer(oldShard, newShard);\n  await verifyIntegrity(oldShard, newShard);\n  await switchTraffic(oldShard, newShard);\n  await completeMigration(oldShard);\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-23",
        "questionId": "q-25740",
        "question": "Channel: backend/microservices. Difficulty: advanced. Scenario: A retail platform uses CQRS + Event Sourcing across Order, Payment, and Inventory. A customer places an order; OrderPlaced triggers AuthorizePayment and ReserveInventory; on PaymentAuthorized and InventoryReserved, proceed to Settlement; on any failure, execute compensations. Design a cross-service Saga with an in-process orchestrator, per-tenant state, an outbox for commands, and idempotent handlers via correlationId; specify concrete events/commands, versioned contracts, and compensating actions. Provide a minimal state machine sketch and a small code snippet demonstrating dedupe and a compensating action?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement RFC 8693 token exchange with multi‑tenant isolation: validate incoming bearer tokens (OIDC or SAML) per tenant issuer, audience, and allowed grant_type; enforce mutual TLS for service clients with optional PKC…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement consistent hashing for sharding across multiple regions, using primary-replica replication with synchronous writes for critical data and quorum-based reads (R+W>N) for consistency.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use a 3-shard ring with replication factor 2; tenant_id -> hashToShard; shardMap holds tenant boundaries and replicas; rebalancing: mark source shard draining, move tenants to target shard, then swap",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "An in-process orchestrator tracks SagaState per correlationId: on OrderPlaced issue AuthorizePayment and ReserveInventory; on PaymentAuthorized and InventoryReserved emit Settlement; otherwise run com",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nTests orchestration of cross-service sagas with event-sourcing, focusing on idempotency, outbox patterns, and compensating actions under partial failure.\n\n## Key Concepts\n- Saga orchestration across Order/Payment/Inventory with CQRS\n- Event contracts versioning and backward compatibility\n- Outbox pattern for durable commands\n- Idempotent handlers using correlationId\n- Compensating actions mapped to failure modes\n\n## Code Example\n```javascript\nfunction isDuplicate(eventId, correlationId, store) {\n  const key = correlationId + \":\" + eventId;\n  if (store.has(key)) return true;\n  store.set(key, true);\n  return false;\n}\n```\n\n## Follow-up Questions\n- How would you test timeouts and outbox retries in this saga?\n- How do you model idempotent read models for settlement status?",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-24",
        "questionId": "q-22667",
        "question": "Channel: backend/authentication. Scenario: a multi-IdP broker for a data platform serving tenants Databricks, Instacart, Netflix. You must issue internal, short-lived JWTs for service-to-service calls. Describe an end-to-end plan to normalize SAML assertions and OIDC id_tokens into tenant JWTs, enforce per-tenant aud/iss, implement nonce/replay protection with a high-availability cache, and support revocation via a signed registry. Include a minimal code snippet for issuing a JWT with tenant, provider, sub, jti, and kid?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Per-tenant Saga with a correlated state machine and idempotent handlers. Maintain a tenant-scoped outbox and a correlation ID for each job. Compensations run in reverse order: Ingest, Transcode, Water",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "End-to-end CBJWT flow: require TLS client auth, extract leaf cert fingerprint at handshake, bind it to an internal JWT (tenant_id, aud, nonce, iat) and issue the CBJWT. Validate OIDC tokens via per-te",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a single internal JWT issuer that accepts SAML assertions and OIDC id_tokens from multiple IdPs and outputs tenant-scoped tokens for service-to-service calls. Normalize claims, enforce tenant-s",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Dependency Injection is a fundamental IoC pattern where the Spring Container manages bean instantiation and dependency resolution through ApplicationContext, promoting loose coupling by wiring collaborators via construc…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssesses cross-provider token broker design, strict per-tenant policy, and robust replay protection.\n\n## Key Concepts\n- claim normalization across IdPs\n- per-tenant audience/issuer enforcement\n- nonce/jti replay protection\n- revocation registry and key rotation\n- SAML and OIDC interoperability\n\n## Code Example\n```javascript\nfunction issueTenantJwt(normalized, opts) {\n  const payload = {\n    tenant: normalized.tenant,\n    sub: normalized.sub,\n    iss: normalized.iss,\n    aud: normalized.aud,\n    scope: normalized.scope,\n    iat: Math.floor(Date.now()/1000),\n    exp: Math.floor(Date.now()/1000) + 300,\n    jti: crypto.randomUUID(),\n  };\n  return signJwt(payload, opts.kid);\n}\n```\n\n## Follow-up Questions\n- How would you handle cross-IdP key rotation?\n- How to monitor for replay and revoke tokens promptly?",
        "difficulty": "advanced"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-08-02T05:28:05.350Z",
    "version": 1
  }
]