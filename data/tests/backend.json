[
  {
    "id": "test-backend",
    "channelId": "backend",
    "channelName": "Backend",
    "title": "Backend Knowledge Test",
    "description": "Test your understanding of Backend with 25 questions.",
    "questions": [
      {
        "id": "backend-q-0",
        "questionId": "q-13857",
        "question": "Channel: backend/caching. Difficulty: advanced. Scenario: A social app caches user feeds with Redis as the canonical fast store and Memcached as a secondary hot-cache. Describe a concrete, scalable invalidation strategy for writes that keeps both caches coherent, minimizes cache stampede, and handles TTL coherence across regions. Include data structures and a minimal code snippet showing a per-key mutex-based get/set and invalidation flow for a write?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: on OrderPlaced, a tiny in-process saga orchestrator emits InventoryReserve and PaymentCharge commands, storing them in an Outbox with a correlationId. If InventoryReserve fails, issue CancelOrde",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use Redis as the canonical source for feed keys and a separate Memcached layer for hot reads. Invalidate on write by publishing to a channel with the affected feed keys; each reader subscribes and inv",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a partitioned key-space with consistent hashing and virtual nodes to map user_id to a shard; a cluster-wide shard map in a highly available store. Replicate each shard to 3–5 replicas across regio",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Build a per-tenant Issuer that routes OIDC/SAML/OAuth/opaque inputs through a small in-memory policy engine evaluating IP, geo, time window, and deviceFingerprint. If approved, issue a short-lived JWT",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests practical mastery of multi-layer caching, invalidation patterns, and race-condition handling under high load. It probes Redis and Memcached integration, Lua-based locks, and cross-region TTL coherence.\n\n## Key Concepts\n- Cache invalidation strategies across layers\n- Redis pub/sub and keyspace notifications\n- Per-key mutex with Lua scripting to prevent stampedes\n- Write-through vs write-behind coherence\n- Multi-region TTL synchronization\n\n## Code Example\n```javascript\n// Pseudo: acquire per-key lock, fetch fresh data, set both caches\nfunction getFeed(key) {\n  if (Redis.get(key)) return Redis.get(key)\n  if (Memcache.add(key + ':lock', 1, 1)) {\n    const fresh = loadFromDB(key)\n    Redis.set(key, fresh, 300)\n    Memcache.set(key, fresh, 300)\n    Memcache.delete(key + ':lock')\n    return fresh\n  } else {\n    // wait and retry\n  }\n}\n```\n\n## Follow-up Questions\n- How would you test stampede under simulated burst?\n- How would you evolve this for cross-region replication?",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-1",
        "questionId": "q-23406",
        "question": "You are building a global API gateway that enforces per-tenant quotas using a distributed counter store. Tenants number in the millions, and the system must scale across two data centers while allowing safe shard rebalancing with minimal disruption. Propose a beginner-friendly plan to shard by tenantId into 2-4 shards, implement cross-DC replication, and support windowed bursts. Outline data model, shard lookup, write path with replication, and a simple conflict-resolution rule. Include a brief pseudo-code sketch for a ShardRouter interface and a CrossDCReplicator outline, plus key trade-offs between latency, consistency, and availability?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: adopt a cache-aside with per-tenant version tokens, invalidate on DB updates, use Redis key-prefix tenant:userid, a Redis Lua script to atomically check version and refresh, implement a lock wit",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement strict refresh-token rotation per tenant: on each refresh, issue a new refresh token, invalidate the old by jti, and bind to tenant_id, device_id, and the current PKCE verifier hash. Store t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan should shard quotas by tenantId into 2-4 shards; each shard replicates across DCs via asynchronous log-based replication; use a windowed counter (per minute) to bound bursts; safe rebalancing by",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a per-tenant saga orchestrator with explicit compensation. Route events by tenantId; aggregates are event-sourced for Order, Inventory, Payment, Shipping. Ensure idempotence with a dedupe store ke",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of practical throttling at scale, emphasizing shard routing, cross-DC replication, and rebalancing without breaking quota accounting.\n\n## Key Concepts\n- Shard routing by tenantId\n- Cross-DC replication and eventual consistency\n- Safe shard rebalancing with reconciliation\n- Per-bucket conflict resolution\n\n## Code Example\n```javascript\n// ShardRouter interface\ninterface ShardRouter {\n  shardForTenant(tenantId: string): string;\n}\n\n// Cross-DC replicator outline\nclass CrossDCReplicator {\n  constructor(localStore, remoteStore, config) {}\n  async replicateWrite(key, value) { /* ... */ }\n}\n```\n\n## Follow-up Questions\n- How would you handle clock skew in a multi-DC setup?\n- What metrics would you surface to detect shard hot spots?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-2",
        "questionId": "q-7730",
        "question": "Design a globally distributed time-series storage system for telemetry with per-tenant sharding and dynamic shard rebalancing. Data must be replicated across three regions with low write latency, and reads should remain fast even during rebalancing. Explain shard layout, rebalancing protocol, replication mechanism, and how you achieve exactly-once writes and monotonic reads under regional failures?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant shards with a composite key (tenant_id, day_bucket) and separate Raft groups per shard across three regions. Dynamic rebalancing via a central metadata service and consistent hashing wi",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a single, versioned write contract (OpenAPI/SDL/Proto) in a central registry and route REST POST /items, GraphQL addItem(input: AddItemInput), and gRPC AddItemRequest to a single write path. Imple",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a connection pool manager for aiohttp using a semaphore to limit concurrent connections, exponential backoff for retrying failed requests, and circuit breaker pattern to gracefully degrade under high load and…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use per-tenant versioned keys with the pattern `tenant:{tid}:product:{pid}:v{ver}`. On each update, atomically increment the tenant's version counter and publish the new version to signal cache invalidation. Reads first…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a globally scalable time-series store with per-tenant isolation, dynamic rebalancing, and cross-region replication while preserving strong read/write guarantees.\n\n## Key Concepts\n\n- Time-series partitioning and per-tenant sharding\n- Dynamic shard rebalancing with minimal downtime\n- Cross-region replication and strong vs. eventual consistency\n- Exactly-once semantics, idempotency, and read repair\n\n## Code Example\n\n```javascript\nfunction shardKey(tenantId, ts) {\n  const bucket = Math.floor(ts / (1000 * 60 * 60 * 24)); // daily bucket\n  return `${tenantId}:${bucket}`;\n}\n```\n\n## Follow-up Questions\n\n- How would you monitor shard skew and automatic rebalancing performance?\n- How would you test failover, PITR, and recovery time objectives (RTO) in production?",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-3",
        "questionId": "q-26739",
        "question": "You're designing a global time-series store for multi-tenant SaaS. Data is written in 1-hour shards per tenant and must survive regional outages. Design a scalable sharding and replication strategy across 3 regions that isolates tenants, supports hot tenants, and controls storage costs. Compare fixed-size vs sliding-window shards, decide on replication factor, and outline a test plan for cross-region failover and data consistency?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a dynamic rate limiter using sliding windows with load-based thresholds and machine learning for behavior analysis, adjusting limits in real-time while preserving fairness through weighted allocation and prior…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use a stable, deterministic hash of the deviceId to map devices to shards, leveraging a virtual node ring to tolerate node movement. For roaming devices, route new events to the target shard while maintaining a short re…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Define a single internal User model and three adapters that project this model into REST OpenAPI, GraphQL SDL, and gRPC proto. REST: GET /users/{id} returns User; GraphQL: type User; query user(id: ID",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement 1-hour rolling shards per tenant using a composite key (tenantId + timestamp). Replicate each shard across all three regions with quorum-based consistency (W=2, R=2) to ensure durability and read availability…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates the ability to design scalable, resilient multi-region systems with per-tenant isolation, hot-tenant mitigation, and cost control—core competencies for distributed systems engineers at senior levels.\n\n## Key Concepts\n\n- Time-based shard design and tenant isolation strategies\n- Multi-region replication with quorum-based consistency\n- Hot-tenant mitigation and dynamic shard rebalancing\n- Data lifecycle management: tiered storage, retention, compaction, and archival\n\n## Code Example\n\n```javascript\nfunction shardKey(tenantId, timestamp) {\n  const hourBucket = Math.floor(timestamp / 3600000);\n  return `${tenantId}:${hourBucket}`;\n}\n\nclass TimeSeriesStore {\n  constructor(regions, replicationFactor = 3) {\n    this.regions = regions;\n    this.replicationFactor = replicationFactor;\n    this.quorum = Math.floor(replicationFactor / 2) + 1;\n  }\n\n  async write(tenantId, timestamp, data) {\n    const key = shardKey(tenantId, timestamp);\n    const replicas = this.selectReplicas(key);\n    const writes = replicas.map(region => this.writeToRegion(region, key, data));\n    await Promise.all(writes);\n    return this.verifyQuorum(writes);\n  }\n\n  selectReplicas(shardKey) {\n    return this.regions.slice(0, this.replicationFactor);\n  }\n\n  async read(tenantId, timestamp) {\n    const key = shardKey(tenantId, timestamp);\n    const replicas = this.selectReplicas(key);\n    const reads = replicas.map(region => this.readFromRegion(region, key));\n    return this.mergeResults(await Promise.all(reads));\n  }\n}\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-4",
        "questionId": "q-20548",
        "question": "You're building a globally distributed telemetry store for a ride-hailing platform with 100k tenants and heterogeneous load. Describe a scalable sharding and replication strategy that supports per-tenant isolation, dynamic rebalancing, and cross-region recovery. Include shard layout, hot-tenant handling, replication/consistency guarantees, failure modes, and a concrete write path for writeTelemetry(tenantId, event)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an orchestrated saga with an Event Store and CQRS read model. On order placement, emit OrderCreated, reserve inventory, then emit InventoryReserved; proceed to PaymentRequested. If payment succeed",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "You're building a globally distributed telemetry store for a ride-hailing platform with 100k tenants and heterogeneous load. Describe a scalable sharding and replication strategy that supports per-tenant isolation, dyna…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Check token expiration, scope mismatch, and refresh token revocation. Debug by validating token claims, checking client configuration, and reviewing token storage.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a shared service layer with separate API adapters. Use data loaders in GraphQL resolvers to prevent N+1 queries and apply REST-specific caching strategies. Optimize database queries independently for each API…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question evaluates your ability to design scalable, shard-aware architectures with cross-region replication and per-tenant isolation—critical requirements for large, latency-sensitive platforms like ride-hailing services.\n\n## Key Concepts\n\n- Consistent hashing versus range partitioning for tenant data distribution\n- Cross-region replication, quorum models, and latency trade-offs\n- Hot-tenant detection and live shard rebalancing strategies\n- Data isolation, tombstone handling, garbage collection, and disaster recovery procedures\n\n## Code Example\n\n```python\ndef shard_for(tenant_id: str, num_shards: int) -> int:\n    \"\"\"Determine shard using consistent hashing.\"\"\"\n    return hash(tenant_id) % num_shards\n\ndef write_telemetry(tenant_id: str, event: dict, shard_map: dict) -> None:\n    shard_id = shard_for(tenant_id, len(shard_map))\n    primary, replicas = shard_map[shard_id]\n    \n    # Write to quorum of replicas\n    for node in [primary] + replicas:\n        async_write(node, tenant_id, event)\n```",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-5",
        "questionId": "q-17153",
        "question": "Channel: backend/server-architecture. Difficulty: advanced. Scenario: You run a geo-distributed, real-time recommendations service that ingests billions of events daily. Design a globally distributed, sharded event store with region-local writes and cross-region replication. Specify shard key, replication topology, a cross-region sequencer, and an idempotent write path. Include data model Event{user_id, event_type, ts, payload}, partitioning, and failure handling; map in-region write and cross-region merge to concrete endpoints and reads?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use a centralized policy engine (OPA) with Rego policies and JWT claims to enforce per-tenant field-level permissions across REST, GraphQL, and gRPC. Ingest tenant context and user roles from JWT claims into OPA input,…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Use user_id as the shard key for balanced distribution across regions. Replicate each shard to three regions with a Raft-backed in-region quorum for strong consistency. A regional sequencer assigns monotonically increas…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Design a contract-first API gateway that unifies auth, error payloads, and tracing across REST, GraphQL, and gRPC. Centralize schemas in a registry: OpenAPI for REST, SDL for GraphQL, and proto for gR",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a canonical UserProfile model (DTO + protobuf) behind per-protocol adapters. REST: POST /users with Idempotency-Key header; GraphQL: mutation createUser(input, clientMutationId); gRPC: CreateUse",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design cross-region sharding and replication with low latency reads, strong intra-region consistency, and safe cross-region merges under failures.\n\n## Key Concepts\n- **Shard key**: user_id ensures even data distribution and colocates user events for efficient reads\n- **In-region replication**: Raft-style quorum (2 of 3 replicas) provides strong consistency with low-latency writes\n- **Global sequencing**: Regional sequencers coordinate via Paxos/Raft to assign monotonically increasing global timestamps\n- **Idempotent writes**: Composite key (user_id, global_seq, event_type) deduplicates retried operations\n- **Failure handling**: Write-ahead log (WAL) for durability, outbox pattern for atomic cross-region updates, replay queues for recovery, and backpressure to handle traffic spikes\n\n## Code Example\n```javascript\n// Idempotent event append with cross-region merge\nasync function appendEvent(event) {\n  const globalSeq = await sequencer.nextTimestamp();\n  const idempotencyKey = `${event.user_id}:${globalSeq}:${event.event_type}`;\n  \n  // Check for duplicates\n  if (await cache.exists(idempotencyKey)) {\n    return { status: 'duplicate', seq: globalSeq };\n  }\n  \n  // Write to local region with Raft quorum\n  const writeResult = await raftGroup.write({\n    key: `shard_${hash(event.user_id)}`,\n    value: { ...event, global_seq: globalSeq, idempotency_key: idempotencyKey }\n  });\n  \n  // Async replication to other regions\n  await replicateCrossRegion(event, writeResult.quorumAck);\n  \n  return { status: 'success', seq: globalSeq };\n}\n\n// Cross-region merge for reads\nasync function readUserEvents(userId, region) {\n  const localEvents = await readRegion(userId, region);\n  const remoteEvents = await Promise.all(\n    otherRegions.map(r => readRegion(userId, r))\n  );\n  \n  // Merge by global_seq, deduplicate by idempotency_key\n  return mergeByTimestamp([...localEvents, ...remoteEvents], 'global_seq');\n}\n```",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-6",
        "questionId": "q-6540",
        "question": "You're building a multi-tenant backend auth service that accepts tokens from different IdPs (OIDC and SAML). For a beginner-friendly task, describe a concrete end-to-end flow to issue short-lived, tenant-bound access tokens with replay protection. Include: 1) how to normalize incoming tokens, 2) how to validate per-tenant issuer/audience and signatures via a metadata cache, 3) the structure of the issued JWT (tenant, sub, scope, iat, exp, jti, nonce), 4) how to implement replay protection using a per-tenant Redis store with a 10-minute TTL, 5) edge cases like clock skew and cross-tenant token reuse. Show small code snippets for issuing and verifying tokens?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Normalize OIDC id_token and SAML assertions to a common Principal {tenant, sub, roles}. Validate issuer/audience per tenant and signature via a per-tenant metadata cache. Issue a JWT with iss=tenant,",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use cache-aside with per-key versioning. When a write (pricing/inventory/promotions) occurs, publish an invalidation for the affected keys to a bus (Kafka). Each cache stores a version; on hit, if ver",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: define a single JWT-based auth flow across REST, GraphQL, and gRPC; implement a shared error envelope {code, message, details} and map service-errors to it. REST: /users/{id} returns {code,...}.",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Map tenant IDs to shards with virtual nodes; use a central shard registry to rebalance live without downtime; replicate writes regionally with per-tenant quotas and quorum writes (W=2,R=2) and a per-t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests the ability to design a multi-IdP, tenant-scoped token flow with replay protection and cross-tenant semantics.\n\n## Key Concepts\n- Normalization of tokens across IdPs\n- Tenant-scoped metadata cache\n- JWT structure and claims\n- Replay protection with Redis TTL\n- Clock skew handling and cross-tenant semantics\n\n## Code Example\n```javascript\n// Issuance (pseudocode)\nfunction issueToken(principal, tenant) {\n  const jti = crypto.randomUUID();\n  const payload = { iss: tenant, sub: principal.sub, aud: 'api', scope: principal.scope, iat: now(), exp: now()+600, jti, nonce: crypto.randomBytes(16).toString('hex'), tenant };\n  const token = signJWT(payload, tenantKey);\n  redis.set(`nonce:${tenant}:${jti}`, '1', 'EX', 600);\n  return token;\n}\n\n// Verification (pseudocode)\nfunction verifyToken(token) {\n  const payload = verifyJWT(token);\n  if (!redis.exists(`nonce:${payload.tenant}:${payload.jti}`)) throw new Error('Replay');\n  return payload;\n}\n```\n\n## Follow-up Questions\n- How would you scale nonce storage across millions of tenants?\n- How would you rotate IdP keys and invalidate old tokens?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-7",
        "questionId": "q-28870",
        "question": "Channel: backend/server-architecture\nDifficulty: beginner\nScenario: A global feature-flag service stores flag definitions and per-tenant overrides in a distributed KV store. It shards by flagKey and replicates to two data centers for low latency. As flags grow and new regions are added, design a beginner-friendly plan to shard by flagKey, ensure cross-DC replication, and rebalance with minimal disruption. Include concrete code inspiration: 1) a ShardRouter interface, 2) a simple flagKey→shard mapper, 3) a CrossDCReplicator outline. End with trade-offs between consistency and latency during rebalancing?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan: Shard by `flagKey` using a stable hash-based mapper, replicate to two data centers with per-flag versioning for conflict resolution, and implement a `ShardRouter` that routes `flagKey → shardId`. During rebalancin…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: isolate tenants with per-tenant event store and read models; orchestrator coordinates Inventory, Payment, and Shipping using a long-running Saga with event-driven choreography; use a durable out",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Hash-based partitioning on tenant_id with 3 regional replicas. Use a consistent-hash ring to map tenants to shards; replication uses multi-master with CRDT-like tombstones for conflict resolution. Rea",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Implement a pluggable auth layer that accepts OIDC JWTs and SAML assertions, normalizes them into a single internal JWT, and enforces tenant and audience checks via a simple policy engine. Use a share",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis question tests practical understanding of scaling and sharding through the domain of feature flags. It evaluates familiarity with cross-data-center replication strategies and safe rebalancing at a beginner level.\n\n## Key Concepts\n- Sharding by `flagKey` with stable hash-based mapping\n- Cross-data-center replication with versioning for conflict resolution\n- Safe rebalance strategies using consistent hashing with virtual nodes\n- Handling in-flight requests during shard migrations\n- Trade-offs between latency and consistency during rebalancing\n\n## Code Example\n```java\n// 1. ShardRouter Interface\npublic interface ShardRouter {\n    int route(String flagKey);\n    void updateRoutingTable(Map<Integer, List<String>> newMapping);\n}\n\n// 2. Simple flagKey → shard mapper using consistent hashing\npublic class ConsistentHashRouter implements ShardRouter {\n    private final int numShards;\n    private final HashFunction hashFn;\n\n    public ConsistentHashRouter(int numShards) {\n        this.numShards = numShards;\n        this.hashFn = Hashing.murmur3_128();\n    }\n\n    @Override\n    public int route(String flagKey) {\n        return Math.abs(hashFn.hashString(flagKey, StandardCharsets.UTF_8).asInt()) % numShards;\n    }\n\n    @Override\n    public void updateRoutingTable(Map<Integer, List<String>> newMapping) {\n        // Handle migration: route in-flight requests to both old and new shards\n    }\n}\n\n// 3. CrossDCReplicator outline\npublic class CrossDCReplicator {\n    private final DataCenter primary;\n    private final DataCenter secondary;\n    private final VersionTracker versionTracker;\n\n    public void replicate(FlagUpdate update) {\n        long version = versionTracker.nextVersion(update.flagKey());\n        FlagUpdate versionedUpdate = update.withVersion(version);\n        \n        primary.apply(versionedUpdate);\n        CompletableFuture<Void> sync = secondary.applyAsync(versionedUpdate);\n        \n        // Handle conflicts using last-writer-wins or custom resolution\n        sync.join();\n    }\n}\n```\n\n## Trade-offs: Consistency vs. Latency During Rebalancing\n\n| Approach | Consistency | Latency | Use Case |\n|----------|-------------|---------|----------|\n| **Read from both shards** | Strong | Higher (fan-out read) | Critical flags |\n| **Read from new shard only** | Eventual | Lower | Non-critical flags |\n| **Dual-write during migration** | Strong | Higher | All flags |\n| **Quorum writes (W > N/2)** | Configurable | Moderate | Balanced needs |\n\nDuring rebalancing, prefer eventual consistency for non-critical flags to maintain low latency, while ensuring quorum-based replication for flags requiring strong consistency.",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-8",
        "questionId": "q-5144",
        "question": "You're building a poly-protocol API gateway that accepts REST, GraphQL, and gRPC and routes to a mixed backend (REST, GraphQL, and gRPC services). Propose a concrete plan to: (1) translate REST calls to GraphQL queries and gRPC invocations with minimal latency, (2) publish an OpenAPI spec that mirrors the backend capabilities as REST, including schemas and examples, (3) enforce multi-tenant JWT auth with per-tenant audiences and rotates JWKS, (4) implement idempotency via request-id dedupe cache, (5) bridge streaming (GraphQL subscriptions and gRPC streams) to REST clients, (6) outline failure modes, retries, and backoff. Include data structures and a small translation snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose PlaceOrderSaga across Inventory, Payments, Shipping. Orchestrator issues InventoryReserve, then PaymentCharge, then ShippingSchedule; on any failure apply compensations: RestockInventory, Refu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a tenant-aware gateway architecture that leverages Redis-backed feature flags to control access to REST, GraphQL, and gRPC endpoints on a per-tenant basis. Implement a centralized OpenAPI specification management…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan a poly-protocol gateway that translates REST calls to GraphQL queries and gRPC invocations with minimal latency, while exposing an OpenAPI REST surface that mirrors backend capabilities. Enforce",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Test pagination by verifying correct page numbers, page sizes, total counts, and boundary conditions like page=1, page=last, and invalid page numbers return appropriate responses.",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nBridging REST, GraphQL, and gRPC exposes gaps in schema, latency, and security. The candidate should articulate concrete translation layers, OpenAPI mapping, and robust auth with per-tenant controls.\n\n## Key Concepts\n- Poly-protocol gateways and protocol translation\n- REST to GraphQL and gRPC mapping\n- OpenAPI surface generation from heterogeneous backends\n- Multi-tenant JWT validation and JWKS rotation\n- Idempotency with request-id dedupe and caching\n- Streaming bridges: GraphQL subscriptions, gRPC streams to REST (SSE/chunked)\n- Failure modes, retries, and backoff strategies\n\n## Code Example\n```javascript\nfunction translateRestToGraphQL(restPath, method, body) {\n  // Map REST path to a GraphQL operation; derive operationName, variables\n  // Example: GET /user/{id} -> query User(id: $id) { ... }\n  // Return { query, variables }\n}\n```\n\n## Follow-up Questions\n- How would you test translation latency under load?\n- How would you handle schema drift between REST OpenAPI and GraphQL schemas?\n- What metrics would you collect for end-to-end tracing across protocols?",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-9",
        "questionId": "q-30041",
        "question": "Channel: backend/caching\\nDifficulty: beginner\\nTopics: redis, memcached, cache-invalidation\\nQuestion: You're building a multi-tenant catalog service and implement a two-tier cache with Redis as the hot tier and Memcached as the backing tier. For each tenant, use a dedicated namespace and TTLs. Describe the end-to-end read path for a cache miss, per-tenant key naming, tier promotion on miss, and invalidation on updates? Provide a concrete get_item(tenant_id, item_id) flow and a minimal Python snippet that seeds Redis and Memcached with TTLs?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Propose a PaymentSaga coordinating Payment, Ledger, and Notification. Use a per-tenant orchestrator with correlationId; events: PaymentRequested, PaymentAuthorized, LedgerDebited, LedgerCredited, Refu",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Design a two-tier cache per tenant with Redis as the hot tier and Memcached as the backing tier. Use a key naming convention of `tenant:{tenantId}:item:{itemId}` for tenant isolation. **Read path on cache miss:** 1. Che…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Issue a 15-min access JWT and a rotate-on-use refresh token bound to a device fingerprint. In Redis store refresh:{jti} -> {userId, fingerprint, exp}. On refresh validate fingerprint, revoke old jti,",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "End-to-end CBJWT flow: require TLS client auth, extract leaf cert fingerprint at handshake, bind it to an internal JWT (tenant_id, aud, nonce, iat) and issue the CBJWT. Validate OIDC tokens via per-te",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests the ability to design a practical multi-tenant caching solution with tiered caching, including namespace isolation, promotion policies, and invalidation workflows under realistic latency constraints.\n\n## Key Concepts\n\n- **Cache-aside pattern**: The application checks the cache first, falls back to the database on miss, and populates the cache\n- **Tiered caching**: Redis serves as the hot tier for low-latency access, while Memcached acts as a backing tier to reduce database load\n- **Per-tenant namespaces**: Isolating tenant data prevents cross-tenant data leakage and simplifies multi-tenant management\n- **Write-through invalidation**: On updates, both cache tiers are invalidated to maintain consistency",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-10",
        "questionId": "q-13136",
        "question": "Channel: backend/microservices. Difficulty: beginner. Scenario: A marketplace where a user submits content for monetization. The ContentModerationService must approve before the content becomes Visible and Revenue credits the creator. Design a minimal Saga using event-sourcing and CQRS: define events ContentSubmitted, ModerationApproved, ModerationRejected, ContentVisible, RevenueCredited; orchestrate with an in-process saga manager, implement idempotent handlers via correlationId, and apply an outbox for commands. Provide event contracts, a read-model sketch, and a small code snippet mapping events to commands and a dedupe example?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a cache-aside pattern with tenant-scoped keys: tenant:{tenantId}:product:{productId}. On read: GET; if miss, fetch from DB, then SET with per-tenant TTL. Invalidation on updates via a tenant",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Define events ContentSubmitted, ModerationApproved, ModerationRejected, ContentVisible, RevenueCredited. The orchestrator issues ModerationRequest on ContentSubmitted; on approval, publish ContentVisi",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Use a stable, deterministic hash of the deviceId to map devices to shards, leveraging a virtual node ring to tolerate node movement. For roaming devices, route new events to the target shard while maintaining a short re…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Design session-bound internal JWTs: include `sid` (session ID), `tenant_id`, `sub`, `jti` (JWT ID for replay prevention), and `aud` (audience). Set TTL to 15 minutes. Persist active `sid`/`jti` entries in Redis per tena…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests understanding of a beginner-friendly, real-world saga with event-sourcing and CQRS: how to coordinate cross-service work, model event contracts, and ensure idempotent handlers with an outbox.\n\n## Key Concepts\n- Saga orchestration vs choreography in a write-side workflow\n- Event contracts and read-model sketching for status tracking\n- Outbox pattern and correlation-based deduplication\n\n## Code Example\n```javascript\n// Map write events to commands (simplified)\nfunction mapEventToCommand(evt){\n  switch(evt.type){\n    case 'ContentSubmitted':\n      return { type: 'ModerationRequest', payload: { contentId: evt.contentId, correlationId: evt.id } }\n    case 'ModerationApproved':\n      return { type: 'PublishContent', payload: { contentId: evt.contentId } }\n    case 'ModerationRejected':\n      return { type: 'HideContent', payload: { contentId: evt.contentId } }\n  }\n}\n\n// Deduplication (idempotent handler)\nif (outbox.has(correlationId)) return; outbox.add(correlationId);\n```\n\n## Follow-up Questions\n- How would you test idempotence guarantees and read-model integrity?\n- What are trade-offs of in-process vs distributed saga orchestration for this flow?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-11",
        "questionId": "q-539",
        "question": "What is dependency injection in Spring and how does it improve application design?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Scale horizontally using hash-based sharding by user_id. Map each user to a shard using: shard = hash(user_id) % NUM_SHARDS. Route all reads and writes for that user to their assigned shard; replicate each shard synchro…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Dependency Injection is a fundamental IoC pattern where the Spring Container manages bean instantiation and dependency resolution through ApplicationContext, promoting loose coupling by wiring collaborators via construc…",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Saga state: orderId, currentStep, version. Steps: CartVerified -> InventoryReserved -> PaymentHeld -> OrderCommitted. Use an append-only event store, projections, and periodic snapshots. Idempotency v",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: isolate tenants with per-tenant event store and read models; orchestrator coordinates Inventory, Payment, and Shipping using a long-running Saga with event-driven choreography; use a durable out",
            "isCorrect": false
          }
        ],
        "explanation": "## Why Asked\nTests mastery of Spring's IoC container implementation and enterprise architecture patterns for scalable backend systems.\n\n## Key Concepts\n- Inversion of Control (IoC) Container\n- ApplicationContext lifecycle management\n- Bean post-processors and dependency wiring\n- Constructor injection vs field vs setter injection\n- Dependency resolution and proxy generation\n- Singleton vs prototype scope\n- @ComponentScan and autowiring strategies\n- Circular dependency resolution\n\n## Code Example\n```\n@Service\n@Transactional\npublic class UserService {\n    private final UserRepository userRepository;\n    private final CacheManager cacheManager;\n    \n    @Autowired\n    public UserService(UserRepository userRepository, \n                       @Qualifier(\"redisCacheManager\") CacheManager cacheManager) {\n        this.userRepository = userRepository;\n        this.cacheManager = cacheManager;\n    }\n    \n    @PostConstruct\n    public void init() {\n        // Bean initialization logic\n    }\n}\n\n@Configuration\n@EnableTransactionManagement\n@ComponentScan(basePackages = \"com.example\")\npublic class AppConfig {\n    @Bean\n    @Primary\n    public UserRepository userRepository(DataSource dataSource) {\n        return new JpaUserRepository(dataSource);\n    }\n}\n```\n\n## Follow-up Questions\n- How does Spring resolve circular dependencies?\n- What's the difference between @Autowired and @Inject?\n- How do you implement custom bean post-processors?\n- What are the implications of different bean scopes in distributed systems?",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-12",
        "questionId": "q-22978",
        "question": "Channel: backend/authentication; Difficulty: beginner; You're building a policy-driven auth gateway that normalizes OIDC or SAML tokens into an internal JWT and gates access with per-endpoint policy language. How would you implement a minimal version: 1) tenant-aware normalization, 2) in-memory TTL policy cache per tenant, 3) a sample policy for /payments requiring payments:write, 4) an OpenAPI security snippet for the internal JWT, 5) a SAML-to-claims adapter, 6) a middleware that enforces policy before downstream calls?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Plan to enforce per-tenant alg policies and key rotation via a tenant-key cache (short TTL) and alg/iss/aud binding. JWKS cache keyed by tenant+kid with a revalidation gate to prevent stampedes. Valid",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a multi-tenant OnboardSaga coordinating TenantAuth, Billing, DataIngestion, and Analytics services. Use CQRS + Event Sourcing with per-tenant read models and versioned events, orchestrating all operations via…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Normalize both OIDC and SAML tokens into an internal JWT that carries tenant and scopes. Implement a tiny per-tenant policy language (tenant+scope checks) and cache rules in memory with TTL. Example:",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use a consistent-hash ring with virtual shards, partitioning by instrument_id or customer_id to spread load. Replicate writes to three regions asynchronously for latency. Hot shards trigger split, reh",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to blend multiple token types, implement a small, pluggable policy engine, and ensure tenant-scoped access with TTL caching. It also touches SAML adapters and middleware integration.\n\n## Key Concepts\n- Token normalization, per-tenant policy language, TTL cache\n- SAML-to-claims adapter, middleware enforcement\n- Edge cases: clock skew, cache invalidation, tenant isolation\n\n## Code Example\n```typescript\n// Pseudo-implementation sketch\ntype Claims = { tenant:string; scopes:string[]; sub:string }\nfunction normalize(token:string):Claims { /* validate OIDC/SAML and map to internal claims */ }\nfunction loadPolicy(tenant:string, path:string):Policy|undefined { /* TTL cache */ }\nfunction authorize(token:string, path:string):boolean {\n  const c = normalize(token);\n  const p = loadPolicy(c.tenant, path);\n  return p ? c.scopes.includes(p.requiredScope) && c.tenant===p.tenant : false;\n}\n```\n\n## Follow-up Questions\n- How would you propagate policy updates across a multi-instance deployment?\n- How would you test the policy engine and TTL cache behavior?\n- How would you handle token revocation and clock skew in this design?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-13",
        "questionId": "q-13529",
        "question": "Channel: backend/authentication. You're building a beginner-friendly authentication gateway that must accept **OIDC** or **SAML** tokens and support a lightweight **token-exchange** endpoint to issue service-scoped internal JWTs (inspired by RFC 8693). Describe a concrete flow: (1) validate incoming tokens and map to an internal JWT with tenant and scopes, (2) implement a minimal /token-exchange that takes a subject token and returns a short-lived internal JWT for a downstream service, (3) TTL-based revocation cache and per-tenant audience checks, (4) a small middleware to enforce service-scoped access. Include concrete snippets for: 1) the OpenAPI security section for the exchanged token, 2) a SAML-to-claims adapter, 3) a middleware that validates audience and scope?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a single OpenTelemetry tracer across REST, GraphQL, and gRPC. Emit spans per layer (NotesService.GetNote, GraphQL.GetNote, REST.GetNote). Propagate trace context via HTTP traceparent and for",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a multi-tenant OnboardSaga coordinating TenantAuth, Billing, DataIngestion, and Analytics services. Use CQRS + Event Sourcing with per-tenant read models and versioned events, orchestrating all operations via…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Design a beginner-friendly /token-exchange flow: accept an OIDC or SAML subject token, validate it, and issue a short-lived internal JWT for a downstream service. Bind to tenant, enforce per-tenant au",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Use per-tenant key namespaces (tenant:{tid}:prod:{pid}) and a version token. Read path: fetch version; on miss, query DB and seed both data and version. Write path: update DB, bump tenant:{tid}:ver, a",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nTests ability to design token-exchange, multi-idp support, and revocation basics at a beginner level.\n\n## Key Concepts\n- OIDC/SAML input normalization\n- Token-exchange pattern (RFC 8693-like)\n- Tenant-scoped audience and service scopes\n- TTL-based revocation cache\n\n## Code Example\n```javascript\n// Sketch: /token-exchange handler validates subject token, issues internal JWT with tenant+scope\n```\n\n## Follow-up Questions\n- How would you test the /token-exchange flow for token replay or drifted claims?\n- What are the risks of a TTL revocation cache and how would you mitigate them?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-14",
        "questionId": "q-16498",
        "question": "In a backend service that serves user profiles, Redis caches profiles with a 5-minute TTL. Writes can come from multiple services. Design a beginner-friendly, concrete plan to handle cache invalidation and avoid stale reads: (1) how to invalidate the cache on profile updates, (2) how to prevent cache stampede on misses, (3) how to maintain eventual consistency after writes. Include a minimal code snippet showing an invalidation flow and a safe lazy-refresh?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Load per-tenant contracts from a central registry containing OpenAPI specs and proto definitions, with versioned migrations and backward-compatibility checks. Validate compatibility before routing, and enable zero-downt…",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Test empty body (400), missing required fields (422), invalid data types (400), negative quantities (422), excessive quantities (400), duplicate orders (409), and valid requests (201).",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use cache-aside: on read, if Redis miss, fetch from DB and cache for 5 minutes; on update, delete the key and publish an invalidation event; prevent stampede with per-key Redis lock (SET NX PX) during",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "An orchestration-based Saga for refunds: start a RefundCommand when payment is settled; on success, move the order forward in the fulfillment pipeline. If the refund fails or times out after the shipment label has been…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nReason: tests understanding of cache invalidation, race conditions, and Redis basics in a beginner-friendly way.\n\n## Key Concepts\n\n- Cache-aside pattern\n- Cache stampede prevention with distributed locks\n- Invalidation events and TTL usage\n- Simple versioning for consistency\n\n## Code Example\n\n```javascript\n// Invalidation on update\nasync function updateUser(userId, data) {\n  db.updateUser(userId, data)\n  redis.del('user:' + userId)\n  redis.publish('cache_invalidate', userId)\n}\n\n// Lazy refresh on read\nasync function getUser(userId) {\n  const cache = redis.get('user:' + userId)\n  if (cache) return JSON.parse(cache)\n  const lockKey = 'lock:user:' + userId\n  const gotLock = redis.set(lockKey, '1', { NX: true, PX: 1000 })\n  if (gotLock) {\n    const user = db.query('SELECT * FROM users WHERE id = ?', userId)\n    redis.set('user:' + userId, JSON.stringify(user), { EX: 300 })\n    redis.del(lockKey)\n    return user\n  } else {\n    await sleep(50)\n    return getUser(userId)\n  }\n}\n```\n\n## Follow-up Questions\n\n- How would you adapt this to Redis streams for invalidation propagation?\n- How would you measure stampede risk and tune TTLs accordingly?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-15",
        "questionId": "q-18841",
        "question": "Channel: backend/authentication. Intermediate. You're building a multi-tenant API gateway that accepts OIDC ID tokens and SAML 2.0 assertions to mint per-tenant, short-lived internal JWTs. A new requirement is nonce-based replay protection. Describe a concrete plan to: (1) validate OIDC and SAML tokens and map to internal claims including tenant_id, user_id, and scopes; (2) cache per-tenant JWKS and SAML metadata with rotation policy; (3) enforce per-tenant audience and issuer; (4) implement a scalable distributed nonce store with TTL to prevent replay; (5) expose a /validate endpoint that returns effective claims for a token without full decoding, and show a minimal code example for issuing a new JWT with a nonce and for nonce verification during validation?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Validate OIDC and SAML tokens, extract tenant_id, user_id, and scopes; cache per-tenant JWKS and SAML metadata with automatic rotation; enforce per-tenant audience (aud) and issuer (iss) validation; implement a distribu…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a connection pool manager for aiohttp using a semaphore to limit concurrent connections, exponential backoff for retrying failed requests, and circuit breaker pattern to gracefully degrade under high load and…",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Map OIDC/SAML claims to a normalized JWT: subject, tenant_id, groups, scope; derive issuer/audience per tenant; fetch per-tenant JWKS with rotation flag; enforce consent via policy store; use Redis fo",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Two-DC, hash-based sharding for feature flags. ShardRouter.GetShard(flagName) returns shardID. Each shard maintains two replicas; writes are asynchronously replicated to the other DC with a simple vec",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical replay-protection patterns in multi-tenant SSO architectures, along with real-world token normalization from OIDC and SAML protocols, JWKS key rotation, and designing scalable validation endpoints.\n\n## Key Concepts\n\n- Token validation across OIDC and SAML standards\n- Per-tenant JWKS rotation and SAML metadata caching\n- Nonce-based replay protection with time-to-live (TTL) enforcement\n- Internal JWT issuance including tenant_id, user_id, scopes, and nonce\n\n## Code Example\n\n```javascript\n// Issue internal token with nonce (Node.js)\nconst jwt = require('jsonwebtoken');\n\nasync function issueInternalToken({ tenant_id, user_id, scopes, nonce }) {\n  const payload = {\n    tenant_id,\n    sub: user_id,\n    scopes,\n    nonce,\n    iat: Math.floor(Date.now() / 1000),\n    exp: Math.floor(Date.now() / 1000) + 300 // 5-minute expiry\n  };\n  return jwt.sign(payload, getInternalSigningKey(tenant_id), { algorithm: 'RS256' });\n}\n\n// Validate nonce during token validation\nasync function validateAndConsumeNonce(tenant_id, nonce) {\n  const key = `nonce:${tenant_id}:${nonce}`;\n  const exists = await redis.setnx(key, '1');\n  if (!exists) {\n    throw new Error('Replay detected: nonce already used');\n  }\n  await redis.expire(key, 300); // Match token TTL\n  return true;\n}\n\n// /validate endpoint - lightweight claim extraction\napp.post('/validate', async (req, res) => {\n  const { token } = req.body;\n  const [header] = token.split('.');\n  const decodedHeader = JSON.parse(Buffer.from(header, 'base64'));\n  const tenant_id = decodedHeader.tenant_id;\n  \n  const claims = await extractClaimsWithoutFullDecode(token, tenant_id);\n  return res.json({ valid: true, claims });\n});\n```",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-16",
        "questionId": "q-30526",
        "question": "You have an endpoint POST /api/orders that accepts a JSON body with order items. Describe how you would test the edge cases and error handling for this endpoint, and what HTTP status codes you would expect to receive?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Test empty body (400), missing required fields (422), invalid data types (400), negative quantities (422), excessive quantities (400), duplicate orders (409), and valid requests (201).",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use region-based shards with 3-replica Raft groups. Rebalance via a two-phase migration: prep copy on target shard (backfill), then coordinated cutover with a lease epoch barrier. Tombstones for delet",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Validate OIDC ID tokens or SAML 2.0 assertions, extract tenant_id and scopes, normalize them, and issue an internal JWT with audience bound to the downstream service. Store token_jti in an in-memory TTL cache for quick…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate OIDC and SAML tokens, extract tenant_id, user_id, and scopes; cache per-tenant JWKS and SAML metadata with automatic rotation; enforce per-tenant audience (aud) and issuer (iss) validation; implement a distribu…",
            "isCorrect": false
          }
        ],
        "explanation": "For comprehensive API testing of this endpoint, you should cover multiple edge case scenarios. First, test with an empty request body - expect 400 Bad Request since the payload is required. Next, test with missing required fields like items array - expect 422 Unprocessable Entity which indicates validation errors specific to missing fields. For invalid data types (passing a string where an array is expected), return 400 Bad Request. Test with negative quantity values in items - should return 422 since business logic validation failed. Test with quantities exceeding reasonable limits (like 10,000 units) - return 400 with a message about exceeding maximum order size. Test duplicate order submissions using idempotency keys - expect 409 Conflict if the order was already processed. For valid payloads, expect 201 Created with the order details in the response body including a generated order ID.\n\nUse HTTP status codes correctly: 201 for successful creation, 400 for malformed JSON or invalid format, 422 for semantic validation errors (missing fields, invalid values), 409 for conflicts like duplicate resources. Always return descriptive error messages and error codes in the response body for easier debugging.",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-17",
        "questionId": "q-24473",
        "question": "Channel: backend/cache\nDifficulty: intermediate\nYou're building a high-throughput social feed caching layer using Redis and Memcached. Describe a practical plan to keep per-user feeds fresh, prevent cache stampede on invalidation, and handle cross-region replicas. Include data model for a cached feed entry, and a minimal code sketch showing an atomic invalidation/refresh triggered by a write to the source of truth?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Build a per-tenant Issuer that routes OIDC/SAML/OAuth/opaque inputs through a small in-memory policy engine evaluating IP, geo, time window, and deviceFingerprint. If approved, issue a short-lived JWT",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Take incoming OIDC ID token or SAML assertion, map to internal JWT with claims: sub, tenant, scope, aud, exp, jti. Use a tenant-scoped audience and 5–15 min TTL. Maintain a TTL-based revocation cache",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Propose a gateway that derives REST endpoints from OpenAPI specifications, stitches a unified GraphQL schema from microservices, and forwards GraphQL field resolvers to corresponding gRPC methods; enforce per-tenant aut…",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: per-user feed cache with short TTLs; leverage Redis keyspace notifications to trigger refresh on invalidation; apply singleflight to coalesce concurrent refreshes; maintain a separate invalidati",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\nReal-world: cache invalidation is tricky at scale; tests knowledge of Redis/ Memcached features, race conditions, and cross-region consistency.\n\n## Key Concepts\n- cache invalidation strategies; stampede prevention; singleflight; keyspace notifications; cross-region replication.\n\n## Code Example\n```lua\n-- Redis Lua script: atomically invalidate and refresh per-user feed\nlocal key = KEYS[1]\nlocal refreshing = redis.call('GET', key..':refreshing')\nif not refreshing then\n  redis.call('SET', key..':refreshing', '1', 'EX', 5)\n  -- enqueue background refresh (implementation-specific)\n  redis.call('PUBLISH', 'refresh:'..key, '1')\n  return 1\nelse\n  return 0\nend\n```\n\n## Follow-up Questions\n- How would you test stampedes under bursty writes?\n- How would you adapt for multi-region consistency and TTL alignment?",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-18",
        "questionId": "q-8142",
        "question": "You're building a backend gateway that must support OIDC/OAuth2 and SAML IdPs for hundreds of tenants via a service mesh. Describe a concrete plan to: (1) validate OIDC tokens and SAML assertions with per-tenant audience logic, (2) bind JWTs to a device nonce and use PKCE for public clients, and (3) rotate JWKS and manage per-tenant dynamic client registrations, and (4) implement scalable cross-region revocation and token introspection. Include data structures and a minimal issuance snippet?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Tenant-scoped validation: load per-tenant JWKS and IdP metadata; verify OIDC tokens (aud/iss/nonce) and SAML assertions (Signature, Conditions, AudienceRestriction) against tenant config. JWT binding:",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Use a cache-aside approach: on read, fetch key feed:user:<id>; if miss, compute and cache for 5 minutes. Invalidation: on actions (post/like/follow) publish to feed-inval:<id> and have a worker delete",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Use Redis keyspace notifications to detect cache invalidation and a dedicated pub/sub channel to broadcast invalidations. Evict product:ID, category:ID, and search_index:term keys on DB updates; then",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Outline a Saga across Ingestion, Enrichment, Indexing: persist every step as tenant-scoped events; publish to per-tenant outbox; on Enrichment failure, emit compensations to revert ingestion (delete d",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question probes how to architect a multi-IdP authentication layer that scales across tenants, focusing on correctness and operational concerns rather than theory.\n\n## Key Concepts\n\n- OIDC token validation (audience, issuer, nonce) and SAML assertion validation (Signature, Conditions, AudienceRestriction)\n- JWKS rotation and per-tenant metadata caching\n- JWT binding with per-device nonce and PKCE for public clients\n- Refresh token rotation and reuse detection\n- Scalable, cross-region revocation and token introspection\n\n## Code Example\n\n```javascript\n// Minimal token issuance example\nfunction issueJwt(payload, kid) {\n  const header = { alg: 'RS256', kid };\n  const token = jwt.sign(payload, getPrivateKeyForKid(kid), {\n    algorithm: 'RS256',\n    header,\n    expiresIn: '15m'\n  });\n  return token;\n}\n```\n\n## Follow-up Questions\n\n- How would you handle IdP metadata rotation without downtime?\n- What are the security implications of per-tenant token binding and how would you test them?",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-19",
        "questionId": "q-21538",
        "question": "Channel: backend/caching\nDifficulty: intermediate\nYou're building a multi-tenant e-commerce backend caching product searches and price lookups in Redis and Memcached. Describe a concrete, scalable plan for per-tenant cache invalidation when a product price or availability changes. Include: 1) event publishing per tenant, 2) Redis data structures to track tenants and invalidation tombstones, 3) race/stampede mitigation, 4) cache hydration strategy across a fleet, and 5) a minimal code sketch showing invalidation publish and cache miss handling?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "## Multi-Tenant Cache Invalidation Strategy When a product price or availability changes, publish an invalidation event to a per-tenant channel. Use the write-through pattern: update the database first, then publish the…",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan a Saga orchestrated across Inventory, Kitchen, Delivery, and Payment. On order placement, emit ReserveInventory, StartKitchen, ScheduleDelivery, and InitiatePayment. If Payment fails after reserv",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Build a per-tenant Issuer that routes OIDC/SAML/OAuth/opaque inputs through a small in-memory policy engine evaluating IP, geo, time window, and deviceFingerprint. If approved, issue a short-lived JWT",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Adopt a unified versioning approach (REST v1, GraphQL v1, gRPC v1) and a shared JWT auth via a single middleware. Include claims: aud, iss, tenant, roles. Standardize errors: REST {code,message,detail",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Matters\n\nThis question tests your ability to design cache systems for multi-tenant e-commerce platforms. You need to demonstrate understanding of:\n\n1. **Tenant Isolation**: How to isolate tenant data while sharing infrastructure efficiently\n2. **Cache Consistency**: How to keep caches consistent across distributed systems\n3. **Stampede Prevention**: How to prevent performance problems when many users request the same missing data\n4. **Multi-Cache Systems**: Understanding both Redis and Memcached strategies\n\n## Key Concepts Explained\n\n### Per-Tenant Invalidation Events\nWhen any product data changes, you must notify only tenants affected by that change. Publishing to tenant-specific channels ensures efficient, targeted invalidation without flooding all tenants.\n\n### Tombstones with TTL\nA tombstone marks data as deleted or invalid. Using TTL ensures tombstones auto-cleanup, preventing memory bloat. This approach handles eventual consistency by allowing reads to verify data freshness against the tombstone version.\n\n### Race Condition Handling\nWithout proper mitigation, multiple concurrent requests for the same uncached item will all hit the database simultaneously (cache stampede). Distributed locking ensures only one request fetches data while others wait.\n\n### Cache Hydration\nActive tenants benefit from immediate invalidation and on-demand rehydration. Idle tenants need background workers to pre-warm cache, ensuring acceptable latency on first access.\n\n### Redis vs Memcached\nRedis offers richer data structures (Pub/Sub, Streams, Lua scripting) making it ideal for complex invalidation logic. Memcached is simpler with consistent hashing via mcrouter, better for high-throughput, simple key-value workloads.",
        "difficulty": "intermediate"
      },
      {
        "id": "backend-q-20",
        "questionId": "q-8280",
        "question": "You're building a food-delivery backend with Order, Restaurant, and Delivery services. Implement a beginner-friendly saga using CQRS and Event Sourcing: place an order (OrderPlaced), reserve restaurant and delivery slots (RestaurantReserved, DeliveryScheduled), and handle compensation (OrderCancelled) if the restaurant declines or payment fails. Describe event schemas, aggregate design for Order, and a minimal projection that keeps a real-time order status (orderId, status, ETA)?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Architect a global, sharded ingestion path with tenant+topic as the partition key, using a log-based core (Kafka or Pulsar) with geo-replication. Ensure exactly-once delivery via transactional produce",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Propose an Order aggregate that records: OrderPlaced, RestaurantReserved, DeliveryScheduled, OrderCancelled. Use an event store per order and a read-model projection updating a Status table. The saga",
            "isCorrect": true
          },
          {
            "id": "c",
            "text": "Hash-based partitioning on tenant_id with 3 regional replicas. Use a consistent-hash ring to map tenants to shards; replication uses multi-master with CRDT-like tombstones for conflict resolution. Rea",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Validate SAML first, verify signature using cached IdP certs (refresh from metadata). Build a JWT with iss from IdP, aud as tenant/service, and include standard jti. Short expiry (15 minutes). For rev",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nThis tests applying CQRS and Event Sourcing to a practical domain, showing how events drive state and how a simple saga coordinates steps with compensations.\n\n## Key Concepts\n- Event Sourcing\n- CQRS\n- Saga/Orchestration\n- Projections/read models\n\n## Code Example\n```javascript\n// pseudo-code illustrating event append and projection\nconst events = [{type:'OrderPlaced', data:{orderId:'o1',...}}]\neventStore.append('o1', events)\nreadModel.upsert('o1', {orderId:'o1', status:'Placed', eta:null})\n```\n\n## Follow-up Questions\n- How would you implement idempotent replay and deduplication? how to handle partial failures and retries?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-21",
        "questionId": "q-26441",
        "question": "Channel: backend/authentication; Difficulty: advanced; Topics: jwt, oauth2, oidc, saml. You are building a high-security API gateway that must support both user-attested AuthN (OIDC/SAML) and device-attested AuthN via FIDO2 attestation. The gateway issues an internal JWT containing tenant, user_id, device_id, and a per-tenant nonce. Outline end-to-end flow and provide concrete snippets: 1) OpenAPI security for the internal JWT, 2) a FIDO2 attestation verifier, 3) middleware that binds device and user, enforces audience, and prevents replay?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use an onboarding Saga with a correlationId per user. Events: UserCreated, CreditGranted, CreditFailed. An in-process orchestrator tracks state: INIT → CREDIT_GRANTED → DONE or ROLLBACK. Deduplicate b",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "I would define a canonical Event model stored in a schema registry, with per-protocol adapters generated from a shared source. Use versioned contracts (v1/v2) and a deprecation window, with contract t",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "## End-to-End Authentication Flow The authentication flow proceeds as follows: 1. **User Authentication**: Validate the OIDC/SAML assertion against tenant-specific JWKS, enforcing correct `audience` and `issuer` claims.…",
            "isCorrect": true
          },
          {
            "id": "d",
            "text": "Hash tenant_id to 8 shards initially; replicate each shard to 2 additional nodes (replication factor 3). Monitor shard load and split hot shards into two sub-shards with online migration. Use per-shard connection pools…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Question Is Asked\n\nThis question evaluates the candidate's ability to architect multi-layered authentication systems that combine user attestation (OIDC/SAML) with device attestation (FIDO2), while enforcing per-tenant policies and replay protection.\n\n## Key Concepts Demonstrated\n\n| Concept | Implementation |\n|---------|----------------|\n| JWT Issuance | Internal tokens scoped to tenant audience with device bindings |\n| FIDO2 Attestation | Credential verification and device-to-user binding within tenant context |\n| Per-Tenant JWKS | Tenant-specific key rotation and metadata distribution |\n| Audience Validation | Enforcing token scope to prevent cross-tenant abuse |\n| Replay Protection | Nonce tracking to detect and block token reuse attacks |",
        "difficulty": "advanced"
      },
      {
        "id": "backend-q-22",
        "questionId": "q-10138",
        "question": "You're building a read-heavy product catalog for multiple tenants. Cache per-tenant SKU data in Redis with keys like t:{tenant}:sku:{sku}. When updates occur in Catalog/Inventory services, design a cache-invalidation flow that avoids stampedes and stays consistent across Redis and Memcached. Include: (a) invalidation method (pub/sub vs keyspace events), (b) how to refresh, (c) per-tenant TTL choices, and (d) a minimal snippet showing an invalidate path?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Use per-tenant keys in Redis, e.g. t:{tenant}:sku:{sku}. On updates, Catalog/Inventory publish a cache_invalidate message; a listener deletes t:{tenant}:sku:{sku}. The next read repopulates from sourc",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Plan: on OrderPlaced, a tiny in-process saga orchestrator emits InventoryReserve and PaymentCharge commands, storing them in an Outbox with a correlationId. If InventoryReserve fails, issue CancelOrde",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Implement a single write path with an idempotency key stored in Redis keyed by client+request_id; REST POST /reviews uses body and Idempotency-Key header; GraphQL addReview(input: ReviewInput, extensi",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Plan: tag every event with `tenantId` and `articleId`, orchestrating Encoding → Thumbnails → CDN via a per-tenant outbox. Ensure idempotence using a `StepState` keyed by `(tenantId, articleId, step)` and replay-safe han…",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\n\nThis question tests practical cache invalidation decisions in multi-tenant backends, including per-tenant keys, redis vs memcached tradeoffs, and stampede prevention.\n\n## Key Concepts\n\n- Cache invalidation patterns in multi-tenant stores\n- Redis vs Memcached differences (key eviction, TTLs, pub/sub, keyspace events)\n- Stampede prevention (locks, double-checked refresh)\n- TTL strategy and data freshness guarantees\n\n## Code Example\n\n````javascript\n// Publisher (on update)\npubsub.publish('cache_invalidate', `t:${tenant}:sku:${sku}`);\n\n// Listener (cache invalidation)\ncache.del(`t:${tenant}:sku:${sku}`);\n````\n\n## Follow-up Questions\n\n- How would you test correctness under bursty updates?\n- How would you handle multi-tenant hot keys and pre-warming?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-23",
        "questionId": "q-4998",
        "question": "You're building a multi-tenant analytics service that ingests time-series data from dashboards. Data must be sharded by tenant_id, replicated for fault tolerance, and handle 1000+ tenants with varying write rates. Propose a concrete sharding plan, replication factor, and how you'd handle hot tenants and resharding with minimal downtime?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Implement a single write path with an idempotency key stored in Redis keyed by client+request_id; REST POST /reviews uses body and Idempotency-Key header; GraphQL addReview(input: ReviewInput, extensi",
            "isCorrect": false
          },
          {
            "id": "b",
            "text": "Implement a phased migration from SAML to OIDC with parallel flows. Add a claim-mapping layer translating SAML attributes to OIDC claims and preserving role/audience constraints. JWTs with short lifet",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Plan: validate external token per-tenant metadata, map claims to a unified internal JWT schema, issue short-lived access tokens, refresh as needed, and check a TTL revocation cache before issuing toke",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Hash tenant_id to 8 shards initially; replicate each shard to 2 additional nodes (replication factor 3). Monitor shard load and split hot shards into two sub-shards with online migration. Use per-shard connection pools…",
            "isCorrect": true
          }
        ],
        "explanation": "## Why This Is Asked\n\nTests ability to design a practical, scalable storage layer using basic sharding concepts, replication, and online resharding without heavy ops.\n\n## Key Concepts\n\n- Sharding strategy (tenant_id-based)\n- Replication factor and failure tolerance\n- Handling hot tenants via shard splitting\n- Online resharding with minimal downtime\n- Consistency and visibility across shards\n\n## Code Example\n\n```javascript\n// Simple shard mapper (conceptual)\nfunction shardForTenant(tenantId, shardCount) {\n  let hash = 0;\n  for (let i = 0; i < tenantId.length; i++) hash = (hash * 31 + tenantId.charCodeAt(i)) % shardCount;\n  return Math.abs(hash);\n}\n\n// Shard splitting logic\nfunction splitHotShard(shardId, newShardId) {\n  // Migrate half the tenants to new shard\n  // Update routing table atomically\n  // Maintain dual-write during migration\n}\n```\n\n## Follow-up Questions\n\n- How would you handle cross-tenant analytics queries?\n- What's your strategy for backup and disaster recovery?\n- How do you ensure consistency during shard migrations?",
        "difficulty": "beginner"
      },
      {
        "id": "backend-q-24",
        "questionId": "q-7716",
        "question": "You're building a Redis-backed cache layer in front of a Product service. As a beginner, provide a concrete cache-invalidation plan covering (a) cache-aside with TTLs, (b) write-through vs write-around considerations, (c) a simple Redis pub/sub invalidation channel for cross-instance updates, and (d) a lightweight guard against cache stampede. Include a small get-or-set code flow and a publish-invalidate sketch?",
        "type": "single",
        "options": [
          {
            "id": "a",
            "text": "Adopt cache-aside: on read, fetch from Redis; if miss, load from DB and SETEX with a 5–10 min TTL. On writes, invalidate the cache and push an invalidate event via Redis pub/sub. To avoid stampede, ac",
            "isCorrect": true
          },
          {
            "id": "b",
            "text": "Implement a client credentials flow to issue an internal JWT bound to tenant X (audience: tenant:X). Validate issuer, exp, and audience, and consult a TTL-based revocation list before allowing access.",
            "isCorrect": false
          },
          {
            "id": "c",
            "text": "Saga state: orderId, currentStep, version. Steps: CartVerified -> InventoryReserved -> PaymentHeld -> OrderCommitted. Use an append-only event store, projections, and periodic snapshots. Idempotency v",
            "isCorrect": false
          },
          {
            "id": "d",
            "text": "Use a cache-aside pattern with tenant-scoped keys: tenant:{t}:item:{id}. On read, fetch from Redis/Memcached; on miss, load from DB, then cache with TTL (e.g., 5–10 min). On write, update DB, delete t",
            "isCorrect": false
          }
        ],
        "explanation": "## Why This Is Asked\nAssess practical grasp of cache invalidation, TTL design, cross-instance notifications, and stampede prevention in real services.\n\n## Key Concepts\n- Cache-aside pattern\n- TTL and eviction timing\n- Pub/Sub invalidation across instances\n- Lightweight distributed locks (SETNX) to prevent stampede\n- Trade-offs between consistency and availability\n\n## Code Example\n```python\nimport redis, time\n\nr = redis.Redis(host='localhost', port=6379, db=0)\n\ndef get_item(id):\n    key = f\"item:{id}\"\n    v = r.get(key)\n    if v is not None:\n        return v\n    if r.setnx(f\"lock:{key}\", 1):\n        r.expire(f\"lock:{key}\", 5)\n        v = load_from_db(id)\n        r.setex(key, 600, v)\n        r.delete(f\"lock:{key}\")\n        return v\n    time.sleep(0.05)\n    return r.get(key)\n\ndef update_item(id, data):\n    save_to_db(id, data)\n    r.delete(f\"item:{id}\")\n    r.publish(\"cache:invalidate\", f\"item:{id}\")\n```\n\n## Follow-up Questions\n- How would you unit-test and simulate stampede conditions?\n- What 2-3 metrics indicate your cache-invalidation strategy is healthy?",
        "difficulty": "beginner"
      }
    ],
    "passingScore": 70,
    "createdAt": "2026-09-16T07:18:34.967Z",
    "version": 1
  }
]